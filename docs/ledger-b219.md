# B219 执行台账

任务：80ca63b4-c416-4f4e-ba70-555310679c16
卡号：B219
分支：cards/B219-charter

## 记录

- 2026-08-23：`pwd && rg --files -g '!*node_modules*' | sed -n '1,160p' && git status --short --branch && find . -maxdepth 3 -type f \\( -iname '*ledger*' -o -iname '*log*' -o -iname '*plan*' -o -iname 'AGENTS.md' \\) -print`；原始输出确认工作树为 `/root/.handoff/worktrees/80ca63b4`，当前分支为 `## cards/B219-charter`，现有计划目录为 `docs/superpowers/plans/`，未发现既有 B219 台账。
- 2026-08-23：读取 `docs/superpowers/specs/2026-08-23-b219-permission-precondition-helper.md`、`docs/superpowers/plans/2026-08-23-b202-pty-test-root.md`、`docs/ledger-b186.md`；原始输出确认 B219 spec 已批准、现状核查定义四处恒红与三处已有守卫，且本节点需把函数名/签名交给 plan 定义。
- 2026-08-23：`id -u; go test ./internal/client ./internal/config ./internal/executor/grok ./internal/release -run 'TestCursorRootFallsBackToCwdWhenHomeUnwritable|TestCursorRootErrorNamesBothPaths|TestLoadStripUpdateDoesNotBlockOnSaveFailure|TestSyncAuthKeepsTaskCopyWhenWriteFails|TestReadCursorPermissionDeniedIsReported|TestActivateUnwritableDir' -count=1`；原始输出为 `0`、四个包均 `ok`（client 0.011s、config 0.008s、grok 0.010s、release 0.008s）。当前沙箱 euid 为 0 但该基线筛选测试未红；这不能替代目标 root executor 的 DAC_OVERRIDE 复核，计划验收仍须保留真实探针分支与变异测试。
- 2026-08-23：分包 `go test ... -v` 基线复核；原始输出显示 `TestReadCursorPermissionDeniedIsReported` 在 `cursor_layout_test.go:77` 因 `root 无视权限位，本用例无意义` 跳过，`TestActivateUnwritableDir` 在 `install_test.go:349` 因 `root 无视目录权限，跳过`，其余四个目标断言为 PASS；config/grok 的真实错误均为 `permission denied`。该结果确认现有 euid 守卫会丢掉可测覆盖，且不能作为通用能力判定。
- 2026-08-23：`codegraph` 查询前先执行 `command -v codegraph`；原始输出为 `codegraph: command not found`。随后以 `rg`/`nl -ba` 复核调用面：四个恒红测试在 `internal/client/cursordir_test.go:39-59,80-100`、`internal/config/config_test.go:468-490`、`internal/executor/grok/authsync_test.go:252-...`，已有 mode 守卫在 `internal/client/cursor_layout_test.go:75-99` 与 `internal/release/install_test.go:334-356`；`internal/prochost/fence_inherit_test.go:139-181` 的守卫针对 RLIMIT，不是文件 mode。覆盖债：本轮未使用 codegraph，以上符号与调用面以 grep/nl 复核。
- 2026-08-23：`go doc testing.TB; go doc os.Chmod; go doc os.CreateTemp; go doc os.OpenFile` 及 Go 源码定位命令；文档输出确认 `testing.TB` 提供 `Cleanup`/`Skipf`，`os.Chmod` 的平台语义不同，`os.CreateTemp` 负责创建并打开临时文件且由调用者删除，`os.OpenFile` 是指定 flag 的打开入口；`/usr/local/go` 为 `go1.26.1`，源码位置为 `testing/testing.go:889,1238-1242,1293-1299`、`os/file.go:410,644-647`、`os/tempfile.go:35-39`。命令同时原样产生多条缓存写入失败，例如 `go: writing stat cache: mkdir /root/go/pkg/mod/cache/download/github.com/charmbracelet/x/conpty: read-only file system`；该缓存写入失败未被归因，后续计划仅引用已输出的文档与源码行。
- 2026-08-23：`rg -n -C 2 'ErrPermission' /usr/local/go/src/io/fs /usr/local/go/src/os /usr/local/go/src/syscall`；原始输出确认 `/usr/local/go/src/io/fs/fs.go:153-162` 定义 `fs.ErrPermission`，`/usr/local/go/src/os/error.go:94-101` 说明新代码使用 `errors.Is(err, fs.ErrPermission)`。计划将非 permission 的探针错误判为 setup failure，不静默 skip。
- 2026-08-23：对照 `internal/prochost/fence_inherit_test.go:139-181`；原始源码显示该守卫在 `:176-177` 检查的是 root 对 `CAP_SYS_RESOURCE`/RLIMIT 单向门的能力，非文件 mode。判断：B219 helper 只收编 `cursor_layout_test.go` 与 `install_test.go` 两处 mode 守卫，fence 守卫保持原样，并在计划/收尾说明该边界。
- 2026-08-23：读取 `.github/workflows/ci.yml:18-31,102-125`；原始输出确认 Linux CI 在 `ubuntu-latest` 执行 `go test ./... -count=1`，Windows 运行期只跑 `internal/prochost/...` 与 `internal/executor/claudecode/...`。`capsh --print` 原始输出含 `Current IAB: !cap_dac_override,!cap_dac_read_search,...`，当前 euid=0 但无 DAC_OVERRIDE；尝试 `runuser -u nobody -- id -u` 原始失败为 `runuser: cannot set groups: Operation not permitted`。判断：当前 root 沙箱不能模拟目标 root+DAC_OVERRIDE，也不能本地降权复跑；计划保留探针和变异接缝，不能把当前 root 结果当非 root/目标 root 证明。
- 2026-08-23：新增 `docs/superpowers/plans/2026-08-23-b219-permission-precondition-helper.md`；计划完整列出 Task 1 两个新文件、Task 2 五个修改文件与 fence 不改边界，给出 `DenyWrite(testing.TB, string)`、`DenyRead(testing.TB, string)` 精确接口、三态纯决策函数、完整代码块、定向测试与强制变异验收。
- 2026-08-23：`rg -n 'TBD|TODO|同 Task|适当|filepath\\.Join\\(\\.\\.\\.' docs/superpowers/plans/2026-08-23-b219-permission-precondition-helper.md` 原始输出为 `no banned markers`；`git diff --check` 原始输出为空；计划章节扫描列出 Task 1/Task 2、Files、测试范围、步骤、验收栏与完整性自声明。计划文件仍未提交。
- 2026-08-23：复核计划 Task 2 的代码块；补齐 `internal/config/config_test.go` 与 `internal/executor/grok/authsync_test.go` 的完整目标函数内容，保留既有断言并仅替换 mode 前提。计划完整性扫描与 `git diff --check` 待补齐后重跑。
- 2026-08-23：修正 Task 2 Consumes 代码块为合法精确签名 `func DenyWrite(t testing.TB, path string)` / `func DenyRead(t testing.TB, path string)`，并重跑计划扫描；原始输出为 `no banned markers`、`no malformed call signatures`，`git diff --check` 无输出。
- 2026-08-23：`git add docs/superpowers/plans/2026-08-23-b219-permission-precondition-helper.md docs/ledger-b219.md && git diff --cached --check && git diff --cached --stat && git status --short`；原始失败为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/80ca63b4/index.lock': Read-only file system`。暂存与提交尚未完成。
- 2026-08-23：在提升权限下重跑暂存与校验；原始输出为 `docs/ledger-b219.md | 22 +++`、计划文件 `762 +++`、`2 files changed, 784 insertions(+)`，状态为 `A docs/ledger-b219.md` 与 `A docs/superpowers/plans/2026-08-23-b219-permission-precondition-helper.md`。暂存校验通过。
- 2026-08-23：`git commit -m "plan(b219): add permission precondition helper plan"`；原始输出为 `[cards/B219-charter f7ff2e52] plan(b219): add permission precondition helper plan`、`2 files changed, 785 insertions(+)`，创建计划与台账两个文件。随后 `git rev-parse --abbrev-ref HEAD && git rev-parse HEAD && git status --short --branch` 原始输出为分支 `cards/B219-charter`、提交 `f7ff2e52f4d59eb0093fea6e5c993a9f126473cd`、状态 `## cards/B219-charter`。
- 2026-08-23：`git add docs/ledger-b219.md && git diff --cached --check && git commit --amend --no-edit`；原始输出为 `[cards/B219-charter 1f026cd5] plan(b219): add permission precondition helper plan`、`2 files changed, 786 insertions(+)`。本记录随后以独立 ledger commit 收口，不再 amend，避免循环改写最终提交。
- 2026-08-23：`git add docs/ledger-b219.md && git diff --cached --check && git commit -m "chore(ledger): record B219 plan close"`；原始输出为 `[cards/B219-charter 1e5ee63f] chore(ledger): record B219 plan close`、`1 file changed, 1 insertion(+)`。
- 2026-08-23：`git status --short --branch && git log -2 --oneline --decorate`；原始输出为 `## cards/B219-charter`，HEAD 为 `1e5ee63f (HEAD -> cards/B219-charter) chore(ledger): record B219 plan close`，上一提交为 `1f026cd5 plan(b219): add permission precondition helper plan`；状态无未提交改动。该状态记录随本次台账收口。
- 2026-08-23：本执行工作树读取 B219 spec/plan 后执行 `pwd && git status --short --branch && rg --files docs/superpowers/specs docs/superpowers/plans .agents .codex`；原始输出确认工作树为 `/root/.handoff/worktrees/98e65c6a`、当前分支为 `## cards/B219-charter-2`、状态无未提交改动；计划冻结为新增 `internal/testperm` 两文件并改造六个 mode 前提调用点，`fence_inherit_test.go` 保持原样。
- 2026-08-23：按计划执行 `id -u; go test ./internal/client ./internal/config ./internal/executor/grok ./internal/release -run 'TestCursorRootFallsBackToCwdWhenHomeUnwritable|TestCursorRootErrorNamesBothPaths|TestLoadStripUpdateDoesNotBlockOnSaveFailure|TestSyncAuthKeepsTaskCopyWhenWriteFails|TestReadCursorPermissionDeniedIsReported|TestActivateUnwritableDir' -count=1 -v`；原始输出为 euid `0`，client/config/grok/release 均 `PASS`，`TestReadCursorPermissionDeniedIsReported` 与 `TestActivateUnwritableDir` 按现有 root 守卫 `SKIP`；四个恒红目标在当前沙箱均 `PASS`。该结果仅记录当前环境，不替代 root+DAC_OVERRIDE 验收。
- 2026-08-23：只新增计划规定的 `internal/testperm/permission_test.go` 后执行 `go test ./internal/testperm -run 'TestDecideProbe|TestApplyProbe' -count=1`；原始失败为 `undefined: probeAction`、`undefined: probeSkip`、`undefined: probeContinue`、`undefined: probeFatal`、`undefined: decideProbe`、`undefined: apply`（`internal/testperm/permission_test.go:20,24-26,30,50,69`）。失败原因为 helper 功能尚未实现，非测试拼写错误；TDD 红灯已确认。
- 2026-08-23：新增计划规定的 `internal/testperm/permission.go` 最小实现后执行 `gofmt -w internal/testperm/permission.go internal/testperm/permission_test.go`；原始输出为空，命令退出成功。
- 2026-08-23：执行 `go test ./internal/testperm -count=1`；原始失败为 `TestDecideProbe/permission_denied`，实际 message 为 `权限前提已成立：写 探针被拒绝...`，单测要求子串 `限制已生效`；同时两个恢复时序子测试输出并通过。判断：实现文案与已写测试契约不一致，需调整 permission 分支文案。
- 2026-08-23：调整 permission 分支文案并执行 `gofmt -w internal/testperm/permission.go && go test ./internal/testperm -count=1`；原始输出为空（gofmt）后为 `ok github.com/Xsxdot/handoff/internal/testperm 0.002s`。
- 2026-08-23：执行 `git diff --check`；原始输出为空，退出成功。
- 2026-08-23：执行 `go vet ./internal/testperm ./internal/client ./internal/config ./internal/executor/grok ./internal/release`；原始输出为空，命令退出成功。
- 2026-08-23：执行 `git status --short && git diff --stat && git diff -- internal/client/cursordir_test.go internal/client/cursor_layout_test.go internal/config/config_test.go internal/executor/grok/authsync_test.go internal/release/install_test.go && rg -n 'Geteuid|os\\.Chmod|0o000|0o444|0o500|testperm\\.Deny' ... && git diff -- internal/prochost/fence_inherit_test.go`；原始输出显示仅台账与五个 Task 2 测试文件有改动（`42 insertions(+), 29 deletions(-)`），目标文件仅保留 7 个 `testperm.Deny*` 调用，`fence_inherit_test.go` diff 为空。
- 2026-08-23：读取 `internal/testperm/permission.go`、`permission_test.go` 并扫描 `rg -n '^func [A-Z]|^type [A-Z]|^const \\(' internal/testperm`；原始输出确认 helper 对外仅有 `DenyWrite`/`DenyRead` 两个导出函数，文件头/导出函数注释存在，三态决策与 cleanup 单测在位。
- 2026-08-23：自审发现 `DenyRead` 的目录类型错误分支缺少结构化日志；补充 `slog.Default().Error`（包含 operation/path/reason）并执行 `gofmt -w internal/testperm/permission.go && go test ./internal/testperm -count=1`；原始输出为空（gofmt）后为 `ok github.com/Xsxdot/handoff/internal/testperm 0.003s`。
- 2026-08-23：补充日志后执行 `go build ./...`；原始输出为空，命令退出成功。
- 2026-08-23：补充日志后再次执行 `go vet ./internal/testperm ./internal/client ./internal/config ./internal/executor/grok ./internal/release`；原始输出为空，命令退出成功。
- 2026-08-23：补充日志后重跑 client 定向命令；原始输出为三个目标均 `--- PASS`，helper 日志含探针被拒绝和恢复，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/client 0.006s`。
- 2026-08-23：补充日志后重跑 config 定向命令；原始输出为目标 `--- PASS`，helper 探针被拒绝、配置回写错误与恢复日志均出现，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/config 0.002s`。
- 2026-08-23：补充日志后重跑 grok 定向命令；原始输出为目标 `--- PASS`，helper 探针被拒绝、grok 写回错误与恢复日志均出现，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/executor/grok 0.003s`。
- 2026-08-23：补充日志后重跑 release 定向命令；原始输出为目标 `--- PASS`，helper 探针被拒绝与恢复日志均出现，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/release 0.002s`。
- 2026-08-23：最终暂存前执行 `git diff --check && git status --short && git diff --name-only && git diff --stat`；原始输出显示 diff check 为空，仅 7 个预期文件变更：台账、5 个调用点测试文件与 `internal/testperm/permission.go` 日志补丁，统计 `52 insertions(+), 29 deletions(-)`。
- 2026-08-23：提升权限执行 `git add docs/ledger-b219.md internal/testperm/permission.go internal/client/cursordir_test.go internal/client/cursor_layout_test.go internal/config/config_test.go internal/executor/grok/authsync_test.go internal/release/install_test.go && git diff --cached --check && git diff --cached --stat && git status --short`；原始输出为 `7 files changed, 53 insertions(+), 29 deletions(-)`，diff check 为空，7 个预期文件均已暂存。
- 2026-08-23：按 task 收尾执行 `go build ./...`；原始输出为空，命令退出成功。
- 2026-08-23：执行 `git status --short internal/testperm docs/ledger-b219.md && git diff --check && git diff --stat -- internal/testperm docs/ledger-b219.md`；原始输出为 ` M docs/ledger-b219.md`、`?? internal/testperm/`，diff check 为空，已跟踪文件 diff stat 为 `docs/ledger-b219.md | 8 ++++++++`；新目录未被 diff stat 统计因尚未暂存。
- 2026-08-23：执行 `git add internal/testperm/permission.go internal/testperm/permission_test.go docs/ledger-b219.md && git diff --cached --check && git diff --cached --stat && git status --short`；原始失败为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/98e65c6a/index.lock': Read-only file system`，普通沙箱无法写共享 worktree index，尚未暂存。
- 2026-08-23：提升权限重跑同一暂存/校验命令；原始输出为 `docs/ledger-b219.md | 10 ++`、`internal/testperm/permission.go | 177 +++++++++++++++++++++++++++++++++++`、`internal/testperm/permission_test.go | 87 +++++++++++++++++`，合计 `3 files changed, 274 insertions(+)`，diff check 为空，三项均显示已暂存。
- 2026-08-23：执行 `git add internal/testperm/permission.go internal/testperm/permission_test.go docs/ledger-b219.md && git diff --cached --check && git diff --cached --stat && git commit -m "test(b219): add permission precondition helper"`（提升权限）；原始输出为 `3 files changed, 275 insertions(+)`，提交 `[cards/B219-charter-2 47468063] test(b219): add permission precondition helper`，新增 helper 与单测已提交。
- 2026-08-23：按 Task 2 文件清单替换六个 mode 前提调用点并执行 `gofmt -w internal/client/cursordir_test.go internal/client/cursor_layout_test.go internal/config/config_test.go internal/executor/grok/authsync_test.go internal/release/install_test.go`；原始输出为空，命令退出成功。`fence_inherit_test.go` 未修改。
- 2026-08-23：执行 `go test ./internal/client -run 'TestCursorRootFallsBackToCwdWhenHomeUnwritable|TestCursorRootErrorNamesBothPaths|TestReadCursorPermissionDeniedIsReported' -count=1 -v`；原始输出中三个用例均打印 helper 入口/施加限制/探针被拒绝/恢复日志，分别为 `--- PASS: TestReadCursorPermissionDeniedIsReported`、`--- PASS: TestCursorRootFallsBackToCwdWhenHomeUnwritable`、`--- PASS: TestCursorRootErrorNamesBothPaths`，末尾为 `PASS` 与 `ok github.com/Xsxdot/handoff/internal/client 0.006s`。
- 2026-08-23：执行 `go test ./internal/config -run '^TestLoadStripUpdateDoesNotBlockOnSaveFailure$' -count=1 -v`；原始输出为 helper 写限制施加、`permission denied` 探针被拒绝、配置回写错误日志、权限恢复，随后 `--- PASS: TestLoadStripUpdateDoesNotBlockOnSaveFailure`、`PASS`、`ok github.com/Xsxdot/handoff/internal/config 0.006s`。
- 2026-08-23：执行 `go test ./internal/executor/grok -run '^TestSyncAuthKeepsTaskCopyWhenWriteFails$' -count=1 -v`；原始输出为 helper 对 `.grok` 目录施加限制、写探针 `permission denied`、grok 写回失败并保留任务侧副本日志、权限恢复，随后 `--- PASS: TestSyncAuthKeepsTaskCopyWhenWriteFails`、`PASS`、`ok github.com/Xsxdot/handoff/internal/executor/grok 0.003s`。
- 2026-08-23：执行 `go test ./internal/release -run '^TestActivateUnwritableDir$' -count=1 -v`；原始输出为 helper 对安装目录施加限制、写探针 `permission denied`、权限恢复，随后 `--- PASS: TestActivateUnwritableDir`、`PASS`、`ok github.com/Xsxdot/handoff/internal/release 0.002s`。
- 2026-08-23：按计划临时将 `internal/testperm/permission.go` 的 `if probeErr == nil {` 改为 `if true { // B219 mutation：强制把限制判为未生效`，开始执行变异验收；该变异仅存在于工作树，随后必须恢复。
- 2026-08-23：变异命令 `go test ./internal/client -run 'TestCursorRootFallsBackToCwdWhenHomeUnwritable|TestCursorRootErrorNamesBothPaths' -count=1 -v`；原始输出显示两个目标均打印“探针成功，当前机器无法表达”并 `--- SKIP`，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/client 0.005s`。
- 2026-08-23：变异命令 `go test ./internal/config -run '^TestLoadStripUpdateDoesNotBlockOnSaveFailure$' -count=1 -v`；原始输出显示目标打印“探针成功，当前机器无法表达”并 `--- SKIP`，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/config 0.003s`。
- 2026-08-23：变异命令 `go test ./internal/executor/grok -run '^TestSyncAuthKeepsTaskCopyWhenWriteFails$' -count=1 -v`；原始输出显示目标打印“探针成功，当前机器无法表达”并 `--- SKIP`，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/executor/grok 0.003s`。
- 2026-08-23：变异验收三条命令均按预期 `SKIP` 后立即用 `apply_patch` 将 `if true` 恢复为 `if probeErr == nil {`；变异未留在工作树。
- 2026-08-23：恢复后执行 `go test ./internal/testperm -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/testperm 0.002s`。
- 2026-08-23：恢复后重跑 client 定向命令 `go test ./internal/client -run 'TestCursorRootFallsBackToCwdWhenHomeUnwritable|TestCursorRootErrorNamesBothPaths|TestReadCursorPermissionDeniedIsReported' -count=1 -v`；原始输出中三个目标均为 `--- PASS`，helper 日志均含探针被拒绝与权限恢复，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/client 0.011s`。
- 2026-08-23：恢复后执行 config 定向命令 `go test ./internal/config -run '^TestLoadStripUpdateDoesNotBlockOnSaveFailure$' -count=1 -v`；原始输出为目标 `--- PASS`，helper 探针被拒绝、配置回写失败日志与权限恢复均出现，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/config 0.002s`。
- 2026-08-23：恢复后执行 grok 定向命令 `go test ./internal/executor/grok -run '^TestSyncAuthKeepsTaskCopyWhenWriteFails$' -count=1 -v`；原始输出为目标 `--- PASS`，helper 探针被拒绝、grok 写回失败日志与权限恢复均出现，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/executor/grok 0.003s`。
- 2026-08-23：恢复后执行 release 定向命令 `go test ./internal/release -run '^TestActivateUnwritableDir$' -count=1 -v`；原始输出为目标 `--- PASS`，helper 探针被拒绝与权限恢复均出现，末尾为 `PASS`、`ok github.com/Xsxdot/handoff/internal/release 0.003s`。
- 2026-08-23：Task 2 收尾执行 `go build ./...`；原始输出为空，命令退出成功。
- 2026-08-23：按计划再次执行全文件 `gofmt -w internal/testperm/permission.go internal/testperm/permission_test.go internal/client/cursordir_test.go internal/client/cursor_layout_test.go internal/config/config_test.go internal/executor/grok/authsync_test.go internal/release/install_test.go`；原始输出为空，命令退出成功。
- 2026-08-23：执行 `git diff --check`；原始输出为空，退出成功。
