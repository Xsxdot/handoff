# 项目至少有一个位置且最多绑定一台远程开发机

状态：已接受。

一个项目可以仅有本机位置、仅有一个远程位置，或同时拥有本机位置和一个远程位置，但不能绑定多台远程开发机。这个约束既保留了本地与远程并存的开发场景，也避免调度者在多个远程目标之间做隐式选择；每个 handoff 任务继续绑定一个具体项目位置或工作树，从而保持执行目标唯一。每个被选择的位置都独立配置 Git 来源或已有目录；本机目录可以通过系统文件选择器指定，远程目录只接受明确路径。
