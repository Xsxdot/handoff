# 机器拥有 Env、执行器与自动审批者配置

状态：已接受。

Env 以 `.env` 文件形式存放在每台机器自己的 handoff 目录中，一台机器可以维护多份文件，每份文件包含多个变量。执行器的启用状态及其可选 Env 文件、自动审批者所调用的执行器及其可选 Env 文件都按机器配置；handoff 在分级审批中先轻量调用自动审批者，只有无法确定时才升级到用户。全局设置不再重复维护执行者或审批者配置。
