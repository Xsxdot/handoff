# B265 收口：IM 悬浮与看板五列（产品已由 B275 交付）

- **卡**：B265
- **级别**：**L1**。plan 增量为零：本卡不再改代码。验收一眼可核：用户证词 + B275 已含本卡范围 + charter 看板五列已在工作流 `board`。
- **状态**：**已批准**（2026-09-05；用户：「265实际上已经做完了，我就是要拿这个走一圈的」）
- **基线**：`origin/main` @ `47a57d7b`
- **发现自**：B265 原想法卡；实现归并进 B275。不 reopen B275，不派 implement。

## 问题陈述

B265 记的是两件事：工作项页以外 IM 可悬浮/缩小/拖动，进工作项后右侧常驻；看板列改为代办、沟通中、进行中、审核中、结束，由工作流配置节点到列的映射。

这两件事已经在 B275（「B156.2-156.3 前端原型与对话框整合（含 B265）」）做完并合进 main。本卡一直停在待办，是拿来走 charter 流程的占位，不是还欠产品。

## 方案

按已交付收口。同一份文件挂 `spec:` 与 `plan:`。列序合法走完：待办 → spec → implement → review → finish → 已完成。**不派 implement / review**：没有新 diff 可审，派出去只会引诱执行者重做已在 main 的 IM。

对照事实（收口用，不在本卡重做）：

- B275 spec 用户故事 1–2、8 与本卡标题同范围（IM 两处挂载 + 看板五列）。
- 现役 charter 工作流 `board.columns` = 代办 / 沟通中 / 进行中 / 审核中 / 结束。

弃选：待办直接跳已完成（账本拒绝越级）。弃选：再派一轮 implement 把悬浮 IM 重写一遍。

## 用户故事

1. 作为账本读者：B265 从待办合法走到已完成，timeline 写明产品由 B275 交付、本卡只走流程。
2. 作为对照：本卡工作树相对 main 零功能 diff。

## 测试决定

本卡无新缝、无新测试。验收不靠本卡测试，靠用户证词与 B275 交付指针。

## L1 plan

1. 本文件挂 `spec:` + `plan:`，定级 L1 记 note。
2. 人工移列 implement → review → finish → 已完成；不 `card dispatch`。
3. `card accept` 记用户 2026-09-05 证词与 B275 指针。

验收：卡在已完成；无功能代码提交。本节点该干什么一律以注入的纪律块为准。

## Out of Scope

- **永不做**：为收口再实现一份 IM / 看板列。
- **本期不做**：TUI 自动 `card bind`；空 HOME 协调者（B347）；删除载体（B348）。
