# B302 台账

- 2026-08-30 用户：「建一张新卡自主推进」「终端 tab 启动各 agent TUI 后 Shift+回车不换行而是发送」。派发节点 linux-01 / codex。
- 建卡 B302，project=handoff，优先级高，move spec。
- 主仓在 cards/B294-breakdown，从 origin/main 开 worktree `~/.handoff/worktrees/b302-spec` 分支 `cards/B302-spec`。
- 根因读数：xterm Keyboard.ts case 13（约 L100-104）`result.key = ev.altKey ? ESC+CR : CR`，shiftKey 未参与。terminalInput.ts 文件头：Enter 留给 xterm。
- B268 已独占 customKeyEventHandler；本卡只在该槽加一条。B268 OOS 的 Kitty 协议 ≠ 单键 CSI u。
- 定级 L2：单子系统、不动契约；plan 非零；真机 TUI 一眼可核但机制+真机两扇门。
- 方案冻结：alt-screen 才拦截，序列 `ESC[13;2u`。用户授权自主推进，协调者批准。
- 2026-08-30 执行节点事实：当前分支为 `cards/B302-charter`，工作树干净；本节点只产出 `docs/superpowers/plans/b302-plan.md`，不切换分支、不实现代码。
- 2026-08-30 执行节点查图：仓内存在 `codegraph/`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_web` 未运行成功，原始输出为 `go: downloading github.com/Xsxdot/charter/graph v0.10.0`、`go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.mod786330125.tmp: read-only file system`、`open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.lock: read-only file system`；随后按纪律降级 `rg`，记图覆盖债。
- 2026-08-30 执行节点源码事实：`rg` 命中生产接缝 `web/src/app/workbench/terminalInput.ts` 的 `installTerminalInputFix`/`attachCustomKeyEventHandler`，真实调用方为 `web/src/app/workbench/TerminalTab.tsx`；已有缝级测试文件为 `web/src/app/workbench/terminalInput.test.ts`，TerminalTab 另有集成夹具。
- 2026-08-30 执行节点基线判据复核：在 `web/` 运行 `npm test -- --run src/app/workbench/terminalInput.test.ts`，原始输出为 `> web@0.0.0 test`、`> vitest run --run src/app/workbench/terminalInput.test.ts`、`sh: 1: vitest: not found`，未验证测试结果；运行 `npm run typecheck`，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`、`sh: 1: tsc: not found`，未验证类型结果。
- 2026-08-30 执行节点环境事实：在 `web/` 运行 `npm ci --ignore-scripts` 成功，原始输出为 `added 290 packages, and audited 291 packages in 3s`、`found 0 vulnerabilities`；依赖目录为未跟踪安装产物，不纳入提交。
- 2026-08-30 执行节点基线绿读数：`cd web && npm test -- --run src/app/workbench/terminalInput.test.ts` 通过 `Test Files 1 passed (1)`、`Tests 21 passed (21)`；输出含 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`，但测试进程退出成功。`cd web && npm test -- --run src/app/workbench/TerminalTab.test.tsx` 通过 `Test Files 1 passed (1)`、`Tests 47 passed (47)`；`cd web && npm run typecheck` 退出成功且无错误输出。
- 2026-08-30 执行节点依赖源码事实：`web/package-lock.json:2569-2573` 锁定 `@xterm/xterm` 5.5.0；安装后源码验证 `web/node_modules/@xterm/xterm/src/common/input/Keyboard.ts:100-104` 的 Enter 仅按 `altKey` 选择 `ESC+CR`/`CR`，不读 `shiftKey`；`src/browser/Terminal.ts:1001-1007` 说明 custom key handler 返回 `false` 会在 xterm 处理前短路，`typings/xterm.d.ts:1014-1040` 定义其签名为 `(event: KeyboardEvent) => boolean`；`src/common/InputHandler.ts:1939-1948` 与 `typings/xterm.d.ts:1461-1465,1527-1542` 验证 DECSET 1049/47/1047 与 `buffer.active.type` 的 alternate 判据。
- 2026-08-30 执行节点可观测性事实：`web/src/app/workbench/terminalDebug.ts:109-127` 的 `logTermFix(label: string, kind: ..., text: string)` 是终端补漏结构化日志入口，当前开关关闭时静默、开启时用 `console.debug`；B302 成功拦截路径应扩充 kind 并记录序列，禁止新建 `console.log` 或静默返回。
- 2026-08-30 执行节点测试依赖事实：xterm 5.5.0 `web/node_modules/@xterm/xterm/src/common/CoreTerminal.ts:150-159` 提供 `write(data, callback?)`，并明确异步解析应使用 callback；因此 alt-screen 测试若通过真实终端写入 `\x1b[?1049h`，必须在 callback 内断言/派发键盘事件。一次探索命令因正则括号未闭失败，原始输出为 `rg: regex parse error:`、`(?:write(data)`、`error: unclosed group`，不把该命令当验证依据。
- 2026-08-30 执行节点依赖源码补充：xterm 5.5.0 `web/node_modules/@xterm/xterm/src/common/CoreTerminal.ts:171-173` 的 `input(data, wasUserInput = true)` 直接触发 data event；因此缝级测试可同时断言 `term.input('\x1b[13;2u')` 与既有 `onData` 收到同一序列，确认无额外 `\r`。
- 2026-08-30 执行节点查图补充：按 best 领域 id 运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_web_workbench` 仍未运行成功，原始输出为 `go: downloading github.com/Xsxdot/charter/graph v0.10.0`、`go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.mod173803237.tmp: read-only file system`、`open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.lock: read-only file system`；保持图覆盖债，不以 JSON 静态字段冒充 context 结果。
- 2026-08-30 执行节点产出事实：`docs/superpowers/plans/b302-plan.md` 已按法定路径落盘；内容覆盖单一 d_web_workbench task 的文件集、精确 Interfaces、基线命令、红绿与变异回归、日志/注释、四条用户故事、真机 TUI 门、缺陷族、序列化边界、上下文预算和双向接缝审计。
- 2026-08-30 执行节点自审事实：计划占位符扫描无输出（`placeholder scan: clean`），30 个 tilde fence 成对，`git diff --check` 输出 `diff check: clean`；新增接缝断言明确覆盖 `defaultPrevented`、`cancelBubble`、精确 CSI u、无 CR、主屏放行及 Alt/Ctrl/Meta 边界。
- 2026-08-30 提交事实：执行 `git diff --check && git add docs/superpowers/plans/b302-plan.md docs/superpowers/specs/b302-ledger.md && git commit -m "docs(B302): plan Shift+Enter TUI terminal fix"`；原始输出为 `[cards/B302-charter 00343057] docs(B302): plan Shift+Enter TUI terminal fix`、`2 files changed, 423 insertions(+)`、`create mode 100644 docs/superpowers/plans/b302-plan.md`。随后按纪律仅 amend 一次收进该台账追加。
- 2026-08-30 实现节点接管事实：当前分支为 `cards/B302-charter-2`，工作树初始干净；已读取 handoff 技能、B302 规格与实现计划，按计划仅触及 web 终端输入接缝及其测试/日志。
- 2026-08-30 实现节点基线首次执行：`cd web && npm test -- --run src/app/workbench/terminalInput.test.ts` 原始输出为 `> web@0.0.0 test`、`> vitest run --run src/app/workbench/terminalInput.test.ts`、`sh: 1: vitest: not found`，退出码 127，未验证。
- 2026-08-30 实现节点基线首次执行：`cd web && npm test -- --run src/app/workbench/TerminalTab.test.tsx` 原始输出为 `> web@0.0.0 test`、`> vitest run --run src/app/workbench/TerminalTab.test.tsx`、`sh: 1: vitest: not found`，退出码 127，未验证。
- 2026-08-30 实现节点基线首次执行：`cd web && npm run typecheck` 原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`、`sh: 1: tsc: not found`，退出码 127，未验证。
- 2026-08-30 实现节点环境事实：`cd web && npm ci --ignore-scripts` 成功，原始输出为 `added 290 packages, and audited 291 packages in 2s`、`71 packages are looking for funding`、`found 0 vulnerabilities`；`web/node_modules` 为安装产物，不纳入提交。
- 2026-08-30 实现节点基线复核：`cd web && npm test -- --run src/app/workbench/terminalInput.test.ts` 通过，原始输出含 `Test Files  1 passed (1)`、`Tests  21 passed (21)`，并有 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package` 提示；进程退出码 0。
- 2026-08-30 实现节点基线复核：`cd web && npm test -- --run src/app/workbench/TerminalTab.test.tsx` 通过，原始输出含 `Test Files  1 passed (1)`、`Tests  47 passed (47)`；进程退出码 0。
- 2026-08-30 实现节点基线复核：`cd web && npm run typecheck` 成功退出码 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，无错误输出。
- 2026-08-30 实现节点 TDD 首红：先仅向 `terminalInput.test.ts` 加入真实 Terminal 的 B302 alt-screen/主屏/修饰键断言，未改生产代码；`cd web && npm test -- --run src/app/workbench/terminalInput.test.ts` 真实失败，原始输出为 `25 tests | 1 failed`，首个用例 `交替屏 Shift+Enter 发 CSI u，不再让 xterm 发 CR` 断言 `expected "input" to be called 1 times, but got 0 times`（`terminalInput.test.ts:217:19`），其余 `24 passed`；失败原因为功能缺失而非拼写错误。
- 2026-08-30 实现节点生产接入首轮：按补充要求仅在既有 `keydown` 分支最前加入 B302 判断，并扩展 `logTermFix` 的 `Shift+Enter 换行` kind；局部测试已确认 CSI u 发送与无 CR 断言成立，但真实运行仍在 `ev.cancelBubble` 断言失败，原始输出为 `25 tests | 1 failed`、`AssertionError: expected false to be true`（`terminalInput.test.ts:222:29`），其余 `24 passed`；该结果未判绿。
- 2026-08-30 实现节点测试适配事实：jsdom 在 `dispatchEvent` 返回后会把 `cancelBubble` 复位为 false；已用 `vi.spyOn(ev, 'stopPropagation')` 直接锁定生产分支调用，并撤回无效的 `ev.cancelBubble` 赋值，不改变生产行为。
- 2026-08-30 实现节点局部绿灯：`cd web && npm test -- --run src/app/workbench/terminalInput.test.ts` 通过，原始输出含 `Test Files  1 passed (1)`、`Tests  25 passed (25)`，并有计划允许的 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package` 提示；进程退出码 0。
- 2026-08-30 实现节点变异前唯一命中核验：`rg -F -n "const TUI_SHIFT_ENTER = '\\x1b[13;2u'" src/app/workbench/terminalInput.ts` 原始输出为 `214:  const TUI_SHIFT_ENTER = '\\x1b[13;2u'`，精确变异锚点命中 1 处。
- 2026-08-30 实现节点变异编译门：将唯一的 `TUI_SHIFT_ENTER` 临时改为 `\\r` 后执行 `cd web && npm run typecheck`，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，退出码 0；变异可编译。
- 2026-08-30 实现节点变异行为门：变异为 `\\r` 后执行 `cd web && npm test -- --run src/app/workbench/terminalInput.test.ts`，真实失败，原始输出为 `25 tests | 1 failed`，首个用例期望 `\\u001b[13;2u`、实际 `\\r`，其余 `24 passed`；确认测试拦截序列错误。
- 2026-08-30 实现节点变异恢复：用 `apply_patch` 将 `TUI_SHIFT_ENTER` 恢复为 `\\x1b[13;2u`；恢复后同一局部测试通过，原始输出含 `Test Files  1 passed (1)`、`Tests  25 passed (25)`，并有计划允许的 canvas 提示，退出码 0。
- 2026-08-30 实现节点收尾验证：`cd web && npm test -- --run src/app/workbench/terminalInput.test.ts` 通过，原始输出含 `Test Files  1 passed (1)`、`Tests  25 passed (25)`，并有计划允许的 canvas 提示；进程退出码 0。
- 2026-08-30 实现节点收尾验证：`cd web && npm test -- --run src/app/workbench/TerminalTab.test.tsx` 通过，原始输出含 `Test Files  1 passed (1)`、`Tests  47 passed (47)`；进程退出码 0。
- 2026-08-30 实现节点收尾验证：`cd web && npm run typecheck` 成功退出码 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，无错误输出。
- 2026-08-30 实现节点收尾验证：`git diff --check` 成功退出码 0，原始输出为空。
- 2026-08-30 实现节点附加验证：`cd web && npm run lint` 退出码 1；原始错误为 `/root/.handoff/worktrees/4e1d36cc/web/src/api/pty.ts 115:9 error Unexpected lexical declaration in case block no-case-declarations`、`/root/.handoff/worktrees/4e1d36cc/web/src/app/flows/NodeEditor.test.tsx 50:9 error 'view' is never reassigned. Use 'const' instead prefer-const`、`/root/.handoff/worktrees/4e1d36cc/web/src/app/workbench/terminalHostResponse.ts 45:25 error Unexpected control character(s) in regular expression: \\x1b no-control-regex`、`47:26` 同错、`49:23` 同错；末尾为 `✖ 23 problems (5 errors, 18 warnings)`。
- 2026-08-30 实现节点附加验证：`cd web && npx eslint src/app/workbench/terminalInput.ts src/app/workbench/terminalInput.test.ts src/app/workbench/terminalDebug.ts` 成功退出码 0，原始输出为空。
- 2026-08-30 实现节点提交前审计：`git status --short --branch` 显示当前分支 `cards/B302-charter-2`，仅 4 个计划内文件有改动；`git diff --name-only` 原始输出为 `docs/superpowers/specs/b302-ledger.md`、`web/src/app/workbench/terminalDebug.ts`、`web/src/app/workbench/terminalInput.test.ts`、`web/src/app/workbench/terminalInput.ts`；`git diff --check` 成功且无输出；`rg -n "attachCustomKeyEventHandler" web/src/app/workbench` 仅命中 terminalInput.ts 的文档与唯一生产挂载。
- 2026-08-30 实现节点提交前复核：再次执行 `git diff --check`，退出码 0，原始输出为空。
- 2026-08-30 实现节点提交事实：执行 `git commit -m "fix(web): preserve Shift+Enter in alternate terminal TUI (B302)"`，原始输出为 `[cards/B302-charter-2 041dcfdd] fix(web): preserve Shift+Enter in alternate terminal TUI (B302)`、`4 files changed, 116 insertions(+), 3 deletions(-)`；随后仅 amend 一次将本条台账收进同批提交，不追写 amend 后 hash。
- 2026-08-30 review minor 节点首次聚焦测试：`npm test -- --run src/app/workbench/terminalInput.test.ts -t '交替屏退出后'` 退出码 127，原始输出为 `> web@0.0.0 test`、`> vitest run --run src/app/workbench/terminalInput.test.ts -t 交替屏退出后`、`sh: 1: vitest: not found`；依赖待安装，测试结果未验证。
- 2026-08-30 review minor 节点环境事实：在 `web/` 执行 `npm ci --ignore-scripts` 成功，原始输出为 `added 290 packages, and audited 291 packages in 2s`、`71 packages are looking for funding`、`found 0 vulnerabilities`；`web/node_modules` 为安装产物，不纳入提交。
- 2026-08-30 review minor 节点聚焦绿灯：`npm test -- --run src/app/workbench/terminalInput.test.ts -t '交替屏退出后'` 通过，原始输出含 `Test Files  1 passed (1)`、`Tests  1 passed | 25 skipped (26)`，并有 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package` 提示；进程退出码 0。
- 2026-08-30 review minor 节点变异前唯一命中核验：`rg -F -n "term.buffer.active.type === 'alternate'" web/src/app/workbench/terminalInput.ts` 原始输出为 `235:        term.buffer.active.type === 'alternate'`；精确变异锚点命中 1 处。
- 2026-08-30 review minor 节点变异编译门：将唯一的 `term.buffer.active.type === 'alternate'` 临时改为 `!==` 后执行 `npm run typecheck`，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，退出码 0；变异可编译。
- 2026-08-30 review minor 节点变异行为门：取反判定后执行 `npm test -- --run src/app/workbench/terminalInput.test.ts -t '交替屏退出后'`，真实失败，原始输出含 `Test Files  (26 tests | 1 failed | 25 skipped)`、`input ... actually been called 1 times`、收到 `"\\u001b[13;2u"`；确认回切守卫被该测试拦截。
- 2026-08-30 review minor 节点变异全量局部结果：取反判定后执行 `npm test -- --run src/app/workbench/terminalInput.test.ts`，真实失败，原始输出为 `Test Files  1 failed (1)`、`Tests  3 failed | 23 passed (26)`；失败包含 alternate、normal 与回切三条 Shift+Enter 守卫。
- 2026-08-30 review minor 节点恢复绿灯：恢复 `term.buffer.active.type === 'alternate'` 后执行 `npm test -- --run src/app/workbench/terminalInput.test.ts` 通过，原始输出为 `Test Files  1 passed (1)`、`Tests  26 passed (26)`，并有 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package` 提示；进程退出码 0。
- 2026-08-30 review minor 节点集成测试：`npm test -- --run src/app/workbench/TerminalTab.test.tsx` 通过，原始输出为 `Test Files  1 passed (1)`、`Tests  47 passed (47)`；进程退出码 0。
- 2026-08-30 review minor 节点全量编译检查：`npm run typecheck` 通过，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，退出码 0。
- 2026-08-30 review minor 节点测试文件 lint：`npx eslint src/app/workbench/terminalInput.test.ts` 退出码 0，原始输出为空。
- 2026-08-30 review minor 节点提交前审计：`git diff --check` 退出码 0；`git diff --stat` 显示仅 `docs/superpowers/specs/b302-ledger.md` 与 `web/src/app/workbench/terminalInput.test.ts` 共新增 29 行，生产文件无差异。
- 2026-08-30 review minor 节点提交事实：执行 `git commit -m "test(web): cover Shift+Enter after terminal alt-screen exit"`，原始输出为 `[cards/B302-charter-3 e433bf8d] test(web): cover Shift+Enter after terminal alt-screen exit`、`2 files changed, 31 insertions(+)`；随后仅 amend 一次将本条台账收进同批提交，不追写 amend 后 hash。
