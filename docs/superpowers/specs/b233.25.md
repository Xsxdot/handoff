# B233.25 spec：存量测试红修复——合线已知失败六族归零

## 背景

- 来源：`docs/roadmap.md`「来自 B233.17 验收」存量红条；用户残余清单第 2 类第 3 行。
- 六族（均非本批次引入，2026-09-13 复核仍在）：
  1. `TestRepoContractGate`（cmd）——契约门测试，预期随 B233.23 图收口转绿；若仍红按其
     断言与实况对齐（不许删断言凑绿）。
  2. `TestServePermissionHookDenyWithReasonAndStep0`（cmd）——实测报
     `listen unix ... bind: invalid argument`，即用户报告的「macOS unix socket 路径过长」；
     修测试的 socket 落盘路径（短目录），生产代码不动。
  3. `TestLegacyNodeEventSequenceUnchanged`（internal/agentd）——legacy 节点事件序列不变
     测试，先诊断根因。
  4. agy permission 族（internal/executor/agy 等）——先诊断根因。
  5. `TestCoordinatorCancelTurnUsesSessionID`——先诊断根因。
  6. hostapi wakehome 两条——先诊断根因。

## 目标

`go test -p 1 ./...` 全量绿（本机环境可复现的失败全部根因修复）；确属本机环境不可复现/
依赖真机的（如需真二进制），逐条给出证据与结论，不得静默跳过。

## 验收

1. `go test -p 1 ./...` 全绿，或每条残余红有「根因 + 为什么本机不能修 + 证据」的记录；
2. 修复方式是根因修复：不许 sleep/跳过/删断言式打补丁；测试的平台假设类问题（socket
   路径、并行假红）在测试代码里修；
3. 行为修复（若根因在生产代码）走最小改动，附前后对照说明；
4. `go build ./...` 绿；本节点该干什么一律以注入的纪律块为准。

## Out of Scope

- 图数据（B233.23/.24 已管 TestRepoContractGate 的主根因）、真机依赖项的线上验证、合 main。
