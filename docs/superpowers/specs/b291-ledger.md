# B291 执行台账

- 2026-08-29：当前分支为 `cards/B291-charter`，HEAD 为 `e32ae6e5`，初始 `git status --short --branch` 仅输出 `## cards/B291-charter`。
- 2026-08-29：执行 `go test ./internal/agentd -run TestDisciplinesListFromLedgerOnly -count=1` 失败；原始输出为 `go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.mod131694418.tmp: read-only file system`、`open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.lock: read-only file system`、`FAIL github.com/Xsxdot/handoff/internal/agentd [setup failed]`，未作为功能红灯依据。
- 2026-08-29：使用任务可写模块缓存执行 `env GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go test ./internal/agentd -run TestDisciplinesListFromLedgerOnly -count=1` 实际通过，但 `-v` 复核显示该用例因 `PTY 测试根目录不可用` 被 `--- SKIP`，不是功能绿灯。
- 2026-08-29：设置 `HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/a1750540/p` 后执行 `env HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/a1750540/p GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go test ./internal/agentd -run TestDisciplinesListFromLedgerOnly -count=1 -v` 实际断言失败：`ledgerapi_test.go:1317: names = [charter-implement charter-review review], want [charter-implement charter-review]`，退出码 1。
- 2026-08-29：移除 `newLedgerEnv` 共享的 `PutDiscipline(discipline.NameReview, ...)`；保留 `newNoPTYLedgerEnv` 的局部 implement/review 种子及既有 `seedDisciplineOnLedger`。生产 `internal/agentd/ledgerapi.go` 未修改。
- 2026-08-29：修改后执行 `env HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/a1750540/p GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go test ./internal/agentd -run TestDisciplinesListFromLedgerOnly -count=1 -v` 实际通过；原始输出为 `--- PASS: TestDisciplinesListFromLedgerOnly`、`ok github.com/Xsxdot/handoff/internal/agentd 1.460s`，退出码 0。
- 2026-08-29：执行 `gofmt -w internal/agentd/ledgerapi_test.go && env GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go build ./...` 实际通过，原始输出为空，退出码 0。
- 2026-08-29：移除共享种子后 `go test ./internal/agentd -count=1` 实际失败 6 支；原始失败包括 `TestStartCardStepReleasesSlotOnFinish`、`TestCardStepReturns202`、`TestCardStepAcceptsImplementWithoutInlineFile`、`TestCardStepLegacyActorFallback`、`TestCardStepIgnoresUnknownFields`、`TestCardStepBodySizeLimitBoundary/恰好等于上限放行`，共同错误为 `未知纪律块名字 "review": 纪律块 review v0: ledger: 记录不存在`。
- 2026-08-29：为上述需要派发 review 正文的测试分别调用 `seedDisciplineOnLedger`，并为 `cardstep_test.go` 补回 `discipline` 测试依赖；其余拒发/在飞测试未共享回灌。
- 2026-08-29：执行 `gofmt -w internal/agentd/cardstep_test.go internal/agentd/ledgerapi_test.go && env HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/a1750540/p GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go test ./internal/agentd -run 'Test(StartCardStepReleasesSlotOnFinish|CardStepReturns202|CardStepAcceptsImplementWithoutInlineFile|CardStepLegacyActorFallback|CardStepIgnoresUnknownFields|CardStepBodySizeLimitBoundary)$' -count=1 -v` 实际通过；六支均输出 `--- PASS`，末行为 `ok github.com/Xsxdot/handoff/internal/agentd 1.939s`，退出码 0。
- 2026-08-29：重新执行 `env HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/a1750540/p GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go test ./internal/agentd -count=1` 实际通过，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 210.123s`，退出码 0。
- 2026-08-29：变异前以 `rg -F -n -c 'names, err := s.ledger.ListDisciplineNames()' internal/agentd/ledgerapi.go` 确认唯一命中为 `1`，并执行 `git diff --check`，退出码 0、无输出。
- 2026-08-29：临时在 `handleDisciplineNames` 成功路径追加 `names = append(names, "disk-only")`；执行 `gofmt -w internal/agentd/ledgerapi.go && env GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go build ./...` 实际通过，原始输出为空，退出码 0，排除编译假读数。
- 2026-08-29：变异后先执行目标行为断言 `env HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/a1750540/p GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go test ./internal/agentd -run TestDisciplinesListFromLedgerOnly -count=1 -v`，实际失败：`ledgerapi_test.go:1318: names = [charter-implement charter-review disk-only], want [charter-implement charter-review]`，退出码 1；确认该测试拦住语义变异。
- 2026-08-29：已移除上述临时 `disk-only` 变异，生产 `internal/agentd/ledgerapi.go` 恢复无差异状态。
- 2026-08-29：最终状态执行 `env HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/a1750540/p GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go test ./internal/agentd -run TestDisciplinesListFromLedgerOnly -count=1 && env GOMODCACHE=/root/.handoff/tmp/a1750540/gomodcache go build ./... && git diff --check && git diff -- internal/agentd/ledgerapi.go` 实际通过；原始测试输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.837s`，编译、diff 检查及生产文件 diff 均无输出，退出码 0。
- 2026-08-29：执行 `git add internal/agentd/cardstep_test.go internal/agentd/ledgerapi_test.go docs/superpowers/specs/b291-ledger.md && git diff --cached --check && git diff --cached --stat && git commit -m "test(B291): isolate ledger discipline fixtures"` 实际成功；原始输出为 `[cards/B291-charter 211fd2f5] test(B291): isolate ledger discipline fixtures`、`3 files changed, 27 insertions(+), 4 deletions(-)`。
