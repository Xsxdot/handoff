# B340 真机：B335 工作分支 push origin

- **卡**：B340
- **级别**：L1。plan 增量为零：implement 只新增本文件旁的台账一行。验收一眼可核：节点 pass 后 origin 上有工作分支。
- **状态**：**已批准**（2026-09-05；用户授权补 B335 origin 交接真机）
- **基线**：`origin/main` @ `38a3b9a2`

## 问题陈述

B335 合入后，squad 节点 `--target/--executor` 已真机拒绝。pass 后把工作分支推 origin 还没在活 agentd 上跑过。

## L1 plan

1. 只改 `docs/superpowers/specs/b340-ledger.md`：追加一行 `origin-publish probe`。
2. 不改 Go/TS。不点名 `--target`/`--executor`。
3. 验收：implement pass 后 origin 能 fetch 到本卡工作分支；卡事件含工作分支已发布。

本节点该干什么一律以注入的纪律块为准。
