# B233.16 契约增量：跨机执行消费面收窄到能力接口

**上游状态：已批准**（源 spec：`docs/superpowers/specs/b233.16.md`，头部状态行「已批准（2026-09-11，用户原话「批准，从 contract 起」）」已核实）
**级别：L3 轻档**
**架构形态：按子系统分域的平铺领域包，无横向 controller/service/dao 分层**（`codegraph/best.json`）
**冻结状态：随本提交冻结**
**本节点：** charter / contract；交棒：`breakdown`
**有效基线：** `cards/B233.1-charter-7`（基线提交 `c567c3d2`；本分支不切换、不越过）

L3 轻档不做本节点直通竖切；运行时最薄路径由 plan 承接。本卡不新增 HTTP 路径、事件类型、CLI 命令、依赖方向或传输协议。Ticket 0 = 消费方组合接口 + 具名消费入口 + 编译期签名锁；新增导出符号已随骨架写入本分支视图 diff（`codegraph/diffs/cards-B233.16-charter.json`）。

## 1. 范围与现状查证

本卡把「一次任务/卡节点执行的完整闭环」的跨机取用点，从聚合 `*client.Client` 收窄为能力接口：
纯执行点直接持 B233.3 冻结在提供方 `internal/client` 的 `client.ExecutionClient`；混合点在使用方包内声明最小组合接口并嵌入它；工厂 / 组装点保留聚合。

精确签名按当前工作树核对；行号是本轮读数，符号锚是引用锚。

| 现状签名 / 类型 | 代码事实出处 | 本卡契约 |
| --- | --- | --- |
| `type ExecutionClient interface { Dispatch; Reply; Continue; Stop; WaitEvent; FollowEvents }` | `internal/client/execution.go`（接口 `:35-42`；`_ Transport/ExecutionClient = (*Client)(nil)` 断言 `:50-53`） | 六方法不变、归属不变；消费方只复用，不复制、不改名、不改方法集。 |
| `func (c *Client) Dispatch(ctx, opts DispatchOpts) (*proto.Task, error)` | `internal/client/client.go`（`:762`） | 签名与 `POST /api/tasks` 不变。 |
| `func (c *Client) Reply(ctx, taskID, ticketID, answer string) error` | `internal/client/client.go`（`:689`） | 不变。 |
| `func (c *Client) Continue(ctx, taskID, instructions string) error` | `internal/client/client.go`（`:1068`） | 不变。 |
| `func (c *Client) Stop(ctx, taskID string) (bool, error)` | `internal/client/client.go`（`:1129`） | 不变。 |
| `func (c *Client) WaitEvent(ctx, taskID string, all bool) (*proto.Event, error)` | `internal/client/client.go`（`:1478`） | 不变。 |
| `func (c *Client) FollowEvents(ctx, taskID string, all bool, idle time.Duration, onEvent func(*proto.Event) error, onBacklog func(*BacklogSummary) error) error` | `internal/client/client.go`（`:1761`） | 不变。 |
| `func (c *Client) Status(ctx) (*proto.StatusResp, error)` | `internal/client/client.go`（`:484`） | 探活消费面方法；不加入 `ExecutionClient`。 |
| `func (c *Client) Attach(ctx, taskID string) (*AttachInfo, error)` | `internal/client/client.go`（`:663`） | 快照消费面方法；不加入 `ExecutionClient`。 |
| `func (c *Client) WaitArchived(ctx, taskID string) (*proto.Event, error)` | `internal/client/wait_archived.go`（`:70`） | 归档等待方法；不加入 `ExecutionClient`。 |
| `func (c *Client) Run(ctx, taskID, cmd string) (string, int, error)` | `internal/client/client.go`（`:1239`） | 一次性命令方法；不加入 `ExecutionClient`。 |
| `func (c *Client) StopAndReclaim(ctx, taskID string) error` | `internal/client/compensate.go`（`:19`） | 回收方法；不加入 `ExecutionClient`。 |
| `func (c *Client) Diff(ctx, taskID, base string) (string, error)` | `internal/client/client.go`（`:1189`） | 本轮 diff 方法；不加入 `ExecutionClient`。 |
| `func (c *Client) Done(ctx, taskID, note string) (bool, error)` | `internal/client/client.go`（`:1098`） | 归档方法；不加入 `ExecutionClient`。 |
| `func runStop(cmd *cobra.Command, c *client.Client, taskID string) error` | `cmd/stop.go`（现状 `:27`） | 收窄 `c client.ExecutionClient`。 |
| replyCmd RunE 内联 `c.Reply` | `cmd/reply.go`（现状 RunE `:47`） | 收敛为具名 `runReply(cmd, c client.ExecutionClient, taskID, answer string) error`。 |
| continueCmd RunE 内联 `c.Continue` | `cmd/continue.go`（现状 RunE `:32`） | 收敛为具名 `runContinue(cmd, c client.ExecutionClient, taskID, instructions string) error`。 |
| `func resolveBareDiscipline(ctx, cli *client.Client, rawFile string) (discipline.ResolvedDiscipline, error)` | `cmd/dispatch.go`（现状 `:185`） | 收窄 `cli dispatchClient`（组合面 +`Status`）；派发经具名 `dispatchTask`。 |
| `dispatchCmd` RunE 内联 `cli.Dispatch(...)` | `cmd/dispatch.go`（现状 `:282`） | 收敛为具名 `dispatchTask(ctx, cli client.ExecutionClient, opts client.DispatchOpts) (*proto.Task, error)`。 |
| `dispatchTransport` / `dispatchTransportWithOpts` 体内 `cl.Dispatch` | `cmd/card_dispatch.go`（现状 `:67`/`:86`） | 派发经具名 `dispatchTask`；工厂 `targetClient` 仍返回聚合。 |
| `func resolveCardDispatchDiscipline(ctx, st *ledger.Store, name, target string) (...)` | `cmd/card_dispatch.go`（现状 `:247`） | 收窄 `cl dispatchClient`（组合面 +`Status`），客户端由调用方经 `targetClient` 注入。 |
| card dispatch 补偿闭包内联 `cl.StopAndReclaim` | `cmd/card_dispatch.go`（现状 `:336-350`） | 收敛为具名 `compensateCardDispatch(ctx, cl compensateClient, cardID, target, taskID string) error`。 |
| waitCmd RunE 内联 `cli.WaitEvent` / `autoSyncAfterWait` | `cmd/wait.go`（现状 `:129`/`:149`） | 一次性消费点经收窄后的 `waitClient`；`autoSyncAfterWait` 参数收窄。 |
| `func runUntilDone(cmd, taskID, addr string, cli *client.Client) error` | `cmd/wait.go`（现状 `:167`） | 收窄 `cli waitClient`（组合面 +`WaitArchived`/`Attach`/`Status`）。 |
| `func runFollow(cmd, taskID, addr string, cli *client.Client) error` | `cmd/wait.go`（现状 `:236`） | 收窄 `cli waitClient`（仅 `FollowEvents` + 既有旁路）。 |
| `func warnIfTimeoutBelowStall(ctx, cli *client.Client, idle time.Duration)` | `cmd/wait.go`（现状 `:280`） | 收窄 `cli waitClient`（+`Status`）。 |
| `func autoSyncAfterWait(cmd, cli *client.Client, addr string, ev *proto.Event)` | `cmd/wait.go`（现状 `:334`） | 收窄 `cli waitClient`（+`Attach`）。 |
| `func newTargetClient() (*client.Client, func(), error)` / `func newTargetClientNamed(name string) (*client.Client, func(), error)` | `cmd/root.go`（现状 `:237`/`:241`） | 工厂保留聚合（组装合法）。 |
| `func targetClient(target string) (*client.Client, func(), error)` | `cmd/card_dispatch.go`（现状 `:194`） | 工厂保留聚合。 |
| `func (s *Server) stepTransport(ctx, opts ledgerstep.DispatchOpts) (string, string, error)` | `internal/agentd/cardstep.go`（现状 `:328`） | 派发经具名 `dispatchStep(ctx, cl client.ExecutionClient, opts, target)`。 |
| agentd 补偿闭包内联 `cl.StopAndReclaim` | `internal/agentd/cardstep.go`（现状 `:121-134`） | 收敛为具名 `Server.compensateStep(ctx, cl stopReclaimClient, cardID, target, taskID string) error`。 |
| `func (s *Server) pushWorkBranchOrigin(ctx, target, branch, taskID string) error` | `internal/agentd/cardstep.go`（现状 `:177`） | 命令执行经具名 `Server.pushWorkBranchVia(ctx, cl runClient, target, branch, taskID)`。 |
| `func (s *Server) resolveStepDiscipline(node, reqTarget string) (...)` / `func (s *Server) disciplineTargetCap(target string) *bool` | `internal/agentd/cardstep.go`（现状 `:251`/`:308`） | 探活经具名 `probeStatus(ctx, cl statusClient)`。 |
| `func (s *Server) clientForTarget(target string) (*client.Client, error)` | `internal/agentd/server.go`（现状 `:422`） | 取客户端总入口保留聚合（工厂/组装）。 |
| `type StepRunner struct { ... Clients func(target string) (*client.Client, error) ... }` | `internal/ledgerstep/runner.go`（现状字段 `:41`） | 字段收窄 `Clients func(target string) (StepClient, error)`。 |
| `func clientFinalMessage(ctx, cl *client.Client, taskID string) (string, error)` | `internal/ledgerstep/wire.go`（现状 `:115`） | 收窄 `cl finalMessageClient`（组合面 +`Attach`）。 |

**装配侧直通镜像**：`internal/agentd/cardstep.go` 的 `Clients` 字段装配从方法值 `s.clientForTarget` 改为闭包 `func(target string) (ledgerstep.StepClient, error) { return s.clientForTarget(target) }`——签名透传（编译期透传接线，属空壳范畴），实现体不变。

**依赖库既成行为**：本卡不改读限、拨号超时、keep-alive、握手、错误码与 HTTP 路径（沿用 B233.3 / B233.10 冻结的库行为表，逐字未动）；同进程 Go 类型收窄，不引入新 wire，故本节点不新增依赖库行为执法条目。

**对侧常量执法**（本卡触及的常量 / 字面值）

| 常量 / 字面值 | 真正生产者 | 真正消费者 | 结论 |
| --- | --- | --- | --- |
| `client.ExecutionClient`（接口符号） | B233.3 冻结于 `internal/client/execution.go` | 本卡新增的三包消费方组合接口嵌入它 | 活跃；生产消费方首次持有，不再是死接口。 |
| `client.AttachInfo`（快照 DTO） | `internal/client/client.go` 的 `Client.Attach` | `autoSyncAfterWait` / `clientFinalMessage` / `StepRunner` 各消费点 | 活跃；本卡只收窄声明，不改 wire。 |
| `ErrUnreachable` / `ErrTunnelDisconnected` | `Client.do` | CLI 降级、登记 | 活跃；本卡沿 B233.3 现状，不扩大区分。 |
| `ErrIdleTimeout` / `ErrDependencyFailed` / `ErrArchivedEventMissing` | `internal/client` 的 wait 路径 | `runFollow` / `runUntilDone` 的错误分流 | 活跃；本卡不改分流。 |
| `Status.DisciplinesSupported`（`*bool` 能力位） | agentd `/api/status` 响应 | `resolveBareDiscipline` / `resolveCardDispatchDiscipline` / `probeStatus` | 活跃；三态（nil / false / true）语义不变。 |

零使用 / 疑似漂移常量：无新增。

**图覆盖债（不得把空 `who-calls` 写成无调用方）**：`codegraph sym ExecutionClient` 未命中（B233.3 Ticket 0 新增、基线未吸收）；`runStop`、`cmd/reply.go` / `cmd/continue.go` / `cmd/stop.go` 无图节点；`StepRunner.Clients` 字段、`clientFinalMessage` 的 client 参数式调用边未入图（`d_ledger→d_transport` 直调计数为 0 即此债）。消费点清单以本节点源码查证与本分支视图 diff 为准。

## 2. 冻结接口（Ticket 0 已落码，编译通过）

以下接口与具名入口是本节点冻结物；实现票不得改名、不得另造同义入口、不得增删方法。所有接口定义在使用方，嵌入提供方内部冻结的 `client.ExecutionClient`；额外方法只含该消费点今日真实调用，不为未来预留。

### 2.1 `cmd`（`d_cli` → `d_transport`）

```go
// cmd/dispatch.go
type dispatchClient interface {
	client.ExecutionClient
	Status(ctx context.Context) (*proto.StatusResp, error)
}
func dispatchTask(ctx context.Context, cli client.ExecutionClient, opts client.DispatchOpts) (*proto.Task, error)
func resolveBareDiscipline(ctx context.Context, cli dispatchClient, rawFile string) (discipline.ResolvedDiscipline, error)

// cmd/wait.go
type waitClient interface {
	client.ExecutionClient
	WaitArchived(ctx context.Context, taskID string) (*proto.Event, error)
	Attach(ctx context.Context, taskID string) (*client.AttachInfo, error)
	Status(ctx context.Context) (*proto.StatusResp, error)
}
func runUntilDone(cmd *cobra.Command, taskID, addr string, cli waitClient) error
func runFollow(cmd *cobra.Command, taskID, addr string, cli waitClient) error
func warnIfTimeoutBelowStall(ctx context.Context, cli waitClient, idle time.Duration)
func autoSyncAfterWait(cmd *cobra.Command, cli waitClient, addr string, ev *proto.Event)

// cmd/card_dispatch.go
type compensateClient interface {
	client.ExecutionClient
	StopAndReclaim(ctx context.Context, taskID string) error
}
func resolveCardDispatchDiscipline(ctx context.Context, cl dispatchClient, st *ledger.Store, name, target string) (discipline.ResolvedDiscipline, error)
func compensateCardDispatch(ctx context.Context, cl compensateClient, cardID, target, taskID string) error

// cmd/stop.go / cmd/reply.go / cmd/continue.go
func runStop(cmd *cobra.Command, c client.ExecutionClient, taskID string) error
func runReply(cmd *cobra.Command, c client.ExecutionClient, taskID, answer string) error
func runContinue(cmd *cobra.Command, c client.ExecutionClient, taskID, instructions string) error
```

工厂与组装点保留聚合：`cmd/root.go#newTargetClient` / `#newTargetClientNamed`、`cmd/card_dispatch.go#targetClient` 返回 `*client.Client`。

### 2.2 `internal/agentd`（`d_gateway` → `d_transport`）

```go
// internal/agentd/cardstep.go
type runClient interface {
	client.ExecutionClient
	Run(ctx context.Context, taskID, cmd string) (string, int, error)
}
type statusClient interface {
	client.ExecutionClient
	Status(ctx context.Context) (*proto.StatusResp, error)
}
type stopReclaimClient interface {
	client.ExecutionClient
	StopAndReclaim(ctx context.Context, taskID string) error
}
func dispatchStep(ctx context.Context, cl client.ExecutionClient, opts ledgerstep.DispatchOpts, target string) (*proto.Task, error)
func probeStatus(ctx context.Context, cl statusClient) (*proto.StatusResp, error)
func (s *Server) compensateStep(ctx context.Context, cl stopReclaimClient, cardID, target, taskID string) error
func (s *Server) pushWorkBranchVia(ctx context.Context, cl runClient, target, branch, taskID string) error
```

取客户端总入口 `internal/agentd/server.go#Server.clientForTarget` 保留聚合。

### 2.3 `internal/ledgerstep`（`d_ledger` → `d_transport`）

```go
// internal/ledgerstep/runner.go
type StepClient interface {
	client.ExecutionClient
	Diff(ctx context.Context, taskID, base string) (string, error)
	Attach(ctx context.Context, taskID string) (*client.AttachInfo, error)
	Done(ctx context.Context, taskID, note string) (bool, error)
}
// StepRunner.Clients 字段
Clients func(target string) (StepClient, error)

// internal/ledgerstep/wire.go
type finalMessageClient interface {
	client.ExecutionClient
	Attach(ctx context.Context, taskID string) (*client.AttachInfo, error)
}
func clientFinalMessage(ctx context.Context, cl finalMessageClient, taskID string) (string, error)
```

## 3. 目标图 entries 棘轮

`codegraph/target.json` 随本提交冻结。三条入传输边的处理（**不新开方向、不改预算**）：

- `d_cli→d_transport`（预算 10）：entries 保持 `["client 实体","client.Client"]`（能力接口容器「client 实体」已在场）；仅追加 B233.16 注记。
- `d_gateway→d_transport`（预算 9）：entries 由 `["targetclient.Pool","client.Client"]` 增为 `["targetclient.Pool","client.Client","client 实体"]`——能力接口符号容器「client 实体」在场；`client.Client` 保留（图内调用边落点仍是 `*Client` 实现方法）。仅追加 B233.16 注记。
- `d_orchestration→d_transport`（预算 5）：非本卡执行消费点（`Manager` 不持 `client.ExecutionClient`），entries 与预算不动；追加 B233.16 注记。

**「聚合符号退场」的落点说明（重要，防后人误读）**：本卡消费点的聚合退场发生在**源码签名**（见本分支视图 diff 的 `nodesModified`：消费点参数类型由 `*client.Client` 改为能力组合面），而非图 entries。图内调用边仍解析到 `k_client_Client`（`label=client.Client`）的实现方法——`ExecutionClient` 接口及其方法分派不在基线图中（B233.3 覆盖债）。因此从 entries 删除 `client.Client` 是**假棘轮**：实测 `d_cli→d_transport` 会从豁免 0 条变成 legacy 71 条（预算 10，直接 over-budget 炸图）。故本卡保留聚合容器 entry，只增补能力接口容器 entry。

**spec 现状不一致（交 breakdown/协调者知悉，不退回 spec）**：spec §要变的事实 #4 写「`d_ledger→d_transport`」，§现状事实 #4 写「`d_orchestration→d_transport`」。查证结论：`d_ledger→d_transport` 在 `target.json` 中**不存在**（`internal/ledgerstep` 经 `Clients` 字段消费 client 的边图未覆盖，`d_ledger→d_transport` 直调计数 0）；`d_orchestration→d_transport` 存在但非本卡消费点。本卡按真实图处理，不新建 `d_ledger→d_transport` 方向。

## 4. 可执行冻结

命中可执行冻结（哈希 / 密钥派生 / 编码格式）：**无命中**——本卡是同进程 Go 类型收窄，不产生可计算金样本。

本卡的可执行冻结以 **T1 编译期签名锁**落地（金样本测试形态：签名赋值断言）：

- `cmd/execution_signature_lock_test.go`：`runStop` / `runReply` / `runContinue` / `dispatchTask` / `resolveBareDiscipline` / `resolveCardDispatchDiscipline` / `compensateCardDispatch` / `runUntilDone` / `runFollow` / `warnIfTimeoutBelowStall` / `autoSyncAfterWait` 的参数类型钉在能力接口上。
- `internal/agentd/execution_signature_lock_test.go`：`dispatchStep` / `probeStatus` / `(*Server).compensateStep` / `(*Server).pushWorkBranchVia` / `(*Server).stepTransport`。
- `internal/ledgerstep/execution_signature_lock_test.go`：`(*StepRunner).Clients` 字段类型 / `clientFinalMessage`。

**变异证据（本轮实跑，配对还原）**：把 `runStop` 的第二参数、`probeStatus` 的第二参数、`StepRunner.Clients` 的字段类型分别改回 `*client.Client`，`go vet` 立即编译失败：

```
# github.com/Xsxdot/handoff/cmd
vet: cmd/execution_signature_lock_test.go:20:94: cannot use runStop (value of type ... *client.Client ...) as ... ExecutionClient ... value
# github.com/Xsxdot/handoff/internal/agentd
vet: ... cannot use probeStatus ... as ... statusClient ... value
# github.com/Xsxdot/handoff/internal/ledgerstep
vet: ... cannot use (&StepRunner{}).Clients ... as func(string) (StepClient, error) ... value
```

三次变异均在断言后还原，`go build ./...` 退出码 0。

## 5. 冻结清单

每条单独可判 pass/fail，失败可定位。实现票逐条对账。

### 5.1 接口归属与方法集

1. `client.ExecutionClient` 定义仍在 `internal/client`，六方法签名与 B233.3 冻结逐字一致。
2. 消费方组合接口（`cmd.dispatchClient` / `cmd.waitClient` / `cmd.compensateClient` / `agentd.runClient` / `agentd.statusClient` / `agentd.stopReclaimClient` / `ledgerstep.StepClient` / `ledgerstep.finalMessageClient`）均定义在各自使用方包内。
3. 每个消费方组合接口都嵌入 `client.ExecutionClient`，不复制其六方法。
4. `cmd.dispatchClient` 额外方法集恰为 `{Status}`。
5. `cmd.waitClient` 额外方法集恰为 `{WaitArchived, Attach, Status}`。
6. `cmd.compensateClient` 额外方法集恰为 `{StopAndReclaim}`。
7. `agentd.runClient` 额外方法集恰为 `{Run}`。
8. `agentd.statusClient` 额外方法集恰为 `{Status}`。
9. `agentd.stopReclaimClient` 额外方法集恰为 `{StopAndReclaim}`。
10. `ledgerstep.StepClient` 额外方法集恰为 `{Diff, Attach, Done}`。
11. `ledgerstep.finalMessageClient` 额外方法集恰为 `{Attach}`。
12. 任一组合接口都不含 `HTTPClient`、工作区、卡、机器、PTY 方法。

### 5.2 消费点依赖声明

13. `runStop` 的第二参数类型是 `client.ExecutionClient`。
14. `runReply` 的客户端参数类型是 `client.ExecutionClient`。
15. `runContinue` 的客户端参数类型是 `client.ExecutionClient`。
16. `dispatchTask` 的客户端参数类型是 `client.ExecutionClient`。
17. `resolveBareDiscipline` 的客户端参数类型是 `dispatchClient`。
18. `resolveCardDispatchDiscipline` 的客户端参数类型是 `dispatchClient`。
19. `compensateCardDispatch` 的客户端参数类型是 `compensateClient`。
20. `runUntilDone` / `runFollow` / `warnIfTimeoutBelowStall` / `autoSyncAfterWait` 的客户端参数类型是 `waitClient`。
21. `dispatchStep` 的客户端参数类型是 `client.ExecutionClient`。
22. `probeStatus` 的客户端参数类型是 `statusClient`。
23. `(*Server).compensateStep` 的客户端参数类型是 `stopReclaimClient`。
24. `(*Server).pushWorkBranchVia` 的客户端参数类型是 `runClient`。
25. `StepRunner.Clients` 的字段类型是 `func(target string) (StepClient, error)`。
26. `clientFinalMessage` 的客户端参数类型是 `finalMessageClient`。

### 5.3 具名入口与可注入性

27. `cmd` reply 路径的跨机调用落在具名 `runReply` 上，由 `replyCmd` RunE 调用；RunE 不再内联 `c.Reply`。
28. `cmd` continue 路径的跨机调用落在具名 `runContinue` 上，由 `continueCmd` RunE 调用；RunE 不再内联 `c.Continue`。
29. `cmd` 裸派发的跨机调用经具名 `dispatchTask`，`dispatchCmd` RunE 与 `dispatchTransport` / `dispatchTransportWithOpts` 都不再直接内联 `cli.Dispatch` / `cl.Dispatch`。
30. card dispatch 的补偿跨机调用落在具名 `compensateCardDispatch` 上。
31. agentd 节点派发的跨机调用经具名 `dispatchStep`。
32. agentd 探活的跨机调用经具名 `probeStatus`。
33. agentd 补偿的跨机调用落在具名 `(*Server).compensateStep` 上。
34. agentd 推分支的命令执行落在具名 `(*Server).pushWorkBranchVia` 上。

### 5.4 编译期锁（有牙）

35. `cmd/execution_signature_lock_test.go` 的签名赋值断言覆盖冻结清单 13–20。
36. `internal/agentd/execution_signature_lock_test.go` 的签名赋值断言覆盖冻结清单 21–24 与 `stepTransport`。
37. `internal/ledgerstep/execution_signature_lock_test.go` 的签名赋值断言覆盖冻结清单 25–26。
38. 把冻结清单 13–26 中任一参数 / 字段类型改回 `*client.Client`，对应包的 `go build` / `go vet` 编译失败。

### 5.5 工厂与组装点

39. `cmd/root.go#newTargetClient` 与 `#newTargetClientNamed` 仍返回 `*client.Client`。
40. `cmd/card_dispatch.go#targetClient` 仍返回 `*client.Client`。
41. `internal/agentd/server.go#Server.clientForTarget` 仍返回 `*client.Client`。
42. `internal/targetclient` 的 `New` / `Pool.For` / `NewLocal` 签名不变。
43. `internal/agentd/cardstep.go` 的 `Clients` 装配闭包只做 `s.clientForTarget` 签名透传，不改选路。

### 5.6 行为不变

44. HTTP 路径不变：`POST /api/tasks`、`/reply`、`/continue`、`/stop`、事件 WS、`GET /api/tasks/{id}`、`/done`、`/api/status`。
45. JSON 键不变；事件类型字面值不变；不新增 HTTP 路径、CLI 命令、事件类型。
46. CLI stdout / stderr 契约不变：`reply`/`continue` 成功单行 `{"ok":true}`；`dispatch` 单行任务 JSON；`wait` 单行事件 JSON。
47. 退出码与错误文案不变：`wait --timeout` 的 `ExitTimeout`、`ErrDependencyFailed` / `ErrArchivedEventMissing` 分流逐字不变。
48. 超时值不变：探活 10s、`--follow` 15s stall 核对窗、`unaryAPITimeout=30s`、`dialTimeout=10s`。
49. 失败诊断语义不变：`ErrUnreachable` 与 `ErrTunnelDisconnected` 的区分沿 B233.3 现状。
50. `StepRunner.awaitNode` 仍走 `WaitEvent` 等终态后取 `clientFinalMessage`，归档顺序（PublishWorkBranch → Done）不变。

### 5.7 目标图

51. `d_cli→d_transport` entries 含 `client 实体` 与 `client.Client`，预算 10。
52. `d_gateway→d_transport` entries 含 `targetclient.Pool`、`client.Client`、`client 实体`，预算 9。
53. `d_orchestration→d_transport` 预算 5、entries 不变。
54. 本卡不新建 `d_ledger→d_transport` 方向，不增删任何契约方向，不改任何预算。
55. `codegraph check` 相对基线不新增 fail / warn。
56. 本分支视图 diff 收录本节点新符号（8 个接口模型 + 8 个具名函数/方法），随骨架同提交。

## 6. 拍板记录（三重闸门）

命中且只命中以下一条（难逆转 + 无上下文会惊讶 + 真取舍）：

1. **组合接口只嵌入 `client.ExecutionClient`，不把 `Status`/`Attach`/`WaitArchived`/`Run`/`StopAndReclaim`/`Diff`/`Done` 加进 `ExecutionClient`。** 难逆转：`ExecutionClient` 是 B233.3 跨子系统冻结物，加方法要动 CLI / gateway / ledger 三域与图契约；无上下文时后人看到 wait/cardstep 到处写组合接口会想「统一塞进 ExecutionClient 更省事」；真取舍：被否方案是把辅助方法并入冻结接口（违反 B233.3 冻结 13 与卡 note「不把工作区/卡/PTY 塞进该接口」），或为每个消费点复制一份六方法（语义分裂成多份）。反过来写（把 `Status` 加进 `ExecutionClient`）现有签名锁 T1 不会自动变红（锁的是消费点参数类型，不是接口方法集），故必须靠冻结清单 1–12 锁住。

**未命中三重闸门的实现级决定**（不写拍板记录，落文中说明即可）：组合接口的命名（`dispatchClient`/`waitClient`/…）、`finalMessageClient` 是否嵌套 `StepClient`、具名入口落在哪个既有文件、条目的逐字新增。它们改了对契约对侧不可见，属单包实现选择。

**显式不做**（spec 已否，本节点复核不翻案）：只收纯执行点；一次收到全消费面（GC / preview / PTY / 项目 / 机器 / 事件镜像）；让工厂 / 池返回窄接口；新造 HTTP 路径、事件类型、CLI 命令、依赖方向或传输协议；借收窄改任何 wire 行为、错误文案、超时或日志契约。

## 7. 移交 plan 附区

以下不占冻结条目、不参与逐条打勾；plan 出稿时吸收。

- **T2 最小替身注入**：为每个消费点配只实现所需方法的测试替身，经命令入口（CLI）、装配点（agentd）、`StepRunner` 字段（ledgerstep）替换后跑通既有用例；替身不含 `HTTPClient`、工作区、卡、PTY 方法。
- **T3 机械守卫**：扩展现有 `cmd/execution_surface_test.go`，把「执行面文件不得持聚合依赖」纳入源码扫描，白名单只许工厂所在符号（`newTargetClient` / `newTargetClientNamed` / `targetClient`）。
- **T4 行为不变**：`go test ./...` 全绿；B229 / B233.10 / B233.12 / B233.13 定向用例逐条复跑。
- `cmd/reply.go` / `cmd/continue.go` 的具名入口新增后，既有命令测试的 RunE 路径不变；无需改测试入口。
- `dispatchTransport` / `dispatchTransportWithOpts` 是 `cmd` 既有测试缝，本轮只把其内联 `Dispatch` 收敛到 `dispatchTask`，缝签名不动；plan 复核 `swapDispatchTransport` 系列用例。
- 探活 `probeStatus` 统一 10s 时限：`resolveStepDiscipline` 与 `disciplineTargetCap` 两处原各自 `context.WithTimeout(..., 10*time.Second)` 合并到 `probeStatus`；plan 确认无行为漂移（`disciplineTargetCap` 的本机短路分支不取 client）。
- 事件镜像（`internal/ledgermirror`）与其它非执行能力面的消费点收窄不在本卡，落 `docs/roadmap.md`。

## 8. 本节点法定状态

- 契约增量文档：本文件；每个跨接缝签名均带现状代码出处。
- Ticket 0：本节点已落消费方组合接口、具名入口、`StepRunner.Clients` 字段收窄与装配直通镜像；**本轮编译通过**（`go build ./...` 退出码 0）。
- 目标图：`codegraph/target.json` 随本提交冻结；本分支视图 diff `codegraph/diffs/cards-B233.16-charter.json` 收录 16 个新增节点（8 接口模型 + 8 具名函数/方法）、9 个修改节点。
- 直通竖切：L3 轻档不要求；运行时最薄路径由 plan 承接。
- 可执行冻结：哈希 / 密钥派生 / 编码格式**无命中**；T1 编译期签名锁已落地并跑过，变异证据见 §4。
- 三重闸门拍板：§6，1 项。
- 欠账：T2 最小替身、T3 机械守卫扩展、T4 行为复跑、图对账由 breakdown → plan → implement 承接；本节点不把「接口已收窄、测试待补」写成已验收。
