# B300 执行台账

- 2026-08-29：用户报告「本机开远程终端，grok/opencode 等 TUI 选中文字自动复制但到不了本机」，要求评估修法与是否并入 B272。
- 2026-08-29：`handoff card show B272` —— B272「把本机的文件或者图片拖到远程机器的终端的时候，把文件传过去」，charter 流待办列，无 spec。用户裁定剪贴板问题单独建卡、不与 B272 关联。
- 2026-08-29：`handoff card add` 建卡 B300（charter 待办，优先级中），`card note` 记初查结论；后 `card move B300 spec` 进 spec 列。
- 2026-08-29：grep `web/src/app/workbench/` 无 clipboard/copy 实现（负事实）；`TerminalTab.tsx:36` 引用 `desktopFileDrop` 仅为「拖文件插入路径」桌面壳通道，与剪贴板无关。
- 2026-08-29：grep `internal/ptyhost/wire/` 无 osc/escape/filter 逻辑（负事实）——输出字节流透传，OSC 52 可达浏览器。
- 2026-08-29：`web/package.json` 读数：`@xterm/xterm ^5.5.0`、`@xterm/addon-fit ^0.10.0`、`@xterm/addon-webgl ^0.18.0`——5.x 有 `parser.registerOscHandler`。
- 2026-08-29：`codegraph sym TerminalTab` 命中 `d_web` 域（`n_web_app_workbench_TerminalTab`，anchor `web/src/app/workbench/TerminalTab.tsx`），图覆盖无债。
- 2026-08-29：读 `web/src/api/pty.ts:102-135` —— `attached` 控制帧含 `since` / `truncated` / `backlog_bytes`（缺键表示旧服务端）；据此确认「积压重放门」可纯前端实现，无需动 wire 契约。
- 2026-08-29：读 `web/src/app/workbench/terminalHostResponse.ts` 全文——该文件管反方向（xterm 设备回包不上送），其注释佐证「切 tab / 重连会把环形缓冲整段重放进新的 xterm，历史转义序列会再次触发解析器行为」的机制，是重放门必要性的依据。
- 2026-08-29：读 `web/src/app/workbench/TuiTab.tsx` 头部——TuiTab 是任务对话帧流渲染（`useFramesStream`），非 xterm 终端，不涉剪贴板，划出范围。
- 2026-08-29：spec 定稿落 `docs/superpowers/specs/b300.md`，定级 L2，待用户批准。
- 2026-08-29：spec 头部回写批准判决（已批准，L2）后，执行 `git add docs/superpowers/specs/b300.md docs/superpowers/specs/b300-ledger.md && git commit -m "docs(spec): B300 终端 TUI 复制落到本机剪贴板（OSC 52）"`，输出 `2 files changed, 88 insertions(+)`；本条台账与 spec 同批 amend 收口（amend 换 hash，不回填追认）。
- 2026-08-29：plan 节点基线复核：`cd web && npm test` 实际 1181 passed（约 32s）；`npx vitest run src/app/workbench/TerminalTab.test.tsx` 实际 47 passed；`npm run lint` 实际存量 23 problems（5 errors, 18 warnings）——lint 判据取「不新增」。
- 2026-08-29：库事实查证：`web/node_modules/@xterm/xterm/typings/xterm.d.ts:1822` 确认 `registerOscHandler(ident, callback): IDisposable`；TerminalTab 挂载 effect 已有 `hostReply: 'drop-all'|'replay'|'live'` 重放状态机（`TerminalTab.tsx:252`、`:420-435`），OSC 门直接复用，不新造计数。
- 2026-08-29：plan 落盘 `docs/superpowers/plans/b300-plan.md`（三 task：解码纯函数 → 注册+三道门+失败提示 → 全量门禁；真机清单六条标注协调者执行）。执行位置变更声明：用户 2026-08-29 指示「直接全做完，需要派发的用子 agent」——后续 implement/review 以本地 subagent 执行，卡生命周期照走。
- 2026-08-29：Task1 绿：落地 `terminalOsc52.ts`（plan 全文）后复跑 `npx vitest run src/app/workbench/terminalOsc52.test.ts` → `Tests 11 passed (11)`，Duration 588ms。
- 2026-08-29：Task1 提交：`git add web/src/app/workbench/terminalOsc52.ts web/src/app/workbench/terminalOsc52.test.ts && git commit` → `6cf284ae3 feat(web): OSC 52 载荷解码纯函数与表驱动测试（B300）`，`2 files changed, 82 insertions(+)`；存量脏改动（WorkbenchPage.tsx、.b266-* 等未跟踪文件）未触碰。
- 2026-08-29：Task2 首红：替身改造（termInstance 加 `parser.registerOscHandler`、beforeAll 定义 `navigator.clipboard`、beforeEach 复位 osc52Handler/writeText）+ 7 支新测试落地后，`npx vitest run src/app/workbench/TerminalTab.test.tsx` → `Tests 7 failed | 47 passed (54)`，失败均为 `TypeError: osc52Handler is not a function`（组件未注册 handler），既有 47 支全绿。
- 2026-08-29：Task2 绿：`terminalDebug.ts` 追加 `logTermOsc52`；`TerminalTab.tsx` 五处（import×2、copyNotice state、noticeTimer、term.parser.registerOscHandler(52) 三道门 handler、cleanup clearTimeout+dispose、JSX copy-notice 块）按 plan 落地后复跑 → `Tests 54 passed (54)`，Duration 1.66s（47 既有 + 7 新增）。
- 2026-08-29：Task2 提交：`git add web/src/app/workbench/TerminalTab.tsx web/src/app/workbench/TerminalTab.test.tsx web/src/app/workbench/terminalDebug.ts && git commit` → `4a8b3e385 feat(web): 终端 OSC 52 复制落到本机剪贴板（B300）`，`3 files changed, 139 insertions(+), 1 deletion(-)`；WorkbenchPage.tsx 等无关脏改动未 add。
- 2026-08-29：Task3 首轮全量 `npm test` 红：`Tests 16 failed | 1183 passed (1199)`，红在 `Shell.test.tsx`（14/44）与 `WorkbenchKeepAlive.test.tsx`（2/2）；失败原文 `TypeError: Cannot read properties of undefined (reading 'registerOscHandler') @ TerminalTab.tsx:310`。
- 2026-08-29：根因（debug 纪律，失败原文直接指认）：全仓共三处 xterm 整体替身（`grep vi.mock('@xterm/xterm')`），plan Task 2 只改造了 `TerminalTab.test.tsx` 一处；`Shell.test.tsx` 与 `WorkbenchKeepAlive.test.tsx` 各自的 `termInstance` 无 `parser` 键，B300 后 TerminalTab 挂载即调 `term.parser.registerOscHandler`，effect 内 TypeError 挂载即崩。plan 文件清单与代码现实的矛盾（既有测试因替身改造而红），按预案小步修正。
- 2026-08-29：修正：两处替身补 `parser: { registerOscHandler: vi.fn(() => ({ dispose: vi.fn() })) }`（照 plan 替身形状；二者不测 OSC 52 行为，不捕获 handler）；拒绝 `term.parser?.` 生产代码防御——那是替替身缺键打的补丁，且把剪贴板功能变回静默缺席。复跑两文件 → `Tests 46 passed (46)`，Duration 5.12s。
- 2026-08-29：替身修复提交：`git add web/src/app/shell/Shell.test.tsx && git commit` → `739d1ad8f test(web): Shell 的 xterm 替身补 parser 键适配 OSC 52 注册（B300 波及）`，`1 file changed, 2 insertions(+)`。
- 2026-08-29：Task3 门禁终读数（修复后新鲜全量）：`npm run typecheck`（tsc -b）exit 0；`npm test` → `Test Files 115 passed (115)`、`Tests 1199 passed (1199)`，Duration 22.34s（基线 1181 + 缝1 的 11 + 缝2 的 7）；`npm run lint` → `✖ 23 problems (5 errors, 18 warnings)`，与基线持平零新增。三条门禁全过。
- 2026-08-29：偏离声明：`Shell.test.tsx` 替身修复**随分支提交**（不提交则分支自身门禁破：新检出必红 14 支），超出 plan 点名 add 清单，属「plan 与代码现实矛盾」预案内的小步修正；`WorkbenchKeepAlive.test.tsx` 为未跟踪存量文件，按 add 清单约束**不提交**，修复只留在本机工作树（该文件不在分支上，新检出全量本就不含它）。
- 2026-08-29：台账随最终提交入库（本条及此前 implement 期各条）；本卡分支 `cards/B300-charter` 提交序列：`6cf284ae3`（Task1 解码纯函数+测试）→ `4a8b3e385`（Task2 注册+三道门+失败提示）→ `739d1ad8f`（Shell 替身 parser 键，波及修正）→ 台账收口提交。未 push；真机清单六条留 acceptance（协调者执行）。
