# B233.24 spec+plan（L1）：基线锚保鲜——validate 完整性问题清零

## 背景（spec）

- 来源：`docs/roadmap.md`「来自 B233.17 验收」validate ~130 条条目；用户残余清单第 2 类。
- 基线实况（59dfb16c5）：`codegraph validate` = 130 issues + 4 edgeIssues
  （典型：no-import——handler 节点仍锚在旧文件 server.go；基线图里还有已不存在的文件
  reclaim.go）。正主是 B233.23 的全量重扫，本卡收残余。

## 目标（spec）

`codegraph validate` issues=0 且 edgeIssues=0（`--stale` 保鲜问题如有，逐条列明并修）。

## 验收（spec）

1. `codegraph validate` 退出 0；
2. `codegraph check` 保持 fails=0（不因修锚引入新违约）；
3. 每处数据修订可追溯到「盘上真实代码」（file:line 与符号签名真实存在）。

## plan（L1 三行）

1. B233.23 重扫后跑 `codegraph validate` 取残余清单（预期大头已被重扫消掉）；
2. 残余逐条修：基线/视图数据的锚重锚、缺失 packages 条目按配方补齐、summary 按转录规则
   修正；禁止为凑绿改源码；
3. 验收：validate 退出 0；check 仍 fails=0。
