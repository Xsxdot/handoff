# B332 真机：机器人协调者走完 L1 并回复房间

- **卡**：B332
- **级别与档位**：**L1**。plan 增量为零：补一条与现有 grok 环境坐下用例对称的 claude 环境坐下 CLI 测试。验收一眼可核：`go test` 该用例绿。
- **状态**：**已批准**（2026-09-04；本卡是 19964630 二进制升级后的真机狗粮，用户要求机器人协调者走完 charter L1，房间有人说话必须回复）
- **基线**：`origin/main` @ `19964630`
- **发现自**：B329/B330 合入后升级本机与 linux-01 二进制的真机验证。不 reopen B329 / B330。
- **姐妹卡**：B333（本 grok bind 坐下；禁止改 `cmd/room_test.go`）

## 问题陈述

B329 已有 `TestCardBindUsesGrokSessionEnvironment`（无 flag、只靠 `GROK_SESSION_ID` 坐下）。对称的 claude 宿主路径只在 `currentSeatIdentity` 单测里出现，CLI `card bind` 没有对等用例。本卡补上这条缝，并让机器协调者把 L1 快道真机走完。

## 方案

在 `cmd/card_driver_test.go` 增加 `TestCardBindUsesClaudeSessionEnvironment`：

1. `clearSeatSourceEnv`
2. `CLAUDE_CODE_SESSION_ID=claude-bind`
3. 无 `--cli/--session` 的 `card bind`
4. 席位必须是 `cli:claude#claude-bind`，`DriverSource=bind`
5. 形态对齐现有 `TestCardBindUsesGrokSessionEnvironment`

禁止改产品代码。禁止改 `cmd/room_test.go`（B333 的文件）。

## 用户故事

1. 作为机器协调者：坐下本卡后走完 L1（spec 已在本文件，挂 `spec:` 与 `plan:`，move implement，`--step implement`，review 过，finish 可 FF）。
2. 作为用户：在本卡房间说话，协调者必须 `--kind reply` 回复。

## 实现决定

改 `cmd/card_driver_test.go` 一处，紧挨 grok 环境坐下用例。不改 `cmd/*.go` 非测试文件。

## 测试决定

缝：`card bind` CLI（调用方本测试）× `currentSeatIdentity` 的 claude 宿主分支。

1. `go test ./cmd/ -run TestCardBindUsesClaudeSessionEnvironment -count=1` 绿。
2. 变异：若仍只认 `HANDOFF_SESSION_*`、不读 `CLAUDE_CODE_SESSION_ID`，本测试必须红。

## L1 plan（同一文件，过 implement 门）

1. 在 `cmd/card_driver_test.go` 的 `TestCardBindUsesGrokSessionEnvironment` 旁加 `TestCardBindUsesClaudeSessionEnvironment`，断言无 flag 的 bind 写下 `cli:claude#claude-bind`。
2. `go test ./cmd/ -run TestCardBindUsesClaudeSessionEnvironment -count=1`。
3. 不改 `cmd/room_test.go`，不改产品代码。

验收：上述测试绿。本节点该干什么一律以注入的纪律块为准。房间用户发言必须回复。

## Out of Scope

- **永不做**：给 coordinate 注册 `--cli/--session`；用 `USER`/hostname 推导席位。
- **本期不做**：Pi/Kimi/Codex Desktop 宿主探测；给 runner 加 linux-01 成员。
- **后续要做**：无。本卡是真机狗粮。
