# B286 spec 台账

- 2026-08-28 用户「还有最后一批是吧？做完吧」。原始六批 bug 已合完。剩余高优 charter 卡 C7/C8/C11。C7/C8 剩余代码在 handoff，C11 在 charter，拆成 B286 + C11 两张 L2。
- 2026-08-28 origin/main `f8e252ef3`（含 B234）。linux-01 agentd `016aef7e` 2026-08-25。本机 agentd 仍旧。
- 2026-08-28 `handoff template show charter-default` Version=5，Def.target 空。B271 已把空 target 收成本机。
- 2026-08-28 `cmd/card_node.go` `runStepDispatch`：202 后固定「已受理」，退出 0。`TestCardDispatchStepReturnsImmediately` 锁住该 stdout。
- 2026-08-28 `DispatchSnapshot` 已有 `target`/`branch`/`base`/`base_commit`/`discipline_name`。CLI 不读。
- 2026-08-28 `NodeStep.RunOnce` 只在 `Produces != nil` 时 `Diff`。review 节点 produces 空。`Override.Purpose` 可区分 review。
- 2026-08-28 C7 卡 note 已更正原题（v4 并无 target）。剩余是 202 静默 + 基线静默。修法「缺 target 当场拒」与 B271 冲突，弃选。
- 2026-08-28 C8 法条矛盾半已由 B229.7 删除平台层台账句。真越轨样本 C1.5（生产代码）与 C1.11（测试文件）。白名单只留台账目录。
- 2026-08-28 `handoff project ls --target linux-01` 有 handoff 与 charter。B286 派 handoff 仓。
- 2026-08-28 未跑 codegraph CLI（不在 PATH）。
- 2026-08-28 独立审查总判修订后再批（C1 水位/通道；I1–I8）。r1 吸收：Follow 且 POST 前水位；成功行不打本地 ref/origin；fetch 族移出必堵；Diff 失败不落裁决；Name=review purpose 空负例；探活 400；skill 改 441 与 607。批准推进。
- 2026-08-28 计划节点基线：当前分支为 `cards/B286-charter`，HEAD=`5cfa8274`，工作树初始干净；规格要求产出 `docs/superpowers/plans/b286-plan.md`，台账仍为本文件。
- 2026-08-28 仓内存在 `codegraph/best.json`；按平台不变量运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_cli`、`d_execution`、`d_ledger`、`d_execution_contract` 均失败，原文为 `go: downloading github.com/Xsxdot/charter/graph v0.10.0`、`go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.mod376605260.tmp: read-only file system`、`open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.lock: read-only file system`；本计划改用源码核对并保留图覆盖债。
- 2026-08-28 基线命令 `go test ./cmd -run 'TestCardDispatchStepReturnsImmediately' -count=1` 与 `go test ./cmd -count=1` 均失败；原始输出含 `go: downloading github.com/Xsxdot/charter/graph v0.10.0`、`go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.mod652271194.tmp: read-only file system`、`# github.com/Xsxdot/handoff/cmd`、`internal/agentd/codegraph.go:22:2: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.lock: read-only file system`、`FAIL github.com/Xsxdot/handoff/cmd [setup failed]`。
- 2026-08-28 基线命令 `go test ./internal/ledgerstep -run 'TestReviewStepPassAndFailLoop|TestNodeStepWithoutProducesDoesNotInvokeOutputHooks' -count=1` 实际通过，原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgerstep 0.233s`；`go test ./internal/ledgerstep -count=1` 的并行结果未产生可见输出，状态未验证。
- 2026-08-28 将 `GOMODCACHE` 指向可写任务临时目录后，`env GOMODCACHE=/root/.handoff/tmp/1031b0e7/gomodcache go test ./cmd -run 'TestCardDispatchStepReturnsImmediately' -count=1` 实际通过，原始末行 `ok github.com/Xsxdot/handoff/cmd 0.141s`。
- 2026-08-28 将 `GOMODCACHE` 指向可写任务临时目录后，`env GOMODCACHE=/root/.handoff/tmp/1031b0e7/gomodcache go test ./internal/ledgerstep -count=1` 实际通过，原始末行 `ok github.com/Xsxdot/handoff/internal/ledgerstep 6.494s`。
- 2026-08-28 源码核对：`cmd/card_node.go:22` 的 `runStepDispatch(cmd *cobra.Command, id, node string) error` 当前在 `cmd/card_node.go:45-50` 只调用 `client.Client.CardStep`、打印固定「已受理」行并返回 nil；`cmd/card_dispatch.go:274-282` 的 `cardDispatchCmd` 将 `--step` 直接路由到它。
- 2026-08-28 源码核对：`internal/ledger/mirror.go:179-185` 的 `func (s *Store) MaxSeq() (int64, error)` 读取全账本最大 seq；`internal/ledger/follow.go:18-71` 的 `func (s *Store) Follow(ctx context.Context, members func() ([]string, error), fromSeq int64, pollInterval time.Duration, onEvent func(Event) error) error` 从 `fromSeq` 排他读取、按升序推进 cursor，并在回调返回错误时结束。
- 2026-08-28 源码核对：`internal/ledgerstep/node.go:133-310` 的 `func (n *NodeStep) RunOnce(ctx context.Context, cardID string) (Outcome, error)` 当前顺序是 Dispatch→Await→ParseVerdict→`RecordReviewVerdict`→撤等人→仅当 `verdict.Pass && n.Node.Produces != nil` 时调用 `n.Diff`→路由；`internal/ledgerstep/runner.go:378-401` 的 `diffNode` 通过 `Client.Diff(ctx, taskID, "")` 投影 `ChangedPaths`。
- 2026-08-28 源码核对：`internal/ledger/types.go:161-180` 的 `NodeOverride.Purpose` 是节点级用途覆盖，`ledger.PurposeReview` 常量值为 `"review"`；`NodeDef.Produces` 在 `internal/ledger/types.go:218-220` 为可空声明，故只应以 `Override.Purpose` 触发 C8，不以节点名或模板用途触发。
- 2026-08-28 测试夹具核对：`cmd/card_dispatch_test.go:77-89` 的 `newCardStepCLIEndpoint` 将 mock agentd 与临时 `ledger.db` 写入同一 `DataDir`；`cmd/ledgercli_test.go:22-45` 的 `runLedgerCLI` 经真实 root 命令返回 stdout/stderr/error；`internal/ledgerstep/node_test.go:12-21` 的 `nodeLedger` 加载 `seedLedgerStepStore` 后创建卡，适合直接从 `NodeStep.RunOnce` 声明缝注入 Dispatch/Await/Diff。
- 2026-08-28 缺陷族清单出处为 `docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md:76-89`：生命周期/状态机中断、静默失败/误导报错、跨平台假设、假红测试、门禁绕过，以及序列化边界和新增枚举值白名单设问；本卡不触及 Web/浏览器 API，因此 webview 第六族不适用。
- 2026-08-28 审查正文 `docs/superpowers/reviews/b286-spec-review.md:24-35,57-88,97-114` 已把 C7 的 POST 前水位/Follow/仅新事件、C8 的 Diff 失败不落裁决与 purpose 负例、同步探活 400、skill 441/607 和真实调用方 `cmd/card_dispatch.go` 作为必须吸收项；本计划按这些已批准修订编写。
- 2026-08-28 `git rev-parse --verify origin/fix/b286-step-review` 与 `git merge-base HEAD origin/fix/b286-step-review` 均输出 `5cfa827464f4b736f02bd092efa2c42f8ba01d17`；当前工作树仅有本计划节点此前追加的台账修改。
- 2026-08-28 将 `GOMODCACHE` 指向可写任务临时目录后，`env GOMODCACHE=/root/.handoff/tmp/1031b0e7/gomodcache go test ./cmd -count=1` 实际通过，原始输出为 `ok github.com/Xsxdot/handoff/cmd 10.003s`。
- 2026-08-28 计划 `docs/superpowers/plans/b286-plan.md` 已落盘：含 C7 `runStepDispatch` 接缝和 C8 `NodeStep.RunOnce` 接缝的精确 Consumes/Produces、完整红测代码、最小实现代码块、日志/注释步骤、绿测命令、序列化边界、缺陷族、真机清单和 spec 故事归属；实现代码未改。
- 2026-08-28 计划自检命令 `rg -n 'TBD|TODO|同 Task|适当错误处理|适当的错误处理|<[^>]+>' docs/superpowers/plans/b286-plan.md` 实际退出 1，原始输出为空；表示未命中这些计划占位文本或尖括号占位符。
- 2026-08-28 计划结构核对命令 `rg -n '^## (1|2|3|4)\\.|^### [1-4]\\.[1-9]|Consumes：|Produces|基线|最小测试范围|slog|注释|序列化边界|缺陷族|接缝双向' docs/superpowers/plans/b286-plan.md` 实际退出 0，输出包含四个主节、两个 task 的基线/测试范围/日志/注释/序列化/缺陷族/接缝段及 Consumes/Produces；原始输出已由本回合命令取得。
- 2026-08-28 计划自检命令 `git diff --check` 实际退出 0，原始输出为空。
- 2026-08-28 计划提交前 `git diff --name-only` 实际输出仅 `docs/superpowers/specs/b286-ledger.md`；计划文件因尚未跟踪不出现在该命令输出。`git status --short --branch` 实际输出为 `## cards/B286-charter`、` M docs/superpowers/specs/b286-ledger.md`、`?? docs/superpowers/plans/b286-plan.md`。
- 2026-08-28 首次文档提交实际成功：`b3447286 docs(B286): add implementation plan`；`git show --stat --oneline --summary HEAD` 实际显示 `docs/superpowers/plans/b286-plan.md | 987`、`docs/superpowers/specs/b286-ledger.md | 20`、共 `1007 insertions(+)`。提交后 `git status --short --branch` 实际仅输出 `## cards/B286-charter`；`git diff --check` 实际退出 0 且无输出；再次占位符扫描实际退出 1 且无输出。
- 2026-08-28 台账核验提交实际成功：`617e7dc55170c744b97d789eb9865a707089aa5d docs(B286): record plan verification`；提交前后 `git status --short --branch` 实际仅输出 `## cards/B286-charter`，`git diff --check` 实际退出 0 且无输出；`git log -2 --oneline` 实际输出 `617e7dc5 docs(B286): record plan verification`、`b3447286 docs(B286): add implementation plan`。
