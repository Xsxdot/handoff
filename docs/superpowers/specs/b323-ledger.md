# B323 台账

- 2026-09-04：B156.3 真机验收。探针 B321 coordinate 241s rebuilt=false output_bytes=1487，room read count=0。隔离 HOME `handoff status` first-run 新 token → 401。
- 2026-09-04：B265 房间 hi → 载体已更换 循环。resume 477ms stderr 38 字节 = `Error: Session not found`。热路径 B321 二次唤醒 rebuilt=false。
- 2026-09-04：GET /coordinator command=`opencode --session <id>` 无 HOME。无 HOME 跑 → Session not found；`HOME=/Users/sycm/.handoff/home/muse` 同样命令 TUI 能起。
- 2026-09-04：muse 隔离 HOME 无 AGENTS.md/skills；`copyMainCredential` 注释「不搬运技能/规则树」；probe_test 锁 skills 不存在。
- 2026-09-04：仓库 `repos/handoff/~/.handoff/home/muse` 约 167M。`expandTurnHomeDir` 失败原样返回。
- 2026-09-04：用户 OK bug-batch。主卡 B323，并入 B324–B327。排除 B328/B329/B330。wait 不改。工作树 `/Users/sycm/.handoff/worktrees/b323-home` 分支 `cards/B323-charter` 自 `origin/main` bcc424cbd。主仓 `fix/B322-restore-pump` 脏，未在那棵树上写 spec。
- 2026-09-04 现状读数（worktree HEAD）：
  - `copyMainCredential` 只拷表内相对路径，空目录才拷。
  - `attachLocator` 拼 `cli --session id`。
  - `keystone.Locate` miss 重建 ref 无 HomeDir。
  - `Wake` 会 overlay spec.HomeDir，故冷 Resume 日志里有 home_dir 仍 Session not found → 会话不在该 HOME 的 db。
  - `config.DefaultPath` = `UserHomeDir()/.handoff/config.yaml`。
- 2026-09-04 分流：架构级（隔离 HOME 供给+使用同一路径）走本卡 spec，不在排查现场改。不镜像 Output 进房间。
- 2026-09-04：独立审查 `docs/superpowers/reviews/b323-spec-review.md`。无 Critical，维持 L2。吸收 I1–I6 为 r1 即批准。I5 选叫机器人补缺失表内 CLI 凭据（已存在不覆盖）。
- 2026-09-04 10:01：基线命令 `go test ./internal/hostapi -count=1` 原始结果：`ok   github.com/Xsxdot/handoff/internal/hostapi  0.790s`，退出码 0。
- 2026-09-04 10:01：基线命令 `go test ./internal/keystone -count=1` 原始结果：`ok   github.com/Xsxdot/handoff/internal/keystone  0.151s`，退出码 0。
- 2026-09-04 10:01：基线命令 `go test ./internal/agentd -run 'Test(Coord|Wake|ResumeTurnRequest)' -count=1` 原始结果：`ok   github.com/Xsxdot/handoff/internal/agentd  2.347s`，退出码 0。
- 2026-09-04 10:01：基线命令 `go test ./internal/config ./internal/toolchain ./internal/skill -count=1` 原始结果：`ok   github.com/Xsxdot/handoff/internal/config 0.101s`; `ok   github.com/Xsxdot/handoff/internal/toolchain 0.022s`; `ok   github.com/Xsxdot/handoff/internal/skill 0.008s`，退出码 0。
- 2026-09-04 10:01：基线命令 `go build ./...` 原始结果：标准输出为空，退出码 0。
- 2026-09-04 10:04：代码图 `context agentd` 原始失败：`ERROR context domain not found domain=agentd`，候选 best 领域包含 `d_execution_host`、`d_keystone`、`d_gateway` 等；按错误提示改查 `context d_execution_host`，命中 `internal/hostapi` / `internal/prochost` 及 `n_hostapi_Host_RunTurn`。
- 2026-09-04 10:04：代码图 `sym RunTurn` 命中 `n_hostapi_Host_RunTurn`，签名为 `func (h *Host) RunTurn(ctx context.Context, req TurnRequest) (TurnReply, error)`，文件 `internal/hostapi/hostapi.go:66`；`flow n_hostapi_Host_RunTurn` 仅返回 `return runTurn(ctx, req)`，并报告 `coordinatorRunner.Resume` 调用者，未覆盖 Launch 链路。
- 2026-09-04 10:04：代码图对 `keystone.Service.Locate`、`attachLocator`、`coordinatorRunner` 的直接符号查询分别未命中/仅给近似候选；随后用候选节点确认 `attachLocator.Locate` 位于 `internal/agentd/server.go:2581`，`coordinatorRunner.Launch/Resume` 位于 `internal/agentd/server.go:2522/2530`。判断：B323 计划标注 keystone/agentd runner/locator 的图覆盖债，源码核对作为补充。
- 2026-09-04 10:07：`internal/ledger/store.go:49-60` 证实 DSN 以 `postgres://`/`postgresql://` 开头才走 PG，否则直接作为 SQLite 文件路径；计划因此要求隔离配置中的相对 SQLite DSN 先 `filepath.Abs`，URL DSN 原样保留。
- 2026-09-04 10:07：序列化边界核对：`internal/proto/contract_fixture_test.go:176-180` 与 `web/src/api/testdata/CoordinatorStatus.json:1-9` 仍写旧 attach 命令 `opencode --session sess-coord`；`web/src/api/scheduling.fetch.test.ts:83-99` 与 `web/src/api/contract.test.ts:632-645` 也逐字锁该旧样例。判断：计划只更新协调者 attach fixture/测试期望，不改 `CarrierRunCommandResp` 的既有 `HOME=~` 合约。
- 2026-09-04 10:10：基线命令 `go test ./internal/proto -count=1` 原始结果：`ok   github.com/Xsxdot/handoff/internal/proto 0.003s`，退出码 0。
- 2026-09-04 10:10：`web/package.json` 现有脚本为 `test: vitest run`、`typecheck: tsc -b`、`build: tsc -b && vite build`；计划把协调者状态 fixture 回归限定到 `npm test -- --run src/api/scheduling.fetch.test.ts src/api/contract.test.ts` 与 `npm run typecheck`，不触碰前端字段类型。
- 2026-09-04 10:11：基线前端命令 `npm test -- --run src/api/scheduling.fetch.test.ts src/api/contract.test.ts`（cwd `web`）失败，原始输出为 `> web@0.0.0 test`、`> vitest run --run src/api/scheduling.fetch.test.ts src/api/contract.test.ts`、`sh: 1: vitest: not found`。
- 2026-09-04 10:11：基线前端命令 `npm run typecheck`（cwd `web`）失败，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`、`sh: 1: tsc: not found`；计划保留该命令作为实现后验收，当前只能记未验证。
- 2026-09-04 10:18：B323 计划写入 `docs/superpowers/plans/b323-plan.md`。计划决定 coordinator 全套采用普通文件/目录复制，源/目标 symlink 用 `Lstat` 拒绝；只写 `.handoff/config.yaml`、`.config/opencode/AGENTS.md`、`.config/opencode/skills/` 和缺失的表内凭据，绝不整树 `RemoveAll` 或改写 session db。
- 2026-09-04 10:18：计划决定冷 `Locate` 通过 keystone 进程内 `SessionRefResolver` 在 locator 前补 HOME；agentd 读取唯一 coordinator squad 的成员声明顺序，复用已上线过滤和 `Carrier` 只读，不调用 `LaunchAdmit`，HTTP 测试比较 GET 前后 squad/carrier running 计数均为 0。
- 2026-09-04 10:18：计划决定 `Host.RunTurn` 的展开失败通过公开 RunTurn 两支测试锁住，`buildEnv` 返回展开结果给 driver 日志；attach 采用服务端 shell word，带空格路径单引号并用 `sh -n` 经 HTTP 缝校验，`SessionSpec`/`SessionRef`/HTTP 字段形状不变。
- 2026-09-04 10:18：计划自审通过：spec 故事 1→Task 1/3，故事 2→Task 3/keystone 保持 rebuild，故事 3→Task 2/3，故事 4→Task 1/3，故事 5→Task 1/3；占位符扫描无 `TBD`/`TODO`/“同 Task”；web 依赖缺失的两条基线命令已按原始报错记为未验证。
- 2026-09-04 10:23：提交计划与台账命令 `git commit -m "docs(b323): 编写协调者隔离 HOME 实现计划"` 原始输出：`[cards/B323-charter 1d26f29e] docs(b323): 编写协调者隔离 HOME 实现计划`；`2 files changed, 1047 insertions(+)`；`create mode 100644 docs/superpowers/plans/b323-plan.md`。
- 2026-09-04 本执行节点核对：当前分支为 `cards/B323-charter-2`，HEAD 为 `7e2e9fb18a9dfb9a5bf68a7e3c8a2ae6f03044b6`，工作树初始为空；`b323-plan.md` 末尾明确本节点只提交计划与同步台账，不实现代码、不派发、不调用 handoff CLI、不起 executor。
- 2026-09-04 本执行节点判断：本回合不新增生产代码或测试；法定产出是保留已提交的 B323 spec/plan，并把本节点核对事实写入同一台账。
- 2026-09-04 本执行节点验证：命令 `git diff --check` 退出码 0，标准输出为空。
- 2026-09-04 本执行节点提交命令 `git add docs/superpowers/specs/b323-ledger.md && git commit -m "docs(b323): 记录计划节点核对"` 原始输出：`[cards/B323-charter-2 2714b308] docs(b323): 记录计划节点核对`；`1 file changed, 3 insertions(+)`。
- 2026-09-04 12:41：当前实现分支为 `cards/B323-charter-3`，HEAD `45c0c440`，工作树初始为空；按平台不变量亲自运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_execution_host`，退出码 0，命中 `internal/hostapi` 与 `Host.RunTurn`，输出含 `unscannedEntries=5` 警告。
- 2026-09-04 12:43：先新增 `RunTurn` 成功/失败两条公开缝测试并运行 `go test ./internal/hostapi -run 'TestRunTurn(ExpandsTildeHomeDirAtSeam|HomeExpansionFailureDoesNotLaunch)$' -count=1`；首轮编译红原因为内部 `buildEnv` 测试预先适配了新返回值，撤回后再次运行得到真实断言红：`TestRunTurnHomeExpansionFailureDoesNotLaunch` 原始失败 `展开失败必须带原串，err=<nil>`，成功支仍通过。
- 2026-09-04 12:44：`keystone` 新缝首红为 `SetSessionRefResolver undefined`；落端口壳后再次运行两条 Locate 测试得到断言红，hot 实际 command 为 `HOME=~/hot opencode --session sess-new`，cold 实际 command 为 `HOME= opencode --session sess-new`。实现 resolver 交接后 `gofmt ... && go test ./internal/keystone -run 'TestLocate(HotRefResolvesHomeBeforeLocator|ColdRefResolvesRegisteredHomeBeforeLocator)$' -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/keystone 0.005s`。
- 2026-09-04 12:45：Task 1 实现 `ExpandHomePath` 导出、`buildEnv` 返回 `(env, expandedHome, error)`，并在 `cmd.Start` 前 fail-closed；`gofmt` 后 `go test ./internal/hostapi -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/hostapi 0.635s`，退出码 0。
- 2026-09-04 12:47：新增协调者供给测试后，首红为 `coordinatorHomeSupplier`/`prepareHome` 缺席；类型壳后真实断言红为隔离配置读回默认 first-run token。接入供给器后 `go test ./internal/agentd -run '^TestCoordinatorHome' -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/agentd 0.007s`，覆盖 Launch/Resume、skills/auth 白名单、占用 session 保留。
- 2026-09-04 12:49：新增 HTTP cold Locate/空格 HOME/attach 展开失败三缝；首红为 `attachLocator.expandHome` 字段缺席，字段壳后再次运行真实行为红：cold command=`opencode --session sess-cold`、空格 command 同样无 HOME、展开失败实际 HTTP `200 {"machine":"","dir":"/repo/handoff","command":"opencode --session sess-coord"}`。
- 2026-09-04 12:51：协调者 runner 接入 `prepareHome` 后第一次回归因测试配置缺 `StallTimeout` 得到原始错误：`校验配置 ...: stalltimeout 必须为正时长（当前 0s）；省略该键即用默认 2h`；补齐测试夹具配置后 `go test ./internal/agentd -run '^TestCoordinatorHome' -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/agentd 0.007s`。
- 2026-09-04 12:53：三条 HTTP 缝实现 attach resolver/绝对 HOME/命令 quoting 与冷读登记载体后，`go test ./internal/agentd -run 'TestCoord(StatusColdLocateUsesRegisteredHomeWithoutAdmission|StatusQuotesHomePathWithSpaces|AttachHomeExpansionFailureReturns400)$' -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/agentd 0.532s`。
- 2026-09-04 12:55：为 `normalizeCoordinatorSpec` 补 TDD 顺序：撤回无测试实现及调度调用，新增测试；符号首红为 `undefined: normalizeCoordinatorSpec`，壳后断言红为 `空 HomeDir 必须被拒绝: <nil>`；重新实现并接回 Launch/Wake 后 `go test ./internal/agentd -run '^TestNormalizeCoordinatorSpecRequiresAbsoluteHome$' -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/agentd 0.004s`。
- 2026-09-04 12:57：Task 4 更新 Go fixture sample 与 TS 精确 command 断言；按计划亲自运行 `go test ./internal/proto/ -run TestContractFixtures -update -count=1`，原始结果 `ok github.com/Xsxdot/handoff/internal/proto 0.004s`。随后 `go test ./internal/proto -run TestContractFixtures -count=1` 原始结果 `ok github.com/Xsxdot/handoff/internal/proto 0.004s`，`git diff --check` 标准输出为空、退出码 0；JSON command 已为 `HOME=/repo/coordinator opencode --session sess-coord`，CarrierRunCommand fixture 未改。
- 2026-09-04 12:59：触及包局部回归命令 `go test ./internal/hostapi -count=1 && go test ./internal/keystone -count=1 && go test ./internal/agentd -run 'Test(Coord|Wake|ResumeTurnRequest|CoordinatorHome)' -count=1 && go test ./internal/proto ./internal/config ./internal/toolchain ./internal/skill -count=1` 原始结果依次为：`ok github.com/Xsxdot/handoff/internal/hostapi 0.644s`；`ok github.com/Xsxdot/handoff/internal/keystone 0.118s`；`ok github.com/Xsxdot/handoff/internal/agentd 3.441s`；`ok` 分别为 proto `0.004s`、config `0.033s`、toolchain `0.004s`、skill `0.015s`，退出码 0。
- 2026-09-04 13:00：变异前以 `rg -F -n 'return ExpandHomePath(homeDir)' internal/hostapi/driver.go` 核对命中唯一，输出行 258，随后 `go build ./...` 退出码 0；变异为 `return homeDir, nil` 后单条 `go test ./internal/hostapi -run '^TestRunTurnHomeExpansionFailureDoesNotLaunch$' -count=1` 原始断言红：`展开失败必须带原串，err=<nil>`。变异全量 `go test ./internal/hostapi -count=1` 退出码 1，原始失败包含 `TestBuildEnvExpandsTildeHomeDir` 的字面 `HOME=~`、`TestRunTurnExpandsTildeHomeDirAtSeam` 的字面 HOME、`TestRunTurnHomeExpansionFailureDoesNotLaunch` 的 `err=<nil>`；已恢复语义。
- 2026-09-04 13:01：按计划运行 web 回归（cwd `web`）。`npm test -- --run src/api/scheduling.fetch.test.ts src/api/contract.test.ts` 退出码 127，原始输出 `> web@0.0.0 test`、`> vitest run --run src/api/scheduling.fetch.test.ts src/api/contract.test.ts`、`sh: 1: vitest: not found`；`npm run typecheck` 退出码 127，原始输出 `> web@0.0.0 typecheck`、`> tsc -b`、`sh: 1: tsc: not found`。前端测试与类型检查未验证。
- 2026-09-04 13:03：白名单自审新增 `TestCoordinatorHomeSupplierRejectsSymlinkedRuleParent`；现实现首轮红为 `规则父路径 symlink 必须被拒绝: <nil>`，随后加入逐级 `Lstat` 路径检查，`go test ./internal/agentd -run '^TestCoordinatorHome' -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/agentd 0.009s`。
- 2026-09-04 13:07：全量 agentd 回归 `go test ./internal/agentd -count=1` 曾连续运行约 197 秒、期间无中间输出，最终原始结果：`ok github.com/Xsxdot/handoff/internal/agentd 196.883s`，退出码 0。
- 2026-09-04 13:13：gofmt 收口后触及包回归 `go test ./internal/hostapi ./internal/keystone ./internal/agentd ./internal/proto ./internal/config ./internal/toolchain ./internal/skill -count=1` 原始结果：hostapi `ok ... 0.631s`、keystone `ok ... 0.106s`、agentd `ok ... 202.633s`、proto `ok ... 0.005s`、config `ok ... 0.015s`、toolchain `ok ... 0.002s`、skill `ok ... 0.010s`，退出码 0。
- 2026-09-04 13:14：恢复变异并亲自运行全量 Go 编译 `go build ./...`，退出码 0，标准输出为空。
- 2026-09-04 13:15：按平台图闸运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check`，退出码 0，原始 JSON 顶层 `"fails": []`；存在既有 `anchor-off-domain`、`container-unplaced`、`legacy`、`oversized-package` 等 warns，本次未改图文件。
- 2026-09-04 13:22：集成全量 `go test ./... -count=1` 退出码 0。原始结果含根包 `ok github.com/Xsxdot/handoff 0.878s`、cmd `ok ... 19.108s`、agentd `ok ... 186.351s`、hostapi `ok ... 0.628s`、keystone `ok ... 0.144s`、proto `ok ... 0.009s`；其余有测试包均为 `ok`，无测试包显示 `[no test files]`。
- 2026-09-04 13:25：提交前检查 `gofmt -l`（本卡全部 Go 文件）无输出、退出码 0；`git diff --check` 无输出、退出码 0；最新 `go build ./...` 退出码 0、标准输出为空。当前改动仅覆盖 B323 台账、hostapi/keystone/agentd Go 实现与测试，以及 CoordinatorStatus Go/TS fixture 与断言；web 测试/类型检查因 `vitest`/`tsc` 缺失仍未验证。
- 2026-09-04 13:26：提交命令 `git add docs/superpowers/specs/b323-ledger.md internal/agentd/coordapi_test.go internal/agentd/coordinator_home.go internal/agentd/coordinator_home_test.go internal/agentd/scheddrain.go internal/agentd/server.go internal/hostapi/driver.go internal/hostapi/probe.go internal/hostapi/probe_test.go internal/hostapi/runturn_test.go internal/keystone/keystone.go internal/keystone/keystone_spec_test.go internal/proto/contract_fixture_test.go web/src/api/contract.test.ts web/src/api/scheduling.fetch.test.ts web/src/api/testdata/CoordinatorStatus.json && git commit -m "fix(b323): isolate coordinator home across runs and attach"` 原始输出：`[cards/B323-charter-3 80ddbd1f] fix(b323): isolate coordinator home across runs and attach`；`16 files changed, 1081 insertions(+), 46 deletions(-)`；新增 `internal/agentd/coordinator_home.go` 与 `internal/agentd/coordinator_home_test.go`。
- 2026-09-04 本执行节点核对：当前分支为 `cards/B323-charter-4`，HEAD 为 `1c725dbf768fd99760feaed1e83c4bbb8ddedcb8`，工作树初始为空；补充失败点为 macOS `t.TempDir` 位于 `/var/folders`，系统别名 `/var -> /private/var` 被 `rejectCoordinatorSymlinkPath` 误判为供给越界；规则目录父路径 symlink 拒绝测试仍必须保留。
- 2026-09-04 本执行节点 TDD：新增系统卷别名回归测试首轮编译红为 `undefined: coordinatorLstat` / `undefined: coordinatorEvalSymlinks`；落测试缝壳并让路径检查使用默认 `os.Lstat` 后，断言红为 `协调者路径含 symlink "/var"`。
- 2026-09-04 本执行节点修复：`rejectCoordinatorSymlinkPath` 仅放行根路径 `/var` 且 `filepath.EvalSymlinks` 精确解析为 `/private/var` 的系统卷别名；普通规则父路径 symlink 仍拒绝。命令 `go test ./internal/agentd -run '^Test(CoordinatorHomeSupplierRejectsSymlinkedRuleParent|RejectCoordinatorSymlinkPathAllowsSystemVolumeVarAlias)$' -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/agentd 0.004s`，退出码 0。
- 2026-09-04 本执行节点编译验证：命令 `go build ./...` 退出码 0，标准输出为空。
- 2026-09-04 本执行节点变异自验：确认 `/private/var` 比较表达式命中唯一后取反；变异版 `go build ./...` 退出码 0；单条系统别名行为测试原始失败为 `协调者路径含 symlink "/var"`；变异版 `go test ./internal/agentd -count=1` 原始结果为该同一测试失败，退出码 1，耗时 146.431s。结论：新增守卫确实被测试拦截。
- 2026-09-04 本执行节点恢复验证：恢复 `/private/var` 精确比较后，命令 `gofmt -w internal/agentd/coordinator_home.go internal/agentd/coordinator_home_test.go && go test ./internal/agentd -run '^Test(CoordinatorHomeSupplierRejectsSymlinkedRuleParent|RejectCoordinatorSymlinkPathAllowsSystemVolumeVarAlias)$' -count=1` 原始结果：`ok github.com/Xsxdot/handoff/internal/agentd 0.004s`，退出码 0。
- 2026-09-04 本执行节点平台编译：命令 `GOOS=darwin GOARCH=arm64 go test ./internal/agentd -run '^Test(CoordinatorHomeLaunchAndResumeSupplyFullHome|CoordinatorHomeSupplierRejectsSymlinkedRuleParent|RejectCoordinatorSymlinkPathAllowsSystemVolumeVarAlias)$' -count=1 -c -o /root/.handoff/tmp/605ea58a/agentd-darwin.test` 退出码 0，标准输出为空；仅验证 Darwin arm64 测试二进制可编译，未在本机执行 Darwin 二进制。
- 2026-09-04 本执行节点收口检查：恢复后 `go build ./...` 退出码 0、标准输出为空；`git diff --check` 退出码 0、标准输出为空；`gofmt -l internal/agentd/coordinator_home.go internal/agentd/coordinator_home_test.go` 退出码 0、标准输出为空。待提交改动仅为本台账、路径检查修复及回归测试。
- 2026-09-04 本执行节点提交命令 `git commit -m "fix(b323): tolerate macOS system var alias"` 原始输出：`[cards/B323-charter-4 8bbddc05] fix(b323): tolerate macOS system var alias`；`3 files changed, 60 insertions(+), 2 deletions(-)`。
