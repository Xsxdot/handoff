# B278 spec 台账

- 2026-08-28 用户「把第三批和第六批合成一批修」。第三批 = B235/B257，第六批 = B251/B260。聚合卡 B278。
- 工作树 `/Users/sycm/.handoff/worktrees/batch-dispatch-wire`，分支 `fix/dispatch-wire`（从 origin/main @ `3ae31175a`，其上 B277 只加了 Go flows 扫描文档）。
- B235 源卡实证：BM1.1 拍板 `e38140d` 在 `origin/card/BM1.1-judge-calibration`，下一节点 `cards/BM1.1-charter-3` 从 `82f2767`（charter-2）起。
- B235 代码：`ViaTemplate` 有工作分支时覆盖 `base` 并 `LocalBaseBranch=true`（`internal/ledgerstep/dispatch.go:214-217`）。`resolveLocalBaseBranch` 故意不 fetch（`internal/agentd/workspace.go:1023-1030`）。`DispatchSnapshot` 无起点 sha。
- B235 源卡第一方案「改用卡 base_branch」否决：会丢掉工作分支上上一节点的代码，与 B192 冲突。本期改为同名分支快进并集 + 不同名只警告。
- B257 源卡实证：B156.3.1 / B156.3.7 并发首派同一 `effective_base_branch`，后到者 `cannot lock ref` 被包进「基线提交在任务仓库中不存在」。重派即成功。
- B257 代码：`ResolveBaseBranch` fetch 失败 `%w ErrBaseCommitMissing`（`workspace.go:1152-1156`）；HTTP `writeDispatchError` 一律当「任务仓库落后」（`server.go:1214-1216`）。根因是同进程双 goroutine 同仓库 fetch，不是基线缺失。修法：仓库路径互斥 + 残留重试 + 独立 sentinel。
- B251 源卡：B249 breakdown 写成 `2026-08-25-b249-breakdown.md`，声明路径无日期。`containsPath` 精确相等（`node.go:303-309`），失败 `haltForHuman`。plan `on_fail=breakdown`，故不能改成裁决 fail。B250 已完成，阻塞边仍在。
- B251 弃选放宽匹配：produces 是机器键（B201 / product-backlog「精确字符串相等」）。
- B260 源卡：CLI `card show` 的 `tasks` 大驼峰，`card` 蛇形。`TaskLink` / `ledger.Relation` 无 json tag。HTTP `proto.TaskStateRow` / `proto.Relation` 注释刻意 PascalCase，Web `ledger.ts` 按 `TaskID`/`From` 消费。本期只改 CLI；relations 同文档一并改。
- `internal/ledger/wire_test.go` 钉了 Card/Event/Decision，没钉 TaskLink/Relation。
- 图：本机无 `codegraph` 二进制；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve ...` 输出空。定位靠 grep / 读码。
- 无前端页面形态，不走原型。
- 2026-08-28 独立审查 `01a0464e` 被进程重启打断；`01a04667` 续跑出完整报告。Critical 3 / Important 10。协调者吸收见 `docs/superpowers/reviews/b278-spec-review.md`。
- r1 关键吸收：`--step` 不赌 CLI stderr；不同名不每次警告；fetch 失败退回本地；禁止 FETCH_HEAD；Transport 回传 BaseCommit；skill 只改仓内 `skills/handoff/SKILL.md`；HTTP 负例；重试 50–200ms ×2。
- 用户 2026-08-28「老样子」授权批准 r1 并无人值守推进到合 main。
- 2026-08-28 acceptance：复跑定向缝测试全绿；六发变异均编译过且对应缝级测试转红后还原；真机 `go run . card show B278` 的 `tasks[].task_id` 可读。对象 `1fc1ecd64`。审查三项 minor 记账不修。跳过图对账。
- 2026-08-28 执行节点基线复核：`git status --short --branch` 输出 `## cards/B278-charter`，`git log -1 --oneline` 输出 `c840605a docs(b278): spec r1 批准——吸收独立审查`；工作树无未提交改动。
- 2026-08-28 执行节点复核：仓内存在 `codegraph/`、`codegraph/best.json`、`codegraph/baseline.json`；`go.mod` 声明 `github.com/Xsxdot/charter/graph v0.9.0`。
- 2026-08-28 执行节点运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_workspace d_ledger d_gateway d_cli d_transport`（分域并行调用）；命令成功返回 context JSON，输出含 `view: baseline`、`best=true`、各领域声明/接口/主链和 `actual`，同时出现焦点配额/未扫描入口提示；结果不能据此断言无调用方。
- 2026-08-28 执行节点运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --help`；成功列出 `context`、`chain`、`sym`、`who-calls`、`entity` 等子命令，确认后续查图入口。
- 2026-08-28 执行节点 `sym` 实测：`resolveLocalBaseBranch`、`containsPath` 不在图中并返回「图未覆盖或名字有误」；`resolveDispatchBase` 为 `func resolveDispatchBase(ctx context.Context, repo, rev string) (resolved string, fetched bool, err error)`（`internal/agentd/workspace.go:1011`）；`ResolveBaseline` 为 `func ResolveBaseline(ctx context.Context, repo, sha string) (Baseline, error)`（`workspace.go:1097`）；`ResolveBaseBranch` 为 `func ResolveBaseBranch(ctx context.Context, repo, remote, branch string) (string, error)`（`workspace.go:1140`）。
- 2026-08-28 执行节点 `sym` 实测：`Store.RecordDispatch` 为 `func (s *Store) RecordDispatch(cardID string, snap DispatchSnapshot) error`（`internal/ledger/events.go:141`）；基线 `Transport` 为 `type Transport func(ctx context.Context, opts DispatchOpts) (taskID string, err error)`、`DispatchResult` 当前只有 Card/Task/Target/Branch/Template/TemplateVersion/DisciplineName/DisciplineVersion（`internal/ledgerstep/dispatch.go:46-49`）；`cliTransport` 为 `func cliTransport(ctx context.Context, opts ledgerstep.DispatchOpts) (string, error)`（`cmd/card_dispatch.go:105`），`Server.stepTransport` 为 `func (s *Server) stepTransport(ctx context.Context, opts ledgerstep.DispatchOpts) (string, error)`（`internal/agentd/cardstep.go:178`）。
- 2026-08-28 执行节点 `sym` 实测：`Server.writeDispatchError` 为 `func (s *Server) writeDispatchError(w http.ResponseWriter, projectRef string, err error)`（`internal/agentd/server.go:1206`）；`buildPrompt` 为 `func buildPrompt(body string, c ledger.Card, base string, carry bool, extra string) string`（`internal/ledgerstep/dispatch.go:380`）；`TaskLink` 在 `internal/ledger/tasks.go:14`，`Relation` 在 `internal/ledger/types.go:118`，HTTP 侧另有 `internal/proto/ledger.go:36` 与 Web 侧 `web/src/api/ledger.ts:53`。
- 2026-08-28 执行节点补读源码发现代码图 baseline 与工作树源码存在漂移：`internal/ledgerstep/dispatch.go:380` 实际签名为 `func buildPrompt(body string, c ledger.Card, base string, carry, omitAccept bool, extra, outputPath string) string`，而 `sym buildPrompt` 返回旧 5 参数签名；计划以当前源码为准并把该图保鲜债列入文档。
- 2026-08-28 执行节点运行 `git branch --list 'fix/dispatch-wire' 'cards/B278-charter'; git merge-base HEAD fix/dispatch-wire`；仅列出 `cards/B278-charter`，并返回原文 `fatal: Not a valid object name fix/dispatch-wire`，当前仓库没有本地有效的 `fix/dispatch-wire` ref；未切换分支，合并目标只按规格文字保留。
- 2026-08-28 执行节点基线测试（未改实现）：`go test ./internal/agentd -run 'Test(ResolveBaseline|ResolveDispatchBase|ResolveBaseBranch|DispatchWireLocalBaseBranch|DispatchAutoBranchStartsAtBaseCommit|DispatchRecordsBaseline|DispatchBaseBranchName)' -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/agentd 1.456s`，退出 0。
- 2026-08-28 执行节点基线测试：`go test ./internal/ledgerstep -run 'Test(ViaTemplate|BuildPrompt|NodeStep|Runner)' -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/ledgerstep 5.714s`，退出 0；`go test ./internal/ledger -run 'Test(RecordDispatch|LinkTask|AddRelation|Wire)' -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/ledger 0.323s`，退出 0；`go test ./cmd -run 'Test(CardDispatch|CardShow|Dispatch)' -count=1` 输出 `ok  github.com/Xsxdot/handoff/cmd 2.947s`，退出 0。
- 2026-08-28 执行节点基线测试：`go test ./internal/agentd -run 'TestDispatchWireLocalBaseBranchEndToEnd|TestDispatchCardEmptyBaseStartsAtOriginDefaultTip|TestDispatchWithoutCardDefaultMarkerKeepsEmptyBaseHead|TestDispatchCardDefaultBaseRejectsMissingOriginHead' -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/agentd 0.646s`，退出 0。
- 2026-08-28 执行节点查证仓内技能：`skills/handoff/SKILL.md:339-460` 的账本模式说明 `card dispatch --step` 为 202 受理且进度看 `card wait`，本卡计划必须把 B251 的日期前缀约束写入该仓内文件，不能依赖 CLI stderr。
- 2026-08-28 执行节点查证测试接缝：`internal/agentd` 当前没有 `gitRunNet` 注入变量或现成 fetch 锁实现；网络 fetch 集中在 `workspace.go:240`、`ResolveBaseline:1112`、`ResolveBaseBranch:1152`，clone 另见 `projectadmin.go:542`。B257 计划需使用仅限 fetch/ref-read 的私有接缝，且 clone 保持直接调用；B235 的同名分支验收使用真实临时 Git 仓库，不与 B257 复用假 Git 框架。
- 2026-08-28 执行节点查证 CLI 测试接缝：`cmd/card_dispatch.go:61-142` 有 `dispatchTransport`、`dispatchTransportWithOpts` 及 `swapDispatchTransport*`，当前回调均返回 `(string, error)`；B235 计划必须逐一升级为携带 40 字符 `base_commit` 的精确签名并保留既有测试替换缝。
- 2026-08-28 执行节点基线补测：`go test ./internal/agentd -run 'TestLedgerAPI' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.249s`，退出 0；`go test ./internal/agentd -run 'TestRegisterProject|TestClone' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 2.714s`，退出 0。后者为 B257 的 clone 不入 fetch 锁验收基线。
- 2026-08-28 当前执行节点重跑 B257/B235/B251/B260 计划基线：`go test ./internal/agentd -run 'Test(ResolveBaseline|ResolveDispatchBase|ResolveBaseBranch|DispatchWireLocalBaseBranch|DispatchAutoBranchStartsAtBaseCommit|DispatchRecordsBaseline|DispatchBaseBranchName)' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 1.832s`；B235 集成命令输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.792s`；`go test ./internal/ledgerstep -run 'Test(ViaTemplate|BuildPrompt|NodeStep|Runner)' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/ledgerstep 6.216s`；ledger 输出 `ok github.com/Xsxdot/handoff/internal/ledger 0.639s`；cmd 输出 `ok github.com/Xsxdot/handoff/cmd 3.134s`；Ledger API 输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.476s`；clone 基线输出 `ok github.com/Xsxdot/handoff/internal/agentd 2.581s`，均退出 0。
- 2026-08-28 当前执行节点按平台不变量运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_workspace d_ledger d_gateway d_cli d_transport`，原始结果为 `Error: accepts 1 arg(s), received 5` 及 usage；该多焦点调用未验证，后续只使用合法单领域 codegraph 查询或源码核对。
- 2026-08-28 当前执行节点新增 B257 接缝测试后首红：`go test ./internal/agentd -run 'TestResolve(BaseBranchConcurrentFetchUsesRemoteRef|BaselineAndBranchShareRepoFetchLock|BaseBranchLockContentionHasIndependentSentinel)' -count=1` 编译失败，原文为 `undefined: runNetFetch` 与 `undefined: errFetchRefLockContention`；确认是待实现符号缺席而非测试拼写错误。
- 2026-08-28 B257 空壳补齐后断言红：上述测试实跑出现真实原文 `cannot lock ref`，且锁竞争用例仍得到 `基线提交在任务仓库中不存在`；说明调用方尚未接入重试/独立 sentinel。新增 server 测试首次编译红，原文为 `env.srv.writeDispatchError undefined (cannot refer to unexported method)`，因现有 `server_test.go` 为 `agentd_test` 黑盒包，测试改放同包白盒测试位置，不新增导出生产 API。
- 2026-08-28 B257 锁与归因实现后实跑：`gofmt -w internal/agentd/workspace.go internal/agentd/workspace_test.go internal/agentd/server.go` 后，`go test ./internal/agentd -run 'TestResolve(BaseBranchConcurrentFetchUsesRemoteRef|BaselineAndBranchShareRepoFetchLock|BaseBranchLockContentionHasIndependentSentinel|BaseBranchAlwaysFetches|BaselineMissingRejects)|TestWriteDispatchErrorKeepsFetchLockContentionDistinct' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.910s`，退出 0。
- 2026-08-28 B257 扩展验收实跑：`go test ./internal/agentd -run 'Test(ResolveBaseline|ResolveDispatchBase|ResolveBaseBranch|DispatchWireLocalBaseBranch|DispatchAutoBranchStartsAtBaseCommit|DispatchRecordsBaseline|DispatchBaseBranchName|ResolveBaseBranchConcurrentFetchUsesRemoteRef|ResolveBaselineAndBranchShareRepoFetchLock|ResolveBaseBranchLockContentionHasIndependentSentinel)|TestWriteDispatchErrorKeepsFetchLockContentionDistinct' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 2.391s`；同三条并发/clone 用例 `-race` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 1.733s`；`go test ./internal/agentd -run 'TestRegisterProject|TestClone' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 2.266s`；`go build ./...` 无输出、退出 0。
- 2026-08-28 B235 失败测试初跑暴露一个测试夹具错误：origin 领先用例直接在未推送的临时克隆 checkout `work`，原始输出为 `error: pathspec 'work' did not match any file(s) known to git`；已修正为先在被测克隆创建并 push `work`，再由 origin writer 追加提交。该次不作为生产结论。
- 2026-08-28 执行节点落盘计划初稿：`docs/superpowers/plans/b278-plan.md` 已覆盖 B257→B235→B251/B260 DAG、精确接口、基线命令、六条接缝、八条用户故事、序列化边界、缺陷族和仓内 skill/HTTP/clone/FETCH_HEAD 约束；尚未提交，待自审与 `git diff --check`。
- 2026-08-28 执行节点自审：首次 `git diff --cached --check` 报原始 trailing whitespace 三行（`docs/superpowers/plans/b278-plan.md:3-5`）；已移除 Markdown 行尾空格，随后同命令无输出、退出 0，暂存文件仅为计划与本账本，共 2 个文件、395 行新增。
- 2026-08-28 执行节点完成本节点落盘：计划与账本在当前分支提交后复核工作树保持 clean，`git log -1 --oneline` 指向本次 docs(b278) 提交；不 push、不切分支。
# 2026-08-28 B235 local base resolution and transport verification
- Test red before implementation: `go test ./internal/agentd -run 'TestResolveLocalBaseBranch(UsesLocalTipWhenLocalIsAhead|UsesOriginTipWhenOriginIsAhead|FallsBackWhenOriginUnavailable|RejectsDivergedTips)' -count=1` failed with local-ahead/origin-ahead/diverged assertions because the existing resolver returned the local ref without fetching origin.
- Implemented B235 local resolver: fetches `origin/<branch>` under the per-repository lock, falls back to local on any fetch/ref-read failure, compares ancestry without moving refs, and returns the independent divergence sentinel with both full SHAs.
- Targeted verification: `go test ./internal/agentd -run 'TestResolveLocalBaseBranch(UsesLocalTipWhenLocalIsAhead|UsesOriginTipWhenOriginIsAhead|FallsBackWhenOriginUnavailable|RejectsDivergedTips)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/agentd 1.041s`.
- Transport contract was changed to return `(taskID, baseCommit, err)`; `ViaTemplate` records `base` and `base_commit` in the result and dispatch snapshot. `go test ./internal/ledgerstep -run TestViaTemplateCarriesTransportBaseCommitIntoResultAndSnapshot -count=1` -> `ok github.com/Xsxdot/handoff/internal/ledgerstep 0.085s`.
- Real card-step wiring verification: `go test ./internal/agentd -run TestCardStepDeliversResolvedDiscipline -count=1` -> `ok github.com/Xsxdot/handoff/internal/agentd 0.134s`; the fake transport response carried `base_commit` and the dispatched ledger payload retained it.
- Clone isolation verification: `go test ./internal/agentd -run TestCloneDoesNotWaitForRepoFetchLock -count=1` -> `ok github.com/Xsxdot/handoff/internal/agentd 0.101s`.

# 2026-08-28 B251 exact output path verification
- A combined patch attempt for `node.go`, `dispatch.go`, and the skill failed before changing files because the skill context line did not match: `apply_patch verification failed: Failed to find expected lines in /root/.handoff/worktrees/7f5c43cd/skills/handoff/SKILL.md`. The production/test patch was then split and applied with the actual context.
- Test red before implementation: `go test ./internal/ledgerstep -run 'TestBuildPromptIncludesOutputPathWithoutCardContext|TestNodeStep(DatePrefixedDeclaredOutputMarksNeedsHuman|ExactDeclaredOutputWinsOverDatePrefixedPath|UnrelatedDatePrefixedOutputRemainsOrdinaryMissing)' -count=1` failed on missing `不要加日期前缀` and missing `请改名为：docs/b249-breakdown.md`; the raw output also showed the exact path case passed through and the unrelated date file stayed on the ordinary missing branch.
- Implemented B251 without changing `containsPath`: same-directory `YYYY-MM-DD-` plus declared basename is detected only for an actionable human hint; exact path still takes the success path and unrelated date files remain ordinary missing output.
- Targeted green result: `go test ./internal/ledgerstep -run 'TestBuildPromptIncludesOutputPathWithoutCardContext|TestNodeStep(DatePrefixedDeclaredOutputMarksNeedsHuman|ExactDeclaredOutputWinsOverDatePrefixedPath|UnrelatedDatePrefixedOutputRemainsOrdinaryMissing)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/ledgerstep 0.319s`.

# 2026-08-28 B260 serialization seam red
- Added real CLI `card show`, ledger zero-value, and HTTP projection tests before changing production tags.
- `go test ./internal/ledger -run TestLedgerLinkAndRelationZeroValuesKeepSnakeKeys -count=1` failed with raw zero-value JSON `{"CardID":"","Target":"","TaskID":"","Purpose":"","CreatedAt":"0001-01-01T00:00:00Z"}` and `{"From":"","To":"","Type":"","CreatedAt":"0001-01-01T00:00:00Z"}`; both snake keys were missing.
- `go test ./cmd -run TestCardShowLedgerWireUsesSnakeCase -count=1` failed with raw task map `map[CardID:D1 CreatedAt:2026-08-28T04:19:38.601269222Z Purpose:implement Target:mac-02 TaskID:T-wire-260]`, missing `card_id`.
- `go test ./internal/agentd -run TestLedgerAPIDetailKeepsPascalCaseProjection -count=1` -> `ok github.com/Xsxdot/handoff/internal/agentd 0.170s` before ledger tags, proving the explicit HTTP proto projection already kept PascalCase.
- Added only the nine specified JSON tags to `TaskLink` and `Relation`; SQL and HTTP projection code were not changed.
- `go test ./internal/ledger -run 'Test(TaskLink|LedgerLinkAndRelationZeroValuesKeepSnakeKeys|Wire|AddRelationTypes)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/ledger 0.105s`.
- `go test ./cmd -run 'Test(CardAddListShowMove|CardShowLedgerWireUsesSnakeCase|CardDispatch|CardShow|Dispatch)' -count=1` -> `ok github.com/Xsxdot/handoff/cmd 2.523s`.
- `go test ./internal/agentd -run 'Test(LedgerAPI|LedgerAPIDetailKeepsPascalCaseProjection)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/agentd 0.384s`.
- B251 task-close test: `go test ./internal/ledgerstep -run 'Test(ViaTemplate|BuildPrompt|NodeStep|Runner)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/ledgerstep 5.615s`.
- B260 task-close ledger test: `go test ./internal/ledger -run 'Test(RecordDispatch|LinkTask|AddRelation|Wire|LedgerLinkAndRelationZeroValuesKeepSnakeKeys)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/ledger 0.303s`.
- B260 task-close CLI test: `go test ./cmd -run 'Test(CardDispatch|CardShow|Dispatch|CardAddListShowMove|CardShowLedgerWireUsesSnakeCase)' -count=1` -> `ok github.com/Xsxdot/handoff/cmd 2.637s`.
- Combined agentd seam test after all task changes: `go test ./internal/agentd -run 'Test(LedgerAPI|LedgerAPIDetailKeepsPascalCaseProjection|CardStepDeliversResolvedDiscipline|ResolveLocalBaseBranch|ResolveBaseBranch|ResolveBaseline|WriteDispatchErrorKeepsFetchLockContentionDistinct|CloneDoesNotWaitForRepoFetchLock)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/agentd 3.300s`.
- Mutation precheck: `rg -F -n 'actualBase[datePrefixLength:] != declaredBase' internal/ledgerstep/node.go | wc -l` -> `1`; the planned semantic mutation target was unique.
- Mutation behavior check: changed that unique comparison from `!=` to `==`; `go test ./internal/ledgerstep -run TestNodeStepDatePrefixedDeclaredOutputMarksNeedsHuman -count=1` compiled the package and failed because the expected `请改名为：docs/b249-breakdown.md` comment was absent. Restored the comparison immediately.
- Mutation compile gate: with the same unique comparison changed to `==`, `go build ./...` completed with no output and exit 0; the subsequent behavior assertion again failed with the ordinary missing-path branch. Restored the production comparison to `!=` before continuing.
- Updated `TestDispatchWireLocalBaseBranchEndToEnd` to assert the origin-unavailable real HTTP/client path leaves `refs/heads/work` unchanged; the test comment now describes fetch failure fallback rather than claiming no fetch.
- Real client/HTTP integration result: `go test ./internal/agentd -run TestDispatchWireLocalBaseBranchEndToEnd -count=1` -> `ok github.com/Xsxdot/handoff/internal/agentd 0.242s`.
- Post-B251/B260 full compile gate: `go build ./...` completed with no output and exit 0.
- Final integration attempt: `go test ./...` ran all packages but exited `FAIL`; the cmd package failure was `TestMaybeInstallServiceLinuxRootInstalls` at `cmd/init_test.go:495`.
- Reproduction: `go test ./cmd -run TestMaybeInstallServiceLinuxRootInstalls -count=1 -v` failed with raw environment error `托管失败：加载配置 /tmp/handoff.yaml: 写默认配置 /tmp/handoff.yaml: open /tmp/handoff.yaml: read-only file system`; this test is outside B278's changed files and was not modified.
- Final B257/B235 seam reruns: `go test ./internal/agentd -run 'Test(ResolveBaseline|ResolveDispatchBase|ResolveBaseBranch|DispatchWireLocalBaseBranch|DispatchAutoBranchStartsAtBaseCommit|DispatchRecordsBaseline|DispatchBaseBranchName|ResolveLocalBaseBranch|WriteDispatchErrorKeepsFetchLockContentionDistinct)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/agentd 2.538s`; the B235 integration baseline command -> `ok github.com/Xsxdot/handoff/internal/agentd 0.588s`; clone boundary command -> `ok github.com/Xsxdot/handoff/internal/agentd 1.720s`.
- Corrected the `resolveDispatchBase` comment to distinguish B235's locked origin same-name probe from the ordinary `ResolveBaseBranch` D2 path; no behavior change.
- Added structured warning logs for the newly expanded `Transport`, `LinkTask`, and `RecordDispatch` error exits in `ViaTemplate`, carrying card/target/task/base context; success remains covered by the existing completion log.
- After logging-only edits, `go test ./internal/ledgerstep -run 'Test(ViaTemplate|BuildPrompt|NodeStep|Runner)' -count=1` -> `ok github.com/Xsxdot/handoff/internal/ledgerstep 6.504s`.
- Commit preflight: `git diff --check` completed with no output and exit 0; final `go build ./...` completed with no output and exit 0.
- Staging check: after `git add -A`, `git diff --cached --check` completed with no output and exit 0.
- Commit created: `git commit -m "fix: harden dispatch baseline and ledger wire"` -> `[cards/B278-charter-2 d0e25e5d] fix: harden dispatch baseline and ledger wire`, `23 files changed, 1163 insertions(+), 170 deletions(-)`; post-commit `git status --short --branch` -> `## cards/B278-charter-2`, `git log -1 --oneline` -> `d0e25e5d fix: harden dispatch baseline and ledger wire`.
- Commit ledger inclusion amend: `git commit --amend --no-edit` -> `[cards/B278-charter-2 8af3bd3f] fix: harden dispatch baseline and ledger wire`, `23 files changed, 1164 insertions(+), 170 deletions(-)`.
