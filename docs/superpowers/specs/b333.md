# B333 真机：本 grok 坐下走完 L1 并收房间消息

- **卡**：B333
- **级别与档位**：**L1**。plan 增量为零：补一条协调者 kind 房间发言走 grok 宿主出示的 CLI 测试。验收一眼可核：`go test` 该用例绿。
- **状态**：**已批准**（2026-09-04；本卡是 19964630 二进制升级后的真机狗粮，用户要求这场 grok 作为协调者走完 charter L1，房间说话能收到能回复）
- **基线**：`origin/main` @ `19964630`
- **发现自**：B329/B330 合入后升级本机与 linux-01 二进制的真机验证。不 reopen B329 / B330。
- **姐妹卡**：B332（机器人 coordinate；禁止改 `cmd/card_driver_test.go`）

## 问题陈述

B329 已有 `TestRoomSendCoordinatorAcceptsExplicitSeatFlags`（手填 `--cli/--session`）。缺「bind 席位来自 `GROK_SESSION_ID`、随后 `--kind reply` 不带 flag」的 CLI 用例。本卡补上这条缝，并由这场 grok 坐下把 L1 快道真机走完。

## 方案

在 `cmd/room_test.go` 增加 `TestRoomSendCoordinatorKindUsesGrokHostSession`：

1. `clearSeatSourceEnv`，设 `GROK_SESSION_ID=grok-room`
2. 无 flag 的 `card bind`，席位 `cli:grok#grok-room`
3. 无 `--cli/--session` 的 `room send --kind reply`
4. 该条 `room_message` 的 actor 必须是 `cli:grok#grok-room`，kind=`reply`，body 逐字一致

禁止改产品代码。禁止改 `cmd/card_driver_test.go`（B332 的文件）。

## 用户故事

1. 作为这场 grok：`card bind` 后走完 L1。implement 派 linux-01/codex，用来验证刚换上的 linux-01 二进制。
2. 作为用户：在本卡房间说话，本会话经 `card wait` 收到并用 `--kind reply` 回复。

## 实现决定

改 `cmd/room_test.go` 一处。不改 `cmd/*.go` 非测试文件。

## 测试决定

缝：`room send --kind reply` CLI × `currentSeatIdentity` 的 grok 宿主分支。

1. `go test ./cmd/ -run TestRoomSendCoordinatorKindUsesGrokHostSession -count=1` 绿。
2. 变异：若 `--kind reply` 仍只认 `HANDOFF_SESSION_*`，本测试必须红。

## L1 plan（同一文件，过 implement 门）

1. 在 `cmd/room_test.go` 增加 `TestRoomSendCoordinatorKindUsesGrokHostSession`，按上面四步断言。
2. `go test ./cmd/ -run TestRoomSendCoordinatorKindUsesGrokHostSession -count=1`。
3. 不改 `cmd/card_driver_test.go`，不改产品代码。

验收：上述测试绿。本节点该干什么一律以注入的纪律块为准。房间用户发言必须回复。

## Out of Scope

- **永不做**：`--kind user` 走席位出示；看板上填一对。
- **本期不做**：Pi/Kimi/Codex Desktop 宿主探测。
- **后续要做**：无。本卡是真机狗粮。
