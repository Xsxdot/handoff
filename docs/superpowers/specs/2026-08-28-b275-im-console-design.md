# B275：B156.2-156.3 前端 + B265 对话框整合——IM 会话面与看板形态重做

- **卡**：B275（B156.2-156.3 前端原型与对话框整合，含 B265）
- **级别与档位**：**L2**（用户裁决，2026-08-28：跳过 contract/breakdown 直接进 plan——两条 wire 增量窄到可在 plan/implement 内冻结，不值两个节点。入口复核原判 L3 轻档：动跨子系统 wire 契约，见「契约语义与接缝」；裁决留痕于此， increments 的精确签名由 plan 写死、review 对照 spec 验收）
- **状态**：用户已批准（2026-08-27 原型走查逐轮定稿，2026-08-28 授权按 charter 自主推进到底并合并回 acc/b156.2-156.3；同日裁决直接进 plan 列）
- **形态权威**：`prototypes/b275-frontend-proto/`（随本卡分支强制入库，file:// 直开零依赖）。**实现与验收一律对照原型代码本身；本 spec 与后续 plan 只写决策与接缝，不用文字转述 UI——任何样式/结构歧义以原型代码为准。**

## 问题陈述

B156.2（协作层：房间制会话）与 B156.3（自动化层：规则引擎 + agentd 拉起协调会话）的后端已完成并汇入 `feature/b156.2-156.3`（已 ff 合入本卡基线 acc/b156.2-156.3）。但前端形态缺位：

1. B156.2.8 交付过第一版房间面（`web/src/app/rooms/{RoomsListPage,RoomDetailPage,InboxPage}.tsx` + Shell 左栏 dock 入口，`web/src/app/shell/Shell.tsx:459-460`），当时**没有原型先行**，形态未经走查；
2. B265（对话框想法）定了新形态：IM 悬浮化、看板五列、删更新弹窗等；
3. 原型 `b275-frontend-proto` 已把两者合并走查定稿（变体 B 磨砂玻璃方案），本卡把它落进真实前端。

## 方案

以原型代码为唯一形态权威，在真实前端（`web/`，React + TS）复刻。B156.2.8 的三页房间面被新形态取代：列表/房间/收件箱三个整页路由废弃，会话面收敛为**单组件三态面板**（列表→房间→详情），两处挂载（工作项页右侧常驻 / 其余页面右下角悬浮框），同一套样式。

弃选：

- **保留 B156.2.8 整页路由继续演进**：否。三态面板与整页是两种信息架构，并行维护两份会话面必漂移；契约面（五端点）不变，只是消费方换形态。
- **待回复独立收件箱页**：否（用户裁决）。待回复是会话列表的筛选开关，不是页面。
- **微信绿气泡 / 深色科技舱 / 蓝紫玻璃拟态 / 细线评论风**：原型阶段均已走查否决，最终选定变体 B（浅灰底 + 磨砂玻璃气泡），决策记录见原型 README。

## 用户故事

1. 作为协调者，我在任何页面都能从右下角悬浮按钮打开会话面板（与 home 悬浮按钮并排），不被更新弹窗遮挡——更新 toast 已删除。
2. 作为协调者，我在工作项（看板）页右侧有常驻会话面板，可收起为悬浮按钮；两种打开方式样式完全一致。
3. 作为协调者，我在会话列表看到微信行式结构（头像/房名/预览/时间/未读红徽章），待回复房间置顶琥珀底、预览带 `[待回复]` 前缀。
4. 作为协调者，我能在列表 header 下用「全部项目 ∨」下拉筛项目、用「⚑ 需要你 N」开关只看待回复（N = 待回复房间数，选中琥珀高亮，两项样式统一为纯文字项而非按钮）；全员房无项目归属、不受项目筛选影响。
5. 作为协调者，我点进房间看到 header（返回 + 房名 + ⋯），聊天区为变体 B 磨砂气泡（对方磨砂白、自己磨砂黑、圆头像、胶囊输入框），发言直达房间。
6. 作为协调者，我点 ⋯ 进房间详情：协调者卡（B 号圆形头像 + 载体信息 + attach 按钮）与卡片区（参照卡片抽屉：优先级/状态/spec chips）。
7. 作为协调者，我点协调者头像或 attach 按钮，弹确认框；确认后在**该房间对应目录**打开终端 tab 并填入 attach 命令。
8. 作为协调者，我看板列为代办/沟通中/进行中/审核中/结束（列映射在工作流卡片上可配，B265）。
9. 作为协调者，我在卡抽屉有「拉起协调者」动作区（B156.3），卡片抽屉从中间区域最右侧打开、不挡会话面板。
10. 作为协调者，房间已读即清未读（打开即已读，沿用契约 §7 语义）。

## 契约语义与接缝

本卡**不动** B156.2 已定五端点语义（`web/src/api/rooms.ts:66-106` 镜像 `internal/proto/rooms.go`）。契约增量三条，均只加投影、不改既有字段语义：

1. **未读数投影**：`Service.Unread` 已存在（`internal/collab/service.go:344`）但 wire 面无投影——`RoomSummary`（`internal/proto/rooms.go:50-61`）无 unread 字段。增量：会话列表行携带未读数（字段形状归 plan 写死），组装在既有 ListRooms 装配点，双侧金样本（`rooms_fixture_test.go` / `rooms.test.ts`）同步锁。
2. **协调者可 attach 投影**：详情页的「attach」需要一个可 attach 目标（task）与打开终端的工作目录。现状 `RoomSummary.bound_session` 是 driver_session 投影（`internal/collab/service.go:271`），它能否解析出可 attach 的 task 是**代码事实**，plan 第一刀查明；查不出可解析路径，则作为第二条投影增量（房间详情携带协调者 task 与工作目录），同样只加不改。
3. **列表预览投影**：`listRooms` 已持有全量 `room_message` 事件扫描，顺手把每个房间最后一条消息投影到 `RoomSummary.preview`，不增加任何额外读取。形状为 `{body, seq, created_at}`；`body` 在服务端按 Unicode rune 截断至 120 个字符并以省略号标记，`seq` 与 `created_at` 原样携带。前端列表直接消费该字段，删除逐房间 `limit=1` 历史请求；打开房间仍只发一次既有 `limit=200` 历史请求。

边界声明：房间只读态（`read_only`）、Live 租约、消息 kind 白名单等既有语义全部沿用，不在本卡重议。

## 实现决定

形态实现**全部以原型代码为准**，以下为决策级条目（用户可见的名字与行为）：

1. **会话面板单组件三态**：列表 / 房间 / 详情三态在一个面板组件内切换；工作项页右侧常驻（可收起为悬浮按钮）与其余页面右下角悬浮框是同一组件的两种挂载。左栏 dock 的会话/收件箱入口删除（Shell.tsx:459-460 与对应 dock 项），`/rooms`、`/rooms/:id`、`/inbox` 整页路由与三页面文件退役。
2. **UI 方案 = 原型变体 B**：浅灰底 + 磨砂玻璃气泡等全部样式细节照抄原型 `pages/board.html` 与 `index.html` 的 IM 段；会话列表结构参照微信（行式、分隔线从头像右侧起、未读红徽章压头像角 #fa5151），待回复置顶琥珀底。
3. **筛选行**：「▦ 全部项目 ∨」项目下拉 +「⚑ 需要你 N」开关，纯文字项样式（非按钮），N 由待回复房间数动态算出；待回复集由收件箱聚合（`fetchInbox` 的 card_id 集）导出，不落独立页面。
4. **房间详情**：协调者卡（B 号圆形头像、载体、在线态）+ attach 按钮，点击均弹确认框；确认后在该房间对应目录打开终端 tab 并填入 attach 命令。终端 init 命令通道已存在（`internal/proto/pty.go:64` `InitCommand`），TS 镜像 `CreatePtySessionReq`（`web/src/api/types.ts:733`）缺 `init_command` 字段，补上（既有承诺的镜像补全，非新契约）。协调者会话不可 attach 时按钮置灰并说明，不许静默无反应。
5. **看板列映射**（B265）：看板列从「工作流状态平铺 + 终止」（`web/src/app/cards/columns.ts:6`）改为可配置映射，配置面在工作流卡片（flows 页）新增「看板列映射」表；默认映射即原型五列（代办/沟通中/进行中/审核中/结束）。映射的持久化形状若涉及 wire 增量，plan 一并写死。
6. **卡抽屉**（B156.3）：新增「拉起协调者」动作区（调既有 `runCardStep` 面，`web/src/api/ledger.ts:252`）；抽屉改为相对中间区域右侧打开、不挡会话面板（原型已在 `pages/board.html` 验证定位方案：抽屉 absolute 于 `.content` 内）。
7. **更新 toast 删除**（B265）：`web/src/app/update/UpdateToasts.tsx` 及其挂载点（Shell.tsx:60、:627）删除；更新提示的常驻落点即设置页（既有），不再在右下角弹 toast。
8. **原型 `pages/conversations.html` 是孤儿页**（无任何入口）：删除，不迁进真实前端。

## 测试决定（接缝清单）

最高可测缝 = **web api fetch 层**（`web/src/api/rooms.ts` 等，fetch mock 金样本既有先例 `rooms.fetch.test.ts`）与 **React 组件层**（vitest + testing-library，rooms 三页既有测试同形替换）。契约增量各带双侧金样本。

- 缝 1：`web/src/api/rooms.ts#fetchRooms` —— 调用方会话面板列表态；未读投影字段过金样本。
- 缝 2：会话面板组件三态切换与筛选行逻辑 —— 调用方 Shell 两处挂载；项目筛选、需要你计数、待回复置顶逐条钉。
- 缝 3：`web/src/api/client.ts#createPtySession` 的 `init_command` 透传 —— 调用方房间详情 attach 确认框。
- 缝 4：看板列映射纯逻辑（`columns.ts` 同位替换）—— 调用方看板页与 flows 配置表；映射→列序、默认五列、未知状态兜底钉死。
- 缝 5：契约增量（unread 投影 / 可 attach 投影）—— Go 金样本 `rooms_fixture_test.go` 与 TS 金样本 `rooms.test.ts` 双侧同步。

假缝核对：面板内部的纯呈现 helper 不占缝名额，归 plan 的内部锁候选。

## Out of Scope

- **永不做**：待回复独立收件箱页（用户裁决）；会话面回左栏 dock。
- **本期不做（已有归属）**：B156.2 integrate 四条跨卡残余（@提及消费面 / carrier wire 载体 / Live 恒假 / 租约三法），已落 `docs/roadmap.md`，本卡不揽。
- **本期不做**：后端消息通道、规则引擎、approver 链路的任何行为变更；本卡只加三条投影、不动既有语义。
- **本期不做**：会话面消息的 Markdown/代码块富渲染——原型是 plain text 气泡，照抄；富渲染另开卡。

## 备注

- 基线动作留痕：`acc/b156.2-156.3` 原基于 `feat/b156.3-web-write`，不含 B156.2/156.3 已完成代码；已于 2026-08-28 将 `feature/b156.2-156.3`（811 提交，含 B156.2 全部后端 + B156.2.8 房间面前端 + B156.3 契约线）fast-forward 合入，作为本卡真实基线。
- 原型随卡分支入库用 `git add -f`（`prototypes/.gitignore` 只放行 base/；fork 副本入库有 desktop-console/ 先例，属状态标注式破例）。
- 过程台账：`docs/superpowers/ledgers/2026-08-28-b275-spec-ledger.md`。
