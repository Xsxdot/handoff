# B156.3 contract 节点执行台账（cards/B156.3-charter-3）

> 按纪律块「边干边落台账」整理；本文件与契约产出物同批提交。
> spec 对话裁决在卡 timeline note ①–⑬（含前两轮中止记录），不在此重复。

| # | 事实 / 动作 | 证据（命令与原始输出要点） |
| --- | --- | --- |
| 1 | 工作分支基线刷新到 origin | `git fetch origin && git merge --no-edit origin/claude/kai-156-3-ba-df7357` → Fast-forward `c6a45857..7c5e6b5d`，spec +128/-25 |
| 2 | 第零步自检通过 | `grep -c '7.0 架构决定' …b156.3-automation-keystone-design.md` = 1；`git log --oneline -1` = 7c5e6b5d |
| 3 | spec 头部状态行已回写「已批准」，无需代写 | 读 spec 3–4 行 |
| 4 | B156.2 未落地确认（只有 spec 提交） | `git log --oneline -1 origin/cards/B156.2-charter` = afea153c docs(b156.2) spec 定稿；无实现提交 → 触发 §7.0「本期建同一个账本门面包，不建第二个」分支 |
| 5 | 现状读数复核全部命中 | node.go routeTo(104) 后 RunOnce 返回即止(271–279)；cardstep.go 包注释 10–12「不做恢复…刻意取舍」；rounds.go MaxRounds=3(14)；move.go ClaimCard(143)/ReleaseCard(181) |
| 6 | 卡优先级为自由文本默认「中」 | cards.go NewCard(20)、prepareCard 缺省(140–142) → 队列排序只认出厂词表 |
| 7 | graph check 基线通过（改动前） | `go run . graph check` exit=0, fails=[] |
| 8 | codegraph 容器 id 由 Go 包名生成、末段取前缀的实证 | ptyhost/engine→k_engine_*、hostproc→k_hostproc_*、sessdir→k_sessdir_*；web TS 多段拼接 k_web_api_client。→ 门面命名弃 `<x>/api` 子包，取唯一包名 ledgerapi/hostapi/ptyapi/schedclient/keysclient |
| 9 | check 执法语义查证（charter/graph v0.8.0 源码） | check.go：跨域 call 边 callee 的 Container.Label 必须命中 entries 否则计 LegacyHits；无契约方向=new-direction fail；声明方向无活跃边=dead-contract fail；entries 需 to 域内有同 Label 非 deleted 节点=dead-entry；implements 跨域需 Interfaces 登记 |
| 10 | diff 的 containersAdded 校验要求领域存在于 baseline.json | validate.go:198 ValidateDiff(g=baseline,d)；View.Domains 只来自基线（merge.go 注释）→ baseline.json 手工补 d_scheduling/d_keystone 两域（领域可人改是 spec §3.1 明文）|
| 11 | Ticket 0 骨架落码 | internal/{ledger/registry.go, ledgerapi, schedclient, scheduling, keystone, keysclient, hostapi, ptyapi} + NodeOverride.Squad 三侧镜像（ledger/proto/web.ts）+ agentd/server.go 组装点 SetupAutomation 及适配器 |
| 12 | 编译与 vet | `go build ./...` 干净；`go vet`（九个新触包）干净 |
| 13 | 竖切测试三轮迭代后通过 | 首败：registry 新建 CAS 把不存在当 -1 与 expect 0 冲突 → 改按版本 0 语义；二败：满员误报 ErrNoHealthy → resolve 区分 ErrNoSlot/ErrNoHealthy；三败：测试残留重复断言块 → 删。终局 `go test ./internal/keystone/ -run TestIgnitionVerticalSlice` ok |
| 14 | 全量回归 | `go build ./... && go test ./...` 无非 ok 行（agentd 包 134s 含既有用例） |
| 15 | best.json/baseline.json 建图 | best domains 12→14（顶层）、containers 232→250；validate issues=null |
| 16 | target.json 冻结 | 新方向 gateway→scheduling、gateway→keystone（budget 0）；三条既有方向 entries += 门面 Label；`graph check --view=cards-B156.3-charter-3` exit=0 fails=0（warns 仅 legacy 预算内/anchor-off-domain 等存量类）|
| 17 | 分支视图 diff 落盘 | diffs/cards-B156.3-charter-3.json：18 containersAdded、58 nodesAdded、3 nodesModified（NodeOverride 三镜像）、8 edgesAdded（组装点绑定边）|
| 18 | 出稿自检 | `graph resolve --doc …b156.3-contract.md --view=cards-B156.3-charter-3`：13 锚 0 坏（修掉一处 file_missing 后复跑）|
| 19 | 金样本评估 | 无哈希/密钥派生/编码格式类语义 → 无可执行冻结命中，行为语义由竖切测试锁定 |
| 20 | opencode 四跳探针未复跑 | 按「本次补充③」作上游实测事实引用（卡 timeline⑫），已在契约 §9 列为实现票实跑义务——此处如实标注「未验证」 |
| 21 | 本轮未碰 handoff 写路径、未起新 executor | 全程只读本地数据 + go 工具链 + graph 子命令 |

## 放弃的尝试

- 曾把出站接口设计成直接引用 ledgerapi.DTO 以省适配器 → 因接口方法集需逐字对齐且
  编制域会重新 import 账本类型而放弃，改字节化端口 + 组装点小适配器（拍板记录①）。
- 曾计划新增 EvIgnitionQueued 账本事件常量 → 按对侧常量查执法找不到真实消费者
  （看板排队形态走 registry 读面），撤销该常量避免死常量漂移。

## 门面归一轮（2026-08-26，contract 第二轮）

| # | 事实 / 动作 | 证据（命令与原始输出要点） |
| --- | --- | --- |
| 22 | 第零步通过：合并上游基线 | `git fetch origin && git merge --no-edit origin/claude/kai-156-3-ba-df7357` → Fast-forward `9a6b242f..e2302432`；`ls internal/ledger/api/api.go` 存在；`git log --oneline -1` = e2302432（merge：汇入 feature/b156-workbench） |
| 23 | 裸 check 改动前 16 违规（baseline 缺两侧符号） | `go run . graph check` → fails=16：dead-contract×5、dead-entry×10、dead-interface×1；baseline meta=92394f56/b231 全量扫描，无 d_collab 领域 |
| 24 | check 执法语义复读（charter/graph v0.8.0 check.go） | implements 边 [impl(d_ledger),iface(d_x)] 把 d_x→d_ledger 计为活方向并要求契约 interfaces 登记接口节点 Name；dead-entry 按「容器 Label 精确相等且该容器有非 deleted 节点属 to 域」判 |
| 25 | 两份账本门面并存确认 | internal/ledgerapi/ledgerapi.go（本期自建）与 internal/ledger/api/api.go（B156.2）各自有 Facade；keysclient.LedgerView 经类型别名漏内部类型 |
| 26 | 归一轮代码落地 | 删 internal/ledgerapi；新增 internal/ledger/api/{registry.go,keystone.go} 六方法；keysclient.LedgerView→proto DTO；agentd 组装点改绑 api.Facade（import 别名 ledgerapi）+ roomNarrator（RecordRoomMessage 指针行）；slice_test.go 同步 |
| 27 | 跨卡硬约束自证 | `git diff e2302432 --stat -- internal/ledger/api/api.go internal/proto/rooms_fixture_test.go web/src/api/testdata/RoomsFixture.json internal/proto/rooms.go` → 空输出（零改动） |
| 28 | 编译/vet/fmt | `go build ./...` exit 0；`go vet ./internal/... ./cmd/...` exit 0；`gofmt -l internal cmd` 无输出 exit 0 |
| 29 | 测试 | `go test ./... -count=1` → 47 包 ok；cmd 仅 TestRepoContractGate 红（图回灌前，属待修事实）、grok 仅 TestGrokToolTimingPaired 偶发（单独 `-run` 与整包复跑均 ok，包不触碰本轮文件）；竖切 `go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v` PASS 0.14s |
| 30 | 图回灌 | 按 charter Absorb 语义脚本併入两份视图 diff（3636→3664→3722 节点）+ 本轮增量（删 11 符号/3 空容器、增 8 节点/1 容器、implements×2、边换绑）+ meta 刷新 + best.json 容器归属同步；d_collab 领域定义取自 origin/feature/b156-workbench 的基线段 |
| 31 | 已併入视图按 absorb 语义删除 | `git rm codegraph/diffs/cards-B156.2-charter-2.json codegraph/diffs/cards-B156.3-charter-3.json`（B192/B205 视图不动） |
| 32 | target.json 契约面更新 | d_gateway→d_ledger entries 换「账本薄门面 Facade」；新增 d_scheduling⇒d_ledger / d_keystone⇒d_ledger（budget 0，interfaces [Registry]/[LedgerView]，entries 容器 Label），声明与首个消费边同提交 |
| 33 | 收官门禁全绿 | `go run . graph validate --repo .` issues=null；裸 `go run . graph check --repo .` fails=0（bestCoverage assigned=view=252）；`go test ./cmd/ -run TestRepoContractGate -count=1 -v` PASS |
| 34 | 本轮未碰 handoff 写路径、未起新 executor | 全程只读本地数据 + go 工具链 + graph 只读子命令（validate/check/views/sym） |

## 归一轮放弃的尝试

- 曾考虑给 api.Facade 直接满足 schedclient.Registry（免适配器）→ Record/哨兵错误是
  schedclient 的类型，提供方不得反向 import 使用方（架构法第八条），维持组装点小适配器。
- 曾考虑全量逐符号重扫 baseline → 3722 节点超出单轮容量，取「存量全量扫描 + 视图机械
  併入 + 定向增量对账」的等价路径并在契约 §13 如实声明。

## 叙事换绑轮（2026-08-26，contract 第三轮，cards/B156.3-charter-5）

| # | 事实 / 动作 | 证据（命令与原始输出要点） |
| --- | --- | --- |
| 35 | 第零步通过 | `git fetch origin && git merge --no-edit origin/claude/kai-156-3-ba-df7357` → Already up to date；`git log --oneline -1` = 2e2b2b37 |
| 36 | 缺陷三条款本机复核 | internal/proto/rooms.go:32-33「Send 一律拒收 pointer，本键只应由 Service.Pointer 置真」；internal/collab/service.go:56 Send 见 pointer 即 ErrKindNotAllowed（旧注释「执法在 Send」为假陈述）；service.go:82-86 Pointer 空壳返回 (0,nil) |
| 37 | check 执法语义复读（charter/graph v0.8.0 check.go+merge.go+cli.go） | 裸 check 与 TestRepoContractGate 均吃 Merge(baseline,nil) 纯基线视图；dead-contract 要求已声明方向在**被检视图**内有活跃 call/implements/组装点豁免边；组装点豁免=caller 节点 file ∈ target.assembly，豁免前已计入 liveDirections；域归属唯一链路 best.containers→parent 上溯 |
| 38 | 实证 dead-contract：先只加 target.json d_keystone→d_collab | `go run . graph check --repo .` → fails=1「dead-contract d_keystone→d_collab 没有活跃边」，证实声明必须与消费边同落基线 |
| 39 | 改法五条落地 | server.go：roomNarrator 改持 *collab.Service（rooms := collab.New(facade)），Say 只递 roomID+Body；删假注释；拍板记录⑦ 改写进契约 §13/§14 |
| 40 | 图定向增量 | baseline.json：新容器 k_agentd_roomNarrator、Say 换绑+行号 2337+摘要、新边 Say→n_collab_Service_Pointer、六节点行号 +1、meta→charter-5/2e2b2b37b125/narrator-rebind；best.json：k_agentd_roomNarrator→d_keystone（1 行 diff）；target.json：45 条契约 |
| 41 | best.json 首次写坏已回滚重做 | json.dump(indent=1) 重排整文件（781 行噪声）→ `git checkout` 恢复后改外科手术插入；中途一次插入产生悬空逗号（JSONDecodeError line 387）→ 手工修复为合法 1 行 diff |
| 42 | 五步门禁全绿（本轮实跑） | `go build ./...` exit 0；`go vet ./...` exit 0；`gofmt -l internal cmd` 空；`go test ./... -count=1` 48 包 ok 零失败（TestRepoContractGate PASS 0.04s）；裸 check fails=0 warns=97、validate issues=null（bestCoverage assigned=view=253） |
| 43 | 竖切与锚自检 | `go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1` ok 0.08s（竖切用 fakeNarrator 不触真实路）；`graph resolve --doc …b156.3-contract.md` 15 锚 0 坏 |
| 44 | 本轮欠账如实入档 | narrator→Pointer 路径当前零可观测行为（Pointer 空壳），无测试可锁——最小行为归 C7 子卡（调用形态已由双方协调者写进其 plan 约束），见契约 §14.4；本轮未引入新 Go 符号 → 合法无视图 diff |
| 45 | 本轮未碰 handoff 写路径、未起新 executor | 全程只读本地数据 + go 工具链 + graph 只读子命令（check/validate/resolve） |

## 叙事换绑轮放弃的尝试

- 曾考虑用 implements 边 [collab.Service, m_keysclient_Narrator] 建活方向 → 结构上永不
  成立（Pointer(roomID,msg) ≠ Say(cardID,text)），图里不许有源码不存在的意图，弃。
- 曾考虑把方向改声明成 d_orchestration→d_collab（迁就 Say 的旧容器归属）→ 违背跨卡
  商定的方向语义（keystone⇒collab），弃；改为按消费侧端口归属立 k_agentd_roomNarrator
  容器（拍板记录⑧），同文件其余适配器迁移留移交区。
