# B276 spec 台账

- 2026-08-28 用户「继续做下一批」。沿用 2026-08-27 分批：第二批 = B245/B211/B256/B259/B258/B261 方向 1。
- 工作树 `/Users/sycm/.handoff/worktrees/batch-silent-wrong`，分支 `fix/silent-wrong` @ `8ddb060ae`（当前 origin/main）。
- 建卡 B276，源卡搁置。B261 `needs` 已清。
- B245 三处调用点核对：`cmd/dispatch.go:193-199`、`cmd/card_dispatch.go:207-221`、`internal/agentd/cardstep.go:145-166`。共同出口 `internal/discipline/dispatch.go:26` `ErrUnsupportedTarget`。
- B211：`internal/agentd/server.go:568` INFO；`webui.Embedded()` stub/embed 双文件；`cmd/status.go#renderStatusWithLookup` 无人读标记。
- B256：`cmd/service.go:342-369` `isEphemeralBin` 认 `go-build*` 分量 + `/tmp`。linux-01 全量在 `/tmp/hbfin`，`filepath.Abs("service.go")` 被当临时文件跳过。卡上「macOS 路径导致 Linux 不识别」不成立——本 spec 按此纠正。
- B259：`cmd/graph.go:19` Short 前缀 `[deprecated：请改用 codegraph 二进制]`。
- B258：`internal/agentd/server.go:993-1013` 三种成因同一 404「工单不存在」。skill 三处「404 即跳过」：`skills/handoff/SKILL.md` 178 / 254 / 596。
- B261：`internal/ledgerstep/dispatch.go:165` 瞬间冻结；`SetAcceptance` 只写「更新验收判据」。在飞判定用 `LatestTaskStates.LastType`，空=未知=在飞。
- 图：未跑 codegraph context（见 spec 备注）。
- 2026-08-28 独立审查 `docs/superpowers/reviews/b276-spec-review.md`：Critical 2 / Important 5 / Minor 2。
- 用户裁决 B259 改为删除 `handoff graph`，不是改 Short。spec r1 整段重写 B259：删 `cmd/graph.go`、permgate 白名单、平台不变量例外；替代入口锁定 `go run github.com/Xsxdot/charter/graph/cmd/codegraph`（charter 仓 `graph/cmd/codegraph/main.go` 存在；go.mod 钉 v0.9.0）。
- 吸收 Important：B211 JSON 名 `web_embedded` + false 必须出现在线上、填充 `handleStatus`；B256 候选先 `!isEphemeralBin` 否则 Fatal；B258 skill 锁 `--target`；B261 查询收口 `SetAcceptance`、接缝真打 CLI、一归档一在飞仍警告。CLI `cmd/card.go:179-182` 确认真写 `SetAcceptance`、不经 HTTP。
- 协调者意见：审查 Issue 1/2 必须改（否则按旧正文会做错题，删了又不给替代会比 deprecated 文案更坏）。Issue 3–7 写进 spec。Issue 8 活模板改入口纳入 B259 方案，不留 OOS。Issue 9 给实现注记，不单独立项。L2 保持。
- 用户 2026-08-28 授权吸收审查后无人值守推进到合 main 并 push。spec 头部回写已批准 r1。
- 2026-08-28 本节点核对当前工作树：分支 `cards/B276-charter`，HEAD `358bda7d`，工作树初始无未提交改动；有效基线依照 spec 仍为 `fix/silent-wrong` 的 r1 内容。
- 2026-08-28 `codegraph/best.json` 现状域查询：B245 入口落 d_cli/d_policy/d_gateway，B211 状态落 d_gateway/d_protocol，B256 服务路径落 d_cli，B259 平台/权限落 d_policy/d_cli，B261 写判据落 d_ledger/d_cli；`context` 输出均 `truncated=false`，但各域存在 `fociTruncated`，不可把焦点配额外的空结果当无调用方。
- 2026-08-28 `codegraph sym`：`resolveBareDiscipline` = `func resolveBareDiscipline(ctx context.Context, cli *client.Client, rawFile string) (discipline.ResolvedDiscipline, error)`；`resolveCardDispatchDiscipline` = `func resolveCardDispatchDiscipline(ctx context.Context, st *ledger.Store, name, target string) (discipline.ResolvedDiscipline, error)`；`Server.resolveStepDiscipline` = `func (s *Server) resolveStepDiscipline(node ledger.NodeDef, reqTarget string) (discipline.ResolvedDiscipline, error)`。
- 2026-08-28 `codegraph sym renderStatusWithLookup` 退出 1，原文：`Error: 符号 "renderStatusWithLookup" 不在图中（图未覆盖或名字有误）；近似候选: []`；本节点计划须把该符号作为 grep 覆盖债。
- 2026-08-28 `codegraph sym` 还确认：`ResolveDispatch` = `func ResolveDispatch(lookup DisciplineLookup, ref DisciplineRef, platformEnabled bool, targetCap *bool) (ResolvedDiscipline, error)`；`Server.handleStatus` = `func (s *Server) handleStatus(w http.ResponseWriter, r *http.Request)`；`resolveServiceBinFrom` = `func resolveServiceBinFrom(exe string, candidates []string) (string, error)`；`isEphemeralBin` = `func isEphemeralBin(path string) bool`；`Store.SetAcceptance` = `func (s *Store) SetAcceptance(id, criteria, actor string) error`；`IsSelfCommand` = `func IsSelfCommand(s string) (hit bool, sub string)`。
- 2026-08-28 首次直接用默认 Go 模块缓存运行 codegraph 的原始失败：`go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.9.0.mod688382737.tmp: read-only file system`，随后 `open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.9.0.lock: read-only file system`；改用任务专属 `GOMODCACHE=/root/.handoff/tmp/45ee7d16/gomodcache` 后 codegraph context 成功运行。
- 2026-08-28 基线 `GOMODCACHE=/root/.handoff/tmp/45ee7d16/gomodcache GOCACHE=/root/.handoff/tmp/45ee7d16/gocache GOSUMDB=off go test ./cmd -run 'TestBareDispatch(CarriesAssembledDiscipline|RefusesUnsupportedTarget)$' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/cmd  0.030s`，退出 0。
- 2026-08-28 基线 `... go test ./internal/agentd -run 'Test(CardStepDeliversResolvedDiscipline|StartCardStepRejectsUnsupportedTarget)$' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd  0.752s`，退出 0。
- 2026-08-28 基线 `... go test ./cmd -run 'Test(IsEphemeralBin|ResolveServiceBin)' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/cmd  0.004s`，退出 0；当前工作树不在 `/tmp`，因此 Linux `/tmp` 仓复现未验证。
- 2026-08-28 基线 `... go test ./internal/agentd -run 'TestStatus(Reports|TaskCounts|ListenAux)|TestStatusAlwaysReportsUpdate' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd  0.630s`，退出 0；`... go test ./internal/proto -run 'TestContractFixtures' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/proto  0.010s`，退出 0。
- 2026-08-28 基线 `... go test ./internal/ledger -run 'Test(LiveMirrorTargets|LatestTaskStates|CardStepInFlight)' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/ledger  0.590s`，退出 0。
- 2026-08-28 基线 `... go test ./cmd -run 'TestCardUpdate' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/cmd  0.355s`，退出 0；`... go test ./internal/agentd -run 'TestPatchCard' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd  0.644s`，退出 0。
- 2026-08-28 基线 `... go test ./internal/permgate -run 'Test(IsSelfCommand|JudgeUnknownGraphSubcommand)' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/permgate  0.004s`，退出 0；`... go test ./cmd -run 'TestRepoContractGate' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/cmd  0.041s`，退出 0。
- 2026-08-28 基线 `GOMODCACHE=/root/.handoff/tmp/45ee7d16/gomodcache GOCACHE=/root/.handoff/tmp/45ee7d16/gocache GOSUMDB=off go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 原始输出：`containers: 239`、`edges: 4735`、`issues: null`、`edgeIssues: null`、`unscannedEntries: 6`，退出 0；替代查图入口当前可运行。
- 2026-08-28 基线观察 `GOMODCACHE=/root/.handoff/tmp/45ee7d16/gomodcache GOCACHE=/root/.handoff/tmp/45ee7d16/gocache GOSUMDB=off go run . graph --help` 退出 0；原始输出首行为 `[deprecated：请改用 codegraph 二进制] 查询仓库内的代码图...`，随后显示 `handoff graph` 用法，证明待删除别名当前仍存在。
- 2026-08-28 基线探测 `GOMODCACHE=/root/.handoff/tmp/45ee7d16/gomodcache GOCACHE=/root/.handoff/tmp/45ee7d16/gocache GOSUMDB=off go test -tags embedweb ./internal/agentd -run '^$' -count=1` 失败；原始输出：`FAIL github.com/Xsxdot/handoff/internal/agentd [setup failed]`、`internal/webui/embed.go:15:12: pattern all:dist: no matching files found`。因此带 embedweb 的真机构建在当前工作树未验证；默认构建与 `true` 状态 JSON/渲染分支在计划中分别锁定。
- 2026-08-28 计划产出 `docs/superpowers/plans/b276-plan.md` 已写入，覆盖六个 task 的有界文件集、精确接口、基线命令、红绿步骤、日志/注释、序列化边界、缺陷族与双向接缝矩阵；计划文件 `~~~` fence 计数 46 为偶数，`rg -n 'TBD|同 Task|加适当错误处理|TODO'` 无输出。
- 2026-08-28 提交 `ce19de46`（分支 `cards/B276-charter`）已创建计划与台账提交；未 push。随后为把提交事实写入台账将 amend 该提交。
- 2026-08-28 当前实现工作树核对：分支 `cards/B276-charter-2`，HEAD `9347f983`，工作树无未提交改动；本轮未切换分支。
- 2026-08-28 实现前局部基线：`go test ./cmd -run 'TestBareDispatch(CarriesAssembledDiscipline|RefusesUnsupportedTarget)$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/cmd 0.007s`，退出 0；`go test ./internal/agentd -run 'Test(CardStepDeliversResolvedDiscipline|StartCardStepRejectsUnsupportedTarget)$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.476s`，退出 0。
- 2026-08-28 实现前局部基线：`go test ./internal/agentd -run 'TestStatus(Reports|TaskCounts|ListenAux)|TestStatusAlwaysReportsUpdate' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.602s`，退出 0；`go test ./internal/proto -run 'TestContractFixtures' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/proto 0.002s`，退出 0；`go test ./cmd -run 'Test(IsEphemeralBin|ResolveServiceBin)' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/cmd 0.004s`，退出 0。
- 2026-08-28 实现前局部基线：`go test ./internal/ledger -run 'Test(LiveMirrorTargets|LatestTaskStates|CardStepInFlight)' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/ledger 0.605s`，退出 0；`go test ./cmd -run 'TestCardUpdate' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/cmd 0.360s`，退出 0；`go test ./internal/agentd -run 'TestPatchCard' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.741s`，退出 0。
- 2026-08-28 实现前局部基线：`go test ./internal/permgate -run 'Test(IsSelfCommand|JudgeUnknownGraphSubcommand)' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/permgate 0.002s`，退出 0；`go test ./cmd -run 'TestRepoContractGate' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/cmd 0.032s`，退出 0。
- 2026-08-28 B245 红测首跑：cmd 两条新增测试真实失败，裸派发与模板卡均输出“目标机 agentd 不支持接收下发的纪律正文（能力位缺席或不支持）：请先把目标机升级到同批版本再派发”，未含“探活失败”；internal/agentd 首版测试未执行断言，真实输出为 `PTY 测试根目录不可用: ... mkdir /tmp/handoff-pty-2934807797: read-only file system ...` 后 `--- SKIP`，已改用 `newNoPTYLedgerEnv` 排除夹具环境跳过。
- 2026-08-28 B245 绿测：将三处 Status/客户端错误改为结构化 Error 日志并直接返回“目标机探活失败：请确认目标机可达、agentd 正在运行且 token 一致”及 `%w` cause；能力位 nil/false 仍走原升级拒发。新增三条测试与两条存量升级测试输出：`go test ./cmd ...` 为 `ok github.com/Xsxdot/handoff/cmd 0.339s`，`go test ./internal/agentd ...` 为 `ok github.com/Xsxdot/handoff/internal/agentd 0.668s`，退出均 0。
- 2026-08-28 B245 task 收尾：`go build ./...` 原始输出无正文、退出 0；触及测试 `go test ./cmd ./internal/agentd ...` 输出 `ok github.com/Xsxdot/handoff/cmd 0.355s`、`ok github.com/Xsxdot/handoff/internal/agentd 0.656s`，退出 0。
- 2026-08-28 B211 首红：新增真实 GET 测试首跑为编译失败，原始错误 `internal/agentd/status_test.go:164:9: got.WebEmbedded undefined (type *proto.StatusResp has no field or method WebEmbedded)`；cmd 三态 root status 测试真实失败，false 人读无 stub 且 true JSON 缺 `web_embedded`。已核实为字段/投影功能缺失，不是拼写错误。
- 2026-08-28 B211 空壳后断言红：`go test ./internal/agentd ...` 原始输出为 WARN 级别仍为 INFO、真实 GET `WebEmbedded == nil`；`go test ./cmd ...` 原始输出 false 人读无 stub、true JSON 缺键；`go test ./internal/proto ...` 原始 fixture 差异显示实际新增 `"web_embedded": false`。三者均为预期功能缺失断言红。
- 2026-08-28 B211 绿测：Handler 默认构建 false 记录 WARN（含 stub、`-tags embedweb`、`embedded=false`），真实 GET 投影非 nil false，CLI root status 对 false/true/nil 分别正确渲染/保留/省略，契约 fixture 用 `-update` 后复测通过；`go test ./internal/agentd ...` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.135s`，`go test ./cmd ...` 输出 `ok github.com/Xsxdot/handoff/cmd 0.277s`，`go test ./internal/proto ...` 输出 `ok github.com/Xsxdot/handoff/internal/proto 0.003s`，退出均 0。
- 2026-08-28 B211 task 编译收尾：`go build ./...` 原始输出无正文、退出 0。完整 `go test ./internal/agentd ./internal/proto ./cmd` 在本环境未完成/无退出输出；分别运行后 proto 为 `ok github.com/Xsxdot/handoff/internal/proto (cached)`、退出 0，agentd 输出大量日志后无完整退出结论，cmd 失败原文：`--- FAIL: TestMaybeInstallServiceLinuxRootInstalls`、`init_test.go:495: Linux root 下答 y 必须真的装，实得输出:`、`托管失败：加载配置 /tmp/handoff.yaml: 写默认配置 /tmp/handoff.yaml: open /tmp/handoff.yaml: read-only file system`，最终 `FAIL github.com/Xsxdot/handoff/cmd 10.270s`，退出 1；该失败未改动。
- 2026-08-28 B256 红测首轮：将回退候选改为 `t.TempDir` 内普通文件后，真实输出 `找不到不在临时目录且可写的服务夹具根；os.TempDir="/root/.handoff/tmp/3b39a348"`；探明本沙箱将 GOCACHE 放在 os.TempDir 下且 `/var/cache` 不可写，已保留候选先判定逻辑，改用运行时非临时 TMPDIR 重新验证。
- 2026-08-28 B256 绿测：回退测试改为自建候选，先断言 `regularFileExists` 与 `!isEphemeralBin`；新增 t.TempDir/go-build-b256 普通文件被跳过测试；service.go 未改。`go test ./cmd -run 'Test(IsEphemeralBin|ResolveServiceBin)' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/cmd 0.006s`，退出 0。
- 2026-08-28 B256 task 收尾：`go build ./...` 原始输出无正文、退出 0；触及测试输出 `ok github.com/Xsxdot/handoff/cmd 0.005s`，退出 0。
- 2026-08-28 B259 红测：新增根命令删除断言后，`TestRootRejectsDeletedGraphCommand` 实际得到 `err=<nil>` 且 stdout 展示 deprecated graph 命令树；permgate resolve 仍为 `(false, "")` / `Action:1, Reason:"黑名单未命中", Rule:""`。新增平台正文断言后，`TestComposeEnabledKeepsHeadBaseTailOrderAndSources` 真实失败：`平台正文不得提供 handoff graph 执行入口`。
- 2026-08-28 B259 绿测：删除 `cmd/graph.go`，移除 permgate graph 二级只读白名单，平台层改为 canonical `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . <子命令>`；定向 `cmd`、`internal/permgate`、`internal/discipline` 测试均输出 `ok`，`go build ./...` 无输出退出 0。canonical `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 输出 `containers: 239`、`domainDecls: 23`、`domains: 20`、`edgeIssues: null`、`edges: 4735`、`issues: null`、`nodes: 3636`、`unscannedEntries: 6`，退出 0。修正一次测试期望：删除整棵 graph 后未知子命令命中 top-level `graph`。
- 2026-08-28 B258 红测基线：按计划 Python 检查原始 skill 文本时 `Traceback ... AssertionError`，首个断言缺少 `handoff show <task> --target <机器>`。
- 2026-08-28 B258 绿测：三处 404 处置均改为任务实际机器上的 `handoff show <task> --target <机器>` + `pending_tickets`，并保留「同一个 ticket 回答两次，第二次 404。」与「工单已被消耗」；完整 Python 检查输出 `B258 skill 404 判据检查通过`，退出 0。
- 2026-08-28 B261 红测：新增真实 CLI `card update --accept` 测试，空/`completed`/混合在飞场景 stderr 均为空，且卡事件只有「更新验收判据」；`go test ./cmd -run '^TestCardUpdateAcceptanceReportsInFlightTasks$' -count=1` 原始失败含 `stderr 是否警告=false，want true`。新增 HTTP PATCH 测试改用 `newNoPTYLedgerEnv` 排除 PTY 环境后，`go test ./internal/agentd -run '^TestPatchCardAcceptanceReportsInFlightTasks$' -count=1` 原始失败含 `事件是否警告=false，want true`，现有 archived-only 子例通过。
- 2026-08-28 B261 绿测：`SetAcceptance` 写入后统一查询 `LatestTaskStates`，用既有 EvComment 记录在飞影响；CLI 读取本次新增评论并输出 stderr，HTTP 保持 200/`ok=true`。真实 CLI 测试 `go test ./cmd -run '^TestCardUpdateAcceptanceReportsInFlightTasks$' -count=1` 输出 `ok github.com/Xsxdot/handoff/cmd 0.888s`；真实 PATCH 测试 `go test ./internal/agentd -run '^TestPatchCardAcceptanceReportsInFlightTasks$' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.469s`；既有 taskstate 触及测试输出 `ok github.com/Xsxdot/handoff/internal/ledger 0.645s`，合并命令退出 0。
- 2026-08-28 变异自验：B245 唯一命中 `B245_MUTATION_MATCHES=1`，`go build ./...` 无输出退出 0，取反探活文案后单测原始断言失败 `错误必须说明探活失败`；同组定向测试最终 `FAIL github.com/Xsxdot/handoff/cmd 0.187s`。B211 唯一命中 `B211_MUTATION_MATCHES=1`，编译退出 0，反转 false 分支后真实状态测试同时报 false 未画 stub、true 错画 stub，退出 1。B261 唯一命中 `B261_MUTATION_MATCHES=1`，编译退出 0，`&&` 改 `||` 后真实 CLI 测试在 archived-only 与混合场景失败，原始断言含 `stderr 是否警告=true，want false` 与终态 task 不应出现在警告，退出 1；三次变异均已恢复。
- 2026-08-28 B276 集成定向收尾：cmd 输出 `ok github.com/Xsxdot/handoff/cmd 0.795s`，agentd 输出 `ok github.com/Xsxdot/handoff/internal/agentd 2.936s`，ledger `ok .../internal/ledger 0.646s`，discipline `ok .../internal/discipline 0.002s`，permgate `ok .../internal/permgate 0.004s`，proto `ok .../internal/proto 0.005s`；并行 `go build ./...` 无输出退出 0。B258 完整脚本输出 `B258 skill 三处 target-aware 判据通过` 退出 0；recipe 禁止入口扫描无输出、`RECIPE_SCAN_EXIT 1`。
- 2026-08-28 全量集成 `go test ./... -count=1` 实际退出 1：根包与除 cmd 外各包均输出 `ok`（agentd `135.672s`、ledger `17.685s` 等），cmd 原始结尾为 `FAIL github.com/Xsxdot/handoff/cmd`。当前工作树复跑唯一失败 `go test ./cmd -run '^TestMaybeInstallServiceLinuxRootInstalls$' -count=1`：`init_test.go:495: Linux root 下答 y 必须真的装`；原文含 `写默认配置 /tmp/handoff.yaml: open /tmp/handoff.yaml: read-only file system`，exit 1。该环境失败未改动测试。
- 2026-08-28 B256 夹具去除仓路径兜底：Linux 改用 `/dev/shm/handoff-b256-fixture` 作为 system-backed durable root（先尝试 `/var/cache`），macOS/Windows 使用对应系统稳定根；仍逐项断言普通文件且 `!isEphemeralBin`。复跑 `go test ./cmd -run 'Test(IsEphemeralBin|ResolveServiceBin)' -count=1` 输出 `ok github.com/Xsxdot/handoff/cmd 0.006s`，退出 0。
- 2026-08-28 最终六组定向复测：cmd `ok github.com/Xsxdot/handoff/cmd 1.097s`、agentd `ok github.com/Xsxdot/handoff/internal/agentd 2.168s`、ledger `ok github.com/Xsxdot/handoff/internal/ledger 0.667s`、discipline `ok github.com/Xsxdot/handoff/internal/discipline 0.009s`、permgate `ok github.com/Xsxdot/handoff/internal/permgate 0.002s`、proto `ok github.com/Xsxdot/handoff/internal/proto 0.007s`，均退出 0；并行 `go build ./...` 无输出退出 0。
- 2026-08-28 收尾静态审查：`git diff --check` 无输出；`rg -n 'handoff graph|go run \\. graph' docs/codegraph-scan-recipe.md` 无输出、退出 1（无旧入口）；`rg` 未找到 `graphcli`、`selfCmdNestedReadOnly` 或 graph 挂载残留。准备在当前分支提交，未 push。
- 2026-08-28 初次提交 `d21b09c0`（`fix: surface silent failures and stale diagnostics`）已在当前分支创建；为把提交事实写回台账，随后仅 amend 台账，不 push。
