# B305 台账

- 2026-09-02 linux-01 agy 1.1.24 strace print-mode：open `$HOME/.gemini/config/hooks.json`（ENOENT）、`antigravity-cli/hooks.json`、`$HOME/.gemini/hooks.json`；workspace `.agents/hooks.json` 从未出现。`hooks_manager loaded 0 named hooks from 0 hooks.json file(s)`。
- 同日：`HOME=/tmp/fake` + `$HOME/.gemini/config/hooks.json` → `loaded 1 named hooks`；`appDataDir=/tmp/fake/.gemini/antigravity-cli`；用户 `~/.gemini/config/hooks.json` 仍不存在。
- 同日：`--dangerously-skip-permissions` 日志 `auto-approving all tool permissions`；deny hook 开火但 `uname -s` 仍输出 `Linux`。
- 同日：`toolPermission: always-proceed` 同样 ignore deny，uname 跑出。
- 同日：`permissionOverrides` + `decision:allow` 仍 `Print mode: soft-denying tool confirmation RunCommand`。
- 同日：`permissions.allow: ["command(*)"]` 下 deny / exit 1 / 空 stdout 均拦不住 uname。
- 同日：`command(echo)` + deny → `tool call denied by pre-tool hook`；`command(git status)` + deny → 同样被拦。
- 同日：`command(uname)` + deny → 仍输出 `Linux`（原生例外，本卡 allow 清单不收录）。
- 同日：`command(go)` + deny → `tool call denied by pre-tool hook: go-deny`；`command(go)` + allow → `go env GOVERSION` 输出 `go1.26.1`。
- 同日：B304 隔离 task `aa208c7b` symlink 全局 hooks 后：工单 → approve `echo hello-b304-agy`（命中用户 settings.allow）completed；复合 echo+git 被原生 soft-deny。symlink 已拆。
- 2026-09-02 执行 `go test ./internal/executor/agy -count=1; go build ./...`：原始输出 `ok\tgithub.com/Xsxdot/handoff/internal/executor/agy\t0.587s`；build 无输出且命令退出 0。
- 2026-09-02 T-A/T-B/T-C/T-D 首红命令 `gofmt -w internal/executor/agy/taskenv_test.go internal/executor/agy/proc_test.go internal/executor/agy/adapter_test.go && go test ./internal/executor/agy -count=1 -run 'TestWriteTaskEnvAgyHome|TestWriteTaskEnvMissingOAuth|TestManagedTaskTmpEnvHome|TestAgyArgv'`：原始输出 `internal/executor/agy/adapter_test.go:68:47: undefined: agyHomeDirName`、`internal/executor/agy/taskenv_test.go:152:36: undefined: agyHomeDirName`、`FAIL ... [build failed]`；退出 1。确认首红为新接缝符号缺席。
- 2026-09-02 空壳常量后复跑同一局部测试：原始输出为 `TestManagedTaskTmpEnvHome` 缺 `HOME=/.../agyhome`、`TestWriteTaskEnvAgyHome` 读取任务 HOME hooks 报 `no such file or directory`、`TestWriteTaskEnvMissingOAuth` 报「应失败」；`FAIL ...`，退出 1。确认断言红分别锁定 env、任务物料和缺 token 错误。
- 2026-09-02 最小实现后局部命令 `gofmt -w internal/executor/agy/taskenv.go internal/executor/agy/adapter.go && go test ./internal/executor/agy -count=1 -run 'TestWriteTaskEnvAgyHome|TestWriteTaskEnvMissingOAuth|TestManagedTaskTmpEnvHome|TestAgyArgv'`：原始输出 `ok\tgithub.com/Xsxdot/handoff/internal/executor/agy\t0.017s`；退出 0。
- 2026-09-02 agy 包收尾命令 `go test ./internal/executor/agy -count=1`：原始输出 `ok\tgithub.com/Xsxdot/handoff/internal/executor/agy\t0.607s`；退出 0。
- 2026-09-02 加入双语权限模型文档、agyArgv 注释及可选 `.orig-google-oauth` 断言后，执行 `gofmt -w internal/executor/agy/taskenv.go internal/executor/agy/taskenv_test.go internal/executor/agy/proc.go internal/executor/agy/proc_test.go internal/executor/agy/adapter.go internal/executor/agy/adapter_test.go && go test ./internal/executor/agy -count=1 && go build ./...`：原始输出 `ok\tgithub.com/Xsxdot/handoff/internal/executor/agy\t0.467s`；build 无输出且命令退出 0。
- 2026-09-02 变异自验：唯一锚点检查命中 `internal/executor/agy/adapter.go:48` 1 处；将 `HOME` 语义改为 `wrong-agyhome` 后 `go build ./...` 原始输出为空、退出 0；单测 `go test ./internal/executor/agy -count=1 -run '^TestManagedTaskTmpEnvHome$'` 原始输出为断言失败，实得 HOME 为 `.../wrong-agyhome`，退出 1；随后完整 `go test ./internal/executor/agy -count=1` 原始输出含同一 `TestManagedTaskTmpEnvHome` 断言失败并 `FAIL ...`，退出 1。变异命中且测试拦截。
- 2026-09-02 恢复变异后执行 `gofmt -w internal/executor/agy/adapter.go && go test ./internal/executor/agy -count=1 && go build ./...`：原始输出 `ok\tgithub.com/Xsxdot/handoff/internal/executor/agy\t0.895s`；build 无输出且命令退出 0。
- 2026-09-02 强化 T-A 断言（禁止 `toolPermission`、检查四级任务 HOME 目录 0700）后执行 `gofmt -w internal/executor/agy/taskenv_test.go && go test ./internal/executor/agy -count=1`：原始输出 `ok\tgithub.com/Xsxdot/handoff/internal/executor/agy\t0.766s`；退出 0。
- 2026-09-02 集成命令 `go test ./... -count=1`：原始输出逐包均为 `ok`，包括 `github.com/Xsxdot/handoff/internal/agentd 140.843s`、`github.com/Xsxdot/handoff/internal/executor/agy 0.351s`、`github.com/Xsxdot/handoff/internal/prochost 21.338s`，直至 `github.com/Xsxdot/handoff/internal/upgrade 0.004s`、`github.com/Xsxdot/handoff/scripts/codegraph-flows 0.002s`；无 `FAIL`，退出 0。
- 2026-09-02 隔离 agy 真机 approve/deny 未在本执行节点运行；实现计划明确该 Task 2 由协调者执行，本节点不调用 handoff CLI，真机结果未验证。
- 2026-09-02 提交命令 `git add README.md README.zh-CN.md docs/superpowers/specs/b305-ledger.md internal/executor/agy/adapter.go internal/executor/agy/adapter_test.go internal/executor/agy/proc.go internal/executor/agy/proc_test.go internal/executor/agy/taskenv.go internal/executor/agy/taskenv_test.go && git commit -m "fix(B305): agy task HOME permission gate"`：原始输出 `[cards/B305-charter 0c0e9728] fix(B305): agy task HOME permission gate`、`9 files changed, 300 insertions(+), 6 deletions(-)`；退出 0。随后仅为纳入本行台账执行一次 amend。
- 2026-09-02 协调者隔离真机（agentd `127.0.0.1:17777` pid 1588031，二进制 `2a682fa7`，未碰生产 1370445/:7777）：
  - allow 任务 `85d2df67-91d8-4101-a5a2-1c796628f337`：工单 `go env GOVERSION && git rev-parse …` → `--approve` → out.jsonl step 2 DONE output 含 `go1.26.1`。用户 `~/.gemini/config/hooks.json` 不存在。任务 `agyhome/.gemini/config/hooks.json` 含 `handoff-safety-gate`。
  - deny 任务 `bd7671f5-77b6-4fef-a49a-8ca40769fa68`：同形工单 → `--deny --reason b305-deny` → step 2 ERROR `tool call denied by pre-tool hook: …原因：b305-deny`。无 go 版本产出。
- 2026-09-02 acceptance 复跑 linux-01 `/tmp/handoff-b305-src` @ `2a682fa7`：`go test ./internal/executor/agy -count=1` 原始输出 `ok … 0.542s` 退出 0；`go build ./...` 退出 0。本机 macOS 全包 `perm.sock` sun_path 超限红（预存，非本卡）。
- 2026-09-02 变异：`adapter.go` `HOME=+agyHomeDirName` 唯一处改为 `"wrong-agyhome"`；`go build ./internal/executor/agy` 退出 0；`TestManagedTaskTmpEnvHome` 断言失败（实得 wrong-agyhome）；还原后同测试 `ok`。
