# B233.23 spec+plan（L1）：图收口——编排容器归属修正 + 基线全量重扫，check 归零

## 背景（spec）

- 来源：`docs/roadmap.md`「来自 B233.17 验收」check 6 红条；用户残余清单第 2 类。
- 6 红实况（59dfb16c5 基线）：
  - 3 条 dead-entry：契约 d_gateway→d_orchestration 声明的入口「orchestration 实体 /
    orchestration.Manager / orchestration（包级函数）」在 d_orchestration 解析不到——
    基线还停在 Manager 迁包前的 `k_agentd_*` 容器命名（图里仍有 reclaim.go 等已迁走文件），
    重扫后会以 `k_orchestration_*` 等实际容器名出现；
  - 3 条 over-budget：d_gateway→d_orchestration 296/0、d_orchestration→d_workspace 9/0、
    d_workspace→d_orchestration 3/0。
- `codegraph/best.json`、`target.json`、`domains/*.json` 是人工维护的应然；**本卡是获授权
  修订它们的卡**（用户拍板「改 best.json 或做一次完整图重扫」，两个手段都用）。
- 扫描配方：`docs/codegraph-scan-recipe.md`（完整性自检、summary 转录、写面只有
  baseline.json）。

## 目标（spec）

`codegraph check` fails=0（3 dead-entry + 3 over-budget 全部消除），不造假基线、
不删真实节点硬凑。

## 验收（spec）

1. `codegraph check` 退出 0，fails=[]；
2. `codegraph validate` 问题数较基线（130 issues + 4 edgeIssues）不新增（归零归 B233.24）；
3. `go build ./...` 绿（扫描不改源码，编译应天然绿）；
4. best.json/target.json 每处修订在 diff 里带 note（容器归属为什么、预算棘轮到多少、
   逐条理由）；
5. 扫描交付说明列出 target 未覆盖目录（配方要求）。

## plan（L1 三行）

1. 按 `docs/codegraph-scan-recipe.md` 做基线全量重扫（只写 codegraph/baseline.json，含文件级
   与符号级完整性自检）；
2. 依重扫结果修 best.json 容器登记（k_agentd_* → 实际容器名归域；preview 等未登记容器补登记）
   并对三条 over-budget 逐条处置——能经既有缝消的小口径消掉，消不掉的把 target.json 预算
   棘轮到重扫实况并写 note；
3. 验收：`codegraph check` fails=0；validate 不新增；`go build ./...` 绿。
