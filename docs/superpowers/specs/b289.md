# B289：把 B274 合入 acc/b156.2-156.3

定级 L1。发现自隔离实例真机：发送 200 后列表不出现新消息。B274 已在 `acc/b156.2-b156.3` @ `cd797e00` 修好，用户 2026-08-27 确认「可以了」，但**故意不合 main、也没进本线**。

## 不整条合 sibling

`acc/b156.2-b156.3` 相对本线只多 4 个提交：

| 提交 | 处理 |
|---|---|
| `b1f5b1f5` K6 旧控制台 | **不合**。本线已用 B282 的编制/排队/协调者面板替换 |
| `2adce7be` C6–C8 旧房间页 | **不合**。本线已用 B275 `RoomPanel` 替换 `RoomDetailPage`/`RoomsListPage`/`InboxPage` |
| `cd797e00` B274 | **要**。History 升序截尾 + 唤醒带 spec + 失败不刷指针 + 失败推进 cursor |
| `58228fc0` 收尾 roadmap | 本线已有 B282 残余账本；B274 语义写进本 spec，不另合那份 roadmap |

## 不能 cherry-pick

本线 `Wake(ctx, card, evs)` 无 spec 参数；`RoomDetailPage.tsx` 已删除。必须把 B274 **语义**迁到现树：

1. `History`：无 before = 该房间**最新** limit 条（升序）；before>0 为排他上界。不得从最老一侧 `break`。
2. `Wake` 增加 `spec SessionSpec`；无绑定时带着它去 `launchRound`，禁止空 spec。
3. `launchRound` 只在**重建成功**后落「载体已更换」；失败路径零指针。
4. 唤醒失败也推进 automation cursor。
5. `wakeCoordinatorRound` 从载体解析 spec 再 Wake（与 `launchCoordinatorRound` 同形）。
6. 前端：本线 `RoomPanel` 已轮询无 before 的最新页，不恢复 `RoomDetailPage`。

## 验收

- `TestHistoryReturnsNewestWindow`：5 条取最新 2 为 m4/m5；before 上界得 m2/m3。
- `TestWakeWithoutSessionConsumesSpec`：无绑定 Wake 把 spec 原样交给 Launch。
- `TestAutomationWakeFailureAdvancesCursor`：Launch 失败后 cursor 前进，第二轮不再 Launch。
- 触及包测试绿。图：Wake 签名变更，合 main 时随 absorb（同 B274 残余）。

## Plan（即本页）

- 改 `internal/collab/service.go` History + 补 `TestHistoryReturnsNewestWindow`。
- 改 `internal/keystone/keystone.go` Wake/launchRound；补 spec 测试；改 `slice_test.go` 调用。
- 改 `internal/agentd/scheddrain.go` / `wakeconsumer.go`；补 cursor 测试。
- 不改 RoomPanel 形态；不合并 K6/C8 旧页。
