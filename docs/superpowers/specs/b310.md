# B310 agy run_command 停在 ACTIVE：auto-allow 重放不再回传 hook

状态：**已批准**（用户 2026-09-03 报障：命令放行后子进程未起、卡到 stall）
定级 **L1**（单子系统、不动契约；plan 即本页末三行）
卡：B310
日期：2026-09-03
基线：`origin/main` @ `db4d6e99`
台账：`docs/superpowers/specs/b310-ledger.md`

## 问题陈述

谁：`--executor agy` 跑分。cwd/`--add-dir` 已对。

`go test …` 命中静态白名单 `permission_auto_allow` 后，`out.jsonl` 停在 `run_command` `ACTIVE`，无 DONE。`handoff run` 同命令 19s 结束。ps 无 `go test`。agentd `pending: 0`。

linux-01 任务 `38c9c82d-8f29-40b8-81dd-d3d73c3a73ab`（agentd `db4d6e99+dirty1`）：

- `permission-hook` **还在**（`ep_poll` 等 `perm.sock`），不是「钩子没起来」
- HOME 与 worktree `.agents/hooks.json` **各有一份** `handoff-safety-gate`。B309 `--add-dir` 之后两份都会开火
- 10:56:40.220 第一次 `step_2` → auto-allow → `自动放行已回传 executor`
- 10:56:40.248 第二次 `step_2`（无「发生重连」，因为第一次 Respond 已从 pending 删掉）→ `isPermissionReplay` 见审计事件 → **不再 Respond**
- 第一份 hook 拿到 allow 退出；第二份一直等到 timeout 86400s；agy 等齐 PreToolUse 才 exec

## 方案

推荐：**auto-allow 的重放仍向当前 pending 连接 `RespondPermission(once)`**，审计事件仍只落一次。SSE 重放时 pending 已空，Respond 失败只打日志（原注释已承认）。

弃选：只删双写 hooks（B304 测 workspace 文件；且 hook 重试仍会撞同一 replay）；缩短 hook timeout（治标，仍丢命令）。

## 用户故事

1. `go test` 白名单放行后命令真正执行，`out.jsonl` 有 DONE/产出，不是永久 ACTIVE。
2. 同一 `permission_id` 的 auto-allow 审计仍只有一条。

## 测试决定

缝：`Manager.handlePermission`（agy `onPermissionAsk` → 审批链）。同一 `PermissionID` 第二次进来：`RespondPermission` 再调一次 `once`，`permission_auto_allow` 事件仍 1 条。改现有 `TestSafeCommandPermissionAuditsOnceWithoutTicket` 的「重放响应数 = 1」（那正是本 hang）。

## 实现决定（即 plan）

1. `handlePermission`：replay 且已有 auto-allow 审计时，再 `RespondPermission(once)`，不补审计、不建工单。
2. 上述测试改为重放后响应两次 `once`、审计仍 1。
3. 不部署除非另说。三道挂死的跑分任务请先 `stop`（stall 2h）。

## Out of Scope

- 永不做：为放行而取消 PreToolUse；argv skip-permissions。
- 本期不做：去掉 HOME/workspace 双写 hooks（B304 锁 workspace 文件；本卡靠重放回传兜住双开火）。
- 不 reopen B309。

## 备注

过程读数：`docs/superpowers/specs/b310-ledger.md`。
