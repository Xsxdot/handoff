# B343 设置页登记载体 CLI 下拉补 agy

- **卡**：B343
- **级别**：**L1**。plan 增量为零：下拉加一项。验收一眼可核：弹窗能选 agy。
- **状态**：**已批准**（2026-09-05；用户要 linux-01 用 agy，自主推进）
- **基线**：B342 之后
- **发现自**：编制改小队。不 reopen B279。

## 问题陈述

设置页「登记载体」弹窗 CLI 下拉是 `CLI_OPTIONS = ['opencode', 'claude', 'codex', 'grok']`，没有 agy。linux-01 要用 agy，只能走 API。

现状：`web/src/app/settings/SchedulingPage.tsx` 的 `CLI_OPTIONS`。

## 方案

下拉补 `agy`。顺序跟执行者名单一致：opencode / claude / grok / codex / agy（或保持现序只在末尾加 agy——保持现序末尾加，少动）。

弃选：自由输入框（会让拼写错误进编制）。

## 用户故事

1. 点「登记载体」，CLI 下拉里能选 agy 并保存。

## 测试决定

缝：`SchedulingPage` 登记弹窗 CLI combobox。点「登记载体」后存在 option `agy`。

## L1 plan

1. `CLI_OPTIONS` 加 `agy`。
2. 测试断言 option agy 可见。

验收：测试绿；变异删 agy 则红。

## Out of Scope

- **永不做**：把 CLI 改成任意字符串无校验。
- **本期不做**：删除载体 API；b334-probe 残留清理。
