# B271 spec 台账

- 2026-08-28 计划文档 docs/superpowers/plans/b271-plan.md 已写入 Task 1/2/3：共享自机判定与门铃、本机账本源及两镜像分流、节点/CLI/WorkBranch 本机归一；包含精确接口、基线/红绿命令、结构化日志与注释要求、序列化边界、缺陷族及接缝双向审计。
- 2026-08-28 计划静态检查 git diff --check 实跑退出码 0，原始输出为空。
- 2026-08-28 计划最终修改后的 git diff --check 实跑退出码 0，原始输出为空。
- 2026-08-28 计划残留与无占位扫描 rg -n startLedgerMirror/hostName/cfg.LedgerPath/TBD/TODO/同 Task/适当的错误处理 docs/superpowers/plans/b271-plan.md 实跑退出码 1，原始输出为空。
- 2026-08-28 计划围栏复核再次得到 32 个行首波浪线围栏与 4 个行首反引号围栏，退出码 0，数量均为偶数。
- 2026-08-28 补充 Store 门铃完整实现代码块后，git diff --check 仍退出码 0；残留/占位扫描退出码 1、原始输出为空；计划围栏计数为 36 个波浪线围栏和 4 个反引号围栏，均为偶数。
- 2026-08-28 最终修订 setupLedger 为真实 ledger 生命周期、修正基线正则与本机/远端 client 作用域后，git diff --check 退出码 0；残留/占位扫描退出码 1、原始输出为空；围栏复核输出 36 与 4，均为偶数。
- 2026-08-28 收口前工作树检查实跑退出码 0：git status --short 仅显示本台账修改与目标计划未跟踪文件；git diff --check 原始输出为空；git diff --stat 仅统计台账的 20 行新增，计划仍需 git add 后纳入统计。
- 2026-08-28 已执行 git add docs/superpowers/plans/b271-plan.md docs/superpowers/specs/b271-ledger.md，退出码 0；随后 git diff --cached --check 退出码 0，暂存统计为计划 930 行、台账 21 行，共 951 行新增。
- 2026-08-28 首次提交命令 git commit -m docs: add B271 implementation plan 实跑退出码 0，原始输出为 [cards/B271-charter 21b968ca]、2 files changed、952 insertions、计划文件新建；随后因补记本行将 amend。
- 2026-08-28 计划文件存在性检查 test -s docs/superpowers/plans/b271-plan.md 实跑退出码 0，原始输出为空。
- 2026-08-28 计划结构检查 rg -n '^## [1-5]\\. |^### (3\\.|4\\.|5\\.)|接缝 → 测试|测试 → 接缝' docs/superpowers/plans/b271-plan.md 实跑退出码 0；输出列出第 1 至 5 章、Task 2/3 全部子章及双向接缝表。
- 2026-08-28 无占位词扫描 rg -n 'TBD|TODO|同 Task|适当的错误处理' docs/superpowers/plans/b271-plan.md 实跑退出码 1，原始输出为空。
- 2026-08-28 计划代码围栏检查分别得到 32 个行首波浪线围栏与 4 个行首反引号围栏，均为偶数，原始输出为 32 与 4。

- 2026-08-28 用户「那就继续下一批」。原分组第四批 = B271 单卡（loopback 镜像 EOF），第五批 B234/B193 不动。
- 工作树 `/Users/sycm/.handoff/worktrees/b271-loopback`，分支 `fix/b271-local-dispatch`（从 `main` @ `d319f92d2`）。
- 卡 B271 待办→spec。源 note：B268 本机 grok；空 target 被拒；登记 `local=http://127.0.0.1:7777`；`machine=local` EOF；卡流停在 dispatched；权限门与 EOF 同一毫秒；自动批是 `approver_decision`；`resume --force` 不发 completed。
- `handoff template show charter-default`：`target` 空串，`discipline=charter-must-override`，`purpose=charter`。
- 本机 `config.yaml` `targets.local.addr=http://127.0.0.1:7777`。linux-01 在飞 B281 implement（`af294af9`，`cards/B281-charter-2`），本卡不碰。
- 代码：`ViaTemplate` 空 target 拒发（`internal/ledgerstep/dispatch.go:137-139`）。`targetClient("")` 拒（`cmd/card_dispatch.go:156-158`）。`resolveStepDiscipline` 空则跳过探活（`cardstep.go:136-141`）。
- 任务镜像：`machine=` 日志、`mirrorDiscoveryTick=30s`、`onEvent` → `hub.Publish`（`internal/agentd/mirror.go`）。账本镜像：`target=` 日志、空 target 因 `!registered[link.Target]` 被跳过（`ledgermirror/mirror.go:236-238`）。
- WS 掐线：`writeLiveBatch` 对 `seq <= lastWrittenSeq` 且大于重放面断开（`server.go:2090-2098`）。自订二次 Publish 会打中这条。`ReadTimeout=30s` 注释写明 Hijack 后对 WS 不生效，排除为 EOF 主因。
- `waitForTurnEnd` 走 `cl.WaitEvent`（`runner.go:417-419`），不是卡流。
- `LinkTask` 允许空 target（`cmd/status_test.go` 已用）。`WorkBranch` 跨机是字符串不等。
- 图：`codegraph context d_ledger` 有 ledger/ledgermirror/ledgerstep。`sym ViaTemplate` 命中。`who-calls waitForTurnEnd` 回到 `awaitNode` ← `Run`。任务镜像不在 d_ledger 包列表。
- 无前端页面形态，不走原型。
- 定级 L2：空 Target 字段语义收口，不新开 HTTP，不改状态机。
- 弃选闭区间 from_seq、魔法别名「本机」、钩子兼写账本、为自订删乱序断开、force 冒充 completed。
- 2026-08-28 独立审查 `01a046d9` 写入 `docs/superpowers/reviews/b271-spec-review.md`。总判修订后再批。Critical 2（WorkBranch 空串短路 / B192；本机源订 Hub 污染 Watchers）+ Important 5。
- 协调者吸收 r1：废止 B192 §2.1.3，去掉空串短路，历史缺字段视作本机、LocalBaseBranch 作后门；本机源不订 Hub（门铃+store），Watchers 语义不动；接缝覆盖 stepTransport/awaitNode/diffNode；Addr 去 scheme；For 之前分流。不抬 L3。不选 Hub 内部订。
- 用户 2026-08-28「老样子」授权批准 r1 并无人值守推进到合 main。
- 2026-08-28 执行节点按用户指定执行 `git fetch origin fix/b271-local-dispatch` 与 `git merge --ff-only origin/fix/b271-local-dispatch`；快进成功，HEAD 为 `ef61ffea`，父提交为 `d319f92d`，无冲突。
- 2026-08-28 已读取 `docs/superpowers/specs/b271.md`、本台账与 `docs/superpowers/reviews/b271-spec-review.md`；r1 的本机空 target、self classifier、store 门铃本机源、WorkBranch 历史缺字段口径与 6 条接缝作为计划依据。
- 2026-08-28 已用 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_transport_channel`、`context d_gateway`、`context d_orchestration`、`sym ViaTemplate`、`who-calls waitForTurnEnd`、`chain n_ledgerstep_Dispatcher_ViaTemplate --with-source`、`chain n_agentd_Mirror_discoverOnce --with-source`、`chain n_ledgermirror_Mirror_reconcile --with-source`、`chain n_ledgerstep_StepRunner_awaitNode --with-source` 查图；其中未截断的 `ViaTemplate` 链与 `reconcile` 链确认了当前拒绝空 target、WorkBranch/For 顺序及 await/diff 调用面；任务镜像领域存在图覆盖债，未以 grep 结果冒充图覆盖。
- 2026-08-28 基线命令 `go test ./internal/agentd -count=1` 实跑通过，原始结果为 `ok github.com/Xsxdot/handoff/internal/agentd 150.339s`，退出码 0。
- 2026-08-28 基线命令 `go test ./cmd -count=1` 实跑失败；命令输出末尾原文为 `FAIL`、`FAIL\tgithub.com/Xsxdot/handoff/cmd\t10.687s`、`FAIL`，退出码 1；失败原因未验证，计划不据此归因。
- 2026-08-28 基线命令 `go test ./internal/config ./internal/store ./internal/ledgerstep ./internal/ledgermirror -count=1` 实跑通过，原始结果逐包为 `ok github.com/Xsxdot/handoff/internal/config 0.024s`、`ok github.com/Xsxdot/handoff/internal/store 5.209s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 7.803s`、`ok github.com/Xsxdot/handoff/internal/ledgermirror 3.327s`，退出码 0。
- 2026-08-28 针对本卡 CLI 接缝的基线命令 `go test ./cmd -run 'Test(TargetEndpoint|BareDispatch|CardDispatch)' -count=1` 实跑通过，原始结果为 `ok github.com/Xsxdot/handoff/cmd 2.194s`，退出码 0；全 `./cmd` 仍保留上一行未归因失败记录。
- 2026-08-28 再查 `codegraph context d_ledger`：命令退出码 0，但输出预算截断（1391 行），并报告 `fociTruncated total=57 shown=5 reason=focus-quota`、`truncated=false`；图接口列表仍显示旧的 `ledgermirror.New` 形状，故 ledgermirror 的 `Machines`/`Options` 签名以已读源码为准，记录为图覆盖/新鲜度债，不据图旧签名改实现。
- 2026-08-28 Task 1 基线 `go test ./internal/config ./internal/store -count=1` 退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/config 0.054s`、`ok github.com/Xsxdot/handoff/internal/store 2.845s`。
- 2026-08-28 Task 1 基线 `go test ./cmd -run 'TestTargetEndpointLocalAuth|TestTargetEndpointLocalRewrite' -count=1` 退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.009s`。
- 2026-08-28 Task 1 红灯 `go test ./internal/config -run TestIsSelfTarget -count=1` 退出码 1；原始输出为 `internal/config/listenclass_test.go:55:14: undefined: IsSelfTarget`、`internal/config/listenclass_test.go:70:13: undefined: LocalDialAddr`、`FAIL`、`build failed`。
- 2026-08-28 Task 1 红灯 `go test ./internal/store -run TestEventDoorbell -count=1` 退出码 1；原始输出为 `internal/store/eventdoorbell_test.go:18:27: st.SubscribeEventDoorbell undefined`、`internal/store/eventdoorbell_test.go:20:19: st.SubscribeEventDoorbell undefined`、`internal/store/eventdoorbell_test.go:22:28: st.SubscribeEventDoorbell undefined`、`FAIL`、`build failed`。
- 2026-08-28 为验证断言有效性落入最小空壳后，`go test ./internal/config -run TestIsSelfTarget -count=1` 退出码 1；原始输出包含 6 个正向子用例的 `= false, want true`，末尾为 `FAIL`。
- 2026-08-28 为验证断言有效性落入最小空壳后，`go test ./internal/store -run TestEventDoorbell -count=1` 退出码 1；原始输出为 `task-b 应在一秒内收到自己的门铃`、`FAIL`。
- 2026-08-28 Task 1 实现后首次触及包检查：`go test ./internal/config ./internal/store -count=1` 退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/config 0.013s`、`ok github.com/Xsxdot/handoff/internal/store 4.018s`。
- 2026-08-28 Task 1 修正 cmd 未使用 import/变量后，`gofmt -w cmd/root.go` 退出码 0；`go test ./internal/config ./internal/store -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/config 0.010s`、`ok github.com/Xsxdot/handoff/internal/store 3.752s`；`go test ./cmd -run 'TestTargetEndpointLocalAuth|TestTargetEndpointLocalRewrite' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.011s`。
- 2026-08-28 Task 1 config 变异先确认唯一命中 `if loopbackOK && targetHP == loopbackHP`（rg 仅输出 `97:`），改为 `!=` 后 `go build ./...` 退出码 0；先跑行为断言 `go test ./internal/config -run TestIsSelfTarget -count=1` 退出码 1，原始输出为 `other_direct_host` 与 `other_port` 两个断言失败，末尾 `FAIL`；随后已恢复。
- 2026-08-28 Task 1 store 变异先确认唯一命中 `s.signalEventDoorbell(evt.TaskID)`（rg 仅输出 `773:`），改为错误 task 后 `go build ./...` 退出码 0；先跑行为断言 `go test ./internal/store -run TestEventDoorbell -count=1` 退出码 1，原始输出为 `task-b 应在一秒内收到自己的门铃`、`FAIL`；随后已恢复。
- 2026-08-28 Task 1 收尾：恢复变异后 `go build ./...` 退出码 0；`go test ./internal/config ./internal/store -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/config 0.015s`、`ok github.com/Xsxdot/handoff/internal/store 3.707s`；`go test ./cmd -run 'TestTargetEndpointLocalAuth|TestTargetEndpointLocalRewrite' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.006s`；`git diff --check` 退出码 0、原始输出为空。
- 2026-08-28 Task 2 基线 `go test ./internal/ledgermirror -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgermirror 2.841s`。
- 2026-08-28 Task 2 基线 `go test ./internal/agentd -run 'TestMirror' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.214s`。
- 2026-08-28 Task 2 基线 `go test ./cmd -run TestSetupLedgerMountsWithRetiredEnabledFlag -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.082s`。
- 2026-08-28 Task 2 本机账本镜像接缝红灯：`gofmt -w internal/agentd/mirror_test.go` 退出码 0；`go test ./internal/ledgermirror -run TestMirrorLocalTargetUsesLocalSource -count=1` 退出码 1，原始输出为 `unknown field LocalSource in struct literal of type Options`、`undefined: NewLocalSource`、`FAIL`；`go test ./internal/agentd -run TestMirrorDiscoverOnceSkipsSelfTarget -count=1` 退出码 1，原始输出为 `too many arguments in call to NewMirror`（新测试 5 参数、现实现 4 参数）、`FAIL`。
- 2026-08-28 Task 2 接缝测试实现后，`gofmt -w ...` 退出码 0；初次账本镜像编译错误原文为 `internal/ledgermirror/mirror.go:462:35: not enough arguments in call to m.st.MirrorWatermark`、`have (string)`、`want (string, string)`、`FAIL`，修正为当前 ledger Store 两参数签名后；`go test ./internal/ledgermirror -run TestMirrorLocalTargetUsesLocalSource -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgermirror 0.195s`；`go test ./internal/agentd -run TestMirrorDiscoverOnceSkipsSelfTarget -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.062s`。
- 2026-08-28 Task 2 包级验收 `go test ./internal/store ./internal/ledgermirror ./internal/agentd -count=1` 退出码 0；最终原始输出为 `ok github.com/Xsxdot/handoff/internal/store 6.238s`、`ok github.com/Xsxdot/handoff/internal/ledgermirror 3.713s`、`ok github.com/Xsxdot/handoff/internal/agentd 156.600s`。期间第一次同命令调用因前台 30s 分段只取得部分输出，未以其不完整状态作结论。
- 2026-08-28 Task 2 命名回归 `go test ./internal/agentd -run 'TestMirror|TestEventDoorbell' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.406s`；cmd 接线 `go test ./cmd -run TestSetupLedgerMountsWithRetiredEnabledFlag -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.154s`。
- 2026-08-28 Task 2 任务镜像变异先确认 `if m.isSelfTarget(name) {` 唯一命中为 `mirror.go:117`，改为 `!` 后 `go build ./...` 退出码 0；首次行为测试因 PTY harness 跳过（原始输出 `--- SKIP`，原因包含 `/tmp ... read-only file system` 与 repo socket 超过上限），不计为绿；改用不依赖 PTY 的真实 HTTP/WS harness 后，`go test ./internal/agentd -run TestMirrorDiscoverOnceSkipsSelfTarget -count=1 -v` 退出码 1，原始输出为 `本机 target ListTasks 请求数 = 1, want 0`、`FAIL`；随后已恢复变异。
- 2026-08-28 Task 3 基线 `go test ./internal/ledgerstep -run 'Test(ViaTemplate|Continuation|StepRunner)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgerstep 3.660s`；`go test ./internal/agentd -run 'Test(CardStep|Step)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 3.964s`；`go test ./cmd -run 'Test(TargetEndpoint|BareDispatch|CardDispatch|NamedTarget)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 3.557s`。
- 2026-08-28 Task 2 无 PTY 夹具接入后的收尾复核：`go build ./...` 退出码 0、原始输出为空；`go test ./internal/store ./internal/ledgermirror ./internal/agentd -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/store 4.446s`、`ok github.com/Xsxdot/handoff/internal/ledgermirror 3.321s`、`ok github.com/Xsxdot/handoff/internal/agentd 148.995s`；`go test ./internal/agentd -run 'TestMirror|TestEventDoorbell' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 1.611s`；`go test ./cmd -run TestSetupLedgerMountsWithRetiredEnabledFlag -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.582s`。
- 2026-08-28 Task 3 首红补测：新增 `TestViaTemplateEmptyTargetIsLocal` 后先修正测试自身的 `PutTemplate` 返回值错误；随后 `go test ./internal/ledgerstep -run TestViaTemplateEmptyTargetIsLocal -count=1` 编译通过但断言红，原始输出为 `空 target 本机派发: 目标机未定：--target 或模板 target 至少一个`、`FAIL`。该红直接命中待移除的空 target 拒绝分支。
- 2026-08-28 Task 3 节点本机接缝首红：新增真实 HTTP `TestLocalStepDisciplineProbesStatus` 与 `TestLocalStepTransportUsesLocalClient` 后，改签名测试先以 `assignment mismatch: 3 variables but h.srv.resolveStepDiscipline returns 2 values` 编译红；补齐签名与最小本机路由后两测退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.298s`。
- 2026-08-28 Task 3 CLI 本机接缝首红：新增 `TestTargetClientEmptyAndConfiguredSelf` 后 `go test ./cmd -run TestTargetClientEmptyAndConfiguredSelf -count=1` 退出码 1，原始输出为 `target_client_test.go:39: targetClient(""): 未指定目标机（--target 或模板 target 至少一个）`、`FAIL`；该红直接命中待移除的 CLI 空 target 拒绝分支。
- 2026-08-28 Task 3 runner 真实协议测试：新增 `TestRunnerLocalClientUsesWaitAndDiffWire`，使用真实 httptest WS/Attach/Done/Diff 与 StepRunner.Run；`go test ./internal/ledgerstep -run TestRunnerLocalClientUsesWaitAndDiffWire -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgerstep 0.127s`。
- 2026-08-28 Task 3 agentd 本机实现后复核：`go test ./internal/agentd -run 'TestLocalStep(DisciplineProbesStatus|TransportUsesLocalClient)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.298s`。
- 2026-08-28 Task 3 CLI 本机实现后复核：`go test ./cmd -run TestTargetClientEmptyAndConfiguredSelf -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.006s`。
- 2026-08-28 Task 3 runner 日志补齐并格式化后收尾复核：`go build ./...` 退出码 0、原始输出为空；`go test ./internal/ledgerstep -run 'TestViaTemplate|TestStepRunner|TestLocal' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgerstep 2.592s`；`go test ./internal/agentd -run 'Test(CardStep|Mirror|LocalStep|WSOutOfOrderPublishNotDropped)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 3.974s`；`go test ./cmd -run 'Test(TargetEndpoint|BareDispatch|CardDispatch|TargetClient|NamedTarget)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 2.680s`。
- 2026-08-28 Task 3 dispatch 变异先确认 `if hasWorkBranch && previousTarget != target {` 唯一命中为 `dispatch.go:174`，改为 `==` 后 `go build ./...` 退出码 0；行为断言 `go test ./internal/ledgerstep -run TestViaTemplateSelfAliasContinuesLocalWorkBranch -count=1` 退出码 1，原始输出为 `本机空 target 第二轮派发: 工作分支只存在于创建它的那台机器：上次目标机 ""，本次目标机 ""；请先 push 到 origin（git push origin cards/D1-implement），再用显式 --base 指定`，末尾 `FAIL`；随后已恢复变异。
- 2026-08-28 Task 3 agentd 路由变异先确认 `if s.IsSelfTarget(name) {` 唯一命中为 `server.go:324`，改为 `!` 后 `go build ./...` 退出码 0；行为断言 `go test ./internal/agentd -run TestLocalStepTransportUsesLocalClient -count=1` 退出码 1，原始输出为 `stepTransport("local") wire target = "local"，期望空字符串`、`FAIL`；随后已恢复变异。
- 2026-08-28 Task 3 cardstep 客户端路由变异先确认 `cl, err := s.clientForTarget(target)` 唯一命中为 `cardstep.go:139`，改为 `s.pool.For(target)` 后 `go build ./...` 退出码 0；行为断言 `go test ./internal/agentd -run TestLocalStepDisciplineProbesStatus -count=1` 退出码 1，原始输出为 `本机纪律探活: 目标机探活失败：请确认目标机可达、agentd 正在运行且 token 一致：target  未在配置中登记`、`FAIL`；随后已恢复变异。
- 2026-08-28 Task 3 CLI 变异初测：唯一命中 `if canonical == "" {`（`card_dispatch.go:193`）改为 `!=` 后 `go build ./...` 退出码 0；原有行为测试仍退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.012s`。该结果未判定测试存活，先确认变异改变了本机/远端分支但原测试只覆盖同一 HTTP 端点，随后补充远端不同端点断言。
- 2026-08-28 Task 3 CLI 变异补强：在真实 HTTP 测试中加入不同端点的 `devbox` 远端后，变异 `canonical != ""` 下行为断言 `go test ./cmd -run TestTargetClientEmptyAndConfiguredSelf -count=1` 退出码 1；原始输出为 `CLI 采用远端客户端 target="" canonical_target=""`、`CLI 采用本机客户端 target=devbox canonical_target=devbox`、`本机/本机别名 Status 命中 3 次，期望 2`、`FAIL`；随后已恢复为 `canonical == ""`。
- 2026-08-28 Task 3 变异恢复后最终局部回归：`go build ./...` 退出码 0、原始输出为空；`go test ./internal/ledgerstep -run 'TestViaTemplate|TestStepRunner|TestLocal' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgerstep 3.020s`；`go test ./internal/agentd -run 'Test(CardStep|Mirror|LocalStep|WSOutOfOrderPublishNotDropped)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 4.100s`；`go test ./cmd -run 'Test(TargetEndpoint|BareDispatch|CardDispatch|TargetClient|NamedTarget)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 2.809s`。
- 2026-08-28 收尾集成 `go test ./... -count=1` 退出码 1；原始失败为 `TestStartCardStepAssemblesRunHolder`：`目标机探活失败：请确认目标机可达、agentd 正在运行且 token 一致：状态查询请求: agentd 地址不含主机名（原始地址 "http://"）：relay 形态的机器没有 addr，应经 targetclient 选路而不是直连构造`；`TestLegacyStepFallbackActorIsHostOnly` 同一空 Listen 夹具返回同一错误；`TestNoDirectClientNewInAgentd` 原始断言为 `server.go:346 直连构造了 agentd 客户端：return client.New(config.LocalDialAddr(cfg.Listen), cfg.Token), nil`。同一命令其余包原始输出为 `ok`，但整体 `FAIL`；原因尚未据此归因，进入兼容性修正。
- 2026-08-28 为兼容全量 agentd 夹具，`newNoPTYLedgerEnv` 回填真实 httptest Listen、挂接最小 Manager 并写入 implement/review 测试纪律；agentd 本机 client 构造收口到 `targetclient.NewLocal`，保留 Pool.For 远端路径。`go test ./internal/agentd -run 'Test(StartCardStepAssemblesRunHolder|LegacyStepFallbackActorIsHostOnly|NoDirectClientNewInAgentd)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.262s`。
- 2026-08-28 单独复核 `go test ./cmd -count=1` 退出码 1；唯一失败为 `TestMaybeInstallServiceLinuxRootInstalls`，原始错误为 `托管失败：加载配置 /tmp/handoff.yaml: 写默认配置 /tmp/handoff.yaml: open /tmp/handoff.yaml: read-only file system`，随后输出 `FAIL`、`FAIL github.com/Xsxdot/handoff/cmd 11.894s`、`FAIL`。该失败与此前基线全 cmd 失败相同，环境性归因未额外验证；其余 Task 3 命名 cmd 子集仍以局部回归结果为准。
- 2026-08-28 最终非 cmd 全包回归 `go test ./internal/config ./internal/store ./internal/ledgermirror ./internal/ledgerstep ./internal/agentd -count=1` 退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/config 0.018s`、`ok github.com/Xsxdot/handoff/internal/store 19.183s`、`ok github.com/Xsxdot/handoff/internal/ledgermirror 8.816s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 22.408s`、`ok github.com/Xsxdot/handoff/internal/agentd 167.539s`。
- 2026-08-28 恢复 `cmd/target_client_test.go` 原有 token、relay 与无端点回归，并保留新增本机/远端真实 HTTP 测试；`go test ./cmd -run 'Test(CheckTokenEntropyRejectsWeak|EndpointsPreserveRelayTransportForUpgrade|NamedTargetNoEndpointReportsClearly|TargetClientEmptyAndConfiguredSelf)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.008s`。
- 2026-08-28 最终收尾命令：`go build ./...` 退出码 0、原始输出为空；`go test ./internal/ledgerstep -run 'TestViaTemplate|TestStepRunner|TestLocal' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgerstep 3.582s`；`go test ./internal/agentd -run 'Test(CardStep|Mirror|LocalStep|WSOutOfOrderPublishNotDropped)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 4.781s`；`go test ./cmd -run 'Test(TargetEndpoint|BareDispatch|CardDispatch|TargetClient|NamedTarget)' -count=1` 退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 3.277s`；`git diff --check` 退出码 0、原始输出为空。
