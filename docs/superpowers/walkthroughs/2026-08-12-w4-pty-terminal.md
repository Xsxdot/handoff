# W4 PTY 终端真机走查记录

> **状态：骨架（待审核者执行）。** 本文件由执行机器上的 agentd 完成实现后建出，
> 但走查需要真实浏览器 + 真实终端交互，当前执行环境不具备，故每一行均标
> 「未验（待审核者在本地带浏览器的环境执行）」。**没有编造任何证据。**
>
> 依据：[W4 PTY 终端设计 spec §9 走查](2026-08-12-w4-pty-terminal-design.md) 与
> 实现计划 Task 17。

## 走查步骤

| # | 走查项（spec §9） | 判据 | 结果 |
|---|---|---|---|
| 1 | 本机工作树开终端 | `stty size` 与窗口一致；中文输入正常（输入「你好」再退格，字符边界不错乱） | 未验（待审核者在本地带浏览器的环境执行） |
| 2 | devbox 开终端 | 能交互；**且确认走反代**——本机 agentd 日志有 `/ws/pty` 转发行，浏览器只连了本机 | 未验（待审核者在本地带浏览器的环境执行） |
| 3 | 终端里 `ssh -T git@github.com` 或 `git push --dry-run` | 成功。这是 §4.2 的验收 | 未验（待审核者在本地带浏览器的环境执行） |
| 4 | 长任务 + 关页面 + 换窗口尺寸重开 | 会话自动恢复、输出连续；`truncated` 时清屏不重复 | 未验（待审核者在本地带浏览器的环境执行） |
| 5 | 两个浏览器窗口接同一会话 | 输出双方都见；任一方可输入；尺寸取两者最小 | 未验（待审核者在本地带浏览器的环境执行） |
| 6 | 点 `×` 关闭 | 确认弹层出现 → 确认后 `handoff footprint` 里该会话消失、进程归零 | 未验（待审核者在本地带浏览器的环境执行） |
| 7 | `pathenv` 补全在终端里生效 | 终端里 `echo $PATH` 含 B71 补出来的目录（对照 agentd 自身的 PATH） | 未验（待审核者在本地带浏览器的环境执行） |
| 8 | **托管形态**的 `env_forward` | 见下方「托管形态验证」小节 | 未验（待审核者在本地带浏览器的环境执行） |
| 9 | shell 自己 `exit` | 终端显示退出码；`GET /api/pty/sessions` 里该会话 `exit_code` 出现 | 未验（待审核者在本地带浏览器的环境执行） |
| 10 | 切换左栏基准目录 | 会话**不死**（切回来输出连续），且没有 DELETE 请求 | 未验（待审核者在本地带浏览器的环境执行） |
| 11 | `config.yaml` 未配 `env_forward` 时触发一次 Save | 文件里**仍然没有** `env_forward` 键，而 `SSH_AUTH_SOCK` 转发照常工作 | 未验（待审核者在本地带浏览器的环境执行） |
| 12 | agentd 日志的三态结论 | 每个 `env_forward` 变量各有一行 inherited / resolved / unavailable | 未验（待审核者在本地带浏览器的环境执行） |
| 13 | **home 基准**：右下悬浮按钮开终端（spec §11 第 2 条） | 终端起在 `$HOME`（`pwd` 是 home）；tab 标题是 `bash · home`；关它同样先弹确认 | 未验（待审核者在本地带浏览器的环境执行） |
| 14 | 恢复出来的 tab 在 home 组里也认得出来 | 开一个 home 终端 → 刷新页面 → 它回来了，且没有多出第二个（`dedupKey` 生效） | 未验（待审核者在本地带浏览器的环境执行） |

## 托管形态的 `env_forward` 验证

**理想做法**：在 `handoff service install` 托管的 agentd 上开终端，`ssh -T` 成功。

**拿不到停机窗口时的退路**（B71 V2 同款约束：launchd label 固定、无法与生产实例
并存）：用隔离最小环境复现：

```bash
env -i PATH=/usr/bin:/bin:/usr/sbin:/sbin HOME="$HOME" /tmp/handoff-pty agentd --listen 127.0.0.1:7788 --data-dir /tmp/pty-walkthrough
```

然后向 7788 开一个终端，在里面 `echo $SSH_AUTH_SOCK` 与 `ssh -T git@github.com`。

> **这条如果没能在真托管形态下验，就在这一格写「托管形态未验 + 原因」，不打勾。**
> spec §9 明确要求如实记，不要用最小环境的绿灯冒充托管形态的绿灯。

结果：未验（待审核者在本地带浏览器的环境执行）

## 对照 spec §11 的 12 条验收标准

| # | 验收标准 | 结论 |
|---|---|---|
| 1 | 本机工作树里开出的终端可交互，`stty size` 与窗口一致，中文输入正常 | 未验（待审核者在本地带浏览器的环境执行） |
| 2 | home 基准（悬浮按钮）开出的终端同样可用 | 未验（待审核者在本地带浏览器的环境执行） |
| 3 | 远程开发机（devbox）的终端可用，且确认走 WS 反代而非浏览器直连 | 未验（待审核者在本地带浏览器的环境执行） |
| 4 | 终端里 `git push` / `ssh` 可用；agentd 日志里能看到每个 `env_forward` 变量的三态结论 | 未验（待审核者在本地带浏览器的环境执行） |
| 5 | 跑长任务时关闭页面，重开后会话自动恢复、输出连续；`truncated` 场景下先清屏不重复 | 未验（待审核者在本地带浏览器的环境执行） |
| 6 | 两个客户端同时接入：输出双方都看到、任一方可输入、尺寸取最小 | 未验（待审核者在本地带浏览器的环境执行） |
| 7 | 切换左栏基准目录**不杀**会话；点 `×` **才杀**，且杀后 `handoff footprint` 中该会话进程归零 | 未验（待审核者在本地带浏览器的环境执行） |
| 8 | shell 自己 `exit` 后，终端显示退出码，会话在列表里标为已退出 | 未验（待审核者在本地带浏览器的环境执行） |
| 9 | Windows agentd（或 `pty_supported=false` 的对端）上，前端显示实话而非可点的死按钮 | 未验（无 `pty_supported=false` 的对端；分支已由 Task 16 `usePtySupport.test.ts` 的 true/false/nil 三态单测覆盖） |
| 10 | 老版本 agentd（`pty_supported=nil`）不被误判为「不支持」，文案区分「对端没上报」与「明确不支持」 | 未验（无 `pty_supported=nil` 的对端；分支已由 Task 16 `usePtySupport.test.ts` 的三态单测覆盖） |
| 11 | 用户没有显式配置时，新版 `Save` 写出的 `config.yaml` **不含** `env_forward` 键，而 `SSH_AUTH_SOCK` 转发照样工作 | 未验（待审核者在本地带浏览器的环境执行） |
| 12 | W4 spec §2.6 已追加指向 §1 的修正说明 | 通过（文档核对）：`docs/superpowers/specs/2026-08-12-w4-shell-calibration-design.md:179` 保留指向 `2026-08-12-w4-pty-terminal-design.md#1-前提修正控制台会话在能力上等价于主令牌` 的警告，锚点存在 |

## 缺陷修复回填（2026-08-13）

审核者在本地带浏览器的环境跑完了本分支的真机走查（12 条验收：10 通过 / 1 部分 / 1 未验），
并定性了三个缺陷。三条均已在本分支修复：

| 缺陷 | 现象 | 修法 | 提交 |
|---|---|---|---|
| A | WebGL 上下文运行期丢失后终端永久白屏；spec §6.3「不能白屏」在运行期这侧是断的 | 注册 `onContextLoss` 并 dispose addon，交回 DOM 渲染 | bcb3e2c |
| B | `ranRef` + 局部 `cancelled` 在 StrictMode 下互相拆台，开发模式恢复不出任何终端 tab | 取消标志改为跨 effect run 的 ref，进入 effect 时撤销 | 83f7100 |
| C | 建会话中途被卸载会漏孤儿 shell（服务端已建、id 从未回报、无人能连能杀） | 该路径显式 DELETE 掉这个没人知道的会话 | 895166f |

仍未验证、不在本次修复范围内的两条：
- 验收第 3 条只做到「两个本机 agentd 之间验通反代链路」，真 devbox + 界面远程终端入口未验（devbox 上跑的是 main，没有 PTY 接口；界面机器节点来自项目位置，假远程不上树）。
- 验收第 9 条（`pty_supported=false` 的对端）无 Windows 对端，未验。
