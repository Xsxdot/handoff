# Handoff 需求总账

> 三期（B4–B18）公共验收证据：`go build ./...` + `go vet ./...` + `gofmt -l .`（无输出）+
> `go test ./...` 全绿 + `go test -race ./internal/agentd/ ./internal/executor/opencode/ ./internal/localsync/ ./cmd/` 全绿，
> **远程执行机与本地各独立跑一次**（08-09）。以下各行「验收」列只记该条**专属的真机验证**。
> 全部条目均无原型/流程图，自动免除对照。

## Backlog

| ID | 标题 | 状态 | 优先级 | Spec | 原型/流程图 | 验收 | 变更痕迹 | 备注 |
|----|------|------|--------|------|------------|------|---------|------|
| B1 | 二期：审批链/executor 选择/dispatch 扩展/可观测性 | ✅ done(已验) | 高 | [spec](specs/2026-08-08-handoff-approver-dispatch-observability-design.md) | — | go build + gofmt + go vet + go test ./... 全绿（合并结果上重跑）、go test -race ./internal/agentd/ ./internal/executor/opencode/ 绿；attach 本机/远程 execve 路径真机实测通过；真实旧库迁移实测无损；无原型/流程图，自动免除对照 08-08 | 08-09 三期压测修正：当时「attach 远程路径实测通过」只验了 execve 路径解析、未验成功的远程连接，实际远程 attach 从未可用（见 B17）——验收标准定松了 | 08-08 完成并合入 main（c89932a，25 提交）。由 handoff 自身派发 devbox/opencode 执行，三轮审核：外部审阅发现 2 P0 + 5 P1 + 4 P2 全部修复 |
| B2 | Claude Code adapter（任务级五动作全链路） | ✅ done(已验) | 高 | [spec](specs/2026-08-08-handoff-claude-code-adapter-design.md) | — | `go test ./...` 全绿（15 个包）；devbox 真机端到端 7 项全过：①`attach` 两窗口都活、render.log 实时增长 ②权限升级带完整 161 字文本、`event_truncated=false`、黑名单命中 1ms 内升级人工 ③`reply --approve` 后命令**真的执行了**（产出提交 `b59f6a0`）④`session_id` `d39dcfa4-…` 在 285KB `out.jsonl` 里全程唯一，跨两次 continue 与一次权限往返不变 ⑤`diff` + `done` 归档，tmux 会话被清理 ⑥agentd 重启后 `执行器存活，重建订阅继续消费 alive=true`，重启后再 `continue` 产出真实提交 `660b56b` ⑦`HANDOFF_ENV_PROBE=ok`——B19 的 env 注入首次在 claude adapter 上实证；无原型/流程图，自动免除对照 08-09 | 08-08 spike 实测定案：`--permission-prompt-tool` + 内置 stdio MCP server 挂权限门；stream-json 双向流跨回合存活；可视化对齐现状（tmux 两窗口，不自研 TUI）；继承 user/project settings 保 skills、任务级 deny/ask 收口。08-09 真机验收中现次生缺陷并当场修掉：`tmux new-session -d` 一创建会话就返回，**不代表会话内脚本已跑到 `exec 3<> in.fifo`**，而 `WriteInput` 以 `O_WRONLY\|O_NONBLOCK` 打开 fifo，读端未就绪时 POSIX 规定直接失败（ENXIO，macOS 文案 "device not configured"）——加 `waitFIFOReader` 轮询等读端（`ab68513`）；首版修复又漏了失败路径的会话回收（`rollback` 靠 `r.proc` 判空，而 `StartProc` 返回 nil），补 `p.Kill()`（`ce3aa14`）。两轮都先独立跑 RED 确认失败现象与真机一致 | 来源：二期 spec §4.4 范围外单独立项 |
| B3 | grok adapter（任务级五动作全链路） | ✅ done(已验) | 中 | [spec](specs/2026-08-08-handoff-grok-adapter-design.md) | — | `go test ./...` 全绿（15 个包）；devbox 真机走完 plan Task 9 全部 8 步（任务 `6a59bcdb` 与三个提问专项探针 `1c129cf6`/`47c36ab9`/`367b146d`）：①grok 已登录、tmux 可用 ②派发即 running ③`handoff-6a59bcdb` 两窗口都活（窗口 0 = serve、窗口 1 = `tail -f render.log`），render.log 累计 4152 字节 52 行，含模型推理、工具动作与 `【模型提问】` 段 ④权限门两条路径都实证：`printf > retry.md` 由廉价模型审批者**自动批准**（6.5s），`rm -rf /tmp/b3check && …` 命中黑名单**1ms 内升级人工**（18:14:05.796→.797），`reply --approve` 后 `/tmp/b3check` **真的被删掉**了 ④b `ask_user_question` 不挂死且只产出**一张**工单，问题文本带四个选项，render.log 有 `【模型提问】`，回合 5 秒内收口；`reply --answer "选 5000ms"` 后 `relayed=true`、模型续接并原样复述 ⑤`continue` 在**同一会话**（`019fe603-…`，跨 agentd 重启不变）产出真实提交 `ce008af`，`done` 后 managed worktree 与 tmux 会话都已清理 ⑥secret 未泄漏：`ps -eo command` 真实命中 0（此前数到 2 是 grep 自匹配，用快照法复核）、agentd.log 命中 0 ⑦执行中重启 agentd → `grok 任务已恢复` + `执行器存活，重建订阅继续消费 alive=true`；无原型/流程图，自动免除对照 08-09 | 08-08 spike 实测**推翻原前提**：grok 1.0.0 实现完整 ACP，权限门是协议内建的 `session/request_permission`，不存在原记的「缺程序化审批挂载点」；标题与优先级随之修正。08-09 [plan](plans/2026-08-09-handoff-grok-adapter.md) 落地，Task 1（抽取 `internal/executor/turn`）已单独合入 main（282c932）。08-09 真机验收中 `ask_user_question` 连翻两次车，都在本轮修掉：①**应答形态一直是错的**——先回裸 `{}`，再照抄 `session/request_permission` 的内嵌形态 `{"outcome":{"outcome":"cancelled"}}`，两次都被 grok 判为工具执行失败。真相是 grok 自己 serve.log 给的：`invalid type: map, expected variant identifier at line 1 column 11`——那是内部标签枚举 `AskUserQuestionExtResponse` 的报错，标签字段 `outcome` 的值必须是**变体名字符串**；合法变体从 grok 二进制符号表读出只有 `accepted`/`skip_interview`/`chat_about_this`，选 `skip_interview`（语义正是「用户没走这个通道作答」），改在 `0c70414` ②**一次提问给审核者两张工单**——工具通道转交问题后，模型结束回合又没输出收尾协议，兜底把整段叙述文本（「已调用一次提问工具；本回合结束。」）当成第二个问题上报；兜底要留（保证回合不静默结束），但本回合已通过工具给过问题时该诉求已满足，加回合级 `askedViaTool` 取走式标记抑制，改在 `74eba6a`。两处都先跑 RED 再修 | 来源：二期 spec §4.4。领于 08-09；Task 2 起逐 task 派发 devbox 执行 |
| B4 | 远程 target 派发前代码同步保证 | ✅ done(已验) | 中 | [spec §3](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 真机两条路径都验：①本地有 2 个未推提交时派发 → 400 拒发，错误含 sha `32abad5e…` + 仓库路径 + `请先 git push` 提示；②远程落后时派发 → 日志「基线提交缺失，补拉远端」→ `git fetch --all --prune`（4.5s）→「补拉远端后基线提交已就位」→ 放行 08-09 | 08-08 定策略：自动 fetch（基线缺失才 fetch 再复查，仍缺失即拒发） | 来源：08-08 devbox 真实测试——远程仓库落后 2 提交需手动 push+pull |
| B5 | 任务停止/取消命令（handoff stop） | ✅ done(已验) | 中 | [spec §4](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 真机验证：running 任务 stop → 状态落 failed、tmux 会话 `handoff-6cfa341e` 消失、该任务 serve 进程退出（其他任务的 serve 不受影响）；重复 stop → 409 且日志为 WARN 非 ERROR 08-09 | 08-08 定终态：复用 failed + 事件写明原因，不新增 aborted 状态；08-09 追加：Stop 需自行清理 managed worktree（见 B15），CLI 提示文案随之改为按响应体 `worktree_removed` 打印 | 来源：08-08 真实测试——废弃 running 任务只能 ssh 杀 tmux 会话，缺 CLI 一等入口 |
| B6 | 权限描述截断导致审核者盲批 | ✅ done(已验) | 高 | [spec §5](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 单测覆盖三条契约：工单 request 存全文、事件 payload 截至 200 字并带截断标记、黑名单命中长命令**尾部**的 `rm -rf`（正是旧实现漏掉的那条）08-09 | 08-08 定方案：工单存全文、事件仍截断；连带把黑名单/审批者改为对全文匹配。刻意接受误升级变多（错误方向是「多叫醒审核者一次」，反方向是「漏放一条 rm -rf」） | 来源：08-08 真实测试——permTextLimit 截断长命令，安全门形同虚设 |
| B7 | agentd 侧 PATH 继承与工具链探测 | ✅ done(已验) | 中 | [spec §6](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 真机验证：故意用最小 PATH（`/usr/bin:/bin:/usr/sbin:/sbin`）启动 agentd，日志「已合并登录 shell 的 PATH」且 added 列表含 `/usr/local/go/bin` 08-09 | **08-08 方案被实测推翻并修正**：初稿 `$SHELL -l -c` 在 devbox 上一无所获——该机 PATH 追加写在 `.zshrc`（交互式才加载），而 `-l` 只 source `.zprofile`/`.zlogin`，修复会在它要解决的那台机器上恰好无效。改为 `-l -i -c` + 只取 stdout、不看退出码、stderr 丢弃 | 来源：08-08 真实测试——agentd 继承的 PATH 缺 go |
| B8 | --worktree 归属校验接受仓库子目录 | ✅ done(已验) | 低 | [spec §7](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 真实 git 仓库集成测试：仓库子目录被按 ErrBadWorkspaceReq 拒绝、真 worktree 仍被接受且 Managed=false 08-09 | — | 来源：08-08 二期审阅 P2-1——git-common-dir 向上查找使 /repo/internal/sub 被当作 worktree 接受 |
| B9 | 审批者裁决输出的 nonce 防伪 | ✅ done(已验) | 中 | [spec §8](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 表驱动单测：回显正确 nonce → approve 生效；nonce 错误 → 判无效；缺 nonce → 判无效（两者均 fail-closed 升级）08-09 | — | 来源：08-08 二期审阅 P2-4——权限原文由被监管的 executor 产生（不可信）且被插进审批 prompt |
| B10 | workspace git 调用无超时 | ✅ done(已验) | 中 | [spec §9](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 单测：已取消 ctx 下 PrepareWorkspace 与 RemoveManagedWorktree 均立即失败；真机日志可见 `timeout=2m0s` 字段 08-09 | — | 来源：08-08 二期审阅 P2-6——全部 context.Background()，worktree add 挂死会拖住 dispatch 的 HTTP handler |
| B11 | attach 无参列表的建议命令丢 --target | ✅ done(已验) | 低 | [spec §10](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 真机验证：非 TTY 输出为 `handoff attach <id> --target devbox`（本机任务不带）；4xx 日志确认降为 WARN（dispatch 400、stop 409 实测均为 WARN）08-09 | — | 来源：08-08 二期终验 |
| B12 | 任务完成后本地自动同步远程任务分支 | ✅ done(已验) | 中 | [spec §11](specs/2026-08-08-handoff-backlog-cleanup-design.md) | — | 真机端到端：远程任务完成 → wait 自动同步 → 本地 `已同步分支 handoff/46e84025（新增 2 个提交）`，且该输出走 stderr、stdout 仍只有单行事件 JSON；同步失败时（B17 未修前）wait 退出码仍为 0，符合「同步失败不阻断唤醒」设计 08-09 | — | 来源：08-08 三期 brainstorm 用户追加——与 B4 构成闭环 |
| B13 | isTTY 把 /dev/null 当成终端 | ✅ done(已验) | 中 | — | — | 真机验证：`handoff attach --target devbox < /dev/null` 正确走非 TTY 降级并打印建议命令（旧实现会打表格再报「读取选择」错误）08-09 | 08-09 三期压测中定位并直接修复，未单独出 spec；改用 github.com/mattn/go-isatty（原为间接依赖，零新增模块） | 来源：08-08 三期压测实测 |
| B14 | 探活档位切换的日志级别不对称 | ✅ done(已验) | 低 | — | — | 真机验证：round2 二进制启动（00:31:55）之后日志再无一条 INFO 级「探活降频」，此前约每 4 秒一条 08-09 | 08-09 三期压测中定位并直接修复，未单独出 spec；两档统一为 Debug | 来源：08-08 三期压测实测 |
| B15 | 落 failed 的任务永久泄漏 managed worktree | ✅ done(已验) | 高 | — | — | 真机验证两条泄漏路径均已堵住：①令 tmux 不在 agentd 的 PATH 中使 adapter.Start 失败 → 日志「managed worktree 已删除」、worktrees/ 下零残留；②`--new-worktree` 任务 stop → worktree 已删而分支 `handoff/c6418a7b` 保留 08-09 | 08-09 三期压测中定位并直接修复，未单独出 spec。实现改为 defer 统一补偿，并由执行者补上 `executorStarted` 守卫：executor 接管工作区后不再补偿删除（否则会把运行中的任务脚下抽空）——该权衡不在审核指令内，是执行者自行判断 | 来源：08-09 三期压测实测（实证残留 worktrees/0995f42e 与 /6cfa341e，已手工清理） |
| B16 | dispatch 500 掩盖 executor 启动失败真因 | ✅ done(已验) | 中 | — | — | 真机验证：tmux 缺失时 dispatch 报 `启动 executor 失败: tmux 启动 handoff-e0075fc4: exec: "tmux": executable file not found in $PATH`（此前为扁平「派发任务失败」）08-09 | 08-09 三期压测中定位并直接修复，未单独出 spec；状态码保留 500（agentd 侧环境问题），关键是回显可行动真因 | 来源：08-09 三期压测实测 |
| B17 | target 无法配置 ssh 用户名，远程 attach/pull 实际不可用 | ✅ done(已验) | 高 | — | — | 真机端到端：配置 `user: sycm` 后 `handoff pull` 换算出 `sycm@100.73.238.21:/Users/sycm/workspace/handoff` 并成功拉回分支（修复前报 Connection closed）08-09 | 08-09 三期压测中定位并直接修复，未单独出 spec；抽 `sshHostFromTarget` 为 attach/pull 唯一换算点，config.Target 加可选 `user`，README targets 示例同步补齐 | 来源：08-09 三期压测实测；连带修正 B1 的验收结论 |
| B18 | agentd 重启后 waiting_review 任务无法续接 | ✅ done(已验) | 高 | — | — | 真机端到端（本缺陷的完整闭环）：重启 agentd → 启动恢复日志「执行器存活，重建订阅继续消费 task=0126bf0a alive=true state=waiting_review, recovered=1」→ `handoff continue` 返回 `{"ok":true}` 并真的产出了一个提交（修复前为永久 500）08-09 | 08-09 三期压测中**由审核者亲身撞上**并直接修复，未单独出 spec：换二进制重启 agentd 后 round2 审阅指令发不进去，只能重新派发。修法①启动恢复纳入 waiting_review 探活重建但**不改状态**（executor 不在时也不追加 failed 事件，避免噪音）；②ErrTaskNotRunning 映射为带重派提示的 409 | 来源：08-09 三期压测实测 |
| B19 | agent 启动前注入环境变量 | ✅ done(已验) | 中 | [spec](specs/2026-08-09-handoff-agent-env-injection-design.md) | — | 合并结果上重跑 go build + go vet + gofmt -l .（无输出）+ go test ./... 全绿 + go test -race ./internal/agentd/ ./internal/envfile/ ./internal/executor/opencode/ ./internal/localsync/ ./cmd/ 全绿；devbox 真机端到端：①启动预检载入 5 个 key，日志只有 key 名没有值 ②run_serve.sh 的 export 行排在 OPENCODE_* 之前、值单引号包裹 ③`ps eww` 确认 serve 进程实际环境里 export 前缀已剥离、注释已跳过、`${VAR}` 单层展开（文件内键与进程环境两种来源都验）、单引号内不展开 ④审批者在「审批者开始裁决」同一毫秒读同一份 env ⑤文件移走后不重启即拒发（证实每次读盘）、语法错回显「第 3 行键名非法」、两种拒发均未创建任务；无原型/流程图，自动免除对照 08-09 | 08-09 brainstorm 定案：格式选 dotenv + 单层展开而非「shell 片段 source 进去」——后者零解析代码但只对走 shell 脚本的 adapter 有效，而 backlog 上排队的 B2/B3 两个 adapter 进程模型完全不同，会让本功能两个月内退化成「只有 opencode 能用」；注入经 executor.StartReq 契约下发，审批者同享一份（否则代理只配半边，审批者连不出去会静默 fail-closed 升级） | 来源：08-09 用户需求。B7 是「自动猜用户环境」，本条是「显式声明该给什么环境」，两者互补 |
| B20 | 归档时 executor 回收失败无兜底也无信号 | ✅ done(已验) | 低 | [spec](specs/2026-08-09-handoff-runtime-reconciliation-design.md) | — | go test ./... (ok, 16 包全绿)；devbox 真机两条路径都验：①兜底回收成功——删掉 `claude.json` 造出「恢复凭据丢失但 tmux 会话仍活」的孤儿现场，重启 agentd 后 `done`，日志「按确定性命名兜底回收成功 tmux=handoff-efb6a1f6」且 `tmux ls` 中该会话已消失（正是 B20 现场存活 11.5 小时的那类残留）②回收失败留信号——用 tmux 垫片让 `kill-session` 必失败，`done` 后任务事件流出现 progress 事件「executor 资源可能残留：tmux 会话 handoff-f1d40727，请手动 tmux kill-session -t handoff-f1d40727（原因：exit status 1）」，不再静默；grok 端到端未跑（会触发 B26 顶掉用户 grok 登录态），其冷恢复能力在 spike 阶段单独真机验过；无原型/流程图，自动免除对照 08-09 | 与 B21/B24 同源（状态机记录的状态与 executor 实际存活性无对账机制），合并进 Spec A 一并解决 08-09 | 领于 08-09。来源：08-09 真机实证。`done` 时 `Adapter.Stop` 返回 `ErrTaskNotRunning`（agentd 曾重启、adapter 内存运行态已丢），代码按设计只打一条 ERROR 继续归档（manager.go Done 的 doc 明写「executor 残留交给人工兜底」），结果 tmux 会话 `handoff-46e84025` 与其 `opencode serve`（port 49234）孤儿存活 11.5 小时，直到人工 `tmux kill-session`。两个可改点：①有确定性兜底没用上——会话名恒为 `handoff-<id8>` 且 `serve.json` 存了 tmux_session，运行态丢失时完全可以按名回收；②信号不对称——worktree 清理失败会发 progress 事件提示人工，executor 停不掉却完全静默，审核者无从知道有残留。B18 落地后 running/waiting_review 会在启动时重建运行态，触发面已大幅收窄，故优先级低 |
| B21 | executor 空回合后任务卡在 running，唯一信号迟到 2 小时且无恢复出口 | ✅ done(已验) | 中 | [spec](specs/2026-08-09-handoff-runtime-reconciliation-design.md) | — | go test ./... (ok, 16 包全绿)；devbox 真机：任务 running 中直接杀掉 executor 进程，21:39:47 杀、21:39:48 就收到 `failed` 事件（1 秒，不再是 2h `StallTimeout`），状态 `running→waiting_review reason=result`；随后事件通道关闭触发的对账日志「任务无需对账，跳过」证实取走式停止标记生效、没有补出第二条重复事件；空回合本身转失败结果（不再静默、不再产空工单）由三个 adapter 的单测覆盖；grok 端到端未跑（会触发 B26 顶掉用户 grok 登录态）；无原型/流程图，自动免除对照 08-09 | 与 B20/B24 同源，合并进 Spec A 一并解决 08-09。08-09 首版把本条记成「没有任何看门狗」是错的——`RunWatchdog` 确实存在（每分钟扫 running/waiting_answer，最新事件早于 `StallTimeout` 即发 stalled 事件唤醒审核者）。核对 `internal/agentd/watchdog.go` 后改写：问题不是「无信号」，而是「信号迟到 + 有信号也没有出口」，优先级随之从高降到中 | 来源：08-09 B2 修复派发实证。任务 `356368f2` 在 `state=running` 下静止 1 小时：opencode 连做 7 个回合工具调用后，最后一步 `step-finish` 的 `reason=unknown`、tokens 全 0（供应商流中断，reasoning 截在半句「Loop opening the fif」），会话随即 idle。agentd 只打了一条 `WARN idle 但回合无文本，跳过分类`。两个缺口：①**信号迟到**——`StallTimeout` 默认 2h，而这里 4 秒内就能确定「回合结束且零文本产出」，审核者要白等 2 小时才收到 stalled；②**有信号也没有出口**——stalled 按设计只唤醒不改状态（executor 可能仍在干活，语义是对的），但此时 `continue` 要求 waiting_review、`resume` 只重投未送达的应答，两条 CLI 出口都够不着，唯一办法仍是 `stop` 后重新派发。可考虑：adapter 侧对「idle 且回合零文本」直接发事件（不必等 2h 的通用兜底），以及给 stalled 任务一条一等恢复入口。B18 解决的是「agentd 重启后运行态丢失」，本条是「executor 还活着但这一回合空转」，另一条路径 |
| B22 | handoff wait 重放已处理的历史事件 | 💡 idea | 中 | — | — | — | — | 来源：08-09 三期与 B2/B3 集成期间反复踩。服务端游标从任务起点算起，对一个已经跑了几十个事件的任务挂 `wait`，要连按十几次才能追到当前水位，实际用不了——本会话所有监听最后都退回轮询 `show` 的状态字段。缺的是「从当前水位开始等」的语义（或客户端可传游标/`--since=now`） |
| B23 | 黑名单对叙述性文本误判升级 | ✅ done(已验) | 低 | [spec](specs/2026-08-09-handoff-permission-gate-precision-design.md) · [plan](plans/2026-08-09-handoff-permission-gate-precision.md) · [探针](plans/2026-08-09-permission-payload-probe.md) | — | `go build ./...` + `go vet ./...` + `gofmt -l .`（无输出）+ `go test ./...`（17 个包 ok）+ `go test -race ./internal/agentd/ ./internal/permgate/ ./internal/executor/{claudecode,grok,opencode}/ ./internal/localsync/ ./cmd/` **连跑 3 轮全绿**。devbox 真机：grok 任务 `2ad0a746` 执行 `git commit --allow-empty -m "chore: 验证 rm -rf 字样不再误触发审批"`——**未产生 gate 工单、未唤醒审核者**，agentd 日志 `level=INFO msg=权限判定：交审批者 … tool=bash reason=仅引号内字面量命中黑名单，降级交审批者裁决 rule=(?i)rm\s+-[a-z]*[rf][a-z]*[rf]`，任务事件里只有 `approver_decision`、`permission_request` 为 0。判据改为「剥离引号字面量后重新匹配」：引号外仍命中→硬升级，仅引号内命中→降级交审批者，带 `sh -c`/`eval` 等执行包装器时不降级（引号内内容会被真执行）。无原型/流程图，自动免除对照 08-09 | 08-09 与 B27 合并为一个 spec：两者是同一处判据（对整串展示文本做正则）的两面，分开做必然打架——单独收紧 B23 会更容易漏、单独补 B27 会更容易误报。同时**范围收窄**：原文的「同一等价请求重复升级、无本任务已批准记忆」明确不做（spec §2 非目标），结构化判据落地后重复升级里绝大多数本就是误判会自然消失，剩下的重复是真危险命令重复，而「批准一次后同样命令自动放行」是实打实的安全削弱 | 来源：08-09 观察。B6 把黑名单改为对权限全文匹配（方向正确，堵住了尾部藏 `rm -rf`），代价是提交信息、计划正文里出现的危险词也会命中——审核者被叫醒去批一条「commit message 里提到 rm -rf」的请求。同一等价请求重复出现时也会重复升级，没有「本任务内已批准过同一条」的记忆。两者都是「多叫醒一次」的安全方向，但叫多了会训练出盲批习惯，反而侵蚀 B6 的收益 |
| B24 | executor tmux 会话丢失后 waiting_review 任务成为孤儿 | ✅ done(已验) | 中 | [spec](specs/2026-08-09-handoff-runtime-reconciliation-design.md) | — | go test ./... (ok, 16 包全绿)；devbox 真机 opencode 与 claude 两个 executor 各验一遍：杀掉 executor 进程后 `continue` 走完恢复阶梯——「续发指令时 executor 已不在，进入恢复阶梯」→「进入冷恢复」→「恢复结果 alive=true mode=cold note=已从磁盘载入原会话，上下文完整」，且模型真的复述出原会话内容（MARKER-OPENCODE-5521；MARKER-CLAUDE-8834 + 提交 hash 8b92078 + 分支名），证明不是新开会话冒充恢复；agentd 重启时判不可恢复的 waiting_review 任务按新语义原地保留（「启动恢复完成 waiting_review_kept=1」）而非变孤儿；grok 端到端未跑（会触发 B26 顶掉用户 grok 登录态），其 `session/load` 冷恢复在 spike 阶段单独真机验过（ALPACA-7731）；无原型/流程图，自动免除对照 08-09 | 与 B20/B21 同源，合并进 Spec A 一并解决 08-09；写 spec 时核对代码发现下述「`done` 也走不通」不成立（`Done` 里 `Stop` 失败只记日志、照常归档），实际缺口只有 `continue`，见 spec §1.2 | 领于 08-09。来源：08-09 审核者亲身撞上。B18 让 agentd 重启后能按会话名探活重建；但若 tmux 会话本身没了（本次是审核者用 `pkill -f "handoff agentd"` 误杀——该模式匹配到了 tmux **server** 自身保留的 argv `tmux new-session -d -s agentd "...handoff agentd..."`，整个 server 连同三个 executor 会话一起没了），任务就永久停在 waiting_review：`continue` 报 409「运行态已丢失，请重新派发」，`done` 也走不通，分支上的成果只能靠 `pull` 抢救、后续修改只能新开任务。可考虑：waiting_review 且运行态不可重建时，允许 `done` 归档（成果已在分支上），或提供「按原分支重新派发并续接」的一等入口 |
| B25 | TestNilApproverKeepsCurrentBehavior 对负载敏感 | ✅ done(已验) | 低 | — | — | 08-09 B23/B27 收尾时复现并修掉：7 包并发 `-race` 时该用例翻红一次，改后同样负载连跑 3 轮全绿。**根因不是「sleep 式断言」而是「拿错了就绪信号」**：`escalatePermission` 按 U-1 的必需顺序**先落 waiting_answer 状态、再建工单、最后才追加 permission_request 事件**，三处测试却拿 `waitTaskState` 当就绪信号后立刻 `mustEvents`，那段窗口在 CPU 吃满时足以读到空列表。修法：新增 `waitEvent` 助手轮询事件本身，三处改用它。顺带修掉同批暴露的另一处——`TestStartWritesPromptBeforeWaitingReady` 给「真子进程拉起 + 写 fifo + 事件穿过 streamLoop」钉了 2s 死线，放宽到 10s（外层 ctx 本就 15s，不削弱断言）。无原型/流程图，自动免除对照 08-09 | 来源：08-09 多次并行跑测试时偶发失败，单独重跑即绿。该用例依赖时间窗口而非条件等待，机器负载高（如同时跑 `-race` 与真机 e2e）时翻红，属典型的 sleep 式断言。改为条件轮询即可 |
| B26 | grok 任务令牌刷新会顶掉 auth 软链，刷完的凭据困在任务目录、用户登录态失效 | ✅ done(已验) | 高 | [spec](specs/2026-08-09-handoff-grok-credential-ownership-design.md) · [plan](plans/2026-08-09-handoff-grok-credential-ownership.md) | — | `go test ./...`（15 个包 ok）+ `go test -race ./internal/executor/grok/`（ok）+ gofmt/vet 干净，08-09 本机复核。三条防线各做了变异检查、确认对缺防线的实现真的变红：①拆掉「严格更晚」→ 倒灌用例变红；②写回失败时错误复位软链 → 保留副本用例变红；③退回「先删后建」→ 无破链窗口用例变红。无原型/流程图，自动免除对照。**真机验收未做**：spec §8 要求在 devbox 上跨过一次真实令牌刷新（6 小时一轮）确认 `~/.grok/auth.json` 的 `expires_at` 前进、刷新后新派发任务能正常起来——这条留待下次自然跨过刷新窗口时验，未验之前不能算端到端闭环 | 08-09 交付：分支 `handoff/b6fe7365`，5 个提交（3 实现 + 1 修复 + 1 计划勘误），opencode 执行、三轮审核。审核中拦下三处：README:197 仍留着被本需求证伪的「token 刷新后会自动重建」；`resetAuthLink` 先删后建有破链窗口（Symlink 失败则任务永久失去凭据，与 spec §5「下轮再试恢复」矛盾，已改原子 rename）；补的「无破链窗口」用例最初用 `chmod 0500` 注入、对旧实现照样绿（删文件卡的是目录写权限，旧实现第一步就被挡住，窗口从未走到），改用超长 target 触发 ENAMETOOLONG 才有判别力。计划文档里那条 `if false { continue }` 变异配方也是错的（变量未使用、Go 编译失败），已勘误。08-09 brainstorm 出 spec，三处修正了此前的记载：①**触发频率量化**——实测 access token 寿命 **6 小时**（`create_time` 09:55:11Z / `expires_at` 15:55:11Z），不是罕见地雷，持续用 grok 的一天里每 6 小时必然出现一次窗口；②**本行备注里记的方向二「让任务级 home 复用真实 ~/.grok 而只隔离 config.toml」不可能**——`grok agent serve` 无 `--permission-mode`/`--allow`/`--deny`，README 环境变量表里配置目录只有 `GROK_HOME` 一个入口，且项目级 `.grok/config.toml` 只支持 `[mcp_servers]`，**config.toml 就是 home**；③方向一「写回」被采纳，但形态定为「周期巡检 + 收编写回 + 软链复位」而非任务收尾写回——写回晚了不只是用户 CLI 用不了，期间新派发的任务软链指向已作废凭据会必然 500。另记两条排除路线：`auth_provider_command` 是假根治（README 第 331 行的 login 时序会先走 OIDC 静默刷新，只要 auth.json 里有 refresh_token 就关不掉）；`XAI_API_KEY` 真机验证**确实能根治**（全新 home 不放 auth.json、`auth_type=ApiKey` 建会话成功、home 里从未出现 auth.json），但属计费换轨，用户选择只做巡检写回、API key 只进文档不进代码。08-09 首版把触发条件写成「跑完一个 grok 任务就把登录态弄失效」，**过强**。用户重新 `grok login`（17:55:12）后我又跑了 5 个 grok 任务（`1c129cf6`/`36f294ee`/`47c36ab9`/`367b146d`/`6a59bcdb`，含一个带两次提交、两次权限裁决、一次 agentd 重启的完整端到端），**软链全部完好**、`~/.grok/auth.json` 的 mtime 一直停在 17:55:12 没被动过。所以触发条件是「任务恰好跨过一次令牌刷新」，不是每次任务——严重性不变（不可预测的地雷，踩中就要重新登录），但复现要靠等令牌临期，不能靠重复跑任务 | 来源：08-09 B3 真机验证实证。`EnsureAuthLink` 的设计意图是对的（软链而非拷贝，注释明写「拷贝会让每个任务 home 各自持有凭据并独立刷新，而刷新令牌轮换可能反噬用户本人的登录态——凭据只应有一个权威副本」），但**软链拦不住 grok**：它刷新令牌时是 unlink 后重写，不是顺着链接写回。实证——任务 `30b92291` 跑完一个完整回合后，`<taskDir>/grokhome/auth.json` 从软链变成了 `-rw-------` 普通文件（mtime 17:49，内容与 `~/.grok/auth.json` 不同），而 `~/.grok/auth.json` 仍停在 11:13 的旧令牌；同机另外 6 个 grok 任务目录的软链都完好，因为它们都在刷新前就失败了。后果：**跑完一个 grok 任务就把用户的 grok 登录态弄失效**——紧接着的下一次 dispatch 直接 500 `grok 未登录或凭据已失效`（新任务的软链指向的正是那份已被服务端轮换掉的旧令牌），用户本人的 `grok` CLI 同样受影响，只能重新 `grok login`。`EnsureAuthLink` 的函数注释里其实已经记着「spike 实测任务级 home 的软链会在 token 刷新前后消失」，但当时的对策只是「Start 与 Resume 都重建软链」——重建只是让下一次**启动**能读到文件，救不回已经被轮换走的令牌。可能的方向：任务结束/刷新后把任务目录里的新凭据写回 `~/.grok/auth.json`（需处理并发任务互相覆盖），或干脆让任务级 home 直接复用真实 `~/.grok` 而只隔离 config.toml |
| B27 | 写文件类工具不设门，危险面只覆盖了 Bash | ✅ done(已验) | 中 | [spec](specs/2026-08-09-handoff-permission-gate-precision-design.md) · [plan](plans/2026-08-09-handoff-permission-gate-precision.md) · [探针](plans/2026-08-09-permission-payload-probe.md) | — | `go build ./...` + `go vet ./...` + `gofmt -l .`（无输出）+ `go test ./...`（17 个包 ok）+ `go test -race ./internal/agentd/ ./internal/permgate/ ./internal/executor/{claudecode,grok,opencode}/ ./internal/localsync/ ./cmd/` **连跑 3 轮全绿**。devbox 真机三条：①**范围内写入不叫人**——claude 任务 `da4df33a` 与 grok 任务 `2ad0a746` 各做一次 Write + 一次 Edit，两个任务都**直接跑到 waiting_review、全程没进过 waiting_answer**，`permission_request` 事件 0 条、gate 工单 0 张，agentd 日志 `本段执行自动放行工作区内写入 task=da4df33a n=2` / `task=2ad0a746 n=3`；②**越界写必须叫人**——`continue` 让 claude 写 `/tmp/e2e-outside.md`，任务进 waiting_answer 并建 gate 工单（request 为 `Write: /tmp/e2e-outside.md`），agentd 留 **WARN 级** `权限判定：升级人工 … tool=write paths=[/tmp/e2e-outside.md] workdir=/Users/sycm/.handoff/worktrees/da4df33a reason="目标路径越出任务范围: /tmp/e2e-outside.md"`；③**拒绝后文件确实没被创建**——`reply --answer reject` 后 `ls /tmp/e2e-outside.md` 为 `No such file or directory`。无原型/流程图，自动免除对照 08-09 | 08-09 与 B23 合并为一个 spec（合并理由见 B23 同列）。同时**更正本条原记载**：原写「两个 adapter 形状同源，是两边共有的口子」不准确——核对代码后，claude（`allowRules` 含 Edit/Write）与 grok（`allowRules = ["Edit","Write"]`）确实零路径维度，但 opencode 另有一条 `external_directory: "ask"`，名义上覆盖越界写、实际是否拦得住绝对路径写入未验证（已列入 spec §6.1 探针待验项）。范围也已明确收窄：spec 只覆盖 Write/Edit 的工具级路径判据，`printf > ~/.ssh/authorized_keys` 这类 Bash 重定向越界不在范围内（spec §2 非目标） | 来源：08-09 B2 真机验收时的观察，**核对 spec 后确认是设计层遗留、不是 B2 的实现缺陷**。任务级 settings 的形状是「`allow` 兜底放行其余工具（对应 opencode 的 `"*": "allow"`，避免 ls/grep 连环唤醒）+ `ask` 收窄危险面」，而 `ask` 表里全是 `Bash(rm:*)` 这类**命令前缀**规则。于是 `Bash` 里的 `rm -rf` 会进审批链，`Write`/`Edit` 直接写 `~/.ssh/authorized_keys`、`~/.zshrc`、`~/Library/LaunchAgents/*.plist` 却完全不经过任何人——同样是「把宿主机改坏」，一条要人批、一条静默通过。两个 adapter 形状同源（opencode 二期就是 `"*": "allow"`），所以这不是 claude 侧的回归，是两边共有的口子。触发面窄（模型极少主动写仓库外的绝对路径），但一旦触发就是最坏后果，且当前**连事件都不会留**。可考虑：对 `Write`/`Edit` 加一条「目标路径在任务 worktree 外即升级」的规则（路径判定比命令前缀匹配更可靠），或至少让越界写产出一条事件让审核者事后可查 |
| B28 | codex adapter（任务级五动作全链路） | 🔨 doing | 中 | [spec](specs/2026-08-09-handoff-codex-adapter-design.md) | — | — | 08-09 brainstorm 出 spec，前置 spike 已完成（codex-cli 0.144.1，四轮真实回合），spec §1 记录全部实证。**四处推翻了按 schema 名字做的推断**：①`turn/start` 是**异步**的（立即返回 `inProgress`，终态在 `turn/completed` 通知里），首版泵循环据此改写；②workspace-write 沙箱**不拦 `/tmp`**（`excludeSlashTmp`/`excludeTmpdirEnvVar` 默认 false），实测 `touch /tmp/…` 在 `on-request` 档下零工单直接成功；③**沙箱拒网不产工单**——显式钉死 `networkAccess:false` 后模型的 `curl` 直接失败、`approval_count=0`，审核者全程不知情，「逃逸沙箱就升级」只对文件系统成立（spec §2.2 记了取舍，§6 V-3 要求文件系统那条单独实证，不许沿用）；④任务级 `CODEX_HOME` 只隔离掉用户 `AGENTS.md`/`hooks.json`/MCP server，**隔离不掉 codex 内置插件与 skills**（每任务多下 28MB）。另有一次**探针作废重跑**：验「协议参数能否压过用户 config」的首版命令走了机器上的代理变量，报 `Failed to connect to 127.0.0.1 port 1087`，在触及沙箱之前就先挂了、毫无判别力；加 `env -u *_proxy` + `--noproxy '*'` 重跑并补宿主机控制组（同命令直接跑返回 200），才拿到 `curl: (6) Could not resolve host` 这个有效结论 | 领于 08-09。来源：08-09 用户需求「我想把 codex 也加上」。**home 方案中途改过一次**：初版按 B2/B3 惯例定任务级 `CODEX_HOME` + 抽 `internal/authsync` 复用 B26 的写回机制，用户看到实证后改为**复用用户级 `~/.codex`**（executor 一般跑在专用开发机，把污染源卸掉即可）。这一改是净收益：**B26 那一整类问题在 codex 上架构性消失**（凭据只有一份权威副本，无软链、无写回、无巡检，authsync 抽取整个不需要），每任务再省 28MB 与一段插件下载延迟。关键前提是 spike 实证了**协议级参数压得过用户 config**（真实 home 基线 `networkAccess:true`，`turn/start.sandboxPolicy` 传 `false` 后 DNS 真被掐），所以安全判据由 handoff 代码钉死、不靠开发机干净；开发机只需清理影响**行为**的 `AGENTS.md`/`hooks.json`/`mcp_servers`（`hooks.json` 没有协议级开关能关，是已知软肋，写进 README 前置条件）。与 B2/B3 的另一处结构性差异是配置隔离不再依赖 home 隔离——grok 那边 `config.toml` 就是 home。**明确接受的行为差异**（spec §2.1）：审批档选 `on-request` 让 OS 沙箱当筛子，于是工作区内的 `rm -rf` 不再进黑名单（删的是本任务分支自己的成果，git 可救）；选它而不选 `untrusted` 是因为后者每条 shell 命令一次审批往返，一个真实任务几十次第 1 层模型调用，延迟叠加不可接受，方向上也与 B27 探针结论同向。另记 `item/commandExecution/requestApproval` 的 payload 带**结构化 `commandActions`**（`{"type":"read","path":…}`），比 claude/grok 那边对展示文本做正则可靠得多，但改判据是跨三个 adapter 的事，本期非目标 |

## 待验证的空白

- ~~**opencode 的 `external_directory: "ask"` 是否真拦得住绝对路径写入**~~ —— **08-09 B23/B27 探针实证，空白已填，且推翻了 B27 对 opencode 的前提**。生产配置（`edit: "allow"`、`external_directory: "ask"`）下真机实测：工作树**内**的 Write **不产生任何权限事件**、文件直接写成（这已经是 B27 想要的自动放行）；工作树**外**的绝对路径 Write 触发 `permission: "external_directory"`、**文件未被创建**、任务停在待答复（这已经是 B27 想要的升级人工）。结论：opencode 的越界写入早已被 `external_directory` 拦住，B27 对 opencode 的覆盖由它承担，`edit` 因此**保持 `allow` 不变**——原计划要把它翻成 `ask`，那只会给每一次范围内的正常编辑加一道「判完还是自动放行」的空门，噪音为正、安全收益为零。规则表实际改动只落在 claude（Write/Edit 移出 allow 进 ask）与 grok（`allowRules` 置空）。详见 [探针结论 §3.1](plans/2026-08-09-permission-payload-probe.md)。
- ~~**分级审批链第 1 层（廉价模型审批者）至今零生产验证**~~ —— **08-09 B19 派发中首次实证，空白已填**。三条权限各走一条路径：`mkdir -p /tmp/b19check/env && printf ... > config.yaml` 由第 1 层模型裁决**自动批准**（9.9s）；`pkill -f 'b19check'; pkill -f 'agentd'; rm -rf /tmp/b19check` 被黑名单硬规则**1ms 内升级人工**（没浪费模型调用），人工拒绝——该命令会杀掉承载该任务自身的生产 agentd；`rm -rf /tmp/b19check` 升级人工后批准。结论：第 1 层确有价值，它在「编码任务偶尔要碰任务目录外的临时文件」这类请求上真的挡住了一次唤醒；而真正危险的命令走的是它前面的黑名单，不是模型。三期「上百次调用 0 次权限」的观察依然成立——触发面窄，但触发时该链路工作正常。
- ~~**B19 的注入对 B2/B3 两个 adapter 尚未落地**~~ —— **08-09 两边都已实现并真机实证，空白已填**。B2（Claude Code）经 `run_claude.sh` 注入，真机 `HANDOFF_ENV_PROBE=ok` 由模型自己回读确认；B3（grok）经 `run_grok.sh` 注入，agentd 日志 `注入 env 变量到 grok serve 进程 keys=[https_proxy http_proxy NO_PROXY no_proxy] count=4`（只打 key 名不打值，符合凭据纪律）。三个 adapter 进程模型不同但都走 `executor.StartReq.Env` 同一契约。
