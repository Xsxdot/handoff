# B374 plan 节点台账

节点：plan（上游 spec `docs/specs/2026-09-15-b374-rooms-pagination.md` 已批准；冻结契约 `docs/superpowers/specs/b374-contract.md`；拆解稿 `docs/superpowers/specs/b374-breakdown.md` 已拍板 P-1..P-6 全选甲）。
卡：B374　工作分支：`cards/B374-charter-6`　基点：`d73f993b`（= origin/cards/B374-charter-5 头部）。
一行一条历史读数；命令与原始输出照抄，不做二次加工。

## 0. 落点

1. 2026-09-16：产出物写 `docs/superpowers/plans/b374-plan.md`（法定路径）。plan 只写计划与判据，不写实现代码、不建脚手架。
2. 2026-09-16：`git status --short --branch` → `## cards/B374-charter-6`（工作树干净）；`git rev-parse HEAD` → `d73f993b3869d9033c9ad9121a8690ccdf8f5330`。`git log --oneline -3` 头部为 `d73f993b breakdown(B374): 拍板 P-1..P-6 全选甲`，确认起点是拍板提交而非「待拍板」稿。
3. 2026-09-16：`git fetch origin cards/B374-charter-5` → `d73f993b breakdown(B374): 拍板 P-1..P-6 全选甲`；`git merge-base HEAD origin/cards/B374-charter-5` → `d73f993b3869d9033c9ad9121a8690ccdf8f5330`（本机分支无漂移，无需 rebase）。

## 1. 基线复核（判据先在基线跑）

4. 2026-09-16：`go build ./...` → 退出码 0，无输出。
5. 2026-09-16：`go test ./internal/agentd/ -run 'TestRooms' -count=1` → `ok  github.com/Xsxdot/handoff/internal/agentd  3.376s`。
6. 2026-09-16：`go test ./internal/collab/ ./internal/logx/ -count=1` → `ok  github.com/Xsxdot/handoff/internal/collab  3.033s`、`ok  github.com/Xsxdot/handoff/internal/logx  0.002s`。
7. 2026-09-16：P-5 事实（默认改 warn 会打红既有测试）：`HANDOFF_LOG_LEVEL=warn go test ./internal/logx/ -run TestSetupWritesJSONToFile -count=1` → `--- FAIL: TestSetupWritesJSONToFile (0.00s)`、`logx_test.go:24: 文件日志缺少消息:`、`FAIL`。
8. 2026-09-16：web 依赖安装：`npm ci --no-audit --no-fund`（workdir `web/`）→ `added 290 packages in 2s`。
9. 2026-09-16：web 全量测试：`npx vitest run`（workdir `web/`）→ `Test Files  124 passed (124)`、`Tests  1328 passed (1328)`、`Duration  18.46s`（另有 `Not implemented: HTMLCanvasElement's getContext()` 提示，为既有 jsdom 噪声，不影响通过）。
10. 2026-09-16：web 类型检查：`npx tsc -b`（workdir `web/`）→ 退出码 0，无输出。
11. 2026-09-16：`npm ping` → `npm notice PONG 823ms`（npm registry 可达，证明上条 `npm ci` 可复现）。
12. 2026-09-16：`web/node_modules` 被 `.gitignore` 忽略（`git check-ignore web/node_modules` → `web/node_modules`）。

## 2. 图查证

13. 2026-09-16：`codegraph sym n_agentd_Server_handleRoomsList --repo .` → `anchor=moved`、`container=k_agentd_Server`、`domain=d_coordination_api`、`file=internal/agentd/roomsapi.go:93`。
14. 2026-09-16：`codegraph sym n_collab_Service_ListRoomsPage --repo . --view cards-B374-charter` → `anchor=ok`、`container=k_collab_Service`、`domain=d_collab`、`file=internal/collab/service.go:333`。
15. 2026-09-16：`codegraph sym n_collab_trimRoomPage --repo . --view cards-B374-charter` → `anchor=ok`、`container=k_collab_fn`、`domain=d_collab`、`file=internal/collab/service.go:397`。
16. 2026-09-16：图覆盖债：不带 `--view cards-B374-charter` 时 `codegraph sym n_collab_Service_ListRoomsPage` 报「不在图中」（Ticket 0 新符号）；`codegraph sym n_logx_parseLevel` → `domain=d_runtime_config`（baseline 残留 id，`best.json` 归 `d_policy`，契约 §9 C-4 已记）。两者均不归本卡修。
17. 2026-09-16：`codegraph validate --repo . --view cards-B374-charter` → `issues: null`、`containers: 300`、`edges: 5507`、`nodes: 4499`。
18. 2026-09-16：`codegraph check --repo .` → `fails: []`（warns 为既有 `anchor-off-domain` / `best-dangling k_collab_model`，非本卡引入）。

## 3. 现状读数（代码事实锚）

19. 2026-09-16：`internal/agentd/roomsapi_test.go` 中 `/api/rooms` 列表请求实读 14 处：无参 12 处（`":/api/rooms\""` 计数）+ `?project=` 2 处，行号 87、123、168、185、211、254、309、325、375、396、411、457、623、859。其中 623 是 `TestRoomsEndpoints503WithoutLedger`（未装配 503，`withRooms` 先返回）→ **保持无参**；其余 13 处 F9 落地后会命中 426 → 改 `limit=50`。
20. 2026-09-16：`internal/agentd/roomsapi.go` 现状 `s.log.Info` 高频项：`:114`（会话列表响应成功）、`:205`/`:211`（逐房间 attach 投影）、`:218`（attach 投影完成）、`:278`/`:283`（后台刷新逐 task/逐轮）；`s.log.Warn`/`Debug` 分支不涉及降级。
21. 2026-09-16：`internal/collab/service.go:404` `listRooms`：`:415` 起省略号 rooms 切片、`:433-438` 追加 `project:<p>` 群与 `global`、`:499-513` active/sunk 分流 + `SliceStable` 活动降序 + `append(active, sunk...)`。群房间 `LastActivity` 为零值 → 沉 active 段末尾。
22. 2026-09-16：`internal/collab/room/room.go:166` `IsTerminalStatus` = `status == "已完成" || status == "终止"`；`service.go:426` `ReadOnly: isTerm <= (c.Following != "")`。
23. 2026-09-16：`internal/logx/logx.go:31` `Setup`、`:33` stderr TextHandler、`:34-36` 文件 JSONHandler 双挂、`:46` `parseLevel` 默认 `slog.LevelInfo`、`:59-100` `multiHandler` 四方法（单写改造后应删，基线 `grep -rn multiHandler internal/` 仅命中 logx 自身）。
24. 2026-09-16：`internal/agentd/mirror.go:34` `mirrorDiscoveryTick = 30 * time.Second`、`:48` `mirrorDiscoverBudget = 3 * time.Second`、`:130` `Run` 周期调 `:183 discoverOnce`（`:186` `fanCtx := context.WithTimeout(ctx, mirrorDiscoverBudget)` 对全部 target 共享一个预算）。
25. 2026-09-16：`internal/ledger/tasks.go:62` `func (s *Store) AllTaskLinks() ([]TaskLink, error)`（全量读 `card_tasks`，按 `target, task_id` 排序）。
26. 2026-09-16：`web/src/api/rooms.ts:85` 现状 `fetchRooms = (project = ''): Promise<RoomSummary[]>`，`:88` `.then((response) => response.rooms ?? [])` 只解包数组。
27. 2026-09-16：`web/src/app/rooms/RoomPanel.tsx:204` `usePoll(loadRooms, COLLAB_POLL_MS)`、`:171-181` `loadRooms`、`:354` 列表容器 `<div className="min-h-0 flex-1 overflow-y-auto p-2">`、`:355` `visible.map(...)`；`usePoll`（`web/src/app/data/usePoll.ts:28`）立即首拉 + `setInterval` 续拉。
28. 2026-09-16：`web/src/app/rooms/RoomPanel.test.tsx` 中 `vi.mocked(fetchRooms).mockResolvedValue(...)` 共 18 处（含 `beforeEach`）；`Shell.test.tsx:53` 1 处 mock；`pollInterval.test.tsx` 与 `rooms.fetch.test.ts` 各用 mock 但不返回数组语义。

## 4. 判断与设计决定

29. 2026-09-16：T4 轮转 handler 用「共享 `rotationState`（io.Writer + 轮转）+ 内部 JSONHandler 转发属性」形态，而非 Ticket 0 式的 `multiHandler`。理由：`Setup` 末尾 `.With("component", component)` 走 `WithAttrs`，若 handler 不转发属性则 `TestSetupWritesJSONToFile` 会因缺 `"component":"test"` 变红（脚手架阶段已实测该断言存在）。
30. 2026-09-16：T2 分页测试夹具一律 `Project: ""` 并用 `cardIDs()` 过滤 card 房间，因为 `listRooms` 恒追加 `global`（有 project 时还有 `project:<p>`），精确条数断言若不屏蔽群房间会误红。
31. 2026-09-16：T4 `rotate_test.go` 直接调 `newRotatingHandler` 属**内部锁**（合法理由：轮转阈值 100MB，从 `Setup` 缝构造需真实写 100MB）；T4 另有经 `Setup` 缝的 F19/F20 断言，不靠它顶替。已在 plan §11 自我声明。
32. 2026-09-16：T3 web 依赖已在本节点装好（条目 8），plan 写明缺依赖时 `npm ci`；未跑过的 web 命令不许写成绿，环境不允许安装则标「未验证」。
33. 2026-09-16：错误分流三路按协调者约束落 plan：parse 错直回 400；仅 `ErrInvalidCursor` 经 `roomsListErrorStatus` → 400；其余 → 500。426 文案冻结原文写入 `roomsListLegacyMessage`。
34. 2026-09-16：欠账 8 只做机内通路独立性结论（镜像发现与列表刷新无共享锁/goroutine/远程调用），不改 `mirror.go`（P-2）。欠账 9 按 P-1 甲，在 `b156.2.8-plan.md:1449` 与 `b156.2-breakdown.md:39` 原文后追加废止注。

## 5. 产出自检

35. 2026-09-16：plan 全文无占位符：`grep -n 'TBD\|TODO\|同 Task\|适当的错误处理' docs/superpowers/plans/b374-plan.md` 无命中（形态例外已在 §11 自我声明）。
36. 2026-09-16：跨 task 签名一致性：`ListRoomsPage(project, member, pageCursor string, limit int) (proto.RoomsPage, error)` 在 T1 Consumes 与 T2 Produces 逐字一致；`RoomsPage` Go/TS 字段 `rooms`/`next_cursor`/`has_more` 逐字一致；两处 limit 常量均 50/200 并各带断言。
37. 2026-09-16：本回合未碰 handoff CLI、未起新 executor、未派发。产出物为计划文档，不含实现代码。
38. 2026-09-16：提交事实。`git add docs/superpowers/plans/b374-plan.md docs/superpowers/ledgers/2026-09-16-b374-plan-ledger.md && git commit -m "docs(B374): plan for pagination, attach refresh scoping and log triple"` 原始输出：`[cards/B374-charter-6 672ac742] docs(B374): plan for pagination, attach refresh scoping and log triple`、`2 files changed, 1726 insertions(+)`。本条随手 amend 收进同批提交；amend 会换 hash，不回写、不 chase，收口判据是工作树干净。
