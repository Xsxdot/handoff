# B233.4 plan 台账

节点：charter/plan  
卡：B233.4  
分支：cards/B233.4-charter-3  
法定产出：docs/superpowers/plans/b233.4-plan.md  
规则：每确立一个事实立刻追加一行；提交事实记命令与原始输出，随后只 amend 一次。

---

## 2026-09-07 开工

- 角色：plan 节点。只写计划，不写实现代码、不建脚手架。
- 拍板：breakdown §0 P1–P5 全数 (a)，提交 `65476f0b`。plan 不得把未选方案写成事实。
- 轻档：单卡单轮 implement，禁止扇出子卡。内部 ticket T1–T6 是执行顺序。
- Ticket 顺序：T1→T2→T3/T5→T6；T1/T4 可与 T2 并行写在同一 plan 里。push 归属仍暂缓。
- HEAD `65476f0bae0e3035add3afc82d5fda15a81c6436`，分支 `cards/B233.4-charter-3`。拍板提交在 HEAD。
- 有效基线分支：`cards/B233.1-charter-7`。
- 有图：`codegraph/` 在仓内。视图 `cards-B233.4-charter`。
- `codegraph --repo . --view cards-B233.4-charter check`：`fails: []`；`warns` 145（存量 oversized / prefix-family / anchor-off-domain，本卡不修）。
- 实然预算：`d_orchestration→d_workspace` 18 / 预算 19；`d_gateway→d_workspace` 1/1；`d_cli→d_workspace` 9/9；`d_workspace→d_protocol` 3/3；`d_workspace→d_ledger` 无边（禁止）。
- `internal/workspace` 生产 import 仅 `context fmt reflect regexp`，不含 proto/ledger。生产 agentd 除测试外零 import workspace。
- Ticket 0 空壳在：`internal/workspace/{capability,identity,retain}.go`。`MayRecycle` who-calls 无生产边（unscannedEntries=5）。
- 签名（图 `sym`，行号本轮读数）：`PrepareWorkspace` workspace.go:418；`ResolveBaseline` :1241；`RemoveManagedWorktree` :889；`DiffRange` :1449（无 ctx，内部 `context.Background()`）；`ReadFile` :1789；`taskDiffTarget` :2833；`id8` :1414；`taskBranch` :1422；`CreateManualWorktree` manualworktree.go:87；`Manager.Dispatch` manager.go:721；`Done` :1522；`Stop` :1640 仍删 managed 树；`compensateWorkspace` :1185；`Reclaim` reclaim.go:251；`GC` gc.go:36；`NewManager` manager.go:308 签名不改；组装点 `cmd/agentd.go:209` + `Server.SetManager` server.go:365。
- flow：Dispatch steps=95 degraded=false；Stop 28；Done 32；Reclaim 30；GC 31；CreateManualWorktree 50。PrepareWorkspace / compensateWorkspace / MayRecycle / handleTaskDiff **degraded=true steps=[]**，已读源码。禁止用 chain 冒充。
- who-calls：PrepareWorkspace ← Dispatch / PrepareBranch；RemoveManagedWorktree ← Stop/Done/compensate/Dispatch 补偿链；CreateManualWorktree ← handleProjectWorktreeCreate；DiffRange / taskDiffTarget ← handleTaskDiff；SetCardBaseBranch 短名拒，节点 `n_ledger_Store_SetCardBaseBranch`。
- 现状行为：Dispatch 直调包级 `EnsureRepoUsable`/`ResolveBaseline`/`PrepareWorkspace`；Stop 在 transit(failed) 后 `RemoveManagedWorktree` 成功则 `worktreeRemoved=true`；Done 归档后删 managed；Reclaim 先 `IsTerminal` 再 git remove（不走 RemoveManagedWorktree，有 --force/prune）；GC 非终态 continue、非 managed skip、不扫 `worktrees/manual`。
- `cmd/wait.go:428` 第三套 `id8`（通知短 id，同样前 8 字节）。breakdown T1：发现则本票收口。
- CLI `cmd/stop.go:38-40`：removed true 打印「已删除」；false 打印「分支与 worktree 保留」（不说留存）。Web `Composer.tsx:167`：false 文案含「或清理失败」。
- 库事实出处：`WorkspaceGitTimeout=2m` workspace.go:113；`FetchTimeout=2m` :925；`removeWorktreeAttempts=5` / `backoff=400ms` :864-865；`gitExec` 空 `core.hooksPath` :128-150；`id8` 按字节截断 :1414-1418。
- 基线测试（亲自跑，HEAD `65476f0b`，未改实现）：
  - `go test ./internal/workspace -count=1` ok 0.558s
  - `go test ./internal/agentd -run 'TestID8MatchesWorkspaceCanonical|TestBaseCommitReMatchesWorkspaceIsCommitSHA' -count=1` ok 0.575s
  - `go test ./internal/agentd -run 'TestStopRemovesManagedWorktree|TestStopReportsWorktreeRemoved|TestDoneRemovesManagedWorktree|TestDoneKeepsUserWorktree|TestDispatchFailedAfterWorkspaceCleansManagedWorktree|TestTaskDiffTargetKeepsWorktreeWhenPresent|TestTaskDiffTargetFallsBackToRepoWhenWorktreeGone|TestTaskDiffTargetKeepsWorktreeWhenBranchUnknown|TestPrepareWorkspaceNewWorktreeAllowsDirtyMainRepo|TestPrepareWorkspaceMutualExclusionAndInjection|TestReclaimRefusesNonTerminal|TestReclaimRefusesNotManaged' -count=1` ok 2.073s
  - `go test ./internal/client -run 'TestStopParsesWorktreeRemoved' -count=1` ok 0.527s
  - `go build -o /dev/null .` 退出 0，stdout 空
  - `go test ./cmd -run 'Stop' -count=1` ok 0.615s（无既有 stop CLI 用例）
- 判断：今日从声明缝得不到本卡要锁的结果——Dispatch 不经 Capability；Stop 仍删 managed 树（`TestStopRemovesManagedWorktree` 把删树锁成绿）；Done/Reclaim/GC 未问 `MayRecycle`；`NewResultRef` 无生产调用。Ticket 0 空壳不是活路径。DAG 点亮活路径的是 T2；T1 先收口唯一截断规则（今日同值，写下去对齐测试仍绿）。T3 退役旧 Stop 删树正向。
- P1–P5 全部按 (a) 落笔。禁止 P1-b 整包搬家、P2-c nil 降级包级函数、P4-b 新 Task 字段、P3-b 只改 CLI。
- 未调 handoff CLI；未写实现代码。
- 计划已落盘 `docs/superpowers/plans/b233.4-plan.md`。T1–T6 按拍板 (a)。内部锁：T1 对齐测试、T4 assembleResultRef 空 commit（已声明）。真机清单归协调者。
- `codegraph --repo . --view cards-B233.4-charter resolve --doc docs/superpowers/plans/b233.4-plan.md`：anchors 28，`ok` 20 / `moved` 8，无 unresolved。
- 自审：未调 handoff CLI；未写实现代码；基线命令均亲自跑到退出码。

## 提交（历史读数，随后 amend 一次）

```
git commit -m "docs(B233.4): 实现计划（工作区与 Git 能力独立）"
[cards/B233.4-charter-3 bd9a1875] docs(B233.4): 实现计划（工作区与 Git 能力独立）
 2 files changed, 1366 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.4-plan-ledger.md
 create mode 100644 docs/superpowers/plans/b233.4-plan.md
```
