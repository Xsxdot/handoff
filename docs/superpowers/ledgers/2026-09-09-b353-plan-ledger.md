# B353 plan 台账

日期：2026-09-09
卡：B353（并入 B354 / B355）
节点：plan
工作分支：`cards/B353-charter-3`
开工 HEAD：`d8765663`（`docs(B353): 协调者拍板 P1-A、P2-A、P3-B`）

## 过程

1. 本节点只写法定计划 `docs/superpowers/plans/b353-plan.md` 与本台账；不写实现代码、不建卡、不派发、不调用 handoff CLI、不起新的 executor。
2. 输入状态来自仓内文件：`docs/superpowers/specs/b353.md` 头部为“已批准（2026-09-09，用户原话「推进吧」）”；`docs/superpowers/specs/b353-contract.md` 头部为“已冻结（本提交冻结）”；`docs/superpowers/specs/b353-breakdown.md` 头部为“已拍板（2026-09-09；协调者按用户「同意」落点，不改产品承诺）”。
3. 有效实现基线仍是 spec/contract 声明的 `cards/B233.1-charter-7 @ 48f2736a`；本节点只在当前 `cards/B353-charter-3` 工作，不切分支、不越过基线。

## 代码图与源码核对

4. 仓内存在 `codegraph/`，PATH 中命中 `/root/.local/bin/codegraph`；本节点按平台不变量使用已安装 `codegraph` 查询。
5. `codegraph sym n_cmdrunCardWait` 命中 `cmd/card_wait.go:48`，现状签名为 `func runCardWait(cmd *cobra.Command, cardID string, subtree bool, timeout time.Duration) error`；`codegraph who-calls n_cmdrunCardWait` 命中 `cardWaitCmd.RunE`，并警告基线仍有 5 个未扫描入口。
6. `codegraph sym n_client_Client_FollowEvents` 命中 `internal/client/client.go:1751`，现状签名为 `func (c *Client) FollowEvents(ctx context.Context, taskID string, all bool, idle time.Duration, onEvent func(*proto.Event) error, onBacklog func(*BacklogSummary) error) error`；flow 为 `degraded=false`、29 steps；who-calls 命中 `cmd/wait.go#runFollow`，并警告基线仍有 5 个未扫描入口。
7. `codegraph sym n_ledger_Store_Follow` 命中 `internal/ledger/follow.go:18`，现状签名为 `func (s *Store) Follow(ctx context.Context, members func() ([]string, error), fromSeq int64, pollInterval time.Duration, onEvent func(Event) error) error`。
8. `codegraph sym n_agentd_automationWakeEvent` 命中 `internal/agentd/wakeconsumer.go:130`，现状签名为 `func automationWakeEvent(ev proto.LedgerEvent) (keystone.WakeEvent, bool, error)`；flow 输出 `degraded=true`、`steps=[]`；who-calls 只返回自身节点并警告 5 个未扫描入口，因此其调用面以源码核对。
9. `codegraph sym n_agentd_Server_consumeAutomationEventsOnce` 命中 `internal/agentd/wakeconsumer.go:185`，现状签名为 `func (s *Server) consumeAutomationEventsOnce(ctx context.Context) (processed int, escalated bool, err error)`；flow 输出 `degraded=true`、`steps=[]`；who-calls 只返回自身节点并警告 5 个未扫描入口，因此其调用面以源码核对。
10. `codegraph context d_cli` 命中 best 领域 `d_cli`，响应 `truncated=false` 但 `fociTruncated.total=86, shown=5`；上下文响应显示现状容器有 `d_protocol` 归属，计划不把 context 邻域当流程图。其它直接领域以 breakdown 已核对的 best 顶层域与源码为准。
11. 图覆盖债：`WaitDeliveryPolicy` 未命中独立节点，生产实现仍以 `internal/client/delivery.go#WaitDeliveryPolicy` 为唯一权威；不把 `isDeliverable` 当第二策略源。

## 基线命令与原始输出

12. 命令：`go test ./internal/client -run 'Test(B353|WaitDeliveryPolicy|WaitEvent|Follow)' -count=1`

    原始输出：`ok  	github.com/Xsxdot/handoff/internal/client	7.871s`；退出码 0。

13. 命令：`go test ./cmd -run '^TestCardWaitSubtreeExitsWhenAllDone$' -count=1`

    原始输出：`ok  	github.com/Xsxdot/handoff/cmd	2.155s`；退出码 0。

14. 命令：`go test ./internal/agentd -run 'Test(Automation|B2336)' -count=1`

    原始输出：`ok  	github.com/Xsxdot/handoff/internal/agentd	3.216s`；退出码 0。

15. 命令：`go test ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353|WaitDeliveryPolicy|Follow|CardWait|Automation)' -count=1`

    原始输出：`ok  	github.com/Xsxdot/handoff/internal/client	4.954s`、`ok  	github.com/Xsxdot/handoff/internal/ledger	1.046s`、`ok  	github.com/Xsxdot/handoff/internal/agentd	2.175s`、`ok  	github.com/Xsxdot/handoff/cmd	2.171s`；退出码 0。

16. 命令：`go test -race ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353)' -count=1`

    原始输出：`ok  	github.com/Xsxdot/handoff/internal/client	1.012s [no tests to run]`、`ok  	github.com/Xsxdot/handoff/internal/ledger	1.011s [no tests to run]`、`ok  	github.com/Xsxdot/handoff/internal/agentd	1.025s [no tests to run]`、`ok  	github.com/Xsxdot/handoff/cmd	1.020s [no tests to run]`；退出码 0。该读数只证明基线命令可运行，不证明本卡行为。

## 判断

17. 计划采用 breakdown 已拍板的单张跨域实现卡内部 DAG：T0 边界核验 → T1 任务 wait/follow 唯一策略 → T2 card wait → T3 wakeconsumer → T4 三链穿缝回归与 skill/README 同步。T2 与 T3 共享 T1 产物，可在 T1 后受控并行；T4 串行收口。
18. 计划的测试必须穿过三条声明缝：`Client.WaitEvent`/`Client.FollowEvents`，`runCardWait`/`Store.Follow`/stdout encoder，`Server.consumeAutomationEventsOnce`/fake Keystone 入口；只测 `WaitDeliveryPolicy` 或 `automationWakeEvent` 的内部锁不能替代缝级断言。
19. 真实 executor 事件、PG LISTEN、WS/relay、跨机/重启、宿主 Monitor 逐行唤醒与多平台行为仍按 breakdown §5 标为“未验证，需真机”，不在计划的本机测试结论中冒充已验。

20. 命令：`rg -n -C 5 'card wait|一次工作流只挂一次|delivery_failed|permission_auto_allow|decision_answered|--follow|一次一挂' skills/handoff/SKILL.md README.md`。原始结果定位到：README 当前已有 `wait --follow`/四种 harness 说明、任务侧 `delivery_failed`/`stalled`，但没有 card wait 的新退出/过滤说明；skill `card wait` 当前命令为 `handoff card wait <id> [--subtree] [--timeout 3h]`，当前文案是“一次工作流只挂一次”、长挂到终态且只跳 mirrorSkip 三项。该现状必须由 T4 按 P2-A 同批同步。

21. 命令：`go build ./...`；原始输出为空，退出码 0。
22. 命令：`git diff --check`；原始输出为空，退出码 0。
23. 命令：`codegraph resolve --repo . --doc docs/superpowers/specs/b353-breakdown.md`；原始输出返回 14 个 anchors，均为 `anchor: ok` 或 `anchor: moved`，退出码 0；`WaitDeliveryPolicy`、`runCardWait`、`Client.WaitEvent`、`Client.FollowEvents`、`Store.Follow`、`automationWakeEvent`、`Server.consumeAutomationEventsOnce`、`Service.Decide`、`EventType`、`RoomMessage` 均可解析。

24. 计划文档已落在法定路径 `docs/superpowers/plans/b353-plan.md`，内容覆盖 T0→T1→T2/T3→T4、精确接口、三条声明缝、序列化边界、日志/注释动作、缺陷族对抗审查和真机未验证清单；未修改运行时代码。
25. 计划自审后修正两处实现歧义：默认 `card wait` 与 follow 继续复用 `allDone`，但收尾日志按 `follow` 区分；`RoomMessage.by_system` 通过 raw JSON 区分缺失/false/true/null，并在 wakeconsumer consumer seam 给出完整错误/唤醒形状。
26. 命令：`rg -n 'TBD|TODO|同 Task N|适当错误处理|另写测试|必要的错误|待补|占位符|cardWaitFollow|必要的' docs/superpowers/plans/b353-plan.md`；原始输出只命中 CLI flag 的合法变量/声明和“占位符扫描无命中”自审文字，没有计划占位内容；退出码 0（因命中合法文本）。
27. 命令：`git diff --no-index --check /dev/null docs/superpowers/plans/b353-plan.md`；原始输出为空，随后检查命令退出码 0。
28. 命令：`/root/.local/bin/codegraph resolve --repo . --doc docs/superpowers/plans/b353-plan.md`；原始输出 5 个 anchors：`runCardWait`/`runFollow` 为 `ok`，`WaitDeliveryPolicy`/`isDeliverable`/`runLedgerCLI` 为 `moved`；退出码 0。
29. 命令：`if rg -n 'TBD|TODO|同 Task N|适当错误处理|另写测试|必要的错误|待补' docs/superpowers/plans/b353-plan.md; then exit 1; else exit 0; fi`；原始输出为空，退出码 0，未命中占位内容。
30. 命令：`git add docs/superpowers/plans/b353-plan.md docs/superpowers/ledgers/2026-09-09-b353-plan-ledger.md && git diff --cached --check && git diff --cached --name-only && git diff --cached --stat && git status --short --branch`；原始输出依次为两条文件名、`.../ledgers/2026-09-09-b353-plan-ledger.md | 65 ++`、`docs/superpowers/plans/b353-plan.md | 802 +++++++++++++++++++++`、`2 files changed, 867 insertions(+)`，状态为当前分支且两文件均 `A`；退出码 0。暂存内容只有计划与台账，无实现文件。
31. 命令：`git add docs/superpowers/ledgers/2026-09-09-b353-plan-ledger.md && git diff --cached --check && git diff --cached --name-only && git diff --cached --stat`；原始输出为两条文件名、台账 `66 ++`、计划 `802 +++++++++++++++++++++`、`2 files changed, 868 insertions(+)`；退出码 0。
32. 提交命令：`git commit -m "docs(B353): write wake consumer implementation plan"`；原始输出：`[cards/B353-charter-3 85ac7ee0] docs(B353): write wake consumer implementation plan`、`2 files changed, 869 insertions(+)`、创建本台账和 `b353-plan.md`；随后按纪律只 amend 一次收录本提交事实，故该 hash 会变化且不回写追逐。

33. T0 实际核验：`git status --short --branch` 输出 `## cards/B353-charter-4`；契约 resolve 命令为 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b353-contract.md`，实际返回 10 个 anchors，状态均为 `ok` 或 `moved`，退出码 0。
34. T1 追加接缝测试后运行 `go test ./internal/client -run 'TestB353|TestWaitDeliveryPolicy|TestWaitEvent|TestFollow' -count=1`；原始输出：`ok  github.com/Xsxdot/handoff/internal/client  7.976s`；退出码 0。生产代码未改，现有唯一 `WaitDeliveryPolicy` 已满足新增策略、WaitEvent 与 FollowEvents 断言。
35. T2 首红命令：`go test ./cmd -run '^TestB353CardWait|^TestCardWaitSubtreeExitsWhenAllDone$|^TestWaitRejectsCardFlag$' -count=1`；原始失败包含 `unknown flag: --follow`，旧默认 stdout 实际含 `comment`、`needs_human`、`needs_cleared`、`status_moved` 四行，帮助缺少 `--follow`；另发现存量子树测试把 status_moved 当 stdout，已按冻结契约改为核对账本终态。
36. T2 实现后运行同一命令；原始输出：`ok  github.com/Xsxdot/handoff/cmd  7.691s`；退出码 0。已验证 card wait 默认首个可动作退出、follow 过滤并持续到终态、两种 timeout 为 124、帮助 flag 与存量子树终态接缝。
37. T3 首红命令：`go test ./internal/agentd -run '^TestB353Automation|^TestAutomation|^TestB2336' -count=1`；原始失败为 `策略真事件未完整唤醒，processed=0` 与 `by_system null ... yes=true`，另测试夹具首次尝试的非终态状态迁移被工作流 gate 拒绝；修正夹具为同状态 status_moved 后进入实现。
38. T3 实现后再次运行同一命令；原始输出：`ok  github.com/XSxdot/handoff/internal/agentd  3.832s`；退出码 0。已验证 task_mirrored 复用 WaitDeliveryPolicy、四类 needs/decision、真人 room、系统/null room、同卡合并及坏 envelope consumer 错误缝。
39. T4 组合回归命令 `go test ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353|WaitDeliveryPolicy|Follow|CardWait|Automation)' -count=1`；原始输出：`ok  github.com/XSxdot/handoff/internal/client 5.075s`、`ok  github.com/XSxdot/handoff/internal/ledger 1.087s`、`ok  github.com/XSxdot/handoff/internal/agentd 2.486s`、`ok  github.com/XSxdot/handoff/cmd 7.541s`；退出码 0。
40. T4 race 命令 `go test -race ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353)' -count=1`；原始输出：`ok  github.com/XSxdot/handoff/internal/client 1.129s`、`ok  github.com/XSxdot/handoff/internal/ledger 1.018s [no tests to run]`、`ok  github.com/XSxdot/handoff/internal/agentd 1.882s`、`ok  github.com/XSxdot/handoff/cmd 6.671s`；退出码 0，无 data race 输出。
41. T4 全量编译命令 `go build ./...`；标准输出为空，退出码 0。
42. T4 文档 grep 命令 `rg -n 'card wait .*--follow|一次一挂|一次工作流只挂一次|delivery_failed|permission_auto_allow|decision_answered' skills/handoff/SKILL.md README.md`；实际命中 README/skill 的 card wait `--follow`、默认一次一挂、完整策略及 `delivery_failed`/`decision_answered` 文案，退出码 0。`git diff --check` 标准输出为空，退出码 0。
43. T4 计划图门禁命令 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/plans/b353-plan.md`；原始返回 5 anchors，均为 `ok` 或 `moved`，退出码 0。
44. 变异锚点唯一性核验命令 `rg -n -F 'return client.WaitDeliveryPolicy(proto.EventType(envelope.TaskType)), nil' cmd/card_wait.go`；原始输出唯一命中 `229: return client.WaitDeliveryPolicy(proto.EventType(envelope.TaskType)), nil`。首次删除式变异的 `go build ./...` 原始错误为 `cmd/card_wait.go:21:2: "github.com/Xsxdot/handoff/internal/client" imported and not used`，该发编译失败，不计入存活统计。
45. 改用编译有效的反转变异 `return !client.WaitDeliveryPolicy(proto.EventType(envelope.TaskType)), nil` 后，`go build ./...` 标准输出为空、退出码 0；行为命令 `go test ./cmd -run '^TestB353CardWaitFollowFiltersAndContinues$' -count=1` 原始失败为 `follow 最后一条 task_mirrored 应是可动作 delivery_failed，payload={"node":"node","attempt":"attempt","task_type":"permission_auto_allow","payload":{"rule":"safe"}}`，退出码 1。加强该断言后，相关范围命令 `go test ./cmd -run '^TestCardWait|^TestB353CardWait|^TestWaitRejectsCardFlag$' -count=1` 原始输出含唯一失败测试 `--- FAIL: TestB353CardWaitFollowFiltersAndContinues`，末尾为 `FAIL github.com/Xsxdot/handoff/cmd`，退出码 1；确认变异命中且测试有牙。
46. 恢复 `WaitDeliveryPolicy` 正向实现后，T2 范围命令 `go test ./cmd -run '^TestB353CardWait|^TestCardWaitSubtreeExitsWhenAllDone$|^TestWaitRejectsCardFlag$' -count=1` 原始输出：`ok github.com/Xsxdot/handoff/cmd 7.562s`；退出码 0。
47. 恢复后组合回归命令 `go test ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353|WaitDeliveryPolicy|Follow|CardWait|Automation)' -count=1` 原始输出：`ok github.com/Xsxdot/handoff/internal/client 5.073s`、`ok github.com/Xsxdot/handoff/internal/ledger 1.083s`、`ok github.com/Xsxdot/handoff/internal/agentd 1.864s`、`ok github.com/Xsxdot/handoff/cmd 7.481s`；退出码 0。
48. 恢复后 race 命令 `go test -race ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353)' -count=1` 原始输出：`ok github.com/Xsxdot/handoff/internal/client 1.150s`、`ok github.com/Xsxdot/handoff/internal/ledger 1.012s [no tests to run]`、`ok github.com/Xsxdot/handoff/internal/agentd 1.991s`、`ok github.com/Xsxdot/handoff/cmd 6.549s`；退出码 0，无 data race 输出。
49. 恢复后全量编译命令 `go build ./...`；标准输出为空，退出码 0。
50. 恢复后文档核验命令 `rg -n 'card wait .*--follow|一次一挂|一次工作流只挂一次|delivery_failed|permission_auto_allow|decision_answered' skills/handoff/SKILL.md README.md`；原始命中 README:383/429/434-436/440/446-447/661 与 SKILL:95-97/282/454/489/491/495-496，退出码 0。`git diff --check` 标准输出为空，退出码 0。
51. 恢复后计划图门禁命令 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/plans/b353-plan.md`；原始返回 5 anchors：`runCardWait`/`runFollow` 为 `ok`，`WaitDeliveryPolicy`/`isDeliverable`/`runLedgerCLI` 为 `moved`；退出码 0。
52. 收尾全量测试命令 `go test ./...`；进程最终退出码 1。原始失败行仅为 `--- FAIL: TestRepoContractGate (0.04s)`，其断言为 `契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）`、`契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）`，以及 `legacy 命中: ...，warn 147 条`；末尾为 `FAIL github.com/Xsxdot/handoff/cmd`。其余包在原始输出中均为 `ok` 或 `[no test files]`。另跑 `go test ./cmd -count=1` 过滤失败输出，原始同样只命中上述三行失败信息；本卡未修改 `cmd/graph_gate_test.go`、`codegraph/target.json` 或图配置，故该失败记录为既有图基线阻断，未在本卡范围修复。
53. 提交前核验：`gofmt -l cmd/card_wait.go cmd/card_wait_test.go internal/agentd/wakeconsumer.go internal/agentd/wakeconsumer_test.go internal/client/client_test.go internal/client/execution_test.go internal/client/follow_test.go` 原始输出为空、退出码 0；`git diff --check && git status --short --branch` 原始输出为空 diff 检查且 `## cards/B353-charter-4` 加 10 个预期修改文件、退出码 0；`rg -n 'fmt\\.Print|log\\.Print|console\\.log' cmd/card_wait.go internal/agentd/wakeconsumer.go` 原始输出为空、退出码 1（无命中）。
54. 暂存核验命令 `git add README.md cmd/card_wait.go cmd/card_wait_test.go docs/superpowers/ledgers/2026-09-09-b353-plan-ledger.md internal/agentd/wakeconsumer.go internal/agentd/wakeconsumer_test.go internal/client/client_test.go internal/client/execution_test.go internal/client/follow_test.go skills/handoff/SKILL.md && git diff --cached --check && git diff --cached --name-only && git diff --cached --stat`；原始输出为 10 个预期文件名、`10 files changed, 835 insertions(+), 49 deletions(-)`，diff check 无输出，退出码 0。
55. 提交命令 `git commit -m "fix(B353): filter card wake events"`；原始输出：`[cards/B353-charter-4 fc2c9303] fix(B353): filter card wake events`、`10 files changed, 836 insertions(+), 49 deletions(-)`；退出码 0。随后按纪律只 amend 一次收录本提交事实，故最终 hash 会变化且不回写追逐。

## 复审补充

56. 复审起点：`git status --porcelain=v1` 无工作树改动；`git log --oneline -1` 输出 `5440ebdc fix(B353): filter card wake events`。当前实现已具备 `status_moved` 非终态不编码和 task mirror `null` 保留形状，但 `startCardWaitIdle.noteActivity` 使用容量 1 的非阻塞通知，连续账本事件可能合并而漏刷新 idle；现有 wakeconsumer room JSON 边界测试直接调用 `automationWakeEvent`，缺少从 `consumeAutomationEventsOnce` 的完整调用方穿缝。
57. 复审新增测试首跑：`go test ./cmd -run '^TestB353CardWait(StatusMovedNonTerminalDoesNotEmit|PreservesNullAndRejectsMissingMirroredPayload|FollowIdleRefreshesEachLedgerEvent)$' -count=1` 与重复 20 次均输出 `ok github.com/Xsxdot/handoff/cmd`；`go test ./internal/agentd -run '^TestB353AutomationRoomJSONBoundaries(ThroughConsumer)?$' -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/agentd`。这些测试在当前实现先绿，未形成 idle 的首红；复审明确要求的容量 1 通知缺陷仍按代码审查事实修正，后续以组合回归和 race 验证。
58. idle 修正：`cmd/card_wait.go` 将 `startCardWaitIdle` 的容量 1 非阻塞 activity 通知改为无缓冲逐事件握手，并在 context 取消时退出发送；未改 `Store.Follow` 签名、轮询或退出模型。
59. T2 回归命令 `go test ./cmd -run '^TestB353CardWait|^TestCardWaitSubtreeExitsWhenAllDone$|^TestWaitRejectsCardFlag$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/cmd 12.924s`；退出码 0。
60. T3 回归命令 `go test ./internal/agentd -run '^TestB353Automation|^TestAutomation|^TestB2336' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 5.027s`；退出码 0。新增 room JSON 边界已穿过 `consumeAutomationEventsOnce`，不是仅 helper 直测。
61. T1 回归命令 `go test ./internal/client -run 'TestB353|TestWaitDeliveryPolicy|TestWaitEvent|TestFollow' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/client 7.833s`；退出码 0。
62. T4 组合回归命令 `go test ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353|WaitDeliveryPolicy|Follow|CardWait|Automation)' -count=1` 原始输出四包均 `ok`：client `5.027s`、ledger `1.119s`、agentd `3.731s`、cmd `13.065s`；退出码 0。
63. T4 race 命令 `go test -race ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353)' -count=1` 原始输出四包均 `ok`：client `1.133s`、ledger `1.013s [no tests to run]`、agentd `3.357s`、cmd `12.137s`；退出码 0，无 data race 输出。
64. 全量编译命令 `go build ./...` 标准输出为空，退出码 0。
65. 图契约核对：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b353-breakdown.md` 与 `...b353-contract.md` 均退出 0；返回的 anchors 全部为 `ok` 或 `moved`，本次改动未新增契约符号。
66. 变异锚点唯一性：`rg -n -F 'return client.WaitDeliveryPolicy(proto.EventType(envelope.TaskType)), nil' cmd/card_wait.go` 唯一命中 `229`。将其改为取反后 `go build ./...` 标准输出为空、退出码 0；随后 `go test ./cmd -run '^TestB353CardWaitFollowFiltersAndContinues$' -count=1` 真实失败，原始断言为 `follow 最后一条 task_mirrored 应是可动作 delivery_failed，payload={"node":"node","attempt":"attempt","task_type":"permission_auto_allow","payload":{"rule":"safe"}}`，退出码 1。该发变异编译通过且命中唯一，证明 card wait 策略反向测试有牙；已恢复正向实现。
67. 恢复后组合回归再次运行：`go test ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353|WaitDeliveryPolicy|Follow|CardWait|Automation)' -count=1` 原始输出 client `ok ... 5.044s`、ledger `ok ... 1.082s`、agentd `ok ... 3.166s`、cmd `ok ... 13.020s`，退出码 0。
68. 收尾全量测试 `go test ./...` 真实退出码 1；已完成包包括 root、agentd、client、ledger 等均输出 `ok`，cmd 包失败原因由独立命令复核为既有 `TestRepoContractGate`。`go test ./cmd -run '^TestRepoContractGate$' -count=1` 原始失败为：`契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）`；`契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）`；另有 `legacy 命中: ...，warn 147 条`，末尾 `FAIL github.com/Xsxdot/handoff/cmd`。本卡未改图门禁文件，故该失败未归因于本卡。
69. 复审补充的真实 CLI idle 穿缝 `go test ./cmd -run '^TestB353CardWaitFollowIdleRefreshesAuditLedgerEvent$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/cmd 5.111s`；退出码 0。该测试通过 `runLedgerCLI` 写入任意 comment 账本事件，验证 follow 的 idle 被刷新且 stdout 仍为空。
70. 最终组合回归 `go test ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353|WaitDeliveryPolicy|Follow|CardWait|Automation)' -count=1` 原始输出 client `ok ... 5.048s`、ledger `ok ... 1.057s`、agentd `ok ... 3.595s`、cmd `ok ... 18.152s`；退出码 0。
71. 最终 race `go test -race ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353)' -count=1` 原始输出 client `ok ... 1.136s`、ledger `ok ... 1.017s [no tests to run]`、agentd `ok ... 3.212s`、cmd `ok ... 17.531s`；退出码 0，无 data race 输出。
72. 最终 `go build ./...` 标准输出为空、退出码 0；`gofmt -l cmd/card_wait.go cmd/card_wait_test.go internal/agentd/wakeconsumer.go internal/agentd/wakeconsumer_test.go` 标准输出为空、退出码 0；`git diff --check` 标准输出为空、退出码 0；结构化日志禁用扫描命令无输出、退出码 0。
73. 最终文档核对 `rg -n 'card wait .*--follow|一次一挂|一次工作流只挂一次|delivery_failed|permission_auto_allow|decision_answered' skills/handoff/SKILL.md README.md` 命中 README 行 383、429、434–436、440、446–447、661 与 SKILL 行 95–97、282、454、489、491、495–496、730；退出码 0。最终计划图 resolve 返回 5 anchors，全部为 `ok` 或 `moved`，退出码 0。
74. 强化序列化断言后，`go test ./cmd -run '^TestB353CardWaitPreservesNullAndRejectsMissingMirroredPayload$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/cmd 2.110s`；退出码 0。缺失 payload 错误断言同时覆盖 card、seq、`task_mirrored`，null envelope payload 断言原始 JSON 仍为 `null`。
75. 最终源码变更后的组合回归 `go test ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353|WaitDeliveryPolicy|Follow|CardWait|Automation)' -count=1` 原始输出 client `ok ... 4.998s`、ledger `ok ... 1.072s`、agentd `ok ... 3.658s`、cmd `ok ... 18.046s`；退出码 0。
76. 最终源码变更后的 race `go test -race ./internal/client ./internal/ledger ./internal/agentd ./cmd -run 'Test(B353)' -count=1` 原始输出 client `ok ... 1.140s`、ledger `ok ... 1.010s [no tests to run]`、agentd `ok ... 2.926s`、cmd `ok ... 17.462s`；退出码 0，无 data race 输出。最终 `go build ./...` 标准输出为空，退出码 0。
77. 提交命令 `git commit -m "fix(B353): close wake consumer review gaps"` 原始输出：`[cards/B353-charter-5 c70fdd30] fix(B353): close wake consumer review gaps`、`5 files changed, 286 insertions(+), 3 deletions(-)`；退出码 0。随后只 amend 一次收录本提交事实，故最终 hash 会变化且不回写追逐。
