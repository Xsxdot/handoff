# C1.11 breakdown 台账

- 2026-08-25：读取 `/root/.codex/skills/handoff/SKILL.md` 完整文件（374 行）；本节点不调用 handoff CLI、不派发子任务。
- 2026-08-25：`git status --short --branch` 原始输出：`## cards/C1.11-charter-2`；当前工作树未显示未提交改动。
- 2026-08-25：`rg --files` 确认仓内存在 `codegraph/best.json`、C1.11 spec/contract 路径与 `docs/superpowers/ledgers/2026-08-25-c1.11-contract-ledger.md`，尚未发现 C1.11 breakdown 台账。
- 2026-08-25：`sed -n '1,12p'` 核对上游状态头：spec 原文含 `状态：已批准（2026-08-25，用户「做吧」）`；contract 原文含 `本文性质：契约冻结`，均已回写在文件头部。
- 2026-08-25：`jq '.domains | ... select(.value.parent == null)' codegraph/best.json` 实际输出 12 个顶层域；本卡实现入口归属读数为 `k_agentd_Server -> d_gateway`、`k_agentd_fn -> d_orchestration`，模块/换版方向归 `d_maintenance`，`d_protocol` 仅承接 roadmap 中的声明欠账记录。
- 2026-08-25：`jq '.containers | to_entries[] | select(.key|test("codegraph|agentd|server|main|desktop";"i"))' codegraph/best.json` 实际输出 `k_agentd_Server -> d_gateway`、`k_agentd_fn -> d_orchestration`、`k_web_app_codegraph -> d_web_admin` 等；判断：本卡不改 `web/`，因此不触及 `d_web`。
- 2026-08-25：读取 `internal/agentd/codegraph.go` 与 `internal/agentd/codegraph_test.go`；实测当前 handler 在 best+target 成功分支内加载声明、只把它传给 `Check`，响应未含 `decls`；现有 fixture/测试入口均在 `internal/agentd/codegraph_test.go`，可作为主缝有界文件集。
- 2026-08-25：`rg -n 'github.com/Xsxdot/charter/graph' go.sum desktop/go.sum` 原始输出显示根与 desktop 均为 `v0.7.0`；`sed -n '1,36p' go.mod` 确认当前根模块仍 require `v0.7.0`，本卡冻结目标为下游实现同提交升到 `v0.8.0` 并 tidy desktop。
- 2026-08-25：`rg -n '^## |^### |d_protocol|声明|欠账' docs/roadmap.md` 原始输出显示 roadmap 现有条目按主题分节、尚无 C1.11 声明欠账条目；迁移卡需在 `docs/roadmap.md` 记录移出的 `TestCanTransit` 与完整 `stateMachine` 欠账。
- 2026-08-25：读取分域协议 §7.3，确认边界域验收只验契约形状、行为事实写“真机清单，归协调者执行”；§7.1 要求新增字段逐处列手写序列化并穿真实 wire 回归，新增枚举逐处核白名单；§9.5 要求圈不出有界文件集先插竖切卡。
- 2026-08-25：`rg -n 'd_coordination_task|d_orchestration|TestCanTransit|stateMachine' codegraph docs/roadmap.md go.mod desktop/go.mod internal web` 实际输出：旧声明 id 只出现在 `codegraph/domains/d_coordination_task.json` 与 baseline 数据，`TestCanTransit` 在 `internal/proto/proto_test.go`，`d_orchestration` 已是 best 顶层域；判断：本卡不改 baseline 或 proto 实现，只迁声明并记 roadmap 欠账。
- 2026-08-25：使用 `apply_patch` 创建法定产出 `docs/superpowers/specs/c1.11-breakdown.md`；内容覆盖待拍板清单、触及域/资格、契约逐条核对、三张四段式子卡、DAG、缺陷族、真机清单和自检。
- 2026-08-25：文档锚点自检命令 `env GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/5d7777ce/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph@v0.8.0 resolve --repo . --doc docs/superpowers/specs/c1.11-breakdown.md` 实际退出 0；原始 JSON 的 5 个本仓锚点为 `handleProjectCodegraph=ok`、`Handler=moved`、`forwardIfRequested=ok`、`writeJSON=moved`、`handleProjectCodegraphSource=ok`，无坏锚。
- 2026-08-25：`jq empty codegraph/best.json` 实际退出 0 无输出；`jq empty codegraph/target.json` 实际退出 0 无输出；结构扫描 `rg -n '^## |^### |^#### |待拍板|缺陷族|真机清单|入口指针|契约引用' docs/superpowers/specs/c1.11-breakdown.md` 实际退出 0，命中 3 张卡及四段式标题。
- 2026-08-25：普通权限执行 `git add docs/superpowers/specs/c1.11-breakdown.md docs/superpowers/ledgers/2026-08-25-c1.11-breakdown-ledger.md` 失败；原始报错：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/5d7777ce/index.lock': Read-only file system`。
- 2026-08-25：提权 `git add ...` 成功；随后 `git diff --cached --check` 实际退出 0 无输出，`git diff --cached --stat` 原始输出为 `2 files changed, 288 insertions(+)`，`git status --short --branch` 为 `## cards/C1.11-charter-2` 加两份预期新增文件。
- 2026-08-25：提交前复核实际退出 0：`git diff --cached --check` 无输出；`git diff --cached --name-status` 仅列 `A docs/superpowers/ledgers/2026-08-25-c1.11-breakdown-ledger.md` 与 `A docs/superpowers/specs/c1.11-breakdown.md`；统计为 `2 files changed, 289 insertions(+)`；分支仍为 `## cards/C1.11-charter-2`。
