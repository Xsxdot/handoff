# B249 实现节点台账

- 2026-08-25：命令 `pwd && git status --short --branch && rg --files docs/superpowers && sed -n ...`；原始输出为 `/root/.handoff/worktrees/34461480`、`## cards/B249-charter-4`，工作区无状态行；事实：本轮在当前分支开工，初始无未提交改动。
- 2026-08-25：命令 `sed -n '1,260p' docs/superpowers/specs/2026-08-25-b249-breakdown.md` 的首次路径尝试；原始输出为 `sed: can't read docs/superpowers/breakdowns/2026-08-25-b249-breakdown.md: No such file or directory`；事实：首次误用了 `breakdowns/` 路径，未读取到文件。
- 2026-08-25：命令 `sed -n '1,320p' docs/superpowers/specs/2026-08-25-b249-breakdown.md` 与 `rg -n '^#|^##|^###|Task|必须|新增|测试|文件|白名单|草稿|落点|台账' docs/superpowers/specs/2026-08-25-b249-permission-noise-reduction.md`；原始输出包含 B249 的 d_orchestration → d_execution 依赖、三根范围、正向安全命令白名单、审计事件、四执行器环境与真实机未验证清单；事实：本轮实现边界由已批准 spec、contract、breakdown、plan 冻结。
- 2026-08-25：命令 `rg --files docs/superpowers/ledgers | rg 'b249|B249'`；原始输出只有 `docs/superpowers/ledgers/2026-08-25-b249-contract-ledger.md`；事实：本执行台账需要使用 `docs/superpowers/ledgers/` 下的新文件。
- 2026-08-25：判断：本轮严格不修改 `internal/permgate/blacklist.go` 与 `internal/permgate/blacklist_test.go`；事实：B248 前置改动由协调者处理，本轮不复制其正则变更。
- 2026-08-25：命令 `go test ./internal/executor/... -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/executor 0.013s`、`claudecode 4.017s`、`codex 5.939s`、`fake 0.001s`、`grok 1.390s`、`opencode 17.957s`、`rawtap 0.003s`、`turn 0.023s`，退出码 0；事实：Task 1 编辑前 executor 基线测试通过。
- 2026-08-25：命令 `go test ./internal/executor/claudecode ./internal/executor/grok ./internal/executor/opencode -run TestManagedTaskTmpEnv -count=1`；原始输出为三个包均 `undefined: managedTaskTmpEnv` 且 `FAIL ... build failed`，退出码 1；事实：新增任务临时环境接缝的首红原因是生产符号缺失，测试断言尚未执行。
- 2026-08-25：命令 `go test ./internal/executor/claudecode ./internal/executor/grok ./internal/executor/opencode -run TestManagedTaskTmpEnv -count=1`；原始输出为三个 `TestManagedTaskTmpEnv` 均断言 `managed tmp dir = "", want "/data/tmp/01234567"`，退出码 1；事实：空壳已编译，行为红直接命中任务临时目录契约而非测试拼写错误。
- 2026-08-25：命令 `go test ./internal/executor/claudecode ./internal/executor/grok ./internal/executor/opencode -run TestManagedTaskTmpEnv -count=1`；原始输出为 `internal/executor/grok/adapter.go:78:12: undefined: os`，claudecode/opencode 测试通过，整体退出码 1；事实：实现首轮缺少 grok adapter 的 `os` import，未替其归因。
- 2026-08-25：修正 grok adapter import 后，命令 `go test ./internal/executor/claudecode ./internal/executor/grok ./internal/executor/opencode -run TestManagedTaskTmpEnv -count=1`；原始输出为三个包均 `ok`，退出码 0；事实：三家任务临时目录环境 helper 的最小行为断言通过。
- 2026-08-25：命令 `gofmt -w ... && git diff --check && go test ./internal/executor/... -count=1`；原始输出中 `claudecode` 多个 Start 用例失败，统一错误为 `创建任务临时目录 /root/.handoff/tmp/tmp/<task>: mkdir /root/.handoff/tmp/tmp: read-only file system`，另有 `TestStartProcKillsShimWhenFIFOReaderNeverReady` 与 `TestClaudeToolTimingPaired` 失败，`codex/fake/grok` 通过，整体退出码 1；事实：现有 adapter 测试使用非 `<DataDir>/tasks/<id>` 的 TaskDir，当前严格两级推导落到不可写父目录，必须兼容该测试形态后再继续。
- 2026-08-25：在三家 helper 对非生产测试 TaskDir 采用其直接父目录作为临时 DataDir 后，命令 `gofmt -w ... && git diff --check && go test ./internal/executor/... -count=1`；原始输出为 executor、claudecode、codex、fake、grok、opencode、rawtap、turn 均 `ok`，退出码 0；事实：Task 1 executor 子系统测试与空白检查通过。
- 2026-08-25：命令 `go test ./internal/permgate -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/permgate 0.007s`，退出码 0；事实：Task 2 编辑前 permgate 基线测试通过。
- 2026-08-25：命令 `go test ./internal/permgate -run 'TestInScopeUsesTaskTmpAsThirdRoot|TestWriteArgTargetsB249Shapes|TestJudgeSafeCommand|TestIsSelfCommand' -count=1`；原始输出包含 `unknown field TaskTmpDir in struct literal of type Scope`、`scope.TaskTmpDir undefined`、`undefined: RuleSafeCommand`，退出码 1；事实：Task 2 新接缝首红为冻结字段与规则符号缺失，测试未进入断言阶段。
- 2026-08-25：补齐 `Scope.TaskTmpDir` 与 `RuleSafeCommand` 空契约后，命令 `go test ./internal/permgate -run 'TestInScopeUsesTaskTmpAsThirdRoot|TestWriteArgTargetsB249Shapes|TestJudgeSafeCommand|TestIsSelfCommand' -count=1`；原始输出为 task-tmp 仍 `(false, "")`、14 个安全命令均 `Action:1, Reason:"黑名单未命中"`、graph resolve 仍命中 `graph`、四种新写入形态均为空，退出码 1；事实：空契约已编译，断言红分别锁定第三根、白名单、graph 和落点提取缺口。
- 2026-08-25：命令 `gofmt -w internal/permgate/... && go test ./internal/permgate -run 'TestInScopeUsesTaskTmpAsThirdRoot|TestWriteArgTargetsB249Shapes|TestJudgeSafeCommand|TestIsSelfCommand' -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/permgate 0.006s`，退出码 0；事实：Task 2 首批第三根、四种落点、全量安全命令正反例及 graph resolve 测试通过。
- 2026-08-25：命令 `gofmt -w internal/permgate/permgate_test.go && git diff --check && go test ./internal/permgate -count=1`；原始输出中仅 `TestJudgeBashInScopeFallsBack/go_test_./..._2>&1` 失败，实得 `consult（reason="黑名单未命中"）`，其余测试输出通过，退出码 1；事实：safe matcher 当前把合法 fd 复制语法 `2>&1` 当作未许可单个 `&`，需修正 tokenizer。
- 2026-08-25：命令 `gofmt -w internal/permgate/permgate.go && git diff --check && go test ./internal/permgate -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/permgate 0.008s`，退出码 0；事实：Task 2 permgate 全包测试与空白检查通过，fd 复制语法修正后保持既有测试绿。
- 2026-08-25：命令 `go test ./internal/agentd ./internal/proto ./internal/client -count=1`；原始输出为 agentd `--- FAIL: TestFullLoop ... 首个事件 type=question, want permission_request`、`--- FAIL: TestPermissionImmediateVisible ... WaitEvent: context deadline exceeded`，agentd 退出码 1（耗时 129.147s）；proto `ok ... 0.006s`、client `ok ... 9.271s`；事实：Task 3 编辑前基线 agentd 实跑失败，proto/client 通过，未对失败作归因。
- 2026-08-25：命令 `go test ./internal/agentd -run TestJudgePermissionSafeCommandUsesTaskTmpScope -count=1 && go test ./internal/proto -run TestPermissionAutoAllowEventTypeJSON -count=1 && go test ./internal/client -run TestPermissionAutoAllowIsNotDeliverable -count=1`；原始输出为 agentd 测试断言 `task tmp safe command verdict = ... Action:2, Reason:"目标路径越出任务范围: .../tmp/wire-tas/out"`，退出码 1，后续命令未执行；事实：Manager 尚未接入 TaskTmpDir，新增行为红直接命中第三根接线缺失。
- 2026-08-25：命令 `go test ./internal/proto -run TestPermissionAutoAllowEventTypeJSON -count=1; ...; go test ./internal/client -run TestPermissionAutoAllowIsNotDeliverable -count=1`；原始输出为 proto/client 均 `undefined: EventTypePermissionAutoAllow` / `undefined: proto.EventTypePermissionAutoAllow`，`proto_status=1 client_status=1`；事实：新增协议常量缺失，两个测试均停在符号编译红。
- 2026-08-25：补齐 Manager TaskTmpDir 接线、appendEvent 审计调用点、permission_auto_allow 常量与 client 过滤后，命令 `gofmt -w ... && go test ./internal/agentd -run TestJudgePermissionSafeCommandUsesTaskTmpScope -count=1; ...`；原始输出为 agentd/proto/client 均 `ok`（分别 `0.043s`、`0.001s`、`0.003s`），三项状态均 0；事实：Task 3 首批第三根接线、事件常量 JSON roundtrip、不可交付过滤测试通过。
- 2026-08-25：命令 `gofmt -w internal/agentd/permgate_wire_test.go && go test ./internal/agentd -run 'TestSafeCommandPermissionAuditsOnceWithoutTicket|TestSafeCommandAuditFailureStillResponds|TestInScopeWriteDoesNotCreateSafeCommandAudit|TestJudgePermissionSafeCommandUsesTaskTmpScope' -count=1`；原始输出为 `TestSafeCommandPermissionAuditsOnceWithoutTicket` 失败：`replayed response count = 2, want 1`，退出码 1；事实：现有重放判据只检查 ticket，白名单 AutoAllow 无 ticket，需增加基于已落库审计事件的 PermissionID 幂等检查。
- 2026-08-25：增加 `permission_auto_allow` 审计事件的 PermissionID 重放检查后，命令 `gofmt -w internal/agentd/manager.go && go test ./internal/agentd -run 'TestSafeCommandPermissionAuditsOnceWithoutTicket|TestSafeCommandAuditFailureStillResponds|TestInScopeWriteDoesNotCreateSafeCommandAudit|TestJudgePermissionSafeCommandUsesTaskTmpScope' -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.276s`，退出码 0；事实：审计一次性、AppendEvent 失败仍 Respond once、范围写不审计、TaskTmp 接线测试通过。
- 2026-08-25：命令 `gofmt -w internal/agentd/manager.go internal/agentd/permgate_wire_test.go internal/proto/proto.go internal/proto/proto_test.go internal/client/client.go internal/client/client_internal_test.go && git diff --check && go test ./internal/agentd ./internal/proto ./internal/client -count=1`；原始输出为 `--- FAIL: TestFullLoop (0.24s) integration_test.go:262: 首个事件 type=question, want permission_request`、`--- FAIL: TestPermissionImmediateVisible (5.22s) integration_test.go:553: WaitEvent: context deadline exceeded`、`FAIL github.com/Xsxdot/handoff/internal/agentd 128.157s`、`ok github.com/Xsxdot/handoff/internal/proto 0.003s`、`ok github.com/Xsxdot/handoff/internal/client 9.354s`，退出码 1；事实：Task 3 三包收尾实跑中 agentd 仍有该基线集成失败，proto/client 通过，未对失败作归因。
- 2026-08-25：命令 `go test ./internal/store ./internal/proto ./internal/client ./internal/ledgermirror -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/store 4.415s`、`ok github.com/Xsxdot/handoff/internal/proto 0.004s`、`ok github.com/Xsxdot/handoff/internal/client 9.386s`、`ok github.com/Xsxdot/handoff/internal/ledgermirror 2.280s`，退出码 0；事实：Task 4 编辑前四包基线测试通过。
- 2026-08-25：命令 `gofmt -w internal/store/store_test.go internal/store/eventhook_test.go internal/client/follow_test.go internal/client/backlog_internal_test.go internal/ledgermirror/mirror_test.go`；原始输出为空，退出码 0；事实：Task 4 新增测试文件格式化完成。
- 2026-08-25：命令 `go test ./internal/store -run 'TestPermissionAutoAllowJSONRoundtrip|TestEventHookCarriesPermissionAutoAllow' -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/store 0.115s`，退出码 0；事实：真实 Store JSON 缺失/空串区分、omitempty 字段与 event hook 新类型测试通过。
- 2026-08-25：命令 `go test ./internal/client -run 'TestFollowFiltersAuditEvents|TestFollowAllDeliversAuditEvents|TestFollowFiltersPermissionAutoAllowFromStore|TestComputeBacklogSkipsAuditEvents' -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/client 0.059s`，退出码 0；事实：Store 事件经 WS JSON 后 all=false 过滤、all=true 可见及 backlog 过滤测试通过。
- 2026-08-25：命令 `go test ./internal/ledgermirror -run TestMirrorFlowsLinkedTaskEvents -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/ledgermirror 0.121s`，退出码 0；事实：新 permission_auto_allow 未加入 mirrorSkip，既有镜像 harness 实际落账可见。
- 2026-08-25：命令 `git diff --check`；原始输出为空，退出码 0；事实：Task 4 测试改动无空白错误。
- 2026-08-25：命令 `go test ./internal/store ./internal/proto ./internal/client ./internal/ledgermirror -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/store 4.328s`、`ok github.com/Xsxdot/handoff/internal/proto 0.003s`、`ok github.com/Xsxdot/handoff/internal/client 9.449s`、`ok github.com/Xsxdot/handoff/internal/ledgermirror 2.298s`，退出码 0；事实：Task 4 四包完整测试通过。
- 2026-08-25：命令 `git status --short --branch`；原始输出显示当前分支 `## cards/B249-charter-4`，改动集中在 B249 计划文件集，新增执行台账；事实：未切分支，`internal/permgate/blacklist.go` 与 `internal/permgate/blacklist_test.go` 未出现在 `git diff --name-only -- ...` 输出中。
- 2026-08-25：命令 `git diff --stat`；原始输出为 31 个已跟踪文件、`1011 insertions(+), 63 deletions(-)`；事实：当前实现包含 Task 1–4 的生产/测试改动，台账仍是未跟踪文件待收口。
- 2026-08-25：命令 `git diff --check`；原始输出为空，退出码 0；事实：当前全量工作树改动无空白错误。
- 2026-08-25：命令 `go build ./...`；原始输出为空，退出码 0；事实：全仓编译通过。
- 2026-08-25：命令 `go vet ./internal/permgate ./internal/agentd ./internal/client ./internal/proto ./internal/store ./internal/ledgermirror ./internal/executor/...`；原始输出为空，退出码 0；事实：计划列出的受影响包 vet 通过。
- 2026-08-25：命令 `rg -F -n 'for _, b := range []string{scope.Workdir, scope.TaskDir, scope.TaskTmpDir}' internal/permgate/path.go`；原始输出唯一命中 `50: for _, b := range []string{scope.Workdir, scope.TaskDir, scope.TaskTmpDir} {`；事实：范围第三根变异锚命中唯一。
- 2026-08-25：变异将唯一锚改为 `[]string{scope.Workdir, scope.TaskDir}`；命令 `go test ./internal/permgate -run TestInScopeUsesTaskTmpAsThirdRoot -count=1` 原始输出为 `--- FAIL: TestInScopeUsesTaskTmpAsThirdRoot/task-tmp ... InScope("/root/.handoff/tmp/34461480/TestInScopeUsesTaskTmpAsThirdRoot2110695185/001/tmp/abcd1234/out.txt") = (false, ""), want (true, "/root/.handoff/tmp/34461480/TestInScopeUsesTaskTmpAsThirdRoot2110695185/001/tmp/abcd1234")`、`FAIL .../internal/permgate 0.003s`，退出码 1；事实：行为断言先变红，证明变异打中第三根守卫。
- 2026-08-25：变异状态命令 `go build ./...`；原始输出为空，退出码 0；事实：变异编译通过，随后才进行全包计数。
- 2026-08-25：变异状态命令 `go test ./internal/permgate -count=1`；原始输出包含同一 `TestInScopeUsesTaskTmpAsThirdRoot/task-tmp` 断言失败，且最终为 `FAIL .../internal/permgate 0.007s`，退出码 1；事实：编译通过的变异被触及范围包测试拦截。
- 2026-08-25：恢复第三根后命令 `go test ./internal/permgate -run TestInScopeUsesTaskTmpAsThirdRoot -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/permgate 0.002s`，退出码 0；事实：生产代码已恢复，变异自验不留变异。
- 2026-08-25：命令 `go test ./internal/permgate ./internal/client ./internal/proto ./internal/store ./internal/ledgermirror ./internal/executor/... -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/permgate 0.020s`、`ok github.com/Xsxdot/handoff/internal/client 9.543s`、`ok github.com/Xsxdot/handoff/internal/proto 0.004s`、`ok github.com/Xsxdot/handoff/internal/store 4.574s`、`ok github.com/Xsxdot/handoff/internal/ledgermirror 2.317s`、`ok github.com/Xsxdot/handoff/internal/executor 0.004s`、`ok .../claudecode 4.067s`、`ok .../codex 5.971s`、`ok .../fake 0.002s`、`ok .../grok 1.389s`、`ok .../opencode 17.982s`、`ok .../rawtap 0.006s`、`ok .../turn 0.023s`，退出码 0；事实：非 agentd 受影响包与四执行器全量测试通过。
- 2026-08-25：命令 `go test ./internal/agentd -skip 'TestFullLoop|TestPermissionImmediateVisible' -count=1`；原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 132.126s`，退出码 0；事实：排除已记录基线集成失败的 agentd 其余测试通过。
- 2026-08-25：命令 `gofmt -l` 逐一检查本轮改动的 30 个 Go 文件；原始输出为空，退出码 0；事实：改动 Go 文件格式化检查通过。
- 2026-08-25：补强 `TestFollowAllDeliversAuditEvents` 以断言 all=true 的 permission_auto_allow 类型和 permission_id JSON payload 后，命令 `gofmt -w internal/client/follow_test.go` 原始输出为空，随后 `go test ./internal/client -run 'TestFollowFiltersAuditEvents|TestFollowAllDeliversAuditEvents|TestFollowFiltersPermissionAutoAllowFromStore' -count=1` 原始输出为 `ok github.com/Xsxdot/handoff/internal/client 0.056s`，退出码 0；事实：审计事件全量可见且 payload 可解码。
- 2026-08-25：最终收尾命令 `go build ./...`；原始输出为空，退出码 0；并行 `git diff --check` 原始输出为空，退出码 0；`git status --short --branch` 原始输出首行为 `## cards/B249-charter-4`，其余为本轮 31 个改动文件及执行台账；事实：最终编译、空白检查通过，仍在当前分支且未提交。
- 2026-08-25：命令 `git add internal docs/superpowers/ledgers/2026-08-25-b249-execution-ledger.md`；原始错误为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/34461480/index.lock': Read-only file system`，退出码 128；事实：已知 linked worktree git 元数据只读，需按环境约束申请提权重试，未改生产代码。
- 2026-08-25：提权重试同一 `git add internal docs/superpowers/ledgers/2026-08-25-b249-execution-ledger.md`；原始输出为空，退出码 0；随后 `git status --short --branch && git diff --cached --check` 原始输出为当前分支 `## cards/B249-charter-4`、台账 A 及本轮内部文件 M，空白检查无输出且退出码 0；事实：B249 改动已完整暂存。
- 2026-08-25：提权命令 `git commit -m "feat: reduce permission gate noise"`；原始输出为 `[cards/B249-charter-4 f0c10de7] feat: reduce permission gate noise`、`32 files changed, 1075 insertions(+), 65 deletions(-)`、`create mode 100644 docs/superpowers/ledgers/2026-08-25-b249-execution-ledger.md`，退出码 0；事实：B249 首次本地提交已创建，未 push；本行随后随 amend 纳入最终提交。
- 2026-08-25：提权命令 `git add docs/superpowers/ledgers/2026-08-25-b249-execution-ledger.md && git commit --amend --no-edit`；原始输出为 `[cards/B249-charter-4 d3ee745a] feat: reduce permission gate noise`、`32 files changed, 1076 insertions(+), 65 deletions(-)`，退出码 0；事实：提交记录台账已纳入本地 amend，未 push。
