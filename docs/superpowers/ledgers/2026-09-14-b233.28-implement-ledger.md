# B233.28 implement 台账

节点：implement。产出物：本卡实现（facade.go 行为 + gateway 读路径切门面 + 测试）。
有效基线：`cards/B233.1-charter-7` @ `56c7a9c7`。本分支：`cards/B233.28-charter-7`，起点 `d088b3fb`（plan 提交）。
计划：`docs/superpowers/plans/b233.28-plan.md`。契约：`docs/superpowers/specs/b233.28-contract.md`。

- 2026-09-14：`git status --short` 起点 → 空（干净工作树），分支 `cards/B233.28-charter-7`，HEAD `d088b3fb`。
- 2026-09-14：W1 新建 `internal/orchestration/facade_test.go`。首红（计划原文助手名 `seedWaitingAnswerTask` 与既有 `watchdog_test.go:45` 同名冲突，编译红 `redeclared`）：重命名为 `seedFacadeWaitingAnswerTask` 后复跑。
- 2026-09-14：W1 跑红 `go test ./internal/orchestration/ -run 'TestFacadeAnswerTicket|TestFacadeResumeIfIdle' -count=1` → 原始输出：
  ```
  --- FAIL: TestFacadeAnswerTicketAnswersAndEmitsEvent (0.06s)
      facade_test.go:47: AnswerTicket = (false,B233.28 Ticket 0：工单应答门面未接线)，want (true,nil)
  --- FAIL: TestFacadeResumeIfIdleOnlyWhenNoPending (0.09s)
      facade_test.go:116: 无待办未回迁: waiting_answer
  --- FAIL: TestFacadeResumeIfIdleConcurrentSingleTransition (0.08s)
      facade_test.go:135: 并发回迁最终态 = waiting_answer，want running
  FAIL	github.com/Xsxdot/handoff/internal/orchestration	0.245s
  EXIT=1
  ```
  红原因：功能缺失（空壳哨兵 / 空实现），非 typo。

- 2026-09-14：W1 落码（facade.go `AnswerTicket`/`ResumeIfIdle` 落地、删 `ErrTicketAnswerUnwired`）后复跑 `go test ./internal/orchestration/ -run 'TestFacadeAnswerTicket|TestFacadeResumeIfIdle' -count=1` → `ok ... 0.231s`，退出码 0。
- 2026-09-14：W1 变异自验（先确认编译过，再看断言红）：
  - 命中唯一断言：`grep -n` 确认 `if tk.TaskID != taskID {`/`if len(pending) > 0 {`/`TicketAnsweredPayload{...}` 各 1 处。
  - 变异1 归属守卫改 `tk.TaskID != taskID && tk.ID == ""`（可编译，因仍用 tk）→ `TestFacadeAnswerTicket` 红 `跨任务工单 err=<nil>，want ErrNotFound`。
  - 变异2 待办守卫改 `len(pending) >= 0` → `TestFacadeResumeIfIdleOnlyWhenNoPending` 红 `无待办未回迁: waiting_answer`。
  - 变异3 payload 换 `map[string]string` → `TestFacadeAnswerTicket` 红 `payload = {"answer":"答","ticket_id":"tk1"}`（键序错）。
  - 每次变异后 `cp /tmp/facade.bak` 还原，末次复跑两测 `ok`。
  - 注：初版变异 `if false {`/`if false {` 触发 Go 「declared and not used」编译红，按纪律判「不算数」，整块替换为可编译等价变异重做。
- 2026-09-14：`go build ./...` → 空输出，退出码 0；`grep -rn 'ErrTicketAnswerUnwired' --include='*.go' .` → 零命中（退出码 1）。
- 2026-09-14：W2 在 `internal/agentd/server_test.go` 追加 `recordingOrchClient` + `TestHandleReplyDelegatesToFacade`；`newTestEnvWithCfg` 注入真 Manager（`orchestration.NewManager(st, srv.Hub(), {fake}, cfg, nil, nil, newTestGate(t), logger)` + `SetWorkspace` + `srv.SetManager`）。
- 2026-09-14：W2 跑红 `go test ./internal/agentd/ -run 'TestHandleReplyDelegatesToFacade' -count=1` → 原始输出：
  ```
  --- FAIL: TestHandleReplyDelegatesToFacade (0.05s)
      server_test.go:1224: reply 返回 404, want 200
  FAIL
  ```
  （handler 未走门面：fake 未落库、直打 s.st 得 404。）
- 2026-09-14：W2 落码（handleReply 只调 `s.mgr.AnswerTicket`/`ResumeIfIdle`，加 `s.mgr==nil` 503 守卫，删 `Server.resumeIfIdle`）后复跑 → `ok ... 0.054s`，退出码 0。
- 2026-09-14：W2 复跑既有 reply 面 `go test ./internal/agentd/ -run 'Reply|HandleReply|B23313' -count=1` → `ok ... 2.413s`，退出码 0；`go build ./...` 退出码 0。
- 2026-09-14：W2 变异（handleReply 改回直打 `s.st.GetTicket` 内联）→ `TestHandleReplyDelegatesToFacade` 红 `reply 返回 404, want 200`，已还原。

- 2026-09-14：W3 新增 `internal/agentd/b23328_source_guard_test.go`（AST 扫描 10 个生产文件里的 `s.st.<收口方法>`）。首红：`go test ./internal/agentd/ -run 'TestB23328GatewayNoDirectStoreTaskProjectCalls' -count=1` → 列出 34 处直打（`handlers.go` 15、`projectadmin.go` 4、`codegraph.go` 2、`tasksfanout.go` 2、`taskroute.go` 2、`roomsapi.go` 4、`coordapi.go` 1、`workspacefiles.go` 1、`taskplan.go` 1、`update.go` 1 等），`本卡收口面仍有 34 处 s.st 直打`，退出码 1。
- 2026-09-14：W3 切调用点：10 个生产文件全部 `s.st.<收口方法>` → `s.mgr.<同名>`，并在 `handleListTasks`/`handleGetTask`/`taskOrErr`/`handleEvents`（Accept 前）/`handleTaskPlan`/`handleInbox`/`handleProjectBranches`/`handleProjectWorktreeCreate`（守卫上移）/`handleProjectCodegraph`/`handleProjectCodegraphSource` 加 `s.mgr==nil` 503 守卫；`tasksAll`/`ticketDestructive`/`resolveCoordWorkdir`/`resolveWorkspace` 按 best-effort 各自守卫（后两者 fail-closed）。
- 2026-09-14：W3 复跑静态断言 → `ok ... 0.007s`，退出码 0；`go build ./...` 退出码 0；`go vet ./internal/agentd/` 退出码 0。
- 2026-09-14：W3 夹具注入：新增 `ensureTestManager`（`b23313_gateway_testhelpers_test.go`，转调已有 `newManagerForServer(t, env.srv, nil)`），按 §3.3 清单逐文件注入；额外补 `taskplan_test.go` 的 `TestTaskPlanMissingIsNotFound`/`TestTaskPlanUnknownTaskIsNotFound` 两处（计划 §3.3 只提 `seedPlanTask`，但这两测直接调 `newTestAgentdEnv` 未过 seedPlanTask，按 P-1-A 同款处置）——**偏离计划的增补，已登记**。
- 2026-09-14：W3 首轮整包跑 `go test ./internal/agentd/ -count=1` → 原始红：
  ```
  --- FAIL: TestTaskPlanMissingIsNotFound (0.08s)
      taskplan_test.go:90: 无 plan_path 任务状态码 = 503，期望 404
  --- FAIL: TestTaskPlanUnknownTaskIsNotFound (0.09s)
      taskplan_test.go:113: 状态码 = 503，期望 404
  FAIL	github.com/Xsxdot/handoff/internal/agentd	161.869s
  ```
  补两处注入后复跑整包 → `ok github.com/Xsxdot/handoff/internal/agentd 171.071s`，退出码 0。
- 2026-09-14：W3 变异（先确认编译过再数红）：
  - 变异A `handlers.go` 的 `handleListTasks` 改回 `s.st.ListTasks()`（命中唯一）→ 静态断言红 `handlers.go:208 生产路径仍直打 store: s.st.ListTasks`。
  - 变异B 删 `b23319_retained_test.go` 的 `ensureTestManager` → `TestTaskListAnnotatesProjectID` 红 `GET /api/tasks 解码: json: cannot unmarshal object into type []proto.TaskView`（503 错误对象）。
  - 两处均已还原。

- 2026-09-14：W4 复跑（facade/roomsapi gofmt 收尾后）：
  - `go build ./...` → 空输出，退出码 0。
  - `go vet ./internal/agentd/ ./internal/orchestration/ ./cmd/...` → 空输出，退出码 0。
  - `gofmt -l $(git diff --name-only -- '*.go')` → 无输出（本轮改动文件全干净；`internal/agentd/roomsapi.go` 的既有非 gofmt 注释重排为 gofmt -w 顺带结果）。仓内 `internal/agentd/wakeconsumer.go` 等存量非本卡文件的 gofmt 漂移未动。
  - `go test ./internal/orchestration/ ./internal/agentd/ ./cmd/... -count=1` → `ok orchestration 67.939s` / `ok agentd 180.673s` / `ok cmd 48.445s`，退出码 0。
  - `codegraph check --view cards/B233.28-charter --repo .` → 原始读数 `fails=[]`、`d_gateway->d_orchestration=76`（≤76），退出码 0。
  - `codegraph validate --view cards/B233.28-charter --repo .` → `issues=None`（0 issue），退出码 0。
  - `git diff --stat codegraph/target.json codegraph/best.json codegraph/baseline.json` → 空（零改动）。
  - `grep -rn 'ErrTicketAnswerUnwired' --include='*.go' .` → 零命中；`grep 'func (s \*Server) resumeIfIdle'` → 零命中（残留 `resumeIfIdle` 字样仅在描述编排侧行为的注释里，无生产引用）。
  - `grep -rn 's\.st\.\(收口方法\)' --include='*.go' internal/agentd | grep -v _test` → 零命中（收口面清零）。
- 2026-09-14：未验证项（沿用 plan/breakdown §6，本卡机内不宣称）：真实跨机 reply/列任务/项目位置五分支、agentd 重启窗口自愈、并发双 reply 回迁唯一、图重扫后命中真实下降。均标「未验证，需真机」。
- 2026-09-14：提交事实（历史读数）——`git add -A && git commit -m "implement(B233.28): gateway 任务面/项目位置走编排查询门面——W1..W4 落地 ..."` → 原始输出 `[cards/B233.28-charter-7 d948ef7d] implement(B233.28): ...`；`git status --short` 提交后为空。本行随该提交 amend 一次收进同批（amend 会换 hash，是 git 事实，不 chase HEAD；收口判据是工作树干净）。
