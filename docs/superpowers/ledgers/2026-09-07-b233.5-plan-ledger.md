# B233.5 plan 台账

节点：charter/plan  
卡：B233.5  
分支：cards/B233.5-charter-3  
法定产出：docs/superpowers/plans/b233.5-plan.md  
规则：每确立一个事实立刻追加一行；提交事实记命令与原始输出，随后只 amend 一次。

---

## 2026-09-07 开工

- 角色：plan 节点。只写计划，不写实现代码、不建脚手架、不扇出子卡、不合 main、不 absorb。
- 拍板：breakdown §0 P1–P11 全数 (a)，提交 `fed8de82`。plan 不得把未选方案写成事实。
- 轻档：单卡单轮 implement，内部 ticket T1–T7 是执行顺序，不是 `card split`。
- HEAD `fed8de8231ab7dad5f23a79c2942a985a756fa80`，分支 `cards/B233.5-charter-3`。拍板提交在 HEAD。
- 有效基线分支：`cards/B233.1-charter-7`。
- 有图：`codegraph/` 在仓内。视图 `cards-B233.5-charter`。
- 真机清单归协调者，机内不得写成绿。

## 2026-09-07 查图与源码

- `codegraph --repo . --view cards-B233.5-charter check`：`fails: []`；`warns` 147（存量 oversized/prefix-family 等，本卡不修）。
- `context d_scheduling`：包 `internal/scheduling` + `internal/schedclient`；kind=logic。context 主链仍是 New/RunCommand/ApplyDetect，Ticket 0 符号未进 context 主链——接线后以 `sym`/`who-calls`/`check` 为准。
- `sym Admit`：`n_scheduling_Service_Admit` scheduling.go:367，anchor=moved。
- `sym BindPhysical` / `ResolveLookup` / `OccupancyKeys` / `EffectiveReceiverName` / `IdentityOf` / `EncodeDefaultCarrier`：均 `k_scheduling_fn`，anchor=ok，status=added。
- `sym bindingFor`：**不在图中**（图覆盖债）。源码 `scheduling.go:731`。
- `sym AdmitCarrier` / `DefaultCarrier` / `withCarrierHome`：**不在图中**（前两个尚未存在；withCarrierHome 短名覆盖债）。
- `sym handleDispatch`：`n_agentd_Server_handleDispatch` server.go:1359，domain 基线 `d_coordination_api`（best 归 `d_gateway`，本卡不修归属）。
- `sym Manager.Dispatch`：manager.go:753，anchor=moved，domain `d_coordination_task`。
- `sym startCardStep`：cardstep.go:46。图签名过期为 `(cardID, node, actor string)`，源码是 `(cardID string, req proto.CardStepReq)`。
- `sym admitSquadStep`：scheddispatch.go:47。
- `sym releaseSchedulingBinding`：scheddrain.go:385，anchor=moved。
- `sym CreateTask`：store.go:328。
- `flow Admit` / `BindPhysical` / `ResolveLookup` / `startCardStep`：**degraded=true，steps=[]**。已读源码。禁止用 chain 冒充。
- `flow Manager.Dispatch`：degraded=false，steps=95。源码仍 `resolveExecutor`（空则 `cfg.Executor.Default`），CreateTask 不写 Carrier。
- `who-calls n_scheduling_Service_Admit`：图内无生产边，`unscannedEntries=5`。grep 调用方 `scheddispatch.go#Server.admitSquadStep`。
- `who-calls BindPhysical` / `ResolveLookup` / `OccupancyKeys`：无生产边（Ticket 0 空壳）。
- `who-calls handleDispatch`：边 `e_http_post_api_tasks` → handleDispatch。
- 生产 import：`go list` `internal/scheduling` 只有 std + `internal/schedclient`，无 ledger/agentd。
- `d_gateway→d_scheduling` target.json 预算 0，entries 含 `scheduling.Service` 与包级函数。不新开 `d_orchestration→d_scheduling` / `d_cli→d_scheduling`。

## 2026-09-07 现状行为（读源码，行号本轮读数）

- Ticket 0 空壳已在：`receiver.go` / `occupancy.go` + 金样本测试。生产未接线。
- `PutCarrier` (:194) / `PutSquad` (:342) **未**调 `CrossKindConflict`。
- `acquire` (:653) 手拼 `kindSquad+"/"+q.Name+"/"+member.Carrier` 与 `kindCarrier+"/"+c.Name`。空小队会写出 `squad//x`。
- `Release` (:437) 同样手拼；空小队仍 stepRunning `squad//<carrier>`。
- `bindingFor` (:731) 仍「请求覆盖 > 载体缺省」，**不写 HomeDir**。
- `Admit` 要求小队存在且 RoleExecutor；无 `AdmitCarrier`。
- 无 `DefaultCarrier` / `SetDefaultCarrier`。`KindDefaultCarrier`/`DefaultCarrierID` 仅常量和编解码。
- `handleDispatch` (:1359) 把 `Receiver` 原样塞进 `DispatchReq`，**不** ResolveLookup / Admit / BindPhysical。
- `writeDispatchError` 无 `ErrNoDefault`/`ErrNameConflict`/`ErrPhysicalOverride`/`ErrNoSlot` 分支（会落到 500「派发任务失败」）。
- `admitSquadStep` 仍 `effectiveCovers` 产出 target/executor 喂给 `Admit`。
- `effectiveCovers` (:108) 注释与 `TestEffectiveCoversParityMatrix` 仍锁「请求压过节点」。
- `startCardStep` (:59) P10-(a) 已拒绝非空 `--target`/`--executor`。准入后 (:98) **再** `Carrier(binding.Carrier)` 读活 HOME。
- `releaseSchedulingBinding` (:386) `Squad==""` 直接 return。
- `Manager.Dispatch` 不解析 Receiver；`resolveExecutor("")` → `cfg.Executor.Default`；CreateTask 写 Target/Executor/HomeDir，**不写 Carrier**。
- `withCarrierHome` manager.go:1145 续接用 **任务** HOME。Continue/resume **零** import scheduling。
- `CreateTask` INSERT 无 carrier 列。`taskColumns`/`scanTaskRow` 无 carrier。`allowedTaskFields` 无 carrier。迁移 map 无 carrier。DDL 无 carrier。
- `proto.Task.Carrier` JSON `carrier,omitempty` 已冻。`taskSample` Carrier 为空（fixture 缺键）。
- `web/src/api/types.ts` Task **无** `carrier`，也无 `home_dir`（C-12：不借机补 home_dir）。
- `DispatchOpts.Receiver` 非空才写 JSON `receiver`。`Client.Dispatch` map **总是**写 `target`/`executor`（空串也在）。
- `cmd/dispatch.go` 无 `--receiver`；`DispatchOpts.Target: targetName`（全局 `--target` 当执行落点，C-1 禁止）。
- `newIntegEnvCfg`（integration_test.go:82）HTTP 派发共用夹具：**无** SetupAutomation、无默认载体。T2 接线后空 receiver 必 `ErrNoDefault`——必须在该夹具预置默认载体，否则整组集成假红。
- `dispatchPlan` 仍传 `Target: "local"`。默认载体 Machine 必须是 `local`（本机别名），否则 BindPhysical 把测试残留的 target 当 overlay 误杀。
- 库事实：`json.Unmarshal` 忽略未知字段（契约 §3，标准库）。`omitempty` 空串缺席。`maxCASAttempts=8` scheduling.go:42。`kindRunning="sched_running"`。Release 到 0 不再下探（stepRunning next<0 钳 0）。

## 2026-09-07 基线测试（亲自跑，HEAD `fed8de82`，未改实现）

```
go test ./internal/scheduling/ -count=1
ok  	github.com/Xsxdot/handoff/internal/scheduling	0.879s

go test ./internal/proto/ -run 'TestTaskCarrierJSONKey|TestTaskHomeDirJSON|TestContractFixtures|TestBindingHomeDir' -count=1
ok  	github.com/Xsxdot/handoff/internal/proto	0.426s

go test ./internal/agentd/ -count=1 -run 'TestSquadNodeAdmitsAndDispatchesThroughBinding|TestEffectiveCoversParityMatrix|TestAdmissionRequiresOnline|TestSquadNodeRejectsNamedTargetAndExecutor|TestCardStepAdmittedRoundReleasesCapacity|TestDispatchEnvFailureReturns500WithCause'
ok  	github.com/Xsxdot/handoff/internal/agentd	3.237s

go test ./internal/store/ -run 'TestTaskLifecycle|TestCreateTaskPersistsPhase2Fields' -count=1
ok  	github.com/Xsxdot/handoff/internal/store	0.477s

go test ./internal/client/ -run 'TestDispatchSerializesCardDefaultBaseMarker' -count=1
ok  	github.com/Xsxdot/handoff/internal/client	0.524s

go test ./cmd/ -run 'Dispatch' -count=1
ok  	github.com/Xsxdot/handoff/cmd	1.595s

go build -o /dev/null .
exit 0，stdout 空
```

今日绿、接线后必须改锁的正向：

- `TestEffectiveCoversParityMatrix`「请求压过节点」行：T2 P7 必须退役物理覆盖正向，否则新实现假红或旧断言假绿。
- 所有走 HTTP `POST /api/tasks` 且不预置默认载体的用例（`newIntegEnvCfg`、`TestDispatchEnvFailureReturns500WithCause`、`TestDispatchExecutorStartFailureReturnsReason`）：T2 后空 receiver 会 `ErrNoDefault`。夹具必须预置默认载体，不是把失败改写成「未给 receiver」。
- Ticket 0 金样本绿 ≠ Dispatch 已走默认载体。

## 2026-09-07 判断

- 今日从声明缝得不到本卡要锁的结果：handleDispatch 不经 ResolveLookup；Admit 不能直派载体；bindingFor 仍覆盖物理；Task.carrier 不落盘；CLI 无 `--receiver` 且全局 `--target` 写入 body。Ticket 0 空壳不是活路径。
- DAG 点亮活路径：T1 先接线编制门面（Put 冲突闸 / OccupancyKeys / AdmitCarrier / 默认 singleton / bindingFor 退役覆盖）。T5 可与 T1 并行（store 列）。T3 消费已填身份（依赖 T5 落盘）。T2 共享准备链依赖 T1+T5，成功路径还要 T3 把 Carrier 写入 Task。T4 依赖 T2 真的 Admit 过。T6 依赖 T2 HTTP `receiver`。T7 收全部。
- 推荐提交序：T1 → T5 → T3 → T2 → T4 → T6 → T7。与 breakdown 图的「T1∥T5 然后 T2∥T3」相容：T2 错误路径可先于 T3，成功路径与 T3 同批或之后。
- P1–P11 全部按 (a) 落笔。禁止扇出、禁止新 HTTP、禁止 Manager import scheduling、禁止全局 `--target` 当 overlay、禁止有效 Covers 覆盖物理、禁止裸派发发明无卡队列。
- 未调 handoff CLI；未写实现代码。
- 计划已落盘 `docs/superpowers/plans/b233.5-plan.md`。T1–T7 按拍板 (a)。内部锁：T3 Dispatch 源码不含 ResolveLookup（已声明「从声明缝构造不出」）。真机清单归协调者。
- `codegraph --repo . --view cards-B233.5-charter resolve --doc docs/superpowers/plans/b233.5-plan.md`：anchors 30，全部 `ok` 或 `moved`，无 unresolved。`bindingFor` / `withCarrierHome` 为 moved 且无 nodeId（图覆盖债，与拆解 §6 同款）。
- 占位符扫描：正文无 TBD 待填；命中「占位」的是 SQL 占位符、`失败不得占位`、节名「占位符扫描」。
- 自审：未调 handoff CLI；未写实现代码；基线命令均亲自跑到退出码。

## 提交（历史读数，随后 amend 一次）

```
git commit -m "docs(B233.5): 实现计划（载体与小队统一派发）"
[cards/B233.5-charter-3 6dfdf5d1] docs(B233.5): 实现计划（载体与小队统一派发）
 2 files changed, 1714 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.5-plan-ledger.md
 create mode 100644 docs/superpowers/plans/b233.5-plan.md
```
