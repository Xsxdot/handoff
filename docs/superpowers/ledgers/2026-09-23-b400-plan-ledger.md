# B400 plan 台账（2026-09-23）

卡 B400 · 入口节点 charter:plan · 产出 `docs/superpowers/plans/b400-plan.md`。
角色：plan 节点，只出计划不写实现。基线分支 `cards/B400-charter-2`，起手 HEAD `54585a62`。
所有命令在本工作树 `/root/.handoff/worktrees/fc645031` 亲跑；临时 git 探针在 `$TMPDIR` 下。

---

## §1 起手核对

```text
$ git status --short
（clean）
$ git branch --show-current
cards/B400-charter-2
$ git log --oneline -3
54585a62 spec(B400) 批准回写: 已批准（2026-09-23）
d16472a5 spec(B400) 修订: 补豁免通道/无附件前提/远端树约束（独立审核后）
c7b3dd11 merge(B398): 冻结物理身份与路由 target 分字段（FrozenTarget）+ 图对账视图——并入功能线
```

- 读 `docs/superpowers/specs/b400.md`（已批准）。spec 要求：首节点派发前校验本卡附件路径
  在解析出的基线远端 commit 树中，缺则拒发；显式 base_branch 豁免；无附件放行。

## §2 基线亲跑读数

```text
$ go version
go version go1.26.1 linux/amd64

$ go build ./...
BUILD_EXIT=0

$ go vet ./internal/ledgerstep/ ./internal/ledger/ ./internal/workspace/ ./internal/agentd/
VET_EXIT=0

$ go test ./internal/ledgerstep/ -count=1 -timeout 300s
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	13.196s
```

### git 行为探针（$TMPDIR 真跑）

```text
$ git ls-tree -r --name-only <sha> -- ':(literal)docs/superpowers/specs/b400.md'
docs/superpowers/specs/b400.md
exit=0
$ git ls-tree -r --name-only <sha> -- ':(literal)docs/superpowers/plans/b400-plan.md'
（空输出）
exit=0
$ git cat-file -e <sha>:docs/superpowers/plans/b400-plan.md
fatal: path '...' does not exist in '<sha>'
present_exit=0 / absent_exit 非零
```

克隆 origin/HEAD 自检（第二个探针目录）：

```text
$ git -C <clone> symbolic-ref --short refs/remotes/origin/HEAD
origin/main
$ git -C <clone> show-ref --verify --quiet refs/heads/main && echo yes
yes
$ git -C <clone> ls-tree -r --name-only origin/main -- ':(literal)README.md'
README.md
$ git -C <clone> ls-tree -r --name-only origin/main -- ':(literal)docs/superpowers/specs/b400.md'
（空）
```

结论（写入 plan §4「git 行为事实」）：`ls-tree ... -- ':(literal)<path>'` 在场输出非空、缺失空输出且退出 0；
克隆自动带 `refs/remotes/origin/HEAD`；`git ls-tree` 全仓此前零命中，需新增只读 `workspace.BaseTreeMissingPaths`。

## §3 关键判断（落进 plan）

- 首派判定复用既有 `Store.WorkBranch`（`ErrNotFound` ＝ 无非审阅 dispatched 快照），
  `internal/ledger` **零代码改动**；豁免读 `Card.BaseBranch`（卡自有字段）。
  这是本计划与 spec §2「改动落在 ledger」的字面偏差，已在 plan §2/§13 明示。
- 护栏插在 `ViaTemplate` 的 `resolveDefaultBase := base == ""` 之后、`Transport` 之前，
  保证 `SetCardBaseBranch` 冻结前可改基线（spec §5 冻结点不动）。
- 探针注入 `Dispatcher.ProbeBaseAttachments`，生产实现在 `internal/agentd`（本机项目位置 →
  `workspace.BaseTreeMissingPaths`），不新增依赖方向、不改 wire。
- P1：本机探不到项目位置时 fail-open（返回 `ErrBaseProbeUnavailable`，护栏跳过并告警），
  避免误杀「项目只登记在远端」的派发；已作为 §0 待拍板项给出，推荐甲。
- 红锚：T1 端到端用例只用既有符号，基线可编译会红；T2/T3 单测引用新增符号故无红（plan §12 声明）。

## §4 读过的源码（定位依据）

- `internal/ledgerstep/dispatch.go`（`viaTemplate` 派发决议段 `:201-202` hasWorkBranch、`:283-296` base/resolveDefaultBase）
- `internal/ledger/cards.go`（`SetCardBaseBranch` `:642-688` 冻结点）
- `internal/ledger/events.go`（`WorkBranch` `:575-613`、`RecordDispatch` `:150`）
- `internal/ledger/relations.go`（`EffectiveBaseBranch` `:194-205`）
- `internal/ledger/types.go`（`Attachment` `:126-129`、`Card.BaseBranch` `:145`）
- `internal/workspace/gitworkspace.go`（`ResolveDefaultBaseBranch` `:1328`、`ResolveDispatchBase` `:1078`、`needsBaseBranchSync` `:1045`）
- `internal/workspace/projectresolve.go`（`ResolveProject` `:79`、`ErrProjectNotRegistered` `:47`）
- `internal/store/projects.go`（`ListProjectLocations` `:99`、`CreateProjectLocation` `:59`）
- `internal/agentd/cardstep.go`（Dispatcher 装配 `:199-218`、`startCardStep` `:129`）
- `internal/agentd/server.go`（`st *store.Store` `:109`、`runStepFn` `:243-245`）
- `internal/agentd/ledgerapi.go`（附件 kind 白名单 `:777`）
- `deploy/workflows/charter-v4.json`（contract/plan 节点 gate=spec；produces kind）
- 既有计划体例：`docs/superpowers/plans/b396-plan.md`、`b398-plan.md`

## §5 夹具亲读（签名写进 plan §4）

- `internal/agentd/ledgerapi_test.go:139 newLedgerEnv`；`:44 ledgerPost`
- `internal/agentd/ledger_fixtures_test.go:13 seedAgentdLedger`
- `internal/agentd/cardstep_discipline_test.go:131 seedDisciplineOnLedger`
- `internal/agentd/cardstep_test.go:88 waitFor`
- `internal/ledgerstep/dispatch_test.go:20 seedLedgerStepStore`、`:48 dispatchTestCard`
- `internal/workspace/gittesthelpers_test.go:110 newOriginAndClone`、`:129 commitOnOrigin`、`:19 gitAt`

## §6 偏差与未验证

- **未用 codegraph**：执行机（linux-01）未安装，协调者指示跳过（工具在协调者本机）。按 spec §6
  接缝清单直读源码；不记「未命中符号」为图覆盖债。
- **已验证**：T1 红锚——本节点以同形临时文件（`internal/agentd/b400_tmp_probe_test.go`，跑完已删）
  在基线实跑，确认（a）测试编译得过、（b）生产装配（HTTP step→startCardStep→runStep→ViaTemplate）
  与真仓库/探针调用链走通、（c）因护栏缺失而红。原文见 §7。
- **未验证**：T2~T4 测试的绿（本节点不写实现）；真机清单（plan §11）；agentd/workspace 目标子集回归。
  实现卡必须自跑并抄原文。

## §7 T1 红锚原型实跑（临时文件，跑完已删）

```text
$ go test ./internal/agentd/ -run TestB400TmpFirstDispatchRejectsWhenSpecMissingOnDefaultBase -count=1 -timeout 120s
... INFO 模板派发目标已归一 card=B1 template=feature-impl raw_target="" target="" frozen_target=""
... INFO 按模板派发 card=B1 node=impl ... branch=cards/B1-implement base="" resolve_default_base=true ...
... WARN 模板派发传输失败 card=B1 ... cause="Transport 不应被触达：基线护栏缺失"
... WARN 派发失败，转等人 node=impl card=B1 cause="派发: Transport 不应被触达：基线护栏缺失"
--- FAIL: TestB400TmpFirstDispatchRejectsWhenSpecMissingOnDefaultBase (0.99s)
    b400_tmp_probe_test.go:137: 护栏未在 Transport 前拦下（transportCalled=1）
FAIL	github.com/Xsxdot/handoff/internal/agentd	0.994s
```

解读：护栏不存在 ⇒ `resolve_default_base=true`、`base=""` 下派发直达被替换的 Transport，
`transportCalled=1` ⇒ T1 断言在基线红。这正是 plan §3「最薄路径条」要的红锚。
临时文件删除后 `git status --short` 只剩本节点两个产出物。

## §8 产出与提交

- 产出：`docs/superpowers/plans/b400-plan.md`（本节点法定路径）；本台账。
- 提交事实（历史读数，随后 amend 一次收进本批提交；amend 会换 hash，不回写本行）：

```text
$ git add docs/superpowers/plans/b400-plan.md docs/superpowers/ledgers/2026-09-23-b400-plan-ledger.md
$ git commit -q -m "plan(B400): 新卡首个节点的基线护栏——实现计划 + 台账"
$ git log --oneline -1
a1a7a113 plan(B400): 新卡首个节点的基线护栏——实现计划 + 台账
```
