# Ledger CLI B156.1 implementation ledger

- Baseline: `816249da`; `go build ./...` and `go test ./...` passed before Task 1.
- Task 1 completed: added the direct ledger CLI base with config DSN/SQLite fallback, default workflow seeding, actor identity, destructive-action confirmation, and the reusable in-process test harness; commit scope: `cmd/ledgercli.go cmd/ledgercli_test.go docs/superpowers/ledgers/2026-08-19-b156.1-command-surface.md`.
- Task 1 verification: `go build ./cmd/` passed; `go test ./cmd/ -run TestOpenLedgerFallback -v` remains expected to fail before workflow registration with `unknown command "workflow" for "handoff"`.
