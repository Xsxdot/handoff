# B402 plan 台账

节点：charter:plan（B402）。产出物：`docs/superpowers/plans/b402-plan.md`。本文件与本节点产出物同批提交。
角色：只出计划，不写实现代码、不建脚手架、不派发、不调 handoff CLI、不起新 executor。

## 2026-09-25 开节点与基线

### `git branch --show-current` / `git log --oneline -3`

```text
cards/B402-charter-6
2fb6a9d7 breakdown(B402): 拍板 P1-P5 拆解边界
4ab13fca breakdown(B402): 零文本单次续接拆解提案——代码单轮闭合 + S1-S5 测试/文档/图子卡 + P1-P5 待拍板
d83657fd contract(B402): 零文本失败单次自动续接契约冻结 + Ticket 0 骨架
```

有效基线 `cards/B402-spec` @ `5d6607fc`（`git merge-base HEAD 5d6607fc` = `5d6607fc`）。spec/contract/breakdown 三份文件头状态位实读一致（spec「已批准」、contract「上游状态：已批准 + 冻结状态」、breakdown「已拍板」）。

### 上游文件与 Ticket 0 在场（源码实读，非记忆）

- `internal/proto/failure.go`：`FailureClass`（`:15`）、`FailureClassZeroText="zero_text"`（`:23`）。
- `internal/executor/executor.go`：`Result.FailureClass`（`:96`）。
- `grep -n FailureClass` 五家 adapter：opencode `adapter.go:2232`、codex `:912`、grok `:679`、claudecode `:893`、agy `:691`，恰 5 处零文本分支。
- `internal/orchestration/contracts.go:142`：`FailedPayload.FailureClass`（omitempty）；`NewFailedPayload(...,class)`（`:163`）；`manager.go:3498` 透传 `r.FailureClass`。
- `internal/ledgerstep/wire.go`：`TurnEnd`（`:41`）、`failedPayload`（`:48`）、`failureClassOf`（`:55`）、`waitForTurnEnd`（`:88`）、`waitForTurnEndGrace`（`:135`）。
- `internal/ledgerstep/runner.go`：`ZeroTextContinueInstruction`（`:416`）、`awaitNode` 续接编排（`:426`，判零文本 `:450`）。
- `internal/ledgerstep/node.go`：无条件 `FinishTask` defer 已删；`:486` 显式归档、`:489` 归档失败转人工。
- 既有测试：`b402_retry_test.go`（四例 awaitNode + 两例 RunOnce）、`wire_test.go`（分类闭合）、`handleresult_notrailer_test.go`（additive）。

## 2026-09-25 亲跑基线（命令与原始输出）

```text
$ go build ./...                                              → BUILD_EXIT=0
$ go vet ./...                                                → VET_EXIT=0
$ go test ./internal/ledgerstep/ ./internal/proto/ ./internal/executor/... -count=1
    ok  internal/ledgerstep 14.932s
    ok  internal/proto 0.030s
    ok  internal/executor 0.210s
    ok  internal/executor/agy 0.400s
    ok  internal/executor/claudecode 4.143s
    ok  internal/executor/codex 6.038s
    ok  internal/executor/fake 0.007s
    ok  internal/executor/grok 1.439s
    ok  internal/executor/opencode 21.240s
    ok  internal/executor/rawtap 0.009s
    ok  internal/executor/turn 0.101s
    → SUBSET_EXIT=0
$ codegraph --repo . check                                   → CHECK_EXIT=0（viewContainers=338 / crossDomainEdges=1254 / misplacedSkipped=0）
$ codegraph --repo . --view cards-B402-charter check         → VIEW_CHECK_EXIT=0
$ codegraph --repo . validate                                → VALIDATE_EXIT=1
    issues = [cards-B272-charter] 新增容器 k_dropdir_fn 引用不存在的基线领域 d_coordination_api
             [cards-B374-charter] 新增容器 k_collab_model 已存在于基线，containersAdded 只接受新容器
    （两条均非本卡引入，不修）
$ grep -n 'B402' docs/roadmap.md                             → 无命中，GREP_EXIT=1
```

> 注：`go test ./internal/orchestration/ -count=1` 本节点未单独复跑（contract 台账记 78s 全绿），已列为 T0 门禁第 3 条必须复跑项，不写成结论。

## 2026-09-25 图查询（先查图，未命中再 grep）

```text
$ codegraph --repo . --view cards-B402-charter sym FailureClass            → m_proto_FailureClass（added, anchor=ok）
$ codegraph --repo . --view cards-B402-charter sym TurnEnd                 → m_ledgerstep_TurnEnd（added, anchor=ok）
$ codegraph --repo . --view cards-B402-charter sym failureClassOf          → n_ledgerstep_failureClassOf（added, anchor=ok）
$ codegraph --repo . --view cards-B402-charter sym awaitNode               → n_ledgerstep_StepRunner_awaitNode（modified, anchor=ok）
$ codegraph --repo . --view cards-B402-charter sym RunOnce                 → n_ledgerstep_NodeStep_RunOnce（anchor=ok, status=None → 触碰未登记 modified，扫描欠账）
$ codegraph --repo . --view cards-B402-charter sym handleResult/Continue   → 基线命中（普通路径）
$ codegraph --repo . --view cards-B402-charter sym ZeroTextContinueInstruction
    → 不在图中（近似候选: []）→ 记入图覆盖债，plan 对该常量只用普通路径 runner.go:416
```

plan 初稿的 `file#Symbol` 锚中 `executor.go#Result.FailureClass`、`contracts.go#FailedPayload.FailureClass` 被 `resolve` 判 vanished，`wire.go#failureClassOf` 判 file_missing；已按「图未覆盖用普通路径、不以 grep 冒充」改为 `path:line` 与全路径锚。

```text
$ codegraph --repo . resolve --doc docs/superpowers/plans/b402-plan.md
    → RESOLVE_EXIT=1（初稿：2 vanished + 1 file_missing）
    → 修正后 RESOLVE_EXIT=0（moved×4 / ok×1）
```

## 2026-09-25 计划落盘与裁决

- 产出：`docs/superpowers/plans/b402-plan.md`。结构：§0 待拍板（无新增，吸收 P1–P5）/ §1 现状 R1–R4 / §2 分流 / §3 DAG / §4 基线事实 / §5 接口契约 / §6 T0–T5 详情 / §7 缺陷族 / §8 预算 / §9 类型 / §10 接缝双向覆盖 / §11 真机 / §12 占位符扫描 / §13 自审三查 / 图覆盖债。
- 决定：P1=甲 下本 plan 不含生产代码 task；T0 门禁复核 Ticket 0；T1/T2/T3 只加测试、T4 只改 roadmap、T5 只碰 codegraph 产物。
- 两处 harness 复用例外已按纪律在 §12 显式声明（T2 早退矩阵 9 行断言列全 + 指认 `b402_retry_test.go:153-183`/`node_test.go:14,658,665`；T3 e2e 8 条断言列全 + 指认 `gateway_http_test.go` 与 `manager_test.go:276`、`fake.go`）。
- 未验证项：T1–T5 的实现（本节点只读）；真实供应商/跨机/旧 agentd 行为归 §11 真机。T0 基线四闸已亲跑到结果。

## 2026-09-25 收口（提交事实，历史读数）

```text
$ git add docs/superpowers/plans/b402-plan.md docs/superpowers/ledgers/2026-09-25-b402-plan-ledger.md
$ git commit -m "plan(B402): 零文本单次续接测试矩阵/e2e/文档/图收口实现计划 + 台账"
  [cards/B402-charter-6 ba7f2b94] plan(B402): 零文本单次续接测试矩阵/e2e/文档/图收口实现计划 + 台账
   2 files changed, 706 insertions(+)
   create mode 100644 docs/superpowers/ledgers/2026-09-25-b402-plan-ledger.md
   create mode 100644 docs/superpowers/plans/b402-plan.md
  → COMMIT_EXIT=0
```

随后按台账纪律追加本节并 `git commit --amend` 收进同批提交——amend 会换 hash，不把新 hash 回写本台账；收口判据是工作树干净。

提交前新鲜证据：`go build ./...` exit 0；`go vet ./...` exit 0；`go test ./internal/ledgerstep/ ./internal/proto/ ./internal/executor/... -count=1` exit 0；`codegraph check`/`--view cards-B402-charter check` 均 fails=0；`codegraph validate` 仅既存 B272/B374 两条；`codegraph resolve --doc docs/superpowers/plans/b402-plan.md` exit 0。

## 2026-09-25 交棒

产出：`docs/superpowers/plans/b402-plan.md`、本台账。下一节点：implement（T0 门禁 → T1/T2/T3/T4，T5 在测试落地后）。欠账：T0 需复跑 `./internal/orchestration/`；T5 扫描欠账见 plan 图覆盖债。
