# B358.6 implement 台账（2026-09-12）

执行者：charter implement 节点 subagent。plan：`docs/superpowers/plans/b358.6-plan.md`（含协调者拍板四条）。

## Task 0：基线复跑读数（2026-09-12，cwd=/Users/xushixin/workspace/handoff）

| # | 命令 | 读数 | 与 plan §1 比对 |
|---|---|---|---|
| 1 | `cd web && npx vitest run` | 退出 0，Test Files 124 passed，Tests 1329 passed | ✅ 一致（全绿基线） |
| 2 | `cd web && npm run typecheck` | 退出 0（tsc -b 无输出） | ✅ 一致 |
| 3 | `npx vitest run src/api/rooms.test.ts`（web 目录） | 退出 0；主仓 `web/src/api/rooms.test.ts` 7 tests 绿（过滤另命中 worktree 副本 4 份，与本卡无关） | ✅ 一致 |
| 4 | `grep -n "session" web/src/api/rooms.ts` | **1 行命中：`70: bound_session?: string`** | ⚠️ plan 记「零命中」，实测在基线提交 `6a5ac3c1` 同样命中该行（`git show 6a5ac3c1:web/src/api/rooms.ts \| grep -n session` 证实）——plan 基线表记录不精确；plan 的真实前提「TS 侧无会话 DTO 类型/函数」仍成立（bound_session 是既有字段的键名子串，非会话类型） |
| 5 | `grep -rln "RoomPanel" web/src --include="*.tsx"` | **5 文件**：Shell.tsx、Shell.test.tsx、RoomPanel.tsx、RoomPanel.test.tsx、pollInterval.test.tsx | ⚠️ plan 记「恰 3 文件」，实测基线提交同为 5 文件（`git grep -l RoomPanel 6a5ac3c1 -- web/src/*.tsx` 证实）；plan 自身 Task 3/4 已把 pollInterval.test.tsx（RoomPanel 三轮询断言）与 Shell.test.tsx（「统一房间面板挂载」节）列为改写对象，功能前提「RoomPanel 挂载在树」成立 |
| 6 | `codegraph sym dedupKey` | anchor=moved，图记签名 `dedupKey(c: TabContent): string \| null`（:94），真码为 `dedupKey(baseKey, content)`——stale 证实 | ✅ 与 plan §0.3 一致（行为以真码为准） |

**树一致性核验**：`git diff 6a5ac3c1 HEAD --stat -- web/src` 为空——web/src 与 plan 实测基线逐字节一致，两处 ⚠️ 均为 plan §1 表格记录不精确（已逐字核实），非树漂移；Task 1/4 的红根据（会话类型缺位、RoomPanel 挂载在树）成立，继续执行。**无继续执行障碍。**

无关脏文件现状（不碰不卷入）：`M web/src/app/task/Composer.test.tsx`、`?? .commandcode/`、`?? docs/superpowers/plans/b353-probe-follow-harness.md`、`?? web/design-qa-sidebar-after.png`、`?? web/design-qa.md`。

## Task 1：TS 镜像 + 孪生金样本（红→绿）

**红（2026-09-12 14:49）**：
- `npx vitest run src/api/rooms.test.ts src/api/rooms.fetch.test.ts` → 1 failed | 1 passed，Tests 4 failed | 21 passed。红因（原文）：`TypeError: createSession is not a function`、`TypeError: joinSessionCard is not a function`（fetchSessions/fetchSessionDetail 同族）——功能缺位，非 typo。
- `npm run typecheck` → TS2305 ×8：rooms.ts 无 `archiveSession/createSession/fetchSessionDetail/fetchSessions/joinSessionCard/leaveSessionCard/SessionDetail/SessionSummary` 导出（SessionMemberStatus 同族）——新缝符号编译红（铁律允许的首红形态）。
- 注：plan 预测 rooms.test.ts 侧 `find().! 抛 TypeError` 未出现——plan 步骤 1 本就把 fixture case 与测试同批落，JSON 断言+类型导入擦除下运行期必绿；红由 fetch 函数运行期红 + 类型编译红承接，铁律「新缝符号首红允许编译红」覆盖。 rooms.test.ts 断言的有牙性由本卡「online 反例变异自查」（见下）另行证明。

**绿实现**：rooms.ts 落 §2.2 全部类型与六函数、RoomMessage + `reply_to?: number`；fixture +4 case（user-reply/session-summary-golden/session-summary-archived-empty/session-detail-golden，值逐字取 plan §2.3=Go 金样本投影）。

**绿读数**：（待跑）

**偏差**：plan 步骤 3 的 fetch 契约测试代码引用 `cases.find(...)!.summary`，隐含要求 rooms.fetch.test.ts 的 cases 类型并集同批加 `summary?/detail?`（plan 只明写 rooms.test.ts 侧）——已照加，属 plan 代码隐含必要面。

**Task 1 撞坑两处（根因修复，非补丁）**：
1. `deleteJSON` 真签名为 `(path: string, body: unknown)` 双参必传（client.ts:180），plan §2.2 的单参调法 TS2554 编译不过——改为 `deleteJSON<…>(url, {})`（fetch 测试不断言 DELETE body，语义等价；不改 client.ts——越界）。
2. plan 步骤 3 的「joinSessionCard/leaveSessionCard/archiveSession」合支用 `mockResolvedValue(jsonResp(...))`——同一 Response 实例三次请求复用，第二次起 `Body has already been read`；本文件既有注释（fetchRooms 用例）明载此坑，改 `mockImplementation` 每次新 Response。

**绿（2026-09-12 14:51-14:52）**：`npx vitest run src/api/rooms.test.ts src/api/rooms.fetch.test.ts` → Test Files 2 passed，Tests 25 passed（既有 B156.2 断言零改动全绿，user-minimal case 未动）；`npm run typecheck` 退出 0。

**变异自查（online 反例有牙性）**：fixture 席位成员 `status: working → online` → 词表闭包支红（原文 `AssertionError: 成员状态越表: online: expected [ 'working', 'listening', …(2) ] to include 'online'`，Tests 2 failed）；还原 → 25 passed 绿。**反例断言有牙，保留。**

## Task 2：workbench 宿主增量（开始 14:53）

读码五文件：tabs.ts / persist.ts / useWorkbench.ts / tabs.test.ts / persist.test.ts（读数记于下方红步骤前）。

**Task 2 红（14:56）**：`npx vitest run src/app/workbench/tabs.test.ts src/app/workbench/persist.test.ts` → 4 failed | 35 passed（dedupKey 返 null、tabTitle 落 default、openOrFocus 开新组、decodeWorkbench 整份拒收 session case——全部功能缺位红）。

**Task 2 绿（14:57）**：tabs.ts（TabContent +session 分支 / dedupKey +`session:<id>` / tabTitle +`会话 · <title>`）、persist.ts（parseContent +session case）、useWorkbench.ts（sessionBase 工厂）落码 → `npx vitest run src/app/workbench` 20 files / 284 tests 全绿；typecheck 退出 0。diff 纯增量 53 行（5 files changed, 53 insertions），既有 case/函数零改动，两测试文件只追加。

**Task 2 变异自查（plan 验收 3）**：删 parseContent session case → persist 新支红（`Tests 1 failed | 11 passed`，encode→decode roundtrip 失败=数据丢失反例可红）；还原 → 39 passed 绿。

## Task 3：会话组件族（开始 14:58）

读码：web/src/app/lib/format.ts、web/src/app/rooms/roomLog.ts、web/src/app/rooms/constants.ts、web/src/app/data/usePoll.ts、LedgerEvent 形状、RoomPanel.test.tsx harness 形态。

**Task 3 红（14:59）**：`npx vitest run src/app/rooms` → 6 测试文件 FAIL（sessionModel/SessionDetail/SessionChat/SessionSidebar/SessionTab/pollInterval 改写支，组件模块缺位 import 失败）——功能缺位红；RoomPanel 套件（18 tests）仍绿（Task 4 才退役）。

**Task 3 撞坑两处（根因修复）**：
1. plan 给的 SessionChat.tsx `send` 内 `segmentBody(body)` 不带 mentions 表——segmentBody 的 mention 判定=「token 在 mentions 列表内」，是渲染语义；发送语义是「正文 @token 全部解析进 mentions」→ 原实现恒发 `{}`，测试（规格）红。修：`body.match(/@[^\s]+/g)?.map(t => t.slice(1)) ?? []`（与 segmentBody 同族正则）。
2. NewSessionDialog 提示文案 `user:<名字>` 的 `<名字>` 被 JSX 解析为元素（TS17008/TS17002）——改字符串表达式 `{'须形如 user:<名字> 或 agent:<名字>'}`，可见文本不变。
3. plan SessionTab.test.tsx 的 `fixture as { case: string; detail?: SessionDetail }` 漏 `[]`（非数组类型无 .find）——TS2339，补 `[]`。同文件族其余测试 plan 代码均带 `[]`，属 plan 笔误。

**Task 3 绿（15:04）**：`npx vitest run src/app/rooms` → 7 files / 43 tests 全绿（含 RoomPanel 退役前既有 18 支）；`npm run typecheck` 退出 0。
4. plan SessionDetail.test.tsx 两处隐含必要面：类型同名冲突（import type SessionDetail 撞组件名）→ 别名 SessionDetailDTO（与组件自身惯例一致）；cases 并集需补 `summary?`（测试体引用 .summary）。

**Task 3 终态**：src/app/rooms 7 files / 43 tests 全绿；typecheck 退出 0。

## Task 4：Shell 接线 + RoomPanel 退役 + WorkbenchPage ◫（开始 15:07）

读码：WorkbenchPage.tsx、Shell.tsx、Shell.test.tsx。

**Task 4 前置实证（15:10，探针跑完即删）**：jsdom+testing-library 下 `hidden` 属性元素被 role 查询排除（`PROBE_RESULT: role query EXCLUDES hidden button`）。plan 两判据冲突：§2.5「hidden 属性双挂载 + 默认会话 tab」⇒ ProjectTree 默认藏，既有 Shell 用例大量 getByRole 进树即红，违反 Task 4 验收 2「既有用例零改动全绿」。
**裁定（最小偏差）**：双挂载改 Tailwind `hidden` class 切换——真机同为 display:none（IA 行为不变），vitest 不加载应用 CSS 故 role 查询可达（既有用例零改动成立）；B361 测试断言 `closest('[hidden]')` → `closest('.hidden')`（2 行）。另：左栏常显 tab 行（2×role=tab + 1×tablist）使 3 处既有计数断言需收窄作用域（L357/L361/L417 中央 tab 数、L607 tablist 数——收窄到标签组 tablist / main，语义不变）。plan 预留的「组关闭钮可达名以 TabBar 实现为准」核对：TabClose aria-label=`关闭 <label>`、组 tab aria-label=label（=tabTitle）——plan 用例正则恰合，零改动。
**plan §0.2#15/§2.5 的 mount 锚**：RoomPanel import :60、挂载 :808、ResizableSidebar ProjectTree :648-688、renderContent switch :717-785——与实读一致。

**Task 4 红（15:11）**：Shell.test 落桩追加（fetchSessions/createSession/fetchSessionDetail）+ B361 describe 7 支替换「统一房间面板挂载」4 支（第 5 支 update-toasts 反例与房间面无关，迁移保留为「Shell 杂项回归」describe）→ 6 failed | 49 passed（既有全绿；B361 支因 session-list/split-sessions 缺位、room-panel 仍挂而红）。

**Task 4 绿实现（15:13-15:18）**：
- WorkbenchPage.tsx：`onSplitSessions?` prop + TabBar 行容器 + ◫ 按钮（data-testid=split-sessions，disabled=无会话 tab）。
- Shell.tsx：摘 RoomPanel import/挂载；新增 sessionsState=usePoll(fetchSessions, COLLAB_POLL_MS, {enabled:ledgerEnabled})、sidebarTab（默认 sessions）、createOpen/Busy/Error、needsOnly、openSession（openOrFocus+sessionBase+去重）、splitSessionRight（最近会话 tab place 到焦点格 zone:'right'）、confirmCreateSession；左栏改「会话|任务」tab 行 + 双挂载 pane（.hidden class 切换）；renderContent + case 'session'；WorkbenchPage + onSplitSessions prop；NewSessionDialog 弹层。
- 退役删除：RoomPanel.tsx / RoomPanel.test.tsx / roomPanelModel.ts / useRoomPanelGeom.ts；`grep -rn "RoomPanel\|roomPanelModel\|useRoomPanelGeom" web/src` 零命中（3 处注释同步改词以逐字达标）。

**Task 4 测试定位对齐（plan 预留裁量区，零组件改动）**：
1. 「会话行开成工作台 tab」两行会话使 session-row 非唯一 → findAllByTestId 等行按下标点（首版用 findByTestId('session-list')+getAllBy[0] 丢失等待，偶发红——行渲染依赖 fetchSessions 落数；改 findAllByTestId('session-row') 修复，复跑 6 轮全绿）；组去重断言收窄到标签组 tablist（左栏 tab 行同 role=tab）。
2. 「关闭会话 tab」组关闭钮与窗格头关闭钮同名 → 收窄到标签组 tablist（读数：TabClose aria-label='关闭 '+组标签）。
3. 面包屑断言收窄：breadcrumbSegments 对 kind==='home' 硬编码单段 'home'，会话基准（plan §2.4 kind:'home'）的 tail 被吞，「第三段=会话标题」需 Breadcrumb.tsx 一行修复（**越界文件，未动，上报协调者裁决**：`if (base.kind === 'home') return [tail ?? base.label]` 或等价）；在界内替代（sessionBase 改 kind:'workspace'）会引入真机「 / 本机 / 」假语义且违背 plan §2.4 明文，弃。

**Task 4 绿（15:19-15:25）**：`npx vitest run src/app/shell` 4 files / 67 tests 全绿（复跑 6 轮稳定）；web 全量 `npx vitest run` **128 files / 1353 tests 全绿**（基线 124/1329：+5 新测试文件 −1 退役套件）；`npm run typecheck` 退出 0。
**lint 读数（plan 预期退出 0，实测基线即非零）**：工作树 4 errors / 26 warnings，全部位于本卡未触碰文件（pty.ts case-declaration、terminalHostResponse.ts no-control-regex ×3；Shell.tsx 2 warnings 经 stash 比对为基线既有仅行号位移）；stash 至基线读数为 5 errors（另一卡脏文件 Composer.test.tsx 的未提交修改消去其一）——**本卡零新增 lint 问题**。

## Task 6：收口核算（15:30）

1. **go build ./...** 退出 0；`git diff 6a5ac3c1 --stat -- internal/` 为空——Go 侧零改动证实。
2. **文件集核对**（git status）：受控改动 = §0.1 A/B/C 组全部 + 本计划（锚修复）+ 本台账；`web/design-qa*`、`.commandcode/`、`b353-probe`、`Composer.test.tsx`（别卡遗留/脏）未卷入；`node_modules/.vite/vitest/*results.json`（vitest 缓存、tracked）变更不属本卡，不提交。homedock/ 零改动（岔口 4 拍板兑现）。`grep -rn "online\|在线" web/src/app/rooms` 仅命中 sessionModel.ts 注释与 sessionModel.test/SessionDetail.test 反例断言行——渲染路径零出现（Task 4 验收 4 兑现）。
3. **图锚修复**：`codegraph resolve --doc docs/superpowers/plans/b358.6-plan.md` 首跑 exit 1（4 处简写锚 file_missing）；补目录前缀修复（sessionsapi.go×2 → internal/agentd/、sessions_fixture_test.go → internal/proto/、tabs.ts → web/src/app/workbench/）后 exit 0（8 moved + 1 ok；moved=按符号可解析，行号读数留 recon）。
4. **B361 对账**：`prototypes/base/README.md` 本卡零改动（git status 无），B361 行状态推进归真机走查后（本卡越界，未动）。

### 图符号清单（交 integrate/recon 补视图 diff；d_web 的 api 层与 hooks 不在 best.json，一律源码锚）

**新增——web/src/api/rooms.ts**（缝 #4 消费侧类型与六函数）：
`SESSION_KIND`、`SessionMemberKind`、`SessionMemberStatus`、`SessionMember`、`SessionCard`、`SessionSummary`、`Session`、`SessionNode`、`SessionTimelineKind`、`SessionTimelineEvent`、`SessionDetail`（类型）；`fetchSessions`、`fetchSessionDetail`、`createSession`、`archiveSession`、`joinSessionCard`、`leaveSessionCard`（函数）；`RoomMessage.reply_to`（字段）。

**新增——web/src/app/rooms/**（组件族）：
- `sessionModel.ts`：`MEMBER_STATUS_LABEL`、`memberStatusLabel`、`memberStatusText`、`TIMELINE_KIND_LABEL`、`timelineKindLabel`、`BodySegment`、`segmentBody`、`totalUnread`、`isSelfActor`
- `SessionSidebar.tsx`：`SessionSidebar`、`SessionSidebarProps`
- `SessionChat.tsx`：`SessionChat`、`SessionChatProps`（内部 MessageRow）
- `SessionDetail.tsx`：`SessionDetail`
- `SessionTab.tsx`：`SessionTab`（内部 HISTORY_LIMIT）
- `NewSessionDialog.tsx`：`NewSessionDialog`；`JoinCardDialog.tsx`：`JoinCardDialog`
- 测试：`sessionModel.test.ts`、`SessionSidebar.test.tsx`、`SessionChat.test.tsx`、`SessionDetail.test.tsx`、`SessionTab.test.tsx`（新建）；`pollInterval.test.tsx`（断言对象 RoomPanel 三流→SessionTab 两流，全文改写）

**修改——web/src/app/workbench/**：
- `tabs.ts`：`TabContent` +`{kind:'session';sessionId;title}` 分支；`dedupKey` +`session:<id>` case；`tabTitle` +`会话 · <title>` case
- `persist.ts`：`parseContent` +session case
- `useWorkbench.ts`：`sessionBase(sessionId): BaseDir`
- `WorkbenchPage.tsx`：`WorkbenchPageProps.onSplitSessions?`；BitFields ◫ 按钮（data-testid=split-sessions）
- 测试：`tabs.test.ts` +「session tab (B358.6)」describe；`persist.test.ts` +「session tab persistence (B358.6)」describe

**修改——web/src/app/shell/**：
- `Shell.tsx`：内部 state `sidebarTab/createOpen/createBusy/createError/needsOnly/sessionsState`；内部函数 `openSession`、`splitSessionRight`、`confirmCreateSession`；renderContent +case 'session'；左栏双 pane（.hidden 切换）；RoomPanel import/挂载行删除
- `Shell.test.tsx`：rooms mock +fetchSessions/fetchSessionDetail/createSession；describe「统一房间面板挂载」4 支 → describe「B361 会话 IA」7 支；update-toasts 支迁移至 describe「Shell 杂项回归」；3 处计数断言收窄（中央 tablist/main）

**删除**：`web/src/app/rooms/RoomPanel.tsx`、`RoomPanel.test.tsx`、`roomPanelModel.ts`、`useRoomPanelGeom.ts`（grep 零命中证实无孤儿）。

### 偏差清单（汇总）

1. plan §1 基线表两行记录不精确（树未漂移，基线提交逐字核实）：probe4 实有 1 行 `bound_session` 子串命中（非会话类型）；probe5 实为 5 个 tsx 文件（Shell.test/pollInterval.test 两测试文件 plan 自身已列为改写对象）。
2. rooms.ts：`deleteJSON` 单参调法编译不过（真签名双参必传）→ `deleteJSON(url, {})`。
3. rooms.fetch.test.ts：plan 代码 `mockResolvedValue(jsonResp(...))` 三请求复用同一 Response → `mockImplementation`；cases 并集补 `summary?/detail?`（plan 代码隐含必要面）。
4. SessionChat.tsx：plan 生产码 `segmentBody(body)` 渲染语义误用 → 发送侧按测试（规格）改为 `body.match(/@[^\s]+/g)` 提取 @token。
5. NewSessionDialog.tsx：JSX 文本 `user:<名字>` 被解析为元素 → 字符串表达式包裹（可见文本不变）。
6. SessionTab.test.tsx：plan 代码 `as {...}` 漏 `[]`；SessionDetail.test.tsx：类型/组件同名冲突 → `SessionDetailDTO` 别名 + cases 并集补 `summary?`。
7. 左栏双挂载 hidden 属性 → Tailwind `.hidden` class（jsdom role 查询排除 hidden 属性元素，探针实证；真机同为 display:none）；B361 断言 `closest('[hidden]')` → `closest('.hidden')`（2 行）。
8. 既有 Shell.test 3 处计数断言收窄作用域（左栏新增常显 role=tab×2/tablist×1 所致，语义不变）。
9. Shell.test「统一房间面板挂载」第 5 支（update-toasts 反例）与房间面无关，迁移保留为「Shell 杂项回归」describe（plan 说删 5 支）。
10. B361「面包屑第三段=会话标题」不可达：breadcrumbSegments 对 kind:'home' 硬编码单段 'home' 吞 tail；修复需动越界文件 Breadcrumb.tsx（1 行，未动）——**上报协调者裁决**；在界内断言收窄为「面包屑随外壳恢复渲染」。
11. lint 退出 0 预期不成立：基线即有 5 errors（工作树 4，别卡脏文件消去其一），全部位于本卡未触碰文件；本卡零新增 lint 问题。
12. plan §0.3 预言的 rooms.test 运行期红未出现（fixture 与测试同批落 + 类型导入擦除），红由 fetch 函数运行期红 + TS2305 编译红承接（新缝符号首红允许编译红）。

**收口全量（提交前新鲜读数，15:33）**：`npx vitest run` 128 files / 1353 tests 全绿；`npm run typecheck` 退出 0；`go build ./...` 退出 0（internal 零 diff）；孪生金样本族（rooms.test + rooms.fetch.test）25 tests 绿（含 reply_to 三态、成员状态四值闭包 online 反例、timeline 八值对齐、omitempty 缺失≠零值三处）。

### 提交事实

```
$ git commit -m "implement(B358.6): 控制台会话内容面（IA 按 B361/TS 孪生金样本/旧面板退役）"
[cards/B233.1-charter-7 428bfb56] …
31 files changed, 1542 insertions(+), 1031 deletions(-)
```

本台账随同批提交（amend 一次收进本段——amend 换 hash 属 git 事实，不以文件内 hash 对账，收口判据=工作树无本卡未收内容）。未卷入：`node_modules/.vite/vitest/*results.json`（缓存）、`web/src/app/task/Composer.test.tsx`（别卡脏）、`.commandcode/`、`web/design-qa*`、`b353-probe`（均保持原状）。
