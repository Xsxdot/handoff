# 节点化工作流执行 ledger

- Task 1 完成；spec 符合性 PASS、代码质量 PASS，无修复轮。测试先临时撤去生产类型后观察到预期编译失败，再恢复实现并通过目标回归；`go build ./...`、`go vet ./...` 通过，`go test ./internal/ledger/ -run 'TestWorkflow' -v` 通过，`gofmt -l .` 无输出。全量 `go test ./...` 的既有环境敏感失败原文见执行记录，`internal/ledger` 全包通过。提交范围：`internal/ledger/types.go`、`internal/ledger/workflows.go`、`internal/ledger/workflows_test.go`、本 ledger。
- Task 2 完成；spec 符合性 PASS、代码质量 PASS，无修复轮。`go test ./internal/ledger/ -run TestPutWorkflow -v` 通过，包含 9 个非法节点拒绝用例、合法节点接受用例及既有版本化回归；`go build ./...` 通过，`gofmt -l .` 无输出。全量 `go test ./...` 仍为同一组既有环境敏感失败，`internal/ledger` 全包通过。提交范围：`internal/ledger/workflows.go`、`internal/ledger/workflows_test.go`、本 ledger。
- Task 3 完成；spec 符合性 PASS、代码质量 PASS，无修复轮。`go test ./internal/ledgerstep/ -v` 与 `TestBuildPromptThreeSections` 通过；变异验证将 `if carry` 改为 `if false` 后目标测试按预期失败，恢复后通过；`go build ./...` 通过，`gofmt -l .` 无输出。全量 `go test ./...` 仍为既有环境敏感失败。提交范围：`internal/ledgerstep/dispatch.go`、`internal/ledgerstep/dispatch_test.go`、本 ledger。
- Task 4 修复轮 1：现有 runner 尚未在 Task 5 通用化，保留的 ReviewStep 已缩为只委托 NodeStep 的过渡装配器，删除重复决策实现；既有测试全部改用 NodeStep。
- Task 4 完成；spec 符合性 PASS、代码质量 PASS。`go test ./internal/ledgerstep/ -v`、`go build ./...`、`go vet ./internal/ledgerstep/`、`gofmt -l .` 通过；两条路由/人工基线变异验证按预期击穿并恢复。全量 `go test ./...` 仍为既有环境敏感失败。提交范围：`internal/ledgerstep/node.go`、`internal/ledgerstep/node_test.go`、本 ledger。
- Task 5 完成；spec 符合性 PASS、代码质量 PASS，无修复轮。`go build ./...`、`go vet ./...`、`go test ./internal/ledgerstep/ ./cmd/ -count=1`、agentd 节点装配回归及 `gofmt -l .` 通过；HTTP 的 implement 白名单测试留待 Task 7 的明确 API 收口。移除本地合并执行、兼容 ReviewStep、gitscript 及相关测试，Runner 改按卡钉版本节点名执行。提交范围：`internal/ledgerstep`、`internal/agentd/cardstep.go`、`internal/agentd/cardstep_test.go`、`cmd/card_node.go`、`cmd/card_node_test.go`、`cmd/card_dispatch.go`、本 ledger。
- Task 6 完成；spec 符合性 PASS、代码质量 PASS，无修复轮。目标 `go test ./internal/discipline/ ./internal/ledger/ -v -count=1`、纪律 API 回归、`go build ./...`、`go vet ./...`、`gofmt -l .` 通过；变异删除 `HumanBases: []string{"main"}` 后 `TestDefaultWorkflowsAreNodeForm` 按预期失败，已恢复。完整 `go test ./...` 仍有未改动 Task 7 API 断言 `TestCardStepRejectsImplement` 与既有环境敏感失败。提交范围：`internal/discipline`、`internal/ledger/workflows.go`、`internal/ledger/workflows_test.go`、`internal/agentd/discipline_test.go`、本 ledger。
- Task 7 完成；spec 符合性 PASS、代码质量 PASS，无修复轮。新增工作流单条读写、纪律块名列表与任意节点名 step API；`ErrBadState` 在 PUT 工作流接口映射为 400，implement 仍由 Web 层拒绝。`go test ./internal/agentd/ -v -count=1`、`go build ./...`、`go vet ./...`、`gofmt -l .` 通过。提交范围：`internal/agentd/ledgerapi.go`、`internal/agentd/ledgerapi_test.go`、本 ledger。
- Task 8 完成；spec 符合性 PASS、代码质量 PASS，无修复轮。`--step` 帮助与未知节点回归通过，README 已补节点化工作流说明；`go build ./...`、`go vet ./...`、`gofmt -l .` 通过。`go test ./... -count=1` 的原始失败为 `internal/client` 的 `TestCursorRootFallsBackToCwdWhenHomeUnwritable`、`TestCursorRootErrorNamesBothPaths`，`internal/config` 的 `TestLoadStripUpdateDoesNotBlockOnSaveFailure`，`internal/permgate` 的 3 个 socket 路径过长用例，以及 `internal/executor/grok` 的 `TestSyncAuthKeepsTaskCopyWhenWriteFails`；节点化相关包通过。提交范围：`README.md`、`cmd/card_node_test.go`、本 ledger。
