# 代码图一期执行记账

职责：记录 codegraph phase 1 各 task 的双裁决、修复轮次、提交范围与亲自执行的验证结果。
边界：只记录本分支证据；控制台真机验收留给审核者本地执行。

- 基线：`a65f913e`（代码图一期实现 plan），当前分支 `codegraph-phase1`，开工时工作树干净。
- Task 1 / 完成裁决：spec 符合（Graph/Node/Container/TestRef/Edge/Diff 字段、LoadGraph/LoadDiff/ListViews、路径错误上下文、缺失 diffs 目录返回空列表均实现）；代码质量符合（internal/codegraph 零内部依赖，职责/边界与导出 API 注释齐全，测试夹具可复用）。验证：`go test ./internal/codegraph/ -v` 实际通过（4 tests）；`gofmt -l internal/codegraph/` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 1 提交）。
- Task 2 / 完成裁决：spec 符合（Validate/ValidateDiff 覆盖容器、节点、边引用并确定性排序；Merge 支持纯基线、added/modified/deleted 节点与边状态，删除对象保留）；代码质量符合（校验/合并职责边界清晰，导出 API 注释完整，无内部依赖）。验证：`go test ./internal/codegraph/ -v` 实际通过（9 tests）；`gofmt -l internal/codegraph/` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 2 提交）。
- Task 3 / 完成裁决：spec 符合（Resolve 支持 id/精确名字与候选提示；Neighborhood 支持 chain/who-calls 多焦点并集、上下游深度、0/不限语义、删除残影跳过、未扫描统计与警示）；代码质量符合（BFS 结果确定性排序、错误带焦点上下文、无网络与内部依赖）。验证：`go test ./internal/codegraph/ -v` 实际通过（13 tests）；`gofmt -l internal/codegraph/` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 3 提交）。
- Task 4 / 完成裁决：spec 符合（CheckStale 覆盖文件存在、行号界内、func/model 名字 token 窗口；entry 仅做前两级；unscanned 跳过；同文件缓存与稳定排序均实现）；代码质量符合（只读报告、不重扫不修复，导出结果带结构化原因）。验证：`go test ./internal/codegraph/ -v` 实际通过（15 tests）；`gofmt -l internal/codegraph/` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 4 提交）。
- Task 5 / 完成裁决：spec 符合（graph validate/views/chain/who-calls 均为本地只读 JSON CLI；--repo/--depth/--view/--stale 已接入；默认深度 2、0 不限、查询顶层含 unscannedEntries/warning；错误经 RunE 返回）；代码质量符合（CLI 薄壳复用 codegraph，输出使用 OutOrStdout，进程内测试清理 flag 状态，无网络调用）。验证：`go test ./cmd/ -run TestGraph -v` 实际通过（5 tests）；真实冒烟 `go run . graph validate --repo .` 实际退出 0，输出 nodes=189/edges=132/containers=16/issues=null/unscannedEntries=96；chain/who-calls 实际退出 0 且输出目标链路；`gofmt -l cmd/` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 5 提交）。
- Task 6 / 完成裁决：spec 符合（扫描用法、产物、Meta/Container/TestRef/Node/Graph/Diff 全字段表与硬纪律齐全，验收命令明确）；代码质量符合（纯文档、字段说明与 types.go 对齐、未引入运行时代码）。验证：`gofmt -l .` 实际无输出；`git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 6 提交）。
- Task 7 / 修复轮 1：测试初写的原始编译错误为 `internal/agentd/codegraph_test.go:5:2: "encoding/json" imported and not used`、`undefined: os`、`undefined: strings`、`undefined: url`；补齐测试 imports 后，未注册路由阶段原始失败为 `json: cannot unmarshal number into Go value of type struct { Baseline codegraph.Graph ... }` 与同类 source 响应错误，随后实现 handler/路由。
- Task 7 / 完成裁决：spec 符合（GET 项目代码图返回 baseline/views/stale；项目不存在与无 baseline 404；source 支持 span 默认/上限、越界截断、相对路径校验与 machine 转发）；代码质量符合（handler 只读，入口/错误 cause/成功统计日志齐全，复用 store、codegraph、writeJSON、forwardIfRequested）。验证：`go test ./internal/agentd/ -run TestCodegraph -v` 实际通过（2 tests）；`go test ./internal/agentd/` 实际通过（110.865s）；`gofmt -l internal/agentd/` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 7 提交）。
- Task 8 / 环境记录：前端首次 vitest 原始环境错误为 `npm error code EROFS ... /root/.npm/_cacache/tmp/*** ... read-only file system`；提升权限后因无 node_modules 原始错误为 `Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@tailwindcss/vite'`，随后 `npm ci` 实际成功（added 290 packages，found 0 vulnerabilities）。
- Task 8 / 修复轮 1：首次构建原始错误为 `src/app/codegraph/graphmath.ts(80,3): error TS6133: 'NODE_W' is declared but its value is never read.`；布局改用 `xStep = max(XSP, NODE_W)` 后重跑通过。
- Task 8 / 完成裁决：spec 符合（CgNode/CgGraph/CgDiff/CgStaleNode/CodegraphResp/CgSourceResp、两个 API 请求、mergeView/scannedEntries/buildAdj/neighborhood/layoutBands/chainTree 均实现；纯函数与 Go 侧 deleted/BFS 语义一致）；代码质量符合（算法独立于组件、请求复用既有 request、类型检查无新增错误）。验证：`npx vitest run src/app/codegraph/graphmath.test.ts` 实际通过（7 tests）；`npx tsc --noEmit -p .` 实际通过；`npm run build` 实际通过；`gofmt -l .` 与 `git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 8 提交）。
- Task 9 / 修复轮 1：复核发现可选 `onOpenCodegraph` 未传入时仍渲染无效按钮；改为仅在 prop 存在时渲染，保持既有 ProjectTree 调用方形状。
- Task 9 / 修复轮 2：eslint 原始错误为 `CodegraphPage.tsx 95:73 Expected an assignment or function call and instead saw an expression`，以及占位组件 `_project/_view/_nodeId/_stale/_onJump`、`_view/_foci/_depth/_staleIds/...`、`_entry/_onSelect` 的 `@typescript-eslint/no-unused-vars`；改为显式 props 使用与 if 分支后清零。
- Task 9 / 完成裁决：spec 符合（useCodegraph 一次性加载、页面工具条/状态机/三栏外壳、CallTree 入口与焦点事件、/codegraph 路由及左栏入口均实现；Task 10/11 所需三个占位保持同签名）；代码质量符合（可选入口不扰动旧调用方，组件只承载状态/渲染，eslint 无错误）。验证：`npx vitest run src/app/codegraph/` 实际通过（8 tests）；`npx vitest run src/app/tree/` 实际通过（105 tests）；`npx vitest run src/app/shell/Shell.test.tsx` 实际通过（19 tests）；`npx eslint ...` 实际 0 errors、3 个既有 warnings；`npx tsc --noEmit -p .`、`npm run build`、`gofmt -l .`、`git diff --check` 实际通过/无输出。Commit 范围：`HEAD^..HEAD`（Task 9 提交）。
- Task 10 / 失败测试阶段：占位 FocusGraph 测试原始失败为 3 tests，分别报 `Cannot read properties of undefined (reading 'style')`、`Unable to fire a "click" event - please provide a DOM element`、`Unable to find an element with the text: runE`；替换真实组件后通过。
- Task 10 / 修复轮 1：eslint 原始警告为 `FocusGraph.tsx 34:38 React Hook useEffect has a complex expression in the dependency array`；提取 `focusKey` 后清零。
- Task 10 / 完成裁决：spec 符合（FocusGraph 支持上游/焦点/下游竖向层级、焦点与并集点击、历史按钮、层级选择、空白拖动/滚轮平移、ctrl/cmd 光标缩放、差异染色、deleted 边过滤、stale 标记）；代码质量符合（布局与邻域算法复用 graphmath，hook 依赖无 lint 警告，缩放范围 0.3–2.5）。验证：`npx vitest run src/app/codegraph/` 实际通过（11 tests）；`npx tsc --noEmit -p .`、`npm run build`、`npx eslint src/app/codegraph/FocusGraph.tsx` 实际通过；`gofmt -l .` 与 `git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 10 提交）。
- Task 11 / 失败测试阶段：占位 DetailPanel 测试原始失败为 `Unable to find an element with the text: func Do(ctx context.Context) error`。
- Task 11 / 完成裁决：spec 符合（DetailPanel 展示职责/新旧签名/参数/返回/字段/测试/调用关系/失鲜/实时源码窗口并支持 onJump；SeqView 按 DFS 调用顺序绘制容器列、跨列箭头与同列回环）；代码质量符合（源码读取复用 fetchCodegraphSource，图遍历复用 buildAdj，空入口安全返回）。验证：`npx vitest run src/app/codegraph/DetailPanel.test.tsx` 实际通过（1 test）；`npx vitest run` 实际通过（81 files/797 tests）；`npx tsc --noEmit -p .`、`npm run build`、`npx eslint src/app/codegraph` 实际通过；`gofmt -l .` 与 `git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 11 提交）。
- Task 12 / 后端全量原始失败记录：`go build ./...` 实际通过；随后 `go test ./...` 原始失败包括 `--- FAIL: TestCursorRootFallsBackToCwdWhenHomeUnwritable`、`--- FAIL: TestCursorRootErrorNamesBothPaths`、`--- FAIL: TestLoadStripUpdateDoesNotBlockOnSaveFailure`、`--- FAIL: TestPermServerAskThenRespond`/`RespondUnknownID`/`ReRegisterSameID`（裁决 socket 路径过长）、`--- FAIL: TestSyncAuthKeepsTaskCopyWhenWriteFails`，最终包失败含 `FAIL github.com/Xsxdot/handoff/internal/client`、`FAIL github.com/Xsxdot/handoff/internal/config`、`FAIL github.com/Xsxdot/handoff/internal/executor/grok`；这些均为仓库既有环境敏感测试，未改无关判据。代码图相关包复验 `go test ./internal/codegraph ./cmd ./internal/agentd` 实际通过；`gofmt -l .` 实际无输出。
- Task 12 / 前端全量：`npm run test` 实际通过（81 files/797 tests）；`npx tsc --noEmit -p .` 与 `npm run build` 实际通过，build 仅有 Vite chunk size 提示。
- Task 12 / 真实 CLI：`go run . graph validate --repo . --stale` 实际退出 1，输出 nodes=189/edges=132/issues=null/views=[]/unscannedEntries=96/stale=5，失鲜节点为 n_handleContinue、n_handleEvents、n_handleStatus、n_handleTaskBundle、n_taskOrErr；`go run . graph who-calls Server.handleDispatch --repo . --depth 0` 实际退出 0，输出含 e_dispatch/e_continue/e_pull/e_wait 等 CLI 入口层。
- Task 12 / 终审修复轮 1：完整 diff 终审发现两项质量问题并一次性修复：CLI 无 diff 时 views 从 null 正规化为空数组；Merge 对非法新增边按边界注释跳过并补回归测试。修复后 `go test ./internal/codegraph ./cmd ./internal/agentd` 实际通过，`gofmt -l .` 与 `git diff --check` 实际无输出。
- Task 12 / 最终范围复审：相对基线 `a65f913e` 的完整 diff 共 38 个计划内文件，未发现额外未授权文件；internal/codegraph 依赖隔离与 CLI/代码图网络扫描均无输出；修复后 CLI 复验引用问题仍为 0，stale 仅如实保留上述 5 个既有节点。Commit 范围：`HEAD^..HEAD`（Task 12 收尾提交）。
- 领域视图增量（plan `docs/superpowers/plans/2026-08-21-codegraph-domain-view.md`，基线 `4c7531971`）
- Task 1 / 完成裁决：spec 符合（Domain/Container.Domain/Graph.Domains/View.Domains 契约、domains 透传、领域 parent/环/容器叶子归属校验与无 domains 合法降级均实现）；代码质量符合（internal/codegraph 无日志/内部依赖，问题串带领域或容器 id 与原因）。验证：`go test ./internal/codegraph/ -run TestValidateDomain -v` 实际通过（2 tests）；`go test ./internal/codegraph/ -v` 实际通过（18 tests）；`gofmt -l internal/codegraph/` 与 `git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`。
- Task 2 / 完成裁决：spec 符合（DomainStat/DomainTree 按 id 与切片稳定排序，直属成员统计、嵌套 children、跨领域接口与 deleted 跳过、无 domains 返回 nil 均实现）；代码质量符合（纯投影、不改输入、不打日志，注释明确不按包名推导且与前端规则对齐）。验证：`go test ./internal/codegraph/ -run TestDomainTree -v` 实际通过（3 tests）；`go test ./internal/codegraph/ -v` 实际通过（22 tests）；`gofmt -l internal/codegraph/` 与 `git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`。
- Task 3 / 完成裁决：spec 符合（graph domains 输出 view/domains/warning 契约、validate 输出 domains 计数、agentd 成功日志补 domains 统计且仍使用固定 cause 失败键）；代码质量符合（CLI 无日志/网络，旧图 warning 可行动且领域树复用 DomainTree）。验证：`go test ./cmd/ -run 'TestGraphDomains|TestGraphValidateReportsDomainCount' -v` 实际通过（2 tests）；`go test ./cmd/ -run TestGraph -v` 实际通过（7 tests）；`go test ./internal/agentd/ -run TestCodegraph -v` 实际通过（2 tests）；真实冒烟 `go run . graph domains --repo internal/codegraph/testdata/repo` 实际退出 0，输出 4 个领域、d_svc 2 个 children、d_svc/api interfaces 为 n_runE；`gofmt -l cmd/ internal/agentd/` 与 `git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`。
- Task 4 / 完成裁决：spec 符合（顶层 domains、Domain 字段表、容器 domain、领域切分原则、两条领域硬纪律及 domains CLI 验收命令均补齐）；代码质量符合（文档字段名与 Go 契约一致，明确旧图降级与容器叶子边界）。验证：`git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`。
- Task 5 / 完成裁决：spec 符合（CgDomain/CgContainer.domain/CgGraph.domains/CgView.domains 契约、mergeView 空对象兜底、neighborhood expand 边界、路径/祖先/子域/范围/领域聚合/叶子根算法均实现）；代码质量符合（纯函数、不发请求不碰 DOM、不按包名推导，并与 Go 跨领域规则对齐）。验证：`npx vitest run src/app/codegraph/domains.test.ts` 实际通过（8 tests）；`npx vitest run src/app/codegraph` 实际通过（5 files/20 tests）；`npm run typecheck` 实际通过；`git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`。
