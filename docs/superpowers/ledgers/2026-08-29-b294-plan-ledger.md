# B294 plan 台账（2026-08-29）

卡：B294（远端预览：执行机发布会话，左栏第四种任务行点开隔离 Chromium）；本节点：`plan`；分支：`cards/B294-plan`。

- [fact] `pwd && git status --short --branch && rg --files ...`：当前目录为 `/root/.handoff/worktrees/3da7945a`，分支为 `cards/B294-plan`，工作树初始无未提交改动；法定输入 `docs/superpowers/specs/b294.md`、`b294-contract.md`、`b294-breakdown.md` 可读；仓内存在 `codegraph/best.json`、`codegraph/target.json`。
- [fact] `git log --oneline --decorate -8`：当前 HEAD 为 `1fa4668b docs(B294): 拍板 breakdown P0–P5`；其父提交为 contract 冻结基线，任务注入的有效基线与当前分支一致。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_orchestration`、`... context d_gateway`、`... context d_transport`、`... context d_web`：命令均实际运行；返回 best 领域上下文及源码窗口，但输出超出工具窗口并被标记 `Warning: truncated output`，未把截断部分当额外事实。上下文结果确认 `d_orchestration`、`d_gateway`、`d_transport`、`d_web` 的领域查询可用；`d_web` 结果披露现状归属与 misplaced 列表，计划按 best 领域而非旧 baseline 视图写边界。
- [cmd] `... codegraph sym --help`、`... codegraph flow --help`：实际输出分别确认 `sym <符号名或节点 id>` 与 `flow <节点 id 或名字>` 用法；未用 `chain` 冒充流程图。
- [cmd] `... codegraph sym Server.Handler`：实际返回 `internal/agentd/server.go:489`，签名 `func (s *Server) Handler() http.Handler {`，节点 `n_agentd_Server_Handler`。
- [cmd] `... codegraph sym Pool.For`：实际返回 `internal/targetclient/pool.go:107`，签名 `func (p *Pool) For(name string) (*client.Client, error)`，节点 `n_targetclient_Pool_For`。
- [cmd] `... codegraph sym Client.StreamEventsOnce`：实际返回 `internal/client/client.go:1574`，签名 `func (c *Client) StreamEventsOnce(ctx context.Context, taskID string, fromSeq int64, onEvent func(proto.Event) error) error`，节点 `n_client_Client_StreamEventsOnce`。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/proto ./internal/client ./internal/agentd ./cmd -count=1`：首次编译依赖后实际退出码 0；原始通过行是 `ok  github.com/Xsxdot/handoff/internal/proto 0.013s`、`ok  github.com/Xsxdot/handoff/internal/client 9.326s`、`ok  github.com/Xsxdot/handoff/internal/agentd 218.461s`、`ok  github.com/Xsxdot/handoff/cmd 12.999s`。
- [cmd] `npm ci --ignore-scripts`（目录 `web`）：实际退出码 0；原始输出为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`。依赖只落到 `web/node_modules`，未改受版本控制文件。
- [cmd] `npm test -- --run src/api/contract.test.ts`（目录 `web`）：实际退出码 0；原始输出含 `Test Files 1 passed (1)`、`Tests 39 passed (39)`。
- [cmd] `npm run typecheck`（目录 `web`）：实际退出码 0，无错误输出。
- [cmd] `jq empty codegraph/best.json && jq empty codegraph/target.json && jq empty codegraph/diffs/cards-B294-charter-2.json && git diff --check`：实际退出码 0，无输出。
- [fact] 三份输入的冻结边界：`PreviewOpenReq`、`PreviewSession`、`PreviewListResp`、`PreviewEvent`、`PreviewOpenResp`、`PreviewCloseResp` 与五条 agentd route/一条 preview WS route 已存在；owner persistence、事件 producer、mirror、SOCKS/PAC/Chromium、web preview row 仍是 Ticket 0 之后的实现欠账。
- [fact] contract §2.3 的现状 client 签名为：`CreatePreview(ctx context.Context, req proto.PreviewOpenReq) (*proto.PreviewSession, error)`、`ListPreviews(ctx context.Context) (*proto.PreviewListResp, error)`、`ListPreviewsAll(ctx context.Context) (*proto.PreviewListResp, error)`、`ClosePreview(ctx context.Context, id string) (*proto.PreviewCloseResp, error)`、`OpenPreview(ctx context.Context, id, machine string) (*proto.PreviewOpenResp, error)`、`StreamPreviewEventsOnce(ctx context.Context, onEvent func(proto.PreviewEvent) error) error`；REST 已走 `Client.do`，WS 已走 `wsDialOptions`。
- [fact] 当前 Ticket 0 handler 在 `internal/agentd/preview.go` 统一返回 HTTP 503 与 `预览会话尚未接线`；`Server.Handler` 在 `internal/agentd/server.go` 注册 `POST/GET/DELETE /api/previews`、`POST /api/previews/{id}/open`、`GET /ws/previews`。
- [fact] 当前 `targetclient.Pool` 的 `entry` 已持有 `*relay.Dialer`，`internal/relay/dialer.go#Dialer.DialContext(ctx context.Context, network, addr string) (net.Conn, error)` 已实现 relay app-yamux stream；直连 target 由 `client.New` 构造。P2 已拍板为 Pool 生命周期管理的 raw `DialContext`，计划将其作为内部 preview upstream 接缝写入而不新增浏览器可见 HTTP/CONNECT 端点。
- [fact] 当前 `internal/store.Open` 在 `handoff.db` 建表并以 `Store` 持有 SQLite；现有迁移/DDL 使用 `IF NOT EXISTS`、WAL 与 `busy_timeout(5000)`，Store 叶子方法通常不打日志，调用方负责上下文日志。P3 已拍板复用该 Store，不造 coordinator 中央 truth。
- [fact] 当前 `ProjectTree` 的 `TaskRow`/`TaskIconSlot` kind 类型为 `'tui' | 'terminal' | 'file'`，普通任务点击调用 `onOpenTask`、行可写既有拖放 MIME；`Shell` 当前只装配 `useProjectTree`/`useTasks`。U4 必须添加独立 `preview` 分支，不扩张 `OpenItem`/workbench 的闭集。
- [judgment] 按 breakdown 的 P0–P5，计划采用：页面本地 `is-open` 投影；`TTLSeconds=7200` 且 idle 过期、Chromium 附着续命；Pool raw dial；复用 `handoff.db`；独立 preview mirror 与集中接口+OS launcher；via 只接受 spec 的 IP/CIDR/域名，不加通配符。
- [fact] `cmd/agentd.go` 的现有生命周期是 `wdCtx, wdCancel` → `srv.Pool().Warm(wdCtx)` → 任务 `Mirror.Run(wdCtx)` → `sd.Serve(..., srv.Handler(), srv.GracefulShutdownCleanup(wdCancel), ...)` → Serve 返回后关闭 Pool；B294 的 preview owner/mirror/launcher 必须挂在这条同一条退出边界。
- [fact] `internal/targetclient/pool.go#entry` 当前持有 `target config.Target`、`client *client.Client`、可选 `dialer *relay.Dialer` 与 `cleanup func()`；`Pool.For` 在同一 target 配置下复用 entry，配置变化时先 cleanup 再重建，`Pool.Close` 终止全池。raw preview 拨号因此应成为 entry 的池内能力，不让调用方关闭 client/dialer。
- [fact] `internal/relay/dialer.go#Dialer.DialContext` 的精确签名为 `func (d *Dialer) DialContext(ctx context.Context, network, addr string) (net.Conn, error)`；P2 计划复用它的 relay 分支，同时为 direct entry 提供同一签名的池内拨号函数。
- [fact] `ProjectTree` 现有 `OpenItem.kind` 与 `TaskRow.kind` 均为 `'tui' | 'terminal' | 'file'`；`TaskRow` 写入既有拖放 MIME，普通任务 click 调用 `onOpenTask`；`Shell.openTaskTui` 会进入 workbench。preview 必须是树的专用第四种行，不能进入 OpenItem/workbench/任务拖放协议。
- [fact] breakdown 的 U0–U5 是序贯同轮 implement 单元，文件域分别为 Store/proto、owner/lifecycle、client+Pool+mirror、SOCKS/PAC/launcher、web tree、跨域回归；U5 明确不加生产 API，真实 Chromium/桌面/跨机/DNS/重启清单需协调者真机执行。
- [fact] contract §3.1 的接缝清单把 `POST /api/previews`、owner `GET /api/previews`、聚合 `GET /api/previews?scope=all`、`DELETE /api/previews/{id}`、`POST /api/previews/{id}/open`、`GET /ws/previews`、client REST/WS、Pool raw dial、web preview row 与 CLI 方法分别列为边界；计划的每个测试入口会标明其穿过的接缝，纯 helper 测试只作补充。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/store ./internal/targetclient -count=1`：实际退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/store 3.420s`、`ok github.com/Xsxdot/handoff/internal/targetclient 0.157s`。
- [cmd] `npm test -- --run src/app/tree/counts.test.ts src/app/tree/search.test.ts src/app/tree/ProjectTree.test.tsx`（目录 `web`）：实际退出码 0，原始结果 `Test Files 3 passed (3)`、`Tests 79 passed (79)`，Vitest `v4.1.10`。
- [cmd] `npm test -- --run src/app/shell/Shell.test.tsx`（目录 `web`）：实际退出码 0，原始结果 `Test Files 1 passed (1)`、`Tests 41 passed (41)`，Vitest `v4.1.10`。
- [fact] 计划文档已落盘为 `docs/superpowers/plans/b294-plan.md`，内容覆盖 U0–U5 序贯 DAG、精确 Interfaces/Consumes/Produces、每单元基线/测试范围/日志/注释步骤、缺陷族、序列化边界、双向接缝和协调者真机清单。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/plans/b294-plan.md`：实际退出码 0，返回 3 个 anchors；`Server.Handler`→`internal/agentd/server.go:489`、`Pool.For`→`internal/targetclient/pool.go:107`、`Client.StreamEventsOnce`→`internal/client/client.go:1574`，均为 `anchor: moved`。
- [judgment] 首轮占位符扫描发现接口代码块省略私有字段和自审自述命中扫描词；已改为列出 Hub/Mirror/Allowlist/Proxy/OpenService 的具体字段，并删除自审中的扫描词。`rg` 目标扫描无输出；文档 fence 计数为 30、为偶数；`git diff --check` 实际无输出。
- [cmd] 计划判据组合基线 `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/store ./internal/proto -count=1`：实际退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/store 5.938s`、`ok github.com/Xsxdot/handoff/internal/proto 0.011s`。
- [cmd] 计划判据组合基线 `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/client ./internal/targetclient ./internal/agentd -count=1`：实际退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/client 9.740s`、`ok github.com/Xsxdot/handoff/internal/targetclient 0.171s`、`ok github.com/Xsxdot/handoff/internal/agentd 245.806s`。
- [cmd] 计划判据组合基线 `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd ./internal/targetclient ./internal/relay -count=1`：实际退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 244.852s`、`ok github.com/Xsxdot/handoff/internal/targetclient 0.167s`、`ok github.com/Xsxdot/handoff/internal/relay 0.022s`。
- [cmd] 计划判据组合基线 `(cd web && npm test -- --run src/app/tree/counts.test.ts src/app/tree/search.test.ts src/app/tree/ProjectTree.test.tsx src/app/shell/Shell.test.tsx && npm run typecheck)`：实际退出码 0，原始结果 `Test Files 4 passed (4)`、`Tests 120 passed (120)`，typecheck 无错误输出。
- [cmd] 计划判据组合基线 `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/proto ./internal/client ./internal/agentd ./cmd -count=1`：实际退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/proto 0.003s`、`ok github.com/Xsxdot/handoff/internal/client 9.729s`、`ok github.com/Xsxdot/handoff/internal/agentd 244.894s`、`ok github.com/Xsxdot/handoff/cmd 13.102s`。
- [cmd] 计划判据组合基线 `(cd web && npm test -- --run src/api/contract.test.ts src/app/tree/ProjectTree.test.tsx src/app/shell/Shell.test.tsx && npm run typecheck)`：实际退出码 0，原始结果 `Test Files 3 passed (3)`、`Tests 143 passed (143)`，typecheck 无错误输出。
- [fact] 计划最终自检：`rg -n "TBD|TODO|同 Task|加适当错误处理|适当的错误处理|/\\*|\\*/" docs/superpowers/plans/b294-plan.md` 无输出；文档 fence 计数为 32 且为偶数；`git diff --check` 实际退出码 0；三份图 JSON 的 `jq empty` 实际退出码 0。
- [cmd] 计划最终 `codegraph resolve --doc` 实际退出码 0，返回 anchors：`internal/agentd/server.go#Server.Handler` line 489、`internal/targetclient/pool.go#Pool.For` line 107、`internal/client/client.go#Client.StreamEventsOnce` line 1574，均 `anchor: moved`。
- [judgment] 计划通过本节点自审：U0–U5 文件集有界且序贯；每个实现单元有基线、最小测试范围、结构化日志、职责/边界注释步骤；50 条冻结接缝由声明缝测试双向覆盖；缺失/零值跨 JSON/HTTP/WS/TS 投影已列断言；真实 Chromium/OS/DNS/多机行为只列协调者真机未验证项。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/store ./internal/proto -count=1`：本实现节点基线实际退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/store 4.890s`、`ok github.com/Xsxdot/handoff/internal/proto 0.003s`。
- [fact] 当前实现分支 `cards/B294-implement` 工作树基线无未提交改动；`internal/proto/preview.go` 与 `internal/client/preview.go` 已有冻结 DTO/client 线形状，`internal/agentd/preview.go` 五条 handler 仍统一返回 Ticket 0 的 HTTP 503。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/store ./internal/proto -run 'Preview|Contract' -count=1`：按 TDD 先写 U0 测试后实际编译红；原始关键输出为 `undefined: PreviewRecord`、`st.InsertPreview undefined`、`st.GetPreview undefined`、`st.ClosePreview undefined`、`st.TouchPreview undefined`，`internal/proto` 仍 `ok`。该红由缺失 preview Store 接缝导致，不是测试 typo。
- [cmd] `gofmt -w internal/store/previews.go internal/store/previews_test.go internal/proto/preview_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/store ./internal/proto -run 'Preview|Contract' -count=1`：U0 生产实现后实际退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/store 0.204s`、`ok github.com/Xsxdot/handoff/internal/proto 0.005s`。
- [fact] U0 已落地 `preview_sessions` DDL、PreviewRecord roundtrip、条件 close/touch/expire 与 entry URL 更新；Source/LastActiveAt/ClosedAt 只留在 Store，不进入 wire。
- [cmd] `gofmt -w internal/agentd/preview_owner_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run 'Preview' -count=1`：先写 U1 Handler/WS seam 后实际编译红；初次命中测试文件自定义 `testLogger` 与既有测试符号重名，修正为 `previewTestLogger` 后仍原始报错 `undefined: PreviewOwner`、`undefined: NewPreviewOwner`、`undefined: NewPreviewHub`、`undefined: PreviewOwnerDeps`、`env.srv.SetPreviewOwner undefined`。该红确认 owner API/Server 注入接缝尚未实现。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewOwnerHTTPCreateListClose$' -count=1`：去除无关 PTY 测试环境后，U1 空壳实际取得断言红；原始输出为 `create status=503 body={"error":"预览会话尚未接线"}`。这证明新 Handler seam 的断言有牙。
- [cmd] `gofmt -w internal/agentd/preview.go internal/agentd/preview_owner.go internal/agentd/preview_static.go internal/agentd/server.go internal/agentd/preview_owner_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run 'Preview' -count=1`：U1 owner Handler/WS seam 实际退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.249s`。
- [fact] U1 已保留 Ticket 0 的 nil owner 503 兼容路径；注入 owner 后真实 Handler 负责严格 JSON、owner-only close、path static start/stop、TTL owner 恢复入口与 preview WS 文本事件。
- [cmd] `gofmt -w internal/targetclient/preview_dial_test.go internal/targetclient/pool.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/targetclient -run '^TestPoolDialContextUsesRegisteredTarget$' -count=1`：U2 新 raw dial 空壳编译通过后实际断言红；原始输出为 `DialContext: preview raw dial 空壳`。
- [cmd] `gofmt -w internal/agentd/preview_mirror_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewMirrorListAndEvents$' -count=1`：U2 mirror 空壳编译后实际断言红；原始输出为 `ListAll: preview mirror 空壳`。
- [cmd] `gofmt -w internal/agentd/preview_mirror.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewMirrorListAndEvents$' -count=1`：U2 独立 mirror 实现实际退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.228s`。
- [fact] U2 mirror 使用 machine+unit-separator+session ID 作为内存 key，列表刷新先替换快照，远端事件只盖 machine 并投影到 PreviewHub；没有复用任务 cursor 或写 coordinator Store。
- [cmd] `gofmt -w internal/agentd/preview_proxy_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run 'PreviewProxy|PreviewAllowlist' -count=1`：U3 SOCKS/allowlist 新 seam 首次编译红；原始输出为 `undefined: ParsePreviewAllowlist`、`undefined: RenderPreviewPAC`、`undefined: NewPreviewProxy`，另有测试端口字节 typo `cannot use 443 ... as byte`，已修为 `0x01, 0xbb` 后仍确认三个生产符号缺失。
- [cmd] `gofmt -w internal/agentd/preview_proxy.go internal/agentd/preview_owner.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run 'PreviewProxy|PreviewAllowlist' -count=1`：U3 SOCKS/allowlist 实现实际退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.007s`。
- [cmd] `gofmt -w internal/agentd/preview_launcher_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run 'PreviewOpen|PreviewLauncher' -count=1`：U3 launcher 新 seam 首次编译红；原始输出为 `undefined: PreviewLaunchSpec`、`undefined: PreviewBrowserHandle`、`undefined: NewPreviewOpenService`，测试 import typo 已补 `internal/proto`。
- [cmd] `git commit -m "docs(B294): add preview implementation plan"`：实际退出码 0，初次提交 hash 为 `4e419d6c063a07411cc4caccb5529dac8ff790b1`，包含法定计划与台账；随后为把本提交事实留在台账中执行 amend，最终 hash 以收尾命令为准。
- [cmd] `gofmt -w internal/agentd/preview_launcher_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewOpenServiceLaunchesIsolatedChromiumAndFocusesExisting$' -count=1`：U3 launcher 空壳编译后实际断言红；原始输出为 `first open resp=&{Opened:false} err=preview open service 空壳`，证明启动服务接缝断言有效。
- [cmd] `gofmt -w internal/agentd/preview_launcher.go internal/agentd/preview_launcher_unix.go internal/agentd/preview_launcher_windows.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewOpenService(LaunchesIsolatedChromiumAndFocusesExisting|FailureDoesNotReportOpened)$' -count=1`：首次实现编译失败，原始输出为 `internal/agentd/preview_launcher.go:256:28: undefined: previewExpired`；改为直接按 `LastActiveAt + TTLSeconds` 判断后，本命令实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.133s`。
- [cmd] `gofmt -w cmd/agentd.go internal/agentd/ptyreclaim.go internal/agentd/preview_launcher.go internal/agentd/preview_launcher_unix.go internal/agentd/preview_launcher_windows.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run 'Preview|GracefulShutdown' -count=1`：预览 opener/mirror/owner 生命周期接线后实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 9.384s`。
- [cmd] `(cd web && npm test -- --run src/app/data/usePreviews.test.ts)`：实际未启动测试，原始输出为 `sh: 1: vitest: not found`；web 依赖未安装，测试结果未验证。
- [cmd] `(cd web && npm ci --ignore-scripts)`：实际退出码 0，原始输出为 `added 290 packages ... found 0 vulnerabilities`，仅恢复锁定测试依赖。
- [cmd] `(cd web && npm test -- --run src/app/data/usePreviews.test.ts)`：U4 usePreviews 新接缝实际编译红；原始输出为 `Error: Failed to resolve import "./usePreviews" ... Does the file exist?`，确认新符号缺失而非断言误报。
- [cmd] `(cd web && npm test -- --run src/app/data/usePreviews.test.ts)`：usePreviews/API-W​​S 本地投影实现后实际退出码 0，原始输出为 `Test Files 1 passed (1)`、`Tests 3 passed (3)`。
- [cmd] `(cd web && npm test -- --run src/app/tree/counts.test.ts src/app/tree/search.test.ts)`：U4 preview 计数/搜索接缝在既有函数上实际断言红；原始输出为 `counts ... expected running:2/previews:2, received running:0` 与 `search ... Cannot read properties of undefined (reading 'name')`，确认缺少 preview 行为而非编译问题。
- [cmd] `(cd web && npm test -- --run src/app/tree/counts.test.ts src/app/tree/search.test.ts)`：加入 preview origin/machine/label 投影后实际退出码 0，原始输出为 `Test Files 2 passed (2)`、`Tests 18 passed (18)`。
- [cmd] `(cd web && npm test -- --run src/app/tree/ProjectTree.test.tsx)`：加入 preview row 渲染后首轮实际断言红；原始输出为 `Unable to find an element by: [data-testid="preview-row-local-preview"]`，确认第四种行尚未接入。
- [cmd] `(cd web && npm test -- --run src/app/tree/ProjectTree.test.tsx)`：首次接入后既有直接调用缺省 preview props，实际出现 `TypeError: Cannot read properties of undefined (reading 'filter')`；为兼容旧调用补默认空集合后，本命令实际退出码 0，原始输出为 `Test Files 1 passed (1)`、`Tests 64 passed (64)`。
- [cmd] `(cd web && npm test -- --run src/app/shell/Shell.test.tsx -t 'Shell 把 preview')`：Shell seam 在未接入 hook/tree 前实际断言红；原始输出为 `Unable to find an element by: [data-testid="preview-row-preview-1"]`。
- [cmd] `(cd web && npm test -- --run src/app/shell/Shell.test.tsx -t 'Shell 把 preview')`：Shell 接入 usePreviews 与专用 open 回调后实际退出码 0，原始输出为 `Test Files 1 passed (1)`、`Tests 1 passed (1)`。
- [cmd] `(cd web && npm run typecheck)`：U4 web 类型检查实际退出码 0，原始输出仅为 npm script 标题，无 tsc 错误。
- [cmd] `(cd web && npm test -- --run src/app/data/usePreviews.test.ts -t 'origin 归一化')`：补充 git/HTTPS origin 同口径断言后实际断言红；原始输出为 `expected '' to be 'github.com/Xsxdot/handoff'`，暴露 URL-only 归一化缺陷。
- [cmd] `(cd web && npm test -- --run src/app/data/usePreviews.test.ts)`：按 projectid.NormalizeGitURL 规则修正 origin 归一化后实际退出码 0，原始输出为 `Test Files 1 passed (1)`、`Tests 4 passed (4)`。
- [cmd] `(cd web && npm test -- --run src/app/data/usePreviews.test.ts -t '首次拉取')`：补充 closed 清除 is-open 投影断言后实际断言红；原始输出为 `expected true to be false`，确认 stale local projection 会被事件清除的缺口。
- [cmd] `(cd web && npm test -- --run src/app/data/usePreviews.test.ts)`：closed 事件清理本地 open key 后实际退出码 0，原始输出为 `Test Files 1 passed (1)`、`Tests 4 passed (4)`。
- [cmd] `(cd web && npm test -- --run src/api/ws.test.ts -t 'preview WS')`：首个 URL 断言把 jsdom 实际端口误写为无端口，原始输出为 `expected ws://localhost/ws/previews, received ws://localhost:3000/ws/previews`；已改为只断言路径和无 query。
- [cmd] `(cd web && npm test -- --run src/api/ws.test.ts -t 'preview WS')`：修正环境相关断言后实际退出码 0，原始输出为 `Test Files 1 passed (1)`、`Tests 1 passed (1)`。
- [cmd] `(cd web && npm test -- --run src/api/contract.test.ts src/api/ws.test.ts src/app/data/usePreviews.test.ts src/app/tree/counts.test.ts src/app/tree/search.test.ts src/app/tree/ProjectTree.test.tsx src/app/shell/Shell.test.tsx && npm run typecheck)`：U4 全部触及 web seam 实际退出码 0；原始输出为 `Test Files 7 passed (7)`、`Tests 174 passed (174)`，随后 `tsc -b` 无错误输出。
- [cmd] `gofmt -w internal/agentd/preview_regression_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewCloseEventReclaimsLocalBrowserResources$' -count=1`：首轮编译红为测试遗漏 `store` import，原始输出为 `undefined: store`；补正测试导入后，实际断言红为 `processes after owner close=1`，暴露 opener 未消费 owner closed 事件的缺口。
- [cmd] `gofmt -w internal/agentd/preview_regression_test.go internal/agentd/preview_launcher_test.go internal/agentd/preview_launcher.go internal/agentd/preview_launcher_unix.go internal/agentd/preview_launcher_windows.go cmd/agentd.go internal/agentd/ptyreclaim.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go build ./... && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/client ./internal/targetclient ./internal/agentd ./internal/relay -run 'Preview|Dial|GracefulShutdown' -count=1`：U3/U4/U5 Go 收尾编译与局部测试实际退出码 0；原始测试输出为 `ok github.com/Xsxdot/handoff/internal/client 0.206s`、`ok github.com/Xsxdot/handoff/internal/targetclient 0.004s`、`ok github.com/Xsxdot/handoff/internal/agentd 0.795s`、`ok github.com/Xsxdot/handoff/internal/relay 0.005s`，build 无输出。
- [cmd] `(cd web && npm test -- --run src/app/preview_regression.test.tsx && npm run typecheck)`：U5 web 零值/跨机器回归实际退出码 0；原始输出为 `Test Files 1 passed (1)`、`Tests 2 passed (2)`，随后 `tsc -b` 无错误输出。
- [cmd] `GOOS=windows GOARCH=amd64 GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test -c -o /root/.handoff/tmp/06b806e2/agentd.test.exe ./internal/agentd`：实际失败，原始输出含 `go: writing go.mod cache: ... read-only file system` 与 `FAIL github.com/Xsxdot/handoff/internal/agentd [setup failed]`；Windows 交叉编译未验证。
- [cmd] `GOOS=windows GOARCH=amd64 GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go test -c -o /root/.handoff/tmp/06b806e2/agentd.test.exe ./internal/agentd`：改用可写任务缓存后实际退出码 0；原始输出为依赖下载清单，无编译错误。
- [cmd] `gofmt -w internal/agentd/preview_regression_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestStopPreviewServicesContinuesOwnerCleanupAfterOpenerError$' -count=1`：关停顺序回归实际断言红；原始输出为 `owner cleanup did not continue`，证明 opener 错误会提前阻断 owner 收口。
- [cmd] `gofmt -w internal/agentd/server.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestStopPreviewServicesContinuesOwnerCleanupAfterOpenerError$' -count=1`：StopPreviewServices 改为记录 opener 错误后继续 mirror/owner 收口，实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.042s`。
- [cmd] `gofmt -w internal/agentd/preview_launcher.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewCloseEventReclaimsLocalBrowserResources$' -count=1`：opener 订阅 owner closed 事件并回收 proxy/profile 后实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.065s`。
- [cmd] `gofmt -w internal/agentd/preview_launcher_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewOpenService(StartFailureCleansProfile|LaunchesIsolatedChromiumAndFocusesExisting|FailureDoesNotReportOpened)$' -count=1`：启动失败逆序清理、首次启动/重复聚焦与查找失败用例实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.175s`。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewOpenServiceLaunchesIsolatedChromiumAndFocusesExisting$' -count=1`：先把 U3 启动参数断言纠正为冻结值 `<-loopback>` 后实际断言红；原始输出为 `launch spec=... ProxyBypassList:<-loopback ...` 与 `FAIL`，确认生产参数少了 `>`。
- [cmd] `gofmt -w internal/agentd/preview_launcher.go internal/agentd/preview_launcher_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestPreviewOpenService(StartFailureCleansProfile|LaunchesIsolatedChromiumAndFocusesExisting|FailureDoesNotReportOpened)$' -count=1`：修正 `ProxyBypassList` 为 `<-loopback>` 后实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.195s`。
- [cmd] `gofmt -w internal/agentd/preview_owner_test.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestDefaultPreviewWorkspaceResolverReadsGitMetadata$' -count=1`：为生产默认 workspace resolver 新增接缝测试后实际编译红；原始输出为 `undefined: defaultPreviewWorkspaceResolver` 与 `FAIL`，确认符号缺失而非断言误报。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestDefaultPreviewWorkspaceResolverReadsGitMetadata$' -count=1`：默认 resolver 空壳编译后实际断言红；原始输出为 `workspace metadata root="" origin="" branch=""` 与 `FAIL`，确认需要读取 Git 工作区元数据。
- [cmd] `gofmt -w internal/agentd/preview_owner.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run '^TestDefaultPreviewWorkspaceResolverReadsGitMetadata$' -count=1`：首次替换空壳时保留了重复符号，实际编译失败；原始输出为 `defaultPreviewWorkspaceResolver redeclared in this block`。删除空壳后同命令实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.027s`。
- [cmd] `GOOS=windows GOARCH=amd64 GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go test -c -o /root/.handoff/tmp/06b806e2/agentd.windows.focus.test.exe ./internal/agentd`：为 Windows 真实窗口聚焦接缝新增跨平台编译测试后实际编译红；原始输出为 `undefined: previewEnumWindows`、`undefined: previewWindowProcessID`、`undefined: previewWindowVisible`、`undefined: previewRestoreWindow`、`undefined: previewForegroundWindow`，确认 Windows 聚焦实现缺少 OS 接缝。
- [cmd] `gofmt -w internal/agentd/preview_launcher_windows.go internal/agentd/preview_launcher_windows_test.go && GOOS=windows GOARCH=amd64 GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go test -c -o /root/.handoff/tmp/06b806e2/agentd.windows.focus.test.exe ./internal/agentd`：Windows launcher 使用 user32 枚举可见窗口、恢复并设置前台的实现实际交叉编译通过，命令无输出；Windows 桌面运行时行为仍未验证。
- [cmd] `rg -n -F 'if !changed {' internal/agentd/preview_owner.go` 与 `wc -l`：实际命中数为 `1`，唯一锚点为 `328: if !changed {`；用于后续语义变异自验。
- [cmd] 变异自验：将唯一命中 `if !changed` 取反为 `if changed` 后，`GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go build ./...` 实际退出码 0、无输出，确认变异可编译；随后单用例实际变红，原始输出为 `close status=404 body={"error":"预览会话 preview-test: 预览会话已关闭"}`。
- [cmd] 变异自验全局触及包：`GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run 'Preview' -count=1` 实际失败 4 个用例；原始输出为 `TestPreviewMirrorListAndEvents`、`TestPreviewOwnerHTTPCreateListClose`、`TestPreviewOwnerPathUsesStaticAndWSPublishesFullEvents`、`TestPreviewCloseEventReclaimsLocalBrowserResources` 均因 `close status=404` 或 `close preview: 预览会话已关闭` 失败，证明该断言族能拦截语义变异。
- [cmd] 恢复 `if !changed` 后，`gofmt -w internal/agentd/preview_owner.go && GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./internal/agentd -run 'Preview' -count=1` 实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 0.936s`。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/3da7945a/gomodcache go test ./... -count=1`：全量 Go 测试实际失败；绝大多数包为 `ok`，但 `internal/prochost` 的既有 `TestWindowsCrossCompiles`、`TestWindowsVets` 因子进程继承只读缓存而失败，原始错误含 `go: writing go.mod cache: ... read-only file system`、`FAIL github.com/Xsxdot/handoff/internal/prochost`；同时发现 `web/node_modules/flatted/golang/pkg/flatted [no test files]` 被 `./...` 枚举。该次不能作为通过依据。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go test ./... -count=1`：改用可写任务缓存后全量 Go 测试实际完成且所有列出的包均为 `ok`；关键输出含 `ok github.com/Xsxdot/handoff/internal/agentd 200.110s`、`ok github.com/Xsxdot/handoff/internal/prochost 11.649s`、`ok github.com/Xsxdot/handoff/internal/store 7.580s`、`ok github.com/Xsxdot/handoff/internal/targetclient 0.159s`，无 `FAIL`。
- [cmd] 在 `web` 目录运行 `npm test`：实际退出码 0；原始汇总为 `Test Files 115 passed (115)`、`Tests 1162 passed (1162)`。同时输出 jsdom 提示 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`，未导致测试失败。
- [cmd] 在 `web` 目录运行 `npm run typecheck`：实际退出码 0，原始输出为 `> tsc -b`，无类型错误。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache GOSUMDB=off GOPROXY=direct go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check`：实际退出码 0，原始 JSON 首部为 `{"fails": []}`；输出包含既有 `legacy`、`oversized-package`、`prefix-family` 与当前 `k_web_api_preview` 的 `best-dangling` 警告，无新的失败闸门。
- [cmd] `gofmt -l` 覆盖本轮 Go 文件、`git diff --check` 与 `GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go build ./...`：三项均实际退出码 0，均无输出。
- [fact] 静态复核确认 relay 的既有 `Dialer.DialContext` 返回 HTTP app-yamux stream，而 preview SOCKS 需要 owner 侧原始 TCP；按计划 U2 的 raw relay framing 条款，补充独立加密 raw stream，并保持既有 HTTP app stream 不变。
- [cmd] `gofmt -w internal/relay/listener_test.go && GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go test ./internal/relay -run '^TestListenerRawDialThroughFakeRelay$' -count=1`：raw listener seam 首次实际编译红；原始输出为 `d.RawDialContext undefined (type *Dialer has no field or method RawDialContext)`。
- [cmd] `gofmt -w internal/relay/dialer.go internal/relay/listener.go internal/targetclient/pool.go internal/relay/listener_test.go && GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go test ./internal/relay -run '^TestListenerRawDialThroughFakeRelay$' -count=1`：新增加密 raw stream、owner-side DNS/TCP bridge 与 Pool relay raw 选路后实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/relay 0.006s`。
- [cmd] `GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go test ./internal/relay -run 'Test.*(Relay|Listener|Dialer)' -count=1`：raw framing 与既有 HTTP relay 流回归实际退出码 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/relay 0.008s`。
- [cmd] relay raw framing 后最终重跑 `GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go test ./... -count=1`：实际完成且所有列出的包均为 `ok`；关键输出含 `ok github.com/Xsxdot/handoff/internal/agentd 227.064s`、`ok github.com/Xsxdot/handoff/internal/relay 0.010s`、`ok github.com/Xsxdot/handoff/internal/prochost 7.242s`、`ok github.com/Xsxdot/handoff/internal/targetclient 0.157s`，无 `FAIL`。
- [cmd] `GOOS=windows GOARCH=amd64 GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache go build ./...`：relay raw framing 与 Windows launcher 接线最终交叉编译实际退出码 0，无输出。
- [cmd] 最终 `GOMODCACHE=/root/.handoff/tmp/06b806e2/gomodcache GOSUMDB=off GOPROXY=direct go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check`：实际退出码 0，原始 JSON 为 `"fails": []`；仅有既有结构告警与 `k_web_api_preview` 的 `best-dangling`，未新增失败。
- [fact] 收尾前工作树检查：本轮 B294 实现/测试/台账文件均已列入待提交范围；`gofmt -l`、`git diff --check` 实际无输出。下一步只执行 git add、cached diff check 与 commit，不 push。
