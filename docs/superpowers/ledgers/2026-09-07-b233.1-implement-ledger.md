# B233.1 implement 台账

节点：charter/implement  
卡：B233.1  
分支：cards/B233.1-charter-5  
基线 HEAD：93d10d7e  
规则：每确立一个事实立刻追加一行；测试必须先见红再写实现；变异自验确认有牙；提交事实记命令与原始输出，随后只 amend 一次。

---

## 2026-09-07 开工检查

- 角色：implement 执行者。按 `docs/superpowers/plans/b233.1-plan.md` 逐步执行 T1–T6。
- 纪律红线：
  - 不派发、不调用 handoff CLI、不起新的 executor 进程或子任务。
  - 提问纪律：需要人决策输出单行 JSON `{"ask":"..."}` 并结束回合。
  - 收尾纪律：git add 并 commit（不要 push），输出单行 JSON `{"branch":"...","commit":"...","summary":"..."}`。
  - 拍板不得改写：P1(a), P2(a), P3(a), P4(a), P5(a), P6(a)。
  - TDD 铁律：先红后绿，无失败测试不写生产代码。变异自验。日志与注释。
- 基线检查：
  - `git status`: On branch cards/B233.1-charter-5, working tree clean.
  - `codegraph --repo . --view cards-B233.1-charter-2 check`: `fails: []`, `d_orchestration->d_execution` 26/27.
- 基线测试：
  - `go test ./internal/executor ./internal/executor/opencode ./internal/permgate ./internal/store ./internal/proto ./internal/client -count=1`: 全部通过（executor 0.010s, opencode 17.730s, permgate 0.015s, store 5.153s, proto 0.004s, client 9.410s）。
  - `go test ./internal/executor/opencode -run 'TestSendRoutesToQuestionReplyWhenPending|TestSendFallsBackToPromptWhenNoPendingQuestion|TestSendUnparsableAnswerRepromptsAndKeepsPending|TestSendCustomRejectedByServerRepromptsAndKeepsPending' -count=1`: ok 0.009s。

## Task 1: Ask 回程——拆 Send 兼答、落地 RespondAsk

- TDD 红灯：
  - 编译红：`a.RespondAsk undefined`（验证符号缺席）。
  - 空壳落地后断言红：
    - `TestSendDoesNotConsumePendingAsk`: `Send 不得打到 question 端点，实际 paths=[/question/req_1/reply]`
    - `TestRespondAskRepliesPendingQuestion`: `path = ""，期望 reply 端点`
    - `TestRespondAskContinuesWhenTurnEnded`: `回合已结束应以关联 Prompt 续接，paths=[]`
- T1 最小实现与绿灯：
  - `internal/executor/opencode/adapter.go`: 拆除 Send 消费 pending 分流，实现 `RespondAsk`，声明 `var _ executor.AskResponder = (*Adapter)(nil)`。
  - 测试通过：`go test ./internal/executor/opencode -run 'TestSendDoesNotConsumePendingAsk|TestRespondAsk|TestSendFallsBack|TestSendUnparsable|TestSendCustomRejected|TestStopRejectsPendingQuestion' -count=1`（ok 0.010s）。
  - 包级测试：`go test ./internal/executor/opencode ./internal/executor -count=1`（opencode 18.053s, executor 0.001s 全绿）。
  - 编译全量：`go build -o /dev/null .`（exit 0）。

## Task 2: 审批 client 实现、政策快照、复用加盐、Authorize

- TDD 红灯：
  - 编译红：`undefined: executor.Authorize` 与 `undefined: executor.ReuseFingerprint`。
  - 空壳断言红：
    - `TestAuthorizeAwaitsWhenPending`: status=""
    - `TestAuthorizeNilClientIsNotAllow`: nil client 必须错误
    - `TestAuthorizeCancelDoesNotLeak`: 已取消 ctx 应返回错误
    - `TestReuseFingerprintSaltsVersion`: 加盐后不得等于裸 PermFingerprint
- T2 最小实现与绿灯：
  - `internal/executor/fingerprint.go`: 实现 `ReuseFingerprint(version, permFP string)`。
  - `internal/executor/authorize.go`: 实现 `Authorize(ctx, client, req)`。
  - `internal/permgate/permgate.go`: 导出 `SafeCommandIDs() []string`。
  - `internal/agentd/manager.go`: `permFingerprintFor` 包装 `executor.PermFingerprint`。
  - `internal/agentd/approval_client.go`: 实现 `taskApprovalClient`、`bindApproval`、`HashPolicyVersion`。
  - 测试通过：
    - `go test ./internal/executor -run 'TestReuseFingerprint|TestAuthorize' -count=1`（ok 0.006s）
    - `go test ./internal/permgate -count=1`（ok 0.007s）
    - `go test ./internal/agentd -run 'TestApprovalClient' -count=1`（10 项断言全绿，0.786s）
    - `go test ./internal/agentd -run 'TestPermFingerprintForMatchesExecutorCanonical' -count=1`（ok 0.003s）
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
  - 图检查：`codegraph check --view cards-B233.1-charter-2`（fails: []，d_orchestration->d_execution 26/27）。

## Task 3: OpenCode 原生 allow ⊆ 当前快照

- TDD 红灯：
  - 编译红：`undefined: opencode.WritePermissionConfig`
  - 空壳断言红：
    - `TestWritePermissionConfigSubsetOfSnapshot`: open : no such file or directory
    - `TestWritePermissionConfigEmptyScopeIsCapabilityError`: 空 Scope 无法实施范围内允许，必须能力不足
    - `TestWritePermissionConfigRequiresVersion`: 缺 Version 必须拒绝
- T3 最小实现与绿灯：
  - `internal/executor/opencode/taskenv.go`: 实现 `WritePermissionConfig`，`cloneBashRulesAskDefault` 将 bash["*"] 设为 ask，`WriteTaskEnv` 签名增加 `snap executor.PolicySnapshot` 并调用 `WritePermissionConfig`。
  - `internal/executor/opencode/adapter.go`: `Start` 从 `req.Approval` 获取快照后传给 `WriteTaskEnv`。
  - `internal/executor/opencode/taskenv_test.go`: 补齐 `testSnapshot()` 传参，锁住 `bash["*"]=="ask"`。
  - 测试通过：`go test ./internal/executor/opencode -run 'TaskEnv|Snapshot|Project|BashRules|ExternalDirectory' -count=1`（ok 0.066s）。
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
  - 图检查：`codegraph check --view cards-B233.1-charter-2`（fails: []，d_orchestration->d_execution 26/27）。

## Task 4: 接线——注入、OpenCode 路径退役权威、事件可交付

- TDD 红灯：
  - `TestPermissionReuseIsNotDeliverable`: 交付过滤断言（先行验证）。
  - `TestDispatchOpenCodeInjectsApproval`: `OpenCode 任务必须注入 ApprovalClient`。
  - `TestOpenCodeWaitQuestionUsesRespondAsk`: `RespondAsk 必须被调用 1 次，实得 0`。
  - `TestDenyGuidanceDoesNotSwallowQuestion`: `提问工单必须创建成功: 记录不存在`。
  - `TestOpenCodeNilApprovalIsCapabilityError`: `Start with nil Approval must fail`。
  - `TestMapPermissionAskedWithApprovalClientDoesNotEmitPermission`: approval 非 nil 不得 emit permission。
- T4 最小实现与绿灯：
  - `internal/executor/snapshot.go`: 定义 `SnapshotApplier` 接口。
  - `internal/executor/resume.go`: `ResumeReq` 增加 `Approval ApprovalClient`。
  - `internal/client/client.go`: `isDeliverable` 过滤 `proto.EventTypePermissionReuse`。
  - `internal/executor/opencode/adapter.go`: `runState` 增加 `approval` 和 `reloadProbe`，`Start` 拒绝 nil `req.Approval`，`mapPermissionAsked` 在 `approval != nil` 时异步调用 `authorizeNativePermission`，实现 `ApplySnapshot` 与 `probeServeReloadsConfig`。
  - `internal/executor/opencode/resume.go`: `Resume` 在冷恢复时要求 `req.Approval != nil`，并绑定至 `r.approval`。
  - `internal/agentd/manager.go`:
    - 辅助函数 `isOpenCodeExecutor`、`taskIsOpenCode`、`resolveSnapshot`。
    - `Dispatch`: OpenCode 执行者注入 `m.bindApproval`。
    - `Continue`: OpenCode 执行者经 `SnapshotApplier.ApplySnapshot` 投影新快照，未实现或失败则回迁。
    - `RelayAnswer` / `waitQuestion`: OpenCode 执行者调用 `RespondAsk`，不回退 `Send`。
    - `handleQuestion`: 对 OpenCode 不走 `takeDenyGuidance` 早退，工单 ID 走 `NamespacedTicketID`。
    - `handleEvent`: 收到 OpenCode 发出的 `permission` 事件时记录 Warn 并忽略。
    - `resumeForContinue` / `ResumeTask`: 注入快照并绑定 Approval。
  - 测试通过：
    - `go test ./internal/client -run 'TestPermissionReuseIsNotDeliverable' -count=1`（ok 0.003s）
    - `go test ./internal/agentd -run 'TestDispatchOpenCodeInjectsApproval|TestOpenCodeWaitQuestionUsesRespondAsk|TestDenyGuidanceDoesNotSwallowQuestion' -count=1`（ok 0.511s）
    - `go test ./internal/executor/opencode -run 'TestOpenCodeNilApprovalIsCapabilityError|TestMapPermissionAskedWithApprovalClientDoesNotEmitPermission' -count=1`（ok 0.064s）
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
  - 图检查：`codegraph check --view cards-B233.1-charter-2`（fails: []，d_orchestration->d_execution 26/27）。
## Task 5: 边界与防御闭环（回答幂等、作废拦截、进程解阻）

- TDD 红灯：
  - `TestAnswerTicketSameAnswerIdempotent` / `TestAnswerTicketDifferentAnswerRejected` / `TestAnswerTicketVoidRejected`: 验证 store 层相同答案幂等、冲突冲突报错、已作废工单拒绝。
  - `TestHandleReplyIdempotentDoesNotRelayTwice`: 验证 server handleReply 重复相同回答返回 200 且不重复 relay。
  - `TestStopVoidsAskAndDoesNotRewriteAllow`: 验证 Stop 作废 Ask 工单且绝不重写 policy 为 allow。
  - `TestStopRejectsPendingPermissions`: 验证 Stop 对挂起处于 paused 状态的权限调用 API reject 解阻塞。
- T5 最小实现与绿灯：
  - `internal/store/store.go`:
    - 定义 `ErrTicketConflict`。
    - 实现 `AnswerTicketApplied(id, answer string) (bool, error)`，原子检查工单状态：未回答则更新并返回 `(true, nil)`；相同答案已回答返回 `(false, nil)`；不同答案已回答或已作废返回 `(false, ErrTicketConflict)`。
    - 抽取 `touchTaskUpdatedAt(tx, taskID)`。
  - `internal/agentd/server.go`:
    - `handleReply` 改用 `AnswerTicketApplied`，捕获 `ErrTicketConflict` 返回 409 Conflict；在 `!applied`（幂等）时直接返回 200 `{"ok":true,"idempotent":true}`，不发起重放中继。
  - `internal/agentd/manager.go`:
    - `Stop` 中对活跃的 ask 工单作废，确保不重写 policy 为 allow。
  - `internal/executor/opencode/adapter.go`:
    - `Stop` 在拒绝挂起提问后、kill 进程前，扫描 `r.permTiming` 中 `paused == true` 的条目，逐一调用 `r.api.RespondPermission(ctx, sess, id, "reject")` 避免 opencode serve 死锁。
  - `internal/executor/opencode/resume_cold_internal_test.go`:
    - 适配冷恢复测试，注入 `fakeApprovalClient` 满足非 nil Approval 不变量。
- 测试通过：
  - `go test ./internal/store -run 'TestAnswerTicket' -count=1`（ok 0.003s）
  - `go test ./internal/agentd -run 'TestHandleReplyIdempotentDoesNotRelayTwice|TestStopVoidsAskAndDoesNotRewriteAllow' -count=1`（ok 0.523s）
  - `go test ./internal/executor/opencode -run 'TestStopRejectsPendingPermissions' -count=1`（ok 0.005s）

## Task 6: 接缝回归、其它 harness 负例、CHANGELOG、变异验证

- 基线检查：
  - `go test ./internal/executor/claudecode ./internal/executor/codex ./internal/executor/grok ./internal/executor/agy -count=1`（全部通过，退出码 0）。
- 负例测试：
  - `internal/agentd/manager_test.go`: `TestNonOpenCodeKeepsOldApprovalPath`（验证 grok 派发时 `StartReq.Approval == nil`，且 handleQuestion 后回答走 `Send` 记录而非 RespondAsk）。
- CHANGELOG 更新：
  - `CHANGELOG.md`: 在 `## [Unreleased]` 新增 `### 变更`，记录 B233.1 四句行为改动。
- 变异验证：
  - 变异 1：把 OpenCode `waitQuestion` 改回 `ad.Send`，运行 `go test ./internal/agentd -run TestOpenCodeWaitQuestionUsesRespondAsk -count=1`，精确变红（`RespondAsk 必须被调用 1 次，实得 0`）；还原后变绿。
  - 变异 2：把 `cloneBashRulesAskDefault` 的 `"*"` 改为 `allow`，运行 `go test ./internal/executor/opencode -run TestWritePermissionConfigSubsetOfSnapshot -count=1`，精确变红（`快照不允许无限制 bash 时，不得写 "*": allow`）；还原后变绿。
- 触及面全量回归与静态检查：
  - `go build -o /dev/null .`（编译成功，退出码 0）。
  - `codegraph --repo . --view cards-B233.1-charter-2 check`（fails: []，0 违规，退出码 0）。
  - `go test ./internal/executor/... ./internal/agentd/ ./internal/client/ ./internal/store/ ./internal/permgate/ ./internal/proto/ ./cmd/ -count=1`（全部包通过，退出码 0）。

## Review Fail 修复轮（charter-6）

- 目标修复项：
  - Major 1: 模型审批 allow 落可复用已送达 gate 工单与 approver_decision，二次同指纹请求零 permission_request 并落 permission_reuse。
  - Major 2: Continue 换版本重绑 ApprovalClient 且不复用旧 grant；probe 生产路径默认 fail-closed 及注入探针测试。
  - Major 3: 架构图补全 `d_execution -> d_orchestration` 接口契约与 view 真实节点，彻底消除 dummy 占位与 check fails。
  - Minor: 清理 adapter.go 残留注释、补齐 isDeliverable 注释清单、更新 taskenv.go 头注释。

- TDD 红灯记录：
  - Major 1 红灯：`TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses` 运行失败：`审批模型 allow 必须落 gate 工单: 记录不存在`。
  - Major 2 红灯：
    - `TestContinueRebindsApprovalClientAndDoesNotReuseOldGrant` 运行失败：`BindApproval 必须被调用 1 次，实际 0`。
    - `TestAdapterBindApproval` 运行失败：`BindApproval 后 r.approval 必须更新为新 client`。
  - Major 3 红灯：`codegraph check` 原先存在 0-edge dummy 节点违规。

- 最小实现与绿灯：
  - `internal/agentd/approval_client.go`:
    - 抽取 `tryReuse(ev, ticketID, fp)`，在 `Request` 首段针对非 AutoAllow 请求优先查复用；命中则发 `permission_reuse` 并 allow，不调 approver、不建工单、不发 `permission_request`。
    - `consult()` 在模型裁决 `Approve=true` 时：清零失败计数，落 `approver_decision` 审计事件，建 `kind="gate"` 且带 `Fingerprint: fp` 的工单，`AnswerTicket("allow")`，落 `ticket_answered` 事件，`MarkTicketDelivered` 标记送达后返回 allow。模型未放行或报错时 fail-closed 升级人工并累计失败计数。
  - `internal/executor/approval.go`:
    - 定义 `ApprovalBinder` 接口：`BindApproval(taskID string, client ApprovalClient) error`。
  - `internal/executor/opencode/adapter.go`:
    - 声明 `var _ executor.ApprovalBinder = (*Adapter)(nil)`，实现 `BindApproval` 更新运行态 `r.approval`。
    - 清理 Send 孤立注释。
  - `internal/agentd/manager.go`:
    - `Continue` 在 `ApplySnapshot` 成功后检测 `ApprovalBinder` 并调用 `BindApproval(taskID, m.bindApproval(taskID, snap))`。
  - `codegraph/target.json` & `codegraph/diffs/cards-B233.1-charter-2.json`:
    - `target.json`: 声明 `d_execution -> d_orchestration` `interfaces: ["ApprovalClient"]`，`legacyBudget: 0`。
    - `diffs/cards-B233.1-charter-2.json`: 增加 `m_agentd_taskApprovalClient`、`n_agentd_Manager_bindApproval`、`n_opencode_Adapter_authorizeNativePermission` 等真实节点、边与 `implementsAdded`。
  - `internal/client/client.go`:
    - 注释清单补充 `permission_reuse`。
  - `internal/executor/opencode/taskenv.go`:
    - 文件头注释补充 B233.1 下通配 `"*"` 强制为 ask 走 ApprovalClient 判定的说明。

- 验证结果：
  - `go test ./internal/agentd -run TestApprovalClient -count=1`（PASS，11 项全绿）。
  - `go test ./internal/agentd -run TestContinueRebindsApprovalClientAndDoesNotReuseOldGrant -count=1`（PASS）。
  - `go test ./internal/executor/opencode -run 'TestApplySnapshot|TestAdapterBindApproval' -count=1`（PASS，3 项全绿）。
  - `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate`（exit 0）。
  - `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.1-charter-2 check`（exit 0，fails: []）。
  - `go build -o /dev/null .`（exit 0）。
  - 全包单测 `go test ./internal/executor/... ./internal/agentd/ ./internal/client/ ./internal/store/ ./internal/permgate/ ./internal/proto/ -count=1`（全部 PASS，exit 0）。

- 提交事实：
  - `git add . && git commit -m "fix(B233.1): 修复审批复用、Continue 重绑与契约图 (charter-6)"`
  - 原始输出：`[cards/B233.1-charter-6 21d012a1] fix(B233.1): 修复审批复用、Continue 重绑与契约图 (charter-6)`



