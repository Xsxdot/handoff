# Ledger Node B156.1 implementation ledger

- Baseline: `069ef0bc`; existing `dispatch_templates` schema and discipline block files were present before this plan.
- Task 1 completed: added immutable versioned DispatchTemplate storage, default feature/review templates with embedded handoff-verdict contract, per-target model overrides, and coverage for idempotent seeding; commit scope: `internal/ledger/templates.go internal/ledger/templates_test.go docs/superpowers/ledgers/2026-08-19-ledger-node.md`.
- Task 2 completed: added `template list/show/put`, version selection, required-definition validation, and unified default template seeding in `openLedger`; `go test ./cmd/ -run TestTemplateListShowPut -v` passed; commit scope: `cmd/template.go cmd/template_test.go cmd/ledgercli.go docs/superpowers/ledgers/2026-08-19-ledger-node.md`.
- Task 3 completed: added strict `handoff-verdict` parsing that selects the final fenced block, preserves raw evidence, and rejects missing, malformed, or unknown verdicts; `go test ./internal/ledgernode/ -run TestParseVerdict -v` passed; commit scope: `internal/ledgernode/verdict.go internal/ledgernode/verdict_test.go docs/superpowers/ledgers/2026-08-19-ledger-node.md`.
- Task 4 completed: added stateless event-derived round counting with per-node isolation, human reset handling, malformed-payload skipping, and `MaxRounds=3`; `go test ./internal/ledgernode/ -run TestCountRounds -v` passed; commit scope: `internal/ledgernode/rounds.go internal/ledgernode/rounds_test.go docs/superpowers/ledgers/2026-08-19-ledger-node.md`.
- Task 5 completed: added `RecordDispatch` snapshots and card dispatch with CAS/driver claim, discipline hash, template substitution/versioning, project/target/model/base/plan forwarding, task linking, rollback, and CLI tests; `go test ./cmd/ -run TestCardDispatchClaimAndSnapshot -v` and `go test ./internal/ledger/ -run TestRecordDispatch -v` passed; commit scope: `cmd/card_dispatch.go cmd/card_dispatch_test.go internal/ledger/events.go internal/ledger/events_test.go docs/superpowers/ledgers/2026-08-19-ledger-node.md`.
- Task 6 completed: added ReviewNode/MergeNode decision engines, review verdict event recording, round cap and parse-failure handling, main/manual merge routing, objective-gate-first integration merge, raw-error timeline evidence, and decision logs; `go test ./internal/ledgernode/ ./internal/ledger/ -v` passed; commit scope: `internal/ledgernode/node.go internal/ledgernode/node_test.go internal/ledger/events.go docs/superpowers/ledgers/2026-08-19-ledger-node.md`.
