# B233.10 spec 台账

日期：2026-09-10
卡：B233.10
节点：spec
基线：`cards/B233.1-charter-7` @ `3fd6874f`

## 过程与判断

1. 用户把另一人的架构意见交给本会话核对，并回复「OK，继续」。采纳：三条边界作为 B233 剩余验收；本卡只做派发生命周期。
2. `handoff card split B233` 得 B233.10；`card move spec`；优先级改高。父卡仍待办，不作实现卡。
3. 亲读 `internal/agentd/cardstep.go#Server.startCardStep`：绑小队时 :83 `admitSquadStep`，goroutine :167 `defer releaseSchedulingBinding`。
4. 亲读 `internal/ledgerstep/runner.go` :337 `Receiver: node.Override.Squad`；`stepTransport` :350 把 Receiver 交给 `Client.Dispatch`。`DispatchOpts` 无冻结载体字段。
5. 亲读 `internal/agentd/server.go#Server.handleDispatch` :1374 再 `prepareReceiverBinding`；成功 :1410 把占用留给 `releaseTaskCarrierOccupancy`（Done/Stop）。
6. `prepareReceiverBinding` :71 载体 `AdmitCarrier`，:73 小队 `Admit`。因此卡节点路径是起源 `Admit` + 执行机 `Admit`。
7. 编制计数经 `facadeAsRegistry` 写入账本 `sched_running`；键见 `internal/scheduling/occupancy.go`。本环境账本 dialect=postgres，同键跨机可见。未在本回合对 linux-01 做占用计数真机，不把历史 no_slot 写成已复现结论。
8. `codegraph context d_scheduling` 命中编制域包。`who-calls Service.Admit` 基线 edges 空、5 个未扫描入口，记图覆盖债；调用方以源码为准。
9. 弃选「起源占用、执行机凭 token 跳过」：起源不是 executor 所在，节点结束会提前释放。弃选「只删起源准入、执行机仍对小队 Admit」：落点仍可换成员。
10. 定级 L3 轻档：动 POST /api/tasks 身份字段，跨编制/网关/账本装配/client；审批与工作区不纳入故不重档。
11. spec 落盘时头部为待用户批准。批准前不实现、不派 contract。
12. 2026-09-10 用户原话「同意，批这条 spec。」头部回写已批准；卡移 contract。
13. 提交命令：`git add docs/superpowers/specs/b233.10.md docs/superpowers/ledgers/2026-09-10-b233.10-spec-ledger.md docs/roadmap.md && git commit -m "docs(B233.10): spec 批准 L3 轻档（一次派发生命周期）"`。原始输出：`3 files changed, 172 insertions(+)`，`[cards/B233.1-charter-7 13b999b3] docs(B233.10): spec 批准 L3 轻档（一次派发生命周期）`。随后 amend 一次收进同批；不回写 amend 后 hash。
