# B233.1 contract 台账

日期：2026-09-06。节点：charter contract。工作分支：`cards/B233.1-charter-2`。开工 HEAD：`63aa4aff`。

## 过程

1. 核对上游 spec `docs/superpowers/specs/b233.1.md` 头部状态行 = **已批准**（2026-09-06），与 prompt「上游 spec 已批准」一致，未回写。
2. 查图：`codegraph summary` / `domains` / `context d_execution_contract` / `sym Adapter` / `sym Dispatch`。`who-calls handlePermission` 仍需完整节点 id `n_agentd_Manager_handlePermission`（短名不在图中，记图覆盖债）。
3. 基线代码事实已对照：`internal/executor/executor.go#Adapter`、`opencode/adapter.go#Send`、`manager.go#Dispatch`/`handlePermission`/`handleQuestion`/`RelayAnswer`/`reuseDecision`/`consultApprover`、`permgate.go#Gate.Judge`、`approver.go#Decide`、`client.go#Dispatch`/`Reply`/`isDeliverable`、`store.go#CreateTicket`/`FindReusableGrant`/`AnswerTicket`、`cmd/reply.go`、`opencode/taskenv.go#WriteTaskEnv`、`proto.go#EventType`/`Ticket`。
4. 架构法第一条实操裁决：本卡 **不** 把审批升格为新顶层子系统（轻档、物理落点 OOS、client 先立在使用方）。
5. `codegraph check` 现状 `d_orchestration->d_execution` 使用 26 / 预算 27。Ticket 0 不把 manager 生产路径改接到新编码函数，避免超预算。
6. Ticket 0 空壳：`internal/executor/approval.go`、`ask.go`、`fingerprint.go`；`StartReq.Approval` 注入槽；金样本测试；agentd 侧指纹对齐测试与 `gateDecision` 金样本。
7. 契约文档写到法定路径 `docs/superpowers/specs/b233.1-contract.md`。视图 `codegraph/diffs/cards-B233.1-charter-2.json`。target.json 仅给既有 `d_orchestration→d_execution` 补 B233.1 注记，不新开方向、不改预算。

## 命令读数

- `gofmt -w` 触及的 Go 文件：exit 0。
- `go test ./internal/executor/ ./internal/agentd/ -count=1 -timeout 120s -run 'TestNamespacedTicketIDGoldenVectors|TestPermFingerprintGoldenVectors|TestPermFingerprintCommandDomainIgnoresTwinKind|TestApprovalStatusLiterals|TestStartReqApprovalSlotIsOptional|TestPermFingerprintForMatchesExecutorCanonical|TestGateDecisionGoldenVectors'`：两包 `ok`，exit 0。
- `go test ./internal/executor/... -count=1 -timeout 180s`：executor / agy / claudecode / codex / fake / grok / opencode / rawtap / turn 全 `ok`，exit 0。
- `go build -o /dev/null .`：exit 0。
- `go test ./internal/agentd/ -count=0`：`ok ... [no tests to run]`，exit 0（编译测试包）。
- `codegraph --repo . resolve --doc docs/superpowers/specs/b233.1-contract.md --view cards-B233.1-charter-2`：exit 0。新符号 `ApprovalClient` / `StartReq` / `AskResponder` / `NamespacedTicketID` / `PermFingerprint` 均为 `anchor=ok`。存量符号部分 `moved`（行号相对基线漂移），均决议到节点，无坏锚。
- `codegraph --repo . check --view cards-B233.1-charter-2`：exit 0，`fails []`。`d_orchestration->d_execution` 仍 26/27。
- `codegraph --repo . validate --view cards-B233.1-charter-2`：exit 0，`issues: null`。

## 判断

- 不升格 `d_approval`：轻档不换并行开发；物理落点是父卡 OOS。
- AskResponder 不进 Adapter 五动作。
- 可执行冻结走编码金样本，不把 OpenCode Send 拆线（那是可观测行为，留给 implement，且须能变红的测试）。
- 三重闸门五条均写入契约 §7。
- 欠账见契约 §8：无「已实现但零测试」。

## 提交

```
git commit -m "contract(B233.1): 冻结派发事件流、Ask 与审批 client 契约"
[cards/B233.1-charter-2 17f03c10] contract(B233.1): 冻结派发事件流、Ask 与审批 client 契约
 13 files changed, 1053 insertions(+), 1 deletion(-)
 create mode 100644 codegraph/diffs/cards-B233.1-charter-2.json
 create mode 100644 docs/superpowers/ledgers/2026-09-06-b233.1-contract-ledger.md
 create mode 100644 docs/superpowers/specs/b233.1-contract.md
 create mode 100644 internal/agentd/fingerprint_contract_test.go
 create mode 100644 internal/agentd/gatedecision_contract_test.go
 create mode 100644 internal/executor/approval.go
 create mode 100644 internal/executor/approval_test.go
 create mode 100644 internal/executor/ask.go
 create mode 100644 internal/executor/ask_test.go
 create mode 100644 internal/executor/fingerprint.go
 create mode 100644 internal/executor/fingerprint_test.go
```

随后 amend 一次把本段收进同批提交。不回填 amend 后 HEAD。
