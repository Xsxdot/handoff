# B156.1 Web ledger

- Task 1：实现 `LatestTaskStates` 与 `OpenTicketCounts`，补齐镜像流回放测试；双裁决通过，无修复轮。验证：`go test ./internal/ledger/ -run 'Test(LatestTaskStates|OpenTicketCounts)' -v` 通过。commit 范围：`internal/ledger/taskstate.go`、`internal/ledger/taskstate_test.go`、本 ledger。
- Task 2 修复轮 1：双裁决发现未挂账摘要遍历配置 map 的输出顺序不稳定；按 target 名、task ID 和 unknown target 排序后复审通过。commit 范围：`internal/agentd/ledgerapi.go`、本 ledger。
- Task 2：新增账本 cards/flows/decisions/health HTTP 读面与 move/note/answer 写面、错误翻译、未挂账 target 摘要缓存；agentd 无条件开账本库并注入 Server，镜像仍按 targets 门控。双裁决通过。验证：`go test ./internal/agentd/ -run TestLedgerAPI -count=1 -v`、`go test ./cmd/ -run '^$'`、`go vet ./internal/agentd/ ./cmd/` 均通过。commit 范围：`internal/agentd/server.go`、`internal/agentd/ledgerapi.go`、`internal/agentd/ledgerapi_test.go`、`cmd/agentd.go`、本 ledger。
- Task 3：新增账本 API 客户端类型/端点函数、导出 `request`/`postJSON`，实现 cards 列/需要你/跟随纯逻辑及契约测试；双裁决通过，无修复轮。验证：`npm test -- --run columns` 通过（2 files/13 tests）、`npm run typecheck` 通过、`npm run lint` 退出 0（13 个既有 warning、0 errors）。commit 范围：`web/src/api/client.ts`、`web/src/api/ledger.ts`、`web/src/app/cards/columns.ts`、`web/src/app/cards/columns.test.ts`、本 ledger。
- Task 4 修复轮 1：补齐 workflow wire 字段与未选工作流时的全状态列序，并将 `CardsPage` cards 归一化为稳定 memo；抽屉契约复审通过。commit 范围：`internal/agentd/ledgerapi.go`、`web/src/api/ledger.ts`、`web/src/app/cards/CardsPage.tsx`、`web/src/app/cards/CardItem.tsx`、`web/src/app/cards/columns.test.ts`。
- Task 4 修复轮 2：按真实 Go 空列表会序列化为 `null` 的边界，补齐抽屉事件/附件/task_states 与 health mirror 的空值归一化；复审通过。commit 范围：`web/src/app/cards/CardDrawer.tsx`、`web/src/app/cards/CardItem.tsx`、`web/src/app/cards/ListView.tsx`、`web/src/app/cards/CardsPage.tsx`。
- Task 4：实现 `/cards` 看板/列表、需要你就地筛选、项目裁决细条、未挂账摘要、卡片 chips、单抽屉详情、关系/并入/任务实况/分层 timeline/评论与 move 二次确认；双裁决通过。验证：`npm test -- --run CardDrawer columns` 通过（3 files/14 tests）、`npm run typecheck` 通过、`npm run lint` 退出 0（13 个既有 warning、0 errors）。commit 范围：`web/src/app/cards/CardsPage.tsx`、`web/src/app/cards/CardItem.tsx`、`web/src/app/cards/CardDrawer.tsx`、`web/src/app/cards/CardDrawer.test.tsx`、`web/src/app/cards/ListView.tsx`、相关 API wire 字段与本 ledger。
- Task 5 修复轮 1：新增 ProjectTree dock props 兼容既有测试构造器，缺省时入口无操作且角标为零；双裁决复审通过。验证：`npm run typecheck` 通过。commit 范围：`web/src/app/tree/ProjectTree.tsx`。
- Task 5：新增 `/flows` 工作流/派发模板只读双 tab，接入 Shell 路由与 ProjectTree `▤` 工作项入口及需要你角标；Shell 轮询卡片与项目级裁决计算角标。双裁决通过。验证：`npm test` 通过（73 files/760 tests）、`npm run typecheck` 通过、`npm run lint` 退出 0（13 个既有 warning、0 errors）、`npm run build` 通过。commit 范围：`web/src/app/flows/FlowsPage.tsx`、`web/src/app/shell/Shell.tsx`、`web/src/app/tree/ProjectTree.tsx`、本 ledger。
