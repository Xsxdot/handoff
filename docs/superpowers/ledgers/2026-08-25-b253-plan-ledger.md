# B253 计划轮台账

- 2026-08-25：基线 `git status --short --branch` 原始输出：`## cards/B253-charter`；`git log -8 --oneline --decorate` 首行：`deb3d461 (HEAD -> cards/B253-charter, origin/claude/archive-247-close-252-254-4fe606, cards/B254-charter, cards/B252-charter) docs(spec): B252/B253/B254 三卡 spec 定稿；B247 搁置；roadmap 收四条残余`。工作树起始无未提交改动。
- 2026-08-25：仓内存在 `codegraph/baseline.json`、`codegraph/best.json`、`codegraph/target.json`；未发现 B253 既有计划或台账，按约定在产出物同目录新建本台账。
- 2026-08-25：已读 `docs/superpowers/specs/2026-08-25-b253-card-wait-snapshot.md`；规格状态为已批准，选定方案是 `card wait` 建连时逐成员输出一次 `type=card_snapshot` 单行 JSON；明确不做 cursor/事件回放、`--step` 202 同步回吐和 task wait 改动。
- 2026-08-25：`go run . graph context d_cli --repo . 2>/dev/null | jq ...` 原始关键输出：`{"view":"baseline","domain":{"id":"d_cli","label":"协调者命令面"},"packages":[{"path":".","summary":""},{"path":"cmd","summary":""}],"actual":{"containers":3,"byCurrentDomain":[{"id":"d_coordination_cli","label":"协调者命令","containers":3}],"misplaced":[],"misplacedSkipped":3},"warning":"领域声明缺失：codegraph/domains/d_cli.json；请按 roadmap 1a 补齐","fociTruncated":{"total":87,"shown":5,"reason":"focus-quota"}}`。
- 2026-08-25：`go run . graph context d_ledger --repo . 2>/dev/null | jq ...` 原始关键输出：`{"view":"baseline","domain":{"id":"d_ledger","label":"卡片账本"},"packages":[{"path":"internal/ledger","summary":""},{"path":"internal/ledgermirror","summary":""},{"path":"internal/ledgerstep","summary":""}],"actual":{"containers":12,"byCurrentDomain":[{"id":"d_ledger","label":"账本域","containers":12}],"misplaced":[],"misplacedSkipped":0},"warning":"领域声明缺失：codegraph/domains/d_ledger.json；请按 roadmap 1a 补齐","fociTruncated":{"total":57,"shown":5,"reason":"focus-quota"}}`。图上下文有焦点配额截断，计划另记图覆盖债并以源码复核。
- 2026-08-25：图 `d_ledger` 接口列表原始签名含 `func (s *Store) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]Event, error)`、`func (s *Store) Follow(ctx context.Context, members func() ([]string, error), fromSeq int64, pollInterval time.Duration, onEvent func(Event) error) error`、`func (s *Store) GetCard(id string) (Card, error)`、`func (s *Store) NeedsOf(cardID string) (string, error)` 未在接口清单中出现但源码存在，计划以源码为准并列图覆盖债。
- 2026-08-25：源码核对：`cmd/card_wait.go#runCardWait` 先 `st.MaxSeq()`，再构造 `members`，随后 `checkDone`，最后 `st.Follow(..., start, 2*time.Second, ...)`；当前 stdout 仅 `json.Encoder.Encode(e)` 输出事件，已完成子树在 Follow 前写 stderr 并返回。
- 2026-08-25：源码核对：`internal/ledger.CardView` 的派生字段为 `NeedsReason string json:"needs,omitempty"`；`ListCards` 通过 `needsMap` 计算，`NeedsOf(cardID string) (string, error)` 单卡按 `needs_human`/`needs_cleared` seq 顺序回放，清除后返回空串。
- 2026-08-25：源码核对：`internal/ledger.Subtree(rootID string) ([]string, error)` 返回 root、parent 后代和并入成员，集合由 map 组装，当前返回顺序未排序；计划要求快照成员按 `card_id` 排序，以固定脚本线序，Follow 成员动态重算语义不改。
- 2026-08-25：源码核对：`internal/ledger.Event` JSON 字段含 `seq`、`card_id`、`type`、`actor`、`payload`、`created_at` 等；`EventsFromAsc` 的 `fromSeq` 是排他游标，`Follow` 每轮调用 `members()` 并从游标读事件。
- 2026-08-25：源码核对：对照物 `internal/client/backlog.go#BacklogSummary` 的 stdout 线格式使用稳定 `type` 字段和显式 JSON 字段；`cmd/wait_backlog_test.go#TestBacklogSummaryLineIsSingleJSON` 断言恰好一行、合法 JSON、`type` 和关键字段。
- 2026-08-25：基线判据命令 `go test ./cmd/ -run 'TestCardWaitSubtreeExitsWhenAllDone|TestBacklogSummaryLineIsSingleJSON' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/cmd  2.139s`，exit 0。
- 2026-08-25：基线判据命令 `go test ./cmd/ -run 'TestCardWait|TestBacklogSummaryLineIsSingleJSON|TestWaitRejectsCardFlag' -count=1` 原始输出：`ok   github.com/Xsxdot/handoff/cmd  2.177s`，exit 0；`go vet ./cmd` 原始 stdout 为空，exit 0。
- 2026-08-25：`go run . graph sym runCardWait --repo .` 命中 `n_cmdrunCardWait`，签名原文：`func runCardWait(cmd *cobra.Command, cardID string, subtree bool, timeout time.Duration) error {`；`sym Store.NeedsOf` 命中 `func (s *Store) NeedsOf(cardID string) (string, error)`；`sym Store.Subtree` 命中 `func (s *Store) Subtree(rootID string) ([]string, error)`。
- 2026-08-25：`go run . graph chain runCardWait --with-source --repo .` 原始关键输出含边 `n_cmdrunCardWait -> n_cmdopenLedger`、`n_cmdrunCardWait -> n_ledger_Store_GetCard`，并报告 `unscannedEntries: 6`、`warning: 基线仍有 6 个未扫描入口：查询结果为空不等于没有调用方`；`who-calls Store.NeedsOf` 未返回调用者节点但同样报告该 warning，不能据此判断无调用面。
- 2026-08-25：已读项目缺陷族清单 `docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md:76-83`；本卡触及通用五族（生命周期/状态机中断、静默失败/误导报错、跨平台、假红、门禁绕过）中的前两族及序列化边界，webview/平台表现差异不适用；计划末尾逐族回答。
- 2026-08-25：`rg -n 'card_snapshot|switch .*type|Event.*Type|json.RawMessage' cmd internal/ledger internal/client` 原始结果无 `card_snapshot` 命中；现有 `proto.EventType` 白名单只在执行域 client 内使用，B253 新线型不进入该枚举或事件过滤 switch。
- 2026-08-26：第一次写计划的 `apply_patch` 被拒，原始错误为 `apply_patch verification failed: invalid hunk at line 540, 'go vet ./cmd' is not a valid hunk header. Valid hunk headers: '*** Add File: {path}', '*** Delete File: {path}', '*** Update File: {path}'`；未产生计划文件。
- 2026-08-26：第二次生成计划的工具脚本在落盘前失败，原始错误为 `SyntaxError: Unexpected identifier 'json'`；未产生计划文件。
- 2026-08-26：第三次用 apply_patch 成功创建 `docs/superpowers/plans/b253-plan.md`；随后补充了 cmd/card_wait_test.go 的完整 import 块、生产调用前日志、测试代码围栏与自审措辞。
- 2026-08-26：计划文件自审 `wc -l` 原始输出为 `604 docs/superpowers/plans/b253-plan.md`、`19 docs/superpowers/ledgers/2026-08-25-b253-plan-ledger.md`；`git status --short --branch` 仅显示当前分支和这两份未跟踪产出物。
- 2026-08-26：占位符扫描 `rg -n 'TBD|TODO|同 Task|适当的错误处理|占位符' docs/superpowers/plans/b253-plan.md || true` 唯一命中计划自审说明行（该行描述扫描动作，不是执行占位）；已复核代码块无执行占位。计划自审还复核了测试代码段围栏、完整 import 块和日志步骤表达，未改实现文件。
- 2026-08-26：暂存后自审命令 `git diff --cached --check` 原始 stdout 为空且 exit 0；`rg -c '^~~~' docs/superpowers/plans/b253-plan.md` 原始输出 `fence_count=26`（成对）；严格占位符扫描原始输出 `placeholder_matches=` 后无匹配；暂存内容仅为本计划与本台账。
- 2026-08-26：`git commit -m "docs(plan): B253 card wait snapshot"` 原始输出：`[cards/B253-charter 022ce20e] docs(plan): B253 card wait snapshot`、`2 files changed, 647 insertions(+)`；随后 `git status --short --branch` 原始输出仅 `## cards/B253-charter`，`git show --stat --oneline --decorate HEAD` 确认提交只含本计划与本台账。
