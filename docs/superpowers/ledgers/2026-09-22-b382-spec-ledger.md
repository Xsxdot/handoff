# B382 spec 台账（2026-09-22）

> 本文件是 B382 spec 节点的过程账：命令与原始读数逐条落这里，spec 正文只留结论与指针。

## 1. 现状读数（代码事实，带出处）

- **工作分支的唯一来源**：`Store.WorkBranch` 只扫 `dispatched` 事件快照——跳过 `purpose=review`、取最后一条非审阅快照的 `Branch`；一条都没有则 `ErrNotFound`，文案在 `internal/ledger/events.go:610`（`internal/ledger/events.go:575-613`）。
- **审阅轮拒发点**：`internal/ledgerstep/dispatch.go:245-248`（`if purpose == PurposeReview { if !hasWorkBranch { return zero, fmt.Errorf("审阅轮取工作分支: %w", workErr) } }`）。
- **派发 base 决议**：`internal/ledgerstep/dispatch.go:274-286`——先取 `EffectiveBaseBranch`，若有工作分支则 `base = workInfo.Branch`（注释原话：「后续节点从工作分支起，不是从卡合并基线起」）；卡片上下文另行取一次 `cardBase` 当合并目标（`:291`）。
- **pass 后推 origin**：`internal/ledgerstep/node.go:430`（`PublishWorkBranch`）。
- **base_branch 的契约与写入口**：`internal/ledger/cards.go:639`「SetCardBaseBranch 为**尚未出现任何 dispatched 事件**的卡设置或清除显式基线」；CLI 面 `card add --base-branch`（空=继承/主线）、`card update --base-branch`（空串=清除）。
- **跨机闸**：`internal/ledgerstep/dispatch.go:207-220`——工作分支未发布 origin 且目标机不同即拒发，并给 `git push origin <分支>` 指路。
- **图查询**：`codegraph --repo . sym WorkBranch` → 命中 `n_ledger_Store_WorkBranch`，domain `d_ledger`，container `k_ledger_Store`，`internal/ledger/events.go:575`（图覆盖无债）。

## 2. 四张真机实例的取数（`handoff card show <卡>`，2026-09-22）

| 卡 | dispatched 快照 | effective_base_branch | 代码落点（git 核验） |
|---|---|---|---|
| B373 | **0 次** | `''`（空） | `feat/b373-opencode-monitor` 已在功能线内 |
| B320 | **0 次** | `''`（空） | `80e53e17f` 在功能线内 |
| B367 | **0 次** | `''`（空） | `398b3551f` 在功能线内 |
| B372 | **0 次** | `''`（空） | 合并提交 `d46d4ec3e` 在功能线内 |
| B233.28（对照，正常卡） | 有 | `cards/B233.1-charter-7` | — |

**结论（弃选 A 的依据）**：四张人工实现卡的 `effective_base_branch` 全为空 ⇒ A 的「回落 base_branch」对真实卡**无效**；一旦人工去设该字段，就把 finish 的合并去向一起改掉（正常卡该字段=合并目标，见 B233.28），且失败是静默的（review 会跑、看着成功，看的是另一棵树）。

## 3. 定级先例检索（本仓成文）

- **B396** spec 头部：「级别：L2 单子系统（节点运行锁：`internal/ledger` 的锁实现 + `cmd`/`agentd` 的判据面）」——与本卡形态同形 ⇒ **L2**。
- **B393** spec 头部：「L2 单子系统（`internal/agentd` + `internal/keystone` 的唤醒执行链）」。
- **B389** spec 头部：「L3 轻档——跨账本 / 调度 / agentd / CLI 四域」——反例：真跨四域者才是 L3。

## 4. 旁证发现（不在本卡范围）

- `RecordBranchMerged`（`internal/ledger/events.go:358`「落合并环节的外部动作事件」）全仓调用方检索：**仅** `internal/ledger/events_test.go:119` 的用例——「合并回基线」目前在账本上没有机器记录。

## 5. 裁决与裁定

- **decision #10 答复**（2026-09-22，用户）：选 E——人工登记工作分支作为快照缺失时的第二来源；`base_branch` 保持合并基线语义，不合并。A 因 §2 实测弃用。
- **生效范围裁定**（2026-09-22，用户）：人工登记**只在卡上还没有过非审阅 dispatched 快照时生效**，不允许覆盖已有快照。
- 附带更正：裁决文本里「修复已在 a25f0c746 是误推」一节已过时——`a25f0c746` 现已在功能线 `cards/B233.1-charter-7` 历史内，上游 main 头 `85840cf2` 亦在其内。
