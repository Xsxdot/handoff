# B233.10 contract 台账

日期：2026-09-10
卡：B233.10
节点：contract
基线：`cards/B233.1-charter-7`
法定产出：`docs/superpowers/specs/b233.10-contract.md`

## 过程与判断

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-10 | 当前工作树初始干净，当前分支为 `cards/B233.10-charter`，HEAD 为 `a1571673`；spec 头部状态为「已批准」。 | `git status --short --branch && git log -5 --oneline --decorate` → `## cards/B233.10-charter`；HEAD `a1571673 (HEAD -> cards/B233.10-charter, origin/cards/B233.1-charter-7) docs(B233.10): spec 批准 L3 轻档（一次派发生命周期）`；`sed -n '1,4p' docs/superpowers/specs/b233.10.md` → `状态：**已批准**（2026-09-10，用户原话「同意，批这条 spec。」）`。 |
| 2026-09-10 | 本项目有 `codegraph/best.json`、`codegraph/target.json`；架构形态沿用最优图声明的「按子系统分域的平铺领域包，无横向 controller/service/dao 分层」。 | `find . -maxdepth 2 -type f ...` → `./codegraph/baseline.json`、`./codegraph/best.json`、`./codegraph/target.json`；`jq '.domains | ...' codegraph/best.json` → 命中 `d_gateway`、`d_transport`、`d_orchestration`、`d_protocol`、`d_scheduling` 等顶层域。 |
| 2026-09-10 | 图查询入口可用；`Server.handleDispatch`、`Client.Dispatch`、`Service.Admit` 均有图符号，但均为 `anchor: moved`；`Service.Admit` 的 `who-calls` 不可作为「无调用方」事实。 | `GOMODCACHE=/root/.handoff/tmp/8bdfc04f/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'Server.handleDispatch'` → `internal/agentd/server.go:1360`，签名 `func (s *Server) handleDispatch(w http.ResponseWriter, r *http.Request)`；同命令 `sym 'Client.Dispatch'` → `internal/client/client.go:759`，签名 `func (c *Client) Dispatch(ctx context.Context, opts DispatchOpts) (*proto.Task, error)`；`sym 'Service.Admit'` → `internal/scheduling/scheduling.go:489`，签名 `func (s *Service) Admit(req IgnitionRequest) (Binding, error)`。 |
| 2026-09-10 | 编制图的上游调用覆盖不足，源码查证为事实源。 | `... codegraph --repo . who-calls 'Service.Admit'` → `edges: null`、`unscannedEntries: 5`、`warning: 基线仍有 5 个未扫描入口：查询结果为空不等于没有调用方`。 |
| 2026-09-10 | 编制域现有 `Service` 以 Registry 持久计数；`AdmitCarrier` 只写载体键，`Admit` 写小队成员键与载体键，`Release` 按 `OccupancyKeys` 反向释放。 | `nl -ba internal/scheduling/scheduling.go | sed -n '375,418p;488,586p'` → `AdmitCarrier` 读 `carrier/<name>` 并返回 `Binding{Squad:"", Carrier:..., Target:..., Executor:..., HomeDir:...}`；`Admit` 调 `admitInto`；`Release` 调 `OccupancyKeys` 后分别 `stepRunning(...,-1)`。 |
| 2026-09-10 | 现有卡节点路径在起源侧准入并在 goroutine 返回时释放；执行机 `POST /api/tasks` 又按 Receiver 解析并准入。 | `nl -ba internal/agentd/cardstep.go | sed -n '75,173p'` → `admitSquadStep`、`defer s.releaseSchedulingBinding(cardID, binding)`；`nl -ba internal/agentd/receiver_bind.go | sed -n '59,105p'` → `prepareReceiverBinding` 内 `AdmitCarrier` / `Admit`；`nl -ba internal/agentd/server.go | sed -n '1367,1414p'` → `handleDispatch` 调 `prepareReceiverBinding`，成功置 `released = true`。 |
| 2026-09-10 | 现有任务快照已具备 `Carrier`、`Squad`、`HomeDir`，终态释放入口位于 gateway 的 Done/Stop；`Continue` 与 `Resume` 现有入口分别进 Manager，不经过编制准入。 | `nl -ba internal/proto/proto.go | sed -n '223,234p'` → `Task.HomeDir`、`Task.Carrier`、`Task.Squad`；`nl -ba internal/agentd/server.go | sed -n '1693,1759p'` → Done/Stop 后 `releaseTaskCarrierOccupancy`；`nl -ba internal/agentd/server.go | sed -n '1622,1650p;1796,1818p'` → Continue/Resume 调 Manager。 |
| 2026-09-10 | `POST /api/tasks` 目前只在 `client.Dispatch` 的 body 中发送 `receiver`/可空 `home_dir`，目标端 `dispatchRequest` 已有 `Target`/`Executor`/`HomeDir`/`Receiver`，但无冻结载体/小队 wire 字段。 | `nl -ba internal/client/client.go | sed -n '708,799p'` → `DispatchOpts` 与 `Client.Dispatch`；`nl -ba internal/agentd/server.go | sed -n '1318,1355p'` → `dispatchRequest` 字段；`nl -ba internal/agentd/cardstep.go | sed -n '330,361p'` → `stepTransport` 将 `Receiver`/`HomeDir` 透传。 |
| 2026-09-10 | HTTP 既成行为：服务端路由保持 `POST /api/tasks`；请求 JSON 解码由 `io.LimitReader(..., 4<<20)` 限制；client 使用带 context 的单次 `hc.Do`，无 POST 自动重试，底层直连 Transport 的拨号 timeout 为 10 秒。 | `nl -ba internal/agentd/server.go | sed -n '630,634p;1360,1371p'` → 路由与 `4<<20`；`nl -ba internal/client/client.go | sed -n '49,55p;378,429p'` → `dialTimeout = 10 * time.Second` 与单次 `hc.Do(req)`。 |
| 2026-09-10 | 图上下文命中 `d_scheduling`，但暴露领域声明缺失与 5 个未扫描入口；本卡不借图空洞否认源码调用，也不改存量图结构。 | `... codegraph --repo . context d_scheduling` → `domain.id=d_scheduling`、`packages=internal/schedclient, internal/scheduling`、`warning` 含 `领域声明缺失：codegraph/domains/d_scheduling.json`；未据此推断未显示的调用边。 |

## 收口记录

本台账按「先记录事实，再写契约，再跑验证，最后记录提交」追加；提交命令与原始输出在收口时追加，随后只 amend 一次收进同批提交，不追写 amend 后 hash。

| 2026-09-10 | Ticket 0 骨架已落：编制域新增 `Service.Select`、`Service.AdmitFrozen` 空壳；ledgerstep/client/agentd 的派发形状增加 `carrier`/`squad` 直通字段；新增 wire 金样本测试。该骨架不消费冻结身份，不改变现有准入与释放行为。 | `gofmt -w ... && git diff --check && go test ./internal/client ./internal/scheduling ./internal/ledgerstep ./internal/agentd -run 'TestDispatchFrozenIdentityJSONGolden|TestOccupancy|TestAdmitCarrier|TestCardStep|TestContinue|TestDone|TestStop' -count=1` → exit 0；原始输出：`ok github.com/Xsxdot/handoff/internal/client 0.015s`、`ok github.com/Xsxdot/handoff/internal/scheduling 0.243s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 0.005s [no tests to run]`、`ok github.com/Xsxdot/handoff/internal/agentd 10.497s`。 |
| 2026-09-10 | Ticket 0 金样本补齐 `model`，并新增 nil/显式空串 `HomeDir` 与空冻结身份的省略断言；图视图加入两个新空壳和四个修改后的 DTO。 | `gofmt -w internal/client/frozen_dispatch_test.go && git diff --check` → exit 0；`jq empty codegraph/target.json codegraph/diffs/cards-B233.10-charter.json` → exit 0，无输出。 |
| 2026-09-10 | 文档符号锚复核通过；首次 resolve 曾因图未覆盖的 `Service.AdmitCarrier`、`Server.releaseTaskCarrierOccupancy`、`Server.prepareReceiverBinding` 报 `Error: 文档锚点检查失败: ... (vanished)`，已改为带图覆盖债标注的源码出处并重跑。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --view cards-B233.10-charter --doc docs/superpowers/specs/b233.10-contract.md` → exit 0；原始关键输出含 `Service.Select`/`Service.AdmitFrozen` 两项 `anchor: ok`，命令无 `Error`。 |
| 2026-09-10 | 本轮 Ticket 0 编译与相关回归测试通过。 | `GOMODCACHE=/root/.handoff/tmp/8bdfc04f/gomodcache go test ./internal/client ./internal/scheduling ./internal/ledgerstep ./internal/agentd -run 'TestDispatchFrozenIdentityJSON|TestOccupancy|TestAdmitCarrier|TestCardStep|TestContinue|TestDone|TestStop' -count=1` → exit 0；原始输出：`ok github.com/Xsxdot/handoff/internal/client 0.024s`、`ok github.com/Xsxdot/handoff/internal/scheduling 0.307s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 0.006s [no tests to run]`、`ok github.com/Xsxdot/handoff/internal/agentd 6.428s`。 |
| 2026-09-10 | 全仓编译通过。 | `GOMODCACHE=/root/.handoff/tmp/8bdfc04f/gomodcache go build ./...` → exit 0；原始输出为依赖下载行，命令无错误输出。 |
| 2026-09-10 | 当前分支视图契约检查无失败；仅报告既有图覆盖/结构与 legacy 警告。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check --view cards-B233.10-charter` → exit 0；原始首项为 `{"fails": []}`，`warns` 含既有 `best-dangling`、`container-misplaced`、`legacy` 等警告。 |
| 2026-09-10 | 移除未被契约要求的空壳错误导出，保留两个方法签名空壳；重跑最终骨架测试与全仓编译。 | `GOMODCACHE=/root/.handoff/tmp/8bdfc04f/gomodcache go test ./internal/client ./internal/scheduling ./internal/ledgerstep ./internal/agentd -run 'TestDispatchFrozenIdentityJSON|TestOccupancy|TestAdmitCarrier|TestCardStep|TestContinue|TestDone|TestStop' -count=1` → exit 0；原始输出：`ok github.com/Xsxdot/handoff/internal/client 0.037s`、`ok github.com/Xsxdot/handoff/internal/scheduling 0.286s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 0.008s [no tests to run]`、`ok github.com/Xsxdot/handoff/internal/agentd 10.272s`。`GOMODCACHE=/root/.handoff/tmp/8bdfc04f/gomodcache go build ./...` → exit 0，原始输出为空。 |
| 2026-09-10 | 最终图锚与目标图 JSON 复核通过；分支视图检查无失败。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --view cards-B233.10-charter --doc docs/superpowers/specs/b233.10-contract.md` → exit 0，原始输出列出 `Service.Select`/`Service.AdmitFrozen` 为 `anchor: ok`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check --view cards-B233.10-charter` → exit 0，原始输出 `fails: []`；`jq empty codegraph/target.json codegraph/diffs/cards-B233.10-charter.json` → exit 0，无输出。 |
| 2026-09-10 | 契约文档、目标图、分支视图、Ticket 0 骨架与台账首次冻结提交。 | `git commit -m "contract(B233.10): freeze dispatch lifecycle seam"` → 原始输出：`[cards/B233.10-charter 8d1b12b0] contract(B233.10): freeze dispatch lifecycle seam`；`10 files changed, 611 insertions(+), 5 deletions(-)`。随后只 amend 一次收进本台账，不追写 amend 后 hash。 |

## 本轮 P2 契约补冻核对（2026-09-10）

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-10 | 本轮工作树初始干净，当前分支为 `cards/B233.10-charter-3`；上游 spec 头部状态为「已批准」。 | `git status --short --branch` → `## cards/B233.10-charter-3`；`sed -n '1,4p' docs/superpowers/specs/b233.10.md` → `状态：**已批准**（2026-09-10，用户原话「同意，批这条 spec。」）`。 |
| 2026-09-10 | 当前历史包含上一轮契约冻结 `e43991da` 与 Ticket 0/拆解提案提交；本轮工作分支 HEAD 为 `f81a4920`。 | `git log --oneline --decorate -8` → `f81a4920 (HEAD -> cards/B233.10-charter-3, origin/cards/B233.10-charter-2, cards/B233.10-charter-2) docs(B233.10): 出稿一次派发生命周期拆解提案`；`e43991da ... contract(B233.10): freeze dispatch lifecycle seam`。 |
| 2026-09-10 | 项目存在 `codegraph/best.json`、`codegraph/target.json` 与本卡既有视图；本轮 P2 不新增顶层域。 | `rg --files codegraph docs/superpowers/ledgers | sed -n '1,160p'` → 命中 `codegraph/best.json`、`codegraph/target.json`、`codegraph/diffs/cards-B233.10-charter.json` 及现有台账文件。 |
| 2026-09-10 | `RecoverOnStartup` 的现状签名和源码锚为 `internal/agentd/watchdog.go:485`；它先 `Store.ListTasks`，探活筛选只处理 running/waiting_answer/waiting_review。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'RecoverOnStartup'` → `signature: func RecoverOnStartup(st *store.Store, hub *Hub, probe func(taskID string) bool, sweep func(taskID string), log *slog.Logger) error`、`file: internal/agentd/watchdog.go`、`line: 485`；`nl -ba internal/agentd/watchdog.go | sed -n '485,519p'` → `st.ListTasks()`、`if t.State != ...Running ...WaitingReview`、逐任务恢复。 |
| 2026-09-10 | 任务状态与存储事实已核对：`TaskState.IsTerminal` 仅把 completed/failed 视为终态；`Store.ListTasks` 返回全量任务；`Store.CreateTask` 原样落盘任务快照。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'TaskState.IsTerminal'` → `file: internal/proto/proto.go`、`line: 34`、`summary: IsTerminal 报告该状态是否为终态（completed / failed）`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'Store.ListTasks'` → `signature: func (s *Store) ListTasks() ([]proto.Task, error)`、`file: internal/store/store.go`、`line: 433`；`nl -ba internal/store/store.go | sed -n '327,348p;433,457p'` → `CreateTask` INSERT `carrier,squad,state`，`ListTasks` 查询完整 taskColumns。 |
| 2026-09-10 | 现有 registry 端口可列 kind；`sched_running` 是 scheduling 内部既有 kind，载体/成员键由 `OccupancyKeys` 编码，`Service.Release` 当前是成对反向操作。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'Registry'` → `interface Registry` at `internal/schedclient/schedclient.go:31`，fields include `List(kind string) ([]Record, error)`；`nl -ba internal/scheduling/scheduling.go | sed -n '25,33p;570,598p;892,950p'` → `kindRunning = "sched_running"`、`Release` 调 `OccupancyKeys`/`stepRunning`；`nl -ba internal/agentd/server.go | sed -n '2671,2689p'` → `facadeAsRegistry.List` 转调账本门面。 |
| 2026-09-10 | `handleDispatch` 当前在请求侧准入后设置错误 defer 回滚，Manager 之后才 `CreateTask`；这支持 P2 冻结 `AdmitFrozen → CreateTask` 顺序和“错误仍走 defer”条目。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'Server.handleDispatch'` → `signature: func (s *Server) handleDispatch(w http.ResponseWriter, r *http.Request)`、`file: internal/agentd/server.go`、`line: 1364`；`nl -ba internal/agentd/server.go | sed -n '1371,1417p'` → `prepareReceiverBinding`、`defer`、`released = true`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'Manager.Dispatch'` → `file: internal/agentd/manager.go`、`line: 800`；`nl -ba internal/agentd/manager.go | sed -n '1077,1112p'` → `State: pending`、`m.st.CreateTask(task)`。 |
| 2026-09-10 | 图查询对 `Registry.List` 未给出直接符号，按纪律回落源码；`TaskState.IsTerminal` 有图锚，`RecoverOnStartup`/`Store.ListTasks` 的 baseline domain 为 `d_coordination_task`，不据此改最优结构。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'Registry.List'` → 原始报错：`Error: 符号 "Registry.List" 不在图中（图未覆盖或名字有误）；近似候选: [n_agentd_facadeAsRegistry_List(facadeAsRegistry.List)]。确认图未覆盖时回落 grep，并把该符号记入本节点产出物的「图覆盖债」小节`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_coordination_task` → 原始报错：`Error: 领域 "d_coordination_task" 不在最优树词表中；context 已按最优树词表取声明，最优树领域候选: d_cli, d_collab, d_execution, d_execution_adapters, d_execution_contract, d_execution_host, d_gateway, d_keystone, d_ledger, d_maintenance, d_orchestration, d_policy, d_protocol, d_scheduling, d_sessions, d_transport, d_transport_channel, d_transport_tunnel, d_web, d_web_admin, d_web_cards, d_web_command, d_web_contract, d_web_shell, d_web_workbench, d_workspace。"d_coordination_task" 是**现状视图**领域 id...`；不把失败查询写成调用方结论。 |
| 2026-09-10 | P2 冻结边界已写入契约：新增 #77–#90，拆开启动时机、carrier/member 精确匹配、无主键释放、有主键保护、崩溃窗口、CreateTask 顺序、defer 保持、无新 kind 与无 reservation DTO；同时更新目标图注记，未新增 Go 符号或分支视图 diff。 | `git diff --check` → exit 0，无输出；文件指认：`docs/superpowers/specs/b233.10-contract.md` §3.4、§4.5、§7、§9、§10、§11 C-7/C-8；`codegraph/target.json` 的 `d_scheduling → d_ledger` `legacyBudgetNote` 增加 `B233.10 P2`。 |
| 2026-09-10 | 文档锚点首次复核失败，原因在输出中明确为两个历史相对路径锚的 `file_missing`；已改为完整仓内路径。 | 首次 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --view cards-B233.10-charter --doc docs/superpowers/specs/b233.10-contract.md` 原始报错：`Error: 文档锚点检查失败: scheddrain.go#Server.releaseSchedulingBinding (file_missing)`；随后 `internal/agentd/scheddrain.go#Server.releaseSchedulingBinding` 与 `internal/agentd/cardstep.go#Server.startCardStep` 已补全。 |
| 2026-09-10 | 当前契约全部文档锚点复核通过，包含本轮新增的 RecoverOnStartup、ListTasks、IsTerminal、Registry 锚与既有 P2 相关入口。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --view cards-B233.10-charter --doc docs/superpowers/specs/b233.10-contract.md` → exit 0；原始输出 `anchors` 全部解析，末项为 `internal/agentd/scheddrain.go#Server.releaseSchedulingBinding ... anchor: moved`，无 `Error`。 |
| 2026-09-10 | 本轮法定 Ticket 0/仓内编译通过；本轮无生产代码变更，但以新鲜命令确认现有骨架仍可编译。 | `go build ./...` → exit 0；原始输出为空。 |
| 2026-09-10 | 当前契约图闸门无失败；输出仅包含既有结构/覆盖/legacy 警告。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check --view cards-B233.10-charter` → exit 0；原始首项为 `{"fails": []}`，`warns` 含既有 `anchor-off-domain`、`best-dangling`、`container-misplaced`、`container-unplaced`、`legacy`、`oversized-package`、`prefix-family`。 |
| 2026-09-10 | 目标图与既有本分支视图均为有效 JSON。 | `jq empty codegraph/target.json codegraph/diffs/cards-B233.10-charter.json` → exit 0，无输出。 |
| 2026-09-10 | 契约复核补齐 `Store.CreateTask` 精确签名出处，并将无主释放限定为计数值大于零，避免把零值记录误当作泄漏。 | 文件指认：`docs/superpowers/specs/b233.10-contract.md` §1 与 §3.4、冻结条目 #82；现状出处 `internal/store/store.go#Store.CreateTask`（当前 `:334`）。 |

### 失败命令完整原文补录

以下为上表失败查询的完整命令输出；上表中的 `...` 仅为表格内缩略，完整原文以此处为准。

`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'Registry.List'`：

```text
Error: 符号 "Registry.List" 不在图中（图未覆盖或名字有误）；近似候选: [n_agentd_facadeAsRegistry_List(facadeAsRegistry.List)]。确认图未覆盖时回落 grep，并把该符号记入本节点产出物的「图覆盖债」小节
Usage:
  codegraph sym <符号名或节点 id> [flags]

Flags:
  -h, --help   help for help

Global Flags:
      --base string   棘轮基准 revision（缺省取默认分支 merge-base）
      --repo string   目标仓库根目录 (default ".")
      --stale        附带保鲜检测结果
      --view string   叠加的视图名（codegraph/diffs/<名>.json）

exit status 1
```

`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym 'Task.IsTerminal'`：

```text
Error: 符号 "Task.IsTerminal" 不在图中（图未覆盖或名字有误）；近似候选: []。确认图未覆盖时回落 grep，并把该符号记入本节点产出物的「图覆盖债」小节
Usage:
  codegraph sym <符号名或节点 id> [flags]

Flags:
  -h, --help   help for help

Global Flags:
      --base string   棘轮基准 revision（缺省取默认分支 merge-base）
      --repo string   目标仓库根目录 (default ".")
      --stale        附带保鲜检测结果
      --view string   叠加的视图名（codegraph/diffs/<名>.json）

exit status 1
```

`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_coordination_task`：

```text
2026/09/10 12:15:24 INFO graph context started domain=d_coordination_task
2026/09/10 12:15:24 ERROR context domain not found domain=d_coordination_task view=baseline vocabulary=best candidateCount=26
2026/09/10 12:15:24 ERROR graph context assembly failed domain=d_coordination_task stage=assemble error="领域 \"d_coordination_task\" 不在最优树词表中；context 已按最优树词表取声明，最优树领域候选: d_cli, d_collab, d_execution, d_execution_adapters, d_execution_contract, d_execution_host, d_gateway, d_keystone, d_ledger, d_maintenance, d_orchestration, d_policy, d_protocol, d_scheduling, d_sessions, d_transport, d_transport_channel, d_transport_tunnel, d_web, d_web_admin, d_web_cards, d_web_command, d_web_contract, d_web_shell, d_web_workbench, d_workspace。\"d_coordination_task\" 是**现状视图**领域 id，它的容器分布在: d_orchestration(12 容器)、d_maintenance(5 容器)、(best 未归位)(3 容器)、d_policy(3 容器)、d_workspace(2 容器)、d_gateway(1 容器)、d_web_shell(1 容器)——请按需改用其中一个最优树 id（分布非单值，不代为改写）"
Error: 领域 "d_coordination_task" 不在最优树词表中；context 已按最优树词表取声明，最优树领域候选: d_cli, d_collab, d_execution, d_execution_adapters, d_execution_contract, d_execution_host, d_gateway, d_keystone, d_ledger, d_maintenance, d_orchestration, d_policy, d_protocol, d_scheduling, d_sessions, d_transport, d_transport_channel, d_transport_tunnel, d_web, d_web_admin, d_web_cards, d_web_command, d_web_contract, d_web_shell, d_web_workbench, d_workspace。"d_coordination_task" 是**现状视图**领域 id，它的容器分布在: d_orchestration(12 容器)、d_maintenance(5 容器)、(best 未归位)(3 容器)、d_policy(3 容器)、d_workspace(2 容器)、d_gateway(1 容器)、d_web_shell(1 容器)——请按需改用其中一个最优树 id（分布非单值，不代为改写）
Usage:
  codegraph context <领域> [flags]

Flags:
  --collapse-util     收桩跨领域高扇入节点 (default true)
  --fold-external     按外部领域折叠节点 (default true)
  --full              恢复旧的全量节点字段
  -h, --help          help for help
      --max-tokens int   近似 token 预算（0 = 不限） (default 30000)
      --source-span int  源码窗口行数（上限 200） (default 40)
      --with-source     附带源码窗口 (default true)

Global Flags:
      --base string   棘轮基准 revision（缺省取默认分支 merge-base）
      --repo string   目标仓库根目录 (default ".")
      --stale        附带保鲜检测结果
      --view string   叠加的视图名（codegraph/diffs/<名>.json）

exit status 1
```

### 收口复核（本轮）

| 2026-09-10 | 契约文档完成最终符号锚复核；全部锚点解析成功，包含本轮 P2 新增的 `RecoverOnStartup`、`Store.ListTasks`、`Store.CreateTask`、`TaskState.IsTerminal`、`Registry` 与 `facadeAsRegistry.List`。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --view cards-B233.10-charter --doc docs/superpowers/specs/b233.10-contract.md` → exit 0；原始输出为 `anchors` JSON，无 `Error`。 |
| 2026-09-10 | 最终图闸门无失败；已有架构/覆盖/legacy 警告未因本轮契约补冻新增失败项。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check --view cards-B233.10-charter` → exit 0；原始输出首项为 `{"fails": []}`，其余为既有 `warns` 与 `legacyHits/bestCoverage`。 |
| 2026-09-10 | 目标图与本卡既有分支视图均保持有效 JSON；工作树文本差异无空白错误。 | `jq empty codegraph/target.json codegraph/diffs/cards-B233.10-charter.json` → exit 0，无输出；`git diff --check` → exit 0，无输出。 |
| 2026-09-10 | 本轮最终仓内编译通过；既有冻结身份 wire 金样本测试通过。 | `go build ./...` → exit 0，无输出；`go test ./internal/client -run TestDispatchFrozenIdentityJSONGolden -count=1` → exit 0；原始输出：`ok github.com/Xsxdot/handoff/internal/client 0.005s`。 |
| 2026-09-10 | 契约冻结提交已创建；随后只将本条提交记录 amend 收入同批提交，不回写 amend 后 hash。 | `git add codegraph/target.json docs/superpowers/specs/b233.10-contract.md docs/superpowers/ledgers/2026-09-10-b233.10-contract-ledger.md`；`git commit -m "contract(B233.10): freeze P2 startup reconciliation"` → 原始输出：`[cards/B233.10-charter-3 c7187e56] contract(B233.10): freeze P2 startup reconciliation`、`3 files changed, 159 insertions(+), 16 deletions(-)`。 |
