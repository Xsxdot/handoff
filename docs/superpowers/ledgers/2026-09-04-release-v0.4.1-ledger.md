# v0.4.1 发布台账

本台账记录 v0.4.1 发布准备、tag 先行导致的 release 空转、实际验收命令与结果。

边界：只记录本次版本发布与 tag 重打，不纳入工作区其他改动。

## 2026-09-04

- 发布内容：`v0.4.0..HEAD` 共三个提交——`17a6748ff`（docs：B318 spec 与 changelog 收尾）、`80e53e1`（fix(web)：B320 `/cards` 切断 main 固有宽）、`b0b96ae`（fix(agentd,hostapi)：展开载体 HOME 前导 `~`，回归锁各一）。
- 事故：先提交后直接 `git push origin main` + `git tag -a v0.4.1 && git push origin v0.4.1`，此时 `CHANGELOG.md` 尚无 `## [v0.4.1]` 小节。`release.yml` 按 `push: tags: v*` 触发 run `33827852927`，抽不到小节只能回落 `--generate-notes` 并打警告。
- 止损（Release 尚未建出，`gh release view v0.4.1` 报 `release not found`）：`gh run cancel 33827852927` 已提交取消；`git push origin :refs/tags/v0.4.1` 删远端 tag，`git tag -d v0.4.1` 删本地 tag。
- 发布前验证（prepare 提交前，工作区干净）：`go test ./cmd -run TestRepoContractGate -count=1` 退出 0（`~` 展开新增两函数未超调用图预算，`codegraph/baseline.json` 无需改动）；`go build ./...` 退出 0；此前两回归测试 `TestWithCarrierHomeExpandsTilde`、`TestBuildEnvExpandsTildeHomeDir` 已分别通过。
- 冻结决定：将 `[Unreleased]` 下 B320 条目保留，补 `~` 展开修复条目，一并冻结为 `## [v0.4.1] - 2026-09-04`；`[Unreleased]` 下留 `_（下一版的改动记在这里。）_` 占位。B319 spec 系演示卡文档，不进用户可见改动。
- 待完成：将发布准备提交推送到 `origin/main`，在新 HEAD 上重打并推送 `v0.4.1` 标签，确认旧 release run 已取消且无 Release 建出，等待并核对新的 GitHub Actions release run 与 Release 资产（说明应从 CHANGELOG 小节抽取，不应出现回落警告）。
