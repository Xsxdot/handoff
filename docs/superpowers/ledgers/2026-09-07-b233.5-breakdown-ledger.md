# B233.5 breakdown 台账

日期：2026-09-07。节点：charter breakdown。工作分支：`cards/B233.5-charter-2`。开工 HEAD：`e87af1a2`（契约冻结提交）。

## 过程

1. 核对上游：spec `docs/superpowers/specs/b233.5.md` 头部状态行 = **已批准**（2026-09-06）。契约 `docs/superpowers/specs/b233.5-contract.md` 头部写「冻结状态：随本提交冻结」，未钉日期+提交号——与 B233.2/3/4 拆解期补钉惯例一致，本节点核对后回写契约修订记录，不改冻结正文。定级 L3 轻档。有效基线 `cards/B233.1-charter-7`。禁止合 main、禁止 absorb、禁止写实现、禁止扇出子卡。
2. 执行形态：handoff 派发形态 2（executor 出稿，协调者拍板）。岔口一律待拍板、禁止自批。
3. 最优图顶层领域（`codegraph/best.json` `parent` 为空）：`d_cli` logic、`d_collab` logic、`d_execution` boundary、`d_gateway` boundary、`d_keystone` logic、`d_ledger` logic、`d_maintenance` boundary、`d_orchestration` logic、`d_policy` logic、`d_protocol` logic、`d_scheduling` logic、`d_sessions` boundary、`d_transport` boundary、`d_web` logic、`d_workspace` boundary。`codegraph domains` 默认基线出现 `d_coordination`，与 best 不一致——清单以 best 为准。
4. `codegraph --repo . context d_scheduling`：包 `internal/scheduling` + `internal/schedclient`。`sym Admit --view cards-B233.5-charter` 命中 `n_scheduling_Service_Admit`（anchor=moved，行 367）。`sym bindingFor` **不在图中**。`who-calls n_scheduling_Service_Admit` 基线无上游边，warning：5 个未扫描入口。调用方 grep：`internal/agentd/scheddispatch.go#Server.admitSquadStep`。
5. `codegraph --repo . --view cards-B233.5-charter check`：`fails []`。无视图 `fails`：`d_execution→d_orchestration` dead-contract/dead-interface（ApprovalClient，B233.1 视图债）。本卡不修。
6. 架构法第三条本轮实数：`internal/agentd` 非测试 **90** 文件、**28534** 行（红线 20000）；`work*`=7、`preview*`=10、`sched*`=3。`internal/scheduling` 非测试 5 文件 / 1363 行（`scheduling.go` 883）。`cmd` 非测试 56 文件（正当 CLI 扁平）。
7. Ticket 0 工作树 API 事实：`receiver.go`/`occupancy.go` 纯函数与金样本已在。生产未接线：`Admit` 仍要求小队；`bindingFor` 仍请求覆盖 Target/Executor；`Release` 仍手拼 `kindSquad+"/"+squad+"/"+carrier`；`PutCarrier`/`PutSquad` 未调 `CrossKindConflict`；`Manager.Dispatch` 仍 `resolveExecutor`（空则 `cfg.Executor.Default`）；`handleDispatch` 只透传 `Receiver`；`startCardStep` 准入后**再读活载体**取 HOME；`releaseSchedulingBinding` 在 `Squad==""` 直接 return；`CreateTask` INSERT **无** `carrier` 列；`allowedTaskFields` 无 carrier；CLI `cmd/dispatch.go` 无 `--receiver`，并把全局 `targetName` 写入 `DispatchOpts.Target`。
8. 编制 HTTP 面既有：`GET/PUT /api/squads*`、`GET /api/queue`、detect/run-command。无默认载体端点。冻结 63 禁止本卡新增 HTTP 路径。
9. 不新立顶层域；不新开 `d_orchestration→d_scheduling` / `d_cli→d_scheduling`。`k_store_*` 归 `d_orchestration`。
10. 法定产出写到 `docs/superpowers/specs/b233.5-breakdown.md`。契约补钉头部冻结状态并追加 §13 C-0…C-12（冻结条目 1–75 一字未改）。
11. `codegraph --repo . resolve --doc docs/superpowers/specs/b233.5-breakdown.md --view cards-B233.5-charter`：全部锚 `ok` 或 `moved`，无坏锚。`bindingFor` / `withCarrierHome` 为 moved 且 `bindingFor` 无 nodeId（图覆盖债，已写入拆解 §6）。契约文档 resolve 同样无坏锚。
12. 判断：L3 轻档不扇出（P1 待拍板，提案 a）。11 个岔口集中稿首。内部 ticket T1–T7。真机 14 条。未写实现、未建卡、未派发、未合 main、未 absorb。

## 提交

```
git commit -m "docs(B233.5): 拆解载体与小队统一派发提案"
[cards/B233.5-charter-2 6a67f618] docs(B233.5): 拆解载体与小队统一派发提案
 3 files changed, 495 insertions(+), 1 deletion(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.5-breakdown-ledger.md
 create mode 100644 docs/superpowers/specs/b233.5-breakdown.md
```

随后 amend 一次把本段收进同批提交。不回填 amend 后的 HEAD。
