# B233.3 contract 台账

日期：2026-09-07。节点：charter contract。工作分支：`cards/B233.3-charter`。开工 HEAD：`124121e4`（与 `origin/cards/B233.1-charter-7` 同提交，有效基线）。

## 过程

1. 核对上游 spec `docs/superpowers/specs/b233.3.md` 头部状态行 = **已批准**（2026-09-06，用户原话「按这个推进吧」），未回写。
2. 定级 L3 轻档：无直通竖切，运行时最薄路径由 plan 承接。
3. 查图：`codegraph summary`（4499 节点 / 5507 边 / 23 领域）；`context d_transport`（包：client / proxycfg / relay / targetclient）；`sym isDeliverable` / `HTTPClient` / `forwardWorktreeIfRequested`。`who-calls n_client_Client_HTTPClient` 空（基线 unscannedEntries=5，记图覆盖债）。`sym ErrUnreachable` 不在图中。
4. 基线代码事实已对照：`targetclient.newWithDialer` / `New` / `Pool.For` / `NewLocal` / `Warm`；`client.New` / `NewRelay` / `do` / `Dispatch` / `Reply` / `Continue` / `Stop` / `WaitEvent` / `FollowEvents` / `isDeliverable` / `HTTPClient`；`agentd.forwardWorktreeIfRequested` / `attachCardBaseBranches` / `forwardTo`；`relay.DerivePSK` / `Encode` / `relayYamuxConfig` / `CheckTokenEntropy`。
5. 依赖库既成行为已读源码：`net/http.Request.isReplayable`（POST 默认不可重放，除非 `Idempotency-Key` / `X-Idempotency-Key`）；`yamux.DefaultConfig` keepalive 30s；`coder/websocket.Dial` 未给 HTTPClient 时退回 `http.DefaultClient`；`golang.org/x/crypto/hkdf.New` = Extract+Expand。
6. 架构法第一条：本卡不把能力 client 升格为新顶层子系统；物理拆 `internal/client` 包是 OOS。
7. Ticket 0 空壳：`internal/client/execution.go`（Transport / ExecutionClient / ErrTunnelDisconnected）、`delivery.go`（WaitDeliveryPolicy）；`isDeliverable` 改为别名。金样本：PSK、CONNECT/CONNECT_OK 帧、Dispatch 禁止标准幂等头、HTTPClient 生产调用白名单、yamux 保活、选路工厂接口断言。
8. 契约写到法定路径 `docs/superpowers/specs/b233.3-contract.md`。视图 `codegraph/diffs/cards-B233.3-charter.json`。target.json 只给既有 `d_cli→d_transport` / `d_gateway→d_transport` / `d_orchestration→d_transport` 补 B233.3 注记；不在 d_cli 声明 `interfaces: ExecutionClient`（会 dead-interface）；不新开方向、不改预算。

## 命令读数

- `gofmt -w` 触及的 Go 文件：exit 0。`gofmt -l` 再扫为空。
- `go test ./internal/client/ ./internal/targetclient/ ./internal/relay/ -count=1 -timeout 180s`：三包 `ok`，exit 0。
- `go build -o /dev/null .`：exit 0。
- `codegraph --repo . resolve --doc docs/superpowers/specs/b233.3-contract.md --view cards-B233.3-charter`：25 锚，无坏锚。新符号 `Transport` / `ExecutionClient` / `WaitDeliveryPolicy` 均为 `anchor=ok`。
- `codegraph --repo . check --view cards-B233.3-charter`：exit 1，`fails` 2 条，与 HEAD 相同（`d_execution→d_orchestration` ApprovalClient dead-contract / dead-interface）。本卡未新增 fail。
- `codegraph --repo . validate --view cards-B233.3-charter`：`issues: null`。

## 判断

- 不升格新子系统：轻档不换并行开发。
- 跨机审批回程走 Reply，不新开 ApprovalClient HTTP 面。
- Dispatch 禁止标准幂等头，避免 net/http 把 POST 当可重放。
- 三重闸门六条写入契约 §7。
- 欠账见契约 §8：无「已实现但零测试」。

## 提交

```
git commit -m "contract(B233.3): 冻结能力 client 复用直连与 relay 传输契约"
[cards/B233.3-charter fd43fb68] contract(B233.3): 冻结能力 client 复用直连与 relay 传输契约
 14 files changed, 809 insertions(+), 31 deletions(-)
 create mode 100644 codegraph/diffs/cards-B233.3-charter.json
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.3-contract-ledger.md
 create mode 100644 docs/superpowers/specs/b233.3-contract.md
 create mode 100644 internal/client/delivery.go
 create mode 100644 internal/client/delivery_internal_test.go
 create mode 100644 internal/client/execution.go
 create mode 100644 internal/client/execution_test.go
 create mode 100644 internal/client/wsdial_export_test.go
 create mode 100644 internal/relay/e2e_psk_golden_test.go
 create mode 100644 internal/relay/frame_golden_test.go
 create mode 100644 internal/relay/mux_contract_test.go
```

随后 amend 一次把本段收进同批提交。不回填 amend 后 HEAD。
