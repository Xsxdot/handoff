# B358.2 plan 节点台账（2026-09-12）

计划产出物：`docs/superpowers/plans/b358.2-plan.md`（本台账随同批提交）。
角色：L3 重档 plan 节点子系统执行者；只写计划与台账，不写实现、不碰卡、不做 git 操作。

## 一、输入与基线读数

- 工作树：分支 `cards/B233.1-charter-7`，HEAD `0be4a2c6`（B358.1 已合入：Send/VerifyWriter 成员执法制已在树）。
- 读过的输入（逐字核对）：
  - `docs/superpowers/specs/b358-breakdown.md` §3.3（S2 四段式）、§2.4 澄清 3 + 发现 A、§5 真机清单 #5、§6 图覆盖债。
  - `docs/superpowers/specs/b358-contract.md` §3.2（DTO/词表）、§3.9（R2/R3 载体）、§4.2 条 14–18、§4.3 条 31、§4.7 条 51–54、§4.8 条 55–56、§7.5（补签轮落码）、§8.5/§8.10（欠账）、§9 附区（driver_takeover 归属判据）。
  - `docs/superpowers/specs/b358.md` §4.3 末条、§4.4。
- 现状代码读数（本轮工作树，行号为当日读数）：
  - `internal/collab/sessions.go#sessionTimeline`（:307）：driver_takeover 分支用**全量卡表** `byCard`（`ListAllCards("")`）判归属——发现 A 主 bug 在树；`session_created` 分支查载荷键 `session` 而 **CreateSession 落的载荷键是 `id`**（`internal/ledger/sessions.go:64-66`）→ 过滤恒空、他场会话的 created 行串场；`session_card_joined/card_left` 分支**无归属判定**（载荷 `session` 键不比对）→ 同族串场。
  - `sessionTimeline` 唯一调用方 = `SessionDetail`（sessions.go:89）；签名可安全收紧。
  - `sessionNodes`（:284）：task_mirrored envelope `{node,attempt,task_type,payload}`（`internal/ledger/mirror.go:74`），无 round/target 字段；解码失败静默 continue、无日志（缺陷族 2 欠账）。
  - `memberStatus`（:256）：`lc.DriverLease` + `nowFn()`（`internal/collab/service.go:31` 注入点）；无租约 → last_active+零值；未过期 → working+到期时刻；过期 → last_active+到期时刻。生产无 listening 载体（`RenewDriverLease` 生产零调用方，breakdown 澄清 3）。
  - `needsHumanByCard`（:346）：needs_human/needs_cleared 翻转 Summary.NeedsHuman 已在；timeline 无 needs_human 分支。
  - `internal/ledger/binding.go#BindSeat`（:58-61）：同事务落 `EvDriverSeatBound`、payload 恰 `{to}`（R2 已落码，`binding_test.go:149` 有可变红测试）；`RebindSeat` 落 `EvDriverTakeover{from,to}`。
  - `internal/ledger/types.go`：`StatusDone="已完成"`、`StatusClosed="终止"`（:15-20）；`EvStatusMoved` 载体三处生产（`move.go:67` `{from,to}`、`cards.go:709` 终止 `{from,to,reason}`、`:730` 复活）。
  - `internal/ledger/move.go#MoveCard(id,to,expect,actor)`：`to` 必须在工作流 States（fixture workflow bug 含 待办/进行中/已完成）；`expect=""` 免 CAS。
  - `internal/proto/sessions.go`：timeline kind 词表 8 值含 `SessionEventSeatBound`/`SessionEventCardClosed`/`SessionEventNeedsHuman`（:111-120）；无 needs_cleared kind（决定：needs_cleared 不进 timeline）。
  - `internal/proto/sessions_fixture_test.go`：金样本 5 支（reply_to / Summary / Detail / 状态词表 / timeline kind 词表 `TestSessionTimelineKindVocabulary`）。
  - collab 非测试零 import ledger（契约条 36）→ 生产代码里 status_moved/已完成/终止 只能字面量+注释指认唯一定义点；测试文件已 import ledger，可作漂移哨兵。

## 二、判据基线复核（本节点亲跑）

- `go build ./...` → 退出 0。
- `go test ./internal/collab -run 'TestSession' -count=1` → ok（0.614s）。
- `go test ./internal/proto -run 'TestSession' -count=1` → ok（0.462s）。
- **breakdown §3.3 ③ 字面判据 `-run TestSessionsFixture` 实测零匹配**（proto 包无该名字测试；-run 是正则，无匹配=空跑假绿）→ 调整为 `-run 'TestSession'`（命中金样本 4 支：Summary/Detail/成员状态词表/timeline kind 词表；reply_to 金样本名 `TestRoomMessageReplyToGolden` 不被该正则命中、且与投影键集无关），记入计划覆盖核对。
- 工作树 status 读数（入场既有脏文件，不触碰）：`M web/src/app/task/Composer.test.tsx`、`?? .commandcode/`、`?? docs/superpowers/plans/b353-probe-follow-harness.md`、`?? web/design-qa-sidebar-after.png`、`?? web/design-qa.md`。
- `WorkflowDef` 兼有 `States` 与 `Nodes` 两字段；`PutWorkflow` 落库前 `def.withStatesFromNodes()`（`internal/ledger/workflows.go:204`）——夹具只给 Nodes 时 States 已派生，Task 2 用 `st.MoveCard(cardID, ledger.StatusDone/StatusDoing, …)` 生产 status_moved 的判据成立（否则 move.go:44 的 States 校验会拒）。

## 三、codegraph 探针（图覆盖债）

本轮逐符号 `codegraph sym`，全部「不在图中」：`sessionTimeline`、`sessionMembers`、`memberStatus`、`sessionNodes`、`needsHumanByCard`、`collab.Service.SessionDetail`、`Service.SessionDetail`。与 breakdown §6 记录一致（Ticket 0 新符号只在 `codegraph/diffs/cards-B358-charter.json` 视图 diff）。计划一律用 `file#Symbol` 源码锚；实现节点 recon 须把本卡新增/修改符号补进视图 diff。

## 四、做出的判断（plan 决定，非岔口）

1. 发现 A 修复按族扩全：除字面点名的 driver_takeover 外，`session_created`（载荷键错位）、`session_card_joined/left`（无判定）同属「timeline 只装本会话的结构事实」的归属偏差，一并修并各自带反例——根因同一函数同一判据，拆开修反而留半拉子。
2. `needs_cleared` 不进 timeline：proto 词表 8 值无它（补签轮逐值冻结），label 翻转由 `needsHumanByCard` 承担，测试锁「清除后无新增 timeline 行」。
3. `SessionNode.Round/Target/Title` 留白定稿：envelope 无对应字段，一律零值/空串，不为填空造新账本读（breakdown 授权 plan 定填法）。
4. `memberStatus.LastActive` 钉既有语义：有租约行（含过期）= 租约到期时刻（可证实的「活到几点」），无租约 = 零值；断言用 `lc.DriverLease` 回读等值。
5. 成员状态扫取三值集 `{working,last_active,empty}`（严于四值词表——listening 生产零载体，澄清 3；`online` 等词表外串自然被拒）。
6. B287 双会话反例（审查移交）预期绿：`consumeRoomMentions` 已按 `room.SameRoom` 过滤（service.go:185）；变异验证需临时触碰 service.go（越界文件）→ 以「临时探针、当场还原、零最终 diff」声明方式落步骤；若实测红即升级岔口（修复面越出 S2 有界文件集）。

## 五、放弃的尝试

- 无。（曾考虑让 plan 节点先写一支临时测试证红发现 A——越本节点红线「不写实现/测试代码」，改为计划内「先红后绿」步骤 + 本台账的代码级推理记录。）

## 六、占位符与内部锁自审

- 占位符扫描：计划内测试与实现代码全部成块给出；无 TBD/「同 Task N」/纯描述性骨架。唯一例外声明：无（未使用「复用既有 harness 省略代码」出口）。
- 内部锁声明：无——全部新测试入口为导出门面方法（`SessionDetail`/`ListSessions`/`Send`/`Mentions`），落在 spec §6 接缝 #1/#5 上；未直调任何未导出投影函数。变异验证的临时触碰（Task 4 变异①②、Task 5 break 变异、Task 6 SameRoom 摘除）是当场还原的探针，不是测试，不构成内部锁。

## 七、产出

- `docs/superpowers/plans/b358.2-plan.md`（本台账同批）。
- 待拍板岔口见计划 §12（三条，均不阻塞派发：判据字面修正 ×1、同族扩修 ×1、条件岔口 ×1）。
