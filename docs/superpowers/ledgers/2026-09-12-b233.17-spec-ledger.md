# B233.17 spec 台账

- 2026-09-12 用户：「继续推进下一张卡。」卡 `B233.17` `move spec`。阻塞边 B233.12–.16 均已完成。
- 2026-09-12 用户：「都做。」三句都进本卡：处理请求的代码不再免检；后厨加编译期门锁；有电话的房间直闯配额降到 0。
- 功能线 `cards/B233.1-charter-7` @ `686d87abf`。`codegraph/target.json` assembly = `main.go` / `internal/agentd/server.go` / `internal/agentd/codegraph.go` / `cmd/agentd.go`。
- `server.go` 3010 行；`handle*` 21 个（`handleStatus` `:889` … `handleEvents` `:2333`）；`Handler()` 路由表 `:637`；`NewServer` `:249`；`SetupAutomation` `:2673` 仍 `collab.New` / `hostapi.NewWithCredentialPathFor` / `keystone.New`。
- `codegraph.go` 整文件两个 handler：`handleProjectCodegraph` `:27`、`handleProjectCodegraphSource` `:123`，无插线。
- 嵌套 `internal/<sub>/internal/`：`find internal -type d -name internal` 只有顶层 `internal/`，四包均未封。
- 生产构造：`orchestration.NewManager` `internal/orchestration/manager.go:291`；`scheduling.New` `internal/scheduling/scheduling.go:173`；`workspace.NewCapability` `internal/workspace/capability_impl.go:17`；`approval.NewClient` `internal/approval/client.go:72`。
- 目标图本卡相关边（功能线读数）：`d_gateway→d_orchestration` budget 321 interfaces=`OrchestrationClient` entries 空；`d_gateway→d_scheduling` budget 0 interfaces=`SchedulingClient` entries=`scheduling.Service`+包级函数；`d_gateway→d_workspace` 1；`d_cli→d_workspace` 9；`d_orchestration→d_workspace` 19；`d_workspace→d_orchestration` 2；`d_workspace→d_protocol` 3；`d_policy→d_transport` 2；`d_maintenance→d_transport` 3。
- B233.15 finish 留下 check 5 红（预算未抬）：cli→workspace 11/9、gateway→workspace 17/1、orchestration→workspace 49/19、workspace→orchestration 3/2、workspace→protocol 23/3。正主本卡。
- `codegraph context d_gateway` 命中；`codegraph sym OrchestrationClient` 空（图覆盖债，以源码 `internal/agentd/orchestration_client.go` 为准）。
- 定级 L3 轻档：跨子系统 + 动目标图；三句一轮可收口，不扇出。
- 弃选：server.go 整文件退出名单且把 SetupAutomation 的 New 全上移 cmd；全仓按 best 重排；全仓预算清零；抬预算把 15 的超限抹绿。

- 提交（历史读数）：`git commit` → `1f4022d59 docs(B233.17): spec 组装点收窄、编译期封界、预算棘轮`；随后 amend 一次收进同批，不回写新 hash。
