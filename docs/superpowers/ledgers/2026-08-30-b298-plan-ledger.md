# B298 plan 节点台账

日期：2026-08-30
节点：plan（L3 轻档）
工作树：`/Users/sycm/.handoff/worktrees/b298-charter`
分支：`cards/B298-charter` HEAD `c2524c8c2d1dc5614a1b8aa38c005e13ba16d0e0`
上游：spec `docs/superpowers/specs/b298.md`；契约 `docs/superpowers/specs/b298-contract.md`（冻结 `26e2ab7f`）；拆解 `docs/superpowers/specs/b298-breakdown.md`

## 事实流水

- 2026-08-30：命令 `git status --short --branch && git rev-parse HEAD && git log -5 --oneline`。原始输出：`## cards/B298-charter...origin/cards/B298-charter`；HEAD `c2524c8c2d1dc5614a1b8aa38c005e13ba16d0e0`；`c2524c8c breakdown(B298): define gc implementation tickets`；`26e2ab7f contract(B298): freeze gc wire and skeleton`；`b0a1ac44 docs(B298): 已批准 L3 轻档 spec（终态缓存清理 / handoff gc）`。判断：保持当前分支，不 rebase 到 main。
- 2026-08-30：命令 `command -v codegraph || echo codegraph_not_on_PATH`。原始输出：`codegraph_not_on_PATH`。判断：改用 `go run github.com/Xsxdot/charter/graph/cmd/codegraph`（go.mod 钉 `github.com/Xsxdot/charter/graph v0.10.0`）。
- 2026-08-30：命令 `go env GOROOT GOPATH GOMODCACHE GOVERSION`。原始输出：`/usr/local/go`、`/Users/sycm/go`、`/Users/sycm/go/pkg/mod`、`go1.26.1`。与契约 §4 依赖库版本一致。
- 2026-08-30：实读 `docs/superpowers/specs/b298.md` / `b298-contract.md` / `b298-breakdown.md`。拍板已在拆解稿：岔口 1 `GCResp.scanned` = 本轮读过的任务表终态行数（含无叶子/无残树）；岔口 2 全包红为沙箱/flake 非本卡；岔口 3 C-2 工作树失败不抑制缓存删除。C-4：§6 附区吸收标注落 plan 文档头。
- 2026-08-30：实读 Ticket 0：`internal/agentd/gc.go#Manager.GC` 返回 `ErrGCUnwired`；`handleGC` 成功路径在 `err==nil` 后直接落到函数结束，不 `writeJSON`；`cmd/gc.go#runGC` 成功路径恒 `return nil`（断言 40 未实现）；`jsonDecode` 是 `gc.go` 本地重复解码器，`handleReclaim` 已用 `json.NewDecoder`。
- 2026-08-30：插入点实读 `internal/agentd/manager.go`：`Done` 工作树块 1459-1474 后 `return nil`；`Stop` 工作树块 1565-1580 后 `return worktreeRemoved, nil`；`compensateWorkspace` 1090 起，`RemoveManagedWorktree` 失败 1100-1105 `return`，非 managed 缺 PrevRef / checkout 失败同样提前 return。C-2：缓存删除必须用 defer 挂在空 WorkDir 守卫之后，不能写在成功路径末尾。
- 2026-08-30：`Dispatch` defer 907 行在 `err != nil && !executorStarted` 时调 `compensateWorkspace`。CreateTask 在 968，Start 在 994；Start 失败会 `transitBestEffort(failed)` 且走补偿。
- 2026-08-30：命令 `go test ./internal/proto -run 'TestGCGoldenJSON' -count=1 && go test ./internal/client -run 'TestGCPostDouble404IsUnsupported' -count=1 && go test ./cmd -run 'TestRunGCDegradesOnOldAgentd|TestRenderGCDistinguishesUnknownBytes' -count=1 && go test ./internal/agentd -run 'TestHandleGCTicket0|TestDoneRemovesManagedWorktree|TestCompensateKeepsBranchWhenWorktreeRemoveFails|TestDoneWorktreeRemoveFailureDoesNotBlockArchive|TestReclaimRemovesCleanWorktree|TestReclaimRefusesDirtyWithoutForce|TestReclaimListShowsResidueOnly' -count=1 && go test ./internal/executor -run 'TestTaskTmpDirGoldenVectors' -count=1`。原始输出：
  ```
  ok  github.com/Xsxdot/handoff/internal/proto  0.685s
  ok  github.com/Xsxdot/handoff/internal/client  0.519s
  ok  github.com/Xsxdot/handoff/cmd  0.572s
  ok  github.com/Xsxdot/handoff/internal/agentd  4.897s
  ok  github.com/Xsxdot/handoff/internal/executor  0.414s
  ```
  退出 0。判断：Ticket 0 与既有收口/reclaim/harness 基线绿；`TestHandleGCTicket0` 是 503 假绿，T2 必须退役。
- 2026-08-30：命令 `go test ./internal/proto ./internal/client ./internal/agentd ./cmd -count=1`（本机无沙箱）。原始输出：
  ```
  ok  github.com/Xsxdot/handoff/internal/proto  0.325s
  ok  github.com/Xsxdot/handoff/internal/client  9.120s
  ok  github.com/Xsxdot/handoff/internal/agentd  85.359s
  ok  github.com/Xsxdot/handoff/cmd  5.728s
  ```
  退出 0。判断：与岔口 2 协调者无沙箱复跑一致；本 plan 基线四包绿。T4 若红先重跑一次再归因。
- 2026-08-30：命令 `git diff --name-only 26e2ab7f -- web/`。原始输出空。命令 `git diff --name-only 1fa4668b -- web/` 列出若干 `web/src/...`（spec 基线到本分支之间的无关提交，不是本卡改动）。判断：T4 以冻结提交 `26e2ab7f` 与 plan 提交为对照，禁止本卡向 `web/` 加路径。
- 2026-08-30：命令 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B298-charter sym 'internal/agentd/manager.go#Manager.Done'`。原始输出：`Error: 符号 "internal/agentd/manager.go#Manager.Done" 不在图中（图未覆盖或名字有误）；近似候选: []。` 退出 1。判断：`sym` 拒 `file#Symbol` 查询形，记图覆盖债；改查短名。
- 2026-08-30：命令 `go run ... codegraph --repo . --view cards-B298-charter sym 'Manager.Done'`。命中 `n_agentd_Manager_Done`，`file=internal/agentd/manager.go`，`line=1387`，`anchor=moved`，签名 `func (m *Manager) Done(ctx context.Context, taskID, note string) (err error)`。
- 2026-08-30：命令 `go run ... who-calls 'Manager.GC'`。`n_agentd_Server_handleGC` dist=-1；edges null；warning `基线仍有 6 个未扫描入口`。`flow 'Manager.GC'`：`degraded=true`，`missing=基线没有 flows 段`，`steps=[]`，callers 仅 handleGC。判断：禁止用 chain 冒充 flow；GC 行为以源码为准。
- 2026-08-30：命令 `go run ... chain 'Manager.GC'`。仅自身节点，无出边。判断：Ticket 0 空壳尚未调用 Reclaim/ListTasks/RemoveAll，最薄路径必须由 T1 helper + T2 点亮，不得把 503 当成已有活路径。
- 2026-08-30：命令 `go run ... who-calls 'Manager.Done'`。调用/邻域含 `Server.handleDone`、`POST /api/tasks/{id}/done`、`RemoveManagedWorktree`；`compensateWorkspace` 图记行 1051（现状 1090，保鲜债）。未把 chain 当控制流。
- 2026-08-30：stdlib 实读（GOROOT `/usr/local/go`，go1.26.1）：`path/filepath/path.go:123-132` Join 忽略空元素并 Clean；`path.go:381-407` WalkDir 含 root、字典序、**不跟随符号链接**、根用 `os.Lstat`；`os/removeall_at.go:15-32` 空路径/不存在返回 nil；`removeall_at.go:66-160` 递归并保留首错；`removeall_at.go:163-177` `openDirAt` 对非目录（含指向目录的 symlink）报错不进入目标；`io/fs/fs.go:116-122` DirEntry.Info 对 symlink 描述链接本身，可能 `ErrNotExist`；`fs.go:167-175` Size 对普通文件是字节长度；`os/stat.go:9-27` Stat 跟随、Lstat 不跟随。
- 2026-08-30：`ReclaimList` 的 `Scanned` 实现只计 `IsTerminal && WorktreeManaged && WorkDir!=""`（`reclaim.go:356-360`），注释却写「终态任务总数」。岔口 1 拍板取注释语义：GC 计**全部终态行**（含无叶子无残树）。用测试钉死，不得复制 ReclaimList 的过滤计数。
- 2026-08-30：`proto.CanTransit`：`failed→running` 合法，`completed→running` 非法。T2 预览/执行之间「变成 running」用 failed 任务 `UpdateTaskState(running)`。
- 2026-08-30：`TaskState.IsTerminal` 只认 completed/failed；waiting_review 非终态。Done 入口要求 waiting_review，迁 completed 之后才到缓存删除，故收口时自己已是终态、不算占用者。
- 2026-08-30：占用快照必须 `store.Store.ListTasks`（`store.go:414`）。`ActiveTasksByWorkDir` 按 workdir 不是短号，禁止复用。
- 2026-08-30：harness 定位：`internal/agentd/reclaim_test.go`（`initGitRepo`/`newWorktree`/`newReclaimManager`/`seedTerminalTask`）；`manager_test.go` `newTestManager`/`mustCreateTask`/`mustDone`/`compensateOnlyManager`/`TestDoneRemovesManagedWorktree`/`TestDoneWorktreeRemoveFailureDoesNotBlockArchive`/`TestCompensateKeepsBranchWhenWorktreeRemoveFails`；`reclaim_server_test.go` Handler+Host `127.0.0.1:7777`+Bearer；`cmd/gc_test.go` 直接调 `runGC` 并恢复 `gcForce/gcYes/gcJSON`。
- 2026-08-30：删除失败注入：Manager 已有测试缝 `writeTaskFile`/`sweepProcs`（生产 nil）。缓存删除用同款 `removeCacheLeafFn`，不靠 chmod（跨平台不稳）。
- 2026-08-30：`render.log` 位置 `DataDir/tasks/<id>/render.log`（`render_stream_test.go:26-32`）。叶子只删 `TaskTmpDir` 与 `tasks/<id>/tmp`，任务目录本身不动。
- 2026-08-30：`handleReclaim` 用 `json.NewDecoder(r.Body).Decode`，解码失败当 force=false。T2 删除 `jsonDecode`，改走同一写法。
- 2026-08-30：`Server.Handler`：`GET/POST /api/gc` 注册在 api mux 498-499；`mux.Handle("/api/", api)` 600；`root.Handle("/", s.auth(mux))` 617。未鉴权走 `writeUnauthorized` → 401。
- 2026-08-30：判断：单实现卡内部 ticket 顺序 helper → T1 接线 → T2 GC+写响应 → T3 CLI → T4 缝合。不另开卡。不改 `TaskTmpDir` 路径形状。不改 `web/`。不改 `internal/proto/gc.go` 字段。
- 2026-08-30：放弃的尝试：用 `sym file#Symbol` 查图（被拒）；把 `ReclaimList.Scanned` 的过滤计数直接当作 GC scanned（与岔口 1 全文冲突，否决）；把缓存删除只写在 compensate 成功路径末尾（会被 1105 return 截走，否决）。
- 2026-08-30：命令 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B298-charter resolve --doc docs/superpowers/plans/b298-plan.md`。退出 0。16 个锚：ok = TaskTmpDir、Manager.GC、handleGC、Reclaim、ReclaimList、IsTerminal、GCPreview、GC、runGC、renderGC；moved = Done、Stop、compensateWorkspace、writeJSON、ListTasks、newTargetClient（行号漂移，nodeId 命中）。坏锚 0（无 error/miss）。
- 2026-08-30：占位符扫描：除 §9 自我声明外无 TBD/TODO/FIXME/「适当的错误处理」/「同 Task N」。修正了三处出稿错误：① POST 解码失败必须把 force 写成 body 零值而不是保留 query；② `sumRegularFileBytes` 失败日志先判 `m.log != nil`；③ `TestGCExecuteSkipsDirtyWithoutForceAndContinues` 对已有 cleanID 只 `writeCacheLeaves`、不再 `CreateTask` 撞主键。
- 2026-08-30：四包基线本节点无沙箱已绿（见上），与岔口 2 一致。计划提交范围仅 plan + 本台账。
