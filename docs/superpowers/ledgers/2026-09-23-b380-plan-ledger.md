# B380 plan 节点台账（2026-09-23）

基线：`cards/B233.1-charter-7`（本卡合并目标）；本节点工作分支 `cards/B380-charter-3`，起手 HEAD `64b4c455`。
节点：plan（charter）。产出：`docs/superpowers/plans/b380-plan.md` + 本台账。

## 事实流水

- **输入核对**：contract `docs/superpowers/specs/b380-contract.md`（冻结 `2a5378ab`）、breakdown `docs/superpowers/specs/b380-breakdown.md`（拍板回填 `64b4c455`）实读。spec `docs/superpowers/specs/b380.md` **不在本工作树**（`ls` 报 No such file）；按 breakdown §2.1 经 `git show 117bbd5a:docs/superpowers/specs/b380.md` 读到全文 108 行，头部「上游状态：已批准（2026-09-23 四点裁定）」，一致。
- **拍板约束**：P1=甲（代码单轮闭合，不再开实现子卡）→ 本 plan 无 Go 代码 task；P2=乙（S1 并入 implement）→ 唯一实现 task 是三份文档；P3=甲（spec 落点维持 origin，合并归协调者）→ 执行者不搬 spec；P4=甲（不补机内端到端）→ 跨机归真机清单。
- **spec 服务点**：spec §5 明写「README / handoff skill 的事件表若标注了交付性，实现时逐一核对同步」——S1 的契约依据。
- **代码冻结实现在场复核**（grep 实读）：
  - `internal/orchestration/facade.go:127` `AppendEvent(...EventTypeTicketAnswered...)`、`:133` `m.hub.Publish(evt)`；
  - `internal/orchestration/manager.go:2554` `approvePermission`、`:2577` `AppendEvent`；
  - `internal/approval/client.go:314` `consult`、`:430` `AppendEvent`、`:551` 私有 payload；
  - `internal/ledger/taskstate.go:147` `OpenTickets`、`:188` `case evTicketsVoided, "completed", "failed", "archived":`；
  - `internal/proto/proto.go` `EventTypeTicketAnswered` 注记已改述为「会 Publish（B380）……但在客户端不可交付」。
- **编译与测试（本轮亲跑，原始输出）**：

```
$ go version
go version go1.26.1 linux/amd64

$ go build ./...
BUILD_EXIT=0

$ go test ./internal/ledger/ -run TestOpenTicketsTerminal -count=1
ok  	github.com/Xsxdot/handoff/internal/ledger	0.613s
LEDGER_EXIT=0

$ go test ./internal/orchestration/ -run TestB380 -count=1
ok  	github.com/Xsxdot/handoff/internal/orchestration	0.307s
ORCH_EXIT=0

$ go test ./internal/approval/ -run TestB380 -count=1
ok  	github.com/Xsxdot/handoff/internal/approval	0.101s
APPROVAL_EXIT=0
```

- **文档基线 grep（本轮亲跑）**：

```
$ grep -n '只入库' skills/handoff/SKILL.md
96:`wait`（只入库）。任务流的集合外全是可动作事件，包括 `delivery_failed`、`stalled`、

$ grep -nE '逐 seq|watermark.*对账' docs/roadmap.md
ROADMAP_GREP_EXIT=1          # 无命中

$ grep -n 'published to the live' README.md
（无命中）

$ grep -n 'ticket_answered' README.md
435:`ticket_answered`, `permission_auto_allow`, and `permission_reuse`. Every other
```

- **图查询（本轮亲跑，先查图）**：

```
$ codegraph --repo . sym AnswerTicket
{"view":"baseline","query":"AnswerTicket","matches":[{"id":"n_store_Store_AnswerTicket","domain":"d_orchestration",...,"file":"internal/store/store.go","line":1266,...}]}
# 命中 store.Store.AnswerTicket；未命中 orchestration 的 Manager.AnswerTicket

$ codegraph --repo . sym OpenTickets
Error: 符号 "OpenTickets" 不在图中（图未覆盖或名字有误）；近似候选: []
EXIT=0

$ codegraph --repo . sym EventTypeTicketAnswered
Error: 符号 "EventTypeTicketAnswered" 不在图中（图未覆盖或名字有误）；近似候选: []
EXIT=0

$ codegraph --repo . check
EXIT=0    # assignedContainers 338 = viewContainers 338
```

  与 contract §7 图覆盖债一致（`Manager.AnswerTicket` / `Store.OpenTickets` / `EventTypeTicketAnswered`）。plan 对三者只用普通路径、不带 `#Symbol` 锚；未以 grep 顶替图查询。

- **本节点未碰 handoff CLI、未派发、未起新 executor**；只读文件与 git/codegraph 查询。
- **残余记录**：`README.zh-CN.md:247`（「`progress`、审批链审计事件只入库不唤醒」）不在 breakdown §3.1④ 有界文件集内，本 plan 不动，记 plan §7 残余（执行者不得自行扩围）。

## 提交事实（历史读数）

```
$ git add docs/superpowers/plans/b380-plan.md docs/superpowers/ledgers/2026-09-23-b380-plan-ledger.md
$ git commit -q -m "plan(B380): 关单镜像文档收口——实现计划 + 台账"
$ git log --oneline -1
1c97c177 plan(B380): 关单镜像文档收口——实现计划 + 台账
```

随后按纪律 amend 一次把本条台账收进同批提交（hash 会变，属 git 事实）。收口判据是工作树干净，不追台账里的 hash 等于 HEAD。
