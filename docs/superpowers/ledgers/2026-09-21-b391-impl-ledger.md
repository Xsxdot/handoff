# B391 implement 节点台账（2026-09-21）

节点：implement（charter 流程，卡 B391）。按 `docs/superpowers/plans/b391-plan.md` 实施。
裁决：改实现——在写入门 `Service.Send` 归一化剥可选前导 `@`，存储保持契约裸口径；
`ResolveDelivery`/`MessageWakeTargets`/`wakeconsumer`/`session wait` 零改动。

改动文件：
- `internal/collab/service.go`（实现：`normalizeMentions` + Send 落账前归一化 + 成功日志 mentions 读数）
- `internal/collab/sessions_test.go`（缝级回归 + 反例）
- `cmd/session.go`（`--mention` 帮助文案）
- `cmd/session_test.go`（CLI 端到端 + 帮助文案断言）

## 一、Task 1 红（先写测试，跑红）

`$ go test ./internal/collab/ -run 'TestSendNormalizesMentionAtPrefixToSeat|TestSendMentionNormalizationReverseCases' -count=1`

```
--- FAIL: TestSendNormalizesMentionAtPrefixToSeat (0.11s)
    sessions_test.go:1185: 落账 mentions 应为契约裸口径 [B1 user:sy agent:opencode]，实得 ["@B1" "@user:sy" "@agent:opencode"]
--- FAIL: TestSendMentionNormalizationReverseCases (0.09s)
    sessions_test.go:1277: @空座卡不落空: 应空集，实得 [@B2]
FAIL
FAIL	github.com/Xsxdot/handoff/internal/collab	0.204s
FAIL
```

红因是断言失败（功能缺失），非编译错误/typo——bug 复现：`@B1` 存原样、`GetCard` 查不到。

## 二、Task 1 绿

`$ go test ./internal/collab/ -run 'TestSendNormalizesMentionAtPrefixToSeat|TestSendMentionNormalizationReverseCases|TestWakeTargetsAddressing|TestResolveDeliveryPurity|TestIsAddressed' -count=1`

```
ok  	github.com/Xsxdot/handoff/internal/collab	0.453s
```

`$ go build ./...` → exit 0（无输出）。

## 三、Task 2/3 CLI 绿

`$ go test ./cmd/ -run 'TestSessionSendMentionAtPrefixWakesSeat|TestSessionSendMentionHelpMatchesAcceptedForms|TestSessionSendReplyToFlag|TestRoomSendCarriesRefAndMention|TestSessionSendNegativePaths' -count=1`

```
ok  	github.com/Xsxdot/handoff/cmd	0.637s
```

## 四、变异复验（确认测试有牙）

变异前先断言命中唯一：`grep -c "msg.Mentions = normalizeMentions(rawMentions)" internal/collab/service.go` → `1`。

变异：把 `msg.Mentions = normalizeMentions(rawMentions)` 改成 `msg.Mentions = rawMentions`（只改一行，可编译）。

`$ go build ./...` → `BUILD=0`（编译通过，排除「编译不过导致假 0 红」）。

`$ go test ./internal/collab/ -run 'TestSendNormalizesMentionAtPrefixToSeat|TestSendMentionNormalizationReverseCases' -count=1`

```
--- FAIL: TestSendNormalizesMentionAtPrefixToSeat (0.12s)
    sessions_test.go:1185: 落账 mentions 应为契约裸口径 [B1 user:sy agent:opencode]，实得 ["@B1" "@user:sy" "@agent:opencode"]
--- FAIL: TestSendMentionNormalizationReverseCases (0.15s)
    sessions_test.go:1277: @空座卡不落空: 应空集，实得 [@B2]
FAIL
FAIL	github.com/Xsxdot/handoff/internal/collab	0.271s
FAIL
```

结论：去掉归一化后重新变红且行为确变。变异已还原（grep 确认仅 `normalizeMentions(rawMentions)` 一处）。

## 五、收尾回归（触及包全量）

`$ go test ./internal/collab/... -count=1`

```
ok  	github.com/Xsxdot/handoff/internal/collab	7.776s
?   	github.com/Xsxdot/handoff/internal/collab/client	[no test files]
?   	github.com/Xsxdot/handoff/internal/collab/cursor	[no test files]
ok  	github.com/Xsxdot/handoff/internal/collab/room	0.002s
```

`$ go test ./cmd/ -count=1`

```
ok  	github.com/Xsxdot/handoff/cmd	65.792s
```

## 六、图对账

`$ codegraph validate` → `issues`: 1 个既有 issue（`[cards-B374-charter] 新增容器 k_collab_model 已存在于基线`），非本卡引入；nodes 5315，失鲜 0。新增未导出 `normalizeMentions` 未触发新 issue。

## 七、提交事实（历史读数）

见收尾提交。
