# B233.15 implement 台账（2026-09-11）

节点：implement（executor）。产物：`internal/workspace/*` 迁移落地 + `internal/agentd`/`cmd` 消费侧改写 + 图对账。
计划：`docs/superpowers/plans/b233.15-plan.md`；spec：`docs/superpowers/specs/b233.15.md`。
基线分支：`cards/B233.15-charter-2`，起点 `bdeda1e9`。本文件边干边记；结论进本节点产出物。

## 0. 开工读数

- `git branch --show-current` → `cards/B233.15-charter-2`
- `git log --oneline -1` → `bdeda1e9 docs(B233.15): 实现计划（工作区实现迁出，P1-P4 落实）`
- `git status` → clean
- `go build ./...` → exit 0（BUILD_OK）

## 1. 计划与现实的偏差（逐条声明，落地时处置）

1. **`Baseline` 类型重名**：`internal/workspace/capability.go` 已有 `Baseline{Start,Ahead,Fetched}`，
   与 `internal/agentd/workspace.go:938` 的 `Baseline` 字段完全同形。迁移时删除 agentd 侧重复定义，
   复用契约面类型。
2. **`ResolveBaseBranch` 名冲突**：agentd 已有导出 `ResolveBaseBranch(ctx,repo,remote,branch)`（D2 远端补拉），
   与 plan §2.1 要导出的 `ResolveBaseBranch(repo) string`（原 `resolveBaseBranch`）撞名。按 plan §2.1
   为准：3 参远端补拉函数降为私有 `resolveRemoteBaseBranch`，1 参推导函数导出为 `ResolveBaseBranch`。
3. **`id8`/`taskBranch` 重复**：`internal/workspace/identity.go` 已有 `ID8`/`TaskBranch`，迁入的
   `id8`/`taskBranch` 改为直接调用导出函数并删除私有包装。
4. **`log`/`truncateRunes`/`shortHash` 两侧各留一份**（plan §1 假缝禁令）：workspace 侧随 `workspace.go`
   迁入；agentd 侧新增 `logging.go`（log）、`shortHash` 落到 `workspacefiles.go`。

（后续实验读数与判断追加于下）

## 2. P1 迁移落地读数

```
$ go build ./...
BUILD_OK   # 全量生产编译通过（exit 0）
```

- `git mv` 迁出 11 文件（gitworkspace/manualworktree/workspaceprobe/gitroot/gitignore/runshell/procgroup×2/opennonblock×2/bundle）。
- 新增 `internal/workspace/{injection,named,reclaimhelpers,capability_impl}.go`；新增 `internal/agentd/logging.go`。
- 冲突处置：`Baseline` 复用契约面类型；3 参 `ResolveBaseBranch` 降为私有 `resolveRemoteBaseBranch`；
  `errFetchRefLockContention`/`errLocalBaseBranchDiverged` 必须导出为
  `ErrFetchRefLockContention`/`ErrLocalBaseBranchDiverged`（server.go 的 writeDispatchError 跨包
  errors.Is 映射要用；plan §2.1 未列，但属 spec「错误哨兵可判别」要求的必要导出）。
- `env.go` 实际用 `envfile.ErrBaseMismatch`（非本包哨兵），plan §6 该行不适用，未改。

## 3. P1b 测试迁移读数

- `git mv` 13 个测试文件到 `internal/workspace/`；新建 `gittesthelpers_test.go`（两侧各一份）、
  `bundlehelpers_test.go`（agentd 侧）、`reclaimhelpers_test.go`（workspace，拆自 reclaim_test.go）。
- 删除 `workspace_identity_contract_test.go`（agentd 副本已不存在，语义由 workspace/identity_test.go 覆盖）。
- 拆出 HTTP 依赖用例：`workspace_run_test.go` 的 `TestTaskRunMissingWorkdirReturns400` → agentd
  `workspace_run_http_test.go`；`workspace_fence_test.go` 的 RunCmd/quotaNote 用例 → workspace
  `fence_test.go`；`TestCloneDoesNotWaitForRepoFetchLock` → workspace `clone_lock_test.go`（改写为
  CloneRepo 直测，保留性质）。
- 因私有符号不可跨包，agentd 测试新增私有副本 `canonPath`/`mustWriteFile`；`maxRunOutput` 不可跨包，
  改用 `testBigFileBytes = 1<<21` 造超限文件，删除依赖精确上限的长度断言（截断提示断言保留）。

```
$ go test ./internal/workspace/... -count=1
ok  	github.com/Xsxdot/handoff/internal/workspace	7.071s

$ go test ./internal/agentd -count=1
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.22s)
    scheddispatch_test.go:531: 第 1 条漂移：got=GOLDEN|2|dispatched... want=GOLDEN|2|comment...
FAIL	github.com/Xsxdot/handoff/internal/agentd	197.459s

$ go test ./cmd/... -count=1
ok  	github.com/Xsxdot/handoff/cmd	34.953s
```

**基线证据（该 agentd 失败非本卡引入）**：在 detached worktree `bdeda1e9`（本卡起点，未含任何本卡改动）
上单跑同一用例，得到逐字相同的失败：

```
$ git worktree add --detach /root/.handoff/tmp/0eacc05e/base-base bdeda1e9
$ (base-base) go test ./internal/agentd -run TestLegacyNodeEventSequenceUnchanged -count=1
--- FAIL: TestLegacyNodeEventSequenceUnchanged (2.83s)
    scheddispatch_test.go:531: 第 1 条漂移：got=GOLDEN|2|dispatched... want=GOLDEN|2|comment...
FAIL
```

判断：`TestLegacyNodeEventSequenceUnchanged` 是既有失败（事件时序/环境相关），与本卡实现迁出无关；
本卡交付范围内的 workspace/cmd 全绿，agentd 除该既有失败外全绿。

## 4. P2 具名能力缝级断言 + 变异自验

新增 `internal/workspace/named_test.go`（S4）：DeleteBranch、BranchTip、RestoreWorktree、
ResolveBaseBranch(1 参推导)、CloneRepo(本地/出网两路)、OriginURL/TopLevel/HeadBranch/ProbeCommit、
RemoveWorktree(force 语义)、PruneWorktrees。

```
$ go test ./internal/workspace/ -run 'TestDeleteBranch|TestBranchTip|TestRestoreWorktree|TestResolveBaseBranchPrefers|TestCloneRepoLocal|TestRepoProbes|TestRemoveWorktreeForce|TestPruneWorktrees' -count=1
ok  	github.com/Xsxdot/handoff/internal/workspace	0.425s
```

**变异自验（每发先确认命中唯一 + 编译通过，再看红）**：

```
命中唯一：grep -c '"branch", "-D", branch' named.go = 1
变异 A：named.go `"branch", "-D", branch` → `"branch", "--list", branch`
  go build ./internal/workspace/  → BUILD_DONE（编译通过）
  go test -run TestDeleteBranchRemovesOnlyNamedBranch → --- FAIL
  已还原（grep -c = 1）

命中唯一：grep -c 'if force {' reclaimhelpers.go = 1
变异 B：reclaimhelpers.go `if force {` → `if true {`
  go build → 编译通过；go test -run TestRemoveWorktreeForceDiscardsDirty → --- FAIL
  已还原（grep -c = 1）

命中唯一：grep -c '"checkout", ref' named.go = 1
变异 C：named.go `"checkout", ref` → `"status", ref`
  go build → 编译通过；go test -run TestRestoreWorktreeChecksOutRef → --- FAIL
  已还原（grep -c = 1）
```

结论：三条具名能力断言均有牙（变异生效即变红），非直通镜像。

## 5. P3 图对账（部分：仅事实性锚修正；扫描增量按 plan 分派）

- 已改 `codegraph/domains/d_workspace.json` 的 lifecycle 锚：
  `internal/agentd/workspace.go#PrepareWorkspace|#RemoveManagedWorktree`
  → `internal/workspace/gitworkspace.go#PrepareWorkspace|#RemoveManagedWorktree`（事实修正）。
- `codegraph validate`（正确语法为 `validate --repo .`，plan §11 写反了子命令序）：
  改前 issues=3，改后 issues=1：

```
$ go run .../codegraph validate --repo .
issues: ["调用边门控: n_cmd_localProjectRoot（cmd/project.go）→ n_agentd_MainWorktreeRoot（internal/agentd/gitroot.go）：包级函数调用，调用文件未 import internal/agentd，不可能编译通过"]
```

- `codegraph check --repo .`：`fails: []`，exit 0（spec 硬闸满足，不增）。

**判断**：剩余 1 条 validate issue 的根因是 baseline 仍把 `MainWorktreeRoot` 记在 agentd，
而代码已迁到 workspace；完整修复需要重建本分支视图增量（move 94 个节点 + 新增 workspace 符号
+ 重算 target 预算），这正是 plan §10 step 2 / §14 明示的**独立图扫描节点**（本 executor 不派发、
不伪造扫描）。故 `codegraph/diffs/<分支>.json` 与 `target.json` 棘轮**留给图对账节点**，本节点不估数。
`codegraph/best.json` 按 plan §10 step 4 不动。

## 6. P4 收口验收读数

```
$ gofmt -l internal/workspace internal/agentd cmd
（空）
$ go vet ./internal/workspace/... ./internal/agentd/... ./cmd/...
VET_EXIT=0
$ go build ./...
BUILD_OK
$ go test ./internal/workspace/... -count=1
ok  	github.com/Xsxdot/handoff/internal/workspace	6.825s
$ go test ./internal/agentd -skip TestLegacyNodeEventSequenceUnchanged -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	253.035s
$ go test ./cmd/... -count=1
ok  	github.com/Xsxdot/handoff/cmd	49.269s
$ grep -rn 'exec.Command("git"\|func gitRun\|func gitProbe\|func gitRunNet\|func gitExec' internal/agentd --include='*.go' | grep -v _test.go
（空）
$ grep -n '^func [A-Z].*git\(Run\|Probe\|Exec\)' internal/workspace/*.go
（空）
$ go test ./internal/agentd -run 'TestDispatchDirtyWorktree409|TestDispatchRepoUnusable400|TestStopRemovesManagedWorktree|TestCompensateUserWorktreeRestores|TestWorkspace|TestTaskFile|TestDiff|TestRunCmd|TestAssembleResultRef|TestReclaim|TestManualWorktree|TestProject' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	11.848s
```

唯一未绿：`TestLegacyNodeEventSequenceUnchanged`，已证在起点 `bdeda1e9`（未含本卡改动）上同样失败，
属既有问题（见 §3），不在本卡交付范围内。

## 7. 提交事实（历史读数）

```
$ git add -A && git commit -m "refactor(B233.15): 工作区实现迁出 agentd，git 主体进 internal/workspace"
[cards/B233.15-charter-2 bb00a889] refactor(B233.15): 工作区实现迁出 agentd，git 主体进 internal/workspace
 64 files changed, ...
$ git rev-parse HEAD
bb00a8898ef24539b1061585dd2d5522e44e3ddb
$ git status --short
（空）
```

本文件随同批提交，提交后按纪律只 amend 一次收进同批；amend 换 hash 属 git 事实。
