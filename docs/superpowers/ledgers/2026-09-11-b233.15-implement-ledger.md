# B233.15 implement 台账（2026-09-11）

节点：implement（executor）。产物：`internal/workspace/*` 迁移落地 + `internal/agentd`/`cmd` 消费侧改写 + 图对账。
计划：`docs/superpowers/plans/b233.15-plan.md`；spec：`docs/superpowers/specs/b233.15.md`。
基线分支：`cards/B233.15-charter-2`，起点 `bdeda1e9`。本文件边干边记；结论进本节点产出物。

## 0. 开工读数

- `git branch --show-current` → `cards/B233.15-charter-2`
- `git log --oneline -1` → `bdeda1e9 docs(B233.15): 实现计划（工作区实现迁出，P1-P4 落实）`
- `git status` → clean
- `go build ./...` → exit 0（BUILD_OK）

## 1. 计划与现实的偏差（逐条声明，落地时处置）

1. **`Baseline` 类型重名**：`internal/workspace/capability.go` 已有 `Baseline{Start,Ahead,Fetched}`，
   与 `internal/agentd/workspace.go:938` 的 `Baseline` 字段完全同形。迁移时删除 agentd 侧重复定义，
   复用契约面类型。
2. **`ResolveBaseBranch` 名冲突**：agentd 已有导出 `ResolveBaseBranch(ctx,repo,remote,branch)`（D2 远端补拉），
   与 plan §2.1 要导出的 `ResolveBaseBranch(repo) string`（原 `resolveBaseBranch`）撞名。按 plan §2.1
   为准：3 参远端补拉函数降为私有 `resolveRemoteBaseBranch`，1 参推导函数导出为 `ResolveBaseBranch`。
3. **`id8`/`taskBranch` 重复**：`internal/workspace/identity.go` 已有 `ID8`/`TaskBranch`，迁入的
   `id8`/`taskBranch` 改为直接调用导出函数并删除私有包装。
4. **`log`/`truncateRunes`/`shortHash` 两侧各留一份**（plan §1 假缝禁令）：workspace 侧随 `workspace.go`
   迁入；agentd 侧新增 `logging.go`（log）、`shortHash` 落到 `workspacefiles.go`。

（后续实验读数与判断追加于下）

## 2. P1 迁移落地读数

```
$ go build ./...
BUILD_OK   # 全量生产编译通过（exit 0）
```

- `git mv` 迁出 11 文件（gitworkspace/manualworktree/workspaceprobe/gitroot/gitignore/runshell/procgroup×2/opennonblock×2/bundle）。
- 新增 `internal/workspace/{injection,named,reclaimhelpers,capability_impl}.go`；新增 `internal/agentd/logging.go`。
- 冲突处置：`Baseline` 复用契约面类型；3 参 `ResolveBaseBranch` 降为私有 `resolveRemoteBaseBranch`；
  `errFetchRefLockContention`/`errLocalBaseBranchDiverged` 必须导出为
  `ErrFetchRefLockContention`/`ErrLocalBaseBranchDiverged`（server.go 的 writeDispatchError 跨包
  errors.Is 映射要用；plan §2.1 未列，但属 spec「错误哨兵可判别」要求的必要导出）。
- `env.go` 实际用 `envfile.ErrBaseMismatch`（非本包哨兵），plan §6 该行不适用，未改。

## 3. P1b 测试迁移读数

- `git mv` 13 个测试文件到 `internal/workspace/`；新建 `gittesthelpers_test.go`（两侧各一份）、
  `bundlehelpers_test.go`（agentd 侧）、`reclaimhelpers_test.go`（workspace，拆自 reclaim_test.go）。
- 删除 `workspace_identity_contract_test.go`（agentd 副本已不存在，语义由 workspace/identity_test.go 覆盖）。
- 拆出 HTTP 依赖用例：`workspace_run_test.go` 的 `TestTaskRunMissingWorkdirReturns400` → agentd
  `workspace_run_http_test.go`；`workspace_fence_test.go` 的 RunCmd/quotaNote 用例 → workspace
  `fence_test.go`；`TestCloneDoesNotWaitForRepoFetchLock` → workspace `clone_lock_test.go`（改写为
  CloneRepo 直测，保留性质）。
- 因私有符号不可跨包，agentd 测试新增私有副本 `canonPath`/`mustWriteFile`；`maxRunOutput` 不可跨包，
  改用 `testBigFileBytes = 1<<21` 造超限文件，删除依赖精确上限的长度断言（截断提示断言保留）。

```
$ go test ./internal/workspace/... -count=1
ok  	github.com/Xsxdot/handoff/internal/workspace	7.071s

$ go test ./internal/agentd -count=1
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.22s)
    scheddispatch_test.go:531: 第 1 条漂移：got=GOLDEN|2|dispatched... want=GOLDEN|2|comment...
FAIL	github.com/Xsxdot/handoff/internal/agentd	197.459s

$ go test ./cmd/... -count=1
ok  	github.com/Xsxdot/handoff/cmd	34.953s
```

**基线证据（该 agentd 失败非本卡引入）**：在 detached worktree `bdeda1e9`（本卡起点，未含任何本卡改动）
上单跑同一用例，得到逐字相同的失败：

```
$ git worktree add --detach /root/.handoff/tmp/0eacc05e/base-base bdeda1e9
$ (base-base) go test ./internal/agentd -run TestLegacyNodeEventSequenceUnchanged -count=1
--- FAIL: TestLegacyNodeEventSequenceUnchanged (2.83s)
    scheddispatch_test.go:531: 第 1 条漂移：got=GOLDEN|2|dispatched... want=GOLDEN|2|comment...
FAIL
```

判断：`TestLegacyNodeEventSequenceUnchanged` 是既有失败（事件时序/环境相关），与本卡实现迁出无关；
本卡交付范围内的 workspace/cmd 全绿，agentd 除该既有失败外全绿。

## 4. P2 具名能力缝级断言 + 变异自验

新增 `internal/workspace/named_test.go`（S4）：DeleteBranch、BranchTip、RestoreWorktree、
ResolveBaseBranch(1 参推导)、CloneRepo(本地/出网两路)、OriginURL/TopLevel/HeadBranch/ProbeCommit、
RemoveWorktree(force 语义)、PruneWorktrees。

```
$ go test ./internal/workspace/ -run 'TestDeleteBranch|TestBranchTip|TestRestoreWorktree|TestResolveBaseBranchPrefers|TestCloneRepoLocal|TestRepoProbes|TestRemoveWorktreeForce|TestPruneWorktrees' -count=1
ok  	github.com/Xsxdot/handoff/internal/workspace	0.425s
```

**变异自验（每发先确认命中唯一 + 编译通过，再看红）**：

```
命中唯一：grep -c '"branch", "-D", branch' named.go = 1
变异 A：named.go `"branch", "-D", branch` → `"branch", "--list", branch`
  go build ./internal/workspace/  → BUILD_DONE（编译通过）
  go test -run TestDeleteBranchRemovesOnlyNamedBranch → --- FAIL
  已还原（grep -c = 1）

命中唯一：grep -c 'if force {' reclaimhelpers.go = 1
变异 B：reclaimhelpers.go `if force {` → `if true {`
  go build → 编译通过；go test -run TestRemoveWorktreeForceDiscardsDirty → --- FAIL
  已还原（grep -c = 1）

命中唯一：grep -c '"checkout", ref' named.go = 1
变异 C：named.go `"checkout", ref` → `"status", ref`
  go build → 编译通过；go test -run TestRestoreWorktreeChecksOutRef → --- FAIL
  已还原（grep -c = 1）
```

结论：三条具名能力断言均有牙（变异生效即变红），非直通镜像。

## 5. P3 图对账（部分：仅事实性锚修正；扫描增量按 plan 分派）

- 已改 `codegraph/domains/d_workspace.json` 的 lifecycle 锚：
  `internal/agentd/workspace.go#PrepareWorkspace|#RemoveManagedWorktree`
  → `internal/workspace/gitworkspace.go#PrepareWorkspace|#RemoveManagedWorktree`（事实修正）。
- `codegraph validate`（正确语法为 `validate --repo .`，plan §11 写反了子命令序）：
  改前 issues=3，改后 issues=1：

```
$ go run .../codegraph validate --repo .
issues: ["调用边门控: n_cmd_localProjectRoot（cmd/project.go）→ n_agentd_MainWorktreeRoot（internal/agentd/gitroot.go）：包级函数调用，调用文件未 import internal/agentd，不可能编译通过"]
```

- `codegraph check --repo .`：`fails: []`，exit 0（spec 硬闸满足，不增）。

**判断**：剩余 1 条 validate issue 的根因是 baseline 仍把 `MainWorktreeRoot` 记在 agentd，
而代码已迁到 workspace；完整修复需要重建本分支视图增量（move 94 个节点 + 新增 workspace 符号
+ 重算 target 预算），这正是 plan §10 step 2 / §14 明示的**独立图扫描节点**（本 executor 不派发、
不伪造扫描）。故 `codegraph/diffs/<分支>.json` 与 `target.json` 棘轮**留给图对账节点**，本节点不估数。
`codegraph/best.json` 按 plan §10 step 4 不动。

## 6. P4 收口验收读数

```
$ gofmt -l internal/workspace internal/agentd cmd
internal/agentd/coordinator_home.go
$ go vet ./internal/workspace/... ./internal/agentd/... ./cmd/...
VET_EXIT=0
$ go build ./...
BUILD_OK
$ go test ./internal/workspace/... -count=1
ok  	github.com/Xsxdot/handoff/internal/workspace	6.825s
$ go test ./internal/agentd -skip TestLegacyNodeEventSequenceUnchanged -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	253.035s
$ go test ./cmd/... -count=1
ok  	github.com/Xsxdot/handoff/cmd	49.269s
$ grep -rn 'exec.Command("git"\|func gitRun\|func gitProbe\|func gitRunNet\|func gitExec' internal/agentd --include='*.go' | grep -v _test.go
（空）
$ grep -n '^func [A-Z].*git\(Run\|Probe\|Exec\)' internal/workspace/*.go
（空）
$ go test ./internal/agentd -run 'TestDispatchDirtyWorktree409|TestDispatchRepoUnusable400|TestStopRemovesManagedWorktree|TestCompensateUserWorktreeRestores|TestWorkspace|TestTaskFile|TestDiff|TestRunCmd|TestAssembleResultRef|TestReclaim|TestManualWorktree|TestProject' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	11.848s
```

唯一未绿：`TestLegacyNodeEventSequenceUnchanged`，已证在起点 `bdeda1e9`（未含本卡改动）上同样失败，
属既有问题（见 §3），不在本卡交付范围内。

## 7. 提交事实（历史读数）

```
$ git add -A && git commit -m "refactor(B233.15): 工作区实现迁出 agentd，git 主体进 internal/workspace"
[cards/B233.15-charter-2 bb00a889] refactor(B233.15): 工作区实现迁出 agentd，git 主体进 internal/workspace
 64 files changed, ...
$ git rev-parse HEAD
bb00a8898ef24539b1061585dd2d5522e44e3ddb
$ git status --short
（空）
```

本文件随同批提交，提交后按纪律只 amend 一次收进同批；amend 换 hash 属 git 事实。

（§7 的 `bb00a889` 是上一轮 `cards/B233.15-charter-2` 的历史读数。本轮 review fail 修复在
`cards/B233.15-charter-3` 上，审阅对象 `cards/B233.15-charter-2 @ 329c75fc`；本轮提交 hash 见 §10。）

## 8. review fail 修复轮（第 1 轮，2026-09-11，charter-3）

审阅结论：2 major + 3 minor。承重目标（迁出达成、只经具名能力、plan §4 三处差异成立）已确认，本轮只修五条，不扩大范围。

- **M1 冻结契约未回写**：`docs/superpowers/specs/b233.4-contract.md` 末尾追加
  `## 13. B233.15 修订记录（2026-09-11）`，逐字写明修订第 14 条依据（b233.15 spec 契约语义节
  「类型跨缝直引（proto），行为跨缝注入」）、新口径（允许 import `internal/proto`；第 15 条不得
  import `internal/ledger` 不变）、`d_workspace→d_protocol` 预算按实棘轮（数字归图对账列）。
  冻结正文第 1–12 节一字未改。
- **M2 日志丢 stderr 字段**：`DeleteBranch`/`RestoreWorktree`/`RemoveWorktree` 改为
  `(stderr string, err error)` 形态（对齐 `CloneRepo` 先例），manager.go 两处与 reclaim.go 一处
  恢复 `"stderr", truncateRunes(..., 300/300/200)` 字段。判据读数（`git diff bdeda1e9 -- internal/agentd/manager.go internal/agentd/reclaim.go`）：
  三处日志行（compensate 切回、deleteCreatedBranch、prunable Warn）与迁移前逐字一致（键名、截断长度均未变，diff 中显示为上下文行）。
- **M3 预览探针日志级别**：新增 `workspace.ProbeOriginURL(ctx, repo) (string, bool)`（走
  `gitProbe`，无 origin 静默 Debug），`preview_owner.go#defaultPreviewWorkspaceResolver` 改调它；
  `projectadmin.go:388` 登记路径仍用 `workspace.OriginURL`（无 origin 必须拒登记，语义不同，未动）。
- **M4 变异证据不齐**：见 §9，逐条补齐。
- **M5 读数纠正**：① §6 的 gofmt 读数改为含 `internal/agentd/coordinator_home.go` 的真实口径
  （该文件在起点 `bdeda1e9` 同样未格式化，与 biz 卡无关，未改）；
  ② `docs/superpowers/plans/b233.15-plan.md` 两处 `TestStopRemovesManagedWorktree` 更正为
  `TestStopRetainsManagedWorktree`（实际测试名，见 `internal/agentd/manager_test.go:659`）。

## 9. M4 具名能力缝级变异证据（逐条）

方法：每发先核 `count(old)==1`（唯一命中），再编译（`go build ./internal/workspace/`），
编译通过才跑单测；跑完立即还原并核 `file==original`。编译不过的发（初版 FindWorktree / BundleRange）
整块换成可编译的等价变异后重做，初版不计。

| # | 能力 | 变异（改了哪一行） | 该红测试 | 读数 |
|---|---|---|---|---|
| 1 | DeleteBranch | named.go `"branch", "-D", branch`→`"branch", "--list", branch` | `TestDeleteBranchRemovesOnlyNamedBranch` | 编译 exit 0；测试 FAIL `named_test.go:28: doomed 分支应已被删除` |
| 2 | BranchTip | gitworkspace.go `"rev-parse", "refs/heads/"+branch`→`"rev-parse", "--abbrev-ref", "refs/heads/"+branch` | `TestBranchTipReturnsHeadSHA` | 编译 exit 0；FAIL `named_test.go:45: BranchTip="main"，期望 40 位 sha` |
| 3 | ResolveDispatchBase | gitworkspace.go `resolved, err = resolveRemoteBaseBranch(...)`→`resolved, err = rev, nil` | `TestResolveDispatchBaseLocalBranchUsesConfiguredRemote` | 编译 exit 0；FAIL `workspace_test.go:1134: ...实得 main` |
| 4 | ResolveDefaultBaseBranch | gitworkspace.go `branch := strings.TrimPrefix(ref, prefix)`→`branch := ref` | `TestResolveDefaultBaseBranchUsesOriginHead` | 编译 exit 0；FAIL `workspace_test.go:1096: 默认分支="origin/main"，期望 main` |
| 5 | ListWorktrees | reclaimhelpers.go `gitRun(..., "worktree", "list", "--porcelain")`→`"worktree", "list"` | `TestClassifyCleanWorktree` | 编译 exit 0；FAIL `reclaimhelpers_test.go:119: 期望 clean，实得 absent` |
| 5 | ClassifyWorktree | reclaimhelpers.go parsePorcelainStatus `if len(line) < 4 {`→`if len(line) < 4 \|\| strings.HasPrefix(line, "??") {` | `TestClassifyUntrackedOnlyIsDirty` | 编译 exit 0；FAIL `reclaimhelpers_test.go:141: 只有未跟踪文件时也应判 dirty，实得 clean` |
| 5 | FindWorktree | reclaimhelpers.go `want := canonPath(workdir)`→`want := filepath.Clean(workdir)` | `TestFindEntryMatchesAcrossSymlink` | 编译 exit 0；FAIL `reclaimhelpers_test.go:95: 经符号链接给的 workdir 应能匹配到条目` |
| 6 | RemoveWorktree | reclaimhelpers.go `if force {`→`if true {` | `TestRemoveWorktreeForceDiscardsDirty` | 编译 exit 0；FAIL `named_test.go:153: 脏树不带 force 必须拒绝` |
| 6 | PruneWorktrees | reclaimhelpers.go `"worktree", "prune"`→`"worktree", "list"` | `TestPruneWorktreesClearsPrunable` | 编译 exit 0；FAIL `named_test.go:183: prune 后 .../wt-prune 不应在册` |
| 7 | CloneRepo | named.go `args := []string{"clone", "--", originURL, dest}`→`...{"clone", "--", originURL}` | `TestCloneRepoLocalAndProxyPaths` | 编译 exit 0；FAIL `named_test.go:90: 本地克隆落点应是仓库` |
| 8 | OriginURL | named.go `gitRun(ctx, repo, "remote", "get-url", "origin")`→`"get-url", "upstream"` | `TestRepoProbes` | 编译 exit 0；FAIL `named_test.go:110: OriginURL="" ... 期望 origin URL` |
| 8 | TopLevel | named.go TopLevel `return val, true`→`return val, false` | `TestRepoProbes` | 编译 exit 0；FAIL `named_test.go:123: TopLevel(sub) ok=false` |
| 8 | HeadBranch | named.go HeadBranch 失败分支 `return ""`→`return "main"` | `TestRepoProbes` | 编译 exit 0；FAIL `named_test.go:132: detached 时 HeadBranch 应为空，实得 "main"` |
| 8 | ProbeCommit | named.go `gitProbe(..., "rev-parse", rev)`→`"rev-parse", "--short", rev` | `TestRepoProbes` | 编译 exit 0；FAIL `named_test.go:137: ProbeCommit(main)="1b9bb54"，期望 40 位 sha` |
| 9 | ProbeWorkspaces | workspaceprobe.go `return []proto.Workspace{}, msg`→`return nil, msg` | `TestProbeWorkspacesBadDirDegrades` | 编译 exit 0；FAIL `workspaceprobe_test.go:74: 失败时必须返回空切片而非 nil` |
| 10 | BundleRange | bundle.go `if strings.TrimSpace(out) == "0" {`→`== "-1" {` | `TestBundleRangeEmptyWidens` | 编译 exit 0；FAIL `bundle_test.go:95: 空区间不该失败` |
| 11 | RestoreWorktree | named.go `"checkout", ref`→`"status", ref` | `TestRestoreWorktreeChecksOutRef` | 编译 exit 0；FAIL `named_test.go:61: HEAD="feature"，期望 main` |

**变异初版未过编译的两发（按要求整块替换后重做）**：
- `FindWorktree` 初版 `if canonPath(p) == want {`→`if p == workdir {` 导致 `want` 未使用，`go build` exit 1（`declared and not used: want`）——不算数；换成 `want := canonPath(workdir)`→`filepath.Clean(workdir)` 后编译过、测试红（见上表）。
- `BundleRange` 初版 `if strings.TrimSpace(out) == "0" {`→`if false {` 导致 `out` 未使用，`go build` exit 1——不算数；换成 `== "-1" {` 后编译过、测试红（见上表）。

**构造不出、如实记的**：CloneRepo 计划里的变异「两路都带代理」（`if withProxy {`→`if true {`）
在现有夹具下**不红**：`TestCloneRepoLocalAndProxyPaths` 用本地路径 origin，git 对本地 clone 忽略
`http.proxy`，且本地路在 `ConfigureGitNet` 注入前执行，故 `withProxy` 分支在本地夹具下不可观测
（测试注释已声明此局限）。代理参数本身的牙由 `TestGitNetArgsCarryProxyBeforeSubcommand`/
`TestGitNetArgsUnchangedWithoutProxy`（`workspace_test.go:1036/1053`）承担。上表改用「丢 dest」变异，
证明该测试确实穿过 CloneRepo 的 clone 落点。未新增测试夹具，避免扩大范围。

## 10. review 修复轮收口读数（2026-09-11，charter-3）

```
$ gofmt -l internal/workspace internal/agentd cmd
internal/agentd/coordinator_home.go     # 存量遗存，起点 bdeda1e9 同样未格式化，与本卡无关
$ go vet ./internal/workspace/... ./internal/agentd/... ./cmd/...
VET_EXIT=0
$ go build ./...
BUILD_EXIT=0
$ go test ./internal/workspace/... -count=1
ok  	github.com/Xsxdot/handoff/internal/workspace	6.215s
$ go test ./cmd/... -count=1
ok  	github.com/Xsxdot/handoff/cmd	34.686s
$ go test ./internal/agentd -run 'TestDispatchDirtyWorktree409|TestDispatchRepoUnusable400|TestStopRetainsManagedWorktree|TestCompensate|TestWorkspace|TestScratch|TestTaskFile|TestTaskRunMissingWorkdirReturns400|TestDefaultPreviewWorkspaceResolverReadsGitMetadata|TestPreviewOwner|TestReclaim|TestManualWorktree|TestDiff|TestRunCmd|TestAssembleResultRef|TestProject' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	11.755s
$ go test ./internal/agentd -skip TestLegacyNodeEventSequenceUnchanged -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	176.165s
$ grep -rn 'exec.Command("git"\|func gitRun\|func gitProbe\|func gitRunNet\|func gitExec' internal/agentd --include='*.go' | grep -v _test.go
（空）
$ grep -n '^func [A-Z].*git\(Run\|Probe\|Exec\)' internal/workspace/*.go
（空）
```

已知既有红 `TestLegacyNodeEventSequenceUnchanged` 不计（§3 已证起点同红）。全量 agentd 用
`-skip` 复跑即全绿。图产物（`codegraph/diffs/<分支>.json`、`target.json` 棘轮）留「图对账」列，本轮不做。

### 10.1 提交事实（本轮，历史读数）

```
$ git add -A && git commit -m "fix(B233.15): review 修复轮——契约修订回写、stderr 字段还原、预览探针 fail-open、变异证据补齐"
[cards/B233.15-charter-3 35a3be9c] fix(B233.15): review 修复轮——契约修订回写、stderr 字段还原、预览探针 fail-open、变异证据补齐
$ git rev-parse HEAD
35a3be9c2216200d56707e8a53f40d12458f82ca
$ git status --short
（空）
```

本文件随同批 amend 一次收进同批提交；amend 换 hash 属 git 事实（收口判据是工作树干净）。


