# B229 契约落地台账

- 2026-08-24：`git status && git branch --show-current && git log --oneline -5` 原始输出：工作树干净，分支 `cards/B229-charter`，HEAD `10906bb1 docs(spec): B229 纪律块分层重构 + 入账本 + 派发期下发（已批准，L3 重档）`。
- 2026-08-24：读取 `docs/superpowers/specs/2026-08-24-b229-discipline-layers-and-store.md` 头部，原始状态行为 `> 状态：**已批准（用户，2026-08-24）** · 级别 **L3** · 档位 **重档**`；开工前上游状态核对通过。
- 2026-08-24：`ls codegraph` 原始输出含 `baseline.json best.json diffs domains target.json`，本项目为存量有图项目，非绿地降档路径。
- 2026-08-24：`internal/discipline/platform.go` 实读：平台层正文为二进制内常量 `platformInvariantHead`（:10-14）/`platformInvariantTail`（:16），`Compose`（:23）是唯一组装函数，签名 `func Compose(base Block, platformEnabled bool) Block`。
- 2026-08-24：`internal/discipline/resolver.go` 实读：`:23` `maxBlockSize = 64 << 10`；`:29` `Dir(dataDir)`；`:87` `For(executor)` 三档（配置有值读盘 :99 / 显式空串关闭 :95 / 未配置内置默认 :91）；`:141` `ByName(name, executor)` 三档（磁盘 `<dir>/<name>.md` :148 → 内置同名 :172 → 报错 :177）；`:206` `Preflight`。spec 所引「六条回退分支」「resolver.go:141」「resolver.go:91」逐条对上。
- 2026-08-24：`internal/discipline/discipline.go` 实读：`:14-36` 六份 `go:embed builtin/{subagent,single-context,review,spec-draft,plan-writing,finishing}.md`；`:46-52` 角色名五常量；`:66-71` `defaultTier` 表仅 implement 分档；`:95` `builtinByName` 仅 `NameImplement` 分支调 `builtinFor`（spec「四个角色根本没有档位」属实）；`:59-62` `Block{Text,Source}`。
- 2026-08-24：`internal/ledgerstep/dispatch.go` 实读：`:8` 包注释原文「不解析纪律块——只把角色名传下去，正文由 agentd 解析注入」（本轮唯一被显式撤销的既有契约声明）；`:27` `DispatchOpts.Discipline` 只携名字；`:113-116` 纪律名取模板+override；`:269-274` `RecordDispatch` 快照只记 `DisciplineName` 无版本。
- 2026-08-24：`internal/ledger/store.go` 实读（grep `PRIMARY KEY (name, version)`）：PG `workflows` CREATE :234、`dispatch_templates` CREATE :237；SQLite 同两表 CREATE :296、:299。spec 引用行号逐一命中，`(name, version)` 主键在两方言同构。
- 2026-08-24：`internal/ledger/templates.go` 实读：`:66` `PutTemplate(name, def) (int, error)` 只插新版不改旧行；`:88` `GetTemplate(name, version)` 0=最新（`ORDER BY version DESC LIMIT 1`）；`:126` `ListTemplateNames()`。这是缝 2 的同构原型。
- 2026-08-24：`internal/ledger/events.go` 实读：`:113-114` 注释原文「原字段是正文指纹……同一个问题的答案换成名字」；`:115-127` `DispatchSnapshot` 十字段，无纪律块版本号；`:137` `RecordDispatch` 经 `appendEvent` 落 `EvDispatched`。
- 2026-08-24：`internal/config/config.go` 实读：`:159-168` `LedgerConfig{Enabled bool yaml:"enabled,omitempty"（默认 false）; DSN string}`，注释明写「不开时 CLI 的 card/workflow/decision 三族拒绝执行」。消费点三处：`cmd/agentd.go:248`、`cmd/ledgercli.go:30`、`cmd/status.go:86`（grep 实测）。
- 2026-08-24：`internal/agentd/manager.go` 实读：`:359` `resolveDisciplineFor(name, execName)`（spec :359 命中），内部 `ByName`/`For` 二选一后 `Compose(base, PlatformInvariantsEnabled())`；三个调用点 `:754`（Dispatch）、`:1265`（resumeForContinue）、`:3374`（启动恢复）与 spec 一致；`:1272-1283` 点名任务续接解析失败**拒绝续接**、未点名失败降级不注入；`:3377-3384` 启动恢复（热重连 Cold=false）点名失败只 Error 不阻断——不对称是刻意的。
- 2026-08-24：能力位四处投影链实读：Go `proto.StatusResp.LaunchersSupported *bool`（status.go:218）、Go `proto.Machine.LaunchersSupported *bool`（projects.go:160）、TS `StatusResp.launchers_supported?: boolean | null`（types.ts:243）、TS `Machine.launchers_supported?: boolean`（types.ts:385）；上报点 server.go:683；`PtySupported` 同链（status.go:200/projects.go:148）。spec「nil = 老版本不支持」范式成立。
- 2026-08-24：wire 派发链实读：`ledgerstep.DispatchOpts.Discipline`（dispatch.go:27）→ agentd `stepTransport`（cardstep.go:102-121）→ `client.DispatchOpts.Discipline`（client.go:727）→ POST `/api/tasks` body `"discipline"`（client.go:757）→ 目标机 `dispatchRequest.Discipline`（server.go:1096）→ `manager.DispatchReq.Discipline`（server.go:1127）→ `resolveDisciplineFor`（manager.go:754）。正文解析确实发生在执行机、读执行机自己的盘。
- 2026-08-24：`internal/proto/proto.go` 实读：`:237-242` `Task.Discipline`（来源标注，不落盘，仅派发响应回显）；`:243-249` `Task.DisciplineName`（落盘，「不落盘的话一次 continue 或一次 agentd 重启就会让点名的任务静默退回兜底块」）。
- 2026-08-24：`internal/client/client.go` 实读：`:492` `Status(ctx) (*proto.StatusResp, error)` 已存在——协调者侧派发前探目标机能力位无需新增传输方法。
- 2026-08-24：`internal/agentd/cardstep.go` 实读：`:45-55` `startCardStep` 装配 `Dispatcher{St: s.ledger, ...}`——协调者侧 agentd 持库，缝 1 可直取；`:102` stepTransport 是环节派发的 Transport 生产实现。
- 2026-08-24：`internal/agentd/ledgerapi.go:658` 实读：卡节点下拉的角色名清单 = 五个 `discipline.Name*` 常量 + `discipline.List(discipline.Dir(...))` 磁盘文件；内置删除与目录退役后此清单改由账本 `ListDisciplineNames` 供（实现票事项，本票冻结其来源切换）。
- 2026-08-24：`internal/discipline/files.go` 实读：`:57 List(dir)`、`:84 Read(dir,name)` 是执行机纪律目录的读取面，随目录退役。
- 2026-08-24：对侧常量查执法：`charter-must-override` 全仓 grep（*.go/*.md/*.py/*.ts，除 superpowers 文档）零代码引用——它是工作流模板 v5 里的数据值，消费方式是「Resolver.ByName 对未知名字报错」（resolver.go:177）。重构后同一语义由缝 1「名字不在账本 → 拒发」承接，哨兵存活、非漂移。
- 2026-08-24：`go run . graph check --repo .` 实际输出 `"fails": []`——基线当前无违规。
- 2026-08-24：`go run . graph resolve --repo . --doc docs/superpowers/specs/2026-08-24-b229-discipline-layers-and-store.md` 实际列出 2 个符号锚（templates.go#PutTemplate=moved、manager.go#resolveDisciplineFor=ok），无坏锚，退出码 0。
- 2026-08-24：spec 备注「图覆盖债」消化：best.json 含 `k_discipline_Resolver/fn/model`、`k_ledgerstep_Dispatcher/StepRunner/fn/model`、`k_ledger_Store/model`，graph resolve 命中 `n_agentd_Manager_resolveDisciplineFor`——spec 点名的四组符号均已入图，无需补扫。
- 2026-08-24：`git merge-base HEAD origin/claude/config-sync-workflow-arch-fd96b7` = `10906bb1`，即当前 HEAD——本分支正坐在有效基线尖端。
- 2026-08-24：`ls web/node_modules` 报 `No such file or directory`——本轮 web 侧 tsc/vitest 无法执行，TS 镜像改动记「未验证」，沿 B185 台账同款处置留协调者。
- 2026-08-24：`internal/proto/contract_fixture_test.go` 实读：夹具生成器覆盖 `Task`（:66）、`StatusResp`（:86）、`MachinesResp`（:80）、`DisciplineResp`（:99）等 40 型；给 Task/StatusResp/Machine 加字段落骨架时必须 `-update` 刷新夹具并随提交 review 差异。
- 2026-08-24：Ticket 0 骨架首版落盘（缝1在 ledgerstep 调 discipline.Compose）后 `go build ./...` 退出 0；`go test ./internal/ledgerstep/ -run 'TestResolveDispatch|TestDisciplineStoreRoundTrip' -count=1` 经两轮断言修正（尾部标记是收口段前缀、平台层自带「只读」字样撞种子标记）后 ok。
- 2026-08-24：`go run . graph check --repo .` 在申报新边 d_ledger→d_policy 后实际退出码非零，原始 fails 共 5 条：`budget-raised`（新增契约携带存量预算 1，基准缺席按 0 处理）、`dead-contract`（d_ledger→d_policy 无活跃边）、`dead-entry`×3（entries「ledgerstep.ResolveDispatch」「discipline.Compose」按 checker 语义须为容器 Label，如「ledger.Store」）。原始输出已存会话。
- 2026-08-24：读 charter/graph v0.6.0 源码核实机制：check.go:77-90 dead-contract 只认视图内活跃 call/implements/组装点豁免边；fitness.go CheckBudgetRatchet 基准缺席契约预算按 0 比较、target.go:33 `legacyBudgetNote` 可降档 budget-raised；cli.go:64-78 视图 = baseline.json + diffs 叠加，不重扫源码；absorb 命令注释「分支合并回主线后执行」。结论：分支上冻结全新方向，仓库自有闸门 cmd/graph_gate_test.go（跑无视图基线）必红且无法在本分支生命周期转绿。
- 2026-08-24：实测确认 `cmd -run TestRepoContractGate` 在申报新边时 FAIL（原始输出：契约违规 [dead-contract] d_ledger→d_policy…），撤销新边后同测试 ok。基线扫描含 0 个 _test.go 节点——测试文件不进图，测试内跨包引用无图面后果。
- 2026-08-24：**归属订正落定**：缝 1 从 ledgerstep 迁回 internal/discipline（平台组装本体所在域），账本依赖以 `DisciplineLookup func(name)(int,string,error)` 注入，d_policy 不 import d_ledger；ledgerstep 生产代码不新增 import，ViaTemplate 消费缝 1 产物。删除 internal/ledgerstep/discipline.go 与其测试，新建 internal/discipline/dispatch.go + dispatch_test.go、internal/ledger/disciplines_test.go、internal/ledgerstep/discipline_passthrough_test.go（真实 SQLite 直通竖切）。先例：custom-launchers 契约 §6 订正（初稿声明的 d_localint→d_contract 被 Ticket 0 推翻）。
- 2026-08-24：target.json 最终形态：仅 d_cli→d_policy 与 d_gateway→d_policy 两条既有边 entries 追加「discipline（包级函数）」，legacyBudget 不动，零新增方向；`git diff --stat codegraph/target.json` = 两处 entries 各 +3 行。
- 2026-08-24：终验 `go build ./...` 退出 0（BUILD_OK）；`go vet ./...` 无输出退出 0；`gofmt -l .`（除 node_modules）无输出；`git diff --check` 无输出。
- 2026-08-24：终验 `go test ./internal/... -count=1` 全绿：44 个包 ok，0 FAIL。
- 2026-08-24：终验 `go test ./cmd/ . -count=1`：cmd ok（7.716s，含 TestRepoContractGate）、root ok。全仓 Go 测试无失败项。
- 2026-08-24：终验 `go test ./internal/proto/ -run TestContractFixtures -count=1` ok——刷新后的 Task/StatusResp/MachinesResp 夹具逐字节比对通过，wire 键名冻结生效（disciplines_supported / discipline_name / discipline_version）。
- 2026-08-24：终验缝测试原文：TestResolveDispatchTriState（nil/false 拒发、true 放行三态表）、TestResolveDispatchAssembly（平台头→角色→收口尾顺序 + 未点名纯平台层 + RawText Version=0 + platform off）、TestResolveDispatchRefusal（未知名透传拒发、Name+RawText 同填报错、带空白名字报错、ErrUnsupportedTarget 可辨）、TestResolveDispatchPassthrough（真实 SQLite 两缝穿透）全部 PASS；TestDisciplineStoreRoundTrip（v1/v2 只增不改、Get(0) 最新、ErrNotFound、非法名/空正文/超 64KiB 拒绝）PASS。
- 2026-08-24：终验图闸门：`graph check --repo .` fails=[]；`graph check --repo . --view cards-B229-charter` fails=[]；`graph resolve --doc docs/superpowers/specs/b229-contract.md` 13 锚全部 ok/moved、坏锚 0。
- 2026-08-24：TS 侧验证缺口维持登记：web/node_modules 缺失，tsc/vitest 本轮无法执行，types.ts 三处镜像（Task.discipline_name/discipline_version 补齐、StatusResp.disciplines_supported、Machine.disciplines_supported）与 vitest fixture 断言留协调者执行。
