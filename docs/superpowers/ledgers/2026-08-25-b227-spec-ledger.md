# B227 spec 节点台账（2026-08-25）

协调者本地会话。本文件记原始读数与实验过程；结论与裁决在
`docs/superpowers/specs/2026-08-25-b227-codex-sandbox-git-metadata.md` 正文。

---

## 1. 事件流取证：三级影响的归因被推翻

**目的**：卡上第四条 comment 断言「fetch 连提权都救不回来」。查证它成立与否。

**命令与读数**（task a1b0469b = B249 的 plan 轮，linux-01）：

```
handoff show a1b0469b-618d-4b73-9ed8-a7df037e1688 --target linux-01
→ recent_events 68 条，seq 13986-14195
→ Counter({'progress': 50, 'approver_decision': 8, 'ticket_answered': 8,
           'completed': 1, 'archived': 1})
```

8 张 approver_decision 全部 `"decision": "approve"`，`reason` 空，elapsed_ms 5934~6524。
命令原文（逐字）：

```
exec-a18082eb  /bin/bash -lc 'git add docs/superpowers/plans/b249-plan.md docs/ledgers/2026-08-25-b249-ledger.md'
exec-750b9722  /bin/bash -lc 'git add ... \n git diff --cached --check \n git diff --cached --stat ...'
exec-34dcf596  /bin/bash -lc 'git add ... \n git diff --cached --check \n git status --short --branch'
exec-401ddf3d  /bin/bash -lc 'git add ... \n git diff --cached --check \n git status --short --branch'
exec-51725a2b  /bin/bash -lc 'git commit -m "docs(b249): add implementation plan"'
exec-f41357bb  /bin/bash -lc 'git add ... && git commit -m "docs(b249): record plan validation"'
exec-56af2586  /bin/bash -lc 'git add ... && git commit -m "docs(b249): finalize ledger"'
exec-b389ce64  /bin/bash -lc 'git add ... && git commit -m "docs(b249): record final status"'
```

**判据**：`git fetch` **没有出现在任何一张工单里**。

**结论（推翻原断言）**：不是「提权对 fetch 无效」，是执行者当轮**选择了不提权**。
`git add` / `git commit` 的提权路径 8/8 通畅。真正的性质是**行为不可预测**：
B185 卡死 2h、B248 自己提权重试成功、B249 放弃并交回——同一类失败三种反应。

---

## 2. hooks 逃逸链：本机实测（含对照组）

**目的**：验证「A′ 开放 .git 可写域后，codex 可经 .git/hooks 提权到宿主」是否成立，
以及 `-c core.hooksPath=<空目录>` 能否堵住。

**靶子**：`/tmp/b227chk` 一次性 git 仓库（git 2.50.1 Apple Git-155, macOS）。
在主仓 `.git/hooks/post-checkout` 放一个把 pid/cwd 写进证据文件的脚本。

```
实验 a  git -C repo worktree add -q $D/wt1 -b probe1
        → HOOK_RAN pid=57798 cwd=/private/tmp/b227chk/wt1

实验 b  git -C repo -c core.hooksPath=$D/nohooks worktree add -q $D/wt2 -b probe2
        → (hook 未运行)

实验 c  先 git -C repo config core.hooksPath "$D/repo/.git/hooks"（模拟 codex 篡改 config）
        再 git -C repo -c core.hooksPath=$D/nohooks worktree add -q $D/wt3 -b probe3
        → (hook 未运行)  命令行 -c 压过 .git/config

对照组  git -C repo worktree add -q $D/wt4 -b probe4   （不加 -c，config 仍指向 hooks）
        → HOOK_RAN pid=57817 cwd=/private/tmp/b227chk/wt4
```

**对照组是本实验的判别力来源**：它排除了「hook 本来就不跑」的假绿。四项全部符合预期。

**结论**：
1. `git worktree add` 触发共享 `.git/hooks/post-checkout`。该命令由 agentd 自己执行
   （`internal/agentd/workspace.go` 第 3 层的 `gitRun`），**跑在 agentd 进程里、不在任何沙箱内**。
2. 逃逸链成立：沙箱内 codex 写 `.git/hooks/post-checkout` → 下次任何人对同仓派发
   `--new-worktree` → agentd 以自身身份无沙箱执行该脚本。
3. `-c core.hooksPath=<空目录>` 有效，且**压得过被篡改的 `.git/config`**。
4. 该链**今天已经存在**：原地模式下 cwd 即仓库根，`.git/hooks` 本就在可写域内。
   本卡只是把 worktree 模式拉平过去，因此加固独立于本卡即应成立。

---

## 3. codex 二进制策略面：strings 取证

**靶子**：`/opt/homebrew/lib/node_modules/@openai/codex/node_modules/@openai/codex-darwin-arm64/vendor/aarch64-apple-darwin/bin/codex`，`codex-cli 0.147.0`。

**读数（逐字摘录）**：

```
use_default | with_additional_permissions | require_escalated

"Per-command sandbox override. Defaults to `use_default`;
 use `require_escalated` for unsandboxed execution."

"Sandboxed filesystem or network access for this command;
 only with `sandbox_permissions: \"with_additional_permissions\"`."

"additional permissions are disabled; enable `features.exec_permission_approvals`
 before using `with_additional_permissions`"

`permissions` cannot be combined with `sandboxPolicy`
`permissionProfile` cannot be combined with `sandboxPolicy`
`permissions.filesystem` is reserved for requirements-level filesystem constraints
 and cannot define a profile
```

**三条结论**：

1. **`require_escalated` = 整条命令完全脱离沙箱**，不是「把某路径加进可写域」。
   因此「permgate 白名单放行 git 写」（C / B249 第 2 条）放行的是**无沙箱执行**，
   其授权面**严格宽于** A′ 要开的 `.git` 可写。安全序是 **A′ > C**，与直觉相反。
2. **存在第三档 `with_additional_permissions`**：沙箱内追加权限、不脱沙箱，
   但需 `features.exec_permission_approvals`。今天 handoff 未使用该档。
3. **`permissions` 面与 `sandboxPolicy` 互斥**，且 `permissions.filesystem` 另有约束
   （不能在此定义 profile）。方案 D 的落地成本因此高于「换个字段名」。

**未查证**：执行机 linux-01 上的 codex 版本（本轮未上执行机）。方案 D 的可行性完全压在这条上，
故 D 只作 roadmap 条目、不进本期。

---

## 4. 子系统归属：最优树查询

架构法定义子系统 = 最优树顶层领域，归属按 `containers` 表指名、不按文件路径推。

```
codegraph sym sandboxPolicy    → container k_codex_fn
codegraph sym gitExec          → container k_agentd_fn
codegraph sym GitTurnStatus    → container k_turn_fn

best.json containers:
  k_codex_fn  → d_execution_adapters → 顶层 d_execution     (任务执行)
  k_turn_fn   → d_execution_contract → 顶层 d_execution     (任务执行)
  k_agentd_fn → d_orchestration      → 顶层 d_orchestration (任务编排)
```

**注意**：`codegraph sym` 返回的 `domain` 字段是 **baseline 视图**（`"view":"baseline"`），
定级必须用 best.json 的 containers 表二次解析，不能直接采信 sym 的 domain。
（baseline 里 gitExec 归 `d_coordination_task`、sandboxPolicy 归 `d_executor`，
与最优树的顶层划分不是一套词表。）

---

## 5. 子 agent 独立探索的关键读数（来源：子 agent 报告，本会话未逐条复现）

**已由本会话独立复现的**：§2 的 hooks 链与 hooksPath 加固、§3 的 `require_escalated`
语义与 `permissions` 互斥（两者本台账均有自跑读数）。

**未复现、按子 agent 报告采信的**（标注为二手）：

- **symlink 骗不过 seatbelt**：profile 只开工作树，经工作树内 symlink 写 common dir
  仍 `Operation not permitted` —— 沙箱按解析后的目标端判。据此排除一切
  「把元数据 symlink/bind mount 进可写根」的构造。（macOS 实测；Linux Landlock 同向为**推断**。）
- **窄 allowlist 实测判死**：只开 `objects`+`refs`+`logs`+`worktrees` 四棵子树时，
  `git branch -D` / `git tag` / `git gc` / `git rebase` **直接失败（rc≠0）**；
  `git add`+`commit` / `stash` / `checkout -b` rc=0 但 stderr 带
  `packed-refs.lock: Operation not permitted` 等拒绝噪声。
  → `sandboxPolicy` 面上做不出精细化；带噪声的 rc=0 更毒（模型可能据 stderr 误判失败）。
- **git 元数据落点分布**（`git rev-parse --git-path` 实测）：
  `index`/`HEAD`/`FETCH_HEAD`/`logs/HEAD` 在 `.git/worktrees/<id>/`（每树私有）；
  `objects`/`refs/heads/*`/`logs/refs/*`/`packed-refs`/`config` 在 `.git/`（common，共享）。
  → 只开 `.git/worktrees/<id8>/` 必然不够，`git add` 就要写 objects。
- **B（clone --shared）的依赖点清单**：`cmd/pull.go:70` 与 `internal/agentd/bundle.go:32`
  的注释明写「用 RepoPath 而不是 Workdir()：分支对象在主仓库里」；
  `internal/agentd/reclaim.go:296` 走 `git worktree remove`、`:307` 兜底 `prune`；
  `internal/agentd/workspaceprobe.go:57` 靠 `git worktree list --porcelain` 列任务工作区；
  `internal/agentd/gitroot.go:42` 用 `--git-common-dir` 归并 location（B62 主键约束）。

**未采信、需下游复核的**：子 agent 称「B249 数据蒸发 98.4%」是拿 684/695 直接算的，
未独立复核那三天取证。本 spec 只用其定性结论（大头在 codex），不引用该百分比作判据。

---

## 6. 本轮放弃的尝试

- **想直接查执行机 codex 版本**：`handoff run` 需要一个活任务，而 linux-01 上唯一在跑的
  是 B249 的 implement 轮（task 34461480，17:27 派出），借用会干扰他人回合。放弃，
  记为 roadmap 条目 D 的前置未知项。

---

## 7. 本节点现状核对

- 当前执行分支为 `cards/B227-charter`；工作区初始 `git status --short --branch` 仅报告
  `## cards/B227-charter`，无未提交改动。
- 仓内存在 `codegraph/best.json`、`codegraph/baseline.json` 与领域文件；本机执行
  `codegraph --help` 的原始输出为 `/bin/bash: line 1: codegraph: command not found`。
  因此本节点按 best.json 的容器表核对领域，并把「未能运行 context/sym/chain」记为图覆盖债。
- best.json 将 `k_codex_fn` 归入 `d_execution_adapters`（顶层 `d_execution`），将
  `k_agentd_fn` 归入 `d_orchestration`；baseline.json 记录现状签名：
  `sandboxPolicy(taskTmp string) map[string]any`、
  `newRunState(taskID, taskDir, repoPath string) *runState`、
  `gitExec(ctx context.Context, repo string, quiet bool, args ...string) (stdout, stderr string, err error)`、
  `PrepareWorkspace(ctx context.Context, req WorkspaceReq) (Workspace, error)`。

## 8. 基线判据第一次实跑

- 命令：`go test ./internal/executor/turn ./internal/executor/codex ./internal/agentd`
- 原始输出（分三次读取同一进程）：

  ```text
  go: downloading github.com/Xsxdot/charter/graph v0.8.0
  go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.8.0.mod947282035.tmp: read-only file system
  # github.com/Xsxdot/handoff/internal/agentd
  internal/agentd/codegraph.go:22:2: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.8.0.lock: read-only file system
  # github.com/Xsxdot/handoff/internal/agentd
  internal/agentd/server.go:40:2: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.8.0.lock: read-only file system
  FAIL	github.com/Xsxdot/handoff/internal/agentd [setup failed]
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.035s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	5.922s
  FAIL
  ```

- 为取得可运行的 agentd 基线，命令改为：
  `GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex ./internal/agentd`
- 关键原始输出（依赖下载、编译期间分批读取）：

  ```text
  go: downloading github.com/coder/websocket v1.8.15
  go: downloading github.com/Xsxdot/charter/graph v0.8.0
  go: downloading golang.org/x/sys v0.47.0
  go: downloading github.com/google/uuid v1.6.0
  go: downloading gopkg.in/yaml.v3 v3.0.1
  go: downloading github.com/jackc/pgx/v5 v5.10.0
  go: downloading modernc.org/sqlite v1.56.0
  go: downloading github.com/flynn/noise v1.1.0
  go: downloading github.com/hashicorp/yamux v0.1.2
  go: downloading golang.org/x/crypto v0.55.0
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	(cached)
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	5.926s
  ok  	github.com/Xsxdot/handoff/internal/agentd	124.371s
  ```

## 9. 计划锁缝的定向基线

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitTurnStatusDetectsNewCommit|TestSandboxPolicyGrantsTaskTmp|TestTmpEnvPointsGoToolchainAtTaskTmp' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.019s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.003s
  ```

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/agentd -run 'TestPrepareWorkspaceNewWorktree$|TestPrepareWorkspaceNewWorktreeAllowsDirtyMainRepo$' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/agentd	0.148s
  ```

## 10. 本节点计划产出与自审

- 已创建法定产出物 `docs/superpowers/plans/b227-plan.md`。计划覆盖两条声明缝：
  `internal/executor/codex/adapter.go#sandboxPolicy` 与
  `internal/agentd/workspace.go#gitExec`；并将 Linux Landlock 真机验证明确标为协调者执行，
  未将其降级为机内断言。
- 计划自审命令：`awk '/^```/{n++} END{print n}' docs/superpowers/plans/b227-plan.md`，
  原始输出：`52`（代码围栏成对）；`git diff --check` 无输出、退出码 0。
- 计划占位符扫描命令：`rg -n 'TBD|TODO|同 Task|适当的错误处理|若意外先绿' docs/superpowers/plans/b227-plan.md`，
  原始输出为空、退出码 1；扫描命令文本本身已从计划正文移除，并在计划末尾声明了
  `appserver_test.go` 复用既有 harness 的唯一例外。

## 11. 收口时的 linked-worktree git 元数据阻断

- 命令：`git add docs/superpowers/plans/b227-plan.md docs/superpowers/ledgers/2026-08-25-b227-spec-ledger.md`
- 原始输出：

  ```text
  fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/02772ff3/index.lock': Read-only file system
  ```

- 该失败与本卡问题陈述一致：当前执行器工作树的 index.lock 位于主仓公共 git 目录，
  不在当前沙箱可写域。本节点尚未把它写成已提交事实；后续只按平台授权的必要收口
  流程重试 `git add`/`git commit`。

## 12. B227 implement 节点基线复跑

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitTurnStatusDetectsNewCommit|TestSandboxPolicyGrantsTaskTmp|TestTmpEnvPointsGoToolchainAtTaskTmp' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.028s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.005s
  ```

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/agentd -run 'TestPrepareWorkspaceNewWorktree$|TestPrepareWorkspaceNewWorktreeAllowsDirtyMainRepo$' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/agentd	0.098s
  ```

## 13. T1 声明缝红测

- 先只加入 T1/T2 测试并执行 `gofmt`，未改生产代码。
- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitCommonDir|TestNewRunStateCachesGitCommonDirAndSkipsNonGit|TestSandboxPolicyGrantsTaskTmpAndGitCommonDir|TestSandboxPolicySurvivesClientJSONSerialization' -count=1`
- 原始输出：

  ```text
  # github.com/Xsxdot/handoff/internal/executor/turn_test [github.com/Xsxdot/handoff/internal/executor/turn.test]
  internal/executor/turn/gitprobe_test.go:68:19: undefined: turn.GitCommonDir
  internal/executor/turn/gitprobe_test.go:82:18: undefined: turn.GitCommonDir
  internal/executor/turn/gitprobe_test.go:92:22: undefined: turn.GitCommonDir
  FAIL	github.com/Xsxdot/handoff/internal/executor/turn [build failed]
  # github.com/Xsxdot/handoff/internal/executor/codex [github.com/Xsxdot/handoff/internal/executor/codex.test]
  internal/executor/codex/adapter_sandbox_test.go:20:7: r.gitCommonDir undefined (type *runState has no field or method gitCommonDir)
  internal/executor/codex/adapter_sandbox_test.go:21:55: r.gitCommonDir undefined (type *runState has no field or method gitCommonDir)
  internal/executor/codex/adapter_sandbox_test.go:26:7: r.gitCommonDir undefined (type *runState has no field or method gitCommonDir)
  internal/executor/codex/adapter_sandbox_test.go:27:68: r.gitCommonDir undefined (type *runState has no field or method gitCommonDir)
  internal/executor/codex/export_test.go:40:35: too many arguments in call to sandboxPolicy
  have (string, string)
  want (string)
  FAIL	github.com/Xsxdot/handoff/internal/executor/codex [build failed]
  FAIL
  ```

- 判断：红因是计划要求的新接缝符号/签名缺失，不是测试拼写错误；进入最小生产实现。

## 14. T1 最小实现与接缝绿灯

- 实现：新增 `turn.GitCommonDir`；`runState` 在唯一 `newRunState` 构造点缓存 common dir；`sandboxPolicy` 按任务临时目录、common dir 顺序投影，空 common dir 不占位；`startTurn` 使用缓存值。
- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitCommonDir|TestNewRunStateCachesGitCommonDirAndSkipsNonGit|TestSandboxPolicyGrantsTaskTmpAndGitCommonDir|TestSandboxPolicySurvivesClientJSONSerialization' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.026s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.023s
  ```

## 15. T2 hooks 对照红测

- 先加入 `TestPrepareWorkspaceDoesNotRunRepositoryHook`，未改 `gitExec` 生产实现；对照组先确认原 hook 可运行。
- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/agentd -run TestPrepareWorkspaceDoesNotRunRepositoryHook -count=1`
- 原始输出：

  ```text
  --- FAIL: TestPrepareWorkspaceDoesNotRunRepositoryHook (0.06s)
      workspace_test.go:655: target worktree 不得运行恶意 hook，stat err=<nil>
  FAIL
  FAIL	github.com/Xsxdot/handoff/internal/agentd	0.067s
  ```

- 判断：对照组已创建 marker，目标断言因现有 `gitExec` 未覆盖 hooks 而失败；进入 T2 最小实现。

## 16. T2 最小实现与 hooks 绿灯

- 实现：`gitExec` 每次调用通过 `os.MkdirTemp` 创建独立空目录，在 `git -C <repo> -c core.hooksPath=<dir> <args...>` 中覆盖仓库配置，并在返回前回收目录；回收失败只 Warn，不覆盖 Git 返回值。
- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/agentd -run TestPrepareWorkspaceDoesNotRunRepositoryHook -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/agentd	0.079s
  ```

## 17. T1 task 收尾测试

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.039s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	6.116s
  ```

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitCommonDir|TestNewRunStateCachesGitCommonDirAndSkipsNonGit|TestSandboxPolicyGrantsTaskTmpAndGitCommonDir|TestSandboxPolicySurvivesClientJSONSerialization|TestTmpEnvPointsGoToolchainAtTaskTmp' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.017s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.019s
  ```

## 18. T2 task 收尾测试

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/agentd`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/agentd	127.793s
  ```

## 19. 策略根变异自验

- 变异前唯一命中计数：`rg -F -o 'if root != ""' internal/executor/codex/adapter.go | wc -l`，原始输出 `1`。
- 将唯一判定临时改为 `if root == ""`，未保留该变异。
- 编译阶段命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/codex -run '^$' -count=1`。
- 编译阶段原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.003s [no tests to run]
  ```

- 行为断言命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/codex -run TestSandboxPolicyGrantsTaskTmpAndGitCommonDir -count=1`。
- 行为断言原始输出：

  ```text
  --- FAIL: TestSandboxPolicyGrantsTaskTmpAndGitCommonDir (0.00s)
      discipline_test.go:45: JSON writableRoots = []interface {}{}, want ["/root/.handoff/tmp/137a7dc9" "/srv/repos/handoff/.git"]
  FAIL
  FAIL	github.com/Xsxdot/handoff/internal/executor/codex	0.003s
  FAIL
  ```

- 判断：变异唯一命中、可编译且被行为断言拦截；已恢复原实现。

## 28. B227 真机修复轮：私有 git 目录红测

- 测试先扩展为三根可写域，并新增 linked worktree 改名后读取私有目录、私有目录为空时不追加空串的断言；未改生产代码。
- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitDir|TestNewRunStateCachesGitCommonDirAndSkipsNonGit|TestSandboxPolicyGrantsTaskTmpAndGitCommonDir|TestSandboxPolicySurvivesClientJSONSerialization' -count=1`
- 原始输出：

  ```text
  # github.com/Xsxdot/handoff/internal/executor/turn_test [github.com/Xsxdot/handoff/internal/executor/turn.test]
  internal/executor/turn/gitprobe_test.go:135:19: undefined: turn.GitDir
  internal/executor/turn/gitprobe_test.go:143:18: undefined: turn.GitDir
  internal/executor/turn/gitprobe_test.go:154:22: undefined: turn.GitDir
  FAIL	github.com/Xsxdot/handoff/internal/executor/turn [build failed]
  # github.com/Xsxdot/handoff/internal/executor/codex [github.com/Xsxdot/handoff/internal/executor/codex.test]
  internal/executor/codex/adapter_sandbox_test.go:29:7: r.gitDir undefined (type *runState has no field or method gitDir)
  internal/executor/codex/adapter_sandbox_test.go:30:79: r.gitDir undefined (type *runState has no field or method gitDir)
  internal/executor/codex/export_test.go:40:49: too many arguments in call to sandboxPolicy
  have (string, string, string)
  want (string, string)
  FAIL	github.com/Xsxdot/handoff/internal/executor/codex [build failed]
  FAIL
  ```

- 判断：红因是新私有目录符号、运行态字段与三参策略签名缺失，不是测试拼写错误；下一步先落空壳并复跑断言红。

## 29. B227 真机修复轮：空壳后的断言红

- 先加入 `GitDir`/`gitDir`/三参策略的空壳与调用接线，未实现 git 查询或第三根投影。
- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitDir|TestNewRunStateCachesGitCommonDirAndSkipsNonGit|TestSandboxPolicyGrantsTaskTmpAndGitCommonDir|TestSandboxPolicySurvivesClientJSONSerialization' -count=1`
- 原始输出：

  ```text
  --- FAIL: TestGitDirUsesGitReportedPrivateDir (0.02s)
      gitprobe_test.go:140: linked worktree git-dir = "."，want "/root/.handoff/tmp/3b337882/TestGitDirUsesGitReportedPrivateDir2610242977/001/.git/worktrees/created-worktree"
  --- FAIL: TestGitDirRejectsNonGitPath (0.00s)
      gitprobe_test.go:155: 非 git 目录应返回空路径与错误，got path="" err=<nil>
  FAIL	github.com/Xsxdot/handoff/internal/executor/turn	0.025s
  2026/08/25 21:25:00 INFO git 公共目录已准备为沙箱可写根 task=task-git repo=/root/.handoff/tmp/3b337882/TestNewRunStateCachesGitCommonDirAndSkipsNonGit3437585700/001 common_dir=/root/.handoff/tmp/3b337882/TestNewRunStateCachesGitCommonDirAndSkipsNonGit3437585700/001/.git
  2026/08/25 21:25:00 INFO 帧写入器就绪 path=/root/.handoff/tmp/3b337882/TestNewRunStateCachesGitCommonDirAndSkipsNonGit3437585700/002/frames.jsonl resume_seq=0 resume_turn=0
  2026/08/25 21:25:00 INFO 帧写入器就绪 path=/root/.handoff/tmp/3b337882/TestNewRunStateCachesGitCommonDirAndSkipsNonGit3437585700/004/frames.jsonl resume_seq=0 resume_turn=0
  --- FAIL: TestSandboxPolicySurvivesClientJSONSerialization (5.01s)
      appserver_test.go:226: Call turn/start: context deadline exceeded
  --- FAIL: TestSandboxPolicyGrantsTaskTmpAndGitCommonDir (0.00s)
      discipline_test.go:46: JSON writableRoots = []interface {}{"/root/.handoff/tmp/137a7dc9", "/srv/repos/handoff/.git"}，want ["/root/.handoff/tmp/137a7dc9" "/srv/repos/handoff/.git" "/srv/repos/handoff/.git/worktrees/task-id"]
  FAIL	github.com/Xsxdot/handoff/internal/executor/codex	5.024s
  ```

- 判断：私有目录查询与三根策略投影的断言均已真实失败；JSON-RPC 超时是服务端未收到满足断言的报文后的测试表现，进入最小实现。

## 30. B227 真机修复轮：私有目录最小实现绿灯

- 实现：`GitDir` 通过 `git -C <repo> rev-parse --absolute-git-dir` 取证并做绝对化/Clean；`newRunState` 缓存该结果，失败只 Debug 并置空；`sandboxPolicy` 按 task tmp、common dir、private git dir 顺序投影并去重原地模式的重复根。
- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitDir|TestNewRunStateCachesGitCommonDirAndSkipsNonGit|TestSandboxPolicyGrantsTaskTmpAndGitCommonDir|TestSandboxPolicySurvivesClientJSONSerialization' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.022s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.020s
  ```

- 判断：私有目录查询、三根顺序、JSON-RPC 序列化及空私有目录跳过断言均通过；hooks 加固文件未改动。

## 31. B227 真机修复轮：T1 包级收尾

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.053s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	6.117s
  ```

- 判断：T1 触及的两个 Go 包全量测试通过。

## 32. B227 真机修复轮：策略根变异自验

- 变异前唯一命中计数：`rg -F -o 'if root != ""' internal/executor/codex/adapter.go | wc -l`，原始输出 `1`。
- 将唯一判定临时改为 `if root == ""`。
- 编译阶段命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/codex -run '^$' -count=1`。
- 编译阶段原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.002s [no tests to run]
  ```

- 行为断言命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/codex -run TestSandboxPolicyGrantsTaskTmpAndGitCommonDir -count=1`。
- 行为断言原始输出：

  ```text
  --- FAIL: TestSandboxPolicyGrantsTaskTmpAndGitCommonDir (0.00s)
      discipline_test.go:46: JSON writableRoots = []interface {}{}，want ["/root/.handoff/tmp/137a7dc9" "/srv/repos/handoff/.git" "/srv/repos/handoff/.git/worktrees/task-id"]
  FAIL
  FAIL	github.com/Xsxdot/handoff/internal/executor/codex	0.002s
  FAIL
  ```

- 判断：变异唯一命中、编译通过且行为断言拦截；原实现已恢复。

## 33. B227 真机修复轮：T2 包级收尾

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/agentd`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/agentd	148.694s
  ```

- 判断：hooks 加固所在 agentd 包全量测试通过。

## 34. B227 真机修复轮：全仓编译首次环境失败

- 命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./... -run '^$' -count=1`
- 原始报错：

  ```text
  go: writing go.mod cache: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/github.com/charmbracelet: read-only file system
  go: writing go.mod cache: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/charm.land: read-only file system
  FAIL	github.com/Xsxdot/handoff [setup failed]
  FAIL	github.com/Xsxdot/handoff/cmd [setup failed]
  FAIL	github.com/Xsxdot/handoff/internal/ptyhost [setup failed]
  FAIL	github.com/Xsxdot/handoff/internal/ptyhost/engine [setup failed]
  FAIL	github.com/Xsxdot/handoff/internal/ptyhost/hostproc [setup failed]
  ```

- 原始输出同时显示已缓存包（含 `internal/agentd`、`internal/executor/codex`、`internal/executor/turn`）为 `[no tests to run]`，但全仓命令整体退出 1；该命令未作为编译通过依据。

## 35. B227 真机修复轮：全仓编译通过

- 命令：`GOMODCACHE=/root/.handoff/tmp/3b337882/gomodcache go test ./... -run '^$' -count=1`
- 原始输出：

  ```text
  go: downloading github.com/google/uuid v1.6.0
  go: downloading github.com/coder/websocket v1.8.15
  go: downloading gopkg.in/yaml.v3 v3.0.1
  go: downloading charm.land/huh/v2 v2.0.3
  go: downloading github.com/Xsxdot/charter/graph v0.8.0
  go: downloading github.com/mattn/go-isatty v0.0.24
  go: downloading github.com/jackc/pgx/v5 v5.10.0
  go: downloading modernc.org/sqlite v1.56.0
  go: downloading github.com/spf13/cobra v1.10.2
  go: downloading golang.org/x/sys v0.47.0
  go: downloading github.com/creack/pty v1.1.24
  go: downloading github.com/charmbracelet/x/ansi v0.11.6
  go: downloading github.com/flynn/noise v1.1.0
  go: downloading github.com/spf13/pflag v1.0.9
  go: downloading github.com/hashicorp/yamux v0.1.0
  go: downloading golang.org/x/crypto v0.55.0
  go: downloading charm.land/bubbles/v2 v2.0.0
  go: downloading charm.land/bubbletea/v2 v2.0.2
  go: downloading charm.land/lipgloss/v2 v2.0.1
  go: downloading github.com/catppuccin/go v0.3.0
  go: downloading github.com/charmbracelet/x/exp/ordered v0.1.0
  go: downloading github.com/charmbracelet/x/exp/strings v0.0.0-20240722160745-212f7b056ed0
  go: downloading github.com/mitchellh/hashstructure/v2 v2.0.2
  go: downloading github.com/clipperhouse/displaywidth v0.11.0
  go: downloading github.com/clipperhouse/uax29/v2 v2.7.0
  go: downloading github.com/lucasb-eyer/go-colorful v1.3.0
  go: downloading github.com/mattn/go-runewidth v0.0.20
  go: downloading github.com/dustin/go-humanize v1.0.1
  go: downloading github.com/charmbracelet/x/term v0.2.2
  go: downloading github.com/atotto/clipboard v0.1.4
  go: downloading github.com/rivo/uniseg v0.4.7
  go: downloading github.com/jackc/pgpassfile v1.0.0
  go: downloading github.com/jackc/pgservicefile v0.0.0-20240606120523-5a60cdf6a761
  go: downloading golang.org/x/text v0.41.0
  go: downloading github.com/jackc/puddle/v2 v2.2.2
  go: downloading github.com/charmbracelet/colorprofile v0.4.2
  go: downloading github.com/muesli/cancelreader v0.2.2
  go: downloading github.com/charmbracelet/ultraviolet v0.0.0-20260205113103-524a6607adb8
  go: downloading golang.org/x/sync v0.22.0
  go: downloading github.com/xo/terminfo v0.0.0-20220910002029-abceb7e1c41e
  go: downloading github.com/charmbracelet/x/termios v0.1.1
  go: downloading github.com/charmbracelet/x/windows v0.2.2
  go: downloading modernc.org/libc v1.74.4
  go: downloading modernc.org/memory v1.11.0
  go: downloading modernc.org/mathutil v1.7.1
  go: downloading github.com/remyoudompheng/bigfft v0.0.0-20230129092748-24d4a6f8daec
  ok  	github.com/Xsxdot/handoff	0.018s [no tests to run]
  ok  	github.com/Xsxdot/handoff/cmd	0.005s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/agentd	0.007s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/buildinfo	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/client	0.007s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/config	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/discipline	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/envfile	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/executor	0.005s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/executor/claudecode	0.006s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.005s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/executor/fake	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/executor/grok	0.005s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/executor/opencode	0.005s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/executor/rawtap	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.007s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/initflow	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/launcher	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ledger	0.006s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ledgermirror	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.008s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/localsync	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/logx	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/pathenv	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/permgate	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/prochost	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/projectid	0.005s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/proto	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/proxycfg	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ptyhost	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/engine	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/hostproc	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/sessdir	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/wire	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/ptytestroot	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/relay	0.005s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/release	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/selfupdate	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/service	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/skill	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/store	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/targetclient	0.003s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/testperm	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/toolchain	0.002s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/upgrade	0.004s [no tests to run]
  ok  	github.com/Xsxdot/handoff/internal/webui	0.002s [no tests to run]
  ```

- 判断：使用当前任务允许的可写缓存根后，全仓 Go 编译检查通过；命令未运行测试正文。

## 36. B227 真机修复轮：全仓集成测试通过

- 命令：`GOMODCACHE=/root/.handoff/tmp/3b337882/gomodcache go test ./...`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff	0.485s
  ok  	github.com/Xsxdot/handoff/cmd	12.669s
  ok  	github.com/Xsxdot/handoff/internal/agentd	133.419s
  ok  	github.com/Xsxdot/handoff/internal/buildinfo	0.002s
  ok  	github.com/Xsxdot/handoff/internal/client	9.351s
  ok  	github.com/Xsxdot/handoff/internal/config	0.022s
  ok  	github.com/Xsxdot/handoff/internal/discipline	0.006s
  ok  	github.com/Xsxdot/handoff/internal/envfile	0.006s
  ok  	github.com/Xsxdot/handoff/internal/executor	0.006s
  ok  	github.com/Xsxdot/handoff/internal/executor/claudecode	4.066s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	5.991s
  ok  	github.com/Xsxdot/handoff/internal/executor/fake	0.003s
  ok  	github.com/Xsxdot/handoff/internal/executor/grok	1.364s
  ok  	github.com/Xsxdot/handoff/internal/executor/opencode	17.661s
  ok  	github.com/Xsxdot/handoff/internal/executor/rawtap	0.003s
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	(cached)
  ok  	github.com/Xsxdot/handoff/internal/initflow	0.004s
  ok  	github.com/Xsxdot/handoff/internal/launcher	0.002s
  ok  	github.com/Xsxdot/handoff/internal/ledger	16.964s
  ok  	github.com/Xsxdot/handoff/internal/ledgermirror	2.300s
  ok  	github.com/Xsxdot/handoff/internal/ledgerstep	9.073s
  ok  	github.com/Xsxdot/handoff/internal/localsync	0.213s
  ok  	github.com/Xsxdot/handoff/internal/logx	0.003s
  ok  	github.com/Xsxdot/handoff/internal/pathenv	0.005s
  ok  	github.com/Xsxdot/handoff/internal/permgate	0.014s
  ok  	github.com/Xsxdot/handoff/internal/prochost	21.285s
  ok  	github.com/Xsxdot/handoff/internal/projectid	0.002s
  ok  	github.com/Xsxdot/handoff/internal/proto	0.003s
  ok  	github.com/Xsxdot/handoff/internal/proxycfg	0.002s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost	0.033s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/engine	16.039s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/hostproc	0.019s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/sessdir	0.004s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/wire	0.003s
  ok  	github.com/Xsxdot/handoff/internal/ptytestroot	0.004s
  ok  	github.com/Xsxdot/handoff/internal/relay	0.008s
  ok  	github.com/Xsxdot/handoff/internal/release	0.080s
  ok  	github.com/Xsxdot/handoff/internal/selfupdate	0.003s
  ok  	github.com/Xsxdot/handoff/internal/service	0.004s
  ok  	github.com/Xsxdot/handoff/internal/skill	0.008s
  ok  	github.com/Xsxdot/handoff/internal/store	7.116s
  ok  	github.com/Xsxdot/handoff/internal/targetclient	0.158s
  ok  	github.com/Xsxdot/handoff/internal/testperm	0.003s
  ok  	github.com/Xsxdot/handoff/internal/toolchain	0.002s
  ok  	github.com/Xsxdot/handoff/internal/upgrade	0.004s
  ok  	github.com/Xsxdot/handoff/internal/webui	0.002s
  ```

- 判断：全仓集成测试退出码 0；Linux Codex 真机验收仍未在本节点执行。

## 37. B227 真机修复轮：计划修订记录

- 修改 `docs/superpowers/plans/b227-plan.md`，追加 0.4 真机验收修订，并同步 T1 接口表为 `GitDir`、三参 `sandboxPolicy` 与三参测试缝；未改变 hooks 加固和协调者真机验收范围。

## 38. B227 真机修复轮：提交前审计

- 命令：`gofmt -d internal/executor/turn/gitprobe.go internal/executor/turn/gitprobe_test.go internal/executor/codex/adapter.go internal/executor/codex/export_test.go internal/executor/codex/discipline_test.go internal/executor/codex/adapter_sandbox_test.go internal/executor/codex/appserver_test.go`；原始输出为空，退出码 0。
- 命令：`git diff --check`；原始输出为空，退出码 0。
- 命令：`rg -n 'TBD|TODO|同 Task|适当的错误处理|若意外先绿' docs/superpowers/plans/b227-plan.md`；原始输出为空，退出码 1（无匹配）。
- 命令：`git diff --name-only`；原始输出为：

  ```text
  docs/superpowers/ledgers/2026-08-25-b227-spec-ledger.md
  docs/superpowers/plans/b227-plan.md
  internal/executor/codex/adapter.go
  internal/executor/codex/adapter_sandbox_test.go
  internal/executor/codex/appserver_test.go
  internal/executor/codex/discipline_test.go
  internal/executor/codex/export_test.go
  internal/executor/turn/gitprobe.go
  internal/executor/turn/gitprobe_test.go
  ```

- 判断：源码格式、空白、计划占位符均通过；改动集中在 T1 文件、台账及按补充要求追加修订记录，`internal/agentd/workspace.go` 未改。

## 39. B227 真机修复轮：收口首次暂存阻断

- 命令：`git add docs/superpowers/ledgers/2026-08-25-b227-spec-ledger.md docs/superpowers/plans/b227-plan.md internal/executor/turn/gitprobe.go internal/executor/turn/gitprobe_test.go internal/executor/codex/adapter.go internal/executor/codex/export_test.go internal/executor/codex/discipline_test.go internal/executor/codex/adapter_sandbox_test.go internal/executor/codex/appserver_test.go`
- 原始报错：

  ```text
  fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/3b337882/index.lock': Read-only file system
  ```

- 判断：工作树文件可写，但 linked worktree 的 index.lock 仍落在主仓公共 git 元数据目录；按平台授权流程重试暂存与提交。

## 40. B227 真机修复轮：授权暂存与暂存审计

- 受控授权重试同一条 `git add` 命令，退出码 0，原始输出为空。
- 命令：`git status --short --branch`；原始输出：

  ```text
  ## cards/B227-charter-4
  M  docs/superpowers/ledgers/2026-08-25-b227-spec-ledger.md
  M  docs/superpowers/plans/b227-plan.md
  M  internal/executor/codex/adapter.go
  M  internal/executor/codex/adapter_sandbox_test.go
  M  internal/executor/codex/appserver_test.go
  M  internal/executor/codex/discipline_test.go
  M  internal/executor/codex/export_test.go
  M  internal/executor/turn/gitprobe.go
  M  internal/executor/turn/gitprobe_test.go
  ```

- 命令：`git diff --cached --check`；原始输出为空，退出码 0。
- 命令：`git diff --cached --stat`；原始输出：

  ```text
   .../ledgers/2026-08-25-b227-spec-ledger.md         | 319 +++++++++++++++++++++
   docs/superpowers/plans/b227-plan.md                |  28 +-
   internal/executor/codex/adapter.go                 |  38 ++-
   internal/executor/codex/adapter_sandbox_test.go    |   3 +
   internal/executor/codex/appserver_test.go          |   5 +-
   internal/executor/codex/discipline_test.go         |  13 +-
   internal/executor/codex/export_test.go             |   4 +-
   internal/executor/turn/gitprobe.go                 |  24 ++
   internal/executor/turn/gitprobe_test.go            |  47 +++
   9 files changed, 454 insertions(+), 27 deletions(-)
  ```

- 判断：暂存内容与预期文件集一致，暂存 diff 无空白错误。

## 41. B227 真机修复轮：提交

- 命令：`git commit -m "fix(b227): add linked worktree git dir to codex sandbox"`
- 原始输出：

  ```text
  [cards/B227-charter-4 e9973a0c] fix(b227): add linked worktree git dir to codex sandbox
   9 files changed, 490 insertions(+), 27 deletions(-)
  ```

- 判断：提交已落当前分支；追加本条台账后需 amend，使最终提交同时包含完整台账。

## 27. B227 review 修复：路径归一化红测

- 基线定向命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn ./internal/executor/codex -run 'TestGitTurnStatusDetectsNewCommit|TestGitCommonDirNormalizesMainAndLinkedWorktree|TestGitCommonDirRejectsNonGitPath|TestSandboxPolicyGrantsTaskTmp' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.040s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	0.004s
  ```

- 为复现 review 指出的 macOS `/var` 符号链接差异，测试暂时改为通过仓库符号链接调用
  `GitCommonDir`，但保留未归一化的 want/got 比较。
- 红测命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn -run '^TestGitCommonDirNormalizesMainAndLinkedWorktree$' -count=1`
- 原始输出：

  ```text
  --- FAIL: TestGitCommonDirNormalizesMainAndLinkedWorktree (0.02s)
      gitprobe_test.go:92: linked worktree common-dir = "/root/.handoff/tmp/cd51ecc1/TestGitCommonDirNormalizesMainAndLinkedWorktree3521898013/001/.git"，want "/root/.handoff/tmp/cd51ecc1/TestGitCommonDirNormalizesMainAndLinkedWorktree3521898013/002/repo-link/.git"
  FAIL
  FAIL	github.com/Xsxdot/handoff/internal/executor/turn	0.017s
  FAIL
  ```

- 判断：失败来自路径断言而非编译或拼写错误；实现未改动，下一步只在测试侧对 want 与 got
  做 `filepath.EvalSymlinks` 归一化。

## 28. B227 review 修复：归一化绿测与变异自验

- 修复：`gitprobe_test.go` 对 want 与 got 均执行 `filepath.Clean` + `filepath.EvalSymlinks`；
  `GitCommonDir` 生产实现未改动。
- 行为绿测命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn -run '^TestGitCommonDirNormalizesMainAndLinkedWorktree$' -count=1`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.020s
  ```

- 变异唯一命中检查：`rg -F -o 'return filepath.Clean(resolved)' internal/executor/turn/gitprobe_test.go | wc -l`，原始输出 `1`。
- 变异第一发（把返回值改为 `cleaned`）编译命令：
  `GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn -run '^$' -count=1`。
- 原始输出（该发不计数）：

  ```text
  # github.com/Xsxdot/handoff/internal/executor/turn_test [github.com/Xsxdot/handoff/internal/executor/turn.test]
  internal/executor/turn/gitprobe_test.go:99:2: declared and not used: resolved
  FAIL	github.com/Xsxdot/handoff/internal/executor/turn [build failed]
  FAIL
  ```

- 已将变异改为编译有效的语义变更（丢弃 `EvalSymlinks` 返回路径但保留错误检查）。编译
  命令原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.002s [no tests to run]
  ```

- 变异行为断言命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn -run '^TestGitCommonDirNormalizesMainAndLinkedWorktree$' -count=1`。
- 原始输出：

  ```text
  --- FAIL: TestGitCommonDirNormalizesMainAndLinkedWorktree (0.01s)
      gitprobe_test.go:92: linked worktree common-dir = "/root/.handoff/tmp/cd51ecc1/TestGitCommonDirNormalizesMainAndLinkedWorktree3311370653/001/.git"，want "/root/.handoff/tmp/cd51ecc1/TestGitCommonDirNormalizesMainAndLinkedWorktree3311370653/002/repo-link/.git"
  FAIL
  FAIL	github.com/Xsxdot/handoff/internal/executor/turn	0.015s
  FAIL
  ```

- 变异全包命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/executor/turn -count=1`。
- 原始输出：

  ```text
  --- FAIL: TestGitCommonDirNormalizesMainAndLinkedWorktree (0.01s)
      gitprobe_test.go:92: linked worktree common-dir = "/root/.handoff/tmp/cd51ecc1/TestGitCommonDirNormalizesMainAndLinkedWorktree3099771040/001/.git"，want "/root/.handoff/tmp/cd51ecc1/TestGitCommonDirNormalizesMainAndLinkedWorktree3099771040/002/repo-link/.git"
  FAIL
  FAIL	github.com/Xsxdot/handoff/internal/executor/turn	0.033s
  FAIL
  ```
- 失败计数命令输出：`1`。变异命中且被唯一相关断言拦截；已恢复归一化实现。

## 29. B227 review 修复：收尾验证

- 格式命令：`gofmt -d internal/executor/turn/gitprobe_test.go`；原始输出为空。
- 全仓编译首次命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go build ./...`。
- 原始错误输出：

  ```text
  go: writing go.mod cache: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/charm.land: read-only file system
  go: writing go.mod cache: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/github.com/charmbracelet: read-only file system
  go: writing go.mod cache: open /root/.handoff/tmp/02772ff3/gomodcache/cache/download/golang.org/x/crypto/@v/v0.0.0-20210322153248-0c34fe9e7dc2.mod962712471.tmp: read-only file system
  cmd/init_huh.go:20:2: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/charm.land: read-only file system
  ../../tmp/02772ff3/gomodcache/github.com/!xsxdot/charter/graph@v0.8.0/cli/cli.go:29:2: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/github.com/spf13: read-only file system
  internal/ptyhost/engine/platform_unix.go:23:2: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/github.com/creack: read-only file system
  cmd/attach.go:32:2: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/github.com/mattn: read-only file system
  ```

- 全仓编译重跑命令：`GOMODCACHE=/root/.handoff/tmp/cd51ecc1/gomodcache go build ./...`；退出码
  `0`，原始输出为依赖下载进度，无错误。
- 触及包全量测试命令：`GOMODCACHE=/root/.handoff/tmp/cd51ecc1/gomodcache go test ./internal/executor/turn ./internal/executor/codex -count=1`。
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	0.053s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	6.090s
  ```

- 计划要求的 agentd 全包测试命令：`GOMODCACHE=/root/.handoff/tmp/cd51ecc1/gomodcache go test ./internal/agentd -count=1`。
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/agentd	130.800s
  ```

## 30. B227 review 修复：提交前差异检查

- 命令：`git diff --check && git diff --name-only && git status --short --branch`
- 原始输出：

  ```text
  docs/superpowers/ledgers/2026-08-25-b227-spec-ledger.md
  docs/superpowers/plans/b227-plan.md
  internal/executor/turn/gitprobe_test.go
  ## cards/B227-charter-3
   M docs/superpowers/ledgers/2026-08-25-b227-spec-ledger.md
   M docs/superpowers/plans/b227-plan.md
   M internal/executor/turn/gitprobe_test.go
  ```

- 判断：差异检查无错误；仅包含 review 要求的测试、计划修订与台账，未触及生产实现。

## 31. B227 review 修复：提交

- 暂存命令首次在普通沙箱失败：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/cd51ecc1/index.lock': Read-only file system`。
- 获得本次 linked-worktree Git 元数据写权限后，暂存成功。
- 提交命令：`git commit -m "test(b227): normalize git common dir paths"`
- 原始输出：

  ```text
  [cards/B227-charter-3 5bb7867e] test(b227): normalize git common dir paths
   3 files changed, 180 insertions(+), 13 deletions(-)
  ```

- 后续将把本条台账记录 amend 进同一提交；不 push。

## 21. 全仓编译首次尝试未验证

- 先执行 `git diff --check`，退出码 0；随后命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go build ./...`。
- 原始失败输出（节录保持命令原文中的只读错误）：

  ```text
  go: writing go.mod cache: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/charm.land: read-only file system
  go: writing go.mod cache: open /root/.handoff/tmp/02772ff3/gomodcache/cache/download/golang.org/x/crypto/@v/v0.0.0-20210322153248-0c34fe9e7dc2.mod985865736.tmp: read-only file system
  cmd/init_huh.go:20:2: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/charm.land: read-only file system
  ../../tmp/02772ff3/gomodcache/github.com/!xsxdot/charter/graph@v0.8.0/cli/cli.go:29:2: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/github.com/spf13: read-only file system
  internal/ptyhost/engine/platform_unix.go:23:2: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/github.com/creack: read-only file system
  cmd/attach.go:32:2: mkdir /root/.handoff/tmp/02772ff3/gomodcache/cache/download/github.com/mattn: read-only file system
  ```

- 判断：本次全仓编译未验证；失败发生在该任务临时模块缓存写入，非业务断言结果。将按同样 `GOMODCACHE` 形态改用新建的任务临时缓存重跑。

## 22. 全仓编译重跑

- 命令：`mktemp -d /root/.handoff/tmp/c0f89ac3/b227-gomodcache.XXXXXX`。
- 原始输出：`/root/.handoff/tmp/c0f89ac3/b227-gomodcache.8pqU7A`。
- 命令：`GOMODCACHE=/root/.handoff/tmp/c0f89ac3/b227-gomodcache.8pqU7A go build ./...`。
- 原始输出：下载依赖进度后进程退出码 `0`，无编译错误输出。

## 23. 全量集成测试

- 命令：`GOMODCACHE=/root/.handoff/tmp/c0f89ac3/b227-gomodcache.8pqU7A go test ./...`
- 原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff	0.263s
  ok  	github.com/Xsxdot/handoff/cmd	12.426s
  ok  	github.com/Xsxdot/handoff/internal/agentd	128.544s
  ok  	github.com/Xsxdot/handoff/internal/buildinfo	0.002s
  ok  	github.com/Xsxdot/handoff/internal/client	9.338s
  ok  	github.com/Xsxdot/handoff/internal/config	0.017s
  ok  	github.com/Xsxdot/handoff/internal/discipline	0.003s
  ok  	github.com/Xsxdot/handoff/internal/envfile	0.009s
  ok  	github.com/Xsxdot/handoff/internal/executor	0.002s
  ok  	github.com/Xsxdot/handoff/internal/executor/claudecode	4.061s
  ok  	github.com/Xsxdot/handoff/internal/executor/codex	6.011s
  ok  	github.com/Xsxdot/handoff/internal/executor/fake	0.002s
  ok  	github.com/Xsxdot/handoff/internal/executor/grok	1.393s
  ok  	github.com/Xsxdot/handoff/internal/executor/opencode	17.925s
  ok  	github.com/Xsxdot/handoff/internal/executor/rawtap	0.005s
  ok  	github.com/Xsxdot/handoff/internal/executor/turn	(cached)
  ok  	github.com/Xsxdot/handoff/internal/initflow	0.009s
  ok  	github.com/Xsxdot/handoff/internal/launcher	0.002s
  ok  	github.com/Xsxdot/handoff/internal/ledger	16.901s
  ok  	github.com/Xsxdot/handoff/internal/ledgermirror	2.303s
  ok  	github.com/Xsxdot/handoff/internal/ledgerstep	8.516s
  ok  	github.com/Xsxdot/handoff/internal/localsync	0.179s
  ok  	github.com/Xsxdot/handoff/internal/logx	0.003s
  ok  	github.com/Xsxdot/handoff/internal/pathenv	0.005s
  ok  	github.com/Xsxdot/handoff/internal/permgate	0.007s
  ok  	github.com/Xsxdot/handoff/internal/prochost	22.140s
  ok  	github.com/Xsxdot/handoff/internal/projectid	0.003s
  ok  	github.com/Xsxdot/handoff/internal/proto	0.032s
  ok  	github.com/Xsxdot/handoff/internal/proxycfg	0.006s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost	0.031s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/engine	15.948s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/hostproc	0.024s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/sessdir	0.002s
  ok  	github.com/Xsxdot/handoff/internal/ptyhost/wire	0.001s
  ok  	github.com/Xsxdot/handoff/internal/ptytestroot	0.005s
  ok  	github.com/Xsxdot/handoff/internal/relay	0.009s
  ok  	github.com/Xsxdot/handoff/internal/release	0.086s
  ok  	github.com/Xsxdot/handoff/internal/selfupdate	0.003s
  ok  	github.com/Xsxdot/handoff/internal/service	0.005s
  ok  	github.com/Xsxdot/handoff/internal/skill	0.005s
  ok  	github.com/Xsxdot/handoff/internal/store	6.897s
  ok  	github.com/Xsxdot/handoff/internal/targetclient	0.157s
  ok  	github.com/Xsxdot/handoff/internal/testperm	0.008s
  ok  	github.com/Xsxdot/handoff/internal/toolchain	0.005s
  ok  	github.com/Xsxdot/handoff/internal/upgrade	0.003s
  ok  	github.com/Xsxdot/handoff/internal/webui	0.001s
  ```

## 24. 收口暂存首次尝试

- 命令：`git add docs/superpowers/ledgers/2026-08-25-b227-spec-ledger.md internal/agentd/workspace.go internal/agentd/workspace_test.go internal/executor/codex/adapter.go internal/executor/codex/adapter_sandbox_test.go internal/executor/codex/appserver_test.go internal/executor/codex/discipline_test.go internal/executor/codex/export_test.go internal/executor/turn/gitprobe.go internal/executor/turn/gitprobe_test.go`
- 原始输出：

  ```text
  fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/c0f89ac3/index.lock': Read-only file system
  ```

## 26. B227 实现提交

- 命令：`git commit -m "fix(b227): isolate git metadata and hooks"`
- 原始输出：

  ```text
  [cards/B227-charter-2 586d975b] fix(b227): isolate git metadata and hooks
   10 files changed, 547 insertions(+), 23 deletions(-)
   create mode 100644 internal/executor/codex/adapter_sandbox_test.go
  ```

- 判断：工作区文件可写但 linked worktree 的 Git 元数据仍不可写；实现、测试与集成结果不受此暂存失败影响，提交需走平台授权的必要 Git 元数据写入流程。

## 25. 收口提交首次尝试

- 命令：`git commit -m "fix(b227): isolate git metadata and hooks"`
- 原始输出：

  ```text
  fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/c0f89ac3/index.lock': Read-only file system
  ```

## 20. hooks 覆盖变异自验

- 变异前唯一命中计数：`rg -F -o 'cmdArgs := append([]string{"-C", repo, "-c", "core.hooksPath=" + hooksPath}, args...)' internal/agentd/workspace.go | wc -l`，原始输出 `1`。
- 将唯一命令行覆盖临时改为 `cmdArgs := append([]string{"-C", repo}, args...)`，未保留该变异。
- 编译阶段命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/agentd -run '^$' -count=1`。
- 编译阶段原始输出：

  ```text
  ok  	github.com/Xsxdot/handoff/internal/agentd	0.003s [no tests to run]
  ```

- 行为断言命令：`GOMODCACHE=/root/.handoff/tmp/02772ff3/gomodcache go test ./internal/agentd -run TestPrepareWorkspaceDoesNotRunRepositoryHook -count=1`。
- 行为断言原始输出：

  ```text
  --- FAIL: TestPrepareWorkspaceDoesNotRunRepositoryHook (0.03s)
      workspace_test.go:655: target worktree 不得运行恶意 hook，stat err=<nil>
  FAIL
  FAIL	github.com/Xsxdot/handoff/internal/agentd	0.031s
  FAIL
  ```

- 判断：变异唯一命中、可编译且被行为断言拦截；已恢复原实现。
