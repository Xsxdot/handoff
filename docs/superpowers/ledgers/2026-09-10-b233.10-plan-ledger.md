# B233.10 plan 台账

卡：B233.10
法定产出：`docs/superpowers/plans/b233.10-plan.md`
当前节点：plan；本轮只产出计划与台账，不实现代码、不派发、不调用 handoff CLI。

| 时间 | 事实 / 判断 | 原始证据 |
|---|---|---|
| 2026-09-10 | 当前执行分支为 `cards/B233.10-charter-5`，HEAD 为 `58f7521f8cdec2c87840696362895409d12842c4`；有效基线分支当前解析为 `94246fc8a2acc7996c254cb6b473c7c9b5aef225`；工作树初始无修改。 | `git branch --show-current` → `cards/B233.10-charter-5`；`git rev-parse HEAD` → `58f7521f8cdec2c87840696362895409d12842c4`；`git rev-parse cards/B233.1-charter-7` → `94246fc8a2acc7996c254cb6b473c7c9b5aef225`；`git status --short` → 无输出。遵循 P1，不切换到旧 linux-01 ref。 |
| 2026-09-10 | spec、contract、breakdown 三份输入均存在且已拍板；法定计划路径为 `docs/superpowers/plans/b233.10-plan.md`。 | `test -f docs/superpowers/specs/b233.10.md`、`test -f docs/superpowers/specs/b233.10-contract.md`、`test -f docs/superpowers/specs/b233.10-breakdown.md` 均由后续读取命令成功；用户注入的本节点产出物路径。 |
| 2026-09-10 | 图全局结构检查通过：302 containers、23 domain declarations、5522 edges、5 个未扫描入口，`edgeIssues` 与 `issues` 为 null。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` → 原始 JSON：`{"containers":302,"domainDecls":23,"domains":23,"edgeIssues":null,"edges":5522,"entitiesWithoutLifecycle":2,"issues":null,"nodes":4514,"unscannedEntries":5,...}`。 |
| 2026-09-10 | `context d_scheduling` 返回 baseline 视图；领域声明缺失 warning，packages 为 `internal/schedclient`、`internal/scheduling`，输出没有可用调用边；不能据此否认源码调用。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_scheduling` → `warning: ... 领域声明缺失：codegraph/domains/d_scheduling.json`，`interfaces` 仅含 `New`、`RunCommand`、`Service.ApplyDetect`，`edges:null`。 |
| 2026-09-10 | 本卡视图定位到四个关键符号：`Service.Select` 签名为 `func (s *Service) Select(req IgnitionRequest) (Binding, error)`（scheduling.go:525），`Service.AdmitFrozen` 签名为 `func (s *Service) AdmitFrozen(binding Binding) (Binding, error)`（scheduling.go:531），`Server.handleDispatch`（server.go:1364），`Client.Dispatch` 签名为 `func (c *Client) Dispatch(ctx context.Context, opts DispatchOpts) (*proto.Task, error)`（client.go:762）。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.10-charter sym ...` 四次原始 JSON 分别返回上述 `anchor: ok/moved`、签名与文件行。 |
| 2026-09-10 | 四个关键方法的图流程均为 `degraded=true`、`missing: 基线没有 flows 段`、`steps: []`；`handleDispatch` 仍识别 `POST /api/tasks`，`Client.Dispatch` 仍识别 `handoff dispatch` 调用方/通道。计划必须以源码流程为准，不能以空 flow 冒充流程结论。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.10-charter flow ...` 四次原始 JSON；Select/AdmitFrozen 的 callers/channels 为空，handleDispatch 的 caller/channel 为 `POST /api/tasks`，Client.Dispatch 的 caller/channel 为 `handoff dispatch`。 |
| 2026-09-10 | 关键基线测试在当前工作树均通过，但这不代表 B233.10 行为已实现：scheduling、client wire 金样本、ledgerstep 透传、agentd 代表性接缝均为 `ok`。 | `go test ./internal/scheduling -run 'Test(ConcurrentAdmitRespectsTwoLevelCaps|AdmitCarrierOccupiesCarrierKeyOnly|Occupancy.*)' -count=1` → `ok github.com/Xsxdot/handoff/internal/scheduling 0.275s`；`go test ./internal/client -run '^TestDispatchFrozenIdentityJSONGolden$' -count=1` → `ok github.com/Xsxdot/handoff/internal/client 0.008s`；`go test ./internal/ledgerstep -run 'Test(ViaTemplateCarriesHomeDirPointer|RunnerExecutorModelOverridePriorityAndPairRule)' -count=1` → `ok github.com/Xsxdot/handoff/internal/ledgerstep 0.460s`；`go test ./internal/agentd -run 'Test(SquadNodeAdmitsAndDispatchesThroughBinding|CardStepAdmittedRoundReleasesCapacity|DispatchPersistsFrozenIdentity|HandleDispatchStartFailureReleases|HandleDoneIdempotentReleasesBareCarrier|ContinueDoesNotAdmit|RecoverOnStartup)' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 1.908s`。 |
| 2026-09-10 | `cmd/agentd.go` 当前在对外服务前调用任务级 `agentd.RecoverOnStartup(st, srv.Hub(), mgr.ResumeTask, mgr.SweepTaskProcs, logger)`；同文件的 `setupLedger` 先调用 `srv.SetupAutomation(lst)`，因此计划可在 `Server` 组装点加入带注册表的启动钩子，再复用现有自由函数。 | `rg -n "SetupAutomation|RecoverOnStartup" cmd/agentd.go`；`sed -n '440,485p' cmd/agentd.go`；原始输出显示 `srv.SetupAutomation(lst)` 在 `setupLedger` 中、自由函数调用在启动恢复段。 |
| 2026-09-10 | `internal/schedclient/schedclient.go` 的冻结接口为 `Record{ID string, Version int, Seq int64, Body []byte}` 与 `Registry{Put(kind,id,expectVersion,body,actor)(int,error); Get(kind,id)(Record,error); List(kind)([]Record,error); Delete(kind,id,expectVersion,actor) error}`；注释确认 `expectVersion=0` 是必须不存在、CAS 冲突返回 `ErrCASConflict`。 | `sed -n '1,70p' internal/schedclient/schedclient.go`。 |
| 2026-09-10 | `internal/ledger/api/registry.go` 的 Facade 只转调账本注册表，`internal/agentd/server.go` 的 `facadeAsRegistry` 完成 DTO/错误哨兵映射；启动对账应使用 `List("sched_running")` 与逐记录 CAS `Put`，不调用成对 `Service.Release`，以保持 carrier/member 独立。 | `sed -n '1,90p' internal/ledger/api/registry.go`、`sed -n '2650,2695p' internal/agentd/server.go`。 |
| 2026-09-10 | 现有 `RecoverOnStartup` 只筛 running/waiting_answer/waiting_review，跳过 pending；`Store.ListTasks` 返回完整任务且从持久化列恢复 Carrier/Squad/State。P2 对账必须另取全量任务快照，并把 pending 视为占用。 | `sed -n '447,530p' internal/agentd/watchdog.go`、`sed -n '425,490p' internal/store/store.go`。 |
| 2026-09-10 | `SetupAutomation` 把 ledger facade 保存到 `Server.autoLedger`，并以 `facadeAsRegistry` 构造 `Server.scheduling`；同包新增私有 reconciliation helper 与 `Server.RecoverOnStartup` 组装方法不需要修改冻结的自由函数签名。 | `sed -n '2550,2615p' internal/agentd/server.go`。 |
| 2026-09-10 | `who-calls 'Service.Admit' --depth 2` 只返回焦点节点、0 条边，并警告 `基线仍有 5 个未扫描入口：查询结果为空不等于没有调用方`；调用面必须以源码和测试夹具复核，不能把空图当作无调用方。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . who-calls 'Service.Admit' --depth 2` 原始 JSON。 |
| 2026-09-10 | `context d_gateway`、`context d_orchestration`、`context d_transport` 均完成，但输出带 stale/truncated/unprojected 警告；计划只把它们用于领域入口定位，不用缺失边推导行为。 | 三次 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context ...` 原始输出；本行保留命令级事实。 |
| 2026-09-10 | 法定计划已写入 `docs/superpowers/plans/b233.10-plan.md`，覆盖 S1–S6、四个实现 task、七处序列化边界、#1–#90 条款归属、缺陷族、真机未验证清单、日志/注释/测试范围和台账要求；未写实现代码。 | `wc -l docs/superpowers/plans/b233.10-plan.md` → `708`（随后增补后以提交内容为准）；`git status --short --branch` 只显示当前分支与计划/台账未跟踪文件。 |
| 2026-09-10 | 计划文本自检：围栏计数为偶数，仓内占位关键词扫描无输出；`git diff --check --no-index /dev/null docs/superpowers/plans/b233.10-plan.md` 退出 1 且无输出（no-index 比较因文件内容不同的正常退出，未见 whitespace error）。 | `awk '/^```/{n++} END{print "fences=" n, "balanced=" (n%2==0)}' ...` → `fences=28 balanced=1`；`rg -n 'TBD|TODO|同 Task|适当的错误处理' ...` → 无输出；`git diff --check --no-index ...` → 无输出、exit 1。 |
| 2026-09-10 | staged 计划/台账校验通过：`git diff --cached --check` 无输出且退出 0；staged 仅含法定计划与台账，共 807 行新增；围栏仍为偶数，占位关键词扫描 clean。 | `git diff --cached --check`；`git diff --cached --stat` → `2 files changed, 807 insertions(+)`；`git status --short --branch` → 当前分支下仅两份 A 文件；`awk ...` → `fences=28 balanced=1`；placeholder scan → `placeholder_scan=clean`。 |
| 2026-09-10 | 首次提交已创建；按台账纪律，以下提交输出记录后只 amend 一次收进同批提交，不追写 amend 后 hash。 | `git commit -m "docs: plan B233.10 dispatch lifecycle"` → `[cards/B233.10-charter-5 86feecba] docs: plan B233.10 dispatch lifecycle`；`2 files changed, 808 insertions(+)`。 |
| 2026-09-10 | 当前实现节点实际工作树位于 `cards/B233.10-charter-6`，HEAD 为 `a9cc43238b70e1d0ffceaca423d84a2f5d282360`，状态干净；未切换分支。 | `git status --short --branch && git rev-parse HEAD && git branch --show-current` → `## cards/B233.10-charter-6`、`a9cc43238b70e1d0ffceaca423d84a2f5d282360`、`cards/B233.10-charter-6`。 |
| 2026-09-10 | 实现前编制基线测试通过。 | `go test ./internal/scheduling -run 'Test(ConcurrentAdmitRespectsTwoLevelCaps|AdmitCarrierOccupiesCarrierKeyOnly|Occupancy.*)' -count=1` → `ok github.com/Xsxdot/handoff/internal/scheduling 0.178s`。 |
| 2026-09-10 | 实现前 client 冻结身份 JSON 金样本通过。 | `go test ./internal/client -run '^TestDispatchFrozenIdentityJSONGolden$' -count=1` → `ok github.com/Xsxdot/handoff/internal/client 0.004s`。 |
| 2026-09-10 | 实现前 agentd 小队节点代表性基线通过。 | `go test ./internal/agentd -run 'Test(SquadNodeAdmitsAndDispatchesThroughBinding|CardStepAdmittedRoundReleasesCapacity)' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.442s`。 |
| 2026-09-10 | 实现前 ledgerstep 透传/覆盖代表性基线通过。 | `go test ./internal/ledgerstep -run 'Test(ViaTemplateCarriesHomeDirPointer|RunnerExecutorModelOverridePriorityAndPairRule)' -count=1` → `ok github.com/Xsxdot/handoff/internal/ledgerstep 0.406s`。 |
| 2026-09-10 | 当前代码已经包含 Carrier/Squad/HomeDir 的 Ticket 0 数据字段与 JSON 投影，但 `Service.Select`、`Service.AdmitFrozen` 仍返回“尚未接线”空壳；`admitSquadStep` 仍调用 `Admit`，卡节点 goroutine 仍 defer 执行占用释放，`handleDispatch` 仍总走 `prepareReceiverBinding`，重复 Done 仍调用释放。 | `sed`/`rg` 读取 `internal/scheduling/scheduling.go`、`internal/agentd/scheddispatch.go`、`internal/agentd/cardstep.go`、`internal/agentd/server.go` 原始源码。 |
| 2026-09-10 | Task 1 接缝首红确认是功能缺失而非 typo：5 个冻结选择/准入测试均进入断言并失败，现有空壳返回“尚未接线”。 | `go test ./internal/scheduling -run 'TestB23310(Select|AdmitFrozen)' -count=1` → `--- FAIL: TestB23310SelectDoesNotOccupy ... scheduling: 冻结身份选择尚未接线`；`--- FAIL: TestB23310SelectReportsFullWithoutWriting ... want ErrNoSlot`；`--- FAIL: TestB23310AdmitFrozenUsesExactCarrier ... want ErrNoSlot`；`--- FAIL: TestB23310AdmitFrozenRejectsPhysicalMismatch ... want ErrRoleMismatch`；`--- FAIL: TestB23310AdmitFrozenDirectHasNoMemberKey ... want success`；最终 `FAIL github.com/Xsxdot/handoff/internal/scheduling 1.070s`。 |
| 2026-09-10 | Task 1 最小实现后冻结选择/准入测试通过；Select 只读筛选，AdmitFrozen 按载体与成员身份做一次 CAS。 | `gofmt -w internal/scheduling/scheduling.go internal/scheduling/scheduling_test.go && go test ./internal/scheduling -run 'TestB23310(Select|AdmitFrozen)' -count=1` → `ok github.com/Xsxdot/handoff/internal/scheduling 0.514s`。 |
| 2026-09-10 | Task 2 新接缝首红实际击中 source 侧计数回归：直接调用 `admitSquadStep` 后 carrier/c1 计数为 1 而期望 0；满员排队子测通过，说明排队不携带载体的断言有执行。 | `go test -v ./internal/agentd -run '^TestB23310CardSelectDoesNotOccupyBeforeHTTP$' -count=1` → `=== RUN .../选择不写计数`；`起源侧 Select 写入 carrier/c1=1，want 0`；`=== RUN .../满员排队不保存载体` → `--- PASS`；最终 `FAIL github.com/.../internal/agentd 0.417s`。 |
| 2026-09-10 | Task 2 最小实现后新接缝两子测通过；卡 goroutine 不再释放 source execution occupancy，step transport 继续携带冻结 Carrier/Squad/HomeDir。 | `gofmt ... && go test -v ./internal/agentd -run 'Test(B23310CardSelectDoesNotOccupyBeforeHTTP|... )' -count=1` → `TestB23310CardSelectDoesNotOccupyBeforeHTTP` 两子测均 `PASS`，`TestCardStepAdmittedRoundReleasesCapacity` `PASS`，命令退出 0；旧 PTY 夹具 `TestSquadNodeAdmitsAndDispatchesThroughBinding`、`TestSquadNodeFullQueuesWithoutTrace` 原始输出为 `SKIP`：`PTY 测试根目录不可用；... 原因=mkdir /tmp/handoff-pty-...: read-only file system` 与 repo-dot socket 上限错误，故不把其记为已验证。 |
| 2026-09-10 | Task 3 首次测试命中测试拼写错误，未进入行为断言；立即修正为账本契约字面量 `sched_running`，不把该编译红计作功能红。 | `go test ./internal/agentd -run 'Test(B23310ClientDispatchUsesFrozenIdentity|HandleDoneIdempotentDoesNotReleaseCompletedCarrier)' -count=1` → 原始编译错误：`internal/agentd/receiver_occupancy_test.go:142:39: undefined: scheduling.KindSchedRunning`、`:150:43`、`:162:44`；最终 `FAIL ... [build failed]`。 |
| 2026-09-10 | Task 3 行为首红已确认：客户端冻结身份请求仍被服务端按 Receiver 重解析并报物理覆盖冲突；completed 重复 done 仍把 carrier 记录从 `(v1,count1)` 改为 `(v2,count0)`。 | `go test -v ./internal/agentd -run 'Test(B23310ClientDispatchUsesFrozenIdentity|HandleDoneIdempotentDoesNotReleaseCompletedCarrier)' -count=1` → `冻结身份派发: dispatch: 状态码 409: {"error":"scheduling: 禁止覆盖已绑定载体的机器、引擎或 HOME"}`；`已 completed 的 done 不应释放载体：before=(v1,count1) after=(v2,count0)`；最终 `FAIL github.com/.../internal/agentd 0.566s`。 |
| 2026-09-10 | Task 4 新接缝首红为预期符号缺失：四支启动对账测试均无法编译，因为 `Server.RecoverOnStartup` 尚未存在；这只证明接缝缺席，尚未证明断言有牙。 | `go test -v ./internal/agentd -run '^TestB23310Recover' -count=1` → `internal/agentd/recover_occupancy_test.go:66:13: srv.RecoverOnStartup undefined`、`:135:17`、`:174:16`；最终 `FAIL ... [build failed]`。 |
| 2026-09-10 | Task 4 空壳接缝落地后再次出现断言红：四支测试均实际进入行为断言并收到 `启动恢复调度占用对账尚未接线`，因此不是测试编译/拼写问题。 | `gofmt -w internal/agentd/server.go && go test -v ./internal/agentd -run '^TestB23310Recover' -count=1` → 四支 `--- FAIL`，前三支分别报 `pending 启动对账`、`无主启动对账`、`活跃 owner 启动对账` 的桩错误，末支报 `malformed sched_running 应返回带 key 错误，实际 启动恢复调度占用对账尚未接线`；最终 `FAIL github.com/.../internal/agentd 0.812s`。 |
| 2026-09-10 | Task 4 最小实现后四支启动对账测试通过：pending owner 保持、carrier/member 孤儿各自 CAS 清零、running/waiting_review owner 保持且 completed owner 清零、malformed body 拒绝并阻断后续探活。 | `gofmt -w internal/agentd/watchdog.go internal/agentd/server.go cmd/agentd.go && go test -v ./internal/agentd -run '^TestB23310Recover' -count=1` → 四支均 `--- PASS`，最终 `PASS`、`ok github.com/Xsxdot/handoff/internal/agentd 0.695s`。 |
| 2026-09-10 | Task 3 client 接缝与 JSON 三态测试通过。 | `go test -v ./internal/client -run 'Test(B23310|DispatchFrozenIdentity)' -count=1` → `TestDispatchFrozenIdentityJSONGolden`、`TestDispatchFrozenIdentityJSONOmissionAndHomeDirTriState` 均 `PASS`；最终 `PASS`、`ok github.com/Xsxdot/handoff/internal/client 0.006s`。 |
| 2026-09-10 | Task 3/4 agentd 触及范围测试通过；输出中包含既有 Dispatch/Done/Stop/Continue/Resume、冻结 HTTP、启动对账及卡节点接缝，全部执行的测试为 `PASS`，最终包测试通过。 | `go test -v ./internal/agentd -run 'Test(B23310|Dispatch|Done|Stop|Continue|Resume|Manager.*Carrier)' -count=1` → 输出末尾 `PASS`、`ok github.com/handoff/internal/agentd 23.338s`；未观察到 FAIL，旧测试亦均 `--- PASS`。 |
| 2026-09-10 | Task 3/4 收尾全仓编译通过。 | `go build ./...` → 原始 stdout/stderr 为空，exit code `0`。 |
| 2026-09-10 | 为锁住 Task 2 选择后装配失败不误释放，首次测试夹具尝试引用不存在模板，被工作流校验在被测入口前拒绝；这不是功能红，已改用合法模板 + 缺失纪律名制造同一后续失败。 | `go test -v ./internal/agentd -run '^TestB23310CardSelectAssemblyFailureDoesNotRelease$' -count=1` → 原始错误：`scheddispatch_test.go:176: 写入缺失模板工作流: 节点 "implement" 引用的模板 "missing-template" 不可用: ledger: 当前状态不允许该操作`；最终 `FAIL ... 0.176s`。 |
| 2026-09-10 | Task 2 选择后装配失败的有效行为红已确认：缺失纪律导致入口返回错误后，现有旧回滚仍写入 `carrier/c1`，违反 Select 只读。 | `go test -v ./internal/agentd -run '^TestB23310CardSelectAssemblyFailureDoesNotRelease$' -count=1` → `Select 后装配失败不应释放/写入 carrier/c1，读结果=<nil>`；最终 `FAIL github.com/.../internal/agentd 0.259s`。 |
| 2026-09-10 | 删除 Select 后装配失败路径的旧 Release 回滚后测试通过；卡槽仍释放，执行占用只由目标任务终态持有。 | `gofmt -w internal/agentd/cardstep.go && go test -v ./internal/agentd -run '^TestB23310CardSelectAssemblyFailureDoesNotRelease$' -count=1` → `--- PASS`、`PASS`、`ok github.com/.../internal/agentd 0.196s`。 |
| 2026-09-10 | 变异自验准备时尝试把唯一锚点输出到 `/tmp`，但该环境 `/tmp` 只读；未把失败命令当作命中结论，改用无写入的 `rg -c` 唯一性检查。 | `rg ... | tee /tmp/b23310-select-anchor.count` → 原始错误：`tee: /tmp/b23310-select-anchor.count: Read-only file system`；同一命令仍显示锚点 `scheduling.go:584`，但因 tee 失败 exit 非 0，故按未验证处理。 |
| 2026-09-10 | 首次 Select 成员边界变异编译通过，但相关行为测试仍为绿；按变异纪律判定该变异未打中唯一守卫（载体同样满员，后续 carrier 守卫遮蔽了成员守卫），已恢复原代码，不计作测试拦截。 | 变异 `memberRunning >= member.MaxConcurrency`→`>` 后：`go build ./...` exit `0`、无输出；`go test -v ./internal/scheduling -run '^TestB23310SelectReportsFullWithoutWriting$' -count=1` → `--- PASS`、`PASS`；恢复变异前测试代码。 |
| 2026-09-10 | 为避免上述遮蔽，补充了成员政策满而载体仍有物理位的 seam 测试；原实现测试通过，确认该断言能区分两级边界。 | `go test -v ./internal/scheduling -run '^TestB23310SelectSkipsFullMemberBeforeCarrier$' -count=1` → `--- PASS`、`PASS`、`ok github.com/Xsxdot/handoff/internal/scheduling 0.111s`。 |
| 2026-09-10 | Select 成员边界变异完成有效自验：变异编译通过，单条行为断言先变红，证明测试能拦截语义错误；随后已恢复 `>=` 原实现。 | 变异 `memberRunning >= member.MaxConcurrency`→`>`：`go build ./...` exit `0`、无输出；`go test -v ./internal/scheduling -run '^TestB23310SelectSkipsFullMemberBeforeCarrier$' -count=1` → `成员满员但载体有位时不应选择 A: {Squad:S Carrier:A ...}`、最终 `FAIL ... 0.099s`；恢复后续代码。 |
| 2026-09-10 | completed Done 释放守卫变异完成有效自验：命中锚点唯一，变异编译通过，单条幂等行为断言变红，证明版本/count 不变守卫有牙；随后已移除变异调用。 | `rg -c -F 'if cur, err := s.st.GetTask(taskID); err == nil && cur.State == proto.TaskStateCompleted {' internal/agentd/server.go` → `1`；插入重复 `releaseTaskCarrierOccupancy(cur)` 后 `go build ./...` exit `0`；`go test -v ./internal/agentd -run '^TestHandleDoneIdempotentDoesNotReleaseCompletedCarrier$' -count=1` → `before=(v1,count1) after=(v2,count0)`、最终 `FAIL ... 0.279s`；已恢复原代码。 |
| 2026-09-10 | 冻结成员精确匹配变异完成有效自验：唯一命中 `candidate.Carrier == binding.Carrier`，变异为 `!=` 后编译通过，A 满员断言变红，证明不会把冻结 B 请求错配到 A；随后已恢复原实现。 | `rg -c -F 'if candidate.Carrier == binding.Carrier {' internal/scheduling/scheduling.go` → `1`；变异后 `go build ./...` exit `0`；`go test -v ./internal/scheduling -run '^TestB23310AdmitFrozenUsesExactCarrier$' -count=1` → `A 满员冻结准入 error = <nil>，want ErrNoSlot`、最终 `FAIL ... 0.126s`；已恢复原代码。 |
