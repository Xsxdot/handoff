# B233.6 breakdown 台账

本台账按拆解回合追加；命令行输出只记录本回合实际运行结果，不把历史文档中的结果冒充当前验证。

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-07 | 当前工作树干净；分支为 `cards/B233.6-charter-5`，HEAD 为 `0e3ca8c38341512866607ff597bf080c7d4003d`。 | `git status --short --branch && git rev-parse HEAD && git branch --show-current` → `## cards/B233.6-charter-5`；`0e3ca8c38341512866607ff597bf080c7d4003d`；`cards/B233.6-charter-5` |
| 2026-09-07 | spec 头部状态为已批准，批准日期与用户原话已落在文件头；附件台账路径在当前工作树不存在，作为流程欠账记录，不改写 spec 状态。 | `sed -n '1,18p' docs/superpowers/specs/b233.6.md` → `状态：**已批准**（2026-09-06，用户原话「按这个推进吧」）`；`sed -n '1,20p' docs/superpowers/ledgers/2026-09-06-b233.6-spec-ledger.md` → `sed: can't read docs/superpowers/ledgers/2026-09-06-b233.6-spec-ledger.md: No such file or directory` |
| 2026-09-07 | contract 头部冻结状态原为「随本提交冻结」；已回写为带日期和冻结提交的「已冻结」，并在 contract §9 记录 C-0/C-1。 | `git diff -- docs/superpowers/specs/b233.6-contract.md` → 原冻结状态行 `**冻结状态：随本提交冻结**`，现为 `**冻结状态：已冻结（2026-09-07；冻结提交 \`0e3ca8c38341512866607ff597bf080c7d4003d\`）**`；新增 `## 9. 拆解期状态修订`。 |
| 2026-09-07 | 依据项目最优图，`best.json` 的 parent 为空顶层领域共 15 个；本拆解候选触及 `d_ledger`、`d_gateway`、`d_transport`，其余顶层领域不因调用链验收命令而列为改动归属。 | `jq -r '.domains | to_entries[] | select((.value.parent // null) == null) | [.key,.value.type,.value.label] | @tsv' codegraph/best.json` → `d_orchestration logic 任务编排`；`d_gateway boundary 控制门面`；`d_workspace boundary 项目与工作区`；`d_execution boundary 任务执行`；`d_sessions boundary 终端会话`；`d_transport boundary 跨机连接`；`d_protocol logic 协议契约`；`d_ledger logic 卡片账本`；`d_collab logic 协作房间`；`d_cli logic 协调者命令面`；`d_web logic Web 控制台`；`d_policy logic 运行策略与配置`；`d_maintenance boundary 安装与换版`；`d_scheduling logic 编制调度`；`d_keystone logic 协调者 Keystone` |
| 2026-09-07 | oversized-package 警告所涉目录仍可按入口与职责圈出有界文件集；因此本稿为每张子卡列显式文件清单，不新增无法归属的目录级功能卡。 | `rg --files internal/agentd -g '*.go' | rg -v '_test\\.go$' | wc -l` → `91`；`rg --files internal/ledger internal/ledgerstep internal/ledgermirror -g '*.go' | rg -v '_test\\.go$' | wc -l` → `33` |
| 2026-09-07 | 当前图契约检查不是绿：两项失败均为既有 `d_execution→d_orchestration` dead-contract/dead-interface `ApprovalClient`；本拆解不把它归因于 B233.6。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.6-charter check` → JSON `fails` 为：`{"kind":"dead-contract","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）"}`；`{"kind":"dead-interface","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的接口 \"ApprovalClient\" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）"}`；末行 `Error: 契约对照发现 2 处违规`、`exit status 1`。 |
| 2026-09-07 | 上游背景强调 Ask/审批由源执行任务与类型化 client 负责，工作流只转发并补充卡/节点/attempt/source 身份；本稿将行为事实依赖列入真机清单。 | `sed -n '1,18p' docs/superpowers/specs/2026-09-06-foundation-infrastructure-boundaries-draft.md` → 文件头为 `状态：基于源码核查的架构讨论草案，方法名表达能力意图，尚未冻结 Go 签名或实施计划`，并写明 `2026-09-06 用户已确认通过派发事件流上浮交互、审批仍由 client 处理`。 |
| 2026-09-07 | 相关现有测试入口已亲自列出；receiver/调度、ledger/mirror、client/mirrorstream 与 wakeconsumer 的测试文件均存在，可作为有界集合的现状入口。 | `rg --files internal/agentd internal/ledgerstep internal/ledger internal/ledgermirror internal/client | rg '(receiver_dispatch|scheddispatch|scheddrain|cardstep|taskstate|tasks|events|mirror|execution|mirrorstream|wakeconsumer|runner|dispatch).*_test\\.go$'` → 输出包含 `internal/agentd/receiver_dispatch_test.go`、`internal/agentd/scheddispatch_test.go`、`internal/ledger/taskstate_test.go`、`internal/ledger/tasks_test.go`、`internal/ledgermirror/mirror_test.go`、`internal/client/mirrorstream_test.go`、`internal/client/execution_test.go`、`internal/ledgerstep/runner_test.go`、`internal/ledgerstep/dispatch_test.go`。 |
| 2026-09-07 | 拆解稿符号锚解析通过；当前视图返回 6 个锚，1 个 `moved` 不算坏锚，未返回 vanished/错误。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.6-charter resolve --doc docs/superpowers/specs/b233.6-breakdown.md` → `exit=0`；anchors 为 `internal/ledgerstep/dispatch.go#Dispatcher.ViaTemplate`（line 137，`moved`）、`internal/ledger/tasks.go#TaskLink`（line 14，`ok`）、`internal/ledger/mirror.go#Store.AppendMirroredEvent`（line 37，`ok`）、`internal/ledgermirror/mirror.go#Mirror.Run`（line 238，`ok`）、`internal/agentd/wakeconsumer.go#automationWakeEvent`（line 39，`ok`）、`internal/client/client.go#Client.Reply`（line 689，`moved`）。 |
| 2026-09-07 | 拆解稿、拆解台账、contract 当前长度分别为 289、17、319 行；工作树包含 contract 修改与两个新增文件，尚未提交。 | `wc -l docs/superpowers/specs/b233.6-breakdown.md docs/superpowers/ledgers/2026-09-07-b233.6-breakdown-ledger.md docs/superpowers/specs/b233.6-contract.md` → `289`、`17`、`319`、`622 total`；此前同命令在加入符号锚前为 `289`、`14`、`319`、`622 total`，本行以当前读数为准。 |
| 2026-09-07 | 上一条长度记录在后续补充文件集合时已过时，以本次实测为准；文档仍未提交。 | `wc -l docs/superpowers/specs/b233.6-breakdown.md docs/superpowers/ledgers/2026-09-07-b233.6-breakdown-ledger.md docs/superpowers/specs/b233.6-contract.md` → `296`、`16`、`319`、`631 total`。 |
| 2026-09-07 | 图 context 显示 d_orchestration 的冻结生命周期锚为 `internal/agentd/manager.go#Dispatch` → `#transit`，并包含 `Manager.Dispatch`、`Manager.RelayAnswer`；因此 manager 从 d_gateway 改列为 d_orchestration，d_gateway 保留 server/cardstep 门面。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.6-charter context d_orchestration` → `exit=0`；原始日志含 `domain=d_orchestration`、`packages=2`，JSON 含 `domain.id=d_orchestration`、`kind=logic`、`lifecycle.from=internal/agentd/manager.go#Dispatch`、`lifecycle.to=internal/agentd/manager.go#transit`、接口 `n_agentd_Manager_Dispatch` 与 `n_agentd_Manager_RelayAnswer`。 |
| 2026-09-07 | 对 gateway/execution/transport context 做符号筛选时，前两个筛选命令因 `rg` 无匹配退出 1，不能据此宣称目标符号归属；transport 筛选命令退出 0 且日志显示 d_transport context 成功。该次失败不改写图结论。 | `go run ... context d_gateway | rg '...'`、`go run ... context d_execution | rg '...'` → `exit=1`（无匹配筛选结果；上游 context 日志分别含 `graph context completed domain=d_gateway`、`domain=d_execution`）；`go run ... context d_transport | rg '...'` → `exit=0`，日志含 `graph context completed domain=d_transport`，筛选到 `n_client_Client_Reply`。 |
| 2026-09-07 | 直接图 context 命令均退出 0：d_gateway 类型为 boundary、d_execution 类型为 boundary、d_transport 类型为 boundary；d_execution 的 context 包含 internal/executor 与 internal/prochost，证明它是本卡的外部行为对端但不在本稿改动集合；d_transport 包含 internal/client。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.6-charter context d_gateway` → `exit=0`，日志 `graph context completed domain=d_gateway`、`kind=boundary`；同参数 `context d_execution` → `exit=0`，日志 `domain=d_execution`、packages 含 `internal/executor`/`internal/prochost`、`kind=boundary`；同参数 `context d_transport` → `exit=0`，日志 `domain=d_transport`、packages 含 `internal/client`、`kind=boundary`。 |
| 2026-09-07 | 直接图 context 输出较长，工具提示 truncated；因此只采用未截断的 domain/kind/packages 日志作归属事实，不把被截断的 chain/foci 当完整依赖清单。 | 三条直接 `context` 命令均原始输出含 `Warning: truncated output`，同时各自含 `assemble context completed` 与 `graph context completed`；未据此推断未显示的调用边。 |
| 2026-09-07 | 拆解稿符号锚再次解析通过；图全局 validate 通过且 `issues: null`；git diff 检查通过。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.6-charter resolve --doc docs/superpowers/specs/b233.6-breakdown.md` → `exit=0`，6 锚中 `Dispatcher.ViaTemplate` 与 `Client.Reply` 为 `moved`，其余 4 个为 `ok`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` → `exit=0`，`containers: 300`、`domainDecls: 23`、`edgeIssues: null`、`issues: null`、`views` 含 `cards-B233.6-charter`；`git diff --check` → `exit=0`、无输出。 |
| 2026-09-07 | 完成最终文档空白检查；新拆解稿与本稿台账无行尾空白，tracked contract 的差异也通过 diff 检查。当前最终长度为拆解稿 297 行、台账 22 行、contract 319 行。 | `wc -l docs/superpowers/specs/b233.6-breakdown.md docs/superpowers/ledgers/2026-09-07-b233.6-breakdown-ledger.md docs/superpowers/specs/b233.6-contract.md` → `297`、`22`、`319`、`638 total`；`rg -n '[[:blank:]]+$' docs/superpowers/specs/b233.6-breakdown.md docs/superpowers/ledgers/2026-09-07-b233.6-breakdown-ledger.md || true` → 无输出；`git diff --check` → `exit=0`、无输出。 |
| 2026-09-07 | 法定产出物、contract 状态回写和本台账首次提交成功；随后按纪律只做一次 amend 收入本提交事实，本行记录 amend 前历史读数，不追写 amend 后 hash。 | `git add docs/superpowers/specs/b233.6-breakdown.md docs/superpowers/specs/b233.6-contract.md docs/superpowers/ledgers/2026-09-07-b233.6-breakdown-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short` → `exit=0`；`git commit -m "docs(B233.6): 出稿工作流共享派发拆解提案"` → `exit=0`，原始输出 `[cards/B233.6-charter-5 58d213de] docs(B233.6): 出稿工作流共享派发拆解提案`、`3 files changed, 330 insertions(+), 1 deletion(-)`、创建 breakdown/ledger 文件。 |
