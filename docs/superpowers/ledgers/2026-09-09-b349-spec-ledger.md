# B349+B352 spec 台账

- 2026-09-09：两卡移 spec。B349「评估消费端 source identity」；B352「镜像重启全量重放按水位去重」。同批消费正确性。
- 2026-09-09：用户选 A。`proto.LedgerEvent` 补 source 三列（omitempty），投影停止丢列；wakeconsumer 在 node/attempt 之外核 source_target+source_task 对应当前派发。不扩 envelope JSON，不新增事件类型。B 绕 Facade、C 无代码收口弃选。
- 2026-09-09：用户选 A（card wait 同一道闸）。`task_mirrored` 只有 node/attempt/source 对应当前派发才算可动作；旧 attempt 留账本不打 stdout。闸只留 wakeconsumer 的 B 弃选。
- 2026-09-09：用户同意 A（B352 本机持久化 cursor）。水位写这台 agentd 的 DataDir，不进共享账本；seen 仍内存；重启从上次 seq 续拉（至少一次）。写共享账本的 B、启动 MAX(seq) 的 C 弃选。
- 2026-09-09：本机镜像 `source_target` 取 TaskLink.Target，可为空（`internal/ledgermirror/mirror.go` 约 :517–518；`mirror_test.go:546-548` 本机夹具期望空串）。当前派发 `DispatchSnapshot.Target` 亦可空（`internal/ledgerstep/runner_test.go:733`）。匹配规则必须允许两边都空，不能把空 target 当缺身份一刀切。
- 2026-09-09：图：`codegraph sym LedgerEvent` 命中 `m_proto_LedgerEvent`（`internal/proto/ledger.go:119`），字段无 Source*。`sym acceptsCurrentWorkflowAttempt` 不在图中。`who-calls consumeAutomationEventsOnce` 节点 id 为 `n_agentd_Server_consumeAutomationEventsOnce`。
- 2026-09-09：房间游标已是 DataDir 文件介质（`server.go:2567` `room-cursors.json`）。自动化水位同类：进程私有、不进账本。具体文件名归 contract。
- 2026-09-09：基线 `cards/B233.1-charter-7` @ `f2129a25`。spec 草稿 `docs/superpowers/specs/b349.md`。
- 2026-09-09：用户要求按纪律派独立 subagent 审 spec。已派 `01a084c3-a666-7fe2-9220-1bb0c2ebff9a`（只读，产物 `docs/superpowers/reviews/b349-spec-review.md`）。
- 2026-09-09：独立审查总判「修订后再批」。C1 一则、Important I1–I4、Minor M1–M5。协调者核活代码后全部成立，未推回。
- 2026-09-09：用户「吸收审查改稿」。C1/I1–I4 写入 `docs/superpowers/specs/b349.md`：card wait 当前身份只从事件所属卡 × envelope.node 的最新 EvDispatched 取；身份钉 snapshot.Attempt+Target；接缝锁双空 target 与 source_seq 不参与；废止 B233.6 plan §5.2 L493/L503，不退回 contract/P3；落盘在内存 cursor 赋值之后、读回挂 SetupAutomation。M1–M5 句子级吸收。状态仍待用户批准。
- 2026-09-09：用户原话「推进吧」批准 spec。头部回写已批准。
- 2026-09-09：提交 `git add docs/superpowers/specs/b349.md docs/superpowers/ledgers/2026-09-09-b349-spec-ledger.md docs/superpowers/reviews/b349-spec-review.md && git commit` → `[cards/B233.1-charter-7 9818f241] docs(B349): 批准消费正确性 spec` / `3 files changed, 380 insertions(+)`。随后只 amend 一次收本行。
- 2026-09-09：`handoff card move B349 spec` / `B352 spec` 均成功；note 已记同批开卡。

## 现状读数（功能线 `cards/B233.1-charter-7`）

- B233.6 contract §2.4：工作流消费镜像事件必须同时检查 node、attempt 与 source identity。拆解 P3 拍板 A：闸在 wakeconsumer，ledger 保持薄。
- 实现只核 envelope 的 node/attempt：`internal/agentd/wakeconsumer.go#acceptsCurrentWorkflowAttempt`。`currentWorkflowAttempt` 扫 `EvDispatched` 快照，不读 `source_target/source_task/source_seq`。
- `ledger.Event` 有三列（`internal/ledger/types.go#Event`）；`proto.LedgerEvent` 没有（`internal/proto/ledger.go:120-127`）。
- 投影丢列：`internal/ledger/api/api.go#eventWire`、`internal/agentd/ledgerapi.go#ledgerEventWire` 只抄 Seq/CardID/Type/Actor/Payload/CreatedAt。wakeconsumer 经 `autoLedger.EventsFromAsc` 拿到的是丢列后的 proto。
- `card wait` 走 `Store.Follow`，回调类型是 `ledger.Event`，stdout Encode 已带 source 三字段；但 `cardWaitEventActionable` 对 `task_mirrored` 只问 `WaitDeliveryPolicy`，不核 attempt / source。
- store 唯一索引 `(source_target, source_task, source_seq)`（`internal/ledger/store.go` PG `:231-235` / SQLite `:308-313`）只防同一源事件重插。跨 target 同卡事件按设计可入列。这不是「当前 attempt」闸。
- B233.6 plan 第 503 行曾把 source identity 推给 store 去重、S3 不扩 proto。review-3 指出缺口；用户 2026-09-08 裁决不阻塞 B233.6，独立评估（本卡）。
- B352：`Server.automationCursor` / `automationSeen` 在 `internal/agentd/server.go:198-199` 内存；`SetupAutomation` 把 seen 建成空 map。重启后 `consumeAutomationEventsOnce` 从 0 读全流。镜像水位 `MirrorWatermark`（账本 `MAX(source_seq)`）已持久，管的是不重写卡事件，不是不重复唤醒。
- 真机（B233.6 DUT `737e2203`）：重启后 B1–B4 各被重新唤醒一轮；空座跳过，占座会重复拉起。roadmap「来自 B233.6 真机验收」。
- `card wait` 起点是 `Store.MaxSeq()`，不重放历史。B352 只打自动化消费者。

## B349 contract 节点台账

- 2026-09-09：`git status --short --branch && git log -5 --oneline --decorate` 原始输出首行为 `## cards/B349-charter`，HEAD 为 `0d9673ed (origin/cards/B233.1-charter-7) docs(B349): 批准消费正确性 spec`；工作树无改动。`sed -n '1,260p' docs/superpowers/specs/b349.md` 原始头部状态为 `状态：已批准（2026-09-09，用户原话「推进吧」；独立审查修订已吸收）`。事实：上游状态位已批准，无需回写。
- 2026-09-09：`codegraph sym LedgerEvent` 实际命中 `m_proto_LedgerEvent`，文件 `internal/proto/ledger.go:119`，字段仅 `Seq/CardID/Type/Actor/Payload/CreatedAt`；同查询命中未改动的 `web/src/api/ledger.ts` 孪生。本卡只改 Go wire DTO，Web 类型不改。
- 2026-09-09：`codegraph sym Server.consumeAutomationEventsOnce` 实际命中 `n_agentd_Server_consumeAutomationEventsOnce`，签名为 `func (s *Server) consumeAutomationEventsOnce(ctx context.Context) (processed int, escalated bool, err error)`，`internal/agentd/wakeconsumer.go:211`；`codegraph sym Facade.EventsFromAsc` 命中 `func (f *Facade) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]proto.LedgerEvent, error)`，`internal/ledger/api/api.go:81`。
- 2026-09-09：`codegraph sym currentWorkflowAttempt` 与 `codegraph sym acceptsCurrentWorkflowAttempt` 实际返回“符号不在图中（图未覆盖或名字有误）”；源码查证为 `internal/agentd/wakeconsumer.go#Server.currentWorkflowAttempt`（当前返回 `attempt string, found bool, err error`）与 `#Server.acceptsCurrentWorkflowAttempt`。事实：两符号属于图覆盖债，不把图缺席当作代码缺席。
- 2026-09-09：`nl -ba internal/ledger/api/api.go | sed -n '145,156p'` 与 `nl -ba internal/agentd/ledgerapi.go | sed -n '111,116p'` 实际显示两处投影只赋 `Seq/CardID/Type/Actor/Payload/CreatedAt`，未赋 `SourceTarget/SourceTask/SourceSeq`。`internal/ledger/events.go#Store.EventsFromAsc`（当前 :63-103）已从数据库读取三列；事实：丢列发生在两处 wire 投影，不在 Store 读面。
- 2026-09-09：`nl -ba cmd/card_wait.go | sed -n '192,233p'` 实际显示 `cardWaitEventActionable` 对 `task_mirrored` 只解包 `task_type/payload` 并调用 `client.WaitDeliveryPolicy`，`runCardWait`（当前 `:56-190`）仍经 `Store.Follow` 回调编码；事实：card wait 的身份闸尚未落地，且未新造第三消费链。
- 2026-09-09：`nl -ba internal/agentd/server.go | sed -n '194,200p;2547,2567p'` 实际显示 `Server` 只有内存 `automationCursor int64`/`automationSeen map[int64]struct{}`，`SetupAutomation` 只装配 `cursor.New(filepath.Join(s.conf().DataDir, "room-cursors.json"))` 给房间服务；`nl -ba internal/agentd/scheddrain.go | sed -n '50,89p'` 实际显示自动化循环由 `StartAutomation` 调 `consumeAutomationEventsOnce`。事实：B352 需在 SetupAutomation 等价装配点读回独立 DataDir 文件，不复用 room cursor 或 `$HOME/.handoff/cursors`。
- 2026-09-09：`nl -ba internal/ledger/events.go | sed -n '109,139p'` 实际显示 `DispatchSnapshot` 的当前字段含 `Target`、`TaskID`、`Node`、`Attempt`；注释明确 `Attempt` 为本次派发返回的 TaskID。`internal/ledger/mirror.go:34-63` 实际显示 source 三列是镜像身份列，payload 不复制 source。事实：身份闸比较键冻结为当前快照 `Attempt+Target` 与事件 `SourceTask+SourceTarget`，`SourceSeq` 不参与身份比较。
- 2026-09-09：`nl -ba internal/ledger/follow.go | sed -n '18,70p'` 实际显示 `Store.Follow` 对非正 poll interval 使用 `2*time.Second`，每次 `EventsFromAsc` 上限 500；PG 使用 `pgx.Connect`、`LISTEN card_events`、`WaitForNotification(ctx)`，通知内容不解析且 ticker 兜底。事实：B349 不改 Follow/PG 读限、通知时序或轮询默认。
- 2026-09-09：`nl -ba internal/collab/cursor/cursor.go | sed -n '26,125p'` 实际显示房间 cursor 文件介质是 JSON、首次访问读回、每次写入使用 tmp+rename、权限目录 `0700`/文件 `0600`；事实：B352 自动化 cursor 采用同类本机文件介质，但文件名和键形状由本 contract 冻结，不与房间 cursor 共用。
- 2026-09-09：Ticket 0 已修改 `internal/proto/ledger.go`、`internal/ledger/api/api.go`、`internal/agentd/ledgerapi.go`，并新增 Facade/HTTP 投影穿缝断言；`gofmt -w internal/proto/ledger.go internal/ledger/api/api.go internal/agentd/ledgerapi.go internal/ledger/api/api_test.go internal/agentd/ledgerapi_test.go` 原始输出为空、退出码 0。
- 2026-09-09：`go test ./internal/ledger/api -run '^TestFacadeEventsFromAscPreservesSourceIdentity$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/ledger/api 0.078s`，退出码 0；`go test ./internal/agentd -run '^TestCardDetailProjectsMirroredSourceIdentity$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.136s`，退出码 0；`go test ./internal/proto -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/proto 0.006s`，退出码 0。事实：source 三列编码/两处投影测试本轮通过。
- 2026-09-09：`jq empty codegraph/target.json codegraph/best.json` 原始输出为空、退出码 0。事实：目标图与结构树 JSON 语法通过。
- 2026-09-09：`codegraph resolve --doc docs/superpowers/specs/b349-contract.md` 原始输出列出 20 个锚点，退出码 0；此前两个未覆盖私有符号锚已改为 `wakeconsumer.go:54-92`/`:97-130` 行段，坏锚清零。
- 2026-09-09：`codegraph validate` 原始输出 `{"containers":300,"domainDecls":23,"domains":23,"edgeIssues":null,"edges":5507,"entitiesWithoutLifecycle":2,"issues":null,"nodes":4499,"unscannedEntries":5,...}`，退出码 0。事实：目标图验证通过；基线仍报告 5 个未扫描入口。
- 2026-09-09：`codegraph check` 原始输出 `Error: 契约对照发现 2 处违规`，退出码 1；`fails` 原文为 `dead-contract d_execution→d_orchestration`（声明方向无活跃边）与 `dead-interface d_execution→d_orchestration`（接口 `ApprovalClient` 不存在）。事实：这是既有基线契约违规，本卡未修改该方向。
- 2026-09-09：`go build ./...` 原始输出为空、退出码 0；Ticket 0 本轮编译通过。
- 2026-09-09：`go vet ./internal/proto/ ./internal/ledger/api/ ./internal/agentd/` 原始输出为空、退出码 0。
- 2026-09-09：`go test ./internal/ledger/api -run '^TestFacadeEventsFromAscPreservesSourceIdentity$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/ledger/api 0.073s`、退出码 0；`go test ./internal/agentd -run '^TestCardDetailProjectsMirroredSourceIdentity$' -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.184s`、退出码 0；`go test ./internal/proto -count=1` 原始输出 `ok github.com/Xsxdot/handoff/internal/proto 0.003s`、退出码 0。
- 2026-09-09：`git commit -m "contract(B349): freeze source identity contract"` 原始输出 `[cards/B349-charter d56d3e58] contract(B349): freeze source identity contract` / `8 files changed, 403 insertions(+), 16 deletions(-)` / `create mode 100644 docs/superpowers/specs/b349-contract.md`；随后按台账纪律只 amend 一次收录本提交事实。
