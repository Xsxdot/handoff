# B349+B352 spec 台账

- 2026-09-09：两卡移 spec。B349「评估消费端 source identity」；B352「镜像重启全量重放按水位去重」。同批消费正确性。
- 2026-09-09：用户选 A。`proto.LedgerEvent` 补 source 三列（omitempty），投影停止丢列；wakeconsumer 在 node/attempt 之外核 source_target+source_task 对应当前派发。不扩 envelope JSON，不新增事件类型。B 绕 Facade、C 无代码收口弃选。
- 2026-09-09：用户选 A（card wait 同一道闸）。`task_mirrored` 只有 node/attempt/source 对应当前派发才算可动作；旧 attempt 留账本不打 stdout。闸只留 wakeconsumer 的 B 弃选。
- 2026-09-09：用户同意 A（B352 本机持久化 cursor）。水位写这台 agentd 的 DataDir，不进共享账本；seen 仍内存；重启从上次 seq 续拉（至少一次）。写共享账本的 B、启动 MAX(seq) 的 C 弃选。
- 2026-09-09：本机镜像 `source_target` 取 TaskLink.Target，可为空（`internal/ledgermirror/mirror.go` 约 :517–518；`mirror_test.go:546-548` 本机夹具期望空串）。当前派发 `DispatchSnapshot.Target` 亦可空（`internal/ledgerstep/runner_test.go:733`）。匹配规则必须允许两边都空，不能把空 target 当缺身份一刀切。
- 2026-09-09：图：`codegraph sym LedgerEvent` 命中 `m_proto_LedgerEvent`（`internal/proto/ledger.go:119`），字段无 Source*。`sym acceptsCurrentWorkflowAttempt` 不在图中。`who-calls consumeAutomationEventsOnce` 节点 id 为 `n_agentd_Server_consumeAutomationEventsOnce`。
- 2026-09-09：房间游标已是 DataDir 文件介质（`server.go:2567` `room-cursors.json`）。自动化水位同类：进程私有、不进账本。具体文件名归 contract。
- 2026-09-09：基线 `cards/B233.1-charter-7` @ `f2129a25`。spec 草稿 `docs/superpowers/specs/b349.md`。
- 2026-09-09：用户要求按纪律派独立 subagent 审 spec。已派 `01a084c3-a666-7fe2-9220-1bb0c2ebff9a`（只读，产物 `docs/superpowers/reviews/b349-spec-review.md`）。
- 2026-09-09：独立审查总判「修订后再批」。C1 一则、Important I1–I4、Minor M1–M5。协调者核活代码后全部成立，未推回。
- 2026-09-09：用户「吸收审查改稿」。C1/I1–I4 写入 `docs/superpowers/specs/b349.md`：card wait 当前身份只从事件所属卡 × envelope.node 的最新 EvDispatched 取；身份钉 snapshot.Attempt+Target；接缝锁双空 target 与 source_seq 不参与；废止 B233.6 plan §5.2 L493/L503，不退回 contract/P3；落盘在内存 cursor 赋值之后、读回挂 SetupAutomation。M1–M5 句子级吸收。状态仍待用户批准。
- 2026-09-09：用户原话「推进吧」批准 spec。头部回写已批准。
- 2026-09-09：提交 `git add docs/superpowers/specs/b349.md docs/superpowers/ledgers/2026-09-09-b349-spec-ledger.md docs/superpowers/reviews/b349-spec-review.md && git commit` → `[cards/B233.1-charter-7 9818f241] docs(B349): 批准消费正确性 spec` / `3 files changed, 380 insertions(+)`。随后只 amend 一次收本行。
- 2026-09-09：`handoff card move B349 spec` / `B352 spec` 均成功；note 已记同批开卡。

## 现状读数（功能线 `cards/B233.1-charter-7`）

- B233.6 contract §2.4：工作流消费镜像事件必须同时检查 node、attempt 与 source identity。拆解 P3 拍板 A：闸在 wakeconsumer，ledger 保持薄。
- 实现只核 envelope 的 node/attempt：`internal/agentd/wakeconsumer.go#acceptsCurrentWorkflowAttempt`。`currentWorkflowAttempt` 扫 `EvDispatched` 快照，不读 `source_target/source_task/source_seq`。
- `ledger.Event` 有三列（`internal/ledger/types.go#Event`）；`proto.LedgerEvent` 没有（`internal/proto/ledger.go:120-127`）。
- 投影丢列：`internal/ledger/api/api.go#eventWire`、`internal/agentd/ledgerapi.go#ledgerEventWire` 只抄 Seq/CardID/Type/Actor/Payload/CreatedAt。wakeconsumer 经 `autoLedger.EventsFromAsc` 拿到的是丢列后的 proto。
- `card wait` 走 `Store.Follow`，回调类型是 `ledger.Event`，stdout Encode 已带 source 三字段；但 `cardWaitEventActionable` 对 `task_mirrored` 只问 `WaitDeliveryPolicy`，不核 attempt / source。
- store 唯一索引 `(source_target, source_task, source_seq)`（`internal/ledger/store.go` PG `:231-235` / SQLite `:308-313`）只防同一源事件重插。跨 target 同卡事件按设计可入列。这不是「当前 attempt」闸。
- B233.6 plan 第 503 行曾把 source identity 推给 store 去重、S3 不扩 proto。review-3 指出缺口；用户 2026-09-08 裁决不阻塞 B233.6，独立评估（本卡）。
- B352：`Server.automationCursor` / `automationSeen` 在 `internal/agentd/server.go:198-199` 内存；`SetupAutomation` 把 seen 建成空 map。重启后 `consumeAutomationEventsOnce` 从 0 读全流。镜像水位 `MirrorWatermark`（账本 `MAX(source_seq)`）已持久，管的是不重写卡事件，不是不重复唤醒。
- 真机（B233.6 DUT `737e2203`）：重启后 B1–B4 各被重新唤醒一轮；空座跳过，占座会重复拉起。roadmap「来自 B233.6 真机验收」。
- `card wait` 起点是 `Store.MaxSeq()`，不重放历史。B352 只打自动化消费者。
