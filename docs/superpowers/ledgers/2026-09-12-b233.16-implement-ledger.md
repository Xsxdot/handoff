# B233.16 implement 台账（2026-09-12）

节点：implement（handoff 任务执行者）。执行已批准 plan `docs/superpowers/plans/b233.16-plan.md`
（提交 4bdd8bcd，起点 83311cf1）的 T2/T3/T4/T5。
本文件只记实验过程与原始读数；结论进最终报文。

## 0. 开工读数

```
$ git branch --show-current
cards/B233.16-charter-6
$ git log --oneline -3
4bdd8bcd plan(B233.16): 实现计划——T2 替身注入/T3 源码守卫/T4 行为回归/T5 图对账
83311cf1 breakdown(B233.16): 复核轮闭环——P1 独立复核、节点计数 16→17、拍板回写确认
f335c2b5 contract(B233.16): freeze wait-once named entry (P1=A) + boundary clarifications
$ git status --short
（空，工作树干净）
```

- 本轮**只新增测试代码**：4 个既有测试文件追加，未改任何生产文件（见 §6 变异后 `git diff` 干净）。
- 有界文件集（plan §2）：`cmd/execution_signature_lock_test.go`、`internal/agentd/execution_signature_lock_test.go`、
  `internal/ledgerstep/execution_signature_lock_test.go`、`cmd/execution_surface_test.go` + 本台账。未新增测试文件。

## 1. T2-CLI（`cmd/execution_signature_lock_test.go`）

import 追加 `bytes/encoding/json/errors/strings/sync/atomic`，末尾追加 T2 段（§3.2 逐字）。

**首跑编译红（plan 代码缺陷，见 §7 偏差账）**：

```
# github.com/Xsxdot/handoff/cmd [github.com/Xsxdot/handoff/cmd.test]
cmd/execution_signature_lock_test.go:297:3: unknown field waitEvent in struct literal of type waitStub
cmd/execution_signature_lock_test.go:347:3: unknown field followEvent in struct literal of type waitStub
FAIL	github.com/Xsxdot/handoff/cmd [build failed]
```

修正：把 `waitEvent`/`followEvent` 嵌进 `execStub` 字面量（Go 不允许在字面量里直接设被提升字段）后：

```
$ go test ./cmd -run 'TestCLIExecutionStubsDoNotImplementTransport|TestExecutionSignatureLocks|TestRunStopUsesInjected|TestRunReplyUsesInjected|TestRunContinueUsesInjected|TestDispatchTaskUsesInjected|TestResolveBareDisciplineUsesInjected|TestResolveCardDispatchDisciplineUsesInjected|TestCompensateCardDispatchUsesInjected|TestRunWaitOnceUsesInjected|TestRunUntilDoneUsesInjected|TestRunFollowUsesInjected|TestWarnIfTimeoutBelowStallUsesInjected|TestAutoSyncAfterWaitUsesInjected' -count=1
ok  	github.com/Xsxdot/handoff/cmd	0.091s
```

## 2. T2-agentd（`internal/agentd/execution_signature_lock_test.go`）

import 追加 `sync/atomic`、`time`，末尾追加 T2 段（§4.2 逐字）。

```
$ go test ./internal/agentd -run 'TestAgentdExecutionStubsDoNotImplementTransport|TestExecutionSignatureLocks|TestDispatchStepUsesInjected|TestProbeStatusUsesInjected|TestCompensateStepUsesInjected|TestPushWorkBranchViaUsesInjected' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	0.030s
```

## 3. T2-ledgerstep（`internal/ledgerstep/execution_signature_lock_test.go`）

import 追加 `encoding/json/sync/atomic/time/client/proto`，末尾追加 T2 段（§5.2 逐字）。

```
$ go test ./internal/ledgerstep -run 'TestStepClientStubDoesNotImplementTransport|TestStepRunnerClientsInjectsStepClientOnly|TestExecutionSignatureLocks' -count=1
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.006s
$ go test ./internal/ledgerstep -run 'TestRunnerLocalClientUsesWaitAndDiffWire|TestB2336TerminalRun' -count=1
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.608s
```

既有真实 HTTP 链路用例（`TestRunnerLocalClientUsesWaitAndDiffWire`）保持绿、未删改。

## 4. T3（`cmd/execution_surface_test.go`）

import 追加 `fmt/go/scanner/go/token/regexp`，末尾追加 §6.2 全段。

```
$ go test ./cmd -run 'TestCLIExecutionCommandsDoNotHandRollHTTP|TestCLIExecutionSurfaceHoldsNarrowDependencies|TestCLIExecutionSurfaceGuardHasTeeth' -count=1
ok  	github.com/Xsxdot/handoff/cmd	0.011s
```

## 5. 变异自验（先确认编译过，再数红；两发均已整块还原、diff 干净）

### 5.1 T2 注入面：临时让生产 `runStop` 忽略入参（`cmd/stop.go`）

变异体（可编译）：

```go
func runStop(cmd *cobra.Command, c client.ExecutionClient, taskID string) error {
	_ = c
	removed := false
	fmt.Fprint(cmd.OutOrStdout(), stopOutcomeLine(taskID, removed))
	return nil
}
```

```
$ go build ./cmd/
build-exit=0                       # 变异编译通过（①排除）
$ go test ./cmd -run 'TestRunStopUsesInjectedExecutionClient' -count=1
--- FAIL: TestRunStopUsesInjectedExecutionClient (0.00s)
    execution_signature_lock_test.go:188: Stop 调用次数 = 0，want 1（替身必须真的被注入）
FAIL
```

结论：该用例的牙是调用计数，生产忽略入参即红（不是只证明「能编译」）。还原后 `git diff -- cmd/stop.go` 为空、用例回绿。

### 5.2 T3 守卫：临时在 `cmd/stop.go` 加 `var _ *client.Client`（包级）

```
$ go build ./cmd/
build-exit=0                       # 变异编译通过
$ go test ./cmd -run 'TestCLIExecutionSurfaceHoldsNarrowDependencies' -count=1
--- FAIL: TestCLIExecutionSurfaceHoldsNarrowDependencies (0.00s)
    execution_surface_test.go:139: 执行面文件不得持聚合依赖（白名单仅工厂符号 map[newTargetClient:true newTargetClientNamed:true targetClient:true]）：[stop.go:6:<package>]
FAIL
```

结论：源码守卫对非白名单聚合真会红。还原后 diff 干净。

## 6. T4 行为回归（plan §7）

```
$ gofmt -l cmd internal/agentd internal/ledgerstep
internal/agentd/coordinator_home.go        # 存量：HEAD 版本即非 gofmt 干净（见下），非本卡引入
$ git show HEAD:internal/agentd/coordinator_home.go | gofmt -l
<standard input>                            # 证实 HEAD 存量
$ go build ./...
exit=0
$ go vet ./cmd/ ./internal/agentd/ ./internal/ledgerstep/
exit=0
```

定向复跑（plan §7.2）：

```
$ go test ./cmd -run 'TestStopCLIRetainCopy|TestDispatch|TestWait|TestWaitUntilDone|TestReply|TestContinue|TestCardDispatch|TestCard|TestExecutionSignatureLocks' -count=1
ok  	github.com/Xsxdot/handoff/cmd	13.511s
$ go test ./internal/ledgerstep -run 'TestRunner|TestB2336|TestClientFinalMessage|TestWire' -count=1
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	2.763s
$ go test ./internal/agentd -run 'TestCardStep|TestStartCardStep|TestDisciplineTargetCap|TestB351|TestB23310|TestProbe' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	11.161s
$ go test ./internal/client -run 'TestDispatchFrozenIdentityJSONGolden|TestDispatchFrozenIdentityJSONOmissionAndHomeDirTriState' -v -count=1
--- PASS: TestDispatchFrozenIdentityJSONGolden (0.00s)
--- PASS: TestDispatchFrozenIdentityJSONOmissionAndHomeDirTriState (0.00s)
ok  	github.com/Xsxdot/handoff/internal/client	0.007s
```

整包/全量：

```
$ go test ./internal/ledgerstep -count=1
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	10.142s

$ go test ./cmd -count=1
--- FAIL: TestRepoContractGate (0.04s)
    graph_gate_test.go:38: 契约违规 [over-budget] d_cli->d_workspace 直调 11 条超出预算 9
    graph_gate_test.go:38: 契约违规 [over-budget] d_gateway->d_workspace 直调 17 条超出预算 1
    graph_gate_test.go:38: 契约违规 [over-budget] d_orchestration->d_workspace 直调 49 条超出预算 19
    graph_gate_test.go:38: 契约违规 [over-budget] d_workspace->d_orchestration 直调 3 条超出预算 2
    graph_gate_test.go:38: 契约违规 [over-budget] d_workspace->d_protocol 直调 23 条超出预算 3
    graph_gate_test.go:40: legacy 命中: map[...]，warn 139 条     # Logf，随失败一并打印
FAIL	github.com/Xsxdot/handoff/cmd	34.803s

$ go test ./internal/agentd -count=1
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.23s)
    scheddispatch_test.go:531: 第 1 条漂移：
        got=GOLDEN|2|dispatched|web:test|[...]
        want=GOLDEN|2|comment|web:test|[{"body":"挂账 task T-fake-01@ftm（implement）"},{"kind":"普通"}]
FAIL	github.com/Xsxdot/handoff/internal/agentd	194.864s

$ go test ./... -count=1
...（除下列两包外全 ok；cmd/agentd 失败项与上面逐字一致）
FAIL	github.com/Xsxdot/handoff/cmd	44.561s
FAIL	github.com/Xsxdot/handoff/internal/agentd	226.362s
FAIL
```

全量**唯一**两条红 = plan §7.4 登记的两条基线存量红（`TestRepoContractGate` 5 条 workspace over-budget、
`TestLegacyNodeEventSequenceUnchanged` 事件序列漂移），无第三条。本卡只加测试、无生产 diff，
故归因判据采「红的内容与 §0/plan 基线逐字一致，且与新增测试无调用关系」。

## 7. T5 契约图对账（plan §8）

```
$ codegraph --repo . check                       # exit=1
fails = 5（逐条：d_cli/d_gateway/d_orchestration→d_workspace、d_workspace→d_orchestration/d_protocol）
warns = 139；"kind" 行 144 = 5 + 139
Error: 契约对照发现 5 处违规

$ codegraph --repo . check --view cards-B233.16-charter   # exit=1
与不带视图输出 byte-identical（diff = IDENTICAL）；同 5 fails / 139 warns

$ codegraph --repo . validate                    # exit=1
issue 行 33；md5 = 3cf8e46e9055631f934590acb5c5627a
$ git stash push -- <4 测试文件> ；codegraph --repo . validate
md5 = 3cf8e46e9055631f934590acb5c5627a（与改动后 byte-identical）
→ validate 未新增 issues；随后 git stash pop 恢复
```

图覆盖债（与 plan §0 基线逐字一致）：

```
sym ExecutionClient / runStop / runReply / runWaitOnce / dispatchStep / probeStatus / StepClient
  → Error: 符号不在图中（近似候选 []）
sym runUntilDone → 命中（baseline 视图）
```

`codegraph/target.json` 复核（只读，未改）：`d_cli→d_transport` budget=10 entries=[client 实体, client.Client]；
`d_gateway→d_transport` budget=9 entries=[targetclient.Pool, client.Client, client 实体]；
无 `d_ledger→d_transport`。与 contract #52–#56 一致。

## 8. 填补级偏差账（plan 逐字代码 vs 实际落地）

1. **plan §3.2 `waitStub` 字面量非法**：plan 在 `&waitStub{ ... waitEvent: ..., followEvent: ... }` 里直接设置
   被嵌入 `*execStub` 提升的字段，Go 不允许（编译红见 §1）。落地改为把二者嵌进 `execStub: &execStub{waitEvent: ...}` /
   `{followEvent: ...}`，语义不变（哨兵仍是同一替身字段）。属测试夹具写法修正，不动生产。
2. **plan §3.2 `dispatchStub`/`waitStub` 等组合替身的 `var _ X = (*Y)(nil)` 断言**：逐字落地，编译通过。
3. **plan §4.2 map 字面量对齐**：仅 gofmt 空白差异，无逻辑偏差。
4. 其余 T2/T3 代码块逐字落地，签名与生产定义（本节点已核：`cmd/dispatch.go:116/123/199`、`cmd/wait.go:50/139/186/255/299/353`、
   `cmd/card_dispatch.go:247/275/282`、`internal/agentd/cardstep.go:41-60/79/87/99`、`internal/ledgerstep/runner.go:23/34/387/414/448`、
   `internal/ledgerstep/wire.go:122`）一致。

## 9. 收尾

- 产出物：4 个测试文件扩展 + 本台账；无生产改动；未提交前 `git status` 仅这 4 个测试文件 + 本台账。
- 提交事实见 §10。

## 10. 提交事实（历史读数；作证人 amend 前）

```
$ git add cmd/execution_signature_lock_test.go cmd/execution_surface_test.go \
    internal/agentd/execution_signature_lock_test.go internal/ledgerstep/execution_signature_lock_test.go \
    docs/superpowers/ledgers/2026-09-12-b233.16-implement-ledger.md
$ git commit -m "test(B233.16): T2 替身注入/T3 源码守卫/T4 回归/T5 图对账"
[cards/B233.16-charter-6 4fae45bb] test(B233.16): T2 替身注入/T3 源码守卫/T4 回归/T5 图对账
 5 files changed, 1010 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-12-b233.16-implement-ledger.md
```

随后按台账惯例 amend 一次，把本「提交事实」段收进同批提交（amend 换 hash 是 git 的事实，非漏记）。
收口判据是工作树干净，不是文件里的 hash 等于 HEAD。
