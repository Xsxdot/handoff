# B351 implement 节点台账

日期：2026-09-09
节点：implement（L3 轻档）
工作树：`/root/.handoff/worktrees/8a0fada5`
分支：`cards/B351-charter-4`
起点 HEAD：`ba329088 docs: add B351 implementation plan`
上游：`docs/superpowers/plans/b351-plan.md`、`docs/superpowers/specs/b351-contract.md`

## 事实流水

- 2026-09-09：读取 handoff 技能文件；未调用 handoff CLI、未派发子任务、未启动 executor。
- 2026-09-09：工作树初始干净；当前分支 `cards/B351-charter-4`，HEAD `ba329088 docs: add B351 implementation plan`。
- 2026-09-09：Task 1 基线命令 `go test ./internal/ledger -run '^(TestB351|TestPurposeRounds|TestDDLDialectParity|TestOpenCreatesSchema)' -count=1`；退出 0，原始输出：`ok  \tgithub.com/Xsxdot/handoff/internal/ledger\t0.403s`。
- 2026-09-09：Task 2 基线命令 `go test ./internal/client -run '^(TestB351|TestStopParsesWorktreeRemoved|TestReclaimOnOldAgentdReportsUnsupported|TestReclaimUnknownTaskIsNotMistakenForUnsupported|TestReclaimForceCarriesIntoRequestBody)' -count=1`；退出 0，原始输出：`ok  \tgithub.com/Xsxdot/handoff/internal/client\t0.007s`。
- 2026-09-09：Task 3 基线命令 `go test ./internal/ledgerstep -run '^(TestViaTemplateSecondRoundGetsNumberedBranch|TestViaTemplateEmptyTargetIsLocal|TestB2336DispatchFailureRollsBackSnapshot|TestViaTemplateStopsSnapshotAfterWriteGateCloses)' -count=1`；退出 0，原始输出：`ok  \tgithub.com/Xsxdot/handoff/internal/ledgerstep\t0.324s`。
- 2026-09-09：Task 4 基线命令 `go test ./cmd ./internal/agentd -run '^(TestB351|TestCardDispatch|TestStartCardStep|TestStepTransport)' -count=1`；退出 0，原始输出：`ok  \tgithub.com/Xsxdot/handoff/cmd\t3.458s`、`ok  \tgithub.com/Xsxdot/handoff/internal/agentd\t1.236s`。
- 2026-09-09：基线全量编译命令 `go build ./...`；退出 0，原始输出为空。
- 2026-09-09：Task 1 声明缝红测 `go test ./internal/ledger -run '^(TestB351|TestPurposeRounds|TestB351DispatchRoundsDDLDialectParity|TestDDLDialectParity|TestOpenCreatesSchema)' -count=1`；退出 1。原始断言：`ddl_parity_test.go:73: DDL 缺少 B351 片段 "card_dispatch_rounds"`（postgres/sqlite）；`events_test.go:71: 记录 implement 失败轮次 1: ledger: dispatch round not wired`；`store_test.go:29: 表 card_dispatch_rounds 不存在: SQL logic error: no such table: card_dispatch_rounds (1)`。
- 2026-09-09：Task 1 绿测与收尾编译 `gofmt -w internal/ledger/store.go internal/ledger/dispatch_rounds.go internal/ledger/events.go internal/ledger/store_test.go internal/ledger/events_test.go internal/ledger/ddl_parity_test.go && go test ./internal/ledger -run '^(TestB351|TestPurposeRounds|TestB351DispatchRoundsDDLDialectParity|TestDDLDialectParity|TestOpenCreatesSchema)' -count=1 && go build ./...`；退出 0，原始测试输出：`ok  \tgithub.com/Xsxdot/handoff/internal/ledger\t0.481s`；`go build ./...` 无输出。
- 2026-09-09：Task 2 声明缝红测 `gofmt -w internal/client/client_test.go && go test ./internal/client -run '^(TestB351|TestB351StopAndReclaimOrderAndSoftErrors|TestStopParsesWorktreeRemoved|TestReclaimOnOldAgentdReportsUnsupported|TestReclaimUnknownTaskIsNotMistakenForUnsupported|TestReclaimForceCarriesIntoRequestBody)' -count=1`；退出 1。新增测试原始断言：Stop 404/409、Reclaim 404/旧端均得到 `client: stop and reclaim not wired`；Stop 500 未发请求（`请求顺序 = []，want [POST /api/tasks/task-b351/stop]`）；Stop 200 + Reclaim 500 未发请求（`请求顺序 = []，want [POST /api/tasks/task-b351/stop POST /api/tasks/task-b351/reclaim]`）；旧 agentd 同样得到 `client: stop and reclaim not wired`。
- 2026-09-09：Task 2 绿测与收尾编译 `gofmt -w internal/client/compensate.go internal/client/client_test.go && go test ./internal/client -run '^(TestB351|TestB351StopAndReclaimOrderAndSoftErrors|TestStopParsesWorktreeRemoved|TestReclaimOnOldAgentdReportsUnsupported|TestReclaimUnknownTaskIsNotMistakenForUnsupported|TestReclaimForceCarriesIntoRequestBody)' -count=1 && go build ./...`；退出 0，原始测试输出：`ok  \tgithub.com/Xsxdot/handoff/internal/client\t0.009s`；`go build ./...` 无输出。
- 2026-09-09：Task 3 声明缝红测 `gofmt -w internal/ledgerstep/dispatch_test.go && go test ./internal/ledgerstep -run '^(TestB351TransportFailureDoesNotCompensate|TestViaTemplateSecondRoundGetsNumberedBranch|TestViaTemplateEmptyTargetIsLocal|TestB2336DispatchFailureRollsBackSnapshot|TestViaTemplateStopsSnapshotAfterWriteGateCloses)' -count=1`；退出 1。新增失败轮次/补偿断言真实失败：`dispatch_test.go:307: 落账冲突后的 implement 轮次 count=1 err=<nil>，want count=2 err=nil`；`dispatch_test.go:378: 写闸关闭后的 implement 轮次 count=0 err=<nil>，want count=1 err=nil`。输出同时显示原有第二轮、空 target 与传输失败回归继续执行。
- 2026-09-09：Task 3 绿测与收尾编译 `gofmt -w internal/ledgerstep/dispatch.go internal/ledgerstep/dispatch_test.go && go test ./internal/ledgerstep -run '^(TestB351TransportFailureDoesNotCompensate|TestViaTemplateSecondRoundGetsNumberedBranch|TestViaTemplateEmptyTargetIsLocal|TestB2336DispatchFailureRollsBackSnapshot|TestViaTemplateStopsSnapshotAfterWriteGateCloses)' -count=1 && go build ./...`；退出 0，原始测试输出：`ok  \tgithub.com/Xsxdot/handoff/internal/ledgerstep\t0.486s`；`go build ./...` 无输出。
- 2026-09-09：Task 4 CLI 声明缝红测在补齐 implement 纪律后真实执行：`go test ./cmd -run '^TestB351CardDispatchAssemblesStopAndReclaimCompensator$' -count=1`；退出 1。原始失败：`card_dispatch_test.go:308: CLI 补偿请求 = []，want [POST /api/tasks/T-b351-cli/stop POST /api/tasks/T-b351-cli/reclaim]`；日志同时显示 `派发补偿钩子未装配`。
- 2026-09-09：Task 4 agentd 首次红测使用 `newLedgerEnv` 时被环境 skip，原始输出：`PTY 测试根目录不可用；source=tmp root=\"/tmp\" 实测 socket=46 字节，上限=100，原因=mkdir /tmp/handoff-pty-3064884197: read-only file system；source=repo-dot root=\"/root/.handoff/worktrees/8a0fada5/.pty-test-root/handoff-pty-1308516255\" 实测 socket=113 字节，上限=100`。未据此判定通过；测试改用计划已列的 `newNoPTYLedgerEnv` 并显式设置配置路径。
- 2026-09-09：Task 4 agentd 声明缝红测 `gofmt -w internal/agentd/cardstep_discipline_test.go && go test ./internal/agentd -run '^TestB351AgentdStartCardStepAssemblesCompensator$' -count=1`；退出 1。原始断言：`cardstep_discipline_test.go:235: startCardStep 装配的 Dispatcher.Compensate 不应为空`。
- 2026-09-09：Task 4 生产装配钩子接入后，首次 agentd 回归真实收到 `[POST /api/tasks POST /api/tasks/T-fake-01/stop POST /api/tasks/T-fake-01/reclaim]`；测试原期望漏计 Transport 的首个 POST，原始失败：`agentd 补偿请求 = [POST /api/tasks POST /api/tasks/T-fake-01/stop POST /api/tasks/T-fake-01/reclaim]，want [POST /api/tasks/T-fake-01/stop POST /api/tasks/T-fake-01/reclaim]`。已将测试期望修正为完整顺序，未削弱 Stop→Reclaim 断言。
- 2026-09-09：Task 4 单项绿测与收尾编译 `gofmt -w cmd/card_dispatch.go internal/agentd/cardstep.go cmd/card_dispatch_test.go cmd/dispatch_discipline_test.go internal/agentd/cardstep_discipline_test.go && go test ./cmd -run '^TestB351CardDispatchAssemblesStopAndReclaimCompensator$' -count=1 && go test ./internal/agentd -run '^TestB351AgentdStartCardStepAssemblesCompensator$' -count=1 && go build ./...`；退出 0，原始测试输出：`ok  \tgithub.com/Xsxdot/handoff/cmd\t0.132s`、`ok  \tgithub.com/Xsxdot/handoff/internal/agentd\t0.161s`；`go build ./...` 无输出。
- 2026-09-09：Task 4 计划范围回归 `go test ./cmd -run '^(TestB351CardDispatchAssemblesStopAndReclaimCompensator|TestCardDispatch)' -count=1`；退出 0，原始输出：`ok  \tgithub.com/Xsxdot/handoff/cmd\t4.031s`。
- 2026-09-09：Task 4 计划范围回归 `go test ./internal/agentd -run '^(TestB351AgentdStartCardStepAssemblesCompensator|TestStartCardStep|TestStepTransport)' -count=1`；退出 0，原始输出：`ok  \tgithub.com/Xsxdot/handoff/internal/agentd\t1.252s`。
- 2026-09-09：B351 跨接缝回归 `go test ./internal/ledgerstep ./internal/ledger ./internal/client ./cmd ./internal/agentd -run '^TestB351' -count=1`；退出 0，原始输出：`ok  \tgithub.com/Xsxdot/handoff/internal/ledgerstep\t0.135s`、`ok  \tgithub.com/Xsxdot/handoff/internal/ledger\t0.165s`、`ok  \tgithub.com/Xsxdot/handoff/internal/client\t0.010s`、`ok  \tgithub.com/Xsxdot/handoff/cmd\t0.170s`、`ok  \tgithub.com/Xsxdot/handoff/internal/agentd\t0.224s`。
- 2026-09-09：变异 1 前用 `rg -F -c 'total := succeeded + failed' internal/ledger/events.go` 确认唯一命中 `1`；将加法改为 `total := succeeded`。行为断言 `go test ./internal/ledger -run '^TestB351PurposeRoundsAddsFailedRoundsByPurpose$' -count=1` 真实变红：`implement 轮次 count=1 err=<nil>，want count=3 err=nil`；编译 `go build ./...` 退出 0、无输出；相关全集测试真实失败，唯一 `--- FAIL` 为 additive 计数断言，原始总结：`FAIL github.com/Xsxdot/handoff/internal/ledger 0.387s`。随后恢复原加法。
- 2026-09-09：变异 2 前用 `rg -F -c 'if hasHTTPStatus(err, http.StatusNotFound) {' internal/client/compensate.go` 确认唯一命中 `1`；将 Stop 404 守卫改为 Stop 409。行为断言 `go test ./internal/client -run '^TestB351StopAndReclaimOrderAndSoftErrors/stop_404$' -count=1` 真实变红，原始断言：`应软成功，实得 中止任务: 状态码 404: {\"error\":\"stop failure\"}`；编译 `go build ./...` 退出 0、无输出；相关全集测试真实出现 2 个 `--- FAIL`（Stop 404 软成功、Stop 409 未进入 Reclaim），原始总结：`FAIL github.com/Xsxdot/handoff/internal/client 0.010s`。随后恢复 Stop 404 守卫。

- 2026-09-09：约束图验证 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B351-charter resolve --doc docs/superpowers/specs/b351-breakdown.md` 退出 0；输出中 B351 的 `ViaTemplate`、`PurposeRounds`、`StopAndReclaim`、`targetClient`、`startCardStep`、`clientForTarget`、`DispatchCompensator`、`RecordDispatchRound`、`runStepDispatch` 等锚点均为 `ok` 或 `moved`。
- 2026-09-09：收尾全包触及包测试 `go test ./internal/ledger ./internal/client ./internal/ledgerstep ./cmd ./internal/agentd -count=1` 退出 1；ledger/client/ledgerstep/agentd 均输出 `ok`，cmd 输出 `FAIL`，后续单独核实为仓库既有 `TestRepoContractGate` 失败。
- 2026-09-09：收尾全量编译 `go build ./...` 退出 0，无输出。
- 2026-09-09：cmd 本卡回归 `go test ./cmd -run 'TestB351|TestCardDispatch' -count=1` 退出 0，原始输出 `ok  github.com/Xsxdot/handoff/cmd 4.139s`。
- 2026-09-09：`TestRepoContractGate` 单独复核退出 1，原始失败为 `dead-contract`（`d_execution→d_orchestration`）、`dead-interface`（`ApprovalClient`）及 `legacy 命中 ... warn 147 条`；未改动契约图。
- 2026-09-09：最终全量测试 `go test ./... -count=1` 退出 1；原始结尾输出为 cmd：`FAIL github.com/Xsxdot/handoff/cmd 44.313s`，其余触及包与 agentd 等包输出 `ok`，最终 `FAIL`；失败内容与单独复核的既有 `TestRepoContractGate` 一致。
- 2026-09-09：最终格式检查 `gofmt -d cmd/card_dispatch.go cmd/card_dispatch_test.go cmd/dispatch_discipline_test.go internal/agentd/cardstep.go internal/agentd/cardstep_discipline_test.go internal/client/client_test.go internal/client/compensate.go internal/ledger/ddl_parity_test.go internal/ledger/dispatch_rounds.go internal/ledger/events.go internal/ledger/events_test.go internal/ledger/store.go internal/ledger/store_test.go internal/ledgerstep/dispatch.go internal/ledgerstep/dispatch_test.go` 退出 0，无输出。
- 2026-09-09：最终 B351 回归 `go test ./internal/ledgerstep ./internal/ledger ./internal/client ./cmd ./internal/agentd -run '^TestB351' -count=1` 退出 0；原始输出：ledgerstep `ok ... 0.170s`、ledger `ok ... 0.204s`、client `ok ... 0.012s`、cmd `ok ... 0.141s`、agentd `ok ... 0.199s`。
- 2026-09-09：最终全量编译 `go build ./...` 退出 0，无输出。
- 2026-09-09：最终 `git diff --check` 退出 0；`git diff -- cmd/card_node.go` 无输出，direct card_node 路径未修改；工作树仅包含本卡实现、测试和台账文件。
- 2026-09-09：提交命令 `git commit -m "feat: reclaim orphan dispatch rounds"`；原始输出：`[cards/B351-charter-4 81264546] feat: reclaim orphan dispatch rounds`、`16 files changed, 701 insertions(+), 31 deletions(-)`、创建台账文件。

## 偏差

- 首次并行启动基线命令在 30 秒等待窗口内未返回可判定结果；未据此下结论，随后逐条重跑并记录以上真实结果。
- Task 4 组装级测试在本机受限文件系统下使用 `newNoPTYLedgerEnv`，因为本测试不依赖 PTY；未修改生产 PTY 语义。

- 2026-09-09（review 修复回合）：当前工作树起点为分支 `cards/B351-charter-5`、HEAD `ff884f77 feat: reclaim orphan dispatch rounds`，工作树干净；本回合只处理 B351 review findings，不调用 handoff CLI、不派发子任务、不启动 executor。
- 2026-09-09（review 修复回合）：`go test ./internal/ledgerstep -run '^(TestB2336DispatchFailureRollsBackSnapshot|TestViaTemplateStopsSnapshotAfterWriteGateCloses|TestViaTemplateSecondRoundGetsNumberedBranch|TestViaTemplateNodePurposeTakesReviewPath|TestViaTemplateEmptyTargetIsLocal)$' -count=1` 退出 0，原始输出：`ok   github.com/Xsxdot/handoff/internal/ledgerstep 0.631s`。
- 2026-09-09（review 修复回合）：`go test ./internal/ledger -run '^(TestB351PurposeRoundsAddsFailedRoundsByPurpose|TestWorkBranchSkipsReviewRounds|TestPurposeRoundsCountsPerPurpose)$' -count=1` 退出 0，原始输出：`ok   github.com/Xsxdot/handoff/internal/ledger 0.417s`。
- 2026-09-09（review 修复回合）：`go build ./...` 退出 0，原始输出为空。
- 2026-09-09（review 修复回合，变异自验）：`rg -F -c 'total := succeeded + failed' internal/ledger/events.go` 命中 `1`；将唯一命中临时改为 `total := succeeded - failed`。
- 2026-09-09（review 修复回合，变异自验）：行为先验 `go test ./internal/ledger -run '^TestB351PurposeRoundsAddsFailedRoundsByPurpose$' -count=1` 真实失败，原始断言：`implement 轮次 count=-1 err=<nil>，want count=3 err=nil`；不是未运行测试。
- 2026-09-09（review 修复回合，变异自验）：变异后 `go build ./...` 退出 0，原始输出为空，故该变异可编译；相关测试 `go test ./internal/ledger -run '^(TestB351PurposeRoundsAddsFailedRoundsByPurpose|TestPurposeRoundsCountsPerPurpose|TestDDLDialectParity|TestB351DispatchRoundsDDLDialectParity)$' -count=1` 退出 1，唯一断言失败为 `TestB351PurposeRoundsAddsFailedRoundsByPurpose`，末段为 `FAIL github.com/Xsxdot/handoff/internal/ledger 0.278s`、`FAIL`。
- 2026-09-09（review 修复回合，变异自验）：已恢复 `total := succeeded + failed` 原实现；变异不进入交付差异。
- 2026-09-09（review 修复回合，恢复后）：`go test ./internal/ledgerstep -count=1` 退出 0，原始输出：`ok   github.com/Xsxdot/handoff/internal/ledgerstep 11.567s`。
- 2026-09-09（review 修复回合，恢复后）：`go test ./internal/ledgerstep ./internal/ledger ./internal/client ./cmd ./internal/agentd -run '^TestB351' -count=1` 退出 0；原始输出依次为 ledgerstep `ok ... 0.677s`、ledger `ok ... 0.141s`、client `ok ... 0.014s`、cmd `ok ... 0.204s`、agentd `ok ... 0.241s`。
- 2026-09-09（review 修复回合，恢复后）：`go build ./...` 退出 0，原始输出为空；`git diff --check` 退出 0、无输出；`git diff -- cmd/card_node.go` 退出 0、无输出；工作树仅有本回合台账、`internal/ledgerstep/dispatch.go` 注释和 `dispatch_test.go` 测试差异。
- 2026-09-09（review 修复回合）：提交命令 `git add docs/superpowers/ledgers/2026-09-09-b351-implement-ledger.md internal/ledgerstep/dispatch.go internal/ledgerstep/dispatch_test.go && git commit -m "test: cover orphan dispatch compensation edges"`；原始输出：`[cards/B351-charter-5 0ffc2359] test: cover orphan dispatch compensation edges`、`3 files changed, 224 insertions(+), 5 deletions(-)`。随后按规则只 amend 一次收录本事实，不回写 amend 后 hash。
- 2026-09-09（review 修复回合）：在 `internal/ledgerstep/dispatch_test.go` 新增并扩展声明缝反例，覆盖补偿错误保留原始落账错误、Compensate=nil 仍记轮次、RecordDispatchRound 失败仍调用补偿、WriteGate/落账失败不新增 `EvDispatched`/`card_tasks` 且 WorkBranch/CountRounds 不变、失败 review 轮次编号与失败后实现第三次分支 `-3`；生产行为测试前未改动。
- 2026-09-09（review 修复回合）：`gofmt -w internal/ledgerstep/dispatch_test.go && go test ./internal/ledgerstep -run '^(TestB2336DispatchFailureRollsBackSnapshot|TestViaTemplateStopsSnapshotAfterWriteGateCloses|TestB351CompensationErrorPreservesOriginalLedgerError|TestB351NilCompensatorStillRecordsRound|TestB351RoundWriteFailureStillCompensates|TestB351FailedImplementRoundThenSuccessUsesThirdBranch|TestB351FailedReviewRoundUsesPurposeReviewNumber)$' -count=1` 退出 0，原始输出：`ok   github.com/Xsxdot/handoff/internal/ledgerstep 0.837s`。
- 2026-09-09（review 修复回合）：修正 `internal/ledgerstep/dispatch.go` 中“远端 task 仍由真机项回收”过时注释，明确已创建 task 由注入的 `Compensate` best-effort 回收且不覆盖本地原始错误。
- 2026-09-09（review 修复回合）：`go test ./internal/ledgerstep -count=1` 退出 0，原始输出：`ok   github.com/Xsxdot/handoff/internal/ledgerstep 11.789s`。
- 2026-09-09（review 修复回合）：`go test ./internal/ledgerstep ./internal/ledger ./internal/client ./cmd ./internal/agentd -run '^TestB351' -count=1` 退出 0；原始输出依次为 ledgerstep `ok ... 0.618s`、ledger `ok ... 0.137s`、client `ok ... 0.007s`、cmd `ok ... 0.193s`、agentd `ok ... 0.229s`。
- 2026-09-09（review 修复回合）：`go build ./...` 退出 0，原始输出为空。
