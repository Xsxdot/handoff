# codegraph 目标图与契约对照执行记账

职责：记录本分支 Task 1–8 的双裁决、修复轮次、提交范围与亲自执行的验证结果。
边界：Task 9 的用户级配置改动归审核者本机执行，不在本账记录。

- 基线：`5a3a40d5`（实现计划提交），当前分支 `feat/codegraph-target-check`，开工时工作树干净。
- Task 1 / 完成裁决：spec 符合（Target/TargetMeta/TargetDomain/Assignment/Contract 模型、真实 target.json 加载、缺失显式报错、路径/type/引用/预算校验均落地）；代码质量符合（internal/codegraph 不依赖 handoff 内部包，职责边界注释与导出 API 注释齐全，图内路径比较未引入 filepath）。验证：`go test ./internal/codegraph/ -run 'Target|ContractBudget' -count=1` 实际通过；`go test ./internal/codegraph/ -count=1` 实际通过；`gofmt -l .` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 1 提交）。
- Task 2 / 完成裁决：spec 符合（assignments 精确指派优先于 paths，支持精确路径与 `dir/**` 整段前缀，未命中返回空串）；代码质量符合（路径全程按 `/` 进行字符串比较，方法职责与 target 模型边界清晰，测试覆盖图外与相似前缀）。验证：`go test ./internal/codegraph/ -run TestDomainOf -count=1` 实际通过；`go test ./internal/codegraph/ -count=1` 实际通过；`gofmt -l .` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 2 提交）。
- Task 3 / 完成裁决：spec 符合（Graph/Diff/View 的 implements wire 字段、基线/增量合并状态、基线与 diff 两侧引用完整性校验、真实 JSON 穿透测试与扫描配方文档均落地）；代码质量符合（implements 与 call 边表分列保持 wire 兼容，非法新增 implements 引用沿既有 Merge 宽容边界跳过，校验报文含语义上下文，internal/codegraph 仍无 handoff 内部依赖）。验证：`go test ./internal/codegraph/ -count=1` 实际通过；`go test ./cmd/ -count=1` 实际通过；`gofmt -l .` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 3 提交）。
- Task 4 / 完成裁决：spec 符合（跨域 call 入口声明/legacy 预算/无契约方向、implements 反向接口契约、组装点豁免、deleted 跳过、图外与死规则告警、稳定排序均实现）；代码质量符合（Check 纯函数无 I/O/日志，Report 显式初始化空切片与预算 map，路径和边状态判定职责集中）。验证：`go test ./internal/codegraph/ -run 'TestCheck' -count=1` 实际通过；`go test ./internal/codegraph/ -count=1` 实际通过；`gofmt -l .` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 4 提交）。
- Task 5 / 完成裁决：spec 符合（`graph check [--repo] [--view]` 加载并校验 target、复用视图合并与 Check、stdout 输出 Report 后以 fail 返回非零、缺失 target 显式拒绝、命令复用后 reset 状态）；代码质量符合（CLI 仅编排，错误带中文上下文，输出走 OutOrStdout，未引入 I/O 到 codegraph）。验证：`go test ./cmd/ -run 'TestGraphCheck' -count=1` 实际通过；`go test ./cmd/ -count=1` 实际通过；`gofmt -l .` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 5 提交）。
- Task 6 / 完成裁决：spec 符合（Absorb 纯函数处理节点/调用边/implements 边增改删、旧签名清理、删除节点牵连边剔除、SaveGraph temp+rename 原子写；`graph absorb <view>` 校验 diff、刷新可注入 commit/branch、写盘成功后删 diff，git 取号失败不猜）；代码质量符合（internal/codegraph 不依赖 cmd，输入图/diff 不变，CLI 删除动作严格位于 SaveGraph 成功之后，错误带中文上下文）。验证：`go test ./internal/codegraph/ -run 'TestAbsorb' -count=1` 实际通过；`go test ./cmd/ -run TestGraphAbsorb -count=1` 实际通过；`go test ./internal/codegraph/ ./cmd/ -count=1` 实际通过；`gofmt -l .` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 6 提交）。
- Task 7 / 完成裁决：spec 符合（三个分域模板分别接入 target domains/graph check、契约冻结 target contracts、全量与分支 check/legacy 棘轮；五个指定纪律块保留 handoff 禁令并给只读 graph 例外，未改 subagent 块）；代码质量符合（模板为纯数据且既有变量白名单/裁决契约断言保留，文本例外逐文件回归断言，seed 幂等语义未动）。验证：`go test ./internal/ledger/ ./internal/discipline/... -count=1` 实际通过；`gofmt -l .` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 7 提交）。
- Task 8 / 修复轮 1：首次 `go test ./... -count=1` 的本次回归原文为 `--- FAIL: TestCodegraphEndpoint`、`代码图响应形状: nodes=7 ... stale=[{m_notifier svc/notifier.go 2 文件不存在}]`；根因证据是 `internal/agentd/codegraph_test.go` 的 fixture 复制清单未包含 Task 3 新增的 `svc/notifier.go`。同步复制该 fixture 并把节点数断言从 6 改为 7；`go test ./internal/agentd/ -run TestCodegraphEndpoint -count=1` 实际通过。
- Task 8 / 完成裁决：spec 符合（根目录六域 target v1、路径/assignment 归域、域类型、assembly、真实 baseline 方向 contracts 与 legacyBudget、RepoContractGate 均落地）；代码质量符合（target 通过 LoadTarget+ValidateTarget，门测试直接调用纯 Check，真实 fixture 回归同步，未修改 Task 9 用户级配置）。验证：`go test ./cmd/ -run RepoContractGate -count=1 -v` 实际通过，日志为 legacy 12 个方向、warn 12 条；`go test ./internal/agentd/ -run TestCodegraph -count=1` 实际通过；`go test ./cmd/ ./internal/codegraph/ -count=1` 实际通过；`gofmt -l .` 实际无输出。`go test ./... -count=1` 第二次实际失败，原始失败行包括：`--- FAIL: TestCursorRootFallsBackToCwdWhenHomeUnwritable`、`--- FAIL: TestCursorRootErrorNamesBothPaths`、`--- FAIL: TestLoadStripUpdateDoesNotBlockOnSaveFailure`、`--- FAIL: TestPermServerAskThenRespond`（裁决 socket 路径过长，114 字节，上限 107）、`--- FAIL: TestPermServerRespondUnknownID`、`--- FAIL: TestPermServerReRegisterSameID`、`--- FAIL: TestSyncAuthKeepsTaskCopyWhenWriteFails`；本轮未改这些非代码图失败项。Commit 范围：`HEAD^..HEAD`（Task 8 提交）。
- 整分支终审 / 完成裁决：相对 `5a3a40d5` 的完整 diff 为 31 个计划内文件，无额外未授权文件；`git diff --check 5a3a40d5..HEAD`、`gofmt -l .` 均无输出，internal/codegraph 未发现 handoff 内部依赖。最终相关包集合 `go test ./internal/codegraph ./cmd ./internal/agentd ./internal/ledger ./internal/discipline/... -count=1` 实际通过；终审无发现项，无修复波。全仓失败项已在 Task 8 原始记录中保留。Commit 范围：待提交（终审记账）。
