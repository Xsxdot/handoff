# B233.14 implement 节点台账（2026-09-12）

分支：`cards/B233.14-charter-5`（基线 `cards/B233.14-charter-4` @ `a56de75b`；合并目标 `cards/B233.1-charter-7` @ `94246fc8`）
产出物：`docs/superpowers/plans/b233.14-plan.md` 落地代码 + 本台账
上游：contract 冻结 @ `a49eea84`、breakdown P1–P5 全 A、plan @ `a56de75b`
范围：单卡 L3 轻档；W1（门面/组装/测试迁移）→ W2（空 HOME×纪律）。不新开 `d_cli` 合同；不拆 `B233.14.x`。

边干边落；提交事实在收口时以 amend 收进同批（收口判据是工作树干净，不是文件里 hash 等于 HEAD）。

## 1. 动手前基线核验（原始读数）

```
$ go build ./...
BUILD_EXIT=0

$ go vet ./internal/agentd/ ./internal/orchestration/ ./internal/scheduling/ ./cmd/
VET_EXIT=0
```

基线存量红（§0 已声明，本卡不引入、不修复）：`internal/agentd` 的 `TestLegacyNodeEventSequenceUnchanged`；`cmd` 的 `TestRepoContractGate`。

## 2. Task 1（W1：gateway 持门面 + New 上移 cmd）

### 2.1 首红（编译红：新缝符号缺席）

新增 `internal/agentd/b23314_signature_lock_test.go`、`internal/agentd/b23314_scheduling_testhelpers_test.go` 后：

```
$ go test ./internal/agentd/ -run 'B23314' -count=1
internal/agentd/b23314_scheduling_testhelpers_test.go:22:13: srv.SchedulingRegistry undefined (type *Server has no field or method SchedulingRegistry)
internal/agentd/b23314_scheduling_testhelpers_test.go:52:16: invalid operation: srv.Scheduling() (value of type *scheduling.Service) is not an interface
internal/agentd/b23314_signature_lock_test.go:62:18: cannot use fake (variable of interface type SchedulingClient) as *scheduling.Service value in argument to s.SetScheduling: need type assertion
internal/agentd/b23314_signature_lock_test.go:66:22: invalid operation: s.scheduling (variable of type *scheduling.Service) is not an interface
FAIL	github.com/Xsxdot/handoff/internal/agentd [build failed]
```

判断：红因是「符号缺席」（`SchedulingRegistry` 未定义、字段/签名不是接口），非 typo。

### 2.2 生产改动

- `internal/agentd/server.go`：`Server.scheduling` 类型 `*scheduling.Service` → `SchedulingClient`；`SetScheduling(SchedulingClient)` 加入 typed-nil/nil 落 nil 分支；`Scheduling() SchedulingClient`；新增 `SchedulingRegistry() schedclient.Registry`（`autoLedger==nil` 返回 nil）；`SetupAutomation` 删 `scheduling.New` + `SetKnownMachines`。
- `internal/agentd/scheduling_client.go`：删第二条重复编译期断言（保留顶部一条）。
- `cmd/agentd.go`：`setupLedger` 在 `SetupAutomation` 后、`StartAutomation` 前构造 `scheduling.New(srv.SchedulingRegistry())` + `SetKnownMachines(经 srv.Conf())` + `SetScheduling`；`SchedulingRegistry` 为空则 `lst.Close()` + 拒启动并报错。import 增 `internal/scheduling`。

### 2.3 测试迁移

- 新增 `b23314_scheduling_testhelpers_test.go`（`newSchedulingForTest` / `SetupAutomationForTest` / `mustScheduling`）。
- `*_test.go` 内 26 处 `X.SetupAutomation(Y)` → `SetupAutomationForTest(t, X, Y)`；`.Scheduling()`（13 文件 40+ 处）→ `mustScheduling(t, srv)`；`schedEnv.svc`、`putOnlineCarrier` 取具体 `*scheduling.Service`。外部测试包（`agentd_test`）用限定名 `agentd.SetupAutomationForTest`。
- `schedapi_test.go` 的 `TestSchedulingEndpointsDegradedWithoutSetup` 保持不装配（测 503 降级）。

### 2.4 绿

```
$ go build ./...
BUILD_EXIT=0
$ go vet ./internal/agentd/ ./cmd/
VET_EXIT=0
$ go test ./internal/agentd/ -run 'B23314' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	0.006s
$ go test ./internal/agentd/ -run 'TestSquadsGetEmptyIsLegalState|TestCarrierPutRoundtripThroughWire|TestCarrierPutUnknownMachineIs400|TestSchedulingEndpointsDegradedWithoutSetup' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	1.238s
$ go test ./internal/agentd/ -run 'TestAutomationQueueRestartReplay|TestDrainQueuesDefersCoordinatorNoSlotAndContinues|TestCoordinatorSessionRefResolverEmptyHomeOnlineCarrier|TestB23313ConcurrentDoneReleasesOnce' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	1.529s
$ go test ./cmd/ -run 'TestSetupLedgerMountsWithRetiredEnabledFlag' -count=1
ok  	github.com/Xsxdot/handoff/cmd	0.153s
```

### 2.5 变异自验（S1；每发都编译通过 + 断言红 + 立即还原）

变异 A（字段与 `Scheduling()` 从 `SchedulingClient` 换成匿名 interface，编译等价）：

```
$ go test ./internal/agentd/ -run 'TestB23314SchedulingFieldIsInterface' -count=1
--- FAIL: TestB23314SchedulingFieldIsInterface (0.00s)
    b23314_signature_lock_test.go:31: Server.scheduling 接口 = interface { ... }，want agentd.SchedulingClient
FAIL
MUTATION_EXIT=1        # 还原后 grep 确认 scheduling SchedulingClient
```

变异 B（`server.go` 加 `_ = scheduling.New(nil)`）：

```
--- FAIL: TestB23314SchedulingNewOnlyInCmd (0.00s)
    b23314_signature_lock_test.go:101: server.go:2774 出现 scheduling.New(；...
FAIL
MUTATION_B_EXIT=1      # 还原后 grep -c = 1（仅注释）
```

变异 C（typed-nil 判据 `rv.Kind() == reflect.Ptr` → `reflect.Func`，编译过）：

```
--- FAIL: TestB23314SetSchedulingRejectsTypedNil (0.00s)
    b23314_signature_lock_test.go:76: typed-nil 必须落成 nil 接口，got (*scheduling.Service)(nil)
FAIL
MUTATION_C2_EXIT=1     # 还原后 grep -c "rv.Kind() == reflect.Ptr" = 2
```

（首次变异 C 用 `s/rv.Kind().../false/` 命中了 `server.go:379` 与 `:2759` 两处，且引入「declared and not used」，属编译红不算数——已按「变异必须打中唯一、必须编译过」整块替换重做。）

## 3. Task 2（W2：空 HOME × 非空纪律）

### 3.1 首红（编译红：`userHomeDir`/`taskProfileHome` 缺席）

```
$ go test ./internal/orchestration/ -run 'B23314' -count=1
internal/orchestration/b23314_empty_home_discipline_test.go:25:10: undefined: userHomeDir
internal/orchestration/b23314_empty_home_discipline_test.go:35:15: undefined: taskProfileHome
FAIL	github.com/Xsxdot/handoff/internal/orchestration [build failed]
```

### 3.2 生产改动

- `internal/orchestration/manager.go`：新增包内缝 `var userHomeDir = os.UserHomeDir`；`expandCarrierHome` 内 `os.UserHomeDir()` → `userHomeDir()`；新增 `taskProfileHome(taskHome string) (string, error)`（非空经 expand 后必须绝对，空取主 HOME，失败报错）；`prepareTaskProfile` 删除「空 `Task.HomeDir` 直接 return error」门，改算 `writePoint`，`Isolated = start.Task.HomeDir != ""`，经 `taskProfileHome` 失败带上下文报错。
- 新增 `internal/orchestration/b23314_empty_home_discipline_test.go`（`taskProfileHome` 单元 + Dispatch 缝级 + 真落盘 `opencode.Profile.Prepare` + 主 HOME 不可得拒派）。

### 3.3 绿

```
$ go test ./internal/orchestration/ -run 'B23314' -count=1
ok  	github.com/Xsxdot/handoff/internal/orchestration	0.442s
$ go test ./internal/orchestration/ -run 'TestDispatchInjectsTaskDisciplineIntoProfile|TestDispatchPersistsDisciplineBeforeExecutorStarts|TestDispatchSkipsStartWhenPersistFails|TestDispatchRejectsDisciplineWithoutTaskProfile' -count=1
ok  	github.com/Xsxdot/handoff/internal/orchestration	0.482s
$ go test ./internal/orchestration/ -count=1
ok  	github.com/Xsxdot/handoff/internal/orchestration	48.433s
```

### 3.4 变异自验（S2）

变异 #20（加回空 HOME 拒门）：

```
--- FAIL: TestB23314EmptyHomeDisciplineReallyWritesOverlay (0.13s)
    b23314_empty_home_discipline_test.go:170: Dispatch: MUTATION：空 HOME 拒绝非空纪律
FAIL
MUTATION_2_EXIT=1      # 还原后 grep -c MUTATION = 0
```

承重变异（顺带 `start.Task.HomeDir = writePoint` 回写身份快照）：

```
--- FAIL: TestB23314EmptyHomeDisciplinePreparesProfile (0.14s)
FAIL
MUTATION_HOMEDIR_EXIT=1  # 还原后 grep -c = 0
```

## 4. Task 3（图对账）

```
$ codegraph check --view cards-B233.14-charter --repo .
fails=6 warns=138  # 与 plan §0 基线逐项一致；--base cards/B233.1-charter-7 fails=7 warns=139
                   # 差值 = 消解 baseline 的 dead-interface SchedulingClient；不新增
$ codegraph validate --repo .        # EXIT=1（存量：2 条 gc.go no-import + 120 条 B233.13 视图引用）
$ codegraph resolve --doc docs/superpowers/specs/b233.14-contract.md --repo .   EXIT=0
$ codegraph resolve --doc docs/superpowers/plans/b233.14-plan.md --repo .       EXIT=0
```

validate 存量对账（git stash 前后比对，集合差）：
- base（stash 后）：issues=123，含 `[cards-B233.14-charter] 调用边门控 n_cmd_setupLedger→n_scheduling_New ... 未 import scheduling`。
- 本卡落地后：issues=122，**only in base = 1**（上述那条，迁 New 后 cmd 已 import scheduling，自动消解），**only in mine = 0**。
- 其余 122 条全为 `[cards-B233.13-charter]` 视图引用与 `gc.go` 既有 no-import，非本卡引入。

```text
$ git status --short codegraph/
CODEGRAPH_DIRTY_COUNT=0     # target/best/diff 已随 contract 冻结，本卡未改图文件
```

## 5. Task 4（收口）——全部原始读数

```
$ go build ./...                                  BUILD_EXIT=0
$ go vet ./internal/agentd/ ./internal/orchestration/ ./internal/scheduling/ ./cmd/
                                                  VET_EXIT=0
$ go test ./internal/orchestration/ -count=1      ok  48.433s
$ go test ./internal/agentd/ -run 'B23314' -count=1                       ok
$ go test ./internal/agentd/ -run 'TestB23313ConcurrentDoneReleasesOnce' -count=1   ok
$ go test ./internal/orchestration/ -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1  ok
$ go test ./internal/approval/ -run 'TestClientApproverAllowUndeliveredUntilAck' -count=1    ok
$ go test ./internal/scheduling/ -run 'TestIdentityOfAndBindPhysical|TestResolveLookupTable' -count=1  ok
$ go test ./internal/scheduling/ -run 'TestPutCarrierDefaultsCredentialAndRequiresKnownMachine' -count=1  ok
$ go test ./internal/proto/ -run 'TestTaskHomeDirJSON' -count=1            ok
```

结构断言（`rg` 不在环境，改用 grep）：

```
$ grep -n 'scheduling\s\+\*scheduling\.Service' internal/agentd/server.go
（无匹配）                                        # 字段已是接口
$ grep -rn 'scheduling\.New(' internal/agentd/ --include='*.go' | grep -v _test.go
internal/agentd/server.go:2771:// autoLedger 未装配时返回 nil；... scheduling.New(nil)
                                                  # 仅注释；源码守卫剔注释后无匹配
$ grep -rn 'scheduling\.New(' cmd/ --include='*.go' | grep -v _test.go
cmd/agentd.go:490:	sched := scheduling.New(reg)      # 仅组装点
```

跨平台编译：

```
$ GOOS=windows go build ./...   WIN_EXIT=0
$ GOOS=darwin go build ./...    DARWIN_EXIT=0
```

受影响包全量（只允许 §0 两条存量红）：

```
$ go test ./internal/agentd/ -count=1
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.15s)   # 存量红，事件序列漂移
$ go test ./cmd/ -count=1
--- FAIL: TestRepoContractGate (0.04s)                   # 存量红，读 codegraph/baseline.json（B233.13/14 视图未 absorb）
  dead-interface d_gateway→d_orchestration (OrchestrationClient)
  dead-interface d_gateway→d_scheduling (SchedulingClient)
  over-budget d_cli->d_workspace 11/9 等 5 条
```

判断：与 §0/plan §1.1 基线逐条一致，**无新增失败**。`TestRepoContractGate` 的判据用 `codegraph check` 对账（见 §4），合同 #31 成立。

## 6. 收尾自审

- 错误分支日志：`SetScheduling` nil/typed-nil 带 `error_kind`；`taskProfileHome` 失败带 task/executor/home/cause；`SchedulingRegistry` nil 时 `setupLedger` 显式 `fmt.Errorf` 拒启动。成功路径有出口日志（挂接、overlay 准备/完成）。
- 新文件头注释 / 导出函数文档：`b23314_*_test.go`、`taskProfileHome`、`SchedulingRegistry`、`SetScheduling` 均带职责+边界+为什么。
- 与 plan Interfaces 签名一致：`SetScheduling(SchedulingClient)` / `Scheduling() SchedulingClient` / `SchedulingRegistry() schedclient.Registry` / `taskProfileHome(string) (string, error)` / `userHomeDir func() (string, error)`。
- 未验证项：真机清单（plan §6）——跨平台真机、空 HOME 载体真机派发、`~` 前缀 overlay 真落点、agentd 重启——**本节点未跑真机，不宣称通过**。

## 7. 提交事实

```
$ git add -A && git commit -m "implement(B233.14): ..."
[cards/B233.14-charter-5 fc5a3ecc] implement(B233.14): 编制入站封界——gateway 持 SchedulingClient 门面、New 上移 cmd 组装点、空 HOME 可带纪律
 23 files changed, 802 insertions(+), 120 deletions(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-12-b233.14-implement-ledger.md
 create mode 100644 internal/agentd/b23314_scheduling_testhelpers_test.go
 create mode 100644 internal/agentd/b23314_signature_lock_test.go
 create mode 100644 internal/orchestration/b23314_empty_home_discipline_test.go
```

随后以 amend 把本台账收进同批；amend 换 hash 属 git 事实，收口判据是工作树干净。

## 8. 审查补充（review major：生产组装必须有测试锁）

基线 `cards/B233.14-charter-5` @ `205e488b`；只补测试锁，不改产品行为，不新开 `d_cli` 合同。

### 8.1 补锁

- `cmd/agentd_ledger_test.go#TestSetupLedgerMountsWithRetiredEnabledFlag`：`setupLedger` 后断言 `srv.Scheduling()` 是具体 `*scheduling.Service`（删 `srv.SetScheduling(sched)` 必红）。原测试只验账本 health，不验编制域注入，是 review 指出的假绿。
- `internal/orchestration/b23314_empty_home_discipline_test.go#TestB23314NonEmptyHomeDisciplineIsIsolated`：非空 `Task.HomeDir` 时 `Isolated=true`、写入点=载体 HOME 原字面、读回 `Task.HomeDir` 不变。

### 8.2 变异自验（每发先确认编译过、打中唯一、立即还原）

删 `cmd/agentd.go` 的 `srv.SetScheduling(sched)`（改为 `_ = sched`，保住变量使用，编译过）：

```
--- FAIL: TestSetupLedgerMountsWithRetiredEnabledFlag (0.13s)
    agentd_ledger_test.go:64: setupLedger 后必须注入具体 *scheduling.Service，got <nil>
FAIL
```

`Isolated: start.Task.HomeDir != ""` → `false`：

```
--- FAIL: TestB23314NonEmptyHomeDisciplineIsIsolated (0.09s)
    b23314_empty_home_discipline_test.go:169: 非空 HOME 必须 Isolated=true，got HomeDir=".../carrier"
FAIL
```

`Isolated: start.Task.HomeDir != ""` → `true`（空 HOME 锁，复跑确认）：

```
--- FAIL: TestB23314EmptyHomeDisciplinePreparesProfile (0.17s)
    b23314_empty_home_discipline_test.go:119: 空 HOME 沿用主 HOME，Isolated 必须 false
FAIL
```

三发均 `git status` 确认工作树恢复干净、`grep MUTATION` 无残留。

### 8.3 补充后复跑读数

```
$ go build ./...                                      BUILD_EXIT=0
$ go vet ./internal/agentd/ ./internal/orchestration/ ./internal/scheduling/ ./cmd/
                                                      VET_EXIT=0
$ go test ./cmd/ -run 'TestSetupLedgerMountsWithRetiredEnabledFlag' -count=1   ok
$ go test ./internal/orchestration/ -run 'B23314' -count=1                     ok
$ go test ./internal/orchestration/ -count=1          ok  50.562s
$ go test ./internal/agentd/ -count=1                 FAIL 仅 TestLegacyNodeEventSequenceUnchanged（存量红）
$ go test ./cmd/ -count=1                             FAIL 仅 TestRepoContractGate（存量红）
$ go test ./internal/agentd/ -run 'TestB23313ConcurrentDoneReleasesOnce' -count=1   ok
$ go test ./internal/orchestration/ -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1  ok
$ go test ./internal/approval/ -run 'TestClientApproverAllowUndeliveredUntilAck' -count=1   ok
$ go test ./internal/scheduling/ -run 'TestIdentityOfAndBindPhysical|TestResolveLookupTable' -count=1   ok
$ go test ./internal/proto/ -run 'TestTaskHomeDirJSON' -count=1   ok
```

判断：两条全量失败均为 §0 声明的功能线存量红，无新增。`StartAutomation` 遇 `scheduling==nil` 静默 no-op 现已由 `TestSetupLedgerMountsWithRetiredEnabledFlag` 的注入断言挡住（生产组装漏接 → `Scheduling()` nil → 红）。

## 9. 审查补充（review major：装配顺序承重）

基线 `cards/B233.14-charter-6` @ `3886669a`；只补顺序锁测试，不改产品行为。

### 9.1 补锁

- `cmd/agentd_ledger_test.go#TestB23314SetSchedulingBeforeStartAutomation`：源码顺序断言——`setupLedger` 函数体内 `srv.SetScheduling(` 必须早于 `srv.StartAutomation(`。
- 为什么必须由源码顺序断言承重：`StartAutomation` 遇 `s.scheduling==nil` 只记 Warn 后静默 no-op（`internal/agentd/scheddrain.go:52-57`），两行对调不会让任何运行期状态变红；`TestSetupLedgerMountsWithRetiredEnabledFlag` 的注入断言在「先 StartAutomation 再 SetScheduling」下仍绿（SetScheduling 随后补上，`Scheduling()` 非 nil），故需要独立的顺序锁。
- helper `goFuncBody` 文本切分函数体；边界：只答调用顺序，不解析 AST / 注释。

### 9.2 变异自验（编译过、命中唯一、立即还原）

对调 `cmd/agentd.go`：把 `srv.StartAutomation(ctx)` 移到 `srv.SchedulingRegistry()` 之前、删原位置（每处 replace 前 `count==1` 断言）：

```
$ go build ./...
BUILD_EXIT=0
$ go test ./cmd/ -run 'TestB23314SetSchedulingBeforeStartAutomation|TestSetupLedgerMountsWithRetiredEnabledFlag' -count=1
--- FAIL: TestB23314SetSchedulingBeforeStartAutomation (0.00s)
    agentd_ledger_test.go:106: 装配顺序错误：StartAutomation 先于 SetScheduling；StartAutomation 遇 scheduling==nil 会静默 no-op，自动化循环永不启动
--- PASS: TestSetupLedgerMountsWithRetiredEnabledFlag (0.12s)
FAIL
```

判断：顺序对调后 `TestSetupLedgerMountsWithRetiredEnabledFlag` 仍绿（假绿佐证），顺序锁变红——正是本补充要挡的洞。还原后 `git diff --stat cmd/agentd.go` 为空（仅 `cmd/agentd_ledger_test.go` 改动）。

### 9.3 补充后复跑读数

```
$ go build ./...                                      BUILD_EXIT=0
$ go vet ./internal/agentd/ ./internal/orchestration/ ./internal/scheduling/ ./cmd/
                                                      VET_EXIT=0
$ go test ./cmd/ -run 'TestB23314SetSchedulingBeforeStartAutomation|TestSetupLedgerMountsWithRetiredEnabledFlag' -count=1   ok
```

```
$ go test ./cmd/ -count=1
--- FAIL: TestRepoContractGate (0.03s)     # §0 声明的功能线存量红，读 codegraph/baseline.json（B233.13/14 视图未 absorb）
FAIL	github.com/Xsxdot/handoff/cmd	34.601s
$ go test ./internal/agentd/ -run 'B23314' -count=1          ok
$ go test ./internal/orchestration/ -run 'B23314' -count=1   ok
```

判断：`cmd` 全量仅 `TestRepoContractGate` 一条存量红，无新增失败；本卡两条定向测试绿。

### 9.4 提交事实

```
$ git add -A && git commit -m "test(B233.14): 锁 setupLedger 装配顺序——SetScheduling 必须在 StartAutomation 之前"
```
