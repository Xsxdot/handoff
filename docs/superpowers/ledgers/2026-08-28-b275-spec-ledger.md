# B275 spec 台账

## 2026-08-28

- 基线发现：acc/b156.2-156.3（原 = feat/b156.3-web-write 8222ca1f9）不含 B156.2/156.3 已完成代码；
  已完成代码在 feature/b156.2-156.3（811 提交领先、0 落后，含 internal/collab、roomsapi、
  web/src/app/rooms 三页、rooms.ts 五端点、B156.3 契约线、codegraph best/target）。
  处置：git merge --ff-only feature/b156.2-156.3 → acc 推进到 e266d1d79。
- 契约面现状核查（spec 接缝用）：
  - proto/rooms.go:50 RoomSummary 无 unread；collab/service.go:344 Service.Unread 存在但无 wire 投影 → 增量①。
  - bound_session=driver_session 投影（service.go:271），能否解析可 attach task 未知 → contract 第一刀。
  - proto/pty.go:64 InitCommand 已有；web types.ts:733 CreatePtySessionReq 缺 init_command → 镜像补全。
  - Shell.tsx:459-460 dock 入口 onOpenRooms/onOpenInbox；:60/:627 UpdateToasts 挂载点。
  - cards/columns.ts:6 列=工作流状态平铺+终止（B265 要改可配映射）。
- 原型定稿：变体 B（浅灰底+磨砂玻璃气泡）；A/C 与变体切换逻辑已撤（board.html/index.html 同步），
  README B275 段已更新。截图复验 5 张（列表/房间/详情/需要你筛选/index 浮窗）全部无 PAGE ERR，
  环境 /tmp/b275shots（playwright-core + chrome-headless-shell）。
- 定级判决：原判 L3 轻档（动 wire 契约两条增量）；用户 2026-08-28 裁决降 L2，跳过 contract/breakdown 直接进 plan，增量签名由 plan 写死、review 对照 spec 验收。
- 执行基线修正：按用户指定执行 `git fetch origin`（发现 `origin/acc/b156.2-156.3`）与 `git reset --hard origin/acc/b156.2-156.3`；原始结果为 `HEAD is now at ac4a3cad docs(b275): 用户裁决降 L2——跳过 contract/breakdown 直接进 plan`，当前分支仍为 `cards/B275-charter`。
- 代码图基线查询：`go run . graph context d_web --repo . --with-source --max-tokens 18000 --source-span 30` 成功，结果 `truncated=false`、`fociTruncated` 未报告，警告为 `基线仍有 6 个未扫描入口` 与 `领域声明缺失：codegraph/domains/d_web.json`；`actual` 报告 d_web 视图容器分散于 d_web_admin/d_web_cards/d_web_command/d_web_contract/d_web_shell/d_web_workbench，未将该图结果当作完整调用面。
- 原型/规格输入确认：`docs/superpowers/specs/2026-08-28-b275-im-console-design.md` 共 84 行，指定 `prototypes/b275-frontend-proto/` 为形态权威；原型 README 共 140 行，列明三态 IM、右侧/悬浮双挂载、B 变体、筛选、五列看板、flows 映射、卡抽屉、删除更新 toast 与孤儿 conversations 页处理。
- 修正基线上的前端门禁复核：在 `web/` 执行 `npm test -- --run`、`npm run typecheck`、`npm run lint`、`npm run build` 均退出 127；原始错误分别为 `sh: 1: vitest: not found`、`sh: 1: tsc: not found`、`sh: 1: eslint: not found`、`sh: 1: tsc: not found`。当前工作区未安装 web 依赖，结果未用于判断源码是否通过。
- 安装依赖：在 `web/` 执行 `npm ci` 成功，输出 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`。
- 依赖安装后基线门禁：`npm test -- --run` 退出 0，`Test Files 113 passed (113)`、`Tests 1139 passed (1139)`，仅输出既有 `HTMLCanvasElement's getContext() method` jsdom 提示；`npm run typecheck` 退出 0；`npm run build` 退出 0（Vite 6.4.3，1971 modules transformed）；`npm run lint` 退出 1，唯一 error 为既有 `web/src/app/flows/NodeEditor.test.tsx:50:9 'view' is never reassigned. Use 'const' instead`，另有 18 条 warning。
- Go 基线门禁：`go test ./internal/proto ./internal/collab` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/proto 0.006s` 与 `ok github.com/Xsxdot/handoff/internal/collab 4.148s`；`go test ./internal/agentd` 退出 0，原始输出为 `ok  github.com/Xsxdot/handoff/internal/agentd 142.972s`。
- 关键接口查证：`ledger.Store.TasksOf(cardID string) ([]TaskLink, error)` 位于 `internal/ledger/tasks.go:37`，按 `created_at` 升序返回卡片挂账；`client.Client.Attach(ctx context.Context, taskID string) (*AttachInfo, error)` 位于 `internal/client/client.go:673`，读取 `/api/tasks/{id}` 的任务快照；`nextTerminalSeq(wb Workbench) int` 位于 `web/src/app/workbench/tabs.ts:361` 且已导出，RoomPanel 可复用现有工作台入口。
- 终端通道复核：修正基线的 `web/src/api/types.ts:733-750` 已有 `CreatePtySessionReq.init_command?: string`，`web/src/app/workbench/TerminalTab.tsx:74-78,217` 已把 `initCommand` 透传为 `init_command`；因此计划只需给 `TabContent`/Shell 保留房间 attach 命令，不能把“TS 缺字段”误写成新的实现任务。
- 计划落盘与自审：`docs/superpowers/plans/b275-plan.md` 已写入，包含 Task 1–4 有界实现文件集、精确增量签名/代码块、Task 5 协调者收口、缺陷族审查、序列化边界、接缝双向覆盖和 spec 故事归属；`git diff --check` 退出 0，未发现 TBD/TODO/同 Task/适当错误处理等实现占位词（自审章节中的禁止词清单文字除外）。
- B275 implement 基线重跑：`go test ./internal/proto ./internal/collab ./internal/agentd` 退出 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/proto 0.007s`、`ok github.com/Xsxdot/handoff/internal/collab 3.620s`、`ok github.com/Xsxdot/handoff/internal/agentd 129.319s`。
- B275 Task 1 Web 基线重跑：`cd web && npm test -- --run src/api/rooms.test.ts src/api/rooms.fetch.test.ts` 退出 127；原始错误为 `sh: 1: vitest: not found`。
- B275 implement 依赖准备：在 `web/` 执行 `npm ci` 退出 0；原始输出为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`。
- B275 Task 1 红测：首次新增 Go 投影测试在补齐 `RoomSummary` 类型后，`go test ./internal/proto ./internal/agentd -run 'TestRoomSummaryGoldenProjection|TestRoomsListUnreadAndAttachProjection|TestRoomsListWithoutAttachmentKeepsAttachMissing'` 的 proto 断言通过，但 agentd 真实入口断言失败：`--- FAIL: TestRoomsListUnreadAndAttachProjection`、`两条未读消息应投影 unread=2: ... Unread:0 Attach:<nil>`；确认失败原因为 handle/service/attach 投影尚未实现而非测试编译 typo。
- B275 Task 1 实现验证：`gofmt -w internal/proto/rooms.go internal/proto/rooms_fixture_test.go internal/collab/service.go internal/agentd/roomsapi.go internal/agentd/roomsapi_test.go && go test ./internal/proto ./internal/collab ./internal/agentd -run 'TestRoomSummaryGoldenProjection|TestRoomsListUnreadAndAttachProjection|TestRoomsListWithoutAttachmentKeepsAttachMissing|TestRoomsListEndpoint'` 退出 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/proto 0.002s`、`ok github.com/Xsxdot/handoff/internal/collab 0.003s [no tests to run]`、`ok github.com/Xsxdot/handoff/internal/agentd 0.617s`。
- B275 Task 1 Web 实现验证：`cd web && npm test -- --run src/api/rooms.test.ts src/api/rooms.fetch.test.ts` 退出 0；原始输出为 `Test Files 2 passed (2)`、`Tests 13 passed (13)`，耗时 `617ms`。
- B275 Task 2 修改前基线：`go test ./internal/ledger ./internal/agentd` 退出 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/ledger 18.706s`、`ok github.com/Xsxdot/handoff/internal/agentd 135.474s`。
- B275 Task 2 Web 修改前基线：`cd web && npm test -- --run src/app/cards/columns.test.ts src/api/ledger.test.ts src/app/flows/FlowsPage.test.tsx` 退出 0；原始输出为 `Test Files 3 passed (3)`、`Tests 18 passed (18)`，耗时 `958ms`。
- B275 Task 2 红测：新增 BoardLayout/FlowDetail 编译红明确为 `unknown field Board`、`undefined: BoardLayout`、`got.Def.Board undefined`；Web seam 红为 `defaultBoardLayout is not a function`、`normalizeBoardLayout is not a function`、putFlow 未发 board，以及旧 FlowDetail fixture 的 board 缺失。确认是功能缺失而非 typo，进入最小实现。
- B275 Task 2 首轮实现验证：Web `npm test -- --run src/app/cards/columns.test.ts src/api/ledger.test.ts src/api/contract.test.ts` 退出 0，`Test Files 3 passed (3)`、`Tests 54 passed (54)`；Go 目标测试仅 `TestContractFixtures` 失败，原始失败为 `FlowDetail: 序列化结果与 fixture 不一致（契约已漂移，如需接受变更请用 -update）`，同时 internal/ledger 与 internal/agentd 目标测试输出 `ok`。失败是 fixture 未同步，不改行为结论。
- B275 Task 2 fixture 同步：`go test ./internal/proto -run TestContractFixtures -update` 退出 0，原始输出 `ok github.com/Xsxdot/handoff/internal/proto 0.006s`；FlowDetail 金样本已按实际 Marshal 结果更新。
- B275 Task 3 修改前局部基线：`cd web && npm test -- --run src/app/rooms/RoomsListPage.test.tsx src/app/rooms/RoomDetailPage.test.tsx src/app/rooms/InboxPage.test.tsx` 退出 0，原始输出为 `Test Files 1 passed (1)`、`Tests 5 passed (5)`，耗时 `749ms`；现有三页仍待 Task 4 删除。
- 误用工作目录复跑 Task 2：在 `web/` 以仓库相对路径读取台账失败，原始错误为 `tail: cannot open 'docs/superpowers/ledgers/2026-08-28-b275-spec-ledger.md' for reading: No such file or directory`；测试未因该命令执行，立即改用仓库根目录复跑。
- B275 Task 2 修正后局部验证：`cd web && npm test -- --run src/app/cards/columns.test.ts src/app/cards/CardsPage.test.tsx src/app/flows/FlowsPage.test.tsx src/api/ledger.test.ts src/api/contract.test.ts` 退出 0，原始输出为 `Test Files 5 passed (5)`、`Tests 63 passed (63)`，耗时 `1.12s`。
- B275 Task 3 新接缝首红：新增 `RoomPanel.test.tsx` 后执行 `cd web && npm test -- --run src/app/rooms/RoomPanel.test.tsx`，退出 1，真实错误为 `Failed to resolve import \"./RoomPanel\" ... Does the file exist?`，确认是新符号缺席而非测试断言 typo。
- B275 Task 3 空壳断言红：添加仅返回 `null` 的 RoomPanel 后再次执行同一测试，退出 1，4 个测试均因列表/房间/详情元素缺席失败；首个断言为 `Unable to find an element with the text: ⚑ 需要你 1`，确认接缝测试有行为断言后进入实现。
- B275 Task 3 首轮实现测试：RoomPanel 4 项中 3 项通过，attach 正控因测试 double 未提供 WorkbenchApi 必需的 `wb` 而触发原始错误 `TypeError: Cannot read properties of undefined (reading 'groups')`；这是测试夹具契约缺失，补齐合法 EMPTY_WORKBENCH 后复跑，不改生产逻辑。
- B275 Task 3 RoomPanel 接缝验证：`cd web && npm test -- --run src/app/rooms/RoomPanel.test.tsx` 退出 0，原始输出为 `Test Files 1 passed (1)`、`Tests 4 passed (4)`，耗时 `1.02s`。
- B275 Task 4 修改前局部基线：`cd web && npm test -- --run src/app/shell/Shell.test.tsx src/app/tree/ProjectTree.test.tsx src/app/cards/CardDrawer.test.tsx src/app/cards/CardsPage.test.tsx` 退出 0，原始输出为 `Test Files 4 passed (4)`、`Tests 134 passed (134)`，耗时 `3.34s`。
- B275 Task 4 红测：新增壳/树/抽屉接缝后同一局部测试退出 1；真实失败包括 `Unable to find ... data-testid=room-panel`、`Unable to find role=button name=打开房间面板`、树仍包含 `aria-label=会话`、以及 `Unable to find role=button name=▶ 拉起协调者`；确认功能缺失而非测试拼写错误。
- B275 Task 4 旧资产删除前引用扫描：`rg -n "RoomsListPage|RoomDetailPage|InboxPage|UpdateToasts|onOpenRooms|onOpenInbox|conversations.html" web/src prototypes/b275-frontend-proto` 找到旧页/更新提示文件与 Shell 旧路由/树入口测试引用；按计划删除这些资产并将轮询测试改接 RoomPanel。
- B275 Task 4 局部验证：删除旧页/更新提示、移除树入口并挂载统一面板后，`cd web && npm test -- --run src/app/rooms/RoomPanel.test.tsx src/app/rooms/pollInterval.test.tsx src/app/shell/Shell.test.tsx src/app/tree/ProjectTree.test.tsx src/app/cards/CardDrawer.test.tsx src/app/cards/CardsPage.test.tsx` 退出 0，原始输出为 `Test Files 6 passed (6)`、`Tests 138 passed (138)`，耗时 `3.41s`。
- B275 Task 4 删除后引用扫描：`rg -n "RoomsListPage|RoomDetailPage|InboxPage|UpdateToasts|onOpenRooms|onOpenInbox|conversations.html" web/src prototypes/b275-frontend-proto || true` 退出 0 且无输出，未留下旧页/更新提示/旧入口死引用。
- B275 全量类型检查首次复跑退出 2，原始错误为 `src/app/cards/CardDrawer.test.tsx(700,57): error TS2345 ... Property 'needs' is missing ...`; 新增测试夹具漏了 CardDetail 必填字段，已补齐并继续验证。
- B275 Task 2 lint 基线文件核查：`web/src/app/flows/NodeEditor.test.tsx:50` 确有 `let view`，已改为 const-safe 的回调初始化写法；该文件不在 git diff 统计中之前是基线未触及源码事实。
- B275 Task 3/4 结构化日志补齐：RoomPanel 的 rooms/inbox/history/preview/mark-read/send/attach 入口均记录请求前后或错误上下文，正文不进入日志；401 终态在面板显示会话过期文案。
- B275 Task 3/4 局部复验：`cd web && npm test -- --run src/app/rooms/RoomPanel.test.tsx src/app/rooms/pollInterval.test.tsx src/app/shell/Shell.test.tsx src/app/tree/ProjectTree.test.tsx src/app/cards/CardDrawer.test.tsx src/app/cards/CardsPage.test.tsx` 退出 0，原始输出为 `Test Files 6 passed (6)`、`Tests 138 passed (138)`，耗时 `4.07s`。
- B275 Task 3/4 局部 lint：对 RoomPanel、房间模型/日志、轮询、Shell、ProjectTree、CardDrawer、NodeEditor 测试执行 `npx eslint ...` 退出 0；原始输出为 1 个既有 Shell warning、4 个既有 ProjectTree fast-refresh warning，共 `5 problems (0 errors, 5 warnings)`。
- B275 Web 类型检查：`cd web && npm run typecheck` 退出 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，无错误输出。
- B275 Web 全量测试：`cd web && npm test -- --run` 退出 0，原始输出为 `Test Files 110 passed (110)`、`Tests 1123 passed (1123)`、耗时 `31.54s`；保留既有 `Not implemented: HTMLCanvasElement's getContext() method...` jsdom 提示。
- B275 Go 全量测试：`go test ./...` 退出 1；各新增/触及包与多数全仓包输出 `ok`，但 `cmd` 包真实失败为 `--- FAIL: TestRepoContractGate`，原文含 `契约违规 [dead-contract] 契约 d_cli→d_collab ...`、`契约违规 [dead-contract] 契约 d_gateway→d_collab ...`、`契约违规 [dead-entry] ... "collab 包级函数" ...`，并有 `--- FAIL: TestMaybeInstallServiceLinuxRootInstalls`，原文为 `托管失败：加载配置 /tmp/handoff.yaml: 写默认配置 /tmp/handoff.yaml: open /tmp/handoff.yaml: read-only file system`；最终原文为 `FAIL github.com/Xsxdot/handoff/cmd`、`FAIL`。该全量失败未被替换为通过，保留为待协调者处置的既有/环境门禁事实。
- B275 收口复核：当前工作树包含统一 `RoomPanel`/纯投影模型/结构化日志、Shell 挂载、CardDrawer 协调者入口与旧房间/更新提示资产删除；新增文件尚未纳入 git diff 统计，待最终 `git add` 后核验。
- B275 Web 全量 lint：在 `web/` 执行 `npm run lint` 退出 0；原始输出为 `✖ 20 problems (0 errors, 20 warnings)`，警告均为既有 hooks/fast-refresh 规则提示。
- B275 Web production build：在 `web/` 执行 `npm run build` 退出 0；原始输出为 `tsc -b`、`vite v6.4.3`、`✓ 1970 modules transformed`、`✓ built in 2.37s`，另有既有 chunk 大小 warning。
- B275 Go compile gate：执行 `go build ./...` 退出 0，原始输出为空。
- B275 变异前唯一命中校验：`rg -n -F "return room.kind === 'card' && needRoomIDs.has(room.id)" web/src/app/rooms/roomPanelModel.ts` 输出唯一命中 `23:  return room.kind === 'card' && needRoomIDs.has(room.id)`，计数为 `1`。
- B275 变异编译校验：将 `room.kind === 'card'` 取反为 `room.kind !== 'card'` 后执行 `cd web && npm run typecheck` 退出 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，确认变异可编译。
- B275 变异行为断言：上述变异后先执行 RoomPanel 单测，退出 1；原始失败为 `RoomPanel.test.tsx (4 tests | 1 failed)`、`Expected ... B2`、`Received ... B1...`，具体断言 `RoomPanel.test.tsx:76:21`，确认该测试能拦截语义取反。
- B275 变异恢复类型检查：恢复 `room.kind === 'card'` 后执行 `cd web && npm run typecheck` 退出 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`。
- B275 变异恢复行为复验：恢复后执行 RoomPanel 单测退出 0，原始输出为 `Test Files 1 passed (1)`、`Tests 4 passed (4)`，耗时 `1.09s`。
- B275 Web final full tests：恢复变异后在 `web/` 执行 `npm test -- --run` 退出 0；原始输出为 `Test Files 110 passed (110)`、`Tests 1123 passed (1123)`，耗时 `15.72s`，保留既有 jsdom canvas 提示。
- B275 scoped backend 收口命令 `go test ./internal/proto ./internal/collab ./internal/ledger ./internal/agentd`：进程输出先给出 `ok .../internal/proto (cached)`、`ok .../internal/collab (cached)`、`ok .../internal/ledger (cached)`，本次包装轮询未捕获其最终状态；随后独立执行 `go test ./internal/agentd` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 150.383s`。前三包已由该命令输出确认，agentd 由独立命令确认。
- B275 final whitespace check：执行 `git diff --check` 退出 0，原始输出为空。
- B275 final retired-asset scan：执行 `rg -n "RoomsListPage|RoomDetailPage|InboxPage|UpdateToasts|onOpenRooms|onOpenInbox|conversations.html" web/src prototypes/b275-frontend-proto` 退出 1，原始输出为空，未找到旧页、旧入口或孤儿原型引用。
- B275 日志出口补齐后局部复验：`cd web && npm test -- --run src/app/rooms/RoomPanel.test.tsx src/app/cards/CardDrawer.test.tsx` 退出 0，原始输出为 `Test Files 2 passed (2)`、`Tests 45 passed (45)`，耗时 `1.86s`。
- B275 日志出口补齐后类型检查：`cd web && npm run typecheck` 退出 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`。
- B275 final Web lint after log changes：`cd web && npm run lint` 退出 0；原始输出为 `✖ 20 problems (0 errors, 20 warnings)`，未新增 error。
- B275 final Web full tests after log changes：`cd web && npm test -- --run` 退出 0；原始输出为 `Test Files 110 passed (110)`、`Tests 1123 passed (1123)`，耗时 `15.76s`，保留既有 jsdom canvas 提示。
- B275 final Web build after log changes：`cd web && npm run build` 退出 0；原始输出为 `tsc -b`、`vite v6.4.3`、`✓ 1970 modules transformed`、`✓ built in 2.13s`，另有既有 chunk 大小 warning。
- B275 pre-commit scope check：执行 `git status --short && git diff --check && git diff --stat && git ls-files --others --exclude-standard`；`git diff --check` 无输出，修改集为计划内 Go/Web/原型删除/台账文件，未跟踪文件仅 `RoomPanel.test.tsx`、`RoomPanel.tsx`、`roomLog.ts`、`roomPanelModel.ts`，未发现 `web/node_modules`。
- B275 final whitespace check after all edits：再次执行 `git diff --check` 退出 0，原始输出为空。
- B275 staging check：执行 `git add --all && git status --short && git diff --cached --check && git diff --cached --stat`；暂存区包含计划内 49 个文件，`git diff --cached --check` 无输出，新增四个 RoomPanel 相关文件已纳入，未见 `web/node_modules`。
