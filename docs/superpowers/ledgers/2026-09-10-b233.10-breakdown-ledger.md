# B233.10 breakdown 台账

日期：2026-09-10
卡：B233.10
节点：breakdown
基线：`cards/B233.1-charter-7`
法定产出：`docs/superpowers/specs/b233.10-breakdown.md`

## 过程与判断

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-10 | 当前工作树干净，工作分支为 `cards/B233.10-charter-2`，HEAD 为 `e43991da31d0a7777633f4107984e318426ea3c3`；有效基线分支当前解析为 `94246fc8a2acc7996c254cb6b473c7c9b5aef225`。 | `git status --short --branch` → `## cards/B233.10-charter-2`；`git rev-parse HEAD` → `e43991da31d0a7777633f4107984e318426ea3c3`；`git branch --show-current` → `cards/B233.10-charter-2`；`git rev-parse cards/B233.1-charter-7` → `94246fc8a2acc7996c254cb6b473c7c9b5aef225`。 |
| 2026-09-10 | 上游 spec 已批准；contract 原头部仍是“随本提交冻结”，不满足流程要求的“已冻结”状态位，已回写为冻结提交 `e43991da`，并追加拆解期边界澄清 C-0–C-6。 | `sed -n '1,12p' docs/superpowers/specs/b233.10.md` → `状态：**已批准**（2026-09-10，用户原话「同意，批这条 spec。」）`；`sed -n '1,16p' docs/superpowers/specs/b233.10-contract.md` → 原文含 `冻结状态：随本提交冻结`；本回合 apply_patch 后该行为 `冻结状态：已冻结（2026-09-10；冻结提交 `e43991da`）`。 |
| 2026-09-10 | 项目最优图的顶层子系统共 15 个；本卡生产触点提案归 `d_scheduling`（logic）、`d_gateway`（boundary）、`d_orchestration`（logic）、`d_transport`（boundary）。`d_transport_channel`、`d_execution_adapters`、`d_execution_contract` 是子域，不另列顶层。 | `jq -r '.domains | to_entries[] | select((.value.parent // "") == "") | [.key,.value.type,.value.label] | @tsv' codegraph/best.json` → 输出含 `d_orchestration logic 任务编排`、`d_gateway boundary 控制门面`、`d_execution boundary 任务执行`、`d_transport boundary 跨机连接`、`d_protocol logic 协议契约`、`d_ledger logic 卡片账本`、`d_scheduling logic 编制调度` 及其余顶层域；`jq -r '.containers | to_entries[] | select(.key | test("agentd_(Server|Manager|facadeAsRegistry)|client_Client|ledgerstep_Dispatcher|scheduling_Service|proto_")) | [.key,.value] | @tsv' codegraph/best.json` → `k_agentd_Manager d_orchestration`、`k_agentd_Server d_gateway`、`k_client_Client d_transport_channel`、`k_ledgerstep_Dispatcher d_ledger`、`k_proto_Task d_protocol`、`k_scheduling_Service d_scheduling`。 |
| 2026-09-10 | 图命令可用；全局图结构检查通过。视图检查也退出 0，但 warnings 含既有容器未放置/legacy 警告，不能当本卡新增缺陷。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` → exit 0，原始 JSON `{"containers":302,"domainDecls":23,"domains":23,"edgeIssues":null,"edges":5522,"entitiesWithoutLifecycle":2,"issues":null,"nodes":4514,"unscannedEntries":5,...}`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.10-charter check` → exit 0，原始首项 `{"fails":[]}`，其余为既有 warns。 |
| 2026-09-10 | 图符号确认 `Service.Select`、`Service.AdmitFrozen` 是本卡 Ticket 0 空壳，`Server.handleDispatch` 与 `Client.Dispatch` 为既有接缝；`Service.Admit` 调用图存在未扫描债，空边不等于无调用方。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.10-charter sym 'Service.Select'` → `internal/scheduling/scheduling.go:525`，`anchor: ok`，`status: added`；同命令 `sym 'Service.AdmitFrozen'` → `internal/scheduling/scheduling.go:531`，`anchor: ok`，`status: added`；`sym 'Server.handleDispatch'` → `internal/agentd/server.go:1364`，`anchor: moved`；`sym 'Client.Dispatch'` → `internal/client/client.go:762`，`anchor: moved`；`who-calls 'Service.Admit'` → `edges: null`、`unscannedEntries: 5`、`warning: 基线仍有 5 个未扫描入口：查询结果为空不等于没有调用方`。 |
| 2026-09-10 | 源码顺序确认：卡节点当前在起源侧 `Admit` 并于 goroutine defer 释放；`stepTransport` 已把 Ticket 0 的 Carrier/Squad 透传；执行机当前仍按 Receiver 进入 `prepareReceiverBinding`；任务终态在 Done/Stop 释放，且已完成重复 Done 现状仍调用释放。 | `nl -ba internal/agentd/cardstep.go | sed -n '47,175p'` → `admitSquadStep`、`defer s.releaseSchedulingBinding`；`nl -ba internal/agentd/cardstep.go | sed -n '332,364p'` → `cl.Dispatch` 传 `Carrier`/`Squad`；`nl -ba internal/agentd/server.go | sed -n '1318,1418p'` → `prepareReceiverBinding` 后 `Manager.Dispatch`，成功置 `released = true`；`nl -ba internal/agentd/server.go | sed -n '1702,1768p'` → 重复 Done、Done、Stop 均调用 `releaseTaskCarrierOccupancy`。 |
| 2026-09-10 | 源码顺序确认：`Service.AdmitCarrier` 写载体键；`Service.Admit` 走两级准入；`Service.Select`/`AdmitFrozen` 当前返回“尚未接线”；`OccupancyKeys` 已区分载体直派与小队成员键。 | `nl -ba internal/scheduling/scheduling.go | sed -n '375,418p;488,568p'` → `AdmitCarrier` 只写 `carrier/<name>`，`Admit` 走 `admitInto`，`Select`/`AdmitFrozen` 返回空壳错误；`nl -ba internal/scheduling/occupancy_test.go | sed -n '9,54p'` → 现有金样本断言 `carrier/muse`、`squad/exec/muse` 与空小队不生成成员键。 |
| 2026-09-10 | 序列化链路清单已圈定：`ledgerstep.Dispatcher.ViaTemplate` → `ledgerstep.DispatchOpts` → `Server.stepTransport` → `client.DispatchOpts`/手搭 body map → `POST /api/tasks` → `dispatchRequest` → `Manager.DispatchReq`/`proto.Task` → Store；既有 `carrier`/`squad`/`home_dir` 金样本不等于执行准入已接线。 | `rg -n 'HomeDir|Carrier|Squad|DispatchOpts\\{' internal/ledgerstep/dispatch.go internal/ledgerstep/runner.go internal/agentd/cardstep.go internal/client/client.go internal/agentd/server.go` → 命中上述各投影点；`nl -ba internal/client/client.go | sed -n '713,806p'` → body map 对 `carrier`/`squad`/指针 `home_dir` 的现状投影；`nl -ba internal/agentd/server.go | sed -n '1320,1338p'` → `dispatchRequest` 三字段现状形状。 |
| 2026-09-10 | 发现一个需协调者/contract 裁决的生命周期缺口：冻结准入成功后、`Manager.Dispatch` 创建任务前若宿主进程直接重启，现有 deferred release 只覆盖返回错误，源码未显示可持久化的执行占用所有权；该行为事实未用真机验证，不能在拆解中假设已安全。 | 静态顺序见 `nl -ba internal/agentd/server.go | sed -n '1377,1418p'` → 准入/绑定在 `Manager.Dispatch` 之前、返回错误才执行 defer；`rg -n 'RecoverOnStartup|sched_running|releaseTaskCarrierOccupancy' internal/agentd internal/scheduling` → 命中任务恢复与释放现有入口，但未见本窗口的 durable reservation 载体。结论标记“未验证，需真机”；若要新增恢复载体，先退回 contract。 |
| 2026-09-10 | 架构法第三条命中 `internal/agentd` oversized/prefix warning，但本卡可按接缝圈出有限文件集，不插竖切还债卡；`cmd` oversized 与本卡无关。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.10-charter check 2>/dev/null | jq -c '{fails,oversized:[.warns[] | select(.kind=="oversized-package")],prefix:[.warns[] | select(.kind=="prefix-family" and (.detail|contains("internal/agentd")))]}'` → `fails:[]`；oversized `internal/agentd` 为 `83` 个图内源文件，prefix 为 `preview=10`、`project=5`、`work=6`；均是既有架构债。 |
| 2026-09-10 | 发现已批准 spec 的功能线快照与当前本地基线 ref 不一致；不擅自改写上游 spec，已在 contract §11 C-6 留痕，列为待拍板。 | `git show -s --format='%H %s' 3fd6874f` → `3fd6874f15e6eff32c606ac9a07637904b1602e3 docs(B233.9): 拷本机凭据后补跑跨机 dispatch 与写闸改名`；`git show -s --format='%H %s' cards/B233.1-charter-7` → `94246fc8a2acc7996c254cb6b473c7c9b5aef225 fix(B233.1): 修复审批复用、Continue 重绑与契约图 (charter-6)`；`git merge-base --is-ancestor 3fd6874f cards/B233.1-charter-7` → exit 1。 |
| 2026-09-10 | 法定文档锚点检查通过；首次检查报 `internal/agentd/receiver_bind.go#Server.prepareReceiverBinding (vanished)`，已改为文件指针加符号文本并补齐短路径；复跑 exit 0。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.10-charter resolve --doc docs/superpowers/specs/b233.10-breakdown.md` → exit 0；原始输出为 JSON `{"anchors":[{"ref":"internal/scheduling/scheduling.go#Service.Select","anchor":"ok"}, {"ref":"internal/agentd/cardstep.go#Server.startCardStep","anchor":"ok"}, {"ref":"internal/agentd/scheddispatch.go#Server.admitSquadStep","anchor":"ok"}, {"ref":"internal/agentd/server.go#dispatchRequest","anchor":"ok"}, {"ref":"internal/store/store.go#Store.CreateTask","anchor":"ok"}, {"ref":"internal/client/client.go#DispatchOpts","anchor":"ok"}, {"ref":"internal/ledgerstep/dispatch.go#DispatchOpts","anchor":"ok"}, {"ref":"internal/ledgerstep/runner.go#StepRunner.dispatchNodeWithGate","anchor":"ok"}, {"ref":"internal/proto/proto.go#Task","anchor":"ok"}]}`，其余解析项为 `moved`。 |
| 2026-09-10 | 文档尾空格与 diff 检查通过。 | `git diff --check` → exit 0；`rg -n '[[:blank:]]+$' docs/superpowers/specs/b233.10-breakdown.md docs/superpowers/specs/b233.10-contract.md docs/superpowers/ledgers/2026-09-10-b233.10-breakdown-ledger.md` → 无输出。 |
| 2026-09-10 | 增补 `d_ledger`/`d_protocol` 依赖型子系统的逐族结论后，文档锚点与差异检查仍通过。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.10-charter resolve --doc docs/superpowers/specs/b233.10-breakdown.md` → exit 0，原始 JSON 的 `anchors` 中无 `vanished`/`file_missing`；`git diff --check` → exit 0。 |
| 2026-09-10 | 初次提交已创建；后续只按纪律 amend 一次把本记录收进同批提交。 | `git commit -m "docs(B233.10): 出稿一次派发生命周期拆解提案"` → 原始输出：`[cards/B233.10-charter-2 5783559f] docs(B233.10): 出稿一次派发生命周期拆解提案`；`3 files changed, 392 insertions(+), 1 deletion(-)`；创建台账、拆解稿，修改 contract。 |
| 2026-09-10 | 本轮重跑工作树干净，当前执行分支为 `cards/B233.10-charter-4`，HEAD 为 `0f419bb162b1d09031117ce7df7d433c7da2d0a7`；本地有效基线同名分支仍为旧指针 `94246fc8`。 | `git status --short --branch` → `## cards/B233.10-charter-4`；`git rev-parse HEAD` → `0f419bb162b1d09031117ce7df7d433c7da2d0a7`；`git branch --show-current` → `cards/B233.10-charter-4`；`git rev-parse cards/B233.1-charter-7` → `94246fc8a2acc7996c254cb6b473c7c9b5aef225`。 |
| 2026-09-10 | P1 已按方案 A 裁决：spec 的 `3fd6874f` 是现状读数且为 `origin/cards/B233.1-charter-7 @ a1571673` 的祖先；不改 spec，本地 `94246fc8` 不作为冻结基线。 | `git show-ref --verify refs/remotes/origin/cards/B233.1-charter-7` → `a1571673a22c0740e666cdadbe9617948d76a557 refs/remotes/origin/cards/B233.1-charter-7`；`git show -s --format='%H %s' origin/cards/B233.1-charter-7` → `a1571673a22c0740e666cdadbe9617948d76a557 docs(B233.10): spec 批准 L3 轻档（一次派发生命周期）`；`git merge-base --is-ancestor 3fd6874f origin/cards/B233.1-charter-7` → exit 0。 |
| 2026-09-10 | 回写 contract §11 C-6：记录 P1 已裁决的远端祖先关系与旧执行机指针语义；回写 breakdown 稿首为“已拍板（2026-09-10）”，P2 #77–#90 纳入同一张实现卡验收，P3 采用单卡。 | `sed -n '1,12p' docs/superpowers/specs/b233.10.md` → spec 状态为 `已批准（2026-09-10，用户原话「同意，批这条 spec。」）`；`sed -n '1,10p' docs/superpowers/specs/b233.10-contract.md` → contract 状态为 `已冻结（2026-09-10；冻结提交 e43991da）；P2 补冻随本提交冻结`；`git log --oneline --decorate -3` → `0f419bb1 ... contract(B233.10): freeze P2 startup reconciliation`、`f81a4920 ... docs(B233.10): 出稿一次派发生命周期拆解提案`、`e43991da ... contract(B233.10): freeze dispatch lifecycle seam`。 |
| 2026-09-10 | 按 `best.json` 顶层 `parent` 为空的 15 个领域核对触点：本稿功能承接 `d_scheduling`（logic）、`d_gateway`（boundary）、`d_orchestration`（logic）、`d_transport`（boundary）；`d_execution` 仅作 boundary 真机项，`d_ledger`/`d_protocol` 为依赖核对，不单独派卡。 | `jq -r '.domains | to_entries[] | select((.value.parent // "") == "") | [.key,.value.kind,.value.label] | @tsv' codegraph/best.json` → `d_orchestration`、`d_gateway`、`d_workspace`、`d_execution`、`d_sessions`、`d_transport`、`d_protocol`、`d_ledger`、`d_collab`、`d_cli`、`d_web`、`d_policy`、`d_maintenance`、`d_scheduling`、`d_keystone`；本稿对应类型与文件集已回写。 |
| 2026-09-10 | 默认 best 图解析本稿锚点失败；因 Ticket 0 符号只在本卡视图中，不能把默认图失败写成通过，按既有视图约定重跑。 | `codegraph --repo . resolve --doc docs/superpowers/specs/b233.10-breakdown.md` → 原始报错：`Error: 文档锚点检查失败: internal/scheduling/scheduling.go#Service.Select (vanished)`；后续用 `--view cards-B233.10-charter`。 |
| 2026-09-10 | 按本卡分支视图重跑文档锚点成功；无 `vanished`/`file_missing`，共 23 个锚点。 | `set -o pipefail; codegraph --repo . --view cards-B233.10-charter resolve --doc docs/superpowers/specs/b233.10-breakdown.md | jq -c '{anchorCount:(.anchors|length),bad:[.anchors[] | select(.anchor=="vanished" or .anchor=="file_missing")]}'` → `{"anchorCount":23,"bad":[]}`。 |
| 2026-09-10 | 目标图契约检查成功；`fails` 为空，147 条 warning 为既有图覆盖/legacy/oversized 类提示，本卡无新增图失败。 | `set -o pipefail; codegraph --repo . --view cards-B233.10-charter check | jq -c '{fails,warningCount:(.warns|length),bestCoverage}'` → `{"fails":[],"warningCount":147,"bestCoverage":{"assignedContainers":262,"viewContainers":301,"crossDomainEdges":1053,"misplacedSkipped":135}}`。 |
| 2026-09-10 | 试图把图检查输出写入 `/tmp` 时失败；未把失败原因自行归因，随后改用直接输出/管道过滤完成检查。 | `codegraph --repo . --view cards-B233.10-charter check >/tmp/b23310-check.json` → `/bin/bash: line 1: /tmp/b23310-check.json: Read-only file system`；`sed: can't read /tmp/b23310-check.json: No such file or directory`。 |
| 2026-09-10 | 文档差异与尾空格检查成功。 | `git diff --check` → 无输出；`rg -n '[[:blank:]]+$' docs/superpowers/specs/b233.10-breakdown.md docs/superpowers/specs/b233.10-contract.md docs/superpowers/ledgers/2026-09-10-b233.10-breakdown-ledger.md` → 无输出。 |
| 2026-09-10 | 冻结条目逐条核对计数成功，#1–#90 每项恰好一行，无缺号、重复号。 | `awk -F'|' '/^\\| [0-9]+ / {n=$2+0; seen[n]++; count++} END {missing=""; dup=""; for (i=1;i<=90;i++) if (!seen[i]) missing=missing i ","; else if (seen[i]>1) dup=dup i ","; print "rows=" count " missing=" (missing==""?"none":missing) " duplicates=" (dup==""?"none":dup)}' docs/superpowers/specs/b233.10-breakdown.md` → `rows=90 missing=none duplicates=none`。 |
| 2026-09-10 | 任务指定的实现起点 `cards/B233.10-charter-3`、当前执行分支 `cards/B233.10-charter-4` 与远端同名分支均指向同一 HEAD；不切换分支。 | `git rev-parse cards/B233.10-charter-3` → `0f419bb162b1d09031117ce7df7d433c7da2d0a7`；`git rev-parse cards/B233.10-charter-4` → `0f419bb162b1d09031117ce7df7d433c7da2d0a7`；`git rev-parse origin/cards/B233.10-charter-3` → `0f419bb162b1d09031117ce7df7d433c7da2d0a7`。 |
| 2026-09-10 | 已核对代表性契约/测试入口可定位；`internal/executor` 为目录而非单文件，故文档仅作根契约/边界指针，不把整个执行器纳入生产修改集。 | `test -f internal/proto/contract_fixture_test.go && echo 'contract_fixture_test.go exists'` → `contract_fixture_test.go exists`；`rg --files internal/executor` → 输出现有执行器目录文件；`test -f docs/superpowers/specs/b233.10-breakdown.md && echo 'breakdown exists'` → `breakdown exists`。 |

## 收口记录

| 2026-09-10 | 收口前复核成功：分支视图锚点无坏锚，目标图无 fails，#1–#90 无缺号/重复，差异检查无输出；尾空格扫描无匹配（该工具以无匹配返回 exit 1）。 | `codegraph --repo . --view cards-B233.10-charter resolve --doc docs/superpowers/specs/b233.10-breakdown.md | jq -c '{anchorCount:(.anchors|length),bad:[.anchors[] | select(.anchor=="vanished" or .anchor=="file_missing") ]}'` → `{"anchorCount":23,"bad":[]}`；`codegraph --repo . --view cards-B233.10-charter check | jq -c '{fails,warningCount:(.warns|length),bestCoverage}'` → `{"fails":[],"warningCount":147,"bestCoverage":{"assignedContainers":262,"viewContainers":301,"crossDomainEdges":1053,"misplacedSkipped":135}}`；冻结条目 awk → `rows=90 missing=none duplicates=none`；`git diff --check` → 无输出；`rg -n '[[:blank:]]+$' ...` → 无输出、exit 1；`git status --short --branch` → `## cards/B233.10-charter-4` 加 3 个修改文件。 |

| 2026-09-10 | 本轮法定产出首次提交已创建；随后只 amend 一次把本提交事实收进同批提交，不追写 amend 后 hash。 | `git add docs/superpowers/specs/b233.10-breakdown.md docs/superpowers/specs/b233.10-contract.md docs/superpowers/ledgers/2026-09-10-b233.10-breakdown-ledger.md && git commit -m "docs(B233.10): 回写已拍板拆解"` → 原始输出：`[cards/B233.10-charter-4 37b0eb4b] docs(B233.10): 回写已拍板拆解`；`3 files changed, 76 insertions(+), 53 deletions(-)`。 |

本台账按“先记录事实，再写拆解，再跑文档/图校验，最后记录提交”追加。提交命令与原始输出在收口时追加；随后只 amend 一次收进同批提交，不追写 amend 后 hash。
