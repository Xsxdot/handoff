# B369.6（S6）plan 节点台账

节点：plan（卡 B369.6「S6 响应式四 tab 与终端输入层」）。分支 `cards/B369.6-charter`，父卡 `4ab6415a`（有效基线 `cards/B369-charter-4`）。法定产出：`docs/superpowers/plans/b369.6-plan.md`。本节点只出计划文档，不写实现代码。

## 一、基线复核（本轮实测，原始读数）

| 命令 | 原始结果 |
| --- | --- |
| `go version` | `go version go1.26.1 linux/amd64` |
| `node --version` | `v24.16.0` |
| `npx vitest --version` | `vitest/4.1.10 linux-x64 node-v24.16.0` |
| `go build ./...`（根） | EXIT=0 |
| `cd mobile && go build ./...` | EXIT=0 |
| `go test ./internal/proto/... ./internal/mobilecore/... -count=1` | 两者 `ok`（EXIT=0） |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.069s` |
| `codegraph check --view cards-B369-charter` | `fails=[]`（EXIT=0） |
| `codegraph validate --view cards-B369-charter` | `issues=null`、`edgeIssues=null` |
| `codegraph check`（基线） | `fails=[]` |
| `cd web && npm ci` | `added 290 packages`（工作树起于零 node_modules） |
| `cd web && npx vitest run` | `Test Files 130 passed (130)`、`Tests 1414 passed (1414)`、`Duration 23.00s` |
| `cd web && npx tsc -b` | EXIT=0 |
| `cd web && npx eslint .` | **EXIT=1，4 errors**：`terminalHostResponse.ts:45/47/49 no-control-regex`、`pty.ts:115 no-case-declarations`（本卡 T0 的依据） |

## 二、图覆盖债（`codegraph sym <名> --view cards-B369-charter`）

- 命中：`Shell`、`ProjectTree`、`CardItem`、`CardsPage`、`SettingsPage`、`FileTree`、`usePoll`、`useWorkbench`、`installTerminalInputFix`、`TerminalTab`、`isDesktopShell`、`defaultBoardLayout`。
- **未命中（债）**：`SessionSidebar`、`SessionTab`、`sessionModel` 不在图（无近似候选；grep 确认文件存在）。本卡新增符号（`useShellViewport`/`MobileTabBar`/`MobileKeybar`/`statusVocab`）同为未来首建，图未覆盖。web 新增符号的图对账归父卡 integrate/后续图对齐节点；本节点不补图、不改 `codegraph/` 冻结物。债记入 plan §2。

## 三、判据先在基线跑（关键，逐条实测）

- **eslint 转绿判据**：给 `terminalHostResponse.ts` 加文件级 `/* eslint-disable no-control-regex -- ... */`、把 `pty.ts` 的 `case 'attached':` 包成块 `{...}`，实测 `npx eslint .` → **EXIT=0（0 errors）**、`npx tsc -b` → EXIT=0。**两文件已 `git checkout --` 还原，工作树干净**（`git status --porcelain` 空）。
- **断点判据**：jsdom 实测 `window.innerWidth === 1024`、`window.matchMedia === undefined`。临时实现 `useShellViewport`+`viewportOf` 并跑探针：`viewportOf(375/767)='phone'`、`768/1023='pad'`、`1024='desktop'`；hook 在 stub 375 下渲染 `phone/true`、1024 下 `desktop/false`（3 tests passed）。探针文件已删。
- **词表读源码判据**：在 `web/src/app/cards/` 下用 `readFileSync(resolve(here,'../../../../internal/ledger/types.go'))` 实测读出 `["待办","进行中","待审阅","已完成","终止"]`（1 test passed）；`process.cwd()` = `/root/.handoff/worktrees/1faddafb/web`，用 `fileURLToPath(import.meta.url)` 相对定位与 cwd 两条都通。探针已删。
- **IME 时序判据（库行为出处）**：`@xterm/xterm@5.5.0`（`web/node_modules/@xterm/xterm/package.json`）。真 `Terminal` 探针实测：`compositionstart` → 设 `textarea.value` → `compositionend`，**不 await** 时 `onData` 为空数组（假绿陷阱），**await 一个宏任务（10ms）后收到 `['你好']` 恰一次**；补漏装/不装结果一致。出处：`lib/xterm.js` 的 `CompositionHelper._finalizeComposition` 用 `setTimeout(...,0)` 从 `textarea.value` 取串发 `triggerDataEvent`。plan §8.4 的用例含显式 await 与 why 注释。探针全部删除。
- **`insertFromComposition` 变体**：实测 `inputType:'insertFromComposition'` 的事件不参与落定（它由 compositionend 驱动），`isComposing` 键不影响；故 plan 的用例用 `compositionend` 驱动 + await，不依赖 inputType 变体。
- **Shell 测试状态复位**：`Shell.test.tsx` 顶部 **零 `afterEach`**（`grep -c afterEach` = 0）→ plan 新增 `afterEach` 复位 `innerWidth`，防用例顺序污染。

## 四、试错记录

- 首轮读别的任务的 plan 文档（`/root/.handoff/worktrees/0a2774e7/...`）被协调者拒（越出任务范围）。改用 `git fetch origin cards/B369.1-charter --no-tags` + `git show origin/cards/B369.1-charter:docs/superpowers/plans/b369.1-plan.md`（只读），并用本工作树既有的 `b358.7-plan.md`/`b358.8-plan.md` 作形态参考。
- 临时探针文件（`__probe_ime.test.ts`/`__vp_probe.test.*`/`__vocab_probe.test.ts`/`__cwd_probe.test.ts`/`__ime*.test.ts`/`__eslint_probe.ts`/`__fs_probe.test.ts`/`__tscheck.test.ts`）全部跑完即删；`git status --porcelain` 收口时为空。
- `web/node_modules` 在起手时不存在（`npm ci` 前 `npx vitest` 因缺 `@tailwindcss/vite` 报 `ERR_MODULE_NOT_FOUND`）；`npm ci` 后 290 包实装，基线 1414 测试全绿。
- 临时探针首版用 `.ts` 写含 JSX 的 Shell 探针，esbuild 报 `Expected ">" but found "data"`；改 `.tsx` 后通过（plan 的 `useShellViewport.test.tsx`/`MobileTabBar.test.tsx` 因此用 tsx 后缀）。

## 五、本节点判断

- P1/P2/P3 拍板（全案 A）已逐条落到 plan 的 task 边界：不新开 `/m` 树（T1 条件渲染）、不新增账本写入面（T2 复用 `moveCard`、`web/src/api/**` 零改动）、键条落 `MobileKeybar.tsx` 非 `terminal*` 前缀（T4）。
- 三条 spec 声明缝中只有缝 3（移动终端输入层）属 S6；三条内部锁（响应式壳/词表/项目离线）已按唯一合法形状在 plan §11 声明「从声明缝构造不出 + 为什么」。
- 真机项（§13）四条标「未验证，需真机」，且均由协调者执行、不派发（web 响应式 + IME + 配对清单数据 + 扫码入口）。
- 无退回 contract 项；无占位符；跨 task 类型一致（本卡无跨卡签名交接）。

## 六、提交事实（历史读数）

```
$ git add docs/superpowers/plans/b369.6-plan.md docs/superpowers/ledgers/2026-09-19-b369.6-plan-ledger.md && git commit -m "plan(B369.6): S6 响应式四 tab 与终端输入层实现计划"
```

（随后按红线只 amend 一次把本台账段收进同批提交——amend 会换 hash，此为 git 事实，不复写历史读数。）
