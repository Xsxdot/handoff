# v0.4.0 发布台账

本台账记录 v0.4.0 发布准备、阻塞根因、实际验收命令与结果。

边界：只记录本次版本发布与发布前置门修复，不纳入工作区中已有的其他未提交改动。

## 2026-09-03

- 发布前状态：`main` 与 `origin/main` 同步，最新正式标签为 `v0.3.9`；工作区已有 `internal/ledger/*` 修改、`.b266-*` 辅助文件及其他未跟踪内容，本次未触碰。
- 远端事实：当前 HEAD `a930e3e1` 的 CI run `33762477414` 失败，失败 job 为 `go`；此前相邻 main push run `33760783244`、`33758432162` 同样失败。
- 失败原文：`TestRepoContractGate` 报 `d_orchestration->d_workspace 直调 20 条超出预算 19`。
- 根因取证：`985041e4` 将代码图基线从旧扫描版切换为完整扫描版，完整扫描误加入两条伪边：`n_agentd_Manager_ReclaimList -> n_agentd_DirtyWorktreeError_Error` 与 `n_agentd_atomicReplace -> n_agentd_runOutputBuffer_Write`。源码及对应函数流均未发现这两条调用；前者是 `m.log.Error` 的同名方法误配，后者是 `*os.File.Write` 的同名方法误配。
- 修复决定：从 `codegraph/baseline.json` 删除两条伪边，保持 `codegraph/target.json` 的真实 19 条预算不变；将 `[Unreleased]` 说明冻结为 `## [v0.4.0] - 2026-09-03`。
- 首轮复跑：`go test ./cmd -run TestRepoContractGate -count=1 -v` 退出 0；契约闸 PASS，`d_orchestration->d_workspace` 实际命中 18 条，低于 19 条预算。
- 变异复验：临时把该预算改为 17，`go test ./cmd -run TestRepoContractGate -count=1 -v` 退出 1，并明确报 `18 条超出预算 17`；恢复为 19 后再进入全量验证。
- 变异还原：恢复预算 19 后同一契约闸命令退出 0，JSON 断言确认目标条目仍为 19。
- 发布准备提交：`git commit -m "chore(release): prepare v0.4.0"` 退出 0，首次实际输出为 `[main 6d317e0e] chore(release): prepare v0.4.0`；为补记提交与验收证据执行两次 amend，最终仍为单一发布准备提交。
- 发布前验证：隔离 worktree `/private/tmp/handoff-release-v0.4.0` 上 `go build ./...`、`go vet ./...`、NUL-safe `gofmt -l`、`bash install_test.sh`、`GOOS=windows GOARCH=amd64 go build ./...`、`GOOS=windows GOARCH=arm64 go build ./...`、`GOOS=windows GOARCH=amd64 go vet ./...` 均退出 0。
- 全量 Go 测试：默认并发 `go test ./... -count=1` 首轮因本机默认临时目录过长导致 agy Unix socket 路径超限，并在全量负载下触发 hostapi 1 秒假 CLI 时序失败；改用短 `TMPDIR=/private/tmp/handoff-v040-testtmp` 后仍只出现该 hostapi 时序失败，单测重复 10 次全通过；最终 `TMPDIR=... go test ./... -count=1 -p 1` 退出 0，所有包通过。
- Web 验证：`npm ci` 退出 0（仅 Node 23 engine 警告）；`npm run build` 退出 0（仅 chunk size 警告）；首次 `npm test` 为 1318/1319，唯一失败为既有 FileTab 冲突状态测试的时序断言，单测通过后再次完整 `npm test` 为 124/124 文件、1319/1319 测试通过；`scripts/build-release-local.sh` 退出 0，embedweb 产物约 27M 且工作区状态未变。
- 待完成：将发布准备提交推送到 `origin/main`，推送 `v0.4.0` 标签，等待并核对 GitHub Actions 与 Release 资产。
