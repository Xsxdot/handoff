# B370 plan 台账（镜像身份闸降级判定）

节点：plan（输入 `docs/superpowers/specs/b370.md`、`b370-contract.md`、`b370-breakdown.md`；breakdown §9 已拍板 F1–F7 全 A）。
卡：B370　基线分支：`cards/B233.1-charter-7`（合并目标）　本分支：`cards/B370-charter-7`。
产出：`docs/superpowers/plans/b370-plan.md`（本节点唯一法定产出物；不写实现、不建脚手架）。
一行一条历史读数；命令与原始输出照抄。

## 1. 上游状态位（读文件头）

1. 2026-09-15：`b370.md:3` `状态：**已批准**（2026-09-14，用户原话「批准，进 contract」）`——批准。
2. 2026-09-15：`b370-contract.md:4-5` `本分支：cards/B370-charter-4`、`冻结状态：本提交随 codegraph/target.json、codegraph/diffs/cards-B370-charter-4.json 与 Ticket 0 骨架同批冻结`——已冻结。
3. 2026-09-15：`b370-breakdown.md:3` `状态：**已拍板**（2026-09-14；... 本地协调者已裁决 F1–F7，裁决见 §9）`——拍板已回填。

## 2. 图与代码现状查证

4. 2026-09-15：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym JudgeMirroredWake` 原始输出：`Error: 符号 "JudgeMirroredWake" 不在图中（图未覆盖或名字有误）；近似候选: []；exit status 1`——图覆盖债（另四符号 WakeGateReason/WakeGateDecision/WakeGateEvent/CurrentWorkflowAttempt 同欠）。
5. 2026-09-15：`codegraph sym cardWaitCurrentWorkflowAttempt` 命中 `n_cmd_cardWaitCurrentWorkflowAttempt`（`d_cli`，`cmd/card_wait.go:195`，anchor=ok）；`codegraph sym currentWorkflowAttempt` 命中 `n_agentd_Server_currentWorkflowAttempt`（`d_gateway`，`internal/agentd/wakeconsumer.go:56`，anchor=ok）——两个待退役符号确在基线图。
6. 2026-09-15：`grep -rn "cardWaitCurrentWorkflowAttempt\|currentWorkflowAttempt" --include=*.go .` 只命中各自定义处（`cmd/card_wait.go:192/195`、`internal/agentd/wakeconsumer.go:53/57`）与调用点，无第三个消费点。
7. 2026-09-15：读 `internal/ledger/events.go#Store.EventsFromAsc`（`:63-103`）：`seq > fromSeq` 排他、`ORDER BY seq ASC`、`LIMIT limit`，`limit<=0` 取 1000。
8. 2026-09-15：读 `internal/ledger/events.go#DispatchSnapshot`（`:115-139`）：`Target/TaskID/Node/Attempt/Branch/Purpose` 等字段；`Node/Attempt` 带 `omitempty`。
9. 2026-09-15：读 `internal/ledger/mirror.go#Store.AppendMirroredEvent`（`:62`）：payload 由 `fmt.Sprintf("{\"node\":%q,\"attempt\":%q,...}")` 生成，键恒存在（值可空串）——「缺键」形态只能用 raw `INSERT` 造。
10. 2026-09-15：读 `internal/ledger/store.go:322-331`：SQLite `card_events` 有可空列 `source_target/source_task/source_seq`，raw 夹具可只填部分列。

## 3. 基线亲跑读数（动手前）

11. 2026-09-15：`go build ./...` 退出码 0。
12. 2026-09-15：`go vet ./internal/client/ ./internal/agentd/ ./cmd/` 退出码 0。
13. 2026-09-15：`go test ./cmd/ -run 'TestB349CardWaitSourceIdentity|TestB349CardWaitSubtreeUsesEventCardIdentity|TestB353CardWait' -count=1` → `ok  github.com/Xsxdot/handoff/cmd  20.435s`。
14. 2026-09-15：`go test ./internal/agentd/ -run 'TestB349AutomationSourceIdentity|TestB2336StaleAttemptDoesNotWake|TestB2336WakeconsumerEnvelopeJSONBoundaries|TestB353AutomationUsesWaitDeliveryPolicy' -count=1` → `ok  github.com/Xsxdot/handoff/internal/agentd  2.445s`。
15. 2026-09-15：`go test ./internal/client/ -count=1` → `ok  github.com/Xsxdot/handoff/internal/client  9.386s`；`go test ./internal/ledger/ -count=1` → `ok  github.com/Xsxdot/handoff/internal/ledger  19.174s`。
16. 2026-09-15：`codegraph --repo . --view cards-B370-charter-4 check` 退出码 0，fails 空；`codegraph --repo . check`（无视图）退出码 1，fails 1 条 `dead-contract d_transport→d_ledger`（baseline 未重扫的已知红，contract §8）；`codegraph --repo . validate` 退出码 0。
17. 2026-09-15：`gofmt -l internal/agentd/wakeconsumer.go` 有输出，`gofmt -d` 显示是既有注释空行差异（`:230` 段落），非本卡引入；`internal/client/wakegate.go`、`cmd/card_wait.go` 无差异。

## 4. 关键机制探针（亲跑，不落库）

18. 2026-09-15：`scripts/codegraph-rescan` 全量重扫工作树（`go run . --repo ../.. --old ../../codegraph/baseline.json --out /tmp/.../baseline.new.json`）原始 stderr 末行：`nodes=5364 containers=338 edges=6741 implements=65 projections=460 lifecycle=133 packages=89`——重扫可用，T4/F7 步骤落地。
19. 2026-09-15：以临时视图 `cards-B370-charter-7.json`（派生自 fresh baseline，含契约 5 符号 + 两退役符号 `nodesDeleted`）跑 `codegraph validate` 退出码 0、`codegraph --view cards-B370-charter-7 check` fails 空、退出码 0——F7 派生流程可行。随后已删除临时视图，工作树干净。
20. 2026-09-15：`internal/client` 包子测试探针（临时 `zz_b370_probe_test.go`，跑后已删）：用 `ledger.Open` + `PutWorkflow` + `CreateCard` + `RecordDispatch` 起真实 SQLite，`CurrentWorkflowAttempt` 命中 `att-1`；`JudgeMirroredWake` 对空身份当前返回 `{Deliver:false Reason:empty_workflow_identity}`——确认骨架保守态，测试 harness 可行。
21. 2026-09-15：`codegraph resolve --doc docs/superpowers/plans/b370-plan.md` 退出码 0；16 锚：6 `ok`、10 `moved`、0 `vanished`/`file_missing`（首跑曾因两个未来符号 `#scanCardDispatchSnapshots`/`#judgeEmptyEnvelopeIdentity` 报 vanished，已把两行改为「`wakegate.go` 内新未导出 `...`」措辞，不写不存在的锚）。

## 5. 落稿结论

22. 2026-09-15：计划落 `docs/superpowers/plans/b370-plan.md`，按 breakdown §3 的 T0–T4 与入口指针；接缝覆盖闸按 spec §测试决定矩阵（7 情形 × 两消费点）；F1–F7 全 A 逐条对齐（计划 §11.3）。
23. 2026-09-15：内部锁例外已声明（`internal/client/wakegate_test.go` 直测是附加内部锁；理由：nil store / 分页到尾 / `node==""` 从声明缝构造不出，见计划 §7.4）。
24. 2026-09-15：本节点产出物是计划文档，不含实现代码、脚手架、codegraph diff 或台账外的运行时改动。计划稿后已把全部探针（临时实现、临时测试、临时视图）回滚，`git status` 仅剩两份新文档。

## 6. 计划代码亲跑验证（探针，跑后全部回滚）

25. 2026-09-15：把计划中的 `wakegate.go` 全文与 `client/wakegate_test.go` 测试代码原样写入工作树，`gofmt -l` 无输出，`go test ./internal/client/ -run 'TestB370' -count=1 -v` → `--- PASS` 四支（`TestB370CurrentWorkflowAttempt` 含 5 子测、`TestB370JudgeMirroredWakeEmptyIdentity` 含 6 子测、`TestB370JudgeMirroredWakeNonEmptyIdentity` 含 6 子测、`TestB370JudgeMirroredWakeNilStore`），`ok github.com/Xsxdot/handoff/internal/client 3.234s`。
26. 2026-09-15：把计划 T2 的三处替换（删 `cardWaitCurrentWorkflowAttempt`、删 `Server.currentWorkflowAttempt`、两消费点 reason 日志）原样写入，`go build ./...`、`go vet ./cmd/ ./internal/agentd/` 退出码 0；`gofmt -l` 只剩 `wakeconsumer.go` 既有 `:230` 注释空行差异（非本卡引入）。
27. 2026-09-15：把计划 T3 的三支穿缝测试原样追加，`go test ./cmd/ -run 'TestB370|TestB349CardWait|TestB353CardWait' -count=1` → `ok cmd 37.570s`；`go test ./internal/agentd/ -run 'TestB370|TestB349AutomationSourceIdentity|TestB2336StaleAttemptDoesNotWake|TestB2336WakeconsumerEnvelopeJSONBoundaries|TestB353Automation|TestAutomationEventMappingThroughConsumer' -count=1` → `ok internal/agentd 7.217s`。B349 三支与 B2336 保绿在真实实现下成立。
28. 2026-09-15：F7 派生流程用真实实现代码亲跑：`scripts/codegraph-rescan` 全量重扫 → `nodes=5364 containers=338 edges=6742 ...`；按计划脚本派生 `codegraph/diffs/cards-B370-charter-7.json`（added 7 / modified 2 / deleted 2 / edgesAdded 13 / edgesDeleted 6）；`codegraph validate` 退出码 0、`codegraph --view cards-B370-charter-7 check` fails 空、退出码 0。节点 id 与计划预期一致（`n_client_scanCardDispatchSnapshots`、`n_client_judgeEmptyEnvelopeIdentity`）。
29. 2026-09-15：探针全部回滚（`git checkout --` 五个文件、`rm wakegate_test.go` 与临时视图），`git status --short` 只剩 `docs/superpowers/plans/b370-plan.md`、`docs/superpowers/ledgers/2026-09-15-b370-plan-ledger.md` 两份本节点产出。

## 7. 提交（历史读数）

30. 2026-09-15：本节点实际建提交，命令 `git add docs/superpowers/plans/b370-plan.md docs/superpowers/ledgers/2026-09-15-b370-plan-ledger.md && git commit -m ...`；提交当时 `git log --oneline -1` 原始输出：`e52030e1 plan(B370): 身份闸降级判定实现计划——T0–T4 单轮 + 穿缝矩阵 + F7 分支视图重扫`；`git show --stat --oneline HEAD` 原始输出 `2 files changed, 1485 insertions(+)`。本条回填后 amend 一次收进同一提交（amend 会换 hash，收口判据是工作树干净，不 chase hash）。
