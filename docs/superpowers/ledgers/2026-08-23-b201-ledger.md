# B201 执行台账

- 2026-08-23：当前工作树为 `/root/.handoff/worktrees/a382a198`，分支为 `cards/B201-charter`，基线提交为 `80bb199ab471a9228652b3780e30fd4a274f838a`；`git status --short --branch` 仅输出 `## cards/B201-charter`。
- 2026-08-23：`git diff --check` 退出 0、无输出；基线 Go 为 `go1.26.1 linux/amd64`；`go env GOMOD` 为 `/root/.handoff/worktrees/a382a198/go.mod`。
- 2026-08-23：已完整读取 `docs/superpowers/specs/2026-08-23-b201-node-output-attach.md`；本节点产物是实现计划，不实现代码。
- 2026-08-23：`codegraph` 未安装，命令输出为 `codegraph: command not found`；本计划的现状签名改用 `rg`/源码行号核对，并登记为图覆盖债。
- 2026-08-23：基线命令 `go test ./internal/ledger -run 'TestWorkflow(LegacyDefStillDecodes|NodesProjectToStates|NodeCarriesPurposeAndAcceptanceSwitch|PutWorkflowAcceptsGoodNodes)' -count=1` 真实通过，原始输出：`ok github.com/Xsxdot/handoff/internal/ledger 0.384s`。
- 2026-08-23：基线命令 `go test ./internal/ledgerstep -count=1` 真实通过，原始输出：`ok github.com/Xsxdot/handoff/internal/ledgerstep 3.844s`。
- 2026-08-23：基线命令 `go test ./internal/proto -run TestContractFixtures -count=1` 真实通过，原始输出：`ok github.com/Xsxdot/handoff/internal/proto 0.003s`。
- 2026-08-23：基线命令 `npm run typecheck`（目录 `web`）失败，原始输出：`> web@0.0.0 typecheck`、`> tsc -b`、`sh: 1: tsc: not found`；未将其写成代码结论，web 判据待依赖可用后复跑。
- 2026-08-23：基线命令 `npx vitest run src/api/contract.test.ts src/app/flows/NodeEditor.test.tsx src/app/flows/NodeEditor.purpose.test.tsx`（目录 `web`）失败，原始输出含 `npm error code EROFS`、`npm error syscall open`、`npm error path /root/.npm/_cacache/tmp/***`、`npm error rofs Invalid response body while trying to fetch https://registry.npmjs.org/vitest: EROFS: read-only file system`；未将其写成代码结论，web 判据待依赖可用后复跑。
- 2026-08-23：基线命令 `go build ./...` 真实退出 0，无输出。
- 2026-08-23：基线命令 `go vet ./internal/ledger ./internal/ledgerstep ./internal/proto ./internal/client` 真实退出 0，无输出。
- 2026-08-23：基线命令 `gofmt -l internal/ledger/types.go internal/ledger/workflows.go internal/ledgerstep/node.go internal/ledgerstep/runner.go internal/ledgerstep/dispatch.go internal/proto/ledger.go internal/agentd/ledgerapi.go` 真实退出 0，无输出。
- 2026-08-23：基线命令 `go run . graph check --repo . --view cards-B201-charter` 失败，原始输出：`Error: 读取视图 codegraph/diffs/cards-B201-charter.json: open codegraph/diffs/cards-B201-charter.json: no such file or directory`、`exit status 1`；本计划登记 B201 无视图的图覆盖债。
- 2026-08-23：源码核对确认 `ledger.NodeDef` 当前位于 `internal/ledger/types.go:178-210` 且无 `Produces`；节点写入先由 `WorkflowDef.withStatesFromNodes` 投影、再在 `Store.PutWorkflow` JSON 编码（`internal/ledger/workflows.go:16-39,133-155`）；`Store.AttachFile` 同 path 在 `internal/ledger/cards.go:444-466` 幂等；gate 缺附件在 `internal/ledger/move.go:90-93` 仅按 kind 判定。
- 2026-08-23：源码核对确认 `NodeStep.RunOnce` 在 `internal/ledgerstep/node.go:150-197` 取得 `target/taskID`、等待并记录裁决、清等人标记后直接 `routeTo`；`NodeStep.Await` 当前签名为 `func(context.Context, string, string) (string, error)`（`node.go:45-46`）；`StepRunner` 的装配与客户端池位于 `internal/ledgerstep/runner.go:18-175`。
- 2026-08-23：源码核对确认 `buildPrompt` 当前签名为 `func(string, ledger.Card, string, bool, bool, string) string`（`internal/ledgerstep/dispatch.go:270-316`），`TemplateDispatch` 位于同文件 `:48-75`；协调者侧可通过 `(*client.Client).Diff(ctx, taskID, base) (string, error)` 取已有 diff 通道（`internal/client/client.go:1082-1106`）。
- 2026-08-23：源码核对确认工作流 HTTP 读取把 `ledger.NodeDef` 投影为 `proto.NodeDef`（`internal/agentd/ledgerapi.go:111-126`），HTTP 写入直接解码 `[]ledger.NodeDef`（`:533-546`）；Go wire `proto.NodeDef` 当前在 `internal/proto/ledger.go:63-76`，TS mirror 在 `web/src/api/ledger.ts:109-125`，故新增 `produces` 必须穿过两处投影与 fixture。
- 2026-08-23：基线命令 `jq empty deploy/workflows/charter-v4.json` 真实退出 0、无输出；当前 charter 配置的 `contract`/`breakdown`/`plan` 节点均无 `produces`，且 `plan` 的 gate 仍为 `require_attachment: spec`（文件 `deploy/workflows/charter-v4.json:16-64`）。
- 2026-08-23：源码核对确认 Web/agentd 附件 kind 白名单为 `spec|plan|doc|contract`（`internal/agentd/ledgerapi.go:639-643,790-795`）；本计划将 breakdown 产出使用既有 `doc` kind，并把 charter plan gate 对齐为 `doc`，不新增 kind 枚举。
- 2026-08-23：基线命令 `go test ./internal/agentd -run 'Test(FlowGetReturnsNodes|FlowPutCreatesNewVersion|AttachmentKindsCoverDefaultWorkflowGates)' -count=1` 真实通过，原始输出：`ok github.com/Xsxdot/handoff/internal/agentd 0.471s`。
- 2026-08-23：尝试用 apply_patch 生成实现计划时，第一次因 patch 行缺少 '+' 失败；第二次用 JS String.raw 自动加前缀时因内容中的 Go 反引号 struct tag 触发 `SyntaxError: Unexpected identifier 'json'` 失败；计划文件尚未创建。
- 2026-08-23：第三次生成计划 patch 时，内容已避开反引号，但 JS 将 const content 重新赋值，触发 `TypeError: Assignment to constant variable.`；计划文件仍未创建。
- 2026-08-23：用 sentinel 替换 Markdown/Go 反引号并通过 apply_patch 创建 `docs/superpowers/plans/2026-08-23-b201-node-output-attach.md` 的 Task 1、Task 2；命令返回 `{}`，文件已进入工作树。
- 2026-08-23：追加 Task 3（NodeStep 输出校验/挂载/路由）到实现计划；apply_patch 返回 `{}`。
- 2026-08-23：追加 Task 4（prompt 路径注入、runner 渲染、Client.Diff 接线）到实现计划；apply_patch 返回 `{}`。
- 2026-08-23：首次追加 Task 5 时，JS String.raw 内容仍含 Go struct tag 的实际反引号，触发 `SyntaxError: Unexpected identifier 'json'`；计划文件未追加 Task 5。
- 2026-08-23：用 sentinel 替换 Task 5 中所有 struct tag/Markdown 反引号并追加 Go/TS wire、HTTP/fixture、NodeEditor、charter 配置和部署说明任务；apply_patch 返回 `{}`。
- 2026-08-23：追加执行顺序、红绿提交动作、spec 用户故事归属、跨 task 签名/序列化审计、缺陷族审查、上下文预算和计划自审；计划主体已完整落盘。
- 2026-08-23：静态检查 `wc -l -c` 得计划 1599 行/68838 字节、台账 30 行/6752 字节；`git diff --check` 退出 0 无输出；`jq empty deploy/workflows/charter-v4.json` 退出 0 无输出；`git status --short --branch` 输出 `## cards/B201-charter` 加两个未跟踪文档。
- 2026-08-23：占位符扫描命中计划自审段第 1595 行中对 `TBD|TODO|同 Task N|适当错误处理` 的自我声明，未命中实现段；该自审声明是格式铁律要求的例外说明。
- 2026-08-23：结构检查命中五个 Task，且每个 Task 都有文件范围、Interfaces、基线/失败测试/实现/验证/缺陷族段；`test -f` 确认 spec、plan、ledger 三个文档均存在；计划尾部审计确认五条用户故事均已归属。
- 2026-08-23：首次执行 `git add docs/superpowers/plans/2026-08-23-b201-node-output-attach.md docs/superpowers/ledgers/2026-08-23-b201-ledger.md` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/a382a198/index.lock': Read-only file system`；尚未提交。
- 2026-08-23：在 require_escalated 下重新 `git add` 成功；`git diff --cached --check` 退出 0 无输出；`git status --short --branch` 显示仅暂存 ledger 与 plan 两个新增文件；cached stat 为 2 files changed, 1633 insertions。
- 2026-08-23：`git commit -m "docs(plan): add B201 output attachment implementation plan"` 真实成功，原始输出为 `[cards/B201-charter cb1b6ab1] docs(plan): add B201 output attachment implementation plan`、`2 files changed, 1634 insertions(+)`；下一步只把本条提交记录写回台账并收口，不 push。
- 2026-08-23：首个提交后对台账追加记录，`git diff --check` 真实退出 0、无输出。
- 2026-08-23：本次接管确认当前工作树为 `/root/.handoff/worktrees/73bce92e`，分支为 `cards/B201-charter-2`；`git status --short --branch` 真实输出仅为 `## cards/B201-charter-2`。
- 2026-08-23：完整读取 `/root/.codex/skills/handoff/SKILL.md`；本回合遵守执行者不得调用 handoff CLI、不得启动 executor 的边界。
- 2026-08-23：读取实现计划全文确认本节点交付边界是提交实现计划与台账，不实现 internal/ledger、internal/ledgerstep、agentd 或 Web 代码；计划末尾明确本节点红线为只生成并提交文档。
- 2026-08-23：`wc -l -c` 真实输出计划为 `1605 70152`、spec 为 `217 14038`、台账为 `37 8502`；`git ls-tree` 确认 HEAD 已跟踪 spec、plan、ledger 三个法定文档。
- 2026-08-23：`git show --no-patch` 真实确认当前 HEAD 为 `0acf48473cd146730b29b91f50e1e334c74b080b`，提交主题为 `plan(b201): 协调者更正——四道 gate 一律不动`，父提交为 `b99d381e9e32aac5193a18d0e383e0c54805c8c3`。
- 2026-08-23：`git diff --check` 真实退出 0、无输出；本次台账追加没有空白错误。
- 2026-08-23：计划结构扫描 `rg -n '^## Task [1-5]：|^### (Interfaces|1\\. 基线判据先跑|2\\. 写失败测试|3\\. 最小实现|4\\. (注释与验证|注释、验证与提交前范围)|5\\. 缺陷族验收)|本节点红线|五条用户故事均已指到|四道 gate 一律不动' docs/superpowers/plans/2026-08-23-b201-node-output-attach.md` 真实命中 Task 1 至 Task 5 及各自 Interfaces、基线、失败测试、实现、验证、缺陷族段，并命中第 1471 行 gate 冻结、第 1600 行用户故事覆盖和第 1605 行节点红线。
- 2026-08-23：`jq empty deploy/workflows/charter-v4.json` 真实退出 0、无输出；仓内 charter 配置 JSON 可解析。
- 2026-08-23：计划自审占位符扫描 `rg -n 'TBD|TODO|适当错误处理|同 Task N' docs/superpowers/plans/2026-08-23-b201-node-output-attach.md` 真实仅命中第 1601 行自审文字，未命中实现段；该命中是计划对自身扫描规则的说明，不是待实现占位符。
- 2026-08-23：验收前 `git status --short --branch` 真实输出 `## cards/B201-charter-2` 与 ` M docs/superpowers/ledgers/2026-08-23-b201-ledger.md`；本回合尚未修改计划或生产代码。
- 2026-08-23：首次执行 `git add docs/superpowers/ledgers/2026-08-23-b201-ledger.md` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/73bce92e/index.lock': Read-only file system`；按纪律保留原文，改用同一命令申请受控权限重试。
- 2026-08-23：在受控权限下重试 `git add docs/superpowers/ledgers/2026-08-23-b201-ledger.md` 真实成功、无输出。
- 2026-08-23：暂存验收 `git diff --cached --check` 真实退出 0、无输出；`git diff --cached --stat` 原始输出为 `docs/superpowers/ledgers/2026-08-23-b201-ledger.md | 12 ++++++++++++`、`1 file changed, 12 insertions(+)`；`git status --short --branch` 原始输出为 `## cards/B201-charter-2` 与 `M  docs/superpowers/ledgers/2026-08-23-b201-ledger.md`。
- 2026-08-23：`git commit -m "chore(ledger): record B201 document validation"` 真实成功，原始输出为 `[cards/B201-charter-2 b7874b4f] chore(ledger): record B201 document validation`、`1 file changed, 13 insertions(+)`；未执行 push。
- 2026-08-23：最终收口前复核真实结果：`git status --short --branch` 输出 `## cards/B201-charter-2`；`git log -3 --oneline --decorate` 输出 `a0c7d00f (HEAD -> cards/B201-charter-2) chore(ledger): record B201 validation commit`、`b7874b4f chore(ledger): record B201 document validation`、`0acf4847 ... plan(b201): 协调者更正——四道 gate 一律不动`；`git show --stat --oneline HEAD` 输出 `a0c7d00f chore(ledger): record B201 validation commit`、`1 file changed, 1 insertion(+)`；`git diff --check` 退出 0、无输出；文档历史扫描真实列出 plan 提交 `0acf4847`、`cb1b6ab1` 与 spec 提交 `c0386832`、`765e1a60`。
- 2026-08-23：本次实现接管确认工作树为 `/root/.handoff/worktrees/0d8817d4`，当前分支为 `cards/B201-charter-3`；未切换分支、未修改 git 配置。
- 2026-08-23：基线命令 `go test ./internal/ledger -run 'TestWorkflow(LegacyDefStillDecodes|NodesProjectToStates|NodeCarriesPurposeAndAcceptanceSwitch|PutWorkflowAcceptsGoodNodes)' -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledger	0.343s`。
- 2026-08-23：基线命令 `go test ./internal/ledgerstep -run 'Test(ReviewStepPassAndFailLoop|ReviewStepRoundCapAndParseFailure|ReviewStepMarksNeedsHumanWhenReviewFails|NodeStepDispatchOnlyReturnsDispatched|NodeStepVerdictRoutesOnPass)' -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.860s`。
- 2026-08-23：基线命令 `go test ./internal/ledgerstep -run 'Test(BuildPromptThreeSections|RunnerPassesNodePurposeAndAcceptanceSwitch|RunnerFindsNodeInPinnedWorkflowVersion)' -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.315s`。
- 2026-08-23：基线命令 `go test ./internal/proto -run TestContractFixtures -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/proto	0.020s`。
- 2026-08-23：基线命令 `go test ./internal/agentd -run 'Test(FlowGetReturnsNodes|FlowPutCreatesNewVersion|AttachmentKindsCoverDefaultWorkflowGates)' -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/agentd	0.712s`。
- 2026-08-23：基线命令 `jq empty deploy/workflows/charter-v4.json` 真实退出 0、无输出。
- 2026-08-23：Task 1 新增 round-trip、非法 produces、legacy 兼容测试后，`go test ./internal/ledger -run 'TestWorkflow(NodeProducesRoundTripAndPresence|RejectsIncompleteProduces|LegacyWorkflowNodeHasNoProduces)' -count=1` 真实失败；原始输出含 `undefined: NodeOutput`、`unknown field Produces in struct literal of type NodeDef`、`got.Def.Nodes[0].Produces undefined`，最终为 `FAIL	github.com/Xsxdot/handoff/internal/ledger [build failed]`；失败原因为功能符号尚未实现，非测试 typo。
- 2026-08-23：Task 1 实现 `ledger.NodeOutput`、`NodeDef.Produces`、produces 校验和成功日志计数后，目标测试命令真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledger	0.561s`。
- 2026-08-23：Task 1 收尾命令 `go test ./internal/ledger -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledger	10.622s`；`git diff --check` 真实退出 0、无输出。
- 2026-08-23：Task 2 基线命令 `go test ./internal/ledgerstep -run 'Test(ReviewStepPassAndFailLoop|NodeStepDispatchOnlyReturnsDispatched|NodeStepVerdictRoutesOnPass)' -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.319s`。
- 2026-08-23：Task 2 新增纯函数测试后，`go test ./internal/ledgerstep -run 'Test(RenderOutputPath|ChangedPaths|ChangedPathsText)' -count=1` 真实失败；原始输出为 `undefined: RenderOutputPath`、`undefined: ChangedPaths`、`undefined: changedPathsText`，最终为 `FAIL	github.com/Xsxdot/handoff/internal/ledgerstep [build failed]`；失败原因为生产函数尚未实现，非测试 typo。
- 2026-08-23：Task 2 实现 `RenderOutputPath`、`ChangedPaths`、`changedPathsText` 后，目标测试命令真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.339s`。
- 2026-08-23：Task 2 收尾命令 `go test ./internal/ledgerstep -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	3.502s`；`git diff --check` 真实退出 0、无输出。
- 2026-08-23：Task 3 基线命令 `go test ./internal/ledgerstep -run 'Test(ReviewStepPassAndFailLoop|ReviewStepRoundCapAndParseFailure|ReviewStepMarksNeedsHumanWhenReviewFails|NodeStepDispatchOnlyReturnsDispatched|NodeStepVerdictRoutesOnPass)' -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.722s`。
- 2026-08-23：Task 3 新增五个 NodeStep 产出行为测试后，目标命令真实失败；原始输出为多个 `unknown field OutputPath in struct literal of type NodeStep`、`unknown field Diff in struct literal of type NodeStep`、`unknown field Attach in struct literal of type NodeStep`，最终为 `FAIL	github.com/Xsxdot/handoff/internal/ledgerstep [build failed]`；失败原因为三个输出 hook 尚未实现，非测试 typo。
- 2026-08-23：Task 3 首次实现后执行 `gofmt -w internal/ledgerstep/node.go internal/ledgerstep/node_test.go` 失败，原始输出：`internal/ledgerstep/node.go:50:12: missing parameter name`；已将 Diff 函数签名首参数补为命名参数，未将该格式化失败归因于测试。
- 2026-08-23：Task 3 实现输出 hook、pass 后 diff 校验、Attach-before-route、缺产物 comment、Attach Warn-and-route 与 legacy hook 绕过后，目标测试命令真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	1.237s`。
- 2026-08-23：Task 3 收尾命令 `go test ./internal/ledgerstep -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	3.929s`；`git diff --check` 真实退出 0、无输出。
- 2026-08-23：Task 4 基线命令 `go test ./internal/ledgerstep -run 'Test(BuildPromptThreeSections|RunnerPassesNodePurposeAndAcceptanceSwitch|RunnerFindsNodeInPinnedWorkflowVersion)' -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.252s`。
- 2026-08-23：Task 4 新增 prompt 与 runner 路径测试后，目标命令真实失败；原始输出为既有 `buildPrompt` 调用 `too many arguments`、`unknown field Now in struct literal of type StepRunner`、`too many arguments in call to runner.dispatchNode`、`opts.OutputPath undefined`，最终为 `FAIL	github.com/Xsxdot/handoff/internal/ledgerstep [build failed]`；失败原因为新接口尚未实现，非测试 typo。
- 2026-08-23：Task 4 实现 TemplateDispatch/DispatchOpts.OutputPath、七参数 buildPrompt、固定时钟路径渲染、NodeStep 输出 hook 装配和 Client.Diff 投影后，目标测试命令真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	0.498s`。
- 2026-08-23：Task 4 收尾命令 `go test ./internal/ledgerstep -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	3.575s`；`git diff --check` 真实退出 0、无输出。
- 2026-08-23：Task 5 基线命令 `go test ./internal/proto -run TestContractFixtures -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/proto	0.003s`。
- 2026-08-23：Task 5 基线命令 `go test ./internal/agentd -run 'Test(FlowGetReturnsNodes|FlowPutCreatesNewVersion|AttachmentKindsCoverDefaultWorkflowGates)' -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/agentd	0.331s`。
- 2026-08-23：Task 5 基线命令 `jq empty deploy/workflows/charter-v4.json` 真实退出 0、无输出。
- 2026-08-23：Task 5 首次运行 HTTP 失败测试 `go test ./internal/agentd -run TestFlowNodeProducesRoundTripsThroughHTTPWire -count=1` 未执行断言而被测试环境跳过；`-v` 原始输出为 `PTY 测试根目录不可用... mkdir /tmp/handoff-pty-1795588799: read-only file system... 实测 socket=113 字节，上限=100`，最终 `--- SKIP: TestFlowNodeProducesRoundTripsThroughHTTPWire`、`PASS`。该结果不是功能通过，HTTP 断言仍未验证。
- 2026-08-23：为获得可执行的 wire 红测，新增不依赖 PTY 的 `TestLedgerNodeWirePreservesProduces`；`go test ./internal/agentd -run TestLedgerNodeWirePreservesProduces -count=1` 真实失败，原始输出：`wire 丢失 produces: {"name":"breakdown","override":{},"gate":{}}`，最终 `FAIL github.com/Xsxdot/handoff/internal/agentd`；失败原因为 agentd projection 尚未传递 Produces。
- 2026-08-23：加入 proto.NodeOutput/NodeDef.Produces 与 agentd projection 后，`go test ./internal/agentd -run TestLedgerNodeWirePreservesProduces -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/agentd	0.003s`。
- 2026-08-23：按 fixture harness 更新 NodeDef 样本但未刷新 JSON 时，`go test ./internal/proto -run TestContractFixtures -count=1` 真实失败；原始输出明确显示实际 JSON 新增 `"produces":{"kind":"doc","path":"docs/superpowers/plans/b201-plan.md"}` 而已存 fixture 无该字段，最终 `FAIL github.com/Xsxdot/handoff/internal/proto`；这是预期的契约漂移红测。
- 2026-08-23：按计划使用显式更新命令 `go test ./internal/proto -run TestContractFixtures -update` 刷新 NodeDef fixture，真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/proto	0.007s`。
- 2026-08-23：Task 5 实现后复跑 Web `npm run typecheck`（目录 `web`）失败，原始输出：`> tsc -b`、`sh: 1: tsc: not found`，退出码 127；Web 类型检查未验证。
- 2026-08-23：Task 5 Web 指定测试命令 `npx vitest run src/api/contract.test.ts src/app/flows/NodeEditor.test.tsx src/app/flows/NodeEditor.purpose.test.tsx`（目录 `web`）失败，原始输出含 `npm error code EROFS`、`npm error syscall open`、`npm error path /root/.npm/_cacache/tmp/***`、`npm error rofs Invalid response body while trying to fetch https://registry.npmjs.org/vitest: EROFS: read-only file system`；Web 测试未验证。
- 2026-08-23：Task 5 Go fixture 复核 `go test ./internal/proto -run TestContractFixtures -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/proto	0.003s`。
- 2026-08-23：Task 5 Go agentd 目标命令真实退出 0；`-v` 原始输出显示 `TestLedgerNodeWirePreservesProduces` 为 `PASS`，但 `TestFlowGetReturnsNodes`、`TestFlowPutCreatesNewVersion`、`TestFlowNodeProducesRoundTripsThroughHTTPWire`、`TestAttachmentKindsCoverDefaultWorkflowGates` 均因 `PTY 测试根目录不可用` 而 `SKIP`；HTTP 端到端断言未验证。原始环境错误含 `mkdir /tmp/handoff-pty-4122864813: read-only file system` 与 repo-dot socket 超过上限。
- 2026-08-23：`jq empty deploy/workflows/charter-v4.json` 在 Task 5 配置修改后真实退出 0、无输出。
- 2026-08-23：Task 5/跨 task 包测试真实通过：`go test ./internal/ledger -count=1` 输出 `ok  	github.com/Xsxdot/handoff/internal/ledger	11.704s`；`go test ./internal/ledgerstep -count=1` 输出 `ok  	github.com/Xsxdot/handoff/internal/ledgerstep	4.561s`；`go test ./internal/proto -count=1` 输出 `ok  	github.com/Xsxdot/handoff/internal/proto	0.004s`；`go test ./internal/agentd -count=1` 运行约 111 秒后输出 `ok  	github.com/Xsxdot/handoff/internal/agentd	111.319s`。
- 2026-08-23：补齐 proto/agentd 的既有 `omit_acceptance` wire 字段并修复 diff header 空白边界后，`go test ./internal/proto -run TestContractFixtures -count=1` 输出 `ok  	github.com/Xsxdot/handoff/internal/proto	0.005s`，`go test ./internal/agentd -run TestLedgerNodeWirePreservesProduces -count=1` 输出 `ok  	github.com/Xsxdot/handoff/internal/agentd	0.006s`，`jq empty deploy/workflows/charter-v4.json` 退出 0、无输出。
- 2026-08-23：全仓编译命令 `go build ./...` 真实退出 0、无输出；触及 Go 文件的 `gofmt -l ...` 真实退出 0、无输出；`git diff --check` 真实退出 0、无输出。
- 2026-08-23：最终集成命令 `go test ./...` 真实失败；B201 触及包仍输出 `ok`（agentd 119.477s、ledger 14.433s、ledgerstep 5.358s、proto 0.004s），失败包为 `github.com/Xsxdot/handoff/internal/executor/claudecode`。原始失败包含 `TestPermServerAskThenRespond`、`TestPermServerRespondUnknownID`、`TestPermServerReRegisterSameID`：`裁决 socket 路径过长（113/116 字节，上限 107）: /root/.handoff/tasks/0d8817d4-e502-439d-8908-0772516c832b/tmp/.../perm.sock——把 DataDir 配到更浅的位置`；`TestResumeContinuesFromOffset`：`Resume 应判活并续读: alive=false err=裁决 socket 路径过长（115 字节，上限 107）...`，最终 `FAIL github.com/Xsxdot/handoff/internal/executor/claudecode`、`FAIL`。未替该失败归因。
- 2026-08-23：为复跑全量测试执行 `TMPDIR=/tmp go test ./...`，真实失败；原始输出含 `github.com/Xsxdot/handoff/cmd` 为 `FAIL`，agentd 多个测试为 `mkdir /tmp/agentd-test-...: read-only file system`，claudecode 为 `裁决 socket 路径过长（114/115/116 字节，上限 107）`、`mkdir /tmp/hc-...: read-only file system`，最终 `FAIL github.com/Xsxdot/handoff/internal/agentd`、`FAIL github.com/Xsxdot/handoff/internal/executor/claudecode`、`FAIL`。该复跑未证明全量通过。
- 2026-08-23：最终代码状态下，`go test ./internal/ledger ./internal/ledgerstep ./internal/proto -count=1` 真实通过，原始输出分别为 `ok  	github.com/Xsxdot/handoff/internal/ledger	11.977s`、`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	4.627s`、`ok  	github.com/Xsxdot/handoff/internal/proto	0.003s`；`go test ./internal/agentd -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/agentd	116.299s`。
- 2026-08-23：最终代码状态下 `go build ./...` 真实退出 0、无输出；触及 Go 文件 `gofmt -l ...` 真实退出 0、无输出；`git diff --check` 真实退出 0、无输出；`jq empty deploy/workflows/charter-v4.json` 真实退出 0、无输出。
- 2026-08-23：最终静态门 `go vet ./internal/ledger ./internal/ledgerstep ./internal/proto ./internal/agentd` 真实退出 0、无输出；charter 四节点 gate/produces 查询真实输出 contract=spec+contract path、breakdown=contract+doc path、plan=spec+plan path、implement=plan 且 produces=null；最终 `gofmt -l ...` 与 `git diff --check` 仍真实退出 0、无输出。
- 2026-08-23：补充 StepRunner 缺失等待客户端的显式错误分支后，`go test ./internal/ledgerstep -count=1` 真实通过，原始输出：`ok  	github.com/Xsxdot/handoff/internal/ledgerstep	3.723s`。
- 2026-08-23：首次执行本卡完整 `git add` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/0d8817d4/index.lock': Read-only file system`；尚未暂存或提交。
- 2026-08-23：在受控权限下重试同一完整 `git add` 真实成功、无输出；`git diff --cached --check` 真实退出 0、无输出；暂存统计为 `23 files changed, 1008 insertions(+), 22 deletions(-)`；当前分支仍为 `cards/B201-charter-3`。
- 2026-08-23：`git commit -m "feat(charter): declare document node outputs"` 真实成功，原始输出：`[cards/B201-charter-3 e8f94b76] feat(charter): declare document node outputs`、`23 files changed, 1009 insertions(+), 22 deletions(-)`；包含两个新文件 `internal/ledgerstep/output.go` 与 `internal/ledgerstep/output_test.go`；未 push。
