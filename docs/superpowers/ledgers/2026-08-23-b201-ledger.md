# B201 执行台账

- 2026-08-23：当前工作树为 `/root/.handoff/worktrees/a382a198`，分支为 `cards/B201-charter`，基线提交为 `80bb199ab471a9228652b3780e30fd4a274f838a`；`git status --short --branch` 仅输出 `## cards/B201-charter`。
- 2026-08-23：`git diff --check` 退出 0、无输出；基线 Go 为 `go1.26.1 linux/amd64`；`go env GOMOD` 为 `/root/.handoff/worktrees/a382a198/go.mod`。
- 2026-08-23：已完整读取 `docs/superpowers/specs/2026-08-23-b201-node-output-attach.md`；本节点产物是实现计划，不实现代码。
- 2026-08-23：`codegraph` 未安装，命令输出为 `codegraph: command not found`；本计划的现状签名改用 `rg`/源码行号核对，并登记为图覆盖债。
- 2026-08-23：基线命令 `go test ./internal/ledger -run 'TestWorkflow(LegacyDefStillDecodes|NodesProjectToStates|NodeCarriesPurposeAndAcceptanceSwitch|PutWorkflowAcceptsGoodNodes)' -count=1` 真实通过，原始输出：`ok github.com/Xsxdot/handoff/internal/ledger 0.384s`。
- 2026-08-23：基线命令 `go test ./internal/ledgerstep -count=1` 真实通过，原始输出：`ok github.com/Xsxdot/handoff/internal/ledgerstep 3.844s`。
- 2026-08-23：基线命令 `go test ./internal/proto -run TestContractFixtures -count=1` 真实通过，原始输出：`ok github.com/Xsxdot/handoff/internal/proto 0.003s`。
- 2026-08-23：基线命令 `npm run typecheck`（目录 `web`）失败，原始输出：`> web@0.0.0 typecheck`、`> tsc -b`、`sh: 1: tsc: not found`；未将其写成代码结论，web 判据待依赖可用后复跑。
- 2026-08-23：基线命令 `npx vitest run src/api/contract.test.ts src/app/flows/NodeEditor.test.tsx src/app/flows/NodeEditor.purpose.test.tsx`（目录 `web`）失败，原始输出含 `npm error code EROFS`、`npm error syscall open`、`npm error path /root/.npm/_cacache/tmp/***`、`npm error rofs Invalid response body while trying to fetch https://registry.npmjs.org/vitest: EROFS: read-only file system`；未将其写成代码结论，web 判据待依赖可用后复跑。
- 2026-08-23：基线命令 `go build ./...` 真实退出 0，无输出。
- 2026-08-23：基线命令 `go vet ./internal/ledger ./internal/ledgerstep ./internal/proto ./internal/client` 真实退出 0，无输出。
- 2026-08-23：基线命令 `gofmt -l internal/ledger/types.go internal/ledger/workflows.go internal/ledgerstep/node.go internal/ledgerstep/runner.go internal/ledgerstep/dispatch.go internal/proto/ledger.go internal/agentd/ledgerapi.go` 真实退出 0，无输出。
- 2026-08-23：基线命令 `go run . graph check --repo . --view cards-B201-charter` 失败，原始输出：`Error: 读取视图 codegraph/diffs/cards-B201-charter.json: open codegraph/diffs/cards-B201-charter.json: no such file or directory`、`exit status 1`；本计划登记 B201 无视图的图覆盖债。
- 2026-08-23：源码核对确认 `ledger.NodeDef` 当前位于 `internal/ledger/types.go:178-210` 且无 `Produces`；节点写入先由 `WorkflowDef.withStatesFromNodes` 投影、再在 `Store.PutWorkflow` JSON 编码（`internal/ledger/workflows.go:16-39,133-155`）；`Store.AttachFile` 同 path 在 `internal/ledger/cards.go:444-466` 幂等；gate 缺附件在 `internal/ledger/move.go:90-93` 仅按 kind 判定。
- 2026-08-23：源码核对确认 `NodeStep.RunOnce` 在 `internal/ledgerstep/node.go:150-197` 取得 `target/taskID`、等待并记录裁决、清等人标记后直接 `routeTo`；`NodeStep.Await` 当前签名为 `func(context.Context, string, string) (string, error)`（`node.go:45-46`）；`StepRunner` 的装配与客户端池位于 `internal/ledgerstep/runner.go:18-175`。
- 2026-08-23：源码核对确认 `buildPrompt` 当前签名为 `func(string, ledger.Card, string, bool, bool, string) string`（`internal/ledgerstep/dispatch.go:270-316`），`TemplateDispatch` 位于同文件 `:48-75`；协调者侧可通过 `(*client.Client).Diff(ctx, taskID, base) (string, error)` 取已有 diff 通道（`internal/client/client.go:1082-1106`）。
- 2026-08-23：源码核对确认工作流 HTTP 读取把 `ledger.NodeDef` 投影为 `proto.NodeDef`（`internal/agentd/ledgerapi.go:111-126`），HTTP 写入直接解码 `[]ledger.NodeDef`（`:533-546`）；Go wire `proto.NodeDef` 当前在 `internal/proto/ledger.go:63-76`，TS mirror 在 `web/src/api/ledger.ts:109-125`，故新增 `produces` 必须穿过两处投影与 fixture。
- 2026-08-23：基线命令 `jq empty deploy/workflows/charter-v4.json` 真实退出 0、无输出；当前 charter 配置的 `contract`/`breakdown`/`plan` 节点均无 `produces`，且 `plan` 的 gate 仍为 `require_attachment: spec`（文件 `deploy/workflows/charter-v4.json:16-64`）。
- 2026-08-23：源码核对确认 Web/agentd 附件 kind 白名单为 `spec|plan|doc|contract`（`internal/agentd/ledgerapi.go:639-643,790-795`）；本计划将 breakdown 产出使用既有 `doc` kind，并把 charter plan gate 对齐为 `doc`，不新增 kind 枚举。
- 2026-08-23：基线命令 `go test ./internal/agentd -run 'Test(FlowGetReturnsNodes|FlowPutCreatesNewVersion|AttachmentKindsCoverDefaultWorkflowGates)' -count=1` 真实通过，原始输出：`ok github.com/Xsxdot/handoff/internal/agentd 0.471s`。
- 2026-08-23：尝试用 apply_patch 生成实现计划时，第一次因 patch 行缺少 '+' 失败；第二次用 JS String.raw 自动加前缀时因内容中的 Go 反引号 struct tag 触发 `SyntaxError: Unexpected identifier 'json'` 失败；计划文件尚未创建。
- 2026-08-23：第三次生成计划 patch 时，内容已避开反引号，但 JS 将 const content 重新赋值，触发 `TypeError: Assignment to constant variable.`；计划文件仍未创建。
- 2026-08-23：用 sentinel 替换 Markdown/Go 反引号并通过 apply_patch 创建 `docs/superpowers/plans/2026-08-23-b201-node-output-attach.md` 的 Task 1、Task 2；命令返回 `{}`，文件已进入工作树。
- 2026-08-23：追加 Task 3（NodeStep 输出校验/挂载/路由）到实现计划；apply_patch 返回 `{}`。
- 2026-08-23：追加 Task 4（prompt 路径注入、runner 渲染、Client.Diff 接线）到实现计划；apply_patch 返回 `{}`。
- 2026-08-23：首次追加 Task 5 时，JS String.raw 内容仍含 Go struct tag 的实际反引号，触发 `SyntaxError: Unexpected identifier 'json'`；计划文件未追加 Task 5。
- 2026-08-23：用 sentinel 替换 Task 5 中所有 struct tag/Markdown 反引号并追加 Go/TS wire、HTTP/fixture、NodeEditor、charter 配置和部署说明任务；apply_patch 返回 `{}`。
- 2026-08-23：追加执行顺序、红绿提交动作、spec 用户故事归属、跨 task 签名/序列化审计、缺陷族审查、上下文预算和计划自审；计划主体已完整落盘。
- 2026-08-23：静态检查 `wc -l -c` 得计划 1599 行/68838 字节、台账 30 行/6752 字节；`git diff --check` 退出 0 无输出；`jq empty deploy/workflows/charter-v4.json` 退出 0 无输出；`git status --short --branch` 输出 `## cards/B201-charter` 加两个未跟踪文档。
- 2026-08-23：占位符扫描命中计划自审段第 1595 行中对 `TBD|TODO|同 Task N|适当错误处理` 的自我声明，未命中实现段；该自审声明是格式铁律要求的例外说明。
- 2026-08-23：结构检查命中五个 Task，且每个 Task 都有文件范围、Interfaces、基线/失败测试/实现/验证/缺陷族段；`test -f` 确认 spec、plan、ledger 三个文档均存在；计划尾部审计确认五条用户故事均已归属。
- 2026-08-23：首次执行 `git add docs/superpowers/plans/2026-08-23-b201-node-output-attach.md docs/superpowers/ledgers/2026-08-23-b201-ledger.md` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/a382a198/index.lock': Read-only file system`；尚未提交。
- 2026-08-23：在 require_escalated 下重新 `git add` 成功；`git diff --cached --check` 退出 0 无输出；`git status --short --branch` 显示仅暂存 ledger 与 plan 两个新增文件；cached stat 为 2 files changed, 1633 insertions。
- 2026-08-23：`git commit -m "docs(plan): add B201 output attachment implementation plan"` 真实成功，原始输出为 `[cards/B201-charter cb1b6ab1] docs(plan): add B201 output attachment implementation plan`、`2 files changed, 1634 insertions(+)`；下一步只把本条提交记录写回台账并收口，不 push。
- 2026-08-23：首个提交后对台账追加记录，`git diff --check` 真实退出 0、无输出。
- 2026-08-23：本次接管确认当前工作树为 `/root/.handoff/worktrees/73bce92e`，分支为 `cards/B201-charter-2`；`git status --short --branch` 真实输出仅为 `## cards/B201-charter-2`。
- 2026-08-23：完整读取 `/root/.codex/skills/handoff/SKILL.md`；本回合遵守执行者不得调用 handoff CLI、不得启动 executor 的边界。
- 2026-08-23：读取实现计划全文确认本节点交付边界是提交实现计划与台账，不实现 internal/ledger、internal/ledgerstep、agentd 或 Web 代码；计划末尾明确本节点红线为只生成并提交文档。
- 2026-08-23：`wc -l -c` 真实输出计划为 `1605 70152`、spec 为 `217 14038`、台账为 `37 8502`；`git ls-tree` 确认 HEAD 已跟踪 spec、plan、ledger 三个法定文档。
- 2026-08-23：`git show --no-patch` 真实确认当前 HEAD 为 `0acf48473cd146730b29b91f50e1e334c74b080b`，提交主题为 `plan(b201): 协调者更正——四道 gate 一律不动`，父提交为 `b99d381e9e32aac5193a18d0e383e0c54805c8c3`。
- 2026-08-23：`git diff --check` 真实退出 0、无输出；本次台账追加没有空白错误。
- 2026-08-23：计划结构扫描 `rg -n '^## Task [1-5]：|^### (Interfaces|1\\. 基线判据先跑|2\\. 写失败测试|3\\. 最小实现|4\\. (注释与验证|注释、验证与提交前范围)|5\\. 缺陷族验收)|本节点红线|五条用户故事均已指到|四道 gate 一律不动' docs/superpowers/plans/2026-08-23-b201-node-output-attach.md` 真实命中 Task 1 至 Task 5 及各自 Interfaces、基线、失败测试、实现、验证、缺陷族段，并命中第 1471 行 gate 冻结、第 1600 行用户故事覆盖和第 1605 行节点红线。
- 2026-08-23：`jq empty deploy/workflows/charter-v4.json` 真实退出 0、无输出；仓内 charter 配置 JSON 可解析。
- 2026-08-23：计划自审占位符扫描 `rg -n 'TBD|TODO|适当错误处理|同 Task N' docs/superpowers/plans/2026-08-23-b201-node-output-attach.md` 真实仅命中第 1601 行自审文字，未命中实现段；该命中是计划对自身扫描规则的说明，不是待实现占位符。
- 2026-08-23：验收前 `git status --short --branch` 真实输出 `## cards/B201-charter-2` 与 ` M docs/superpowers/ledgers/2026-08-23-b201-ledger.md`；本回合尚未修改计划或生产代码。
- 2026-08-23：首次执行 `git add docs/superpowers/ledgers/2026-08-23-b201-ledger.md` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/73bce92e/index.lock': Read-only file system`；按纪律保留原文，改用同一命令申请受控权限重试。
- 2026-08-23：在受控权限下重试 `git add docs/superpowers/ledgers/2026-08-23-b201-ledger.md` 真实成功、无输出。
- 2026-08-23：暂存验收 `git diff --cached --check` 真实退出 0、无输出；`git diff --cached --stat` 原始输出为 `docs/superpowers/ledgers/2026-08-23-b201-ledger.md | 12 ++++++++++++`、`1 file changed, 12 insertions(+)`；`git status --short --branch` 原始输出为 `## cards/B201-charter-2` 与 `M  docs/superpowers/ledgers/2026-08-23-b201-ledger.md`。
- 2026-08-23：`git commit -m "chore(ledger): record B201 document validation"` 真实成功，原始输出为 `[cards/B201-charter-2 b7874b4f] chore(ledger): record B201 document validation`、`1 file changed, 13 insertions(+)`；未执行 push。
