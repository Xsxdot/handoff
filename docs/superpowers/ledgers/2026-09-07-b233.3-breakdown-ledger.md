# B233.3 breakdown 台账

日期：2026-09-07。节点：charter breakdown。工作分支：`cards/B233.3-charter-2`。开工 HEAD：`328285ee8d1aebecd504c0238e131e0f3339fb42`。

## 过程

1. 形态 2：handoff executor 出稿，协调者拍板。不写实现、不建卡、不派发。法定产出 `docs/superpowers/specs/b233.3-breakdown.md`。L3 轻档；本节点 prompt：「不要扇出子卡，除非拆解时证明单子系统工作量超过流程固定成本」。传输层不更新卡、不选审批者。ApprovalClient dead-* 与功能分支 HEAD 相同，本卡未加剧，拆解不回头修 233.1 absorb。
2. 上游状态位（读文件，非会话记忆）：spec `docs/superpowers/specs/b233.3.md` 头部「状态：**已批准**（2026-09-06，用户原话「按这个推进吧」）」✓。契约 `docs/superpowers/specs/b233.3-contract.md` 头部「冻结状态：随本提交冻结」——无日期+提交号。本工作树 HEAD=`328285ee` 即 contract 提交（`git log -1 --oneline`）。拆解期拟按 b298/B233.1 惯例把状态行补钉为「已冻结（2026-09-07，提交 328285ee）」，冻结正文不改。
3. 查图：`codegraph --repo . domains` 默认基线视图出现 `d_coordination`，与 best 顶层域不一致——子系统清单以 `codegraph/best.json` 顶层（`parent` 空 + `type`）为准。`codegraph --repo . views` 含 `cards-B233.3-charter`。`sym ExecutionClient` / `WaitDeliveryPolicy` 在该视图 `anchor=ok`，域 `d_transport_channel`。`sym forwardWorktreeIfRequested` 基线域字段为 `d_coordination_api`；best 容器 `k_agentd_Server` → `d_gateway`。`who-calls n_client_Client_HTTPClient` 空边，`unscannedEntries=5`，生产调用方以 grep 为准。
4. `codegraph --repo . check --view cards-B233.3-charter`：exit 1，fails 2 条，均为 `d_execution→d_orchestration` 的 ApprovalClient `dead-contract` / `dead-interface`。与契约 §10 及本节点 prompt「未加剧、不回头修 233.1 absorb」一致。本卡未新开该方向。
5. 架构法第三条实读（非测试源，本轮 `find`+`wc`）：`internal/agentd` 89 文件 / 28308 行，无子包，最大前缀家族 `preview*`=10；命中「单包 ≥40 且无子包」与项目红线 20000 行。`internal/client` 14 / 3517；`internal/targetclient` 3 / 442；`internal/relay` 6 / 1165。本卡能圈出有界文件集，不插竖切卡。
6. 代码事实（本轮 grep/读源，非行为验收）：
   - 生产 `.HTTPClient()`：仅 `internal/agentd/forward.go`、`internal/agentd/forward_ws.go`（测试文件另有 testhttp.ConfigureClient）。Ticket 0 `TestProductionHTTPClientCallersAreGatewayOnly` 已锁。
   - `forwardWorktreeIfRequested` 成功路径仍调 `attachCardBaseBranches`（`forward.go` 约 224–225 行）。`handleProjectWorktreeCreate` 本机路径同样挂卡。既有 `TestForwardWorktreeStripsCardIDsAndAttachesLocally` 正向锁「转发后本机挂卡」。
   - `isDeliverable` 已是 `WaitDeliveryPolicy` 别名；调用点仍在 `waitOnce` / `FollowEvents` / `backlog.go`。`TestWaitEventSkipsProgress` 锁 WaitEvent 跳过 progress 且 cursor 落到 question seq。FollowEvents 内存 `fromSeq` 在过滤前推进（`client.go` 约 1740 行），与 WaitEvent 磁盘 cursor 不同；冻结 35 写的是 WaitEvent cursor。
   - `Client.do` 单次 `c.hc.Do(req)`；失败非 ctx 取消则包装 `ErrUnreachable`。`ErrTunnelDisconnected` 仅空壳+互斥测试，生产未包装。
   - 生产 `client.NewRelay` 旁路：仅 `cmd/upgrade.go` 的 `newAgentdClient`（`ep.RelayURL != ""` 时自建 Dialer）。CLI 具名 target 走 `cmd/root.go#newTargetClientNamed` → `targetclient.New`。`desktop` 的 `client.New(ep.Addr, ep.Token)` 是本机 agentd Endpoint（freeze 2）。`card_node` / `card_driver` / `project` 的 `client.New` 走 `LocalEndpoint`（freeze 2）。契约 §8「部分 card 命令、desktop」作为具名旁路清单过宽，拆解期回写澄清。
   - Web `NewWorktreeDialog` 向 `POST /api/projects/{name}/worktrees?machine=` 发送 `card_ids`，依赖响应 `CardResults`（`web/src/api/types.ts` 注释）。CLI 无对等建树+card_ids 入口。
   - `Dispatch` 手搭 `map[string]any` 发 `POST /api/tasks`；生产零 `Idempotency-Key` / `X-Idempotency-Key`（Ticket 0 已锁）。无服务端派发幂等去重实现（本轮 grep 仅测试文件）。
   - `Pool.NewLocal` 不接收 `*Pool`，按构造不入 `entries`；无专门测试。Warm 测试锁「只碰 relay / 分机退避 / ctx 取消」，不锁「Warm 成功 ≠ agentd 可达」。
7. 不退回 contract：未发现必须新方法/新事件类型/新 CLI/新依赖方向。P3-(b) 若删掉 `ExecutionClient.WaitEvent` 的 `all` 参数才退回。P4-(b) 把挂卡下放到 Web 调用方会扩到 `d_web`，spec 未把控制台列为改动面——若拍板 (b) 需在拍板记录写明扩范围，不是静默加缝。
8. 不扇出子卡（提案）：未证明单子系统在「不含完整重试器 + 不含 Web 改动」时超过约 70 分钟。内部 ticket T1–T6 是单卡执行顺序，不是 `card split`。P1 待拍板。
9. 项目缺陷族清单 `docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md` §3：顶部没有「基线版本：charter@\<commit\>」。按项目清单加严并并入基线「承重安全属性」。清单债不在本卡改实例化协议。
10. 法定产出写到 `docs/superpowers/specs/b233.3-breakdown.md`。契约追加 §11 修订记录 C-0…C-8，冻结正文未改。
11. `codegraph --repo . resolve --doc docs/superpowers/specs/b233.3-breakdown.md --view cards-B233.3-charter`：exit 0，36 锚，`ok` 或 `moved`，坏锚 0。无 nodeId 的 moved：`ErrTunnelDisconnected` / `ErrUnreachable`（图未覆盖 var，与契约 §10 一致）、两支测试函数。`codegraph --repo . resolve --doc docs/superpowers/specs/b233.3-contract.md --view cards-B233.3-charter`：先因 §11 C-2 写成 `client.go#Client.FollowEvents` 报 `file_missing`；改为 `internal/client/client.go#Client.FollowEvents` 后 exit 0，26 锚，坏锚 0。
12. 待拍板岔口 P1–P7 集中在稿首。真机清单 12 条。未调用 handoff CLI、未起 executor、未写实现代码。

## 判断

- L3 轻档单卡直进成立：有界文件集写得出；未证明单子系统在 P5-(a)+P4-(a) 下超过约 70 分钟。P1 待拍板。
- 不退回 contract：无新方法/新事件类型/新 CLI/新依赖方向。P3-(b) 若删 `all` 参数才退回。
- 最大产品岔口是 P4（挂卡落点）与 P5（是否落地重试器）。P2 的生产 `NewRelay` 旁路经 grep 只有 `cmd/upgrade.go`。
- agentd 89 文件 / 28308 行命中升格判据，回答为「本卡圈得出有界集，不插竖切」。
- ApprovalClient 2 条 dead-* 与 HEAD 相同，不修 absorb。

## 提交

```
git commit -m "docs(B233.3): 拆解能力 client 复用直连与 relay 传输"
[cards/B233.3-charter-2 d5fd6aea] docs(B233.3): 拆解能力 client 复用直连与 relay 传输
 3 files changed, 448 insertions(+), 1 deletion(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.3-breakdown-ledger.md
 create mode 100644 docs/superpowers/specs/b233.3-breakdown.md
```

随后 amend 一次把本段收进同批提交。不回填 amend 后 HEAD。

## 拍板

2026-09-07 协调者：P1–P7 全选 (a)。理由见拆解稿表「裁决」列。新提交，不 amend 出稿提交。


