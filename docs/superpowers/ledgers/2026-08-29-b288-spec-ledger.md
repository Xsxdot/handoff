# B288 spec 台账（2026-08-29）

- 基线：卡基线 cards/B285-review-2 @ 00006512d（origin 同步）；本会话推进
  2928c6e03（option-1 原型 + design-qa + assets 上线）、4e317fe0e（顶部 chrome
  基准 + close.svg）。均已 push origin。
- 机制查证：派发节点工作分支一律从卡有效基线切（internal/ledgerstep/dispatch.go:209
  EffectiveBaseBranch → DispatchOpts.Base），故形态稿必须落在 cards/B285-review-2
  本体，不能另开分支（上一会话 00006512d 同一先例）。
- 用户拍板（2026-08-29 00:26 截屏 + 消息）：option-1 页面为基准，含新顶部 chrome
  （面包屑 + 标签栏）；要求 spec/plan/实现一比一对照原型代码，不文字转述样式；
  无人值守，不经 handoff 派发，subagent 推进，合并前停。
- 范围裁定 1：拖放上/下落区不做。b288-workbench-ux 的五区依赖「列内上下叠放」
  中央模型（placeTask 的 col.panes 竖排，index.html:627-629）；现真机模型无竖排
  （tabs.ts 只有横向 groups）。该模型归 B264（card show B264：「终止 tab页不再以
  目录为分组…」，base origin/main）。本卡落点视觉 1:1 只覆盖现有三区。
- 范围裁定 2：项目行悬停「工作项/代码图」链接不做，与 W4 §3.1「树不再是筛选的
  编辑入口」冻结决策冲突（ProjectTree.tsx:13-15 现状注释）。
- 现状读数：tabTitle tui=taskId 前 8 位（web/src/app/workbench/tabs.ts:387）；
  落点指示 ring-primary/60 + 3px 边条（WorkbenchPage.tsx:369-387）；已结束只收
  orphan（web/src/app/tree/archived.ts:48-76）；箭头在行左（ProjectTree.tsx:113-126）；
  面包屑零交互约束（web/src/app/shell/Breadcrumb.tsx:6-14）。
- 图覆盖：codegraph sym 命中 tabTitle / ProjectTree / WorkbenchPage（baseline 视图），
  web 符号在图内，收尾走图对账。
- 原型树形态要点（b288-workbench-ux renderTree, index.html:814-935）：任务组 live
  = openSet ∪ 非终态；已结束 aKey 按项目归集、默认收起、搜索旁路；目录组机器行
  悬停 ⌘/＋ 动作；方案 2 行 = 类型头像 + 状态点 + 标题 + 右机器名。
- 测试设施：web vitest + testing-library（web/package.json scripts.test=vitest run，
  typecheck=tsc -b）。

## plan 节点（2026-08-29）

- 读码：tabs.ts / archived.ts / WorkbenchPage.tsx / ProjectTree.tsx / TabBar.tsx /
  Breadcrumb.tsx / useWorkbench.ts / paneDrop.ts 全文；b288-workbench-ux renderTree
  与 option-1 样式段。focusTab 需新增（open 的去重键对无会话终端为 null，
  open 聚焦会开出第二个终端）。
- plan 落盘 docs/superpowers/plans/b288-plan.md：T1 命名 → T2 TabBar → T3 面包屑 →
  T4 拖放 → T5 列宽 → T6a 已结束口径 → T6b 左栏重绘 → T7 接线 → T8 全量。
  五项检查过；组件测试用「照抄既有 harness + 断言逐条列全」出口并声明。
