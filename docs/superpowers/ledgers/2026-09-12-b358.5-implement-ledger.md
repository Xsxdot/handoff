# B358.5 implement 台账（2026-09-12）

- 执行者：charter implement 节点 subagent；分支 `cards/B233.1-charter-7`（基点 4162505f + plan 提交 b2c4ebe0）
- 依据：`docs/superpowers/plans/b358.5-plan.md` + 文末协调者拍板六条（岔口 11.1=方案 A 守卫收窄；11.5=B365 新卡；11.2 owner 必填 flag 准；11.3 send 两形态按 flag 准；11.4 join 哨兵字面回写准；11.6 两处校验准）
- 有界文件集：`cmd/session.go`、`cmd/session_test.go`、`cmd/room_test.go` + 本台账（breakdown 回写由协调者拍板时完成，不归本卡）

## §一 Task 0：基线复跑（2026-09-12/13 交替日，工作树 @ plan 提交 b2c4ebe0）

| 命令 | 读数 | 与 plan §1 基线比对 |
| --- | --- | --- |
| `go build ./...` | 退出 0（BUILD_OK） | 一致 |
| `go test ./cmd -count=1` | FAIL，失败恰 8 支：TestRepoContractGate、TestServePermissionHookDenyWithReasonAndStep0、TestRoomSendLandsRoomMessageWithUserKind、TestRoomSendCoordinatorAcceptsExplicitSeatFlags、TestRoomSendCoordinatorKindUsesGrokHostSession、TestRoomSendCarriesRefAndMention、TestRoomReadStdoutSeqPrefix、TestRoomListSortedByActivity | 一致（§0.3 红窗 6 + 禁触 2） |
| `go test ./cmd -run TestRepoContractGate -count=1 -v` | FAIL，违规恰 6 条（原文见 §一.1） | 一致 |
| `go test ./internal/agentd -run TestLegacyNodeEventSequenceUnchanged -count=1` | FAIL（B362 golden 漂移：got=GOLDEN|2|dispatched|… want=GOLDEN|2|comment|…） | 一致 |
| `go test ./cmd -run 'TestSessionWait' -count=1` | ok，7 支全 PASS（含 TestSessionWaitSourceGuard） | 一致 |
| `go test ./cmd -run TestRoom -count=1 -v`（红窗枚举） | FAIL 6 支与 §0.3 逐支同名；TestRoomSendUserRejectsSeatFlagsWithoutSideEffects、TestRoomInboxWalksAgentdHTTP PASS | 一致 |

### §一.1 TestRepoContractGate 基线违规 6 条原文（收口逐行比对用）

1. `[dead-interface] 契约 d_gateway→d_orchestration 声明的接口 "OrchestrationClient" 在 d_gateway 中不存在（无同名非 deleted 节点；期望在 d_gateway 找到）`
2. `[over-budget] d_cli->d_workspace 直调 11 条超出预算 9`
3. `[over-budget] d_gateway->d_workspace 直调 17 条超出预算 1`
4. `[over-budget] d_orchestration->d_workspace 直调 49 条超出预算 19`
5. `[over-budget] d_workspace->d_orchestration 直调 3 条超出预算 2`
6. `[over-budget] d_workspace->d_protocol 直调 23 条超出预算 3`

### §一.2 图覆盖债（plan §0 声明承接）

本卡触及面整体不在 `codegraph/best.json`（`openRoomService`/`roomServiceFor` 在 baseline 视图；B358.3 新符号与 collab 会话门面七方法均不在）——一律 `file#Symbol` 源码锚；新增/修改符号清单见 §五，交 integrate/recon 补视图 diff。

## §二 Task 1：session 六子命令（红→绿）

### §二.1 步骤 1 红（session_test.go 末尾追加 7 支 + mustSendFixture，既有内容零改动）

```
$ go test ./cmd -run 'TestSessionCreateCommand|TestSessionListColumnsAndUnread|TestSessionDetailThreeSections|TestSessionJoinLifecycle|TestSessionLeaveLifecycle|TestSessionSendNegativePaths|TestSessionArchiveCommand' -count=1 -v
--- FAIL: TestSessionCreateCommand (0.00s)      # session_test.go:264: session create: unknown flag: --owner
--- FAIL: TestSessionListColumnsAndUnread (0.01s)
--- FAIL: TestSessionDetailThreeSections (0.01s)
--- FAIL: TestSessionJoinLifecycle (0.01s)
--- FAIL: TestSessionLeaveLifecycle (0.01s)
--- FAIL: TestSessionSendNegativePaths (0.01s)
--- FAIL: TestSessionArchiveCommand (0.01s)
FAIL    github.com/Xsxdot/handoff/cmd
```

红因核实：`unknown flag: --owner` / unknown command 形——session 根命令只挂 wait，六子命令不存在（功能缺失非 typo），与 plan §4 预期红一致。

### §二.2 步骤 2–4 绿（session.go 续写六子命令 + init 注册行 + 头注释一行）

```
$ go build ./... → 退出 0
$ go test ./cmd -run 'TestSession' -count=1 -v
  收窄前：新 7 支全 PASS；TestSessionWaitSourceGuard --- FAIL（守卫对文件级子串
  "proto.RoomMsgUser" 过 Fire——plan 岔口 1 预言的红，非本卡违规）
  收窄后（拍板①方案 A）：TestSession 七支新 + wait 七支（含守卫）全 PASS，
  ok github.com/Xsxdot/handoff/cmd
```

守卫收窄执行：`cmd/session_test.go#TestSessionWaitSourceGuard` 函数体整块替换为 plan §4 步骤 5 方案 A 代码（kind 门控/mentions 直读两类禁令收窄为 runSessionWait/buildSessionWake 两函数 AST 级 SelectorExpr 禁令；广播帮手名保留文件级子串禁令；MessageWakeTargets 唯一入口断言扩展到两函数）。wait 段生产码零改动（runSessionWait/buildSessionWake/sessionWaitCmd/既有 init 三行原文未动——收窄只改守卫测试）。

### §二.3 步骤 5 守卫收窄变异两发（先红后还原绿；文件修改一律 Edit 工具，grep 确认）

| # | 变异内容（runSessionWait 内临时插入） | 编译 | 守卫读数 |
| --- | --- | --- | --- |
| ① | `if msg.Kind == proto.RoomMsgUser { hit = true }`（插在 `hit := false` 后） | `go build ./cmd` 过 | `session_test.go:226: runSessionWait 出现 kind 门控/mentions 直读形状 "RoomMsgUser"——通道内不得重造寻址判定（条 42）` → --- FAIL |
| ② | `for _, m := range msg.Mentions { _ = m }`（插在 IsSessionRoom 判断后） | `go build ./cmd` 过 | `session_test.go:226: runSessionWait 出现 kind 门控/mentions 直读形状 "Mentions"——通道内不得重造寻址判定（条 42）` → --- FAIL |

两发均为可编译的语义变异（SelectorExpr 打中 AST 禁令，非删行）；还原后 `grep -c MUTATION session.go` = 0，`go test ./cmd -run TestSessionWaitSourceGuard -count=1` → PASS（绿）。变异未提交。

结论：守卫收窄后禁令仍有牙——wait 两函数内的 kind 闸/mentions 直读两形状都被拦，同文件 send 段的合法引用不被误伤（AST 级精确度高于文件级子串）。

## §三 Task 2：红窗 6 支改写收口（cmd/room_test.go 就地改写）

- 文件头注释追加改写说明一行；6 支逐支改写（函数体首行「B358.5 红窗改写」注释指认原断言与迁移去向），名字全部保留。
- 存活测试字节级核对（对照 HEAD `git show` + awk 函数提取 + diff）：TestRoomSendUserRejectsSeatFlagsWithoutSideEffects、TestRoomInboxWalksAgentdHTTP、mustAddCard 均 IDENTICAL；clearSeatSourceEnv 在 cmd/card_driver_test.go 未触碰。
- `git diff cmd/room_test.go` hunk 复核：全部改动落在文件头注释 + 6 支改写函数体（hunk 头显示的邻近函数名是 diff 上下文标注，实际改动行属下一函数，已逐 hunk 检视确认）。

```
$ go build ./... → 退出 0
$ go test ./cmd -run TestRoom -count=1 -v
--- PASS: TestRoomSendLandsRoomMessageWithUserKind        （红窗改写 1 收口绿）
--- PASS: TestRoomSendCoordinatorAcceptsExplicitSeatFlags （红窗改写 2 收口绿）
--- PASS: TestRoomSendCoordinatorKindUsesGrokHostSession  （红窗改写 3 收口绿）
--- PASS: TestRoomSendUserRejectsSeatFlagsWithoutSideEffects（存活，绿）
--- PASS: TestRoomSendCarriesRefAndMention                （红窗改写 4 收口绿）
--- PASS: TestRoomReadStdoutSeqPrefix                     （红窗改写 5 收口绿）
--- PASS: TestRoomListSortedByActivity                    （红窗改写 6 收口绿）
--- PASS: TestRoomInboxWalksAgentdHTTP                    （存活，绿）
ok    github.com/Xsxdot/handoff/cmd
```

## §四 Task 3：收口核算（三段律全量 + 红窗对账 + 契约闸比对）

| 命令 | 读数 | 判据 |
| --- | --- | --- |
| `go build ./...` | 退出 0 | 收口判据 1 ✓ |
| `go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` | ok ×5（collab、collab/room、ledger、ledger/api、proto） | 收口判据 5 后半 ✓ |
| `go test ./cmd -count=1` | 失败恰 2 支：TestRepoContractGate、TestServePermissionHookDenyWithReasonAndStep0 | 收口判据 2 ✓（红窗 6 支全部收口绿，无增无减） |
| `go test ./internal/agentd -count=1` | 失败恰 1 支：TestLegacyNodeEventSequenceUnchanged | 收口判据 5 前半 ✓ |
| `go test ./cmd -run TestRepoContractGate -count=1 -v` | 违规恰 6 条，与 §一.1 基线逐行一致（1 dead-interface + 5 over-budget，逐字相同） | 收口判据 4 ✓（本卡零新增跨域 import：session.go 新增仅 stdlib sort/strings/text/tabwriter） |
| `go test ./cmd -run 'TestSession' -count=1` | ok（新 7 支 + wait 七支含收窄守卫全绿） | 收口判据 3 ✓ |
| `go test ./cmd -run 'TestSessionWait' -count=1` | ok | B358.3 面无波及 ✓ |

### §四.1 红窗逐支核算表

| 基线红（§0.3） | 改写后 | 名字保留 |
| --- | --- | --- |
| TestRoomSendLandsRoomMessageWithUserKind | PASS（旧面 room send 报错形状 + session send 人形态落账） | ✓ |
| TestRoomSendCoordinatorAcceptsExplicitSeatFlags | PASS（成对 flag 席位落账，kind 收敛 user） | ✓ |
| TestRoomSendCoordinatorKindUsesGrokHostSession | PASS（grok 值改 flag 出示，环境键不参与） | ✓ |
| TestRoomSendCarriesRefAndMention | PASS（--ref/--mention 迁 session send） | ✓ |
| TestRoomReadStdoutSeqPrefix | PASS（#seq 前缀/--after 照抄 + 旧房间读对质） | ✓ |
| TestRoomListSortedByActivity | PASS（活动降序迁 session list） | ✓ |
| TestRepoContractGate | FAIL（禁触，违规 6 条逐行同基线） | 禁触 |
| TestServePermissionHookDenyWithReasonAndStep0 | FAIL（macOS 环境性红，禁触） | 禁触 |

### §四.2 图符号清单（交 integrate/recon 补视图 diff；全部 file#Symbol 源码锚）

**新增（cmd/session.go）**：`validSessionMemberIdentity`、`sessionCreateCmd`（var+RunE）、`sessionListCmd`、`sessionDetailCmd`、`sessionArchiveCmd`、`sessionJoinCmd`、`sessionLeaveCmd`、`sessionSendCmd`；包级 var `sessionCreateOwner`、`sessionListMember`、`sessionListJSON`、`sessionDetailJSON`、`sessionSendRefs`、`sessionSendMention`、`sessionSendCLI`、`sessionSendSession`。

**新增（cmd/session_test.go）**：`mustSendFixture`、TestSessionCreateCommand、TestSessionListColumnsAndUnread、TestSessionDetailThreeSections、TestSessionJoinLifecycle、TestSessionLeaveLifecycle、TestSessionSendNegativePaths、TestSessionArchiveCommand。

**修改（cmd/session.go）**：`init()` 追加 8 行 flag 注册 + 1 行 AddCommand（wait 的 Flags/AddCommand/rootCmd 三行零改动）；文件头注释追加 B358.5 交界行；import 增 sort/strings/text/tabwriter。

**修改（cmd/session_test.go）**：`TestSessionWaitSourceGuard` 函数体整块替换（拍板① 方案 A 守卫收窄，见 §二.3）。

**修改（cmd/room_test.go）**：6 支红窗改写符号（名字不变）：TestRoomSendLandsRoomMessageWithUserKind、TestRoomSendCoordinatorAcceptsExplicitSeatFlags、TestRoomSendCoordinatorKindUsesGrokHostSession、TestRoomSendCarriesRefAndMention、TestRoomReadStdoutSeqPrefix、TestRoomListSortedByActivity；文件头注释追加改写说明行。

**codegraph resolve --doc 读数**：7 锚全解析——2 ok（collab/room/room.go#VerifyWriter、internal/ledger/store.go#Store.tval）；5 moved（scheddispatch_test.go#TestLegacyNodeEventSequenceUnchanged:493、sessionsapi.go#handleSessionDetail:111、session_test.go#TestSessionWaitSourceGuard:186、sessionsapi.go#validSessionOwner:31、session.go#validSessionMemberIdentity:203）。moved = 符号在源码解析到行但不在入库视图（plan §0 声明的图覆盖债，交 recon 补视图）。

### §四.3 偏差清单

无。plan §4 步骤 1 红读数注记一处形状差异（不构成偏差）：plan 预期红显示「unknown command "create"」字样，实测 cobra 报「unknown flag: --owner」（同一红因：create 子命令不存在，flags 未注册——功能缺失非 typo，见 §二.1）。
