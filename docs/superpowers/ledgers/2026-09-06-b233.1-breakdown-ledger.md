# B233.1 breakdown 台账

日期：2026-09-06。节点：charter breakdown。工作分支：`cards/B233.1-charter-3`。开工 HEAD：`e76373041ddaf38c792030ecf8cc2af34a099c60`。

## 过程

1. 形态 2：handoff executor 出稿，协调者拍板。不写实现、不建卡、不派发。法定产出 `docs/superpowers/specs/b233.1-breakdown.md`。L3 轻档，prompt 禁止扇出新子卡。
2. 上游状态位：spec 头部「已批准」（2026-09-06）；契约头部原写「冻结状态：随本提交冻结」。HEAD=`e7637304` 即 contract 提交。拆解期按 b298 惯例把状态行补钉为「已冻结（2026-09-06，提交 e7637304）」，冻结正文不改。
3. 查图：`codegraph --repo . domains` 走的是基线视图（出现 `d_coordination`，与 best 顶层域不一致）。子系统清单以 `codegraph/best.json` 顶层领域为准（`parent` 空 + `type`）。`--view cards-B233.1-charter-2` 能决议 Ticket 0 新符号 `ApprovalClient` / `AskResponder` / `PermFingerprint` / `NamespacedTicketID` / `StartReq`；基线容器 domain 字段仍是 `d_executor`，与 contract §10 图覆盖债一致。
4. `codegraph who-calls handlePermission` 短名不在图中；完整 id `n_agentd_Manager_handlePermission`。未当调用图证据。
5. 架构法第三条实读（非测试源）：`internal/agentd` 88 文件 / 27902 行（命中「单包 ≥40 且无子包」与项目红线 20000 行）；最大前缀家族 `preview*`=10（与本卡无关）。`internal/executor/opencode` 11 文件 / 4691 行。本卡能圈出有界文件集，不插竖切卡（prompt 禁止扇出新子卡）。
6. 代码事实（本轮 grep/读源，非行为验收）：
   - `Adapter.Send`（`internal/executor/opencode/adapter.go#Send`）有 pending question 走 `ReplyQuestion`。
   - `Manager.waitQuestion` / `RelayAnswer` 的 ask 分支调 `ad.Send`。
   - `handleQuestion` 拒绝理由优先吞下一条提问（函数注释原文）。
   - `isDeliverable` 未把 `permission_reuse` 列入假分支；web `QUIET_EVENTS` 已含该类型。
   - `FindReusableGrant` 无政策版本条件；`tickets` 表无 `policy_version` 列。
   - `WriteTaskEnv` 写死 `edit:allow` 与 bash 模式表。
   - `StartReq.Approval` 槽已在，`Manager.Dispatch` 的 `ad.Start` 调用未注入。
   - `Gate.Judge` 出口仍是 AutoAllow/Consult/Escalate；client 对外不得返回 Consult。
7. 不退回 contract：未发现必须新接缝。复用键版本若做成 `proto.Ticket` 新 JSON 字段才退回（P2-(c)）。
8. 不扇出子卡。内部 ticket T1–T6 是单卡执行顺序，不是 `card split`。
9. 法定产出写到 `docs/superpowers/specs/b233.1-breakdown.md`。契约追加 §11 修订记录 C-0…C-7，冻结正文未改。
10. `codegraph --repo . resolve --doc docs/superpowers/specs/b233.1-breakdown.md --view cards-B233.1-charter-2`：exit 0，31 锚，`ok` 或 `moved`，坏锚 0。
11. 待拍板岔口 P1–P6 集中在稿首。真机清单 16 条。未调用 handoff CLI、未起 executor、未写实现代码。

## 判断

- L3 轻档单卡直进成立：有界文件集写得出，prompt 禁止扇出。
- 不退回 contract：无新方法/新事件类型/新 CLI。P2-(c) 若拍板才退回。
- 最大岔口是 P1（Request/Await 调用点），两种都消费已冻 `ApprovalClient`。
- agentd 88 文件/27902 行命中升格判据，回答为「本卡圈得出有界集，不插竖切」。

## 提交

```
git commit -m "docs(B233.1): 拆解派发事件流、Ask 与审批 client 闭环"
[cards/B233.1-charter-3 c4d95109] docs(B233.1): 拆解派发事件流、Ask 与审批 client 闭环
 3 files changed, 469 insertions(+), 1 deletion(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-06-b233.1-breakdown-ledger.md
 create mode 100644 docs/superpowers/specs/b233.1-breakdown.md
```

随后 amend 一次把本段收进同批提交。不回填 amend 后 HEAD。


