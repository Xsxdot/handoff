# B358.9 T4 实现台账（web 控制台身份内容面 + TS 孪生金样本）

节点：Implement（本回合只做 plan §7 的 T4；T1–T3 已实现并过审，不回头改 Go 侧）。
计划：`docs/superpowers/plans/b358.9-plan.md` §7。
契约：`docs/superpowers/specs/b358.9-contract.md`；拍板：breakdown P-4 甲 / P-5 甲。
卡：B358.9；工作分支：`cards/B358.9-charter-9`；角色：实现节点，法定产出为 T4 有界文件集内代码改动。
一行一条历史读数；命令与原始输出照抄。

## 0. 开工状态位

1. 2026-09-18：`git branch --show-current` → `cards/B358.9-charter-9`；开工前 `git status --porcelain` → 无输出（干净）。
2. 2026-09-18：`git log --oneline -3` → HEAD `ab161124`（T2T3 提交），其下 `e3b11c06`（T1）。
3. 2026-09-18：`ls web/node_modules` → 不存在（被 .gitignore 忽略）；`npm ci --prefix web --ignore-scripts --no-audit --no-fund` → `added 290 packages in 2s`。
4. 2026-09-18：`which codegraph` → `/usr/local/bin/codegraph`（恒在层要求已满足，未自行安装）。

## 1. 基线复核

5. 2026-09-18：`cd web && npx tsc -b` → 退出码 `0`，无输出。
6. 2026-09-18：身份读缝 Go 侧金样本实读：`internal/proto/identity_test.go#TestIdentityRespWireShape`（三键 member/device/configured 恒出）、`internal/proto/sessions_fixture_test.go#TestRoomMessageDeviceStampGolden`（device 零值省键、非零在线）。T4 fixture 与之逐键同源。

## 2. 先写失败测试（红）

7. 2026-09-18：`web/src/api/testdata/RoomsFixture.json` 末尾追加三 case：`identity-configured`（`{member:"user:sycm",device:"mbp / Safari",configured:true}`）、`identity-unconfigured`（空值三键）、`user-stamped`（`device:"mbp / Safari"`）——与 Go 金样本唯一来源同一 JSON，TS 不手抄。
8. 2026-09-18：`rooms.test.ts` import 增 `IdentityResp`、cases 类型增 `identity?`；追加「身份读缝孪生样本（B358.9）」三支。
9. 2026-09-18：`rooms.fetch.test.ts` import 增 `fetchIdentity`；createSession 用例改「只发 {title}」；新增 `fetchIdentity GET /api/identity 三键解码`。
10. 2026-09-18：`sessionModel.test.ts` import 增 `isSelfActor`/`signatureText`；追加「自方判定与落款（B358.9 P-4）」三支（含 `web:` 反例、null 回落）。
11. 2026-09-18：`NewSessionDialog.test.tsx` 整体重写（表单只有标题、onCreate(title)、configured=false 提示含 console_user 且禁用、空标题禁用）。
12. 2026-09-18：`SessionChat.test.tsx` mock 增 `fetchIdentity`；beforeEach 补默认解析值；追加落款人名+端名、自方靠右两支。
13. 2026-09-18：`Shell.test.tsx` mock 增 `fetchIdentity`；删除 owner 记忆/自动补员三支，改「只交标题」「不调用 addSessionMember」两支。

14. 2026-09-18：跑红（`cd web && npx vitest run <5 文件>`）→ `EXIT=1`，`Tests 9 failed | 64 passed (73)`；失败原文摘要（均为功能缺失）：

```
FAIL src/api/rooms.fetch.test.ts > fetchIdentity GET /api/identity 三键解码
TypeError: fetchIdentity is not a function
FAIL src/app/rooms/NewSessionDialog.test.tsx > 表单只有标题：无群主输入框（B358.9 反例）
AssertionError: expected <input …(6)></input> to be null
FAIL src/app/rooms/SessionChat.test.tsx > 落款渲染人名+端名；device 缺失只出人名
FAIL src/app/rooms/sessionModel.test.ts > isSelfActor 与人名精确等值：他人 user:/agent:/web: 都不给自方
AssertionError: expected false to be true
FAIL src/app/rooms/sessionModel.test.ts > signatureText 有端戳出人名·端名，缺端戳只出人名
TypeError: signatureText is not a function
```

15. 2026-09-18：编译红（新缝符号缺席，先核实非拼写错）`cd web && npx tsc -b` → `TSC_EXIT=2`：

```
src/api/rooms.fetch.test.ts(11,3): error TS2305: Module '"./rooms"' has no exported member 'fetchIdentity'.
src/api/rooms.test.ts(10,3): error TS2305: Module '"./rooms"' has no exported member 'IdentityResp'.
src/app/rooms/NewSessionDialog.test.tsx(11,55): error TS2322: ... Property 'configured' does not exist ...
src/app/rooms/SessionChat.test.tsx(7,28): error TS2305: ... has no exported member 'fetchIdentity'.
src/app/rooms/sessionModel.test.ts(9,35): error TS2305: ... has no exported member 'signatureText'.
```

## 3. 最小实现

16. 2026-09-18：`web/src/api/rooms.ts`：新增 `IdentityResp` 接口（三键恒出）与 `fetchIdentity`；`createSession(title)` 只发 `{title}`；`fetchSessions` 注释改「解析人名」。
17. 2026-09-18：`web/src/app/rooms/sessionModel.ts`：`isSelfActor(actor, selfMember)` 精确等值 + null 回落 `user:`（退役 `web:` 判据）；新增 `signatureText(actor, device?)`。
18. 2026-09-18：`web/src/app/rooms/SessionChat.tsx`：import `fetchIdentity`/`signatureText`/`IdentityResp`；`MessageRow` 增 `selfMember`；落款改 `signatureText(event.actor, payload.device) · #seq`；组件增身份态（一次拉取，失败落 null 并 `logRoom('warn','session_identity_fetch_failed')`）。
19. 2026-09-18：`web/src/app/rooms/NewSessionDialog.tsx`：重写为标题-only；props 改 `configured`/`onCreate(title)`；未配名渲染含 `console_user` 的 `role="alert"` 且创建禁用。
20. 2026-09-18：`web/src/app/shell/Shell.tsx`：import 改 `createSession`/`fetchIdentity`（删 `addSessionMember`/`saveLastSessionOwner`）；删 `sessionMemberIdentities` memo；增 `consoleConfigured` 身份态（拉取失败保持放行，只服务端明确 `configured=false` 才提示）；`confirmCreateSession(title)` 去 owner/记忆/补员；渲染传 `configured`/`onCreate(title)`。
21. 2026-09-18：删除 `web/src/app/rooms/sessionOwnerPrefs.ts`（S4 后无消费者；全仓 grep 确认仅 NewSessionDialog/Shell 消费，均已改）。
22. 2026-09-18：`pollInterval.test.tsx` 的 `vi.mock('../../api/rooms')` 补 `fetchIdentity` 桩（新消费点触及该 mock；测试文件随实现同步，plan §2.1 允许）。

## 4. 验收命令与原始输出

23. 2026-09-18：靶向跑（6 文件含 Shell）`npx vitest run ...` → `EXIT=0`，`Test Files 6 passed (6)` / `Tests 129 passed (129)`。
24. 2026-09-18：全量类型 `cd web && npx tsc -b` → `TSC_EXIT=0`。
25. 2026-09-18：全量测试 `cd web && npx vitest run` → `VITEST_EXIT=0`，`Test Files 131 passed (131)` / `Tests 1428 passed (1428)`。
26. 2026-09-18：lint（触及文件）`npx eslint <12 文件>` → `ESLINT_EXIT=0`，2 warnings（Shell.tsx 78/268，均为基线既有：react-refresh、wb 依赖；stash 对照 HEAD 同样 2 warnings，非本次引入）。

## 5. 变异自验（先确认变异打唯一且编译过，再数红）

27. 2026-09-18：变异前逐条 `grep -Fc` 断言命中唯一：`return actor === selfMember`=1、`return device ? ... : actor`=1、`request<IdentityResp>('/api/identity')`=1、`postJSON<Session>('/api/sessions', { title })`=1、`{!configured && (`=1。
28. 2026-09-18：M1 `isSelfActor` 精确等值→`startsWith('user:')`：`TSC_EXIT=0`；`--- FAIL: isSelfActor 与人名精确等值`；`Tests 1 failed | 20 passed`。有牙。
29. 2026-09-18：M2 `signatureText` 条件取反（`!device ? ...`）：`TSC_EXIT=0`；`--- FAIL: signatureText 有端戳出人名·端名`；`Tests 1 failed`。有牙。
30. 2026-09-18：M3 `fetchIdentity` 端点改 `/api/identityX`：`TSC_EXIT=0`；`--- FAIL: fetchIdentity GET /api/identity 三键解码`。有牙。
31. 2026-09-18：M4 `createSession` 改回带 owner：`TSC_EXIT=0`；`--- FAIL: createSession POST {title}——不再带 owner`。有牙。
32. 2026-09-18：M5 NewSessionDialog 未配名提示门 `!configured` 改 `false`：`TSC_EXIT=0`；`--- FAIL: 未配名：渲染含 console_user 的可行动提示且创建禁用`。有牙。
33. 2026-09-18：首轮 M2 用整行删除导致 `device` 未用 → `TSC_EXIT=2`（编译不过），**该发不算数**；整块替换为可编译的等价取反（`!device ?`）后重做，方得上面的红。未出现「编译不过当 0 红」的假读数。
34. 2026-09-18：一次操作失误：M2 还原时误用 `/tmp/mut.bak`（内容为上一次其他文件）覆盖 `sessionModel.ts`，`git diff --stat` 显示结构错乱；立即 `git checkout -- sessionModel.ts` 从 HEAD 还原并按 §2 同款 edit 重贴 `isSelfActor`/`signatureText`。重贴后全量 `tsc -b` 退出 0、`vitest run` 131/1428 全绿（读数见下）。

## 6. 图查询与图覆盖债

35. 2026-09-18：`codegraph --repo . sym fetchIdentity --view cards-B358.9-charter-4` → 未命中（近似候选 []）；`sym isSelfActor`、`sym signatureText` → 同样未命中。本节点图覆盖债（未命中符号）：`fetchIdentity`、`isSelfActor`、`signatureText`、`IdentityResp`、`createSession`（TS 符号，图未覆盖），回落 grep 读源码，未拿 chain 冒充。
36. 2026-09-18：`codegraph --repo . resolve --view cards-B358.9-charter-4 --doc docs/superpowers/plans/b358.9-plan.md` → 返回 anchors（含 `internal/collab/room/delivery.go#ResolveDelivery` ok、`internal/proto/sessions_fixture_test.go#TestRoomMessageDeviceStampGolden` moved 等），无 error。

## 7. 复跑确认（还原后）

37. 2026-09-18：重贴实现后 `cd web && npx tsc -b` → `TSC_EXIT=0`；`npx vitest run` → `VITEST_EXIT=0`，`Test Files 131 passed (131)` / `Tests 1428 passed (1428)`。

## 8. 判断与放弃的尝试

38. 2026-09-18：`SessionChat` 身份拉取失败落 `null`（isSelfActor 走 `user:` 兜底）而非阻断渲染——与 plan §7.2 一致；`logRoom('warn', ...)` 不静默。
39. 2026-09-18：Shell 的 `consoleConfigured` 初值 `true`（放行），仅服务端明确 `configured=false` 才禁用——plan §7.2「拉取失败不得误判未配名」。
40. 2026-09-18：未碰 handoff CLI、未起任何 executor、未改 Go 侧、未切分支、未改 git 配置。
41. 2026-09-18：真机渲染项（缺名横幅、落款人名+端名、新建表单渲染）归 plan §10.1 清单，协调者执行；本节点 jsdom 只证组件契约。

## 9. 提交（历史读数）

42. 2026-09-18：本节点产出物（T4 有界文件集内 13 个文件 + pollInterval.test.tsx + 本台账）同批提交。命令与原始输出：

```
$ git add web/src/api/rooms.ts web/src/api/rooms.test.ts web/src/api/rooms.fetch.test.ts web/src/api/testdata/RoomsFixture.json web/src/app/rooms/sessionModel.ts web/src/app/rooms/sessionModel.test.ts web/src/app/rooms/SessionChat.tsx web/src/app/rooms/SessionChat.test.tsx web/src/app/rooms/NewSessionDialog.tsx web/src/app/rooms/NewSessionDialog.test.tsx web/src/app/rooms/pollInterval.test.tsx web/src/app/shell/Shell.tsx web/src/app/shell/Shell.test.tsx docs/superpowers/ledgers/2026-09-18-b358.9-t4-implement-ledger.md
$ git rm web/src/app/rooms/sessionOwnerPrefs.ts
$ git commit -m "implement(B358.9/T4): web 控制台身份内容面——fetchIdentity/isSelfActor 精确等值 + 端戳落款 + owner 手输退役"
```

（提交事实只记当时命令；amend 会换 hash，不在本文件追写。收口判据是工作树干净。）
