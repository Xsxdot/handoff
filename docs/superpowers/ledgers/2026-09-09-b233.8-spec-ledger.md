# B233.8 spec 台账

日期：2026-09-09
卡：B233.8
节点：spec
基线：`cards/B233.1-charter-7` @ `2fe0a5d4`

## 过程与判断

1. B351 已合入功能线 `2fe0a5d4`。用户授权自主推进本卡，不逐项请示。范围取卡标题与 B233.7 R3，不扩全仓搬目录。
2. 亲读 `internal/agentd/approval_client.go`：生产实现 `taskApprovalClient` 在 agentd。
3. 亲读 `internal/agentd/manager.go` Dispatch 约 1145–1152：只对 OpenCode 与 Claude 注入 ApprovalClient。
4. 亲读 `handlePermission` :2109、`handleQuestion` :3005：仍在；非 OpenCode 提问仍可吞拒绝理由。
5. `POST /api/tasks` / `Manager.Dispatch` 按 B233.1 为保留生产入口，不退役。
6. 定级 L3 轻档：跨编排/执行/网关，工作量单轮可收口。全仓 ideal 搬家列入 OOS。
7. 批准来源：用户 2026-09-09「自主推进233.8，不需要再问我了」；头部写已批准。

8. 首次提交命令 `git commit -m "docs(B233.8): spec 批准 L3 轻档（ApprovalClient 迁包与旧入口退役）"`；原始输出见随后 amend 收录。不回写 amend 后 hash。
