# B233.8 spec 台账

日期：2026-09-09
卡：B233.8
节点：spec
基线：`cards/B233.1-charter-7` @ `2fe0a5d4`

## 过程与判断

1. B351 已合入功能线 `2fe0a5d4`。用户授权自主推进本卡，不逐项请示。范围取卡标题与 B233.7 R3，不扩全仓搬目录。
2. 亲读 `internal/agentd/approval_client.go`：生产实现 `taskApprovalClient` 在 agentd。
3. 亲读 `internal/agentd/manager.go` Dispatch 约 1145–1152：只对 OpenCode 与 Claude 注入 ApprovalClient。
4. 亲读 `handlePermission` :2109、`handleQuestion` :3005：仍在；非 OpenCode 提问仍可吞拒绝理由。
5. `POST /api/tasks` / `Manager.Dispatch` 按 B233.1 为保留生产入口，不退役。
6. 定级 L3 轻档：跨编排/执行/网关，工作量单轮可收口。全仓 ideal 搬家列入 OOS。
7. 批准来源：用户 2026-09-09「自主推进233.8，不需要再问我了」；头部写已批准。
8. 首次提交 `6fa64203`。独立审查 fail：范围与 B233.7 R3 / B233.1 §7 冲突；Claude「已注入」被误当成权威已迁；提问三解；接缝不是符号+调用方；轻档与五家纳入不一致。
9. 收窄：本卡只迁 ApprovalClient 生产实现出 agentd，OpenCode Dispatch/Continue/Resume 不回退；handlePermission/handleQuestion 点名为非 OpenCode 残桩并 OOS。五家权威迁移列入后续。
10. 修订后提交不 chase amend hash。

8. 首次提交命令 `git commit -m "docs(B233.8): spec 批准 L3 轻档（ApprovalClient 迁包与旧入口退役）"`；原始输出见随后 amend 收录。不回写 amend 后 hash。

## Contract 节点过程

1. 2026-09-09：开工时当前工作树为 `cards/B233.8-charter`，附件 spec 缺失；经协调者提供上游后执行 `git fetch origin cards/B233.8-charter && git merge --no-ff ef78a7dc`，原始输出：`From github.com:Xsxdot/handoff`、`* [new branch] cards/B233.8-charter -> origin/cards/B233.8-charter`、`Merge made by the 'ort' strategy`，HEAD 为合并提交 `8bad8875`。
2. 2026-09-09：`sed -n '1,240p' docs/superpowers/specs/b233.8.md` 核实头部状态为 `已批准`（2026-09-09）；范围为只迁 ApprovalClient 生产实现出 agentd，OpenCode Dispatch/Continue/Resume 不回退。
3. 2026-09-09：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . summary` 实跑输出：`本仓库有代码图：4504 节点 / 5522 边 / 23 领域`；`codegraph sym taskApprovalClient` 与 `sym Manager.bindApproval` 基线均失败，原始错误均含 `符号 ... 不在图中（图未覆盖或名字有误）`。
4. 2026-09-09：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.1-charter-2 sym taskApprovalClient` 实跑命中 `m_agentd_taskApprovalClient`，文件 `internal/agentd/approval_client.go:23`；同视图 `sym n_agentd_Manager_bindApproval` 命中，签名为 `func (m *Manager) bindApproval(taskID string, snap executor.PolicySnapshot) executor.ApprovalClient`。
5. 2026-09-09：`jq` 读取 `codegraph/best.json` 核实 `k_agentd_Manager`、`k_agentd_fn`、`k_store_Store` 属 `d_orchestration`，`k_executor_model`、`k_executor_fn` 属 `d_execution_contract`，`k_permgate_*` 属 `d_policy`；目标图已有 `d_execution→d_orchestration` 的 `interfaces: ["ApprovalClient"]` 与 `d_orchestration→d_execution` 预算 27。
6. 2026-09-09：亲读 `internal/agentd/approval_client.go` 全文件及 `internal/agentd/manager.go` 的 Manager、ticket payload、判据/审批/事件/状态 helper；当前 client 依赖 Manager 私有能力、Store 与 Hub，直接物理搬文件会形成包边界编译错误，契约需冻结新的编排实现端口与组装调用。
7. 2026-09-09：查证当前接缝签名：`internal/agentd/manager.go#Manager.resolveSnapshot`、`#Manager.judgePermission`、`#Manager.autoAllowPermission`、`#Manager.shouldConsultApprover`、`#Manager.countApproverFail`、`#Manager.NoteDeliveryFailed`、`#Manager.transitBestEffort` 与 `internal/agentd/approval_client.go#Manager.bindApproval`；`internal/executor/opencode/adapter.go#Adapter.BindApproval` 现为 `func (a *Adapter) BindApproval(taskID string, client executor.ApprovalClient) error`。
8. 2026-09-09：查证依赖行为：`Hub.WaitAnswer` 只受调用方 context 控制且无额外超时，`Hub.Publish` 对订阅者非阻塞发送并丢弃慢订阅者；`Store.CreateTicket` 使用 INSERT OR IGNORE，`FindReusableGrant` 要求 gate、allow 且 delivered_at 非空，`MarkTicketDelivered` 仅更新未送达票据，`AnswerTicketApplied` 对冲突答案返回 `ErrTicketConflict`。出处为 `internal/agentd/hub.go#Hub.WaitAnswer`、`#Hub.Publish` 与 `internal/store/store.go#Store.CreateTicket`、`#Store.FindReusableGrant`、`#Store.MarkTicketDelivered`、`#Store.AnswerTicketApplied`。
9. 2026-09-09：确定骨架物理落点为 `internal/approval`，归现有 `d_orchestration`，不新增 `d_approval`；骨架只声明 `Client`、`Hooks`、`AnswerHub`、`ConsultDecision`、`NewClient` 与 `executor.ApprovalClient` 实现边，不接入 Manager 运行路径。
10. 2026-09-09：一次组合 `rg` 查询因正则未闭合失败，原始输出：`regex parse error: unopened group`；放弃该查询并改用分组字面符号搜索，随后命中上述现状签名。
11. 2026-09-09：Ticket 0 骨架 `internal/approval/client.go` 已用 `gofmt -w internal/approval/client.go` 格式化；`go test ./internal/approval` 原始输出 `? github.com/Xsxdot/handoff/internal/approval [no test files]`、退出码 0；`go build ./...` 无 stdout、退出码 0。
12. 2026-09-09：首次图校验因 diff 未声明新容器失败，原始错误：`Error: 发现 5 个完整性问题、0 个失鲜节点`；其中五项均为 `新增节点 ... 引用不存在的容器 k_approval_model/k_approval_fn`，退出码 1。按 `codegraph/validate.go#ValidateDiff` 的 `containersAdded` 规则补入两个新容器。
13. 2026-09-09：补容器后 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 原始 JSON 关键字段为 `"containers": 300`、`"issues": null`、`"views": [...,"cards-B233.8-charter"]`，退出码 0；`--view cards-B233.8-charter check` 原始 JSON 的 `"fails": []`，退出码 0，另有既存 warnings，未把 warnings 改写为通过。
14. 2026-09-09：首次 `resolve --doc docs/superpowers/specs/b233.8-contract.md` 失败，原始错误：`Error: 文档锚点检查失败: internal/agentd/approval_client.go#Manager.bindApproval (vanished)`；为当前视图补入现状组装/恢复/adapter 接缝节点后重跑成功，输出 17 个锚点，`Manager.bindApproval`、`Manager.resolveSnapshot`、`Adapter.BindApproval`、`Adapter.authorizeNativePermission` 为 `anchor: ok`，其余已覆盖现状节点为 `moved`，退出码 0。
15. 2026-09-09：`sym approval` 模糊查询失败，原始错误：`Error: 符号 "approval" 不在图中（图未覆盖或名字有误）`；按错误建议改查节点 ID，`sym m_approval_AnswerHub`、`sym m_approval_ConsultDecision`、`sym m_approval_Hooks`、`sym m_executor_ApprovalClient` 均返回 `status: added`/`anchor: ok`，各退出码 0。`git diff --check` 无输出、退出码 0。
16. 2026-09-09：最终验证：`validate` 输出 `"issues": null`、退出码 0；`--view cards-B233.8-charter check` 输出 `"fails": []`、退出码 0（warnings 为既存图债，未当作失败）；`resolve --doc docs/superpowers/specs/b233.8-contract.md` 输出 17 个锚点且退出码 0；`go test ./internal/approval` 输出 `? github.com/Xsxdot/handoff/internal/approval [no test files]`、退出码 0；`go build ./...` 无 stdout、退出码 0；`jq` 复核 target 保留 `d_execution→d_orchestration` interfaces `ApprovalClient` / budget 0 与 `d_orchestration→d_execution` budget 27、无新增 entries。
17. 2026-09-09：提交命令 `git commit -m "docs(B233.8): freeze ApprovalClient migration contract"` 原始输出：`[cards/B233.8-charter 8052b55c] docs(B233.8): freeze ApprovalClient migration contract`、`6 files changed, 590 insertions(+), 2 deletions(-)`；本行随后随台账单独 amend 收入，不回写 amend 后 hash。
