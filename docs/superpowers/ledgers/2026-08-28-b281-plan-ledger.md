# B281 plan 节点台账

- 2026-08-28：当前分支为 `cards/B281-charter-2`；`git status --short` 初始无输出。遵守本节点只写计划与台账，不改实现、不切分支、不 push。
- 2026-08-28：执行 `git fetch origin feat/B281-workbench-ia` 成功，原始输出：`From github.com:Xsxdot/handoff`、`* branch feat/B281-workbench-ia -> FETCH_HEAD`、`3e07208f..7e13dd0b feat/B281-workbench-ia -> origin/feat/B281-workbench-ia`。原型实际从 `origin/feat/B281-workbench-ia:prototypes/b264-tab-groups/index.html` 读取，未复制进实现目录。
- 2026-08-28：原型 `index.html` 实际核对：CSS 第 40-65 行是 `.cols` 横向列、每列最多两格、窗格四向/中间投放；HTML 第 198-208 行是面包屑、全局组栏、布局条、中央列与可隐藏文件抽屉；脚本第 397-460 行是全局查找/新组/目录终端与 payload；第 485-577 行是四向投放、列最多两格、加空列与布局形状；第 626-652 行是可关闭文件抽屉；第 654-767 行是项目→机器→任务/目录双同级组；第 769-801 行是全局组栏；第 821-872 行是列/窗格渲染。原型 `g4` 形状（第 564-567 行）与 B281 明确弃选六/五宫格的边界冲突，计划只吸收列/两格语义，不实现固定四宫格按钮。
- 2026-08-28：完整读取 `docs/superpowers/specs/b281.md`。冻结行为为：全局标签组、不同项目同组、左右加列、一列最多上下两格、中间替换、目录拖放用该目录 cwd、文件抽屉可关闭、整页路由覆盖但不卸载终端；不改 agentd/PTY/账本/跨机同步协议，不把原型 HTML 当实现。
- 2026-08-28：代码图命令 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_web_workbench` 成功。输出确认 best 领域 `d_web_workbench`，包为 `web/src/app/files`、`homedock`、`tree`、`workbench`；`fociTruncated.total=10, shown=5`；`actual.byCurrentDomain` 显示 8 个容器实际在 `d_web`；8 个 `k_web_app_*` 容器列入 `actual.misplaced`；`projScanned=false` 的实体警告表示不能把图当投影清单。该覆盖债不阻断本计划，因为本卡只改已实读的 d_web 前端文件，跨领域依赖以源码/测试复核。
- 2026-08-28：代码图 `sym n_web_app_workbench_useWorkbench` 成功，命中 `web/src/app/workbench/useWorkbench.ts:145`；`who-calls n_web_app_workbench_useWorkbench --with-source` 成功，确认 `Shell` 是图内调用方，并报告既有 `unscannedEntries=6`。`sym n_web_app_workbench_WorkbenchPage` 命中 `WorkbenchPage.tsx:162`，但 `who-calls` 只有自身节点且提示 6 个未扫描入口；`sym/who-calls n_web_app_tree_ProjectTree` 命中 `ProjectTree.tsx:232`，同样提示未扫描入口。未命中的调用面通过 `rg` 复核并记录为图覆盖债。
- 2026-08-28：首次在无 `web/node_modules` 的基线执行 `cd web && npm test -- --run src/app/workbench/tabs.test.ts src/app/workbench/paneDrop.test.ts src/app/workbench/persist.test.ts src/app/workbench/restore.test.ts src/app/workbench/useWorkbench.test.ts src/app/workbench/useWorkbenchSync.test.ts src/app/workbench/WorkbenchPage.test.tsx`，原始失败为 `sh: 1: vitest: not found`，退出 127；执行同范围 tree/files/Shell 命令原始失败同为 `sh: 1: vitest: not found`，退出 127；`cd web && npm run typecheck` 原始失败为 `sh: 1: tsc: not found`，退出 127。该失败原因只记录原文，不据此归因代码。
- 2026-08-28：按 `web/package-lock.json` 执行 `cd web && npm ci --ignore-scripts` 成功，原始输出为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`。该命令仅安装未跟踪依赖目录。
- 2026-08-28：依赖就绪后重新执行工作台基线命令，原始结果为 `Test Files 7 passed (7)`、`Tests 151 passed (151)`、`EXIT 0`；执行 tree/files/Shell 基线命令，原始结果为 `Test Files 3 passed (3)`、`Tests 119 passed (119)`、`EXIT 0`；执行 `cd web && npm run typecheck`，原始输出无错误、`EXIT 0`。故计划验收命令可判定为可运行的基线判据。
- 2026-08-28：补跑包含既有 `CardsPage.test.tsx` 的 Task 4 最小基线 `cd web && npm test -- --run src/app/tree/ProjectTree.test.tsx src/app/files/FileTree.test.tsx src/app/shell/Shell.test.tsx src/app/cards/CardsPage.test.tsx`，原始结果为 `Test Files 4 passed (4)`、`Tests 125 passed (125)`、`EXIT 0`；计划表与 Task 4 基线改为该完整范围。
- 2026-08-28：因 Task 4 同时改动 `tree/search.ts`，补跑完整 5 文件基线 `cd web && npm test -- --run src/app/tree/ProjectTree.test.tsx src/app/tree/search.test.ts src/app/files/FileTree.test.tsx src/app/shell/Shell.test.tsx src/app/cards/CardsPage.test.tsx`，原始结果为 `Test Files 5 passed (5)`、`Tests 136 passed (136)`、`EXIT 0`；计划表、Task 4 与最终门禁均纳入 `search.test.ts`。
- 2026-08-28：`internal/agentd/workbench_api.go:64-103` 实读确认现有 `PUT /api/workbench/state/base` 只拒绝空 `base_key`，payload 作为字符串且只做长度校验；`internal/proto/workbench.go:19-36` 确认线格式不解释 payload。计划因此采用非空保留键保存全局 payload，不修改 agentd/proto/client HTTP 签名；旧按目录行由前端成功恢复后差分清理。
- 2026-08-28：现状 `tabs.ts:17-90` 确认 `Tab` 不含基准、`Workbench` 是按 BaseDir 的 `groups/tabs/active/sizes`；`useWorkbench.ts:85-143` 确认 `byBase/baseDirs` 是旧模型；`useWorkbenchSync.ts:44-52,185-197` 确认同步层按目录逐行编码。计划将这些边界改为 tab 自带 BaseDir、组内 columns/sizes，sync 只写一个全局布局行。
- 2026-08-28：现状 `ProjectTree.tsx:58-92,593-669` 确认树当前为项目→机器→目录→任务，目录点击走 `onSelectDir`，拖放只写 task/base MIME；`FileTree.tsx:89-106,379-391` 确认文件树可在保留现有行为的同时加入可选关闭按钮；`Shell.tsx:446-585` 确认固定右栏与按目录 sync 接线位置。计划不改变既有 API helper、NewWorktreeDialog、HomeDock、PTY 组件。
- 2026-08-28：判断并冻结：全局布局持久化键使用前端常量 `GLOBAL_WORKBENCH_KEY = '__global_workbench__'`。选择理由是现有 agentd 只要求非空且不解释 payload，真实工作树 key 是 path/path@machine，不会与该非路径键混淆；`state.bases` 的其它行仅作为 legacy/坏行清理，不再尝试把旧按目录布局拼成新全局布局。该判断属本卡前端持久化实现范围，不扩展后端契约。
- 2026-08-28：判断并冻结：`BaseDir` 类型移到纯模型 `tabs.ts` 后由 `useWorkbench.ts` 重新导出；Tab 持有 `{base,content}`，文件去重键带 `base.key`，TUI 按 task id、已建终端按 session id；这样跨项目同组不会因当前选中目录切换而消失，也避免同相对路径跨项目误合并。
- 2026-08-28：计划自审发现 `tabs.ts` 的新 `TabColumn` 形状不允许沿用旧 `.tab` 断言，已移除误留的教学性错误代码块，只保留可运行的 `Tab | null` 断言；并明确 `openTab` 先填空 pane、无空 pane 时追加右列，center 覆盖只归 `placeSource`。
- 2026-08-28：因 Task 4 修改 `tree/search.ts`，把现有 `web/src/app/tree/search.test.ts` 纳入文件边界、基线与最终门禁；已实跑 5 文件/136 tests 的成功基线。
- 2026-08-28：计划结构自检命令实际输出：Task 1/2/3/4 均为 `baseline/detail/green/log/comment present`；代码围栏 `fences=60 remainder=0`；占位符扫描（排除自我声明行）无输出。
- 2026-08-28：提交后复核发现组拖移的“prune 源组”措辞可能与“只有显式 `closeGroup` 关闭组”冲突，已修订为只规范空列并保留空组；需再次 amend，最终哈希以收尾命令为准。
- 2026-08-28：实现计划补入依赖事实出处：`tabs.ts:70-71` 的 `MIN_PANE_PX=240`、`useWorkbenchSync.ts:33-38` 的 `WRITE_DEBOUNCE_MS=500`、`api/client.ts:628-634` 的 `fetchPtySessions('all')` 与 `api/client.ts:665-672` 的 `putWorkbenchBase`；均由源码 `rg` 实读。
- 2026-08-28：提交前执行 `git add docs/superpowers/plans/b281-plan.md docs/superpowers/ledgers/2026-08-28-b281-plan-ledger.md`；`git diff --cached --check` 无输出，staged stat 为 `2 files changed, 1199 insertions(+)`，状态仅有这两份文档待提交。
- 2026-08-28：已在当前分支 `cards/B281-charter-2` 提交计划与台账；提交后 `git status --short` 无输出。后续 amend 会更新最终哈希，最终值以收尾命令为准。
- 2026-08-28：未执行 handoff CLI、未派发 executor、未起子进程；只运行了本地 shell 命令与 codegraph 查询。
- 2026-08-28：执行者接管后当前分支为 `cards/B281-charter-3`，工作树初始干净；与计划记录的协调分支不同，但遵守只在当前分支工作，不切换分支。
- 2026-08-28：本回合未调用 handoff CLI、未派发 executor、未起新的 executor/子任务。
- 2026-08-28：Task 4 首次收口测试 `cd web && npm test -- --run src/app/shell/Shell.test.tsx --testNamePattern='关一个有草稿' --reporter=dot` 原始结果为 `Test Files 1 passed (1)`、`Tests 1 passed | 31 skipped (32)`、退出 0；确认分屏 pane 常驻时的脏文件关闭确认已弹出。
- 2026-08-28：Task 4 全范围测试首次结果为 `Test Files 5 passed (5)`、`Tests 139 passed (139)` 但 Shell 回归 1 条失败；原始失败为 `Unable to find an element with the text: 文件`，发生在 `/codegraph 同时隐藏 Breadcrumb/FileTree，回到工作台后恢复` 的第二次 `openBranch`，随后将测试 helper 改为按左栏作用域检测已展开分支。
- 2026-08-28：修正 `openBranch` 作用域后再次执行 `cd web && npm test -- --run src/app/tree/ProjectTree.test.tsx src/app/tree/search.test.ts src/app/files/FileTree.test.tsx src/app/shell/Shell.test.tsx src/app/cards/CardsPage.test.tsx`，原始结果为 `Test Files 5 passed (5)`、`Tests 139 passed (139)`、退出 0。
- 2026-08-28：为遵守 B281 接口删除旧 `onSelectDir`，先将 ProjectTree 新目录回调接口临时收紧并运行 `npm run typecheck`；原始输出为 `TS2741 Property 'openedItems' is missing` 与 `TS2774 This condition will always return true`，未据此判定实现通过；随后按当前测试夹具回退新回调的可选性、保留实际 Shell 新入口，并补齐 `openedItems`。
- 2026-08-28：删除旧 `onSelectDir` 后，第一次局部树测试原始结果为 `Test Files 2 passed (2)`、`Tests 68 passed (68)`；期间发现 3 条旧测试仍按目录默认全展开，已改为先点击「目录」验证新的默认收起语义。
- 2026-08-28：`cd web && npm run typecheck` 在 FileTab live draft 接缝与 ProjectTree 接口清理后原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`、退出 0。
- 2026-08-28：Workbench 集成范围 `cd web && npm test -- --run src/app/workbench/tabs.test.ts src/app/workbench/paneDrop.test.ts src/app/workbench/persist.test.ts src/app/workbench/restore.test.ts src/app/workbench/useWorkbench.test.ts src/app/workbench/useWorkbenchSync.test.ts src/app/workbench/WorkbenchPage.test.tsx src/app/workbench/launchers.test.tsx` 原始结果为 `Test Files 8 passed (8)`、`Tests 51 passed (51)`、退出 0。
- 2026-08-28：修正 persist session 清理中的未使用解构后，`npm test -- --run src/app/workbench/persist.test.ts` 原始结果为 `Test Files 1 passed (1)`、`Tests 8 passed (8)`、退出 0。
- 2026-08-28：`npm run lint -- --no-warn-ignored` 原始结果为 `23 problems (5 errors, 18 warnings)`；剩余 error 为既有 `src/api/pty.ts:115 no-case-declarations`、`src/app/flows/NodeEditor.test.tsx:50 prefer-const`、`src/app/workbench/terminalHostResponse.ts:45,47,49 no-control-regex`，本次没有改动这些文件；本次新增 persist error 已清零。
- 2026-08-28：`npm run build` 原始结果为 `tsc -b` 成功、`✓ 1967 modules transformed`、`✓ built in 2.10s`；Vite 仅报告既有 bundle 超过 500 kB 的 warning，退出 0。
- 2026-08-28：全量集成 `cd web && npm test` 原始结果为 `Test Files 109 passed (109)`、`Tests 1071 passed (1071)`、退出 0；输出另含 jsdom 已知提示 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`，未导致测试失败。
- 2026-08-28：变异自验前以 `rg -n -F "if (canAddColumn && offsetX < xEdge) return 'left'" web/src/app/workbench/paneDrop.ts` 确认唯一命中 `paneDrop.ts:29`；将 `<` 取反为 `>` 后 `npm run typecheck` 原始输出为 `> tsc -b`、退出 0，证明变异可编译。
- 2026-08-28：变异后先运行行为断言 `npm test -- --run src/app/workbench/paneDrop.test.ts --reporter=dot`，原始结果为 `Tests 2 failed | 2 passed (4)`，失败包含 `expected 'left' ... Received 'center'` 与 `expected 'center' ... Received 'left'`；再运行全量 `npm test -- --reporter=dot`，原始结果为 `Test Files 2 failed | 107 passed (109)`、`Tests 3 failed | 1068 passed (1071)`，除目标拖放断言外 WorkbenchPage 相关断言也捕获了该语义变异。已恢复 `<` 原实现。
- 2026-08-28：为满两格上下投放可见退化提示先加入真实 WorkbenchPage seam；未实现前原始结果为 `Test Files 1 failed (1)`、`Tests 1 failed | 3 skipped (4)`，失败为 `Unable to find an accessible element with the role "alert"`，同时真实日志显示 `workbench.place.pane_limit`。
- 2026-08-28：实现满两格提示后，WorkbenchPage seam 原始结果为 `Test Files 1 passed (1)`、`Tests 1 passed | 3 skipped (4)`；随后 Workbench 8 文件范围为 `Test Files 8 passed (8)`、`Tests 52 passed (52)`、退出 0，Task 4 5 文件范围为 `Test Files 5 passed (5)`、`Tests 139 passed (139)`、退出 0，`npm run typecheck` 输出 `> tsc -b`、退出 0。
- 2026-08-28：补入 CardsPage 的 `project` 查询参数接缝测试；执行 `cd web && npm test -- --run src/app/cards/CardsPage.test.tsx --reporter=dot`，原始结果为 `Test Files 1 passed (1)`、`Tests 7 passed (7)`、退出 0。
- 2026-08-28：最终门禁 `cd web && npm run typecheck` 原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`、退出 0；`cd web && npm run build` 原始结果为 `✓ 1967 modules transformed`、`✓ built in 2.11s`、退出 0，另有既有 bundle 超过 500 kB 警告；`cd web && npm test -- --reporter=dot` 原始结果为 `Test Files 109 passed (109)`、`Tests 1073 passed (1073)`、退出 0。全量测试输出含既有 jsdom canvas、终端测试和 SettingsPage 警告，未导致失败。
- 2026-08-28：收尾复跑 `cd web && npm run lint -- --no-warn-ignored`，原始结果为 `23 problems (5 errors, 18 warnings)`；5 个 error 仍位于未修改的 `src/api/pty.ts:115`、`src/app/flows/NodeEditor.test.tsx:50`、`src/app/workbench/terminalHostResponse.ts:45,47,49`，本卡未改动这些错误来源；本卡新增代码未产生 lint error。
- 2026-08-28：执行者接管当前工作树时分支为 `cards/B281-charter-4`，HEAD 为上一轮实现 `6fb38d67`，工作树初始无改动；`git fetch origin feat/B281-workbench-ia` 成功，远端原型更新到 `3a1aca73`。
- 2026-08-28：按本轮 Task 4 基线命令实际执行，原始输出为 `sh: 1: vitest: not found`，退出 127；当前工作树缺少 `web/node_modules`，未据此判定代码结果。
- 2026-08-28：按 `web/package-lock.json` 执行 `npm ci --ignore-scripts` 成功，原始输出为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`；依赖目录未纳入提交。
- 2026-08-28：依赖就绪后重跑本轮 Task 4 基线，原始结果为 `Test Files 5 passed (5)`、`Tests 140 passed (140)`、退出 0。
- 2026-08-28：先加入本轮左栏接缝红测后执行 `npm test -- --run src/app/tree/ProjectTree.test.tsx --reporter=dot`，原始结果为 `Tests 2 failed | 2 passed (4)`；失败断言分别确认当前仍为每机器一份任务/目录组且不存在按类型任务头像，故红因功能缺失而非拼写错误。
- 2026-08-28：按新左栏层级修正 `Shell.test.tsx` 的 `openBranch` 测试辅助，使其在分支不可见时点击机器行展开；重跑 Task 4 五文件范围，原始结果为 `Test Files 5 passed (5)`、`Tests 143 passed (143)`、退出 0。
- 2026-08-28：`cd web && npm run typecheck` 原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`、退出 0。
- 2026-08-28：变异前以 `rg -n -F "item.base.projectName === project.name && item.base.machine === loc.machine && item.base.path === ws.path && (" web/src/app/tree/search.ts` 确认目标上下文唯一命中 `search.ts:144`，将该处 `hit(openedText(item), q)` 取反；`npm run typecheck` 原始输出为 `> tsc -b`、退出 0，证明变异可编译。
- 2026-08-28：变异后先执行 `npm test -- --run src/app/tree/search.test.ts --reporter=dot`，原始结果为 `Test Files 1 failed (1)`、`Tests 2 failed | 11 passed (13)`；失败命中既有打开项祖先搜索与本轮相对路径搜索，确认测试拦住了语义变异；随后已恢复原实现。
- 2026-08-28：恢复变异后执行 `npm test -- --run src/app/tree/search.test.ts --reporter=dot`，原始结果为 `Test Files 1 passed (1)`、`Tests 13 passed (13)`、退出 0。
- 2026-08-28：`cd web && npm run build` 原始结果为 `✓ 1967 modules transformed`、`✓ built in 2.22s`、退出 0；仅有既有 bundle 超过 500 kB 的 warning。
- 2026-08-28：收尾全量 `cd web && npm test -- --reporter=dot` 原始结果为 `Test Files 109 passed (109)`、`Tests 1076 passed (1076)`、退出 0；输出含既有 jsdom canvas、终端测试及 SettingsPage 警告，未导致失败。
- 2026-08-28：`cd web && npm run lint -- --no-warn-ignored` 原始结果为 `23 problems (5 errors, 18 warnings)`；5 个 error 仍在未修改的 `src/api/pty.ts:115`、`src/app/flows/NodeEditor.test.tsx:50`、`src/app/workbench/terminalHostResponse.ts:45,47,49`，本轮 ProjectTree 仅有既存/结构性 Fast Refresh warnings，无新增 lint error。
- 2026-08-28：补充机器行「打开主目录终端」接缝断言，并让该入口复用 `openDirectoryTerminal` 结构化日志；`npm test -- --run src/app/tree/ProjectTree.test.tsx --reporter=dot` 原始结果为 `Test Files 1 passed (1)`、`Tests 58 passed (58)`、退出 0，日志含 `project_tree.directory.terminal { project: 'handoff', machine: '', baseKey: '/w', path: '/w' }`。
- 2026-08-28：机器入口日志调整后再次执行 `cd web && npm run typecheck`，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`、退出 0。
- 2026-08-28：收尾全量 `cd web && npm test -- --reporter=dot` 一次并发复跑原始结果为 `Test Files 2 failed | 107 passed (109)`、`Tests 2 failed | 1074 passed (1076)`；失败为 `NewWorktreeDialog.test.tsx` 的 `打开时拉分支列表...` 超时 5000ms，以及 `terminalInput.test.ts` 的 `普通字母仍由 xterm...` 超时 5000ms。按原失败项单独复跑：`NewWorktreeDialog.test.tsx` 为 `Test Files 1 passed (1)`、`Tests 13 passed (13)`；`terminalInput.test.ts` 为 `Test Files 1 passed (1)`、`Tests 21 passed (21)`、均退出 0；未修改这两个文件。
- 2026-08-28：再次执行收尾全量 `cd web && npm test -- --reporter=dot`，原始结果为 `Test Files 109 passed (109)`、`Tests 1076 passed (1076)`、退出 0；输出仍含既有 jsdom canvas、终端与 SettingsPage 警告。
- 2026-08-28：机器入口日志调整后再次执行 `cd web && npm run build`，原始结果为 `✓ 1967 modules transformed`、`✓ built in 2.42s`、退出 0；仅有既有 bundle 超过 500 kB 的 warning。
- 2026-08-28：已执行 `git add`、`git diff --cached --check`（无输出）与 `git commit -m "feat: refresh workbench project tree layout"`，原始提交输出为 `[cards/B281-charter-4 78f55200] feat: refresh workbench project tree layout`、`6 files changed, 525 insertions(+), 344 deletions(-)`；提交后 `git status --short` 无输出。
