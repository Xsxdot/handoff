# B281 plan 节点台账

- 2026-08-28：当前分支为 `cards/B281-charter-2`；`git status --short` 初始无输出。遵守本节点只写计划与台账，不改实现、不切分支、不 push。
- 2026-08-28：执行 `git fetch origin feat/B281-workbench-ia` 成功，原始输出：`From github.com:Xsxdot/handoff`、`* branch feat/B281-workbench-ia -> FETCH_HEAD`、`3e07208f..7e13dd0b feat/B281-workbench-ia -> origin/feat/B281-workbench-ia`。原型实际从 `origin/feat/B281-workbench-ia:prototypes/b264-tab-groups/index.html` 读取，未复制进实现目录。
- 2026-08-28：原型 `index.html` 实际核对：CSS 第 40-65 行是 `.cols` 横向列、每列最多两格、窗格四向/中间投放；HTML 第 198-208 行是面包屑、全局组栏、布局条、中央列与可隐藏文件抽屉；脚本第 397-460 行是全局查找/新组/目录终端与 payload；第 485-577 行是四向投放、列最多两格、加空列与布局形状；第 626-652 行是可关闭文件抽屉；第 654-767 行是项目→机器→任务/目录双同级组；第 769-801 行是全局组栏；第 821-872 行是列/窗格渲染。原型 `g4` 形状（第 564-567 行）与 B281 明确弃选六/五宫格的边界冲突，计划只吸收列/两格语义，不实现固定四宫格按钮。
- 2026-08-28：完整读取 `docs/superpowers/specs/b281.md`。冻结行为为：全局标签组、不同项目同组、左右加列、一列最多上下两格、中间替换、目录拖放用该目录 cwd、文件抽屉可关闭、整页路由覆盖但不卸载终端；不改 agentd/PTY/账本/跨机同步协议，不把原型 HTML 当实现。
- 2026-08-28：代码图命令 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_web_workbench` 成功。输出确认 best 领域 `d_web_workbench`，包为 `web/src/app/files`、`homedock`、`tree`、`workbench`；`fociTruncated.total=10, shown=5`；`actual.byCurrentDomain` 显示 8 个容器实际在 `d_web`；8 个 `k_web_app_*` 容器列入 `actual.misplaced`；`projScanned=false` 的实体警告表示不能把图当投影清单。该覆盖债不阻断本计划，因为本卡只改已实读的 d_web 前端文件，跨领域依赖以源码/测试复核。
- 2026-08-28：代码图 `sym n_web_app_workbench_useWorkbench` 成功，命中 `web/src/app/workbench/useWorkbench.ts:145`；`who-calls n_web_app_workbench_useWorkbench --with-source` 成功，确认 `Shell` 是图内调用方，并报告既有 `unscannedEntries=6`。`sym n_web_app_workbench_WorkbenchPage` 命中 `WorkbenchPage.tsx:162`，但 `who-calls` 只有自身节点且提示 6 个未扫描入口；`sym/who-calls n_web_app_tree_ProjectTree` 命中 `ProjectTree.tsx:232`，同样提示未扫描入口。未命中的调用面通过 `rg` 复核并记录为图覆盖债。
- 2026-08-28：首次在无 `web/node_modules` 的基线执行 `cd web && npm test -- --run src/app/workbench/tabs.test.ts src/app/workbench/paneDrop.test.ts src/app/workbench/persist.test.ts src/app/workbench/restore.test.ts src/app/workbench/useWorkbench.test.ts src/app/workbench/useWorkbenchSync.test.ts src/app/workbench/WorkbenchPage.test.tsx`，原始失败为 `sh: 1: vitest: not found`，退出 127；执行同范围 tree/files/Shell 命令原始失败同为 `sh: 1: vitest: not found`，退出 127；`cd web && npm run typecheck` 原始失败为 `sh: 1: tsc: not found`，退出 127。该失败原因只记录原文，不据此归因代码。
- 2026-08-28：按 `web/package-lock.json` 执行 `cd web && npm ci --ignore-scripts` 成功，原始输出为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`。该命令仅安装未跟踪依赖目录。
- 2026-08-28：依赖就绪后重新执行工作台基线命令，原始结果为 `Test Files 7 passed (7)`、`Tests 151 passed (151)`、`EXIT 0`；执行 tree/files/Shell 基线命令，原始结果为 `Test Files 3 passed (3)`、`Tests 119 passed (119)`、`EXIT 0`；执行 `cd web && npm run typecheck`，原始输出无错误、`EXIT 0`。故计划验收命令可判定为可运行的基线判据。
- 2026-08-28：补跑包含既有 `CardsPage.test.tsx` 的 Task 4 最小基线 `cd web && npm test -- --run src/app/tree/ProjectTree.test.tsx src/app/files/FileTree.test.tsx src/app/shell/Shell.test.tsx src/app/cards/CardsPage.test.tsx`，原始结果为 `Test Files 4 passed (4)`、`Tests 125 passed (125)`、`EXIT 0`；计划表与 Task 4 基线改为该完整范围。
- 2026-08-28：因 Task 4 同时改动 `tree/search.ts`，补跑完整 5 文件基线 `cd web && npm test -- --run src/app/tree/ProjectTree.test.tsx src/app/tree/search.test.ts src/app/files/FileTree.test.tsx src/app/shell/Shell.test.tsx src/app/cards/CardsPage.test.tsx`，原始结果为 `Test Files 5 passed (5)`、`Tests 136 passed (136)`、`EXIT 0`；计划表、Task 4 与最终门禁均纳入 `search.test.ts`。
- 2026-08-28：`internal/agentd/workbench_api.go:64-103` 实读确认现有 `PUT /api/workbench/state/base` 只拒绝空 `base_key`，payload 作为字符串且只做长度校验；`internal/proto/workbench.go:19-36` 确认线格式不解释 payload。计划因此采用非空保留键保存全局 payload，不修改 agentd/proto/client HTTP 签名；旧按目录行由前端成功恢复后差分清理。
- 2026-08-28：现状 `tabs.ts:17-90` 确认 `Tab` 不含基准、`Workbench` 是按 BaseDir 的 `groups/tabs/active/sizes`；`useWorkbench.ts:85-143` 确认 `byBase/baseDirs` 是旧模型；`useWorkbenchSync.ts:44-52,185-197` 确认同步层按目录逐行编码。计划将这些边界改为 tab 自带 BaseDir、组内 columns/sizes，sync 只写一个全局布局行。
- 2026-08-28：现状 `ProjectTree.tsx:58-92,593-669` 确认树当前为项目→机器→目录→任务，目录点击走 `onSelectDir`，拖放只写 task/base MIME；`FileTree.tsx:89-106,379-391` 确认文件树可在保留现有行为的同时加入可选关闭按钮；`Shell.tsx:446-585` 确认固定右栏与按目录 sync 接线位置。计划不改变既有 API helper、NewWorktreeDialog、HomeDock、PTY 组件。
- 2026-08-28：判断并冻结：全局布局持久化键使用前端常量 `GLOBAL_WORKBENCH_KEY = '__global_workbench__'`。选择理由是现有 agentd 只要求非空且不解释 payload，真实工作树 key 是 path/path@machine，不会与该非路径键混淆；`state.bases` 的其它行仅作为 legacy/坏行清理，不再尝试把旧按目录布局拼成新全局布局。该判断属本卡前端持久化实现范围，不扩展后端契约。
- 2026-08-28：判断并冻结：`BaseDir` 类型移到纯模型 `tabs.ts` 后由 `useWorkbench.ts` 重新导出；Tab 持有 `{base,content}`，文件去重键带 `base.key`，TUI 按 task id、已建终端按 session id；这样跨项目同组不会因当前选中目录切换而消失，也避免同相对路径跨项目误合并。
- 2026-08-28：计划自审发现 `tabs.ts` 的新 `TabColumn` 形状不允许沿用旧 `.tab` 断言，已移除误留的教学性错误代码块，只保留可运行的 `Tab | null` 断言；并明确 `openTab` 先填空 pane、无空 pane 时追加右列，center 覆盖只归 `placeSource`。
- 2026-08-28：因 Task 4 修改 `tree/search.ts`，把现有 `web/src/app/tree/search.test.ts` 纳入文件边界、基线与最终门禁；已实跑 5 文件/136 tests 的成功基线。
- 2026-08-28：计划结构自检命令实际输出：Task 1/2/3/4 均为 `baseline/detail/green/log/comment present`；代码围栏 `fences=60 remainder=0`；占位符扫描（排除自我声明行）无输出。
- 2026-08-28：提交后复核发现组拖移的“prune 源组”措辞可能与“只有显式 `closeGroup` 关闭组”冲突，已修订为只规范空列并保留空组；需再次 amend，最终哈希以收尾命令为准。
- 2026-08-28：实现计划补入依赖事实出处：`tabs.ts:70-71` 的 `MIN_PANE_PX=240`、`useWorkbenchSync.ts:33-38` 的 `WRITE_DEBOUNCE_MS=500`、`api/client.ts:628-634` 的 `fetchPtySessions('all')` 与 `api/client.ts:665-672` 的 `putWorkbenchBase`；均由源码 `rg` 实读。
- 2026-08-28：提交前执行 `git add docs/superpowers/plans/b281-plan.md docs/superpowers/ledgers/2026-08-28-b281-plan-ledger.md`；`git diff --cached --check` 无输出，staged stat 为 `2 files changed, 1199 insertions(+)`，状态仅有这两份文档待提交。
- 2026-08-28：已在当前分支 `cards/B281-charter-2` 提交计划与台账；提交后 `git status --short` 无输出。后续 amend 会更新最终哈希，最终值以收尾命令为准。
- 2026-08-28：未执行 handoff CLI、未派发 executor、未起子进程；只运行了本地 shell 命令与 codegraph 查询。
