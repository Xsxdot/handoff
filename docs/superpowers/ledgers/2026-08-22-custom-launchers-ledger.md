# 需求 B：工作台自定义启动项执行 ledger

基线：`74fdb762` 之后的任务分支 HEAD `aba1248c`。

## 执行记录

- 2026-08-22：基线核验完成。工作区干净；`OpenOptions.InitCommand`、启动项契约类型、能力位类型均已存在。B1 开始；尚未提交。
- 2026-08-22：Task 1 B1 完成，改动 `internal/ptyhost/engine/engine.go`、`initcmd_test.go`。失败测试实测：`go test ./internal/ptyhost/engine/ -run TestInitCommand 2>&1 | tail -20` 返回 3 条非空命令失败、空命令守卫通过。实现后 `gofmt -l internal/ptyhost/` 无输出；`go test ./internal/ptyhost/engine/ 2>&1 | tail -30` 输出 `ok github.com/Xsxdot/handoff/internal/ptyhost/engine`；`GOOS=windows go build ./... && echo "windows 构建通过"` 输出 `windows 构建通过`。双裁决通过：spec 覆盖首字节/3 秒兜底/空命令兼容/Q4 原文加换行/无命令日志；代码质量通过：`firstOut` 只由 pump 观测、写入错误不告警、平台文件未改。Task 1 commit 范围：本行、B1 实现与测试。
- 2026-08-22：Task 2 B2 完成，改动 `internal/agentd/launchers_api.go`、`launchers_api_test.go`、`server.go`。失败测试实测：`go test ./internal/agentd/ -run TestLaunchers 2>&1 | tail -20` 在 handler 接入前返回各用例 404。实现后 targeted 输出 `ok github.com/Xsxdot/handoff/internal/agentd 2.163s`；完整 `go test ./internal/agentd/ 2>&1 | tail -10` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 125.810s`；`git diff --check` 无输出。Task 2 测试采用既有 `newTestAgentdEnvWithCfg`/`doJSON` 搭台，未新造 HTTP 服务；双裁决通过：spec 覆盖 CRUD、五档校验、派生字段双向与忽略客户端字段、空列表、日志脱敏；代码质量通过：跨机复用 `forwardIfRequested`，GET 回非 nil 列表，错误保留真因。Task 2 commit 范围：本行、B2 handler/测试/路由清单。
- 2026-08-22：Task 3 B3 完成，改动 `internal/agentd/pty_api.go`、`server.go`、`machines.go`、`pty_launcher_test.go`，以及实际 out-of-process 链所需的 `internal/ptyhost/client.go`、`hostproc/hostproc.go` InitCommand 序列化桥。先确认 `ptyhost.Host.Open` 写 `launchSpec`、hostproc 再建 engine；若不补桥，agentd 字段会静默丢失。测试最终采用外部效果路线而非断言 Open 内部入参：真实 agentd→ptyhost→假 shell 观察 TERM、env 文件变量、InitCommand 回显与 env 文件覆盖 sessionEnv 的最后出现；缺失文件用会话列表前后长度反向断言，合法文件作正面对照；能力位用白盒 `handleStatus`/`localMachine`/`fillFromStatus` 断言。失败测试编译/运行前未单独保存，已在实现后实测 targeted `go test ./internal/agentd/ -run 'Test(CreatePtySession|StatusReportsLaunchers|LocalMachineReportsLaunchers|FillFromStatusCarriesLaunchers)' 2>&1 | tail -40` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 3.349s`；完整 `go test ./internal/agentd/ 2>&1 | tail -10` 输出 `ok github.com/Xsxdot/handoff/internal/agentd 120.178s`；`go test ./internal/ptyhost/... 2>&1 | tail -20` 五包全部 `ok`；`git diff --check` 无输出。双裁决通过：spec 覆盖空字段兼容、env/命令单独与同时、最后变量覆盖、400 不建会话、ErrBadName、三处能力位和 InitCommand 脱敏日志；代码质量通过：新增桥接字段为 `omitempty`，不改对外签名，非 unix 测试按 build tag 排除。Task 3 commit 范围：本行、B3 agentd/ptyhost 桥与测试。
- 2026-08-22：Task 4 B4 完成，改动 `web/src/api/client.ts`、`MachineLaunchers.tsx`、`MachineLaunchers.test.tsx`、`MachineDetail.tsx`。先跑失败测试时环境原始输出为 `npm error EROFS ... /root/.npm/_cacache/tmp/***` 且工作区没有 vitest/tsc；随后 `npm ci` 输出 `added 290 packages ... found 0 vulnerabilities`。实现后 `npx vitest run src/app/machines src/api 2>&1 | tail -25` 输出 11 个测试文件、103 个测试全部通过；`npx tsc -b && echo "tsc ok"` 输出 `tsc ok`；`npx eslint src/app/machines src/api` 无输出；`git diff --check` 无输出。双裁决通过：spec 覆盖 API GET/PUT、Env 文件下拉、空名字/双空前端实话校验、env_missing 标注、服务端返回值刷新、true/false/undefined 三态门，且断开机器不发请求；代码质量通过：能力门在无 hooks 的外层 wrapper，错误原文展示，无命令日志。Task 4 commit 范围：本行、B4 API/组件/测试/详情页。
- 2026-08-22：Task 5 B5 完成，改动 `useMachineCaps.ts`/测试、`Shell.tsx`、`BlankTab.tsx`、`TabBar.tsx`、`WorkbenchPage.tsx`、`TerminalTab.tsx`/测试、`tabs.ts`/测试、`persist.ts`/测试，以及新增工作台启动项入口测试。失败测试先实测命中启动项请求字段、persist 三处丢 launcher、标题仍为 bash 的红灯；实现后 `cd web && npx vitest run src/app/workbench src/app/data 2>&1 | tail -35` 输出 20 个测试文件、261 个测试全部通过；`npx tsc -b` 通过；`npx eslint src/app/workbench src/app/data src/app/shell/Shell.tsx` 无 error（仅既有 Shell 恢复 effect 与既有 Fast Refresh warning）；`git diff --check` 无输出。悬置决定：①Task 3 已采用 ledger 上一条所记的真实 agentd→ptyhost→假 shell 外部效果断言；②Task 5(b) 选择在 `wb.base.machine` 变化时由 Shell 的 `useEffect` 调 `launchersState.refresh()`，避免 enabled 未变化时沿用旧机列表；③Task 5(e) `stripTab`、`parseContent`、`pruneDeadSessions` 三处均保留 `launcher`，并有真实 `encodeBase→decodeBase` 用例及死会话清理用例覆盖。双裁决通过：Q1 独立 `onPickLauncher` 保持 PickKind 闭集，Q2 轮询只在能力位明确 true 时启用，Q3 启动项 tab 标题用名字，Q4 请求字段缺席时保持历史形态；启动项查不到具体配置时 TerminalTab 不传 env/init 字段，按普通终端创建。Task 5 commit 范围：本行、B5 web 工作台实现与测试。
