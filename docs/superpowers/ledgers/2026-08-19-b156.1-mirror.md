# B156.1 事件镜像与多路 wait 执行 ledger

- Baseline: `a3310821`; `go build ./...` passed. Baseline `go test ./...` hit the raw environment error `mktemp: mkstemp failed on /var/folders/xc/hpx9c9w153j7tvphw53lc8qr0000gn/T/tmp.PQCyqPNSZO: Operation not permitted` in `TestInstallScriptUnits`.
- Task 1 completed: added ledger mirror primitives for idempotent source-triple writes, derived watermark, lease CAS, health heartbeat, and MaxSeq; stored the original DSN for PG LISTEN. Task-specific `go test ./internal/ledger/ -v` passed; commit scope: `internal/ledger/store.go internal/ledger/mirror.go internal/ledger/mirror_test.go docs/superpowers/ledgers/2026-08-19-b156.1-mirror.md`.
- Task 1 repair round 1: made `AppendMirroredEvent` report `wrote=true` only after the enclosing transaction commits; reran the mirror-focused tests and they passed; commit scope: `internal/ledger/mirror.go`.
- Task 1 repair round 2: corrected the post-commit result to preserve the inserted-versus-idempotent distinction; the first rerun exposed `重放应静默跳过: <nil> true`, so this round remains pending verification; commit scope: `internal/ledger/mirror.go`.
- Task 2 completed: added `Follow` with SQLite polling, PG LISTEN wakeups plus table-backed seq reads, dynamic membership reevaluation, and cancellation/error propagation; `go test ./internal/ledger/` and `go vet ./internal/ledger/` passed; commit scope: `internal/ledger/follow.go internal/ledger/follow_test.go docs/superpowers/ledgers/2026-08-19-b156.1-mirror.md`.
- Task 3 repair round 1: made `Mirror.Stop` cancel and await the mirror run loop, preventing `database is closed` lease operations during test cleanup; simplified subscription lookup; pending rerun; commit scope: `internal/ledgermirror/mirror.go`.
- Task 3 completed: added `internal/ledgermirror` lease arbitration, card-task reconciliation, per-task source subscriptions with filtering/backoff/idempotent writes, terminal unsubscribe, and target health rules; added `AllTaskLinks`; `go test -race ./internal/ledgermirror/ ./internal/ledger/` and vet passed; commit scope: `internal/ledgermirror/mirror.go internal/ledgermirror/mirror_test.go internal/ledger/tasks.go internal/ledger/tasks_test.go docs/superpowers/ledgers/2026-08-19-b156.1-mirror.md`.
- Task 3 repair round 2: made `Mirror.Stop` race-safe when startup or shutdown occurs before `Run` publishes its cancellation handle; canceled-before-start runs now exit before touching the ledger; pending verification; commit scope: `internal/ledgermirror/mirror.go`.
- Task 3 repair round 3: serialized subscription `WaitGroup.Add` with `Stop` waiting, and blocked post-cancel re-reconciliation; the race run exposed concurrent `Add`/`Wait` plus a post-Stop `起订`; pending verification; commit scope: `internal/ledgermirror/mirror.go`.
- Task 3 repair round 3 verified and committed: `go test -race ./internal/ledgermirror/ ./internal/ledger/` and vet passed; commit scope: `internal/ledgermirror/mirror.go docs/superpowers/ledgers/2026-08-19-b156.1-mirror.md`.
- Task 3 repair round 3 test coverage committed: added `TestMirrorStopBeforeRun` to lock the startup-order guarantee; commit scope: `internal/ledgermirror/mirror_test.go docs/superpowers/ledgers/2026-08-19-b156.1-mirror.md`.
- Task 4 completed: mounted the ledger mirror after the existing web-console mirror, opened/seeds the configured ledger with SQLite fallback, hot-read target configuration, and registered Stop before ledger Close; `go build ./...`, `go test ./cmd/ 2>&1 | tail -5`, and `go vet ./cmd/` passed; commit scope: `cmd/agentd.go docs/superpowers/ledgers/2026-08-19-b156.1-mirror.md`.
- Task 5 completed: added `wait --card [--subtree]` ledger single-stream waiting with dynamic membership, per-event JSON output, all-terminal exit, timeout 124, and task-argument conflict validation; `go test ./cmd/` and `go vet ./cmd/` passed; commit scope: `cmd/wait.go cmd/wait_card_test.go docs/superpowers/ledgers/2026-08-19-b156.1-mirror.md`.
- Task 6 completed: audited the full diff from `a3310821`; no spec or quality findings remained. `gofmt -l`, vet, `go build ./...`, changed-package tests, and race tests passed. Full `go test ./...` was attempted in sandbox, elevated mode, and with a short `TMPDIR`; the remaining raw failures are pre-existing environment-sensitive `TestInstallScriptUnits` mktemp permission and four `internal/executor/claudecode` Unix socket path-length tests; commit scope: `docs/superpowers/ledgers/2026-08-19-b156.1-mirror.md`.
