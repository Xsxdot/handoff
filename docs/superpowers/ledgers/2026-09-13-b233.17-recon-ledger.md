# B233.17 recon 台账（2026-09-13）

节点：`charter:recon`（图对账：组装点收窄与编译期封界）
待对账分支：`cards/B233.17-charter-5` @ `7d846fed6`（工作分支实际为 `cards/B233.17-charter-6`，同 HEAD）
有效基线：`cards/B233.1-charter-7` @ 契约头部 `686d87abf`；本分支视图 base `9d785ae5`
契约视图：`codegraph/diffs/cards-B233.17-charter-2.json`
本节点工作树：`/root/.handoff/worktrees/b0cafb18`，HEAD `7d846fed6974f32f3b84d6bd8b8315fd7d606013`
只读不提交业务代码；本节点只改 `codegraph/diffs/cards-B233.17-charter-2.json`。

## 0. 无图判定

仓库有 `codegraph/` 目录，进入对账流程。

## 1. 改动面（BASE..HEAD）

BASE = 本分支基线 `686d87ab`（git merge-base HEAD 该提交即 `94246fc8`；契约头部 C-4 更正后的有效基线为 `686d87ab`，本节点以契约冻结点为准取 `686d87ab`）。

`git diff --name-status 686d87ab..7d846fed6` 生产 `.go`：

```text
M internal/agentd/server.go            （handler 迁出，瘦身）
A internal/agentd/handlers.go          （39 个 Server.handle*/helper/DTO 新定义点）
M internal/approval/client.go          （裁决映射改走 nested decision）
A internal/approval/internal/decision/decision.go
M internal/orchestration/cachegc.go     （纯路径逻辑迁 nested cacheplan）
A internal/orchestration/internal/cacheplan/cacheplan.go
M internal/orchestration/gc.go
M internal/scheduling/status.go
A internal/scheduling/internal/logging/logging.go
M internal/workspace/gitworkspace.go    （shell/进程组原语改走 nested gitproc）
R internal/workspace/runshell.go        → internal/workspace/internal/gitproc/runshell.go
R internal/workspace/workspace_procgroup_unix.go  → internal/workspace/internal/gitproc/procgroup_unix.go
R internal/workspace/workspace_procgroup_other.go → internal/workspace/internal/gitproc/procgroup_other.go
```

## 2. 逐项对账结论

### 2.1 handler 迁出（缝 1）

基线把 39 个 `Server.handle*`/同段 helper/DTO 锚在 `internal/agentd/server.go`；本分支物理迁到 `internal/agentd/handlers.go`。
**补**：视图 `nodesModified` 把这 39 节点 `file` 改 `internal/agentd/handlers.go`，`line` 逐行按新文件 `func`/`type` 声明行。

`Server.SetManager` 仍留 `server.go`（插线），未改锚——协调者口径明确。

新增 `Server.releaseTaskCarrierOccupancy`（`handlers.go:998`，基线未收录）：**补** `nodesAdded`（`k_agentd_Server`）。

### 2.2 四包 nested（缝 2）

- `workspace`：`runShell`/`resolveRunShell`/`runShellCandidates`/`dedupStrings`/`statFile` 迁 `internal/workspace/internal/gitproc/runshell.go`，`setProcGroup` unix/other 两实现迁 `procgroup_{unix,other}.go`。**补**：7 节点 `nodesModified` 改锚，其中两个同名 `setProcGroup` 按基线 id `n_workspace_setProcGroup`/`_2` 分别对到 unix/other 文件。
- `orchestration`：`cacheActiveLeaf` 等 9 函数 + `cacheLeafPlan` 类型迁 `internal/orchestration/internal/cacheplan/cacheplan.go`（新导出名 `ActiveLeaf`/`LegacyLeaf`/…/`PlanTaskCacheLeaves`/`SumRegularFileBytes`/`LeafPlan`）。**补**：该 10 节点（视图原由 B233.13 引入）`file`/`line`/`name`/`signature` 改锚 nested。
- `approval`：`MapGateAnswer`/`GateDecision`/`PermEventText` 迁 `internal/approval/internal/decision/decision.go`（新导出）。**补**：新增容器 `k_approval_decision`（domain `d_coordination_task`）+ 3 节点。
- `scheduling`：`StatusLog` 迁 `internal/scheduling/internal/logging/logging.go`（新导出）。**补**：新增容器 `k_scheduling_logging`（domain `d_scheduling`）+ 1 节点。

### 2.3 矛盾核对

- 视图/基线记了代码不存在的符号/边：基线 `n_agentd_Manager_appendGCWorktreesExecute（internal/agentd/gc.go）→ workspace` 两条边为 **B233.13 遗留陈旧边**，本卡未引入（见 §3 基线复现），未在本视图新增或删除，留 finish absorb。
- 视图对 178 个 B233.13 编排符号的 `file`/`line` 全部命中当前源码（`sym --view` 抽查 anchor=ok）。

## 3. validate 双口径读数（相对基线）

```text
codegraph validate --repo .                              → 132 issues、0 失鲜
codegraph validate --view cards-B233.17-charter-2 --repo . → 132 issues、0 失鲜
基线 686d87ab 副本 codegraph validate                    → 122 issues、0 失鲜
```

相对基线新增 10 条，全部为 `调用边门控`（no-import）这一**既有种类**，无新增种类：

```text
# 9 条：handler/SetManager 在基线仍锚 server.go，边门控读基线文件 → server.go 无 workspace import
调用边门控: n_agentd_Server_SetManager → n_workspace_NewCapability
调用边门控: n_agentd_Server_handleTaskDiff → n_workspace_{TaskDiffTarget,DiffBaseFor,ManualBranchExists}
调用边门控: n_agentd_Server_handleTaskBundle → n_workspace_BundleRange
调用边门控: n_agentd_Server_handleTaskBranches → n_workspace_{Branches,ResolveBaseBranch}
调用边门控: n_agentd_Server_handleTaskFile → n_workspace_TruncatedNotice
调用边门控: n_agentd_Server_handleTaskRun → n_workspace_RunCmd
# 1 条：B233.13 视图把 cacheActiveLeaf 边起点落旧 cachegc.go，本卡改锚 nested 后仍按旧文件门控
[cards-B233.13-charter] 调用边门控: n_orchestration_cacheActiveLeaf → n_executor_TaskTmpDir
```

（另 2 条 `n_agentd_Manager_appendGCWorktreesExecute → n_workspace_{MayRecycle,RecycleInput}` 为基线自有存量，`686d87ab` 副本已复现，不在本卡新增 10 条内。）

根因：`validate` 的边门控读**基线文件锚**判定 import，视图 `nodesModified` 改锚不回写基线。本节点红线禁止改 `baseline.json`；这 10 条随 finish absorb 本视图（基线重锚）后自然消除。9 条 handler/SetManager 边系本卡 handler 迁出产生，1 条 cacheActiveLeaf 系本卡 cache 符号改锚 nested 产生。

`codegraph check`：无视图 fails=7（3 dead-entry + 1 dead-interface + 3 over-budget）、带本卡视图 fails=6（dead-interface 被视图补 OrchestrationClient 消除）——与 plan §2 / contract §9 基线逐条一致，未新增 fail 条目/种类。

## 4. 自检

- `codegraph validate --view cards-B233.17-charter-2` 通过引用完整性（132 与基线一致、本视图零 tag 新增）。
- 抽查 3 个新符号 `codegraph sym --view cards-B233.17-charter-2`：
  - `ActiveLeaf` → `internal/orchestration/internal/cacheplan/cacheplan.go:26`，anchor=ok；
  - `PlanTaskCacheLeaves` → 同文件 `:91`，anchor=ok；
  - `RunShell` → `internal/workspace/internal/gitproc/runshell.go:106`，anchor=ok；
  - `MapGateAnswer` → `internal/approval/internal/decision/decision.go:20`；`StatusLog` → `internal/scheduling/internal/logging/logging.go:14`；`Server.handleStatus` → `internal/agentd/handlers.go:37`。
- 只动 `codegraph/diffs/cards-B233.17-charter-2.json`；`baseline.json`/`target.json`/`best.json`/业务代码未改（`git diff --name-only` 仅该文件）。

## 5. 图覆盖债（留给人）

- `internal/scheduling/status.go#statusLog`（转发 nested `logging.StatusLog` 的包内小函数）未入图：基线该文件只收 6 个声明，`statusLog` 属既有图覆盖债（B231 已记符号级漏建类），本节点不为它单独造节点。
- 同段其余 handler 私有 helper（`writeReclaimError`/`writeDispatchError`/`taskOrErr` 等）已随 `nodesModified` 收录，无遗漏。
- `internal/workspace/gitworkspace.go` 的 `GitRun`/`GitProbe`/`GitRunNet` 三个转发函数基线未收（既有覆盖债），本卡未搬迁它们，不在本节点增补范围。
