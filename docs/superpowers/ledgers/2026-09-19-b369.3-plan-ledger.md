# B369.3（S3）plan 节点台账

节点：plan（卡 B369.3「S3 工具链 gate 与轮换文档」）。分支 `cards/B369.3-charter-2`。法定产出：`docs/superpowers/plans/b369.3-plan.md`。本节点只出计划文档，不写实现代码、不建脚手架。上一轮因 OOM 连坐中断未提交、产物丢失，本轮重派（协调者工单 cbf14833 明确：不重跑工具链实验，以 contract §0 darwin 台账为据静态核对）。

## 一、基线复核（本节点实测）

| 命令 | 原始结果 |
| --- | --- |
| `git branch --show-current` / `git log --oneline -1` | `cards/B369.3-charter-2` / HEAD `4ab6415a` |
| `go version` | `go version go1.26.1 linux/amd64` |
| `go build ./...`（根） | 空输出，EXIT=0 |
| `cd mobile && go build ./...` | EXIT=0 |
| `cd mobile && go test ./... -count=1` | `? github.com/Xsxdot/handoff/mobile/bind [no test files]`，EXIT=0 |
| `cd mobile && go vet ./...` / `gofmt -l .` | 空 / 空 |
| `go list ./... \| grep -c handoff/mobile` | `0` |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.066s` |
| `codegraph --repo . check --view cards-B369-charter` | `fails=[]` |
| `codegraph --repo . validate --view cards-B369-charter` | `issues=null`、`edgeIssues=null` |
| `codegraph --repo . check`（基线） | `fails=[]` |
| `grep -n "tool golang.org/x/mobile" mobile/go.mod` | 无命中（tool 指令不存在） |
| `cd mobile && go tool -n golang.org/x/mobile/cmd/gobind` | `go: no such tool "golang.org/x/mobile/cmd/gobind"`，EXIT=2 |
| `ls mobile/` | `bind/ go.mod go.sum`（无 build.sh / README.md） |

## 二、图覆盖债（`codegraph sym`）

- 命中（基线）：`NewDialer` → `n_relay_NewDialer`（`d_transport_tunnel`）。
- 命中（view `cards-B369-charter`）：`EncodePairBundle` → `n_proto_EncodePairBundle`（`d_protocol`）；`Core.Pair` → `n_mobilecore_Core_Pair`（`d_transport_channel`）。
- 未命中：本卡三件产物全在 `mobile/` 嵌套独立 module，**图外**（扫描配方排除 `mobile` 前缀，contract §1.3 / breakdown §1.2）。本卡不补图、不改冻结物。

## 三、库行为出处（读源码原文）

- `golang.org/x/mobile/cmd/gomobile/bind.go:87` 调 `mobileModuleAvailable()`；`:92` 报 `gomobile bind requires golang.org/x/mobile in the current module...` 并提示 `go get -tool golang.org/x/mobile/cmd/gobind`；`:391` 的 `mobileModuleAvailable` 以 `packages.Load("golang.org/x/mobile/bind")` 成败判定。
- `mobile/bind/bind.go` 头注已写明两条硬约束与 gomobile 版本（S4 卡落）。

## 四、$TMPDIR 探针实测（tool 指令落地形状）

在 `$TMPDIR/mobile-probe3`（`cp -r mobile/.`，replace 改绝对路径）：

```
$ printf '\ntool golang.org/x/mobile/cmd/gobind\n' >> go.mod
$ GOPROXY=off go get -tool golang.org/x/mobile/cmd/gobind@v0.0.0-20260908204917-8b95e45f8d3e
go: added golang.org/x/mobile v0.0.0-20260908204917-8b95e45f8d3e
go: added golang.org/x/mod v0.41.0
go: upgraded golang.org/x/sync v0.22.0 => v0.23.0
go: upgraded golang.org/x/sys v0.47.0 => v0.48.0
go: upgraded golang.org/x/tools v0.0.0-20180917221912-90fa682c2a6e => v0.50.0
$ GOPROXY=off go mod tidy -diff           → 无输出，EXIT=0
$ GOPROXY=off go build ./...              → EXIT=0
$ go tool golang.org/x/mobile/cmd/gobind  → 正常启动
```

`probe-get`（`go get` + `go mod tidy`）与 `probe3` 产出的 `go.mod` / `go.sum` 逐字节相同 → 提交结果确定。探针仅在 `$TMPDIR`，`git status --porcelain` 干净。

试错记录：`go get -tool ...` 在模块 cache 缺失时需网络；沙箱内以 `GOPROXY=off` + 本地 module cache 通过（`golang.org/x/mobile@v0.0.0-20260908204917-8b95e45f8d3e` 已在 cache）。`replace ../` 在 `$TMPDIR` 探针里会指向错误父目录，必须改绝对路径。

## 五、裁掉的验证（协调者工单 cbf14833）

协调者指示：验证到此为止，不重跑工具链实验、不做 stub dry-run/shellcheck。此前一轮曾试的 build.sh 探针与 `shellcheck` 结论**不写入 plan**（仅作过程，未进产出物结论）。plan 以 contract §0 darwin 台账为据，真实 bind 标「未验证」。

## 六、提交事实（历史读数）

```
$ git add docs/superpowers/plans/b369.3-plan.md docs/superpowers/ledgers/2026-09-19-b369.3-plan-ledger.md && git commit -m "plan(B369.3): S3 工具链 gate 与轮换文档实现计划"
[cards/B369.3-charter-2 2c5b61a1] plan(B369.3): S3 工具链 gate 与轮换文档实现计划
 2 files changed, 706 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-19-b369.3-plan-ledger.md
 create mode 100644 docs/superpowers/plans/b369.3-plan.md
```

（随后按红线只 amend 一次把本段收进同批提交——amend 会换 hash，历史读数 2c5b61a1 即提交当时读数，不复写 amend 后 HEAD。）
