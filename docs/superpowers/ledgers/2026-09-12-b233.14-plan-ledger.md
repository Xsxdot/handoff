# B233.14 plan 节点台账（2026-09-12）

分支：`cards/B233.14-charter-4`　有效基线：`cards/B233.1-charter-7` @ `94246fc8`（本卡不切、不越过）
产出物：`docs/superpowers/plans/b233.14-plan.md`
上游：spec `docs/superpowers/specs/b233.14.md`（已批准）、contract `...-contract.md`（冻结 @ `a49eea84`）、breakdown `...-breakdown.md`（P1–P5 全 A，HEAD `7ad85123`）

边干边落；提交事实在收口时以 amend 收进同批（收口判据是工作树干净，不是文件里 hash 等于 HEAD）。

## 1. 动手前基线核验（命令 + 原始读数）

```
$ go build ./...
BUILD_EXIT=0

$ go vet ./internal/agentd/ ./internal/orchestration/ ./internal/scheduling/ ./cmd/
VET_EXIT=0

$ go test ./internal/orchestration/ -count=1
ok  	github.com/Xsxdot/handoff/internal/orchestration	45.480s

$ go test ./internal/orchestration/ -run 'TestDispatchInjectsTaskDisciplineIntoProfile|TestDispatchPersistsDisciplineBeforeExecutorStarts|TestDispatchSkipsStartWhenPersistFails|TestDispatchRejectsDisciplineWithoutTaskProfile' -count=1
PASS ... ok 0.438s

$ go test ./internal/scheduling/ -run 'TestIdentityOfAndBindPhysical|TestResolveLookupTable' -count=1
ok  	github.com/Xsxdot/handoff/internal/scheduling	0.003s

$ go test ./internal/proto/ -run 'TestTaskHomeDirJSON' -count=1
ok  	github.com/Xsxdot/handoff/internal/proto	0.002s

$ go test ./internal/agentd/ -run 'TestB23313ConcurrentDoneReleasesOnce|TestB23313SetManagerRejectsTypedNil' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	0.371s

$ go test ./internal/orchestration/ -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1
ok  	github.com/Xsxdot/handoff/internal/orchestration	0.048s

$ go test ./internal/approval/ -run 'TestClientApproverAllowUndeliveredUntilAck' -count=1
ok  	github.com/Xsxdot/handoff/internal/approval	0.053s

$ codegraph validate --repo .
EXIT=0

$ codegraph resolve --doc docs/superpowers/specs/b233.14-contract.md --repo .
EXIT=0
```

### 1.1 基线存量红（本卡不引入，不在本卡修复范围）

```
$ go test ./internal/agentd/ -count=1        # 全量
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.22s)
FAIL	github.com/Xsxdot/handoff/internal/agentd	146.203s

$ go test ./internal/agentd/ -run TestLegacyNodeEventSequenceUnchanged -count=1 -v
    scheddispatch_test.go:531: 第 1 条漂移：
         got=GOLDEN|2|dispatched|web:test|[...]
         want=GOLDEN|2|comment|web:test|[{"body":"挂账 task T-fake-01@ftm（implement）"},{"kind":"普通"}]
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.99s)     # 连跑两次都红，确定性

$ go test ./cmd/ -count=1
--- FAIL: TestRepoContractGate (0.04s)
    graph_gate_test.go:38: 契约违规 [dead-interface] ... "OrchestrationClient" 在 d_gateway 中不存在
    graph_gate_test.go:38: 契约违规 [dead-interface] ... "SchedulingClient" 在 d_gateway 中不存在
    graph_gate_test.go:38: 契约违规 [over-budget] d_cli->d_workspace 直调 11 条超出预算 9
    graph_gate_test.go:38: 契约违规 [over-budget] d_gateway->d_workspace 直调 17 条超出预算 1
    graph_gate_test.go:38: 契约违规 [over-budget] d_orchestration->d_workspace 直调 49 条超出预算 19
    graph_gate_test.go:38: 契约违规 [over-budget] d_workspace->d_orchestration 直调 3 条超出预算 2
    graph_gate_test.go:38: 契约违规 [over-budget] d_workspace->d_protocol 直调 23 条超出预算 3
```

判断：两条均为功能线既有存量红。`TestRepoContractGate` 读 `codegraph/baseline.json`（尚未 absorb B233.13/B233.14 视图），因此 `OrchestrationClient`/`SchedulingClient` 在 baseline 缺失被判 dead-interface；其修复归 absorb（B233.17/B233.15 同类）。合同 §5.5 #31 的判据是「相对基线不新增 fail/warn」，用 `codegraph check` 对账，不用该测试。

### 1.2 图对账基线

```
$ codegraph check --view cards-B233.14-charter --repo .
"fails":[6]  dead-interface d_gateway→d_orchestration (OrchestrationClient)
             over-budget d_cli->d_workspace 11/9
             over-budget d_gateway->d_workspace 17/1
             over-budget d_orchestration->d_workspace 49/19
             over-budget d_workspace->d_orchestration 3/2
             over-budget d_workspace->d_protocol 23/3
"warns": 138

$ codegraph check --base cards/B233.1-charter-7 --repo .
"fails":[7]  # 多一条 dead-interface SchedulingClient
"warns": 139
```

结论：#31 成立——本卡视图相对基线不新增 fail/warn（视图里 `SchedulingClient` 的 dead-interface 被接口模型消解，6 < 7）。

```
$ codegraph sym SchedulingClient --repo .
Error: 符号 "SchedulingClient" 不在图中 ...   # 图覆盖债（Ticket 0 新增，基线未 absorb）
$ codegraph sym 'scheduling.New' --repo .
Error: ... 不在图中 ...
```

`target.json` 已是本卡目标态（contract a49eea84 冻结）：`d_gateway→d_scheduling` interfaces=["SchedulingClient"]、budget 0、entries 保留 `scheduling.Service` + `scheduling（包级函数）`；`assembly` 含 `cmd/agentd.go`。`best.json` 有 `k_agentd_scheduling_client → d_gateway`。视图 diff `codegraph/diffs/cards-B233.14-charter.json`（base `02180141`）含 `m_agentd_SchedulingClient`、`k_agentd_scheduling_client`、`n_cmd_setupLedger→n_scheduling_New`。实现节点预计不改图文件，只复核。

### 1.3 库/环境行为探针

```
$ cat > probe/main.go <<'EOF'
package main
import ("fmt"; "os")
func main() { os.Setenv("HOME", ""); h, err := os.UserHomeDir(); fmt.Printf("HOME=\"\" -> home=%q err=%v\n", h, err) }
EOF
$ go run .
HOME="" -> home="" err=$HOME is not defined
```

读源码确认：
- 五家 `Profile.Prepare` 空 `HomeDir` 拒绝：`internal/executor/opencode/profile.go:77-79`（其余四家同形）。
- overlay 落点：`opencode/profile.go:126-138`（`<home>/.handoff/task-overlay/<name>`；文件名含 `..` 拒绝）。
- `Rules`/`Skills` 仅当非空才写：`opencode/profile.go:88-123`。
- `expandCarrierHome` 空串 no-op（`internal/orchestration/manager.go:1125-1136`）。
- `prepareTaskProfile` 空 HOME 拒门：`manager.go:1060-1063`。
- `IdentityOf`/`BindPhysical` 空 HOME 合法、显式空串覆盖非空拒绝：`receiver.go:148/161`、`receiver_test.go:215`。

## 2. 现状签名/调用面（本工作树读数，行号会漂，`file#Symbol` 是锚）

- `Server.scheduling *scheduling.Service` — `internal/agentd/server.go#Server:176`
- `SetupAutomation` 内 `scheduling.New(facadeAsRegistry{...})` + `SetKnownMachines` — `server.go#SetupAutomation:2682-2690`
- `SetScheduling(svc *scheduling.Service)` — `server.go#SetScheduling:2751`
- `Scheduling() *scheduling.Service` — `server.go#Scheduling:2781`
- `Server.Conf() func() *config.Config` — `server.go#Conf:401`（B233.13 已导出）
- 生产对 `s.scheduling` 调用点 25 处（receiver_bind 6、schedapi 11、scheddispatch 2、scheddrain 5、server 2、coordapi 1、coordinator_home 1）；闭包 = `SchedulingClient` 18 方法（Ticket 0 已锁）。
- 测试消费面：`.Scheduling()` 57 处（13 文件）；`SetDefaultCarrier` 经 `Scheduling()` 3 处（integration:118,767；receiver_dispatch:326）+ `seedDefaultFakeCarrier` 内 1 处；`schedEnv.svc *scheduling.Service`（schedapi_test.go:32）；`putOnlineCarrier(svc *scheduling.Service)`（coordapi_test.go:125）。
- `setupLedger` 调 `SetupAutomation` 后 `StartAutomation` — `cmd/agentd.go#setupLedger:480/483`。
- `TestRepoContractGate` 读 `codegraph/baseline.json` — `cmd/graph_gate_test.go:29-40`。

## 3. 判断与舍弃

- 选用 P5-A：单卡，Task 1（W1 门面/组装/测试迁移）→ Task 2（W2 空 HOME×纪律）→ Task 3（图对账，验证为主）→ Task 4（收口）。W2 与 W1 生产文件不相交，但按 P5 先 W1 稳定类型/组装。
- 舍弃：拆 B233.14.x（spec 禁）；新开 d_cli 合同（本次补充明令）；把 DTO 搬 gateway / 接口放 schedclient（spec 弃选）。
- `scheduling.New(` 源码守卫需剔除 `//` 注释后再匹配——`scheduling_client.go` 顶部注释含该字样，裸 `strings.Contains` 会假红。
- `userHomeDir` 提为包内变量：breakdown C 已允许「把 os.UserHomeDir 提为可替换的包内测试缝」以覆盖 #19；`expandCarrierHome` 同步改用它，生产默认不变，测试可注入。
- 基线存量红 `TestLegacyNodeEventSequenceUnchanged`（agentd）与 `TestRepoContractGate`（cmd）：不属本卡，收口判据按「无新增失败」而非「全绿」。

## 4. 覆盖债

- `codegraph sym` 未命中：`SchedulingClient`、`SchedulingRegistry`、`scheduling.New`、`prepareTaskProfile`、`taskProfileHome`、`expandCarrierHome`。文档用 `file#Symbol` + 行锚，不把空 `who-calls` 当无调用方。
- 非小队直调计数：实现迁 `New` 后 `d_gateway→d_scheduling` 由 3 降 2（`handleCarrierDetect→ApplyDetect`、`handleCarrierRunCommand→RunCommand`），目标图预算 0 由包级函数/接口 entries 承载，不棘轮。

## 5. 收口事实

产出物落盘：
- `docs/superpowers/plans/b233.14-plan.md`（本节点法定产出物）
- `docs/superpowers/ledgers/2026-09-12-b233.14-plan-ledger.md`（本台账）

计划自审：
- spec 覆盖 §5.1 逐条指到 task。
- 占位符扫描：无 TBD/「同 Task N」/「加适当的错误处理」；测试迁移以「处置表 + 可机械执行规则 + 指认 harness」替代逐文件代码，已在 §5.2 自我声明。
- 接缝双向覆盖 §5.4；内部锁 `TestB23314SchedulingNewOnlyInCmd`、`TestB23314TaskProfileHome` 逐条声明理由，不顶替缝级断言。
- 跨 task 签名一致性 §5.3（`SchedulingClient`/`SetScheduling`/`Scheduling`/`SchedulingRegistry`/`taskProfileHome` 唯一命名来源）。

计划里没有需要驱动 handoff 派发系统自身的验收步骤（无「本 task 由协调者执行」项）。

本节点未跑实现级判据（不写实现代码）；已跑的是基线复核与库行为探针（§1）。收口判据 = 工作树干净。

（提交命令与原始输出：见下方 amend 后的本批提交。）

