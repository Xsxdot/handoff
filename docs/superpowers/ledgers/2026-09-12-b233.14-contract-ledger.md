# B233.14 contract 台账

- 2026-09-12 起点：分支 `cards/B233.14-charter`，有效基线 `cards/B233.1-charter-7` @ `02180141`（不切、不越过）。本节点不调 handoff CLI、不起 executor。
- 上游 spec 状态位已核实「已批准」（`docs/superpowers/specs/b233.14.md:3`）。spec ledger：`docs/superpowers/ledgers/2026-09-12-b233.14-spec-ledger.md`。

## 现状查证（本轮 grep/读数）

- `Server.scheduling *scheduling.Service`：`internal/agentd/server.go:176`。
- `SetupAutomation` `scheduling.New(facadeAsRegistry{...})`：`internal/agentd/server.go:2682`；`SetKnownMachines` `:2683`。
- `SetScheduling(*scheduling.Service)`：`internal/agentd/server.go:2751`；`Scheduling() *scheduling.Service`：`:2781`。
- `facadeAsRegistry`：`internal/agentd/server.go:2789`；`translateRegistryErr` `:2825`。
- gateway 生产对 `s.scheduling` 调用点 18 方法闭包（`receiver_bind.go` / `schedapi.go` / `scheddispatch.go` / `scheddrain.go` / `coordapi.go` / `server.go` / `coordinator_home.go`），逐条见 contract §1 表。
- 空 HOME×纪律门：`internal/orchestration/manager.go#prepareTaskProfile` `:1054`，`if start.Task.HomeDir == ""` 在 `:1060`。
- 五家 `Profile.Prepare` 拒绝空 HomeDir：`opencode/claudecode/codex/grok/agy profile.go:77`。
- overlay 落点 `<home>/.handoff/task-overlay/<name>`：opencode/profile.go:126；Rules/Skills 分离写 `:88-123`。
- `expandCarrierHome` 对空串 no-op：`internal/orchestration/manager.go:1125`。
- `IdentityOf` 空 HOME 合法：`internal/scheduling/receiver.go:148`；显式空覆盖非空拒绝：`receiver_test.go:215`。

## 对侧常量执法

- `scheduling.SetDefaultCarrier`：**无生产发出/消费**（仅测试）→ 标「疑似漂移」，不入接口（接口=生产消费闭包）。归 B233.5 后续卡。
- `scheduling.SetKnownMachines`：装配期配置，座落具体 `*Service`，不入使用方接口。
- `Err*` 哨兵、`ErrCASConflict`、`ErrDetectUnwired` 均活跃（contract §3）。

## 图探针（本轮实跑，均配对还原）

- 删 `d_gateway→d_scheduling` entries 的 `scheduling.Service` → `over-budget 1/0`（假棘轮实锤，保留容器）。
- 加 `interfaces:["SchedulingClient"]` 而无接口节点 → `dead-interface`；视图加 `k_agentd_scheduling_client` + `m_agentd_SchedulingClient` 后消失。
- 视图 `base` 用 `d_coordination_api` 之外的领域会报「引用不存在的基线领域」；用 `d_coordination_api` 通过。
- 新增 `d_cli→d_scheduling` 契约 → `budget-raised` + `dead-contract`；协调者裁决不新开合同。
- 把 `cmd/agentd.go` 列入 `assembly`，视图加 `n_cmd_setupLedger→n_scheduling_New` 边 → 无新 fail/warn。d_cli 存量 legacy 命中数下降（assembly 豁免），无新增违规。
- `codegraph check --view cards-B233.14-charter`：fails=6，与基线 `check` 逐项一致（`NEW fails vs baseline: 0`），全部是存量 over-budget / `OrchestrationClient` dead-interface。
- `codegraph resolve --doc docs/superpowers/specs/b233.14-contract.md` → exit 0（首轮 7 处坏锚已修：`#Service.X`→`#X`；`receiver_test.go#...`→`internal/scheduling/receiver_test.go#...`）。

## Ticket 0 骨架

- 新增 `internal/agentd/scheduling_client.go`：`SchedulingClient` 接口（18 方法闭包）+ `var _ SchedulingClient = (*scheduling.Service)(nil)` 编译期断言。
- 编译原始输出：
  ```text
  $ go build ./...
  BUILD_EXIT=0
  $ go vet ./internal/agentd/
  VET_EXIT=0
  ```
- 回归：`go test ./internal/scheduling/ -run 'TestIdentityOfAndBindPhysical|TestResolveLookupTable|TestDefaultCarrierRecordGolden' -count=1` → `ok ... 0.003s`。

## 图冻结物

- `codegraph/target.json`：`d_gateway→d_scheduling` 增 `interfaces:["SchedulingClient"]`、预算 0 不动、entries 保留、note 追加；`assembly` 增 `cmd/agentd.go`。
- `codegraph/best.json`：`k_agentd_scheduling_client → d_gateway`。
- `codegraph/diffs/cards-B233.14-charter.json`：base `02180141`，容器 + 接口符号 + `n_cmd_setupLedger→n_scheduling_New` 边。

## 拍板（三重闸门，详见 contract §6）

1. `scheduling.New` 组装点上移 `cmd` + gateway 暴露账本→Registry 适配。
2. 空 HOME 展开主 HOME 只作 overlay 写入点，不回写 `Task.HomeDir`。
3. `SchedulingClient` 只收生产消费闭包，不收 `SetDefaultCarrier`/`SetKnownMachines`。

## 提交事实

- 本批产出：`internal/agentd/scheduling_client.go`、`docs/superpowers/specs/b233.14-contract.md`、本台账、`codegraph/target.json`、`codegraph/best.json`、`codegraph/diffs/cards-B233.14-charter.json`。
- 收口判据：工作树干净；不回填 hash chase。
