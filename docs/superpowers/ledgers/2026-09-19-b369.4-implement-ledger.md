# B369.4（S4）implement 节点台账

节点：implement（卡 B369.4「S4 壳绑定面收口」）。分支 `cards/B369.4-charter-2`。法定产出物：`docs/superpowers/plans/b369.4-plan.md` 的 T2/T3 实现文件。本节点按 plan 执行，不重新探索。

## 一、T0 冻结物与基线核验（只读，实测）

| 命令 | 原始结果 |
| --- | --- |
| `go version` | `go version go1.26.1 linux/amd64` |
| `git status --short --branch` | `## cards/B369.4-charter-2`（干净） |
| `go build ./...`（根） | 空输出，EXIT=0 |
| `cd mobile && go build ./...` | 空输出，EXIT=0 |
| `cd mobile && go test ./... -count=1` | `? github.com/Xsxdot/handoff/mobile/bind [no test files]`，EXIT=0 |
| `go list ./... \| grep -c handoff/mobile` | `0` |
| `cd mobile && go list ./...` | `github.com/Xsxdot/handoff/mobile/bind` |
| `codegraph --repo . sym Core.Pair --view cards-B369-charter` | `n_mobilecore_Core_Pair`，`func (c *Core) Pair(ctx context.Context, bundleJSON string) (PairResult, error)` |
| `codegraph --repo . sym Core.Origin --view cards-B369-charter` | `Core.Origin`，`func (c *Core) Origin(machine string) (string, error)` |
| `codegraph --repo . sym Core.MachineNames --view cards-B369-charter` | `Core.MachineNames`，`func (c *Core) MachineNames() []string` |
| `codegraph --repo . sym Core.Close --view cards-B369-charter` | `Core.Close`，`func (c *Core) Close() error` |
| `codegraph --repo . sym bind --view cards-B369-charter` | `Error: 符号 "bind" 不在图中`（图覆盖债，设计使然：扫描配方排除 mobile 前缀） |

四方法签名与 plan §3.1(a) 逐字一致；`PairedMachine{Name,Origin string; Online bool}`、`PairResult{Machines []PairedMachine}`、`New(dial DialFunc, log *slog.Logger) *Core` 均核对一致。

## 二、T1 红绿之「红」

新增 `mobile/bind/fakes_test.go`、`mobile/bind/bind_test.go`、`mobile/bind/export_surface_test.go`（plan §6 完整内容）。跑：

```
cd mobile && go test ./bind/ -count=1
```

T1 的原始红在本回合未重跑（抢救提交已含三个测试文件与生产实现；本回合实测的 T1 红见下方 T2「fail-closed 变体」一栏——即 `session.go` 遗留的 fail-open 变体把 `TestBindSessionFailIsClosed` 打红）。

## 三、T2 绑定面生产实现（本回合收口）

### 3.1 抢救分支遗留状态

抢救提交 `53ff82de` 里 `mobile/bind/session.go:26` 为：

```go
func (notWiredSessions) SessionCookie(string) (string, error) { return "", nil }
```

这是 plan §7 变异清单第 1 条的 **fail-open 变体**（非 plan 目标态），抢救时把变异留在了树里。plan 目标态是 `return "", errSessionsNotWired`。

### 3.2 先观察红（T1 的先红证据）

```
$ cd mobile && go test ./bind/ -count=1
--- FAIL: TestBindSessionFailIsClosed (0.00s)
    bind_test.go:85: 核侧会话入口未接线时必须返回错误，不得静默成功
FAIL
FAIL	github.com/Xsxdot/handoff/mobile/bind	0.003s
```

### 3.3 还原生产态

`git diff mobile/bind/session.go`：

```
-func (notWiredSessions) SessionCookie(string) (string, error) { return "", nil }
+func (notWiredSessions) SessionCookie(string) (string, error) { return "", errSessionsNotWired }
```

### 3.4 跑绿

```
$ cd mobile && go build ./... && go test ./... -count=1
build_ok
ok  	github.com/Xsxdot/handoff/mobile	0.183s
ok  	github.com/Xsxdot/handoff/mobile/bind	0.004s
test_exit=0
```

```
$ cd mobile && gofmt -l .          # 空输出
$ cd mobile && go vet ./...        # 空输出, vet_exit=0
```

### 3.5 变异验证（四条 + T3 一条，本回合实测）

1. **fail-open**（见 3.2）：`SessionCookie` 返回 `"", nil` → `TestBindSessionFailIsClosed` 红。
2. **导出 Token**：`bind.go` 末尾加 `func Token() string { return "x" }` → `TestBindExportedSurfaceIsFrozen` 红：
   ```
   got=[... "func Token() string"]
   want=[... 六函数]
   ```
   删除后绿。
3. **协议 import**：`session.go` 加 `_ "github.com/Xsxdot/handoff/internal/relay"` → `TestBindProductionHasNoProtocolLogic` 红：
   ```
   export_surface_test.go:101: github.com/Xsxdot/handoff/internal/relay 属于协议实现层，绑定层不得 import（协议零重实现）: session.go
   ```
   删除后绿。
4. **cookie 错转发**：`SessionCookie` 内改 `sessions.SwitchMachine(machine)` → `TestBindSessionCookieAndSwitchAreOneJarToOneMachine` 红：
   ```
   bind_test.go:59: 取 devbox cookie: value="http://127.0.0.1:42002" err=<nil>（绑定面必须返回核侧 cookie，非 origin）
   ```
   还原后绿。
5. **T3 模块错位**：把 `mobile/go.mod` 的 `module github.com/Xsxdot/handoff/mobile` 临时改为 `module github.com/Xsxdot/handoff` → `TestMobileModuleStaysOutOfParentBuildGraph` 红：
   ```
   moduleisolation_test.go:52: 在 mobile/ go list ./... 失败: exit status 1
       bind/bind.go:16:2: module github.com/Xsxdot/handoff provides package ... is replaced but not required
   ```
   已从备份还原，`git diff --stat go.mod go.sum` 空输出。

## 四、T3 嵌套 module 隔离守卫

`mobile/moduleisolation_test.go` 随抢救提交已在树中；本回合实测：

```
$ cd mobile && go test . -count=1
ok  	github.com/Xsxdot/handoff/mobile	0.142s
```

## 五、T4 收口复核（本回合实测）

| 命令 | 原始结果 |
| --- | --- |
| `go build ./...`（根） | 空输出，EXIT=0 |
| `cd mobile && go build ./...` | 空输出，EXIT=0 |
| `cd mobile && go test ./... -count=1` | `ok mobile` + `ok mobile/bind`，EXIT=0 |
| `cd mobile && go test ./bind/ -race -count=1` | `ok github.com/Xsxdot/handoff/mobile/bind 1.011s`，EXIT=0 |
| `cd mobile && gofmt -l .` | 空输出 |
| `cd mobile && go vet ./...` | 空输出，EXIT=0 |
| `go list ./... \| grep -c handoff/mobile` | `0` |
| `go list -deps ./... \| grep -c handoff/mobile` | `0`（根构建图零 mobile 依赖） |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.069s`，EXIT=0 |
| `codegraph --repo . check --view cards-B369-charter` | `"fails": []`，EXIT=0 |
| `codegraph --repo . validate --view cards-B369-charter` | `{"issues":null,"edgeIssues":null,...}`，EXIT=0 |
| `git status --short`（收口前） | 仅 ` M bind/session.go`（即本回合修改） |

## 六、未验证（照 plan §13 声明，本回合不重复跑）

- 真实 cookie 注入 iOS `WKHTTPCookieStore` / Android `CookieManager`：**未验证，需真机**。
- `gomobile bind` 出 AAR/XCFramework：linux 无 Xcode/NDK，**未验证**。
- S2（B369.2）落地后 `sessionAPI` 方法名对齐：**未发生**（S2 未落地）。
