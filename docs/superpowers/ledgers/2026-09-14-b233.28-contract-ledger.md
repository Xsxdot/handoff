# B233.28 contract 台账

上游：`docs/superpowers/specs/b233.28.md`（头部状态行「已批准」已核实，未重写）。
基线：`cards/B233.1-charter-7` @ `56c7a9c77`。本分支：`cards/B233.28-charter-2`。

- 2026-09-14：check 基线读数——`codegraph check --repo .` 原始输出顶层 `fails: []`、`legacyHits["d_gateway->d_orchestration"]=76`、`bestCoverage assignedContainers=338 viewContainers=338 crossDomainEdges=1248 misplacedSkipped=0`。
- 2026-09-14：baseline 容器查证——`k_store_Store`(65 节点)/`k_store_fn`(25)/`k_store_model`(10) 三容器均 domain=`d_orchestration`；`internal/store/auth.go`(会话) 与 `internal/store/workbench.go`(工作台) 的方法与任务/项目/事件方法**同处这三容器**，best.json 无 per-symbol 域、无独立会话/工作台容器。
- 2026-09-14：协调者裁决 **C**：本轮不改挂 `k_store_*`、不新建 `k_store_gateway_*`、不拆 Go 类型；best.json 与 target.json 预算 76 都不动；登录/工作台命中记容器粒度债，写进 contract §6/§7。
- 2026-09-14：现状签名逐条查证（行号为本轮读数）——`store.go` `GetTask:408`/`ListTasks:439`/`UpdateTaskState:525`/`AppendEvent:778`/`EventsFrom:901`/`EventsFromAsc:949`/`LatestEvent:985`/`CountEvents:1048`/`GetTicket:1108`/`AnswerTicketApplied:1275`/`PendingTickets:1356`；`mirror.go` `MirrorEventsFrom:80`/`ListMirrorTasks:138`/`MirrorTaskTarget:179`；`projects.go` `GetProjectLocationByName:82`/`ListProjectLocations:99`/`UpdateProjectLocation:153`。
- 2026-09-14：gateway 直打点查证——`handlers.go` `s.st.ListTasks:203`、`GetTask:254,:933,:939,:956,:988,:1005,:1120,:1536`、`PendingTickets:265`、`EventsFrom:271`、`EventsFromAsc:1641`、`LatestEvent:1658`、`CountEvents:1740`、`GetTicket:365`、`AnswerTicketApplied:382`、`AppendEvent:403`；`resumeIfIdle:459`；`projectadmin.go` `GetProjectLocationByName:87,:166,:246`、`UpdateProjectLocation:136`；`codegraph.go` `GetProjectLocationByName:33,:144`。
- 2026-09-14：Ticket 0 落码——新建 `internal/orchestration/facade.go`（15 个 `*Manager` 方法：10 任务查询直通镜像 + `AnswerTicket`/`ResumeIfIdle` 空壳 + 3 项目位置直通镜像）；`orchestration_client.go#OrchestrationClient` 方法集扩（新增 15 方法）。空壳哨兵 `orchestration.ErrTicketAnswerUnwired`。
- 2026-09-14：`go build ./...` → 原始输出为空，退出码 0。
- 2026-09-14：`gofmt -l internal/orchestration/facade.go internal/orchestration/orchestration_client.go` → 无输出（退出码 0）。
- 2026-09-14：`go vet ./internal/orchestration/` → 无输出，退出码 0。
- 2026-09-14：分支视图写入 `codegraph/diffs/cards/B233.28-charter.json`（15 `nodesAdded` + 1 `nodesModified`）；`python3 -m json.tool` 通过。
- 2026-09-14：`codegraph validate --repo . --view cards/B233.28-charter` → 原始 `nodes:5299 issues:null`（0 issue）；`codegraph check --view cards/B233.28-charter` → `fails: []`、`legacyHits["d_gateway->d_orchestration"]=76`（预算未动）、bestCoverage 与基线一致。
- 2026-09-14：`codegraph resolve --doc docs/superpowers/specs/b233.28-contract.md` → `anchors` 19 条，`bad=0`，退出码 0。
- 2026-09-14：契约文档落盘 `docs/superpowers/specs/b233.28-contract.md`。
- 2026-09-14：`git add … && git commit -m "contract(B233.28): freeze 任务面/项目位置走编排查询门面"` → 原始输出 `[cards/B233.28-charter-2 484608e7] contract(B233.28): freeze …`、`5 files changed, 530 insertions(+)`。本行随同批 amend 收进该提交，故记录的是提交当时的读数。
