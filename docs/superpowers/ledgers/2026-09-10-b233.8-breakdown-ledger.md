# B233.8 breakdown 台账

本台账按拆解回合追加；命令输出只记录本回合实际运行结果，不把上游台账读数冒充当前验证。

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-10 | 当前工作树初始干净；当前分支为 `cards/B233.8-charter-2`，HEAD 为 `2eb03fec`；有效基线引用解析为 `2fe0a5d4`。 | `pwd && git status --short --branch && git log -1 --oneline` → `/root/.handoff/worktrees/e0eb84cf`；`## cards/B233.8-charter-2`；`2eb03fec docs(B233.8): freeze ApprovalClient migration contract`。`git rev-parse cards/B233.1-charter-7` → `2fe0a5d4`。 |
| 2026-09-10 | 曾按错误的技能根路径读取 handoff skill，命令失败；随后按实际根路径读取成功，未调用 handoff CLI。 | `sed -n '1,240p' /root/.codex/skills/r0/handoff/SKILL.md` → `sed: can't read /root/.codex/skills/r0/handoff/SKILL.md: No such file or directory`；随后 `sed -n '1,560p' /root/.codex/skills/handoff/SKILL.md` 成功读完。 |
| 2026-09-10 | 上游状态位一致：spec 头部为「已批准」（2026-09-09）；contract 头部当前为「冻结（本提交）」，本稿将其补钉为带日期与冻结提交的「已冻结」，不改变冻结正文。 | `sed -n '1,18p' docs/superpowers/specs/b233.8.md` → `状态：**已批准**（2026-09-09，用户授权自主推进……）`；`sed -n '1,12p' docs/superpowers/specs/b233.8-contract.md` → `状态：**冻结（本提交）**`。 |
| 2026-09-10 | `codegraph/best.json` 顶层 `parent` 为空的领域共 15 个；本卡实际触及 `d_orchestration`（logic）与 `d_execution`（boundary）。`d_execution_contract`、`d_execution_adapters` 是 `d_execution` 子域，不另列顶层子系统；`k_approval_*` 归 `d_orchestration`。 | `jq -r '.domains | to_entries[] | select((.value.parent // "") == "") | [.key,.value.type,.value.label,.value.responsibility] | @tsv' codegraph/best.json` → 输出含 `d_orchestration logic 任务编排`、`d_execution boundary 任务执行` 及其余顶层领域；`jq ... .containers ...` → `k_agentd_Manager d_orchestration`、`k_approval_fn d_orchestration`、`k_approval_model d_orchestration`、`k_executor_fn d_execution_contract`、`k_executor_model d_execution_contract`、`k_opencode_Adapter d_execution_adapters`。 |
| 2026-09-10 | 代码图 CLI 可用；Ticket 0 视图的新增/关键锚点可解析，旧实现类型 `taskApprovalClient` 未被图覆盖，须在稿中列为图覆盖债并以源码检索核对。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --help` → exit 0；`... --view cards-B233.8-charter sym 'Manager.bindApproval'` → 命中 `n_agentd_Manager_bindApproval`；`sym 'Adapter.BindApproval'` → 命中 `n_opencode_Adapter_BindApproval`；`sym 'Adapter.authorizeNativePermission'` → 命中；`sym 'ApprovalClient'` → 命中 `m_executor_ApprovalClient`；`sym 'taskApprovalClient'` → `Error: 符号 "taskApprovalClient" 不在图中（图未覆盖或名字有误）`。 |
| 2026-09-10 | 当前目标图对照无新增违规；既有 warning 与本卡无关。图结构完整性校验通过。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.8-charter check` → `{"fails":[],"warns":[...]}`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` → `{"containers":300,"domainDecls":23,"domains":23,"edgeIssues":null,"issues":null,"nodes":4504,...}`。 |
| 2026-09-10 | Ticket 0 骨架可编译；定向测试尚未在本回合取得完整终态输出，不能据此宣称行为已验证；`go build ./...` 已返回成功。 | `go build ./...` → exit 0；`go test ./internal/approval ./internal/agentd ./internal/executor/opencode -count=1` 在本次工具返回时仍显示 `? github.com/Xsxdot/handoff/internal/approval [no test files]`，其余包未返回终态，记为未验证。 |
| 2026-09-10 | 上述定向测试随后取得完整终态：agentd 与 OpenCode 包均通过；`internal/approval` 当前无测试文件。 | `go test ./internal/approval ./internal/agentd ./internal/executor/opencode -count=1` → `? github.com/Xsxdot/handoff/internal/approval [no test files]`、`ok github.com/Xsxdot/handoff/internal/agentd 181.053s`、`ok github.com/Xsxdot/handoff/internal/executor/opencode 18.175s`，exit 0。 |
| 2026-09-10 | 关键回归通过：Dispatch 注入、Continue 重绑且换版本不复用、非 OpenCode 旧路径、正常送达；OpenCode client 消费/投递失败回调/重绑也通过。 | `go test ./internal/agentd -run '^(TestDispatchOpenCodeInjectsApproval|TestContinueRebindsApprovalClientAndDoesNotReuseOldGrant|TestNonOpenCodeKeepsOldApprovalPath|TestNormalDeliveryMarksTicketDelivered)$' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.465s`；`go test ./internal/executor/opencode -run '^(TestMapPermissionAskedWithApprovalClientDoesNotEmitPermission|TestAuthorizeNativePermissionReportsDeliveryFailure|TestAdapterBindApproval)$' -count=1` → `ok github.com/Xsxdot/handoff/internal/executor/opencode 0.074s`。 |
| 2026-09-10 | 默认 cmd 契约门禁仍失败；原始失败是既有 `d_execution→d_orchestration` dead-contract/dead-interface 与 legacy 149 条，不能归因于本拆解稿，也不能写成通过。 | `go test ./cmd -run '^TestRepoContractGate$' -count=1` → `--- FAIL: TestRepoContractGate`；`契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边`；`契约违规 [dead-interface] ... 接口 "ApprovalClient" 在 d_execution 中不存在`；`legacy 命中: map[...]，warn 149 条`；`FAIL github.com/Xsxdot/handoff/cmd`，exit 1。 |
| 2026-09-10 | 旧生产实现类型静态未退役是当前基线事实；旧 Manager 入口三函数仍存在；OpenCode 包未匹配到 agentd/permgate import。 | `rg -n '^type taskApprovalClient|\\*taskApprovalClient' internal/agentd --glob '*.go' --glob '!**/*_test.go'` → 无输出/exit 1；`rg -n '^func \\(m \\*Manager\\) (handlePermission|handleQuestion|waitQuestion)' internal/agentd/manager.go` → 命中 2109、3005、3091；`rg -n '"github.com/Xsxdot/handoff/internal/(agentd|permgate)"' internal/executor/opencode --glob '*.go'` → 无输出/exit 1。 |
| 2026-09-10 | 法定 breakdown 锚点最终解析通过；14 个锚点均为 `ok` 或 `moved`，无 `vanished` / `file_missing`。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.8-charter resolve --doc docs/superpowers/specs/b233.8-breakdown.md` → exit 0；输出 14 个 anchors：`Client`、`NewClient`、`Manager.bindApproval`、`HashPolicyVersion`、`Manager.Dispatch`、`Manager.Continue`、`Manager.handlePermission`、`Manager.handleQuestion`、`Manager.waitQuestion`、`Manager.NoteDeliveryFailed`、`ApprovalClient`、`ApprovalBinder`、`Adapter.BindApproval`、`Adapter.authorizeNativePermission`，状态均 `ok`/`moved`。 |
| 2026-09-10 | 法定稿无行尾空白；工作树包含 contract 状态/澄清、breakdown 与 breakdown 台账三项待提交变更。 | `git diff --check` → exit 0、无输出；`rg -n '[[:blank:]]+$' docs/superpowers/specs/b233.8-breakdown.md docs/superpowers/specs/b233.8-contract.md docs/superpowers/ledgers/2026-09-10-b233.8-breakdown-ledger.md` → 无输出/exit 1；`git status --short --branch` → `## cards/B233.8-charter-2`、`M docs/superpowers/specs/b233.8-contract.md`、两个 `??`（breakdown 与 breakdown-ledger）。 |
| 2026-09-10 | 当前视图契约检查与结构检查最终通过；target 中 `d_execution→d_orchestration` 仍为 `legacyBudget:0`、`interfaces:["ApprovalClient"]`；曾误把 target 根对象当数组查询，失败后按 `.contracts[]` 更正。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.8-charter check` → `{"fails":[], ...}`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` → `{"containers":300,"domainDecls":23,"domains":23,"edgeIssues":null,"issues":null,...}`；误命令 `jq -c '.[] | select(.from=="d_execution" and .to=="d_orchestration")' codegraph/target.json` → `jq: error (at codegraph/target.json:388): Cannot index array with string "from"`；更正 `jq -c '.contracts[] | select(.from=="d_execution" and .to=="d_orchestration")' codegraph/target.json` → `{"from":"d_execution","to":"d_orchestration","legacyBudget":0,"interfaces":["ApprovalClient"],"legacyBudgetNote":"B233.1：OpenCode 已消费 ApprovalClient 接口缝，实现由编排侧绑定；零调用预算。 B233.8：生产实现物理落点冻结为 internal/approval（best 归 d_orchestration），Manager.bindApproval 仍是组装缝；复用既有接口方向与零调用预算，不新增方向。"}`。 |
| 2026-09-10 | 首次提交法定 breakdown、contract 状态/边界回写与本节点台账成功；随后只 amend 一次收录本提交事实，本行记录 amend 前历史读数，不追写 amend 后 hash。 | `git commit -m "docs(B233.8): add ApprovalClient migration breakdown"` → exit 0；原始输出：`[cards/B233.8-charter-2 0f0aaa7f] docs(B233.8): add ApprovalClient migration breakdown`、`3 files changed, 219 insertions(+), 1 deletion(-)`、创建 `docs/superpowers/ledgers/2026-09-10-b233.8-breakdown-ledger.md` 与 `docs/superpowers/specs/b233.8-breakdown.md`。 |
| 2026-09-10 | 计划节点读取 `b233.8-breakdown.md` 后确认 P1/P2 仍为“待拍板”；两项会分别改变实现 DAG 与法定文件集，不能在计划中自行假设。 | `sed -n '1,40p' docs/superpowers/specs/b233.8-breakdown.md` → `**状态：待拍板（2026-09-10）**`，并列出 P1、P2 的 A/B 方案；`git status --short --branch` → `## cards/B233.8-charter-3`。 |
| 2026-09-10 | 协调者拍板 P1=A、P2=A；本计划采用单卡 B233.8 实现 DAG，并保留 `internal/agentd/approval_client.go` 作为 `bindApproval`/哈希组装缝，迁出 `taskApprovalClient` 到 `internal/approval/client.go`。 | 用户回复：`P1 方案 A：轻档单卡，不拆子卡号，实现挂 B233.8。P2 方案 A：保留 approval_client.go 作为 bindApproval/哈希组装缝，taskApprovalClient 迁到 internal/approval/client.go。卡 note 已拍板 2026-09-10。` |
| 2026-09-10 | 计划节点在当前 HEAD `70d839ec` 重跑基线：构建、编排/执行包全包测试、关键回归均通过；`TestRepoContractGate` 仍失败于冻结物已知 dead-contract/dead-interface 与 legacy 命中，不能把该失败归因于本卡。 | `go build ./...` → exit 0、无输出；`go test ./internal/approval ./internal/agentd ./internal/executor/opencode -count=1` → `? github.com/Xsxdot/handoff/internal/approval [no test files]`、`ok github.com/Xsxdot/handoff/internal/agentd 196.387s`、`ok github.com/Xsxdot/handoff/internal/executor/opencode 18.194s`、exit 0；`go test ./internal/agentd -run '^(TestDispatchOpenCodeInjectsApproval|TestContinueRebindsApprovalClientAndDoesNotReuseOldGrant|TestNonOpenCodeKeepsOldApprovalPath|TestNormalDeliveryMarksTicketDelivered)$' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.538s`；`go test ./internal/executor/opencode -run '^(TestMapPermissionAskedWithApprovalClientDoesNotEmitPermission|TestAuthorizeNativePermissionReportsDeliveryFailure|TestAdapterBindApproval)$' -count=1` → `ok github.com/Xsxdot/handoff/internal/executor/opencode 0.086s`；`go test ./cmd -run '^TestRepoContractGate$' -count=1` → `FAIL`，原始失败含 `契约违规 [dead-contract]`、`契约违规 [dead-interface]`、`legacy 命中 ... warn 149 条`。 |
| 2026-09-10 | 计划节点复核代码图与静态边界：关键符号可解析；`Manager.Dispatch` 与 `Adapter.authorizeNativePermission` 的 flow 均 `degraded` 且 `steps: []`，因此计划以源码窗口为准；旧生产类型仍在 agentd，非 OpenCode 旧入口仍在，OpenCode adapter 无 agentd/permgate import，目标契约仍为零预算 ApprovalClient 方向。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.8-charter sym 'ApprovalClient'` → 命中 `internal/executor/approval.go:87`；`sym 'Manager.bindApproval'` → 命中 `internal/agentd/approval_client.go:29`；`sym 'Adapter.authorizeNativePermission'` → 命中 `internal/executor/opencode/adapter.go:817`；两次 `flow` → `degraded:true`、`missing:"基线没有 flows 段"`、`steps:[]`；`rg` 命中 `internal/agentd/approval_client.go:23` 的 `taskApprovalClient`、`manager.go:2109/3005/3091` 的 `handlePermission`/`handleQuestion`/`waitQuestion`，OpenCode import 检索无输出；`jq -c '.contracts[] | select(.from=="d_execution" and .to=="d_orchestration")' codegraph/target.json` → `legacyBudget:0`、`interfaces:["ApprovalClient"]`。 |
| 2026-09-10 | 计划节点复核图闸现状：结构校验与卡视图 check 通过，但 warnings 是存量债，计划不得以 warnings 数量判断本卡；实现后只要求 `fails: []`、目标方向不增反向预算/entry。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.8-charter check` → `"fails": []`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` → `"issues": null`、`"edgeIssues": null`。 |
| 2026-09-10 | 计划节点亲读实现将复用的依赖行为：Hub 广播满缓冲走 default 丢弃慢订阅者；WaitAnswer 只受调用方 context 控制并在取消时清理 waiter；工单创建是 INSERT OR IGNORE；可复用 grant 只接受同任务、同指纹、gate、allow、已送达；Ack Delivered 只更新 delivered_at 为空的票据；不同答案/作废返回 ErrTicketConflict；Approver.Decide 叠加 timeout 且错误 fail-closed。 | `internal/agentd/hub.go:157-225`、`internal/store/store.go:1006-1030`、`:1148-1195`、`:1262-1304`、`internal/agentd/approver.go:118-169`。 |
| 2026-09-10 | breakdown 头部的当前执行分支按本节点实际工作树更正为 `cards/B233.8-charter-3`；未切换分支。 | `git status --short --branch` → `## cards/B233.8-charter-3`。 |
