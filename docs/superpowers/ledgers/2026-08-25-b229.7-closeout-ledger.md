# B229.7 收口回归 + CHANGELOG 台账（2026-08-25）

## 事实记录（边干边追加）

- [merge] `git merge --no-ff acc/integration-B229` → 成功，ort 策略，无冲突；合并提交 d9470a7e。63 文件变化（+2648/−1938），含 T1~T5 十二个提交与 codegraph 棘轮。
- [build] 合并后 `go build ./...` → exit 0，输出仅 "BUILD_OK"。
- [fact] platform_test.go 既有断言只查头/尾标记与顺序，不锁「台账」内容；dispatch_test.go 用 headMark/tailMark 只查顺序。全仓 grep 确认无其它测试依赖平台层第3条/第②问文本。
- [red] 新增 TestComposeEnabledWithEmptyBaseOmitsLedgerFromPlatformLayer 首跑红，原文：platform_test.go:50: 平台层组装产出含「台账」2 次（head 第3条 + tail 第②问），失败原因为功能缺失非 typo。
- [impl] platform.go：head 删第3条（余1./2.天然有序）；tail 删②并把③重编号为②；包头注释「三条底线」→「两条底线」；新增注释说明台账句为何不在平台层。
- [green] go test ./internal/discipline/ -count=1 → 全 PASS（含新测试 TestComposeEnabledWithEmptyBaseOmitsLedgerFromPlatformLayer）。
- [mutation] 变异自验：先 grep -c 确认锚点唯一（=1），把 head 第2条尾部加「，并边干边落台账」（语义变异非删行），go build 过、单测红（「台账」1 次），证明测试有牙；已回滚，包内测试复绿。
- [changelog] 核对 [Unreleased] 既有条目覆盖 breakdown §188 五条行为变化：行「纪律块正文改为派发时组装…内置角色块移除」=①；「裸 dispatch 注入平台层+能力位拒发」=②③；「控制台 /api/discipline/file 处置」=⑤；「ledger.enabled 退休」=④。五条齐。
- [changelog] 增记本卡行为变化：平台不变量三条→两条，落台账要求改由角色层只给产出型角色。
- [regress] `go build ./... && go vet ./...` → 无输出，EXIT=0。
- [regress] `gofmt -l .` → 无输出（GOFMT_EXIT=0）。
- [regress] `go test ./... -count=1` → 46 包全 ok（`go list ./... | wc -l`=46，输出逐行 ok 零 FAIL），关键读数：internal/agentd 117.788s、cmd 10.912s、internal/discipline 0.004s、internal/ledgerstep 6.743s。
- [regress] `git diff --check` → 无输出（DIFFCHECK_EXIT=0）。
- [env] 本机安装版 handoff 二进制（213b58d8f803）跑 `handoff graph check` 报错退出 1，原文：「Error: 目标图不可用，check 拒绝执行: 目标图 codegraph/target.json 使用不支持的 schema version 3；请先运行 codegraph migrate」。与集成台账记载一致；改用本分支自建二进制 /tmp/opencode/handoff-b2297（`go build -o` 成功）执行。
- [graph] 自建二进制无视图 `graph check` → `"fails": []`、EXIT=0；legacyHits d_gateway->d_policy=27 与棘轮一致。
- [graph] `--view cards-B229-charter` → `"fails": []`、EXIT_VIEW=0（warns 仅 anchor-off-domain/anchor-off-graph/container-misplaced/legacy/oversized-package/prefix-family 既有类别，gw_policy=27）。
- [web] node_modules 不就绪：根目录 node_modules 仅含 .vite 缓存（find -type f 唯一命中 vitest results.json），web/node_modules 不存在——tsc/vitest 未验证，需真机。

## 续跑复验（2026-08-25 第二会话，HEAD=50296994）

- [resume] 本回合为同任务续跑：产出已由上一会话提交（50296994），按「没亲自跑到不许写结论」纪律，全部门禁本回合重跑，不引用上会话读数。
- [regress] `go build ./... && go vet ./...` → 无输出，BUILD_VET_EXIT=0。
- [regress] `gofmt -l .` → 无输出（GOFMT_EXIT=0）；`git diff --check` → 无输出（DIFFCHECK_EXIT=0）。
- [regress] `go test ./... -count=1` → 46 包逐行 ok 零 FAIL，TEST_EXIT=0（关键读数：internal/agentd 131.524s、cmd 10.594s、internal/discipline 0.004s、internal/ledgerstep 6.278s）。
- [graph] 安装版二进制（213b58d8f803）`graph check` 原文报错退出 1：「Error: 目标图不可用，check 拒绝执行: 目标图 codegraph/target.json 使用不支持的 schema version 3；请先运行 codegraph migrate」——环境限制非代码缺陷；自建 /tmp/opencode/handoff-b2297r2（`go build -o` 成功）执行。
- [graph] 自建二进制无视图 `graph check` → `"fails": []`、EXIT=0；`--view cards-B229-charter` → `"fails": []`、EXIT_VIEW=0（warns 仅既有六类 anchor-off-domain/anchor-off-graph/container-misplaced/legacy/oversized-package/prefix-family，gw_policy=27 与棘轮一致）。
- [mutation] 本回合变异自验守卫有牙：TestComposeEnabledWithEmptyBaseOmitsLedgerFromPlatformLayer 先绿（PASS）；把 head 第 2 条后回加「3. 每确立一个事实就往台账文件追加一行……」→ 红，原文 `platform_test.go:50: 平台层组装产出含「台账」1 次…FAIL`（MUTATION_EXIT=1）；`git checkout -- platform.go` 还原后包内测试复绿（ok）。测试锁的是平台层产出不含台账这一调用方可见行为，非内部帮手。
- [changelog] 本回合逐条对账 breakdown §188 五条：①内置角色块消失=「纪律块正文改为派发时组装…内置角色块六份 .md 从二进制移除」行；②裸派发注入平台层+③老目标机拒发=「裸 handoff dispatch 与环节派发现在都注入平台不变量层正文…拒发并给升级指引」行；④ledger.enabled 退休=同名行；⑤控制台纪律文件编辑面处置=「/api/discipline/file 读/写端点…返回可行动错误」行。五条齐 + 本卡增补条（平台不变量三条→两条）在场。
- [scope] 全仓 grep「每确立一个事实就往台账文件追加一行」：Go 源码零命中（含非测试），仅存于 spec/breakdown/历史台账等文档引文与 CHANGELOG 叙述——二进制内平台层已清零。
- [repro] 真机对照可复跑命令（B229 判据 5，归协调者；组合顺序头→角色→尾已由 TestResolveDispatchAssembly 锁定、平台层不含台账已由 TestComposeEnabledWithEmptyBaseOmitsLedgerFromPlatformLayer 锁定，故角色正文 grep 与整块组装等价）：
  - 平台层半边（任何机、仓内）：`go test ./internal/discipline/ -run TestComposeEnabledWithEmptyBaseOmitsLedgerFromPlatformLayer -count=1 -v`
  - 角色层半边（协调者真机、真实账本）：`handoff discipline get charter-review | grep -c 边干边落台账` 期望 0；`handoff discipline get charter-implement | grep -c 边干边落台账` 期望 ≥1
  - 本机旁证：本开发机 `/root/.handoff/ledger.db` `discipline list` 为空表——种子确在协调者侧账本，对照无法在本机代跑，判据归位正确。
