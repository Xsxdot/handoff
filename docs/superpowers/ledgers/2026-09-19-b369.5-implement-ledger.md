# B369.5 implement 台账

节点：charter/implement
卡：B369.5（S5 CLI `console --qr`）
分支：cards/B369.5-charter-2
基线 HEAD（开工）：0af6c6b4（wip 抢救提交，含 b369.5-plan.md）
依据：`docs/superpowers/plans/b369.5-plan.md`（照 plan 执行，不重新探索）
规则：每确立一个事实立刻追加一行；无失败测试不写生产代码；变异自验确认有牙；提交事实记命令与原始输出。

---

## 2026-09-19 开工检查

- `git status`：On branch cards/B369.5-charter-2，working tree clean。
- `git diff --stat 4ab6415a..HEAD`：仅 `docs/superpowers/plans/b369.5-plan.md`（1057 行）——plan 已随 0af6c6b4 落盘。
- `go version`：go1.26.1 linux/amd64；`codegraph` 在 `/usr/local/bin/codegraph`。
- 纪律红线：不派发、不调 handoff CLI、不起新 executor；不切分支、不改 git config；临时文件用 $TMPDIR。

## T1 依赖：rsc.io/qr v0.2.0

- `go get rsc.io/qr@v0.2.0` → `go: added rsc.io/qr v0.2.0`（EXIT=0）。
- 首次 `go mod tidy` 把它清掉（当时 console.go 尚未 import）——预期；T3 落地后重加。
- T3 后 `go get rsc.io/qr@v0.2.0` → added；`go mod tidy` → EXIT=0。
- 结果：`go.mod:22` 主 require 块内 `rsc.io/qr v0.2.0`（非 indirect）；`go.sum` 加 2 行。
- 与 plan 一致：主 require、两行 sum。

## T2 图契约修订：codegraph/target.json

- 改前 `d_cli→d_protocol`：`legacyBudget:1`，entries `["proto 实体"]`。
- 改后：entries `["proto 实体","proto（包级函数）"]`，`legacyBudgetNote` 追加 B369.5 说明；预算仍 1。
- 依据：协调者工单 74f09134 裁决 A（plan §6 授权留痕）。

## T4 红灯：main 缝在实现前必红

- 写临时探针 `cmd/zz_qr_red_probe_test.go`（对两 target 配置跑 `console --bundle` 并断言可 `DecodePairBundle`）。
- `go test ./cmd/ -run TestConsoleQRBundleRedProbe -count=1` →
  ```
  --- FAIL: TestConsoleQRBundleRedProbe (0.00s)
      zz_qr_red_probe_test.go:36: console --bundle: unknown flag: --bundle
  FAIL
  EXIT=1
  ```
- 红因是「功能缺失」（unknown flag），非 typo；确认后删除探针。

## T3 实现：cmd/console.go

- 按 plan §T3 全量替换为给定代码（flags + 组装 + 渲染 + 互斥）。
- `go build ./...` → EXIT=0。

## T4 绿灯：cmd/console_test.go

- import 块按 plan 替换；文件末尾追加 9 支测试（roundtrip/部分 bundle/无 target/互斥/QR 产物/一行 URL/token 源码守卫/容量/超容量）。
- `go test ./cmd/ -run TestConsole -count=1 -v` → PASS（12 支：既有 3 + 新增 9）。
- 容量实测读数：`实测 8 机直连 bundle 字节数=1959（avg=244.9/机）`（plan 预期 1903–1957，落在断言上界 2600 内）。

## 变异自验（确认测试有牙）

每次变异先 `go build ./...` 确认编译通过（无 build failed/undefined），再跑行为断言。

1. **去掉 `Token: ep.Token` 赋值**（`m := proto.PairMachine{Name: ep.Name}`）：
   - 编译 OK；`TestConsoleQRBundleRoundTrip` 红：
     `console --bundle: 组装配对载体: proto: 配对载体畸形: machines[0]: token 不能为空`
   - 还原即绿。
2. **去掉 RunE 前置互斥守卫**（`if false && (consolePrintURL || consoleNoOpen)`）：
   - 首次跑 `TestConsoleOutputModesMutuallyExclusive` 仍绿 → 探针确认行为已变但测试没抓住。
   - 根因：互斥子例用无 target 配置，守卫缺失时 `runConsolePair` 仍因「无 target」报错——假绿。
   - 加固：子例改用**可达 fake agentd target** 配置，并断言 stdout 为空。
   - 复跑变异 → 红（`qr 与 print-url 互斥`、`bundle 与 no-open 互斥` 两支 FAIL）。还原即绿。
3. **去掉 `MarkFlagsMutuallyExclusive("qr","bundle")`**：
   - 编译 OK；`TestConsoleOutputModesMutuallyExclusive/qr_与_bundle_互斥` 红。还原即绿。
4. **离线机标空 ticket 而非无 ticket**（`m.Ticket = &proto.PairTicket{}`）：
   - 编译 OK；`TestConsoleBundlePartialOnOfflineTarget` 红：`离线机必须无 ticket（部分 bundle）`。还原即绿。

## T5 图视图 diff：codegraph/diffs/cards-B369-charter.json

- `nodesAdded` +5（`n_cmd_runConsolePair` 121 / `n_cmd_buildPairBundle` 186 / `n_cmd_pairMachineForTarget` 229 / `n_cmd_issuePairTicketForTarget` 259 / `n_cmd_renderBundleQR` 279）——行号按实现实际（与 plan 给定值逐一相同）。
- `edgesAdded` +13（plan 清单逐条）。
- 只跑 `codegraph` 二进制；符号未命中记图覆盖债（见下）。

## T6 收口复核（原始读数）

| 命令 | 结果 |
|---|---|
| `go build ./...` | EXIT=0 |
| `GOOS=windows GOARCH=amd64 go build ./...` | EXIT=0 |
| `go vet ./cmd/` | EXIT=0 |
| `gofmt -l cmd/console.go cmd/console_test.go` | 无输出 |
| `go test ./cmd/ -run TestConsole -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.024s` |
| `go test ./cmd/ -count=1` | `ok github.com/Xsxdot/handoff/cmd 39.691s` |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.067s` |
| `codegraph --repo . check --view cards-B369-charter` | `fails: []`；`d_cli->d_protocol` 不在 legacyHits；`d_cli->d_policy` 仍 35 |
| `codegraph --repo . validate --view cards-B369-charter` | `issues: null`、`edgeIssues: null` |
| `codegraph --repo . check`（纯基线） | `fails: []` |
| `codegraph --repo . validate`（纯基线） | issues None / edgeIssues None |
| `cd desktop && go mod tidy && git diff --exit-code -- desktop/go.mod desktop/go.sum` | EXIT=0（无输出，qr 不进 desktop 闭包） |

## 图覆盖债

- `codegraph sym consoleCmd --view cards-B369-charter` 返回「不在图中」，近似候选 `n_cmd_consoleCmd_RunE`（图只收 RunE 节点，不收命令变量）。回落 grep 确认定义于 `cmd/console.go:82`。本卡未改该变量语义，债记于此（与 plan §2 同）。
- 本卡新增 5 符号已随视图 diff 落盘；`cmd/console_test.go` 不计图边。

## 交付物

- `cmd/console.go`（改）、`cmd/console_test.go`（改）、`go.mod`、`go.sum`、`codegraph/target.json`（改）、`codegraph/diffs/cards-B369-charter.json`（改）。
- `internal/proto/pairing.go` 零改动（契约冻结）。
- CHANGELOG 按 plan §11 交 integrate/finish 补，本卡不越界。

## 提交事实

- `git add -A && git commit -m "feat(B369.5): CLI console --qr/--bundle 配对载体 ..."` 原始输出：
  ```
  [cards/B369.5-charter-2 c7ff206b] feat(B369.5): CLI console --qr/--bundle 配对载体
   7 files changed, 743 insertions(+), 5 deletions(-)
   create mode 100644 docs/superpowers/ledgers/2026-09-19-b369.5-implement-ledger.md
  COMMIT_EXIT=0
  c7ff206b2c37d25f94f3098ae43a6b309a54dd69
  ```
- 随后把本段追加进台账并 `--amend` 一次收进同批（amend 换 hash 是 git 事实；收口判据是工作树干净，不是台账 hash 等于 HEAD）。
