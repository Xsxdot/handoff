# B229 图对账复验轮台账（cards/B229-charter-6，task a361ad3a）

- 2026-08-25：开工核对 `git status && git branch --show-current && git log --oneline -3`；原始输出：工作树干净，当前分支 `cards/B229-charter-6`，HEAD 链 `725a4ee9 graph(b229) ← 0f625e02 docs(b229.7) ← 50296994 discipline(b229.7)`。
- 2026-08-25：分支名与计划报文出入的核实：计划写「本机工作分支 cards/B229-charter-5」，本回合实际被放在新建的 `cards/B229-charter-6` 上；`git rev-parse cards/B229-charter-5 cards/B229-charter-6` 原始输出两行均为 `725a4ee971f2a2825a4a8c123a5a8db3b9a6f018`，且 `git merge-base --is-ancestor` 确认 -5 是 -6 祖先（同一提交）。判断：-6 与 -5 同顶点，快进合并照计划在当前分支 -6 上执行即可达成期望终态（tip=fbada69d9），不切分支、不违铁律 3。
- 2026-08-25：`git fetch origin cards/B229-charter-5 && git merge --ff-only origin/cards/B229-charter-5 && git log --oneline -2`；原始输出：`Updating 725a4ee9..fbada69d`、`Fast-forward`、tip=`fbada69d contract(b229): 五处跨域预算按图对账裁决调整，逐条带放宽理由`，仅 `codegraph/target.json` 一文件 +16/-6。与计划期望 tip 完全一致。
- 2026-08-25：`git diff 725a4ee9..fbada69d9 -- codegraph/target.json` 逐条核对五处：① 新增契约 d_cli→d_gateway legacyBudget=1 带 note（依据 e24b9a31:cmd/agentd.go:273 srv.SetLedger 既存调用）；② d_cli→d_ledger 1→4 带 note；③ d_gateway→d_ledger 14→17 带 note；④ d_gateway→d_orchestration 319→321 带 note；⑤ d_gateway→d_transport 8→9 带 note（能力位探活 cl.Status 直调）。五条均写 legacyBudgetNote，与计划第二节清单一致。
- 2026-08-25：判据一复跑 `go run . graph check --repo . --view cards-B229-charter`（全文 /tmp/opencode/b229-recheck-view.txt）：`"fails": []`（EXIT=0）；warns 中恰 5 条 budget-raised：d_cli→d_gateway 新增契约携带存量预算 1、d_cli→d_ledger 1→4、d_gateway→d_ledger 14→17、d_gateway→d_orchestration 319→321、d_gateway→d_transport 8→9。legacyHits 读数 d_cli->d_gateway=1、d_cli->d_ledger=4、d_gateway->d_ledger=17、d_gateway->d_orchestration=321、d_gateway->d_transport=9，与五条契约预算逐一相等。与期望完全一致。
- 2026-08-25：note 降档口径独立核实：graph 命令树在 charter 外部模块（cmd/graph.go 仅挂载别名），模块缓存 `/root/go/pkg/mod/github.com/!xsxdot/charter/graph@v0.6.0/codegraph/fitness.go` 的 `ApplyBudgetRatchet` 文档注释明写「理由一律取当前 target 的 LegacyBudgetNote；TrimSpace 后非空才降为 warn，否则进 fails」「只有 budget-raised 会被 note 降档」。本机读数（5 条全在 warns、fails 空）与 target.json 五条非空 note 互证。
- 2026-08-25：判据二复跑 `go run . graph validate --repo .`（全文 /tmp/opencode/b229-recheck-validate.txt）：末行 `Error: 发现 14 个完整性问题、0 个失鲜节点`（EXIT=1，validate 有问题即非零退出）；issues 恰 14 条，全部为基线侧 no-import 边（internal/agentd/{manager,discipline}.go → internal/discipline 包级函数）；`grep -c '\[cards-B229-charter\]'`=0。views 列表含 cards-B229-charter。与期望一致。
- 2026-08-25：14 条基线问题的性质核实：`internal/discipline/` 现存文件仅 discipline/dispatch/platform 三源（resolver.go/files.go 已随 T 系列删除），而视图 `edgesDeleted` 共 17 条，其中 14 条与 validate issues 的 (from,to) 对一一对应（NewManager→Dir/NewResolver、Server_handleDiscipline*→Dir/Read/Write/List/Builtins/DefaultTierFor、Manager_resolveDisciplineFor→Compose 等），另 3 条为同族（Manager_Dispatch→Resolver_For、resumeForContinue→Resolver_For、Manager_ResumeTask→resolveDisciplineFor）。结论：分支代码删除的调用视图全部记了删除；baseline 残留属 absorb（finish 节点职权）前的预期陈旧，非本节点漏建。
- 2026-08-25：判据三复跑 `go run . graph check --repo .`（无视图，全文 /tmp/opencode/b229-recheck-nocheck.txt）：fails 恰 1 条 `dead-contract d_cli→d_gateway`（「声明的方向没有活跃 call、implements 或组装点豁免边」）；budget-raised warns 仍 5 条。与期望一致。
- 2026-08-25：dead-contract 过渡态三事实独立核实：① 视图 `edgesAdded` 含 `n_cmd_setupLedger→n_agentd_Server_SetLedger` 及 `n_cmd_agentdCmd_RunE→n_cmd_setupLedger`，带视图 check 时该方向有活跃边故 fails 空；② `python3` 全文扫描 `setupLedger` 在 codegraph/baseline.json 中不出现（baseline 尚无该符号，合并前 dead-contract 必然出现）；③ `go run . graph absorb --help` 首行原文「把分支视图 diff 併入 baseline 并删除该 diff（分支合并回主线后执行）」；④ 代码事实 cmd/agentd.go:451 `srv.SetLedger(lst)` 在 setupLedger 内（:440 起）。裁定：该 dead-contract 属合并前过渡态而非新问题，不阻断。
- 2026-08-25：新符号抽查（自检）：`graph sym n_cmd_setupLedger --view cards-B229-charter` 命中（d_coordination_cli，cmd/agentd.go:440，anchor ok）；`n_ledgerstep_PreflightDiscipline` 命中（d_ledger，internal/ledgerstep/dispatch.go:338，anchor ok）；`n_ledger_Store_PutDiscipline` 命中（d_ledger，internal/ledger/disciplines.go:43，anchor ok）；`n_agentd_Server_resolveStepDiscipline` 命中（d_coordination_api，cardstep.go:128，anchor ok）。首查 `n_agentd_Server_PreflightDiscipline` 未命中系本人猜错包归属（真身在 ledgerstep），图覆盖债：无。
- 2026-08-25：视图是否需随契约更新：不需要——带视图 check fails 已空、五契约 legacyHits 与预算相等，说明 725a4ee971 重建的实现期全量视图已天然满足新契约面；本轮未改任何 codegraph 文件，红线（不动 target/baseline/best/业务代码）全程未触碰。
- 2026-08-25：自查：① 本台账所有结论均来自本回合亲自执行并落盘的命令输出；② 台账边干边追加；③ 本回合仅运行只读 graph 子命令（check/validate/sym/absorb --help），未派发任务、未起新 executor。
