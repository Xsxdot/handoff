# B233.7 plan 台账

本台账按计划编写回合追加；只记录本工作树实际执行的命令和原始输出，不把 spec/contract/breakdown 台账的历史读数冒充本回合验证。

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-08 | 本回合在 `cards/B233.7-charter-3` 工作，初始工作树干净；HEAD 为 `ec61dfc5`。本卡计划不得切换分支、不得越过冻结基线。 | `git status --short --branch && git log -1 --oneline` → `## cards/B233.7-charter-3`；`ec61dfc5 docs(B233.7): 拍板拆解 P1-B/P2-A/P3-A/P4-A` |
| 2026-09-08 | 输入 spec、contract、breakdown 均可读；冻结基线文本为 `cards/B233.1-charter-7 @ 497a7712`；P1-B、P2-A、P3-A、P4-A 已拍板。 | `sed -n '1,80p' docs/superpowers/specs/b233.7.md docs/superpowers/specs/b233.7-contract.md docs/superpowers/specs/b233.7-breakdown.md` → 三份文件均输出；breakdown 待拍板清单逐字包含 `P1 ... 拍板 B`、`P2 ... 拍板 A`、`P3 ... 拍板 A`、`P4 ... 拍板 A`。 |
| 2026-09-08 | 代码图命令可用。最佳领域查询应使用 `d_orchestration`、`d_execution`/`d_execution_adapters`、`d_gateway`、`d_workspace`、`d_execution_host`、`d_scheduling`、`d_keystone`、`d_transport`；现状 `d_coordination_task` 不是最佳词表 id。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . domains` → 退出码 0；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_coordination_task` → 退出码 1，原始错误：`Error: 领域 "d_coordination_task" 不在最优树词表中；context 已按最优树词表取声明，最优树领域候选: d_cli, d_collab, d_execution, d_execution_adapters, d_execution_contract, d_execution_host, d_gateway, d_keystone, d_ledger, d_maintenance, d_orchestration, d_policy, d_protocol, d_scheduling, d_sessions, d_transport, d_transport_channel, d_transport_tunnel, d_web, d_web_admin, d_web_cards, d_web_command, d_web_contract, d_web_shell, d_web_workbench, d_workspace。"d_coordination_task" 是**现状视图**领域 id，它的容器分布在: d_orchestration(10 容器)、d_maintenance(5 容器)、(best 未归位)(3 容器)、d_policy(3 容器)、d_workspace(2 容器)、d_gateway(1 容器)、d_web_shell(1 容器)——请按需改用其中一个最优树 id（分布非单值，不代为改写）`；`context d_scheduling` → 退出码 0，warning 指向缺失 `codegraph/domains/d_scheduling.json`。 |
| 2026-09-08 | 图已命中 Dispatch、RelayAnswer、WakeHome、Admit、handleTaskDiff、handleQuestion 的部分入口；`flow` 对 Dispatch/RelayAnswer/WakeHome 有序步骤可用，handleTaskDiff/handleQuestion 的 flow 为 degraded 空步骤，不能拿 chain 代替；`n_executor_Profile` 未被图收录。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym/flow/who-calls ...` → 成功入口的原始响应分别报告 `n_agentd_Manager_Dispatch` 签名 `func (m *Manager) Dispatch(ctx context.Context, req DispatchReq) (task *proto.Task, err error)`、`n_agentd_Manager_RelayAnswer` 签名 `func (m *Manager) RelayAnswer(taskID, ticketID, answer string) error`、`n_hostapi_Host_WakeHome` 签名 `func (h *Host) WakeHome(ctx context.Context, req WakeRequest) (WakeReply, error)`、`n_scheduling_Service_Admit` 签名 `func (s *Service) Admit(req IgnitionRequest) (Binding, error)`；`sym n_executor_Profile` → `Error: 符号 "n_executor_Profile" 不在图中（图未覆盖或名字有误）；近似候选: []`。 |
| 2026-09-08 | 基线可编译，且既有 Ask、ResultRef fallback、diff 目标、OpenCode wait-question 定向回归通过；五家 executor/profile、hostapi、scheduling、workspace、client 触及包现状通过。 | `go build ./...` → 退出码 0，标准输出为空；`go test ./internal/agentd -run 'TestQuestionReissueAfterAnswerCreatesNewTicket|TestOpenCodeWaitQuestionUsesRespondAsk|TestTaskDiffTargetFallsBackToRepoWhenWorktreeGone|TestAssembleResultRefEmptyCommitAllowed' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.376s`；`go test ./internal/executor/... ./internal/hostapi ./internal/scheduling ./internal/workspace ./internal/client -count=1` → 各触及包均 `ok`，OpenCode `18.745s`、hostapi `0.647s`、scheduling `4.058s`、workspace `0.011s`、client `9.633s`。 |
| 2026-09-08 | WakeHome 现有真实输出/凭据反例基线通过；B350 竖切基线按冻结规则失败，说明 S5 先改测试断言即可锁定物理载体身份。 | `go test ./internal/hostapi -run 'TestWakeHomeReadyRequiresTurnOutputNotCredFile|TestWakeHomeSuppliesMainCredentialBeforeTurn|TestWakeHomeOccupiedNeverOverwrites' -count=1 -v` → 三测试均 `PASS`，末尾 `ok github.com/Xsxdot/handoff/internal/hostapi 0.022s`；`go test ./internal/keystone/ -run '^TestIgnitionVerticalSlice$' -count=1 -v` → `slice_test.go:157: 绑定解析错误: {Squad:exec-1 Carrier:opencode-1 Target:linux-01 Executor:opencode Model: HomeDir:/home/coordinator/.opencode-home}（期望载体 opencode-1 / 机 linux-01 / 执行者覆盖 claude）`，末尾 `FAIL github.com/Xsxdot/handoff/internal/keystone 0.158s`。 |
| 2026-09-08 | 仓库图门禁基线仍有既有 dead-contract/dead-interface；本卡计划不修改图契约或把该失败写成实现通过。 | `go test ./cmd -run '^TestRepoContractGate$' -count=1` → `--- FAIL: TestRepoContractGate`；`graph_gate_test.go:38: 契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边`；`graph_gate_test.go:38: 契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在`；末尾 `FAIL github.com/Xsxdot/handoff/cmd 0.043s`。 |
| 2026-09-08 | 法定计划已写入 `docs/superpowers/plans/b233.7-plan.md`，覆盖 S0–S6、P1-B/P2-A/P3-A/P4-A、精确接口、文件边界、定向基线、红绿接缝、日志/注释、五项自审与真机清单；本节点未修改运行时代码。 | `wc -l docs/superpowers/plans/b233.7-plan.md` → `751 docs/superpowers/plans/b233.7-plan.md`；`git status --short` → 两个新文件：计划与本台账。 |
| 2026-09-08 | 计划格式校验通过；图锚修订后 resolve 通过，保留 `bindingFor` moved 与 `API.do` ok 的实际响应。首次 resolve 曾因非图覆盖的 coordinator_home 方法锚失败，已改为 plain path + 方法名。 | `git diff --check` → 退出码 0；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/plans/b233.7-plan.md` → 退出码 0，anchors：`internal/scheduling/scheduling.go#bindingFor`=`moved`、`internal/executor/opencode/api.go#API.do`=`ok`；首次原始失败为 `Error: 文档锚点检查失败: internal/agentd/coordinator_home.go#coordinatorHomeSupplier.Prepare (vanished)`。 |
| 2026-09-08 | 占位符扫描只命中计划自带的扫描规则、合规例外声明与跨卡审计“待拍板”状态；计划已明确这不是实现占位，且测试 harness 例外逐条列出断言和既有文件。 | `rg -n 'TBD|TODO|占位|同 Task|加适当的错误|待拍板' docs/superpowers/plans/b233.7-plan.md` → 命中行 `683`、`686`、`689`、`740`、`748`、`750`；无其它未声明占位文本。 |
| 2026-09-08 | 初次提交本节点计划与台账；随后只 amend 一次收录本行。amend 会更换 HEAD hash，台账不追写 amend 后 hash。 | `git commit -m "docs(B233.7): 编写质量复审核销计划"` → `[cards/B233.7-charter-3 9f9d7512] docs(B233.7): 编写质量复审核销计划`；`2 files changed, 779 insertions(+)`；新增 plan 与 plan ledger。 |
