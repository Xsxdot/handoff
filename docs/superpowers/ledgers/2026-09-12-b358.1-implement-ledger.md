# B358.1 implement 台账（2026-09-12）

执行者：charter:implement 节点。依据：`docs/superpowers/plans/b358.1-plan.md`（含协调者拍板四条裁决）。
分支 `cards/B233.1-charter-7`，起点 `7c8bc65f`（plan 提交）。

## §一 Task 0 基线复跑（2026-09-12）

判据五条 + 红窗两处，读数与 plan §1 一致：

```
$ go build ./...
BUILD_EXIT_0（退出 0，无输出）

$ go test ./internal/collab/... -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	1.339s
?   	github.com/Xsxdot/handoff/internal/collab/client	[no test files]
?   	github.com/Xsxdot/handoff/internal/collab/cursor	[no test files]
ok  	github.com/Xsxdot/handoff/internal/collab/room	0.461s

$ grep -rn "KindAllowed" internal/ cmd/    （退出 0，恰 3 命中）
internal/collab/service.go:81:	if !room.KindAllowed(msg.Kind) || msg.Kind == proto.RoomMsgPointer {
internal/collab/room/room.go:164:// KindAllowed kind 是否在受控词表内。
internal/collab/room/room.go:165:func KindAllowed(kind string) bool {

$ go test ./cmd -run TestRoom -count=1
ok  	github.com/Xsxdot/handoff/cmd	0.799s

$ go test ./internal/agentd -run 'TestRoomMessages|TestRoomSend|TestRoomRead' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	0.721s
```

红窗基线（S1 前全绿确认，verbose 枚举）：

```
$ go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1 -v
--- PASS 全部 21 支，其中 §0.4 清单 10 支全绿：
TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection / TestRoomMessagesEndpoint /
TestRoomSendEndpoint / TestRoomReadEndpoint / TestInboxThreeSources / TestInboxMentionSource /
TestInboxRefIDShapes / TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke
存活确认（S1 后不得误改）：TestRoomSendErrMapping PASS（在 agentd 包）。

$ go test ./cmd -run TestRoom -count=1 -v
--- PASS 全部 8 支，其中 §0.4 清单 6 支全绿：
TestRoomSendLandsRoomMessageWithUserKind / TestRoomSendCoordinatorAcceptsExplicitSeatFlags /
TestRoomSendCoordinatorKindUsesGrokHostSession / TestRoomSendCarriesRefAndMention /
TestRoomReadStdoutSeqPrefix / TestRoomListSortedByActivity
存活确认：TestRoomSendUserRejectsSeatFlagsWithoutSideEffects PASS。
```

结论：红窗是 S1 制造的，不是继承的。plan §0.4 清单与实测一致（16 支，agentd 10 + cmd 6）。

## §二 图覆盖债读数（2026-09-12）

亲跑 `codegraph sym`，触及符号全部「不在图中」（与 plan §0.3 / breakdown §6 同欠，本卡全部触及面不在 best.json）：

```
$ codegraph sym collab.Service / collab.Service.Send / collab.Service.Pointer /
  room.Resolve / room.VerifyWriter / collab.consumeRoomMentions /
  collab.sendToSession / collab.sessionWriters
→ 逐个返回：Error: 符号 "…" 不在图中（图未覆盖或名字有误）；近似候选: []
```

处置：本卡一律 file#Symbol 源码锚；新增/修改符号清单见 §四第 4 条，交 integrate/recon 补 `codegraph/diffs` 视图。

## §三 红绿记录

### Task 1

**T1 步骤 2-4（红）**：service_test.go 追加 `mustSession`/`TestSendOldRoomsArchivedReadOnly`/`TestSessionArchiveSendReadOnly`（判据原文在 plan §4），基线上跑红，红因=功能缺失非 typo：

```
$ go test ./internal/collab -run 'TestSendOldRoomsArchivedReadOnly|TestSessionArchiveSendReadOnly' -count=1
--- FAIL: TestSendOldRoomsArchivedReadOnly (0.01s)
    service_test.go:577: 旧房间 B1 发言必须 ErrReadOnly，got <nil>
--- FAIL: TestSessionArchiveSendReadOnly (0.01s)
    service_test.go:615: 归档会话发言必须 ErrReadOnly，got <nil>
FAIL
FAIL	github.com/Xsxdot/handoff/internal/collab	0.566s
```

（基线行为与 plan §1 判据先行确证一致：旧卡房间发言成功、归档会话发言成功。）


**T1 步骤 5（绿）**：service.go 换 Send 终局编排（Task 1 形）+ 新增 sendToSession + consumeRoomMentions 过滤条件改 `room.SameRoom`：

```
$ go build ./internal/... && go test ./internal/collab -run 'TestSendOldRoomsArchivedReadOnly|TestSessionArchiveSendReadOnly' -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	0.579s
```

**T1 步骤 6（红→替换）**：旧版 TestSendConsumesRoomMentions（卡房间 relay 夹具）在 S1 语义下红，红因=卡房间发言被拒（预期行为变化，非回归）：

```
$ go test ./internal/collab -run TestSendConsumesRoomMentions -count=1
--- FAIL: TestSendConsumesRoomMentions (0.01s)
    service_test.go:538: collab: 房间已只读（并入冻结或终态归档）
```

**T1 步骤 7（夹具迁移，包绿）**：7a service_test.go 4 支（竖切/无卡事件/空actor/提及过滤）迁会话夹具；7b 2 支（History 对质×2）Send→Pointer 夹具；7c 删 8 支矩阵锁死测试（，替代落点照 plan §4 表）；7d readmodel_test.go 10 支迁会话夹具（Pending×2/Consume×4/Mentions×1/Unmerge×1/未读×2）+ 文件头加 `ledgerapi` import（测试文件 import，不构成生产边）。

**偏差 #1（自主消化，上报）**：plan §0.4 红窗清点漏项——`internal/collab/cursorfile_test.go` 2 支（TestCursorFilePersistsAcrossInstances/TestCursorConcurrentMarkRead）夹具用卡房间 Send 造水位消息，S1 落地即红（`collab: 房间已只读`）。判定：两测试锁的是游标文件介质（持久化/并发原子写），与房间语义正交；处置=按 plan 自身夹具迁移模式（卡房间 Send→会话房间 Send，发送者=群主）迁移，断言零改动。依据：plan Task 1 步骤 9 硬判据「`go test ./internal/collab/...` 必须 ok」+ plan 7a-7d 同款迁移先例；不属「改判据迁就实现」。plan §0.4/§0.1 未列此文件的漏项记入本行，交协调者对账。

**T1 扩集（拍板 ①：Store 级幂等短路，红→绿）**：新建 `internal/ledger/sessions_test.go`（拍板 ① 显式扩进的对应测试文件）：

```
$ go test ./internal/ledger -run TestArchiveSessionIdempotentLandsExactlyOneEvent -count=1   # 修复前
--- FAIL: TestArchiveSessionIdempotentLandsExactlyOneEvent (0.01s)
    sessions_test.go:58: 全生命周期应恰一条 EvSessionArchived（重复归档不叠加），got 2
# Store.ArchiveSession 加已归档短路后
$ go test ./internal/ledger -run TestArchiveSessionIdempotentLandsExactlyOneEvent -count=1
ok  	github.com/Xsxdot/handoff/internal/ledger	0.573s
```

TestArchiveSessionLandsExactlyOneEvent（collab 侧）按拍板 ① 强读法写：重复归档幂等 nil + 全生命周期恰一条（plan 原稿的「单次半边」注释已被拍板裁决取代）。

**T1 步骤 8**：sessions_test.go 扩 TestSessionArchiveReadOnly（条 7 加「拒绝路径不落 EvSessionCardJoined」断言）+ 新增 TestArchiveSessionLandsExactlyOneEvent（条 6 强读法）/TestSessionOutlivesCardTerminal（条 13）。

**T1 步骤 9-10（验收）**：

```
$ go test ./internal/collab -run 'TestSendOldRoomsArchivedReadOnly' -count=1    → ok 0.322s
$ go test ./internal/collab -run 'TestSessionArchiveSendReadOnly' -count=1      → ok 0.338s
$ go test ./internal/collab -run 'TestSendConsumesRoomMentions' -count=1        → ok 0.329s
$ go test ./internal/collab -run 'TestListRooms|TestHistory' -count=1           → ok 0.366s
$ go test ./internal/collab -run 'TestSessionArchiveReadOnly|TestSessionOutlivesCardTerminal|TestArchiveSessionLandsExactlyOneEvent' → ok 0.343s
$ go test ./internal/collab -run 'TestSendRejectsUnknownRoom' -count=1          → ok 0.392s（ErrNoRoom 语义未漂移，缺陷族 2 佐证）
$ go test ./internal/collab/... -count=1   → collab ok 1.075s / room ok 0.233s
$ go build ./...                            → 退出 0
$ go test ./internal/ledger -count=1        → ok 2.319s（触及包 ledger 全绿，短路修复无波及）
```

日志自查：旧房间拒收 Info、归档拒收 Warn、落账 Info、消费失败 Warn（room/actor/seq/cause 字段齐）——已内嵌于 Send/sendToSession/consumeRoomMentions。

### Task 2

**T2 步骤 1-2（红）**：delivery_gate_test.go 追加 AST 守卫 + service_test.go 追加 kind 自由测试，双红：

```
$ go test ./internal/collab/room -run TestVerifyWriterHasNoKindBranches -count=1
--- FAIL: TestVerifyWriterHasNoKindBranches (0.00s)
    delivery_gate_test.go:100: VerifyWriter 形参 "kind"——kind 书写者矩阵已废止（B358 spec §4.2），按 kind 分权的形状不得回潮
    delivery_gate_test.go:107: VerifyWriter 引用 proto.RoomMsgUser——按 kind 分权的矩阵回潮
    delivery_gate_test.go:107: VerifyWriter 引用 proto.RoomMsgRelay——按 kind 分权的矩阵回潮
$ go test ./internal/collab -run TestSessionKindFreedom -count=1
--- FAIL: TestSessionKindFreedom (0.01s)
    service_test.go:433: 词表外 kind 应放行（发言自由，spec §4.2）: collab: 书写者与房间身份不符
```

**T2 步骤 3（最小实现）**：room.go——VerifyWriter 换过渡形 `(actor string)`（旧矩阵体整体删除）、KindAllowed 函数删除、Room.ParentSession 字段与 Resolve 父卡查表块删除、新增 strings import；errors.go——ErrKindNotAllowed 注释更新（哨兵本体与错误文本不动）；service.go——sendToSession 调用点改过渡签名（Send 顶部 pointer 拒收已在 T1 落位）。

**T2 步骤 4（绿 + grep 验收）**：

```
$ go build ./... && go test ./internal/collab/... -count=1
ok  collab 1.526s / ok  room 0.465s
$ grep -rn "KindAllowed" internal/ cmd/    → 无输出，GREP_EXIT=1（零命中；ErrKindNotAllowed 不含子串不受扰）
$ go test ./internal/collab/room -run TestVerifyWriterHasNoKindBranches -count=1 → ok
$ go test ./internal/collab -run 'TestSessionKindFreedom|TestSendRejectsPointerViaSend' -count=1 → ok
```

**T2 步骤 5（枚举残留面核对）**：`grep -rn "RoomMsg" internal/collab --include="*.go" | grep -v _test` 实测 6 命中 / 5 处，逐处理由：

1. service.go:87 Send `proto.RoomMsgPointer` 拒收——pointer 专用入口守卫，非白名单；
2. service.go:126 sendToSession `proto.RoomMsgUser`——B287 触发条件（回复即清提及的行为语义），非唤醒白名单；
3. service.go:177 Pointer `proto.RoomMsgPointer` 置位——既有；
4. service.go:253 Pending `RoomMsgUser` 比较——读侧过滤（绑定卡房间历史留言），plan 同列理由；
5. room/delivery.go:31 + :67 `proto.RoomMsgPointer`——寻址纯函数系统行判别（Ticket 0 冻结语义，contract 条 26）。plan §5 步骤 5 写「四处」，实测 delivery.go 有两处（ResolveDelivery 与其伴随纯判定 IsAddressed）——同一理由（寻址纯函数系统行判别）覆盖，非新增白名单形状，记入本行供 review 对账。

`wakeconsumer/card_wait` 的 RoomMsgUser 残留不在 internal/collab，归 S3（其 grep 断言负责）。`proto.RoomMsg*` 词表常量不删（wire 兼容）。

### Task 3

**T3 步骤 1（红）**：service_test.go 追加 TestSessionMembershipEnforcement + assertActor + TestSessionRebindRevokesOldSeatWriter，执法未接入时双红：

```
$ go test ./internal/collab -run 'TestSessionMembershipEnforcement|TestSessionRebindRevokesOldSeatWriter' -count=1
--- FAIL: TestSessionMembershipEnforcement (0.02s)
    service_test.go:495: 非成员发言必须 ErrNotWriter，got <nil>
--- FAIL: TestSessionRebindRevokesOldSeatWriter (0.01s)
    service_test.go:560: 换绑后旧席位必须 ErrNotWriter，got <nil>
```

**T3 步骤 2（最小实现）**：room.go——VerifyWriter 升终形 `(writers []string, actor string)`；service.go——新增 sessionWriters（显式成员 ∪ 会话内卡当前席位，ListAllCards 全量投影，读失败向上传播），sendToSession 中段换成终形调用。

**T3 步骤 3（绿）**：

```
$ go build ./... && go test ./internal/collab/... -count=1
ok  collab 1.530s / ok  room 0.449s
$ go test ./internal/collab -run 'TestSessionMembershipEnforcement|TestSessionRebindRevokesOldSeatWriter' -count=1 → ok
$ go test ./internal/collab/room -run TestVerifyWriterHasNoKindBranches -count=1 → ok（终形对 AST 守卫仍绿：无 kind 形参、无 RoomMsg 引用）
```

门禁单一（源码事实，plan Task 3 验收表末行）：HTTP `handleRoomSend`（agentd roomsapi.go 直调 s.rooms.Send）与 CLI `cmd/room.go` room send 同走 `Service.Send` 一道门；0.2#15 的 CLI flag 层拒绝先于 svc.Send。行为面呈现归 S4/S5 端点测试。

### Task 4

**T4 第 1 步（breakdown §3.2 ③ 四条判据复跑）**：

```
$ go build ./...                                              → BUILD_OK（退出 0）
$ go test ./internal/collab/... -count=1                      → ok collab 0.951s / ok room 0.238s
$ grep -rn "KindAllowed" internal/ cmd/                       → 无输出，GREP_EXIT=1（零命中）
$ go test ./internal/collab -run 'TestListRooms|TestHistory' -count=1 → ok 0.381s
```

**T4 第 2 步（红窗复核：失败集合必须恰等于 plan §0.4，无新增）**：

```
$ go test ./cmd -run TestRoom -count=1 -v   → --- FAIL 恰 6 支：
TestRoomListSortedByActivity / TestRoomReadStdoutSeqPrefix / TestRoomSendCarriesRefAndMention /
TestRoomSendCoordinatorAcceptsExplicitSeatFlags / TestRoomSendCoordinatorKindUsesGrokHostSession /
TestRoomSendLandsRoomMessageWithUserKind
$ go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1 -v → --- FAIL 恰 10 支：
TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke / TestInboxMentionSource / TestInboxRefIDShapes /
TestInboxThreeSources / TestRoomMessagesEndpoint / TestRoomReadEndpoint / TestRoomSendEndpoint /
TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection
存活测试确认仍绿：TestRoomSendErrMapping、TestRoomsEndpoints503WithoutLedger（agentd）；
TestRoomSendUserRejectsSeatFlagsWithoutSideEffects、TestRoomInboxWalksAgentdHTTP（cmd）。
```

红窗集合 == plan §0.4 清单，无新增（偏差 #1 的 cursorfile 2 支在 collab 包内、已按夹具迁移消化，不属 agentd/cmd 红窗）。处置按拍板 ②：归 S4/S5，本卡不改。

**T4 第 3 步（变异抽查两发，缺陷族 8 承重安全属性）**：

发①（sessionWriters 加 actor → 人人可写）：plan 原文「把 sessionWriters 返回 append(writers, actor)」按字面在该函数体内不可编译（`actor` 不在 sessionWriters 作用域，`undefined: actor`）——按 implement 纪律「编译不过的变异不算数」，替换为可编译等价变异（sendToSession 调用点 `writers = append(writers, actor)`，语义同为「actor 恒在成员集」）：

```
$ go test ./internal/collab -run 'TestSessionMembershipEnforcement|TestSessionRebindRevokesOldSeatWriter' -count=1   # 变异中
--- FAIL: TestSessionMembershipEnforcement / --- FAIL: TestSessionRebindRevokesOldSeatWriter
# 还原后
$ 同命令 → ok 0.359s
```

发②（注释掉 sendToSession 的 session.Archived 拒收，`if false && session.Archived` 可编译形）：

```
$ go test ./internal/collab -run TestSessionArchiveSendReadOnly -count=1   # 变异中
--- FAIL: TestSessionArchiveSendReadOnly (0.01s)
# 还原后
$ 同命令 → ok 0.335s
```

两发均先红后绿；变异未提交（还原后 `git diff internal/collab/service.go` 仅含本卡计划改动）。

**T4 第 4 步（图覆盖债符号清单，交 integrate/recon 补 codegraph/diffs）**：

- 修改（生产）：`collab.Service.Send`（internal/collab/service.go）、`collab.consumeRoomMentions`（service.go）、`room.VerifyWriter`（签名三形变迁，终形 `(writers []string, actor string) error`）、`room.Room`（删 ParentSession 字段）、`room.Resolve`（删父卡查表）、`room.ErrKindNotAllowed`（仅注释）、`ledger.Store.ArchiveSession`（internal/ledger/sessions.go，拍板 ① 已归档短路）。
- 新增（生产）：`collab.Service.sendToSession`、`collab.Service.sessionWriters`。
- 删除（生产）：`room.KindAllowed`、`room.Room.ParentSession`。
- 测试符号（internal/collab）：新增 `TestSendOldRoomsArchivedReadOnly`、`TestSessionArchiveSendReadOnly`、`TestSessionKindFreedom`、`TestSessionMembershipEnforcement`、`TestSessionRebindRevokesOldSeatWriter`、`TestArchiveSessionLandsExactlyOneEvent`、`TestSessionOutlivesCardTerminal`、`TestSendRejectsEmptyActor`（替代 TestSendRejectsEmptyActorForUserKind）、`assertActor`、`mustSession`、`TestVerifyWriterHasNoKindBranches`（room 包）、`TestArchiveSessionIdempotentLandsExactlyOneEvent`（ledger 包）；重写迁移 `TestSendUserMessageVerticalSlice`/`TestSendGroupMessageLandsAsCardlessEvent`/`TestMentionsFiltersByMember`/`TestHistoryFiltersNonRoomEvents`/`TestHistoryReturnsNewestWindow`/`TestSendConsumesRoomMentions`/`TestSessionArchiveReadOnly`/readmodel_test.go 10 支/cursorfile_test.go 2 支；删除 8 支矩阵锁死测试（T1 步骤 7c 清单）。

## §八 自审结论（Task 4 第 5 步收口）

- 每条错误分支带上下文日志：旧房间拒收（Info，room/kind/actor）、会话归档拒收（Warn，room/actor）、书写者被拒（Warn，actor/writers）、书写者集组装失败（Warn，session/cause）、消费失败两处（Warn，room/member/seq/cause）——齐；成功路径出口日志：落账 Info（room/kind/actor/seq）、指针行 Info——齐。
- 新文件头注释：internal/ledger/sessions_test.go 有职责+边界头；导出函数文档注释：Store.ArchiveSession/VerifyWriter/sendToSession/sessionWriters 均更新。
- 触及包测试绿（collab/collab/room/ledger）、`go build ./...` 退出 0。
- 与 plan Interfaces 签名一致：VerifyWriter 终形 `(writers []string, actor string) error`、sessionWriters `([]string, error)`、sendToSession 私有签名同 plan；client.LedgerClient 未动。
- 收尾提交：受控文件集 = 9 个修改生产/测试文件 + internal/ledger/sessions.go/sessions_test.go（拍板 ① 扩集）+ 本台账；无关脏文件（web/src/app/task/Composer.test.tsx、.commandcode/、docs/superpowers/plans/b353-probe-follow-harness.md、web/design-qa*）不卷入。

## §九 提交事实

```
$ git add internal/collab/{service.go,service_test.go,sessions_test.go,readmodel_test.go,cursorfile_test.go} \
    internal/collab/room/{room.go,errors.go,delivery_gate_test.go} \
    internal/ledger/{sessions.go,sessions_test.go} \
    docs/superpowers/ledgers/2026-09-12-b358.1-implement-ledger.md
$ git commit -m "implement(B358.1): 会话语义收口与旧规则废止（T0–T4）"
[cards/B233.1-charter-7 d92f0b92] implement(B358.1): 会话语义收口与旧规则废止（T0–T4）
 11 files changed, 924 insertions(+), 328 deletions(-)
```

（本节随 amend 一次收进同批提交，amend 换 hash 属 git 事实，不 chase；收口判据=受控文件集工作树干净。无关脏文件 web/src/app/task/Composer.test.tsx、.commandcode/、docs/superpowers/plans/b353-probe-follow-harness.md、web/design-qa-sidebar-after.png、web/design-qa.md 保留在工作树未卷入。）
