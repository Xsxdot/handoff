# B312 plan 节点台账

- 2026-09-03：开工命令 `git status --short --branch`；原始输出 `## cards/B312-charter-3`，工作树无改动，当前分支保持不变。
- 2026-09-03：实读上游 spec `docs/superpowers/specs/2026-09-03-b307-bind-current-session-design.md`、contract `docs/superpowers/specs/b312-contract.md`、breakdown `docs/superpowers/specs/b312-breakdown.md`；三份输入均可读。按本回合补充裁决固定 D1 方案甲（CardView 增加可选字段并走 Go→TS 投影）、D2 方案乙（CLI bind/rebind --self 走本机账本出示，不经 HTTP body Identity；HTTP rebind 仅 mode=launch；Web 永不 mode=self）、D3 方案乙（Launch 后 CAS 冲突不自动 kill，新会话返回 409 与 session_id 供人工回收）。
- 2026-09-03：`command -v codegraph` 无输出，随后直接调用 `codegraph --repo . ...` 的原始错误均为 `/bin/bash: line 2: codegraph: command not found`；按平台不变量改用 `GOMODCACHE=/root/.handoff/tmp/31aa1c4e/gomodcache GOCACHE=/root/.handoff/tmp/31aa1c4e/gocache go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . ...`。
- 2026-09-03：误把 `GOMODCACHE=... GOCACHE=...` 作为 shell 命令前缀变量执行的 codegraph 尝试失败；原始错误为 `/bin/bash: line 2: GOMODCACHE=/root/.handoff/tmp/31aa1c4e/gomodcache: No such file or directory`（重复出现 7 次）。后续改用真正的环境变量参数后成功。
- 2026-09-03：上述官方 `go run` 实跑 `summary`，原始结果 `本仓库有代码图：4007 节点 / 4965 边 / 23 领域（codegraph/）`；实跑 `context` 的最佳领域为 `d_protocol`、`d_ledger`、`d_gateway`、`d_keystone`、`d_collab`、`d_cli`、`d_web`，并按上下文输出的源码窗口核对后续计划边界。
- 2026-09-03：对 `internal/ledger/binding.go#Store.ClaimCardAs`、`Store.RebindDriver`、`internal/keystone/keystone.go#Service.Wake`、`internal/collab/room/room.go#VerifyWriter`、`internal/collab/service.go#Service.Pending`、`internal/agentd/server.go#coordinatorRunner.Resume`、`internal/client/squads.go#Client.CoordinatorLaunch`、`web/src/api/scheduling.ts#launchCoordinator` 实跑 `codegraph sym`；每项原始结果均为 `Error: 符号 "..." 不在图中（图未覆盖或名字有误）；近似候选: []` 并退出码 1。计划将这些现状锚记为图覆盖债，改用 `rg`/源码。
- 2026-09-03：基线命令 `GOMODCACHE=/root/.handoff/tmp/31aa1c4e/gomodcache GOCACHE=/root/.handoff/tmp/31aa1c4e/gocache go test ./internal/proto/... ./internal/ledger/... ./internal/keystone/... ./internal/collab/... ./internal/client/... ./internal/agentd/... ./cmd/... -count=1` 实跑退出码 0；原始结果含 `ok github.com/Xsxdot/handoff/internal/proto 0.010s`、`ok .../internal/ledger 21.785s`、`ok .../internal/ledger/api 0.869s`、`ok .../internal/keystone 0.215s`、`ok .../internal/collab 5.436s`、`ok .../internal/client 9.569s`、`ok .../internal/agentd 174.328s`、`ok github.com/Xsxdot/handoff/cmd 17.385s`，以及三个 collab 子包的 `[no test files]`。
- 2026-09-03：Web 基线命令 `npm test -- --runInBand` 在 `web/` 实跑退出码 127，原始输出为 `> web@0.0.0 test`、`> vitest run --runInBand`、`sh: 1: vitest: not found`；未安装依赖，不把 Web 测试写成已通过。
- 2026-09-03：Web 基线命令 `npm run typecheck` 在 `web/` 实跑退出码 127，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`、`sh: 1: tsc: not found`；未安装依赖，计划将其列为实现节点的阻断性前置检查。
- 2026-09-03：只读检查确认 `web/package-lock.json` 存在；计划把 `npm ci` 作为 Web 实现节点恢复其既定测试工具链的明确前置命令。
- 2026-09-03：源码核对确认 `internal/proto/seat.go` 已有并通过身份金样本的 `EncodeSeatIdentity`/`ParseSeatIdentity` 骨架；当前 `proto.CardView`、ledger/agentd/cmd 三处卡投影没有 `driver_source`，当前 `internal/proto/scheduling.go` 没有 `CoordinatorRebindReq`。
- 2026-09-03：源码核对确认当前 `internal/agentd/coordapi.go` launch 只接受 `manual`/`card_create`，`internal/agentd/ledgerapi.go` 仍注册旧 `/api/cards/{id}/rebind`；当前 `internal/client/coordinator.go` 仍提供 source 专用 `CoordinatorLaunchAs`，当前 `cmd/card_coordinate.go` 与 Web 新建对话框仍有 `card_create` 拉起路径。
- 2026-09-03：源码核对确认 `internal/ledger/store.go` PostgreSQL/SQLite DDL 与 `ensureSchema` 尚无 `driver_source`，`internal/ledger/binding.go`/`move.go`/`tasks.go` 的旧 Claim/Rebind/Release/Takeover 仍会更新席位列；`internal/ledger/api/api.go#LedgerClient` 当前不含新席位写方法，计划保持该接口不扩展。
- 2026-09-03：源码核对确认 `internal/agentd/server.go#coordinatorRunner.Resume` 当前只映射 CLI/SessionID/HomeDir/Workdir/Model、没有 `Env`；`internal/keystone/keystone.go#Wake` 当前内存缺失会 Launch；`internal/collab` Pending/VerifyWriter 当前按卡上 `DriverSession` 比较；计划分别落来源执法、Resume env 和规范身份比较。
- 2026-09-03：本计划已写入法定路径 `docs/superpowers/plans/b312-plan.md`；内容包含 D1/D2/D3 开头裁决、精确 Consumes/Produces、T1→T5 DAG、基线命令、序列化边界、缺陷族/接缝双向自审与 Web 缺依赖的前置处置。
- 2026-09-03：为固定图覆盖债原始错误，重跑 `... codegraph ... sym 'internal/ledger/binding.go#Store.ClaimCardAs'`；原始 stderr 为 `Error: 符号 "internal/ledger/binding.go#Store.ClaimCardAs" 不在图中（图未覆盖或名字有误）；近似候选: []。确认图未覆盖时回落 grep，并把该符号记入本节点产出物的「图覆盖债」小节`，随后为退出码 1 的 `Usage:`/`exit status 1`。
- 2026-09-03：计划自审命令 `rg -n "TBD|同 Task|适当错误处理|TODO|FIXME" docs/superpowers/plans/b312-plan.md || true` 无输出；路径检查除明确新增的 `internal/collab/room/room_test.go` 外无缺失；`git diff --check` 成功，工作树仅有本计划与本节点台账两个未跟踪文件。
- 2026-09-03：初次任务计数正则 `rg -c '^## T[1-5]：' ...` 未命中并输出 `tasks=`（正则漏掉章节编号）；改用 `rg -c '^## [4-8]\\. T[1-5]：' ...` 后原始输出为 `tasks=5`、`interfaces=1`、`seam_sections=4`、`serialization_sections=7`。
- 2026-09-03：最终计划自审重跑：占位符扫描无输出；精确文件路径检查无 `MISSING` 输出（新增 `internal/collab/room/room_test.go` 明确列为新文件）；`git diff --check` 成功；章节统计原始输出 `tasks=5`、`interfaces=1`、`seam_sections=4`、`serialization_sections=7`。
- 2026-09-03：实现节点开工前重跑基线 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/proto/... ./internal/ledger/... ./internal/keystone/... ./internal/collab/... ./internal/client/... ./internal/agentd/... ./cmd/... -count=1`；原始结果退出码 0，包含 `ok internal/proto 0.007s`、`ok internal/ledger 22.322s`、`ok internal/ledger/api 0.894s`、`ok internal/keystone 0.246s`、`ok internal/collab 5.433s`、三个 collab 子包 `[no test files]`、`ok internal/client 9.648s`、`ok internal/agentd 177.865s`、`ok cmd 18.641s`。
- 2026-09-03：提交命令 `git commit -m "docs(B312): add current-session implementation plan"`；原始输出 `[cards/B312-charter-3 45173f41] docs(B312): add current-session implementation plan`、`2 files changed, 757 insertions(+)`、`create mode 100644 docs/superpowers/ledgers/2026-09-03-b312-plan-ledger.md`、`create mode 100644 docs/superpowers/plans/b312-plan.md`。随后按纪律只 amend 一次收进本条。
- 2026-09-03：`gofmt -w internal/ledger/move_test.go internal/ledger/tasks_test.go internal/ledger/api/api_test.go internal/ledger/binding_test.go && go test ./internal/ledger/... -count=1` 首次实跑因 `internal/ledger/binding_test.go:11:2: "fmt" imported and not used` 退出；移除未使用导入并把旧入口测试改为“不写席位”断言后重跑，原始结果 `ok github.com/Xsxdot/handoff/internal/ledger 16.273s`、`ok github.com/Xsxdot/handoff/internal/ledger/api 0.736s`。
- 2026-09-03：`go test ./internal/ledger/... ./internal/ledgerstep/... ./cmd/... -count=1` 实跑退出；原始通过行为为 `ok internal/ledger 19.856s`、`ok internal/ledger/api 0.956s`、`ok internal/ledgerstep 12.595s`，cmd 失败项包含 `TestCardAddCoordinateTriggersCardCreateLaunch` 与 `TestCardAddCoordinateLaunchFailureDegrades`，均收到 `--coordinate 已废止：建卡不占座，请改用 card coordinate <id>`。
- 2026-09-03：为筛选 cmd 失败项执行 `go test ./cmd/... -count=1 2>&1 | rg -E '^--- FAIL|^    [^ ]|^FAIL|^ok'`；命令本身失败，原始错误 `rg: error parsing flag -E: grep config error: unknown encoding: ^--- FAIL|^    [^ ]|^FAIL|^ok`。筛选命令参数需改用 ripgrep 的 `-e`。
- 2026-09-03：`go test ./cmd/... -count=1` 失败筛选的原始失败为 `TestCardAddCoordinateTriggersCardCreateLaunch`、`TestCardAddCoordinateLaunchFailureDegrades`（均收到 `--coordinate 已废止：建卡不占座，请改用 card coordinate <id>`）及 `TestCardDispatchGuardFollowsOwnership`（因未登记目标机而收到 `目标机 mac-02 未登记（handoff init/机器登记先行）`）。历史用例已迁移到 B312 的“旧 flag 拒绝、建卡不拉起、裸 dispatch 不改席位”断言，并补齐目标机夹具。
- 2026-09-03：`gofmt -w cmd/card_coordinate_test.go cmd/card_dispatch_test.go && go test ./cmd/... -count=1 2>&1 | rg -e '^--- FAIL' -e '^    [^ ]' -e '^FAIL' -e '^ok` 实跑退出码 0，原始结果 `ok github.com/Xsxdot/handoff/cmd 13.917s`。
- 2026-09-03：在 `web/` 实跑 `npm ci`，退出码 0；原始输出 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`。依赖工具链已恢复。
- 2026-09-03：依赖恢复后首次 `npm test -- --run` 实跑退出；原始摘要 `Test Files 5 failed | 118 passed (123)`、`Tests 8 failed | 1295 passed (1303)`，失败断言集中在旧 `card_create` source、旧“▶ 拉起协调者”、旧“拉起通道（开卡即绑 / 一键拉起）”及新建对话框开卡即绑 UI。
- 2026-09-03：更新 Web 历史测试至 B312 契约后，`npm test -- --run src/api/scheduling.fetch.test.ts src/app/cards/CardDrawer.test.tsx src/app/cards/CoordinatorPanel.test.tsx src/app/cards/NewCardDialog.test.tsx src/app/settings/SchedulingPage.test.tsx` 实跑退出码 0；原始结果 `Test Files 5 passed (5)`、`Tests 75 passed (75)`。
- 2026-09-03：`npm run typecheck` 首次实跑发现 `src/api/contract.test.ts(165,9): error TS2322`，原因是 JSON fixture 的 `driver_source` 被推成宽 `string`；以 `CardView['driver_source']` 在契约测试接缝收窄后重跑，原始输出仅含 `> tsc -b` 与 npm notice，退出码 0。
- 2026-09-03：`npm test -- --run` 在 `web/` 实跑退出码 0；原始结果 `Test Files 123 passed (123)`、`Tests 1303 passed (1303)`、`Duration 17.65s`。运行时保留既有 jsdom 提示 `Not implemented: HTMLCanvasElement's getContext()`，未导致测试失败。
- 2026-09-03：先补回归测试 `TestCoordRebindNoSquadIsActionableBadRequest`，再实跑 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/agentd/... -run '^TestCoordRebindNoSquadIsActionableBadRequest$' -count=1`；原始结果 `--- FAIL: TestCoordRebindNoSquadIsActionableBadRequest`、`无协调者小队的 rebind 应 400，状态=502 body={"error":"识别协调者小队失败: 未登记协调者小队"}`、`FAIL github.com/Xsxdot/handoff/internal/agentd`，确认缺口为 rebind 错误分类而非测试拼写。
- 2026-09-03：按 launch 的既有分类最小补齐 rebind lookup/admission 分支；`gofmt -w internal/agentd/coordapi.go internal/agentd/coordapi_test.go && GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/agentd/... -run '^TestCoordRebindNoSquadIsActionableBadRequest$' -count=1` 实跑退出码 0，原始结果 `ok github.com/Xsxdot/handoff/internal/agentd 0.179s`。
- 2026-09-03：补测 `TestRebindSeatRejectsSourceOnlyLegacySeat` 后实跑 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/ledger/... -run '^TestRebindSeatRejectsSourceOnlyLegacySeat$' -count=1`；原始结果含 `--- FAIL: TestRebindSeatRejectsSourceOnlyLegacySeat`、`仅来源席位应拒绝换绑并返回状态错误: <nil>`、`FAIL github.com/Xsxdot/handoff/internal/ledger`，确认生产分支误把 source-only 存量席位当作空座。
- 2026-09-03：在 RebindSeat 空座判断后加入“source-only 存量席位拒绝且不写入”的最小分支；`gofmt -w internal/ledger/binding.go internal/ledger/binding_test.go && GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/ledger/... -run '^TestRebindSeatRejectsSourceOnlyLegacySeat$' -count=1` 实跑退出码 0，原始结果 `ok github.com/Xsxdot/handoff/internal/ledger 0.108s`、`ok github.com/Xsxdot/handoff/internal/ledger/api 0.004s [no tests to run]`。
- 2026-09-03：受影响 Go 全量命令 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/proto/... ./internal/ledger/... ./internal/ledgerstep/... ./internal/keystone/... ./internal/collab/... ./internal/client/... ./internal/agentd/... ./cmd/... -count=1` 实跑退出码 1；原始结果 `ok internal/proto 0.008s`、`ok internal/ledger 21.107s`、`ok internal/ledger/api 0.778s`、`--- FAIL: TestRunnerMissingSessionHaltsWithCardEvent`、`卡上应有含 "会话未设置" 的评论与 needs_human: comments=false flagged=true`、`ok internal/keystone 0.202s`、`ok internal/collab 5.156s`、`ok internal/client 9.530s`、`ok internal/agentd 165.726s`、`ok cmd 17.487s`、`FAIL`；失败仅由本轮无必要地改变 ledgerstep 用户可见文案造成，已恢复原文案。
- 2026-09-03：恢复 ledgerstep 原有缺会话错误文案后，`gofmt -w internal/ledgerstep/runner.go && GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/ledgerstep/... -count=1` 实跑退出码 0，原始结果 `ok github.com/Xsxdot/handoff/internal/ledgerstep 10.037s`。
- 2026-09-03：为列表未知席位来源先补 `ListView.test.tsx`，实跑 `npm test -- --run src/app/cards/ListView.test.tsx`；原始结果 `Test Files 1 failed (1)`、`Tests 1 failed`、`Unable to find an element with the text: 席位异常`，DOM 实际渲染未知 source 为 `叫机器人`，确认 UI 来源分流缺口。
- 2026-09-03：将 ListView 来源映射补为 bind→坐下、coordinate→叫机器人、未知→席位异常；`npm test -- --run src/app/cards/ListView.test.tsx` 实跑退出码 0，原始结果 `Test Files 1 passed (1)`、`Tests 1 passed (1)`。
- 2026-09-03：补测 CardDrawer 未知来源后实跑 `npm test -- --run src/app/cards/CardDrawer.test.tsx`；原始结果 `Test Files 1 failed (1)`、`Tests 1 failed | 34 passed (35)`，`Unable to find an element with the text: 席位异常`，DOM 实际渲染为 `非法来源`，确认抽屉词汇仍未统一。
- 2026-09-03：将 CardDrawer 未知来源文案统一为席位异常；`npm test -- --run src/app/cards/CardDrawer.test.tsx` 实跑退出码 0，原始结果 `Test Files 1 passed (1)`、`Tests 35 passed (35)`。
- 2026-09-03：尝试为 agentd source-only rebind 增加 HTTP 测试时实跑 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/agentd/... -run '^TestCoordRebindSourceOnlySeatDoesNotLaunch$' -count=1`；原始编译错误为 `internal/agentd/coordapi_test.go:281:26: env.ledger.db undefined (cannot refer to unexported field db)`、`FAIL ... [build failed]`。该测试因越界访问测试夹具私有字段被删除，未将此发数计入行为红灯。
- 2026-09-03：迁移 agentd 测试夹具为暴露 SQLite 路径时首次实跑同一 source-only 测试，原始编译错误为 `internal/agentd/ledgerapi_test.go:6:2: "database/sql" imported and not used`、`FAIL ... [build failed]`；移除错误文件中的重复导入后重跑。
- 2026-09-03：source-only HTTP 回归测试正常编译后实跑同一命令，原始结果 `--- FAIL: TestCoordRebindSourceOnlySeatDoesNotLaunch`、`source-only 旧席位 rebind 应 409，状态=502 body={"error":"换绑协调者失败: 写协调者席位: 卡 B1 的旧席位缺少身份，不能直接换绑: ledger: 当前状态不允许该操作"}`、`FAIL github.com/Xsxdot/handoff/internal/agentd`，确认当前实现先启动机器人后才暴露脏席位。
- 2026-09-03：在 HTTP rebind 进入 Launch 前拒绝 source-only 存量席位并返回 409；`gofmt -w internal/agentd/coordapi.go && GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/agentd/... -run '^TestCoordRebindSourceOnlySeatDoesNotLaunch$' -count=1` 实跑退出码 0，原始结果 `ok github.com/Xsxdot/handoff/internal/agentd 0.188s`。
- 2026-09-03：Web 相关新增/变更测试与类型检查命令 `npm test -- --run src/api/scheduling.fetch.test.ts src/app/cards/CardDrawer.test.tsx src/app/cards/CoordinatorPanel.test.tsx src/app/cards/ListView.test.tsx src/app/cards/NewCardDialog.test.tsx src/app/settings/SchedulingPage.test.tsx && npm run typecheck` 在 `web/` 实跑退出码 0；原始结果 `Test Files 6 passed (6)`、`Tests 77 passed (77)`，随后 `> web@0.0.0 typecheck` 无错误并退出 0。
- 2026-09-03：Web 全量 `npm test -- --run` 实跑退出码 0；原始结果 `Test Files 124 passed (124)`、`Tests 1305 passed (1305)`、`Duration 17.65s`；同时出现既有 jsdom 提示 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`，未导致失败。
- 2026-09-03：全仓 Go 编译 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go build ./...` 实跑退出码 0，原始 stdout/stderr 为空。
- 2026-09-03：变异前确认 `rg -n -F 'case SeatSourceBind, SeatSourceCoordinate:' internal/proto/seat.go` 唯一命中 `31: case SeatSourceBind, SeatSourceCoordinate:`，`wc -l` 原始输出 `1`；随后把该比较暂变为仅 `SeatSourceBind`。
- 2026-09-03：变异编译 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go build ./...` 实跑退出码 0，原始 stdout/stderr 为空，确认该发变异编译通过。
- 2026-09-03：变异行为断言 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/ledger -run '^TestBindSeatAndRebindSeatUseAtomicSeatContract$' -count=1` 实跑退出码 1；原始结果 `--- FAIL: TestBindSeatAndRebindSeatUseAtomicSeatContract`、`已有合法席位 bind 应冲突: 坐下席位无效: 席位来源非法: "coordinate"`、`FAIL github.com/Xsxdot/handoff/internal/ledger`，确认测试拦住了该 coordinate 来源变异。
- 2026-09-03：恢复唯一变异行后，`gofmt -w internal/proto/seat.go && GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/ledger -run '^TestBindSeatAndRebindSeatUseAtomicSeatContract$' -count=1` 实跑退出码 0，原始结果 `ok github.com/Xsxdot/handoff/internal/ledger 0.140s`。
- 2026-09-03：最终受影响 Go 全量测试 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go test ./internal/proto/... ./internal/ledger/... ./internal/ledgerstep/... ./internal/keystone/... ./internal/collab/... ./internal/client/... ./internal/agentd/... ./cmd/... -count=1` 实跑退出码 0；原始结果 `ok internal/proto 0.007s`、`ok internal/ledger 20.619s`、`ok internal/ledger/api 0.966s`、`ok internal/ledgerstep 12.222s`、`ok internal/keystone 0.234s`、`ok internal/collab 4.931s`、`ok internal/client 9.463s`、`ok internal/agentd 164.013s`、`ok cmd 17.257s`，以及 collab/client、collab/cursor、collab/room 的 `[no test files]`。
- 2026-09-03：最终收口检查 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go build ./... && git diff --check && git status --short --branch` 实跑退出码 0；Go build 与 diff check 无输出，status 原始首行 `## cards/B312-charter-5`，仅列本卡实现/测试/台账变更。
- 2026-09-03：提交前补充兼容入口注释后，重复执行 `GOMODCACHE=/root/.handoff/tmp/4dad61da/gomodcache GOCACHE=/root/.handoff/tmp/4dad61da/gocache go build ./... && git diff --check && git status --short --branch` 实跑退出码 0；Go build 与 diff check 无输出，status 原始首行为 `## cards/B312-charter-5`。
- 2026-09-03：提交命令 `git commit -m "feat(B312): bind current session seats"` 实跑退出码 0；原始输出 `[cards/B312-charter-5 f1d845a6] feat(B312): bind current session seats`、`69 files changed, 1528 insertions(+), 957 deletions(-)`，并列出删除 `cmd/card_coordinate.go`、创建 `cmd/card_seat.go`、`web/src/api/testdata/CoordinatorRebindReq.json`、`web/src/app/cards/ListView.test.tsx`。
- 2026-09-03：Important 修复轮开始；当前工作树为 `cards/B312-charter-6 @ a13c1493`，未发现未提交改动。仅按本轮明确的 I1/I2/I3 检查，不读取 B307/B313/B316 对照分支或提交。
- 2026-09-03：首轮新增缝测试实跑：Web 因 `vitest: not found` 未进入用例；`npm ci` 安装 lockfile 依赖后，I1 的 `CoordinatorPanel.test.tsx` 与 `CardDrawer.test.tsx` 均真实断言红（2 个新增用例失败，现状把 bind+attach=null 渲染为状态不一致且无换绑按钮）。I2 `go test ./cmd/... -run '^TestCardRebindSelfEvictsAgentdSession$' -count=1` 真实断言红：`card_driver_test.go:245: self 换绑成功后必须通知 agentd Forget`。I3 首跑误因 `newLedgerEnv` 的 PTY 根目录不可用而 SKIP，已改用 no-PTY 夹具，未将 SKIP 计为红灯。
- 2026-09-03：实现后局部命令实跑并退出码 0：`npm test -- --run src/app/cards/CoordinatorPanel.test.tsx src/app/cards/CardDrawer.test.tsx` 原始结果 `Test Files 2 passed (2)`、`Tests 42 passed (42)`；`npm run typecheck` 无错误；`go test ./internal/agentd/... -run '^(TestCardStepRejectsSeatActorMismatch|TestCardStepEmptySeatKeepsLegacyAcceptance|TestCoordForgetAfterSelfRebindClearsStaleSession)$' -count=1` 原始结果 `ok github.com/Xsxdot/handoff/internal/agentd 0.521s`；`go test ./cmd/... -run '^(TestCardRebindSelfEvictsAgentdSession|TestCardRebindSelfAndTakeoverEvent)$' -count=1` 原始结果两项 `PASS`、包退出码 0。期间 typecheck 首次报 `CoordinatorPanel.test.tsx(6,70): error TS6133: 'rebindCoordinatorLaunch' is declared but its value is never read.`，删除未使用测试 import 后复跑通过。
- 2026-09-03：变异自验三发均先断言唯一命中且变异可编译，再先跑单条行为测试：I1 将 `bound && !attach_active` 取反后 `npm test ... -t 'bind 席位没有机器人 attach 时仍显示已绑定和换绑，不显示打开终端'` 真实 1 failed/5 skipped；I3 将席位不等比较取反后 `go test ./internal/agentd/... -run '^TestCardStepRejectsSeatActorMismatch$' -count=1 -v` 真实返回 `实得 202 ({"ok":true})` 并失败；I2 将 forget URL 改为 `/coordinator/not-forget` 后 `go test ./cmd/... -run '^TestCardRebindSelfEvictsAgentdSession$' -count=1 -v` 真实收到 404 并失败。三发均恢复原实现，未将变异结果当作产品测试结论。
- 2026-09-03：收尾验证命令均实跑退出码 0：`go build ./...` 无输出；`go test ./internal/client/... -count=1` 原始结果 `ok github.com/Xsxdot/handoff/internal/client 9.124s`；`go test ./cmd/... -count=1` 原始结果 `ok github.com/Xsxdot/handoff/cmd 10.978s`；`go test ./internal/agentd/... -count=1` 原始结果 `ok github.com/Xsxdot/handoff/internal/agentd 168.203s`；Web `npm test -- --run src/api/scheduling.fetch.test.ts src/app/cards/CardDrawer.test.tsx src/app/cards/CoordinatorPanel.test.tsx src/app/cards/ListView.test.tsx src/app/cards/NewCardDialog.test.tsx src/app/settings/SchedulingPage.test.tsx` 原始结果 `Test Files 6 passed (6)`、`Tests 79 passed (79)`，随后 `npm run typecheck` 无错误；`git diff --check` 无输出。
- 2026-09-03：提交命令 `git commit -m "fix(B312): close current session binding gaps"` 实跑退出码 0；原始输出 `[cards/B312-charter-6 101b2bda] fix(B312): close current session binding gaps`、`11 files changed, 254 insertions(+), 2 deletions(-)`。随后仅为把本条提交历史收入同批台账而 amend 一次；不追写 amend 后 hash。
- 2026-09-03：B312 Important I1 复现命令 `npm test -- --run src/app/cards/CardDrawer.test.tsx -t '坐下席位没有机器人 attach'` 并行重复实跑；失败原始摘要包含 `Test Files 1 failed (1)`、`Tests 1 failed | 35 skipped (36)`、`Unable to find an accessible element with the role \"button\" and name \"换绑：叫机器人\"`，失败位置为 `CardDrawer.test.tsx:115`，同时 stdout 为 `coordinator.status.done { card: 'B-bind', bound: true, attach_active: false }`；确认是异步状态渲染竞态，未修改生产代码。
- 2026-09-03：按 I1 要求将 `CardDrawer.test.tsx:115` 的同步 `screen.getByRole` 改为 `await screen.findByRole`，仅改变测试等待方式。
- 2026-09-03：正式改动后的 `npm test -- --run src/app/cards/CardDrawer.test.tsx` 实跑退出码 0，随后并行重复三次均为原始结果 `Test Files 1 passed (1)`、`Tests 36 passed (36)`；`npm run typecheck` 实跑退出码 0，无输出。
- 2026-09-03：I1 变异前以完整行锚点 `expect(await screen.findByRole('button', { name: '换绑：叫机器人' })).toBeVisible()` 唯一命中 `CardDrawer.test.tsx:115`；将 `findByRole` 暂变回同步 `getByRole` 后，`npm run typecheck` 实跑退出码 0；单条行为测试原始结果为 `Test Files 1 failed (1)`、`Tests 1 failed | 35 skipped (36)`，错误为 `Unable to find an accessible element with the role \"button\" and name \"换绑：叫机器人\"`；整文件变异结果为 `Test Files 1 failed (1)`、`Tests 1 failed | 35 passed (36)`。随后恢复正式 `findByRole`。
- 2026-09-03：提交命令 `git commit -m \"test(B312): await coordinator rebind button\"` 实跑退出码 0；原始输出 `[cards/B312-charter-7 669f990c] test(B312): await coordinator rebind button`、`2 files changed, 5 insertions(+), 1 deletion(-)`。随后仅为把本条提交历史收入同批台账而 amend 一次；不追写 amend 后 hash。
