# B349+B352 实现计划台账

日期：2026-09-09
卡：B349（B352 并入）
节点：charter plan
工作分支：cards/B349-charter-3
开工 HEAD：1e283551af1518fc76eb5444af547974e8ffa188

## 过程与判断

1. 当前分支 HEAD 为 `1e283551`，其父 `0d9673ed` 为 `origin/cards/B233.1-charter-7`；HEAD 提交说明为 `docs(B349): 拍板拆解 F1-F6 维持冻结`，符合起点含 cards/B349-charter-2 拍板回写。
2. 本节点只产出法定计划与本台账；不写实现代码、不派发、不调用 handoff CLI、不起新的 executor；T0 按 breakdown 作为已完成前置，不重开。
3. 已读 `docs/superpowers/specs/b349.md`、`b349-contract.md`、`b349-breakdown.md`。F1–F6 维持冻结；T1→T2→T3→T4 为同一轮 implement 的内部顺序，不扇出子卡。
4. 基线源码事实：`proto.LedgerEvent` 已有 `SourceTarget/SourceTask/SourceSeq`；`currentWorkflowAttempt` 仍返回 `string` 且只比较 envelope attempt；`cardWaitEventActionable` 仍为 `func cardWaitEventActionable(ev ledger.Event) (bool, error)`；`Server` 仅以内存字段承载 `automationCursor`。

## 代码图查证

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_orchestration --max-tokens 5000`

原始输出结论：退出码 0；日志报告 `truncated=true`、`fociTruncated`，返回 best 领域 `d_orchestration`，并报告现状实际领域为 `d_coordination_task`/`d_coordination_api`，不据截断邻域写完整依赖结论。

图覆盖债沿用冻结 breakdown：`currentWorkflowAttempt`、`acceptsCurrentWorkflowAttempt`、`eventWire`、`WaitDeliveryPolicy`、`cardWaitEventActionable` 等以源码锚查证；不能把图缺席当作代码缺席。

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym n_ledger_api_Facade_EventsFromAsc`、`sym n_ledger_Store_Follow`、`sym n_keystone_Service_Decide`、`sym n_agentd_Server_SetupAutomation`、`sym n_agentd_Server_StartAutomation`、`sym m_ledger_DispatchSnapshot`

原始输出结论：上述六次均退出码 0；图返回的签名分别为 `func (f *Facade) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]proto.LedgerEvent, error)`、`func (s *Store) Follow(ctx context.Context, members func() ([]string, error), fromSeq int64, pollInterval time.Duration, onEvent func(Event) error) error`、`func (s *Service) Decide(ev WakeEvent) Decision`、`func (s *Server) SetupAutomation(st *ledger.Store)`、`func (s *Server) StartAutomation(ctx context.Context)`，以及 `DispatchSnapshot` 模型。

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym n_client_WaitDeliveryPolicy`

原始输出：

    Error: 符号 "n_client_WaitDeliveryPolicy" 不在图中（图未覆盖或名字有误）；近似候选: []
    Usage:
      codegraph sym <符号名或节点 id> [flags]
    ...
    exit status 1

判断：`WaitDeliveryPolicy` 按源码 `internal/client/delivery.go:23` 查证，加入图覆盖债；不把上述图命令失败改写为通过。

## 基线验收命令

命令：`go test ./internal/ledger/api -run '^TestFacadeEventsFromAscPreservesSourceIdentity$' -count=1`

原始输出：

    ok  github.com/Xsxdot/handoff/internal/ledger/api  0.136s

命令：`go test ./internal/agentd -run '^TestCardDetailProjectsMirroredSourceIdentity$' -count=1`

原始输出：

    ok  github.com/Xsxdot/handoff/internal/agentd  0.209s

命令：`go test ./internal/proto -count=1`

原始输出：

    ok  github.com/Xsxdot/handoff/internal/proto  0.008s

命令：`go test ./internal/agentd -run 'Test(B349|B2336StaleAttempt|B353Automation)' -count=1`

原始输出：

    ok  github.com/Xsxdot/handoff/internal/agentd  1.597s

命令：`go test ./cmd -run 'Test(B349|B353)CardWait' -count=1`

原始输出：

    ok  github.com/Xsxdot/handoff/cmd  15.909s

命令：`go test ./internal/agentd -run 'Test(B349|AutomationCursor)' -count=1`

原始输出：

    ok  github.com/Xsxdot/handoff/internal/agentd  0.176s

判断：这些基线命令均亲跑返回 ok；它们只证明 T0 与现有 B233.6/B353 回归夹具可运行，不证明本卡 source identity 或 DataDir cursor 行为已实现。计划的 T1–T3 必须新增/迁移调用方测试后再以同范围命令验收。

## 计划自检与基线全量门禁

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/plans/b349-plan.md`

原始输出结论：退出码 0；现有锚点均为 `ok` 或 `moved`，新增私有实现符号未伪造为图锚点。

命令：`rg -n 'TBD|同 Task N|TODO|FIXME' docs/superpowers/plans/b349-plan.md`

原始输出：退出码 1，无匹配。

命令：`git diff --check`

原始输出：退出码 0，无输出。

命令：`go test ./internal/proto ./internal/ledger/api ./internal/agentd ./cmd -count=1`

原始输出末段：

    ok   github.com/Xsxdot/handoff/internal/agentd 195.832s
    FAIL
    FAIL github.com/Xsxdot/handoff/cmd 35.262s
    FAIL

命令：`go test ./... -count=1`

原始失败行：

    --- FAIL: TestRepoContractGate (0.04s)
        graph_gate_test.go:38: 契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）
        graph_gate_test.go:38: 契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）
    FAIL
    FAIL github.com/Xsxdot/handoff/cmd
    FAIL

判断：触及包与全量回归的失败均落在基线已知 `TestRepoContractGate` 两项违规；本计划不修改契约图或扩大 B349 文件范围。

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate`

原始输出：退出码 0，`containers=300`、`domains=23`、`edges=5507`、`nodes=4499`、`issues=null`、`edgeIssues=null`。

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check`

原始报错原文：

    Error: 契约对照发现 2 处违规
    {
     "fails": [
      {"kind":"dead-contract","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）"},
      {"kind":"dead-interface","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的接口 \"ApprovalClient\" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）"}
     ]
    }
    exit status 1

判断：`check` 的 fail 集合保持上述基线两项；warn/legacy 输出未被改写为本卡结论。

命令：`git diff --cached --check`

原始输出：首次暂存检查因计划 Markdown 的 9 处行尾双空格退出码 2；用 `apply_patch` 去除行尾空格后重新暂存，复跑退出码 0、无输出。暂存文件仅为本计划与本台账。

命令：`git commit -m "docs(B349): add implementation plan"`

原始输出：

    [cards/B349-charter-3 ea74f07c] docs(B349): add implementation plan
     2 files changed, 741 insertions(+)
     create mode 100644 docs/superpowers/ledgers/2026-09-09-b349-plan-ledger.md
     create mode 100644 docs/superpowers/plans/b349-plan.md

判断：这是提交前 HEAD；补记本事实后只执行一次 `git commit --amend --no-edit`，不把 amend 后 hash 写回本台账。

## 收口规则

本台账与法定计划同批提交；提交命令与原始输出在提交发生后追加，再只 amend 一次收进同批提交；不把 amend 后 hash 回写台账追逐。收口判据为工作树干净。

## 本实现节点追加

命令：`git status --short --branch && git log -1 --oneline`

原始输出：

    ## cards/B349-charter-4
    07d799e1 docs(B349): add implementation plan

判断：当前工作树干净；本节点在 `cards/B349-charter-4` 工作，不切分支、不改 git 配置。

命令：`go test ./internal/agentd -run 'Test(B349|B2336StaleAttempt|B353Automation)' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/internal/agentd	2.006s

命令：`go test ./cmd -run 'Test(B349|B353)CardWait' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/cmd	15.874s

命令：`go test ./internal/agentd -run 'Test(B349|AutomationCursor)' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/internal/agentd	0.196s

判断：T1/T2/T3 基线测试均可运行且通过；当前没有 B349 source identity 或 DataDir cursor 的新增调用方断言，不能据此判定本卡功能已实现。

命令：`gofmt -w internal/agentd/wakeconsumer_test.go && go test ./internal/agentd -run '^TestB349AutomationSourceIdentity$' -count=1`

原始输出：

    --- FAIL: TestB349AutomationSourceIdentity (1.97s)
        --- FAIL: TestB349AutomationSourceIdentity/current_attempt_requires_source_task_and_target (0.21s)
            wakeconsumer_test.go:103: source identity 闸 processed=3 escalated=false err=<nil>，want 1/false/nil
        --- FAIL: TestB349AutomationSourceIdentity/latest_malformed_identity_snapshot_is_ignored (0.21s)
            wakeconsumer_test.go:126: TaskID!=Attempt 的最新快照不应遮蔽合法快照，processed=0 escalated=false err=<nil>
        --- FAIL: TestB349AutomationSourceIdentity/snapshot_scan_paginates_to_tail (1.55s)
            wakeconsumer_test.go:148: 分页到尾且双空 target 应唤醒，processed=0 escalated=false err=<nil>
    FAIL
    FAIL	github.com/Xsxdot/handoff/internal/agentd	1.978s

判断：失败均为新增消费缝行为断言；测试已编译并运行，原因是现有实现只比较 envelope attempt、没有过滤 `TaskID != Attempt`，且 snapshot 返回字符串无法比较 target/分页尾部的完整快照。可进入 T1 最小生产实现。

命令：`gofmt -w internal/agentd/wakeconsumer.go && go test ./internal/agentd -run '^TestB349AutomationSourceIdentity$' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/internal/agentd	1.994s

判断：T1 新增 source identity 接缝测试通过；分页测试先推进 501 条审计事件的消费游标，再验证 current snapshot 扫描确实跨页到尾部。

命令：`gofmt -w internal/agentd/wakeconsumer_test.go && go test ./internal/agentd -run 'Test(B349|B2336StaleAttempt|B353Automation)' -count=1`

原始输出（首次收尾失败）：

    --- FAIL: TestB2336StaleAttemptDoesNotWake (0.14s)
        wakeconsumer_test.go:641: 仅当前 attempt 应唤醒一次，processed=0 escalated=false
    FAIL
    FAIL	github.com/Xsxdot/handoff/internal/agentd	3.462s

判断：既有 B2336 夹具的 `TaskID` 使用 `task-old/task-new` 而非 `Attempt`，与本卡冻结的 `TaskID == Attempt` 合格快照条件冲突；仅迁移测试夹具为 `attempt-old/attempt-new`，未放宽生产条件。

命令：`gofmt -w internal/agentd/wakeconsumer_test.go && go test ./internal/agentd -run 'Test(B349|B2336StaleAttempt|B353Automation)' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/internal/agentd	3.906s

命令：`go build ./...`

原始输出：无输出，退出码 0。

判断：T1 触及 agentd 回归与全仓编译均通过。

命令：`gofmt -w cmd/card_wait_test.go && go test ./cmd -run '^TestB349CardWaitSourceIdentity$' -count=1`

原始输出（末段）：

    --- FAIL: TestB349CardWaitSourceIdentity (2.15s)
        card_wait_test.go:463: 仅当前镜像和卡原生事件应输出，got=[task_mirrored seq=3 wrong-task task_mirrored seq=4 wrong-target task_mirrored seq=5 old-attempt task_mirrored seq=6 current task_mirrored seq=7 no-snapshot task_mirrored seq=9 missing-identity needs_human seq=10]
    FAIL
    FAIL	github.com/Xsxdot/handoff/cmd	2.153s

判断：新增测试真实穿过 `runCardWait` 和 `Store.Follow`，基线错误输出了旧/错/无快照/缺身份镜像，证明身份闸断言有牙；随后实现 T2。

命令：`gofmt -w cmd/card_wait.go && go test ./cmd -run '^TestB349CardWaitSourceIdentity$' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/cmd	2.101s

命令：`gofmt -w cmd/card_wait_test.go && go test ./cmd -run 'Test(B349|B353)CardWait' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/cmd	18.057s

判断：T2 新增真实 wait 接缝及既有 B353 wait 回归均通过；旧 B353 镜像夹具已补齐 `TaskID == Attempt` 的派发快照，不放宽身份闸。

命令：`go build ./...`

原始输出：无输出，退出码 0。

判断：T2 触及 cmd 回归与全仓编译均通过。

命令：`gofmt -w internal/agentd/wakeconsumer_test.go && go test ./internal/agentd -run '^TestB349AutomationCursorPersistence$' -count=1`

原始输出：

    --- FAIL: TestB349AutomationCursorPersistence (0.43s)
        --- FAIL: TestB349AutomationCursorPersistence/persists_and_resumes_from_the_same_DataDir (0.20s)
            wakeconsumer_test.go:936: 消费后应存在 cursor 文件: open /root/.handoff/tmp/640729a0/TestB349AutomationCursorPersistencepersists_and_resumes_from_th2536499448/003/automation-cursor.json: no such file or directory
        --- FAIL: TestB349AutomationCursorPersistence/save_failure_keeps_memory_ahead_without_faking_disk_waterline (0.23s)
            wakeconsumer_test.go:986: Save 失败应保留消费结果并返回错误，processed=1 err=<nil>
    FAIL
    FAIL	github.com/Xsxdot/handoff/internal/agentd	0.438s

判断：T3 新增测试已编译并从真实 `SetupAutomation`/`consumeAutomationEventsOnce` 运行；失败原因是当前没有 DataDir cursor 文件，也没有把 Save 失败返回到消费缝，进入 T3 最小实现。

命令：`gofmt -w internal/agentd/automation_cursor.go internal/agentd/server.go internal/agentd/wakeconsumer.go && go test ./internal/agentd -run '^TestB349AutomationCursorPersistence$' -count=1`

原始输出（首次实现后）：

    --- FAIL: TestB349AutomationCursorPersistence (0.40s)
        --- FAIL: TestB349AutomationCursorPersistence/persists_and_resumes_from_the_same_DataDir (0.21s)
            wakeconsumer_test.go:953: room cursor 不应被自动化 cursor 改写，before=(open .../room-cursors.json: no such file or directory,"") after=(open .../room-cursors.json: no such file or directory,"")
    FAIL
    FAIL	github.com/Xsxdot/handoff/internal/agentd	0.410s

判断：实现行为已达到文件断言；失败仅因测试直接比较两个独立 `os.ErrNotExist` 错误值，改为 `errors.Is` 比较缺文件状态，不改变生产代码。

命令：`gofmt -w internal/agentd/wakeconsumer_test.go && go test ./internal/agentd -run '^TestB349AutomationCursorPersistence$' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/internal/agentd	0.381s

命令：`go test ./internal/agentd -run 'Test(B349|AutomationCursor)' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/internal/agentd	2.303s

命令：`go build ./...`

原始输出：无输出，退出码 0。

判断：T3 新增 cursor 文件/重装配/Save 失败测试、现有 automation cursor 回归和全仓编译均通过。

命令：`go test ./internal/proto ./internal/ledger/api ./internal/agentd ./cmd -count=1`

原始输出关键末段：

    ok  	github.com/Xsxdot/handoff/internal/proto	0.003s
    ok  	github.com/Xsxdot/handoff/internal/ledger/api	0.699s
    ok  	github.com/Xsxdot/handoff/internal/agentd	162.804s
    --- FAIL: TestRepoContractGate (0.04s)
        graph_gate_test.go:38: 契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）
        graph_gate_test.go:38: 契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）
    FAIL	github.com/Xsxdot/handoff/cmd	34.580s

判断：三个前置包全量通过；cmd 唯一失败仍为计划已记录的两项 `TestRepoContractGate` 基线违规，未见 B349 新增失败。

命令：`go test ./... -count=1`

原始输出关键末段：

    ok  	github.com/Xsxdot/handoff/internal/agentd	180.128s
    ok  	github.com/Xsxdot/handoff/internal/ledger/api	1.028s
    ok  	github.com/Xsxdot/handoff/internal/proto	0.005s
    ... 其余包均为 ok 或 [no test files]
    FAIL

前置 cmd 失败原文：

    --- FAIL: TestRepoContractGate (0.04s)
        graph_gate_test.go:38: 契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）
        graph_gate_test.go:38: 契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）
    FAIL	github.com/Xsxdot/handoff/cmd	39.898s

判断：全仓除上述计划已记录的两项 `TestRepoContractGate` 基线违规外均通过；未出现 B349 新增失败。

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b349-breakdown.md`

原始输出：退出码 0；13 个现有锚点均为 `ok` 或 `moved`，其中本轮移动符号为 `eventWire`、`automationWakeEvent`、`consumeAutomationEventsOnce`、`SetupAutomation`、`runCardWait`、`cardWaitEventActionable`、`WaitDeliveryPolicy`。

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate`

原始输出：退出码 0；`containers=300`、`domains=23`、`edges=5507`、`nodes=4499`、`issues=null`、`edgeIssues=null`。

命令：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check`

原始输出关键失败：

    Error: 契约对照发现 2 处违规
    {
     "fails": [
      {"kind":"dead-contract","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）"},
      {"kind":"dead-interface","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的接口 \"ApprovalClient\" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）"}
     ]
    }
    exit status 1

判断：`check` 失败集合仍恰为计划基线两项，没有新增契约违规。

命令：`rg -n -F 'ev.SourceTask != current.Attempt || ev.SourceTarget != current.Target' internal/agentd/wakeconsumer.go`

原始输出：

    127:		ev.SourceTask != current.Attempt || ev.SourceTarget != current.Target {

变异：将上述唯一命中中的 `ev.SourceTarget != current.Target` 改为 `ev.SourceTarget == current.Target`。

命令：`gofmt -w internal/agentd/wakeconsumer.go && go build ./...`

原始输出：无输出，退出码 0。

命令：`go test ./internal/agentd -run '^TestB349AutomationSourceIdentity$' -count=1`

原始输出关键失败：

    --- FAIL: TestB349AutomationSourceIdentity (1.71s)
        --- FAIL: TestB349AutomationSourceIdentity/current_attempt_requires_source_task_and_target (0.20s)
            wakeconsumer_test.go:109: 仅当前 source identity 应唤醒一次: [...ticket_id=wrong-target...]
        --- FAIL: TestB349AutomationSourceIdentity/latest_malformed_identity_snapshot_is_ignored (0.13s)
            wakeconsumer_test.go:128: TaskID!=Attempt 的最新快照不应遮蔽合法快照，processed=0 escalated=false err=<nil>
        --- FAIL: TestB349AutomationSourceIdentity/snapshot_scan_paginates_to_tail (1.38s)
            wakeconsumer_test.go:153: 分页到尾且双空 target 应唤醒，processed=0 escalated=false err=<nil>
    FAIL

命令：`go test ./internal/agentd -run 'Test(B349|B2336StaleAttempt|B353Automation)' -count=1`

原始输出：新增 source identity、B353 策略、B2336 stale attempt、cursor persistence 共 6 个失败，末段为 `FAIL github.com/Xsxdot/handoff/internal/agentd 3.902s`。

判断：变异编译通过且先由行为断言打红，命中唯一确认有效；随后恢复比较符并复跑 `go test ./internal/agentd -run '^TestB349AutomationSourceIdentity$' -count=1`，原始输出为 `ok  github.com/Xsxdot/handoff/internal/agentd 1.751s`。

命令：`go test ./internal/agentd -run 'Test(B349|B2336StaleAttempt|B353Automation|AutomationAttachDefersAndThenWakes)' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/internal/agentd	3.793s

命令：`go test ./cmd -run 'Test(B349|B353)CardWait' -count=1`

原始输出：

    ok  	github.com/Xsxdot/handoff/cmd	17.898s

判断：T1/T2/T3 的新增测试、既有 stale/policy/attach 和 B353 card wait 回归在最新夹具下均通过。

命令：`go test ./internal/proto ./internal/ledger/api ./internal/agentd ./cmd -count=1`

原始输出关键末段：

    ok  	github.com/Xsxdot/handoff/internal/proto	0.005s
    ok  	github.com/Xsxdot/handoff/internal/ledger/api	0.699s
    ok  	github.com/Xsxdot/handoff/internal/agentd	167.727s
    --- FAIL: TestRepoContractGate (0.04s)
        graph_gate_test.go:38: 契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）
        graph_gate_test.go:38: 契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）
    FAIL	github.com/Xsxdot/handoff/cmd	35.504s

判断：最终四包回归中所有非基线测试通过；cmd 仍仅有计划记录的两项契约门禁失败。

命令：`go build ./... && git diff --check && git status --short --branch && git diff --name-only`

原始输出：

    ## cards/B349-charter-4
     M cmd/card_wait.go
     M cmd/card_wait_test.go
     M docs/superpowers/ledgers/2026-09-09-b349-plan-ledger.md
     M internal/agentd/server.go
     M internal/agentd/wakeconsumer.go
     M internal/agentd/wakeconsumer_test.go
    ?? internal/agentd/automation_cursor.go
    cmd/card_wait.go
    cmd/card_wait_test.go
    docs/superpowers/ledgers/2026-09-09-b349-plan-ledger.md
    internal/agentd/server.go
    internal/agentd/wakeconsumer.go
    internal/agentd/wakeconsumer_test.go

判断：全仓编译与 diff 空白检查退出码均为 0；变更文件集合仅为计划 §3.1 的生产/测试/计划台账文件，其中新建 `internal/agentd/automation_cursor.go` 在未暂存状态由 `git status` 显示。

命令：`git commit -m "feat(B349): gate mirrored wake identity and persist cursor"`

原始输出：

    [cards/B349-charter-4 d1f1a44c] feat(B349): gate mirrored wake identity and persist cursor
     7 files changed, 829 insertions(+), 50 deletions(-)
     create mode 100644 internal/agentd/automation_cursor.go

判断：实现、测试、cursor 文件和本台账已完成首次提交；按台账规则本条事实收入后只执行一次 amend，不把 amend 后 hash 回写台账。

命令：`git status --short && git log -5 --oneline --decorate`

原始输出：工作树无未提交改动；`16fc1826 (HEAD -> cards/B349-charter-5, cards/B349-review-1, cards/B349-charter-4) feat(B349): gate mirrored wake identity and persist cursor`。

判断：当前执行基线为已提交的 B349 实现；review-1 补缝仅涉及 `cmd/card_wait_test.go` 与 `internal/agentd/wakeconsumer_test.go`，不改产品语义。

命令：`go test ./cmd -run 'Test(B349|B353)CardWait' -count=1`

原始输出：`ok  github.com/Xsxdot/handoff/cmd 17.985s`。

命令：`go test ./internal/agentd -run '^TestB349AutomationCursorPersistence$' -count=1`

原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd 0.431s`。

判断：新增 review-1 覆盖前的两个既有接缝测试均通过；尚未覆盖子卡 `--subtree` 身份归属及损坏 cursor 后新 Server 重试未落盘事件。

变更：新增 `TestB349CardWaitSubtreeUsesEventCardIdentity`，通过真实 `runCardWait --subtree --follow` 写入根卡与子卡不同派发身份，并断言子卡当前事件唯一输出；新增 `TestB349AutomationCursorPersistence` 的损坏 JSON、目录型不可读 cursor 和 Save 失败后新 Server 重试断言。

命令：`gofmt -w cmd/card_wait_test.go internal/agentd/wakeconsumer_test.go && go test ./cmd -run '^TestB349CardWaitSubtreeUsesEventCardIdentity$' -count=1 && go test ./internal/agentd -run '^TestB349AutomationCursorPersistence/(corrupt or unreadable cursor|save failure keeps memory ahead without faking disk waterline)$' -count=1`

原始输出：`ok  github.com/Xsxdot/handoff/cmd 2.111s`；`ok  github.com/Xsxdot/handoff/internal/agentd 0.221s`。

判断：review-1 要求的两个新增接缝测试均通过，且测试入口分别是 `runCardWait`、`SetupAutomation + consumeAutomationEventsOnce`。

变异自验 1：临时将 `cmd/card_wait.go` 的 `ev.SourceTask != snapshot.Attempt` 反转为 `==`；命中上下文唯一，未改 imports/变量，编译通过，先跑单条行为断言 `go test ./cmd -run '^TestB349CardWaitSubtreeUsesEventCardIdentity$' -count=1`。

原始输出关键失败：`--- FAIL: TestB349CardWaitSubtreeUsesEventCardIdentity`；日志显示当前子卡事件被 source identity mismatch 跳过，随后测试报 `stdout 不是 ledger.Event JSON: unexpected end of JSON input`。随后恢复原比较符。

变异自验 2：临时将 `internal/agentd/server.go` 坏 cursor 错误分支的 `loadedCursor = 0` 改为 `999999`；编译通过，先跑单条行为断言 `go test ./internal/agentd -run '^TestB349AutomationCursorPersistence/corrupt or unreadable cursor/corrupt json$' -count=1`。

原始输出关键失败：`wakeconsumer_test.go:1020: 损坏 cursor 不应装配为已保存水位: 999999`；随后恢复 `loadedCursor = 0`。两次变异均实际改变行为并被新增测试拦截，未保留变异。

命令：`gofmt -w cmd/card_wait_test.go internal/agentd/wakeconsumer_test.go && go test ./internal/agentd -run 'Test(B349|B2336StaleAttempt|B353Automation|AutomationAttachDefersAndThenWakes)' -count=1 && go test ./cmd -run 'Test(B349|B353)CardWait' -count=1`

原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd 4.044s`；`ok  github.com/Xsxdot/handoff/cmd 20.264s`。

判断：恢复生产文件后，B349 新增接缝、既有 stale/policy/attach、cursor 及 B353 card wait 回归均通过。

命令：`go build ./... && git diff --check`

原始输出：无输出，退出码 0。

判断：全仓编译与差异空白检查通过。

命令：`go test ./... -count=1`

原始输出关键结果：根包 `ok`；`internal/agentd` `ok 182.276s`；其余已执行 internal 包均为 `ok` 或 `[no test files]`；命令退出码 1，`cmd` 包失败。

命令：`go test ./cmd -run '^TestRepoContractGate$' -count=1`

原始输出：

    --- FAIL: TestRepoContractGate (0.04s)
        graph_gate_test.go:38: 契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）
        graph_gate_test.go:38: 契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）
        graph_gate_test.go:40: legacy 命中: map[d_cli->d_execution:11 d_cli->d_gateway:1 d_cli->d_ledger:4 d_cli->d_maintenance:18 d_cli->d_orchestration:13 d_cli->d_policy:32 d_cli->d_protocol:1 d_cli->d_sessions:2 d_cli->d_transport:10 d_cli->d_workspace:9 d_collab->d_protocol:1 d_gateway->d_execution:1 d_gateway->d_ledger:1 d_gateway->d_maintenance:7 d_gateway->d_orchestration:210 d_gateway->d_policy:23 d_gateway->d_protocol:1 d_gateway->d_sessions:12 d_gateway->d_transport:1 d_gateway->d_workspace:1 d_keystone->d_protocol:2 d_ledger->d_protocol:2 d_maintenance->d_protocol:1 d_maintenance->d_transport:3 d_orchestration->d_execution:26 d_orchestration->d_gateway:1 d_orchestration->d_maintenance:5 d_orchestration->d_policy:9 d_orchestration->d_protocol:5 d_orchestration->d_sessions:1 d_orchestration->d_transport:2 d_orchestration->d_workspace:18 d_policy->d_maintenance:2 d_policy->d_transport:2 d_sessions->d_execution:6 d_transport->d_policy:4 d_workspace->d_orchestration:2 d_workspace->d_protocol:3]，warn 147 条
    FAIL github.com/Xsxdot/handoff/cmd 0.042s

判断：全量测试未发现本卡新增测试失败；唯一可复现的 cmd 失败为计划基线已记录的两项契约门禁及 legacy 警告统计，不修改本卡范围处理。

命令：`git add cmd/card_wait_test.go internal/agentd/wakeconsumer_test.go docs/superpowers/ledgers/2026-09-09-b349-plan-ledger.md && git commit -m "test(B349): cover subtree identity and cursor recovery"`

原始输出：`[cards/B349-charter-5 17805758] test(B349): cover subtree identity and cursor recovery`；`3 files changed, 226 insertions(+)`。

判断：review-1 要求的补缝测试已形成提交；本条收入后只执行一次 amend，最终 hash 以 amend 后 HEAD 为准，不回写该历史读数。
