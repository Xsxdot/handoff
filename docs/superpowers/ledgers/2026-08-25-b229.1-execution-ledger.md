# B229.1 执行台账：T1 执行机切换与本地解析退役

- 2026-08-25：开工核对 `git status --short --branch`；原始输出：`## cards/B229.1-charter`（工作树干净），HEAD=e24b9a31（breakdown 提交），`git merge-base --is-ancestor cards/B229-charter-2 HEAD` 通过——有效基线是本分支祖先，符合卡面要求。
- 2026-08-25：P4 裁断推断（判断，非新假设）：仓内无 P4 拍板记录（grep ledgers/specs 零命中、其余 B229.x 分支均停在 e24b9a31）；但①冻结契约 §2.5.4 明文「`<DataDir>/discipline/` 不再被读取」与 P4(b)「端点继续读写死目录」互斥；②本卡拆除判据 grep 无条件含 `discipline.Write` 生产代码零命中，(b) 方案无法满足。两条独立证据都只有 (a) 自洽，故按 P4=(a) 实施（file 端点拒服务）。此推断随本条留痕。
- 2026-08-25：读 spec / b229-contract.md / b229-breakdown.md 全文；T1 验收判据以卡面注入为准。
- 2026-08-25：实读 manager.go 现状——`resolveDisciplineFor` :359、构造点 :270、三调用点 :760/:1271/:3380、Cold 拒绝 vs 热重连 Error 不阻断的既有不对称在 :1272-1291/:3382-3396；DispatchReq 已有 `DisciplineText/DisciplineVersion` 字段（Ticket 0 声明）。
- 2026-08-25：实读 server.go dispatchRequest :1084——wire 三键 `discipline/discipline_text/discipline_version` 已解码并透传 DispatchReq（Ticket 0），本票 server 侧零改动。
- 2026-08-25：grep 全仓生产代码对 discipline 包的调用面：manager.go（Resolver 构造+For/ByName/Compose）、discipline.go 端点（Dir/List/Read/Write/DefaultTierFor/Builtins）、ledgerapi.go:658（Name* 五常量+List(Dir())）、cmd/agentd.go:201（NewManager 传 srv.DisciplineMapping）。proto.DisciplineResp 类型保留不动。
- 2026-08-25：`discipline.Name*` 五常量被 ledger/ledgerstep/cmd 的**测试**引用且不在拆除 grep 清单内——决定保留常量，只删 embed/Tier/defaultTier/builtinFor/builtinByName/Builtins/DefaultTierFor，避免越界碰他包文件集。
- 2026-08-25：设计定案——正文落盘文件名 `<taskDir>/discipline.md`；落盘点排在 plan 写盘之后、CreateTask/ad.Start 之前（失败即无任务行，补偿 defer 清工作区）；continue/resume 读同一文件逐字节回注；任务目录写入经 `writeTaskFile` 测试缝（沿 sweepProcs 先例，生产恒 nil 退 os.WriteFile）供「落盘失败→不启动」测试锁。
- 2026-08-25：TDD 红——新增 internal/agentd/dispatch_discipline_test.go 八个用例（收文即用逐字节、点名无正文拒派×磁盘有无两态、空正文未点名不注入、先落盘后启动探针、落盘失败不启动、continue 消费落盘正文/Cold 缺失拒绝/热重连不阻断），先跑确认失败原因=功能缺失。
- 2026-08-25：首红实读——编译红仅 `disciplineFileName`/`writeTaskFile` 两处新建缝符号缺席（核实非拼写错）；补缝壳（常量+writeTaskDirFile 助手+plan 写盘走缝）后再跑，八用例全为断言红（注入的是内置块、拒派缺失、零注缺失），确认断言有牙后才进实现。
- 2026-08-25：实现落地——manager.go 删 resolveDisciplineFor 与 Resolver 字段/构造/Preflight；Dispatch 收文即用（点名无正文→errDisciplineResolveFailed 拒派）、正文落 `<taskDir>/discipline.md` 先于 CreateTask/ad.Start、Task.DisciplineVersion 随 CreateTask 落库；resumeForContinue/ResumeTask 改 loadPersistedDiscipline 读落盘正文，沿用 Cold 拒绝/热重连 Error 不阻断不对称；NewManager 签名去 discMapping 参数。
- 2026-08-25：`go test ./internal/agentd -run 'TestDispatch…|TestContinue…|TestResumeHotReconnect…'` 首轮 7/8 绿，TestDispatchConsumesDeliveredText 红：「落盘任务 version/name = 0/review, want 3/review」——store 层无 discipline_version 列。按 store.go 自述的加列四处改法补齐：建表 DDL、迁移 map、INSERT 列清单与占位符与实参、taskColumns 常量+scanTaskRow。改后同批八用例全 ok（原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 1.073s`）。
- 2026-08-25：旧行为测试清理（被新判据取代）：manager_test.go 删 （钉的全是本卡刻意移除的内置兜底/执行机组装行为；未知名拒发归缝 1 由 T3 穿线）；resume_test.go 删三个 Reparses/Unresolvable 用例（由 dispatch_discipline_test.go 的落盘正文三态取代）。
- 2026-08-25：NewManager 去 discMapping 参数波及点逐一更新：cmd/agentd.go:201 及 agentd 包内测试构造共十余处（env/executor_default/discipline/manager/integration/projectadmin/reclaim/update/status/server_test/client_test）。
- 2026-08-25：端点改造落地（TDD：discipline_test.go 全文重写先红后绿）——GET /api/discipline 恒 200 且 Builtins/Files 为 wire 层空数组 `[]`（raw body 断言，防 null）；Dir 字段置空；Bindings 的 DefaultTier 恒空串；GET/PUT /api/discipline/file 改 410 + 文案「纪律块已入账本：请用 handoff discipline put/get/list 管理…」（PUT 不碰磁盘）；mapping PUT 语义不动（file 档仍校验纯文件名+可读，判据内联等价实现）。ledgerapi.go 下拉改 s.ledger.ListDisciplineNames()，账本读不了如实 500。
- 2026-08-25：`go test ./internal/agentd -run 'TestDisciplinesListFromLedgerOnly|TestDisciplineGetReturnsEmptyCatalog|TestDisciplineFileEndpointsRetired|TestDisciplineMapping' -count=1 -v` 六用例全 PASS。
- 2026-08-25：拆除执行——git rm internal/discipline/{resolver.go,resolver_test.go,files.go,files_test.go} 与 builtin/ 六份 .md；discipline.go 重写为角色名词汇表+Block 类型。拆除判据 grep 实测：生产代码对 discipline 包符号零命中；字面 grep 仅余 envfile.NewResolver/envfile.Dir 两处——envfile 是另一子系统的 B19 env 机制（同名符号碰撞），不在本卡范围，如实记录不掩盖。
- 2026-08-25：全仓 `go vet ./...` 无输出；相邻包回归 `go test ./internal/store ./internal/ledger ./internal/ledgerstep ./internal/client ./cmd -count=1` 五包全 ok（store 加列后迁移与既有用例无恙）。
- 2026-08-25：变异自验①（守卫失活）：把 Dispatch 拒派条件的守卫改为恒假（唯一性 grep 计数=1 先核）→ 编译过 → TestDispatchRefusesNamedWithoutDeliveredText 两子态全 FAIL → 还原。证明反向断言有牙。
- 2026-08-25：**事故与恢复**：变异①还原时误用 `git checkout internal/agentd/manager.go`，把尚未提交的实现整体回滚到 HEAD。处置：按本台账留痕的编辑记录逐块重建 manager.go（收文即用守卫/落盘/continue/resume/NewManager 签名/缝字段全部复原），八用例复跑全 ok 后才继续。教训：变异还原必须用备份文件或反向 patch，禁止 git checkout 未提交文件。
- 2026-08-25：变异自验②（降级复活）：loadPersistedDiscipline 读失败分支改为返回 ("",nil)（先核唯一命中）→ 编译过 → TestContinueColdNamedWithoutPersistedTextIsRefused/named/refuse FAIL、热重连用例保持绿（符合预期语义差）→ 从备份 cp 还原并编译确认。两条承重断言都被证实拦得住。
- 2026-08-25：修测试卫生问题——TestDisciplinesListFromLedgerOnly 曾把磁盘残留文件写到 conf().DataDir 拼出的相对路径，污染仓库工作树（internal/agentd/discipline/disk-only.md）；改用 t.TempDir() 并删除残留目录。
- 2026-08-25：终验——`gofmt -l internal/ cmd/` 无输出（manager.go/resume_test.go 补格式化）；`go build ./... && go test ./internal/agentd ./internal/discipline ./internal/config -count=1` 原始输出 BUILD_OK + 三包 ok（agentd 118.5s / discipline 0.003s / config 0.013s）。验收判据逐条核对：收文即用✓ 反向拒派×2态✓ 空正文未点名零注入✓ continue 落盘正文/Cold 拒绝/热重连不阻断✓ DisciplineVersion 落盘读回✓ 先落盘后启动探针+落盘失败不启动✓ 拆除 grep（envfile 同名例外已记）✓ builtin 目录与 resolver.go 已删✓ GET 恒空数组✓ 下拉改账本源含种子不含磁盘✓ file 端点 410 可行动文案且 PUT 零落盘✓。
