# B233.3 implement 台账

节点：charter/implement  
卡：B233.3  
分支：cards/B233.3-charter-4  
基线 HEAD：3db68ff4aba9735c5fbe7174041f42774604e5a5  
规则：每确立一个事实立刻追加一行；测试必须先见红再写实现；变异自验确认有牙；提交事实记命令与原始输出，随后只 amend 一次。

---

## 2026-09-07 开工检查

- 角色：implement 执行者。按 `docs/superpowers/plans/b233.3-plan.md` 逐步执行 Task 1–Task 6（T5, T1, T2, T4, T3, T6）。
- 纪律红线：
  - 不派发、不调用 handoff CLI、不起新的 executor 进程或子任务。
  - 提问纪律：需要人决策输出单行 JSON `{"ask":"..."}` 并结束回合。
  - 收尾纪律：git add 并 commit（不要 push），输出单行 JSON `{"branch":"...","commit":"...","summary":"..."}`。
  - 拍板不得改写：P1(a), P2(a), P3(a), P4(a), P5(a), P6(a), P7(a)。
  - TDD 铁律：先红后绿，无失败测试不写生产代码。变异自验。日志与注释。
- 基线检查：
  - `git status`: On branch cards/B233.3-charter-4, working tree clean.
  - `git rev-parse HEAD`: 3db68ff4aba9735c5fbe7174041f42774604e5a5.
  - `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.3-charter check`: fail_count=2 (ApprovalClient dead-contract, dead-interface)，warn_count=145。与计划预期一致。
- 基线测试：
  - `go build -o /dev/null .`: 退出 0。
  - `go test ./internal/targetclient ./internal/relay ./internal/client -count=1`: 全绿（targetclient 5.180s, relay 0.026s, client 9.348s）。
  - `go test ./internal/agentd -run 'TestForwardWorktreeStripsCardIDsAndAttachesLocally|TestForwardWorktreeErrorAndCancel' -count=1`: 全绿（1.236s）。

## Task 1（T5）：建树转发停写卡——传输搬运，应用挂卡

- TDD 红灯：
  - 新建 `internal/agentd/forward_ledger_guard_test.go`。
  - `go test ./internal/agentd -run TestForwardSourceDoesNotWriteLedger -count=1`：断言红（0.003s，`forward_ledger_guard_test.go:23: forward.go 是传输路径，不得写账本（含 "attachCardBaseBranches"）`）。
- T1 最小实现与绿灯：
  - `internal/agentd/forward.go`: `forwardWorktreeIfRequested` 返回 `(handled bool, ws *proto.Workspace, cardIDs []string)`，传输层不再写账本与挂卡，成功时返回目标 workspace 与原始 cardIDs 交由应用层挂卡；`forwardJSON` 文档注释同步更新。
  - `internal/agentd/projectadmin.go`: `handleProjectWorktreeCreate` 接管跨机转发成功的挂卡逻辑，`s.attachCardBaseBranches(*forwarded, cardIDs, s.ledgerActor(r))` 后再写 200 响应。
  - 测绿：`go test ./internal/agentd -run 'TestForwardSourceDoesNotWriteLedger|TestForwardWorktreeStripsCardIDsAndAttachesLocally|TestForwardWorktreeErrorAndCancel' -count=1`（ok 1.306s）。
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
- 变异自验：
  - 变异 `projectadmin.go:921` `s.ledger != nil` -> `s.ledger == nil`。
  - 跑测：`go test ./internal/agentd -run TestForwardWorktreeStripsCardIDsAndAttachesLocally -count=1` 报红（`forward_test.go:268: 本地挂卡结果错误: [] body=...`），确认测试具有阻拦能力。
  - 还原变异，重跑全绿（1.306s）。

## Task 2（T1）：选路工厂与池

- 锁现状测试：
  - `internal/targetclient/pool_test.go`: 补充 `TestNewLocalDoesNotEnterPool`（验证 NewLocal 不入池）和 `TestPoolForReturnedClientMustStayReusable`（验证调用方关空闲连接不迫使池重建 client）。
  - `internal/targetclient/warm_test.go`: 补充 `TestWarmSuccessDoesNotProbeAgentdHTTP`（验证预热成功不得去拨 agentd HTTP），补齐 `net/http`、`net/http/httptest`、`strings` import。
  - `internal/targetclient/warm.go`: 文档注释补齐说明「预热成功只说明隧道，不能当成 agentd 可达（冻结 9）」。
  - 跑测：`go test ./internal/targetclient -run 'TestNewLocalDoesNotEnterPool|TestPoolForReturnedClientMustStayReusable|TestWarmSuccessDoesNotProbeAgentdHTTP|TestNewDirect|TestNewRelay' -count=1`（ok 0.003s）。
  - 包级测试：`go test ./internal/targetclient -count=1`（ok 5.175s）。
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
- 变异自验：
  - 变异 `warm_test.go:131` `hits != 0` -> `hits == 0`。
  - 跑测：`go test ./internal/targetclient -run TestWarmSuccessDoesNotProbeAgentdHTTP -count=1` 报红（`warm_test.go:132: Warm 成功不得去拨 agentd HTTP，hits=0`）。
  - 还原变异，重跑全绿（0.003s）。

## Task 3（T2）：能力面接线与裸 HTTP 白名单

- 守卫测试与日志落地：
  - 新建 `cmd/execution_surface_test.go`，测试 `TestCLIExecutionCommandsDoNotHandRollHTTP`，断言 CLI 5 个生产执行命令（`dispatch.go`, `wait.go`, `reply.go`, `continue.go`, `stop.go`）不手拼 HTTP（不含 `.HTTPClient()`、`http.NewRequest`、`client.NewRelay(`）。
  - 跑测：`go test ./cmd -run TestCLIExecutionCommandsDoNotHandRollHTTP -count=1`（ok 0.004s）。
  - 跑既有白名单：`go test ./internal/client -run 'TestProductionHTTPClientCallersAreGatewayOnly|TestTransportAndExecutionClientCompileOnAggregate' -count=1`（ok 0.013s）。
  - `internal/client/client.go`: 为 `Dispatch`、`Reply`、`Continue`、`Stop` 补充结构化日志（入口 Info、错误分支 Error 带 cause、成功路径 Info 带关键标识）。
  - 回归测试：`go test ./internal/client ./cmd -run 'TestTransportAndExecutionClientCompileOnAggregate|TestProductionHTTPClientCallersAreGatewayOnly|TestWSDialOptionsPinsClientTransport|TestDispatchOmitsStandardIdempotencyHeaders|TestCLIExecutionCommandsDoNotHandRollHTTP|TestReplyRoundTrip|TestWaitEventSkipsProgress' -count=1`（client 0.140s, cmd 0.014s 全绿）。
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
- 变异自验：
  - 在 `cmd/dispatch.go` 引入可编译变异 `var _ = ".HTTPClient()"`。
  - 跑测：`go test ./cmd -run TestCLIExecutionCommandsDoNotHandRollHTTP -count=1` 报红（`execution_surface_test.go:27: dispatch.go 生产路径不得手拼 HTTP / 取裸客户端（含 ".HTTPClient()"）`）。
  - 还原变异，重跑全绿（0.004s）。

## Task 4（T4）：可交付策略归属

- 策略归属与日志补充：
  - `internal/client/client.go`: 在 `waitOnce` 与 `FollowEvents` 的不可交付跳过分支增加 `Debug` 级日志（`"wait/follow 跳过不可交付事件", "task", taskID, "seq", ev.Seq, "type", ev.Type`），并在 `waitOnce` 补充 why 注释（策略在应用谓词，传输 `streamOnce` 必须仍见到该帧）。
  - 跑测：`go test ./internal/client -run 'TestWaitDeliveryPolicyMatchesIsDeliverableAlias|TestIsDeliverableAliasesWaitDeliveryPolicy|TestFollowAllDeliversAuditEvents|TestWaitEventSkipsProgress' -count=1`（ok 0.047s）。
  - 跑包测试：`go test ./internal/client -run 'TestFollow' -count=1`（ok 4.943s）。
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
- 变异自验：
  - 变异 `client.go:1630` `!isDeliverable(ev.Type)` -> `isDeliverable(ev.Type)`。
  - 跑测：`go test ./internal/client -run TestWaitEventSkipsProgress -count=1` 报红（`client_test.go:200: WaitEvent 返回 seq=1, want 2（question 的 seq）`）。
  - 还原变异，重跑全绿（0.078s）。

## Task 5（T3）：失败诊断——不可达 ≠ 隧道断开 ≠ ctx 取消

- TDD 红灯：
  - 在 `internal/client/execution_test.go` 追加 `TestRelayClosedDispatchIsTunnelDisconnected`、`TestRelayCanceledDispatchIsNotTunnelOrUnreachable`、`TestMarkForwardedRelayCopyKeepsTunnelSentinel`。
  - 跑测：`go test ./internal/client -run 'TestRelayClosedDispatchIsTunnelDisconnected|TestRelayCanceledDispatchIsNotTunnelOrUnreachable|TestMarkForwardedRelayCopyKeepsTunnelSentinel' -count=1`。
  - 断言红（`TestRelayClosedDispatchIsTunnelDisconnected` 报 `要 ErrTunnelDisconnected，实得 dispatch 请求: 对端 agentd 够不着: ...`；`TestMarkForwardedRelayCopyKeepsTunnelSentinel` 同报断言红；取消用例验证通过）。
- T5 最小实现与绿灯：
  - `internal/client/client.go`: `Client` 结构体新增 `relayBacked bool` 字段，并在 `NewRelay` 显式置 `true`；`MarkForwarded` 与 `NoRedirect` 副本拷贝补齐 `relayBacked: c.relayBacked`；`do` 方法在 `hc.Do` 失败时若为 `c.relayBacked` 则包装 `ErrTunnelDisconnected`，直连仍包装 `ErrUnreachable`，ctx 取消/超时保持原样上抛；`Dispatch`、`Reply`、`Continue`、`Stop` 入口日志补齐 `"relay", c.relayBacked`。
  - `internal/client/execution.go`: 更新 `ErrTunnelDisconnected` 注释，阐明生产路径已由 `Client.do` 在 `relayBacked` 时包装该哨兵，与 `ErrUnreachable` 互斥。
  - 测绿：`go test ./internal/client -run 'TestRelay|TestErrTunnel|TestUnreachable|TestNotUnreachable|TestDispatchOmits' -count=1`（ok 0.060s）。
  - 包级测试：`go test ./internal/client -count=1`（ok 9.409s）。
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
- 变异自验：
  - 变异 `client.go:415` `c.relayBacked` -> `!c.relayBacked`。
  - 跑测：`go test ./internal/client -run TestRelayClosedDispatchIsTunnelDisconnected -count=1` 报红（`execution_test.go:166: 要 ErrTunnelDisconnected，实得 dispatch 请求: 对端 agentd 够不着: ...`）。
  - 还原变异，重跑全绿（0.011s）。

## Task 6（T6）：旁路、一次 Do、金样本、CHANGELOG、真机清单

- TDD 红灯与锁现状测试：
  - 在 `internal/client/execution_test.go` 增加 `countRoundTripper` 与 `TestDispatchDoOncePerCall`，锁定单次 Dispatch 只调一次 `hc.Do`（P5-(a) 禁重试进 do）。跑测：`go test ./internal/client -run TestDispatchDoOncePerCall -count=1`（ok 0.004s）。
  - 在 `cmd/upgrade_test.go` 追加 `TestUpgradeProductionDoesNotCallNewRelay` 源码守卫。
  - 跑测：`go test ./cmd -run TestUpgradeProductionDoesNotCallNewRelay -count=1`。
  - 断言红（`upgrade_test.go:47: cmd/upgrade.go 生产路径不得调用 client.NewRelay；具名 relay 走 targetclient.New`）。
- T6 最小实现与绿灯：
  - `cmd/upgrade.go`: 引入 `internal/targetclient`；`newAgentdClient` 改用 `targetclient.New` 工厂选路，移除旁路 `relay.NewDialer` + `client.NewRelay`；定义 `upgradeDialErr` 替身满足 `agentdPeer` 接口在选路失败时的延迟报错契约。
  - `CHANGELOG.md`: 在 `[Unreleased]` 的 `### 变更` 写入 B233.3 跨机失败诊断与建树转发说明。
  - 测绿：`go test ./cmd -run 'TestUpgradeProductionDoesNotCallNewRelay|TestUpgradeWaitTimeoutPushLeavesRoomForWindowsRepetition' -count=1`（ok 0.007s）。
  - 测绿：`go test ./internal/client -run 'TestDispatchDoOncePerCall|TestDispatchOmitsStandardIdempotencyHeaders|TestRelayClosedDispatchIsTunnelDisconnected|TestProductionHTTPClientCallersAreGatewayOnly' -count=1`（ok 0.015s）。
  - 全仓编译：`go build -o /dev/null .`（exit 0）。
- 变异自验：
  - 变异 `internal/client/execution_test.go` `ct.n != 1` -> `ct.n != 2`。
  - 跑测：`go test ./internal/client -run TestDispatchDoOncePerCall -count=1` 报红（`execution_test.go:228: do 单次调用 hc.Do 次数=1，want 1（重试不得放进 do）`）。
  - 还原变异，重跑全绿（0.004s）。

## 触及包全量与图闸验收

- 触及包测试：
  - `go test ./internal/targetclient ./internal/relay -count=1`: 全绿（targetclient 5.169s, relay 0.010s）。
  - `go test ./internal/agentd -count=1`: 全绿（202.179s）。
  - `go test ./internal/client -count=1`: 全绿（9.409s）。
  - `go test ./cmd -skip TestRepoContractGate -count=1`: 全绿（16.099s）。（注：`TestRepoContractGate` 对应基线遗留的 2 处 ApprovalClient dead 违规，见下方图闸核对）。
- 图闸核对：
  - `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.3-charter check`: fails=2（`d_execution→d_orchestration` 的 dead-contract 与 dead-interface ApprovalClient，与基线计划完全一致，未增减）。
- 全仓编译：
  - `go build -o /dev/null .`: 退出 0。
- 真机声明：
  - 依据 plan §11，真机项归协调者执行，机内标明「未验证，需真机」；未用真重试器验单任务（P5-(a) 锁单次 hc.Do 与禁标准幂等头已机内验证通过）；无 relay 节点拓扑。

## 提交事实（历史读数，随后 amend 一次收口）

```
git commit -m "feat(B233.3): 跨机访问收口（能力 client 复用直连与 relay 传输）"
[cards/B233.3-charter-4 00c2af31] feat(B233.3): 跨机访问收口（能力 client 复用直连与 relay 传输）
 14 files changed, 494 insertions(+), 43 deletions(-)
 create mode 100644 cmd/execution_surface_test.go
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.3-implement-ledger.md
 create mode 100644 internal/agentd/forward_ledger_guard_test.go
```









