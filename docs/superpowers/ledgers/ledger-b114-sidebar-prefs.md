# B114 左栏偏好与手工工作树执行记录

| Task | 状态 | Commit 范围 | 自动化验证实得输出 |
|---|---|---|---|
| 1 | 完成 | `1230a96f`：proto 契约、fixture、TS 类型、ledger | `go test ./internal/proto/ -run TestContractFixtures -update`：`ok github.com/Xsxdot/handoff/internal/proto 0.628s`；`go test ./internal/proto/`：`ok github.com/Xsxdot/handoff/internal/proto 0.215s`；`cd web && npx vitest run src/api/contract.test.ts && npx tsc -b`：`1` 个文件、`22` 个测试通过；`gofmt -l . && go test ./internal/...`：exit 0，`gofmt -l .` 无输出。首次前端运行因缺失依赖失败，随后 `npm ci` 成功安装 287 packages；首次沙箱运行因 `/var/folders` EPERM 失败，提升权限后通过。 |
| 2 | 完成 | `5f3a7db2`：`manualworktree.go`、`manualworktree_test.go`、ledger | 预期失败：编译报 `undefined: CreateManualWorktree`、`undefined: ErrBadWorktreeReq`、`undefined: ManualWorktreeRoot`；首轮实现编译报既有测试 helper `branchExists redeclared`，改名为 `manualBranchExists` 后修复；`go test ./internal/agentd/ -run 'TestCreateManualWorktree|TestManualWorktreeRoot' -v`：全部成功（新建/已有/推导 + 9 条拒绝 + 根目录）；`gofmt -l . && go test ./internal/...`：exit 0，`gofmt -l .` 无输出；收尾重跑目标测试：全部成功。 |
| 3 | 完成 | `bbc6c918`：`projectadmin.go`、`server.go`、handler tests、ledger | 路由未注册时预期失败：未知项目 404 用例通过，其余建树/分支列表用例均返回原始 `404 page not found`；接入 handler 与路由后 `go test ./internal/agentd/ -run 'TestProjectWorktreeCreate|TestProjectBranches' -v`：6 个用例全部 PASS；`gofmt -l . && go test ./internal/...`：exit 0，`gofmt -l .` 无输出。 |
| 4 | 完成 | `b5096a28`：`web/src/api/client.ts`、`client.test.ts`、ledger | 首次 Vitest 沙箱运行因 `/var/folders` EPERM 失败；提升权限后首次运行 10 个测试中 1 个失败，原因为新 spy 未恢复、收到上一用例 URL；补 `mockRestore()` 后 `npx vitest run src/api/client.test.ts && npx tsc -b`：`1` 个文件、`10` 个测试通过；`npx vitest run`：`67` 个文件、`703` 个测试通过。 |
| 5 | 完成 | `79f38412`：`treePrefs.ts`、`treePrefs.test.ts`、ledger | `npx vitest run src/app/tree/treePrefs.test.ts`：`1` 个文件、`14` 个测试通过；`npx vitest run && npx tsc -b`：`68` 个文件、`717` 个测试通过，TypeScript 编译 exit 0。 |
| 6 | 完成 | `de4d05c8`：`IconMenu.tsx`、`IconMenu.test.tsx`、ledger | 预期失败：扩展项 4 个用例失败，老用例 7 个通过；实现后 `npx vitest run src/app/lib/IconMenu.test.tsx`：`1` 个文件、`11` 个测试通过；`npx vitest run && npx tsc -b`：`68` 个文件、`722` 个测试通过，TypeScript 编译 exit 0。 |
| 7 | 完成 | `d8594b41`：`TreePrefsMenu.tsx`、`ProjectTree.tsx`、`ProjectTree.test.tsx`、ledger | 预期失败：新增显示偏好 4 个用例均因找不到按钮失败，既有 38 个通过；接入后因 `keepOpen` 菜单选项仍含项目名，2 个断言误查全局文本，改为限定 `tree-scroll`；`npx vitest run src/app/tree/ProjectTree.test.tsx && npx tsc -b`：`1` 个文件、`42` 个测试通过；`npx vitest run`：`68` 个文件、`726` 个测试通过。 |
| 8 | 完成 | `c8b73e78`：`NewWorktreeDialog.tsx`、`NewWorktreeDialog.test.tsx`、ledger | 预期失败：模块不存在；实现后 `npx vitest run src/app/tree/NewWorktreeDialog.test.tsx && npx tsc -b`：`1` 个文件、`7` 个测试通过；`npx vitest run`：`69` 个文件、`733` 个测试通过。 |
| 9 | 完成 | `332a28ff`：`ProjectTree.tsx`、`ProjectTree.test.tsx`、`Shell.tsx`、ledger | 预期失败：机器入口新增 2 个用例因找不到「新建工作树」按钮失败，既有 43 个通过；实现后 `npx vitest run src/app/tree/ProjectTree.test.tsx && npx tsc -b`：`1` 个文件、`45` 个测试通过；`npx vitest run && npx tsc -b && npx eslint .`：`69` 个文件、`736` 个测试通过，tsc exit 0，eslint `0 errors / 13 warnings`。 |
| 10 | 完成 | 本收尾 commit（`docs(ledger): B114 执行记录`）：补齐 3 个测试文件头注释及 2 个新组件导出声明注释；终审 ledger | `gofmt -l . && go build ./... && go vet ./... && go test ./...`：exit 0，`gofmt -l .` 无输出；`cd web && npx tsc -b && npx eslint . && npx vitest run && npm run build`：exit 0，`69` 个文件、`736` 个测试通过，eslint `0 errors / 13 warnings`，Vite 构建成功（1931 modules）；`git diff --check` 无输出；`git diff -- internal/ | grep -n "RemoveAll"`、`grep -n "fmt\\.Printf"` 与前端 `grep -n "console\\.log"` 均无输出；spec §7 需要肉眼看页面的 3 项：未验：无浏览器。 |
| 审核修复 | 完成 | 本提交：`ProjectTree` 右键新建工作树入口复用 `locationProblem`，新增断开机器下「无新建、保留编辑/注销」回归用例 | 红测：`npx vitest run src/app/tree/ProjectTree.test.tsx` 为 `46` 个中 `1` 个失败，实际出现断开机器的「新建工作树」；修复后目标测试 `46/46` 通过；`cd web && npx vitest run && npx tsc -b && npx eslint .`：`69` 个文件、`737` 个测试通过，tsc exit 0，eslint `0 errors / 13 warnings`。 |
