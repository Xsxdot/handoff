# B114 左栏偏好与手工工作树执行记录

| Task | 状态 | Commit 范围 | 自动化验证实得输出 |
|---|---|---|---|
| 1 | 完成 | `1230a96f`：proto 契约、fixture、TS 类型、ledger | `go test ./internal/proto/ -run TestContractFixtures -update`：`ok github.com/Xsxdot/handoff/internal/proto 0.628s`；`go test ./internal/proto/`：`ok github.com/Xsxdot/handoff/internal/proto 0.215s`；`cd web && npx vitest run src/api/contract.test.ts && npx tsc -b`：`1` 个文件、`22` 个测试通过；`gofmt -l . && go test ./internal/...`：exit 0，`gofmt -l .` 无输出。首次前端运行因缺失依赖失败，随后 `npm ci` 成功安装 287 packages；首次沙箱运行因 `/var/folders` EPERM 失败，提升权限后通过。 |
| 2 | 完成 | 待提交：`manualworktree.go`、`manualworktree_test.go`、ledger | 预期失败：编译报 `undefined: CreateManualWorktree`、`undefined: ErrBadWorktreeReq`、`undefined: ManualWorktreeRoot`；首轮实现编译报既有测试 helper `branchExists redeclared`，改名为 `manualBranchExists` 后修复；`go test ./internal/agentd/ -run 'TestCreateManualWorktree|TestManualWorktreeRoot' -v`：全部成功（新建/已有/推导 + 9 条拒绝 + 根目录）；`gofmt -l . && go test ./internal/...`：exit 0，`gofmt -l .` 无输出；收尾重跑目标测试：全部成功。 |
| 3 | 完成 | 待提交：`projectadmin.go`、`server.go`、handler tests、ledger | 路由未注册时预期失败：未知项目 404 用例通过，其余建树/分支列表用例均返回原始 `404 page not found`；接入 handler 与路由后 `go test ./internal/agentd/ -run 'TestProjectWorktreeCreate|TestProjectBranches' -v`：6 个用例全部 PASS；`gofmt -l . && go test ./internal/...`：exit 0，`gofmt -l .` 无输出。 |
| 4 | 完成 | 待提交：`web/src/api/client.ts`、`client.test.ts`、ledger | 首次 Vitest 沙箱运行因 `/var/folders` EPERM 失败；提升权限后首次运行 10 个测试中 1 个失败，原因为新 spy 未恢复、收到上一用例 URL；补 `mockRestore()` 后 `npx vitest run src/api/client.test.ts && npx tsc -b`：`1` 个文件、`10` 个测试通过；`npx vitest run`：`67` 个文件、`703` 个测试通过。 |
| 5 | 完成 | 待提交：`treePrefs.ts`、`treePrefs.test.ts`、ledger | `npx vitest run src/app/tree/treePrefs.test.ts`：`1` 个文件、`14` 个测试通过；`npx vitest run && npx tsc -b`：`68` 个文件、`717` 个测试通过，TypeScript 编译 exit 0。 |
