# B402 spec 台账

- 2026-09-25：`handoff card show B402` 读取到卡状态为 `待办`，项目 `handoff`，charter v12；随后执行 `handoff card move B402 spec`，状态成功进入 `spec`（账本 seq 21322）。
- 2026-09-25：读取 B402 现场事实：四起跨载体的零文本/无 trailer 记录；`serve.log` 共同出现 `handoff_exit code=-1`，其中现场 `render.log` 为空，`frames.jsonl` 有大量 reasoning/tool 帧；第 3 起曾在 `turn_failed` 后被自动归档，续接只能重新派发。
- 2026-09-25：代码图查询 `codegraph sym handleResult`、`codegraph sym ParseVerdict`、`codegraph sym Manager.Continue`、`codegraph sym finishTask`；`FinishTask` 图中命中为 `internal/ledgerstep/runner.go#finishTask`；`codegraph context d_execution_adapters` 与 `d_ledger` 受输出截断，新增 B402 符号图覆盖债已记入 spec。
- 2026-09-25：根因核对：`internal/ledgerstep/node.go:278-295` 在 Await 后注册 `FinishTask` defer；`ParseVerdict` 失败转 `needs_human` 后仍会执行 defer。`internal/ledgerstep/wire.go:58-94,137-214` 只返回裸字符串，丢失失败事件分类。
- 2026-09-25：核对现有能力：`internal/client/execution.go:35-42` 已有 `Continue`；`internal/agentd/handlers.go:824-852` 已有非空 instructions 门；`internal/orchestration/manager.go:1395-1482` 已实现 waiting_review 状态门和 executor 恢复阶梯。
- 2026-09-25：核对失败生产点：OpenCode `adapter.go:2204-2234`、Codex `adapter.go:907-915`、Grok `adapter.go:672-682`、Claude Code `adapter.go:886-897`、AGY `adapter.go:683-695` 均为明确零文本失败分支；`turn.NoTrailerResult` 保持无自动重试。
- 2026-09-25：用户裁决：选择 B，仅 `zero_text` 自动重试一次；设计全文已写入 `docs/superpowers/specs/b402.md`，尚未实现代码、尚未运行 B402 专项测试。
- 2026-09-25：当前仓库主工作树仍有既存用户改动，未触碰：`internal/agentd/cardstep.go`、`internal/ledger/types.go`、`.commandcode/`、`node_modules/.vite/deps/`。
