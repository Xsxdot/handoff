# B351 spec 台账

基线：`cards/B233.1-charter-7` @ `6097892b`  
卡：B351 于 2026-09-09 由「待办」移入 spec。

## 现场重建

- 2026-09-09：`handoff card show B351` 当时在「待办」。note seq 10247：B233.6 真机验收发现 Transport 成功后本地快照+挂账失败，远端 task running 无自动回收；同名分支阻断重试（`probe/B6-implement`），需人工 stop + reclaim + 删分支。
- 2026-09-09：`git rev-parse HEAD` → `6097892bd14c5a16c60e2b2abead100da2887d65`。
- 2026-09-09：`codegraph sym ViaTemplate` → `n_ledgerstep_Dispatcher_ViaTemplate` / `d_ledger`。`PurposeRounds` → `n_ledger_Store_PurposeRounds` / `d_ledger` / moved。`Client.Stop` → `n_client_Client_Stop` / `d_transport_channel` / moved。

## 读数

- `PurposeRounds` 实现只扫 `TasksOf`，不看 `EvDispatched`。B233.6 回滚挂账后计数为 0。
- `ViaTemplate` 非审阅：`rounds>0` 才把分支写成 `cards/<卡>-<purpose>-<rounds+1>`，故第二轮是 `-2` 不是 `-1`。
- `Manager.Dispatch`：PrepareWorkspace（建分支）在 CreateTask 之前；撞名发生在 Prepare，失败不落 task 行。B39 defer 仅 `err != nil && !executorStarted`。
- Transport 成功 ⇒ `executorStarted==true`，B39 不会删树删分支。
- `Manager.Stop` 成功路径 `worktreeRemoved` 恒 false（B233.4 留存）。收树必须另走 reclaim。
- 生产 Dispatcher 装配两处：`cmd/card_dispatch.go` 裸模板；`internal/agentd/cardstep.go` `startCardStep`。CLI `--step` 只 `Client.CardStep` POST 本机 agentd。
- 裸 `handoff dispatch` 不写 `card_tasks`，全机「未挂账」扫描会误杀。

## 裁决

- 2026-09-09 用户原话「2」= 方案 **B**：永不删分支；落账失败另记耗费一轮（不是 `EvDispatched`）；`PurposeRounds` 能看见；重试 `-2`。Stop+强制收树与 A 相同。C（pending 覆盖崩溃窗口）未选。

## 判断

- L3 轻档：跨账本挂号、编排失败出口、既有 Stop/Reclaim 执行面；工作量不超过轻档阈值。
- 崩溃窗口、补偿失败后仍 running 的孤儿、跨机真机 → OOS / roadmap，不进本卡。

## 批准

- 2026-09-09：用户原话「推进吧上」。头部回写已批准。下一步 contract。
- 提交命令（历史读数，amend 后 hash 会变）：`git add docs/superpowers/specs/b351.md docs/superpowers/ledgers/2026-09-09-b351-spec-ledger.md docs/superpowers/reviews/b351-spec-review.md && git commit -m "docs(B351): spec 批准 L3 轻档（孤儿 Stop+改名）"`

## 独立审查

- 2026-09-09：subagent `01a08578-7410-7fa1-99d7-497de4998c61`，报告 `docs/superpowers/reviews/b351-spec-review.md`。总判修订后再批。C1 + I1–I3 已吸收进 spec；M1 叫醒理由改准。`codegraph resolve --doc docs/superpowers/specs/b351.md`：短路径 `dispatch_test.go#…` 为 `file_missing`（现状表已改为带包路径的引用）；`Dispatcher.ViaTemplate` / `PurposeRounds` / `runStepDispatch` / `startCardStep` / `Client.Stop` 为 ok 或 moved。
