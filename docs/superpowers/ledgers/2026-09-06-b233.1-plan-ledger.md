# B233.1 plan 台账

节点：charter/plan  
卡：B233.1  
分支：cards/B233.1-charter-4  
法定产出：docs/superpowers/plans/b233.1-plan.md  
规则：每确立一个事实立刻追加一行；提交事实记命令与原始输出，随后只 amend 一次。

---

## 2026-09-06 开工

- 角色：plan 节点。只写计划，不写实现代码、不建脚手架。
- 拍板：breakdown §0 P1–P6 全数 (a)。plan 不得把未选方案写成事实。
- 轻档：单卡单轮 implement，禁止扇出子卡。
- 真机 OpenCode 模型：opencode-go/deepseek-v4-flash；不可用则验收失败贴原文。本节点不派 Codex。
- HEAD `9e7c32badc36ac1d105d9684221c3ce6199cdc05`，分支 `cards/B233.1-charter-4`。拍板提交在 HEAD。
- 有图：`codegraph` 在 PATH（`/Users/sycm/.local/bin/codegraph`）。视图 `cards-B233.1-charter-2`。
- `codegraph check --view cards-B233.1-charter-2`：`fails: []`；`warns` 145（存量 anchor-off-domain / best-dangling / container-misplaced，本卡不修）。
- 实然预算：`d_orchestration→d_execution` 实际 26 / 预算 27。
- `flow n_opencode_Adapter_Send_2`：degraded=false，steps=7；有 pending 则 reply 分支（源码 `adapter.go:554-557` 仍 `takePendingQuestionSnapshot` → `replyPendingQuestion`）。
- `flow n_agentd_Manager_handlePermission` / `handleQuestion` / `waitQuestion` / `WriteTaskEnv` / `isDeliverable` / `Gate.Judge`：degraded=true，steps=[]，已读源码。记图覆盖债。
- `who-calls handlePermission` 短名拒；`n_agentd_Manager_handlePermission` 仅 `handleEvent`。
- `who-calls n_opencode_WriteTaskEnv`：`Adapter.Start`。
- `FindReusableGrant` ← `reuseDecision`；`AnswerTicket` ← `approvePermission` / `handleReply`。
- Ticket 0 空壳在：`internal/executor/{approval,ask,fingerprint}.go`。`StartReq.Approval` 已有字段，Dispatch 未注入。
- 基线测试（亲自跑）：
  - `go test ./internal/executor ./internal/executor/opencode ./internal/permgate ./internal/store ./internal/proto ./internal/client -count=1` 退出 0；耗时 executor 0.589s / opencode 19.923s / permgate 0.382s / store 1.022s / proto 0.832s / client 9.686s。
  - `go test ./internal/executor -run 'TestNamespacedTicketIDGoldenVectors|TestPermFingerprintGoldenVectors|TestApprovalStatusLiterals|TestStartReqApprovalSlotIsOptional' -count=1` ok 0.339s。
  - `go test ./internal/agentd -run 'TestPermFingerprintForMatchesExecutorCanonical|TestGateDecisionGoldenVectors' -count=1` ok 0.544s。
  - `go test ./internal/client -run 'TestPermissionAutoAllowIsNotDeliverable' -count=1` ok 0.314s。
  - `go test ./internal/executor/opencode -run 'TestSendRoutesToQuestionReplyWhenPending|TestWriteTaskEnv$' -count=1` ok 0.272s（旧正向：Send 仍消费 pending；`bash[*]=allow`）。
  - `go build -o /dev/null .` 退出 0，stdout 空。
- 判断：本卡要锁的行为从声明缝今天还得不到（Send 兼答、handlePermission 仍是 OpenCode 权威、isDeliverable 缺 permission_reuse、AnswerTicket 二次 404、WriteTaskEnv 写死 allow）。DAG 第一条必须是 T1 点亮 RespondAsk / Send 不消费。
- P1–P6 全部按 (a) 落笔。禁止 P2(c)、P5(c)。
- 编排不得 import `internal/executor/opencode`。Continue 重写 opencode.json 必须经执行侧可选接口，不能在 manager 里拼 JSON。
- 计划已落盘 `docs/superpowers/plans/b233.1-plan.md`。T1–T6 按拍板 (a)。内部锁仅 `Authorize` 合成步骤（已声明）。真机清单归协调者。
- 自审：未调 handoff CLI；未写实现代码；基线命令均亲自跑到退出码。

## 提交（历史读数，随后 amend 一次）

```
git commit -m "docs(B233.1): 实现计划（派发事件流、Ask 与审批 client）"
[cards/B233.1-charter-4 8ccd22b7] docs(B233.1): 实现计划（派发事件流、Ask 与审批 client）
 2 files changed, 1880 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-06-b233.1-plan-ledger.md
 create mode 100644 docs/superpowers/plans/b233.1-plan.md
```
