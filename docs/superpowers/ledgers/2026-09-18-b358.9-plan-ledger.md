# B358.9 plan 台账（会话身份本质重做：一名多端、人名收口、端只落款）

节点：plan（上游 breakdown `docs/superpowers/specs/b358.9-breakdown.md`，状态「已拍板（2026-09-17，P-1..P-5 全按甲）」；
冻结 contract `docs/superpowers/specs/b358.9-contract.md`；批准 spec `docs/superpowers/specs/b358.9.md`）。
卡：B358.9　有效基线分支：`cards/B233.1-charter-7` @ `94246fc8`（merge-base 实读）。
角色：本节点法定产出 `docs/superpowers/plans/b358.9-plan.md`。不写实现代码、不建卡、不派发、不调用 handoff CLI、不起新 executor。
一行一条历史读数；命令与原始输出照抄，不做二次加工。

## 0. 落点与角色

1. 2026-09-18：本节点法定产出 `docs/superpowers/plans/b358.9-plan.md`（不带日期前缀）；台账本文件。
   只读查图 + 读码 + 跑基线命令；不改生产代码。

## 1. 开工状态位与工作树

2. 2026-09-18：`git branch --show-current` → `cards/B358.9-charter-6`；`git status --porcelain` 无输出。
3. 2026-09-18：`git merge-base HEAD cards/B233.1-charter-7` → `94246fc8a2acc7996c254cb6b473c7c9b5aef225`（= 基线分支头）。
4. 2026-09-18：`git rev-parse HEAD` → `25b7219b2c7107289dfa1c2ad7c3ec708c7b5c1e`（= breakdown 拍板回写提交）。
5. 2026-09-18：contract 头部实读「**上游状态：已批准**」「**冻结状态：本提交随 `codegraph/diffs/cards-B358.9-charter-4.json`…同批冻结**」
   「**有效基线：** `cards/B233.1-charter-7` @ `94246fc8`」——核对通过。
6. 2026-09-18：breakdown 头部实读「状态：**已拍板（2026-09-17，用户拍板；P-1..P-5 全按甲）**」——核对通过。

## 2. 图事实查证（best.json / 分支视图）

7. 2026-09-18：`codegraph summary --repo .` → 「本仓库有代码图：5299 节点 / 6618 边 / 26 领域」。
8. 2026-09-18：`codegraph views --repo .` → 含 `cards-B358.9-charter-4`。
9. 2026-09-18：`codegraph sym Server.resolveConsoleIdentity --view cards-B358.9-charter-4` → `n_agentd_Server_resolveConsoleIdentity` /
   `d_gateway` / `anchor=ok` / `internal/agentd/identity.go:38` / `status=added`。
10. 2026-09-18：`codegraph sym n_proto_MemberIdentity --view cards-B358.9-charter-4` → `d_protocol` / `anchor=ok` /
    `internal/proto/identity.go:45` / `status=added`。
11. 2026-09-18：`codegraph sym Server.roomUserActor --view cards-B358.9-charter-4` → `n_agentd_Server_roomUserActor` /
    `d_gateway` / `anchor=ok` / `internal/agentd/roomsapi.go:49`。
12. 2026-09-18：`codegraph sym Service.sessionMembers --view cards-B358.9-charter-4` → `n_collab_Service_sessionMembers` /
    `d_collab` / `anchor=ok` / `internal/collab/sessions.go:219`。
13. 2026-09-18：`codegraph sym` **未命中**（图覆盖债，见 §6）：`sessionWriters`、`validSessionOwner`、`validSessionMemberIdentity`、
    `cmd.sessionSendActor`。
14. 2026-09-18：`codegraph flow n_agentd_Server_handleRoomSend --view cards-B358.9-charter-4` → `degraded=true steps=0 callers=1
    implementations=0 channels=1`（无控制流数据，回落读源码；未拿 chain 冒充）。
15. 2026-09-18：`codegraph validate --repo . --view cards-B358.9-charter-4` → `issues` 恰一条
    `[cards-B374-charter] 新增容器 k_collab_model 已存在于基线，containersAdded 只接受新容器`；退出非零「发现 1 个完整性问题、
    0 个失鲜节点」（既有、非本卡）。
16. 2026-09-18：`codegraph check --repo . --view cards-B358.9-charter-4` → `fails` 恰一条
    `dead-contract d_transport→d_ledger`；退出非零「契约对照发现 1 处违规」（B370 遗留）。
17. 2026-09-18：`grep -n 'Device' internal/collab/sessions.go internal/collab/service.go internal/collab/room/room.go
    internal/collab/room/delivery.go internal/collab/cursor/cursor.go` → 零命中（端戳尚未落入判定面，契约 §5 D 组条 25 现状成立）。

## 3. 现状代码事实（plan 用）

18. 2026-09-18：`internal/agentd/roomsapi.go:49-51` `roomUserActor` → `return "web:" + hostOnly(r.RemoteAddr)`；
    消费点 `:115`(list)、`:447/:449/:453`(send)、`:467/:468`(read)、`:538/:540/:555`(inbox)，共 11 处 `roomUserActor`。
19. 2026-09-18：`internal/agentd/sessionsapi.go:33-39` `validSessionOwner`（HTTP 重复实现）、`:78`(create 校验)、`:116`(member add 校验)；
    `cmd/session.go:260-266` `validSessionMemberIdentity`（CLI 重复实现）、`:280`(create 校验)。
20. 2026-09-18：`internal/agentd/sessionsapi.go:63-93` `handleSessionCreate` **owner 必填**（`:77-82` 缺/非法即 400）；契约 §3.8 owner 缺省未落。
    `:106` `handleSessionMemberAdd` 用 `AddSessionMember(id, actor, actor)`（B366 自助加入）。
21. 2026-09-18：`internal/collab/sessions.go:219-234` `sessionMembers`：显式成员 kind 默认 `human`，仅当
    `identity==session.Owner && session.Owner!=""` 且前缀 `cli:` 才判 `agent`——`agent:<名>` 成员恒被判 `human`（漂移，待修）。
22. 2026-09-18：`internal/collab/service.go:118-139` `sessionWriters` = `session.Members` ∪ 会话内各卡 `driver_session`（空座不产）；
    `:145-177` `sendToSession` = 归档只读 → `sessionWriters` → `VerifyWriter` → `RecordRoomMessage`。协调者派生现状成立。
23. 2026-09-18：`internal/collab/room/room.go:102` `VerifyWriter`：actor 非空 + 成员逐字相等。
24. 2026-09-18：`internal/proto/rooms.go:37-42` `RoomMessage.Device` 已落（Ticket 0）；`internal/agentd/roomsapi.go` / `ledgerapi.go`
    `grep -n 'Device'` 零命中（发送路径尚未装填）。
25. 2026-09-18：`internal/agentd/auth.go:46-53` `identity{session, device}`；`internal/agentd/server.go:877` 鉴权中间件
    `identity{session: sess.ID, device: sess.DeviceName}` 已装填；`:691` `GET /api/identity` 已注册（不挂 withRooms）。
26. 2026-09-18：`internal/config/config.go:141` `ConsoleUser string yaml:"console_user,omitempty"`；`decodeStrict` 已知键清单 `:578` 含 `console_user`。
27. 2026-09-18：`internal/agentd/identity.go` `resolveConsoleIdentity`：`s.conf().ConsoleUser`、`proto.MemberIdentity`、缺名返回
    `consoleIdentity{Configured:false}`（绝不回落 web:）。
28. 2026-09-18：`web/src/app/rooms/sessionModel.ts:106-108` `isSelfActor(actor) => actor.startsWith('web:')`；
    `web/src/app/rooms/NewSessionDialog.tsx:18-52` owner 手输 + datalist + owner 预检；
    `web/src/api/rooms.ts:213-217` `createSession(title, owner)`；`:202-206` `fetchSessions()`。
29. 2026-09-18：`web/src/app/shell/Shell.tsx:464-490` `confirmCreateSession(title, owner)`：`createSession` → `saveLastSessionOwner` →
    `addSessionMember` 自助补员；`:187-197` `sessionMemberIdentities` memo；`:1049` 传 `memberIdentities`。
30. 2026-09-18：夹具计数：`webUserMember` 在 `internal/agentd/roomsapi_test.go` 17 处、`sessionsapi_test.go` 10 处；两文件是仅有的
    打 `/api/rooms`、`/api/sessions`、`/api/inbox` 的测试文件（grep 证）。
31. 2026-09-18：`internal/agentd/roomsapi_test.go:70-73` `newRoomsEnv` = `newNoPTYLedgerEnv` + seed + `SetupAutomationForTest`；
    `newNoPTYLedgerEnv`（`ledgerapi_test.go:158`）构造 `config.Config{Token: testToken, DataDir: ...}`——**无 ConsoleUser**。
32. 2026-09-18：`web/src/app/rooms/SessionTab.test.tsx`、`SessionChat.test.tsx`、`Shell.test.tsx` 分别 mock `../../api/rooms`；
    除 Shell 外未 mock `fetchIdentity`（T4 需补）。

## 4. 亲跑的可执行事实（本节点依据）

33. 2026-09-18：`go build ./...` → 退出码 `0`，无输出。
34. 2026-09-18：`go test ./internal/proto/ ./internal/config/ -count=1` → `ok github.com/Xsxdot/handoff/internal/proto 0.004s`、
    `ok github.com/Xsxdot/handoff/internal/config 0.012s`。
35. 2026-09-18：`go test ./internal/collab/... -count=1` → `ok .../internal/collab 7.953s`、`.../collab/room 0.002s`、
    `collab/client`、`collab/cursor` 无测试文件。
36. 2026-09-18：`go test ./internal/agentd/ -count=1`（首次）→ 包级 `FAIL github.com/Xsxdot/handoff/internal/agentd 190.801s`；
    复跑 `go test ./internal/agentd/ -count=1 -timeout 15m > /tmp/agentd2.log 2>&1; echo EXIT=$?` → `EXIT=0`、零 `FAIL|panic`。
    首跑包级 FAIL 无具体 FAIL 行（疑似测试环境抖动/超时），复跑全绿。**以复跑为准：agentd 全量绿。**
37. 2026-09-18：`go test ./cmd/ -count=1` → `--- FAIL: TestRepoContractGate`（`graph_gate_test.go:38`
    `契约违规 [dead-contract] 契约 d_transport→d_ledger 声明的方向没有活跃 call…`）；其余 cmd 全绿。
    **基线既有红，非本卡引入**（与 contract §7.3 一致）。
38. 2026-09-18：`go test ./cmd/ -run 'TestSession' -count=1` → `ok github.com/Xsxdot/handoff/cmd 4.611s`。
39. 2026-09-18：`npm ci --prefix web --ignore-scripts --no-audit --no-fund` → `added 290 packages in 2s`（原无 `web/node_modules`；
    `web/.gitignore` 已忽略，不进提交）。
40. 2026-09-18：`cd web && npx tsc -b` → 退出码 `0`，无输出。
41. 2026-09-18：`cd web && npx vitest run` → `Test Files 131 passed (131)` / `Tests 1426 passed (1426)`，退出码 0
    （有一条 `Not implemented: HTMLCanvasElement's getContext()...` 既有 stderr 噪声，不影响通过）。

## 5. 判断与设计决议（本节点）

42. 2026-09-18：**S1 读面 fail-closed 范围**：按 breakdown P-2 甲只覆盖 `GET /api/sessions`、`GET /api/rooms` 两个列表读面；
    `GET /api/rooms/{id}/messages`（历史）无 member 维度、无未读，**不设身份门**（契约 §8.1 与 P-2 均未点它）——写进 plan
    §4 显式声明，防实现轮擅自扩面。
43. 2026-09-18：**S1 未配名时「未配名 ≠ 非成员」**：统一 `requireConsoleIdentity` 先于 owner/member 解析，403 + 文案含
    `console_user`；不与 `ErrNotWriter`(403「非成员」) 混码——靠文案与 `GET /api/identity` 的 `configured` 区分。
44. 2026-09-18：**夹具 ConsoleUser 注入点**：`newRoomsEnv` 内 `srv.cfg.Store(&cfg)` 覆写（改动留在 `roomsapi_test.go` 有界文件集内，
    不动 `ledgerapi_test.go` 的 `newNoPTYLedgerEnv`）。
45. 2026-09-18：核查 `TestSessionMemberAddEndpoint`：其 owner 显式 `user:tester`，T1 后 actor=`user:sycm`；前态 403 的判据是
    「console 成员 `user:sycm` 尚未入列」，与 owner 是谁无关——**无需改 owner 夹具**（本节点初判有误，此处更正）。
46. 2026-09-18：`isSelfActor` 取服务端 identity（P-4 甲）在 `SessionChat` 内自取 `fetchIdentity`（不改 `SessionTab`，守 breakdown 有界文件集）。
47. 2026-09-18：`sessionOwnerPrefs.ts` 在 S4 后无消费者（Shell/NewSessionDialog 均去 owner 输入），列入 T4 文件集删除。
48. 2026-09-18：T2 的 `sessionWriters` 空座断言是**内部锁**（私有方法），理由：从声明缝 `Service.Send` 构造不出「书写者集不含空串」的
    区分性断言（`Send("")` 恒 ErrNotWriter，与空座无关）；已在 plan §3 自我声明。

## 6. 图覆盖债（未命中符号）

49. 2026-09-18：`codegraph sym` 未覆盖（出稿用 `file#Symbol` 源码锚）：`sessionWriters`、`sendToSession`、`validSessionOwner`、
    `validSessionMemberIdentity`、`handleSessionCreate`、`handleSessionsList`、`handleSessionMemberAdd`、`handleSessionJoinCard`、
    `handleSessionArchive`、`handleSessionLeaveCard`、`cmd.sessionSendActor`。收尾跑 `codegraph resolve --doc` 校锚。

## 7. 放弃的尝试

50. 2026-09-18：无。本轮为只读 plan 出稿，无代码改动；未碰 handoff CLI、未起 executor。首跑 agentd 包级 FAIL 未定位到具体用例，
    复跑全绿，按「环境抖动」记录，未做进一步归因。

## 8. 提交（历史读数）

51. 2026-09-18：本节点法定产出物 `docs/superpowers/plans/b358.9-plan.md` 与本台账同批提交。提交命令与原始输出：

```
$ git add docs/superpowers/plans/b358.9-plan.md docs/superpowers/ledgers/2026-09-18-b358.9-plan-ledger.md
$ git commit -m "plan(B358.9): 会话身份本质实现计划——四子卡 T1..T4（gateway 换脸/collab kind/CLI 收敛/web 内容面）"
[cards/B358.9-charter-6 327163da] plan(B358.9): 会话身份本质实现计划——四子卡 T1..T4（gateway 换脸/collab kind/CLI 收敛/web 内容面）
 2 files changed, 1492 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-18-b358.9-plan-ledger.md
 create mode 100644 docs/superpowers/plans/b358.9-plan.md
```

52. 2026-09-18：提交事实只记当时命令与原始输出（历史读数）；amend 换 hash 属 git 事实，不在本文件追写。
    收口判据是工作树干净，不是文件里的 hash 等于 HEAD。
