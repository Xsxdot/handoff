# B233.2 plan 台账

节点：charter / plan
卡：B233.2
工作树：`/Users/sycm/.handoff/worktrees/4d22b67a`
分支：`cards/B233.2-charter-3`
HEAD（开工）：`0f1441d4819ad62501a0e57cbfbe736370f79618`

| 时刻 | 事实 |
|---|---|
| 开工 | HEAD `0f1441d4` 拍板 P1–P6 全选 a。法定产出 `docs/superpowers/plans/b233.2-plan.md`。本节点不写实现。 |
| 图 | `codegraph --repo . --view cards-B233.2-charter check`：2 fail，均为存量 `d_execution→d_orchestration` dead-contract/dead-interface `ApprovalClient`（节点只在 233.1 视图）。warns=145。`d_orchestration→d_execution` 读数 26（预算 27）。 |
| 图 | `sym Provider/BaselineReport/Recoverer/Coordinator/OneShot/Profile/Skills/OneShotArgs/Dispatch/Decide/RunTurn/Install/adaptersForWithProbe/restorer` 命中。`copyCoordinatorRules`、`n_keysclient_Runner` 短名不在图中。`n_executor_Provider` 节点 id 不命中（模型 id 是 `m_executor_Provider`）。 |
| 图 | `who-calls OneShotArgs` → `Approver.Decide`（基线域标签 `d_coordination_task`）。`who-calls n_hostapi_Host_RunTurn` 只列出 `coordinatorRunner.Resume`，漏 Launch（与契约 §11 同债）。`who-calls BaselineReport` 无生产调用方。`who-calls Install` → `skillInstallCmd.RunE`。 |
| 图 | `flow Approver.Decide` degraded=true steps=[]。`flow Dispatch` degraded=false steps=95。`flow RunTurn` 1 步 return，摘要仍写「骨架期恒返回 ErrUnavailable」，与源码 `runTurn` 不符。`flow Install` degraded=false steps=7。 |
| 基线 | `go test ./internal/executor ./internal/executor/grok ./internal/skill -run 'TestBaselineReportMatrix\|TestHarnessNameLiterals\|TestUnsupportedError\|TestOneShotArgv\|TestOneShotArgsStillEncodesGrokLowUntilMigrated\|TestAgentDirsContract\|TestRecovererIsNotAdapterMethodSet\|TestGuardRemove\|TestZeroProfileReport\|TestCoordTypesMirror\|TestRequireLaunchPrompt' -count=1` → 三包 ok。 |
| 基线 | `go test ./internal/executor -count=1` ok 0.209s。`go test ./internal/agentd -run 'TestCoordinatorHomeSupplyOnLaunchAndResume\|TestOneShot\|TestApproverDecide' -count=1` ok 0.969s。`go test ./cmd -run 'TestAdapterRegistryHasAlwaysAvailableExecutors\|TestAdaptersForRegistersClaudeOnAllPlatforms' -count=1` ok 0.571s。`go build -o /dev/null .` 退出 0。 |
| 源码 | `Approver.Decide` 仍调 `OneShotArgs`；grok 默认 `--effort low`。`coordinatorRunner.Launch` 仍 `RunTurn(context.Background())`。`copyCoordinatorRules` 仍写死 `.config/opencode`。`skill.Install` 仍持 `agentDirs`。`Manager` 仍有私有 `restorer`。`WriteTaskEnv`（claude）无 `PolicySnapshot`。`Dispatch` 只给 OpenCode 注入 Approval。 |
| 源码 | `hostapi` 包注释/driver.go 写明只 import 标准库。Coordinator 实现不得放进 hostapi（会破该约束）。P4(a) 落点选 `internal/executor/opencode/coordinator.go` 包 `hostapi.Host`。 |
| 源码 | `extractNonceForTest` 已从 prompt 文本取 `nonce=`，不是从 flag 解析。T4 把 stub 从 argv 改成 `req.Prompt` 即可复用。 |
| 源码 | `credRelPath` 无 claude/agy。`driveTurn` 用 `exec.Command` + `watchDeadline(ctx)`/`killGroup`，故 Coordinator 把调用方 ctx 传进 `RunTurn` 即可被取消打断。 |
| 判断 | 最薄路径未点亮：BaselineReport 无生产调用方。DAG 第一条 = T1。P1–P6 全选 a，不扇出。 |
| 判断 | hostapi 只 import 标准库 → Coordinator 落 opencode 包。Approver 与 ads 时序：NewApprover 在 ads 之前，BindOneShot 必须在 ads 之后。skill install 是 CLI 进程不是 agentd，Skills 在 cmd 组装。 |
| 产出 | 计划写到 `docs/superpowers/plans/b233.2-plan.md`。自审：六条缝均有入口；无 TBD；P1–P6 全 a。 |
| 提交 | 命令：`git add docs/superpowers/plans/b233.2-plan.md docs/superpowers/ledgers/2026-09-07-b233.2-plan-ledger.md && git commit -m "docs(B233.2): 写 harness 能力归拢实现计划"` |
| 提交原文 | `[cards/B233.2-charter-3 43f19b63] docs(B233.2): 写 harness 能力归拢实现计划` / `2 files changed, 2069 insertions(+)` / `create mode 100644` 两份文档。随后 amend 一次收进本行。 |
