# B358.9 T2/T3 + 上轮 review findings 实现台账

节点：Implement（本回合做三件事：修上轮 review findings、plan §5 的 T2、plan §6 的 T3；T4 留后续）。
计划：`docs/superpowers/plans/b358.9-plan.md` §4（findings）/§5（T2）/§6（T3）。
契约：`docs/superpowers/specs/b358.9-contract.md`。
卡：B358.9　有效基线：`cards/B233.1-charter-7`。
工作分支：`cards/B358.9-charter-8`。角色：实现节点（有 commit 义务）。
一行一条历史读数；命令与原始输出照抄。

## 0. 开工状态位

1. 2026-09-18：`git branch --show-current` → `cards/B358.9-charter-8`；`git status --porcelain` → 无输出。
2. 2026-09-18：`git rev-parse HEAD` → `e3b11c06`（T1 提交）。
3. 2026-09-18：`which codegraph` → `/usr/local/bin/codegraph`（恒在层要求已满足，未自行安装）。

## 1. 基线复核

4. 2026-09-18：`go build ./...` → 退出码 `0`，无输出。
5. 2026-09-18：`gofmt -l cmd/session.go cmd/session_test.go` 在**改动前**（`git stash` 后）即列出两文件名 → `cmd/session.go`、`cmd/session_test.go` 的 gofmt 债为基线既有，非本回合新增。本回合新增/改动的其它文件仍逐一 gofmt 清洁。

## 2. finding 1（major）：join/leave fail-closed 回归锁

6. 2026-09-18：`internal/agentd/identity_face_test.go` 的 `TestConsoleFaceFailClosedWithoutName` probes 追加
   `POST /api/sessions/{id}/cards`（join）与 `DELETE /api/sessions/{id}/cards/{cardID}`（leave）；探针循环由
   `if GET else` 改为 `switch GET/DELETE/default`（此前 DELETE 无法从该表发起）。改动仅限测试锁，生产行为零改（review 已用 overlay 探针证实行为正确）。
7. 2026-09-18：绿跑 `go test ./internal/agentd/ -count=1 -run 'TestConsoleFaceFailClosedWithoutName'` → `ok .../agentd 0.212s`（PASS）。
8. 2026-09-18：**拆门变异（join）**——`git` 外手工把 `handleSessionJoinCard` 的身份门整段删除、`actor` 改 `""`。
   `go build ./...` 退出 `0`（编译通过，先确认）；`go test ... -run TestConsoleFaceFailClosedWithoutName` 原文：

```
--- FAIL: TestConsoleFaceFailClosedWithoutName (0.12s)
    identity_face_test.go:164: POST /api/sessions/session:1/cards 未配名必须 403，实得 404 {"error":"collab: 房间不存在"}
FAIL
```

9. 2026-09-18：**拆门变异（leave）**——把 `handleSessionLeaveCard` 的身份门删除、`actor` 改 `""`。
   `go build ./...` 退出 `0`；靶向跑原文：

```
--- FAIL: TestConsoleFaceFailClosedWithoutName (0.17s)
    identity_face_test.go:164: DELETE /api/sessions/session:1/cards/B1 未配名必须 403，实得 200 {"ok":true}
FAIL
```

   两发均先确认编译通过再读断言红；锁有牙。变异后已从备份还原，`go build ./...` 复跑 `BUILD_OK`。

## 3. finding 4（minor）：非法 console_user 重复 Warn 收敛为一条

10. 2026-09-18：`internal/agentd/identity.go#resolveConsoleIdentity` 非法名分支由 `s.log.Warn` 降为 `s.log.Debug`
    （拒收门 `requireConsoleIdentity` 仍以带 method/path 的单条 Warn 汇报同一次失败）；注释写明理由。
11. 2026-09-18：新增锁 `TestConsoleFaceIllegalNameWarnsOnce`（`identity_face_test.go`）：用既有
    `roomsLogCapture` 不过滤级别捕获，断言非法 console_user 一次请求恰一条 Warn。绿跑 `--- PASS`。
12. 2026-09-18：**变异自证**——把 identity.go 该行改回 `s.log.Warn`。`go build ./...` 退出 `0`；靶向跑原文：

```
--- FAIL: TestConsoleFaceIllegalNameWarnsOnce (0.17s)
    identity_face_test.go:210: 非法 console_user 失败应恰一条 Warn（不重复），实得 2
FAIL
```

   有牙；已还原。

## 4. finding 2（major）：端戳隔离机器守卫（contract §10 / §5 D-25）

13. 2026-09-18：新增 `internal/collab/device_isolation_gate_test.go#TestDeviceStampStaysOutOfJudgementPaths`：
    遍历 `internal/collab` 与 `internal/ledger` 的非测试 `.go`，出现 `.Device`/`Device:`/`Device =`/`Device==`
    任一即红（agentd 设置端戳、proto 定义端戳在扫描面外＝合法载体）。
14. 2026-09-18：绿跑 `go test ./internal/collab/ -count=1 -run TestDeviceStampStaysOutOfJudgementPaths` → `--- PASS`（0.00s）。
15. 2026-09-18：**变异自证**——在 `internal/collab/service.go#sendToSession` 插 `_ = msg.Device`。
    `go build ./...` 退出 `0`；靶向跑原文：

```
--- FAIL: TestDeviceStampStaysOutOfJudgementPaths (0.00s)
    device_isolation_gate_test.go:43: service.go 出现端戳引用 ".Device"——Device 只做落款，不得进入寻址/写者/游标判定面（契约 D-25）
FAIL
```

   有牙；已还原。

## 5. finding 3（minor）：协调者追认，未改

16. 2026-09-18：`internal/agentd/identity.go` 两行注释随换脸更新——协调者已追认属注释一致性维护、不构成行为面扩面，
    本回合不处置（在上轮 T1 已随换脸提交）。

## 6. T2（d_collab）：sessionMembers kind 修正 + 游标/协调者回归

17. 2026-09-18：先写测试（`internal/collab/sessions_test.go` 追加 `TestSessionMemberKindByIdentityPrefix`、
    `TestSessionSeatMemberKindDerivedFromCard`；`internal/collab/service_test.go` 追加
    `TestSessionWritersExcludesEmptySeat`（内部锁，理由见 plan §3）、`TestCursorKeyedByMemberIdentity`）。
18. 2026-09-18：P-3 夹具修正：`sessions_test.go` 四处 `"cli:claude#desktop"` → `"user:claude"`（断言意图不变）。
19. 2026-09-18：红跑 `go test ./internal/collab/ -count=1 -run 'TestSessionMemberKindByIdentityPrefix|...'` 原文：

```
--- PASS: TestSessionWritersExcludesEmptySeat (0.11s)
--- PASS: TestCursorKeyedByMemberIdentity (0.12s)
--- FAIL: TestSessionMemberKindByIdentityPrefix (0.11s)
--- PASS: TestSessionSeatMemberKindDerivedFromCard (0.13s)
FAIL
```
   失败原因（功能缺失，非 typo）：`sessions_test.go:333: agent:<名> 成员 kind 应为 agent，实得 "human"`。
20. 2026-09-18：最小实现 `internal/collab/sessions.go#sessionMembers`：显式成员 kind 改由
    `proto.ParseMemberIdentity` 前缀判定（`agent:`→agent、`user:`→human），`user:` 不再因 `cli:` 分支被误报；
    脏行（旧 web:/cli: 脸）Warn 后按 human 兜底。删除旧 `cli:` 分支与旧注释。
21. 2026-09-18：绿跑 T2 八支（四新 + 既有 `TestSessionMembershipEnforcement`/`TestSessionRebindRevokesOldSeatWriter`/
    `TestWakeTargetsAddressing`/`TestSessionWritersReadFailureNotWriterGuard`）全 `--- PASS`，`ok .../collab 0.881s`。
22. 2026-09-18：**变异自证（kind）**——把 `parsedKind == proto.IdentityKindAgent` 改
    `parsedKind == "web"`。`go build ./...` 退出 `0`；靶向跑原文：
    `--- FAIL: TestSessionMemberKindByIdentityPrefix` / `sessions_test.go:333: agent:<名> 成员 kind 应为 agent，实得 "human"`。有牙。
23. 2026-09-18：**变异自证（空座书写者）**——`sessionWriters` 去掉 `&& c.DriverSession != ""`。编译通过；原文：
    `--- FAIL: TestSessionWritersExcludesEmptySeat` / `service_test.go:480: 空座不得产空串书写者: [user:sy ]`。有牙。
24. 2026-09-18：**变异自证（游标键）**——`MarkRead` 实参改 `member+"-drift"`。编译通过；原文：
    `--- FAIL: TestCursorKeyedByMemberIdentity` / `service_test.go:499: 人名键游标应生效: <nil> 1`。有牙。
    三发均先确认 `go build ./...` 过再读断言红；已还原，`go build ./...` 复跑 `BUILD_OK`。

## 7. T3（d_cli）：校验收敛 + --agent 回归

25. 2026-09-18：先写测试（`cmd/session_test.go` 追加 `TestSessionMemberNotationSingleDefinition`、
    `TestSessionSendAgentEmptyFallsThrough`）。
26. 2026-09-18：红跑 `go test ./cmd/ -count=1 -run 'TestSessionMemberNotationSingleDefinition|TestSessionSendAgentEmptyFallsThrough|TestSessionSendAgentIdentity|TestSessionSendActorResolution'` 原文：

```
--- PASS: TestSessionSendAgentIdentity (0.13s)
    session_test.go:1004: cmd/session.go 仍内联统一记法规则 "CutPrefix(identity, \"user:\")"——应收敛到 proto.ValidateMemberIdentity
--- FAIL: TestSessionMemberNotationSingleDefinition (0.00s)
--- PASS: TestSessionSendAgentEmptyFallsThrough (0.15s)
--- PASS: TestSessionSendActorResolution (0.00s)
FAIL
```
   失败原因为功能缺失（cmd 侧仍内联 CutPrefix）；`TestSessionSendAgentEmptyFallsThrough` 基线已绿（Ticket 0 已落 `--agent`）。
27. 2026-09-18：最小实现 `cmd/session.go#validSessionMemberIdentity` 改 `return proto.ValidateMemberIdentity(identity)`；
    删除两段 `strings.CutPrefix`，注释改为「收敛到 proto 唯一处」。`strings` import 仍被 `TrimSpace`/`Join` 使用，保留。
28. 2026-09-18：绿跑 `go test ./cmd/ -run TestSession -count=1` → `ok .../cmd 5.203s`（全部 23 支 `--- PASS`）。
29. 2026-09-18：**变异自证（收敛守卫）**——把内联 CutPrefix 写回 `validSessionMemberIdentity`。编译通过；原文：
    `--- FAIL: TestSessionMemberNotationSingleDefinition` /
    `session_test.go:1004: cmd/session.go 仍内联统一记法规则 "CutPrefix(identity, \"user:\")"...`。有牙。
30. 2026-09-18：**变异自证（空 --agent）**——`sessionSendActor` 加 `case agentFlag == "": return err`。编译通过；原文：
    `--- FAIL: TestSessionSendAgentEmptyFallsThrough` / `session_test.go:1015: 空 --agent 应等价无 flag: --agent 空串被误判`。有牙。
    已还原，`go build ./...` 复跑 `BUILD_OK`。

## 8. 验收与收口（本轮法定命令）

31. 2026-09-18：`go build ./...` → 退出码 `0`，无输出。
32. 2026-09-18：`go test ./internal/agentd/ -count=1 -timeout 15m > /tmp/agentd_final.log 2>&1; echo EXIT=$?` →
    `EXIT=0`；`grep -cE '^--- FAIL'` → `0`；`ok github.com/Xsxdot/handoff/internal/agentd 163.851s`。
33. 2026-09-18：`go test ./internal/collab/... -count=1` → `ok .../collab 7.097s`、`ok .../collab/room 0.002s`；
    client/cursor 无测试文件；退出码 `0`。
34. 2026-09-18：`go test ./cmd/ -run TestSession -count=1` → `ok .../cmd 5.408s`；退出码 `0`。
35. 2026-09-18：`go vet ./internal/collab/ ./internal/agentd/ ./cmd/` → 退出码 `0`，无输出。
36. 2026-09-18：`gofmt -l` 对本回合新增/改动文件（除 `cmd/session.go`/`cmd/session_test.go` 两处基线既有债）
    零输出；我新加的 `service_test.go` 注释行经 gofmt 修正后清洁。

## 9. 图查询与图覆盖债

37. 2026-09-18：`codegraph resolve --repo . --doc internal/collab/sessions.go` → `{"anchors": []}`。
38. 2026-09-18：`codegraph sym sessionMembers --view cards-B358.9-charter-4` → 命中（d_collab）。
39. 2026-09-18：`codegraph sym validSessionMemberIdentity --view cards-B358.9-charter-4` → 未命中
    （`Error: 符号 "validSessionMemberIdentity" 不在图中`）。本节点**图覆盖债（未命中符号）**：
    `validSessionMemberIdentity`（生产，cmd）。未命中时回落 grep 读源码，未拿 chain 冒充。
    （`sessionMembers` 命中，已在 view 内。）

## 10. 判断与放弃的尝试

40. 2026-09-18：T2 的 `TestSessionSeatMemberKindDerivedFromCard` 基线即绿（席位 kind 现状已正确）——它是条 41 的回归锁，
    非新行为；按 plan 逐条对账保留，不造「先红」假象。
41. 2026-09-18：`findings 1` 的 leave 探针在拆门变异下返回 200（零值 actor 下 `LeaveCard` 幂等成功）——正是「拆门后
    agentd 仍绿」的成因；锁到位后此路径被 403 断言堵住。
42. 2026-09-18：无放弃的尝试。未碰 handoff CLI、未起任何 executor。

## 11. 提交（历史读数）

43. 2026-09-18：本节点产出物（`internal/collab/sessions.go`、`internal/collab/sessions_test.go`、
    `internal/collab/service_test.go`、`internal/collab/device_isolation_gate_test.go`（新建）、
    `cmd/session.go`、`cmd/session_test.go`、`internal/agentd/identity.go`、`internal/agentd/identity_face_test.go`）
    与本台账同批提交。命令与原始输出：

```
$ git add internal/collab/sessions.go internal/collab/sessions_test.go internal/collab/service_test.go internal/collab/device_isolation_gate_test.go cmd/session.go cmd/session_test.go internal/agentd/identity.go internal/agentd/identity_face_test.go docs/superpowers/ledgers/2026-09-18-b358.9-t2t3-implement-ledger.md
$ git commit -m "implement(B358.9/T2T3): collab kind 前缀修正 + CLI 校验收敛 + join/leave fail-closed 锁 + 端戳隔离守卫"
[cards/B358.9-charter-8 b1653aa1] implement(B358.9/T2T3): collab kind 前缀修正 + CLI 校验收敛 + join/leave fail-closed 锁 + 端戳隔离守卫
 9 files changed, 432 insertions(+), 24 deletions(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-18-b358.9-t2t3-implement-ledger.md
 create mode 100644 internal/collab/device_isolation_gate_test.go
```

44. 2026-09-18：提交事实只记当时命令与原始输出（历史读数）；amend 换 hash 属 git 事实，不在本文件追写。
    收口判据是工作树干净，不是文件里的 hash 等于 HEAD。
