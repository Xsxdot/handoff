# B312 契约节点台账

- 2026-09-03：开工核对命令 `git status --short --branch && git log -1 --oneline`；原始输出为 `## cards/B312-charter`、`5f8b71b3 docs(B307): freeze approved bind-current-session spec`；工作树无改动，基线与任务指定一致。
- 2026-09-03：开工核对 `docs/superpowers/specs/2026-09-03-b307-bind-current-session-design.md` 头部；原始状态行为 `- **状态**：已批准（用户 2026-09-03：「同意」；含 r1：工作台一起改，审查 C2–C4 / I1–I6 按拟文）`，无需回写。
- 2026-09-03：按平台不变量尝试 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --help`；失败原文：`go: downloading github.com/Xsxdot/charter/graph v0.10.0`、`go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.mod969208608.tmp: read-only file system`、`open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.lock: read-only file system`。
- 2026-09-03：已改用本机已有 codegraph 二进制；其 `--help` 输出确认可用子命令包含 `check`、`contract`、`resolve`、`sym`、`validate`、`views`。
- 2026-09-03：本轮实跑本机 `codegraph --repo . check | jq -c '{fails,legacyHits}'`；原始输出为 `{"fails":[],"legacyHits":{"d_cli->d_execution":11,"d_cli->d_gateway":1,"d_cli->d_ledger":4,"d_cli->d_maintenance":18,"d_cli->d_orchestration":13,"d_cli->d_policy":32,"d_cli->d_protocol":1,"d_cli->d_sessions":2,"d_cli->d_transport":10,"d_cli->d_workspace":9,"d_gateway->d_execution":1,"d_gateway->d_ledger":1,"d_gateway->d_maintenance":7,"d_gateway->d_orchestration":210,"d_gateway->d_policy":23,"d_gateway->d_sessions":12,"d_gateway->d_transport":1,"d_gateway->d_workspace":1,"d_orchestration->d_execution":26,"d_orchestration->d_gateway":1,"d_orchestration->d_maintenance":5,"d_orchestration->d_policy":9,"d_orchestration->d_protocol":5,"d_orchestration->d_sessions":1,"d_orchestration->d_transport":2,"d_orchestration->d_workspace":18,"d_workspace->d_orchestration":2,"d_workspace->d_protocol":3,"d_maintenance->d_protocol":1,"d_maintenance->d_transport":3,"d_policy->d_maintenance":2,"d_policy->d_transport":2,"d_sessions->d_execution":6,"d_transport->d_policy":4}}`。
- 2026-09-03：上游 `docs/superpowers/specs/2026-09-03-b307-bind-current-session-design.md` 头部状态为 `已批准（用户 2026-09-03：「同意」；含 r1：工作台一起改，审查 C2–C4 / I1–I6 按拟文）`，基线为 `acc/b156.2-156.3`；本节点未读取或引用 B307 muse-spark 产出。
- 2026-09-03：现状签名已逐一核对：`internal/ledger/binding.go#Store.ClaimCardAs`=`func (s *Store) ClaimCardAs(id, owner, carrier string) error`、`#Store.RebindDriver`=`func (s *Store) RebindDriver(id, toSession, carrier, expect string) error`；`internal/ledger/move.go#Store.ClaimCard`、`#Store.ReleaseCard`；`internal/ledger/tasks.go#Store.TakeoverCard`=`func (s *Store) TakeoverCard(id, session, actor string) error`；`internal/ledger/api/api.go#Facade.BindDriver`=`func (f *Facade) BindDriver(id, session, carrier, expect string) error`。
- 2026-09-03：现状卡座位存储为 `cards.driver_session`，PG DDL 在 `internal/ledger/store.go:205-212`、SQLite DDL 在 `:284-291`；现状读列 `internal/ledger/cards.go#cardColumns` 只含 `driver_session, driver_heartbeat_at`，`scanCard` 在 `:355-370` 读取这两列。`ensureSchema` 另以 `ALTER TABLE` 加 `driver_carrier`（`:374-390`），但当前 `Card` 未声明 `DriverCarrier`，故其对外投影不是事实源。
- 2026-09-03：当前 CLI `cmd/ledgercli.go#ledgerActor`（实际行 `:44-50`）返回 `cli:<USER>@<hostname>`；本工作树生产源码没有 `ledgerSession` 符号。`cmd/card_coordinate.go#coordinateAfterCreate` 当前调用 `CoordinatorLaunchAs(..., "card_create")`；`internal/agentd/coordapi.go#Server.handleCoordLaunch` 当前只接受 `manual|card_create`、默认 `manual`；`web/src/api/scheduling.ts#launchCoordinator` 当前类型为 `'manual' | 'card_create'`。
- 2026-09-03：Keystone 现状签名为 `internal/keystone/keystone.go#Service.Wake(ctx context.Context, card string, evs []WakeEvent, spec keysclient.SessionSpec) (RoundResult, error)`、`#Service.LaunchForCard(ctx context.Context, card, source string, spec keysclient.SessionSpec) (RoundResult, error)`、`#Service.Locate(card, workdir string) (keysclient.AttachInfo, error)`；`Wake` 在 `sessions[card]` 缺失时现走 `launchRound`（`:100-105`），`Locate` 只查内存 `sessions`（`:159-167`）。
- 2026-09-03：当前协作写权 `internal/collab/room/room.go#VerifyWriter` 比较 `proto.Card.DriverSession`，`internal/collab/service.go#Service.Pending` 以 `ListAllCards` 的 `DriverSession == consumer` 建绑定集合；`#Service.Consume` 签名为 `func (s *Service) Consume(seq int64, consumer string) error`。当前 CLI `card dispatch --step` 与节点调用方仍从 `ledgerActor()` 填 `proto.CardStepReq.Actor`（`cmd/card_dispatch.go`、`cmd/card_node.go`）。
- 2026-09-03：当前 HTTP 组装点 `internal/agentd/scheddrain.go#Server.launchCoordinatorRound` 构造 `keysclient.SessionSpec` 后调用 `s.keystone.LaunchForCard(ctx, card, source, spec)`（`:212-246`），唤醒路径 `#Server.wakeCoordinatorRound` 调 `s.keystone.Wake(ctx, card, evs, spec)`（`:249-290`）；`internal/agentd/server.go#coordinatorRunner.Resume` 通过 `resumeTurnRequest` 将 `SessionRef` 的 CLI/session/HOME/workdir/model 传入 `hostapi.RunTurn`。
- 2026-09-03：判断：B312 采用现有 `driver_session` 字段承载规范化席位身份字符串，并新增 `driver_source` 列/JSON 字段；不另造第二个席位真相表。身份编码、当前会话环境变量、按钮/HTTP/唤醒/房间/派发的接缝语义在法定契约文档中原子化冻结。
- 2026-09-03：按骨架先行落 `internal/proto/seat.go` 与 `internal/proto/seat_test.go`：身份编码固定为 `cli:<cli>#<session_id>`，并拒绝 `cli:user@host` 等旧/歧义值。运行 `gofmt -w internal/proto/seat.go internal/proto/seat_test.go` 无输出；运行 `go test ./internal/proto/... -run 'TestSeatIdentity' -count=1`，原始输出为 `ok github.com/Xsxdot/handoff/internal/proto 0.002s`，退出码 0。
- 2026-09-03：首次运行 `codegraph --repo . check --view cards-B312-charter` 失败原文：`Error: 视图 cards-B312-charter 引用不完整: [diff 节点 m_proto_SeatSource 的 modelKind "value" 不在枚举内（entity/dto/config）]`；将视图节点的 `modelKind` 修正为 `entity` 后重跑成功。
- 2026-09-03：运行 `jq empty codegraph/target.json && jq empty codegraph/diffs/cards-B312-charter.json && codegraph --repo . check --view cards-B312-charter | jq -c '{fails,legacyHits}'`；原始输出为 `{"fails":[],"legacyHits":{"d_cli->d_execution":11,"d_cli->d_gateway":1,"d_cli->d_ledger":4,"d_cli->d_maintenance":18,"d_cli->d_orchestration":13,"d_cli->d_policy":32,"d_cli->d_protocol":1,"d_cli->d_sessions":2,"d_cli->d_transport":10,"d_cli->d_workspace":9,"d_gateway->d_execution":1,"d_gateway->d_ledger":1,"d_gateway->d_maintenance":7,"d_gateway->d_orchestration":210,"d_gateway->d_policy":23,"d_gateway->d_sessions":12,"d_gateway->d_transport":1,"d_gateway->d_workspace":1,"d_orchestration->d_execution":26,"d_orchestration->d_gateway":1,"d_orchestration->d_maintenance":5,"d_orchestration->d_policy":9,"d_orchestration->d_protocol":5,"d_orchestration->d_sessions":1,"d_orchestration->d_transport":2,"d_orchestration->d_workspace":18,"d_workspace->d_orchestration":2,"d_workspace->d_protocol":3,"d_maintenance->d_protocol":1,"d_maintenance->d_transport":3,"d_policy->d_maintenance":2,"d_policy->d_transport":2,"d_sessions->d_execution":6,"d_transport->d_policy":4}}`。
- 2026-09-03：运行 `codegraph --repo . resolve --doc docs/superpowers/specs/b312-contract.md`，退出码 0；原始输出末尾为 `"ref": "internal/client/client.go#Client.do" ... "anchor": "ok"`，未输出 Error，文档现状锚均可解析或明确使用 file:line 覆盖图缺口。
- 2026-09-03：首次 `codegraph --repo . resolve --doc docs/superpowers/specs/b312-contract.md` 失败原文：`Error: 文档锚点检查失败: internal/agentd/scheddrain.go#Server.launchCoordinatorRound (vanished)`；又发现 `internal/agentd/coordapi.go#Server.handleCoordLaunch`、`internal/ledger/api/api.go#Facade.BindDriver` 同属图未提供的符号锚，改为带现状行号的 file:line 引用后重跑通过。
- 2026-09-03：运行 `codegraph --repo . validate --view cards-B312-charter`；原始输出为 `{"containers":279,"domainDecls":23,"domains":23,"edgeIssues":null,"edges":4965,"entitiesWithoutLifecycle":1,"issues":null,"nodes":4007,"unscannedEntries":6,"views":["cards-B312-charter"]}`，退出码 0。
- 2026-09-03：第一次 `go test ./... -count=1` 失败原文：`go: downloading github.com/Xsxdot/charter/graph v0.10.0`、`go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.mod990967495.tmp: read-only file system`、`open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.lock: read-only file system`；同一命令同时打印了若干独立包 `ok`，但整体因根包/cmd/agentd/client setup failed，不据此宣称全仓通过。
- 2026-09-03：改用任务可写缓存 `GOMODCACHE=/root/.handoff/tmp/0d744229/gomodcache GOCACHE=/root/.handoff/tmp/0d744229/gocache go test ./... -count=1`，最终轮询到退出码 0。原始通过行包括：`ok github.com/Xsxdot/handoff 0.355s`、`ok github.com/Xsxdot/handoff/cmd 16.894s`、`ok github.com/Xsxdot/handoff/internal/agentd 247.105s`、`ok github.com/Xsxdot/handoff/internal/client 9.566s`、`ok github.com/Xsxdot/handoff/internal/collab 5.257s`、`ok github.com/Xsxdot/handoff/internal/keystone 0.138s`、`ok github.com/Xsxdot/handoff/internal/ledger 21.997s`、`ok github.com/Xsxdot/handoff/internal/ledger/api 0.741s`、`ok github.com/Xsxdot/handoff/internal/proto 0.006s`、`ok github.com/Xsxdot/handoff/internal/scheduling 2.628s`、`ok github.com/Xsxdot/handoff/internal/targetclient 5.162s`，以及其它已输出的 `ok` 包；`? ... [no test files]` 仅出现在无测试包。全仓退出码为 0。
- 2026-09-03：运行 `gofmt -d internal/proto/seat.go internal/proto/seat_test.go`、`jq empty codegraph/target.json`、`jq empty codegraph/diffs/cards-B312-charter.json`、`git diff --check` 均无输出且退出码 0；随后 `git status --short` 原始输出为 ` M codegraph/target.json`、`?? codegraph/diffs/`、`?? docs/superpowers/ledgers/2026-09-03-b312-contract-ledger.md`、`?? docs/superpowers/specs/b312-contract.md`、`?? internal/proto/seat.go`、`?? internal/proto/seat_test.go`。
- 2026-09-03：最终文档锚自检再次运行 `codegraph --repo . resolve --doc docs/superpowers/specs/b312-contract.md`，退出码 0；原始输出列出 24 个现状锚，末尾为 `internal/client/client.go#Client.do ... "anchor": "ok"`，无 Error。
- 2026-09-03：最终视图闸门再次运行 `codegraph --repo . check --view cards-B312-charter | jq -c '{fails,legacyHits}'`，原始输出仍为 `{"fails":[],"legacyHits":{"d_cli->d_execution":11,"d_cli->d_gateway":1,"d_cli->d_ledger":4,"d_cli->d_maintenance":18,"d_cli->d_orchestration":13,"d_cli->d_policy":32,"d_cli->d_protocol":1,"d_cli->d_sessions":2,"d_cli->d_transport":10,"d_cli->d_workspace":9,"d_gateway->d_execution":1,"d_gateway->d_ledger":1,"d_gateway->d_maintenance":7,"d_gateway->d_orchestration":210,"d_gateway->d_policy":23,"d_gateway->d_sessions":12,"d_gateway->d_transport":1,"d_gateway->d_workspace":1,"d_orchestration->d_execution":26,"d_orchestration->d_gateway":1,"d_orchestration->d_maintenance":5,"d_orchestration->d_policy":9,"d_orchestration->d_protocol":5,"d_orchestration->d_sessions":1,"d_orchestration->d_transport":2,"d_orchestration->d_workspace":18,"d_workspace->d_orchestration":2,"d_workspace->d_protocol":3,"d_maintenance->d_protocol":1,"d_maintenance->d_transport":3,"d_policy->d_maintenance":2,"d_policy->d_transport":2,"d_sessions->d_execution":6,"d_transport->d_policy":4}}`。
- 2026-09-03：首次 `git diff --cached --check` 失败原文指出契约头部 4 行有 Markdown 强制换行用的尾随双空格；已去除尾随空格并重新 `git add docs/superpowers/specs/b312-contract.md`。
- 2026-09-03：再次运行 `git diff --cached --check && git diff --cached --stat`，无 check 输出；stat 原始输出为 `codegraph/diffs/cards-B312-charter.json | 53 +++`、`codegraph/target.json | 14 +-`、`docs/superpowers/ledgers/2026-09-03-b312-contract-ledger.md | 26 ++`、`docs/superpowers/specs/b312-contract.md | 394 +++++++++++++++++++++`、`internal/proto/seat.go | 63 +++`、`internal/proto/seat_test.go | 35 ++`，合计 `6 files changed, 578 insertions(+), 7 deletions(-)`。
- 2026-09-03：提交命令 `git commit -m "docs(B312): freeze current-session seat contract"`；原始输出为 `[cards/B312-charter ef29b32f] docs(B312): freeze current-session seat contract`、`6 files changed, 580 insertions(+), 7 deletions(-)`，并创建本节点法定文档、台账、目标图视图 diff 和协议骨架文件。随后按台账纪律只 amend 一次收录本行，故最终 HEAD hash 将变化且不回写追逐。
- 2026-09-03：按平台不变量以任务可写缓存运行 `GOMODCACHE=/root/.handoff/tmp/01535cc5/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . summary`；原始输出为 `本仓库有代码图：4007 节点 / 4965 边 / 23 领域 (codegraph/)`。
- 2026-09-03：按 `codegraph/best.json` 的 `.domains | to_entries[] | select(.value.parent == null)` 实跑顶层领域核对；原始类型序列为 `d_orchestration logic`、`d_gateway boundary`、`d_workspace boundary`、`d_execution boundary`、`d_sessions boundary`、`d_transport boundary`、`d_protocol logic`、`d_ledger logic`、`d_collab logic`、`d_cli logic`、`d_web logic`、`d_policy logic`、`d_maintenance boundary`、`d_scheduling logic`、`d_keystone logic`。本次方案实际触及 `d_transport/d_gateway/d_protocol/d_ledger/d_collab/d_cli/d_web/d_keystone`；`d_scheduling` 仅被控制面消费，不改其文件。
- 2026-09-03：实读命令行、控制面、Keystone、协作房间与 Web 入口；确认 `cmd/room.go` 的 `roomSendCmd`、`cmd/card_dispatch.go` 的 `runStepDispatch`、`cmd/card_node.go` 的节点提交仍以 `ledgerActor()` 作 actor；`internal/agentd/ledgerapi.go` 仍注册旧 `POST /api/cards/{id}/rebind`，`internal/agentd/roomsapi.go#Server.handleCardRebind` 仍消费旧 `{to_session,carrier,expect}`；Web 当前仍有 `NewCardDialog` 的 coordinate checkbox、`CoordinatorPanel` 的 manual launch、`web/src/api/scheduling.ts` 的 `manual|card_create` source 类型。
- 2026-09-03：实读现有投影与测试入口；确认 `internal/proto/ledger.go#Card`、`internal/ledger/api/api.go#cardWire`、`internal/agentd/ledgerapi.go#ledgerCardWire` 和 `web/src/api/ledger.ts#Card` 是 driver 字段手写序列化/投影边界；现有回归入口为 `internal/ledger/binding_test.go`、`internal/ledger/api/api_test.go`、`internal/agentd/coordapi_test.go`、`internal/agentd/roomsapi_test.go`、`cmd/card_driver_test.go`、`cmd/card_coordinate_test.go`、`cmd/card_dispatch_test.go`、`web/src/api/scheduling.fetch.test.ts`、`web/src/app/cards/{NewCardDialog,CoordinatorPanel,CardDrawer}.test.tsx`。
- 2026-09-03：判断：B312 仍按 B307 §4 的 L3 约束只提出一张跨域实现子卡，域内工作以其内部票据 DAG 组织；不按领域拆成多个子卡，以保留 Launch→CAS、Wake、房间和 Web 的接缝闭环。所有生产入口与测试入口列入该卡的显式有界文件集；未列入的调度域只作消费依赖。
- 2026-09-03：追加核对发现 contract §6.2 要求“会话列表”展示 `driver_session/driver_source`，但现状 `proto.CardView`、`ledgerCardViewWire`、`web/src/api/ledger.ts#CardView` 均没有这两个字段；列表 `/api/cards` 消费的是 `CardView` 而非详情 `Card`。这是新增 wire 投影接缝，不能在 breakdown 中自行补加，列为退回 contract 的待拍板项。
- 2026-09-03：实读 `web/package.json`；原始 scripts 为 `"test": "vitest run"`、`"typecheck": "tsc -b"`、`"build": "tsc -b && vite build"`，故 breakdown 的 Web 验收使用 `npm test`/`npm run typecheck`，未据此宣称运行结果。
- 2026-09-03：运行 `git diff --check && GOMODCACHE=/root/.handoff/tmp/01535cc5/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b312-breakdown.md`；原始输出为 5 个现状锚，其中 `internal/agentd/ledgerapi.go#ledgerCardViewWire`、`internal/ledger/api/api.go#cardWire`、`internal/agentd/ledgerapi.go#ledgerCardWire` 标记 `moved`，`web/src/api/ledger.ts#CardView` 与 `internal/proto/ledger.go#Card` 标记 `ok`，无 Error，命令退出码 0。
- 2026-09-03：提交命令 `git commit -m "docs(B312): add breakdown proposal"`；原始输出为 `[cards/B312-charter-2 b723f926] docs(B312): add breakdown proposal`、`2 files changed, 261 insertions(+)`，创建 `docs/superpowers/specs/b312-breakdown.md`。随后按台账纪律只 amend 一次收录本行，故最终 HEAD hash 将变化且不回写追逐。
