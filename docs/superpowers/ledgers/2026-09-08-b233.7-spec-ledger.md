# B233.7 spec 台账

日期：2026-09-08  
卡：B233.7（B350 并入）  
基线：`cards/B233.1-charter-7` @ `497a7712`

## 拍板

| 时点 | 决定 |
|---|---|
| 2026-09-08 | 原则：干净重构，不堆债。R 编号是审查痕迹，不是用户作业。 |
| 2026-09-08 | R6：接上任务附加纪律（派发真带、执行真读、检查验内容），不拆半截管子。 |
| 2026-09-08 | R8：不做「网络超时后再派仍同一任务」。失败先查状态，再自行决定是否重派。说明书改真话；handoff skill 补操作纪律。 |
| 2026-09-08 | R7：跨机真机要做（此前做过一轮）。不在本卡做。全部 B233 收尾卡完成后再统一真机验证。 |
| 2026-09-08 | R5 已核销（CancelTurn 按 SessionID）。R1/R2/R4/R9/B350 按不堆债直接收进本卡。R3 行为缺口本卡收，迁包归 B233.8。 |
| 2026-09-08 | spec 批准：用户原话「同意。开始吧。」后续派发节点走小队，不点名机器/执行器。 |

## 读数

- 功能线 tip `497a7712`。Ask 已答重发另开 uuid 且 Request 不落 QuestionID（`handleQuestion` :2948–2962）。权限 `RespondPermission` 失败只日志（`authorizeNativePermission` :816–818）。`handleTaskDiff` 丢 ResultRef（:1901–1909）。`bindingFor` 忽略 `req.Executor`。竖切仍要 claude 覆盖。
- `codegraph` 本会话无可用输出，符号从源码核对。

## 放弃的尝试

- 核销三选一口径（全修 / 分类表 / 只修挡合 main）：被用户「不堆债」替代；再拆成 R6/R8/R7 三个产品取舍。
- 把 R8 做成服务端去重：用户明确先不做。
