# B233.7 spec 台账

日期：2026-09-08  
卡：B233.7（B350 并入）  
基线：`cards/B233.1-charter-7` @ `497a7712`

## 拍板

| 时点 | 决定 |
|---|---|
| 2026-09-08 | 原则：干净重构，不堆债。R 编号是审查痕迹，不是用户作业。 |
| 2026-09-08 | R6：接上任务附加纪律（派发真带、执行真读、检查验内容），不拆半截管子。 |
| 2026-09-08 | R8：不做「网络超时后再派仍同一任务」。失败先查状态，再自行决定是否重派。说明书改真话；handoff skill 补操作纪律。 |
| 2026-09-08 | R7：跨机真机要做（此前做过一轮）。不在本卡做。全部 B233 收尾卡完成后再统一真机验证。 |
| 2026-09-08 | R5 已核销（CancelTurn 按 SessionID）。R1/R2/R4/R9/B350 按不堆债直接收进本卡。R3 行为缺口本卡收，迁包归 B233.8。 |
| 2026-09-08 | spec 批准：用户原话「同意。开始吧。」后续派发节点走小队，不点名机器/执行器。 |

## 读数

- 功能线 tip `497a7712`。Ask 已答重发另开 uuid 且 Request 不落 QuestionID（`handleQuestion` :2948–2962）。权限 `RespondPermission` 失败只日志（`authorizeNativePermission` :816–818）。`handleTaskDiff` 丢 ResultRef（:1901–1909）。`bindingFor` 忽略 `req.Executor`。竖切仍要 claude 覆盖。
- `codegraph` 本会话无可用输出，符号从源码核对。
- 2026-09-08 实跑 `rg -n "type taskApprovalClient|func \\(.*taskApprovalClient|DisciplineText|ProfileReq\\{|bindingFor|delivery_failed|EventTypeDeliveryFailed|isDeliverable|TaskOverlay|assembleResultRef|handleTaskDiff" internal docs/superpowers/specs/b233.7.md codegraph/target.json`：确认 `taskApprovalClient` 在 `internal/agentd/approval_client.go`，`ProfileReq.TaskOverlay` 在 `internal/executor/profile.go`，`NoteDeliveryFailed`/`EventTypeDeliveryFailed`/`handleTaskDiff` 等现状符号均存在。
- 2026-09-08 实跑 `nl -ba internal/agentd/manager.go | sed -n '2895,3030p;3265,3295p'`：`handleQuestion` 已答重问分支把 ticket 改为 uuid；`waitQuestion` OpenCode 路径把命名 ticket 的 native id 传给 `RespondAsk`，失败调用 `NoteDeliveryFailed`；`NoteDeliveryFailed` 发布 `delivery_failed`。
- 2026-09-08 实跑 `nl -ba internal/executor/profile.go | sed -n '22,61p'; rg -n "ProfileReq" internal/agentd internal/executor --glob '*.go'`：生产 `ProfileReq` 调用仅见 `coordinator_home.go:109`，该调用只填 Rules/Skills，不填 TaskOverlay；五家 Profile 均已有 Prepare/Verify 方法。
- 2026-09-08 实跑 `nl -ba internal/agentd/server.go | sed -n '1862,1924p'`：`handleTaskDiff` 调用 `assembleResultRef` 后丢弃返回值，随后把原 `headRev` 传给 `Workspace().DiffRange`。
- 2026-09-08 实跑 `nl -ba internal/scheduling/scheduling.go | sed -n '866,878p'; nl -ba internal/keystone/slice_test.go | sed -n '149,158p'`：`bindingFor` 使用 `IdentityOf(c)` 的 CLI，竖切仍把请求 `Executor: "claude"` 和期望绑定 executor 写成 `claude`。
- 2026-09-08 实跑 `nl -ba internal/client/delivery.go | sed -n '9,34p'; nl -ba internal/proto/proto.go | sed -n '61,66p'`：`WaitDeliveryPolicy` 默认交付除显式审计类外的事件，`delivery_failed` 未列入排除项；proto 常量实际被客户端策略消费。
- 2026-09-08 实跑 `nl -ba internal/agentd/approval_client.go | sed -n '23,113p'`：`taskApprovalClient` 实现 `ApprovalClient`；Request 形成 `ApprovalRef`/Decision，Acknowledge 的 formed/delivered/executed 语义分别为无动作/标记送达/无动作。
- 2026-09-08 实跑 `nl -ba internal/agentd/workspace_capability.go | sed -n '102,128p'; nl -ba internal/workspace/identity.go | sed -n '36,46p'`：`assembleResultRef` 以 `rev-parse` 得准确 commit，失败留空；`NewResultRef` 强制非空 commit 为 40 位小写 hex。
- 2026-09-08 实跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --help`：退出码 0，列出 `check`、`resolve`、`validate`、`sym` 等子命令。
- 2026-09-08 实跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . domains`：退出码 0；当前图含任务编排/控制入口/任务生命周期、执行器适配、项目工作区等域。
- 2026-09-08 实跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . views`：退出码 0；已有视图为 `cards-B233.1-charter-2`、`cards-B233.2-charter`、`cards-B233.3-charter`、`cards-B233.4-charter`、`cards-B233.5-charter`、`cards-B233.6-charter`，本卡无新增符号，暂不建空视图。
- 2026-09-08 实跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym internal/agentd/manager.go#Manager.Dispatch`：原始失败为 `Error: 符号 "internal/agentd/manager.go#Manager.Dispatch" 不在图中（图未覆盖或名字有误）；近似候选: []`，随后 `exit status 1`；按规则保留源码锚并记图覆盖债。
- 2026-09-08 实跑 `go env GOROOT` 得 `/usr/local/go`；实跑 `nl -ba /usr/local/go/src/net/http/client.go | sed -n '90,110p;197,209p'`：`http.Client.Timeout` 覆盖连接、重定向及响应体读取，0 表示无超时，deadline 由 `Timeout>0` 计算；`nl -ba /usr/local/go/src/net/http/request.go | sed -n '866,882p'`：Request context 控制连接、发送及响应读取全生命周期；`nl -ba /usr/local/go/src/os/file.go | sed -n '927,941p'`：`os.WriteFile` 会先截断再写入，途中失败可能留下部分文件。
- 2026-09-08 实跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b233.7-contract.md`：退出码 0，全部文档锚点通过；图对缺少索引的局部源码符号改用文件+行号事实引用。
- 2026-09-08 实跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate`：退出码 0；输出 `issues: null`、`edgeIssues: null`，现有视图完整性校验通过。
- 2026-09-08 实跑 `git diff --cached --check`：退出码 0；契约文档、target 与台账无尾随空格错误。随后实跑 `python3 -m json.tool codegraph/target.json`：退出码 0，原始输出 `target.json valid JSON`。
- 2026-09-08 再跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b233.7-contract.md`：退出码 0，输出 23 个锚点结果，均解析为 `ok` 或 `moved`；再跑 `validate`：退出码 0，原始关键输出为 `issues: null`、`edgeIssues: null`。
- 2026-09-08 实跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check`：退出码 1；原始失败为既有 `d_execution→d_orchestration` dead-contract（`ApprovalClient` 无活跃边且接口不在图域），另有既有 legacy/warn 输出；本卡未新增该方向，仅在 target 既有边上追加 B233.7 说明。

## 放弃的尝试

- 核销三选一口径（全修 / 分类表 / 只修挡合 main）：被用户「不堆债」替代；再拆成 R6/R8/R7 三个产品取舍。
- 把 R8 做成服务端去重：用户明确先不做。
- 2026-09-08 实跑 `go build ./...`：退出码 0。
- 2026-09-08 实跑 `go test ./...`：输出 `FAIL\tgithub.com/Xsxdot/handoff/cmd\t19.385s` 后本轮测试进程未及时结束，发送 Ctrl-C，最终退出码 130；随后实跑 `go test ./cmd -count=1`：退出码 1，原始失败为 `--- FAIL: TestRepoContractGate`、`契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边`、`契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在`。
- 2026-09-08 实跑 `go test ./internal/agentd -run 'TestQuestionReissueAfterAnswerCreatesNewTicket|TestOpenCodeWaitQuestionUsesRespondAsk|TestTaskDiffTargetFallsBackToRepoWhenWorktreeGone|TestAssembleResultRefEmptyCommitAllowed' -count=1`：退出码 0。
- 2026-09-08 实跑 `go test ./internal/executor/... ./internal/hostapi ./internal/scheduling ./internal/workspace ./internal/client -count=1`：退出码 0；executor、opencode、hostapi、scheduling、workspace、client 包均通过。
- 2026-09-08 放弃尝试：包含 `internal/agentd` 的相关包组合测试长时间无新输出，本轮发送 Ctrl-C，退出码 130；保留上面的定向 agentd 与其余相关包结果，不把组合命令写成通过。
- 2026-09-08 实跑 `git commit -m "contract(B233.7): 冻结质量债核销契约"`：原始输出 `[cards/B233.7-charter 9cf19905] contract(B233.7): 冻结质量债核销契约`、`3 files changed, 252 insertions(+), 3 deletions(-)`、`create mode 100644 docs/superpowers/specs/b233.7-contract.md`；随后按纪律仅 amend 一次收进本批台账，amend 后 hash 以 git 为准，不回写追逐。
