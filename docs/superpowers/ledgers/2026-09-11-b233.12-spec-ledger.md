# B233.12 spec 台账

- 2026-09-11 开卡：`handoff card split B233` ×6 得 B233.12–.17。阻塞边：12→13；12–16→17。
- 基线：功能线 worktree `/Users/sycm/.handoff/worktrees/b233.1-fl` @ `4485a4e7e8b7c9ec687e11a36a84f9653f69bc06`。
- 读数：`authorizeNativePermission` :828–857 先 `Authorize` 再 `RespondPermission`；`Client.Request` :127 调 `Hooks.AutoAllow`；`bindApproval` :50 把 `autoAllowPermission` 配进去（该函数含 `RespondPermission`）；`consult` :373 `MarkTicketDelivered`；`TestClientConsultAllowPersistsGrant` :424 断言 `DeliveredAt != nil`。
- `codegraph sym ApprovalClient` 命中 `internal/executor/approval.go:87`。`codegraph who-calls autoAllowPermission` 空；生产调用方以源码为准。
- B233.7-contract 表：`delivered` 只能在真实 `RespondPermission` 成功后发出。本卡落实，不改字面值。
- 定级 L3 轻档：跨审批 / 执行 / 编排，单条投递缝，不迁 Manager、不迁五家权威。
- 弃选：只留钩子投递；本卡拆光 Hooks；五家权威迁移；Continue 热加载。
- 普通模型卡（.15/.16）不在本节点指定执行器。
- 独立审查（子代理，只读）：有条件过。裁决表/findings 落卡 seq 12292、12293。Important-1 测试须穿 `bindApproval` 生产 AutoAllow；Important-2 免审无工单不得写进 `UndeliveredAnswers`。已回写 spec 正文「修订」段。未二次派审。
- 提交（历史读数）：`git commit` → `994d0476 docs(B233.12): spec 审批入站收口（投递与送达确认）`；随后 amend 一次收台账行。
