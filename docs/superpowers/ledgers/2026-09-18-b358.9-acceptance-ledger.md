# B358.9 acceptance 台账（会话身份本质重做：一名多端、人名收口、端只落款）

节点：acceptance（协调者侧收口；本仓无 charter-acceptance 纪律块）。
卡：B358.9　上游：spec（已批准）/contract（冻结）/breakdown（已拍板）/plan（T1–T4）。
待验收分支头：`cards/B358.9-review-3` @ `f7e7b6ba`（T4 implement）——链上含 T1 `e3b11c0` / T2T3 `ab16112` / plan `7105044`。
工作树：`/Users/sycm/.handoff/worktrees/b3589-acc`；基线对照树：`/tmp/b3589-base`（@ `94246fc8`）。
纪律：复跑 → 变异复验 → 真机 → 才归档。命令与原始输出照抄。

---

## 1. 复跑（本轮新鲜证据，协调者亲跑）

1. 2026-09-18：`go build ./...`（工作树 b3589-acc）→ 退出 0。
2. `go test ./... -count=1` 全仓：
   - 唯一 FAIL = `cmd`（`--- FAIL: TestRepoContractGate`，`graph_gate_test.go:38` 报 `dead-contract d_execution→d_orchestration` + `dead-interface "ApprovalClient"`）。
   - **基线同红**：在 `/tmp/b3589-base`（`94246fc8` = 本卡基线 `cards/B233.1-charter-7` 头）跑 `go test ./cmd/ -run TestRepoContractGate -count=1` → 同样 FAIL、同样两条违规。判定：B370 遗留，不是本卡回归。
   - `internal/hostapi` 在全量并行那轮 FAIL 过一次；单跑 `go test ./internal/hostapi/ -count=1` → `ok 2.398s`。判定：环境抖动（并发负载），非本卡。
   - 本卡触及包单跑全绿：`internal/agentd` / `internal/collab/...` / `cmd`（除上述既存红）。
3. web 侧（工作树 `web/`，`npm ci` 全装后）：
   - `npx tsc -b` → 退出 0。
   - `npx vitest run` → **Test Files 131 passed (131)、Tests 1428 passed (1428)**，Duration 26.28s。

## 2. 变异复验（协调者独立变异，非执行者自报）

每发：唯一命中 → 编译/类型检查过 → **缝级测试**红 → 还原回绿。

| 编号 | 变异点（承重行为） | 结果 |
| --- | --- | --- |
| M1 | `internal/agentd/identity.go` 缺名分支改回旧脸回落（`Member:"web:"+r.RemoteAddr, Configured:true`） | 编译过；`TestConsoleFaceFailClosedWithoutName` 红（`identity_face_test.go:168` 拒收文案应含 console_user，实得「书写者与房间身份不符」）；还原 → `ok` |
| M2 | `web/src/app/rooms/sessionModel.ts` `isSelfActor` 改回 `actor.startsWith('web:')` | 3 支缝级测试红（sessionModel 2 支含反例「web: 永不判自方」+ SessionChat 1 支）；还原 → 21 passed |
| M3 | `internal/agentd/roomsapi.go` 端戳不装填（`Device: id.Device` → `""`） | 编译过；`TestConsoleFaceActorAndDeviceStamp` 红（`identity_face_test.go:124` 端戳应为登录会话设备名，实得 ""）；还原 → `ok` |
| M4 | `internal/collab/sessions.go` kind 恒判 human（掏空 agent 分支） | 编译过；`TestSessionMemberKindByIdentityPrefix` 红（`sessions_test.go:333` agent:<名> 应 agent，实得 human）；还原 → `ok` |

观察（非阻塞，已记残余）：`TestConsoleFaceNoWebFallbackSourceGuard` 只扫 `roomsapi.go`/`sessionsapi.go`，不含 `identity.go`——M1 的旧脸字面量写进 `identity.go` 时守卫不红（行为锁兜住了）。

## 3. 真机验收（行为事实，独立实例实测）

环境：本分支自建二进制的独立实例——`listen 127.0.0.1:7799`、`datadir /tmp/b3589/data`、`console_user: sycm`（不碰在跑的正式 agentd）。

1. **端戳来源（plan §10.3）**：
   - `handoff console --print-url --device mac-air` 兑换后，`GET /api/auth/sessions` → `device_name: "mac-air"`。
   - `GET /api/identity` → `{"member":"user:sycm","device":"mac-air","configured":true}`。
   - 消息面：控制台发话 payload `{"kind":"user","body":"…","mentions":["agent:probe"],"device":"mac-air"}`；CLI 主 agent 发话 payload **无 device 键**（零值省键）。端戳只落款、不进权力面。
2. **未配名 fail-closed（P-1/P-2）**：去掉 `console_user` 重启实例后
   - `GET /api/identity` → `{"member":"","device":"","configured":false}`；
   - 写面 `POST /api/rooms/session:1/messages` → **403** + `{"error":"本机未配置控制台人名 console_user，无法确定身份；请在 agentd 配置文件中设置 console_user 后重试"}`；
   - 写面 `POST /api/sessions` → **403** 同文案；读面 `GET /api/sessions` → **403** 同文案（P-2 甲）；
   - **零落账**：拒收前后房间最新 seq 均 = 3。
3. **一名多端 + 已读按人合并（U2）**：同一 `console_user` 两次登录（`mac-air` / `ipad`）→ 两端 `/api/identity` 均 `member:"user:sycm"`、device 各异；agent 发一条后两端未读均 = 3；从 `mac-air` 标记已读（upto_seq=4）后**两端未读均 = 0**。
4. **外部 harness 真机链（T3/U4/U5）**：
   - `session create "…" --owner agent:probe` → `{"id":"session:1","owner":"agent:probe","members":["agent:probe"]}`；
   - `session send session:1 "…" --agent probe` → `actor=agent:probe, seq=2`；
   - 控制台以 `user:sycm` @agent:probe 发话 → 挂着的 `session wait agent:probe --timeout 25s` **命中**：`{"session":"session:1","hit":{"seq":3,"room":"session:1","actor":"user:sycm","body":"@agent:probe 真机唤醒"},"unread":2}`。
   - 观察：会话仅 `agent:probe` 一个成员时，未出示 agent 身份的 CLI 席位发话被拒（`书写者与房间身份不符`）——写面 = 成员∪席位，符合设计，非缺陷。
   - B366 自助加入实测：`POST /api/sessions/session:1/members`（空体）→ `{"ok":true}`，成员由服务端注入（body 身份被忽略）。
5. **游标迁移（plan §10.5）**：把 `room-cursors.json` 写成旧脸 `{"web:mac-air":{"session:1":4}}` 重启实例后
   - 该人的未读 = **3**（旧 `web:` 行不冒充「已读」，无脏读）；
   - agentd 日志无 panic/error；
   - 标记已读后文件自愈为 `{"user:sycm":{"session:1":4},"web:mac-air":{"session:1":4}}`（新键写入；旧孤儿行保留）。
6. **OOS 登记（plan §10.6）**：`docs/roadmap.md` 已有三条（老 `web:` 孤儿成员行展示治理 / handoff 拉起的会话级主 agent 唤醒绑定 / 拉权对称）。
7. **真实浏览器渲染（plan §10.1，agent-browser + Chrome 153 实测）**：
   - 环境：`go build -tags embedweb`（真前端产物嵌进二进制，非 stub），两个独立实例——配名 `127.0.0.1:7799`（`console_user: sycm`）/ 未配名 `127.0.0.1:7800`（独立 datadir，避 `agentd.lock` 互斥）。
   - ① **缺名横幅**：未配名实例左栏渲染 `alert`：`会话列表已断开：本机未配置控制台人名 console_user，无法确定身份；请在 agentd 配置文件中设置 console_user 后重试`（可行动、含 `console_user`）。截图 `shot-noname-banner.png`。
   - ② **新建表单只有标题**：对话框 DOM = heading「新建会话」+ LabelText「标题」+ textbox「会话标题」+ 取消/创建；`grep -ciE 'owner|群主'` = **0**。截图 `shot-newsession-form.png`。
   - ③ **落款人名+端名**：本人消息 `user:sycm · mac-air · #3`；④ **端戳缺失回退仅人名**：主 agent 消息 `agent:probe · #2` / `agent:probe · #4`（无端名）。
   - 自方判定：本人消息靠右深色、agent 消息靠左（`isSelfActor` 取服务端 identity 精确等值的可见效果）。截图 `shot-session-signature.png`。

## 4. 契约对照（有图项目）

- 分支视图 `codegraph check --repo . --view cards-B358.9-charter-4` → `warns` 若干（budget-raised），`fails` **1 条**：`dead-contract d_transport→d_ledger`。
- 归因实测：该 fail 在 **contract 提交 `1d77658d` 与 plan 提交 `7105044` 就已在场**（两处同为这一条），T1–T4 未改变它；`target.json` 中该方向的 `legacyBudgetNote` 自述「**B370：新增方向**……骨架提交时活跃边承载在分支视图 cards-B370-chart…」——即这条声明属**别卡 B370**，其实现不在本分支。
- 基线 `94246fc8` 的 fail 集是另一对（`d_execution→d_orchestration`，与 `TestRepoContractGate` 同源）。
- 判定：**本卡代码未引入该 fail**；分支图快照混入了 B370 的声明（图卫生问题），记残余。本卡各接缝预算：T2/T3/T4 的 review 回合已逐条对账（「图三闸与基线逐条相同」）。

## 5. 残余与未做（逐条落 roadmap.md）

1. ~~**真实浏览器渲染未验**~~ → **已验**：见 §3.7（agent-browser + Chrome 153，四项逐条实测，截图三张）。
2. 跨**物理机**同名未验：本次以同一 agentd 上两台设备登录（`mac-air`/`ipad`）代替「两台机器」；跨机需把本分支部署到第二台机器。
3. 源码守卫 `TestConsoleFaceNoWebFallbackSourceGuard` 未覆盖 `identity.go`（行为锁兜底）。
4. 旧 `web:` 游标孤儿行不清理（纯缓存死行，无行为影响）。
5. 分支图快照混入 B370 声明 → `d_transport→d_ledger` dead-contract（图卫生，非本卡代码）。

## 6. 结论

复跑 ✅（本卡触及包与 web 全绿；唯一红为既存 `TestRepoContractGate`，基线同红）、变异复验 ✅（M1–M4 全有牙且还原回绿）、真机 ✅（端戳来源 / fail-closed+零落账 / 一名多端+已读按人合并 / 外部 harness 链 / 游标迁移 / **真实浏览器渲染四项**，6 项逐条实测）、契约对照 ⚠️（唯一 fail 归因 B370 声明，非本卡）、OOS 登记 ✅。
**真机清单已清空**（plan §10.1–§10.5 逐条执行完毕；§10.6 OOS 三条已在 roadmap）。残余 §5.2–§5.5 均非阻塞、已落 roadmap。**可以归档。**
