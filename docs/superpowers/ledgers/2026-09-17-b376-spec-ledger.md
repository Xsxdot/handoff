# B376 spec 台账

- 2026-09-17 接手：`handoff card bind B376`；卡在 spec。产品对齐三桶已在卡 comment seq 14747。
- 基线：`cards/B233.1-charter-7` @ `acb68493`。
- 读数（本机 `~/.handoff/handoff.db`）：gate 工单 320 全已答；`approver_decision` 40（approve 18 / escalate 11 / error 11）；`permission_auto_allow` 1。最近一次审批 2026-09-02。`config.yaml` `approver.executor: grok`。
- 读数（linux-01 `/root/.handoff/handoff.db`）：累计 `approver_decision` 4623（approve 3674 / escalate 746 / error 203）；`permission_auto_allow` 1127（go-test 461、git-status 131、git-log 113、grep 107、git-diff 104…）。`config.yaml` `approver.executor: codex`，`env` 对 agy/codex/grok/opencode 均指 `proxy.env`。
- 近 14 天 linux-01：decision 1690（approve 1069 / escalate 509 / error 112）；auto_allow 701。仍进审批的只读大宗：`cd &&` 391、白名单 `| head/tail/grep` ~140、分号串 61、`go run …/codegraph` 31、`codegraph` 查询 ~40、`sed -n` 18、`npm --prefix web test` 17、`git branch`/`merge-base` ~11。`git add`/`commit` 仅 20。
- 现状代码：`safeCommandID` 拒 `|` `;` 换行，仅 ledger amend 允许一对 `&&`。`ApproverConfig.Executor` 是 `string`。停用后 `Client.Request` 用 `verdict.Reason` 升级。
- `codegraph sym Gate.Judge` → `n_permgate_Gate_Judge` `d_policy` `internal/permgate/permgate.go:159`。`codegraph who-calls Judge` 空边（图覆盖债）。`codegraph sym Approver.Decide` → `n_orchestration_Approver_Decide` `d_orchestration` `internal/orchestration/approver.go:119`。
- 用户 2026-09-17 批准静默补洞：`cd` 当静默段、`;` 与 `&&`/`||` 同等拆、`npm --prefix`、`sed -n`；产品已点名的 rg/codegraph/只读 git/读管道一并写死形态。`go run codegraph` 故意留 Consult。
- 更正：曾写「裸 echo 拆 `;` 后会消失、不必进表」——错。拆完 echo 自己是一段，不收则整条 Consult。现场（linux-01 任务 `20af766e` drain）：`bash: sed -n '…' f; echo "=== cursor"`。`bash:` 是工具名，Command 是后面那串。2026-09-17 用户：「补，无重定向的 echo/printf 进静默。」`echo x > file` 仍非静默。
- 定级 L2：Judge 三出口与 Decide 签名不变；yaml 标量仍合法；不新开跨子系统契约。
- 弃选：整串进白名单表；半段放行；failover 把干净 escalate 当故障；改 wait 假集合。
- 2026-09-17 用户批准 spec：「批准，进 plan。」头部回写已批准；无重定向 echo/printf 同日补批。
- 提交（历史读数）：`git commit` → `a1e1a9d2 docs(B376): spec 权限门降噪（拆段静默 + failover + 可观测）`（3 files, 174 insertions）。随后 amend 一次收台账行。
