# agentd service lifecycle ledger

- 2026-08-20：基线 `go test ./...` 已执行；已有失败为 `TestDispatchAutoRegisterSurvivesMissingLocalAgentd`、`TestRegisterDegradesWhenLocalAndNoTarget`、`TestRegisterFailsWhenNoLocalAndNoTarget`，原始报错均为本机 agentd 的 `project add` 返回 404，未归因到本计划。
- 2026-08-20：Task 1 开始；先添加跨平台契约测试，尚未提交。
- 2026-08-20：Task 1 双裁决通过；spec 符合：Manager 新增 Stop/Restart、Status 新增 Disabled、ErrNotInstalled 可由 errors.Is 判别、三平台仅落显式报错桩、两处 fake 补齐；代码质量：gofmt 无输出、`go build ./...` 通过、`go test ./internal/service/` 通过。Task 1 commit 范围：`internal/service/service.go`、`internal/service/service_test.go`、三平台桩、`cmd/service_test.go`、`desktop/internal/shell/lifecycle_test.go`、本 ledger；根 `cmd` 与 desktop 全包仍受基线 404、缺 frontend embed、只读 `/tmp` 失败影响。
- 2026-08-20：Task 2 双裁决通过；spec 符合：launchd 的 Start/Stop/Restart、plist 安装判据、Disabled 两种格式、轮询复核、PID 变化复核与 Install enable 全部实现并由 `TestLaunchd*` 覆盖；代码质量：gofmt 无输出、`go build ./...` 与 `go test ./internal/service/` 通过。Task 2 commit 范围：`internal/service/launchd.go`、`internal/service/launchd_test.go`、本 ledger。
- 2026-08-20：Task 3 双裁决通过；spec 符合：systemd 的 Start/Stop/Restart、unit 文件安装硬拒、enable/disable --now、active 复核、Disabled 状态与 sudoHint 全部实现并由 `TestSystemd*` 覆盖；代码质量：gofmt 无输出、`go build ./...` 与 `go test ./internal/service/` 通过。Task 3 commit 范围：`internal/service/systemd.go`、`internal/service/systemd_test.go`、本 ledger。
- 2026-08-20：Task 4 双裁决通过；spec 符合：schtasks 的 `/Disable→/End`、`/Enable→/Run`、停止复核、restart 顺序、未安装硬拒、XML UTF-8/UTF-16LE Disabled 判据全部实现并由 `TestWindows*` 覆盖；代码质量：gofmt 无输出、`go build ./...` 与 `go test ./internal/service/` 通过。平台验证：本轮未做 Windows 真机走查，按计划留待下一次 Windows 走查。Task 4 commit 范围：`internal/service/windows.go`、`internal/service/windows_test.go`、本 ledger。
- 2026-08-20：Task 5 双裁决通过；spec 符合：service start/stop/restart 已注册，输出与 stop 警告、ErrNotInstalled→install 处置、status 五档与生命周期 Info/Error 日志全部实现并由 `TestService*` 覆盖；代码质量：gofmt 无输出、根 `go build ./...` 与 `go test ./internal/service/` 通过。完整 `go test ./cmd/` 仍复现基线的 3 个 project-registration 404 失败，新增 service 测试全部通过。Task 5 commit 范围：`cmd/service.go`、`cmd/service_test.go`、本 ledger。
- 2026-08-20：Task 6 双裁决通过；spec 符合：EnsureRunning 对 Disabled 直接返回、不调用 Start/Install，桌面边界注释与 main.go 注释已同步；代码质量：gofmt 无输出，`go build ./...`（根模块）、`go test ./internal/shell/ -run 'TestEnsureRunning' -v` 通过。desktop 全包仍有已知环境失败：`main.go:41:12: pattern all:frontend/dist: no matching files found`，以及 `TestSyncOnOpenOrderIsLoadBearing` 写 `/tmp` 的 `read-only file system`。Task 6 commit 范围：`desktop/internal/shell/lifecycle.go`、`desktop/internal/shell/lifecycle_test.go`、`desktop/main.go`、本 ledger。
- 2026-08-20：Task 7 双裁决通过；spec 符合：中英文 README 的托管说明与命令表、CHANGELOG Unreleased 均补充 start/stop/restart，并明确 stop 持续到 start、重启机器也不会复活；代码质量：文档 diff 已核对与 Task 5 输出及行为一致，`git diff --check` 无输出。Task 7 commit 范围：`README.md`、`README.zh-CN.md`、`CHANGELOG.md`、本 ledger。
- 2026-08-20：整分支终审首轮发现 `cmd/service.go` 文件头和 install 成功提示仍指向原始 `systemctl stop / launchctl bootout`，未体现新命令；一次性修正为 `handoff service stop`（临时）与 `handoff service uninstall`（彻底摘管）。复审通过：范围 diff、gofmt、`git diff --check`、根 `go build ./...` 与 `go test ./cmd/ -run 'TestService' -v` 均通过。修复范围：`cmd/service.go`、本 ledger；根全量测试仍有基线 project-registration 404 与既有 grok/authsync 失败，desktop 全包仍有缺 frontend embed 与只读 `/tmp` 失败。
- 2026-08-20：审核修复轮双裁决通过；spec 符合：launchd Start 在 bootstrap/kickstart 双失败时同时保留两边原文并在 Error 日志中记录两边，waitRunning/waitStopped 改用 currentPid 轻量探针且注释说明 pid 与运行态的等价关系；代码质量：`gofmt -l` 无输出、`go build ./...`、`go test ./internal/service/` 与 launchd 专项测试通过。全量 `go test ./...` 返回 1，原始失败行包括 `--- FAIL: TestCursorRootFallsBackToCwdWhenHomeUnwritable`、`--- FAIL: TestCursorRootErrorNamesBothPaths`、`--- FAIL: TestLoadStripUpdateDoesNotBlockOnSaveFailure`、`--- FAIL: TestPermServerAskThenRespond`、`--- FAIL: TestSyncAuthKeepsTaskCopyWhenWriteFails`。修复范围：`internal/service/launchd.go`、`internal/service/launchd_test.go`、本 ledger。
