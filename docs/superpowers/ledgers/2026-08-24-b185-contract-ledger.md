# B185 契约落地台账

- 2026-08-24：`git status --short --branch` 原始输出为 `## cards/B185-charter`，无工作区改动；`git log --oneline --decorate -12` 原始输出显示 `4f745783 (HEAD -> cards/B185-charter, origin/main, origin/HEAD) merge(b185): spec 已批准落 main（范围收窄为只搬宿主，恢复拆至 B225）`。
- 2026-08-24：读取 `docs/superpowers/specs/2026-08-23-b185-step-runner-in-agentd.md` 头部，原始状态行为 `- 状态：**已批准**（2026-08-23，用户批准第二版）`；满足开工前状态核对。
- 2026-08-24：`nl -ba internal/agentd/ledgerapi.go | sed -n '390,430p'` 原始输出确认 `handleCardStep` 当前局部请求结构只有 `Step string json:"step"`（399-401），用 `json.NewDecoder(r.Body).Decode` 解码（402），actor 在 413 行硬编码为 `"web:" + r.RemoteAddr`，成功响应为 HTTP 202 与 `{"ok":true}`（416-417）。
- 2026-08-24：`nl -ba internal/ledgerstep/runner.go | sed -n '19,45p'` 原始输出确认 `StepRunner` 的可注入字段含 `Session string`（28）、`Target string`（36）、`Executor string`/`Model string`（39-40）和 `Extra string`（42），没有 `PlanPath` 字段；Session 注释要求区分两个并发驱动者（26-27）。
- 2026-08-24：`nl -ba cmd/ledgercli.go | sed -n '53,70p'` 原始输出确认 CLI `ledgerActor()` 格式为 `cli:<user>@<host>`（61），`ledgerSession()` 返回 `fmt.Sprintf("%s#%d", ledgerActor(), os.Getpid())`（68-69），即 `cli:<user>@<host>#<pid>`。
- 2026-08-24：`nl -ba internal/ledgerstep/dispatch.go | sed -n '60,96p;227,256p'` 原始输出确认 `TemplateDispatch.PlanPath string`（64）按调用方 CWD 读取（94-95、228-235），但 `StepRunner.dispatchNode` 只构造 `TemplateDispatch` 的覆盖项与 `Extra`，没有 PlanPath（runner.go 160-170）；agentd `startCardStep` 构造 `StepRunner` 时也只传 Session/Dispatcher/Clients（cardstep.go 43-49）。
- 2026-08-24：`nl -ba cmd/card_dispatch.go | sed -n '152,218p'` 原始输出确认 `--step` 分支在 163-165 行直接调用 `runStepDispatch`，`--plan` 只在非 `--step` 的 `TemplateDispatch`（192 行）传入；`--target`、`--executor`、`--model`、`--extra` 旗标定义于 211、213、216-217 行。
- 2026-08-24：`nl -ba web/src/api/ledger.ts | sed -n '236,241p'` 与 `nl -ba web/src/api/ledger.test.ts | sed -n '65,68p'` 原始输出确认看板 `runCardStep(id, step)` 只 POST `{ step }` 到 `/api/cards/{id}/step`，现有测试逐字段断言该形状。
- 2026-08-24：`nl -ba internal/proto/contract_fixture_test.go | sed -n '1,18p;32,57p'` 原始输出确认固定件由 Go `json.Marshal` 生成并与 `web/src/api/testdata/<类型名>.json` 逐字节比较；只有 `go test ./internal/proto/ -run TestContractFixtures -update` 才重写 fixture，默认不覆盖。
- 2026-08-24：`sed -n '1,260p' codegraph/target.json` 原始输出确认仓库存在目标图，包含 `d_cli`、`d_controlplane`、`d_contract` 等域及 contracts；`find codegraph/diffs -maxdepth 1 ...` 原始输出未列出 `cards-B185-charter.json`，本轮不引入代码符号，视图 diff 暂无理由新建。
- 2026-08-24：按契约冻结纪律更新 `codegraph/target.json`：新增 `POST /api/cards/{id}/step` 的 `d_cli→d_controlplane` 与 `d_web→d_controlplane` 入口，新增 `proto.CardStepReq` 的 CLI/控制面契约条目，以及 `ledgerstep.StepRunner` 的控制面→账本条目；仅改目标图，不改实现代码。
- 2026-08-24：`go run . graph check --repo .` 实际退出码 `1`；原始报错为 `Error: 契约对照发现 2 处违规`，fails 为 `d_cli->d_ledger 直调 2 条超出预算 1` 与 `d_controlplane->d_contract 直调 20 条超出预算 19`，另有 `main.go` 图外 warning；该基线失败发生在本轮文档/目标图变更前，未将其伪报为本轮通过。
- 2026-08-24：`git diff --check && go run . graph resolve --repo . --doc docs/superpowers/specs/b185-contract.md && go run . graph check --repo .` 中 `git diff --check` 与 `graph resolve` 实际通过；resolve 原始结果列出 14 个锚点，全部为 `ok` 或既有 `moved`，无坏锚。随后 graph check 仍实际退出码 `1`，原始失败仍为上述 2 个 over-budget，未新增 B185 特定 fail。
- 2026-08-24：`go test ./internal/proto/ -run TestContractFixtures -count=1` 原始输出为 `ok  github.com/Xsxdot/handoff/internal/proto  0.004s`，退出码 `0`；现有 Go→Web fixture 固定件通过。
- 2026-08-24：`go test ./internal/ledgerstep/ -count=1` 原始输出为 `ok  github.com/Xsxdot/handoff/internal/ledgerstep  5.198s`，退出码 `0`；现有 StepRunner/覆盖优先级基线通过。
- 2026-08-24：`go test ./internal/agentd/ -run 'TestCardStep(Returns202|SecondReturns409|RejectsImplement|AcceptsNodeName)$' -count=1` 原始输出为 `ok  github.com/Xsxdot/handoff/internal/agentd  0.730s`，退出码 `0`；这是未改实现的当前行为基线，不能当作 B185 新实现已完成。
- 2026-08-24：`go test ./cmd/ -run 'TestCardDispatchStepExecutorModelFlags$' -count=1` 原始输出为 `ok  github.com/Xsxdot/handoff/cmd  0.114s`，退出码 `0`；CLI 现有 `--step` 覆盖旗标基线通过，未改 `cmd/`。
- 2026-08-24：首次执行 `git add codegraph/target.json docs/superpowers/specs/b185-contract.md docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short --branch` 失败；原始报错为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/ef71da8e/index.lock': Read-only file system`，尚未暂存或提交。
- 2026-08-24：提升权限重跑同一暂存/校验命令成功；原始统计为 `codegraph/target.json | 17 ++-`、台账 `20 +++`、契约文档 `157 +++++++++++++++++++++`，合计 `3 files changed, 192 insertions(+), 2 deletions(-)`；`git diff --cached --check` 无输出，三文件均显示已暂存。
- 2026-08-24：补记暂存后再次执行 `git add docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short --branch`（提升权限）成功；原始统计为 `3 files changed, 193 insertions(+), 2 deletions(-)`，`git diff --cached --check` 无输出，分支仍为 `cards/B185-charter`。
