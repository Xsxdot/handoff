# B185 契约落地台账

- 2026-08-24：`git status --short --branch` 原始输出为 `## cards/B185-charter`，无工作区改动；`git log --oneline --decorate -12` 原始输出显示 `4f745783 (HEAD -> cards/B185-charter, origin/main, origin/HEAD) merge(b185): spec 已批准落 main（范围收窄为只搬宿主，恢复拆至 B225）`。
- 2026-08-24：读取 `docs/superpowers/specs/2026-08-23-b185-step-runner-in-agentd.md` 头部，原始状态行为 `- 状态：**已批准**（2026-08-23，用户批准第二版）`；满足开工前状态核对。
- 2026-08-24：`nl -ba internal/agentd/ledgerapi.go | sed -n '390,430p'` 原始输出确认 `handleCardStep` 当前局部请求结构只有 `Step string json:"step"`（399-401），用 `json.NewDecoder(r.Body).Decode` 解码（402），actor 在 413 行硬编码为 `"web:" + r.RemoteAddr`，成功响应为 HTTP 202 与 `{"ok":true}`（416-417）。
- 2026-08-24：`nl -ba internal/ledgerstep/runner.go | sed -n '19,45p'` 原始输出确认 `StepRunner` 的可注入字段含 `Session string`（28）、`Target string`（36）、`Executor string`/`Model string`（39-40）和 `Extra string`（42），没有 `PlanPath` 字段；Session 注释要求区分两个并发驱动者（26-27）。
- 2026-08-24：`nl -ba cmd/ledgercli.go | sed -n '53,70p'` 原始输出确认 CLI `ledgerActor()` 格式为 `cli:<user>@<host>`（61），`ledgerSession()` 返回 `fmt.Sprintf("%s#%d", ledgerActor(), os.Getpid())`（68-69），即 `cli:<user>@<host>#<pid>`。
- 2026-08-24：`nl -ba internal/ledgerstep/dispatch.go | sed -n '60,96p;227,256p'` 原始输出确认 `TemplateDispatch.PlanPath string`（64）按调用方 CWD 读取（94-95、228-235），但 `StepRunner.dispatchNode` 只构造 `TemplateDispatch` 的覆盖项与 `Extra`，没有 PlanPath（runner.go 160-170）；agentd `startCardStep` 构造 `StepRunner` 时也只传 Session/Dispatcher/Clients（cardstep.go 43-49）。
- 2026-08-24：`nl -ba cmd/card_dispatch.go | sed -n '152,218p'` 原始输出确认 `--step` 分支在 163-165 行直接调用 `runStepDispatch`，`--plan` 只在非 `--step` 的 `TemplateDispatch`（192 行）传入；`--target`、`--executor`、`--model`、`--extra` 旗标定义于 211、213、216-217 行。
- 2026-08-24：`nl -ba web/src/api/ledger.ts | sed -n '236,241p'` 与 `nl -ba web/src/api/ledger.test.ts | sed -n '65,68p'` 原始输出确认看板 `runCardStep(id, step)` 只 POST `{ step }` 到 `/api/cards/{id}/step`，现有测试逐字段断言该形状。
- 2026-08-24：`nl -ba internal/proto/contract_fixture_test.go | sed -n '1,18p;32,57p'` 原始输出确认固定件由 Go `json.Marshal` 生成并与 `web/src/api/testdata/<类型名>.json` 逐字节比较；只有 `go test ./internal/proto/ -run TestContractFixtures -update` 才重写 fixture，默认不覆盖。
- 2026-08-24：`sed -n '1,260p' codegraph/target.json` 原始输出确认仓库存在目标图，包含 `d_cli`、`d_controlplane`、`d_contract` 等域及 contracts；`find codegraph/diffs -maxdepth 1 ...` 原始输出未列出 `cards-B185-charter.json`，本轮不引入代码符号，视图 diff 暂无理由新建。
- 2026-08-24：按契约冻结纪律更新 `codegraph/target.json`：新增 `POST /api/cards/{id}/step` 的 `d_cli→d_controlplane` 与 `d_web→d_controlplane` 入口，新增 `proto.CardStepReq` 的 CLI/控制面契约条目，以及 `ledgerstep.StepRunner` 的控制面→账本条目；仅改目标图，不改实现代码。
- 2026-08-24：`go run . graph check --repo .` 实际退出码 `1`；原始报错为 `Error: 契约对照发现 2 处违规`，fails 为 `d_cli->d_ledger 直调 2 条超出预算 1` 与 `d_controlplane->d_contract 直调 20 条超出预算 19`，另有 `main.go` 图外 warning；该基线失败发生在本轮文档/目标图变更前，未将其伪报为本轮通过。
- 2026-08-24：`git diff --check && go run . graph resolve --repo . --doc docs/superpowers/specs/b185-contract.md && go run . graph check --repo .` 中 `git diff --check` 与 `graph resolve` 实际通过；resolve 原始结果列出 14 个锚点，全部为 `ok` 或既有 `moved`，无坏锚。随后 graph check 仍实际退出码 `1`，原始失败仍为上述 2 个 over-budget，未新增 B185 特定 fail。
- 2026-08-24：`go test ./internal/proto/ -run TestContractFixtures -count=1` 原始输出为 `ok  github.com/Xsxdot/handoff/internal/proto  0.004s`，退出码 `0`；现有 Go→Web fixture 固定件通过。
- 2026-08-24：`go test ./internal/ledgerstep/ -count=1` 原始输出为 `ok  github.com/Xsxdot/handoff/internal/ledgerstep  5.198s`，退出码 `0`；现有 StepRunner/覆盖优先级基线通过。
- 2026-08-24：`go test ./internal/agentd/ -run 'TestCardStep(Returns202|SecondReturns409|RejectsImplement|AcceptsNodeName)$' -count=1` 原始输出为 `ok  github.com/Xsxdot/handoff/internal/agentd  0.730s`，退出码 `0`；这是未改实现的当前行为基线，不能当作 B185 新实现已完成。
- 2026-08-24：`go test ./cmd/ -run 'TestCardDispatchStepExecutorModelFlags$' -count=1` 原始输出为 `ok  github.com/Xsxdot/handoff/cmd  0.114s`，退出码 `0`；CLI 现有 `--step` 覆盖旗标基线通过，未改 `cmd/`。
- 2026-08-24：首次执行 `git add codegraph/target.json docs/superpowers/specs/b185-contract.md docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short --branch` 失败；原始报错为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/ef71da8e/index.lock': Read-only file system`，尚未暂存或提交。
- 2026-08-24：提升权限重跑同一暂存/校验命令成功；原始统计为 `codegraph/target.json | 17 ++-`、台账 `20 +++`、契约文档 `157 +++++++++++++++++++++`，合计 `3 files changed, 192 insertions(+), 2 deletions(-)`；`git diff --cached --check` 无输出，三文件均显示已暂存。
- 2026-08-24：补记暂存后再次执行 `git add docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short --branch`（提升权限）成功；原始统计为 `3 files changed, 193 insertions(+), 2 deletions(-)`，`git diff --cached --check` 无输出，分支仍为 `cards/B185-charter`。
- 2026-08-24：最终补记行加入后重新暂存，原始统计为 `3 files changed, 194 insertions(+), 2 deletions(-)`；`git commit -m "docs(b185): freeze step dispatch contract"` 实际成功，原始输出为 `[cards/B185-charter 62c63bef] docs(b185): freeze step dispatch contract`，创建契约文档与台账并提交目标图。
- 2026-08-24：提交后执行 `git status --short --branch && git log -3 --oneline --decorate && git diff --check && go run . graph resolve --repo . --doc docs/superpowers/specs/b185-contract.md`；原始状态为 `## cards/B185-charter`、HEAD `88aa4332 chore(ledger): close B185 contract`、上一提交 `62c63bef docs(b185): freeze step dispatch contract`，`git diff --check` 无输出，resolve 14 个锚点均为 `ok` 或既有 `moved`，退出码 `0`。
- 2026-08-24：本轮起始执行 `pwd && git branch --show-current && git status --short && rg --files -g '!*node_modules*' | sed -n '1,160p'`；原始输出确认工作树为 `/root/.handoff/worktrees/e0f28290`、当前分支为 `cards/B185-charter-2`，`git status --short` 无输出。
- 2026-08-24：读取 B185 spec、contract 与本台账；原始文档头确认 spec 状态为「已批准（2026-08-23，用户批准第二版）」、contract 标注「契约冻结」且声明本轮不改实现域/看板/card wait；contract §1 冻结 `internal/proto.CardStepReq` 六字段、legacy 看板 `{step}` 的 actor fallback、CLI `ledgerSession` actor、四层 StepRunner 落点与 implement「内联本地文件」守卫。
- 2026-08-24：读取 `codegraph/target.json` 与 `2026-08-21-handoff-instantiation-checklist.md`；原始输出确认目标图的 `subsystems` 含 `d_ledger`/`d_controlplane`/`d_cli`/`d_web`/`d_contract`，类型均为 logic，本次 project defect families 为通用五族加序列化边界、枚举白名单、webview/平台表现候选族与承重安全属性追加设问。
- 2026-08-24：读取 `2026-08-23-b192-breakdown.md` 作为轻档拆解范例；原始文档结构包含稿首待拍板清单、目标图子系统与四条派卡资格核、契约逐条核对、四段式单元/DAG、逐族验收、真机清单与自检。
- 2026-08-24：执行 `rg -n -C 8 'func runStepDispatch|runStepDispatch|cardDispatchStep|stepTransport|type CardStep|type CardStepReq|handleCardStep' cmd internal web/src/api` 与定点 `sed`；原始输出确认现状 CLI `runStepDispatch` 在 `cmd/card_node.go#runStepDispatch` 同步调用 `StepRunner.Run` 并编码 `Outcome`，agentd `handleCardStep` 在 `internal/agentd/ledgerapi.go#handleCardStep` 只解码 `{step}`、按节点名拒绝 `implement`，`startCardStep`/`stepTransport` 在 `internal/agentd/cardstep.go` 负责异步装配与远端 dispatch，Web `runCardStep` 仍发送 `{step}`。
- 2026-08-24：执行 `git log -8 --oneline --decorate && git status --short --branch`；原始输出确认当前分支 `cards/B185-charter-2` 与 `cards/B185-charter` 同指 `dd2118fc chore(ledger): record B185 final verification`，其上已有 contract 冻结与收口提交；本轮仅台账有未提交修改。
- 2026-08-24：执行 `rg -n 'TargetEndpoint\(\)|client.New\(' cmd/*.go` 与 `sed`；原始输出确认 `cmd#TargetEndpoint` 已提供本机 agentd 地址/token，`cmd#newTargetClient` 在本机模式返回 `client.New(addr, token)`，可作为 CLI→本机 agentd 的既有拨号装配入口；当前 `internal/client#Client.Dispatch` 只覆盖 `/api/tasks`，尚无 card step 方法。
- 2026-08-24：执行 `sed -n '1,180p' web/package.json`；原始输出确认 Web 校验命令为 `npm run test`（vitest run）、`npm run typecheck`（tsc -b）、`npm run build`（tsc -b + vite build）。
- 2026-08-24：执行 `sed`/`rg` 检查 `internal/agentd/ledgerapi_test.go`、`internal/agentd/cardstep_test.go`、`cmd/card_dispatch_test.go`、`cmd/ledgercli_test.go`；原始输出确认已有 `TestCardStepReturns202`、`TestCardStepSecondReturns409`、`TestCardStepRejectsImplement`、`TestCardStepAcceptsNodeName`、`TestCardDispatchStepExecutorModelFlags`、`TestCardDispatchStepExtraReachesPrompt` 与 `runLedgerCLI`/`swapDispatchTransportWithOpts`，但它们分别覆盖旧 handler/旧同步 CLI runner，不能单独证明新的 CLI→本机 `/api/cards/{id}/step` HTTP 穿线。
- 2026-08-24：执行 `rg --files | rg -i '(^|/)change(log|s)?'` 与 `sed -n '1,100p' CHANGELOG.md`；原始输出确认根目录 `CHANGELOG.md` 存在，`[Unreleased]` 有占位，spec 明确要求记录 `--step` 不再打印 `Outcome` 与新增本机 agentd 硬依赖。
- 2026-08-24：执行 B203 测试定位命令 `rg -n 'ExecutorModelOverrides|PairRule|...' internal/ledgerstep/*_test.go ...`；原始输出确认既有 `TestViaTemplateExecutorModelOverridesAndPairRule`、`TestViaTemplateSameExecutorKeepsTemplateModel`、`TestRunnerExecutorModelOverridePriorityAndPairRule`、`TestRunnerSameExecutorKeepsNodeModel`，可作为 wire 搬运后覆盖优先级/成对规则的既有回归锚点，不应改写其语义断言。
- 2026-08-24：新增 `docs/superpowers/specs/b185-breakdown.md`；文档已写入待拍板 P1–P3、五个有序单元 U0–U4、目标图子系统/四条派卡资格、冻结契约逐条核对、既有夹具缺口、缺陷族逐族结论、变异对照和真机清单；本轮未改实现代码、contract 或 Web/agentd/CLI 实现。
- 2026-08-24：执行 `git diff --check && rg -n '^## 0\.|...|webview' docs/superpowers/specs/b185-breakdown.md && wc -l ...`；原始输出为 `git diff --check` 无输出，文档命中 §0–§7、U0–U4、契约疑点/真机/序列化/枚举/承重/webview 等必备段，长度 `473` 行；台账为 `36` 行。
- 2026-08-24：执行 `go run . graph resolve --repo . --doc docs/superpowers/specs/b185-breakdown.md`；原始输出列出 11 个文档锚点，全部为 `ok` 或既有 `moved`（无坏锚），命令退出码 `0`；另输出既有 relay/proxy INFO 日志。
- 2026-08-24：执行 `git diff --check && git diff --stat && git status --short --branch`；原始输出 `git diff --check` 无输出，分支为 `## cards/B185-charter-2`，当前仅有台账修改与未跟踪拆解文档；未发现实现代码变更。
- 2026-08-24：执行 `rg -n '�|���|TODO|TBD|无风险' ... || true; rg -n '^\\| P[0-9]' ...; rg -n '^### U[0-4]' ...; git diff --check`；原始输出无乱码/TODO/TBD 命中，P1–P3 与 U0–U4 各命中，`git diff --check` 无输出。
- 2026-08-24：首次执行 `git add docs/superpowers/specs/b185-breakdown.md docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md` 失败；原始报错为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/e0f28290/index.lock': Read-only file system`，尚未暂存。
- 2026-08-24：提升权限重跑同一 `git add docs/superpowers/specs/b185-breakdown.md docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md` 成功；原始输出无内容，两个文档已进入 index。
- 2026-08-24：执行 `git diff --cached --check && git diff --cached --stat && git status --short --branch` 未完成；原始输出为 `docs/superpowers/specs/b185-breakdown.md:3: trailing whitespace`、`:4: trailing whitespace`、`:5: trailing whitespace`，原因是 Markdown 硬换行使用行尾双空格；需改为无尾随空格后重验。
- 2026-08-24：修正拆解文档头部 Markdown 行尾双空格后执行 `git diff --check && git diff --stat && git status --short --branch`；原始输出 `git diff --check` 无输出，未暂存差异为台账 2 行与拆解头部 9 行，当前状态 `MM`/`AM`，待重暂存。
- 2026-08-24：提升权限重跑 `git add ...` 后执行 `git diff --cached --check && git diff --cached --stat && git status --short --branch`；原始输出 `git diff --cached --check` 无输出，暂存统计为拆解 `476` 行、台账 `20` 行，合计 `2 files changed, 496 insertions(+)`，分支仍为 `cards/B185-charter-2`，两文件均已暂存。
- 2026-08-24：执行 `git add docs/superpowers/specs/b185-breakdown.md docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git commit -m "docs(b185): add breakdown proposal"` 成功；原始输出为 `[cards/B185-charter-2 4925ac31] docs(b185): add breakdown proposal`、`2 files changed, 497 insertions(+)`，拆解文档已入库。
- 2026-08-24：提交后执行 `git diff --check && git status --short --branch`；原始输出 `git diff --check` 无输出，状态仅为 `M docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md`，说明首个提交干净且仅待补写本次提交事实。
- 2026-08-24：执行 `git commit -m "chore(ledger): close B185 breakdown"` 成功；原始输出为 `[cards/B185-charter-2 f5a38ff1] chore(ledger): close B185 breakdown`、`1 file changed, 2 insertions(+)`。
- 2026-08-24：执行 `git commit -m "chore(ledger): record B185 breakdown close"` 成功；原始输出为 `[cards/B185-charter-2 5df1b86f] chore(ledger): record B185 breakdown close`、`1 file changed, 1 insertion(+)`。
- 2026-08-24：最终复核执行 `git status --short --branch && git log -4 --oneline --decorate && git diff --check && go run . graph resolve --repo . --doc docs/superpowers/specs/b185-breakdown.md`；原始输出状态为 `## cards/B185-charter-2` 且无工作区改动，HEAD 为 `5df1b86f`，`git diff --check` 无输出；resolve 再次列出 11 个锚点，全部 `ok` 或既有 `moved`，退出码 `0`，另有既有 relay/proxy INFO 日志。
- 2026-08-24：执行 `git commit -m "chore(ledger): record B185 final verification"` 成功；原始输出为 `[cards/B185-charter-2 4dd34c31] chore(ledger): record B185 final verification`、`1 file changed, 2 insertions(+)`。
- 2026-08-24：执行 `git commit -m "chore(ledger): record B185 close commit"` 成功；原始输出为 `[cards/B185-charter-2 a206b2a4] chore(ledger): record B185 close commit`、`1 file changed, 1 insertion(+)`。
- 2026-08-24：本轮执行 `pwd && git branch --show-current && git status --short --branch`；原始输出为 `/root/.handoff/worktrees/975d456b`、`cards/B185-charter-3`、`## cards/B185-charter-3`，工作区初始无改动。
- 2026-08-24：读取 handoff skill `/root/.codex/skills/handoff/SKILL.md` 至 EOF；原始内容确认本执行者不得驱动 handoff 状态机，本轮未调用 handoff CLI、未派发 executor。
- 2026-08-24：按代码图优先执行 `go run . graph sym StepRunner --repo . --stale`、`... Client.Dispatch ...`、`... handleCardStep ...`、`... runStepDispatch ...`；四条命令均 exit 0，分别命中 `internal/ledgerstep/runner.go:22`、`internal/client/client.go:746`、`internal/agentd/ledgerapi.go:389`、`cmd/card_node.go:17`；StepRunner 图结果缺少当前 Executor/Model 字段。四条输出均附带原始告警：`基线仍有 7 个未扫描入口：查询结果为空不等于没有调用方`，该告警作为 grep 覆盖债记录。
- 2026-08-24：按代码图优先执行 `go run . graph who-calls n_client_Client_Dispatch --repo .`、`... n_agentd_Server_handleCardStep ...`、`... n_cmdrunStepDispatch ...`、`... m_ledgerstep_StepRunner ...`；四条命令均 exit 0；图命中 `handoff dispatch → dispatchCmd.RunE → Client.Dispatch`、`card dispatch → cardDispatchCmd.RunE → runStepDispatch`，handler/StepRunner 未给出调用边；每条输出原样带 `基线仍有 7 个未扫描入口：查询结果为空不等于没有调用方`。
- 2026-08-24：读取当前源文件并用 `go run . graph resolve --repo . --doc docs/superpowers/specs/b185-breakdown.md` 复核；resolve exit 0，11 个锚点均为 `ok` 或既有 `moved`；源码确认 `internal/client.Client.do(ctx, method, path, body)` 会 JSON marshal、设置 Bearer/Content-Type、保留 `httpError` 原始 body，`internal/agentd.Server.runStepFn` 是既有测试替换缝，`cmd.LocalEndpoint() (addr, token string, err error)` 忽略 `--target` 返回本机端点。
- 2026-08-24：基线执行 `go test ./internal/proto/ -run TestContractFixtures -count=1`；原始输出：`ok  github.com/Xsxdot/handoff/internal/proto  0.047s`，exit 0。
- 2026-08-24：基线执行 `(cd web && npm run test -- src/api/contract.test.ts src/api/ledger.test.ts)`；原始报错：`sh: 1: vitest: not found`，exit 127；未把 Web 测试写成通过。
- 2026-08-24：基线执行 `(cd web && npm run typecheck)`；原始报错：`sh: 1: tsc: not found`，exit 127；未把 Web 类型检查写成通过。
- 2026-08-24：基线执行 `go test ./internal/client -run 'TestCardStep' -count=1`；原始输出：`ok  github.com/Xsxdot/handoff/internal/client  0.005s [no tests to run]`，exit 0但尚无 CardStep 用例，计划中保留必须命中测试的门槛。
- 2026-08-24：基线执行 `go test ./internal/agentd/ -run 'TestCardStep(Returns202|SecondReturns409|RejectsImplement|AcceptsNodeName)$' -count=1`；原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd  1.065s`，exit 0；该绿只证明旧 202/409/implement/node-name 行为，不能证明新契约。
- 2026-08-24：基线执行 B203 回归 `go test ./internal/ledgerstep/ -run 'Test(ViaTemplateExecutorModelOverridesAndPairRule|ViaTemplateSameExecutorKeepsTemplateModel|RunnerExecutorModelOverridePriorityAndPairRule|RunnerSameExecutorKeepsNodeModel)$' -count=1`；原始输出：`ok  github.com/Xsxdot/handoff/internal/ledgerstep  1.302s`，exit 0。
- 2026-08-24：基线执行 `go test ./cmd/ -run 'TestCardDispatchStepExecutorModelFlags$' -count=1` 与 `go test ./cmd/ -run 'TestCardDispatchStepExtraReachesPrompt$' -count=1`；原始输出分别为 `ok  github.com/Xsxdot/handoff/cmd  0.274s`、`ok  github.com/Xsxdot/handoff/cmd  0.238s`，均 exit 0；两条仍验证旧同步 runner 接缝。
- 2026-08-24：基线执行 `go test ./...` 两次；工具输出均在 `cmd` 包结尾观察到原始行 `FAIL`、`FAIL github.com/Xsxdot/handoff/cmd 8.835s`（第二次为 `FAIL github.com/Xsxdot/handoff/cmd 7.087s`），工具返回 exit 未报告（`exit=undefined`），因此全量基线结论记为未验证通过，计划只把它列为 U4 实现后门禁。
- 2026-08-24：基线执行 `git diff --check`；原始输出为空，exit 0。
- 2026-08-24：现状源码查证：`internal/agentd/handleCardStep` 当前只解码 `Step`、按 `discipline.NameImplement` 拒绝并把 actor 硬编码为 `web:<RemoteAddr>`；`startCardStep(cardID, node, actor string) error` 当前装配 `Session`/`Dispatcher.Actor` 但未填 `Target/Executor/Model/Extra`；`cmd.runStepDispatch(cmd, st, id, node, actor string) error` 当前同步 `StepRunner.Run` 并 JSON 输出 `Outcome`；`web.runCardStep(id, step)` 当前固定发送 `{ step }`。
- 2026-08-24：执行 `test -e docs/superpowers/plans/2026-08-24-b185-step-in-agentd.md`；原始输出 `exists_exit=1`，指定计划路径当前不存在；读取 `web/package.json:7-12,29-48` 确认 Web 判据命令为 `npm run test`/`npm run typecheck`，依赖包含 `vitest` 与 `typescript`，但基线环境缺少对应可执行文件。
- 2026-08-24：按补充要求新增计划 `docs/superpowers/plans/2026-08-24-b185-step-in-agentd.md`；计划 833 行、3856 词，覆盖 U0→(U1,U2)→U3→U4、精确 Interfaces、完整生产代码块、基线命令、日志/注释步骤、最小测试范围、序列化边界、缺陷族、变异表、回归网、真机清单和契约疑点；本轮未修改 `cmd/`、`internal/agentd/`、`internal/ledgerstep/`、`internal/proto/`、`web/` 实现或冻结 contract。
- 2026-08-24：执行计划自审命令 `rg -n 'TBD|TODO|同 Task N|加适当的错误处理' docs/superpowers/plans/2026-08-24-b185-step-in-agentd.md || true`；原始输出为空；`git diff --check -- docs/superpowers/plans/2026-08-24-b185-step-in-agentd.md` 原始输出为空；实现域 `git status --short -- cmd internal/agentd internal/ledgerstep internal/proto web` 原始输出为空。
- 2026-08-24：执行 `git diff --check && git diff --stat && git diff --name-only && git status --short --branch`；原始输出为 `git diff --check` 无输出、已跟踪差异仅台账 `18 insertions`、状态 `## cards/B185-charter-3`，计划文件以未跟踪状态出现；命令 exit 0，随后必须把计划和台账一起暂存。
- 2026-08-24：首次执行提升权限的 `git add docs/superpowers/plans/2026-08-24-b185-step-in-agentd.md docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short --branch` 失败；原始报错逐条为计划第 3、4、5、6、96、680 行 `trailing whitespace`，exit 2；未将该校验写成通过，随后只删除 Markdown 行尾空白。
- 2026-08-24：清理行尾空白后提升权限重跑同一暂存/校验命令成功；原始统计为台账 `20` 行、计划 `833` 行，合计 `2 files changed, 853 insertions(+)`，`git diff --cached --check` 无输出，分支仍为 `cards/B185-charter-3`。
- 2026-08-24：执行 `git add docs/superpowers/plans/2026-08-24-b185-step-in-agentd.md docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git diff --cached --check && git commit -m "docs(b185): add step agentd implementation plan"` 成功；原始输出为 `[cards/B185-charter-3 b2a13d5b] docs(b185): add step agentd implementation plan`、`2 files changed, 854 insertions(+)`。
- 2026-08-24：执行 `git add docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git diff --cached --check && git commit -m "chore(ledger): record B185 plan commit"` 成功；原始输出为 `[cards/B185-charter-3 2b86677f] chore(ledger): record B185 plan commit`、`1 file changed, 1 insertion(+)`。
- 2026-08-24：执行 `git add docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md && git diff --cached --check && git commit -m "chore(ledger): close B185 plan"` 成功；原始输出为 `[cards/B185-charter-3 c0d34978] chore(ledger): close B185 plan`、`1 file changed, 1 insertion(+)`。
- 2026-08-24：最终复核 `git status --short --branch && git log -4 --oneline --decorate && git diff --check && rg -n 'TBD|TODO|同 Task N|加适当的错误处理' docs/superpowers/plans/2026-08-24-b185-step-in-agentd.md || true && git diff --name-only HEAD~3..HEAD -- cmd internal/agentd internal/ledgerstep internal/proto web`；原始输出为干净状态 `## cards/B185-charter-3`、HEAD `c0d34978`，`git diff --check` 无输出，禁用占位符扫描无输出，实现域差异文件列表无输出，exit 0。
- 2026-08-24：尝试追加台账收尾事实的 `apply_patch` 失败；原始报错为 `apply_patch verification failed: Failed to find expected lines in /root/.handoff/worktrees/975d456b/docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md`，未改变文件。
- 2026-08-24：读取当前状态：`git status --short --branch` 原始输出为 `## cards/B185-charter-3`；`git log -5 --oneline --decorate` 原始输出首行为 `1de332ec (HEAD -> cards/B185-charter-3) chore(ledger): record B185 final verification`；工作树无未提交差异。
- 2026-08-24：执行 `git add docs/superpowers/ledgers/2026-08-24-b185-contract-ledger.md` 成功；随后执行 `git diff --cached --check`，原始输出为空，exit 0。
- 2026-08-24：更新台账后再次执行 `git diff --cached --check`，原始输出为空，exit 0；该状态作为提交前格式门禁。
