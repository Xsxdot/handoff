# B233.13 breakdown 台账

- 2026-09-11 起点：承接 contract @ `40848750`；当前执行工作树分支 `cards/B233.13-charter-2`（HEAD `40848750`）；有效基线 `cards/B233.1-charter-7` @ `760a5be9`。本节点不切分支、不改 git 配置、不调 handoff CLI、不起 executor。
- 协调者随本节点派发注入的裁决：DTO（`DispatchReq`/`RegisterProjectReq`/`RecoverReport`）留 gateway、与接口同包；编排实现 import gateway；handler 不 import orchestration；cmd 组装。`SetManager` 保留名、参数类型改 `OrchestrationClient`。（本稿记 D1/D2，§0.1）
- 读现状（本工作树复核，非会话记忆）：
  - `Server.mgr *Manager` `internal/agentd/server.go:128`；`SetManager` `:369`（内含 `m.conf = s.conf` 与 `m.ws == nil` 兜底，接口化后不可达 → P1）。
  - `Manager.Done` `internal/agentd/manager.go:1696`、`Stop` `:1825`、`transit` `:3726`（幂等分支 `:3731` `return nil` 只报成功）。
  - `store.UpdateTaskState` CAS `internal/store/store.go:525-552`（实测 `WHERE id=? AND state=?`，影响 0 行返回 `ErrBadTransit`）。
  - `handleDone` 已 `completed` 短路 `internal/agentd/server.go:1746`、成功后无条件 `releaseTaskCarrierOccupancy` `:1772`；`handleStop` `:1800`/`:1815`；释放实现 `:1822`。
  - `NewManager` 非测试调用点仅 `cmd/agentd.go:218`；`SetManager` 组装 `:219-220`；`SetTaskProcCounter`/`ResumeTask`/`ForceReclaim`/`MismatchTransit` `:223/:239/:261/:262`。
  - 混合文件：`gc.go`/`projectadmin.go` 同时含 `func (s *Server)` 与 `func (m *Manager)`；`workspace_capability.go` 同时含 `NewGitCapability`/`gitCapability`（工作区实现，本卡不迁）与 4 个 Manager 方法。
  - Manager 依赖的 gateway 包内未导出符号候选：`checkProcHeadroom`(`admission.go:53`)、`recoverTransit`(`watchdog.go:595`)、`resolveProject`(`projectresolve.go:67`)、`resolveDispatchBase`(`workspace.go:1099`)、`truncateRunes`(`server.go:2624`)、`gitProbe`(`workspace.go`)、`voidTicketsWithAudit`(`ticketvoid.go:46`)。脚本 `python3` 扫描 move 集引用得出；存在方法名误报，最终以编译错误收敛（P3）。
  - 测试：约 23 个 `internal/agentd` 测试文件引 `NewManager`/`Manager`；多数内部包 `package agentd`（`manager_test.go:15`、`export_test.go:1`），外部包先例 `server_test.go`/`integration_test.go`/`hub_test.go`/`status_test.go`/`lock_test.go` 为 `agentd_test`。orchestration import gateway 后内部包测试再 import orchestration 成环（P2）。
- 本节点实测命令与原始结果：
  - `go build ./...` → 退出码 0（`BUILD_EXIT=0`）。
  - `codegraph resolve --doc docs/superpowers/specs/b233.13-contract.md --repo .` → 退出码 0；`--doc docs/superpowers/specs/b233.13.md` → 退出码 0。
  - `codegraph check --view cards-B233.13-charter --repo .` → 退出码 0（输出含各域边计数）。
- 产出：`docs/superpowers/specs/b233.13-breakdown.md`（单实现范围 `B233.13-impl`，不扇出子卡）。
- 回写：`docs/superpowers/specs/b233.13-contract.md` 追加 §13 修订记录（C-1/D1/D2），§10 加指向。
- 待拍板（本稿 §0.2）：P1 SetManager 注入落点；P2 测试归置；P3 共享助手导出/迁移；P4 DTO gateway 内落点与图归属；P5 typed-nil 防护。
- 未验证（需真机，归协调者）：终态释放崩溃窗口；并发 done/stop 真机时序；迁包后启动恢复；OpenCode 免审回归；平台矩阵。见 breakdown §6。
- 提交：本台账与产出物、contract 修订同批提交；`go build ./...` 已于提交前复核。

- 2026-09-11 协调者拍板 P1–P5 全 A：SetManager 只赋值、测试按归属拆、助手最小导出、DTO 新文件、typed-nil 入口拒绝。
