# B233.17 contract 台账

- 2026-09-12 从功能线 spec `docs/superpowers/specs/b233.17.md`（`def5b3f55`）重开本节点。有效基线 `cards/B233.1-charter-7` @ `94246fc8`；本分支 `cards/B233.17-charter-2`。
- 上游状态位复核：spec 头部「状态：**已批准**（2026-09-12，用户原话「都做」）」，一致，直接引用。
- 目标图 `codegraph/target.json` assembly（功能线读数）：`main.go`、`internal/agentd/server.go`、`internal/agentd/codegraph.go`、`cmd/agentd.go`。
- `codegraph.go` 整文件两个 handler、零插线：`handleProjectCodegraph` `:27`、`handleProjectCodegraphSource` `:123`（170 行）。
- `server.go` 3010 行、21 个 `Server.handle*`（`:889`–`:2333`）；`Handler()` `:637`；`NewServer` `:249`；`SetManager` `:373`；`SetScheduling` `:2753`；`SetupAutomation` `:2673`（`collab.New` `:2687`、`hostapi.NewWithCredentialPathFor` `:2691`、`keystone.New` `:2716`）。
- 四包公开构造：`orchestration.NewManager` `manager.go:291`；`scheduling.New` `scheduling.go:173`；`workspace.NewCapability` `capability_impl.go:17`；`approval.NewClient` `client.go:72`。
- 嵌套 `internal/`：`find internal -type d -name internal` 只有顶层 `internal/`，四包均未封。
- gateway 生产 import 四包里间：0（`grep internal/{orchestration,scheduling,workspace,approval}/internal internal/agentd/` 无命中）。

## 基线滞后查证（B233.13 未 absorb）

- `codegraph/baseline.json` meta commit `9d785ae5`；仍有 104 个节点挂 `internal/agentd/manager.go`（该文件磁盘不存在），`internal/orchestration/` 零节点。
- `best.json` 仍把 `k_agentd_fn`/`k_agentd_model`/`k_agentd_Manager`/`k_agentd_Hub`/`k_store_Store`/`k_store_fn` 归 `d_orchestration`。
- `codegraph check` 基线 fails 6：`dead-interface OrchestrationClient` + 5 条 over-budget（cli→workspace 11/9、gateway→workspace 17/1、orchestration→workspace 49/19、workspace→orchestration 3/2、workspace→protocol 23/3）。
- `d_gateway→d_orchestration` 直调 201 条构成（基线读数，非 assembly caller）：155 `agentd（包级函数）` + 27 `store.Store` + 8 `agentd 实体` + 7 `agentd.Manager` + 3 `store（包级函数）` + 1 `agentd.Hub`；全部为陈旧归属。
- roadmap:163 记 `absorb cards-B233.13-charter` 拒收；spec OOS 明确完整图真重扫不在本卡。

## 拍板（协调者 2026-09-12）

- 否 A（entries 用 `agentd.*`/`store.Store` 旧容器）——给直闯发通行证、棘轮没牙、test-3 变异不红。
- 否 C（保持 321 不降）——用户「都做」含配额到 0，本节点必须把 `d_gateway→d_orchestration` 冻成 budget 0。
- 选 B 语义、手段不是干等重扫：本卡视图补 B233.13 没吸进去的编排符号 + `OrchestrationClient`；entries 用 `orchestration.Manager`/`orchestration（包级函数）`/`orchestration 实体`。check 因基线仍挂 `internal/agentd/manager.go` 多红的部分记「继承 B233.13 未 absorb」，实现期收。
- 工作区五条超限：冻门面口径（具名能力写 entries，直调 budget 0），不把 17/1、49/19 写成合法配额。
- `policy`/`maintenance→transport`：复核后无执行面消费点 → 本卡核实、不动数字。

## 探测（target.json，均配对还原）

- 探测 1（budget 0 + entries 现状旧容器）：`d_gateway->d_orchestration 直调 7 条超出预算 0`（旧容器 entries 豁免后仅余 7 条 `agentd.Manager`），fails 6→7 中的该条仍在；证明旧容器当 entries 会吃掉豁免。
- 探测 2（budget 0 + entries orchestration.*）：新增 3 条 `dead-entry`（orchestration 容器不在基线）+ over-budget 207/0；证明 B 语义需视图补符号，dead-entry 才能消。
- 合成视图探针（临时 view，已删）：仅补容器+节点不补边时，`d_gateway->d_orchestration` 仍 hits 201，`outside-file` 0，`new-direction` 0；确认 `Check` 只经 `best.json` 归域，视图容器需 best 映射才计入。→ 本卡不动 best.json，接受继承滞后。

## 落地

- `codegraph/target.json`：assembly 删 `internal/agentd/codegraph.go`；六条方向 budget 0 + 门面 entries；`policy`/`maintenance→transport` 追加复核 note。roundtrip 无损（`json.dumps(indent=1)` 与原文逐字节一致）。
- 本分支视图 `codegraph/diffs/cards-B233.17-charter-2.json`：base `9d785ae5`，containersAdded 5、nodesAdded 178（全部取自 B233.13 视图 diff 的编排容器/节点），不复制迁出边。
- Ticket 0 守卫 `internal/agentd/b23317_boundary_test.go` 五条：嵌套 internal import 守卫+哨兵、assembly 冻结断言、`b23317HandlerDefsIn` 守卫+哨兵、棘轮预算锁。
- `codegraph resolve --doc docs/superpowers/specs/b233.17-contract.md`：anchors 9、无 file_missing，退出 0。

## 本轮命令与原始输出

- 冻结后 `codegraph check --repo .`（无视图，即 `TestRepoContractGate` 口径）：7 fails（3 dead-entry orchestration.* + dead-interface OrchestrationClient + over-budget gateway→orchestration 207/0、orchestration→workspace 9/0、workspace→orchestration 3/0）；workspace 三条原超限方向（cli→workspace、gateway→workspace、workspace→protocol）已被 entries 覆盖、不再超。
- 冻结后 `codegraph check --view cards-B233.17-charter-2 --repo .`：fails 6（视图补 OrchestrationClient 后 dead-interface 消除；余 3 dead-entry + 3 over-budget，均为继承滞后 + 棘轮目标态）。
- `codegraph validate --view cards-B233.17-charter-2 --repo .`：issues 122，与基线 `validate` 的 122 条逐条相同（零新增）。
- `go build ./...` → 退出码 0（本轮）。
- `go vet ./internal/agentd/` → 退出码 0（本轮）。
- `go test ./internal/agentd/ -run TestB23317 -v` → 5 条 PASS（本轮）。
- `go test ./internal/agentd/ -run 'TestB2331[0-7]'` → ok 5.568s（本轮）。
- 变异（配对还原）：把 `d_gateway→d_orchestration` 的 `legacyBudget` 改回 321，`go test ./internal/agentd/ -run TestB23317FacadeBudgetsStayRatchet` →
  `b23317_boundary_test.go:229: d_gateway→d_orchestration 配额 321 高于冻结值 0——棘轮只降不抬，不许抬配额抹绿`；还原后 ok。
- 提交（历史读数）：`git commit` → 见本批；amend 一次收进同批台账，不回写新 hash。
