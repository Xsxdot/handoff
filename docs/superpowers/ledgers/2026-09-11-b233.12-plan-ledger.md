# B233.12 plan 台账

- 2026-09-11 进入 plan 节点。工作树 `cards/B233.12-charter-2`，`git status` 干净，HEAD `2577493c docs(B233.12): 拍板 breakdown P1-P5`（与 origin/cards/B233.12-charter 一致）。有效基线 `cards/B233.1-charter-7`，本节点不切换、不越过。
- 上游状态核实（文件内）：spec 头部「状态：已批准（2026-09-11）」；contract 头部「冻结状态：随本提交冻结」；breakdown 头部「状态：已拍板（2026-09-11）」。P1=A / P2=A / P3=拆法(i)+计数跟审计 / P4=纳入第二处 / P5=保持 replay，均在 breakdown §0 文件内。
- 读现行代码（本工作树复核，行号为读数）：
  - `internal/approval/client.go`：`Request` AutoAllow 在返回 allow 前调 `Hooks.AutoAllow`（:120-131）；`consult` 在返回 allow 前 `MarkTicketDelivered`（:373-376，待删）；`Acknowledge` 仅 `AckDelivered` 写库（:204-214）；`NoteDeliveryFailed` 跨任务隔离（:231-249）。
  - `internal/agentd/approval_client.go`：`bindApproval` 绑 `AutoAllow: m.autoAllowPermission`（:50）。
  - `internal/agentd/manager.go`：`autoAllowPermission` 审计 + `RespondPermission` + 成功后才 `noteAutoAllowed`（:2302-2350）；`handlePermission` AutoAllow 分支调 `autoAllowPermission`（:2136-2138）；`respondAutoAllowReplay`（:2685）；`approvePermission` 成功后 `markDelivered`（:2639）；`waitPermission` 成功后 `markDelivered`（:2998）；`NoteDeliveryFailed`（:3393）；`takeAutoAllowed`（:2363，汇总消费 :3561）。
  - `internal/executor/opencode/adapter.go`：`authorizeNativePermission`（:817）→ `RespondPermission`（:851）→ 失败无条件 `reportDeliveryFailure`（:853）；`Authorize` 失败 reject 分支（:834-843）；`deliveryFailureReporter` 接口（:148）。
  - `internal/executor/authorize.go`：`Authorize` 返回 `ApprovalDecision`，丢弃 `ApprovalResult.Ref`（:16-31）；`mapGateAnswer` 对人工 allow 返回 `Rule==""`。
  - `internal/store/store.go`：`MarkTicketDelivered` 幂等 `WHERE id=? AND delivered_at IS NULL`（:1189）；`FindReusableGrant` 要求 `answer='allow' AND delivered_at IS NOT NULL`（:1161）；`UndeliveredAnswers`（:1211）。
- 基线命令与原始输出（本节点亲自运行，当前工作树未做代码改动）：
  - `go build ./internal/approval ./internal/executor ./internal/executor/opencode ./internal/agentd ./internal/store` → `build_exit=0`。
  - `go test ./internal/approval -run 'TestClient(ConsultAllowPersistsGrant|AutoAllowHasNoTicket|NoteDeliveryFailedTaskIsolation|ReuseRequiresDeliveredGrant|AcknowledgeStages)' -count=1` → `ok  	github.com/Xsxdot/handoff/internal/approval	0.363s`。
  - `go test ./internal/executor/opencode -run 'Test(AuthorizeNativePermissionReportsDeliveryFailure|StartToPermissionFlow)' -count=1` → `ok  	github.com/Xsxdot/handoff/internal/executor/opencode	0.020s`。
  - `go test ./internal/agentd -run 'Test(SafeCommandPermissionAuditsOnceWithoutTicket|AnsweredTicketReplayStillResponds|ApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses|ApproverApprovesPermissionWithoutWaking|ApprovePermissionAdapterForFailureNotesDeliveryFailed)' -count=1` → `ok  	github.com/Xsxdot/handoff/internal/agentd	0.559s`。
- 关键事实（写进计划）：`mockApprovalClient.Request`（`adapter_test.go:1775`）现状默认返回 `ApprovalDecision{Status: Allow}`（Rule 为空）并期望 `TestAuthorizeNativePermissionReportsDeliveryFailure` 上报一次。P1=A 的闸门（Deny 或 Rule approver/reuse 才上报）会让该测试假红，故实现计划把该 fake 缺省改为 `Rule:"approver"` 并转成可配置 decision。
- 关键事实（写进计划）：`mapGateAnswer` 对人工 allow 返回 `Rule==""`，与范围内自动放行（perm gate `judgeFileWrite` 返回 `AutoAllow` 且 Rule 为空）同形；P1 按 allow-list 判定必然让人工放行失败不再上报 `delivery_failed`。已在计划 §8 记录为 P1 取舍，冻结 #11/#12 不覆盖人工放行，故不违冻结。
- 出稿：`docs/superpowers/plans/b233.12-plan.md`。内容：4 个 task（Task1 审批 client 送达收口 / Task2 agentd 拆分+组装 / Task3 adapter 闸门 / Task4 非 OpenCode 回归与整卡审计）、有界文件集、基线判据、红绿步骤、双向接缝表、序列化边界、缺陷族逐族、真机清单、覆盖矩阵、派发前自审。
- 本节点不写实现代码、不建脚手架、不调用 handoff CLI、不派发、不切换分支。
- 提交（历史读数）：`git add docs/superpowers/plans/b233.12-plan.md docs/superpowers/ledgers/2026-09-11-b233.12-plan-ledger.md && git commit -m "docs(B233.12): 实现计划（审批入站收口，P1-P5 落实）"` → `[cards/B233.12-charter-2 c222fcae] docs(B233.12): 实现计划（审批入站收口，P1-P5 落实）`；`2 files changed, 779 insertions(+)`。随后 amend 一次收本行。

## implement 节点（自 `cards/B233.12-charter-3` @ `c806a5d7` 续接）

- 2026-09-11 进入 implement。`git status` 干净，HEAD `c806a5d7 docs(B233.12): 实现计划（审批入站收口，P1-P5 落实）`，分支 `cards/B233.12-charter-3`（与指令「从 cards/B233.12-charter-2 @ c806a5d7 续接」所指提交一致）。

### Task 1 审批 client 送达确认收口（S2）

- 先改测试锁新时序（红）：
  - `TestClientConsultAllowPersistsGrant` 整体改写为 `Request` 后 `answer=allow`、`DeliveredAt==nil`、`FindReusableGrant` 未命中，`Acknowledge(AckDelivered)` 后才送达并复用。
  - `TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses` 的 `DeliveredAt` 断言反向 + `FindReusableGrant` 未命中 + req2 前显式 `Acknowledge(AckDelivered)`。
- 跑红原始输出（两处均命中预期文案）：
  - `go test ./internal/approval -run 'TestClientConsultAllowPersistsGrant' -count=1` → `--- FAIL: TestClientConsultAllowPersistsGrant`，`client_test.go:432: Request 返回 allow 时不得已送达（DeliveredAt 必须为空）: &{... DeliveredAt:2026-09-11 04:51:36.673644386 +0000 UTC ...}`；`FAIL github.com/Xsxdot/handoff/internal/approval 0.070s`。
  - `go test ./internal/agentd -run 'TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses' -count=1` → `--- FAIL: TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses`，`approval_client_test.go:560: Request 返回 allow 时不得已送达（DeliveredAt 必须为空）`；`FAIL github.com/Xsxdot/handoff/internal/agentd 0.092s`。
- 最小实现：删除 `internal/approval/client.go#Client.consult` 的提前 `MarkTicketDelivered` 块；日志改「审批者批准决定已落库，尚未送达」；补 boundary 注释；`Client.Request` AutoAllow 分支补注释（不改行为）。
- 跑绿：`go test ./internal/approval -run 'TestClientConsultAllowPersistsGrant' -count=1` → `ok github.com/Xsxdot/handoff/internal/approval 0.071s`；`go test ./internal/agentd -run 'TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.069s`。
- 基线回归：`go test ./internal/approval -run 'TestClient(ConsultAllowPersistsGrant|AutoAllowHasNoTicket|NoteDeliveryFailedTaskIsolation|ReuseRequiresDeliveredGrant|AcknowledgeStages)' -count=1` → `ok github.com/Xsxdot/handoff/internal/approval 0.398s`。
- 反向变异（加回 `MarkTicketDelivered`）：先确认编译并跑行为断言，两测试均红（`--- FAIL`），随后还原。变异命中唯一、编译通过、行为改变，测试有牙。

### Task 2 agentd 拆分 AutoAllow 审计与回传、组装只绑审计（S3/R1）

- 新增生产侧接缝测试 `TestApprovalClientAutoAllowProductionHookDoesNotRespond`（经生产 `bindApproval`，断言 `adapter.recordedPerms()` 为空、`permission_auto_allow` 恰 1、无工单、`takeAutoAllowed`==1）。
- 跑红：`go test ./internal/agentd -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1` → `--- FAIL`，`approval_client_test.go:661: 生产 AutoAllow 钩子不得回传原生应答，实得 [native-auto-prod:once]`。
- 最小实现：新增 `m.auditAutoAllowOnly`（审计 + `noteAutoAllowed`）；`m.autoAllowPermission` = `auditAutoAllowOnly` + 一次回传；`bindApproval` 的 `Hooks.AutoAllow` 改指 `m.auditAutoAllowOnly`（附注释）。`respondAutoAllowReplay` 未改（P5）。
- 跑绿：`go test ./internal/agentd -run 'Test(ApprovalClientAutoAllowProductionHookDoesNotRespond|...7 项...)' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.806s`。
- 反向变异 1（`bindApproval` 改回 `m.autoAllowPermission`）：`TestApprovalClientAutoAllowProductionHookDoesNotRespond` 红，随后还原。
- 反向变异 2（`autoAllowPermission` 回传 decision 由 `once` 改 `reject`）：`TestSafeCommandPermissionAuditsOnceWithoutTicket` 红（`responded permissions = [safe-1:reject], want [safe-1:once]`），随后还原。
- 回归确认：7 项命令 → `ok github.com/Xsxdot/handoff/internal/agentd 0.770s`。

### Task 3 OpenCode adapter 免审失败不上报、按决定闸门（S1）

- 改 `fakeApprovalClient`：加 `decision` 字段 + `setDecision`，`Request` 按配置返回，零值缺省 `{Allow, Rule:"approver"}`；新增 `ackStagePresent` helper；新增 `TestAuthorizeNativePermissionAllowDeliversOnce`（allow 成功恰一次 + `AckDelivered` 在成功之后 + 零上报）与 `TestAuthorizeNativePermissionAutoAllowFailureDoesNotReport`（免审失败零上报、无 `AckDelivered`/`AckExecuted`）。
- 跑红：`go test ./internal/executor/opencode -run 'Test(AuthorizeNativePermissionAutoAllowFailureDoesNotReport|AuthorizeNativePermissionAllowDeliversOnce)' -count=1` → `--- FAIL: TestAuthorizeNativePermissionAutoAllowFailureDoesNotReport`，`adapter_test.go:1976: 免审 AutoAllow 无工单，失败不得上报 delivery_failed: [{...}]`；`AllowDeliversOnce` 绿（既有成功路径已恰一次）。
- 最小实现：`internal/executor/opencode/adapter.go` 新增 `deliveryFailureHasTicket(dec)`；`authorizeNativePermission` 终态回传失败按闸门分叉（有工单上报、无工单 Warn 不产事件）；`Authorize` 失败 reject 分支加注释、保持既有上报。
- 跑绿：`go test ./internal/executor/opencode -run 'Test(AuthorizeNativePermissionReportsDeliveryFailure|AuthorizeNativePermissionAllowDeliversOnce|AuthorizeNativePermissionAutoAllowFailureDoesNotReport|StartToPermissionFlow)' -count=1` → `ok github.com/Xsxdot/handoff/internal/executor/opencode 0.160s`。
- 反向变异 1（`deliveryFailureHasTicket` 改恒真 `|| true`）：`TestAuthorizeNativePermissionAutoAllowFailureDoesNotReport` 红，随后还原。
- 反向变异 2（allow 回传 decision `once` 改 `reject`）：`TestAuthorizeNativePermissionAllowDeliversOnce` 红（`应答体="{\"response\":\"reject\"}"，应含 once`），随后还原。

### Task 4 非 OpenCode 兼容回归与整卡接缝审计（无生产改动）

- 兼容回归：`go test ./internal/agentd -run 'Test(SafeCommandPermissionAuditsOnceWithoutTicket|...7 项...)' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 1.024s`；`go test ./internal/approval -run 'TestClient(AutoAllowHasNoTicket|ReuseRequiresDeliveredGrant|AcknowledgeStages|NoteDeliveryFailedTaskIsolation|ConsultFailClosed)' -count=1` → `ok github.com/Xsxdot/handoff/internal/approval 0.473s`。
- 编译面：`go build ./internal/approval ./internal/executor ./internal/executor/opencode ./internal/agentd ./internal/store` → `build_exit=0`；`go vet ./internal/approval ./internal/executor/opencode ./internal/agentd` → `vet_exit=0`；`go build ./...` → `full_build_exit=0`。
- 卡级汇总：`go test ./internal/approval ./internal/executor/opencode` → `ok .../approval 0.666s`、`ok .../opencode 18.474s`。`go test ./internal/agentd -count=1` → `FAIL github.com/Xsxdot/handoff/internal/agentd 225.810s`，唯一失败 `TestLegacyNodeEventSequenceUnchanged`。
- 关键事实：`TestLegacyNodeEventSequenceUnchanged`（`internal/agentd/scheddispatch_test.go`）与审批/投递无关；`git stash` 后在干净基线 `c806a5d7` 上单独运行仍同样红（got `dispatched`，want `comment`），确认为存量失败，非本卡引入，不在本卡文件集内。
- 双向接缝审计（§8.3）：S1=`TestAuthorizeNativePermissionAllowDeliversOnce`+`TestAuthorizeNativePermissionAutoAllowFailureDoesNotReport`；S2=`TestClientConsultAllowPersistsGrant`+`TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses`；S3=`TestApprovalClientAutoAllowProductionHookDoesNotRespond`；R1=`TestSafeCommandPermissionAuditsOnceWithoutTicket`。每条缝至少一支真实入口断言，全部绿。
- 收口自检：`git diff --check` → `diff_check_clean=1`；占位语句扫描 `grep -nE 'TBD|TODO|加适当的错误处理|同 Task' docs/superpowers/plans/b233.12-plan.md` 仅命中计划 §11 自述那一行（自引用），无真实占位。
- 提交（历史读数）：`git add <8 个文件>... && git commit -m "feat(B233.12): 审批入站收口，Hooks 不再投递、送达只在原生回传成功后"` → `[cards/B233.12-charter-3 05e58e88] ...`；`8 files changed, 271 insertions(+), 27 deletions(-)`。随后 amend 一次收本行。
