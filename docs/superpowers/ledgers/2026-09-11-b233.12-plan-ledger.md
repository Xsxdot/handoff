# B233.12 plan 台账

- 2026-09-11 进入 plan 节点。工作树 `cards/B233.12-charter-2`，`git status` 干净，HEAD `2577493c docs(B233.12): 拍板 breakdown P1-P5`（与 origin/cards/B233.12-charter 一致）。有效基线 `cards/B233.1-charter-7`，本节点不切换、不越过。
- 上游状态核实（文件内）：spec 头部「状态：已批准（2026-09-11）」；contract 头部「冻结状态：随本提交冻结」；breakdown 头部「状态：已拍板（2026-09-11）」。P1=A / P2=A / P3=拆法(i)+计数跟审计 / P4=纳入第二处 / P5=保持 replay，均在 breakdown §0 文件内。
- 读现行代码（本工作树复核，行号为读数）：
  - `internal/approval/client.go`：`Request` AutoAllow 在返回 allow 前调 `Hooks.AutoAllow`（:120-131）；`consult` 在返回 allow 前 `MarkTicketDelivered`（:373-376，待删）；`Acknowledge` 仅 `AckDelivered` 写库（:204-214）；`NoteDeliveryFailed` 跨任务隔离（:231-249）。
  - `internal/agentd/approval_client.go`：`bindApproval` 绑 `AutoAllow: m.autoAllowPermission`（:50）。
  - `internal/agentd/manager.go`：`autoAllowPermission` 审计 + `RespondPermission` + 成功后才 `noteAutoAllowed`（:2302-2350）；`handlePermission` AutoAllow 分支调 `autoAllowPermission`（:2136-2138）；`respondAutoAllowReplay`（:2685）；`approvePermission` 成功后 `markDelivered`（:2639）；`waitPermission` 成功后 `markDelivered`（:2998）；`NoteDeliveryFailed`（:3393）；`takeAutoAllowed`（:2363，汇总消费 :3561）。
  - `internal/executor/opencode/adapter.go`：`authorizeNativePermission`（:817）→ `RespondPermission`（:851）→ 失败无条件 `reportDeliveryFailure`（:853）；`Authorize` 失败 reject 分支（:834-843）；`deliveryFailureReporter` 接口（:148）。
  - `internal/executor/authorize.go`：`Authorize` 返回 `ApprovalDecision`，丢弃 `ApprovalResult.Ref`（:16-31）；`mapGateAnswer` 对人工 allow 返回 `Rule==""`。
  - `internal/store/store.go`：`MarkTicketDelivered` 幂等 `WHERE id=? AND delivered_at IS NULL`（:1189）；`FindReusableGrant` 要求 `answer='allow' AND delivered_at IS NOT NULL`（:1161）；`UndeliveredAnswers`（:1211）。
- 基线命令与原始输出（本节点亲自运行，当前工作树未做代码改动）：
  - `go build ./internal/approval ./internal/executor ./internal/executor/opencode ./internal/agentd ./internal/store` → `build_exit=0`。
  - `go test ./internal/approval -run 'TestClient(ConsultAllowPersistsGrant|AutoAllowHasNoTicket|NoteDeliveryFailedTaskIsolation|ReuseRequiresDeliveredGrant|AcknowledgeStages)' -count=1` → `ok  	github.com/Xsxdot/handoff/internal/approval	0.363s`。
  - `go test ./internal/executor/opencode -run 'Test(AuthorizeNativePermissionReportsDeliveryFailure|StartToPermissionFlow)' -count=1` → `ok  	github.com/Xsxdot/handoff/internal/executor/opencode	0.020s`。
  - `go test ./internal/agentd -run 'Test(SafeCommandPermissionAuditsOnceWithoutTicket|AnsweredTicketReplayStillResponds|ApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses|ApproverApprovesPermissionWithoutWaking|ApprovePermissionAdapterForFailureNotesDeliveryFailed)' -count=1` → `ok  	github.com/Xsxdot/handoff/internal/agentd	0.559s`。
- 关键事实（写进计划）：`mockApprovalClient.Request`（`adapter_test.go:1775`）现状默认返回 `ApprovalDecision{Status: Allow}`（Rule 为空）并期望 `TestAuthorizeNativePermissionReportsDeliveryFailure` 上报一次。P1=A 的闸门（Deny 或 Rule approver/reuse 才上报）会让该测试假红，故实现计划把该 fake 缺省改为 `Rule:"approver"` 并转成可配置 decision。
- 关键事实（写进计划）：`mapGateAnswer` 对人工 allow 返回 `Rule==""`，与范围内自动放行（perm gate `judgeFileWrite` 返回 `AutoAllow` 且 Rule 为空）同形；P1 按 allow-list 判定必然让人工放行失败不再上报 `delivery_failed`。已在计划 §8 记录为 P1 取舍，冻结 #11/#12 不覆盖人工放行，故不违冻结。
- 出稿：`docs/superpowers/plans/b233.12-plan.md`。内容：4 个 task（Task1 审批 client 送达收口 / Task2 agentd 拆分+组装 / Task3 adapter 闸门 / Task4 非 OpenCode 回归与整卡审计）、有界文件集、基线判据、红绿步骤、双向接缝表、序列化边界、缺陷族逐族、真机清单、覆盖矩阵、派发前自审。
- 本节点不写实现代码、不建脚手架、不调用 handoff CLI、不派发、不切换分支。
- 提交（历史读数）：`git add docs/superpowers/plans/b233.12-plan.md docs/superpowers/ledgers/2026-09-11-b233.12-plan-ledger.md && git commit -m "docs(B233.12): 实现计划（审批入站收口，P1-P5 落实）"` → `[cards/B233.12-charter-2 c222fcae] docs(B233.12): 实现计划（审批入站收口，P1-P5 落实）`；`2 files changed, 779 insertions(+)`。随后 amend 一次收本行。
