# B233.2 contract 台账

日期：2026-09-07。节点：charter contract。工作分支：`cards/B233.2-charter`。开工 HEAD：`124121e4`。

## 过程

1. 核对上游 spec `docs/superpowers/specs/b233.2.md` 头部状态行 = **已批准**（2026-09-06，用户原话「按这个推进吧」；并纠正迁全部现有 agent）。与 prompt 一致，未回写。
2. 定级 L3 轻档：无直通竖切；Ticket 0 只落空壳、类型、编码函数与金样本。
3. 查图：`codegraph summary` / `domains` / `context d_execution_contract` / `sym Adapter` / `who-calls OneShotArgs` / `who-calls Install` / `who-calls n_hostapi_Host_RunTurn`。基线无 `ApprovalClient`（仍只在 `cards-B233.1-charter-2` 视图）；`copyCoordinatorRules` 短名不在图中。
4. 基线代码事实已对照：`internal/executor/executor.go#Adapter`、`oneshot.go#OneShotArgs`、`hostapi.go#Host.RunTurn`、`driver.go#supportedCLIs`、`keysclient.go#Runner`、`agentd/server.go#coordinatorRunner`、`coordinator_home.go#Prepare`/`copyCoordinatorRules`、`skill/install.go#Install`/`agentDirs`、`approver.go#Decide`、`manager.go#restorer`、`toolchain/detect.go#credRelPath`、`cmd/agentd.go` 组装表。
5. 架构法第一条：不新立 `d_harness`；能力接口落 `d_execution_contract`。hostapi 仍归 `d_execution_host`。不提前写 `d_keystone→d_execution`（dead-contract）。
6. Ticket 0：`capability.go` / `coordinator.go` / `oneshot_cap.go` / `profile.go` / `skills_cap.go` / `recoverer.go`；`grok.OneShotArgv`；五家 `var _ Recoverer = (*Adapter)(nil)`。
7. 契约文档写到法定路径 `docs/superpowers/specs/b233.2-contract.md`。视图 `codegraph/diffs/cards-B233.2-charter.json`。target.json 仅给既有 `d_orchestration→d_execution` 补 B233.2 注记。

## 命令读数

- `gofmt -w` 触及的 Go 文件：exit 0。
- `go build -o /dev/null .`：exit 0。
- `go test ./internal/executor/ ./internal/executor/grok/ ... ./internal/skill/ -count=1 -timeout 180s -run 'TestHarnessNameLiterals|...|TestAgentDirsContract'`：相关包 `ok`，exit 0。
- `go test ./internal/agentd/ -count=0`：`ok ... [no tests to run]`，exit 0。
- `go test ./internal/executor/... -count=0`：九包 `ok`，exit 0。
- `codegraph --repo . validate --view cards-B233.2-charter`：exit 0，`issues: null`。
- `codegraph --repo . resolve --doc docs/superpowers/specs/b233.2-contract.md --view cards-B233.2-charter`：exit 0。新符号 `Recoverer` / `Coordinator` / `OneShotArgv` 为 `anchor=ok`。存量符号部分 `moved`（行号相对基线漂移），无 vanished / file_missing。
- `codegraph --repo . check --view cards-B233.1-charter-2`：`fails []`。
- `codegraph --repo . check --view cards-B233.2-charter`：exit 1，2 条 fail 与无视图相同（`ApprovalClient` 只在 233.1 视图）。本卡未新开死契约。`d_orchestration->d_execution` 仍 26/27。

## 判断

- 能力契约在提供方（executor 包），不按第九条拆到各消费方。
- 不另立 Executor 接口。
- grok effort 是调用方 Limits；argv 编码留在 grok 包。
- 三重闸门六条写入契约 §8。
- 欠账见契约 §9：无「已实现但零测试」。

## 提交

```
git commit -m "contract(B233.2): 冻结 harness 五类能力面契约"
[cards/B233.2-charter aeff84c6] contract(B233.2): 冻结 harness 五类能力面契约
 26 files changed, 2740 insertions(+), 3 deletions(-)
 create mode 100644 codegraph/diffs/cards-B233.2-charter.json
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.2-contract-ledger.md
 create mode 100644 docs/superpowers/specs/b233.2-contract.md
 create mode 100644 internal/executor/capability.go
 create mode 100644 internal/executor/coordinator.go
 create mode 100644 internal/executor/oneshot_cap.go
 create mode 100644 internal/executor/profile.go
 create mode 100644 internal/executor/skills_cap.go
 create mode 100644 internal/executor/recoverer.go
 create mode 100644 internal/executor/grok/oneshot_argv.go
```

随后 amend 一次把本段收进同批提交。不回填 amend 后 HEAD。


