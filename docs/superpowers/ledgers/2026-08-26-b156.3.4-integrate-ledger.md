# B156.3.4 集成节点台账（claude/kai-156-3-ba-df7357）

> 卡 B156.3.4 ｜ 隔离集成工作树 `/root/.handoff/tmp/b71e4517/integ-b15634`
> （检出承载分支 claude/kai-156-3-ba-df7357）执行合并与门禁；执行器工作树
> /root/.handoff/worktrees/b71e4517 与协调者主工作树 /root/.handoff/repos/handoff
> 未触碰，未 push。每条读数均为本节点亲跑；未跑到的明写「未验证」。

## 开工核对

- 基线：本地分支 `claude/kai-156-3-ba-df7357` = `3e5371b4`（B156.3.3 集成台账顶，
  即本卡 effective_base_branch 当前位置）。
- 目标：`cards/B156.3.4-charter-3` = `75607559`（Task D 收尾台账提交；charter-4 与
  执行器工作树同指此提交，断机恢复链 charter-2/3/4 三 ref 同指，零丢失）。
- **排除项核对**：`git merge-base --is-ancestor b738db70 75607559` → **NO**——
  acceptance probe 的证据台账提交不在目标谱系，合并不会把它当业务实现带入 ✓。
- `git merge-base 3e5371b4 75607559` = `2b5dd44a`（K3 修复轮2）；基线不是目标的祖先
  （is-ancestor → NO）→ 真实合并，非快进；待并入提交
  `git rev-list --count 3e5371b4..75607559` = **11**。
- 待并 diff（基线→目标）：14 文件 +2232/−134；其中 `.go` 共 **9** 个
  （cmd/card.go、cmd/card_coordinate.go、cmd/card_coordinate_test.go、
  internal/agentd/coordapi.go、internal/agentd/coordapi_test.go、
  internal/agentd/server.go、internal/client/coordinator.go、
  internal/keystone/keystone.go、internal/keystone/keystone_spec_test.go）——
  与 plan「有界文件集」逐一对上，无越界文件。

## 合分支

- 真实 `git merge --no-ff 75607559 -m "merge(B156.3.4): K4 协调者生命周期接线——launch/status/attach 三端点+开卡即绑+attach互斥（launchRound 消费 spec）"`
  → exit 0，ort 策略，**冲突 0**、手工改文件 0。
- 合并提交 **55ab673c**，parents = (`3e5371b4`, `75607559`)。
- 树一致性：merged tree `fc215e3d` ≠ 目标 tree `b6a64f45`，差异**仅**基线侧两份文档
  （docs/roadmap.md +10、docs/superpowers/ledgers/2026-08-26-b156.3.3-integrate-ledger.md
  +117）；`git diff 75607559 HEAD -- '*.go'` = **0 行** → 9 个代码文件与已验收目标
  逐字节相同，零第三方混入。
- `git diff --check` → 无输出，exit 0。
- 合并后再核：`git merge-base --is-ancestor b738db70 HEAD` → **NO**（acceptance
  台账提交确认未混入集成分支）✓。

## 全量门禁（三档读数）

1. `go build ./...` → **exit 0**；`go vet ./...` → **exit 0**（各自单独落码）。
2. `go test ./... -count=1` 两读数：
   - 默认环境档（TMPDIR=/root/.handoff/tmp/b71e4517，检出位于其下）：exit **1**，
     50 包 ok，唯一红 `TestResolveServiceBinFallsBackFromGoBuildCache`（cmd 包）。
     分栏：**本卡引入的红 = 0；基线既有的红 = 1**。基线对照命令与读数：另开
     detached 检出（`git worktree add --detach /root/.handoff/tmp/b71e4517/baseline-wt
     3e5371b4`）同位置复跑 `go test ./cmd/ -run
     TestResolveServiceBinFallsBackFromGoBuildCache -count=1` → **同样 FAIL，exit 1**
     ——环境诱发（检出路径在 os.TempDir() 下）、基线既有，非本卡引入（B156.3.3
     集成轮已定位并落 roadmap 残余账本）。
   - 权威档 `TMPDIR=/tmp/handoff-itest go test ./... -count=1` → **exit 0，
     51 包 ok / 0 FAIL**（agentd 139.824s、cmd 11.852s、scheduling 0.714s、
     client 9.406s、keystone 0.137s、ledger 19.209s）。六支 TestCoord 在 agentd
     包内全绿。
3. `gofmt -l <9 个本轮改动 .go>`（git diff --name-only 3e5371b4 HEAD -- '*.go' 展开）
   → 输出为空、exit 0。**正控**：故意写坏格式的探针文件 cmd/zz_badfmt_probe.go
   → `gofmt -l` 打印该文件名（证明命令真的在看）→ 删除探针，工作树回到 clean。

## 契约对照（graph check 双读数）

- 改前（基线 3e5371b4，detached 检出）：`go run . graph check --repo .` →
  `"fails": []`（fails=0）、warns=101，exit 0。
- 改后（合并树 55ab673c）：同命令 → `"fails": []`（fails=0）、warns=101，exit 0。
- warns 集合逐条对照：**new warns 0 / gone warns 0**（101 条全等）→ legacy 直调
  计数零变化、零新增容器边、**集成阶段契约错配 = 0**。
- 视图对照：`go run . graph views` 改前/改后均为
  `["cards-B156.2-charter-4","cards-B192-charter","cards-B205-charter"]` →
  **changed views：无**；分支视图 diff 无，fails/warnings 增量均无。

## 接缝缺陷记账

- **集成阶段新发现缺陷数 = 0**：合并零冲突；权威全量档绿；图闸双侧 fails 清零且
  warns 全等。

## review 核销项登记（本轮不修业务代码）

按协调者指示，review 的 1 major + 3 minor 全部作为**后续核销项**记录，本轮一律不动
业务代码。原文锚点在 b738db70 acceptance 台账 §7（该提交不入集成分支），故在此与
docs/roadmap.md「来自 B156.3.4 集成轮」节**双落痕防孤儿**：

1. **major**｜失败拉起后两级名额永久占用且 409 文案双重误导：生产 launch 路径无
   Release 臂（LaunchAdmit 成功→LaunchForCard 失败→502，名额挂回合终局而失败拉起
   无回合可终局；文案坐标 internal/agentd/coordapi.go:134-135）→ 建议 K5：修 409
   文案＋立名额归还验收条目。
2. **minor**｜GET /coordinator 的 attach 三元组 machine/dir 恒空串（coordapi.go:206
   `Locate(id, "")` 数据来源）→ K6 镜像时核销。
3. **minor**｜handleCoordAttach 错误分支无测试锁：active 缺失 400／定位失败回滚／
   ?machine= 转发三条均无断言。
4. **minor**｜Task 0 遇 K3 未并入直接 merge 而非按 plan 提问（结果正确零冲突；
   流程偏差记账）。

## 回旋镖对账（plan/implement 预测 vs 实测）

| 预测 | 实测 | 结论 |
|---|---|---|
| implement Task D 全量 51 ok / FAIL 0 | 权威档 51 ok / 0 FAIL | 成立 |
| 图闸 fails=0（implement Task D 读数） | 合并树 fails=0，warns 与基线全等 | 成立 |
| 有界文件集（plan §有界文件集 9 个 .go） | 合并 diff 的 .go 与目标逐字节相同、无越界 | 成立 |
| 「K4 只加新文件+两处既有文件小改」→ 可零冲突 | 实测零冲突 | 成立 |
| 耗时 | plan 未给小时级预测（如实记）；集成轮一次通过、零返工 | — |

## 问一/问二增量核（集成轮自身）

- **集成轮新增导出符号 = 0**：合并对 `.go` 的净效果 = 目标树原样搬运
  （`git diff 75607559 HEAD -- '*.go'` 0 行）；本轮新增文件仅本文档与 roadmap 一节，
  均非 .go。配对原语消费关系维持 implement 轮读数不变
  （CoordinatorLaunchAs 生产命中 3 行含真实调用点 1 处）。
- 新增依赖方向 = **0**（warns 101 条改前/改后全等佐证，legacy 边无一增减）。

## 棘轮

- warns 全等 → 无任何接缝存量直调下降，无可提请下调项；本次变更集不做预算调整。

## 交棒（集成完成 ≠ 完成）

- **下一步：acceptance**。机内三档门禁与图闸本轮已全部亲跑（读数见上）；真机腿
  （四跳拉起、attach 端到端、Windows 条件项等 breakdown §十五 真机清单）**本轮一条
  都未执行**，登记为未执行，归协调者 acceptance 阶段。
- 并入 main、归档收口 = 协调者裁决；本分支不 push、不开 PR。
- review 四条核销项（上节）随本卡移交，K5/K6 派卡时应核销 major 与 minor②。

## 收尾自查

① 有没有把没亲自跑到结果的命令写成结论？——三档读数、基线对照、图对照双读数均为
   本节点实跑并留原始输出（/root/.handoff/tmp/b71e4517/ 下 fulltest-default-tmpdir.log、
   fulltest-neutral-tmpdir.log、baseline-redcheck.log、graph-{baseline,merged}.json、
   views-{baseline,merged}.txt）；真机清单明标未执行。
② 本轮碰过 handoff CLI 写操作或起过新 executor 吗？——没有；仅
   `go run . graph check/views`（只读本地图数据，平台不变量允许），无 dispatch/wait/
   reply，无子任务。
