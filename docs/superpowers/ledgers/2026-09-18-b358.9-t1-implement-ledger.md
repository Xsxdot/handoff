# B358.9 T1 实现台账（gateway 端点换脸 + fail-closed + 端戳 + owner 缺省）

节点：Implement（本回合只做 plan §4 的 T1；T2–T4 留后续回合）。
计划：`docs/superpowers/plans/b358.9-plan.md` §4。
契约：`docs/superpowers/specs/b358.9-contract.md`。
卡：B358.9　有效基线：`cards/B233.1-charter-7` @ `94246fc8`（merge-base 实读）。
工作分支：`cards/B358.9-charter-7`。
角色：实现节点，法定产出为 T1 有界文件集内代码改动。一行一条历史读数；命令与原始输出照抄。

## 0. 开工状态位

1. 2026-09-18：`git branch --show-current` → `cards/B358.9-charter-7`；`git status --porcelain` → 无输出。
2. 2026-09-18：`git merge-base HEAD cards/B233.1-charter-7` → `94246fc8a2acc7996c254cb6b473c7c9b5aef225`（= 计划 pin）。
3. 2026-09-18：`git rev-parse HEAD` → `71050444365b0f91a4eceaa676f4f56acc7056f5`（= plan 提交）。
4. 2026-09-18：`which codegraph` → `/usr/local/bin/codegraph`（恒在层要求已满足，未自行安装）。

## 1. 基线命令复核（§4.4 验收前置）

5. 2026-09-18：`go build ./...` → 退出码 `0`，无输出。
6. 2026-09-18：`go test ./internal/agentd/ -count=1 -run 'TestConsoleFace|...'`（红跑前置，见 §2）随写测试执行。

## 2. 先写失败测试（红）

7. 2026-09-18：新建 `internal/agentd/identity_face_test.go`（6 支：换脸+端戳、未配名全端点 fail-closed、
   非法名、owner 缺省、多端已读合并、源码零 `"web:"` 守卫）。
8. 2026-09-18：夹具换脸：`roomsapi_test.go` 常量 `webUserMember="web:127.0.0.1"` → `consoleMember="user:sycm"`；
   全文替换标识符；`newRoomsEnv` 末尾 `srv.cfg.Store` 覆写 `ConsoleUser="sycm"`；
   `TestRoomSendEndpoint` 断言 `HasPrefix(ev.Actor,"web:")` → `ev.Actor != consoleMember`。
9. 2026-09-18：`sessionsapi_test.go` 全文替换 `webUserMember` → `consoleMember`；`TestSessionsCreateEndpoint`
   「owner 缺失 → 400」改为「缺省 → 200 且 owner == consoleMember」。
10. 2026-09-18：首跑编译红（辅助函数签名不匹配，非功能红，先修）：

```
internal/agentd/identity_face_test.go:102:36: cannot use env.testAgentdEnv (variable of type *testAgentdEnv) as *httptest.Server value in argument to ledgerPostCookie
internal/agentd/identity_face_test.go:229:39: cannot use env.testAgentdEnv (variable of type *testAgentdEnv) as *httptest.Server value in argument to ledgerPostCookie
internal/agentd/identity_face_test.go:234:35: cannot use env.testAgentdEnv (variable of type *testAgentdEnv) as *httptest.Server value in argument to ledgerGetCookie
FAIL	github.com/Xsxdot/handoff/internal/agentd [build failed]
```

11. 2026-09-18：改用 `env.ts` 后断言红原文（旧脸/未拒收/owner 必填，全部为「功能缺失」）：

```
--- FAIL: TestConsoleFaceActorAndDeviceStamp (0.21s)
    identity_face_test.go:104: cookie 发言应 200: 403 {"error":"collab: 书写者与房间身份不符"}
--- FAIL: TestConsoleFaceFailClosedWithoutName (0.20s)
    identity_face_test.go:160: POST /api/rooms/session:1/messages 拒收文案应含 console_user（可行动）: {"error":"collab: 书写者与房间身份不符"}
--- FAIL: TestConsoleFaceRejectsIllegalName (0.16s)
    identity_face_test.go:176: 非法 console_user 必须 fail-closed 403+文案: 403 {"error":"collab: 书写者与房间身份不符"}
--- FAIL: TestSessionCreateOwnerDefaultsToConsoleIdentity (0.20s)
    identity_face_test.go:186: 缺 owner 已配名应 200 且缺省为人名: 400 {"error":"owner 必须是统一记法 user:\u003cname\u003e 或 agent:\u003cname\u003e"}
--- FAIL: TestConsoleFaceNoWebFallbackSourceGuard (0.00s)
    identity_face_test.go:261: roomsapi.go 仍有旧传输层脸字面量 "web:"（应已换脸为 resolveConsoleIdentity）
FAIL
```

（`TestConsoleReadMergesAcrossDevices` 未单列红——它在整支跑中被同一批影响；红跑命令见下。）

## 3. 最小实现

12. 2026-09-18：`roomsapi.go`：删除 `roomUserActor`，新增 `requireConsoleIdentity`（未配名写 403 +
    文案含 `console_user`，日志 Warn 带 method/path）；`handleRoomsList`、`handleRoomSend`
    （装填 `Device: id.Device`）、`handleRoomRead`、`handleInbox` 过身份门并改用 `id.Member`。
13. 2026-09-18：`sessionsapi.go`：`validSessionOwner` 收敛为 `proto.ValidateMemberIdentity`
    （import 增 `internal/proto`）；`handleSessionCreate` owner 缺省=`id.Member`、显式仍校验；
    `handleSessionMemberAdd`/`handleSessionsList`/`handleSessionArchive`/`handleSessionJoinCard`/
    `handleSessionLeaveCard` 各过身份门并改用 `id.Member`。
14. 2026-09-18：身份门覆盖清单（显式豁免）：需身份=写面 send/read/create/member add/archive/join/leave
    与读面 rooms list/sessions list/inbox；**不加门**=`handleRoomMessages`（历史）、`handleSessionDetail`
    （会话详情）——无 member 维度、无未读投影，契约 §8.1 与 P-2 未点名，不以「一致性」扩面。
15. 2026-09-18：更新 `identity.go` 头注释（旧 `roomUserActor` 欠账已清）与 `sessionsapi.go` 头注释。

## 4. 验收命令与原始输出

16. 2026-09-18：`go build ./...` → 退出码 `0`，无输出。
17. 2026-09-18：触及包靶向跑 `go test ./internal/agentd/ -count=1 -run 'TestConsoleFace|TestSessionCreateOwnerDefaultsToConsoleIdentity|TestConsoleReadMergesAcrossDevices'`
    → `ok github.com/Xsxdot/handoff/internal/agentd 0.945s`。
18. 2026-09-18：全量 agentd（§4.4 验收）`go test ./internal/agentd/ -count=1 -timeout 15m > /tmp/agentd_b3589_t1.log 2>&1; echo EXIT=$?`
    → `EXIT=0`；`grep -cE '^--- FAIL'` → `0`；`ok github.com/Xsxdot/handoff/internal/agentd 167.816s`。

## 5. 变异自验（先确认编译通过，再数失败）

19. 2026-09-18：变异①（打唯一，count==1 断言过）——去掉端戳装填 `Device: id.Device,`。
    `go build ./...` 退出 `0`（编译通过）；靶向跑：
    `--- FAIL: TestConsoleFaceActorAndDeviceStamp` / `identity_face_test.go:123: 端戳应为登录会话设备名，实得 ""`。有牙。
20. 2026-09-18：变异②——`requireConsoleIdentity` 的 `if !id.Configured` 改 `if false && !id.Configured`（语义取反）。
    编译通过；靶向跑 `TestConsoleFaceFailClosedWithoutName`、`TestConsoleFaceRejectsIllegalName` 皆红。
    有牙（fail-closed 面被测试拦住）。
21. 2026-09-18：变异③——`handleSessionCreate` owner 缺省 `id.Member` 改 `"user:other"`。编译通过；靶向跑：
    `--- FAIL: TestSessionCreateOwnerDefaultsToConsoleIdentity` / `identity_face_test.go:193: 缺省 owner 应为解析人名 "user:sycm"，实得 "user:other"`。有牙。
22. 2026-09-18：三发变异均先确认 `go build ./...` 过、再读断言红；未出现「编译不过当 0 红」的假读数。
    变异后已从 `/tmp/*.bak` 还原，`go build ./...` 复跑 `BUILD_OK`。

## 6. 图查询与图覆盖债

23. 2026-09-18：`codegraph resolve --repo . --doc internal/agentd/roomsapi.go` / `identity_face_test.go` → 均 `{"anchors": []}`。
24. 2026-09-18：`codegraph sym requireConsoleIdentity --view cards-B358.9-charter-4` → 未命中（图未覆盖，回落 grep）；
    `codegraph sym validSessionOwner --view cards-B358.9-charter-4` → 未命中。
    本节点**图覆盖债（未命中符号）**：`requireConsoleIdentity`、`validSessionOwner`（生产）；
    测试辅助 `setConsoleUser`/`mustNamedSession`/`ledgerPostCookie`/`ledgerGetCookie`/`roomMessageEvents`（测试，不入图）。
    未命中时回落 grep 读源码，未拿 chain 冒充。

## 7. 判断与放弃的尝试

25. 2026-09-18：`newRoomsEnv` 配名注入点选 `roomsapi_test.go` 内 `srv.cfg.Store`（计划 §4.1 指定），
    不动 `ledgerapi_test.go#newNoPTYLedgerEnv`——保持 plan §2.1 有界文件集。
26. 2026-09-18：`sessionsapi_test.go` 全文替换 `webUserMember` 后，`TestSessionMemberAddEndpoint`
    前态 403 判据仍成立（console 成员 `user:sycm` 未入列），与 owner 无关；全量 agentd 绿证实。
27. 2026-09-18：无放弃的尝试。未碰 handoff CLI、未起任何 executor。

## 8. 提交（历史读数）

28. 2026-09-18：本节点产出物（`internal/agentd/identity_face_test.go` 新建 + `roomsapi.go`/`sessionsapi.go`/
    `identity.go`/`roomsapi_test.go`/`sessionsapi_test.go` 改动）与本台账同批提交。命令与原始输出：

```
$ git add internal/agentd/identity_face_test.go internal/agentd/roomsapi.go internal/agentd/sessionsapi.go internal/agentd/identity.go internal/agentd/roomsapi_test.go internal/agentd/sessionsapi_test.go docs/superpowers/ledgers/2026-09-18-b358.9-t1-implement-ledger.md
$ git commit -m "implement(B358.9/T1): gateway 会话/房间端点换脸——requireConsoleIdentity fail-closed + 端戳 + owner 缺省"
[cards/B358.9-charter-7 662b94b7] implement(B358.9/T1): gateway 会话/房间端点换脸——requireConsoleIdentity fail-closed + 端戳 + owner 缺省
 7 files changed, 539 insertions(+), 94 deletions(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-18-b358.9-t1-implement-ledger.md
 create mode 100644 internal/agentd/identity_face_test.go
```

29. 2026-09-18：提交事实只记当时命令与原始输出（历史读数）；amend 换 hash 属 git 事实，不在本文件追写。
    收口判据是工作树干净，不是文件里的 hash 等于 HEAD。
