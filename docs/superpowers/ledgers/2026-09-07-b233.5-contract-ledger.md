# B233.5 contract 台账

日期：2026-09-07。节点：charter contract。工作分支：`cards/B233.5-charter`。开工 HEAD：`9605d19b`。

## 过程

1. 核对上游 spec `docs/superpowers/specs/b233.5.md` 头部状态行 = **已批准**（2026-09-06，用户原话「按这个推进吧」），与 prompt 一致，未回写。定级 L3 轻档。有效功能基线 `cards/B233.1-charter-7` @ `9605d19b`（已含 233.1–4）。禁止合 main、禁止 absorb target/best/baseline。
2. 查图：`codegraph context d_scheduling`；`sym Admit` 命中 `n_scheduling_Service_Admit`（anchor=moved，行 364）；`sym bindingFor` **不在图中**（图覆盖债）。`who-calls n_scheduling_Service_Admit` 基线无上游边（warning：5 个未扫描入口）。`check` 无视图 fails：`d_execution→d_orchestration` dead-contract/dead-interface（ApprovalClient 仍只在 `cards-B233.1-charter-2` 视图）。`check --view cards-B233.4-charter` 另有 B233.4 实现接线超预算（本卡不 absorb、不涨那些预算）。
3. 现状代码事实：普通派发 `Manager.Dispatch` / `POST /api/tasks` 走 `executor.default` + 可选 target/executor/model/HOME，Task 无载体名。小队准入只在卡节点 `admitSquadStep`；`bindingFor` 允许请求覆盖 Target/Executor/Model；Binding 无 HomeDir，HOME 事后再读活载体。PutCarrier/PutSquad 不跨 kind 查重。默认载体登记不存在。
4. 架构法第一条：不新立顶层域。选择/准入/默认载体/物理绑定归既有 `d_scheduling`。不新开 `d_orchestration→d_scheduling`（Manager 不解析名称）。
5. Ticket 0 空壳：`internal/scheduling/{receiver,occupancy}.go` 纯函数与金样本；`Binding.HomeDir`、`proto.Task.Carrier`、`DispatchReq.Receiver` / HTTP `receiver` 直通字段。不改 `bindingFor`/`Admit`/`Manager.Dispatch` 生产行为。
6. 契约文档写到法定路径 `docs/superpowers/specs/b233.5-contract.md`。视图 `codegraph/diffs/cards-B233.5-charter.json`（复制 B233.1 视图节点并追加本卡符号）。target.json 仅给既有 `d_gateway→d_scheduling` 补 B233.5 注记，不新开方向、不改预算。

## 命令读数

- `gofmt -w` 触及的 Go 文件后 `gofmt -l` 空输出。
- `go test ./internal/scheduling/ -count=1 -timeout 90s`：`ok github.com/Xsxdot/handoff/internal/scheduling`，exit 0。
- `go test ./internal/scheduling/ ./internal/proto/ ./internal/client/ ./internal/ledgerstep/ ./internal/agentd/ -count=0`：五包 `ok ... [no tests to run]`，exit 0。
- `go test ./internal/agentd/ -count=1 -timeout 180s -run 'TestSquadNodeAdmitsAndDispatchesThroughBinding|TestEffectiveCovers|TestAdmissionRequiresOnline|TestDispatch'`：`ok ... 15.051s`，exit 0。
- `go build -o /dev/null .`：exit 0。
- `codegraph --repo . --view cards-B233.5-charter validate`：exit 0，`issues: null`。
- `codegraph --repo . --view cards-B233.5-charter check`：exit 0，`fails []`。
- `codegraph --repo . resolve --doc docs/superpowers/specs/b233.5-contract.md --view cards-B233.5-charter`：exit 0。新符号随视图决议；存量 `Manager.Dispatch`/`Admit` 等为 `moved`，均无坏锚。`bindingFor` 决议到文件行但无 nodeId（图覆盖债）。

## 判断

- 不升格新顶层域；新符号归既有 `d_scheduling`。
- 默认载体 singleton 用编制 registry，不用 `executor.default`。
- `bindingFor` 今日覆盖物理身份是现状，不是契约。冻结为 `BindPhysical`。
- 可执行冻结走占用键、默认 JSON、哨兵字面值、名称分类与物理覆盖禁令金样本。
- 三重闸门三条均写入契约 §8。
- 欠账见契约 §9：无「已实现但零测试」。

## 提交

```
git commit -m "contract(B233.5): 冻结载体与小队统一派发契约"
[cards/B233.5-charter 5a49b44c] contract(B233.5): 冻结载体与小队统一派发契约
 14 files changed, 2038 insertions(+), 7 deletions(-)
 create mode 100644 codegraph/diffs/cards-B233.5-charter.json
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.5-contract-ledger.md
 create mode 100644 docs/superpowers/specs/b233.5-contract.md
 create mode 100644 internal/scheduling/occupancy.go
 create mode 100644 internal/scheduling/occupancy_test.go
 create mode 100644 internal/scheduling/receiver.go
 create mode 100644 internal/scheduling/receiver_test.go
```

随后 amend 一次把本段收进同批提交。不回填 amend 后的 HEAD。
