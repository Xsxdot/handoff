# B249 contract 节点台账

- 2026-08-25：命令 `git status --short --branch`；原始输出 `## cards/B249-charter`；事实：当前工作分支为 `cards/B249-charter`，初始无未提交改动。
- 2026-08-25：命令 `sed -n '1,240p' docs/superpowers/specs/2026-08-25-b249-permission-noise-reduction.md`；原始输出头部含 `**状态：已批准**（2026-08-25，用户在 spec 对话中批准；两个裁决点见「已定裁决」节）`；事实：上游 spec 已批准，可进入契约查证。
- 2026-08-25：命令 `rg --files codegraph docs/superpowers/specs docs/superpowers/ledgers | rg '(contract|target\\.json|best\\.json|b231|b230|b239)'`；原始输出包含 `codegraph/best.json`、`codegraph/target.json`、`docs/superpowers/specs/b239-contract.md`；事实：仓库存在代码图，适用有图项目契约冻结路径。
- 2026-08-25：命令 `rg -n "type Scope|func InScope|judgeBash|judgeCommand|TaskDir|taskTmpDir|autoAllowPermission|selfcmd|SelfCmd|Judge\\(" internal`；原始输出命中 `internal/permgate/path.go:39,50`、`internal/permgate/permgate.go:151,216`、`internal/agentd/manager.go:1847,1889`、`internal/executor/codex/adapter.go` 等；事实：B249 触及判据范围、命令判定、自指令白名单、Manager 组装点与执行器临时目录实现。
- 2026-08-25：命令 `nl -ba internal/permgate/path.go`；原始输出显示 `InScope(path string, scope Scope) (in bool, base string, err error)` 当前只遍历 `scope.Workdir` 与 `scope.TaskDir`（`internal/permgate/path.go:22-68`）；事实：第三根必须进入 `Scope` 并由同一 `InScope` 循环判定。
- 2026-08-25：命令 `nl -ba internal/agentd/manager.go | sed -n '1845,1912p'`；原始输出显示 `judgePermission` 当前用 `filepath.Join(m.cfg.DataDir, "tasks", taskID)` 组装 `Scope.TaskDir`，`autoAllowPermission` 当前只回传 `RespondPermission(..., "once", "")` 并累计计数，注释明确「不建工单、不发事件」（`internal/agentd/manager.go:1845-1912`）；事实：B249 必须把范围第三根接到执行契约，并为白名单 AutoAllow 增加只入库审计事件。
- 2026-08-25：命令 `nl -ba internal/executor/codex/adapter.go | sed -n '111,152p;266,287p'`；原始输出显示现有私有 `taskTmpDir(taskDir string) string` 返回 `<DataDir>/tmp/<id8>`，`tmpEnvKVs` 注入 `TMPDIR`/`GOTMPDIR`/`GOCACHE`，Codex Start 在 `startServe` 前组装这些变量（`internal/executor/codex/adapter.go:111-152,266-287`）；事实：新执行契约必须复用该路径规则，不能让判据复制 `<DataDir>/tmp/<id8>`。
- 2026-08-25：命令 `nl -ba internal/executor/executor.go | sed -n '53,75p;208,231p'`；原始输出显示 `StartReq.TaskDir` 的现状契约是 `DataDir/tasks/<id>`，`Adapter.Start(ctx context.Context, req StartReq) error` 是执行挂载入口（`internal/executor/executor.go:53-75,208-231`）；事实：新查询放在 `internal/executor` 契约包，输入采用 `dataDir` 与 `taskID`，不让判据依赖具体 adapter。
- 2026-08-25：命令 `nl -ba internal/proto/proto.go | sed -n '38,124p'` 与 `nl -ba internal/agentd/manager.go | sed -n '1729,1807p;1991,1999p;2315,2328p'`；原始输出显示当前事件词表有 `permission_request`、`approver_decision`、`permission_reuse`，其中后两者只入库不 Publish，静态 `AutoAllow` 路径当前无事件；事实：白名单审计需要新增独立事件词，不得伪装为已有 `permission_reuse` 或唤醒事件。
- 2026-08-25：命令 `rg -n 'func shortTaskID|"name": "shortTaskID"|discipline_test|TaskTmpDirForTest' internal codegraph/baseline.json`；原始输出命中 `internal/executor/codex/adapter.go:135`、`internal/executor/codex/export_test.go:40`、`internal/executor/codex/discipline_test.go:60`、`codegraph/baseline.json:33365`；事实：Codex 当前自有短 ID 规则有图节点，Ticket 0 迁移到执行契约包时需记录旧符号删除与新调用边。
- 2026-08-25：命令 `git rev-parse --abbrev-ref HEAD && git rev-parse HEAD && git status --short`；原始输出为 `cards/B249-charter`、`dd445a569b7a69e67145b37d688fd4638e233a4f`、`?? docs/superpowers/ledgers/2026-08-25-b249-contract-ledger.md`；事实：当前分支与基线提交未切换，台账是本轮唯一未跟踪文件。
- 2026-08-25：命令 `rg -n '"n_codex_(taskTmpDir|shortTaskID|tmpEnvKVs)"|"n_executor_(Adapter|StartReq|NormalizePermTool)"|"m_permgate_Scope"|"n_agentd_Manager_autoAllowPermission"|"n_client_isDeliverable"' codegraph/baseline.json`；原始输出命中 `m_permgate_Scope`、`n_agentd_Manager_autoAllowPermission`、`n_client_isDeliverable`、`n_codex_taskTmpDir`、`n_codex_tmpEnvKVs`，未命中 `n_codex_shortTaskID`；事实：当前基线图已有相关调用方与 `taskTmpDir`，短 ID 函数未形成同名 Codex 图节点，视图 diff 需以实际基线节点为准。
- 2026-08-25：命令 `sed -n '105,155p' internal/executor/codex/adapter.go && sed -n '250,300p' internal/executor/codex/adapter.go`；原始输出显示 Codex `taskTmpDir(taskDir string)` 从 `TaskDir` 向上两级取 DataDir 后拼 `tmp/<前8位>`，`Start` 与 `resume` 均调用它，且 `Start` 在 `req.Env` 后追加 `TMPDIR`/`GOTMPDIR`/`GOCACHE`；事实：Ticket 0 可将路径计算迁入 `internal/executor`，保留 Codex 环境变量与目录创建行为。
- 2026-08-25：命令 `sed -n '1,110p' internal/executor/executor.go && rg -n 'TaskTmpDir|tmpEnvKVs|taskTmpDir\(' internal/executor internal/agentd`；原始输出显示 `StartReq.TaskDir` 契约为 `DataDir/tasks/<id>`，目前只有 Codex 定义并使用 `taskTmpDir`；事实：新查询应放在 `internal/executor`，由协作控制侧调用，避免 `permgate` 反向依赖执行器。
- 2026-08-25：命令 `sed -n '1,260p' docs/superpowers/specs/2026-08-25-b249-permission-noise-reduction.md`；原始输出含 spec 的三条接缝、全量白名单、四执行器临时目录裁决、`/tmp` 不纳入、状态已批准与 out-of-scope；事实：本节点冻结三条签名/端点契约、依赖方向、审计事件与测试边界，不自行扩展黑名单或默认出口。
- 2026-08-25：命令 `gofmt -w internal/executor/tempdir.go internal/executor/tempdir_test.go internal/executor/codex/adapter.go`；原始输出为空，退出码 0；事实：Ticket 0 的执行契约查询、金样本测试与 Codex 直通镜像已按 Go 格式化。
- 2026-08-25：命令 `go test ./internal/executor/...`；原始输出为 `ok  github.com/Xsxdot/handoff/internal/executor 0.006s`、`ok  github.com/Xsxdot/handoff/internal/executor/claudecode 4.016s`、`ok  github.com/Xsxdot/handoff/internal/executor/codex 5.964s`、`ok  github.com/Xsxdot/handoff/internal/executor/fake 0.001s`、`ok  github.com/Xsxdot/handoff/internal/executor/grok 1.376s`、`ok  github.com/Xsxdot/handoff/internal/executor/opencode 17.807s`、`ok  github.com/Xsxdot/handoff/internal/executor/rawtap 0.003s`、`ok  github.com/Xsxdot/handoff/internal/executor/turn 0.023s`，退出码 0；事实：Ticket 0 骨架与 Codex 直通镜像本轮编译、测试通过。
- 2026-08-25：命令 `sed -n '10270,10320p;22295,22355p;38350,38425p;46050,46120p;47570,47625p' codegraph/baseline.json`；原始输出包含基线节点 `m_permgate_Scope`、`n_agentd_Manager_autoAllowPermission`、`n_client_isDeliverable`、`n_codex_taskTmpDir`、`n_codex_tmpEnvKVs`；事实：视图 diff 可用既有节点 ID 表达后续 Scope、审计过滤与路径查询接线，新增 `n_executor_TaskTmpDir` 归属 `k_executor_fn`。
- 2026-08-25：命令 `rg -n '^### codegraph/diffs|nodesAdded|edgesAdded|nodesDeleted' docs/codegraph-scan-recipe.md`；原始输出显示 diff 必须用 `nodesAdded` 完整节点、`nodesModified` 完整新节点、`nodesDeleted` ID、`edgesAdded` 调用边；事实：本分支视图 diff 只记录 Ticket 0 新符号及真实新增调用边，不改 baseline/best。
- 2026-08-25：命令 `nl -ba internal/permgate/permgate.go | sed -n '55,265p'`、`nl -ba internal/permgate/path.go | sed -n '1,90p'`、`nl -ba internal/permgate/selfcmd.go | sed -n '1,145p'`、`nl -ba internal/agentd/manager.go | sed -n '1720,1920p'`、`nl -ba internal/proto/proto.go | sed -n '35,135p'`、`nl -ba internal/client/client.go | sed -n '115,160p'`；原始输出显示现状 `Scope` 仅 `Workdir/TaskDir`、`judgeBash` 先收集路径再落命令判定、`autoAllowPermission` 当前不写事件、自指令只读表无 `graph`、客户端过滤五类既有事件；事实：契约增量必须明确第三根、白名单顺序、独立审计事件及客户端非交付过滤，且保留未知自指令 fail-closed。
- 2026-08-25：命令 `go env GOROOT && rg -n 'func Join|func Rel|func Abs' "$(go env GOROOT)/src/path/filepath/path.go" "$(go env GOROOT)/src/path/filepath/path_unix.go"`；原始输出为 `/usr/local/go`、`path.go:130 Join`、`path.go:161 Abs`、`path.go:185 Rel`；命令 `nl -ba /usr/local/go/src/path/filepath/path.go | sed -n '120,215p'`；原始输出显示 `Join` 忽略空元素并 Clean、`Abs` 对相对路径接当前工作目录、`Rel` 跨卷/无法相对化返回 error；事实：`TaskTmpDir` 使用 `filepath.Join` 只做纯路径计算，`InScope` 继续以 `Abs`/`Rel` 的错误结果 fail-closed，不把 `/tmp` 共享根加入契约。
- 2026-08-25：命令 `awk '/"container": "k_executor_fn"/{flag=1} flag && /"order"/{print; flag=0}' codegraph/baseline.json | tail -10`；原始输出为 `"order": 138`、`"order": 25`；事实：新增 `executor.TaskTmpDir` 采用执行契约函数容器的稳定新序号，不占用既有节点 ID。
- 2026-08-25：命令 `rg -n 'type ResumeReq|func \(.*Resume' internal/executor internal/executor/{claudecode,grok,opencode}/resume.go` 与 `nl -ba internal/executor/resume.go | sed -n '1,65p'`；原始输出显示 `ResumeReq` 含 `TaskID`、`TaskDir`、`Env`，`Resume` 统一签名为 `(executor.ResumeReq) (executor.ResumeOutcome, error)`；事实：四执行器冷恢复沿用现有 ResumeReq，不新增参数，只在 Cold 重启路径追加任务临时环境。
- 2026-08-25：命令 `nl -ba internal/store/store.go | sed -n '729,765p'`、`nl -ba internal/ledgermirror/mirror.go | sed -n '80,95p;345,365p'`；原始输出显示 `Store.AppendEvent` 同步落库并触发 event hook，镜像仅跳过 `progress`/`approver_decision`/`approver_disabled`；事实：新增白名单审计事件由 Store 入库、不调用 hub Publish，且默认进入 ledger mirror；客户端另行标成不可交付，避免 replay 唤醒。
- 2026-08-25：命令 `go run . graph resolve --repo . --doc docs/superpowers/specs/b249-contract.md`；原始输出返回 JSON `anchors`，现状符号 `Scope`、`InScope`、`Gate.Judge`、`Gate.judgeBash`、`Manager.judgePermission`、`Manager.autoAllowPermission`、`EventType`、`Store.AppendEvent`、`isDeliverable`、`ResumeReq`、Codex `taskTmpDir`、`WriteArgTargets`、`handlePermission` 均 `anchor: ok`；本轮新增或因本轮改行的 `Adapter.Start`、`TaskTmpDir`、金样本、`tmpEnvKVs`、`selfCmdReadOnly`、`mirrorSkip` 返回 `anchor: moved`；事实：文档锚点均能解析到文件/符号，新增符号待视图 diff 合并后由视图解析确认。
- 2026-08-25：命令 `go run . graph validate --repo .`；原始输出为 JSON `{"containers":239,"domainDecls":2,"domains":20,"edgeIssues":null,"edges":4745,"entitiesWithoutLifecycle":0,"issues":null,"nodes":3662,"unscannedEntries":6,"views":["cards-B192-charter","cards-B205-charter","cards-B249-charter"]}`，退出码 0；事实：目标图与本分支视图 diff 通过图校验，新增视图已被发现且无 issue/edge issue。
- 2026-08-25：命令 `go run . graph resolve --help`；原始输出明确 `--view string 叠加的视图名（codegraph/diffs/<名>.json）`；事实：文档锚点自检可在 B249 视图叠加下复跑。
- 2026-08-25：命令 `go run . graph resolve --repo . --view cards-B249-charter --doc docs/superpowers/specs/b249-contract.md`；原始输出 JSON 中 `internal/executor/tempdir.go#TaskTmpDir` 为 `anchor: ok, nodeId: n_executor_TaskTmpDir`，其余已入图生产符号为 `ok`；退出码 0；事实：本分支视图已覆盖 Ticket 0 新契约符号，文档锚点解析通过。
- 2026-08-25：命令 `go run . graph resolve --repo . --view cards-B249-charter --doc docs/superpowers/specs/b249-contract.md`（修正 `Adapter` 锚点和新文件行号后重跑）；原始输出显示 `internal/executor/executor.go#Adapter`、`internal/executor/tempdir.go#TaskTmpDir` 与既有生产符号均 `anchor: ok`，退出码 0；`tempdir_test.go#TestTaskTmpDirGoldenVectors`、`codex#tmpEnvKVs`、`selfcmdReadOnly`、`mirrorSkip` 为 `anchor: moved`（无节点或因本轮源码行变动）；事实：文档中的生产契约锚点没有坏锚。
- 2026-08-25：命令 `git diff --stat && git diff --check && git status --short --branch`；原始输出为 `## cards/B249-charter`、`M codegraph/target.json`、`M internal/executor/codex/adapter.go`、以及五个新增未跟踪文件（契约、台账、视图 diff、TaskTmpDir 源码与测试），`git diff --check` 无输出；命令 `python3 -m json.tool codegraph/target.json >/dev/null && python3 -m json.tool codegraph/diffs/cards-B249-charter.json >/dev/null; echo json_exit=0`；原始输出 `json_exit=0`；事实：目标图与视图 diff 是合法 JSON，工作树无空白错误；未跟踪文件的 diff stat 未展开，待 add 后核对。
- 2026-08-25：判断：契约文档将基线 Codex `shortTaskID` 改为历史查证表述，更新当前 `tmpEnvKVs` 行号，并明确 `permgate` 不新增执行器运行态依赖；事实：文档不再把已迁移符号或旧行号描述成当前代码事实。
- 2026-08-25：命令 `go run . graph validate --repo .`；原始输出为 JSON `{"containers":239,"domainDecls":2,"domains":20,"edgeIssues":null,"edges":4745,"entitiesWithoutLifecycle":0,"issues":null,"nodes":3662,"unscannedEntries":6,"views":["cards-B192-charter","cards-B205-charter","cards-B249-charter"]}`，退出码 0；事实：最终目标图校验无 issue、无 edge issue。
- 2026-08-25：命令 `go run . graph resolve --repo . --view cards-B249-charter --doc docs/superpowers/specs/b249-contract.md`；原始输出中全部已入图生产契约锚点与 `internal/executor/tempdir.go#TaskTmpDir` 为 `anchor: ok`，退出码 0；测试辅助/未入图变量的 `anchor: moved` 保持已知状态；事实：最终契约文档无坏生产符号锚。
- 2026-08-25：命令 `git diff --check`；原始输出为空，退出码 0；事实：最终文本与代码改动无空白错误。
- 2026-08-25：命令 `go test ./internal/executor/...`；原始输出为各 executor 子包 `ok` 且标记 `(cached)`，退出码 0；事实：执行契约测试命令再次通过。
- 2026-08-25：命令 `go test -count=1 ./internal/executor/...`；原始输出为 `ok github.com/Xsxdot/handoff/internal/executor 0.002s`、`claudecode 4.090s`、`codex 5.965s`、`fake 0.001s`、`grok 1.400s`、`opencode 17.841s`、`rawtap 0.004s`、`turn 0.028s`，退出码 0；事实：Ticket 0 骨架与 Codex 直通镜像本轮禁缓存编译/测试通过。
- 2026-08-25：命令 `git add codegraph/target.json codegraph/diffs/cards-B249-charter.json docs/superpowers/ledgers/2026-08-25-b249-contract-ledger.md docs/superpowers/specs/b249-contract.md internal/executor/tempdir.go internal/executor/tempdir_test.go internal/executor/codex/adapter.go`；原始报错 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/b86fcf6e/index.lock': Read-only file system`；事实：当前沙箱不能写 worktree 的 Git 元数据，暂未完成暂存；需要按权限规则请求对该 Git 元数据路径的写权限。
- 2026-08-25：命令 `git diff --cached --check && git diff --cached --stat`；原始输出 `docs/superpowers/specs/b249-contract.md:3: trailing whitespace.` 与 `docs/superpowers/specs/b249-contract.md:4: trailing whitespace.`，退出码 2；事实：契约文档标题行使用 Markdown 强制换行空格，违反 Git 空白闸门，已就地移除并重新暂存。
- 2026-08-25：命令 `git add docs/superpowers/ledgers/2026-08-25-b249-contract-ledger.md docs/superpowers/specs/b249-contract.md`（权限提升后）；原始输出为空，退出码 0；事实：空白修复及台账修复已重新进入暂存区。
- 2026-08-25：命令 `git diff --cached --check`；原始输出为空，退出码 0；命令 `git diff --cached --stat`；原始输出为 `codegraph/diffs/cards-B249-charter.json 47 +++++`、`codegraph/target.json 7 +-/`、契约台账 42 行、契约文档 191 行、Codex adapter 10 行、TaskTmpDir 17 行、测试 43 行，总计 `346 insertions(+), 11 deletions(-)`；命令 `git status --short --branch`；原始输出为 `## cards/B249-charter` 加 7 个 staged 路径；事实：冻结提交范围完整，暂存区通过空白检查。
- 2026-08-25：命令 `git diff --cached --check`；原始输出为空，退出码 0；事实：提交前暂存区最终通过空白闸门。
- 2026-08-25：命令 `git commit -m "contract(B249): freeze permission noise reduction contract"`；原始输出为 `[cards/B249-charter 5cc0d27a] contract(B249): freeze permission noise reduction contract`、`7 files changed, 349 insertions(+), 11 deletions(-)`，并创建视图 diff、契约文档、台账、TaskTmpDir 源码与测试；退出码 0；事实：B249 契约与 Ticket 0 已完成首次冻结提交。
- 2026-08-25：命令 `nl -ba internal/executor/tempdir.go`、`nl -ba internal/executor/tempdir_test.go | sed -n '1,90p'`、`nl -ba internal/executor/codex/adapter.go | sed -n '120,145p'`；原始输出显示新符号 `TaskTmpDir` 在 `tempdir.go:11`，金样本测试在 `tempdir_test.go:9`，Codex `taskTmpDir` 在 `adapter.go:129-132` 调用 `executor.TaskTmpDir`；事实：本分支视图 diff 新增 `n_executor_TaskTmpDir`、修改 `n_codex_taskTmpDir` 并加入真实调用边。
- 2026-08-25：命令 `git diff --check && git status --short`；原始输出为 `M internal/executor/codex/adapter.go`、`?? docs/superpowers/ledgers/2026-08-25-b249-contract-ledger.md`、`?? docs/superpowers/specs/b249-contract.md`、`?? internal/executor/tempdir.go`、`?? internal/executor/tempdir_test.go`，`git diff --check` 无输出且退出码 0；事实：代码、契约文档与台账无空白错误，图文件随后纳入冻结提交。

## B249 breakdown 节点（2026-08-25）

- 2026-08-25：命令 `git status --short --branch && git log -5 --oneline`；原始输出为 `## cards/B249-charter-2`、HEAD `328ecacd contract(B249): freeze permission noise reduction contract`，状态无未提交改动；事实：本节点在当前分支、契约冻结提交之后开工。
- 2026-08-25：命令 `nl -ba docs/superpowers/specs/2026-08-25-b249-permission-noise-reduction.md | sed -n '1,5p'`；原始输出显示 spec 第 4 行为 `**状态：已批准**（2026-08-25，用户在 spec 对话中批准；两个裁决点见「已定裁决」节）`；事实：上游 spec 状态位为已批准。
- 2026-08-25：命令 `nl -ba docs/superpowers/specs/b249-contract.md | sed -n '1,6p'`；原始输出显示 contract 第 3 行为 `**上游状态：已批准**`、第 5 行为 `**冻结状态：本提交随 codegraph/target.json 与本分支视图 diff 一并冻结**`；事实：contract 状态位为已批准且已冻结。
- 2026-08-25：命令 `python3 - <<'PY' ... json.load(open('codegraph/best.json'))['domains'] ...`；原始输出确认 `parent` 为空的顶层域为 `d_orchestration,d_gateway,d_workspace,d_execution,d_sessions,d_transport,d_protocol,d_ledger,d_cli,d_web,d_policy,d_maintenance`，类型分别为 logic/boundary/boundary/boundary/boundary/boundary/logic/logic/logic/logic/logic/boundary；事实：项目最优图的顶层域清单以这 12 个为准。
- 2026-08-25：命令 `rg -n -C 3 'd_orchestration|d_execution' codegraph/target.json`；原始输出显示已有 contract `d_orchestration → d_execution`、`legacyBudget: 27`、入口 `executor.TaskTmpDir`；事实：本次拆解只消费已冻结接缝，不新增 contract 方向或入口。
- 2026-08-25：命令 `git show --stat --oneline HEAD`；原始输出为契约文档、目标图、B249 视图 diff、执行器 TaskTmpDir/Codex 镜像及 contract ledger 共 7 个文件的冻结提交；事实：Ticket 0 与契约已随 HEAD 入库。
- 2026-08-25：命令 `nl -ba internal/executor/tempdir.go`；原始输出显示 `TaskTmpDir` 注释仅写“task-private temporary directory”“short shape is part of the executor contract”，未含 AF_UNIX 107、测试子路径 51、旧 61/新 27 字节账；事实：协调者指出的知识搬家缺口仍存在，必须列入后续实现任务并把中文精确字节账放在该权威注释。
- 2026-08-25：命令 `nl -ba internal/executor/codex/adapter.go | sed -n '111,131p'`；原始输出显示 Codex `taskTmpDir` 注释含完整 61→27 字节账，且函数体调用 `executor.TaskTmpDir`；事实：字节账目前只留在 Codex 直通镜像注释，不能作为长期权威位置，需搬到 `internal/executor/tempdir.go#TaskTmpDir`，并将该注释语言统一为中文。
- 2026-08-25：命令 `git branch -a --no-color`、`git for-each-ref --format='%(refname:short) %(objectname:short)'`；原始输出显示当前有 `cards/B249-charter-2`、`cards/B249-charter`、`cards/B248-charter`，以及 `origin/claude/codex-sandbox-handoff-permissions-558d49 dd445a56`，没有名为 `claude/codex-sandbox-handoff-permissions-558d49` 的本地 ref；事实：用户指定的后续基线提醒在本地以 `origin/` 远端跟踪 ref 存在，当前拆解不执行合并。
- 2026-08-25：命令 `git show origin/claude/codex-sandbox-handoff-permissions-558d49:internal/permgate/blacklist.go | rg -n -C 4 'execWrapperRx'`；原始输出为第一段 `...\\s-c\\b`、第二/三段原样，未见 `\\s-[a-z]*c[a-z]*\\b`；命令 `sed -n '45,78p' internal/permgate/blacklist.go`；原始输出确认当前分支同样仍为 `...\\s-c\\b`；事实：以实跑为准，当前工作分支与该远端基线均不含 B248 所述第一段放宽，后续 plan/implement 开工前必须先合入用户指定工作分支，届时若实跑已含再跳过。
- 2026-08-25：命令 `git rev-parse claude/codex-sandbox-handoff-permissions-558d49`；原始报错原文为 `fatal: ambiguous argument 'claude/codex-sandbox-handoff-permissions-558d49': unknown revision or path not in the working tree.`；事实：该名称不是当前仓库的本地可解析 ref，后续基线检查应使用协调者提供的实际工作分支/远端同步结果，不能凭名称假设已合入。
- 2026-08-25：命令 `rg -n -C 5 'func \\(m \\*Manager\\) ...' ...`；原始报错原文为 `rg: regex parse error: ... error: unopened group`；放弃：该复合正则不可用，后续以分拆的 `rg`/源码编号命令取证，不把失败命令当成代码结论。
- 2026-08-25：命令 `rg -n '^func \\(a \\*Adapter\\) (Start|Resume)' internal/executor/{claudecode,codex,grok,opencode}`；原始输出确认四家均有 `Adapter.Start`，且均有 `Adapter.Resume`（claudecode: adapter.go:194/resume.go:41；codex: adapter.go:258/resume.go:48；grok: adapter.go:181/resume.go:52；opencode: adapter.go:334/resume.go:44）；事实：四执行器 Start 与 Cold Resume 的环境接线属于同一实现轮的有界文件集。
- 2026-08-25：命令 `rg -l 'judgePermission|autoAllowPermission|handlePermission|permission_auto_allow|EventTypePermission' internal/agentd --glob '*_test.go'`；原始输出为 `eventframes_test.go watchdog_test.go permgate_wire_test.go manager_test.go integration_test.go resume_test.go approver_test.go regression_round2_test.go`；事实：agentd 权限/事件回归需从这些既有测试入口取最薄穿透路径，不能只测内部 helper。
- 2026-08-25：命令 `rg -l 'isDeliverable|EventTypePermission|permission_reuse|mirrorSkip|AppendEvent' internal/proto internal/client internal/ledgermirror internal/store --glob '*_test.go'`；原始输出列出 client、store 相关测试文件，未列出 proto/ledgermirror 专门测试文件；事实：审计事件的生产、存储、客户端 replay 过滤与 mirror 语义需在实现任务中按真实序列化边界补断言，不能用“两端各自有测试”替代链路测试。
- 2026-08-25：判断：本节点按 spec 已定轻档执行归一轮，不扇出子卡；单张统一实现卡仍需圈出 `permgate`、`agentd`、`proto/client/store/mirror`、四执行器及其回归测试的有界文件集，并以依赖 DAG 标出“契约查询/环境接线 → 判据与事件 → 全量门禁”的顺序。
- 2026-08-25：命令 `rg --files docs/superpowers/specs | rg 'checklist|charter|defect|instantiation'` 与 `nl -ba docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md | sed -n '49,90p'`；原始输出确认项目通用五族为生命周期/状态机中断、静默失败/误导报错、跨平台假设、假红测试、门禁绕过，并追加序列化边界、枚举新值白名单两条设问；事实：B249 还命中 Webview 第六族候选的触及条件（本卡不触及 Web 控制台/浏览器 API，因此不纳入）。
- 2026-08-25：命令 `python3 ... codegraph/best.json`（首次按错误数据形状读取 `containers`）；原始报错原文为 `AttributeError: 'str' object has no attribute 'get'`；放弃：不把 `containers` 当对象表读取，改用已成功读出的 `domains` 顶层域与既有 best 映射。
- 2026-08-25：命令 `rg -n 'type .*Payload|permissionPayload|EventType|isDeliverable|mirrorSkip|json\\.Marshal|json\\.Unmarshal|Event struct|EventFrame' internal/agentd internal/proto internal/store internal/client internal/ledgermirror`；原始输出确认新事件链上的手写投影/过滤点包括 `internal/agentd/manager.go` payload 与 AppendEvent、`internal/proto/proto.go#EventType`/`Event`、`internal/store/store.go#Store.AppendEvent`、`internal/agentd/eventframes.go`、`internal/client/client.go#isDeliverable`、`internal/client/backlog.go`、`internal/ledgermirror/mirror.go#mirrorSkip`；事实：统一实现卡必须列出并穿过这些真实边界，不能只做两个孤立包测试。
- 2026-08-25：命令 `rg -n 'AutoAllow|judgePermission|permission_request|PermissionReuse|EventType' internal/agentd/*_test.go`；原始输出确认 `internal/agentd/permgate_wire_test.go` 已有 Judge/AutoAllow 入口，`internal/agentd/manager_test.go` 已有权限重放/复用入口，`internal/agentd/approver_test.go` 已有审批事件不唤醒断言，`internal/agentd/integration_test.go` 已有真实权限请求链；事实：实现验收应沿现有 Manager/Store/adapter fake 入口补测试，避免只调用 `safeCommandID` 或手搭事件。
- 2026-08-25：命令 `rg -n 'isDeliverable|all=true|EventTypeApproverDecision|AppendEvent|mirrorSkip' internal/client/*_test.go internal/store/*_test.go internal/ledgermirror/*_test.go`；原始输出确认 `internal/client/client_internal_test.go` 有 `isDeliverable` 断言，`internal/client/follow_test.go` 有审计过滤与 `all=true` 断言，`internal/store/eventhook_test.go`/`store_test.go` 有 AppendEvent/权限事件入口，`internal/ledgermirror/mirror_test.go` 有镜像事件入口；事实：新 `permission_auto_allow` 的 replay 过滤、all 可见、入库与镜像验收均有有界测试落点。
- 2026-08-25：通过 `apply_patch` 写入 `docs/superpowers/specs/2026-08-25-b249-breakdown.md` 与 contract §8 三条边界澄清；事实：拆解稿已形成一张 B249-I1 统一实现卡，含六个图顶层域（其中 d_ledger 仅作消费验证）、逐条契约核对、五族/追加设问、真机清单、有限文件集和 DAG；contract 追加记录不改变冻结签名/语义。
- 2026-08-25：命令 `sed -n ... docs/superpowers/specs/2026-08-25-b249-breakdown.md`；原始输出显示稿首已集中说明无新增实现取舍、B248 基线前置，正文包含 `B249-I1` 四段式与五族矩阵；命令 `git diff --check`；原始输出为空；事实：拆解文本当前无 Git 空白错误，且未写实现代码。
- 2026-08-25：命令 `sed -n '1,18l' ...` 与 `rg -n '\\\\n|。`$|\\\\\\\\s|\\\\\\\\b' ...`；原始输出发现 B248 正则在文档中多写一层反斜杠、序列化结论多一个尾部反引号；判断：按“实跑输出优先”修正文档为单反斜杠 regex 形状并去除孤立反引号。
- 2026-08-25：命令 `go run . graph resolve --repo . --view cards-B249-charter --doc docs/superpowers/specs/2026-08-25-b249-breakdown.md`；原始输出为 anchors 中 `internal/permgate/blacklist.go#execWrapperRx`=moved、`internal/executor/tempdir.go#TaskTmpDir`=ok、`internal/agentd/manager.go#Manager.judgePermission`=ok、`internal/proto/proto.go#EventType`=ok、`internal/store/store.go#Store.AppendEvent`=ok、`internal/client/client.go#isDeliverable`=ok、`internal/ledgermirror/mirror.go#mirrorSkip`=moved，以及 `tempdir.go#TaskTmpDir`=file_missing；命令最终报错原文 `Error: 文档锚点检查失败: tempdir.go#TaskTmpDir (file_missing)` / `exit status 1`；事实：首次锚点检查失败，已定位为拆解稿中的相对文件锚，待改为完整仓内路径；moved 锚不是 file_missing。
- 2026-08-25：命令 `python3 - <<'PY' ...`；原始输出为 `{'has_pending': True, 'has_domains': True, 'has_contract': True, 'has_card': True, 'has_families': True, 'has_realmachine': True, 'has_dag': True}`；事实：拆解稿四样产出、缺陷族、真机清单和 DAG 的结构化存在性检查通过。
- 2026-08-25：通过 `apply_patch` 将拆解稿内唯一相对锚 `tempdir.go#TaskTmpDir` 改为 `internal/executor/tempdir.go#TaskTmpDir`；事实：待重跑锚点检查，未改变验收语义。
- 2026-08-25：命令 `go run . graph resolve --repo . --view cards-B249-charter --doc docs/superpowers/specs/2026-08-25-b249-breakdown.md`；原始输出为 `internal/executor/tempdir.go#TaskTmpDir`、`internal/agentd/manager.go#Manager.judgePermission`、`internal/proto/proto.go#EventType`、`internal/store/store.go#Store.AppendEvent`、`internal/client/client.go#isDeliverable` 为 `anchor: ok`，`execWrapperRx`/`mirrorSkip` 为 `anchor: moved`，进程退出码 0；stderr/stdout 另有 `INFO relay egress configured ...` 与 `INFO 已配置出网代理 ...`；事实：拆解稿无 `file_missing` 坏锚，moved 为已有代码行漂移提示。
- 2026-08-25：命令 `go run . graph resolve --repo . --view cards-B249-charter --doc docs/superpowers/specs/b249-contract.md`；原始输出为 19 个契约锚点，生产锚点均 `anchor: ok` 或已知 `anchor: moved`，无 `file_missing`，退出码 0；另有原始 INFO `relay egress configured ...`、`已配置出网代理 ...`。
- 2026-08-25：命令 `go run . graph validate --repo .`；原始输出为 `{"containers":239,"domainDecls":2,"domains":20,"edgeIssues":null,"edges":4745,"entitiesWithoutLifecycle":0,"issues":null,"nodes":3662,"unscannedEntries":6,"views":["cards-B192-charter","cards-B205-charter","cards-B249-charter"]}`，退出码 0；事实：目标图与 B249 视图无 issue/edge issue。
- 2026-08-25：命令 `python3 ...` 对拆解稿做结构检查；原始输出为 `{'one_card': True, 'four_sections': True, 'families': True, 'additions': True, 'real_machine': True, 'no_impl_code': True}`；命令 `git diff --check`；原始输出为空；事实：唯一子卡、四段式、五族/追加设问、真机条目、无实现函数文本和空白门禁均通过。
- 2026-08-25：命令 `git diff --check && git status --short --branch && git diff --stat -- docs/superpowers/ledgers/2026-08-25-b249-contract-ledger.md docs/superpowers/specs/b249-contract.md && wc -l docs/superpowers/specs/2026-08-25-b249-breakdown.md`；原始输出为分支 `## cards/B249-charter-2`、两份已跟踪文档修改、拆解稿未跟踪、已跟踪 diff `40 insertions(+)`、拆解稿 `196` 行；`git diff --check` 为空；事实：提交前范围仅为 B249 contract ledger、contract 修订记录和 breakdown 文档。
- 2026-08-25：命令 `git add docs/superpowers/ledgers/2026-08-25-b249-contract-ledger.md docs/superpowers/specs/b249-contract.md docs/superpowers/specs/2026-08-25-b249-breakdown.md && git diff --cached --check && git diff --cached --stat && git status --short --branch`（提升权限）；原始输出为 `3 files changed, 237 insertions(+)`、`git diff --cached --check` 无输出、分支 `## cards/B249-charter-2`，三文件均 staged；事实：首轮提交暂存区通过空白门禁，范围为本台账、196 行拆解稿和 contract §8 六行修订。
- 2026-08-25：命令 `git add docs/superpowers/ledgers/2026-08-25-b249-contract-ledger.md && git diff --cached --check && git diff --cached --stat && git status --short --branch`（提升权限）；原始输出为 `3 files changed, 238 insertions(+)`、暂存区空白检查无输出、分支 `## cards/B249-charter-2`，三文件 staged；事实：第二次暂存已把上一条命令结果纳入首轮提交，提交前范围未扩展。
