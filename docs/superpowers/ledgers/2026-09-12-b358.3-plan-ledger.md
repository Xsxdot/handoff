# B358.3 plan 台账（2026-09-12）

执行者：charter:plan 节点（L3 重档，子系统执行者依契约自写）。产出物：`docs/superpowers/plans/b358.3-plan.md`。
分支 `cards/B233.1-charter-7`，工作树起点 `77affbb5`（B358.1 `0be4a2c6`、B358.2 `77affbb5` 已合入）。

## §一 事实与读数（边读边记）

1. **skill 加载**：`charter:plan` 已加载（SessionStart 网关之外显式调用）。
2. **图覆盖债探针**（2026-09-12 实跑）：
   - `codegraph sym automationWakeEvent` → 命中（`n_agentd_automationWakeEvent`，d_coordination_task，anchor=moved，wakeconsumer.go:144，基线视图）。
   - `codegraph sym MessageWakeTargets` / `sym SessionWake` → 「不在图中（图未覆盖或名字有误）；近似候选: []」。
   - `codegraph sym consumeAutomationEventsOnce` / `roomServiceFor` / `wakeCoordinatorRound` / `EventsFromAsc` → 命中（基线视图）。
   - 结论：本卡触及面大体与 breakdown §6 同欠（Ticket 0 新符号只在视图 diff）；plan 一律 `file#Symbol` 源码锚。
3. **红窗基线实测**（2026-09-12 @ 77affbb5 工作树，对账用）：
   - `go test ./internal/agentd -count=1` → FAIL，失败恰 11 支：
     TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection / TestRoomMessagesEndpoint / TestRoomSendEndpoint / TestRoomReadEndpoint / TestInboxThreeSources / TestInboxMentionSource / TestInboxRefIDShapes / TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke（前 10 = B358.1 plan §0.4 红窗，归 S4）+ TestLegacyNodeEventSequenceUnchanged（B362 golden，归 B362 卡）。
   - `go test ./cmd -count=1` → FAIL，失败恰 8 支：
     TestRoomSendLandsRoomMessageWithUserKind / TestRoomSendCoordinatorAcceptsExplicitSeatFlags / TestRoomSendCoordinatorKindUsesGrokHostSession / TestRoomSendCarriesRefAndMention / TestRoomReadStdoutSeqPrefix / TestRoomListSortedByActivity（前 6 = B358.1 红窗，归 S5）+ TestRepoContractGate（图契约既有违规：dead-interface OrchestrationClient + 5 条 over-budget，与本卡无关）+ TestServePermissionHookDenyWithReasonAndStep0（macOS unix socket bind: invalid argument，环境性）。
   - 合计 19 支，与任务书「B358.1 红窗 16 + B362 golden 1 + 其余 2」逐支对上。wake 族（TestAutomation*/TestB2336*/TestB349*/TestB353*）当前全绿。
4. **B362 卡读数**（handoff card show B362）：TestLegacyNodeEventSequenceUnchanged 在补签前基线 a921d1df 即红，分支既有失败非 B358 引入；根因排查归 charter:debug（B362 卡），S3 不碰。
5. **机内事实查证**（写计划引用的现状）：
   - `internal/agentd/server.go:178` `autoLedger *ledgerapi.Facade`；`SetupAutomation`（:2668）装配 `s.rooms = collab.New(facade)` + `SetCursorStore(cursor.New(DataDir/room-cursors.json))`（:2691-2692）——wakeconsumer 可直接经 `s.rooms` 走 `MessageWakeTargets`，测试环境 `newNoPTYAutomationEnv` 已装配。
   - `internal/agentd/roomsapi.go:24` 已 import `internal/collab/room`（IsSessionRoom 无新增跨域边）。
   - `internal/collab/sessions.go#MessageWakeTargets`/`#AddressesCard`/`#replyAuthorOf`（:127/:145/:162）、`internal/collab/room/delivery.go#ResolveDelivery`（签名无成员集合）——寻址唯一入口已冻结，collab 本卡零改动。
   - `internal/collab/service.go#History(roomID, beforeSeq, limit)`（:228，beforeSeq 为排他上界；limit<=0 → historyDefaultLimit=200）；`sendToSession` 置 `msg.Room=r.ID`、无卡事件落账（:150-158）；writer 执法 = 显式成员 ∪ 席位（:101）。
   - `internal/collab/service.go:45-46` historyDefaultLimit=200——referenced 查找用默认窗，窗外省键（降级声明，见 plan 裁定 5）。
   - `internal/ledger/events.go#Store.EventsFromAsc`（:63）cardIDs 空=全流、fromSeq 排他；`internal/ledger/mirror.go:201` `Store.MaxSeq`。
   - `cmd/card_wait.go`：`runCardWait`（:52）默认起点 `st.MaxSeq()`（:78）、`exitCodeError{code: ExitTimeout}`（:178-182）、`cmd/exit.go:22` `ExitTimeout=124`、json.Encoder 逐行 stdout。
   - `cmd/room.go#roomServiceFor`（:33）/`openRoomService`（:39）；`runLedgerCLI`（cmd/ledgercli_test.go:33）是 CLI 测试夹具（写 DataDir 配置 + Ledger.Enabled + rootCmd 直跑）。
   - cursor 使用面：`sessions.go:50`（ListSessions）、`service.go:450`（ListRooms，仅 member!="" 分支）、:512 MarkRead、:523 Unread——CLI room 命令族不传 member，openRoomService 挂文件游标对既有命令行为中性（实测核对）。
   - `internal/collab/cursor/cursor.go`：`New(path)` 文件介质 tmp+rename；`Service.New` 默认纯内存空游标——CLI 若不挂文件介质，unread 投影与 gateway 不一致（条 44 要求同一介质），必须挂。
   - `internal/proto/sessions_fixture_test.go` 为 package proto（裸标识符）。
   - keystone：`WakeEvent{Kind,Card,Summary}` + `WakeMessage`（keystone.go:27）+ `briefing` 以 `- [kind] summary` 列表进开场简报（keystone.go:313-318）——fake runner 的 resumes 可观测 Kind 与 Summary。
6. **岔口发现**（记入 plan §岔口）：
   - 岔口#1：breakdown §3.4 ③「reply_to 指向已换绑前的旧席位 → 唤醒当前席位」与契约 §4.6 条 47 命中判据（target = 会话内某卡**当前** driver_session）字面冲突；Ticket 0 冻结的 `WakeTargets` 注释（sessions.go:113-114）也只认「作者若是某卡**当前**席位」。plan 默认按契约（旧席位不命中→外部/未匹配分支），翻转方案（席位历史归映射）已画好，待协调者拍板。
   - 交界：cmd/session.go 尚不存在，S3 建（session 根命令 + wait）；S5 续写其余子命令——plan §0 申报。
7. **既有测试改写清单**（S3 语义变更的伴生改写，非红窗）：wakeconsumer_test.go 内 5 支以「卡房间 user 消息广播唤醒」为前提——TestAutomationEventMappingThroughConsumer、TestB353AutomationMapsCardActionEvents、TestB353AutomationRoomJSONBoundaries、TestB353AutomationRoomJSONBoundariesThroughConsumer、TestAutomationWakeFailureAdvancesCursor。Task 2 内改写并收口为绿。

## §二 判据基线复核（计划落地前）

- 判据所引命令与读数见 §一.3（19 支红窗逐支点名、wake 族全绿）；Task 2/4 的红绿测试在基线的预期红因均已写明（功能缺失：会话消息零唤醒 / SessionWake 类型不存在）。
- 契约 §3.8 wire 形状（SessionWake/SessionCite）逐字取自冻结文本；`Task 1` 金样本按 `sessions_fixture_test.go` 既有形态（package proto）。

## §三 自审记录（出稿后追加）

1. **出稿过程中的事实纠错**（写测试代码时逐条查证，避免把错签名交给执行者）：
   - `RebindSeat` 是**四参** `(id, identity, source, expect)`，expect=CAS 期望席位（`internal/ledger/binding.go:74-104`）——初稿误写三参，已改为传当前席位；席位段禁空白/`#`（`internal/proto/seat.go:73-84`）——初稿夹具里的 `" rebound-session "` 非法身份已改为 `rebound-session-1/2`。
   - `AddSessionMember` 只在 `client.LedgerClient`/`ledgerapi.Facade`/`ledger.Store` 三层（`client.go:60`、`api.go:141`、`sessions.go:233`），**collab.Service 未包它**——cmd 夹具改经 Facade 直调，并在计划中显式注明（S5 落 CLI 成员面时自行包装）。
   - 归档会话拒绝 JoinCard（契约条 7）→ 结构事件逐值测试的次序改为「Leave → Join(新卡) → BindSeat(空座) → RebindSeat → Archive 最后」；`BindSeat` 只吃空座（条 52 拒绝路径），换绑用独立新卡。
   - `loadCLIConfig()` 返回 `*config.Config`（出错给零值，`cmd/root.go:288`）——openRoomService 挂游标行按实签定稿。
   - 守卫读源路径：cmd 包内测试读 `session.go`（相对包目录），与 Task 3 读 `wakeconsumer.go` 同款。
2. **占位符扫描**：无 TBD/「同 Task N」/无代码描述。两处声明例外（plan §10 末）：测试夹具复用既有 harness（文件名已指认、断言逐条列全）；`TestSessionWaitSourceGuard` 以照抄 Task 3 形态 + 三元组替换给出（断言逐条列全，非骨架）。
3. **五项检查**：plan §7 逐项落档——缺陷族 9 族逐族映射验收栏；序列化边界（SessionWake roundtrip + ReplyTo 两态 + CLI stdout 穿真实编码边界）；每 task 有界文件集；d_gateway 边界面行为验收 = 机内 stdout 金样本 + 真机申报（breakdown §5 #1/#2 归协调者）；接缝双向覆盖（测试→缝：全部入口在缝上；缝→测试：接缝 #2 五条断言 + 守卫各有其测试；内部锁：零）。
4. **跨 task 类型/一致性**：`SessionWake`/`SessionCite` 在 Task 1（定义+金样本）与 Task 4（装配+输出测试）逐字同源；`roomMessageWakeEvents` 签名在 Task 2 实现块与 Task 3 AST 守卫断言一致；19 支红窗名单在 plan §3 与本台账 §一.3 一致。
5. **最薄路径条**：Task 2 是第一任务中的点亮步骤（缝上今天调不到「@命中唤醒」——卡闸拦死），红绿落在其缝级测试；Task 1 编译级红同理。
6. **派发前自审**：计划无「驱动派发系统自身」的验收步骤；全部判据机内可复跑。跨卡申报（S5/S6 交界、Produces/Consumes 签名）见 plan §8。
7. **图覆盖债**（plan §2 + §一台账.2）：`MessageWakeTargets`、`SessionWake` 不在图中（探针原文）；`automationWakeEvent`/`consumeAutomationEventsOnce`/`roomServiceFor`/`wakeCoordinatorRound`/`EventsFromAsc` 命中基线视图。实现节点收尾把新增/修改符号补进视图 diff（recon）。

## §四 提交记录

（本节点产出物与台账同批提交后回填一次；不 chase amend 后 hash。）
