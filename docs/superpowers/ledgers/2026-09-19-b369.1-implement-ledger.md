# B369.1（S1）implement 节点台账

节点：implement（卡 B369.1「S1 配对 wire 回归与孪生样本」）。分支 `cards/B369.1-charter-2`。
法定产出：`internal/proto/pairing_fixture_test.go`（改）、`mobile/bind/pairing_fixture.json`（新）。
本节点不碰产品代码（`internal/proto/pairing.go` 零改动）。

## 一、基线复核（本轮实测）

| 命令 | 原始结果 |
| --- | --- |
| `git branch --show-current` | `cards/B369.1-charter-2` |
| `git status` | `nothing to commit, working tree clean` |
| `go version` | `go version go1.26.1 linux/amd64` |
| `go test ./internal/proto/... -count=1` | `ok github.com/Xsxdot/handoff/internal/proto 0.009s`（EXIT=0） |
| `go build ./...`（根） | EXIT=0 |
| `test -f mobile/bind/pairing_fixture.json` | NO（不存在） |
| `codegraph --repo . sym PairBundle --view cards-B369-charter` | 命中 `m_proto_PairBundle` |
| `codegraph --repo . sym EncodePairBundle --view cards-B369-charter` | 命中 `n_proto_EncodePairBundle` |
| `codegraph --repo . sym PairVersion --view cards-B369-charter` | 未命中（图覆盖债，回落 grep 确认 `pairing.go:24`） |

## 二、T1 红绿（改 `pairing_fixture_test.go`）

- 步骤 3 先红：`go test ./internal/proto/ -run TestPairingFixtureMatchesCanonicalBytes -count=1`
  → `--- FAIL: TestPairingFixtureMatchesCanonicalBytes`，原文
  `读取共享金样本失败（T2 是否已落文件？）: open .../mobile/bind/pairing_fixture.json: no such file or directory`（EXIT=1）。
  失败原因是「功能缺失」（文件不存在），非拼写错。
- 新增三支测试先绿（wire 已冻结实现）：
  `go test ./internal/proto/ -run 'TestPairBundleRoundTripProperty|TestPairBundleJSONKeysMatchWire|TestPairBundleRejectsMalformedSentinelExact' -count=1 -v`
  → 三支 `--- PASS`，`ok ... 0.009s`（EXIT=0）。

## 三、T2 共享金样本（新 `mobile/bind/pairing_fixture.json`）

- 文件字节 = `EncodePairBundle(pairFixture())` + `"\n"`，实测 `wc -c` = `589`（588 + 1 换行）。
- 步骤 2 跑绿：`go test ./internal/proto/ -count=1` → `ok ... 0.012s`（EXIT=0）。

## 四、变异自验（两段判定：先编译过，再数红）

1. **版本字段语义变异**（`internal/proto/pairing.go`，`EncodePairBundle` 内 `json.Marshal(b)` 前插入 `b.Version = PairVersion + 1`）：
   - 编译：`go build ./internal/proto/` → `BUILD_OK`（无 `build failed`/`cannot`/`undefined`）。
   - 行为红：`TestPairBundleRoundTrip` FAIL `pairing_fixture_test.go:71: proto: 配对载体版本不受支持: 2`；
     `TestPairBundleRoundTripProperty` FAIL `... 轮 0 解码失败: proto: 配对载体版本不受支持: 2 raw={"v":2,...}`。
   - 还原：`git checkout -- internal/proto/pairing.go` → `go test ./internal/proto/ -count=1` → `ok ... 0.012s`（EXIT=0）。
2. **json tag 语义变异**（`Addr` 的 `json:"addr,omitempty"` → `json:"adr,omitempty"`）：
   - 命中唯一性：`grep -c 'json:"addr,omitempty"'` = 1（唯一）。
   - 编译：`go build ./internal/proto/` → `BUILD_OK`。
   - 行为红：`TestPairBundleJSONKeysMatchWire` FAIL、`TestPairingFixtureMatchesCanonicalBytes` FAIL。
   - 还原：`git checkout -- internal/proto/pairing.go` → `go test ./internal/proto/ -count=1` → `ok ... 0.015s`（EXIT=0）。

变异验证使用 `git checkout -- <文件>` 还原；变异 1 的锚 `json.Marshal(b)` 命中唯一（`grep -c` = 1）。

## 五、T3 收口闸（本轮实测）

| 命令 | 原始结果 |
| --- | --- |
| `go build ./...`（根） | BUILD_EXIT=0 |
| `go test ./internal/proto/... -count=1` | `ok ... 0.040s`（PROTO_EXIT=0） |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.071s`（GATE_EXIT=0） |
| `codegraph --repo . check --view cards-B369-charter` | `fails=[]`（CHECK_VIEW_EXIT=0） |
| `codegraph --repo . validate --view cards-B369-charter` | `issues=null`、`edgeIssues=null`（VALIDATE_EXIT=0） |
| `codegraph --repo . check`（基线） | `fails=[]`（CHECK_BASE_EXIT=0） |

`git diff --stat`（未提交时）仅 `internal/proto/pairing_fixture_test.go | 246 +++`，`mobile/bind/pairing_fixture.json` 为新增未跟踪文件。

## 六、图覆盖债

`PairVersion`/`ErrPairVersion`/`ErrPairMalformed` 三符号不在 `cards-B369-charter` 视图；回落 grep 确认定义于 `internal/proto/pairing.go`。本卡新增代码全在 `_test.go`（测试不计图边）与 `mobile/`（图外嵌套 module），**本卡不补图**，债记于此，供 S4/图对齐节点处置。

## 七、提交事实（历史读数）

```
$ git add internal/proto/pairing_fixture_test.go mobile/bind/pairing_fixture.json docs/superpowers/ledgers/2026-09-19-b369.1-implement-ledger.md && git commit -m "implement(B369.1): 配对 wire 属性回归 + 键集/拒绝分型断言 + 共享金样本"
```

（随后按纪律只 amend 一次把本台账段收进同批提交——amend 会换 hash，此为 git 事实，不复写历史读数。）

## 八、review major 追加修复轮（分支 `cards/B369.1-charter-3`）

触发：review 指出金样本里 `PairMachine.Credential` 恒为空，`credential,omitempty` 键从未出现，
键集断言与金样本都无法拦住该 tag 的改名/删键。仅动
`internal/proto/pairing_fixture_test.go` 与 `mobile/bind/pairing_fixture.json`，`pairing.go` 零改动。

改动：
1. `pairFixture()` 的 `offline` 机器补 `Credential: strings.Repeat("0f", 8)`（非空），使
   canonical 字节带上 `"credential":"0f0f0f0f0f0f0f0f"` 键。
2. `TestPairBundleJSONKeysMatchWire` 的 `machines[2]` 期望键集加上 `credential`，并新增值断言
   `machines[2]["credential"] == "0f0f0f0f0f0f0f0f"`。
3. 同步刷新 `mobile/bind/pairing_fixture.json`（含末尾单换行；实测 `wc -c` = 621）。

红绿与变异（本轮实测，临时文件用 `$TMPDIR`）：
- 改测试未改金样本时先红：`go test ./internal/proto/ -run TestPairingFixtureMatchesCanonicalBytes -count=1`
  → `--- FAIL: TestPairingFixtureMatchesCanonicalBytes`，stored 缺 `credential` 键（EXIT=1）。红因是「功能缺失/样本未同步」。
- 落金样本后全绿：`go test ./internal/proto/... -count=1` → `ok github.com/Xsxdot/handoff/internal/proto 0.013s`（EXIT=0）。
- **overlay 变异**（不改盘上 `pairing.go`）：`$TMPDIR/pairing_mut.go` 把
  `Credential string \`json:"credential,omitempty"\`` → `json:"credentialX,omitempty"`，`count(old)==1` 已断言唯一；
  `go test -overlay=$TMPDIR/overlay.json ./internal/proto/ -count=1`：
  - 单测行为红：`TestPairBundleJSONKeysMatchWire` FAIL `pairing_fixture_test.go:256: machines[2] 缺键 "credential": got [name token credentialX node] want [...]`。
  - 全量红：另加 `TestPairingFixtureMatchesCanonicalBytes` FAIL（canonical 出 `credentialX`）。
  - 变异语义改动编译通过（非 build failed），确属打中唯一。
  - `git diff --stat internal/proto/pairing.go` 空、`git status --short` 仅两个产出物 + 本台账。

T3 复跑（本轮实测）：`go build ./...` EXIT=0；`go test ./internal/proto/... -count=1` ok；
`go test ./cmd/ -run TestRepoContractGate -count=1` ok；`codegraph --repo . check --view cards-B369-charter` `fails=[]` EXIT=0；
`validate --view cards-B369-charter` `issues=null edgeIssues=null` EXIT=0；基线 `check` `fails=[]` EXIT=0。

未验证：壳侧 Kotlin/Swift 对共享样本的解码仍归真机清单 #1，本仓不验（同 §八上文口径）。
