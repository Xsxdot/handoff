# B371 spec 台账（2026-09-14）

一行一条历史读数；原始输出照抄，不做二次加工。

## 1. 现状复现（修复前）

1. 基线：`git rev-parse HEAD` → `442583457b69e881299e61bd8deec22bf11dedee`（分支 `cards/B371-charter-4`）。
2. 本机确为「无 VCS 自动戳」环境（`go version -m` 无 `vcs.*` 行）。实测命令与输出：

   ```
   $ OUT=/tmp/b371-check scripts/build-deploy.sh darwin/arm64
   EXIT=1
   ...
   ==> 把产物拷进 internal/webui/dist/
   ==> 构建 darwin/arm64  版本 442583457b69  提交 442583457b69 (...)  modified=false
   ==> 自检：注入的提交号必须真的进了产物
   ```

   即在「前端产物嵌入自检」与收尾 `OK：` 之前 exit 1；此时产物已产出（`/tmp/b371-check` 存在，28798706 字节）。
3. 根因读数：`go version -m /tmp/b371-check | grep -c 'vcs\.revision'` → `0`；`set -euo pipefail` 下命令替换里 `grep` 无命中返回 1 直接终止赋值。
4. 修复表达式在 pipefail 下语义验证（临时 body，未落盘）：

   ```
   $ bash -c 'set -euo pipefail; AUTO_REV="$(... | grep -o ... | cut -d= -f2 || true)"; echo "reached, AUTO_REV=[$AUTO_REV]"'
   reached, AUTO_REV=[] exit-of-assign=0
   ```

## 2. 修复与验收

5. 改动：`scripts/build-deploy.sh:105` 取自动戳命令替换末尾加 `|| true`，并加解释「为什么」的注释。`set -euo pipefail`、四个 `-X` 注入、两门自检判据均未动。
6. 验收命令（spec 计划第 2 条）：`OUT=/tmp/b371-check scripts/build-deploy.sh darwin/arm64` → `EXIT=0`，末行 `OK：/tmp/b371-check（28M），版本 442583457b69+dirty1`；两门自检均执行（版本注入自检无报错、`ok github.com/Xsxdot/handoff/internal/webui 0.003s`）。

## 3. 变异自验（确认断言有牙）

7. 变异：`count("| cut -d= -f2 || true)") == 1` 断言命中唯一后，改为 `|| false`（改语义，非删行；编译/语法均通过）。
8. 变异后行为断言：`OUT=/tmp/b371-mut scripts/build-deploy.sh` → `EXIT=1`，输出止于 `==> 自检：注入的提交号必须真的进了产物`，无 `OK：`。行为确实改变（绿→红），证明「退出码 + 末行 OK：」断言能拦住本缺陷。
9. 还原修复后 `grep -n 'cut -d= -f2'` → `108: ... || true)`，`git diff` 仅 `scripts/build-deploy.sh`（+4/-1）。

## 4. 回归与全量编译

10. `go build ./...` → exit 0（无输出）。
11. `CGO_ENABLED=0 go test -tags embedweb -count=1 ./internal/webui/...` → `ok github.com/Xsxdot/handoff/internal/webui 0.003s`。
12. 本机平台构建（spec 计划第 3 条，可运行产物才能问 `--version`）：`OUT=/tmp/b371-native scripts/build-deploy.sh` → `EXIT=0`，末行 `OK：/tmp/b371-native（29M），版本 442583457b69+dirty1`；`/tmp/b371-native version` → `442583457b69+dirty1` / `revision 442583457b69e881299e61bd8deec22bf11dedee`，与 `git rev-parse HEAD` 一致。

## 5. 结论

13. 修复仅一处、语义最小；无自动戳时走既有 `[ -n "$AUTO_REV" ]` 提示分支，不打断收尾。全量字节码由 `go build ./...` 兜底，触及子系统测试绿。

## 6. 提交读数（历史读数，记录于提交当时）

14. `git add scripts/build-deploy.sh docs/superpowers/ledgers/2026-09-14-b371-spec-ledger.md && git commit ...` 原始输出：

    ```
    [cards/B371-charter-4 8db528f2] fix(B371): build-deploy.sh 取自动戳对无 VCS 自动戳容错
     2 files changed, 51 insertions(+), 1 deletion(-)
     create mode 100644 docs/superpowers/ledgers/2026-09-14-b371-spec-ledger.md
    ```

    本条记录后做一次 amend 把台账收进同批提交，HEAD 随之改变——历史读数不追 HEAD。
