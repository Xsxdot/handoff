# B173 调用边门控与假边清洗执行记账

职责：记录本计划各 task 的双裁决、修复轮次、提交范围与亲自执行的验证结果。

- 基线：`232333e6`（计划提交），当前分支 `feat/b173-edgegate`，开工时工作树干净。
- Task 1 / 完成裁决：spec 符合（CheckEdges 实现跨语言与 Go 包粒度 import 门控，端点缺失/同包/无生产 Go 文件保守放行；EdgeIssue JSON 契约、validate 视图新增边门控、absorb 拒绝门控均接线）；代码质量符合（internal/codegraph 仅依赖 stdlib，go/parser 排除 `_test.go`，缓存包 import 集合，不改图数据）。验证：先跑 `go test ./internal/codegraph -run 'TestCheckEdges|TestEdgeIssue'`，原始红错为 `undefined: CheckEdges` 与 `undefined: EdgeIssue`；实现后该命令实际通过；`go build ./...` 实际通过；`go test ./internal/codegraph ./cmd` 实际退出 1，原始失败首行为 `--- FAIL: TestRepoContractGate (0.02s)`，其后为基线既有契约违规；`go test ./cmd -run 'TestGraph' -count=1` 实际通过；`gofmt -l internal/codegraph cmd` 与 `git diff --check` 实际无输出；真实 `go run . graph validate --repo .` 实际退出 1，输出 `nodes=3664`、`edges=4748`、`edgeIssues=106`，原因计数 `no-import=90`、`cross-language=16`。修复轮次：0。Commit 范围：`HEAD^..HEAD`。
- Task 2 / 完成裁决：spec 符合（在“硬纪律”最前补入限定符解析、跨包 import、receiver 类型、同名禁证、跨语言禁边与 validate 门控六项纪律）；代码质量符合（纯文档，术语与 Task 1 的 `EdgeIssue` 判据一致，未重复或冲突修改既有领域/节点规则）。验证：`git diff --check` 实际无输出；通读硬纪律小节并用 `rg` 实际确认新规则各出现一次。修复轮次：0。Commit 范围：`HEAD^..HEAD`。
- Task 3 / 完成裁决：spec 符合（按门控结果清除 106 条假边，基线 4748→4642；保留真实 check 红项，不改 target 预算，并补交接文档读数）；代码质量符合（清洗前有 106 条与 JSON roundtrip 硬断言，输出保持 2 空格 JSON + 末尾换行，抽查覆盖两条假边和一条真边）。验证：计划指定的 `/tmp/b173-validate.json` 写入首次原始失败为 `/bin/bash: line 1: /tmp/b173-validate.json: Read-only file system`，提升权限重跑取证实际完成；清洗脚本实际输出 `边 4748 -> 4642（删 106 条）`；validate 实际退出 0，`nodes=3664`、`edges=4642`、`issues=null`、`edgeIssues=null`；check 实际退出 1，恰 `18` 条（`new-direction=4`，`over-budget=14`），new-direction 为 `d_release→d_localint` 与 `d_release→d_remote`×3，over-budget 方向集合符合计划；抽查实际为 `n_web_app_machines_MachineEnv→n_config_save=false`、`n_opencode_API_CreateSession_2→n_relay_Decode=false`、`n_agentd_Manager_FootprintAll→n_store_Store_ListTasks=true`；`git diff --check` 实际无输出。修复轮次：0。Commit 范围：`HEAD^..HEAD`。
- 整分支终审 / 完成裁决：相对 `232333e6` 的完整 diff 仅含计划内 7 个文件；未发现规格遗漏、未授权改动或代码质量问题，无修复波。验证：`go test ./internal/codegraph`、`go test ./cmd -run 'TestGraph' -count=1`、`go build ./...`、`gofmt -l internal/codegraph cmd`、`git diff --check 232333e6..HEAD`、`python3 -m json.tool codegraph/baseline.json` 均实际通过。Commit 范围：`HEAD^..HEAD`。
