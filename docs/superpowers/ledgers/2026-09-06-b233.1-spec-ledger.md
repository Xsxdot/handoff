# B233.1 spec 台账

日期：2026-09-06。节点：charter spec。基线：`main` @ `2d07af43`。

## 过程

1. `handoff card show B233` / `B233.1`：父卡待办、子卡已在 spec 列；B233.1 阻塞 233.2/3/4。本会话 `handoff card bind B233.1` 坐下。
2. 背景附件当时不在本工作树。用户告知另一台机器补提交后：`git fetch origin main` + `git pull --ff-only`，`d870bfea..2d07af43`，草稿三份与 kimi-code 进入 main。
3. 读 `2026-09-06-foundation-infrastructure-boundaries-draft.md`、domain-model、process-atlas、foundation-domain-model-ledger。已确认：Ask/审批经派发事件流上浮、审批 client、无独立交互领域、工作流不参与本卡。
4. `codegraph summary` / `domains` / `context d_execution` / `context d_execution_contract` / `sym Adapter` / `sym EventType`。`who-calls handlePermission` 空结果，记图覆盖债。
5. 读 `internal/executor/executor.go#Adapter`、`opencode/adapter.go#Send`、`manager.go#Dispatch`/`handlePermission`/`handleQuestion`/`waitQuestion`、`permgate.go#Judge`、`approver.go#Decide`、`client.go#Dispatch`/`isDeliverable`、`cmd/reply.go`、`store.go#FindReusableGrant`、`proto.go` 事件词表与 Task 字段。
6. 用户裁定试点 OpenCode；会员到期，真机必须用免费模型。本机 `~/.handoff/config.yaml`：`executor.default=opencode`，`executor.model=opencode-go/deepseek-v4-flash`，`approver.executor=grok`。
7. 其余承重项按已确认草案收口，不再逐项追问：身份=Task/回合/原生请求 ID；事件流复用 proto 词表；政策快照绑定回合；OpenCode 路径退役 handleQuestion/handlePermission 权威实现。写成 `docs/superpowers/specs/b233.1.md`，状态待用户批准。

## 命令读数

- `git pull --ff-only origin main`：fast-forward `d870bfea..2d07af43`，6 files, 2886 insertions。
- `git rev-parse --short=8 HEAD` → `2d07af43`。
- `codegraph who-calls handlePermission`：无匹配（空）。
- 未跑 Go 测试、未派发真实 OpenCode 任务、未改运行配置。
- `git commit` spec/台账/roadmap：`[main f976df19] docs(B233.1): 批准执行基础首段 spec（L3 轻档）`，3 files changed, 275 insertions。

8. 用户原话「同意」批准 spec。回写头部状态为已批准（2026-09-06），准备挂 `spec:` 附件并 move `contract`。

## 判断

- L3 轻档：三条契约面，一条贯穿故事。
- 免费模型是验收环境约束，不是产品开关，不写进配置 schema。
- 本期不做的首次配置与 push 归属写入 roadmap，避免只活在 OOS 行。
