# B358.3 implement 台账（2026-09-12）

执行者：charter implement 节点 subagent。plan：`docs/superpowers/plans/b358.3-plan.md`（HEAD cc08ef15）。基线分支 cards/B233.1-charter-7 @ 77affbb5（+plan 提交 cc08ef15）。

## §0 开工前状态

- 工作树既有脏文件（不属本卡，禁卷入提交）：`web/src/app/task/Composer.test.tsx`（modified）、`.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、`web/design-qa-sidebar-after.png`、`web/design-qa.md`（untracked）。

## §一 基线复核（红窗 19 支，2026-09-12 @ cc08ef15 实测）

### §一.1 cmd 包基线（`go test ./cmd -count=1`，grep "--- FAIL"）

原始输出：

```
--- FAIL: TestRepoContractGate (0.03s)
--- FAIL: TestRoomListSortedByActivity (0.01s)
--- FAIL: TestRoomReadStdoutSeqPrefix (0.01s)
--- FAIL: TestRoomSendCarriesRefAndMention (0.01s)
--- FAIL: TestRoomSendCoordinatorAcceptsExplicitSeatFlags (0.01s)
--- FAIL: TestRoomSendCoordinatorKindUsesGrokHostSession (0.01s)
--- FAIL: TestRoomSendLandsRoomMessageWithUserKind (0.01s)
--- FAIL: TestServePermissionHookDenyWithReasonAndStep0 (0.00s)
FAIL	github.com/Xsxdot/handoff/cmd	26.445s
```

与 plan §3 清单 12–19 逐支同名、8 支无增无减。**基线成立。**

### §一.2 agentd 包基线（`go test ./internal/agentd -count=1`）

原始输出：

```
--- FAIL: TestInboxGoldenKeyShapes (0.01s)
--- FAIL: TestInboxIntegrationSmoke (0.01s)
--- FAIL: TestInboxMentionSource (0.01s)
--- FAIL: TestInboxRefIDShapes (0.02s)
--- FAIL: TestInboxThreeSources (0.02s)
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.04s)
--- FAIL: TestRoomMessagesEndpoint (0.02s)
--- FAIL: TestRoomReadEndpoint (0.02s)
--- FAIL: TestRoomSendEndpoint (0.01s)
--- FAIL: TestRoomsListEndpoint (0.02s)
--- FAIL: TestRoomsListUnreadAndAttachProjection (0.02s)
FAIL	github.com/Xsxdot/handoff/internal/agentd	82.183s
```

与 plan §3 清单 1–11 逐支同名、11 支无增无减。**基线成立（19 支全部对上）。**

### §一.3 codegraph 探针

（plan §2 已声明：`MessageWakeTargets`、`SessionWake` 不在图中；本卡符号清单见收尾 §符号清单，补图归 recon。）

## §二 Task 1：proto SessionWake/SessionCite DTO + Go 金样本

1. **红**：`go test ./internal/proto -run TestSessionWakeFixture -count=1` →

```
internal/proto/sessions_fixture_test.go:156:16: undefined: SessionCite
internal/proto/sessions_fixture_test.go:188:32: undefined: SessionWake
internal/proto/sessions_fixture_test.go:200:11: undefined: SessionWake
FAIL	github.com/Xsxdot/handoff/internal/proto [build failed]
```

红因=类型缺席（新缝符号首红允许编译红，非 typo）。绿实现：`internal/proto/sessions.go` 末尾追加 `SessionWake`/`SessionCite`（逐字取契约 §3.8）。

2. **绿**：`go test ./internal/proto -count=1` → `ok github.com/Xsxdot/handoff/internal/proto`；`go build ./...` → BUILD_OK。
3. **变异验证（去 Referenced 的 omitempty）**：
   - **第一次变异无效（放弃的尝试，纪律案例）**：用 `perl -pi -e` 原地改文件，`&&` 链全部 exit 0、还原后 go test 绿——但复查发现 `perl -pi` 的写入在本执行环境沙盒里**未落地**，文件从未被修改，第一轮的「变异后绿」是变异没生效的假读数，不是测试没牙。按 implement 纪律「变异后一切如常先怀疑变异没生效」处置：整体换成 Edit 工具做变异重做。
   - **重做（Edit 工具）**：去掉 omitempty → 行为断言确认变异生效 → `go test ./internal/proto -run TestSessionWakeFixture -count=1`：

```
    sessions_fixture_test.go:197: 无引用锚时 referenced 必须省键: {"session":"session:1","hit":{"seq":42,"room":"session:1","actor":"cli:opencode#ab12","body":"@B1 这个方案跑不通"},"referenced":null,"unread":1}
FAIL
FAIL	github.com/Xsxdot/handoff/internal/proto	0.460s
```

   还原 → `go test ./internal/proto -count=1` → `ok`；`git diff internal/proto/sessions.go` 仅含本任务 16 行新增（DTO 追加），变异已还原。
4. **教训落账**：本环境 Bash 沙盒会静默丢弃 `perl -pi`/`sed -i` 类原地写——本卡后续全部变异与代码修改改用 Edit/Write 工具；凡「变异后一切如常」必须先 `grep` 确认写入落地再解释读数。

**Task 1 验收栏全过**。

## §三 Task 2：wakeconsumer 改接寻址

1. **判据基线复核**：`go test ./internal/agentd -run 'TestAutomation|TestB2336|TestB349|TestB353' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 1.405s`，全绿（与 plan 一致，继续）。
2. **红（缝级测试落码后首跑）**：`go test ./internal/agentd -run 'TestB3583' -count=1` →

```
--- FAIL: TestB3583MentionCardWakesItsSeatOnce      processed=0（@卡号零唤醒）
--- FAIL: TestB3583NonMemberAndMissingCardTargetsDoNotWake  unread=3，want 1
--- FAIL: TestB3583ReplyToSeatAuthorWakesItsCard
--- FAIL: TestB3583ReboundCardMentionHitsNewSeat
--- FAIL: TestB3583MixedHitsNeverMergeOrBroadcast
--- FAIL: TestB3583CardRoomMessagesDoNotWake        （现状广播形状仍唤醒卡房间消息）
共 6 红；TestB3583NoAddressingDoesNotWake / ReplyToReboundOldSeatDoesNotWake /
StructureEventsNeverWake 现状绿（卡闸拦死一切会话消息 → 零唤醒反例天然成立，
实现后必须保持绿）。红因=功能缺失，非 typo。
```

   注：首跑曾因 plan 代码块携带未用的 `internal/ledger` import 编译红（build failed），删该 import 后见断言红。
3. **偏差 1（plan 数值笔误对齐）**：`TestB3583NonMemberAndMissingCardTargetsDoNotWake` 的未读断言 plan 原文 `want 1` 与 plan 自身夹具矛盾——夹具落 3 条会话消息且 user:alice 无游标，同一投影逻辑下（Task 4 `TestSessionWaitReferencedAndUnread`：3 条消息断言 unread=3）未读照记应为 3。按契约条 47「未读照记」语义对齐断言为 `want 3`，断言对象不变（外部分支不得写游标/吞事件破坏未读）。
4. **绿（实现）**：wakeconsumer.go 五处改动——import 补 `collab/room`；`automationWakeEvent` 删首行卡闸 + 删 `EvRoomMessage` case（职责上移）；`decodeHumanRoomMessage` 整体替换为 `decodeRoomMessageForWake`（保留 by_system:null 边界）；新增 `automationWakeEvents`（统一映射入口）与 `roomMessageWakeEvents`（寻址分流）；消费循环改接 `wakes` 多卡 pending。`go build ./...` 退出 0。
5. **既有 5 支广播测试改写**（每处注明「B358.3：广播形状删除（条 30）」）：`TestAutomationEventMappingThroughConsumer`（6→5，用户留言入不应出现清单）、`TestB353AutomationMapsCardActionEvents`（5→4，真人 room payload 入不应出现清单）、`TestB353AutomationRoomJSONBoundaries`（整支删除，留删除说明）、`TestB353AutomationRoomJSONBoundariesThroughConsumer`（期望翻转：全部可解码 payload wantProcessed=0 零 Resume，invalid 仍 wantErr）、`TestAutomationWakeFailureAdvancesCursor`（夹具改会话发言 @卡号，置失败位之前完成预绑定 Launch）。
6. **改写后跑绿**：`go test ./internal/agentd -run 'TestWake|TestAutomation|TestB2336|TestB349|TestB353|TestB3583' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 1.468s`。
7. **变异 A（无寻址不唤醒可变红，条 29）**：
   - 第一次尝试按 plan 字面「唤醒 session.Cards[0]」→ 编译红 `undefined: session`（session 在检查点之后定义）——编译不过的变异不算数，整块替换为可编译等价变异（分支内自查 GetSession 后唤醒首个卡）重做。
   - 重做后：`go test -run TestB3583NoAddressingDoesNotWake` → `无寻址会话消息不应唤醒，processed=1 escalated=false err=<nil>`（红，行为断言确认变异生效）→ 还原 → wake 族绿。
8. **变异 B（非会话房间不唤醒可变红，裁定 1）**：
   - plan 字面「删掉 room.IsSessionRoom 早退分支」→ 编译红 `room imported and not used`（import 悬空）——不算数，替换为可编译等价语义变异：非会话房间消息 `return WakeMessage{Card: msg.Room}`（旧广播形状回潮，保留 IsSessionRoom 引用）。
   - 重做后：`go test -run TestB3583CardRoomMessagesDoNotWake` → `卡房间消息不唤醒，processed=1 escalated=false err=<nil>`（红）→ 还原 → wake 族绿（`ok ... 1.220s`），grep 确认无 MUTATION 残留。
9. **变异 C**（绕过 MessageWakeTargets → 守卫红）依赖 Task 3 守卫测试，随 Task 3 执行。

## §四 Task 3：agentd 源码级守卫

1. `TestB3583WakePathSourceGuard` 落进 wakeconsumer_b358_test.go（禁词表 decodeHumanRoomMessage / proto.RoomMsgUser / .Mentions / 四个广播函数名 + AST 断言 roomMessageWakeEvents 必经 MessageWakeTargets）。跑绿：`go test ./internal/agentd -run TestB3583WakePathSourceGuard -count=1` → `ok`。
2. 既有守卫复核：`go test ./internal/collab/room -run 'TestResolveDeliveryShapeHasNoMemberSet|TestVerifyWriterHasNoKindBranches|TestWakePathSourceUsesAddressing' -count=1` → `ok`（零改动即绿）。
3. **变异 C / Task 3 变异（合一执行）**：`s.rooms.MessageWakeTargets(msg)` 替换为手写 `targets := []string{msg.Room}` → `go test -run TestB3583WakePathSourceGuard` →

```
    wakeconsumer_b358_test.go:394: 会话分流未经 MessageWakeTargets——寻址判定唯一入口被绕过（条 42）
FAIL
```

   还原 → wake 族绿（`ok ... 1.258s`），grep MUTATION 残留=0。

**Task 3 验收栏全过**。

## §五 Task 4：handoff session wait 通道本体

1. **红**：`go test ./cmd -run TestSessionWait -count=1` → 5 支红（OutputsHitJSON / ReferencedAndUnread / DefaultSinceOnlyWaitsForNewEvents / MemberExactMatch / IsReadOnly），红因 `session wait: unknown command "session" for "handoff"`（命令缺席，功能缺失；InvalidFlags 在命令缺席下对 err!=nil 断言天然成立不红）。注：plan 代码块携带未用的 `time` import 致编译红先现，删后见断言红。
2. **绿**：cmd/session.go（root+wait）落码 + cmd/room.go openRoomService 挂游标行（import 补 path/filepath、collab/cursor）。两处对 plan 代码的类型/结构适配：
   - **偏差 3（类型适配）**：`buildSessionWake` 的 ev 参数由 plan 的 `proto.LedgerEvent` 改为 `ledger.Event`——CLI 读侧 `Store.EventsFromAsc` 返回 `[]ledger.Event`（两类型同形，plan 字段级用法 ev.Seq/ev.Actor 不变）。
   - --since 负值校验按 plan 步骤 3 的补丁说明落在 runSessionWait 开头。
3. **跑绿**：`go test ./cmd -run 'TestSessionWait|TestRoom' -count=1` → TestSessionWait 全绿；TestRoom 失败 6 支与基线（§一.1 清单 12–17）逐支同名无增无减（S5 红窗，openRoomService 游标行对其零影响）。
4. **cmd 源码守卫**：TestSessionWaitSourceGuard 落码（文件 session.go、必经函数 runSessionWait、禁词表 proto.RoomMsgUser/.Mentions/四个广播函数名）→ `go test ./cmd -run 'TestSessionWait'` 全绿。
5. **变异（逐字相等可变红，条 39）**：
   - plan 字面 `strings.EqualFold` 变异首次尝试：补 strings import 后编译过、变异落地（grep 确认），但 **TestSessionWaitMemberExactMatch 仍绿**——变异不可观测。根因：plan 原夹具未传 --since，cursor=启动时流尾不扫历史消息，夹具消息在正确实现与变异下都不可见，测试恒 124 恒绿（无牙）。
   - **偏差 2（夹具修正）**：给 TestSessionWaitMemberExactMatch 补 `--since 0`（测试注释已注明），使命中判定可观测；「缺省=流尾」行为由 TestSessionWaitDefaultSinceOnlyWaitsForNewEvents 继续锁定，不受影响。
   - 重做变异 → `go test -run TestSessionWaitMemberExactMatch` → `member 与 target 必须逐字相等（条 39）: err=<nil>`（红）→ 还原 → `go test ./cmd -run 'TestSessionWait'` 全绿（ok 1.125s），grep MUTATION 残留=0。

**Task 4 验收栏全过**（39 逐字相等+变异；40 缺省=流尾排他；41 只对会话房间 EvRoomMessage 输出；42 双守卫；43 恰一行 JSON+省键+Task 1 金样本；44 unread 同一投影=3；45 只读=事件数不变+cursor 文件不落；46 命中 0/超时 124 两支各锁，timeout=0 永久阻塞由 ctx 无 deadline select 形状保证）。

## §六 Task 5 收口读数（全部实测原文）

1. `go build ./...` → BUILD_OK（退出 0）。
2. `go test ./internal/agentd -count=1` 失败集合（11 支，grep "--- FAIL"）：

```
TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke / TestInboxMentionSource /
TestInboxRefIDShapes / TestInboxThreeSources / TestLegacyNodeEventSequenceUnchanged /
TestRoomMessagesEndpoint / TestRoomReadEndpoint / TestRoomSendEndpoint /
TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection
```

   与 §一.2 基线逐支同名、**无增无减**。本卡改写的 5 支广播测试（ThroughConsumer/MapsCardActionEvents/RoomJSONBoundaries*/WakeFailureAdvancesCursor）不在失败集合（收口为绿）；无任何 TestB3583/TestWake/TestAutomation 红入集合。
3. `go test ./cmd -count=1` 失败集合（8 支）：

```
TestRepoContractGate / TestRoomListSortedByActivity / TestRoomReadStdoutSeqPrefix /
TestRoomSendCarriesRefAndMention / TestRoomSendCoordinatorAcceptsExplicitSeatFlags /
TestRoomSendCoordinatorKindUsesGrokHostSession / TestRoomSendLandsRoomMessageWithUserKind /
TestServePermissionHookDenyWithReasonAndStep0
```

   与 §一.1 基线逐支同名、**无增无减**。
4. `go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` → collab / collab/room / ledger / ledger/api / proto 全部 `ok`（零回归，proto 含 Task 1 金样本）。
5. `go test ./cmd -run TestRepoContractGate -count=1` → 仍红，违规清单原文：

```
[dead-interface] 契约 d_gateway→d_orchestration 声明的接口 "OrchestrationClient" 在 d_gateway 中不存在
[over-budget] d_cli->d_workspace 直调 11 条超出预算 9
[over-budget] d_gateway->d_workspace 直调 17 条超出预算 1
[over-budget] d_orchestration->d_workspace 直调 49 条超出预算 19
[over-budget] d_workspace->d_orchestration 直调 3 条超出预算 2
[over-budget] d_workspace->d_protocol 直调 23 条超出预算 3
legacy 命中（37 边 map）+ warn 140 条
```

   与 plan §3 清单 18 的基线摘要逐条一致（dead-interface 1 + over-budget 5 + legacy/warn 汇总），**只多不少=零新增**。判据成立的结构性依据：闸门读图快照（视图未重生成），本卡 diff 不含图数据与闸门测试代码。
6. wake 族复核（§3 规则 4）：`go test ./internal/agentd -run 'TestWake|TestAutomation|TestB2336|TestB349|TestB353|TestB3583' -count=1` → `ok ... 1.117s` 全绿（基线绿 + 新增 10 支 B3583 + 守卫 1 支 + 改写 4 支绿；RoomJSONBoundaries 直调版按 plan 整支删除）。cmd session 族 8 支（7 wait + 1 守卫）全绿。
7. **diff 面复核**（§0 文件集八文件 + 台账）：

```
M  internal/proto/sessions.go、internal/proto/sessions_fixture_test.go
M  internal/agentd/wakeconsumer.go、internal/agentd/wakeconsumer_test.go
M  cmd/room.go
?? internal/agentd/wakeconsumer_b358_test.go
?? cmd/session.go、cmd/session_test.go
?? docs/superpowers/ledgers/2026-09-12-b358.3-implement-ledger.md（本台账）
```

   禁触零 diff 确认：`internal/ledger/**`、`internal/collab/**`、`internal/keystone/**`、`internal/agentd/server.go`、`cmd/card_wait.go`、`internal/ledger/follow.go` 均不在改动列表（条 49 Store.Follow 语义不变的源码级确认）。无关脏文件（Composer.test.tsx / .commandcode/ / b353-probe-follow-harness.md / design-qa*）不卷入提交。

**跨域边申报**：agentd→collab.room 基线已存在（roomsapi.go 先例），wakeconsumer.go 补文件级 import 不新增包级边；cmd→collab.cursor 为本卡新增包级 import——plan 裁定 7 显式指示的组装点绑定（openRoomService 挂 room-cursors.json 介质），`SetCursorStore(*cursor.Store)` 在 plan §8 Consumes 申报清单内；闸门读数未加重。

## §七 新增/修改符号清单（交 recon 补视图 diff）

**新增（生产）**：proto.SessionWake、proto.SessionCite；cmd.sessionCmd、cmd.sessionWaitCmd、cmd.runSessionWait、cmd.buildSessionWake、cmd.sessionWaitSince、cmd.sessionWaitTimeout、常量 cmd.sessionWaitPollInterval。
**新增（测试）**：agentd.mustWakeSessionFixture、agentd.drainAutomation、agentd.sendSessionMessage、agentd.contains、TestB3583NoAddressingDoesNotWake、TestB3583MentionCardWakesItsSeatOnce、TestB3583NonMemberAndMissingCardTargetsDoNotWake、TestB3583ReplyToSeatAuthorWakesItsCard、TestB3583ReplyToReboundOldSeatDoesNotWake、TestB3583ReboundCardMentionHitsNewSeat、TestB3583MixedHitsNeverMergeOrBroadcast、TestB3583StructureEventsNeverWake、TestB3583CardRoomMessagesDoNotWake、TestB3583WakePathSourceGuard；cmd.mustWaitFixture、cmd.decodeSessionWake、TestSessionWaitOutputsHitJSON、TestSessionWaitReferencedAndUnread、TestSessionWaitDefaultSinceOnlyWaitsForNewEvents、TestSessionWaitMemberExactMatch、TestSessionWaitIsReadOnly、TestSessionWaitInvalidFlags、TestSessionWaitSourceGuard。
**修改（生产）**：agentd.automationWakeEvent（删卡闸与 EvRoomMessage 分支）、agentd.decodeHumanRoomMessage→agentd.decodeRoomMessageForWake（换名+语义改接）；消费循环 consumeAutomationEventsOnce 内部改接（签名不变）。
**新增（生产，agentd）**：agentd.Server.automationWakeEvents、agentd.Server.roomMessageWakeEvents。
**修改（测试，改写）**：TestAutomationEventMappingThroughConsumer（6→5）、TestB353AutomationMapsCardActionEvents（5→4）、TestB353AutomationRoomJSONBoundaries（整支删除）、TestB353AutomationRoomJSONBoundariesThroughConsumer（期望翻转）、TestAutomationWakeFailureAdvancesCursor（夹具改会话发言）。
**删除（生产）**：agentd.decodeHumanRoomMessage（广播形状函数，守卫钉死不得回潮）。

## §八 自主消化偏差清单（如实汇总）

1. **偏差 1**：TestB3583NonMemberAndMissingCardTargetsDoNotWake 未读断言 plan 原文 `want 1` 与其自身夹具（3 条消息）矛盾，按契约条 47「未读照记」同一投影逻辑对齐为 `want 3`（断言对象不变）。§三.3。
2. **偏差 2**：TestSessionWaitMemberExactMatch 夹具补 `--since 0`——plan 原夹具下命中判定不可观测（缺省=流尾不扫历史，正确实现与 EqualFold 变异同为 124），测试无牙；补参后逐字相等可锁定且变异红已验证。§五.5。
3. **偏差 3**：buildSessionWake 的 ev 参数类型 proto.LedgerEvent→ledger.Event（CLI 读侧 Store.EventsFromAsc 既有返回类型，两类型同形，字段用法不变）。§五.2。
4. **变异方式偏离 plan 字面（各一发）**：Task 2 变异 A/B 与 Task 4 变异的 plan 字面形式或编译不过（session 未定义 / room import 悬空 / strings 未 import）或不可观测（EqualFold 无 --since），均按 implement 纪律「两段判定 + 改语义不改有无」替换为可编译可观测的等价语义变异重做，红绿原文见 §三.7/§三.8/§五.5。
5. **流程性发现**：本执行环境 Bash 沙盒静默丢弃 `perl -pi` 类原地写——首例（Task 1 变异）已识别并全程改用 Edit/Write 工具，后核各变异落地均以 grep 确认。§二.4。

**真机归协调者申报（未实现，按 plan Task 5 步骤 8）**：breakdown §5 #1（真实主 agent 外部会话被 @ 后经 session wait 通道醒来）与 #2（真 keystone 拉起协调者回合 + 载荷现场核对）——fake 观测点只证明调用形状，不证真机行为。
