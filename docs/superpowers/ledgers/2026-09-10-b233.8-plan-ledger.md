# B233.8 计划节点台账

卡：B233.8
节点：plan
有效基线：`cards/B233.1-charter-7` @ `94246fc8a2acc7996c254cb6b473c7c9b5aef225`（当前工作分支另含 Ticket 0/contract/breakdown 文档）
法定产出：`docs/superpowers/plans/b233.8-plan.md`

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-10 | 当前节点在 `cards/B233.8-charter-3`、HEAD `70d839ecff4cefd4f4a3bb1413344c766103f127` 开工；工作树初始只有本计划台账的拍板记录改动。 | `git status --short --branch && git rev-parse HEAD && git rev-parse cards/B233.1-charter-7` → `## cards/B233.8-charter-3`、`70d839ecff4cefd4f4a3bb1413344c766103f127`、`94246fc8a2acc7996c254cb6b473c7c9b5aef225`。 |
| 2026-09-10 | P1/P2 已拍板 A/A；breakdown 已回写为 `已拍板（2026-09-10）`。 | 用户回复选择单卡与保留 `approval_client.go` 组装/哈希缝；随后 `apply_patch` 更新 `docs/superpowers/specs/b233.8-breakdown.md`。 |
| 2026-09-10 | 代码图关键符号可解析；两个 flow 均降级且 `steps: []`，因此本计划以源码窗口为流程事实，不拿 chain 冒充 flow。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.8-charter sym 'ApprovalClient'` → `internal/executor/approval.go:87`；`sym 'Manager.bindApproval'` → `internal/agentd/approval_client.go:29`；`sym 'Adapter.authorizeNativePermission'` → `internal/executor/opencode/adapter.go:817`；两次 `flow` → `degraded:true`、`missing:"基线没有 flows 段"`、`steps:[]`。 |
| 2026-09-10 | 基线构建与最小包测试通过；当前 `internal/approval` 只有 Ticket 0 空壳、无测试文件。 | `go build ./...` → exit 0、无输出；`go test ./internal/approval ./internal/agentd ./internal/executor/opencode -count=1` → `? github.com/Xsxdot/handoff/internal/approval [no test files]`、`ok github.com/Xsxdot/handoff/internal/agentd 196.387s`、`ok github.com/Xsxdot/handoff/internal/executor/opencode 18.194s`、exit 0。 |
| 2026-09-10 | 四个既有关键回归在迁包前通过，证明 Dispatch、Continue、非 OpenCode 旧路径、送达标记和 OpenCode adapter 负例是可执行基线。 | `go test ./internal/agentd -run '^(TestDispatchOpenCodeInjectsApproval|TestContinueRebindsApprovalClientAndDoesNotReuseOldGrant|TestNonOpenCodeKeepsOldApprovalPath|TestNormalDeliveryMarksTicketDelivered)$' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.538s`；`go test ./internal/executor/opencode -run '^(TestMapPermissionAskedWithApprovalClientDoesNotEmitPermission|TestAuthorizeNativePermissionReportsDeliveryFailure|TestAdapterBindApproval)$' -count=1` → `ok github.com/Xsxdot/handoff/internal/executor/opencode 0.086s`。 |
| 2026-09-10 | 图闸结构基线通过，warnings 为存量；仓内 `TestRepoContractGate` 当前失败于冻结物已知 dead-contract/dead-interface 与 legacy 命中，计划不把它写成绿。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.8-charter check` → `"fails": []`；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` → `"issues": null`、`"edgeIssues": null`；`go test ./cmd -run '^TestRepoContractGate$' -count=1` → `FAIL`，原始失败含 `契约违规 [dead-contract]`、`契约违规 [dead-interface]`、`legacy 命中 ... warn 149 条`。 |
| 2026-09-10 | 基线静态边界：旧 `taskApprovalClient` 仍在 agentd；`handlePermission`/`handleQuestion`/`waitQuestion` 位于 manager.go；OpenCode adapter 没有 agentd/permgate import；target 仍声明 `d_execution→d_orchestration`、`legacyBudget:0`、`ApprovalClient`。 | `rg` 命中 `internal/agentd/approval_client.go:23`、`manager.go:2109/3005/3091`；OpenCode import `rg` 无输出；`jq -c '.contracts[] | select(.from=="d_execution" and .to=="d_orchestration")' codegraph/target.json` → `legacyBudget:0`、`interfaces:["ApprovalClient"]`。 |
| 2026-09-10 | `internal/approval` 的 task-level 基线仅包含 Ticket 0 空壳且可编译，未有行为测试。 | `go test ./internal/approval -count=1` → `? github.com/Xsxdot/handoff/internal/approval [no test files]`，exit 0。 |
| 2026-09-10 | 计划文档已落在法定路径；Task 1 夹具、Task 3 冷/热 Resume seam 测试主体、精确测试名、Hook 映射、序列化边界和双向接缝审计已补齐。 | `rg -c '^~~~' docs/superpowers/plans/b233.8-plan.md` → `30`；占位符扫描命令 → `placeholder_scan=empty`；`git diff --check` → `exit 0`；`test -f docs/superpowers/plans/b233.8-plan.md` → `artifact=present`；`wc -l < docs/superpowers/plans/b233.8-plan.md` → `690`。 |
| 2026-09-10 | 计划、拍板 breakdown 与两份台账均已暂存；暂存区只含文档产出，无实现代码。 | `git add docs/superpowers/plans/b233.8-plan.md docs/superpowers/ledgers/2026-09-10-b233.8-plan-ledger.md docs/superpowers/specs/b233.8-breakdown.md docs/superpowers/ledgers/2026-09-10-b233.8-breakdown-ledger.md && git diff --cached --check` → exit 0；`git diff --cached --stat` → `4 files changed, 724 insertions(+), 4 deletions(-)`；`git status --short --branch` → `## cards/B233.8-charter-3` 与四项文档变更。 |
| 2026-09-10 | 首次文档提交已创建；按台账规则随后只 amend 一次把本行收入同批提交，保留本次 hash 作为历史读数，不追写 amend 后 hash。 | `git commit -m "docs(B233.8): add ApprovalClient migration plan"` → `[cards/B233.8-charter-3 93d36d20] docs(B233.8): add ApprovalClient migration plan`、`4 files changed, 725 insertions(+), 4 deletions(-)`、创建 plan 与 plan-ledger。 |
| 2026-09-10 | 实现节点在当前分支 `cards/B233.8-charter-4`、HEAD `3f8c2257` 开工；工作树初始干净。 | `git status --short --branch` → `## cards/B233.8-charter-4`。 |
| 2026-09-10 | Task 1 approval 包基线通过，但没有测试文件，说明 skeleton 尚未被行为断言覆盖。 | `go test ./internal/approval -count=1` → `? github.com/Xsxdot/handoff/internal/approval [no test files]`; `exit_code=0`。 |
| 2026-09-10 | Task 1 首次红测确认新接缝缺失：行为测试无法编译，因为 `Client.NoteDeliveryFailed` 尚未声明；这是新符号的编译红，不是断言结果。 | `gofmt -w internal/approval/client_test.go && go test ./internal/approval -run '^(TestClient\|TestNewClient)' -count=1` → `internal/approval/client_test.go:450:9: client.NoteDeliveryFailed undefined (type *approval.Client has no field or method NoteDeliveryFailed)`、`:456:9` 同类；`FAIL ... [build failed]`、`exit_code=1`。 |
| 2026-09-10 | Task 1 空壳阶段已进一步跑出行为断言红；`PolicySnapshot`、Request、Ack 和 delivery forwarding 均由 skeleton/空壳击穿，测试接缝有效。 | `gofmt -w internal/approval/client.go && go test ./internal/approval -run '^(TestClient\|TestNewClient)' -count=1` → `TestNewClientFillsAndPreservesTaskID: PolicySnapshot: approval: client skeleton`；其余行为含 `Request: approval: client skeleton`、`same-task delivery failure=[]`；`FAIL ... [exit_code=1]`。 |
| 2026-09-10 | Task 1 Client 最小迁移完成，九支新行为测试通过。 | `gofmt -w internal/approval/client.go internal/approval/client_test.go && go test ./internal/approval -run '^(TestClient\|TestNewClient)' -count=1` → `ok github.com/Xsxdot/handoff/internal/approval 0.607s`、`exit_code=0`。 |
| 2026-09-10 | Task 1 收尾包测试通过；新包当前生产实现已具备 Hooks、真实 Store/Hub 容错与四方法契约。 | `go test ./internal/approval -count=1` → `ok github.com/Xsxdot/handoff/internal/approval 0.711s`、`exit_code=0`。 |
| 2026-09-10 | Task 2 既有 agentd seam 基线通过；迁移前行为回归可执行。 | `go test ./internal/agentd -run '^(TestDispatchOpenCodeInjectsApproval\|TestContinueRebindsApprovalClientAndDoesNotReuseOldGrant\|TestNonOpenCodeKeepsOldApprovalPath\|TestNormalDeliveryMarksTicketDelivered\|TestApprovalClient)' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 1.734s`、`exit_code=0`。 |
| 2026-09-10 | Task 2 moved concrete seam 红测命中旧生产类型，证明测试确实经过 Manager.bindApproval 而非静态检查。 | `gofmt -w internal/agentd/approval_client_test.go && go test ./internal/agentd -run '^TestBindApprovalUsesMovedProductionClient$' -count=1` → `TestBindApprovalUsesMovedProductionClient: bindApproval 必须返回 *approval.Client，got *agentd.taskApprovalClient`；`FAIL ... [exit_code=1]`。 |
| 2026-09-10 | Task 2 组装缝切换到 `approval.NewClient` 后 moved concrete 测试与既有四类 agentd 回归通过。 | `gofmt -w internal/agentd/approval_client.go internal/agentd/approval_client_test.go && go test ./internal/agentd -run '^TestBindApprovalUsesMovedProductionClient$' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.050s`; `go test ./internal/agentd -run '^(TestDispatchOpenCodeInjectsApproval\|TestContinueRebindsApprovalClientAndDoesNotReuseOldGrant\|TestNonOpenCodeKeepsOldApprovalPath\|TestNormalDeliveryMarksTicketDelivered\|TestApprovalClient)' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 1.475s`; `exit_code=0`。 |
| 2026-09-10 | Task 3 新增冷/热 OpenCode Resume 组装断言均通过；断言覆盖 ResumeReq.Approval、Continue 重绑和快照任务身份。 | `gofmt -w internal/agentd/reconcile_test.go internal/agentd/resume_test.go && go test ./internal/agentd -run '^(TestContinueColdResumeInjectsApproval\|TestResumeTaskOpenCodeInjectsApproval)$' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.149s`、`exit_code=0`。 |
| 2026-09-10 | Task 3 完整 agentd 包测试通过，包含非 OpenCode 旧权限/提问路径与 Manager 恢复回归。 | `go test ./internal/agentd -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 187.266s`、`exit_code=0`。 |
| 2026-09-10 | Task 4 OpenCode adapter 完整测试通过；native permission 注入、RespondPermission 失败与 Ack 负例保持绿灯。 | `go test ./internal/executor/opencode -count=1` → `ok github.com/Xsxdot/handoff/internal/executor/opencode 18.605s`、`exit_code=0`。 |
| 2026-09-10 | 迁移后静态边界通过：agentd 生产代码无旧命名实现，OpenCode 无 agentd/permgate import；旧 permission/question/waitQuestion 与 POST /api/tasks/handleDispatch 均保留。 | `git diff --check && test -z "$(rg -n '^type taskApprovalClient|\\*taskApprovalClient' internal/agentd --glob '*.go' --glob '!**/*_test.go' || true)" && test -z "$(rg -n 'github.com/Xsxdot/handoff/internal/(agentd|permgate)' internal/executor/opencode --glob '*.go' || true)` → `exit_code=0`; `rg` → `manager.go:2109/3005/3091`、`server.go:633/1357`。 |
| 2026-09-10 | 迁移后全仓 Go 编译通过。 | `go build ./...` → 无输出、`exit_code=0`。 |
| 2026-09-10 | 迁移后卡视图图闸通过；输出仍仅含计划声明的存量 warnings，无 fails。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.8-charter check` → JSON 首字段 `"fails": []`、`exit_code=0`；warnings/legacyHits 为既有存量，未修改图文件。 |
| 2026-09-10 | 迁移后图结构验证通过，target 契约方向与预算未变。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` → `{"edgeIssues":null,"issues":null}`、`exit_code=0`；`jq -c '.contracts[] | select(.from=="d_execution" and .to=="d_orchestration")' codegraph/target.json` → `{"from":"d_execution","to":"d_orchestration","legacyBudget":0,"interfaces":["ApprovalClient"],...}`、`exit_code=0`。 |
| 2026-09-10 | 变异前唯一性检查通过，AutoAllow 比较符在生产 Client 仅命中一处。 | `test "$(rg -F -n 'if verdict.Action == permgate.AutoAllow {' internal/approval/client.go | wc -l)" -eq 1 && rg -F -n ...` → `internal/approval/client.go:120: if verdict.Action == permgate.AutoAllow {`、`exit_code=0`。 |
| 2026-09-10 | AutoAllow 取反变异编译通过，相关行为测试立即翻红，证明变异实际命中守卫；范围测试亦有 6 个 `--- FAIL` 标记。随后已恢复原比较符。 | 变异为 `if verdict.Action != permgate.AutoAllow {`；`go build ./...` → 无输出、`exit_code=0`；`go test ./internal/approval -run '^TestClientAutoAllowHasNoTicket$' -count=1` → `auto allow result ... Status:pending`、`FAIL`、`exit_code=1`；`go test ./internal/approval -run '^(TestClient\|TestNewClient)' -count=1 2>&1 | grep -c '^--- FAIL'` → `6`、`exit_code=0`；恢复通过 apply_patch。 |
| 2026-09-10 | 变异恢复后的格式、diff 与 approval 包回归通过。 | `gofmt -w ... && gofmt -l ... && git diff --check && go test ./internal/approval -count=1` → `gofmt -l`/`git diff --check` 无输出；`ok github.com/Xsxdot/handoff/internal/approval 0.699s`、`exit_code=0`。 |
| 2026-09-10 | 变异恢复后最终相关 seam 回归通过；六条 Manager/生命周期断言与三条 OpenCode boundary 断言均绿。 | `go test ./internal/agentd -run '^(TestDispatchOpenCodeInjectsApproval\|TestContinueRebindsApprovalClientAndDoesNotReuseOldGrant\|TestContinueColdResumeInjectsApproval\|TestResumeTaskOpenCodeInjectsApproval\|TestNonOpenCodeKeepsOldApprovalPath\|TestNormalDeliveryMarksTicketDelivered\|TestBindApprovalUsesMovedProductionClient)$' -count=1 && go test ./internal/executor/opencode -run '^(TestMapPermissionAskedWithApprovalClientDoesNotEmitPermission\|TestAuthorizeNativePermissionReportsDeliveryFailure\|TestAdapterBindApproval)$' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 0.735s`、`ok github.com/Xsxdot/handoff/internal/executor/opencode 0.089s`、`exit_code=0`。 |
| 2026-09-10 | 仓内契约门禁实现后对照仍为冻结物已知失败；未出现新的违规，且没有修改图配置掩盖它。 | `go test ./cmd -run '^TestRepoContractGate$' -count=1` → `契约违规 [dead-contract] 契约 d_execution→d_orchestration ...`、`契约违规 [dead-interface] ... "ApprovalClient" 在 d_execution 中不存在 ...`、`legacy 命中: map[...]，warn 149 条`；`FAIL github.com/Xsxdot/handoff/cmd`、`exit_code=1`。 |
| 2026-09-10 | 提交前工作树检查通过；无 go.mod/go.sum、codegraph 或越界文件变更，静态边界与 diff 检查无输出。 | `git diff --check && test -z "$(rg -n '^type taskApprovalClient|\\*taskApprovalClient' internal/agentd --glob '*.go' --glob '!**/*_test.go' || true)" && test -z "$(rg -n 'github.com/Xsxdot/handoff/internal/(agentd|permgate)' internal/executor/opencode --glob '*.go' || true)" && test -z "$(git status --short | awk '$2 ~ /go\\.mod|go\\.sum|^codegraph\\// {print}')" && git status --short --branch` → `## cards/B233.8-charter-4`、仅计划文件集文档/实现/测试变更、`exit_code=0`。 |
| 2026-09-10 | 实现提交已创建；本行保留提交时的历史 hash，随后仅允许一次 amend 将本行收入同批提交，不追写 amend 后 hash。 | `git commit -m "fix(B233.8): migrate ApprovalClient implementation out of agentd"` → `[cards/B233.8-charter-4 22d5adf5] fix(B233.8): migrate ApprovalClient implementation out of agentd`、`8 files changed, 1090 insertions(+), 284 deletions(-)`、`create mode 100644 internal/approval/client_test.go`、`exit_code=0`。 |
