# B233.27 spec 台账

- 2026-09-14：卡从待办移入 spec。来源 d9b0228d 复审 P1。
- 2026-09-14：读 `AdmitFrozen` 约 :668 只核请求 vs 登记；`handleDispatch` :608 随后本机 `Dispatch`。
- 2026-09-14：`TestIsLocalMachine` 断言 `linux-01` 不是本机别名。`coordinator_pty.go:50-54` 写明现网登记名是 targets 键，要用 `IsSelfTarget`。
- 2026-09-14：用户对「本机身份用 CanonicalTarget，不用单独 IsLocalMachine」回复 OK。
- 2026-09-14：spec 初稿落盘，状态待批准。定级 L1。
- 2026-09-14：用户原话「批准」。头部回写已批准。L1 同文件兼 plan。
- 2026-09-14：TDD 先红：TestB23327FrozenRemote / Ordinary 在无闸时 200，加 CanonicalTarget 闸后 400 且 Dispatch=0。变异 `canon != ""`→`== ""` 该反面变 200；还原后 TestB23327 绿。
- 2026-09-14：`go build ./...` 绿；`go test ./internal/agentd` 绿。生命周期夹具默认机从 b23310-loop 改为 local（第二 listener 不是 IsSelfTarget）。
- 2026-09-14：独立审阅 Minor：拆 `errReceiverNotCarrierMachine`（HTTP 不得再带「小队角色不符」）；`handleDispatch` 函数头写入闸。spec 实现决定 #2 回写。
