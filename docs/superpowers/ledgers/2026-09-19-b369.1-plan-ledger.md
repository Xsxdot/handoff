# B369.1（S1）plan 节点台账

节点：plan（卡 B369.1「S1 配对 wire 回归与孪生样本」）。分支 `cards/B369.1-charter`。法定产出：`docs/superpowers/plans/b369.1-plan.md`。本节点只出计划文档，不写实现代码。

## 一、基线复核（本轮实测）

| 命令 | 原始结果 |
| --- | --- |
| `go version` | `go version go1.26.1 linux/amd64` |
| `go test ./internal/proto/... -count=1` | `ok github.com/Xsxdot/handoff/internal/proto 0.004s`（EXIT=0） |
| `go build ./...`（根） | EXIT=0 |
| `go build ./...`（`mobile/` 模块） | EXIT=0 |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.066s`（EXIT=0） |
| `codegraph check --view cards-B369-charter` | `fails=[]`（EXIT=0） |
| `codegraph validate --view cards-B369-charter` | `issues=null`、`edgeIssues=null`（EXIT=0） |
| `codegraph check`（基线） | `fails=[]`（EXIT=0） |
| `test -f mobile/bind/pairing_fixture.json` | NO（不存在） |
| `grep rapid\|gopter\|testing/quick` | 无命中（本卡用标准库 `math/rand`） |

## 二、图覆盖债（`codegraph sym --view cards-B369-charter`）

- 命中：`PairBundle`/`PairRelay`/`PairMachine`/`PairTicket`/`EncodePairBundle`/`DecodePairBundle`。
- **未命中**：`PairVersion`、`ErrPairVersion`、`ErrPairMalformed`（无近似候选；回落 grep 确认定义于 `internal/proto/pairing.go:24/33/35`）。本卡不补图（新增全在 `_test.go`，测试不计图边），债记入 plan §2。

## 三、判据先在基线跑（关键）

- **共享金样本规范字节**：本轮以临时探针在 `internal/proto` 内跑 `EncodePairBundle(pairFixture())`，实测输出紧凑 JSON `len=588`（原文已写入 plan §5.2）。
- **属性测试可跑**：临时探针以 `seed=0xB369`、500 轮随机 bundle 跑通「Encode∘Decode∘Encode 逐字节恒等 + 字段/nil-ness 恒等」（含空/非空 Relay、relay/直连两形态、1..4 机、有/无 Ticket、零值/非零时间）。
- **变异可红**：原地把 `EncodePairBundle` 改成 `b.Version = PairVersion + 1` → `TestPairBundleRoundTrip` FAIL `proto: 配对载体版本不受支持: 2`、`TestPairBundleRoundTripProperty` FAIL `decode: ... raw={"v":2,...}`。`git checkout -- internal/proto/pairing.go` 还原后 `go test ./internal/proto/ -count=1` → ok。
- **拒绝面双重分型**：7 条畸形载荷实测均 `errors.Is(err, ErrPairMalformed)`；`v=999`/`v=0` 均 `ErrPairVersion`（互斥可判）。
- **样本护栏判据正确**：文件不存在时 `os.ReadFile` 必败 → T1 的 `TestPairingFixtureMatchesCanonicalBytes` 在 T2 落文件前必红（先红后绿成立）。

## 四、试错记录

- 探针首次编译失败（`got["machines"].(json.RawMessage)` 对 `json.RawMessage` 值取接口非法）→ 删掉该无用断言。
- 变异验证的临时文件一度试图用 `/tmp` 备份，被协调者拒（`/tmp` 在任务范围外）；按其指引改为「原地改 → 跑红 → `git checkout --` 还原 → 跑绿」，并用 `$TMPDIR` 约束写入 plan §4.3。已按此重跑，工作树干净。
- 临时探针 `internal/proto/zzz_probe_test.go` 与 `pairing.go` 变异均已还原/删除；`git diff --stat internal/proto/pairing.go` 为空。

## 五、提交事实（历史读数）

```
$ git add docs/superpowers/plans/b369.1-plan.md docs/superpowers/ledgers/2026-09-19-b369.1-plan-ledger.md && git commit -m "plan(B369.1): S1 配对 wire 回归与孪生样本实现计划"
```

（随后按红线只 amend 一次把本台账段收进同批提交——amend 会换 hash，此为 git 事实，不复写历史读数。）
