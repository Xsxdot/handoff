# B358.1 plan 节点台账（2026-09-12）

- **节点**：charter plan（L3 重档——子系统执行者依契约自写实现计划，协调者只审）
- **执行者**：B358.1（d_collab）子系统执行者
- **工作目录**：`/Users/xushixin/workspace/handoff`，分支 `cards/B233.1-charter-7`，HEAD `6a45231d`（contract 补签轮），Ticket 0 骨架在工作树
- **产出物**：`docs/superpowers/plans/b358.1-plan.md`（法定路径）
- **本节点纪律**：不写实现代码、不做 git 操作、不碰卡；只新建计划与本台账

---

## 一、输入加载与判据来源

- Skill `charter:plan` 已加载（正文执行：格式铁律、五项检查、自审三查、红线）。
- 任务书：`docs/superpowers/specs/b358-breakdown.md` §3.2（S1 四段式）+ §2.4 澄清 1/2；contract `b358-contract.md` §3.5/§3.7/§4.1（条 6/7/13）/§4.4（条 34/35）/§8.2/§8.7 + 补签轮 §3.8/§3.9/§4.6–4.8（R2 席位事件已落码，S1 词表清理不触 `EvDriverSeatBound`）；spec `b358.md` §4.1/§4.2/§4.5、§6 接缝 #1。
- 验收判据逐条继承 breakdown §3.2 ③，未降级改写；缺陷族九条的答案映射进计划各任务验收栏。

## 二、图查证（有图先查图）

`codegraph sym` 逐符号执行，全部未命中（与 breakdown §6 探针一致），本节点复跑记录：

```
sym collab.Service            → Error: 不在图中（近似候选: []）
sym collab.Service.Send       → Error: 不在图中
sym collab.Service.Pointer    → Error: 不在图中
sym room.Resolve              → Error: 不在图中
sym room.VerifyWriter         → Error: 不在图中
sym collab.consumeRoomMentions→ Error: 不在图中
```

结论：计划一律用 `file#Symbol` 源码锚；S1 新增/修改符号清单落计划 Task 4，交 integrate/recon 补进 `codegraph/diffs` 视图。**图覆盖债无新增类别**（沿用 breakdown §6 的整卡欠账）。

## 三、基线读数（判据先在基线跑，2026-09-12 亲跑）

| 命令 | 结果 |
| --- | --- |
| `go build ./...` | 退出 0 |
| `go test ./internal/collab/... -count=1` | `ok internal/collab`（1.303s）、`ok internal/collab/room`（0.441s） |
| `grep -rn "KindAllowed" internal/ cmd/ --include="*.go" \| grep -v NotAllowed \| wc -l` | 3（room.go 注释、room.go 函数、service.go:81 调用点） |
| `go test ./cmd -run TestRoom -count=1` | ok |
| `go test ./internal/agentd -run 'TestRoomMessages\|TestRoomSend\|TestRoomRead' -count=1` | ok |

代码级基线事实（读码确证，非记忆）：

- `Service.Send`（internal/collab/service.go:80-106）今天**不查会话本体**——归档会话发言会成功（`TestSessionArchiveReadOnly` 只覆盖 JoinCard，breakdown §2.4 澄清 2 成立）。
- `room.Resolve` 会话分支恒可写（internal/collab/room/room.go:66-71）；归档判定归门面是契约 §3.7 冻结语义。
- `VerifyWriter` 唯一生产调用点 service.go:91；`KindAllowed` 唯一调用点 service.go:81。二者在 agentd/cmd **零生产引用**——删除不外溢。
- `Room.ParentSession` 只在 room.go 内部（字段、Resolve 父块、VerifyWriter relay 分支）——矩阵废止后是死代码，可随之删除。
- `consumeRoomMentions` 过滤条件 `ev.CardID != roomID`（service.go:124）——会话房间事件 `CardID==""`，故 B287 今天不覆盖会话房间。
- `Store.RecordRoomMessage`（internal/ledger/rooms.go:14-30）：cardID 空串=群级无卡事件；会话房间消息经 `r.CardID==""` 天然落无卡事件（contract 条 33）。
- `Store.ArchiveSession`（internal/ledger/sessions.go:137-152）：UPDATE+appendEvent 无「已归档短路」——**重复归档每次都落一条 `EvSessionArchived`**。contract 条 6「幂等；落恰一条」的重复归档读法存疑 → 岔口（见 §七-1）。
- Facade（internal/ledger/api/api.go:128-130）：`JoinCardToSession` 只翻译 `ErrNotFound`，`ledger.ErrBadState`（已属会话/会话已归档，sessions.go:165/173）原样穿透——collab 侧无 `ErrBadState` 哨兵（client.go 只冻结 ErrNotFound）→ S4 映射载体需拍板（见 §七-3）。
- pointer 豁免两条上游亲读确证：`cmd/card_dispatch.go:434`（`roomPointer` swap 缝，生产=直调 `Service.Pointer`）与 `internal/agentd/server.go:2951`（`roomNarrator.Say`）；server.go 注释明言指针行断言一律存在式不数行数。
- `cmd/room.go` roomSendCmd：`--kind user` + `--cli/--session` 的拒绝发生在 CLI flag 层（room.go:126-133）、先于 `svc.Send`（:149）——S1 改 Send 语义后 `TestRoomSendUserRejectsSeatFlagsWithoutSideEffects` 仍绿（拒绝先于触达）。

## 四、S1 波及面红窗清单（S1 语义落地即被打断、但不在 S1 有界文件集的测试）

S1 有界文件集（breakdown §3.2 ④）= internal/collab/service.go、room/room.go、service_test.go、sessions_test.go、readmodel_test.go、room/delivery_gate_test.go。以下外部测试以「旧房间发言成功」为断言/夹具，S1 落地即红；改写责任按 DAG 归 S4（agentd「对应 _test.go」）与 S5（cmd/room_test.go）——全清单见计划「已知红窗」节：

- `internal/agentd/roomsapi_test.go` 10 支：TestRoomsListEndpoint、TestRoomsListUnreadAndAttachProjection、TestRoomMessagesEndpoint、TestRoomSendEndpoint、TestRoomReadEndpoint、TestInboxThreeSources、TestInboxMentionSource、TestInboxRefIDShapes、TestInboxGoldenKeyShapes、TestInboxIntegrationSmoke（行号映射见计划）。
- `cmd/room_test.go` 6 支：TestRoomSendLandsRoomMessageWithUserKind、TestRoomSendCoordinatorAcceptsExplicitSeatFlags、TestRoomSendCoordinatorKindUsesGrokHostSession、TestRoomSendCarriesRefAndMention、TestRoomReadStdoutSeqPrefix、TestRoomListSortedByActivity。
- 存活确认：TestRoomSendErrMapping（四段断言在 S1 语义下全部仍真）、TestRoomSendUserRejectsSeatFlagsWithoutSideEffects（CLI flag 层先拒）。

## 五、设计裁决（计划的任务骨架依据）

1. **Send 重构形态**：pointer 拒收（保留 ErrKindNotAllowed，其唯一残余生产点）→ `room.Resolve`（存在性，不存在的卡仍 ErrNoRoom）→ 非会话房间一律 `ErrReadOnly`（P5 只读归档；错误语义=「房间在、只是归档」，缺陷族 2）→ 会话分支 `sendToSession`：GetSession（ErrNotFound→ErrNoRoom）→ 归档只读 → 书写者执法 → 发布（无卡事件）→ B287 触发。
2. **VerifyWriter 终形** `VerifyWriter(writers []string, actor string) error`：函数体只剩 actor 非空 + 成员执法（breakdown ③ 源码审查项的机器化 = room 包 AST 守卫，追加进 delivery_gate_test.go）。writers 由门面 `sessionWriters` 组装（显式成员 ∪ 会话内卡当前席位）——契约 §3.7「Resolve 只解析形态不查会话表」决定成员集只能门面送。**T2 落过渡形 `VerifyWriter(actor string)`（T2 删矩阵时成员执法未接入）、T3 落终形**——两段签名都写死在计划里，AST 守卫对两形皆绿。
3. **测试迁移策略**（T1 包内 17 处）：会话语义类夹具迁会话房间；旧房间读面对质类（History/Unmerge）改用 `Service.Pointer` 夹具（顺带锁「旧房间读面不断」）；Pending 卡房间读侧过滤用 `lc.RecordRoomMessage` 原始行夹具（发言入口已死，测的是读侧）；8 支矩阵锁死测试删除并注明替代落点（T2 kind 自由 / T3 成员执法+换绑剥权重立）。
4. **contract 条 6/7/13 吸收**：条 7（归档 Join 拒 + 不落事件）与条 13（卡终态不归档会话）补门面级断言；条 6 单次归档「恰一条」补断言，重复归档读法记岔口。
5. **不动清单**：internal/ledger、internal/proto、internal/agentd、cmd 生产文件零触碰；ListRooms 只读标记不动（S6 退役面）；proto.RoomMsg* 词表常量不删（wire 兼容）；wakeconsumer 的 RoomMsgUser 残留归 S3。

## 六、与 breakdown §3.2 ③ 的覆盖核对结论（逐条）

| breakdown ③ 判据 | 计划吸收 |
| --- | --- |
| `go test ./internal/collab/... -count=1` 退 0 | Task 4 收口命令 ①② |
| 归档会话发言 → ErrReadOnly | Task 1 TestSessionArchiveSendReadOnly |
| 未归档会话成员（显式/席位）发言成功 + actor 落账可查 | Task 3 TestSessionMembershipEnforcement（assertActor 落账断言） |
| 非成员（含会话外卡席位）→ ErrNotWriter | Task 3（user:outsider / cli:codex#outside 两反例） |
| 换绑后旧席位拒、新席位成（同一会话前后两条） | Task 3 TestSessionRebindRevokesOldSeatWriter（重立自被删的卡房间形态测试） |
| 旧卡房间 user → ErrReadOnly；同房 Pointer 成功落账（存在式） | Task 1 TestSendOldRoomsArchivedReadOnly |
| 会话房间 user 回复 → 提及消费（EvMessageConsumed 存在）+ mention 源不再返回 | Task 1 TestSendConsumesRoomMentions 重写（存在式 + Mentions 空） |
| `go build ./...` 退 0 且 grep KindAllowed 零命中 | Task 2 实现 + Task 4 复跑 |
| VerifyWriter 不再含按 kind 分权分支（源码审查项） | Task 2 AST 守卫 TestVerifyWriterHasNoKindBranches（机器化，强于人工审查——调整已注明理由） |
| room list 读面（ListRooms/History）旧房间仍可读 | Task 1 Pointer 夹具迁移 + 既有 ListRooms 假夹具测试不动 + Task 4 复跑 |
| 真实多进程并发发言 → 真机清单 #8 | 计划「明确不做/移交」注明，非 S1 机内判据 |

结论：③ 逐条已吸收，无降级；一条机器化调整（源码审查项→AST 守卫）已在计划注明。

## 七、岔口与缺口（「待拍板」，已写进计划岔口节）

1. **contract 条 6 的「幂等 + 落恰一条」在重复归档时的读法**。Ticket 0 `Store.ArchiveSession` 无已归档短路：N 次归档落 N 条 `EvSessionArchived`。若按「重复归档也不落第二条」读法，需在门面 `Service.ArchiveSession`（sessions.go，**出 S1 有界文件集**）或 Store 加短路。**待拍板**：a) 维持「每次归档动作恰一条事件」读法（本计划现行）；b) 批准把 `Service.ArchiveSession` 的已归档短路扩进 S1 有界文件集（+1 函数 +1 测试）。建议 b（timeline 不出重复 archived 行，且短路在门面属会话语义）。
2. **红窗处置**（§四清单，agentd 10 + cmd 6）：S1 落地到 S4/S5 落地之间两包测试红。**待拍板**：a) 接受窗口，S1 台账记红清单（本计划已内建清点与复核步骤），S4/S5 各自收口——建议此项；b) 把旧面测试改写划给 S1（扩有界文件集，且与 S4/S5 的端点语义改造冲突）。
3. **（信息项，归 S4）**契约 §9「JoinCardToSession 门面错误用 ErrBadState」——collab client 接口无 ErrBadState 哨兵，ledger.ErrBadState 原样穿透（api.go:128 只翻 NotFound）。S4 做 HTTP 映射时需拍板载体（gateway 侧 errors.Is(ledger.ErrBadState) 或回 contract 加哨兵）。不阻塞 S1。

## 八、产出与自审

- **计划文档已写毕**：`docs/superpowers/plans/b358.1-plan.md`（2026-09-12）。
- 结构：§0 任务书与硬边界（含 0.4 已知红窗 16 支清单）→ §1 基线判据 → §2 终局形状与中间态声明 → Task 0–4（各含文件范围/Interfaces/红绿步骤/全码/验收栏）→ §8 明确不做 → §9 序列化边界与接缝覆盖双向表 + 内部锁 3 条声明 → §10 缺陷族九族映射 → §11 岔口 3 条 → §12 五项自审与三查 → §13 收口判据 → 实现者纪律注入段。
- **占位符扫描**：初稿检出 5 处风险并当场修正——「同上」×3（改逐行显文）、cmd 测试名笔误 1 处（`TestRoomSendCarriesRefAndMention`）、Task 2 Consumes 措辞 1 处（改为显列符号面）；终稿 grep `TBD|待定|适当|同上|同 Task|TODO` 无实质命中。
- **自审三查**：
  - spec 覆盖：breakdown §3.2 ① 契约引用逐条落 task（§3.5→T1/T3、§3.7→T1/T2、条 6/7/13→T1、条 34/35→T1/T2、§8.2→T1、§8.7→T2、澄清 1/2→T1/T3、P4/P5→夹具记法与 T1）；③ 判据逐条对照见本台账 §六，无降级。
  - 占位符：无（见上）。
  - 跨 task 签名一致：`VerifyWriter` 三形态（旧 `(r,kind,actor)` → T2 `(actor)` → T3 `(writers,actor)`）在各任务显式给出、调用点同步替换、AST 守卫对三形态判据逐形核对；`sessionWriters` 返回 `([]string, error)` 与调用点一致。
- **五项检查**：缺陷族映射（§10）、序列化边界（零新字段+既有断言保留）、上下文预算（任务文件集 ⊆ breakdown ④ 有界集，AST 守卫追加进已列名的 delivery_gate_test.go 不新增文件）、类型标注（逻辑型机内闭环 + 真机显式移交）、接缝覆盖双向（缝→测试表 + 测试→缝表 + 3 条内部锁声明）——全部通过。
- **岔口（待拍板）**：①条 6 重复归档读法（建议批准 facade 短路扩有界集）；②红窗处置（建议接受窗口由 S4/S5 收口）；③ErrBadState 映射载体（信息项归 S4）。
- **节点纪律遵守**：未写实现代码、未做 git 操作、未碰卡、未动无关脏文件（web/src/app/task/Composer.test.tsx 与未跟踪文件原样）。
