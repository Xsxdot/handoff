# B233.6 plan 台账

日期：2026-09-07。节点：charter implementation。工作分支：`cards/B233.6-charter-7`。

| 2026-09-07 | 实现节点接管当前工作树；当前分支为有效基线要求的 `cards/B233.6-charter-7`，工作树初始干净；本执行回合将在计划白名单内实现 S1—S3，S0 仍由协调者负责。 | `pwd && git status --short --branch` → `/root/.handoff/worktrees/7bf4cf3f`；`## cards/B233.6-charter-7`；无状态行。 |
| 2026-09-07 | 计划台账文件存在于 `docs/superpowers/ledgers/2026-09-07-b233.6-plan-ledger.md`；spec 中引用的 `docs/superpowers/ledgers/2026-09-06-b233.6-spec-ledger.md` 当前不存在，未创建该历史文件。 | `rg --files docs | rg 'b233\\.6|2026-09-06.*ledger|ledger.*233'` → 命中 `2026-09-07-b233.6-plan-ledger.md`、`2026-09-07-b233.6-contract-ledger.md` 等；无 `2026-09-06-b233.6-spec-ledger.md`。 |

## 过程

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-07 | 当前工作树干净；本轮 HEAD 为 `05984ed0a8aa2b612019e411db9f6f0d4d0dc90e`；法定 plan 台账此前不存在。 | `command -v codegraph || true; git rev-parse HEAD; git branch --show-current; git status --short --branch; test -f docs/superpowers/ledgers/2026-09-07-b233.6-plan-ledger.md; printf 'ledger_exists_exit=%s\\n' "$?"` → `/root/.local/bin/codegraph`；`05984ed0a8aa2b612019e411db9f6f0d4d0dc90e`；`cards/B233.6-charter-6`；`## cards/B233.6-charter-6`；`ledger_exists_exit=1`。 |
| 2026-09-07 | 本节点只产出 `docs/superpowers/plans/b233.6-plan.md` 与本台账，不实现源码；breakdown 已拍板 P1—P4 取 A、P5 不扇出，S0—S3 在本 plan 内串行执行。 | `sed -n '1,40p' docs/superpowers/specs/b233.6-breakdown.md` → 文件头写明有效基线 `cards/B233.1-charter-7`、角色边界为 plan/拆解不写实现，以及待拍板清单中 P1=A、P2=A、P3=A、P4=A、P5=不扇出。 |
| 2026-09-07 | `d_ledger` 是 best 图中的 logic 领域，包含 `internal/ledger`、`internal/ledgermirror`、`internal/ledgerstep`；context 输出被截断且报告 `unscannedEntries=5`、`fociTruncated.total=58`，因此不把 context 当完整调用面。 | `codegraph --repo . --view cards-B233.6-charter context d_ledger` → 退出 0；日志 `domain=d_ledger`、`kind=logic`、`packages=4`；JSON 含 `unscannedEntries: 5`、`fociTruncated.total: 58`、`actual.byCurrentDomain d_ledger containers=14`；工具提示 `Warning: truncated output`。 |
| 2026-09-07 | 图未覆盖 `Dispatcher.ViaTemplate` 的文档锚，后续必须以源码/grep 作为事实并在计划中记录图覆盖债。 | `codegraph --repo . --view cards-B233.6-charter sym internal/ledgerstep/dispatch.go#Dispatcher.ViaTemplate` → `Error: 符号 "internal/ledgerstep/dispatch.go#Dispatcher.ViaTemplate" 不在图中（图未覆盖或名字有误）；近似候选: []。确认图未覆盖时回落 grep，并把该符号记入本节点产出物的「图覆盖债」小节`。 |
| 2026-09-07 | 图中已命中的关键接口签名：`ViaTemplate`、`TasksOf`、`AllTaskLinks`、`RecordDispatch`、`AppendMirroredEvent`、`MirrorWatermark`、`Mirror.Run`、`automationWakeEvent`、`Client.Reply`、`Client.StreamEventsOnce`；其中 `ViaTemplate`/`AppendMirroredEvent`/`Mirror.Run` 等标记为 `moved` 或 `modified` 的图锚，计划引用当前源码签名。 | `codegraph ... sym ...`（上述 10 个节点）→ 各命令退出 0；原始结果逐项给出文件/行号与签名，例如 `ViaTemplate`=`func (d *Dispatcher) ViaTemplate(ctx context.Context, c ledger.Card, req TemplateDispatch) (DispatchResult, error)`、`AppendMirroredEvent`=`func (s *Store) AppendMirroredEvent(cardID string, ev MirroredEvent) (bool, error)`、`Client.Reply`=`func (c *Client) Reply(ctx context.Context, taskID, ticketID, answer string) error`。 |
| 2026-09-07 | `flow` 只把 `Dispatcher.ViaTemplate`（49 步、degraded=false）与 `Client.Reply`（7 步、degraded=false）作为有序流程依据；`Mirror.Run` 与 `automationWakeEvent` 返回 `degraded=true`、`steps=[]`，后续计划必须以源码核对，不能拿 chain 冒充流程图。 | `codegraph --repo . --view cards-B233.6-charter flow n_ledgerstep_Dispatcher_ViaTemplate` → `degraded=false steps=49 callers=2 channels=1`；`flow n_client_Client_Reply` → `degraded=false steps=7 callers=1 channels=1`；`flow n_ledgermirror_Mirror_Run` → `degraded=true steps=0`；`flow n_agentd_automationWakeEvent` → `degraded=true steps=0`。 |
| 2026-09-07 | 基线金样本已绿；S2/S3 新增测试族当前不存在。 | `go test ./internal/ledger/ -run '^TestAppendMirroredEventWorkflowEnvelopeGolden$' -count=1` → `ok github.com/Xsxdot/handoff/internal/ledger 0.080s`；`go test ./internal/ledger/ ./internal/ledgermirror/ -run '^TestB2336' -count=1` → 两包均 `ok ... [no tests to run]`；`go test ./internal/agentd/ ./internal/client/ ./internal/ledgerstep/ -run '^TestB2336' -count=1` → 三包均 `ok ... [no tests to run]`。 |
| 2026-09-07 | S1 当前既有 receiver/card-step/dispatch 回归与 S3 既有 Reply/RelayAnswer/NodeStep 回归在基线通过。 | `go test ./internal/ledgerstep/ ./internal/agentd/ ./internal/client/ -run 'Test(B2336|Dispatch|CardStep|Receiver)' -count=1` → `ok`：ledgerstep `0.088s`、agentd `17.151s`、client `0.012s`；`go test ./internal/client/ ./internal/agentd/ -run 'Test(B2336|Reply|RelayAnswer)' -count=1` → client `0.145s`、agentd `0.810s`；`go test ./internal/ledgerstep/ -run 'Test(NodeStep|B2336)' -count=1` → `ok ... 3.650s`。 |
| 2026-09-07 | 基线全仓编译通过，标准输出为空。 | `go build -o /dev/null .` → 退出码 0，原始输出为空。 |
| 2026-09-07 | 视图图校验基线 `validate` 通过；`check` 仍有两项存量 d_execution→d_orchestration 契约失败，计划验收须保留为基线已知失败而非归因本卡。 | `codegraph --repo . --view cards-B233.6-charter validate` → JSON `issues: null`、`edgeIssues: null`、`containers: 300`、`nodes: 4499`、`unscannedEntries: 5`；`codegraph --repo . --view cards-B233.6-charter check` → `fails` 为 `dead-contract`（d_execution→d_orchestration 无活跃边）与 `dead-interface`（接口 `ApprovalClient` 不存在），末行 `Error: 契约对照发现 2 处违规`。 |
| 2026-09-07 | S0 三领域 context 命令均实际成功；d_gateway 是 boundary、d_transport 是 boundary；各输出带图截断/未扫描提示，不能据此宣称完整调用面。 | `context d_gateway` → 退出 0，日志 `domain=d_gateway`、`kind=boundary`、`packages=1`、`unscannedEntries=5`、`fociTruncated.total=125`；`context d_transport` → 退出 0，日志 `domain=d_transport`、`kind=boundary`、`packages=4`、`unscannedEntries=5`、`fociTruncated.total=56`；两次均有 `Warning: truncated output`。 |
| 2026-09-07 | 当前图存在陈旧调用面：`who-calls AppendMirroredEvent` 把 `Mirror.subscribe` 显示为旧 `target config.Target` 签名，而源码是 `(*client.Client)`；`Mirror.Run`/`automationWakeEvent` 的 who-calls 为空且都报告 5 个未扫描入口，不能当作无调用方。 | `codegraph --repo . --view cards-B233.6-charter who-calls ...` → `AppendMirroredEvent` 节点 `Mirror.subscribe` file `internal/ledgermirror/mirror.go:455` signature `func (m *Mirror) subscribe(ctx context.Context, link ledger.TaskLink, target config.Target)`；`Mirror.Run` 与 `automationWakeEvent` 仅返回自身节点，均有 `warning: 基线仍有 5 个未扫描入口`。 |
| 2026-09-07 | 发现冻结输入存在未决接缝：账本事件已有 `SourceTarget/SourceTask/SourceSeq`，但 `internal/ledger/api/api.go#eventWire`、`internal/agentd/ledgerapi.go#ledgerEventWire` 与 `internal/proto/ledger.go#LedgerEvent` 当前均未投影这些字段；`wakeconsumer` 仅收到 `proto.LedgerEvent`，无法按 contract §2.4/S3 要求同时检查 source identity 与 envelope 的 Node/Attempt。breakdown S3 有界集合也未列这三个 wire 文件。 | `rg -n 'LedgerEvent|SourceTarget|SourceTask|SourceSeq' internal/agentd internal/proto internal/ledger` → 命中 `internal/ledger/api/api.go:147`、`internal/agentd/ledgerapi.go:111` 的投影函数，`internal/proto/ledger.go:120` 的 `LedgerEvent` 仅含 `Seq/CardID/Type/Actor/Payload/CreatedAt`；`sed -n '1,180p' internal/agentd/ledgerapi.go` → `ledgerEventWire` 只赋上述 6 个字段；`sed -n '1,220p' internal/proto/ledger.go` → `LedgerEvent` 无 source/node/attempt 字段。 |
| 2026-09-07 | 协调者裁决不扩展 S3 文件集：S2 在 store 层把 `node/attempt` 冻结进 task_mirrored payload，`wakeconsumer` 经进程内 `autoLedger.EventsFromAsc` 直接取得原 payload；HTTP `proto.LedgerEvent.Payload` 是 `json.RawMessage` 原样透传，因此不纳入 `internal/ledger/api/api.go`、`internal/agentd/ledgerapi.go`、`internal/proto/ledger.go`。 | 协调者回复原文要点：`wakeconsumer.go:106` 走进程内 `autoLedger.EventsFromAsc`；`proto/ledger.go:120-127` 的 Payload 原样透传；要求按 breakdown 已声明集合写 plan。 |
| 2026-09-07 | 有效基线分支存在且可解析；当前分支相对该基线包含历史上多批次改动，S0 只能在实现阶段按 S1—S3 文件白名单过滤，不能把整份分支差异直接当作本卡实现差异。 | `git show-ref --verify refs/heads/cards/B233.1-charter-7 && git diff --name-only cards/B233.1-charter-7...HEAD` → `94246fc8a2acc7996c254cb6b473c7c9b5aef225 refs/heads/cards/B233.1-charter-7`；随后输出从 `CHANGELOG.md`、`cmd/...`、`codegraph/...`、`docs/...`、`internal/agentd/...`、`internal/client/...`、`internal/ledger/...`、`internal/ledgermirror/...`、`internal/ledgerstep/...` 等开始的长文件清单，命令退出 0。 |
| 2026-09-07 | 首次尝试以单个大补丁写计划时，补丁工具因代码块行缺少 `+` 前缀拒绝，未产生文件；随后拆分补丁成功写入计划。 | `apply_patch` → `apply_patch verification failed: invalid hunk at line 111, 'type TaskLink struct {' is not a valid hunk header.`；后续两次 `apply_patch` 均返回 `{}`。 |
| 2026-09-07 | 法定计划已落盘，包含 S0—S3、每段文件集/Interfaces/基线命令/测试断言/日志注释步骤/缺陷族/序列化边界/双向接缝覆盖；S3 明确排除三处 wire/API 文件。 | `wc -l docs/superpowers/plans/b233.6-plan.md` → `535 docs/superpowers/plans/b233.6-plan.md`；`test -f docs/superpowers/plans/b233.6-plan.md` → 退出 0；`git status --short` → 计划与台账均为未跟踪文件。 |
| 2026-09-07 | 第一次空缺扫描只命中了自审句中的术语，非计划缺口；已改为不包含扫描词的自审句，后续必须以退出码 1 的无命令输出作为通过。 | `rg -n 'TBD|TODO|同 Task|适当错误处理|待补|占位' docs/superpowers/plans/b233.6-plan.md` → 命中自审行 `534`；该行随后改写。 |
| 2026-09-07 | 最终空缺扫描无命中；章节结构完整；wire/API 路径只在“明确排除”说明中出现，未进入 S3 文件集。计划补齐了 S1 请求结构、S2 DispatchSnapshot/MirroredEvent/Options 的完整字段块。 | `rg -n 'TBD|TODO|同 Task|适当错误处理|待补|占位' docs/superpowers/plans/b233.6-plan.md` → 退出码 1、无输出；`rg -n '^## [0-9]|^### [0-9]' ...` → 章节 0—6 及各子章节；`rg -n 'internal/(ledger/api/api\\.go|agentd/ledgerapi\\.go|proto/ledger\\.go)' ...` → 仅命中第 17、176 行的排除说明；`wc -l ...` → `535 docs/superpowers/plans/b233.6-plan.md`。 |
| 2026-09-07 | 两份法定产物暂存成功，暂存差异无空白错误且仅包含计划与台账。 | `git diff --cached --check` → 退出码 0；`git diff --cached --name-only` → `docs/superpowers/ledgers/2026-09-07-b233.6-plan-ledger.md`、`docs/superpowers/plans/b233.6-plan.md`；`git diff --cached --stat` → `2 files changed, 636 insertions(+)`；`git status --short --branch` → `## cards/B233.6-charter-6` 及两项 A。 |
| 2026-09-07 | 法定计划与台账首次提交成功；随后仅允许一次 amend 将本行历史读数收进同批提交。 | `git add docs/superpowers/ledgers/2026-09-07-b233.6-plan-ledger.md && git commit -m 'docs(B233.6): 出稿共享派发实现计划'` → `exit=0`；原始输出：`[cards/B233.6-charter-6 06d9c2c4] docs(B233.6): 出稿共享派发实现计划`、`2 files changed, 637 insertions(+)`、`create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.6-plan-ledger.md`、`create mode 100644 docs/superpowers/plans/b233.6-plan.md`。 |
| 2026-09-07 | 实现前局部基线：S1 派发/接收者回归、golden envelope、既有 Reply/RelayAnswer 与 NodeStep 均实际通过；S2/S3 的 `TestB2336` 当前尚无测试。一次并行基线命令因 vet 进程被系统杀死而失败，保留原始输出，不能视为通过。 | `go test ./internal/ledgerstep/ ./internal/agentd/ ./internal/client/ -run 'Test(B2336|Dispatch|CardStep|Receiver)' -count=1` → `ok github.com/Xsxdot/handoff/internal/ledgerstep 3.930s`; `ok github.com/Xsxdot/handoff/internal/agentd 29.384s`; `ok github.com/Xsxdot/handoff/internal/client 0.016s`。 `go test ./internal/ledger/ -run '^TestAppendMirroredEventWorkflowEnvelopeGolden$' -count=1` → `ok github.com/Xsxdot/handoff/internal/ledger 5.217s`。 `go test ./internal/ledger/ ./internal/ledgermirror/ -run '^TestB2336' -count=1` → `modernc.org/sqlite/lib: /usr/local/go/pkg/tool/linux_amd64/vet: signal: killed`; `ok github.com/Xsxdot/handoff/internal/ledger 0.006s [no tests to run]`; `ok github.com/Xsxdot/handoff/internal/ledgermirror 1.281s [no tests to run]`; `FAIL`。 `go test ./internal/agentd/ ./internal/client/ ./internal/ledgerstep/ -run '^TestB2336' -count=1` → 三包 `ok ... [no tests to run]`。 `go test ./internal/client/ ./internal/agentd/ -run 'Test(B2336|Reply|RelayAnswer)' -count=1` → `ok .../client 6.575s`; `ok .../agentd 8.794s`。 `go test ./internal/ledgerstep/ -run 'Test(NodeStep|B2336)' -count=1` → `ok .../ledgerstep 21.231s`。 |
| 2026-09-07 | S2/S3 新增行为测试已先红后绿；绿灯覆盖挂账身份投影、尝试身份变化后的 watermark 续订阅、旧/空 workflow 身份不唤醒；既有 automation 回归在 envelope 加身份后仍通过。 | `go test ./internal/ledger/ -run '^TestB2336TaskLinkProjection$' -count=1` 首红：`internal/ledger/tasks_test.go:51: workflow projection={CardID:P1 Node: Attempt: Target:mac-02 TaskID:task-review ...}, want node/attempt review/attempt-review`；修复后同命令 → `ok github.com/Xsxdot/handoff/internal/ledger 0.118s`。 `go test ./internal/ledgermirror/ -run '^TestB2336ProjectionChangeResubscribesWithNewAttempt$' -count=1` 首红：`mirror_test.go:153: 等不到事件源调用：投影变化后应取消旧订阅并重订阅`；测试修正为按订阅次序发事件后同命令 → `ok github.com/Xsxdot/handoff/internal/ledgermirror 0.109s`。 `go test ./internal/agentd/ -run '^TestB2336StaleAttemptDoesNotWake$' -count=1` 首红：`wakeconsumer_test.go:158: 仅当前 attempt 应唤醒一次，processed=3 escalated=false`；实现后同命令 → `ok github.com/Xsxdot/handoff/internal/agentd 0.220s`。 `go test ./internal/agentd/ -run 'TestAutomation' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 2.276s`。 |
| 2026-09-07 | 三包定向回归已通过；测试覆盖 S2 投影/镜像与 S3 旧尝试门禁、automation、Reply/RelayAnswer；未把先前 vet 被杀的并行基线当作通过。 | `go test ./internal/ledger/ -run 'Test(B2336TaskLinkProjection|AppendMirroredEventWorkflowEnvelopeGolden|LinkTask|LatestTaskStates|OpenTicketCounts|CardStepInFlight)' -count=1` → `ok github.com/Xsxdot/handoff/internal/ledger 1.445s`；`go test ./internal/ledgermirror/ -run 'Test(B2336ProjectionChangeResubscribesWithNewAttempt|Mirror)' -count=1` → `ok github.com/Xsxdot/handoff/internal/ledgermirror 3.862s`；`go test ./internal/agentd/ -run 'Test(B2336StaleAttemptDoesNotWake|Automation|Reply|RelayAnswer)' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 3.918s`。 |
| 2026-09-07 | ledger 包级测试实际通过。 | `go test ./internal/ledger/ -count=1` → `ok  github.com/Xsxdot/handoff/internal/ledger 16.165s`。 |
| 2026-09-07 | ledgermirror 包级测试实际通过。 | `go test ./internal/ledgermirror/ -count=1` → `ok  github.com/Xsxdot/handoff/internal/ledgermirror 3.456s`。 |
| 2026-09-07 | agentd 包级测试实际通过。 | `go test ./internal/agentd/ -count=1` → `ok  github.com/Xsxdot/handoff/internal/agentd 173.470s`。 |
| 2026-09-07 | client 包级测试实际通过。 | `go test ./internal/client/ -count=1` → `ok  github.com/Xsxdot/handoff/internal/client 9.407s`。 |
| 2026-09-07 | ledgerstep 包级测试实际通过。 | `go test ./internal/ledgerstep/ -count=1` → `ok  github.com/Xsxdot/handoff/internal/ledgerstep 9.829s`。 |
| 2026-09-07 | 全仓 Go 编译实际通过，命令无标准输出。 | `go build ./...` → 退出码 `0`，原始输出为空。 |
| 2026-09-07 | 变异锚点唯一命中，选择取反当前尝试比较作为语义变异；未删除代码，避免制造未使用符号编译假失败。 | `count=$(rg -F -c 'if current != *envelope.Attempt {' internal/agentd/wakeconsumer.go || true); printf 'mutation_anchor_count=%s\\n' "$count"` → `mutation_anchor_count=1`。 |
| 2026-09-07 | 语义变异确实命中且测试变红，说明当前尝试门禁测试有牙；该发测试已完成编译并执行，不是编译失败伪红。随后恢复原比较符。 | 变异后 `go test ./internal/agentd/ -run '^TestB2336StaleAttemptDoesNotWake$' -count=1` → `--- FAIL: TestB2336StaleAttemptDoesNotWake (0.15s)`；原始断言：`wakeconsumer_test.go:169: 当前 attempt 未唤醒或内容错误: resumes=[... [ticket] question: {"ticket_id":"old-ticket"} ...]`；末尾 `FAIL github.com/.../internal/agentd 0.158s`。 |
| 2026-09-07 | 恢复正确比较符后，新增旧尝试门禁测试重新通过。 | `go test ./internal/agentd/ -run '^TestB2336StaleAttemptDoesNotWake$' -count=1` → `ok  github.com/Xsxdot/handoff/internal/agentd 0.209s`。 |
| 2026-09-07 | S3 退役路径静态扫描只命中既有 `client.go` 的 HomeDir/cursor 文档说明；实现文件没有新增 WorkflowSendAnswer、InteractionService、harness、exec.Command 或 cursor 文件路径。 | `rg -n 'WorkflowSendAnswer|InteractionService|harness|exec\\.Command|\\.handoff/cursors|HOME' internal/agentd/wakeconsumer.go internal/ledgerstep/runner.go internal/ledgerstep/wire.go internal/client/client.go internal/client/execution.go cmd/card_node.go cmd/card_wait.go cmd/reply.go` → 退出码 `0`；原始命中：`internal/client/client.go:725` 的 `HomeDir` 注释、`internal/client/client.go:1623` 的 `~/.handoff/cursors` 注释。 |
| 2026-09-07 | 当前视图图结构校验实际通过；工具仍报告 5 个未扫描入口与 2 个无生命周期实体，不能将图视作完整调用面。 | `codegraph --repo . --view cards-B233.6-charter validate` → 退出码 `0`；原始 JSON：`{"containers":300,"domainDecls":23,"domains":23,"edgeIssues":null,"edges":5507,"entitiesWithoutLifecycle":2,"issues":null,"nodes":4499,"unscannedEntries":5,"views":["cards-B233.1-charter-2","cards-B233.2-charter","cards-B233.3-charter","cards-B233.4-charter","cards-B233.5-charter","cards-B233.6-charter"]}`。 |
| 2026-09-07 | 当前视图契约检查仍失败于已知两项存量契约，不是本卡新增边：`dead-contract d_execution→d_orchestration` 与 `dead-interface d_execution→d_orchestration ApprovalClient`；命令末尾明确报 `Error: 契约对照发现 2 处违规`。 | `codegraph --repo . --view cards-B233.6-charter check` → 退出码 `1`；原始失败项：`{"kind":"dead-contract","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）"}`；`{"kind":"dead-interface","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的接口 \\\"ApprovalClient\\\" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）"}`；末行：`Error: 契约对照发现 2 处违规`。 |
| 2026-09-07 | 收尾 `go test ./... -count=1` 实际失败，不能作为通过；触及包 ledger、ledgermirror、ledgerstep、agentd、client 均在该轮输出 `ok`，另有存量 `cmd` 与 `internal/keystone` 失败，待单包复核。 | 原始关键输出：`FAIL github.com/Xsxdot/handoff/cmd 22.348s`；`--- FAIL: TestIgnitionVerticalSlice (0.11s)`、`slice_test.go:157: 绑定解析错误: {Squad:exec-1 Carrier:opencode-1 Target:linux-01 Executor:opencode Model: HomeDir:/home/coordinator/.opencode-home}（期望载体 opencode-1 / 机 linux-01 / 执行者覆盖 claude）`、`FAIL github.com/Xsxdot/handoff/internal/keystone 0.115s`；末尾 `FAIL`。工具输出因 98,929 原始 token 超限被截断，未据此补写其余包结论。 |
| 2026-09-07 | 单包复核确认 `cmd` 失败仅为既知图契约闸：`TestRepoContractGate` 报相同两项 `dead-contract`/`dead-interface`，另报告存量 legacy 命中 147 条；没有出现本卡相关测试失败。 | `go test ./cmd/ -count=1` → 退出码 `1`；原始关键输出：`--- FAIL: TestRepoContractGate (0.04s)`；`graph_gate_test.go:38: 契约违规 [dead-contract] ...`；`graph_gate_test.go:38: 契约违规 [dead-interface] ...`；`graph_gate_test.go:40: legacy 命中: map[...]，warn 147 条`；`FAIL github.com/Xsxdot/handoff/cmd 17.254s`。 |
| 2026-09-07 | 单包复核确认 keystone 仍有独立存量失败 `TestIgnitionVerticalSlice`，与本卡触及文件无交集；不修改该包。 | `go test ./internal/keystone/ -count=1` → 退出码 `1`；原始输出含 `--- FAIL: TestIgnitionVerticalSlice (0.15s)`、`slice_test.go:157: 绑定解析错误: {Squad:exec-1 Carrier:opencode-1 Target:linux-01 Executor:opencode Model: HomeDir:/home/coordinator/.opencode-home}（期望载体 opencode-1 / 机 linux-01 / 执行者覆盖 claude）`、`FAIL github.com/Xsxdot/handoff/internal/keystone 0.158s`。 |
| 2026-09-07 | 收尾格式与日志静态检查实际通过：触及生产文件已 gofmt，差异无空白错误，未命中 `fmt.Print*`/`log.Print*`/`console.log`。 | `gofmt -w internal/ledger/tasks.go internal/ledger/tasks_test.go internal/ledger/mirror.go internal/ledgermirror/mirror.go internal/ledgermirror/mirror_test.go internal/agentd/wakeconsumer.go internal/agentd/wakeconsumer_test.go && git diff --check && rg -n 'fmt\\.Print(f|ln)?|log\\.Print(f|ln)?|console\\.log' internal/ledger/tasks.go internal/ledger/mirror.go internal/ledgermirror/mirror.go internal/agentd/wakeconsumer.go || true` → 退出码 `0`，原始输出为空。 |
| 2026-09-07 | 为新增错误分支补充结构化上下文日志；仅做日志增强，行为代码未改变。格式化和增量锁缝回归实际通过。 | `gofmt -w internal/ledger/tasks.go internal/ledgermirror/mirror.go internal/agentd/wakeconsumer.go && git diff --check` → 退出码 `0`，原始输出为空；`go test ./internal/ledger/ -run 'Test(B2336TaskLinkProjection|AppendMirroredEventWorkflowEnvelopeGolden)' -count=1` → `ok  github.com/Xsxdot/handoff/internal/ledger 0.257s`；`go test ./internal/ledgermirror/ -run '^TestB2336ProjectionChangeResubscribesWithNewAttempt$' -count=1` → `ok  github.com/Xsxdot/handoff/internal/ledgermirror 0.135s`；`go test ./internal/agentd/ -run '^TestB2336StaleAttemptDoesNotWake$' -count=1` → `ok  github.com/Xsxdot/handoff/internal/agentd 0.221s`。 |
| 2026-09-07 | 日志增强后全仓 Go 编译再次实际通过。 | `go build ./...` → 退出码 `0`，原始输出为空。 |
| 2026-09-07 | 实现提交首次成功；随后按台账纪律只允许一次 amend 把本行提交事实收进同批提交，不回写 amend 后 hash。 | `git commit -m 'feat(B233.6): route workflow events by attempt'` → `exit=0`；原始输出：`[cards/B233.6-charter-7 fc175d5f] feat(B233.6): route workflow events by attempt`、`8 files changed, 504 insertions(+), 25 deletions(-)`。 |
