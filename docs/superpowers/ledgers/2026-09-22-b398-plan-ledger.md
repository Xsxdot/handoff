# B398 plan 台账（2026-09-22）

> 卡 B398 · 入口节点 charter:plan · spec `docs/superpowers/specs/b398.md`（已批准）
> 工作树 `cards/B398-charter-3`，起手 HEAD `9ddd7dbe`。凡引用行号者动手前重核，漂了以符号为准。
> 本节点只出计划，不写实现。台账与产出物同批提交。

## 0. 起手事实

- `git log --oneline -1` → `9ddd7dbe spec(B398): 冻结物理身份与路由 target 分字段——L2 定稿 + 台账 + roadmap`
- 分支 `cards/B398-charter-3`；工作树干净。

## 1. 亲跑命令与原始输出

### 1.1 编译与静态检查（基线）

```text
$ go build ./...
BUILD_EXIT=0

$ go vet ./internal/agentd/ ./internal/ledgerstep/ ./internal/client/ ./internal/scheduling/ ./cmd/
VET_EXIT=0
```

### 1.2 既有测试基线（全绿）

```text
$ go test ./internal/ledgerstep/ ./internal/client/ ./internal/scheduling/ -count=1 -timeout 300s
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	13.929s
ok  	github.com/Xsxdot/handoff/internal/client	13.376s
ok  	github.com/Xsxdot/handoff/internal/scheduling	6.167s

$ go test ./internal/agentd/ -run 'TestB23310|TestB23327|TestSquadNode|TestLocalStep|TestCanonicalTarget|TestCardStep|TestStartCardStep|TestReceiver' -count=1 -timeout 300s
ok  	github.com/Xsxdot/handoff/internal/agentd	13.190s
```

### 1.3 复现红（临时诊断测试，跑完已删除，不进仓）

诊断 1（`internal/agentd/zz_b398_diag_test.go`，已删）：`setupB23310CardTaskEnvWithMachine(..., "linux-01")` +
`startCardStep`。原始读数：

```text
B398DIAG runner.Target="linux-01" Dispatcher.Carrier="c1" Dispatcher.Squad="sq1"
B398DIAG runner.Run err=<nil>
B398DIAG event type=comment payload={"body":"本节点派发失败：\n派发: dispatch: 状态码 400: {\"error\":\"scheduling: 小队角色不符: 冻结物理身份与载体登记不一致\"}","kind":"普通","refs":null}
B398DIAG event type=needs_human payload={"reason":"派发失败"}
```

同一诊断的日志链（关键两行）：

```text
准备派发节点 card=B1 node=implement target=linux-01 kind="" output_path=""
模板派发目标已归一 card=B1 template=feature-impl raw_target=linux-01 target=""
Dispatch 进入 url=http://127.0.0.1:36629 relay=false executor=fake target="" carrier=c1 squad=sq1 home_dir_set=true
```

诊断 2（`internal/agentd/zz_b398_t2_test.go`，已删）：接收端直接 POST，带 `frozen_target` 与不带：

```text
B398DIAG receiver with frozen_target code=400 body={"error":"scheduling: 小队角色不符: 冻结物理身份与载体登记不一致"}
B398DIAG (expected RED at baseline)
B398DIAG receiver legacy target code=200 body={... "target":"linux-01","carrier":"self-box" ...}
```

诊断 3（`internal/agentd/zz_b398_t1_test.go`，已删）：走整条装配路径的 E2E 断言，基线红：

```text
本机别名冻结派发未形成 task（len=0）；卡事件:
--- FAIL: TestB398FrozenCarrierLocalAliasDispatches (0.26s)
FAIL	github.com/Xsxdot/handoff/internal/agentd	0.269s
```

结论（亲跑到结果）：**路由 target 与冻结身份混用一个字段；本机别名（`linux-01` 指向本 agentd）
场景下冻结机器名在 `Dispatcher.NormalizeTarget` 处被折成空串，接收端 `AdmitFrozen` 拿空串
与登记值 `linux-01` 比对，被 `ErrRoleMismatch` 拒发。** 与 spec §1 的判读一致，但丢值点
比 spec 写的 `cardstep.go:186` 更靠后：装配点 `cardstep.go:236` 在 squad 分支把
`runner.Target` 重新赋回 `binding.Target`，所以真正把冻结值折空的是
`internal/ledgerstep/dispatch.go:169-173` 的 `NormalizeTarget`（诊断 1 的日志两行就是证据）。

## 2. 读码读数（带出处，行号为 9ddd7dbe 现状）

| 环节 | 出处 | 读数 |
|---|---|---|
| 冻结值取用 | `internal/agentd/cardstep.go:175-178` | `target := req.Target; if binding.Target != "" { target = binding.Target }` |
| 丢值（第一处） | `internal/agentd/cardstep.go:186` | `resolved, target, err := s.resolveStepDiscipline(node, target)`，返回 canonical（别名→空） |
| squad 分支回写 | `internal/agentd/cardstep.go:232-239` | `if binding.Squad != "" { runner.Target = binding.Target ... }`——把 raw 重新赋回（掩盖了 186 的改写） |
| 真丢值点 | `internal/ledgerstep/dispatch.go:169-173` | `rawTarget := target; if d.NormalizeTarget != nil { target = d.NormalizeTarget(target) }`；`DispatchOpts.Target` 用归一值 |
| 透传载荷 | `internal/ledgerstep/dispatch.go:341-356` | `DispatchOpts{Target: target, Carrier: d.Carrier, Squad: d.Squad, HomeDir: d.HomeDir, ...}`——**无独立冻结字段** |
| 传输层（本身正确） | `internal/agentd/cardstep.go:387-398` | `canonical` 只决定 client；`if opts.Carrier != "" { dispatchTarget = opts.Target }`，拿到的已是空串 |
| 接收端重建 Binding | `internal/agentd/handlers.go:517-534` | `AdmitFrozen(Binding{..., Target: req.Target, ...})` |
| 判据 | `internal/scheduling/scheduling.go:667-674`、`:701-708` | `samePhysicalMachine("", "linux-01")` 既非「两边 IsLocalMachine」也非字符串相等 → 拒 |
| 本机别名归一 | `internal/agentd/server.go:445-454`、`internal/config/listenclass.go:80-105` | 指向 Listen 的登记名 `CanonicalTarget` → `""` |

## 3. 图查询记录（codegraph）

```text
$ codegraph --repo . summary
本仓库有代码图：5315 节点 / 6639 边 / 26 领域

$ codegraph --repo . sym DispatchOpts     → 命中 m_client_DispatchOpts、m_agentd_...（含 ledgerstep 源码签名）
$ codegraph --repo . sym CardStepReq      → 命中 m_proto_CardStepReq
$ codegraph --repo . sym AdmitFrozen      → 命中 n_scheduling_Service_AdmitFrozen
$ codegraph --repo . sym resolveStepDiscipline / stepTransport / ViaTemplate / CanonicalTarget / Binding → 均命中
$ codegraph --repo . sym dispatchRequest  → 命中 m_agentd_dispatchRequest（anchor=moved）
$ codegraph --repo . flow ViaTemplate     → degraded=true steps=0（基线无 flows 段），按纪律读源码
$ codegraph --repo . who-calls handleDispatch → 未命中，报「近似候选 n_agentd_Server_handleDispatch」，需全限定名
```

### 图覆盖债

- `codegraph flow ViaTemplate` 返回 `degraded`（基线没有 flows 段），按纪律读源码，未拿 chain 冒充。
- `codegraph who-calls handleDispatch` 需全限定名 `Server.handleDispatch`，短名未命中——通过 grep 确认调用面
  （HTTP 路由挂载 + `receiver_dispatch_test.go` 直调 `postDispatch`），记债。
- `internal/ledgerstep/dispatch.go` 的 `DispatchOpts` 与 `internal/client/client.go` 同名类型：`sym DispatchOpts`
  返回两处，已用 `--json`/grep 区分（`grep -n "type DispatchOpts" internal/ledgerstep/dispatch.go` →
  `:27`；`internal/client/client.go` → `:713`）。

## 4. 判断与弃选

- 采用 spec §3 的修向：拆「路由 target」与「冻结物理身份」两个字段。命名冻结为
  `FrozenTarget`（Go）/ `frozen_target`（JSON）。
- 弃选 A/B/C/D 理由同 spec §3，不再重复。
- 新增判断（本节点亲跑得到）：spec §1 点 3 标注的丢值点 `cardstep.go:186` 在 squad 分支被
  `:236` 回写掩盖，**真正折空发生在 `ViaTemplate` 的 `NormalizeTarget`**。修向不变——两条
  路径都要拆字段：装配点把 `binding.Target` 同时送进新的冻结字段（route 仍交
  `canonical`），并在接收端优先用冻结字段建 Binding。任何一处漏拆，诊断 3 的 E2E 都保持红。
- 本节点未验证项：真机清单（§11）未跑（归协调者）；`internal/agentd` 全量跑存在预存在 flake
  `TestPtyWSAttachedBacklogBytesKeyPresent`（B396 plan §4 记录，非本卡引入），本卡测试范围不含它。
