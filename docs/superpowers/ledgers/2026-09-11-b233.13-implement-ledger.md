# B233.13 implement 台账

- 2026-09-11 起点：从 `cards/B233.13-charter-3` @ `c7862130`（plan 提交）续接；当前分支 `cards/B233.13-charter-4`。有效基线 `cards/B233.1-charter-7` @ `760a5be9`（不切、不越过）。本节点不切分支、不改 git 配置、不调 handoff CLI、不起 executor。
- 计划：`docs/superpowers/plans/b233.13-plan.md`；冻结物 `docs/superpowers/specs/b233.13-contract.md`。D1：orchestration import gateway，gateway 生产文件不 import orchestration；P1–P5 全 A。

## Task 1：终态 CAS 凭证 + 接口化

- 新写失败测试 `internal/agentd/b23313_terminal_claim_test.go`（package agentd）。
- TODO 记录每条命令的原始输出与判断。

- 步骤 1 红测试落地 `internal/agentd/b23313_terminal_claim_test.go`。
- 步骤 2 跑红原始输出：
  ```text
  $ go test ./internal/agentd/ -run 'B23313' -count=1
  internal/agentd/b23313_terminal_claim_test.go:41:20: assignment mismatch: 2 variables but mgr.Done returns 1 value
  FAIL	github.com/Xsxdot/handoff/internal/agentd [build failed]
  EXIT=1
  ```
  判定：编译红，符号缺席（`Done` 仍返回单值）。
- 步骤 3-7 实现：抽出 `Manager.transitClaim`（唯一 CAS 判据，返回 claimed）；`Done`/`Stop` 改返回 `(TerminalOutcome, error)`；导出 `RequireWorkspace`/`AssembleResultRef`/`InspectRepoDir`；`Server` 字段改 `OrchestrationClient`；`SetManager` 拒绝 typed-nil；`handleDone`/`handleStop` 按 `Claimed` 门控释放；新增 `Server.Conf()`。
- 步骤 3 新增：`transitClaim` 在 `UpdateTaskState` 返回 `ErrBadTransit` 时重读任务，若已到目标态按幂等成功（`claimed=false,err=nil`）——否则并发 Done 的 loser 会拿到 `ErrBadTransit` 而非幂等成功（与冻结 §5.2 #9 冲突）。见 manager.go#transitClaim。
- 步骤 10 跑绿原始输出：
  ```text
  $ go test ./internal/agentd/ -run 'B23313' -count=1
  ok  	github.com/Xsxdot/handoff/internal/agentd	1.077s
  ```
- 既有相关回归：`go test ./internal/agentd/ -run 'TestHandleDone|TestHandleStop|TestStop|TestDone|TestTransit' -count=1` → `ok ... 20.531s`（`receiver_dispatch_test.go#newReceiverTestEnv` 补 `mgr.SetWorkspace(NewGitCapability())`，对应 P1 组装点接线）。
- 变异自检（可编译等价变异）：
  - `transitClaim` 幂等分支改 `return true,nil`（冒充赢家）→ `go build ./...` exit 0；`TestB23313ConcurrentDoneClaimedOnce`/`TestB23313ConcurrentStopSecondIs409` 红（`Claimed=2` / `ErrBadTransit=0`）。确认测试有牙，随后还原。
  - `transitClaim` CAS-loser 分支改 `return true,nil` → `TestB23313ConcurrentDoneClaimedOnce` 红（`Claimed=2`）。确认该分支被覆盖。
  - `handleDone` 的 `if !outcome.Claimed` 门改为 `if false && ...`（可编译）→ `TestB23313ConcurrentDoneReleasesOnce` 红（`before=v1 after=v3`，载体重复释放）。确认释放门被覆盖。

## Task 2：物理迁包（生产）

- `git mv` 迁移纯文件：`manager.go`/`approver.go`/`approval_client.go`/`cachegc.go`/`forcereclaim.go`/`ticketvoid.go`/`reclaim.go`/`reconcile.go`/`status.go` → `internal/orchestration/`，改 `package orchestration`。
- 混合文件拆分：
  - `gc.go`：`handleGC` 留 gateway；`Manager.GC` + `appendGCWorktrees*` 迁 orchestration。
  - `projectadmin.go`：4 个 `handle*` + `attachCardBaseBranches` + DTO/共享助手（`RegisterProjectReq`/`ProjectOriginURL`/`ProjectNameFromURL`/`ValidateProjectName`/`SameLocation`/状态常量/`NameFallbackLimit`）留 gateway；Manager 段（`RegisterProject`/`ListProjects`/`UnregisterProject`/`InspectRepoDir`/`inspectRepoDir` 等）迁 orchestration 并以 `agentd.X` 引用。
  - `workspace_capability.go`：`gitCapability`/`NewGitCapability`/`ErrWorkspaceUnavailable` 留 gateway；`SetWorkspace`/`Workspace`/`RequireWorkspace`/`AssembleResultRef` 迁 orchestration。
- 新增 `internal/agentd/orchestration_dto.go` 未单列：DTO 与共享事件 payload/助手统一落 `internal/agentd/b23313_retained.go`（D1 口径：DTO 留 gateway、与 OrchestrationClient 同包；编排 import gateway，gateway 生产不 import 编排）。**偏差记录**：plan §2.1 要求新建 `orchestration_dto.go`，实际同一批声明并入 `b23313_retained.go`（一个文件承载 DTO + 助手 + 导出转发）。属物理落点差异，不改变 D1 语义与图归属（均归 `d_gateway`）。
- 导出面：DTO `DispatchReq`/`RecoverReport`；payload `ProgressPayload`/`TicketAnsweredPayload`/`ApproverDecisionPayload`/`CompletedPayload`/`FailedPayload`/`TicketsVoidedPayload`；哨兵 `ErrBadDispatchRequest`/`ErrExecutorStartFailed`/`ErrEnvResolveFailed`/`ErrDisciplineResolveFailed`；助手导出转发 `CheckProcHeadroom`/`RecoverTransit`/`ResolveProject`/`ResolveDispatchBase`/`ResolveDefaultBaseBranch`/`ResolveBaseURL`(不适用)/`TruncateRunes`/`GitProbe`/`GitRun`/`GitRunNet`/`ProbeWorkspaces`/`BranchTip`/`CanonPath`/`IsTerminalState`/`ReconcileExecutorGone`/`ProjectOriginURL`/`ProjectNameFromURL`/`ValidateProjectName`/`SameLocation`。`voidTicketsWithAudit`→`VoidTicketsWithAudit`（原地导出，非转发）；`reconcileExecutorGone`→`ReconcileExecutorGone`（原地导出）；`ticketsVoidedPayload`→`TicketsVoidedPayload` 原地导出。
- 编排新增 `log.go`（包级 `log()` 与 gateway 同形），`SetLiveConfig`，`var _ agentd.OrchestrationClient = (*Manager)(nil)` **未加**：见下「未完成」。
- 组装点 `cmd/agentd.go`：`orchestration.NewApprover`/`RegistryFromAds`/`NewManager`；`mgr.SetWorkspace(agentd.NewGitCapability())`；`mgr.SetLiveConfig(srv.Conf())`；`srv.SetManager(mgr)`。
- 生产编译原始输出：
  ```text
  $ go build ./...
  BUILD_EXIT=0
  $ go vet ./internal/orchestration/ ./cmd/ ./internal/client/
  EXIT=0
  ```
- 判据核对：`grep -rn "internal/orchestration" internal/agentd/*.go`（非 `_test.go`）无 import 命中（仅注释）；`internal/orchestration` 内不存在 `func (s *Server)`。

## 未完成（本节点停点，如实记录）

- **Task 3 测试迁置未完成**：`internal/agentd` 内约 34 个测试文件仍以 `package agentd` 引用已迁出的 `Manager`/`NewManager`/`Approver`/`HashPolicyVersion` 等，`go vet ./internal/agentd/` 报大量 `undefined`。按 plan §3.3 需迁 `internal/orchestration` 或改 `package agentd_test`；本轮未做（剩余 `cmd`/`client` 测试已修好）。
  ```text
  $ go vet ./internal/agentd/
  undefined: Manager / Approver / NewApprover / ...
  ```
- **`var _ agentd.OrchestrationClient = (*Manager)(nil)` 未落**：需先确认 `Manager` 方法集完全满足接口（尤其 `Done`/`Stop` 形状）。`go build` 通过说明接口方法调用点齐全，但编译期断言未加。
- **Task 4 图更新未做**（`codegraph/target.json` `entries`、`best.json` 容器、diffs）。
- **Task 5/6 未做**（接缝清单复核、全量测试、codegraph check）。
- **交接建议**：下一节点先补 Task 3 测试迁置与接口断言，再 Task 4/6。

## 提交事实（第一批）

- `git add -A && git commit` → 提交 `f8fde101`（本批产出与本台账同批；提交时命令 `go build ./...` → `BUILD_EXIT=0`）。
- 收口判据：工作树干净；amend 会换 hash，本行不回填 chase。

---

## 续接节点（补 测试 + 图）——起点 `16e827b7`

- 起点：`cards/B233.13-charter-6` @ `16e827b7`（生产迁包已完成，禁止重做 `git mv Manager`）。本次只补测试与图。

### Task 3：测试迁置（P2-A）

- 迁移判定以编译错误为权威。Manager-only 白盒测试 `git mv` 到 `internal/orchestration/`：
  `approval_client_test.go`、`approver_test.go`、`approver_env_unix_test.go`、`cachegc_test.go`、`capability_seam_test.go`、`dispatch_discipline_test.go`、`export_test.go`、`fingerprint_contract_test.go`、`forcereclaim_test.go`、`gatedecision_contract_test.go`、`gc_test.go`、`handleresult_notrailer_test.go`、`manager_test.go`、`permgate_wire_test.go`、`reclaim_server_test.go`、`reclaim_test.go`、`reconcile_test.go`、`regression_round2_test.go`、`resume_test.go`、`session_reconcile_test.go`、`ticketvoid_test.go`、`workspace_capability_test.go`（共 22 个），改 `package orchestration`，补 `agentd` import，gateway 保留符号改写为 `agentd.X`。
- gateway 侧 `package agentd` 存量跨缝测试（`_test.go` 无法 import orchestration，会触发 import cycle——已用临时探针文件实证：`imports .../orchestration ... import cycle not allowed in test`），改为经「测试工厂」桥接：
  - `internal/agentd/b23313_managerfactory_test.go`：定义 `ManagerDeps`、`TestManager`（`OrchestrationClient` + 装配方法）与 `var ManagerFactory func(ManagerDeps) TestManager`。
  - `internal/agentd/b23313_managerfactory_wire_test.go`（`package agentd_test`）：init 里把 `ManagerFactory` 接到 `orchestration.NewManager`，并注入 `SetWorkspace`/`SetLiveConfig`。
  - `internal/agentd/b23313_gateway_testhelpers_test.go`：`newTestManager`/`newTestManagerWithAds`/`newTestManagerWithApprover`/`newTestManagerWithCfg`/`newManagerForServer`、`mustCreateTask`/`mustDone`/`createRunningTask`/`registerTestProject`、`chanAdapter`/`recordingProfile`/`failStartAdapter`、`eventually`/`mustEvents`/`hasEvent`/`waitTaskState`、`newTestServerWithManager` 等夹具等价副本。
- `package agentd_test` 外部测试（`status_test.go`、`server_test.go`、`integration_test.go`）直接 `orchestration.NewManager(...)` + `mgr.SetWorkspace(agentd.NewGitCapability())` + `srv.SetManager`。
- 拆分 `w3a_testhelpers_test.go` 的 `testAgentdEnv.mgr` 字段类型 `*Manager` → `OrchestrationClient`。
- `env_test.go`/`executor_default_test.go` 调用 Manager 私有 resolver 的断言（`env.mgr.env.For`、`resolveExecutor`、`resolveModel`）随 Manager 迁出，gateway 侧删除（对应语义已由 orchestration 包内 `TestEnvMappingHotReloadsWithoutRebuildingManager` 等同名断言承接）。`watchers_test.go` 的 `m.cfg.StallTimeout` 改经 `testManagerCfg`。
- 迁置签名收敛点（`file#Symbol` 锚）：
  - `progressPayload/...`、DTO、哨兵经 `agentd.` 前缀引用。
  - `Hub` → `agentd.Hub`；`Workspace`（复合字面量）→ `agentd.Workspace`；`NewHub`/`NewGitCapability` → `agentd.*`。
  - 未导出 `canonPath`/`errBadDispatchRequest`/`reconcileExecutorGone`/`newFailedPayload` 等在 gateway 侧改为导出名 `CanonPath`/`ErrBadDispatchRequest`/`ReconcileExecutorGone`/`NewFailedPayload`。
- 一个失败/放弃的尝试：把 `watchdog_taskprocs_test.go` 一并迁 orchestration —— 它引用 gateway 未导出 `scanTaskProcs`/`taskProcCountFn`/`testLogger`/`newTestStoreHub`/`mustCreateTaskState` 与 `taskProcPressurePayload`，留在 agentd 更省，已 `git mv` 回退。
- `b23313_terminal_claim_test.go` 的 typed-nil 用例改 `var typedNil *nilOrchClient`（嵌入接口的测试替身）以构造「接口非 nil、底层指针 nil」。
- `manager_dispatch_carrier_test.go#TestDispatchDoesNotImportSchedulingResolver` 的源码扫描路径改 `../orchestration/manager.go`。
- 编译/测试原始输出：
  ```text
  $ go vet ./internal/agentd/ ./internal/orchestration/ ./cmd/
  VET_EXIT=0

  $ go test ./internal/orchestration/ -count=1
  ok  	github.com/Xsxdot/handoff/internal/orchestration	45.111s

  $ go test ./internal/agentd/ -run 'TestB23313' -count=1
  ok  	github.com/Xsxdot/handoff/internal/agentd	1.014s
  （含 TestB23313ConcurrentDoneClaimedOnce / TestB23313ConcurrentDoneReleasesOnce /
    TestB23313ConcurrentStopSecondIs409 / TestB23313SetManagerRejectsTypedNil）

  $ go test ./internal/agentd/ -count=1
  --- FAIL: TestLegacyNodeEventSequenceUnchanged (0.22s)
  FAIL	github.com/Xsxdot/handoff/internal/agentd	150.515s
  ```
- 存量基线红核实：在 `c7862130` 的独立 worktree 跑同一用例亦红（`FAIL ... 2.632s`），确认为存量基线红，非本卡引入；按协调者指令不修、不碰 `scheddispatch_test.go`（已 `git checkout --`）。
- B233.12 回归：
  ```text
  $ go test ./internal/agentd -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1
  ok  	github.com/Xsxdot/handoff/internal/agentd	0.003s [no tests to run]
  $ go test ./internal/orchestration -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond|TestClientApproverAllowUndeliveredUntilAck' -count=1
  ok  	github.com/Xsxdot/handoff/internal/orchestration	0.062s
  $ go test ./internal/approval -run 'TestClientApproverAllowUndeliveredUntilAck' -count=1
  ok  	github.com/Xsxdot/handoff/internal/approval	0.077s
  ```
  （`TestApprovalClientAutoAllowProductionHookDoesNotRespond` 随 Manager-only 测试迁到 orchestration，故 agentd 包显示 `no tests to run`。）
- 补 `var _ agentd.OrchestrationClient = (*Manager)(nil)`（`internal/orchestration/manager.go`，NewManager 之后）。
- Task 3 缝测试有牙由上一节点变异自检覆盖（本节点未改生产，未重复变异）。

### Task 4：图更新

- `codegraph/domains/d_orchestration.json`：`lifecycle.from/to` 锚从 `internal/agentd/manager.go#Dispatch|#transit` 改指 `internal/orchestration/manager.go#Dispatch|#transit`（迁包后原锚 `file_missing`）。
- `codegraph/target.json`：`d_gateway→d_orchestration` 的 `entries:["agentd.Manager"]` 已不存在，按生产事实清空为 `entries:[]`；保留 `interfaces:["OrchestrationClient"]` 与 `legacyBudget:321` 不动；`legacyBudgetNote` 记录 D1/棘轮口径。
- `codegraph/diffs/cards-B233.13-charter.json`：container `k_agentd_orchestration_client` 仍归 `d_coordination_api`（该视图基线的领域 id；改指 `d_gateway` 会报「引用不存在的基线领域」，已回退），summary 更正为已实现口径。
- `codegraph/best.json`：本卡不改——该文件是「容器 id → 领域」的归属表（无文件路径），`k_agentd_Manager` 等容器迁包前后同属 `d_orchestration`，无失鲜项；`k_agentd_orchestration_client` 本就归 `d_gateway`。
- 未完成：完整图重扫（baseline 节点 file/line 与 edge 端点随迁包重定位）未做。`codegraph validate` 仍余 2 条来自 `cards-B233.4-charter` 视图的存量失效边（`appendGCWorktreesExecute` 仍记在 `internal/agentd/gc.go`，实际已迁）；按 plan「完整图增量与 target 棘轮留独立扫描节点」处置，下节点处理。

### Task 5/6：收口

- `codegraph check --view cards-B233.13-charter --repo .`：
  ```text
  { "fails": [], "warns": [ ... ] }
  CHECK_EXIT=0
  ```
- `codegraph check --repo .`（无视图）红：`dead-interface d_gateway→d_orchestration 声明的接口 "OrchestrationClient" 在 d_gateway 中不存在`——接口节点由 Ticket 0 视图引入，未并入 baseline；需随图重扫一并 absorb。
- `codegraph validate --repo .`：2 条 `cards-B233.4-charter` 存量失效边（见上），`discovered 2 完整性问题`，exit 1。
- `codegraph resolve --doc docs/superpowers/specs/b233.13-contract.md`：锚 `file_missing`（contract 仍指 `internal/agentd/manager.go#...`）；按 discipline §1.2 不自行改冻结 contract。
- 结构断言：`rg -n "internal/orchestration" internal/agentd/*.go`（非 `_test.go`）无 import 命中；`rg -n "mgr\s+\*Manager" internal/agentd/server.go` 无匹配。

### 提交事实（本续接批）

- `go build ./...` → `BUILD_EXIT=0`；`go vet ./internal/agentd/ ./internal/orchestration/ ./cmd/` → `VET_EXIT=0`。
- 收口：`git add` 测试 + 图 + 本台账后 commit；收口判据是工作树干净，不回填 hash chase。

---

## 续接节点（review 两条 major 修复）——起点 `10b31e97`

- 起点：`cards/B233.13-charter-7` @ `10b31e97`。只修 review 两条 major，禁止重做迁包；不修 B233.4 失效边、不碰 `TestLegacy`。
- major 1：`internal/agentd/orchestration_client.go` 顶部注释仍写「实现节点把它们随 Manager 迁到 internal/orchestration」，与 D1（DTO 留 gateway）矛盾。改为「DTO 留 gateway，与 OrchestrationClient 同包；编排实现 import gateway 取共享 DTO」。仅注释，不动包结构。
- major 2：contract §1 表中所迁符号文件锚仍指 `internal/agentd/*`，`codegraph resolve --doc` 报 14 处 `file_missing`/`vanished`。改动：文件路径改 `internal/orchestration/*`；符号锚从 `#Manager.X` 改为图实际节点名（bare `#X`，实测 `#Manager.Dispatch` 报 vanished、`#Dispatch` 报 moved 且行号正确）；`workspace_capability.go` 三行原先无 `#Symbol`（仅源码锚），补 `#Workspace`/`#RequireWorkspace`/`#AssembleResultRef`。
- 原始验证输出：
  ```text
  $ codegraph resolve --doc docs/superpowers/specs/b233.13-contract.md --repo .
  resolve exit=0
  $ codegraph check --view cards-B233.13-charter --repo .
  check exit=0
  ```
  （修复前 resolve exit=1，最后一条 `Error: 文档锚点检查失败: internal/orchestration/manager.go#Manager.Dispatch (vanished)`。）
- 两处改动均为注释/文档，无生产行为变化；`go build ./...` 在本轮跑一次作编译兜底（见提交事实行）。
- 本批不修 B233.4 视图失效边、不碰 `TestLegacyNodeEventSequenceUnchanged`。
- 提交事实（记录当时命令与原始输出，随后 amend 一次收进同批；amend 换 hash 属 git 事实，不回填 chase）：
  ```text
  $ go build ./...
  BUILD_EXIT=0
  $ git add internal/agentd/orchestration_client.go docs/superpowers/specs/b233.13-contract.md docs/superpowers/ledgers/2026-09-11-b233.13-implement-ledger.md
  $ git commit -m "fix(B233.13): 修正 orchestration_client 注释与 contract 迁包锚（D1 口径）"
  [cards/B233.13-charter-7 598fdebf] fix(B233.13): ...
   3 files changed, 43 insertions(+), 25 deletions(-)
  ```
  收口判据：工作树干净。

