# C1.11 契约落地台账

- 2026-08-25：开工核对命令 `pwd && git status --short --branch && rg --files -g '!*node_modules*' | sed -n '1,160p'`；原始输出首行 `/root/.handoff/worktrees/fe509380`，分支为 `## cards/C1.11-charter`，未显示工作树改动；当前仓库包含 `codegraph/`、`internal/agentd/`、`desktop/` 与 `web/`。
- 2026-08-25：读取 `docs/superpowers/specs/2026-08-25-codegraph-decls-migration-spec.md`；头部原文为 `> **状态：已批准（2026-08-25，用户「做吧」）**`；判断：上游状态已批准，可进入契约查证。
- 2026-08-25：本轮未调用 handoff CLI、未派发任务、未启动 executor 或子任务；仅使用本地 shell 读取仓内文件。
- 2026-08-25：首次按猜测路径读取模块缓存失败；原始报错为 `rg: /root/go/pkg/mod/github.com/!xsxdot/charter/graph@v0.7.0/codegraph: IO error for operation on /root/go/pkg/mod/github.com/!xsxdot/charter/graph@v0.7.0/codegraph: No such file or directory (os error 2)`，随后 `nl` 与 `rg --files` 对同一路径同样报 `No such file or directory`；依赖源码位置未验证，改用 `go list` 解析。
- 2026-08-25：`go list -f '{{.Dir}}' github.com/Xsxdot/charter/graph/codegraph` 失败；原始输出为 `go: downloading github.com/Xsxdot/charter/graph v0.7.0`、`go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.7.0.mod294766428.tmp: read-only file system`、`open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.7.0.lock: read-only file system`；依赖源码查证暂未完成。
- 2026-08-25：使用任务临时模块缓存执行 `env GOMODCACHE=/root/.handoff/tmp/fe509380/gomodcache go list -m -json github.com/Xsxdot/charter/graph@v0.8.0` 成功；原始输出确认 `Version: v0.8.0`、`Time: 2026-08-25T06:39:06Z`、`Hash: dbe039d4de680f3a89d9573e59ee9fa2211b4aad`、`Ref: refs/tags/graph/v0.8.0`。
- 2026-08-25：按缓存目录读取 v0.8.0 源码的命令失败；原始报错为 `rg: /root/.handoff/tmp/fe509380/gomodcache/github.com/!xsxdot/charter/graph@v0.8.0/codegraph: IO error for operation on ...: No such file or directory (os error 2)`，`nl` 对 `decls.go`、`check.go`、`decl.go` 同样报 `No such file or directory`；仅列出模块版本不等于源码已缓存，继续执行 `go mod download`。
- 2026-08-25：`env GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/fe509380/gomodcache go mod download -json github.com/Xsxdot/charter/graph@v0.8.0` 成功；原始 JSON 确认源码目录 `/root/.handoff/tmp/fe509380/gomodcache/github.com/!xsxdot/charter/graph@v0.8.0`、`Sum=h1:7/9qwDXNChBkayrdURRVp5xSZZ4ZTMBOKROfcOEymKs=`、`GoModSum=h1:Tk9+SdktmKHL9nCFQnbwsEzs6jxZ8Vd4H/K4mJfWEg4=`。
- 2026-08-25：实读 v0.8.0 依赖源码：`codegraph/decl.go:21-27` 定义 `DomainDecl` 及 `json` 键；`codegraph/decls.go:18-50` 的 `LoadDomainDecls(repoRoot string) (map[string]DomainDecl, error)` 读取 `repoRoot/codegraph/domains/*.json`，目录不存在返回空 map 与 nil，单文件读/JSON/文件名或责任句校验失败返回带路径错误；`codegraph/check.go:49` 的 `Check(t *Target, b *Best, v *View, decls map[string]DomainDecl) *Report` 保持现有签名；`codegraph/decls.go:65` 的 `ValidateDecls(v *View, best *Best, repoRoot string, decls map[string]DomainDecl) []string` 已将 best 作为主词表参数。
- 2026-08-25：仓内现状查证：`internal/agentd/codegraph.go#handleProjectCodegraph` 在 `:75` 组装 `baseline/views/stale`，`:76-100` 仅在 best 存在且 target 成功时加载声明并把它传给 `Check`，`:103` 经 `writeJSON` 返回 200；`internal/agentd/server.go#Handler` 的 API 路由为 `:514-515`，`writeJSON` 为 `:2194-2200`，当前响应没有 `decls` 键。
- 2026-08-25：模块现状查证：根 `go.mod:7` 与 `go.sum:11-12` 仍为 `github.com/Xsxdot/charter/graph v0.7.0`；`desktop/go.mod:25` 通过 `replace github.com/Xsxdot/handoff => ../` 间接消费根模块；C1.11 升级坐标为 v0.8.0，属于下游实现的同提交变更，本契约节点不改模块文件。
- 2026-08-25：边界负向证据命令 `env GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/fe509380/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph@v0.8.0 validate --repo .` 实际退出码 `1`；原始输出为 `Error: 发现 1 个完整性问题、0 个失鲜节点`，JSON 中 `domainDecls: 2`、`domains: 20`、`issues: ["[decl d_coordination_task] 领域 d_coordination_task 不在 best domains 段中"]`，末行 `exit status 1`；判断：迁移前 v0.8.0 词表判据确实变红。
- 2026-08-25：`jq '{meta,assembly,contracts:(.contracts|length)}' codegraph/target.json && jq '.domains | keys' codegraph/best.json && jq '.domains | length' codegraph/best.json && jq '.containers | length' codegraph/best.json` 实际输出 target schema `3`、assembly `main.go`/`internal/agentd/server.go`、`contracts: 37`；best domains 共 `23` 个、containers 共 `232` 个；判断：本卡目标图需把新的响应组装点 `internal/agentd/codegraph.go` 纳入 assembly，best 结构树不改。
- 2026-08-25：依赖 CLI 源码查证 `graph/cli/cli.go:124-157`：v0.8.0 `validate` 先 `LoadBest`，再 `LoadDomainDecls`，按声明 id 调 `ValidateDecls(declView, best, graphRepo, singleDecl)`，问题追加到 `issues` 并非零退出；`graph/cli/cli.go:704-731,856` 确认符号锚自检命令为 `resolve --doc <Markdown>`。
- 2026-08-25：`jq '.domains.d_orchestration, .domains.d_workspace' codegraph/best.json` 实际输出：`d_orchestration` label 为 `任务编排`、type `logic`；`d_workspace` label 为 `项目与工作区`、type `boundary`；现有 `d_coordination_task.json` 仍声明旧 id，且包含 4 条 invariants、lifecycle、12 条 stateMachine 迁移。
- 2026-08-25：文档锚点自检命令 `env GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/fe509380/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph@v0.8.0 resolve --repo . --doc docs/superpowers/specs/c1.11-contract.md` 成功退出 0；原始 JSON 的 5 个本仓锚点均解析到节点，结果为 `handleProjectCodegraph=ok`、`Handler=moved`、`forwardIfRequested=ok`、`writeJSON=moved`、`handleProjectCodegraphSource=ok`；无坏锚。
- 2026-08-25：文档结构复核发现契约中的负向命令代码块因补丁转义出现多余字符 `+go run`；已用 `apply_patch` 修正为真实命令 `... GOMODCACHE=... go run ...`；`git diff --check` 随后实际退出 0、无输出。
- 2026-08-25：目标图对照命令 `env GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/fe509380/gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph@v0.8.0 check --repo .` 实际退出 0；原始 JSON 顶部为 `"fails": []`，`warns` 含既有 anchor-off-domain、anchor-off-graph、container-misplaced、legacy、oversized-package、prefix-family 结果；本次新增 assembly 未产生 fail。
- 2026-08-25：目标图闸测试命令 `env GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/fe509380/gomodcache go test ./cmd -run '^TestRepoContractGate$' -count=1` 实际退出 0；原始输出末行为 `ok github.com/Xsxdot/handoff/cmd 0.030s`，此前仅有依赖下载行。
- 2026-08-25：宿主既有回归命令 `env GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/fe509380/gomodcache go test ./internal/agentd -count=1` 实际退出码 `0`；原始输出为 `ok  github.com/Xsxdot/handoff/internal/agentd 122.846s`。
- 2026-08-25：提交前新鲜审计并行命令实际均退出 0：`codegraph resolve --repo . --doc docs/superpowers/specs/c1.11-contract.md` 返回 5 个锚点（3 个 `ok`、2 个 `moved`，无坏锚）；`jq empty codegraph/target.json` 无输出；`git diff --check` 无输出；`git status --short --branch` 为 `## cards/C1.11-charter` 加目标图、契约文档、台账三条预期路径。
- 2026-08-25：首次普通权限执行 `git add codegraph/target.json docs/superpowers/specs/c1.11-contract.md docs/superpowers/ledgers/2026-08-25-c1.11-contract-ledger.md` 失败；原始报错为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/fe509380/index.lock': Read-only file system`，退出码 `128`。
- 2026-08-25：按同一命令提权重跑 `git add ...` 成功；原始输出为空、退出码 `0`；目标图、契约文档、台账进入暂存区，随后本条追加造成台账尾差，提交前需再次暂存。
- 2026-08-25：暂存区最终审计实际均退出 0：`git diff --cached --check` 无输出；统计为 `3 files changed, 200 insertions(+), 1 deletion(-)`；name-status 仅为 `M codegraph/target.json`、新增本卡台账、 新增 `docs/superpowers/specs/c1.11-contract.md`；`git status --short --branch` 为 `## cards/C1.11-charter` 且三者均已暂存。目标图缓存 diff 仅新增 `internal/agentd/codegraph.go` assembly。
- 2026-08-25：追加上一条后重新审计：`git diff --cached --check` 无输出退出 0；`git status --short --branch` 仍仅显示目标图、契约、台账三项已暂存；`git diff --cached --stat` 为 `3 files changed, 201 insertions(+), 1 deletion(-)`。
- 2026-08-25：冻结提交命令 `git commit -m "docs(contract): freeze C1.11 decls migration"` 实际成功；原始输出为 `[cards/C1.11-charter fb1f66df] docs(contract): freeze C1.11 decls migration`、`3 files changed, 202 insertions(+), 1 deletion(-)`，未 push。提交后 `git status --short --branch` 仅为 `## cards/C1.11-charter`；`git show --stat --oneline --summary HEAD` 确认包含 `codegraph/target.json`、本台账、`docs/superpowers/specs/c1.11-contract.md`。
- 2026-08-25：`env GOMODCACHE=/root/.handoff/tmp/fe509380/gomodcache go mod download -json github.com/Xsxdot/charter/graph@v0.8.0` 未完成下载；原始 JSON 含 `Error: ... verifying go.mod ... open /root/go/pkg/sumdb/sum.golang.org/latest: read-only file system`，同时确认 `Origin.Hash=dbe039d4de680f3a89d9573e59ee9fa2211b4aad`；改用 `GOSUMDB=off` 重试。
