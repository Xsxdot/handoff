# B356 台账

- 2026-09-11：用户报 `card wait --subtree` 子卡工单收不到事件。排查：Subtree 成员齐全；活库 B233.10 有 8 条 permission_request 镜像；根因是 MaxSeq 排他无建连对账 + 唤醒按事件 card_id 不冒泡。开卡 B356。
- 2026-09-11：用户「开卡修吧」「在当前分支上修，修完更新本机和 linux-01，走真实任务」。定级 L2。spec/plan 落盘。不远程派发。
- 2026-09-11 implement：TDD 先红后绿。`go test ./internal/ledger/ -run TestOpenTicketCounts` ok；`go test ./cmd/ -run TestCardWait` ok；`go test ./internal/agentd/ -run TestAutomation` ok；`go build ./...` 0。变异：`cardSnapshotType` 改名 → TestCardWaitSubtreeSnapshotIncludesChildTicket 红 `type=card_snapshot_mutated`；关掉 resolveWakeCard 冒泡 → TestAutomationWakeBubblesChildTicketToParentCoordinate 红 Resume=0。均已还原复绿。
