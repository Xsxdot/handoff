# B358.9 spec 台账：会话身份本质定义（2026-09-17 对话 trail）

- 背景：B358.8 真机走查三项反馈（新建会话身份混乱/筛选两行/⋯独占一行）。排查中发现另一会话落账 seq14690（控制台身份重设计），用户表示没看懂且不同意（本会话 question 回复），seq14690 处置作废（B358.8 seq14691），卡留 acceptance。
- 本轮按 charter:spec 对话逐条确立 9 决定（用户原话意译，问答原文见卡事件流）：
  1. 成员对称：人、主 agent、卡协调者平等成员；创建=先动手，无特权。
  2. 拉权对称：任一成员可拉新人（question 拍板）。
  3. 人名全局指定：默认本机名方案被否（多电脑用户名未必相同会造出两个"人"），改直接指定（拟 `user:sycm` 待批）。
  4. 一名多端：用户主动提出多电脑同时在线难题；协调者提案、用户拍板。权力/名字归人，落款/已读归端；@ 人扇出各端。
  5. 主 agent 名=载体名：用户先没看懂（"agent:main vs 跟引擎走"），重讲后用户用自己的模型拍板——我建群我选载体=载体名；agent 发起=其 cli 名；统一为载体名。
  6. 卡协调者自动跟进（question 拍板；用户原话"拉进这个会话的卡的协调者都一样是群里的成员"）。
  7. owner 保持现状（协调者推导，未单独提问，审批时可改；spec 内已标推导待批）。
  8. 不搬家（question 拍板）。
  9. 解析失败 fail-closed（question 拍板）：拦下+可行动报错，不用旧脸回落。
- 独立审阅（只读 subagent，结论"修改后批准"，11 findings 已处置）：决定 7 未标推导、端名/老屋句偷换、U2 owner 未定义、B366 矛盾、接缝 #2 语义空洞、写死签名/路径/落款/键名、B358.4 引用扩大、OOS 无出处、读数分界、ledger 引证、基线空悬、二解残余。处置见 spec 本轮改写。
- 现状核实读数（spec 事实依据，非设计）：
  - `Members=[]string{owner}`（`internal/ledger/sessions.go:50-53`）——初建名单只 owner。
  - CLI 会话子命令：create/list/detail/archive/join<card>/leave/send/wait（`cmd/session.go:60` 注册、`271/305/354/406/426/446/477`），无加成员命令。
  - 席位身份形如 `cli:<engine>#<seat>`（`internal/proto/seat.go:38-48`）；主 agent（无卡）裸 `cli:<user>@<host>`（`cmd/ledgercli.go:42-50`）。
  - 游标按（member→room→seq）做键（`internal/collab/cursor/cursor.go:16,23`）。
  - dev main.tsx 有 StrictMode（`web/src/main.tsx:1,7`）——真机复验 dimensions 抛错 A/B 归因用。
- 定级判据：跨 collab/agentd 接入/web/CLI 四面 + wire 相邻（端点 actor 语义）→ L3；多面并行 + 集成 → 重档（breakdown 可纠）。
- 卡：B358.9（B358 子卡，2026-09-17 建）。spec 附件已挂（seq14696 注）。

## 2026-09-17 产品与方案群对齐（改写 trail）

- 切法选 **B**：首卡一名多端 + fail-closed + 不搬家；拉权对称另开。
- 改口径：已读按人合并（废分端红点）；卡进群协调者首卡必跟可 @；主 agent 名＝载体/启动身份。
- 架构师干净切法：解析只收口 agentd；VerifyWriter 只认人名键；游标键＝人名；协调者挂拉卡路径；禁旧脸回落/一端一名/半新半旧/协调者特权旁路/本卡任意拉人。
- spec 正文已按上列改写，待用户批准后交棒 contract。

## 两版合并（2026-09-17 晚；远端 xsx 会话 × 本地 sycm 会话）

- 背景：远端 `bc1e44947`（产品与方案群"选 B"）与本地 `85b30dd46`（本会话第二轮）同源分叉，推送被拒（非快进）。用户裁决两项：已读**按人合并**、拉权**另开子卡**。
- 合并基线：`bc1e44947`；叠加项：
  1. 端戳来源＝本次登录会话登记的设备名（`internal/agentd/authroutes.go:199,242-250` 的 `joinDevice(ticket.device, browserName(UA))`；`cmd/console.go:47-53` 缺省主机名）。弃"端取服务端机器名"——同台 agentd 下多浏览器会塌成一个端，落款与"我始终是我"都失真。端缺失不拦门。
  2. 决定 4 改写：主 agent 名＝**启动身份**（自报、自选、会话内唯一），**不查执行器词表**（用户原话："并不是所有的 harness 我们都已经支持了执行器，所以这里不能卡死"）。
  3. 决定 5 判**现状已成立、零新增写入**：协调者＝该卡当前席位 → 席位成员随卡出入群进成员投影（`internal/collab/sessions.go:216-244`）与写者集（`internal/collab/service.go:118-139`），`@卡号` 寻址到当前席位；**不写成员表**（写入会随换绑留脏行；`AddSessionMember` 注释：席位成员不入此列）。本卡对该条＝验收回归。
  4. 游标：删"老键只读冻结"，改"键＝人名、旧键自然弃用"（游标纯缓存非权威，`internal/collab/cursor/cursor.go` 包注释：重启丢失无害、开房自愈）。
  5. 补 4 缺口：CLI 需能出示 `agent:<启动身份>`（今天 `--cli/--session` 只产席位串，写权字符串等值 → `agent:*` 成员发不出话，`cmd/session.go:498-510`）；@ 投递＝订阅且流尾起不补历史（`internal/agentd/wakeconsumer.go:178-230` 非席位 target 零唤醒）；控制台身份读缝（人名+端名，今天 status 无身份字段）；新建会话表单删 owner 字段（决定 6）。
  6. 本卡不做段新增两条：handoff 拉起的会话级主 agent 的拉起+@ 唤醒绑定 → 另卡（用户拍板"先只跑通外部自理"）；拉人 op 另开时须保持 B366 自助加入语义不变（决定 7 老屋恢复路径，不许被吞）。
- 本地版留档：分支 `keep/b358.9-round2`（`85b30dd46`），不再合入。
- 并行提醒：本文件同一晚被两个会话（xsx@MacBook-Pro / sycm@MacBook-Air）改过，已撞一次；合并后由本会话推。
- 合并提交（历史读数，amend 前）：
  ```
  [cards/B233.1-charter-7 c70071207] docs(B358.9): 两版合并——选 B 基线 + 本会话身份细节（已读按人、拉权另开、端取登录设备名）
   3 files changed, 49 insertions(+), 27 deletions(-)
  ```
  本行随同批提交 amend 收口；amend 后 hash 变化属 git 事实，不在本文件追写。
- **批准（2026-09-17 用户终审）**：三项待批定案——人名 `user:sycm`、配置键 `console_user`、新建会话表单删 owner 字段；spec 头部状态行回写「已批准（2026-09-17 用户批准）」。交棒：L3 → contract。
- 批准回写提交（历史读数，amend 前）：
  ```
  [cards/B233.1-charter-7 e626e4017] docs(B358.9): 用户批准回写——状态行落判决，三项待批定案
   2 files changed, 7 insertions(+), 5 deletions(-)
  ```
  本行随同批提交 amend 收口；amend 后 hash 变化属 git 事实，不在本文件追写。
