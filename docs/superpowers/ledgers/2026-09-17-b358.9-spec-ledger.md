# B358.9 spec 台账：会话身份本质定义（2026-09-17 对话 trail）

- 背景：B358.8 真机走查三项反馈（新建会话身份混乱/筛选两行/⋯独占一行）。排查中发现另一会话落账 seq14690（控制台身份重设计），用户表示没看懂且不同意（本会话 question 回复），seq14690 处置作废（B358.8 seq14691），卡留 acceptance。
- 本轮按 charter:spec 对话逐条确立 9 决定（用户原话意译，问答原文见卡事件流）：
  1. 成员对称：人、主 agent、卡协调者平等成员；创建=先动手，无特权。
  2. 拉权对称：任一成员可拉新人（question 拍板）。
  3. 人名全局指定：默认本机名方案被否（多电脑用户名未必相同会造出两个"人"），改直接指定（拟 `user:sycm` 待批）。
  4. 一名多端：用户主动提出多电脑同时在线难题；协调者提案、用户拍板。权力/名字归人，落款/已读归端；@ 人扇出各端。
  5. 主 agent 名=载体名：用户先没看懂（"agent:main vs 跟引擎走"），重讲后用户用自己的模型拍板——我建群我选载体=载体名；agent 发起=其 cli 名；统一为载体名。
  6. 卡协调者自动跟进（question 拍板；用户原话"拉进这个会话的卡的协调者都一样是群里的成员"）。
  7. owner 保持现状（协调者推导，未单独提问，审批时可改；spec 内已标推导待批）。
  8. 不搬家（question 拍板）。
  9. 解析失败 fail-closed（question 拍板）：拦下+可行动报错，不用旧脸回落。
- 独立审阅（只读 subagent，结论"修改后批准"，11 findings 已处置）：决定 7 未标推导、端名/老屋句偷换、U2 owner 未定义、B366 矛盾、接缝 #2 语义空洞、写死签名/路径/落款/键名、B358.4 引用扩大、OOS 无出处、读数分界、ledger 引证、基线空悬、二解残余。处置见 spec 本轮改写。
- 现状核实读数（spec 事实依据，非设计）：
  - `Members=[]string{owner}`（`internal/ledger/sessions.go:50-53`）——初建名单只 owner。
  - CLI 会话子命令：create/list/detail/archive/join<card>/leave/send/wait（`cmd/session.go:60` 注册、`271/305/354/406/426/446/477`），无加成员命令。
  - 席位身份形如 `cli:<engine>#<seat>`（`internal/proto/seat.go:38-48`）；主 agent（无卡）裸 `cli:<user>@<host>`（`cmd/ledgercli.go:42-50`）。
  - 游标按（member→room→seq）做键（`internal/collab/cursor/cursor.go:16,23`）。
  - dev main.tsx 有 StrictMode（`web/src/main.tsx:1,7`）——真机复验 dimensions 抛错 A/B 归因用。
- 定级判据：跨 collab/agentd 接入/web/CLI 四面 + wire 相邻（端点 actor 语义）→ L3；多面并行 + 集成 → 重档（breakdown 可纠）。
- 卡：B358.9（B358 子卡，2026-09-17 建）。spec 附件已挂（seq14696 注）。
