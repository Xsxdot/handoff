# B233.7 implement 台账

职责：记录本卡实现节点的事实、命令原始结果、放弃尝试与判断；不替代测试或验收证据。

- 2026-09-08：当前分支为 `cards/B233.7-charter-4`，起始 HEAD 为 `863422981afd5606b70df707776166077f7e4297`；工作树初始干净。
- 2026-09-08：已读取 `docs/superpowers/specs/b233.7.md`、`docs/superpowers/specs/b233.7-contract.md`、`docs/superpowers/specs/b233.7-breakdown.md`、`docs/superpowers/plans/b233.7-plan.md`；本节点按 S1–S6 实现，S0/跨机真机由协调者负责。
- 2026-09-08：`go build ./...`：退出码 0；标准输出为空。
- 2026-09-08：`go test ./internal/agentd -run 'TestQuestionReissueAfterAnswerCreatesNewTicket|TestOpenCodeWaitQuestionUsesRespondAsk|TestTaskDiffTargetFallsBackToRepoWhenWorktreeGone|TestAssembleResultRefEmptyCommitAllowed' -count=1`：退出码 0；原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.189s`。
- 2026-09-08：`go test ./internal/executor/... ./internal/hostapi ./internal/scheduling ./internal/workspace ./internal/client -count=1`：退出码 0；executor、五家 executor 子包、hostapi、scheduling、workspace、client 全部输出 `ok`。
- 2026-09-08：`go test ./internal/hostapi -run 'TestWakeHomeReadyRequiresTurnOutputNotCredFile|TestWakeHomeSuppliesMainCredentialBeforeTurn|TestWakeHomeOccupiedNeverOverwrites' -count=1 -v`：退出码 0；三测试均 `PASS`，最终输出 `ok github.com/Xsxdot/handoff/internal/hostapi 0.008s`。
- 2026-09-08：`go test ./internal/keystone/ -run '^TestIgnitionVerticalSlice$' -count=1 -v`：退出码 1；原始失败为 `slice_test.go:157: 绑定解析错误: {Squad:exec-1 Carrier:opencode-1 Target:linux-01 Executor:opencode Model: HomeDir:/home/coordinator/.opencode-home}（期望载体 opencode-1 / 机 linux-01 / 执行者覆盖 claude）`。
- 2026-09-08：S1 先扩展 `manager_test.go` 的 Ask 入口断言；首次编译红原因为 `ticketRequest.QuestionID undefined`，为验证新接缝已在 `manager.go` 增加空字段后复跑，断言红原因为两条重发工单的 `question_id` 均为 `""`，不是拼写/夹具错误。
- 2026-09-08：实现 QuestionID 持久化与 ticket 回读后，`gofmt -w internal/agentd/manager.go internal/agentd/manager_test.go` 退出码 0；`go test ./internal/agentd -run 'TestQuestionReissueAfterAnswerCreatesNewTicket|TestQuestionReissuePreservesNativeIDAcrossAnswerPaths|TestOpenCodeWaitQuestionUsesRespondAsk|TestQuestionTicketIdempotentOnReplay|TestQuestionWithoutIDFallsBackToUUID' -count=1 -v` 退出码 0；五测试均 `PASS`，最终输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.485s`。
- 2026-09-08：S1 permission 竖切测试首次编译报原文 `internal/executor/opencode/adapter_test.go:1860:2: undefined: eventually`，该测试 helper 不存在，立即改为本文件已有的 deadline 轮询；修正后测试真实跑红，原始失败为 `权限审批与回传失败回调超时`，证明当前 adapter 仅日志、不调用失败回调。
- 2026-09-08：实现 OpenCode 的可选 `deliveryFailureReporter` 回调与 agentd `taskApprovalClient.NoteDeliveryFailed` 绑定任务校验；`gofmt -w internal/agentd/manager.go internal/agentd/approval_client.go internal/executor/opencode/adapter.go` 退出码 0；permission 失败竖切与 Ask 回程定向测试均退出码 0，原始最终输出分别为 `ok github.com/Xsxdot/handoff/internal/executor/opencode 0.017s`、`ok github.com/Xsxdot/handoff/internal/agentd 0.367s`。
- 2026-09-08：S2 新增 `Manager.Dispatch`→Profile spy 竖切后，`go test ./internal/agentd -run '^TestDispatch(InjectsTaskDisciplineIntoProfile|RejectsDisciplineWithoutTaskProfile)$' -count=1 -v` 退出码 1；原始失败为 `Profile.Prepare 请求=[]，want 一次且一个 overlay` 与 `无 Profile 的非空纪律应带 task/executor 错误，实得 <nil>`，确认生产派发尚未调用 Profile 且会静默启动。
- 2026-09-08：接入 `prepareTaskProfile` 后，首次重跑仅第二测试因测试断言未适配计划要求而失败：原始错误为 `无 Profile 的非空纪律应带 task/executor 错误，实得 任务 ... executor "fake" home "" 无法准备非空纪律 Profile overlay`；将断言改为检查带标签错误并核对任务留下 `failed` 后，定向两测试退出码 0，最终输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.249s`。
- 2026-09-08：S3 基线 `go test ./internal/agentd -run 'TestTaskDiff|TestAssembleResultRef|TestDispatchCallsCapability' -count=1` 退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.300s`；既有测试未覆盖 HTTP diff 对 `ResultRef.Commit` 的消费。
- 2026-09-08：S3 HTTP/Capability spy 竖切首次真实运行退出码 1；原始断言为 `DiffRange 入参={repo:... base:main head:HEAD}，want ... head="9cd6773ef49e986ea7b0d2124ddcb9c64e10af8e"`，确认 `handleTaskDiff` 当前丢弃 `assembleResultRef` 返回值。
- 2026-09-08：保留 `handleTaskDiff` 的 `ResultRef` 并以非空 `Commit` 作为 DiffRange head、空 commit 记录降级日志；`gofmt -w internal/agentd/server.go` 退出码 0；S3 竖切与 `TestTaskDiff|TestAssembleResultRef|TestResultRef|TestDispatchCallsCapability` 定向回归均退出码 0，原始输出分别为 `ok github.com/Xsxdot/handoff/internal/agentd 0.086s` 与 `ok github.com/Xsxdot/handoff/internal/agentd 0.375s`。
- 2026-09-08：改写 S4 前实跑 `go test ./internal/executor/opencode -run '^TestApplySnapshotInjectedProbeSuccess$' -count=1 -v` 退出码 0；原始输出含 `PASS` 与 `ok github.com/Xsxdot/handoff/internal/executor/opencode 0.005s`，但该通过依赖测试恒真 `reloadProbe`，不作为真实探测证据。
- 2026-09-08：将 `TestApplySnapshotInjectedProbeSuccess` 的 `reloadProbe` 改为读取并解析真实 `taskDir/opencode.json`，只在 `external_directory=ask` 且 `bash["*"]=ask` 时返回 true；复跑 `gofmt -w internal/executor/opencode/adapter_test.go` 与定向测试均退出码 0，原始最终输出 `ok github.com/Xsxdot/handoff/internal/executor/opencode 0.005s`。
- 2026-09-08：S5 将 `TestIgnitionVerticalSlice` 断言改为 carrier 的物理 `Executor=opencode`、`Target=linux-01`、`HomeDir=/home/coordinator/.opencode-home`，保留请求 Executor=claude 冲突输入；`gofmt -w internal/keystone/slice_test.go` 与 `go test ./internal/keystone/ -run '^TestIgnitionVerticalSlice$' -count=1 -v` 均退出码 0，最终输出 `ok github.com/Xsxdot/handoff/internal/keystone 0.097s`。
- 2026-09-08：S6 修改 `b233.3.md`、`roadmap.md` 与 `skills/handoff/SKILL.md`，将派发写请求失败语义改为先查状态、再由协调者决定是否重派，并明确不自动认重；修改前检索命中旧语义于 `b233.3.md:52,87`、`roadmap.md:85`，skill 中原有 `reply` 502→`resume` 条目保留。
- 2026-09-08：S6 旧语义清理命令 `rg -n '写请求重试必须幂等|模拟超时重试仍单任务|重试仍是一次任务|未用真重试器验单任务' docs/superpowers/specs/b233.3.md docs/roadmap.md skills/handoff/SKILL.md`：退出码 1，无输出；新语义检索有命中，且 `git diff --check` 退出码 0。
- 2026-09-08：补齐 `manager.go` 的提问/审批注释与回答日志，成功日志仅记录 `answer_bytes`，不记录回答正文；五家 Profile.Verify 新增的非缺失规则读取错误分支均带上下文 Error 日志；`gofmt` 退出码 0。
- 2026-09-08：S1 定向回归 `go test ./internal/agentd ./internal/executor/opencode -run 'Test(Question|OpenCode|Permission|Delivery)' -count=1 -v`：退出码 0；agentd 与 opencode 测试全部 `PASS`，最终输出分别为 `ok github.com/Xsxdot/handoff/internal/agentd 6.483s`、`ok github.com/Xsxdot/handoff/internal/executor/opencode 0.409s`。
- 2026-09-08：S2 定向回归 `go test ./internal/executor/... -run 'TestProfiles|TestProfile|Test.*Overlay' -count=1` 与 `go test ./internal/agentd -run 'TestCoordinatorHome|TestDispatch|TestB2337' -count=1`：均退出码 0；最终输出分别含 executor 各子包 `ok`（无测试子包标记 `[no tests to run]`）与 `ok github.com/Xsxdot/handoff/internal/agentd 13.014s`。
- 2026-09-08：S1/S2 task 收尾 `go build ./...`：退出码 0；标准输出为空。
- 2026-09-08：S3/S4/S5 回归：`go test ./internal/hostapi ./internal/executor/opencode -run 'Test(WakeHome|ApplySnapshot|Probe)' -count=1 -v`、`go test ./internal/scheduling ./internal/keystone -count=1`、`go test ./internal/agentd -run 'TestTaskDiff|TestAssembleResultRef|TestResultRef|TestDispatchCallsCapability' -count=1` 均退出码 0；最终输出含 `ok github.com/Xsxdot/handoff/internal/hostapi 0.311s`、`ok github.com/Xsxdot/handoff/internal/executor/opencode 4.242s`、`ok github.com/Xsxdot/handoff/internal/scheduling 3.129s`、`ok github.com/Xsxdot/handoff/internal/keystone 0.125s`、`ok github.com/Xsxdot/handoff/internal/agentd 0.350s`。
- 2026-09-08：静态检查 `rg -n -C 1 'reloadProbe|return true|task-[12]\\.md' ...` 退出码 0；恒真 probe 未命中，命中的 `return true` 均为既有真实分支或测试 recorder；`rg -n 'fmt\\.Print|log\\.Print|console\\.log'` 退出码 1、无输出；`git diff --check` 退出码 0。
- 2026-09-08：变异前唯一命中校验：`if ref.Commit != "" {` 仅命中 `internal/agentd/server.go:1912`，计数 1，退出码 0。将比较符改为 `==` 后 `go build ./...` 退出码 0；先跑行为断言 `TestTaskDiffUsesResultRefCommit` 真实失败，原始差异为 `DiffRange ... head:HEAD` 而期望 40 位 commit；随后跑变异相关测试集仅该测试失败，退出码 1；已恢复原比较符。
- 2026-09-08：一次诊断检索因正则未闭合失败，原始输出为 `rg: regex parse error:\n    (?:func (.*assembleResultRef|assembleResultRef|taskDiffTarget)\n    ^\nerror: unclosed group`；随后改用字面 alternation 检索成功，未据失败输出作归因。
- 2026-09-08：恢复变异后的 S3 回归 `go test ./internal/agentd -run 'TestTaskDiff|TestAssembleResultRef|TestResultRef|TestDispatchCallsCapability' -count=1`：退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.238s`；`go test ./internal/executor -run '^TestProfiles' -count=1 -v`：退出码 0，五家 Profile 的全局/任务层校验均 `PASS`，最终输出 `ok github.com/Xsxdot/handoff/internal/executor 0.005s`；恢复后 `go build ./...` 退出码 0、标准输出为空。
- 2026-09-08：为覆盖“空白但非空纪律”先扩展已声明 Dispatch/Profile 竖切；首次重跑因测试变量误替换出现编译错误，原始输出 `internal/agentd/manager_test.go:3513:7: no new variables on left side of :=`，立即修正测试夹具后再跑。测试随后真实断言红：空白纪律没有新增 Profile overlay，证明原实现的 `TrimSpace` 会丢弃非空正文。
- 2026-09-08：将 `prepareTaskProfile` 空纪律判定收紧为仅 `start.Discipline == ""`；`TestDispatchInjectsTaskDisciplineIntoProfile`、`TestDispatchRejectsDisciplineWithoutTaskProfile` 均退出码 0，最终输出分别为 `ok github.com/Xsxdot/handoff/internal/agentd 0.181s`、`ok github.com/Xsxdot/handoff/internal/agentd 0.105s`；同期 `go build ./...` 退出码 0、标准输出为空。
- 2026-09-08：首次全量 `go test ./...` 因新测试直接调用 `httptest.NewServer` 触发 `TestNoDirectHttptestServers`，原始失败为 `diffbase_test.go:68 仍直接调用 httptest.NewServer(，请改用 internal/testhttp`；随后改用仓内 `testhttp.NewServer`，定向 `TestNoDirectHttptestServers|TestTaskDiff...` 退出码 0。
- 2026-09-08：`go test ./cmd -run '^TestRepoContractGate$' -count=1` 退出码 1；原始门禁输出为 `契约违规 [dead-contract] ... d_execution→d_orchestration ...`、`契约违规 [dead-interface] ... ApprovalClient ...`、`legacy 命中: map[...]，warn 147 条`。该图门禁不在 B233.7 允许改动范围。
- 2026-09-08：触及子系统全量回归 `go test ./internal/agentd ./internal/executor/... ./internal/hostapi ./internal/scheduling ./internal/keystone ./internal/workspace ./internal/client -count=1` 退出码 0；agentd `164.985s`，executor 各子包、hostapi、scheduling、keystone、workspace、client 最终均输出 `ok`。
- 2026-09-08：静态清理检索仍仅命中 roadmap 中 B234 的既有 OOS 说明 `roadmap.md:627`（“其余 httptest.NewServer”），本卡新增 `internal/agentd/diffbase_test.go` 无直调；旧重试语义无命中。
- 2026-09-08：修复直调 helper 后最终 `set -o pipefail; go test ./... 2>&1 | tail -n 120` 退出码 1；原始末段显示 `cmd` 仍因既有契约门禁失败（`FAIL .../cmd 16.677s`），而 `internal/agentd 167.252s`、所有 executor、hostapi、keystone、scheduling、client 等触及包均 `ok`，其余全仓包也均 `ok` 或 `[no test files]`。
- 2026-09-08：提交前收口检查：`git diff --check` 退出码 0；`go build ./...` 退出码 0、标准输出为空；待提交路径仅为本卡实现、测试、文档与本台账。
- 2026-09-08：提交命令 `git commit -m "fix(B233.7): 核销契约轴质量复审"` 原始输出：`[cards/B233.7-charter-4 7c5341d0] fix(B233.7): 核销契约轴质量复审`、`20 files changed, 1007 insertions(+), 52 deletions(-)`，退出码 0；随后按台账纪律仅 amend 一次收录本行。
- 2026-09-08：review-1 修复前复跑 `go test ./internal/agentd -run '^TestResumeRoute$|^TestTaskDiffUsesResultRefCommit$' -count=1 -v` 退出码 0，`TestResumeRoute` 与 ResultRef commit HTTP seam 均 PASS；`go test ./internal/executor -run '^TestProfilesVerifyTaskOverlayContent$' -count=1 -v` 五家均 PASS；`go build ./...` 退出码 0。
- 2026-09-08：先补 review-1 seam 断言后定向测试真实变红：S3 两支 HTTP 测试分别报告成功日志缺 `result_path_empty=true`、空 commit fallback 日志缺 `commit_fallback=true`；五家 Profile.Verify 缺失 overlay 日志仅有完成摘要，缺 `expected_state=present`/`actual_state=missing`。既有 `TestResumeRoute` 在新增 store JSON 与 `WaitDeliveryPolicy` 断言下仍退出码 0，确认 Manager/store 行为已存在但此前证据不完整。
- 2026-09-08：S3 增加空 Path/非空 commit 与空 commit/HTTP fallback 两条真实 handler→Capability seam，并在成功/降级日志加入 `result_path_empty`、`commit_fallback`；五家 Profile.Verify 在任务层缺失/内容不符分支加入 expected-vs-actual 状态与字节数结构化日志。`gofmt` 退出码 0；两条 S3 测试、`TestResumeRoute`、五家 Profile overlay 测试均 PASS；`go build ./...` 退出码 0。
- 2026-09-08：文件集偏差说明：`internal/agentd/dispatch_discipline_test.go` 的既有改动来自前一轮实现，为适配 S2 非空纪律必须经 Profile/HomeDir 的新契约而补齐同构测试夹具；本 review-1 回合未继续修改该文件，未新增生产边界或公共接口。按计划核对其余本回合改动均落在 S2/S3 已声明文件集。
- 2026-09-08：包级回归 `go test ./internal/agentd -run 'Test(ResumeRoute|TaskDiff|AssembleResultRef|ResultRef|DispatchInjectsTaskDisciplineIntoProfile|DispatchRejectsDisciplineWithoutTaskProfile|Question|OpenCode|Permission|Delivery)' -count=1` 退出码 0；`go test ./internal/executor/... -run 'TestProfiles|TestProfile|Test.*Overlay' -count=1` 退出码 0（各子包 ok 或 no tests to run）；`go test ./internal/client -run '^TestWaitDeliveryPolicy' -count=1` 退出码 0；`go build ./...` 退出码 0，标准输出为空。
- 2026-09-08：变异自验一：唯一命中 `if start.Discipline == "" {` 计数 1，改为 `strings.TrimSpace` 后 `go build ./...` 退出码 0；行为断言 `TestDispatchInjectsTaskDisciplineIntoProfile` 真实失败：`空白但非空纪律必须逐字节进入 Profile overlay`，已恢复原比较。
- 2026-09-08：变异自验二：OpenCode Verify 唯一日志锚 `"actual_state", actualState` 计数 1，改为恒 `present` 后 `go build ./...` 退出码 0；行为断言及全表 `TestProfilesVerifyTaskOverlayContent` 均只在 opencode 子测试失败，原始日志出现 `expected_state=present actual_state=present`，已恢复。
- 2026-09-08：变异自验三：S3 fallback 日志唯一锚 `"commit_fallback", true` 计数 1，改为 false 后 `go build ./...` 退出码 0；行为断言 `TestTaskDiffEmptyCommitFallsBackThroughHTTP` 真实失败，原始日志出现 `commit_fallback=false`，准确 commit/空 Path 用例仍 PASS，已恢复。
- 2026-09-08：触及子系统收尾 `go build ./...` 退出码 0；`go test ./internal/agentd ./internal/executor/... ./internal/client -count=1` 退出码 0，原始输出为 agentd `ok ... 156.489s`、executor 主包/agy/claudecode/codex/fake/grok/opencode/rawtap/turn 全部 `ok`、client `ok ... 9.326s`。
- 2026-09-08：集成全量 `set -o pipefail; go test ./...` 退出码 1；原始末段显示 agentd `ok ... 175.930s`、executor 全部 `ok`、其余内部包全部 `ok`/`no test files`，仅 `github.com/Xsxdot/handoff/cmd` 失败。单独 `go test ./cmd -run '^TestRepoContractGate$' -count=1 -v` 原始失败为 `契约违规 [dead-contract] 契约 d_execution→d_orchestration ...`、`契约违规 [dead-interface] ... ApprovalClient ...`、`legacy 命中 ... warn 147 条`；未改图门禁，记为既有阻塞而非本回合归因。
- 2026-09-08：按 review-1 最新要求将空白纪律用例改为 TrimSpace 语义；先运行既有生产实现，`go test ./internal/agentd -run '^TestDispatchInjectsTaskDisciplineIntoProfile$' -count=1 -v` 真实断言红，原始输出为 `TrimSpace 后为空的纪律不得调用 Profile.Prepare，调用次数=2`，确认当前 `==` 判空不满足验收。
- 2026-09-08：将 `prepareTaskProfile` 改为 `strings.TrimSpace(start.Discipline) == ""`；`TestDispatchInjectsTaskDisciplineIntoProfile` 与 `TestDispatchRejectsDisciplineWithoutTaskProfile` 均 PASS，`go test ./internal/agentd -run '^TestDispatchInjectsTaskDisciplineIntoProfile$|^TestDispatchRejectsDisciplineWithoutTaskProfile$' -count=1 -v` 退出码 0。
- 2026-09-08：最新 TrimSpace 改动后全仓编译 `go build ./...` 退出码 0，标准输出为空。
- 2026-09-08：TrimSpace 改动后的触及包全量 `go test ./internal/agentd ./internal/executor/... ./internal/client -count=1` 退出码 0；agentd `176.878s`，executor 主包/agy/claudecode/codex/fake/grok/opencode/rawtap/turn 全部 `ok`，client `9.487s`。
- 2026-09-08：最新改动后的集成全量 `set -o pipefail; go test ./... 2>&1 | tail -n 120` 退出码 1；原始末段显示 agentd `ok ... 186.396s`、client/executor 及其余内部包全部 `ok`/`no test files`，仅 `github.com/Xsxdot/handoff/cmd` 失败（`FAIL .../cmd 16.692s`）。该失败与前次相同，未修改 cmd 或图契约文件。
- 2026-09-08：TrimSpace 变异自验唯一命中 `if start.Discipline == "" {` 计数 1；变异后 `go build ./...` 退出码 0，行为断言 `TestDispatchInjectsTaskDisciplineIntoProfile` 真实失败，原始输出为 `TrimSpace 后为空的纪律不得调用 Profile.Prepare，调用次数=2`；已恢复 `strings.TrimSpace` 实现。
- 2026-09-08：恢复实现后的最终编译与关键回归 `go build ./... && go test ./internal/agentd -run '^Test(DispatchInjectsTaskDisciplineIntoProfile|DispatchRejectsDisciplineWithoutTaskProfile|ResumeRoute|TaskDiffAllowsEmptyResultPathWithCommit|TaskDiffEmptyCommitFallsBackThroughHTTP)$' -count=1` 退出码 0，agentd 输出 `ok ... 1.288s`。
- 2026-09-08：收口静态检查 `git diff --check` 退出码 0；受影响文件无 `fmt.Print`/`log.Print`/`console.log` 命中；当前变更仅含本卡 review-1 测试、生产修复与台账。
- 2026-09-08：提交命令 `git commit -m "fix(B233.7): 补齐复审接缝证据"` 原始输出：`[cards/B233.7-charter-5 8aefcf04] fix(B233.7): 补齐复审接缝证据`、`12 files changed, 252 insertions(+), 25 deletions(-)`，退出码 0；随后仅 amend 一次收录本行，amend 后 hash 不回写台账。
