# B229.3 执行台账 —— T3 派发侧缝 1 接线与 DispatchSnapshot 版本号

日期 2026-08-25 · 分支 `cards/B229.3-charter` · 基线 e24b9a31（cards/B229-charter-2 同点）

## 判断与事实（边干边追加）

- 起点：工作树干净，分支 cards/B229.3-charter @ e24b9a31；Ticket 0 骨架已含 wire 字段声明（client.DispatchOpts.DisciplineText/DisciplineVersion、dispatchRequest、DispatchSnapshot.DisciplineVersion、ledgerstep 两字段）。
- 缝 1 骨架实读：internal/discipline/dispatch.go 已实现 ResolveDispatch/ErrUnsupportedTarget/DisciplineRef；internal/ledger/disciplines.go 已实现 PutDiscipline/GetDiscipline。
- 设计裁决：Dispatcher 以**数据字段**携带已解析三元组（DisciplineText/DisciplineVersion），ViaTemplate 原样透传进 DispatchOpts 与 dispatched 快照——依据卡面判据「Dispatcher 数据字段携带已解析三元组」+ 契约 §2.2「ViaTemplate 只消费其产物，ledgerstep 生产代码零新增 import」。解析动作在两个调用方装配处（cmd/card_dispatch.go、agentd/cardstep.go startCardStep）经 discipline.ResolveDispatch 完成。
- 设计裁决：有效（角色名，目标机）的回退顺序（req 覆盖 > 模板缺省）抽成 ledgerstep.PreflightDiscipline 供两个调用方共用，ViaTemplate 内部同用一份私有函数——两处各抄一份迟早漂移，探错目标机的拒发闸比没有闸更危险。
- 设计裁决：cardstep 的解析放在 startCardStep **同步段**（goroutine 之前），拒发错误同步上抛 → handleCardStep default 分支映射为 400 + 可行动文案；对应卡面判据「拒发错误上抛且 errors.Is 可辨，HTTP 层映射为可行动文案；不发任务」。
- 设计裁决：能力位探活失败（网络错/对端过老 404）按 cap=nil 处置 → ResolveDispatch 拒发（契约 §2.4 缺席=不支持的保守方向），探活错误本身带上下文记 Warn。
- P1 岔口推断：仓内无裁决记录；卡面验收判据保留「P1 裁决 (a) 时加 --discipline-file 用例」且扇出已发生（裁决先于扇出是 breakdown 明文流程），故按 P1=(a) 执行——加 flag 与用例。若实际裁决为 (b)，回退范围隔离在 cmd/dispatch.go 的 flag 注册 + runDispatch 解析段 + 对应用例。此判断记入最终报文 notes。
- 红①（编译红，新建缝符号）：go test ./internal/ledgerstep -run 'TestViaTemplateCarriesResolvedDiscipline|…' → `unknown field DisciplineText in struct literal of type Dispatcher`（×3 用例 ×2 字段）。符号名与 Ticket 0 在 DispatchOpts/DispatchSnapshot 上已冻结的字段名逐字同源，非拼写错。
- 红②（断言红，空壳落地后）：三个接线用例全红——DisciplineText 未到位（0 vs 767）、未点名无平台层正文、日志缺 discipline_version 计量字段。原因均为「功能缺失」非 typo。
- TestDispatchSnapshotVersionKeyRegression 首跑即绿：它锁的是 Ticket 0 已交付的冻结 wire 行为（契约 §5 明言「现无测试」故补钉），不是新行为断言——红绿仪式不适用于已冻结行为的回归钉。
- 绿①实现：Dispatcher 加 DisciplineText/DisciplineVersion 数据字段；ViaTemplate 透传进 DispatchOpts 与快照；DispatchResult.DisciplineVersion 填充；派发日志加 discipline_version 计量字段；更新包注释（撤销「正文由 agentd 解析注入」旧契约声明）。
- 绿①落地：go test ./internal/ledgerstep -count=1 → ok（全包，含既有回归）。
- 红③（断言红）：cardstep 两用例——全链路 wire 无 discipline_text（空串）；能力位 nil/false 时 startCardStep 照常放行未拒发。
- 绿②落地中的回归与处置：同步解析改动撞了 4 处既有夹具假设，逐一裁决：
  (1) 在飞守卫次序：TestStartCardStepRejectsSecondInFlight 依赖 claim 先于一切 → 改回 claim-first，解析失败时当场 release 槽位（语义不变、不漏槽）。
  (2) 目标机未定的环节请求（无 target 且模板无 target）：既有语义是 202 受理后异步失败。PreflightDiscipline 改为纯计算不报空目标错；resolveStepDiscipline 对空 target 跳过解析走原有异步失败路径（该形态下派发必然在 ViaTemplate 被拒，不存在绕闸通道）。相关夹具零改动。
  (3) TestStartCardStepReleasesSlotOnFinish 夹具自相矛盾（建 demo 项目卡 D1 却调 B1）：旧代码不同步校验卡才侥幸绿。实测前缀 handoff→B1、demo→D1 后把夹具项目改为 handoff，节点名改用 ledger.StatusReview。
  (4) TestCardStepPropagatesRequestFields 点名 linux-01 会真实过闸：夹具补种 review 正文 + 假目标机（cap=true），其钉的字段传播断言不变。
- 绿②落地：go test ./internal/agentd -run 'TestCardStep|TestStartCardStep|TestRequiresInlineLocalFile' -count=1 → ok。
- 红④（断言红 + 新符号旗标红）：裸派发四用例——wire 无 discipline_text、能力位缺席/false 不拒发、--discipline-file unknown flag（新符号首红）。另发现测试进程 cwd 是本仓脏工作树，helper 需 --no-sync-check 与既有远程用例对齐（已修）。
- 范围裁决：cmd/card_dispatch.go（裸卡派发，CLI 侧 ViaTemplate 第三调用方）必须同票接线——契约 §2.2「ViaTemplate 只消费产物」意味着它的调用方都要喂解析产物；不接则 T1 落地后该路径静默下发空正文，恰是缺陷三换马甲。入口指针「openLedger（lookup 闭包源）/newTargetClientNamed（复用）」正对应此处。红⑤（新符号编译红）：dispatchRequest 无 disciplineText/disciplineVersion 字段。
- 变异自验三连（两段判定：先编译后数红；锚唯一性先断言）：
  M1 internal/agentd/cardstep.go stepTransport 丢 DisciplineText 转发（锚唯一）→ 编译过，TestCardStepDeliversResolvedDiscipline 红（wire 正文空串）。已还原。
  M2b cmd/dispatch.go 探活结果弃用恒放行（首版变异 status 未使用=编译不过，按纪律整块替换为可编译等价 `_ = status` 后重做）→ 编译过，能力位缺席/false 两子用例全红（拒发失效被逮住）。已还原。
  M3c internal/ledgerstep 快照版本写死 0（前两版锚不唯一：同文本出现 2 次，加 RecordDispatch 行上下文后命中唯一）→ 编译过，TestViaTemplateCarriesResolvedDiscipline 红于原始 payload 键断言。已还原。
- 验收命令实跑：go test ./internal/ledgerstep ./internal/agentd ./cmd -count=1 → 三包全 ok（agentd 131.9s）；go build ./... && go vet ./... 退出 0；gofmt -l 无输出。
- 契约闸：TestRepoContractGate 随 go test ./cmd 全绿（生产 import 仅新增 d_cli→d_policy、d_gateway→d_policy 的 discipline 包级函数调用，均为 target.json 已声明 entry）。
- 收尾复跑（变异还原后终态）：go test ./internal/ledgerstep ./internal/agentd ./cmd -count=1 → ok 5.563s / ok 125.333s / ok 8.495s。
- 收尾自审：错误分支均带上下文日志（探活失败、客户端取得失败、拒发闸、读文件失败）；成功路径有出口日志；新文件均有头注释，PreflightDiscipline 等导出符号有文档注释；与契约 §2.2/§2.3 冻结签名一致（零新增 wire 键、零新增 import 方向）。
