# B233.14 spec 台账

- 2026-09-12 用户：「三句都进，按你建议做。」B233.13 已合功能线 `148be3a5`。卡 `move spec`。
- 读数：`Server.scheduling *scheduling.Service` `internal/agentd/server.go:176`；`SetupAutomation` `scheduling.New` `:2682`；`SetScheduling(*scheduling.Service)` `:2751`。目标图 `d_gateway → d_scheduling` budget 0 entries=`scheduling.Service`。
- `prepareTaskProfile`：`HomeDir==""` 且纪律非空则失败（`internal/orchestration/manager.go`）。空 HOME 身份合法见 `IdentityOf`。
- 定级 L3 轻档：入站 DIP + 空 HOME×纪律，同进程，不迁 `Service` 包（已在 `internal/scheduling`）。
- DTO 留编制包（与 13 D1 不同：实现早已不在 gateway，无环）。`New` 迁 `cmd`。
- 弃选：New 留 SetupAutomation；DTO 搬 gateway；接口放 schedclient；空 HOME 改写绝对路径；本卡 assembly/封界。
