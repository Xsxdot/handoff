# B399 plan 台账（2026-09-22）

> 本文件是 B399 plan 节点的过程账：读码读数、亲跑命令原始输出、图查询、放弃的尝试逐条落这里，
> 计划正文只留结论与指针。
> 工作树：功能线 `cards/B233.1-charter-7`，起手 `63fd6c5e`（本树是 spec r1；r2 为 `235996304`，已 fetch）。
> 节点：charter（plan 节点，产出物 `docs/superpowers/plans/b399-plan.md`，不写实现）。

## 1. 环境与前置

```text
$ pwd
/root/.handoff/worktrees/17af87a5
$ git branch --show-current
cards/B399-charter        # 任务注入的节点分支；合并目标 cards/B233.1-charter-7
$ git rev-parse HEAD
63fd6c5edeba68ef9ebef4ca9e019ee613887427
$ go version
go version go1.26.1 linux/amd64
$ which codegraph
/usr/local/bin/codegraph
$ echo $TMPDIR
/root/.handoff/tmp/17af87a5
```

spec r2 本树无：`git cat-file -t 235996304` → `fatal: Not a valid object name`。
`git fetch origin cards/B233.1-charter-7` → `63fd6c5e..23599630`，随后 `git show 235996304 -- docs/superpowers/specs/b399.md`
取到 r2 全部差异（三处：§3 第 1 条改写、§5 第一条改写、「不变量改写」段）。spec r2 只改文档，`git diff --name-only 235996304~1 235996304 -- internal cmd` 为空。

## 2. 证伪 r1 §3.1（本节点核心核查）——原始输出

```text
$ grep -rn "RenewDriverLease" internal/ cmd/ --include=*.go | grep -v _test
internal/ledger/binding.go:312:func (s *Store) RenewDriverLease(...)
# 无任何非测试调用方（唯一命中是定义行）
$ grep -rn "\.RenewDriverLease(" . --exclude-dir=.git
internal/ledger/api/api_test.go:57               # 测试
internal/ledger/binding_test.go:275/283/290/296/317/346  # 测试
internal/collab/readmodel_test.go:620/634        # 测试
internal/collab/sessions_test.go:877             # 测试
# 生产零调用方
$ grep -rn "\.DriverLease(" internal/ --include=*.go | grep -v _test
internal/collab/service.go:619:  expiresAt, exists, err := s.lc.DriverLease(r.BoundSession)   # 展示（Live 列）
internal/collab/sessions.go:261:  expiresAt, exists, err := s.lc.DriverLease(identity)          # 展示（成员状态）
# 只被展示面读，不参与互斥
$ grep -rn "DriverLeaseRenewInterval" . --exclude-dir=.git
internal/ledger/binding.go:26:  DriverLeaseRenewInterval = 2 * time.Minute
internal/ledger/binding_test.go:268-269          # 常量冻结断言
docs/…（spec/plan/roadmap）
# 只是常量，无续租 goroutine
```

`internal/collab/sessions_test.go:875` 注释原文：「未过期租约：Store.RenewDriverLease 是本测试专属生产者（生产零调用方，…）」。
`docs/roadmap.md:644` 记债：「租约三法零生产消费方且不属三期：`RenewDriverLease`/`DropDriverLease`/`AllDriverLeases`」。
`internal/agentd/server.go:1178` 注释原文（旧）：「…本分支唤醒路径未接 ClaimWake/RenewDriverLease，超过租约的回合没有任何续租兜底，因此上界必须自缚于租约之内。」

⇒ r1 事实前提不成立；跨机互斥闸是 `wake_claims`：

```text
$ grep -rn "wakeClaimTTL" internal/ --include=*.go
internal/agentd/wakeconsumer.go:32:  wakeClaimTTL = 5 * time.Minute   # const
internal/agentd/wakeconsumer.go:506: got, err := s.ledger.ClaimWake(seq, card, holder, wakeClaimTTL)
$ grep -rn "ClaimWake" internal/ledger/wakeclaim.go
internal/ledger/wakeclaim.go:31:func (s *Store) ClaimWake(seq int64, card, holder string, ttl time.Duration) (bool, error)
# wakeclaim.go:52 `if existingHolder != holder && toTime(leaseUntil).After(s.timeNow())` ⇒ 同 holder 续期即 upsert
```

## 3. 基线亲跑（全部为本节点实跑原文）

```text
$ go build ./...
(build_exit=0)
$ go vet ./internal/agentd/ ./internal/keystone/ ./internal/hostapi/
(vet_exit=0)
$ go test ./internal/agentd/ -count=1
ok  github.com/Xsxdot/handoff/internal/agentd  155.628s
$ go test ./internal/hostapi/ -count=1
ok  github.com/Xsxdot/handoff/internal/hostapi  0.941s
$ go test ./internal/keystone/ -count=1
ok  github.com/Xsxdot/handoff/internal/keystone  0.265s
$ go test ./internal/ledger/ -count=1
ok  github.com/Xsxdot/handoff/internal/ledger  17.319s
$ go test ./internal/agentd/ -run 'TestAutomationFallbackResumeRebuildFailure|TestB389WakeEndpointExecutesAndRebinds|TestB393WakeTurnTimeoutWithinLease|TestB393WakeRoundFailureWritesFailEvent|TestB393WakeRoundQueueRetryDoesNotFloodFailRows' -count=1 -v
--- PASS: TestB393WakeTurnTimeoutWithinLease
--- PASS: TestB393WakeRoundFailureWritesFailEvent
--- PASS: TestB393WakeRoundQueueRetryDoesNotFloodFailRows
--- PASS: TestB389WakeEndpointExecutesAndRebinds
--- PASS: TestAutomationFallbackResumeRebuildFailure
PASS
ok  github.com/Xsxdot/handoff/internal/agentd  0.681s
```

关键含义（写进计划的红/绿判据）：
- `TestAutomationFallbackResumeRebuildFailure` 现状 PASS：generic resume 错误 ⇒ `Launch`（`launches==2`）——T2 要推翻的行为。
- `TestB393WakeTurnTimeoutWithinLease` 现状 PASS：`coordWakeTurnTimeout(2m30s) < DriverLeaseTTL(5m)`——r2 要作废的假设。

## 4. 图查询（codegraph --repo .）

命中：

```text
$ codegraph --repo . sym Wake            → n_keystone_Service_Wake (keystone.go:105)
$ codegraph --repo . sym briefing        → n_keystone_Service_briefing (anchor=moved; 基线行 322，实到 322+)
$ codegraph --repo . sym rebuildAfterResumeFailure → n_keystone_Service_rebuildAfterResumeFailure (159)
$ codegraph --repo . sym RunTurn         → n_hostapi_Host_RunTurn (hostapi.go:66)
$ codegraph --repo . sym coordinatorRunner → m_agentd_coordinatorRunner
$ codegraph --repo . sym NewCoordinatorRunner → n_agentd_NewCoordinatorRunner
$ codegraph --repo . sym LaunchForCard   → n_keystone_Service_LaunchForCard
$ codegraph --repo . sym consumeAutomationEventsOnce → n_agentd_Server_consumeAutomationEventsOnce
$ codegraph --repo . sym Store.RenewDriverLease → n_ledger_Store_RenewDriverLease
```

未命中（图覆盖债，回退 grep）：`coordWakeTurnTimeout`、`readyCtx`、`wakeClaimTTL`、`WakeRoundEvent`、
`WakeRoundDedupePrefix`、`ClaimWake`、`completeWakeBatch`、`WakeClaimsBefore`、`classifyRunnerErr`、
`startWakeClaimRenewal`、`wakeFailClass`、`ErrTurnTimeout`、`ErrSessionNotFound`。

`codegraph --repo . flow Wake` → `degraded=true, steps=0, missing="基线没有 flows 段"`：按纪律**不拿 chain 冒充**，
已读源码补控制流（`keystone.go:145-158`）。`codegraph context d_execution_host` 成功（用 best 领域 id）。

## 5. 读码读数（带出处）

| 环节 | 出处 | 读数 |
|---|---|---|
| 上界 | `internal/agentd/server.go:1182` | `var coordWakeTurnTimeout = ledger.DriverLeaseTTL / 2`（2m30s） |
| 超时错误 | `internal/hostapi/driver.go:152-156` | `hostapi: 回合超时（上界 %v），已终止进程树: %w`（无哨兵） |
| 会话不存在通道 | `internal/hostapi/driver.go:157-165` | stderr 尾部进错误串（无哨兵） |
| resume 分流 | `internal/keystone/keystone.go:147-157` | 任何 resume 错误 → `rebuildAfterResumeFailure` → `Launch` |
| 简报 | `internal/keystone/keystone.go:322-344` | 首句「醒来第一件事：读卡…」末句「点火前看上一节点产出…」 |
| 失败出口 | `internal/agentd/scheddrain.go:539-547` | 只 `writeWakeRoundFail` + 「唤醒回合失败」日志，无「结束」行 |
| 认领点 | `internal/agentd/wakeconsumer.go:506` | `ClaimWake(seq, card, holder, wakeClaimTTL)`；仅消费循环调用（`:682`） |
| 队列路径 | `internal/agentd/scheddrain.go:178` | `drainIgnitionRequest` **不认领**（PopReady CAS 出队） |
| `EnsureComment` | `internal/ledger/events.go:299` | 返回 `(wrote bool, err error)`，同键重试 `wrote=false` |
| `ClaimWake` 续期 | `internal/ledger/wakeclaim.go:52` | 同 holder + `done_at` 空 ⇒ upsert 续期 |
| 写超时 | `cmd/agentd.go:442-449` | `WriteTimeout = RunCmdTimeout + 1m = 11m`（见计划 §8 残余） |

## 6. 协调者工单往返（决策留痕）

- 工单 `eaaf8bd5`：证伪 r1 §3.1，请协调者裁决。协调者回执：核查成立，r1 前提作废，用户裁定 **A**（回合内认领续租 + 30m），spec 已改 r2。
- 工单 `247a6623`：四处口径。协调者逐条答复：① **不同意**「其他错误也重建」——只有真 `Session not found` 才 Launch；② **不同意**「超时轮不落 needs_human」——超时轮落**恰一次** needs_human；③ 同意乙（正常消费收口 + 不引入 B390 式同 seq 重试）；④ 同意（end 是日志行 + fail 加 class，不加 phase=end）。
- 四条裁定已逐条写入计划 §0、§5、§6、T2、T3。

## 7. 放弃的尝试 / 自我更正

1. 初稿把 `coordWakeTurnTimeout` 直接写 30m 而不处理认领续租——核查 lease 后确认会产生同卡双跑，改为「配套续租」，并回工单请协调者裁决（已按 A 落地）。
2. 初稿曾倾向「其他错误仍重建 / 超时轮不落 needs_human」——协调者驳回，计划已按回执改正（无残留）。
3. 初稿 T2.4/T2.5 用了 `svc.RunnerForTest()` 设想——会污染生产 API，改为「runner 直传 + `fakeRunner` 增 `refs` 字段」（仅测试面）。
4. `flow Wake` 无数据时曾想用 `chain` 看调用面，按纪律改用 `sym`+读源码（chain 非 flow）。

## 8. 收口自审

- ① 有没有把没亲自跑到结果的命令写成结论？——§3 全是实跑原文；`WriteTimeout` 残余明确标「读码事实，非实测」。
- ② 这一轮碰过 handoff CLI 或起过新 executor 吗？——未碰；只用 codegraph/git/go test/grep。

## 10. 提交事实（历史读数）

```text
$ git add docs/superpowers/plans/b399-plan.md docs/superpowers/ledgers/2026-09-22-b399-plan-ledger.md
$ git commit -m "plan(B399): 唤醒回合上界/认领续租/错误分流/增量简报——实现计划 + 台账"
[cards/B399-charter 59454449] plan(B399): 唤醒回合上界/认领续租/错误分流/增量简报——实现计划 + 台账
 2 files changed, 1402 insertions(+)
 create mode 100644 docs/superpowers/plans/b399-plan.md
 create mode 100644 docs/superpowers/ledgers/2026-09-22-b399-plan-ledger.md
```

（本行为提交后追加进台账的收口记录；随后 amend 一次收进同批提交——amend 换 hash 是 git 事实，不 chase。）
