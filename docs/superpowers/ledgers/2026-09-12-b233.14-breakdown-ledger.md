# B233.14 breakdown 台账（2026-09-12）

节点：`charter:breakdown`（B233.14 编制入站封界：gateway 持门面；接收机自证；空 HOME 可带纪律）
分支：`cards/B233.14-charter-3`（本卡不切分支、不改 git 配置、不调 handoff CLI、不起 executor）
有效基线：`cards/B233.1-charter-7`（本卡不切、不越过）
上游：spec `docs/superpowers/specs/b233.14.md`（头部「已批准（2026-09-12…）」）；contract `docs/superpowers/specs/b233.14-contract.md`（头部「冻结状态：随本提交冻结」@ `a49eea84`）
产出物：`docs/superpowers/specs/b233.14-breakdown.md`
起点 HEAD `a49eea84`（charter-3 == charter-2 == charter，a40a611d 上轮归档）

本台账只记过程与原始读数；结论与待拍板回写 breakdown 正文与 §0。

## 1. 入口读数（本工作树复核，非会话记忆）

- `Server.scheduling *scheduling.Service`：`internal/agentd/server.go:176`。
- `SetupAutomation` `:2668`；`s.scheduling = scheduling.New(facadeAsRegistry{f: facade})` `:2682`；`SetKnownMachines` `:2683`。
- `SetScheduling(*scheduling.Service)` `:2751`；`Scheduling() *scheduling.Service` `:2781`；`facadeAsRegistry` `:2789`；`translateRegistryErr` `:2825`；`RecoverOnStartup` 用 `facadeAsRegistry{f: s.autoLedger}` `:2741`。
- 生产对 `s.scheduling` 的直接方法调用 18 个（receiver_bind/schedapi/scheddispatch/scheddrain/coordapi/server/coordinator_home），与 contract §1 表逐一核对一致；唯一装配期调用 `SetKnownMachines`（`server.go:2683`）。
- 生产 `SetDefaultCarrier` 仍无调用点（仅测试）——contract §3「疑似漂移」复核成立。
- `prepareTaskProfile`：`internal/orchestration/manager.go:1054`；空 HOME 拒绝门 `:1060`；`ProfileReq.HomeDir: start.Task.HomeDir, Isolated: start.Task.HomeDir != ""` `:1073-1077`。**仅 `Dispatch` 调用一次（`:1007`）**；`resumeForContinue` 不调用（`loadPersistedDiscipline` 回注，`:1456`）。
- `withCarrierHome` `:1108`；`expandCarrierHome` `:1125`（空串 no-op；`~`/`~/`/`~\` 展开 `os.UserHomeDir()`，失败原样返回）。
- `Task.HomeDir` 派发期赋值 `:925-932`（`req.HomeDir==nil || *req.HomeDir==""` → 空串）。
- `cmd/agentd.go#setupLedger` `:466`；`SetupAutomation` `:480`；`StartAutomation` `:483`（`StartAutomation` 有 `s.scheduling == nil` 闸，`internal/agentd/scheddrain.go:52`）。
- `Server.Conf()` `internal/agentd/server.go:401`（返回活配置取值函数）。
- 五家 `Profile.Prepare` 拒绝空 `HomeDir`：opencode/claudecode/codex/grok/agy `profile.go:78`，均 `errors.New("Profile.Prepare 缺少 HomeDir")`。
- overlay 落点 `filepath.Join(req.HomeDir, ".handoff", "task-overlay", Clean(o.Name))`：`internal/executor/opencode/profile.go:125-138`，其余四家同形；`Rules`/`Skills` 仅在非空时写 `:88-123`。
- `proto.Task.HomeDir json:"home_dir,omitempty"`：`internal/proto/proto.go:227`；金样本 `internal/proto/proto_test.go#TestTaskHomeDirJSON` `:180`。
- `IdentityOf` `internal/scheduling/receiver.go:148`；`BindPhysical` `:153`；`DefaultHomeDir`/`IsolatedHomeRoot` `internal/scheduling/status.go:55/:33`。
- `Scheduling()` 测试消费点实测 **57 处**（`grep -c '\.Scheduling()' internal/agentd/*_test.go` 合计）；其中经 `Scheduling()` 取回具体服务后调的方法：`Admit`×1、`AdmitCarrier`×7、`ApplyDetect`×1、`Enqueue`×4、`PutCarrier`×1、`PutSquad`×7、`QueueSnapshot`×2、`Release`×5、`SetDefaultCarrier`×3。
- `*scheduling.Service` 具体类型测试引用：`coordapi_test.go:125`（`putOnlineCarrier(t, svc *scheduling.Service, …)` 辅助）、`schedapi_test.go:32`（`schedEnv.svc *scheduling.Service` 字段）、`hostprobe_test.go:51/113/284`（`schedEnv{svc: srv.Scheduling()}`）。
- `SetScheduling(scheduling.New(...))` 具体实参：`receiver_occupancy_test.go:392`、`scheddispatch_test.go:443`、`scheddrain_test.go:131`。
- `SetupAutomation` 测试装配点：11 个文件 33 处（coordapi/hostprobe/integration/receiver_dispatch/recover_occupancy/roomsapi/schedapi/scheddispatch/scheddrain/server/wakeconsumer）。
- `SetDefaultCarrier` 测试点 6 处（integration 2、receiver_dispatch 2、server_test 1、+ receiver_dispatch 局部 `svc` 1）。

## 2. 本节点实跑命令与原始结果

| # | 命令 | 原始结果 |
|---|---|---|
| 1 | `go build ./...` | 无输出，`BUILD=0` |
| 2 | `go vet ./internal/agentd/ ./internal/orchestration/ ./internal/scheduling/ ./cmd/` | 无输出，`VET=0` |
| 3 | `go test ./internal/scheduling/ -run 'TestIdentityOfAndBindPhysical\|TestResolveLookupTable\|TestDefaultCarrierRecordGolden' -count=1` | `ok … 0.003s` |
| 4 | `go test ./internal/orchestration/ -run 'TestDispatchInjectsTaskDisciplineIntoProfile\|TestDispatchRejectsDisciplineWithoutTaskProfile\|TestDispatchPersistsFrozenIdentity' -count=1` | `ok … 0.302s` |
| 5 | `go test ./internal/orchestration/ -count=1` | `ok … 46.131s`（全包绿） |
| 6 | `go test ./internal/agentd/ -run TestB23313ConcurrentDoneReleasesOnce -count=1` | `ok … 0.176s` |
| 7 | `go test ./internal/orchestration/ -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1` | `ok … 0.055s` |
| 8 | `go test ./internal/approval/ -run TestClientApproverAllowUndeliveredUntilAck -count=1` | `ok … 0.049s` |
| 9 | `codegraph check --view cards-B233.14-charter` | `fails=6`（1 dead-interface `OrchestrationClient` + 5 over-budget）、`warns=138` |
| 10 | `codegraph check`（不带视图） | `fails=7`（多一条 `SchedulingClient` dead-interface）、`warns=139` |
| 11 | `codegraph validate` / `codegraph validate --view cards-B233.14-charter` | 退出码 0（无输出） |
| 12 | `codegraph resolve --doc b233.14.md` / `--doc b233.14-contract.md` | 均退出码 0 |
| 13 | 遍历 `codegraph sym <符号>` | HIT：`Server`、`SetupAutomation`、`Server.SetScheduling`、`Server.Scheduling`、`Manager.Dispatch`、`Service.*`（16/18）；MISS：`SchedulingRegistry`、`facadeAsRegistry`、`prepareTaskProfile`、`taskProfileHome`、`expandCarrierHome`、`withCarrierHome`、`Service.AdmitCarrier`、`Service.DeleteCarrier`、`Service.DefaultCarrier`、`OrchestrationClient`（记图覆盖债） |
| 14 | 视图 diff 解析 `codegraph/diffs/cards-B233.14-charter.json` | `base=02180141`；`containersAdded=[k_agentd_scheduling_client]`；`nodesAdded=[m_agentd_SchedulingClient]`；`edgesAdded=[[n_cmd_setupLedger,n_scheduling_New]]` |

## 3. 本节点探针（配对还原，工作树已恢复干净）

- **探针 A（验证 contract §2.3 与 §5.2 #18 的 tilde 一致性）**：临时在 `internal/orchestration/` 加 `zz_tmp_verify_test.go`，用 `recordingProfile` 抓 `ProfileReq`；派发 `HomeDir="~/.handoff/home/muse"` + 非空纪律。原始输出：
  `zz_tmp_verify_test.go:33: ProfileReq.HomeDir="~/.handoff/home/muse" Isolated=true` → PASS，随后 `rm` 删除，`git status` 干净。
  结论：**现状非空 tilde HOME 原样透传给 `Profile.Prepare`**。
- **探针 B（纯 stdlib，放任务 tmp，不入仓）**：`filepath.Join("~/.handoff/home/muse", ".handoff", "task-overlay", …)` → `Join="~/.handoff/home/muse/.handoff/task-overlay/task-1/discipline.md"`，`IsAbs=false`。即非空 tilde HOME 下 overlay 落点是**相对路径**，`Profile.Prepare` 会把 `~` 当普通目录名在 cwd 下建目录。contract §5.2 #18「写入点 = `Task.HomeDir`、现状不回退」与该事实冲突（见 breakdown §2.3 C-1 / §0 P3）。
- 探针文件均未留在工作树；`git status --short` 空。

## 4. 现场结论（供 breakdown 引用）

- 契约 §2.3 的 `taskProfileHome`（非空展开 `~`、空取 `os.UserHomeDir()`）会**顺带修正**非空 tilde 的相对路径缺陷；#18 字面「现状不回退」需澄清。
- 空 HOME×纪律缝仅发生在 `Manager.Dispatch`；`resume/continue` 不重跑 `prepareTaskProfile`。
- `Scheduling()` 返回接口后，~57 处测试消费点与 3 个具体类型夹具需迁移（最大实现风险，contract §8 已移交 plan）。
- `SetScheduling` 接口化后存在 typed-nil 风险（contract 未冻结此点；参照 B233.13 P5）。

## 5. 待拍板与回写

- 本稿 §0 列 P1–P5；裁决由协调者逐条回写 §0 与 contract 修订区。
- 结论：本节点不建卡、不派发、不调 handoff CLI、不起 executor。收口判据：工作树干净（本台账 + 产出物 + contract 修订同批提交）。

## 6. 提交事实（历史读数，随后 amend 一次收口）

- `git add docs/superpowers/specs/b233.14-breakdown.md docs/superpowers/specs/b233.14-contract.md docs/superpowers/ledgers/2026-09-12-b233.14-breakdown-ledger.md`
- `git commit -m "breakdown(B233.14): …"` → `[cards/B233.14-charter-3 2b20b4f6] breakdown(B233.14): 编制入站封界拆解提案——单一实现范围 + P1–P5 待拍板 + 契约澄清回写`
- 提交前 `go build ./...` → `BUILD=0`；`codegraph resolve --doc b233.14-breakdown.md` / `--doc b233.14-contract.md` → 均退出码 0。
- 收口判据是工作树干净，不是文件里的 hash 等于 HEAD（本批 amend 收进台账后 hash 会变）。
