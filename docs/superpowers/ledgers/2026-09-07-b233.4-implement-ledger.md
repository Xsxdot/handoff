# B233.4 implement 台账

节点：charter/implement  
卡：B233.4  
分支：cards/B233.4-charter-4  
基线 HEAD：3aed2512e51c0f6e2e4e62dd79141cd02c5ac476  
规则：每确立一个事实立刻追加一行；测试必须先见红再写实现；变异自验确认有牙；提交事实记命令与原始输出，随后只 amend 一次。

---

## 2026-09-07 开工检查

- 角色：implement 执行者。按 `docs/superpowers/plans/b233.4-plan.md` 逐步执行 T1–T6。
- 纪律红线：
  - 不派发、不调用 handoff CLI、不起新的 executor 进程或子任务。
  - 提问纪律：需要人决策输出单行 JSON `{"ask":"..."}` 并结束回合。
  - 收尾纪律：git add 并 commit（不要 push），输出单行 JSON `{"branch":"...","commit":"...","summary":"..."}`。
  - 拍板不得改写：P1(a), P2(a), P3(a), P4(a), P5(a) 全选 a 已拍板。
  - TDD 铁律：先红后绿，无失败测试不写生产代码。变异自验。日志与注释。
  - Stop 不得删 managed 树；Capability nil fail-closed，禁止降级包级函数；不改 forward.go、不改 push 归属、不加 CardIDs；真机清单归协调者，机内不得写成已绿。
- 基线检查：
  - `git status`: On branch cards/B233.4-charter-4, working tree clean.
  - `codegraph --repo . --view cards-B233.4-charter check`: fails: [], legacyHits d_orchestration->d_workspace 18/19, d_cli->d_workspace 9/9.
  - `go test ./internal/workspace -count=1`: ok 0.002s.
  - `go test ./internal/agentd -run 'TestID8MatchesWorkspaceCanonical|TestBaseCommitReMatchesWorkspaceIsCommitSHA' -count=1`: ok 0.016s.
  - `go test ./internal/agentd -run 'TestStopRemovesManagedWorktree|TestStopReportsWorktreeRemoved|TestDoneRemovesManagedWorktree|TestDoneKeepsUserWorktree|TestDispatchFailedAfterWorkspaceCleansManagedWorktree|TestTaskDiffTargetKeepsWorktreeWhenPresent|TestTaskDiffTargetFallsBackToRepoWhenWorktreeGone|TestTaskDiffTargetKeepsWorktreeWhenBranchUnknown|TestPrepareWorkspaceNewWorktreeAllowsDirtyMainRepo|TestPrepareWorkspaceMutualExclusionAndInjection|TestReclaimRefusesNonTerminal|TestReclaimRefusesNotManaged' -count=1`: ok 1.812s.
  - `go test ./internal/client -run 'TestStopParsesWorktreeRemoved' -count=1`: ok 0.009s.
  - `go build -o /dev/null .`: exit 0.
  - `cd web && npm test -- src/app/task/Composer.test.tsx`: 6 passed, 719ms.

## Task 1: 身份收口——生产路径只留一套截断规则

- TDD 红灯与断言锁定：
  - 追加 `TestResolveBaselineRejectsUppercaseSHA`：大写 hex 必须失败且错误链含 `ErrBadWorkspaceReq`。
  - 删除 `baseCommitRe` 后，`TestBaseCommitReMatchesWorkspaceIsCommitSHA` 编译红（`undefined: baseCommitRe`），退役该过渡测试。
- T1 最小实现与绿灯：
  - `internal/agentd/workspace.go`: 引入 `internal/workspace`，`id8` 委托 `workspace.ID8`，`taskBranch` 委托 `workspace.TaskBranch`，删除 `baseCommitRe`，`ResolveBaseline` 校验改调 `workspace.IsCommitSHA`。
  - `cmd/wait.go`: 引入 `internal/workspace`，`id8` 委托 `workspace.ID8`。
  - 测试通过：
    - `go test ./internal/agentd -run 'TestID8MatchesWorkspaceCanonical|TestResolveBaselineRejectsUppercaseSHA' -count=1`: ok 0.025s.
    - `go test ./internal/workspace -run 'TestID8AndTaskBranchGoldenVectors|TestIsCommitSHAGoldenVectors' -count=1`: ok 0.002s.
    - `go test ./cmd -run 'id8|ID8|Wait' -count=1`: ok 2.690s.
    - `go build -o /dev/null .`: exit 0.
- 变异自验：
  - 将 `internal/agentd/workspace.go` 的 `id8` 改为 `taskID[:7]`，运行 `go test ./internal/agentd -run 'TestID8MatchesWorkspaceCanonical' -count=1`。
  - 变异结果：`FAIL: TestID8MatchesWorkspaceCanonical: id8("abcdefgh-rest") = "abcdefg", workspace.ID8 = "abcdefgh"`。断言有牙。
  - 恢复实现后测试绿：ok 0.003s。
- 图预算核对：
  - `codegraph --repo . --view cards-B233.4-charter check`: fails: [], legacyHits d_orchestration->d_workspace 18/19, d_cli->d_workspace 9/9.
- 提交事实：
  `git commit -m "docs(B233.4): T1 身份编码收成 workspace.ID8/TaskBranch"`
  输出：`[cards/B233.4-charter-4 71433d49] docs(B233.4): T1 身份编码收成 workspace.ID8/TaskBranch`

## Task 2: Capability 适配器与 Dispatch 接线

- TDD 红灯：
  - 编译红：`undefined: NewGitCapability`、`m.SetWorkspace undefined`、`undefined: ErrWorkspaceUnavailable`。
  - 空壳断言红：
    - `TestDispatchCallsCapability`: `Dispatch 必须经 Capability：ensure=0 resolve=0 prepare=0`
    - `TestDispatchNilCapabilityFailsClosed`: `nil Capability 必须失败且错误链含 ErrWorkspaceUnavailable，实得 <nil>`
- T2 最小实现与绿灯：
  - `internal/agentd/workspace_capability.go`: 实现 `gitCapability` 包装既有包级函数，导出 `NewGitCapability`、`ErrWorkspaceUnavailable`、`SetWorkspace`、`Workspace`。
  - `internal/agentd/manager.go`: `Manager` 结构体增加 `ws workspace.Capability` 字段；`Dispatch` 前置检查 `requireWorkspace()`（禁止 nil 降级包级函数），`EnsureRepoUsable` / `ResolveBaseline` / `Prepare` 全面改走 `m.ws`。
  - `internal/agentd/server.go`: `SetManager` 在 `ws==nil` 时默认绑定 `NewGitCapability()`；`writeDispatchError` 映射 `ErrWorkspaceUnavailable` 为 HTTP 503。
  - `cmd/agentd.go`: `NewManager` 之后调用 `mgr.SetWorkspace(agentd.NewGitCapability())`。
  - `internal/agentd/manager_test.go`: `newTestManagerWithApprover` 与 `TestDispatchFailedAfterWorkspaceCleansManagedWorktree` 绑定 `NewGitCapability()`。
  - 测试通过：
    - `go test ./internal/agentd -run 'TestDispatchCallsCapability|TestDispatchNilCapabilityFailsClosed|TestDispatchFailedAfterWorkspaceCleansManagedWorktree|TestPrepareWorkspaceNewWorktreeAllowsDirtyMainRepo|TestPrepareWorkspaceMutualExclusionAndInjection' -count=1`: ok 0.669s.
    - `go test ./internal/workspace -count=1`: ok 0.002s.
    - `go build -o /dev/null .`: exit 0.
- 静态约束检查：
  - `git grep -n -E 'NewGitCapability|gitCapability\{' -- ':!*_test.go'`: 仅出现在 `workspace_capability.go`、`cmd/agentd.go`、`server.go`。
  - `go list -f '{{.Imports}}' ./internal/workspace`: `[context fmt reflect regexp]`，无 proto/ledger。
- 图预算核对：
  - `codegraph --repo . --view cards-B233.4-charter check`: fails: [], legacyHits d_orchestration->d_workspace 18/19.
- 提交事实：
  `git commit -m "feat(B233.4): Dispatch 经 workspace.Capability 准备基线与隔离树"`
  输出：`[cards/B233.4-charter-4 499686d0] feat(B233.4): Dispatch 经 workspace.Capability 准备基线与隔离树`

## Task 3: 留存接线——Stop 留现场，归档/显式/补偿才回收

- TDD 红灯：
  - 更新 `TestStopRetainsManagedWorktree` 与 `TestStopReportsWorktreeRemoved` 后断言红：
    - `TestStopRetainsManagedWorktree`: `成功 Stop 后 worktree_removed 必须为 false（契约 C-6）`
    - `TestStopReportsWorktreeRemoved`: `managed stop 应返回 false`
- T3 最小实现与绿灯：
  - `internal/agentd/manager.go`:
    - `Stop`: 移除 `RemoveManagedWorktree`，调用 `workspace.MayRecycle(TriggerStop)` 判定为 keep，`worktreeRemoved` 恒为 false。
    - `Done`: 状态迁为 completed 后调用 `workspace.MayRecycle(TriggerDone)`，判定为 recycle 时经 `m.ws.RecycleManaged` 清理。
    - `compensateWorkspace`: 调用 `workspace.MayRecycle(TriggerCompensate)`，经 `m.ws.RecycleManaged` 清理。
  - `internal/agentd/reclaim.go`:
    - `Reclaim`: 增加 `workspace.MayRecycle(TriggerExplicit)` 判据检查，非 recycle 拒绝回收。
  - `internal/agentd/gc.go`:
    - `appendGCWorktreesExecute`: 对 managed 行增加 `workspace.MayRecycle(TriggerExplicit)` 检查，keep 则 skip。
  - `internal/agentd/server_test.go`: 增加 `TestStopManagedReturnsWorktreeRemovedFalseInBody`。
  - `cmd/stop.go` & `cmd/stop_test.go`: 抽取出 `stopOutcomeLine` 与 `runStop`，提示文案明确「现场已留存，显式 reclaim/gc 才清」；`TestStopCLIRetainCopy` 验证投影。
  - Web: `web/src/app/task/Composer.tsx` 将 false 提示改为「已停止；现场已留存，显式 reclaim/gc 才清。」，`Composer.test.tsx` 增加二次确认后提示断言；更新 `client.ts` 与 `types.ts` 注释。
  - 测试通过：
    - `go test ./internal/agentd -run 'TestStop|TestDoneRemovesManagedWorktree|TestDoneKeepsUserWorktree|TestDispatchFailedAfterWorkspaceCleansManagedWorktree|TestReclaimRefusesNonTerminal|TestReclaimRefusesWaitingReview|TestReclaimRefusesNotManaged' -count=1`: ok 3.225s.
    - `go test ./cmd -run 'TestStopCLIRetainCopy' -count=1`: ok 0.004s.
    - `go test ./internal/workspace -run TestMayRecycle -count=1`: ok 0.001s.
    - `cd web && npm test -- src/app/task/Composer.test.tsx`: 7 passed, 953ms.
    - `go build -o /dev/null .`: exit 0.
- 约束自检：
  - `grep -n -E 'RemoveManagedWorktree|RecycleManaged' internal/agentd/manager.go`: Stop 函数体内 0 次调用。
- 图预算核对：
  - `codegraph --repo . --view cards-B233.4-charter check`: fails: [], legacyHits d_orchestration->d_workspace 18/19.
- 提交事实：
  `git commit -m "feat(B233.4): Stop 留存 managed 工作树，回收改问 MayRecycle"`
  输出：`[cards/B233.4-charter-4 73bb0d50] feat(B233.4): Stop 留存 managed 工作树，回收改问 MayRecycle`

## Task 4: 结果引用与 diff 右端

- TDD 红灯与断言锁定：
  - `TestTaskDiffTargetFallbackHeadRevIsNotHEAD`: 树已回收时右端不得是主仓 HEAD，应是任务分支。
  - `TestAssembleResultRefEmptyCommitAllowed`: commit 未知时允许空，不得编造 commit。
  - `TestBaseCommitDoesNotDriftWithDefaultBranch`: 任务 BaseCommit 锁定基线快照，不得随主线 commit 漂移。
- 变异自验：
  - 将 `taskDiffTarget` 中回退分支篡改为 `"HEAD"`，运行 `go test ./internal/agentd -run 'TestTaskDiffTargetFallbackHeadRevIsNotHEAD' -count=1`。
  - 变异结果：`FAIL: TestTaskDiffTargetFallbackHeadRevIsNotHEAD (0.00s) diffbase_test.go:142: 树已回收时右端不得是主仓 HEAD`。断言有牙。
  - 恢复后测试通过：ok 0.003s。
- T4 最小实现与绿灯：
  - `internal/agentd/workspace_capability.go`: 实现 `assembleResultRef(ctx, task, repo, headRev)`，rev-parse 解析 commit 并做 `IsCommitSHA` 校验（非法或失败留空不编造），检测工作树存活，组装 `workspace.NewResultRef`，打 Info/Warn 日志。
  - `internal/agentd/server.go`:
    - `handleTaskDiff`: 前置调用 `s.mgr.assembleResultRef` 记录结果引用；改经 `s.mgr.Workspace().DiffRange` 执行 diff。
    - `handleTaskFile`: 改经 `s.mgr.Workspace().ReadFile` 读取文件，映射回 `proto.FileRead` 写入 JSON。
  - 测试通过：
    - `go test ./internal/agentd -run 'Diff|ReadFile|TaskDiffTarget|BaseCommit|ResultRef|assembleResultRef' -count=1`: ok 3.966s.
    - `go build -o /dev/null .`: exit 0.
- 图预算核对：
  - `codegraph --repo . --view cards-B233.4-charter check`: fails: [], legacyHits d_orchestration->d_workspace 18/19.
- 提交事实：
  `git commit -m "feat(B233.4): 审阅 diff 组装 ResultRef 且右端不用主仓 HEAD"`
  输出：`[cards/B233.4-charter-4 ed663a1c] feat(B233.4): 审阅 diff 组装 ResultRef 且右端不用主仓 HEAD`

## Task 5: 不挂卡——工作区函数零写账本

- TDD 红灯与断言锁定：
  - `TestProjectWorktreeCreateAttachesCardsAfterGit`: 增加间谍 `cardCheckingCap`，断言建树必须经 `Capability.CreateManual`（未接线时 `createCalls < 1` 红灯），且在 `CreateManual` 执行期间账本不得写卡基线（卡基线为空）。
  - `TestProjectWorktreeCreateNilCapabilityFailsClosed`: nil Capability 建树必须返回 503（未接线时返回 200 红灯）。
  - 运行红灯：
    - `TestProjectWorktreeCreateAttachesCardsAfterGit`: `必须经 Capability.CreateManual 建树，实得 0`
    - `TestProjectWorktreeCreateNilCapabilityFailsClosed`: `nil Capability 建树必须返回 503，实得 status=200`
- T5 最小实现与绿灯：
  - `internal/agentd/projectadmin.go`: `handleProjectWorktreeCreate` 检查 `s.mgr` 与 `s.mgr.requireWorkspace()`，未就绪返回 503；改调 `s.mgr.Workspace().CreateManual(...)` 建树，入参 `workspace.ManualReq` 零传 CardIDs；建树成功后再在应用层调用 `attachCardBaseBranches` 逐卡挂基线。
  - `internal/agentd/forward.go`: 零改动（`git diff 3aed2512 --name-only` 不含 `forward.go`）。
  - 测试通过：
    - `go test ./internal/agentd -run 'TestProjectWorktreeCreateAttachesCardsAfterGit|TestProjectWorktreeCreateNilCapabilityFailsClosed' -count=1`: ok 0.253s.
    - `go test ./internal/agentd -run 'Worktree|CardBase|ForwardWorktree|CreateManual|PrepareReqHasNoCardIDs' -count=1`: ok 11.638s.
    - `go test ./internal/workspace -run 'TestPrepareReqHasNoCardIDs|TestCapabilityHasNoPush' -count=1`: ok 0.002s.
    - `go build -o /dev/null .`: exit 0.
- 图预算核对：
  - `codegraph --repo . --view cards-B233.4-charter check`: fails: [], legacyHits d_orchestration->d_workspace 18/19.
- 提交事实：
  `git commit -m "feat(B233.4): 手工建树经 Capability，挂卡留在应用"`
  输出：`[cards/B233.4-charter-4 42b9dac5] feat(B233.4): 手工建树经 Capability，挂卡留在应用`





