# B233.4 contract 台账

日期：2026-09-07。节点：charter contract。工作分支：`cards/B233.4-charter`。开工 HEAD：`124121e4`。

## 过程

1. 核对上游 spec `docs/superpowers/specs/b233.4.md` 头部状态行 = **已批准**（2026-09-06，用户原话「按这个推进吧」），与 prompt 一致，未回写。定级 L3 轻档。基线声明 `main @ a9b839f9`；本卡有效功能基线按 prompt 为 `cards/B233.1-charter-7 @ 124121e4`（已含 B233.1 实现）。
2. 查图：`codegraph summary` / `domains` / `sym PrepareWorkspace` / `context d_workspace` / `check`。`PrepareWorkspace` 容器 `k_agentd_fn`，基线 domain `d_coordination_task`，best 归 `d_orchestration`。无 `--view` 时 check 两条 fail 均为 B233.1 `ApprovalClient` dead-contract/dead-interface（视图未 absorb）。`--view cards-B233.1-charter-2` 则 `fails []`。
3. 基线代码事实已对照：`workspace.go#PrepareWorkspace`/`ResolveBaseline`/`RemoveManagedWorktree`/`DiffRange`/`taskDiffTarget`/`gitExec`/`id8`、`manualworktree.go#CreateManualWorktree`（零引用 CardIDs）、`projectadmin.go#attachCardBaseBranches`、`forward.go#forwardWorktreeIfRequested`、`manager.go#Dispatch`/`Done`/`Stop`、`reclaim.go#Reclaim`、`gc.go#GC`、`client.go#Client.Diff`、`proto.go#Task`/`IsTerminal`、`projects.go#CreateWorktreeReq`。
4. 架构法第一条：不新立顶层域。第三条：agentd oversized + `work*` 前缀族——契约包 `internal/workspace` 立户口，本节点不整包搬家。
5. `d_orchestration→d_workspace` 现状 18 / 预算 19。Ticket 0 不把 Manager 生产路径接到新包，避免超预算。
6. Ticket 0 空壳：`internal/workspace/{capability,identity,retain}.go`；金样本测试；agentd 侧 `id8`/`baseCommitRe` 对齐测试。
7. 契约文档写到法定路径 `docs/superpowers/specs/b233.4-contract.md`。视图 `codegraph/diffs/cards-B233.4-charter.json`（复制 B233.1 视图节点并追加本卡符号，否则 check 会把 ApprovalClient 打成 dead-interface）。target.json 仅给既有 `d_orchestration→d_workspace` 补 B233.4 注记，不新开方向、不改预算。best.json 增 `k_workspace_fn` / `k_workspace_model` → `d_workspace`。

## 命令读数

- `gofmt -w internal/workspace/*.go internal/agentd/workspace_identity_contract_test.go`：exit 0；随后 `gofmt -l` 空输出。
- `go test ./internal/workspace/ ./internal/agentd/ -count=1 -timeout 180s -run 'TestID8AndTaskBranchGoldenVectors|TestTaskBranchPrefixLiteral|TestIsCommitSHAGoldenVectors|TestNewResultRefRejectsBadCommit|TestCapabilityHasNoPush|TestPrepareReqHasNoCardIDs|TestManualModeLiterals|TestTriggerAndDecisionLiterals|TestMayRecycle|TestID8MatchesWorkspaceCanonical|TestBaseCommitReMatchesWorkspaceIsCommitSHA'`：两包 `ok`，exit 0。
- `go test ./internal/workspace/ -count=1 -timeout 60s`：`ok`，exit 0。
- `go test ./internal/workspace/ ./internal/agentd/ -count=0`：两包 `ok ... [no tests to run]`，exit 0（编译测试包）。
- `go build -o /dev/null .`：exit 0。
- `codegraph --repo . --view cards-B233.4-charter validate`：exit 0，`issues: null`。
- `codegraph --repo . --view cards-B233.4-charter check`：exit 0，`fails []`。`d_orchestration->d_workspace` 仍 18/19。
- `codegraph --repo . resolve --doc docs/superpowers/specs/b233.4-contract.md --view cards-B233.4-charter`：exit 0。新符号 `Capability` / `MayRecycle` 均为 `anchor=ok`。存量 `Manager.Dispatch`/`Done`/`Stop` 为 `moved`（行号相对基线漂移），均决议到节点，无坏锚。

## 判断

- 不升格新顶层域；契约包归既有 `d_workspace`。
- Stop 今日删 managed 树是现状，不是契约。冻结为取消留存，生产接线留给 implement。
- 契约包不 import proto/ledger（预算已满 / 禁止写卡）。
- 可执行冻结走编码金样本与 `MayRecycle` 表，不把 Stop/Dispatch 拆线。
- 三重闸门三条均写入契约 §7。
- 欠账见契约 §8：无「已实现但零测试」。

## 提交

```
git commit -m "contract(B233.4): 冻结工作区能力契约"
[cards/B233.4-charter ddfe3e60] contract(B233.4): 冻结工作区能力契约
 12 files changed, 2347 insertions(+), 2 deletions(-)
 create mode 100644 codegraph/diffs/cards-B233.4-charter.json
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.4-contract-ledger.md
 create mode 100644 docs/superpowers/specs/b233.4-contract.md
 create mode 100644 internal/agentd/workspace_identity_contract_test.go
 create mode 100644 internal/workspace/capability.go
 create mode 100644 internal/workspace/capability_test.go
 create mode 100644 internal/workspace/identity.go
 create mode 100644 internal/workspace/identity_test.go
 create mode 100644 internal/workspace/retain.go
 create mode 100644 internal/workspace/retain_test.go
```

随后 amend 一次把本段收进同批提交。不回填 amend 后 HEAD。
