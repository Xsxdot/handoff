# B233.13 contract 台账

- 2026-09-11 起点：功能线 `cards/B233.1-charter-7` @ `760a5be9`；spec 已批准 @ `1f848057`。分支 `cards/B233.13-charter`。
- 读现状：
  - `Server.mgr *Manager` `internal/agentd/server.go:128`；`SetManager(m *Manager)` `:369`。
  - handler 直调 `s.mgr.Dispatch/Continue/Done/Stop/Status/FootprintAll/Reclaim/ReclaimList/GC/RegisterProject/ListProjects/UnregisterProject/RelayAnswer/NoteDeliveryFailed/RecoverStuck` 与 `s.mgr.Workspace()/requireWorkspace()/assembleResultRef()/inspectRepoDir()`；出处见 contract §1。
  - `Manager.Done(ctx,taskID,note) error` `internal/agentd/manager.go:1696`；`Manager.Stop(ctx,taskID) (bool,error)` `:1825`。
  - `transit` 已在目标态幂等 `return nil` `internal/agentd/manager.go:3731`。
  - `store.UpdateTaskState` CAS（WHERE state=旧值）`internal/store/store.go:537-550`。
  - `handleDone` 成功后 `releaseTaskCarrierOccupancy` `internal/agentd/server.go:1772`；`handleStop` `:1815`；释放实现 `:1822` 调 `scheduling.Release` `internal/scheduling/scheduling.go:747`。
- 目标图 `d_gateway → d_orchestration` budget 321，entries=`agentd.Manager`；`assembly`=`main.go`/`internal/agentd/server.go`/`internal/agentd/codegraph.go`。
- 决定：本节点（L3 轻档 contract）冻结接口与终态 CAS 语义，Ticket 0 只落新导出 `OrchestrationClient` + `TerminalOutcome` 空壳，编译通过；Manager 迁包与 Server 字段切换归 breakdown→implement。
- 落码：`internal/agentd/orchestration_client.go`（接口 + TerminalOutcome）。
- 编译证据：`go build ./...` 退出码 0；`go test -run '^$' ./internal/agentd/` 退出码 0（仅编译，不跑用例）；`go vet ./internal/agentd/` 退出码 0（2026-09-11 本轮）。
- 图冻结：`best.json` 新增 `k_agentd_orchestration_client: d_gateway`；`target.json` 给 `d_gateway→d_orchestration` 加 `interfaces:["OrchestrationClient"]`、预算 321 不动、注记 B233.13；分支视图 `codegraph/diffs/cards-B233.13-charter.json` 落两个新符号。
- 查证：spec 字面「entries 改成 client 接口名」会 `dead-entry`（本轮实测 check fails=1）；改为接口进 `interfaces`、entries 留 `agentd.Manager` 后 `codegraph check --view cards-B233.13-charter` fails=0。已在 contract §6 记。
- `codegraph validate` 退出码 0（含本视图）；`codegraph resolve --doc docs/superpowers/specs/b233.13-contract.md` 退出码 0（三个 workspace_capability 方法图未收录，改源码锚）。
- 待拍板：DTO（DispatchReq/RegisterProjectReq/RecoverReport）随 Manager 迁入 `internal/orchestration`（唯一不成环解）；`SetManager` 是否改名。见 contract §10。
- 提交：见下条收尾（本台账与本节点产出物同批）。
