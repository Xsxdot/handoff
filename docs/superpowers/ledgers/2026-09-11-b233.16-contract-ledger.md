# B233.16 contract 台账（2026-09-11）

节点：`charter:contract`（B233.16 跨机执行消费面：生产持有 ExecutionClient）
基线：功能线 `cards/B233.1-charter-7` @ `c567c3d2`；本分支 `cards/B233.16-charter`
产出物：`docs/superpowers/specs/b233.16-contract.md`
本台账只记过程与原始读数；结论与裁决回写 contract 正文。

## 1. 入口读数

- 上游 spec 头部状态行「已批准（2026-09-11，用户原话「批准，从 contract 起」）」已核实，直接引用。
- 本卡冻结物：消费方组合接口 8 个（cmd 3 / agentd 3 / ledgerstep 2）、具名消费入口 8 个、`StepRunner.Clients` 字段收窄、`target.json` 三条入传输边 entries/注记、本分支视图 diff。

## 2. 现状查证（命令与原始读数）

| # | 命令 / 检查 | 结果 |
|---|---|---|
| 1 | `grep -rn '\*client\.Client\|client\.ExecutionClient' --include=*.go`（生产） | 生产零持有 `client.ExecutionClient`；`*client.Client` 消费面按包：cmd 若干、internal/agentd、internal/ledgerstep |
| 2 | `codegraph sym ExecutionClient` | **未命中**（B233.3 Ticket 0 新增、基线未吸收）→ 图覆盖债 |
| 3 | `codegraph sym runStop` | **未命中**（cmd/stop.go 无图节点） |
| 4 | `codegraph sym StepRunner` / `clientFinalMessage` | 命中（d_ledger，`k_ledgerstep_model` / `k_ledgerstep_fn`）；`StepRunner.Clients` 字段未覆盖 |
| 5 | `git show e43991da --stat` | B233.10 Ticket 0 先例：contract 提交含 `codegraph/diffs/cards-B233.10-charter.json`（nodesAdded 2 空壳方法 + nodesModified 4 结构体），`target.json` 只在 `legacyBudgetNote` 追加注记 |
| 6 | `target.json` 三条入传输边 | `d_cli→d_transport` 预算 10 entries `["client 实体","client.Client"]`；`d_gateway→d_transport` 预算 9 entries `["targetclient.Pool","client.Client"]`；`d_orchestration→d_transport` 预算 5 无 entries。**`d_ledger→d_transport` 不存在**，`d_ledger→d_transport` 直调计数 0 |
| 7 | 消费点 grep：`runUntilDone`/`runFollow`/`warnIfTimeoutBelowStall`/`autoSyncAfterWait`/`resolveBareDiscipline`/`resolveCardDispatchDiscipline` | 各仅有生产调用方（RunE 或装配点），无「为走满分支」的假缝 |

## 3. Ticket 0 落码（编译期透传）

把 8 个消费方组合接口与 8 个具名入口折入既有文件（避免新增生产源文件触发 `oversized-package` warn 计数）：

- `cmd/dispatch.go`：`dispatchClient` 接口、`dispatchTask`、`resolveBareDiscipline` 收窄。
- `cmd/wait.go`：`waitClient` 接口、`runUntilDone`/`runFollow`/`warnIfTimeoutBelowStall`/`autoSyncAfterWait` 收窄。
- `cmd/card_dispatch.go`：`compensateClient` 接口、`resolveCardDispatchDiscipline` 收窄、`compensateCardDispatch` 具名入口、两处 `cl.Dispatch` → `dispatchTask`。
- `cmd/stop.go` / `cmd/reply.go` / `cmd/continue.go`：`runStop` 收窄；新增 `runReply` / `runContinue` 具名入口并替换 RunE 内联。
- `internal/agentd/cardstep.go`：`runClient`/`statusClient`/`stopReclaimClient` 接口、`dispatchStep`/`probeStatus`/`compensateStep`/`pushWorkBranchVia` 具名入口；`stepTransport`/补偿/推分支/探活改走具名入口；`Clients` 装配改签名透传闭包。
- `internal/ledgerstep/runner.go`：`StepClient` 接口、`Clients` 字段收窄。
- `internal/ledgerstep/wire.go`：`finalMessageClient` 接口、`clientFinalMessage` 收窄。

编译证据（本轮）：
```
$ go build ./...
(无输出)
$ echo $?
0
```

## 4. 目标图 entries 棘轮（先变异验证，再落改）

变异测试 1（把 `d_gateway→d_transport` entries 加 `client 实体`、`d_orchestration→d_transport` entries 加 `client 实体`）：

```
$ cp codegraph/target.json /root/.handoff/tmp/98094c30/target.json.bak
$ python3 <mutation>  # entries 增 client 实体
$ codegraph check --view cards-B233.16-charter
new fails: set()   removed fails: set()   warn delta: +0 -0
$ cp /root/.handoff/tmp/98094c30/target.json.bak codegraph/target.json
$ grep -c "B233.16 TEST" codegraph/target.json   → 0
$ git diff --quiet codegraph/target.json && echo git-diff-clean  → git-diff-clean=YES
```

变异测试 2（加 `client 实体` 到 `d_gateway`，不改 `d_orchestration`）：同为零 delta。结论：加 `client 实体` 相对 check 惰性，不引 fail/warn。

反证（为何不删 `client.Client`）：图内 `d_cli→d_transport` 调用边落点是 `k_client_Client`（label `client.Client`）；删该 entry 会让 71 条边从豁免变 legacy，预算 10 直接 over-budget。故「聚合符号退场」只落在源码签名（视图 `nodesModified`），entries 保留聚合容器。

落改：`d_gateway→d_transport` entries 增 `client 实体`；`d_cli` / `d_gateway` / `d_orchestration` 三条边追加 B233.16 注记。落改后：

```
$ codegraph check --view cards-B233.16-charter
new fails: set()   removed fails: set()   warn delta: +0 -0
$ codegraph validate --view cards-B233.16-charter
B233.16 issues: []   （26 个 issue 全部来自 cards-B233.4-charter 存量）
```

## 5. 可执行冻结与变异证据

T1 签名锁测试三文件：`cmd/execution_signature_lock_test.go`、`internal/agentd/execution_signature_lock_test.go`、`internal/ledgerstep/execution_signature_lock_test.go`。

变异 1（`runStop` 第二参数 → `*client.Client`）：
```
$ go vet ./cmd/
vet: cmd/execution_signature_lock_test.go:20:94: cannot use runStop (value of type func(cmd *cobra.Command, c *client.Client, taskID string) error) as func(*cobra.Command, client.ExecutionClient, string) error value in variable declaration
（已还原，grep 'func runStop' → client.ExecutionClient）
```
变异 2（`probeStatus` 第二参数 → `*client.Client`）：`go vet ./internal/agentd/` 报 `cannot use probeStatus ...`（1 条），已还原。
变异 3（`StepRunner.Clients` 字段 → `func(target string) (*client.Client, error)`）：`go vet ./internal/ledgerstep/` 报 `cannot use (&StepRunner{}).Clients ...`（1 条），已还原。

三次还原后 `go build ./...` 退出码 0。

## 6. 测试读数

```
$ go test ./cmd/ ./internal/agentd/ ./internal/ledgerstep/ ./internal/client/ ./internal/targetclient/
--- FAIL: TestRepoContractGate (0.03s)      ← 存量：5 条 d_*→d_workspace over-budget（基线即红）
--- FAIL: TestLegacyNodeEventSequenceUnchanged ← 存量：与不带本卡改动时逐字同因
ok   internal/ledgerstep / internal/client / internal/targetclient
```

两条失败均在 stash 掉本卡改动后原样复现，非本卡引入。`TestRepoContractGate` 仅有基线 5 条 workspace over-budget，无 B233.16 相关契约违规。

## 7. 拍板记录（三重闸门）

命中 1 项（写进 contract §6）：组合接口只嵌入 `client.ExecutionClient`，不把辅助方法并入冻结接口。其余实现级决定（接口命名、`finalMessageClient` 方法集、具名入口落点、entries 逐字值）未命中三闸门，不写拍板记录。

## 8. 图覆盖债

- `codegraph sym ExecutionClient` / `runStop` 未命中；`cmd/stop.go`、`cmd/reply.go`、`cmd/continue.go` 无图节点。
- `StepRunner.Clients` 字段与经字段的调用边未入图 → `d_ledger→d_transport` 直调计数 0。
- spec §要变的事实 #4 的 `d_ledger→d_transport` 与 §现状事实 #4 的 `d_orchestration→d_transport` 不一致；真实图中 `d_ledger→d_transport` 不存在（覆盖债），`d_orchestration→d_transport` 非本卡消费点。已在 contract §3 记录，不退回 spec、不新建方向。

## 9. 提交记录

```
$ git add -A && git commit -m "contract(B233.16): freeze execution consumer surfaces on capability interfaces"
[cards/B233.16-charter 9a5ff72c] contract(B233.16): freeze execution consumer surfaces on capability interfaces
```

提交含本节点全部产出物：6 个改动源文件 + 3 个签名锁测试 + `codegraph/target.json` + 本分支视图 diff + contract 文档 + 本台账。本记录随同批产出物 amend 收进；记录里的 hash 是 amend 前读数（git 事实，不 chase 回填）。

## 10. run 2（P1=A 加冻 wait 一次性消费点）——过程与原始读数

来源：breakdown 稿 §0 拍板归档（提交 `9e03cb35`）P1=A，退回 contract 加冻。

| # | 命令 / 检查 | 结果 |
|---|---|---|
| 1 | 读 `cmd/wait.go` RunE | 确认 `:139` 内联 `cli.WaitEvent(ctx, taskID, false)`、`:159` `autoSyncAfterWait`，无具名入口；`cli` 来自 `newTargetClient()` 聚合。 |
| 2 | 加冻落码 | 新增具名 `runWaitOnce(cmd *cobra.Command, cli waitClient, taskID, addr string) error`（`cmd/wait.go:139`），含一次性 `WaitEvent` + `--notify` + `writeEventLine` + `autoSyncAfterWait`；RunE `:129` 只剩 `return runWaitOnce(cmd, cli, taskID, addr)`，取 addr/构造 client/三形态分派保留在 RunE。 |
| 3 | T1 增断言 | `cmd/execution_signature_lock_test.go:29` 增 `_ func(*cobra.Command, waitClient, string, string) error = runWaitOnce`。 |
| 4 | 编译 | `go build ./...` 无输出、`echo $?`=0；`go vet ./cmd/` 无输出、退出码 0。 |
| 5 | T1 测试 | `go test ./cmd -run TestExecutionSignatureLocks -count=1` → `ok github.com/Xsxdot/handoff/cmd 0.006s`。 |
| 6 | 变异验证（配对还原） | 把 `runWaitOnce` 客户端参数改回 `*client.Client` → `go vet ./cmd/` 退出码 1，原文：`vet: cmd/execution_signature_lock_test.go:29:61: cannot use runWaitOnce (value of type func(cmd *cobra.Command, cli *"github.com/Xsxdot/handoff/internal/client".Client, taskID string, addr string) error) as func(*cobra.Command, waitClient, string, string) error value in variable declaration`；`cp` 还原后 `go vet ./cmd/` 退出码 0。 |
| 7 | 图 | 视图 diff 增 `n_cmd_runWaitOnce`（func，container `k_cmd_fn`，order 744，file `cmd/wait.go`, line 139, signature `func runWaitOnce(cmd *cobra.Command, cli waitClient, taskID, addr string) error`），新增节点 16→17。 |
| 8 | 图对账 | `codegraph check --view cards-B233.16-charter` 与不带视图输出逐字节一致（`diff` IDENTICAL，各 801 行）→ 不新增 fail/warn；`codegraph validate --view` 26 个 issue 全来自 `cards-B233.4-charter` 存量，无 B233.16 issue；`codegraph resolve --doc docs/superpowers/specs/b233.16-contract.md` exit=0。 |
| 9 | C-0 回写 | contract 头部冻结状态改为「已冻结（2026-09-11；冻结提交 03b5f089，spec 勘误 e4ede597，本轮加冻见本提交）」。 |
| 10 | C-2 / C-3 / P2 / P4 | C-2：T3 范围仅 `cmd`、agentd/ledgerstep 由 T1 承载、三包扩展落 roadmap；C-3：`runFollow` 有效依赖面为 `waitClient`（`warnIfTimeoutBelowStall` 的 Status 与 `autoSyncAfterWait` 的 Attach 并集）；P2：替身直接注入已冻具名入口、生产零改动；P4：白名单符号级 + 剔除注释 + 可变红守卫。四条写入 contract §1 边界澄清与 §7。 |

**注**：breakdown 稿 §2.2 #56 与 §5 记「16 个新增节点（8 接口模型 + 8 具名函数/方法）」，本轮加冻后为 17（8 模型 + 9 函数/方法）。breakdown 由后续复核轮重新核对，本节点只产出 contract，未越界改 breakdown 正文。

### 提交记录（run 2）

```
$ git add -A && git commit -m "contract(B233.16): freeze wait-once named entry (P1=A) + boundary clarifications"
[cards/B233.16-charter-3 7e9686b5] contract(B233.16): freeze wait-once named entry (P1=A) + boundary clarifications
```

本记录随同批产出物 amend 收进；记录里的 hash 是 amend 前读数（git 事实，不 chase 回填）。
