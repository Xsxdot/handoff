# B233.7 breakdown 台账

本台账按拆解回合追加；命令输出只记录本回合实际运行结果，不把上游台账读数冒充当前验证。

| 时间 | 事实 / 判断 | 命令与原始输出 |
| --- | --- | --- |
| 2026-09-08 | 当前工作树初始干净；当前分支为 `cards/B233.7-charter-2`，HEAD 为 `3f069037`。 | `pwd && git status --short --branch && git log -1 --oneline` → `/root/.handoff/worktrees/e99ece7d`；`## cards/B233.7-charter-2`；`3f069037 contract(B233.7): 冻结质量债核销契约` |
| 2026-09-08 | 上游 spec 已批准，contract 头部写的是“随本提交冻结”，尚未写成带日期/提交的“已冻结”状态行；本拆解不引用失真的会话记忆，后续回写 contract 状态。 | `sed -n '1,18p' docs/superpowers/specs/b233.7.md` → `状态：**已批准**（2026-09-08，用户原话「同意。开始吧。」）`；`sed -n '1,12p' docs/superpowers/specs/b233.7-contract.md` → `**冻结状态：随本提交冻结（本契约、codegraph/target.json 与台账同批提交）**` |
| 2026-09-08 | 本卡法定产物为 `docs/superpowers/specs/b233.7-breakdown.md`；按仓内约定新建同目录 breakdown 台账。 | `ls -l docs/superpowers/specs/b233.7*` → 已有 spec/contract，无 breakdown；`rg --files docs/superpowers/ledgers | rg 'b233\.6-breakdown|b233\.7'` → 有 `2026-09-07-b233.6-breakdown-ledger.md` 与 `2026-09-08-b233.7-spec-ledger.md`，无本卡 breakdown 台账。 |
| 2026-09-08 | `codegraph/best.json` 顶层领域共 15 个；本卡候选触及 d_orchestration、d_gateway、d_workspace、d_execution、d_transport、d_execution_host、d_scheduling、d_keystone；执行子域 d_execution_contract/d_execution_adapters 仍归顶层 d_execution。 | `jq -r '.domains | to_entries[] | select((.value.parent // null) == null) | [.key,.value.type,.value.label] | @tsv' codegraph/best.json` → `d_orchestration logic 任务编排`；`d_gateway boundary 控制门面`；`d_workspace boundary 项目与工作区`；`d_execution boundary 任务执行`；`d_sessions boundary 终端会话`；`d_transport boundary 跨机连接`；`d_protocol logic 协议契约`；`d_ledger logic 卡片账本`；`d_collab logic 协作房间`；`d_cli logic 协调者命令面`；`d_web logic Web 控制台`；`d_policy logic 运行策略与配置`；`d_maintenance boundary 安装与换版`；`d_scheduling logic 编制调度`；`d_keystone logic 协调者 Keystone` |
| 2026-09-08 | best.json 容器归属显示 Manager/approval 在 d_orchestration，Server 在 d_gateway，workspace 能力在 d_workspace，executor contract/adapters/hostapi 在 d_execution 顶层，scheduling 与 keystone 各自独立；本拆解按此分域，不把“测试命令经过的包”自动算成改动子系统。 | `jq -r '.containers | to_entries[] | select((.key|test("agentd|executor|workspace|scheduling|keystone|opencode|claudecode|grok|codex|agy|hostapi|client";"i"))) | [.key,.value] | @tsv' codegraph/best.json` → 相关映射原样输出：`k_agentd_Manager d_orchestration`、`k_agentd_Server d_gateway`、`k_workspace_fn d_workspace`、`k_executor_fn d_execution_contract`、`k_opencode_Adapter d_execution_adapters`、`k_hostapi_Host d_execution_host`、`k_scheduling_Service d_scheduling`、`k_keystone_Service d_keystone` 等。 |
| 2026-09-08 | 已答再问当前生成新 uuid 工单且不保存原生 QuestionID；waitQuestion/RelayAnswer 只从命名工单主键推导，因而新 uuid 回程 QuestionID 为空。 | `nl -ba internal/agentd/manager.go | sed -n '2915,2967p;2983,3027p'` → `ticketID = uuid.NewString()`；`ticketRequest{Kind: "ask", Question: ev.Text}`；OpenCode 回程仅以 `NativeIDFromTicket(taskID, ticketID)` 设置 `QuestionID`。 |
| 2026-09-08 | OpenCode 原生权限由既有 ApprovalClient 形成/等待，adapter 负责 RespondPermission；当前 authorizeNativePermission 失败只日志并返回，成功才 AckDelivered/AckExecuted。Manager 已有统一 NoteDeliveryFailed。 | `nl -ba internal/executor/opencode/adapter.go | sed -n '788,831p'` → 失败分支输出 `RespondPermission 失败` 日志后 `return`；成功路径调用两次 Acknowledge；`nl -ba internal/agentd/manager.go | sed -n '3269,3291p'` → `NoteDeliveryFailed` 追加并发布 `delivery_failed`。 |
| 2026-09-08 | R4 的失败事实当前缺少从 opencode adapter 回到 manager 的已冻结承载点；adapter 契约说明 adapter 不直接碰 store，只能经事件或返回值交给 manager。该接缝的承载方案列为待拍板/必要时退回 contract，不在拆解稿内自造新公共接口或事件。 | `nl -ba internal/executor/executor.go | sed -n '1,20p;161,183p'` → adapter 只产 `AdapterEvent`/返回值，状态与工单由 manager 落库；`rg -n 'NoteDeliveryFailed|DeliveryFailure' internal/executor internal/agentd` → `NoteDeliveryFailed` 仅在 agentd manager 内现有调用点出现，executor 无现成失败回调。 |
| 2026-09-08 | 普通 Dispatch 已把纪律正文落任务目录并填 StartReq.Discipline，但当前唯一生产 ProfileReq 调用是协调者专用 coordinator_home.go，未填 TaskOverlay；五家 Profile 已写 overlay 到 `.handoff/task-overlay/`，Verify 只检查全局 RulesRelFile。 | `rg -n 'ProfileReq\\{|DisciplineText|StartReq\\{' internal/agentd internal/executor --glob '*.go'` → 生产 `ProfileReq` 调用见 `internal/agentd/coordinator_home.go:109`；`nl -ba internal/agentd/manager.go | sed -n '1020,1116p'` → 纪律落盘并填 `StartReq{..., Discipline: discText, ...}`；各家 profile `Prepare` 有 `TaskOverlay` 写入、`Verify` 仅 Stat 全局规则。 |
| 2026-09-08 | 任务 diff 当前组装 ResultRef 后丢弃，DiffRange 仍用原 headRev；assembleResultRef 能以 rev-parse 取得准确 commit，commit 为空时允许降级。 | `nl -ba internal/agentd/server.go | sed -n '1862,1924p'` → `if _, err := s.mgr.assembleResultRef(...); err != nil` 后 `DiffRange(..., headRev)`；`nl -ba internal/agentd/workspace_capability.go | sed -n '102,128p'` → commit 成功才保存，失败留空。 |
| 2026-09-08 | B350 竖切测试仍断言请求 Executor=claude 覆盖载体 CLI；bindingFor 生产函数取载体 IdentityOf(c) 的 Executor，物理身份规则本身不应改。 | `nl -ba internal/keystone/slice_test.go | sed -n '149,158p'` → 请求 `Executor: "claude"` 且期望绑定 executor `claude`；`nl -ba internal/scheduling/scheduling.go | sed -n '866,878p'` → `Target`/`Executor`/`HomeDir` 来自 `IdentityOf(c)`，仅 `Model` 使用请求覆盖。 |
| 2026-09-08 | WakeHome 生产路径对已支持 CLI 使用 RunTurn + DetectPrompt，并以实际非空输出判 ready；已有测试反面覆盖凭据文件存在但检测回合失败。R1 的本卡范围应锁真实输出/同名规则测试，不改 hostapi 生产语义。 | `nl -ba internal/hostapi/probe.go | sed -n '329,371p'` → 支持 CLI 调 `detectTurn`，错误/空输出不 ready；`rg -n 'TestWakeHomeReadyRequiresTurnOutputNotCredFile|reloadProbe' internal/hostapi internal/executor --glob '*.go'` → 找到真实输出反面测试，未找到 `reloadProbe` 恒真测试。 |
| 2026-09-08 | R8 的旧说明仍在 B233.3 spec §方案/测试决定承诺“写请求重试必须幂等（重复派发仍是一次任务）”；repo skill 的排障表也把 reply delivery_failed 视为 resume，不应把网络失败后的重新 dispatch 写成同一任务保证。 | `rg -n -C 3 '写请求重试|重试仍|重派|单任务|失败先查' docs/superpowers/specs/b233.3.md docs/roadmap.md skills/handoff/SKILL.md` → B233.3.md:52 为 `写请求重试必须幂等（重复派发仍是一次任务）`，B233.3.md:67 为 `模拟超时重试仍单任务`；roadmap 已记录 `未用真重试器验单任务`；skill 排障表已有 resume 处置。 |
| 2026-09-08 | Ask/permission/diff/overlay/physical identity 的现有测试入口均可列入有界文件集；没有因此把整个 internal/agentd 或 internal/executor 目录塞入一张功能卡。 | `rg --files internal/agentd internal/executor internal/hostapi internal/workspace internal/scheduling internal/keystone | rg '(manager|approver|integration|diffbase|resultref|profile|adapter|question|probe|slice|admit).*_test\\.go$'` → 输出包含 `internal/agentd/manager_test.go`、`approver_test.go`、`integration_test.go`、`resultref_test.go`、五家 profile/adapter 测试、`internal/hostapi/probe_test.go`、`internal/keystone/slice_test.go`。 |
| 2026-09-08 | 代码图命令可用；法定顶层领域以 `best.json` 的 `parent` 为空为准。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --help` 退出码 0；命令列表含 `absorb chain check completion context contract domains entity flow migrate resolve summary sym tree validate version views who-calls`。`jq -r '.domains | to_entries[] | select((.value.parent // "") == "") | [.key,.value.kind,.value.label] | @tsv' codegraph/best.json` 退出码 0；原始行含 `d_orchestration`、`d_gateway`、`d_workspace`、`d_execution`、`d_sessions`、`d_transport`、`d_protocol`、`d_ledger`、`d_collab`、`d_cli`、`d_web`、`d_policy`、`d_maintenance`、`d_scheduling`、`d_keystone`。 |
| 2026-09-08 | B350 入口定位完成：生产物理绑定与竖切测试是两处有界入口；契约明确只改测试/过时注释，不改生产绑定。 | `rg -n "B350\\|bindingFor\\|TestIgnitionVerticalSlice" internal/keystone/slice_test.go docs/superpowers/specs/b233.7.md docs/superpowers/specs/b233.7-contract.md` 退出码 0；命中 `internal/scheduling/scheduling.go#bindingFor`、`internal/keystone/slice_test.go#TestIgnitionVerticalSlice` 及契约“不改生产函数”条目。 |
| 2026-09-08 | 按拆解纪律回写上游契约冻结状态与拆解期边界澄清；未新增契约面。 | `apply_patch` 成功；`docs/superpowers/specs/b233.7-contract.md` 头部现为 `已冻结（2026-09-08；冻结提交 3f069037）`，并新增 §9：协调者专用 Profile 边界、执行子域归属、R8 不增运行时边，以及 R4 失败承载若扩展公共接缝必须退回 contract。 |
| 2026-09-08 | 法定 breakdown 已写入，包含待拍板清单、8 个顶层/子域边界说明、S0–S6 四段式子卡、DAG、七条行为闭环、真机清单和逐族缺陷审查。 | `git diff --check` 退出码 0；`wc -l` → breakdown 359 行、contract 232 行、breakdown ledger 23 行。 |
| 2026-09-08 | 首次符号锚解析发现图未覆盖/路径不完整锚；已移除未解析锚并保留 plain path + 现状符号说明。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b233.7-breakdown.md` 首次原始失败含 `internal/executor/opencode/adapter.go#Adapter.authorizeNativePermission (vanished)`、`profile.go#Profile (file_missing)`、`internal/agentd/workspace_capability.go#Manager.assembleResultRef (vanished)`；修订后同命令退出码 0，输出 11 个 anchors，均为 `ok` 或 `moved`。 |
| 2026-09-08 | 当前代码图结构校验通过；输出仍报告既有未扫描/生命周期统计，不是本卡新增错误。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 退出码 0；原始输出含 `containers: 300`、`domains: 23`、`edgeIssues: null`、`issues: null`、`nodes: 4499`、`unscannedEntries: 5`。 |
| 2026-09-08 | 代码图契约对照未通过；本回合只记录原始失败，不把它归因为本卡实现。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check` 退出码 1；原始失败：`{"kind":"dead-contract","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）"}`；`{"kind":"dead-interface","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的接口 \"ApprovalClient\" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）"}`；末尾：`Error: 契约对照发现 2 处违规`。 |
| 2026-09-08 | 追加图覆盖债说明后再次解析法定稿，符号锚仍全部可解析。 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b233.7-breakdown.md` 退出码 0；输出 11 个 anchors，`anchor` 值均为 `ok` 或 `moved`。 |
| 2026-09-08 | 初次提交法定稿、contract 状态/边界回写与本节点台账；随后按纪律只 amend 一次收录本行。 | `git add docs/superpowers/specs/b233.7-breakdown.md docs/superpowers/specs/b233.7-contract.md docs/superpowers/ledgers/2026-09-08-b233.7-breakdown-ledger.md` 退出码 0；`git commit -m "docs(B233.7): 出稿质量债核销拆解提案"` 原始输出：`[cards/B233.7-charter-2 09c0b773] docs(B233.7): 出稿质量债核销拆解提案`、`3 files changed, 399 insertions(+), 1 deletion(-)`、新增 breakdown 与 breakdown ledger。 |
| 2026-09-08 | 协调者拍板 P1-B / P2-A / P3-A / P4-A，回写 breakdown 头部与待拍板清单。P4 不把冻结哈希改成执行机过期的 `94246fc8`。 | 见 `docs/superpowers/specs/b233.7-breakdown.md` 待拍板清单；本行之后同批提交。 |
