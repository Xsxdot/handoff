# 台账：B156.2.1（C1）implement 节点

分支 cards/B156.2.1-charter-2，起点 HEAD 2be32e52（charter 节点产出）。法定产出物：绑定席位与活性租约实现（plan §5–§9）。本台账边干边追加，随实现同批提交。跨卡审计六条裁决已读；对本卡的三处影响（裁决一保留 api_test.go、裁决四图闸只认 fails=0、三处行号漂移顺手改）按裁决执行。

## 基线复核（Task 1 步骤 1，2026-08-26 本工作树）

```text
$ go test ./internal/ledger -count=1
ok  	github.com/Xsxdot/handoff/internal/ledger	2.250s

$ go run . graph check --repo . --view cards-B156.2-charter-4
JSON 顶层键 fails/warns/legacyHits/bestCoverage；解析：fails=0 warns=97
—— 复现 F2/F3（绿＋fails=0 warns=97）✓。bestCoverage crossDomainEdges=1003。
```

## Task 1（schema 迁移）

- 【红】新建 internal/ledger/binding_test.go（迁移测试一支），首跑原始输出：
  `binding_test.go:50: 升列后应可读 driver_carrier（存量行取零值）: SQL logic error: no such column: driver_carrier (1)`
  —— 与 plan 预期红点逐字一致（Open 本身不失败、红在列缺席），断言红非 typo 红 ✓。
- 【绿】store.go 两处：PG/SQLite ddl 切片各插 driver_leases 建表（card_prefixes 之前）；DDL 循环后内联 ALTER TABLE cards ADD COLUMN driver_carrier TEXT ＋双文案容忍（未抽 helper，零新函数符号）。
  迁移测试绿；`go test ./internal/ledger -count=1` 包回归绿（2.107s）✓。
- 【变异①】容忍条件删掉 `already exists` 分支（保留 duplicate column）：SQLite 下仍绿——预期内（SQLite 文案是 duplicate column；PG 半归 F6 源码核对＋真机清单 R1）。
- 【变异②】整个容忍条件删成裸 return err：迁移测试红，原始输出：
  `binding_test.go:64: 二次 Open 应幂等: 建账本 schema: SQL logic error: duplicate column name: driver_carrier (1)` —— 测试有牙 ✓。变异经 git diff 确认完全还原（store.go 仅余 +23 行正确实现）。

## Task 2（租约四法填肉）

- 【红】binding_test.go 追加 leaseExpires helper＋三支租约测试，首跑三红。原始红点：
  - TestRenewDriverLeaseUpsertsOwnRowOnly：`binding_test.go:100: 首建行 expires_at=0001-01-01 00:00:00 +0000 UTC want 2026-08-26 10:01:00 +0000 UTC`
  - TestDriverLeaseReadsReturnExpiredRowsAndMissing：`binding_test.go:142: 读面不过滤过期行且带 Session: {Session: ExpiresAt:0001-01-01...} ok=false err=<nil>`
  - TestAllDriverLeasesReturnsAllRowsSorted：`binding_test.go:172: 应 3 行含过期行: [] <nil>`
  偏差声明：plan 预言前两支死于 `no such table: driver_leases`——那是按「Task1 未落表」推的；实际执行顺序里 Task 1 已建表，stub 恒 true,nil 且零写入 → 直查 ErrNoRows / 读面 false。三条红因仍全部是「功能缺失」型断言红（F1 核心：空壳不写库），非 typo 红，准进下一步。
- 【绿】binding.go 替换四空壳（RenewDriverLease upsert / DropDriverLease 幂等删 / DriverLeaseOf 不过滤读 / AllDriverLeases 排序全量读）；文件头删「Ticket 0 空壳」句；import 增 database/sql/errors/fmt。定向三支绿＋包回归绿（2.085s）✓。
- 【变异①】upsert 删 ON CONFLICT 子句：`续期: false 续驱动租约: constraint failed: UNIQUE constraint failed: driver_leases.session (1555)` 红 ✓。
- 【变异②】AllDriverLeases 删 ORDER BY session：连跑 5 次全部红（排序断言在本引擎上稳定有牙，无需乱序插桩）✓。
- 【变异③】DropDriverLease 改假删（UPDATE expires_at）：`删除后行应消失` 红 ✓。
- 三次变异均 git diff 确认还原，工作树仅余正确实现。

## Task 3（绑定二法填肉）

- 【红】binding_test.go 追加 carrierOf/countTakeoverEvents helper＋六支测试（plan §7 步骤 2 全部代码块），首跑六红。原始红点：
  - TestClaimCardAsWritesCarrierColumns：`不存在卡应 ErrNotFound: <nil>`（stub 对不存在卡也返回 nil——比 plan 预言的「carrier 应落列」更早红，同为功能缺失型）
  - TestLegacyClaimCardDelegatesWithEmptyCarrier：`前置载体应已落列`（与预言一致）
  - TestRebindDriverConflictKeepsEverything：`expect 不符应 CAS 冲突: <nil>`（与预言一致）
  - TestRebindDriverSuccessGoldPayload：`新绑定未覆写`（与预言一致）
  - TestRebindDriverEmptyExpectRequiresUnbound：`有绑定时空 expect 应 CAS 冲突: <nil>`（与预言一致）
  - TestCarrierOpaqueRoundTrip：`换绑 carrier 不改写: got "" want "|rebind"`（首例 carrier="" 使存取断言空过、死在换绑断言——功能缺失型，非 plan 预言的首例断言；偏差如实记）
- 【绿】binding.go 替换 ClaimCardAs/RebindDriver 空壳（闭包签名保留 sink *eventSink 与 move.go 原形同形，vet 无意见）；move.go ClaimCard 函数体替换为转调 ClaimCardAs(id,owner,"")；binding.go 文件头「Ticket 0 空壳」句已删。
  `go test ./internal/ledger -count=1` 全绿（2.561s）＝既有 move_test/tasks_test 归属语义组零改动全绿；`go test ./internal/ledgerstep/... -count=1` 绿（0.961s）✓。
- 【变异①】ClaimCard 恢复旧体内联 UPDATE（不写 carrier）：`ClaimCard 转调应传空载体: "desktop:y"` 红 ✓。
- 【变异②】payload 加第三键 reason:x：`payload 应恰有 from/to 两键: map[from:... reason:x to:...]` 红 ✓（多键即红）。
- 【变异③】payload 去 to 键：`payload 应恰有 from/to 两键: map[from:sess-old]` 红 ✓（缺键即红）。
- 【变异④】CAS 判定降格为 `expect != "" && ...`：`有绑定时空 expect 应 CAS 冲突: <nil>` 红 ✓。
- 四次变异均还原并 git diff 核对，工作树仅余正确实现。

## Task 4（门面缝级断言＋Following 出键＋BindDriver 注销账）

- 红绿定位：按 plan §8 步骤 1 声明执行——缝级两支排在 Task 3 之后直接跑绿；其红灯证据由 F1 探针（plan 台账）与 Task 3 六条断言红背书；TestFollowingProjectionKeyPresence 为序列化边界防回归锁（红绿模板豁免）。
- 新建 internal/ledger/api/api_test.go（入口 newFacadeStore 返回 (*ledger.Store, client.LedgerClient)，接口形态与裁决二一致）；api.go BindDriver 注释替换为欠账 #4 销账文本（函数体一字未动）。C2 的 api_rooms_test.go 未出现（并行卡尚未落，无撞名问题——裁决一我方保留 api_test.go ✓）。
- 【绿】`go test ./internal/ledger/... -count=1` 两包 ok（api 包从 [no test files] 变为有测试，plan 已预告属预期）；`go build ./...` 无输出；图闸 `graph check --view cards-B156.2-charter-4` fails=0 warns=97 ✓（warns 不作判据——裁决四照办）。registry.go/keystone.go 本轮未出现于 internal/ledger/api/（B156.3 尚未落，见到时不动）。
- 【变异①】入口临时改直引具体类型 *Facade：编译过、测试过（自查用，证明备选形态可运行）；git diff 对比后还原——提交版入口确为 client.LedgerClient 接口变量 ✓（接缝降格形态未入库）。
- 【变异②】cardWire 删 `Following: v.Following` 行：`列表投影应带 Following="P1": ...` 红 ✓（在席不出键即红）。还原后 api.go 仅余注释改动。

## 收口（§9 全量回归，2026-08-26）

```text
$ go test ./internal/ledger/... -count=1
ok  	github.com/Xsxdot/handoff/internal/ledger	2.500s
ok  	github.com/Xsxdot/handoff/internal/ledger/api	0.877s

$ go test ./internal/ledgerstep/... -count=1
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	1.585s

$ go build ./...
（退出码 0，无输出）

$ gofmt -l internal/ledger internal/ledger/api internal/collab
（空输出）

$ go run . graph check --repo . --view cards-B156.2-charter-4
fails=0 warns=97

$ git diff --check
（空输出）
```

真机清单（未验证，维持 plan §9 原状）：R1＝PG 半迁移（driver_carrier ALTER 双文案容忍的 PG 侧、driver_leases TIMESTAMPTZ 建表）；R2＝墙钟下 TTL 翻转与续租节拍。本轮未做任何让 PG 判据「看起来完整」的补猜（按本轮补充末条要求，机内只验 SQLite 侧实测原文）。

## 跨卡裁决落地与偏差记录（本轮补充要求的收尾段）

- 裁决一：C1 保留 internal/ledger/api/api_test.go ✓（C2 的 api_rooms_test.go 本轮未出现于工作树，无撞名事实）。
- 裁决二：C1 入口本就是接口形态（newFacadeStore 返回 client.LedgerClient），零改动即合规；变异①自检确认提交版非具体类型直引 ✓。
- 裁决四：图闸判据只认 fails=0（两次复核均 fails=0 warns=97，warns 只记不判）；registry.go/keystone.go 本轮未出现，见到不动；--view 视图存在且引用完整，未触发「停下报告」分支 ✓。
- 行号漂移三处顺手改（plan F11 与 §3 Consumes）：cards.go:19-21→244、workflows.go:154-155→155 已照改。derived.go 一处如实声明：协调者判「96 实为 95」，本人 grep -n 实测 `view.Following = relation.To` 赋值在 **96**、其 case 分派守卫在 **95**——plan 引用改写为「derived.go:95-96（merged_into 分派给 Following 赋值）」区间式锚，避免写入与实测相悖的单值。
- 红点偏差（功能缺失型不变）：Task 2 前两支死于「无行」而非 plan 预言的「无表」（执行顺序上 Task 1 已建表）；TestClaimCardAsWritesCarrierColumns 死在更早的 ErrNotFound 断言；TestCarrierOpaqueRoundTrip 首例 carrier="" 使存取断言空过、死在换绑断言。均已逐条记录原始输出。
- 提交文件集偏差：plan §9 预列八文件含 plan-ledger.md——该文件已随 charter 提交（2be32e52）入库；本节点台账按仓内惯例另落 execution-ledger.md，且 plan 文档因行号修正带 M。最终 name-status 仍为八文件，组成与 plan 预列不同（见 git show 输出）。
- 收尾自审：错误分支日志带 cause/上下文、成功路径 Info 出口（RenewDriverLease/DropDriverLease/ClaimCardAs/RebindDriver/Open 迁移均有）；binding.go 头注释保留权威位置＋互不代写声明、「Ticket 0 空壳」句删除；导出方法注释齐全；触及包测试绿、全量编译过、图闸 fails 空；Interfaces 与 contract §3.2/§3.4 冻结签名逐字一致。
- 纪律自查：①本台账全部命令输出均为本工作树实跑原文，PG 侧如实标「未验证」归真机清单；②本节点未调用 handoff CLI 写命令、未起任何 executor 进程（仅 graph check 只读子命令）；③产出物与台账随原子提交入库。
