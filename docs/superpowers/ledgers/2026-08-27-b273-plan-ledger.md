# B273 plan 节点台账

- 2026-08-27：B243 新增接缝测试首次运行 `go test ./internal/ledgerstep -run 'TestWaitForTurnEnd|TestFinalMessage' -count=1` 的原始输出为 `FAIL ... missing ',' in argument list`，修正测试夹具中的 Go 反引号语法后，空壳阶段单测 `go test ./internal/ledgerstep -run '^TestWaitForTurnEndWaitsForCompletedFinalText$' -count=1` 实际断言红：`wait calls = 1, want 2`。
- 2026-08-27：B243 最小实现后 `go test ./internal/ledgerstep -run 'TestWaitForTurnEnd|TestFinalMessage' -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/ledgerstep 0.004s`。
- 2026-08-27：B242 新增接缝测试首次运行 `go test ./internal/ledgerstep -run 'TestParseVerdict|TestNodeStep.*Verdict' -count=1` 的编译红为 `got.salvaged undefined`、`got.notesDropped undefined`、`got.findingsDropped undefined`；字段占位后单条 `TestParseVerdictSalvagesFirstVerdictWhenNotesIsBroken` 实际断言红：`裁决 JSON 解析失败: invalid character ':' after object key:value pair`。
- 2026-08-27：B242 最小实现后 `go test ./internal/ledgerstep -run 'TestParseVerdict|TestNodeStep.*Verdict' -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/ledgerstep 0.298s`。
- 2026-08-27：B244 新增接缝测试首次运行 `go test ./internal/executor/turn ./internal/executor/codex -run 'TestRenderPrompt|TestProtocolRules' -count=1` 实际断言红：缺少“是否提交听角色纪律”“角色禁止修改工作树”“commit 填当前 HEAD”“角色要求提交时，必须 git add 并 commit”，且仍保留无条件 commit 铁律。
- 2026-08-27：B244 最小实现后 `go test ./internal/executor/turn ./internal/executor/codex -run 'TestRenderPrompt|TestProtocolRules' -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/executor/turn 0.002s`、`ok  github.com/Xsxdot/handoff/internal/executor/codex 0.003s [no tests to run]`。
- 2026-08-27：B241 新增 HTTP 测试首次使用 `newLedgerEnv` 时 `... -run 'TestFlowNodePurposeSurvivesHTTPGetPutGet|TestLedgerNodeWireOmitsZeroPurpose' -count=1 -v` 实际只得到 PTY 不可用并 `SKIP`；改用现有 `newNoPTYLedgerEnv` 后，同命令实际断言红：`first purpose = ""`，说明测试接缝真实命中。
- 2026-08-27：B241 最小实现后 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test ./internal/agentd -run 'TestFlowNodePurposeSurvivesHTTPGetPutGet|TestLedgerNodeWireOmitsZeroPurpose' -count=1 -v` 实际退出 0，两个测试均 `PASS`，原始包输出 `ok  github.com/Xsxdot/handoff/internal/agentd 0.167s`。
- 2026-08-27：全仓编译 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go build ./...` 实际退出 0；第一次运行下载依赖，原始输出为多行 `go: downloading ...`，未出现 `build failed`、`cannot` 或 `undefined`。
- 2026-08-27：B243 变异前 `rg -F -n 'payload.FinalText != nil && *payload.FinalText != ""' internal/ledgerstep/wire.go | wc -l` 实际输出 `1`；将条件改为 `payload.FinalText == nil || *payload.FinalText != ""` 后 `go build ./...` 实际退出 0，行为断言 `go test ./internal/ledgerstep -run '^TestWaitForTurnEndWaitsForCompletedFinalText$' -count=1` 实际红：`wait calls = 1, want 2`；随后恢复原条件。
- 2026-08-27：B242 变异前 `rg -F -n 'return match[1], true' internal/ledgerstep/verdict.go | wc -l` 实际输出 `1`；将返回值改为固定 `pass` 后 `go build ./...` 实际退出 0，行为断言 `go test ./internal/ledgerstep -run '^TestParseVerdictUsesFirstVerdictNotNotesMention$' -count=1` 实际红：`verdict 被 notes 引用覆盖`；随后恢复首个匹配值。
- 2026-08-27：B244 变异前 `rg -F -n '角色要求提交时，必须 git add 并 commit（不要 push）。' internal/executor/turn/protocol.go | wc -l` 实际输出 `1`；将“必须”改为“不必”后 `go build ./...` 实际退出 0，行为断言 `go test ./internal/executor/turn -run '^TestProtocolRulesMakeCommitConditionalOnRole$' -count=1` 实际红：`prompt 缺少 "角色要求提交时，必须 git add 并 commit"`；随后恢复原文。
- 2026-08-27：B241 变异前 `rg -F -n 'Purpose: node.Override.Purpose,' internal/agentd/ledgerapi.go | wc -l` 实际输出 `1`；将投影改为 `Purpose: node.Override.Model` 后 `go build ./...` 实际退出 0，行为断言 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test ./internal/agentd -run '^TestFlowNodePurposeSurvivesHTTPGetPutGet$' -count=1` 实际红：`first purpose = ""`；随后恢复原投影。
- 2026-08-27：四条实现收尾局部测试实际退出 0：`go test ./internal/ledgerstep -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/ledgerstep 6.400s`；`go test ./internal/executor/turn ./internal/executor/codex -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/executor/turn 0.116s`、`ok  github.com/Xsxdot/handoff/internal/executor/codex 5.994s`；`GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test ./internal/agentd -run 'TestFlow|TestLedgerNodeWire' -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/agentd 0.841s`。
- 2026-08-27：集成 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test . ./internal/... -count=1` 实际退出 1；除 `internal/prochost` 外已输出多个 `ok`，`TestWindowsVets` 原始报错为 `internal/ledgerstep/wire.go:84:5: the cancelGrace function is not used on all paths (possible context leak)` 与 `internal/ledgerstep/wire.go:77:5: this return statement may be reached without using the cancelGrace var defined on line 84`。
- 2026-08-27：将 B243 宽限生命周期收进 `waitForTurnEndGrace` 并直接 `defer cancel()` 后，`go test ./internal/ledgerstep -run 'TestWaitForTurnEnd|TestFinalMessage' -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/ledgerstep 0.005s`；`GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test ./internal/prochost -run '^TestWindowsVets$' -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/prochost 2.459s`。
- 2026-08-27：修复后集成 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test . ./internal/... -count=1` 实际退出 0；原始输出包含根包 `ok  github.com/Xsxdot/handoff 0.597s`、`ok  github.com/Xsxdot/handoff/internal/agentd 122.117s`、`ok  github.com/Xsxdot/handoff/internal/ledgerstep 7.944s`、`ok  github.com/Xsxdot/handoff/internal/prochost 2.976s`，其余列出的 internal 包均为 `ok`。
- 2026-08-27：单独运行 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test ./cmd -run '^TestMaybeInstallServiceLinuxRootInstalls$' -count=1 -v` 实际退出 1，原始错误为 `托管失败：加载配置 /tmp/handoff.yaml: 写默认配置 /tmp/handoff.yaml: open /tmp/handoff.yaml: read-only file system`；未修改 cmd 测试或环境配置。
- 2026-08-27：最终 B243 条件变异前 `rg -F -n 'slog.InfoContext(ctx, "收到带 final_text 的 completed")' internal/ledgerstep/wire.go | wc -l` 实际输出 `1`；将对应条件改为 `payload.FinalText == nil || *payload.FinalText != ""` 后 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go build ./...` 实际退出 0，行为断言实际红：`wait calls = 1, want 2`；随后恢复原条件。
- 2026-08-27：提交前最终审计实际通过：`gofmt -d` 对全部 12 个触及文件无输出；`git diff --check` 无输出；最终 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go build ./...` 退出 0；`go test ./internal/ledgerstep -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/ledgerstep 6.092s`；`go test ./internal/executor/turn ./internal/executor/codex -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/executor/turn 0.075s`、`ok  github.com/Xsxdot/handoff/internal/executor/codex 6.006s`；`GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test ./internal/agentd -run 'TestFlow|TestLedgerNodeWire' -count=1` 输出 `ok  github.com/Xsxdot/handoff/internal/agentd 0.885s`。
- 2026-08-27：本节点 `git status --short --branch` 实际显示当前分支为 `cards/B273-charter-2`，工作树干净；不切换分支，后续只在当前分支工作。
- 2026-08-27：本节点基线 `go test ./internal/ledgerstep -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/ledgerstep 4.830s`。
- 2026-08-27：本节点基线 `go test ./internal/executor/turn ./internal/executor/codex -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/executor/turn 0.085s`、`ok  github.com/Xsxdot/handoff/internal/executor/codex 5.982s`。
- 2026-08-27：本节点基线 `GOMODCACHE=/root/.handoff/tmp/4b7658e0/b273-gomodcache GOSUMDB=off go test ./internal/agentd -run 'TestFlow|TestLedgerNodeWire' -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/agentd 0.707s`；命令实际下载依赖，未修改仓库源码或 git 配置。
- 2026-08-27：源码补充核对确认 `proto.Event` 的事件正文统一字段为 `Payload`；`AttachInfo.RecentEvents` 与 `finalMessageFromEvents` 的当前调用链使用 `[]proto.Event`，本卡将按真实调用面改为计划要求的 `[]*proto.Event` 并同步调用方/夹具。

- 2026-08-27：`git status --short --branch && git log -5 --oneline --decorate` 原始输出确认当前分支为 `cards/B273-charter`，HEAD 为 `ef5ebfbf`，其上游为 `origin/fix/batch2-bugs`；工作树初始无未提交改动。
- 2026-08-27：读取 `docs/superpowers/specs/b273.md`，确认 spec 已批准 r1；本节点法定产出为 `docs/superpowers/plans/b273-plan.md`，四条改法独立落地，禁止抽公共框架；B243 宽限必须注入后续 `WaitEvent` 的 parent child deadline，宽限到期且 parent 存活时成功返回。
- 2026-08-27：`find codegraph -maxdepth 3 -type f -print` 原始输出确认仓内存在 `codegraph/best.json`、`baseline.json`、`target.json` 与 domains 文件，适用有图项目路径；`jq -r '.domains | to_entries[] | select(.value.parent == null) | [.key,.value.type,.value.label] | @tsv' codegraph/best.json` 原始输出列出 12 个 best 顶层域。
- 2026-08-27：`codegraph context d_ledger`（通过 `GOMODCACHE=/root/.handoff/tmp/27c30ff0/b273-gomodcache GOSUMDB=off go run github.com/Xsxdot/charter/graph/cmd/codegraph@v0.9.0 ...`）实际返回 `actual.byCurrentDomain=[d_ledger:12]`、`misplaced=[]`、`truncated=false`、`fociTruncated.total=57 shown=5`，并列出 `d_ledger` 职责为账本保存与事件镜像。
- 2026-08-27：`codegraph context d_protocol` 实际启动并返回 best 领域 `d_protocol`，包为 `internal/proto`，接口含 `m_proto_Event`；该次完整 JSON 输出超过工具显示预算，未把被截断的尾部当作结论。
- 2026-08-27：`codegraph context d_gateway --max-tokens 4000` 实际返回 best 领域 `d_gateway`（boundary，控制门面），包为 `internal/agentd`，`truncated.atDepth=0 droppedNodes=24`，`fociTruncated.total=77 shown=5`，`actual.byCurrentDomain=[d_coordination_api:3,d_coordination_task:1]`，`misplaced=[]`；计划继续以源码补足未覆盖 handler。
- 2026-08-27：`codegraph context d_web` 实际返回 best 领域 `d_web`，但输出被工具截断；结果显示 `actual.byCurrentDomain` 主要为 `d_web`，存在 best/current 视图容器归属分布，计划只记录图债，不修改 web 类型，因为 `web/src/api/ledger.ts#NodeOverride.purpose?: string` 已存在。
- 2026-08-27：对 `internal/ledgerstep/wire.go#waitForTurnEnd`、`internal/ledgerstep/verdict.go#ParseVerdict`、`internal/executor/turn/protocol.go#ProtocolRules`、`internal/agentd/ledgerapi.go#ledgerNodeWire` 运行 `codegraph sym`，四次均返回原始错误：`Error: 符号 "..." 不在图中（图未覆盖或名字有误）；近似候选: []。确认图未覆盖时回落 grep，并把该符号记入本节点产出物的「图覆盖债」小节`，退出状态 1。
- 2026-08-27：对 `waitForTurnEnd`、`ParseVerdict` 运行 `codegraph who-calls`，对 `StepRunner.awaitNode`、`Server.handleFlowGet` 运行 `codegraph chain --with-source`；四次均返回原始错误 `Error: 节点 "..." 不在图中；近似候选:` 与 `exit status 1`。相关符号按 spec 说明回落 grep，列入计划图覆盖债。
- 2026-08-27：首次运行 `go run ... codegraph@v0.9.0 context ...` 使用默认模块缓存，原始错误为 `go: writing stat cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.9.0.info772716982.tmp: read-only file system`；随后将模块缓存改到 `/root/.handoff/tmp/27c30ff0/b273-gomodcache` 后成功，未修改仓库源码或 git 配置。
- 2026-08-27：`go test ./internal/ledgerstep -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/ledgerstep 6.273s`。
- 2026-08-27：`go test ./internal/executor/turn ./internal/executor/codex -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/executor/turn 0.165s`；codex 未产生独立输出，命令仍退出 0。
- 2026-08-27：首次 `go test ./internal/agentd -run 'TestFlow|TestLedgerNodeWire' -count=1` 因默认模块缓存只读失败，原始输出包含 `go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.9.0.mod24106577.tmp: read-only file system`、`internal/agentd/codegraph.go:22:2: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.9.0.lock: read-only file system`、`FAIL github.com/Xsxdot/handoff/internal/agentd [setup failed]`。
- 2026-08-27：用 `GOMODCACHE=/root/.handoff/tmp/27c30ff0/b273-gomodcache GOSUMDB=off go test ./internal/agentd -run 'TestFlow|TestLedgerNodeWire' -count=1` 实际退出 0，原始输出 `ok  github.com/Xsxdot/handoff/internal/agentd 0.841s`。
- 2026-08-27：源码核对确认 `waitForTurnEnd` 当前只按 `event.Type` 返回；`StepRunner.awaitNode` 将 `Client.WaitEvent` 传入，`clientFinalMessage` 再 Attach；`finalMessageFromEvents` 当前对 completed 倒序但遇到缺失 `final_text` 会立即回落 summary，不能表达“多条 completed 优先非空 final_text”。
- 2026-08-27：源码核对确认 `ParseVerdict` 当前只对最后一个围栏正文做整体 `json.Unmarshal`，解析失败由 `NodeStep.RunOnce` 调 `haltForHuman`；成功路径目前无“抢救字段丢失”的普通评论。
- 2026-08-27：源码核对确认 `ProtocolRules` 当前第 2 条是无条件 `全部完成后必须 git add 并 commit（不要 push）`，`RenderPrompt` 原样嵌入 `ProtocolRules`；`internal/executor/codex/developer_instructions_test.go` 复用同一常量。
- 2026-08-27：源码核对确认 `ledger.NodeOverride.Purpose string json:"purpose,omitempty"` 已存在，`proto.NodeOverride` 缺该字段，`ledgerNodeWire` 当前只投影 executor/discipline/target/model；`handleFlowGet` 逐节点走该投影，`handleFlowPut` 解码账本 `[]ledger.NodeDef`。
- 2026-08-27：源码核对确认 `internal/agentd/ledgerapi_test.go` 已提供 `newLedgerEnv`、`ledgerGet`、`ledgerPut`，其环境是真实 SQLite ledger + `httptest.Server`；已有 `TestFlowNodeProducesRoundTripsThroughHTTPWire` 可照抄 HTTP 往返形态，但 B241 必须新增 purpose=review 与零值省略断言。
- 2026-08-27：源码核对补充精确签名：`StepRunner.awaitNode()` 返回 `func(context.Context, string, string) (string, error)`；`NodeStep.RunOnce(ctx context.Context, cardID string) (Outcome, error)`；`RenderPrompt(taskID, planContent, disciplineBlock string) (string, error)`；`Client.WaitEvent(ctx context.Context, taskID string, all bool) (*proto.Event, error)`；`Server.handleFlowGet/Put(w http.ResponseWriter, r *http.Request)`。
- 2026-08-27：源码核对补充测试夹具事实：`seedAgentdLedger` 不提供 `charter` 工作流，只提供 `feature` 等固定流；B241 计划测试因此复用 `newLedgerEnv`/`seedAgentdLedger` 后用真实 `ledger.Store.PutWorkflow("charter", ledger.WorkflowDef{...})` 建立最小 charter，再走 HTTP GET→PUT→GET。
- 2026-08-27：计划实现选择补充 B243 可测试性：`turnEndGrace` 用包内可恢复变量而非 const，测试临时设为 `time.Nanosecond` 并在 defer 恢复；宽限 fake 在 child `ctx.Done()` 上返回，避免测试睡真实一秒。
- 2026-08-27：`docs/superpowers/plans/b273-plan.md` 已按法定路径创建，包含四个独立 task 的文件范围、精确接口、基线/红绿命令、完整关键代码块、日志注释要求、序列化边界、缺陷族、自审、接缝双向覆盖和用户故事归属；本节点未修改实现源码。
- 2026-08-27：补强计划自审：B243 失败事件测试同时覆盖 `failed` 与 `turn_failed`，宽限变量恢复避免真实等待；B241 测试先在真实账本中建立最小 `charter` 再走 HTTP GET→PUT→GET；接口段已对齐源码真实签名。
- 2026-08-27：`git add docs/superpowers/plans/b273-plan.md docs/superpowers/ledgers/2026-08-27-b273-plan-ledger.md` 实际退出 0；随后 `git diff --cached --check` 实际退出 0，`git diff --cached --name-only` 实际只列出本计划与本台账两条法定产出，`git diff --cached --stat` 实际为 `2 files changed, 768 insertions(+)`；占位符与未替换标记扫描实际退出 0。计划内容是本节点唯一产出，未改实现源码。
