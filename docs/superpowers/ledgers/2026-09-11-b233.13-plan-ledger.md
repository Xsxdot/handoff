# B233.13 plan 台账

- 2026-09-11 起点：承接 `cards/B233.13-charter-2` @ `6f91bb09`（P1–P5 全 A 拍板提交）；当前分支 `cards/B233.13-charter-3`（HEAD `8d8f9d79`）；有效基线 `cards/B233.1-charter-7` @ `760a5be9`。本节点不切分支、不改 git 配置、不调 handoff CLI、不起 executor。
- 本次补充：从 charter-2 最新提交续接；法定产出 `docs/superpowers/plans/b233.13-plan.md`；不写实现。
- 本节点实测命令与原始结果：
  - `go build ./...` → `BUILD_EXIT=0`。
  - `go vet ./internal/agentd/` → `VET_EXIT=0`。
  - `codegraph check --view cards-B233.13-charter --repo .` → `"fails": []`；warn `d_gateway->d_orchestration 预算内直调 210/321`；`CHECK_EXIT=0`。
  - `go test ./internal/agentd -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1` → `ok ... 0.053s`。
  - `go test ./internal/approval -run 'TestClientApproverAllowUndeliveredUntilAck' -count=1` → `ok ... 0.066s`。
- 读现状（脚本 + grep，非会话记忆）：
  - `Server.mgr *Manager` `internal/agentd/server.go:128`；`SetManager` `:369`（含 `m.conf = s.conf`、`m.ws == nil` 兜底）。
  - `Manager.Done` `:1696`、`Stop` `:1825`、`transit` `:3726`（幂等分支 `:3731`）；`handleDone` 无条件释放 `:1772`、快照失败分支 `:1768`；`handleStop` `:1815/:1811`；释放实现 `:1822`；`handleDone` 已 completed 短路 `:1746`。
  - handler 对 `s.mgr` 调用面 = 现存 `OrchestrationClient` 方法集；仅 `requireWorkspace`/`assembleResultRef`/`inspectRepoDir` 未导出需改导出（`server.go:1988/2010/2156`、`projectadmin.go:811/958`）。
  - gateway 生产文件（非迁移集）引用迁移符号：`progressPayload`(watchdog)、`ticketAnsweredPayload`(server)、`approverDecisionPayload`(roomsapi)、`isTerminalState`(server)、`DirtyWorktreeError`(server)、`ErrProjectAlreadyExists/OriginMismatch`(server)、`CanonPath`(manualworktree)、`ReconcileExecutorGone`(watchdog)、四个 `err*` 哨兵(server/projectresolve)、`ErrWorkspaceUnavailable`/`NewGitCapability`(server)。
  - 迁移集引用 gateway 专属助手：P3 的 7 条 + 本次新增 `resolveDefaultBaseBranch`/`resolveBaseBranch`/`gitRun`/`gitRunNet`/`probeWorkspaces`/`forwardIfRequested`/`forwardWorktreeIfRequested`。
  - 测试：`package agentd` 内约 27 个测试文件引用迁移符号；`agentd_test` 外部包先例 `hub_test.go`/`integration_test.go`/`lock_test.go`/`server_test.go`/`status_test.go`；共享 helper `w3a_testhelpers_test.go`、`newReceiverTestEnv`(receiver_dispatch_test.go)、`runningCountIn`(scheddispatch_test.go)、`newTestGate`(manager_test.go)、`discardLogger`(watchdog_test.go)。
- 判断：
  - contract §5.1 #2/§2.2 的「`internal/orchestration` 不 import `internal/agentd`」与 §13 修订 D1（import gateway）字面冲突；按 D1 执行，plan §1.2 留痕，未改冻结物。
  - D1 使编排包大量引用 gateway，`d_orchestration→d_gateway` 现有 `legacyBudget:2` 有超预算风险；迁移前无法实测，plan §1.3 给出「check fails=[] 放过，超预算调既有方向预算」的判据。
  - 协调者授权：gateway 生产仍用的符号留 gateway 并导出，P3 的 7 条是下限不是上限；plan §2.3 落成完整收敛表。
- 未验证（需真机/迁移后判定）：§1.3 反方向预算；run 首轮 grep import 断言；Task 3 跨缝测试改外部包的具体可访问性边界；真机清单见 plan §6。
- 提交：本台账与 `docs/superpowers/plans/b233.13-plan.md` 同批提交；`go build ./...` 已于提交前复核。
