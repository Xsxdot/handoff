# B285 spec 台账

- 2026-08-28：用户确认「关组内最后内容即关组（唯一组重置为空组）」，开卡 B285 → spec。
- 2026-08-28：本机 `codegraph` 不在 PATH；工作树有 `codegraph/best.json`，本节点未跑通查图命令，记图覆盖债，改动范围按源码与 B281 spec 收在 `d_web`。
- 2026-08-28：fork `prototypes/b264-tab-groups` → `prototypes/b285-workbench-polish`（gitignore 副本）：去掉 layout-strip、空窗格可关、关内容收列、组空则关组、任务行强制方案 2、面包屑已跟 focusedTask。
- 2026-08-28：现状读数：`closeTab` 留 null；`openTerminal`→`openTab` 加列；`TerminalTab` 自带路径条；`WorkbenchPage` ＋分屏 + 1px 拖放条；`task-row` `onMouseDown preventDefault`；`Breadcrumb` 绑 `wb.base`；`appendRestoredTab` 先填空格。
- 2026-08-28：定级 L2 草案。spec 待用户批准。
- 2026-08-28：用户批准 spec。头部回写「已批准（2026-08-28）」；`prototypes/base/README.md` 追加 B285 fork 确认中。
- 2026-08-28：按 B285 补充执行 `git fetch origin cards/B281-charter-9 && git reset --hard origin/cards/B281-charter-9`；原始结果为 `HEAD is now at fbf849cf docs(B285): 批准工作台收口 spec 并入库对照原型`。
- 2026-08-28：校正后确认 `docs/superpowers/specs/b285.md` 与 `prototypes/b285-workbench-polish/index.html` 存在；当前分支为 `cards/B285-charter-2`。
- 2026-08-28：执行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_web_workbench`；结果 `truncated=false`、`fociTruncated.total=10 shown=5`，当前归属为 `d_web` 的 8 个工作台相关容器均列入 `actual.misplaced`；代码图覆盖债保留，源码核对仍需补足未入图的 B285 调用面。
- 2026-08-28：在校正后的 B281 基线执行 B285 触及范围测试命令；原始失败为 `sh: 1: vitest: not found`，退出码 127，未据此下基线通过结论。
- 2026-08-28：按 `web/package-lock.json` 执行 `npm ci --ignore-scripts`；原始结果为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`，退出码 0；依赖未纳入提交。
- 2026-08-28：依赖恢复后在 B281 基线重跑 B285 触及范围 `npm test -- --run src/app/workbench/tabs.test.ts src/app/workbench/paneDrop.test.ts src/app/workbench/persist.test.ts src/app/workbench/restore.test.ts src/app/workbench/useWorkbench.test.ts src/app/workbench/useWorkbenchSync.test.ts src/app/workbench/WorkbenchPage.test.tsx src/app/tree/ProjectTree.test.tsx src/app/tree/search.test.ts src/app/files/FileTree.test.tsx src/app/shell/Shell.test.tsx src/app/cards/CardsPage.test.tsx`；原始结果为 `Test Files  12 passed (12)`、`Tests  202 passed (202)`，退出码 0。
- 2026-08-28：在 B281 基线执行 `npm run typecheck`；原始输出为 npm 启动行后无错误，退出码 0。
- 2026-08-28：尝试用 `codegraph sym closeTab --with-source` 与 `codegraph sym appendRestoredTab --with-source`；两次均原样失败 `Error: unknown flag: --with-source`，退出码 1；后续改用 `who-calls` 支持的源码参数形态，未据此下签名结论。
- 2026-08-28：执行 `codegraph who-calls openTerminal`；原始失败为 `Error: 节点 "openTerminal" 不在图中；近似候选: `，退出码 1；图覆盖债保留。
- 2026-08-28：执行 `codegraph who-calls findBaseOfTask`；结果含 `Shell`、`TaskDeepLink`、`BoardPage` 三个调用方，但原始响应同时报告 `unscannedEntries: 6` 与 `warning: 基线仍有 6 个未扫描入口：查询结果为空不等于没有调用方`；不以此代替源码接缝核对。
- 2026-08-28：在 B281 基线单独执行 `npm test -- --run src/app/workbench/tabs.test.ts src/app/workbench/persist.test.ts src/app/workbench/restore.test.ts`；原始结果为 `Test Files  3 passed (3)`、`Tests  30 passed (30)`，退出码 0。
- 2026-08-28：在 B281 基线单独执行 `npm test -- --run src/app/workbench/useWorkbench.test.ts src/app/workbench/WorkbenchPage.test.tsx src/app/workbench/TerminalTab.test.tsx`；原始结果为 `Test Files  3 passed (3)`、`Tests  58 passed (58)`，退出码 0。
- 2026-08-28：在 B281 基线单独执行 `npm test -- --run src/app/tree/ProjectTree.test.tsx src/app/shell/Shell.test.tsx`；原始结果为 `Test Files  2 passed (2)`、`Tests  96 passed (96)`，退出码 0。
- 2026-08-28：计划落盘于 `docs/superpowers/plans/b285-plan.md`；任务集固定为 Task 1（tabs/persist/restore）、Task 3（Shell/ProjectTree）、Task 2（Workbench UI/Terminal），执行顺序为 1 → 3 → 2，以便删除 Shell 的 ⌘D 调用后再删除 `splitColumn` API。
- 2026-08-28：计划自审命令 `rg -n 'TBD|TODO|FIXME|适当的错误处理|同 Task' docs/superpowers/plans/b285-plan.md || true`、三项输入/产出 `test -f` 与 `git diff --check` 均退出 0；占位扫描无命中，未修改实现代码。
- 2026-08-28：执行 `git add docs/superpowers/plans/b285-plan.md docs/superpowers/ledgers/2026-08-28-b285-spec-ledger.md` 后运行 `git diff --cached --check`；原始结果退出码 0，暂存统计为 2 files changed、1086 insertions(+)。
- 2026-08-28：首次提交命令 `git commit -m "docs(B285): write workbench polish implementation plan"` 成功，原始提交为 `e218d50cfed478590a09a5a8e5117ae3004765a4`，分支 `cards/B285-charter-2`；随后为把本事实纳入同一提交而 amend。
- 2026-08-28：本节点复核当前 HEAD 为 `ed2dc4f3`、分支 `cards/B285-charter-3`、工作树干净；源码确认 Task 1 仍为旧布局生命周期，Task 2 仍有 `splitColumn`/“＋分屏”/1px 预览，Task 3 仍有 Shell ⌘D 与 selected breadcrumb。
- 2026-08-28：Task 1 红测首次命令未进入测试，原始输出为 `sh: 1: vitest: not found`，退出码 127；按 `web/package-lock.json` 执行 `npm ci --ignore-scripts`，原始输出为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`，退出码 0。
- 2026-08-28：写入 Task 1 锁缝后执行 `npm test -- --run src/app/workbench/tabs.test.ts src/app/workbench/persist.test.ts src/app/workbench/restore.test.ts`；原始结果为 `Test Files 3 failed (3)`、`Tests 5 failed | 29 passed (34)`，失败断言命中旧 close/normalize/restore 行为，另有 `TypeError: closePane is not a function`。
- 2026-08-28：完成 Task 1 最小实现后重跑同一三文件命令；原始结果为 `Test Files 3 passed (3)`、`Tests 34 passed (34)`，退出码 0；随后执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误，退出码 0。
- 2026-08-28：Task 1 变异将唯一 `if (group.columns[column].panes.length === 0)` 改为 `< 0`；先执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误，退出码 0；再执行 `npm test -- --run src/app/workbench/tabs.test.ts -t '关闭 pane 收列、收组'`，原始结果为 `Tests 1 failed | 16 skipped (17)`，失败原文为 `expected ... to have a length of 1 but got 2`；恢复变异后重跑 Task 1 三文件测试与 typecheck，原始结果分别为 `Test Files 3 passed (3)`、`Tests 34 passed (34)` 与 `> tsc -b` 无错误。
- 2026-08-28：Task 3 红测在实现前执行 `npm test -- --run src/app/tree/ProjectTree.test.tsx src/app/shell/Shell.test.tsx`；原始结果为 `Test Files 2 failed (2)`、`Tests 5 failed | 92 passed (97)`，失败包含 `expected null not to be null`（方案 2 SVG）、`Unable to find ... task-avatar-tui`、selected breadcrumb 非空、目录终端 tab 数未增加、⌘D `expected true to be false`。
- 2026-08-28：Task 3 实现后先修正测试中受焦点内容文本与文件抽屉造成的查询歧义（源码行为已通过）；最终重跑同一两文件命令，原始结果为 `Test Files 2 passed (2)`、`Tests 97 passed (97)`，退出码 0；随后 `npm run typecheck` 原始输出为 `> tsc -b` 后无错误，退出码 0。
- 2026-08-28：Task 3 focused breadcrumb 变异把唯一展示表达式改回 `wb.base`；先执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误，退出码 0；再执行 focused Shell 用例，原始失败为 `Expected ... 主目录`、`Received: handoff本机integration/b2-b3`，退出码 1；恢复后继续验证。
- 2026-08-28：Task 3 native drag 变异在唯一任务行渲染入口恢复 `onMouseDown={(e) => e.preventDefault()}`；先执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误，退出码 0；再执行 `npm test -- --run src/app/tree/ProjectTree.test.tsx -t '任务行 mousedown 不阻断原生拖放'`，原始失败为 `expected true to be false`，退出码 1；撤销变异并重跑 Task 3 两文件测试，原始结果为 `Test Files 2 passed (2)`、`Tests 97 passed (97)`。
- 2026-08-28：Task 2 红测在实现前执行 `npm test -- --run src/app/workbench/useWorkbench.test.ts src/app/workbench/WorkbenchPage.test.tsx src/app/workbench/TerminalTab.test.tsx`；原始结果为 `Test Files 3 failed (3)`、`Tests 5 failed | 57 passed (62)`，失败原文包含 `TypeError: result.current.closePane is not a function`、`Unable to find [data-testid="drop-preview"]`、`Unable to find role button name 关闭 空窗格`，以及 TerminalTab 仍渲染路径标题。
- 2026-08-28：修正 Task 2 测试中的 DOM 事件坐标与 closePane 后列收缩预期；执行 `npm test -- --run src/app/workbench/useWorkbench.test.ts src/app/workbench/WorkbenchPage.test.tsx src/app/workbench/TerminalTab.test.tsx`，原始结果为 `Test Files 3 passed (3)`、`Tests 62 passed (62)`；执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误。
- 2026-08-28：删除 `splitColumn`/“增加分屏”后同步更新 Shell 拖放与旧控件回归；执行五文件触及测试（ProjectTree、Shell、useWorkbench、WorkbenchPage、TerminalTab），原始结果为 `Test Files 5 passed (5)`、`Tests 158 passed (158)`；同命令串接 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误。
- 2026-08-28：Task 2 pane-close 变异先核对唯一命中，将 `group.columns[column].panes.splice(row, 1)` 改为 `splice(row, 0)`；`npm run typecheck` 原始输出为 `> tsc -b` 后无错误；focused closePane 用例真实失败，原文为 `expected [ …(2) ] to have a length of 1 but got 2`，随后恢复。
- 2026-08-28：Task 2 半区预览变异先核对唯一右区 class 命中，将 `dragOver.zone === 'right'` 的 `w-1/2` 改为 `w-1`；`npm run typecheck` 原始输出为 `> tsc -b` 后无错误；focused 预览用例真实失败，原文为 `expect(element).toHaveClass("w-1/2")` 且实际 class 含 `w-1`，随后恢复。
- 2026-08-28：为覆盖已打开任务只聚焦原组，新增 Shell seam 断言；首次运行 Shell/ProjectTree 测试时因未限定左栏的重复 `TUI · T1` 文本失败，原始错误为 `Found multiple elements with the text: TUI · T1`，随后改为在 complementary 内查询。
- 2026-08-28：修正查询范围后重跑 Shell/ProjectTree 测试，原始结果为 `Test Files 2 passed (2)`、`Tests 97 passed (97)`；同命令串接 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误。
- 2026-08-28：最终集成触及范围全量测试命令（12 个 test 文件）真实通过，原始结果为 `Test Files 12 passed (12)`、`Tests 210 passed (210)`，退出码 0。
- 2026-08-28：最终执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误，退出码 0。
- 2026-08-28：最终 `git diff --check` 退出码 0；源码扫描 `rg -n "splitColumn|增加分屏" web/src --glob '!**/*.test.*' || true` 无输出；禁止调用扫描仅命中 TabBar 既有 tab 按钮的 `onMouseDown`，未命中本卡任务行或 `console.log`/`fmt.Printf`/`print(`。
- 2026-08-28：执行 `git add` 暂存 17 个改动文件，`git diff --cached --check` 退出码 0；提交 `git commit -m "feat(B285): polish workbench pane lifecycle and focus"` 成功，原始提交为 `c2df07682aca22a1f1ee1251b95db447358b9a95`，分支为 `cards/B285-charter-3`；随后仅为纳入本事实 amend 同一提交。
- 2026-08-28：再次 amend 纳入上一条台账事实；amend 前 `git status --short` 无输出，提交链保持在当前分支且未 push。
- 2026-08-28：本轮起点核对当前分支为 `cards/B285-charter-4`、工作树干净；review-1 对应源码事实为 `closePane` 的有内容返回仍为 `return next`，`WorkbenchPage.placeFromDrop` 对无目录 MIME 的坏/缺 `DRAG_BASE_MIME` 使用 `?? base`，`Shell.openTaskTui` 尚无成功 debug 出口。
- 2026-08-28：写入 review-1 三条接缝测试后首次执行 `npm test -- --run src/app/workbench/tabs.test.ts src/app/workbench/WorkbenchPage.test.tsx src/app/shell/Shell.test.tsx`；原始结果为 `sh: 1: vitest: not found`，退出码 127，未据此下测试结论。
- 2026-08-28：依赖恢复后运行同一三文件红测；原始结果为 `Test Files  3 failed (3)`、`Tests  4 failed | 65 passed (69)`。失败断言分别为残留列 `expected ... length 1 but got 2`、任务 MIME 缺失/损坏 base 仍实际写入 `/local` pane、以及 `expected console.debug ... shell.task.open` 而收到仅 `workbench.restore.success`/`workbench.select`，确认三项均为功能缺失而非 typo。
- 2026-08-28：最小修复后重跑 `npm test -- --run src/app/workbench/tabs.test.ts src/app/workbench/WorkbenchPage.test.tsx src/app/shell/Shell.test.tsx`；原始结果为 `Test Files  3 passed (3)`、`Tests  69 passed (69)`，退出码 0。
- 2026-08-28：修复后执行 `npm run typecheck`；原始输出为 `> tsc -b` 后无错误，退出码 0。
- 2026-08-28：变异复核前核对 `tabs.ts` 的 closePane focus 锚点命中 `1` 次、WorkbenchPage 的 task-base 与 invalidTaskBase 锚点各命中 `1` 次。
- 2026-08-28：将 closePane 有内容返回临时变异为 `return next`；先执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误、退出码 0；再跑唯一生命周期用例，原始结果为 `1 failed | 17 skipped`，失败原文 `expected ... length of 1 but got 2`，确认测试拦截，随后恢复。
- 2026-08-28：首个 task-base 变异仅将 `taskBase` 改为 `taskBase ?? base`；先执行 `npm run typecheck` 退出 0，再跑两条任务 MIME 用例得到 `Test Files 1 passed (1)`、`Tests 2 passed (2)`；因 `invalidTaskBase` 仍拦截而判定变异未改变行为，不计入存活读数并替换为完整语义变异。
- 2026-08-28：完整 task-base 语义变异为 `taskBase ?? base` 且 `invalidTaskBase = false`；先执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误、退出码 0；再跑两条任务 MIME 用例，原始结果为 `Test Files 1 failed (1)`、`Tests 2 failed | 10 skipped (12)`，两例均实际写入 `/local` pane，确认测试拦截，随后恢复正式实现。
- 2026-08-28：正式实现恢复后按计划跑 Task 1 触及测试，原始结果 `Test Files 3 passed (3)`、`Tests 35 passed (35)`，退出码 0；跑 Task 2 触及测试，原始结果 `Test Files 3 passed (3)`、`Tests 64 passed (64)`，退出码 0；跑 Task 3 触及测试，原始结果 `Test Files 2 passed (2)`、`Tests 98 passed (98)`，退出码 0；并执行 `npm run typecheck`，原始输出为 `> tsc -b` 后无错误，退出码 0。
- 2026-08-28：执行 `git diff --cached --check` 退出码 0、暂存 7 个文件；提交 `git commit -m "fix(B285): close review-1 workbench gaps"` 成功，原始输出为 `[cards/B285-charter-4 5e1cabb5] fix(B285): close review-1 workbench gaps`，待将本条事实 amend 入同一提交。
