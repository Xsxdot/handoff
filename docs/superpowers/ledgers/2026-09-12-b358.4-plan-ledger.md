# B358.4 plan 台账（2026-09-12）

节点：charter plan（L3 重档，d_gateway 子系统执行者依契约自写）。工作树基线：`75adb4eb`（B358.3 implement 已合入），分支 `cards/B233.1-charter-7`。

## 已确立事实

- [2026-09-12] codegraph 存在于仓根 `/Users/xushixin/workspace/handoff/codegraph`（baseline.json/best.json/target.json/domains/diffs）。有图先查图成立。
- [2026-09-12] 工作树 git status：`M web/src/app/task/Composer.test.tsx`、`?? .commandcode/`、`?? docs/superpowers/plans/b353-probe-follow-harness.md`、`?? web/design-qa-sidebar-after.png`、`?? web/design-qa.md` —— 均与本卡无关，不碰。

## 输入取证

- B358.1 plan §0.4 红窗清单（agentd 10 支 + cmd 6 支）与存活确认（TestRoomSendErrMapping / TestRoomsEndpoints503WithoutLedger / TestRoomSendUserRejectsSeatFlagsWithoutSideEffects / TestRoomInboxWalksAgentdHTTP）读到（`b358.1-plan.md:58-84`）。
- B358.3 plan §3 红窗基线 19 支 + 核算规则 6 条读到（`b358.3-plan.md` §3）——本卡核算方式与它同款。
- B358.4 卡 note（B358.1 review 移交两支）仓内文档未检索到卡原文（cards 由 handoff CLI 数据面持有，本节点不碰卡）——以协调者派工报文为准逐字吸收：① `sessionWriters` 读账本失败向上传播现仅源码成立，补 fake 注入反例锁「读失败不伪装 403」；② 会话房间不存在 → ErrNoRoom 经 Send 路径无直测，本卡补直测。

## 基线实测（2026-09-12 @ 75adb4eb 工作树，原文留档）

- `go test ./internal/agentd -count=1`：FAIL **恰 11 支** = 红窗 10（TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection / TestRoomMessagesEndpoint / TestRoomSendEndpoint / TestRoomReadEndpoint / TestInboxThreeSources / TestInboxMentionSource / TestInboxRefIDShapes / TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke）+ `TestLegacyNodeEventSequenceUnchanged`（B362 golden，禁触）。
- `go test ./cmd -count=1`：FAIL **恰 8 支** = TestRoomSendLandsRoomMessageWithUserKind / TestRoomSendCoordinatorAcceptsExplicitSeatFlags / TestRoomSendCoordinatorKindUsesGrokHostSession / TestRoomSendCarriesRefAndMention / TestRoomReadStdoutSeqPrefix / TestRoomListSortedByActivity（归 S5）+ TestRepoContractGate + TestServePermissionHookDenyWithReasonAndStep0（环境性）。
- `go test ./cmd -run TestRepoContractGate -count=1 -v` 违规**恰 6 条**：`[dead-interface] OrchestrationClient` + 5 over-budget（d_cli→d_workspace 11/9、d_gateway→d_workspace 17/1、d_orchestration→d_workspace 49/19、d_workspace→d_orchestration 3/2、d_workspace→d_protocol 23/3）——与 B358.3 §3 记载逐条一致。
- `go test ./internal/agentd -run 'TestSession' -count=1` → ok：恰 auth 域 4 支（TestSessionFailureReasons / TestSessionRoutesListRevokeLogout / TestSessionCannotIssueTicket / TestSessionCookieAttrsStayInSync）——`-run 'TestSession'` filter 的既有命中面，与协作会话同名不同物，禁触。

## 源码事实（file#Symbol 读数，2026-09-12）

- `internal/agentd/roomsapi.go#collabErr`：ErrNoRoom/ErrKindNotAllowed→400、ErrNotWriter→403、ErrReadOnly/ErrNotBound→409、默认 500；无 ErrBadState/NotFound 格。`#handleRoomSend` actor 服务端注入（roomUserActor="web:"+hostOnly）、空正文 400。`#handleRoomMessages` 的 ErrNoRoom→404 分支今天不可达（读侧宽容，roomsapi.go:296-300 既有裁决）。
- `internal/collab/sessions.go#mapSessionError`：client.ErrNotFound→collab.ErrNoRoom（唯一归一）。`#SessionDetail/#ArchiveSession/#JoinCard/#LeaveCard` 均经它出栈。
- `internal/collab/service.go#sendToSession`：`GetSession` 失败→mapSessionError→ErrNoRoom（移交②的源码事实，无直测）；`#sessionWriters`：ListAllCards 失败向上传播、注释明言「不伪装成 ErrNotWriter」（移交①的源码事实，无直测）。
- `internal/collab/room/room.go#Resolve`：IsSessionRoom 分支只解析形态、不查会话表（session:999 不在 Resolve 失败，ErrNoRoom 只能来自 sendToSession 的 GetSession）。
- `internal/ledger/sessions.go`：JoinCardToSession 已属他会话→`ErrBadState`（:183）、会话已归档→`ErrBadState`（:175）、卡/会话不存在→`ErrNotFound`（getSessionTx :295 文案含会话 id、getCardTx :377 裸哨兵）；ArchiveSession 幂等短路（B358.1 拍板①）；LeaveCardToSession 幂等。
- `internal/ledger/api/api.go#translateNotFound`：只翻 ErrNotFound→client.ErrNotFound，**ErrBadState 原样透传**——gateway 侧 `errors.Is(err, ledger.ErrBadState)` 可判（agentd 本就 import ledger），**无需新哨兵、无需回 contract**。
- `internal/proto/sessions.go`：SessionSummary/SessionDetail JSON 键集冻结；`nodes/timeline/preview/members/cards/last_active/seat/card_id/card_title` 均 omitempty（缺失≠零值的分辨依据）。
- `internal/agentd/ledgerapi.go:49-53` rooms 注册块 + `#withRooms`（server.go:2768，ledger/rooms 缺一 503）；`ledgerEnv{testAgentdEnv, ledger}`（ledgerapi_test.go:134）、`webUserMember="web:127.0.0.1"`、`newRoomsEnv`（roomsapi_test.go:30）。
- `cmd/session.go` 存在（S3 的 `handoff session wait`）——禁触；`/api/sessions` 路由全仓零命中（无冲突）；`internal/agentd` 无协作会话 TestSession 测试（4 支命中是 auth 域）。

## 决策与岔口（出稿时点）

- **P4 注入点（gateway 半边）定稿**：POST /api/sessions body 只收 `title`；owner=roomUserActor 服务端注入（P4 字面「gateway/CLI 既有 actor 注入面不动」；owner 进 Members → 控制台成员天然在书写者集内，S6 全链可用）。备选（body.owner 统一记法）记入 plan 岔口，翻转成本一处。
- **错误映射定稿**（写进验收字面断言）：新 `sessionErr`＝ErrNoRoom→404、ledger.ErrBadState→409、默认 500；发言/已读路径沿用既有 `collabErr`（ErrNoRoom→400 既有映射，TestRoomSendErrMapping 存活锁）。无需新哨兵、无需回 contract。
- **路由命名定稿**（写进验收字面断言）：POST/GET `/api/sessions`、GET `/api/sessions/{id}`、POST `/api/sessions/{id}/archive`、POST `/api/sessions/{id}/cards`、DELETE `/api/sessions/{id}/cards/{cardID}`。
- codegraph 探针 12 符号（collab.Service 六会话方法、sessionWriters、mapSessionError、collabErr、handleRoomSend、registerLedgerRoutes、proto.SessionDetail）**全部不在图**——与 breakdown §6 同欠，产出物记图覆盖债。

## 产出与定稿（2026-09-12）

- **计划落盘**：`docs/superpowers/plans/b358.4-plan.md`（Task 0 基线复跑 → Task 1 会话六端点红→绿 → Task 2 红窗 10 支改写 → Task 3 移交两支 → Task 4 收口核算；路由表/错误映射表/P4 owner 注入在 §2 定稿并写进验收字面断言）。
- **ledgerDelete 事实更正**：出稿初版按「可能不存在」写了条件退路 + 新增 helper 代码；grep 查证 `internal/agentd/ledgerapi_test.go:114` **已有** `func ledgerDelete(t, env, path, body string)`——计划改为直接用既有签名（DELETE 无体传 `""`），条件退路整段删除（§9 退路同闸改判「无条件退路」）。
- 自审修正两笔（出稿当轮即改）：① TestRoomsListUnreadAndAttachProjection 改写稿首发的 `first` seq 未使用（`_ = first` 收尾）改为不捕获；② global 行断言恢复进改写稿时引入 `i` 未定义笔误，随即修为索引形式。两处均为计划文本内代码笔误，未落任何实现代码。
- 占位符扫描：全文无 TBD/「同 Task N」；正当出口声明 1 处（Task 2 TestInbox 五支机械改写照抄既有文件本体，逐支场名与赋值形态列全）。
- 本节点零实现代码、零 git 操作、零碰卡；工作树无关脏文件未动。
