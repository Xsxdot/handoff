# B358.9 breakdown 台账（会话身份本质重做：一名多端 / 人名收口 / 端只落款）

节点：breakdown（上游 contract `docs/superpowers/specs/b358.9-contract.md`，头部「上游状态：已批准」
「冻结状态：本提交随 codegraph/diffs/cards-B358.9-charter-4.json … 同批冻结」）。
卡：B358.9　有效基线：`cards/B233.1-charter-7` @ `94246fc8`（merge-base 实读）。
角色：handoff 派发形态——executor 出稿、本地协调者拍板；本稿岔口一律「待拍板」，不自行选定。
一行一条历史读数；命令与原始输出照抄，不做二次加工。

## 0. 落点与角色

1. 2026-09-17：本节点法定产出 `docs/superpowers/specs/b358.9-breakdown.md`（不带日期前缀）；台账本文件。
   不写实现代码、不建卡、不派发、不调 handoff CLI、不起新 executor。

## 1. 开工状态位与工作树

2. 2026-09-17：`git branch --show-current` → `cards/B358.9-charter-5`；`git status --porcelain` 无输出。
3. 2026-09-17：`git merge-base HEAD cards/B233.1-charter-7` → `94246fc8a2acc7996c254cb6b473c7c9b5aef225`（= 基线分支头）。
4. 2026-09-17：`git rev-parse HEAD` → `1d77658df1f8b4f9515f5ef14d4b7c03405aec76`（= contract 冻结提交）。
5. 2026-09-17：spec 头部 `docs/superpowers/specs/b358.9.md:5` 实读「**状态**：**已批准（2026-09-17 用户批准）**」——核对通过。
6. 2026-09-17：contract 头部 `docs/superpowers/specs/b358.9-contract.md:3-7` 实读「**上游状态：已批准**」「**冻结状态：本提交随 … 同批冻结**」「**有效基线：** `cards/B233.1-charter-7` @ `94246fc8`」——核对通过，与当前 HEAD 一致。

## 2. 图事实查证（best.json / 分支视图）

7. 2026-09-17：`codegraph views --repo .` → 含 `cards-B358.9-charter-4`。
8. 2026-09-17：读 `codegraph/best.json` 顶层领域（`parent` 缺省）与类型：

```
d_orchestration logic
d_gateway boundary
d_workspace boundary
d_execution boundary
d_sessions boundary
d_transport boundary
d_protocol logic
d_ledger logic
d_collab logic
d_cli logic
d_web logic
d_policy logic
d_maintenance boundary
d_scheduling logic
d_keystone logic
```

9. 2026-09-17：`codegraph validate --repo . --view cards-B358.9-charter-4` →
   `issues` = 恰一条 `[cards-B374-charter] 新增容器 k_collab_model 已存在于基线，containersAdded 只接受新容器`（既有、非本卡）；`edgeIssues=null`。
10. 2026-09-17：`codegraph check --repo . --view cards-B358.9-charter-4` → `fails` 恰一条 `dead-contract d_transport→d_ledger`（B370 遗留）；`warns` 含 `budget-raised d_cli→d_protocol 预算 1→2`（本卡 target.json 注记后的预期抬档，非违规）。
11. 2026-09-17：`codegraph sym` 逐符号：`Server.resolveConsoleIdentity`→d_gateway/ok、`Server.handleIdentity`→d_gateway/ok、
    `sessionDeviceName`→d_gateway/ok、`n_proto_MemberIdentity`→d_protocol/added、`Service.sessionMembers`→d_collab/ok、
    `VerifyWriter`→d_collab/moved、`Server.roomUserActor`→d_gateway/ok、`Server.ledgerActor`→d_gateway/moved、
    `cmd.sessionSendActor`→d_cli/ok。**未命中（图覆盖债，见 §5）**：`handleSessionCreate`、`handleSessionsList`、
    `handleSessionMemberAdd`、`validSessionMemberIdentity`、`sessionWriters`、`sendToSession`、`roomMessageWakeEvents`、`automationWakeEvents`。
12. 2026-09-17：`codegraph who-calls n_proto_ValidateMemberIdentity --view cards-B358.9-charter-4` → 仅自身节点（零生产消费者）——
    H 组条 42「两处重复校验收敛到 proto」尚未落地，符合契约欠账状态。

## 3. 现状代码事实（子卡入口指针与缺陷族用）

13. 2026-09-17：`internal/agentd/roomsapi.go:49` `Server.roomUserActor` → `return "web:" + hostOnly(r.RemoteAddr)`；
    消费点 `:115`(list)、`:447/:449/:453`(send)、`:467/:468`(read)、`:538/:540/:555`(inbox)。
14. 2026-09-17：`internal/agentd/sessionsapi.go:33` `validSessionOwner`（HTTP 重复实现）、`:78`(create 校验)、`:116`(member add 校验)；
    `cmd/session.go:260` `validSessionMemberIdentity`（CLI 重复实现）、`:280`(create 校验)。
15. 2026-09-17：`internal/agentd/sessionsapi.go:63` `handleSessionCreate` **owner 必填**（`:77-82` 缺/非法即 400）；
    契约 §3.8 owner 缺省尚未落地。`:106` `handleSessionMemberAdd` 用 `AddSessionMember(id, actor, actor)`（B366 自助加入）。
16. 2026-09-17：`internal/agentd/ledgerapi.go:56-63` 七个 `/api/sessions` 路由 + `:63` `/members`，全部经 `withRooms`。
    `internal/agentd/server.go:691` `GET /api/identity` 已注册（不挂 withRooms）。
17. 2026-09-17：`internal/collab/sessions.go:219` `sessionMembers`：显式成员 `kind` 默认 `human`，仅当
    `identity==session.Owner && session.Owner!=""` 且前缀 `cli:` 才判 `agent`（`:222-228`）——`agent:<名>` 成员恒被判 `human`（契约 §1.2 漂移）。
18. 2026-09-17：`internal/collab/service.go:118` `sessionWriters` = `session.Members` ∪ 会话内各卡当前 `driver_session`（空座不产）；
    `:145` `sendToSession` = 归档只读 → `sessionWriters` → `VerifyWriter` → `RecordRoomMessage`。协调者派生（决定 5）现状已成立。
19. 2026-09-17：`internal/collab/room/room.go:102` `VerifyWriter` 只剩 actor 非空 + 成员逐字相等（kind 矩阵已废）。
20. 2026-09-17：`internal/proto/rooms.go:37-42` `RoomMessage.Device` 已落（Ticket 0）；**发送路径尚未装填**——
    `grep -n "Device" internal/agentd/roomsapi.go internal/agentd/ledgerapi.go` 零命中（契约欠账 1 核心行为未落）。
21. 2026-09-17：`internal/agentd/auth.go:46-52` `identity{session, device}`；`server.go:877` 鉴权中间件
    `identity{session: sess.ID, device: sess.DeviceName}` 已装填。**测试侧无任何 `identity{...}` 构造**（grep 零命中）——
    契约 §8.7「identity{...} 构造随 device 补齐」的测试半边不适用（已核）。
22. 2026-09-17：`web/src/app/rooms/sessionModel.ts:107` `isSelfActor` 仍按 `actor.startsWith('web:')`；
    `web/src/app/rooms/NewSessionDialog.tsx` 仍有 owner 手输输入框（`:44-52`）；`web/src/api/rooms.ts` 无
    `fetchIdentity`/`IdentityResp` 镜像；`web/src/api/testdata/RoomsFixture.json` 无 `device` 样本。
23. 2026-09-17：台账条目 13 的消费面统计：`grep` 计数 `/api/rooms` 60 处、`/api/sessions` 51 处、`/api/inbox` 2 处
    （`internal/agentd/*_test.go`）；`webUserMember="web:127.0.0.1"` 在 `roomsapi_test.go` 17 处、`sessionsapi_test.go` 10 处。
    端点换脸 + fail-closed 落地后这批夹具必须同步换脸（契约 §8.7）。
24. 2026-09-17：`internal/agentd/sessionsapi_test.go:302`（`TestSessionExplicitMembers`）以
    `AddSessionMember(session.ID,"cli:claude#desktop","user:sy")` 把**席位形状串**写进显式成员并断言在列——
    与契约 §5 F 组条 35「`session.Members` 不含 `cli:` 席位串」字面冲突（见产出物待拍板 P-5）。
25. 2026-09-17：`web/src/api/rooms.ts:206/216/229` 三个消费点签名（`fetchSessions()` 无参、`createSession(title,owner)`、
    `addSessionMember(id,identity='')`）；`Shell.tsx:475` `confirmCreateSession` 建后 `addSessionMember` 自助补员，
    `:476` 写 `localStorage` owner 记忆。

## 4. 亲跑的可执行事实（本节点依据）

26. 2026-09-17：`go build ./...` → 退出码 `0`。
27. 2026-09-17：`go test ./internal/proto/ ./internal/config/ -count=1` → `ok github.com/Xsxdot/handoff/internal/proto 0.007s`、`ok github.com/Xsxdot/handoff/internal/config 0.012s`。
28. 2026-09-17：`go test ./internal/collab/... -count=1` → `ok .../internal/collab 6.352s`、`.../collab/room 0.002s`（client/cursor 无测试文件）。
29. 2026-09-17：`go test ./cmd/ -run 'TestSession' -count=1` → `ok github.com/Xsxdot/handoff/cmd 4.562s`。
30. 2026-09-17：`go test ./internal/agentd/ -run 'TestSession|TestRooms|TestInbox|TestConsoleIdentity' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 12.610s`。
31. 2026-09-17：`npm ci --prefix web --ignore-scripts --no-audit --no-fund` → `added 290 packages in 3s`（本工作树原无
    `web/node_modules`；`web/.gitignore:10` 已忽略 `node_modules`，工作树保持干净）。**这一步使本节点首次可机内跑 web 测试**。
32. 2026-09-17：`cd web && npx vitest run src/api/rooms.test.ts` → `Test Files 1 passed (1)`、`Tests 14 passed (14)`，退出码 0。
33. 2026-09-17：`cd web && npx vitest run` → `Test Files 131 passed (131)`、`Tests 1426 passed (1426)`，退出码 0。
34. 2026-09-17：`cd web && npx tsc -b` → 退出码 0。
35. 2026-09-17：`codegraph resolve --repo . --view cards-B358.9-charter-4 --doc docs/superpowers/specs/b358.9-contract.md` → 退出码 0（全锚 ok/moved）。
36. 2026-09-17：确认 web 测试本轮可跑，故契约 §7.2「TS 孪生金样本未锁（无 web/node_modules）」的**环境前提已改变**——
    本稿把 S4 的 TS 孪生金样本与 vitest 回归从「真机」移回**机内可闭环**（见产出物 §5 真机清单与 §缺陷族）。

## 5. 放弃的尝试

37. 2026-09-17：无。本轮为只读出稿节点，未做代码改动；未碰 handoff CLI、未起 executor。

## 6. 图覆盖债（未命中符号）

38. 2026-09-17：`codegraph sym` 未覆盖（记债，出稿用源码锚）：`handleSessionCreate`、`handleSessionsList`、
    `handleSessionMemberAdd`、`handleSessionJoinCard`、`handleSessionArchive`、`handleSessionLeaveCard`、
    `validSessionOwner`、`validSessionMemberIdentity`、`sessionWriters`、`sendToSession`、
    `roomMessageWakeEvents`、`automationWakeEvents`。均落 `file#Symbol` 源码锚，收尾跑 `codegraph resolve --doc` 校锚。

## 7. 判断与拍板（本节点为提案，不含裁决）

39. 2026-09-17：契约未覆盖但 spec 承诺的产品行为均已找到落点；新增待拍板岔口 P-1..P-7 集中列产出物稿首。
40. 2026-09-17：边界澄清 C-1..C-3（无 handleSessionSend、ledgerapi web: 审计面不在本卡、identity{} 测试面不适用）
    已回写 `docs/superpowers/specs/b358.9-contract.md` §11「修订记录（breakdown 出稿轮，2026-09-17）」（不退回 contract，不改冻结正文）。

## 8. 出稿自检（收尾亲跑）

41. 2026-09-17：`codegraph resolve --repo . --view cards-B358.9-charter-4 --doc docs/superpowers/specs/b358.9-breakdown.md` → 见下条结果（收尾亲跑）。
42. 2026-09-17：`go build ./...` 复跑 → 退出码 0；`git status --porcelain`（产出物落盘后）见提交节。
43. 2026-09-17：`npx vitest run` / `npx tsc -b` 结果见台账条目 33-34（web/node_modules 为 `npm ci` 临时装入，被 `web/.gitignore:10` 忽略，不进提交）。

## 9. 提交（历史读数）

44. 2026-09-17：本节点法定产出物 `docs/superpowers/specs/b358.9-breakdown.md`、台账本文件、contract §11 修订记录同批提交。
    提交命令与原始输出：

```
$ git add docs/superpowers/specs/b358.9-breakdown.md docs/superpowers/specs/b358.9-contract.md docs/superpowers/ledgers/2026-09-17-b358.9-breakdown-ledger.md
$ git commit -m "breakdown(B358.9): 会话身份本质拆解提案——四子卡（gateway 换脸/collab kind/CLI 收敛/web 内容面）+ P-1..P-5 待拍板 + C-1..C-3 澄清"
[cards/B358.9-charter-5 e0344c77] breakdown(B358.9): 会话身份本质拆解提案——四子卡（gateway 换脸/collab kind/CLI 收敛/web 内容面）+ P-1..P-5 待拍板 + C-1..C-3 澄清
 3 files changed, 442 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-17-b358.9-breakdown-ledger.md
 create mode 100644 docs/superpowers/specs/b358.9-breakdown.md
```

45. 2026-09-17：本条（提交事实）随同批提交 amend 收口；amend 后 hash 变化属 git 事实，不在本文件追写。
    收口判据是工作树干净，不是文件里的 hash 等于 HEAD。

### 收尾亲跑（本提交）

46. 2026-09-17：`codegraph resolve --repo . --view cards-B358.9-charter-4 --doc docs/superpowers/specs/b358.9-breakdown.md` → 退出码 0
    （首跑报 5 处坏锚：`ledgerapi.go#Server.ledgerActor`/`identity_test.go#...`/`sessions_fixture_test.go#...` 缺包路径、
    `Server.handleSessionCreate`/`Service.sessionWriters` 带方法接收者前缀而图内为包级名——修为包内短名与全路径后复跑全 ok/moved）。
47. 2026-09-17：`codegraph resolve --repo . --view cards-B358.9-charter-4 --doc docs/superpowers/specs/b358.9-contract.md` → 退出码 0
    （§11 C-2 引用的 `ledgerapi.go#Server.ledgerActor` 修为 `internal/agentd/ledgerapi.go#Server.ledgerActor` 后复跑通过）。
