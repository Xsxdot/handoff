# B279 spec 台账（2026-08-28）

spec 正文：`docs/superpowers/specs/2026-08-28-b279-scheduling-console-design.md`。
本文件记过程读数与判断原始记录；结论与裁决在 spec 正文。

## 分支拓扑读数（acc worktree 实测）

- `git merge-base acc/b156.2-156.3 claude/kai-156-3-ba-df7357` = `06647d442`（merge(b156.2): C5 消费恰好一次…）。
- `git rev-list --left-right --count acc...kai` = 40 / 53（acc 侧 40 提交为 B275 等；kai 侧 53 提交为 K1-K4 集成线）。
- kai 相对分叉点：57 文件 +13256/-116；其中 web 侧仅 `web/src/api/scheduling.ts`(+97) 与三个 testdata 金样本（SquadsResp/QueueResp/CoordinatorLaunchResp）。
- `git merge-base --is-ancestor`：kai 是 K5 祖先 = 真；kai 是 K6 祖先 = 真；K5 是 K6 祖先 = 假（K5/K6 平行）。
- K5 相对 kai 的文件（全部后端）：agentd 的 cardstep/coordapi/scheddrain/wakeconsumer/server + keystone + 两份 plan 文档。
- K6 相对 kai 的非 web 文件：coordapi.go(+test)、proto/scheduling.go、proto/contract_fixture_test.go + 两份 plan 文档。
- K6 相对分叉点的 web 文件：api/contract.test.ts、api/scheduling.ts、五个 testdata 金样本、cards/{CardDrawer,CardItem,CardsPage,ListView,QueuePanel,CoordinatorPanel}(+tests)、settings/{SchedulingPage,SettingsPage.test}。

## 契约读数

- kai `internal/proto/scheduling.go`：CarrierView(11)/SquadView(24)/SquadsResp(34)/CarrierInput(41)/SquadInput(52)/SquadPutResp(61)/QueueEntry(69)/QueueResp(86)/CoordinatorLaunchResp(93)。
- K6 对 proto/scheduling.go 的 diff（尾部 +24 行）：CoordinatorAttachInfo（Machine/Dir/Command，注释明写客户端不得拼接改写）、CoordinatorStatus（Bound/AttachActive/Attach 三态）、CoordinatorAttachReleaseResp。
- kai `internal/agentd/cardstep.go:64`：`node.Override.Squad != ""` → admitSquadStep——节点小队绑定在 kai 已交付，flows「节点编排」小队列的存储面零新增契约。
- acc `internal/ledger/types.go:212` NodeDef：Name/Template/Override(NodeOverride)/Dispatch/Verdict/CarryCardContext/MaxRounds/OmitAcceptance/Produces；`internal/ledger/workflows.go:17` defaultBoardColumns = 代办/沟通中/进行中/审核中/结束（B275 列映射默认）。
- acc `cmd/card_dispatch_test.go:348`：`card dispatch --step "进行中"`——节点取值域 = 工作流节点名（中文状态名）；`cmd/card_node_test.go` 断言 --step flag 说明不含写死白名单（旧白名单已清除）。
- acc `internal/ledger/workflows.go:93` withNodesFromStates：老 def 按 States 补 NodeDef 序列，`node := NodeDef{Name: state}`——节点名=状态名的模型证据。

## acc 前端现状读数（B275 合并后）

- `web/src/app/settings/SettingsPage.tsx`：SectionKey = machines/discipline/general/env/update；:43 有 `?section=update` 深链先例；:93-97 section 渲染分派。无 automation 分区。
- `web/src/app/flows/`：FlowsPage.tsx + NodeEditor.tsx（及各自测试）；看板列映射 UI 在 FlowsPage.tsx:167（aria-label「看板列映射」），:222 per-node 看板列 select。
- `web/src/app/cards/`：CardsPage/CardDrawer/CardItem/ListView/NewCardDialog/MigrateDialog + 测试（B275 形态）。
- `web/src/api/scheduling.ts`：acc 上不存在（随 kai 合入）。

## 判断记录

1. K6 旧 web UI 舍弃取 acc 侧：走查定稿原型与 K6 信息架构不同（独立路由页 vs 三处嵌入），用户 2026-08-28 走查确认原型路线；wire/金样本/测试保留。
2. 定级 L3 轻档：对接 kai/K6 已冻结 wire 契约（对接即动契约层）；新增契约为零。用户授权同 B275 判例直接进 plan（「老样子，按照之前的流程处理」2026-08-28）。
3. 基线合并顺序 kai→K5→K6：K5/K6 平行且同改 coordapi.go，后并者解冲突；web/ 冲突一律取 acc 侧。
4. 原型三轮走查的决策留痕在 `prototypes/b279-automation-proto/README.md` B279 段（排队横带非第六列 / 节点=工作流状态节点 / 标签只在多对一列显形 / 节点编排合并表 / 编制挂设置分区）。
5. plan 第一刀查明项：web 建卡请求 wire 是否已有开卡即绑字段（kai K4 服务端语义已交付「开卡即绑」）；缺则按 B275 init_command 先例镜像补全。

## 跑过的命令（摘录）

- `git branch -a --list '*156*'`、`git log --oneline`（kai/K5/K6 头部）、`git merge-base --is-ancestor` 三次、`git diff --stat`（kai vs merge-base）、`git diff --name-only`（K5/K6 vs kai、K6 web）、`git show kai:internal/proto/scheduling.go`、grep NodeDef/看板列映射/SettingsPage section。
- 原型自检：三页内联脚本抽取 `node --check` 全绿；Chrome headless 截图六张（board 初态/节点胶囊/抽屉 B141.2 接管中、settings?section=automation、flows 节点编排两版），存 /tmp/b279shots/（易失，形态权威以入库原型为准）。

## B282 plan 节点读数（2026-08-28）

- 当前执行分支实测为 `cards/B282-charter`，HEAD=`eea12d202aea16500726ff4aa44f46f152fec908`，工作区干净；`refs/remotes/origin/acc/b156.2-156.3` 指向同一提交。kai/K5/K6 对象可解析为 `17bf788543265876caff9f5de71f651ed1d7bb7e`、`d66da8b8342d73474e1d18b5d565ce3f945917ea`、`ccfe0f09f5d3c0c0cd54d4ac194e67dcd599a57b`。
- 先执行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context frontend`；原始结果为 `Error: 领域 "frontend" 不在最优树词表中；context 已按最优树词表取声明，最优树领域候选: d_cli, d_collab, d_execution, d_execution_adapters, d_execution_contract, d_execution_host, d_gateway, d_keystone, d_ledger, d_maintenance, d_orchestration, d_policy, d_protocol, d_scheduling, d_sessions, d_transport, d_transport_channel, d_transport_tunnel, d_web, d_web_admin, d_web_cards, d_web_command, d_web_contract, d_web_shell, d_web_workbench, d_workspace`，故按最优词表改查 `d_web`、`d_web_cards`、`d_scheduling`、`d_web_workbench`、`d_web_contract`；`d_web` context 返回 `truncated=false`，但提示 6 个未扫描入口和 `d_web_contract` 声明缺失，actual 显示 api 容器实际归属 `d_web_contract` 的错位。
- 原型文件实测为 `prototypes/b279-automation-proto/{README.md,index.html,pages/{settings,board,flows}.html,shared/{shell,styles}.html}`；README 将该目录定为 B279 形态权威，并明确设置自动化分区、看板排队横带/节点胶囊、卡抽屉协调者三态、开卡即绑、flows 节点编排表的入口与走查路径。
- kai proto 读取确认：`CarrierView`/`SquadView`/`SquadsResp`/`CarrierInput`/`SquadInput`/`SquadPutResp`/`QueueEntry`/`QueueResp`/`CoordinatorLaunchResp` 已在 `internal/proto/scheduling.go`；TS 起点 `web/src/api/scheduling.ts` 及 `SquadsResp.json`、`QueueResp.json`、`CoordinatorLaunchResp.json` 已在 kai commit；K6 proto 增量为 `CoordinatorAttachInfo`、`CoordinatorStatus`、`CoordinatorAttachReleaseResp`，并在 `internal/proto/contract_fixture_test.go` 增加对应 fixture。`CoordinatorAttachInfo{Machine,Dir,Command}` 由服务端产生，客户端不得拼接或改写。
- 当前 acc 前端读数：`web/src/api/ledger.ts:139-151` 已有 `BoardLayout`/`FlowDetail`；`:153-160` 的 `NewCardReq` 尚无开卡即绑字段；`web/src/app/settings/SettingsPage.tsx` 尚无 automation section；`web/src/app/flows/FlowsPage.tsx` 仍是 B275 看板列映射；`web/src/app/board/BoardPage.tsx` 是任务看板而非 `/cards` 工作项看板；`web/src/app/cards/CardsPage.tsx`、`CardDrawer.tsx`、`NewCardDialog.tsx` 是本卡主要消费面。
- 基线命令实测：`go test ./internal/proto ./internal/agentd` 退出 0，原始结果为 `ok github.com/Xsxdot/handoff/internal/proto (cached)` 与 `ok github.com/Xsxdot/handoff/internal/agentd 174.475s`；Web 四个命令 `npm test -- --run`、`npm run typecheck`、`npm run build`、`npm run lint` 均退出 127，原始错误均为 `sh: 1: vitest: not found`、`sh: 1: tsc: not found`（build）、`sh: 1: eslint: not found`，待依赖安装后复核。
- 执行 `npm ci` 安装锁定依赖退出 0：`added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`。
- 依赖安装后的 Web 基线：`npm test -- --run` 退出 0，`Test Files 110 passed (110)`、`Tests 1135 passed (1135)`，仅有既有 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`；`npm run typecheck` 退出 0；`npm run build` 退出 0，`1970 modules transformed`，仅有既有大 chunk 警告；`npm run lint` 退出 0，`20 problems (0 errors, 20 warnings)`，警告均为既有文件。
- 第一刀查明结果：acc 与 kai 的 `internal/proto/ledger.go:180-189`、`internal/agentd/ledgerapi.go:740-776` 的 `NewCardReq`/`handleCardCreate` 都没有 coordinate 字段；kai 的开卡即绑是 CLI `cmd/card.go` 在 `CreateCard` 成功后调用 `coordinateAfterCreate`，其 HTTP 语义为 `POST /api/cards/{id}/coordinator/launch` body `{"source":"card_create"}`。因此前端不改建卡 wire，而是在每张建卡成功响应后调用已冻结拉起端点；失败只显式反馈，不回滚已建卡。
- 原型 flows 实测（`pages/flows.html:112-126`）：节点行来自工作流状态机、不可增删；每行配置看板列与小队；额外「拉起通道」行的选择只允许 coordinator。冻结 wire 没有 flow 级 launch-squad 字段，且后端 `resolveCoordinatorSquad` 只接受唯一 coordinator 小队（kai `internal/agentd/coordapi.go:57-84`），故前端该行仅展示当前 coordinator roster/歧义提示，不把它伪造写入 `putFlow`；可持久化的节点选择只写 `NodeDef.Override.Squad`。
- 终端接缝实测：`web/src/app/workbench/tabs.ts:31-40` 的 terminal `TabContent.initCommand?` 已存在，`web/src/app/workbench/TerminalTab.tsx:71-79,217` 已映射到 `init_command`；`web/src/app/workbench/useWorkbench.ts:85-96,179-192` 只有 `openTerminal`，计划新增带 `initCommand` 的动作以复用该既有通道。
- `docs/superpowers/plans/b282-plan.md` 已按 S1–S7 写入：逐 task 文件范围、Consumes/Produces、基线命令、锁缝红绿断言、结构化日志/注释、真机清单、缺陷族/序列化/双向接缝自审；flows 拉起通道明确不伪造 flow wire，开卡即绑明确不改 NewCardReq。
- 计划文件与台账执行 `git diff --check` 无输出；占位符正则检查仅命中自审文字中的“占位”描述，已改为不包含保留字的声明；当前未提交状态为台账 modified、计划文件 untracked，未触碰实现代码。
- 计划与台账首提交实测为 `02082f289b3fea7a3a83f3a35ce3b30b5ce1a274`（`docs(plan): B282 scheduling console implementation plan`）；提交后 `git status --short --branch` 原始输出仅为 `## cards/B282-charter`，工作区干净。
- Task 0 基线复跑实测：`go test ./internal/proto ./internal/agentd` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/proto 0.018s` 与 `ok github.com/Xsxdot/handoff/internal/agentd 125.548s`；`cd web && npm test -- --run` 退出 127，原始错误为 `sh: 1: vitest: not found`，未将缺失依赖误判为代码回归，后续先执行锁定依赖安装再复跑。
- 安装 web 锁定依赖 `npm ci` 退出 0，原始输出为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`；随后 `npm test -- --run` 退出 0，原始摘要为 `Test Files 110 passed (110)`、`Tests 1135 passed (1135)`，仅出现既有 jsdom canvas 警告。
- kai 合并实测：`git merge --no-ff 17bf788543265876caff9f5de71f651ed1d7bb7e` 首次在 `internal/proto/contract_fixture_test.go` 内容冲突；解决时保留 acc 的带 `Board` FlowDetail fixture 与 kai 的 Squads/Queue/CoordinatorLaunch fixtures，删除重复的 FlowDetail case。`gofmt -w internal/proto/contract_fixture_test.go && go test ./internal/proto ./internal/agentd` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/proto 0.003s` 与 `ok github.com/Xsxdot/handoff/internal/agentd 133.134s`。
- K5 合并实测：`cmd/agentd.go` 与 `internal/agentd/server.go` 发生冲突；已保留协作房间装配字段/缓存，同时加入 K5 的自动化循环字段、`StartAutomation(ctx)` 和初始化。`gofmt -w cmd/agentd.go internal/agentd/server.go && git diff --check && go test ./internal/proto ./internal/agentd` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/proto (cached)` 与 `ok github.com/Xsxdot/handoff/internal/agentd 126.926s`。
- K6 合并实测：`internal/agentd/coordapi_test.go`、`web/src/app/cards/CardsPage.tsx`、`CardsPage.test.tsx` 发生冲突。保留 K6 attach/status/forward 测试并按 K5 的准入释放语义修正容量断言；补入 K5 后端失败释放测试。现有 B275 卡片/设置页面文件按计划恢复 acc 侧，K6 旧 UI 资产暂留待 Task 1–4 替换。`gofmt -w internal/agentd/coordapi_test.go && go test ./internal/proto ./internal/agentd` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/internal/proto 0.004s` 与 `ok github.com/Xsxdot/handoff/internal/agentd 131.887s`；`cd web && npm test -- --run src/api/contract.test.ts` 退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 41 passed (41)`。
- Task 2 定向基线：`cd web && npm test -- --run src/app/settings/SettingsPage.test.tsx` 退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 5 passed (5)`；新增自动化页面失败接缝后首次运行退出 1，原始断言为 `Unable to find an accessible element with the role "heading" and name /自动化/`，确认页面尚未渲染该功能。
- Task 2 误用工作目录：在仓库根目录执行 `npm test -- --run src/app/settings/SchedulingPage.test.tsx src/app/settings/SettingsPage.test.tsx` 退出 254，原始错误为 `npm error code ENOENT` 与 `Could not read package.json: Error: ENOENT: no such file or directory, open '/root/.handoff/worktrees/0cfc7bed/package.json'`；未将该结果作为测试结论。
- Task 3 定向基线：`cd web && npm test -- --run src/app/cards/CardsPage.test.tsx src/app/cards/columns.test.ts` 退出 0，原始摘要为 `Test Files 2 passed (2)`、`Tests 17 passed (17)`；删除 K6 旧 QueuePanel 测试，准备按 B282 接缝重写队列折叠/字段/断线保留断言。
- Task 3 QueuePanel 首红：`cd web && npm test -- --run src/app/cards/QueuePanel.test.tsx` 退出 1，原始摘要为 `Test Files 1 failed (1)`、`Tests 2 failed (2)`；断言原始错误分别为 `Unable to find an accessible element with the role "button" and name "⧗ 排队中 2"` 与 `Unable to find an accessible element with the role "button" and name "打开 B1"`，确认旧组件缺少折叠工具条与卡片按钮语义。
- QueuePanel 首次实现运行仍退出 1，原始错误为折叠状态下 `queryByText('B1')` 能找到 hidden DOM 节点；调整测试改以可访问的 `打开 B1` 按钮查询验证折叠，不改变呈现契约。
- QueuePanel 第二次实现运行仍退出 1，原始错误为 `Unable to find an element with the text: wake`；原因是 actor 标签和值在同一文本节点查询边界内，改为独立值节点以锁定字段可见性。
- QueuePanel 接缝实现后复跑退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 2 passed (2)`。
- `nodeLabelFor` 新接缝首红：`cd web && npm test -- --run src/app/cards/columns.test.ts` 退出 1，原始错误为 `TypeError: nodeLabelFor is not a function`，确认符号尚未存在而非断言拼写错误。
- `nodeLabelFor` 空壳复跑仍退出 1，原始断言为 `expected undefined to be '待审阅'`，确认断言有牙后进入最小实现。
- `nodeLabelFor` 最小实现复跑退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 11 passed (11)`。
- Task 3 消费侧红测已加入：CardItem 断言 `⧗ 排队 #2`/节点胶囊，CardsPage 断言 `⧗ 排队中 1` 与 `getQueue` 调用；尚未运行，待生产接线后执行。
- Task 3 消费侧接线后定向测试退出 0：`cd web && npm test -- --run src/app/cards/CardItem.children.test.tsx src/app/cards/CardsPage.test.tsx src/app/cards/QueuePanel.test.tsx src/app/cards/columns.test.ts`，原始摘要为 `Test Files 4 passed (4)`、`Tests 24 passed (24)`。
- Task 3 收尾首轮类型检查：`cd web && npm run typecheck` 退出 2，原始错误为 `src/app/cards/QueuePanel.tsx(47,22): error TS2503: Cannot find namespace 'JSX'.`、`src/app/settings/SchedulingPage.tsx(55,62): error TS2503: Cannot find namespace 'JSX'.`、`src/app/settings/SchedulingPage.test.tsx(46,23): error TS2339: Property 'mock' does not exist on type '(name: string, expect: number, input: CarrierInput) => Promise<SquadPutResp>'.`; `go build ./...` 与 `git diff --check` 同轮退出 0（Go 无输出、diff check 无输出）。
- Task 3 收尾复核：`cd web && npm run typecheck` 退出 0；同轮定向测试（CardItem/CardsPage/QueuePanel/columns/SchedulingPage/SettingsPage/API）退出 0，原始摘要为 `Test Files 8 passed (8)`、`Tests 78 passed (78)`。
- Task 4 定向基线：`cd web && npm test -- --run src/app/cards/CardDrawer.test.tsx src/app/cards/NewCardDialog.test.tsx src/app/shell/Shell.test.tsx src/app/workbench/useWorkbench.test.ts` 退出 0，原始摘要为 `Test Files 4 passed (4)`、`Tests 107 passed (107)`。
- CoordinatorPanel 新接缝首红：`cd web && npm test -- --run src/app/cards/CoordinatorPanel.test.tsx` 退出 1，原始摘要为 `Test Files 1 failed (1)`、`Tests 2 failed (2)`；原始断言分别为找不到 `▶ 拉起协调者` 与 `/attach 与自动唤醒互斥/`，第三条交回测试已通过，确认旧组件缺少新三态按钮语义与 attach 确认框。
- CoordinatorPanel 替换后定向测试退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 3 passed (3)`。
- CardDrawer 新接缝首红：`cd web && npm test -- --run src/app/cards/CardDrawer.test.tsx` 退出 1，原始断言为 `Unable to find an element with the text: 未绑定`，确认抽屉尚未挂载真实 CoordinatorPanel。
- CardDrawer 替换旧节点动作后定向测试退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 34 passed (34)`；旧 `runCardStep` 节点按钮测试随产品边界删除，状态转移按钮保留。
- Workbench 新接缝首红：`cd web && npm test -- --run src/app/workbench/useWorkbench.test.ts` 退出 1，原始错误为 `TypeError: result.current.openTerminalWithCommand is not a function`，确认 hook 尚未导出带 initCommand 的动作。
- Workbench 新动作实现后定向测试退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 19 passed (19)`；断言确认目标分组 tab 内容为 `{ kind: 'terminal', seq: 1, initCommand: 'opencode --session sess-coord' }`。
- Shell 协调者基准新接缝首红：`cd web && npm test -- --run src/app/shell/Shell.test.tsx` 退出 1，原始错误为 `TypeError: coordinatorBase is not a function`，确认树匹配/ synthetic workspace helper 尚未导出。
- Shell helper 与路由回调实现后 `cd web && npm test -- --run src/app/shell/Shell.test.tsx` 退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 34 passed (34)`；同轮消费侧 `CoordinatorPanel/CardDrawer/useWorkbench` 测试退出 0，原始摘要为 `Test Files 3 passed (3)`、`Tests 56 passed (56)`。
- 新增 WorkbenchApi 必填动作后首次 `cd web && npm run typecheck` 退出 2，原始错误为 `src/app/workbench/WorkbenchPage.test.tsx(106,3): error TS2322`，缺失 `openTerminalWithCommand`；测试桩已补齐，未将该中间结果作为最终结论。
- Shell/Workbench 类型接缝修正后 `cd web && npm run typecheck` 退出 0。
- NewCardDialog 开卡即绑首红：`cd web && npm test -- --run src/app/cards/NewCardDialog.test.tsx` 退出 1，原始摘要为 `Test Files 1 failed (1)`、`Tests 4 failed (4)`；新增两测均因找不到 `/开卡即绑/` 复现功能缺失，既有批量成功/部分失败测试也在同轮受 mock 状态影响未通过，待实现后复核。
- NewCardDialog 实现后复跑 `cd web && npm test -- --run src/app/cards/NewCardDialog.test.tsx` 退出 1，原始摘要为 `Test Files 1 failed (1)`、`Tests 2 failed (2)`；一条新测因 `/协调者/` 同时命中 checkbox 与失败结果，另一条既有部分失败测 `findAllByText(/已建/)` 实得 1 而非 2，需隔离复核。
- NewCardDialog 协调者失败用例隔离复跑退出 0：`Tests 1 passed | 16 skipped`；部分失败用例首轮隔离确认两张成功卡均最终呈现，但 `findAllByText` 会在第一张出现时提前返回，测试改为 `waitFor(getAllByText(/^已建 /) 长度 2)`。
- NewCardDialog 完整定向复跑 `cd web && npm test -- --run src/app/cards/NewCardDialog.test.tsx` 退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 17 passed (17)`。
- Task 5 FlowsPage 新接缝首红：`cd web && npm test -- --run src/app/flows/FlowsPage.test.tsx` 退出 1，原始摘要为 `Test Files 1 failed (1)`、`Tests 3 failed (7)`；失败均为缺少“节点编排”、节点小队选择器及 coordinator 歧义提示，确认现有 NodeEditor/加删列 UI 尚未满足 S6。
- Task 5 首次实现后定向测试仍退出 1：原始摘要为 `Test Files 1 failed (1)`、`Tests 1 failed (7)`；唯一失败是测试用 `getByText('进行中')` 同时命中节点单元格和看板列 option，改为限定节点编排区域的 cell 查询，不改变产品断言。
- Task 5 FlowsPage 最小实现复跑 `cd web && npm test -- --run src/app/flows/FlowsPage.test.tsx` 退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 7 passed (7)`；节点行来自详情、不可增删，executor 写入 `override.squad` 且保留旧 override，coordinator roster 仅展示并不写 flow 字段。
- Task 4 收口定向复跑 `cd web && npm test -- --run src/app/cards/CoordinatorPanel.test.tsx src/app/cards/CardDrawer.test.tsx src/app/cards/NewCardDialog.test.tsx src/app/workbench/useWorkbench.test.ts src/app/shell/Shell.test.tsx` 退出 0，原始摘要为 `Test Files 5 passed (5)`、`Tests 107 passed (107)`。
- Task 4/5 同轮 `go build ./...` 退出 0；`cd web && npm run typecheck` 首次退出 2，原始错误为 `src/app/flows/FlowsPage.tsx(101,35): error TS6133: 'templates' is declared but its value is never read.`，已删除替换 NodeEditor 后的死参数，待复跑类型检查。
- 删除死参数后复跑 `cd web && npm run typecheck` 退出 0；`cd web && npm test -- --run src/app/flows/FlowsPage.test.tsx` 退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 7 passed (7)`。
- `cd web && npm run lint` 首次 Task 5 接线退出 1：原始输出为 `SchedulingPage.tsx:53:18 error @typescript-eslint/no-empty-object-type`、`:56:32 error @typescript-eslint/no-unused-vars`，另有 25 条 warning；已将空 props 改为 `Record<string, never>` 并用默认空解构，待复跑。
- `gofmt -l internal/proto internal/agentd internal/keystone` 输出 `internal/proto/rooms_fixture_test.go`；该文件来自已合入后端基线，待最终收口格式化并复跑。
- 修正 SchedulingPage props 与新增接线 warning 后 `cd web && npm run lint` 退出 0，原始摘要为 `19 problems (0 errors, 19 warnings)`；warning 均来自既有项目规则/文件，新增 CardsPage、QueuePanel、FlowsPage warning 已清理或按公开 helper 注释抑制。
- 为 nodeLabelFor 增加“未显式映射走 fallback”行为断言后，`cd web && npm test -- --run src/app/cards/columns.test.ts` 首红退出 1，原始摘要为 `Tests 1 failed (12)`，断言为 `expected undefined to be '自定义一'`，确认原实现错误地拒绝 fallback 节点。
- nodeLabelFor 改用 boardColumnFor 派生同列后复跑同一命令退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 12 passed (12)`。
- 变异前唯一命中检查实跑 `rg -F -n "return nodesInColumn.length > 1 ? status : undefined" web/src/app/cards/columns.ts`，原始输出仅一行：`48:  return nodesInColumn.length > 1 ? status : undefined`。
- 变异将唯一比较符 `> 1` 改为 `> 2` 后，`cd web && npm run typecheck` 退出 0；随后行为断言 `npm test -- --run src/app/cards/columns.test.ts -t "只在同列存在多个节点"` 真实退出 1，原始错误为 `expected undefined to be '待审阅'`，证明变异编译通过且被测试拦截。
- 已恢复 `> 1`；恢复后 `cd web && npm test -- --run src/app/cards/columns.test.ts` 退出 0（`Tests 12 passed (12)`），并行 `npm run typecheck` 退出 0。
- 最终格式化 `gofmt -w internal/proto/rooms_fixture_test.go` 后复跑 `gofmt -l internal/proto internal/agentd internal/keystone` 退出 0，原始输出为空。
- 集成前 `git status --short --branch` 仅列本卡前端、一个基线格式化文件与台账；`git diff --name-only` 未见计划外路径；`git diff --check` 退出 0 且无输出。工作分支为 `cards/B282-charter-2`。
- Task 6 Web 全量测试 `cd web && npm test -- --run` 退出 0，原始摘要为 `Test Files 114 passed (114)`、`Tests 1157 passed (1157)`；仅有既有 jsdom canvas `Not implemented: HTMLCanvasElement's getContext() method` 警告。
- Task 6 `cd web && npm run typecheck` 退出 0；原始输出只有 npm script、`tsc -b` 与空结果。
- Task 6 `cd web && npm run build` 退出 0，原始输出为 `1973 modules transformed`、`✓ built in 2.32s`；仅有既有单 bundle 超过 500 kB 的 Vite warning。
- Task 6 `cd web && npm run lint` 退出 0，原始摘要为 `19 problems (0 errors, 19 warnings)`；warning 文件/规则与前次相同，未新增本卡 error。
- Task 6 `go vet ./...` 退出 0，原始输出为空。
- Task 6 `go test ./...` 实跑约 174 秒后退出 1；原始最终摘要显示 `ok github.com/Xsxdot/handoff/internal/agentd 173.648s` 及其余 internal 包多为 `ok`，但 `FAIL github.com/Xsxdot/handoff/cmd 18.108s`，故 Go 全量当前不能判绿，待定位 cmd 失败测试。
- `go test ./cmd -count=1 -run 'TestRepoContractGate|TestMaybeInstallServiceLinuxRootInstalls'` 退出 1，原始失败为：`TestRepoContractGate` 报 `dead-contract`/`dead-entry`（d_cli→d_collab、d_gateway→d_collab）及 `legacy 命中 ... warn 102 条`；`TestMaybeInstallServiceLinuxRootInstalls` 报 `/tmp/handoff.yaml: ... open /tmp/handoff.yaml: read-only file system`。诊断命令 `go test ... 2>&1 | rg ...` 仅用于提取原文且因 rg 返回码为 0，不作为测试成功结论；全量与单包复现的退出 1 才是最终结果。
- CoordinatorPanel 对 `releaseCoordinator` 返回 `{ok:false}` 的新增断言首红：定向命令退出 1，原始错误为找不到 `交回无头失败：服务端未确认释放`，同时日志仍显示 `coordinator.release.done`，确认实现未检查 ok 字段。
- 增加 ok=false 错误分支后 `cd web && npm test -- --run src/app/cards/CoordinatorPanel.test.tsx` 退出 0，原始摘要为 `Test Files 1 passed (1)`、`Tests 4 passed (4)`；失败时保留人工接管态且不刷新。
- 释放分支变更后的最终 Web 全量 `cd web && npm test -- --run` 退出 0，原始摘要为 `Test Files 114 passed (114)`、`Tests 1158 passed (1158)`；仍仅出现既有 jsdom canvas warning。
- 同轮最终 `cd web && npm run typecheck` 退出 0；`npm run build` 退出 0，原始摘要为 `1973 modules transformed`、`✓ built in 2.17s`，仅有既有大 bundle warning；`npm run lint` 退出 0，原始摘要为 `19 problems (0 errors, 19 warnings)`，warning 集合与前次一致。
- 最终复核 `git diff --check` 退出 0、无输出；`gofmt -l internal/proto internal/agentd internal/keystone` 退出 0、无输出；`go build ./...` 退出 0、无输出。gofmt 唯一修正为 `internal/proto/rooms_fixture_test.go` 中字段对齐空格。
- 提交前 `git status --short --branch` 实测仍仅列本卡实现/测试、`web/src/api/scheduling.fetch.test.ts`、台账及上述 gofmt 文件；`git diff --check` 再次退出 0；未发现计划外路径。
- B282 首次提交 `git commit -m "feat(web): add B282 scheduling console prototype"` 退出 0，生成提交 `7b3932b4`；随后为纳入本行台账执行 amend，最终提交哈希以收尾复核为准。
- 2026-08-28 补充修复基线实测：`git status --short --branch` 原始输出为 `## cards/B282-charter-3`，工作区干净；HEAD 为 `da512f25 (feat(web): add B282 scheduling console prototype)`。
- 补充修复前 `go test ./cmd -count=1 -run 'TestRepoContractGate|TestMaybeInstallServiceLinuxRootInstalls'` 退出 1。原始失败为 `TestRepoContractGate` 的 `dead-contract`（`d_cli→d_collab`、`d_gateway→d_collab`）与 `dead-entry`（`collab 包级函数`），以及 `TestMaybeInstallServiceLinuxRootInstalls` 的 `/tmp/handoff.yaml: ... open /tmp/handoff.yaml: read-only file system`。
- 补充修复前 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 退出 0，原始摘要为 `containers: 263`、`domains: 23`、`edges: 4746`、`nodes: 3718`、`issues: null`、`edgeIssues: null`、`unscannedEntries: 6`。
- 补充修复前 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --help` 退出 0，确认可用 `validate` 子命令；平台禁止调用 handoff CLI，故后续使用等价的 codegraph validate 命令验收图。
- 图与源码盘点实测：当前 baseline 只有 `k_collab_Service`、`k_collab_fn`、`k_collab_model`、`k_collab_client_model`，`internal/collab/room/room.go`、`internal/collab/room/errors.go`、`internal/collab/cursor/cursor.go`、`cmd/room.go`、`internal/agentd/roomsapi.go` 均没有节点；`internal/collab/service.go:63` 的 `New`、`cmd/card_dispatch.go:347` 的 `roomPointer` 也没有节点。
- 真实跨域调用点实测：`cmd/room.go:34` 调 `collab.New`，`cmd/room.go:54/83/121` 经 `openRoomService` 后分别调用 `Service.ListRooms`、`Service.History`、`Service.Send`；`cmd/card_dispatch.go:348` 调 `Service.Pointer`；`internal/agentd/roomsapi.go:56/305/336/358/423` 分别调用 `ListRoomsForMember`、`History`、`Send`、`MarkRead`、`Mentions`。`internal/agentd/server.go:2264-2265` 的 `collab.New`/`cursor.New` 已不在本补图范围内。
- 真实声明盘点实测：room 包含 `Room` 与 `Resolve`、`VerifyWriter`、`KindAllowed`、`IsTerminalStatus`、`SameRoom`、`MentionsMember`、`UnmarshalMessage`、`ReadAllEvents`、`ConsumedSeqs`、`RoomIDOf`、`MessageKind` 及私有 `log`、`isGroupRoom`、`groupRoomKind`；cursor 包含 `Store` 与 `New`、`MarkRead`、`Cursor`、`Snapshot`、`ensureLoaded`、`persist`。常量/错误哨兵无独立节点价值，未虚构节点。
- 补图与测试路径改动后，`jq empty codegraph/baseline.json` 退出 0，`go test ./cmd -count=1 -run 'TestRepoContractGate|TestMaybeInstallServiceLinuxRootInstalls'` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.033s`。
- 同轮 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 退出 1；原始 issue 为既有视图 `cards-B156.2-charter-4` 的四条：`新增容器 k_collab_cursor_Store 已存在于基线`、`k_collab_cursor_fn`、`k_collab_room_fn`、`k_collab_room_model`（`containersAdded 只接受新容器`）。该视图的 `containersAdded` 已实测包含这四个容器，故 baseline 吸收容器后需处理视图重复声明才能满足“全量 validate 零 issue”。
- 视图回灌：四项容器已被 baseline 吸收，从 `cards-B156.2-charter-4` 的 `containersAdded` 移除；其余 `k_web_app_rooms` 容器、节点与边均保留。`jq empty codegraph/diffs/cards-B156.2-charter-4.json` 退出 0。
- 视图回灌后 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 退出 0，原始输出为 `containers: 267`、`nodes: 3760`、`edges: 4760`、`issues: null`、`edgeIssues: null`、`unscannedEntries: 6`，视图列表仍含 `cards-B156.2-charter-4`、`cards-B192-charter`、`cards-B205-charter`。
- 视图回灌后 `go test ./cmd -count=1 -run 'TestRepoContractGate|TestMaybeInstallServiceLinuxRootInstalls'` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.034s`。
- 收尾首次 Web 命令因工作树无 `node_modules` 均退出 127：`npm test -- --run` 原始错误 `sh: 1: vitest: not found`；`npm run typecheck`、`npm run build` 原始错误 `sh: 1: tsc: not found`；`npm run lint` 原始错误 `sh: 1: eslint: not found`。按锁文件执行 `npm ci` 退出 0，原始输出为 `added 290 packages, and audited 291 packages in 3s`、`found 0 vulnerabilities`。
- 依赖安装后 Web 全量 `npm test -- --run` 退出 0，原始摘要为 `Test Files 114 passed (114)`、`Tests 1158 passed (1158)`；仅出现既有 jsdom canvas 警告 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`。
- 依赖安装后 `npm run typecheck` 退出 0；`npm run build` 退出 0，原始输出为 `1973 modules transformed`、`✓ built in 5.32s`，仅有既有单 bundle 超过 500 kB warning；`npm run lint` 退出 0，原始摘要为 `19 problems (0 errors, 19 warnings)`，警告均来自既有文件/规则。
- 收尾 Go 命令 `go build ./...` 与 `go vet ./...` 均退出 0、无输出；`go test ./...` 退出 0，原始输出包含 `ok github.com/Xsxdot/handoff/internal/agentd 158.232s`、`ok github.com/Xsxdot/handoff/internal/collab 5.183s`，其余包均 `ok` 或 `[no test files]`。
- 最终按既有 diff canonical 形态对齐 baseline 四个回灌容器：room/cursor 容器的 label/kind 分别为 `collab.room 包级函数/函数组`、`collab.room 数据模型/实体`、`collab.cursor 包级函数/函数组`、`collab.cursor Store 类型/类型`；未改 target/best/domains。
- 容器命名对齐后复跑：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 退出 0，原始输出仍为 `containers: 267`、`nodes: 3760`、`edges: 4760`、`issues: null`、`edgeIssues: null`；`go test ./cmd -count=1 -run 'TestRepoContractGate|TestMaybeInstallServiceLinuxRootInstalls'` 退出 0，原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.034s`；`jq empty` 两份图文件、`git diff --check`、`gofmt -l internal/proto internal/agentd internal/keystone` 均退出 0、无输出。
- 提交前审计实测：`git status --short --branch` 仅列 `cmd/init_test.go`、`codegraph/baseline.json`、`codegraph/diffs/cards-B156.2-charter-4.json`、本台账四项修改；`git diff --check` 退出 0；受保护路径 diff 为空；charter-4 的 `containersAdded` 实际只剩 `k_web_app_rooms`。
- `git add cmd/init_test.go codegraph/baseline.json codegraph/diffs/cards-B156.2-charter-4.json docs/superpowers/ledgers/2026-08-28-b279-spec-ledger.md` 退出 0；随后 `git diff --cached --check && git diff --cached --stat && git status --short --branch` 退出 0，暂存统计为 4 files changed、562 insertions、9 deletions。
- `git commit -m "fix(graph): backfill collab contract baseline"` 退出 0，生成提交 `4a50cefa`（待纳入本行提交事实后 amend，最终哈希以收尾复核为准）。
- 台账纳入后首次 amend 退出 0，HEAD 实测为 `fa1dadb6ea5cf0c6ef79eba693b790e889ab13d6`；收尾复核中 `git status --short --branch` 仅输出 `## cards/B282-charter-3`，`git diff --check` 退出 0，`git diff HEAD^ --name-only` 仅含上述四个预期文件。
- B282 review 轮 1 补充修复启动：`pwd` 为 `/root/.handoff/worktrees/05700332`，`git status --short --branch` 仅输出 `## cards/B282-charter-4`，工作树干净；本轮按协调者裁决处理 1、3、4、5、6，Task 6 agentd/终端真机验收留给 acceptance 节点。
- 代码图盘点实跑 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_web_cards` 退出 0；返回 `packages` 为 `web/src/app/cards`、`web/src/app/flows`，并报告 `unscannedEntries: 6`、`warning: 基线仍有 6 个未扫描入口`，未将空图解释为无调用方。
- 本轮首轮红测：Web 依赖缺失时 `npm test -- --run ...` 退出 127，原始错误为 `sh: 1: vitest: not found`；按锁文件执行 `npm ci` 退出 0，原始输出为 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`。
- 依赖安装后 review 行为红测 `npm test -- --run src/app/flows/FlowsPage.test.tsx src/app/cards/CardsPage.test.tsx src/app/settings/SchedulingPage.test.tsx src/api/scheduling.fetch.test.ts` 退出 1：`Test Files 3 failed | 1 passed (4)`、`Tests 3 failed | 21 passed (24)`；真实失败为旧“看板列映射”区域仍存在、自动化说明/枚举缺失、旧版本卡测试尚未渲染卡片（后者已修正测试等待时序，未将该时序失败当产品结论）。
- pinned workflow 红测 `go test ./internal/agentd -run TestFlowGetAcceptsPinnedVersion -count=1` 退出 1，原始断言为 `未返回卡片钉住的旧版本: {Version:2 Nodes:[{Name:旧节点}]}`，确认 handler 忽略 `?version=1`；最初误用含 PTY 的环境时测试被跳过，原始 skip 为 `PTY 测试根目录不可用...`，已改用 `newNoPTYLedgerEnv` 后获得有效断言红。
- 变异自验：对唯一命中 `web/src/app/cards/CardsPage.tsx:186` 的 `fetchFlow(target.name, target.version)` 取反为 `fetchFlow(target.name, 0)`；旧版本卡标签测试先退出 1，原始断言为 `expected ... to have a length of 2 but got 1`，DOM 仅保留状态 `待审阅`，确认行为变异命中。
- 变异编译检查：变异状态执行 `cd web && npm run typecheck` 退出 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，无编译错误；随后已恢复原调用。
- 恢复实现后的受影响 Web 接缝测试退出 0：`Test Files 6 passed (6)`、`Tests 38 passed (38)`。
- Web 全量测试 `cd web && npm test -- --run` 退出 0，原始摘要为 `Test Files 114 passed (114)`、`Tests 1162 passed (1162)`；仅出现既有 jsdom canvas warning：`Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`。
- Web 类型检查 `cd web && npm run typecheck` 退出 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，无错误。
- Web 构建 `cd web && npm run build` 退出 0，原始输出为 `✓ 1973 modules transformed`、`✓ built in 2.12s`；仅有既有单 bundle 超过 500 kB warning。
- Web lint `cd web && npm run lint` 退出 0，原始摘要为 `✖ 19 problems (0 errors, 19 warnings)`；warning 均来自既有文件/规则，未新增 error。
- B282 review 修复后 `go build ./...` 退出 0，原始输出为空。
- B282 review 修复后 `go vet ./...` 退出 0，原始输出为空。
- B282 review 修复后 `go test ./...` 退出 0；原始输出包含 `ok github.com/Xsxdot/handoff/internal/agentd 180.774s`、`ok github.com/Xsxdot/handoff/internal/ledger 21.350s`、`ok github.com/Xsxdot/handoff/internal/scheduling 0.685s`，其余包均为 `ok` 或 `[no test files]`。
- B282 review 修复后 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 退出 0；原始 JSON 为 `containers: 267`、`domainDecls: 2`、`domains: 23`、`edges: 4760`、`nodes: 3760`、`issues: null`、`edgeIssues: null`、`entitiesWithoutLifecycle: 0`、`unscannedEntries: 6`。
- 收尾格式检查 `gofmt -l internal/agentd/ledgerapi.go internal/agentd/ledgerapi_test.go && git diff --check` 退出 0，无格式输出；当前变更仅含 B282 修复、测试与本台账 13 个路径。
- B282 review 修复提交 `git commit -m "fix(web): address B282 review findings"` 退出 0，生成提交 `8b6d3c27`；随后将本条提交事实纳入台账并 amend，最终哈希以收尾复核为准。
- 收尾复核：`git status --short --branch` 仅输出 `## cards/B282-charter-4`，工作树干净；`git diff --check` 退出 0；`git show --stat --oneline --summary HEAD` 显示提交主题为 `fix(web): address B282 review findings`，`git diff HEAD^ --name-only` 仅列本台账、两份 Go 文件与 10 份 Web 源/测试文件，共 13 个预期路径。
- B282 review 轮 3 补充修复启动：当前工作树实测分支为 `cards/B282-charter-5`，`git status --short --branch` 仅输出该分支行且无未提交改动；HEAD 为 `d788d52e fix(web): address B282 review findings`。
- 本轮首红：`go test ./internal/proto -run TestCardViewWireCarriesWorkflowVersion -count=1` 退出 1，原始断言为 `CardView wire 缺 workflow_version`；`go test ./internal/agentd -run TestCardsListWireCarriesPinnedWorkflowVersion -count=1` 退出 1，原始断言为 `卡片列表缺 workflow_version`，响应原文未含该键。
- Web 首次红测命令 `npm test -- --run src/app/cards/CardsPage.test.tsx src/app/flows/FlowsPage.test.tsx` 未执行测试，退出 127，原始错误为 `sh: 1: vitest: not found`；按锁文件安装依赖后再复跑，当前 Web 红测未验证。
- 依赖安装实测 `npm ci` 退出 0，原始输出为 `added 290 packages, and audited 291 packages in 3s`、`found 0 vulnerabilities`。
- 依赖安装后 Web 首红复跑 `npm test -- --run src/app/cards/CardsPage.test.tsx src/app/flows/FlowsPage.test.tsx` 退出 1：`Test Files 2 failed (2)`、`Tests 4 failed | 16 passed (20)`；真实断言红为旧 `cards.queue.*` 日志、缺版本卡仍调用 `fetchFlow('custom', undefined)`、Flows 加载中/失败保存按钮未禁用。
- 第一轮实现定向绿：`go test ./internal/proto -run 'TestCardViewWireCarriesWorkflowVersion|TestContractFixtures' -count=1` 退出 0；`go test ./internal/agentd -run TestCardsListWireCarriesPinnedWorkflowVersion -count=1` 退出 0；`npm test -- --run src/app/cards/CardsPage.test.tsx src/app/flows/FlowsPage.test.tsx src/api/contract.test.ts` 退出 0，原始摘要为 `Test Files 3 passed (3)`、`Tests 61 passed (61)`。
- 变异前唯一命中检查：`rg -F -n "WorkflowVersion: view.WorkflowVersion" internal/agentd/ledgerapi.go` 仅输出 `155: Project: view.Project, Workflow: view.WorkflowName, WorkflowVersion: view.WorkflowVersion,`。
- 变异自验：临时将唯一投影赋值改为 `WorkflowVersion: 0` 后，`go build ./...` 退出 0 且无输出；随后 `go test ./internal/agentd -run TestCardsListWireCarriesPinnedWorkflowVersion -count=1` 退出 1，原始断言为 `列表返回了最新版而非卡片钉住版本: got=0`，证明变异编译通过且被行为测试拦截；现已恢复真实赋值。
- 变异恢复后定向复跑：`go test ./internal/proto -run 'TestCardViewWireCarriesWorkflowVersion|TestContractFixtures' -count=1` 与 `go test ./internal/agentd -run TestCardsListWireCarriesPinnedWorkflowVersion -count=1` 均退出 0；Web 定向复跑 `npm test -- --run src/app/cards/CardsPage.test.tsx src/app/flows/FlowsPage.test.tsx src/api/contract.test.ts` 退出 0，原始摘要为 `Test Files 3 passed (3)`、`Tests 61 passed (61)`；`git diff --check` 退出 0、无输出。
- 收尾 Web 全量测试 `npm test -- --run` 退出 0，原始摘要为 `Test Files 114 passed (114)`、`Tests 1166 passed (1166)`；仅有既有 jsdom 警告 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`。
- 收尾 Web 类型检查 `npm run typecheck` 退出 0，原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，无错误。
- 收尾 Web 构建 `npm run build` 退出 0，原始输出为 `✓ 1973 modules transformed`、`✓ built in 2.08s`；仅有既有单 bundle 超过 500 kB 的 Vite warning。
- 收尾 Web lint `npm run lint` 退出 0，原始摘要为 `✖ 19 problems (0 errors, 19 warnings)`；警告均来自既有项目文件/规则，未新增 error。
- 收尾 Go/图校验实测：`go build ./...` 退出 0、无输出；`go vet ./...` 退出 0、无输出；`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 退出 0，原始 JSON 为 `containers: 267`、`domains: 23`、`edges: 4760`、`nodes: 3760`、`issues: null`、`edgeIssues: null`、`unscannedEntries: 6`。
- 收尾 `go test ./...` 实跑退出 0；原始输出包含 `ok github.com/Xsxdot/handoff/internal/agentd 174.809s`、`ok github.com/Xsxdot/handoff/internal/ledger 21.743s`、`ok github.com/Xsxdot/handoff/internal/proto 0.004s`、`ok github.com/Xsxdot/handoff/internal/scheduling 0.701s`，其余包均为 `ok` 或 `[no test files]`。
- 提交实测：`git commit -m "fix(web): carry pinned workflow version in cards"` 退出 0，原始输出为 `[cards/B282-charter-5 bb70d4e9] fix(web): carry pinned workflow version in cards`，提交含 12 个文件、`192 insertions(+), 31 deletions(-)`。
- 提交后为纳入本条台账执行 `git commit --amend --no-edit`；本轮最终提交哈希以收尾复核为准。
- 收尾审计辅助命令曾因 shell 引号未闭合退出 2，原始错误为 `/bin/bash: -c: line 1: unexpected EOF while looking for matching \`\`'`；该命令未用于测试、构建或图校验结论。

## B282 acceptance 节点（2026-08-28 协调者本机）

工作树 `/Users/sycm/.handoff/worktrees/b282-acc`，分支 `cards/B282-charter-5` @ `c9350e8dc`（与 `cards/B282-review-3` 同 tip）。用户点名的 `cards/B282-charter-3` 是 implement 轮2 首绿，落后 review-1/2 两轮修复，验收不以它为对象。

### 复跑

- `gofmt -l internal/proto internal/agentd internal/keystone` 无输出；`go vet ./...` 退出 0。
- Web：`npm ci` 后 `npm test -- --run` 退出 0，`Test Files 114 passed (114)`、`Tests 1166 passed (1166)`；`npm run typecheck` / `build` / `lint` 均退出 0；lint `19 problems (0 errors, 19 warnings)`，与 review 轮一致。
- `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate` 退出 0：`containers: 267`、`domains: 23`、`edges: 4760`、`nodes: 3760`、`issues: null`、`edgeIssues: null`、`unscannedEntries: 6`。本分支相对 `origin/acc/b156.2-156.3` 改了 `codegraph/baseline.json` 与 `codegraph/diffs/cards-B156.2-charter-4.json`，acceptance 之后走图对账列。
- `go test ./internal/agentd -count=1 -run 'TestSquadNodeAdmitsAndDispatchesThroughBinding|TestSquadNodeFullQueuesWithoutTrace'` 首次红：`计数 carrier/c1=0，want 1` 与 `registry ignition_queue/B2|implement: ledger: 记录不存在`。根因：implement 节点 Dispatch 且无 Verdict，`Run` 派发完即返回，K5 `defer releaseSchedulingBinding` 在断言前清零计数（与 `TestCardStepAdmittedRoundReleasesCapacity` 同款语义）。linux-01 全量 174s 时易赢竞态；本机隔离复跑必红。处置：测试里 `holdAfterSquadRound` 在真实派发后卡住 goroutine 再断言。复跑这两条 + 相关 3 条退出 0；随后 `go test ./internal/agentd` 退出 0（62.920s）。
- `go test ./...` 仍有 1 红：`internal/executor/grok` `TestWriteTaskEnvOmitsModelSectionWhenEmpty`，host grok 配置注入 `[models] default=grok-4.6` / `default_reasoning_effort=xhigh`（日志 `default_from=authority`）。同一条在 `acc/b156.2-b156.3` 工作树同样红；B282 diff 未碰 `internal/executor/grok`。不记为本卡回归。

### 变异

- Web `nodeLabelFor` 唯一命中 `web/src/app/cards/columns.ts:48` 取反为 `length > 1 ? undefined : status`；`npm run typecheck` 退出 0；`columns.test.ts` 2 failed（`expected undefined to be '待审阅'`、`expected undefined to be '自定义一'`）；还原后 12/12 绿。
- Go 唯一命中 `WorkflowVersion: view.WorkflowVersion`（`internal/agentd/ledgerapi.go:155`）改为 `0`；`go build ./...` 退出 0；`TestCardsListWireCarriesPinnedWorkflowVersion` 红 `got=0`；还原后该用例绿。

### 真机（隔离 agentd，不碰生产 DataDir）

- 二进制 `/tmp/b282acc/handoff`，`handoff version` = `b282-acc` / revision `c9350e8dc`；配置 listen `127.0.0.1:7788`、DataDir `/tmp/b282acc/data`、SQLite ledger。Vite `AGENTD_URL=http://127.0.0.1:7788` 于 `127.0.0.1:5173`。
- 1 编制：空库 `GET /api/squads` = `{"carriers":[],"squads":[]}`；PUT carrier `c1` expect=0 → `version:1`；PUT 过期 expect=0 → HTTP 409 `schedclient: 注册表版本冲突`。UI `/settings?section=automation` 显示载体 c1/coordc、小队 exec/coord/coord2、登记按钮与版本号。
- 2 队列：`GET /api/queue` 空库与占用后均为 `{"queue":[]}`。协调者 `max_concurrency=1` 时顺序第二次 `POST .../coordinator/launch` 仍 200 新 session（B1 `ses_fb8173fe…`、B2 `ses_fb81657c…`），未入 `launch_queue`。与 K5「回合结束即归还名额」一致：Launch HTTP 返回后槽位已释放，顺序拉起填不满队列。UI 工具条「⧗ 排队中 0」与空队列一致。点火排队未在本轮用 HTTP 填行（无并发双发、无满员 implement 节点）。
- 3 协调者：未绑 `{bound:false,attach_active:false,attach:null}`；无协调者小队 launch 省略 source 与 `source=card_create` 均 HTTP 400 指路文案；`cli=fake` launch HTTP 502 `hostapi: 载体 CLI "fake" 未实装`；两支协调者小队 HTTP 409 `协调者小队不唯一（候选：coord、coord2）`；`cli=opencode` launch HTTP 200 `woke:true`，status `bound:true`，`attach.command` = `opencode --session ses_…`（服务端原文，machine/dir 空串）。
- 4 attach：`POST attach active=true` 回传同一 command，status `attach_active:true`；`active=false` 回 `{ok:true}`，status `attach_active:false` 且仍 `bound:true`。未在 UI 点开终端 tab 核对 `init_command` 请求帧（Chrome headless 未点抽屉）；穿透由 `CoordinatorPanel.test.tsx` 覆盖。
- 5 开卡即绑：`card add --coordinate` 在双协调者小队时卡 B3 已创建、拉起 409、不回滚（CLI 原文「卡 B3 已创建，可稍后…」）。
- 6 节点标签：B1→待审阅、B2→待合并 后 `/cards` 截图「审核中 2」，两张卡分别带「待审阅」「待合并」胶囊；代办列 B3 无胶囊。
- 7 flows：`PUT /api/flows/boardflow` 给「进行中」写 `override.squad=exec` → version 2；GET 无 launch 字段。UI `/flows` 可见 boardflow v1 卡片与「编辑」；节点编排表只在点编辑后挂载（`aria-label="节点编排"`），本轮 headless 未点编辑。

review 第3轮两条 minor：补 `nodeLabelFor` 多对一派生注释；`coordinator.terminal.open` 日志加 `opened/cause`。定向 Web 测试 51/51 绿。

隔离实例与 Vite 在验收结束后关闭，避免残留 opencode 协调者会话。
