# B390 plan 节点台账（唤醒消费轮静默停摆：红色回路 + H1–H5 + 根因分流）

- 2026-09-21 节点开工：分支 `cards/B390-charter-2`，HEAD `83a811989e558588e014b3e2d4cd9d6be033fa7a`（工作树干净）。合并指令已执行：`git fetch origin cards/B233.1-charter-7 && git merge --no-edit -X ours origin/cards/B233.1-charter-7` 返回 `Already up to date.`（本分支已含 `cc77e0a3`）。spec 附件 `docs/superpowers/specs/b390.md` 可读。
- 2026-09-21 图查询：`codegraph --repo . context d_execution` 跑通（输出 `interfaces=65 entities=15`，说明二进制在、图可用）。符号 `consumeAutomationEventsOnce` / `wakeCoordinatorRound` / `LaunchAdmit` / `acquire` / `stepRunning` 用 `codegraph sym`/grep 复核；`flow` 未对这些符号跑（未命中记图覆盖债：`codegraph flow` 未调用，直接读源码）。图覆盖债：本节点未对 `automationLoop` / `automationWakeEvent` 跑 `flow`。

## 基线事实（本节点亲跑，原始输出）

- **本分支 `internal/agentd` 测试包不编译**（动手前基线，红）：
  `go vet ./internal/agentd/` 退出前输出——
  ```
  # github.com/Xsxdot/handoff/internal/agentd [github.com/Xsxdot/handoff/internal/agentd.test]
  internal/agentd/scheddrain_test.go:271:78: undefined: ledger
  internal/agentd/wakeconsumer_b358_test.go:204:106: undefined: ledger
  internal/agentd/wakeconsumer_b358_test.go:232:106: undefined: ledger
  internal/agentd/wakeconsumer_b358_test.go:291:98: undefined: ledger
  internal/agentd/wakeconsumer_b358_test.go:294:129: undefined: ledger
  ```
  根因（读码 + git 溯源）：这两支测试文件用了 `ledger.SeatBearing`（`internal/ledger/binding.go:149`）却没 import `internal/ledger`。该漏 import 由 B389 线 commit `734caefc`（contract 冻结，2 参 `BindSeat` 改 3 参时引入）造成，B389 线 `6f20dfce`（第 2–7 片）已补 import；但本分支由 `-X ours` 合入 `cards/B233.1-charter-7` 的 `cc77e0a3`，该点不含 `6f20dfce`，故把破损版本落进本分支。
- **补 import 后全包绿**：临时给两文件补 `"github.com/Xsxdot/handoff/internal/ledger"`，`go test ./internal/agentd/ -run 'TestB3583|TestAutomation|TestDrainQueues|TestCoord' -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 11.303s`；全包 `go test ./internal/agentd/ -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 165.798s`。核对无误后已 `git checkout --` 还原，工作树干净。
- **红色回路（临时件，已还原删除）**：`internal/agentd/b390_redloop_test.go` 用 `alwaysNoSlotScheduling`（嵌真 `*scheduling.Service`，只覆写 `LaunchAdmit` 恒返回 `scheduling.ErrNoSlot`）驱动 `runAutomationPass` 3 轮，实跑原文：
  ```
  b390_redloop_test.go:72: RED(a) 准入持续失败时未按节拍重试：3 轮只尝试 1 次（want ≥ 3）
  b390_redloop_test.go:76: RED(b) 准入持续失败未落成 needs_human（账本无卡级可见事件）
  --- FAIL: TestB390RedLoopStalledAdmission (0.23s)
  ```
- **诊断件实跑（临时件，已还原删除）** `internal/agentd/b390_diag_test.go`：
  - 失败吞事件、不重试：`准入失败: event_seq=5 cursor 4 -> 5 admits=1` / `第二轮: admits=1（同 seq 未重试）`。即失败后事件被标 seen、游标推进到 5，第二轮不再尝试。
  - `@` 前缀寻址不命中：`@前缀寻址: processed=0 escalated=false err=<nil>` / `resumes=0`，断言 `@B1 未唤醒该卡（CLI --mention 帮助写的是「@成员或卡号」）` 失败。
- **真机日志**：`/root/.handoff/agentd.log`（本节点读，非跑）：`grep -c` 统计——`自动化` 行仅 3 条（全在 2026-09-16T21:33，含 `自动化循环停止 cause=context canceled`）；`协调者小队`=0、`leader 准入失败`=0、`批次唤醒失败`=0、`消费轮失败`=0、`2026-09-19T23:42`=0。日志级别默认 `warn`（`internal/logx/logx.go:32`，`HANDOFF_LOG_LEVEL` 未设、systemd `Environment=` 为空），故循环的成功/启动等 INFO 行全部不可见（H5 证据）。
- **循环存活证据**（H1 反证）：`/root/.handoff/automation-cursor.json` 在持续变化——16:32:26 `{"seq":17114}` → 16:32:58 `{"seq":17116}` → 16:58:08 `{"seq":17175}`。会话消息 seq 16861（09-21 15:10）已被消费（cursor 17175 > 16861），说明消费循环在跑，`@B382` 那条被静默吞掉。
- **共享账本名额键残留**（H4 证据，用自制只读 pgx 程序读 `postgres://handoff@100.84.251.46:54322/handoff` 的 `registry` 表；本节点亲跑，非推断）：
  - `sched_running|squad/leader/cmd` count=**1**，updated_at=**2026-09-19T23:42:32**（正是故障时刻），version=107；
  - `sched_running|carrier/cmd` count=2（09-21T15:30，活跃）；
  - 陈旧残留：`squad/leader/muse`=2（09-05）、`squad/leader/grok`=3（09-07）、`squad/leader/agy`=1（09-05）、`carrier/grok`=4（09-07）、`carrier/muse`=2（09-18）、`carrier/agy`=1（09-10）；
  - 本机 sqlite `ledger.db` 同样有 09-01 起未自愈的残留：`squad/coord/opencode`=1、`carrier/opencode`=1（2026-09-01T22:42:45）。
- **无 TTL / 不自愈机制**（读码复核）：`internal/scheduling/scheduling.go` 的 `sched_running` 计数只有 `acquire`（`968`）`+1`、`Release`（`747`）`-1`、`stepRunning`（`1099`）带符号增量，**无 lease/expires/owner 字段**（`readCount` 1093–1098 只读 `{count}`）；启动对账 `orchestration.ReconcileSchedRunning`（`internal/orchestration/watchdog.go:467`）注释明写「对共享 sched_running **只读不写**」「跨机占用不得清零」，且只在 agentd 启动时跑一次。故一台机器留下的计数不因重启、不因时间而消失。
- **故障行原文**（卡上 seq=16864，从远程账本 `card_events` 读出，非日志文件）：`协调者小队 leader 准入失败: scheduling: 小队或载体并发已满`。`coordinatorAdmissionError` 的 `Error()` 正产此形状（`internal/agentd/scheddrain.go:31-33`），来源 `wakeCoordinatorRound` 的 `LaunchAdmit` 失败（`scheddrain.go:359-362`）。
- **本分支失败路径读码**（`internal/agentd/wakeconsumer.go:421-458`）：失败后 `s.log.Error(...)` →（若 Escalated）标自生 needs_human seen → `s.advanceAutomationCursor(maxProcessed)` → `return processed, escalated, wakeErr`。即**失败即推进游标、事件永不再试**，且准入失败路径不落 needs_human（needs_human 只在 `result.Escalated` 时由 keystone 内部产生）。这正是卡标题「不重试、不告警、不落需要人」。
- **循环生命周期读码**（`internal/agentd/scheddrain.go:52-81`）：`automationLoop` 无 `recover()`；`runAutomationPass` 每轮读消费 + 清队，错误只 `s.log.Error`。goroutine 若 panic 会永久死亡，systemd `Restart=always` 只在**进程**退出时重启，罩不住单 goroutine（H3 潜在形态，无本机 panic 证据）。
- 分流：`MarkNeedsHuman(cardID, reason, actor string) error` 在 `internal/ledger/events.go:384`（Store）与 `internal/ledger/api/keystone.go:17`（Facade）都在；`Server.ledger *ledger.Store` 在 `server.go:110`，`Server.autoLedger *ledgerapi.Facade` 在 `server.go:176`。

## 会碰的东西（红线自检）

- 本节点只读：codegraph、grep/read、`go build ./...`、`go vet/test`（临时件实跑后还原）、只读 pgx 读账本、读 agentd.log/sqlite/automation-cursor。**未碰 handoff CLI、未起新 executor、未派发**。
- 临时件一律 `$TMPDIR=/root/.handoff/tmp/4c6f4dcc`；`internal/agentd/` 与 `internal/scheduling` / `internal/proto` 内的临时改动已 `git checkout -- internal/` 还原，`git status` 只剩两份 docs 产物。
- **未清理任何共享账本键**（用户红线：属运营动作）。只读。

## 计划代码块实跑核验（本节点亲跑，非推断）

本计划 T0/T1/T2/T3 的关键代码块在写进计划前**已在本工作树临时落地实跑**，跑完 `git checkout -- internal/` 还原：

- **T0**：补 `internal/ledger` import → `go test ./internal/agentd/ -run 'TestB3583|TestAutomation|TestDrainQueues|TestCoord' -count=1` → `ok ... 11.303s`；全包 → `ok ... 165.798s`。
- **T1+T2**：把计划里的 `b390_wake_stall_test.go` 与 T2 的 5 处改动（wakeconsumer 常量/类型/函数+失败分支+retry 调用、server 字段+初始化、scheddrain runAutomationPass、wakeconsumer 补 `internal/scheduling` import）逐字落地：
  - 先跑 → **PASS** `ok github.com/Xsxdot/handoff/internal/agentd 0.162s`（三条断言全绿）；
  - 未误伤：`go test ./internal/agentd/ -run 'TestB390|TestB3583|TestAutomation|TestDrainQueues|TestCoord|TestB349' -count=1` → `ok ... 15.642s`；
  - **变异复验**：把 `s.retryPendingWakes(ctx)` 注释掉重跑 → 重新变红 `RED(a) ... 3 轮只尝试 1 次` / `RED(b) ... 未落成 needs_human`；恢复后再次 PASS。
- **T3**：把 `RunningCounts`、`RegistryRunningView` + `SquadsResp.Running`、`SchedulingClient.RunningCounts`、`handleSquadsGet` 投影（含补 `sort` import）逐字落地 → `go build ./...` 退出 0。
- 结论：计划中的完整代码块**可编译、T1 回路在 T2 前红、T2 后绿、变异后复红**——判据不是纸上预期，是本节点实跑读数。

## 提交事实（历史读数）

- 2026-09-21 plan 节点收口提交命令与原始输出：
  ```
  $ git add docs/superpowers/plans/b390-plan.md docs/superpowers/ledgers/2026-09-21-b390-plan-ledger.md
  $ git commit -m "plan(B390): 唤醒消费轮静默停摆——根因分流 + 红色回路转正 + 可见性/观察面任务"
  [cards/B390-charter-2 5249458a] plan(B390): 唤醒消费轮静默停摆——根因分流 + 红色回路转正 + 可见性/观察面任务
   2 files changed, 742 insertions(+)
   create mode 100644 docs/superpowers/ledgers/2026-09-21-b390-plan-ledger.md
   create mode 100644 docs/superpowers/plans/b390-plan.md
  ```
  随后按纪律把本条历史读数 amend 收进同批提交（amend 换 hash 属 git 事实；收口判据是工作树干净）。
