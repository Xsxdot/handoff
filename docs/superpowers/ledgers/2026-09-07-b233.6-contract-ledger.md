# B233.6 contract 台账

日期：2026-09-07。节点：charter contract。工作分支：`cards/B233.6-charter-4`。

## 过程

1. 开工核对 `docs/superpowers/specs/b233.6.md` 头部状态行为「状态：**已批准**（2026-09-06，用户原话「按这个推进吧」）」；定级 L3 轻档，未回写。
2. `git status --short --branch && git log -1 --oneline` 输出：`## cards/B233.6-charter-4`、`8e442535 docs(B233.5): 验收残余入 roadmap；契约补 C-13 Task.squad`。工作树开工时干净。
3. 现状查证：`internal/ledgerstep/dispatch.go#Dispatcher.ViaTemplate` 经 `Transport` 返回 task 后调用 `Store.LinkTask` 与 `Store.RecordDispatch`；`internal/ledgerstep/runner.go#StepRunner.dispatchNodeWithGate` 是节点派发适配器；`internal/ledgermirror/mirror.go#Mirror.subscribe` 从 `MirrorWatermark` 起拉并调用 `Store.AppendMirroredEvent`。
4. 现状 schema：`internal/ledger/store.go:223-236`（PG）与 `:300-314`（SQLite）的 `card_tasks` 只有 `card_id,target,task_id,purpose,created_at`，`card_events` 已有 `source_target,source_task,source_seq` 及镜像唯一索引；本卡的节点/尝试元数据不能从现有挂账行直接得到。

## 命令读数

以下命令及原始输出在本轮逐项追加；失败项保留原文，不作归因。

1. `gofmt -w internal/ledger/tasks.go internal/ledger/events.go internal/ledger/mirror.go internal/ledgerstep/dispatch.go internal/ledgerstep/runner.go internal/agentd/cardstep.go internal/ledgermirror/mirror.go internal/ledger/taskstate_test.go`：退出码 0。
2. `gofmt -l internal/ledger/tasks.go internal/ledger/events.go internal/ledger/mirror.go internal/ledgerstep/dispatch.go internal/ledgerstep/runner.go internal/agentd/cardstep.go internal/ledgermirror/mirror.go internal/ledger/taskstate_test.go`：原始输出为空，退出码 0。
3. `go test ./internal/ledger/ ./internal/ledgerstep/ ./internal/ledgermirror/ ./internal/agentd/ ./internal/client/ -count=1 -timeout 180s`：

   ```text
   ok  	github.com/Xsxdot/handoff/internal/ledger	20.655s
   ok  	github.com/Xsxdot/handoff/internal/ledgerstep	12.643s
   ok  	github.com/Xsxdot/handoff/internal/ledgermirror	3.889s
   ok  	github.com/Xsxdot/handoff/internal/agentd	162.867s
   ok  	github.com/Xsxdot/handoff/internal/client	9.493s
   ```

   退出码 0；其中新增金样本 `TestAppendMirroredEventWorkflowEnvelopeGolden` 随 `internal/ledger` 一并通过。
4. `go build -o /dev/null .`：原始输出为空，退出码 0。
5. `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate`：退出码 0，原始输出：

   ```text
   {
    "containers": 300,
    "domainDecls": 23,
    "domains": 23,
    "edgeIssues": null,
    "edges": 5507,
    "entitiesWithoutLifecycle": 2,
    "issues": null,
    "nodes": 4499,
    "unscannedEntries": 5,
    "views": ["cards-B233.1-charter-2", "cards-B233.2-charter", "cards-B233.3-charter", "cards-B233.4-charter", "cards-B233.5-charter", "cards-B233.6-charter"]
   }
   ```
6. `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.6-charter resolve --doc docs/superpowers/specs/b233.6-contract.md`：退出码 0；原始输出为 `{"anchors":[...]}`，列出的所有文档锚点均为 `anchor: "ok"` 或图保留的 `anchor: "moved"`，无坏锚。
7. `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check`：退出码 1。原始失败片段：

   ```text
   Error: 契约对照发现 2 处违规
   {
    "fails": [
     {"kind":"dead-contract","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）"},
     {"kind":"dead-interface","from":"d_execution","to":"d_orchestration","detail":"契约 d_execution→d_orchestration 声明的接口 \"ApprovalClient\" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）"}
    ]
   }
   ```
8. `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.6-charter check`：退出码 1；原始失败片段与上项相同，仍为 `d_execution→d_orchestration` 的 `dead-contract` 与 `dead-interface` 两项；本卡不修该存量契约。
9. 视图改为只修改 baseline 已有节点后，重新运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate`：退出码 0；原始摘要仍为 `issues: null`、`views` 含 `cards-B233.6-charter`。
10. 同批重新运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B233.6-charter resolve --doc docs/superpowers/specs/b233.6-contract.md`：退出码 0；原始输出 `{"anchors":[...]}`，无坏锚。
11. `git diff --check`：原始输出为空，退出码 0。

## 提交（历史读数）

本轮冻结提交命令及其原始输出；随后只 amend 一次把本段收进同批提交，amend 后 hash 不回填本台账。

```text
$ git add codegraph/target.json codegraph/diffs/cards-B233.6-charter.json docs/superpowers/ledgers/2026-09-07-b233.6-contract-ledger.md docs/superpowers/specs/b233.6-contract.md internal/agentd/cardstep.go internal/ledger/events.go internal/ledger/mirror.go internal/ledger/tasks.go internal/ledger/taskstate_test.go internal/ledgermirror/mirror.go internal/ledgerstep/dispatch.go internal/ledgerstep/runner.go
$ git commit -m "contract(B233.6): 冻结工作流共享派发与事件镜像契约"
[cards/B233.6-charter-4 a428095a] contract(B233.6): 冻结工作流共享派发与事件镜像契约
 12 files changed, 596 insertions(+), 16 deletions(-)
 create mode 100644 codegraph/diffs/cards-B233.6-charter.json
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.6-contract-ledger.md
 create mode 100644 docs/superpowers/specs/b233.6-contract.md
```
