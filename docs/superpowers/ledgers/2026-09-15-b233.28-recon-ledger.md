# B233.28 recon 台账（2026-09-15）

节点：`recon`（图对账：gateway 任务面/项目位置走编排查询门面）
待对账分支：`cards/B233.28-charter-7` @ `81d6b7c5`（工作分支 `cards/B233.28-charter-8`，HEAD 同）
有效基线：`cards/B233.1-charter-7` @ `56c7a9c7`（git merge-base HEAD 即 `56c7a9c7`）
契约视图：`codegraph/diffs/cards/B233.28-charter.json`（Ticket 0 起于 contract 提交 `41741fed`，base `b5ebef69`）
本节点工作树：`/root/.handoff/worktrees/9be95dd8`
只读不提交业务代码；本节点只改 `codegraph/diffs/cards/B233.28-charter.json` 与本台账。

## 0. 无图判定

仓库有 `codegraph/` 目录，进入对账流程。

## 1. 改动面（BASE..HEAD）

`git diff --name-status 56c7a9c77..HEAD` 生产 `.go`（非测试）：

```text
M internal/agentd/codegraph.go
M internal/agentd/coordapi.go
M internal/agentd/handlers.go
M internal/agentd/projectadmin.go
M internal/agentd/roomsapi.go
M internal/agentd/taskplan.go
M internal/agentd/taskroute.go
M internal/agentd/tasksfanout.go
M internal/agentd/update.go
M internal/agentd/workspacefiles.go
A internal/orchestration/facade.go
M internal/orchestration/orchestration_client.go
```

改动性质：

1. 新增 `internal/orchestration/facade.go`：15 个 `*Manager` 方法（10 查询直通镜像 + `AnswerTicket`/`ResumeIfIdle` 行为 + 3 项目位置）。
2. `OrchestrationClient` 接口方法集扩 15 条签名。
3. gateway 10 个生产文件的 `s.st.<任务/项目闭包方法>` 全部切 `s.mgr.*`，并加未就绪守卫。
4. `handleReply` 收敛为只调 `s.mgr.AnswerTicket`/`ResumeIfIdle`；`Server.resumeIfIdle` 整体删除。

## 2. 逐项对账结论

### 2.1 新增符号（视图 `nodesAdded`）

Ticket 0 已在契约提交落 15 个 `Manager` 方法节点。实现期这 15 个方法从「空壳/直通镜像占位」变为「行为落地 + 行号/摘要更新」，**节点 ID 不变、容器不变**，故仍属 `nodesAdded`（对基线而言仍是新符号），只需把 `line`/`signature`/`summary` 改锚到 implement 后的 HEAD。

**补**：15 个 `nodesAdded` 的 `line`（35→28 等）、`signature`（含 doc 注释与完整体）、`summary`（doc 转录）、`params` 已按 HEAD `internal/orchestration/facade.go` 重锚。

### 2.2 删除符号（视图 `nodesDeleted`）

`internal/agentd/handlers.go#Server.resumeIfIdle`（基线 `:459`）在 implement 中被整体删除（循环搬入编排侧 `Manager.ResumeIfIdle`）。

**补** `nodesDeleted: ["n_agentd_Server_resumeIfIdle"]`。基线该节点的 3 条 out-edge 随之删除（见 2.4）。

### 2.3 改锚符号（视图 `nodesModified`）

调用点切换使 10 个 gateway 生产文件发生行号位移与签名变化。对基线节点逐条比对：**77 个节点**的 `line` 或 `signature` 与 HEAD 不符，其中 23 个签名变化、54 个仅行号漂移（含 DTO 与既有 helper）。逐文件：`handlers.go` 34、`roomsapi.go` 14、`workspacefiles.go` 12、`projectadmin.go` 4、`coordapi.go` 4、`codegraph.go` 2、`update.go` 2、`taskplan.go` 1、`taskroute.go` 1、`tasksfanout.go` 1、`orchestration_client.go` 2（`OrchestrationClient` 签名扩、`TerminalOutcome` 行漂移）。

**补**：77 个 `nodesModified` 写 HEAD 完整节点（`file`/`line`/`signature`/`summary`，签名变化者带 `signatureOld`）。

依据：契约 §10「§1.1 / §1.3 列的 `s.st.*` 全部改用 `s.mgr.*`」——本卡收口面覆盖 `handlers.go` 的全部 `s.st.GetTask`（含 `handleDone`/`handleStop`/`handleResume`），故这些 handler 的签名同样变化。DTO（`replyRequest` 等）与邻近 helper 仅因上方插入守卫/删除函数而行号漂移，一并改锚。

### 2.4 调用边对账（视图 `edgesDeleted` / `edgesAdded`）

代码新增的符号/边：

- 15 个 `Manager` 门面方法对 `store` 包的调用（10 查询直通 + `AnswerTicket` 的 `GetTicket`/`AnswerTicketApplied`/`AppendEvent` + `ResumeIfIdle` 的 `GetTask`/`PendingTickets`/`UpdateTaskState`）——**补 19 条 `edgesAdded`**（`n_orchestration_Manager_*` → `n_store_Store_*`），逐条按 facade.go 真实源码调用点核实（禁裸名撞库）。
- `Manager.AnswerTicket` 构造 `TicketAnsweredPayload`——**补 1 条**边到 `m_orchestration_TicketAnsweredPayload`（与基线 `approvePermission` 同款）。
- 代码删除的边：切 `s.mgr` 后，10 个 gateway 文件对 `store` 收口方法的直调全部消失——**补 39 条 `edgesDeleted`**（22 个 handler 函数的 33 条 `→Store.*`、`handleReply` 的 `→TicketAnsweredPayload`/`→resumeIfIdle`、`resumeIfIdle` 的 3 条 `→Store.*`）。

### 2.5 矛盾核对

- 视图/基线记了代码里不存在的符号/边：本卡未发现需裁 fail 的假边。基线 `Server.resumeIfIdle` 及其 3 条边为真实存在后被本卡删除，走 `nodesDeleted`/`edgesDeleted` 正路，非「假边」。
- 登录/工作台容器粒度债：`k_store_*` 与 `best.json` 归属本轮按 C 裁决不动（契约 §6），非矛盾。

## 3. 自检读数

```text
codegraph validate --view cards/B233.28-charter --repo .  → issues: null（0 issue）
codegraph check    --view cards/B233.28-charter --repo .  → fails: []，d_gateway->d_orchestration = 39（≤76）
```

抽查新符号 `codegraph sym --view cards/B233.28-charter`：

```text
Manager.AnswerTicket        → internal/orchestration/facade.go:107  anchor=ok
Manager.ResumeIfIdle        → internal/orchestration/facade.go:141  anchor=ok
Manager.ListProjectLocations→ internal/orchestration/facade.go:73   anchor=ok
Manager.UpdateProjectLocation→internal/orchestration/facade.go:85   anchor=ok
```

`codegraph sym Server.resumeIfIdle --view` 不再命中（节点已删）。

## 4. 红线自检

- 只动 `codegraph/diffs/cards/B233.28-charter.json`（+ 本台账）；`git diff --stat codegraph/target.json codegraph/best.json codegraph/baseline.json` 空。
- 未改业务代码；未跑 handoff CLI、未起 executor、未派发。
- 无「未亲自跑到结果的结论」：validate/check/sym 命令均本节点实跑。
