# B233.16 breakdown 台账（2026-09-11）

节点：`charter:breakdown`（B233.16 跨机执行消费面：生产持有 ExecutionClient）
基线：功能线 `cards/B233.1-charter-7` @ `c567c3d2`；本分支 `cards/B233.16-charter-2`，起点 HEAD `e4ede597`
产出物：`docs/superpowers/specs/b233.16-breakdown.md`
本台账只记过程与原始读数；结论与裁决回写 breakdown 正文与 §0。

## 1. 入口读数

- 上游状态核实：spec 头部「已批准（2026-09-11）」；contract 递交 `03b5f089`（头部「冻结状态：随本提交冻结」），spec 勘误 `e4ede597`。
- Ticket 0 状态：8 个消费方组合接口 + 8 个具名入口 + `StepRunner.Clients` 字段收窄 + 装配直通闭包 + 三份 `execution_signature_lock_test.go`；`target.json` 三边注记；本分支视图 diff。全部随 contract 提交入库。

## 2. 原始命令与读数

| # | 命令 | 原始结果 |
|---|---|---|
| 1 | `go build ./...` | 无输出，退出码 0 |
| 2 | `go vet ./cmd/ ./internal/agentd/ ./internal/ledgerstep/` | 无输出，退出码 0 |
| 3 | `go test ./cmd/ ./internal/agentd/ ./internal/ledgerstep/ ./internal/client/ ./internal/targetclient/` | `cmd` FAIL（`TestRepoContractGate`）；`agentd` FAIL（`TestLegacyNodeEventSequenceUnchanged`）；`ledgerstep`/`client`/`targetclient` ok。详见 §4 |
| 4 | `codegraph check --view cards-B233.16-charter` | `fails=5`（全部 `over-budget`：d_cli/d_gateway/d_orchestration→d_workspace、d_workspace→d_orchestration/d_protocol）、`warns=139` |
| 5 | `codegraph check`（不带视图） | `fails=5`、`warns=139`，与 #4 的 fail/warn 集合**逐一致** |
| 6 | `codegraph validate --view cards-B233.16-charter` | 26 个完整性问题、0 失鲜；逐条均为 `[cards-B233.4-charter]` diff 边/容器问题，**无一条 B233.16** |
| 7 | `codegraph resolve --doc docs/superpowers/specs/b233.16-contract.md` | exit=0 |
| 8 | `codegraph resolve --doc docs/superpowers/specs/b233.16-breakdown.md` | 首跑 exit=1：坏锚 `server.go#clientForTarget (file_missing)`；改为 `internal/agentd/server.go#Server.clientForTarget` 后 exit=0 |
| 9 | `codegraph sym ExecutionClient` / `codegraph sym runStop` | 均「不在图中」→ 图覆盖债，落稿内 |
| 10 | `codegraph sym StepRunner` | 命中 `m_ledgerstep_StepRunner`，domain `d_ledger` |
| 11 | 视图 diff 解析 | `nodesAdded=16`（8 接口模型 + 8 具名函数/方法）、`nodesModified=9`；与 contract §5 #56 / §8 一致 |
| 12 | `target.json` 三条入传输边 | d_cli→d_transport `legacyBudget=10` entries `["client 实体","client.Client"]`；d_gateway→d_transport `legacyBudget=9` entries `["targetclient.Pool","client.Client","client 实体"]`；d_orchestration→d_transport `legacyBudget=5` 无 entries；**无 d_ledger→d_transport**；三边均含 B233.16 注记 |
| 13 | `git show 03b5f089 -- <7 源文件>` | 逐文件复核：仅类型/装配搬运，无 wire/文案/超时/日志语义改动（详见稿内 §2.1.3） |

## 3. T1 变异复验（本节点独立跑）

把 `cmd/stop.go` 的 `runStop` 第二参数由 `client.ExecutionClient` 改回 `*client.Client`：

```
$ python3 <mutation: cmd/stop.go runStop 第二参数 → *client.Client>
mutated
$ go vet ./cmd/
# github.com/Xsxdot/handoff/cmd
vet: cmd/execution_signature_lock_test.go:20:94: cannot use runStop (value of type func(cmd *cobra.Command, c *"github.com/Xsxdot/handoff/internal/client".Client, taskID string) error) as func(*cobra.Command, "github.com/Xsxdot/handoff/internal/client".ExecutionClient, string) error value in variable declaration
VET_MUT_EXIT=1
$ cp <backup> cmd/stop.go && git diff --quiet cmd/stop.go && echo clean=YES
restored
clean=YES
```

结论：T1 签名锁**有牙**。contract §4 另两条（`probeStatus`、`StepRunner.Clients`）由 contract 台账记录，本节点未重跑，稿内已如实标注。

## 4. 测试红与观察

- `cmd`：`--- FAIL: TestRepoContractGate`，报 5 条 `d_*→d_workspace` over-budget；与 #4/#5 的 `codegraph check` fail 集合逐一致 → 基线存量，非本卡引入。
- `agentd`：`--- FAIL: TestLegacyNodeEventSequenceUnchanged`，`got=GOLDEN|2|dispatched|...` vs `want=GOLDEN|2|comment|web:test|...`；属 B233.10 存量事件序列问题，与 B233.16 类型收窄无关。
- `agentd` 首跑另现 `--- FAIL: TestPtyWSAttachedBacklogBytesKeyPresent`（`backlog_bytes = 24，期望 0`），第二次运行未复现 → **可疑 flaky，未归因**。
- `ledgerstep` / `client` / `targetclient`：ok。

## 5. 契约缺口（P1 依据）

- spec 接缝表第 9 行「cmd wait 的一次性消费点（现状内联）→ `waitCmd` RunE → 组合面（`+Attach`/`Status`）」。
- 实测：`cmd/wait.go:116` RunE 内 `newTargetClient()` 取聚合 `*client.Client`；`:139` 内联 `cli.WaitEvent(...)`。contract §2.1 具名入口无此项，§5.3 #27–34、§5.4 #35 亦不覆盖。
- 结论：spec 明列接缝无冻结物兑现 → §0 P1，待协调者拍板（退回 contract 加冻 vs 认定组装点澄清）。

## 6. 拍板前置状态

- 头部状态：**待拍板（2026-09-11）**。
- §0 待拍板：P1（wait 一次性具名入口缺口）、P2（CLI T2 注入形态）、P3（T3 守卫范围）、P4（T3 白名单粒度/扫描判据）。均未自批。
- 边界澄清待回写：C-0（contract 头部补冻结 hash）、C-2（T3 范围仅 cmd）、C-3（runFollow 有效依赖面为 waitClient）。

## 7. 提交记录

见本批提交（产出物 + 本台账同批提交）。提交事实的原始读数以 `git show --stat` 为准，不 chase 回填 amend 后的 hash。

```
$ git add docs/superpowers/specs/b233.16-breakdown.md docs/superpowers/ledgers/2026-09-11-b233.16-breakdown-ledger.md
$ git commit -m "breakdown(B233.16): 执行消费面收窄拆解提案——单一实现范围 + P1–P4 待拍板"
[cards/B233.16-charter-2 593e07d7] breakdown(B233.16): 执行消费面收窄拆解提案——单一实现范围 + P1–P4 待拍板
 2 files changed, 411 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-11-b233.16-breakdown-ledger.md
 create mode 100644 docs/superpowers/specs/b233.16-breakdown.md
```

随后 `git commit --amend --no-edit` 收进本条台账记录（amend 换 hash 是 git 事实，不回填不 chase）；收口判据是工作树干净。
