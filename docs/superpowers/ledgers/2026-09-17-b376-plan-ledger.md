# B376 plan 台账

- 2026-09-17 远程 plan 第一轮 `04c331cb` 从 origin/main `85840cf` 起，工作树无 spec、无 `internal/approval`。协调者把 spec checkout 进树后仍在错误代码面上探路。`handoff stop`。
- 卡基线已冻结，无法 `card update --base-branch`。第二轮 `730e1584` extra 要求 `git reset --hard origin/cards/B233.1-charter-7`；reset 获批后 `turn_failed`（opencode serve 退出），节点「裁决解析失败」。
- L2 plan 改由协调者在本机功能线写：`docs/superpowers/plans/b376-plan.md`。
