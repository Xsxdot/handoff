# B294 spec 台账（2026-08-29）

- 卡：B294，project=handoff，priority=高，charter v9。建于待办，note 后来源后 `move spec`。
- 基线：`main` @ `c1157471`（`fix(web): align sidebar and tabs with prototype`）。本会话先 `git pull --ff-only origin main` 才读左栏现状。
- 来源：用户要补 SuperDev 覆盖不到的一层（未登记静态页、已经在跑的远端服务）；人看人点，agent 走 SuperDev。

## 对话拍板（按时间）

- 弃选 iframe / 路径反代作为前后端方案：JS 在本机跑，`localhost:<后端>` 会打到笔记本。handoff web 本身是相对 `/api`，但开发态站点身份仍是 `localhost:5173`（vite.config.ts Host/WS Origin）。
- 弃选端口映射：本机 listen 同号会抢端口。
- 弃选像素投影：人点的体验差；agent 已有 SuperDev。
- 选定网络投影：隔离 Chromium + PAC/SOCKS，投影 owner loopback 整段；`--via` 会话级加内网/域名。
- Chromium 必须独立 user-data-dir，且 `--proxy-bypass-list=<-loopback>`（Chrome 默认 localhost 不走代理）。
- 执行机不拨笔记本。通知 = 协调者订阅，与任务镜像同形。
- 多桌面：不自动弹窗；左栏显示，点哪台在哪台开。
- 左栏：B288 之后挂对应项目「任务」组，第四种 kind；点击独立窗口，不进右侧 tab / 标签组。
- 不另 fork 原型：沿 option-1 `TaskRow`。

## 现状探针

- `rg preview` 在 Go/TS 实现中无预览会话（08-09 设计未落地）。
- `OpenItem.kind` / `TaskRow.kind` 闭集 `tui | terminal | file`（ProjectTree.tsx）。
- 项目进行中计数 = `countsForProject` 的 running+pending，不含预览。
- ADR-0009 薄壳只 loadURL。
- 任务镜像：`internal/agentd/mirror.go` 协调者拉远端。

## 批准

- 2026-08-29 用户原话「批准」。头部回写已批准；下一步挂 `spec:` 附件、`move contract`、note 定级 L3 轻档。

## 定级

- L3 轻档。跨 d_cli / d_gateway / d_transport / d_protocol / d_web。web 只消费契约。若 breakdown 发现单子系统超过固定成本，迁重档不重建。

## 未写进 spec 正文的实验念头（防重走）

- Service Worker 拦截 `localhost` fetch：拦不住 WebSocket，且改不了站点 Host 身份。
- 给整扇 Wails 控制台套 SOCKS：会把本机 agentd 的 `127.0.0.1:7777` 一并拐走。
- CLI 成功等 Chrome ack：与「多桌面点击开窗」冲突，已改为会话落库即成功。

## contract 节点查证台账

- 2026-08-29：本轮入口 `git status --short --branch && git log -1 --oneline` 实得工作树干净，分支 `cards/B294-charter-2`，HEAD `5e8826f7 docs(B294): spec 远端预览网络投影与左栏第四种任务行`；spec 头部已是「已批准」。
- 2026-08-29：按平台规定尝试 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --help`；原始失败为 `go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.10.0.mod312105863.tmp: read-only file system` 与同目录 lock 的 `read-only file system`；`codegraph` 命令未安装，故本轮图查证使用仓内既有 `codegraph/{best,target}.json` 与 `rg/jq`，未宣称图命令校验通过。
- 2026-08-29：现状 `internal/agentd/server.go#Server.Handler` 已注册 `/api/*` 与 `/ws/events`、`/ws/pty`，无 preview 路由；`internal/agentd/mirror.go#Mirror` 现只按 `task_id` 订阅 `proto.Event`，preview 将以独立 `PreviewEvent` 镜像接口落壳，不改任务 Hub 语义。
- 2026-08-29：现状 `internal/client/client.go#Client.StreamEventsOnce` 是单连接、调用方持水位、零读超时；`NewWithWSTiming` 的 HTTP transport 拨号超时来自包变量 `dialTimeout=10s`，`HTTPClient().Timeout=0`；preview 事件接口沿用此连接边界，重连与未关闭列表 catch-up 归 agentd 镜像实现。
- 2026-08-29：依赖源码实查 `/root/go/pkg/mod/github.com/hashicorp/yamux@v0.1.2/mux.go#DefaultConfig`：`AcceptBacklog=256`、keepalive 开启且间隔 30s、连接写超时 10s、流关闭超时 5m、流打开超时 75s；`/root/go/pkg/mod/github.com/coder/websocket@v1.8.15/read.go#Conn.SetReadLimit` 及 `ws_js.go` 默认单消息读限为 32768 字节。契约不扩大这些默认值；agentd 既有 WS 转发预算为 10s 拨号与 512KiB 读限。
- 2026-08-29：对侧常量执法：spec 的 `preview.created` / `preview.closed` 当前实现零使用，属于本轮新冻结字面值；Ticket 0 只定义常量与 DTO，不宣称已有生产者/消费者。
- 2026-08-29：架构判断：沿用现有平铺领域包，不新开 subsystem；owner/coordinator 的 preview 读写与镜像落在 `d_gateway`，协议 DTO 在 `d_protocol`，targetclient/WS/SOCKS 实现依赖留给 `d_transport`，CLI/web 仅持对应镜像。
- 2026-08-29：Ticket 0 先后落入 `internal/proto/preview.go`、`internal/client/preview.go`、`internal/agentd/preview.go`、`cmd/preview.go`、`web/src/api/preview.ts` 与类型/fixture 镜像；agentd handler 统一 503，运行态 owner/Chromium/SOCKS 未实现。
- 2026-08-29：`go test ./internal/proto ./internal/client ./internal/agentd ./cmd` 首次组合运行在 `cmd` 的 `TestRepoContractGate` 失败；原始 dead-entry 为新增 `proto.Preview*` 不存在于基线 d_protocol。按闸门语义移除 target 具体新 DTO entries，改由本分支 diff 覆盖，保留基线 `proto 实体` 与 transport entries。
- 2026-08-29：修正 target 后 `GOMODCACHE=/root/.handoff/tmp/50d413fc/gomodcache go test ./cmd -run '^TestRepoContractGate$' -count=1 -v` 输出 `--- PASS: TestRepoContractGate`、`ok github.com/Xsxdot/handoff/cmd 0.037s`。
- 2026-08-29：`npm ci --ignore-scripts` 使用 `web/package-lock.json` 在工作区安装 290 packages，`found 0 vulnerabilities`；随后 `npm run typecheck` 退出 0，`npm test -- --run src/api/contract.test.ts` 输出 `Test Files 1 passed`、`Tests 39 passed`。
- 2026-08-29：`jq empty codegraph/best.json && jq empty codegraph/target.json && jq empty codegraph/diffs/cards-B294-charter-2.json && git diff --check` 退出 0；手动图文件语法通过，官方 codegraph 命令仍因依赖缓存只读未验证。
- 2026-08-29：契约文档、Ticket 0 骨架、fixture、目标图/结构图与分支视图 diff 已创建冻结提交；未 push，交棒 breakdown。
