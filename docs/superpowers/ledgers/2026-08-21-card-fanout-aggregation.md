# 子卡扇出聚合闸与递归护栏执行账本

- 2026-08-21：Task 1 双裁决通过；spec 符合：新增 `Gate.RequireChildrenDone`，父卡转移到目标节点前在同一事务检查直接子卡，已完成/终止均算完结，无子卡空洞放行，拒绝错误包装 `ErrGateBlocked` 并点名未完结子卡；代码质量：拒绝路径 Warn 带 `card/to/pending`，查询使用 `s.q`，中文 why 注释完整，`go test ./internal/ledger/` 与 `gofmt -l internal web 2>/dev/null` 通过。Task 1 commit 范围：`internal/ledger/types.go`、`internal/ledger/move.go`、`internal/ledger/move_children_gate_test.go`、本 ledger。
- 2026-08-21：Task 2 双裁决通过；spec 符合：`CardView` 新增 `children_total/children_done`，只统计直接子卡，完结语义统一为已完成或终止，统计为查询期派生不落列；代码质量：全量状态表避免列表过滤漏计，父子查询使用 `s.q`，失败带“读父子表”上下文，中文 why 注释完整，`go test ./internal/ledger/` 与 `gofmt -l internal web 2>/dev/null` 通过。Task 2 commit 范围：`internal/ledger/types.go`、`internal/ledger/derived.go`、`internal/ledger/derived_children_test.go`、本 ledger。
- 2026-08-21：Task 3 双裁决通过；spec 符合：建子卡后父卡 timeline 新增 `EvComment`，payload 含普通 kind、子卡 id/title body 与 refs，且与子卡写入同一事务；代码质量：复用既有事件类型与 `appendEvent`，中文注释说明原子性原因，`go test ./internal/ledger/` 与 `gofmt -l internal web 2>/dev/null` 通过。Task 3 commit 范围：`internal/ledger/cards.go`、`internal/ledger/cards_test.go`、本 ledger。
- 2026-08-21：Task 4 双裁决通过；spec 符合：新增固定 `maxWorkflowNesting=3`，父链从父卡起统计同名工作流，含新卡的第 4 层包装 `ErrBadState`，异名工作流放行；代码质量：父链 64 层环护栏，拒绝 Warn 含 `parent/workflow/nesting`，错误给出竖切域或更细粒度工作流指引，`go test ./internal/ledger/` 与 `gofmt -l internal web 2>/dev/null` 通过。Task 4 commit 范围：`internal/ledger/cards.go`、`internal/ledger/cards_test.go`、本 ledger。
- 2026-08-21：Task 5 双裁决通过；spec 符合：Web 契约新增 `children_total/children_done` 与 `require_children_done`，NodeEditor 勾选写 true、取消清除字段；代码质量：沿用实际 NodeEditor props、id/label 和 `updateGate` 清理模式，测试注释说明 `omitempty` 原因，专项测试 1/1、flows 回归 8/8、`tsc --noEmit`、`gofmt -l internal web 2>/dev/null` 通过。Task 5 commit 范围：`web/src/api/ledger.ts`、`web/src/app/flows/NodeEditor.tsx`、`web/src/app/flows/NodeEditor.childrengate.test.tsx`、本 ledger。
- 2026-08-21：Task 6 双裁决通过；spec 符合：仅 `children_total > 0` 渲染 `⧉ 子卡 done/total`，无子卡不渲染，标题说明终止也算完结；代码质量：复用既有 `Chip`，未改看板布局或交互，cards 回归 44/44、`tsc --noEmit`、`gofmt -l internal web 2>/dev/null` 通过。Task 6 commit 范围：`web/src/app/cards/CardItem.tsx`、`web/src/app/cards/CardItem.children.test.tsx`、本 ledger。
- 2026-08-21：Task 7 双裁决与整分支终审通过；相对计划起点 `2bf42866` 的完整 diff 无超范围项、无 instrumenting 清单遗漏、无需修补；Go build/vet、`internal/ledger` 与 `internal/agentd` 测试、`gofmt -l cmd internal`，以及前端 98 个测试文件/973 个测试与 `tsc --noEmit` 均通过。Task 7 commit 范围：本 ledger。
