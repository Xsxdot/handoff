# B233.15 spec 台账（2026-09-11）

节点：spec（本地会话，协调者侧）。产物：`docs/superpowers/specs/b233.15.md`。
基线：功能线 `cards/B233.1-charter-7`。本文件记实验过程与原始读数；结论进 spec 正文。

## 0. 会话起点读数

- 卡 `B233.15` 已在 `spec` 列（11:32 移入），note 记基线 `cards/B233.1-charter-7 @ 4485a4e7`、
  「分级：普通模型」「L2 或 L1」「只把 git 实现从 agentd 迁进 internal/workspace，Capability 生产实现
  不再转调同包函数。不改 Prepare/Diff/回收行为。不碰审批/编制/Manager 迁包。」
- 本工作树 `git log -1` = `5e9be36b`；`git merge-base --is-ancestor 4485a4e7 HEAD` → **NO**（卡上基线过期）。

```
$ git fetch --quiet; git log --oneline -1 origin/cards/B233.1-charter-7
1f848057 docs(B233.13): spec 已批准，进 contract
$ git rev-list --count origin/cards/B233.1-charter-7..HEAD   → 0
$ git rev-list --count HEAD..origin/cards/B233.1-charter-7   → 54
$ git merge --ff-only origin/cards/B233.1-charter-7
Updating 5e9be36b..1f848057  Fast-forward  82 files changed
```

ff 后核对：`internal/agentd/workspace*.go`、`internal/workspace/` 在 54 个新提交里**零改动**
（`git diff --stat 5e9be36b..HEAD -- <这些路径>` 只命中 `codegraph/*.json`：baseline 重扫、
best.json +2（`k_approval_fn`/`k_approval_model` → d_orchestration）、target.json +14 行注记）。
故 §1 的源码读数在 ff 后依然成立。

## 1. 现状读数（源码）

| 读数 | 命令/出处 |
|---|---|
| git 实现主体 | `internal/agentd/workspace.go` 2838 行、73 个 `func` |
| 同族实现文件 | `manualworktree.go` 201 行、`workspaceprobe.go` 212 行、`gitroot.go` 68 行、`gitignore.go` 96 行 |
| 运行时原语 | `workspace_procgroup_unix.go` / `workspace_procgroup_other.go`（`RunCmd` 进程组） |
| HTTP 层 | `workspacefiles.go` 531 行（8 个 handler，判断力在被调函数） |
| Capability 适配器 | `internal/agentd/workspace_capability.go#gitCapability`：`NewGitCapability` 返回它，方法体逐个回调本包函数 |
| 契约面 | `internal/workspace/capability.go#Capability`（7 方法）；`identity.go`、`retain.go` |
| 迁出文件的 import | `prochost`（`quotaNote` → `ExplainForkFailure`）、`proto`、`proxycfg`（`gitNetArgs`/`redact`）、`workspace`（自引） |
| 组装注入点 | `cmd/agentd.go:219` `mgr.SetWorkspace(agentd.NewGitCapability())`；`internal/agentd/server.go:374` 同调用 |
| 代理注入 | `cmd/agentd.go:168` `agentd.SetGitProxy(cfg.Proxy)` → `internal/agentd/workspace.go#SetGitProxy` → `gitNetArgs` |
| 跨包消费（迁出符号） | `cmd/agentd.go:431` `agentd.RunCmdTimeout`；`cmd/project.go:76` `agentd.MainWorktreeRoot` |

包内（非测试）调用面（脚本按「定义符号集 ∩ 各文件 `(^|[^.\w])Sym\(`」统计，排除迁出文件自身）：

```
admission.go     checkProcHeadroom                  log
bundle.go        BundleRange,widenEmptyRange        gitProbe,gitRun,log
env.go           handleEnvFileRead/Write            shortHash
manager.go       Dispatch,compensateWorkspace,deleteCreatedBranch  branchTip,gitRun,resolveDefaultBaseBranch,resolveDispatchBase
preview_owner.go defaultPreviewWorkspaceResolver    gitProbe,log
projectadmin.go  probeProjectStatus,projectOriginURL,cloneAndRegisterProject,cloneToPathAndRegister,
                 inspectRepoDir,handleProjectBranches   Branches,EnsureRepoUsable,MainWorktreeRoot,ManualWorktreeRoot,
                                                        gitRun,gitRunNet,probeWorkspaces,resolveBaseBranch
projectresolve.go resolveProject                    log
projecttree.go   buildLocalTree                     probeWorkspaces
reclaim.go       classifyWorktree,repoWorktrees,Reclaim   gitRun,log
runshell.go      resolveRunShell                    log
server.go        handleTaskBranches,Diff,File,Run   Branches,RunCmd,diffBaseFor,manualBranchExists,resolveBaseBranch,
                                                    taskDiffTarget,truncatedNotice   ← assembly 豁免
workspacefiles.go 8 个 handler + resolveWorkspace   CopyEntry,CreateEntry,DeleteEntry,ListDir,ReadFile,RenameEntry,
                                                    SearchInDir,WriteFile,markIgnored,probeWorkspaces,shortHash
```

agentd 测试侧引用（行数）：`workspace_test.go` 147、`workspace_write_test.go` 18、`diffbase_test.go` 11、
`manager_test.go` 9、`workspacefiles_test.go` 7、`manualworktree_test.go` 6、`workspaceprobe_test.go` 6、
`gitprobe_test.go` 5、`workspace_capability_test.go` 5、`workspace_identity_contract_test.go` 5、
`workspace_read_struct_test.go` 5、`gitignore_test.go` 4、`gitroot_test.go` 4，其余 ≤3。

## 2. 图读数（codegraph）

```
$ codegraph check        → fails: 0，warns: 147
   其中 warn 前两条为 anchor-off-domain：
     d_workspace 声明锚 internal/agentd/workspace.go#PrepareWorkspace / #RemoveManagedWorktree
     实际属于 d_coordination_task
   best-dangling：k_workspace_fn、k_workspace_model（best.json 有容器，当前视图无节点）
$ codegraph sym MayRecycle   → 不在图中
$ codegraph sym ID8          → 不在图中（候选只有 n_agentd_id8 / n_cmd_id8）
```

- 归属：`codegraph/best.json` `k_agentd_fn → d_orchestration`；`k_agentd_Mirror`、
  `k_agentd_projectIndex`、`k_agentd_runOutputBuffer`、`k_agentd_DirtyWorktreeError` → `d_workspace`；
  `k_workspace_fn`/`k_workspace_model` → `d_workspace`。
- 预算：`d_workspace→d_protocol` 3（3/3 用满）、`d_workspace→d_orchestration` 2（2/2）、
  `d_orchestration→d_workspace` 19（现状 18）、`d_gateway→d_workspace` 1（1/1）、`d_cli→d_workspace` 9（9/9）。
- 域声明：`codegraph/domains/d_workspace.json:23-24` lifecycle 锚指 agentd 两个符号。
- 扫描配方：`docs/codegraph-scan-recipe.md`（增量写 `codegraph/diffs/<视图名>.json`，
  基线归 absorb、`target.json`/`best.json` 归 contract）；现有视图 `codegraph/diffs/cards-B233.*-charter*.json`。
- **图覆盖债**：`internal/workspace` 整包今天不在 baseline（`MayRecycle`/`ID8` 均未命中），
  本分支视图 diff 必须新建这批判号，属产文档节点/图对账的活。

## 3. 决策记录（用户裁决，2026-09-11 面板）

1. 迁出范围 = **整包实现迁出**（workspace.go + manualworktree.go + workspaceprobe.go + gitroot.go +
   gitignore.go + procgroup 两文件 + 对应测试）；agentd 留 HTTP handler 与编排/登记/回收/镜像消费点。
2. 依赖面 = **proto 直引**（父卡机制「类型跨缝走词表包直引，d_protocol 豁免」，棘轮预算并修订
   B233.4 冻结第 14 条）；**proxycfg / prochost 走组装点注入**，不新开方向。
3. 消费侧 = **按使用方语义收具名能力**；`gitRun` / `gitProbe` / `gitRunNet` 等原语不出包。

## 4. 复核命令（供 contract 复跑）

```
git merge-base --is-ancestor 4485a4e7 HEAD          # 基线是否仍在线上
grep -c '^func ' internal/agentd/workspace.go       # 73
codegraph check                                     # fails 数（基线 0）
codegraph sym MayRecycle ; codegraph sym ID8        # 图覆盖债是否已补
```
