# B233.28 breakdown 台账

上游：`docs/superpowers/specs/b233.28.md`（头部「已批准」已核实，未重写）；`docs/superpowers/specs/b233.28-contract.md`（头部「冻结状态：随本提交冻结」、C 裁决记录 §0 已核实）。
基线：`cards/B233.1-charter-7` @ `56c7a9c77`（`git merge-base --is-ancestor 56c7a9c7 HEAD` 退出 0，确为本分支祖先）。本分支：`cards/B233.28-charter-4`。上游冻结提交：`41741fed`。
本节点：breakdown（出稿，不拍板）。产出物：`docs/superpowers/specs/b233.28-breakdown.md`。

- 2026-09-14：`go build ./...` → 原始输出为空，退出码 0。
- 2026-09-14：`go test ./internal/agentd -run 'TestAttachEmptyListsAreArrays|TestReplyAnswersTicketAndNotifies|TestReplyUnknownTicket404' -count=1` → 原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 0.254s`，退出码 0。
- 2026-09-14：`go test ./internal/orchestration/ -count=1` → 原始输出 `ok github.com/Xsxdot/handoff/internal/orchestration 44.022s`，退出码 0。
- 2026-09-14：`go test ./internal/agentd ./cmd/... -count=1` → 原始输出 `ok github.com/Xsxdot/handoff/internal/agentd 141.728s` / `ok github.com/Xsxdot/handoff/cmd 43.432s`，退出码 0。
- 2026-09-14：`codegraph --repo . check --view cards/B233.28-charter` → `fails: []`、`legacyHits["d_gateway->d_orchestration"]=76`、`bestCoverage {assignedContainers:338, viewContainers:338, crossDomainEdges:1248, misplacedSkipped:0}`。
- 2026-09-14：`codegraph --repo . validate --view cards/B233.28-charter` → `nodes:5299 issues:null`（0 issue）；不带视图 `validate` 同为 `nodes:5299 issues:null`。注意：`--view cards/B233.28-charter` 显式子目录视图未出现在 `views` 列表（列表只列 `diffs/*.json` 顶层 7 个），但 check/validate 仍按其解析认到新增符号（见下条 sym 证据）。
- 2026-09-14：`codegraph --repo . sym OrchestrationClient --view cards/B233.28-charter` → `m_orchestration_OrchestrationClient`、`status: modified`、含 15 个新增方法；`sym Manager.ListTasks --view cards/B233.28-charter` → `n_orchestration_Manager_ListTasks`、`status: added`。Ticket 0 视图符号已生效。
- 2026-09-14：`codegraph --repo . resolve --doc docs/superpowers/specs/b233.28-contract.md` → `anchors 19 bad 0`，退出码 0。
- 2026-09-14：现状 `s.st` 直打全量清点（`grep -n 's\.st\.' --include='*.go' internal/agentd`，60 命中）：任务/项目面命中与 contract §1.1/§1.3 所列**逐条对齐、无遗漏无新增**——`handlers.go` ListTasks:203、GetTask:254/:933/:939/:956/:988/:1005/:1120/:1536、PendingTickets:265/:469、EventsFrom:271、GetTicket:365、AnswerTicketApplied:382、AppendEvent:403、UpdateTaskState:477、EventsFromAsc:1641、LatestEvent:1658、CountEvents:1740、MirrorTaskTarget:1540、MirrorEventsFrom:1639、resumeIfIdle:461；`tasksfanout.go` ListTasks:28、ListMirrorTasks:40；`taskroute.go` GetTask:34、MirrorTaskTarget:42；`taskplan.go` GetTask:43；`roomsapi.go` ListTasks:109/:403、PendingTickets:413、EventsFrom:459；`update.go` ListTasks:289；`projectadmin.go` GetProjectLocationByName:87/:166/:246、UpdateProjectLocation:136；`codegraph.go` GetProjectLocationByName:33/:144；`coordapi.go` ListProjectLocations:264；`workspacefiles.go` ListProjectLocations:101。`handlers.go:189 workspace.LoadProjectIndex(s.st)` / `projecttree.go:28 workspace.BuildLocalTree(ctx, s.st, …)` 是经 workspace 窄接口的注入，**不在** move 清单（contract 未列），保持现状。
- 2026-09-14：`grep -rn "ErrTicketAnswerUnwired"` → 仅 `internal/orchestration/facade.go:26/:30/:102/:106` 四处包内自引用，gateway 生产零引用（空壳期无人调）。implement 落地后删除无外部破坏。
- 2026-09-14：`grep -rn "HandleReply\|resumeIfIdle\|AnswerTicketApplied" --include='*_test.go'` → 既有 reply 断言分布在 `internal/agentd/server_test.go`（420/423/495/505/1122 附近）与 `internal/orchestration/regression_round2_test.go`、`watchdog_test.go:180/:195`。
- 2026-09-14：夹具盘点——`newTestEnv`（`server_test.go`，外部包 agentd_test）被 19 个用例使用、`newTestAgentdEnv`（`w3a_testhelpers_test.go`，内部包 agentd）被 27 个用例使用；两者默认**不注入 manager**。其中 `server_test.go` TestAuthRequired（GET /api/tasks 期望 200）、TestAttachEmptyListsAreArrays（GET /api/tasks/{id} 期望 200）与 `taskplan_test.go`、`tasksfanout_test.go`、`roomsapi_test.go`、`codegraph_test.go`、`taskroute_test.go`、`mirrorws_test.go`、`diffbase_test.go:232`、`workspacefiles_test.go` 命中的 handler 在 move 清单内。这是拆解发现的最大文件集不确定性，落 breakdown P-1（待拍板）。
- 2026-09-14：`grep -n "func (m \*Manager) ListTasks|GetTask|…" internal/orchestration/facade.go` → 15 个方法签名与 contract §2 冻结形状逐字一致；`grep Manager struct` 字段含 `st *store.Store`、`hub *Hub`、`log *slog.Logger`、`appendEvent appendEventFunc`，`AnswerTicket`/`ResumeIfIdle` 行为可在包内落。
- 2026-09-14：**放弃的尝试**：曾试图 `ls $(go env GOMODCACHE)/github.com/` 探测 cache 以定位 charter 图工具源码，被协调者权限拒绝（原因：禁止列 GOMODCACHE）。随即改用已安装 `codegraph` 二进制完成全部图读数，未再发起同类请求，未考古 charter 源码。本行如实记录。
- 2026-09-14：`codegraph --repo . resolve --doc docs/superpowers/specs/b233.28-breakdown.md`（不带视图）→ 原始报错 `Error: 文档锚点检查失败: internal/orchestration/facade.go#Manager.AnswerTicket (vanished)`，退出码非 0——根因：facade.go 是 Bash 卡 Ticket 0 才落的新文件，基线图未收录其符号，须叠加本分支视图。
- 2026-09-14：`codegraph --repo . resolve --doc docs/superpowers/specs/b233.28-breakdown.md --view cards/B233.28-charter` → `anchors 15`、其中 14 `ok` + 1 `moved`（`internal/agentd/roomsapi.go#Server.handleInbox` 行号漂到 379，file#Symbol 锚已再锚定），退出码 0。坏锚 0（`moved`≠坏锚）。
