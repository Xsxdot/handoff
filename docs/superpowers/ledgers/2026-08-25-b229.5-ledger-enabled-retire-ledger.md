# B229.5 · T5 LedgerConfig.Enabled 退休（账本变必需）执行台账

分支 cards/B229.5-charter，基线 cards/B229-charter-2（已验证为 HEAD 祖先：`git merge-base --is-ancestor` → ok）。

## 事实与命令流水（边干边追加）

- 基线确认：`git merge-base --is-ancestor cards/B229-charter-2 HEAD` 退出 0。
- 现状 grep：`Ledger.Enabled` 生产命中三处——cmd/agentd.go:248、cmd/ledgercli.go:30、cmd/status.go:86；测试文件另有引用（保留不属验收范围）。
- contract §2.6 决定逐字读毕：struct 字段与 yaml 键保留、值忽略、加载 Warn「ledger.enabled 已退休：纪律块入库后账本是必需品」、三消费点改恒开、DSN 语义不动。

### 红→绿 1（config Warn）
- 先写 `TestLedgerEnabledRetiredWarnsOnce`（三子例：false 恰一条 / true 同样告警 / 无键零告警），首跑红：
  `config_test.go:803: 「ledger.enabled 已退休」Warn 应恰一条，实得 0`——断言红，非 typo。
- 绿：config.go Load 的 decodeStrict 成功后加指针探测（`*bool` 区分「键存在」与显式 false），按键存在性发 `Warn("ledger.enabled 已退休…")`；Enabled 字段注释改退休说明。
- `go test ./internal/config -count=1` → ok。

### 红→绿 2（CLI openLedger）
- 新增 `TestOpenLedgerIgnoresRetiredEnabledFlag`（显式 enabled=false 直呼 openLedger 应得可用库）；同批翻转旧语义钉 `TestLedgerDisabledByDefault` → `TestLedgerAlwaysOnAfterRetirement`（未配 ledger 段 card add 照常成功 + 落 ledger.db）。首跑双红：
  - `ledgercli_test.go:146: enabled 已退休，openLedger 应恒开库: 账本未启用…`
  - `ledgercli_test.go:171: 退休后 card add 应照常执行，实际报错: 账本未启用…`
- 中途修正一次：card add 在空账本会因「无工作流」合法失败——改用 runLedgerCLI 基座（预写无 ledger 键的 config.yaml，基座只缺才代写）借其 bug 工作流种子。这是测试夹具问题，不是生产实现回退。
- 绿：删除 ledgercli.go openLedger 的 Enabled 闸与「log/slog」孤儿 import。
- 定向测试 ok。

### 红→绿 3（agentd 启动路径）
- 新缝 `setupLedger(cfg, srv, ctx, logger) (stop func(), err error)`：
  - 首红=编译红：`undefined: setupLedger`（新缝允许）；
  - 空壳 `return nil, nil` 后断言红：`agentd_ledger_test.go:50: stop 函数不得为 nil`；
  - 实现后绿：TestSetupLedgerMountsWithRetiredEnabledFlag（enabled=false config → ledger.db 落 DataDir + /api/ledger/health 报 `"enabled":true`）。
- agentd RunE 原 if/else 分支（含 dsn 半配 Warn）整块删除，改为恒调 setupLedger + defer stopLedger()；Stop→Close 次序硬约束收进 stop 闭包。

### 红→绿 4（status 报账本健康）
- 新增 `TestStatusReportsLedgerHealthWithRetiredEnabledFlag`（enabled=false config + 真实 SQLite 种卡/挂账/认领驱动 + fake /api/status watchers:0）。首跑红：
  输出 `⚠ 无人值守`（闸在，lookup 被跳过），want 卡号+驱动者。
- 绿：删除 status.go 的 `if loadCLIConfig().Ledger.Enabled` 外层闸，恒尝试 openLedger（失败仍静默降级旧判据）。
- 定向测试 ok。

### 验收闸（原始结果）

1. 存量 yaml（`ledger:\n  enabled: false`）加载成功且恰一条退休 Warn：`go test ./internal/config -run TestLedgerEnabledRetiredWarnsOnce -count=1` → ok。
2. 三消费点恒开路径各有测试翻绿/新增绿：
   - agentd：TestSetupLedgerMountsWithRetiredEnabledFlag（enabled=false config → DataDir/ledger.db 落盘 + /api/ledger/health `"enabled":true`）；
   - CLI：TestOpenLedgerIgnoresRetiredEnabledFlag + TestLedgerAlwaysOnAfterRetirement（既有 TestLedgerDisabledByDefault 语义翻转）；
   - status：TestStatusReportsLedgerHealthWithRetiredEnabledFlag（账本报卡号+驱动者，不再「无人值守」）。
3. `grep -rn "Ledger.Enabled" --include="*.go" cmd/ internal/ | grep -v _test` → 零命中。首次自检曾命中探测变量 `retiredProbe.Ledger.Enabled`（config.go），改名 `LedgerCfg` 后归零——字段本体保留未删。
4. `go build ./... && go test ./internal/config ./cmd -count=1` → 两包 ok（cmd 7.2s）。`go vet ./cmd ./internal/config` 干净；gofmt 无差异。

### 变异自验（三次均先核命中唯一=1、编译过、再数红）

| 变异 | 打击点 | 结果 |
|---|---|---|
| Warn 改无条件（`!=nil` → 恒真） | config.go 探测条件 | 编译过；no-key 子例红（恰 1 FAIL）→ 已还原 |
| status lookup 条件取反（==nil） | status.go openLedger 判定 | 编译过；status 测试红 → 已还原 |
| 删 `srv.SetLedger(lst)` | cmd/agentd.go setupLedger | 编译过；health 断言红 → 已还原 |

### 收尾自审

- 错误分支带上下文日志：setupLedger 失败记 dsn+cause ✓；成功路径出口日志「账本镜像子系统已挂载」（补 dsn 字段）✓
- 新文件头注释（agentd_ledger_test.go）、setupLedger 全参数文档注释 ✓
- 触及包测试绿、全仓编译过、vet/gofmt 干净 ✓
- 未动 internal/agentd/ledgerapi.go（web health 端点的 enabled:false 分支属 nil-store 兜底，非本卡三消费点）；未动 web 前端；CHANGELOG 归 T7。
- 本回合零 handoff CLI 调用、零新 executor 进程 ✓

放弃的尝试：无（无修复熔断触发；一次测试夹具修正见红→绿 2，非生产实现回退）。



