# B233.4 breakdown 台账

日期：2026-09-07。节点：charter breakdown。工作分支：`cards/B233.4-charter-2`。开工 HEAD：`102a8d7a`。

## 过程

1. 形态 2：handoff executor 出稿，协调者拍板。不写实现、不建卡、不派发。法定产出 `docs/superpowers/specs/b233.4-breakdown.md`。L3 轻档；prompt「不要扇出子卡，除非拆解时证明单子系统工作量超过流程固定成本」。push 归属仍暂缓。
2. 上游状态位：spec 头部「已批准」（2026-09-06，用户原话「按这个推进吧」）。契约头部原写「冻结状态：随本提交冻结」，缺日期+提交号。HEAD=`102a8d7a` 即 contract 提交。拆解期按 b298/b233.1 惯例把状态行补钉为「已冻结（2026-09-07，提交 102a8d7a）」，冻结正文不改，澄清写入新 §12。
3. 查图：`codegraph --repo . --view cards-B233.4-charter`。`codegraph domains` 无 view 时出现 `d_coordination`（基线视图），与 best 顶层域不一致——清单以 `codegraph/best.json` 顶层领域为准（`parent` 空 + `type`）。`Capability` 决议 `internal/workspace/capability.go` 容器 `k_workspace_model` 域 `d_workspace` `anchor=ok`。`PrepareWorkspace` 仍 `d_coordination_task`（图覆盖债，本卡不修）。
4. `codegraph --repo . --view cards-B233.4-charter check`：`fails []`。`d_orchestration->d_workspace` 预算内直调 **18/19**。warn：`d_workspace` 声明锚 `PrepareWorkspace`/`RemoveManagedWorktree` 实际属 `d_coordination_task`（与契约 §10 一致）；`internal/agentd` oversized-package 83 图内源文件；前缀族 `work*`=6、`preview*`=10、`project*`=5。
5. 架构法第三条实读（非测试源）：`internal/agentd` **89** 文件 / **28308** 行（命中「单包 ≥40 且无子包」与项目红线 20000 行）。`work*` 源文件 6（`workspace.go`/`workspacefiles.go`/`workspaceprobe.go`/`workbench_api.go`/`workspace_procgroup_unix.go`/`workspace_procgroup_other.go`）。`internal/workspace` 464 行空壳。`workspace.go` 2844 行。本卡能圈出有界文件集，不插竖切还债卡。
6. `who-calls PrepareWorkspace`：`Manager.Dispatch`、`PrepareBranch`。`who-calls RemoveManagedWorktree`：`Done`/`Stop`/`compensateWorkspace`/`rollbackWorkspace`。`who-calls ResolveBaseline`：`Dispatch`。`who-calls CreateManualWorktree`：`Server.handleProjectWorktreeCreate`（`POST /api/projects/{name}/worktrees`）。基线仍有 5 个未扫描入口。
7. 代码事实（本轮读源，非行为验收）：
   - `Manager.Dispatch` 仍调包级 `ResolveBaseline` + `PrepareWorkspace`；`BaseCommit: start` 随 `CreateTask` 写入。
   - `Manager.Stop` 在 managed 且 WorkDir 非空时调 `RemoveManagedWorktree`，成功则 `worktreeRemoved=true`。`waiting_review` 不是终态，Stop 不拒。
   - `Manager.Done` 在 `waiting_review`→`completed` 后删 managed 树。
   - `MayRecycle` 纯函数已锁 stop=keep / done+终态=recycle；生产路径未调用。
   - `CreateManualWorktree` 零引用 `CardIDs`；挂卡在 `handleProjectWorktreeCreate` 成功之后 `attachCardBaseBranches`。`forwardWorktreeIfRequested` 仍本端挂卡（归 233.3，本卡不改 `forward.go`）。
   - `taskDiffTarget`：树在用 WorkDir+HEAD；树不在用 RepoPath+Branch；不得用主仓 HEAD。既有 `diffbase_test.go`。
   - `gitExec`：`exec.CommandContext` + `-C repo` + `-c core.hooksPath=<MkdirTemp 空目录>`，不拼 shell。
   - 旧测试锁的是今日行为：`TestStopRemovesManagedWorktree` / `TestStopReportsWorktreeRemoved` 要求 stop 后目录消失且 `worktree_removed=true`。补偿路径 `TestDispatchFailedAfterWorkspaceCleansManagedWorktree` 仍应绿。
   - CLI `cmd/stop.go`：`removed==true` 打印「managed worktree 已删除」；false 打印「分支与 worktree 保留」。
   - Web `web/src/app/task/Composer.tsx`：`worktree_removed` true→「工作树已删除」；false→「用户自带 / 原地 / 或清理失败」。Stop 改留存后 false 不再等于清理失败。
   - `NewManager` 无 `workspace.Capability` 字段。生产 `internal/agentd` 非测试文件未 import `internal/workspace`（仅 `workspace_identity_contract_test.go`）。
8. 不退回 contract：未发现必须新接缝。P4-(b) 给 Task 加结果 commit 字段才退回。RunCmd/ListDir/在线编辑/文件树进 Capability 会加方法，退回；本卡不进。
9. 不扇出子卡。单子系统工作量未超过 split+integrate 固定成本；接缝（Stop↔MayRecycle、挂卡在应用）必须同一上下文看见。内部 ticket T1–T6 是单卡执行顺序，不是 `card split`。
10. 法定产出写到 `docs/superpowers/specs/b233.4-breakdown.md`。契约追加 §12 修订记录 C-0…C-14。
11. `codegraph --repo . --view cards-B233.4-charter resolve --doc docs/superpowers/specs/b233.4-breakdown.md`：exit 0，32 锚，`ok` 25 / `moved` 7，坏锚 0。`resolve --doc docs/superpowers/specs/b233.4-contract.md`：exit 0。
12. 待拍板岔口 P1–P5 集中在稿首。真机清单 13 条。未调用 handoff CLI、未起 executor、未写实现代码。

## 命令读数

- `find internal/agentd -name '*.go' ! -name '*_test.go' | wc -l` → `89`
- `find internal/agentd -name '*.go' ! -name '*_test.go' | xargs wc -l | tail -1` → `28308 total`
- `wc -l internal/workspace/*.go` → `464 total`；`internal/agentd/workspace.go` `2844`
- `codegraph --repo . --view cards-B233.4-charter check`：exit 0，`fails []`；legacy `d_orchestration->d_workspace` 18/19
- `codegraph --repo . --view cards-B233.4-charter who-calls PrepareWorkspace`：Dispatch、PrepareBranch
- `codegraph --repo . --view cards-B233.4-charter who-calls RemoveManagedWorktree`：Done、Stop、compensateWorkspace、rollbackWorkspace
- `codegraph --repo . --view cards-B233.4-charter sym Capability`：`m_workspace_Capability` domain=`d_workspace` anchor=ok

## 判断

- L3 轻档单卡直进成立：有界文件集写得出；扇出会把 Stop↔MayRecycle 接缝拆到两张卡。
- 不退回 contract：无新 HTTP、无 Push、无 card_ids 进 Capability。
- 本卡主行为变化是 Stop 留存；机内旧测试会从「删树」改锁「留树」，否则假绿/假红。
- agentd 89 文件/28308 行命中升格判据，回答为「本卡圈得出有界集，不插竖切」。

## 提交

```
git commit -m "docs(B233.4): 拆解工作区与 Git 能力独立"
[cards/B233.4-charter-2 b565092b] docs(B233.4): 拆解工作区与 Git 能力独立
 3 files changed, 495 insertions(+), 1 deletion(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.4-breakdown-ledger.md
 create mode 100644 docs/superpowers/specs/b233.4-breakdown.md
```

随后 amend 一次把本段收进同批提交。不回填 amend 后 HEAD。

## 拍板

2026-09-07 协调者：P1–P5 全选 (a)。理由见拆解稿表「裁决」列。新提交，不 amend 出稿提交。
