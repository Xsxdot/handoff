# B298 spec 台账（2026-08-29）

- 卡：B298，project=handoff，priority=高，charter v9。建于待办；用户当面「开B298」后 `move spec`。
- 基线读数：本工作树 `1fa4668b`。linux-01 / 本机 `du` 同日。
- 来源：用户 2026-08-29 当面。handoff 工作树垃圾曾占盘 80%+，刚手清过，要产品级清理。同日 B297（全局规则同步）搁置。

## 对话拍板（按时间）

- 清的对象：终态任务可再生缓存（旧 `tasks/<id>/tmp` + 新 `tmp/<id8>`）+ 残留 managed 工作树。
- 不动：`render.log` / 整棵任务目录 / SQLite / 分支 / 用户自建树 / `agentd.log`。
- 不做前端：不在设置页加清理分区，也不改开发机详情。用户问「设置页要不要加清理页面」；按 B160 归属判据否决，用户同意。
- 时机：任务 `done`/`stop` 当时丢掉该任务缓存 **并且** 提供 `handoff gc`（默认预览，`--yes` 才删）拖历史。用户第一轮「同意」即覆盖整包；后续把「结束时删除」又问了一遍，属协调者把范围和时机拆成两问，不是新决议。
- 不复用 `reclaim` 命令名：B77 契约是只收工作树。
- 脏树：`--force` 语义与 `reclaim --force` 相同；默认跳过。缓存删除不依赖 `--force`。

## 现状探针

- 本机 `~/.handoff` 11G：tasks 7.7G（缓存 ~7.9G / 其余 0.43G）、worktrees 1.8G、tmp 810M、repos 682M、agentd.log 176M。
- linux-01 `/root/.handoff` 150G：tasks 81G、tmp 67G、repos 1.8G、worktrees 892M、agentd.log 297M。整盘 251G 用 170G（68%）。623 个任务目录。最肥一条 2.3G 几乎全是 `tmp`，`render.log` 2.6M。
- `handoff reclaim --target linux-01`：620 个体检、21 棵残留，多数脏。
- 本机 reclaim：162 个体检、2 棵净残留（`f22ed520`、`37811842`）。名叫 `b156-23-acc` 等树不在 reclaim 表里。
- 最肥本机任务 `tasks/<id>/tmp/gocache` 1.3G — 旧布局；现役代码写 `tmp/<id8>`。

## 定级

- 初稿 L3 轻档。
- 用户问「为什么是 L3」后一度改判 L2（误用 B286「不新增 HTTP」+ 把实操裁决当降级）。
- 独立审查 C1（`docs/superpowers/reviews/b298-spec-review.md`）不接受 L2：定级两问跨子系统且动契约；B286 留 L2 因**不新增 HTTP**；不扇出 = 轻档，轻档不许跳契约冻结。同日 B294 才是形状匹配先例。
- 用户 2026-08-29「按审查的来」。回 **L3 轻档**。
- 用户 2026-08-29 原话「批准」。spec 头部回写已批准。

## 审查吸收（2026-08-29）

独立审查总判「修订后再批」。Critical 1 / Important 3 / Minor 5。用户授权按审查改 spec。

| ID | 处置 |
|---|---|
| C1 L3 轻档 | 头部与定级理由改回；删掉用 B286 为新增 HTTP 辩护 |
| I1 老 agentd 404 | 契约语义：过旧文案、退 0；禁止空预览假绿；故事 3 |
| I2 批处理 vs Reclaim error | 循环在 agentd 内；脏树/判不出 skip 不中止；RemoveAll 失败进报告；退出码规则；缝 2 一净一脏负例 |
| I3 扫描范围 | 只删任务表终态叶子；禁止等值 tmp 根；孤儿目录 OOS+roadmap；字节去重 |
| M1 npm | 问题陈述改为任务私有 tmp/gocache；现役无 NPM_CONFIG_CACHE |
| M2 grok/claudecode | 现状表四份生产 adapter |
| M3 `--force` 仍预览 | 方案 2 命令形态写死 |
| M4 7.7G vs 7.9G | 正文改为「几乎全是 tmp、日志约 0.4G」，不以两个 du 口径对账 |
| M5 compensateWorkspace | 缝 1 / 方案 1 点名第三处，不再写「同一条路径」 |

预览 vs 执行「当下重算」、鉴权走既有 API 入口、`--json` 字节缺席/零可分，一并写入契约语义（审查 §5–§6，未单开 finding）。

## 未写进 spec 正文的实验念头（防重走）

- 把 `GOCACHE` 改成机器级共享缓存：能少占盘，但破坏沙箱隔离（B118 的原因），且不清历史。
- `handoff gc` 默认就删：与 `card close --yes` 的确认纪律相反，150G 误跑不可逆。
- CLI 在协调者本机 `ssh du`：绕过 agentd，状态机最怕的失配。
