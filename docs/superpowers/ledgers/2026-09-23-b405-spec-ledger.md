# B405 spec 台账（2026-09-23）

协调者：session:16（agent:grok，B405 席位）。基线 cards/B233.1-charter-7 @ 0dfa9e74。

- `handoff session list` / `handoff session detail session:16`：确认 B405 挂 session:16，seat=B405。
- `git show cards/B233.1-charter-7:internal/config/config.go`（185–265 行）：ApproverConfig{Executor ExecutorList, Model string, Timeout, Blacklist}，Model 注释「空=用执行者自身默认模型（全候选共用）」；ExecutorList 自定义 UnmarshalYAML 标量/序列两形态（B376）。
- `git grep ApproverConfig cards/B233.1-charter-7 -- internal`：消费面仅 internal/orchestration/approver.go 及测试，无其他生产消费方。
- `git show cards/B233.1-charter-7:internal/orchestration/approver.go`：Decide 遍历 a.names → decideWithCandidate → OneShotReq{Model: a.model}（链级共用，不区分候选）；ApproverAttempt/ApproverDecision 审计结构已存在（B376）。
- `git grep -n approver` internal/config：校验在启用时生效（executor 非空才查 timeout/重复/空串）；未知键报错文案在 config.go:630 列支持键。
- `git show cards/B233.1-charter-7:docs/roadmap.md`（32 行）：「每候选单独 approver.model：本期共用一个 model。来源：b376 OOS」——本卡即收口该残余。
- `git show cards/B233.1-charter-7:docs/superpowers/specs/b376.md`：格式与头部惯例参照。
- 定级判定：单子系统、不动跨进程契约 → L2（非 L1：plan 有解析/校验/选值/回归四块增量）。
- 方案弃选：executor 结构化条目（破坏 B376 同键兼容，弃）；链级 model 废弃（存量漂移，弃）。取 approver.models 映射纯增量。
- 工作树注意：主仓检出在 cards/B380-charter-1（他卡在飞），本卡用独立 worktree 落笔，未动主仓检出分支。
