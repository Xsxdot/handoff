# B180 执行 ledger：丢弃回合收口后的迟到失败结果

- 分支：`cards/B180-charter`
- 起点 commit：`985f37135`
- 执行范围：仅实现计划 Task 1；未修改任何 adapter，未切分支，未 push。

## Task 1 · manager 补守卫

- 实现：在 `handleResult` 的已终结状态判断之后、读取 `r` 与 `transitToReview` 之前，
  丢弃 `waiting_review` 上的 `OK=false` result，并以 Warn 记录原始 `FailReason`；
  `OK=true` 与 `running` 上的失败保持原行为。
- 测试：新增 `TestSettledTurnDropsLateFailureResult` 与
  `TestRunningTurnStillRecordsFailureResult`，复用 `newManagerWithRunningTask`。
- 双裁决：PASS。spec 符合性：守卫位于既有终结判断之后、`r := ev.Result` 与
  `transitToReview` 之前；只丢弃 `waiting_review` 上的失败结果，成功结果与 running
  上的真实失败保持原行为；两条指定用例和额外的迟到成功结果断言覆盖不对称设计。
  代码质量：复用既有 manager 夹具，断言事件数量和任务状态，未修改 adapter，
  `git diff --check` 通过。
- 变异自检：
  - 删除守卫条件中的 `!ev.Result.OK`：`TestSettledTurnDropsLateFailureResult` 通过，
    `TestRunningTurnStillRecordsFailureResult` 失败；原始输出为：
    `manager_test.go:2596: waiting_review 上的成功结果应照旧落一条 completed，实际 0 条`。
  - 删除整段守卫：`TestSettledTurnDropsLateFailureResult` 失败，原始输出为：
    `manager_test.go:2540: 收口后的迟到失败结果不应落 turn_failed，实际 1 条`；
    `TestRunningTurnStillRecordsFailureResult` 通过。
  - 两次变异均已恢复；恢复后定向测试输出：
    `ok github.com/Xsxdot/handoff/internal/agentd 0.158s`。
- 验收结果：`go test ./internal/agentd/ -count=1` 输出
  `ok github.com/Xsxdot/handoff/internal/agentd 117.049s`；
  `go build ./...`、`go vet ./...`、`gofmt -l .` 均无输出且退出码 0。
- Task 1 完成；commit 范围：`internal/agentd/manager.go`、
  `internal/agentd/manager_test.go`、本 ledger（提交前工作树范围）。

## 修复轮次

（无修复轮；Task 1 首轮双裁决通过）

## 终审

（待整分支 commit 后执行）
