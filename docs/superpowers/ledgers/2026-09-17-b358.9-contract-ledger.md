# B358.9 contract 台账：会话身份本质重做（2026-09-17）

本文件是 contract 节点的边干边落台账：确立事实即追加一行。落点与产出物同目录。

## 一、开工核对（上游状态位与基线 pin）

- 分支 `cards/B358.9-charter-4`（自 `acb68493` 续接）。
- 上游 spec `docs/superpowers/specs/b358.9.md` 头部状态行实读「**已批准（2026-09-17 用户批准）**」——核对一致，无需回写。
- 基线 pin：`git merge-base HEAD cards/B233.1-charter-7` = `94246fc8a2acc7996c254cb6b473c7c9b5aef225`（= 基线分支头）。记为有效基线。
- `codegraph --repo .` 在 PATH 上（`/usr/local/bin/codegraph`），版本可用。

## 二、现状查证读数（命令与原始输出摘录）

- 身份解析收口点：
  - `internal/agentd/roomsapi.go:49` `Server.roomUserActor` → `return "web:" + hostOnly(r.RemoteAddr)`。
  - `internal/agentd/ledgerapi.go:102` `Server.ledgerActor` → `return "web:" + r.RemoteAddr`。
  - `cmd/ledgercli.go:44` `ledgerActor` → `cli:<USER>@<host>`。
- 成员记法两处重复实现：`internal/agentd/sessionsapi.go:33#validSessionOwner`、`cmd/session.go:260#validSessionMemberIdentity`（注释自认统一收口归后续重构卡）。
- 写者执法：`internal/collab/room/room.go:102#VerifyWriter(writers []string, actor string)`；书写者集 `internal/collab/service.go:118#sessionWriters`（Members ∪ 卡当前席位）。
- 游标：`internal/collab/cursor/cursor.go#Store.MarkRead(member, roomID string, uptoSeq int64)`（`:35`）、`#Snapshot(member string)`（`:69`）——签名与 string member 键兼容。
- 端戳来源：`internal/agentd/authroutes.go#handleConsole`（`:199`）`DeviceName: sanitizeDeviceName(joinDevice(device, browserName(r.UserAgent())))`；`cmd/console.go:47-53` 缺省主机名。
- 鉴权身份：`internal/agentd/auth.go#identity{session string}`（`:46`）、`#withIdentity`（`:55`）、`#identityFrom`（`:63`）。
- CLI session send：`cmd/session.go#sessionSendCmd`（`:476`）两形态，无 `--agent`。
- 配置：`internal/config/config.go#Config`（`:43`）无身份字段；`decodeStrict`（`:562`）已知键清单手写串。
- 协调者唤醒：`internal/agentd/wakeconsumer.go#Server.roomMessageWakeEvents`（`:190`）席位 target → `WakeMessage{Card}`，外部身份 → 零 Wake。

## 三、对侧常量查执法（谁发出/谁消费）

- `web:<host>`：生产 `roomUserActor`/`ledgerActor`；消费 `VerifyWriter` 逐字相等、`sessionWriters`、游标 member 键、`Mentions`——本卡退役面。
- `cli:<cli>#<session>`：生产 `proto.EncodeSeatIdentity`/`currentSeatIdentity`；消费 `VerifyWriter`、`WakeTargets`/`AddressesCard`、`resolveWakeCard`——沿用。
- `user:`/`agent:`：生产校验两处（HTTP/CLI）+ 测试字面量；消费 `session.Members`/列表。**缺唯一定义处 helper**——本卡新建 `proto.MemberIdentity` 系列。
- `SessionMemberAgent="agent"`：常量在场，但生产分支 `sessionMembers` 只认 `cli:` 前缀 → **疑似漂移**（`agent:*` 成员被判 human）。本卡冻结为待修断言（契约 §5 H 组）。
- `SessionMemberListening`：零生产载体（B358.2 澄清③）——已知漂移，本卡不新增其生产。
- `EvRoomMessage`/`EvMessageConsumed`：活跃事实源，本卡不动。
- `console_user`/`IdentityKind*`/`IdentityResp`：本卡新建，无死常量（消费面 `resolveConsoleIdentity`/`sessionSendActor`/测试）。

## 四、依赖库/平台既成行为

- `decodeStrict` 严格解码：加 `console_user` 必须同步已知键清单（本卡已改）。
- `omitempty` 配置硬要求：`ConsoleUser` 用 `yaml:"console_user,omitempty"`，空值不落盘（本卡测试锁）。
- 设备名由 ticket 登记名+浏览器名拼成、入库前净化（`sanitizeDeviceName`）；端戳取登录会话 `DeviceName`。
- `identity` 零值＝主令牌身份（有意）；端戳装填进 `identity.device`，不引入未鉴权歧义。
- `sessionFromRequest` 鉴权时已查会话行——端戳随一次读出，不二次查库（无新增跨域调用边）。

## 五、落地与提交事实（历史读数）

- Ticket 0 落码：`internal/proto/identity.go`、`internal/agentd/identity.go`、`internal/proto/rooms.go`(+Device)、`internal/config/config.go`(+ConsoleUser)、`internal/agentd/auth.go`(+identity.device)、`internal/agentd/server.go`(注册端点+装填端戳)、`cmd/session.go`(+--agent/sessionSendActor)、`web/src/api/rooms.ts`(+device?)。
- 测试锁：`internal/proto/identity_test.go`、`internal/proto/sessions_fixture_test.go`(+device 金样本)、`internal/agentd/identity_test.go`、`cmd/session_test.go`(+agent 身份/actor 决议)、`internal/config/config_test.go`(+console_user 往返)。
- 图：`codegraph/diffs/cards-B358.9-charter-4.json`；`codegraph/target.json` d_cli→d_protocol 预算 1→2 + entries/d_gateway→d_protocol 注记。

### 跑过的命令与原始结果

- `go build ./...` → 退出码 0。
- `go test ./internal/proto/ -count=1` → `ok github.com/Xsxdot/handoff/internal/proto 0.004s`。
- `go test ./internal/config/ -count=1` → `ok github.com/Xsxdot/handoff/internal/config 0.003s`（含新增 `TestConsoleUserRoundTrip` 通过）。
- `go test ./internal/agentd/ -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 168.160s`。
- `go test ./internal/collab/... -count=1` → `ok`（collab 6.2s、room 0.008s）。
- `go test ./cmd/ -count=1 -run 'TestSessionSendAgentIdentity|TestSessionSendActorResolution|TestSessionSendNegativePaths|TestSessionWaitSourceGuard|TestSessionSendReplyToFlag'` → `ok github.com/Xsxdot/handoff/cmd 0.298s`。
- `go test ./cmd/ -count=1` → 仅 `--- FAIL: TestRepoContractGate`，报 `契约违规 [dead-contract] 契约 d_transport→d_ledger 声明的方向没有活跃 call...`。**stash 全部改动后在干净基线复跑同红** → 该失败为 B370 遗留、非本卡引入。
- `codegraph validate --view cards-B358.9-charter-4` → issues 恰 `[cards-B374-charter] 新增容器 k_collab_model 已存在于基线...`（既有、非本卡）。
- `codegraph check --view cards-B358.9-charter-4` → fails=1（仅 `d_transport→d_ledger` dead-contract）；本卡新增的 `d_cli→d_protocol` 预算经 target 注记后不再超。
- `codegraph validate --stale`（本卡视图 vs 基线）：两者 stale 均 387、集合差为空 → 本卡改动零新增失鲜。

## 六、放弃的尝试

- 端戳初版在 `sessionDeviceName` 里二次 `st.SessionByID` 查库 → 触发 `d_gateway→d_orchestration` 新增调用边并超预算（check over-budget 77/76）。改为在鉴权中间件随 `identity` 一次装填端戳，删除该调用，恢复零增量。

## 七、判断与拍板

- 三重闸门命中两条（见契约 §6）：端戳载体＝`RoomMessage.Device`；主 agent 出示＝`session send --agent` 显式 flag。
- 契约增量文档：`docs/superpowers/specs/b358.9-contract.md`。

## 八、提交事实（历史读数，amend 前）

```
$ git commit -m "contract(B358.9): 会话身份统一记法冻结——Ticket 0 骨架 + 图视图 + 契约文档"
[cards/B358.9-charter-4 b4b857c0] contract(B358.9): 会话身份统一记法冻结——Ticket 0 骨架 + 图视图 + 契约文档
 17 files changed, ...
```

本行随同批提交 amend 收口；amend 后 hash 变化属 git 事实，不在本文件追写。
收口判据是工作树干净，不是文件里的 hash 等于 HEAD。

### 最终法定核对（本提交）

- `codegraph resolve --doc docs/superpowers/specs/b358.9-contract.md --view cards-B358.9-charter-4` → 退出码 0（全部符号锚 ok）。
- `go build ./...` → 退出码 0。
- `codegraph validate --view cards-B358.9-charter-4` → issues 仅 `cards-B374-charter` 既有重复容器（非本卡）。
- `codegraph check --view cards-B358.9-charter-4` → fails=1（仅 B370 遗留 `d_transport→d_ledger`）。
- 变异验证（能变红）：`resolveConsoleIdentity` 缺名分支改成回落 `web:` → `TestConsoleIdentityFailClosedWithoutName` 红，还原绿；`RoomMessage.Device` 键名改 `dev` → `TestRoomMessageDeviceStampGolden` 红，还原绿；`sessionSendActor` agent 分支改返回 `ledgerActor()` → `TestSessionSendAgentIdentity`/`TestSessionSendActorResolution` 红，还原绿。
