# B351 实现计划节点台账

日期：2026-09-09
卡：B351
节点：charter plan
工作分支：cards/B351-charter-3
开工 HEAD：1f92c962c03e50e492f964dd2aeecf39dc661fc8

## 过程与判断

1. 当前工作树起始状态干净，当前分支为 `cards/B351-charter-3`；本节点只产出法定计划与本台账，不写实现代码、不派发、不调用 handoff CLI、不起新的 executor。
2. 已完整读取 `docs/superpowers/specs/b351.md`、`docs/superpowers/specs/b351-contract.md`、`docs/superpowers/specs/b351-breakdown.md`。F1–F4 已拍板；T1→T4 是同一轮 implement 的顺序，不拆并行子卡。
3. 计划法定路径为 `docs/superpowers/plans/b351-plan.md`；本台账与计划同批提交。

## 代码图查证

4. `codegraph --repo . context d_ledger` 退出码 0；返回 best 领域 `d_ledger`、类型 `logic`，包含 `internal/ledger`、`internal/ledgerstep`，并报告 `warning: best.json 未归位容器 39 个`。本计划只据已命中的节点与源码，不据截断/未归位邻域扩边。
5. `codegraph --repo . flow n_ledgerstep_Dispatcher_ViaTemplate` 退出码 0，原始摘要为 `degraded=false steps=49 callers=2 implementations=0 channels=1`；流程步骤中实际包含 `PurposeRounds`、`ReviewRounds`、Transport、WriteGate、`Store.LinkTask`、`Store.RecordDispatch` 及两处错误返回。
6. `codegraph --repo . who-calls n_ledgerstep_Dispatcher_ViaTemplate` 退出码 0；原始命中 `n_cmd_cardDispatchCmd_RunE`、`n_ledgerstep_StepRunner_dispatchNode`，并警告 `基线仍有 5 个未扫描入口：查询结果为空不等于没有调用方`。
7. `codegraph --repo . who-calls n_ledger_Store_PurposeRounds` 退出码 0；原始边为 `Dispatcher.ViaTemplate → Store.PurposeRounds`、`ReviewRounds → PurposeRounds`，上游还命中 `cardDispatchCmd.RunE` 与 `StepRunner.dispatchNode`。
8. `codegraph --repo . who-calls n_agentd_Server_startCardStep` 退出码 0；原始路径为 `POST /api/cards/{id}/step → Server.handleCardStep → Server.startCardStep`，并警告 5 个未扫描入口。
9. `codegraph --repo . tree n_agentd_Server_startCardStep --up --through n_agentd_Server_handleCardStep --from e_http_post_api_cards_id_step` 退出码 0；原始树含 `startCardStep`、`handleCardStep`、`POST /api/cards/{id}/step` 三个节点。
10. 图查询 `codegraph --repo . sym n_cli_targetClient` 原始失败为 `Error: 符号 "n_cli_targetClient" 不在图中（图未覆盖或名字有误）；近似候选: []`；`codegraph --repo . --view cards-B351-charter sym n_ledgerstep_DispatchCompensator` 原始失败同为图未覆盖。按纪律回落源码定位 `cmd/card_dispatch.go#targetClient` 与 `internal/ledgerstep/dispatch.go#DispatchCompensator`，不把失败写成图中不存在调用。
11. `codegraph --repo . sym n_ledgerstep_Dispatcher_ViaTemplate`、`sym n_ledger_Store_PurposeRounds`、`sym n_client_Client_Stop`、`sym n_client_Client_Reclaim`、`sym n_agentd_Server_startCardStep`、`sym n_agentd_Server_clientForTarget` 均退出码 0；其中 `ViaTemplate` 签名为 `func (d *Dispatcher) ViaTemplate(ctx context.Context, c ledger.Card, req TemplateDispatch) (DispatchResult, error)`，`PurposeRounds` 为 `func (s *Store) PurposeRounds(cardID, purpose string) (int, error)`，`Stop` 为 `func (c *Client) Stop(ctx context.Context, taskID string) (worktreeRemoved bool, err error)`，`Reclaim` 为 `func (c *Client) Reclaim(ctx context.Context, taskID string, force bool) (*proto.ReclaimResp, error)`。
12. `codegraph --repo . --view cards-B351-charter sym n_ledger_Store_RecordDispatchRound`、`sym n_client_Client_StopAndReclaim` 均退出码 0，视图原始返回 `view: cards/B351-charter`、`status: added`，签名分别为 `func (s *Store) RecordDispatchRound(cardID, purpose string) error` 与 `func (c *Client) StopAndReclaim(ctx context.Context, taskID string) error`。
13. `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B351-charter resolve --doc docs/superpowers/specs/b351-breakdown.md` 退出码 0；原始 JSON 的 17 个锚点均为 `ok` 或 `moved`，没有 `vanished`/`file_missing`。

图覆盖债：`targetClient`、`DispatchCompensator`、`CountRounds` 的现状 `sym` 未命中；`RecordDispatchAndLinkTask` 与 `ddlStatements` 也按 contract/breakdown 记为现状图缺覆盖，均以源码路径和符号文本为准。一次错误的 `tree` 命令原始报错为 `Error: n_cmd_cardDispatchCmd_RunE 并不调用上面那层 n_ledgerstep_StepRunner_dispatchNode`；这只说明走廊参数选错，不改变已成功的 `who-calls`/`tree` 结果。

## 基线验收

14. `go test ./internal/ledger -run '^(TestB351|TestPurposeRounds|TestDDLDialectParity)' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/ledger  0.879s`，退出码 0。
15. `go test ./internal/ledgerstep -run '^(TestB351|TestViaTemplateSecondRoundGetsNumberedBranch|TestViaTemplateEmptyTargetIsLocal)' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/ledgerstep  1.581s`，退出码 0。
16. `go test ./internal/client -run '^(TestB351|TestReclaimOnOldAgentdReportsUnsupported|TestReclaimUnknownTaskIsNotMistakenForUnsupported|TestReclaimForceCarriesIntoRequestBody)' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/client  0.017s`，退出码 0。
17. `go test ./cmd ./internal/agentd -run '^TestB351' -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/cmd 0.014s [no tests to run]`、`ok  github.com/Xsxdot/handoff/internal/agentd 0.004s [no tests to run]`，退出码 0。
18. `go test ./internal/ledgerstep ./internal/ledger ./internal/client ./cmd ./internal/agentd -run '^TestB351' -count=1` 原始输出：五个包均为 `ok` 且 `[no tests to run]`，退出码 0；因此 Ticket 0 基线没有 B351 行为测试。
19. `go build ./...` 原始输出为空，退出码 0。
20. `git diff --check` 原始输出为空，退出码 0。
21. `go test ./cmd -run '^TestRepoContractGate$' -count=1` 原始退出码 1，原始失败为：`--- FAIL: TestRepoContractGate (0.10s)`；`graph_gate_test.go:38: 契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边（期望在该方向看到至少一条跨子系统边）`；`graph_gate_test.go:38: 契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在（无同名非 deleted 节点；期望在 d_execution 找到）`；末段为 `FAIL github.com/Xsxdot/handoff/cmd 0.113s`、`FAIL`。
22. `go test ./... -count=1` 原始退出码 1；已取得的末段为 `ok github.com/Xsxdot/handoff/internal/agentd 207.189s`、各已完成包为 `ok`，随后 `FAIL`；此前 cmd 输出包含同一 `TestRepoContractGate` 两条 `dead-contract`/`dead-interface` 违规，末段为 `FAIL github.com/Xsxdot/handoff/cmd 72.682s`、`FAIL`。该失败作为基线欠账记录，不归因于 B351。

基线结论：Ticket 0 能编译，既有定向测试能运行，但 `TestB351` 当前均为 no tests；全量与 contract gate 的失败维持既有 `d_execution→d_orchestration` 两条违规。实现节点必须先重新运行每个 task 的基线命令，不能把这些基线绿误作 B351 已实现。

## 源码与测试夹具查证

23. `internal/ledger/store.go` 当前 `ddlStatements` 以 PG/SQLite 两个分支返回 DDL，`ensureSchema` 逐条执行；`Store.mutate` 在 `internal/ledger/store.go:156` 开事务、PG advisory lock、提交后才触发 SQLite listener。新表必须同时加入两方言分支，并通过该事务入口写入。
24. `internal/ledger/dispatch_rounds.go` 当前仅有 `Store.RecordDispatchRound(cardID, purpose string) error` 空壳并返回 `errDispatchRoundUnwired`；文件没有职责头，implement 需要补齐文件边界注释。`internal/ledger/events.go#Store.PurposeRounds` 当前只调用 `TasksOf` 后按 `link.Purpose` 计数；`ReviewRounds` 直接委托 `PurposeRounds(cardID, PurposeReview)`。
25. `internal/ledgerstep/dispatch.go` 当前失败出口为：Transport 成功后先检查 `WriteGate`，然后 `RecordDispatchAndLinkTask`；写闸失败返回 `ErrWriteGateClosed`，落账失败返回 `fmt.Errorf("快照与挂账落账: %w", err)`。文件注释仍写“远端 task 的回收属于真机/集成验收项”，implement 必须改为已接线的 best-effort 补偿说明。
26. `internal/client/client.go:382-429` 的 `Client.do` 使用 `http.NewRequestWithContext`，ctx 取消/超时原样返回；`NewWithWSTiming` 的 `http.Client` 不设置 `Timeout`。`Stop` 非 200 返回 `*httpStatusError`，`Reclaim` 发送 `map[string]bool{"force": force}`，POST 404 会额外 GET `/api/reclaim` 区分旧端，409 解为 `*ReclaimRejected`。
27. `cmd/card_dispatch.go` 的 bare dispatch 在 `cardDispatchCmd.RunE` 本地构造 `&ledgerstep.Dispatcher{St, Transport: cliTransport, Actor, DisciplineText, DisciplineVersion, NormalizeTarget}`；`targetClient(target)` 返回 `(*client.Client, func(), error)`，空 target 走 `LocalEndpoint`，非空走 `newTargetClientNamed`，cleanup 由调用方负责。`internal/agentd/cardstep.go#Server.startCardStep` 在 `StepRunner.Dispatcher` 本地构造 `&ledgerstep.Dispatcher{St, Transport: s.stepTransport, Actor, ... NormalizeTarget: s.CanonicalTarget}`，agentd `clientForTarget` 对空 target 返回本机 client 且不由调用方关闭。
28. `internal/ledgerstep/dispatch_test.go` 已有 `dispatchTestCard`、`seedLedgerStepStore`、`TestViaTemplateSecondRoundGetsNumberedBranch`、`TestViaTemplateEmptyTargetIsLocal` 和 B233.6 回滚/写闸用例；实现测试必须复用这些夹具并把入口放在真实 `Dispatcher.ViaTemplate`。`internal/client/client_test.go` 已有 `httptest.NewServer` 的 Stop/Reclaim 真实 HTTP 用例；`internal/agentd/cardstep_local_test.go` 已有 `newLocalStepHTTPHarness`，`internal/agentd/ledgerapi_test.go` 已有 `newLedgerEnv` 与 `runStepFn` 捕获生产 runner 的夹具；`cmd/card_dispatch_test.go` 已有 `runLedgerCLI`、`swapDispatchTransportWithOpts` 与纪律探活 fake HTTP 夹具。

## 计划与自检待办

29. 计划初稿复核时发现测试代码块含错误构造/未冻结 helper 名称；已按允许的既有 harness 例外改为逐条 pass/fail 断言，并修正 ledger purpose、client.New、CLI 与 agentd 的真实签名。
30. 占位符扫描命令第一次因 shell 引号未闭合失败，原始输出：`/bin/bash: -c: line 1: unexpected EOF while looking for matching \`\``；未采用该次结果，改用无反引号扫描命令。
31. 当前真实 fixture 核对结果：`PurposeImplement`/`PurposeReview` 是 `internal/ledger/types.go` 的受控常量；`LinkTask` 精确签名为 `func (s *Store) LinkTask(cardID, target, taskID, purpose, actor string) error`；`client.New` 精确签名为 `func New(addr, token string) *Client`；CLI `runLedgerCLI` 精确签名为 `func runLedgerCLI(t *testing.T, dir string, args ...string) (string, string, error)`；agentd 已有 `newLedgerEnv`、`newFakeTargetMachine`、`registerFakeTarget` 和 runner 捕获 harness。
32. 按计划逐条重跑 task 基线：`go test ./internal/ledger -run '^(TestB351|TestPurposeRounds|TestDDLDialectParity|TestOpenCreatesSchema)' -count=1` 退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/ledger 0.305s`；`go test ./internal/client -run '^(TestB351|TestStopParsesWorktreeRemoved|TestReclaimOnOldAgentdReportsUnsupported|TestReclaimUnknownTaskIsNotMistakenForUnsupported|TestReclaimForceCarriesIntoRequestBody)' -count=1` 退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/client 0.008s`。
33. `go test ./internal/ledgerstep -run '^(TestViaTemplateSecondRoundGetsNumberedBranch|TestViaTemplateEmptyTargetIsLocal|TestB2336DispatchFailureRollsBackSnapshot|TestViaTemplateStopsSnapshotAfterWriteGateCloses)' -count=1` 退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/ledgerstep 0.467s`；`go test ./cmd ./internal/agentd -run '^(TestB351|TestCardDispatch|TestStartCardStep|TestStepTransport)' -count=1` 退出码 0，原始输出 `ok github.com/Xsxdot/handoff/cmd 3.471s` 与 `ok github.com/Xsxdot/handoff/internal/agentd 1.291s`。
34. 计划自检命令：`codegraph --repo . --view cards-B351-charter resolve --doc docs/superpowers/plans/b351-plan.md` 退出码 0；原始 JSON 将 5 个计划锚点标为 `ok`/`moved`，无 vanished/file_missing。占位符扫描 `rg ... docs/superpowers/plans/b351-plan.md` 退出码 1、无输出，表示未命中列出的占位符/错误签名词。`git diff --check` 退出码 0、无输出；`go build ./...` 退出码 0、无输出。
35. 计划落盘范围核对：`git status --short --branch` 原始输出为 `## cards/B351-charter-3`、仅有 `docs/superpowers/ledgers/2026-09-09-b351-plan-ledger.md` 与 `docs/superpowers/plans/b351-plan.md` 两个未跟踪产物；没有实现源码改动。
36. 首次提交命令与原始输出：`git commit -m "docs: add B351 implementation plan"`；原始输出为 `[cards/B351-charter-3 9078c500] docs: add B351 implementation plan`、`2 files changed, 749 insertions(+)`、`create mode 100644 docs/superpowers/ledgers/2026-09-09-b351-plan-ledger.md`、`create mode 100644 docs/superpowers/plans/b351-plan.md`。随后按收口规则只 amend 一次收录本行，不回写 amend 后 hash。

## 收口规则

提交命令与原始输出在提交发生后追加，随后只执行一次 `git commit --amend --no-edit` 收录本事实；不把 amend 后 hash 回写本台账。最终判据为工作树干净。
