# B233.17 plan 台账（2026-09-12）

节点：`charter:plan`（B233.17 组装点收窄与编译期封界：handler 退出 assembly，internal 封界，预算棘轮）
分支：`cards/B233.17-charter-4`（本卡不切分支、不改 git 配置、不调 handoff CLI、不起 executor、不派发）
上游：spec `docs/superpowers/specs/b233.17.md`（已批准）；contract `docs/superpowers/specs/b233.17-contract.md`（冻结 @ `bd9c086c`）；breakdown `docs/superpowers/specs/b233.17-breakdown.md`（§0.1 拍板 P1–P5 全 A @ `bad7fdb5`）
产出物：`docs/superpowers/plans/b233.17-plan.md`

## 1. 入口读数（本工作树复核）

- 当前分支 `cards/B233.17-charter-4`，起点 HEAD `bad7fdb5`（P1–P5 拍板回写）。
- `codegraph/target.json#assembly` = `["main.go","internal/agentd/server.go","cmd/agentd.go"]`；`codegraph.go` 已不在。
- `internal/agentd/server.go` 3010 行；`Server.handle*` 定义 21 个（`:889` `handleStatus` … `:2333` `handleEvents`；21 个全在 `:889`–`:2621` 连续段内）。
- 连续 handler 段含私有 helper/DTO：`writeReclaimError`/`taskDetail`/`replyRequest`/`replyResult`/`resumeIfIdle`/`dispatchRequest`/`writeDispatchError`/`projectAddRequest`/`writeProjectError`/`continueRequest`/`doneRequest`/`doneResult`/`releaseTaskCarrierOccupancy`/`parseForce`/`taskOrErr`/`taskRepoOrErr`/`runRequest`/`runResponse`/`writeManagerError`。
- 段后非 handler：`noteTruncationDiagnosed` `:2623`、`writeEvent` `:2630`、`writeJSON` `:2641`、`truncateRunes` `:2652`、`SetupAutomation` `:2673` —— 留 server.go。
- 嵌套 `internal/<包>/internal/` 当前不存在。
- 四包公开构造：`orchestration.NewManager` `manager.go:291`；`scheduling.New` `scheduling.go:173`；`workspace.NewCapability` `capability_impl.go:17`；`approval.NewClient` `client.go:72`。
- `internal/orchestration` 生产反 import `internal/agentd`（9 文件）；`executor`/`proto` 均不 import 四包（`go list -deps` 复核 NONE）。
- 生产消费面：`workspace.*` 被 agentd 生产 60+ 导出符号直调；`scheduling.*` 同类；`approval` 仅 `orchestration` 生产经 `approval.NewClient` 消费。

## 2. 本节点实跑命令与原始结果

| # | 命令 | 原始结果 |
|---|---|---|
| 1 | `go build ./...` | 无输出，退出码 0 |
| 2 | `go test ./internal/agentd/ -run TestB23317 -count=1` | `ok github.com/Xsxdot/handoff/internal/agentd 0.006s` |
| 3 | `go test ./cmd -run TestCLIExecutionSurface -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.004s` |
| 4 | `go test ./internal/approval/ ./internal/scheduling/ ./internal/workspace/ -count=1` | `ok approval 0.728s` / `ok scheduling 4.143s` / `ok workspace 5.766s` |
| 5 | `codegraph check --repo .` | `fails=7`（3 dead-entry gateway→orchestration + 1 dead-interface OrchestrationClient + 3 over-budget：207/0、9/0、3/0）；退出码 1 |
| 6 | `codegraph check --view cards-B233.17-charter-2 --repo .` | `fails=6`（dead-interface 被视图消除）；退出码 1 |
| 7 | `codegraph validate --repo .` | 「发现 122 个完整性问题、0 个失鲜节点」；退出码 1（基线存量） |

## 3. 迷你模块实验（本节点亲跑，非记忆；落 `/root/.handoff/tmp/456774b5/`）

- 父包 `internal/workspace` `NewCapability()` 返回嵌套包类型、嵌套包又 import 公开 `workspace` 取 `Capability`/`PrepareReq` → `import cycle not allowed`（失败）。
- 嵌套包不 import 父包、父包 import 嵌套包 → `go build` 成功。
- `internal/<pkg>/internal/<sub>` 不被兄弟包/上层包 import：`use of internal package ... not allowed`（编译期执法）。
- **结论定形态**：nested 只收「不 import 父公开包、且不被父包公开面回引」的细节；凡被 gateway/orchestration 直调或与父包形成回边的公开能力函数留公开包（P3-A）。故 workspace 只搬 `runshell`/`procgroup`（纯 stdlib），orchestration 只搬 `cachegc` 纯函数（依赖 executor/proto），approval 只搬 `gateDecision`/`mapGateAnswer`/`permEventText`（依赖 executor/strings），scheduling 只搬 `statusLog`（依赖 slog）；`scheduling.Service` 与公开读模型/纯函数留公开包（P4-A/C-5）。

## 4. 关键判断

- W1 搬「`:889`–`:2621` 连续 handler 段」到新文件 `handlers.go`；`server.go` 保留插线与段后 helper（`Handler`/`NewServer`/`Set*`/`SetupAutomation`/`noteTruncationDiagnosed`/`writeEvent`/`writeJSON`/`truncateRunes`）。新增 assembly 实质断言 `TestB23317AssemblyFilesHaveNoHandlerDefs`，基线必红（server.go 含 21 个 handle*）。
- W2 四包 nested 均「父包 import nested、nested 不回引父包」，逐一给精确 Produces 签名；公开调用点与测试调用点加包限定。
- W3 修 `executionSurfaceViolations` 函数名上下文：遇包级 `var/const/type`（列 0）重置 `current`；配可变红用例（白名单工厂函数之后包级 `*client.Client`）。
- P1-A：不承诺 `codegraph check` 归零；验收 = validate 零新增（122=122）+ check 不新增 fail/warn + 守卫绿 + `go build` 绿。
- W2 嵌套是否给 `codegraph check` 新增图条目：本节点**不探测**（协调者指令），写成 W4 实现期验证步骤（原文记录，若新增即停回协调者，不得改 best/baseline/target 抹平）。

## 5. 收尾

- 产出物 `docs/superpowers/plans/b233.17-plan.md` 落盘（零上下文可执行、无占位符）。
- 本节点未搬 handler、未建 nested、未改 `target.json`/`best.json`/`baseline.json`、未抬配额、未改 contract（W0 是 implement 步骤）。
- 探针已删；迷你模块在 `/root/.handoff/tmp/456774b5/`（仓外，不影响工作树）。
- 提交（历史读数）：`git commit` 后 amend 一次收进同批；amend 换 hash，不回写追踪。
