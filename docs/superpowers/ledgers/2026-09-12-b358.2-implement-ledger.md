# B358.2 实现台账（implement 节点，2026-09-12）

执行者：charter implement 节点 subagent。计划：`docs/superpowers/plans/b358.2-plan.md`（拍板三条裁决已生效）。
有界文件集：`internal/collab/sessions.go`、`internal/collab/sessions_test.go`（Task 6 临时探针触 `service.go` 当场还原，零最终 diff）。

## 一、入场读数

- 分支 `cards/B233.1-charter-7`，HEAD `1f8ef7e2`（plan + breakdown 修订回写已入库）；基线 `0be4a2c6`（B358.1）已在其历史中。
- `git status`：工作树脏文件与 plan §10 步骤 3 清单一致——`web/src/app/task/Composer.test.tsx`（modified）、`.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、`web/design-qa-sidebar-after.png`、`web/design-qa.md`（untracked）。全部与本卡无关，不触碰。
- 判据基线（plan Task 1 步骤 1）：

```
$ go test ./internal/collab -run 'TestSession' -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	0.592s
EXIT=0
```

## 二、Task 1：sessionTimeline 归属修复（先红后绿）

- 步骤 3 跑红（逐字版原始输出，首次红在组①族循环首条违反）：

```
$ go test ./internal/collab -run TestSessionTimelineBelongsToOneSession -count=1
--- FAIL: TestSessionTimelineBelongsToOneSession (0.01s)
    sessions_test.go:495: 会话一 timeline 串进非本会话卡的事件行（发现 A）: {Seq:7 Kind:card_joined CardID:B3 Detail: Actor:user:sy CreatedAt:2026-09-12 12:00:16.99491 +0000 UTC}
FAIL	FAIL github.com/Xsxdot/handoff/internal/collab	0.549s
```

- `t.Fatalf` 首错即停，单次运行只见一组红。做**临时诊断变体**（组①两处 + 组②一处 Fatalf→Errorf，跑完当场还原逐字版，`git diff --stat` 复核为纯 170 行新增）取得三组各自读数：

```
    sessions_test.go:495: DIAG 会话一 timeline 串进非本会话卡的事件行（发现 A）: {Seq:7 Kind:card_joined CardID:B3 ...}   ← 组②（joined 无判定）
    sessions_test.go:495: DIAG 会话一 timeline 串进非本会话卡的事件行（发现 A）: {Seq:9 Kind:seat_rebound CardID:B2 ...}  ← 组①（takeover 全量卡表）
    sessions_test.go:495: DIAG 会话二 timeline 串进非本会话卡的事件行（发现 A）: {Seq:5 Kind:card_joined CardID:B1 ...}
    sessions_test.go:495: DIAG 会话二 timeline 串进非本会话卡的事件行（发现 A）: {Seq:9 Kind:seat_rebound CardID:B2 ...}
    sessions_test.go:495: DIAG 会话二 timeline 串进非本会话卡的事件行（发现 A）: {Seq:11 Kind:seat_rebound CardID:B1 ...}
    sessions_test.go:503: DIAG 无会话卡 B 的事件串进 timeline: {Seq:9 Kind:seat_rebound CardID:B2 ...}（两次，两场会话各一）
    sessions_test.go:520: DIAG 他场结构事件串场（会话二的 created）: {Seq:6 ...}   ← 组②（created 查错载荷键）
    sessions_test.go:520: DIAG 他场结构事件串场（卡 C 进会话二的 joined）: {Seq:7 ...}
    sessions_test.go:520: DIAG 他场结构事件串场（会话一的 created）: {Seq:4 ...}
    sessions_test.go:520: DIAG 他场结构事件串场（卡 A 进会话一的 joined）: {Seq:5 ...}
```

组①（B 的行串场，含 takeover 全量卡表偏差）红、组②（created 错键 + joined/left 无判定）四路全红、组③防过删无违反（绿）。无「写下去就绿」的组——判据与现状理解成立，继续。

- 步骤 4 实现：`sessionTimeline` 整函数替换（新签名 `(events, session proto.Session)`，归属判据 `session.Cards` 成员集 + 结构事件载荷归属键）；新增 `structEventFields`（解析失败 `log().Warn` + 返回 nil）；`SessionDetail` 调用行改 `sessionTimeline(events, session)`。
- 步骤 5 跑绿：

```
$ go test ./internal/collab -run TestSessionTimelineBelongsToOneSession -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	0.534s
$ go test ./internal/collab -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	0.749s
```

- `gofmt -l internal/collab` 空。提交：见 §八提交清单。
- 提交动作记录：首次提交后发现 gofmt 未过（两处：`sessionTimeline` doc 注释清单后缺 `//` 空行；测试里 `seqs["created/" + session1.ID]` 应为 `"created/"+session1.ID`）——`gofmt -w` 修正后 amend 一次收进同批提交（纪律允许的单次 amend）。教训：提交前 gofmt 检查应在提交动作之前显式跑。修正后 `gofmt -l internal/collab` 空、包回归绿。

## 三、Task 2：timeline 增 seat_bound / card_closed 分支（先红后绿 + 变异）

- 步骤 2 跑红（原始输出，红在 seat_bound 分支缺失；`t.Fatalf` 首错即停，card_closed 分支缺失的继续红由步骤 5 变异红侧证）：

```
$ go test ./internal/collab -run TestSessionTimelineSeatBoundAndCardClosed -count=1
--- FAIL: TestSessionTimelineSeatBoundAndCardClosed (0.01s)
    sessions_test.go:606: 坐下应产生 seat_bound 行: [{Seq:4 Kind:created CardID: ...} {Seq:6 Kind:card_joined CardID:B1 ...} {Seq:7 Kind:card_joined CardID:B2 ...}]
FAIL	github.com/Xsxdot/handoff/internal/collab	0.539s
```

- 步骤 3 实现：`driver_takeover` 块后、`default:` 前插入 `case "driver_seat_bound"` 与 `case "status_moved"`（逐字）。
- 步骤 4 跑绿：

```
$ go test ./internal/collab -run TestSessionTimelineSeatBoundAndCardClosed -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	0.532s
```

- 步骤 5 变异验证：变异前 `grep -c 'switch payloadString(ev.Payload, "to")'` = 1（命中唯一）；变异（摘除两值判定、无条件记行）后 `go build ./internal/collab/...` 编译过，复跑红：

```
--- FAIL: TestSessionTimelineSeatBoundAndCardClosed (0.01s)
    sessions_test.go:625: 普通列间转移不得记 card_closed（契约条 56）: {Seq:10 Kind:card_closed CardID:B1 Detail:{"from":"待办","to":"进行中"} Actor:t ...}
```

还原后复跑绿（`ok ... 0.342s`）。
- 局部回归 `go test ./internal/collab -count=1` → `ok ... 0.708s`；gofmt 空。提交：见 §八。

## 四、Task 3：timeline 增 needs_human 分支 + 标签翻转锁（先红后绿 + 变异）

- 步骤 2 跑红（原始输出，红在 `rows != 1` 分支缺失；标签断言保持绿）：

```
$ go test ./internal/collab -run TestSessionTimelineNeedsHuman -count=1
--- FAIL: TestSessionTimelineNeedsHuman (0.01s)
    sessions_test.go:737: 应恰一条 needs_human 行, got 0: [{Seq:3 Kind:created ...} {Seq:5 Kind:card_joined CardID:B1 ...}]
FAIL	github.com/Xsxdot/handoff/internal/collab	0.533s
```

- 步骤 3 实现：`status_moved` 块后、`default:` 前插入 `case "needs_human"`（逐字）。
- 步骤 4 跑绿：`ok ... 0.545s`。
- 步骤 5 变异验证：变异前 `grep -c 'proto.SessionEventNeedsHuman, ev.CardID'` = 1（锚唯一；`inSession` 守卫 4 处同形故取更长锚）；摘除该分支归属守卫后编译过、复跑红：

```
--- FAIL: TestSessionTimelineNeedsHuman (0.02s)
    sessions_test.go:732: needs_human 行归属错误: {Seq:7 Kind:needs_human CardID:B2 Detail:{"reason":"与本会话无关"} Actor:user:sy ...}
```

**偏差注记**：plan 预言红在「无会话卡 B 的 needs_human 串进会话二」（d2 循环），实测红在同测试更早的「行归属错误」断言（d1 循环）——守卫摘除后卡 B 的行先进了会话一 timeline，同一缺陷（B 串场）被更早的断言拦住。变异有效（行为已变、测试咬合），非「变异未生效」。
- 还原后复跑绿（`ok ... 0.367s`）；局部回归 `ok ... 0.750s`；gofmt 空。提交：见 §八。

## 五、Task 4：成员状态诚实判据（绿确认 + 变异①②）

- 步骤 2 绿确认（写下去应绿，符合预期）：

```
$ go test ./internal/collab -run TestSessionMemberStatusHonest -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	0.538s
```

- 变异锚唯一性：`grep -c 'if expiresAt.After(nowFn())'` = 1；`grep -c 'return proto.SessionMemberLastActive, time.Time{}'` = 1。
- 变异①（`if expiresAt.After(nowFn())` → `if true`）：编译过，红在预言断言：

```
--- FAIL: TestSessionMemberStatusHonest (0.01s)
    sessions_test.go:847: 拨钟过期后应翻 last_active: {... Status:working LastActive:2026-09-12 13:10:08.269812 +0000 UTC}
```

还原后绿（`ok ... 0.349s`）。

- 变异②（`return proto.SessionMemberLastActive, time.Time{}` → `return "online", time.Time{}`）：编译过，红：

```
--- FAIL: TestSessionMemberStatusHonest (0.01s)
    sessions_test.go:856: 无租约成员应报 last_active+零值: {Identity:user:sy Kind:human ... Status:online LastActive:0001-01-01 00:00:00 +0000 UTC}
```

**偏差注记**：plan 预言红在词表扫，实测红在其之前的 owner 断言（`Status:online` 直接显形于报文）——同一缺陷（词表外串生产），更早断言先咬合。变异有效。还原后 `git diff --stat internal/collab/sessions.go` 零输出（还原干净）。
- 局部回归 `go test ./internal/collab -count=1` → `ok ... 0.765s`；gofmt 空。零生产代码改动，提交只含测试：见 §八。

## 六、Task 5：sessionNodes 锁缝 + 解码失败留日志 + 变异

- 步骤 2 绿确认（写下去应绿）：`go test ./internal/collab -run TestSessionNodesAggregation -count=1` → `ok ... 0.555s`。
- 步骤 3 加日志（两守卫分支替换），复跑绿（`ok ... 0.568s`）。
- 步骤 4 变异——**plan 原变异为 no-op，发现 plan 缺陷一处，按 implement 变异纪律替换为有效等价变异重做**：
  1. plan 原变异（首守卫 `continue`→`break`）：编译过，复跑**仍绿**。根因：plan 自家测试数据中有效节点（implement）先 append、坏样本（review-broken）后 append，账本 seq 升序下有效行先于坏样本被投影——`break` 只能吞掉坏样本之后的事件，断言目标（已 append 的有效行）吞不掉。plan 验收栏「cardA 的有效节点在坏样本之后仍被投影」与其自家测试代码样本顺序矛盾（步骤 4 预言「红在应恰一条节点」不成立）。
  2. 有效等价变异（保留 `break`，守卫条件 `err != nil` → `err != nil || true`——守卫对有效行也触发，`break` 吞掉有效节点）：编译过，复跑红：

```
--- FAIL: TestSessionNodesAggregation (0.01s)
    sessions_test.go:915: 应恰一条节点（解码失败跳过、他会话卡不进）: []
```

  恰为 plan 预言的失败报文——证明测试对「节点被吞」有牙（族 2）。
  3. 完全还原（`continue` + 原守卫条件）后复跑绿（`ok ... 0.351s`）；`git diff --stat internal/collab/sessions.go` 仅余步骤 3 日志改动的 8 行。
- 局部回归 `ok ... 0.755s`；gofmt 空。提交：见 §八。
- **移交建议（非本卡动作）**：若后续要让 plan 原变异直接咬合，可把测试样本顺序改为坏样本在前——涉及 plan 逐字测试体的修订，归协调者裁决，本卡未动。

## 七、Task 6：B287 双会话反例（绿确认 + 临时探针）

- **plan 笔误修正（偏差 1）**：plan 代码块取事件用 `st.EventsFromAsc([]string{}, 0, 1000)`（返回 `[]ledger.Event`），而 `room.ConsumedSeqs` 收 `[]proto.LedgerEvent`——编译红 `cannot use events (variable of type []ledger.Event) as []proto.LedgerEvent value`。最小修正：夹具第三返回值接 `lc`，改走既有 `room.ReadAllEvents(lc, 0)`（语义同全流升序读，且与被测路径 `consumeRoomMentions` 同源）。测试其余逐字未动。
- 步骤 2 绿确认（预期绿，实测绿——**Task 6 实测结果：绿，未触发裁决③停手**）：

```
$ go test ./internal/collab -run TestMentionClearScopedToSessionRoom -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	0.560s
```

- 步骤 3 变异探针（有界集外的**临时**触碰）：变异前 `grep -c '!room.SameRoom(ev, roomID) || consumed\[ev.Seq\]' service.go` = 1（锚唯一）；摘除 `!room.SameRoom(ev, roomID) ||` 后编译过、复跑红：

```
--- FAIL: TestMentionClearScopedToSessionRoom (0.01s)
    sessions_test.go:976: 会话二的提及必须保留、会话一的应被清: []
```

（plan 预言红在「不得被牵连消费」的账本断言，实测红在其之前的 `Mentions` 收件箱断言——两房间提及都被牵连消费后收件箱先空，同一缺陷更早断言。变异有效。）**当场还原**：`git diff 0be4a2c6 -- internal/collab/service.go` 零行（零最终 diff，service.go 零触碰声明兑现）；复跑绿（`ok ... 0.338s`）。
- 局部回归 `ok ... 0.751s`；gofmt 空。提交：见 §八。

## 八、Task 7：收口——全量绿 + 金样本复跑 + 源码级对齐审查

- 步骤 1 追加 `TestSessionDetailReadableAfterArchive`（写下去应绿）：`ok ... 0.563s`。
- 步骤 2 全量与金样本（逐条退出码）：

```
$ go build ./...
BUILD_EXIT=0
$ go test ./internal/collab/... -count=1
ok  	github.com/Xsxdot/handoff/internal/collab	0.797s
ok  	github.com/Xsxdot/handoff/internal/collab/room	0.775s
$ go test ./internal/proto -run 'TestSession' -count=1 -v
--- PASS: TestSessionSummaryGoldenProjection (0.00s)
--- PASS: TestSessionDetailGoldenProjection (0.00s)
--- PASS: TestSessionMemberStatusVocabulary (0.00s)
--- PASS: TestSessionTimelineKindVocabulary (0.00s)
ok  	github.com/Xsxdot/handoff/internal/proto	0.679s
```

金样本 4 支命中（判据字面 `-run 'TestSession'`，拍板①落地）；投影零新增 wire 键。

- 步骤 3 源码级审查清单（逐条）：
  - **pass** 契约条 36：`grep -rn '"github.com/Xsxdot/handoff/internal/ledger"' internal/collab --include='*.go' | grep -v _test` → 0 命中。
  - **pass** kind 常量化：`sessionTimeline` 全部 8 处 kind 赋值均取 `proto.SessionEvent*` 常量（行 352/358/365/372/379/389/400/412），无裸字面量 kind（status_moved 的 `"已完成"/"终止"` 是载荷值判定非 kind，漂移由测试用 `ledger.StatusDone/StatusClosed` 生产事件钉住）。
  - **pass** `default: continue` 保留——未知 ledger 事件类型不进 timeline。
  - **pass** `sessionNodes` 解码失败两分支逐事件 `continue` 保留（带 Warn 日志）。
  - **pass** 有界文件集复核：`git diff --stat 0be4a2c6 -- internal/collab` → 仅 `sessions.go`、`sessions_test.go` 两文件；`docs/superpowers/` 本卡新增 = 本台账（plan 与 plan-ledger 及 breakdown 修订行系入场前既有提交 285e7cf1/1f8ef7e2）；入场前脏文件未触碰。
  - 注释失真修正（偏差 3）：`default:` 注释「分支由 Task 2/3 增补」（未来时）在 Task 2/3 落地后已成假话，改为「分支已增补（Task 2/3）」。零行为改动，改后 gofmt 空 + build 0 + collab/room/proto 全绿复跑（0.928s/0.253s/0.235s）。

## 九、图覆盖债移交（recon 补视图 diff 用；plan §2）

**生产符号**（internal/collab/sessions.go）：
- `sessionTimeline`——签名变更：`(events []proto.LedgerEvent, sessionID string, byCard map[string]proto.Card)` → `(events []proto.LedgerEvent, session proto.Session)`
- `structEventFields`——新增
- `sessionNodes`——修改（解码守卫两分支增 Warn 日志，continue 语义不变）
- `Service.SessionDetail`——修改（调用行适配新签名）

**测试符号**（internal/collab/sessions_test.go，全新增）：
- 助手：`sessionStructSeqs`、`cardSet`、`memberByCard`、`memberByIdentity`
- 测试函数：`TestSessionTimelineBelongsToOneSession`、`TestSessionTimelineSeatBoundAndCardClosed`、`TestSessionTimelineNeedsHuman`、`TestSessionMemberStatusHonest`、`TestSessionNodesAggregation`、`TestMentionClearScopedToSessionRoom`、`TestSessionDetailReadableAfterArchive`

## 十、收尾：提交与自主消化偏差清单

### 提交记录
按 plan 每 task 一步提交，共 6 笔（历史事实，hash 为当时读数）：T1 `0dfa2a26`（gofmt 修正 amend 一次）、T2 `2fc60905`、T3 `3c61b291`、T4 `f0c2167f`、T5 `54230bb3`、T6 `3e6106cd`。协调者指令「收尾单提交」——按指令把这 6 笔连同 T7 变更 squash 为单笔收尾提交（message：`implement(B358.2): 详情页投影精化（归属修复/needs_human/seat_bound·card_closed/成员状态）`），squash 换 hash 是 git 事实，不回写 chase；收口判据=工作树干净（除入场前既有脏文件）。

### 自主消化偏差清单
1. **Task 6 plan 代码笔误**：事件读取 `st.EventsFromAsc`（`[]ledger.Event`）与 `room.ConsumedSeqs`（收 `[]proto.LedgerEvent`）类型不符，编译红。最小修正：夹具第三返回值接 `lc`，改走既有 `room.ReadAllEvents(lc, 0)`。测试其余逐字未动。
2. **Task 5 plan 变异设计缺陷**：plan 原变异（首守卫 `continue`→`break`）在 plan 自家测试数据顺序下是 no-op（有效节点先于坏样本投影，break 吞不到断言目标；plan 验收栏「有效节点在坏样本之后」与其测试代码矛盾）。按 implement 变异纪律替换为有效等价变异（守卫条件 `err != nil || true` + break）重做，红在 plan 预言的报文。测试代码逐字未动；若要让原变异直接咬合，建议后续把坏样本排前（归协调者裁决）。
3. **Task 1 后注释失真修正**（见 §八步骤 3 末条）：`default:` 分支注释未来时改过去时，零行为改动。
4. **变异红落点与 plan 预言的偏差（读数差异，非动作偏差）**：Task 3 变异红在「行归属错误」而非 plan 预言的「串进会话二」（同缺陷更早断言）；Task 4 变异②红在 owner 断言而非词表扫（`Status:online` 显形于报文，同缺陷）；Task 6 探针红在 `Mentions` 收件箱断言而非账本断言（同缺陷）。三例均为变异有效、测试咬合，仅 plan 对首触断言的预言不准。
5. **gofmt 提交顺序失误一次**：T1 首次提交后才发现 gofmt 未过（新增代码两处格式），`gofmt -w` + amend 一次修正。此后各 task 提交前显式跑 gofmt，未再犯。

### 最终提交物
- `internal/collab/sessions.go`（有界集）：timeline 归属修复全族 + seat_bound/card_closed/needs_human 三分支 + structEventFields + sessionNodes 日志 + 注释修正。
- `internal/collab/sessions_test.go`（有界集）：7 支新测试 + 4 支新助手 + `strings`/`time` import。
- `docs/superpowers/ledgers/2026-09-12-b358.2-implement-ledger.md`：本台账。
- 工作区无关脏文件（`web/src/app/task/Composer.test.tsx`、`.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、`web/design-qa*`）未触碰、未卷入。







