# B156.3.3 集成节点台账（claude/kai-156-3-ba-df7357）

> 卡 B156.3.3 ｜ 隔离工作树 /root/.handoff/worktrees/5549b33f（分支
> cards/B156.3.3-charter-5）执行，合并落在集成分支 claude/kai-156-3-ba-df7357。
> 每条读数均为本节点亲跑；未跑到的明写「未验证」。

## 开工核对

- `git status`（隔离工作树）：clean；HEAD = `2b5dd44a`「docs(b156.3.3): 修复轮2台账——三
  major 处置、自检两问、正交扫描、三档读数」= 协调者指定的已验收目标提交。
- 基线：`origin/claude/kai-156-3-ba-df7357` = `efc6f7d9`（merge(B156.3.2)）。
  `git merge-base HEAD origin/claude/kai-156-3-ba-df7357` = `efc6f7d9`，即基线是目标的
  祖先，待并入提交共 9 个（`git rev-list --count efc6f7d9..2b5dd44a` = 9）。
- 协调者主工作树 /root/.handoff/repos/handoff 未触碰；未 push 任何远端。

## 合分支（Task：integrate 合并）

- 本地基线分支 `claude/kai-156-3-ba-df7357` 自 origin 同名 ref 建立于
  `efc6f7d9`；在隔离合并工作树 /root/.handoff/tmp/5549b33f/integ-b15633 执行真实
  `git merge --no-ff cards/B156.3.3-charter-5 -m "merge(B156.3.3): …"`。
- 冲突：**0**（ort 策略，退出码 0）。合并提交 **46325dbc**，parents =
  (`efc6f7d987d6…`, `2b5dd44ab6de…`)。
- 树一致性：merge tree = `a6cfd4298d2779883b894227d1972282fab620e4` ==
  目标 2b5dd44a tree（逐字节相同）→ 集成内容 = 已验收内容，无第三方混入。
- diffstat：21 files changed, +4565 / −5。

## 全量门禁（三档读数）

1. `go build ./...` → 退出码 0；`go vet ./...` → 退出码 0。
2. `go test ./... -count=1`：
   - 第一跑（合并工作树位于 `$TMPDIR=/root/.handoff/tmp/5549b33f` 之下）：退出码 1，
     唯一红 `cmd` 包 `TestResolveServiceBinFallsBackFromGoBuildCache`
     （service_test.go:282「有已安装二进制时应回退」）。分栏：**本卡引入的红 = 无；
     基线既有的红 = 该条**。基线对照命令与读数：在基线提交上另开同位置检出
     （git worktree add --detach …/integ-baseline-wt efc6f7d9），复跑
     `go test ./cmd/ -run TestResolveServiceBinFallsBackFromGoBuildCache -count=1`
     → 同样 FAIL，退出码 1。根因（读码 + 双位置对照实验坐实）：
     `TMPDIR` 指向仓库检出父目录时 `isEphemeralBin`（cmd/service.go:342）把仓内候选
     文件判成临时产物，回退臂不命中；同一内容树检出在
     /root/.handoff/worktrees/5549b33f 时单测 ok。环境诱发、基线既有，非本卡引入。
   - 权威全量读数（中性 `TMPDIR=/tmp/handoff-itest` 重跑同一条命令）：
     `go test ./... -count=1` → **退出码 0**，51 包 ok / 0 FAIL（agentd 133.412s、
     cmd 11.700s、ledger 20.250s、ledgerstep 10.744s、prochost 21.495s、scheduling
     0.652s、client 9.484s）。
3. `gofmt -l <13 个本轮改动 .go>`（git diff --name-only efc6f7d9 HEAD -- '*.go' 展开）
   → 输出为空、退出码 0；正控：对故意写坏的 /root/.handoff/tmp/5549b33f/badfmt.go 跑
   同一条命令 → 打印文件名（正控生效，「零输出」不是空跑）。
- web 腿：`npm install --prefer-offline` 退出码 0；`npx vitest run
  src/api/contract.test.ts` → Test Files 1 passed (1)，Tests 39 passed (39)，
  退出码 0（与修复轮 2 读数一致）。

## 契约对照（两次对照）

- 全量对照·改前：基线 `efc6f7d9` 上 `go run . graph check --repo .` → `"fails": []`，
  退出码 0。
- 全量对照·改后：合并树 `46325dbc` 上同命令 → `"fails": []`，退出码 0；warns 全为
  既知图债（anchor-off-domain d_workspace×2、best-dangling k_collab_*×7、
  container-misplaced d_web 子域、legacy 预算内直调、oversized-package cmd/agentd、
  prefix-family），逐项与基线比对 **new warns 0 / gone warns 0**。
- legacyHits 改前/改后逐一对照：34 条跨域直调边全部相等（delta=0）。
  `d_gateway→d_scheduling`（budget 0）两侧均无命中 → 零新增直调，预算守恒。
  **集成阶段契约错配 = 0。**
- 分支视图腿：`graph views` 仅 cards-B156.2-charter-4 / cards-B192-charter /
  cards-B205-charter —— B156.3 线从未产出视图 diff，**本卡分支视图对照不可执行**
  （如实登记；非本卡引入，归 roadmap 图数据债一节既有语境）。

## 接缝缺陷记账

- **集成阶段新发现缺陷数 = 0**：合并零冲突、全量档绿（中性 TMPDIR 下）、图 fails
  双侧清零、契约错配 0。
- 环境观察（非代码缺陷、基线既有，已落 docs/roadmap.md 残余账本「来自 B156.3.3
  集成轮」节）：`TestResolveServiceBinFallsBackFromGoBuildCache` 对「检出路径位于
  os.TempDir() 下」敏感。修法方向留 roadmap：夹具不用仓内真实文件充当 durable
  二进制，或测试内显式固定 TempDir 探针。本期不修（超出卡边界，纪律禁止顺手修
  范围外红）。

## 回旋镖对账（plan/breakdown 预测 vs 实测）

| 预测 | 实测 | 结论 |
|---|---|---|
| 有界文件集（schedapi.go 新建、server.go 仅注册行、cmd/squad.go 新建、card/card_node 若涉及、web/src/api、scheduling registry_read 新建 + scheduling.go 仅哨兵行与 %w 臂） | 实际 diff 21 文件逐一落在追认后集合内；server.go 净 +1 行 | 成立 |
| SetupAutomation 锚点偏移最小化（implement 轮试合实测 2237→2237，偏移 0） | 集成轮对 server.go 零改动（树同哈希可证） | 成立 |
| D5 Healthy 不进登记面 / D3 expect 必填 400 / D2 position 报错语义变更风险≈0 | 全量档相关包全绿，无回归红 | 成立 |
| D6：translateRegistryErr 补 ErrCASConflict 翻译使 K2 CAS 重试链可用 | agentd/cmd 全包绿佐证链路活着；并发冲突微观行为本节点未单独构造探针复验（未验证，归 acceptance 可抽验） | 部分（如实标注） |
| 耗时 | plan 未给小时级预测（如实记）；集成轮本身一次通过（除环境红外零返工） | — |
| 「无风险」结论 vs 实测 | 无被推翻项；两轮修复的 3 major 均在 review/implement 轮暴露，集成轮零新增 | 成立 |

## 问一/问二增量核（集成轮自身）

- 集成轮新增导出符号 = **0**（合并树与已验收目标树同哈希；集成轮只追加本文档与
  roadmap 一节，均非 .go）。新增绑定点 = 0；`scheduling.New(` 组装点保持唯一
  （SetupAutomation，implement 轮读数改前=1 改后=1，本轮未触碰）。
- 新增依赖方向佐证：34 条 legacy 边改前/改后逐一相等 → **零新增依赖方向**。

## 棘轮

- d_gateway→d_scheduling 预算维持 0（两侧 graph check 该边零命中）；34 条存量边
  计数无一下降，无可提请下调项。本次变更集不做预算调整。

## 交棒（集成完成 ≠ 完成）

- **下一步：acceptance。真机清单 3 条，全部「未执行」，登记如下**：
  1. 活体 agentd 上 `handoff squad create/set/list` 全回路冒烟（机内只有 httptest
     命令级测试）；
  2. GET/PUT /api/squads 与 GET /api/queue 经真 HTTP + 鉴权 token 的浏览器/API 冒烟
     （机内有 wire 往返测试，无真机腿）;
  3. `handoff card coordinate <卡号>` 在 K4 launch 实装后的端到端复核（当前服务端
     为冻结路径 stub，K3 只锁命令形状）。
- 并入主线、归档收口 = 协调者裁决；本节点交付至「集成分支就绪 + 报告」为止。

## 收尾自查

① 有没有把没亲自跑到结果的命令写成结论？——三档读数、图对照、vitest、npm、基线
   对照均为本节点实跑并留原始输出文件（/root/.handoff/tmp/5549b33f/fulltest-r*.log、
   graph-{baseline,merged}.json）；D6 微观行为一处明标「未验证」。
② 本轮碰过 handoff CLI 写操作或起过新 executor 吗？——没有；仅 `go run . graph
   views/check`（只读本地图数据，平台不变量允许），无 dispatch/wait/reply，无子任务。
