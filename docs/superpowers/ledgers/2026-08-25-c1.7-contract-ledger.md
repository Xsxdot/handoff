# C1.7 handoff 宿主侧挂载契约台账

- 2026-08-25：已读取 `/root/.codex/skills/handoff/SKILL.md` 全文；本任务执行者不调用 handoff CLI、不派发子任务、不启动新的 executor。
- 2026-08-25：命令 `pwd && git status --short --branch` 原始输出：`/root/.handoff/worktrees/b14b58a7`、`## cards/C1.7-charter`；状态无未提交改动。
- 2026-08-25：命令 `sed -n '1,260p' docs/superpowers/specs/2026-08-24-codegraph-host-mount-spec.md` 原始输出确认 spec 头部状态为：`状态：**已批准**（2026-08-24；用户预授权发 charter graph 新 tag 到 GitHub、重打包 handoff 并重启 agentd——两项均为本刀的法定步骤）`。
- 2026-08-25：命令 `find cards/C1.7-charter -maxdepth 2 -type f -print ...` 原始输出：`find: ‘cards/C1.7-charter’: No such file or directory`；未发现仓内该卡目录，契约以注入计划与已批准 spec 为上游依据。
- 2026-08-25：命令 `rg --files -g '*ledger*' -g '*台账*' -g '.agents/**' -g '.codex/**'` 原始输出列出既有台账目录但无 C1.7 专用文件；据此新建本台账文件。
- 2026-08-25：命令 `sed -n '1,120p' go.mod` 原始输出含 `module github.com/Xsxdot/handoff` 与 `github.com/Xsxdot/charter/graph v0.6.0`，且未见 `replace`；当前契约坐标需升至协调者已验证可消费的 `v0.7.0`。
- 2026-08-25：命令 `sed -n '470,575p' internal/agentd/server.go` 原始输出确认 `api.HandleFunc("GET /api/projects/{name}/codegraph", s.handleProjectCodegraph)`、`api.HandleFunc("GET /api/projects/{name}/codegraph/source", s.handleProjectCodegraphSource)`，当前静态根为 `mux.Handle("/", newSPAHandler(webui.FS(), s.log))`，外层为 `root.Handle("/", s.auth(mux))`。
- 2026-08-25：命令 `sed -n '1,180p' internal/agentd/webhandler.go` 原始输出确认现状签名为 `func newSPAHandler(fsys fs.FS, log *slog.Logger) http.Handler`；其行为为 GET/HEAD 命中文件伺服、未命中回落 `index.html`、其它方法 405、index 缺失 500，且 handler 本身不做鉴权。
- 2026-08-25：命令 `find web/src/app/codegraph -maxdepth 2 -type f -print | sort` 原始输出列出旧副本 19 个文件；命令 `rg -n 'CodegraphPage|fetchCodegraph|fullPageRoute|FileTree|Breadcrumb' ...` 原始输出确认 `Shell.tsx:53` 导入 `CodegraphPage`、`:491` 挂载 `<Route path="/codegraph" element={<CodegraphPage />} />`、`:391` 当前白名单为 `['/cards', '/flows', '/settings', '/machines']`，并且 `:478` 与 `:567` 均以 `!fullPageRoute` 判断。
- 2026-08-25：命令 `find internal/webui -maxdepth 2 -type f -print -exec sed -n '1,120p' {} \;` 原始输出确认 handoff 自有 `internal/webui.FS() fs.FS` 与 `Embedded()`，默认 FS 为 stub，embedweb FS 为 `fs.Sub(distFS, "dist")`；因此 charter FS 与 handoff FS 必须使用不同路由前缀。
- 2026-08-25：尝试命令 `GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go mod download -json github.com/Xsxdot/charter/graph@v0.7.0` 失败，原始错误为：`github.com/Xsxdot/charter/graph@v0.7.0: verifying go.mod: github.com/Xsxdot/charter/graph@v0.7.0/go.mod: open /root/go/pkg/sumdb/sum.golang.org/latest: read-only file system`；输出同时确认下载来源为 `https://github.com/Xsxdot/charter`、tag `graph/v0.7.0`、commit `b8edeedcb363437fb050ca425bd0f59eae8979fc`。未将失败归因于依赖内容。
- 2026-08-25：重试命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go mod download -json github.com/Xsxdot/charter/graph@v0.7.0` 实际成功；原始输出确认 `Version: v0.7.0`、`Dir: /root/.handoff/tmp/b14b58a7/gomodcache/github.com/!xsxdot/charter/graph@v0.7.0`、`Sum: h1:RZSSEF8B8jTHZeLktmK...`、来源 tag `refs/tags/graph/v0.7.0`。
- 2026-08-25：命令 `nl -ba .../graph@v0.7.0/webui/webui.go` 原始输出确认 `webui.FS() fs.FS` 位于依赖 `webui/webui.go:18`，实现 `fs.Sub(distFS, "dist")`（`:19-23`），根直接包含 `index.html` 且 malformed embed 才 panic；`:13` 为 `//go:embed all:dist`。同包测试 `webui/webui_test.go:10-37` 实测契约为根有 index、至少 3 个文件、根不暴露外层 `dist`。
- 2026-08-25：命令 `nl -ba .../graph@v0.7.0/webui/vite.config.ts` 原始输出确认 `base: './'`（`:10`）；`webui/src/api/client.ts:47-48` 的请求固定 `fetch(path, { credentials: 'same-origin' })`，`:58` 与 `:64` 分别生成两条现有 API 路径并对项目/file 做 `encodeURIComponent`；`CodegraphPage.tsx:28-29` 仅从 iframe 自身 `?project=` 读取项目。
- 2026-08-25：命令 `nl -ba /usr/local/go/src/net/http/{fs.go,server.go}` 原始输出确认标准库 `http.FileServerFS(root fs.FS) Handler` 在 `fs.go:975-985`，`http.StripPrefix(prefix string, h Handler)` 在 `server.go:2328-2344`，会精确移除 URL Path/RawPath 前缀后调用下游，前缀不匹配直接 404；宿主契约采用这两个已查证行为，不自写静态伺服器。
- 2026-08-25：命令 `nl -ba internal/agentd/server.go` 原始输出确认当前 import 仅有 handoff `internal/webui`（`:50`），装配处 API 路由为 `:509-510`，静态兜底为 `:542`，auth 包在 `:548`；charter 依赖应以别名导入并在同一 auth 内 mux 注册专属 `/codegraph/app/` 前缀。
- 2026-08-25：命令 `nl -ba web/src/app/workbench/useWorkbench.ts` 与 `ProjectTree.tsx` 原始输出确认 `BaseDir` 在 `useWorkbench.ts:38-50` 含 `projectName: string`；`workspaceBase` 在 `ProjectTree.tsx:159-168` 将 `project.name` 写入该字段；因此 `Shell` 可把当前 `wb.base?.projectName ?? ''` 作为薄壳唯一项目入参。
- 2026-08-25：命令 `nl -ba .../graph@v0.7.0/webui/src/app/codegraph/useCodegraph.ts` 原始输出确认 viewer 在 `:11-14` 对空项目跳过请求，在 `:18` 调 `fetchCodegraph(project)`；`main.tsx:5-7` 直接渲染 `CodegraphPage`，不提供宿主选择器。薄壳不应复制这些逻辑。
- 2026-08-25：命令 `diff -qr .../graph@v0.6.0 .../graph@v0.7.0` 原始输出仅列出 `webui/dist/**` 与 `webui/src/app/codegraph/**` 的新增/变化（含 `BestLeafGraph`、`BestOverlays`、`BestScopePanorama`），未列出 `codegraph/**` 文件差异；据此冻结 v0.7.0 为 viewer 换代坐标，不新增 codegraph wire 响应契约。
- 2026-08-25：命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go test ./cmd -run TestRepoContractGate -count=1` 实际通过，原始末行：`ok   github.com/Xsxdot/handoff/cmd 0.036s`；现有 target/best/baseline 在未改动前无契约 fail。
- 2026-08-25：命令 `rg -n -C ... handleProjectCodegraph ... internal/agentd` 原始输出确认 `internal/agentd/codegraph.go#Server.handleProjectCodegraph` 在 `:27-104` 与 `#Server.handleProjectCodegraphSource` 在 `:109` 均先调用 `forwardIfRequested`；`internal/agentd/forward.go#Server.forwardIfRequested`（`:54-73`）对非空 `?machine=` 执行一跳转发并以 `X-Handoff-Forwarded` 防环。v0.7.0 viewer client 未生成 `machine` 查询，故本刀不扩展 viewer 输入，不修改该现有 forwarding 语义。
- 2026-08-25：用 `apply_patch` 落盘 `docs/superpowers/specs/c1.7-contract.md` 与 `codegraph/target.json`；目标图新增 `internal/agentd/server.go` 为 mux 组装点，本文冻结 v0.7.0、`/codegraph/app/`、`CodegraphFrame` 与 fullPageRoute 原子清单；本轮未落 Go/TS Ticket 0 代码，未创建分支视图 diff。
- 2026-08-25：命令 `git diff --check` 实际退出 `0` 且无输出；命令 `python3 -m json.tool codegraph/target.json >/dev/null && printf 'target-json=ok\\n'` 实际输出 `target-json=ok`、退出 `0`。
- 2026-08-25：命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go test ./cmd -run TestRepoContractGate -count=1` 在目标图新增组装点后实际通过，原始输出：`ok  github.com/Xsxdot/handoff/cmd 0.039s`。
- 2026-08-25：首次命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go run . graph resolve --doc docs/superpowers/specs/c1.7-contract.md` 实际失败；原始错误末行：`Error: 文档锚点检查失败: github.com/Xsxdot/charter/graph/webui/webui.go#FS (file_missing)`、`exit status 1`。同时输出还列出依赖模块的 `CodegraphPage`/`useCodegraph`/client 锚点及一处裸 `Shell.tsx#Shell` 为 `file_missing`；判断：跨仓依赖改用 path:line 证据，仓内裸锚补全路径。
- 2026-08-25：第一次 `apply_patch` 锚点替换失败，原始输出：`apply_patch verification failed: Failed to find expected lines in .../docs/superpowers/specs/c1.7-contract.md`；未产生文件改动，改为按当前文档行内容分两次精确替换。
- 2026-08-25：修复后再次执行 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go run . graph resolve --doc docs/superpowers/specs/c1.7-contract.md` 实际成功，原始输出列出 7 个仓内锚点且均为 `"anchor": "ok"`（`newSPAHandler`、`Server.Handler`、两条 codegraph/forward handler、`Shell`、`BaseDir`、`workspaceBase`），进程退出 `0`；仅有既有 relay/proxy INFO 日志，无解析错误。
- 2026-08-25：首次提交前命令 `git add codegraph/target.json docs/superpowers/specs/c1.7-contract.md docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/b14b58a7/index.lock': Read-only file system`；未宣称已暂存或提交。
- 2026-08-25：提升权限重跑同一 `git add ...` 命令实际成功，原始输出为空、退出码 `0`；之后台账追加了本条，需在最终提交前重新暂存台账。
- 2026-08-25：再次提升权限执行同一 `git add ...` 实际成功，原始输出为空、退出码 `0`；暂存区包含 `codegraph/target.json`、本台账与 `docs/superpowers/specs/c1.7-contract.md`。
- 2026-08-25：提交前命令 `git diff --cached --check` 实际退出 `0`、无输出；`git diff --cached --stat` 实际输出 `3 files changed, 265 insertions(+), 2 deletions(-)`；`git diff --cached --name-status` 实际输出 `M codegraph/target.json`、`A docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md`、`A docs/superpowers/specs/c1.7-contract.md`；`git status --short --branch` 实际为 `## cards/C1.7-charter` 加上述三项 staged，无 unstaged 项。
- 2026-08-25：首次命令 `git commit -m "docs(contract): freeze C1.7 viewer host mount"` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/b14b58a7/index.lock': Read-only file system`；未宣称已提交。
- 2026-08-25：提升权限执行 `git commit -m "docs(contract): freeze C1.7 viewer host mount"` 实际成功，原始输出：`[cards/C1.7-charter bd2e7a24] docs(contract): freeze C1.7 viewer host mount`、`3 files changed, 268 insertions(+), 2 deletions(-)`；该提交包含契约文档、目标图与台账，未 push。
- 2026-08-25：提升权限执行 `git add docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md` 后，命令 `git commit -m "docs(ledger): record C1.7 contract freeze"` 实际成功，原始输出：`[cards/C1.7-charter 9806ffff] docs(ledger): record C1.7 contract freeze`、`1 file changed, 1 insertion(+)`；未 push。
- 2026-08-25：收口复核命令 `git status --short --branch` 实际输出 `## cards/C1.7-charter` 与台账一项未提交修改（本条追加造成，待最终提交收口）。
- 2026-08-25：收口复核命令 `git log -3 --oneline --decorate` 实际输出 `9806ffff (HEAD -> cards/C1.7-charter) docs(ledger): record C1.7 contract freeze`、`bd2e7a24 docs(contract): freeze C1.7 viewer host mount`、`d4053c0a (origin/main, origin/HEAD) docs(spec): C1.7 spec 批准回写`；目标图与契约首提交 `bd2e7a24` 已在当前分支历史。
- 2026-08-25：收口复核命令 `git show --stat --oneline --summary bd2e7a24` 实际输出该冻结提交包含 `codegraph/target.json`、本台账、`docs/superpowers/specs/c1.7-contract.md`，统计 `3 files changed, 268 insertions(+), 2 deletions(-)`；`git diff --check HEAD~2..HEAD` 实际退出 `0` 无输出；`git diff --name-only HEAD~2..HEAD` 实际列出上述三文件。
- 2026-08-25：当前工作树命令 `git status --short --branch && git branch --show-current && git log -1 --format='%H %D'` 原始输出为 `## cards/C1.7-charter-2`、`cards/C1.7-charter-2`、`c0161cd0... HEAD -> cards/C1.7-charter-2, cards/C1.7-charter`；本节点只在该当前分支工作。
- 2026-08-25：命令 `jq -r '.domains | to_entries[] | select(.value.parent == null) | [.key,.value.type,.value.label] | @tsv' codegraph/best.json` 实际列出 12 个顶层子系统；本刀直接候选为 `d_gateway boundary`、`d_web logic`、`d_maintenance boundary`，另有其余顶层域均无本刀文件入口。
- 2026-08-25：命令 `sed -n '1,260p' codegraph/target.json` 实际确认冻结目标图的 `assembly` 为 `main.go` 与 `internal/agentd/server.go`，且恢复后的 `d_cli → d_ledger` contracts.entries 含 `ledger.Store`；本刀不修改该既有条目。
- 2026-08-25：命令 `sed -n '1,120p' desktop/go.mod` 与 `rg -n 'charter/graph|v0\\.[0-9]+' go.mod go.sum desktop/go.mod desktop/go.sum .github/workflows/ci.yml` 实际确认主模块直接依赖 `charter/graph v0.6.0`，`desktop/go.mod` 通过 `replace github.com/Xsxdot/handoff => ../` 接入主模块，`desktop/go.sum` 仍含 `charter/graph v0.3.0`；CI 的 desktop 依赖对齐步骤在 Windows 交叉编译与 install.sh 单测之前。
- 2026-08-25：命令 `find web/src/app/codegraph -maxdepth 2 -type f -print | sort` 实际确认 handoff 旧 codegraph 副本为 19 个文件；`Shell.tsx` 仍在同一文件中导入/渲染旧 `CodegraphPage`，fullPageRoute 白名单未含 `/codegraph`，Breadcrumb 与 FileTree 两处均消费 `!fullPageRoute`。
- 2026-08-25：命令 `sed -n '100,210p' docs/codegraph-best-rationale.md` 实际确认 `d_gateway` 的职责是鉴权/路由门面、`d_maintenance` 的职责是版本落机/服务托管，且代码图核心已外迁 charter；本刀宿主静态挂载归 d_gateway，desktop 依赖/CI 对齐归 d_maintenance，handoff 薄壳与 Shell 接线归 d_web。
- 2026-08-25：命令 `rg -n '第一条（子系统的资格）|派卡资格' /root/.codex/sessions/2026/08/23/rollout-2026-08-23T13-46-54-01a02d28-58c9-7d82-a5d2-cbd893dfc096.jsonl` 实际回显架构法第一条的派卡资格四条：有界文件集、可枚举契约面、同级依赖可排 DAG、逻辑型/边界型标注；本稿逐候选域显式核对。
- 2026-08-25：命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/4e3a7bf0/gomodcache go test ./cmd -run TestRepoContractGate -count=1` 实际通过；原始末行为 `ok   github.com/Xsxdot/handoff/cmd 0.036s`，前置下载日志未改变结论。
- 2026-08-25：用 `apply_patch` 在 `docs/superpowers/specs/c1.7-contract.md` 新增第 10 节 Breakdown 边界澄清：保留 `web/src/api/client.ts` 的既有 wire helper、将 desktop 依赖对齐限定为既有 CI 红的收口检查、明确不触碰恢复后的 `ledger.Store`；同时声明不改变第 1—9 节冻结语义、不新增接缝。
- 2026-08-25：命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/4e3a7bf0/gomodcache go run . graph resolve --doc docs/superpowers/specs/c1.7-breakdown.md` 实际成功，原始 JSON 列出 7 个锚点且均为 `"anchor": "ok"`：`Server.Handler`、`newSPAHandler`、`Shell`、`Server.handleProjectCodegraph`、`Server.forwardIfRequested`、`BaseDir`、`workspaceBase`；末尾仅有 relay/proxy INFO 日志，无解析错误。
- 2026-08-25：命令 `git diff --check` 实际退出 0 且无输出；`wc -l` 实际输出 breakdown 327 行、contract 245 行、本台账 48 行；占位符扫描 `rg -n 'TBD|TODO|同 Task|适当的错误|功能应正常|无风险' docs/superpowers/specs/c1.7-breakdown.md || true` 无匹配。
- 2026-08-25：命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/4e3a7bf0/gomodcache go test ./cmd -run TestRepoContractGate -count=1` 在契约澄清与 breakdown 落盘后实际通过，原始输出：`ok  github.com/Xsxdot/handoff/cmd 0.032s`。
- 2026-08-25：命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/4e3a7bf0/gomodcache go run . graph resolve --doc docs/superpowers/specs/c1.7-contract.md` 实际成功，7 个仓内锚点均为 `anchor: ok`；命令 `python3 -m json.tool codegraph/target.json >/dev/null`、`git diff --check` 均实际退出 0 且无输出。
- 2026-08-25：命令 `git status --short --branch` 实际输出 `## cards/C1.7-charter-2`，当前有修改 `docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md`、`docs/superpowers/specs/c1.7-contract.md` 与未跟踪 `docs/superpowers/specs/c1.7-breakdown.md`；未修改源码、`codegraph/target.json` 或 CI。
- 2026-08-25：首次执行 `git add docs/superpowers/specs/c1.7-breakdown.md docs/superpowers/specs/c1.7-contract.md docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md` 失败；原始错误：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/4e3a7bf0/index.lock': Read-only file system`。未宣称已暂存，改用已验证可行的提升权限重试。
- 2026-08-25：提升权限重跑同一 `git add ...` 命令实际成功，原始输出为空、退出码 0；breakdown、contract 澄清和台账进入暂存区，台账随后追加本条，提交前需重新暂存。
- 2026-08-25：命令 `git diff --cached --check && git diff --cached --stat && git diff --cached --name-status && git status --short --branch` 未执行后续段；原始错误为 `docs/superpowers/specs/c1.7-breakdown.md:327: new blank line at EOF.`。已删除 EOF 多余空行，需重新暂存后复跑。
- 2026-08-25：修正 EOF 后提升权限执行同一 `git add ...` 实际成功，原始输出为空、退出码 0；最新 breakdown、contract 与台账均已暂存，台账随后追加本条，提交前仍需最后一次暂存。
- 2026-08-25：命令 `git diff --cached --check && git diff --cached --stat && git diff --cached --name-status && git status --short --branch` 在 EOF 修正后实际完成：`git diff --cached --check` 无输出退出 0；stat 为 `3 files changed, 357 insertions(+)`；name-status 为台账 M、breakdown A、contract M；status 显示台账有一条最新追加未暂存，需最后暂存。
- 2026-08-25：再次提升权限执行 `git add docs/superpowers/specs/c1.7-breakdown.md docs/superpowers/specs/c1.7-contract.md docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md` 实际成功，原始输出为空、退出码 0；随后追加本条，需再暂存一次。
- 2026-08-25：再次执行 `git diff --cached --check && git diff --cached --stat && git diff --cached --name-status && git status --short --branch` 实际完成：check 无输出退出 0；stat 为 `3 files changed, 359 insertions(+)`；name-status 为台账 M、breakdown A、contract M；status 仅显示台账因本条追加而有 unstaged 尾差。
- 2026-08-25：首次执行 `git commit -m "docs(breakdown): plan C1.7 host viewer mount"` 失败；原始错误：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/4e3a7bf0/index.lock': Read-only file system`。暂存内容未被宣称已提交，改用提升权限重试。
- 2026-08-25：提升权限执行 `git commit -m "docs(breakdown): plan C1.7 host viewer mount"` 实际成功；原始输出：`[cards/C1.7-charter-2 6962c3e8] docs(breakdown): plan C1.7 host viewer mount`、`3 files changed, 362 insertions(+)`，未 push。
- 2026-08-25：提交后命令 `git status --short --branch && git log -1 --oneline --decorate && git show --stat --oneline --summary HEAD` 实际输出 `## cards/C1.7-charter-2`、`6962c3e8 (HEAD -> cards/C1.7-charter-2) docs(breakdown): plan C1.7 host viewer mount`，统计为台账 22 行增量、breakdown 326 行新增、contract 14 行增量；提交后暂存/未跟踪均为空。
- 2026-08-25：提升权限执行 `git commit -m "docs(ledger): record C1.7 breakdown commit"` 实际成功；原始输出：`[cards/C1.7-charter-2 37d380d7] docs(ledger): record C1.7 breakdown commit`、`1 file changed, 2 insertions(+)`，未 push。
- 2026-08-25：提交后命令 `git status --short --branch && git log -2 --oneline --decorate && git show --stat --oneline --summary HEAD` 实际输出 `## cards/C1.7-charter-2`、HEAD 为 `37d380d7 docs(ledger): record C1.7 breakdown commit`、上一提交为 `6962c3e8 docs(breakdown): plan C1.7 host viewer mount`，HEAD 统计为台账 2 行新增；status 无未提交/未跟踪项。
- 2026-08-25：当前执行树命令 `git merge-base --is-ancestor dd95a6e2d HEAD` 与 `git merge-base --is-ancestor b29fc0f45 HEAD` 均失败，原始错误均为 `fatal: Not a valid object name ...`；当前 `go.mod` 仍为 `github.com/Xsxdot/charter/graph v0.6.0`，`desktop/go.sum` 仍只有 `v0.3.0`，未满足本轮补充声明的基线前提，按指令先同步 `origin/main`。
- 2026-08-25：首次执行 `git fetch origin && git merge origin/main` 失败，原始错误为 `error: cannot open '/root/.handoff/repos/handoff/.git/worktrees/af3a1b69/FETCH_HEAD': Read-only file system`；未宣称已 fetch 或 merge。
- 2026-08-25：提升权限重跑 `git fetch origin && git merge origin/main` 实际成功；原始输出确认 `main d4053c0a..b29fc0f4`，并以 merge strategy `ort` 完成，退出码 0，无冲突报错；当前树已含补充要求的 `b29fc0f4` 基线。
- 2026-08-25：合并后复核命令实际输出 `HEAD 0bd11bca`、`b29fc0f45 ancestor rc=0`、根 `go.mod` 为 `github.com/Xsxdot/charter/graph v0.6.0`、`desktop/go.sum` 为 `v0.6.0` 两条 hash 记录；工作树仅本台账有未提交修改。`web/package.json` 的前端门为 `typecheck: tsc -b`、`build: tsc -b && vite build`、`test: vitest run`；现有宿主测试夹具为 `internal/agentd/webroute_test.go#consoleTestEnv`。
- 2026-08-25：基线命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go build ./...` 实际退出码 `0`，原始 stdout 仅为多行 `go: downloading ...`，无错误输出；基线 Go 构建通过。
- 2026-08-25：基线命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go test ./cmd -run TestRepoContractGate -count=1` 实际退出码 `0`，原始末行 `ok  github.com/Xsxdot/handoff/cmd 0.037s`；契约门通过。
- 2026-08-25：基线读数命令在根模块与 `desktop/` 分别实际输出 `github.com/Xsxdot/charter/graph v0.6.0`，均退出码 `0`；`git diff --exit-code -- .github/workflows/ci.yml` 实际退出码 `0` 且无输出；`npm --prefix web run typecheck` 实际退出码 `127`，原始错误为 `sh: 1: tsc: not found`（前置脚本原样输出 `> web@0.0.0 typecheck`、`> tsc -b`），当前 `web/node_modules` 不存在，前端判据未在基线通过。
- 2026-08-25：基线命令 `npm --prefix web run build` 实际退出码 `127`，原始错误为 `sh: 1: tsc: not found`；基线命令 `npm --prefix web test -- Shell.test.tsx` 实际退出码 `127`，原始错误为 `sh: 1: vitest: not found`；两者均未宣称前端行为通过。
- 2026-08-25：基线命令 `cd desktop && GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go mod tidy && git diff --exit-code -- go.mod go.sum` 实际退出码 `0`；原始 stdout 仅为依赖下载行，无 diff 输出，desktop v0.6.0 状态已 tidy-clean。
- 2026-08-25：源码签名复核命令实际确认 `Server.Handler() http.Handler`（`internal/agentd/server.go:432`）、`newSPAHandler(fsys fs.FS, log *slog.Logger) http.Handler`（`internal/agentd/webhandler.go:58`）、`Server.auth(next http.Handler) http.Handler`（`internal/agentd/server.go:575`）、`useWorkbench() WorkbenchApi`（`web/src/app/workbench/useWorkbench.ts:145`）、`BaseDir.projectName string`（`:38-50`）；Shell 当前 `/codegraph` 仍渲染 `CodegraphPage`（`:491`），Breadcrumb/FileTree 分别消费同一 `fullPageRoute`（`:478`、`:567`）。
- 2026-08-25：命令 `sed -n '70,150p' docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md` 实际确认本项目缺陷族为通用五族（生命周期/状态机中断、静默失败/误导报错、跨平台假设、假红测试、门禁绕过）+ 序列化边界 + 新枚举白名单 + Web 控制台的 webview/平台表现差异候选族；计划需逐项给出结论。
- 2026-08-25：使用 `apply_patch` 写入法定计划 `docs/superpowers/plans/c1.7-plan.md`；本次仅新增计划文件，未修改 Go/TS/CI、contract/spec/breakdown 或 `codegraph/target.json`。
- 2026-08-25：计划自检命令 `git diff --check` 实际退出码 `0` 且无输出；占位符扫描 `rg -n 'TBD|TODO|同 Task|适当的错误处理' docs/superpowers/plans/c1.7-plan.md` 实际退出码 `1` 且无输出；结构扫描实际命中 T1/T2/T3 的 `Consumes`/`Produces`、T1→T3/T2 顺序、真机协调者执行标记与 v0.7.0、/codegraph/app/、fullPageRoute、缺陷族、接缝矩阵；当前 `git diff --name-only` 仅为本台账，计划为未跟踪文件。
- 2026-08-25：计划版本/范围扫描 `rg -n 'v0\\.3\\.0|v0\\.4\\.0|v0\\.6\\.0|v0\\.7\\.0|子卡|独立提交|若另一会话|既有 CI 红' docs/superpowers/plans/c1.7-plan.md` 实际只命中基线 v0.6.0、目标 v0.7.0、明确“无子卡/无独立提交”和 T1→T3 顺序说明；未出现旧目标 v0.3.0/v0.4.0 或条件分支措辞。
- 2026-08-25：计划文件复核命令 `wc -l ... && tail -35 docs/superpowers/plans/c1.7-plan.md && git diff --check && git status --short --branch` 实际输出计划 710 行、台账 79 行；`git diff --check` 退出 0 且无输出；工作树仅台账修改、计划未跟踪。
- 2026-08-25：首次执行 `git add docs/superpowers/plans/c1.7-plan.md docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md` 失败；原始错误为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/af3a1b69/index.lock': Read-only file system`，未宣称已暂存。
- 2026-08-25：提升权限执行同一 `git add ...` 实际成功；随后 `git diff --cached --check && git diff --cached --stat && git diff --cached --name-status && git status --short --branch` 实际完成：cached check 退出 0 无输出，统计 `2 files changed, 726 insertions(+)`，name-status 为台账 M、计划 A，分支 `cards/C1.7-charter-3`。
- 2026-08-25：最终提交前复核 `git diff --cached --check && git diff --cached --stat && git diff --cached --name-status && git status --short --branch` 实际完成：cached check 退出 0 无输出，统计 `2 files changed, 727 insertions(+)`，name-status 仍仅为本台账 M 与计划 A，分支 `cards/C1.7-charter-3`，无其它 staged 路径。
- 2026-08-25：提升权限执行 `git add docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md && git commit -m "docs(plan): plan C1.7 host viewer mount"` 实际成功；原始输出为 `[cards/C1.7-charter-3 1bf23836] docs(plan): plan C1.7 host viewer mount`、`2 files changed, 728 insertions(+)`、`create mode 100644 docs/superpowers/plans/c1.7-plan.md`，未 push；本条台账追加待下一收口提交。
- 2026-08-25：提交后复核 `git status --short --branch && git log -2 --oneline --decorate && git show --stat --oneline --summary HEAD && git diff --check` 实际输出分支 `cards/C1.7-charter-3`、HEAD `1bf23836 docs(plan): plan C1.7 host viewer mount`、上一提交 `0bd11bca Merge remote-tracking branch 'origin/main' into cards/C1.7-charter-3`；HEAD 统计为计划 710 行与台账 18 行，`git diff --check` 退出 0；当前仅本条台账未提交。
- 2026-08-25：提升权限执行 `git add docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md && git commit -m "docs(ledger): record C1.7 plan commit"` 实际成功；原始输出为 `[cards/C1.7-charter-3 334700ba] docs(ledger): record C1.7 plan commit`、`1 file changed, 2 insertions(+)`，未 push。
- 2026-08-25：最终复核 `git status --short --branch && git log -3 --oneline --decorate && git show --stat --oneline --summary HEAD && git diff --check && git ls-files docs/superpowers/plans/c1.7-plan.md docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md` 实际输出仅分支行 `## cards/C1.7-charter-3`（无工作树改动）、HEAD `334700ba`、计划与台账均已被 git ls-files 列出；HEAD 统计为台账 2 行新增，`git diff --check` 退出 0。
