# C1.7 handoff 宿主侧挂载契约台账

- 2026-08-25：已读取 `/root/.codex/skills/handoff/SKILL.md` 全文；本任务执行者不调用 handoff CLI、不派发子任务、不启动新的 executor。
- 2026-08-25：命令 `pwd && git status --short --branch` 原始输出：`/root/.handoff/worktrees/b14b58a7`、`## cards/C1.7-charter`；状态无未提交改动。
- 2026-08-25：命令 `sed -n '1,260p' docs/superpowers/specs/2026-08-24-codegraph-host-mount-spec.md` 原始输出确认 spec 头部状态为：`状态：**已批准**（2026-08-24；用户预授权发 charter graph 新 tag 到 GitHub、重打包 handoff 并重启 agentd——两项均为本刀的法定步骤）`。
- 2026-08-25：命令 `find cards/C1.7-charter -maxdepth 2 -type f -print ...` 原始输出：`find: ‘cards/C1.7-charter’: No such file or directory`；未发现仓内该卡目录，契约以注入计划与已批准 spec 为上游依据。
- 2026-08-25：命令 `rg --files -g '*ledger*' -g '*台账*' -g '.agents/**' -g '.codex/**'` 原始输出列出既有台账目录但无 C1.7 专用文件；据此新建本台账文件。
- 2026-08-25：命令 `sed -n '1,120p' go.mod` 原始输出含 `module github.com/Xsxdot/handoff` 与 `github.com/Xsxdot/charter/graph v0.6.0`，且未见 `replace`；当前契约坐标需升至协调者已验证可消费的 `v0.7.0`。
- 2026-08-25：命令 `sed -n '470,575p' internal/agentd/server.go` 原始输出确认 `api.HandleFunc("GET /api/projects/{name}/codegraph", s.handleProjectCodegraph)`、`api.HandleFunc("GET /api/projects/{name}/codegraph/source", s.handleProjectCodegraphSource)`，当前静态根为 `mux.Handle("/", newSPAHandler(webui.FS(), s.log))`，外层为 `root.Handle("/", s.auth(mux))`。
- 2026-08-25：命令 `sed -n '1,180p' internal/agentd/webhandler.go` 原始输出确认现状签名为 `func newSPAHandler(fsys fs.FS, log *slog.Logger) http.Handler`；其行为为 GET/HEAD 命中文件伺服、未命中回落 `index.html`、其它方法 405、index 缺失 500，且 handler 本身不做鉴权。
- 2026-08-25：命令 `find web/src/app/codegraph -maxdepth 2 -type f -print | sort` 原始输出列出旧副本 19 个文件；命令 `rg -n 'CodegraphPage|fetchCodegraph|fullPageRoute|FileTree|Breadcrumb' ...` 原始输出确认 `Shell.tsx:53` 导入 `CodegraphPage`、`:491` 挂载 `<Route path="/codegraph" element={<CodegraphPage />} />`、`:391` 当前白名单为 `['/cards', '/flows', '/settings', '/machines']`，并且 `:478` 与 `:567` 均以 `!fullPageRoute` 判断。
- 2026-08-25：命令 `find internal/webui -maxdepth 2 -type f -print -exec sed -n '1,120p' {} \;` 原始输出确认 handoff 自有 `internal/webui.FS() fs.FS` 与 `Embedded()`，默认 FS 为 stub，embedweb FS 为 `fs.Sub(distFS, "dist")`；因此 charter FS 与 handoff FS 必须使用不同路由前缀。
- 2026-08-25：尝试命令 `GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go mod download -json github.com/Xsxdot/charter/graph@v0.7.0` 失败，原始错误为：`github.com/Xsxdot/charter/graph@v0.7.0: verifying go.mod: github.com/Xsxdot/charter/graph@v0.7.0/go.mod: open /root/go/pkg/sumdb/sum.golang.org/latest: read-only file system`；输出同时确认下载来源为 `https://github.com/Xsxdot/charter`、tag `graph/v0.7.0`、commit `b8edeedcb363437fb050ca425bd0f59eae8979fc`。未将失败归因于依赖内容。
- 2026-08-25：重试命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go mod download -json github.com/Xsxdot/charter/graph@v0.7.0` 实际成功；原始输出确认 `Version: v0.7.0`、`Dir: /root/.handoff/tmp/b14b58a7/gomodcache/github.com/!xsxdot/charter/graph@v0.7.0`、`Sum: h1:RZSSEF8B8jTHZeLktmK...`、来源 tag `refs/tags/graph/v0.7.0`。
- 2026-08-25：命令 `nl -ba .../graph@v0.7.0/webui/webui.go` 原始输出确认 `webui.FS() fs.FS` 位于依赖 `webui/webui.go:18`，实现 `fs.Sub(distFS, "dist")`（`:19-23`），根直接包含 `index.html` 且 malformed embed 才 panic；`:13` 为 `//go:embed all:dist`。同包测试 `webui/webui_test.go:10-37` 实测契约为根有 index、至少 3 个文件、根不暴露外层 `dist`。
- 2026-08-25：命令 `nl -ba .../graph@v0.7.0/webui/vite.config.ts` 原始输出确认 `base: './'`（`:10`）；`webui/src/api/client.ts:47-48` 的请求固定 `fetch(path, { credentials: 'same-origin' })`，`:58` 与 `:64` 分别生成两条现有 API 路径并对项目/file 做 `encodeURIComponent`；`CodegraphPage.tsx:28-29` 仅从 iframe 自身 `?project=` 读取项目。
- 2026-08-25：命令 `nl -ba /usr/local/go/src/net/http/{fs.go,server.go}` 原始输出确认标准库 `http.FileServerFS(root fs.FS) Handler` 在 `fs.go:975-985`，`http.StripPrefix(prefix string, h Handler)` 在 `server.go:2328-2344`，会精确移除 URL Path/RawPath 前缀后调用下游，前缀不匹配直接 404；宿主契约采用这两个已查证行为，不自写静态伺服器。
- 2026-08-25：命令 `nl -ba internal/agentd/server.go` 原始输出确认当前 import 仅有 handoff `internal/webui`（`:50`），装配处 API 路由为 `:509-510`，静态兜底为 `:542`，auth 包在 `:548`；charter 依赖应以别名导入并在同一 auth 内 mux 注册专属 `/codegraph/app/` 前缀。
- 2026-08-25：命令 `nl -ba web/src/app/workbench/useWorkbench.ts` 与 `ProjectTree.tsx` 原始输出确认 `BaseDir` 在 `useWorkbench.ts:38-50` 含 `projectName: string`；`workspaceBase` 在 `ProjectTree.tsx:159-168` 将 `project.name` 写入该字段；因此 `Shell` 可把当前 `wb.base?.projectName ?? ''` 作为薄壳唯一项目入参。
- 2026-08-25：命令 `nl -ba .../graph@v0.7.0/webui/src/app/codegraph/useCodegraph.ts` 原始输出确认 viewer 在 `:11-14` 对空项目跳过请求，在 `:18` 调 `fetchCodegraph(project)`；`main.tsx:5-7` 直接渲染 `CodegraphPage`，不提供宿主选择器。薄壳不应复制这些逻辑。
- 2026-08-25：命令 `diff -qr .../graph@v0.6.0 .../graph@v0.7.0` 原始输出仅列出 `webui/dist/**` 与 `webui/src/app/codegraph/**` 的新增/变化（含 `BestLeafGraph`、`BestOverlays`、`BestScopePanorama`），未列出 `codegraph/**` 文件差异；据此冻结 v0.7.0 为 viewer 换代坐标，不新增 codegraph wire 响应契约。
- 2026-08-25：命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go test ./cmd -run TestRepoContractGate -count=1` 实际通过，原始末行：`ok   github.com/Xsxdot/handoff/cmd 0.036s`；现有 target/best/baseline 在未改动前无契约 fail。
- 2026-08-25：命令 `rg -n -C ... handleProjectCodegraph ... internal/agentd` 原始输出确认 `internal/agentd/codegraph.go#Server.handleProjectCodegraph` 在 `:27-104` 与 `#Server.handleProjectCodegraphSource` 在 `:109` 均先调用 `forwardIfRequested`；`internal/agentd/forward.go#Server.forwardIfRequested`（`:54-73`）对非空 `?machine=` 执行一跳转发并以 `X-Handoff-Forwarded` 防环。v0.7.0 viewer client 未生成 `machine` 查询，故本刀不扩展 viewer 输入，不修改该现有 forwarding 语义。
- 2026-08-25：用 `apply_patch` 落盘 `docs/superpowers/specs/c1.7-contract.md` 与 `codegraph/target.json`；目标图新增 `internal/agentd/server.go` 为 mux 组装点，本文冻结 v0.7.0、`/codegraph/app/`、`CodegraphFrame` 与 fullPageRoute 原子清单；本轮未落 Go/TS Ticket 0 代码，未创建分支视图 diff。
- 2026-08-25：命令 `git diff --check` 实际退出 `0` 且无输出；命令 `python3 -m json.tool codegraph/target.json >/dev/null && printf 'target-json=ok\\n'` 实际输出 `target-json=ok`、退出 `0`。
- 2026-08-25：命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go test ./cmd -run TestRepoContractGate -count=1` 在目标图新增组装点后实际通过，原始输出：`ok  github.com/Xsxdot/handoff/cmd 0.039s`。
- 2026-08-25：首次命令 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go run . graph resolve --doc docs/superpowers/specs/c1.7-contract.md` 实际失败；原始错误末行：`Error: 文档锚点检查失败: github.com/Xsxdot/charter/graph/webui/webui.go#FS (file_missing)`、`exit status 1`。同时输出还列出依赖模块的 `CodegraphPage`/`useCodegraph`/client 锚点及一处裸 `Shell.tsx#Shell` 为 `file_missing`；判断：跨仓依赖改用 path:line 证据，仓内裸锚补全路径。
- 2026-08-25：第一次 `apply_patch` 锚点替换失败，原始输出：`apply_patch verification failed: Failed to find expected lines in .../docs/superpowers/specs/c1.7-contract.md`；未产生文件改动，改为按当前文档行内容分两次精确替换。
- 2026-08-25：修复后再次执行 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/b14b58a7/gomodcache go run . graph resolve --doc docs/superpowers/specs/c1.7-contract.md` 实际成功，原始输出列出 7 个仓内锚点且均为 `"anchor": "ok"`（`newSPAHandler`、`Server.Handler`、两条 codegraph/forward handler、`Shell`、`BaseDir`、`workspaceBase`），进程退出 `0`；仅有既有 relay/proxy INFO 日志，无解析错误。
- 2026-08-25：首次提交前命令 `git add codegraph/target.json docs/superpowers/specs/c1.7-contract.md docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/b14b58a7/index.lock': Read-only file system`；未宣称已暂存或提交。
- 2026-08-25：提升权限重跑同一 `git add ...` 命令实际成功，原始输出为空、退出码 `0`；之后台账追加了本条，需在最终提交前重新暂存台账。
- 2026-08-25：再次提升权限执行同一 `git add ...` 实际成功，原始输出为空、退出码 `0`；暂存区包含 `codegraph/target.json`、本台账与 `docs/superpowers/specs/c1.7-contract.md`。
- 2026-08-25：提交前命令 `git diff --cached --check` 实际退出 `0`、无输出；`git diff --cached --stat` 实际输出 `3 files changed, 265 insertions(+), 2 deletions(-)`；`git diff --cached --name-status` 实际输出 `M codegraph/target.json`、`A docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md`、`A docs/superpowers/specs/c1.7-contract.md`；`git status --short --branch` 实际为 `## cards/C1.7-charter` 加上述三项 staged，无 unstaged 项。
- 2026-08-25：首次命令 `git commit -m "docs(contract): freeze C1.7 viewer host mount"` 失败，原始输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/b14b58a7/index.lock': Read-only file system`；未宣称已提交。
- 2026-08-25：提升权限执行 `git commit -m "docs(contract): freeze C1.7 viewer host mount"` 实际成功，原始输出：`[cards/C1.7-charter bd2e7a24] docs(contract): freeze C1.7 viewer host mount`、`3 files changed, 268 insertions(+), 2 deletions(-)`；该提交包含契约文档、目标图与台账，未 push。
