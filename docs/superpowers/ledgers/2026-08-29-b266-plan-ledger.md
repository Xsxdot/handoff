# B266 plan 节点台账

| 时间 | 事实 / 判断 | 命令与原始结果 |
|---|---|---|
| 2026-08-29 | 当前工作树在 `cards/B266-charter`，起点为 `5e8826f7338fed52e8a45017f8555de0675fc2c8`，无本地改动。 | `git status --short --branch` → `## cards/B266-charter` |
| 2026-08-29 | spec 不在基线工作树；用户指定从已更新的 `origin/main` 读取，未在本地补写 spec。 | `sed -n '...' docs/superpowers/specs/b266.md` → `sed: can't read docs/superpowers/specs/b266.md: No such file or directory` |
| 2026-08-29 | origin/main 已前进到包含 B266 spec 的提交。 | `git fetch origin` → `From github.com:Xsxdot/handoff` / `5e8826f7..6630abeb  main       -> origin/main` |
| 2026-08-29 | B266 spec 的锁缝是：桌面 UA 渲染按钮、点击发送当前 `/cards` URL 的宿主消息、薄壳同源 http(s) 校验并打开系统浏览器、标题栏不加按钮。 | `git show origin/main:docs/superpowers/specs/b266.md` → 原文包含 `仅 isDesktopShell()`、`沿用已有 webkit external 通道`、`只允许本控制台同源 http(s)`、四条测试接缝及三条用户故事。 |
| 2026-08-29 | 最优图领域为 `d_web_cards`，但四个相关容器今日实际落在 `d_web`；图报告 6 个未扫描入口，查询结果不能当作无调用方。 | `GOMODCACHE=/root/.handoff/tmp/347d1589/chart-gomodcache go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . context d_web_cards` → `view: baseline`, `unscannedEntries: 6`, `misplaced: k_web_app_cards, k_web_app_cards_model, k_web_app_flows, k_web_app_flows_model`，均 `current d_web` / `best d_web_cards`。 |
| 2026-08-29 | 图中入口与现状签名：`CardsPage` 在 `web/src/app/cards/CardsPage.tsx:64`；`isDesktopShell` 在 `web/src/app/lib/desktopShell.ts:29`；`requestTitlebarZoom` 在 `:61`；`DesktopTitleBar` 在 `web/src/app/shell/DesktopTitleBar.tsx:36`。 | `codegraph sym ...` 与 `codegraph who-calls ...` 已运行；`CardsPage` / `DesktopTitleBar` 无图内调用方，图警告仍为 6 个未扫描入口；未因此推断无调用方。 |
| 2026-08-29 | 前端现有接线：`CardsPage` 用 `useSearchParams()` 读取 `project`，`useNavigate()` 仅用于任务深链；顶栏健康点在 `ml-auto`，`DesktopTitleBar` 保持独立。 | `nl -ba web/src/app/cards/CardsPage.tsx`、`web/src/app/lib/desktopShell.ts`、`web/src/app/shell/DesktopTitleBar.tsx` → 对应源码窗口已核对。 |
| 2026-08-29 | Wails 依赖版本为 `github.com/wailsapp/wails/v3 v3.0.0-beta.8`，模块源码已下载到任务临时缓存；`RawMessageHandler` 是 `func(window Window, message string, originInfo *OriginInfo)`，`OriginInfo` 含 `Origin` / `TopOrigin` / `IsMainFrame`，`App.Browser.OpenURL` 签名为 `func (bm *BrowserManager) OpenURL(url string) error`。 | `GOMODCACHE=/root/.handoff/tmp/347d1589/desktop-gomodcache go mod download -json github.com/wailsapp/wails/v3@v3.0.0-beta.8` → `Dir /root/.handoff/tmp/347d1589/desktop-gomodcache/github.com/wailsapp/wails/v3@v3.0.0-beta.8`; `nl -ba .../application_options.go` → `RawMessageHandler func(window Window, message string, originInfo *OriginInfo)`; `nl -ba .../application.go` → `type OriginInfo struct { Origin string; TopOrigin string; IsMainFrame bool }`; `nl -ba .../browser_manager.go` → `OpenURL(url string) error`。 |
| 2026-08-29 | Wails beta.8 会把非 `wails:` 原始消息转给 `Options.RawMessageHandler`；其 `Browser.OpenURL` 只是调用平台默认浏览器，Wails 自带 URL 校验不包含本控制台同源约束，因此 plan 必须加同源校验。 | `nl -ba .../application.go` → `if strings.HasPrefix(event.message, "wails:") ... else ... RawMessageHandler(...)`; `nl -ba .../messageprocessor_browser.go` → `ValidateAndSanitizeURL` 后 `browser.OpenURL`; `nl -ba .../urlvalidator.go` → 仅 scheme/host/危险字符等通用校验。 |
| 2026-08-29 | 首次前端基线命令未启动 Vitest：工作树没有 `node_modules`，`npx` 写默认 npm 缓存失败。 | `npx vitest run src/app/cards/CardsPage.test.tsx src/app/lib/desktopShell.test.ts src/app/shell/DesktopTitleBar.test.tsx` → `npm error code EROFS` / `npm error syscall open` / `npm error path /root/.npm/_cacache/tmp/***` / `npm error rofs Invalid response body while trying to fetch https://registry.npmjs.org/vitest: EROFS: read-only file system` |
| 2026-08-29 | npm 依赖装入任务临时缓存后，前端三缝基线全绿。 | `npm ci --cache /root/.handoff/tmp/347d1589/npm-cache` → `added 290 packages, and audited 291 packages in 7s` / `found 0 vulnerabilities`; `npx vitest run src/app/cards/CardsPage.test.tsx src/app/lib/desktopShell.test.ts src/app/shell/DesktopTitleBar.test.tsx` → `Test Files  3 passed (3)` / `Tests  16 passed (16)` |
| 2026-08-29 | 桌面 shell 基线首次失败于环境写保护，不是 B266 断言：既有同步测试向系统 `/tmp` 创建临时文件。 | `go test ./internal/shell/...` → `--- FAIL: TestSyncOnOpenOrderIsLoadBearing` / `ERROR 创建临时文件失败 dir=/tmp cause="open /tmp/.handoff-sync-2211604910: read-only file system"` / `FAIL github.com/Xsxdot/handoff/desktop/internal/shell` |
| 2026-08-29 | 将 `TMPDIR` 指向任务临时目录仍不能使整包基线通过，因为既有夹具把安装目标硬编码为 `/tmp/handoff`；不把这个环境失败归因于 B266。 | `TMPDIR=/root/.handoff/tmp/347d1589 go test ./internal/shell/...` → `--- FAIL: TestSyncOnOpenOrderIsLoadBearing` / `ERROR 创建临时文件失败 dir=/tmp cause="open /tmp/.handoff-sync-649842495: read-only file system"` / `FAIL github.com/Xsxdot/handoff/desktop/internal/shell` |
| 2026-08-29 | 桌面 shell 中不触发 `/tmp/handoff` 同步夹具的既有测试集可通过，且验证了包可编译；整包仍保留上一行环境失败。 | `go test ./internal/shell/... -run '^(TestResolve|TestNormalizeProjectDir|TestAwaitWebviewReady|TestConsoleURL|TestDefaultDeviceName|TestBuildForm|TestApplyAnswers)'` → `ok  github.com/Xsxdot/handoff/desktop/internal/shell  0.065s` |
| 2026-08-29 | 前端类型检查与本卡相关文件 lint 在基线通过。 | `npm run typecheck` → `> tsc -b`，退出码 0；`npx eslint src/app/cards/CardsPage.tsx src/app/cards/CardsPage.test.tsx src/app/lib/desktopShell.ts src/app/lib/desktopShell.test.ts src/app/shell/DesktopTitleBar.test.tsx` → 无输出，退出码 0。 |
| 2026-08-29 | 当前 runner 没有 Wails CLI，故无法在本节点基线执行桌面嵌套模块的装配构建；计划保留仓库 README 要求的 `wails3 task build` 作为实现后由具备工具链的执行者验证的编译门。 | `wails3 task build` → `/bin/bash: line 1: wails3: command not found` |
| 2026-08-29 | Wails Darwin 的 `OriginInfo.Origin` 实际由当前 frame URL 的 `absoluteString` 填入，可能包含 `/console?ticket=...` 路径与查询；不能把来源字符串与 `/cards` URL 做全串相等，也不能把它写日志。 | `nl -ba .../webview_window_darwin.m` → `603: NSURL *url = message.frameInfo.request.URL` / `606: origin = [url absoluteString]`; `nl -ba .../application_darwin.go` → `Origin: o`。 |
| 2026-08-29 | Wails beta.8 三个平台的系统打开实现均以 `exec.Command` 传单独 URL 参数（Darwin `open`、非 Darwin `xdg-open`、Windows `rundll32`），不会经 shell 拼接；计划仍先自行约束 http(s)+同源，再交给 `App.Browser.OpenURL`。 | `nl -ba .../internal/browser/browser_darwin.go` → `exec.Command("open", target)`；`browser_other.go` → `exec.Command("xdg-open", target)`；`browser_windows.go` → `exec.Command("rundll32", "url.dll,FileProtocolHandler", target)`。 |
| 2026-08-29 | 计划已落盘，包含两个实现 task 的有界文件集、逐字接口、完整红测/实现代码块、基线事实、日志/注释步骤、序列化边界、缺陷族、真机清单与双向接缝矩阵；本节点未改实现代码。 | `git diff --check` → 无输出；占位符扫描 `rg -n 'TBD|同 Task|适当的错误处理|TODO|FIXME|若.*改成|占位' docs/superpowers/plans/b266-plan.md` → 无输出；围栏计数 `awk '/^```/{n++} END{print n}' ...` → `48`；`git status --short` → 仅 `docs/superpowers/plans/b266-plan.md` 与本台账为 untracked。 |
| 2026-08-29 | plan 与台账按要求同批提交；随后将本提交事实追加回台账并 amend，最终 hash 以收口命令为准。 | `git commit -m "docs(plan): add B266 browser-open plan"` → `[cards/B266-charter 5789b664] docs(plan): add B266 browser-open plan` / `2 files changed, 713 insertions(+)` / `create mode 100644 docs/superpowers/ledgers/2026-08-29-b266-plan-ledger.md` / `create mode 100644 docs/superpowers/plans/b266-plan.md` |
