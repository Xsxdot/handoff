# B233.17 breakdown 台账（2026-09-12）

节点：`charter:breakdown`（B233.17 组装点收窄与编译期封界：handler 退出 assembly，internal 封界，预算棘轮）
分支：`cards/B233.17-charter-3`（本卡不切分支、不改 git 配置、不调 handoff CLI、不起 executor、不派发）
有效基线分支：`cards/B233.1-charter-7`（本卡不切、不越过）
上游：spec `docs/superpowers/specs/b233.17.md`（头部「已批准（2026-09-12，用户原话『都做』）」，文件内核实）；contract `docs/superpowers/specs/b233.17-contract.md`（头部「冻结状态：随本提交冻结」，冻结提交 `bd9c086c`，文件内核实）
产出物：`docs/superpowers/specs/b233.17-breakdown.md`
起点 HEAD `bd9c086c`（== 分支 `cards/B233.17-charter-2`；本工作树在 `cards/B233.17-charter-3`，同 commit）

本台账只记过程与原始读数；结论与待拍板回写 breakdown 正文与 §0。

## 1. 入口读数（本工作树复核，非会话记忆）

- 当前分支 `cards/B233.17-charter-3`，HEAD `bd9c086c`（与 `cards/B233.17-charter-2` 同 commit，contract 冻结物在此）。
- 祖先链：`bd9c086c` → `def5b3f5`（spec）→ `686d87ab`（B233.14 absorb，功能线读数）→ `9d785ae5` → `94246fc8`。
- `git rev-parse origin/cards/B233.1-charter-7` = `def5b3f5`；本地 `cards/B233.1-charter-7` = `94246fc8`（两 ref 不一致，见 §4 观察）。
- `codegraph/target.json#assembly` = `["main.go","internal/agentd/server.go","cmd/agentd.go"]`（`codegraph.go` 已退出，符合 contract §3.1）。
- `internal/agentd/server.go` 3010 行；`func (s *Server) handle*` 定义 21 个（`codegraph/target.json` 读数与磁盘一致）。
- `internal/agentd/codegraph.go` 170 行，2 个 `Server.handleProjectCodegraph*`，零插线；不在 assembly。
- 全仓 `func (s *Server) handle*` 定义 123 个，分布在 30 个文件（`server.go` 21 + 其余 102）；非 assembly 文件从不被整文件豁免，故本卡只动 assembly 名单。
- `Handler()` `:637`，路由表 82 处 `HandleFunc`；`NewServer` `:249`；`SetManager` `:373`；`SetupAutomation` `:2673`（`collab.New :2687`、`hostapi.NewWithCredentialPathFor :2691`、`keystone.New :2716`）；`SetScheduling` `:2753`；`Scheduling()` `:2809`；`SchedulingRegistry()` `:2773`。均留 assembly（插线）。
- 四包公开构造：`orchestration.NewManager` `manager.go:291`；`scheduling.New` `scheduling.go:173`；`workspace.NewCapability` `capability_impl.go:17`；`approval.NewClient` `client.go:72`。
- 嵌套 `internal/`：`find internal -type d -name internal` 仅顶层 `internal/`（四包均未封，符合 contract §1）。
- gateway/cmd 生产 import 四包里间：0（符合 contract §1）；`grep` 无命中。
- 生产 import 四包公开包的实际清单：`internal/agentd` 生产文件 import `scheduling`（10 文件）、`workspace`（7 文件）；`cmd` import `orchestration`/`scheduling`/`workspace`（`cmd/agentd.go`、`cmd/project.go`、`cmd/wait.go`）。
- gateway 生产 import 具体 `*scheduling.Service` 仅 `scheduling_client.go`（`var _ SchedulingClient = (*scheduling.Service)(nil)` 编译期断言）。
- `internal/orchestration` 生产 import `internal/agentd`（`agentd.Hub`/`TerminalOutcome` 等）9 文件，反向依赖 gateway（既有，非本卡新增）。
- `workspace` 公开面巨大：`internal/agentd` 生产直接调用的 `workspace.*` 导出符号 60+（含 `PrepareWorkspace`/`ReadFile`/`WriteFile`/`GitRun`/`RunCmd`/`Err*` 哨兵等）；`internal/orchestration` 生产另调 30+。
- `approval` 在 `best.json` 归 `d_orchestration`（`k_approval_fn → d_orchestration`），不是顶层独立领域。
- `best.json` 顶层领域 15 个：`d_cli`/`d_collab`/`d_execution`/`d_gateway`/`d_keystone`/`d_ledger`/`d_maintenance`/`d_orchestration`/`d_policy`/`d_protocol`/`d_scheduling`/`d_sessions`/`d_transport`/`d_web`/`d_workspace`（与 b233.14/16 读数一致）。
- `best.json` 把 `k_agentd_fn`/`k_agentd_model`/`k_agentd_Manager`/`k_agentd_Hub`/`k_store_Store`/`k_store_fn` 仍归 `d_orchestration`；`best.json` **不含** `k_orchestration_*`（视图新增容器未归域）。
- `baseline.json` 域分解仍是旧形态（`d_coordination_task`/`d_coordination_api` 等），**不含** `d_orchestration`；`baseline.meta.commit = 9d785ae5`（滞后功能线）。
- 本分支视图 `codegraph/diffs/cards-B233.17-charter-2.json`：`base 9d785ae5`，`containersAdded` 5（`k_agentd_orchestration_client`→域 `d_coordination_api`；`k_orchestration_Manager`/`k_orchestration_Approver`/`k_orchestration_model`/`k_orchestration_fn`→域 `d_coordination_task`），`nodesAdded` 178；不复制 B233.13 的迁出/删除边。
- `internal/agentd/b23317_boundary_test.go` 五条守卫已落码：缝1 assembly 冻结 + handler 哨兵；缝2 嵌套 import 守卫 + 哨兵；缝3 六方向预算棘轮锁。

## 2. 本节点实跑命令与原始结果

| # | 命令 | 原始结果 |
|---|---|---|
| 1 | `go build ./...` | 无输出，退出码 0 |
| 2 | `go test ./internal/agentd/ -run TestB23317 -v` | 5 条 `--- PASS`（`TestB23317GatewayProductionDoesNotImportNestedInternals`/`...NestedInternalGuardHasTeeth`/`...AssemblyFreezeExcludesHandlerFile`/`...FacadeBudgetsStayRatchet`/`...HandlerGuardHasTeeth`），`ok … 0.007s` |
| 3 | `go test ./internal/agentd/ -run 'TestB2331[0-7]' -count=1` | `ok github.com/Xsxdot/handoff/internal/agentd 3.540s` |
| 4 | `go test ./cmd -run 'TestCLIExecutionSurface\|TestCLIExecutionCommandsDoNotHandRollHTTP' -v` | `TestCLIExecutionCommandsDoNotHandRollHTTP` PASS、`TestCLIExecutionSurfaceHoldsNarrowDependencies` PASS、`TestCLIExecutionSurfaceGuardHasTeeth` PASS，`ok … 0.008s` |
| 5 | `codegraph check --repo .`（无视图，= `TestRepoContractGate` 口径） | `fails=7`、`warns=137`：3 `dead-entry`(gateway→orchestration 的 `orchestration 实体`/`orchestration.Manager`/`orchestration（包级函数）`)、1 `dead-interface OrchestrationClient`、3 `over-budget`（`d_gateway->d_orchestration 直调 207 条超出预算 0`、`d_orchestration->d_workspace 9/0`、`d_workspace->d_orchestration 3/0`）；退出码 1 |
| 6 | `codegraph check --view cards-B233.17-charter-2 --repo .` | `fails=6`、`warns=140`：3 `dead-entry` 同上 + 3 `over-budget` 同上（`dead-interface` 被视图补 `OrchestrationClient` 消除）；退出码 1 |
| 7 | `codegraph validate --view cards-B233.17-charter-2 --repo .` | `issues=122`；`codegraph validate --repo .`（基线）`issues=122`，**逐条相同、零新增**；两者退出码 1（基线存量） |
| 8 | `codegraph resolve --doc docs/superpowers/specs/b233.17-contract.md --repo .` | `anchors=9`，无 `file_missing`，退出码 0 |
| 9 | 漏判复现探针（用真实 `cmd.executionSurfaceViolations`，临时 `cmd/zz_b23317_probe_test.go`，跑后即删、`git status` 干净） | 输入 `func targetClient(...)...` 之后紧跟包级 `var leak *client.Client`，`violations=[]`（应判红却放行）——spec §实现决定「B233.16 守卫漏判：包级聚合写在白名单函数之后必须红」现状不成立 |
| 10 | assembly 摘除 `server.go` 探针（临时改 `target.json`，check 后立即从备份还原、`git diff` 干净） | `d_gateway->d_orchestration` hits 207→303；新增 `new-direction d_orchestration -> d_ledger`（1 条）；`d_orchestration->d_workspace` 9→10。证明 handler 搬出 assembly 会**抬升**该方向直调计数（与 contract §3.2 note 一致） |

## 3. 关键判断（可复核）

- **check 的五条相关红无法由本卡源代码搬迁消除**：`check` 经 `best.json` 的 `containers` 映射归域；`best.json` 不含 `k_orchestration_*` 且仍把 `k_agentd_fn`/`k_agentd_Manager`/`k_store_Store` 归 `d_orchestration`，`baseline.json`（meta `9d785ae5`）仍挂已删的 `internal/agentd/manager.go` 节点。视图只叠加图、不动 `best.json`，故 3 条 `dead-entry` 与 207/9/3 `over-budget` 是**图基线滞后**的直接读数，不随 handler 搬家/直调改走接口而改变。contract §6 已认账，但 §7.3 #23 仍把「不再 over-budget」标为「实现期判据」，与 §6 不自洽。
- **spec 明确要求的「修 B233.16 守卫漏判」在 contract 冻结清单中无着落**：spec §实现决定末条与 §方案 3 最后一颗 bullet、§测试决定 3 都点名；contract §5 五条守卫与 §7 冻结条目均未收录。实测该漏判成立（探针 #9）。
- 本卡为结构闸：`go build ./...`=0、守卫五条全绿、`validate` 零新增；行为面零改动（未跑全量 `go test ./...`，标注「未验证」）。

## 4. 观察（非阻塞，供协调者归档）

- spec 基线 `cards/B233.1-charter-7 @ 686d87abf` 与 contract 基线 `cards/B233.1-charter-7 @ 94246fc8` 不一致（`94246fc8` 是 `686d87ab` 的祖先）。contract 冻 `target.json` 时 assembly 已含 `cmd/agentd.go`（`686d87ab` 引入），故 contract 头部 hash 应为 `686d87abf`。
- 本工作树 `origin/cards/B233.1-charter-7` = `def5b3f5`（B233.17 spec 提交），本地 `cards/B233.1-charter-7` = `94246fc8`；两个 ref 指向不同代。属 handoff 工作树的 ref 环境现象，不代表功能线分支内容。

## 5. 收尾

- 产出物 `docs/superpowers/specs/b233.17-breakdown.md` 落盘；头部落款「待拍板（2026-09-12）」。
- 未建卡、未派发、未调 handoff CLI、未起新 executor。
- 探针文件已删；`target.json`、视图 diff 已从备份逐字节还原（`git status` 干净）。
- 本台账与产出物同批提交（收尾纪律：本节点按纪律块正常 commit）。
- 提交（历史读数）：`git commit -m "breakdown(B233.17): …"` → `[cards/B233.17-charter-3 802448f1] … 2 files changed, 428 insertions(+)`；本台账随后 amend 一次收进同批（amend 换 hash，不回写追踪）。
