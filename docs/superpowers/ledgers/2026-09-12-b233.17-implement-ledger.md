# B233.17 implement 台账（2026-09-12）

节点：`charter:implement`（B233.17 组装点收窄与编译期封界：handler 退出 assembly，internal 封界，预算棘轮）
分支：`cards/B233.17-charter-5`（本卡不切分支、不改 git 配置、不调 handoff CLI、不起 executor、不派发）
上游：plan `docs/superpowers/plans/b233.17-plan.md` @ `9572bcb93`，基线 `cards/B233.17-charter-4` @ `bad7fdb5`
执行序：W0 → W1 → W2 → W3 → W4

## W0 文档更正

- contract 头部 `94246fc8` → `686d87abf`（P5-A/C-4）。
- 新增 §8.5「B233.17 边界澄清（拍板回写）」C-1..C-5。
- `codegraph resolve --doc docs/superpowers/specs/b233.17-contract.md` → anchors 10、无 file_missing、退出 0。

## W1 缝 1：handler 迁出 assembly 文件

- 新增 assembly 实质断言 `TestB23317AssemblyFilesHaveNoHandlerDefs`（`internal/agentd/b23317_boundary_test.go`）。
- 红（搬迁前）：

```text
--- FAIL: TestB23317AssemblyFilesHaveNoHandlerDefs (0.00s)
    b23317_boundary_test.go:260: assembly 文件 internal/agentd/server.go 不得定义 Server.handle*（缝 1）：[Server.handleStatus ... Server.handleEvents]
FAIL github.com/Xsxdot/handoff/internal/agentd 0.007s
```

- 搬迁：`server.go` 第 884–2621 行（21 个 `Server.handle*` + 同段私有 helper/DTO）整体剪到新 `internal/agentd/handlers.go`（文件头职责/边界注释 + import 集）。
- `server.go` 保留 `NewServer`/`Set*`/`Handler()`/`SetupAutomation`/`noteTruncationDiagnosed`/`writeEvent`/`writeJSON`/`truncateRunes`/`wantsHTML`/`writeUnauthorized`。
- 绿：`go build ./...` 退出 0；`go test ./internal/agentd/ -run TestB23317 -count=1` → `ok ... 0.011s`。
- 变异（加 `handleZZProbe` 回 server.go）：

```text
--- FAIL: TestB23317AssemblyFilesHaveNoHandlerDefs (0.00s)
    b23317_boundary_test.go:260: assembly 文件 internal/agentd/server.go 不得定义 Server.handle*（缝 1）：[Server.handleZZProbe]
FAIL
```

  探针已删（`grep -c handleZZProbe server.go` = 0），再跑 `TestB23317` → `ok`；`git diff --check` = 0。

## W2 缝 2：四包编译期封界

- W2a `cacheplan`：`internal/orchestration/internal/cacheplan/cacheplan.go`（ActiveLeaf/LegacyLeaf/TmpRoot/PathEqual/ID8/IsTmpRoot/ActiveLeafOccupied/LeafPlan/PlanTaskCacheLeaves/SumRegularFileBytes）。`cachegc.go` 只留 `removeCacheLeaf`/`purgeTaskCache`，`gc.go` 改走 `cacheplan.*`。
- W2b `gitproc`：`internal/workspace/internal/gitproc/{runshell.go,procgroup_unix.go,procgroup_other.go}`（RunShell/ResolveRunShell/RunShellCandidates/SetProcGroup/KillProcGroup）。`gitworkspace.go` 改走 `gitproc.*`；删除公开包 3 文件；`runshell_test.go` 迁入嵌套。
- W2c `decision`：`internal/approval/internal/decision/decision.go`（GateDecision/MapGateAnswer/PermEventText）。`client.go` 改走 `decisionpkg.*`（局部变量 `decision` 遮蔽，用别名）。
- W2d `logging`：`internal/scheduling/internal/logging/logging.go`（StatusLog）。`status.go#statusLog` 转发。
- 实测：`find internal -type d -name internal` → orchestration/workspace/approval/scheduling 四个 nested 目录。
- 编译期锁变异（临时 `internal/agentd/zz_b23317_probe.go` import cacheplan）原文：

```text
internal/agentd/zz_b23317_probe.go:3:8: use of internal package github.com/Xsxdot/handoff/internal/orchestration/internal/cacheplan not allowed
```

  探针已删；`go build ./internal/agentd` 恢复退出 0。
- 绿：`go build ./...`=0；`GOOS=windows/darwin go build ./internal/workspace/...`=0；`go test ./internal/{orchestration,approval,scheduling,workspace}/... -count=1` 全部 ok；`go test ./internal/agentd/ -run TestB23317`=ok。

## W3 缝 3 的锁：B233.16 executionSurfaceViolations 函数名重置

- 新增可变红用例：白名单工厂函数之后包级 `var leak *client.Client` 必须判红。
- 红（修复前）：`execution_surface_test.go:167: 白名单工厂函数之后的包级 *client.Client 必须判红（函数名上下文不得沿用）`，退出 1。
- 首版按「列 0 声明行」重置 `current`，实测仍放行——`stripCommentsAndStrings` 把空行 newline 还原成空格，`var` 行不再在列 0（调试原文：`CLEANED="package cmd\n func targetClient(...) {\n}\n var leak *client.Client\n"`，`VIOL=[]`）。
- 改为按花括号深度 == 0 判包级：depth 0 时 `func` 设置 current、`var/const/type` 重置 current；depth 由每行 `{`−`}` 累加。
- 绿：`go test ./cmd -run TestCLIExecutionSurface -count=1` → `ok github.com/Xsxdot/handoff/cmd 0.006s`。

## W4 全接缝回归与图对账

原始读数（本工作树 `a34bd6cb` 后）：

```text
go build ./...                                          → 退出码 0
go test ./internal/{agentd,orchestration,approval,scheduling,workspace}/ ./cmd -count=1
    agentd: FAIL（仅 TestLegacyNodeEventSequenceUnchanged，基线存量红）
    orchestration/approval/scheduling/workspace: ok
    cmd: FAIL（仅 TestRepoContractGate，基线存量红）
go test ./internal/agentd/ -run TestB23317 -count=1     → ok
go test ./cmd -run TestCLIExecutionSurface -count=1     → ok
codegraph check --repo .                                → fails=7（3 dead-entry + 1 dead-interface + 3 over-budget），与 plan §2 基线逐条一致
codegraph check --view cards-B233.17-charter-2 --repo . → fails=6（dead-interface 被视图消除），与 plan §2 一致
codegraph validate --repo .                             → 132 个完整性问题、0 失鲜节点
codegraph validate --view cards-B233.17-charter-2       → 132
git diff --check                                        → 退出码 0
git status --short --branch                             → 仅第 3.1 节列出的文件（见下）
```

**validate 数字动因核查（补充拍板允许 nested 新文件导致 issues 数字可动）**：用 `git archive bad7fdb5` 落基线副本实测 `codegraph validate` = **122**；本树 = 132。逐条 diff 后**新增 10 条全部是 `调用边门控` 这一既有种类**，内容为 `n_agentd_Server_handleTask*`（旧基线把 handler 归在 `server.go`，今在 `handlers.go`）与 `n_orchestration_cacheActiveLeaf`（今在 nested `cacheplan`）对 workspace/executor 的调用——即「基线节点→文件映射滞后于本卡搬迁」产生，非新问题种类（`NEW KINDS: set()`），符合补充「数字可动、禁止新增与本卡无关的问题种类」。未改 `best.json`/`baseline.json`/`target.json`。

**基线存量红（非本卡引入，已在 `git archive bad7fdb5` 副本复现）**：
- `TestRepoContractGate`：7 条契约违规（3 dead-entry + dead-interface + 3 over-budget），与 contract §6「继承 B233.13 未 absorb」一致。
- `TestLegacyNodeEventSequenceUnchanged`：`scheddispatch_test.go:531` 事件序列漂移，roadmap 已记。

**未验证（真机）**：跨机任务执行/真实 executor 会话/平台矩阵运行（plan §10），本卡为结构重排，机内只验编译与契约形状。

**文件集合门禁**：`git diff --name-status bad7fdb5` 仅含 plan §3.1 所列文件与新增 nested 实现/测试；`codegraph/{best,baseline,target}.json` 与视图 diff 未改。
