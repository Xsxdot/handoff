# B233.17 implement 台账（2026-09-12）

节点：`charter:implement`（B233.17 组装点收窄与编译期封界：handler 退出 assembly，internal 封界，预算棘轮）
分支：`cards/B233.17-charter-5`（本卡不切分支、不改 git 配置、不调 handoff CLI、不起 executor、不派发）
上游：plan `docs/superpowers/plans/b233.17-plan.md` @ `9572bcb93`，基线 `cards/B233.17-charter-4` @ `bad7fdb5`
执行序：W0 → W1 → W2 → W3 → W4

## W0 文档更正

- contract 头部 `94246fc8` → `686d87abf`（P5-A/C-4）。
- 新增 §8.5「B233.17 边界澄清（拍板回写）」C-1..C-5。
- `codegraph resolve --doc docs/superpowers/specs/b233.17-contract.md` → anchors 10、无 file_missing、退出 0。

## W1 缝 1：handler 迁出 assembly 文件

- 新增 assembly 实质断言 `TestB23317AssemblyFilesHaveNoHandlerDefs`（`internal/agentd/b23317_boundary_test.go`）。
- 红（搬迁前）：

```text
--- FAIL: TestB23317AssemblyFilesHaveNoHandlerDefs (0.00s)
    b23317_boundary_test.go:260: assembly 文件 internal/agentd/server.go 不得定义 Server.handle*（缝 1）：[Server.handleStatus ... Server.handleEvents]
FAIL github.com/Xsxdot/handoff/internal/agentd 0.007s
```

- 搬迁：`server.go` 第 884–2621 行（21 个 `Server.handle*` + 同段私有 helper/DTO）整体剪到新 `internal/agentd/handlers.go`（文件头职责/边界注释 + import 集）。
- `server.go` 保留 `NewServer`/`Set*`/`Handler()`/`SetupAutomation`/`noteTruncationDiagnosed`/`writeEvent`/`writeJSON`/`truncateRunes`/`wantsHTML`/`writeUnauthorized`。
- 绿：`go build ./...` 退出 0；`go test ./internal/agentd/ -run TestB23317 -count=1` → `ok ... 0.011s`。
- 变异（加 `handleZZProbe` 回 server.go）：

```text
--- FAIL: TestB23317AssemblyFilesHaveNoHandlerDefs (0.00s)
    b23317_boundary_test.go:260: assembly 文件 internal/agentd/server.go 不得定义 Server.handle*（缝 1）：[Server.handleZZProbe]
FAIL
```

  探针已删（`grep -c handleZZProbe server.go` = 0），再跑 `TestB23317` → `ok`；`git diff --check` = 0。
