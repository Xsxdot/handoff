# B229.2 执行台账 —— T2 能力位 DisciplinesSupported 上报置 true

- 2026-08-25：开工。分支 cards/B229.2-charter（基线含 T1=5585ecc2），工作树干净。
- 2026-08-25：grep 核对全仓 `DisciplinesSupported` 赋值形态零命中生产代码（仅 proto fixture 两处）——上报点确实未接线，本卡不落地则 ResolveDispatch（dispatch.go:65 targetCap nil/false 无条件拒发）会让每次派发全停。
- 2026-08-25：核销 T1 四件事于当前树 internal/agentd/manager.go——①收文即用 :757 `discText := req.DisciplineText`（本地解析已退役）；②正文落盘 :923-929 discipline.md 先写后启动 executor、失败拒派；③continue :1300 loadPersistedDiscipline→注入 :1326；④启动恢复/resume :3410 loadPersistedDiscipline→回注 :3431（Cold 缺失拒绝、热重连 Error 不阻断）。
- 2026-08-25：确认 web/src/api/types.ts 两处投影位已由 Ticket 0 预置（StatusResp disciplines_supported?: boolean|null 约行 251；Machine 约 395「缺席按不支持处置」注释）——本卡无需改 web。
- 2026-08-25：红——新增 discipline_capability_test.go 三支（HTTP 子串断言/本机记录/fillFromStatus 搬运含 nil），实现前跑 `-run '…Disciplines…'` 全 FAIL，失败原因均为功能缺失（JSON 无该键、记录为 nil），非 typo。
- 2026-08-25：绿——server.go handleStatus 置 resp.DisciplinesSupported=&true（附 §2.4 四件事核对单注释）；machines.go localMachine 就地填本机位；fillFromStatus 原样搬运（含 nil）。`go test ./internal/agentd -run 'TestStatus' -count=1` → ok（5.152s）。
- 2026-08-25：变异自验三发，锚点先验唯一（`grep -c` 均 =1）、编译均过、行为均变——①server.go `disciplinesOK := true→false` → TestStatusReportsDisciplinesSupported FAIL；②machines.go 同形变异 → TestLocalMachine…FAIL；③fillFromStatus 搬运行改抄 PtySupported → TestFillFromStatus…FAIL。逐发恢复后 `git diff --stat` 仅剩实现本体（server.go +9 / machines.go +5）。
- 2026-08-25：收尾——`go build ./...` 通过；`go test ./internal/agentd -count=1` 全包 ok（137.190s）。触及包仅 internal/agentd；proto/web 零改动（字段与 TS 投影位 Ticket 0 已预置）。
- 2026-08-25：流程属性声明核销——「T1 合入先于 T2 上报」无运行时断言可锁，靠 DAG 排序 + review 保障；该残余风险已登记于 b229-breakdown.md §5 真机清单第 8 条（升级批次核对：确认没有任何一台机器在 T1 合入前收到上报 true 的中间构建），本卡不重复建断言、不改动已冻结的 breakdown 正文。



## 补刀轮（协调者三问复核，分支 cards/B229.2-review-1）
- 2026-08-25：开工。分支 cards/B229.2-review-1，HEAD=5bb29836（实现本体已提交），工作树干净。本轮任务：①核三处投影链做满且语义各别；②核「变异锚点唯一」结论来源；③核对线格式与 web/src/api/types.ts 对齐。
- 补刀轮：grep -o 计数核验当前树锚点唯一——server.go `disciplinesOK := true` 出现次数=1（:694），machines.go=1（:108）；fillFromStatus 搬运行唯一（machines.go:161）。前一轮「锚点唯一」在仓内只有台账自述、无命令留痕，本轮以实测重建该结论。
- 补刀轮：亲跑 `go test ./internal/agentd -run 'TestStatus' -count=1` → ok 5.436s（验收命令复现）。
- 补刀轮·变异一发（server.go:694 true→false，变异前 grep -o 计数=1）：TestStatusReportsDisciplinesSupported FAIL，失败响应原文含 `"disciplines_supported":false`——顺带实证置位时字段在线上存在。
- 补刀轮·变异二发（machines.go:108 true→false，计数=1）：TestLocalMachineReportsDisciplinesSupported FAIL。附 minor：该 Fatalf 以 %v 打 *bool，置 false 时打印指针地址而非值（nil 时打 <nil>），断言逻辑正确、仅报文可读性欠佳，记账不阻塞。
- 补刀轮·变异三发（machines.go:161 改抄 st.PtySupported，计数=1）：TestFillFromStatusCarriesDisciplinesSupportedIncludingNil FAIL（true 未搬运）。逐发 git checkout 恢复后 `git status --porcelain` 仅剩本台账。
- 补刀轮：`go build ./...` 通过（BUILD_OK）。
- 补刀轮·投影链核验：生产代码中 DisciplinesSupported 赋值点恰为三处（server.go:695 handleStatus / machines.go:109 localMachine / machines.go:161 fillFromStatus），与 LaunchersSupported 先例站点集完全一致，无第四处投影面；第三处为指针原样搬运（`= st.DisciplinesSupported`），非就地造值，且被变异三发证明测试有牙。
- 补刀轮·线格式对齐核验：Go 侧 StatusResp/Machine 均 *bool+omitempty+json:"disciplines_supported"（proto/status.go:230、proto/projects.go:165）——nil 整键缺席、&true 输出 `"disciplines_supported":true`；TS 侧 types.ts:251 `?: boolean|null` 与 :395 `?: boolean` 均可选，缺席合法且语义即注释「缺席按不支持处置」，与 nil 搬运语义一致；Go 对 *bool+omitempty 永不输出 null，无双方向不匹配。web/src 全仓仅这两处类型声明、无消费者（Ticket 0 预置），本卡 web 零改动成立。
- 补刀轮·锚点唯一结论来源澄清：前一轮「grep -c 均 =1」在仓内产物中只有台账自述、无命令留痕，无法与「纯自述」区分——如实记入 finding。本轮已实测重建：三个锚点出现次数均=1（grep -o|wc -l），三发变异全部被对应测试拦下，变异结论成立且升级为一手证据。
- 补刀轮·T1 核对单锚点抽查：manager.go :757 `discText := req.DisciplineText`、:923 writeTaskDirFile 落盘失败拒派、:1300 loadPersistedDiscipline（continue）、:3410 loadPersistedDiscipline（resume）均在位，server.go handleStatus 注释四件事引用无误。
