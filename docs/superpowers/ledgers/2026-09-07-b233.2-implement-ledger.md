# B233.2 implement 台账

节点：charter/implement
卡：B233.2
分支：cards/B233.2-charter-4
基线 HEAD：66efe347
规则：每确立一个事实立刻追加一行；测试必须先见红再写实现；变异自验确认有牙；提交事实记命令与原始输出，随后只 amend 一次。

---

## 2026-09-07 开工检查

- 角色：implement 执行者。按 `docs/superpowers/plans/b233.2-plan.md` 逐步执行 T1–T7。
- 纪律红线：
  - 不派发、不调用 handoff CLI、不起新的 executor 进程或子任务。
  - 提问纪律：需要人决策输出单行 JSON `{"ask":"..."}` 并结束回合。
  - 收尾纪律：git add 并 commit（不要 push），输出单行 JSON `{"branch":"...","commit":"...","summary":"..."}`。
  - 拍板不得改写：P1(a), P2(a), P3(a), P4(a), P5(a), P6(a)。
  - TDD 铁律：先红后绿，无失败测试不写生产代码。变异自验。日志与注释。
  - NewManager 签名一字不改。禁止两张表。禁止扩 supportedCLIs 冒充协调。禁止 import internal/permgate 进各家 adapter。
  - grok --effort low 留在调用方 Limits。OpenCode 真机只用免费模型 opencode-go/deepseek-v4-flash。
  - ApprovalClient 那 2 条 check fail 是 233.1 视图存量，本卡不修。
- 基线检查：
  - `git status`: On branch cards/B233.2-charter-4, working tree clean.
  - `git log -n 1 --oneline`: 66efe347 docs(B233.2): 写 harness 能力归拢实现计划
  - `go test ./internal/executor ./internal/executor/grok ./internal/skill -run 'TestBaselineReportMatrix|TestHarnessNameLiterals|TestUnsupportedError|TestOneShotArgv|TestOneShotArgsStillEncodesGrokLowUntilMigrated|TestAgentDirsContract|TestRecovererIsNotAdapterMethodSet|TestGuardRemove|TestZeroProfileReport|TestCoordTypesMirror|TestRequireLaunchPrompt' -count=1`: 全绿 (executor 0.003s, grok 0.007s, skill 0.002s)。
  - `go test ./internal/executor -count=1`: ok 0.002s。
  - `go test ./internal/agentd -run 'TestCoordinatorHomeSupplyOnLaunchAndResume|TestOneShot|TestApproverDecide' -count=1`: ok 0.015s。
  - `go test ./cmd -run 'TestAdapterRegistryHasAlwaysAvailableExecutors|TestAdaptersForRegistersClaudeOnAllPlatforms' -count=1`: ok 0.008s。
  - `go build -o /dev/null .`: exit code 0。
  - `codegraph --repo . --view cards-B233.2-charter check`: 2 fail (ApprovalClient dead-contract/dead-interface 存量), `d_orchestration->d_execution` 26/27。

## Task 1: 五家 Provider.Report 接线 / Registry 从 ads 推导

- TDD 红灯：
  - 测试：`cmd/agentd_test.go`（`TestProductionAdapterReportsMatchBaseline`, `TestFakeReportIsExecutionOnly`, `TestRegistryGetUnknownListsSupportedHarnesses`, `TestRequireClaudeCoordinationIsUnsupported`）与 `internal/executor/capability_test.go`（`TestCLIOnPathIsNotFiveCapabilities`）。
  - 执行 `go test ./cmd -run 'TestProductionAdapterReportsMatchBaseline|TestFakeReportIsExecutionOnly|TestRegistryGetUnknownListsSupportedHarnesses|TestRequireClaudeCoordinationIsUnsupported' -count=1`：
    - `--- FAIL: TestProductionAdapterReportsMatchBaseline (0.00s): opencode 未实现 Provider`
    - `--- FAIL: TestFakeReportIsExecutionOnly (0.00s): fake 必须实现 Provider，否则 Registry 漏登记`
    - `--- FAIL: TestRequireClaudeCoordinationIsUnsupported (0.00s): err = 未知 harness "claude"`
- T1 最小实现与绿灯：
  - 新建 `internal/executor/opencode/report.go`、`claudecode/report.go`、`grok/report.go`、`codex/report.go`、`agy/report.go`、`fake/report.go`。
  - 修改 `internal/agentd/manager.go`：在 Manager 中增加 `reg *executor.Registry`，在 `NewManager` 内部通过 `registryFromAds(ads)` 推导，新增导出 `RegistryFromAds`。
  - 修改 `internal/agentd/server.go`：在 Server 中增加 `providers *executor.Registry` 与 `SetProviders` 方法。
  - 修改 `cmd/agentd.go`：在 `defaultAdapters` 后调用 `srv.SetProviders(agentd.RegistryFromAds(ads))`。
  - 测试通过：
    - `go test ./cmd -run 'TestProductionAdapterReportsMatchBaseline|TestFakeReportIsExecutionOnly|TestRegistryGetUnknownListsSupportedHarnesses|TestRequireClaudeCoordinationIsUnsupported' -count=1`: ok (0.010s)
    - `go test ./internal/executor ./internal/executor/opencode ./internal/executor/claudecode ./internal/executor/grok ./internal/executor/codex ./internal/executor/agy ./internal/executor/fake -count=1`: 全绿
- T1 变异自验：
  - 变异点：`internal/executor/fake/report.go` 将 `CapExecution` 的 `Supported: true` 语义反转为 `false`（唯一命中且编译通过）。
  - 变异执行：`go test ./cmd -run 'TestFakeReportIsExecutionOnly' -count=1` 报 `--- FAIL: TestFakeReportIsExecutionOnly (0.00s): agentd_test.go:272: fake 必须声明 execution`。
  - 恢复变异后验证重归绿灯：ok 0.004s。

## Task 2: 执行会话 Require + Recoverer + Claude 免审 ⊆ 快照

- TDD 红灯：
  - 测试：`internal/executor/claudecode/taskenv_test.go`（`TestWriteTaskEnvEmptyWorkdirWithSnapshotFails`, `TestWriteTaskEnvSnapshotAllowIsSubsetAndNotNakedBash`）与 `internal/agentd/resume_test.go`（`TestFakeAdapterDoesNotSatisfyRecoverer`）。
  - 执行 `go test ./internal/executor/claudecode -run 'TestWriteTaskEnvEmptyWorkdirWithSnapshotFails|TestWriteTaskEnvSnapshotAllowIsSubsetAndNotNakedBash|TestWriteTaskEnvGeneratesSettingsAndMCP' -count=1`：
    - `internal/executor/claudecode/taskenv_test.go:17:131: too many arguments in call to WriteTaskEnv`（编译红：缺少 PolicySnapshot 参数）。
- T2 最小实现与绿灯：
  - 新建 `internal/executor/policy.go`，导出 `ErrUnrepresentablePolicy`；`internal/executor/opencode/taskenv.go` 改用 `executor.ErrUnrepresentablePolicy` 别名。
  - 修改 `internal/executor/claudecode/taskenv.go`：`WriteTaskEnv` 入参增加 `snap executor.PolicySnapshot`，对非空 Version 但空 Workdir 返回 `ErrUnrepresentablePolicy`，allow 表剔除裸 "Bash"。
  - 修改 `internal/executor/claudecode/adapter.go`：在 Start 中若 `Approval != nil` 读取政策快照并传给 `WriteTaskEnv`。
  - 修改 `internal/agentd/manager.go`：在 `Dispatch` 中增加 `m.reg.Require(execName, executor.CapExecution)` 检查及日志；Approval 注入扩至 Claude；将两处 `ad.(restorer)` 替换为 `ad.(executor.Recoverer)` 并彻底删除私有 `type restorer interface`。
  - 修改 `internal/agentd/manager.go` 中的 `registryFromAds`：对未实现 `Provider` 的测试 stub 提供具备 execution 的静态条目，保证测试 mock 适配执行。
  - 测试通过：
    - `go test ./internal/executor/claudecode -count=1`: ok (4.049s)
    - `go test ./internal/executor -count=1`: ok (0.002s)
    - `go test ./internal/agentd -run 'TestResume|TestDispatch|TestApprovalClient' -count=1`: ok (15.169s)
    - `grep -n "type restorer interface" internal/agentd/manager.go`: 0 命中。
- T2 变异自验：
  - 变异点：`internal/executor/claudecode/taskenv.go` 在 `WriteTaskEnv` 的 allow 列表中违规追加 `"Bash"`（唯一命中且编译通过）。
  - 变异执行：`go test ./internal/executor/claudecode -run 'TestWriteTaskEnvSnapshotAllowIsSubsetAndNotNakedBash' -count=1` 报 `--- FAIL: TestWriteTaskEnvSnapshotAllowIsSubsetAndNotNakedBash (0.00s): 快照下 allow 不得含裸 Bash`。
  - 恢复变异后验证重归绿灯：ok 0.002s。

## Task 3: 协调会话 OpenCode 实装，其余不支持，Runner 退役为包装

- TDD 红灯：
  - 测试：`internal/executor/opencode/coordinator_test.go`（`TestCoordinatorLaunchEmptyPrompt`, `TestCoordinatorResumeEmptySession`, `TestCoordinatorLaunchPassesCtxAndGetsSession`, `TestCoordinatorLaunchHonorsCancel`）与 `internal/agentd/coordinator_runner_test.go`（`TestCoordinatorRunnerUnsupportedDoesNotCallOpenCode`, `TestCoordinatorRunnerUnsupportedGrokCodexAGY`）。
  - 执行 `go test ./internal/executor/opencode -run 'TestCoordinator' -count=1`：`undefined: NewCoordinator`。
  - 执行 `go test ./internal/agentd -run 'TestCoordinatorRunner' -count=1`：`unknown field coord / reg in struct literal of type coordinatorRunner`。
- T3 最小实现与绿灯：
  - 新建 `internal/executor/opencode/coordinator.go`：实现 `executor.Coordinator` 接口（`Launch`, `Resume`, `CancelTurn`），依托 `hostapi.RunTurn` 与 `agentd/resumeTurnRequest` 结构，支持上下文取消与错误包装。
  - 修改 `internal/agentd/server.go`：`coordinatorRunner` 结构改为持有 `coord executor.Coordinator` 与 `reg *executor.Registry`；`Launch` / `Resume` 前通过 `reg.Require(name, executor.CapCoordination)` 校验并记录 slog，非 OpenCode 明确阻断；`SetupAutomation` 中组装 `opencode.NewCoordinator` 与 `s.providers`；更新 `coordinator_home_test.go` 构造。
  - 架构图契约检查：保持 `codegraph/target.json` 无死契约，`codegraph --repo . --view cards-B233.2-charter check` 维持基线存量 2 fail（ApprovalClient），新增 fail 为 0。
  - 测试通过：
    - `go test ./internal/executor/opencode -run 'TestCoordinator' -count=1`: ok (0.003s)
    - `go test ./internal/agentd -run 'TestCoordinator' -count=1`: ok (0.005s)
- T3 变异自验：
  - 变异点：`internal/agentd/server.go` 的 `coordinatorRunner.Launch` 中将 `r.reg.Require(name, executor.CapCoordination)` 能力校验旁路（绕过 Require 检查）。
  - 变异执行：`go test ./internal/agentd -run TestCoordinatorRunner -count=1` 报：
    - `--- FAIL: TestCoordinatorRunnerUnsupportedDoesNotCallOpenCode (0.00s): coordinator_runner_test.go:28: err = <nil>`
    - `--- FAIL: TestCoordinatorRunnerUnsupportedGrokCodexAGY (0.00s): coordinator_runner_test.go:44: grok err = <nil>`

## Task 4: OneShot.Invoke 迁入 Approver，grok effort 是调用方 Limits

- TDD 红灯：
  - 测试：`internal/executor/oneshot_run_test.go`（`TestRunOneShotProcessHonorsCancel`）、四家 `oneshot_argv_test.go` 金样本（opencode, claudecode, agy, codex）、`internal/agentd/approver_test.go`（`TestDecideInvokesOneShotWithGrokEffortLow`, `TestDecideDoesNotCallOneShotArgs`）、`internal/agentd/approval_client_test.go`。
  - 编译红：`undefined: executor.RunOneShotProcess`、`undefined: opencode/claudecode/agy/codex.OneShotArgv`、`app.BindOneShot undefined`。
- T4 最小实现与绿灯：
  - 新建 `internal/executor/oneshot_run.go`：实现 `RunOneShotProcess`，支持 context 取消（`exec.CommandContext`）与 64KiB 截断防失控。
  - 新建四家 `oneshot_argv.go`（opencode, claudecode, agy, codex）；新建五家 `oneshot.go`（opencode, claudecode, agy, codex, grok），实现 `executor.OneShot`（`Invoke`）并委托挂接至各家 `Adapter.Invoke`。
  - 修改 `internal/agentd/approver.go`：`Approver` 引入 `shot executor.OneShot` 字段与 `BindOneShot` 方法；`Decide` 改调 `a.shot.Invoke`，为 grok 填入 `req.Limits.Effort = executor.EffortLow`；保留 `a.env` 注入；彻底删除 `runCmd` 与 `defaultRunCmd`。
  - 修改 `cmd/agentd.go`：在组装点调用 `bindApproverOneShot` 针对各家 switch 绑定（生产包内禁止 switch）。
  - 退役 `OneShotArgs`：`git rm internal/executor/oneshot.go internal/executor/oneshot_test.go`，从 `oneshot_cap_test.go` 删除旧过渡测试；`git grep "OneShotArgs" -- "*.go"` 确认全仓生产代码零命中。
  - 架构图契约检查：`codegraph --repo . --view cards-B233.2-charter check` 保持存量 2 fail，新增 0 fail。
  - 测试通过：
    - `go test ./internal/executor ./internal/executor/grok ./internal/executor/opencode ./internal/executor/claudecode ./internal/executor/agy ./internal/executor/codex -count=1`: 全绿
    - `go test ./internal/agentd -run 'TestApprover|TestDecide|TestApprovalClient' -count=1`: ok (3.335s)
- T4 变异自验：
  - 变异点：`internal/agentd/approver.go` 的 `Decide` 中将 `req.Limits.Effort = executor.EffortLow` 注释掉。
  - 变异执行：`go test ./internal/agentd -run TestDecideInvokesOneShotWithGrokEffortLow -count=1` 报 `--- FAIL: TestDecideInvokesOneShotWithGrokEffortLow (0.00s): approver_test.go:749: grok 审批必须由调用方传入 EffortLow，got ""`。
  - 恢复变异后验证重归绿灯：ok 0.003s。
## Task 5: 隔离 HOME 规则供给迁入 Profile.Prepare

- TDD 红灯：
  - 测试：`internal/executor/opencode/profile_test.go`（`TestProfileZeroReport`, `TestProfilePrepareWritesRulesAndSkills`, `TestProfilePreparePermissionFailure`）、`internal/executor/claudecode/profile_test.go`（`TestProfileZeroReport`, `TestProfilePrepareEngineDeficiency`）、`internal/agentd/coordinator_home_test.go`。
  - 执行 `go test ./internal/agentd -run TestCoordinatorHome -count=1`：
    - `--- FAIL: TestCoordinatorHomeSupplyOnLaunchAndResume (0.00s): runner.Launch: 协调者供给缺少 Profile`。
- T5 最小实现与绿灯：
  - 新建五家 `profile.go`（`opencode`, `claudecode`, `grok`, `codex`, `agy`）：各家导出 `RulesRelFile` 与 `SkillsRelDir` 常量，`*Adapter` 实现 `executor.Profile` 接口（`Inspect`, `Prepare`, `Verify`）；opencode 写入 `.config/opencode/AGENTS.md` 与 skills 目录，claude 返回引擎不足 Note 且 Prepared=true。
  - 修改 `internal/agentd/coordinator_home.go`：`coordinatorHomeSupplier` 引入 `profileFor func(cli string) (executor.Profile, error)` 与 `loadRules func(mainHome, cli string) ([]executor.ProfileFile, []executor.ProfileFile, error)`；`Prepare` 调用 `prof.Prepare` 并校验 `rep.Prepared`；彻底删除 `copyCoordinatorRules` 及相关私有函数；保持 symlink 白名单防护。
  - 修改 `internal/agentd/server.go`：`Server` 增加 `ruleLoader` 字段与 `SetRuleLoader` 方法；`SetupAutomation` 组装 `supplier.loadRules = s.ruleLoader` 与 `supplier.profileFor`（从 `s.providers.Get(base)` 取出并类型断言 `executor.Profile`）。
  - 修改 `cmd/agentd.go`：在组装根增加 `loadCarrierRules` 与 `readSkillTree`，持有五家路径常量从主 HOME 抽象读取；调用 `srv.SetRuleLoader(loadCarrierRules)`。编排包 `internal/agentd` 保持零知识。
  - 架构图契约与代码洁净度检查：
    - `git grep "copyCoordinatorRules" -- "*.go"`：全仓生产代码零命中。
    - `codegraph --repo . --view cards-B233.2-charter check` 维持基线存量 2 fail（ApprovalClient），新增 0 fail。
  - 测试通过：
    - `go test ./internal/executor/opencode ./internal/executor/claudecode -run TestProfile -count=1`: ok (0.004s)
    - `go test ./internal/agentd -run TestCoordinatorHome -count=1`: ok (0.016s)
    - `go test ./internal/agentd -count=1`: 全绿 (186.554s)
    - `go test ./cmd -run 'Agentd|Skill' -count=1`: ok (0.364s)
- T5 变异自验：
  - 变异点：`internal/agentd/coordinator_home.go` 的 `Prepare` 中将 `if !rep.Prepared` 变异为 `if rep.Prepared`。
  - 变异执行：`go test ./internal/agentd -run TestCoordinatorHome -count=1` 报 `--- FAIL: TestCoordinatorHomeSupplyOnLaunchAndResume (0.00s): runner.Launch: Profile.Prepare 未成功 prepared=false notes=[]`。
## Task 6: Skills 路径表迁出

- TDD 红灯：
  - 测试：新建 `internal/executor/skills_paths_test.go`（`TestNativeSkillRelDirs`, `TestAdaptersImplementSkills`）。
  - 执行 `go test ./internal/executor -run 'TestNativeSkillRelDirs|TestAdaptersImplementSkills' -count=1`：
    - 编译红：各 Adapter 未实现 `executor.Skills` 接口（`Inspect(context.Context, string) ([]executor.SkillSite, error)` 等）。
- T6 最小实现与绿灯：
  - 新建五家 `skills.go`（`opencode`, `claudecode`, `grok`, `codex`, `agy`）：`*Adapter` 实现 `executor.Skills`（`Inspect`, `Install`, `Remove`），内部引用各家 `SkillsRelDir`，遵守父目录缺失则跳过（不代建 HOME）原则，`Remove` 调用 `executor.GuardRemove` 鉴权。各家通过 `Profile()` 方法提供 `executor.Profile` 避免 Go 语言层面 `Inspect` 方法同名冲突。
  - 修改 `internal/skill/install.go` 与 `state.go`：删除静态 `agentDirs` 表；`Install` 与 `Status` 接收 `providers []executor.Skills`；基准副本保持；单家失败（返回错误或跳过）记录至 Site Note 并继续，不全盘报错。
  - 修改 `internal/skill/install_test.go` 与 `state_test.go`：注入五家真实 Provider 实例 `defaultTestProviders()`；增加单家失败其它仍成功的组合测试 `TestInstallOneProviderFailsOthersSucceed`；移除旧静态表测试（已转至 `skills_paths_test.go`）。
  - 修改 `cmd/skill.go` 与 `cmd/status.go`：在组装点通过 `defaultSkillProviders` 统一注入五家 `executor.Skills`。
  - 架构图契约与代码洁净度检查：
    - `git grep "agentDirs" -- "*.go"`：全仓零命中。
    - `git grep ".claude/skills" internal/skill cmd`：除单测金样本外生产代码零平行表。
    - `codegraph --repo . --view cards-B233.2-charter check` 维持基线存量 2 fail（ApprovalClient），新增 0 fail。
  - 测试通过：
    - `go test ./internal/skill ./internal/executor ./cmd -run 'Skill|Status' -count=1`: 全绿
- T6 变异自验：
  - 变异点：`internal/skill/install.go` 的 `Install` 中将调用 `p.Install` 循环变异为 `continue` 跳过。
  - 变异执行：`go test ./internal/skill -run TestInstallSkipsMissingAgentDirs -count=1` 报 `--- FAIL: TestInstallSkipsMissingAgentDirs (0.00s): 已装落点 = 1，期望 2`。
  - 恢复变异后验证重归绿灯：ok 0.003s。

