# B351 breakdown 节点台账

- 2026-09-09：入口命令 `pwd && git status --short --branch && rg --files -g '!*node_modules*' | sed -n '1,160p'` 原始输出为工作树 `/root/.handoff/worktrees/0c9a4c03`、分支 `cards/B351-charter-2`，无未提交改动；本轮不切分支、不改 git 配置。
- 2026-09-09：亲读 `docs/superpowers/specs/b351.md`、`docs/superpowers/specs/b351-contract.md`、`docs/superpowers/reviews/b351-spec-review.md`；spec 头部原文状态为「已批准」（2026-09-09），contract 头部原文为「本提交随 ... 冻结」，契约的冻结声明与本节点输入一致。
- 2026-09-09：`best.json` 顶层域查询命令 `jq -r '.domains | to_entries[] | select(.value.parent == null) | [.key, (.value.type // ""), (.value.label // "")] | @tsv' codegraph/best.json` 实际列出 15 个父为空域；本卡提案仅触及 `d_ledger`、`d_orchestration`、`d_transport`、`d_cli`、`d_gateway`。
- 2026-09-09：代码图帮助命令 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --help` 实际返回 `resolve`、`sym`、`validate` 等子命令；后续文档锚点核验使用仓内规定的 `go run ... codegraph --repo .` 入口，不调用 handoff CLI、不派发、不起 executor。
- 2026-09-09：源码核对确认 `internal/ledgerstep/dispatch.go#Dispatcher.ViaTemplate` 在 Transport 成功后分别经过 WriteGate 与 `RecordDispatchAndLinkTask` 失败出口；`internal/ledger/events.go#Store.PurposeRounds` 当前只统计 `TasksOf` 挂账行；`cmd/card_dispatch.go` 与 `internal/agentd/cardstep.go#Server.startCardStep` 是两处生产 Dispatcher 装配，`cmd/card_node.go#runStepDispatch` 只 POST 本机 agentd。
- 2026-09-09：源码核对确认 `internal/client/client.go#Client.Stop` 成功后保留 managed worktree，`internal/agentd/manager.go#Manager.Reclaim` 仅对终态 managed worktree 做资源回收且不删分支；因此 breakdown 将 Stop→Reclaim(force=true) 作为同一补偿接缝，404/409 语义按 contract 逐项验收。
- 2026-09-09：代码图 `sym` 实际命中 `targetClient`（`n_cmdtargetClient`，`d_cli`）、`cliTransport`（`n_cmdcliTransport`，`d_cli`）、`runStepDispatch`（`n_cmdrunStepDispatch`，`d_cli`）与 `Server.startCardStep`/`Server.clientForTarget`/`Server.stepTransport`（`d_gateway` 相关节点，部分 anchor=moved）；同一串查询在 `sym ddlStatements` 处失败，原始输出为 `Error: 符号 "ddlStatements" 不在图中（图未覆盖或名字有误）；近似候选: []`，故 DDL 入口按源码 `internal/ledger/store.go#ddlStatements` 及 contract 图覆盖债记载，未把它当图事实。
- 2026-09-09：本节点法定产出 `docs/superpowers/specs/b351-breakdown.md` 已按四项产出要求落盘：集中待拍板清单、4 个顶层域与派卡资格核、contract #1–#68 逐条对照、行为闭环、单一 `B351-impl` 四段式子卡/DAG、逐族缺陷审查及真机清单；未写实现代码、未建卡、未派发。
- 2026-09-09：命令 `git diff --check && git status --short --branch && git diff --stat` 实际无 `git diff --check` 输出；随后原始输出为 `## cards/B351-charter-2`、两个未跟踪文件 `docs/superpowers/ledgers/2026-09-09-b351-breakdown-ledger.md` 与 `docs/superpowers/specs/b351-breakdown.md`，尚未提交。
- 2026-09-09：命令 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . --view cards-B351-charter resolve --doc docs/superpowers/specs/b351-breakdown.md` 实际退出码 0；原始 JSON 报告 17 个锚点，状态仅为 `ok`/`moved`，无 `vanished`/`file_missing`。坏锚自检通过。
- 2026-09-09：预提交命令 `git diff --cached --check && git diff --cached --stat` 首次失败；原始输出逐行报告 `docs/superpowers/specs/b351-breakdown.md:3` 至 `:10: trailing whitespace.`（Markdown 强制换行的两个空格）。已删除这些尾随空格，未改变文义；该次命令未作为通过依据。
- 2026-09-09：提交命令 `git commit -m "docs(B351): add orphan compensation breakdown"` 原始输出为 `[cards/B351-charter-2 cdbd9b86] docs(B351): add orphan compensation breakdown`、`2 files changed, 318 insertions(+)`，并创建 breakdown 与 breakdown-ledger 两个文件；随后仅为收录此提交事实执行一次 amend。
- 2026-09-09：协调者拍板 F1–F4 全部维持冻结（无新岔口）；头部回写「已拍板」。提交命令（历史读数）：`git commit -m "docs(B351): 拍板拆解 F1-F4 维持冻结"`。
