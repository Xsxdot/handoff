# B233.2 breakdown 台账

日期：2026-09-07。节点：charter breakdown。工作分支：`cards/B233.2-charter-2`。开工 HEAD：`f571e323860b8ae3dd9e1cf10ae0b7a7597a5803`。

## 过程

1. 形态 2：handoff executor 出稿，协调者拍板。不写实现、不建卡、不派发、不调 handoff CLI。法定产出 `docs/superpowers/specs/b233.2-breakdown.md`。L3 轻档；prompt：「不要扇出子卡，除非拆解时证明单子系统工作量超过流程固定成本。迁全部现有 harness，不是试点+第二家。OpenCode 真机必须用免费模型。」
2. 上游状态位：spec `docs/superpowers/specs/b233.2.md` 头部「已批准」（2026-09-06，用户原话「按这个推进吧」；并纠正迁全部现有 agent）。契约 `docs/superpowers/specs/b233.2-contract.md` 头部「冻结状态：随本提交冻结」，无日期+提交号。HEAD=`f571e323` 即 contract 提交（`git log -1 --oneline` → `f571e323 contract(B233.2): 冻结 harness 五类能力面契约`）。
3. 有效基线：prompt 写 `cards/B233.1-charter-7`。本工作树历史已含 `626fcdf5 feat(B233.1)` / `94246fc8 fix(B233.1)` / `b3f711ab graph(B233.1)`，再叠 `124121e4` spec 合并与 `f571e323` 契约冻结。
4. 子系统清单以 `codegraph/best.json` 顶层领域为准（`parent` 空 + `type`）：15 个顶层域。本卡触及候选：`d_execution`(boundary)、`d_orchestration`(logic)、`d_keystone`(logic)、`d_maintenance`(boundary)、`d_cli`(logic)、`d_gateway`(boundary)、`d_policy`(logic，接 233.1 免审不放宽)。不新立 `d_harness`（契约 §4.1 已冻）。
5. 架构法第三条实读（非测试 `.go`，本轮 `os.walk` 计数）：`internal/agentd` 85 文件 / 28288 行（命中「单包 ≥40 且无子包」与项目红线 20000 行）；最大前缀家族 `preview*`=10（与本卡无关）。`internal/executor` 92 文件 / 19570 行，有子包，未命中「无子包」；根包 17 文件 / 1215 行。`internal/hostapi` 5/892；`internal/skill` 2/186；`internal/keysclient` 1/83。`cmd` 56/9695（正当扁平，本卡只触 `cmd/skill.go` 与 `cmd/agentd.go` 组装表）。
6. `codegraph --repo . domains` 默认基线视图出现 `d_coordination`，与 best 顶层域不一致——清单不以基线视图为准。`codegraph --repo . views`：`cards-B233.1-charter-2`、`cards-B233.2-charter`。
7. Ticket 0 符号（`--view cards-B233.2-charter`）：`Recoverer` / `Coordinator` / `OneShot` / `Profile` / `Skills` / `Provider` / `OneShotArgv` 均 `anchor=ok`，`status=added`。容器 domain 字段仍是基线 `d_executor`（契约 §11 已记；best 归 `d_execution_contract`）。
8. `who-calls OneShotArgs` → `n_agentd_Approver_Decide`（基线域标签 `d_coordination_task`）。`who-calls Install` → `n_cmd_skillInstallCmd_RunE` / `e_cli_skill_install`。`who-calls n_hostapi_Host_RunTurn` 只列出 `coordinatorRunner.Resume`，漏 Launch（与契约 §11 一致）。`who-calls copyCoordinatorRules`：节点不在图中。
9. `codegraph check --view cards-B233.2-charter`：`fails` 2 条（`d_execution→d_orchestration` dead-contract / dead-interface `ApprovalClient`），与契约 §11 相同；`d_orchestration→d_execution` 预算内 26/27。`check --view cards-B233.1-charter-2`：`fails []`。`target.json` 无 `d_keystone→d_execution`。
10. 代码事实（本轮读源，非行为验收）：`Manager.Dispatch` 已注入 `StartReq.Approval`（B233.1）；`Approver.Decide` 仍调 `OneShotArgs`；`coordinatorRunner.Launch` 用 `context.Background()` + `Host.RunTurn`；`copyCoordinatorRules` 写死 `.config/opencode`；`claude.WriteTaskEnv` 无 `PolicySnapshot` 入参、`allowRules` 包级表；`supportedCLIs` 仅 `opencode`；`credRelPath` 无 claude / 无 agy；五家 `var _ Recoverer` 已钉，`manager.go` 仍有私有 `restorer`；`fake` 无 Resume。
11. `git rev-parse cards/B233.1-charter-7`：本工作树无该分支名。历史已含 B233.1 实现提交。不把缺分支名写成「233.1 未落地」。
12. 不退回 contract：冻结 1–134 均可归入内部 ticket；无新接缝。不扇出：未证明单子系统工作量超过扇出固定成本（共享 Registry/Manager/Approver 接缝，按厂商拆会把契约面拆碎）。
13. 项目缺陷族清单顶部仍缺 `基线版本：charter@<commit>`——按项目清单加严并并入承重安全属性；清单债不在本卡改实例化协议。
14. 法定产出写到 `docs/superpowers/specs/b233.2-breakdown.md`。契约追加 §12 C-0…C-8，冻结条目 1–134 未改；头部状态补钉为「已冻结（2026-09-07，提交 f571e323）」。
15. `codegraph --repo . resolve --doc docs/superpowers/specs/b233.2-breakdown.md --view cards-B233.2-charter`：先因表格里短锚 `taskenv.go#WriteTaskEnv` 报 `file_missing`（exit 1）。改为 `internal/executor/opencode/taskenv.go#WriteTaskEnv` 后：exit 0，27 锚，`ok` 19 / `moved` 8，坏锚 0。
16. `codegraph --repo . resolve --doc docs/superpowers/specs/b233.2-contract.md --view cards-B233.2-charter`：exit 0。
17. 待拍板岔口 P1–P6 集中在稿首。真机清单 16 条。未调用 handoff CLI、未起 executor、未写实现代码。不自批。

## 判断

- L3 轻档单卡直进：未证明单子系统超过扇出固定成本；按厂商拆 spec 已否。P1 待拍板。
- 不退回 contract：无新方法/新事件类型/新 CLI。冻结 1–134 均可归入 T1–T7。
- agentd 85 文件/28288 行命中升格判据，回答为「本卡圈得出有界集，不插竖切」。
- 最大岔口是 P1（是否迁重档）与 P2（Bundle vs 两张表）。

## 提交

```
git commit -m "docs(B233.2): 拆解 harness 能力归拢"
[cards/B233.2-charter-2 c0266d7a] docs(B233.2): 拆解 harness 能力归拢
 3 files changed, 475 insertions(+), 1 deletion(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.2-breakdown-ledger.md
 create mode 100644 docs/superpowers/specs/b233.2-breakdown.md
```

随后 amend 一次把本段收进同批提交。不回填 amend 后 HEAD。

## 拍板

2026-09-07 协调者：P1–P6 全选 (a)。理由见拆解稿表「裁决」列。新提交，不 amend 出稿提交。
