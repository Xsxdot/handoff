# B358.4 implement 台账（2026-09-12）

- 执行者：charter implement 节点（子卡 B358.4，分支 `cards/B233.1-charter-7` @ `bb7355b0` 起跑）
- 依据：`docs/superpowers/plans/b358.4-plan.md` + 协调者拍板三条（§12.1 修正：CreateSession owner 取请求体统一记法 `user:`/`agent:` 前缀校验；审计 actor 照旧 roomUserActor 注入；owner 是初始成员，两字段不混用）
- 有界文件集：`internal/agentd/sessionsapi.go`（新）+ `sessionsapi_test.go`（新）+ `ledgerapi.go`（注册 6 行）+ `roomsapi_test.go`（红窗 10 支改写）+ `internal/collab/sessions_test.go`（移交两支）+ 本台账

## 续跑接手（2026-09-12，第二任执行者）

- 前任执行者因平台故障死于 Task 0（基线复跑未完成）：本文件当时仅上面 8 行存根，零代码文件、零提交。本节起为第二任执行者续写，从 Task 0 重跑。
- 分支 `cards/B233.1-charter-7` @ `bb7355b0` 确认；工作树脏文件仅 `web/src/app/task/Composer.test.tsx`（M）、`.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、`web/design-qa*`（均禁触不卷入）。

### 拍板①落点设计（读码后预登记，读码出处随行）

1. **`handleSessionCreate`**：请求体解码 `{title, owner}`，owner 前缀校验 `user:<name>`/`agent:<name>`（其余 400）；审计 actor = `roomUserActor(r)` 服务端注入，与 owner 两字段不混用（owner 进 `Members` 初值，`ledger/sessions.go#CreateSession` :39-75 `Members: []string{owner}` 已核对）。
2. **夹具 `mustConsoleSession` 调整（拍板①连锁，plan §4 原文按旧口径假设 owner=roomUserActor）**：经 HTTP 建会话 body 带 `owner="user:tester"`（统一记法先例 `wakeconsumer_b358_test.go#mustWakeSessionFixture`），随后以 `ledger.Store.AddSessionMember(session.ID, webUserMember, …)` 把控制台成员显式坐进会话——因为 HTTP 发言路径 actor 由服务端注入恒为 `web:127.0.0.1`（`roomsapi.go#handleRoomSend` :336-338），而书写者执法要求 actor ∈ Members∪席位（`room/room.go#VerifyWriter` :102-114）；拍板①后 owner 不再是 webUserMember，不坐进则全部 HTTP 发言 403。Store 级 AddSessionMember 零事件副作用（`ledger/sessions.go#AddSessionMember` 不落事件），即 plan §8.5「facade 方法已备好」的测试侧使用，不违反「不加 AddSessionMember 端点」显式不做项。
3. **TestInbox* 五支 mention 夹具的发送者改 `user:tester`（夹具 owner），不用 plan 模板里的 webUserMember**：`sendToSession` 对 kind=user 消息触发 `consumeRoomMentions(roomID, actor)`（`collab/service.go#sendToSession` :158-164）——发送者=@ 目标=webUserMember 时，@webUserMember 的提及刚落账即被发送者自己的回复清空，收件箱 mention 源必空、五支必红。发送者=owner（真成员）+ @webUserMember 才是「别人 @ 控制台成员」的语义。偏差记 Task 2 偏差清单。
4. **TestSessionsListUnreadAndMemberForgery 的 `found.Owner` 断言改对 `sessionFixtureOwner`（`user:tester`）**：plan 原文对 webUserMember 是旧口径残留（拍板①后 owner=请求体值），断言意图「标题/群主投影正确」不变。偏差记 Task 1 偏差清单。
5. **`handleSessionDetail` 把会话 id 包回错误文案（`fmt.Errorf("会话 %s: %w", id, err)`）**：plan §2.2 声称 404 文案含会话 id「由 getSessionTx 文案 + writeErr 原样透出满足」在现码链不成立——`ledger/api/api.go#translateNotFound` 对 ErrNotFound 返回**裸** `client.ErrNotFound`（丢 Store 层「会话 %s: …」文案），`collab/sessions.go#mapSessionError` 再归一为**裸** `ErrNoRoom`（文案「collab: 房间不存在」）。plan 自己的验收（Task 1 验收 2「404+文案含 id」）是绑定断言，最小落点在本卡有界新文件 handler 内包 id（%w 保住 ErrNoRoom 映射 → 仍 404）。plan 内部矛盾（handler 代码 vs 自家验收）按验收侧收口，偏差记 Task 1 偏差清单。

## Task 0：基线复跑（@ `bb7355b0`，与 plan 基线 `75adb4eb` 同树——plan 提交 bb7355b0 只加 plan/拍板文档）

七条命令逐条复跑，读数与 plan §1 基线**全部一致**，无停手项：

| # | 命令 | 本次读数 | 与基线比对 |
|---|---|---|---|
| 1 | `go build ./...` | 退出 0 | 一致 |
| 2 | `go test ./internal/agentd -count=1` | FAIL，失败恰 11 支：红窗 10 支（红因逐支 `collab: 房间已只读（并入冻结或终态归档）`，TestRoomSendEndpoint 的 HTTP 面 409 同文案）+ `TestLegacyNodeEventSequenceUnchanged`（golden 漂移：got 第 2 条 dispatched vs want comment） | 一致 |
| 3 | `go test ./cmd -count=1` | FAIL，`--- FAIL` 恰 8 支：TestRepoContractGate / TestServePermissionHookDenyWithReasonAndStep0 / TestRoomSendLandsRoomMessageWithUserKind / TestRoomSendCoordinatorAcceptsExplicitSeatFlags / TestRoomSendCoordinatorKindUsesGrokHostSession / TestRoomSendCarriesRefAndMention / TestRoomReadStdoutSeqPrefix / TestRoomListSortedByActivity | 逐支同名一致 |
| 4 | `go test ./cmd -run TestRepoContractGate -count=1 -v` | 违规恰 6 条（原文如下） | 一致 |
| 5 | `go test ./internal/agentd -run 'TestSession' -count=1` | ok，恰 auth 域 4 支 PASS（TestSessionFailureReasons / TestSessionRoutesListRevokeLogout / TestSessionCannotIssueTicket / TestSessionCookieAttrsStayInSync） | 一致 |
| 6 | `go test ./internal/collab/... -count=1` | ok（collab、collab/room 绿；client/cursor 无测试文件） | 一致 |
| 7 | `grep -rn "api/sessions" internal/ cmd/ --include="*.go" \| grep -v _test` | 零命中（grep 退出 1） | 一致 |

**TestRepoContractGate 基线违规 6 条原文**（收口比对用，逐字）：

1. `契约违规 [dead-interface] 契约 d_gateway→d_orchestration 声明的接口 "OrchestrationClient" 在 d_gateway 中不存在（无同名非 deleted 节点；期望在 d_gateway 找到）`
2. `契约违规 [over-budget] d_cli->d_workspace 直调 11 条超出预算 9`
3. `契约违规 [over-budget] d_gateway->d_workspace 直调 17 条超出预算 1`
4. `契约违规 [over-budget] d_orchestration->d_workspace 直调 49 条超出预算 19`
5. `契约违规 [over-budget] d_workspace->d_orchestration 直调 3 条超出预算 2`
6. `契约违规 [over-budget] d_workspace->d_protocol 直调 23 条超出预算 3`

**红窗 verbose 枚举**（`go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1 -v`）：

- FAIL 恰 10 支：TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection / TestRoomMessagesEndpoint / TestRoomSendEndpoint / TestRoomReadEndpoint / TestInboxThreeSources / TestInboxMentionSource / TestInboxRefIDShapes / TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke——与 plan §0.4 逐支同名。
- 存活确认 PASS：TestRoomSendErrMapping、TestRoomsEndpoints503WithoutLedger、TestRoomsListWithoutAttachmentKeepsAttachMissing、TestRoomsListWireIncludesZeroUnread、TestRoomsListAttachTimeoutDoesNotBlockMainList、TestRoomsListUsesBackgroundAttachCache、TestRoomsListDropsExpiredAndFailedRemoteAttachCache、TestRoomsListTTFB200Cards、TestInboxDecisionOpenAllIncludingProjectLevel、TestInboxTicketExcludesDrivenTask、TestInboxDestructiveTicketFloatsWithWatchers。

（`go test ./cmd -run TestRoom -count=1 -v`）：FAIL 恰 6 支（TestRoomSendLandsRoomMessageWithUserKind / TestRoomSendCoordinatorAcceptsExplicitSeatFlags / TestRoomSendCoordinatorKindUsesGrokHostSession / TestRoomSendCarriesRefAndMention / TestRoomReadStdoutSeqPrefix / TestRoomListSortedByActivity）；存活 PASS：TestRoomSendUserRejectsSeatFlagsWithoutSideEffects、TestRoomInboxWalksAgentdHTTP——cmd 禁触面无意外。

**Task 0 验收**：七条读数一致；红窗名单逐支同名；存活测试绿；已记。

## Task 1：会话六端点（红→绿）

- **红**：新建 `sessionsapi_test.go`（7 支 + 夹具）后 `go test ./internal/agentd -run 'TestSession' -count=1 -v` → 新 7 支全 FAIL，红因逐支 `POST /api/sessions: 404 404 page not found`（路由未接=功能缺失非 typo）；auth 域 4 支保持 PASS。
- **绿**：新建 `sessionsapi.go`（六 handler + `sessionErr` + `validSessionOwner`；拍板①：`handleSessionCreate` 请求体解码 `{title, owner}`、owner 前缀校验 `user:`/`agent:`（`validSessionOwner`，web: 机器位/裸名/缺失一律 400）、审计 actor=`roomUserActor` 服务端注入）+ `ledgerapi.go#registerLedgerRoutes` 追加 §2.1 六行（rooms 块后，逐字）。
- `go build ./...` 退出 0；`go test ./internal/agentd -run 'TestSession' -count=1` → ok（新 7 支 + auth 4 支全绿）。
- **验收字面断言**：
  1. `grep -c "api/sessions" internal/agentd/ledgerapi.go` = 6，六行与 §2.1 逐字一致（ledgerapi.go:56-61）；独占注册 grep（排除 _test/ledgerapi/sessionsapi）零命中。
  2. 映射表：TestSessionDetailEndpointKeyset（404+文案含 `session:999`）/ TestSessionJoinCardEndpoint（409 含「已属会话」、404×2）/ TestSessionArchiveEndpoint（409 含「已归档」）/ TestSessionLeaveCardEndpoint（404）逐格绿。
  3. 反例两支绿：TestSessionsCreateEndpoint（owner=`user:mallory` 生效、落账 actor=webUserMember 与 body 塞的 actor 无关、`web:127.0.0.1`/裸名/缺 owner 各 400、空标题 400）；TestSessionsListUnreadAndMemberForgery（`?member=user:mallory` 伪造不影响 Unread=2）。
  4. 键集金样绿：顶层恰 {summary,timeline}+nodes 缺键、Summary 11 键、Member/Card 键集、cards 恰一张、seat 缺键，全穿真实 HTTP。
  5. `go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1` 失败仍恰基线 10 支（Task 1 未动 roomsapi_test.go）。

### Task 1 偏差清单

1. **TestSessionDetailEndpointKeyset 的 SessionMember[0] 键集 3→4**：plan 断言 {identity,kind,status}，现仗实得 4 键——`last_active` 虽带 omitempty tag，encoding/json 对 time.Time struct 的 omitempty 无效，零值照序列化（`0001-01-01T00:00:00Z`）。生产 proto 禁触，断言按现仗行为收口为 4 键 + 零值时刻判据（「无租约成员报零值、不伪造 now」，比原断言更硬）。
2. **`handleSessionDetail` 把会话 id 包回错误文案**（`fmt.Errorf("会话 %s: %w", id, err)`）：plan §2.2 声称 404 文案含 id 由 Store 层文案透出满足，实际 `translateNotFound`（返回裸 client.ErrNotFound）与 `mapSessionError`（返回裸 ErrNoRoom）双重归一丢文案；plan 自家验收要求文案含 id，最小落点在本卡有界新文件内（%w 保住 ErrNoRoom→404 映射）。详见「续跑接手」预登记 5。
3. **夹具 `mustConsoleSession`**：plan 原文按旧口径（owner=roomUserActor）写「owner 即控制台成员」；拍板①后改为请求体 owner=`user:tester` + `Store.AddSessionMember` 坐进控制台成员 webUserMember（HTTP 发言 actor 服务端注入恒 web:127.0.0.1，不坐进则全部发言 403）。详见「续跑接手」预登记 2。
4. **TestSessionsListUnreadAndMemberForgery 的 Owner 断言**改对 `sessionFixtureOwner`（plan 原文对 webUserMember 是旧口径残留）。详见「续跑接手」预登记 4。

## Task 2：红窗 10 支改写收口

- roomsapi_test.go 就地改写：文件头追加改写说明；10 支函数体逐支改写、名字全保留，每支首行有「B358.4 红窗改写」注释（文件头 1 处 + 逐支 10 处 = 11 处标记）。
- 前五支（TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection / TestRoomMessagesEndpoint / TestRoomSendEndpoint / TestRoomReadEndpoint）按 plan §5 对照表全文落：夹具卡房间→会话房间（mustConsoleSession）、发送者 webUserMember（夹具已坐进成员集）、TestRoomSendEndpoint 改全流读 `EventsFromAsc(nil,…)` + 补「不存在会话房间→400」断言。
- TestInbox* 五支仅动 mention 源夹具：插入 `session := mustConsoleSession(t, env, "<场名>")`（场名依次：收件箱三源场/收件箱提及场/收件箱引用形状场/收件箱键集场/收件箱冒烟场），发送目标换 `session.ID`；decision/ticket 夹具与全部键集/RefID/冒烟断言逐字保留；赋值形态逐支照抄原文件（捕获 seq 两支 `seq, err := …`，不捕获三支 `if _, err := …`）。
- `go build ./...` 退出 0；`go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1 -v` → 21 支全 PASS（红窗 10 支收口绿 + 存活 11 支无波及）。
- `go test ./internal/agentd -count=1` → 失败恰 1 支：`TestLegacyNodeEventSequenceUnchanged`（B362 golden，禁触面原红因漂移）。
- diff 核对：`git diff -U0 internal/agentd/roomsapi_test.go` 的 hunk 只落在文件头 + 恰 10 支函数（TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection / TestRoomMessagesEndpoint / TestRoomSendEndpoint / TestRoomReadEndpoint / TestInboxThreeSources / TestInboxMentionSource / TestInboxRefIDShapes / TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke），其余测试逐字未动。

### Task 2 偏差清单

1. **TestInbox* 五支 mention 发送者用 `sessionFixtureOwner`（user:tester），不用 plan 模板里的 webUserMember**：`sendToSession` 对 kind=user 落账后调 `consumeRoomMentions(roomID, actor)`——发送者=@ 目标=webUserMember 时刚落的 @ 立即被消费，收件箱 mention 源必空、五支必红（读码结论，Task 2 首跑前预登记于「续跑接手」3；绿跑证实）。发送者=owner 才是「别人 @ 控制台成员」的语义。

## Task 3：B358.1 review 移交两支（collab 直测）

- `internal/collab/sessions_test.go` 末尾追加 `listAllCardsFailingClient` stub + `errInjectedLedgerRead` + 两支测试；既有内容零改动（git diff --stat internal/collab 只含 sessions_test.go，+56 行）；imports 零新增。
- `go build ./...` 退出 0；`go test ./internal/collab -run 'TestSessionWritersReadFailureNotWriterGuard|TestSessionSendToMissingSessionRoomIsErrNoRoom' -count=1 -v` → 两支 PASS；`go test ./internal/collab/... -count=1` 全绿；collab 生产文件零改动。

### Task 3 偏差清单

1. **TestSessionWritersReadFailureNotWriterGuard 夹具补「拉卡进群」一步**：plan 原文按「写下去即绿」给出夹具（只建会话不拉卡），首跑即红——`sessionWriters` 对无卡会话在 `len(session.Cards)==0` 短路返回显式成员（`collab/service.go#sessionWriters`），ListAllCards 根本不被调用，发言成功、读失败构造不出来（红因原文：`sessions_test.go:1044: 读卡列表失败后发言应报错`）。测试侧补 `sessionCard` + `svc.JoinCard` 后收绿，断言意图（读失败原样上抛、不伪装 ErrNotWriter）不变；collab 生产码未动。

## Task 4：收口核算与变异自验

### 三段律全量

- `go build ./...` → 退出 0。
- `go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` → 退出 0（collab、collab/room、ledger、ledger/api、proto 全 ok；collab/client、collab/cursor 无测试文件）。
- `go test ./internal/agentd -count=1` → FAIL，失败集合**恰 1 支**：`TestLegacyNodeEventSequenceUnchanged`（B362 golden，禁触面，原红因漂移仍在）——逐支比对：基线 11 支 − 红窗 10 支（本卡收口绿）= 恰 golden 1 支，无增无减。
- `go test ./cmd -count=1` → FAIL，失败集合**恰基线 8 支逐支同名**：TestRepoContractGate / TestServePermissionHookDenyWithReasonAndStep0 / TestRoomSendLandsRoomMessageWithUserKind / TestRoomSendCoordinatorAcceptsExplicitSeatFlags / TestRoomSendCoordinatorKindUsesGrokHostSession / TestRoomSendCarriesRefAndMention / TestRoomReadStdoutSeqPrefix / TestRoomListSortedByActivity——与 Task 0 基线逐支比对无增无减。
- `go test ./internal/agentd -run 'TestSession' -count=1` → ok（新 7 支 + auth 4 支）。

### 契约闸两轮逐行比对（基线 → 收口）

两轮清单**逐行一致**（1 dead-interface + 5 over-budget，数字全同）：dead-interface OrchestrationClient；over-budget d_cli→d_workspace 11/9、d_gateway→d_workspace 17/1、d_orchestration→d_workspace 49/19、d_workspace→d_orchestration 3/2、d_workspace→d_protocol 23/3。**无新增、无减少**（本卡零新增跨域 import）。

### 变异抽查两发（先红后还原绿，变异不提交）

- **变异①**（`handleSessionsList` 的 `member := s.roomUserActor(r)` 改读 `r.URL.Query().Get("member")`，编译过）：先做行为断言——`TestSessionsListUnreadAndMemberForgery` 翻红，红因原文 `sessionsapi_test.go:170: 已读后未读应为 0（伪造 member 参数不得带回他人未读）: …Unread:2`（变异按伪造成员的空游标算出未读 2）。还原后同支绿。
  - 配套偏差：plan 原版该测试两成员游标皆空、变异必存活（没牙）；补「POST /read 后带伪造参数重查、断言 unread=0」阶段使变异可观测（见 Task 4 偏差 1）。
- **变异②**（`sessionErr` 的 ErrNoRoom 格 `code = http.StatusNotFound` 改 `http.StatusBadRequest`——按变异纪律①用可编译等价变异改语义，不整行删格以免牵连 collab import 编译不过）：`TestSessionDetailEndpointKeyset` / `TestSessionJoinCardEndpoint` / `TestSessionLeaveCardEndpoint` 三支 404 断言全翻红，红因原文样例 `sessionsapi_test.go:253: 不存在会话详情应 404: 400 {"error":"会话 session:999: collab: 房间不存在"}`（顺带证实详情 404 文案含会话 id）。还原后三支绿。

### 图符号清单（交 integrate/recon 补 `codegraph/diffs` 视图；本卡触及面不在 best.json，一律 file#Symbol 源码锚）

新增生产符号（`internal/agentd/sessionsapi.go`）：

- `internal/agentd/sessionsapi.go#validSessionOwner`
- `internal/agentd/sessionsapi.go#sessionErr`
- `internal/agentd/sessionsapi.go#Server.handleSessionCreate`
- `internal/agentd/sessionsapi.go#Server.handleSessionsList`
- `internal/agentd/sessionsapi.go#Server.handleSessionDetail`
- `internal/agentd/sessionsapi.go#Server.handleSessionArchive`
- `internal/agentd/sessionsapi.go#Server.handleSessionJoinCard`
- `internal/agentd/sessionsapi.go#Server.handleSessionLeaveCard`

新增测试符号：

- `internal/agentd/sessionsapi_test.go#sessionFixtureOwner`（const）、`#mustConsoleSession`、`#assertKeyset`、`#TestSessionsCreateEndpoint`、`#TestSessionsListUnreadAndMemberForgery`、`#TestSessionDetailEndpointKeyset`、`#TestSessionJoinCardEndpoint`、`#TestSessionLeaveCardEndpoint`、`#TestSessionArchiveEndpoint`、`#TestSessionsEndpoints503WithoutLedger`
- `internal/collab/sessions_test.go#listAllCardsFailingClient`（含 ListAllCards 方法）、`#errInjectedLedgerRead`、`#TestSessionWritersReadFailureNotWriterGuard`、`#TestSessionSendToMissingSessionRoomIsErrNoRoom`

修改符号：

- `internal/agentd/ledgerapi.go#registerLedgerRoutes`（rooms 块后追加会话六行注册）
- `internal/agentd/roomsapi_test.go` 改写 10 支：TestRoomsListEndpoint / TestRoomsListUnreadAndAttachProjection / TestRoomMessagesEndpoint / TestRoomSendEndpoint / TestRoomReadEndpoint / TestInboxThreeSources / TestInboxMentionSource / TestInboxRefIDShapes / TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke（名字全保留）
- plan 文档：`internal/agentd/wakeconsumer_b358_test.go#mustWakeSessionFixture` 坏锚修复（补目录前缀）

（`ledgerDelete` 是 `internal/agentd/ledgerapi_test.go:114` 既有 helper，本卡只消费、非新符号。）

### 收尾锚点与文件集

- `codegraph resolve --doc docs/superpowers/plans/b358.4-plan.md`：修坏锚后退出 0（11 锚全 ok/moved）。
- `git status --porcelain` 受控 = 5 个有界文件（sessionsapi.go 新、sessionsapi_test.go 新、ledgerapi.go、roomsapi_test.go、collab/sessions_test.go）+ plan（锚点修复 1 行）+ 本台账；无关脏文件（web/src/app/task/Composer.test.tsx、.commandcode/、docs/superpowers/plans/b353-probe-follow-harness.md、web/design-qa*）不卷入提交。

### Task 4 偏差清单

1. **TestSessionsListUnreadAndMemberForgery 补「已读后伪造参数重查」阶段**：plan 的变异抽查①（handler 改读 query member）对原版测试不生效——两成员游标皆空、未读同为 2，变异存活。补 MarkRead 后重查断言 unread=0，原 handler 绿、变异红（变异纪律「先做行为断言确认打中」）。断言主判据（Unread=2）不变。
2. plan 文档锚点修复 1 行（`wakeconsumer_b358_test.go#mustWakeSessionFixture` → `internal/agentd/wakeconsumer_b358_test.go#mustWakeSessionFixture`），plan Task 4 步骤 4 明示授权。

### 收口判据对照（plan §13）

1. build 退出 0 ✓；2. agentd 失败恰 {golden} ✓；3. cmd 恰基线 8 支 ✓；4. 契约闸逐行一致 ✓；5. TestSession ok + collab/ledger/proto ok ✓；6. 文件集受控 ✓；7. 台账含 Task 0 读数、红窗核算、契约闸两轮、符号清单、变异两发 ✓。
