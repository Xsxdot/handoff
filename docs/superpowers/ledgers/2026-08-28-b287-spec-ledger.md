# B287 spec 台账（2026-08-28）

spec 正文：`docs/superpowers/specs/b287-spec.md`。
本文件记过程读数与判断原始记录；结论与裁决在 spec 正文。

## 卡与基线

- 2026-08-28 建卡 B287（`handoff card add --project handoff --base-branch acc/b156.2-156.3 --priority 中`，自动落 charter 流「待办」），note 记六项缺陷明细，随即 `card move B287 spec`。
- 工作树 `/Users/xushixin/workspace/handoff/.worktrees/acc-b156.2-156.3`（分支 acc/b156.2-156.3）本会话初快进 eea12d202 → 5d4b4a154（= origin 顶端），pull 复核 Already up to date。
- 用户提供的 6 张截图从 `~/Downloads/AnyTimeDelete/截屏/`（自动删除目录）拷入仓内 `docs/lab/screenshots/b287/`，按问题编号命名。

## 现状读数（探索 agent 2026-08-28，acc worktree；file:line 为现状快照）

**①待回复残留**
- 渲染：`web/src/app/rooms/RoomPanel.tsx:57`（`[待回复]` 前缀）+ `:70`（行琥珀底色）；入参 `:310` `roomNeedsReply(room, needRoomIDs)`。
- 数据：`web/src/app/rooms/roomPanelModel.ts:22-24`（`kind==='card' && needRoomIDs.has(room.id)`）；`needRoomIDs` 来自 `RoomPanel.tsx:163-166` 收件箱轮询 → `web/src/api/rooms.ts:122-124 fetchInbox` → GET `/api/inbox`。
- 后端三源：`internal/agentd/roomsapi.go:369-442 handleInbox`——①open 裁决（`:373-389`）；②waiting_answer 工单（`:399-420`，无 card_id 不产生角标）；③未消费 @提及（`:423-438`）。
- 发送路径：`RoomPanel.tsx:235-256 send()` → `web/src/api/rooms.ts:107-116` → `internal/agentd/roomsapi.go:321-346 handleRoomSend` → `internal/collab/service.go:80-101 Send`（只落 room_message）。发送后仅 `historyPoll.refresh()`（`:248`），不刷 inbox。
- 提及消费唯一入口 `internal/collab/service.go:195-216 Consume`，发送路径不调用 → 回复文字不清除任何一源（结构性，非偶发）。

**②消息不显示**
- 链路：`RoomPanel.tsx:321-322` history.map → `:195 historyPoll` → `:151-161 fetchRoomMessages(limit=200)` → GET `/api/rooms/{id}/messages?limit=200`（从不传 before 游标）→ `internal/agentd/roomsapi.go:301-317` → `internal/collab/service.go:128-147 History`。
- 主嫌疑：`History` 全流**升序**读（`internal/collab/room/room.go:227-241` 每页 1000）后 `internal/collab/service.go:142-144` 取最早 200 条即 break——超 200 条后新消息永远落在窗口外，前端无翻页。
- 次嫌疑：`web/src/app/data/usePoll.ts:55-65` refresh 复用 `inFlightRef` 里发送前启动的旧请求，旧 effect 已 cleanup，结果被 `if (stopped) return` 丢弃（`:92-96`）；`document.hidden` 停表 `:84-87`。轮询 5s（`web/src/app/rooms/constants.ts`），无 WS。

**③状态两遍**
- 看板卡：`web/src/app/cards/CardsPage.tsx:272` 传 `nodeTag`；`web/src/app/cards/CardItem.tsx:56`（右上角纯文本 status）+ `:84`（标签行黑 pill nodeTag）。判定 `web/src/app/cards/columns.ts:45-50 nodeLabelFor`（列内多节点时返回状态名本身）。
- 抽屉头：`web/src/app/cards/CardDrawer.tsx:611`（描边 pill status）+ `:612`（黑 pill nodeLabel）；nodeLabel 由 `:364-368` 同一 status 算出。
- 会话打开的抽屉 = 同一 CardDrawer（`RoomPanel.tsx:357` → `Shell.tsx:593` navigate → `CardsPage.tsx:100` cardDeepLink）。会话面板自身头部 `RoomPanel.tsx:361` 只渲染一次，不在去重范围。
- 背景先例：B279 原型决策「标签只在多对一列显形」（`prototypes/b279-automation-proto/README.md`，经 B279 spec 台账转引）。

**④悬浮球重叠**
- 会话球 `RoomPanel.tsx:374` `fixed bottom-20 right-5 z-40 size-11`（占 80–124px）；新建球 `web/src/app/homedock/HomeDock.tsx:68-85` `fixed right-5 bottom-11 size-11`（占 44–88px）→ 80–88px 带重叠 8px。错误 toast `Shell.tsx:644` 同列 `bottom-24`。
- 原型：IM 悬浮球 `board.html:247` `.im-dock right:20px bottom:80px`，无第二球（dock 条是侧栏底部，`board.html:303-310`）——重叠是实现期改动未错开。

**⑤拖动/缩放缺失**
- `RoomPanel.tsx:375` 浮窗 `fixed bottom-20 right-5 h-[520px] w-[360px]`，零 drag/resize 代码，几何写死。
- 现成模式：`web/src/app/homedock/HomeWindow.tsx:60-81`（document pointermove 拖拽会话）、`:88-90`（left/top/width/height style）、`:213-221`（右下 15px 拉伸角）；几何持久化 `web/src/app/homedock/useHomeDock.ts:83-119`。
- 原型本身非可拖浮窗（`board.html:178-249` 右侧常驻栏 + 收起悬浮球）——可拖/缩是本卡新增能力。

**⑥表单与原型差异**（实现 `web/src/app/settings/SchedulingPage.tsx:194-215` 自定义 overlay modal；原型 `prototypes/base/pages/settings.html:265-284` 载体 dialog 480px、`:287-303` 小队 dialog 440px，form-grid 两栏）
- label 缺语义后缀：原型「载体名（唯一，登记后不可改）」「模型（留空 = CLI 默认）」「成员载体（按勾选顺序解析：第一个健康且有空的载体领活）」「角色（不混编）」「并发上限（0 / 留空 = 不限）」，实现多数裸 label。
- 小队角色：原型显示「执行者队/协调者队」，实现直接暴露英文值 executor/coordinator。
- 原型有 role hint（协调者队成员必须落在协调机），实现无。
- 机器/CLI 选项两侧同为前端硬编码常量（实现 `SchedulingPage.tsx:18-23`），一致，不动。

## 岔口裁决

1. **待回复清除语义**（2026-08-28，AskUserQuestion）：用户选**「回复即清提及类」**——用户消息入房自动消费该房间未消费 @提及，前端发送后立即刷新收件箱；裁决/工单保留角标直到显式处理。弃选：「角标只看 @提及」（裁决/工单失去列表提醒）、「按消息流派生」（改动面最大、三源全失去提醒）。

## 判断记录

1. **定级 L2**：六项改动不新增、不修改任何 wire 契约面（inbox 聚合语义内变、房间消息 GET 语义内变、carrier/squad PUT 不动、无新端点），接缝全部落在既有符号 + 既有调用方；web 与 Go collab/agentd 两个码区但同一产品域（工作台会话/看板），与 B282 单卡双码区先例同构。L1 判据 1 不中（plan 增量非零，六项各有实现决定）。
2. **issue2 根因判读**：升序截尾是结构性主因（与用户「消息太多」假设吻合）；send 后 usePoll inFlight 复用竞态为次因（至慢一个轮询周期）。两处都修，前者是验收主体。
3. **issue3 保留 B279 语义**：节点标签「多对一列显形」照旧，只是呈现位从标签行迁到右上角 chip；无节点标签时右上角显示状态名。

## 修订记录（2026-08-28，spec 出稿后、批准前）

- 用户告知原问题②「会话消息不显示」此前已修：B274 在 `acc/b156.2-b156.3` @ `cd797e00`
  修复、用户 08-27 确认「可以了」，但一直未进本线；用户 08-28 晚在远程机完成移植合并。
- 本会话 pull 核实：`24be42238 fix(B289): port B274 History newest-window and wake
  pointer flood onto acc`（快进 5d4b4a154 → 24be42238）。读 diff 确认：
  `internal/collab/service.go#History` 改为读全量事件流后从尾部截最新 limit 条
  （`out = out[n-limit:]`），`beforeSeq` 为排他上界；注释自引「B274 真机：发送 200
  列表不动」。同提交还迁了 Wake 带 spec、launchRound 叙事、cursor 推进三件（属
  B289 范围，与 B287 无涉）。`docs/superpowers/specs/b289.md` 明写「前端：本线
  RoomPanel 已轮询无 before 的最新页」。
- **动作**：spec 六项减为五项（问题陈述/方案/用户故事/接缝清单/OOS 全部同步）；
  接缝表删除 History 缝与 usePoll 竞态缝；「会话历史翻页」残余从 B287 OOS 移出，
  roadmap 条目改源为 B289 残余（服务端 before 游标已在，缺 UI）。卡标题「6 项」
  同步改「5 项」。
- **留痕（防重走）**：侦查发现的 send 后 usePoll in-flight 复用竞态
  （`web/src/app/data/usePoll.ts:55-65`，旧响应被 cleanup 守卫丢弃、至慢一个 5s
  轮询周期）随问题②一并移出——B289 修后它至多造成显示延迟，不再构成「不显示」。
  若日后有人报「发送后要等几秒才出现」，从这里接手。

## 跑过的命令（摘录）

- `git fetch origin acc/b156.2-156.3`；`git -C <worktree> merge --ff-only origin/acc/b156.2-156.3`（eea12d202→5d4b4a154，111 文件）。
- `handoff card add / note / move B287`（输出见卡 timeline seq 6881 等）。
- 探索 agent 1 只（Explore，47 次工具调用），产出六项锚点报告如上。
