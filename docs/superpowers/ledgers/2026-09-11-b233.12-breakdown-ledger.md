# B233.12 breakdown 台账

- 2026-09-11 进入拆解节点。工作树 `cards/B233.12-charter`，`git status` 干净，HEAD `118b9921 contract(B233.12): freeze delivery and DeliveredAt ownership`。
- 上游状态核实：spec 头部「已批准」（2026-09-11）；contract 头部「冻结状态：随本提交冻结」。均为文件内状态位，不依赖会话记忆。
- 读现行代码（本工作树）：`internal/approval/client.go`（Request :120-132 AutoAllow 钩子在返回 allow 前调用；consult :373 `MarkTicketDelivered`；Acknowledge :204-214 仅 AckDelivered 写库；NoteDeliveryFailed :231）；`internal/agentd/approval_client.go`（bindApproval :50 绑 `m.autoAllowPermission`）；`internal/agentd/manager.go`（autoAllowPermission :2302 审计+RespondPermission；handlePermission :2110；respondAutoAllowReplay :2685；approvePermission :2596 走 `markDelivered` :2639；waitPermission :2998；RelayAnswer :1972/:2003；handleQuestion :3139；NoteDeliveryFailed :3393；markDelivered :3376）；`internal/executor/opencode/adapter.go`（authorizeNativePermission :817 → RespondPermission :851；reportDeliveryFailure :802；Authorize 失败 reject :836）；`internal/store/store.go`（MarkTicketDelivered :1189 幂等；FindReusableGrant :1161 要求 answer='allow' AND delivered_at IS NOT NULL；UndeliveredAnswers :1211）；`internal/executor/approval.go`（ApprovalClient :87；ApprovalDecision 仅 Status/Reason/Rule，无工单归属信号；Authorize :16 丢弃 ApprovalResult.Ref）。
- 查图：`codegraph domains`（读 baseline）与 `codegraph/best.json` 顶层 parent 为空领域为 d_orchestration / d_gateway / d_workspace / d_execution / d_sessions / d_transport / d_protocol / d_ledger / d_collab / d_cli / d_web / d_policy / d_maintenance / d_scheduling / d_keystone。containers 映射：`k_approval_*`/`k_store_*`/`k_agentd_Manager`→d_orchestration；`k_opencode_Adapter`→d_execution_adapters（parent d_execution）；`k_executor_*`→d_execution_contract（parent d_execution）；`k_permgate_*`→d_policy；`k_proto_*`→d_protocol。
- 生产 callers 核实（grep，排除 _test.go）：`MarkTicketDelivered` 仅 `approval.Client.Acknowledge`（正确）、`approval.Client.consult`（提前，待删）、`Manager.markDelivered`（:1972/:2003/:2639/:2998/:3139，均在成功 RespondPermission 之后）。`autoAllowPermission` 生产 caller 仅 `handlePermission` :2137 与 `bindApproval` :50。
- 基线命令与原始输出：
  - `go build ./internal/approval ./internal/executor ./internal/executor/opencode ./internal/agentd ./internal/store` → exit=0。
  - `go test ./internal/approval -run 'TestClientConsultAllowPersistsGrant|TestClientAutoAllowHasNoTicket' -count=1` → `ok github.com/Xsxdot/handoff/internal/approval 0.145s` exit=0。
- 发现（事实，写入拆解稿）：除 contract 现状表所列 `internal/approval/client_test.go#TestClientConsultAllowPersistsGrant :424` 外，`internal/agentd/approval_client_test.go#TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses :559` 同样断言 `Request` 后 `DeliveredAt != nil` 并立即复用；改语义后该测试必红，须一并改。contract 现状表未列此第二处。
- 发现（事实）：`executor.Authorize` 只回传 `ApprovalDecision`，丢弃 `ApprovalResult.Ref`；adapter 在 `authorizeNativePermission` 内自造 `NamespacedTicketID`，无法从既有返回值判断决定是否有工单。这影响「AutoAllow 无工单失败不产 delivery_failed」的闸位选择，标待拍板。
- 本轮无新提交：本节点只出稿（待拍板），提交见收尾行。
- 出稿：`docs/superpowers/specs/b233.12-breakdown.md`（状态：待拍板；§0 P1–P5 岔口清单）。contract 追加 §8 现状修订（C-1 第二处锁错时序测试、C-4 AckDelivered 无工单 no-op、C-3/#11 待拍板提示），不改冻结字面值。
- 符号锚自检：`codegraph resolve --doc docs/superpowers/specs/b233.12-breakdown.md` exit=0；`codegraph resolve --doc docs/superpowers/specs/b233.12-contract.md` exit=0；两者 `vanished`/`file_missing` 计数=0。图未收录的 approval.Client 四方法、Manager 四个回传/重放函数、adapter.reportDeliveryFailure 用源码行锚，已列图覆盖债。
- 回归确认：`go build ./internal/approval ./internal/executor ./internal/executor/opencode ./internal/agentd ./internal/store` exit=0（本节点只改文档，无代码变更）。

- 提交（历史读数）：`git commit -m "docs(B233.12): 拆解审批入站收口（待拍板）"` → `[cards/B233.12-charter 0aa0b325] docs(B233.12): 拆解审批入站收口（待拍板）`；`3 files changed, 273 insertions(+)`。随后 amend 一次收本行。
