# B370 spec 台账（2026-09-14）

一行一条历史读数；原始输出照抄，不做二次加工。

## 1. 现状读数（来自同日 debug 排查，命令与输出原文）

1. 全库镜像身份键判定（`card_events` 的 `payload` 是 jsonb，用键存在运算符）：

   ```sql
   SELECT COUNT(*) FILTER (WHERE payload ? 'node') AS with_node,
          COUNT(*) FILTER (WHERE NOT (payload ? 'node')) AS without_node
   FROM card_events WHERE type = 'task_mirrored';
   ```

   原始输出：`with_node=0 without_node=4289 with_both=0  最高含node_seq=0  最高无node_seq=13965`

2. 卡 B369 三个 task 的镜像身份：`task=c99a527d… mirrors=32 with_node=0`、`task=82965bef… mirrors=2 with_node=0`、`task=72f377bc… mirrors=4 with_node=0`。

3. 镜像 lease 持有者（写入者）：`holder=sycmdeMacBook-Air.local lease_until=2026-09-14T16:38:40+08:00`；本机 `hostname` → `MacBook-Pro.local`（写入者不是本机）。

4. mac-02 构建身份：`/Users/sycm/.local/bin/handoff version` → `08ca44f4…`（2026-09-11T10:21）；血缘核查：

   ```
   git merge-base --is-ancestor 0e3ca8c3 08ca44f416ff…   → LACKS_B2336
   git merge-base --is-ancestor 08ca44f416ff… main       → NOT_ON_MAIN
   git merge-base --is-ancestor 08ca44f416ff… HEAD       → NOT_ON_BRANCH
   ```

5. 本机在装构建：`c567c3d2fbda+dirty1`（含 B233.6 的 envelope 身份投影与 B349 闸）。

6. 旧 envelope 形状（mac-02 写入者产出，jsonb 输出键序被规范化，故顺序不等于写入顺序）：

   ```
   {"payload": {"kind": "gate", "ticket_id": "…"}, "task_type": "permission_request"}
   ```

   即只有 `payload` / `task_type` 两键，无 `node` / `attempt`。

7. B233.6 的写入格式改动（`git show 0e3ca8c3 -- internal/ledger/mirror.go`）：

   ```
   -		payload := fmt.Sprintf(`{"task_type":%q,"payload":%s}`, ev.Type, inner)
   +		payload := fmt.Sprintf(`{"node":%q,"attempt":%q,"task_type":%q,"payload":%s}`,
   ```

8. 红色回路（cmd 包临时探针，跑完已删）：同一张卡 + 同一 `dispatched` 快照 + 同一 source 三列，

   - 旧格式 envelope → `card wait --follow` stdout 为空（`RED: 身份键缺失的 permission_request 被静默丢弃`，测试 FAIL）；
   - 新格式 envelope（带 node/attempt）→ 正常输出该行（对照 PASS）。

## 2. 止血（mac-02 换版）

9. 构建：`git worktree add /tmp/handoff-b-c567 c567c3d2` + `OUT=/tmp/handoff-mac02 scripts/build-deploy.sh darwin/arm64`。首次因 `web/src/app/task/Composer.test.tsx` 的 TS6133 中断；去掉该测试文件里未使用的 `stopTask` 导入后成功。产物 `version` = `c567c3d2fbda+dirty1`（与在装版同戳）。
10. 脚本自检两门手工复核通过：`grep -aq -- c567c3d2fbda… /tmp/handoff-mac02` → `GREP_OK`；`CGO_ENABLED=0 go test -tags embedweb ./internal/webui/...` → `ok github.com/Xsxdot/handoff/internal/webui 0.459s`。脚本自身 exit 1 的原因见 B371（`set -o pipefail` 下取不到自动戳的 grep 返回 1）。
11. 双端校验一致：本机与 mac-02 `shasum -a 256` 均为 `3e363b173490b0b55309194ac73ad9e487285698cb4bbf0381e814840dca89dd`。
12. 换版与重启：mac-02 备份 `~/.local/bin/handoff.bak-20260914` → 替换 → `launchctl kickstart -k gui/<uid>/dev.gosuper.handoff.agentd`。新进程 pid=96003，`handoff version` → `c567c3d2fbda+dirty1`。
13. 换版后 lease 仍在 mac-02 且正常续期（`until=17:18:44`，其时 mac-02 `date` = `17:18:35`）。
14. **端到端证据（新写入者带身份）**：B369 于 17:24:57 派发 `node=breakdown attempt=3bb5b62d…`，mac-02 新进程 17:25:04–17:25:15 原始日志：

    ```
    起订 sub=linux-01/3bb5b62d-e8eb-474e-99f2-d911541dfb59 node="breakdown" attempt="3bb5b62d-e8eb-474e-99f2-d911541dfb59"
    建立远端账本订阅 card=B369 target=linux-01 task=3bb5b62d-e8eb-474e-99f2-d911541dfb59 node=breakdown attempt=3bb5b62d-e…
    镜像订阅建立 task=3bb5b62d-e8eb-474e-99f2-d911541dfb59 machine=linux-01 from_seq=0
    ```

## 3. 换版只恢复一半：身份来自派发快照投影

15. 挂账身份投影（等价 `internal/ledger#projectTaskLinks` 的 SQL）：

    ```sql
    SELECT COUNT(*), COUNT(*) FILTER (WHERE d.node <> '')
    FROM card_tasks ct LEFT JOIN LATERAL (
      SELECT ce.payload->>'node' AS node FROM card_events ce
      WHERE ce.card_id = ct.card_id AND ce.type='dispatched'
        AND ce.payload->>'target' = ct.target AND ce.payload->>'task_id' = ct.task_id
      ORDER BY ce.seq DESC LIMIT 1) d ON true;
    ```

    原始输出：`挂账总数=836  能投影出身份=13  投影为空=823`。

16. `dispatched` 事件按日（带 node / 空 node）：`2026-09-14 5/3`、`09-13 0/1`、`09-12 6/20`、`09-11 3/29`、`09-10 0/126`、`09-09 0/30`、`09-08 0/14`、`09-07 0/53`。带 node 的 13 条全部来自 charter 卡驱动的 `--step` 派发（B233.16 自 09-11 22:06 起、B358、B368、B369）。
17. 今天空 node 派发形状：`B233.26 purpose=review|charter executor=opencode`（11:15 / 10:46 / 10:37）——不是纯历史遗留。

## 4. 现状代码与冻结面

18. 闸的两处实现（`codegraph --repo . sym <符号>` 皆 `anchor=ok`）：

    - `n_cmd_cardWaitEventActionable` ∈ `d_cli`（`cmd/card_wait.go`，含 `cardWaitCurrentWorkflowAttempt`）
    - `n_agentd_Server_acceptsCurrentWorkflowAttempt` ∈ `d_gateway`（`internal/agentd/wakeconsumer.go`，含 `Server.currentWorkflowAttempt`）
    - 全仓 grep：除这两处外无第三个身份闸消费点。

19. B349 冻结正文（`docs/superpowers/specs/b349.md`）：

    - 第 79 行：「plain / 旧镜像缺 node/attempt、以及没有任何当前 `EvDispatched` 的 `task_mirrored`：不得猜成当前尝试（B233.6 已冻）。本卡把 card wait 对此类事件从『今日按类型表 Encode』**收紧为不 Encode**。」
    - 第 62 行原则：「身份闸和类型表一样，写在**消费点**，不扩 `mirrorSkip`、不把判定下沉成账本派生查询（P3）。」
    - 第 77 行：「缺 `source_task` 的 `task_mirrored` **闭集拒绝叫醒**（记 seen / 当审计，打日志，不把消费循环打死）。」

20. 依赖边事实：`cmd` 已依赖 `internal/agentd`；`internal/client` 不依赖 `internal/ledger`（仅注释提及）；`internal/agentd` 依赖 `internal/ledger` 与 `internal/proto`。

21. 编码旧规则的既有测试：`cmd/card_wait_test.go#TestB349CardWaitSourceIdentity`、`#TestB349CardWaitSubtreeUsesEventCardIdentity`、`internal/agentd/wakeconsumer_test.go#TestB349AutomationSourceIdentity`、`#TestB2336StaleAttemptDoesNotWake`（后者是防迟到回归，应保持绿）。

## 5. 用户裁决（2026-09-14）

22. 承重岔口一（降级语义）：用户选 **「能判定才拦」**——能证明是旧 attempt 的仍丢，无从判定的放行。
23. 承重岔口二（收口位置）：用户选 **「收成一处共享实现」**——新降级判定与现有身份判定合并为一个共享符号，两份消费点调用，现有两份近重复的快照扫描退役；包名与签名归 contract。
24. 分流（同日更早）：**先止血（mac-02 换版，已完成）+ 立卡加固（本卡）**。
25. spec 定稿并经用户批准（2026-09-14，用户原话「批准，进 contract」）；定级判决 **L3 / 轻档** 写进 spec 头部；Out of Scope 的两条推迟项已落 `docs/roadmap.md`（「来自 B370 spec」节）。
