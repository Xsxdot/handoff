# B250 执行台账

- 2026-08-25：`pwd && git branch --show-current && git status --short`；原始输出为工作树 `/root/.handoff/worktrees/2eb5ab04`、分支 `cards/B250-charter`，工作区无未提交改动。
- 2026-08-25：读取 `docs/superpowers/specs/2026-08-25-b250-attachment-kind-identity.md` 与 `internal/ledger/cards.go`、`internal/ledger/move.go`、`cmd/card.go` 及相关测试；原始核对确认 AttachFile 当前只比较 `Path`，DetachFile 当前只按裸 path 且无条数返回，CLI 没有附件动作提示；本节点需覆盖双 kind、精确/裸 path detach、stderr 提示与 implement gate。
- 2026-08-25：先运行 `go test -count=1 ./internal/ledger ./cmd`；原始失败包含 `go: writing go.mod cache: open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.7.0.mod678278816.tmp: read-only file system`、`open /root/go/pkg/mod/cache/download/github.com/!xsxdot/charter/graph/@v/v0.7.0.lock: read-only file system`，并以 `FAIL` 结束。
- 2026-08-25：再运行 `go test -count=1 -run 'TestUpdateCardAttachAccept|TestAttachmentKindsOnSamePathUnlockImplementGate' ./internal/ledger`；原始输出为 `--- FAIL: TestUpdateCardAttachAccept`（`cards_test.go:303: 附件: [{Kind:spec Path:specs/x.md}]`）与 `--- FAIL: TestAttachmentKindsOnSamePathUnlockImplementGate`（`move_attachment_any_gate_test.go:94: 同路径双 kind 应各自保留: [{Kind:spec Path:docs/b250.md}]`），末尾为 `FAIL github.com/Xsxdot/handoff/internal/ledger`。失败原因是功能缺失而非测试编译错误，TDD 红灯成立。
- 2026-08-25：实现最小修复并运行 `gofmt -w internal/ledger/cards.go internal/ledger/cards_test.go internal/ledger/move_attachment_any_gate_test.go cmd/card.go cmd/card_test.go && go test -count=1 -run 'TestUpdateCardAttachAccept|TestAttachmentKindsOnSamePathUnlockImplementGate' ./internal/ledger`；原始输出为 `ok github.com/Xsxdot/handoff/internal/ledger 0.244s`。
- 2026-08-25：运行 `GOMODCACHE=/root/.handoff/tmp/2eb5ab04/gomodcache GOCACHE=/root/.handoff/tmp/2eb5ab04/gocache go test -count=1 -run '^TestCardUpdateAttachmentKindsAndDetachMessages$' ./cmd`；原始输出先完成编译并进入测试，随后 `--- FAIL: TestCardUpdateAttachmentKindsAndDetachMessages`，`card_test.go:123: 裸 path 应摘掉同 path 全部附件: [{Kind:plan Path:docs/superpowers/specs/b250.md}]`。同一输出已显示账本 `摘附件完成 ... exact=false count=1 attachments="[{Kind:plan Path:...}]"`；核对后确认是测试复用带 `omitempty` 的 Card 变量，空附件 JSON 未覆盖旧字段，非生产逻辑；已清空测试目标后重试。
- 2026-08-25：修正测试 JSON 目标初始化后重跑 `GOMODCACHE=/root/.handoff/tmp/2eb5ab04/gomodcache GOCACHE=/root/.handoff/tmp/2eb5ab04/gocache go test -count=1 -run '^TestCardUpdateAttachmentKindsAndDetachMessages$' ./cmd`；原始输出为 `ok github.com/Xsxdot/handoff/cmd 0.127s`。
- 2026-08-25：task 局部收尾运行 `GOMODCACHE=/root/.handoff/tmp/2eb5ab04/gomodcache GOCACHE=/root/.handoff/tmp/2eb5ab04/gocache go test -count=1 ./internal/ledger ./cmd`；原始输出为 `ok github.com/Xsxdot/handoff/internal/ledger 16.427s` 与 `ok github.com/Xsxdot/handoff/cmd 12.016s`。
- 2026-08-25：全量编译运行 `GOMODCACHE=/root/.handoff/tmp/2eb5ab04/gomodcache GOCACHE=/root/.handoff/tmp/2eb5ab04/gocache go build ./...`；原始输出为空，进程退出成功。
- 2026-08-25：变异前用 `rg -n -F 'if attachment.Kind == kind && attachment.Path == path {' internal/ledger/cards.go` 命中两处（`cards.go:473` attach 判重、`:547` detach 解析），未直接替换；改用包含唯一日志行的更长上下文锚，排除误打第二处。
- 2026-08-25：将唯一 attach 判重条件临时取反为 `attachment.Kind != kind && attachment.Path == path`，先运行 `go test -count=1 -run '^TestUpdateCardAttachAccept$' ./internal/ledger`；原始输出为 `--- FAIL: TestUpdateCardAttachAccept`，`cards_test.go:303: 附件: [{Kind:spec Path:specs/x.md}]`，行为断言确认变异命中。
- 2026-08-25：变异后运行 `GOMODCACHE=/root/.handoff/tmp/2eb5ab04/gomodcache GOCACHE=/root/.handoff/tmp/2eb5ab04/gocache go build ./...`；原始输出为空，进程退出成功，变异编译通过。
- 2026-08-25：在编译通过后运行 `... go test -count=1 ./internal/ledger ./cmd 2>&1 | tee /root/.handoff/tmp/2eb5ab04/b250-mutation.log` 并检查 `rg -c '^--- FAIL'`；原始计数为 `3`，失败项为 `TestUpdateCardAttachAccept`、`TestAttachmentKindsOnSamePathUnlockImplementGate`、`TestCardUpdateAttachmentKindsAndDetachMessages`，末尾分别为 `FAIL github.com/Xsxdot/handoff/internal/ledger` 与 `FAIL github.com/Xsxdot/handoff/cmd`。变异已恢复。
- 2026-08-25：恢复变异后运行 `gofmt -w cmd/card.go cmd/card_test.go internal/ledger/cards.go internal/ledger/cards_test.go internal/ledger/move_attachment_any_gate_test.go && ... go test -count=1 ./internal/ledger ./cmd && ... go build ./...`；原始输出为 `ok github.com/Xsxdot/handoff/internal/ledger 16.795s`、`ok github.com/Xsxdot/handoff/cmd 11.410s`，编译无输出且退出成功。
- 2026-08-25：集成收尾运行 `GOMODCACHE=/root/.handoff/tmp/2eb5ab04/gomodcache GOCACHE=/root/.handoff/tmp/2eb5ab04/gocache go test -count=1 ./...`；原始输出以 `ok github.com/Xsxdot/handoff 0.788s`、`ok github.com/Xsxdot/handoff/cmd 14.296s` 开始，随后 `internal/agentd 128.195s`、`internal/ledger 23.154s`、`internal/ledgerstep 12.628s`、其余列出的 internal 包均为 `ok`，进程退出码 0。
- 2026-08-25：运行 `git diff --check && git status --short --branch && git diff --stat && git diff --numstat`；原始输出为分支 `## cards/B250-charter`、5 个源文件修改、B250 执行台账为未跟踪文件，`git diff --check` 无输出且退出成功。
- 2026-08-25：首次运行 `git add cmd/card.go cmd/card_test.go internal/ledger/cards.go internal/ledger/cards_test.go internal/ledger/move_attachment_any_gate_test.go docs/superpowers/ledgers/2026-08-25-b250-execution-ledger.md`；原始失败为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/2eb5ab04/index.lock': Read-only file system`，尚未暂存。
- 2026-08-25：在提升权限下重跑同一 `git add ...`；原始输出为空，暂存成功。
- 2026-08-25：运行 `git diff --cached --check && git diff --cached --stat && git status --short --branch`；原始输出为 6 个文件、`240 insertions(+), 12 deletions(-)`，分支 `## cards/B250-charter`，6 个目标均已暂存，`git diff --cached --check` 无输出。
- 2026-08-25：运行 `git commit -m "fix(ledger): distinguish attachment kinds"`；原始输出为 `[cards/B250-charter fe471590] fix(ledger): distinguish attachment kinds`、`6 files changed, 241 insertions(+), 12 deletions(-)`，包含 B250 实现、测试与台账。
- 2026-08-25：本修复轮读取 handoff 技能全文（仅遵循执行/审阅约束，未调用 handoff CLI）；`git status --short --branch` 原始输出为 `## cards/B250-charter-2`，工作区干净；`git log -5 --oneline --decorate` 原始输出确认当前 HEAD 为 `619c91d8 fix(ledger): distinguish attachment kinds`，并与 `cards/B250-charter` 同指，基线提交为 `ab8f1135`。
- 2026-08-25：审阅现状确认 `cmd/card.go` 仍先 `GetCard` 再 `AttachFile`/`DetachFile`，并以每次新建 `slog.New(slog.NewTextHandler(cmd.ErrOrStderr(), nil))` 打 INFO；本修复轮范围为将判断与写入合并到 ledger 返回值、改用普通 stderr 输出并补足返回值/单次事务测试。
- 2026-08-25：先仅修改 `internal/ledger/cards_test.go` 与 `cmd/card_test.go`，新增 AttachFile/DetachFile 返回值语义断言，并把 CLI 断言收紧为提示在 stderr 恰好一次且不进入 slog；生产代码尚未改动，等待红灯。
- 2026-08-25：运行 `GOMODCACHE=/root/.handoff/tmp/656aedb4/gomodcache GOCACHE=/root/.handoff/tmp/656aedb4/gocache go test -count=1 -run 'TestAttachmentMutationResults|TestCardUpdateAttachmentKindsAndDetachMessages' ./internal/ledger ./cmd`；原始结果为 ledger 编译红 `assignment mismatch: 2 variables but s.AttachFile returns 1 value`（3 处）及 `2 variables but s.DetachFile returns 1 value`（3 处），确认返回值接缝尚不存在；cmd 既有测试断言失败，原始关键输出为 `用户提示必须是 stderr 普通输出且只出现一次`，stderr 含 `level=INFO msg=附件已存在，跳过：spec:docs/superpowers/specs/b250.md`，捕获的 slog 也含同一消息。命令退出码 1，红因是目标功能缺失而非 typo。
- 2026-08-25：红灯后实现最小返回值接缝：`AttachFile` 返回 `(bool, error)`，`DetachFile` 返回 `([]Attachment, error)`，两者均在同一 `mutate` 事务读判写后于提交成功才返回结果；CLI 删除事务外 `GetCard`/展示判定，改消费返回值并用 `fmt.Fprintf(cmd.ErrOrStderr(), ...)` 输出；移除 CLI 的临时 slog handler 与账本重复用户文案；同步更新所有 Go 调用方并运行 `gofmt`，等待定向绿灯。
- 2026-08-25：运行 `GOMODCACHE=/root/.handoff/tmp/656aedb4/gomodcache GOCACHE=/root/.handoff/tmp/656aedb4/gocache go test -count=1 -run 'TestAttachmentMutationResults|TestCardUpdateAttachmentKindsAndDetachMessages' ./internal/ledger ./cmd`；原始输出为 `ok github.com/Xsxdot/handoff/internal/ledger 0.142s`、`ok github.com/Xsxdot/handoff/cmd 0.158s`，退出码 0。
- 2026-08-25：运行 `GOMODCACHE=/root/.handoff/tmp/656aedb4/gomodcache GOCACHE=/root/.handoff/tmp/656aedb4/gocache go test -count=1 ./internal/ledger ./cmd ./internal/agentd ./internal/ledgerstep`；通过 `write_stdin` 收到完整原始输出：`ok github.com/Xsxdot/handoff/internal/ledger 18.270s`、`ok github.com/Xsxdot/handoff/cmd 11.869s`、`ok github.com/Xsxdot/handoff/internal/agentd 146.774s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 8.773s`，最终退出码 0。
- 2026-08-25：运行 `GOMODCACHE=/root/.handoff/tmp/656aedb4/gomodcache GOCACHE=/root/.handoff/tmp/656aedb4/gocache go build ./...`；原始输出为空，退出码 0。
- 2026-08-25：变异前运行 `rg -n -F 'if attachment.Kind == kind && attachment.Path == path {' internal/ledger/cards.go`；原始输出为 `476: ...`（AttachFile 判重）与 `557: ...`（detachSelector 解析），命中不唯一；后续变异使用包含 `return nil // 幂等` 的完整上下文锚，避免误改第二处。
- 2026-08-25：将 AttachFile 判重条件唯一变异为 `attachment.Kind != kind && attachment.Path == path`；先运行 `GOMODCACHE=/root/.handoff/tmp/656aedb4/gomodcache GOCACHE=/root/.handoff/tmp/656aedb4/gocache go build ./...`，原始输出为空、退出码 0，确认变异可编译；再运行 `... go test -count=1 -run '^TestAttachmentMutationResults$' ./internal/ledger`，原始失败为 `重複 attach 应返回新增 false: added=true err=<nil>`，退出码 1，确认行为变异命中。
- 2026-08-25：编译通过后运行 `... go test -count=1 ./internal/ledger ./cmd > /root/.handoff/tmp/656aedb4/b250-mutation.log 2>&1`，原始退出码 1；随后 `rg -c '^--- FAIL' ...` 原始输出为 `4`，`rg '^--- FAIL|^FAIL' ...` 列出 `TestUpdateCardAttachAccept`、`TestAttachmentMutationResults`、`TestAttachmentKindsOnSamePathUnlockImplementGate`、`TestCardUpdateAttachmentKindsAndDetachMessages`，并列出两个包的 `FAIL`。变异测试确实运行且被拦截。
- 2026-08-25：恢复唯一变异条件为 `attachment.Kind == kind && attachment.Path == path`，并更新 agentd/ledgerstep 过时注释；运行 `gofmt -w ...`（目标文件完整列表见命令）无输出，随后运行 `... go test -count=1 ./internal/ledger ./cmd ./internal/agentd ./internal/ledgerstep`，完整原始输出为 `ok github.com/Xsxdot/handoff/internal/ledger 14.794s`、`ok github.com/Xsxdot/handoff/cmd 10.764s`、`ok github.com/Xsxdot/handoff/internal/agentd 130.320s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 8.107s`，退出码 0。
- 2026-08-25：恢复后再次运行 `GOMODCACHE=/root/.handoff/tmp/656aedb4/gomodcache GOCACHE=/root/.handoff/tmp/656aedb4/gocache go build ./...`；原始输出为空，退出码 0。
- 2026-08-25：集成收尾运行 `GOMODCACHE=/root/.handoff/tmp/656aedb4/gomodcache GOCACHE=/root/.handoff/tmp/656aedb4/gocache go test -count=1 ./...`；完整原始输出为根包 `ok github.com/Xsxdot/handoff 0.607s`、`ok github.com/Xsxdot/handoff/cmd 13.638s`，以及 `internal/agentd 130.206s`、`internal/ledger 17.198s`、`internal/ledgerstep 9.311s`、其余列出的 internal 包均为 `ok`，最终退出码 0。
- 2026-08-25：运行 `git diff --check && git status --short --branch && git diff --stat && git diff --name-only && rg -n 'fmt\\.Printf|console\\.log|slog\\.New\\(slog\\.NewTextHandler\\(cmd.ErrOrStderr|attachmentsForDetach|GetCard\\(id\\)' ...`；原始输出为 `git diff --check` 无输出、当前分支 `## cards/B250-charter-2`、12 个预期文件修改、`113 insertions(+), 85 deletions(-)`；红线 grep 仅命中 agentd 其它 endpoint 与 card update 最终输出的 `GetCard(id)`，未命中禁止打印/旧展示 helper/临时 slog handler。
- 2026-08-25：首次追加上述终审台账行的 `apply_patch` 因上下文中误写包路径而失败，原始工具报错为 `apply_patch verification failed: Failed to find expected lines`；读取台账尾部校正上下文后重试成功，未改动代码。
- 2026-08-25：运行 `GOMODCACHE=/root/.handoff/tmp/656aedb4/gomodcache GOCACHE=/root/.handoff/tmp/656aedb4/gocache go vet ./...`；原始输出为空，退出码 0。
- 2026-08-25：最终暂存前运行 `git diff --check && git status --short --branch && git diff --stat && git diff --numstat`；原始输出为 `git diff --check` 无输出、分支 `## cards/B250-charter-2`、12 个预期文件修改，统计 `116 insertions(+), 85 deletions(-)`。
- 2026-08-25：首次运行 `git add cmd/card.go cmd/card_test.go docs/superpowers/ledgers/2026-08-25-b250-execution-ledger.md internal/agentd/ledgerapi.go internal/ledger/cards.go internal/ledger/cards_test.go internal/ledger/move_attachment_any_gate_test.go internal/ledger/move_test.go internal/ledger/store_pg_test.go internal/ledgerstep/node.go internal/ledgerstep/node_test.go internal/ledgerstep/runner.go`；原始失败为 `fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/656aedb4/index.lock': Read-only file system`。
- 2026-08-25：在提升权限下重跑同一 `git add ...`；原始输出为空，退出码 0，12 个目标文件已暂存。
- 2026-08-25：运行 `git diff --cached --check && git diff --cached --stat && git status --short --branch`；原始输出为暂存 diff check 无输出，12 个文件、`119 insertions(+), 85 deletions(-)`，分支 `## cards/B250-charter-2`，12 个目标均显示为已暂存 `M`。
- 2026-08-25：运行 `git commit -m "fix(ledger): make attachment mutations atomic"`；原始输出为 `[cards/B250-charter-2 2f2c7644] fix(ledger): make attachment mutations atomic`、`12 files changed, 120 insertions(+), 85 deletions(-)`。
