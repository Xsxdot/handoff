# B369.2 实现台账（执行者边干边落）

- **卡**：B369.2（S2 连接核兑换、切机清罐与离线补配）
- **分支**：`cards/B369.2-charter-2`
- **计划**：`docs/superpowers/plans/b369.2-plan.md`

---

## Task 0：基线复跑（不写码）

### 0.1 工具链与基线读数（亲跑）

| 命令 | 退出码 | 原始读数 |
| --- | --- | --- |
| `go version` | 0 | `go version go1.26.1 linux/amd64` |
| `go build ./...` | 0 | 无输出 |
| `go test ./internal/mobilecore/... ./internal/proto/... -count=1` | 0 | `ok .../mobilecore 0.026s`；`ok .../proto 0.031s` |
| `go test ./internal/mobilecore/... -race -count=1` | 0 | `ok .../mobilecore 1.021s` |
| `go vet ./internal/mobilecore/...` | 0 | 无输出 |
| `go test ./internal/relay/... -count=1` | 0 | `ok .../relay 0.016s` |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | 0 | `ok .../cmd 0.068s` |
| `codegraph validate --view cards-B369-charter` | 0 | `"edgeIssues": null`、`"issues": null` |
| `codegraph check --view cards-B369-charter` | 0 | `"fails": []` |
| `codegraph sym 'Core.Pair' --view cards-B369-charter` | 0 | 命中 `n_mobilecore_Core_Pair`（core.go:109） |
| `codegraph sym 'Core.Activate' --view cards-B369-charter` | 1 | `符号 "Core.Activate" 不在图中（图未覆盖或名字有误）`——本卡首建，记图覆盖债 |

### 0.2 已知基线红（协调者裁决，不修）

`go test ./internal/client/... -count=1` 必红，原文：

```text
--- FAIL: TestProductionHTTPClientCallersAreGatewayOnly (0.01s)
    execution_test.go:158: 生产代码不得再取 HTTPClient 拼请求（网关 forward 除外）: internal/mobilecore/core.go, internal/mobilecore/proxy.go
FAIL
FAIL	github.com/Xsxdot/handoff/internal/client	9.188s
FAIL
```

本卡验收判据是「违规集合不新增」，不是「变绿」。

---

## Task 1：程序化 ticket→cookie 兑换（最薄路径）

### 1.1 红（写测试，跑编译失败）

新建 `session_test.go`（夹具 + 4 支测试）后 `go test ./internal/mobilecore/... -count=1`：

```text
internal/mobilecore/session_test.go:46:25: undefined: sessionCookieName
internal/mobilecore/session_test.go:80:15: undefined: sessionCookieName
internal/mobilecore/session_test.go:142:18: core.Activate undefined (type *Core has no field or method Activate)
internal/mobilecore/session_test.go:146:16: undefined: sessionCookieName
internal/mobilecore/session_test.go:155:17: core.ActiveMachine undefined (type *Core has no field or method ActiveMachine)
internal/mobilecore/session_test.go:218:22: core.Activate undefined (type *Core has no field or method Activate)
internal/mobilecore/session_test.go:221:12: core.ActiveMachine undefined (type *Core has no field or method ActiveMachine)
internal/mobilecore/session_test.go:222:66: core.ActiveMachine undefined (type *Core has no field or method ActiveMachine)
internal/mobilecore/session_test.go:224:22: core.Session undefined (type *Core has no field or method Session)
internal/mobilecore/session_test.go:248:18: core.Activate undefined (type *Core has no field or method Activate)
internal/mobilecore/session_test.go:248:18: too many errors
FAIL	github.com/Xsxdot/handoff/internal/mobilecore [build failed]
FAIL
```

编译红证明符号缺席（首红合规）。

### 1.2 绿

新建 `session.go`；改 `core.go`（哨兵 `errCoreClosed`、`Core`/`machine` 结构、`Pair`→`registerLocked`、`Activate`/`Session`/`ActiveMachine`/`cookieValidLocked`/`expiryFrom`）。

```text
go build ./...                 -> EXIT=0
go test ./internal/mobilecore/... -count=1 -> ok .../mobilecore 0.024s
go test ./internal/mobilecore/... -race -count=1 -> ok .../mobilecore 1.110s
go vet ./internal/mobilecore/... -> EXIT=0
gofmt -l internal/mobilecore -> 零输出
grep -n '\.HTTPClient()' internal/mobilecore/session.go -> 零命中（EXIT=1）
```

### 1.3 变异（跟随 302）：先红后绿

把 `exchangeTicket` 的 `return http.ErrUseLastResponse` 改为 `return nil`（命中唯一，count=1）：

```text
--- FAIL: TestExchangeTicketVerticalSlice (0.00s)
    session_test.go:144: Activate: 兑换 ticket 期望 302，得到 200
FAIL
FAIL	github.com/Xsxdot/handoff/internal/mobilecore	0.008s
FAIL
```

还原后：`ok .../mobilecore 0.006s`（EXIT=0）。

---

## Task 2：切机清罐与同机缓存

### 2.1 红（缓存缺失）

追加 `TestSessionJarSwitchesMachine`、`TestActivateSameMachineCached` 后：

```text
--- FAIL: TestActivateSameMachineCached (0.00s)
    session_test.go:395: 同机重复 Activate 必须命中缓存（不重复领票），tickets=2
FAIL
FAIL	github.com/Xsxdot/handoff/internal/mobilecore	0.010s
FAIL
```

`TestSessionJarSwitchesMachine` 在 Task 1 的「每次清/换」实现下**首绿**（计划 §5 步骤 2 已声明豁免），其变异在 2.3 验证可红。

### 2.2 绿（替换 `Activate`：显式清罐 + 同机缓存）

```text
go test ./internal/mobilecore/... -count=1 -> ok .../mobilecore 0.028s
go test ./internal/mobilecore/... -race -count=1 -> ok .../mobilecore 1.112s
```

### 2.3 变异（缓存不看机器）：先红后绿

`if c.active == machine && c.cookieValidLocked()` → `if c.cookieValidLocked()`（命中唯一，count=1），跑 `TestSessionJarSwitchesMachine`：

```text
--- FAIL: TestSessionJarSwitchesMachine (0.01s)
    session_test.go:334: 切机后必须重兑换，cookie 不得沿用旧机: A="sess-handoff-mobile-alpha-1" B="sess-handoff-mobile-alpha-1"
FAIL
FAIL	github.com/Xsxdot/handoff/internal/mobilecore	0.014s
FAIL
```

还原后 `ok`。

---

## Task 3：离线补配与离线登记

### 3.1 红（`Retry` 不存在）

追加 `TestRetryOfflineThenOnline`、`TestTokenEntropyGateRejectsWeakToken`（import 补 `errors`/`net/http/httptest`/`internal/client`）：

```text
internal/mobilecore/session_test.go:448:18: core.Retry undefined (type *Core has no field or method Retry)
internal/mobilecore/session_test.go:459:20: core.Retry undefined (type *Core has no field or method Retry)
FAIL	github.com/Xsxdot/handoff/internal/mobilecore [build failed]
```

### 3.2 绿

`registerLocked` 三条离线分支改为 `replaceMachine` 登记离线记录；新增 `Retry`；`Origin`/`MachineNames` 离线语义。

```text
go test ./internal/mobilecore/... -count=1 -> ok .../mobilecore 0.032s
go test ./internal/mobilecore/... -race -count=1 -> ok .../mobilecore 1.119s
```

### 3.3 变异（去熵闸）：先红后绿

删除 `DefaultDial` 里 `relay.CheckTokenEntropy(m.Token)` 分支（命中唯一），跑 `TestTokenEntropyGateRejectsWeakToken`：

```text
--- FAIL: TestTokenEntropyGateRejectsWeakToken (0.00s)
    session_test.go:470: 短 token 必须被熵闸拒绝
FAIL
FAIL	github.com/Xsxdot/handoff/internal/mobilecore	0.003s
FAIL
```

还原后 `ok`。

---

## Task 4：资源安全、幂等与并发收口

### 4.1 测试

追加 `TestPairIdempotentNoLeak`、`TestCloseIdempotentAndRejectsAfter`、`TestConcurrentPairActivateClose`、两条源码守卫（`TestSessionFileHasNoRawHTTPClient`、`TestSessionCookieNameMatchesAgentdSource`）。首跑即绿（关闭拒绝语义在 T1/T3 已随实现落地）——源码守卫与幂等断言直接命中既有实现，属「写下去就绿」的守卫类，其变异在 4.3 验证承重。

```text
go test ./internal/mobilecore/ -run '<T4 五支>' -count=1 -> ok .../mobilecore 0.006s
```

### 4.2 绿（`Close` 补清罐）

```text
go test ./internal/mobilecore/... -count=1 -> ok .../mobilecore 0.030s
go test ./internal/mobilecore/... -race -count=1 -> ok .../mobilecore 1.125s
go vet ./internal/mobilecore/... -> EXIT=0
gofmt -l internal/mobilecore -> 零输出
```

### 4.3 变异（反代注入 Authorization）：先红后绿

`proxy.go` Director 在 `r.Host = target.Host` 后加 `r.Header.Set("Authorization", "Bearer INJECTED")`（命中唯一）：

```text
--- FAIL: TestExchangeTicketVerticalSlice (0.00s)
    session_test.go:167: 兑换请求不得带 Authorization，上游看到 "Bearer INJECTED"
FAIL
FAIL	github.com/Xsxdot/handoff/internal/mobilecore	0.008s
FAIL
```

还原后 `ok`（`proxy.go` 内容还原，工作树改动集合回到有界文件集）。

---

## Task 5：收口核验与移交

### 5.1 全量判据复跑原文

```text
go build ./...                                      -> EXIT=0
go test ./internal/mobilecore/... -count=1          -> ok .../mobilecore 0.035s
go test ./internal/mobilecore/... -race -count=1    -> ok .../mobilecore 1.156s
go vet ./internal/mobilecore/...                    -> EXIT=0
go test ./internal/relay/... -count=1               -> ok .../relay 0.013s
go test ./cmd/ -run TestRepoContractGate -count=1   -> ok .../cmd 0.124s
grep -rn '\.HTTPClient()' internal/mobilecore       -> 生产码恰两条：core.go:313、proxy.go:38（session.go 零命中）
go test ./internal/client/ -run TestProductionHTTPClientCallersAreGatewayOnly -count=1
    -> FAIL（已知基线红）执行原文：
       execution_test.go:158: 生产代码不得再取 HTTPClient 拼请求（网关 forward 除外）: internal/mobilecore/core.go, internal/mobilecore/proxy.go
       —— 违规文件集合与基线**同集合**，未新增 session.go（裁决合规）
codegraph validate --view cards-B369-charter        -> "edgeIssues": null, "issues": null
codegraph check --view cards-B369-charter           -> "fails": []
```

测试清点：`mobilecore` 共 15 支全 PASS（Ticket 0 两支 `TestPairVerticalSlice`/`TestPairPartialBundle` + 本卡 13 支）。

### 5.2 图覆盖债符号清单（交 integrate/recon 补 `codegraph/diffs`；本卡不手改冻结 diff）

新增符号：`mobilecore.SessionCookie`（model，`k_mobilecore_model`）、`mobilecore.Core.Activate` / `.Session` / `.ActiveMachine` / `.Retry`（func，`k_mobilecore_Core`）、`mobilecore.exchangeTicket` / `.toSessionCookie` / `.sameSiteName`（func，`k_mobilecore_fn`）。
新增边：`Core.Activate`→`exchangeTicket`、`exchangeTicket`→`Client.IssueAuthTicket`、`exchangeTicket`→`toSessionCookie`。
`codegraph sym` 未命中记录：`Core.Activate`、`Retry`、`Session`、`ActiveMachine`、`SessionCookie`、`exchangeTicket`、`sessionCookieName`（本卡首建）。

### 5.3 收尾自审

- 每条错误分支带上下文日志（`exchangeTicket` 各失败分支 `log.Warn` 带 machine/origin/status/cause；`registerLocked` 三条离线分支带 cause）；成功路径有出口日志（兑换成功、会话罐切换、配对在线、补配结束）。✓
- 新文件头注释（职责+边界）、导出函数文档注释齐备。✓
- 触及包测试绿、全量编译过、`-race`/`vet`/`gofmt` 干净。✓
- 与 plan Interfaces 签名一致（`SessionCookie` 字段、`exchangeTicket(ctx,machine,origin,cl,log)`、`Core.Activate/Session/ActiveMachine/Retry`）。✓
- 红线：未改 `internal/client/execution_test.go`、未改 `proxy.go`（仅变异后还原）、新增文件零 `.HTTPClient()`；未碰 `internal/proto`/`agentd`/`relay`/`cmd`/`mobile/bind`/`codegraph`。✓
- 未派发、未调 handoff CLI、未起新 executor。✓

### 5.4 提交事实

`git add internal/mobilecore/core.go internal/mobilecore/session.go internal/mobilecore/session_test.go docs/superpowers/ledgers/2026-09-19-b369.2-impl-ledger.md && git commit -m ...` 原始输出：

```text
[cards/B369.2-charter-2 2133f3b8] implement(B369.2): S2 连接核兑换切机清罐与离线补配
 4 files changed, 1249 insertions(+), 29 deletions(-)
 create mode 100644 docs/superpowers/ledgers/2026-09-19-b369.2-impl-ledger.md
 create mode 100644 internal/mobilecore/session.go
 create mode 100644 internal/mobilecore/session_test.go
```

（本台账随该提交的 amend 收进同批；amend 换 hash 是 git 事实，不 chase。）
