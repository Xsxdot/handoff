# B163 账本镜像活配置执行记账

职责：记录 Task 1–5 的双裁决、修复轮次、验证原文与提交范围。Task 6 真机验收由协调者执行，不在本账范围。

- 基线：当前分支 `claude/b163-ledger-mirror`，起点 `4fb53a27`，开工时工作树干净；`go test ./internal/ledgermirror/` 实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror  1.411s`。
- Task 1 / 完成裁决：spec 符合（账本镜像改由 `Machines` 提供活机器清单与客户端，`Source`/`DefaultSource` 改收客户端并使用 `MarkForwarded`，订阅登记保存客户端实例，配置变化按实例不等退订重订，For 失败与三类退订原因均有结构化日志，四个既有用例断言未改）；代码质量符合（本包不解析 `config.Target` 地址/令牌、不引入 `targetclient`，For 在锁外调用，取消与订阅表修改保持既有并发边界，生产默认与测试夹具职责清晰）。验证：`go test ./internal/ledgermirror/ -count=1` 实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror  1.355s`；`gofmt -l internal/ledgermirror` 实际无输出；`git diff --check` 实际无输出。装饰性变异红色原始尾部：`--- FAIL: TestMirrorFlowsLinkedTaskEvents (5.08s)`、`mirror_test.go:124: 镜像未按期落账（应恰 2 条：progress 过滤、幂等不重）`、`FAIL`；恢复后绿色原始尾部：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 1.355s`。Commit 范围：`HEAD^..HEAD`（Task 1 提交）。
- Task 2 / 修复轮 1：按计划删除 `case !ok` 的首次临时变异直接触发编译错误，原始尾部为 `internal/ledgermirror/mirror.go:255:9: declared and not used: ok`；原因是该分支后的 `clients[link.Target] == nil` 仍会以另一原因收订，测试仅断言取消无法区分判据。加强 `TestMirrorDropsSubWhenMachineRemoved`，捕获订阅日志并断言 `reason=机器或挂账已不在`，使删除判据与客户端取不到判据可观测区分；修复后定向测试实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 0.262s`。Commit 范围：工作树至 Task 2 提交前。
- Task 2 / 完成裁决：spec 符合（新增机器运行期起订、客户端实例变化退订重订且水位续拉、机器删除退订、relay 客户端起订四条判据；`Pool` 满足 `Machines` 的编译期断言；删除判据额外锁定三类退订 reason）；代码质量符合（fake 线程安全且 Names 排序，测试信号使用 channel/context/水位而非时序猜测，relay 测试只验证镜像消费客户端不碰 addr）。验证：`go test ./internal/ledgermirror/ -count=1` 实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 2.096s`；`gofmt -l internal/ledgermirror` 实际无输出；`git diff --check` 实际无输出。变异 1（删客户端实例判等）红色原始尾部：`--- FAIL: TestMirrorResubscribesWhenMachineClientChanges (5.11s)`、`machines_test.go:214: 等不到事件源调用：机器配置变更后应退订重订`、`FAIL`；恢复绿色：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 0.157s`。变异 2（删机器/挂账不存在分支，并以 `link := want[key]` 保持变异可编译）红色原始尾部：`--- FAIL: TestMirrorDropsSubWhenMachineRemoved (1.15s)`、`机器消失的退订原因不正确`、日志含 `reason=本轮取不到该机器的客户端`、`FAIL`；恢复绿色：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 0.126s`。变异 3（固定空启动机器快照）红色原始尾部：`--- FAIL: TestMirrorSubscribesMachineAddedAtRuntime (5.40s)`、`machines_test.go:173: 等不到事件源调用：运行期新增的机器应在一个对账周期内起订`、`FAIL`；恢复绿色：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 2.096s`。Commit 范围：`HEAD^..HEAD`（Task 2 提交）。
- Task 3 / 完成裁决：spec 符合（账本镜像移除 `len(cfg.Targets)>0` 启动闸，恒挂载并注入 `srv.Pool()`，启动日志使用池的活机器数并标注运行期新增无需重启，保留 `defer lm.Stop()` 在 `defer lst.Close()` 之后注册以保证 LIFO 顺序）；代码质量符合（删除磁盘 `config.Load(p)` 重读与启动快照回退，保留其余配置加载用途，注释说明共用池与停机边界）。Task 3 初始命令 `go build ./... && gofmt -l cmd` 的原始失败为 `cmd/agentd.go:279:33: cannot use func() map[string]config.Target ... as ledgermirror.Machines value in argument to ledgermirror.New`、`missing method For`；布线修复后 `go build ./... && test -z "$(gofmt -l cmd internal)"` 实际通过，`git diff --check` 实际通过。Commit 范围：`HEAD^..HEAD`（Task 3 提交）。
- Task 4 / 完成裁决：spec 符合（`tasksAll` 以一次 `s.conf().Targets` 活配置快照过滤镜像任务，并复用同一快照构造 machines 行；已删机器遗留行不删除，仅读时隐藏；新增 Debug 跳过计数日志；真实 HTTP+JSON 测试锁定任务与 machines 两侧）；代码质量符合（过滤位于镜像快照转视图前，fetched 不会被已删机器污染，既有本机任务与裸数组/信封契约未动）。验证：基线 `go test ./internal/agentd/ -run TestTasks -count=1` 实际通过：`ok   github.com/Xsxdot/handoff/internal/agentd 0.832s`；先写测试原始红色为 `--- FAIL: TestTasksScopeAllSkipsUnregisteredMachine (0.75s)`、`tasksfanout_test.go:138: 任务数 = 4，期望 3（本机 2 + devbox 1，已删机器那条不算）`、`FAIL`；实现后 `go test ./internal/agentd/ -run TestTasks -count=1` 实际通过：`ok   github.com/Xsxdot/handoff/internal/agentd 1.932s`；过滤变异红色为 `--- FAIL: TestTasksScopeAllSkipsUnregisteredMachine (1.63s)`、`tasksfanout_test.go:138: 任务数 = 4，期望 3（本机 2 + devbox 1，已删机器那条不算）`、`FAIL`；恢复后绿色为 `ok   github.com/Xsxdot/handoff/internal/agentd 1.003s`。`gofmt -l internal/agentd` 与 `git diff --check` 实际无输出。Commit 范围：`HEAD^..HEAD`（Task 4 提交）。
- Task 5 / 整分支终审与完成裁决：相对起点 `4fb53a27..HEAD` 的完整 diff 仅含计划范围 7 个文件，无额外未授权文件；日志走查确认取客户端失败 Warn、三类退订 reason、起订 Info、cmd 挂载 Info、任务汇总跳过 Debug 均存在且使用 slog；注释/API 走查确认 `Machines`、`subscription`、`Source`、`DefaultSource`、`New`、`subscribe`、`dropSubLocked` 与包边界说明齐全。`go build ./...` 实际通过；`gofmt -l .` 实际无输出并退出 0；`go vet ./...` 实际通过；`git diff --check 4fb53a27..HEAD` 实际通过。`go test ./...` 实际退出 1，已通过的包包括 `cmd`、`internal/agentd`（130.422s）、`internal/ledgermirror`、`internal/targetclient` 等；原始失败为既有 `internal/executor/claudecode` 测试的 Unix socket 路径长度：`裁决 socket 路径过长（113/116 字节，上限 107）`，以及 `TestResumeContinuesFromOffset` 的 `115 字节，上限 107`。使用仓库内浅层 `TMPDIR` 重跑后仍仅同一既有 `internal/executor/claudecode` 路径失败（`112/116/115 字节`），本轮不修改无关代码；无发现项，无修复波。Commit 范围：`HEAD^..HEAD`（Task 5 提交）。
- 审核修复 / 修复轮 1：修复 `internal/ledgermirror/machines_test.go` 中日志采集器的数据竞争，新增互斥保护的 `safeBuf`（`Write` 与 `String` 均持锁），`TestMirrorDropsSubWhenMachineRemoved` 保留原 `reason=机器或挂账已不在` 断言不变；复查本轮新增测试，除该对象外未发现跨 goroutine 共享的非并发安全对象。验证：`go test -race ./internal/ledgermirror/ -count=3` 实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 8.748s`；`go test ./internal/ledgermirror/ -count=1` 实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 2.224s`。Commit 范围：工作树至审核修复提交前。
- 审核修复 / 完成裁决：按要求重新做删除分支变异（`link := want[key]` 并删除 `case !ok`），`TestMirrorDropsSubWhenMachineRemoved` 实际红在 reason 断言，原始尾部为 `--- FAIL: TestMirrorDropsSubWhenMachineRemoved (1.17s)`、`machines_test.go:272: 机器消失的退订原因不正确`、日志含 `reason=本轮取不到该机器的客户端`、`FAIL`；恢复后定向测试实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 0.181s`。最终恢复状态再次验证：`go test -race ./internal/ledgermirror/ -count=3` 实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 8.306s`；`go test ./internal/ledgermirror/ -count=1` 实际通过：`ok   github.com/Xsxdot/handoff/internal/ledgermirror 2.152s`；`git diff --check` 与 `gofmt -l internal/ledgermirror` 实际无输出。无额外修复项。Commit 范围：`HEAD^..HEAD`（审核修复提交）。
