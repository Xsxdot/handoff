# B233.3 plan 台账

节点：charter/plan  
卡：B233.3  
分支：cards/B233.3-charter-3  
法定产出：docs/superpowers/plans/b233.3-plan.md  
规则：每确立一个事实立刻追加一行；提交事实记命令与原始输出，随后只 amend 一次。禁止把 amend 后 HEAD 写回台账再 chase。

---

## 2026-09-07 开工

- 角色：plan 节点。只写计划，不写实现代码、不建脚手架。
- 拍板：breakdown §0 P1–P7 全数 (a)，提交 `1eef56c7`。plan 不得把未选方案写成事实。
- 轻档：单卡单轮 implement，禁止扇出子卡。内部 ticket T1–T6 是执行顺序，不是 `card split`。
- 传输层不更新卡、不选审批者。acceptance 须标明未用真重试器验单任务（P5-(a)）。
- HEAD `1eef56c7ffad028a2212459afb9bfff2810169cf`，工作树干净。
- 有图：`codegraph` 在 PATH（`/Users/sycm/.local/bin/codegraph`）。视图 `cards-B233.3-charter`。
- 有效基线分支：`cards/B233.1-charter-7`（合并目标；不越过它碰别的分支）。

## 查图

- 视图：`cards-B233.3-charter`。`codegraph check --view cards-B233.3-charter`：fail_count=2（`d_execution→d_orchestration` dead-contract / dead-interface ApprovalClient）；warn_count=145。与契约 §10 / breakdown 一致，本卡不修 absorb。
- `sym ExecutionClient` / `Transport` / `WaitDeliveryPolicy`：anchor=ok，域 `d_transport_channel`。
- `sym ErrTunnelDisconnected` / `ErrUnreachable` / `ErrNoEndpoint`：图未覆盖。记图覆盖债。
- `sym isDeliverable`：anchor=moved，源码 `client.go:125`。
- `who-calls n_client_Client_HTTPClient`：edges 空，`unscannedEntries=5`。生产调用方以 grep 为准：`forward.go`、`forward_ws.go`。
- `who-calls n_agentd_Server_attachCardBaseBranches`：只显示 `handleProjectWorktreeCreate`。源码 `forward.go:225` 仍调用，图漏边。记覆盖债。
- `who-calls n_agentd_Server_forwardWorktreeIfRequested`：无入边。源码唯一调用点 `projectadmin.go:916`。记覆盖债。
- `who-calls n_client_isDeliverable`：`waitOnce` / `FollowEvents` / `computeBacklog`；CLI `runFollow` 折叠在 `d_coordination_cli`。
- `flow n_client_Client_do` / `n_agentd_Server_forwardWorktreeIfRequested`：degraded=true，steps=[]。已读源码。禁止用 chain 冒充。
- `flow n_client_Client_Dispatch`：degraded=false，10 步，经 `do`。行号相对视图，以源码 `client.go:737` 为准。
- `flow n_client_Client_WaitEvent`：degraded=false，21 步：readCursor → waitOnce 循环 → writeCursor。
- `context d_transport`：actual 分 `d_transport_channel`（10 容器）与 `d_transport_tunnel`（9）；misplaced=[]；fociTruncated total=56 shown=5。

## 源码事实（本轮读/grep，非会话记忆）

- `forward.go:223-226` 成功路径仍 `attachCardBaseBranches`。`handleProjectWorktreeCreate` 在 `forwardWorktreeIfRequested` 返回 true 时直接 return，自己不挂跨机卡。
- `Client.do`：单次 `c.hc.Do`；非 ctx 失败一律包装 `ErrUnreachable`。`ErrTunnelDisconnected` 仅空壳。
- 探针（写后即删 `plan_probe_t3_test.go`）：`NewRelay` + `Dialer.Close` + `Dispatch` → `errors.Is(ErrUnreachable)=true`，`ErrTunnelDisconnected=false`。原文：`dispatch 请求: 对端 agentd 够不着: POST /api/tasks: Post "http://localhost/api/tasks": relay dialer is closed`。
- 生产 `client.NewRelay`：仅 `cmd/upgrade.go:97` 的 `newAgentdClient` 与工厂 `targetclient.newWithDialer`。CLI 具名走 `newTargetClientNamed` → `targetclient.New`。
- `NewLocal` 不接收 `*Pool`，不入 `entries`。无专门测试。
- Warm 只 `ensureTunnel`（relay Dialer.Ensure），不 HTTP 探 agentd。无「Warm≠可达」反面断言。
- `MarkForwarded` / `NoRedirect` 逐字段拷贝，无 `relayBacked` 字段（本卡 T3 要加并抄过去）。
- `cmd/dispatch.go` / `wait.go` / `reply.go` / `continue.go` / `stop.go` 生产已调 `Dispatch`/`WaitEvent`/`FollowEvents`/`Reply`/`Continue`/`Stop`。
- `go.mod`：`github.com/coder/websocket v1.8.15`、`github.com/hashicorp/yamux v0.1.2`。Go 1.26.1。`isReplayable` 在 `/usr/local/go/src/net/http/request.go:1534-1548`；`shouldRetryRequest` 在 `transport.go:847-849`；yamux `DefaultConfig` `mux.go:62-72`；websocket `cloneWithDefaults` `dial.go:75-77`。

## 基线测试（亲自跑）

- `go build -o /dev/null .` 退出 0，stdout 空。
- `go test ./internal/targetclient ./internal/relay ./internal/client -count=1`：ok targetclient 6.147s / relay 0.563s / client 9.385s。
- `go test ./internal/client -run 'TestTransportAndExecutionClientCompileOnAggregate|TestProductionHTTPClientCallersAreGatewayOnly|TestWSDialOptionsPinsClientTransport|TestDispatchOmitsStandardIdempotencyHeaders|TestWaitDeliveryPolicyMatchesIsDeliverableAlias|TestIsDeliverableAliasesWaitDeliveryPolicy|TestErrTunnelDisconnectedDistinctFromUnreachable|TestFollowAllDeliversAuditEvents|TestWaitEventSkipsProgress|TestUnreachableWhenNoResponse|TestNotUnreachableOnContextCancel' -count=1` ok 0.575s。
- `go test ./internal/targetclient -run 'TestNewDirect|TestNewRelay|TestNewNoEndpoint|TestNewRelayLowEntropyToken|TestPoolRebuildsOnTargetChange|TestPoolReusesClient|TestWarmOnlyTouchesRelayTargets' -count=1` ok 0.410s。
- `go test ./internal/relay -run 'TestConnectFrameGoldenBytes|TestConnectOKFrameGoldenBytes|TestRegisterFrameTypeLiteral|TestDerivePSKGoldenVector|TestRelayYamuxConfigKeepsLibraryKeepalive' -count=1` ok 0.213s。
- `go test ./internal/agentd -run 'TestForwardWorktreeStripsCardIDsAndAttachesLocally|TestForwardWorktreeErrorAndCancel' -count=1` ok 1.841s。今日绿是因为 forward 路径仍挂卡。

## 判断

- 最薄路径：故事 3 / 冻结 53–54 今天从建树缝得不到「传输不写卡」——forward 仍挂卡。DAG 第一条 = T5。
- T3 接线测试写下去会红（探针已证实）。T6 upgrade 源码守卫写下去会红（仍有 `client.NewRelay`）。
- T1 新增 NewLocal/Warm 断言锁的是现状，今日写下去会绿；不是红绿实现周期。
- P1–P7 全部按 (a)。禁止扇出、禁止重试器、禁止改 Web、禁止改握手字节。
- 未调 handoff CLI；未写实现代码（探针文件已删）。
- 法定产出已落盘 `docs/superpowers/plans/b233.3-plan.md`。T5 为最薄路径；P1–P7 全 (a)。
- `codegraph --repo . --view cards-B233.3-charter resolve --doc docs/superpowers/plans/b233.3-plan.md`：anchors=23，ok=13，moved=10，坏锚 0。曾有两处短路径 `client.go#Client.Dispatch` / `forward.go#stripWorktreeCardIDs` 报 `file_missing`，已改成带包前缀。
- 自审：无 TBD 占位符；内部锁三条已声明；acceptance 标明未用真重试器验单任务。

## 提交（历史读数，随后 amend 一次）

```
git commit -m "docs(B233.3): 实现计划（能力 client 复用直连与 relay 传输）"
[cards/B233.3-charter-3 f7526d3c] docs(B233.3): 实现计划（能力 client 复用直连与 relay 传输）
 2 files changed, 1280 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-07-b233.3-plan-ledger.md
 create mode 100644 docs/superpowers/plans/b233.3-plan.md
```
