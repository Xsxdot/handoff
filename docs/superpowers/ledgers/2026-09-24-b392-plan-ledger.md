# B392 plan 台账

## 2026-09-24 开节点与基线

### `git status --short --branch` / `git log --oneline -3`

```text
## cards/B392-charter-4
nothing to commit, working tree clean
a0597ad8 breakdown(B392): 拍板 P1-P5 并回写契约澄清
299eedfa breakdown(B392): mobile bind 生产接线轻档复核提案——0 子卡单实现轮 + P1-P5 待拍板
6efc3954 contract(B392): mobile bind 会话生产接线契约冻结 + Ticket 0 骨架
```

判断：本节点在 `cards/B392-charter-4`；有效合并目标 `cards/B392-spec @400ee3c4`。spec 头部「已批准」、contract「冻结状态」在本工作树实读在位。Ticket 0 生产接线已落 HEAD（`adapter.go` + `bind.go#liveCore` + `session.go#newCoreSessions(liveCore)`），本节点只出计划文档。

### 现状源码读数（图外回落 file:line）

- 生产组装：`mobile/bind/bind.go:40` `liveCore = mobilecore.New(nil, log)`，`:42` `core coreAPI = liveCore`；`mobile/bind/session.go:20` `sessions sessionAPI = newCoreSessions(liveCore)`。`grep notWiredSessions` 仅命中 `session.go:5` 注释，无装配。
- 适配器：`mobile/bind/adapter.go`（`sessionCoreAPI` :18、`coreSessions` :29、`newCoreSessions` :35、`SessionCookie` :39、`SwitchMachine` :58）。
- 测试缝：`mobile/bind/fakes_test.go:103` `swapCore`、`:109` `swapSessions`；`mobile/bind/adapter_test.go` 八支契约测试 + `TestDefaultRuntimeSharesOneCore`。
- 核侧：`internal/mobilecore/core.go:107` New、`:125` Pair、`:219` Activate、`:266` Session、`:279` ActiveMachine、`:333` Origin、`:350` MachineNames、`:361` Close；DTO `internal/mobilecore/session.go:42`，兑换 `:69`，超时 `:35`。
- 夹具参照（不复制、不复用其未导出 helper）：`internal/mobilecore/session_test.go` 的 `fakeAgentd`（`/api/status`、`/api/auth/tickets`、`/console` 302+Set-Cookie）。
- 领票对侧：`internal/client/client.go` `New` :195、`IssueAuthTicket` :1274（`POST /api/auth/tickets`）。
- 导出面冻结：`mobile/bind/export_surface_test.go:21-29` 七函数；生产 import 禁令 :77（只扫非 `_test.go`）。
- gobind 形状：`mobile/bind/gobind_surface_test.go:18` `gobindPinned`，`:37-39` 缺 gobind 时 skip。

### 基线命令与原始读数（本节点亲跑）

```text
go version → go1.26.1 linux/amd64
go build ./... (root)                                   → exit 0
go list ./... | grep -c handoff/mobile                  → 0
go list -deps ./... | grep -c handoff/mobile            → 0
(cd mobile) go build ./...                              → exit 0
(cd mobile) go test ./... -count=1                      → ok mobile 0.378s; ok mobile/bind 0.006s
(cd mobile) go test ./bind/ -race -count=1              → ok mobile/bind 1.017s
(cd mobile) go vet ./...                                → exit 0（无输出）
(cd mobile) gofmt -l .                                  → 无输出
```

### codegraph 读数

```text
codegraph --repo . sym coreSessions / newCoreSessions / Core.Activate / Core.Pair / agentd.sessionCookie
  → 全部「不在图中」，退出 1（mobile 前缀被扫描配方排除；Core.* 为 S2 新符号未进旧 baseline）
codegraph --repo . check     → 退出 0，fails=[]（bestCoverage assigned=338 view=338 edges=1254 misplacedSkipped=0）
codegraph --repo . validate  → 退出 1，2 个既存问题（cards-B272-charter 的 k_dropdir_fn 引用不存在领域；cards-B374-charter 的 k_collab_model 重复），非本卡（本卡无视图）
codegraph --repo . resolve --doc docs/superpowers/specs/b392-contract.md → 退出 0，两锚 ok
```

判断：本卡零根模块新符号 → `target.json`/`best.json` 不改、无视图 diff；`mobile/bind` 全符号图外，记图覆盖债（沿用 roadmap 图债卡，不在本卡重扫）。

### 计划落法

- 产出 `docs/superpowers/plans/b392-plan.md`：单实现轮，5 个 task（真实 Core 竖切夹具/归属 → 失败闭合 + `-race` 并发 → 旧空态测试隔离 → README + 文档回归 → 四变异复验）。全部只加 `_test.go` 与 `README.md`，零生产 `.go` 改动。
- 生产守卫落法照 breakdown P3=A + 协调者口径：默认身份守卫 = 既有 `TestDefaultRuntimeSharesOneCore`（不 swap）；真实行为竖切 = 独立 Core 经 swap 注入导出面，不碰 `liveCore`。
- 零子卡（L3 轻档），无跨卡扇出审计；真机项（gobind/AAR/XCFramework/平台 jar）标「由协调者执行，不派发」。

## 2026-09-24 收口

- 产出：`docs/superpowers/plans/b392-plan.md`、本台账。
- 未改任何生产代码 / 测试代码（本节点只出计划）。

### 提交事实（历史读数，amend 前）

```text
git add docs/superpowers/plans/b392-plan.md docs/superpowers/ledgers/2026-09-24-b392-plan-ledger.md
git commit -m "plan(B392): 单实现轮计划——真实 Core 竖切/守卫四变异与 README 交接（0 子卡）"
→ [cards/B392-charter-4 f0e0934d] 2 files changed, 860 insertions(+)
  create mode 100644 docs/superpowers/ledgers/2026-09-24-b392-plan-ledger.md
  create mode 100644 docs/superpowers/plans/b392-plan.md
```

amend 会换 hash，不把新 hash 回写本台账；收口判据是工作树干净。

### 计划自检读数（本节点亲跑）

```text
代码块语法：plan 内两处完整 Go 文件（realcore_test.go、readme_test.go）经 gofmt -e 校验 → 均 OK；
             片段块（Task 2 续写、旧测试替换、import 增行、变异片段）经拼接/语义核对，无需独立编译。
codegraph --repo . resolve --doc docs/superpowers/plans/b392-plan.md → 退出 0，两锚
  internal/agentd/authroutes.go#sessionCookie、internal/client/client.go#Client.IssueAuthTicket 均 ok。
```

## 2026-09-24 第 2 版（review fail 修订）

### 来料：review 裁决

上一轮 plan 被判 fail：P0 = 默认生产竖切缺失（第 1 版用「既有身份断言 + swap 隔离竖切」顶替 spec §8.3 的「不 swap 直走完整链」，且明确写了 notWiredSessions/第二个 Core 不由默认竖切抓）。P1 = 真实 Core 并发需可控 HTTP gate；变异流程加固；TryLock 失败路径 goroutine 清理；README 旧 cookie 删除条件（name=handoff_session+host+Path=/）并由测试锁定；gobind 验收加 `go version -m` 钉版比对；修正 grep -c 退出码、Task5 git status 预期与红绿措辞。指令：只改 plan/ledger，提交并推送分支（收尾纪律块优先：**只 commit，不 push**）。

### P0 可行性核查（亲读源码，非记忆）

```text
mobile/bind/bind.go:40        liveCore = mobilecore.New(nil, log)
internal/mobilecore/core.go:106-115  New(dial==nil → DefaultDial)
internal/mobilecore/core.go:66-69    DefaultDial 直连形态 → client.New(m.Addr, m.Token)
internal/proto/pairing.go:64-93      PairMachine 直连形态 = Addr 非空 + Token 非空
```
判断：默认 `liveCore` 的 `DefaultDial` 可直连本地 httptest 夹具（`Addr` 指向夹具 URL），故「不 swap、直用 liveCore 走 Pair→SwitchMachine→SessionCookie」**可实现**，不需改写冻结契约为 swap 方案。`liveCore` 是包级单例且 `Close` 不可逆 → 默认竖切放**子进程**（`-test.run` 限定）执行，父测试只起夹具/传地址/校验退出码。

### 计划落法（第 2 版）

- Task 3 新增 `default_slice_test.go`：默认生产竖切（不 swap，子进程隔离，真实 Core + 本地 HTTP 夹具，断言真实 cookie 值与归属）——P0 承重守卫。
- Task 2 并发改为 `TestRealCoreGateSerializesSwitchAndRead`：夹具 `gateConsole` 在 `/console` 兑换处做可控 HTTP gate，确定性制造切机在途，含成功读取 B 且不串机；`-race` 与双锁 TryLock 作补充。
- Task 4 增补 `adapter_test.go` 的 TryLock 失败路径 `defer` 放行（P1c）。
- Task 5 README 删除条件改 `name=handoff_session + host + Path=/`、不区分端口，测试逐字锁定（P1d）。
- Task 6 变异加固：任务私有 `mktemp -d $TMPDIR`、唯一命中检查、`trap` 自动还原、`sha256sum`+`cmp` 核验、每次变异先 build（P1b）。
- §4.4 gobind 加 `go version -m "$(command -v gobind)"` 钉版比对（P1e）。
- 修正 `grep -c` 退出码说明、Task 6.5 git status/还原预期、Task 1/2/3 明确「写即为绿」仅 Task 5 红→绿（P1f）。

### 本节点亲跑读数（与第 1 版一致，复核）

```text
go version                                  → go1.26.1 linux/amd64
go build ./... (root)                       → exit 0
go list ./... | grep -c handoff/mobile      → 打印 0，grep 退出码 1（无匹配即 0，非失败）
go list -deps ./... | grep -c handoff/mobile→ 打印 0，grep 退出码 1
(cd mobile) go build ./...                  → exit 0
(cd mobile) go test ./... -count=1          → ok mobile 0.805s; ok mobile/bind 0.005s
(cd mobile) go test ./bind/ -race -count=1  → ok mobile/bind 1.019s
(cd mobile) go vet ./...                    → exit 0（无输出）
(cd mobile) gofmt -l .                      → 无输出
codegraph --repo . sym coreSessions         → 退出 1「不在图中，近似候选 []」（预期：mobile 图外）
codegraph --repo . check                    → 退出 0，fails=[]（41 条 warns 基线既存）
codegraph --repo . validate                 → 退出 1，2 个既存他分支视图问题（B272/B374），非本卡
codegraph --repo . resolve --doc docs/superpowers/plans/b392-plan.md → 退出 0，两锚均 ok
```

### 计划自检读数（本节点亲跑）

```text
plan 内 ```go 块 8 个；其中 3 个完整文件（Task1 realcore_test.go、Task3
  default_slice_test.go、Task5 readme_test.go）经 gofmt -e → 退出 0；
其余 4 个片段块（Task2 续写、Task4.1 旧/新测试、Task4.2 adapter 测试）经
  「package bind + 片段」包装后 gofmt -e → 退出 0；
Task4.1 的 import 增行片段设计为插入既有 import 块，不独立编译（属预期）。
codegraph --repo . resolve --doc docs/superpowers/plans/b392-plan.md → 退出 0，两锚均 ok。
```

### 产出

- 改写 `docs/superpowers/plans/b392-plan.md`（第 2 版，6 个 task）。
- 本台账追加本节。
- 未改任何生产代码 / 测试代码（本节点只出计划）。

### 提交事实（历史读数）

```text
git add docs/superpowers/plans/b392-plan.md docs/superpowers/ledgers/2026-09-24-b392-plan-ledger.md
git commit -m "plan(B392) v2: 默认生产竖切+HTTP gate 并发+变异加固（review fail 修订）"
```
（commit 原始输出见下一行提交回执；hash 由 git 生成，不在此预写。）

## 2026-09-24 第 3 版（v2 review fail 修订）

### 来料：review 六条必改

1. Task 6 变异脚本改严格 shell 失败语义（`set -euo pipefail`；不用 `|| true` 吞结果；`PIPESTATUS[0]`/显式断言测试非零且输出含预期红证据；build/唯一命中/恢复失败均硬失败；`trap` 聚合两文件恢复状态并按原始 hash 与备份核对）。
2. 默认子进程用 `exec.CommandContext` + 超时，HTTP client timeout；gate 的 entered/read/switch channel 全部 `select`+timer，release 安全关闭与 defer，失败不互等。
3. gate 必须有读请求已进入/启动的同步证据；真实 Core 无法在 `ActiveMachine` 与 `Session` 间确定性注入 TOCTOU → gate 只作正向真实链覆盖，TryLock 是唯一确定性去锁红证据，不得声称 gate 令变异④必红；`-race` 继续作附加闸。
4. gobind 版本检查必须把 `v0.0.0-20260908204917-8b95e45f8d3e` 纳入退出码断言，缺工具/版本不符阻塞。
5. 子进程过滤环境变量、检查 last-cookie HTTP 状态、断言 `Close` 返回 nil。
6. 计划/台账明确默认无 swap 竖切是 §8.3 承重项，独立 swap 真实 Core 仅补充，并记录上游 breakdown P3 旧措辞待协调者对齐。提交并推送分支。

### 权限插曲（事实记录）

- 首次校验时我在 `$TMPDIR`（工作树外）建目录/脚本被协调者拒绝（工单 `per_0d2e1035d001JDQkACPyYugyVb`）。改用**纯管道、零落盘**校验（python 读 plan → 管道给 `gofmt -e` / `bash -n`），不再写工作树外任何文件；工单回答 `0c22b7d4-bf14-4830-b4e2-e9a2b0bfa505` 准予「任务私有临时目录」但本节点最终选择零落盘，符合更严约束。

### 计划落法（第 3 版，逐条对必改）

- **①Task 6 全节重写**：6.0 harness `set -euo pipefail`；`m_unique_hit`/`mut_build`/`m_restore` 失败硬失败；`on_exit` trap **聚合 `session.go`+`adapter.go`** 还原状态（原本成功但还原失败→整体失败），`m_restore` 按**原始 sha256 + `cmp`**双重核验；`run_expect_red` 断言「非零退出 + `FAIL`/`panic` 标记 + 预期红证据 ERE」且原始输出落 `$MUT_ROOT/*.red`；附加闸 `run_capture` 只记录退出码不断言。6.1–6.5 全部改为调用上述函数。
- **②进程/HTTP/等待加固**：Task 3 父测试改 `exec.CommandContext(ctx)` + `childTimeout=60s`；`fetchLastCookie` 用 `http.Client{Timeout}`；Task 2 gate 测试所有等待（entered/readBStarted/readAStarted/switchDone/readB/readA）全 `select`+`time.After(step=5s)`，`release` 由 defer 安全关闭（已关闭 no-op）。
- **③gate 定位**：Task 2 gate 增设 `readBStarted`/`readAStarted` 读请求「已启动」同步证据；函数头与 Task 6.4 明写 gate 只作正向覆盖、TryLock 是唯一确定性去锁红证据、去锁后 gate 很可能照样绿；6.4 的 `-race` 改 `run_capture`。
- **④gobind 版本断言**：§4.4 第 1 项改 `set -euo pipefail`；`command -v gobind` 缺工具阻塞；`go version -m | grep -qE "golang\.org/x/mobile[[:space:]]+v0.0.0-20260908204917-8b95e45f8d3e"` 版本不符阻塞。
- **⑤子进程卫生**：新增 `childEnv`（先剔除父环境 `B392_*` 再注入）；`fetchLastCookie` 断言 `resp.StatusCode == http.StatusOK`；子进程末尾显式 `if err := Close(); err != nil { t.Fatalf }`（失败路径另有幂等 defer 收尾）。
- **⑥承重归属**：§0.1 加「承重归属」blockquote，§4.5「缝 → 测试」标注 Task 3 为承重锁；并记录 **breakdown §0 P3=甲 旧措辞**（「守卫测试自建独立真实 Core 走完整链，不碰 `liveCore`」）与本次 review 口径冲突、**待协调者对齐**（本节点无 breakdown 修改权）。
- 头部工作分支由 `cards/B392-charter-5` 更正为 `cards/B392-charter-6`；新增第 3 版头注；§9 增第 3 版修订记录。

### 本节点亲跑读数（原始）

```text
go version                                            → go1.26.1 linux/amd64
go build ./... (root)                                 → exit 0
(cd mobile) go build ./...                            → exit 0
(cd mobile) go test ./... -count=1                    → ok github.com/Xsxdot/handoff/mobile 0.316s
                                                        ok github.com/Xsxdot/handoff/mobile/bind 0.010s
(cd mobile) go vet ./...                              → exit 0（无输出）
(cd mobile) gofmt -l .                                → 无输出
codegraph --repo . resolve --doc docs/superpowers/plans/b392-plan.md
  → 退出 0；internal/agentd/authroutes.go#sessionCookie（:343, n_agentd_sessionCookie）ok；
    internal/client/client.go#Client.IssueAuthTicket（:1267, n_client_Client_IssueAuthTicket）ok
codegraph --repo . check                              → 退出 0，fails=[]（41 条 warns 基线既存）
codegraph --repo . sym coreSessions                   → 退出 1「不在图中，近似候选 []」（预期：mobile 图外）
```

### 代码块校验（零落盘，纯管道）

```text
python3 <提取 plan 的 ```go / ```bash 块> | gofmt -e / bash -n
→ go 块 8 个：7 个独立经 gofmt -e 退出 0；1 个（Task 4.1 import 增行片段）是设计上插入既有 import 块的片段，不独立编译（属预期，历史台账同记）。
→ bash 块 7 个：逐个及合并 `bash -n` 均退出 0（含 Task 6 harness/变异、§4.4 gobind 断言）。
```

### 承重归属与待对齐（第 3 版新增，review 必改⑥）

- **默认无 swap 生产竖切（Task 3，`default_slice_test.go` 子进程）是 spec §8.3 的唯一承重项**。
- **独立 swap 真实 Core 竖切（Task 1/2，`realcore_test.go`）与既有身份守卫（`TestDefaultRuntimeSharesOneCore`）仅补充**，不顶替承重项。
- **上游 breakdown §0 P3=甲 旧措辞**「守卫测试自建独立真实 Core 走完整链，不碰 `liveCore`」与 review 口径冲突 → 记为**待协调者对齐**；本节点不改 `b392-breakdown.md`。

### push 纪律（冲突处置）

- 本次补充/工单要求「提交并推送分支」；但注入的执行纪律收尾块明令「必须 git add 并 commit（**不要 push**）」，且总则「纪律块与本 prompt 冲突时以纪律块为准」。
- 处置：**只 commit，不 push**。是否 push 由协调者决定。

### 提交事实（历史读数，amend 前）

```text
git add docs/superpowers/plans/b392-plan.md docs/superpowers/ledgers/2026-09-24-b392-plan-ledger.md
git commit -m "plan(B392) v3: 严格变异 shell + 子进程/gate 加固 + gobind 版本断言 + §8.3 承重归属（v2 review fail 修订）"
→ [cards/B392-charter-6 b7357153] plan(B392) v3: 严格变异 shell + 子进程/gate 加固 + gobind 版本断言 + §8.3 承重归属（v2 review fail 修订）
  2 files changed, 401 insertions(+), 107 deletions(-)
```
（以上为提交当时原始输出；随后 amend 一次把本行收进同批提交，amend 会换 hash——这是 git 的事实，不把 amend 后 HEAD 写回本台账；收口判据是工作树干净。）

## 2026-09-24 第 4 版（v3 review fail 修订）

### 来料：v3 review 七条必改

1. Darwin Bash 3.2 兼容 harness（不得 `declare -A`；临时文件任务私有且清理）。
2. 每条变异逐测试名断言 FAIL 与红证据，禁止 `|| true` 吞结果。
3. TryLock 测试所有 channel `select`+timer 并回收。
4. gobind 用严格字符串钉版（不用带 `.` 的正则）。
5. hash 状态初始化/恢复边界。
6. `readBStarted` 注释准确。
7. 默认无 swap 子进程真实竖切为 §8.3 必做，独立 swap Core/gate/`-race` 仅补充，TryLock 是去互斥唯一确定性红证据。执行器按纪律只 commit 不 push。

### 上游 breakdown 对齐核实（亲跑）

```text
git show --stat 2749dd2a
→ docs/superpowers/ledgers/2026-09-24-b392-breakdown-ledger.md | 3 ++-
  docs/superpowers/specs/b392-breakdown.md                     | 8 ++++----
  docs/superpowers/specs/b392-contract.md                      | 1 +
git show 2749dd2a -- docs/superpowers/specs/b392-breakdown.md
→ 三处改述：§0 P3 行「必须新增一条不调用 swapCore/swapSessions 的默认生产真实 Core 竖切（隔离子进程，直接走 liveCore）；独立真实 Core + swap 仅作补充行为覆盖，不替代默认守卫」；
  §0「协调者裁决口径」段「默认生产无 swap 真实 Core 竖切是 spec §8.3 的必做承重项」；
  §120「测试隔离」+ §235 P3 汇总行同口径。
```

判断：第 3 版所记「上游 breakdown P3 旧措辞与 review 口径冲突、待协调者对齐」现已闭合——breakdown 与本 plan（默认无 swap 承重、独立 swap 仅补充）逐条一致。本节点不改 `b392-breakdown.md`。

### 计划落法（第 4 版，逐条对七必改）

- **①Bash 3.2 兼容**：`mobile/bind` 无改动；§6.0 harness 删两处 `declare -A`，改每文件 `$MUT_ROOT/<tag>.orig.sha`+`.state` 元数据文件；新增 `sha256_of`（`sha256sum` → `shasum -a 256` → `openssl dgst -sha256`）；`MUT_FILES` 改空格分隔字符串；新增 `m_cleanup`，`trap on_exit` 成功路径 `rm -rf $MUT_ROOT`、失败 `MUT-ROOT-KEPT`；运行脚本本体在 `bash` 后 `rm -f`，不往仓内写临时文件。
- **②逐名 FAIL**：`run_expect_red` 第 4 参数为空格分隔测试名列表，逐名 `grep -Fq "--- FAIL: <name>"`，缺一 `MUT-NO-PERTEST-FAIL` 硬失败；新增「变异↔测试名」映射表；4 条变异调用全部补名；无 `|| true`。
- **③TryLock select+timer**：Task 4.2 重写——`sessionEntered`/`readDone`/`switchDone` 全 `select`+`time.After(step=5s)`，缓冲 channel，`defer` 关 `sessionGate`；import 加 `"time"`；步骤重编号。
- **④gobind 严格钉版**：§4.4 第 1 项改 `awk '$1=="mod" && $2=="golang.org/x/mobile"{print $3}'` + `[ "$got" != "$gobind_pinned" ]`。
- **⑤hash 边界**：`m_backup` 落 `.orig.sha`+`.state`；`m_restore` 分「无备份=untouched / 有备份缺 hash=FAILED」；`m_state` 无记录按 untouched，`set -u` 安全。
- **⑥注释**：Task 2 gate 的 `readBStarted` 注释改述「goroutine 已启动、即将调用」，明写不证明持锁/进入 Core。
- **⑦承重归属**：§0.1 强化为「Task 3 必做承重项；独立 swap/gate/`-race` 仅补充；TryLock 唯一确定性去互斥红证据」；头注、§9 同步。

### 本节点亲跑读数（原始）

```text
go version                                            → go1.26.1 linux/amd64
go build ./... (root)                                 → exit 0
(cd mobile) go build ./...                            → exit 0
(cd mobile) go test ./... -count=1                    → ok github.com/Xsxdot/handoff/mobile 0.320s
                                                        ok github.com/Xsxdot/handoff/mobile/bind 0.004s
(cd mobile) go test ./bind/ -race -count=1            → ok github.com/Xsxdot/handoff/mobile/bind 1.025s
(cd mobile) go vet ./...                              → exit 0（无输出）
(cd mobile) gofmt -l .                                → 无输出
codegraph --repo . check                              → 退出 0（fails=[]，41 条 warns 基线既存）
codegraph --repo . resolve --doc docs/superpowers/plans/b392-plan.md
  → 退出 0；internal/agentd/authroutes.go#sessionCookie（:343, n_agentd_sessionCookie）ok；
    internal/client/client.go#Client.IssueAuthTicket（:1267, n_client_Client_IssueAuthTicket）ok
codegraph --repo . sym coreSessions                   → 退出 1「不在图中，近似候选 []」（预期：mobile 图外）
```

### 代码块校验（纯管道，零落盘）

```text
python3 <读 plan，re.findall 取 ```go / ```bash 块> → 管道给 gofmt -e / bash -n：
→ ```go 块 9 个：3 个完整文件（realcore_test.go/default_slice_test.go/readme_test.go）gofmt -e rc=0；
  另 5 个片段块（Task2 续写、Task4.1 旧测试与新测试、Task4.2 import 块与测试）以 `package bind` 包装后 rc=0；
  1 个为 Task4.1 的 import 增行片段（设计为插入既有 import 块，不独立编译，属预期）。
→ ```bash 块 7 个：逐个 `bash -n`（stdin）均 rc=0（含 Task 6 harness/四变异、§4.4 gobind 断言）。
```

说明：首次尝试在 `$TMPDIR` 落盘校验被协调者拒绝（工单 `per_0d2f8feb5001cMQX1SpJ4EeIRQ`），随后又尝试仓内 `mktemp -d` + `trap rm -rf` 复合命令被拒（工单 `31990ed1-b16d-474b-9b15-8341b3e0db25`）。最终改用**纯管道零落盘**（python 读 plan → subprocess 管道给 `gofmt -e`/`bash -n`），不写任何临时文件，符合更严约束。

### 产出

- 改写 `docs/superpowers/plans/b392-plan.md`（第 4 版）。
- 本台账追加本节。
- 未改任何生产代码 / 测试代码（本节点只出计划）。
- 第 4 版另含一次小补丁提交（未改判据）：补记脚本清理要求；v4.1 review 已改为任务工作树内唯一临时目录与失败证据保留，仓外 `$TMPDIR` 路径不再作为执行方案。补丁提交 hash 不 chase 回写。

## 2026-09-24 v4.1 review 修订（协调者）

- 修正 Task 6 证据保留：仅原始 `rc=0` 且还原成功才清理 `MUT_ROOT`；原始红输出/现场在任何失败路径保留。
- 修正临时路径协议：`MUT_ROOT` 改为任务工作树内唯一 `.b392-mut.*`，禁止固定 `$TMPDIR` 脚本路径；权限拒绝则停止并记未验。
- `m_restore` 在 hash 元数据缺失时先复制备份再报错，避免异常路径留下变异。
- 修正 TryLock 失败路径：增加有界 reader/switcher join 尝试，无法退出时明确报错。
- 本次只改 plan/ledger；尚未执行四变异、真实默认子进程测试或真机验收。提交 hash 在本批收口后由 git 事实决定，不回写台账 chase。

### 提交事实（历史读数，amend 前）

```text
git add docs/superpowers/plans/b392-plan.md docs/superpowers/ledgers/2026-09-24-b392-plan-ledger.md
git commit -m "plan(B392) v4: Bash 3.2 兼容 harness + 逐名变异断言 + TryLock timer + gobind 严格钉版（v3 review fail 修订）"
→ [cards/B392-charter-7 0629711d] plan(B392) v4: Bash 3.2 兼容 harness + 逐名变异断言 + TryLock timer + gobind 严格钉版（v3 review fail 修订）
  2 files changed, 268 insertions(+), 63 deletions(-)
```

（以上为提交当时原始输出；随后 amend 一次把本行收进同批提交，amend 会换 hash——这是 git 的事实，不把 amend 后 HEAD 写回本台账；收口判据是工作树干净。）

