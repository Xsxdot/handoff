# B233.2 spec 台账

日期：2026-09-06。节点：charter spec。

## 过程

1. 初版误把 233.1「先一家再第二家验接口」套到本卡。用户纠正：B233.2 应迁所有 agent。
2. 用户「按这个推进吧」，并指定真机：codex/agy=linux-01（codex 限额约 3h 后恢复）；grok/opencode=本机；OpenCode 免费模型。Claude 未点名，本机 agentd 已登记 claude 且存在 `~/.claude`。
3. `handoff card bind B233.2`；`move spec`。
4. 读 OneShotArgs、hostapi supportedCLIs、skill agentDirs、coordinator_home 包注释、Adapter、keysclient.Runner。
5. 写 `docs/superpowers/specs/b233.2.md`，头部按「按这个推进吧」回写已批准。

## 命令读数

- `handoff status`：本机执行者 agy/claude/codex/fake/grok/opencode；233.1 合同任务 grok running。
- `handoff squad list`：grok@mac-02、codex@linux-01、agy@linux-01。
- 未跑 Go 测试、未派 233.2 实现（被 233.1 阻塞）。
- `git commit`：`[main f01606fe] docs(B233.2): 批准 harness 能力归拢 spec（迁全部现有 agent）`。
