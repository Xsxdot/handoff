# B233.16 plan 台账（2026-09-12）

节点：plan（handoff 任务执行者）。产物：`docs/superpowers/plans/b233.16-plan.md`。
本文件只记实验过程与原始读数；结论进 plan 正文。

## 0. 开工读数

```
$ pwd
/root/.handoff/worktrees/4e1ae2da
$ git branch --show-current
cards/B233.16-charter-5
$ git status
On branch cards/B233.16-charter-5
nothing to commit, working tree clean
$ git log --oneline -5
83311cf1 breakdown(B233.16): 复核轮闭环——P1 独立复核、节点计数 16→17、拍板回写确认
f335c2b5 contract(B233.16): freeze wait-once named entry (P1=A) + boundary clarifications
9e03cb35 docs(B233.16): 拆解稿拍板归档——P1=A 退回 contract 加冻，P2–P4=A
cd854f0e breakdown(B233.16): 执行消费面收窄拆解提案——单一实现范围 + P1–P4 待拍板
e4ede597 docs(B233.16): spec 勘误——契约图两处判断被 contract 实测推翻
```

## 1. 上游输入核对

- `docs/superpowers/specs/b233.16.md`：状态「已批准（2026-09-11）」；§测试决定 T1–T5。
- `docs/superpowers/specs/b233.16-contract.md`：冻结清单 #1–#57；头部「已冻结（2026-09-11；冻结提交 03b5f089，spec 勘误 e4ede597，本轮加冻见本提交）」；§7 移交 T2/T3/T4/T5。
- `docs/superpowers/specs/b233.16-breakdown.md`：§0 拍板 P1=A/P2=A/P3=A/P4=A；§4 单一实现范围 + 有界文件集；§6 真机/存量红。
- 结论：上游齐备，无缺失；无提问工单。

## 2. 图查询（有图先查图，使用 PATH 上的 codegraph）

协调者指示：使用 `codegraph`（/usr/local/bin），不得用 `go run github.com/Xsxdot/charter/...`（版本 skew 风险）。

```
$ codegraph --repo . context d_cli
（INFO 日志，foci=5，含 cmd/root.go:237 工厂窗口等）

$ codegraph --repo . sym ExecutionClient
Error: 符号 "ExecutionClient" 不在图中（图未覆盖或名字有误）……
$ codegraph --repo . sym runStop
Error: 符号 "runStop" 不在图中……
$ codegraph --repo . sym runReply
Error: 符号 "runReply" 不在图中……
$ codegraph --repo . sym runWaitOnce
Error: 符号 "runWaitOnce" 不在图中……
$ codegraph --repo . sym dispatchStep
Error: 符号 "dispatchStep" 不在图中……
$ codegraph --repo . sym probeStatus
Error: 符号 "probeStatus" 不在图中……
$ codegraph --repo . sym StepClient
Error: 符号 "StepClient" 不在图中……
$ codegraph --repo . sym runUntilDone
（命中，返回 baseline 节点）
```

图覆盖债：`ExecutionClient`/`runStop`/`runReply`/`runWaitOnce`/`dispatchStep`/`probeStatus`/`StepClient` 未入 baseline 图（Ticket 0 新符号，best 图未吸收）；本计划用 `文件#符号`+行锚。

## 3. 基线判据实测

```
$ go build ./...
（无输出）
$ echo $?
0

$ go vet ./cmd/ ./internal/agentd/ ./internal/ledgerstep/
（无输出）
$ echo $?
0

$ go test ./cmd -run 'TestExecutionSignatureLocks|TestStopCLIRetainCopy|TestDispatch|TestWait|TestReply|TestContinue|TestCLIExecutionCommandsDoNotHandRollHTTP' -count=1
ok  	github.com/Xsxdot/handoff/cmd	0.624s

$ go test ./internal/ledgerstep -run 'TestRunner|TestB2336|TestClientFinalMessage|TestWire|TestB23316' -count=1
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	2.495s

$ go test ./internal/agentd -run 'TestCardStep|TestStartCardStep|TestDisciplineTargetCap|TestB351|TestB23310|TestCardStepProbe' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	9.553s
```

存量红（与本卡无关）：

```
$ go test ./cmd -run TestRepoContractGate -count=1
--- FAIL: TestRepoContractGate (0.03s)
    graph_gate_test.go:38: 契约违规 [over-budget] d_cli->d_workspace 直调 11 条超出预算 9
    graph_gate_test.go:38: 契约违规 [over-budget] d_gateway->d_workspace 直调 17 条超出预算 1
    graph_gate_test.go:38: 契约违规 [over-budget] d_orchestration->d_workspace 直调 49 条超出预算 19
    graph_gate_test.go:38: 契约违规 [over-budget] d_workspace->d_orchestration 直调 3 条超出预算 2
    graph_gate_test.go:38: 契约违规 [over-budget] d_workspace->d_protocol 直调 23 条超出预算 3
FAIL

$ go test ./internal/agentd -run TestLegacy -count=1
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.15s)
    scheddispatch_test.go:531: 第 1 条漂移：
        got=GOLDEN|2|dispatched|web:test|[{"attempt":"T-fake-01"},...]
        want=GOLDEN|2|comment|web:test|[{"body":"挂账 task T-fake-01@ftm（implement）"},{"kind":"普通"}]
FAIL
```

## 4. 图基线

```
$ codegraph --repo . check
fails = 5（全部 [over-budget]：d_cli→d_workspace / d_gateway→d_workspace /
        d_orchestration→d_workspace / d_workspace→d_orchestration / d_workspace→d_protocol）
warns = 139
Error: 契约对照发现 5 处违规

$ codegraph --repo . check --view cards-B233.16-charter
fails = 5（与不带视图逐条一致）；"kind" 行共 144 = 5 fails + 139 warns
Error: 契约对照发现 5 处违规

$ codegraph --repo . validate
（无 over-budget/非法输出；尾部为 flag 帮助）
```

`codegraph/target.json` 复核（Ticket 0 已冻结，本节点只读）：
- `d_cli→d_transport` entries = `["client 实体","client.Client"]`，含 B233.16 注记。
- `d_gateway→d_transport` entries = `["targetclient.Pool","client.Client","client 实体"]`，含 B233.16 注记。
- `d_orchestration→d_transport` 预算不动、含 B233.16 注记。
- 三边均不新开方向、不改预算。

## 5. T3 扫描算法独立验证（不改工作树）

把 `stripCommentsAndStrings` + `executionSurfaceViolations` 的逐字逻辑抽到仓外临时程序
（`/root/.handoff/tmp/4e1ae2da/opencode/scan_check/main.go`）跑，喂真实执行面文件与 synthetic 输入：

```
REAL dispatch.go -> []
REAL wait.go -> []
REAL reply.go -> []
REAL continue.go -> []
REAL stop.go -> []
REAL card_dispatch.go -> []
nonFactory -> [2:sneaky]
factory -> []
decoys -> []
rawstring -> []
```

结论：真实执行面文件零误报；非白名单聚合用法判红；工厂符号放行；注释/字符串（含多行原始
字符串）不误报且行结构不被破坏。此读数进 plan §6.1。

## 6. 设计判断（落 plan）

- `P2=A`：替身直接注入已冻具名入口；T2 测试放三份 `execution_signature_lock_test.go`
  （均在 breakdown §4④ 有界文件集内，避免新增文件）。
- `P3=A`/`P4=A`：T3 仅 `cmd`、只扫六个执行面文件、符号级白名单、剔除注释/字符串、
  synthetic 可变红守卫。
- T2 的牙 = 替身调用计数；另加 `client.Transport` 不满足断言作为接口隔离的可观察判据。
- 本卡无生产代码改动，故 T2 测试「写下去即绿」（最薄路径条豁免）；T3 算法已独立验证有牙。
- T4/T5 只跑命令、记原文，不改文件。

## 7. 收尾

- 产出物：`docs/superpowers/plans/b233.16-plan.md`。
- 本台账与产出物同批提交；提交命令与原始输出见下方「提交事实」段（提交后回填）。

## 8. 提交事实（历史读数；作证人 amend 前）

```
$ git add docs/superpowers/plans/b233.16-plan.md docs/superpowers/ledgers/2026-09-12-b233.16-plan-ledger.md
$ git commit -m "plan(B233.16): 实现计划——T2 替身注入/T3 源码守卫/T4 行为回归/T5 图对账"
[cards/B233.16-charter-5 f464dd5d] plan(B233.16): 实现计划——T2 替身注入/T3 源码守卫/T4 行为回归/T5 图对账
 2 files changed, 1422 insertions(+)
 create mode 100644 docs/superpowers/ledgers/2026-09-12-b233.16-plan-ledger.md
 create mode 100644 docs/superpowers/plans/b233.16-plan.md
$ git status --short
（空）
```

随后按纪律 amend 一次，把本「提交事实」段收进同批提交（amend 换 hash 是 git 的事实，非漏记）。收口判据是工作树干净。
