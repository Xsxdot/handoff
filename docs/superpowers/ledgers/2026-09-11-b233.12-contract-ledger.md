# B233.12 contract 台账

- 2026-09-11 用户批准 spec，卡 `move contract`。头部回写「已批准」。
- 查证：`ApprovalClient` `internal/executor/approval.go:87`；`Hooks` `internal/approval/client.go:44`；`consult` 提前送达 `:373`；`bindApproval` `:50` → `autoAllowPermission` `:2343` `RespondPermission`；`authorizeNativePermission` `:851` 第二次回传；`Acknowledge` `:204-214` 仅 AckDelivered 写库；`NoteDeliveryFailed` `:3393` 无工单也会追加 `delivery_failed`。
- `codegraph who-calls autoAllowPermission` 空，记图覆盖债。
- Ticket 0：无新导出符号，不改 `target.json`，不造视图 diff。轻档无直通竖切。
- 拍板两条：钩子只禁投递；免审失败不产 `delivery_failed`。金样本类可执行冻结：无命中。
- 本轮编译：`go build ./internal/approval ./internal/executor ./internal/executor/opencode ./internal/agentd ./internal/store` exit=0。
- `codegraph resolve --doc docs/superpowers/specs/b233.12-contract.md` exit=0，anchors=14，vanished=0。`Client.Request/consult/Acknowledge` 与 `reportDeliveryFailure` 改源码行锚。

- 提交（历史读数）：`17f7a1e9 contract(B233.12): freeze delivery and DeliveredAt ownership` contract(B233.12): freeze delivery and DeliveredAt ownership；随后 amend 一次收本行。
