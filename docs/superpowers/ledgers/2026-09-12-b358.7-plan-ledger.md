# B358.7 plan 节点台账（2026-09-12）

节点：plan（L3 重档，S7 子系统执行者依契约自写）。产出：`docs/superpowers/plans/b358.7-plan.md`。
角色边界：不写实现代码、不做 git 提交、不碰卡；本文件与计划文档随 implement 批次同提交。

## 一、基线读数（判据先在基线跑，2026-09-12 实测）

分支 `cards/B233.1-charter-7`，HEAD `4f40120f`（implement(B358.5)）。

1. 节内 kind 名基线：`awk 'NR>=578 && NR<=666' skills/handoff/SKILL.md | grep -c "escalation\|deviation\|closing\|relay"` → **0**（exit 1）。旧节内本无这四个词；命中在节外 :291（`deny_guidance_relayed`）、:406-407（占座节 `escalation`/`closing`）、:749（排障表）。断言 1 锁「改写不引入」，非「清除节内旧词」。
2. 三档词基线：`grep -c "@主 agent\|推翻级\|填补级" skills/handoff/SKILL.md` → **3**（:586、:608、:618）。恰好压在 ≥3 达标线上——断言 2 判据力弱（改写不使其变红），真红绿在断言 3/4。已写进计划 §6.4。
3. roadmap 基线：`grep -n "B358" docs/roadmap.md` → **无命中**（exit 1）。红。
4. 工作区脏文件基线（**与本卡无关，一律不碰**）：`git diff --stat` = `web/src/app/task/Composer.test.tsx`（1 file，1+/1-）；未跟踪 `.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、`web/design-qa-sidebar-after.png`、`web/design-qa.md`。

## 二、树上事实查证（新模型文本的依据）

- `cmd/session.go` 命令面：`wait <member> [--since][--timeout]` / `create <title> --owner` / `list [--member][--json]` / `detail <session> [--json]` / `archive` / `join` / `leave` / `send <session> <text...> [--ref][--mention][--cli][--session]`。寻址来源 = `--mention` flag（`StringArrayVar(&sessionSendMention, "mention", …)`），不从正文解析。
- `session send` 无 `--reply-to` flag；`internal/agentd/roomsapi.go` grep `reply|Reply` 零命中——`reply_to` 隐式寻址在模型层保留（wakeconsumer 消费、`session wait` 出引用条），但发言命令无设置入口。纪律文本据此写「要唤醒谁显式 --mention 谁」，不断言 CLI 能设 reply_to。
- `cmd/room.go`：`room send --kind` flag 面仍在（`--kind` 默认 user、非 user 走席位出示），但 S1 后旧房间只读归档——写路径被门面收死。占座节 :394/:406-407 与排障表 :745/:749 的 room 机制话术与现实不符，**在 S7 有界范围外**，立为计划 §7 岔口 1（待拍板：另卡收口 vs 批准越界同批改）。
- `docs/superpowers/disciplines/b156.2-charter-implement-seed.md` 存在——收口摘要执法点的种子引用保留。
- B363 在库唯一出处 = breakdown §0 P6 裁决行（B363 纠正注）；B364 本仓无档，引用出处是协调者派发指令——计划 §7 岔口 2 记缺口。

## 三、出稿决定

- 新节标题定「**协作会话纪律：会话、寻址与三档升级**」——换载体话术后节名随内容更替；AC-1 的 awk 边界判据同步用新节名（breakdown 原文「协作房间纪律节范围」指节实体，不锁节名字面）。
- 保留机制四款（简报六段/收口四段/重建四步/叙事三问）正文骨架逐字保留，只换载体词；新增「会话即工作单元：在场与寻址」「升级三档」两小节承载 spec §2.5 三档判据与寻址规则；协调者侧新规（模型级/跨仓裁决先进会话叙事再动手，B364 前身教训、先例已行）落在三档小节末。
- roadmap 节落文件末尾（既有各节无时序纪律）；「永不做」清单不落（roadmap 头部明文）。
- 五项 OOS 逐条对照 spec §7 全数入节，已在他节登记的三项（多人时代/四期/历史翻页）带交叉引用重录，保「逐条在文」判据自洽。

## 四、产出与移交

- [plan] `docs/superpowers/plans/b358.7-plan.md` 落盘（T1 基线/T2 SKILL.md 逐字替换文本/T3 roadmap 逐字追加文本/T4 验收与提交；AC-1～4；缺陷族；岔口四条）。
- [plan] 本台账落盘。
- 移交 implement：按计划 §2.2 一个 Edit + §3.2 一个追加，验收复跑 §1/§5，提交归 implement 批次。
