# B233.11 spec 台账

- 2026-09-10 用户原话「建卡，修启动对账，别跨机清别人占用。」
- 根因读数：`watchdog.go#reconcileSchedRunning` 以本机 `ListTasks` 为 owner，对共享 `sched_running` 全量清无主键。
- 锁错行为的测试：`TestB23310RecoverClearsOrphanCarrierAndMemberIndependently`；终态键清零在 `TestB23310RecoverKeepsWaitingReviewAndRunningOwners` 后半。
- 定级 L1：只改启动对账写 0 的授权；不改占用契约键与派发入口。
- 崩溃窗口 / owner 字段不做，进 roadmap。
- 2026-09-10 implement 红（断言红，非编译红）：`go test ./internal/agentd/ -run 'TestB2331[01]Recover' -count=1` → 3 FAIL：
  - `TestB23311RecoverKeepsForeignOccupancyWithoutLocalOwner`: before=(v1,count2) after=(v2,count0)
  - `TestB23311RecoverKeepsForeignKeysAlongsideLocalOwner`: carrier/carrier-F 被改写 version=2 count=0
  - `TestB23311RecoverKeepsLiveAndTerminalOwners`: carrier/carrier-C 被改写 version=2 count=0
- 测试改动：`TestB23310RecoverClearsOrphanCarrierAndMemberIndependently` 删除（无本机任务改为保持，非清零）；`TestB23310RecoverKeepsWaitingReviewAndRunningOwners` → `TestB23311RecoverKeepsLiveAndTerminalOwners`，终态键改保持；新增他机键与本机 owner 并存。
- 实现：`reconcileSchedRunning` 去掉清零 Put，改为只读；保留 malformed 解码校验并阻断后续恢复。
- 绿：`go test ./internal/agentd/ -run 'TestB2331[01]Recover' -count=1 -v` → 5 PASS。
- 变异自验：foreign 分支加回 `Put(count=0)`，`go build ./internal/agentd/` BUILD_OK，`go test ... -run TestB23311...` → 3 FAIL（测试拦得住）。已 revert，BUILD_OK。
- 全量编译：`go build ./...` BUILD_ALL_OK；`go vet ./internal/agentd/` VET_OK。
- 触及包全量：`go test ./internal/agentd/ -count=1` → 唯一 FAIL `TestLegacyNodeEventSequenceUnchanged`；`git stash` 后在基线 deb2cc56 上同样 FAIL，属预设失败，与本卡无关。
- 提交（历史读数，随后 amend 收进同批）：`git commit -m "fix: 启动对账不得清跨机共享占用 (B233.11)"` →
  `[cards/B233.11-charter 2683a53a] fix: 启动对账不得清跨机共享占用 (B233.11)` / `3 files changed, 71 insertions(+), 37 deletions(-)`
