# B369.6（S6）implement 节点台账

节点：implement（卡 B369.6「S6 响应式四 tab 与终端输入层」）。分支 `cards/B369.6-charter-2`，父卡 `4ab6415a`（有效基线 `cards/B369-charter-4`）。法定计划：`docs/superpowers/plans/b369.6-plan.md`。本节点按 T0→T1→T2→T3→T4→T5 执行，分次提交。

## 一、基线复核（本轮实测，原始读数）

- 开始：分支 `cards/B369.6-charter-2`，工作区干净，HEAD `0bc90dbc`（plan 提交）。
- `codegraph --repo . check --view cards-B369-charter` → EXIT=0（末段 `"crossDomainEdges": 1249`、`"viewContainers": 343`）。
- web 依赖初始缺失：首次 `npx eslint .` 原始错误含 `Cannot find package '@eslint/js'`（node_modules 空）。运行 `npm ci` → `added 290 packages, and audited 291 packages in 8s`。

## 二、T0（eslint 基线错误转绿）

- 红：`npx eslint .` → `✖ 30 problems (4 errors, 26 warnings)`，EXIT=1。4 errors 为 `pty.ts:115 no-case-declarations`、`terminalHostResponse.ts:45/47/49 no-control-regex`。
- 施加：`terminalHostResponse.ts` 第 2 行插文件级 `/* eslint-disable no-control-regex -- ... */`；`pty.ts` 的 `case 'attached':` 包成块作用域 `{ ... }`。
- 绿：`npx eslint .` → `✖ 26 problems (0 errors, 26 warnings)`，EXIT=0；`npx tsc -b` → EXIT=0。
- 提交 `fix(web): 清除 eslint 基线错误（ESC 正则禁用 + case 块作用域）` → `9d335e40`。

## 三、T1（断点谱系 + Shell 条件渲染 + MobileTabBar）

- 新：`web/src/app/shell/useShellViewport.ts`、`MobileTabBar.tsx`（+ 各自测试）；改：`Shell.tsx`、`Shell.test.tsx`。
- 红：T1 施加前 `npx vitest run src/app/shell -t "B369.6"` 无 `mobile-home`/底栏 testid（本节点以「实现前不存在这些 testid」为据；测试文件与实现同批落地，见下绿读数）。
- 绿：`npx vitest run src/app/shell` → `Test Files 6 passed (6)`、`Tests 81 passed (81)`；`npx tsc -b` → EXIT=0。
- 偏差（记录）：计划 S3 的 `!ledgerEnabled` 兜底 effect 在 `useLedgerEnabled` 的 loading 期（enabled 恒 false）会把移动首屏误切到「项目」，导致会话下钻用例红。本节点加 `!ledgerLoading` 条件（探测结束才判），语义仍为「账本确实未启用才退项目」，与计划意图一致。
- 变异（AC-5）：把 `viewportOf` 两档判据改为 `width <= -1`（恒 desktop）；先 `npx tsc -b` → EXIT=0（变异可编译），再 `npx vitest run src/app/shell` → `Tests 6 failed | 75 passed (81)`，FAIL 列表含四条 B369.6 紧凑用例与两条 `useShellViewport` 分档用例；`cp` 还原后 `git diff --stat` 对 `useShellViewport.ts` 为空。
- 提交 T1（待收尾 amend 记事实）。

## 四、T2（卡 tab 状态词表与筛选）

- 新：`web/src/app/cards/statusVocab.ts`（+ 测试）；改：`CardsPage.tsx`、`CardsPage.test.tsx`。
- 绿：`npx vitest run src/app/cards` → `Test Files 10 passed (10)`、`Tests 103 passed (103)`。
- 偏差（记录）：计划 §6.4 用例用卡号 `B1`/`B2` 断言，实际撞到队列面板的跨用例残留 mock（`Found multiple elements with the text: B1`）。改为断言卡片标题（`已完成卡`/`进行中卡`），锁的是同一行为（chip 过滤生效/复位），无判据降级。
- 变异（AC-4）：把 `statusVocab.ts` 的 `'已完成'` 改为 `'完成'`（唯一命中 count=1）；`npx tsc -b` → EXIT=0（可编译）；`npx vitest run src/app/cards` → `Tests 4 failed | 99 passed (103)`，含 `statusVocab.test.ts` 逐值用例与两条 B369.6 chip 用例；还原后同命令 `10 passed (103)`。
- 提交 `feat(web): 卡 tab 状态词表与筛选（B369.6 T2）` → `3baaf0f5`。

## 五、T3（项目 tab 离线语义，测试锁定既有行为）

- 改：`Shell.test.tsx`（`describe('B369.6 移动断点谱系')` 内追加「不可达位置标已断开且不渲染其目录内容」）。
- 绿：`npx vitest run src/app/shell` → `Test Files 6 passed (6)`、`Tests 82 passed (82)`。产品代码零改动（`ProjectTree` 既有离线语义即满足）。

## 六、T4（移动终端键条 + IME 组合输入）

- 新：`web/src/app/workbench/MobileKeybar.tsx`（+ 测试）；改：`TerminalTab.tsx`（`keybar?: boolean` + 渲染）、`TerminalTab.test.tsx`（keybar 2 支）、`terminalInput.test.ts`（IME 2 支）、`Shell.tsx`（`case 'terminal'` 传 `keybar={compact}`）。
- 绿：`npx vitest run src/app/workbench` → `Test Files 21 passed (21)`、`Tests 310 passed (310)`。
- 变异（AC-6，键条可红）：把 `TerminalTab.tsx` 的唯一锚点 `{keybar && (` 改为 `{keybar && false && (`（count=1）；`npx tsc -b` → EXIT=0；`npx vitest run src/app/workbench -t "keybar"` → `Tests 1 failed | 1 passed`，FAIL 为「keybar 开启时渲染键条…」；还原后全绿。
- 变异（IME，计划 §8.5 声称的那发）：按计划把 `onInputAfter` 改成无条件 `term.input(ie.data)` **编译不过**（`TS6133 seqBeforeInput` 未使用 + `TS2345 string|null 不能赋给 string`）——按纪律该发不算数。改用可编译等价变异（`onInputAfter` 去掉 `ie.isComposing` 守卫，count=1，`tsc` EXIT=0）：`terminalInput.test.ts` 仍 `28 passed`——新 IME 两支未变红。如实记：IME 组合路径走 xterm 自身 CompositionHelper，不经 `installTerminalInputFix`；新用例是**行为锁**（与计划 §8.5 声明一致），IME 变异不在整卡 AC 清单（AC-4/5/6）内。该发未变红的编造结论不采用。
- 提交 `feat(web): 移动终端键条与 IME 组合输入（B369.6 T4）` → `18c9a4cd`。（T1→`41bf997e`、T3→`7d744c45` 亦已落。）

## 七、T5（收口复核，本轮 75a94d89 抢救分支上亲自复跑）

分支 `cards/B369.6-charter-3`，HEAD `9cbcdb9d`（OOM 抢救 wip）。`web/node_modules` 起手缺失，`npm ci` → `added 290 packages`。以下为原始读数：

| 命令 | 原始结果 | 退出码 |
| --- | --- | --- |
| `cd web && npx tsc -b` | 无输出 | 0 |
| `cd web && npx eslint .` | `✖ 27 problems (0 errors, 27 warnings)` | 0 |
| `cd web && npx vitest run` | `Test Files 134 passed (134)`、`Tests 1440 passed (1440)`、`Duration 22.47s` | 0 |
| `go build ./...` | 无输出 | 0 |
| `cd mobile && go build ./...` | 无输出 | 0 |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok  github.com/Xsxdot/handoff/cmd  0.068s` | 0 |
| `codegraph --repo . check --view cards-B369-charter` | `"fails": []`；`crossDomainEdges 1249`、`viewContainers 343` | 0 |
| `codegraph --repo . validate --view cards-B369-charter` | `"issues": null`、`"edgeIssues": null` | 0 |
| `codegraph --repo . check` | `"fails": []` | 0 |
| `git diff --stat -- web/src/api` | 空输出（P2=A 机械判据；相对基线 `cards/B369-charter-4...HEAD` 该目录仅 `pty.ts` 的 case 块作用域，属 T0 eslint 修复、非新增协议/DTO） | 0 |

**变异自验（本轮独立复跑，先证可编译再计数）**：

- AC-5 断点：`useShellViewport.ts` 两档判据改 `width <= -1`（count=1）→ `npx tsc -b` EXIT=0（可编译）；`npx vitest run src/app/shell` → `Test Files 2 failed | 4 passed (6)`、`Tests 7 failed | 75 passed (82)`；`git checkout --` 还原后 `git status --porcelain` 空。
- AC-4 词表：`statusVocab.ts` `'已完成'`→`'完成'`（count=1）→ `tsc` EXIT=0；`npx vitest run src/app/cards` → `Test Files 2 failed | 8 passed (10)`、`Tests 4 failed | 99 passed (103)`；还原后工作树干净。
- AC-6 键条：`TerminalTab.tsx` `{keybar && (`→`{keybar && false && (`（count=1）→ `tsc` EXIT=0；`npx vitest run src/app/workbench -t "keybar"` → `Tests 1 failed | 1 passed`；还原后干净。

三条变异均先编译过再计数红，且各命中唯一；还原后 `git status --porcelain` 为空。AC-1..AC-9 满足。

## 八、提交事实（历史读数）

```
$ git add docs/superpowers/ledgers/2026-09-19-b369.6-implement-ledger.md && git commit -m "docs(B369.6): T5 收口复核台账（web 三闸 + Go/图闸 + 三条变异自验）"
[cards/B369.6-charter-3 25796f01] docs(B369.6): T5 收口复核台账（web 三闸 + Go/图闸 + 三条变异自验）
 1 file changed, 26 insertions(+), 1 deletion(-)
```

（随后按纪律只 amend 一次把本段收进同批提交——amend 换 hash 是 git 事实，不复写上面的历史读数。）
