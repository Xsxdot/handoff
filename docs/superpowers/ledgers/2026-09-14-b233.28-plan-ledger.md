# B233.28 plan 台账

节点：plan。产出物：`docs/superpowers/plans/b233.28-plan.md`。
有效基线：`cards/B233.1-charter-7` @ `56c7a9c7`。本分支：`cards/B233.28-charter-6`。起点含拍板提交：`cdbf6d5989dfa41aa8640933ff2854d227e171f6`（P-1..P-4 均为 A，C-1..C-4 已回写）。
上游核实（文件内，不靠会话记忆）：spec `docs/superpowers/specs/b233.28.md:3`「状态：**已批准**」；contract `docs/superpowers/specs/b233.28-contract.md:6`「冻结状态：随本提交冻结」+ `:13-29` C-1..C-4；breakdown `docs/superpowers/specs/b233.28-breakdown.md:2`「状态：已拍板」+ `:61-64` P-1..P-4=A。

- 2026-09-14：`go build ./...` → 空输出，退出码 0。
- 2026-09-14：`go vet ./internal/agentd/ ./internal/orchestration/ ./cmd/...` → 空输出，退出码 0。
- 2026-09-14：`go test ./internal/orchestration/ -count=1` → `ok github.com/Xsxdot/handoff/internal/orchestration 59.354s`，退出码 0。
- 2026-09-14：`go test ./internal/agentd/ -count=1` → `ok github.com/Xsxdot/handoff/internal/agentd 159.038s`，退出码 0。
- 2026-09-14：`go test ./cmd/... -count=1` → `ok github.com/Xsxdot/handoff/cmd 42.232s`，退出码 0。
- 2026-09-14：`go test ./internal/agentd/ -run 'TestReplyAnswersTicketAndNotifies|TestReplyUnknownTicket404|TestReplySelfHealsWithoutWaiter|TestReplyRelayFailureReturns502|TestHandleReplyIdempotentDoesNotRelayTwice|TestAttachEmptyListsAreArrays|TestAuthRequired' -count=1` → `ok ... 0.697s`，退出码 0。
- 2026-09-14：`codegraph check --view cards/B233.28-charter --repo .` → `fails: []`、`d_gateway->d_orchestration: 76`、`warns_count: 36`、`bestCoverage {assignedContainers:338, viewContainers:338, crossDomainEdges:1248, misplacedSkipped:0}`，退出码 0。
- 2026-09-14：`codegraph validate --view cards/B233.28-charter --repo .` → `issues:null`（0 issue），退出码 0。
- 2026-09-14：现状签名查证（读源码，非记忆）：`store.AnswerTicketApplied` 语义 `internal/store/store.go:1275-1305`；`store.UpdateTaskState` CAS `:525-552`；`store.GetProjectLocationByName` `internal/store/projects.go:82-90`；`TicketAnsweredPayload` `internal/orchestration/contracts.go:115`；`EventTypeTicketAnswered` `internal/proto/proto.go:106`；`facade.go` 空壳与哨兵 `:26,30,102,106`；`OrchestrationClient` 15 方法 `internal/orchestration/orchestration_client.go:54-74`；`var _ OrchestrationClient = (*Manager)(nil)` `internal/orchestration/manager.go:339`；`NewManager` 签名 `manager.go:297`。
- 2026-09-14：`grep -rhoP 's\.st\.\w+' internal/agentd/*.go | grep -v _test | sort -u` → 全量生产直打方法清点，与 contract §1.1/§1.3 逐条对齐，收口面闭包无遗漏（见 plan §3.1）。
- 2026-09-14：`grep -rn 'ErrTicketAnswerUnwired' --include='*.go' .` → 仅 `internal/orchestration/facade.go` 4 处包内自引用，gateway 生产零引用（plan W1 删哨兵无外部破坏）。
- 2026-09-14：夹具盘点核实（读测试源码）：`newTestEnv`/`newTestEnvWithCfg`（`server_test.go`，`package agentd_test`）与 `newTestAgentdEnv`/`newTestAgentdEnvWithCfg`（`w3a_testhelpers_test.go`，`package agentd`）默认不注入 manager；`newManagerForServer(t, srv, ads)`（`b23313_gateway_testhelpers_test.go:299`）已存在，可注入真 Manager。受影响用例逐条落 plan §3.3。
- 2026-09-14：plan 自审发现并修正两处初稿缺口——① contract §1.1 列的 `handleDone`/`handleStop` 的 `s.st.GetTask`（`:933,:939,:956,:988,:1005`）属收口面（§10「§1.1/§1.3 列的 `s.st.*` 全部改用 `s.mgr.*`」），初稿误列为「不切」，已改；② `render_stream_test.go#newRenderServer`、`bundle_handler_test.go#newBundleEnv`、`ws_regression_round2_test.go` 三处自建 Server/env 的夹具初稿漏列，已补入 §3.3。修正后逐文件闭包与源码清点一致。
- 2026-09-14：本节点未实现任何调用点、未建脚手架；只落 plan 文档与台账。收口判据：工作树含 plan + 台账两文件，同批提交。
- 2026-09-14：提交事实（历史读数）——`git commit -m "plan(B233.28): gateway 任务面/项目位置走编排查询门面——W1..W4 单卡实现计划"` → 原始输出 `[cards/B233.28-charter-6 fc785931] plan(B233.28): ...`；`git status --short` 提交后为空。本行随该提交 amend 一次收进同批（amend 会换 hash，是 git 事实，不 chase HEAD）。
