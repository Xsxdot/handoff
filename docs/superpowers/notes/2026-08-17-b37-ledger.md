# B37 Windows 原生执行机——执行 ledger

**执行者**：handoff 任务 `50194cd7-4956-4bd5-9a8a-edf5a12c6bfa`
**分支**：`feat/b37-windows-native-executor`
**计划**：`docs/superpowers/plans/2026-08-17-b37-windows-native-executor.md`
**Spec**：`docs/superpowers/specs/2026-08-17-windows-native-executor-design.md`
**基线 commit**：`ecacae05`

## 执行记录

（每 task 完成、每轮修复追加一行；commit 范围按该 task 的完整 diff 记录。）

- Task 1 完成：补齐真实 Windows vet 失败清单中的 unix-only 测试 build tag；`procenum_test.go` 的 unix-only `Getpgid` 断言拆入 `procenum_unix_test.go`；新增 `TestWindowsVets`；CI 的 Windows 门追加 amd64 vet。双裁决：spec 符合、代码质量通过，无修复轮。commit 范围：`.github/workflows/ci.yml`、6 个 executor/prochost unix 测试文件、`internal/prochost/procenum_test.go`、`internal/prochost/procenum_unix_test.go`、`internal/prochost/shim_test.go`、`internal/prochost/windows_build_test.go`、本 ledger。
  - 实测 `GOOS=windows GOARCH=amd64 go vet ./...`：无输出。
  - 变异验证：移除 `internal/prochost/platform_test.go` 的 `//go:build unix` 后，`TestWindowsVets` 按预期失败，原始错误为 `undefined: syscall.Kill`；还原后通过。
  - 六闸门：build、vet、`go test ./... -count=1`、gofmt、Windows amd64 build/vet 均通过；首次测试运行受沙箱临时目录权限影响，获准在沙箱外重跑后根包与 cmd 包均 `ok`。
  - Windows 运行期行为未验证（待真机）。

- Task 2 完成：`platform_other.go` 收紧为 `!unix && !windows`；新增 Windows `LockFileEx`/撞锁判定骨架、平台 fence mode 常量；`golang.org/x/sys` 已处于 direct require，`go mod tidy` 后 `go.mod` 与 `go.sum` 均无变化。双裁决：spec 符合、代码质量通过，无修复轮。commit 范围：`internal/prochost/platform_windows.go`、`internal/prochost/platform_other.go`、`internal/prochost/platform_unix.go`、本 ledger。
  - 编译门：darwin build/vet、Windows amd64 build/vet 均通过；全量 `go test ./... -count=1` 根包与 cmd 包均 `ok`；gofmt 无输出。
  - Windows LockFileEx 运行期行为未验证（待真机）；本机仅完成 Windows 编译与 vet。

- Task 3 完成：泛化 shim 的进程容器安装点；unix 复用 RLIMIT_NPROC 且保持装载失败不阻断；Windows 实现 Job Object（无条件创建、`KILL_ON_JOB_CLOSE`、可选 `ActiveProcessLimit +1`、shim 自持句柄）、breakaway detached spawn、shim-only job 回收与单进程终止。双裁决：spec 符合、代码质量通过；补充成功日志与 shim 头部语义注释后复审通过，无未决项。commit 范围：`internal/prochost/platform_windows.go`、`internal/prochost/platform_unix.go`、`internal/prochost/platform_other.go`、`internal/prochost/shim.go`、本 ledger。
  - unix `Fence|Shim` 回归锚点在沙箱外通过；完整 build/vet/test/gofmt/Windows build+vet 门通过。
  - Windows Job Object、detached spawn、按 job 回收运行期行为未验证（待真机）；本机仅完成 Windows 编译与 vet。

- Task 4 完成：`fenceLimit` 增加平台模式；Windows 直接取 `TaskHardLimit`、不访问 `procLimit`，unix 比例模式保持原语义；`SetFencePolicy` 接入三参；agentd 在 Windows 且 `TaskBudget>0` 时明示告警档缺席。双裁决：spec 符合、代码质量通过，无修复轮。commit 范围：`internal/prochost/fence.go`、`internal/prochost/fence_test.go`、`cmd/agentd.go`、本 ledger。
  - 新增两条硬上限测试先红后绿；完整 build/vet/test/gofmt/Windows build+vet 门通过。
  - Windows Job Object 围栏运行期行为未验证（待真机）；本机仅完成策略逻辑测试与 Windows 编译/vet。
