# B107 ledger：控制台文件树右键菜单

**执行者**：handoff 504de42d-cfb5-4a94-ac18-c670907f4bf6（subagent 驱动，本人不亲自改码）
**分支**：feat/b107-file-tree-context-menu
**计划**：docs/superpowers/plans/2026-08-15-b107-file-tree-context-menu.md
**Spec**：docs/superpowers/specs/2026-08-15-b107-file-tree-context-menu-design.md
**基线 commit（分支起点）**：6b1b4591799f4e9a3a48fa3dcb7d49beba534356

## 执行记录

- (2026-08-15) 开工。基线 commit：6b1b4591799f4e9a3a48fa3dcb7d49beba534356。
- Task 1 完成（57be0791 + d9cb242b）：CreateEntry/RenameEntry/DeleteEntry + 3 哨兵，全部经 os.Root。首审 FAIL 1 项（通用失败分支缺 Warn 日志）→ 修复轮 1（d9cb242b）→ 复审 PASS。commit 范围：internal/agentd/workspace.go、workspace_test.go。
- Minor 记账（终审 triage）：① TestEntryOpsSymlinkEscape 注释称「三个动作」但只测了建与删（Rename 的符号链接逃逸无直接用例，实现经 root.Stat/root.Rename 实际安全）；② CreateEntry 的 kind 非 "file"/"dir" 时静默按建文件处理，无拒绝；③ validateEntryName 不拒 NUL 字节（超长名会落到通用错误）。
- Task 2 完成（f7e420d7 + d8e2589f）：CopyEntry 复制，含目录递归与 foo copy N 命名。首审 FAIL 1 项（目录名带点被错误拆扩展名，spec §3.4）→ 修复轮 1（d8e2589f，加 isDir 参数 + 带点目录断言）→ 复审 PASS。commit 范围：internal/agentd/workspace.go、workspace_test.go。
- Minor 记账（终审 triage）：④ CopyEntry 逃逸时 rootErrIsEscape 对已二次包装的 ErrPathEscape 判定为假，走通用 Warn 而非逃逸专用 Warn（仅日志分类偏差，errors.Is 链不受影响）。
- Task 3 完成（e162f9b5）：SearchInDir + proto.SearchHit/SearchResult，纯 Go 遍历 + 条数/超时/跳过生成物三条护栏。审查双 PASS（观察项均非阻塞）。commit 范围：internal/agentd/workspace.go、internal/proto/projects.go、internal/agentd/workspace_test.go。
- Minor 记账（终审 triage）：⑤ SearchInDir 内 io.ReadFull 错误被丢弃当非二进制（真实 I/O 错误大概率随后暴露）；⑥ scanned++ 在 IsRegular 过滤前（符号链接也计入扫描文件数）；⑦ SearchInDir doc comment 硬编码引用行号「1543 行注释」，行号易随改动失效。
- Task 4 完成（5fd4cf38）：5 条端点（建/复制/改名/删/搜）全部经 forwardIfRequested 转发 + writeEntryError 错误映射，路由清单与文件头边界注释同步。审查双 PASS。commit 范围：internal/agentd/workspacefiles.go、server.go、internal/proto/projects.go、internal/agentd/workspacefiles_test.go。
- Minor 记账（终审 triage）：⑧ 五个新 handler 入口 Info 的 "path" 记的是 r.URL.Path（路由路径）而非 path 查询参数（工作树路径），与 handleWorkspaceDir/handleWorkspaceFile 的既有约定不一致，日志里看不到是哪个工作树；⑨ handleWorkspaceEntryCopy 的 doc comment 声称「rel 空串按不存在拒绝」，实测 CopyEntry("") 走 statat empty path 兜底 500 而非 404，行为与注释不符（空 rel 复制返回 500）。
- Task 5 完成（c2d74932）：CreatePtySessionReq 加 Rel 字段，resolvePtyBase 支持 rel（词汇层校验，非 os.OpenRoot，注释点明判据不同）。审查双 PASS。commit 范围：internal/proto/pty.go、internal/agentd/pty_api.go、internal/agentd/pty_api_test.go。
- Task 6 完成（d4db595b）：ContextMenu 迁到 shared + separator/disabled/键盘导航；project 内需先 npm install，并补装 @testing-library/user-event（devDependency，package.json/lock 一并提交）。审查双 PASS。commit 范围：web/src/app/shared/ContextMenu.tsx(.test.tsx)、web/src/app/tree/ProjectTree.tsx（仅 import）、web/package.json、web/package-lock.json。
- Task 7 完成（00c47338 + 1859ae4d）：FileTree 右键菜单接线（5 个 client 函数 + EntryNameDialog/DeleteEntryDialog + useDirEntries.reload + rel 透传链路 tabs/useWorkbench/TerminalTab/Shell + 搜索面板）。首审代码质量 FAIL 1 项（目录行删除/重命名/复制后刷新错层，spec §6.4 意图落空）→ 修复轮 1（1859ae4d，reload 目标统一为 parentOf + 3 条回归锚）→ 复审 PASS。全量 vitest 495 用例 0 failed，tsc/build 0 error。commit 范围：web/src/ 12 个文件。
- Minor 记账（终审 triage）：⑩ DeleteEntryDialog 的 rel prop 声明未使用（死 prop）；⑪ 搜索 runSearch 闭包读 search 状态，请求在途改关键词时旧响应会盖掉新结果（轻量竞态）；⑫ 查找命中列表 key={i}（静态列表可接受但非稳定键）；⑬ 查找面板打开时顶部「搜索文件…」前端过滤框仍显示（语义冗余）；⑭ Shift+F10/ContextMenu 键触发菜单时 clientX/Y 为 0，菜单钉在左上角；⑮ 空名只挡全空白，" foo " 带首尾空格的名字放行到服务端；⑯ 复制失败走树顶 opError 横幅而非弹层（错误原文已透传，仅呈现面不同）。

## 终审机械判据

（终审时逐条贴出输出）
