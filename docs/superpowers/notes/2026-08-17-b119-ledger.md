# B119 执行 ledger

- 分支：`fix/b119-sweep-refusal`
- 起点：`88d46e599`

## Tasks

- Task 1：完成；双裁决通过（规范覆盖活 executor 降级点名、跳过 executor 本体、无名册空跑；代码 diff 无额外行为）；commit `43f0e511`。针对性命令 `TMPDIR=$PWD/.gotest_tmp GOTMPDIR=$PWD/.gotmp GOCACHE=$PWD/.gocache go test -run 'Test(Sweep|Footprint|Classify)' ./internal/prochost/ -count=1` 原始输出：`ok github.com/Xsxdot/handoff/internal/prochost 0.217s`。完整包测试原始失败包含：`TestStartRecordsStartedAt: Start 未记录 StartedAt，got 0`、`TestEnumProcsFindsSelf: sysctl kern.proc.uid: operation not permitted`、`TestProcLimitPositive: sysctl kern.maxprocperuid: operation not permitted`、`TestShimWritesRosterImmediately: 10s 内没等到非空名册`。
- Task 2：完成；双裁决通过（终态统一清扫、回合边界保留一次清扫、Stop/Done/非终态/幂等路径覆盖）；commit `62d51f87`。原始测试输出：`go test -run 'Test(Transit|HandleResultSweeps|StopSweeps|DoneSweeps)' ./internal/agentd/ -count=1 -v` 全部 PASS，末行 `ok github.com/Xsxdot/handoff/internal/agentd 6.403s`。
- Task 3：完成；双裁决通过（停源头后才落 failed、失败保持 running、ForceReclaim 不删 worktree、不重复清扫、ErrExecutorAlive 记录实际 killed）；commit `1576cc17`。原始测试输出：`go test -run 'TestForceReclaim|TestStop|TestHandleResult' ./internal/agentd/ -count=1 -v` 全部 PASS，末行 `ok github.com/Xsxdot/handoff/internal/agentd 1.185s`。
- Task 4：完成；双裁决通过（硬上限接 ForceReclaim、失败保持活跃并边沿告警、四处 watchdog 测试接线、生产接线保留 RecoverOnStartup sweep）；commit `b20a589c`。原始测试输出：`go test ./internal/agentd/ ./cmd/... -count=1` 退出码 0、无输出；`grep -rn "transitFailedWithEvent" internal/ cmd/` 退出码 1、无输出。

## 修复轮次

- Task 1 / Round 1：完成 Sweep 分段决策与 executor 本体跳过；针对性测试通过后提交 `43f0e511`。
- Task 2 / Round 1：完成 transit 终态收口与回合边界清扫；入口路径测试通过后提交 `62d51f87`。
- Task 3 / Round 1：完成 stopExecutor 返回值、ForceReclaim 与 ErrExecutorAlive 分诊；测试通过后提交 `1576cc17`。
- Task 4 / Round 1：完成 watchdog reclaim 接线、失败边沿告警与测试适配；包级测试通过后提交 `b20a589c`。

## Task 5 总回归原始输出

以下命令均使用工作区 `.gotest_tmp` / `.gotmp` / `.gocache`，命令输出原样记录；空输出表示命令退出码 0。

```text
$ go build ./...

$ go vet ./...

$ gofmt -l .

$ go test ./... -count=1
--- FAIL: TestInstallScriptUnits (0.03s)
    install_test.go:15: install.sh 单测失败:
        mktemp: mkstemp failed on /var/folders/xc/hpx9c9w153j7tvphw53lc8qr0000gn/T/tmp.ZAxqXY9R11: Operation not permitted
FAIL
FAIL github.com/Xsxdot/handoff 0.714s
--- FAIL: TestProjectAddRejectsNonRepo (0.01s)
panic: nil Context [recovered, repanicked]
...
FAIL
FAIL github.com/Xsxdot/handoff/cmd 1.590s

$ go test -race ./internal/agentd/ ./internal/prochost/ -count=1

```

## 变异测试原始输出

```text
Mutation 1: 把 Sweep 活 executor 分支改回 return 0, VerdictOK, ErrExecutorAlive
$ go test -run 'TestSweepAlive' ./internal/prochost/ -count=1
--- FAIL: TestSweepAliveSkipsGroupPhaseButStillRosterKills (0.00s)
    footprint_test.go:212: 段②应回收 2 个名册成员，实得 0
--- FAIL: TestSweepAliveNeverSignalsExecutorItself (0.00s)
    footprint_test.go:248: 应只回收 501 这一条，实得 0
FAIL
FAIL github.com/Xsxdot/handoff/internal/prochost 0.589s
FAIL

Mutation 2: 把 transit 终态分支的 m.sweep(taskID) 改为 _ = taskID
$ go test -run 'TestTransitToTerminalSweeps|TestForceReclaimSuccess' ./internal/agentd/ -count=1
--- FAIL: TestForceReclaimSuccessTransitsFailed (0.01s)
    forcereclaim_test.go:46: 终态迁移应触发一次清扫，实得 0 次
--- FAIL: TestTransitToTerminalSweeps (0.00s)
    manager_test.go:2118: 迁入终态应清扫一次该任务，实得 []
FAIL
FAIL github.com/Xsxdot/handoff/internal/agentd 0.516s
FAIL

Mutation 3: 注释掉 ForceReclaim stopExecutor 失败分支的 return
$ go test -run 'TestForceReclaimKeepsTaskActive|TestScanTaskProcsReclaimFailure' ./internal/agentd/ -count=1
--- FAIL: TestForceReclaimKeepsTaskActiveWhenStopFails (0.04s)
    forcereclaim_test.go:74: 停不掉 executor 时 ForceReclaim 必须返回错误
FAIL
FAIL github.com/Xsxdot/handoff/internal/agentd 0.562s
FAIL
```

## Minor 记账（不进修复回路，终审 triage）

- `go test ./... -count=1` 原始失败：`TestInstallScriptUnits` 报 `mktemp ... Operation not permitted`；`TestProjectAddRejectsNonRepo` 报 `panic: nil Context`。本轮未据此修改代码。
- Task 1 完整 `internal/prochost` 测试原始失败还包括 `TestStartRecordsStartedAt` 的 `StartedAt=0`、`TestEnumProcsFindsSelf` / `TestProcLimitPositive` 的 `operation not permitted`、`TestShimWritesRosterImmediately` 超时；针对性 Sweep/Footprint/Classify 测试已通过。

## 终审与真机复验交接

终审已按分支起点 `88d46e599` 复审完整 diff；代码改动集中在 B119 §2.1–§2.3，未发现需要进入第二轮修复波的问题。`git diff --check` 通过，三条变异还原后关键测试通过。

按 spec §5 的真机复验状态：

- 症状 A：未验。启动普通任务并观察 `agentd.log` 的具体复验待协调者执行；预期核对「点名回收完成」而非「执行者仍存活，拒绝清扫」。
- 硬上限档：未验。尚未构造 fork 炸弹；需核对 executor 被停、进程被收、任务落 `failed`、理由含真实进程数且 worktree 仍在。
- 失败路径：未验。人工构造不易；需核对任务保持活跃、失败提示只在首个失败边沿产生且后续轮次继续重试。

## Task 5 状态

- Task 5：完成；总回归、三条变异测试、终审与真机复验交接说明已落盘；commit 范围为 `b20a589c..` 本次 ledger 提交。
