# B103 执行 ledger

**分支**：`fix/b103-proc-roster-blindspot`
**起点**：`931d6a14`（spec + plan 提交）
**执行方式**：subagent-driven（逐 task 实现 + 独立审查双裁决）

## 进度

| 时间 | 事件 | commit 范围 | 说明 |
|---|---|---|---|
| 2026-08-15 | ledger 初始化 | - | 起点 `931d6a14` |
| 2026-08-15 | Task 1 完成 | `8550e1c1` | 名册合并改并集语义（mergeRoster/marshalRoster/writeRosterBytes）；审查 APPROVE，无修复轮 |
| 2026-08-15 | Task 2 完成 | `a9355648` | 采样改累积语义、间隔 1s、未变不落盘（rosterSampler）；审查 APPROVE，无修复轮 |
| 2026-08-15 | Task 3 完成 | `850b5a5e` | 清扫返回 error，ErrExecutorAlive 透传；审查 APPROVE，无修复轮 |
| 2026-08-15 | Task 4 完成 | `a71c3062` | Done/Stop 停完 executor 后补清扫 + 存活锁竞态有界重试；审查 APPROVE，无修复轮 |
| 2026-08-15 | Task 5 完成 | `646fc614` | 采样开销实测：`BenchmarkRosterSampleReal` 约 0.17 ms/次，占 1s 间隔约 0.017%，可接受；ledger 补实测数字。进度行内写的是填充时版本 `5279379c`（自引用：最终 hash 写进 ledger 会改变自身），对象库真实存在 |
| 2026-08-15 | 整分支终审 | `931d6a14..9accd577` | 终审 REVISE（4 项低严重度收口）→ 修复 commit `9accd577` → 范围复审 FINAL APPROVE。修复项：gofmt 对齐、roster.go 两处「每 15s」陈旧注释改引 rosterInterval、未变路径补耗时 Warn（spec §3.1）、重试 defer 改捕获原值 |

## Minor 发现（终审统一 triage）

- `roster.go:69`（descendantsOf 注意节）与 `roster.go:201`（writeRoster 注意节）注释仍写「每 15s 被调用一次」，与新 1s 间隔不符（Task 2 按要求未碰 roster.go）。
- `snapshotRoster` 已无调用方，保留为悬挂入口（spec 明确要求保留）。
- 重试用例 defer 还原 `sweepRetryGap` 时硬编码 `200 * time.Millisecond`，建议改成捕获原值（当前不影响正确性）。
- `TestSweepAfterStopGivesUpAfterBoundedRetries` 未断言打了 Warn（spec 未要求）。
- 包级可变变量 `sweepRetryGap` 无锁保护，依赖测试顺序执行；将来若给用例加 t.Parallel 需留意。

## 终审处置（2026-08-15）

终审对 ledger 既有的 Minor 逐条 triage：

- `roster.go` 两处「每 15s」陈旧注释 → **修**（本改动直接造成，已随 `9accd577` 修掉）
- `snapshotRoster` 无调用方 → **不修**（spec 决策保留悬挂入口）
- 重试 defer 硬编码 200ms → **修**（`9accd577` 改捕获原值）
- Warn 未断言 → **不修**（spec 未要求，日志文本断言脆弱）
- 包级变量无锁 → **不修**（当前无 t.Parallel，串行安全；与 `rosterInterval` 同款既有测试缝形态）

终审新增发现：gofmt 违规（roster_test.go 注释对齐，`9accd577` 已修）；未变路径耗时 Warn 永不触发（spec §3.1 字面不符，`9accd577` 已修）；`handleResult` 注释文字漂移 → 不修（语义仍成立）；`TestMarshalRosterStableForSameSet` 覆盖冗余 → 不修。

## 实测数据

**基准**：`internal/prochost/shim_test.go` 的 `BenchmarkRosterSampleReal`，用真实 `enumProcs`（不打桩），200 次迭代。

- **单次采样耗时**：`BenchmarkRosterSampleReal-10  200  171478 ns/op`，即约 **0.171 ms/次**。
- **当时机器进程总数**：**592**（方法：`ps -A -o pid | wc -l`，含表头一行，实际进程 591）。机器平台：darwin/arm64（Apple M5）。
- **相对 1s 间隔占比**：0.171 ms / 1000 ms ≈ **0.017%**（计算：171478 ns = 0.171 ms；0.171 / 1000 = 0.000171 ≈ 万分之 1.7）。
- **结论**：单次采样约 0.17 ms，占 1s 间隔的约 0.017%，开销可接受——依据：真机 592 进程下整轮枚举 + 合并 + 序列化仅 0.17 ms，远低于 1s 间隔，且内容未变时不落盘（`TestRosterSamplerSkipsWriteWhenUnchanged`），稳态无磁盘 I/O。
