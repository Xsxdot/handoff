# B233.9 隔离实例真机（2026-09-10）

DUT 功能线 `cards/B233.1-charter-7` @ `b1873d4c`。生产 agentd 未触碰。

## 实例

| | 生产 | 隔离 |
|---|---|---|
| 本机 | pid **550** `~/.handoff` :7777，全程仍在 | pid 79308 `~/.handoff-b2339` :7788 sqlite，收工已停 |
| linux-01 | pid **1066664** `/root/.handoff` :7777，全程仍在 | pid 1399899 `/tmp/handoff-b2339-data` :7789 sqlite，收工已停 |

隔离 token `b2339-iso-token`，二进制从 `b1873d4c` 现编（darwin/arm64 与 linux/amd64）。

## 验证

### 1. 共享派发（本机隔离）

`handoff dispatch --receiver grok-local --new-worktree --prompt ...` 走到 `Manager.Dispatch`：仓库校验、managed worktree、纪律落盘、Profile overlay 之后启动 grok。因隔离 HOME 无 grok 登录，Start 返回 Authentication required，任务落 failed。这是真入口，不是 mock。

fake 载体因「无法准备非空纪律 Profile overlay」更早失败，同样走 B39 启动前补偿（删树+删新建分支）。

### 2. B351 Stop / Reclaim HTTP（两侧隔离）

任务 `825f83a8-4290-4069-ba1a-940a0e22d864`（failed）：

- `POST /api/tasks/.../stop` → **409** `任务当前状态不允许该操作`（B351：Stop 409 继续 Reclaim）
- `POST /api/tasks/.../reclaim` `{"force":true}` → **200** `{"removed":false,"action":"already_absent",...,"branch":"probe/b2339-grok3"}`（不删分支）

linux-01 隔离、不存在的 task id：

- Stop → **404** `任务不存在`（B351 软成功、不 Reclaim）
- Reclaim force → **404** `记录不存在`

### 3. B233.8 / OpenCode 组装

隔离实例无可用 OpenCode 登录（detect pending / grok ACP Authentication required）。传输腿用上项 HTTP 覆盖。OpenCode ApprovalClient 注入仍以机内测试为准，不把隔离失败写成迁包回退。

### 未跑满（第一轮）

- 写闸关闭后远端仍 running 再 Stop+改名 `-2`：隔离 grok 未能登录。
- 跨机 `dispatch --target linux-01-iso`：linux-01 隔离未登记 grok 凭据。

## 收工（第一轮）

隔离进程已杀；生产 pid 550 与 1066664 仍是原进程。

---

# 补跑 2026-09-10（拷本机凭据）

用户授权把本机 grok/opencode 登录文件拷进隔离 HOME（`auth.json` + grok `config.toml` / `AGENTS.md`，不拷 session）。生产 7777 未动。

隔离 HOME：

- 本机 `~/.handoff-b2339/home/grok`、`~/.handoff-b2339/home/opencode`
- linux-01 `/tmp/handoff-b2339-data/home/grok`、`.../home/opencode`

载体 `grok-iso` detect **online**（两侧）。本轮隔离 pid：本机 **85283** :7788；linux-01 **1443042**（中途重启后）:7789。生产 pid **550** / **1066664** 全程未变。

## 跨机完整回路

本机隔离 CLI：

`dispatch --target linux-01-iso --receiver grok-iso --new-worktree --new-branch iso/b2339-cross1`

- task `7b088dd0-70b8-4f83-b578-a34a5f13065f` **running**，HOME=`/tmp/handoff-b2339-data/home/grok`，基线 `ffb780cb`
- `waiting_review`，summary「按计划仅回复pong，未改文件未提交」
- `done` → state **completed**，managed worktree 已回收，分支 `iso/b2339-cross1` 仍在

## B351 写闸后改名

在 linux-01 隔离账本建探针卡 P1（workflow `iso-b2339`）。`POST /api/cards/P1/step` implement。Transport 返回后删 `card_run_locks` 关写闸。

1. 首轮 task `92a04f4c-48b2-49f7-a8d7-1b9029ea3997` 分支 **`cards/P1-implement`**。写闸关闭 → 无 `card_tasks` 挂账 → `card_dispatch_rounds` 一行 purpose=implement → Stop+Reclaim（工作树删除，**分支保留**）。任务终态 failed。
2. 次轮 `重跑轮分支挂号 round=2 branch=cards/P1-implement-2`。task `4ccc9a82-ba15-4fe6-a5a9-450f604c2aa7` **running** 后 completed。git 同时存在 `cards/P1-implement` 与 `cards/P1-implement-2`。

## 仍不在本轮

- 非 OpenCode 权限/提问权威迁移（B233.8 OOS，产品卡）
- OpenCode 隔离 harness 登录（拷了 auth.json；本轮用 grok 跑满共享派发与 B351）

## 收工（补跑）

隔离 agentd 已停；生产 pid 550 与 1066664 仍是原进程。
