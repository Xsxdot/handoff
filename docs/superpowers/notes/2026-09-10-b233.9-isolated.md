# B233.9 隔离实例真机（2026-09-10）

DUT 功能线 `cards/B233.1-charter-7` @ `b1873d4c`。生产 agentd 未触碰。

## 实例

| | 生产 | 隔离 |
|---|---|---|
| 本机 | pid **550** `~/.handoff` :7777，全程仍在 | pid 79308 `~/.handoff-b2339` :7788 sqlite，收工已停 |
| linux-01 | pid **1066664** `/root/.handoff` :7777，全程仍在 | pid 1399899 `/tmp/handoff-b2339-data` :7789 sqlite，收工已停 |

隔离 token `b2339-iso-token`，二进制从 `b1873d4c` 现编（darwin/arm64 与 linux/amd64）。

## 验证

### 1. 共享派发（本机隔离）

`handoff dispatch --receiver grok-local --new-worktree --prompt ...` 走到 `Manager.Dispatch`：仓库校验、managed worktree、纪律落盘、Profile overlay 之后启动 grok。因隔离 HOME 无 grok 登录，Start 返回 Authentication required，任务落 failed。这是真入口，不是 mock。

fake 载体因「无法准备非空纪律 Profile overlay」更早失败，同样走 B39 启动前补偿（删树+删新建分支）。

### 2. B351 Stop / Reclaim HTTP（两侧隔离）

任务 `825f83a8-4290-4069-ba1a-940a0e22d864`（failed）：

- `POST /api/tasks/.../stop` → **409** `任务当前状态不允许该操作`（B351：Stop 409 继续 Reclaim）
- `POST /api/tasks/.../reclaim` `{"force":true}` → **200** `{"removed":false,"action":"already_absent",...,"branch":"probe/b2339-grok3"}`（不删分支）

linux-01 隔离、不存在的 task id：

- Stop → **404** `任务不存在`（B351 软成功、不 Reclaim）
- Reclaim force → **404** `记录不存在`

### 3. B233.8 / OpenCode 组装

隔离实例无可用 OpenCode 登录（detect pending / grok ACP Authentication required）。传输腿用上项 HTTP 覆盖。OpenCode ApprovalClient 注入仍以机内测试为准，不把隔离失败写成迁包回退。

### 未跑满

- 写闸关闭后远端仍 running 再 Stop+改名 `-2`：需要 executor 已接管的 running 任务。隔离 grok 未能登录，该项仍部分未验。
- 跨机 `dispatch --target linux-01-iso` 完整跑通：linux-01 隔离未登记 grok 凭据。

## 收工

隔离进程已杀；生产 pid 550 与 1066664 仍是原进程。
