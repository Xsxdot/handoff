# B100 ledger —— 穷举 EventTypeFailed 读取点并逐个判定

**分支**：`feat/b100-turn-failed-event`
**Task**：5/5（Task 1 新增 turn_failed 类型、Task 2 handleResult 改发、Task 3 follow 收流、Task 4 自动同步，均已合入本分支）
**本次工作**：穷举所有 `EventTypeFailed` / `"failed"` 读取点，逐个判定要不要把 `turn_failed` 一并纳入；唯一改动是 `web/src/app/task/EventMark.tsx` 补 `turn_failed: '回合失败'`。

## 检索命令与底稿

```bash
grep -rn "EventTypeFailed\|'failed'\|\"failed\"" internal/ cmd/ web/src/ | grep -v _test.go
grep -rn "fail_reason" internal/client cmd/ | grep -v _test.go   # 结果为空
```

## 逐点判定（每一条命中）

### Go / internal / cmd（16 处）

| 命中点 | 它在做什么 | 改 / 不改 | 为什么 |
|---|---|---|---|
| `internal/proto/status.go:148` | `PullStageFailed = "failed"` | 不改 | 拉取阶段的枚举值，与 proto.EventType 无关，是另一张枚举（PullStage） |
| `internal/proto/proto.go:26` | `TaskStateFailed TaskState = "failed"` | 不改 | 任务状态枚举定义（TaskState），不是事件类型 |
| `internal/proto/proto.go:46` | `EventTypeFailed EventType = "failed"` | 不改 | 事件类型**常量定义处**，非消费点 |
| `internal/proto/proto.go:51` | 为何与 turn_failed 分开的注释 | 不改 | 说明文字，非消费点 |
| `internal/executor/codex/adapter.go:380` | `a.finishTurn(r, "failed", ...)` | 不改 | 本地 turn 状态串（status 形参），与 proto.EventType 无关 |
| `internal/executor/codex/adapter.go:681` | `case "failed":` | 不改 | 本地 turn 状态串的 switch，非事件类型 |
| `internal/executor/codex/adapter.go:901` | 注释：解析失败返回 "failed" | 不改 | 注释，非消费点 |
| `internal/executor/codex/adapter.go:913` | `return "failed", ...` | 不改 | 本地 turn 状态串，非事件类型 |
| `internal/client/client.go:1463` | `if ev.Type == proto.EventTypeFailed` 决定 follow 收流 | **已改（Task 3）** | 现只对 failed 收流，turn_failed 走普通投递，与 completed 同款继续跟随 |
| `internal/agentd/watchdog.go:361` | `AppendEvent(... EventTypeFailed ...)` | 不改 | **生产者**：看门狗回收时任务真正终结才发 failed。spec 明说其余三个 failed 生产者不动 |
| `internal/agentd/watchdog.go:439` | `log.Info("启动恢复完成", ..., "failed", failed, ...)` | 不改 | 日志字段（恢复统计的 failed 计数），不是事件类型判定，无消费语义 |
| `internal/agentd/mirror.go:261` | `if ev.Type == EventTypeCompleted \|\| EventTypeFailed { return errMirrorDone }` | 不改 | **镜像订阅的终态收手**。turn_failed 不是终态——任务此刻 waiting_review，discoverOnce 的 `State.IsTerminal()`（mirror.go:174）也不收它的订阅。对 turn_failed 收手会把镜像订阅掐断，turn_failed 之后的事件就镜像不到了 |
| `internal/agentd/manager.go:1262` | Stop 路径 `AppendEvent(... EventTypeFailed ...)` | 不改 | **生产者**：协调者主动中止（stop）是任务真正终结才发 failed |
| `internal/agentd/reconcile.go:163` | 对账路径 `AppendEvent(... EventTypeFailed ...)` | 不改 | **生产者**：executor 已不在、任务终结才发 failed |
| `cmd/wait.go:244` | `shouldAutoSync` | **已改（Task 4）** | 自动同步触发集已加 `EventTypeTurnFailed` |

### Go 侧额外核对：`cmd/status.go:265`

`order := []string{"pending", "running", "waiting_answer", "waiting_review", "completed", "failed"}` —— 这是**任务状态排序枚举**（renderCounts 渲染计数用），不是事件类型消费点，与 turn_failed 无关。**不改**。

### web/src（12 处，均为任务状态枚举或测试）

web/ 里的 `"failed"` 绝大多数是**任务状态**（TaskState 枚举），不是事件类型，与 turn_failed 无关。逐条：

| 命中点 | 它在做什么 | 改 / 不改 | 为什么 |
|---|---|---|---|
| `web/src/app/tree/ProjectTree.tsx:353` | `<StateDot tone={loc.probe_error !== '' ? 'failed' : 'active'} />` | 不改 | **不是事件类型**。这是任务/位置连接状态（探测失败时圆点染红），消费的是 TaskState 视觉基调 |
| `web/src/app/board/columns.ts:53` | `isFailed(state)` 返回 `state === 'failed'` | 不改 | **任务状态**判断（isFailed），非事件类型 |
| `web/src/app/board/columns.ts:76` | `export type StateTone = ... \| 'failed'` | 不改 | **任务状态视觉基调类型**，非事件类型 |
| `web/src/app/board/columns.ts:84` | `STATE_TONES` 里 `failed: 'failed'` | 不改 | **任务状态 → 视觉基调映射**，非事件类型 |
| `web/src/app/board/columns.ts:141` | `stateBadgeVariant` 的 `case 'failed': return 'destructive'` | 不改 | **任务状态**的徽章样式，非事件类型 |
| `web/src/app/task/AdvanceActions.tsx:30` | `isTerminal(state)` 返回 `state === 'completed' \|\| state === 'failed'` | 不改 | **任务状态终态判定**（stop 在这两个状态下不可用）。turn_failed 是事件类型、不是任务状态，进不了这个判断，也不该进 |
| `web/src/app/board/columns.test.ts:38,39` | 看板列契约测试断言 `stateToColumn('failed')` / `isFailed('failed')` | 不改 | **测试**，钉的是任务状态映射（TaskState） |
| `web/src/app/board/columns.test.ts:64` | 状态基调契约测试 `stateTone('failed')` | 不改 | **测试**，任务状态基调 |
| `web/src/app/board/columns.test.ts:83` | `needsIntervention('failed')` 为 false | 不改 | **测试**，任务状态干预判定 |
| `web/src/app/board/columns.test.ts:89` | `stateBadgeVariant('failed')` 为 destructive | 不改 | **测试**，任务状态徽章 |
| `web/src/app/board/StateDot.test.tsx:20,40` | StateDot tone="failed" / TaskState state="failed" | 不改 | **测试**，任务状态视觉呈现 |
| `web/src/app/board/BoardPage.test.tsx:40` | 卡片渲染 'failed' 状态的边框 | 不改 | **测试**，任务状态卡片呈现 |
| `web/src/app/task/blocks.test.tsx:101` | `it.each(['completed','failed','delivery_failed','stalled','some_new_event'])` 断言无可裁决物的事件不挂「裁决入口」 | 不改 | **测试**。它钉的是 ADJUDICABLE 不含这些事件类型；turn_failed 同样不在 ADJUDICABLE（非可裁决工单），语义一致，无需新增用例 |
| `web/src/app/task/frames.test.ts:209` | `['waiting_review','completed','failed','pending']` 断言无结果+已停任务 → 'gone' | 不改 | **测试**，钉的是**任务状态**（toolState 按任务状态判断工具条），非事件类型 |
| `web/src/api/testdata/StatusResp.json:19` | `task_counts` 里的 `"failed": 0` | 不改 | **测试夹具**，任务状态计数 |
| `web/src/api/contract.test.ts:127` | 断言六个任务状态键恒存在 | 不改 | **测试**，任务状态键清单 |

### web/src 补充核对（grep 模式漏掉的**无引号** `failed:` 键）

grep 模式 `'failed'`/`"failed"` 抓不到无引号的对象键 `failed:`。为穷尽起见，把 grep 之外的 web 命中一并核对（task 说明点名了 StateDot.tsx）：

| 命中点 | 它在做什么 | 改 / 不改 | 为什么 |
|---|---|---|---|
| `web/src/app/board/StateDot.tsx:29,40` | `DOT_CLASS` / `TEXT_CLASS` 的 `failed: 'bg-state-failed'` / `failed: 'text-state-failed'` | 不改 | **任务状态视觉基调映射**（StateTone → 类名），非事件类型；与 turn_failed 无关 |
| `web/src/app/board/BoardPage.tsx:159,167` | `const failed = isFailed(task.state)` 后拼 `border-destructive/40 bg-destructive/5` | 不改 | **任务状态**判定（isFailed 来自 columns.ts），卡片终态视觉区分，非事件类型 |
| `web/src/app/board/BoardPage.tsx:13,153` | 注释里提到 failed | 不改 | 注释，非消费点 |
| `web/src/index.css:54,107,152` | `--state-failed` / `--color-state-failed` CSS 变量 | 不改 | 任务状态配色变量，非事件类型 |
| `web/src/api/client.ts:181` | 注释提到「落 failed」 | 不改 | 注释，非消费点 |

### web/src/app/task/EventMark.tsx（唯一改动）

> 注：grep 未命中它（键 `failed` 不带引号），但它正是**事件类型 → 中文文案**的消费映射，属于本 task 必须判定的读取点，task 说明已点名。

- 第 13-20 行 `EVENT_LABEL`：事件类型 → 中文文案映射，原 `failed: '任务失败'`。
  - **改**：加一行 `turn_failed: '回合失败'`（放在 failed 之后、delivery_failed 之前），并在映射旁补注释说明 failed 已收窄为「任务终结」、turn_failed 表示回合失败但任务仍在 waiting_review。
- 第 26 行 `ADJUDICABLE`（只含 permission_request / question）：**不改**。turn_failed 不是可裁决工单，放进它会误导人跑去工单面板扑空。

## Step 3：backlog_summary 对账分析（不改）

命中点：`internal/client/client.go:1381`（reconcileBacklog 返回 `sum.State == proto.TaskStateFailed` 作为 terminal）与 `internal/client/backlog.go`（computeBacklog）。

逐条验证结论：

1. **turn_failed 不进 actionable**：backlog.go 的 `Actionable` 直接取自 `snap.PendingTickets`（backlog.go:104，工单权威表），turn_failed 不是工单事件（payload 无 ticket_id，ticketIDOf 只认 permission_request / question），天然不在其中。✓
2. **turn_failed 不能导致对账把任务当死**：terminal 判定看的是**任务状态** `sum.State == proto.TaskStateFailed`（client.go:1381），而 turn_failed 发出后任务迁到 waiting_review（非终态），判定为 false——对账不会据此收流。✓

**判定：现有代码已满足这两条，不改。**

## Step 4：fail_reason 文案嗅探检索

```bash
grep -rn "fail_reason" internal/client cmd/ | grep -v _test.go
```

**结果为空**。internal/client 与 cmd/ 下没有任何按 `fail_reason` 文案内容判断分支的代码（newFailedPayload 的生产端透传不算消费判断）。判定：无文案嗅探，不需要改。

## 结论汇总

- 要改的消费点只有 **1 处**：`web/src/app/task/EventMark.tsx`（EVENT_LABEL 补 turn_failed）。
- Go 侧结论与 task 说明一致：除 Task 2/3/4 已改的之外，其余命中点全是**生产者**（三个发 failed 的终态路径）或**终态判定/无关枚举**，均不改。
- 未发现 task 说明之外需要改的遗漏点。

## 验证

- `go build ./... && go vet ./... && go test -count=1 ./...`：全绿。
- `cd web && npx vitest run && npx tsc -b && npx vite build`：0 error。

## 进度与终审

| 时间 | 事件 | commit 范围 | 说明 |
|---|---|---|---|
| 2026-08-15 | Task 1 完成 | `6b8740cf` | proto 新增 `EventTypeTurnFailed = "turn_failed"`；审查 APPROVE |
| 2026-08-15 | Task 2 完成 | `6e057fe3` | handleResult 回合失败改发 turn_failed，Stop 仍发 failed；三处旧契约断言（TestHandleResultSweepsProcsOnFail 与 handleresult_notrailer 两个 payload 用例）随新契约更新；审查 APPROVE |
| 2026-08-15 | Task 3 完成 | `be632f7a` | follow 只对 failed 收流；turn_failed 与 completed 同款继续跟随。**发现**：类型分开后 follow 旧代码本就对 turn_failed 不命中收流分支，行为已在 Task 1/2 自动正确，本 task 的价值在注释/日志契约对齐 |
| 2026-08-15 | Task 4 完成 | `6bac0abb` | 自动同步触发集抽成 shouldAutoSync 并加入 turn_failed；审查 APPROVE |
| 2026-08-15 | Task 5 完成 | `56d8e203` | 穷举 30 处命中点逐个判定写进本 ledger；唯一改动 web EventMark.tsx 补 turn_failed→回合失败；backlog 对账两条结论成立不改；fail_reason 嗅探 0 命中；审查 APPROVE |
| 2026-08-15 | Task 6 完成 | `6a0fff82` | README 事件表拆 completed/turn_failed/failed 三行，failed 收窄为任务终结 |
| 2026-08-15 | 终审 | `56c371e7..e9d7903b` | 终审 REVISE（3 项收口）→ 修复 commit `e9d7903b` → 范围复审 FINAL APPROVE。修复项：shouldAutoSync 归位到 autoSyncAfterWait 之后并更新其 doc 注释、client.go follow 注释按真实旧实现改写、proto 常量块 gofmt 对齐 |

## Minor 发现（终审统一 triage）

- Task 3 的 follow 收流改动实为注释/日志契约对齐（行为由类型分开自动修正）——不是缺陷，记录事实供 B100 复盘。
- Task 5 ledger 头部「Go / internal / cmd（15 处）」与实际 16 行不符（多列了 watchdog.go:439 日志字段一行）——纯计数笔误，表内容完整。
- `internal/agentd/hostguard.go`、`hostguard_test.go` 两文件本身 gofmt 违规（不在本次改动范围内，分支起点已有，未动）。
