# B233.26 独立审计报告：刀1+刀2 重构 diff（4f66f42df..3cc71fcf7）

审计日期：2026-09-13。审计员与实现者无关，只读审计（本报告为唯一写入）。
审计区间：`git diff 4f66f42df..3cc71fcf7`（759879b42 docs / e2941e7ae 刀1 / 3cc71fcf7 刀2），
工作树 cards/B233-residual @ 3cc71fcf7（clean）。
声明文档：`docs/superpowers/specs/b233.26.md`、`docs/superpowers/plans/b233.26-plan.md`、
`docs/superpowers/notes/b233.26-reversal-survey.md`。

## 结论

**无 P1，无 P2；P3 共 6 条。** 「纯归域重构、行为不变」的声明经独立核验成立；
门槛复跑全绿（含两遍全量测试、刀1 提交点独立编译+测试）。

---

## 1. 迁移完整性 —— 通过

- **agentd 侧消亡**：`ls internal/agentd/` 对 hub/shutdown/pullstate/admission/watchdog/
  ticketvoid/eventframes/coordinator_home/logging/b23313_retained/orchestration_client/
  managerfactory 全部零命中。logging.go 删除（并轨 orchestration/log.go，后者本区
  零改动、逐字同语义 `slog.Default()`）；b23313_retained.go 转形为
  `internal/orchestration/contracts.go`（R074，sha 变化来自别名/垫片段按 spec 退役）。
- **方向反转实证**：`grep -rn 'internal/agentd' internal/orchestration --include='*.go'`
  生产文件仅 `coordinator_home.go:3` 一处**注释**；`go list -deps ./internal/orchestration`
  不含 agentd（grep exit 1）。刀1 提交点（e2941e7ae）git archive 到 /tmp 独立构建，
  `go list -deps` 同样已无 agentd——反转在刀1 即完成。
- **git mv 相似度**：18 个 rename 全部 R065–R099（`git diff -M --name-status`）。最低的
  coordinator_home_test.go R065 是「拆两包」所致（见 §2e-2）；生产文件最低 R072
  （pullstate.go，全部为导出改名）。逐文件与基线做忽略 package 行的全文 diff：
  hub/admission/ticketvoid/shutdown/watchdog/orchestration_client 仅注释头与包名差异，
  **实现体逐字一致**；watchdog.go 全文 diff 仅 3 行（注释头 + `reconcileSchedRunning`→
  `ReconcileSchedRunning` 纯改名）；pullstate.go 全部为 pullTracker→PullTracker 等
  8 个改名，方法体逐行一致。

## 2. 行为不变核验 —— 通过（5 处高风险逐一判定）

### 2a. cmd 装配 —— 等价

`cmd/agentd.go` diff 逐 hunk 核对：18 行变化全部是 `agentd.`→`orchestration.` 前缀、
注释里的包名，构造顺序（SetProcHeadroom→NewServer→SetProviders→NewManager→
SetManager→SetTaskProcCounter→setupLedger→RecoverOnStartup→RunWatchdog→NewMirror→
NewShutdown→keystone/ledgermirror）与注入实参无一改动；`srv.Hub()`/`srv.Conf()` 调用
形态不变（返回类型随访问器变）。`cmd/agentd_test.go:134` 装配顺序断言仅改匹配串
`go agentd.RunWatchdog(`→`go orchestration.RunWatchdog(`，setup<recover<watchdog 的
顺序语义未动——符合 spec 约束 1 的「只许改前缀」。

### 2b. RecoverOnStartup 包装 / facadeAsRegistry / ReconcileSchedRunning —— 等价

- `internal/agentd/server.go:1008-1017`：Server.RecoverOnStartup 包装留 gateway，序列
  `orchestration.ReconcileSchedRunning(s.st, facadeAsRegistry{f: s.autoLedger}, log)` →
  `orchestration.ReconcileSchedRunning` 错误短路 → `orchestration.RecoverOnStartup(s.st,
  s.hub, probe, sweep, log)`，与基线唯一差异是前缀。
- `facadeAsRegistry` 定义在 server.go 生产段，本区间 diff 零命中（grep 的 2 处命中即
  上述调用点的前缀行）；测试侧消费（scheddispatch_test.go:72 等）原样。
- `ReconcileSchedRunning`：见 §1，watchdog.go 全文 diff 仅此一处改名，实现体逐字一致
  ——纯改名成立。

### 2c. errors.Is 对外语义 —— 等价

- `ErrBadDispatchRequest`/`ErrRepoUnusable` 基线定义即别名
  （`b23313_retained.go:185/:26` = `workspace.ErrBadDispatchRequest`/`workspace.ErrRepoUnusable`，
  git show 实证）。退役后双方改指 workspace 正身：orchestration 侧 manager.go 4 处 +
  projectadmin.go 13 处、agentd 侧 handlers.go:674/:680/:823——**同一 error 值**，
  errors.Is 匹配、HTTP 400/409/500 分类、文案逐字节不变。
- 其余哨兵随迁保名：contracts.go:179-201 与基线 errors.New 文案逐字一致（ErrWorkdirBusy/
  ErrExecutorStartFailed/ErrEnvResolveFailed/ErrDisciplineResolveFailed/ErrReclaim 三兄弟）；
  ErrWorkspaceUnavailable 自 agentd/workspace_capability.go（整文件删除）迁至
  orchestration/workspace_capability.go:35，值与文案一致；handlers.go writeDispatchError/
  writeReclaimError/writeProjectError 的全部 case 集合与状态码无增删改。
- `IsTerminalState` 退役：基线实现 `s == proto.TaskStateCompleted || s == proto.TaskStateFailed`
  （b23313_retained.go:203-205）与 `proto.TaskState.IsTerminal()`（proto.go:34-36）逐字
  等价；两处消费（status.go:94、handlers.go:224）改方法调用，语义恒等。

### 2d. 刀1 测试重写三处 —— 等价（判定声明成立）

1. **reclaim/gc HTTP 用例迁 gateway_http_test.go**：4 个 reclaim 用例 + 2 个 gc HTTP
   用例全部迁入，断言集逐条一致（409+ReasonDirty+非空 Dirty 清单、409+ReasonNotTerminal、
   404、200 列表行、gc 的 401/200/preview/force/执行五连断言、releasable_bytes:0 在场）。
   夹具重实现等价：`initRepo`/`newWorktree` 与包内 initGitRepoIn/gittesthelpers#newWorktree
   逐命令一致；`permgate.New(nil, logger)` 与 newTestGate(t) 同款；`fake.New(nil)` vs
   chanAdapter——reclaim/gc 端点不触达 adapter 与 gate，等价。请求构造差异仅两点且无
   语义影响：body 请求统一加了 Content-Type:application/json（handler 用 json.Decoder
   不读 Content-Type）；task ID 从时间戳改确定性 TestName 派生（每用例单任务，无碰撞面）。
   外部测试包（orchestration_test）是必须的：测试变体 *Manager 不满足 plain 变体的
   OrchestrationClient，文件头论证成立。
2. **TestPermissionStateVisibleBeforeTicket 回程直驱**：独立比对旧 HTTP 路径
   （handleReply @4f66f42df handlers.go:381-460 + resumeIfIdle:461-483）与新直驱
   （regression_round2_test.go:55-100）。基线事实：old newTestServerWithManager 的
   server hub 与 manager hub 是**两个实例**，reply 时 `s.hub.NotifyAnswer` 恒 false
   （waitPermission 阻塞在 manager hub 上）——旧测试确定性走的正是「无等待者→
   RelayAnswer 自愈→resumeIfIdle」分支，实现者的等价声明所据属实。新路径三步
   （AnswerTicketApplied→RelayAnswer→CAS 回迁循环）与旧分支逐步对应：CAS 循环的
   读态/查 pending/ErrBadTransit 重试（3 次上限）与旧 resumeIfIdle 语义一致；同步在
   answer goroutine 内完成再 wg.Wait，与旧「HTTP 返回时回迁已落定」时序等价。两处
   子分支差异经查在测试域不可达：ErrTicketConflict 容忍（每轮仅一次应答、工单全新，
   不可能命中）；RelayAnswer 错误被 `_ =`（chanAdapter.RespondPermission 默认
   respondErr=nil 恒返回 nil，manager_test.go:149-156）。U-1 被测契约
   （escalatePermission 先 transit 后 CreateTicket，manager.go:2077-2097）的检测力
   不减。**唯一失实点**：注释称「真 Server 回程由 gateway_http_test.go 覆盖」——
   gateway_http_test.go 只有 gc/reclaim，真 Server reply 覆盖在
   internal/agentd/server_test.go:338-1165（未改动、覆盖仍在）→ 记 P3-1。
3. **TestMain 随 hub_test 迁移**：orchestration 测试二进制从「无 TestMain」变为
   slog 默认输出丢弃（Level Debug）——manager/watchdog 的大量 Info/Warn 日志在测试
   失败时不再进 stderr，损失诊断可见性但不改任何断言；agentd 二进制失去 TestMain 后
   coordinator_sessionref.go 的 slog.Default() 日志开始打到 stderr（噪音，无断言影响）。
   记 P3-2。

### 2e. 刀2 测试处置三处 —— 等价

1. **TestGracefulShutdownKeepsPtySession 改经导出 Serve**：断言集原样四条
   （关停返回 nil、wdCanceled 关闭、PTY 会话仍登记、无「停止 PTY 会话失败」日志）。
   serveWithListeners 跨包不可见故必须走导出 Serve；TOCTOU（探活端口→Close→Serve
   重绑）注释如实，窗口内无进程内其他占用者，风险为 acknowledged flake 面 → P3-3。
2. **coordinator_home e2e 拆两段**：旧 TestCoordinatorHomeSupplyOnLaunchAndResume
   是「真 Prepare→真 runner→假 CLI 捕获」单流（字面 ~ 经真 Prepare 展开喂真 runner）。
   拆后：orchestration 侧保留全部**供给内容断言**（config 投影/AGENTS/skills/auth/
   occupied-target 哨兵/Resume 补供，逐条与旧一致，仅去掉 runner 段）；agentd 侧
   coordrunner_test.go 用真 runner+真假 CLI 锁「prepareHome 返回值=子进程 HOME、
   字面 ~ 不泄漏、Resume -s argv」——prepareHome 换成桩。**剩余组合覆盖**：缝是
   `func(SessionSpec)(string,error)` 纯函数值，两侧契约各自锁死（Prepare 返回展开后
   绝对路径 / runner 原样传递返回值），单流里唯一消失的是「字面 ~ 一竿子到子进程」
   的端到端串联——其两个半边均已断言，组合无逻辑可错。resolver 用例
   （TestCoordinatorSessionRefResolverEmptyHomeOnlineCarrier）迁 agentd 逐字 diff 为空。
   拆分判据（新导出面 >2 → 降级拆文件）落在 spec 预案内，coordinator_sessionref.go
   文件头已如实登记 → 记 P3-4（覆盖形态变化应入刀4 实现记录，注释已备料）。
3. **pullstate 导出化**：Begin/Stage/Fail/Snapshot/Busy/内部实现与原未导出版逐行一致
   （§1 全文 diff 实证）；update.go 自拉流程 10 处调用仅改大小写，抢锁/409/失败释放/
   成功不释放的语义链原样；3 个 pullstate 用例逐字迁 orchestration/pullstate_test.go
   （仅方法名导出化）。

### 测试总量守恒

`git grep '^func Test'`（agentd+orchestration+cmd+client）基线 1519 → 终态 1522，
diff 仅 +3（两个方向守卫测试 + TestCoordinatorRunnerPassesPreparedHomeToSubprocess），
**零删除**。测试行为覆盖无丢失。

## 3. 最小导出核验 —— 通过

穷举方法：对两提交的 agentd+orchestration 做顶层导出声明集合对比
（`^(func|type|var|const) [A-Z]`）+ 接收者方法集合对比。**新增生产导出共 11 个符号**：

| # | 符号 | 必需性判定 |
|---|---|---|
| 1-2 | `orchestration.PullTracker` + `NewPullTracker` | agentd.Server 持字段并构造（server.go:156/:275），spec §范围2 预案内 |
| 3-7 | `(*PullTracker).Begin/Busy/Stage/Fail/Snapshot` | agentd 侧 update.go（4 个）+ handlers.go:57（Snapshot）跨包消费，五个全部被真实消费，无冗余 |
| 8 | `ReconcileSchedRunning` | Server.RecoverOnStartup 包装留 gateway 且 reconcile 步需 gateway 的 facadeAsRegistry，只能导出被包装方 |
| 9 | `EventFrameHook` | gateway registerEventFrameHook（server.go:315-325）以 (DataDir, log) 调用导出面 |
| 10 | `NormalizeCoordinatorSpec` | scheddrain.go:274/:343 两处 gateway 消费 |
| 11 | `ShellQuote` | server.go:1283 attachLocator 消费 |

无更小方案：前 7 个是「update.go 自拉流程留 gateway」+「Server 持容器」组合的下确界；
后 4 个各有 gateway 侧真实消费点，均可给出 file:line。测试面新增（b23326 守卫 2 个、
coordrunner 1 个）不进 API。另核：基线的 ManagerDeps/ManagerFactory/TestManager（测试
专用导出）随桥退役**降级消失**（testManagerDeps 未导出参数束），导出面净减 16 个顶层
符号（含死代码退役 15 个，见 §4）。newManagerForTest 工厂体与退役前的
b23313_managerfactory_wire_test.go init 体逐行一致，无装配语义漂移。

## 4. 死代码退役证据复核 —— 通过

全仓 grep（internal/ cmd/ desktop/，剔除 workspace 正身定义/测试/注释）：
ResolveProject、ResolveDispatchBase、ResolveDefaultBaseBranch、BranchTip、
ProbeWorkspaces、CanonPath、IsTerminalState、RecoverTransit、ErrBadDispatchRequest、
ErrRepoUnusable **零残留引用**。CanonPath 唯一消费者 projectadmin_test.go:813 归置为
`canonPathForTest`（实现逐字一致，注释修正）。Git 三兄弟改直引 workspace
（projectadmin.go:213/:408、workspace_capability.go:40）。RecoverTransit 垫片消亡，
manager.go:3138 同包直调 recoverTransit。TruncateRunes：agentd truncateRunes
（server.go:960）与 turn.TruncateRunes（turn/text.go:45）实现体逐字等价（rune 截断、
不加标记），orchestration 26 处改 turn 版声明成立。

## 5. 守卫与棘轮 —— 通过（守卫牙齿有保留意见）

- `b23326_direction_guard_test.go`：主测试用 go/parser ImportsOnly 扫描 orchestration
  全部生产 .go 文件，命中 `internal/agentd` import 即红；测试文件合法豁免。牙测试
  （TestB23326DirectionGuardHasTeeth）验证解析机制能抓反向 import、注释同形文本放行——
  但它是对合成源码的内联重演，**不经过真实的文件枚举助手**：若 b23326OrchestrationProductionFiles
  失效（如目录解析错），主测试会空转通过且牙测试仍绿。鉴于守卫自述是编译期 import
  cycle 的前置哨兵（真回潮会编译失败），纵深仍在 → 记 P3-5。
- `git diff -- codegraph/` 为空：**零改动**，符合「刀3 才动」。
- `b23317_boundary_test.go` 与 codegraph/target.json 均零改动，棘轮断言原样（读
  target.json 冻结值，数据未动故仍绿）。plan 刀1 步骤 8 字面列了「棘轮更新」未在本
  区间执行——与 spec §范围3（target.json 重棘轮归刀3 图对账）一致，是合理顺延，
  但刀3 必须兑现 → 记 P3-6。

## 6. 门槛复跑 —— 全绿

| 门槛 | 结果 |
|---|---|
| `go build ./...` @3cc71fcf7 | OK |
| `go vet ./internal/agentd ./internal/orchestration ./cmd` | OK |
| `go test -p 1 -count=1 ./...` 第 1 遍 | 62 包 ok，0 FAIL（exit 0） |
| `go test -p 1 -count=1 ./...` 第 2 遍 | 62 包 ok，0 FAIL（exit 0），无 flake，无需隔离复跑 |
| `go list -deps ./internal/orchestration \| grep internal/agentd` | 零输出（exit 1） |
| 刀1 提交点 e2941e7ae（git archive 独立目录） | `go build ./...` OK；`go test -p 1 -count=1 ./internal/agentd ./internal/orchestration ./cmd` 全 ok |

## 7. 范围蔓延 —— 未发现

83 文件逐一归账：cmd 2 个（前缀+断言串）、agentd 31 个、orchestration 43 个、
workspace 3 个与 executor/turn/frames.go 1 个（均为迁移直接牵连的**注释**更新）、
docs 3 个（本卡声明文档）。preview 族、B233.19/.20 适配器（NewMirrorTaskSource 等）、
proto、其他包零触碰；cmd/agentd.go:190-196 preview 装配段与 :282 mirror 装配行原样。

---

## 发现清单

### P1（必须修才能合）

无。

### P2（应修）

无。

### P3（记录性）

1. **注释失实**：`internal/orchestration/regression_round2_test.go:63`（B233.26 注释）
   称「真 Server 回程由 gateway_http_test.go 覆盖」——gateway_http_test.go 仅 gc/reclaim；
   真 Server reply 覆盖在 `internal/agentd/server_test.go:338-1165`（未改动）。建议改为
   指向 server_test.go。覆盖本身无损失。
2. **TestMain 迁移的日志面变化**：orchestration 测试二进制日志被静音（失败时诊断
   信息减少）；agentd 二进制失去静音（coordinator_sessionref resolver 的 slog.Default()
   日志现打 stderr）。无断言影响。建议在刀4 实现记录登记该副作用。
3. **TOCTOU 窗口**：`internal/agentd/ptyreclaim_test.go:139-146` Serve 重绑探活端口的
   竞争窗口已如实注释；属跨包可见性约束下的不可避免选择。保持现状即可，若 CI 出现
   偶发失败优先怀疑此处。
4. **coordinator_home e2e 组合形态变化**：单流「字面 ~→真 Prepare→真 runner→子进程」
   拆为两段缝契约测试（orchestration/coordinator_home_test.go:47 供给断言全保留 +
   agentd/coordrunner_test.go:73 传递断言），组合损失经判定为纯函数值拼装、无逻辑
   可错面。判据已登记于 coordinator_sessionref.go 文件头；刀4 实现记录请引用。
5. **守卫牙测试为合成重演**：`b23326_direction_guard_test.go:71-99` 未把变异源喂给
   真实枚举助手，守卫存在「枚举失效即空转」的理论洞（编译期 cycle 兜底仍在）。可
   接受；若要加强，牙测试改为对 t.TempDir 伪造文件树跑 b23326OrchestrationProductionFiles
   同款扫描。
6. **棘轮更新顺延**：plan 刀1 步骤 8 列的「b23317_boundary_test.go 棘轮更新」未在本
   区间执行（与 spec §范围3 归刀3 一致，target.json 未动故测试仍绿）。刀3 图对账时
   必须兑现：d_gateway→d_orchestration 语义反转 + Interfaces:["OrchestrationClient"]
   归属更新 + legacyBudgetNote 改写。

## 附：审计方法与命令

- 十文件基线比对：`git show 4f66f42df:<file>` vs 现文件，忽略 package 行全文 diff；
- 符号面：两提交 agentd+orchestration 顶层导出声明集合三向对比 + 接收者方法集合对比；
- 测试守恒：`git grep '^func Test' <commit>` 求差；
- 死代码：全仓 grep 逐符号零引用取证；
- 门槛：§6 表所列命令实跑（含刀1 提交点 git archive 至 /tmp 独立构建，未触碰仓库）。
