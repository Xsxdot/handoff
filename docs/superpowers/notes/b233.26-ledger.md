# B233.26 刀3 阶段二 执行台账

工作树 `cards/B233.26-charter-2`，有效基线 `cards/B233-residual`。本节点只做阶段二
（best/target/diffs 重锚 + 冻结测试同步 + 交付说明），不改 baseline.json、不改生产 Go。

## 事实与命令（按发生顺序）

1. `git status` 起点：工作树 clean @ `92995010`（阶段一已由协调者抢救提交）。
2. 基线规模 `python3` 读 `codegraph/baseline.json`：5299 nodes / 338 containers /
   6618 edges / implements 65 / lifecycle 133 / projections 453 / packages 89 / domains 26。
3. 起点实况 `codegraph --repo . check`：fails=6（dead-contract + dead-entry
   d_orchestration→d_gateway、dead-interface d_gateway→d_orchestration、
   2× new-direction d_orchestration→d_scheduling、over-budget d_orchestration→d_execution 40/27）。
   起点 `validate`：2 issues（`[cards-B233.4-charter]` 悬空 `n_agentd_GitProbe`、
   `[cards-B233.5-charter]` 修改节点 `m_agentd_DispatchReq` 不在基线）；0 失鲜。
4. `best.json` 改登记 4 对容器：`k_agentd_Hub/Shutdown/pullTracker/coordinatorHomeSupplier`
   → `k_orchestration_*`（d_orchestration）；删 best 中对应 4 个死 id。
5. `target.json`：删 `d_orchestration→d_gateway` 整条；`d_gateway→d_orchestration`
   删 interfaces、预算 83→76、note 改写；新增 `d_orchestration→d_scheduling`（预算 0，
   entries `scheduling（包级函数）`）；`d_orchestration→d_execution` 补 7 条执行域公开
   entries（预算 27 不变）；`d_orchestration→d_policy` 补公开 entries（预算 16 不变）。
   中间读数：check fails 6→2（只剩两条 over-budget）→0。
6. 冻结测试先红：`go test -p 1 -count=1 ./internal/agentd -run 'B23317'` →
   `--- FAIL: TestB23317FacadeBudgetsStayRatchet ... d_gateway→d_orchestration 的
   interfaces 缺 "OrchestrationClient"`（确认是断言红，非编译红）。
7. 改冻结表 Budget 76、去 Interfaces 锁 → `go test ... -run 'B23317|B23326'` ok。
   变异：Budget 76→75 → 复红（`配额 77 高于冻结值 76` 类断言），Revert 后复绿；
   再次以真实值 76 跑变异 76→75 复红、revert 复绿。牙齿确认。
8. 视图重锚：`cards-B233.4-charter` 删 1 条吸收边（`→ n_agentd_GitProbe`）；
   `cards-B233.5-charter` 删 `nodesModified.m_agentd_DispatchReq`（已随迁为
   `m_orchestration_DispatchReq`）。7 视图悬空引用复扫=0。
9. 终态验收（本回合实跑）：
   - `go build ./...` → BUILD OK（exit 0）
   - `go test -p 1 -count=1 ./internal/agentd -run 'B23317|B23326'` → ok
   - `codegraph --repo . check` → fails:0
   - `codegraph --repo . validate` → issues:None edgeIssues:None
   - `codegraph --repo . validate --stale` → issues:None stale:None unscanned:0
10. 本节点触碰文件：`codegraph/best.json`、`codegraph/target.json`、
    `codegraph/diffs/cards-B233.{4,5}-charter.json`、
    `internal/agentd/b23317_boundary_test.go`、
    `docs/superpowers/notes/b233.26-scan-report.md`、本台账。
    `codegraph/baseline.json` 与生产 Go 零改动。

## 提交事实（历史读数）

命令与原始输出（提交当时的读数）：

```
$ git commit -m "graph(B233.26): 刀3 阶段二——best 容器重登记与 target 棘轮反转"
[cards/B233.26-charter-2 b762e899] graph(B233.26): 刀3 阶段二——best 容器重登记与 target 棘轮反转
 7 files changed, ...
```

`b762e899` 为首次提交 hash；按台账纪律随后 amend 一次把本台账收进同批提交，
amend 会换 hash——收口判据是工作树干净，不是文件内 hash 等于 HEAD。

## 未验证 / 存疑

- 阶段一 baseline.json 未重跑（协调者明令禁止；`validate --stale` 0 失鲜作为未坏的替代判据）。
- 本卡为图对账（docs/test），无生产行为变更；刀4（实现记录/roadmap）不在范围。
