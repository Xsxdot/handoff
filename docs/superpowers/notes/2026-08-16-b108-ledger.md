# B108 ledger：Reveal in Finder（本机半边）

**执行者**：handoff d1284446-63f1-4619-b3f4-b90c948758a4（subagent 驱动，本人不亲自改码）
**分支**：feat/b108-reveal-in-finder
**计划**：docs/superpowers/plans/2026-08-16-b108-reveal-in-finder.md
**Spec**：docs/superpowers/specs/2026-08-16-b108-reveal-in-finder-design.md
**基线 commit（分支起点）**：024fda04

## 执行记录

（每 task 完成、每轮修复各追加一行，含 commit 范围。）

- Task 1 完成（0d3f2f80）：能力位 reveal_supported 上线。StatusResp + Machine 双结构体加 `RevealSupported *bool`，reveal.go 建 `var revealSupportedOS`，handleStatus/localMachine 装配、fillFromStatus 搬运，契约夹具同步（StatusResp.json 机械再生成）。审查双 PASS。commit 范围：internal/proto/status.go、projects.go、contract_fixture_test.go、internal/agentd/reveal.go、server.go、machines.go、machines_test.go、web/src/api/testdata/StatusResp.json。
- Task 2 完成（c0e73f68）：POST /api/workspaces/reveal 端点。revealOpener 注入缝 + isLoopbackAddr + revealTarget（EvalSymlinks 前缀校验，附 B107 红线不适用论证）+ handleWorkspaceReveal（machine→平台→回环→白名单顺序），路由不带 forwardIfRequested。8 条用例全 PASS，全量 go test 30 包全绿。审查双 PASS（变异实测：删 machine 块/删回环判/删逃逸守卫均能咬红）。commit 范围：internal/agentd/reveal.go、reveal_test.go、server.go。
- Task 3 完成（ffcc0f56）：前端能力位 + usePtySupport 泛化为 useMachineCaps。types 补 reveal_supported（Machine 三态/StatusResp 布尔），client 加 revealInFinder（签名无 machine 参数），hook 拆双表仍单次 fetch，Shell 两处调用点改名，contract.test 补断言。vitest 49 文件 497 用例全绿、tsc 0 error、无 usePtySupport 残留。审查双 PASS。commit 范围：web/src/api/types.ts、client.ts、contract.test.ts、web/src/app/data（useMachineCaps.ts/.test.ts 改名）、shell/Shell.tsx。
