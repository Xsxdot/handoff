# B108 ledger：Reveal in Finder（本机半边）

**执行者**：handoff d1284446-63f1-4619-b3f4-b90c948758a4（subagent 驱动，本人不亲自改码）
**分支**：feat/b108-reveal-in-finder
**计划**：docs/superpowers/plans/2026-08-16-b108-reveal-in-finder.md
**Spec**：docs/superpowers/specs/2026-08-16-b108-reveal-in-finder-design.md
**基线 commit（分支起点）**：024fda04

## 执行记录

（每 task 完成、每轮修复各追加一行，含 commit 范围。）

- Task 1 完成（0d3f2f80）：能力位 reveal_supported 上线。StatusResp + Machine 双结构体加 `RevealSupported *bool`，reveal.go 建 `var revealSupportedOS`，handleStatus/localMachine 装配、fillFromStatus 搬运，契约夹具同步（StatusResp.json 机械再生成）。审查双 PASS。commit 范围：internal/proto/status.go、projects.go、contract_fixture_test.go、internal/agentd/reveal.go、server.go、machines.go、machines_test.go、web/src/api/testdata/StatusResp.json。
- Task 2 完成（c0e73f68）：POST /api/workspaces/reveal 端点。revealOpener 注入缝 + isLoopbackAddr + revealTarget（EvalSymlinks 前缀校验，附 B107 红线不适用论证）+ handleWorkspaceReveal（machine→平台→回环→白名单顺序），路由不带 forwardIfRequested。8 条用例全 PASS，全量 go test 30 包全绿。审查双 PASS（变异实测：删 machine 块/删回环判/删逃逸守卫均能咬红）。commit 范围：internal/agentd/reveal.go、reveal_test.go、server.go。
- Task 3 完成（ffcc0f56）：前端能力位 + usePtySupport 泛化为 useMachineCaps。types 补 reveal_supported（Machine 三态/StatusResp 布尔），client 加 revealInFinder（签名无 machine 参数），hook 拆双表仍单次 fetch，Shell 两处调用点改名，contract.test 补断言。vitest 49 文件 497 用例全绿、tsc 0 error、无 usePtySupport 残留。审查双 PASS。commit 范围：web/src/api/types.ts、client.ts、contract.test.ts、web/src/app/data（useMachineCaps.ts/.test.ts 改名）、shell/Shell.tsx。
- Task 4 完成（06b6e391）：FileTree 四分支。revealReason 按 machine→hostname→平台位→放行顺序判，isLoopbackHost 模块级 helper（SSH 转发边缘如实注释），revealEntry 失败原文走 opError 面板，Shell 传 `caps.reveal('')`。六条新用例（含 null 放行）替换旧两条。vitest 49 文件 501 用例全绿、tsc 0 error、vite build 通过。审查双 PASS。commit 范围：web/src/app/files/FileTree.tsx、FileTree.test.tsx、shell/Shell.tsx。
- Minor 记账（终审 triage）：① FileTree.test.tsx 的 stubHostname restore() 未放 try/finally 或 afterEach，断言中途失败会留下被 stub 的 hostname 污染后续用例（当前用例顺序不碰撞）；② isLoopbackHost 的 127.0.0.1/::1/[::1] 三支无直接用例，只测了 localhost 与非回环 IP。

## 变异自验输出（Task 5）

四条变异，每条改→跑→还原，逐一贴出实际输出：

- 变异 1（删 machine 拒绝块）→ 期望 TestRevealRejectsMachine 红。实际：RED，`--- FAIL: TestRevealRejectsMachine`，断言行 `reveal_test.go:103: 状态码 200，期望 400；body={"ok":true}`（请求被放行且真执行了 open）；还原后 GREEN
- 变异 2（删回环判定块）→ 期望 TestRevealRejectsNonLoopback 红。实际：RED，`--- FAIL: TestRevealRejectsNonLoopback`，断言行 `reveal_test.go:118: 状态码 200，期望 409；body={"ok":true}`；还原后 GREEN
- 变异 3（删 HasPrefix 越界守卫）→ 期望 TestRevealSymlinkEscape 红。实际：RED，`--- FAIL: TestRevealSymlinkEscape`，断言行 `reveal_test.go:175: 状态码 200，期望 400；body={"ok":true}`（树外符号链接被放行）。注：同改下 TestRevealPathEscape 仍 PASS——`../../etc/hosts` 撞的是 EvalSymlinks 的「路径不存在」分支而非守卫，两条红线不重叠，符合设计；还原后 GREEN
- 变异 4（revealSupportedOS 恒真）→ 期望 TestRevealUnsupportedPlatform 红。实际：套件仍全绿（变异 4 特例）。原因：测试体先把 `revealSupportedOS = false` 覆盖掉，恒真初值不影响 handler 读到 false。关键确认成立——变异版本下 TestRevealUnsupportedPlatform 中 `revealSupportedOS = false` 之后**仍**触发 501（该用例需 501 才 PASS，它 PASS 了），证明实现读的确实是这个 var，不是实现读错 var、也不是用例没咬住；还原后 GREEN

## 全量回归（Task 5）

- `go build ./...` / `go vet ./...` / `gofmt -l .`：全通过，`gofmt -l` 无文件
- `go test -count=1 ./...`：30 包 ok，0 个 FAIL（输出已落盘 /tmp/b108_gotest.txt）
- 前端 `npx vitest run`：49 files / 501 tests 全过；`npx tsc --noEmit` 0 error
- **实现者没做的事**：`open -R` 的真机弹窗从未被验证过（注入缝换掉了它），这一条留给审核者的真机复验（spec §7 第 1/2 条）。如实写，不要让 ledger 读起来像「全都验过了」。

## 终审（整分支）

- 终审（相对 024fda04 全 diff）：**PASS 无需修复波**。机械判据四条全过（无 fmt.Print、无 usePtySupport 残留、无「暂未实现」reveal 文案、go build/vet/gofmt/全量 test 30 包 0 FAIL、前端 49 文件 501 用例 + tsc 0 error）。spec §1-§7 逐条确认到位；§6 不做的两项确认未误做；§7 真机复验明确留给审核者（`open -R` 弹窗从未验证，注入缝测不到）。
- Minor triage：① stubHostname restore 未入 afterEach → **采纳**，修复轮（70af2851，还原统一进 afterEach，用例去掉手动 restore）→ 范围复审 PASS；② isLoopbackHost 的 127.0.0.1/::1/[::1] 无直接用例 → **搁置**（平凡字符串等值，风险自限）；观察项：MachinesResp.json 夹具本机行缺 reveal_supported（无断言消费它，不改也成立）→ 搁置。

## 审核者复验变异（ac561bd9）

审核者独立复验打了两条实现者没打过的变异，各暴露一个缺口，均已补用例并验证必红：

**缺口 1：isLoopbackAddr 的「判不出来就拒绝」零覆盖。**
把 `return ip != nil && ip.IsLoopback()` 变异成 `return ip == nil || ip.IsLoopback()`（fail-open）后，原测试套仍全绿——现有两条回环用例的 IP 都能被 ParseIP 解析，走不到 `ip == nil` 分支。补 `TestRevealRejectsUnparseableRemote`（空串 / "@" / "unix" 三种不可解析 RemoteAddr 断言 409 且 open 未执行）。补后重跑该变异：
```
reveal_test.go:151: RemoteAddr="" 状态码 200，期望 409；body={"ok":true}
--- FAIL: TestRevealRejectsUnparseableRemote
```
必红。还原后 GREEN。此性质与 B107 的 searchDefaultLimit 同属「注释宣称的安全性质，唯一没被测到的分支」。

**缺口 2：路由注册零覆盖。** 原 8 条用例全直调 handler，把 server.go 的路由注册行整行注掉后测试仍全绿。补三条走 env.ts.URL 完整路由栈（mux + hostGuard + auth）的用例：`TestRevealRouteHappyPath`（200 + 注入缝收到正确路径）、`TestRevealRouteRejectsMachine`（400）、`TestRevealRouteRejectsGet`（405 方法路由）。补后重跑「注掉注册行」变异：
```
reveal_test.go:248: 状态码 = 404, want 200；体 = 404 page not found
reveal_test.go:265: 状态码 = 404, want 400；体 = 404 page not found
reveal_test.go:278: 状态码 = 404, want 405；体 = 404 page not found
```
三条全红。还原后 GREEN。

**ledger 修订**：原「isLoopbackHost 三支无直接用例 → 搁置（真边界在服务端 net.IsLoopback 已实测）」的理由被缺口 1 证伪（服务端那条实测后并非覆盖到底），搁置理由改写为「平凡字符串等值，风险自限」。

复验后全量：`go test -count=1 ./internal/agentd/` ok（38s 全绿）、build/vet/gofmt 干净。commit 范围：internal/agentd/reveal_test.go（+84/-5）。
- 交付 commit 列表（新→旧）：70af2851（修复轮）、144cb10b（ledger）、3ddfe3c5（ledger）、06b6e391（Task 4）、5f0108b3（ledger）、ffcc0f56（Task 3）、246756b0（ledger）、c0e73f68（Task 2）、1f466897（ledger）、0d3f2f80（Task 1）。基线：024fda04。
