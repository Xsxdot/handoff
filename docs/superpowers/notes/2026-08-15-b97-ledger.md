# B97 事件与状态失配对账扫描 —— 执行 ledger

> 本 ledger 由执行协调者在任务执行期间逐项追加，恢复现场以本文件 + `git log` 为准。
> 分支：`feat/b97-state-event-mismatch`（基线 `e98d1ed6`）

## 任务清单与进度

| task | 内容 | 状态 | commit |
|---|---|---|---|
| 1 | 根因：Stop 与 reconcileExecutorGone 改成「先迁状态、后追加事件」 | 完成 | 604d9bdc |
| 2 | `mismatchVerdict` 纯函数与六条护栏用例 | 完成 | 9f2572df |
| 3 | `scanStateMismatch` 接进看门狗并执行迁移（transit + 审计） | 完成 | 0a91de86 |
| 4 | 全量回归 + 护栏↔用例对照 + 未做事项 | 待做 | — |

## 修复记录

- Task 1 完成（604d9bdc，双裁决 PASS）：Stop 与 reconcileExecutorGone 反转顺序为「先迁状态、后追加事件」；两条新测试 `TestStopTransitsBeforeEvent` / `TestReconcileTransitsBeforeEvent` 用 store 事件钩子钉住顺序。全包 `./internal/agentd/` 回归 PASS，无既有用例因顺序变化变红。
- Task 2 完成（9f2572df，双裁决 PASS）：`mismatchVerdict` 纯函数 + 六条护栏用例（终态拆两态 6a/6b）。四护栏（非终态 / 最新事件是 failed / ≥minAge / 启动后事件）各被用例钉住。
- Task 3 完成（0a91de86，双裁决 PASS）：`scanStateMismatch` 接入看门狗 tick（scanStalled 之后），transit 回调注入（生产 `mgr.MismatchTransit()` 包装 `mgr.transit`）；每次动手 Warn（task/seq/state/event_age），迁移失败只记 Error 不重试；迁移后追加 progress 审计含原始 seq；cmd/agentd.go 接线（wdStart + `agentd.MismatchScanMinAge` + `mgr.MismatchTransit()`）。两条新测试钉住「迁移走 transit（工单作废）+ 审计含 seq」与「turn_failed+waiting_review 不动」。既有 4 处 runWatchdog 测试调用适配。
- 终审修复波（6b50a23c，范围复审 PASS）：F1 判据「年龄恰等于 minAge」边界用例（变异 `>=`→`>` 可咬住）；F2 测试注释措辞修正；F3 watchdog.go 陈旧注释 failed→turn_failed。终审结论 CLEAN，无必须修项，三项 triage 发现全部一次修完。
- B97 修复（审核复验）：审核复验发现 `TestScanStateMismatchTransitsAndAudits` 手搓闭包 `transit := func(...) { return m.transit(...) }` 复刻生产接线，`Manager.MismatchTransit()` 适配器本身无任何用例覆盖（变异函数体为裸 `UpdateTaskState` 全绿，判据 §6.3 对生产代码不成立）。已删掉复制品闭包，直接用生产接线 `m.MismatchTransit()`（与 cmd/agentd.go:193 同一接线点）；变异自验确认缺口闭合——变异（绕过 B63 终态收口）→ 用例变红，还原 → 变绿。关键输出：变异 `--- FAIL: TestScanStateMismatchTransitsAndAudits (0.01s)`（`watchdog_test.go:570: 失配任务迁移后挂起工单应被作废（走 transit 收口），实际 1 张`）；还原 `--- PASS: TestScanStateMismatchTransitsAndAudits (0.01s)`。

## Minor 发现记账（不进回路，终审 triage）

- **M1（Task 2 审查提出）**：判据 3 的 `>=` 若被变异成 `>`，现有用例（10s/60s）不会变红——缺一条「事件年龄恰等于 minAge=30s」的边界用例。实现本身用的是 `>=`，正确。→ **终审修复波已修（F1，6b50a23c）**。
- **M2（Task 3 审查提出）**：`TestScanStateMismatchLeavesHealthyTaskAlone` 的注释把 waiting_review 称作「健康终态」——它其实不是状态机的终态（终态只有 completed/failed），判 false 的真正来源是 `latest.Type != failed`。措辞略松，测试逻辑正确。→ **终审修复波已修（F2，6b50a23c）**。

## 护栏与用例对照（Task 4 填充）

| 护栏 | 判据 | 对应用例 |
|---|---|---|
| 1 | 最新事件是 `failed`（不是历史上出现过） | `TestMismatchVerdict` 用例 1（正向命中）与用例 3（latest 是 progress、历史有 failed → false）、用例 4（latest 是 turn_failed → false） |
| 2 | 事件年龄 ≥ 30s（防抢，生产 minAge=mismatchScanMinAge） | `TestMismatchVerdict` 用例 2（事件仅 10s → false）；正向命中由用例 1（60s → true） |
| 3 | 事件产生于本次 agentd 启动之后 | `TestMismatchVerdict` 用例 5（failed 事件产生于 startedAt 之前 → false） |

三条护栏一条不缺。扫描集成层另有 `TestScanStateMismatchTransitsAndAudits`（真实执行 mismatchVerdict 全护栏后迁移）与 `TestScanStateMismatchLeavesHealthyTaskAlone`（turn_failed+waiting_review 健康态不动）。

## 明确没做（如实记录）

- 本扫描兜不住「续接回合正常完成但结果被吞」（库里只有 stalled，扫描无从知道回合其实跑过）——**它是保险丝不是根治**（写入 `mismatchVerdict` 注释）。
- 顺带不做 B101 / B105 / B107 / B108（plan 全局约束）。
- 不做存量数据迁移：B100 之前历史里合法的 `failed`+`waiting_review` 靠「启动后事件」护栏保护，扫描不碰它们，也不主动改写存量。

## 自检结论（Task 4）

- `go build ./...`：通过。
- `go vet ./...`：无输出。
- `gofmt -l internal cmd`：无输出。
- `go test -count=1 ./...`：30 包全 ok（含 internal/agentd 37.5s）。
- 验收点逐条核对：
  1. 判据六用例全绿（含「事件仅 10s 不动」「turn_failed+waiting_review 不动」「启动前事件不动」「终态不动」）。
  2. 迁移走 transit：`TestScanStateMismatchTransitsAndAudits` 断言挂起工单已作废（变异为裸 UpdateTaskState 即红）。
  3. 审计事件：迁移后有一条 progress，文本含原始事件 seq。
  4. 根因两处顺序已反转：`TestStopTransitsBeforeEvent` / `TestReconcileTransitsBeforeEvent` 各钉一条（事件落库瞬间状态已就位）。
