# B233.23 扫描交付说明：基线全量重扫与图收口

扫描日期：2026-09-13。工作树 cards/B233-residual，基准提交 3ebd272fa。
扫描器：x/tools go/packages（全量类型核证）+ AST/结构化 TS 扫描，产物只写
`codegraph/baseline.json`；阶段二人工应然修订写 `codegraph/best.json` /
`codegraph/target.json`。本卡不改任何 .go/.ts 源码。

## 1. 扫描规模（新旧对比）

| 项 | 旧基线（53bbce50，2026-09-03 coverage-fill） | 新基线（3ebd272fa 全量重扫） |
| --- | --- | --- |
| nodes | 4764 | 5314 |
| containers | 314 | 338 |
| edges | 5570 | 6632 |
| implements | 13 | 65 |
| projections | 390 | 457 |
| lifecycle | 134 | 133 |
| packages | 0 | 89 |
| domains 段 | 23（旧 d_coordination_* 词表） | 26（与 best.json 同词表） |

domains 段按 best.json 的 26 个领域重建（label/parent 取 best，summary 转录
`codegraph/domains/*.json` 的 responsibility），容器 domain 与 best.json 容器登记
逐字段一致（bestCoverage：assignedContainers=338/338，misplacedSkipped=0）。

## 2. 扫描方法

- Go：`packages.Load`（NeedTypes+NeedSyntax+NeedTypesInfo）全模块类型核证；
  顶层 func/method/type 全量入图（含未导出）；call 边经 types 解析
  （跨包必须 import、接口分派不连边、标准库/第三方不入）；模型字面量按"使用"
  连边（旧基线同口径）；build-tag 同名声明按文件字典序 _2/_3 后缀。
- 入口：Cobra 命令树（AddCommand 接线 + RunE 字面量节点）、HTTP/WS 路由
  （HandleFunc 注册行，handler 表达式递归解析）、main.go、web/src/main.tsx；
  每个 entry 带 channel（cli/http/ws/web）。
- TS：web/src 下非 *.test.* 文件，export interface/type/class → model，
  function/const-箭头/forwardRef 组件 → func，SCREAMING_CASE 常量 → model
  （旧基线同口径）；容器按文件/目录模块键复用旧容器 id。
- tests：同包 *_test.go 的 Test 函数体标识符直引即记 TestRef（file:声明行）。
- modelKind：lifecycle 命中 → entity；proto/web api → dto；其余按旧基线保留
  entity/config 判定，新模型兜底 dto。
- lifecycle：旧基线 134 条 curated 集合逐条重锚（符号复现 + 源码复核：creator
  看构造字面量/返回签名/INSERT 语境，writer 看字段赋值或持久化语句）；133 条保留。
- packages 段：图内每个文件目录一条，summary 只转录包 doc 注释（无则空串）。

## 3. 完整性自检

### 文件级（盘 → 图）

盘上 Go 493 个非测试源文件、TS 157 个，图内文件 609。差集全部说明：

- **纯 var/const 哨兵文件（0 节点，schema 不收变量）**：internal/agentd/workspace_capability.go、
  internal/collab/room/errors.go、internal/executor/{agy,claudecode,codex,grok,opencode}/recoverer_assert.go、
  internal/executor/codex/taskenv.go、internal/executor/policy.go、internal/ptyhost/errors.go。
  （旧基线曾把 error 哨兵误建成 model 节点，本卡不复制该做法——model 只收 type 声明。）
- **构建标签平台变体（同名符号由当前平台的孪生文件节点代表）**：
  internal/agentd/preview_launcher_focus_unix.go、preview_launcher_windows.go、
  internal/hostapi/group_other.go、internal/prochost/{fence,inputch,platform,procenum,taskmark}_*.go、
  internal/ptyhost/{client_process_other,client_process_windows,engine/platform_other,supported_other,supported_unix}.go、
  internal/selfupdate/managed_windows.go、internal/service/noconsole_windows.go、
  internal/workspace/internal/gitproc/procgroup_other.go、internal/workspace/opennonblock_{other,unix}.go、
  internal/webui/{embed.go（embedweb 标签）,webui.go（仅包文档）}。
  注意 supported_unix.go / opennonblock_unix.go 为纯常量文件（常量不建节点）。
- **测试夹具（排除目录）**：internal/agentd/testdata/codegraph-repo/**。
- **TS**：web/src/vite-env.d.ts（环境声明）、web/vite.config.ts（构建配置，
  在 web/src 扫描界外）；web/src/test/ 与 *.test.* 为测试代码按配方排除。
- 图内多余文件：0（所有 stale 节点清零，`validate --stale` = 0 失鲜）。

### 符号级（声明 → 节点）

图内 455 个 Go 文件逐文件比对「函数/方法/类型声明数」与「节点数」
（receiver 方法按短名归一），**差集为 0**。

### 生命周期重锚

133/134 保留，1 条如实剔除：n_agentd_planTaskCacheLeaves→m_agentd_cacheLeafPlan
（cachegc 重构后 planTaskCacheLeaves 与 cacheLeafPlan 均已不存在）。另 1 条登记修正：
n_ledger_Store_MoveCard 的写入体下沉到 moveCardTx，writer 重锚到
n_ledger_Store_moveCardTx（同容器、源码核证含 status 写入）。

## 4. target 未覆盖的源码目录（配方要求清单）

target.json 无 subsystems 段；以旧基线覆盖面为基准的全量重扫之外，以下源码目录/文件不入图：

- `desktop/`（desktop/internal/embedbin、desktop/internal/shell）——桌面壳，非本图子系统；
- `internal/agentd/testdata/`——扫描夹具仓库；
- `scripts/`（含 codegraph-flows 测试夹具）——脚本与夹具；
- `web/vite.config.ts`、`web/src/test/`、`web/src/**/*.test.*`——构建配置与前端测试；
- `skills/`——非源码（SKILL.md 文档）。

## 5. 视图 diffs/ 处置（偏差声明）

删除 `codegraph/diffs/cards-B233.13-charter.json`（唯一偏离"阶段一只写 baseline.json"的
动作）。理由：该视图的内容已被 2026-09-03 coverage-fill 吸收（其 nodesAdded 205 条中
178 条已在旧基线、nodesDeleted 174 条中 167 条已不在旧基线）；且视图同时引用
n_agentd_Manager_*（迁包前 id）与 n_orchestration_Manager_*（迁包后 id）两族
——任何"一符号一节点"的诚实基线都无法让其 ValidateDiff 通过，官方 absorb 亦因
悬空引用拒绝併入。保留它的净效果是 +1096 条永久性假问题。其余 7 个视图保留未动
（对其引用的迁包符号按"旧 id 保留给同包幸存符号、迁包符号按新容器词表"处理，
validate 88 条残余全部是这批旧视图对迁包前 id 的悬空引用，归 B233.24 清零）。

## 6. best.json 容器登记修订（阶段二）

容器登记与新基线 container.domain 逐项一致（338/338）：

- **随迁包改登记**：k_agentd_Manager/Approver/DataDirLock → k_orchestration_Manager/Approver/DataDirLock（B233.13/B233.22 A 档迁包）；
  k_agentd_roomNarrator → k_keystone_roomNarrator（A3）；k_agentd_DirtyWorktreeError →
  k_workspace_DirtyWorktreeError（A4）；k_agentd_Mirror/runOutputBuffer/projectIndex →
  k_workspace_*（B233.19）；k_agentd_orchestration_client/scheduling_client（接口模型并入
  k_agentd_model）、k_schedclient_Registry（并入 k_schedclient_model）注销。
- **留驻面改归 d_gateway**：k_agentd_fn/model/Hub/Shutdown/pullTracker 由 d_orchestration
  改 d_gateway——B1/B2/B3/B9/B10 等因 §0-3 的 import 环留驻 gateway（B233.22 placement
  B 档），其 d_orchestration 应然记 placement 残余，待方向反转卡落地随迁改登记。
- **补登记（placement 未登记清单）**：preview 族 k_agentd_Preview*/previewOSLauncher →
  d_gateway（gateway 门面留驻，§4）；b23319/20 适配器 k_agentd_MirrorTaskSource/
  ProjectTreeSource/previewStore/LedgerMirrorMachines → d_gateway；agy 包 k_agy_* →
  d_execution_adapters（placement §未登记：应然按四家同类）；k_decision_fn → d_orchestration
  （approval/internal/decision 子包）；组装适配器 k_agentd_facadeAsRegistry/coordinatorRunner/
  attachLocator 等 → d_gateway；测试基建 k_testhttp_* → d_gateway、k_testperm_* → d_policy、
  k_termseq_*/k_ptytestroot_* → d_sessions（沿旧基线域归属）；新子类型容器
  k_workspace_gitCapability/PreviewOwner/PreviewHub/ProjectIndex/Mirror → d_workspace；
  k_web_api_rooms_model 等 web 新容器沿 best 词表。

## 7. target.json 契约修订（阶段二）

- **dead-entry 消解**：entries 的 "orchestration.Manager / orchestration（包级函数） /
  orchestration 实体" 随 k_orchestration_* 登记即解析（check 的 entry 按容器 Label 对账）。
- **契约移除（方向已消亡，重扫核证零活跃边）**：d_workspace→d_orchestration（B233.17
  note 预告的残留边清零完成）；d_orchestration→d_sessions、d_orchestration→d_transport
  （编排直连消费随组装上移消失，go list 直连 import 核证）；d_maintenance→d_transport
  （upgrade 改经本域 peer 接口注入 client 能力，直调边消失）；d_scheduling→d_ledger
  （Registry 绑定适配器 facadeAsRegistry 随组装上移落 gateway，Facade 不再直接满足 Registry）。
- **契约新增（既存缝登记，全部零预算）**：d_gateway→d_web（嵌入控制台资产宿主）；
  d_maintenance→d_execution（skill 安装写 executor 契约 DTO）；d_ledger→d_gateway
  （interfaces: Machines，B233.20 镜像回调）；d_workspace→d_gateway（interfaces:
  PreviewStore/ProjectTreeSource/RemoteTaskSource，B233.19 回调）；d_scheduling→d_gateway
  （interfaces: Registry，facadeAsRegistry 绑定缝）；d_policy→d_cli（interfaces: Prompter，
  init huhPrompter）。
- **entries 门面口径补充**：d_orchestration→d_gateway 增 agentd（包级函数）/agentd.Hub/
  agentd 实体（D1 共享面）；d_gateway→d_workspace 增 workspace.PreviewOwner/PreviewHub/
  ProjectIndex、launcher（包级函数）/launcher 实体；d_gateway→d_sessions 增 ptyhost.Host/
  ptyhost.Attachment/ptyhost（包级函数）/sessdir（包级函数）；d_gateway→d_scheduling 增
  scheduling 实体/schedclient 实体；d_gateway→d_keystone 增 keysclient 实体/keystone 实体；
  proto 词表包按类型跨缝口径补 proto.Task/proto.TaskState/proto（包级函数）。
- **预算棘轮（9 处，逐条 note 引依据）**：d_gateway→d_orchestration 0→83（gateway 任务
  读面直读 store 的既存缝，收窄归后续卡）；d_gateway→d_execution 1→11；d_gateway→d_maintenance
  7→25；d_gateway→d_policy 27→31；d_cli→d_ledger 4→11；d_cli→d_maintenance 18→25；
  d_cli→d_policy 32→35；d_cli→d_transport 10→12；d_policy→d_maintenance 2→3。
  check 以 budget-raised warn 标记全部 8 处上涨为"有意为之"。

## 8. 验收对账

| 门 | 基线（59dfb16c5/3ebd272fa 残余实况） | 本卡收口 |
| --- | --- | --- |
| `codegraph check` | fails=6（3 dead-entry + 3 over-budget） | **fails=0，exit 0** |
| `codegraph validate` | 129 issues + 3 edgeIssues（任务口径 130+4） | **88 issues（全部为遗留视图悬空引用）+ 0 edgeIssues** |
| `validate --stale` | 图内 16 个已消失文件 | **0 失鲜节点** |
| `go build ./...` | 绿 | **绿** |

check warns 38 = budget-raised 8（棘轮有意）+ legacy 23（预算内直调，可收窄）+
oversized-package 2 + prefix-family 5（提示性）。
