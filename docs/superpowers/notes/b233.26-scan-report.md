# B233.26 扫描交付说明：刀3 阶段二（D1 反转图对账）

扫描日期：2026-09-14。工作树 `cards/B233.26-charter-2`，有效基线分支
`cards/B233-residual`。阶段一全量重扫提交 `92995010`（基线上游 `161441b3`）；
本节点只做阶段二人工应然修订，写 `codegraph/best.json`、`codegraph/target.json`、
`codegraph/diffs/*.json` 与 `internal/agentd/b23317_boundary_test.go`，
不改任何生产 `.go`、不改 `codegraph/baseline.json`（`validate --stale` 证明阶段一产物
未坏：0 失鲜）。

## 1. 扫描规模（阶段一重扫对照）

| 项 | B233.23 旧基线（3ebd272fa） | B233.26 阶段一（`92995010`） |
| --- | --- | --- |
| nodes | 5314 | 5299 |
| containers | 338 | 338 |
| edges | 6632 | 6618 |
| implements | 65 | 65 |
| lifecycle | 133 | 133 |
| projections | 457 | 453 |
| packages | 89 | 89 |
| domains | 26 | 26 |

规模小幅下降源于 B233.26 刀1/刀2 的归域重构（十文件搬迁 + ManagerFactory 桥退役 +
`b23313_retained.go` 拆解为 `contracts.go`），非扫描遗漏。`validate --stale` = 0 失鲜，
`validate` = 0 issue（阶段一前 62 条边门控 + 148 失鲜清零的复验见 §5）。

## 2. 完整性差集

盘上非测试源码 644（Go 487 + TS 157），图内文件 610，差集 34 个文件，全部为
合法零节点情形（与 B233.23 §3 同一差集，本卡未新增漏建）：

- **纯 var/const 哨兵文件（0 节点，schema 不收变量）** 8 个：
  `internal/agentd/workspace_capability.go`、`internal/collab/room/errors.go`、
  `internal/executor/{agy,claudecode,codex,grok,opencode}/recoverer_assert.go`、
  `internal/executor/codex/taskenv.go`、`internal/executor/policy.go`、
  `internal/ptyhost/errors.go`。
- **构建标签平台变体（当前平台孪生文件代表节点）** 24 个：
  `internal/agentd/preview_launcher_focus_{darwin,windows}.go`、
  `internal/hostapi/group_other.go`、
  `internal/prochost/{fence,inputch,platform,procenum,taskmark}_*.go`、
  `internal/ptyhost/{client_process_other,client_process_windows,engine/platform_other,supported_other,supported_unix}.go`、
  `internal/selfupdate/managed_windows.go`、`internal/service/noconsole_windows.go`、
  `internal/workspace/internal/gitproc/procgroup_other.go`、
  `internal/workspace/opennonblock_{other,unix}.go`、`internal/webui/{embed,webui}.go`。
- **TS 声明/测试配置** 2 个：`web/src/test/setup.ts`、`web/src/vite-env.d.ts`。

图内多余文件（在图中不在盘上）：**0**（`validate --stale` 0 失鲜佐证）。
符号级差集沿用 B233.23 结论（本卡不重扫源码），阶段一重扫提交自陈差集为 0。

## 3. 容器重登记（best.json）

`best.json` 与基线 338 容器逐字段对账：best 缺失/多余均为 0，空容器入 best 为 0。
本卡只做 4 对随迁容器 id 的改登记（其余 best 条目按 B233.23 原样保留），对账后
`check` 的 `best-dangling`/`container-unplaced` 警告全清：

| 旧（B233.23 登记，死 id） | 新（本卡登记） | 领域 | 成员节点 |
| --- | --- | --- | --- |
| `k_agentd_Hub` | `k_orchestration_Hub` | d_orchestration | 8 |
| `k_agentd_Shutdown` | `k_orchestration_Shutdown` | d_orchestration | 4 |
| `k_agentd_pullTracker` | `k_orchestration_PullTracker` | d_orchestration | 5 |
| `k_agentd_coordinatorHomeSupplier` | `k_orchestration_coordinatorHomeSupplier` | d_orchestration | 1 |

**留驻 d_gateway（未改，实证仍留 gateway）**：`k_agentd_Server`(300 节点)、preview 族
`k_agentd_Preview*`/`previewOSLauncher`/`previewStatic`/`previewStore`、B233.19/.20
适配器 `k_agentd_MirrorTaskSource`/`ProjectTreeSource`/`LedgerMirrorMachines`、
`k_agentd_facadeAsRegistry`、`k_agentd_coordinatorSessionRefResolver`（spec §范围2
协调器 home 降级路径：resolver 留 gateway）。

**契约族随迁对账**：`OrchestrationClient`、`TerminalOutcome`（`orchestration_client.go`）
与 `DispatchReq`/`RecoverReport`/`ProgressPayload`/`CompletedPayload`/`FailedPayload`/
`ApproverDecisionPayload`/`TicketAnsweredPayload`（`contracts.go`）、
`TicketsVoidedPayload`（`ticketvoid.go`）在基线中已归 `k_orchestration_model`
（d_orchestration），best 由 `k_orchestration_model` 覆盖，无需新容器；即契约族已随
刀1 迁入提供方侧容器，本卡不再重复登记。

## 4. target.json 棘轮修订（阶段二）

### 4.1 消亡方向删除

- **删除 `d_orchestration→d_gateway`**（原 legacyBudget 2，entries
  `agentd（包级函数）/agentd.Hub/agentd 实体`）。反转 D1 后编排对 gateway 零 import
  （`go list -deps ./internal/orchestration` 不含 agentd），重扫零活跃边；
  `check` 报 dead-contract + dead-entry，按「消亡即删，不留死契约」删除。冻结测试
  `b23317FrozenContracts` 同步移除该方向（B233.25 移除 `d_workspace→d_orchestration`
  同法）。

### 4.2 方向语义反转：`d_gateway→d_orchestration`

- **interfaces `OrchestrationClient` 移除**：接口契约族迁入提供方 orchestration
  （`m_orchestration_OrchestrationClient` 归 `k_orchestration_model`），gateway 不再
  定义该使用方接口；`check` 原报 dead-interface（在 d_gateway 找不到该接口），删除
  interfaces 声明。
- **预算 83→76（只降不抬）**：重扫 raw 87（`store.Store` 59、orchestration 包级函数 8、
  `orchestration.Hub` 6、`orchestration.PullTracker` 6、store 实体/包级函数 6、
  orchestration 实体 2）；其中 10 条落已声明公开 entries（orchestration 包级函数 8 /
  实体 2），另源在 assembly 的 1 条免除，`check` 判定的直调为 **76**。方向语义随反转：
  旧 83 是「gateway 直读 store 的 legay 缝」，新 76 是 gateway 对编排实现与数据的
  真实依赖（含 hub/pullTracker 消费面）。注意 76 相对 B233.23 的 83 是**下调**，
  未抬配额抹绿；超出的边能写 entries 已写，消不掉的按实况如实记。`legacyBudgetNote`
  改写引 B233.26。
- **冻结测试**：`internal/agentd/b23317_boundary_test.go` 的
  `b23317FrozenContracts` 先见红（interfaces 断言 `d_gateway→d_orchestration 的
  interfaces 缺 "OrchestrationClient"`）→ 改冻结值为 Budget 76、去掉 Interfaces 锁 →
  见绿；变异自验（Budget 76→75）复红，证明牙齿在。

### 4.3 方向补齐：`d_orchestration→d_scheduling`

- **新增条目**（原无契约，重扫后 2 条活跃边）。`ReconcileSchedRunning`
  （`watchdog.go` 随十文件迁入 orchestration）直引编制域公开 helper
  `OccupancyCarrierKey`/`OccupancyMemberKey`（`internal/scheduling/occupancy.go`）。
  两条边均落 `scheduling（包级函数）` entries，直调预算 0（不再扩大）。

### 4.4 随迁超配的消解（不抬配额）

十文件迁入使两条方向的直引上涨，采用「写 entries 收纳入公开面」消化，预算不抬：

- **`d_orchestration→d_execution`**：直引 40 超预算 27（原 entries 仅
  `executor（包级函数）/executor 实体`）。将随迁文件直引的执行域公开 helper/实体
  （`turn（包级函数）`/`turn.FrameWriter`、`prochost（包级函数）`/`prochost.Admission`/
  `prochost.Lock`、`executor.Registry`、`hostapi（包级函数）`）按门面口径写成 entries，
  直调回到 0（预算 27 不变）。均属执行域公开能力面，非穿透内部。
- **`d_orchestration→d_policy`**：直引 17 超预算 16（超 1）。将随迁的
  `coordinator_home.go`（`config（包级函数）`/`config 实体`）与既有 `envfile`/
  `permgate` 公开面写成 entries，直调回到 0（预算 16 不变）。

### 4.5 对账门

`codegraph check` = **fails 0**（另 warns 36 = legacy 21 + budget-raised 8（B233.23
遗留，本卡未新增上涨）+ oversized-package 2 + prefix-family 5，均提示性）。

## 5. 视图重锚（codegraph/diffs/ 7 个）

7 个视图全部保持；本卡只修 B233.26 反转后暴露的两处悬空/过期引用（B233.24 已把
B233.23 重扫的 88 条悬空清零，本卡接续）：

| 视图 | 处置 |
| --- | --- |
| `cards-B233.4-charter` | `edgesAdded` 删 1 条 `n_orchestration_Manager_AssembleResultRef → n_agentd_GitProbe`：`n_agentd_GitProbe` 随刀1 消亡（GitProbe 垫片改直引 workspace），该边已被基线吸收为 `→ n_workspace_GitProbe`（基线在册），视图条目删。 |
| `cards-B233.5-charter` | `nodesModified` 删 `m_agentd_DispatchReq`：`DispatchReq` 随刀1 迁 `internal/orchestration/contracts.go` 并导出化（`m_orchestration_DispatchReq` 已在基线），旧 agentd 节点定义消亡，删除过期条目。 |
| 其余 5 个（B233.1-charter-2/B233.2/B233.3/B233.6/B358） | `validate` 边/节点悬空=0。review 另发现 B233.2 `nodesAdded` 键 `n_agentd_coordinatorHomeSupplier_Prepare` 未改指（validate 不校验视图节点键，故当时静默）。 |

`codegraph validate` = 0 issue、0 edgeIssue（原 2 条
`[cards-B233.4-charter]`/`[cards-B233.5-charter]` issue 清零）。未改任何源码凑绿；
不存在「迁包前后两族 id 并存、诚实基线无法 ValidateDiff」的视图，故无视图删除。

刀4 把 B233.2 该键改指 `n_orchestration_coordinatorHomeSupplier_Prepare`
（container `k_orchestration_coordinatorHomeSupplier`，file
`internal/orchestration/coordinator_home.go:40`），与当前基线 id 对齐。

## 6. 验收对账

| 门 | 结果 |
| --- | --- |
| `go build ./...` | 绿（exit 0） |
| `go test -p 1 -count=1 ./internal/agentd -run 'B23317\|B23326'` | ok |
| `codegraph check` | fails=0（exit 0） |
| `codegraph validate` | 0 issues / 0 edgeIssues（exit 0） |
| `codegraph validate --stale` | 0 失鲜、0 issue、unscannedEntries=0 |

生产 Go 除冻结测试 `b23317_boundary_test.go` 外零改动；`codegraph/baseline.json` 零改动。

## 7. 提交台账（历史读数）

- 阶段一 `92995010`：`graph(B233.26): 刀3 阶段一全量重扫 baseline`——五条 D1
  domOverride 删除、`-repo` 绝对路径、lifecycle 大小写重锚；产物 5299/338/6618。
- 阶段二（本节点）：best 4 容器重登记 + target 棘轮反转与方向补齐 + 视图重锚 2 处 +
  冻结测试同步 + 本说明。命令与原始输出见 `git show` 提交体与上表。
