# B369.1（S1）配对 wire 回归与孪生样本 —— 实现计划

- 卡号：B369.1（父卡 B369；拆解序号 S1）
- 有效基线分支：`cards/B369-charter-4`（本卡合并目标）
- 冻结 contract：`docs/superpowers/specs/b369-contract.md` @ `6f10ba7c`
- 已拍板拆解：`docs/superpowers/specs/b369-breakdown.md` @ `4ab6415a`（P1–P6 全案 A）
- 上游 spec：`docs/superpowers/specs/2026-09-13-mobile-app-design.md`（状态：已批准）
- 图视图：`codegraph/diffs/cards-B369-charter.json`（view=`cards-B369-charter`，base `977720e2`）
- 本节点产出物：`docs/superpowers/plans/b369.1-plan.md`（计划文档；不写实现代码）

---

## 0. 本卡干什么（一段话）

Ticket 0 已落完整配对 wire（`internal/proto/pairing.go`）与 Go 侧金样本（`internal/proto/pairing_fixture_test.go`）。
S1 只做**测试与样本**，不碰产品代码：把 Go 侧回归从「手写 3 台机器的 roundtrip」升级为**属性测试 + 键集逐键断言 + 拒绝面分型断言**，
并产出**跨语言共享金样本** `mobile/bind/pairing_fixture.json`，用同源字节钉住「Go 编 / Kotlin·Swift 解」的整链（壳侧解码测试按 P5=A 归真机清单 #1，本仓只锁共享字节文件的逐字节一致）。

**为什么必须在这里做**：契约条 10 与 §2.4 澄清 1 把配对载体 JSON 的**键集与字面值**定为跨进程契约面（Go 编 / 壳解）。
现状 `TestPairBundleRoundTrip` 只对**一个固定样本**逐字段比对，改一个 `json` tag（如把 `addr` 改名）不会让任何断言变红——
这正是「两端各自有测试、中间链路没测试」的形状。S1 补的三层断言让 tag / 枚举字面量 / 可空语义的任何漂移当场变红。

**边界**：不改 `internal/proto/pairing.go`（契约冻结的签名与 wire 形状）。若实现中发现 `pairing.go` 确有缺陷，**停下提问**，不自行改冻结物。

---

## 1. 基线事实（本轮实测，动手前已复核）

| 事实 | 原始读数 |
|---|---|
| Go 版本 | `go version go1.26.1 linux/amd64` |
| 基线 `internal/proto` 测试 | `go test ./internal/proto/... -count=1` → `ok github.com/Xsxdot/handoff/internal/proto 0.004s`（EXIT=0） |
| 根模块 build | `go build ./...` → EXIT=0 |
| mobile 模块 build | `cd mobile && go build ./...` → EXIT=0 |
| 图闸基线 | `codegraph check --view cards-B369-charter` → `fails=[]`（EXIT=0）；`codegraph validate --view cards-B369-charter` → `issues=null`、`edgeIssues=null`（EXIT=0）；`codegraph check`（基线）→ `fails=[]`（EXIT=0） |
| 契约闸基线 | `go test ./cmd/ -run TestRepoContractGate -count=1` → `ok github.com/Xsxdot/handoff/cmd 0.066s`（EXIT=0） |
| `mobile/bind/pairing_fixture.json` 现状 | **不存在**（`test -f` → NO） |
| `pairing_fixture_test.go` 现状 | 104 行，4 个测试：`TestPairBundleEnvelopeVersionKey` / `TestPairBundleRoundTrip` / `TestPairBundleRejectsUnknownVersion` / `TestPairBundleRejectsMalformed`；本文件 `pairFixture()`（:16）是本卡金样本唯一来源 |
| 现有 roundtrip 强度 | `TestPairBundleRoundTrip`（:56）只比对 `Version/Relay.URL/Relay.Credential/机器数/时间`——**既不比机器字段，也不比键集** |
| 机器级拒绝面 | `pairing.go#PairMachine.Validate`（:75）判名字空/token 空/双形态/两空；`Validate`（:91）判版本/无机器/relay 缺端点 |
| 无库依赖先例 | `go.mod` 无 `rapid`/`gopter`；`internal/` 无 `testing/quick` 引用 → 本卡用**标准库 `math/rand`** 手写属性生成器，不引入新依赖 |
| 项目既有同类先例 | `internal/proto/contract_fixture_test.go#TestContractFixtures`（:58）：用 `runtime.Caller(0)` 定位仓根 fixture 目录、逐字节比对、`-update` 显式刷新 |
| 拒绝面双重分型（实测） | 建测试夹具后实测：7 条畸形载荷均 `errors.Is(err, ErrPairMalformed)` 且 `!errors.Is(err, ErrPairVersion)`；`v=999`/`v=0` 均 `ErrPairVersion` 且 `!ErrPairMalformed` |
| 属性生成器可跑（实测） | 本轮以 `seed=0xB369`、500 轮随机 bundle 跑通「`EncodePairBundle(DecodePairBundle(EncodePairBundle(b))) == EncodePairBundle(b)`」逐字节恒等 + 字段/nil-ness 恒等（含空/非空 Relay、relay/直连两形态、1..4 机、有/无 Ticket、零值/非零时间） |
| 变异可红（实测） | 原地把 `EncodePairBundle` 改成写 `b.Version = PairVersion + 1` 后：`TestPairBundleRoundTrip` → FAIL `proto: 配对载体版本不受支持: 2`；属性测试 → FAIL `decode: ... raw={"v":2,...}`。`git checkout -- internal/proto/pairing.go` 还原后 `go test ./internal/proto/ -count=1` → ok |
| 共享金样本规范字节（实测） | `EncodePairBundle(pairFixture())` 紧凑 JSON，`len=588`（逐字节原文见 §5.2） |

**库行为出处**：本卡只用标准库（`encoding/json`/`math/rand`/`os`/`runtime`/`bytes`），无第三方库行为事实需引出处。
`json:"...,omitempty"` 的语义（nil 指针/空串省键、非 omitempty 字段零值仍出键）在本轮 §5.2 与拒绝面实测中被直接验证，不靠记忆。

---

## 2. 图覆盖债

本轮亲测 `codegraph sym <名> --view cards-B369-charter`：

- **命中（view 内）**：`PairBundle`（`m_proto_PairBundle`）、`PairRelay`、`PairMachine`、`PairTicket`、`EncodePairBundle`（`n_proto_EncodePairBundle`）、`DecodePairBundle`（`n_proto_DecodePairBundle`）。
- **未命中（图覆盖债）**：`PairVersion`、`ErrPairVersion`、`ErrPairMalformed` 三个常量/变量符号不在图（`codegraph sym` 返回「不在图中」，无近似候选）。回落 grep 确认它们在 `internal/proto/pairing.go` 定义。**本卡不补图**（新增代码全在 `_test.go`，测试文件不计图边，见 `rooms_fixture_test.go` 头注先例）；该债记于此，供 S4/后续图对齐节点处置。
- `mobile/bind/pairing_fixture.json` 落在 `mobile/`（嵌套独立 module，**图外**，扫描配方已排除 `mobile` 前缀），不进图、不触发 `graph check`。

---

## 3. Task DAG

```text
T1 属性测试 + 键集断言 + 拒绝面分型 + 跨语言样本护栏（改 pairing_fixture_test.go）
      │  产出可判「键集/字面量/tag 漂移即红」的测试体
      ▼
T2 共享金样本 mobile/bind/pairing_fixture.json（新，字节 = EncodePairBundle(pairFixture())+"\n"）
      │  T1 的 TestPairingFixtureMatchesCanonicalBytes 依赖 T2 的文件存在（写文件前该测试必红）
      ▼
T3 收口：图三闸 + 契约闸复核（不改图、不改产品代码）
```

**最薄路径条**：T1 是本卡要锁行为的**唯一点亮路径**。当前从声明缝（`EncodePairBundle`/`DecodePairBundle`）调用**本已能得到预期结果**（wire 已实现、基线测试绿），故「写下去就会绿」的部分按纪律免除竖切卡；但 T1 的**属性测试与键集断言**写下去会绿，而 `TestPairingFixtureMatchesCanonicalBytes` 在 T2 落文件前**必然红**——这就是 T2 必须在 T1 之后、且先红后绿的唯一处。
**本卡全部是测试/样本，唯一的产品侧行为（编码/解码）已由 Ticket 0 落地并冻结；本卡不新增产品行为，故无「锁缝断言」之外的红绿周期**（见 §4.3 判据）。

---

## 4. Task T1：配对 wire 回归强化（属性 + 键集 + 拒绝分型 + 样本护栏）

### 4.1 精确文件与改动

- **改**：`internal/proto/pairing_fixture_test.go`
- 不改任何其它文件；**尤其不改** `internal/proto/pairing.go`。
- 测试范围（最小化）：只跑 `go test ./internal/proto/... -count=1`，不跑全量测试。
- 关键节点日志：本 task 交付物是测试代码，无生产日志面。可观测性以 `t.Logf` 承载体（样本字节、随机 seed、机器数）；不得向产品代码加 `print`/`log`。
- 注释：文件头已写职责+边界（保留）；新增辅助函数逐个写参数/返回/为什么。

### 4.2 Interfaces

- **Consumes**（读，不改）：
  - `proto.EncodePairBundle(b PairBundle) (string, error)`（`internal/proto/pairing.go:111`）
  - `proto.DecodePairBundle(raw string) (PairBundle, error)`（`internal/proto/pairing.go:127`）
  - `proto.PairVersion int`（`pairing.go:24`）、`proto.ErrPairVersion`/`proto.ErrPairMalformed error`（`pairing.go:33/35`）
  - `PairBundle` / `PairRelay` / `PairMachine` / `PairTicket` 及其 `json` tag（`pairing.go:40-72`）
  - 同文件既有 `pairFixture() PairBundle`（`pairing_fixture_test.go:16`）
  - 先例形态：`internal/proto/contract_fixture_test.go#fixtureDir`（用 `runtime.Caller(0)` 定位仓根）
- **Produces**（测试内符号，不跨卡消费）：
  - 测试函数：`TestPairBundleRoundTripProperty`、`TestPairBundleJSONKeysMatchWire`、`TestPairBundleRejectsMalformedSentinelExact`、`TestPairingFixtureMatchesCanonicalBytes`
  - 辅助函数：`randomPairBundle(rng *rand.Rand) PairBundle`、`randHex(rng *rand.Rand, n int) string`、`randTime(rng *rand.Rand) time.Time`、`pairingFixturePath(t *testing.T) string`

### 4.3 步骤（红绿只套在「样本护栏」这一处）

**步骤 1（改 import 块）**：把现有 import 替换为：

```go
import (
	"bytes"
	"encoding/json"
	"errors"
	"fmt"
	"math/rand"
	"os"
	"path/filepath"
	"runtime"
	"strings"
	"testing"
	"time"
)
```

**步骤 2（追加以下测试体到文件末尾）**：

```go
// randomPairBundle 按 seed 生成一份合法 PairBundle：1..4 台机器、每台非
// relay 即直连形态（Validate 要求恰居其一）、可选 Credential/Ticket、
// 可选非空 Relay（仅当至少一台为 relay 形态时给出）、可选非零时间。
// Token 用随机 hex（≥1 字符即可过 Validate；熵闸执法在 S2，不在 wire 层）。
func randomPairBundle(rng *rand.Rand) PairBundle {
	n := 1 + rng.Intn(4)
	b := PairBundle{Version: PairVersion}
	needRelay := false
	for j := 0; j < n; j++ {
		m := PairMachine{
			Name:  fmt.Sprintf("m%d", j),
			Token: randHex(rng, 1+rng.Intn(80)),
		}
		if rng.Intn(2) == 0 {
			m.Node = fmt.Sprintf("node-%d", j)
			needRelay = true
		} else {
			m.Addr = fmt.Sprintf("http://10.0.0.%d:%d", j+1, 7000+j)
		}
		if rng.Intn(2) == 1 {
			m.Credential = randHex(rng, 8)
		}
		if rng.Intn(2) == 1 {
			m.Ticket = &PairTicket{
				URL:       "http://localhost/console?ticket=" + randHex(rng, 6),
				ExpiresAt: randTime(rng),
			}
		}
		b.Machines = append(b.Machines, m)
	}
	if needRelay {
		b.Relay = &PairRelay{URL: "wss://relay.example/relay", Credential: "pipecred"}
	}
	if rng.Intn(2) == 1 {
		b.IssuedAt = randTime(rng)
		b.ExpiresAt = b.IssuedAt.Add(60 * time.Second)
	}
	return b
}

// randHex 返回 n 个小写十六进制字符。n<=0 时返回空串。
func randHex(rng *rand.Rand, n int) string {
	const hex = "0123456789abcdef"
	var sb strings.Builder
	for i := 0; i < n; i++ {
		sb.WriteByte(hex[rng.Intn(len(hex))])
	}
	return sb.String()
}

// randTime 返回 [1970, 2033) 区间内、秒精度的 UTC 时间。
// 秒精度避免 RFC3339 纳秒进位造成「编码再解码」不可比。
func randTime(rng *rand.Rand) time.Time {
	return time.Unix(int64(rng.Intn(2_000_000_000)), 0).UTC()
}

// TestPairBundleRoundTripProperty 是一条属性顶一族手写用例：对随机构造的
// 合法 bundle，断言 Encode∘Decode∘Encode 逐字节恒等，且关键字段与可空语义
// （Relay/Ticket 的 nil-ness、Credential 的有无、Node/Addr 的择一）守恒。
//
// 为什么不直接 DeepEqual：time.Time 零值的 Location 是 nil，而 JSON 解码
// "0001-01-01T00:00:00Z" 得到 UTC，DeepEqual 会把两者误判为不等。故时间用
// .Equal、其余逐字段比。
func TestPairBundleRoundTripProperty(t *testing.T) {
	rng := rand.New(rand.NewSource(0xB369))
	for i := 0; i < 500; i++ {
		b := randomPairBundle(rng)
		raw, err := EncodePairBundle(b)
		if err != nil {
			t.Fatalf("轮 %d 编码失败: %v bundle=%+v", i, err, b)
		}
		got, err := DecodePairBundle(raw)
		if err != nil {
			t.Fatalf("轮 %d 解码失败: %v raw=%s", i, err, raw)
		}
		again, err := EncodePairBundle(got)
		if err != nil {
			t.Fatalf("轮 %d 再编码失败: %v", i, err)
		}
		if raw != again {
			t.Fatalf("轮 %d roundtrip 逐字节漂移:\n in=%s\nout=%s", i, raw, again)
		}
		if (b.Relay == nil) != (got.Relay == nil) {
			t.Fatalf("轮 %d Relay nil-ness 漂移: %s", i, raw)
		}
		if b.Relay != nil && (b.Relay.URL != got.Relay.URL || b.Relay.Credential != got.Relay.Credential) {
			t.Fatalf("轮 %d Relay 字段漂移: %s", i, raw)
		}
		if len(b.Machines) != len(got.Machines) {
			t.Fatalf("轮 %d 机器数漂移: %s", i, raw)
		}
		for j := range b.Machines {
			wm, gm := b.Machines[j], got.Machines[j]
			if wm.Name != gm.Name || wm.Token != gm.Token || wm.Node != gm.Node ||
				wm.Addr != gm.Addr || wm.Credential != gm.Credential {
				t.Fatalf("轮 %d 机器[%d]字段漂移: %s", i, j, raw)
			}
			if (wm.Ticket == nil) != (gm.Ticket == nil) {
				t.Fatalf("轮 %d 机器[%d] Ticket nil-ness 漂移（缺失 vs 零值）: %s", i, j, raw)
			}
			if wm.Ticket != nil && (wm.Ticket.URL != gm.Ticket.URL || !wm.Ticket.ExpiresAt.Equal(gm.Ticket.ExpiresAt)) {
				t.Fatalf("轮 %d 机器[%d] Ticket 字段漂移: %s", i, j, raw)
			}
		}
		if !b.IssuedAt.Equal(got.IssuedAt) || !b.ExpiresAt.Equal(got.ExpiresAt) {
			t.Fatalf("轮 %d 时间字段漂移: %s", i, raw)
		}
	}
}

// TestPairBundleJSONKeysMatchWire 键集逐键断言：编码产物每个对象的键必须
// 恰好等于契约 §3.1 声明的集合（多键/少键/改名都红）。这是跨语言解码方
// 逐键一致的可判锚（契约条 10），T2 的共享金样本以此规范化字节为准。
func TestPairBundleJSONKeysMatchWire(t *testing.T) {
	raw, err := EncodePairBundle(pairFixture())
	if err != nil {
		t.Fatal(err)
	}
	var top map[string]json.RawMessage
	if err := json.Unmarshal([]byte(raw), &top); err != nil {
		t.Fatal(err)
	}
	assertExactKeys(t, "bundle", top, []string{"v", "relay", "machines", "issued_at", "expires_at"})

	var relay map[string]json.RawMessage
	if err := json.Unmarshal(top["relay"], &relay); err != nil {
		t.Fatal(err)
	}
	assertExactKeys(t, "relay", relay, []string{"url", "credential"})

	var machines []map[string]json.RawMessage
	if err := json.Unmarshal(top["machines"], &machines); err != nil {
		t.Fatal(err)
	}
	if len(machines) != 3 {
		t.Fatalf("金样本应有 3 台机器，实得 %d", len(machines))
	}
	// 0：relay 形态 + ticket；1：直连形态；2：relay 形态无 ticket。
	assertExactKeys(t, "machines[0]", machines[0], []string{"name", "token", "node", "ticket"})
	assertExactKeys(t, "machines[1]", machines[1], []string{"name", "token", "addr"})
	assertExactKeys(t, "machines[2]", machines[2], []string{"name", "token", "node"})

	var ticket map[string]json.RawMessage
	if err := json.Unmarshal(machines[0]["ticket"], &ticket); err != nil {
		t.Fatal(err)
	}
	assertExactKeys(t, "ticket", ticket, []string{"url", "expires_at"})
}

// assertExactKeys 断言 m 的键集与 want 完全相同（顺序无关，多/少/替均失败）。
func assertExactKeys(t *testing.T, label string, m map[string]json.RawMessage, want []string) {
	t.Helper()
	if len(m) != len(want) {
		t.Fatalf("%s 键数漂移: got %v want %v", label, keysOf(m), want)
	}
	for _, k := range want {
		if _, ok := m[k]; !ok {
			t.Fatalf("%s 缺键 %q: got %v want %v", label, k, keysOf(m), want)
		}
	}
}

// keysOf 返回 map 键列表，仅用于失败信息。
func keysOf(m map[string]json.RawMessage) []string {
	ks := make([]string, 0, len(m))
	for k := range m {
		ks = append(ks, k)
	}
	return ks
}

// TestPairBundleRejectsMalformedSentinelExact 拒绝面双重分型：每条畸形载荷
// 必须 ErrPairMalformed 且**不得**同时被 ErrPairVersion 命中；版本错误反之。
// 壳侧按 sentinel 分支渲染，不得解析文案，故两类错误必须互斥可判（契约条 3/4）。
func TestPairBundleRejectsMalformedSentinelExact(t *testing.T) {
	malformed := map[string]string{
		"不可解 JSON":    `not-json`,
		"无机器登记":       `{"v":1,"machines":[]}`,
		"机器名空":        `{"v":1,"machines":[{"name":"","token":"t","addr":"http://x"}]}`,
		"token 空":     `{"v":1,"machines":[{"name":"m","token":"","addr":"http://x"}]}`,
		"双形态自相矛盾":     `{"v":1,"relay":{"url":"wss://r","credential":"c"},"machines":[{"name":"m","token":"t","node":"n","addr":"http://x"}]}`,
		"两形态皆空":       `{"v":1,"machines":[{"name":"m","token":"t"}]}`,
		"relay 形态缺端点": `{"v":1,"machines":[{"name":"m","token":"t","node":"n"}]}`,
	}
	for name, raw := range malformed {
		_, err := DecodePairBundle(raw)
		if !errors.Is(err, ErrPairMalformed) {
			t.Fatalf("%s：应返回 ErrPairMalformed，得到 %v", name, err)
		}
		if errors.Is(err, ErrPairVersion) {
			t.Fatalf("%s：畸形载荷不得同时被判为版本错误: %v", name, err)
		}
	}
	versions := map[string]string{
		"未知版本 v=999": `{"v":999,"machines":[{"name":"m","token":"t","addr":"http://x"}]}`,
		"缺省版本 v=0":   `{"machines":[{"name":"m","token":"t","addr":"http://x"}]}`,
	}
	for name, raw := range versions {
		_, err := DecodePairBundle(raw)
		if !errors.Is(err, ErrPairVersion) {
			t.Fatalf("%s：应返回 ErrPairVersion，得到 %v", name, err)
		}
		if errors.Is(err, ErrPairMalformed) {
			t.Fatalf("%s：版本错误不得同时被判为畸形: %v", name, err)
		}
	}
}

// pairingFixturePath 返回共享金样本在仓内的绝对路径 mobile/bind/pairing_fixture.json。
// 用 runtime.Caller(0) 从本测试文件推仓根（与 contract_fixture_test.go#fixtureDir 同款），
// 不依赖 go test 的 cwd。
func pairingFixturePath(t *testing.T) string {
	t.Helper()
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("无法定位测试文件自身路径")
	}
	return filepath.Join(filepath.Dir(thisFile), "..", "..", "mobile", "bind", "pairing_fixture.json")
}

// TestPairingFixtureMatchesCanonicalBytes 跨语言共享金样本护栏：仓内
// mobile/bind/pairing_fixture.json 必须逐字节等于 EncodePairBundle(pairFixture())
// 加一个换行。壳（Kotlin/Swift）侧用同一份文件做孪生解码断言（P5=A 壳工程
// 不进本仓，真机清单 #1）；本测试是「Go 编」与「壳解」之间唯一的机内链环——
// 它红了说明 Go 侧 wire 形状漂移而共享样本未同步（或反之）。
func TestPairingFixtureMatchesCanonicalBytes(t *testing.T) {
	raw, err := EncodePairBundle(pairFixture())
	if err != nil {
		t.Fatal(err)
	}
	canonical := raw + "\n"
	stored, err := os.ReadFile(pairingFixturePath(t))
	if err != nil {
		t.Fatalf("读取共享金样本失败（T2 是否已落文件？）: %v", err)
	}
	if !bytes.Equal(stored, []byte(canonical)) {
		t.Errorf("共享金样本漂移（改 wire 形状请同步刷新该文件）:\n--- stored ---\n%s\n--- canonical ---\n%s", stored, canonical)
	}
}
```

**步骤 3（跑红——样本护栏；这是本 task 唯一的红绿周期）**：

```bash
go test ./internal/proto/ -run TestPairingFixtureMatchesCanonicalBytes -count=1
```

预期（**动手前已在基线复核该判据的正确性**：文件不存在时 `os.ReadFile` 必然失败）：

```
--- FAIL: TestPairingFixtureMatchesCanonicalBytes
    ... 读取共享金样本失败（T2 是否已落文件？）: open .../mobile/bind/pairing_fixture.json: no such file or directory
FAIL
```

**步骤 4（跑红——变异验证）**：原地改 `internal/proto/pairing.go`（**唯一一次临时改动**）：在 `EncodePairBundle` 的 `Validate` 之后、`json.Marshal` 之前插入一行 `b.Version = PairVersion + 1`，然后：

```bash
go test ./internal/proto/ -count=1
```

预期（本轮基线实测原文）：

```
--- FAIL: TestPairBundleRoundTrip
    pairing_fixture_test.go:65: proto: 配对载体版本不受支持: 2
--- FAIL: TestPairBundleRoundTripProperty
    ... 解码失败: proto: 配对载体版本不受支持: 2 ...
FAIL
```

随后 **必须立即还原**（该行是本卡唯一的临时变异；`pairing.go` 不得带任何改动进 commit）：

```bash
git checkout -- internal/proto/pairing.go
go test ./internal/proto/ -count=1     # 预期 ok
```

> 执行纪律：临时文件一律用 `$TMPDIR`（已指向任务临时目录，在任务范围内），不得使用任务范围外的路径。

**步骤 5（跑绿）**：T2 落文件后（见 §5），`go test ./internal/proto/ -count=1` 全绿。属性测试与键集/拒绝分型测试**写下去即绿**（wire 已实现），不单独占红绿周期。

### 4.4 测试清单与缝覆盖

| 测试 | 入口调用符号（缝） | 锁什么 |
|---|---|---|
| `TestPairBundleRoundTripProperty` | `EncodePairBundle`→`DecodePairBundle` | roundtrip 逐字节恒等 + 可空语义守恒（契约条 2） |
| `TestPairBundleJSONKeysMatchWire` | `EncodePairBundle` | 键集逐键（契约条 1/10；§2.4 澄清 1） |
| `TestPairBundleRejectsMalformedSentinelExact` | `DecodePairBundle` | 版本/畸形两类 sentinel 互斥可判（契约条 3/4/5/6/7/8/9） |
| `TestPairingFixtureMatchesCanonicalBytes` | `EncodePairBundle` | 共享金样本逐字节一致（契约条 10 的跨语言桥） |
| （回归，不改）`TestPairBundleEnvelopeVersionKey` 等 4 个 | `Encode`/`Decode` | 既有回归保持绿 |

**缝 → 测试**：本卡锁的缝 = 配对载体 wire 的编解码对（`EncodePairBundle`/`DecodePairBundle`），契约 §4.1 条 1–9 全部由 `TestPairBundleRejectsMalformedSentinelExact` + 属性/键集测试覆盖；条 10 由 `TestPairingFixtureMatchesCanonicalBytes` + 键集测试覆盖。
**测试 → 缝**：上表每支测试的入口符号都在该缝上，无内部锁。

**变异验证（判据能红）**：`b.Version = PairVersion + 1` 使 `TestPairBundleRoundTrip` 与属性测试红（§4.3 步骤 4 实测）；把任一 `json` tag 改名（如 `json:"addr"` → `json:"adr"`）会使 `TestPairBundleJSONKeysMatchWire` 与 `TestPairingFixtureMatchesCanonicalBytes` 红；把 `DecodePairBundle` 的版本分支删掉会使拒绝分型测试红。

---

## 5. Task T2：共享金样本 `mobile/bind/pairing_fixture.json`

### 5.1 精确文件与改动

- **新**：`mobile/bind/pairing_fixture.json`（`mobile/` 嵌套 module，图外，不影响 `graph check`）
- 不改 `mobile/bind/bind.go`、不改 `mobile/go.mod`
- 测试范围：由 T1 的 `TestPairingFixtureMatchesCanonicalBytes` 覆盖（`go test ./internal/proto/ -count=1`）

### 5.2 文件内容（规范字节 = `EncodePairBundle(pairFixture())` + 换行；本轮实测 `len=588`+1）

```json
{"v":1,"relay":{"url":"wss://relay.example/relay","credential":"pipecred"},"machines":[{"name":"devbox","token":"abababababababababababababababababababababababababababababababab","node":"devbox","ticket":{"url":"http://localhost/console?ticket=x","expires_at":"2023-11-14T22:14:20Z"}},{"name":"lan","token":"cdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcdcd","addr":"http://10.0.0.9:7777"},{"name":"offline","token":"efefefefefefefefefefefefefefefefefefefefefefefefefefefefefefefef","node":"offline"}],"issued_at":"2023-11-14T22:13:20Z","expires_at":"2023-11-14T22:14:20Z"}
```

（文件**末尾必须有且只有一个换行**；上示为一行 JSON。）

**样本内容说明（写进 T2 的文件注释不可行——JSON 不支持注释，故记于此）**：
- token 全为 `ab/cd/ef` 重复的**假值**，非真实凭据（契约 §8 凭据卫生：金样本不落真 token）；
- relay 形态（`devbox`/`offline`）与直连形态（`lan`）各出现，覆盖 `node`/`addr` 择一；
- `offline` 无 `ticket`，覆盖「离线机可缺」的可选键语义；
- `lan` 无 `credential`，覆盖 `omitempty` 省键；时间取 `2023-11-14T22:13:20Z`（`time.Unix(1700000000,0).UTC()`），非 `time.Now()`，保证可重复、可入 git。

### 5.3 步骤（红 → 绿）

**步骤 1**：写入 §5.2 文件（内容含末尾换行）。
**步骤 2**：`go test ./internal/proto/ -count=1` → 预期全绿（`TestPairingFixtureMatchesCanonicalBytes` 由红转绿，属性/键集/拒绝分型亦绿）。
**步骤 3（变异可红，可选但推荐）**：把文件里 `"addr"` 改成 `"adr"` → `TestPairingFixtureMatchesCanonicalBytes` 与 `TestPairBundleJSONKeysMatchWire` 必红；还原后绿。改文件用 `git checkout -- mobile/bind/pairing_fixture.json` 还原。

### 5.4 Interfaces

- **Consumes**：T1 的 `pairingFixturePath(t)` 与 `pairFixture()` 规范字节（同源）。
- **Produces**：一份字节稳定的 JSON 文件，供壳工程真机侧（P5=A，非本仓）解孪生样本。

---

## 6. Task T3：收口复核（图三闸 + 契约闸）

- **改**：无（纯验证）。
- **测试范围**：仅下述四条命令；不跑全量测试。
- 步骤与预期（本轮基线均已实测）：

```bash
go test ./internal/proto/... -count=1            # 预期 ok
go test ./cmd/ -run TestRepoContractGate -count=1 # 预期 ok（warns 33）
codegraph --repo . check --view cards-B369-charter   # 预期 fails=[]
codegraph --repo . validate --view cards-B369-charter # 预期 issues=null edgeIssues=null
codegraph --repo . check                              # 预期 fails=[]（基线仍绿）
```

- 说明：本卡新增代码全在 `_test.go` 与 `mobile/`（图外），**不产生图 diff**。若上述任一闸意外变红，**停下提问**，不自行改 `codegraph/` 冻结物。

---

## 7. 整卡验收判据（行为化，逻辑型机内闭环）

1. `go test ./internal/proto/... -count=1` 退出 0，且以下测试存在并通过：
   `TestPairBundleRoundTripProperty`、`TestPairBundleJSONKeysMatchWire`、`TestPairBundleRejectsMalformedSentinelExact`、`TestPairingFixtureMatchesCanonicalBytes`（外加既有 4 个不破）。
2. 属性测试**可红**：`EncodePairBundle` 版本字段变异（`b.Version = PairVersion + 1`）使 `TestPairBundleRoundTrip`/`TestPairBundleRoundTripProperty` 红；还原后绿（本计划基线已实测）。
3. 键集测试**可红**：任一 `json` tag 改名使 `TestPairBundleJSONKeysMatchWire` 红。
4. `mobile/bind/pairing_fixture.json` 存在，且逐字节等于 `EncodePairBundle(pairFixture())` + `"\n"`（由测试锁定，不靠人工核对）。
5. `go test ./cmd/ -run TestRepoContractGate -count=1` PASS；`codegraph check/validate --view cards-B369-charter` 与基线 `check` 保持 0 fail / 0 issue。
6. 交付物仅：`internal/proto/pairing_fixture_test.go`（改）、`mobile/bind/pairing_fixture.json`（新）。`internal/proto/pairing.go` 零改动（`git diff --stat` 可判）。

---

## 8. 未验证，需真机（不属本卡机内闭环）

- **壳侧 Kotlin/Swift 解码孪生样本**：按 P5=A 壳工程不进本仓，本仓只落共享 JSON；Kotlin `Instant`/Swift `Date` 对 `expires_at`（RFC3339 UTC）的解析一致性须在移动 CI/真机跑（拆解 §5 #1、contract §8.4）。**本卡不得把共享文件存在写成「壳侧解码已验证」。**
- token 熵闸、真实 agentd 应答、真实 relay 均为 S2/S3/S4 面，不属本卡。

---

## 9. 自审三查

1. **spec/契约覆盖逐条能指到 task**：
   - 契约条 1（版本键）→ T1 属性+键集；
   - 条 2（roundtrip 恒等）→ T1 属性；
   - 条 3/4（未知版本/畸形 sentinel）→ T1 拒绝分型；
   - 条 5–9（空列表/名字空/token 空/双形态/两空/relay 缺端点）→ T1 拒绝分型；
   - 条 10（跨语言逐键一致，Go 侧可判锚 + 孪生样本归欠账）→ T1 键集 + T2 共享字节；
   - 拆解 S1 ③「共享金样本字节落地于 `mobile/bind/pairing_fixture.json`」→ T2；
   - 拆解 S1 ③ 图三闸 + gate → T3。
2. **占位符扫描**：无 TBD /「加适当的错误处理」/「同 Task N」/描述性占位。所有测试代码为完整可编译块；文件内容给到字节级。**内部锁声明**：本卡无内部锁——每支测试入口都落在 wire 编解码缝（`EncodePairBundle`/`DecodePairBundle`）上；无「从声明缝构造不出该断言」的例外，故无需退路声明。
3. **跨 task 类型/签名一致性**：T1 Consumes 的 `EncodePairBundle(b PairBundle) (string, error)`、`DecodePairBundle(raw string) (PairBundle, error)`、`ErrPairVersion`/`ErrPairMalformed` 与冻结契约 §3.1 逐字一致；T2 的规范字节由 T1 的 `pairFixture()` 产出，同一来源，无跨卡签名错配（本卡无下游卡消费其 Produces）。

**序列化边界设问**：本卡正是序列化边界卡。新增链路 = `PairBundle`（内存）→ `EncodePairBundle`（Go 编）→ 共享 JSON 字节 →（真机壳解，未验证）。机内穿真实序列化边界的回归 = T1 属性测试（`Encode→Decode→Encode` 逐字节）+ T2 共享字节逐字节比对；用可空类型（`*PairRelay`/`*PairTicket`）区分「字段缺失」与「值为零」，属性测试显式断言 nil-ness 守恒。

**红绿范围声明**：本卡唯一产品行为（编解码）已冻结实现，故红绿只套在 §4.3 步骤 3（样本护栏，T2 前必红）与步骤 4（变异验证）；键集/拒绝分型/属性测试写下去即绿，不配独立周期（符合「内部实现/纯映射步骤不配独立红绿」）。

---

## 10. 红线与不做

- **不改 `internal/proto/pairing.go`**（契约冻结）。任何 wire 形状缺陷 → 停下提问，回 contract。
- **不引入新依赖**（用标准库 `math/rand`，与 `go.mod` 现状一致）。
- **不改图**（`codegraph/` 冻结物）。本卡测试代码不计图边，新增符号不补图，图覆盖债记 §2。
- **不做 S2/S3/S4/S5/S6 的活**：不碰兑换、CLI、构建脚本、壳工程、web。
- **不跑全量测试**：只跑 `./internal/proto/...` 与 `./cmd/ -run TestRepoContractGate`。
- 临时文件一律用 `$TMPDIR`；不触碰任务范围外路径。
