# B156.3.7 plan 节点执行台账（cards/B156.3.7-charter）

> 按纪律块「边干边落台账」整理；本文件与 plan 产出物同批提交。

| # | 事实 / 动作 | 证据（命令与原始输出要点） |
| --- | --- | --- |
| 1 | 第零步自证通过 | `git log --oneline -3` 含 `7b3ccce3`；`grep -c 岔口拍板记录 b156.3-breakdown.md` = 2；`grep -c 归属更正 b156.3-contract.md` = 1 |
| 2 | 工作分支确认 | `git branch --show-current` = cards/B156.3.7-charter，未切分支 |
| 3 | 现状查证：registry 双方言 DDL 已在 store.go | PG 264–269、SQLite 339–344（直读）；四原语在 registry.go:31/80/95/116 |
| 4 | 现状查证：仓内零条直接测 Registry 的测试 | grep `Registry(Put\|Get\|List\|Delete)` 于 internal/**/*_test.go 无命中；唯一触及是 keystone 竖切经门面走 SQLite |
| 5 | 现状查证：既有 PG 冒烟均不触 registry 表 | store_pg_test.go 两支 + mirror_pg_test.go 一支，逐读确认 |
| 6 | 基线判据 V1–V3 | `go build ./...` exit 0；`go test ./internal/ledger/ -count=1` ok 12.489s；`-run 'TestPG' -v` 三支 SKIP 带 newPGStore:14 文案 |
| 7 | 环境探测：本机无 PG 能力 | `which docker podman postgres psql initdb` 全空；无 PG* 环境变量 → PG 腿真跑归真机清单第 4 条，本节点标「未验证」 |
| 8 | 库行为钉版出处 | go.mod 钉 pgx/v5 v5.10.0；pgtype/json.go:24–27+95–101（string→JSONB 原样编码）、152–154+173–179（JSONB→*string 扫描） |
| 9 | 计划级发现：PG JSONB 归一化使字节恒等断言必假红 | JSONB 类型明文行为（文档）→ 判据改 JSON 语义恒等（assertSameJSON），文件头注记不对称性 |
| 10 | 判据探针实跑（SQLite 腿） | 探针=plan Task A1 代码全文落地 internal/ledger/zz_probe_registry_test.go 后 `-run 'TestSQLiteRegistryCASAndSeq\|TestPGRegistryCASAndSeq' -v -count=1`：SQLite PASS (0.09s)、PG SKIP 带原因 |
| 11 | 探针在场门禁 | `go vet ./internal/ledger/` 干净；`gofmt -l internal/ledger/` 无输出；整包 `-count=1` ok 12.520s |
| 12 | 探针删除，工作树净 | `rm zz_probe_registry_test.go && git status --short` 空——diff 无 .go，红线自守 |
| 13 | 引用行号复核 | scheduling.go Enqueue kind 白名单实测在 226–228（sed 直读），与拆解引用一致 |
| 14 | 出稿落盘 | plans/b156.3.7-plan.md + 本台账，同批 commit（不 push） |
| 15 | 未碰 handoff CLI / 未起新 executor | 全程 go/git/grep/sed 只读工具链 + 文档写入 |

## 放弃的尝试

- 曾拟对 body 做**字节级** roundtrip 断言（更强）：核对 pgx 与 PG jsonb 行为后
  确认 PG 腿必然假红，降为语义恒等并留注记——不是放宽，是防假红。
- 曾拟把 SQLite 腿拆到独立文件 registry_sqlite_test.go：越出拆解圈定的有界
  文件集（只允许新增一个测试文件），改为双腿共用一文件并在文件头声明理由。
- 曾拟用冻结五 kind（carrier 等）做冒烟以便「更贴生产」：清理段将触碰生产
  namespace 的行，弃；改用外来 kind（store 层合法，白名单执法在编制域）。

## 协调者复核与追加（2026-08-26）

**文件清单先看**：2 个文档 +505、零删除、零 `.go`——无越轨。裁决 pass，两条 minor
都是如实披露而非掩饰：①PG 腿本环境跑不了（无 docker/PG），标未验证归真机清单第 4 条；
②**PG 的 JSONB 归一化会让「字节恒等 roundtrip」断言必然假红**，判据改成 JSON 语义恒等
并在文件头留档这处不对称。第②条是提前拦下了一支注定假红的测试，质量高于一般 plan 轮。

本轮另一个可取处：判据代码作为探针在基线**真跑过**（SQLite 腿 PASS 0.08s、PG 腿显式
skip 带原因），跑完删探针、工作树干净。「计划里每个断言都亲自跑到过」这条做到了。

### 追加 Task C，并显式扩张有界文件集

复核时按对方卡线的实测教训核了一条本卡没有的判据，缺口属实，故追加 Task C（正文见
plan §4）。要点：

- **缺口**：`ensureSchema` 的 DDL 分 PG / SQLite 两块写。同一张表在两块里各写一遍时，
  合并冲突**只会在其中一块报出来**，另一块被 git 静默合上——它把注意力精确地吸到安全
  的那一半。而本卡 Task A 的 PG 腿无 DSN 时 `t.Skip`，所以「一个方言有表另一个没有」
  在日常 `go test ./...` 里**恒绿**，要等真 PG（很可能是生产建表）才炸。
- **判据形状**：不写「registry 表在不在」（只保护写判据那天想到的那一张），写
  **两个方言的表名集合相等**——以后任何人往任一块加表漏掉另一块都当场翻红，判据不用改。
- **不用行号当判据**：PG 264–269 / SQLite 339–344 只做定位，行号会漂（B228 那 500 个
  行号漂移就是这么来的）。
- **牙齿证明**：变异复验必做——从 PG 支删掉 registry 建表语句，测试必须翻红并点出
  「只在 SQLite 有：registry」。这条**无条件跑、不依赖 DSN**，正是 Task A 的 PG 腿
  覆盖不到的那半。

**扩张是协调者做的，不是执行者越界**：原有界文件集只含 `registry_pg_test.go` 与
`registry.go`，Task C 需要动 `store.go`（一次行为保持的提取重构）并加一个新测试文件。
按纪律「确需越出有界文件集就是拆解错了、要停下提问」，此处由我作为拍板者当场扩张并
写明范围与理由，实现票照此执行不必再问。等价性自证要求写在 Task C 验收里（搬运行之外
无字符改动，用 `git diff` 自证）。

---

# B156.3.7 实现节点执行台账（cards/B156.3.7-charter-2，2026-08-26）

> 分支确认：`git branch --show-current` = cards/B156.3.7-charter-2，基点 d157e8b2，
> 未切分支、未改 git 配置；全程未碰 handoff CLI、未起新 executor。

## Task A（registry 双方言回归测试）

| # | 事实 / 动作 | 证据（命令与原始输出要点） |
| --- | --- | --- |
| A1 | 基线复验 | `go build ./...` exit 0；`go test ./internal/ledger/ -count=1` ok 11.487s；`-run 'TestPG' -v` 三支既有 PG 测试全 SKIP 带 newPGStore:14 文案 |
| A2 | 环境复探：本机无 PG 能力 | `which docker podman postgres psql initdb pg_ctl` 全空；`env | grep -i -E "pg|postgres"` 空 → **PG 腿本环境不可执行** |
| A3 | 落盘 `internal/ledger/registry_pg_test.go` | 与 plan Task A1 代码块逐字符一致（`git show HEAD:plan | sed -n '144,345p'` 与文件 diff 为空） |
| A4 | 双腿首跑 | SQLite 腿 `--- PASS: TestSQLiteRegistryCASAndSeq (0.13s)`；PG 腿 `--- SKIP: TestPGRegistryCASAndSeq (0.00s)` 文案「未设 LEDGER_TEST_PG_DSN，跳过 PG 冒烟」——**真跑了的是 SQLite 腿** |
| A5 | 判据覆盖核对 | assertRegistrySemantics 断言①–⑨与 plan §5 映射表逐条对应（①③④⑤⑨ Put/Get/List/Delete、②⑨ CAS、④⑤ Seq、⑥⑨ ErrNotFound、⑧⑦ 入参与 body 多形态），无缺号 |

## Task C（双方言表集合对等检）

| # | 事实 / 动作 | 证据（命令与原始输出要点） |
| --- | --- | --- |
| C1 | TDD 先红 | 先落 `ddl_parity_test.go` 再提取：`internal/ledger/ddl_parity_test.go:33:17: undefined: ddlStatements`（两处调用点编译红，非拼写错——符号名照 plan Task C 声明） |
| C2 | 提取转绿 | `ensureSchema` 的 DDL 表提取为 `func ddlStatements(pg bool) []string`，保留 var+if/else 形态使语句行缩进零变化；`go test -run TestDDLDialectParity` PASS |
| C3 | 等价性自证 | `git diff internal/ledger/store.go` 全文复核：±行仅函数壳（注释搬家、签名、`if s.dialect == dialectPG`→`if pg`、`return ddl`、新 ensureSchema 头），**任何 SQL 语句行不在 ± 行中** |
| C4 | 变异复验锚点唯一性 | `grep -c "CREATE TABLE IF NOT EXISTS registry"`=2（双方言各一）；`grep -c "body JSONB NOT NULL"`=1（PG 支 registry 语句独有特征，变异打中唯一） |
| C5 | **变异读数**（施加位置=执行位置=本工作树 cards/B156.3.7-charter-2 @ d157e8b2） | 从 PG 支删掉 registry 建表语句后 `go build` COMPILE_OK，`-run TestDDLDialectParity -v`：`只在 PG 有: []` / `只在 SQLite 有: [registry]` / `--- FAIL`；还原后复绿。变异前 cp 备份、还原后 sha256 与变异前一致 |

### 空集守卫（补充纪律①③自检的加固）

parity 判据若因未来 DDL 形态或正则改动而解析失配，两集合同为空也「相等」——空转绿。
已加非空守卫把该形态钉死成红并做正控：正则临时改为不命中模式后
`ddl_parity_test.go:39: 表名解析空结果: pg=0 sqlite=0——createTableName 正则或 DDL 形态已变，对等检失效`
当场翻红；还原后复绿。

## 强制正控读数（验收判据④，全部在 registry.go 上变异→编译确认→跑测→还原→sha256 核验）

每发变异先 `grep -c` 验锚点唯一；`cp /tmp/registry.go.bak` 还原后校验和恒为
`1b7ea93d…7012`（与 HEAD 一致）。测试命令 `-run TestSQLiteRegistryCASAndSeq -v -count=1`。

| 正控 | 被断言不存在的事→真的做出来 | 锚点唯一性 | 当场变红的原始报文 | 对应断言 |
| --- | --- | --- | --- | --- |
| A1 | 摘掉 Put 冲突守卫（`if current != expectVersion`→`if false`），过期版本写入真实落库 | `if current != expectVersion {`=1 | `registry_pg_test.go:52: 已存在行 expect=0 应 ErrCASConflict，得 <nil>`（nil 即写入被接受） | ②（CAS 拒绝+冲突不写入） |
| M2 | seq 计算改常量（子查询整体→字面量 `0`），seq 不再增长 | `(SELECT COALESCE(MAX(seq), 0) + 1 FROM registry)`=2 处全替换 | `registry_pg_test.go:52: 更新后 seq 应严格递增: 0 -> 0` | ④（Seq 单调不变性） |
| M3 | 吞掉 ErrNotFound 哨兵（`%w` 包装→普通文案） | `ErrNotFound)`=1 | `registry_pg_test.go:52: 缺失读应 ErrNotFound，得 registry registry-smoke-test/absent: 缺失但哨兵被吞` | ⑥（errors.Is 可判定） |
| M4a | 摘空 kind 守卫（条件收窄为 `id == ""`） | `if kind == "" || id == ""`=1 | `registry_pg_test.go:52: 空 kind 应被拒` | ⑧ |
| M4b | 摘空 id 守卫（对称收窄为 `kind == ""`） | 同上=1 | `registry_pg_test.go:52: 空 id 应被拒` | ⑧ |
| M4c | 摘空 body 守卫（`len(body) == 0`→`< 0`，保编译） | `if len(body) == 0 {`=1 | `registry_pg_test.go:52: 空 body 应被拒` | ⑧ |
| M5 | 摘 Delete 的 RowsAffected==0 冲突守卫（`count == 0`→`< 0`，sed 行地址 123 打中 Delete 那处） | `count == 0`=2，行号定位消歧 | `registry_pg_test.go:52: 错版本删除应 ErrCASConflict，得 <nil>` | ⑨（删除 CAS） |
| M6 | List 排序反转为 DESC，升序不变性被破坏 | `ORDER BY seq ASC`=1 | `registry_pg_test.go:52: List 应按 seq 升序: [{…ID:ent-2 Seq:3…} {…ID:ent-1 Seq:2…}]` | ⑤（升序不变性） |
| P1 | parity 空集守卫：正则改为永不命中 | 正则串=1 | `表名解析空结果: pg=0 sqlite=0——createTableName 正则或 DDL 形态已变，对等检失效` | 空转绿防线 |

## 放弃的尝试

- M2 第一发变异只把 `COALESCE(MAX(seq), 0) + 1` 替换成 `0`（留在外层 SELECT 内）：
  `(SELECT 0 FROM registry)` 在空表上返回 NULL 触发 NOT NULL 约束，红在①新建而非④单调——
  读数有效但没打中目标断言，整块替换为「子查询整体→字面量 0」重做（见 M2 行）。

## 如实声明（未验证 / 未覆盖）

- **PG 腿行为判据未验证**：本机无 docker、无 postgres 二进制、无 PG 相关环境变量
  （A2 复探）。`TestPGRegistryCASAndSeq` 在真 PG 上的行为归真机清单第 4 条，
  由协调者在指向专用测试库的 `LEDGER_TEST_PG_DSN` 下执行。
- **Put 的 UPDATE 路径 RowsAffected==0 守卫（registry.go:68）未单独正控**：单线程
  判据体在守卫链完好的前提下到不了该分支（它只在并发丢失更新时开火）；其结构同型
  于已正控的 Delete 守卫（M5）。如实记录，不伪装已覆盖。

## 收尾门禁（最终态实测）

```
go build ./...        → exit 0
go vet ./...          → exit 0（VET_OK）
gofmt -l internal/ledger/*.go → 0 行
go test ./internal/ledger/ -count=1 → ok github.com/Xsxdot/handoff/internal/ledger 11.222s
  其中：TestDDLDialectParity PASS(0.00s)、TestSQLiteRegistryCASAndSeq PASS(0.06s)、
       TestPGRegistryCASAndSeq SKIP(0.00s)「未设 LEDGER_TEST_PG_DSN，跳过 PG 冒烟」
```

## 边界核算

- 有界文件集内：`internal/ledger/{registry_pg_test.go 新, ddl_parity_test.go 新,
  store.go 提取重构}`；`registry.go` 最终零 diff（全部变异已还原）。台账文件为纪律块
  要求与本卡法定产出物（plan §法定产出物点名本台账），非代码越界。
- 生产 API 零变化：四原语导出签名逐字未动（registry.go 零 diff）；store.go 新增符号仅
  非导出 `ddlStatements`（Task C 协调者授权的提取产物），其余新增符号全部是包内非导出
  测试符号（Test×3、assertRegistrySemantics、assertSameJSON、cleanRegistrySmoke、
  registrySmokeKind、createTableName）。

---

# B156.3.7 审阅退回修复轮执行台账（cards/B156.3.7-charter-3，2026-08-26）

> 分支确认：`git branch --show-current` = cards/B156.3.7-charter-3，基点 995ac9a6，
> 未切分支、未改 git 配置；全程未碰 handoff CLI、未起新 executor。
> 范围严格限定为审阅轮两条 findings，生产代码零 diff（registry.go / store.go
> 终态 sha256 与基线逐一核验一致）。

## Finding 1（minor→必修）：DDL 对等检补重数判据

| # | 动作 | 证据（命令与原始输出要点） |
| --- | --- | --- |
| F1-1 | 判据升级：集合差集 → **多重集相等**（排序后逐元素等值，`slices.Clone`+`slices.Sort`+`slices.Equal`） | 空集守卫原样保留且仍最先执行；`gofmt -l` 空；正确 DDL 上 PASS（0.00s）——判据文件头「边界」段同步改写重数理由 |
| F1-2 | 正控①：协调者实测存活的「重复登记」变异复跑 | 锚点唯一性：`grep -c "attachments TEXT NOT NULL DEFAULT '\[\]'"`=1（SQLite cards 语句独有片段）；行号 284–291 整块复制插入 SQLite 支后 `attachments…` 计数=2、`go build` COMPILE_OK；`-run TestDDLDialectParity -v`：`SQLite 排序后: [… card_tasks cards cards decisions …]` / `--- FAIL`——**该变异当场翻红**（旧判据下存活） |
| F1-3 | 还原核验 | `cp /tmp/store.go.fixround.bak` 后 sha256=`11547d46…7949f` 与改前一致；parity 复绿 ok |
| F1-4 | 正控②：删表方向在新判据下仍红 | 删 SQLite 支 registry 建表语句（348–352 行整块），COMPILE_OK，FAIL 报文 PG 排序表含 `registry` 而 SQLite 排序表缺之；还原后 sha256 一致、复绿 |

## Finding 2（minor）：Put 更新路径 RowsAffected==0 守卫补机内防线

| # | 动作 | 证据（命令与原始输出要点） |
| --- | --- | --- |
| F2-1 | 探针验证构造法可行（zz_probe_putguard_test.go，验毕即删） | BEFORE UPDATE 触发器在更新落格前删行→探针原文：`PROBE put err = registry registry-smoke-test/lost-1 并发修改: ledger: 状态已被并发修改`、`PROBE get err = <nil> entry = {… Version:1 …}`、`PROBE_RESULT: guard fired ErrCASConflict`——RowsAffected==0 分支被确定性踩中，回滚后行原样保留 |
| F2-2 | 落地为永久测试 `TestSQLiteRegistryPutLostUpdateGuard`（registry_pg_test.go 内新增函数，assertRegistrySemantics 九条断言一字未动） | 首跑 `--- PASS (0.07s)`；断言三连：ErrCASConflict 包装可判定、冲突后版本仍 1、body 语义不变（冲突不写入） |
| F2-3 | 新测试正控：摘守卫必翻红 | 锚点消歧：`grep -c 'count == 0'`=2（Put:68 / Delete:123），sed 行地址 68 打中 Put 处改 `count < 0`，COMPILE_OK 后 `-run TestSQLiteRegistryPutLostUpdateGuard -v`：`registry_pg_test.go:91: RowsAffected==0 守卫应报 ErrCASConflict，得 <nil>` / `--- FAIL`——假成功被放行当场暴露 |
| F2-4 | 还原核验 | sha256=`1b7ea93d…7012` 与基线一致；`git diff` 生产代码零行 |

**协调者二选一指令的答复**：选「构造并加断言」（选项一）。SQLite 腿上经触发器法
确定性可达，故 registry.go 守卫处无需加「无机内防线」注释——防线已存在且带正控
读数。**已知缺口如实点名**：PG 方言无法移植本构造（同表 BEFORE 触发器删自身行的
行为不同型），PG 侧同型守卫与 TestPGRegistryCASAndSeq 的非 skip 真跑同归真机清单
第 4 条，由协调者执行。

## 收尾门禁（最终态实测）

```
go build ./...   → exit 0（BUILD_OK）
go vet ./...     → exit 0（VET_OK）
gofmt -l <本轮两个 .go> → 空
go test ./internal/ledger/ -count=1 → ok github.com/Xsxdot/handoff/internal/ledger 13.524s
定向四支 -v：TestDDLDialectParity PASS(0.00s)；TestPGRegistryCASAndSeq SKIP(0.00s，
  文案「未设 LEDGER_TEST_PG_DSN，跳过 PG 冒烟」未动)；TestSQLiteRegistryCASAndSeq
  PASS(0.06s)；TestSQLiteRegistryPutLostUpdateGuard PASS(0.05s)
——真跑了的是 SQLite 两腿 + parity；PG 腿 skip 带原因，非静默绿。
```

## 「多了什么」正交扫描（验收判据追加项）

**A. 跨子系统绑定点**：`git diff | grep -E '\bNew\(|interface \{|func\('` 仅命中两处
`t.Cleanup(func(){…})` 测试内闭包——本轮 diff 零跨子系统绑定点，无组装点变化。

**B. 新增符号/文件/依赖方向逐个点名出处**：
- 新增导出测试符号仅 `TestSQLiteRegistryPutLostUpdateGuard`（Go test 函数按惯例
  导出但属包内测试面）→ 出处=修复轮补充【二】选项一；
- 新增非导出符号：无（触发器名是函数内局部常量）；
- 新增文件：无；新增依赖方向：无（import 集合不变，slices 已在用）；
- 新增生产符号：**零**（registry.go/store.go 零 diff）。

**C. 计数式判据改前/改后读数**：

| 计数项 | 改前 | 改后 |
| --- | --- | --- |
| `grep -c 'CREATE TABLE IF NOT EXISTS' store.go` | 30 | 30（零触碰） |
| `grep -c 'func Test' registry_pg_test.go` | 2 | 3 |
| `grep -c 't.Fatalf' ddl_parity_test.go` | 2 | 2 |
| `grep -c 'slices\.' ddl_parity_test.go` | 2 | 5（Contains×2 换成 Clone×2+Sort+Equal） |
| internal/ledger 生产 .go 导出符号清单 | 见上轮台账 | 逐条比对零增减 |

**D. 同语义两遍的收敛**：对等检从「差集判红 + 报文」收敛为**单一判据入口**
（多重集相等一次判定），失败报文直接给两侧排序全表——没有第二处重叠判断。

## 边界核算

- 本轮触及文件：`internal/ledger/{ddl_parity_test.go 改, registry_pg_test.go 改}`
  + 本台账。全部在卡有界文件集内（registry_pg_test.go 为本卡新增文件、ddl_parity_test.go
  为 Task C 协调者扩张授权文件），无越界。
- store.go / registry.go 变异均已还原，终态 sha256 与基线一致（F1-3/F2-4 读数），
  生产 API 零变化维持成立。
