# B233.14 实现计划：编制入站封界（gateway 持门面；接收机自证；空 HOME 可带纪律）

状态：已拍板（P1–P5 全 A）的单卡实现计划。L3 轻档，单缝，不拆子卡（禁止 B233.14.x）。
卡号：B233.14（父卡 B233）　路由：contract → breakdown →（单轮）implement → review → acceptance → finish。
有效基线分支：`cards/B233.1-charter-7` @ `94246fc8`（本卡的合并目标以此为准，不切、不越过）。
本计划落点分支：`cards/B233.14-charter-4`（HEAD `7ad85123`）。
冻结物：`docs/superpowers/specs/b233.14-contract.md`（§1–§9，随提交冻结 @ `a49eea84`）＋修订 C-1/C-2/C-3/C-4。源 spec：`docs/superpowers/specs/b233.14.md`。拆解：`docs/superpowers/specs/b233.14-breakdown.md`。台账：`docs/superpowers/ledgers/2026-09-12-b233.14-plan-ledger.md`。

拍板输入（随本节点注入，逐条落实）：

- **P1=A**：`Scheduling()` 返回接口；`SetDefaultCarrier` 不进接口，测试改持具体 `*scheduling.Service` / `SchedulingRegistry()`。
- **P2=A**：`SetScheduling` 入口拒绝 typed-nil（落 `s.scheduling = nil`），与 B233.13 `SetManager` 同款。
- **P3=A**：写入点一律过 `taskProfileHome`/`expandCarrierHome`，非空 `~` 前缀展开为绝对路径；身份快照 `Task.HomeDir` 不改（B347）。
- **P4=确认 #14**：空 HOME 分支 `Isolated=false`；展开后绝对路径 ≠ 隔离 HOME。
- **P5=A**：单卡收口，不拆 `B233.14.x`；先 W1（门面/组装）后 W2（空 HOME×纪律）。
- 本次补充：不新开 `d_cli` 合同；不拆 `B233.14.x`。

本计划不新增 HTTP 路径、CLI 命令、事件类型字面值、顶层领域、跨机协议；不迁包（`Service` 已在 `internal/scheduling`）；不搬 DTO 进 gateway；不把接口放 `schedclient`；不撤 `server.go` 的 assembly；不棘轮预算（B233.17）；不改 `Release` 签名；不重写 `IdentityOf`/`BindPhysical`；不把任务纪律写进载体全局规则/skills。

## 0. 基线核验（动手前亲自跑到，原始输出在台账 §1）

```text
go build ./...                                                              BUILD_EXIT=0
go vet ./internal/agentd/ ./internal/orchestration/ ./internal/scheduling/ ./cmd/  VET_EXIT=0
go test ./internal/orchestration/ -count=1                                  ok 45.480s
go test ./internal/scheduling/ -run 'TestIdentityOfAndBindPhysical|TestResolveLookupTable' -count=1  ok
go test ./internal/proto/ -run 'TestTaskHomeDirJSON' -count=1               ok
go test ./internal/agentd/ -run 'TestB23313ConcurrentDoneReleasesOnce|TestB23313SetManagerRejectsTypedNil' -count=1  ok
go test ./internal/orchestration/ -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1  ok
go test ./internal/approval/ -run 'TestClientApproverAllowUndeliveredUntilAck' -count=1  ok
codegraph validate --repo .                                                 EXIT=0
codegraph resolve --doc docs/superpowers/specs/b233.14-contract.md --repo .  EXIT=0
```

**基线存量红（非本卡引入，不在本卡修复范围）**：

- `go test ./internal/agentd/ -run TestLegacyNodeEventSequenceUnchanged -count=1` 红（连跑两次确定性），事件序列漂移，属功能线既有存量。
- `go test ./cmd/ -run TestRepoContractGate -count=1` 红：读 `codegraph/baseline.json`（未 absorb B233.13/B233.14 视图），报 2 条 dead-interface + 5 条既有 over-budget。合同 #31 的判据用 `codegraph check` 对账，不用该测试。

**图对账基线**：`codegraph check --view cards-B233.14-charter` fails=6 / warns=138；`--base cards/B233.1-charter-7` fails=7 / warns=139。故 #31「相对基线不新增 fail/warn」成立。`target.json` 已是本卡目标态（`d_gateway→d_scheduling` interfaces=["SchedulingClient"]、budget 0、entries 保留）；`best.json` 有 `k_agentd_scheduling_client → d_gateway`。

**库行为事实**（逐条读源码，出处见台账 §1.3）：五家 `Profile.Prepare` 空 `HomeDir` 拒绝（`internal/executor/opencode/profile.go:77-79`）；overlay 落 `<home>/.handoff/task-overlay/<name>`（`:126-138`）；`Rules`/`Skills` 仅非空才写（`:88-123`）；`expandCarrierHome` 空串 no-op（`internal/orchestration/manager.go:1125-1136`）。

## 1. 冻结边界与依赖方向

- `internal/agentd`（gateway）生产字段持使用方接口 `SchedulingClient`（Ticket 0 已定义 `internal/agentd/scheduling_client.go`）；实现是 `internal/scheduling.*Service`（不迁包）。`Service` 不 import gateway——无环。
- gateway 为消费 DTO/纯函数可以 import `internal/scheduling`；生产字段类型必须是接口，禁止 `*scheduling.Service`。
- `scheduling.New` 只在 `cmd/agentd.go#setupLedger` 出现；`SetKnownMachines` 只在具体 `*Service` 上调用。
- 行为缝：`prepareTaskProfile` 删除「空 HOME 拒绝非空纪律」门；写入点经 `taskProfileHome` 恒定非空绝对；`Task.HomeDir` 身份快照永不变。

## 2. 有界文件集（本卡唯一实现范围）

**生产改动**

- `internal/agentd/server.go`：`Server.scheduling` 字段类型、`SetScheduling`、`Scheduling()`、新增 `SchedulingRegistry()`、`SetupAutomation` 删 `scheduling.New`/`SetKnownMachines`。
- `internal/agentd/scheduling_client.go`：删重复编译期断言（Ticket 0 残留，见 Task 1 步骤 0）。
- `internal/orchestration/manager.go`：`prepareTaskProfile`、新增 `taskProfileHome`、包内 `userHomeDir` 缝、`expandCarrierHome` 改用该缝。
- `cmd/agentd.go`：`setupLedger` 构造 `*scheduling.Service` + `SetKnownMachines` + `SetScheduling`；import `internal/scheduling`。

**测试改动**

- 新增：`internal/agentd/b23314_signature_lock_test.go`、`internal/agentd/b23314_scheduling_testhelpers_test.go`（`mustScheduling` + `SetupAutomationForTest`）、`internal/orchestration/b23314_empty_home_discipline_test.go`。
- 机械迁移：把测试里的 `SetupAutomation` 调用改为导出助手 `SetupAutomationForTest`（`package agentd` 与 `agentd_test` 均可调用），再按编译错误取具体 `*scheduling.Service`。明细见 §3.3。

**图**：`codegraph/target.json`、`best.json`、`diffs/cards-B233.14-charter.json` 已随 contract 冻结；本卡仅 Task 3 复核，预计不改。

**禁止**：把整包 `internal/agentd`（workspace/pty/hub/forward）、`internal/executor` 生产、`internal/scheduling` 生产带入改动；圈不出的新文件先停协调者重画。

## 3. 接缝、测试范围与测试文件处置

### 3.1 接缝清单（对照 spec §契约语义与接缝）

| 缝 | 入口符号 | 锁住的行为 | 承接 task |
| --- | --- | --- | --- |
| S1 gateway 对编制 client 的持有与注入 | `Server.SetScheduling` / `Server.Scheduling` / `Server.SchedulingRegistry` / `Server.scheduling` 字段 | 字段类型是 `SchedulingClient`；只实现接口的 fake 可注入；typed-nil 落 nil 后 handler 仍 503；`scheduling.New` 不在 gateway 生产文件 | Task 1（签名锁 + fake + typed-nil + 源码守卫） |
| S2 空 HOME × 非空纪律的启动装配 | `Manager.Dispatch` → `prepareTaskProfile`（→ `taskProfileHome`） | 空 `Task.HomeDir` + 非空纪律成功；`ProfileReq.HomeDir` 为主 HOME 绝对路径、`Isolated=false`；`Task.HomeDir` 仍空；`Rules`/`Skills` 空 | Task 2（缝测试 + 变异可红 + 真落盘） |
| R1 自证不回退（回归） | `scheduling.IdentityOf` / `BindPhysical` / `ResolveLookup` | 空 HOME 合法、显式空串覆盖非空拒绝 | Task 4（既有测试） |
| R2 出站不回退（回归） | `TestB23313ConcurrentDoneReleasesOnce`、`TestApprovalClientAutoAllowProductionHookDoesNotRespond`、`TestClientApproverAllowUndeliveredUntilAck` | B233.13/B233.12 行为不变 | Task 4（既有测试） |

假缝禁令：纯改字段名、纯 import 重写不占接缝名额。`IdentityOf` 既有测试不另占本卡接缝名额（回归 R1）。

### 3.2 测试范围声明（最小化）

- Task 1：`go build ./...`、`go vet ./internal/agentd/ ./cmd/`、`go test ./internal/agentd/ -run 'B23314' -count=1`，以及受影响包的定向回归（`schedapi`/`scheddrain`/`scheddispatch`/`coordapi`/`receiver_*`/`wakeconsumer`/`hostprobe`/`roomsapi`/`recover_occupancy`/`integration`/`server`）。
- Task 2：`go test ./internal/orchestration/ -run 'B23314' -count=1`，再 `go test ./internal/orchestration/ -count=1`。
- Task 3：`codegraph check --view ...`、`codegraph validate`、`codegraph resolve`。
- Task 4：卡级汇总 + 回归 R1/R2。
- **全量测试不属于任何单 task**（implement 三段律）。agentd/cmd 的存量红按「无新增失败」判读。

### 3.3 测试文件处置（P1-A）

**核心问题**：`SetupAutomation` 不再构造编制域服务，但既有 26 处 `SetupAutomation` 调用点中有 20+ 处随后依赖 `Server.scheduling`（如 `postDispatch` 期望 400 而非 503）。逐点改易漏。

**规则（可机械执行）**：

1. 新增**导出**测试助手 `SetupAutomationForTest(t, srv, st)`（定义在 `package agentd` 的 `_test.go`，外部测试包 `agentd_test` 也可调用——同 B23313 `ManagerFactory` 的跨测试包可见性）：内部执行 `srv.SetupAutomation(st)`，再 `scheduling.New(srv.SchedulingRegistry())` + `srv.SetScheduling(svc)`，镜像 `cmd/agentd.go#setupLedger` 的组装语义，返回具体 `*scheduling.Service`。
2. 机械替换：所有既有 `X.SetupAutomation(Y)`（测试内，共 26 处）改为 `SetupAutomationForTest(t, X, Y)`。**例外**：`schedapi_test.go` 的 `TestSchedulingEndpointsDegradedWithoutSetup` 刻意只 `SetLedger` 不 `SetupAutomation`（测 503 降级），保持不变。
3. 白盒已有真实服务时用 `mustScheduling(t, srv)` 取具体 `*scheduling.Service`；外部测试包用 `SetupAutomationForTest` 的返回值。
4. 需要 `SetDefaultCarrier`（不在接口）或把值传给 `putOnlineCarrier(svc *scheduling.Service, …)` 的用例，取具体类型，接口不扩。
5. `.Scheduling()` 单独出现（未紧邻 `SetupAutomation`）的 12 处按编译错误改为 `mustScheduling(t, …)` 或返回的具体服务。测试内构造自定义 registry 的 `scheduling.New(...)`（`scheddrain_test.go:131`、`scheddispatch_test.go:443`、`receiver_occupancy_test.go:392`）保持——`_test.go` 不受源码守卫约束。

**处置表**（按规则 1–5，以编译错误收敛为准）：

| 文件 | 处理 |
| --- | --- |
| `server_test.go`（agentd_test） | 934 改 `SetupAutomationForTest(t, env.srv, ledgerStore)` 取返回 `svc`；943 `svc.SetDefaultCarrier` 用具体类型 |
| `integration_test.go`（agentd_test） | 112/759 改 `SetupAutomationForTest`；`putOnlineCarrierForInteg` 改为接 `svc *scheduling.Service` 参数（调用点 113 取返回值传入）；118/760/764/767 用具体 `svc` |
| `wakeconsumer_test.go`（agentd） | 31 的 `newNoPTYAutomationEnv` 内、849/905 改 `SetupAutomationForTest`；851/856/907/912 的 `env.srv.Scheduling()` 改 `mustScheduling`；987/1017/1044/1096 的 `resumed` 只消费 keystone/账本（`consumeAutomationEventsOnce` 不读 `s.scheduling`），SetupAutomation 仍改助手（无害） |
| `recover_occupancy_test.go`（agentd） | 27 改助手（无害；该测试经 `s.autoLedger` 对账） |
| `scheddrain_test.go`（agentd） | 187/296/339 改助手；66/119 的 `Scheduling()` 改 `mustScheduling`；131/443 自定义 registry 不变；194 `putOnlineCarrier` 用具体类型 |
| `schedapi_test.go`（agentd） | `newSchedEnv`:50、`newSchedNoPTYEnv`:64 改助手；`:51/:72` 的 `svc: env.srv.Scheduling()` 改具体类型；`TestSchedulingEndpointsDegradedWithoutSetup`:453 保持不装配 |
| `scheddispatch_test.go`（agentd） | 72/495 改助手；79/437 取具体类型；443 不变 |
| `coordapi_test.go`（agentd） | 96/111 改助手；`putOnlineCarrier` 保持 `*scheduling.Service`；163/358/382、674/679 取具体类型 |
| `receiver_dispatch_test.go`（agentd） | 34/348 改助手；`seedDefaultFakeCarrier` 内取具体类型；109/153/251/324/355 取具体类型 |
| `coordinator_home_test.go`（agentd） | 444 取具体类型 |
| `hostprobe_test.go`（agentd） | 49/111/282 改助手；51/113/284 取返回值/具体类型 |
| `cardstep_test.go`（agentd） | 236 取具体类型 |
| `b23313_terminal_claim_test.go`（agentd） | 经 `newReceiverTestEnv`/`seedDefaultFakeCarrier` 自动覆盖，核对编译 |
| `receiver_occupancy_test.go`（agentd） | 经 `seedDefaultFakeCarrier` 覆盖；364 取具体类型；392 不变 |
| `roomsapi_test.go`、`manager_dispatch_carrier_test.go` | 34 若在表内改助手（不消费编制域，改后无害）；后者不改 |

**装配助手实现**：完整代码见 Task 1 步骤 7（`newSchedulingForTest` / `SetupAutomationForTest` / `mustScheduling`），§3.3 不重复以免两处漂移。

**内部锁与退路声明**（见 §5.2）：以上迁移是编译驱动的机械替换（`SetupAutomation` → 助手、取具体类型），不改变任何测试的入口符号（入口仍是既有 handler / `Manager.Dispatch` / `SetupAutomation`），故不触发内部锁闸；不设条件退路。

## 4. Task DAG

依赖：Task 1 → Task 2 → Task 3 → Task 4。Task 1 是「点亮 S1」的最薄可跑路径：今天 `Server.scheduling` 是具体 `*scheduling.Service`，写「字段类型是接口」的签名锁必红，故 DAG 第一条就是它。Task 2 是「点亮 S2」的最薄可跑路径：今天空 HOME + 非空纪律必失败（`manager.go:1060`），写成功断言必红。

---

### Task 1：gateway 持门面 + `New` 上移 `cmd` + 测试迁移（W1）

**文件与接口**

- 生产：`internal/agentd/server.go`、`internal/agentd/scheduling_client.go`、`cmd/agentd.go`。
- 测试：新增 `internal/agentd/b23314_signature_lock_test.go`、`internal/agentd/b23314_scheduling_testhelpers_test.go`；§3.3 表。
- Consumes：`scheduling.New(repo schedclient.Registry) *scheduling.Service`；`(*scheduling.Service).SetKnownMachines(fn func(string) bool)`；`schedclient.Registry`；`facadeAsRegistry`（gateway 内已有）；`Server.Conf() func() *config.Config`（已有）。
- Produces：`SchedulingClient`（Ticket 0，不改）；`Server.SetScheduling(svc SchedulingClient)`；`Server.Scheduling() SchedulingClient`；`Server.SchedulingRegistry() schedclient.Registry`；`Server.scheduling SchedulingClient`；`cmd/agentd.go#setupLedger` 组装。

**步骤**

0. **删 `scheduling_client.go` 重复断言。** 文件末有两行完全相同的 `var _ SchedulingClient = (*scheduling.Service)(nil)`（`:58`、`:62`），删去一句并保留顶部注释；这两句是 Ticket 0 残留，重复声明虽合法但是噪声。改完 `go build ./...` 应仍为 0。

1. **切 `Server.scheduling` 字段类型（红/绿锁的锁体）。** `internal/agentd/server.go`：

```go
	// B156.3 自动化层：编制域与 keystone 域的服务引用，SetupAutomation 装配。
	// B233.14：编制域字段是使用方接口 SchedulingClient；具体 *scheduling.Service
	// 只在 cmd 组装点构造后经 SetScheduling 注入，本文件不再 new。
	scheduling SchedulingClient
```

2. **改 `SetScheduling`（P2 typed-nil）。** 替换 `server.go:2751`：

```go
// SetScheduling 注入编制域实现（使用方接口）。
//
// B233.14：参数由具体 *scheduling.Service 改为 gateway 使用方接口 SchedulingClient；
// 生产 *scheduling.Service 只在 cmd 组装点 new。接口里的 typed-nil（(*scheduling.Service)(nil)
// 赋给接口）会让 `s.scheduling == nil` 失效，这里显式识别并落 nil，保住 11 处
// 未就绪 503 守卫（withScheduling / resolveReceiver / StartAutomation / handleDispatch
// 冻结分支 / releaseTaskCarrierOccupancy / coordapi.withCoordinator / scheddispatch /
// scheddrain）。
func (s *Server) SetScheduling(svc SchedulingClient) {
	if svc == nil {
		s.scheduling = nil
		s.log.Warn("SetScheduling 收到 nil 编制实现，按未就绪处理", "error_kind", "scheduling_nil")
		return
	}
	if rv := reflect.ValueOf(svc); rv.Kind() == reflect.Ptr && rv.IsNil() {
		s.scheduling = nil
		s.log.Warn("SetScheduling 收到 typed-nil 编制实现，按未就绪处理", "error_kind", "scheduling_typed_nil")
		return
	}
	s.scheduling = svc
	s.log.Info("编制域已挂接，凭据为使用方接口 SchedulingClient")
}
```

（`reflect` 已在 `server.go` import 中，`SetManager` 同款。）

3. **改 `Scheduling()` 返回类型。** 替换 `server.go:2781`：

```go
// Scheduling 返回编制域能力（使用方接口）；未装配返回 nil（handler 据此降级 503）。
// B233.14：返回类型由 *scheduling.Service 改为 SchedulingClient（测试缝；生产不消费）。
func (s *Server) Scheduling() SchedulingClient { return s.scheduling }
```

4. **新增 `SchedulingRegistry()`。** 紧邻 `SetScheduling` 之后：

```go
// SchedulingRegistry 返回账本门面适配成的编制域持久化端口，供 cmd 组装点构造
// *scheduling.Service（B233.14：scheduling.New 上移 cmd）。
//
// autoLedger 未装配时返回 nil；组装点必须显式判空拒启动——scheduling.New(nil)
// 会建出 repo 为 nil 的坏服务，首次调用才空指针。
func (s *Server) SchedulingRegistry() schedclient.Registry {
	if s.autoLedger == nil {
		return nil
	}
	return facadeAsRegistry{f: s.autoLedger}
}
```

5. **`SetupAutomation` 不再构造 `Service`。** 删除 `server.go:2682-2690` 两段：

```go
	s.scheduling = scheduling.New(facadeAsRegistry{f: facade})
	s.scheduling.SetKnownMachines(func(name string) bool {
		cfg := s.conf()
		if cfg == nil {
			return false
		}
		_, ok := cfg.Targets[name]
		return ok
	})
```

  并把 `SetupAutomation` 文档注释中「编制域服务」改为「账本门面（编制域具体服务由 cmd 组装点 `scheduling.New` 构造后 `SetScheduling` 注入）」。其余 rooms/keystone/hostapi 装配原样保留。

6. **`cmd/agentd.go#setupLedger` 组装。** 在 `srv.SetupAutomation(lst)`（`:480`）之后、`srv.StartAutomation(ctx)`（`:483`）之前插入：

```go
	// B233.14：编制域具体服务在 cmd 组装点构造（scheduling.New 不再落 gateway）。
	// 注册表由 gateway 经 SchedulingRegistry() 交出台账→Registry 适配；判空拒启动，
	// 不得把 nil 喂给 New 静默建出坏服务。
	reg := srv.SchedulingRegistry()
	if reg == nil {
		_ = lst.Close()
		return nil, fmt.Errorf("编制域装配：SchedulingRegistry 为空（SetupAutomation 未装配账本门面）")
	}
	sched := scheduling.New(reg)
	// 保留控制台改机器清单不必重启 agentd：闭包经 Conf() 读活配置。
	sched.SetKnownMachines(func(name string) bool {
		cfg := srv.Conf()()
		if cfg == nil {
			return false
		}
		_, ok := cfg.Targets[name]
		return ok
	})
	srv.SetScheduling(sched)
```

   `cmd/agentd.go` import 增 `"github.com/Xsxdot/handoff/internal/scheduling"`。

7. **新增白盒测试装配助手。** `internal/agentd/b23314_scheduling_testhelpers_test.go`：

```go
package agentd

// b23314_scheduling_testhelpers_test.go —— B233.14 测试装配缝。
//
// 职责：SetupAutomation 不再构造编制域具体服务后，测试需要真实编制域时显式补装
// （镜像 cmd/agentd.go#setupLedger 的组装：SchedulingRegistry + SetKnownMachines
// + SetScheduling）。
// 边界：仅测试构建可见；不改生产导入面。SetupAutomationForTest 导出以便
// package agentd_test 复用（与 B23313 ManagerFactory 同款跨测试包接线）。

import (
	"testing"

	"github.com/Xsxdot/handoff/internal/ledger"
	"github.com/Xsxdot/handoff/internal/scheduling"
)

// newSchedulingForTest 构造编制域具体服务并镜像 cmd 的 SetKnownMachines（经
// Server.Conf() 读活配置）——schedapi 的远程机器放行/未知机器拒绝用例依赖它。
func newSchedulingForTest(t *testing.T, srv *Server) *scheduling.Service {
	t.Helper()
	reg := srv.SchedulingRegistry()
	if reg == nil {
		t.Fatal("SchedulingRegistry() == nil：SetupAutomation 未装配账本门面")
	}
	svc := scheduling.New(reg)
	svc.SetKnownMachines(func(name string) bool {
		cfg := srv.Conf()()
		if cfg == nil {
			return false
		}
		_, ok := cfg.Targets[name]
		return ok
	})
	return svc
}

// SetupAutomationForTest 装配账本/rooms/keystone 并补装编制域具体服务。
func SetupAutomationForTest(t *testing.T, srv *Server, st *ledger.Store) *scheduling.Service {
	t.Helper()
	srv.SetupAutomation(st)
	svc := newSchedulingForTest(t, srv)
	srv.SetScheduling(svc)
	return svc
}

// mustScheduling 返回已装配的真实编制域服务；未装配时补装。返回具体
// *scheduling.Service，供不在接口内的方法（SetDefaultCarrier）与既有具体类型
// 夹具使用。
func mustScheduling(t *testing.T, srv *Server) *scheduling.Service {
	t.Helper()
	if svc, ok := srv.Scheduling().(*scheduling.Service); ok && svc != nil {
		return svc
	}
	svc := newSchedulingForTest(t, srv)
	srv.SetScheduling(svc)
	return svc
}
```

8. **新增签名锁 / fake / typed-nil / 源码守卫测试。** `internal/agentd/b23314_signature_lock_test.go`：

```go
package agentd

// b23314_signature_lock_test.go —— B233.14 S1 缝的可红测试。
//
// 断言四件事：
//   1. Server.scheduling 字段类型是使用方接口 SchedulingClient（变异：改回
//      *scheduling.Service → 红）；
//   2. SetScheduling 参数 / Scheduling 返回类型是 SchedulingClient；
//   3. 只实现接口的 fake 可注入生产字段（不要求具体 *Service）；
//   4. gateway 生产文件不含 scheduling.New(（组装点除外；变异：server.go 加一行 → 红）。

import (
	"os"
	"path/filepath"
	"reflect"
	"strings"
	"testing"

	"github.com/Xsxdot/handoff/internal/scheduling"
)

func TestB23314SchedulingFieldIsInterface(t *testing.T) {
	field, ok := reflect.TypeOf(Server{}).FieldByName("scheduling")
	if !ok {
		t.Fatal("Server.scheduling 字段不存在")
	}
	if field.Type.Kind() != reflect.Interface {
		t.Fatalf("Server.scheduling 类型 = %s，want 接口 SchedulingClient", field.Type)
	}
	if want := reflect.TypeOf((*SchedulingClient)(nil)).Elem(); field.Type != want {
		t.Fatalf("Server.scheduling 接口 = %s，want %s", field.Type, want)
	}
}

func TestB23314SchedulingSignatures(t *testing.T) {
	iface := reflect.TypeOf((*SchedulingClient)(nil)).Elem()
	set, ok := reflect.TypeOf(&Server{}).MethodByName("SetScheduling")
	if !ok {
		t.Fatal("SetScheduling 方法不存在")
	}
	if set.Type.In(1) != iface {
		t.Fatalf("SetScheduling 参数 = %s，want %s", set.Type.In(1), iface)
	}
	get, ok := reflect.TypeOf(&Server{}).MethodByName("Scheduling")
	if !ok {
		t.Fatal("Scheduling 方法不存在")
	}
	if get.Type.Out(0) != iface {
		t.Fatalf("Scheduling 返回 = %s，want %s", get.Type.Out(0), iface)
	}
}

// fakeSchedulingClient 只实现 SchedulingClient（嵌入接口补足其余方法），用于
// 证明生产字段接受非 *scheduling.Service 的实现（契约 #12）。
type fakeSchedulingClient struct{ SchedulingClient }

func (fakeSchedulingClient) DefaultCarrier() (string, error) { return "fake-default", nil }

func TestB23314FieldAcceptsInterfaceOnlyFake(t *testing.T) {
	s := &Server{log: discardLogger()}
	var fake SchedulingClient = fakeSchedulingClient{}
	s.SetScheduling(fake)
	if s.scheduling == nil {
		t.Fatal("生产字段应接受只实现接口的 fake")
	}
	if _, isConcrete := s.scheduling.(*scheduling.Service); isConcrete {
		t.Fatal("注入的 fake 不应是具体 *scheduling.Service")
	}
}

func TestB23314SetSchedulingRejectsTypedNil(t *testing.T) {
	s := &Server{log: discardLogger()}
	var typedNil *scheduling.Service
	s.SetScheduling(typedNil)
	if s.scheduling != nil {
		t.Fatalf("typed-nil 必须落成 nil 接口，got %#v", s.scheduling)
	}
}

// TestB23314SchedulingNewOnlyInCmd 扫描 gateway 生产源文件，禁止出现 scheduling.New(。
// 边界：字符串扫描、剔除 // 行注释；只答「有没有绕过组装点 new」。
func TestB23314SchedulingNewOnlyInCmd(t *testing.T) {
	entries, err := os.ReadDir(".")
	if err != nil {
		t.Fatalf("读取包目录失败: %v", err)
	}
	for _, e := range entries {
		name := e.Name()
		if e.IsDir() || !strings.HasSuffix(name, ".go") || strings.HasSuffix(name, "_test.go") {
			continue
		}
		b, rerr := os.ReadFile(filepath.Join(".", name))
		if rerr != nil {
			t.Fatalf("读取 %s 失败: %v", name, rerr)
		}
		for i, line := range strings.Split(string(b), "\n") {
			if idx := strings.Index(line, "//"); idx >= 0 {
				line = line[:idx]
			}
			if strings.Contains(line, "scheduling.New(") {
				t.Errorf("%s:%d 出现 scheduling.New(；B233.14 起编制域具体服务只在 cmd 组装点构造",
					name, i+1)
			}
		}
	}
}
```

9. **执行 §3.3 迁移表。** 按规则 1–5 改测试：`SetupAutomation` → `SetupAutomationForTest`、`mustScheduling`、`SetDefaultCarrier` 用具体类型、`putOnlineCarrier` 保持 `*scheduling.Service`。

10. **加关键节点日志与注释。** §步骤 1–6 的新分支已带 `error_kind` / `reg == nil` 上下文；`SchedulingRegistry`、`mustScheduling` 文件头写职责+边界；`SetScheduling` 注释写「为什么需要 typed-nil 判据」。

11. **跑红→绿。** 先确认步骤 8 的签名锁在字段未切时红（可在步骤 1 前先落测试再改生产，或改完后用变异复核）。命令与预期：

```text
go build ./...                                                            期望 exit 0
go vet ./internal/agentd/ ./cmd/                                          期望 exit 0
go test ./internal/agentd/ -run 'B23314' -count=1                        期望 ok
go test ./internal/agentd/ -run 'TestSquadsGetEmptyIsLegalState|TestCarrierPutRoundtripThroughWire|TestCarrierPutUnknownMachineIs400|TestSchedulingEndpointsDegradedWithoutSetup' -count=1  期望 ok
go test ./internal/agentd/ -run 'TestAutomationQueueRestartReplay|TestDrainQueuesDefersCoordinatorNoSlotAndContinues|TestCoordinatorSessionRefResolverEmptyHomeOnlineCarrier|TestB23313ConcurrentDoneReleasesOnce' -count=1  期望 ok
go test ./cmd/ -run 'TestSetupLedgerMountsWithRetiredEnabledFlag' -count=1  期望 ok
```

**判据**

- 签名锁三测（字段/签名/fake/typed-nil）绿；源码守卫绿。
- 变异自检：① 字段改回 `*scheduling.Service` → `TestB23314SchedulingFieldIsInterface` 红；② 在 `server.go` 临时加一行 `_ = scheduling.New(nil)` → `TestB23314SchedulingNewOnlyInCmd` 红；③ 去掉 `SetScheduling` 的 typed-nil 分支 → `TestB23314SetSchedulingRejectsTypedNil` 红。
- 受影响包定向测试无**新增**失败（存量红见 §0）。
- `cmd/agentd.go#setupLedger` 后 `Scheduling()` 非 nil（由集成/装配测试隐式锁；`TestSetupLedgerMountsWithRetiredEnabledFlag` 复核装配不回归）。

**缺陷族对抗审查（d_gateway，boundary）**

- 生命周期/状态机中断：装配顺序承重——`SetScheduling` 必须在 `StartAutomation` 之前（后者 `s.scheduling==nil` 即不启动循环）。判据：`cmd/agentd.go` 插入点在 `:480` 与 `:483` 之间；`TestSetupLedger...` 复核。无新运行时资源，无孤儿。
- 静默失败/误导报错：`SchedulingRegistry()` 返回 nil 时 `setupLedger` 显式 `return nil, fmt.Errorf(...)` 拒启动（不是把 nil 喂 `New`）；`SetScheduling` typed-nil 落 nil，未就绪退化为既有 503 而非 panic。
- 跨平台：只改 Go 类型/装配，不解析路径/进程组；`GOOS=windows/darwin go build ./...` 可机内验。
- 假红/假绿：源码守卫剔行注释、跳 `_test.go`，配变更红；接口测试用只实现接口的 fake（嵌入接口），并断言注入值不是具体 `*Service`；`Server` 字段经 reflect 断言，不靠调用点推断。
- 门禁绕过：接口化后 11 处 `s.scheduling == nil` 守卫语义由 typed-nil 落 nil 保持；本卡不新增写路径/HTTP。
- 序列化边界：无新增字段/DTO/投影（见下）。
- 枚举：不新增任何字面值。

**缺陷族对抗审查（d_cli，logic，组装）**

- 生命周期/状态机中断：`setupLedger` 只插几行构造/注入，不改启动/关停次序；`stopLedger` defer 不变。承重顺序：`SetScheduling` 在 `StartAutomation` 之前。
- 静默失败/误导报错：`SchedulingRegistry()` 返回 nil 时显式 `return nil, fmt.Errorf(...)` 并 `lst.Close()`（本函数把 Close 收进 stop，此处提前失败需先 Close），不得把 nil 喂 `New`。
- 跨平台：不解析平台路径、不碰进程组。
- 假红/假绿：`TestSetupLedgerMountsWithRetiredEnabledFlag` 覆盖装配 happy path；`schedapi_test.go` 的 targets 放行/未知机器用例经 `SetupAutomationForTest` 的 `SetKnownMachines(经 Conf())` 锁「活配置读值」不回退。
- 门禁绕过：不新增命令/端点。
- 序列化/枚举：无。
- 承重安全属性：「`scheduling.New` 只在组装点」由 d_gateway 源码守卫锁；`cmd/agentd.go` 在 `assembly` 名单，`n_cmd_setupLedger→n_scheduling_New` 边由 assembly 豁免（target.json 已含）。

**序列化边界设问（本卡）**：本卡不新增数据字段/DTO/手写投影。`Task.HomeDir`/`ProfileReq`/`TaskOverlay` 形状与 JSON 键未动；`ProfileReq`/`ProfileReport` 是进程内结构、无 wire。既有 `TestTaskHomeDirJSON` 锁 `Task.HomeDir` 空串 `omitempty` 省略语义，Task 4 回归。**无 roundtrip 属性测试需求**（无新字段）。

**上下文预算检查**：Task 1 圈定 3 个生产文件 + 3 个新增测试文件 + §3.3 表的 14 个既有测试文件；有界。

**类型标注**：boundary 型 `d_gateway` 与 logic 型 `d_cli` 的真机/机内项分别见 §6 与步骤 11。

---

### Task 2：空 HOME × 非空纪律（W2 行为缝，红→绿）

**文件与接口**

- 生产：`internal/orchestration/manager.go`。
- 测试：新增 `internal/orchestration/b23314_empty_home_discipline_test.go`（`package orchestration`）。
- Consumes：`executor.ProfileReq{HomeDir,Isolated,TaskOverlay}`；`executor.ProfileFromProvider`；`recordingProfile`/`profileRecordingAdapter`（既有 `manager_test.go`）；`newTestManagerWithAds`/`initTestRepo`/`registerTestProject`（既有 harness）；`opencode.New(nil).Profile()`（真落盘）。
- Produces：`taskProfileHome(taskHome string) (string, error)`；包内 `userHomeDir func() (string, error)`；改写后的 `(*Manager).prepareTaskProfile`。

**步骤**

1. **写失败测试（红）。** 新增 `internal/orchestration/b23314_empty_home_discipline_test.go`：

```go
package orchestration

// b23314_empty_home_discipline_test.go —— B233.14 S2 行为缝的红绿判据。
//
// 缝：Manager.Dispatch → prepareTaskProfile。空 Task.HomeDir 是合法身份（沿用
// 主 HOME，B347），删除「空 HOME 拒绝非空纪律」门后，须成功把纪律落任务级
// overlay，且身份快照不被回写。

import (
	"context"
	"errors"
	"os"
	"path/filepath"
	"testing"

	agentd "github.com/Xsxdot/handoff/internal/agentd"
	"github.com/Xsxdot/handoff/internal/executor"
	"github.com/Xsxdot/handoff/internal/executor/opencode"
	"github.com/Xsxdot/handoff/internal/proto"
)

// withMainHome 把包内主 HOME 取值缝替换为测试值。
func withMainHome(t *testing.T, home string) {
	t.Helper()
	prev := userHomeDir
	userHomeDir = func() (string, error) { return home, nil }
	t.Cleanup(func() { userHomeDir = prev })
}

func TestB23314TaskProfileHome(t *testing.T) {
	mainHome := t.TempDir()

	t.Run("空 HOME 取主 HOME", func(t *testing.T) {
		withMainHome(t, mainHome)
		got, err := taskProfileHome("")
		if err != nil || got != mainHome {
			t.Fatalf("taskProfileHome(\"\") = %q, %v；want %q, nil", got, err, mainHome)
		}
	})
	t.Run("非空绝对 HOME 原样", func(t *testing.T) {
		withMainHome(t, mainHome)
		abs := filepath.Join(t.TempDir(), "carrier")
		got, err := taskProfileHome(abs)
		if err != nil || got != abs {
			t.Fatalf("taskProfileHome(%q) = %q, %v；want %q, nil", abs, got, err, abs)
		}
	})
	t.Run("tilde 前缀展开为绝对", func(t *testing.T) {
		withMainHome(t, mainHome)
		got, err := taskProfileHome("~/.handoff/home/muse")
		want := filepath.Join(mainHome, ".handoff/home/muse")
		if err != nil || got != want {
			t.Fatalf("taskProfileHome(tilde) = %q, %v；want %q, nil", got, err, want)
		}
		if !filepath.IsAbs(got) {
			t.Fatalf("展开结果必须绝对: %q", got)
		}
	})
	t.Run("主 HOME 不可得而 taskHome 空则报错", func(t *testing.T) {
		prev := userHomeDir
		userHomeDir = func() (string, error) { return "", errors.New("no home") }
		t.Cleanup(func() { userHomeDir = prev })
		if got, err := taskProfileHome(""); err == nil {
			t.Fatalf("主 HOME 不可得必须报错，got %q", got)
		}
	})
	t.Run("主 HOME 不可得时 tilde 不得静默落相对", func(t *testing.T) {
		prev := userHomeDir
		userHomeDir = func() (string, error) { return "", errors.New("no home") }
		t.Cleanup(func() { userHomeDir = prev })
		if got, err := taskProfileHome("~/x"); err == nil {
			t.Fatalf("tilde 展开失败必须报错，不得返回相对路径 %q", got)
		}
	})
	t.Run("非 tilde 相对 HOME 报错不静默落相对", func(t *testing.T) {
		withMainHome(t, mainHome)
		if got, err := taskProfileHome("relative/home"); err == nil {
			t.Fatalf("相对写入点必须报错，不得落 %q", got)
		}
	})
}

// 主判据 #13/#14/#15/#16：空 HOME + 非空纪律经 Dispatch 成功；写入点为主 HOME
// 绝对路径、Isolated=false；Task.HomeDir 仍空；Rules/Skills 空。
func TestB23314EmptyHomeDisciplinePreparesProfile(t *testing.T) {
	mainHome := t.TempDir()
	withMainHome(t, mainHome)

	profile := &recordingProfile{}
	ad := &profileRecordingAdapter{
		chanAdapter: &chanAdapter{evCh: make(chan executor.AdapterEvent, 1)},
		profile:     profile,
	}
	m, st, _ := newTestManagerWithAds(t, map[string]executor.Adapter{"fake": ad}, "fake")
	repo := initTestRepo(t)
	pid := registerTestProject(t, m, repo)

	const discipline = "空 HOME 纪律\n逐字节保留\r\n"
	emptyHome := ""
	task, err := m.Dispatch(context.Background(), agentd.DispatchReq{
		ProjectID: pid, Prompt: "empty home discipline", Executor: "fake",
		HomeDir: &emptyHome, DisciplineText: discipline,
	})
	if err != nil {
		t.Fatalf("空 HOME + 非空纪律应成功: %v", err)
	}
	reqs := profile.snapshot()
	if len(reqs) != 1 {
		t.Fatalf("Profile.Prepare 调用次数=%d, want 1（%+v）", len(reqs), reqs)
	}
	req := reqs[0]
	if req.HomeDir == "" || !filepath.IsAbs(req.HomeDir) {
		t.Fatalf("ProfileReq.HomeDir=%q, want 非空绝对路径", req.HomeDir)
	}
	if req.HomeDir != mainHome {
		t.Fatalf("写入点=%q, want 主 HOME %q", req.HomeDir, mainHome)
	}
	if req.Isolated {
		t.Fatal("空 HOME 沿用主 HOME，Isolated 必须 false")
	}
	if len(req.TaskOverlay) != 1 || req.TaskOverlay[0].Content != discipline {
		t.Fatalf("overlay=%+v, want 一条逐字节纪律", req.TaskOverlay)
	}
	if len(req.Rules) != 0 || len(req.Skills) != 0 {
		t.Fatalf("Rules/Skills 必须为空，got %d/%d", len(req.Rules), len(req.Skills))
	}
	got, gerr := st.GetTask(task.ID)
	if gerr != nil {
		t.Fatal(gerr)
	}
	if got.HomeDir != "" {
		t.Fatalf("Task.HomeDir 被回写为 %q，身份快照必须保持空串（B347）", got.HomeDir)
	}
	if got.State != proto.TaskStateRunning {
		t.Fatalf("任务状态=%s, want running", got.State)
	}
}

// 真落盘边界（防 recordingProfile 假绿）：真实 opencode.Profile.Prepare + 临时
// 主 HOME，断 overlay 真在 <写入点>/.handoff/task-overlay/<task>/discipline.md，
// 且载体全局规则/skills 树不被本次 overlay 触碰。
func TestB23314EmptyHomeDisciplineReallyWritesOverlay(t *testing.T) {
	mainHome := t.TempDir()
	withMainHome(t, mainHome)

	real := opencode.New(nil)
	ad := &profileRecordingAdapter{
		chanAdapter: &chanAdapter{evCh: make(chan executor.AdapterEvent, 1)},
		profile:     real.Profile(),
	}
	m, _, _ := newTestManagerWithAds(t, map[string]executor.Adapter{"fake": ad}, "fake")
	repo := initTestRepo(t)
	pid := registerTestProject(t, m, repo)

	emptyHome := ""
	task, err := m.Dispatch(context.Background(), agentd.DispatchReq{
		ProjectID: pid, Prompt: "real overlay", Executor: "fake",
		HomeDir: &emptyHome, DisciplineText: "REAL-OVERLAY",
	})
	if err != nil {
		t.Fatalf("Dispatch: %v", err)
	}
	overlay := filepath.Join(mainHome, ".handoff", "task-overlay", task.ID, "discipline.md")
	data, rerr := os.ReadFile(overlay)
	if rerr != nil {
		t.Fatalf("overlay 未真落盘 %q: %v", overlay, rerr)
	}
	if string(data) != "REAL-OVERLAY" {
		t.Fatalf("overlay 内容=%q, want REAL-OVERLAY", string(data))
	}
	rulePath := filepath.Join(mainHome, ".config", "opencode", "AGENTS.md")
	if _, serr := os.Stat(rulePath); !os.IsNotExist(serr) {
		t.Fatalf("载体全局规则不应被本次 overlay 写入: %q stat err=%v", rulePath, serr)
	}
}

// #19：主 HOME 不可得时拒派并落 failed，不静默吞纪律。
func TestB23314EmptyHomeDisciplineRejectsWhenMainHomeUnavailable(t *testing.T) {
	prev := userHomeDir
	userHomeDir = func() (string, error) { return "", errors.New("主 HOME 不可得（注入）") }
	t.Cleanup(func() { userHomeDir = prev })

	profile := &recordingProfile{}
	ad := &profileRecordingAdapter{
		chanAdapter: &chanAdapter{evCh: make(chan executor.AdapterEvent, 1)},
		profile:     profile,
	}
	m, st, _ := newTestManagerWithAds(t, map[string]executor.Adapter{"fake": ad}, "fake")
	repo := initTestRepo(t)
	pid := registerTestProject(t, m, repo)

	emptyHome := ""
	if _, err := m.Dispatch(context.Background(), agentd.DispatchReq{
		ProjectID: pid, Prompt: "no main home", Executor: "fake",
		HomeDir: &emptyHome, DisciplineText: "必须拒派",
	}); err == nil {
		t.Fatal("主 HOME 不可得必须拒派")
	}
	tasks, lerr := st.ListTasks()
	if lerr != nil || len(tasks) != 1 || tasks[0].State != proto.TaskStateFailed {
		t.Fatalf("拒派后应留一条 failed 任务: err=%v tasks=%+v", lerr, tasks)
	}
}
```

2. **跑红。** `go test ./internal/orchestration/ -run 'B23314' -count=1`。预期编译失败——`userHomeDir`、`taskProfileHome` 尚未存在；`TestB23314EmptyHomeDisciplinePreparesProfile` 断言的是今天必失败的路径（`manager.go:1060` 直接 return error）。原始报错贴台账。

3. **加包内主 HOME 缝并改 `expandCarrierHome`。** `internal/orchestration/manager.go` 在 `expandCarrierHome` 之前加：

```go
// userHomeDir 是本机主 HOME 取值缝（B233.14）：生产恒为 os.UserHomeDir；
// 测试替换它以覆盖「主 HOME 不可得必须拒派」（契约 #19、#18 的展开失败分支）。
var userHomeDir = os.UserHomeDir
```

   并把 `expandCarrierHome`（`:1125-1136`）内 `os.UserHomeDir()` 改为 `userHomeDir()`。

4. **新增 `taskProfileHome`。** 紧邻 `expandCarrierHome` 之后：

```go
// taskProfileHome 解析任务纪律 overlay 的落盘写入点（B233.14）。
//
// 语义：返回恒为非空路径的写入点——五家 Profile.Prepare 拒绝空 HomeDir，且
// overlay 必须落盘。非空 taskHome 经 expandCarrierHome 展开 ~ 前缀为绝对路径
// （修掉把 ~ 当目录名、在 cwd 建 ./~/.handoff 的既有缺陷）；空 taskHome
// （载体主 HOME）取本机主 HOME。展开后仍非绝对即报错，绝不静默落相对路径。
//
// 注意：Task.HomeDir 身份快照不由本函数改写（B347：空 HOME 是身份值），
// 本函数只回答「写到哪」。
func taskProfileHome(taskHome string) (string, error) {
	if taskHome != "" {
		writePoint := expandCarrierHome(taskHome)
		if !filepath.IsAbs(writePoint) {
			return "", fmt.Errorf("载体 HOME %q 展开后仍非绝对路径", taskHome)
		}
		return writePoint, nil
	}
	home, err := userHomeDir()
	if err != nil {
		return "", fmt.Errorf("无法解析本机主 HOME: %w", err)
	}
	if home == "" {
		return "", errors.New("无法解析本机主 HOME: 取值为空")
	}
	return home, nil
}
```

5. **改 `prepareTaskProfile`。** 删除 `:1060-1063` 的空 HOME 拒门，改为先算写入点，再构造 `ProfileReq`：

```go
func (m *Manager) prepareTaskProfile(ctx context.Context, start executor.StartReq) error {
	if strings.TrimSpace(start.Discipline) == "" {
		m.log.Info("任务纪律为空，跳过 Profile overlay", "task", start.Task.ID,
			"executor", start.Task.Executor, "overlay_skipped", true)
		return nil
	}
	provider, ok := m.ads[start.Task.Executor].(executor.Provider)
	if !ok {
		return fmt.Errorf("task=%q executor=%q home=%q 不提供 Profile 能力",
			start.Task.ID, start.Task.Executor, start.Task.HomeDir)
	}
	profile, ok := executor.ProfileFromProvider(provider)
	if !ok || profile == nil {
		return fmt.Errorf("task=%q executor=%q home=%q 没有 Profile 能力",
			start.Task.ID, start.Task.Executor, start.Task.HomeDir)
	}
	// B233.14：空 Task.HomeDir 是合法身份（沿用该机主 HOME），不再拒绝非空纪律。
	// 写入点与身份快照分离：写入点必为绝对路径，Task.HomeDir 保持不变（B347）。
	writePoint, err := taskProfileHome(start.Task.HomeDir)
	if err != nil {
		m.log.Error("任务纪律 overlay 写入点解析失败", "task", start.Task.ID,
			"executor", start.Task.Executor, "home", start.Task.HomeDir, "cause", err)
		return fmt.Errorf("task=%q executor=%q home=%q 解析纪律写入点: %w",
			start.Task.ID, start.Task.Executor, start.Task.HomeDir, err)
	}
	req := executor.ProfileReq{
		HomeDir:  writePoint,
		Isolated: start.Task.HomeDir != "",
		TaskOverlay: []executor.ProfileFile{{
			Name: filepath.Join(start.Task.ID, disciplineFileName), Content: start.Discipline,
		}},
	}
	m.log.Info("任务 Profile overlay 准备开始", "task", start.Task.ID,
		"executor", start.Task.Executor, "home", start.Task.HomeDir, "write_point", writePoint,
		"isolated", req.Isolated, "overlay_count", len(req.TaskOverlay),
		"content_bytes", len(start.Discipline))
	rep, err := profile.Prepare(ctx, req)
	if err != nil {
		m.log.Error("任务 Profile overlay 准备失败", "task", start.Task.ID,
			"executor", start.Task.Executor, "home", start.Task.HomeDir,
			"write_point", writePoint, "cause", err)
		return fmt.Errorf("task=%q executor=%q home=%q Profile.Prepare: %w",
			start.Task.ID, start.Task.Executor, start.Task.HomeDir, err)
	}
	if !rep.Prepared {
		err := fmt.Errorf("Profile.Prepare 返回 Prepared=false")
		m.log.Error("任务 Profile overlay 未准备完成", "task", start.Task.ID,
			"executor", start.Task.Executor, "home", start.Task.HomeDir,
			"write_point", writePoint, "cause", err)
		return fmt.Errorf("task=%q executor=%q home=%q: %w", start.Task.ID,
			start.Task.Executor, start.Task.HomeDir, err)
	}
	m.log.Info("任务 Profile overlay 准备完成", "task", start.Task.ID,
		"executor", start.Task.Executor, "home", start.Task.HomeDir, "write_point", writePoint,
		"overlay_count", len(req.TaskOverlay), "content_bytes", len(start.Discipline))
	return nil
}
```

   `errors`/`os`/`filepath`/`fmt` 均已在 `manager.go` import。

6. **跑绿。** `go test ./internal/orchestration/ -run 'B23314' -count=1`，预期 `ok`。

7. **变异可红（#20）。** 临时把 `prepareTaskProfile` 的 `writePoint, err := taskProfileHome(...)` 之前加回 `if start.Task.HomeDir == "" { return fmt.Errorf("... 无法准备非空纪律 Profile overlay") }`，跑步骤 6 命令，确认 `TestB23314EmptyHomeDisciplinePreparesProfile` 与 `...ReallyWritesOverlay` 变红；删掉该行恢复绿。原始输出贴台账。

8. **回归不回退（#17/#18）。** `go test ./internal/orchestration/ -run 'TestDispatchInjectsTaskDisciplineIntoProfile|TestDispatchPersistsDisciplineBeforeExecutorStarts|TestDispatchSkipsStartWhenPersistFails|TestDispatchRejectsDisciplineWithoutTaskProfile' -count=1` 绿；`go test ./internal/orchestration/ -count=1` 绿。

**判据**

- 空 HOME + 非空纪律：`Dispatch` 成功、任务 `running`；恰一次 `Profile.Prepare`，`req.HomeDir` 非空绝对且等于主 HOME、`Isolated=false`；`TaskOverlay` 恰一条含 `discipline.md` 且内容逐字节等于下发正文；`Rules`/`Skills` 空；读回 `Task.HomeDir == ""`。
- 真落盘：overlay 真在 `<主HOME>/.handoff/task-overlay/<task>/discipline.md`；`<主HOME>/.config/opencode/AGENTS.md` 不存在。
- 主 HOME 不可得：`Dispatch` 返回错误、任务落 `failed`、错误文本含 task/executor 上下文。
- 变异：恢复空 HOME 拒门 → 主判据红。
- 现状不回退：`TrimSpace` 空纪律仍跳过 `Profile.Prepare`；非空 HOME `Isolated=true`；`TestDispatch...` 既有四测绿；`./internal/orchestration/` 全绿。

**缺陷族对抗审查（d_orchestration，logic）**

- 生命周期/状态机中断：`prepareTaskProfile` 在 `Dispatch` 内、`CreateTask` 之后、`adapter.Start` 之前；失败走既有 `transitBestEffort(failed)`，无半状态。空 HOME 分支只读主 HOME、只写任务级 overlay，无残留资源。
- 静默失败/误导报错：纪律非空时要么 `Prepare` 成功、要么可行动错误落 `failed`；主 HOME 不可得显式报错（#19）；`taskProfileHome` 对「展开后仍非绝对」显式报错，不沿用 `expandCarrierHome` 的失败原样返回（否则相对路径落 cwd 是静默降级）。
- 跨平台：`userHomeDir` 在 Windows 走 `USERPROFILE`，overlay 路径走 `filepath`；真机差异进 §6。
- 假红/假绿：`recordingProfile` 只记请求，不替代真落盘——补 `TestB23314EmptyHomeDisciplineReallyWritesOverlay`（真 `opencode.Profile.Prepare` + 临时主 HOME）。
- 门禁绕过：不改权限/审批门；失败仍拒启动 executor。
- 序列化边界：无新增，见上。
- 枚举：无新增。
- 承重安全属性：`Task.HomeDir` 空串不回写（B347）有显式断言（变异：空 HOME 分支顺手 `task.HomeDir = writePoint` → 红，且现有测试不会自动红，故必须显式锁）；载体全局规则不被写由 `Rules/Skills` 恒空 + 真落盘断言共同锁。

**序列化边界设问（本卡）**：同上，无新增字段/DTO/投影；`Task.HomeDir` 的 `omitempty` 三态由 `TestTaskHomeDirJSON` 锁定，Task 4 回归。

**上下文预算检查**：Task 2 圈定 1 个生产文件 + 1 个新增测试文件；有界。

**类型标注**：logic 型 `d_orchestration` 由步骤 6/7/8 的机内断言锁定；真机项见 §6。

---

### Task 3：图对账与 assembly 复核

**步骤（逐条贴原始输出到台账）**

1. 复核 `codegraph/target.json`：`d_gateway→d_scheduling` 的 `interfaces` 含 `SchedulingClient`、`entries` 保留 `scheduling.Service` + `scheduling（包级函数）`、`legacyBudget` 仍 0；`assembly` 含 `cmd/agentd.go`。命令：

```text
python3 - <<'PY'
import json
d=json.load(open('codegraph/target.json'))
for c in d['contracts']:
    if c['from']=='d_gateway' and c['to']=='d_scheduling':
        print(c.get('legacyBudget'), c.get('interfaces'), c.get('entries'))
print('assembly', d.get('assembly'))
PY
```

2. `codegraph check --view cards-B233.14-charter --repo .`，目标相对基线不新增 fail/warn（基线：fails=6、warns=138；`--base` 对照 fails=7、warns=139）。
3. `codegraph check --repo .` 与 `codegraph validate --repo .` 退出码 0（validate）；贴原始输出。
4. `codegraph resolve --doc docs/superpowers/specs/b233.14-contract.md --repo .` 退出码 0；`codegraph resolve --doc docs/superpowers/plans/b233.14-plan.md --repo .` 逐锚可判。
5. 预期结果：本卡不改图文件（target/best/diff 已随 contract 冻结），`git status` 对 `codegraph/` 无变更；若工具诊断要求补记迁 `New` 后的边数，贴原始诊断并只改 `legacyBudgetNote`（不改方向/assembly/预算）。

**判据**：第 1–4 项读数与基线一致且不新增违规；第 5 项 `git status codegraph/` 干净。

---

### Task 4：收口验证

**步骤（逐条贴原始输出到台账）**

1. `go build ./...` exit 0。
2. `go vet ./internal/agentd/ ./internal/orchestration/ ./internal/scheduling/ ./cmd/` exit 0。
3. `go test ./internal/orchestration/ -count=1` 全绿。
4. `go test ./internal/agentd/ -run 'B23314' -count=1` 绿。
5. B233.13 回归：`go test ./internal/agentd/ -run 'TestB23313ConcurrentDoneReleasesOnce' -count=1` 绿。
6. B233.12 回归：`go test ./internal/orchestration/ -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1`；`go test ./internal/approval/ -run 'TestClientApproverAllowUndeliveredUntilAck' -count=1` 均绿。
7. 自证不回退（R1）：`go test ./internal/scheduling/ -run 'TestIdentityOfAndBindPhysical|TestResolveLookupTable' -count=1` 绿；`go test ./internal/scheduling/ -run 'TestPutCarrierDefaultsCredentialAndRequiresKnownMachine' -count=1` 绿。
8. 协议金样本：`go test ./internal/proto/ -run 'TestTaskHomeDirJSON' -count=1` 绿。
9. 结构断言：`rg -n 'scheduling\s+\*scheduling\.Service' internal/agentd/server.go` 应无匹配；`rg -n 'scheduling\.New\(' --glob '!*_test.go' internal/agentd/` 应无匹配；`rg -n 'scheduling\.New\(' --glob '!*_test.go' cmd/` 应仅命中 `cmd/agentd.go`。
10. 受影响包定向：`go test ./internal/agentd/ -count=1` 与 `go test ./cmd/ -count=1`，与 §0 基线逐条比对，**无新增失败**（基线存量红 `TestLegacyNodeEventSequenceUnchanged`、`TestRepoContractGate` 允许保留，台账逐条区分）。
11. Task 3 命令复跑。

**判据**：1–9 全过；10 只允许 §0 列出的两条存量红；11 与 Task 3 一致。

---

## 5. 自审三查

### 5.1 spec 覆盖（逐条指到 task）

| spec 用户故事 / 冻结条 | 承接 |
| --- | --- |
| 用户故事 1：前台生产字段类型是编制 client 接口，组装点仍能 new 并注入，fake 可用 | Task 1 步骤 1–4、7–9；Task 4 步骤 9 |
| 用户故事 2：点名已登记载体，开工环境 = 登记机器/CLI/HOME；另写一套不同值 → 失败 | Task 1 回归（既有 `receiver_dispatch_test.go`）；R1（`IdentityOf`/`BindPhysical` 绿） |
| 用户故事 3：空 HOME 载体 + 非空纪律能启动，纪律任务级可见，载体全局规则不被改写，`Task.HomeDir` 仍空 | Task 2 步骤 1（主判据 + 真落盘）、6、7 |
| 用户故事 4：B233.13 并发 Done 只释放一次、B233.12 免审不回退 | Task 4 步骤 5–6 |
| 冻结 §5.1 #1–#12 | Task 1 步骤 1–9、11；Task 3 步骤 1–2 |
| 冻结 §5.2 #13–#20 | Task 2 步骤 1–8 |
| 冻结 §5.3 #21–#23 | Task 4 步骤 7；不重写身份规则（无生产改动） |
| 冻结 §5.4 #24–#28 | Task 4 步骤 5–8；`Release` 签名不变（无改动） |
| 冻结 §5.5 #29–#32 | Task 3 步骤 1–5 |
| P1–P5 | Task 1（P1/P2/P5-W1）、Task 2（P3/P4/P5-W2） |

### 5.2 占位符扫描

- 无 TBD /「加适当的错误处理」/「同 Task N」/「描述做什么却不给代码」。Task 1、Task 2 的生产改动与新增测试均给完整代码块。
- **测试例外声明（纪律允许的唯一出口）**：Task 1 步骤 9 的既有测试迁移以「§3.3 逐文件处置表 + 五条可机械执行规则 + 指认既有 harness（`SetupAutomationForTest`、`mustScheduling`、`newSchedulingForTest`）」代替逐文件完整代码。理由：改动是编译驱动的机械迁移（接口化后 `SetupAutomation` 不再建服务、取具体实现），形态因白盒/外部包而异；每个文件的处置点已逐条列全，可判 pass/fail。
- **内部锁声明**：Task 2 的 `TestB23314TaskProfileHome` 各子用例入口 `taskProfileHome` 不在接缝 S2 的入口（`Dispatch→prepareTaskProfile`）上，属**附加内部锁**；合法理由的唯一形状——从声明缝（`Dispatch`）构造「主 HOME 不可得」以外的分支（非空绝对原样、tilde 展开、展开后非绝对报错）要么不可控、要么会污染同一进程的 `userHomeDir` 全局态，无法在单支缝级断言里穷举；故以直接单元断言附加，**不顶替** S2 缝级断言——`TestB23314EmptyHomeDisciplinePreparesProfile`（入口 `Manager.Dispatch`）与 `...ReallyWritesOverlay`（入口 `Manager.Dispatch`）仍在。
- **退路声明**：本计划无「若意外先绿就改成直喂 X」类条件退路；§3.3 迁移不改变任何测试入口符号。
- **源码守卫局限声明**：`TestB23314SchedulingNewOnlyInCmd` 是字符串扫描（剔 `//` 行注释、跳 `_test.go`），不解析别名与块注释；它只回答「gateway 生产文件有没有 new」，不回答「走组装点的用法对不对」。

### 5.3 跨 task 类型/签名一致性

- `SchedulingClient` 只在 `internal/agentd/scheduling_client.go` 定义（Ticket 0，18 方法）；Task 1/2/3 引用同名同形状，Task 1 不扩接口（`SetDefaultCarrier`/`SetKnownMachines` 不入）。
- Task 1 Produces 的 `SetScheduling(SchedulingClient)` / `Scheduling() SchedulingClient` / `SchedulingRegistry() schedclient.Registry` 是 Task 3 图对账与 Task 4 结构断言的唯一命名来源。
- Task 2 Produces 的 `taskProfileHome(string) (string, error)` 与 `userHomeDir func() (string, error)` 只在 `internal/orchestration` 包内；无跨包消费。
- `cmd/agentd.go#setupLedger` 的 `scheduling.New(reg)` 签名与 Task 1 的 `SchedulingRegistry()` 返回 `schedclient.Registry` 逐字对齐。

### 5.4 接缝覆盖（双向，对照 spec 测试决定的接缝清单）

- **测试 → 缝**（看入口符号，不看标注）：
  - `TestB23314SchedulingFieldIsInterface` / `TestB23314SchedulingSignatures` / `TestB23314FieldAcceptsInterfaceOnlyFake` / `TestB23314SetSchedulingRejectsTypedNil`：入口 `Server.scheduling`（reflect）与 `Server.SetScheduling`，落在 S1。
  - `TestB23314SchedulingNewOnlyInCmd`：入口是源码扫描，非符号调用；它是 S1「`scheduling.New` 不落 gateway」的**机械守卫**，属 S1 的补充锁（见下内部锁声明）。
  - `TestB23314EmptyHomeDisciplinePreparesProfile` / `...ReallyWritesOverlay` / `...RejectsWhenMainHomeUnavailable`：入口 `Manager.Dispatch`，调用链穿过 S2（`Dispatch→prepareTaskProfile`），落在 S2。
  - `TestB23314TaskProfileHome`：入口 `taskProfileHome`，**不在** S2 入口上——内部锁（见下）。
  - R1/R2：入口 `IdentityOf`/`BindPhysical`（R1）、`handleDone`/`bindApproval`（R2），在回归清单上，由既有测试承接。
- **缝 → 测试**：S1 至少被 `TestB23314FieldAcceptsInterfaceOnlyFake`（经 `Server` 字段）+ typed-nil 测试锁；S2 至少被 `TestB23314EmptyHomeDisciplinePreparesProfile`（经 `Manager.Dispatch`）+ 真落盘锁；R1/R2 由既有测试锁。双向不互相代替。
- **内部锁声明（唯一合法形状）**：`TestB23314SchedulingNewOnlyInCmd` 与 `TestB23314TaskProfileHome` 是内部锁/机械守卫。
  - `TestB23314SchedulingNewOnlyInCmd`：从 S1 的符号入口（`SetScheduling`/字段）**构造不出**「生产源码不含 `scheduling.New(`」这条断言——它是文本性质，不是运行期符号性质；故以源码扫描附加，不顶替 S1 的字段/注入/typed-nil 缝级断言。
  - `TestB23314TaskProfileHome`：从声明缝（`Dispatch`）构造「非空绝对原样 / tilde 展开 / 相对报错」分支要么不可控（`Dispatch` 的路径由工作区准备决定）、要么需替换进程级 `userHomeDir` 影响同 test binary 其它用例；故以直接单元断言附加，不顶替 `TestB23314EmptyHomeDisciplinePreparesProfile` 的缝级断言。逐条已在本节自我声明。
- **退路同闸**：§3.3 的迁移与 Task 1 步骤 6 的组装均不改变任何测试入口符号，故不触发本闸；无未声明的退路。

### 5.5 跨卡审计与派发前自审

- 本卡为 L3 轻档单卡，**不扇出子卡**（spec 禁 `B233.14.x`），故无「跨卡签名逐字比对 / spec 故事跨卡归属」的扇出审计；本卡内部按 §5.1/§5.3 自审。
- 本计划无任何驱动 handoff 派发系统自身的验收步骤；不涉及「本 task 由协调者执行，不派发」的标注。

## 6. 真机清单（未验证，需协调者执行；本节点不宣称通过）

1. **空 HOME 载体 + 非空纪律真机启动（承重）**：目标机登记 `HomeDir=""` 载体，派发非空纪律任务，确认任务能起、纪律在任务级可见（overlay 落 `<主HOME>/.handoff/task-overlay/<task>/discipline.md`）、`<主HOME>/.config/<harness>/AGENTS.md` 与 skills 树未被改写、`Task.HomeDir` 仍空。五家 harness 各抽一家或全跑。
2. **非空 `~` 前缀 HOME 的 overlay 落点**：登记 `~/.handoff/home/<name>` 载体 + 非空纪律，确认真实 overlay 落在展开后的绝对主 HOME 下、**未**在 agentd 工作目录下建出 `./~/.handoff/...`。
3. **主 HOME 不可得拒派**：以无法解析用户主目录的环境启动 agentd（或注入失败源），确认派发空 HOME + 非空纪律时任务落 `failed`、报文可行动，无静默吞纪律。
4. **真实 agentd 重启后编制服务重建**：重启后 `SetKnownMachines` 经 `Conf()` 读活配置的行为保持（控制台改机器清单不必重启）。
5. **跨平台矩阵**：Linux/macOS/Windows 执行机各启动一次并完成一轮空 HOME 带纪律派发；一个平台绿不得推广为全平台结论。
6. **基线存量红对账**：`TestLegacyNodeEventSequenceUnchanged`（agentd）与 `TestRepoContractGate`（cmd）为功能线既有存量，不在本卡验收范围，以协调者对账为准。

## 7. 台账

落点：`docs/superpowers/ledgers/2026-09-12-b233.14-plan-ledger.md`（与本计划同批提交）。边干边落：每确立一个事实（提交、命令与原始输出、放弃的尝试、判断）追加一行；提交事实用 amend 收进同批，收口判据是工作树干净，不 chase hash。
