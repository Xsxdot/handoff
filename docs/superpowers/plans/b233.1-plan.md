# B233.1 实现计划：派发事件流、Ask 与审批 client 闭环

状态：执行计划  
卡：B233.1  
定级：L3 轻档（单轮 implement，禁止扇出子卡）  
法定产出：`docs/superpowers/plans/b233.1-plan.md`  
事实台账：`docs/superpowers/ledgers/2026-09-06-b233.1-plan-ledger.md`

**已由 plan docs/superpowers/plans/b233.1-plan.md 吸收（2026-09-06）**（契约 §9 移交 plan 附区）

拍板结果（breakdown §0，提交 `9e7c32ba`）必须按字面执行，不得把未选方案写成事实：

| 岔口 | 本计划落地 |
|---|---|
| **P1(a)** | OpenCode adapter 持 `StartReq.Approval`，原生 `permission.asked` 到达后自己 `Request`/`Await`（经 `Authorize`），再 `RespondPermission`+`Acknowledge`。`handlePermission` 不再是 OpenCode 审批权威。Await 不得堵 SSE/中介循环。 |
| **P2(a)** | 复用键 `sha256(version + "\x00" + PermFingerprint)`；`tickets` 表与 `proto.Ticket` 零字段增量。禁止 P2(c)。 |
| **P3(a)** | Ask 四态继续用 `Answer`/`DeliveredAt`/`VoidAnswer`，不加列。 |
| **P4(a)** | `Authorize` 落在 `internal/executor`，不进 Adapter。 |
| **P5(a)** | Continue 经可选 `SnapshotApplier` 重写任务目录 `opencode.json` 再 `Send`，并探测 serve 是否重载；未重载报能力不足。禁止 P5(c)。 |
| **P6(a)** | 本卡写 `CHANGELOG.md` `[Unreleased]`：`permission_reuse` 不再唤醒 wait；OpenCode Ask 改 `RespondAsk`；原生免审改快照投影；拒绝理由不再吞下一条提问。 |

上游：已批准 spec `docs/superpowers/specs/b233.1.md`；已冻结契约 `docs/superpowers/specs/b233.1-contract.md`（提交 `e7637304`，96 条）；已拍板拆解 `docs/superpowers/specs/b233.1-breakdown.md`。

读者：零上下文、品味存疑。按步骤复制代码。不要发明第三条事件总线、不要给 `proto.Ticket` 加字段、不要把 `Consult` 送出 client、不要把 nil Approval 当全免审、不要回退 `Send` 答 Ask、不要改走付费 OpenCode 模型、不要 import `internal/permgate` 进 opencode 包、不要让编排 import `internal/executor/opencode`。

真机/探针 OpenCode 必须用 `opencode-go/deepseek-v4-flash`。本节点不派 Codex。

---

## 0. 硬边界（写进步骤，不是散文）

- 生产入口保持 `handoff dispatch` → `client.Dispatch` → `POST /api/tasks` → `Manager.Dispatch`。CLI/`cmd/` 与 HTTP 路由预期零改动；若必须改，停下来退回 contract。
- OpenCode 路径判定：`proto.Task.Executor`，空则回退 `cfg.Executor.Default`。名字等于 `"opencode"` 才走新边界（契约 §11 C-1）。
- OpenCode `Start`：`req.Approval == nil` 必须返回能力不足错误，**禁止**当全部免审。
- OpenCode Ask 回程：类型断言 `executor.AskResponder` 失败必须报能力不足，**禁止**回退 `Adapter.Send`。
- OpenCode 原生 `permission.asked`：**不再** `emit(Type:"permission")` 给 Manager。adapter 在独立 goroutine 里 `Authorize` → `RespondPermission` → `Acknowledge`。SSE/mapEvent 线程不得同步 `Await`。
- `Manager.handlePermission` 对 OpenCode 路径不再是权威；其它 harness 保持旧链。
- 复用键只加盐，不改表：`ReuseFingerprint(version, PermFingerprint(ev))` 写入既有 `tickets.fingerprint`。
- 不新增 `EventType`，不新增 `Ticket.Kind`，不新增 CLI 子命令。
- `d_execution → d_policy` 禁止。`d_execution → d_orchestration` 只许 `interfaces: ["ApprovalClient"]`（OpenCode 调注入的 client 时同提交写入 target）。
- `d_orchestration → d_execution` 预算 27、基线实数 26。`permFingerprintFor` 收成 `executor.PermFingerprint` 包装用掉第 27 条。若再调用 `NamespacedTicketID`/`ReuseFingerprint` 导致 `codegraph check` 超预算：把 `legacyBudget` 提到**实际边数**并在 `legacyBudgetNote` 列出新增符号。禁止为过闸去造零边节点。
- 日志用 `slog` / `m.log`，禁 `fmt.Print*`。Ask 全文与命令参数不得默认 Info（截断到 80  rune 的既有入口日志可保留，新代码不要加全文）。
- 真机项标「未验证，需真机」，机内不得写成绿。

---

## 1. 基线证据、图覆盖与最薄路径

### 1.1 本节点已实跑（HEAD `9e7c32ba`，未改实现）

| 范围 | 命令 | 实跑结果 |
|---|---|---|
| 触及包 | `go test ./internal/executor ./internal/executor/opencode ./internal/permgate ./internal/store ./internal/proto ./internal/client -count=1` | 退出 0；opencode 19.923s，client 9.686s，其余 <2s |
| Ticket 0 金样本 | `go test ./internal/executor -run 'TestNamespacedTicketIDGoldenVectors\|TestPermFingerprintGoldenVectors\|TestApprovalStatusLiterals\|TestStartReqApprovalSlotIsOptional' -count=1` | ok 0.339s |
| manager 编码对齐 | `go test ./internal/agentd -run 'TestPermFingerprintForMatchesExecutorCanonical\|TestGateDecisionGoldenVectors' -count=1` | ok 0.544s |
| isDeliverable 现状 | `go test ./internal/client -run 'TestPermissionAutoAllowIsNotDeliverable' -count=1` | ok 0.314s；`permission_reuse` **未**列入假分支（源码 `client.go:140-150`） |
| 旧 Send 正向 | `go test ./internal/executor/opencode -run 'TestSendRoutesToQuestionReplyWhenPending\|TestWriteTaskEnv$' -count=1` | ok 0.272s。今日绿是因为 Send 仍消费 pending、`bash["*"]=="allow"`。T1/T3 必须退役/改锁这些正向，留下 = 假绿或假红 |
| 全仓编译 | `go build -o /dev/null .` | 退出 0，stdout 空 |
| 图闸 | `codegraph --repo . --view cards-B233.1-charter-2 check` | `fails: []`；warns=145 存量，本卡不修 |

### 1.2 图与源码核对

查图命令：`codegraph --repo . --view cards-B233.1-charter-2 <子命令>`。

- `sym n_opencode_Adapter_Send_2`：`internal/executor/opencode/adapter.go:529`，domain 基线 `d_executor`（best 归 `d_execution_contract`，本卡不修归属）。
- `flow n_opencode_Adapter_Send_2`：degraded=false，7 步；`reqID != ""` 则走 reply。源码 `adapter.go:554-557` 仍 `takePendingQuestionSnapshot` → `replyPendingQuestion`。
- `flow n_agentd_Manager_handlePermission` / `handleQuestion` / `waitQuestion` / `n_opencode_WriteTaskEnv` / `n_client_isDeliverable` / `n_permgate_Gate_Judge`：**degraded=true，steps=[]**。禁止用 chain 冒充。已读源码。记图覆盖债。
- `who-calls handlePermission` 短名拒；节点 id `n_agentd_Manager_handlePermission` 仅 `handleEvent`（`manager.go:1788`）。
- `who-calls n_opencode_WriteTaskEnv`：`Adapter.Start`（`adapter.go:373`）。
- `who-calls n_store_Store_FindReusableGrant`：`reuseDecision`。
- `ApprovalClient` / `AskResponder` 无生产消费边（Ticket 0 空壳）。

符号锚（`resolve --doc` 认这种写法）：

`internal/executor/approval.go#ApprovalClient`、`internal/executor/ask.go#AskResponder`、`internal/executor/fingerprint.go#PermFingerprint`、`internal/executor/executor.go#StartReq`、`internal/executor/opencode/adapter.go#Adapter.Send`、`internal/executor/opencode/adapter.go#Adapter.Start`、`internal/executor/opencode/adapter.go#mapPermissionAsked`、`internal/executor/opencode/taskenv.go#WriteTaskEnv`、`internal/agentd/manager.go#Manager.Dispatch`、`internal/agentd/manager.go#Manager.Continue`、`internal/agentd/manager.go#Manager.RelayAnswer`、`internal/agentd/manager.go#Manager.handleQuestion`、`internal/agentd/manager.go#Manager.waitQuestion`、`internal/agentd/manager.go#Manager.handlePermission`、`internal/agentd/manager.go#Manager.Stop`、`internal/client/client.go#isDeliverable`、`internal/store/store.go#AnswerTicket`、`internal/store/store.go#FindReusableGrant`、`internal/permgate/permgate.go#Gate.Judge`、`internal/agentd/server.go#Server.handleReply`、`cmd/agentd.go`（组装点 `NewManager`）。

库行为（禁止凭记忆）：

1. `sha256.Sum256` + `hex.EncodeToString`：与既有 `permFingerprintHash` 同形（`internal/executor/fingerprint.go:35-37`）。复用加盐用 `version + "\x00" + permFP` 的字节，不是 JSON。
2. `encoding/json.Marshal` 对 `map[string]string` 按键排序（Go 1 保证）。`opencode.json` 的 bash 表输出确定。
3. SQLite `UPDATE … WHERE answer IS NULL`：`RowsAffected()==0` 今日被译成 `ErrNotFound`（`store.go:1255-1267`）。T5 改判。
4. OpenCode serve 是否重读 `OPENCODE_CONFIG`：**未验证，需真机**。机内不得假装已重载。

### 1.3 最薄路径

本卡要锁的行为今天从声明缝调用**还得不到**：OpenCode `Send` 仍消费 pending Ask；`AskResponder` 无实现；`Dispatch` 不注入 `Approval`；`permission_reuse` 仍可交付；`AnswerTicket` 二次 404；`WriteTaskEnv` 写死 `edit:allow` 与 `bash["*"]=allow`；拒绝理由仍 `takeDenyGuidance` 吞下一条提问。Ticket 0 空壳不是活路径。

DAG 第一条 = T1（点亮 `RespondAsk` + `Send` 不消费）。T2/T3 文件集不相交，可在 T1 之后按序落地。T4 必须看到三者的实现而不是空壳。

```
Ticket 0（已冻结 e7637304）
   │
   ▼
  T1 Ask 回程（Send 停消费 + OpenCode.RespondAsk）
   │
   ├─→ T2 审批 client 实现 + 快照 + 复用加盐 + Authorize
   │
   └─→ T3 原生投影 ⊆ 快照（WriteTaskEnv）
            │
            ▼
           T4 接线：Dispatch/Continue 注入、OpenCode 退役权威、isDeliverable
            │
            ▼
           T5 取消 / 幂等 / 迟到
            │
            ▼
           T6 接缝回归 + CHANGELOG + 真机清单（协调者）
```

---

## 2. 跨 task 冻结接口（逐字，邻居只看得见这个）

**Produces（T1，T4 Consumes 必须逐字符相同）：**

```go
// internal/executor/opencode/adapter.go
var _ executor.AskResponder = (*Adapter)(nil)
func (a *Adapter) Send(ctx context.Context, taskID, text string) (err error)
func (a *Adapter) RespondAsk(ctx context.Context, req executor.RespondAskReq) error
```

**Produces（T2，T3/T4 Consumes）：**

```go
// internal/executor/fingerprint.go
func ReuseFingerprint(version, permFP string) string

// internal/executor/authorize.go
func Authorize(ctx context.Context, client ApprovalClient, req ApprovalRequest) (ApprovalDecision, error)

// internal/agentd/approval_client.go
func (m *Manager) bindApproval(taskID string, snap executor.PolicySnapshot) executor.ApprovalClient
func HashPolicyVersion(blacklist []string, approverOn bool, safeIDs []string, scope executor.ApprovalScope) string

// 既有签名保持：
func (s *Store) FindReusableGrant(taskID, fingerprint string) (*proto.Ticket, error)
```

**Produces（T3，T4 Consumes）：**

```go
// internal/executor/opencode/taskenv.go
func WritePermissionConfig(taskDir, model string, snap executor.PolicySnapshot) (configPath string, err error)
func WriteTaskEnv(taskDir, taskID, model, planContent, disciplineBlock string, snap executor.PolicySnapshot) (configPath, promptPath string, err error)

var ErrUnrepresentablePolicy = errors.New("快照含无法在原生配置实施的明确禁止")
```

**Produces（T4）：**

```go
// internal/executor/snapshot.go
type SnapshotApplier interface {
    ApplySnapshot(ctx context.Context, taskID string, snap PolicySnapshot) error
}

// internal/executor/resume.go（ResumeReq 增字段，与 StartReq.Approval 同语义）
type ResumeReq struct {
    // …既有字段保持…
    Approval ApprovalClient // nil = 本任务不走新 client；OpenCode 冷恢复必须非 nil
}

func isDeliverable(t proto.EventType) bool // 假集合加入 EventTypePermissionReuse
```

**Produces（T5）：**

```go
var ErrTicketConflict = errors.New("工单已有不同答案")
func (s *Store) AnswerTicket(id, answer string) error // 相同答案第二次返回 nil
func (s *Store) AnswerTicketApplied(id, answer string) (applied bool, err error)
// applied=true：本次写入答案；applied=false 且 err=nil：相同答案幂等
```

**Consumes（各 task 对既有缝）：** 见各 task Interfaces 节。不得改：

```go
type Adapter interface {
    Start(ctx context.Context, req StartReq) error
    Events(taskID string) <-chan AdapterEvent
    Send(ctx context.Context, taskID, text string) error
    RespondPermission(ctx context.Context, taskID, permID, decision, reason string) error
    Stop(taskID string) error
}
func (c *Client) Dispatch(ctx context.Context, opts DispatchOpts) (*proto.Task, error)
func (c *Client) Reply(ctx context.Context, taskID, ticketID, answer string) error
func composeAnswer() (string, error) // --approve→allow，--deny→deny / deny: reason
```

---

## 3. Task 1：Ask 回程——拆 Send 兼答、落地 RespondAsk

### 精确文件集

- `internal/executor/opencode/adapter.go`（`Send`、新增 `RespondAsk`、`replyPendingQuestion` 改为仅供 RespondAsk）
- `internal/executor/opencode/question.go`（不改纯函数；RespondAsk 复用 `parseQuestionAnswers`）
- `internal/executor/opencode/question_test.go`（退役旧正向，加新断言）
- `internal/executor/ask.go`（不改签名）

不得改其它 harness 的 `Send`。不得在本 task 改 `handleQuestion`/`RelayAnswer`（T4）。

### Interfaces

Consumes：`executor.AskResponder`、`executor.RespondAskReq`、`API.ReplyQuestion`、`API.PromptAsync`、`parseQuestionAnswers`。  
Produces：OpenCode `Send` 在有 pending 时 **零次** `ReplyQuestion`；`RespondAsk` 命中原 `QuestionID`。

### 基线判据先跑

```bash
go test ./internal/executor/opencode -run 'TestSendRoutesToQuestionReplyWhenPending|TestSendFallsBackToPromptWhenNoPendingQuestion|TestSendUnparsableAnswerRepromptsAndKeepsPending|TestSendCustomRejectedByServerRepromptsAndKeepsPending' -count=1
```

今日预期：全绿。实现后 `TestSendRoutesToQuestionReplyWhenPending` 必须不存在或改锁「Send 不打 `/question/`」。

测试范围：`go test ./internal/executor/opencode ./internal/executor -count=1`。

### 步骤

1. **写失败测试（缝：`Adapter.Send` / `AskResponder.RespondAsk`）**  
   夹具照抄 `internal/executor/opencode/question_test.go` 的 `httptest` + `newTestAdapter`（定义在 `reconcile_internal_test.go`）+ `a.newRun` + `drainOne`。把 `TestSendRoutesToQuestionReplyWhenPending` **整函数替换**为下面两支（同文件，同包 `opencode`）：

```go
func TestSendDoesNotConsumePendingAsk(t *testing.T) {
	var paths []string
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		paths = append(paths, r.URL.Path)
		w.WriteHeader(http.StatusOK)
	}))
	defer srv.Close()

	a := newTestAdapter(t)
	dir := t.TempDir()
	r := a.newRun("task-1", dir, dir)
	r.session = "ses_a"
	r.api = NewAPI(srv.URL, "pw")
	r.pendingQuestionID = "req_1"
	r.pendingQuestions = []QuestionInfo{{Options: []QuestionOption{{Label: "甲"}, {Label: "乙"}}}}

	if err := a.Send(context.Background(), "task-1", "1.2"); err != nil {
		t.Fatalf("Send 返回错误: %v", err)
	}
	for _, p := range paths {
		if strings.Contains(p, "/question/") {
			t.Fatalf("Send 不得打到 question 端点，实际 paths=%v", paths)
		}
	}
	if r.pendingQuestionID != "req_1" {
		t.Fatalf("Send 之后 pending 必须仍在，got %q", r.pendingQuestionID)
	}
}

func TestRespondAskRepliesPendingQuestion(t *testing.T) {
	var gotPath, gotBody string
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		gotPath = r.URL.Path
		b, _ := io.ReadAll(r.Body)
		gotBody = string(b)
		w.WriteHeader(http.StatusOK)
	}))
	defer srv.Close()

	a := newTestAdapter(t)
	dir := t.TempDir()
	r := a.newRun("task-1", dir, dir)
	r.session = "ses_a"
	r.api = NewAPI(srv.URL, "pw")
	r.pendingQuestionID = "req_1"
	r.pendingQuestions = []QuestionInfo{{Options: []QuestionOption{{Label: "甲"}, {Label: "乙"}}}}

	err := a.RespondAsk(context.Background(), executor.RespondAskReq{
		TaskID: "task-1", TicketID: "task-1:req_1", QuestionID: "req_1", Answer: "1.2",
	})
	if err != nil {
		t.Fatalf("RespondAsk: %v", err)
	}
	if gotPath != "/question/req_1/reply" {
		t.Fatalf("path = %q，期望 reply 端点", gotPath)
	}
	if !strings.Contains(gotBody, "乙") {
		t.Errorf("body = %q，期望含折算 label 乙", gotBody)
	}
	if r.pendingQuestionID != "" {
		t.Error("应答成功后必须清掉挂起请求")
	}
}

func TestRespondAskDoesNotStartPromptWhenPending(t *testing.T) {
	var paths []string
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		paths = append(paths, r.URL.Path)
		w.WriteHeader(http.StatusOK)
	}))
	defer srv.Close()
	a := newTestAdapter(t)
	dir := t.TempDir()
	r := a.newRun("task-1", dir, dir)
	r.session = "ses_a"
	r.api = NewAPI(srv.URL, "pw")
	r.pendingQuestionID = "req_1"
	r.pendingQuestions = []QuestionInfo{{Custom: true, Options: []QuestionOption{{Label: "甲"}}}}

	if err := a.RespondAsk(context.Background(), executor.RespondAskReq{
		TaskID: "task-1", TicketID: "task-1:req_1", QuestionID: "req_1", Answer: "自定义",
	}); err != nil {
		t.Fatalf("RespondAsk: %v", err)
	}
	for _, p := range paths {
		if strings.Contains(p, "/session/") && strings.Contains(p, "prompt") {
			t.Fatalf("原生仍挂起时不得 Prompt，paths=%v", paths)
		}
	}
}

func TestRespondAskContinuesWhenTurnEnded(t *testing.T) {
	var paths []string
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		paths = append(paths, r.URL.Path)
		w.WriteHeader(http.StatusOK)
	}))
	defer srv.Close()
	a := newTestAdapter(t)
	dir := t.TempDir()
	r := a.newRun("task-1", dir, dir)
	r.session = "ses_a"
	r.api = NewAPI(srv.URL, "pw")
	// pending 空 = 提问回合已结束
	err := a.RespondAsk(context.Background(), executor.RespondAskReq{
		TaskID: "task-1", TicketID: "task-1:req_old", QuestionID: "req_old", Answer: "继续：用方案 A",
	})
	if err != nil {
		t.Fatalf("RespondAsk: %v", err)
	}
	joined := strings.Join(paths, ",")
	if !strings.Contains(joined, "/session/ses_a/") {
		t.Fatalf("回合已结束应以关联 Prompt 续接，paths=%v", paths)
	}
	if strings.Contains(joined, "/question/") {
		t.Fatalf("无挂起时不得 ReplyQuestion，paths=%v", paths)
	}
}

var _ executor.AskResponder = (*Adapter)(nil)
```

把 `TestSendUnparsableAnswerRepromptsAndKeepsPending` 与 `TestSendCustomRejectedByServerRepromptsAndKeepsPending` 的入口从 `a.Send(...)` 改成 `a.RespondAsk(..., Answer: 原文)`，断言保持：折算失败不触达服务端、pending 保留、重发 question 事件。`TestSendFallsBackToPromptWhenNoPendingQuestion` 保留（无 pending 时 Send 走 prompt）。

2. **跑红**  
   `go test ./internal/executor/opencode -run 'TestSendDoesNotConsumePendingAsk|TestRespondAskRepliesPendingQuestion|TestRespondAskDoesNotStartPromptWhenPending|TestRespondAskContinuesWhenTurnEnded' -count=1`  
   预期：`RespondAsk` undefined（编译红）或 `Send` 仍打 `/question/`（断言红）。确认不是拼写错。

3. **最小实现**  
   在 `adapter.go` 的 `Adapter` 上增加编译断言（文件内、`Send` 附近）：

```go
var _ executor.AskResponder = (*Adapter)(nil)
```

   替换 `Send` 提问分流（删 `if reqID, qs := r.takePendingQuestionSnapshot(); reqID != ""` 整段），`Send` 在 `BeginTurn`/日志之后直接：

```go
	return r.api.PromptAsync(ctx, r.session, text)
```

   新增 `RespondAsk`（放在 `replyPendingQuestion` 之前）。完整函数：

```go
// RespondAsk 按问题身份回答：原生仍挂起则 ReplyQuestion 且不开新 Prompt；
// 提问回合已结束则以带 TicketID 关联的 Prompt 续接。
//
// 参数：req.TaskID 必须命中运行态；req.TicketID 必填（尝试身份）；req.QuestionID
// 可空，空时用 NativeIDFromTicket(taskID, ticketID)。
// 注意：不得按「当前唯一挂起槽」寻址——只给 TaskID 不合法。
func (a *Adapter) RespondAsk(ctx context.Context, req executor.RespondAskReq) (err error) {
	if req.TaskID == "" || req.TicketID == "" {
		return fmt.Errorf("RespondAsk 必须携带 TaskID 与 TicketID")
	}
	r := a.lookup(req.TaskID)
	if r == nil {
		return fmt.Errorf("任务 %s: %w", req.TaskID, executor.ErrTaskNotRunning)
	}
	select {
	case <-r.stopCh:
		return fmt.Errorf("任务 %s 已停止（运行态保留待回收），不能答问", req.TaskID)
	default:
	}
	native := req.QuestionID
	if native == "" {
		native = executor.NativeIDFromTicket(req.TaskID, req.TicketID)
	}
	a.log.Info("RespondAsk 进入", "task", req.TaskID, "ticket", req.TicketID,
		"question", native, "answer_runes", len([]rune(req.Answer)))
	defer func() {
		if err != nil {
			a.log.Error("RespondAsk 失败", "task", req.TaskID, "ticket", req.TicketID, "cause", err)
		} else {
			a.log.Info("RespondAsk 完成", "task", req.TaskID, "ticket", req.TicketID, "question", native)
		}
	}()
	reqID, qs := r.takePendingQuestionSnapshot()
	if reqID != "" && (native == "" || reqID == native) {
		return a.replyPendingQuestion(ctx, r, reqID, qs, req.Answer)
	}
	// 回合已结束：关联身份后续接，不得假装这是无身份 Send。
	text := "【回答工单 " + req.TicketID + "】\n" + req.Answer
	if err := r.frames.BeginTurn("respond_ask", text); err != nil {
		a.log.Warn("写 turn_start 帧失败，不影响回合", "task", req.TaskID, "cause", err)
	}
	return r.api.PromptAsync(ctx, r.session, text)
}
```

   `Send` 的注释把「有挂起 question 时改 reply」改成：「Send 只发普通续接；答问走 RespondAsk。有 pending 时 Send 不得消费它。」

4. **跑绿**  
   `go test ./internal/executor/opencode -run 'TestSendDoesNotConsumePendingAsk|TestRespondAsk|TestSendFallsBack|TestSendUnparsable|TestSendCustomRejected|TestStopRejectsPendingQuestion' -count=1`  
   然后 `go test ./internal/executor/opencode ./internal/executor -count=1`。

5. **加日志**  
   `RespondAsk` 入口/成功/失败已在上块。`Send` 保持既有续接日志；成功路径不得静默。ReplyQuestion 失败仍走 `replyPendingQuestion` 的 Error 返回（T5 记 delivery_failed）。

6. **加注释**  
   `RespondAsk` 文档注释含参数/返回/注意事项（已写）。`Send` 更新 why：拆开是为了冻结 9/94，不是风格。

### 验收

- `TestSendDoesNotConsumePendingAsk` 命中且 ok。  
- `TestRespondAskRepliesPendingQuestion` 命中且 ok。  
- 原生挂起：`PromptAsync` 路径计数 0（`TestRespondAskDoesNotStartPromptWhenPending`）。  
- 回合结束：Prompt 带 `【回答工单` 前缀。  
- 编译断言 `var _ executor.AskResponder = (*Adapter)(nil)`。  
- 缺陷族：pending 快照只读不清、成功后才清（保持 `takePendingQuestionSnapshot`）。假绿——旧 `TestSendRoutesToQuestionReplyWhenPending` 不得残留。

---

## 4. Task 2：审批 client 实现、政策快照、复用加盐、Authorize

### 精确文件集

- 新增 `internal/executor/authorize.go`
- 新增 `internal/executor/authorize_test.go`
- `internal/executor/fingerprint.go`（加 `ReuseFingerprint`）
- `internal/executor/fingerprint_test.go`
- 新增 `internal/agentd/approval_client.go`
- 新增 `internal/agentd/approval_client_test.go`
- `internal/agentd/manager.go`（`permFingerprintFor` 收成包装；不改 handlePermission 权威，那是 T4）
- `internal/permgate/permgate.go`（导出 `SafeCommandIDs() []string`，供 Version 哈希；不改 `Judge` 语义）

OpenCode 子包本 task 零 import permgate。

### Interfaces

Consumes：`ApprovalClient` 四方法、`Gate.Judge`、`Approver.Decide`、`Store.FindReusableGrant`、`Store.CreateTicket`、`executor.PermFingerprint`、`executor.TruncationMarker`、`executor.OneShotArgs`（审批模型 argv，不得借用 OpenCode 会话）。  
Produces：`Authorize`、`ReuseFingerprint`、`Manager.bindApproval`、`HashPolicyVersion`。

### 基线判据先跑

```bash
go test ./internal/executor -run 'TestPermFingerprintGoldenVectors|TestApprovalStatusLiterals' -count=1
go test ./internal/agentd -run 'TestPermFingerprintForMatchesExecutorCanonical|TestPermissionReuseSkipsSecondTicket|TestPermissionReuseIgnoresDeny' -count=1
go test ./internal/permgate -count=1
```

今日预期全绿。`TestPermissionReuseSkipsSecondTicket` 用的是未加盐指纹；T4 接线后复用走加盐键，本 task 先让 client 单测锁加盐，旧 manager 测试 T4 再改调用。

测试范围：`go test ./internal/executor ./internal/agentd ./internal/store ./internal/permgate -count=1`（agentd 包测试久，实现中循环用 `-run` 收窄到本 task 新测试 + 上列复用测试）。

### 步骤

1. **写失败测试**  
   `internal/executor/fingerprint_test.go` 追加编码金样本（与 Ticket 0 同形；行为锁在 client.Request）：

```go
func TestReuseFingerprintSaltsVersion(t *testing.T) {
	perm := PermFingerprint(AdapterEvent{
		Text: "bash: echo hello",
		Perm: &PermRequest{Tool: PermToolBash, Command: "echo hello"},
	})
	a := ReuseFingerprint("ver-1", perm)
	b := ReuseFingerprint("ver-2", perm)
	c := ReuseFingerprint("ver-1", perm)
	if a == perm {
		t.Fatal("加盐后不得等于裸 PermFingerprint")
	}
	if a == b {
		t.Fatal("不同 Version 必须不同复用键")
	}
	if a != c {
		t.Fatal("同一 Version+指纹必须稳定")
	}
	if len(a) != 64 {
		t.Fatalf("hex sha256 长度=%d", len(a))
	}
}
```

   新增 `internal/executor/authorize_test.go`（内部锁，占位符扫描节已声明：从四方法缝构造不出「pending 后漏 Await」）：

```go
package executor_test

import (
	"context"
	"testing"
	"time"

	"github.com/Xsxdot/handoff/internal/executor"
)

type pendingThenAllow struct {
	awaited int
}

func (pendingThenAllow) PolicySnapshot(context.Context) (executor.PolicySnapshot, error) {
	return executor.PolicySnapshot{Version: "v"}, nil
}
func (s *pendingThenAllow) Request(context.Context, executor.ApprovalRequest) (executor.ApprovalResult, error) {
	return executor.ApprovalResult{
		Ref:      executor.ApprovalRef{ID: "ref-1"},
		Decision: executor.ApprovalDecision{Status: executor.ApprovalPending},
	}, nil
}
func (s *pendingThenAllow) Await(context.Context, executor.ApprovalRef) (executor.ApprovalDecision, error) {
	s.awaited++
	return executor.ApprovalDecision{Status: executor.ApprovalAllow, Rule: "human"}, nil
}
func (*pendingThenAllow) Acknowledge(context.Context, executor.ApprovalAck) error { return nil }

func TestAuthorizeAwaitsWhenPending(t *testing.T) {
	s := &pendingThenAllow{}
	d, err := executor.Authorize(context.Background(), s, executor.ApprovalRequest{NativeID: "p1"})
	if err != nil {
		t.Fatal(err)
	}
	if d.Status != executor.ApprovalAllow {
		t.Fatalf("status=%q", d.Status)
	}
	if s.awaited != 1 {
		t.Fatalf("Await 次数=%d，pending 必须 Await", s.awaited)
	}
}

func TestAuthorizeNilClientIsNotAllow(t *testing.T) {
	d, err := executor.Authorize(context.Background(), nil, executor.ApprovalRequest{NativeID: "p1"})
	if err == nil {
		t.Fatal("nil client 必须错误")
	}
	if d.Status == executor.ApprovalAllow {
		t.Fatal("nil client 不得 allow")
	}
}

func TestAuthorizeCancelDoesNotLeak(t *testing.T) {
	s := &pendingThenAllow{}
	ctx, cancel := context.WithCancel(context.Background())
	cancel()
	_, err := executor.Authorize(ctx, s, executor.ApprovalRequest{NativeID: "p1"})
	if err == nil {
		t.Fatal("已取消 ctx 应返回错误（Await 见 ctx.Err）")
	}
	_ = time.Second
}
```

   将 `pendingThenAllow.Await` 改为尊重 ctx：

```go
func (s *pendingThenAllow) Await(ctx context.Context, _ executor.ApprovalRef) (executor.ApprovalDecision, error) {
	s.awaited++
	if err := ctx.Err(); err != nil {
		return executor.ApprovalDecision{}, err
	}
	return executor.ApprovalDecision{Status: executor.ApprovalAllow, Rule: "human"}, nil
}
```

   `internal/agentd/approval_client_test.go`：照抄 `newTestManager` / `mustCreateTask` / `newTestGate`（`manager_test.go`）。断言逐条：

   1. 范围内 edit（`PermToolEdit` + 路径落在 `Workdir`）→ `Request` 立即 `allow`，store 中 `permission_request` 事件数为 0，可有 `permission_auto_allow`。  
   2. `Text` 含 `executor.TruncationMarker` → `Request` 不得 `allow`（pending 或 deny）。  
   3. `Request` 返回的 `Decision.Status` 只属于 `{allow,deny,pending}`，不得出现 `"consult"`。  
   4. 同 Task + 同 Version + 同 `PermFingerprint` + 已 `Acknowledge(delivered)` 的 allow → 第二次 `Request` 为 allow，且 `permission_request` 仍为 0，有 `permission_reuse` 入库。  
   5. 换 Version 后同指纹不得复用。  
   6. 不同 TaskID 的 client 不得复用对方的 grant。  
   7. `Acknowledge` 三次：`formed` 后 ticket.Answer 非空或有 auto-allow 事件；`delivered` 后 `DeliveredAt` 非空；`executed` 不把 `DeliveredAt` 清掉、且与 delivered 可分（日志/内部计数，测试间谍记录 stage 切片为 `formed,delivered,executed`）。  
   8. client 构造绑定 T1，`Request` 的 NativeID 只在 T1 命名空间建单。  
   9. 注入 Judge 失败 / 审批超时：构造 `Approver` 让 `Decide` 阻塞超过 Timeout 或返回错误 → 不得 allow。  
   10. 审批路径不得调用 OpenCode session API：注入 `Approver.runCmd`（`approver.go` 未导出字段，本包测试可直接赋），记录 argv；断言 argv 来自 `executor.OneShotArgs(approverExecutor, model, prompt)`，不得出现 OpenCode session `/session/` 或 `PromptAsync`。`Approver.Decide` 真实签名是 `Decide(ctx, permission, taskSummary string) ApproverDecision`，字段 `Approve bool`、`Reason string`、`Err error`（不是 Timeout/Allow）。

   完整测试代码（同一文件；harness：`newTestManager` / `mustCreateTask`）：

```go
package agentd

import (
	"context"
	"strings"
	"testing"
	"time"

	"github.com/Xsxdot/handoff/internal/executor"
	"github.com/Xsxdot/handoff/internal/proto"
)

func TestApprovalClientInScopeEditAllowsWithoutPermissionRequest(t *testing.T) {
	m, st, _, _ := newTestManager(t)
	taskID := "T-edit"
	mustCreateTask(t, st, &proto.Task{
		ID: taskID, RepoPath: t.TempDir(), WorkDir: t.TempDir(),
		State: proto.TaskStateRunning, Executor: "fake",
	})
	task, _ := st.GetTask(taskID)
	scope := executor.ApprovalScope{
		Workdir:    task.Workdir(),
		TaskDir:    taskDirOf(m, taskID),
		TaskTmpDir: executor.TaskTmpDir(m.cfg.DataDir, taskID),
	}
	c := m.bindApproval(taskID, executor.PolicySnapshot{Version: "v1", TaskID: taskID, Scope: scope})
	path := task.Workdir() + "/a.go"
	res, err := c.Request(context.Background(), executor.ApprovalRequest{
		NativeID: "perm-edit", Text: "edit " + path,
		Perm: &executor.PermRequest{Tool: executor.PermToolEdit, Paths: []string{path}},
	})
	if err != nil {
		t.Fatal(err)
	}
	if res.Decision.Status != executor.ApprovalAllow {
		t.Fatalf("status=%q want allow", res.Decision.Status)
	}
	evs, err := st.EventsFromAsc(taskID, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	for _, e := range evs {
		if e.Type == proto.EventTypePermissionRequest {
			t.Fatal("范围内写不得发布 permission_request")
		}
	}
}

func taskDirOf(m *Manager, taskID string) string {
	return m.cfg.DataDir + "/tasks/" + taskID
}
```

   其余断言 2–10 按同样 harness 各写一支 `TestApprovalClient*`，文件名 `approval_client_test.go`。事件列表用既有 `(*Store).EventsFromAsc(taskID, 0, 1000)`，禁止新 API。

2. **跑红**  
   `go test ./internal/executor -run 'TestReuseFingerprintSaltsVersion|TestAuthorize' -count=1`  
   预期：`ReuseFingerprint` / `Authorize` undefined。

3. **最小实现**

`internal/executor/fingerprint.go` 追加：

```go
// ReuseFingerprint 把政策版本盐进权限指纹。P2(a)：不改 Ticket 字段。
func ReuseFingerprint(version, permFP string) string {
	return permFingerprintHash(version + "\x00" + permFP)
}
```

`internal/executor/authorize.go` 整文件：

```go
// authorize.go —— ApprovalClient 的 Request/Await 合成。
//
// 职责：Request 后若 pending 则 Await，避免调用方把 pending 当终态。
// 边界：不进 Adapter；client == nil 不得解释成 allow。
package executor

import (
	"context"
	"fmt"
)

// Authorize 向 client 请求一次授权。Request 返回 pending 时 Await 直到终态。
//
// 参数：client 必须非 nil；req 为原生权限。
// 返回：终态 allow/deny；ctx 取消时返回错误，不泄漏 waiter。
func Authorize(ctx context.Context, client ApprovalClient, req ApprovalRequest) (ApprovalDecision, error) {
	if client == nil {
		return ApprovalDecision{}, fmt.Errorf("ApprovalClient 为空，不能当成免审")
	}
	res, err := client.Request(ctx, req)
	if err != nil {
		return ApprovalDecision{}, err
	}
	if res.Decision.Status == ApprovalPending {
		return client.Await(ctx, res.Ref)
	}
	if res.Decision.Status == ApprovalAllow || res.Decision.Status == ApprovalDeny {
		return res.Decision, nil
	}
	return ApprovalDecision{}, fmt.Errorf("审批 client 返回非法状态 %q", res.Decision.Status)
}
```

`internal/permgate/permgate.go` 在 `safeCommandID` 旁导出：

```go
// SafeCommandIDs 返回当前白名单的稳定 id 集合（排序后），供政策快照 Version 哈希。
func SafeCommandIDs() []string {
	return []string{
		"git-ledger-amend", "go-build", "go-test", "go-vet", "gofmt",
		"npm-test", "npm-run", "make",
		"ls", "cat", "grep", "which", "pwd", "head", "tail", "wc",
		"git-status", "git-diff", "git-log", "git-grep", "git-show",
		"git-blame", "git-cat-file", "git-rev-parse", "git-ls-files",
	}
}
```

另加 `safeCommandID` 里 `ls/cat/grep/which/pwd/head/tail/wc` 那些按 `fields[0]` 返回的 id（已写在上表）。实现时对照 `safeCommandID` 每一个 `return "<id>", true`，少一条 Version 会在加规则时静默不变。禁止另发明 id。

`internal/agentd/approval_client.go` 整文件（关键路径，按此落）：

```go
// approval_client.go —— 编排侧 ApprovalClient 实现（B233.1）。
//
// 职责：限定到单个 Task.ID 的政策快照、判定、升级、复用与 Ack。
// 边界：不 import opencode；Consult 不得出站；失败/超时/截断不得 allow。
package agentd

import (
	"context"
	"crypto/sha256"
	"encoding/hex"
	"encoding/json"
	"fmt"
	"sort"
	"strings"
	"time"

	"github.com/Xsxdot/handoff/internal/executor"
	"github.com/Xsxdot/handoff/internal/permgate"
	"github.com/Xsxdot/handoff/internal/proto"
	"github.com/Xsxdot/handoff/internal/store"
)

type taskApprovalClient struct {
	m      *Manager
	taskID string
	snap   executor.PolicySnapshot
}

func (m *Manager) bindApproval(taskID string, snap executor.PolicySnapshot) executor.ApprovalClient {
	if snap.TaskID == "" {
		snap.TaskID = taskID
	}
	return &taskApprovalClient{m: m, taskID: taskID, snap: snap}
}

func (c *taskApprovalClient) PolicySnapshot(context.Context) (executor.PolicySnapshot, error) {
	return c.snap, nil
}

func (c *taskApprovalClient) Request(ctx context.Context, req executor.ApprovalRequest) (executor.ApprovalResult, error) {
	c.m.log.Info("审批 Request", "task", c.taskID, "native", req.NativeID,
		"version", c.snap.Version, "truncated", req.Truncated)
	if req.NativeID == "" {
		return executor.ApprovalResult{}, fmt.Errorf("审批请求缺 NativeID")
	}
	ev := executor.AdapterEvent{PermissionID: req.NativeID, Text: req.Text, Perm: req.Perm}
	if req.Truncated || strings.Contains(req.Text, executor.TruncationMarker) {
		return c.escalate(ctx, ev, "描述截断，fail-closed")
	}
	verdict := c.m.judgePermission(c.taskID, ev)
	switch verdict.Action {
	case permgate.AutoAllow:
		ref := executor.ApprovalRef{ID: executor.NamespacedTicketID(c.taskID, req.NativeID)}
		c.recordAutoAllow(ev, verdict)
		return executor.ApprovalResult{Ref: ref, Decision: executor.ApprovalDecision{
			Status: executor.ApprovalAllow, Rule: verdict.Rule, Reason: verdict.Reason,
		}}, nil
	case permgate.Consult:
		if c.m.shouldConsultApprover(c.taskID) {
			return c.consult(ctx, ev)
		}
		return c.escalate(ctx, ev, verdict.Reason)
	default:
		return c.escalate(ctx, ev, verdict.Reason)
	}
}

func (c *taskApprovalClient) Await(ctx context.Context, ref executor.ApprovalRef) (executor.ApprovalDecision, error) {
	c.m.log.Info("审批 Await", "task", c.taskID, "ref", ref.ID)
	if d, ok, err := c.decisionFromStore(ref.ID); err != nil {
		return executor.ApprovalDecision{}, err
	} else if ok {
		return d, nil
	}
	ans, err := c.m.hub.WaitAnswer(ctx, ref.ID)
	if err != nil {
		if d, ok, serr := c.decisionFromStore(ref.ID); serr != nil {
			return executor.ApprovalDecision{}, serr
		} else if ok {
			return d, nil
		}
		c.m.log.Error("Await 取消且 store 无终态", "task", c.taskID, "ref", ref.ID, "cause", err)
		return executor.ApprovalDecision{}, err
	}
	return mapGateAnswer(ans), nil
}

func (c *taskApprovalClient) Acknowledge(ctx context.Context, ack executor.ApprovalAck) error {
	c.m.log.Info("审批 Acknowledge", "task", c.taskID, "ref", ack.Ref.ID,
		"stage", ack.Stage, "native", ack.NativeID)
	switch ack.Stage {
	case executor.AckFormed:
		return nil
	case executor.AckDelivered:
		if err := c.m.st.MarkTicketDelivered(ack.Ref.ID); err != nil {
			c.m.log.Error("Ack delivered 失败", "task", c.taskID, "ref", ack.Ref.ID, "cause", err)
			return err
		}
		return nil
	case executor.AckExecuted:
		return nil
	default:
		return fmt.Errorf("未知 AckStage %q", ack.Stage)
	}
}

func (c *taskApprovalClient) consult(ctx context.Context, ev executor.AdapterEvent) (executor.ApprovalResult, error) {
	if c.m.approver == nil {
		return c.escalate(ctx, ev, "审批者不可用")
	}
	dec := c.m.approver.Decide(ctx, ev.Text, "")
	if dec.Err != nil || !dec.Approve {
		c.m.log.Info("审批模型未放行，升级", "task", c.taskID, "perm", ev.PermissionID, "cause", dec.Err)
		return c.escalate(ctx, ev, "审批模型未形成允许")
	}
	ref := executor.ApprovalRef{ID: executor.NamespacedTicketID(c.taskID, ev.PermissionID)}
	return executor.ApprovalResult{Ref: ref, Decision: executor.ApprovalDecision{
		Status: executor.ApprovalAllow, Rule: "approver", Reason: dec.Reason,
	}}, nil
}

func (c *taskApprovalClient) escalate(ctx context.Context, ev executor.AdapterEvent, reason string) (executor.ApprovalResult, error) {
	ticketID := executor.NamespacedTicketID(c.taskID, ev.PermissionID)
	fp := executor.ReuseFingerprint(c.snap.Version, executor.PermFingerprint(ev))
	if prior, err := c.m.st.FindReusableGrant(c.taskID, fp); err != nil {
		c.m.log.Warn("复用查询失败，升级人工", "task", c.taskID, "cause", err)
	} else if prior != nil {
		if _, err := c.m.st.AppendEvent(c.taskID, proto.EventTypePermissionReuse, permissionReusePayload{
			TicketID: ticketID, PriorTicketID: prior.ID,
			Fingerprint: fp[:8], Permission: permEventText(ev.Text),
		}); err != nil {
			c.m.log.Error("追加 permission_reuse 失败", "task", c.taskID, "cause", err)
		}
		return executor.ApprovalResult{
			Ref:      executor.ApprovalRef{ID: ticketID},
			Decision: executor.ApprovalDecision{Status: executor.ApprovalAllow, Rule: "reuse", Reason: "复用工单 " + prior.ID},
		}, nil
	}
	c.m.transitBestEffort(c.taskID, proto.TaskStateWaitingAnswer, "permission_request")
	reqJSON, _ := json.Marshal(ticketRequest{Kind: "gate", Permission: ev.Text})
	if _, err := c.m.st.CreateTicket(&proto.Ticket{
		ID: ticketID, TaskID: c.taskID, Kind: "gate",
		Request: reqJSON, CreatedAt: time.Now().UTC(), Fingerprint: fp,
	}); err != nil {
		c.m.transitBestEffort(c.taskID, proto.TaskStateRunning, "权限工单创建失败回滚")
		return executor.ApprovalResult{}, err
	}
	evt, err := c.m.st.AppendEvent(c.taskID, proto.EventTypePermissionRequest, permissionPayload{
		TicketID: ticketID, Permission: permEventText(ev.Text), Kind: "gate",
	})
	if err != nil {
		c.m.log.Error("追加 permission_request 失败", "task", c.taskID, "cause", err)
		return executor.ApprovalResult{Ref: executor.ApprovalRef{ID: ticketID}, Decision: executor.ApprovalDecision{Status: executor.ApprovalPending}}, nil
	}
	c.m.hub.Publish(evt)
	return executor.ApprovalResult{
		Ref:      executor.ApprovalRef{ID: ticketID},
		Decision: executor.ApprovalDecision{Status: executor.ApprovalPending, Reason: reason},
	}, nil
}

func (c *taskApprovalClient) recordAutoAllow(ev executor.AdapterEvent, v permgate.Verdict) {
	c.m.autoAllowPermission(c.taskID, ev, v)
}

func (c *taskApprovalClient) decisionFromStore(ticketID string) (executor.ApprovalDecision, bool, error) {
	tk, err := c.m.st.GetTicket(ticketID)
	if err != nil {
		return executor.ApprovalDecision{}, false, nil
	}
	if tk.Answer == nil || *tk.Answer == store.VoidAnswer {
		return executor.ApprovalDecision{}, false, nil
	}
	return mapGateAnswer(*tk.Answer), true, nil
}

func mapGateAnswer(ans string) executor.ApprovalDecision {
	d, reason := gateDecision(ans)
	if d == "once" {
		return executor.ApprovalDecision{Status: executor.ApprovalAllow}
	}
	return executor.ApprovalDecision{Status: executor.ApprovalDeny, Reason: reason}
}

// HashPolicyVersion 对政策内容做稳定哈希。字段集：黑名单、审批者启用、安全命令表、范围三根。
func HashPolicyVersion(blacklist []string, approverOn bool, safeIDs []string, scope executor.ApprovalScope) string {
	bl := append([]string(nil), blacklist...)
	sort.Strings(bl)
	ids := append([]string(nil), safeIDs...)
	sort.Strings(ids)
	type payload struct {
		Blacklist  []string `json:"blacklist"`
		ApproverOn bool     `json:"approver_on"`
		Safe       []string `json:"safe"`
		Workdir    string   `json:"workdir"`
		TaskDir    string   `json:"task_dir"`
		TaskTmp    string   `json:"task_tmp"`
	}
	b, _ := json.Marshal(payload{Blacklist: bl, ApproverOn: approverOn, Safe: ids,
		Workdir: scope.Workdir, TaskDir: scope.TaskDir, TaskTmp: scope.TaskTmpDir})
	sum := sha256.Sum256(b)
	return hex.EncodeToString(sum[:])
}
```

   `Approver.Decide` 内部已用 `a.timeout` 包 `context.WithTimeout`；超时走 `dec.Err != nil` 且 `Approve=false`，client 不得翻译成 allow。

   `permFingerprintFor` 改为：

```go
func permFingerprintFor(ev executor.AdapterEvent) string {
	return executor.PermFingerprint(ev)
}
```

4. **跑绿** 上列新测试 + `TestPermFingerprintForMatchesExecutorCanonical`。  
5. **日志**：Request 入口（task/native/version/truncated）、Await 入口、Ack 每阶段、每条错误、复用命中、升级。成功路径 Info。命令原文不打 Info。  
6. **注释**：文件头职责+边界；`Authorize`/`ReuseFingerprint`/`HashPolicyVersion`/`bindApproval` 文档注释。

### 验收

T2 ③ 行为化清单（截断 fail-closed、Consult 不出站、复用六条件、换 Version 反面、跨任务隔离、Ack 三分、nil 不在本文件当全免审——那是 T4 Start）。`go test ./internal/executor -run 'TestReuseFingerprint|TestAuthorize' -count=1` ok。

---

## 5. Task 3：OpenCode 原生 allow ⊆ 当前快照

### 精确文件集

- `internal/executor/opencode/taskenv.go`
- `internal/executor/opencode/taskenv_test.go`
- `internal/executor/opencode/adapter.go`（`Start` 调 `WriteTaskEnv` 的新签名；本 task 不接 client 权限桥）

### Interfaces

Consumes：`executor.PolicySnapshot`（Version 非空；Scope 三根）。  
Produces：`WritePermissionConfig`、`WriteTaskEnv` 新签名、`ErrUnrepresentablePolicy`。

### 基线判据先跑

```bash
go test ./internal/executor/opencode -run 'TaskEnv|BashRules|ExternalDirectory' -count=1
```

今日 `TestWriteTaskEnv` 断言 `bash["*"]=="allow"` 且 `edit=="allow"`。T3 后 `bash["*"]` 必须是 `"ask"`（无法精确表达无限制 bash ⊆ 快照）。旧正向必须改，留下 = 假绿。

测试范围：`go test ./internal/executor/opencode -run 'TaskEnv|Snapshot|Project|BashRules|ExternalDirectory' -count=1`。

### 步骤

1. **写失败测试**（缝：`Adapter.Start` → `WriteTaskEnv`/`WritePermissionConfig`；读盘上 JSON，不锁内部表顺序）

```go
func TestWritePermissionConfigSubsetOfSnapshot(t *testing.T) {
	dir := t.TempDir()
	snap := executor.PolicySnapshot{
		Version: "s1", TaskID: "T1",
		Scope: executor.ApprovalScope{Workdir: "/repo", TaskDir: "/task", TaskTmpDir: "/tmp/t"},
	}
	p, err := WritePermissionConfig(dir, "", snap)
	if err != nil {
		t.Fatal(err)
	}
	raw, err := os.ReadFile(p)
	if err != nil {
		t.Fatal(err)
	}
	var cfg struct {
		Permission struct {
			Edit              string            `json:"edit"`
			Bash              map[string]string `json:"bash"`
			Webfetch          string            `json:"webfetch"`
			ExternalDirectory string            `json:"external_directory"`
		} `json:"permission"`
	}
	if err := json.Unmarshal(raw, &cfg); err != nil {
		t.Fatal(err)
	}
	if cfg.Permission.Edit != "allow" {
		t.Errorf("范围内 edit 可精确表达为 allow，got %q", cfg.Permission.Edit)
	}
	if cfg.Permission.ExternalDirectory != "ask" {
		t.Errorf("越界必须 ask，got %q", cfg.Permission.ExternalDirectory)
	}
	if cfg.Permission.Webfetch != "ask" {
		t.Errorf("webfetch 必须 ask，got %q", cfg.Permission.Webfetch)
	}
	if cfg.Permission.Bash["*"] == "allow" {
		t.Fatal("快照不允许无限制 bash 时，不得写 \"*\": allow")
	}
	if cfg.Permission.Bash["*"] != "ask" {
		t.Errorf(`bash["*"]=%q want ask`, cfg.Permission.Bash["*"])
	}
	for _, pattern := range []string{
		"*rm -rf*", "*rm -fr*", "rm *", "*sudo*",
		"*git push*", "*git reset --hard*", "*--force*", "curl *", "wget *",
		"*>/*", "*> /*", "*>~*", "*> ~*", "*tee*",
		"ln *", "* ln *", "install *", "* install *", "dd *", "* dd *",
	} {
		if got := cfg.Permission.Bash[pattern]; got != "ask" {
			t.Errorf("bash[%q]=%q want ask", pattern, got)
		}
	}
}

func TestWritePermissionConfigEmptyScopeIsCapabilityError(t *testing.T) {
	_, err := WritePermissionConfig(t.TempDir(), "", executor.PolicySnapshot{Version: "s1", TaskID: "T1"})
	if err == nil {
		t.Fatal("空 Scope 无法实施范围内允许，必须能力不足")
	}
}

func TestWritePermissionConfigRequiresVersion(t *testing.T) {
	_, err := WritePermissionConfig(t.TempDir(), "", executor.PolicySnapshot{
		TaskID: "T1", Scope: executor.ApprovalScope{Workdir: "/r"},
	})
	if err == nil {
		t.Fatal("缺 Version 必须拒绝")
	}
}
```

   修改 `TestWriteTaskEnv`：调用改带 snap；`bash["*"]` 期望从 `allow` 改为 `ask`。其它 ask 模式表断言保留。

2. **跑红**  
   `go test ./internal/executor/opencode -run 'TestWritePermissionConfig|TestWriteTaskEnv$' -count=1`

3. **最小实现**  
   在 `taskenv.go`：

```go
var ErrUnrepresentablePolicy = errors.New("快照含无法在原生配置实施的明确禁止")

// taskenv.go 的 import 增加 "errors" 与 "github.com/Xsxdot/handoff/internal/executor"（若尚未引用）。

func WritePermissionConfig(taskDir, model string, snap executor.PolicySnapshot) (string, error) {
	if strings.TrimSpace(snap.Version) == "" {
		return "", fmt.Errorf("PolicySnapshot.Version 为空")
	}
	if strings.TrimSpace(snap.Scope.Workdir) == "" {
		return "", fmt.Errorf("%w: 空 Workdir", ErrUnrepresentablePolicy)
	}
	configPath := filepath.Join(taskDir, configFileName)
	model = strings.TrimSpace(model)
	if model == "" {
		model = strings.TrimSpace(os.Getenv("HANDOFF_OPENCODE_MODEL"))
	}
	bash := cloneBashRulesAskDefault()
	cfg := opencodeConfig{
		Model: model,
		Permission: permissionConfig{
			Edit:              "allow",
			Bash:              bash,
			Webfetch:          "ask",
			ExternalDirectory: "ask",
		},
	}
	raw, err := json.MarshalIndent(cfg, "", "  ")
	if err != nil {
		return configPath, err
	}
	if err := os.WriteFile(configPath, raw, 0o644); err != nil {
		return configPath, fmt.Errorf("写 %s: %w", configPath, err)
	}
	return configPath, nil
}

func cloneBashRulesAskDefault() map[string]string {
	out := make(map[string]string, len(bashPermissionRules))
	for k, v := range bashPermissionRules {
		out[k] = v
	}
	out["*"] = "ask" // 无法精确表达「无限制 bash ⊆ 快照」，不准放宽
	return out
}
```

   `WriteTaskEnv` 签名增加最后一参 `snap executor.PolicySnapshot`，配置段改调 `WritePermissionConfig`；prompt 写入保持。`Adapter.Start`：

```go
	snap := executor.PolicySnapshot{}
	if req.Approval != nil {
		s, err := req.Approval.PolicySnapshot(ctx)
		if err != nil {
			return err
		}
		snap = s
	}
	configPath, _, err := WriteTaskEnv(req.TaskDir, req.Task.ID, req.Task.Model, req.PlanContent, req.Discipline, snap)
```

   本 task 允许 Start 在 Approval==nil 时仍调 WriteTaskEnv（T4 会让 nil 直接 fail）。若 `WriteTaskEnv` 因空 Version 失败，Start 失败——T4 与此衔接。

   所有 `WriteTaskEnv(` 调用点（`taskenv_test.go`、可能的 resume）按新签名补 snap。`grep -n 'WriteTaskEnv(' internal/executor/opencode` 逐个改。

4. **跑绿** 上列测试。  
5. **日志**：`WritePermissionConfig` 入口 Info（task_dir、version、workdir 非空？），成功 Info（config 路径），错误 Error。不要把整个 JSON 打 Info。  
6. **注释**：文件头补「原生 allow ⊆ 快照；`*` 不得 allow」。`cloneBashRulesAskDefault` 写 why。

### 验收

读盘 JSON：edit allow、external_directory ask、bash["*"] ask、危险模式全 ask。反面：`"*"=allow` 必须让测试红。空 Workdir → 能力不足。Continue 重投影不在本票（T4/P5）。

---

## 6. Task 4：接线——注入、OpenCode 路径退役权威、事件可交付

### 精确文件集

- `internal/agentd/manager.go`（Dispatch StartReq.Approval、Continue ApplySnapshot、waitQuestion/RelayAnswer AskResponder、handleQuestion 不再吞问、handleEvent OpenCode permission 忽略、isOpenCodeExecutor）
- `internal/executor/opencode/adapter.go`（`mapPermissionAsked` 走 client；`runState.approval`；`Start` nil 拒绝；`ApplySnapshot`；ResumeReq.Approval）
- `internal/executor/executor.go` 或 `resume.go`（`ResumeReq.Approval`）
- 新增 `internal/executor/snapshot.go`
- `internal/client/client.go`（`isDeliverable`）
- `internal/client/client_internal_test.go`
- `internal/client/backlog.go`（若单独复制了类型集合，必须与 isDeliverable 同口径；基线 who-calls 显示 `computeBacklog` → `isDeliverable`，预期零改）
- `codegraph/target.json`（interfaces 边 + 必要时预算）
- `internal/agentd/manager_test.go`（Dispatch 捕获 StartReq；Ask 回程）
- `cmd/agentd.go` 不 new 第二份 client——`bindApproval` 只从 Manager 工厂出来

### Interfaces

Consumes：T1–T3 全部 Produces、`Manager.Dispatch`、`Manager.Continue`。  
Produces：OpenCode 任务 `StartReq.Approval != nil`；非 OpenCode nil；`isDeliverable(permission_reuse)==false`。

### 基线判据先跑

```bash
go test ./internal/client -run 'TestPermissionAutoAllowIsNotDeliverable' -count=1
go test ./internal/agentd -run 'TestAdapterForRoutesByTaskExecutor' -count=1
```

测试范围：`go test ./internal/agentd ./internal/client ./internal/executor/opencode -count=1`（循环用 `-run` 收窄）。

### 步骤

1. **写失败测试**

`internal/client/client_internal_test.go` 追加（缝：`WaitEvent`/`isDeliverable`）：

```go
func TestPermissionReuseIsNotDeliverable(t *testing.T) {
	yes := []proto.EventType{
		proto.EventTypeQuestion, proto.EventTypePermissionRequest,
		proto.EventTypeDeliveryFailed, proto.EventTypeCompleted,
		proto.EventTypeTurnFailed, proto.EventTypeFailed,
	}
	no := []proto.EventType{
		proto.EventTypeProgress, proto.EventTypePermissionAutoAllow,
		proto.EventTypeApproverDecision, proto.EventTypePermissionReuse,
	}
	for _, tpe := range yes {
		if !isDeliverable(tpe) {
			t.Errorf("%s 必须可交付", tpe)
		}
	}
	for _, tpe := range no {
		if isDeliverable(tpe) {
			t.Errorf("%s 必须不可交付", tpe)
		}
	}
}
```

`chanAdapter` 增加 `AskResponder` 可选实现与 Approval 记录——**不要改所有 fake 测试**。新增专用类型（同文件 `manager_test.go`）：

```go
type openCodeStub struct {
	chanAdapter
	asks []executor.RespondAskReq
}

func (a *openCodeStub) RespondAsk(_ context.Context, req executor.RespondAskReq) error {
	a.mu.Lock()
	defer a.mu.Unlock()
	if a.respondErr != nil {
		return a.respondErr
	}
	a.asks = append(a.asks, req)
	return nil
}
```

`TestDispatchOpenCodeInjectsApproval`：

- `newTestManagerWithAds(t, map[string]executor.Adapter{"opencode": stub}, "opencode")`
- 走 `registerTestProject` + `Dispatch`（照抄既有 Dispatch 测试的最小仓库夹具，例如 `TestDispatch*` 里 git init 那段；文件 `manager_test.go`）。
- 断言 `stub.lastStartReq().Approval != nil`。
- 再 Dispatch `Executor: "fake"`（注册 fake）断言 `Approval==nil`。

`TestOpenCodeNilApprovalIsCapabilityError`：直接 `opencode.New(...).Start` 带 `Approval: nil` → 错误，且错误文本含「不是」/「免审」/「Approval」之一。

`TestOpenCodeWaitQuestionUsesRespondAsk`：任务 Executor=opencode，handleQuestion + 回答后 `stub.asks` 长度为 1 且 `Send` 次数 0。构造：`mustCreateTask` + 调 `m.handleQuestion` + `m.RelayAnswer` 或 `hub.NotifyAnswer`。断言 `asks[0].TicketID` 非空。

`TestDenyGuidanceDoesNotSwallowQuestion`：Executor=opencode 时先 `noteDenyGuidance` 再 `handleQuestion`，必须仍 `CreateTicket` kind=ask（`GetTicket` 存在），不得只 `relayDenyGuidance` 早退。

把 `mapPermissionAsked` 的行为用 adapter 单测：Approval 非 nil 时 `drainOne` 不得收到 Type=permission；可用 `fakeApprovalClient`（`approval_test.go` 那份，拷到 opencode 测试或改成 executor 测试包导出）。在 `adapter` 测试里给 `r.approval = fake` 后调 `mapPermissionAsked`，事件通道无 permission。

2. **跑红** 上列测试。

3. **最小实现**

`internal/executor/snapshot.go`：

```go
package executor

import "context"

// SnapshotApplier 是可选能力：Continue 开始时把新快照编进原生配置。
// 未实现则 OpenCode 路径必须报能力不足，不得假装同等保证。
type SnapshotApplier interface {
	ApplySnapshot(ctx context.Context, taskID string, snap PolicySnapshot) error
}
```

`ResumeReq` 增加 `Approval ApprovalClient` 字段，注释与 `StartReq.Approval` 同款。

`isDeliverable` 的 switch 增加 `proto.EventTypePermissionReuse`。

`manager.go` 辅助：

```go
func isOpenCodeExecutor(name, defaultName string) bool {
	if name == "" {
		name = defaultName
	}
	return name == "opencode"
}

func (m *Manager) taskIsOpenCode(taskID string) bool {
	task, err := m.st.GetTask(taskID)
	if err != nil {
		return false
	}
	return isOpenCodeExecutor(task.Executor, m.conf().Executor.Default)
}

func (m *Manager) resolveSnapshot(task *proto.Task) (executor.PolicySnapshot, error) {
	scope := executor.ApprovalScope{
		Workdir:    task.Workdir(),
		TaskDir:    filepath.Join(m.cfg.DataDir, "tasks", task.ID),
		TaskTmpDir: executor.TaskTmpDir(m.cfg.DataDir, task.ID),
	}
	cfg := m.conf()
	ver := HashPolicyVersion(cfg.Approver.Blacklist, cfg.Approver.Executor != "", permgate.SafeCommandIDs(), scope)
	return executor.PolicySnapshot{Version: ver, TaskID: task.ID, Scope: scope}, nil
}
```

Dispatch 在 `ad.Start` 前：

```go
	var approval executor.ApprovalClient
	if isOpenCodeExecutor(execName, m.conf().Executor.Default) {
		snap, serr := m.resolveSnapshot(task)
		if serr != nil {
			return nil, serr
		}
		approval = m.bindApproval(taskID, snap)
	}
	if err := ad.Start(ctx, executor.StartReq{..., Approval: approval}); err != nil {
```

（`StartReq` 其它字段保持原调用，只加 Approval。）

Continue：状态迁 running 之后、`ad.Send` 之前：

```go
	if m.taskIsOpenCode(taskID) {
		task, _ := m.st.GetTask(taskID)
		snap, err := m.resolveSnapshot(task)
		if err != nil {
			m.transitBestEffort(taskID, proto.TaskStateWaitingReview, "continue 快照失败回迁")
			return err
		}
		sa, ok := ad.(executor.SnapshotApplier)
		if !ok {
			m.transitBestEffort(taskID, proto.TaskStateWaitingReview, "continue 无 SnapshotApplier")
			return fmt.Errorf("OpenCode 未实现 SnapshotApplier，无法把新快照投影到原生配置")
		}
		if err := sa.ApplySnapshot(ctx, taskID, snap); err != nil {
			m.transitBestEffort(taskID, proto.TaskStateWaitingReview, "continue 投影失败回迁")
			return err
		}
	}
```

`waitQuestion` / `RelayAnswer` 的 ask 分支：

```go
	if m.taskIsOpenCode(taskID) {
		ar, ok := ad.(executor.AskResponder)
		if !ok {
			return fmt.Errorf("OpenCode 未实现 AskResponder，禁止回退 Send")
		}
		q := ""
		if native := executor.NativeIDFromTicket(taskID, ticketID); native != ticketID {
			q = native
		}
		return ar.RespondAsk(actx, executor.RespondAskReq{
			TaskID: taskID, TicketID: ticketID, QuestionID: q, Answer: ans,
		})
	}
	return ad.Send(actx, taskID, ans)
```

`handleQuestion`：删掉 OpenCode 路径上 `takeDenyGuidance` 早退。改为：

```go
	if !m.taskIsOpenCode(taskID) {
		if guidance := m.takeDenyGuidance(taskID); guidance != "" {
			m.relayDenyGuidance(ctx, taskID, guidance)
			return
		}
	}
	// 其余 CreateTicket / AppendEvent / Publish 保持；ticket id 用 NamespacedTicketID
```

工单 id：`ev.QuestionID != ""` 时用 `executor.NamespacedTicketID(taskID, ev.QuestionID)`（若预算满则保持 `taskID+":"+id`，二者字节相等，测试已锁）。

`handleEvent` permission：

```go
	case adapterEventPermission:
		if m.taskIsOpenCode(taskID) {
			m.log.Warn("OpenCode 仍发出 permission 事件，忽略（权威在 adapter client）",
				"task", taskID, "perm", ev.PermissionID)
			return
		}
		m.handlePermission(ctx, taskID, ev)
```

OpenCode `Start`：

```go
	if req.Approval == nil {
		return fmt.Errorf("OpenCode 任务必须注入 ApprovalClient；nil 不是全部免审")
	}
	r.approval = req.Approval
```

`runState` 加字段 `approval executor.ApprovalClient`。

`mapPermissionAsked` 在 `a.emit(...)` 之前：

```go
	if r.approval != nil {
		ev := executor.AdapterEvent{Type: "permission", PermissionID: pa.ID,
			Text: turn.TruncateMarked(text, permTextHardLimit), Perm: req}
		go a.authorizeNativePermission(r, ev)
		return
	}
```

```go
func (a *Adapter) authorizeNativePermission(r *runState, ev executor.AdapterEvent) {
	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()
	go func() {
		select {
		case <-r.stopCh:
			cancel()
		case <-ctx.Done():
		}
	}()
	a.log.Info("原生权限交 client", "task", r.taskID, "perm", ev.PermissionID)
	dec, err := executor.Authorize(ctx, r.approval, executor.ApprovalRequest{
		NativeID: ev.PermissionID, Text: ev.Text, Perm: ev.Perm,
		Truncated: strings.Contains(ev.Text, executor.TruncationMarker),
	})
	if err != nil {
		a.log.Error("Authorize 失败，拒绝该权限", "task", r.taskID, "perm", ev.PermissionID, "cause", err)
		_ = a.RespondPermission(ctx, r.taskID, ev.PermissionID, "reject", "审批失败")
		return
	}
	decision, reason := "reject", dec.Reason
	if dec.Status == executor.ApprovalAllow {
		decision, reason = "once", ""
	}
	ref := executor.ApprovalRef{ID: executor.NamespacedTicketID(r.taskID, ev.PermissionID)}
	_ = r.approval.Acknowledge(ctx, executor.ApprovalAck{Ref: ref, NativeID: ev.PermissionID, Stage: executor.AckFormed})
	if err := a.RespondPermission(ctx, r.taskID, ev.PermissionID, decision, reason); err != nil {
		a.log.Error("RespondPermission 失败", "task", r.taskID, "perm", ev.PermissionID, "cause", err)
		return
	}
	_ = r.approval.Acknowledge(ctx, executor.ApprovalAck{Ref: ref, NativeID: ev.PermissionID, Stage: executor.AckDelivered})
	_ = r.approval.Acknowledge(ctx, executor.ApprovalAck{Ref: ref, NativeID: ev.PermissionID, Stage: executor.AckExecuted})
	if decision == "reject" && strings.TrimSpace(reason) != "" {
		guide := turn.DenyGuidanceText(reason)
		if perr := r.api.PromptAsync(ctx, r.session, "【关联权限 "+ev.PermissionID+"】\n"+guide); perr != nil {
			a.log.Error("拒绝指导下发失败", "task", r.taskID, "perm", ev.PermissionID, "cause", perr)
		}
	}
	a.log.Info("原生权限已回传", "task", r.taskID, "perm", ev.PermissionID, "decision", decision)
}
```

`ApplySnapshot`：

```go
func (a *Adapter) ApplySnapshot(ctx context.Context, taskID string, snap executor.PolicySnapshot) error {
	r := a.lookup(taskID)
	if r == nil {
		return fmt.Errorf("任务 %s: %w", taskID, executor.ErrTaskNotRunning)
	}
	a.log.Info("ApplySnapshot", "task", taskID, "version", snap.Version)
	if _, err := WritePermissionConfig(r.taskDir, "", snap); err != nil {
		return err
	}
	ok, err := probeServeReloadsConfig(ctx, r)
	if err != nil {
		return err
	}
	if !ok {
		return fmt.Errorf("OpenCode serve 未重载任务目录 opencode.json，新快照无法保证原生 allow ⊆ 快照")
	}
	return nil
}

func probeServeReloadsConfig(ctx context.Context, r *runState) (bool, error) {
	// 无公开热更新 API 时 fail-closed。探测：若 API 有 config 查询则比对 Version
	// 标记文件；否则返回 (false, nil) 让 ApplySnapshot 报能力不足。
	// 禁止返回 true 除非有正面证据。机内测试注入：r.reloadProbe。
	if r.reloadProbe != nil {
		return r.reloadProbe(ctx)
	}
	return false, nil
}
```

`runState` 加 `taskDir string`（Start 已有目录，写入 newRun）、`reloadProbe func(context.Context) (bool, error)` 仅测试。

Resume 冷恢复：把 `req.Approval` 写入 `runState.approval`；nil 且该 adapter 只服务 OpenCode 时与 Start 同样拒绝。

`target.json`：在 contracts 增加

```json
{
  "from": "d_execution",
  "to": "d_orchestration",
  "interfaces": ["ApprovalClient"],
  "note": "B233.1：OpenCode adapter 经注入的 ApprovalClient 回调编排实现"
}
```

若 `codegraph check` 报 orchestration→execution 超 27：把 `legacyBudget` 改为实际边数，`legacyBudgetNote` 写明新增 `PermFingerprint`/`NamespacedTicketID`/`ReuseFingerprint`。

组装点：`bindApproval` 只在 `Manager.Dispatch`/`Continue`/`Resume` 调用，不在 opencode 包 `New`。`grep -n 'bindApproval\\|taskApprovalClient{' -- *.go` 生产代码只许 manager.go。

4. **跑绿** 新测试 + `go test ./internal/client -run 'TestPermissionReuseIsNotDeliverable' -count=1`。  
5. **日志**：Dispatch 注入 Approval Info（task、version）；Continue ApplySnapshot 前后；AskResponder 断言失败 Error；忽略 OpenCode permission 事件 Warn；Authorize goroutine 入口/出口。  
6. **注释**：`isOpenCodeExecutor` why 用 Task.Executor；`handleEvent` 忽略 why；`probeServeReloadsConfig` why fail-closed。

### 验收

- OpenCode Dispatch → `StartReq.Approval != nil` 且 TaskID 相同；非 OpenCode nil。  
- OpenCode + nil Start → 能力不足。  
- Ask：`RespondAsk` 被调，不调 `Send`。断言失败不回退 Send。  
- OpenCode permission 事件不走 `judgePermission` 权威（间谍：client.Request≥1 或 adapter 不 emit）。  
- `isDeliverable(permission_reuse)==false` 且 17–25 条事件集合与冻结一致。  
- `codegraph check --view cards-B233.1-charter-2` `fails: []`。  
- grep 生产 `New.*Approval` / `bindApproval` 仅组装路径。

---

## 7. Task 5：取消、重复答案、迟到答案、投递确认

### 精确文件集

- `internal/store/store.go`（`AnswerTicket`、新增 `AnswerTicketApplied`、`ErrTicketConflict`）
- `internal/store/store_test.go`
- `internal/agentd/server.go`（`handleReply` 幂等不二次中继）
- `internal/agentd/manager.go`（`Stop` 已 void 工单经 transit；确认 OpenCode 挂起权限 reject；已形成决定不改写成 allow）
- `internal/agentd/manager_test.go` 或新 `internal/agentd/ask_idempotency_test.go`

### 基线判据先跑

```bash
go test ./internal/store -run 'AnswerTicket' -count=1
```

今日第二次 `AnswerTicket` 同答案 → `ErrNotFound`。`handleReply` 把它译成 404「工单不存在」。

测试范围：`go test ./internal/store ./internal/agentd -run 'AnswerTicket|Idempotent|Late|Cancel|Void|Reply' -count=1`。

### 步骤

1. **写失败测试**

```go
func openStore(t *testing.T) *store.Store {
	t.Helper()
	s, err := store.Open(filepath.Join(t.TempDir(), "handoff.db"))
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { s.Close() })
	return s
}

func createAskTicket(t *testing.T, s *store.Store, taskID, ticketID string) {
	t.Helper()
	now := time.Now().UTC()
	if err := s.CreateTask(&proto.Task{ID: taskID, RepoPath: "/r", State: proto.TaskStateRunning, CreatedAt: now, UpdatedAt: now}); err != nil {
		t.Fatal(err)
	}
	ok, err := s.CreateTicket(&proto.Ticket{ID: ticketID, TaskID: taskID, Kind: "ask", Request: json.RawMessage(`{}`), CreatedAt: now})
	if err != nil || !ok {
		t.Fatalf("CreateTicket: ok=%v err=%v", ok, err)
	}
}

func TestAnswerTicketSameAnswerIdempotent(t *testing.T) {
	s := openStore(t)
	createAskTicket(t, s, "t1", "tk1")
	if err := s.AnswerTicket("tk1", "hello"); err != nil {
		t.Fatal(err)
	}
	if err := s.AnswerTicket("tk1", "hello"); err != nil {
		t.Fatalf("相同答案第二次必须成功: %v", err)
	}
}

func TestAnswerTicketDifferentAnswerRejected(t *testing.T) {
	s := openStore(t)
	createAskTicket(t, s, "t1", "tk1")
	if err := s.AnswerTicket("tk1", "a"); err != nil {
		t.Fatal(err)
	}
	err := s.AnswerTicket("tk1", "b")
	if err == nil || !errors.Is(err, store.ErrTicketConflict) {
		t.Fatalf("不同答案必须 ErrTicketConflict，got %v", err)
	}
}

func TestAnswerTicketVoidRejected(t *testing.T) {
	s := openStore(t)
	createAskTicket(t, s, "t1", "tk1")
	if _, err := s.VoidPendingTickets("t1"); err != nil {
		t.Fatal(err)
	}
	err := s.AnswerTicket("tk1", "hello")
	if err == nil || !errors.Is(err, store.ErrTicketConflict) {
		t.Fatalf("作废后迟到答案必须拒绝，got %v", err)
	}
}
```

形态照抄 `store_test.go` 的 `store.Open(filepath.Join(t.TempDir(), "handoff.db"))`（包名 `store_test`）。`CreateTask` 若因必填字段失败：按 `TestTaskLifecycle` 把 `Target`/`RepoPath` 补上，不要改 store schema。

`TestHandleReplyIdempotentDoesNotRelayTwice`：HTTP 测 `handleReply` 两次相同 answer，`openCodeStub.asks` 或 `chanAdapter.sends` 长度为 1，第二次 HTTP 200。夹具照抄 `server_test.go` 里已有 reply 测试。

`TestStopVoidsAskAndDoesNotRewriteAllow`：`Stop` 后 pending ask 的 Answer 为 `VoidAnswer`；事先已 allow 且 delivered 的 gate 工单 Answer 仍为 `"allow"`。

2. **跑红**

3. **最小实现**

```go
var ErrTicketConflict = errors.New("工单已有不同答案")

func (s *Store) AnswerTicket(id, answer string) error {
	_, err := s.AnswerTicketApplied(id, answer)
	return err
}

func (s *Store) AnswerTicketApplied(id, answer string) (applied bool, err error) {
	res, err := s.db.ExecContext(context.Background(),
		"UPDATE tickets SET answer = ?, answered_at = ? WHERE id = ? AND answer IS NULL",
		answer, fmtTime(time.Now()), id)
	if err != nil {
		return false, fmt.Errorf("回答工单 %s: %w", id, err)
	}
	n, err := res.RowsAffected()
	if err != nil {
		return false, fmt.Errorf("读取工单影响行数: %w", err)
	}
	if n == 1 {
		s.touchTaskUpdatedAt(id)
		return true, nil
	}
	tk, gerr := s.GetTicket(id)
	if gerr != nil {
		return false, ErrNotFound
	}
	if tk.Answer == nil {
		return false, ErrNotFound
	}
	if *tk.Answer == VoidAnswer {
		return false, fmt.Errorf("%w: 工单已作废", ErrTicketConflict)
	}
	if *tk.Answer == answer {
		return false, nil
	}
	return false, fmt.Errorf("%w", ErrTicketConflict)
}
```

`touchTaskUpdatedAt` 抽出原 AnswerTicket 里刷新 `tasks.updated_at` 的那段。

`handleReply`：

```go
	applied, err := s.st.AnswerTicketApplied(req.TicketID, req.Answer)
	if err != nil {
		if errors.Is(err, store.ErrTicketConflict) {
			writeJSON(w, http.StatusConflict, map[string]string{"error": err.Error()})
			return
		}
		if errors.Is(err, store.ErrNotFound) {
			writeJSON(w, http.StatusNotFound, map[string]string{"error": "工单不存在"})
			return
		}
		writeJSON(w, http.StatusInternalServerError, map[string]string{"error": "内部错误"})
		return
	}
	if !applied {
		writeJSON(w, http.StatusOK, map[string]any{"ok": true, "relayed": true, "idempotent": true})
		return
	}
	// 既有 NotifyAnswer / RelayAnswer 路径不变
```

`Stop`：既有 `transit failed` 会 `VoidPendingTickets`。补：OpenCode 路径 `lookup` 后对仍挂起的 perm/question 调 `RespondPermission(..., "reject", "task stopped")` 与清 pending（adapter `Stop` 已有 `TestStopRejectsPendingQuestion`——确认 Stop 仍走它，不要删）。禁止 `UPDATE tickets SET answer='allow'`。

4. **跑绿**  
5. **日志**：幂等 Info（ticket、idempotent=true）；冲突 Warn；Stop 解除原生挂起 Info/Error。  
6. **注释**：`AnswerTicketApplied` 的 applied 语义；handleReply 为什么幂等不再中继（冻结 75）。

### 验收

相同答案二次成功、原生调用仍 1；不同答案拒绝；void/取消/换尝试迟到拒绝；Stop 停进程且不把已决改成 allow。

---

## 8. Task 6：接缝回归、其它 harness 负例、CHANGELOG、真机清单

### 精确文件集

- `CHANGELOG.md` `[Unreleased]`
- T1–T5 测试作为回归套件再跑
- `internal/agentd/manager_test.go` 负例：claude/codex/grok/agy 名字的任务 `StartReq.Approval==nil` 且 ask 仍 `Send`
- 不改 `web/`、`cmd/dispatch.go`、`cmd/reply.go`（composeAnswer 字面值用既有测试回归）

本 task 含「驱动派发系统自身」的真机项 → **由协调者执行，不派发**（见 §11）。实现者负责机内测试、`go build`、CHANGELOG。

### 基线

`go test ./internal/executor/claudecode ./internal/executor/codex ./internal/executor/grok ./internal/executor/agy -count=1` 今日预期退出 0（实现者动手前重跑；失败贴原文，不归因本卡直到证明是本卡 diff）。

### 步骤

1. **CHANGELOG** 在 `## [Unreleased]` 下新增 `### 变更`（若已有变更节则追加条目）：

```markdown
- **OpenCode 普通派发改走审批 client 与 RespondAsk（B233.1）。** `permission_reuse` 不再唤醒 `handoff wait`。OpenCode 答问不再走无关联 `Send`。原生免审改为当前政策快照的精确子集（bash 默认 ask，范围内 edit 仍可原生 allow）。拒绝理由不再优先吞掉下一条提问。
```

2. **负例测试** `TestNonOpenCodeKeepsOldApprovalPath`：注册 fake 为 default，Dispatch Executor 空或 `"grok"`，`lastStartReq().Approval==nil`；handleQuestion 后回答走 `Send` 记录而非 RespondAsk。

3. **变异（实现者机内）**  
   - 把 OpenCode `waitQuestion` 改回 `ad.Send` → `TestOpenCodeWaitQuestionUsesRespondAsk` 必须红。  
   - 把 `cloneBashRulesAskDefault` 的 `"*"` 改回 `allow` → `TestWritePermissionConfigSubsetOfSnapshot` 必须红。  
   变异必须编译过且命中唯一。编译红的变异不算数。

4. **跑测试**

```bash
go build -o /dev/null .
go test ./internal/executor/... ./internal/agentd/ ./internal/client/ ./internal/store/ ./internal/permgate/ ./internal/proto/ ./cmd/ -count=1
```

沙箱 `Operation not permitted`：记原文，不归因；无沙箱环境复跑。全量测试不在本 task 作为「绿了就收工」——集成全量在 acceptance。本命令是 T6 的触及面回归。

5. **日志/注释**：CHANGELOG 不算代码；若 T6 只加测试，测试名即文档。

6. **真机清单** 交给协调者（§11），本票不把未跑的真机写成绿。

### 验收

spec 五条缝各有入口在缝上的测试（§10 对照表）。P6 CHANGELOG 含四句行为。其它 harness 包测试退出 0。

---

## 9. 五项检查

### 9.1 缺陷族

**生命周期**：Ask 先票后事件（既有 handleQuestion 顺序保持；Publish 失败不删票）。Await 在 adapter 独立 goroutine，stopCh 取消。Stop/transit 作废工单。快照 Version 写入 `ReuseFingerprint` 与 `PolicySnapshot`，进程内绑定；agentd 重启后 Dispatch/Resume 重新 `resolveSnapshot`——**未验证，需真机**（§11 第 10 条）。Continue 不热更新已跑回合的内存 snap（新 Continue 才 resolve）。无新孤儿类型。

**静默失败**：Start 失败不得成功 Task（既有）。AnswerTicket 二次不再 404。截断/超时不得 allow。投影放宽有反面测试。拒绝吞问退役。permission_reuse 不唤醒。能力不足（nil Approval、缺 AskResponder、serve 不重载、空 Workdir）是可行动错误。

**跨平台**：工单 id `taskID:native` 与 OS 无关。JSON 投影测字段不是 inode。Windows Stop/权限回调 **未验证，需真机**。

**假红/假绿**：退役 Send 消费 pending 的正向测试。`isDeliverable` 显式假值。Ticket 0 空壳不当故事 1。夹具 fake serve 不冒充热重载（probe 默认 false）。

**门禁**：原生 ⊆ 快照 + client 第二道。OpenCode 只留 client 一条权威入口。nil 不是全免审。复用只认严格 allow + 已送达。TOCTOU 用回合 Version 缓解。CLI 与 HTTP 同进 RelayAnswer。

**序列化**：不新增 Ticket/EventType 字段。`isDeliverable` 单测。`composeAnswer` T6 回归。client 四方法进程内。P2(a) 无新 JSON 字段。`opencode.json` 读盘断言。

**枚举白名单**：不新增 EventType。`ApprovalStatus`/`AckStage` 不进 `Ticket.Kind`。`gateDecision` 金样本保持。web 预期零改。

**承重安全**：截断、超时≠allow、复用六条件、跨任务隔离、迟到拒绝、Send 不消费 Ask、原生 ⊆ 快照，各有能变红测试。

**webview**：无，因为本卡不改 Web 控制台。

### 9.2 序列化边界

| 边界 | 文件 | 断言 |
|---|---|---|
| 事件类型 → 是否唤醒 | `internal/client/client.go#isDeliverable` | `TestPermissionReuseIsNotDeliverable` |
| CLI 裁决字面值 | `cmd/reply.go#composeAnswer` | 既有 reply 测试 + T6 回归 |
| 原生权限 JSON | `taskenv.go` 写盘 | `TestWritePermissionConfigSubsetOfSnapshot` 读文件 |
| 复用键 | 无新列；fingerprint 文本 | `TestReuseFingerprintSaltsVersion` + client 换 Version 反面 |
| Ticket JSON | 零增量 | 不新写 |

### 9.3 上下文预算

每 task 文件集已圈出。不把整包 agentd 当派发上下文。架构法第三条：agentd 非测试行数超红线是既有债，本卡不插竖切。

### 9.4 类型标注

`d_execution` / `d_transport` 为边界型。机内锁契约形状；行为真机见 §11。`d_orchestration` / `d_policy` / `d_protocol` / `d_cli` 逻辑型，机内闭环。

### 9.5 接缝覆盖（双向）

spec 测试决定 5 条缝：

| 缝 | 锁它的测试（入口符号） |
|---|---|
| 1 普通派发 `Manager.Dispatch` | `TestDispatchOpenCodeInjectsApproval` 调 `Dispatch`；失败不得成功 Task 既有 start 失败测试保持 |
| 2 Ask 回程 `RespondAsk`（调用方 `RelayAnswer`/`waitQuestion` ← `client.Reply`） | `TestOpenCodeWaitQuestionUsesRespondAsk`；`TestSendDoesNotConsumePendingAsk` 入口 `Send` |
| 3 审批 client | `TestApprovalClientInScopeEditAllowsWithoutPermissionRequest` 入口 `Request`；adapter `authorizeNativePermission` |
| 4 事件订阅 `isDeliverable`/`WaitEvent` | `TestPermissionReuseIsNotDeliverable` |
| 5 取消/重复/迟到 `AnswerTicket`/`Stop`/`handleReply` | T5 三支 + Stop 测试 |

缝 → 测试：五条均有。  
测试 → 缝：上表入口都在缝上或链上。

**内部锁（已声明）**：`TestAuthorizeAwaitsWhenPending` / `TestAuthorizeNilClientIsNotAllow` —— 从 ApprovalClient 四方法各自调用构造不出「合成步骤漏 Await」；Authorize 是 P4(a) 便利方法的专属缺陷。不顶替缝级断言。

**无未声明退路**。

---

## 10. 自审三查

### spec 覆盖（用户故事 → task）

| 故事 | task |
|---|---|
| 1 普通派发 + wait 可订阅 | T4+T6 |
| 2 有身份 Ask；Send 不消费 | T1+T4 |
| 3 范围内写免审不唤醒 | T2+T3 |
| 4 越界/截断 permission_request；approve/deny 回传 | T2+T4 |
| 5 同快照复用；故障≠allow | T2+T4 |
| 6 重复答案幂等；迟到拒绝 | T5 |
| 7 取消 | T5 |
| 8 重启未答 Ask | T4 落库 + T5 恢复；真机 §11.10 |
| 9 其它 harness 旧路径 | T6 |

冻结 1–96 归属与 breakdown §2.1 一致，本计划未越界。

### 占位符扫描

无 TBD。「照抄 harness」自我声明：

- OpenCode httptest / `newTestAdapter` / `drainOne` / `a.newRun` ← `question_test.go`、`reconcile_internal_test.go`  
- `newTestManager` / `mustCreateTask` / `newTestGate` / `registerTestProject` / Dispatch git 夹具 ← `manager_test.go`  
- store `Open(temp)` + CreateTask/CreateTicket ← `store_test.go`  
- reply HTTP ← `server_test.go` 既有 reply 测试  
- 事件列表：`(*Store).EventsFromAsc(taskID, 0, 1000)`  

`ApproverDecision` 用 `Approve`/`Err`；作废答案用 `store.VoidAnswer`。

### 跨 task 签名

§2 表格与各 task Interfaces 逐字相同：`Authorize`、`ReuseFingerprint`、`WriteTaskEnv` 新签名、`AnswerTicketApplied`、`SnapshotApplier`、`ResumeReq.Approval`。

---

## 11. 真机清单（协调者执行，不派发）

模型必须 `opencode-go/deepseek-v4-flash`。不可用则该项失败并贴原文，禁止改走会员。机内不得把本表写成绿。

1. `handoff dispatch --executor opencode` 普通任务，不建卡、不走工作流；任务在新边界跑完。  
2. 业务 Ask：`wait` 收到 `question`；`reply --answer` 后若原生仍挂起则不开新回合。  
3. 提问回合已结束的 Ask：关联续接。  
4. 有待答 Ask 时 `continue` / 无关联 Send 不得把该问题答掉。  
5. 工作区范围内编辑：不唤醒 wait、原生允许、审计可查。  
6. 越界/黑名单/截断：`wait` 收到 `permission_request`；`--approve` / `--deny --reason` 回传；拒绝后该工具不继续。  
7. 同任务同快照同描述第二次：不唤醒，审计 `permission_reuse`。  
8. 重复 `reply --answer`：第二次成功且无第二次原生回复。  
9. `handoff stop`：serve 停、挂起解除、wait 见 failed。  
10. 未答 Ask 期间重启 agentd：游标仍见原问题。  
11. 已答未送达 gate：恢复重投或明确失效。  
12. Claude 或 Codex 对照：仍走旧路径（Codex 限额约 3 小时，本卡不要派 Codex；有额度再验）。  
13. 免费模型不可用：失败原文，不得改 `executor.model`。  
14. Continue 后政策已变：新回合新快照；serve 是否采用重写后的 `opencode.json`——未重载必须能力不足，不得假装。  
15. 拒绝带 reason：下一条真提问仍到达。  
16. Windows（若范围内）：Stop 进程树与权限回调抽查。

---

## 12. 图覆盖债（本卡不修图归属）

- `who-calls handlePermission` 短名不命中，须 `n_agentd_Manager_handlePermission`。  
- `flow` 对 handlePermission/handleQuestion/waitQuestion/WriteTaskEnv/isDeliverable/Judge degraded。  
- 基线容器 `k_executor_*` domain 字段仍 `d_executor`；best 归 `d_execution_contract`。  
- `Adapter` 图归属分裂。  
- `codegraph check` 145 条存量 warn。

---

## 13. 实现者收尾命令（每个 task 后）

```bash
go build -o /dev/null .
# 再跑该 task 声明的 -run / 包测试
```

禁止在未跑到结果的命令上写通过。agentd 全包测试耗时长，task 内循环用 `-run`；T6 再跑触及面包。全量 `go test ./...` 留给 acceptance。
