# B156.3.6 plan 节点台账（边干边落）

约定：每确立一个事实追加一行；命令与原始输出随行。本文件与
`docs/superpowers/plans/b156.3.6-plan.md` 同批提交。本轮只写计划与台账，不写实现代码。

- 2026-08-27 输入确认：spec `docs/superpowers/specs/2026-08-26-b156.3-automation-keystone-design.md`、contract `docs/superpowers/specs/b156.3-contract.md`、breakdown `docs/superpowers/specs/b156.3-breakdown.md` 均可读；本卡法定产出路径为 `docs/superpowers/plans/b156.3.6-plan.md`。
- 2026-08-27 当前分支为 `cards/B156.3.6-charter-3`，`git status --short --branch` 输出 `## cards/B156.3.6-charter-3`，工作树干净。
- 2026-08-27 代码图核实：存在 `codegraph/best.json`，最优树包含 `d_web`、`d_web_contract`、`d_web_admin`、`d_web_command`；尝试按纪律运行 `codegraph context d_web`，原始输出为 `/bin/bash: line 1: codegraph: command not found`。当前环境没有 codegraph CLI，未把图文件当作源码链路结论；本计划记录该覆盖债，改以 `best.json` + 当前源码逐条核对。
- 2026-08-27 K3/K4 交付物已在 HEAD：`web/src/api/scheduling.ts` 已有 Carrier/Squad/Queue/CoordinatorLaunch 的 TS 镜像与 `getSquads/getQueue/putCarrier/putSquad/launchCoordinator`；`internal/agentd/coordapi.go` 已注册 launch/status/attach 三端点；`CardDrawer`、`CardsPage`、`SettingsPage` 是 K6 当前 Web 接缝。
- 2026-08-27 contract/breakdown 对照：K6 必须消费 `GET /api/squads`、`GET /api/queue`、`POST /api/cards/{id}/coordinator/launch`、`GET /api/cards/{id}/coordinator`、`POST /api/cards/{id}/attach`；配置页挂 `d_web_admin`，卡面按钮/attach 属 `d_web_command`，TS 请求函数归 `d_web_contract`。
- 2026-08-27 `internal/agentd/coordapi.go` 实读：`coordinatorStatusResp` 为 `{Bound bool json:"bound", AttachActive bool json:"attach_active", Attach *keysclient.AttachInfo json:"attach,omitempty"}`；`keysclient.AttachInfo` 为 `{Machine string json:"machine", Dir string json:"dir", Command string json:"command"}`；`handleCoordStatus` 当前在 `coordapi.go:206` 调 `s.keystone.Locate(id, "")`，因此 status 中的 attach 三元组不会得到项目工作目录。
- 2026-08-27 K4 attach 错误分支实读：`handleCoordAttach` 有 `Active == nil` 的 400、active=true 定位失败后 `SetAttach(id,false)` 回滚、首行 `forwardIfRequested(w,r)` 跨机转发，但现有 `coordapi_test.go` 只覆盖成功/互斥与状态推进，没有这三条错误分支的独立断言；本卡计划纳入同文件核销。
- 2026-08-27 `npm install --prefer-offline`：`added 290 packages, and audited 291 packages in 2s`；`found 0 vulnerabilities`。`node_modules` 未跟踪，工作树仍干净。
- 2026-08-27 基线 `npm test` 原始结果：`Test Files  108 passed (108)`、`Tests  1111 passed (1111)`、`Duration 15.49s`；期间有既有提示 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`，未导致失败。
- 2026-08-27 基线 `npm run typecheck` 原始结果：输出 `> web@0.0.0 typecheck`、`> tsc -b`，无错误，退出 0。
- 2026-08-27 基线 `go build ./...` 原始结果：无输出，工具返回 `exit_code: 0`。
- 2026-08-27 基线 `go test ./internal/agentd/ -run 'TestCoord' -count=1 -v`：6 支 `TestCoord*` 均因 PTY 测试根目录不可用而显式 SKIP，原始原因反复为 `PTY 测试根目录不可用；source=tmp root="/tmp" 实测 socket=46 字节，上限=100，原因=mkdir /tmp/handoff-pty-<随机数>: read-only file system；source=repo-dot root="/root/.handoff/worktrees/6a2fa8ae/.pty-test-root/handoff-pty-<随机数>" 实测 socket=112/113 字节，上限=100`；包最终输出 `PASS` / `ok github.com/Xsxdot/handoff/internal/agentd 0.562s`。K6 计划不把该 SKIP 写成后端错误已验证，要求实现轮在可用 PTY 根目录重跑新增 coordapi 错误锁。
- 2026-08-27 基线前端文件清单：`web/src/app/settings/SettingsPage.tsx` 的 `SECTIONS` 当前为 machines/discipline/general/env/update；`web/src/app/cards/CardsPage.tsx` 只轮询 cards/decisions/ledger health/tasks；`CardItem.tsx` 无队列位置 props；`ListView.tsx` 无队列列；`CardDrawer.tsx` 无协调者状态/拉起/attach 区块。
- 2026-08-27 计划定案：增加 `SchedulingPage` 配置载体/小队，`QueuePanel` + CardItem/ListView 队列位置呈现，`CoordinatorPanel` 卡上状态/拉起/attach；新增 API 镜像与三端点客户端方法。GET coordinator 只读状态与镜像；实际可用 attach 三元组以 POST attach 成功响应为权威，status 的 `attach` 只作为已有绑定的缓存展示；`handleCoordStatus` 改用 `resolveCoordWorkdir(id)` 作为 Locate 的工作目录来源，禁止继续用空串。
- 2026-08-27 计划边界：machine 字段只镜像服务端 `AttachInfo.Machine`，不在 TS 或页面凭空制造；本机空 machine 是既有空串语义，dir 必须来自协调者卡项目位置解析或 POST attach 请求传入的 workdir。跨机真实终端 tab 往返列入真机清单，不伪装为 jsdom 已验证。
- 2026-08-27 `internal/proto/contract_fixture_test.go` 实读：对外 DTO fixture 由 `TestContractFixtures` 逐字节比较，新增 `internal/proto` 类型必须同时加入 cases 并用 `-update` 显式生成 `web/src/api/testdata/*.json`；该真实序列化边界将用于 CoordinatorStatus/AttachInfo/AttachReleaseResp。
- 2026-08-27 `internal/agentd/server.go` 实读：`attachLocator.Locate` 以 `SessionRef.Machine`、传入 `workdir`、`CLI --session SessionID` 组成三元组；`SessionRef` 当前由 keystone 仅保存 CLI/SessionID，本机 machine 为空串是既有语义，计划不臆造机器名。
- 2026-08-27 `web/src/api/client.ts` 实读：`request<T>` 统一透传非 2xx 的 `ApiError.message`，`postJSON/putJSON` 只负责 JSON 方法封装；K6 页面按此接缝呈现服务端 400/409 原文，不另造 fetch。
- 2026-08-27 `web/src/app/data/usePoll.ts` 实读：轮询首拉后以 `data:null` 表示未有快照，断线保留旧 data 并提供 `disconnected/errorText`，401 进入 `sessionExpired` 终止态；配置、队列、协调者状态计划复用该原语。
- 2026-08-27 前端聚焦基线 `npm test -- --run src/app/settings/SettingsPage.test.tsx src/app/cards/CardsPage.test.tsx src/app/cards/CardDrawer.test.tsx src/api/contract.test.ts`：原始结果 `Test Files  4 passed (4)`、`Tests  88 passed (88)`、`Duration 1.91s`；K6 改动前四个接缝测试均绿。
- 2026-08-27 计划落盘：`docs/superpowers/plans/b156.3.6-plan.md` 已覆盖 Task A（DTO/fixture/status 工作目录/三条 attach 错误锁）、Task B（设置页）、Task C（队列与卡位次）、Task D（拉起/attach 抽屉）、Task E（集成测试收口）；文档内列出精确文件、Consumes/Produces、完整新组件代码、红绿步骤、日志/注释要求、序列化边界、双向接缝、自审与真机清单。
- 2026-08-27 计划自检：`git status --short --branch` 原始输出为 `## cards/B156.3.6-charter-3`、两个未跟踪计划文档；`git diff --check` 无输出；`rg -n "TBD|同 Task|适当|TODO|占位|加代码即可" docs/superpowers/plans/b156.3.6-plan.md` 无命中；`git diff --no-index --check /dev/null <计划文件>` 两次均无输出。当前未写任何产品 `.go`/`.ts` 文件。
- 2026-08-27 计划修订：coordapi 三条新增错误锁改用 `newNoPTYLedgerEnv` 装配的 `newNoPTYCoordEnv`，并以 `setCoordForwardTarget` 写入临时 config 的 target；原因是已实测 `newTestAgentdEnvWithCfg` 会在当前环境因 PTY 根目录不可用而显式 SKIP，纯 HTTP 错误分支不能接受该退路。该修订只改变测试装配，不改变产品接口；计划同时列出 `path/filepath` 与 `internal/config` 的精确 import。
- 2026-08-27 契约对齐修订：K4 计划 D3 明确 GET coordinator 未绑定态为 `attach:null`，而当前私有 DTO 的 `json:"attach,omitempty"` 与冻结 wire 矛盾；本计划将 proto DTO/TS 镜像改为非 omitempty 与 `CoordinatorAttachInfo | null`，并把 HTTP/TS 断言钉在 null，不让“字段缺席”冒充 null。
- 2026-08-27 可观测性核实：`rg -n "logger|slog|console..." web/src` 只找到既有 console 调试/告警，没有可复用的结构化 Web logger；计划因此要求前端用成功回执、失败正文、断线和禁态可见性承载观测，Go 端继续用 slog，且不新增 console/credential/session 输出。
- 2026-08-27 依赖行为行号核对：`internal/agentd/server.go:2367-2374` 的 `attachLocator.Locate` 返回 `Machine: ref.Machine`、传入 `workdir` 与 `CLI --session SessionID`；`web/src/api/client.ts:102-112,127-134,152-167` 规定非 2xx 抛 ApiError 原文、postJSON/putJSON 的 JSON 方法封装；`web/src/app/data/usePoll.ts:19-31,50-67,76-86` 规定首拉 data=null、断线保留快照、401 终止轮询。
- 2026-08-27 测试装配行号核对：`internal/agentd/ledgerapi_test.go:151-171` 的 `newNoPTYLedgerEnv` 只装真实 ledger/HTTP 面而不触碰 PTY；`internal/agentd/w3a_testhelpers_test.go:52-86` 的 `newTestAgentdEnvWithCfg` 会先 Resolve PTY 测试根，因此 K6 新增纯 HTTP 锁按计划使用前者。
- 2026-08-27 自检尝试：首次 required-anchor 扫描把 `resolveCoordWorkdir(id)` 作为正则，原始输出为 `missing=resolveCoordWorkdir(id)`；判断为扫描表达式问题而非文档缺失，下一次用固定字符串模式重跑。
- 2026-08-27 自检尝试：为捕获 `git diff --no-index --check` 输出而写入 `/tmp/b15636-*.out`，原始错误为 `/bin/bash: line 10: /tmp/b15636-plan-check.out: Read-only file system` 与 `/bin/bash: line 12: /tmp/b15636-ledger-check.out: Read-only file system`；改用不落临时文件的直接检查。
- 2026-08-27 最终文档自检：占位符扫描无命中（`placeholder_status=1`）；plan/ledger `git diff --no-index --check` 均无输出并返回 1（/dev/null 与未跟踪文件的预期差异）；plan fence 数 `46`、奇偶校验 `0`；固定字符串 anchors 输出 `anchors=ok`；工作树仍只有两份计划/台账未跟踪文档。
- 2026-08-27 实现轮基线复跑：`go test ./internal/proto/ -run TestContractFixtures -count=1` 通过；`go test ./internal/agentd/ -run 'TestCoord(StatusEndpoint|AttachTakeoverMutesWake)$' -count=1 -v` 中两支均显式 SKIP，原文为 `/tmp/...: read-only file system` 与 repo-dot socket `112/113` 超过上限 `100`，包输出 `PASS`；Web 五个 npm 命令均失败，原始错误为 `sh: 1: vitest: not found` 或 `sh: 1: tsc: not found`。
- 2026-08-27 Task A 红测：首次运行新增 proto/coordapi 断言得到 `undefined: CoordinatorStatus`、`undefined: CoordinatorAttachInfo`、`undefined: CoordinatorAttachReleaseResp`；补最小 DTO 后，fixture 测试明确红为两个 fixture 文件不存在，status HTTP 测试明确红为响应缺少 `"attach":null`。转发测试首轮错误是测试夹具对 `ContentLength=-1` 调用 `make`，已改为 `io.ReadAll`，不计为产品红。
- 2026-08-27 Task A 实现：新增 `proto.CoordinatorAttachInfo`、`proto.CoordinatorStatus`、`proto.CoordinatorAttachReleaseResp` 及 TS 镜像/客户端函数；coordapi 改为 `resolveCoordWorkdir`、显式 `attach:null`、DTO 投影、active 缺失日志/回滚日志。`go test ./internal/proto/ -run TestContractFixtures -update -count=1`、随后不带 `-update` 的同命令均退出 0；focused coordapi 中 `TestCoordStatusEndpoint`、`TestCoordAttachMissingActive`、`TestCoordAttachLocateFailureRollsBack` 通过，`TestCoordAttachTakeoverMutesWake` 仍因 PTY 原因显式 SKIP；转发测试第一次产品实现后断言期待多余换行而失败，待改为远端原始 body 后重跑；`go build ./...` 退出 0。
- 2026-08-27 Task A 收口：`npm install --prefer-offline` 输出 `added 290 packages, and audited 291 packages in 2s`、`found 0 vulnerabilities`；`npm test -- --run src/api/contract.test.ts` 输出 `Test Files 1 passed (1)`、`Tests 41 passed (41)`、`Duration 661ms`；`npm run typecheck` 无错误并退出 0。之后 Go focused 重跑输出 `Test Files` 不适用，4 个非 skip coordapi 用例通过、1 个既有 takeover 用例显式 SKIP，包 `PASS`；proto fixture 包 `ok`；`go build ./...` 退出 0。
- 2026-08-27 Task B 红测：新增 `SchedulingPage.test.tsx` 首次运行的原始错误为 `Failed to resolve import "./SchedulingPage"`；补最小页面后 4 个测试中 1 个失败，原因是测试用 `findByRole('alert', {name: ''})` 在两个错误节点间产生多匹配，已改为按错误正文定位；SettingsPage 新入口测试随后因未提供 scheduling poll mock 显式红，补上独立 API mock 与异步断言。
- 2026-08-27 Task B 收口：`npm test -- --run src/app/settings/SchedulingPage.test.tsx src/app/settings/SettingsPage.test.tsx` 输出 `Test Files 2 passed (2)`、`Tests 10 passed (10)`、`Duration 1.13s`；`npm run typecheck` 无错误并退出 0。SchedulingPage 按行携带 version 保存，400/409 原文、首拉失败、断线禁写均有断言；SettingsPage 已挂六个分区含自动化编制。
- 2026-08-27 Task C 基线命令误用记录：在仓库根目录运行 `npm test -- --run src/app/cards/CardsPage.test.tsx` 与 `npm run typecheck`，原始输出为 `npm error code ENOENT`、`Could not read package.json: Error: ENOENT: no such file or directory, open '/root/.handoff/worktrees/755c0165/package.json'`；此后均在 `web/` 目录重跑，未把该失败外推为代码结论。
- 2026-08-27 Task C 红测：先运行 `cd web && npm test -- --run src/app/cards/QueuePanel.test.tsx`，原始错误为 `Failed to resolve import "./QueuePanel"`；加入测试后确认缺少 QueuePanel。再接入队列轮询前运行 CardsPage 新增断言，原始失败为 `Unable to find text #1` 与 `Unable to find /queue connection refused/`；确认是队列呈现尚未接入。接线后首次 focused 测试只剩测试选择器多匹配（`Found multiple elements with the text: B240`、`Found multiple elements with the text: #1`、错误正文在断线提示与 alert 各出现一次），修正为按 region/table 及 `findAllByText` 断言，不改生产行为。
- 2026-08-27 Task C 收口：`npm test -- --run src/app/cards/QueuePanel.test.tsx src/app/cards/CardsPage.test.tsx` 输出 `Test Files 2 passed (2)`、`Tests 10 passed (10)`、`Duration 1.09s`；`npm run typecheck` 输出 `> web@0.0.0 typecheck`、`> tsc -b`，退出 0；`go build ./...` 无输出，退出 0。CardsPage 已挂 QueuePanel，CardItem 显示 `排队 #position`，ListView 新增 `排队` 列并保留 position=0。
- 2026-08-27 Task D 红测：新增 `CoordinatorPanel.test.tsx` 后首次运行原始错误为 `Failed to resolve import "./CoordinatorPanel"`；加入组件后测试装配先报 `ReferenceError: beforeEach is not defined`，补测试导入；再报 `Unable to find role="alert" and name "未登记协调者小队"`，按现有 alert 节点语义改为正文断言。CardDrawer seam 断言先于生产接线运行，40 支旧测中新增断言失败原文为 `Unable to find an element with the text: 未绑定`。
- 2026-08-27 Task D 收口：`npm test -- --run src/app/cards/CoordinatorPanel.test.tsx src/app/cards/CardDrawer.test.tsx` 输出 `Test Files 2 passed (2)`、`Tests 46 passed (46)`、`Duration 2.34s`；`npm test -- --run src/api/contract.test.ts src/app/settings/SettingsPage.test.tsx src/app/settings/SchedulingPage.test.tsx src/app/cards/QueuePanel.test.tsx src/app/cards/CoordinatorPanel.test.tsx src/app/cards/CardsPage.test.tsx src/app/cards/CardDrawer.test.tsx` 输出 `Test Files 7 passed (7)`、`Tests 107 passed (107)`、`Duration 2.14s`；两次 `npm run typecheck` 均输出 `> tsc -b`、退出 0；`go build ./...` 无输出、退出 0。CoordinatorPanel 已挂入 CardDrawer，成功回执、服务端三元组、断线禁态、首拉失败与会话失效均有可见接缝。
- 2026-08-27 Task E 全量 Web：`npm test` 原始输出含既有提示 `Not implemented: HTMLCanvasElement's getContext() method: without installing the canvas npm package`，随后 `Test Files 111 passed (111)`、`Tests 1130 passed (1130)`、`Duration 24.10s`，退出 0；`npm run typecheck` 原始输出为 `> web@0.0.0 typecheck`、`> tsc -b`，退出 0。
- 2026-08-27 Task E 全量 Go：`go test ./...` 原始输出为 50 行 `ok`、7 行 `? ... [no test files]`，无 `FAIL`；可见耗时最长为 `ok github.com/Xsxdot/handoff/internal/agentd 185.049s`，末行含 `? github.com/Xsxdot/handoff/web/node_modules/flatted/golang/pkg/flatted [no test files]`，退出 0；通过计数 50、失败计数 0、无测试包 7。
- 2026-08-27 Task E 门禁：`go build ./...` 无输出、退出 0；`go vet ./...` 无输出、退出 0；`gofmt -l internal/proto/scheduling.go internal/proto/contract_fixture_test.go internal/agentd/coordapi.go internal/agentd/coordapi_test.go` 无输出、退出 0。格式正控先新增临时坏格式文件，再运行 `gofmt -l format-positive-control.go`，原始输出为 `format-positive-control.go`、退出 0，随后用 apply_patch 删除临时文件。
- 2026-08-27 变异自验：变异前 `git grep -n -F 'workdir := s.resolveCoordWorkdir(id)' -- internal/agentd/coordapi.go | wc -l` 输出 `1`，唯一命中行 `209`；将该语义改为 `workdir := ""` 后 `go build ./...` 无输出、退出 0，变异行为断言 `go test ./internal/agentd/ -run '^TestCoordStatusEndpoint$' -count=1 -v` 原始输出为 `绑定态定位三元组异常: &{Machine: Dir: Command:opencode --session sess-coord}`、`--- FAIL: TestCoordStatusEndpoint`，退出 1；恢复原实现后同命令原始输出为 `--- PASS: TestCoordStatusEndpoint`、`PASS`、`ok github.com/Xsxdot/handoff/internal/agentd 0.212s`，退出 0。该变异编译通过且确实被唯一行为断言拦截。
- 2026-08-27 配对原语模式自检：`git grep -E -n '(^|[^[:alnum:]_])New[[:space:]]*\\(' -- '*.go' '*.ts' '*.tsx' | grep -v '_test\\.go' | wc -l` 原始输出 `221`，证明 ERE 模式未把消费方误报为零；对确定只在测试文件中的 `TestCoordStatusEndpoint` 跑同类命令，原始输出 `0`，证明 `_test.go` 排除有效。
- 2026-08-27 配对原语问一（本轮 diff 新增导出符号，生产/测试命中数由 `git grep -E` 精确边界命令统计；声明本身计入生产命中）：`proto.CoordinatorAttachInfo` 6/1、`proto.CoordinatorStatus` 3/2、`proto.CoordinatorAttachReleaseResp` 3/1；`scheduling.CoordinatorAttachInfo` 5/0、`scheduling.CoordinatorStatus` 2/8、`scheduling.CoordinatorAttachReleaseResp` 2/4；`getCoordinatorStatus` 3/4、`attachCoordinator` 3/6、`releaseCoordinator` 3/5；`SchedulingPageProps` 2/0、`SchedulingPage` 4/8；`QueuePanelProps` 2/0、`QueuePanel` 4/10、`queuePositionByCard` 3/2；`CoordinatorPanelProps` 2/0、`CoordinatorPanel` 4/8。无“仅测试命中且生产为零”的导出符号。
- 2026-08-27 配对原语问二（契约义务及具体承载符号）：Go wire 由 `CoordinatorAttachInfo.Machine/Dir/Command`、`CoordinatorStatus.Bound/AttachActive/Attach`、`CoordinatorAttachReleaseResp.OK` 承载；TS JSON 镜像由同名 `CoordinatorAttachInfo`、`CoordinatorStatus`、`CoordinatorAttachReleaseResp` 承载；请求路径/方法/请求体由 `getCoordinatorStatus`、`attachCoordinator`、`releaseCoordinator` 承载；配置页由 `SchedulingPage`、`SchedulingPageProps`、`SECTIONS` 的 `scheduling` 条目承载 carrier/squad/version/CAS/错误原文；队列由 `QueuePanel`、`QueuePanelProps.entries/loading/disconnected/sessionExpired/errorText/onOpenCard`、`queuePositionByCard`、`CardsPage.queuePoll/queueLoading/queuePositions`、`CardItemProps.queuePosition`、`ListView.queuePositions` 与 `排队` 表头承载；抽屉协调者由 `CoordinatorPanel`、`CoordinatorPanelProps.cardId`、`CardDrawer` 的 `<CoordinatorPanel cardId={id} />` 承载 launch/status/attach/release、三元组、busy/断线/失败态。每条义务均有可 grep 的字段或符号，无“在流程中体现”的悬空义务。
- 2026-08-27 分次提交审计：`git log --oneline 17bf7885..HEAD` 输出 `c2906e80` 文档计划、`764d816b` Task A、`3a3d6121` Task B、`ccc0bd3b` Task C、`96525176` Task D 五个提交；每个实现增量提交前均记录 `go build ./...` 退出 0。当前代码图文件未改动，图数据登记条款不适用；真机浏览器/跨机原生终端清单未执行，保持“未验证”。
- 2026-08-27 收口复核：当前分支 `cards/B156.3.6-review-1`；`go build ./...`、`go vet ./...`、改动 Go 文件 `gofmt -l` 均退出 0；故意坏格式正控原始输出为 `format-positive-control.go`，退出 0。`go test ./...` 退出 0，50 个包通过、7 个包 `[no test files]`、失败 0；`npm test` 退出 0，`Test Files 111 passed (111)`、`Tests 1130 passed (1130)`，保留既有 `HTMLCanvasElement's getContext()` warning；`npm run typecheck` 退出 0。
- 2026-08-27 收口 focused：修复 attach 成功日志不再记录包含 session id 的 `command`，仅保留 card/machine/dir；修复后 `go build ./...` 退出 0，`go test ./internal/agentd/ -run 'TestCoord(StatusEndpoint|AttachMissingActive|AttachLocateFailureRollsBack|AttachForwardsMachineQuery)$' -count=1 -v` 为 4 支 PASS、0 FAIL。前端 K6 focused 实测 `Test Files 7 passed (7)`、`Tests 107 passed (107)`，typecheck 退出 0。
- 2026-08-27 收口自检：`git grep` ERE 模式自检 `New` 生产命中 221，确定测试专用 `TestCoordStatusEndpoint` 生产命中 0；新增导出符号逐项生产/测试命中已在上一行清单，未发现测试命中而生产命中为 0。`codegraph check` 原始输出为 `/bin/bash: line 1: codegraph: command not found`，代码图未改动，图登记保持未验证；原生终端 tab/跨机真机行为保持未验证。
