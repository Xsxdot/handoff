# B233.21 plan：非 OpenCode 权限/提问权威收口

前置事实（2026-09-13，功能线含 .18/.19）：

- Manager 方法族（internal/orchestration/manager.go）：handlePermission:2018、
  escalatePermission:2067、judgePermission:2122、auditAutoAllowOnly:2208、
  autoAllowPermission:2251、noteAutoAllowed:2270、takeAutoAllowed:2280、
  shouldConsultApprover:2293、consultApprover:2322、approvePermission:2513、
  hasPermissionAutoAllow:2575、respondAutoAllowReplay:2602、isPermissionReplay:2656、
  reuseDecision:2731、handleQuestion:2923。
- 权威参照：internal/approval/client.go——per-task Client（PolicySnapshot/Request/Await/
  Acknowledge/NoteDeliveryFailed + 内部 tryReuse/consult/escalate/decisionFromStore），
  Hooks 注入，B233.12 已收口「Hooks 不投递」。
- permgate 判据、executor.AdapterEvent、审批者链（Approver）为共享词汇。
- 冻结标注：manager.go:2245「只有 handlePermission 的 AutoAllow 分支调用它（冻结 #14）」、
  ticket id = taskID:permID、顺序契约 P1-2（先状态→工单→waiter→Publish）。

步骤：

1. 在 internal/approval 落非 OpenCode 权威 API：以 permgate.Verdict 三出口为形状
   （AutoAllow / Consult / Escalate），接收判据输入（任务策略快照 + AdapterEvent），
   输出决策与幂等/复用结论。judge/reuse/autoallow/重放幂等实现体迁入；Manager 原方法
   变委托或删除。
2. handlePermission 收缩：重放检查→权威复用/幂等→权威 verdict→按出口落账本（AutoAllow
   审计、consult 分流、escalatePermission 既有链）。顺序契约逐字保持。
3. consult/escalate：决策（shouldConsult/inflight）随权威；账本机制留 Manager。降级阀见
   spec §范围 2。
4. handleQuestion：权威入口落 approval（ask 身份/复用）或按 spec §范围 3 维持现状，二选一
   落账。
5. 缝级测试：三出口决策出自权威 + 重放幂等 + 复用（不破坏既有断言，新增测试）。
6. 验证：spec §验收 1–5；grep 证据。
7. 提交：`refactor(B233.21): 非 OpenCode 权限/提问权威收口——决策核心迁 approval`。

执行中偏差如实记入收尾段。
