# B369.4（S4）壳绑定面收口 —— 实现计划

- 卡号：B369.4（父卡 B369；拆解序号 S4「壳绑定面收口」）
- 有效基线分支：`cards/B369-charter-4`（本卡合并目标）
- 冻结 contract：`docs/superpowers/specs/b369-contract.md` @ `6f10ba7c`
- 已拍板拆解：`docs/superpowers/specs/b369-breakdown.md` @ `4ab6415a`（P1–P6 全案 A）
- 上游 spec：`docs/superpowers/specs/2026-09-13-mobile-app-design.md`（状态：已批准）
- 图视图：`codegraph/diffs/cards-B369-charter.json`（view=`cards/B369-charter`，base `977720e2`）
- 本节点产出物：`docs/superpowers/plans/b369.4-plan.md`（计划文档；本节点不写实现代码）
- 图外声明：`mobile/` 是嵌套独立 module，**图外**（扫描配方 `scripts/codegraph-rescan/main.go:231` 排除 `mobile` 前缀）；本卡不触发 `graph check`

---

## 0. 本卡干什么（一段话）

`mobile/bind` 是壳（Kotlin/Swift）与 Go 连接核的唯一交接面。现状只有一个文件 `mobile/bind/bind.go`，导出 `Pair/Origin/MachineNames/Close` 四方法，生产单例直接是 `mobilecore.New(...)`。P4=A 拍板后，绑定面还须暴露**会话 cookie 桥接**（供壳注入 webview cookie jar）与**切机**（清旧罐 + 重兑换）。本卡：

1. 把 `bind.go` 的核依赖收成**窄接口** `coreAPI`（四方法，contract §3.2 已冻结，`*mobilecore.Core` 直接满足）；
2. 新增 `mobile/bind/session.go`：`sessionAPI` 窄接口 + fail-closed 占位实现 + 导出 `SessionCookie`/`SwitchMachine` 两个稳定壳契约方法，**不暴露 Token/Dial**；
3. 新增回归测试：四方法如实转发、会话 cookie/切机「一罐只装一机 + 切机清旧罐 + 失败上抛」、S2 未接线时 fail-closed、导出面逐字冻结、跨 gomobile 边界的结构体字段形状、绑定层零协议 import；
4. 新增 `mobile/moduleisolation_test.go`：守「mobile/ 是嵌套独立 module、不进根构建图」，并把 `mobile/` 模块内的 `go test ./...` 纳入本仓测试面。

**边界与已知未验证**：S2（B369.2「连接核兑换/切机」）尚未落地，核侧会话入口的**方法名与签名以 S2 落地为准**（协调者工单 `35872f16` 裁决：S4 只声明自己需要的消费面，不发明 S2 冻结物）。因此：

- 本卡在机内能验证的是**绑定层行为**（转发、fail-closed、导出面、结构体形状）——用替身（fake）与源码守卫锁；
- **真实 cookie 注入 iOS `WKHTTPCookieStore` / Android `CookieManager` 的行为未验证，需真机**（拆解 §5 #2）；
- **`gomobile bind` 出 AAR/XCFramework 未在本工作树重跑**（linux 无 Xcode/NDK），归真机/移动 CI（拆解 §5 #3）。

**不做**：不改 `internal/mobilecore/**`（S2 的面）、不改 `mobile/go.mod`（S3 的面）、不产出壳工程（P5=A）、不新增 CLI 命令、不碰任何 `web/` 文件。

---

## 1. 基线事实（本节点实测，动手前复核）

| 事实 | 命令 | 原始读数 |
|---|---|---|
| Go 版本 | `go version` | `go version go1.26.1 linux/amd64` |
| 根模块 build | `go build ./...` | 空输出，EXIT=0 |
| mobile 模块 build | `cd mobile && go build ./...` | 空输出，EXIT=0 |
| mobile 模块测试现状 | `cd mobile && go test ./... -count=1` | `? github.com/Xsxdot/handoff/mobile/bind [no test files]`，EXIT=0 |
| 根模块 `go list ./...` 反依赖 | `go list ./... \| grep -c handoff/mobile` | `0`（0 命中；根模块不含 mobile 包） |
| 嵌套 module 定位 | `cd mobile && go list ./...` | `github.com/Xsxdot/handoff/mobile/bind` |
| 契约闸 | `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok github.com/Xsxdot/handoff/cmd 0.077s`，EXIT=0 |
| 图视图 check | `codegraph --repo . check --view cards-B369-charter` | `"fails": []` |
| 图基线 check | `codegraph --repo . check` | `"fails": []` |
| 图 validate | `codegraph --repo . validate --view cards-B369-charter` | `"issues": null`、`"edgeIssues": null` |
| `bind.go` 现状导出面 | `go doc ./bind`（cwd=`mobile`） | `func Close() error` / `func MachineNames() []string` / `func Origin(machine string) (string, error)` / `func Pair(bundleJSON string) (mobilecore.PairResult, error)` |
| `PairedMachine` 字段 | 探针（`reflect`，仓内合法） | `Name string` / `Origin string` / `Online bool`（全 exported，全 string/bool） |
| `PairResult` 字段 | 探针（`reflect`，仓内合法） | `Machines []PairedMachine`（唯一字段） |

**库行为出处**：本卡只用标准库（`go/parser`、`go/ast`、`go/token`、`go/printer`、`reflect`、`os/exec`、`testing`、`errors`、`log/slog`、`context`、`sync`、`fmt`），无第三方库行为事实需引出处。gomobile 的「struct 字段须 string/bool 等可绑定类型、`error` 映射为异常、不支持 `time.Time`」来自 contract §3.4 与拆解 S4 缺陷族第 6 条（`docs/superpowers/specs/b369-breakdown.md:275`），本卡按此实现并加 `reflect` 守卫，不靠记忆。

---

## 2. 图覆盖债

本节点亲测 `codegraph --repo . sym <名> --view cards-B369-charter`：

- **命中（view 内，Ticket 0 已登记）**：`Core.Pair`（`n_mobilecore_Core_Pair`）、`Core.Origin`（`n_mobilecore_Core_Origin`）、`Core.MachineNames`（`n_mobilecore_Core_MachineNames`）、`Core.Close`（`n_mobilecore_Core_Close`）。四者即本卡 `coreAPI` 消费的四方法。
- **未命中（图覆盖债）**：`bind` 符号不在图（返回「不在图中」，近似候选只有 `card bind`/`card rebind` 等无关节点）。原因已知且是设计：`mobile/` 是嵌套独立 module，扫描配方排除其前缀（`scripts/codegraph-rescan/main.go:231` 的 `"mobile"` 排除项）。本卡新增的 `SessionCookie`/`SwitchMachine`/`coreAPI`/`sessionAPI`/**`mobile/moduleisolation_test.go`** 同样在图外。
- **处置**：图外组装点的导出面变更不触发 `graph check`（contract 修订记录第 3 条、breakdown §1.2 边界澄清）。本卡**不补图、不改 `codegraph/` 冻结物**；该债记于此，供后续图对齐节点处置。若后续需 `mobile/bind` 可查，须另开建图卡调整扫描配方（不在本卡）。

---

## 3. 跨卡接缝与精确接口

### 3.1 Consumes

**(a) contract §3.2 已冻结的 Core 四方法**（`internal/mobilecore/core.go`，`*mobilecore.Core` 直接满足）：

```go
func (c *Core) Pair(ctx context.Context, bundleJSON string) (PairResult, error)
func (c *Core) Origin(machine string) (string, error)
func (c *Core) MachineNames() []string
func (c *Core) Close() error

type PairedMachine struct { Name string; Origin string; Online bool }
type PairResult struct { Machines []PairedMachine }
func New(dial DialFunc, log *slog.Logger) *Core
```

**(b) P4=A 的核侧会话入口（S2 未落地，签名以 S2 为准）**：

本卡**声明自己需要的消费面**（协调者工单 `35872f16` 裁决②）：

```go
// S4 需要的核侧会话入口（窄接口，方法名/签名以 S2 在 internal/mobilecore 落地为准）
SessionCookie(machine string) (string, error)  // 返回该机当前会话 cookie 值（核侧持有）
SwitchMachine(machine string) (string, error)  // 清旧罐 + 为目标机重兑换，返回目标 loopback 源
```

> **纪律**：这不是 S2 的冻结物，也**不得**要求 S2 照抄。S2 落地后，本卡只需改 `session.go` 里 `sessions` 的装配（把 `notWiredSessions{}` 换成对 `*mobilecore.Core` 新方法的适配器）。若 S2 选择「不返回 cookie 值而返回整条 `Set-Cookie`/属性」，则适配器在绑定层解析或转换，导出面 `SessionCookie(machine) (string, error)` 不变。

**(c) 生产核构造**：`mobilecore.New(nil, *slog.Logger) *Core`（contract §3.2 冻结：`dial==nil` 走 `DefaultDial`）。

### 3.2 Produces（本卡冻结的壳交接面）

`mobile/bind` 的**导出函数集合与签名**（绑定面即壳的契约面；P4=A/P5=A 冻结）：

```go
func Pair(bundleJSON string) (mobilecore.PairResult, error)
func Origin(machine string) (string, error)
func MachineNames() []string
func Close() error
func SessionCookie(machine string) (string, error)
func SwitchMachine(machine string) (string, error)
```

约束（拆解 S4 缺陷族第 5/8 条、breakdown §3.5③）：

- 只含上述六个导出**函数**（另有 `PairResult`/`PairedMachine` 作为返回类型）；**不含** `Token`/`Dial`/`Credential` 等门禁绕过面。
- 参数与返回只用 gomobile 可绑定类型（`string`/`bool`/导出 struct 切片/`error`）；不含 `time.Time`、函数值、接口作为参数/返回。
- 不新增导出**变量**（`log`/`core`/`sessions` 均小写私有）。

### 3.3 跨卡对齐说明（供跨卡审计）

- S4 的 `sessionAPI` 是**声明性消费面**，S2 落地后再对齐名字；**S4 不发明 S2 的冻结物**。
- 本卡不产生任何被其他子卡 Consume 的 Go 符号（`mobile/` 图外，S3 的构建脚本/文档、S1 的共享样本都不依赖本卡的函数签名）。跨卡签名比对在本卡的唯一对象是 (a) 的四方法——逐字符与 contract §3.2 一致。

---

## 4. Task DAG

```text
T0 冻结物与基线核验（只读；落台账）
   │
   ▼
T1 绑定面测试与替身（先写，预期编译红）
   │   新增 bind_test.go / export_surface_test.go / fakes_test.go
   ▼
T2 绑定面生产实现（跑绿）
   │   改 bind.go（coreAPI + 真核）；新增 session.go（sessionAPI + fail-closed + 两导出方法）
   ▼
T3 嵌套 module 隔离守卫
   │   新增 mobile/moduleisolation_test.go
   ▼
T4 收口复核（build/vet/gofmt/test/race + 根反依赖 + 契约闸；不改图）
```

**最薄路径条**：本卡要锁的行为「从绑定面导出缝调用 `SessionCookie`/`SwitchMachine` 得到断言结果」今天**缝调不到**（两方法不存在）——因此 DAG 第一条实现路径（T1→T2）就是点亮该行为的唯一最薄可跑路径：T1 写测试必然编译红（`undefined: SessionCookie`），T2 落实现后转绿。这是本卡唯一的红绿周期。

**S2 依赖的现实**：核侧真实兑换在 S2 未落地前不可达，故 T2 的生产装配是 **fail-closed 占位**（返回错误），不是「已有活路径」；真实 cookie 语义归 S2 + 真机（§8）。

---

## 5. Task T0：冻结物与基线核验

### 文件范围

- 只读：`docs/superpowers/specs/b369-contract.md`、`docs/superpowers/specs/b369-breakdown.md`、`docs/superpowers/specs/2026-09-13-mobile-app-design.md`、`codegraph/best.json`、`internal/mobilecore/core.go`。
- 过程记录：`docs/superpowers/ledgers/2026-09-19-b369.4-plan-ledger.md`（本卡台账，随产出物同批提交）。
- 测试范围：无（本 task 只读，不跑测试）。

### Interfaces

- Consumes：contract §3.2/§5、breakdown §3.5、spec §实现决定（回环门禁/多机会话域）。
- Produces：给 T1–T4 的有界文件集与判据；不产生运行时代码。

### 步骤

1. `git status --short --branch` 确认在当前分支 `cards/B369.4-charter` 且工作树干净；有意外改动则保留原文并停下提问，不覆盖用户改动。
2. `codegraph --repo . sym Core.Pair --view cards-B369-charter`、`sym Core.Origin`、`sym Core.MachineNames`、`sym Core.Close`，确认四命中（本节点已实测）；`sym bind` 记图外债。
3. 复核 `internal/mobilecore/core.go` 四方法签名与 §3.1(a) 逐字一致；核对 `PairedMachine{Name,Origin string; Online bool}`。
4. 结论写台账后进 T1；T0 不改任何文件。

---

## 6. Task T1：绑定面测试与替身（先写，预期红）

### 文件范围（新增）

```text
mobile/bind/fakes_test.go            # 替身（fakeCore / fakeSessions / swap 助手）
mobile/bind/bind_test.go             # 四方法转发 + 会话 cookie/切机 + fail-closed + gomobile 结构体形状
mobile/bind/export_surface_test.go   # 导出面逐字冻结 + 零协议 import（源码级守卫）
```

- 测试范围（最小化）：本 task 唯一命令 = `cd mobile && go test ./bind/ -count=1`；**预期编译失败（红）**，因为 `SessionCookie`/`SwitchMachine`/`sessionAPI` 尚不存在。
- 关键节点日志：本 task 是测试代码，无生产日志面；可观测性用 `t.Fatalf` 的上下文报文承载。
- 注释：每个新增文件头写职责+边界；每个 fake 方法与测试函数写「锁什么、为什么」。

### Interfaces

- Consumes：§3.2 六个导出函数（其中 `SessionCookie`/`SwitchMachine` 本 task 尚不存在 → 编译红）；`mobilecore.PairResult`/`PairedMachine`。
- Produces：`fakeCore`、`fakeSessions`、`swapCore`、`swapSessions`、`errBoom`、`resultTypes()`。

### 步骤（红绿之「红」）

1. 写 `mobile/bind/fakes_test.go`（完整代码）：

```go
package bind

import (
	"context"
	"errors"
	"fmt"
	"sync"

	"github.com/Xsxdot/handoff/internal/mobilecore"
)

// fakeCore 是 coreAPI 的替身：只记录「绑定层如实转发」，不重实现协议。
type fakeCore struct {
	mu              sync.Mutex
	pairErr         error
	originErr       error
	closeErr        error
	pairCalls       int
	lastBundleBytes int
	lastMachine     string
}

func newFakeCore() *fakeCore { return &fakeCore{} }

func (f *fakeCore) Pair(_ context.Context, bundleJSON string) (mobilecore.PairResult, error) {
	f.mu.Lock()
	defer f.mu.Unlock()
	f.pairCalls++
	f.lastBundleBytes = len(bundleJSON)
	if f.pairErr != nil {
		return mobilecore.PairResult{}, f.pairErr
	}
	return mobilecore.PairResult{Machines: []mobilecore.PairedMachine{
		{Name: "devbox", Origin: "http://127.0.0.1:41001", Online: true},
	}}, nil
}

func (f *fakeCore) Origin(machine string) (string, error) {
	f.mu.Lock()
	defer f.mu.Unlock()
	f.lastMachine = machine
	if f.originErr != nil {
		return "", f.originErr
	}
	return "http://127.0.0.1:41001", nil
}

func (f *fakeCore) MachineNames() []string { return []string{"devbox"} }

func (f *fakeCore) Close() error {
	f.mu.Lock()
	defer f.mu.Unlock()
	return f.closeErr
}

// fakeSessions 是 sessionAPI 的替身：记录切机/取 cookie 的调用序与当前罐内容，
// 用于锁「切机清罐、一罐只装一机、失败上抛」三条语义（不重实现协议）。
type fakeSessions struct {
	mu           sync.Mutex
	current      string // 当前罐里装着的机器名；空=罐空
	cookieValue  string
	cookieErr    error
	switchErr    error
	cookieCalls  int
	switchCalls  int
	lastMachine  string
	switchedFrom string
	port         int
}

func newFakeSessions() *fakeSessions { return &fakeSessions{} }

func (f *fakeSessions) SessionCookie(machine string) (string, error) {
	f.mu.Lock()
	defer f.mu.Unlock()
	f.cookieCalls++
	f.lastMachine = machine
	if f.cookieErr != nil {
		return "", f.cookieErr
	}
	if f.current != machine {
		// 一罐只装一机：未切到该机前不得返回它的 cookie。
		return "", errors.New("会话罐里不是该机（需先切机）")
	}
	return f.cookieValue, nil
}

func (f *fakeSessions) SwitchMachine(machine string) (string, error) {
	f.mu.Lock()
	defer f.mu.Unlock()
	f.switchCalls++
	f.lastMachine = machine
	if f.switchErr != nil {
		return "", f.switchErr
	}
	f.switchedFrom = f.current
	f.current = machine               // 清旧罐并重兑换
	f.cookieValue = "sess-" + machine // 每机独立 cookie
	f.port++
	return fmt.Sprintf("http://127.0.0.1:%d", 42000+f.port), nil
}

func swapCore(c coreAPI) (restore func()) {
	old := core
	core = c
	return func() { core = old }
}

func swapSessions(s sessionAPI) (restore func()) {
	old := sessions
	sessions = s
	return func() { sessions = old }
}

var errBoom = errors.New("核侧故障")
```

2. 写 `mobile/bind/bind_test.go`（完整代码）：

```go
package bind

import (
	"reflect"
	"testing"

	"github.com/Xsxdot/handoff/internal/mobilecore"
)

func TestBindPairForwardsToCore(t *testing.T) {
	fc := newFakeCore()
	defer swapCore(fc)()
	res, err := Pair(`{"v":1}`)
	if err != nil {
		t.Fatalf("Pair: %v", err)
	}
	if fc.pairCalls != 1 || fc.lastBundleBytes != len(`{"v":1}`) {
		t.Fatalf("Pair 未如实转发: calls=%d bytes=%d", fc.pairCalls, fc.lastBundleBytes)
	}
	if len(res.Machines) != 1 || res.Machines[0].Name != "devbox" || !res.Machines[0].Online {
		t.Fatalf("Pair 结果漂移: %+v", res)
	}
	var _ mobilecore.PairResult = res
}

func TestBindPairSurfacesError(t *testing.T) {
	fc := newFakeCore()
	fc.pairErr = errBoom
	defer swapCore(fc)()
	if _, err := Pair(`{}`); err == nil {
		t.Fatal("核侧错误必须上抛，不得吞成 nil")
	}
}

func TestBindOriginSurfacesError(t *testing.T) {
	fc := newFakeCore()
	fc.originErr = errBoom
	defer swapCore(fc)()
	if _, err := Origin("devbox"); err == nil {
		t.Fatal("Origin 核侧错误必须上抛")
	}
}

// TestBindSessionCookieAndSwitchAreOneJarToOneMachine 锁三事：切机清旧罐、
// 一罐只装一机、取 cookie 经绑定导出面。入口 = SessionCookie/SwitchMachine。
func TestBindSessionCookieAndSwitchAreOneJarToOneMachine(t *testing.T) {
	fs := newFakeSessions()
	defer swapSessions(fs)()

	originA, err := SwitchMachine("devbox")
	if err != nil {
		t.Fatalf("切到 devbox: %v", err)
	}
	if originA == "" {
		t.Fatal("切机应返回目标 loopback 源")
	}
	cookieA, err := SessionCookie("devbox")
	if err != nil || cookieA != "sess-devbox" {
		t.Fatalf("取 devbox cookie: value=%q err=%v（绑定面必须返回核侧 cookie，非 origin）", cookieA, err)
	}

	originB, err := SwitchMachine("lanbox")
	if err != nil {
		t.Fatalf("切到 lanbox: %v", err)
	}
	if originB == originA {
		t.Fatalf("切机应换到另一 loopback 源: %q", originB)
	}
	if fs.switchedFrom != "devbox" {
		t.Fatalf("切机应清掉旧机罐: switchedFrom=%q", fs.switchedFrom)
	}
	// 清罐后旧机 cookie 不可再用（切机前请求它必须失败）。
	if _, err := fs.SessionCookie("devbox"); err == nil {
		t.Fatal("切机后旧机 cookie 仍可取——一罐只装一机的语义失守")
	}
	cookieB, err := SessionCookie("lanbox")
	if err != nil || cookieB != "sess-lanbox" || cookieB == cookieA {
		t.Fatalf("lanbox cookie 应与 devbox 不同: a=%q b=%q err=%v", cookieA, cookieB, err)
	}
}

// TestBindSessionFailIsClosed：S2 未接线时不得静默成功（空 cookie + error）。
func TestBindSessionFailIsClosed(t *testing.T) {
	if _, err := SessionCookie("devbox"); err == nil {
		t.Fatal("核侧会话入口未接线时必须返回错误，不得静默成功")
	}
	if _, err := SwitchMachine("devbox"); err == nil {
		t.Fatal("核侧会话入口未接线时切机必须返回错误")
	}
}

func TestBindMachineNamesForwardsToCore(t *testing.T) {
	fc := newFakeCore()
	defer swapCore(fc)()
	got := MachineNames()
	if len(got) != 1 || got[0] != "devbox" {
		t.Fatalf("MachineNames 未如实转发: %v", got)
	}
}

func TestBindCloseForwardsToCore(t *testing.T) {
	fc := newFakeCore()
	defer swapCore(fc)()
	if err := Close(); err != nil {
		t.Fatalf("Close: %v", err)
	}
	fc.closeErr = errBoom
	if err := Close(); err == nil {
		t.Fatal("Close 核侧错误必须上抛")
	}
}

// TestBindResultTypesAreGomobileSafe 锁跨语言映射边界：绑定面返回的结构体字段
// 只能是 string/bool（gomobile 对 time.Time/error/接口支持有限，不可绑定）。
func TestBindResultTypesAreGomobileSafe(t *testing.T) {
	for _, typ := range resultTypes() {
		for i := 0; i < typ.NumField(); i++ {
			f := typ.Field(i)
			if !f.IsExported() {
				t.Fatalf("%s.%s 未导出，gomobile 不可见", typ.Name(), f.Name)
			}
			switch f.Type.Kind() {
			case reflect.String, reflect.Bool:
			case reflect.Slice:
				if f.Type.Elem().Kind() != reflect.Struct {
					t.Fatalf("%s.%s 切片元素非结构体: %s", typ.Name(), f.Name, f.Type)
				}
			default:
				t.Fatalf("%s.%s 类型 %s 不是 gomobile 可绑定的 string/bool/结构体切片",
					typ.Name(), f.Name, f.Type)
			}
		}
	}
}
```

3. 写 `mobile/bind/export_surface_test.go`（完整代码）：

```go
package bind

import (
	"bytes"
	"go/ast"
	"go/parser"
	"go/printer"
	"go/token"
	"os"
	"path/filepath"
	"reflect"
	"runtime"
	"sort"
	"strings"
	"testing"

	"github.com/Xsxdot/handoff/internal/mobilecore"
)

// wantBindSurface 是绑定面的全部导出函数签名（壳的交接面，P4=A/P5=A 冻结）。
// 顺序无关；多一个、少一个、签名漂移都红。
var wantBindSurface = []string{
	"func Close() error",
	"func MachineNames() []string",
	"func Origin(machine string) (string, error)",
	"func Pair(bundleJSON string) (mobilecore.PairResult, error)",
	"func SessionCookie(machine string) (string, error)",
	"func SwitchMachine(machine string) (string, error)",
}

// TestBindExportedSurfaceIsFrozen 断言绑定面导出函数集合与签名逐字等于冻结面，
// 且不含 Token/Dial（回环门禁承重属性：绑定面不得暴露绕过 cookie 闸的方法）。
func TestBindExportedSurfaceIsFrozen(t *testing.T) {
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	dir := filepath.Dir(thisFile)
	fset := token.NewFileSet()
	pkgs, err := parser.ParseDir(fset, dir, func(fi os.FileInfo) bool {
		return !strings.HasSuffix(fi.Name(), "_test.go")
	}, 0)
	if err != nil {
		t.Fatal(err)
	}
	var got []string
	for _, pkg := range pkgs {
		for _, f := range pkg.Files {
			for _, d := range f.Decls {
				fd, ok := d.(*ast.FuncDecl)
				if !ok || fd.Recv != nil || !fd.Name.IsExported() {
					continue
				}
				var tb bytes.Buffer
				printer.Fprint(&tb, fset, fd.Type)
				got = append(got, "func "+fd.Name.Name+strings.TrimPrefix(tb.String(), "func"))
			}
		}
	}
	sort.Strings(got)
	want := append([]string(nil), wantBindSurface...)
	sort.Strings(want)
	if strings.Join(got, "\n") != strings.Join(want, "\n") {
		t.Fatalf("绑定面导出函数集合漂移:\n got=%q\nwant=%q", got, want)
	}
	for _, s := range got {
		low := strings.ToLower(s)
		if strings.Contains(low, "token") || strings.Contains(low, "dial") {
			t.Fatalf("绑定面不得导出 Token/Dial（门禁绕过面）: %s", s)
		}
	}
}

// TestBindProductionHasNoProtocolLogic：绑定层生产文件只许 import 标准库与
// internal/mobilecore；不得 import internal/relay、internal/client、internal/proto
// （协议逻辑零重实现：一切拨号/选路/编解码在核内，壳侧不碰协议）。
func TestBindProductionHasNoProtocolLogic(t *testing.T) {
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	dir := filepath.Dir(thisFile)
	fset := token.NewFileSet()
	pkgs, err := parser.ParseDir(fset, dir, func(fi os.FileInfo) bool {
		return !strings.HasSuffix(fi.Name(), "_test.go")
	}, parser.ImportsOnly)
	if err != nil {
		t.Fatal(err)
	}
	forbidden := []string{
		"github.com/Xsxdot/handoff/internal/relay",
		"github.com/Xsxdot/handoff/internal/client",
		"github.com/Xsxdot/handoff/internal/proto",
	}
	for _, pkg := range pkgs {
		for name, f := range pkg.Files {
			for _, imp := range f.Imports {
				p := strings.Trim(imp.Path.Value, "`\"")
				for _, bad := range forbidden {
					if p == bad {
						t.Fatalf("%s 属于协议实现层，绑定层不得 import（协议零重实现）: %s", p, filepath.Base(name))
					}
				}
			}
		}
	}
}

// resultTypes 返回绑定面返回给壳的结构体类型（跨 gomobile 映射边界的全部类型）。
func resultTypes() []reflect.Type {
	return []reflect.Type{
		reflect.TypeOf(mobilecore.PairResult{}),
		reflect.TypeOf(mobilecore.PairedMachine{}),
	}
}
```

4. 跑红：

```bash
cd mobile && go test ./bind/ -count=1
```

预期（本节点探针实测的形状）：`SessionCookie`/`SwitchMachine`/`sessionAPI` 未定义 → 编译失败，EXIT≠0，报文含 `undefined: SessionCookie`。**这就是本卡唯一的先红步骤**。

### T1 验收

- `mobile/bind/fakes_test.go`、`bind_test.go`、`export_surface_test.go` 三个文件已落盘。
- `cd mobile && go test ./bind/ -count=1` 因缺生产符号而**红**（编译失败），原文记台账。

---

## 7. Task T2：绑定面生产实现（跑绿）

### 文件范围

```text
mobile/bind/bind.go      # 改：把单例改成 coreAPI 窄接口 + 真实核；新增 Package 注释边界
mobile/bind/session.go   # 新：sessionAPI + fail-closed 占位 + SessionCookie/SwitchMachine
```

- 不改 `mobile/bind/bind.go` 的既有四导出函数签名（只改内部依赖与注释）。
- 不改 `mobile/go.mod`/`go.sum`（本卡无新依赖）。
- 测试范围：只跑 `cd mobile && go test ./bind/ -count=1`（转绿）。
- 关键节点日志：见下（成功路径不静默；每条错误分支带 `machine`/`cause`；禁 `print`）。
- 注释：两文件头写职责+边界；导出函数写参数/返回/注意事项。

### Interfaces

- Consumes：`mobilecore.New(nil, *slog.Logger) *Core`、`mobilecore.PairResult`；§3.1(b) 的会话入口（占位期 fail-closed）。
- Produces：`coreAPI`、`sessionAPI`、`errSessionsNotWired`、`notWiredSessions`；导出 `SessionCookie`/`SwitchMachine`（其余四导出函数签名不变）。

### 步骤（红绿之「绿」）

1. 用下面的完整内容**替换** `mobile/bind/bind.go`：

```go
// Package bind 是移动端 App 的 gomobile 绑定面（B369）。
//
// 职责：把 internal/mobilecore 的连接核能力收敛成 gomobile 可导出的最小面
// （配对、回环源、配对清单、会话 cookie 桥接、切机、关），编为 Android AAR /
// iOS XCFramework。壳（Kotlin/Swift）只经这里调用，协议零重实现。
//
// 边界：本包不含任何协议逻辑；一切拨号/选路/兑换复用主模块 internal/mobilecore。
// 导出面最小 = 绑定面积最小，且**不含 Token/Dial**（回环门禁承重属性：绑定面
// 不得暴露绕过 cookie 闸的方法）。
package bind

import (
	"context"
	"log/slog"

	"github.com/Xsxdot/handoff/internal/mobilecore"
)

// coreAPI 是绑定面对连接核的窄消费面（duck typing）。四方法 = contract §3.2
// 已冻结的 Core 导出面，`*mobilecore.Core` 直接满足。
type coreAPI interface {
	Pair(ctx context.Context, bundleJSON string) (mobilecore.PairResult, error)
	Origin(machine string) (string, error)
	MachineNames() []string
	Close() error
}

var (
	log = slog.Default()
	// core 指向真实连接核；测试用 swapCore 注入替身。
	core coreAPI = mobilecore.New(nil, log)
)

// Pair 解析一份配对 bundle 载荷并登记其中的机器，返回各机器的 webview 源。
// 离线机器标记 online=false（部分 bundle，不整单失败）。
func Pair(bundleJSON string) (mobilecore.PairResult, error) {
	log.Info("绑定面收到配对请求", "payload_bytes", len(bundleJSON))
	res, err := core.Pair(context.Background(), bundleJSON)
	if err != nil {
		log.Error("绑定面配对失败", "cause", err)
		return mobilecore.PairResult{}, err
	}
	log.Info("绑定面配对完成", "machines", len(res.Machines))
	return res, nil
}

// Origin 返回某台机器 webview 应加载的 loopback 源（形如 http://127.0.0.1:<port>/）。
// 未配对机器返回 error，不返回空串冒充成功。
func Origin(machine string) (string, error) {
	origin, err := core.Origin(machine)
	if err != nil {
		log.Error("绑定面取回环源失败", "machine", machine, "cause", err)
		return "", err
	}
	return origin, nil
}

// MachineNames 返回已登记机器名（设置页配对清单）。
func MachineNames() []string {
	names := core.MachineNames()
	log.Debug("绑定面取配对清单", "count", len(names))
	return names
}

// Close 收掉全部回环反代与客户端（幂等）。
func Close() error {
	if err := core.Close(); err != nil {
		log.Error("绑定面关闭失败", "cause", err)
		return err
	}
	log.Info("绑定面已关闭")
	return nil
}
```

2. 新增 `mobile/bind/session.go`（完整内容）：

```go
package bind

import "errors"

// 本文件是 P4=A 要求的核侧会话入口适配层：绑定面导出的 SessionCookie /
// SwitchMachine 是**稳定的壳契约**；核侧实际方法名以 S2（B369.2）落地为准，
// S2 到位后只改本文件的 sessions 装配，不动导出面。
//
// 为什么用 interface 而非直接调 *mobilecore.Core：S2 尚未落地，S4 不发明 S2 的
// 冻结签名；接口让绑定层可编译、可测，且把「名字对齐」收敛到一处。

// sessionAPI 是绑定面对核侧会话入口的窄消费面（duck typing）。
type sessionAPI interface {
	// SessionCookie 返回某台机器已兑换的会话 cookie 值（核侧持有，壳注入 webview）。
	SessionCookie(machine string) (string, error)
	// SwitchMachine 清掉上一台机器的会话并为目标机器重兑换，返回目标 loopback 源。
	SwitchMachine(machine string) (string, error)
}

// errSessionsNotWired 是 S2 未落地时的 fail-closed 结果：绝不静默成功。
var errSessionsNotWired = errors.New("核侧会话入口未接线（S2 未落地）")

// notWiredSessions 在 S2 落地前占住 sessionAPI 的生产接线点。
type notWiredSessions struct{}

func (notWiredSessions) SessionCookie(string) (string, error) { return "", errSessionsNotWired }
func (notWiredSessions) SwitchMachine(string) (string, error) { return "", errSessionsNotWired }

// sessions 是核侧会话入口的装配点；S2 落地后在此接 *mobilecore.Core 的适配器。
var sessions sessionAPI = notWiredSessions{}

// SessionCookie 返回某台机器当前会话 cookie 的值，供壳注入 webview cookie jar。
// 空串 + error 表示未配对或兑换失败；**绝不返回 token**。
func SessionCookie(machine string) (string, error) {
	value, err := sessions.SessionCookie(machine)
	if err != nil {
		log.Error("绑定面取会话 cookie 失败", "machine", machine, "cause", err)
		return "", err
	}
	// 凭据卫生：只记长度，不记值。
	log.Debug("绑定面取会话 cookie 完成", "machine", machine, "value_len", len(value))
	return value, nil
}

// SwitchMachine 切机：核侧清掉上一台机器的会话并为目标机器重兑换，返回目标
// loopback 源。壳拿到返回后须清空 webview cookie jar 再注入 SessionCookie 的值。
func SwitchMachine(machine string) (string, error) {
	log.Info("绑定面切机", "machine", machine)
	origin, err := sessions.SwitchMachine(machine)
	if err != nil {
		log.Error("绑定面切机失败", "machine", machine, "cause", err)
		return "", err
	}
	log.Info("绑定面切机完成", "machine", machine, "origin", origin)
	return origin, nil
}
```

3. 跑绿：

```bash
cd mobile && go build ./... && go test ./bind/ -count=1 && gofmt -l . && go vet ./...
```

预期：`go build` 空输出成功；`go test` → `ok github.com/Xsxdot/handoff/mobile/bind`；`gofmt -l` 空输出；`go vet` 空输出。本节点探针实测 bind 包 10 支测试全 PASS（外加 T3 的 mobile 根包 1 支，共 11 支，见 §11.5）。

4. 变异验证（判据有牙，本节点已实测）：

- 在 `session.go` 把 `notWiredSessions.SessionCookie` 改成 `return "", nil` → `TestBindSessionFailIsClosed` 必须红（报文 `核侧会话入口未接线时必须返回错误，不得静默成功`）；还原后绿。
- 在 `bind.go` 末尾加 `func Token() string { return "x" }` → `TestBindExportedSurfaceIsFrozen` 必须红（报文打印 got/want 集合差异）；删除后绿。
- 在 `session.go` 加 `_ "github.com/Xsxdot/handoff/internal/relay"` → `TestBindProductionHasNoProtocolLogic` 必须红；删除后绿。
- 把 `SessionCookie` 里的 `sessions.SessionCookie(machine)` 改成 `sessions.SwitchMachine(machine)` → `TestBindSessionCookieAndSwitchAreOneJarToOneMachine` 必须红（报文 `取 devbox cookie: value="http://127.0.0.1:42002" ...`）；还原后绿。

### T2 验收

- `cd mobile && go build ./...`、`go test ./bind/ -count=1`、`gofmt -l .`、`go vet ./...` 全 0 退出。
- 导出面恰好六函数、逐字等于 §3.2；无 Token/Dial；绑定层生产文件只 import 标准库与 `internal/mobilecore`。
- 四条变异均能使对应测试红，还原后绿（原文记台账）。
- 日志：`Pair`/`Close` 成功路径有 Info；`SessionCookie` 只记 `value_len` 不记值；各错误分支带 `machine`/`cause`；无 `print`。

---

## 8. Task T3：嵌套 module 隔离守卫

### 文件范围（新增）

```text
mobile/moduleisolation_test.go   # 新：mobile/ 是嵌套独立 module，不进根构建图
```

- 不改 `moduleisolation_test.go`（仓根，守 desktop/）。
- 测试范围：`cd mobile && go test . -count=1`（本 task 新增的守卫）+ 复跑 §7 的 bind 测试确保不破。
- 日志/注释：文件头写职责+先例；helper 写参数/返回。

### Interfaces

- Consumes：`go list ./...`（在仓根与 mobile/ 两个 cwd 各跑一次）、`runtime.Caller`。
- Produces：`mobileRoot(t)`、`TestMobileModuleStaysOutOfParentBuildGraph`。

### 步骤

1. 新增 `mobile/moduleisolation_test.go`（完整内容）：

```go
// 本文件守住一条承重边界：移动端薄壳 module（mobile/）是嵌套的独立 module，
// 它的 Go 连接核经 gomobile 编为 AAR/XCFramework，绝不能进入主模块构建图。
//
// 先例：仓根 moduleisolation_test.go#TestDesktopModuleStaysOutOfParentBuildGraph
// 守 desktop/（用 runtime.Caller 定位自己，不依赖 go test 的 cwd）；
// 本文件守 mobile/，两条边界同款不同物。
package main

import (
	"os/exec"
	"path/filepath"
	"runtime"
	"strings"
	"testing"
)

// mobileRoot 返回 mobile/ 模块根（本测试文件所在目录），不依赖 go test 的 cwd。
func mobileRoot(t *testing.T) string {
	t.Helper()
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	return filepath.Dir(thisFile)
}

// TestMobileModuleStaysOutOfParentBuildGraph：主模块 `go list ./...` 不得含
// mobile/ 模块的包；同时 mobile/ 模块自己必须能列出 bind 包。
func TestMobileModuleStaysOutOfParentBuildGraph(t *testing.T) {
	root := filepath.Dir(mobileRoot(t))
	mobile := mobileRoot(t)

	cmd := exec.Command("go", "list", "./...")
	cmd.Dir = root
	out, err := cmd.CombinedOutput()
	if err != nil {
		t.Fatalf("在仓根 go list ./... 失败: %v\n%s", err, out)
	}
	for _, line := range strings.Split(string(out), "\n") {
		if line == "github.com/Xsxdot/handoff/mobile" ||
			strings.HasPrefix(line, "github.com/Xsxdot/handoff/mobile/") {
			t.Fatalf("主模块的构建图里出现了移动端包 %q——"+
				"mobile/ 必须是嵌套的独立 module（自带 go.mod + replace ../），"+
				"否则 gomobile 绑定面会被卷进根模块构建图", line)
		}
	}

	cmd = exec.Command("go", "list", "./...")
	cmd.Dir = mobile
	out, err = cmd.CombinedOutput()
	if err != nil {
		t.Fatalf("在 mobile/ go list ./... 失败: %v\n%s", err, out)
	}
	if !strings.Contains(string(out), "github.com/Xsxdot/handoff/mobile/bind") {
		t.Fatalf("mobile/ 模块未列出 bind 包，嵌套 module 形状漂移: %q", out)
	}
}
```

2. 跑绿：

```bash
cd mobile && go test . -count=1
```

预期：`ok github.com/Xsxdot/handoff/mobile`。本节点已在真实工作树实测通过（报文 `ok ... 0.318s`）。

3. 变异验证（判据有牙）：把 `mobile/go.mod` 的 `module github.com/Xsxdot/handoff/mobile` 临时改成 `module github.com/Xsxdot/handoff`（或删 `mobile/go.mod`），本守卫的第二个断言或整个模块定位会漂移；**只做到能观察到红即还原**，`mobile/go.mod` 不得带任何改动进提交。

> 注：本节点实测的一个坑——`exec.Command` 的 `cmd.Dir` 只改进程 cwd，不改 Go 的模块定位；`go -C <root> list ./...` 同理会指向 agent 进程所在模块。**必须用 `cmd.Dir`**（本节点实测 `cmd.Dir=<仓根>` 时输出 69 行且不含 mobile，`cmd.Dir=<mobile>` 时输出 `.../mobile/bind`）。不要改用 `t.Setenv("PWD")` 或 `go -C`。

### T3 验收

- `cd mobile && go test ./... -count=1` 退出 0，含 `TestMobileModuleStaysOutOfParentBuildGraph`。
- 仓根 `go list ./...` 与 mobile/ 模块各自形状被锁；`mobile/go.mod`/`go.sum` 零改动。

---

## 9. Task T4：收口复核

### 文件范围

- 改：无（纯验证）。
- 测试范围：仅下列命令；全量测试由协调者统一执行，不归本卡。

### 步骤与预期（本节点均已实测）

```bash
cd mobile && go build ./...                       # 预期：空输出，EXIT=0
cd mobile && go test ./... -count=1               # 预期：ok mobile + ok mobile/bind
cd mobile && go test ./bind/ -race -count=1       # 预期：ok（无数据竞争）
cd mobile && gofmt -l .                           # 预期：空输出
cd mobile && go vet ./...                         # 预期：空输出
go build ./...                                    # 根模块：EXIT=0
go list ./...                                     # 根模块：0 个 handoff/mobile 命中
go test ./cmd/ -run TestRepoContractGate -count=1 # 预期：ok
codegraph --repo . check --view cards-B369-charter   # 预期："fails": []
codegraph --repo . validate --view cards-B369-charter # 预期：issues=null、edgeIssues=null
codegraph --repo . check                          # 基线：预期："fails": []
git diff --check                                  # 预期：空输出
```

**图说明**：本卡新增/修改符号全在 `mobile/`（图外），**不产生图 diff**。若上述任一闸意外变红，停下提问，不自行改 `codegraph/` 冻结物。

**红区声明**：本卡由协调者/执行者在真实开发机上跑；**AAR/XCFramework 重跑与真机 cookie 注入归真机/移动 CI**（§11），不得把「`go build` 通过」写成「能 bind」或「cookie 已注入」。

---

## 10. 验收判据（行为化，边界型机内闭环）

1. `cd mobile && go build ./...`、`go test ./... -count=1`、`gofmt -l .`、`go vet ./...` 全 0 退出。
2. 绑定面导出**恰好**六函数，逐字等于 §3.2；`TestBindExportedSurfaceIsFrozen` 通过，且加 `Token()`/`Dial()` 会让它红。
3. `SessionCookie`/`SwitchMachine` 经绑定导出缝可调用；切机后旧机 cookie 不可取、两台机器 cookie 不同、切机返回不同 loopback 源——由 `TestBindSessionCookieAndSwitchAreOneJarToOneMachine` 锁定，改错转发会红。
4. S2 未接线时 `SessionCookie`/`SwitchMachine` 返回 error（fail-closed），由 `TestBindSessionFailIsClosed` 锁定，改 `return "", nil` 会红。
5. 绑定面返回 struct 字段只含 string/bool/struct 切片（`TestBindResultTypesAreGomobileSafe`）；绑定层生产文件零 `relay`/`client`/`proto` import（`TestBindProductionHasNoProtocolLogic`，加 import 会红）。
6. 根模块 `go list ./...` 0 个 mobile 命中；mobile/ 模块能列出 bind；由 `TestMobileModuleStaysOutOfParentBuildGraph` 锁定。
7. `go test ./cmd/ -run TestRepoContractGate -count=1` PASS；`codegraph check/validate --view cards-B369-charter` 与基线 `check` 保持 0 fail / 0 issue。
8. 交付物仅：`mobile/bind/bind.go`（改）、`mobile/bind/session.go`（新）、`mobile/bind/bind_test.go`（新）、`mobile/bind/export_surface_test.go`（新）、`mobile/bind/fakes_test.go`（新）、`mobile/moduleisolation_test.go`（新）；`internal/**`、`mobile/go.mod`、`mobile/go.sum`、`web/**`、`cmd/**` 零改动。

---

## 11. 五项检查（自审）

### 11.1 缺陷族对抗审查

| 族 | 设问与结论 | 落点 |
|---|---|---|
| 1 生命周期/状态机中断 | 绑定面是包级单例 `core`/`sessions`，`Close` 幂等（既有实现）；`Close` 后 `Pair` 由核返回错误（S2 面）。本卡不新增状态机 | `TestBindCloseForwardsToCore` 锁错误上抛 |
| 2 静默失败/误导报错 | **命中并锁死**：S2 未接线不得静默成功；核侧错误不得吞成 nil；`Origin` 未配对不返空串 | `TestBindSessionFailIsClosed`、`TestBindPairSurfacesError`、`TestBindOriginSurfacesError` |
| 3 跨平台假设 | gomobile AAR/XCFramework 是平台产物，本工作树 linux 不可验；**未验证，需真机/移动 CI** | §0/§9 红区声明 |
| 4 假红/假绿 | 四条变异（fail-open、Token 导出、协议 import、cookie 错转发）均实测可红；`go build` ≠ 能 bind，文档明说 | §7 变异验证、§9 红区 |
| 5 门禁绕过 | **承重命中**：绑定面不得含 Token/Dial；源码守卫 + 协议 import 守卫 | `TestBindExportedSurfaceIsFrozen`、`TestBindProductionHasNoProtocolLogic` |
| 6 序列化边界 | 跨 gomobile 映射边界：返回 struct 字段须 string/bool/struct 切片；`SessionCookie` 返回 string 值被逐字断言 | `TestBindResultTypesAreGomobileSafe`、`TestBindSessionCookieAndSwitchAreOneJarToOneMachine` |
| 7 枚举新值过既有白名单 | 本卡不新增枚举 | — |
| 8 承重安全属性 | 导出面不含 token（见 5）；`Close` 幂等；cookie 值只记长度不记值 | 同上 + §7 日志约束 |
| 9 webview 候选族 | 壳的 webview cookie 注入归真机 | §0/§9 |

### 11.2 序列化边界设问

新增数据从产生到消费：`SessionCookie(machine) (string, error)` 的值来自核（S2 落地后），经 gomobile 映射到 Kotlin/Swift 的 `String`，再注入 webview cookie jar。**本卡机内锁**：(a) 返回值是 `string`（非结构体/接口/`time.Time`），(b) 绑定层逐字返回核值（`sess-devbox` 断言，非 origin），(c) `PairResult`/`PairedMachine` 经 gomobile 的字段形状只含 string/bool。**真实注入行为未验证，需真机**（§0）。「两端各自有测试」不成立——壳侧测试不进本仓（P5=A），故本卡只锁机内这一环，并在 plan 明示断点。

### 11.3 上下文预算检查

有界文件集：`mobile/bind/{bind.go,session.go,bind_test.go,export_surface_test.go,fakes_test.go}` + `mobile/moduleisolation_test.go`（6 个文件，全在一个嵌套 module 内）。不触 `internal/**`/`web/**`/`cmd/**`。圈得出。

### 11.4 类型标注

边界型子系统；机内清单见 §10；**真机清单**：① iOS `WKHTTPCookieStore` / Android `CookieManager` 注入本卡 `SessionCookie` 后 webview 是否带 cookie；②切机清罐是否彻底、无 cookie 的同机请求是否被 agentd 拒（回环门禁）；③`gomobile bind` 出 AAR/XCFramework（拆解 §5 #2/#3）。

### 11.5 接缝覆盖（双向）

**测试 → 缝**（看入口调用符号）：

| 测试 | 入口符号（缝） |
|---|---|
| `TestBindPairForwardsToCore` / `TestBindPairSurfacesError` | `Pair` |
| `TestBindOriginSurfacesError` | `Origin` |
| `TestBindMachineNamesForwardsToCore` | `MachineNames` |
| `TestBindCloseForwardsToCore` | `Close` |
| `TestBindSessionCookieAndSwitchAreOneJarToOneMachine` | `SessionCookie` + `SwitchMachine` |
| `TestBindSessionFailIsClosed` | `SessionCookie` + `SwitchMachine` |
| `TestBindResultTypesAreGomobileSafe` | 返回类型（`Pair`/`SessionCookie` 的值形状） |
| `TestBindExportedSurfaceIsFrozen` / `TestBindProductionHasNoProtocolLogic` | 导出面本身（源码级守卫） |
| `TestMobileModuleStaysOutOfParentBuildGraph` | `go list ./...` 模块边界（缝：嵌套 module） |

**缝 → 测试**：§3.2 六导出函数——`Pair`/`Origin`/`MachineNames`/`Close`/`SessionCookie`/`SwitchMachine` 各至少一支缝级断言；导出面冻结与协议 import 各一支源码级守卫；嵌套 module 一支。覆盖齐。

**内部锁声明**：无内部锁。每支测试入口都落在导出函数缝或导出面/模块边界缝上；`fakeCore`/`fakeSessions` 是注入替身，不单独成测试。`resultTypes()` 是辅助函数，只在 `TestBindResultTypesAreGomobileSafe` 内用。**无「从声明缝构造不出该断言」的例外**，故无需退路声明。

**退路同闸**：本 plan 无「若意外先绿就改成直喂 X」类条件退路。

---

## 12. 自审三查

1. **spec/契约覆盖逐条能指到 task**：
   - contract §4 条 22（mobile/ 独立嵌套 module）→ T3；条 23（根模块零 import mobile/）→ T3。
   - contract §5（组装点 = `mobile/bind/bind.go`，图外）→ §2 + T1/T2。
   - contract §8.2（程序化兑换与 cookie jar 桥接）→ T2 的 `SessionCookie`/`SwitchMachine`（消费面声明）+ §0 未验证声明。
   - breakdown §3.5③（`cd mobile && go build ./...` 0；根 `go list` 不含 mobile；导出面源码断言；P4=A 桥接方法编译期与单元断言（fake 核）；导出面不含 Token/Dial）→ T1/T2/T3 + §10。
   - breakdown §3.5④（有界文件集 `mobile/bind/bind.go` + 可能 `session.go`）→ §7 文件范围。
   - spec §实现决定「回环门禁：反代不注入凭据；程序化兑换在 Go 核；绑定面暴露桥接」→ 本卡只做桥接导出，不碰反代（已在 proxy.go 由 Ticket 0 锁）。
2. **占位符扫描**：无 TBD /「加适当的错误处理」/「同 Task N」/描述性占位。所有文件给到完整可编译代码块。`SessionCookie`/`SwitchMachine` 的核侧消费面是**显式声明的窄接口**（协调者工单裁决②允许的形状），不是占位符——它可编译、可测、fail-closed，且给出 S2 到位后的替换点。
3. **跨 task 类型/签名一致性**：T1 测试引用的符号（`core`/`coreAPI`/`sessions`/`sessionAPI`/`SessionCookie`/`SwitchMachine`/`errBoom`）与 T2 生产定义的签名逐字一致；T3 的 `package main` 与仓根隔离测试同款；T1/T2/T3 的测试命令共享同一模块（`cd mobile`）。

---

## 13. 未验证，需真机 / 后续

- **真实 cookie 注入**：iOS `WKHTTPCookieStore` / Android `CookieManager` 收到本卡 `SessionCookie` 的值后 webview 是否带 cookie、切机清罐是否彻底、无 cookie 同机请求被拒——**未验证，需真机**（拆解 §5 #2）。
- **AAR/XCFramework 重跑**：本工作树 linux 无 Xcode/NDK；contract §0 的 darwin gate 为据，本卡不重跑——**`go build` 通过 ≠ `gomobile bind` 通过**（拆解 §5 #3）。
- **S2 对齐**：`sessionAPI` 的方法名/签名在 S2（B369.2）落地后对齐；届时把 `sessions` 从 `notWiredSessions{}` 换成核适配器。本卡不发明 S2 冻结物。
- **壳工程**：P5=A 壳不进本仓，`mobile/bind` 是本卡唯一交接面。

---

## 14. 红线与不做

- 不改 `internal/mobilecore/**`（S2 的面）；不改 `mobile/go.mod`/`go.sum`（S3 的面）；不产出壳工程（P5=A）。
- 绑定面**不得**导出 `Token`/`Dial`/`Credential` 或任何可绕过 cookie 门禁的方法（源码守卫兜底）。
- 绑定层生产文件**不得** import `internal/relay`/`internal/client`/`internal/proto`（协议零重实现）。
- 不改 `codegraph/` 冻结物；`mobile/` 图外，本卡不产生图 diff。
- 不新建实现卡、不派发、不调 handoff CLI、不切分支、不改 git 配置。
