# B369.6（S6）响应式四 tab 与终端输入层 —— 实现计划

- 卡号：B369.6（父卡 B369；拆解序号 S6）
- 有效基线分支：`cards/B369-charter-4`（本卡合并目标；本节点工作分支 `cards/B369.6-charter`，父卡 `4ab6415a`）
- 冻结 contract：`docs/superpowers/specs/b369-contract.md` @ `6f10ba7c`（§8.10 明文：web 响应式断点谱系与移动终端输入层「纯内容面，归 web 实现节点」）
- 已拍板拆解：`docs/superpowers/specs/b369-breakdown.md` @ `4ab6415a`（P1=A / P2=A / P3=A；§3.7 S6 四段式）
- 上游 spec：`docs/superpowers/specs/2026-09-13-mobile-app-design.md`（状态：已批准；实现决定段：IA 镜像 B358/B361、断点谱系、终端输入层、webview 固定回环源）
- 图视图：`codegraph/diffs/cards-B369-charter.json`（view=`cards-B369-charter`，base `977720e2`）
- 本节点产出物：`docs/superpowers/plans/b369.6-plan.md`（计划文档；不写实现代码）

---

## 0. 本卡干什么（一段话）

把 spec 的移动 IA 与终端输入落到真实控制台：**一份 UI 产物**（桌面/移动同源、响应式），
紧凑视口（phone/pad）下 Shell 切出底栏四个一级 tab（会话 | 卡 | 项目 | 设置），
会话列表为家（复用 `SessionSidebar`）、卡 tab 镜像工作项账本（复用 `CardsPage`/`CardItem`，
状态词表与 `internal/ledger/types.go` 逐值一致）、项目 tab 复用 `ProjectTree`（离线位置只标状态、不降级只读）、
设置 tab 复用 `SettingsPage`；终端加移动键条组件（**非 `terminal*` 前缀**，P3 约束）
并补 IME 组合输入的机内单元测试。桌面视口行为零漂移。

**P1=A**：底栏是同一份 `Shell` 的条件渲染，复用既有 `/cards`、`/settings`、会话列表、项目树，**不新开 `/m` 路由树**。
**P2=A**：卡 tab 只做布局适配 + 复用既有 move/评论入口，**不新增账本写入面**。
**P3=A**：终端输入留本卡；键条做成独立组件且**不得**再造第 6 个 `terminal*` 前缀源文件；IME 真机走真机清单。

**边界**：零 Go 改动、零新增 HTTP 协议/DTO；`web/src/api/**` 一行不改（P2=A 的「复用既有 move」即此）。

---

## 1. 基线事实（本轮实测，动手前已复核）

| 事实 | 原始读数 |
|---|---|
| 根模块 build | `go build ./...` → EXIT=0 |
| mobile 模块 build | `cd mobile && go build ./...` → EXIT=0 |
| proto/mobilecore 测试 | `go test ./internal/proto/... ./internal/mobilecore/... -count=1` → 两者 `ok`（EXIT=0） |
| 契约闸 | `go test ./cmd/ -run TestRepoContractGate -count=1` → `ok github.com/Xsxdot/handoff/cmd 0.069s` |
| 图闸（view） | `codegraph check --view cards-B369-charter` → `fails=[]`；`codegraph validate --view cards-B369-charter` → `issues=null`、`edgeIssues=null`；基线 `codegraph check` → `fails=[]` |
| web 依赖 | `node v24.16.0`；`vitest 4.1.10`（`cd web && npm ci` 实装后，基线 130 个测试文件） |
| **web 全量测试（基线）** | `cd web && npx vitest run` → `Test Files 130 passed (130)`、`Tests 1414 passed (1414)`、`Duration 23.00s` |
| **web typecheck（基线）** | `cd web && npx tsc -b` → EXIT=0 |
| **web eslint（基线，红）** | `cd web && npx eslint .` → **EXIT=1，4 errors**：`terminalHostResponse.ts:45/47/49 no-control-regex`、`pty.ts:115 no-case-declarations`（详见 §4）。**这是本卡必须转绿的门，见 T0。** |
| 断点基建现状 | 全仓**零** `useMediaQuery`/viewport hook；`Shell.tsx` 只在 `isDesktopShell()` 一个维度分支；无 `mobile*` 组件（`grep -rn "mobile\|Mobile" web/src` 零命中） |
| jsdom 默认视口 | 实测探针：`window.innerWidth === 1024`；`window.matchMedia === undefined`（jsdom 30.0.1 不实现）。→ 断点判据必须用 `innerWidth`，且测试用 `Object.defineProperty` 可改写（探针已验证 hook 读到 375） |
| `useShellViewport` 探针 | 临时实现 + 探针：`viewportOf(375/767/768/1023/1024)` 分档正确；hook 在 jsdom 读到 stub 的 375 → `phone/true`，1024 → `desktop/false`（3 tests passed） |
| 账本词表（Go 真源） | `internal/ledger/types.go:15-19`：`StatusTodo="待办"` / `StatusDoing="进行中"` / `StatusReview="待审阅"` / `StatusDone="已完成"` / `StatusClosed="终止"`；探针从 web 测试内用 `node:fs` 读出同五值（见 T2） |
| 词表读源码可达 | 探针：`web/src/app/cards/` 下 `readFileSync(resolve(here,'../../../../internal/ledger/types.go'))` → 得到五个字面量（1 test passed） |
| xterm 版本与 IME 行为 | `web/node_modules/@xterm/xterm@5.5.0`。探针（真 `Terminal`）实测：`compositionstart` → 设 `textarea.value` → `compositionend`，**等一个宏任务后** `onData` 收到 `['你好']` 恰一次；补漏装/不装结果一致。出处：`@xterm/xterm@5.5.0/lib/xterm.js` 的 `CompositionHelper._finalizeComposition` 用 `setTimeout(...,0)` 落定（探针即最小验证程序，见 T4） |
| 既有终端输入测试 | `terminalInput.test.ts`（301 行 fixture）已有「espanso 多字符」与「中文标点 input 先于 keydown」两组；无 composition 生命周期用例 |
| 终端 tab 渲染缝 | `TerminalTab`（`web/src/app/workbench/TerminalTab.tsx:112`）由 Shell 的 `renderContent` case `'terminal'` 注入（`Shell.tsx:832`）；`hostRef`/`termRef` 就在组件内，键条喂 `termRef.current?.input(seq)` 即与手敲输入合流 |
| eslint 修复探针 | ①给 `terminalHostResponse.ts` 加文件级 `/* eslint-disable no-control-regex -- ... */`；②把 `pty.ts` 的 `case 'attached':` 包成 `case 'attached': { ... }`。实测 `npx eslint .` → **EXIT=0（0 errors）**、`npx tsc -b` → EXIT=0；两文件已 `git checkout --` 还原，工作树干净 |

**库行为出处**：本卡引用的唯一库行为是 xterm 5.5.0 的 composition 落定时序（上表出处 + 探针）；断点是浏览器 `innerWidth`，不需要库出处；账本词表以 `internal/ledger/types.go:15-19` 为真源。

---

## 2. 图覆盖债

本轮亲自执行 `codegraph sym <名> --view cards-B369-charter`：

- **命中**：`Shell`（`n_web_app_shell_Shell`）、`ProjectTree`（`n_web_app_tree_ProjectTree`）、`CardItem`、`CardsPage`、`SettingsPage`、`FileTree`、`usePoll`、`useWorkbench`、`installTerminalInputFix`、`TerminalTab`、`isDesktopShell`、`defaultBoardLayout`。
- **未命中（图覆盖债）**：`SessionSidebar`、`SessionTab`、`sessionModel` 三个符号不在图（`codegraph sym` 返回「不在图中」，无近似候选；回落 grep 确认文件存在）。本卡新增符号 `useShellViewport`、`MobileTabBar`、`MobileKeybar`、`statusVocab` 同为**本轮首建**，图未覆盖，回落 grep 设计。
- **本卡不补图**：web 新增文件与符号的图对账（recon）归父卡 integrate 或后续图对齐节点；本节点只记债，不改 `codegraph/` 冻结物。
- web 侧新增模块同属 `d_web`（`d_web_shell`/`d_web_workbench`/`d_web_cards`），**不新增跨域边**；`codegraph check` 的跨域边判据不因本卡改变（收口复跑，T5）。

---

## 3. Task DAG

```text
T0 eslint 基线错误转绿（改 pty.ts / terminalHostResponse.ts，2 处）
      │  依据：整卡验收要求 `npx eslint .` 退出 0，而基线为 1（4 errors）
      ▼
T1 断点谱系 + Shell 条件渲染 + MobileTabBar（新 useShellViewport/MobileTabBar，改 Shell/TerminalTab 的 keybar 缝留到 T4）
      │  T1 是「紧凑视口 → 底栏四 tab + 桌面栏隐藏」的唯一点亮路径
      ▼
T2 卡 tab 状态词表与筛选（新 statusVocab + 改 CardsPage；词表与 Go 逐值一致）
      │
      ▼
T3 项目 tab 离线语义断言（只加 Shell 测试；复用 ProjectTree 既有离线行为）
      │
      ▼
T4 移动终端键条 + IME 组合输入（新 MobileKeybar；改 TerminalTab；补 IME 单测）
      │
      ▼
T5 收口：web 三闸（vitest/tsc/eslint）+ Go/图闸复跑（web-only，预期不变）
```

**最薄路径条**：本卡要锁的行为中，唯二「今天从声明缝调用得不到预期结果」的是——

1. **紧凑视口底栏**：现状零响应式分支，`Shell` 在任何宽度都渲染桌面三栏；T1 的第一支 Shell 测试（375 宽断言 `mobile-home` 与底栏四 tab）**写下去必红**，T1 即点亮该行为的最薄可跑路径。
2. **eslint 门**：基线 4 errors，`npx eslint .` 必红；T0 是整卡验收门的最薄路径。

其余（项目 tab 离线、卡词表、键条点击、IME）在既有实现/纯逻辑上「写下去就会绿」或属纯映射，不配独立竖切卡（各自在正文声明红绿范围）。

---

## 4. Task T0：eslint 基线错误转绿

### 4.1 精确文件与改动

两处，均为**既有文件的最小修复**，不改行为：

**文件 1**：`web/src/app/workbench/terminalHostResponse.ts`（在文件第 1 行之后插入一行）

现状第 1 行：

```ts
// terminalHostResponse —— 判断 xterm 解析输出时自动生成的设备回包与焦点报告。
```

改为（第 1 行原文不动，其后插入文件级禁用注释）：

```ts
// terminalHostResponse —— 判断 xterm 解析输出时自动生成的设备回包与焦点报告。
/* eslint-disable no-control-regex -- 本文件的核心就是匹配 ESC(0x1b) 控制序列，禁用该规则是唯一正确表达 */
```

**文件 2**：`web/src/api/pty.ts`（给 `case 'attached':` 加块作用域）

现状（`switch (ctrl.type)` 内）：

```ts
    switch (ctrl.type) {
      case 'attached':
        // 服务端说它从哪个字节开始给：以**它**的口径为准推进游标。
        // 用本地的猜测会在 truncated 时把游标停在一个环里已经没有的位置。
        cursor = ctrl.since
        const info: { since: number; truncated: boolean; backlog_bytes?: number } = {
          since: ctrl.since,
          truncated: ctrl.truncated,
        }
        // 缺键表示旧服务端；不要填 0，否则调用方会误把旧录像当成没有旧录像。
        if (typeof ctrl.backlog_bytes === 'number') info.backlog_bytes = ctrl.backlog_bytes
        options.onAttached(info)
        return
```

改为（只加 `{` 与 `}`，内部一行不动）：

```ts
    switch (ctrl.type) {
      case 'attached': {
        // 服务端说它从哪个字节开始给：以**它**的口径为准推进游标。
        // 用本地的猜测会在 truncated 时把游标停在一个环里已经没有的位置。
        cursor = ctrl.since
        const info: { since: number; truncated: boolean; backlog_bytes?: number } = {
          since: ctrl.since,
          truncated: ctrl.truncated,
        }
        // 缺键表示旧服务端；不要填 0，否则调用方会误把旧录像当成没有旧录像。
        if (typeof ctrl.backlog_bytes === 'number') info.backlog_bytes = ctrl.backlog_bytes
        options.onAttached(info)
        return
      }
```

### 4.2 步骤（红 → 绿；本 task 判据已在本节点基线复核）

1. **跑红（基线）**：`cd web && npx eslint .` → 预期 EXIT=1，含：

   ```
   /…/web/src/api/pty.ts
     115:9  error  Unexpected lexical declaration in case block  no-case-declarations
   /…/web/src/app/workbench/terminalHostResponse.ts
     45:25  error  Unexpected control character(s) in regular expression: \x1b  no-control-regex
     47:26  error  Unexpected control character(s) in regular expression: \x1b  no-control-regex
     49:23  error  Unexpected control character(s) in regular expression: \x1b  no-control-regex
   ```

2. 施加 §4.1 两处改动。
3. **跑绿**：`cd web && npx eslint .` → 预期 EXIT=0（**本节点实测：施加同样两处后 EXIT=0**）；`cd web && npx tsc -b` → 预期 EXIT=0（实测）。
4. **测试范围**：`npx eslint .` 与 `npx tsc -b`；本 task 不改组件逻辑，不跑 vitest（T1 起再跑）。
5. **注释**：两处禁用/加块都必须带上面给出的 why 注释——尤其 `no-control-regex` 的禁用理由，防止后来者「顺手」把 ESC 匹配改掉。
6. **提交**：单独一笔 `fix(web): 清除 eslint 基线错误（ESC 正则禁用 + case 块作用域）`。

---

## 5. Task T1：断点谱系 + Shell 条件渲染 + MobileTabBar

### 5.1 精确文件与改动

- **新**：`web/src/app/shell/useShellViewport.ts`
- **新**：`web/src/app/shell/MobileTabBar.tsx`
- **改**：`web/src/app/shell/Shell.tsx`（条件渲染 + 底栏；把既有 `ProjectTree` JSX 原样提为变量 `projectTree`）
- **改**：`web/src/app/shell/Shell.test.tsx`（新增紧凑视口用例）
- **新**：`web/src/app/shell/useShellViewport.test.tsx`、`web/src/app/shell/MobileTabBar.test.tsx`
- **测试范围（最小化）**：`npx vitest run src/app/shell`（只跑 shell 包；不跑全量）
- 关键节点日志：Shell 在视口档位变化时 `console.debug('shell.viewport', { viewport, compact })`；切移动 tab 时 `console.debug('shell.mobile_tab.select', { tab })`。禁 `print`；统一走既有 `console.debug` 观测约定（与 Shell 现有打点同族）。
- 注释：两个新文件头写职责+边界（见下代码）；非显然处（为什么用 `innerWidth` 而不是 `matchMedia`、为什么移动内容用绝对覆盖层而不是卸载工作台）必须写 why。

### 5.2 新文件全文

**`web/src/app/shell/useShellViewport.ts`**：

```ts
// useShellViewport —— 控制台响应式断点谱系（spec 实现决定：竖屏手机 / 折叠展开 / pad）。
//
// 职责：把当前视口宽度折成三档 phone/pad/desktop，并在 resize 时更新。
// 边界：
//   - 不渲染、不取数、不路由；只回答「现在是哪一档」
//   - 判据用 innerWidth 而不是 CSS media query：Shell 要按档位**切换渲染结构**
//     （挂/不挂底栏、桌面栏、工作台覆盖层），这是 React 分支，不是样式分支。
//     jsdom 不实现 matchMedia（实测 undefined），innerWidth 才是可测且真实存在的判据
import { useEffect, useState } from 'react'

// PHONE_MAX_WIDTH 对应 Tailwind 的 md 断点下沿（48rem=768px）之下一档：
// ≤767 为竖屏手机，768–1023 为折叠展开/pad，>1023 为桌面。
export const PHONE_MAX_WIDTH = 767
export const PAD_MAX_WIDTH = 1023

export type ShellViewport = 'phone' | 'pad' | 'desktop'

// viewportOf 是纯分档函数，供组件与测试共用。
export function viewportOf(width: number): ShellViewport {
  if (width <= PHONE_MAX_WIDTH) return 'phone'
  if (width <= PAD_MAX_WIDTH) return 'pad'
  return 'desktop'
}

// isCompactViewport 回答「是否走移动壳布局」：phone 与 pad 都是紧凑档。
export function isCompactViewport(viewport: ShellViewport): boolean {
  return viewport !== 'desktop'
}

// readViewportWidth 读数；非浏览器环境（SSR/测试无 window）回退到桌面档，
// 宁可渲染桌面壳也不要抛。
export function readViewportWidth(): number {
  return typeof window === 'undefined' ? PAD_MAX_WIDTH + 1 : window.innerWidth
}

// useShellViewport 返回当前档位。只监听 resize：视口变化在移动端由旋转/分屏触发。
export function useShellViewport(): ShellViewport {
  const [viewport, setViewport] = useState<ShellViewport>(() => viewportOf(readViewportWidth()))
  useEffect(() => {
    const update = () => setViewport(viewportOf(readViewportWidth()))
    update()
    window.addEventListener('resize', update)
    return () => window.removeEventListener('resize', update)
  }, [])
  return viewport
}
```

**`web/src/app/shell/MobileTabBar.tsx`**：

```tsx
// MobileTabBar —— 紧凑视口（phone/pad）下的底栏四个一级 tab（spec 实现决定）。
//
// 职责：渲染「会话 | 卡 | 项目 | 设置」四个切换钮与可选角标；选择动作经 onSelect 上抛。
// 边界：
//   - 不取数、不路由、不判断视口：视口判定归 useShellViewport，内容渲染归 Shell
//   - P1=A：它与桌面三栏是同一份 Shell 的条件渲染产物，不新开 /m 路由树；
//     本组件自身不持有任何页面状态
import { FolderGit2, MessagesSquare, Settings, SquareKanban, type LucideIcon } from 'lucide-react'

export type MobileTab = 'sessions' | 'cards' | 'projects' | 'settings'

const TABS: { key: MobileTab; label: string; Icon: LucideIcon }[] = [
  { key: 'sessions', label: '会话', Icon: MessagesSquare },
  { key: 'cards', label: '卡', Icon: SquareKanban },
  { key: 'projects', label: '项目', Icon: FolderGit2 },
  { key: 'settings', label: '设置', Icon: Settings },
]

export interface MobileTabBarProps {
  active: MobileTab
  onSelect: (tab: MobileTab) => void
  // needsCount 卡 tab 的「需要你」数；unread 会话 tab 未读聚合。两者 0 时不渲染角标。
  needsCount?: number
  unread?: number
}

export function MobileTabBar({ active, onSelect, needsCount = 0, unread = 0 }: MobileTabBarProps) {
  return (
    <nav
      data-testid="mobile-tabbar"
      aria-label="移动底栏导航"
      // pb-[env(safe-area-inset-bottom)]：iOS 底部安全区；桌面浏览器求值为 0。
      className="flex shrink-0 items-stretch border-t bg-background pb-[env(safe-area-inset-bottom)]"
    >
      {TABS.map(({ key, label, Icon }) => {
        const badge = key === 'sessions' ? unread : key === 'cards' ? needsCount : 0
        return (
          <button
            key={key}
            type="button"
            role="tab"
            aria-selected={active === key}
            data-testid={`mobile-tab-${key}`}
            onClick={() => onSelect(key)}
            className={`relative flex flex-1 flex-col items-center gap-0.5 py-2 text-[11px] ${active === key ? 'font-semibold text-foreground' : 'text-muted-foreground'}`}
          >
            <Icon className="size-5" />
            <span>{label}</span>
            {badge > 0 && (
              <span className="absolute right-4 top-1 min-w-4 rounded-full bg-red-500 px-1 text-center text-[10px] leading-4 text-white">
                {badge}
              </span>
            )}
          </button>
        )
      })}
    </nav>
  )
}
```

### 5.3 Shell.tsx 改动（逐处，锚点即现状行）

**S1｜import（在 `import { ResizableSidebar } from './ResizableSidebar'` 之后加两行）**：

```tsx
import { MobileTabBar, type MobileTab } from './MobileTabBar'
import { isCompactViewport, useShellViewport } from './useShellViewport'
```

**S2｜断点状态与下钻状态（插在 `const navigate = useNavigate()` 之后、`const location = useLocation()` 之前）**：

> 位置为什么在这里：`openCoordinatorTerminal`（现状 :130）在本卡里也要加一行 `openMobileDetail()`，而 `const openMobileDetail = …` 在同一个函数体里若声明在使用之后，依赖数组求值时会撞 TDZ。故整组状态与回调必须**先于** `openCoordinatorTerminal` 声明。

```tsx
  // 响应式断点（B369.6）：判据与理由见 useShellViewport 文件头。
  const viewport = useShellViewport()
  const compact = isCompactViewport(viewport)
  // 紧凑视口的底栏当前页；默认「会话」（spec：会话即工作单元、首屏=会话列表）。
  const [mobileTab, setMobileTab] = useState<MobileTab>('sessions')
  // mobileDetail：紧凑视口是否处在「下钻态」——打开了会话/任务/终端后，
  // 内容面从底栏首页切到常驻的工作台（spec 原型 mobile-home → mobile-session /
  // mobile-task 的下钻，复用既有工作台而不新开 /m 树）。返回条清回底栏首页。
  const [mobileDetail, setMobileDetail] = useState(false)
  // openMobileDetail 在紧凑视口把内容面切到工作台；桌面是空操作（工作台本就常驻）。
  // 为什么不做成 effect 监听 openedItems：用户点「返回」后底栏首页要能停住，
  // 而 openedItems 依然非空——"有 tab" 推不出 "想看它"。
  const openMobileDetail = useCallback(() => {
    if (compact) setMobileDetail(true)
  }, [compact])
  useEffect(() => {
    console.debug('shell.viewport', { viewport, compact })
  }, [viewport, compact])
```

**S2b｜紧凑视口的文件/终端下钻回调（插在 `const openTaskTui = …` 之后；S9b 的覆盖层消费。不含 `openDirectory`：它在紧凑视口只开 `fileDrawer`，由 S9b 的覆盖层呈现，不切工作台）**：

```tsx
  // 紧凑视口：目录覆盖层里点文件/开终端都切进工作台（下钻态）。
  // 桌面沿用既有右栏内联行为，这两个回调用不上。
  const openMobileFile = (base: BaseDir, rel: string) => {
    wb.open({ kind: 'file', rel }, base)
    openMobileDetail()
  }
  const openMobileTerminal = (base: BaseDir, rel: string) => {
    wb.openTerminal(base, undefined, rel)
    openMobileDetail()
  }
```

**S2c｜把下钻入口接进既有打开动作（五处各加一行 `openMobileDetail()`，其余一行不动）**：

- `openCoordinatorTerminal`（:130）：在 `wb.openTerminalWithCommand(...)` 之前加；
- `openSession`：在 `wb.openOrFocus(...)` 之前加；
- `openTaskTui`：在 `wb.openOrFocus({ kind: 'tui', taskId }, ...)` 之前加；
- `openTerminalAt`：在 `wb.openOrFocus(spawnTerminalContent(...), base)` 之前加；
- `openWorkbenchItem`：在 `wb.focusTab(...)` 之前加。

依赖数组相应把 `openMobileDetail` 加进去（五个都是 `useCallback`；加依赖后 `eslint react-hooks/exhaustive-deps` 才不报）。

**S2d｜紧凑视口屏蔽整页路由 + `fullPageRoute` 加 `!compact` 前缀**：

移动端四个一级 tab 已由 S9 的内容面承载；整页 Route 与移动内容面若同时渲染会出两份同名页面（撞 testid、双倍轮询、双份写入口）。故：

**S2d-1｜`fullPageRoute`（现状 :491）加 `!compact &&`**：

```tsx
  // 紧凑视口没有「整页路由」形态：底栏 tab 就是整页的移动对应物。加 !compact
  // 前缀后，移动下 /cards?card=X 这类深链不再触发整页分支，而是由 S9 的
  // CardsPage 覆盖层消费 query（同一组件、同一 useSearchParams）。
  const fullPageRoute = !compact && ['/cards', '/flows', '/settings', '/machines', '/codegraph']
    .some((path) => location.pathname.startsWith(path))
```

**S2d-2｜`<Routes>` 里三个整页 Route 加 `!compact` 门**（不渲染即与覆盖层无重复；桌面 `!compact` 恒真、行为逐字不变）：

- `/cards`+`/flows`（现状外包 `{ledgerEnabled && (<>…</>)}`）→ `{!compact && ledgerEnabled && (<>…</>)}`；
- `/settings` → `{!compact && (<Route path="/settings" … />)}`；
- `/codegraph` → `{!compact && (<Route path="/codegraph" … />)}`；
- **不加门**：`<Route path="/tasks/:id" …>`（深链 shim：`TaskDeepLink` 打开 TUI tab 后 `navigate('/')` 回工作台，mobileDetail 由 `openTaskTui` 置位，正是「通知点进来落到任务现场」的路径）与 `<Route path="/machines" …>`（重定向 `/settings`，紧凑下被屏蔽后落空，S9 覆盖层盖住，无可见影响）。

**S3｜账本未启用时的移动首屏兜底（插在 `const { enabled: ledgerEnabled } = useLedgerEnabled()` 之后）**：

```tsx
  // 账本未启用时会话/卡两个 tab 无内容面，移动首屏退到「项目」，
  // 否则首屏是一句「不可用」——状态门要与桌面门控（ledgerEnabled）一致。
  useEffect(() => {
    if (compact && !ledgerEnabled && (mobileTab === 'sessions' || mobileTab === 'cards')) {
      setMobileTab('projects')
    }
  }, [compact, ledgerEnabled, mobileTab])
```

**S4｜提取 ProjectTree 为变量（在 `return (` 之前、`const focusedTaskId = ...` 之后插入）**：

> **先决条件（必须同批做，否则 S4 引用未定义符号）**：`ProjectTree` 底部的入口在紧凑视口下要么因整块不挂（本卡 `!compact &&`）而不可达、要么走成整页路由——两者在「设置 tab 内容 = SettingsPage」下都不是想要的。故先在 `backToWorkbench` 定义之后加两个导航回调：

```tsx
  // openCardsSurface/openSettingsSurface 是两个「去别处看」入口的移动分支：
  // 紧凑视口下切底栏 tab（内容面直接换成对应页），桌面仍走整页路由。
  // 抽成回调解 ProjectTree 的既有 prop 契约不变（它只发信号，不关心去哪）。
  const openCardsSurface = () => {
    if (compact) setMobileTab('cards')
    else navigate('/cards')
  }
  const openSettingsSurface = () => {
    if (compact) setMobileTab('settings')
    else navigate('/settings')
  }
```

`ProjectTree` 的调用面因此改两处（现状 §5.3 S4 的片段里已体现）：`onOpenCards={() => navigate('/cards')}` → `onOpenCards={openCardsSurface}`；`onOpenSettings={() => navigate('/settings')}` → `onOpenSettings={openSettingsSurface}`。桌面分支行为逐字不变。

**S4 正文**：

```tsx
  // projectTree 是项目树的唯一实例来源：桌面左栏与紧凑视口「项目」tab 共用同一份
  // JSX（P1=A 一份产物）。两处同时挂载会撞 testid，故用 `!compact` 门保证任一时刻
  // 只挂一个实例（见下面两处消费点）。
  const projectTree = treeState.data === null ? null : (
    <ProjectTree
      tree={treeState.data}
      tasks={tasks}
      selectedKey={fileDrawer?.key ?? wb.base?.key ?? null}
      ticketCount={tickets.count}
      ticketsByDir={tickets.byDir}
      openItems={openItems}
      focusedTaskId={focusedTaskId}
      onFocusOpenItem={openWorkbenchItem}
      onCloseOpenItem={closeOpenItem}
      onOpenTerminalAt={openTerminalAt}
      onOpenDirectory={openDirectory}
      onOpenTask={openTaskTui}
      previews={previews}
      previewMachines={previewsState.data?.machines ?? []}
      previewOpenKeys={previewsState.openKeys}
      previewOpeningKeys={previewsState.openingKeys}
      onOpenPreview={(id, machine) => { void previewsState.open(id, machine).catch(() => {}) }}
      onOpenBoard={() => setOverlay('board')}
      onOpenCards={openCardsSurface}
      onOpenProjectCards={ledgerEnabled ? openProjectCards : undefined}
      onOpenFlows={() => navigate('/flows')}
      ledgerEnabled={ledgerEnabled}
      cardNeedsCount={cardNeedsCount}
      unlinkedCount={unlinkedTaskIds?.size ?? 0}
      onOpenTickets={() => setOverlay('tickets')}
      onOpenSettings={openSettingsSurface}
      onOpenCodegraph={() => navigate('/codegraph')}
      onOpenProjectCodegraph={openProjectCodegraph}
      onAddProject={() => setWizardOpen(true)}
      onEdit={(p) => setEditProject(p)}
      onUnregister={onUnregister}
      onWorktreeCreated={(project, machine, ws) => {
        treeState.refresh()
        wb.select(workspaceBase(project, machine, ws))
      }}
    />
  )
```

> 提取规则：**逐字复制**现状 `Shell.tsx` 里 `{treeState.data && (<ProjectTree … />)}` 内的 `<ProjectTree>` 元素（现状 :751-791 区间），只把外包的 `treeState.data &&` 换成上面的 `treeState.data === null ? null :`。任何 prop 不得改名、不得增删。

**S5｜桌面左栏消费变量（把现状 `{treeState.data && ( <ProjectTree …/> )}` 整块替换为一行）**：

```tsx
          {projectTree}
```

**S6｜左栏整体只在非紧凑挂载**：`<ResizableSidebar>` → `{!compact && (<ResizableSidebar>`；对应的 `</ResizableSidebar>` → `</ResizableSidebar>)}`。

**S7｜面包屑（现状 `{focusedBase && !desktop && !fullPageRoute && <Breadcrumb …/>}`）** 改成：

```tsx
        {focusedBase && !desktop && !compact && !fullPageRoute && <Breadcrumb base={focusedBase} tail={crumbTail} />}
```

**S8｜右栏文件树（现状 `{fileDrawer !== null && !fullPageRoute && (`）** 改成：

```tsx
      {!compact && fileDrawer !== null && !fullPageRoute && (
```

紧凑视口的目录内容改由 S9b 的覆盖层呈现（右栏在手机上无处安放）。

**S9｜紧凑覆盖层（插在 `<main className="relative min-h-0 min-w-0 flex-1 overflow-hidden">` 的 `</main>` 之前，即既有 `<Routes>…</Routes>` 之后）**：

```tsx
          {/* 紧凑视口的移动内容面。用绝对覆盖层而不是条件卸载：WorkbenchPage 必须
              常驻（B280——卸了 xterm 会断 WS 再重放，OpenTUI/Grok 卡死），覆盖层
              与既有 FullPageCover 同一手法，只是带底栏且由 tab 状态驱动。
              mobileDetail 为真时整层让开，露出下面的常驻工作台（下钻态）。 */}
          {compact && !mobileDetail && (
            <div data-testid="mobile-home" className="absolute inset-0 z-30 flex min-h-0 flex-col bg-background">
              <div className="min-h-0 flex-1 overflow-auto">
                {mobileTab === 'sessions' && (
                  ledgerEnabled ? (
                    <SessionSidebar
                      sessions={sessions}
                      loading={sessionsState.data === null && !sessionsState.disconnected}
                      errorText={sessionsState.disconnected ? sessionsState.errorText : ''}
                      needsOnly={needsOnly}
                      onToggleNeeds={() => setNeedsOnly((current) => !current)}
                      projectFilter={projectFilter}
                      onProjectFilter={setProjectFilter}
                      projectOptions={projectOptions}
                      projectOfCard={projectOfCard}
                      onOpen={openSession}
                      onCreate={() => { setCreateError(''); setCreateOpen(true) }}
                    />
                  ) : (
                    <p className="p-4 text-sm text-muted-foreground">账本未启用，会话不可用。</p>
                  )
                )}
                {mobileTab === 'cards' && <CardsPage onOpenCoordinatorTerminal={openCoordinatorTerminal} />}
                {mobileTab === 'projects' && (projectTree ?? <p className="p-4 text-sm text-muted-foreground">正在读取项目…</p>)}
                {mobileTab === 'settings' && <SettingsPage onClose={() => setMobileTab('sessions')} />}
              </div>
              <MobileTabBar
                active={mobileTab}
                onSelect={(tab) => {
                  console.debug('shell.mobile_tab.select', { tab })
                  // 先把整页路由收回工作台：从 /cards 等整页深链进来时，若不导航，
                  // Routes 里的整页会与下面的移动内容面各渲染一份 CardsPage（撞 testid）。
                  backToWorkbench()
                  setMobileDetail(false)
                  setMobileTab(tab)
                }}
                needsCount={ledgerEnabled ? cardNeedsCount : 0}
                unread={totalUnread(sessions)}
              />
            </div>
          )}
          {/* 下钻态的返回条：没有它，手机用户进了会话/任务现场就回不到底栏首页
              （桌面靠左栏与面包屑，手机两者都不挂）。固定在顶部，含当前焦点内容名。
              紧凑视口下 fullPageRoute 恒假（S2d-1），故不与整页路由互压。 */}
          {compact && mobileDetail && (
            <div
              data-testid="mobile-detail-bar"
              className="absolute inset-x-0 top-0 z-30 flex items-center gap-2 border-b bg-background px-2 py-1.5"
            >
              <button
                type="button"
                data-testid="mobile-detail-back"
                onClick={() => {
                  console.debug('shell.mobile_detail.back', { tab: mobileTab })
                  setMobileDetail(false)
                }}
                className="rounded px-2 py-1 text-sm text-muted-foreground hover:bg-accent hover:text-foreground"
              >
                ‹ 返回
              </button>
              <span className="min-w-0 flex-1 truncate text-sm">{crumbTail ?? focusedBase?.label ?? ''}</span>
            </div>
          )}
```

> 下钻态没有底栏：spec 原型里下钻页（mobile-session/mobile-task）是二级页，用返回条回一级；终端面板也给返回条让位（键条在底部，见 T4）。
> 位置校验：`mobile-home` 与 `mobile-detail-bar` 都落在 `<main>` 内，所以紧凑时工作台与整页路由仍在下面常驻；`fullPageRoute` 的整页覆盖（z-20）在它们之下，不会被压。`mobile-detail-bar` 是 z-30 绝对定位，会给工作台顶部盖住约 34px——终端/任务内容因此上移的风险由 `.relative` 容器内的覆盖层承担，这是既有 `FullPageCover` 同款取舍（真机走查项 §13 #1）。

**S9b｜紧凑视口的目录覆盖层（插在 S9 的 `mobile-detail-bar` 块之后）**：

```tsx
          {/* 紧凑视口的目录面（项目 tab → 位置 → 目录）：右栏无处安放，改为覆盖层，
              带返回条回项目列表。文件/终端下钻切工作台（S2b 的两个回调）——
              下钻时本层让开（!mobileDetail），返回条再把它露回来，形成
              任务/文件 → 目录 → 底栏首页 的逐级返回。 */}
          {compact && fileDrawer !== null && !mobileDetail && (
            <div data-testid="mobile-dir" className="absolute inset-0 z-30 flex min-h-0 flex-col bg-background">
              <div className="flex shrink-0 items-center gap-2 border-b px-2 py-1.5">
                <button
                  type="button"
                  data-testid="mobile-dir-back"
                  onClick={() => setFileDrawer(null)}
                  className="rounded px-2 py-1 text-sm text-muted-foreground hover:bg-accent hover:text-foreground"
                >
                  ‹ 返回
                </button>
                <span className="min-w-0 flex-1 truncate text-sm">{fileDrawer.label}</span>
              </div>
              <div className="min-h-0 flex-1 overflow-hidden">
                <FileTree
                  base={fileDrawer}
                  refreshKey={fileTreeNonce}
                  taskId={currentTaskId}
                  onOpenFile={(rel) => openMobileFile(fileDrawer, rel)}
                  onOpenTerminal={(rel) => openMobileTerminal(fileDrawer, rel)}
                  revealSupported={caps.reveal('')}
                  onClose={() => setFileDrawer(null)}
                />
              </div>
            </div>
          )}
```

> 层次：S9b 在 S9 之后，同 z-30 时后者胜出，故目录覆盖层盖住底栏页；关掉（返回）即回项目列表。桌面分支（`!compact`）的右栏文件树一行不动。

> **已知行为（记缺口，不阻塞）**：紧凑视口下 `/cards?card=B1` 这类深链不再驱动底栏 tab（`mobileTab` 初始恒 `sessions`），URL 里的 `card=` 会被 S9 的 `CardsPage` 覆盖层在切到卡 tab 后消费。移动端深链直落卡详情属增强，spec 未点名；将其纳入的入口是底栏 tab 与 `mobileTab` 的 URL 同步（本卡不做）。
**S10｜HomeDock 门控（现状 `{caps.pty('') !== false && ( <HomeDock …/> )}`）** 改成 `{!compact && caps.pty('') !== false && (` … `)}`，并在原位补注释：

```tsx
      {/* 浮窗终端是桌面形态：手机上它没有可拖动/可缩放的语义，紧凑视口不挂。
          终端在移动端经「项目 → 位置 → 终端」进入，键条见 T4。 */}
```

### 5.4 新增测试全文

**`web/src/app/shell/MobileTabBar.test.tsx`**：

```tsx
// MobileTabBar 的缝侧守卫：四个一级 tab 的存在、顺序、选择上抛与角标。
import { fireEvent, render, screen } from '@testing-library/react'
import { describe, expect, it, vi } from 'vitest'
import { MobileTabBar } from './MobileTabBar'

describe('MobileTabBar', () => {
  it('渲染四个一级 tab，顺序为 会话/卡/项目/设置', () => {
    render(<MobileTabBar active="sessions" onSelect={vi.fn()} />)
    const tabs = screen.getAllByRole('tab')
    expect(tabs.map((el) => el.getAttribute('data-testid'))).toEqual([
      'mobile-tab-sessions', 'mobile-tab-cards', 'mobile-tab-projects', 'mobile-tab-settings',
    ])
  })

  it('点击把目标 tab 上抛', () => {
    const onSelect = vi.fn()
    render(<MobileTabBar active="sessions" onSelect={onSelect} />)
    fireEvent.click(screen.getByTestId('mobile-tab-cards'))
    expect(onSelect).toHaveBeenCalledWith('cards')
  })

  it('active 反映在 aria-selected', () => {
    render(<MobileTabBar active="projects" onSelect={vi.fn()} />)
    expect(screen.getByTestId('mobile-tab-projects')).toHaveAttribute('aria-selected', 'true')
    expect(screen.getByTestId('mobile-tab-sessions')).toHaveAttribute('aria-selected', 'false')
  })

  it('角标：会话显示 Σ 未读、卡显示需要你数，0 时不渲染', () => {
    render(<MobileTabBar active="sessions" onSelect={vi.fn()} unread={2} needsCount={3} />)
    expect(screen.getByTestId('mobile-tab-sessions')).toHaveTextContent('2')
    expect(screen.getByTestId('mobile-tab-cards')).toHaveTextContent('3')
    expect(screen.getByTestId('mobile-tab-projects')).not.toHaveTextContent(/\d/)
  })
})
```

**`web/src/app/shell/useShellViewport.test.tsx`**：

```tsx
// 断点谱系的缝侧守卫：纯分档 + hook 真实视口读数（jsdom 默认 1024）。
// 不依赖 matchMedia（jsdom 不实现）；用 innerWidth，测试以 defineProperty 改写。
import { render, screen } from '@testing-library/react'
import { useEffect, useState } from 'react'
import { afterEach, describe, expect, it } from 'vitest'
import { isCompactViewport, PAD_MAX_WIDTH, PHONE_MAX_WIDTH, useShellViewport, viewportOf } from './useShellViewport'

function setWidth(width: number) {
  Object.defineProperty(window, 'innerWidth', { value: width, configurable: true, writable: true })
}

// Probe 把 hook 结果渲染出来——不用 renderHook，避免依赖版本差异。
function Probe() {
  const v = useShellViewport()
  const [seen, setSeen] = useState('')
  useEffect(() => { setSeen(`${v}/${isCompactViewport(v)}`) }, [v])
  return <span data-testid="vp">{seen}</span>
}

afterEach(() => setWidth(1024))

describe('useShellViewport', () => {
  it('分档：phone ≤767 / pad ≤1023 / desktop >1023', () => {
    expect(viewportOf(375)).toBe('phone')
    expect(viewportOf(PHONE_MAX_WIDTH)).toBe('phone')
    expect(viewportOf(PHONE_MAX_WIDTH + 1)).toBe('pad')
    expect(viewportOf(PAD_MAX_WIDTH)).toBe('pad')
    expect(viewportOf(PAD_MAX_WIDTH + 1)).toBe('desktop')
  })

  it('紧凑 = phone 或 pad', () => {
    expect(isCompactViewport('phone')).toBe(true)
    expect(isCompactViewport('pad')).toBe(true)
    expect(isCompactViewport('desktop')).toBe(false)
  })

  it('hook 按真实视口宽度取值（375 → phone）', async () => {
    setWidth(375)
    render(<Probe />)
    expect(await screen.findByText('phone/true')).toBeInTheDocument()
  })

  it('1024 宽判 desktop', async () => {
    setWidth(1024)
    render(<Probe />)
    expect(await screen.findByText('desktop/false')).toBeInTheDocument()
  })
})
```

**`Shell.test.tsx` 追加**（import 行把 `afterEach` 加进既有 `beforeAll, beforeEach, describe, expect, it, vi` 列表；在文件顶部 `beforeAll(...)` 之前加 `afterEach` 复位）：

```tsx
afterEach(() => {
  // 紧凑视口用例会改写 innerWidth；每个用例后复位成 jsdom 默认（桌面档），
  // 否则后面的桌面断言会被上一个用例污染（用例顺序耦合是隐性假绿）。
  Object.defineProperty(window, 'innerWidth', { value: 1024, configurable: true, writable: true })
})
```

```tsx
describe('B369.6 移动断点谱系', () => {
  it('紧凑视口渲染底栏四 tab、不渲染桌面左栏与右栏文件树', async () => {
    Object.defineProperty(window, 'innerWidth', { value: 375, configurable: true, writable: true })
    renderShell()
    await screen.findByTestId('mobile-home')
    for (const tab of ['sessions', 'cards', 'projects', 'settings']) {
      expect(screen.getByTestId(`mobile-tab-${tab}`)).toBeInTheDocument()
    }
    expect(screen.queryByRole('complementary', { name: '项目导航' })).toBeNull()
    expect(screen.queryByText('文件')).toBeNull()
  })

  it('桌面视口不渲染底栏（既有三栏行为零漂移）', async () => {
    Object.defineProperty(window, 'innerWidth', { value: 1440, configurable: true, writable: true })
    renderShell()
    await screen.findByText('handoff')
    expect(screen.queryByTestId('mobile-tabbar')).toBeNull()
    expect(screen.getByRole('complementary', { name: '项目导航' })).toBeInTheDocument()
  })

  it('切换底栏 tab 换成对应内容面，工作台仍常驻（不卸载）', async () => {
    Object.defineProperty(window, 'innerWidth', { value: 375, configurable: true, writable: true })
    renderShell()
    await screen.findByTestId('mobile-home')
    fireEvent.click(screen.getByTestId('mobile-tab-projects'))
    expect(await screen.findByTestId('project-node-p1')).toBeInTheDocument()
    // 工作台容器仍在 DOM（B280 keep-alive）
    expect(document.querySelector('[data-testid="workbench-group"]')).not.toBeNull()
  })

  it('项目 tab → 目录 → 覆盖层；返回条回到底栏首页（下钻往返）', async () => {
    Object.defineProperty(window, 'innerWidth', { value: 375, configurable: true, writable: true })
    renderShell()
    fireEvent.click(await screen.findByTestId('mobile-tab-projects'))
    // 与桌面 openBranch 同款：机器行点击展开工作树子行；已展开时不要重复点
    // （ProjectTree 的 toggle 会把它收回去）。
    if (screen.queryByText('integration/b2-b3') === null) {
      fireEvent.click(await screen.findByTestId('machine-row'))
    }
    fireEvent.click(await screen.findByText('integration/b2-b3'))
    expect(await screen.findByTestId('mobile-dir')).toBeInTheDocument()
    fireEvent.click(screen.getByTestId('mobile-dir-back'))
    await waitFor(() => expect(screen.queryByTestId('mobile-dir')).toBeNull())
    expect(screen.getByTestId('mobile-home')).toBeInTheDocument()
  })

  it('点会话进下钻态：底栏首页让开、返回条出现；返回回到底栏首页', async () => {
    Object.defineProperty(window, 'innerWidth', { value: 375, configurable: true, writable: true })
    vi.mocked(fetchSessions).mockResolvedValue([sessionSummary()] as never)
    renderShell()
    fireEvent.click(await screen.findByTestId('session-row'))
    expect(await screen.findByTestId('mobile-detail-bar')).toBeInTheDocument()
    // 下钻态：底栏首页整层让开，工作台仍在 DOM（keep-alive）
    expect(screen.queryByTestId('mobile-home')).toBeNull()
    expect(document.querySelector('[data-testid="workbench-group"]')).not.toBeNull()
    fireEvent.click(screen.getByTestId('mobile-detail-back'))
    expect(await screen.findByTestId('mobile-home')).toBeInTheDocument()
    expect(screen.queryByTestId('mobile-detail-bar')).toBeNull()
  })
})
```

### 5.5 红绿与判据

- **红**：S1–S9b 施加前，`npx vitest run src/app/shell -t "B369.6"` → 全红（`mobile-home`/底栏 testid 不存在）。这是 T1 的竖切红。
- **绿**：施加后同命令全绿，且 `npx vitest run src/app/shell` 全绿（既有桌面用例不破）。
- **变异验证（可红，供实现节点执行）**：把 `viewportOf` 的 `<= PHONE_MAX_WIDTH` 改成 `< 0`（即恒 desktop）→ 上面三支紧凑用例红，还原即绿。还原用 `git checkout -- web/src/app/shell/useShellViewport.ts`。

---

## 6. Task T2：卡 tab 状态词表与筛选

### 6.1 精确文件与改动

- **新**：`web/src/app/cards/statusVocab.ts`
- **新**：`web/src/app/cards/statusVocab.test.ts`
- **改**：`web/src/app/cards/CardsPage.tsx`（header 加状态筛选 chip 行）
- **改**：`web/src/app/cards/CardsPage.test.tsx`（筛选 + 词表一致用例）
- **测试范围**：`npx vitest run src/app/cards`
- 关键节点日志：切状态筛选时 `console.debug('cards.status_filter', { status })`（与 CardsPage 既有 `console.info`/`console.debug` 同族）。
- 注释：`statusVocab.ts` 文件头写职责+边界；测试头写「为什么读 Go 源码」。

### 6.2 新文件全文

**`web/src/app/cards/statusVocab.ts`**：

```ts
// statusVocab —— 账本状态词表的前端半边（「词表外状态串即 fail」的可判锚）。
//
// 职责：把 internal/ledger/types.go:15-19 的五个 Status 字面量固化成 TS 常量，
//       并提供「是否词表内」的判据；词表外取值原样透传（不白屏、不发明翻译）。
// 边界：
//   - 不碰 React、不发请求、不做 HTTP
//   - 词表逐值一致由 statusVocab.test.ts 读 Go 源码锁定（改 Go 词表必红）；
//     移动「卡」tab 的筛选 chip 直接迭代本词表，不另抄一份
export const CARD_STATUSES = ['待办', '进行中', '待审阅', '已完成', '终止'] as const

export type CardStatus = (typeof CARD_STATUSES)[number]

const KNOWN = new Set<string>(CARD_STATUSES)

// isKnownCardStatus 判断一个状态串是否在受控词表内。
// 注意：词表外**不是错误**——工作流可自定义状态；调用方按兜底渲染，不丢弃卡。
export function isKnownCardStatus(status: string): boolean {
  return KNOWN.has(status)
}
```

**`web/src/app/cards/statusVocab.test.ts`**：

```ts
// 词表金样本：TS 词表必须与 Go 真源逐值一致。
// 为什么读 Go 源码而不是手抄：手抄的词表会在 Go 侧加/改状态时静默漂移，
// 「两端各自有测试、中间没测」正是本卡要堵的形状。读源码让 Go 变则此测必红。
import { readFileSync } from 'node:fs'
import { dirname, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { describe, expect, it } from 'vitest'
import { CARD_STATUSES, isKnownCardStatus } from './statusVocab'

describe('账本状态词表（与 internal/ledger/types.go 逐值一致）', () => {
  it('TS 词表 = Go 的五个 Status 字面量，顺序也一致', () => {
    const here = dirname(fileURLToPath(import.meta.url))
    const go = readFileSync(resolve(here, '../../../../internal/ledger/types.go'), 'utf8')
    const got = [...go.matchAll(/Status(?:Todo|Doing|Review|Done|Closed)\s*=\s*"([^"]+)"/g)].map((m) => m[1])
    expect(got).toEqual(['待办', '进行中', '待审阅', '已完成', '终止'])
    expect([...CARD_STATUSES]).toEqual(got)
  })

  it('反例：词表外状态不被认作已知（绝不扩词表吞未知值）', () => {
    expect(isKnownCardStatus('自定义状态')).toBe(false)
    expect(isKnownCardStatus('completed')).toBe(false)
  })
})
```

### 6.3 CardsPage.tsx 改动

**C1｜import（`import { boardColumns, cardsInColumn, filterNeeds, mergeStateOrder, needsAttention, nodeLabelFor, normalizeBoardLayout, visibleColumns } from './columns'` 之后）**：

```tsx
import { CARD_STATUSES } from './statusVocab'
```

**C2｜筛选状态（在 `const [needsOnly, setNeedsOnly] = useState(false)` 之后）**：

```tsx
  // statusFilter：移动「卡」tab 的状态词表筛选（'' = 全部）。词表是受控的
  // CARD_STATUSES（与 Go 逐值一致），不提供自由输入——自由输入等于允许词表外串。
  const [statusFilter, setStatusFilter] = useState('')
```

**C3｜过滤（在 `const filtered = useMemo(...)` 的 `return filterNeeds(base, needsOnly)` 处）**：

把

```tsx
    return filterNeeds(base, needsOnly)
```

改为

```tsx
    const byStatus = statusFilter === '' ? base : base.filter((card) => card.status === statusFilter)
    return filterNeeds(byStatus, needsOnly)
```

同时把该 `useMemo` 的依赖数组由 `[cards, needsOnly, project, search, workflow]` 改为 `[cards, needsOnly, project, search, workflow, statusFilter]`。

**C4｜chip 行（插在 header 中「⚑ 需要你」按钮之后、`{showOpenInBrowser && (` 之前）**：

```tsx
        <span data-testid="card-status-filter" className="flex items-center gap-1">
          {CARD_STATUSES.map((status) => (
            <button
              key={status}
              type="button"
              data-testid={`card-status-${status}`}
              aria-pressed={statusFilter === status}
              onClick={() => {
                const next = statusFilter === status ? '' : status
                console.debug('cards.status_filter', { status: next })
                setStatusFilter(next)
              }}
              className={`rounded-full border px-2 py-0.5 text-[11px] ${statusFilter === status ? 'border-foreground bg-accent font-medium' : 'text-muted-foreground'}`}
            >
              {status}
            </button>
          ))}
        </span>
```

### 6.4 CardsPage.test.tsx 追加

```tsx
describe('B369.6 状态词表筛选（移动卡 tab）', () => {
  const cardView = (over: Partial<import('../../api/ledger').CardView>) => ({
    id: 'B1', title: '卡', status: '待办', priority: '中', project: 'handoff', workflow: 'feature',
    parent: '', base_branch: '', attachments: [], following: '', blocked: false, blocked_by: [],
    merged_count: 0, needs: '', open_decisions: 0, children_total: 0, children_done: 0,
    conflict: false, open_tickets: 0, ...over,
  })

  it('chip 行逐值渲染受控词表的五个状态', async () => {
    renderPage()
    for (const status of ['待办', '进行中', '待审阅', '已完成', '终止']) {
      expect(await screen.findByTestId(`card-status-${status}`)).toBeInTheDocument()
    }
  })

  it('点状态 chip 只留该状态；再点一次回全部', async () => {
    const ledger = await import('../../api/ledger')
    vi.mocked(ledger.fetchCards).mockResolvedValue({
      cards: [cardView({ id: 'B1', status: '已完成' }), cardView({ id: 'B2', status: '进行中' })],
      unlinked: { count: 0, tasks: [], unknown_targets: [] },
    } as never)
    renderPage()
    fireEvent.click(await screen.findByTestId('card-status-已完成'))
    expect(await screen.findByText('B1')).toBeInTheDocument()
    await waitFor(() => expect(screen.queryByText('B2')).toBeNull())
    fireEvent.click(screen.getByTestId('card-status-已完成'))
    expect(await screen.findByText('B2')).toBeInTheDocument()
  })
})
```

> 说明：`cardView` 里 `import('../../api/ledger').CardView` 的类型引用是为避免在测试文件顶部再增 import；`CardsPage.test.tsx` 已 mock `fetchCards`（文件顶部 `vi.mock('../../api/ledger', …)`），本用例覆写返回值即可。

### 6.5 红绿与判据

- 本 task 的测试**写下去即绿**（词表是纯映射、筛选是既有 filterNeeds 的加一维），按纪律不配独立红绿周期；其价值是把「词表漂移」变成可红：**变异验证**——把 `statusVocab.ts` 的 `'已完成'` 改成 `'完成'`，`statusVocab.test.ts` 第一支与 `CardsPage` chip 用例齐红，还原即绿。

---

## 7. Task T3：项目 tab 离线语义（测试锁定既有行为）

### 7.1 精确文件与改动

- **改**：`web/src/app/shell/Shell.test.tsx`（在 §5.4 的 `describe('B369.6 移动断点谱系')` 内追加一支）
- **不改产品代码**：离线位置只标状态、不降级只读的机制已在 `ProjectTree.tsx:984-991`（`problem !== ''` 时渲染 `DisconnectedBadge` 且不展开子目录、不渲染工作树）与 `:939-946`（不可达机器行不可拖）落地。本 task 只把它接到「移动项目 tab」的入口上锁。
- **测试范围**：`npx vitest run src/app/shell`
- 关键节点日志：无新增（复用 ProjectTree 既有打点）。

### 7.2 追加用例

```tsx
  it('移动「项目」tab：不可达位置标已断开且不渲染其目录内容（不降级只读）', async () => {
    Object.defineProperty(window, 'innerWidth', { value: 375, configurable: true, writable: true })
    // 本机位置有一个可展开的工作树（证明树可用），另加一台远端位置探测失败：
    // probe_error 非空即 CONTEXT「项目位置不可用」——只标状态、不渲染缓存内容。
    vi.mocked(fetchProjectTree).mockResolvedValue({
      ...tree,
      machines: [],
      projects: [{
        ...tree.projects[0],
        locations: [
          ...tree.projects[0].locations,
          {
            machine: 'devbox', name: 'handoff', path: '/srv/handoff',
            probe_error: 'dial tcp 10.0.0.8:7777: connect: connection refused',
            workspaces: [{ path: '/srv/handoff/wt', branch: '离线分支', head: 'abc1234', is_main: false, managed: true, created_at: '' }],
          },
        ],
      }],
    })
    renderShell()
    fireEvent.click(await screen.findByTestId('mobile-tab-projects'))
    // 离线机器行保持可见并标「已断开」——不静默少一台（CONTEXT「项目位置不可用」）。
    expect(await screen.findByText('已断开')).toBeInTheDocument()
    // 反例锁：离线位置的目录内容一格都不渲染（不降级只读、不摆缓存快照）。
    expect(screen.queryByText('离线分支')).toBeNull()
    // 位置不可用时该机器行不可展开（onClick 为 undefined）；本机行仍可展开。
    const offlineRow = screen.getAllByTestId('machine-row').find((row) => row.getAttribute('aria-disabled') === 'true')
    expect(offlineRow).toBeDefined()
    expect(offlineRow?.getAttribute('aria-expanded')).toBeNull()
  })
```

> 不断言机器行数量：数量是「有 N 台」的可数代理，与本卡要锁的「离线位置不降级」只有相关性；断言直接钉在行为上（离线行 aria-disabled 且不展开、本机行保持可展开）。**为什么不去点离线机器行**：`ProjectTree.tsx:940` 对 `problem !== ''` 的机器行 `onClick` 为 `undefined`——它不可展开，「点了没反应」正是本断言要表达的产品语义。本机行默认未展开故其工作树行不在 DOM，`queryByText('离线分支')` 因此是真反例而非假绿（那个串只出现在离线位置）。

### 7.3 红绿与判据

- **红**：T1 未落地时 `mobile-tab-projects` 不存在 → 红；T1 落地后本用例**写下去即绿**（`ProjectTree` 行为已在）。
- 若本用例意外红，说明 `fetchProjectTree` 的 fixture 形态与 `ProjectTreeResp` 不符——**停下提问**，不要改 `ProjectTree` 的离线语义（它属既有产品行为，不归本卡）。

---

## 8. Task T4：移动终端键条 + IME 组合输入

### 8.1 精确文件与改动

- **新**：`web/src/app/workbench/MobileKeybar.tsx`（**非 `terminal*` 前缀**，P3 约束）
- **新**：`web/src/app/workbench/MobileKeybar.test.tsx`
- **改**：`web/src/app/workbench/TerminalTab.tsx`（加 `keybar?: boolean` prop 与渲染）
- **改**：`web/src/app/workbench/TerminalTab.test.tsx`（键条喂回 `term.input` 用例）
- **改**：`web/src/app/workbench/terminalInput.test.ts`（追加 IME 组合生命周期用例）
- **改**：`web/src/app/shell/Shell.tsx`（`renderContent` 的 `case 'terminal'` 传 `keybar={compact}`）
- **测试范围**：`npx vitest run src/app/workbench`
- 关键节点日志：发送特殊键时 `console.debug('term.keybar.send', { seq })`（在 Shell/TerminalTab 的接线处；`seq` 用 `JSON.stringify` 避免控制字符污染日志可读性）。
- 注释：`MobileKeybar.tsx` 文件头写职责+边界（尤其「为什么不复用 terminalInput.ts」「为什么 onMouseDown preventDefault」）。

### 8.2 新文件全文

**`web/src/app/workbench/MobileKeybar.tsx`**：

```tsx
// MobileKeybar —— 移动端终端的特殊键条（spec 实现决定：特殊键条与 IME 组合）。
//
// 职责：渲染一排常用特殊键（Esc/Tab/Shift+Tab/Ctrl-C/方向键），点击把对应的
//       终端字节序列交给调用方；调用方（TerminalTab）经 term.input() 喂给 xterm，
//       与手敲输入合流到同一条 onData（因此取证日志、WS 未就绪告警全部照常适用）。
// 边界：
//   - 不认识 xterm、不直接写 WS、不管 IME：IME 组合走 xterm 自己的 CompositionHelper
//     （WKWebView/Chromium 通用路径），本组件只补「物理键盘没有的键」
//   - P3 约束：本文件**不得**用 terminal* 前缀（工作台已有 5 个 terminal* 源文件，
//     再造第 6 个会把「终端 I/O 修正」这一单一职责组撑成第二个家族）
//   - onMouseDown 必须 preventDefault：否则点键条会把焦点从 xterm textarea 抢走，
//     点一下丢一次焦点，用户得重新点终端才能继续打字
export interface MobileKeybarProps {
  onKey: (seq: string) => void
}

// KEYBAR_KEYS 是键条的受控键集。seq 是标准终端序列；id 只用于 testid 与 React key。
export const KEYBAR_KEYS: { id: string; label: string; aria: string; seq: string }[] = [
  { id: 'esc', label: 'Esc', aria: 'Esc', seq: '\x1b' },
  { id: 'tab', label: 'Tab', aria: 'Tab', seq: '\t' },
  { id: 'shift-tab', label: '⇧Tab', aria: 'Shift+Tab', seq: '\x1b[Z' },
  { id: 'ctrl-c', label: '^C', aria: 'Ctrl-C', seq: '\x03' },
  { id: 'up', label: '↑', aria: '方向上', seq: '\x1b[A' },
  { id: 'down', label: '↓', aria: '方向下', seq: '\x1b[B' },
  { id: 'left', label: '←', aria: '方向左', seq: '\x1b[D' },
  { id: 'right', label: '→', aria: '方向右', seq: '\x1b[C' },
]

export function MobileKeybar({ onKey }: MobileKeybarProps) {
  return (
    <div
      data-testid="mobile-keybar"
      aria-label="终端特殊键"
      className="flex shrink-0 gap-1 overflow-x-auto border-t bg-background px-1 py-1"
    >
      {KEYBAR_KEYS.map((key) => (
        <button
          key={key.id}
          type="button"
          data-testid={`keybar-${key.id}`}
          aria-label={key.aria}
          onMouseDown={(event) => event.preventDefault()}
          onClick={() => onKey(key.seq)}
          className="shrink-0 rounded border px-2 py-1 text-xs text-muted-foreground hover:bg-accent hover:text-foreground"
        >
          {key.label}
        </button>
      ))}
    </div>
  )
}
```

### 8.3 TerminalTab.tsx 改动

**K1｜import（在 `import { parseOsc52 } from './terminalOsc52'` 之后）**：

```tsx
import { MobileKeybar } from './MobileKeybar'
```

**K2｜props 接口（在 `onConnection?: (connected: boolean) => void` 之后）**：

```tsx
  // keybar 为真时在终端下方挂移动特殊键条（紧凑视口由 Shell 传 compact）。
  // 默认缺席 = 桌面形态，既有渲染逐字节不变。
  keybar?: boolean
```

**K3｜解构（在 `active = true, onConnection,` 之后）**：加 `keybar = false,`

**K4｜渲染（在 `<div ref={hostRef} data-testid="pty-host" … />` 之后、`{error !== null && (` 之前）**：

```tsx
      {keybar && (
        <MobileKeybar
          onKey={(seq) => {
            // 走 term.input 而不是直接 handle.send：与手敲输入合流，取证与尺寸逻辑
            // 不必知道键条存在（与 terminalInput.ts 同一条纪律）。
            console.debug('term.keybar.send', { seq: JSON.stringify(seq) })
            termRef.current?.input(seq)
          }}
        />
      )}
```

**K5｜Shell.tsx 的 `case 'terminal'`（现状返回的 `<TerminalTab … onConnection={…} />`）**，在 `active={active && !fullPageRoute}` 一行之后补：

```tsx
                        keybar={compact}
```

### 8.4 新增/追加测试全文

**`web/src/app/workbench/MobileKeybar.test.tsx`**：

```tsx
// 键条缝侧守卫：受控键集、序列载荷、点击上抛。
import { fireEvent, render, screen } from '@testing-library/react'
import { describe, expect, it, vi } from 'vitest'
import { KEYBAR_KEYS, MobileKeybar } from './MobileKeybar'

describe('MobileKeybar', () => {
  it('渲染受控键集（与 KEYBAR_KEYS 一一对应）', () => {
    render(<MobileKeybar onKey={vi.fn()} />)
    expect(screen.getByTestId('mobile-keybar')).toBeInTheDocument()
    for (const key of KEYBAR_KEYS) {
      expect(screen.getByTestId(`keybar-${key.id}`)).toBeInTheDocument()
    }
  })

  it('点击送入对应终端序列（逐键）', () => {
    const onKey = vi.fn()
    render(<MobileKeybar onKey={onKey} />)
    for (const key of KEYBAR_KEYS) {
      fireEvent.click(screen.getByTestId(`keybar-${key.id}`))
      expect(onKey).toHaveBeenLastCalledWith(key.seq)
    }
    expect(onKey).toHaveBeenCalledTimes(KEYBAR_KEYS.length)
  })

  it('mousedown 被 preventDefault（不抢 xterm 焦点）', () => {
    render(<MobileKeybar onKey={vi.fn()} />)
    const esc = screen.getByTestId('keybar-esc')
    const ev = new MouseEvent('mousedown', { bubbles: true, cancelable: true })
    esc.dispatchEvent(ev)
    expect(ev.defaultPrevented).toBe(true)
  })

  it('方向键序列是 CSI 标准形态', () => {
    expect(KEYBAR_KEYS.find((k) => k.id === 'up')?.seq).toBe('\x1b[A')
    expect(KEYBAR_KEYS.find((k) => k.id === 'left')?.seq).toBe('\x1b[D')
  })
})
```

**`TerminalTab.test.tsx` 追加**（在文件既有 describe 末尾）：

```tsx
  it('keybar 开启时渲染键条，点击把序列喂回 term.input（不直写 WS）', async () => {
    render(<TerminalTab base={WS} seq={1} spawn onSession={vi.fn()} keybar />)
    await waitFor(() => expect(createPtySession).toHaveBeenCalledTimes(1))
    fireEvent.click(screen.getByTestId('keybar-esc'))
    expect(termInstance.input).toHaveBeenCalledWith('\x1b')
    fireEvent.click(screen.getByTestId('keybar-up'))
    expect(termInstance.input).toHaveBeenCalledWith('\x1b[A')
  })

  it('keybar 缺席时不渲染键条（桌面形态逐字节不变）', async () => {
    render(<TerminalTab base={WS} seq={1} spawn onSession={vi.fn()} />)
    await waitFor(() => expect(createPtySession).toHaveBeenCalledTimes(1))
    expect(screen.queryByTestId('mobile-keybar')).toBeNull()
  })
```

**`terminalInput.test.ts` 追加**（在文件末尾；复用既有 `makeRig`/`Rig`）：

```tsx
// B369.6：移动 IME 组合输入路径（合成 IME event）。
//
// 为什么这条必须在这里：移动端终端输入层验收要求「IME 组合输入路径有单元测试」，
// 而组合落定在 xterm 5.5.0 里是**异步**的——CompositionHelper._finalizeComposition
// 用 setTimeout(…,0) 从 textarea.value 取串发 onData（探针实测：不 await 拿不到）。
// 不 await 的测试会假绿（断言空数组==空数组），所以下面的用例显式等一个宏任务。
function composeAndSettle(r: Rig, text: string): Promise<void> {
  r.ta.dispatchEvent(new CompositionEvent('compositionstart', { bubbles: true, composed: true }))
  r.ta.value = text
  const end = new CompositionEvent('compositionend', { bubbles: true, composed: true })
  r.ta.dispatchEvent(end)
  return new Promise((resolve) => setTimeout(resolve, 10))
}

describe('③ 移动 IME 组合输入（B369.6）', () => {
  it('组合落定后完整上屏一次，补漏不双发', async () => {
    rig = makeRig(true)
    rig.ta.focus()
    await composeAndSettle(rig, '你好')
    expect(rig.data).toEqual(['你好'])
  })

  it('不装补漏时同一序列也恰好一次（补漏不接管 composition 通道）', async () => {
    rig = makeRig(false)
    rig.ta.focus()
    await composeAndSettle(rig, '世界')
    expect(rig.data).toEqual(['世界'])
  })
})
```

### 8.5 红绿与判据

- **键条**：`MobileKeybar` 与 `TerminalTab keybar` 属新增行为，`keybar` prop 未加时 `keybar-esc` 不存在，`TerminalTab.test.tsx` 的键条用例**写下去必红**；加 K1–K5 后转绿。这是 T4 的竖切红。
- **IME**：测试**写下去即绿**（xterm 5.5.0 的 composition 通道本就工作，探针实测）。按纪律声明：它是**行为锁**而非新实现，锁住的是「补漏装/不装都不双发、恰好一次」这一既有能力；变异可红——把 `terminalInput.ts` 的 `onInputAfter` 改成无条件 `term.input(ie.data)`，第二支（不装补漏）仍绿但第一支会变红（双发），还原即绿。还原用 `git checkout -- web/src/app/workbench/terminalInput.ts`。

---

## 9. Task T5：收口复核

### 9.1 命令与预期（基线均已实测）

```bash
cd web && npx vitest run          # 预期 Test Files 全部 passed、Tests 全部 passed（基线 130/1414 + 本卡新增）
cd web && npx tsc -b              # 预期 EXIT=0
cd web && npx eslint .            # 预期 EXIT=0（T0 前为 1）
git diff --stat -- web/src/api    # 预期空输出（P2=A：零新增协议/DTO）
```

Go 与图闸（web-only 改动，预期不变；仍须复跑）：

```bash
go build ./...                                     # 预期 EXIT=0
go test ./cmd/ -run TestRepoContractGate -count=1  # 预期 ok
codegraph --repo . check --view cards-B369-charter   # 预期 fails=[]
codegraph --repo . validate --view cards-B369-charter # 预期 issues=null edgeIssues=null
codegraph --repo . check                              # 预期 fails=[]
```

### 9.2 异常处置

- web 三闸任一红：修到绿，**不许降低判据**（例如把 `npx eslint .` 改成只 lint 改动文件——那会让 T0 白做）。
- 图闸红：web 新增文件若引入跨域边（`graph check` 报违规），**停下提问**，不自行改 `codegraph/` 冻结物。
- `git diff --stat -- web/src/api` 非空：说明越出 P2=A 边界，**停下提问**。

### 9.3 提交

实现节点按 T0→T1→T2→T3→T4 分次提交（每次跑对应最小测试范围），T5 一笔收口提交（含台账）。

---

## 10. 整卡验收判据（行为化，逻辑型机内闭环）

| # | 判据 | 判定 |
|---|---|---|
| AC-1 | `cd web && npx vitest run` 退出 0，且新增用例存在并通过：断点分档、底栏四 tab、紧凑不渲染桌面栏、桌面不渲染底栏、切 tab 内容面、词表一致、离线位置不渲染内容、键条逐键序列、IME 恰好一次 | 全过＝pass |
| AC-2 | `cd web && npx tsc -b` 退出 0 | pass |
| AC-3 | `cd web && npx eslint .` 退出 0（基线为 1，T0 转绿） | pass |
| AC-4 | 词表**可红**：改 `statusVocab.ts` 任一状态字面量 → `statusVocab.test.ts` 红；还原绿 | pass |
| AC-5 | 底栏**可红**：把 `viewportOf` 恒 desktop → 紧凑用例红；还原绿 | pass |
| AC-6 | 键条**可红**：去掉 `keybar` prop 的渲染分支 → 键条用例红；还原绿 | pass |
| AC-7 | `git diff --stat -- web/src/api` 为空（P2=A：零新增协议/DTO） | pass |
| AC-8 | 既有 `Shell.test.tsx`/`TerminalTab.test.tsx`/`CardsPage.test.tsx`/`terminalInput.test.ts`/`ProjectTree.test.tsx` 全部保持绿（桌面行为零漂移） | pass |
| AC-9 | Go 侧 `go build ./...`、`go test ./cmd/ -run TestRepoContractGate`、`codegraph check/validate --view cards-B369-charter`、基线 `check` 保持基线读数 | pass |

**真机走查（不属机内闭环，由协调者执行，不派发）**：真实 iOS/Android webview 内对照十一屏原型逐屏（底栏、扫码配对入口、终端键条与 IME、外接键盘收起键条、安全区布局）——见 §13。

---

## 11. 接缝覆盖与内部锁声明

spec §「测试接缝（草单）」的三条缝中，落在 S6 的只有**缝 3：移动终端输入层组件边界（web 侧新建；调用方：任务现场终端面板）**。缝 1/2（配对载体、relay 回环反代）归 S1–S5，不在本卡。

**测试 → 缝**：

| 测试 | 入口调用符号 | 是否在缝 3 上 |
|---|---|---|
| `MobileKeybar.test.tsx`（4 支） | `MobileKeybar` / `KEYBAR_KEYS` | **是**（缝 3 的组件边界） |
| `terminalInput.test.ts#③ 移动 IME`（2 支） | `installTerminalInputFix` | **是**（缝 3 的输入修正接缝） |
| `TerminalTab.test.tsx` 键条 2 支 | `TerminalTab`（keybar prop） | 是（缝 3 的调用方 = 任务现场终端面板） |
| `useShellViewport.test.tsx`、`MobileTabBar.test.tsx`、`Shell.test.tsx` 紧凑用例 | `useShellViewport` / `MobileTabBar` / `Shell` | **否** |
| `statusVocab.test.ts`、`CardsPage.test.tsx` 筛选 | `statusVocab` / `CardsPage` | **否** |

**内部锁声明（对照「只能附加、不能顶替」）**：上表后两行的测试是**内部锁**（入口不在任何缝上），逐一给「从声明缝构造不出这条断言 + 为什么」：

1. **响应式断点/底栏/移动 IA**：spec 的三条声明缝里没有任何一条是「响应式壳」——它是 web 内容面的**根渲染分支**，没有可注入的接缝符号；从缝 3（终端输入）构造不出「375 宽是否渲染底栏四 tab」的断言。故只能以 `Shell` 组件为入口。
2. **账本状态词表**：词表是 `internal/ledger` 的 Go 常量，S6 只在 web 侧做只读镜像；缝 1（配对 wire）/缝 2（relay 反代）与状态词表无关，缝 3 与账本无关。从任一声明缝构造不出「TS 词表 == Go 词表」，只能读 Go 源码（本计划即如此）。
3. **项目 tab 离线语义**：既有 `ProjectTree` 行为，无本卡声明缝；断言入口只能是 `Shell` 的项目 tab。

**缝 → 测试**：缝 3 的**每一条**都由缝级断言锁住——键条的受控键集与逐键序列（`MobileKeybar.test.tsx`）、键条到 `term.input` 的接入（`TerminalTab.test.tsx`）、IME 组合路径恰好一次（`terminalInput.test.ts#③`）；无「锁不住的缝」，无提请删缝。

---

## 12. 缺陷族对抗（继承 breakdown §3.7 ③ 验收栏，逐族结论）

1. **生命周期/状态机中断**：移动内容面用绝对覆盖层而非条件卸载，`WorkbenchPage` 常驻——桌面既有的 xterm keep-alive（B280）不被破坏（`Shell.test.tsx` 断言 `workbench-group` 仍在 DOM）。`useShellViewport` 的 resize 监听在卸载时摘除（测试覆盖不到，代码内保证，见新文件实现）。终端 tab 关闭释放 xterm 是既有职责，本卡不改。真机切后台/回前台归 §13。
2. **静默失败/误导报错**：断点无网络面；账本未启用时移动首屏**显式**退到「项目」并给文案，不再渲染一句「不可用」的空态（S3 + S3 的会话兜底文案）。词表外状态**原样渲染**（`CardItem` 既有行为），不丢卡。键条发送走 `term.input` → `onData` → `handle.send`，WS 未就绪告警照常适用（与手敲同路）。
3. **跨平台假设**：`innerWidth` 与断点是浏览器共同事实；`env(safe-area-inset-bottom)` 在桌面求值 0；WKWebView vs Chromium 的 safe-area/IME 差异**未验证，需真机**（§13）。移动端键条只补物理键，不接管 IME——IME 走 xterm 通用 CompositionHelper（探针在 jsdom 验证，真机 iOS Safari 行为可能有出入）。
4. **假红/假绿**：断点测试显式 `defineProperty(innerWidth)` 而不依赖 jsdom 默认（jsdom 是 1024，会假绿成桌面档）；IME 测试**必须 await 宏任务**，否则断言空数组假绿（已在用例里显式 `setTimeout(…,10)` 并注明原因）；词表测试读 Go 源码而不是手抄（手抄必漂移）；`Shell.test.tsx` 加 `afterEach` 复位 innerWidth，防用例顺序污染（顺序耦合假绿）。变异验证逐条见各 task 的「可红」。
5. **门禁绕过**：web 无权限面；移动页面不直接连 agentd，只连核回环源（本卡不新增任何直连路径，`web/src/api/**` 零改动——AC-7）。反代不注入凭据是核侧承重属性（S2），本卡不碰。
6. **序列化边界**：本卡**新增零序列化字段**——四 tab 全是既有 DTO 的呈现复用；唯一的跨语言边界是**状态词表字面量**，由 `statusVocab.test.ts` 读 Go 源码逐值锁定（这就是本卡的「穿过真实边界的回归」）。`PairBundle` 的跨语言边界归 S1/S4，不在本卡。
7. **枚举新值过既有白名单**：本卡新增的枚举是 `MobileTab`（四个字面量）与 `CARD_STATUSES`（读自 Go）。`MobileTab` 的渲染 switch 用显式四分支（无 default 吞值——底栏遍历受控 `TABS` 数组，新增值必须同时进数组与类型）；`CARD_STATUSES` 与 Go 逐值一致由测试锁；词表外状态渲染兜底为原样透传（不白屏）。
8. **承重安全属性**：无新增安全属性。移动端不得引入「绕过回环门禁」的前端路径——本卡零新增 API 与网络入口，`web/src/api/**` 零改动即该属性的机械证明。
9. **webview 候选族**：**命中**——底栏、键条、IME、安全区布局在真实 iOS/Android webview 的表现未验证，归 §13。

---

## 13. 未验证，需真机（协调者执行，不派发）

1. **移动端真机走查**（拆解 §5 #4）：iOS/Android webview 内对照十一屏原型——底栏四 tab、会话/卡/项目/设置内容面、终端键条与 IME 组合、外接键盘收起键条、安全区（safe-area-inset）布局。
2. **IME 真机行为**：合成 composition 事件在 jsdom 下恰好一次，但 iOS Safari/WKWebView 与 Android WebView 的真实输入法行为（尤其 `insertFromComposition` 的 inputType 变体）可能有出入。
3. **配对清单入口的真实数据**：`SettingsPage` 的配对清单内容来自 `Core.MachineNames`（S2/S4 的 gomobile 绑定面 + 壳桥接），本卡只保证「设置 tab 复用既有 SettingsPage」；真实配对清单渲染**未验证，需真机**（依赖 S2/S4）。
4. **扫码配对入口**：bundle QR 由 CLI（S5）产出、壳侧扫码（P5=A 不进本仓），web 侧无扫码面；本卡不承诺扫码 UI。

---

## 14. 自审三查

1. **spec 覆盖（逐条能指到 task）**：
   - spec 实现决定「web/ 响应式按断点谱系：竖屏手机 / 折叠展开 / pad」→ T1（`useShellViewport` 三档 + Shell 条件渲染）；
   - spec 实现决定「移动端 IA：首屏=会话列表；底栏四个一级 tab：会话|卡|项目|设置」→ T1（`MobileTabBar` + `mobileTab` 路由四内容面）；
   - spec 实现决定「终端输入层含特殊键条与 IME 组合」→ T4（`MobileKeybar` + IME 单测）；
   - spec 用户故事 2/3（会话为家、进任务现场）→ T1 会话 tab 复用 `SessionSidebar`/`SessionTab`；
   - spec 用户故事 4（卡 tab 镜像账本、状态词表筛选）→ T2（`statusVocab` + CardsPage 筛选；P2=A 不新增写入面）；
   - spec 用户故事 5（项目 tab、离线不降级只读）→ T1 项目 tab + T3 离线断言；
   - spec 用户故事 9（设置页配对清单入口）→ T1 设置 tab 复用 `SettingsPage`（真实数据归 §13）；
   - contract §8.10「web/ 响应式断点谱系与移动终端输入层」→ T0–T4 全部；
   - 拆解 §3.7 ③ ⑧ 条验收（断点/会话/卡/项目/终端/联网面/三闸/真机）→ §10 AC-1..AC-9 + §13；
   - 拆解 §3.7 缺陷族 1–9 → §12 逐族结论。
2. **占位符扫描**：无 TBD /「加适当的错误处理」/「同 Task N」/ 描述性占位。新文件给全文；Shell 改动给到锚点与完整片段；测试给完整用例。**测试例外条款不适用**（测试代码已给全）。**内部锁声明**已在 §11 逐条给出（三条：响应式壳、词表、项目离线），无未声明内部锁；**退路同闸**：本计划不含会改变测试入口符号的条件退路（各 task 的「异常处置」只要求停下提问，不含改入口的退路）。
3. **跨 task 类型/签名一致性**：
   - `MobileTabBar` 的 `MobileTab` 三处（`MobileTabBar.tsx` 导出、Shell S2 `useState<MobileTab>`、S9 `setMobileTab`）同一类型；
   - `useShellViewport` 的 `ShellViewport`/`isCompactViewport`/`viewportOf`：T1 定义、T1/T5 消费，签名一致；
   - `MobileKeybar` 的 `KEYBAR_KEYS` 形态（`{id,label,aria,seq}`）在组件与测试中一致；
   - `TerminalTab` 的 `keybar?: boolean`：T4 定义、Shell K5 传 `compact`，类型一致；
   - `statusVocab` 的 `CARD_STATUSES`/`isKnownCardStatus`：T2 定义与消费一致；
   - 本卡**无跨卡签名交接**（S6 不向其他子卡导出符号），故无「A 卡 Produces vs B 卡 Consumes」对齐项。

---

## 15. 五项检查自审（实现类 plan，缺一即 plan failure）

1. **缺陷族对抗审查**：§12 逐族设问，九族全部有结论（含「无，因为……」）；结论直接对应 §10 的 AC-1..AC-9。
2. **序列化边界设问**：本卡新增数据字段 = 零（四 tab 全是既有 DTO 的呈现复用；`web/src/api/**` 零改动由 AC-7 机械锁）。唯一的跨语言边界是**状态词表字面量**：`statusVocab.ts`（TS）← `internal/ledger/types.go:15-19`（Go）。产生到消费之间没有手写 map/DTO 转换/CLI 拼接/跨语言另一侧的新投影；`statusVocab.test.ts` 直接读 Go 真源比对，用「字面量集合精确相等」区分「少一项/多一项/改名」。可空类型不适用（全是非空字符串常量）。roundtrip 属性测试不适用（无编解码）。→ 见 §12.6。
3. **上下文预算检查**：每个 task 圈得出有界文件集——T0：2 文件；T1：`shell/` 下 2 新增 + 2 改 + 2 新测试；T2：`cards/` 下 1 新增 + 1 新测试 + 1 改 + 1 改测试；T3：1 改测试；T4：`workbench/` 下 1 新增 + 1 新测试 + 2 改 + 1 改测试；T5：无改动。无 task 需要跨 >3 个目录，无需插竖切卡。
4. **类型标注**：本卡**无边界型子系统 task**（S6 是纯 web 内容面，接缝对面是浏览器/jsdom，不是外部现实）。故无真机行为验收清单式的「显式真机清单」；真机项集中在 §13 且标「协调者执行，不派发」。类型面已由 §14.3 逐项对齐。
5. **接缝覆盖（双向）**：见 §11——**测试 → 缝**：三支缝级测试（`MobileKeybar`、`TerminalTab keybar`、`terminalInput#③ IME`）入口符号都在 spec 缝 3 上；**缝 → 测试**：缝 3 的每条由缝级断言锁住，无提请删缝；**内部锁**：三条（响应式壳/词表/项目离线）已按唯一合法形状声明「从声明缝构造不出 + 为什么」，均不顶替缝级断言；**退路同闸**：无会改变测试入口符号的条件退路。

**判据先在基线跑声明**：§1 表中每条判据均在本节点实测（web 三闸、Go 闸/图闸、词表读源码、断点 hook、IME 时序、eslint 转绿），移动端代码判据「动手前在基线复核」已满足；xterm 5.5.0 的 composition 异步落定有库出处与最小验证程序（探针）。

---

## 16. 红线与不做

- **零 Go 改动**：`internal/**`、`cmd/**`、`mobile/**` 一行不改（S6 是纯 web 内容面）。
- **零新增协议/DTO**：`web/src/api/**` 一行不改（AC-7 机械判据）；卡「验收通过走 card move」复用既有 `moveCard`（CardDrawer 既有调用），不新增写入面（P2=A）。
- **不新开 `/m` 路由树**（P1=A）：底栏是同一份 Shell 的条件渲染。
- **不得再造 `terminal*` 源文件**（P3=A）：键条落到 `MobileKeybar.tsx`；本卡对 `terminalInput.ts`/`TerminalTab.tsx` 只做追加，不新建 `terminal*`。
- **不改 `codegraph/` 冻结物**；图闸若红，停下提问。
- **不重排桌面行为**：所有改动以「`compact === false` 时 DOM 与行为逐字节不变」为准绳（AC-8）。
- **不跑全量测试以外的越界命令**；临时文件一律用 `$TMPDIR`；变异后用 `git checkout -- <文件>` 还原，不留备份文件。
- **不把没跑到的结果写成结论**：真机项一律标「未验证，需真机」（§13）。
