# B156.3.2 plan 节点台账（边干边落）

约定：每确立一个事实追加一行；命令与原始输出随行。本文件与 b156.3.2-plan.md 同批提交。

- 2026-08-26 第零步自证通过：`git log --oneline -3` 首三行含 `95e08bdb`（plan(b156.3.7) 追加 Task C）；grep 岔口拍板记录=2、归属更正=1；当前分支 `cards/B156.3.2-charter`，工作树干净。
- 2026-08-26 输入齐读：spec（384 行，头部已批准）、contract（566 行，冻结+§13.5 归属更正）、breakdown（492 行，岔口拍板记录在 §38-91）。
- 2026-08-26 源码锚点核实：cardstep.go#startCardStep(45)、stepTransport(177)、resolveStepDiscipline(132)；scheduling.go Admit(176)/Enqueue(225)/PopReady(257)/resolve(332)/hasSlot(374)/acquire(393)/stepRunning(401,重试上限8)；server.go SetupAutomation(2236)/SetScheduling(2252)/facadeAsRegistry(2266)；proto/ledger.go NodeOverride.Squad(64-65)；ledger/types.go NodeOverride.Squad(176-180)；ledgerstep/dispatch.go ViaTemplate(115) Transport(46) DispatchOpts(27)；runner.go dispatchNodeWithGate(311，executor/model 成对规则 317-329)；ResolveNode(runner.go:281)。
- 2026-08-26 生产调用方核实：startCardStep 唯一生产入口 = ledgerapi.go:493（POST /api/cards/{id}/step）；`scheduling.Release` 全仓无生产调用方（仅定义 scheduling.go:216）——名额回收的生产接线无人认领，进 plan 移交注记与待拍板。
- 2026-08-26 测试夹具盘点：agentd 侧 fakeTargetMachine/registerFakeTarget/seedDisciplineOnLedger 在 cardstep_discipline_test.go:14-86；ledgerEnv/newNoPTYLedgerEnv 在 ledgerapi_test.go:141-175；seedAgentdLedger 工作流夹具在 ledger_fixtures_test.go:13；keystone 竖切 harness（registryViaFacade 含 ErrNotFound→schedclient.ErrNotFound 哨兵翻译）在 slice_test.go:81-114；runner 合并公式既有锁 TestRunnerExecutorModelOverridePriorityAndPairRule 在 runner_test.go:117。
- 2026-08-26 基线探针①：`go build ./...` exit 0。
- 2026-08-26 基线探针②：`go test ./internal/scheduling/ ./internal/agentd/ ./internal/keystone/ -count=1` → scheduling [no test files]、agentd ok(122s)、keystone ok(0.145s)。
- 2026-08-26 基线探针③：`go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v` → PASS(0.10s)，harness 存活。
- 2026-08-26 基线探针④：`grep -rn "scheduling|keysclient|schedclient" internal/ledgerstep/*.go` 零命中（exit=1）——K2 的零 import 反面判据在基线即满足。
- 2026-08-26 TOCTOU 基线实证（临时探针 toctou_probe_test.go，验证后已删除、不入提交）：16 路并发 Admit 打「两载体各上限2+小队上限10」（总容量4），基线 acquire 无上界执法 → `PROBE_RESULT successes=6`，超发 50%；并发判据在基线天然红，变异靶真实存在。
- 2026-08-26 探针首跑全败的教训（原始报错：`admit-N err: registry sched_running/squad/s1: ledger: 记录不存在`）：自搭 registry 适配器若不做 `ledger.ErrNotFound → schedclient.ErrNotFound` 哨兵翻译，冷启动计数路径整体失效——facadeAsRegistry（server.go:2274-2280）的哨兵翻译是承重件，新测试 harness 必须照抄该翻译，plan 已把它列为序列化边界判据之一。
- 2026-08-26 探针次生事实：N=16 高争用下出现 CAS 重试预算耗尽型失败（非语义失败）——拍板附加约束「翻红时先查重试预算再查语义」有了实证依据；测试设计须把预算耗尽与语义失败分流（新增 ErrRetryExhausted 哨兵承载该分流）。
- 2026-08-26 判据形状自查：全部判据点数不抽查（成功数==M、四条计数键逐一相等、事件序列全等、队列行字段逐项对照、grep 零命中计数）；无行号判据；并发判据带变异复验程序。
