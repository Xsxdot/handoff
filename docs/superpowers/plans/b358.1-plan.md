# B358.1（d_collab）会话语义收口与旧规则废止——实现计划

- **读者假设**：执行者对本对话零上下文、对代码库了解有限。本计划自包含：每个任务给精确文件路径、完整代码、判据命令与预期；不猜、不占位。
- **卡**：B358.1（B358 的 d_collab 子卡；L3 重档）。
- **基线**：分支 `cards/B233.1-charter-7`，contract 补签轮提交 `6a45231d`，Ticket 0 骨架在工作树（`internal/collab/sessions.go`、`internal/collab/room/delivery.go`、`internal/ledger/sessions.go` 等已落码并全绿）。
- **判据来源**：`docs/superpowers/specs/b358-breakdown.md` §3.2 ③ 逐条继承，**未降级**；契约 `docs/superpowers/specs/b358-contract.md` §3.5/§3.7/§4.1（条 6/7/13）/§4.4（条 34/35）/§8.2/§8.7；spec `docs/superpowers/specs/b358.md` §4.1/§4.2/§4.5、§6 接缝 #1；breakdown §2.4 澄清 1/2。
- **本节点台账**：`docs/superpowers/ledgers/2026-09-12-b358.1-plan-ledger.md`（边干边追加，随代码提交同批入库）。

---

## 0. 本卡干什么（一段话）

B358 把房间锚点从卡翻转到会话（群）：卡房间不再是工作单元，发言自由、身份执法。B358.1 把这件事在 `internal/collab` 落地——三件事：

1. **旧房间只读归档**：卡房间 / `project:<name>` / `global` 三种旧房间的发言一律拒绝（错误=「房间在、只是归档」）；**派发指针与协调者叙事两条上游靠卡房间的 `Service.Pointer` 可写活着，动不得**（`cmd/card_dispatch.go:434` 与 `internal/agentd/server.go:2951`，硬约束）。
2. **会话书写执法**：会话房间（`session:<n>`）的发言权只由「actor 非空 + actor ∈ 显式成员 ∪ 会话内各卡当前席位」决定（breakdown §2.4 澄清 1）；归档会话发言被拒（澄清 2，从「Join 被拒」补齐到「发言被拒」）。kind 白名单与按 kind 分权的书写者矩阵**废止**（发言自由，spec §4.2）。
3. **B287 延伸**：会话房间内 user 发言后，该房间 @本人 的未消费提及被消费（`EvMessageConsumed` 落账），收件箱 mention 源不再返回。

## 0.1 有界文件集（本卡只许碰这些生产/测试文件）

| 文件 | 角色 |
| --- | --- |
| `internal/collab/service.go` | Send 重构、sendToSession、sessionWriters、consumeRoomMentions |
| `internal/collab/room/room.go` | VerifyWriter 缩减、KindAllowed 删除、Room.ParentSession 删除 |
| `internal/collab/room/errors.go` | ErrKindNotAllowed 注释更新（哨兵本体与错误文本不动） |
| `internal/collab/service_test.go` | 发言执法测试族 |
| `internal/collab/sessions_test.go` | 归档/条 7/条 13/成员执法测试 |
| `internal/collab/readmodel_test.go` | 读模型夹具迁移 |
| `internal/collab/room/delivery_gate_test.go` | 追加 VerifyWriter AST 守卫 |

**禁触**：`internal/ledger/**`、`internal/proto/**`、`internal/agentd/**`、`cmd/**`、`web/**`（波及面处置见 §「已知红窗」与「明确不做」）。

## 0.2 现状依赖行为（必须按出处实现；行号为 2026-09-12 读数，以符号锚为准）

| # | 事实 | 出处 |
| --- | --- | --- |
| 1 | `Service.Send` 现路径：KindAllowed 白名单 → Resolve → 只读 → VerifyWriter 矩阵 → RecordRoomMessage；**不查会话本体**（归档会话发言今天成功） | `internal/collab/service.go#Service.Send`（:80-106） |
| 2 | `room.Resolve` 会话分支恒可写，注释明言「归档只读判定由门面在会话本体上做——本函数只解析形态，不查会话表」 | `internal/collab/room/room.go#Resolve`（:66-71） |
| 3 | `VerifyWriter` 按 kind 三分支矩阵；唯一生产调用点 service.go:91 | `internal/collab/room/room.go#VerifyWriter`（:113-162） |
| 4 | `KindAllowed` 七值白名单；唯一调用点 service.go:81 | `internal/collab/room/room.go#KindAllowed`（:165-172） |
| 5 | `Room.ParentSession` + Resolve 父卡查表：只服务 relay 矩阵，矩阵废后即死代码 | `internal/collab/room/room.go:39-48,93-97,128,138` |
| 6 | `consumeRoomMentions` 过滤 `ev.CardID != roomID`——会话房间事件 CardID 为空，B287 今天不覆盖会话 | `internal/collab/service.go#consumeRoomMentions`（:116-134） |
| 7 | `Store.RecordRoomMessage`：cardID 空串 = 群级无卡事件；会话房间经 `r.CardID==""` 天然落无卡事件（contract 条 33） | `internal/ledger/rooms.go#Store.RecordRoomMessage`（:14-30） |
| 8 | `mapSessionError` 把 `client.ErrNotFound` 翻 `ErrNoRoom` | `internal/collab/sessions.go#mapSessionError`（:373-381） |
| 9 | pointer 豁免两条上游：CLI 派发指针（swap 缝 `roomPointer`，生产直调 Pointer）与 roomNarrator 叙事（`n.c.Pointer`） | `cmd/card_dispatch.go:434`、`internal/agentd/server.go:2951` |
| 10 | 指针行断言纪律：roomNarrator 与派发指针两条上游并存，**存在式断言、不数行数** | `internal/agentd/server.go` roomNarrator 注释（:2936-2947） |
| 11 | 指针行 kind=pointer、BySystem=true、actor=`system:pointer`（包内常量）由 Pointer 自置 | `internal/collab/service.go#Service.Pointer`（:140-157） |
| 12 | `room.ReadAllEvents` 全流分页读（Pending/Mentions/Unread/History 共用） | `internal/collab/room/room.go#ReadAllEvents`（:248-262） |
| 13 | `Store.ArchiveSession` 无已归档短路（每次归档落一条事件）；`Store.JoinCardToSession` 归档会话拒并返回包装 `ledger.ErrBadState` | `internal/ledger/sessions.go:137-152、:158-190` |
| 14 | Facade 只翻 `ErrNotFound`，`ErrBadState` 原样穿透 | `internal/ledger/api/api.go:128-130,147` |
| 15 | CLI `room send`：`--kind user`+`--cli/--session` 在 flag 层拒绝，先于 `svc.Send` | `cmd/room.go:126-133,149` |
| 16 | `LedgerClient.GetSession/ListAllCards/RecordRoomMessage` 签名（Consumes 依据） | `internal/collab/client/client.go#LedgerClient`（:26-66） |

## 0.3 图覆盖债

本节点亲跑 `codegraph sym` 对 `collab.Service`、`collab.Service.Send`、`collab.Service.Pointer`、`room.Resolve`、`room.VerifyWriter`、`collab.consumeRoomMentions` 全部返回「不在图中」（原文见台账 §二）。与 breakdown §6 同欠：本卡全部触及面不在 `best.json`。因此本计划一律用 `file#Symbol` 源码锚；S1 新增/修改符号清单在 Task 4 落台账，交 integrate 图对账补进 `codegraph/diffs` 视图。

## 0.4 已知红窗（S1 语义落地即红、但改写责任不在本卡的外部测试）

S1 把「旧房间发言」从成功改为拒绝。以下测试以旧房间发言成功为断言或夹具，S1 合入后即红。**它们不在本卡有界文件集**；按 DAG（S1→S4、S1→S5），改写分别归 S4（agentd「对应 _test.go」）与 S5（`cmd/room_test.go`，其 ③ 已含「room send → 非 0 退出」）。处置裁决见文末「岔口」节；Task 0 清点、Task 4 复核「红集合 == 本清单，无新增」。

`internal/agentd/roomsapi_test.go`（10 支，行号为基线读数）：

1. `TestRoomsListEndpoint`（:81 夹具 Send）
2. `TestRoomsListUnreadAndAttachProjection`（:108,111,160 夹具 Send）
3. `TestRoomMessagesEndpoint`（:485,489 夹具 Send）
4. `TestRoomSendEndpoint`（:535 POST 卡房间期望 200 → 变 409）
5. `TestRoomReadEndpoint`（:567 夹具 Send）
6. `TestInboxThreeSources`（:640 project 房间夹具）
7. `TestInboxMentionSource`（:734）
8. `TestInboxRefIDShapes`（:768）
9. `TestInboxGoldenKeyShapes`（:804）
10. `TestInboxIntegrationSmoke`（:836）

`cmd/room_test.go`（6 支）：

11. `TestRoomSendLandsRoomMessageWithUserKind`
12. `TestRoomSendCoordinatorAcceptsExplicitSeatFlags`
13. `TestRoomSendCoordinatorKindUsesGrokHostSession`
14. `TestRoomSendCarriesRefAndMention`
15. `TestRoomReadStdoutSeqPrefix`（send 作夹具）
16. `TestRoomListSortedByActivity`（send 作夹具）

**确认存活**（S1 语义下断言仍真，不得误改）：`TestRoomSendErrMapping`（不存在房间 400 / 绑定者发言报错 / 终态 409 / 空正文 400 全部仍成立）、`TestRoomSendUserRejectsSeatFlagsWithoutSideEffects`（CLI flag 层先拒，出处 0.2#15）。

---

## 1. 已亲跑的基线与判据

以下命令在基线（`6a45231d` + Ticket 0 工作树）亲跑，结果记入台账 §三。执行者在 Task 0 复跑同组命令，读数不同先停下报告，不得带着意外继续：

| 命令 | 基线结果 |
| --- | --- |
| `go build ./...` | 退出 0 |
| `go test ./internal/collab/... -count=1` | ok（collab + room 两包） |
| `grep -rn "KindAllowed" internal/ cmd/` | 恰 3 命中（room.go 注释、room.go 函数、service.go:81）——全部在本卡文件集内 |
| `go test ./cmd -run TestRoom -count=1` | ok |
| `go test ./internal/agentd -run 'TestRoomMessages|TestRoomSend|TestRoomRead' -count=1` | ok |

判据先行的两条代码级确证（读码得出，出处 0.2#1/#13）：基线上「归档会话发言」**成功**（Send 无会话本体检查）、「旧卡房间 user 发言」**成功**——所以 Task 1 的红绿测试在基线必红，红是有意义的。

## 2. 终局形状（全部任务落地后的样子；任务往里填）

```go
// internal/collab/service.go —— Send 终局
func (s *Service) Send(roomID string, msg proto.RoomMessage, actor string) (int64, error) {
	if msg.Kind == proto.RoomMsgPointer {          // pointer 只能走 Pointer 入口（0.2#11）
		return 0, room.ErrKindNotAllowed
	}
	r, err := room.Resolve(s.lc, roomID)           // 存在性：不存在的卡仍 ErrNoRoom
	if err != nil {
		return 0, err
	}
	if !room.IsSessionRoom(roomID) {               // P5：旧房间只读归档
		return 0, room.ErrReadOnly
	}
	return s.sendToSession(r, msg, actor)          // 归档只读 + 成员执法 + 发布
}

// internal/collab/room/room.go —— VerifyWriter 终局
func VerifyWriter(writers []string, actor string) error // actor 非空 + 成员执法
```

执行顺序线性：`Task 0 → 1 → 2 → 3 → 4`（同缝串行——都动 service.go/room.go 的同一条写路径，并行无益且有冲突）。

**中间态声明（同分支内先后任务，不得中途合并/派发部分成果）**：

- Task 1 后、Task 3 前：会话房间对「任意非空 actor 的 user kind」可写（成员执法未接入）。
- Task 2 后、Task 3 前：会话房间对「任意非空 actor 的任意 kind」可写。
- 两窗口内 collab 包测试全绿（绿的是已迁移测试；成员执法断言在 Task 3 才落）。

---

## 3. Task 0：基线复跑与红窗清点（不写码）

### 文件范围

无代码改动；只写台账。

### Interfaces

无（纯核验任务）。

### 步骤

1. 复跑 §1 表格全部五条命令，读数与基线一致；不一致（依赖被人动过）→ 停下报告协调者，不自行修。
2. 复跑红窗基线：`go test ./cmd -run TestRoom -count=1` 与 `go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1`，确认 §0.4 的 16 支此刻全绿（红窗是 S1 制造的，不是继承的）。
3. 台账追加：基线读数原样 + 红窗清单确认行。

### Task 0 验收

- 五条命令读数与 §1 一致；两处红窗基线全绿；台账有原文。

---

## 4. Task 1：旧房间只读归档 + 会话发言骨架（归档只读接进 Send + B287 延伸）

### 文件范围

`internal/collab/service.go`、`internal/collab/service_test.go`、`internal/collab/sessions_test.go`、`internal/collab/readmodel_test.go`。

### Interfaces

Consumes（既有签名，逐字符保持）：

```go
// internal/collab/client/client.go#LedgerClient（:26-66）
GetSession(id string) (proto.Session, error)                      // 不存在 → Facade 翻 client.ErrNotFound
ListAllCards(project string) ([]proto.Card, error)                // 本任务不用；Task 3 用
RecordRoomMessage(cardID string, msg proto.RoomMessage, actor string) (int64, error)
// internal/collab/room/room.go
Resolve(lc client.LedgerClient, roomID string) (*Room, error)     // 不存在 → ErrNoRoom
IsSessionRoom(roomID string) bool                                 // session:<n> 前缀
SameRoom(ev proto.LedgerEvent, roomID string) bool                // 卡级看 card_id，群/会话级看载荷 Room
ReadAllEvents(lc client.LedgerClient, fromSeq int64) ([]proto.LedgerEvent, error)
VerifyWriter(r *Room, kind, actor string) error                   // 本任务仍是旧签名（Task 2 改）
// 哨兵：room.ErrNoRoom / ErrKindNotAllowed / ErrReadOnly / ErrNotWriter（errors.go，单点定义）
// internal/collab/sessions.go（同包既有私有助手）
mapSessionError(err error) error                                  // client.ErrNotFound → ErrNoRoom
```

Produces（本任务改变/新增的进程内符号）：

```go
// Service.Send 行为改变（签名不变）：旧房间 → ErrReadOnly；会话房间走 sendToSession
func (s *Service) sendToSession(r *room.Room, msg proto.RoomMessage, actor string) (int64, error) // 包内私有，Task 3 会改其中段
func (s *Service) consumeRoomMentions(roomID, member string)   // 过滤条件改 SameRoom（私有）
```

### 步骤（红绿只套在锁缝断言上；夹具迁移步骤不设独立红绿）

**步骤 1（判据先在基线跑）**：确认基线读数（§1）在台账——归档会话发言今天成功、旧卡房间发言今天成功（红的前提）。

**步骤 2（红）**：在 `internal/collab/service_test.go` 追加两支测试（完整代码）：

```go
// TestSendOldRoomsArchivedReadOnly 旧房间只读归档（breakdown 岔口 P5 方案 A；
// contract 欠账 #7 的实现=写路径拒绝，无数据迁移——spec 实现决定 5「内容不迁移」）。
// 三形态发言一律 ErrReadOnly：房间在、只是归档——不是 ErrNoRoom（缺陷族 2：
// 拒绝语义必须可行动）。同一卡房间 Service.Pointer 仍可写：派发指针
// （cmd/card_dispatch.go roomPointer）与协调者叙事（agentd roomNarrator）两条
// 上游靠它活着，pointer 豁免是硬约束。指针行断言用存在式不数行数——两条上游
// 并存，计数式会把对方变成偶发红（agentd roomNarrator 注释同款纪律）。
func TestSendOldRoomsArchivedReadOnly(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	for _, roomID := range []string{card.ID, "project:handoff", "global"} {
		if _, err := svc.Send(roomID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "旧房间不该写进去"}, "user:sy"); err != ErrReadOnly {
			t.Fatalf("旧房间 %s 发言必须 ErrReadOnly，got %v", roomID, err)
		}
	}
	// pointer 豁免：同一卡房间 Pointer 落账成功且事件可查（存在式）。
	seq, err := svc.Pointer(card.ID, proto.RoomMessage{Body: "派发指针仍在"})
	if err != nil {
		t.Fatalf("卡房间 Pointer 必须仍可写（pointer 豁免硬约束）: %v", err)
	}
	events, err := st.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	found := false
	for _, ev := range events {
		if ev.Seq == seq && ev.Type == "room_message" {
			found = true
		}
	}
	if !found {
		t.Fatalf("指针行未落账: seq=%d events=%+v", seq, events)
	}
}

// TestSessionArchiveSendReadOnly 归档只读接进发言路径（breakdown §2.4 澄清 ②：
// 契约 §3.7 的门面归档判定覆盖发言路径，不只 JoinCard）。生命周期序列反例：
// 归档前成员发言成功落账、归档后一律 ErrReadOnly——归档与发言的机内串行反例
// 覆盖状态机半边（账本单写者 Store.mutate 串行化；PG 真库并发归真机清单 #4，
// 缺陷族 1）。发送者用群主身份，Task 3 成员执法落地后不需改。
func TestSessionArchiveSendReadOnly(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "归档发言会话")
	if _, err := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "归档前可以说话"}, "user:sy"); err != nil {
		t.Fatalf("归档前群主发言应成功: %v", err)
	}
	if err := svc.ArchiveSession(sid, "user:sy"); err != nil {
		t.Fatalf("归档: %v", err)
	}
	if _, err := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "归档后还想说"}, "user:sy"); err != ErrReadOnly {
		t.Fatalf("归档会话发言必须 ErrReadOnly，got %v", err)
	}
}
```

**步骤 3**：在 `internal/collab/service_test.go` 追加会话夹具助手（两支红测试依赖它）：

```go
// mustSession 建一场群主为 user:sy 的会话，返回会话房间 id（P4 拍板的统一
// 记法：人=user:<name>。测试夹具从建会话起就用统一记法，成员执法与 mention
// 匹配都按逐字相等）。
func mustSession(t *testing.T, svc *Service, st *ledger.Store, title string) string {
	t.Helper()
	session, err := svc.CreateSession(title, "user:sy", "user:sy")
	if err != nil {
		t.Fatalf("建会话: %v", err)
	}
	return session.ID
}
```

**步骤 4**：跑红——`go test ./internal/collab -run 'TestSendOldRoomsArchivedReadOnly|TestSessionArchiveSendReadOnly' -count=1`，两支必红（基线行为相反）。红即判据成立，记台账。

**步骤 5（最小实现）**：改 `internal/collab/service.go`——`Service.Send` 与新增 `sendToSession`、`consumeRoomMentions` 过滤条件。整段替换（Task 1 终态；注意 VerifyWriter 仍用旧签名，Task 2 才改）：

```go
// Send 发消息：身份与形态执法的唯一写入口（pointer 除外——它只能走 Pointer，
// 经本方法一律拒收）。返回落账 seq。
//
// B358 执法路径（锚点翻转后，契约 §3.7 + breakdown §2.4 澄清 1/2）：pointer
// 拒收 → 房间解析（room.Resolve：存在性，不存在的卡仍 ErrNoRoom）→ 旧房间
// （卡 / project:<name> / global）只读归档，一律 ErrReadOnly（P5 拍板；错误
// 语义是「房间在、只是归档」，不是 ErrNoRoom）→ 会话房间：归档只读（门面查
// 会话本体）→ 书写者执法 → 经 client 发布。kind 不再设白名单——发言自由
// （spec §4.2 废止 kind 白名单，Task 2 落地）；identity 执法不变，账本仍能
// 回答「这句话谁说的」。
//
// 执法规则实现在 room 子包（执法内核），本方法只做编排；成员集由本门面组装
// 后送进 room.VerifyWriter（契约 §3.7：Resolve 只解析形态、不查会话表）。
func (s *Service) Send(roomID string, msg proto.RoomMessage, actor string) (int64, error) {
	if msg.Kind == proto.RoomMsgPointer {
		return 0, room.ErrKindNotAllowed
	}
	r, err := room.Resolve(s.lc, roomID)
	if err != nil {
		return 0, err
	}
	if !room.IsSessionRoom(roomID) {
		// B358 P5：旧房间只读归档。房间存在性已由 Resolve 判定（不存在的卡
		// → ErrNoRoom）；走到这里的都是「在但已归档」的房间。
		log().Info("旧房间已只读归档，发言拒收", "room", roomID, "kind", msg.Kind, "actor", actor)
		return 0, room.ErrReadOnly
	}
	return s.sendToSession(r, msg, actor)
}

// sendToSession 会话房间的发言路径：归档只读（澄清 ②——不只 JoinCard，发言
// 同样在门面查会话本体）→ 书写者执法 → 发布（无卡事件，contract 条 33）→
// B287 触发（kind=user 回复即清提及，Task 1 起延伸到会话房间）。
// 换绑剥权与成员执法在 Task 3 接入（本任务中段仍是旧 VerifyWriter 签名）。
func (s *Service) sendToSession(r *room.Room, msg proto.RoomMessage, actor string) (int64, error) {
	session, err := s.lc.GetSession(r.ID)
	if err != nil {
		return 0, mapSessionError(err)
	}
	if session.Archived {
		log().Warn("会话已归档，发言拒收", "room", r.ID, "actor", actor)
		return 0, room.ErrReadOnly
	}
	if err := room.VerifyWriter(r, msg.Kind, actor); err != nil {
		return 0, err
	}
	msg.Room = r.ID
	// r.CardID 对会话房间恒为空串（Resolve 会话分支不填卡）——RecordRoomMessage
	// 的空 cardID 即群级无卡事件（ledger/rooms.go 语义，contract 条 33）。
	seq, err := s.lc.RecordRoomMessage(r.CardID, msg, actor)
	if err != nil {
		return 0, err
	}
	if msg.Kind == proto.RoomMsgUser {
		// B287：用户发言即清该房间的提及类待回复。B358 起会话房间同权——
		// 会话是唯一活着的发言面，@ 在会话 mention 源只进不出会违背
		// 「@ 只是提醒」的裁决。best-effort：发送已成功，消费失败只告警
		// 不回滚（沿用既有取舍）。
		s.consumeRoomMentions(r.ID, actor)
	}
	log().Info("会话消息已落账", "room", r.ID, "kind", msg.Kind, "actor", actor, "seq", seq)
	return seq, nil
}
```

`consumeRoomMentions` 的过滤条件一行替换 + 注释更新（其余不动）：

```go
// consumeRoomMentions 用户消息落房后，把该房间内 @本人 且未消费的提及一并消费
// （B287 拍板「回复即清提及类」；B358 起卡房间与会话房间同权——卡房间已归档、
// 会话房间是唯一活着的发言面）。裁决与工单两源不受影响——它们要求显式审批/
// 答复，不因一句话消掉。
//
// 消费身份 = 发送 actor，与收件箱 mention 源（Service.Mentions）同标识，保证
// 「发前亮、回复后灭」同一把尺子。幂等由 Consume 兜底；失败逐条告警，下次
// 回复自然重试。
func (s *Service) consumeRoomMentions(roomID, member string) {
	events, err := room.ReadAllEvents(s.lc, 0)
	if err != nil {
		log().Warn("回复后清理提及失败：读事件流", "room", roomID, "member", member, "cause", err)
		return
	}
	consumed := room.ConsumedSeqs(events, member)
	for _, ev := range events {
		if ev.Type != room.RoomEventType || !room.SameRoom(ev, roomID) || consumed[ev.Seq] {
			continue
		}
		if !room.MentionsMember(ev, member) {
			continue
		}
		if err := s.Consume(ev.Seq, member); err != nil {
			log().Warn("回复后清理提及失败：消费落账", "room", roomID, "member", member, "seq", ev.Seq, "cause", err)
		}
	}
}
```

（原过滤 `ev.CardID != roomID` 换成 `!room.SameRoom(ev, roomID)`：卡房间行为逐字节不变——SameRoom 卡级就是 `ev.CardID == roomID`；会话房间按载荷 Room 匹配。）

**步骤 6（红绿③——B287 延伸的缝级断言）**：先跑
`go test ./internal/collab -run TestSendConsumesRoomMentions -count=1` ——旧版（卡房间 relay 夹具）此刻红。用下面的完整替换（先跑红再替换，红要记台账）：

```go
// TestSendConsumesRoomMentions B287 延伸到会话房间（breakdown §3.2 ③）：会话
// 房间内 user 发言后，该房间 @本人 的未消费提及被消费——EvMessageConsumed
// 存在式断言 + 收件箱 mention 源不再返回该条。发送方用会话内卡当前席位身份
// （Task 3 成员执法落地后本测试不需改）；@ 目标是群主。缝级断言，入口
// Service.Send / Service.Mentions（spec §6 接缝 #1）。
func TestSendConsumesRoomMentions(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	card := sessionCard(t, st, "提及载体卡")
	session, err := svc.CreateSession("提及会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session.ID, card.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := st.BindSeat(card.ID, "cli:opencode#seat-1", proto.SeatSourceCoordinate); err != nil {
		t.Fatalf("配人: %v", err)
	}
	// 席位成员 @群主。
	if _, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "看一下这个", Mentions: []string{"user:sy"}}, "cli:opencode#seat-1"); err != nil {
		t.Fatal(err)
	}
	pending, err := svc.Mentions("user:sy", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(pending) != 1 {
		t.Fatalf("发送前应有 1 条未消费提及，got %d", len(pending))
	}
	// 群主 user 发言（回复）→ 该房间 @本人 的提及被消费。
	if _, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "收到"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	pending, err = svc.Mentions("user:sy", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(pending) != 0 {
		t.Fatalf("回复后收件箱 mention 源应清空，got %d", len(pending))
	}
	// EvMessageConsumed 存在式断言：账本上确实落了消费标记（不是读侧巧合）。
	events, err := st.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	consumed := false
	for _, ev := range events {
		if ev.Type == ledger.EvMessageConsumed && ev.Actor == "user:sy" {
			consumed = true
		}
	}
	if !consumed {
		t.Fatal("回复后必须落 EvMessageConsumed（恰好一次语义在账本）")
	}
}
```

**步骤 7（夹具迁移，包绿工程——不设独立红绿，逐支替换后整包跑绿）**：以下测试的夹具房间/发送方式被 S1 语义打断，按表迁移。**每支给出完整替换体**；未列出的测试不动。

7a. `service_test.go`——迁移到会话夹具（4 支）：

```go
// TestSendUserMessageVerticalSlice 直通竖切（B358 锚点翻转后：会话房间是唯一
// 活着的发言面）：一次真实调用 Send(session, user) 穿过 Service →
// client.LedgerClient → internal/ledger/api.Facade → 真 SQLite ledger.Store，
// 落无卡事件后由 History 读回。载荷 roundtrip 断言保留（序列化边界既有金样本
// 语义不因迁移丢失）。
func TestSendUserMessageVerticalSlice(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "竖切会话")

	msg := proto.RoomMessage{Room: sid, Kind: proto.RoomMsgUser,
		Body: "先停一下，验收判据我想改", Refs: []string{"docs/x.md"}, Mentions: []string{"B156"}}
	seq, err := svc.Send(sid, msg, "user:sy")
	if err != nil {
		t.Fatalf("竖切发送失败: %v", err)
	}
	if seq <= 0 {
		t.Fatalf("seq 必须为正: %d", seq)
	}
	events, err := st.EventsFromAsc([]string{}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	var found *ledger.Event
	for i := range events {
		if events[i].Seq == seq {
			found = &events[i]
		}
	}
	if found == nil {
		t.Fatalf("事件流里没有该 seq: %d", seq)
	}
	if found.CardID != "" || found.Actor != "user:sy" {
		t.Fatalf("会话消息必须是无卡事件且 actor 落账: card=%q actor=%q", found.CardID, found.Actor)
	}
	var back proto.RoomMessage
	if err := json.Unmarshal(found.Payload, &back); err != nil {
		t.Fatal(err)
	}
	if back.Body != msg.Body || back.Kind != proto.RoomMsgUser || len(back.Refs) != 1 {
		t.Fatalf("载荷往返不一致: %+v", back)
	}
	history, err := svc.History(sid, 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(history) != 1 || history[0].Seq != seq {
		t.Fatalf("History 读回与发送不符: %+v", history)
	}
}

// TestSendGroupMessageLandsAsCardlessEvent 会话消息走无卡事件（contract 条 33；
// 原 project 房间夹具随旧房间归档迁移到会话房间）。
func TestSendGroupMessageLandsAsCardlessEvent(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "无卡事件会话")
	seq, err := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "全局通知"}, "user:sy")
	if err != nil {
		t.Fatalf("会话房间发送失败: %v", err)
	}
	events, err := st.EventsFromAsc([]string{}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range events {
		if ev.Seq == seq && ev.CardID != "" {
			t.Fatalf("会话级消息必须是无卡事件: %+v", ev)
		}
	}
}

// TestSendRejectsEmptyActor 空 actor 在会话房间被拒（原卡房间夹具随旧房间
// 归档迁移；空 actor 是书写者执法的第一道，Task 3 终形同样覆盖）。
func TestSendRejectsEmptyActor(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "空 actor 会话")
	if _, err := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, ""); err != ErrNotWriter {
		t.Fatalf("空 actor 必须返回 ErrNotWriter，got %v", err)
	}
}

// TestMentionsFiltersByMember 提及源按成员过滤（夹具迁会话房间——会话是
// 唯一活着的发言面）。
func TestMentionsFiltersByMember(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "提及过滤会话")
	if _, err := svc.Send(sid,
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "@B145 看", Mentions: []string{"B145"}}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Send(sid,
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "无提及"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	hit, err := svc.Mentions("B145", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(hit) != 1 {
		t.Fatalf("@提及过滤失准: %d 条", len(hit))
	}
	other, err := svc.Mentions("B999", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(other) != 0 {
		t.Fatalf("未提及成员不得命中: %d 条", len(other))
	}
}
```

7b. `service_test.go`——旧房间读面对质夹具（2 支，Send→Pointer；顺带锁「旧房间 History 读面不断」，breakdown ③ 第 3 条）：

```go
// TestHistoryFiltersNonRoomEvents 旧房间读面对质（B358：旧房间发言入口已死，
// 指针行是卡房间唯一合法的 room_message 生产者——用 Pointer 夹具，同时锁
// 「room list/History 读面对旧房间仍可读」）。History 必须滤掉非 room_message
// 事件（评论）。
func TestHistoryFiltersNonRoomEvents(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	if _, err := st.AddComment(card.ID, "普通评论", "普通", "tester"); err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Pointer(card.ID, proto.RoomMessage{Body: "房内指针行"}); err != nil {
		t.Fatalf("卡房间 Pointer 应可用: %v", err)
	}
	history, err := svc.History(card.ID, 0, 10)
	if err != nil {
		t.Fatal(err)
	}
	if len(history) != 1 {
		t.Fatalf("History 必须滤掉非 room_message 事件: %d 条", len(history))
	}
}

// TestHistoryReturnsNewestWindow 钉住「升序截尾」（B274 真机教训）：无 before
// 时取最新 limit 条。Pointer 夹具（旧房间读面对质）。
func TestHistoryReturnsNewestWindow(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	bodyOf := func(ev proto.LedgerEvent) string {
		var msg proto.RoomMessage
		if err := json.Unmarshal(ev.Payload, &msg); err != nil {
			t.Fatalf("解码 History 载荷: %v", err)
		}
		return msg.Body
	}
	for i := 1; i <= 5; i++ {
		if _, err := svc.Pointer(card.ID, proto.RoomMessage{Body: "m" + itoa(i)}); err != nil {
			t.Fatal(err)
		}
	}
	got, err := svc.History(card.ID, 0, 2)
	if err != nil {
		t.Fatal(err)
	}
	if len(got) != 2 {
		t.Fatalf("无 before 应截最新 2 条，got %d", len(got))
	}
	if bodyOf(got[0]) != "m4" || bodyOf(got[1]) != "m5" {
		t.Fatalf("应升序返回最新两条 m4,m5，got %q %q", bodyOf(got[0]), bodyOf(got[1]))
	}
	older, err := svc.History(card.ID, got[0].Seq, 2)
	if err != nil {
		t.Fatal(err)
	}
	if len(older) != 2 {
		t.Fatalf("before=最新窗首 seq 应再取 2 条，got %d", len(older))
	}
	if bodyOf(older[0]) != "m2" || bodyOf(older[1]) != "m3" {
		t.Fatalf("before 上界应取更早的 m2,m3，got %q %q", bodyOf(older[0]), bodyOf(older[1]))
	}
}
```

7c. `service_test.go`——**删除** 8 支矩阵锁死测试（每支锁的行为已随旧房间归档/矩阵废止死亡；替代断言落点逐支注明，删除记台账）：

| 删除 | 替代落点 |
| --- | --- |
| `TestSendRejectsUnknownKind` | Task 2 `TestSessionKindFreedom`（词表外 kind 放行） |
| `TestSendCoordinatorKindsRequireBinding` | Task 3 `TestSessionMembershipEnforcement`（身份执法取代 kind 分权） |
| `TestSendRelayAllowsDirectParentWriter` | 无需替代——relay 矩阵废止（spec §4.2）；ParentSession 死代码 Task 2 删 |
| `TestSendRelayRejectsGrandparentAndUnrelated` | 无需替代——relay 矩阵废止（spec §4.2）；ParentSession 死代码 Task 2 删 |
| `TestSendUserRejectsCardBinding` | 无需替代——user 反绑矩阵废止（spec §4.2）；席位成员以自己名义发言由 Task 3 `TestSessionMembershipEnforcement` 正向锁定 |
| `TestSendUserRejectsParentBinding` | 无需替代——父卡绑定矩阵废止（spec §4.2）；席位成员以自己名义发言由 Task 3 `TestSessionMembershipEnforcement` 正向锁定 |
| `TestSendRebindRevokesOldSession` | Task 3 `TestSessionRebindRevokesOldSeatWriter`（会话形态重立） |
| `TestSendUserGroupRoomRequiresNonEmpty` | Task 1 `TestSendOldRoomsArchivedReadOnly`（project/global 拒绝）+ `TestSendRejectsEmptyActor`（空 actor） |

7d. `readmodel_test.go`——文件头 import 增加 `ledgerapi "github.com/Xsxdot/handoff/internal/ledger/api"`（测试文件 import 不构成生产边，service_test.go 同款先例），然后迁移（8 支）：

```go
// TestPendingGroupMentionAndConsume 契约 §4「Pending 返回群级@」：@ 到
// consumer 的未消费群消息进 Pending；消费后消失。夹具迁会话房间（会话消息
// 同为无卡事件，Pending 的 CardID=="" 分支同样覆盖）。
func TestPendingGroupMentionAndConsume(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "群提及会话")
	consumer := "cli:codex#a"
	seq, err := svc.Send(sid,
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "@cli:codex#a 看", Mentions: []string{consumer}}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Send(sid,
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "无提及"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	pending, err := svc.Pending(consumer)
	if err != nil {
		t.Fatal(err)
	}
	if len(pending) != 1 || pending[0].Seq != seq {
		t.Fatalf("Pending 应只含 @ 到 consumer 的未消费群消息: %+v", pending)
	}
	if err := svc.Consume(seq, consumer); err != nil {
		t.Fatal(err)
	}
	after, err := svc.Pending(consumer)
	if err != nil {
		t.Fatal(err)
	}
	if len(after) != 0 {
		t.Fatalf("消费后 Pending 应清空: %+v", after)
	}
}

// TestPendingCardRoomUserMessages Pending 的绑定卡房间读侧过滤（B156.2 语义
// 残余：旧房间归档后不再有新留言，历史行仍应可查——分支不删）。卡房间发言
// 入口已死，历史行用出站接口直接落（测试文件 import，不构成生产边）。
func TestPendingCardRoomUserMessages(t *testing.T) {
	svc, st := newFixture(t)
	lc := ledgerapi.New(st)
	consumer := "cli:codex#a"
	bound := mustCard(t, svc, st, "绑定卡")
	other := mustCard(t, svc, st, "非绑定卡")
	mustBind(t, st, bound.ID, consumer)
	mustBind(t, st, other.ID, "cli:codex#b")

	msgSeq, err := lc.RecordRoomMessage(bound.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "给我留言"}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if _, err := lc.RecordRoomMessage(other.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "别人的卡留言"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if _, err := lc.RecordRoomMessage(bound.ID, proto.RoomMessage{Kind: proto.RoomMsgEscalation, Body: "简报"}, consumer); err != nil {
		t.Fatal(err)
	}
	pending, err := svc.Pending(consumer)
	if err != nil {
		t.Fatal(err)
	}
	if len(pending) != 1 || pending[0].Seq != msgSeq {
		t.Fatalf("Pending 应只含绑定卡房间的未消费 user 留言: %+v", pending)
	}
}

// TestConsumeIdempotentSameArgs 契约 §4「同参重试返回 nil 且不产生第二条」
// （夹具迁会话房间）。
func TestConsumeIdempotentSameArgs(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "幂等消费会话")
	seq, err := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "留言"}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	consumer := "cli:codex#a"
	if err := svc.Consume(seq, consumer); err != nil {
		t.Fatal(err)
	}
	if err := svc.Consume(seq, consumer); err != nil {
		t.Fatalf("同参重试应幂等 nil，got %v", err)
	}
	if n := countConsumedFor(t, st, consumer); n != 1 {
		t.Fatalf("同参重试不得产生第二条标记: %d", n)
	}
}

// TestConsumeSecondConsumerOwnMarker 各消费者一条自己的标记，互不顶替
// （夹具迁会话房间）。
func TestConsumeSecondConsumerOwnMarker(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "双消费者会话")
	seq, _ := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "留言"}, "user:sy")
	if err := svc.Consume(seq, "cli:codex#a"); err != nil {
		t.Fatal(err)
	}
	if err := svc.Consume(seq, "cli:codex#b"); err != nil {
		t.Fatalf("他人已消费的同一条对新消费者是首次写入，got %v", err)
	}
	if n := countConsumedFor(t, st, "cli:codex#a"); n != 1 {
		t.Fatalf("A 应恰一条: %d", n)
	}
	if n := countConsumedFor(t, st, "cli:codex#b"); n != 1 {
		t.Fatalf("B 应恰一条: %d", n)
	}
}

// TestConsumeInvalidSeq seq 不存在或非 room_message 一律幂等 nil 且不落标记
// （夹具迁会话房间；评论行仍挂卡上）。
func TestConsumeInvalidSeq(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "无效消费会话")
	card := mustAnyCard(t, svc, st)
	if _, err := st.AddComment(card.ID, "普通评论", "普通", "t"); err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "留言"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := svc.Consume(999999, "cli:codex#a"); err != nil {
		t.Fatalf("不存在的 seq 应幂等 nil，got %v", err)
	}
	events, err := st.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	var commentSeq int64
	for _, ev := range events {
		if ev.Type == ledger.EvComment {
			commentSeq = ev.Seq
		}
	}
	if err := svc.Consume(commentSeq, "cli:codex#a"); err != nil {
		t.Fatalf("非 room_message seq 应幂等 nil，got %v", err)
	}
	if n := countConsumedFor(t, st, "cli:codex#a"); n != 0 {
		t.Fatalf("无效 seq 不得落标记: %d", n)
	}
}

// TestConsumePayloadTwoKeys 消费标记 payload 恰 message_seq/consumer 两键
// （夹具迁会话房间）。
func TestConsumePayloadTwoKeys(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "消费载荷会话")
	seq, _ := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "留言"}, "user:sy")
	if err := svc.Consume(seq, "cli:codex#a"); err != nil {
		t.Fatal(err)
	}
	events, err := st.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range events {
		if ev.Type != ledger.EvMessageConsumed {
			continue
		}
		var m map[string]any
		if err := json.Unmarshal(ev.Payload, &m); err != nil {
			t.Fatal(err)
		}
		if len(m) != 2 || m["message_seq"] != float64(seq) || m["consumer"] != "cli:codex#a" {
			t.Fatalf("消费标记 payload 应恰 message_seq/consumer 两键: %v", m)
		}
		return
	}
	t.Fatal("没有消费标记事件")
}

// TestMentionsExcludesConsumed 未消费提及：消费后从 Mentions 消失（夹具迁
// 会话房间——会话 mention 源是 B358 的活语义）。
func TestMentionsExcludesConsumed(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "提及消费会话")
	seq, err := svc.Send(sid,
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "@B145 看", Mentions: []string{"B145"}}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	hit, err := svc.Mentions("B145", 0, 0)
	if err != nil || len(hit) != 1 {
		t.Fatalf("未消费提及应命中: %v %d", err, len(hit))
	}
	if err := svc.Consume(seq, "B145"); err != nil {
		t.Fatal(err)
	}
	after, err := svc.Mentions("B145", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(after) != 0 {
		t.Fatalf("消费后 Mentions 应清空: %+v", after)
	}
}

// TestListRoomsUnmergeRestoresAndKeepsHistory 拆回解冻 + 历史不丢（旧房间
// 读面对质：Pointer 夹具造消息；Send 断言翻成「旧房间恒 ErrReadOnly」）。
func TestListRoomsUnmergeRestoresAndKeepsHistory(t *testing.T) {
	svc, st := newFixture(t)
	carrier := mustCard(t, svc, st, "承载卡")
	member := mustCard(t, svc, st, "并入卡")
	seq, err := svc.Pointer(member.ID, proto.RoomMessage{Body: "并入前指针行"})
	if err != nil {
		t.Fatal(err)
	}
	if err := st.MergeCards([]string{member.ID}, carrier.ID, "test"); err != nil {
		t.Fatal(err)
	}
	rooms, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID := map[string]proto.RoomSummary{}
	for _, r := range rooms {
		byID[r.ID] = r
	}
	if !byID[member.ID].ReadOnly {
		t.Fatal("并入卡房间应只读")
	}
	if _, err := svc.Send(member.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy"); err != ErrReadOnly {
		t.Fatalf("旧房间发送应 ErrReadOnly（归档语义）: %v", err)
	}
	if err := st.UnmergeCard(member.ID, "test"); err != nil {
		t.Fatal(err)
	}
	rooms2, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID2 := map[string]proto.RoomSummary{}
	for _, r := range rooms2 {
		byID2[r.ID] = r
	}
	if byID2[member.ID].ReadOnly {
		t.Fatal("拆回后房间应解除只读")
	}
	history, err := svc.History(member.ID, 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(history) != 1 || history[0].Seq != seq {
		t.Fatalf("拆回后历史消息不丢: %+v", history)
	}
}
```

再迁 2 支未读水位（`readmodel_test.go` 尾部）：

```go
// TestMarkReadUnreadWatermark 未读水位（夹具迁会话房间；发送者=群主，
// Task 3 成员执法落地后不需改）。
func TestMarkReadUnreadWatermark(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "未读水位会话")
	seqs := []int64{}
	for _, body := range []string{"一", "二", "三"} {
		seq, err := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: body}, "user:sy")
		if err != nil {
			t.Fatal(err)
		}
		seqs = append(seqs, seq)
	}
	if n, err := svc.Unread("user:sy", sid); err != nil || n != 3 {
		t.Fatalf("未读应 3: %v %d", err, n)
	}
	if err := svc.MarkRead("user:sy", sid, seqs[1]); err != nil {
		t.Fatal(err)
	}
	if n, err := svc.Unread("user:sy", sid); err != nil || n != 1 {
		t.Fatalf("读到第二条后未读应 1: %v %d", err, n)
	}
	if err := svc.MarkRead("user:sy", sid, seqs[2]); err != nil {
		t.Fatal(err)
	}
	if n, err := svc.Unread("user:sy", sid); err != nil || n != 0 {
		t.Fatalf("读尽后未读应 0: %v %d", err, n)
	}
}

// TestMarkReadPerRoomAndMember 游标按成员按房间独立（夹具迁会话房间）。
func TestMarkReadPerRoomAndMember(t *testing.T) {
	svc, st := newFixture(t)
	sidA := mustSession(t, svc, st, "会话A")
	sidB := mustSession(t, svc, st, "会话B")
	seqA, _ := svc.Send(sidA, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "a1"}, "user:sy")
	if _, err := svc.Send(sidB, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "b1"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := svc.MarkRead("user:sy", sidA, seqA); err != nil {
		t.Fatal(err)
	}
	if n, _ := svc.Unread("user:sy", sidA); n != 0 {
		t.Fatalf("会话A 已读应 0: %d", n)
	}
	if n, _ := svc.Unread("user:sy", sidB); n != 1 {
		t.Fatalf("会话B 未读应 1: %d", n)
	}
	if n, _ := svc.Unread("cli:codex#a", sidA); n != 1 {
		t.Fatalf("另一成员游标独立，应 1: %d", n)
	}
}
```

**步骤 8（contract 条 7/13 断言，`sessions_test.go`）**：扩展 `TestSessionArchiveReadOnly`（加不落事件断言）并新增 3 支（条 6 单次半边 / 条 13 / 岔口记录载体）：

```go
// TestSessionArchiveReadOnly 归档后只读（contract §4.1 条 7）：归档会话不得
// 再拉卡进群，且拒绝路径不落事件（stream 上不得出现 session_card_joined）。
func TestSessionArchiveReadOnly(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	card := sessionCard(t, st, "归档会话卡")
	session, err := svc.CreateSession("归档会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.ArchiveSession(session.ID, "user:sy"); err != nil {
		t.Fatalf("归档: %v", err)
	}
	if err := svc.JoinCard(session.ID, card.ID, "user:sy"); err == nil {
		t.Fatal("归档会话不得再拉卡进群")
	}
	events, err := st.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range events {
		if ev.Type == ledger.EvSessionCardJoined {
			t.Fatalf("归档后 JoinCard 被拒不得落事件（条 7）: %+v", ev)
		}
	}
}

// TestArchiveSessionLandsExactlyOneEvent contract §4.1 条 6 的单次归档半边：
// 一次 ArchiveSession 落恰一条 EvSessionArchived。重复归档「不落第二条」的
// 读法在 Ticket 0 Store 实现下不成立（sessions.go:137 无已归档短路）——该
// 读法是否成立归协调者拍板（plan 岔口 #1），不在本卡有界文件集内修复。
func TestArchiveSessionLandsExactlyOneEvent(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	session, err := svc.CreateSession("恰一条归档事件", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.ArchiveSession(session.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	events, err := st.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	n := 0
	for _, ev := range events {
		if ev.Type == ledger.EvSessionArchived && payloadString(ev.Payload, "session") == session.ID {
			n++
		}
	}
	if n != 1 {
		t.Fatalf("一次归档应恰一条 EvSessionArchived，got %d", n)
	}
}

// TestSessionOutlivesCardTerminal contract §4.1 条 13：卡终态不导致会话归档
// （会话生命周期独立，spec §4.1；讨论面可能还在用）。
func TestSessionOutlivesCardTerminal(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	sid := mustSession(t, svc, st, "比卡活得久的会话")
	if err := svc.JoinCard(sid, card.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := st.CloseCard(card.ID, ledger.CloseCancelled, "test"); err != nil {
		t.Fatalf("置终态: %v", err)
	}
	detail, err := svc.SessionDetail(sid)
	if err != nil {
		t.Fatal(err)
	}
	if detail.Summary.Archived {
		t.Fatal("卡终态不得归档会话（条 13）")
	}
}
```

（`newFixture` 与 `newSessionFixture` 返回值不同：前者 `(svc, st)`、后者 `(svc, st, lc)`，按需选用；`sessionCard`/`payloadString` 是 sessions_test.go / sessions.go 既有符号。）

**步骤 9**：跑绿——`go test ./internal/collab/... -count=1` 必须 ok。有任何非预期红 → 按 charter:debug 建红回路查根因，**不许改判据迁就实现**。

**步骤 10（日志与注释自查）**：本任务新增/改动函数的日志已内嵌（旧房间拒收 Info、会话归档拒收 Warn、落账 Info、消费失败 Warn——带 room/actor/seq/cause 字段，结构化 logger，禁 print）；注释已更新（Send/sendToSession/consumeRoomMentions 职责+边界+为什么）。缺一补齐才算完成。

### Task 1 验收（判据逐条继承 breakdown §3.2 ③）

| 判据 | 命令/断言 |
| --- | --- |
| 旧卡房间 user 发言 → ErrReadOnly；同房 Pointer 成功落账（存在式） | `go test ./internal/collab -run TestSendOldRoomsArchivedReadOnly -count=1` 退 0 |
| 归档会话发言 → ErrReadOnly（归档前成功——序列反例） | `go test ./internal/collab -run TestSessionArchiveSendReadOnly -count=1` 退 0 |
| 会话房间 user 回复后提及被消费（EvMessageConsumed 存在）+ mention 源不再返回 | `go test ./internal/collab -run TestSendConsumesRoomMentions -count=1` 退 0 |
| room list/History 读面对旧房间仍可读 | `go test ./internal/collab -run 'TestListRooms|TestHistory' -count=1` 退 0（Pointer 夹具即对质锁） |
| 条 7 / 条 13 | `go test ./internal/collab -run 'TestSessionArchiveReadOnly|TestSessionOutlivesCardTerminal' -count=1` 退 0 |
| 包绿 | `go test ./internal/collab/... -count=1` 退 0；`go build ./...` 退 0 |

缺陷族映射：族 1（归档竞态）→ TestSessionArchiveSendReadOnly 串行序列反例 + PG 归真机 #4；族 2（误导报错）→ 拒绝哨兵逐字断言 ErrReadOnly/ErrNoRoom 分明（`TestSendRejectsUnknownRoom` 保持绿佐证不存在房间仍 ErrNoRoom）。

---

## 5. Task 2：kind 白名单与书写者矩阵废止

### 文件范围

`internal/collab/room/room.go`、`internal/collab/room/errors.go`、`internal/collab/room/delivery_gate_test.go`、`internal/collab/service.go`、`internal/collab/service_test.go`。

### Interfaces

Consumes（既有签名，逐字符保持；与 Task 1 相同的符号面）：`proto.RoomMsgPointer`、`room.ErrKindNotAllowed`/`ErrReadOnly`/`ErrNotWriter`/`ErrNoRoom` 哨兵、`room.Resolve(lc, roomID)`、`room.IsSessionRoom(roomID)`。

Produces（本任务后）：

```go
// internal/collab/room/room.go —— VerifyWriter 过渡形（Task 3 升终形）
func VerifyWriter(actor string) error
// 删除（符号级）：room.KindAllowed、room.Room.ParentSession
// 保留：ErrKindNotAllowed（唯一残余生产点 = Send 拒收 pointer）
```

### 步骤

**步骤 1（红）**：`internal/collab/room/delivery_gate_test.go` 追加 AST 守卫（该文件是 room 包源码级守卫的集中地，不新建文件——守住有界文件集）。守卫此刻必红（VerifyWriter 仍带 kind 形参并引用 RoomMsg*）：

```go
// TestVerifyWriterHasNoKindBranches 源码级守卫（B358 废止 kind 书写者矩阵；
// 先例 TestResolveDeliveryShapeHasNoMemberSet / agentd pointer_gate_test）：
// VerifyWriter 的形参不得再有 kind、函数体不得引用 proto.RoomMsg* 词表常量
// ——kind 分权矩阵的任何回潮形状（escalation 绑定者、relay 父卡、user 反绑）
// 从这里当场红。
//
// 为什么是读源码测试而不是缝级断言：被锁对象是「执法代码不含按 kind 分权的
// 分支」这一源码性质，从 Service.Send 的任何调用构造不出「没有分支」这条
// 断言（内部锁，理由声明见 plan §接缝覆盖——breakdown §3.2 ③ 的源码审查项
// 的机器化）。
func TestVerifyWriterHasNoKindBranches(t *testing.T) {
	fset := token.NewFileSet()
	f, err := parser.ParseFile(fset, "room.go", nil, 0)
	if err != nil {
		t.Fatalf("解析 room.go: %v", err)
	}
	var fn *ast.FuncDecl
	ast.Inspect(f, func(n ast.Node) bool {
		if d, ok := n.(*ast.FuncDecl); ok && d.Name.Name == "VerifyWriter" {
			fn = d
		}
		return true
	})
	if fn == nil {
		t.Fatalf("room.go 未找到 VerifyWriter——书写者执法入口不存在")
	}
	for _, p := range fn.Type.Params.List {
		for _, name := range p.Names {
			if strings.EqualFold(name.Name, "kind") {
				t.Errorf("VerifyWriter 形参 %q——kind 书写者矩阵已废止（B358 spec §4.2），按 kind 分权的形状不得回潮", name.Name)
			}
		}
	}
	ast.Inspect(fn, func(n ast.Node) bool {
		if sel, ok := n.(*ast.SelectorExpr); ok {
			if id, ok := sel.X.(*ast.Ident); ok && id.Name == "proto" && strings.HasPrefix(sel.Sel.Name, "RoomMsg") {
				t.Errorf("VerifyWriter 引用 proto.%s——按 kind 分权的矩阵回潮", sel.Sel.Name)
			}
		}
		return true
	})
}
```

（文件已 import `go/ast`、`go/parser`、`go/token`、`strings`——无新 import。）

**步骤 2（红）**：`service_test.go` 追加 kind 自由测试（此刻红：矩阵 default 分支对无卡房间拒协调者类/未知 kind）：

```go
// TestSessionKindFreedom kind 白名单废止的行为锚（spec §4.2「发言自由——身份
// 仍执法，不再管你说的是哪一类话」）：词表外 kind（heartbeat）由成员发出即
// 落账且 kind 原样保留（账本仍能回答「谁说的、说的哪类」）。发送者用群主
// 身份——Task 3 成员执法落地后本测试不需改（群主恒是成员）。
func TestSessionKindFreedom(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	session, err := svc.CreateSession("自由发言会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	seq, err := svc.Send(session.ID, proto.RoomMessage{Kind: "heartbeat", Body: "心跳"}, "user:sy")
	if err != nil {
		t.Fatalf("词表外 kind 应放行（发言自由，spec §4.2）: %v", err)
	}
	events, err := st.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range events {
		if ev.Seq != seq {
			continue
		}
		var back proto.RoomMessage
		if err := json.Unmarshal(ev.Payload, &back); err != nil {
			t.Fatal(err)
		}
		if back.Kind != "heartbeat" {
			t.Fatalf("kind 应原样落账: %q", back.Kind)
		}
		return
	}
	t.Fatalf("账本流上没有 seq=%d", seq)
}
```

（pointer 经 Send 拒收已有 `TestSendRejectsPointerViaSend` 锁着，保持不动，不在此重复。）

**步骤 3（最小实现）**：

3a. `room.go`——删除 `KindAllowed` 函数整体（:164-172）；`VerifyWriter` 换成过渡形（旧矩阵体整体删除）：

```go
// VerifyWriter 书写者执法（B358 过渡形：kind 书写者矩阵已废止，本函数只剩
// actor 非空；成员 ∪ 席位执法由门面组装书写者集后送入——终形签名带 writers，
// 见 Task 3）。失败统一返回 ErrNotWriter（含空 actor）。
func VerifyWriter(actor string) error {
	if strings.TrimSpace(actor) == "" {
		log().Warn("书写者被拒：actor 为空")
		return ErrNotWriter
	}
	return nil
}
```

（room.go 需新增 `strings` import。）

3b. `room.go`——`Room` 结构体删 `ParentSession` 字段（含结构体注释里 relay 一句）；`Resolve` 删父卡查表块（:93-97）与日志里 `parent_session` 键；`Resolve` 尾段终态：

```go
	c, ok := byID[roomID]
	if !ok {
		log().Warn("房间解析失败：卡不存在", "room", roomID)
		return nil, ErrNoRoom
	}
	r := &Room{
		ID:       roomID,
		CardID:   c.ID,
		Kind:     KindCard,
		Project:  c.Project,
		ReadOnly: IsTerminalStatus(c.Status) || c.Following != "",
		Card:     &c,
	}
	log().Info("房间解析完成", "room", roomID, "card", c.ID, "read_only", r.ReadOnly,
		"bound", c.DriverSession)
	return r, nil
```

同时 `Room` 结构体注释里「ParentSession 只在卡房间…」一句删除（relay 矩阵残余清干净）。

3c. `service.go`——Send 顶部 kind 检查收敛为 pointer 拒收（白名单废止）；`sendToSession` 调用点改过渡签名：

```go
	if msg.Kind == proto.RoomMsgPointer {
		return 0, room.ErrKindNotAllowed
	}
```

```go
	if err := room.VerifyWriter(actor); err != nil {
		return 0, err
	}
```

3d. `room/errors.go`——`ErrKindNotAllowed` 注释更新（哨兵变量与错误文本不动，HTTP 映射 roomsapi.go:35 不受扰）：

```go
	// ErrKindNotAllowed 400：pointer 走错门（B358 kind 白名单已废止——发言
	// 自由；本哨兵唯一残余生产点是 Send 拒收 pointer，pointer 只能走
	// Service.Pointer 专用入口）。
	ErrKindNotAllowed = errors.New("collab: 消息形态不在白名单")
```

**步骤 4（绿 + grep 验收）**：

- `go test ./internal/collab/... -count=1` → ok。
- `grep -rn "KindAllowed" internal/ cmd/` → **零命中**（breakdown ③ 字面判据；`ErrKindNotAllowed` 不含子串 `KindAllowed`，不受扰）。

**步骤 5（枚举残留面核对——缺陷族 7 的 S1 份额）**：跑
`grep -rn "RoomMsg" internal/collab --include="*.go" | grep -v _test`，生产代码应只剩四处，逐处理由落台账：

1. `service.go` Send 的 `proto.RoomMsgPointer` 拒收——pointer 专用入口守卫，非白名单；
2. `service.go` sendToSession 的 `proto.RoomMsgUser`——B287 触发条件（回复即清提及的行为语义），非唤醒白名单；
3. `service.go` Pointer 的 `proto.RoomMsgPointer` 置位——既有；
4. `room.go` `ResolveDelivery` 的 `proto.RoomMsgPointer`——寻址纯函数的系统行判别（Ticket 0 冻结语义，contract 条 26）。

`Pending` 的 `RoomMsgUser` 比较在 `service.go#Pending`——读侧过滤（绑定卡房间历史留言），同列理由。**wakeconsumer/card_wait 的 `RoomMsgUser` 残留判断属 S3 唤醒路径（不在本卡文件集），S3 的 grep 断言负责**（breakdown 缺陷族 7 原文分工）。`proto.RoomMsg*` 词表常量**不删**（历史落账行仍携带这些 kind，wire 兼容）。

### Task 2 验收

| 判据 | 命令/断言 |
| --- | --- |
| grep KindAllowed 零命中 | `grep -rn "KindAllowed" internal/ cmd/`（退出码 1 = 无命中） |
| VerifyWriter 无 kind 分权（源码审查项机器化） | `go test ./internal/collab/room -run TestVerifyWriterHasNoKindBranches -count=1` 退 0 |
| 词表外 kind 放行 | `go test ./internal/collab -run TestSessionKindFreedom -count=1` 退 0 |
| pointer 经 Send 仍拒 | `go test ./internal/collab -run TestSendRejectsPointerViaSend -count=1` 退 0 |
| 包绿 | `go test ./internal/collab/... -count=1` 退 0 |

缺陷族映射：族 4（假红/假绿）→ AST 守卫与 grep 锁「删除」而非锁实现；守卫先红后绿（步骤 1→3）；族 7（枚举过白名单回潮）→ 步骤 5 逐处理由 + S3 分工注明。

---

## 6. Task 3：会话书写执法（成员 ∪ 席位）+ 换绑剥权 + pointer 豁免反例

### 文件范围

`internal/collab/service.go`、`internal/collab/room/room.go`、`internal/collab/service_test.go`。

### Interfaces

Consumes：

```go
// internal/collab/client/client.go#LedgerClient
GetSession(id string) (proto.Session, error)          // session.Members / session.Archived
ListAllCards(project string) ([]proto.Card, error)    // 卡号 → DriverSession（席位权威投影）
// internal/collab/sessions.go
mapSessionError(err error) error
```

Produces（本任务后）：

```go
// internal/collab/room/room.go —— VerifyWriter 终形（Task 2 过渡形升格）
func VerifyWriter(writers []string, actor string) error
// internal/collab/service.go —— 包内私有
func (s *Service) sessionWriters(session proto.Session) ([]string, error) // 显式成员 ∪ 会话内卡当前席位
```

签名一致性：`sendToSession` 内 `room.VerifyWriter(writers, actor)`；Task 2 的 AST 守卫对终形仍绿（无 kind 形参、无 RoomMsg 引用）。

### 步骤

**步骤 1（红）**：`service_test.go` 追加（此刻红：执法未接入，非成员能写）：

```go
// TestSessionMembershipEnforcement 会话书写执法（breakdown §3.2 ③ + §2.4
// 澄清 1）：actor ∈ 显式成员 ∪ 会话内各卡当前席位才可发言，在 Service.Send
// 路径执法（HTTP handleRoomSend、CLI room send、未来订阅通道回复全部共享这
// 一道门——缺陷族 5）。全部断言穿 Service.Send + 真账本（缺陷族 4：不许只测
// room 包帮手）。两个承重反例：会话外卡的席位身份不是成员（席位权威在卡 ≠
// 成员权威在会话）；system:pointer 不得借道 Send 写会话（pointer 豁免只服务
// 卡房间的 Service.Pointer，不得扩大成「系统身份可写会话」——缺陷族 8）。
func TestSessionMembershipEnforcement(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	card := sessionCard(t, st, "成员卡")
	outsider := sessionCard(t, st, "会话外卡")
	session, err := svc.CreateSession("执法会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session.ID, card.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := st.BindSeat(card.ID, "cli:opencode#seat-1", proto.SeatSourceCoordinate); err != nil {
		t.Fatalf("配人: %v", err)
	}
	if err := st.BindSeat(outsider.ID, "cli:codex#outside", proto.SeatSourceCoordinate); err != nil {
		t.Fatalf("配外卡: %v", err)
	}

	// 显式成员（群主）发言 → 成功且 actor 落账可查。
	seq1, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "群主发言"}, "user:sy")
	if err != nil {
		t.Fatalf("显式成员发言应成功: %v", err)
	}
	assertActor(t, st, seq1, "user:sy")
	// 会话内卡的当前席位发言 → 成功。
	seq2, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "协调者发言"}, "cli:opencode#seat-1")
	if err != nil {
		t.Fatalf("席位成员发言应成功: %v", err)
	}
	assertActor(t, st, seq2, "cli:opencode#seat-1")

	// 非成员 → ErrNotWriter。
	if _, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "路人"}, "user:outsider"); err != ErrNotWriter {
		t.Fatalf("非成员发言必须 ErrNotWriter，got %v", err)
	}
	// 会话外卡的席位身份 → ErrNotWriter（它只在它自己那张卡的房间是合法书写者）。
	if _, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "外卡席位"}, "cli:codex#outside"); err != ErrNotWriter {
		t.Fatalf("会话外卡席位发言必须 ErrNotWriter，got %v", err)
	}
	// pointer 系统身份借道 Send → ErrNotWriter（pointer 豁免不扩大，缺陷族 8）。
	if _, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "系统"}, "system:pointer"); err != ErrNotWriter {
		t.Fatalf("system:pointer 借道 Send 必须 ErrNotWriter，got %v", err)
	}
	// 空 actor → ErrNotWriter。
	if _, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "匿名"}, ""); err != ErrNotWriter {
		t.Fatalf("空 actor 必须 ErrNotWriter，got %v", err)
	}
}

// assertActor 断言账本流上 seq 那条 room_message 的 actor 落账可查，且会话
// 消息是无卡事件（breakdown ③「成功且 actor 落账可查」的机械载体）。
func assertActor(t *testing.T, st *ledger.Store, seq int64, actor string) {
	t.Helper()
	events, err := st.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range events {
		if ev.Seq != seq {
			continue
		}
		if ev.Actor != actor {
			t.Fatalf("落账 actor 漂移: want %q got %q", actor, ev.Actor)
		}
		if ev.CardID != "" {
			t.Fatalf("会话消息必须是无卡事件: %+v", ev)
		}
		return
	}
	t.Fatalf("账本流上没有 seq=%d", seq)
}

// TestSessionRebindRevokesOldSeatWriter 换绑剥权（breakdown §3.2 ③；原
// TestSendRebindRevokesOldSession 锁的卡房间矩阵已随旧房间归档死亡，本测试
// 是它的会话形态重立）：同一会话内，换绑前旧席位发言成功落账、换绑后旧席位
// 被拒（且不出账）、新席位发言成功——前后两条消息都在账本。反例必须能变红
// （先红后绿，步骤 1 顺序保证）——否则成员执法只在夹具里成立（缺陷族 4）。
func TestSessionRebindRevokesOldSeatWriter(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	card := sessionCard(t, st, "换绑会话卡")
	session, err := svc.CreateSession("换绑会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session.ID, card.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := st.BindSeat(card.ID, "cli:opencode#old", proto.SeatSourceCoordinate); err != nil {
		t.Fatalf("配人: %v", err)
	}
	seqBefore, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "换绑前"}, "cli:opencode#old")
	if err != nil {
		t.Fatalf("换绑前旧席位发言应成功: %v", err)
	}
	if err := st.RebindSeat(card.ID, "cli:opencode#new", proto.SeatSourceCoordinate, "cli:opencode#old"); err != nil {
		t.Fatalf("换绑: %v", err)
	}
	if _, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "换绑后旧席位"}, "cli:opencode#old"); err != ErrNotWriter {
		t.Fatalf("换绑后旧席位必须 ErrNotWriter，got %v", err)
	}
	seqAfter, err := svc.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "换绑后新席位"}, "cli:opencode#new")
	if err != nil {
		t.Fatalf("换绑后新席位发言应成功: %v", err)
	}
	// 前后两条消息都在账本（同一会话，前后两条消息——breakdown ③ 原文）；
	// 被拒那条不出账。
	assertActor(t, st, seqBefore, "cli:opencode#old")
	assertActor(t, st, seqAfter, "cli:opencode#new")
	history, err := svc.History(session.ID, 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range history {
		var msg proto.RoomMessage
		if err := json.Unmarshal(ev.Payload, &msg); err != nil {
			t.Fatal(err)
		}
		if msg.Body == "换绑后旧席位" {
			t.Fatalf("被拒发言不得出账: %+v", ev)
		}
	}
}
```

**步骤 2（最小实现）**：

2a. `room.go`——VerifyWriter 升终形（Task 2 过渡形整体替换）：

```go
// VerifyWriter 会话书写者执法（B358：kind 书写者矩阵废止，本函数体只剩两件
// 事——actor 非空与成员执法；breakdown §3.2 ③ 源码审查项）。writers 是门面
// 按会话组装的书写者集（显式成员 ∪ 会话内各卡当前席位，见
// collab.Service.sessionWriters）——契约 §3.7：Resolve 只解析形态、不查会话
// 表，所以成员集由门面送进来。非成员一律 ErrNotWriter（含空 actor）。
func VerifyWriter(writers []string, actor string) error {
	if strings.TrimSpace(actor) == "" {
		log().Warn("书写者被拒：actor 为空")
		return ErrNotWriter
	}
	for _, w := range writers {
		if w == actor {
			return nil
		}
	}
	log().Warn("书写者被拒：非会话成员", "actor", actor, "writers", len(writers))
	return ErrNotWriter
}
```

2b. `service.go`——新增 `sessionWriters` 并把 `sendToSession` 中段换成终形：

```go
// sessionWriters 组装会话房间的书写者集：显式成员（人与主 agent 的外部会话
// 身份，P4 统一记法）∪ 会话内各卡的当前席位（席位权威在 cards.driver_session，
// 换绑后随卡走——breakdown 缺陷族 8 的「换绑剥权」由集合重建天然获得）。空座
// 卡不产生书写者。账本读失败向上传播（不伪装成 ErrNotWriter——一次读错不该
// 被说成「你不是成员」，缺陷族 2）。
func (s *Service) sessionWriters(session proto.Session) ([]string, error) {
	writers := make([]string, 0, len(session.Members)+len(session.Cards))
	writers = append(writers, session.Members...)
	if len(session.Cards) == 0 {
		return writers, nil
	}
	cards, err := s.lc.ListAllCards("")
	if err != nil {
		log().Warn("会话书写者集组装失败：读卡列表", "session", session.ID, "cause", err)
		return nil, err
	}
	byID := make(map[string]proto.Card, len(cards))
	for _, c := range cards {
		byID[c.ID] = c
	}
	for _, cardID := range session.Cards {
		if c, ok := byID[cardID]; ok && c.DriverSession != "" {
			writers = append(writers, c.DriverSession)
		}
	}
	return writers, nil
}
```

`sendToSession` 中段（替换 Task 1 版本的 VerifyWriter 调用）：

```go
	writers, err := s.sessionWriters(session)
	if err != nil {
		return 0, err
	}
	if err := room.VerifyWriter(writers, actor); err != nil {
		return 0, err
	}
```

**步骤 3（绿）**：`go test ./internal/collab/... -count=1` → ok。步骤 1 的三支在实现前必红（记台账原样输出）。

### Task 3 验收

| 判据（breakdown §3.2 ③ 原文判据） | 断言 |
| --- | --- |
| 未归档会话显式成员发言 → 成功且 actor 落账可查 | TestSessionMembershipEnforcement assertActor(user:sy) |
| 未归档会话席位成员发言 → 成功 | assertActor(cli:opencode#seat-1) |
| 非成员（含会话外卡的席位身份）→ ErrNotWriter | 两个独立反例 |
| 换绑后旧席位拒、新席位成、前后两条在账本、被拒不出账 | TestSessionRebindRevokesOldSeatWriter |
| pointer 豁免不扩大（system:pointer 借道 Send 被拒） | TestSessionMembershipEnforcement 反例 |
| 门禁单一：HTTP/CLI 同走 Send 一道门 | 源码事实（0.2#15 + roomsapi.go:336 直调 s.rooms.Send），记录于台账；行为面由 S4/S5 端点测试呈现 |

缺陷族映射：族 5（门禁绕过）→ 单一 Send 门 + pointer-actor 反例；CLI 自报席位身份的信任面与 B307 现状同基线（`ValidateSeat` 只验格式），真实防伪归真机/OOS（多人身份体系已 OOS 登记）；族 8（承重安全属性）→ 两个可变红反例。

---

## 7. Task 4：收口核验与移交

### 文件范围

无生产码改动；台账追加。

### 步骤

1. **breakdown §3.2 ③ 全量判据逐条复跑**（全部须满足）：

```bash
go build ./...                                        # 退出 0
go test ./internal/collab/... -count=1                # ok（③ 主判据）
grep -rn "KindAllowed" internal/ cmd/                 # 零命中（退出码 1）
go test ./internal/collab -run 'TestListRooms|TestHistory' -count=1   # ok（旧房间读面对质不断）
```

2. **红窗复核**：`go test ./cmd -run TestRoom -count=1` 与 `go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1` ——失败集合必须**恰好等于** §0.4 清单（agentd 10 支 / cmd 6 支），无新增。多出任何一支 = 本卡波及面清点漏项 → 停下报告协调者，不得顺手改（越有界文件集）。名单落台账。
3. **变异抽查（承重安全属性，缺陷族 8）**：
   - 临时把 `sessionWriters` 返回 `append(writers, actor)`（人人可写）→ `go test ./internal/collab -run 'TestSessionMembershipEnforcement|TestSessionRebindRevokesOldSeatWriter' -count=1` 必红 → 还原必绿。
   - 临时注释 `sendToSession` 的 `session.Archived` 拒收 → `TestSessionArchiveSendReadOnly` 必红 → 还原必绿。
   - 变异过程不提交，结果记台账。
4. **图覆盖债符号清单落台账**（交 integrate/recon 补 `codegraph/diffs`）：修改 `Service.Send`、`consumeRoomMentions`；新增 `sendToSession`、`sessionWriters`、`TestSendOldRoomsArchivedReadOnly`、`TestSessionArchiveSendReadOnly`、`TestSessionKindFreedom`、`TestSessionMembershipEnforcement`、`TestSessionRebindRevokesOldSeatWriter`、`assertActor`、`mustSession`、`TestSessionOutlivesCardTerminal`、`TestArchiveSessionLandsExactlyOneEvent`、`TestVerifyWriterHasNoKindBranches`；签名变更 `room.VerifyWriter`；删除 `room.KindAllowed`、`Room.ParentSession`。
5. 台账收口：§八自审结论回填。

### Task 4 验收

- 四条判据命令全部满足；红窗复核无新增；变异两发先红后绿；符号清单在台账。

**本计划无「由协调者执行、不派发」的 task**——Task 0–4 全部是执行者可机内完成的动作；真机清单（PG 并发 #4、跨进程换绑 #3、未读游标并发 #8）归协调者，不在任何 task 内冒充已验。

---

## 8. 明确不做的改动（越界即回退）

1. 不动 `internal/ledger/**`、`internal/proto/**` 生产文件——`Store.ArchiveSession` 无已归档短路等问题走岔口（文末 #1），不边拆边修。
2. 不动 `internal/agentd/**`、`cmd/**`——红窗测试改写归 S4/S5（§0.4）；`TestRoomSendErrMapping` 等存活测试不误改。
3. 不给 `ListRooms` 加旧房间只读标记——旧面 UI 退役归 S6；本卡只做写路径执法。
4. 不删 `proto.RoomMsg*` 词表常量——历史落账行仍携带，wire 兼容；`Pending` 的绑定卡房间分支语义不动（历史留言仍可读）。
5. 不碰 wakeconsumer/card_wait 的 `RoomMsgUser` 判断——S3 唤醒路径（breakdown 缺陷族 7 分工）。
6. 不动 `Service.Pointer` 行为——会话房间的 Pointer 无生产调用方，不在验收面；pointer 豁免（卡房间）是硬约束，任何「统一写入口」式重构都违约。
7. 不做旧房间数据迁移/归档标记——spec 实现决定 5「内容不迁移」；写路径拒绝即归档。
8. 不做真实多进程并发验证——真机清单 #8 归协调者。

## 9. 序列化边界与接缝覆盖

### 序列化边界设问

本卡**零新增数据字段**（RoomMessage/Session DTO 不改编码）。既有跨序列化断言保留：重写的 `TestSendUserMessageVerticalSlice` 保留 payload JSON roundtrip（`json.Unmarshal` 回读逐字段比对）；消费标记 payload 两键断言（`TestConsumePayloadTwoKeys`）随夹具迁移保留。「两端各自有测试 ≠ 链路有测试」的整链回归（Go→HTTP→TS）归 S4/S6（S1 无新 wire 形状，无双侧金样本义务——contract §7.3 的 Go 金样本已锁、本卡不改编码）。

### 接缝覆盖（spec §6 缝 #1：`internal/collab` 入站门面，调用方=控制面 HTTP 端点与 CLI room 命令族）

**缝 → 测试**（缝 #1 的每条断言主题 → 锁它的测试）：

| 缝 #1 断言主题（spec §6 #1） | 锁 |
| --- | --- |
| 锚点解析（会话房间存在性/不存在 → ErrNoRoom） | `TestSessionArchiveSendReadOnly`（存在）、`TestSendRejectsUnknownRoom`（不存在，保持绿） |
| 群↔卡唯一性 | 账本层已锁（`uq_session_cards_card` + `Store.JoinCardToSession` 事务判定，contract 条 9-10）；facade 级呈现归 S4 端点测试——不在 S1 ③ 判据，S1 不重复造 |
| 成员派生 | `TestSessionMembershipEnforcement`（显式 ∪ 席位）+ 既有 `TestSessionExplicitMembers` |
| 归档只读 | `TestSessionArchiveSendReadOnly`（发言）+ `TestSessionArchiveReadOnly`（Join，条 7 扩展） |
| 身份执法（非成员不能发言） | `TestSessionMembershipEnforcement` + `TestSessionRebindRevokesOldSeatWriter` |

**测试 → 缝**（S1 新增/重写测试的入口符号，全部在缝 #1 上或穿它）：

| 测试 | 入口 |
| --- | --- |
| TestSendOldRoomsArchivedReadOnly | `Service.Send` + `Service.Pointer` |
| TestSessionArchiveSendReadOnly | `Service.Send` / `Service.ArchiveSession` |
| TestSendConsumesRoomMentions | `Service.Send` / `Service.Mentions` |
| TestSessionKindFreedom | `Service.Send` |
| TestSessionMembershipEnforcement | `Service.Send` |
| TestSessionRebindRevokesOldSeatWriter | `Service.Send` / `Service.History` |
| TestSessionOutlivesCardTerminal | `Service.SessionDetail` / `Service.JoinCard` |
| TestArchiveSessionLandsExactlyOneEvent | `Service.ArchiveSession` |
| 全部夹具迁移测试 | `Service.Send/History/Pending/Mentions/Consume/Unread/MarkRead/ListRooms`（皆门面方法） |

**内部锁声明（默认非法，逐条给构造不出理由）**：

1. `TestVerifyWriterHasNoKindBranches`（delivery_gate_test.go）——被锁对象是「VerifyWriter 函数体不含 kind 分权分支」这一源码性质；从 `Service.Send` 的任何调用构造不出「没有分支」这条断言。breakdown §3.2 ③ 原文即「源码审查项」，机器化是加强而非降级。
2. `TestRoomEventTypeLiteralMatchesLedger` / `TestRoomStatusLiteralMatchesLedger`（既有，不动）——门面禁令下两侧字面量等式只有测试文件能同时看见（既有声明的内部锁）。
3. `delivery_gate_test.go` 既有两守卫（ResolveDelivery 形状 / 扇出禁令扫描）——既有，理由已声明，本卡不新增同类。

**退路同闸**：本计划步骤正文无「若意外先绿就改入口」类条件退路；执行中若想引入，必须先按本节格式声明理由，否则砍掉。

## 10. 缺陷族对抗（九族 → 验收栏映射总表）

| 族 | 设问 | S1 答案与落点 |
| --- | --- | --- |
| 1 生命周期/状态机中断 | 归档与发言并发？ | 账本单写者 `Store.mutate` 串行化；机内串行序列反例 = `TestSessionArchiveSendReadOnly`；PG 真库并发**未验证，需真机 #4** |
| 2 静默失败/误导报错 | 拒绝语义可行动？ | 旧房间在 → `ErrReadOnly`（不是 ErrNoRoom）；不存在 → `ErrNoRoom`；归档 → `ErrReadOnly`；`sessionWriters` 读错误向上传播不伪装 403。无「报成功但没做」窗口——写路径单事务 `RecordRoomMessage` |
| 3 跨平台假设 | 有无？ | 无——纯账本语义，不触路径/进程组/webview |
| 4 假红/假绿 | 测试能变红吗？ | 全部穿 `Service.Send` + 真 SQLite（`newFixture`/`newSessionFixture`）；红绿顺序由任务步骤保证（先红后实现）；删除类断言用 grep + AST 守卫锁；换绑反例先红后绿 |
| 5 门禁绕过 | 入口全覆盖？ | 唯一发言门 = `Service.Send`（HTTP `handleRoomSend` roomsapi.go:336、CLI `cmd/room.go:149` 两处生产调用点共享）；pointer 专用入口不被 Send 冒名（kind 拒收）+ system:pointer actor 反例；CLI 自报身份信任面 = B307 现状基线，真实防伪**未验证，需真机**且多人身份体系已 OOS |
| 6 序列化边界 | 新字段？ | 无；既有 roundtrip/两键断言随夹具迁移保留（§9） |
| 7 枚举过白名单 | 第二白名单？ | 白名单本体删除（grep 零命中）；collab 生产代码 RoomMsg* 残留逐处理由（Task 2 步骤 5）；wakeconsumer/card_wait 残留归 S3（其 grep 断言负责）；`RoomMsgUser` 在本卡路径只作 B287 触发与 Pending 读过滤，非唤醒条件 |
| 8 承重安全属性 | 可变红吗？ | 「非成员不能写」= 成员执法反例 + 换绑剥权反例；「pointer 豁免不扩大」= system:pointer 反例；Task 4 变异抽查两发 |
| 9 webview 候选族 | 触浏览器面？ | 无——本卡不触 d_web |

## 11. 岔口（协调者拍板项，不阻塞 Task 0–2；Task 3/4 与岔口 #1 相关步骤已按现行读法写）

1. **contract 条 6 重复归档读法**：Ticket 0 `Store.ArchiveSession` 无已归档短路——重复归档每次落事件，与「幂等；落恰一条 EvSessionArchived」的强读法冲突。选项：a) 采纳弱读法（本计划现行——`TestArchiveSessionLandsExactlyOneEvent` 只锁单次恰一条）；b) 批准把 `Service.ArchiveSession` 已归档短路（sessions.go，+1 函数 +1 断言）扩进 S1 有界文件集。建议 b（timeline 不出重复 archived 行）。
2. **红窗处置**（§0.4，agentd 10 + cmd 6）：a) 接受窗口、S4/S5 收口（本计划内建清点与复核）——建议；b) 把旧面测试改写划给 S1（扩有界文件集，与 S4/S5 端点语义改造冲突）。
3. **（信息项，归 S4）**：契约 §9 的 `ErrBadState` 映射在 collab 侧无哨兵载体（`client.LedgerClient` 只冻结 `ErrNotFound`；`ledger.ErrBadState` 穿透，0.2#13/#14）——S4 做 HTTP 状态映射时需拍板载体；若需 client 加哨兵属契约面变更，先回 contract。

## 12. 五项计划自审与自审三查

**五项检查**：

1. 缺陷族对抗——§10 逐族有答案并映射验收栏。✓
2. 序列化边界——零新字段，既有断言保留，声明于 §9。✓
3. 上下文预算——每任务文件集见各「文件范围」，全部落在 breakdown §3.2 ④ 有界文件集内（AST 守卫追加进已列名的 delivery_gate_test.go，不新增文件）。✓
4. 类型标注——S1 是逻辑型子系统（breakdown §1 判 d_collab 逻辑型），验收为机内命令判据；真机项已显式移交（§10 族 1/5）。✓
5. 接缝覆盖双向——§9 两张表 + 三条内部锁逐条声明。✓

**自审三查**：

- **spec 覆盖**：breakdown §3.2 ① 契约引用逐条落 task——§3.5（Send/成员执法 T1/T3）、§3.7（形态/归档门面执法 T1、矩阵废止 T2）、§4.1 条 6/7/13（T1）、§4.4 条 34/35（T1/T2）、§8.2（T1）、§8.7（T2）、澄清 1/2（T1/T3）、P4（夹具统一记法 + 逐字相等执法）、P5（T1）。③ 判据逐条见台账 §六对照表。✓
- **占位符扫描**：全篇无 TBD/「适当处理」/「同 Task N」；所有新测试给完整代码；所有实现给完整代码或逐行替换 delta；未使用「复用夹具豁免」（各测试代码完整给出，无需豁免声明）。✓
- **跨 task 签名一致**：`VerifyWriter` 三形态（旧 `(r,kind,actor)` → T2 `(actor)` → T3 `(writers,actor)`）在各任务步骤中显式给出且调用点同步替换；`sessionWriters` 返回 `([]string, error)` 与 `sendToSession` 调用一致；AST 守卫对三形态的判据（无 kind 形参、无 RoomMsg 引用）逐形核对成立。✓

## 13. 收口判据（B358.1 完成的唯一判定）

Task 4 的四条命令全绿 + 红窗复核无新增 + 变异两发先红后绿 + 台账收口完整。在此之前，任何「应该差不多了」不构成完成；在此之后，本卡交回协调者（附件挂卡与移列归协调者，执行者不做 git 卡操作以外的合并/推远端决策）。

---

## 实现者纪律（派发注入段）

1. **TDD 铁律只套锁缝断言**：标「红绿」的步骤先写测试跑红（原样输出记台账）再最小实现跑绿；夹具迁移、注释、日志步骤不设独立红绿周期。
2. **测试三段律**：每任务只跑触及包（`go test ./internal/collab/...` + 判据单测）；`go build ./...` 每任务收尾跑；全量测试不属于任何单个任务，归 integrate。
3. **修复熔断**：撞到非预期红，先按 charter:debug 建红反馈回路查根因；禁止改判据迁就实现、禁止跳过失败。
4. **中间态不外泄**：Task 1→3 之间的可写窗口是同分支先后任务；不得中途合并、不得按部分成果通知协调者验收。
5. **红线**：不碰 §0.1 禁触文件；不动 pointer 豁免两条上游（0.2#9）；不写实现以外的「顺手活」；发现计划与冻结物矛盾 → 停下提问（远程执行发工单问协调者，本地问用户），不自造替代。
6. **台账边干边落**：`docs/superpowers/ledgers/2026-09-12-b358.1-plan-ledger.md` 每确立一个事实追加一行（命令+原始输出、放弃的尝试、判断）；提交事实记「提交当时命令与输出」，随后至多 amend 一次收进同批提交，不 chase hash。
7. **不派发**：执行者不调 handoff 派发 CLI、不做卡操作（挂附件/移列归协调者）；Task 0–4 无一需要驱动派发系统自身。

---

## 协调者拍板（补记，2026-09-12）

§11 三个岔口与一项调整，裁决如下：

1. **contract 条 6 重复归档（岔口 1）：准 Store 级短路修复，S1 有界文件集显式扩 `internal/ledger/sessions.go`（+对应测试文件）。**理由：契约条 6 冻结的是 `Store.ArchiveSession` 幂等语义；facade 短路会让 collab 测试绿而账本契约继续破（假绿）。扩集 +1 文件在本拍板记录下显式申报，recon 对账按此核对。
2. **红窗处置（岔口 2）：接受 S1→S4/S5 之间的红窗，16 支测试归 S4/S5 各自文件集，S1 台账记精确清单供对账。**理由：那些断言属端点/CLI 面，S1 顺手改会越界并与 S4/S5 的端点改造制造冲突；红窗受 DAG 有界，integrate 全量是最终闸。
3. **ErrBadState HTTP 映射（岔口 3）：归 S4 拍板；若需 client 新哨兵，先回 contract 补签名再落码。**信息项，本轮不动。
4. **VerifyWriter AST 守卫（§12 调整）：批准。**从源码读形状强于人工审查项，先例成立。
