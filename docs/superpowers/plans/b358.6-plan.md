# B358.6（d_web）控制台会话内容面——实现计划

- **卡**：B358.6（B358 拆解 S6，L3 重档子系统执行者依契约自写、协调者只审；本卡是 B358 最后一张子卡）
- **依据**：contract `docs/superpowers/specs/b358-contract.md`（§3.2 wire DTO 键集、§7.3 TS 孪生金样本欠账、§8.4 控制台内容面欠账）＋ breakdown `docs/superpowers/specs/b358-breakdown.md` §3.7（S6 四段式，含 2026-09-12 走查修订标注）＋ spec `docs/superpowers/specs/b358.md` 实现决定 1（**2026-09-12 修订版**）/2–4、§4.4、§6 接缝 #4 ＋ **B361 `docs/superpowers/specs/b361.md`（IA 权威）** ＋ 形态北极星 `prototypes/b358-session-groups/pages/sessions-v2.html`（对照版 sessions.html 保留）
- **前置已合入**：B358.1–.5、.7 全部在树——本计划全部判据对当前工作树（`cards/B233.1-charter-7` @ `6a5ac3c1`）实测；会话取数面 = B358.4 落地的 `/api/sessions` 六端点 + 复用的 `/api/rooms` 发言/已读/历史（`internal/agentd/ledgerapi.go:56-61`、`sessionsapi.go`）
- **图覆盖债**：d_web 的 api 层与 hooks 不在 `codegraph/best.json`；`dedupKey` 图读数 stale（探针原文见台账 §二/§三）——一律 `file#Symbol` 源码锚；新增符号清单在 Task 6 落台账，交 integrate/recon 补视图 diff
- **台账**：`docs/superpowers/ledgers/2026-09-12-b358.6-plan-ledger.md`

## 0. 本卡干什么（一段话）

把会话内容面落进控制台，**IA 按 B361 定稿**：左栏改「会话 | 任务」两 tab 点击切换（项目树原样收进任务 tab，会话 tab 聚合未读徽章）；点会话行把会话开成**工作台 tab**（真仓词汇 = wb 组，多开/可关/◫ 分屏）；群聊面（人话 + @高亮 + 回复引用条 + 卡 chips 空座虚线）与会话详情（成员状态 / 任务节点 / timeline 三块）留在 tab 内部 ⋯/← 切换；**旧 RoomPanel（悬浮窗 + 工作项内嵌常驻栏 + 悬浮球）直接移除**（B361 用户裁决），会话入口收敛左栏会话 tab，面包屑与右栏文件树随工作台外壳自然恢复（零改动）。TS 侧补 Session DTO 全集与孪生金样本（`RoomsFixture.json` 与 `sessions_fixture_test.go` 逐键一致）。真实浏览器三态走查（W1–W6）归协调者（真机 #7），机内只验结构与金样本。

## 0.1 有界文件集（本卡只许碰这些文件）

**A 组：breakdown §3.7 ④ 原有（照任务书）**
- `web/src/api/rooms.ts`——会话 DTO 类型 + 六个 fetch 函数 + `RoomMessage.reply_to`
- `web/src/api/rooms.test.ts`——孪生金样本扩充（session describe 族）
- `web/src/api/rooms.fetch.test.ts`——端点契约测试扩充（与 rooms.test 同属契约层测试文件，breakdown 未单列但它是 rooms.ts 缝的既有测试宿主）
- `web/src/api/testdata/RoomsFixture.json`——fixture 扩充。**路径裁定**：breakdown 写 `web/testdata/RoomsFixture.json`，真码 import 是 `./testdata/RoomsFixture.json`（= `web/src/api/testdata/`，B156.2 既有），沿真码路径
- `web/src/app/rooms/`——新会话组件族（sessionModel / SessionSidebar / SessionTab / SessionChat / SessionDetail / NewSessionDialog / JoinCardDialog + 测试）＋ 旧面板退役（删 `RoomPanel.tsx`、`RoomPanel.test.tsx`、`roomPanelModel.ts`、`useRoomPanelGeom.ts`；改写 `pollInterval.test.tsx`；保留 `roomLog.ts` 与 `constants.ts` 被新族复用）
- `web/src/app/shell/Shell.tsx`——挂载点改写 + 左栏两 tab 壳（左栏 tab 开关落在这里，ProjectTree **零改动**原样保留在任务 tab）
- `web/src/app/shell/Shell.test.tsx`——Shell 结构断言宿主（「统一房间面板挂载」节反写为 B361 会话 IA 断言）
- `web/src/app/homedock/`——**零改动（落空声明）**：该条目是 W1「一等公民页 + dock 入口」时代的遗留；B361 会话入口收敛左栏会话 tab，HomeDock（home 终端浮窗）与会话零耦合（台账 §四.7）

**B 组：B361 必要增补（本计划申报，协调者审查确认——岔口 3）**

B361 把会话宿主从「整块中央区」改为「工作台 tab」，宿主文件必然被触；不改 A 组之外的这四个文件，本卡**编译不过或恢复即丢数据**：

- `web/src/app/workbench/tabs.ts`——`TabContent` 联合 +1 `{kind:'session'}`；`dedupKey`/`tabTitle` 各 +1 case（不改则会话 tab 无法构造、tabbar 无标题）
- `web/src/app/workbench/persist.ts`——`parseContent` +1 session case。**不改 = 数据丢失级**：restore 白名单拒收未知 kind → `parseWorkbench` 整份返回 null → **一次重载丢掉用户全部已存工作台布局**（persist.ts:77-99、122-123 实读）
- `web/src/app/workbench/useWorkbench.ts`——`sessionBase()` 纯函数一个（与 `HOME_BASE`/`scratchBase` 同列的 BaseDir 工厂）
- `web/src/app/workbench/tabs.test.ts`、`web/src/app/workbench/persist.test.ts`——对应测试行（既有测试零改动）

**C 组：B361 形态增补（◫ 分屏开关的宿主；随岔口 3 一并确认）**

- `web/src/app/workbench/WorkbenchPage.tsx`——tabbar 行右侧 ◫ 按钮 + `onSplitSessions?` prop 透传（B361 决定 5「tabbar + ◫ 固定分屏开关」；可选 prop，不传不渲染，既有测试零波及）

**禁触（越界即回退）**：
- `internal/**` 全部（B358.1–.4 已收口；本卡只做 HTTP 消费方）
- `web/src/app/workbench/TabBar.tsx`（会话组图标落 default=Plus，可接受；不为此扩文件集）、`WorkbenchPage.test.tsx`、`paneDrop.ts`、`restore.ts`、`useWorkbenchSync.ts`
- `web/src/app/tree/ProjectTree.tsx`、`web/src/app/cards/**`、`web/src/app/board/**`
- 新增 npm 依赖（零新增——vitest/testing-library/lucide 既有）
- 工作区无关脏文件（`web/src/app/task/Composer.test.tsx`、`.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、**`web/design-qa-sidebar-after.png` 与 `web/design-qa.md`——别卡遗留，不碰不删**）不卷入提交

## 0.2 现状依赖行为（必须按出处实现；行号为 2026-09-12 读数，以符号锚为准）

| # | 事实 | 出处 |
|---|---|---|
| 1 | 会话六端点已注册：POST/GET `/api/sessions`、GET `/api/sessions/{id}`、POST `…/archive`、POST `…/cards`、DELETE `…/cards/{cardID}`，全部 `withRooms` 守卫；发言/已读/历史复用 `/api/rooms/{id}/messages`、`/api/rooms/{id}/read`（`{id}`=会话号） | `internal/agentd/ledgerapi.go:49-61` |
| 2 | 建会话 req `{title, owner}`：owner 统一记法前缀校验（`user:<name>`/`agent:<name>`），缺失/机器位/裸名 400；审计 actor 服务端注入不收 body 字段 | `internal/agentd/sessionsapi.go#validSessionOwner`（:31-37）、`#handleSessionCreate`（:59-89） |
| 3 | 列表 `GET /api/sessions` 信封 `{"sessions":[SessionSummary…]}`，member 维度=服务端注入 `web:<host>`，`?member=` 不参与；未读=该 member 游标投影（**列全部会话，不按成员过滤**） | `internal/agentd/sessionsapi.go#handleSessionsList`（:94-104）、`internal/collab/sessions.go#ListSessions`（:30-64） |
| 4 | 详情 `GET /api/sessions/{id}` → `SessionDetail` 全文；不存在 404 且文案含会话 id | `internal/agentd/sessionsapi.go#handleSessionDetail`（:111-121） |
| 5 | 发言 req `{body, refs?, mentions?}`——**无 reply_to 字段**（回复锚生产面缺口已立 B365）；空正文 400；归档发言 409、非成员发言 403（ErrNotWriter）；已读 req `{upto_seq}`；历史 `?before=&limit=`（limit<=0 服务端取 200）信封 `{"messages":[LedgerEvent…]}` | `internal/agentd/roomsapi.go#handleRoomSend`（:321-346）、`#handleRoomRead`（:349-364）、`#handleRoomMessages`（:301-317）、`#collabErr`（:32-43） |
| 6 | 发言权执法：`sessionWriters` = 显式 Members ∪ 会话内卡当前席位；**AddSessionMember 无任何生产入口**（无 HTTP 端点无 CLI flag——B358.4 拍板②显式不做项+真机核对义务；B358.5 测试经 facade 直注成员）。控制台 actor=`web:<host>` 不在 Members ⇒ 生产里控制台对任何会话发言=403（本卡最重岔口 1） | `internal/collab/service.go#sessionWriters`（:108-129）、`#sendToSession`（:135-167）、`cmd/session_test.go:41/262` |
| 7 | SessionSummary 键集 `id,kind,title,owner,archived,unread,needs_human,last_activity` + omitempty `preview,members,cards`；成员状态词表四值 `working/listening/last_active/empty`（listening 生产零载体）；timeline kind 词表八值；`nodes` 无镜像事件时缺键 | `internal/proto/sessions.go:75-138`；金样本 `internal/proto/sessions_fixture_test.go` 6 支 |
| 8 | `RoomMessage.ReplyTo int64 omitempty`（`reply_to`）：零值省键、非零在线、roundtrip 恒等 | `internal/proto/rooms.go`；`internal/proto/sessions_fixture_test.go#TestRoomMessageReplyToGolden` |
| 9 | 成员投影：显式成员 kind=human/agent、席位成员 kind=seat 带 `card_id`/`card_title`，空座 `Status=empty` 且 `Identity=""`（identity 键恒在——SessionMember.Identity 无 omitempty）；`last_active` omitempty 零值省键 | `internal/collab/sessions.go#sessionMembers`（:219-252）、`internal/proto/sessions.go:49-63` |
| 10 | 真仓 tabbar 词汇：**tab=组**（TabBar.tsx 头注释），组标签=焦点内容名（autoName）；会话行开 tab=`openOrFocus`（全局搜 dedupKey，不命中开新组）→ tabbar 每会话一项；关 = 组关闭；◫ = place 到焦点格右列 | `web/src/app/workbench/TabBar.tsx:1-20`、`web/src/app/workbench/tabs.ts#openOrFocus`（:262-275）、`#placeSource` zone:'right'（:473-477） |
| 11 | 持久化：`encodeWorkbench` 走 `stripContent`（default 分支浅拷贝——session 字段原样落盘）；`decodeWorkbench#parseContent` 按 kind 白名单、未知 kind 返 null 且**整份 payload 拒收** | `web/src/app/workbench/persist.ts:34-44`、`:77-99`、`:122-123` |
| 12 | `Tab.base` 必填且 `parseBase` 白名单 kind∈{workspace,home,scratch}；BaseDir 工厂先例 `HOME_BASE`/`scratchBase` 在 useWorkbench.ts | `persist.ts:70-75`、`useWorkbench.ts:34-41` |
| 13 | 轮询原语 `usePoll(fetcher, intervalMs, {enabled})`：首拉+续拉、document.hidden 停表、断线保留数据、401 终止态；协作面节奏 `COLLAB_POLL_MS=5000` | `web/src/app/data/usePoll.ts`、`web/src/app/rooms/constants.ts:4` |
| 14 | RoomPanel 是 `/api/rooms` 列表与 `/api/inbox` 的**唯一** TS 消费方（grep 证实）；退役后 fetchRooms/fetchInbox 成无 UI 消费导出——保留（端点仍在、Go 金样本仍锁，契约镜像对称），只删 UI 族 | 台账 §四.6；`web/src/app/rooms/RoomPanel.tsx` |
| 15 | Shell 挂载点：`import { RoomPanel } from '../rooms/RoomPanel'`（:60）+ `{ledgerEnabled && <RoomPanel workbench={wb} persistent={cardsRoute} onOpenCard=…>}`（:808）；左栏 ResizableSidebar 直装 ProjectTree（:640-689）；renderContent switch 在 Shell（:717-785，terminal/file/tui）——**会话 tab 内容渲染挂这里，Shell 在文件集内** | `web/src/app/shell/Shell.tsx` |
| 16 | vitest 布局：jsdom + `src/test/setup.ts`；`npm test`=`vitest run`；`npm run typecheck`=`tsc -b`；测试 mock 惯例=vi.mock api 模块 + `vi.stubGlobal('fetch')` | `web/vite.config.ts`、`web/package.json`、`rooms.fetch.test.ts` |

## 0.3 图覆盖债

探针（2026-09-12 亲跑，原文见台账 §二）：在图——`Shell`（ok）、`RoomPanel`（ok）、`tabTitle`（moved）、`dedupKey`（**moved 且签名 stale**：图记 `dedupKey(c)`，真码 `dedupKey(baseKey, content)`，行为以真码为准）、`COLLAB_POLL_MS`（ok）。不在图——`fetchRooms`/`usePoll`/`SessionTab` 及 rooms.ts 全部导出。本卡新增符号（§A/B/C 组全部）清单在 Task 6 落台账交 recon；收尾跑 `codegraph resolve --doc docs/superpowers/plans/b358.6-plan.md` 修坏锚。

## 1. 已亲跑的基线与判据

以下读数在基线（`6a5ac3c1` 工作树）亲跑，执行者在 Task 0 复跑比对，读数不同先停下报告：

| 命令 | 基线结果（2026-09-12 实测） |
| --- | --- |
| `cd web && npx vitest run` | 退出 0（全绿基线） |
| `cd web && npm run typecheck` | 退出 0 |
| `npx vitest run src/api/rooms.test.ts` | 退出 0（孪生金样本现状：B156.2 三类） |
| `grep -n "session" web/src/api/rooms.ts` | 零命中——TS 侧无任何会话类型（Task 1 红的根据：缺位） |
| `grep -rn "RoomPanel" web/src --include="*.tsx" -l` | 恰 3 文件：Shell.tsx、rooms/RoomPanel.tsx、rooms/RoomPanel.test.tsx（+roomPanelModel.ts/useRoomPanelGeom.ts 经 import 关联） |
| `codegraph sym dedupKey` | moved + 签名 stale（§0.3） |

判据先行的代码级确证（读码得出，出处 0.2#5/6/11）：基线上「TS 侧请求 `/api/sessions`」无函数可调（缺位）、「含 session tab 的布局 payload decode」整份拒收（parseContent 白名单）、「RoomPanel 卸载」未发生（挂载在树）——Task 1/2/4 的测试在基线红或缺位，红/缺位是有意义的。

## 2. 终局形状（plan 定稿，随后写进验收作字面断言）

### 2.1 IA（B361 逐条落点）

1. **左栏两 tab**：`ResizableSidebar` 内新增 tab 行——`会话`（含未读徽章 `data-testid="sidebar-unread"`=Σ unread，默认激活）｜`任务`；两 pane 都保持挂载、`hidden` 切换（ProjectTree 不卸载、展开态与树流不丢）；`ledgerEnabled=false` 时不渲染会话 tab 按钮（与旧 RoomPanel 同门控）。
2. **会话行开工作台 tab**：行点击 → `wb.openOrFocus({kind:'session', sessionId, title}, sessionBase(id))`——dedupKey=`session:<id>` 全局去重（重复点击聚焦原 tab），不命中开新组；tabbar 每会话一项（autoName 组显示 `会话 · <title>`）；可多开；可关（组关闭钮）。
3. **◫ 分屏**：WorkbenchPage tabbar 行右侧 `◫ 分屏` 按钮——焦点窗格为非会话 tab 且存在会话 tab 时可用；点击把**最近打开的会话 tab** place 到焦点格 `zone:'right'`（工作项左/会话右）；焦点是会话 tab 或无会话 tab 时禁用。
4. **群聊↔详情**：会话 tab 窗格内部两态，⋯ 进详情、← 返回（B361 决定 3 原样保留）。
5. **移除**：RoomPanel.tsx 全家（浮窗、常驻栏、悬浮球、几何持久化）删除；挂载行删除；「打开房间面板」按钮与 `room-panel` testid 在任何路由/形态下不出现（反例断言）。面包屑与右栏文件树零改动自动恢复工作台外壳。
6. **偏差注记（真机走查 W2/W3 对照时须知）**：① 原型列表头的「▦ 全部项目」筛选落空——SessionSummary 无 project 字段（mock 数据才有），不渲染假筛选器；② 回复**编辑**框不在本卡——`handleRoomSend` 不收 reply_to（0.2#5），缺口归 B365；引用条为只读渲染+点击跳转；③ 成员状态 `listening` 生产零载体（B358.2 澄清③），生产只见 working/last_active/empty，词表位保留渲染映射。

### 2.2 wire 形状（TS 侧定稿）

- `RoomMessage` + `reply_to?: number`（omitempty：缺键≠0 的分辨在金样本断言）。
- 新增类型与函数（逐字，落 `web/src/api/rooms.ts`）：

```ts
export const SESSION_KIND = 'session'

export type SessionMemberKind = 'human' | 'agent' | 'seat'
// 「看板不说谎」：与 proto.SessionMember* 状态词表逐值一致（sessions_fixture_test.go 冻结）。
export type SessionMemberStatus = 'working' | 'listening' | 'last_active' | 'empty'

export interface SessionMember {
  identity: string
  kind: SessionMemberKind
  card_id?: string
  card_title?: string
  status: SessionMemberStatus
  last_active?: string
}

export interface SessionCard { card_id: string; title?: string; status?: string; seat?: string }

export interface SessionSummary {
  id: string
  kind: string
  title: string
  owner: string
  archived: boolean
  unread: number
  needs_human: boolean
  last_activity: string
  preview?: RoomPreview
  members?: SessionMember[]
  cards?: SessionCard[]
}

export interface Session {
  id: string; title: string; owner: string; archived: boolean
  members?: string[]; cards?: string[]
  created_at: string; updated_at: string
}

export interface SessionNode { card_id: string; title?: string; node: string; round?: number; state: string; target?: string }

export type SessionTimelineKind =
  | 'created' | 'archived' | 'card_joined' | 'card_left'
  | 'seat_bound' | 'seat_rebound' | 'needs_human' | 'card_closed'

export interface SessionTimelineEvent {
  seq: number; kind: SessionTimelineKind
  card_id?: string; detail?: string; actor?: string
  created_at: string
}

export interface SessionDetail { summary: SessionSummary; nodes?: SessionNode[]; timeline?: SessionTimelineEvent[] }

// fetchSessions 会话列表（GET /api/sessions）：member 维度服务端注入（web:<host>），
// 前端不得自报身份——不设 member 参数（与 S4 缺陷族 5 反例镜像）。
export const fetchSessions = (): Promise<SessionSummary[]> =>
  request<{ sessions: SessionSummary[] }>('/api/sessions').then((r) => r.sessions ?? [])

// fetchSessionDetail 会话详情三块（GET /api/sessions/{id}）。
export const fetchSessionDetail = (id: string): Promise<SessionDetail> =>
  request<SessionDetail>(`/api/sessions/${encodeURIComponent(id)}`)

// createSession 建会话（POST /api/sessions {title, owner}）：owner 统一记法
// user:<name>/agent:<name>（服务端前缀校验为权威，客户端仅预检）；审计 actor
// 服务端注入——请求体无该字段。
export const createSession = (title: string, owner: string): Promise<Session> =>
  postJSON<Session>('/api/sessions', { title, owner })

// archiveSession 归档会话（POST …/archive，幂等）。控制台本期无归档入口
// （CLI `session archive` 是操作面），函数保留为六端点镜像完整性。
export const archiveSession = (id: string): Promise<{ ok: boolean }> =>
  postJSON<{ ok: boolean }>(`/api/sessions/${encodeURIComponent(id)}/archive`, {})

// joinSessionCard 拉卡进群（POST …/cards {card}；进群 ≠ 配人）。
export const joinSessionCard = (id: string, card: string): Promise<{ ok: boolean }> =>
  postJSON<{ ok: boolean }>(`/api/sessions/${encodeURIComponent(id)}/cards`, { card })

// leaveSessionCard 移卡出群（DELETE …/cards/{cardID}，幂等）。控制台本期无入口，镜像保留。
export const leaveSessionCard = (id: string, card: string): Promise<{ ok: boolean }> =>
  deleteJSON<{ ok: boolean }>(`/api/sessions/${encodeURIComponent(id)}/cards/${encodeURIComponent(card)}`)
```

- 发言/已读/历史**复用既有** `sendRoomMessage`/`markRoomRead`/`fetchRoomMessages`（会话号作 `{id}`），签名零改动。

### 2.3 fixture 扩充（`web/src/api/testdata/RoomsFixture.json`，与 Go 金样本逐键同源）

在现有 8 case 后追加（值逐字取自 `internal/proto/sessions_fixture_test.go`，**同一 JSON 文件双侧共用——TS 侧不得手抄漂移**；Go 侧编码器即权威）：

```json
  {
    "case": "user-reply",
    "message": {
      "room": "session:1",
      "kind": "user",
      "body": "@B233.16 守卫的收口归你",
      "reply_to": 42
    }
  },
  {
    "case": "session-summary-golden",
    "summary": {
      "id": "session:7",
      "kind": "session",
      "title": "架构物理化",
      "owner": "user:sy",
      "archived": false,
      "unread": 2,
      "needs_human": true,
      "last_activity": "1970-01-01T00:00:00Z",
      "members": [
        { "identity": "user:sy", "kind": "human", "status": "last_active" },
        { "identity": "", "kind": "seat", "card_id": "B1", "card_title": "竖切卡", "status": "working" }
      ],
      "cards": [
        { "card_id": "B1", "title": "竖切卡", "status": "进行中", "seat": "cli:opencode#s1" }
      ]
    }
  },
  {
    "case": "session-summary-archived-empty",
    "summary": {
      "id": "session:9",
      "kind": "session",
      "title": "归档场",
      "owner": "agent:claude",
      "archived": true,
      "unread": 0,
      "needs_human": false,
      "last_activity": "1970-01-01T00:00:00Z",
      "cards": [
        { "card_id": "B2", "title": "空座卡", "status": "待办" }
      ]
    }
  },
  {
    "case": "session-detail-golden",
    "detail": {
      "summary": {
        "id": "session:7", "kind": "session", "title": "会话", "owner": "user:sy",
        "archived": false, "unread": 0, "needs_human": false,
        "last_activity": "1970-01-01T00:00:00Z"
      },
      "nodes": [
        { "card_id": "B1", "node": "implement", "state": "running" }
      ],
      "timeline": [
        { "seq": 1, "kind": "created", "created_at": "1970-01-01T00:00:00Z" },
        { "seq": 2, "kind": "card_joined", "card_id": "B1", "created_at": "1970-01-01T00:00:00Z" },
        { "seq": 3, "kind": "seat_bound", "card_id": "B1", "detail": "{\"to\":\"cli:opencode#s1\"}", "actor": "cli:opencode#s1", "created_at": "1970-01-01T00:00:00Z" },
        { "seq": 4, "kind": "seat_rebound", "card_id": "B1", "detail": "{\"from\":\"cli:opencode#s1\",\"to\":\"cli:grok#s2\"}", "actor": "cli:grok#s2", "created_at": "1970-01-01T00:00:00Z" },
        { "seq": 5, "kind": "needs_human", "card_id": "B1", "detail": "{\"reason\":\"组装点冲突\"}", "created_at": "1970-01-01T00:00:00Z" },
        { "seq": 6, "kind": "card_closed", "card_id": "B1", "detail": "{\"from\":\"待审阅\",\"to\":\"已完成\"}", "created_at": "1970-01-01T00:00:00Z" }
      ]
    }
  }
```

fixture 类型守护（rooms.test.ts 顶层 `cases` 类型并集同步加 `summary?: SessionSummary`、`detail?: SessionDetail`）。

### 2.4 workbench 宿主形状（B 组定稿）

```ts
// tabs.ts —— TabContent 联合追加：
  | { kind: 'session'; sessionId: string; title: string }

// tabs.ts —— dedupKey 追加（一会话一 tab，全局去重；title 是显示名不参与去重——
// 会话标题不可改名，无 stale 风险）：
    case 'session': return `session:${content.sessionId}`

// tabs.ts —— tabTitle 追加：
    case 'session': return `会话 · ${content.title}`

// persist.ts —— parseContent 追加（不改 = 含会话 tab 的 payload 整份拒收，重载丢全部布局）：
    case 'session':
      return hasOnly(raw, ['kind', 'sessionId', 'title']) && isString(raw.sessionId) && isString(raw.title)
        ? { kind: 'session', sessionId: raw.sessionId, title: raw.title }
        : null

// useWorkbench.ts —— BaseDir 工厂（与 HOME_BASE/scratchBase 同列；kind 取白名单内的
// 'home'——会话不挂目录，path 留空；key 唯一化使 openItems 已打开行互不撞键）：
export function sessionBase(sessionId: string): BaseDir {
  return { key: `session:${sessionId}`, kind: 'home', path: '', label: '会话', projectName: '', machine: '' }
}
```

`stripContent` 无需改动（default 分支浅拷贝已保留 sessionId/title，0.2#11）。

### 2.5 Shell 接线形状（定稿）

- 卸下 :60 import 与 :808 挂载行；新增 `sessionsState = usePoll(fetchSessions, COLLAB_POLL_MS, { enabled: ledgerEnabled })`（会话 tab 徽章在任务 tab 激活时也要活——徽章数据源在 Shell 不在列表组件）。
- 左栏：`sidebarTab` state（`'sessions' | 'tasks'`，默认 `'sessions'`=家）；tab 行两按钮 `role="tab"`；两 pane `hidden` 切换双挂载。
- 新建会话：Shell 持 `createOpen` state + `NewSessionDialog`；确认 → `createSession(title, owner)` → `sessionsState.refresh()` → 关闭弹层；失败原文入弹层 error。
- renderContent 追加：`case 'session': return <SessionTab sessionId={c.sessionId} title={c.title} onOpenCard={(id) => navigate(`/cards?card=${encodeURIComponent(id)}`)} />`。
- ◫：`splitSessionRight()` 回调经 WorkbenchPage 新 prop `onSplitSessions` 下传（§2.4/Task 4）。
- 日志：会话入口/建会话/分屏走 `console.debug('shell.session.*', …)` 既有 debug 惯例（与 `shell.task.open` 同族）。

## 3. Task 0：基线复跑（不写码）

### 文件范围

无代码改动；读数落台账。

### 步骤

1. 复跑 §1 表六条命令/探针，逐条与基线比对。不符 → 停手上报。
2. 台账记 Task 0 读数。

### Task 0 验收

六条读数与 §1 一致；台账已记。

## 4. Task 1：TS 镜像 + 孪生金样本（rooms.ts / RoomsFixture.json / rooms.test.ts / rooms.fetch.test.ts，红→绿）

### 文件范围

`web/src/api/rooms.ts`、`web/src/api/testdata/RoomsFixture.json`、`web/src/api/rooms.test.ts`、`web/src/api/rooms.fetch.test.ts`。

### Interfaces

**Consumes（既有，逐字）**：`request`/`postJSON`/`deleteJSON`（`web/src/api/client.ts:127/153/186`）；`RoomPreview`（rooms.ts 既有）；fixture import 惯例 `import fixture from './testdata/RoomsFixture.json'`。

**Produces（本卡新增，交 Task 3/4 消费与 recon 入图）**：§2.2 全部类型 + `SESSION_KIND` + `fetchSessions`/`fetchSessionDetail`/`createSession`/`archiveSession`/`joinSessionCard`/`leaveSessionCard`；`RoomMessage.reply_to?: number`。

### 步骤（一条红绿周期贯穿——全部缝级断言锁在 rooms.ts 契约层缝上）

**步骤 1（红）**：fixture 追加 §2.3 三 case + user-reply case；`rooms.test.ts` 顶层 cases 类型并集加 `summary?/detail?` 两键，文末追加（完整代码，直接落）：

```ts
// —— B358.6 会话孪生金样本：与 internal/proto/sessions_fixture_test.go 逐键一致，
// 值以同一 JSON 文件为唯一来源（fixture 与 Go 金样本同源，TS 侧不手抄）。——
describe('session twin fixtures (B358.6)', () => {
  it('SessionSummary 金样本键集逐键在文，kind 恒 session', () => {
    const summary = cases.find((c) => c.case === 'session-summary-golden')!.summary!
    for (const key of ['id', 'kind', 'title', 'owner', 'archived', 'unread', 'needs_human', 'last_activity']) {
      expect(summary, `SessionSummary 缺键 ${key}`).toHaveProperty(key)
    }
    expect(summary.kind).toBe('session')
    expect(summary.unread).toBe(2)
    expect(summary.needs_human).toBe(true)
    expect(summary.preview).toBeUndefined()
  })

  it('成员键集与 omitempty 语义：identity 恒在、席位带 card_id/card_title、last_active 零值省键', () => {
    const summary = cases.find((c) => c.case === 'session-summary-golden')!.summary!
    const [human, seat] = summary.members!
    expect(human).toMatchObject({ identity: 'user:sy', kind: 'human', status: 'last_active' })
    expect(human).not.toHaveProperty('last_active')
    expect(human).not.toHaveProperty('card_id')
    expect(seat).toMatchObject({ identity: '', kind: 'seat', card_id: 'B1', card_title: '竖切卡', status: 'working' })
  })

  it('归档空座场：members omitempty 缺键（缺失≠空数组）、空座卡 seat 缺键=还没配人', () => {
    const summary = cases.find((c) => c.case === 'session-summary-archived-empty')!.summary!
    expect(summary.archived).toBe(true)
    expect(summary.members).toBeUndefined()
    expect(summary.cards![0].seat).toBeUndefined()
  })

  it('SessionDetail 键集：summary 恒在、nodes/timeline 在文', () => {
    const detail = cases.find((c) => c.case === 'session-detail-golden')!.detail!
    for (const key of ['summary', 'nodes', 'timeline']) {
      expect(detail, `SessionDetail 缺键 ${key}`).toHaveProperty(key)
    }
    expect(detail.nodes![0]).toMatchObject({ card_id: 'B1', node: 'implement', state: 'running' })
    expect(detail.nodes![0]).not.toHaveProperty('round')
    expect(detail.timeline![0]).toMatchObject({ seq: 1, kind: 'created' })
    expect(detail.timeline![1]).not.toHaveProperty('actor')
  })

  it('timeline kind 词表恰八值且 fixture 逐值在文（消费方渲染必须与 Go 词表对齐）', () => {
    const kinds = ['created', 'archived', 'card_joined', 'card_left', 'seat_bound', 'seat_rebound', 'needs_human', 'card_closed']
    const detail = cases.find((c) => c.case === 'session-detail-golden')!.detail!
    const fixtureKinds = detail.timeline!.map((row) => row.kind)
    for (const kind of ['created', 'card_joined', 'seat_bound', 'seat_rebound', 'needs_human', 'card_closed']) {
      expect(fixtureKinds, `fixture 应含 kind=${kind}`).toContain(kind)
    }
    for (const kind of fixtureKinds) expect(kinds, `未知 timeline kind ${kind}`).toContain(kind)
  })

  it('成员状态词表恰四值；online 不是合法取值（反例断言：词表闭包）', () => {
    const four: SessionMemberStatus[] = ['working', 'listening', 'last_active', 'empty']
    const summary = cases.find((c) => c.case === 'session-summary-golden')!.summary!
    for (const member of summary.members!) expect(four, `成员状态越表: ${member.status}`).toContain(member.status)
    expect(four).not.toContain('online' as SessionMemberStatus)
    expect(four).not.toContain('在线' as SessionMemberStatus)
  })

  it('reply_to omitempty 三态：缺键=undefined（非 0）、非零在线', () => {
    const minimal = cases.find((c) => c.case === 'user-minimal')!.message! as unknown as Record<string, unknown>
    expect(minimal, '缺键必须 decode 成 undefined 而不是 0（可空 vs 零值分辨）').not.toHaveProperty('reply_to')
    const replied = cases.find((c) => c.case === 'user-reply')!.message!
    expect(replied.reply_to).toBe(42)
  })
})
```

其中 `SessionMemberStatus` 从 `./rooms` import（类型并集同批加）。跑红：

```
$ cd web && npx vitest run src/api/rooms.test.ts
  → FAIL：SessionMemberStatus 未导出、session-* case 缺失（find().! 抛 TypeError）
  ——红因是功能缺位，不是 typo。
```

**步骤 2（绿：最小实现）**：`rooms.ts` 落 §2.2 全部代码（类型块置于 `RoomSummary` 定义后、fetch 函数块置于 `fetchInbox` 后）；`RoomMessage` 接口加一行 `reply_to?: number`（注释：被回复消息的账本 seq；隐式寻址原作者；omitempty——缺键≠0）；fixture 追加 §2.3。

**步骤 3（fetch 契约测试，缝级断言第二半）**：`rooms.fetch.test.ts` 文末追加（完整代码，直接落）：

```ts
// —— B358.6 会话端点契约（六端点 + 复用面）：URL/方法/请求体逐端点断言，
// 并对同一 fixture 做解码断言（序列化边界穿真实 fetch stub → request() 解析）。——
import { createSession, fetchSessionDetail, fetchSessions, joinSessionCard, leaveSessionCard } from './rooms'

describe('fetchSessions (B358.6)', () => {
  it('GET /api/sessions 恰此 URL（无 member 参数——身份服务端注入），解包 sessions 数组', async () => {
    const summary = cases.find((c) => c.case === 'session-summary-golden')!.summary
    const fetchMock = vi.fn().mockImplementation(() => Promise.resolve(jsonResp({ sessions: [summary] })))
    vi.stubGlobal('fetch', fetchMock)
    const sessions = await fetchSessions()
    expect(fetchMock.mock.calls[0][0]).toBe('/api/sessions')
    expect(sessions).toHaveLength(1)
    expect(sessions[0].members![1].kind).toBe('seat')
  })
})

describe('fetchSessionDetail (B358.6)', () => {
  it('GET /api/sessions/{id}，id 走 encodeURIComponent，解码金样本 detail', async () => {
    const detail = cases.find((c) => c.case === 'session-detail-golden')!.detail
    const fetchMock = vi.fn().mockResolvedValue(jsonResp(detail))
    vi.stubGlobal('fetch', fetchMock)
    const out = await fetchSessionDetail('session:7')
    expect(fetchMock.mock.calls[0][0]).toBe('/api/sessions/session%3A7')
    expect(out.timeline).toHaveLength(6)
  })
})

describe('session 写面 (B358.6)', () => {
  it('createSession POST {title, owner}——owner 统一记法原样透传，无 actor 字段', async () => {
    const fetchMock = vi.fn().mockResolvedValue(jsonResp({ id: 'session:1', title: 'T', owner: 'user:sy', archived: false, created_at: '', updated_at: '' }))
    vi.stubGlobal('fetch', fetchMock)
    await createSession('需求对齐', 'user:sy')
    const [url, init] = fetchMock.mock.calls[0] as [string, RequestInit]
    expect(url).toBe('/api/sessions')
    expect(init.method).toBe('POST')
    expect(JSON.parse(init.body as string)).toEqual({ title: '需求对齐', owner: 'user:sy' })
  })

  it('joinSessionCard POST {card} / leaveSessionCard DELETE / archiveSession POST', async () => {
    const fetchMock = vi.fn().mockResolvedValue(jsonResp({ ok: true }))
    vi.stubGlobal('fetch', fetchMock)
    await joinSessionCard('session:1', 'B1')
    const [joinUrl, joinInit] = fetchMock.mock.calls[0] as [string, RequestInit]
    expect(joinUrl).toBe('/api/sessions/session%3A1/cards')
    expect(joinInit.method).toBe('POST')
    expect(JSON.parse(joinInit.body as string)).toEqual({ card: 'B1' })
    await leaveSessionCard('session:1', 'B1')
    const [leaveUrl, leaveInit] = fetchMock.mock.calls[1] as [string, RequestInit]
    expect(leaveUrl).toBe('/api/sessions/session%3A1/cards/B1')
    expect(leaveInit.method).toBe('DELETE')
    await archiveSession('session:1')
    expect(fetchMock.mock.calls[2][0]).toBe('/api/sessions/session%3A1/archive')
  })
})
```

（`archiveSession` 加入首行 import 列表。）

**步骤 4（绿）**：

```
$ cd web && npx vitest run src/api/rooms.test.ts src/api/rooms.fetch.test.ts  → 退出 0
$ cd web && npm run typecheck  → 退出 0
```

### Task 1 验收

1. `npx vitest run src/api/rooms.test.ts` 退出 0——孪生金样本七支新断言全过：Summary/Detail 键集逐键、omitempty 缺失≠零值（members 键缺、seat 键缺、reply_to 缺键=undefined）、成员状态四值词表闭包（online 反例）、timeline kind 词表与 fixture 对齐、reply_to 非零在线。
2. `npx vitest run src/api/rooms.fetch.test.ts` 退出 0——六端点 URL/方法/请求体逐端点过，解码穿真实 fetch stub。
3. `npm run typecheck` 退出 0（强类型承接 fixture，字段漂移在 tsc 报错——孪生金样本编译期半边）。
4. 既有 B156.2 断言零改动全绿（fixture 追加不扰最小金样本——user-minimal case 未动）。

## 5. Task 2：workbench 宿主增量（tabs.ts / persist.ts / useWorkbench.ts + 两测试文件，红→绿）

### 文件范围

`web/src/app/workbench/tabs.ts`、`web/src/app/workbench/persist.ts`、`web/src/app/workbench/useWorkbench.ts`、`web/src/app/workbench/tabs.test.ts`、`web/src/app/workbench/persist.test.ts`（后两者只追加）。

### Interfaces

**Consumes**：`TabContent`/`dedupKey`/`tabTitle`/`parseContent` 现状形状（0.2#10/11/12）。

**Produces（交 Task 3/4）**：`TabContent` 的 `{kind:'session'; sessionId; title}` 分支、`dedupKey` 的 `session:<id>` 键、`tabTitle` 的 `会话 · <title>`、`sessionBase(sessionId): BaseDir`、`parseContent` session case。

### 步骤

**步骤 1（红）**：`tabs.test.ts` 文末追加、`persist.test.ts` 文末追加（完整代码，直接落）：

```ts
// tabs.test.ts —— B358.6 会话 tab 模型分支。
describe('session tab (B358.6)', () => {
  const content = { kind: 'session' as const, sessionId: 'session:7', title: '架构物理化' }
  const base: BaseDir = { key: 'session:session:7', kind: 'home', path: '', label: '会话', projectName: '', machine: '' }

  it('dedupKey= session:<id>，与 title 无关', () => {
    expect(dedupKey(base.key, content)).toBe('session:session:7')
    expect(dedupKey(base.key, { ...content, title: '改名也不影响去重' })).toBe('session:session:7')
  })

  it('tabTitle= 会话 · <title>', () => {
    expect(tabTitle(content, base.label)).toBe('会话 · 架构物理化')
  })

  it('openOrFocus 全局去重：第二次点击聚焦原 tab 不开新组', () => {
    const first = openOrFocus(EMPTY_WORKBENCH, base, content)
    const again = openOrFocus(first, base, { ...content, title: '架构物理化' })
    expect(again.groups).toHaveLength(first.groups.length)
  })
})
```

```ts
// persist.test.ts —— B358.6 会话 tab 持久化：不改 parseContent 时整份 payload 被
// 拒收（数据丢失级），本支是数据丢失反例的正面锚。
describe('session tab persistence (B358.6)', () => {
  it('encode→decode roundtrip 保留 session tab（sessionId/title 双字段）', () => {
    const wb: Workbench = {
      activeGroupId: 'g1',
      groups: [{
        id: 'g1', name: '组 1', autoName: true,
        columns: [{ panes: [{ id: 't1', base: { key: 'session:session:7', kind: 'home', path: '', label: '会话', projectName: '', machine: '' }, content: { kind: 'session', sessionId: 'session:7', title: '架构物理化' } }] }],
        sizes: [1], focus: [0, 0],
      }],
    }
    const decoded = decodeWorkbench(encodeWorkbench(wb))
    expect(decoded).not.toBeNull()
    expect(decoded!.groups[0].columns[0].panes[0]!.content).toEqual({ kind: 'session', sessionId: 'session:7', title: '架构物理化' })
  })
})
```

（两文件的既有 import 行按需补 `dedupKey`/`openOrFocus`/`Workbench`/`encodeWorkbench`/`decodeWorkbench`——均在两文件既有 import 集或同包可达。）

跑红：

```
$ cd web && npx vitest run src/app/workbench/tabs.test.ts src/app/workbench/persist.test.ts
  → FAIL：dedupKey 返 null / tabTitle 落 default / decodeWorkbench 返 null（session case 白名单拒收）
```

**步骤 2（绿：最小实现）**：`tabs.ts`/`persist.ts`/`useWorkbench.ts` 落 §2.4 三段代码（tabs.ts 的 TabContent 联合 + dedupKey + tabTitle 各一 case；persist.ts parseContent 一 case；useWorkbench.ts sessionBase 紧随 scratchBase 之后）。

**步骤 3（绿）**：

```
$ cd web && npx vitest run src/app/workbench  → 退出 0（新增支全绿，既有零改动全绿）
$ cd web && npm run typecheck  → 退出 0
```

### Task 2 验收

1. 上两条命令退出 0。
2. `git diff web/src/app/workbench/{tabs,persist,useWorkbench}.ts` 只含 §2.4 的增量行（既有 case/函数零改动）；`tabs.test.ts`/`persist.test.ts` 只追加。
3. 变异自查（落台账不提交）：把 parseContent 的 session case 注释掉 → persist 新支红（数据丢失反例可变红）；还原 → 绿。

## 6. Task 3：会话组件族（sessionModel + 五组件 + 组件测试，红→绿）

### 文件范围

新建 `web/src/app/rooms/sessionModel.ts`、`sessionModel.test.ts`、`SessionSidebar.tsx`、`SessionSidebar.test.tsx`、`SessionChat.tsx`、`SessionChat.test.tsx`、`SessionDetail.tsx`、`SessionDetail.test.tsx`、`SessionTab.tsx`、`SessionTab.test.tsx`、`NewSessionDialog.tsx`、`JoinCardDialog.tsx`；改写 `web/src/app/rooms/pollInterval.test.tsx`（RoomPanel 退役前置改写——该支断言对象从 RoomPanel 三轮询迁 SessionTab 两轮询，A.6 常量纪律不变）。

### Interfaces

**Consumes（既有 + Task 1/2 产出，逐字）**：

```ts
// ../../api/rooms（Task 1 Produces）
fetchSessions(): Promise<SessionSummary[]>; fetchSessionDetail(id: string): Promise<SessionDetail>
createSession(title: string, owner: string): Promise<Session>
joinSessionCard(id: string, card: string): Promise<{ ok: boolean }>
sendRoomMessage(id: string, body: string, opts?: { refs?: string[]; mentions?: string[] }): Promise<{ seq: number }>
markRoomRead(id: string, uptoSeq: number): Promise<{ ok: boolean }>
fetchRoomMessages(id: string, opts?: { before?: number; limit?: number }): Promise<RoomHistoryItem[]>
// ../data/usePoll
usePoll<T>(fetcher: () => Promise<T>, intervalMs: number, opts?: { enabled?: boolean }): PollState<T>
// ../lib/format
errorMessage(err: unknown): string; formatRelative(iso: string): string
// ./roomLog ./constants（保留复用）
logRoom(level, event, fields?): void; COLLAB_POLL_MS: 5000
// ../workbench（Task 2 Produces）
sessionBase(sessionId: string): BaseDir   // Task 4 Shell 用；本 task 组件不直接用
```

**Produces（交 Task 4 与 recon 入图）**：

```ts
// sessionModel.ts
export const MEMBER_STATUS_LABEL: Readonly<Record<string, string>>   // 恰四值
export function memberStatusLabel(status: string): string            // 词表外原样透传
export function memberStatusText(member: SessionMember): string      // last_active 带相对时间
export const TIMELINE_KIND_LABEL: Readonly<Record<string, string>>   // 八值
export function timelineKindLabel(kind: string): string              // 未知原样透传（不白屏）
export interface BodySegment { text: string; mention: boolean }
export function segmentBody(body: string, mentions?: string[]): BodySegment[]
export function totalUnread(summaries: SessionSummary[]): number
export function isSelfActor(actor: string): boolean                  // actor.startsWith('web:')
// 组件
export function SessionSidebar(props: SessionSidebarProps): JSX.Element
export function SessionTab(props: { sessionId: string; title: string; onOpenCard?: (cardId: string) => void }): JSX.Element
export function SessionChat(props: SessionChatProps): JSX.Element
export function SessionDetail(props: { detail: SessionDetailDTO }): JSX.Element
export function NewSessionDialog(props: { open: boolean; busy: boolean; error: string; onCancel: () => void; onCreate: (title: string, owner: string) => void }): JSX.Element
export function JoinCardDialog(props: { open: boolean; busy: boolean; error: string; onCancel: () => void; onJoin: (cardId: string) => void }): JSX.Element
```

### 步骤

**步骤 1（红）**：先落测试文件（六支：五个新测试 + pollInterval 改写；组件测试的 mock 夹具**照抄既有 harness**——`RoomPanel.test.tsx` 的 `vi.mock('../../api/rooms')` + `room()/message()` 夹具形态与本文件同族，缺的只有 session 侧工厂，补齐如下。此为占位符扫描的正当出口声明：组件测试复用 RoomPanel.test 的 mock/渲染 harness 形态，断言逐条列全、夹具工厂给全文）：

`sessionModel.test.ts`（全文）：

```ts
// sessionModel.test.ts —— 会话渲染词表与纯函数的缝侧守卫。
// 成员状态/ timeline kind 两张渲染词表是「看板不说谎」与「未知值不白屏」的
// 前端半边；变异（往 MEMBER_STATUS_LABEL 加 online 键）必须让第一支变红。
import { describe, expect, it } from 'vitest'
import type { SessionMember, SessionSummary } from '../../api/rooms'
import {
  MEMBER_STATUS_LABEL, TIMELINE_KIND_LABEL,
  memberStatusLabel, memberStatusText, segmentBody, timelineKindLabel, totalUnread,
} from './sessionModel'

describe('成员状态渲染词表（看板不说谎的前端半边）', () => {
  it('标签表恰四值，任何标签不含在线字样（反例断言）', () => {
    expect(Object.keys(MEMBER_STATUS_LABEL).sort()).toEqual(['empty', 'last_active', 'listening', 'working'])
    for (const label of Object.values(MEMBER_STATUS_LABEL)) {
      expect(label).not.toContain('在线')
      expect(label).not.toContain('online')
    }
  })
  it('四值逐一渲染中文标签', () => {
    expect(memberStatusLabel('working')).toBe('工作中')
    expect(memberStatusLabel('listening')).toBe('监听中')
    expect(memberStatusLabel('last_active')).toBe('最后活跃')
    expect(memberStatusLabel('empty')).toBe('空座')
  })
  it('词表外取值原样透传——绝不发明「在线」翻译', () => {
    expect(memberStatusLabel('online')).toBe('online')
    expect(memberStatusLabel('未来新值')).toBe('未来新值')
  })
  it('last_active 且带时间戳渲染相对时间', () => {
    const member = { identity: 'user:sy', kind: 'human', status: 'last_active', last_active: '2026-09-12T00:00:00Z' } as SessionMember
    expect(memberStatusText(member)).toContain('最后活跃')
  })
  it('last_active 无时间戳回落纯标签', () => {
    const member = { identity: 'u', kind: 'human', status: 'last_active' } as SessionMember
    expect(memberStatusText(member)).toBe('最后活跃')
  })
})

describe('@分段', () => {
  it('@命中 mentions 的高亮分段（token 去 @ 后与 mention 逐字相等）', () => {
    expect(segmentBody('@B233.16 守卫的收口归你', ['B233.16'])).toEqual([
      { text: '@B233.16', mention: true },
      { text: ' 守卫的收口归你', mention: false },
    ])
  })
  it('未命中的 @ 不高亮；无 mentions 全不高亮', () => {
    expect(segmentBody('@别人 看看', ['B233.16'])).toEqual([
      { text: '@别人', mention: false }, { text: ' 看看', mention: false },
    ])
    expect(segmentBody('@B233.16 hi', undefined)).toEqual([
      { text: '@B233.16', mention: false }, { text: ' hi', mention: false },
    ])
  })
})

describe('timeline kind 标签', () => {
  it('词表恰八值且中文标签互异（渲染与 Go 词表逐值对齐）', () => {
    expect(Object.keys(TIMELINE_KIND_LABEL).sort()).toEqual(
      ['archived', 'card_closed', 'card_joined', 'card_left', 'created', 'needs_human', 'seat_bound', 'seat_rebound'].sort(),
    )
    expect(new Set(Object.values(TIMELINE_KIND_LABEL)).size).toBe(8)
  })
  it('未知 kind 原样透传（渲染兜底不白屏）', () => {
    expect(timelineKindLabel('weird_kind')).toBe('weird_kind')
  })
})

describe('未读聚合', () => {
  it('会话 tab 徽章 = Σ unread', () => {
    expect(totalUnread([{ unread: 2 }, { unread: 0 }, { unread: 3 }] as SessionSummary[])).toBe(5)
  })
})
```

`SessionDetail.test.tsx`（全文）：

```tsx
// SessionDetail.test.tsx —— 详情三块：成员状态四值渲染、节点、timeline（含未知 kind 兜底）。
// 夹具直接取孪生金样本（同一 JSON）——组件渲染与 Go 编码器同源。
import { describe, expect, it } from 'vitest'
import { render, screen } from '@testing-library/react'
import fixture from '../../api/testdata/RoomsFixture.json'
import type { SessionDetail } from '../../api/rooms'
import { SessionDetail } from './SessionDetail'

const cases = fixture as { case: string; detail?: SessionDetail }[]

describe('SessionDetail', () => {
  it('成员块渲染四值状态中文标签，DOM 不出现在线字样（反例断言）', () => {
    const summaryCase = cases.find((c) => c.case === 'session-summary-golden')!.summary!
    render(<SessionDetail detail={{ summary: summaryCase, nodes: [], timeline: [] }} />)
    expect(screen.getByTestId('member-status-0')).toHaveTextContent('最后活跃')
    expect(screen.getByTestId('member-status-1')).toHaveTextContent('工作中')
    expect(document.body.textContent).not.toContain('在线')
    expect(document.body.textContent).not.toContain('online')
  })

  it('空座席位成员渲染「无人推」与空座标签', () => {
    const summaryCase = cases.find((c) => c.case === 'session-summary-archived-empty')!.summary!
    const withEmptySeat = { ...summaryCase, members: [{ identity: '', kind: 'seat' as const, card_id: 'B2', card_title: '空座卡', status: 'empty' as const }] }
    render(<SessionDetail detail={{ summary: withEmptySeat, nodes: [], timeline: [] }} />)
    expect(screen.getByText(/无人推/)).toBeInTheDocument()
    expect(screen.getByTestId('member-status-0')).toHaveTextContent('空座')
  })

  it('任务节点与 timeline 渲染；timeline detail 原样透传不二次解释', () => {
    const detail = cases.find((c) => c.case === 'session-detail-golden')!.detail!
    render(<SessionDetail detail={detail} />)
    expect(screen.getByTestId('session-node-0')).toHaveTextContent('implement')
    expect(screen.getByTestId('timeline-row-3')).toHaveTextContent('席位换绑')
    expect(screen.getByTestId('timeline-row-3')).toHaveTextContent('cli:grok#s2')
    expect(screen.getByTestId('timeline-row-5')).toHaveTextContent('卡收口')
  })

  it('未知 timeline kind 原样显示，不白屏', () => {
    const detail = cases.find((c) => c.case === 'session-detail-golden')!.detail!
    const rogue = { ...detail, timeline: [{ seq: 99, kind: 'weird_kind' as never, created_at: '1970-01-01T00:00:00Z' }] }
    render(<SessionDetail detail={rogue} />)
    expect(screen.getByTestId('timeline-row-0')).toHaveTextContent('weird_kind')
  })
})
```

`SessionChat.test.tsx`（全文；mock harness 照抄 RoomPanel.test.tsx 形态）：

```tsx
// SessionChat.test.tsx —— 群聊面：@高亮、引用条跳转、卡 chips 空座、发送、
// 归档只读、403 可行动报错（本卡最重岔口 1 的组件半边反例断言）。
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { ApiError } from '../../api/client'
import { sendRoomMessage } from '../../api/rooms'
import type { RoomHistoryItem, SessionSummary } from '../../api/rooms'
import { SessionChat } from './SessionChat'

vi.mock('../../api/rooms', async (importOriginal) => ({
  ...(await importOriginal<typeof import('../../api/rooms')>()),
  sendRoomMessage: vi.fn(),
}))

const summary = (over: Partial<SessionSummary> = {}): SessionSummary => ({
  id: 'session:1', kind: 'session', title: '架构物理化', owner: 'user:sy',
  archived: false, unread: 0, needs_human: false, last_activity: '2026-09-12T00:00:00Z',
  cards: [{ card_id: 'B233.14', title: '编制入站封界', status: '进行中', seat: 'cli:opencode#s1' },
          { card_id: 'B233.17', title: '组装点收窄', status: '待办' }],
  ...over,
})

const event = (seq: number, body: string, over: Partial<RoomHistoryItem> = {}): RoomHistoryItem => ({
  seq, card_id: '', type: 'room_message', actor: 'cli:opencode#s1',
  payload: { room: 'session:1', kind: 'user', body }, created_at: '2026-09-12T00:00:00Z',
  ...over,
})

beforeEach(() => {
  vi.clearAllMocks()
  vi.mocked(sendRoomMessage).mockResolvedValue({ seq: 3 })
  window.HTMLElement.prototype.scrollIntoView = vi.fn()
})

describe('SessionChat', () => {
  it('卡 chips：有座实线显席位、空座虚线显「还没配人」，点 chip 跳卡', async () => {
    const onOpenCard = vi.fn()
    const user = userEvent.setup()
    render(<SessionChat sessionId="session:1" summary={summary()} events={[]} historyError="" onSent={() => {}} onOpenCard={onOpenCard} />)
    const chips = screen.getAllByTestId('session-card-chip')
    expect(chips[0]).toHaveTextContent('cli:opencode#s1')
    expect(chips[1]).toHaveTextContent('空座 · 还没配人')
    await user.click(chips[1])
    expect(onOpenCard).toHaveBeenCalledWith('B233.17')
  })

  it('@mention 高亮与回复引用条：点引用条滚动定位被引用消息', async () => {
    const user = userEvent.setup()
    render(
      <SessionChat
        sessionId="session:1" summary={summary()}
        events={[event(41, '商定的是走分支 B', { actor: 'user:sy' }), event(42, '@B233.16 收口归你，别撞 14 的分支', { payload: { room: 'session:1', kind: 'user', body: '@B233.16 收口归你', reply_to: 41, mentions: ['B233.16'] } })]}
        historyError="" onSent={() => {}}
      />,
    )
    const at = screen.getByTestId('mention-42-0')
    expect(at).toHaveTextContent('@B233.16')
    await user.click(screen.getByTestId('quote-42'))
    expect(window.HTMLElement.prototype.scrollIntoView).toHaveBeenCalled()
    expect(screen.getByTestId('msg-41').className).toMatch(/highlight/)
  })

  it('发送：正文里的 @token 解析进 mentions（服务端据此寻址）', async () => {
    const onSent = vi.fn()
    const user = userEvent.setup()
    render(<SessionChat sessionId="session:1" summary={summary()} events={[]} historyError="" onSent={onSent} />)
    await user.type(screen.getByRole('textbox', { name: '发送消息' }), '@B233.16 收口归你')
    await user.click(screen.getByRole('button', { name: '发送' }))
    await waitFor(() => expect(sendRoomMessage).toHaveBeenCalledWith('session:1', '@B233.16 收口归你', { mentions: ['B233.16'] }))
    expect(onSent).toHaveBeenCalled()
  })

  it('发送 403 渲染可行动原文（控制台非成员——岔口 1 的组件半边）', async () => {
    vi.mocked(sendRoomMessage).mockRejectedValue(new ApiError(403, '你不是这场会话的成员'))
    const user = userEvent.setup()
    render(<SessionChat sessionId="session:1" summary={summary()} events={[]} historyError="" onSent={() => {}} />)
    await user.type(screen.getByRole('textbox', { name: '发送消息' }), 'hi')
    await user.click(screen.getByRole('button', { name: '发送' }))
    expect(await screen.findByRole('alert')).toHaveTextContent('你不是这场会话的成员')
  })

  it('归档只读：输入与发送禁用 + 只读横幅', () => {
    render(<SessionChat sessionId="session:1" summary={summary({ archived: true })} events={[]} historyError="" onSent={() => {}} />)
    expect(screen.getByRole('textbox', { name: '发送消息' })).toBeDisabled()
    expect(screen.getByText('会话已归档，只读。')).toBeInTheDocument()
  })
})
```

`SessionSidebar.test.tsx`（全文）：

```tsx
// SessionSidebar.test.tsx —— 列表行（未读角标/需要你标签/预览）与筛选。
import { describe, expect, it, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import fixture from '../../api/testdata/RoomsFixture.json'
import type { SessionSummary } from '../../api/rooms'
import { SessionSidebar } from './SessionSidebar'

const cases = fixture as { case: string; summary?: SessionSummary }[]

describe('SessionSidebar', () => {
  it('fixture 行渲染未读角标与需要你标签，点击行回调会话', async () => {
    const onOpen = vi.fn()
    const user = userEvent.setup()
    const golden = cases.find((c) => c.case === 'session-summary-golden')!.summary!
    render(<SessionSidebar sessions={[golden]} loading={false} errorText="" needsOnly={false} onToggleNeeds={() => {}} onOpen={onOpen} onCreate={() => {}} />)
    const row = screen.getByTestId('session-row')
    expect(row).toHaveTextContent('架构物理化')
    expect(screen.getByTestId('session-unread')).toHaveTextContent('2')
    expect(row).toHaveTextContent('需要你')
    await user.click(row)
    expect(onOpen).toHaveBeenCalledWith(golden)
  })

  it('needsOnly 过滤与计数；无会话空态', async () => {
    const onToggleNeeds = vi.fn()
    const user = userEvent.setup()
    const golden = cases.find((c) => c.case === 'session-summary-golden')!.summary!
    const plain = { ...golden, id: 'session:8', title: '跨机执行面', needs_human: false, unread: 0 }
    const { rerender } = render(<SessionSidebar sessions={[golden, plain]} loading={false} errorText="" needsOnly={false} onToggleNeeds={onToggleNeeds} onOpen={() => {}} onCreate={() => {}} />)
    expect(screen.getByTestId('session-total')).toHaveTextContent('2 个会话')
    await user.click(screen.getByRole('button', { name: /需要你/ }))
    expect(onToggleNeeds).toHaveBeenCalled()
    rerender(<SessionSidebar sessions={[golden, plain]} loading={false} errorText="" needsOnly onToggleNeeds={() => {}} onOpen={() => {}} onCreate={() => {}} />)
    expect(screen.getByTestId('session-total')).toHaveTextContent('1 个会话')
    expect(screen.queryByText('跨机执行面')).not.toBeInTheDocument()
    rerender(<SessionSidebar sessions={[]} loading={false} errorText="" needsOnly={false} onToggleNeeds={() => {}} onOpen={() => {}} onCreate={() => {}} />)
    expect(screen.getByText('（暂无会话）')).toBeInTheDocument()
  })
})
```

`pollInterval.test.tsx` 是**改写不是新建 harness**（全文替换，原 RoomPanel 断言对象随退役迁 SessionTab）：

```tsx
// A.6 轮询间隔默认值落常量：会话 tab 的详情与历史两流把 COLLAB_POLL_MS 传给
// usePoll，不散写魔数（B358.6：断言对象从 RoomPanel 三流迁 SessionTab 两流）。
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { render } from '@testing-library/react'
import { usePoll } from '../data/usePoll'
import { COLLAB_POLL_MS } from './constants'
import { SessionTab } from './SessionTab'

const pollState = { data: null, disconnected: false, sessionExpired: false, errorText: '', refresh: vi.fn() }

vi.mock('../data/usePoll', () => ({ usePoll: vi.fn(() => pollState) }))
vi.mock('../../api/rooms', () => ({
  fetchSessionDetail: vi.fn(),
  fetchRoomMessages: vi.fn(),
  markRoomRead: vi.fn(),
  joinSessionCard: vi.fn(),
  sendRoomMessage: vi.fn(),
}))

describe('A.6 轮询间隔常量', () => {
  beforeEach(() => vi.mocked(usePoll).mockClear())

  it('会话 tab 的详情与历史流都以 COLLAB_POLL_MS 轮询', () => {
    render(<SessionTab sessionId="session:1" title="架构物理化" />)
    const calls = vi.mocked(usePoll).mock.calls
    expect(calls.filter(([, interval]) => interval === COLLAB_POLL_MS)).toHaveLength(2)
  })
})
```

`SessionTab.test.tsx`（全文新建）：

```tsx
// SessionTab.test.tsx —— tab 窗格宿主：群聊↔详情切换、打开即已读、拉卡进群。
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { fetchRoomMessages, fetchSessionDetail, joinSessionCard, markRoomRead } from '../../api/rooms'
import type { RoomHistoryItem, SessionDetail } from '../../api/rooms'
import fixture from '../../api/testdata/RoomsFixture.json'
import { SessionTab } from './SessionTab'

vi.mock('../../api/rooms', async (importOriginal) => ({
  ...(await importOriginal<typeof import('../../api/rooms')>()),
  fetchSessionDetail: vi.fn(),
  fetchRoomMessages: vi.fn(),
  markRoomRead: vi.fn(),
  joinSessionCard: vi.fn(),
}))

const cases = fixture as { case: string; detail?: SessionDetail }
const detailOf = () => cases.find((c) => c.case === 'session-detail-golden')!.detail!
const event = (seq: number, body: string): RoomHistoryItem => ({
  seq, card_id: '', type: 'room_message', actor: 'cli:opencode#s1',
  payload: { room: 'session:1', kind: 'user', body }, created_at: '2026-09-12T00:00:00Z',
})

beforeEach(() => {
  vi.clearAllMocks()
  vi.mocked(fetchSessionDetail).mockResolvedValue(detailOf())
  vi.mocked(fetchRoomMessages).mockResolvedValue([event(7, '收到')])
  vi.mocked(markRoomRead).mockResolvedValue({ ok: true })
  vi.mocked(joinSessionCard).mockResolvedValue({ ok: true })
})

describe('SessionTab', () => {
  it('默认群聊态渲染详情头；⋯ 进详情、← 返回', async () => {
    const user = userEvent.setup()
    render(<SessionTab sessionId="session:1" title="架构物理化" />)
    expect(await screen.findByRole('textbox', { name: '发送消息' })).toBeInTheDocument()
    await user.click(screen.getByRole('button', { name: '会话详情' }))
    expect(await screen.findByLabelText('会话详情')).toBeInTheDocument()
    await user.click(screen.getByRole('button', { name: '返回群聊' }))
    expect(screen.getByRole('textbox', { name: '发送消息' })).toBeInTheDocument()
  })

  it('打开即 markRead 到历史最大 seq（沿用 markedReads 去重守卫）', async () => {
    render(<SessionTab sessionId="session:1" title="架构物理化" />)
    await waitFor(() => expect(markRoomRead).toHaveBeenCalledWith('session:1', 7))
    expect(markRoomRead).toHaveBeenCalledTimes(1)
  })

  it('＋拉卡进群：输入卡号确认后 POST …/cards', async () => {
    const user = userEvent.setup()
    render(<SessionTab sessionId="session:1" title="架构物理化" />)
    await user.click(await screen.findByRole('button', { name: '拉卡进群' }))
    await user.type(screen.getByRole('textbox', { name: '卡号' }), 'B233.14')
    await user.click(screen.getByRole('button', { name: '确认拉卡' }))
    await waitFor(() => expect(joinSessionCard).toHaveBeenCalledWith('session:1', 'B233.14'))
  })
})
```

跑红：`npx vitest run src/app/rooms` → FAIL（sessionModel/组件模块不存在；pollInterval 改写支因 SessionTab 缺失红）——红因=功能缺位。

**步骤 2（绿：最小实现）**：落七个源文件。全文如下。

`sessionModel.ts`：

```ts
// sessionModel —— 会话内容面的纯投影层：成员状态/ timeline kind 两张渲染词表、
// @分段、未读聚合。边界：不发请求、不碰 React；两张词表是「看板不说谎」
// （proto 四值词表）与「未知值不白屏」（透传兜底）的前端半边，词表漂移由
// sessionModel.test 与孪生金样本双侧拦截。
import type { SessionMember, SessionSummary } from '../../api/rooms'
import { formatRelative } from '../lib/format'

// 恰四值（proto.SessionMember* 词表）；listening 生产零载体（B358.2 澄清③），
// 词表位保留。反例断言：任何值含「在线/online」即红。
export const MEMBER_STATUS_LABEL: Readonly<Record<string, string>> = {
  working: '工作中', listening: '监听中', last_active: '最后活跃', empty: '空座',
}

export function memberStatusLabel(status: string): string {
  return MEMBER_STATUS_LABEL[status] ?? status
}

// memberStatusText 成员行的状态文案：last_active 附相对时间（可证实的读数）。
export function memberStatusText(member: SessionMember): string {
  if (member.status === 'last_active' && member.last_active) {
    return `最后活跃 ${formatRelative(member.last_active)}`
  }
  return memberStatusLabel(member.status)
}

// 恰八值（proto.SessionEvent* 词表）；未知 ledger 事件不进 timeline（collab 侧），
// 但前端仍对未知 kind 透传兜底（缺陷族 7 不白屏）。
export const TIMELINE_KIND_LABEL: Readonly<Record<string, string>> = {
  created: '会话建立', archived: '会话归档', card_joined: '卡进群', card_left: '卡移出',
  seat_bound: '协调者入座', seat_rebound: '席位换绑', needs_human: '需要你', card_closed: '卡收口',
}

export function timelineKindLabel(kind: string): string {
  return TIMELINE_KIND_LABEL[kind] ?? kind
}

export interface BodySegment { text: string; mention: boolean }

// segmentBody 按空白切 @token，与 mentions 逐字比对（token 去 @ 前缀）。
export function segmentBody(body: string, mentions?: string[]): BodySegment[] {
  const set = new Set(mentions ?? [])
  return body
    .split(/(@[^\s]+)/g)
    .filter((token) => token !== '')
    .map((token) => ({ text: token, mention: token.startsWith('@') && set.has(token.slice(1)) }))
}

// totalUnread 会话 tab 徽章的聚合读数（Σ unread）。
export function totalUnread(summaries: SessionSummary[]): number {
  return summaries.reduce((total, summary) => total + summary.unread, 0)
}

// isSelfActor 自方消息判定：控制台发言 actor 恒服务端注入 web:<host>（roomUserActor）。
export function isSelfActor(actor: string): boolean {
  return actor.startsWith('web:')
}
```

`SessionSidebar.tsx`：

```tsx
// SessionSidebar —— 左栏「会话」tab 的列表面（B361：v1 中央区第一栏整体迁入）。
// 边界：纯展示——数据由 Shell 的 usePoll 持有（徽章要跨 tab 活），本组件只渲染
// 与回调；行点击 = 开工作台 tab 的入口，新建走 Shell 的 NewSessionDialog。
import type { SessionSummary } from '../../api/rooms'
import { formatRelative } from '../lib/format'
import { totalUnread } from './sessionModel'

export interface SessionSidebarProps {
  sessions: SessionSummary[]
  loading: boolean
  errorText: string
  needsOnly: boolean
  onToggleNeeds: () => void
  onOpen: (session: SessionSummary) => void
  onCreate: () => void
}

export function SessionSidebar({ sessions, loading, errorText, needsOnly, onToggleNeeds, onOpen, onCreate }: SessionSidebarProps) {
  const visible = needsOnly ? sessions.filter((session) => session.needs_human) : sessions
  const needsCount = sessions.filter((session) => session.needs_human).length
  return (
    <div className="flex min-h-0 flex-1 flex-col" data-testid="session-list">
      {/* 未读徽章的聚合读数挂在列表头：tab 级徽章由 Shell 用同一数据另算 */}
      <div className="flex shrink-0 items-center justify-between px-3 py-2.5">
        <span className="text-sm font-semibold">会话</span>
        <button type="button" aria-label="新建会话" onClick={onCreate} className="rounded-md border px-2 py-1 text-xs hover:bg-accent">＋ 新建会话</button>
      </div>
      <div className="flex shrink-0 items-center border-b px-3 py-1.5 text-xs">
        <button type="button" aria-pressed={needsOnly} onClick={onToggleNeeds} className={needsOnly ? 'font-semibold text-amber-700' : 'text-muted-foreground'}>
          ⚑ 需要你 <span data-testid="needs-count">{needsCount}</span>
        </button>
        <span className="ml-auto text-muted-foreground" data-testid="session-total">{loading ? '读取中' : `${visible.length} 个会话`}</span>
      </div>
      {errorText !== '' && <p role="alert" className="shrink-0 border-b bg-amber-50 px-3 py-1.5 text-xs text-amber-800">会话列表已断开：{errorText}</p>}
      <div className="min-h-0 flex-1 overflow-y-auto p-1">
        {!loading && visible.length === 0 ? (
          <p className="p-2 text-sm text-muted-foreground">（暂无会话）</p>
        ) : visible.map((session) => (
          <button key={session.id} type="button" data-testid="session-row" aria-label={`会话 ${session.title}`}
            onClick={() => onOpen(session)}
            className={`flex w-full items-start gap-2 rounded-xl px-2.5 py-2 text-left transition-colors ${session.needs_human ? 'bg-amber-50 hover:bg-amber-100' : 'hover:bg-accent/60'}`}>
            <span className="relative flex size-10 shrink-0 items-center justify-center rounded-md bg-slate-200 text-[11px] font-semibold text-slate-700">
              {session.title.slice(0, 2)}
              {session.unread > 0 && <span data-testid="session-unread" className="absolute -right-1 -top-1 min-w-4 rounded-full bg-red-500 px-1 text-center text-[10px] leading-4 text-white">{session.unread}</span>}
            </span>
            <span className="min-w-0 flex-1">
              <span className="flex items-baseline gap-1.5">
                <span className={`truncate text-sm ${session.unread > 0 ? 'font-semibold' : ''}`}>{session.title}</span>
                <span className="ml-auto shrink-0 text-[10px] text-muted-foreground">{formatRelative(session.last_activity)}</span>
              </span>
              <span className="mt-0.5 flex items-center gap-1 text-xs text-muted-foreground">
                {session.needs_human && <span className="shrink-0 rounded-full border border-amber-200 bg-amber-100 px-1.5 text-[10px] font-semibold text-amber-700">需要你</span>}
                {session.archived && <span className="shrink-0 rounded-full border px-1.5 text-[10px] text-muted-foreground">已归档</span>}
                <span className="truncate">{session.preview?.body ?? '暂无预览'}</span>
              </span>
            </span>
          </button>
        ))}
      </div>
      {/* 徽章读数对账锚：单测断言 Σ unread 用 */}
      <span hidden data-testid="sidebar-unread-value">{totalUnread(sessions)}</span>
    </div>
  )
}
```

`SessionChat.tsx`：

```tsx
// SessionChat —— 会话 tab 的群聊态：人话气泡 + @高亮 + 回复引用条（只读渲染，
// 回复锚生产面缺口归 B365）+ 卡 chips（空座虚线）+ 发送。
// 边界：member 身份由服务端注入，前端不自报（请求不带身份字段）；发送被拒的
// 原文透传（403 非成员 / 409 归档）——可行动报错是岔口 1 的组件半边。
import { useState } from 'react'
import type { RoomHistoryItem, SessionSummary } from '../../api/rooms'
import { sendRoomMessage } from '../../api/rooms'
import { errorMessage } from '../lib/format'
import { logRoom } from './roomLog'
import { isSelfActor, segmentBody } from './sessionModel'

export interface SessionChatProps {
  sessionId: string
  summary: SessionSummary | null
  events: RoomHistoryItem[]
  historyError: string
  onSent: () => void
  onOpenCard?: (cardId: string) => void
}

function MessageRow({ event, referenced, highlight, onJump }: {
  event: RoomHistoryItem
  referenced: RoomHistoryItem | null
  highlight: boolean
  onJump: (seq: number) => void
}) {
  const payload = event.payload as { body?: string; mentions?: string[]; reply_to?: number }
  const self = isSelfActor(event.actor)
  const body = typeof payload.body === 'string' ? payload.body : ''
  const replyTo = typeof payload.reply_to === 'number' && payload.reply_to > 0 ? payload.reply_to : null
  return (
    <div data-msg-seq={event.seq} data-testid={`msg-${event.seq}`}
      className={`flex flex-col rounded-lg p-1 ${self ? 'items-end' : 'items-start'} ${highlight ? 'highlight bg-amber-50' : ''}`}>
      {replyTo !== null && (
        <button type="button" data-testid={`quote-${event.seq}`} onClick={() => onJump(replyTo)}
          className="mb-1 max-w-[74%] rounded border-l-2 border-border bg-black/[.02] px-2 py-0.5 text-left text-[11px] text-muted-foreground">
          ↩ {referenced ? `${referenced.actor}：${(referenced.payload as { body?: string }).body ?? ''}` : `#${replyTo}`}
        </button>
      )}
      <div className={`max-w-[74%] rounded-2xl px-3 py-2 text-sm shadow-sm ${self ? 'bg-slate-900 text-white' : 'border bg-white/65 backdrop-blur-[12px]'}`}>
        <div className={`mb-0.5 text-[10px] ${self ? 'text-white/60' : 'text-muted-foreground'}`}>{event.actor} · #{event.seq}</div>
        <p className="whitespace-pre-wrap">
          {segmentBody(body, payload.mentions).map((segment, index) => (
            segment.mention
              ? <span key={index} data-testid={`mention-${event.seq}-${index}`} className="font-semibold text-amber-600">{segment.text}</span>
              : <span key={index}>{segment.text}</span>
          ))}
        </p>
      </div>
    </div>
  )
}

export function SessionChat({ sessionId, summary, events, historyError, onSent, onOpenCard }: SessionChatProps) {
  const [draft, setDraft] = useState('')
  const [sending, setSending] = useState(false)
  const [sendError, setSendError] = useState('')
  const [highlightSeq, setHighlightSeq] = useState<number | null>(null)
  const bySeq = new Map(events.map((item) => [item.seq, item]))

  // jumpTo 引用条跳转：滚动定位 + 高亮保留下一次跳转（无定时器——无卸载清理面）。
  const jumpTo = (seq: number) => {
    document.querySelector(`[data-msg-seq="${seq}"]`)?.scrollIntoView({ block: 'center' })
    setHighlightSeq(seq)
    logRoom('debug', 'session_quote_jumped', { session: sessionId, to_seq: seq })
  }

  const send = async () => {
    const body = draft.trim()
    if (body === '' || sending) return
    setSending(true)
    setSendError('')
    const mentions = segmentBody(body).filter((segment) => segment.mention).map((segment) => segment.text.slice(1))
    logRoom('debug', 'session_send_started', { session: sessionId, mentions: mentions.length })
    try {
      await sendRoomMessage(sessionId, body, mentions.length > 0 ? { mentions } : {})
      setDraft('')
      onSent()
      logRoom('debug', 'session_send_succeeded', { session: sessionId })
    } catch (error: unknown) {
      // 403（非成员）/409（归档）等哨兵文案原文透传——ApiError 保留 agentd error 字段。
      setSendError(errorMessage(error))
      logRoom('error', 'session_send_failed', { session: sessionId, error: errorMessage(error) })
    } finally {
      setSending(false)
    }
  }

  const archived = summary?.archived === true
  return (
    <div className="flex min-h-0 flex-1 flex-col">
      <div className="shrink-0 border-b px-3 py-2 text-xs text-muted-foreground">
        {summary?.owner != null && summary.owner !== '' && <span>群主：{summary.owner} · </span>}
        {archived && <span className="font-semibold text-amber-700">会话已归档，只读。</span>}
      </div>
      {(summary?.cards ?? []).length > 0 && (
        <div className="flex shrink-0 items-center gap-1.5 overflow-x-auto border-b bg-slate-50 px-3 py-1.5">
          {summary!.cards!.map((card) => (
            <button key={card.card_id} type="button" data-testid="session-card-chip" onClick={() => onOpenCard?.(card.card_id)}
              className={`inline-flex shrink-0 items-center gap-1.5 rounded-md border bg-background px-2 py-0.5 text-xs ${card.seat ? '' : 'border-dashed'}`}>
              <b className="font-mono">{card.card_id}</b>
              {card.status && <span className="text-muted-foreground">{card.status}</span>}
              <span className={card.seat ? 'text-muted-foreground' : 'text-amber-700'}>{card.seat ?? '空座 · 还没配人'}</span>
            </button>
          ))}
        </div>
      )}
      <div className="min-h-0 flex-1 space-y-2 overflow-y-auto bg-slate-50/60 p-3">
        {events.length === 0 ? <p className="text-sm text-muted-foreground">（还没有消息）</p>
          : events.map((item) => {
            const payload = item.payload as { reply_to?: number }
            const replyTo = typeof payload.reply_to === 'number' && payload.reply_to > 0 ? payload.reply_to : null
            return <MessageRow key={item.seq} event={item} referenced={replyTo !== null ? bySeq.get(replyTo) ?? null : null} highlight={highlightSeq === item.seq} onJump={jumpTo} />
          })}
      </div>
      <footer className="shrink-0 border-t bg-background p-2.5">
        {historyError !== '' && <p role="alert" className="mb-1 text-xs text-amber-800">消息流已断开：{historyError}</p>}
        {sendError !== '' && <p role="alert" className="mb-1 text-xs text-destructive">发送被拒：{sendError}</p>}
        <div className="flex items-end gap-2 rounded-2xl border bg-background p-1.5">
          <textarea aria-label="发送消息" value={draft} onChange={(event) => setDraft(event.target.value)} disabled={archived} rows={2}
            className="min-w-0 flex-1 resize-none border-0 bg-transparent px-1.5 py-1 text-sm outline-none"
            placeholder={archived ? '' : '发消息…（要谁办就 @ 谁；没 @ 的发言不唤醒任何人）'} />
          <button type="button" aria-label="发送" onClick={() => void send()} disabled={archived || sending || draft.trim() === ''}
            className="rounded-xl bg-slate-900 px-3 py-1.5 text-xs text-white disabled:opacity-50">发送</button>
        </div>
      </footer>
    </div>
  )
}
```

`SessionDetail.tsx`：

```tsx
// SessionDetail —— 会话 tab 的详情态：成员（状态只报可证实的四值词表）、
// 协调者派发的任务节点、会话 timeline 三块。detail 载荷原样透传（可对质，
// 不二次解释）；未知 kind 原样显示不白屏（sessionModel 兜底）。
import type { SessionDetail as SessionDetailDTO } from '../../api/rooms'
import { formatRelative } from '../lib/format'
import { memberStatusText, timelineKindLabel } from './sessionModel'

export function SessionDetail({ detail }: { detail: SessionDetailDTO }) {
  const { summary, nodes = [], timeline = [] } = detail
  const members = summary.members ?? []
  return (
    <div className="min-h-0 flex-1 overflow-y-auto p-3" aria-label="会话详情">
      <section aria-label="成员" className="mb-4 rounded-xl border bg-white/65 p-3 shadow-sm">
        <h3 className="mb-2 text-xs font-semibold text-muted-foreground">成员</h3>
        {members.length === 0 ? <p className="text-xs text-muted-foreground">（暂无成员）</p> : members.map((member, index) => (
          <div key={`${member.identity}:${member.card_id ?? ''}:${index}`} className="flex items-center gap-2 border-b py-2 text-sm last:border-b-0">
            <span className="min-w-0 flex-1 truncate">
              {member.identity !== '' ? member.identity : `（${member.card_title ?? member.card_id ?? ''} 无人推）`}
              {member.kind === 'seat' && member.identity !== '' && <span className="ml-1 text-xs text-muted-foreground">· {member.card_title ?? member.card_id}</span>}
            </span>
            <span data-testid={`member-status-${index}`} className="shrink-0 text-xs text-muted-foreground">{memberStatusText(member)}</span>
          </div>
        ))}
      </section>
      <section aria-label="任务节点" className="mb-4 rounded-xl border bg-white/65 p-3 shadow-sm">
        <h3 className="mb-2 text-xs font-semibold text-muted-foreground">协调者派发的任务节点</h3>
        {nodes.length === 0 ? <p className="text-xs text-muted-foreground">（暂无派发节点）</p> : nodes.map((node, index) => (
          <div key={`${node.card_id}:${node.node}:${index}`} data-testid={`session-node-${index}`} className="flex items-center gap-2 border-b py-1.5 text-xs last:border-b-0">
            <b className="font-mono">{node.card_id}</b>
            <span>{node.node}</span>
            <span className="ml-auto text-muted-foreground">{node.state}{node.target ? ` · ${node.target}` : ''}</span>
          </div>
        ))}
      </section>
      <section aria-label="会话 timeline" className="rounded-xl border bg-white/65 p-3 shadow-sm">
        <h3 className="mb-2 text-xs font-semibold text-muted-foreground">会话 timeline（结构事件）</h3>
        {timeline.length === 0 ? <p className="text-xs text-muted-foreground">（暂无结构事件）</p> : timeline.map((row, index) => (
          <div key={row.seq} data-testid={`timeline-row-${index}`} className="flex items-baseline gap-2 py-1 text-xs">
            <span className="shrink-0 font-mono text-[10px] text-muted-foreground">{formatRelative(row.created_at)}</span>
            <span className="shrink-0 font-medium">{timelineKindLabel(row.kind)}</span>
            {row.card_id && <b className="shrink-0 font-mono">{row.card_id}</b>}
            {row.detail && <span className="min-w-0 flex-1 truncate text-muted-foreground" title={row.detail}>{row.detail}</span>}
            {row.actor && <span className="shrink-0 text-muted-foreground">· 由 {row.actor}</span>}
          </div>
        ))}
      </section>
    </div>
  )
}
```

`JoinCardDialog.tsx`：

```tsx
// JoinCardDialog —— 拉卡进群的极简对话框（卡号手输；进群 ≠ 配人，配人仍走 B307）。
import { useState } from 'react'

export function JoinCardDialog({ open, busy, error, onCancel, onJoin }: {
  open: boolean; busy: boolean; error: string
  onCancel: () => void
  onJoin: (cardId: string) => void
}) {
  const [cardId, setCardId] = useState('')
  if (!open) return null
  return (
    <div role="dialog" aria-label="拉卡进群" className="fixed inset-0 z-50 flex items-center justify-center bg-black/40">
      <div className="w-72 rounded-xl border bg-background p-4 shadow-xl">
        <h3 className="text-sm font-semibold">拉卡进群</h3>
        <p className="mt-1 text-xs text-muted-foreground">进群只建讨论面；配不配人仍在卡上按三颗按钮。</p>
        <input aria-label="卡号" value={cardId} onChange={(event) => setCardId(event.target.value)} placeholder="B233.14"
          className="mt-3 w-full rounded-md border px-2 py-1 font-mono text-sm" />
        {error !== '' && <p role="alert" className="mt-2 text-xs text-destructive">{error}</p>}
        <div className="mt-4 flex justify-end gap-2">
          <button type="button" onClick={() => { setCardId(''); onCancel() }} className="rounded-md border px-2.5 py-1 text-xs">取消</button>
          <button type="button" aria-label="确认拉卡" disabled={cardId.trim() === '' || busy}
            onClick={() => onJoin(cardId.trim())}
            className="rounded-md bg-slate-900 px-2.5 py-1 text-xs text-white disabled:opacity-50">确认拉卡</button>
        </div>
      </div>
    </div>
  )
}
```

`NewSessionDialog.tsx`：

```tsx
// NewSessionDialog —— 新建会话对话框。owner 用统一记法 user:<name>/agent:<name>
// （B358.4 拍板①：成员身份不收机器位 web:）；客户端前缀预检只为快速失败，
// 服务端校验是权威（invalidSessionOwner 400 原文透传）。控制台成员身份来源
// 归岔口 2（推荐=对话框显式输入）。
import { useState } from 'react'

export function NewSessionDialog({ open, busy, error, onCancel, onCreate }: {
  open: boolean; busy: boolean; error: string
  onCancel: () => void
  onCreate: (title: string, owner: string) => void
}) {
  const [title, setTitle] = useState('')
  const [owner, setOwner] = useState('')
  if (!open) return null
  const ownerOk = /^(user|agent):.+$/.test(owner.trim())
  return (
    <div role="dialog" aria-label="新建会话" className="fixed inset-0 z-50 flex items-center justify-center bg-black/40">
      <div className="w-80 rounded-xl border bg-background p-4 shadow-xl">
        <h3 className="text-sm font-semibold">新建会话</h3>
        <label className="mt-3 block text-xs text-muted-foreground" htmlFor="new-session-title">标题</label>
        <input id="new-session-title" aria-label="会话标题" value={title} onChange={(event) => setTitle(event.target.value)}
          className="mt-1 w-full rounded-md border px-2 py-1 text-sm" />
        <label className="mt-3 block text-xs text-muted-foreground" htmlFor="new-session-owner">群主身份（统一记法）</label>
        <input id="new-session-owner" aria-label="群主身份" value={owner} onChange={(event) => setOwner(event.target.value)}
          placeholder="user:sy" className="mt-1 w-full rounded-md border px-2 py-1 font-mono text-sm" />
        {!ownerOk && owner.trim() !== '' && <p className="mt-1 text-xs text-amber-700">须形如 user:<名字> 或 agent:<名字></p>}
        {error !== '' && <p role="alert" className="mt-2 text-xs text-destructive">{error}</p>}
        <div className="mt-4 flex justify-end gap-2">
          <button type="button" onClick={() => { setTitle(''); setOwner(''); onCancel() }} className="rounded-md border px-2.5 py-1 text-xs">取消</button>
          <button type="button" disabled={title.trim() === '' || !ownerOk || busy}
            onClick={() => onCreate(title.trim(), owner.trim())}
            className="rounded-md bg-slate-900 px-2.5 py-1 text-xs text-white disabled:opacity-50">创建</button>
        </div>
      </div>
    </div>
  )
}
```

`SessionTab.tsx`：

```tsx
// SessionTab —— 会话的工作台 tab 窗格宿主（B361：群聊↔详情切换留在 tab 内部）。
// 职责：详情+历史两路轮询、打开即已读（markedReads 去重守卫，沿用 RoomPanel 既有
// 模式）、拉卡进群对话框、⋯/← 切换。边界：不发身份字段；已读失败只告警不阻塞渲染。
import { useEffect, useRef, useState } from 'react'
import { fetchRoomMessages, fetchSessionDetail, joinSessionCard, markRoomRead } from '../../api/rooms'
import type { SessionDetail as SessionDetailDTO } from '../../api/rooms'
import { errorMessage } from '../lib/format'
import { usePoll } from '../data/usePoll'
import { COLLAB_POLL_MS } from './constants'
import { logRoom } from './roomLog'
import { JoinCardDialog } from './JoinCardDialog'
import { SessionChat } from './SessionChat'
import { SessionDetail } from './SessionDetail'

const HISTORY_LIMIT = 200

export function SessionTab({ sessionId, title, onOpenCard }: {
  sessionId: string
  title: string
  onOpenCard?: (cardId: string) => void
}) {
  const [view, setView] = useState<'chat' | 'detail'>('chat')
  const [joinOpen, setJoinOpen] = useState(false)
  const [joinBusy, setJoinBusy] = useState(false)
  const [joinError, setJoinError] = useState('')
  const markedReads = useRef<Record<string, number>>({})

  const detailPoll = usePoll(() => fetchSessionDetail(sessionId), COLLAB_POLL_MS)
  const historyPoll = usePoll(() => fetchRoomMessages(sessionId, { limit: HISTORY_LIMIT }), COLLAB_POLL_MS)
  const detail: SessionDetailDTO | null = detailPoll.data
  const history = historyPoll.data ?? []
  const maxSeq = history.reduce((max, item) => Math.max(max, item.seq), 0)

  // 打开即已读（spec §7）：seq 水位去重，失败从水位表剔除让下一轮重试。
  useEffect(() => {
    if (view !== 'chat' || detailPoll.data === null || maxSeq <= 0 || maxSeq <= (markedReads.current[sessionId] ?? 0)) return
    markedReads.current[sessionId] = maxSeq
    logRoom('debug', 'session_mark_read_started', { session: sessionId, upto_seq: maxSeq })
    void markRoomRead(sessionId, maxSeq).catch((error: unknown) => {
      delete markedReads.current[sessionId]
      logRoom('error', 'session_mark_read_failed', { session: sessionId, error: errorMessage(error) })
    })
  }, [view, sessionId, maxSeq, detailPoll.data])

  const joinCard = async (cardId: string) => {
    setJoinBusy(true)
    setJoinError('')
    logRoom('debug', 'session_join_card_started', { session: sessionId, card: cardId })
    try {
      await joinSessionCard(sessionId, cardId)
      setJoinOpen(false)
      detailPoll.refresh()
      logRoom('debug', 'session_join_card_succeeded', { session: sessionId, card: cardId })
    } catch (error: unknown) {
      setJoinError(errorMessage(error))
      logRoom('error', 'session_join_card_failed', { session: sessionId, card: cardId, error: errorMessage(error) })
    } finally {
      setJoinBusy(false)
    }
  }

  return (
    <div className="flex h-full min-h-0 flex-col">
      <header className="flex shrink-0 items-center gap-2 border-b px-3 py-2">
        <span className="min-w-0 flex-1 truncate text-sm font-semibold">{title}</span>
        <button type="button" aria-label="拉卡进群" onClick={() => setJoinOpen(true)} className="rounded-md border px-2 py-1 text-xs hover:bg-accent">＋ 拉卡进群</button>
        {view === 'chat' ? (
          <button type="button" aria-label="会话详情" onClick={() => setView('detail')} className="rounded-md px-2 py-1 text-xs hover:bg-accent">⋯</button>
        ) : (
          <button type="button" aria-label="返回群聊" onClick={() => setView('chat')} className="rounded-md px-2 py-1 text-sm hover:bg-accent">←</button>
        )}
      </header>
      {view === 'chat' ? (
        <SessionChat sessionId={sessionId} summary={detail?.summary ?? null} events={history}
          historyError={historyPoll.disconnected ? historyPoll.errorText : ''} onSent={() => historyPoll.refresh()}
          onOpenCard={onOpenCard} />
      ) : detail === null ? (
        <p className="p-4 text-sm text-muted-foreground">{detailPoll.disconnected ? `详情读取失败：${detailPoll.errorText}` : '正在读取…'}</p>
      ) : (
        <SessionDetail detail={detail} />
      )}
      <JoinCardDialog open={joinOpen} busy={joinBusy} error={joinError} onCancel={() => setJoinOpen(false)} onJoin={(cardId) => void joinCard(cardId)} />
    </div>
  )
}
```

**步骤 3（绿）**：

```
$ cd web && npx vitest run src/app/rooms  → 退出 0（新族全绿）
$ cd web && npm run typecheck  → 退出 0
```

### Task 3 验收

1. `npx vitest run src/app/rooms` 退出 0——组件测试至少覆盖：成员状态四值渲染 + 全 DOM 无在线字样（反例）；空座 chips 虚线 + 「还没配人」；@高亮与引用条跳转（scrollIntoView 被调 + 目标 highlight 类）；发送把 @token 解析进 mentions；403 原文透传；归档只读禁用；timeline 未知 kind 原样显示；需要你筛选；打开即已读水位；拉卡 POST。
2. `pollInterval.test.tsx` 断言对象已迁 SessionTab（两流 × COLLAB_POLL_MS），A.6 纪律存活。
3. `npm run typecheck` 退出 0。
4. 新组件全部经 roomLog 记入口/成功/失败分支（结构化 console，禁裸 print——代码已含）；组件头注释职责+边界已给。

## 7. Task 4：Shell 接线 + 旧 RoomPanel 退役（Shell.tsx / Shell.test.tsx / WorkbenchPage.tsx ◫，红→绿）

### 文件范围

`web/src/app/shell/Shell.tsx`、`web/src/app/shell/Shell.test.tsx`、`web/src/app/workbench/WorkbenchPage.tsx`（仅 ◫ 按钮 + prop，C 组）、删除 `web/src/app/rooms/{RoomPanel.tsx,RoomPanel.test.tsx,roomPanelModel.ts,useRoomPanelGeom.ts}`。

### Interfaces

**Consumes（Task 2/3 Produces，逐字）**：

```ts
// ../rooms
SessionSidebar(props: SessionSidebarProps)      // sessions/loading/errorText/needsOnly/onToggleNeeds/onOpen/onCreate
SessionTab(props: { sessionId: string; title: string; onOpenCard?: (cardId: string) => void })
NewSessionDialog(props: { open: boolean; busy: boolean; error: string; onCancel: () => void; onCreate: (title: string, owner: string) => void })
totalUnread(summaries: SessionSummary[]): number
fetchSessions(): Promise<SessionSummary[]>; createSession(title: string, owner: string): Promise<Session>
// ../workbench
sessionBase(sessionId: string): BaseDir
TabContent 新分支 { kind: 'session'; sessionId: string; title: string }
```

**Produces**：Shell 内 `openSession`/`splitSessionRight`/`confirmCreateSession`（内部函数，recon 入图）；WorkbenchPageProps +`onSplitSessions?: () => void`。

### 步骤

**步骤 1（WorkbenchPage ◫，C 组最小增量）**：`WorkbenchPageProps` 追加一行：

```ts
  // onSplitSessions 由 Shell 注入（B361 决定 5：tabbar + ◫ 固定分屏开关）。
  // 不传不渲染——既有测试与调用面零波及。焦点是会话 tab 或无会话 tab 时由
  // Shell 侧 no-op，按钮置灰由 disabled 判定（hasSessionTab 本地可算）。
  onSplitSessions?: () => void
```

函数签名解构追加 `onSplitSessions`；`return (` 的 `<TabBar … />` 包一层行容器并在右侧渲染按钮：

```tsx
      <div className="flex min-h-0 items-stretch">
        <div className="min-w-0 flex-1">
          <TabBar
            groups={wb.groups}
            activeGroupId={wb.activeGroupId}
            base={base}
            taskName={taskName}
            onActivateGroup={api.activateGroup}
            onCloseGroup={api.closeGroup}
            onNew={openNew}
            onNewLauncher={openLauncher}
            launchers={launcherItems}
            terminalUnavailable={terminalUnavailable}
            onNewGroup={api.addGroup}
            onMoveGroup={moveGroup}
          />
        </div>
        {onSplitSessions && (
          <button type="button" data-testid="split-sessions" aria-label="分屏：工作项在左、会话在右"
            disabled={!wb.groups.some((group) => group.columns.some((column) => column.panes.some((tab) => tab?.content.kind === 'session')))}
            onClick={onSplitSessions}
            className="mx-2 shrink-0 self-center rounded-md border px-2 py-1 text-xs hover:bg-accent disabled:cursor-not-allowed disabled:opacity-45">
            ◫ 分屏
          </button>
        )}
      </div>
```

**步骤 2（红）**：Shell.test.tsx——`vi.mock('../../api/rooms')` 的 mock 对象追加 `fetchSessions: vi.fn().mockResolvedValue([])`、`createSession: vi.fn()`；删除 describe `统一房间面板挂载` 全节（5 支），原位替换为（完整代码，直接落）：

```tsx
// —— B361 会话 IA（B358.6）：左栏两 tab + 会话工作台 tab + 移除反例断言 ——
const sessionSummary = (over: Record<string, unknown> = {}) => ({
  id: 'session:1', kind: 'session', title: '架构物理化', owner: 'user:sy',
  archived: false, unread: 0, needs_human: false, last_activity: '2026-09-12T00:00:00Z',
  ...over,
})

describe('B361 会话 IA', () => {
  it('左栏两 tab 点击切换：会话 tab 显列表（默认），任务 tab 显项目树且双挂载不卸载', async () => {
    renderShell()
    expect(await screen.findByTestId('session-list')).toBeInTheDocument()
    fireEvent.click(screen.getByRole('tab', { name: /任务/ }))
    expect(await screen.findByText('handoff')).toBeInTheDocument()
    expect(screen.getByTestId('session-list').closest('[hidden]')).not.toBeNull()
    fireEvent.click(screen.getByRole('tab', { name: /会话/ }))
    expect(screen.getByTestId('session-list').closest('[hidden]')).toBeNull()
    expect(screen.getByTestId('sidebar-unread')).toBeInTheDocument()
  })

  it('账本关闭时不渲染会话 tab（与旧房间面同门控）', async () => {
    vi.mocked(fetchLedgerHealth).mockResolvedValueOnce({ enabled: false, mirror: [] })
    renderShell()
    await waitFor(() => expect(screen.queryByRole('tab', { name: /会话/ })).toBeNull())
  })

  it('会话行开成工作台 tab（组标签=会话 · 标题），可多开；面包屑跟会话 tab', async () => {
    vi.mocked(fetchSessions).mockResolvedValue([
      sessionSummary(),
      sessionSummary({ id: 'session:2', title: '桌面端体验' }),
    ] as never)
    const user = userEvent.setup()
    renderShell()
    await user.click(await screen.findByTestId('session-row'))
    expect(await screen.findByRole('tab', { name: /架构物理化/ })).toBeInTheDocument()
    await user.click(screen.getAllByTestId('session-row')[1])
    expect(screen.getByRole('tab', { name: /桌面端体验/ })).toBeInTheDocument()
    // 同一会话重复点击只聚焦不开新 tab（dedupKey 全局去重）
    await user.click(screen.getAllByTestId('session-row')[0])
    expect(screen.getAllByRole('tab', { name: /架构物理化/ })).toHaveLength(1)
    // 面包屑随工作台外壳恢复：焦点是会话 tab 时第三段=会话标题
    expect(screen.getByLabelText('当前位置')).toHaveTextContent('架构物理化')
  })

  it('◫ 分屏把最近打开的会话 tab 放到焦点工作 tab 右列（工作项左/会话右）', async () => {
    vi.mocked(fetchSessions).mockResolvedValue([sessionSummary()] as never)
    const user = userEvent.setup()
    renderShell()
    await openBranch()
    fireEvent.click(await screen.findByText('go.mod'))
    await waitFor(() => expect(screen.getAllByRole('button', { name: /关闭 go.mod/ }).length).toBeGreaterThan(0))
    await user.click(await screen.findByTestId('session-row'))
    expect(await screen.findByRole('tab', { name: /架构物理化/ })).toBeInTheDocument()
    // 开会话 tab 后焦点在会话组；经左栏已打开行回聚焦工作 tab（OpenItem 投影是既有锚）
    fireEvent.click(screen.getAllByTestId('open-item-row').find((row) => row.textContent?.includes('go.mod'))!)
    await waitFor(() => expect(screen.getByRole('tab', { name: /go.mod/ })).toHaveAttribute('aria-selected', 'true'))
    const before = screen.getAllByTestId('workbench-pane').length
    fireEvent.click(screen.getByTestId('split-sessions'))
    await waitFor(() => expect(screen.getAllByTestId('workbench-pane')).toHaveLength(before + 1))
  })

  it('反例断言：旧房间面板任何形态都不再出现', async () => {
    renderShell('/cards')
    await screen.findByTestId('session-list')
    expect(screen.queryByTestId('room-panel')).toBeNull()
    expect(screen.queryByRole('button', { name: '打开房间面板' })).toBeNull()
    expect(screen.queryByTestId('room-panel-corner')).toBeNull()
    renderShell('/settings')
    expect(screen.queryByRole('button', { name: '打开房间面板' })).toBeNull()
  })

  it('新建会话：对话框收集标题与统一记法群主，createSession 后刷新列表', async () => {
    const { createSession } = await import('../../api/rooms')
    vi.mocked(createSession).mockResolvedValue({ id: 'session:9', title: '新场', owner: 'user:sy', archived: false, created_at: '', updated_at: '' })
    vi.mocked(fetchSessions).mockResolvedValue([] as never)
    const user = userEvent.setup()
    renderShell()
    await user.click(await screen.findByRole('button', { name: '新建会话' }))
    await user.type(screen.getByRole('textbox', { name: '会话标题' }), '新场')
    await user.type(screen.getByRole('textbox', { name: '群主身份' }), 'user:sy')
    await user.click(screen.getByRole('button', { name: '创建' }))
    await waitFor(() => expect(createSession).toHaveBeenCalledWith('新场', 'user:sy'))
  })

  it('关闭会话 tab（组关闭）后 tabbar 不再含该会话', async () => {
    vi.mocked(fetchSessions).mockResolvedValue([sessionSummary()] as never)
    const user = userEvent.setup()
    renderShell()
    await user.click(await screen.findByTestId('session-row'))
    expect(await screen.findByRole('tab', { name: /架构物理化/ })).toBeInTheDocument()
    fireEvent.click(screen.getByRole('button', { name: /关闭 会话 · 架构物理化/ }))
    await waitFor(() => expect(screen.queryByRole('tab', { name: /架构物理化/ })).toBeNull())
  })
})
```

（`userEvent` 已在该文件 import；组关闭钮的可达名以 TabBar 实现为准——执行者若发现关闭钮 aria-label 形如「关闭组 <名>」，把用例里的 name 正则对齐实现并在断言注释记一句读数，不改组件。）

跑红：`npx vitest run src/app/shell/Shell.test.tsx` → FAIL——`session-list`/`sidebar-unread` 缺位、`split-sessions` 缺位、部分反例支（room-panel 仍挂）红。

**步骤 3（绿：Shell.tsx 改写）**：

1. 删 `:60` 的 `import { RoomPanel } from '../rooms/RoomPanel'` 与 `:808` 挂载行。
2. 追加 import：

```ts
import { fetchSessions, createSession } from '../../api/rooms'
import type { SessionSummary } from '../../api/rooms'
import { NewSessionDialog } from '../rooms/NewSessionDialog'
import { SessionSidebar } from '../rooms/SessionSidebar'
import { SessionTab } from '../rooms/SessionTab'
import { totalUnread } from '../rooms/sessionModel'
import { sessionBase } from '../workbench/useWorkbench'
import { COLLAB_POLL_MS } from '../rooms/constants'
```

3. 组件体追加（锚：`usePoll` 簇附近，`cardsState` 之后）：

```ts
  // 会话流（B361）：徽章要在任务 tab 激活时也活着，数据源在 Shell 持有，
  // 列表组件只渲染。未启用账本时轮询关闭（与旧 RoomPanel 同门控）。
  const sessionsState = usePoll(fetchSessions, COLLAB_POLL_MS, { enabled: ledgerEnabled })
  const sessions = useMemo(() => sessionsState.data ?? [], [sessionsState.data])
  const [sidebarTab, setSidebarTab] = useState<'sessions' | 'tasks'>('sessions')
  const [createOpen, setCreateOpen] = useState(false)
  const [createBusy, setCreateBusy] = useState(false)
  const [createError, setCreateError] = useState('')

  // openSession 会话行的唯一入口：开工作台 tab（openOrFocus 全局去重——重复
  // 点击聚焦原 tab）；先回工作台（/cards 等整页盖上时会话 tab 看不见）。
  const openSession = (session: SessionSummary) => {
    backToWorkbench()
    wb.openOrFocus({ kind: 'session', sessionId: session.id, title: session.title }, sessionBase(session.id))
    console.debug('shell.session.open', { sessionId: session.id, title: session.title })
  }

  // splitSessionRight ◫ 固定分屏（B361 决定 5）：把最近打开的会话 tab place 到
  // 焦点格右列（工作项左/会话右）。焦点是会话 tab 或无会话 tab 时 no-op——
  // 按钮同时由 WorkbenchPage 依 hasSessionTab 置灰，这里再守一道语义边界。
  const splitSessionRight = () => {
    const group = wb.wb.groups.find((candidate) => candidate.id === wb.wb.activeGroupId)
    if (!group) return
    const [column, row] = group.focus
    const focused = group.columns[column]?.panes[row]
    if (!focused || focused.content.kind === 'session') return
    const sessionItem = [...wb.openedItems].reverse().find((item) => item.content.kind === 'session')
    if (!sessionItem) return
    wb.place(
      { kind: 'tab', groupId: sessionItem.groupId, tabId: sessionItem.tabId },
      { groupId: group.id, column, row, zone: 'right' },
    )
    console.debug('shell.session.split', { sessionId: sessionItem.content.kind === 'session' ? sessionItem.content.sessionId : '', groupId: group.id })
  }

  // confirmCreateSession 建会话：owner 统一记法（服务端权威校验），失败原文
  // 留在对话框；成功关弹层并立即刷新会话流（不等下一个 5s 周期）。
  const confirmCreateSession = async (title: string, owner: string) => {
    setCreateBusy(true)
    setCreateError('')
    console.debug('shell.session.create_started', { title })
    try {
      const session = await createSession(title, owner)
      setCreateOpen(false)
      sessionsState.refresh()
      console.debug('shell.session.created', { sessionId: session.id, title, owner })
    } catch (error: unknown) {
      setCreateError(errorMessage(error))
      console.warn('shell.session.create_failed', { title, error: errorMessage(error) })
    } finally {
      setCreateBusy(false)
    }
  }
```

4. 左栏改写（锚：`<ResizableSidebar>` 内、既有 banner 块之后）。`{treeState.data && (<ProjectTree … />)}` 原样搬进任务 pane 的 div（props **零改动**）；会话 pane 与 tab 行新增。形状（ProjectTree props 以 `…` 指认「逐字保留现文件 :648-687 的全部 props」——这是占位符扫描的第二个正当出口声明：搬移不改内容，逐字对象 = Shell.tsx 现文件本体）：

```tsx
      <ResizableSidebar>
        {/* 既有三个 banner 逐字保留（treeState.sessionExpired / disconnected / sync.error） */}
        <div className="flex shrink-0 border-b" role="tablist" aria-label="左栏视图">
          {ledgerEnabled && (
            <button type="button" role="tab" aria-selected={sidebarTab === 'sessions'} data-testid="sidebar-tab-sessions"
              onClick={() => setSidebarTab('sessions')}
              className={`flex flex-1 items-center justify-center gap-1.5 py-2 text-[13px] ${sidebarTab === 'sessions' ? 'border-b-2 border-primary font-semibold' : 'text-muted-foreground'}`}>
              会话
              {totalUnread(sessions) > 0 && (
                <span data-testid="sidebar-unread" className="min-w-4 rounded-full bg-red-500 px-1 text-center text-[10px] leading-4 text-white">{totalUnread(sessions)}</span>
              )}
            </button>
          )}
          <button type="button" role="tab" aria-selected={sidebarTab === 'tasks'} data-testid="sidebar-tab-tasks"
            onClick={() => setSidebarTab('tasks')}
            className={`flex flex-1 items-center justify-center py-2 text-[13px] ${sidebarTab === 'tasks' ? 'border-b-2 border-primary font-semibold text-foreground' : 'text-muted-foreground'}`}>
            任务
          </button>
        </div>
        {ledgerEnabled && (
          <div hidden={sidebarTab !== 'sessions'} className="flex min-h-0 flex-1 flex-col">
            <SessionSidebar sessions={sessions} loading={sessionsState.data === null && !sessionsState.disconnected}
              errorText={sessionsState.disconnected ? sessionsState.errorText : ''}
              needsOnly={needsOnly}
              onToggleNeeds={() => setNeedsOnly((current) => !current)}
              onOpen={openSession}
              onCreate={() => { setCreateError(''); setCreateOpen(true) }} />
          </div>
        )}
        {/* 账本未启用时会话 tab 不渲染，任务 pane 兜底可见（hidden 恒 false） */}
        <div hidden={ledgerEnabled && sidebarTab !== 'tasks'} className="flex min-h-0 flex-1 flex-col">
          {treeState.data && (
            <ProjectTree … props 逐字保留现文件： />
          )}
        </div>
      </ResizableSidebar>
```

`needsOnly` 由 Shell 持有：`const [needsOnly, setNeedsOnly] = useState(false)`（与筛选钮同层）。

5. renderContent switch 追加（锚：`case 'tui'` 之前）：

```ts
                  case 'session':
                    return (
                      <SessionTab
                        sessionId={c.sessionId}
                        title={c.title}
                        onOpenCard={(cardId) => navigate(`/cards?card=${encodeURIComponent(cardId)}`)}
                      />
                    )
```

6. WorkbenchPage 调用处追加 prop（锚：`<WorkbenchPage` 的 props 列表）：

```tsx
              onSplitSessions={splitSessionRight}
```

7. 弹层区（锚：`<AddProjectWizard` 之前）追加：

```tsx
      <NewSessionDialog open={createOpen} busy={createBusy} error={createError}
        onCancel={() => setCreateOpen(false)} onCreate={(title, owner) => void confirmCreateSession(title, owner)} />
```

**步骤 4（退役删除）**：`git rm` 语义删除四文件（执行者直接删）：`web/src/app/rooms/RoomPanel.tsx`、`RoomPanel.test.tsx`、`roomPanelModel.ts`、`useRoomPanelGeom.ts`。删除后 `grep -rn "RoomPanel\|roomPanelModel\|useRoomPanelGeom" web/src` **零命中**（孤儿引用禁令）。

**步骤 5（绿）**：

```
$ cd web && npx vitest run  → 退出 0（web 全量）
$ cd web && npm run typecheck  → 退出 0
$ cd web && npm run lint  → 退出 0
```

### Task 4 验收

1. Shell 结构断言（B361 IA）：左栏两 tab 切换（双挂载 hidden）、默认会话 tab、`sidebar-unread` 徽章在、账本关闭隐藏会话 tab、会话行开 tab + 多开 + 去重、面包屑跟会话 tab、◫ 分屏窗格 +1、组关闭收 tab、新建会话 POST、**反例断言**（room-panel/悬浮球/角点任何路由形态零出现）。
2. `npx vitest run`（web 全量）退出 0——RoomPanel 套件随退役消失，无孤儿测试；既有 Shell 用例（三栏/拖放/路由）零改动全绿。
3. `npm run typecheck`、`npm run lint` 退出 0。
4. `grep -rn "RoomPanel" web/src` 零命中；`grep -rn "online\|在线" web/src/app/rooms` 仅命中 sessionModel.test/sessionModel 的反例断言行（词表红线不在渲染路径）。

## 8. Task 5：真机移交清单（归协调者，不派发）

**本 task 由协调者执行，不派发**（真机 #7，breakdown §5）。机内可验部分已在 Task 1–4 收口；以下是必须在真实浏览器（Wails/Chromium）走查、对照 `sessions-v2.html` 的移交项：

1. **W1 占位（B361 对照项）**：左栏「会话 | 任务」两 tab 切换手感；会话 tab 徽章跨 tab 存活；项目树在任务 tab 原样（展开态/搜索/机器行动作）。
2. **W2 群聊干净度**：tab 窗格内群聊与 v1 观感一致（宿主换了、内容没换）；发送一条无 @ 消息，成员未读 +1、零唤醒。
3. **W3 @ 与引用**：`@卡号` 高亮、引用条点击跳转定位；偏差注记（§2.1.6）三条向走查人说明。
4. **W4 详情三块密度**：tab 窗格宽度下成员/节点/timeline 是否站得住。
5. **W5 卡条**：空座虚线 + 「还没配人」；chip 点击跳 `/cards?card=`。
6. **W6 分屏与多开**：◫ 工作项左/会话右；开第二个会话 tab 验多开；✕ 验可关。
7. **发言权现场核对（岔口 1）**：控制台对会话发言的 403 现场文案是否可行动；按 B358.4 拍板②的核对义务裁决补员路径（新卡或接受只读）。

## 9. Task 6：收口核算（不写码）

### 文件范围

无代码改动；读数与清单落台账。

### 步骤

1. **三段律全量**：`cd web && npx vitest run` 退出 0；`npm run typecheck`、`npm run lint` 退出 0；`go build ./...` 退出 0（Go 侧零改动确认）。
2. **文件集核对**：`git status --short` 受控 = §0.1 A/B/C 组 + 本计划 + 台账；无关脏文件（含 `web/design-qa*`）不在列。
3. **图符号清单**：新增（rooms.ts 会话类型与六函数、sessionModel 七导出、六组件、tabs.ts/persist.ts/useWorkbench.ts 增量、Shell 三内部函数、全部新测试符号）与修改/删除（RoomPanel 四文件、Shell 挂载行、pollInterval 改写）逐符号落台账，交 integrate/recon 补视图 diff；收尾跑 `codegraph resolve --doc docs/superpowers/plans/b358.6-plan.md` 修坏锚。
4. **B361 对账**：`prototypes/base/README.md` B361 行状态仍「确认中」——推进「已确认」归真机走查通过后（B361 §3），本卡不动该文件（越界）。

### Task 6 验收

§13 收口判据全部成立；台账含 Task 0 读数、变异自查记录、图符号清单。

## 10. 明确不做的改动（越界即回退）

1. **不动 `internal/**`**——六端点与发言/已读/历史复用面 B358.4 已收口；不新增 gateway 端点（含 AddSessionMember——岔口 1 走待拍板，不在本卡边加）。
2. **不做回复编辑框**——`handleRoomSend` 不收 reply_to（0.2#5），缺口归 B365；引用条只读渲染。
3. **不做会话归档/移卡的控制台入口**——`archiveSession`/`leaveSessionCard` 仅为六端点镜像保留（fetch 契约测试是消费方）；操作面在 CLI（B358.5）。
4. **不动 ProjectTree**（任务 tab 原样）、**不动 TabBar.tsx**、**不动 useWorkbenchSync/restore**、**不动 homedock/**（§0.1 落空声明）。
5. **不加 npm 依赖、不引入 websocket**（REST 轮询沿用 COLLAB_POLL_MS——B358.2 拍板 5.3）。
6. **不动 wire DTO**——TS 类型与 proto 键集逐值镜像；改形状先回 contract。
7. **不动 `prototypes/**`** 与 `web/design-qa*`（别卡遗留）。
8. **不做历史翻页/富文本**（OOS 已登记）——历史固定 `limit: 200`（与旧面板 HISTORY_LIMIT 同值）。

## 11. 序列化边界与接缝覆盖

### 序列化边界设问

本卡主战场（breakdown 缺陷族 6）：Go DTO → HTTP JSON → TS interface → 组件 props 的跨语言整链。锁法三层：① `rooms.test.ts` 孪生金样本——fixture JSON 与 Go 编码器逐键同源（同一 JSON 文件，值取自 `sessions_fixture_test.go`，Task 1 验收 1）；② `rooms.fetch.test.ts`——解码穿真实 fetch stub → `request()` 解析（不是本地对象自查），含 omitempty 缺失≠零值分辨（members 缺键=undefined、seat 缺键=undefined、reply_to 缺键=undefined 非 0）；③ `SessionDetail.test`/`SessionChat.test` 组件夹具**直接 import 同一 fixture** 渲染——props 链路上没有第二份手抄形状。roundtrip 属性测试不另设：TS 侧是只读消费方（无 encode），wire 编码由 Go 金样本锁。

### 接缝覆盖（spec §6 缝清单，双向）

**缝 #4（控制台契约层缝：`web/src/api/rooms.ts`，调用方 = 会话面板三态与详情页）**：

- **测试 → 缝**：Task 1 全部测试入口符号 = `fetchSessions`/`fetchSessionDetail`/`createSession`/`archiveSession`/`joinSessionCard`/`leaveSessionCard` 与孪生金样本（都在缝上）；Task 3 组件测试入口 = 组件 props，其数据源类型全部产自该缝（调用链穿过缝）。
- **缝 → 测试**：缝的每个新导出至少一支缝级断言——六函数（fetch 契约 4 支）、`reply_to` 类型（三态支）、成员状态词表（词表闭包支）、timeline 词表（对齐支）。

**Shell 结构断言（breakdown §3.7 ③ 的挂载面）**：Shell.test 的 B361 describe——入口是真实 mux 之上的组件树（AppRoutes→Shell），RoomPanel 移除反例与 IA 断言都从渲染树出发，非内部锁。

**内部锁声明（2 支，附加不顶替）**：① `sessionModel.test.ts` 的词表支与 ② `tabs.test.ts`/`persist.test.ts` 的模型支，入口是纯函数直调、不在缝 #4 的 HTTP 入口上。合法理由：**从声明缝构造不出这条断言**——「词表四值闭包 + online 反例」是渲染层映射函数的承重属性（变异=往标签表加键），HTTP 缝上只能锁 wire 值域（Task 1 已锁）锁不了「渲染层不发明翻译」；「parseContent 拒收未知 kind 整份丢布局」是持久化白名单的局部行为，从 HTTP 缝要造「含 session tab 的历史 payload」必须先跑一轮真实写盘恢复，构造成本不成比例且断言等价。两支均为附加（缝级断言在 Task 1/4 不缺位），不顶替。

**缝 #1/#2/#3/#5**：分属 S1/S3/S3/S2（均已收口）——本卡是缝 #4 的消费侧，不新增对其余缝的断言、不触碰其文件。

### 退路同闸

无条件退路。唯一现场裁量点（Task 4 步骤 2 的组关闭钮可达名以 TabBar 实现为准）只改测试断言字符串、不改组件、不改变测试入口符号（仍是渲染树），不构成未声明内部锁。

## 12. 缺陷族对抗（九族 → 验收栏映射）

| 族 | 设问结论 | 验收落点 |
|---|---|---|
| 1 生命周期/状态机中断 | 轮询卸载清理：usePoll 自管（document.hidden 停表）；本卡**刻意不引入新定时器**——引用条高亮保留至下次跳转（无 setTimeout 即无卸载清理面）；markedReads 用 ref 去重无副作用残留 | Task 3 代码注释 + SessionTab 已读支 |
| 2 静默失败/误导报错 | 端点 4xx/5xx 渲染可行动错误（会话列表断开横幅、发送被拒原文、详情读取失败、建会话弹层 error）；空列表 ≠ 加载失败（`loading` 与 `visible.length===0` 分离）；fixture 缺键测试必红（键集断言） | SessionSidebar/SessionChat/SessionTab 错误态 + Task 1 验收 1 |
| 3 跨平台假设 | 不引入新浏览器 API（无 websocket/剪贴板/IntersectionObserver）；`scrollIntoView` 仅在用户点击引用条时调用（jsdom 测试内 stub）——真实浏览器表现**未验证，需真机**（#7） | Task 5 移交清单 |
| 4 假红/假绿 | fixture 与 Go 金样本同源（同一 JSON 文件，值取自 sessions_fixture_test.go——TS 侧零手抄）；「online 不出现」反例断言锁看板不说谎前端半边；RoomPanel 退役以 grep 零命中+全量绿双验，不留孤儿套件 | Task 1 验收 1、Task 3 验收 1、Task 4 验收 4 |
| 5 门禁绕过 | 前端无权限面；member 身份服务端注入——`fetchSessions` 不设 member 参数（与 S4 伪造反例镜像）；请求体不带 actor 字段（createSession 只发 title/owner） | §2.2 注释 + rooms.fetch.test 断言 URL 恰 `/api/sessions` |
| 6 序列化边界 | 见 §11：三层锁穿真实序列化边界，缺失≠零值三处分辨 | §11 + Task 1 |
| 7 枚举白名单 | TS 字面量联合与 Go 词表逐值一致（SessionMemberStatus 四值/SessionTimelineKind 八值——金样本+词表支双侧锁）；渲染对未知值**透传兜底**（memberStatusLabel/timelineKindLabel `?? status`），不白屏；反例：往 MEMBER_STATUS_LABEL 加 online 键 → 词表支红 | sessionModel.test + Task 1 验收 1 |
| 8 承重安全属性 | 无新增安全属性（前端无权限面）；「不报不可证实的」前端半边由词表反例承重 | 同族 4/7 |
| 9 webview 候选族 | **命中**——dock 时代的入口被 B361 收敛为左栏 tab，真实浏览器两 tab 切换/分屏/滚动定位/键盘可达性**未验证，需真机**（Task 5 全单）；@高亮与引用条跳转的键盘可达性列入真机走查 | Task 5 |

## 13. 五项检查与自审三查

1. **缺陷族对抗**：§12 逐族有结论，落验收栏。
2. **序列化边界**：§11，三层锁穿真实边界（fixture 同源 + fetch stub + 组件同夹具）。
3. **上下文预算**：每 task 文件集有界（Task 0/6=0 文件；Task 1=4；Task 2=5；Task 3=13（12 新建 + 1 改写）；Task 4=6 含 4 删除）——组件族按「一个 tab 窗格」切片圈定，不吞整个 app。
4. **类型标注（边界型→逻辑型）**：d_web 在 best.json 为逻辑型，但按 breakdown 缺陷族 9 判 webview 命中——行为验收=机内结构断言（Task 1–4）+ 显式真机移交清单（Task 5，逐条 W1–W6）。
5. **接缝覆盖**：§11 双向 + 内部锁声明 2 支 + 退路同闸。

**自审三查**：

- **spec 覆盖**：breakdown §3.7 ③ 逐条指到验收——TS 金样本逐键+reply_to 三态+状态四值（Task 1）；组件反例（未读/needs_human 渲染、@高亮、引用条跳转、空座 chips、状态四值 online 反例）（Task 3）；Shell 结构断言（IA 断言 + 反例）（Task 4）；web 全量（Task 6）；W1–W6 走查归协调者（Task 5）。B361 五决定逐条落 §2.1；spec 实现决定 2（引用条可点/不折叠线程——按 seq 平铺渲染）/3（详情四块：成员+状态/节点/timeline——summary 即成员块载体）/4（状态词表）各有落点。
- **占位符扫描**：全文无 TBD/「适当的错误处理」/「同 Task N」。**正当出口声明（2 处）**：① Task 3 组件测试的 mock 夹具指认照抄 `RoomPanel.test.tsx` 既有 harness 形态（vi.mock api 模块 + 工厂函数），断言逐条列全、夹具工厂给全文；② Task 4 步骤 3.4 的 ProjectTree props 「逐字保留现文件 :648-687」——搬移不改内容，逐字对象 = `Shell.tsx` 现文件本体（改 props 即越界）。除这两处外全部代码给全文。
- **跨 task 签名一致**：Task 1 Produces（§2.2 类型与函数）↔ Task 3 Consumes 逐字同源；Task 2 Produces（session kind/dedupKey/tabTitle/sessionBase）↔ Task 3/4 Consumes 同源；`SessionSidebarProps` 七字段与 Shell 传参一一对齐；`SessionTabProps` 三字段与 renderContent 一致。

## 14. 岔口（协调者审查重点）

1. **【待拍板·本卡最重】控制台发言权（补员路径缺失）**：B358.4 拍板①后 owner=统一记法、`validSessionOwner` 拒绝 `web:`；`sessionWriters`=显式成员∪席位；**AddSessionMember 无生产入口**（B358.4 拍板②显式不做项+核对义务）。⇒ 控制台（`web:<host>`）对任何会话发言=403 ErrNotWriter——用户故事 1「需要我拍的就在原地答」在控制台当前不可达。本卡处置：403 原文透传渲染（可行动报错反例断言）。**候选裁决**：A（推荐）gateway 增 `POST /api/sessions/{id}/members`（AddSessionMember 接线，微增量；越本卡文件集，落新卡随 integrate 清单）；B 接受控制台只读+403 文案，走查现场按 B358.4 拍板②核对义务收口。若拍 A：本卡 Task 1/3/4 零改动（组件只多一个成功路径），新卡接线后直接活。
2. **【待拍板】新建会话的 owner 来源**：推荐 A——对话框显式输入统一记法（本计划实现形态，服务端权威校验）；被否备选：owner=roomUserActor（`web:` 被前缀校验拒绝，且 B358.4 拍板①已裁成员身份不收机器位）；不发明登录身份（多人时代 OOS）。若协调者改判（如默认值预填策略），仅 NewSessionDialog 与其测试两处微调。
3. **【待拍板·文件集增补确认】B/C 组四个 workbench 文件**（`tabs.ts`/`persist.ts`/`useWorkbench.ts` 必改——不改则编译不过或重载丢整份布局；`WorkbenchPage.tsx` 形态必要——B361 决定 5 的 ◫ 宿主）。依据 0.2#10–12 实读；breakdown §3.7 ④ 的文件集写于 B361 之前（当时会话是整块中央区、无 tab 宿主问题），B361 修订使增补成为 IA 落地的必要面。
4. **【边界声明，非岔口】**homedock 零改动（§0.1 落空声明）；项目筛选下拉落空、回复编辑框归 B365、listening 零生产——三条偏差注记见 §2.1.6，真机走查对照时须知。

## 15. 收口判据（B358.6 完成的唯一判定）

1. `cd web && npx vitest run` 退出 0（web 全量；RoomPanel 套件消失、无孤儿测试）。
2. `cd web && npm run typecheck`、`npm run lint` 退出 0；`go build ./...` 退出 0（Go 侧零改动）。
3. 孪生金样本逐键：`npx vitest run src/api/rooms.test.ts src/api/rooms.fetch.test.ts` 退出 0（含 reply_to 三态、成员状态四值闭包 online 反例、timeline 八值对齐、omitempty 缺失≠零值三处）。
4. IA 断言全绿：左栏两 tab 切换、会话行开 tab（多开/去重/可关）、◫ 分屏窗格 +1、面包屑跟会话 tab、`room-panel`/悬浮球反例零出现、账本关闭隐藏会话 tab。
5. `grep -rn "RoomPanel" web/src` 零命中；`git status --short` 受控 = §0.1 文件集 + 本计划 + 台账（无关脏文件不在列）。
6. 台账含：Task 0 基线读数、Task 2 变异自查（parseContent 删 case→红）记录、图符号清单、Task 5 移交单确认。

## 实现者纪律（派发注入段）

- TDD 三段律：编译/类型全量、测试局部（只跑触及目录）、全量只在 Task 6——每个 task 的测试命令见各验收。
- 日志：会话入口/建会话/分屏走 `console.debug('shell.session.*')`；组件内走 `logRoom`（结构化、不写消息正文）；失败分支必带 error 原文；禁 print。
- 注释：sessionModel 两词表头注释（承重属性=反例断言的靶子）已给；每组件文件头职责+边界已给；导出函数注释已给。
- 修复熔断：任何测试意外红且无法归因到本计划明说的行为变化 → 停手上报协调者，不顺手修越界文件（尤其 `internal/**` 与 ProjectTree）。
- 本计划无驱动派发系统自身的验收步骤；Task 5 标注「由协调者执行，不派发」。

---

## 协调者拍板（补记，2026-09-12）

1. **岔口1（控制台发言权）：候选 A，立 B366（补员端点 + 控制台加入会话入口）。**AddSessionMember 的 Store/Facade/Client 三层已随 Ticket 0 冻结，缺的只是 gateway 接线（POST /api/sessions/{id}/members）——纯接线非契约增补；配控制台 403 时「以当前身份加入会话」一键（把 web actor 坐进成员集，B358.4 偏差#1 已实证该形状）。排程：B358.6 完成后、B358 integrate 前推进（gate 用户故事 1「原地答」）；本卡按 plan 处置（403 原文透传反例断言）不做补员面。
2. **岔口2（新建会话 owner 来源）：准**——对话框显式输入统一记法（前缀校验同 B358.4 拍板①）。
3. **岔口3（有界文件集增补 B/C 组）：准**——workbench 三文件与 WorkbenchPage.tsx 是 B361 IA 的必然落点，breakdown 文件集写于 B361 之前，显式申报正当。
4. **岔口4（homedock 零改动）：准。**
