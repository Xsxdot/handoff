# B358.5（d_cli）session 命令族——实现计划

- **卡**：B358.5（B358 拆解 S5，L3 重档子系统执行者依契约自写、协调者只审）
- **依据**：contract `docs/superpowers/specs/b358-contract.md`（§3.3/§3.5 门面签名、§3.7 会话房间形态、§4.1 条 1–13、§4.6 条 39–50 已由 B358.3 落地、§8.3 欠账 3、§9 附区）＋ breakdown `docs/superpowers/specs/b358-breakdown.md` §3.6（S5 四段式）＋ §2.2 P7（新 session 族）/P4（身份记法）＋ spec `docs/superpowers/specs/b358.md` §4.2（以自己名义发言）、§6 接缝 #1 的 CLI 调用方、§3 用户故事 6（主 agent 巡场）
- **前置已合入**：B358.1（0be4a2c6，S1 会话语义收口）、B358.2（77affbb5，S2 投影精化）、B358.3（75adb4eb，S3 唤醒改接 + session wait 通道）、B358.4（4162505f，S4 会话 HTTP 端点）——本计划全部判据对当前工作树（`cards/B233.1-charter-7` @ `4162505f`）实测，非对冻结时点的旧读数
- **B358.4 拍板①的 CLI 原则（本计划身份设计的上位依据）**：成员身份取统一记法（`user:<name>`/`agent:<name>` 前缀校验），审计 actor 注入面不动（`ledgerActor()`=`cli:<user>@<host>`）；「CLI 侧（S5）同原则」（b358.4-plan 文末拍板原文）
- **图覆盖债**：本卡触及面整体不在 `codegraph/best.json`（探针原文见台账 §三；`openRoomService`/`roomServiceFor` 在 baseline 视图、B358.3 新符号与 collab 会话门面七方法均不在）——一律 `file#Symbol` 源码锚；新增/修改符号清单在 Task 3 落台账，交 integrate/recon 补视图 diff
- **台账**：`docs/superpowers/ledgers/2026-09-12-b358.5-plan-ledger.md`

## 0. 本卡干什么（一段话）

给人与主 agent（外部会话）一条不用控制台的会话入口：在 B358.3 建好的 `cmd/session.go`（root + wait）上**同文件续写**六子命令 **list/detail/create/archive/join/leave/send**，全部直调既有组装点 `openRoomService` → `collab.Service` 会话门面（零新组装点、零新跨域边）。**双重职责**：① 命令族本体（本计划定稿 stdout 形状与退出码契约，§2）；② 按 B358.1 拍板②，把划归本卡的 **cmd 红窗 6 支**（`cmd/room_test.go`：TestRoomSendLandsRoomMessageWithUserKind / TestRoomSendCoordinatorAcceptsExplicitSeatFlags / TestRoomSendCoordinatorKindUsesGrokHostSession / TestRoomSendCarriesRefAndMention / TestRoomReadStdoutSeqPrefix / TestRoomListSortedByActivity）按新会话语义就地改写收口为绿（名字全部保留供核算），旧 `handoff room send <卡号>` 按拍板 P7 保留报错形状。B358.3 的 TestSessionWait* 七支与源码守卫、TestRepoContractGate、TestServePermissionHook、B362 golden **禁触**（守卫禁令与本卡 kind 引用的冲突见岔口 1，须派工前裁决）。

## 0.1 有界文件集（本卡只许碰这些文件）

**生产（1 个）**：
- `cmd/session.go`——同文件续写六子命令（cmd var + RunE + `validSessionMemberIdentity` + init 追加行）；wait 与根命令注册行**零改动**

**测试（2 个）**：
- 新增测试**追加到** `cmd/session_test.go` 末尾（既有 B358.3 内容一字不动；B358.3 交界申报原文「cmd/room_test.go/cmd/session_test.go（按 P7 拍板定文件名）」授权本卡写该文件）
- `cmd/room_test.go`——仅 §5 红窗 6 支就地改写（文件头注释同步一行改写说明）；其余测试一字不动（TestRoomSendUserRejectsSeatFlagsWithoutSideEffects、TestRoomInboxWalksAgentdHTTP 及 mustAddCard/clearSeatSourceEnv 等 helper 禁触）

**禁触（越界即回退）**：
- `cmd/session_test.go` 既有内容（B358.3 的 TestSessionWait* 七支 + mustWaitFixture + decodeSessionWake + TestSessionWaitSourceGuard）——**只许追加、不许改写**；TestSessionWaitSourceGuard 的例外见岔口 1（未经协调者批准不得动）
- `cmd/room.go`——openRoomService/roomServiceFor（B358.3 游标行复用不重挂）、旧 room send 及其 kind=user 拒 flag 守卫行（:133-138，死面形状冻结，见岔口 11.3）全部不动
- `internal/**` 全部（collab/ledger/proto/agentdTicket 0–S4 已收口，本卡只消费）
- `TestRepoContractGate`（基线违规 6 条只同不增）、`TestServePermissionHookDenyWithReasonAndStep0`（macOS 环境性红）、`internal/agentd/scheddispatch_test.go#TestLegacyNodeEventSequenceUnchanged`（B362 golden）
- 工作树无关脏文件（`web/src/app/task/Composer.test.tsx`、`.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、`web/design-qa*`）不卷入提交

## 0.2 现状依赖行为（必须按出处实现；行号为 2026-09-12 读数，以 `file#Symbol` 为准）

| # | 事实 | 出处 |
|---|---|---|
| 1 | 门面七方法可直调：CreateSession/ListSessions/SessionDetail/ArchiveSession/JoinCard/LeaveCard/Send；**未包 AddSessionMember**（成员扩张无 CLI 面——B358.4 拍板 2 显式不做项，CLI 同此不做） | `internal/collab/sessions.go:25-107` |
| 2 | `Send(roomID, msg, actor)` 执法链：pointer 拒收 → Resolve（不存在的卡 ErrNoRoom）→ 旧房间 ErrReadOnly → 会话：GetSession 失败→ErrNoRoom、Archived→ErrReadOnly、成员执法→ErrNotWriter；成功落**无卡事件**返回 seq | `internal/collab/service.go:86-167` |
| 3 | 书写者集 = `session.Members` ∪ 会话内卡当前 `driver_session`；**actor 与 writers 逐字相等**（VerifyWriter 只查非空+逐字相等）——CLI 发言 actor 必须已是成员或会话内席位 | `internal/collab/service.go:108-129`、`internal/collab/room/room.go#VerifyWriter` |
| 4 | 哨兵文案：ErrNoRoom=「collab: 房间不存在」、ErrReadOnly=「collab: 房间已只读（并入冻结或终态归档）」、ErrNotWriter=「collab: 书写者与房间身份不符」；`mapSessionError` 把 client.ErrNotFound **替换**为 ErrNoRoom（Store 层含 id 的文案丢失——CLI 必须在边界把 id 包回去，gateway handleSessionDetail 先例） | `internal/collab/room/errors.go:10-18`、`internal/collab/sessions.go:452-462`、`internal/agentd/sessionsapi.go#handleSessionDetail` |
| 5 | JoinCardToSession：已属他会话→`卡 %s 已属会话 %s`+ErrBadState；会话已归档→`会话 %s 已归档`+ErrBadState；同会话重复 join 幂等（条 10）；LeaveCardToSession 幂等（不在会话内返回 nil，条 11）；ArchiveSession 幂等（条 6，Store 级短路） | `internal/ledger/sessions.go:168-232`、`:140-162` |
| 6 | CreateSession：owner 非空校验（「会话群主不能为空」）、owner 进 Members 初值（条 3）、无前缀校验——前缀校验是 CLI 面职责 | `internal/ledger/sessions.go:39-75` |
| 7 | AddSessionMember 只查非空（无前缀校验；gateway 夹具 `web:127.0.0.1` 先例）——测试夹具坐 CLI actor 走它 | `internal/ledger/sessions.go:233-260` |
| 8 | gateway 半边统一记法已落：`validSessionOwner`（user:/agent: 前缀+非空名，web: 拒绝）；owner=body、审计 actor=roomUserActor 两字段不混用——本卡 CLI 复制同规则（cmd 不得 import agentd，见岔口 6） | `internal/agentd/sessionsapi.go:28-34,74-108` |
| 9 | `ledgerActor()`=`cli:<user>@<host>`（USER env + hostname；审计注入面不动）；`currentSeatIdentity(flagCLI, flagSession)` flag 成对强制（单 flag→「席位 flag 不完整」）、HANDOFF/宿主键为环境半边 | `cmd/ledgercli.go:44-51`、`cmd/card_seat.go:23-101` |
| 10 | 旧 `room send` 守卫行（kind=user 且 Changed(cli/session)→拒绝）在 `cmd/room.go:133-138`——本卡不动该行（岔口 11.3）；`room send <卡号>` 现状一律 ErrReadOnly（S1 只读归档） | `cmd/room.go:128-167` |
| 11 | `openRoomService` 已挂 `cursor.New(DataDir/room-cursors.json)`（B358.3 落）——session list 未读投影复用此行，不重挂；`ListSessions(member)` member=="" 跳过游标读（Unread 恒 0）、返回**创建序**无排序 | `cmd/room.go:45-55`、`internal/collab/sessions.go:30-64` |
| 12 | cobra：SilenceErrors=false（RunE 错误打 stderr 且 Execute 原样返回→`runLedgerCLI` 的 stderr buffer 含错误文案）、PersistentPreRun 起 SilenceUsage=true（参数/flag 校验错仍打 usage）；退出码 `ExitFailure=1`/`ExitTimeout=124`，普通 error 经 main 换算为 1 | `cmd/root.go:36-56`、`cmd/exit.go:20-36` |
| 13 | `SessionSummary`/`SessionDetail` wire 键集已冻结（preview/members/cards/nodes/timeline omitempty）；金样本 `internal/proto/sessions_fixture_test.go` | `internal/proto/sessions.go:75-138` |
| 14 | 测试夹具既有：`runLedgerCLI`（首调自动写 config.yaml）、`mustAddCard`（card add→{"id":…}）、`clearSeatSourceEnv`（清五个席位 env）、`mustWaitFixture`（B358.3；facade.AddSessionMember 先例）；`st.BindSeat(cardID, identity, proto.SeatSourceBind)`；Facade 会话方法 AddSessionMember/JoinCardToSession/ArchiveSession 直通 | `cmd/ledgercli_test.go:33`、`cmd/room_test.go:23`、`cmd/card_driver_test.go:18`、`cmd/session_test.go:28`、`internal/ledger/api/api.go:96-141` |
| 15 | SQLite 方言时间戳 RFC3339Nano（纳秒）——两次发言的 LastActivity 时间序确定 | `internal/ledger/store.go#Store.tval`（:126-131） |
| 16 | HTTP 发言面 kind 服务端固定 user、收 body/refs/mentions、**无 reply_to**——CLI 不设 --reply-to 与 gateway 同基线（回复寻址两面皆缺载体，岔口 5 申报） | `internal/agentd/roomsapi.go:319-347` |
| 17 | **守卫冲突（本计划新发现，岔口 1）**：`TestSessionWaitSourceGuard` 对 `session.go` 全文件子串禁令含 `"proto.RoomMsgUser"`——S5 同文件续写的 session send 必须引用该常量（breakdown §3.6 ③ kind=user），同文件落码必然打红守卫 | `cmd/session_test.go:182-223` |

## 0.3 红窗基线与核算方式

**基线红集合（2026-09-12 @ `4162505f` 工作树实测，原文见台账 §一）**：

`cmd`（8 支）：1. `TestRoomSendLandsRoomMessageWithUserKind` 2. `TestRoomSendCoordinatorAcceptsExplicitSeatFlags` 3. `TestRoomSendCoordinatorKindUsesGrokHostSession` 4. `TestRoomSendCarriesRefAndMention` 5. `TestRoomReadStdoutSeqPrefix` 6. `TestRoomListSortedByActivity`——前 6 支 = B358.1 红窗，**改写责任归本卡**；7. `TestRepoContractGate`（违规恰 6 条：1 dead-interface + 5 over-budget，清单见台账）8. `TestServePermissionHookDenyWithReasonAndStep0`（macOS 环境性）——后 2 支禁触。

`internal/agentd`（1 支）：`TestLegacyNodeEventSequenceUnchanged`（B362 golden）——禁触禁改。

**本卡核算规则（与 B358.4 §0.4 同款）**：

1. **改写的 6 支不算既有红**：Task 2 就地改写后必须**收口为绿**（名字全部保留，函数体首行「B358.5 红窗改写」注释指认原断言与迁移去向；红因是「旧房间发言成功断言随 S1 失效」，不是回归）。
2. **收口判据**：`go test ./cmd -count=1` 失败集合 = **恰** `{TestRepoContractGate, TestServePermissionHookDenyWithReasonAndStep0}`，逐支同名无增无减；`go test ./internal/agentd -count=1` 失败集合 = 恰 `{TestLegacyNodeEventSequenceUnchanged}`。多出一支 = 本卡波及面清点漏项 → 停手上报（修复熔断）；少一支（意外修复）同样上报对账。
3. **TestRepoContractGate 违规清单只同不增不减**：收口时逐行比对基线 6 条（本卡零新增跨域 import——cmd 已 import collab/proto/ledger/ledgerapi/room 全部既有，新增仅 stdlib `sort`/`strings`/`text/tabwriter`）。
4. **B358.3 面保持绿禁触**：`go test ./cmd -run 'TestSessionWait' -count=1` 退出 0（七支含守卫）；TestRoomSendUserRejectsSeatFlagsWithoutSideEffects、TestRoomInboxWalksAgentdHTTP 保持绿。
5. **collab/ledger/proto 三包保持绿**：`go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` 退出 0。

## 1. 已亲跑的基线与判据

以下命令在基线（`4162505f` 工作树）亲跑，结果记台账 §一。执行者在 Task 0 复跑同组命令，读数不同先停下报告，不得带着意外继续：

| 命令 | 基线结果（2026-09-12 实测） |
| --- | --- |
| `go test ./cmd -count=1` | FAIL，失败恰 §0.3 的 8 支 |
| `go test ./cmd -run TestRepoContractGate -count=1 -v` | FAIL，违规恰 6 条（1 dead-interface + 5 over-budget） |
| `go test ./internal/agentd -run TestLegacyNodeEventSequenceUnchanged -count=1` | FAIL（B362 golden，got/welt GOLDEN 序列漂移——非本卡职责） |
| `go test ./cmd -run 'TestSessionWait' -count=1` | ok（B358.3 七支全绿，含 TestSessionWaitSourceGuard） |

判据先行的代码级确证（读码得出，出处 0.2）：基线上 `session` 根命令**只挂了 wait 一个子命令**（`cmd/session.go:183-188`）——Task 1 的六支新测试在基线必红（cobra unknown command），红是有意义的；`room send <卡号>` 非 0（0.2#10）是本卡要**钉住**的报错形状，不是要修的 bug。

## 2. 终局形状（plan 定稿，随后写进验收作字面断言）

### 2.1 命令面总表（六子命令，P7 拍板的新族）

全部注册进既有 `sessionCmd`（`sessionCmd.AddCommand(...)` 追加一行）；组装一律 `openRoomService()`（复用，不重挂游标）；actor 注入面 = `ledgerActor()`（审计）+ `currentSeatIdentity`（席位出示，只消费 flag 对）。

| 命令 | 用法 | 门面调用 | 成功 stdout | 失败（一律非 0） |
|---|---|---|---|---|
| create | `session create <title> --owner <identity>` | `CreateSession(title, owner, actor)` | `proto.Session` JSON 一行（含 `id`=`session:<n>`） | owner 非统一记法 / 空标题（用法错）；账本错 |
| list | `session list [--member <identity>] [--json]` | `ListSessions(member)` | tabwriter 表：表头 `ID\t标题\t群主\t归档\t未读\t需要你\t最近活动` + 每会话一行（**活动降序**，cmd 层呈现排序）；`--json` → 每行一条 `SessionSummary` JSON | 账本错 |
| detail | `session detail <id> [--json]` | `SessionDetail(id)` | 三段文本：`成员:` / `节点:` / `时间线:`（行式见 §2.3）；`--json` → `SessionDetail` JSON 一行 | 不存在 → stderr 含会话 id（`mapSessionError` 丢 id，CLI 边界包回） |
| archive | `session archive <id>` | `ArchiveSession(id, actor)` | `{"ok":true}` | 不存在 → stderr 含 id；重复归档**幂等退出 0** |
| join | `session join <id> <卡号>` | `JoinCard(id, cardID, actor)` | `{"ok":true}` | 已属他会话 → stderr 含「已属会话」；已归档 → stderr 含「已归档」；卡/会话不存在；同会话重复 join **幂等退出 0** |
| leave | `session leave <id> <卡号>` | `LeaveCard(id, cardID, actor)` | `{"ok":true}` | 会话不存在 → stderr 含 id；不在会话内**幂等退出 0** |
| send | `session send <id> <text…> [--ref …] [--mention …] [--cli X --session Y]` | `Send(id, msg, actor)` | `{"ok":true,"seq":<n>}`（与 room send 同契约） | 席位 flag 单只（用法错，缺陷族 4 反例）；空正文；归档 → stderr 含「只读」；非成员/非席位 → stderr 含「书写者」；会话不存在 → stderr 含 id |

**退出码契约（breakdown §3.6 ③）**：成功 0；用法/存在性/状态错误非 0（普通 error → main 按 `ExitFailure=1` 换算）；**不引入新超时、不新增退出码**（wait 的 124 特例不属本卡，零改动）。

### 2.2 身份模型（B358.4 拍板①的 CLI 半边，本计划定稿）

- **两字段不混用**：成员身份（owner、--member、席位出示值）用统一记法 `user:<name>`/`agent:<name>`（前缀+非空名校验，`web:`/裸名/空名拒绝）；审计 actor 恒 `ledgerActor()`=`cli:<user>@<host>`，不做成员匹配用。CreateSession 本就把 owner 落进 Members 初值（0.2#6），P4「开会话时落显式成员」由此满足。
- **session send 两形态**（breakdown §3.6 ③ 逐字）：
  1. **人**：无 `--cli/--session` → actor=`ledgerActor()`，kind=user。落账前提：该 CLI actor 已被坐进会话显式成员（生产路径=owner 或补员面；补员面显式不做见 §7.3——B358.4 拍板 2 的 CLI 半边，已记父卡 integrate 核对项）。
  2. **协调者**：`--cli X --session Y` 成对 → actor=`currentSeatIdentity(X, Y)`=`cli:<X>#<Y>`（席位身份），kind=user。落账前提：该席位是会话内某卡当前 `driver_session`（书写者集席位半边）。
  3. **单只 flag → 用法错误非 0**（缺陷族 4 点名的成对反例）；**环境席位键（HANDOFF_SESSION_*/GROK_SESSION_ID）不参与 session send**——身份判定严格按 flag 有无，无 flag 恒人形态（定稿理由与被否备选见岔口 11.3）。
- **无 --kind flag**：两形态 kind 恒 `proto.RoomMsgUser`——发言自由（spec §4.2 白名单废止），会话聊天面无第二 kind 用例（放弃 --kind 的理由见台账 §四.4）。
- **无 --reply-to**：breakdown §3.6 ③ 验收无此项，gateway 半边同缺（0.2#16）——不静默扩面，缺口申报见岔口 5。
- **不提供「冒充他人成员身份」的直通 flag**（breakdown §3.6 缺陷族 5 字面）：身份只能来自 `ledgerActor()` 或成对席位 flag；自报席位不在书写者集 → ErrNotWriter（反例断言见 Task 1）。

### 2.3 detail/list 文本行式（字面断言锚）

```text
# session detail <id>（文本态，三段；空段只出段头）
成员:
<identity>\t<kind>\t<status>\t<card>          # card=席位成员的 CardID，其余 "-"
节点:
<card>\t<node>\t<state>                        # 无 task_mirrored 事件时空段
时间线:
#<seq>\t<kind>\t<card>\t<detail>               # card 空时 "-"；detail=payload 原样
# session list（表头七列，tabwriter）
ID\t标题\t群主\t归档\t未读\t需要你\t最近活动    # 需要你列：NeedsHuman→"需要你"，否则空
```

## 3. Task 0：基线复跑与红窗清点（不写码）

### 文件范围

无代码改动；读数落台账。

### 步骤

1. 复跑 §1 表格四条命令，逐条与基线读数比对。**任何一条不符 → 停手上报**。
2. verbose 枚举红窗（`go test ./cmd -run TestRoom -count=1 -v`），失败名单与 §0.3 逐支比对（恰 6 支），确认 TestRoomSendUserRejectsSeatFlagsWithoutSideEffects / TestRoomInboxWalksAgentdHTTP / TestSessionWait 七支全绿。
3. `go test ./cmd -run TestRepoContractGate -count=1 -v` 记违规 6 条原文（收口比对用）。
4. 台账记 Task 0 读数。

### Task 0 验收

- §1 四条读数与基线一致；红窗名单逐支同名；存活测试绿；台账已记。

## 4. Task 1：session 六子命令（cmd/session.go 续写 + cmd/session_test.go 追加，红→绿）

### 文件范围

`cmd/session.go`（续写）、`cmd/session_test.go`（末尾追加）。

### Interfaces

**Consumes（既有，逐字）**：

```go
// internal/collab —— 入站门面（spec §6 缝 #1 的 CLI 调用方侧）
func (s *Service) CreateSession(title, owner, actor string) (proto.Session, error)
func (s *Service) ListSessions(member string) ([]proto.SessionSummary, error)
func (s *Service) SessionDetail(id string) (proto.SessionDetail, error)
func (s *Service) ArchiveSession(id, actor string) error
func (s *Service) JoinCard(sessionID, cardID, actor string) error
func (s *Service) LeaveCard(sessionID, cardID, actor string) error
func (s *Service) Send(roomID string, msg proto.RoomMessage, actor string) (int64, error)
var ErrNoRoom / ErrReadOnly / ErrNotWriter                 // collab 哨兵（room 子包再导出）
// cmd 既有
func openRoomService() (*collab.Service, *ledger.Store, error)   // 游标介质已挂（B358.3）
func ledgerActor() string                                        // "cli:<user>@<host>"
func currentSeatIdentity(flagCLI, flagSession string) (string, error) // "cli:<cli>#<session_id>"
const proto.RoomMsgUser = "user"
// 测试夹具既有（同包可见）
func runLedgerCLI(t *testing.T, dir string, args ...string) (string, string, error)
func mustAddCard(t *testing.T, dir, title string) string
func clearSeatSourceEnv(t *testing.T)
// ledger 测试面（_test.go import 先例既有）
func (s *Store) BindSeat(cardID, identity, source string) error   // source=proto.SeatSourceBind
func (s *Store) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]Event, error) // nil=全流
func (f *Facade) AddSessionMember(sessionID, identity, actor string) error
func (f *Facade) JoinCardToSession(sessionID, cardID, actor string) error
func (f *Facade) ArchiveSession(id, actor string) error
```

**Produces（本卡新增，交 recon 入图）**：

```go
// cmd/session.go（续写）
var sessionCreateCmd, sessionListCmd, sessionDetailCmd *cobra.Command
var sessionArchiveCmd, sessionJoinCmd, sessionLeaveCmd, sessionSendCmd *cobra.Command
func validSessionMemberIdentity(identity string) bool
// cmd/session_test.go（追加）
func mustSendFixture(t *testing.T, dir, title string) (*collab.Service, *ledgerapi.Facade, *ledger.Store, string, string)
func TestSessionCreateCommand(t *testing.T)
func TestSessionListColumnsAndUnread(t *testing.T)
func TestSessionDetailThreeSections(t *testing.T)
func TestSessionJoinLifecycle(t *testing.T)
func TestSessionLeaveLifecycle(t *testing.T)
func TestSessionSendNegativePaths(t *testing.T)
func TestSessionArchiveCommand(t *testing.T)
```

### 步骤（红绿只套在锁缝断言上；本任务一条红绿周期贯穿）

**步骤 1（红）**：`cmd/session_test.go` 末尾追加下列全部测试与夹具（完整代码，直接落；既有内容零改动）：

```go
// —— B358.5 追加：session 六子命令缝级测试（create/list/detail/archive/join/
// leave/send）。入口全部是 CLI 命令（spec §6 缝 #1 的 CLI 调用方），数据穿真
// SQLite 账本；成员扩张用 facade.AddSessionMember（gateway 夹具 mustConsoleSession
// 同款先例——成员扩张无 CLI/HTTP 面，B358.4 拍板 2）。断言：§2.1/§2.3 逐命令
// stdout 形状、退出码契约（成功 0 / 用法与存在性错误非 0）、--json roundtrip
// 回冻结 DTO 键集（breakdown §3.6 缺陷族 6）。

// mustSendFixture 开真账本、建会话（owner=user:tester，群主即初始成员——契约
// 条 3）并把 CLI 审计 actor（ledgerActor()=cli:<user>@<host>）坐进显式成员：
// 人形态 session send 的书写者执法（0.2#3）要求它在 Members。返回
// (svc, facade, st, 会话id, actor)。
func mustSendFixture(t *testing.T, dir string, title string) (*collab.Service, *ledgerapi.Facade, *ledger.Store, string, string) {
	t.Helper()
	actor := ledgerActor()
	st, err := ledger.Open(filepath.Join(dir, "ledger.db"))
	if err != nil {
		t.Fatalf("开夹具账本: %v", err)
	}
	t.Cleanup(func() { _ = st.Close() })
	facade := ledgerapi.New(st)
	svc := collab.New(facade)
	session, err := svc.CreateSession(title, "user:tester", "user:tester")
	if err != nil {
		t.Fatalf("建会话: %v", err)
	}
	if err := facade.AddSessionMember(session.ID, actor, "user:tester"); err != nil {
		t.Fatalf("坐进 CLI actor: %v", err)
	}
	return svc, facade, st, session.ID, actor
}

// TestSessionCreateCommand 锁：create 200 路径 stdout=Session JSON 一行（id 形如
// session:<n>）+ 审计 actor 注入面不动（EvSessionCreated 的 actor=ledgerActor()，
// 不是 owner——两字段不混用，B358.4 拍板① 的 CLI 半边）+ owner 统一记法前缀
// 校验反例四发（机器位 web: / 裸名 / 空名 / 缺失）+ 空标题拒绝。
func TestSessionCreateCommand(t *testing.T) {
	dir := t.TempDir()
	out, _, err := runLedgerCLI(t, dir, "session", "create", "需求对齐", "--owner", "user:tester")
	if err != nil {
		t.Fatalf("session create: %v", err)
	}
	var session proto.Session
	if err := json.Unmarshal([]byte(strings.TrimSpace(out)), &session); err != nil {
		t.Fatalf("create stdout 应为 Session JSON 一行: %q err=%v", out, err)
	}
	if !strings.HasPrefix(session.ID, "session:") || session.Owner != "user:tester" || session.Title != "需求对齐" {
		t.Fatalf("create 投影漂移: %+v", session)
	}
	st, err := ledger.Open(filepath.Join(dir, "ledger.db"))
	if err != nil {
		t.Fatal(err)
	}
	defer st.Close()
	events, err := st.EventsFromAsc(nil, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	created := false
	for _, ev := range events {
		if ev.Type != ledger.EvSessionCreated {
			continue
		}
		created = true
		if ev.Actor != ledgerActor() {
			t.Fatalf("建会话审计 actor 应注入面不动 %q，实得 %q", ledgerActor(), ev.Actor)
		}
	}
	if !created {
		t.Fatal("EvSessionCreated 应已落账")
	}
	for _, bad := range []struct{ name, owner string }{
		{"机器位 web:", "web:1"}, {"裸名", "mallory"}, {"空名", "user:"},
	} {
		if _, _, err := runLedgerCLI(t, dir, "session", "create", "x", "--owner", bad.owner); err == nil {
			t.Fatalf("owner %s 必须拒绝（统一记法前缀校验）", bad.name)
		}
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "create", "x"); err == nil {
		t.Fatal("缺 --owner 必须拒绝")
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "create", "  ", "--owner", "user:tester"); err == nil {
		t.Fatal("空标题必须拒绝")
	}
}

// TestSessionListColumnsAndUnread 锁：表头七列（§2.3 字面）+ 两行都在 +
// --member user:tester 下两条消息的会话 Unread=2、另一场 0 + --json 每行可
// Unmarshal 回 proto.SessionSummary（冻结 DTO roundtrip，缺陷族 6）且有消息行
// 键集恰 {id,kind,title,owner,archived,unread,needs_human,last_activity,preview,members}
//（cards 空 → omitempty 缺键：缺失≠零值分辨）。
func TestSessionListColumnsAndUnread(t *testing.T) {
	dir := t.TempDir()
	svc, _, _, idA, _ := mustSendFixture(t, dir, "对账场A")
	_, _, _, idB, _ := mustSendFixture(t, dir, "对账场B")
	for _, body := range []string{"第一条", "第二条"} {
		if _, err := svc.Send(idA, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: body}, "user:tester"); err != nil {
			t.Fatalf("夹具发言 %q: %v", body, err)
		}
	}
	out, _, err := runLedgerCLI(t, dir, "session", "list", "--member", "user:tester")
	if err != nil {
		t.Fatalf("session list: %v", err)
	}
	for _, col := range []string{"ID", "标题", "群主", "归档", "未读", "需要你", "最近活动"} {
		if !strings.Contains(out, col) {
			t.Fatalf("表头缺列 %q: %q", col, out)
		}
	}
	unreadOf := func(line string) string {
		// 行式 = ID\t标题\t群主\t归档\t未读\t…（tabwriter 空格对齐；「需要你」空列
		// 被 Fields 折叠，但未读在第 5 列不受影响）。
		fields := strings.Fields(line)
		if len(fields) < 5 {
			t.Fatalf("行字段不足: %q", line)
		}
		return fields[4]
	}
	var lineA, lineB string
	for _, line := range strings.Split(strings.TrimRight(out, "\n"), "\n") {
		if strings.Contains(line, idA) {
			lineA = line
		}
		if strings.Contains(line, idB) {
			lineB = line
		}
	}
	if lineA == "" || lineB == "" {
		t.Fatalf("两场会话都应在列表: %q", out)
	}
	if got := unreadOf(lineA); got != "2" {
		t.Fatalf("两条无 @ 消息 → 未读列应 2，实得 %q（行 %q）", got, lineA)
	}
	if got := unreadOf(lineB); got != "0" {
		t.Fatalf("无消息会话未读列应 0，实得 %q（行 %q）", got, lineB)
	}
	// --json：每行一条 SessionSummary，roundtrip 回冻结 DTO；有消息行键集字面。
	outJSON, _, err := runLedgerCLI(t, dir, "session", "list", "--json", "--member", "user:tester")
	if err != nil {
		t.Fatalf("session list --json: %v", err)
	}
	lines := strings.Split(strings.TrimSpace(outJSON), "\n")
	if len(lines) != 2 {
		t.Fatalf("--json 应每会话一行恰两行: %q", outJSON)
	}
	for _, line := range lines {
		var summary proto.SessionSummary
		if err := json.Unmarshal([]byte(line), &summary); err != nil {
			t.Fatalf("--json 行应 Unmarshal 回 SessionSummary: %v 原文=%s", err, line)
		}
	}
	var withMsg map[string]json.RawMessage
	for _, line := range lines {
		var probe map[string]json.RawMessage
		if err := json.Unmarshal([]byte(line), &probe); err != nil {
			t.Fatal(err)
		}
		var idOnly struct {
			ID string `json:"id"`
		}
		_ = json.Unmarshal([]byte(line), &idOnly)
		if idOnly.ID == idA {
			withMsg = probe
		}
	}
	wantKeys := 10
	if len(withMsg) != wantKeys {
		t.Fatalf("有消息行键数应 %d（含 preview/members，缺 cards）: %d %v", wantKeys, len(withMsg), withMsg)
	}
	for _, k := range []string{"id", "kind", "title", "owner", "archived", "unread", "needs_human", "last_activity", "preview", "members"} {
		if _, ok := withMsg[k]; !ok {
			t.Fatalf("有消息行缺键 %q", k)
		}
	}
	if _, ok := withMsg["cards"]; ok {
		t.Fatalf("无卡会话 cards 应缺键（omitempty）: %v", withMsg["cards"])
	}
}

// TestSessionDetailThreeSections 锁：三段段头（成员:/节点:/时间线:）逐列可读 +
// owner 行 human + 进群卡号在文 + timeline 含 created/card_joined + --json
// roundtrip 回 proto.SessionDetail（顶层键恰 {summary,timeline}——nodes 无
// task_mirrored 事件缺键）+ 不存在会话非 0 且 stderr 含会话 id（mapSessionError
// 丢 id，CLI 边界包回——0.2#4）。
func TestSessionDetailThreeSections(t *testing.T) {
	dir := t.TempDir()
	_, facade, _, sessionID, _ := mustSendFixture(t, dir, "详情三块场")
	card := mustAddCard(t, dir, "详情进群卡")
	if err := facade.JoinCardToSession(sessionID, card, "user:tester"); err != nil {
		t.Fatalf("夹具拉卡: %v", err)
	}
	out, _, err := runLedgerCLI(t, dir, "session", "detail", sessionID)
	if err != nil {
		t.Fatalf("session detail: %v", err)
	}
	for _, sec := range []string{"成员:", "节点:", "时间线:"} {
		if !strings.Contains(out, sec) {
			t.Fatalf("详情缺段 %q: %q", sec, out)
		}
	}
	if !strings.Contains(out, "user:tester") || !strings.Contains(out, "human") {
		t.Fatalf("成员块应含 owner 行（human）: %q", out)
	}
	if !strings.Contains(out, card) {
		t.Fatalf("进群卡号应在文（空座成员行/时间线行）: %q", out)
	}
	if !strings.Contains(out, "created") || !strings.Contains(out, "card_joined") {
		t.Fatalf("时间线应含 created 与 card_joined 行: %q", out)
	}
	outJSON, _, err := runLedgerCLI(t, dir, "session", "detail", sessionID, "--json")
	if err != nil {
		t.Fatalf("session detail --json: %v", err)
	}
	var raw map[string]json.RawMessage
	if err := json.Unmarshal([]byte(strings.TrimSpace(outJSON)), &raw); err != nil {
		t.Fatalf("--json 应为 SessionDetail JSON 一行: %v", err)
	}
	if len(raw) != 2 {
		t.Fatalf("顶层键应恰 {summary,timeline}（nodes 缺键）: %v", raw)
	}
	if _, ok := raw["summary"]; !ok {
		t.Fatal("缺 summary 键")
	}
	if _, ok := raw["timeline"]; !ok {
		t.Fatal("缺 timeline 键")
	}
	var detail proto.SessionDetail
	if err := json.Unmarshal([]byte(strings.TrimSpace(outJSON)), &detail); err != nil {
		t.Fatal(err)
	}
	if detail.Summary.ID != sessionID || len(detail.Summary.Cards) != 1 || detail.Summary.Cards[0].CardID != card {
		t.Fatalf("详情投影漂移: %+v", detail.Summary)
	}
	kinds := map[string]bool{}
	for _, ev := range detail.Timeline {
		kinds[ev.Kind] = true
	}
	if !kinds["created"] || !kinds["card_joined"] {
		t.Fatalf("--json timeline 缺 created/card_joined: %v", kinds)
	}
	_, errb, err := runLedgerCLI(t, dir, "session", "detail", "session:999")
	if err == nil {
		t.Fatal("不存在会话 detail 必须非 0")
	}
	if !strings.Contains(errb, "session:999") {
		t.Fatalf("报错应含会话 id（可行动）: %q", errb)
	}
}

// TestSessionJoinLifecycle 锁：join 成功 {"ok":true} + 同会话重复 join 幂等 0
//（契约条 10）+ 已属他会话非 0 且含「已属会话」（ErrBadState 文案）+ 不存在卡/
// 会话非 0。
func TestSessionJoinLifecycle(t *testing.T) {
	dir := t.TempDir()
	_, _, _, sessionID, _ := mustSendFixture(t, dir, "拉卡场")
	_, _, _, otherID, _ := mustSendFixture(t, dir, "另一场")
	card := mustAddCard(t, dir, "进群卡")
	out, _, err := runLedgerCLI(t, dir, "session", "join", sessionID, card)
	if err != nil || !strings.Contains(out, `"ok":true`) {
		t.Fatalf("session join: err=%v out=%q", err, out)
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "join", sessionID, card); err != nil {
		t.Fatalf("同会话重复 join 应幂等 0（契约条 10）: %v", err)
	}
	_, errb, err := runLedgerCLI(t, dir, "session", "join", otherID, card)
	if err == nil {
		t.Fatal("已属他会话的卡必须非 0")
	}
	if !strings.Contains(errb, "已属会话") {
		t.Fatalf("报错应可行动（含「已属会话」）: %q", errb)
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "join", sessionID, "B99999"); err == nil {
		t.Fatal("不存在卡必须非 0")
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "join", "session:999", card); err == nil {
		t.Fatal("不存在会话必须非 0")
	}
}

// TestSessionLeaveLifecycle 锁：leave 成功 + 详情 cards 清空 + 不在会话内重复
// leave 幂等 0（契约条 11）+ 不存在会话非 0。
func TestSessionLeaveLifecycle(t *testing.T) {
	dir := t.TempDir()
	_, _, _, sessionID, _ := mustSendFixture(t, dir, "移出场")
	card := mustAddCard(t, dir, "移出卡")
	if _, _, err := runLedgerCLI(t, dir, "session", "join", sessionID, card); err != nil {
		t.Fatalf("夹具 join: %v", err)
	}
	out, _, err := runLedgerCLI(t, dir, "session", "leave", sessionID, card)
	if err != nil || !strings.Contains(out, `"ok":true`) {
		t.Fatalf("session leave: err=%v out=%q", err, out)
	}
	outJSON, _, err := runLedgerCLI(t, dir, "session", "detail", sessionID, "--json")
	if err != nil {
		t.Fatal(err)
	}
	var detail proto.SessionDetail
	if err := json.Unmarshal([]byte(strings.TrimSpace(outJSON)), &detail); err != nil {
		t.Fatal(err)
	}
	if len(detail.Summary.Cards) != 0 {
		t.Fatalf("移出后详情 cards 应空: %+v", detail.Summary.Cards)
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "leave", sessionID, card); err != nil {
		t.Fatalf("不在会话内重复 leave 应幂等 0（契约条 11）: %v", err)
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "leave", "session:999", card); err == nil {
		t.Fatal("不存在会话 leave 必须非 0")
	}
}

// TestSessionSendNegativePaths 锁 send 全部负例：用法/执法负例一段（收口断言
// 零落账），归档负例一段（归档夹具本身落 EvSessionArchived，故零落账收口只
// 罩前一段——段序不能倒）。(a) 席位 flag 单只 → 用法错（缺陷族 4 成对反例，
// 两向）；(b) 空正文；(c) 自报席位不在书写者集 → ErrNotWriter（缺陷族 5 反例：
// 无冒充直通 flag，席位必须是会话内卡当前席位）；(d) 不存在会话 → 含 id；
// (e) 归档后 send 含「只读」、join 含「已归档」（哨兵差异：ErrReadOnly vs
// ErrBadState——breakdown §3.6 ③ 的 join 半边判据字面修订见岔口 4）。
func TestSessionSendNegativePaths(t *testing.T) {
	dir := t.TempDir()
	_, facade, st, sessionID, _ := mustSendFixture(t, dir, "发送负例场")
	card := mustAddCard(t, dir, "负例卡")
	clearSeatSourceEnv(t)
	before, err := st.EventsFromAsc(nil, 0, 10000)
	if err != nil {
		t.Fatal(err)
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "x", "--cli", "grok"); err == nil {
		t.Fatal("只给 --cli 必须拒绝（成对 flag）")
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "x", "--session", "sid"); err == nil {
		t.Fatal("只给 --session 必须拒绝（成对 flag）")
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "   "); err == nil {
		t.Fatal("空正文必须拒绝")
	}
	_, errb, err := runLedgerCLI(t, dir, "session", "send", sessionID, "冒名", "--cli", "grok", "--session", "notseated")
	if err == nil {
		t.Fatal("自报席位不在书写者集必须非 0")
	}
	if !strings.Contains(errb, "书写者") {
		t.Fatalf("报错应含书写者语义（ErrNotWriter）: %q", errb)
	}
	_, errb, err = runLedgerCLI(t, dir, "session", "send", "session:999", "x")
	if err == nil {
		t.Fatal("不存在会话 send 必须非 0")
	}
	if !strings.Contains(errb, "session:999") {
		t.Fatalf("报错应含会话 id（可行动）: %q", errb)
	}
	// 用法/执法负例零落账收口（归档前的全部负例）。
	after, err := st.EventsFromAsc(nil, 0, 10000)
	if err != nil {
		t.Fatal(err)
	}
	if len(after) != len(before) {
		t.Fatalf("用法与执法负例全程零落账: before=%d after=%d", len(before), len(after))
	}
	// 归档负例段：归档夹具本身落 EvSessionArchived（不参与零落账收口）。
	if err := facade.ArchiveSession(sessionID, "user:tester"); err != nil {
		t.Fatalf("夹具归档: %v", err)
	}
	_, errb, err = runLedgerCLI(t, dir, "session", "send", sessionID, "归档后发言")
	if err == nil {
		t.Fatal("归档会话 send 必须非 0")
	}
	if !strings.Contains(errb, "只读") {
		t.Fatalf("报错应含只读语义（ErrReadOnly）: %q", errb)
	}
	_, errb, err = runLedgerCLI(t, dir, "session", "join", sessionID, card)
	if err == nil {
		t.Fatal("归档会话 join 必须非 0")
	}
	if !strings.Contains(errb, "已归档") {
		t.Fatalf("报错应可行动（含「已归档」，ErrBadState）: %q", errb)
	}
}

// TestSessionArchiveCommand 锁：archive 成功 {"ok":true} + 重复归档幂等 0
//（契约条 6，Store 级短路）+ 详情 archived=true + 不存在会话非 0 且含 id。
func TestSessionArchiveCommand(t *testing.T) {
	dir := t.TempDir()
	_, _, _, sessionID, _ := mustSendFixture(t, dir, "归档场")
	out, _, err := runLedgerCLI(t, dir, "session", "archive", sessionID)
	if err != nil || !strings.Contains(out, `"ok":true`) {
		t.Fatalf("session archive: err=%v out=%q", err, out)
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "archive", sessionID); err != nil {
		t.Fatalf("重复归档应幂等 0（契约条 6）: %v", err)
	}
	outJSON, _, err := runLedgerCLI(t, dir, "session", "detail", sessionID, "--json")
	if err != nil {
		t.Fatal(err)
	}
	var detail proto.SessionDetail
	if err := json.Unmarshal([]byte(strings.TrimSpace(outJSON)), &detail); err != nil {
		t.Fatal(err)
	}
	if !detail.Summary.Archived {
		t.Fatalf("归档后 summary.archived 应 true: %+v", detail.Summary)
	}
	_, errb, err := runLedgerCLI(t, dir, "session", "archive", "session:999")
	if err == nil {
		t.Fatal("不存在会话 archive 必须非 0")
	}
	if !strings.Contains(errb, "session:999") {
		t.Fatalf("报错应含会话 id（可行动）: %q", errb)
	}
}
```

跑红：

```
$ go test ./cmd -run 'TestSessionCreateCommand|TestSessionListColumnsAndUnread|TestSessionDetailThreeSections|TestSessionJoinLifecycle|TestSessionLeaveLifecycle|TestSessionSendNegativePaths|TestSessionArchiveCommand' -count=1 -v
--- FAIL: TestSessionCreateCommand（未知命令 "create" for "handoff session"）
--- FAIL: TestSessionListColumnsAndUnread / TestSessionDetailThreeSections /
         TestSessionJoinLifecycle / TestSessionLeaveLifecycle /
         TestSessionSendNegativePaths / TestSessionArchiveCommand
（红因 = 子命令不存在——功能缺失非 typo；B358.3 的 TestSessionWait 七支保持 PASS）
```

**步骤 2（绿：最小实现）**：`cmd/session.go` 续写（追加在 `buildSessionWake` 之后、`init` 之前；完整代码，直接落）。文件 import 增 `"sort"`、`"strings"`、`"text/tabwriter"` 三行（stdlib，无新跨域边）：

```go
// —— B358.5 续写：list/detail/create/archive/join/leave/send 六子命令 ——
//
// 身份两字段（B358.4 拍板① 的 CLI 半边，docs/superpowers/plans/b358.5-plan.md §2.2）：
//   - 成员身份（--owner、--member、席位出示值）用统一记法 user:<name>/agent:<name>
//     并校验前缀——owner 是会话初始成员（契约条 3），web:<host> 是机器位不配当成员；
//   - 审计 actor 沿 CLI 既有注入面 ledgerActor()（cli:<user>@<host>），两字段不混用。
// session send 两形态：无席位 flag=人（ledgerActor）；--cli/--session 成对=协调者
// 席位（currentSeatIdentity 只消费 flag 对，环境席位键不参与——身份显式性与会话
// wait 的显式 member 对称）；单只 flag 用法错。kind 恒 user（发言自由，spec §4.2）。

// validSessionMemberIdentity 校验会话成员统一记法：只认 user:<name>/agent:<name>
// 且前缀后非空。与 gateway 侧 internal/agentd/sessionsapi.go#validSessionOwner
// 同规则两处实现——cmd 不得 import agentd（域方向），漂移由两侧测试钉住
//（TestSessionsCreateEndpoint / TestSessionCreateCommand）；统一收口归后续重构卡
//（b358.5-plan 岔口 6）。
func validSessionMemberIdentity(identity string) bool {
	if rest, ok := strings.CutPrefix(identity, "user:"); ok && rest != "" {
		return true
	}
	rest, ok := strings.CutPrefix(identity, "agent:")
	return ok && rest != ""
}

var sessionCreateOwner string

var sessionCreateCmd = &cobra.Command{
	Use:   "create <title>",
	Short: "建一场会话（群）：--owner 指定群主（统一记法 user:<name>/agent:<name>）",
	Args:  cobra.ExactArgs(1),
	RunE: func(cmd *cobra.Command, args []string) error {
		title := strings.TrimSpace(args[0])
		if title == "" {
			return fmt.Errorf("会话标题不能为空")
		}
		owner := strings.TrimSpace(sessionCreateOwner)
		if !validSessionMemberIdentity(owner) {
			return fmt.Errorf("--owner 必须是统一记法 user:<name> 或 agent:<name>（当前 %q）", sessionCreateOwner)
		}
		svc, st, err := openRoomService()
		if err != nil {
			return err
		}
		defer st.Close()
		actor := ledgerActor()
		session, err := svc.CreateSession(title, owner, actor)
		if err != nil {
			slog.Default().Warn("CLI 建会话失败", "title", title, "owner", owner, "actor", actor, "cause", err)
			return fmt.Errorf("建会话: %w", err)
		}
		slog.Default().Info("CLI 会话已创建", "session", session.ID, "title", title, "owner", owner, "actor", actor)
		return json.NewEncoder(cmd.OutOrStdout()).Encode(session)
	},
}

var (
	sessionListMember string
	sessionListJSON   bool
)

var sessionListCmd = &cobra.Command{
	Use:   "list",
	Short: "列会话（谁需要我：未读、需要你标签；--member 投影未读；--json 机读）",
	Args:  cobra.NoArgs,
	RunE: func(cmd *cobra.Command, _ []string) error {
		svc, st, err := openRoomService()
		if err != nil {
			return err
		}
		defer st.Close()
		summaries, err := svc.ListSessions(sessionListMember)
		if err != nil {
			slog.Default().Warn("CLI 会话列表失败", "member", sessionListMember, "cause", err)
			return fmt.Errorf("列会话: %w", err)
		}
		// 呈现排序（cmd 层）：最近活动降序（谁需要我语义）；门面返回创建序不动
		//（0.2#11）。稳定排序保平手时创建序，输出形状确定（RFC3339Nano 纳秒精度，
		// 0.2#15）。
		sort.SliceStable(summaries, func(i, j int) bool {
			return summaries[i].LastActivity.After(summaries[j].LastActivity)
		})
		if sessionListJSON {
			enc := json.NewEncoder(cmd.OutOrStdout())
			for _, summary := range summaries {
				if err := enc.Encode(summary); err != nil {
					return err
				}
			}
			slog.Default().Info("CLI 会话列表已输出", "member", sessionListMember, "count", len(summaries), "format", "json")
			return nil
		}
		w := tabwriter.NewWriter(cmd.OutOrStdout(), 2, 4, 2, ' ', 0)
		fmt.Fprintln(w, "ID\t标题\t群主\t归档\t未读\t需要你\t最近活动")
		for _, summary := range summaries {
			needs := ""
			if summary.NeedsHuman {
				needs = "需要你"
			}
			fmt.Fprintf(w, "%s\t%s\t%s\t%v\t%d\t%s\t%s\n",
				summary.ID, summary.Title, summary.Owner, summary.Archived,
				summary.Unread, needs, summary.LastActivity.Format("2006-01-02 15:04:05"))
		}
		slog.Default().Info("CLI 会话列表已输出", "member", sessionListMember, "count", len(summaries))
		return w.Flush()
	},
}

var sessionDetailJSON bool

var sessionDetailCmd = &cobra.Command{
	Use:   "detail <session>",
	Short: "会话详情三块：成员 / 派发节点 / timeline（--json 机读）",
	Args:  cobra.ExactArgs(1),
	RunE: func(cmd *cobra.Command, args []string) error {
		svc, st, err := openRoomService()
		if err != nil {
			return err
		}
		defer st.Close()
		detail, err := svc.SessionDetail(args[0])
		if err != nil {
			slog.Default().Warn("CLI 会话详情失败", "session", args[0], "cause", err)
			// mapSessionError 已把 ErrNotFound 替换成裸 ErrNoRoom（0.2#4，Store 层
			// 含 id 文案丢失）——边界把 id 包回去（gateway handleSessionDetail 先例）。
			return fmt.Errorf("会话 %s: %w", args[0], err)
		}
		if sessionDetailJSON {
			slog.Default().Info("CLI 会话详情已输出", "session", args[0], "format", "json")
			return json.NewEncoder(cmd.OutOrStdout()).Encode(detail)
		}
		w := tabwriter.NewWriter(cmd.OutOrStdout(), 2, 4, 2, ' ', 0)
		fmt.Fprintln(w, "成员:")
		for _, m := range detail.Summary.Members {
			card := m.CardID
			if card == "" {
				card = "-"
			}
			fmt.Fprintf(w, "%s\t%s\t%s\t%s\n", m.Identity, m.Kind, m.Status, card)
		}
		fmt.Fprintln(w, "节点:")
		for _, n := range detail.Nodes {
			card := n.CardID
			if card == "" {
				card = "-"
			}
			fmt.Fprintf(w, "%s\t%s\t%s\n", card, n.Node, n.State)
		}
		fmt.Fprintln(w, "时间线:")
		for _, ev := range detail.Timeline {
			card := ev.CardID
			if card == "" {
				card = "-"
			}
			fmt.Fprintf(w, "#%d\t%s\t%s\t%s\n", ev.Seq, ev.Kind, card, ev.Detail)
		}
		slog.Default().Info("CLI 会话详情已输出", "session", args[0],
			"members", len(detail.Summary.Members), "nodes", len(detail.Nodes), "timeline", len(detail.Timeline))
		return w.Flush()
	},
}

var sessionArchiveCmd = &cobra.Command{
	Use:   "archive <session>",
	Short: "归档会话（幂等；归档后只读，卡的终态不等于会话结束）",
	Args:  cobra.ExactArgs(1),
	RunE: func(cmd *cobra.Command, args []string) error {
		svc, st, err := openRoomService()
		if err != nil {
			return err
		}
		defer st.Close()
		actor := ledgerActor()
		if err := svc.ArchiveSession(args[0], actor); err != nil {
			slog.Default().Warn("CLI 归档会话失败", "session", args[0], "actor", actor, "cause", err)
			return fmt.Errorf("归档会话 %s: %w", args[0], err)
		}
		slog.Default().Info("CLI 会话已归档", "session", args[0], "actor", actor)
		return json.NewEncoder(cmd.OutOrStdout()).Encode(map[string]bool{"ok": true})
	},
}

var sessionJoinCmd = &cobra.Command{
	Use:   "join <session> <card>",
	Short: "拉卡进会话（进群≠配人，配人仍走卡上三按钮；一卡同时只挂一个会话）",
	Args:  cobra.ExactArgs(2),
	RunE: func(cmd *cobra.Command, args []string) error {
		svc, st, err := openRoomService()
		if err != nil {
			return err
		}
		defer st.Close()
		actor := ledgerActor()
		if err := svc.JoinCard(args[0], args[1], actor); err != nil {
			slog.Default().Warn("CLI 拉卡进群失败", "session", args[0], "card", args[1], "actor", actor, "cause", err)
			return fmt.Errorf("session join %s %s: %w", args[0], args[1], err)
		}
		slog.Default().Info("CLI 卡已进群", "session", args[0], "card", args[1], "actor", actor)
		return json.NewEncoder(cmd.OutOrStdout()).Encode(map[string]bool{"ok": true})
	},
}

var sessionLeaveCmd = &cobra.Command{
	Use:   "leave <session> <card>",
	Short: "把卡移出会话（幂等：不在会话内亦成功）",
	Args:  cobra.ExactArgs(2),
	RunE: func(cmd *cobra.Command, args []string) error {
		svc, st, err := openRoomService()
		if err != nil {
			return err
		}
		defer st.Close()
		actor := ledgerActor()
		if err := svc.LeaveCard(args[0], args[1], actor); err != nil {
			slog.Default().Warn("CLI 移卡出群失败", "session", args[0], "card", args[1], "actor", actor, "cause", err)
			return fmt.Errorf("session leave %s %s: %w", args[0], args[1], err)
		}
		slog.Default().Info("CLI 卡已移出会话", "session", args[0], "card", args[1], "actor", actor)
		return json.NewEncoder(cmd.OutOrStdout()).Encode(map[string]bool{"ok": true})
	},
}

var (
	sessionSendRefs    []string
	sessionSendMention []string
	sessionSendCLI     string
	sessionSendSession string
)

var sessionSendCmd = &cobra.Command{
	Use:   "send <session> <text...>",
	Short: "会话发言（不分 kind，各自以自己名义；协调者用 --cli/--session 成对出示席位身份）",
	Args:  cobra.MinimumNArgs(2),
	RunE: func(cmd *cobra.Command, args []string) error {
		// 身份判定先于开账本：用法错误不触账本（0 落账）。两形态（§2.2）：
		// 成对 flag=席位（自报身份需成对 flag——旧 room send 守卫的替代形状，
		// breakdown §3.6 ③）；无 flag=人（ledgerActor）。环境席位键不参与本命令。
		cliChanged := cmd.Flags().Changed("cli")
		sessChanged := cmd.Flags().Changed("session")
		if cliChanged != sessChanged {
			return fmt.Errorf("席位身份必须成对出示：--cli 与 --session 需同时给出（自报身份需成对 flag）")
		}
		body := strings.TrimSpace(strings.Join(args[1:], " "))
		if body == "" {
			return fmt.Errorf("消息正文不能为空")
		}
		var actor string
		if cliChanged {
			identity, err := currentSeatIdentity(sessionSendCLI, sessionSendSession)
			if err != nil {
				return err
			}
			actor = identity
		} else {
			actor = ledgerActor()
		}
		svc, st, err := openRoomService()
		if err != nil {
			return err
		}
		defer st.Close()
		msg := proto.RoomMessage{
			Kind: proto.RoomMsgUser, Body: body,
			Refs: sessionSendRefs, Mentions: sessionSendMention,
		}
		seq, err := svc.Send(args[0], msg, actor)
		if err != nil {
			slog.Default().Warn("CLI 会话消息发送失败", "session", args[0], "actor", actor, "cause", err)
			return fmt.Errorf("发送到 %s: %w", args[0], err)
		}
		slog.Default().Info("CLI 会话消息已发送", "session", args[0], "kind", proto.RoomMsgUser, "actor", actor, "seq", seq)
		return json.NewEncoder(cmd.OutOrStdout()).Encode(map[string]any{"ok": true, "seq": seq})
	},
}
```

**步骤 3**：`cmd/session.go` 既有 `init()` 内、`sessionCmd.AddCommand(sessionWaitCmd)` 行之后追加（wait 的 Flags/AddCommand/rootCmd 三行零改动）：

```go
	sessionCreateCmd.Flags().StringVar(&sessionCreateOwner, "owner", "", "群主身份（统一记法 user:<name>/agent:<name>，必填）")
	sessionListCmd.Flags().StringVar(&sessionListMember, "member", "", "投影该成员身份的未读数（缺省不投影）")
	sessionListCmd.Flags().BoolVar(&sessionListJSON, "json", false, "输出 SessionSummary JSON 流（每会话一行）")
	sessionDetailCmd.Flags().BoolVar(&sessionDetailJSON, "json", false, "输出 SessionDetail JSON（一行）")
	sessionSendCmd.Flags().StringArrayVar(&sessionSendRefs, "ref", nil, "引用锚（git 路径/timeline 锚/卡号/附件路径，可重复）")
	sessionSendCmd.Flags().StringArrayVar(&sessionSendMention, "mention", nil, "@成员或卡号（可重复；寻址唤醒的来源）")
	sessionSendCmd.Flags().StringVar(&sessionSendCLI, "cli", "", "手填当前会话物种名（需与 --session 成对）")
	sessionSendCmd.Flags().StringVar(&sessionSendSession, "session", "", "手填当前会话 id（需与 --cli 成对）")
	sessionCmd.AddCommand(sessionCreateCmd, sessionListCmd, sessionDetailCmd, sessionArchiveCmd, sessionJoinCmd, sessionLeaveCmd, sessionSendCmd)
```

并在文件头注释（`cmd/session.go:1-11`）末尾追加一行：

```go
// B358.5 已续写六子命令（create/list/detail/archive/join/leave/send）——交界
// 兑现；wait 段零改动。
```

**步骤 4（绿）**：

```
$ go build ./...   → 退出 0
$ go test ./cmd -run 'TestSession' -count=1 → ok（新 7 支 + B358.3 wait 七支全绿）
```

**⚠ 步骤 5（岔口 1 处置，先看 §11.1 再动手）**：同文件落码后 `go test ./cmd -run TestSessionWaitSourceGuard -count=1` **必然红**——守卫对 `session.go` 全文件禁子串 `"proto.RoomMsgUser"`，而 session send 的 `Kind: proto.RoomMsgUser` 合法引用它（红因是守卫过 Fire，不是本卡违规）。**处置须经协调者在派工报文/审查意见中明示**：

- **方案 A（推荐，已批准时执行）**：把 `cmd/session_test.go#TestSessionWaitSourceGuard` 的禁令收窄为下述版本（wait 通道纯净度断言不降级——kind 门控与 mentions 直读改为 wait 两函数的 AST 级禁令，广播帮手名保留文件级；结构体字面量键 `Mentions:` 不是 SelectorExpr 天然豁免，精确度高于子串）。替换整个函数体（既有测试清单只此一支被授权触碰，变异验证后还原）：

```go
// TestSessionWaitSourceGuard 是 R1 通道的源码级守卫（契约条 42：命中判定
// 唯一入口是 collab.Service.MessageWakeTargets；通道内不得出现第二份寻址
// 判定，无成员集合形状）。B358.5 岔口 1 批准后收窄：kind 门控（RoomMsgUser）
// 与 mentions 直读（.Mentions）两类禁令从文件级子串改为 wait 通道两函数
//（runSessionWait/buildSessionWake）的 AST 级禁令——同文件续写的 session
// send 合法引用 proto.RoomMsgUser 构造发言、不在 wait 路径；广播帮手名仍是
// 文件级禁令。结构体字面量键（Mentions: …）不是 SelectorExpr，天然豁免。
func TestSessionWaitSourceGuard(t *testing.T) {
	raw, err := os.ReadFile("session.go")
	if err != nil {
		t.Fatalf("读 session.go 源: %v", err)
	}
	src := string(raw)
	for _, banned := range []string{
		"broadcastToMembers", "fanOutToMembers", "notifyAllMembers", "wakeAllMembers",
	} {
		if strings.Contains(src, banned) {
			t.Errorf("session.go 出现禁令形状 %q——通道内不得重造寻址判定（条 42）", banned)
		}
	}
	fset := token.NewFileSet()
	f, err := parser.ParseFile(fset, "session.go", src, 0)
	if err != nil {
		t.Fatalf("解析 session.go: %v", err)
	}
	waitFuncs := map[string]*ast.FuncDecl{}
	for _, name := range []string{"runSessionWait", "buildSessionWake"} {
		ast.Inspect(f, func(n ast.Node) bool {
			if d, ok := n.(*ast.FuncDecl); ok && d.Name.Name == name {
				waitFuncs[name] = d
			}
			return true
		})
		if waitFuncs[name] == nil {
			t.Fatalf("%s 不存在——通道本体缺失", name)
		}
	}
	callsAddressing := false
	for _, name := range []string{"runSessionWait", "buildSessionWake"} {
		ast.Inspect(waitFuncs[name], func(n ast.Node) bool {
			switch node := n.(type) {
			case *ast.CallExpr:
				if sel, ok := node.Fun.(*ast.SelectorExpr); ok && sel.Sel.Name == "MessageWakeTargets" {
					callsAddressing = true
				}
			case *ast.SelectorExpr:
				if node.Sel.Name == "RoomMsgUser" || node.Sel.Name == "Mentions" {
					t.Errorf("%s 出现 kind 门控/mentions 直读形状 %q——通道内不得重造寻址判定（条 42）", name, node.Sel.Name)
				}
			}
			return true
		})
	}
	if !callsAddressing {
		t.Fatalf("通道未经 MessageWakeTargets——命中判定唯一入口被绕过（条 42）")
	}
}
```

  收窄后**变异验证两发**（先红后还原绿，不提交变异）：① 在 `runSessionWait` 内临时插入 `if msg.Kind == proto.RoomMsgUser { hit = true }` → 守卫必须红（`RoomMsgUser` 在 wait 函数内）；② 临时插入 `for _, m := range msg.Mentions { _ = m }` → 守卫必须红（`Mentions` 直读）。还原后绿。变异记录落台账。
- **方案 B（守卫一行不动的替代）**：六子命令落**新文件 `cmd/session_commands.go`**（cmd var + RunE + validSessionMemberIdentity + 该文件自己的 init() 注册行——`sessionCmd.AddCommand(...)` 跨文件聚合是 cobra 既有机制；`cmd/session.go` 零改动），session_test.go 追加不变。代价：偏离「同文件续写」交界（B358.3 拍板岔口#2 需协调者改判一行）、`session.go` 头注释「S5 续写」指针变陈旧（由新文件头注释指认实际落点补偿）。

- **派工报文未明示时**：执行者停在步骤 4 收尾（六子命令已绿、红窗未动），上报协调者裁决，**不得自行改守卫或自行拆文件**。

### Task 1 验收（字面断言）

1. `go build ./...` 退出 0；`go test ./cmd -run 'TestSession' -count=1` 退出 0（新 7 支 + wait 七支全绿）。
2. **stdout 形状字面**：create → `proto.Session` JSON 一行且 `id` 前缀 `session:`；list 文本表头恰七列（§2.3）；list `--json` 每行 Unmarshal 回 `proto.SessionSummary`；detail `--json` 顶层键恰 `{summary,timeline}`；send → `{"ok":true,"seq":<n>}`；archive/join/leave → `{"ok":true}`。
3. **退出码契约字面**：owner 前缀/空标题/单只席位 flag/空正文 → 非 0；已属他会话含「已属会话」、归档 send 含「只读」、归档 join 含「已归档」、非成员含「书写者」、不存在会话的 detail/archive/send stderr 含会话 id；join/leave/archive 幂等路径退出 0。
4. **身份两字段不混用**：EvSessionCreated 的 actor = `ledgerActor()`（TestSessionCreateCommand）；owner 前缀校验四反例全拒。
5. 守卫处置按 §11.1 裁决执行（方案 A 含变异两发记录，或方案 B 落新文件）——**裁决前步骤 5 不得执行**。

## 5. Task 2：红窗 6 支改写收口（cmd/room_test.go 就地改写）

### 文件范围

仅 `cmd/room_test.go`；只改 §0.3 清单 6 支的函数体与文件头注释追加一行，**其余测试与 helper 一字不动**。

### Interfaces

Consumes：Task 1 全部六子命令 + `mustSendFixture`（session_test.go，同包可见）+ `mustAddCard`/`clearSeatSourceEnv`（同包既有）。

Produces：6 支同名测试（改写后绿），红窗核算对账面。

### 步骤

**步骤 1**：文件头注释（`cmd/room_test.go:1-4`）追加一行：

```go
// B358.5 红窗改写：下方标注「B358.5 红窗改写」的 6 支原以 room send 卡房间/
// 群房间成功为断言或夹具，随 B358.1 S1 只读归档失效；按新会话语义就地改写
//（名字保留供红窗核算逐支比对），断言意图逐支对应迁移，见
// docs/superpowers/plans/b358.5-plan.md §5。
```

**步骤 2**：逐支改写（全码，直接落；发送者身份一律用夹具已坐进成员列的身份——人形态 `ledgerActor()` 或已坐席卡的席位）：

| # | 测试（名字保留） | 改写内容 |
|---|---|---|
| 1 | `TestRoomSendLandsRoomMessageWithUserKind` | (a) 新增旧面报错形状断言：`room send <卡号>` 非 0 + stderr 含「只读」（P7/S1 经 CLI 呈现）；(b) kind=user 落账成功断言迁 session send 人形态：actor=`cli:` 前缀、body 逐字、stdout `{"ok":true,"seq":<n>}` 契约不变 |
| 2 | `TestRoomSendCoordinatorAcceptsExplicitSeatFlags` | 席位落账断言迁 session send 成对 flag 形态：卡进群+BindSeat 夹具后 `--cli claude --session room-seat` → actor=`cli:claude#room-seat`；旧断言 `kind=reply` 随白名单废止收敛为 **kind=user**（breakdown §3.6 ③） |
| 3 | `TestRoomSendCoordinatorKindUsesGrokHostSession` | grok 宿主值保留为 **flag 值**（`--cli grok --session grok-room`）：会话面身份按 flag 显式出示（岔口 11.3 定稿，环境键不参与），`clearSeatSourceEnv` 保留证明 flag 单独足够 → actor=`cli:grok#grok-room`、kind=user |
| 4 | `TestRoomSendCarriesRefAndMention` | `--ref/--mention` 可重复、进载荷断言原样迁 session send；读账本改全流读（会话消息无卡事件） |
| 5 | `TestRoomReadStdoutSeqPrefix` | `#<seq>` 前缀与 `--after` 排他断言照抄，夹具发言改 session send、读面用 `room read <session:n>`（History 的 SameRoom 会话分支既有）；**新增**旧房间读面对质保留断言：`room read <卡号>` 退出 0（S1 P5 读史不断） |
| 6 | `TestRoomListSortedByActivity` | 活动降序断言迁 `session list`（旧列表无法经发言复现——0.2 表 #20）：两场会话先后发言 → 后发言的排前（cmd 层呈现排序，§2.1） |

逐支代码（改写后全文，直接落）：

```go
// TestRoomSendLandsRoomMessageWithUserKind（B358.5 红窗改写）：原断言「room send
// 卡房间 kind=user 落账成功」随 S1 只读归档失效。改锁两半：(a) 旧面报错形状
//（P7 保留报错——S1 归档语义经 CLI 呈现）；(b) kind=user 落账成功迁 session send
// 人形态，actor 沿用 cli:<user>@<host> 审计注入面，stdout 契约不变。
func TestRoomSendLandsRoomMessageWithUserKind(t *testing.T) {
	dir := t.TempDir()
	id := mustAddCard(t, dir, "房间发言卡")
	_, errb, err := runLedgerCLI(t, dir, "room", "send", id, "先停一下")
	if err == nil {
		t.Fatal("旧卡房间 room send 必须非 0（S1 只读归档，P7 保留报错形状）")
	}
	if !strings.Contains(errb, "只读") {
		t.Fatalf("报错应含只读语义（可行动）: %q", errb)
	}
	_, _, st, sessionID, actor := mustSendFixture(t, dir, "人形态发送场")
	out, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "先停一下")
	if err != nil {
		t.Fatalf("session send: %v", err)
	}
	var resp struct {
		OK  bool  `json:"ok"`
		Seq int64 `json:"seq"`
	}
	if err := json.Unmarshal([]byte(strings.TrimSpace(out)), &resp); err != nil || !resp.OK || resp.Seq <= 0 {
		t.Fatalf("session send stdout = %q err=%v", out, err)
	}
	// 会话消息是无卡事件 → 全流读（EventsFromAsc(nil,…)），按 seq 定位。
	events, err := st.EventsFromAsc(nil, 0, 1000)
	if err != nil {
		t.Fatalf("events: %v", err)
	}
	var found *ledger.Event
	for i := range events {
		if events[i].Type == ledger.EvRoomMessage && events[i].Seq == resp.Seq {
			found = &events[i]
		}
	}
	if found == nil {
		t.Fatalf("账本没有该 seq 的 room_message 行: %+v", events)
	}
	if !strings.HasPrefix(found.Actor, "cli:") || found.Actor != actor {
		t.Fatalf("actor 应沿用 cli:<user>@<host> 注入面（%q）: %q", actor, found.Actor)
	}
	var msg proto.RoomMessage
	if err := json.Unmarshal(found.Payload, &msg); err != nil {
		t.Fatalf("解 payload: %v", err)
	}
	if msg.Kind != proto.RoomMsgUser {
		t.Fatalf("人形态 kind 应为 user: %q", msg.Kind)
	}
	if msg.Body != "先停一下" {
		t.Fatalf("正文不符: %q", msg.Body)
	}
}

// TestRoomSendCoordinatorAcceptsExplicitSeatFlags（B358.5 红窗改写）：席位落账
// 断言迁 session send 成对 flag 形态。夹具：卡进群 + BindSeat——席位身份因
// 「会话内卡当前席位」入书写者集（0.2#3）。旧断言 kind=reply 随白名单废止
// 收敛为 kind=user（breakdown §3.6 ③）。
func TestRoomSendCoordinatorAcceptsExplicitSeatFlags(t *testing.T) {
	clearSeatSourceEnv(t)
	dir := t.TempDir()
	id := mustAddCard(t, dir, "手填房间协调者卡")
	_, facade, st, sessionID, _ := mustSendFixture(t, dir, "协调者席位场")
	if err := facade.JoinCardToSession(sessionID, id, "user:tester"); err != nil {
		t.Fatalf("夹具拉卡: %v", err)
	}
	if err := st.BindSeat(id, "cli:claude#room-seat", proto.SeatSourceBind); err != nil {
		t.Fatal(err)
	}
	out, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "协调者正文",
		"--cli", "claude", "--session", "room-seat")
	if err != nil {
		t.Fatalf("session send with explicit identity: %v", err)
	}
	var response struct {
		Seq int64 `json:"seq"`
	}
	if err := json.Unmarshal([]byte(strings.TrimSpace(out)), &response); err != nil || response.Seq <= 0 {
		t.Fatalf("session send stdout = %q, err=%v", out, err)
	}
	events, err := st.EventsFromAsc(nil, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	for _, event := range events {
		if event.Seq != response.Seq {
			continue
		}
		if event.Actor != "cli:claude#room-seat" {
			t.Fatalf("session actor = %q, want cli:claude#room-seat", event.Actor)
		}
		var message proto.RoomMessage
		if err := json.Unmarshal(event.Payload, &message); err != nil {
			t.Fatal(err)
		}
		if message.Kind != proto.RoomMsgUser || message.Body != "协调者正文" {
			t.Fatalf("session message = %+v", message)
		}
		return
	}
	t.Fatalf("没有找到 seq=%d 的 room_message", response.Seq)
}

// TestRoomSendCoordinatorKindUsesGrokHostSession（B358.5 红窗改写）：原断言
// 「room send --kind reply 经 GROK_SESSION_ID 环境出示席位」随旧面只读失效。
// 会话面身份按 flag 显式出示（b358.5-plan §2.2 定稿点 2——环境席位键不参与
// session send，与 session wait 的显式 member 对称）；grok 宿主值保留作 flag
// 值，clearSeatSourceEnv 证明 flag 单独足够。落账 actor=cli:grok#grok-room、
// kind=user、body 逐字一致。
func TestRoomSendCoordinatorKindUsesGrokHostSession(t *testing.T) {
	clearSeatSourceEnv(t)
	dir := t.TempDir()
	id := mustAddCard(t, dir, "grok 宿主房间协调者卡")
	_, facade, st, sessionID, _ := mustSendFixture(t, dir, "grok 席位场")
	if err := facade.JoinCardToSession(sessionID, id, "user:tester"); err != nil {
		t.Fatalf("夹具拉卡: %v", err)
	}
	if err := st.BindSeat(id, "cli:grok#grok-room", proto.SeatSourceBind); err != nil {
		t.Fatal(err)
	}
	out, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "grok 宿主回复",
		"--cli", "grok", "--session", "grok-room")
	if err != nil {
		t.Fatalf("session send with grok identity flags: %v", err)
	}
	var resp struct {
		Seq int64 `json:"seq"`
	}
	if err := json.Unmarshal([]byte(strings.TrimSpace(out)), &resp); err != nil || resp.Seq <= 0 {
		t.Fatalf("session send stdout = %q, err=%v", out, err)
	}
	events, err := st.EventsFromAsc(nil, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	for _, event := range events {
		if event.Seq != resp.Seq {
			continue
		}
		if event.Actor != "cli:grok#grok-room" {
			t.Fatalf("session actor = %q, want cli:grok#grok-room", event.Actor)
		}
		var message proto.RoomMessage
		if err := json.Unmarshal(event.Payload, &message); err != nil {
			t.Fatal(err)
		}
		if message.Kind != proto.RoomMsgUser || message.Body != "grok 宿主回复" {
			t.Fatalf("session message = %+v", message)
		}
		return
	}
	t.Fatalf("没有找到 seq=%d 的 room_message", resp.Seq)
}

// TestRoomSendCarriesRefAndMention（B358.5 红窗改写）：--ref/--mention 可重复、
// 进载荷断言迁 session send（写入面只在会话房间）；读账本改全流读。
func TestRoomSendCarriesRefAndMention(t *testing.T) {
	dir := t.TempDir()
	_, _, st, sessionID, _ := mustSendFixture(t, dir, "引用场")
	out, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "正文",
		"--ref", "docs/x.md", "--ref", "B156", "--mention", "B145")
	if err != nil {
		t.Fatalf("session send: %v", err)
	}
	var resp struct {
		Seq int64 `json:"seq"`
	}
	if err := json.Unmarshal([]byte(strings.TrimSpace(out)), &resp); err != nil {
		t.Fatalf("解析 send 输出: %v", err)
	}
	events, err := st.EventsFromAsc(nil, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	for i := range events {
		if events[i].Seq != resp.Seq {
			continue
		}
		var msg proto.RoomMessage
		if err := json.Unmarshal(events[i].Payload, &msg); err != nil {
			t.Fatal(err)
		}
		if len(msg.Refs) != 2 || msg.Refs[0] != "docs/x.md" || msg.Refs[1] != "B156" {
			t.Fatalf("refs 载荷漂移: %+v", msg.Refs)
		}
		if len(msg.Mentions) != 1 || msg.Mentions[0] != "B145" {
			t.Fatalf("mentions 载荷漂移: %+v", msg.Mentions)
		}
		return
	}
	t.Fatal("账本没有该 seq 的 room_message 行")
}

// TestRoomReadStdoutSeqPrefix（B358.5 红窗改写）：#<seq> 前缀与 --after 排他
// 断言照抄，夹具发言迁 session send、读面用 room read <session:n>（读侧对
// 会话房间同一 History 面）；新增旧房间读面对质保留断言（S1 P5：读史不断）。
func TestRoomReadStdoutSeqPrefix(t *testing.T) {
	dir := t.TempDir()
	cardID := mustAddCard(t, dir, "历史卡")
	_, _, _, sessionID, _ := mustSendFixture(t, dir, "历史场")
	out, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "第一条")
	if err != nil {
		t.Fatalf("session send: %v", err)
	}
	var resp struct {
		Seq int64 `json:"seq"`
	}
	if err := json.Unmarshal([]byte(strings.TrimSpace(out)), &resp); err != nil {
		t.Fatalf("解析 send 输出: %v", err)
	}
	read, _, err := runLedgerCLI(t, dir, "room", "read", sessionID)
	if err != nil {
		t.Fatalf("room read: %v", err)
	}
	lines := strings.Split(strings.TrimRight(read, "\n"), "\n")
	if len(lines) != 1 || !strings.HasPrefix(lines[0], "#"+fmt.Sprint(resp.Seq)+"\t") {
		t.Fatalf("read 输出每行应以 #<seq> 前缀: %q", read)
	}
	after, _, err := runLedgerCLI(t, dir, "room", "read", sessionID, "--after", fmt.Sprint(resp.Seq))
	if err != nil {
		t.Fatalf("room read --after: %v", err)
	}
	if strings.TrimSpace(after) != "" {
		t.Fatalf("--after 等于最新 seq 应排他返回空: %q", after)
	}
	// 旧房间读面对质保留：卡房间读退出 0（空史合法——读侧宽容既有）。
	if _, _, err := runLedgerCLI(t, dir, "room", "read", cardID); err != nil {
		t.Fatalf("旧卡房间 room read 应可读（S1 P5 对质面）: %v", err)
	}
}

// TestRoomListSortedByActivity（B358.5 红窗改写）：活动降序断言迁 session list
//（旧 room list 不含会话行、且发言面只在会话——0.2 表 #20，旧列表无法经发言
// 复现排序场）。cmd 层呈现排序（b358.5-plan §2.1）：后发言的会话排前。
func TestRoomListSortedByActivity(t *testing.T) {
	dir := t.TempDir()
	_, _, _, idA, _ := mustSendFixture(t, dir, "卡A")
	_, _, _, idB, _ := mustSendFixture(t, dir, "卡B")
	if _, _, err := runLedgerCLI(t, dir, "session", "send", idA, "a1"); err != nil {
		t.Fatalf("send a: %v", err)
	}
	if _, _, err := runLedgerCLI(t, dir, "session", "send", idB, "b1"); err != nil {
		t.Fatalf("send b: %v", err)
	}
	out, _, err := runLedgerCLI(t, dir, "session", "list")
	if err != nil {
		t.Fatalf("session list: %v", err)
	}
	ia, ib := strings.Index(out, idA), strings.Index(out, idB)
	if ia < 0 || ib < 0 || ib > ia {
		t.Fatalf("list 应按最近活动降序（后发言的 %s 应排 %s 前）: %q", idB, idA, out)
	}
}
```

**步骤 3（绿 + 红窗收口复核）**：

```
$ go build ./...   → 退出 0
$ go test ./cmd -run TestRoom -count=1 -v
  → 6 支全部 PASS（红窗本卡收口绿）；TestRoomSendUserRejectsSeatFlagsWithoutSideEffects /
    TestRoomInboxWalksAgentdHTTP 全部 PASS（存活无波及）
$ go test ./cmd -count=1 → 失败恰 2 支：TestRepoContractGate、
    TestServePermissionHookDenyWithReasonAndStep0（均禁触）
$ go test ./cmd -run 'TestSessionWait' -count=1 → ok（B358.3 七支无波及）
```

### Task 2 验收

1. §0.3 红窗 6 支全绿，**名字逐支保留**；每支函数体首行有「B358.5 红窗改写」注释（指认原断言与迁移去向）。
2. 存活测试与全部非红窗测试逐字未动（`git diff cmd/room_test.go` 只含 6 支函数体 + 文件头注释一行）。
3. `go test ./cmd -count=1` 失败集合恰 `{TestRepoContractGate, TestServePermissionHookDenyWithReasonAndStep0}`。

## 6. Task 3：收口核算与移交

### 文件范围

无代码改动（读数与清单落台账）。

### 步骤

1. **三段律全量**：`go build ./...` 退出 0；`go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` 退出 0；`go test ./cmd -count=1`、`go test ./internal/agentd -count=1` 失败集合落台账。
2. **红窗核算（§0.3 规则 2）**：cmd 失败集合逐支比对 = 恰 `{TestRepoContractGate, TestServePermissionHookDenyWithReasonAndStep0}`；agentd 失败集合 = 恰 `{TestLegacyNodeEventSequenceUnchanged}`。多/少都停手上报。
3. **契约闸比对（§0.3 规则 3）**：`go test ./cmd -run TestRepoContractGate -count=1 -v`，违规清单与 Task 0 记录的基线 6 条逐行比对——无新增、无减少，原文落台账。
4. **TestSessionWait 七支复核**：`go test ./cmd -run 'TestSessionWait' -count=1` 退出 0（含守卫；若岔口 1 方案 A 被批准，守卫为收窄版且变异两发记录在台账）。
5. **图符号清单**：新增（session.go 六 cmd var + RunE + validSessionMemberIdentity、session_test.go 七测试 + mustSendFixture、room_test.go 六支改写符号；若方案 B 则加 session_commands.go 全符号）与修改（session.go init 追加行 + 头注释行、room_test.go 文件头注释）逐符号落台账，交 integrate/recon 补视图 diff；收尾跑 `codegraph resolve --doc docs/superpowers/plans/b358.5-plan.md` 修坏锚。
6. **文件集核对**：`git diff --stat` 受控 = §0.1 有界文件集 + 本计划 + 台账；无关脏文件不卷入。

### Task 3 验收

§12 收口判据全部成立；台账含红窗核算原文、TestRepoContractGate 两轮清单、图符号清单、（方案 A 时）守卫变异两发记录。

## 7. 明确不做的改动（越界即回退）

1. **不动 `cmd/session.go` 的 wait 段**——runSessionWait/buildSessionWake/sessionWaitCmd/既有 init 三行（Flags/AddCommand(sessionWaitCmd)/rootCmd.AddCommand）零改动；wait 签名与 stdout 形状冻结（B358.3 拍板）。
2. **不动 `cmd/room.go`**——openRoomService 游标行复用不重挂（B358.3 交界）；旧 room send 及其 kind=user 拒 flag 守卫行（:133-138）不动（岔口 11.3：废止的落点是新族不带旧规则，死面形状冻结）。
3. **不加 AddSessionMember CLI 面**——B358.4 拍板 2 显式不做项的 CLI 半边（成员扩张载体归后续卡，父卡 integrate 核对项已记）。
4. **不加 --kind/--reply-to**——§2.2 定稿；回复寻址缺口申报见岔口 5。
5. **不动 `internal/**`**——门面七方法与 Store 语义 Ticket 0/S1 已收口；本卡只消费（含 mapSessionError 丢 id 的补偿，在 CLI 边界做，不改 collab）。
6. **不动 B362 golden、TestRepoContractGate、TestServePermissionHook**——三者保持基线红形状，只对账不修复。
7. **不改 wire DTO、不加哨兵**——proto 键集已冻结；本卡零新哨兵、零新跨域 import（新增仅 stdlib sort/strings/tabwriter）。

## 8. 序列化边界与接缝覆盖

### 序列化边界设问

新增数据从产生到消费之间的**手写投影恰两处，各有断言**：

1. `session list` 文本行（tabwriter）+ `--json` 流：--json 侧 DTO 直接 `enc.Encode`（无手搭 map、无字段投影），TestSessionListColumnsAndUnread 断言每行 Unmarshal 回 `proto.SessionSummary` + 有消息行键集逐键（`cards` omitempty 缺键 = 「字段缺失 ≠ 值为零」的字面分辨）；文本侧七列表头逐列字面断言。
2. `session detail` 三段文本 + `--json`：同款——TestSessionDetailThreeSections 断言顶层键恰 `{summary,timeline}`（nodes 缺键）+ `proto.SessionDetail` roundtrip。
3. 其余 stdout 信封（`{"ok":true}`/`{"ok":true,"seq":<n>}`/Session 直通 Encode）无字段投影。

roundtrip 属性测试不另设：编码由 encoding/json 承担、proto 形状由金样本锁（`internal/proto/sessions_fixture_test.go`），CLI 层的增量断言 = 「真实 cobra stdout 可 Unmarshal 回冻结键集」，已由两支 --json 测试承担（穿过真实序列化边界，非本地 marshal 自查）。

### 接缝覆盖（spec §6 缝清单，双向）

**缝 #1（会话子系统入站门面，调用方 = CLI room/session 命令族）**——本卡全部缝级测试：

- **测试 → 缝**：Task 1/2 每支测试入口 = cobra 命令（`runLedgerCLI` → rootCmd → session/room 命令 RunE → `openRoomService` → `collab.Service` → 真 SQLite 账本）——入口符号全部在缝上（看入口不看标注）。
- **缝 → 测试**：本卡触及的门面方法每支至少一条缝级断言——CreateSession（TestSessionCreateCommand）、ListSessions（TestSessionListColumnsAndUnread + TestRoomListSortedByActivity 改写）、SessionDetail（TestSessionDetailThreeSections + TestSessionLeaveLifecycle/ArchiveCommand 的 --json 读）、ArchiveSession（TestSessionArchiveCommand）、JoinCard（TestSessionJoinLifecycle）、LeaveCard（TestSessionLeaveLifecycle）、Send（红窗改写 1/2/3/4 + TestSessionSendNegativePaths）；旧面 History/ListRooms 读侧（TestRoomReadStdoutSeqPrefix 改写）。WakeTargets/MessageWakeTargets/AddressesCard 不属本卡（S3 已收口，wait 七支守护）。

**内部锁声明**：无——全部新测试与改写测试的入口都在 CLI 命令缝上，无直调门面的测试。

**缝 #2/#3/#4/#5**：分属 S3（唤醒判定）、S3（席位身份）、S6（控制台契约层）、S2（详情投影 collab 半边）——本卡不新增断言、不触碰其文件；`currentSeatIdentity` 是缝 #3 的既有 CLI 出示函数，本卡只消费不改。

### 退路同闸

无条件退路。步骤正文里的每个动作都是确定动作（mustSendFixture/后续断言全部给定全文）；唯一条件分支是岔口 1 的方案 A/B 裁决（二者都不改变任何测试的入口符号——守卫收窄改的是既有守卫的禁令实现，方案 B 改的是生产文件布局），均已声明并待拍板。

## 9. 缺陷族对抗（九族 → 验收栏映射）

| 族 | 设问结论 | 验收落点 |
|---|---|---|
| 1 生命周期/状态机中断 | 命令一次性进程，无孤儿资源（账本连接 defer st.Close() 既有）；归档竞态由账本单写者（Store.mutate）串行化——机内不另设并发反例，PG 真库并发归真机（breakdown §5 #4/#8） | TestSessionArchiveCommand（幂等 0）；TestSessionSendNegativePaths（归档后 send/join 双拒） |
| 2 静默失败/误导报错 | 错误一律非 0 + stderr 可行动文案：不存在会话的 detail/archive/send/join/leave 全部把 id 包回（mapSessionError 丢 id 的 CLI 边界补偿，0.2#4）；「已属会话/已归档/只读/书写者」哨兵文案透传；禁止「打印 ok 但账本没写」——send 成功以账本 seq 回显为准（stdout seq 与 EventsFromAsc 定位同一 seq 断言） | TestSessionDetailThreeSections / TestSessionArchiveCommand / TestSessionSendNegativePaths / 红窗改写 1 |
| 3 跨平台 | 纯 Go CLI；HOME/actor 注入沿用 ledgercli.go 既有约定（USER env + hostname）；无路径/进程组假设 | 机内全绿即收口；无新增真机项 |
| 4 假红/假绿 | 全部测试穿 cobra 命令对象 + 真 SQLite（同 S4「空壳冒充接线」禁令）；成对 flag 反例（单只 → 拒）必须存在且存在（TestSessionSendNegativePaths 两向）；守卫收窄（若方案 A）配变异两发防「禁令收窄=禁令削弱」 | Task 1 步骤 1 红读数 / TestSessionSendNegativePaths / Task 1 步骤 5 |
| 5 门禁绕过 | 身份只能来自 `ledgerActor()` 或成对席位 flag（无冒充直通 flag——breakdown 缺陷族 5 字面）；自报席位不在书写者集 → ErrNotWriter（可变红：删 VerifyWriter 调用即红）；环境席位键不参与 session send（TestSessionSendNegativePaths 的 clearSeatSourceEnv + 成对 flag 反例锁显式性） | TestSessionSendNegativePaths (c) / 红窗改写 2/3 |
| 6 序列化边界 | §8 两处手写投影各有断言；--json roundtrip 回冻结 DTO 键集，不做无测试的格式发明 | TestSessionListColumnsAndUnread / TestSessionDetailThreeSections |
| 7 枚举白名单 | 不适用——本卡不新增枚举值（kind 恒既有 `proto.RoomMsgUser`；timeline kind 投影是 S2 已收口的读侧） | — |
| 8 承重安全属性 | 「非成员/非席位不能写」经 CLI 面可变红（ErrNotWriter 断言）；「旧 room send 保留报错形状」是 P7 的承重属性，红窗改写 1(a) 钉住 | TestSessionSendNegativePaths / 红窗改写 1(a) |
| 9 webview 候选族 | 无——本卡是 CLI 文本面，不触浏览器；主 agent 巡场（user story 6）的机读面由 --json 承担 | — |

## 10. 五项检查与自审三查

1. **缺陷族对抗**：§9 逐族有结论，落验收栏。
2. **序列化边界**：§8，--json 断言穿真实 cobra stdout 序列化边界。
3. **上下文预算**：每 task 文件集有界（Task 1 = 2 文件、Task 2 = 1 文件、Task 0/3 = 0 文件）。
4. **类型标注（逻辑型）**：行为验收 = 机内命令级闭环（cobra → 门面 → 真账本）；归真机（不属本卡执行）——breakdown §5 #8 的 CLI 与 gateway 并发 MarkRead/读未读（room-cursors.json 双进程）、真实多机 HOME/hostname 下的 actor 形态。
5. **接缝覆盖**：§8 双向 + 内部锁声明（无）。

**自审三查**：

- **spec 覆盖**：breakdown §3.6 ③ 逐条指到验收——create stdout 含 session:<n>（Task 1.1）；list 行含标题/未读/需要你列（TestSessionListColumnsAndUnread + 红窗改写 6）；detail 三块逐列可读（TestSessionDetailThreeSections）；join 幂等 0/已属他会话非 0 含「已属会话」（TestSessionJoinLifecycle）；send 人形态 actor=cli:（红窗改写 1）/协调者席位身份 kind=user（红窗改写 2/3）；归档 send/join 非 0（TestSessionSendNegativePaths）；room read 旧面可读 + room send 非 0（红窗改写 1(a)/5）；退出码契约（§2.1 表 + Task 1 验收 3）；缺陷族 4/5/6 点名断言各有着落；红窗 6 支全吸收（Task 2）。
- **占位符扫描**：全文无 TBD/「适当的错误处理」/「同 Task N」；测试与实现代码全部给全文；复用的既有夹具（runLedgerCLI/mustAddCard/clearSeatSourceEnv、facade 会话方法）指认了文件与行为，不构成占位符。**正当出口声明**：不适用——Task 1/2 的全部新测试与 6 支红窗改写（含夹具增量 JoinCardToSession+BindSeat）均给出完整测试代码，无骨架测试、无「照抄既有 harness」例外使用。
- **跨 task 签名一致**：Task 1 Produces（mustSendFixture/六子命令）与 Task 2 Consumes 逐字同源；Consumes 的门面签名与 contract §3.5 / sessions.go 现状逐字核对（0.2#1）；proto.Session/SessionSummary/SessionDetail 与 contract §3.2 一致。

## 11. 岔口（协调者审查重点；「待拍板」项派工前须裁决）

### 11.1 【待拍板，阻塞 Task 1 步骤 5】守卫禁令与同文件续写的硬冲突

**事实**（0.2#17，台账 §二.21）：`TestSessionWaitSourceGuard`（B358.3 冻结守卫，禁触面）对 `cmd/session.go` 全文件子串禁令含 `"proto.RoomMsgUser"`；本卡 session send 必须引用该常量构造 kind=user 发言（breakdown §3.6 ③）。**「同文件续写」（B358.3 拍板岔口#2 + 派工报文）与「cmd 守卫禁触」（派工报文）在本事实下互斥**——不裁决则 Task 1 落码即红守卫。

- **方案 A（推荐）：同文件续写 + 守卫收窄**。kind 门控/mentions 直读两类禁令从文件级子串改为 wait 两函数（`runSessionWait`/`buildSessionWake`）的 AST 级禁令，广播帮手名保留文件级（完整替换代码见 §4 步骤 5；变异两发先红后还原）。理由：守卫意图是「wait 通道纯净」（其自注释：通道内不得重造寻址判定，契约条 42），文件级范围是 wait 独占文件时代的近似；会话发送面进文件后收窄是根因修复（第一性原理），且 AST 级比子串级更精确（结构体字面量键 `Mentions:` 天然豁免，`msg.Mentions` 直读仍被禁）。代价：触碰一支冻结守卫（禁触面例外的显式申报，变异证据随附）。
- **方案 B：拆文件**。六子命令落新文件 `cmd/session_commands.go`（自带 init() 注册行，cobra 跨文件聚合；`cmd/session.go` 零改动），守卫零触碰。代价：偏离「同文件续写」交界（B358.3 拍板岔口#2 需改判一行）；守卫的文件级过 Fire 保持潜伏（未来任何 session.go 内 RoomMsgUser 引用再触发）。
- 两案 wait 签名/stdout 形状均零改动；裁决记录回写本节后执行者按所选方案执行，未裁决则停在 §4 步骤 4 收尾上报。

### 11.2 【本计划定稿，审查重点】owner 载体 = 必填 flag `--owner`

B358.4 拍板①「session create 的 owner 按此设计（flag 或参数，plan 定稿）」的落点。被否备选：位置参数 `create <title> <owner>`（双位置参数易错位，且 owner 是身份字段 flag 化与 gateway body 字段同名对齐）；缺省 `ledgerActor()`（违反拍板①——`cli:<user>@<host>` 是机器位审计串，非统一记法）。若协调者翻转：改 `sessionCreateCmd` 的 owner 读取行 + TestSessionCreateCommand 的 owner 反例段，其余 task 不动。

### 11.3 【本计划定稿，审查重点】session send 身份严格两形态按 flag 判定；环境席位键不参与；旧 room send 守卫行不动

被否备选：无 flag 时探 `currentSeatIdentity` 环境半边（HANDOFF/宿主键命中即席位）——被否理由：无 flag 必须确定性等于人形态，环境残留键把发言静默变成席位身份是「惊喜形状」；session wait 的 member 即显式参数（契约 §3.8），身份显式性对称；旧 room send 的环境出示路径随死面冻结，不迁移。红窗支 TestRoomSendCoordinatorKindUsesGrokHostSession 按此改写为 flag 形态（名字保留）。若协调者翻转：session send 无 flag 分支改为「先试 currentSeatIdentity，出错回落 ledgerActor()」，该支改写与 TestSessionSendNegativePaths 相应调整。

**附带定稿（同一决策复合体）**：breakdown §3.6 ②「旧 room send 拒绝成对 flag 的规则废止」的落点 = **新族（session send）不带旧规则**，而非删除 `cmd/room.go:133-138` 的守卫行——理由：该行在死面内（旧房间一律 ErrReadOnly，删除它不改变任何可观察结果）、无验收判据要求删除、且存活测试 TestRoomSendUserRejectsSeatFlagsWithoutSideEffects（B358.4 §0.4 不得误改清单）的注释以「CLI flag 层先拒」为前提。若协调者拍板删除守卫行：该存活测试仍绿（拒绝面变为 ErrReadOnly）但需同步修订其注释（触碰存活禁触面，须协调者明示）。

### 11.4 【判据字面修订，回写建议】join 归档哨兵 = ErrBadState（「已归档」文案）

breakdown §3.6 ③ 字面「归档会话 send/join → 非 0 退出 + ErrReadOnly 文案」：send 半边确为 ErrReadOnly（sendToSession 查会话本体，`internal/collab/service.go:140-143`）；join 半边实际是 `ledger.ErrBadState`（「会话 %s 已归档」，`internal/ledger/sessions.go:176-178`，B358.4 §2.2 映射表同源）。本计划验收按实际行为写（TestSessionSendNegativePaths 断言 send 含「只读」、join 含「已归档」）——照 B358.2 plan 拍板①先例申报字面修订，建议随本卡 review 回写 breakdown §3.6 一行。

### 11.5 【跨卡缺口，待拍板】回复寻址（reply_to）无任何生产面

gateway `handleRoomSend` 不收 reply_to（0.2#16 实测）、CLI session send 不设 --reply-to（§2.2）、旧 room send 亦无——S3 wait 通道的 `Referenced` 引用条（条 43）已有消费面但无生产者。spec 用户故事 7（人回复协调者 → 只唤醒它一个）在 CLI+HTTP 两面都缺载体，不属 S5 验收（breakdown §3.6 ③ 未列）。建议记 B358 父卡 integrate 核对项或落新卡（与 B358.4 拍板 2 的 AddSessionMember 核对项同区）；不在本卡静默补。

### 11.6 【已知重复，申报】统一记法前缀校验两处实现

`internal/agentd/sessionsapi.go#validSessionOwner` 与本卡 `cmd/session.go#validSessionMemberIdentity` 同规则两处（cmd 不得 import agentd——域方向；改 agentd 属越界）。漂移由两侧测试钉住（TestSessionsCreateEndpoint / TestSessionCreateCommand）；统一收口归后续重构卡。

### 11.7 【无】先回 contract 项

本卡零新哨兵、零 wire 变更、零新跨域边（新增 import 仅 stdlib `sort`/`strings`/`text/tabwriter`）；无先回 contract 项。

## 12. 收口判据（B358.5 完成的唯一判定）

1. `go build ./...` 退出 0。
2. `go test ./cmd -count=1` 失败集合 = 恰 `{TestRepoContractGate, TestServePermissionHookDenyWithReasonAndStep0}`（逐支比对，无增无减）——红窗 6 支收口绿。
3. `go test ./cmd -run 'TestSession' -count=1` 退出 0（新 7 支 + B358.3 wait 七支全绿）。
4. `go test ./cmd -run TestRepoContractGate -count=1 -v` 违规清单与基线 6 条逐行一致（无新增、无减少）。
5. `go test ./internal/agentd -count=1` 失败集合 = 恰 `{TestLegacyNodeEventSequenceUnchanged}`；`go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` 退出 0。
6. `git diff --stat` 受控 = §0.1 有界文件集 + 本计划 + 台账。
7. 台账含：Task 0 基线读数、红窗核算原文、TestRepoContractGate 两轮清单、图符号清单、岔口 1 裁决记录与（方案 A 时）守卫变异两发记录。

## 实现者纪律（派发注入段）

- TDD 三段律：编译全量、测试局部、集成全量——每个 task 的测试命令只跑触及包（cmd）；全量只在 Task 3。
- 日志：每条错误分支带 session/card/actor/cause 上下文（slog.Default().Warn，room.go 同款），成功路径 Info 不静默（§4 步骤 2 代码已含）；禁 print。
- 注释：session.go 续写段头注释（职责+边界+身份两字段）已给；每个 cmd var 导出语义由 Use/Short 承担；红窗改写支首行注释已给。
- 修复熔断：任何测试意外红且无法归因到本计划明说的行为变化 → 停手上报，不顺手修越界文件；岔口 1 未裁决时停在 §4 步骤 4。
- 本计划无派发系统自身的验收步骤（全部本地 go test/go build/grep），无「由协调者执行，不派发」条目。

---

## 协调者拍板（补记，2026-09-12）

§11 岔口与定稿裁决：

1. **岔口 11.1 守卫冲突：方案 A。**守卫收窄为 wait 两函数 AST 级禁令（runSessionWait/buildSessionWait），session.go 其余部分解禁 `RoomMsgUser` 引用。理由：守卫本意是「wait 通道不得走 kind 闸/本地解析 mentions」，全文件子串禁令是 session.go 只装 wait 时代的近似；send 合法需要 kind=user，AST 收窄让守卫更精准而非更弱，带双变异证明随方案给出。此为对 B358.3 产物的显式修订，变异红绿照纪律记台账。
2. **岔口 11.5 reply_to 生产面缺失：落新卡（B365）。**S3 的 Referenced 消费面就绪但 HTTP/CLI send 均不收回复锚——spec 用户故事 7 无载体。属跨卡缺口非 S5 范围，本卡不做；B358 integrate 后按新卡推进。父卡 integrate 清单挂指针。
3. **11.2 owner=必填 --owner flag（统一记法前缀校验）：准**——B358.4 拍板① CLI 半边同原则。
4. **11.3 send 两形态严格按 flag、环境席位键不参与、旧 room send 守卫行不动：准**——旧 room send 目标已死（S1 只读），废止落点=新族不带旧规则。
5. **11.4 join 归档哨兵字面（ErrBadState/「已归档」）申报回写 breakdown：准**——照 B358.2 判据字面修订先例，同批提交回写一行。
6. **11.6 前缀校验两处实现（gateway/CLI 各一）：准**——两面各自校验是 CLI/gateway 分层的正形，非重复。
