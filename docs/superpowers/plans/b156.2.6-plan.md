# B156.2.6 实现计划：C6 d_gateway·HTTP 面与收件箱编排（欠账 #5 + #11 路由缺席半 + #12 半 + #9 Go 半）

**状态**：plan 节点产出（charter 流），2026-08-26。
**有效基线分支**：`feature/b156-workbench`（合并目标以此为准）。本计划在 `cards/B156.2.6-charter` 上执行，HEAD=06647d44（C5 已合入功能线）。
**上游输入**：spec `2026-08-25-b156.2-collaboration-rooms-design.md`（已批准）；契约 `b156.2-contract.md`（含 §11 微修订、岔口二方案 A 四条件）；breakdown `b156.2-breakdown.md`（C6 卡四段式 + 澄清三/四）。
**台账**：`docs/superpowers/ledgers/ledger-b156.2.6-plan.md`（本计划所有探针/图闸/计数证据在案）。

本卡做五件事（欠账 #5 全量 + #11 路由缺席半 + #12 半 + #9 Go 半）：

1. **六个 HTTP 端点**（契约 §3.5 表六行）注册进 `registerLedgerRoutes` 同一 mux，错误映射逐哨兵；
2. **收件箱三源编排**（契约 §3.6，gateway 层编排单元）：decision（岔口二方案 A 直调面）+ ticket（Watchers 过滤，破坏性不受限）+ mention（collab.Mentions）；
3. **Pointer 路由缺席守卫**（契约 §4「HTTP/CLI 路由表中不存在可达 Pointer 的入口」）：判据 (a) 源码级白名单断言 + (b) 强制正控双读数；
4. **组装点接线**：Server 存 collab.Service + 换绑端口适配器 + 游标介质接线 + cmd/agentd.go 激活；
5. **图登记**：target.json 预定声明（同提交于首个消费边）+ 视图 diff 补节点边 + collab.New/Facade.BindDriver 覆盖债节点补入。

---

## 0. 图事实与决策记录（本计划的裁决边界，先读）

以下探针全部在本轮实测（证据台账 L2/L4/L6/L7/L13/L14），与协调者写下的判据冲突处**以实跑为准**。

### 0.1 预定声明的 entries 不能逐字照抄 §2.3（L7 探针，重大事实）

契约 §2.3 预定声明 entries 原文「collab.Service 与入站门面实体」；`graph check` 的 entries 按**容器 label 精确匹配**（charter/graph@v0.8.0/codegraph/check.go:135 `container.Label != entry`），而 `k_collab_Service` 的 label 是 **`"collab 入站门面"`**（baseline.json 亲读）。逐字照抄会在首提交打出两条 fail：

- `dead-contract | d_gateway -> d_collab | 契约 d_gateway→d_collab 声明的方向没有活跃 call、implements 或组装点豁免边`
- `dead-entry | d_gateway -> d_collab | 契约 d_gateway→d_collab 声明的入口 "collab.Service 与入站门面实体" 在 d_collab 中不存在`

探针 B（entries=`["collab 入站门面"]`）只剩 `dead-contract` 一条（dead-entry 消失）。B156.3 已声明的 `d_keystone→d_collab`（target.json:146-156）用的正是实际 label。**裁决：本卡 target.json 写 `entries: ["collab 入站门面"]`，计划与台账显式记录对 §2.3 字面的偏离及探针证据；C7（d_cli→d_collab）同文。**

### 0.2 预定声明「与首个消费边同提交」是承重要求（L7 探针）

dead-contract 证明：无活跃边的预定声明必红。所以 **target.json 契约与视图 edgesAdded（消费边）必须在同一提交**——不是格式要求。本计划提交切分按此排（commit 2 同时含 target.json 与视图边）。

### 0.3 `collab.New` 无图节点（协调者事实，已复验）

`graph sym collab.New`（无 view/带 view 均）`Error: 符号 "collab.New" 不在图中`。悬空引用探针（L6）：注入指向不存在节点的边 → `graph validate` EXIT=1「diff 边 … 引用未知节点 …」、`graph check --view` EXIT=1「视图 … 引用不完整: [diff 边 …]」——闸门抓得住「声明之间自相矛盾」，抓不住「现实里有而没申报」。**裁决（选项①）：本卡视图 diff 补 `n_collab_New` 节点**（包级构造器有户口是本仓惯例——cursor.New 就有节点；SetupAutomation 装配边需要它），理由记台账 L6/L12。

### 0.4 岔口二四条件落实（决策记录，plan 里必须看得见）

岔口二方案 A（收件箱 decision 源走 `s.ledger` 直调面）在**不违反**四条件的前提下落：

1. **债要可见**：legacy 调用点计数（代码口径）改动前 = **37**（`internal/agentd` 非测试 `.go` 的 `s.ledger.<Method>(` 调用点，34 个不同方法），改动后 = **38**（收件箱 decision 源新增一处 `s.ledger.ListDecisions(...)`）。**图口径不变**：新增边目标 `k_ledger_Store`（label `ledger.Store`，在 entries 内）不进 legacyHits，`graph check` 的「预算内直调 d_gateway->d_ledger 1/17」前后都保持 1——债 +1 只在代码调用点口径可观测，台账 L3。
2. **不得成为先例**：本裁决只覆盖「既有能力的新调用点」（ListDecisions 是既有能力，GET /api/decisions 同款）。本期新增账本能力**一律走门面**——本卡的换绑端点（RebindDriver，本期新增）就因此走组装适配器（§0.5），不援引本条。
3. **将来 gateway 账本需求整体迁门面时该调用点跟着迁**：记入本计划「图覆盖债」节（路由缺失的可观测点 = 收件箱 decision 源的 `s.ledger.ListDecisions` 直调，迁门面时一并迁）。
4. **账本门面只许组装点适配器触达**：gateway 的 handler 与编排路径零 `internal/ledger/api.Facade` 引用。本卡的 rebind 适配器按 §0.5 机械判据核对落。

### 0.5 换绑端点的合规通道（D-rebind 裁决，L11）

`POST /api/cards/{id}/rebind` 调 `Store.RebindDriver`（本期新增能力）。岔口二条件 2 禁止 gateway 直调 s.ledger；条件 4 禁止 gateway handler 引用 Facade；collab.Service 方法集（契约 §3.3 冻结）无 rebind 方法；经 `collab/client.LedgerClient.BindDriver` 会构成 d_gateway→d_collab 的未声明 entry 边（预算 0 即超）。**裁决：gateway 侧定义消费端口 `rebindPort`（§3 T6.2 代码块），组装点 server.go 构造 `facadeBindAdapter{f *ledgerapi.Facade}`（单一 Facade 字段、方法体只转调 `f.BindDriver`），SetupAutomation 注入 `s.rebind`；测试经 `SetRebind` 替换。** 条件 4 机械判据三条核对：一个 Facade 字段 ✓、纯转调 ✓、由组装点注入消费端口 ✓——「另一个子系统消费侧端口」按「端口接口定义在 gateway 消费侧、适配器类型只出现在组装点文件（server.go，assembly 登记点）」的窄读执行，这是条件 4 的裁决边界，冲突争议记台账 L11。适配器文件=server.go → 其 →Facade.BindDriver 边走**组装点豁免**（check.go:92-96），不构成违规新方向；`n_ledger_api_Facade_BindDriver` 当前不在图（C1 覆盖债），视图 diff 补入。

### 0.6 破坏性工单判定（D-destructive 裁决，L10）

proto.Ticket 无机械破坏性标记（proto.go:374-391 亲读）；permGate 黑名单升级与审批者升级都经 `escalatePermission` 建工单，工单上不落分类。**裁决：破坏性 = 该工单 task 事件流存在 `approver_decision` 事件（`ticket_id` 匹配且 `decision ∈ {escalate, error}`）**。理由：approver 是系统对权限请求的第一道自动风险过滤（fail-closed，approver.go:8「一律按 escalate，绝不静默放行」），它不敢放行/裁决失败 = 系统标记的「危险请求」的机械代理；「破坏性/不可逆/外部可见一律升级人工」硬纪律在代码里的落点就是 escalate/error。**已知边界**：黑名单直通（未咨询审批者）的工单按此规则不判破坏性——该路径无 approver_decision 事件；可接受（协调者驱动场景仍经协调者，收件箱是兜底面），要补需回 contract 动冻结物，不在本期。

### 0.7 组装激活（D-assembly 裁决，L8）

`SetupAutomation`（server.go:2236-2246）全仓**无任何调用点**（亲查）。不激活则六端点生产全 503。**裁决：C6 在 SetupAutomation 里存 `s.rooms`（复用同一实例给 roomNarrator）、接线游标 `SetCursorStore(cursor.New(filepath.Join(s.conf().DataDir, "room-cursors.json")))`、注入 `s.rebind`；新增 `SetRooms`/`SetRebind` 测试缝（SetScheduling/SetKeystone 同形）；cmd/agentd.go `setupLedger` 后补一行 `srv.SetupAutomation(lst)`。** 有界文件集由 breakdown 的 5 文件扩到 8（+cmd/agentd.go、+pointer_gate_test.go、+视图 diff），理由见 §2。scheduling/keystone 构造零副作用（骨架期注释自证），激活安全。

### 0.8 错误映射表（契约 §3.3 哨兵集 + §3.5 端点表）

| 哨兵 | HTTP | 说明 |
|---|---|---|
| `collab.ErrNoRoom` | 400（`GET /rooms/{id}/messages` 特例 404） | §3.5 明示 History 的 ErrNoRoom→404 |
| `collab.ErrKindNotAllowed` | 400 | send 端点强制 kind=user，本分支防御性保留 |
| `collab.ErrNotWriter` | 403 | user 类 actor==绑定值可达 |
| `collab.ErrReadOnly` / `collab.ErrNotBound` | 409 | 终态/并入房间可达 |
| `ledger.ErrCASConflict`（rebind） | 409 | 复用 `ledgerErr` 既有映射（ledgerapi.go:66-77） |
| 未装配（s.ledger==nil || s.rooms==nil） | 503 | `withRooms` 兜底，同 withLedger 降级语义 |

---

## 1. 现状查证（全部亲读亲测，出处可复查）

| # | 事实 | 出处 |
|---|---|---|
| 1 | 路由是 `api.HandleFunc("GET /api/cards", …)` 字面量直接打在 `*http.ServeMux` 上；标准库 ServeMux 无枚举已注册模式的 API——「遍历 mux 注册表」类判据写不出来，改 (a)(b) | `internal/agentd/ledgerapi.go:30-52`、`server.go:544` |
| 2 | `collab.Service.Pointer` 在仓内非测试 `.go` 的**唯一**引用 = `roomNarrator.Say`（server.go:2349 `n.c.Pointer(cardID, …)`）——白名单判据今天就是绿的 | `server.go:2348-2351`、台账 L5 |
| 3 | `.Pointer` 选择器全仓非测试文件的干扰项：`unsafe.Pointer(` 恰 6 处（taskmark_darwin.go 1、platform_windows.go 5）**另有 `atomic.Pointer[` 1 处**（server.go:93 `cfg atomic.Pointer[config.Config]`，本计划探针发现）——守卫按接收者形状排除 `unsafe`/`atomic` 两裸标识符，不裸匹配文本 | 台账 L4/L13 |
| 4 | `graph check`/`TestRepoContractGate` 不解析源码：`cmd/graph_gate_test.go:36` 是 `codegraph.Check(tg, best, Merge(g,nil), decls)`，四参全来自 JSON | `cmd/graph_gate_test.go:36`、台账 L13 |
| 5 | `collab.Service` 方法签名与契约 §3.3 一字一致（ListRooms/History/Send/MarkRead/Mentions/Consume）；哨兵再导出 | `internal/collab/service.go:37-43`、§3.3 |
| 6 | 收件箱 decision 源走 `s.ledger.ListDecisions(openOnly)` 直调面，与 GET /api/decisions 同形；岔口二方案 A | `ledgerapi.go:669-676` |
| 7 | ticket 源数据面：`s.st.ListTasks()`（store.go:409）+ `PendingTickets(taskID)`（:1251，answer IS NULL 升序）+ `s.hub.Watchers(taskID)`（hub.go:118）；「破坏性」信号 = `approver_decision` 事件（proto.go:70、manager.go:607-615） | 台账 L9/L10 |
| 8 | `recentEventsLimit = 100`（破坏性事件扫描窗口）、`writeJSON`（server.go:2208）、`writeErr`（ledgerapi.go:82）、`ledgerErr`（:66）、`hostOnly`（hostguard.go:236） | server.go:64-65 等 |
| 9 | 投影先例：`ledgerEventWire`（ledgerapi.go:106）、`ledgerDecisionWire`（:157）、`ledgerCardViewWire`（:142）——收件箱 decision 源直接复用 ledgerDecisionWire | 台账 L9 |
| 10 | `SetupAutomation` 全仓零调用点；Server 无 collab 字段；roomNarrator 持 `c *collab.Service` | 台账 L8 |
| 11 | 图闸基线：`graph check --view cards-B156.2-charter-4` fails=[] EXIT=0（legacyHits d_gateway->d_ledger=1/17）、`graph validate` EXIT=0 | 台账 L2 |
| 12 | `k_collab_Service` 九方法全在图中（C4/C5 已登记）；`k_ledger_api_Facade` **缺 BindDriver**；`collab.New`、`ledgerapi.New` 无节点（包级构造器覆盖债） | 台账 L12 |
| 13 | 金样本 `TestInboxItemGoldenThreeOrigins`：decision 条目带 card_id+payload；ticket/mention 条目 omitempty 省略 card_id（payload 也未出现） | `internal/proto/rooms_fixture_test.go:68-110`、web/src/api/testdata/RoomsFixture.json |
| 14 | gofmt 与 build/test/vet 正交：基线 `gofmt -l internal cmd` 零输出（本卡收尾清单含 gofmt 闸，charter-implement/review 纪律块正文均无 gofmt，无人替你提醒） | 台账 L1 |
| 15 | `SetupAutomation` 现有图边（baseline 亲读）：→ scheduling.New/keystone.New/hostapi.New/ptyapi.New，无 → collab.New/ledgerapi.New | 台账 L12 |

**基线绿（plan 轮实测）**：`go build ./...` EXIT=0；`go test ./internal/agentd/...` ok；`go test ./internal/collab/...` ok；`go vet ./internal/agentd/...` EXIT=0；`gofmt -l internal cmd` 零输出；`graph check/validate` EXIT=0。

## 2. 有界文件集（上下文预算检查）

| 文件 | 动作 | 归属 task |
|---|---|---|
| `internal/agentd/roomsapi.go` | **新建**：六端点 handler + collabErr 错误映射 + 收件箱三源编排 + rebindPort 定义 | T6.2 |
| `internal/agentd/ledgerapi.go` | registerLedgerRoutes 追加 6 行注册 | T6.2 |
| `internal/agentd/server.go` | Server 加 `rooms *collab.Service`/`rebind rebindPort` 字段 + `SetRooms`/`SetRebind` 缝 + `SetupAutomation` 存 rooms/接线游标/注入 rebind + `facadeBindAdapter` 适配器 + `withRooms` 守卫 | T6.2 |
| `cmd/agentd.go` | `setupLedger` 后补 `srv.SetupAutomation(lst)` 一行（生产激活；**文件集扩 1，理由 §0.7**） | T6.2 |
| `internal/agentd/roomsapi_test.go` | **新建**：六端点逐端点头 + 错误映射逐哨兵 + 收件箱三源 + Watchers 翻转正控 + 破坏性不受限 + 集成冒烟 | T6.1（红）+ T6.4（绿） |
| `internal/agentd/pointer_gate_test.go` | **新建**：判据 (a) 源码级白名单守卫（基线即绿，正控 (b) 证明能红） | T6.1（基线绿）+ T6.3（正控） |
| `codegraph/target.json` | 预定声明 `d_gateway→d_collab`（entries 实际 label，见 §0.1） | T6.5 |
| `codegraph/diffs/cards-B156.2-charter-4.json` | 累计视图：nodesAdded（六 handler + 助手 + 适配器 + collab.New + Facade.BindDriver）+ edgesAdded（含消费边）；**沿用既有文件，不许另起新视图** | T6.5 |

圈得出：六个代码/测试文件 + 两个图数据文件，全部在 d_gateway（handler/组装）与 d_ledger/d_collab/d_orchestration 的既有契约面内（新跨域边全部落在已声明或同提交声明的方向上）。测试范围最小化：只跑 `./internal/agentd/...`（主）；`go build ./...`（依赖面回归）；图闸 `graph check --view cards-B156.2-charter-4` + `graph validate`；变异复验限定 `-run` 单测。全仓 go test 不属于本卡。

## 3. Task 分解

### T6.1 立红：端点/收件箱测试 + 白名单守卫（锁缝断言步骤，法定红绿）

**Interfaces**

- Produces（本卡锁定，全部经 HTTP 进入，调用链穿过 spec 测试接缝清单 #1——collab 入站 api 门面）：
  - `GET /api/rooms?project=` → `ListRooms(project string) ([]proto.RoomSummary, error)`
  - `GET /api/rooms/{id}/messages?before=&limit=` → `History(roomID string, beforeSeq int64, limit int) ([]proto.LedgerEvent, error)`
  - `POST /api/rooms/{id}/messages` → `Send(roomID string, msg proto.RoomMessage, actor string) (int64, error)`（actor 服务端注入）
  - `POST /api/rooms/{id}/read` → `MarkRead(member, roomID string, uptoSeq int64) error`
  - `GET /api/inbox` → 收件箱三源聚合（decision/ticket/mention）
  - `POST /api/cards/{id}/rebind` → 换绑端口 `Rebind(id, toSession, carrier, expect string) error`
  - 收件箱内部：`ListDecisions(true) ([]ledger.Decision, error)`（直调面）、`Store.ListTasks() ([]proto.Task, error)`、`Store.PendingTickets(taskID) ([]proto.Ticket, error)`、`Hub.Watchers(taskID) int`、`collab.Service.Mentions(member string, afterSeq int64, limit int) ([]proto.LedgerEvent, error)`
- Consumes（既有，精确签名，见 §1 表）。

**判据→测试映射表（契约 §4 条目号 + 契约 §3.5/§3.6）**：

| 判据 | 测试 |
|---|---|
| 六端点注册进 registerLedgerRoutes 同一 mux、逐端点 httptest 断言 | TestRoomsListEndpoint / TestRoomMessagesEndpoint / TestRoomSendEndpoint / TestRoomReadEndpoint / TestCardRebindCASConflict / TestInboxThreeSources |
| 错误映射逐哨兵（400/403/409/404） | TestRoomSendErrMapping（400 无房、403 绑定者发言、409 终态房、404 GET 历史无房）+ TestCardRebindCASConflict（409 CAS） |
| 未挂账本时随 withLedger 降级 503 | TestRoomsEndpoints503WithoutLedger |
| inbox origin 三值恰为 decision/ticket/mention（金样本） | TestInboxThreeSources（三源齐 + 键集按金样本断言） |
| decision 源=open 全量含项目级 | TestInboxDecisionOpenAllIncludingProjectLevel |
| ticket 源排除 Watchers>0（构造 hub 订阅夹具翻转 Watchers 数——内建正控） | TestInboxTicketExcludesDrivenTask（订阅翻转 1→排除；退订→上浮） |
| ticket 源不排除破坏性 | TestInboxDestructiveTicketFloatsWithWatchers（Watchers=1 且 approver_decision escalate → 仍上浮） |
| mention 源=未消费提及 | TestInboxMentionSource（未消费在列；消费后消失） |
| RefID 三种十进制串/ticket id 原文 | TestInboxRefIDShapes |
| **契约 §4「HTTP/CLI 路由表中不存在可达 Pointer 的入口」= 判据 (a)** | TestPointerRouteAbsentFromSource（基线绿，正控 (b) 见 T6.3） |
| **集成冒烟（澄清三）**：httptest 一发穿 handler→真 SQLite→响应解码回 InboxItem/RoomSummary 与 testdata 孪生一致 | TestInboxIntegrationSmoke |

**步骤**

1. （~2min）基线复核：`go test ./internal/agentd/...` 全绿（台账 L1）；确认 registerLedgerRoutes 无 rooms 路由、Server 无 collab 字段（§1 表 #1/#10）。
2. （~8min）写失败测试：创建 `internal/agentd/roomsapi_test.go`（下方完整代码块，复用 `newLedgerEnv`/`seedCard`/`seedChildCard`/`ledgerGet`/`ledgerPost`/`getJSON`/`mustCreateTask` 等既有助手）与 `internal/agentd/pointer_gate_test.go`（下方完整代码块）。**顺序依赖**：`roomsapi_test.go` 引用尚不存在的 `Server.rooms` 字段与六 handler（T6.2 才落）——红绿是 T6.1 红（整包编译失败）→ T6.4 绿的一个周期；`pointer_gate_test.go` 只引用 stdlib 与 repoRoot，但**与 roomsapi_test.go 同包**，包编译失败时它也跑不了——它的基线绿已在 plan 轮实跑验证（台账 L13 有效读数），implement 轮在 T6.4 复跑。
3. （~2min）跑红：`go test ./internal/agentd/ 2>&1`。预期：**整包编译失败**（`Server.rooms` 未定义 / handler 符号不存在），红文贴台账。守卫测试（(a)）的「基线即绿」不在红阶段复跑（包编译不过），由 T6.4 复跑 + T6.3 正控双读数证明。
4. （~2min）台账记红文。
5. 本 task 不单独提交（与 T6.4 绿同批）。

**完整代码块 A——`internal/agentd/pointer_gate_test.go`（新建；plan 轮基线可编译可跑）**

```go
// Service.Pointer 路由缺席的源码级守卫（B156.2 C6，契约 §4「HTTP/CLI 路由表中
// 不存在可达 Pointer 的入口」与 :404「Pointer 调用方在仓内只有控制面组件」的
// 执行机制）。判据 (a)：全仓非测试 .go 文件里对 collab.Service.Pointer 的引用
// 只允许出现在白名单 pointerWhitelist 内；白名单外出现第二处即红。
//
// 为什么是读源码的 Go 测试而不是 graph check / TestRepoContractGate：
// cmd/graph_gate_test.go:36 的 codegraph.Check 四入参全部来自 JSON，不解析源码；
// 新增一条真实调用而不写进视图 diff，闸门眼里它不存在（B156.3 决定性实验：
// internal/ledgerstep 放真调 Service.Pointer，build/vet/graph check/TestRepoContractGate
// 四项与基线一个数都不差）。只有随 go test 复现的读源码测试才有牙齿。
//
// 判定机制（按接收者形状，协调者复核定案）：go/ast 收集全部 SelectorExpr 且
// Sel.Name=="Pointer"；排除 X 为标识符 unsafe 的（unsafe.Pointer( 类型转换，恰 6 处：
// internal/prochost/taskmark_darwin.go 1、platform_windows.go 5）与 X 为标识符 atomic
// 的（atomic.Pointer[ 泛型类型实例化，另 1 处：server.go:93）——其余都是候选，按
// 所在函数（fd.Recv 推导「类型.方法名」）与白名单比对。**对第三方类型上的同名
// Pointer 方法本测试是过包含的**——过包含是红线的安全方向，判据要求的出口「新的
// 正当引用显式加进白名单并写明理由」正是它的退出通道。
//
// 白名单是显式列表（文件+函数+理由）。将来再出现正当引用（如 C7 的派发指针行，
// cmd/card_dispatch.go 落 Service.Pointer），必须在同一提交把该引用加进白名单并
// 写明理由，不许放宽断言——只堵不留出口会诱发绕过。
package agentd

import (
	"go/ast"
	"go/parser"
	"go/token"
	"os"
	"path/filepath"
	"strings"
	"testing"
)

// pointerWhitelist 是 collab.Service.Pointer 在仓内非测试 .go 文件的全部合法引用处。
type pointerWhitelistEntry struct {
	file string // 相对仓库根的路径
	fn   string // 所在函数（方法按「类型.方法名」，包级按函数名）
	why  string
}

var pointerWhitelist = []pointerWhitelistEntry{
	{"internal/agentd/server.go", "roomNarrator.Say",
		"组装点适配器（target.json assembly 登记点，server.go）：B156.3 叙事换绑轮把协调者叙事经此落卡房间（Service.Pointer）；不在 HTTP 路由可达图上。"},
}

// TestPointerRouteAbsentFromSource 契约 §4 判据 (a)：全仓非测试 .go 文件里每个
// 非 unsafe/atomic 前缀的 Pointer 选择器调用都必须落在白名单内；白名单每条至少
// 有一个命中（防白名单膨胀成无牙条款）。基线今天即绿（唯一引用在 roomNarrator.Say），
// 正控 (b)（T6.3，plan 轮已实跑同形探针验证红/绿两侧）证明它确实会红。
func TestPointerRouteAbsentFromSource(t *testing.T) {
	root := repoRoot(t)
	fset := token.NewFileSet()
	type hit struct{ file, fn string }
	var hits []hit
	walkErr := filepath.WalkDir(root, func(path string, d os.DirEntry, err error) error {
		if err != nil {
			return err
		}
		if d.IsDir() {
			if path != root && (strings.HasPrefix(d.Name(), ".") || d.Name() == "web" ||
				d.Name() == "vendor" || d.Name() == "node_modules") {
				return filepath.SkipDir
			}
			return nil
		}
		rel, err := filepath.Rel(root, path)
		if err != nil {
			return err
		}
		if !strings.HasSuffix(rel, ".go") || strings.HasSuffix(rel, "_test.go") {
			return nil
		}
		f, err := parser.ParseFile(fset, path, nil, 0)
		if err != nil {
			return nil // 解析失败不阻断其它文件（该文件编译错误由 build 兜底）
		}
		// 收集函数声明区间，用于把候选定位到所在函数。
		type span struct {
			name          string
			start, endPos token.Pos
		}
		var spans []span
		ast.Inspect(f, func(n ast.Node) bool {
			if fd, ok := n.(*ast.FuncDecl); ok {
				spans = append(spans, span{pointerFnName(fd), fd.Pos(), fd.End()})
			}
			return true
		})
		ast.Inspect(f, func(n ast.Node) bool {
			sel, ok := n.(*ast.SelectorExpr)
			if !ok || sel.Sel.Name != "Pointer" {
				return true
			}
			// 排除 unsafe.Pointer(（类型转换，恰 6 处）与 atomic.Pointer[（泛型
			// 类型实例化，server.go:93）——X 均为裸标识符，且都是类型构造不是方法调用。
			if id, ok := sel.X.(*ast.Ident); ok && (id.Name == "unsafe" || id.Name == "atomic") {
				return true
			}
			fn := ""
			pos := fset.Position(sel.Pos())
			for _, sp := range spans {
				p := fset.Position(sp.start).Offset
				e := fset.Position(sp.endPos).Offset
				if pos.Offset >= p && pos.Offset <= e {
					fn = sp.name
				}
			}
			hits = append(hits, hit{rel, fn})
			return true
		})
		return nil
	})
	if walkErr != nil {
		t.Fatal(walkErr)
	}
	if len(hits) == 0 {
		t.Fatal("守卫未命中任何 Pointer 选择器：白名单可能已空转，需人工核查（基线应至少命中 server.go 的 roomNarrator.Say）")
	}
	for _, h := range hits {
		ok := false
		for _, w := range pointerWhitelist {
			if h.file == w.file && h.fn == w.fn {
				ok = true
				break
			}
		}
		if !ok {
			t.Errorf("白名单外 Pointer 引用: %s %s——若属正当引用，显式加进 pointerWhitelist 并写明理由，不许放宽断言", h.file, h.fn)
		}
	}
	for _, w := range pointerWhitelist {
		found := false
		for _, h := range hits {
			if h.file == w.file && h.fn == w.fn {
				found = true
				break
			}
		}
		if !found {
			t.Errorf("白名单条目无对应引用，可能已失效: %s %s（%s）", w.file, w.fn, w.why)
		}
	}
}

// pointerFnName 从 FuncDecl 推导函数名：方法返回「类型.方法名」（指针接收者去 *），
// 包级返回函数名。白名单定位用。
func pointerFnName(fd *ast.FuncDecl) string {
	if fd.Recv != nil && len(fd.Recv.List) > 0 {
		typ := fd.Recv.List[0].Type
		name := ""
		if st, ok := typ.(*ast.StarExpr); ok {
			if id, ok := st.X.(*ast.Ident); ok {
				name = id.Name
			}
		} else if id, ok := typ.(*ast.Ident); ok {
			name = id.Name
		}
		return name + "." + fd.Name.Name
	}
	return fd.Name.Name
}

// repoRoot 定位仓库根（测试运行于 internal/agentd，向上两级应见 go.mod）。
func repoRoot(t *testing.T) string {
	t.Helper()
	dir, err := os.Getwd()
	if err != nil {
		t.Fatal(err)
	}
	root := filepath.Dir(filepath.Dir(dir))
	if _, err := os.Stat(filepath.Join(root, "go.mod")); err != nil {
		t.Fatalf("无法定位仓库根: %v", err)
	}
	return root
}
```

**完整代码块 B——`internal/agentd/roomsapi_test.go`（新建；plan 轮已按既有助手形态写全，实现后逐支跑绿）**

```go
// 房间 HTTP 面与收件箱测试（B156.2 C6）：六端点、错误映射逐哨兵、收件箱三源、
// Watchers 翻转正控、破坏性不受限、集成冒烟。全部从 HTTP 进入（spec 测试接缝
// 清单 #1 的调用方侧 = gateway 控制面），调用链穿过 collab 入站 api 门面。
package agentd

import (
	"encoding/json"
	"fmt"
	"strconv"
	"strings"
	"testing"
	"time"

	"github.com/Xsxdot/handoff/internal/ledger"
	"github.com/Xsxdot/handoff/internal/proto"
)

// webUserMember 是测试环境里控制台已认证主体的成员标识：httptest.NewServer 固定
// 监听 127.0.0.1，hostOnly(r.RemoteAddr) 恒为 "127.0.0.1"，故 roomUserActor 恒为
// "web:127.0.0.1"（hostOnly 见 hostguard.go:236）。消息 @ 该标识才进收件箱 mention 源。
const webUserMember = "web:127.0.0.1"

// newRoomsEnv 组装房间 HTTP 测试环境：真 SQLite 账本 + bug 工作流 + SetupAutomation
// （装配 collab.Service 与换绑端口）+ httptest 全链。
func newRoomsEnv(t *testing.T) *ledgerEnv {
	t.Helper()
	env := newNoPTYLedgerEnv(t)
	seedAgentdLedger(t, env.ledger, "bug")
	env.srv.SetupAutomation(env.ledger)
	return env
}

// inboxItems 拉取收件箱并解码 items。
func inboxItems(t *testing.T, env *ledgerEnv) []proto.InboxItem {
	t.Helper()
	var out struct {
		Items []proto.InboxItem `json:"items"`
	}
	code := env.getJSON(t, "/api/inbox", &out)
	if code != 200 {
		t.Fatalf("GET /api/inbox: %d", code)
	}
	return out.Items
}

func hasOrigin(items []proto.InboxItem, origin string) bool {
	for _, it := range items {
		if it.Origin == origin {
			return true
		}
	}
	return false
}

// itemKeys 序列化一条收件箱条目并返回 JSON 键集（金样本键集断言用）。
func itemKeys(t *testing.T, it proto.InboxItem) map[string]bool {
	t.Helper()
	raw, err := json.Marshal(it)
	if err != nil {
		t.Fatal(err)
	}
	var m map[string]any
	if err := json.Unmarshal(raw, &m); err != nil {
		t.Fatal(err)
	}
	keys := map[string]bool{}
	for k := range m {
		keys[k] = true
	}
	return keys
}

func TestRoomsListEndpoint(t *testing.T) {
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡A")
	if _, err := env.srv.rooms.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "hi"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	var out struct {
		Rooms []proto.RoomSummary `json:"rooms"`
	}
	code := env.getJSON(t, "/api/rooms?project=p", &out)
	if code != 200 {
		t.Fatalf("GET /api/rooms: %d", code)
	}
	found := false
	for _, r := range out.Rooms {
		if r.ID == card.ID {
			found = true
		}
	}
	if !found {
		t.Fatalf("卡房间应出现在列表: %+v", out.Rooms)
	}
}

func TestRoomMessagesEndpoint(t *testing.T) {
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡B")
	seq, err := env.srv.rooms.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "第一条"}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	var out struct {
		Messages []proto.LedgerEvent `json:"messages"`
	}
	code := env.getJSON(t, "/api/rooms/"+card.ID+"/messages?limit=10", &out)
	if code != 200 {
		t.Fatalf("GET messages: %d", code)
	}
	if len(out.Messages) != 1 || out.Messages[0].Seq != seq {
		t.Fatalf("历史应恰一条且 seq 一致: %+v", out.Messages)
	}
}

func TestRoomSendEndpoint(t *testing.T) {
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡C")
	code, body := ledgerPost(t, env, "/api/rooms/"+card.ID+"/messages", `{"body":"你好"}`)
	if code != 200 {
		t.Fatalf("POST messages: %d %s", code, body)
	}
	events, err := env.ledger.EventsFromAsc([]string{card.ID}, 0, 10)
	if err != nil {
		t.Fatal(err)
	}
	found := false
	for _, ev := range events {
		if ev.Type != ledger.EvRoomMessage {
			continue
		}
		var msg proto.RoomMessage
		if err := json.Unmarshal(ev.Payload, &msg); err != nil {
			t.Fatal(err)
		}
		if msg.Body == "你好" {
			found = true
			if !strings.HasPrefix(ev.Actor, "web:") {
				t.Fatalf("actor 应服务端注入（不经请求体）: %q", ev.Actor)
			}
		}
	}
	if !found {
		t.Fatalf("消息未落账: %+v", events)
	}
}

func TestRoomReadEndpoint(t *testing.T) {
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡D")
	seq, err := env.srv.rooms.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	code, body := ledgerPost(t, env, "/api/rooms/"+card.ID+"/read",
		fmt.Sprintf(`{"upto_seq":%d}`, seq))
	if code != 200 {
		t.Fatalf("POST read: %d %s", code, body)
	}
	// 读尽后未读应 0（MarkRead 到当前最大 seq → 水位语义清空）
	if n, err := env.srv.rooms.Unread(webUserMember, card.ID); err != nil || n != 0 {
		t.Fatalf("读尽后未读应 0: %v %d", err, n)
	}
}

func TestRoomSendErrMapping(t *testing.T) {
	env := newRoomsEnv(t)
	// 不存在的房间 → 400（ErrNoRoom）
	if code, _ := ledgerPost(t, env, "/api/rooms/NO-SUCH/messages", `{"body":"x"}`); code != 400 {
		t.Fatalf("不存在房间应 400: %d", code)
	}
	// GET 不存在房间历史 → 404（History 的 ErrNoRoom 特例）
	if code, _ := ledgerGet(t, env, "/api/rooms/NO-SUCH/messages"); code != 404 {
		t.Fatalf("GET 不存在房间应 404: %d", code)
	}
	// 绑定者本人以 user 发言 → 403（ErrNotWriter）
	card := seedCard(t, env, "卡E")
	if err := env.ledger.ClaimCardAs(card.ID, webUserMember, ""); err != nil {
		t.Fatal(err)
	}
	if code, _ := ledgerPost(t, env, "/api/rooms/"+card.ID+"/messages", `{"body":"x"}`); code != 403 {
		t.Fatalf("绑定者发言应 403: %d", code)
	}
	// 终态卡房间 → 409（ErrReadOnly）
	parent := seedCard(t, env, "父")
	child := seedChildCard(t, env, parent.ID, "子")
	if err := env.ledger.MoveCard(child.ID, ledger.StatusDone, "", "test"); err != nil {
		t.Fatal(err)
	}
	if code, _ := ledgerPost(t, env, "/api/rooms/"+child.ID+"/messages", `{"body":"x"}`); code != 409 {
		t.Fatalf("终态房间应 409: %d", code)
	}
	// 空正文 → 400（handler 卫生检查）
	if code, _ := ledgerPost(t, env, "/api/rooms/"+parent.ID+"/messages", `{"body":"  "}`); code != 400 {
		t.Fatalf("空正文应 400: %d", code)
	}
}

func TestCardRebindCASConflict(t *testing.T) {
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡F")
	if err := env.ledger.ClaimCardAs(card.ID, "cli:a@h", "desktop"); err != nil {
		t.Fatal(err)
	}
	code, body := ledgerPost(t, env, "/api/cards/"+card.ID+"/rebind",
		`{"to_session":"cli:b@h","carrier":"cli","expect":"WRONG"}`)
	if code != 409 {
		t.Fatalf("expect 不符应 409: %d %s", code, body)
	}
	// expect 正确 → 200 且换绑生效
	code, body = ledgerPost(t, env, "/api/cards/"+card.ID+"/rebind",
		`{"to_session":"cli:b@h","carrier":"cli","expect":"cli:a@h"}`)
	if code != 200 {
		t.Fatalf("换绑应 200: %d %s", code, body)
	}
	card2, err := env.ledger.GetCard(card.ID)
	if err != nil {
		t.Fatal(err)
	}
	if card2.DriverSession != "cli:b@h" {
		t.Fatalf("换绑未生效: %+v", card2)
	}
}

func TestRoomsEndpoints503WithoutLedger(t *testing.T) {
	env := newTestAgentdEnv(t)
	code, _ := ledgerGet(t, env, "/api/rooms")
	if code != 503 {
		t.Fatalf("未挂账本应 503: %d", code)
	}
}

func TestInboxThreeSources(t *testing.T) {
	env := newRoomsEnv(t)
	// decision 源：卡级 + 项目级 open 裁决
	card := seedCard(t, env, "卡G")
	if _, err := env.ledger.OpenDecision(card.ID, "一句话：契约语义冲突", []string{"a", "b"}, "coord"); err != nil {
		t.Fatal(err)
	}
	if _, err := env.ledger.OpenDecision("", "项目级裁决", nil, "coord"); err != nil {
		t.Fatal(err)
	}
	// mention 源：群房间 @ 用户
	if _, err := env.srv.rooms.Send("project:p", proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "改动影响 B145", Mentions: []string{webUserMember}}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	// ticket 源：等待人工任务上的未答复工单
	mustCreateTask(t, env.st, &proto.Task{ID: "t1", RepoPath: "/r", Executor: "fake", State: proto.TaskStateWaitingAnswer})
	if _, err := env.st.CreateTicket(&proto.Ticket{
		ID: "t1:p1", TaskID: "t1", Kind: "gate",
		Request: json.RawMessage(`{"kind":"gate","permission":"x"}`), CreatedAt: time.Now()}); err != nil {
		t.Fatal(err)
	}
	items := inboxItems(t, env)
	for _, origin := range []string{proto.InboxOriginDecision, proto.InboxOriginTicket, proto.InboxOriginMention} {
		if !hasOrigin(items, origin) {
			t.Fatalf("收件箱应含 %s 源: %+v", origin, items)
		}
	}
}

func TestInboxDecisionOpenAllIncludingProjectLevel(t *testing.T) {
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡H")
	if _, err := env.ledger.OpenDecision(card.ID, "卡级裁决", nil, "coord"); err != nil {
		t.Fatal(err)
	}
	if _, err := env.ledger.OpenDecision("", "项目级裁决", nil, "coord"); err != nil {
		t.Fatal(err)
	}
	items := inboxItems(t, env)
	decCount := 0
	hasProjectLevel := false
	for _, it := range items {
		if it.Origin != proto.InboxOriginDecision {
			continue
		}
		decCount++
		if it.CardID == "" {
			hasProjectLevel = true
		}
	}
	if decCount != 2 || !hasProjectLevel {
		t.Fatalf("decision 源应含 open 全量（含项目级）: %+v", items)
	}
}

func TestInboxTicketExcludesDrivenTask(t *testing.T) {
	env := newRoomsEnv(t)
	mustCreateTask(t, env.st, &proto.Task{ID: "t1", RepoPath: "/r", Executor: "fake", State: proto.TaskStateWaitingAnswer})
	if _, err := env.st.CreateTicket(&proto.Ticket{
		ID: "t1:p1", TaskID: "t1", Kind: "gate",
		Request: json.RawMessage(`{"kind":"gate"}`), CreatedAt: time.Now()}); err != nil {
		t.Fatal(err)
	}
	// 无人驱动：上浮
	if items := inboxItems(t, env); !hasOrigin(items, proto.InboxOriginTicket) {
		t.Fatalf("无人驱动应上浮工单: %+v", items)
	}
	// 订阅翻转 Watchers=1：排除（内建正控——真实 hub 订阅，非 mock 返回值）
	ch, unsub := env.srv.hub.Subscribe("t1")
	defer unsub()
	_ = ch
	if items := inboxItems(t, env); hasOrigin(items, proto.InboxOriginTicket) {
		t.Fatalf("Watchers>0 应排除工单: %+v", items)
	}
	// 退订回 0：重新上浮
	unsub()
	if items := inboxItems(t, env); !hasOrigin(items, proto.InboxOriginTicket) {
		t.Fatalf("退订后应重新上浮工单: %+v", items)
	}
}

func TestInboxDestructiveTicketFloatsWithWatchers(t *testing.T) {
	env := newRoomsEnv(t)
	mustCreateTask(t, env.st, &proto.Task{ID: "t1", RepoPath: "/r", Executor: "fake", State: proto.TaskStateWaitingAnswer})
	if _, err := env.st.CreateTicket(&proto.Ticket{
		ID: "t1:p1", TaskID: "t1", Kind: "gate",
		Request: json.RawMessage(`{"kind":"gate"}`), CreatedAt: time.Now()}); err != nil {
		t.Fatal(err)
	}
	// 审批者升级信号（D-destructive，台账 L10）：approver_decision escalate
	if _, err := env.st.AppendEvent("t1", proto.EventTypeApproverDecision, approverDecisionPayload{
		TicketID: "t1:p1", Decision: "escalate"}); err != nil {
		t.Fatal(err)
	}
	ch, unsub := env.srv.hub.Subscribe("t1")
	defer unsub()
	_ = ch
	if items := inboxItems(t, env); !hasOrigin(items, proto.InboxOriginTicket) {
		t.Fatalf("破坏性工单不受 Watchers 限制应上浮: %+v", items)
	}
}

func TestInboxMentionSource(t *testing.T) {
	env := newRoomsEnv(t)
	seq, err := env.srv.rooms.Send("project:p", proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "改动影响 B145", Mentions: []string{webUserMember}}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	items := inboxItems(t, env)
	var mention *proto.InboxItem
	for i := range items {
		if items[i].Origin == proto.InboxOriginMention {
			mention = &items[i]
		}
	}
	if mention == nil {
		t.Fatal("mention 源应含未消费提及")
	}
	if mention.RefID != strconv.FormatInt(seq, 10) {
		t.Fatalf("mention RefID 应为消息 seq 十进制串: %q", mention.RefID)
	}
	// 消费后消失
	if err := env.srv.rooms.Consume(seq, webUserMember); err != nil {
		t.Fatal(err)
	}
	if items := inboxItems(t, env); hasOrigin(items, proto.InboxOriginMention) {
		t.Fatalf("消费后 mention 应消失: %+v", items)
	}
}

func TestInboxRefIDShapes(t *testing.T) {
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡I")
	dec, err := env.ledger.OpenDecision(card.ID, "一句话：X", nil, "coord")
	if err != nil {
		t.Fatal(err)
	}
	seq, err := env.srv.rooms.Send("project:p", proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "b", Mentions: []string{webUserMember}}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	mustCreateTask(t, env.st, &proto.Task{ID: "t1", RepoPath: "/r", Executor: "fake", State: proto.TaskStateWaitingAnswer})
	if _, err := env.st.CreateTicket(&proto.Ticket{
		ID: "tkt-42", TaskID: "t1", Kind: "ask",
		Request: json.RawMessage(`{"kind":"ask","question":"q"}`), CreatedAt: time.Now()}); err != nil {
		t.Fatal(err)
	}
	items := inboxItems(t, env)
	for _, it := range items {
		switch it.Origin {
		case proto.InboxOriginDecision:
			if it.RefID != strconv.FormatInt(dec.ID, 10) {
				t.Fatalf("decision RefID 应为十进制 id 串: %q", it.RefID)
			}
		case proto.InboxOriginTicket:
			if it.RefID != "tkt-42" {
				t.Fatalf("ticket RefID 应为 ticket id 原文: %q", it.RefID)
			}
		case proto.InboxOriginMention:
			if it.RefID != strconv.FormatInt(seq, 10) {
				t.Fatalf("mention RefID 应为消息 seq 十进制串: %q", it.RefID)
			}
		}
	}
}

func TestInboxGoldenKeyShapes(t *testing.T) {
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡J")
	if _, err := env.ledger.OpenDecision(card.ID, "一句话：X", nil, "coord"); err != nil {
		t.Fatal(err)
	}
	if _, err := env.srv.rooms.Send("project:p", proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "b", Mentions: []string{webUserMember}}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	mustCreateTask(t, env.st, &proto.Task{ID: "t1", RepoPath: "/r", Executor: "fake", State: proto.TaskStateWaitingAnswer})
	if _, err := env.st.CreateTicket(&proto.Ticket{
		ID: "tkt-42", TaskID: "t1", Kind: "gate",
		Request: json.RawMessage(`{"kind":"gate"}`), CreatedAt: time.Now()}); err != nil {
		t.Fatal(err)
	}
	// 金样本键集断言（contract_fixture_test.go 同款）：decision 带 card_id+payload；
	// ticket/mention omitempty 省略 card_id。
	for _, it := range inboxItems(t, env) {
		keys := itemKeys(t, it)
		if it.Origin == proto.InboxOriginDecision {
			if !keys["card_id"] || !keys["payload"] {
				t.Fatalf("decision 条目应带 card_id+payload: %v", keys)
			}
		} else if keys["card_id"] {
			t.Fatalf("空 card_id 必须省略: %v %+v", keys, it)
		}
	}
}

func TestInboxIntegrationSmoke(t *testing.T) {
	// 澄清三：httptest 一发穿 handler→真 SQLite→响应解码回 InboxItem/RoomSummary，
	// 与 testdata/RoomsFixture.json 孪生一致（键集形状，非逐字节值）。
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡K")
	if _, err := env.ledger.OpenDecision(card.ID, "一句话：契约语义冲突", []string{"a", "b"}, "coord"); err != nil {
		t.Fatal(err)
	}
	if _, err := env.srv.rooms.Send("project:p", proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "b", Mentions: []string{webUserMember}}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	mustCreateTask(t, env.st, &proto.Task{ID: "t1", RepoPath: "/r", Executor: "fake", State: proto.TaskStateWaitingAnswer})
	if _, err := env.st.CreateTicket(&proto.Ticket{
		ID: "tkt-42", TaskID: "t1", Kind: "gate",
		Request: json.RawMessage(`{"kind":"gate"}`), CreatedAt: time.Now()}); err != nil {
		t.Fatal(err)
	}
	items := inboxItems(t, env)
	if len(items) < 3 {
		t.Fatalf("集成冒烟三源应齐: %+v", items)
	}
	for _, it := range items {
		keys := itemKeys(t, it)
		if !keys["origin"] || !keys["title"] || !keys["ref_id"] {
			t.Fatalf("InboxItem 基础键缺一: %v", keys)
		}
	}
	var rooms struct {
		Rooms []proto.RoomSummary `json:"rooms"`
	}
	if code := env.getJSON(t, "/api/rooms", &rooms); code != 200 {
		t.Fatalf("GET /api/rooms: %d", code)
	}
	if len(rooms.Rooms) == 0 {
		t.Fatal("RoomSummary 列表应非空")
	}
}
```

> 代码块为最终形态（非占位）：测试文件编译依赖 T6.2 落地 `Server.rooms` 字段与
> 六 handler，故红绿是 T6.1 红（编译失败）→ T6.4 绿的一个周期。占位残留扫描在
> T6.4 步骤 5。

### T6.2 实现：roomsapi.go + 组装接线（内部实现步骤，不配独立红绿）

**Interfaces**

- Produces（本卡新导出符号，均入图，见 T6.5）：
  - collab：无新符号（消费既有 Service 方法）。
  - agentd：`rebindPort` 接口、`facadeBindAdapter` 类型与 `Rebind` 方法、`Server.SetRooms`/`Server.SetRebind`、`Server.withRooms`、六 handler + 助手。
- Consumes：`collab.Service.ListRooms/History/Send/MarkRead/Mentions`、`ledger.Store.ListDecisions`、`store.Store.ListTasks/PendingTickets/EventsFrom/AppendEvent`、`Hub.Watchers`、`internal/ledger/api.Facade`（仅组装适配器）。

**步骤**

1. （~3min）新建 `internal/agentd/roomsapi.go`（下方完整代码块）。
2. （~3min）`ledgerapi.go` registerLedgerRoutes 末尾追加 6 行（下方代码块 B）。
3. （~5min）`server.go`：Server 结构体加两个字段 + `SetRooms`/`SetRebind` 缝 + `SetupAutomation` 改写（存 rooms、接线游标、注入 rebind）+ `withRooms` 守卫 + `facadeBindAdapter`（下方代码块 D/E）。新增 import `cursor "github.com/Xsxdot/handoff/internal/collab/cursor"`。
4. （~2min）`cmd/agentd.go`：`setupLedger` 返回前补一行 `srv.SetupAutomation(lst)`（代码块 F）。
5. （~1min）编译门：`go build ./...`。
6. （~1min）日志与注释检查：新文件头写职责+边界、每条错误分支带上下文日志、成功路径不静默（代码块内已含）。

**完整代码块 A——`internal/agentd/roomsapi.go`（新建）**

```go
// 协作房间 HTTP 面与收件箱编排（B156.2 C6）：六个房间端点的注册进
// registerLedgerRoutes 同一 mux；收件箱三源（open 裁决 / 未答复工单 / @提及）
// 按契约 §3.6 在 gateway 层编排，不进 collab 也不进 ledger。
//
// 边界：本文件是 gateway 对 d_collab 入站 api 门面的消费侧（spec 测试接缝清单
// #1 的调用方）；不得 import internal/collab 之外的门面（internal/ledger/api），
// 账本能力经 s.ledger 既有直调面（岔口二方案 A，仅既有能力）或 rebindPort
// 端口（组装点注入适配器）触达。actor/成员标识服务端注入，不经请求体。
package agentd

import (
	"encoding/json"
	"errors"
	"net/http"
	"strconv"
	"strings"

	"github.com/Xsxdot/handoff/internal/collab"
	"github.com/Xsxdot/handoff/internal/ledger"
	"github.com/Xsxdot/handoff/internal/proto"
)

// collabErr 把 collab.Service 哨兵翻译为 HTTP 状态（契约 §3.3 哨兵集 + §3.5 映射表）。
// ErrNoRoom/ErrKindNotAllowed→400、ErrNotWriter→403、ErrReadOnly/ErrNotBound→409。
// History 的 ErrNoRoom→404 特例由 handleRoomMessages 自行处理。
func collabErr(w http.ResponseWriter, err error) {
	code := http.StatusInternalServerError
	switch {
	case errors.Is(err, collab.ErrNoRoom), errors.Is(err, collab.ErrKindNotAllowed):
		code = http.StatusBadRequest
	case errors.Is(err, collab.ErrNotWriter):
		code = http.StatusForbidden
	case errors.Is(err, collab.ErrReadOnly), errors.Is(err, collab.ErrNotBound):
		code = http.StatusConflict
	}
	writeErr(w, code, err)
}

// roomUserActor 是房间面的控制台成员/actor 标识（服务端注入）：沿用
// ledgerapi.go:458 的 step 补 actor 先例（"web:"+hostOnly）。@用户的提及
// 与已读游标都以此标识为成员维度（D-identity，台账 L15）。
func (s *Server) roomUserActor(r *http.Request) string {
	return "web:" + hostOnly(r.RemoteAddr)
}

// handleRoomsList GET /api/rooms?project= → ListRooms（扁平活动排序列表）。
func (s *Server) handleRoomsList(w http.ResponseWriter, r *http.Request) {
	rooms, err := s.rooms.ListRooms(r.URL.Query().Get("project"))
	if err != nil {
		s.log.Warn("会话列表读取失败", "project", r.URL.Query().Get("project"), "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	writeJSON(w, http.StatusOK, map[string]any{"rooms": rooms})
}

// handleRoomMessages GET /api/rooms/{id}/messages?before=&limit= → History。
// 不存在的房间映射 404（契约 §3.5 History 行特例）。
func (s *Server) handleRoomMessages(w http.ResponseWriter, r *http.Request) {
	roomID := r.PathValue("id")
	before, _ := strconv.ParseInt(r.URL.Query().Get("before"), 10, 64)
	limit, _ := strconv.Atoi(r.URL.Query().Get("limit"))
	events, err := s.rooms.History(roomID, before, limit)
	if err != nil {
		if errors.Is(err, collab.ErrNoRoom) {
			s.log.Warn("房间历史请求命中不存在房间", "room", roomID)
			writeErr(w, http.StatusNotFound, err)
			return
		}
		s.log.Warn("房间历史读取失败", "room", roomID, "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	writeJSON(w, http.StatusOK, map[string]any{"messages": events})
}

// handleRoomSend POST /api/rooms/{id}/messages → 用户发言（kind 服务端固定 user，
// actor 服务端注入，均不经请求体）。空正文拒绝（无意义消息不进房间）。
func (s *Server) handleRoomSend(w http.ResponseWriter, r *http.Request) {
	roomID := r.PathValue("id")
	var req struct {
		Body     string   `json:"body"`
		Refs     []string `json:"refs,omitempty"`
		Mentions []string `json:"mentions,omitempty"`
	}
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		writeErr(w, http.StatusBadRequest, errors.New("bad json"))
		return
	}
	if strings.TrimSpace(req.Body) == "" {
		writeErr(w, http.StatusBadRequest, errors.New("消息正文不能为空"))
		return
	}
	seq, err := s.rooms.Send(roomID, proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: req.Body, Refs: req.Refs, Mentions: req.Mentions,
	}, s.roomUserActor(r))
	if err != nil {
		s.log.Warn("房间消息发送失败", "room", roomID, "actor", s.roomUserActor(r), "cause", err)
		collabErr(w, err)
		return
	}
	s.log.Info("房间消息已发送", "room", roomID, "seq", seq, "actor", s.roomUserActor(r))
	writeJSON(w, http.StatusOK, map[string]int64{"seq": seq})
}

// handleRoomRead POST /api/rooms/{id}/read {upto_seq} → MarkRead（打开房间即已读）。
func (s *Server) handleRoomRead(w http.ResponseWriter, r *http.Request) {
	roomID := r.PathValue("id")
	var req struct {
		UptoSeq int64 `json:"upto_seq"`
	}
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		writeErr(w, http.StatusBadRequest, errors.New("bad json"))
		return
	}
	if err := s.rooms.MarkRead(s.roomUserActor(r), roomID, req.UptoSeq); err != nil {
		s.log.Warn("已读游标置位失败", "room", roomID, "member", s.roomUserActor(r), "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	writeJSON(w, http.StatusOK, map[string]bool{"ok": true})
}

// handleInbox GET /api/inbox → 收件箱三源聚合（契约 §3.6 编排单元，位置冻结在
// gateway 层）。decision/mention 源失败如实 500（承重）；ticket 源失败降级跳过
// （与 handleCardsList 的 tickets 徽标同族，不吞主查询）。
func (s *Server) handleInbox(w http.ResponseWriter, r *http.Request) {
	items := make([]proto.InboxItem, 0, 8)

	// 源① decision：open 裁决全量（含 CardID 为空的项目级），岔口二方案 A 直调面。
	decisions, err := s.ledger.ListDecisions(true)
	if err != nil {
		s.log.Warn("收件箱 decision 源读取失败", "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	for _, d := range decisions {
		payload, err := json.Marshal(ledgerDecisionWire(d))
		if err != nil {
			s.log.Warn("收件箱 decision 投影失败", "decision", d.ID, "cause", err)
			continue
		}
		items = append(items, proto.InboxItem{
			Origin: proto.InboxOriginDecision, Title: decisionTitle(d), CardID: d.CardID,
			RefID: strconv.FormatInt(d.ID, 10), Payload: payload,
		})
	}

	// 源② ticket：等待人工输入态任务上的未答复工单；Watchers>0 排除（无人驱动
	// 才上浮），破坏性工单不受限（既有硬纪律，D-destructive 判据见 ticketDestructive）。
	tasks, err := s.st.ListTasks()
	if err != nil {
		s.log.Warn("收件箱 ticket 源读取失败", "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	for _, task := range tasks {
		if task.State != proto.TaskStateWaitingAnswer {
			continue
		}
		pending, err := s.st.PendingTickets(task.ID)
		if err != nil {
			s.log.Warn("收件箱 ticket 源读取失败", "task", task.ID, "cause", err)
			continue
		}
		if len(pending) == 0 {
			continue
		}
		watchers := s.hub.Watchers(task.ID)
		for _, tk := range pending {
			if watchers > 0 && !s.ticketDestructive(task.ID, tk.ID) {
				continue
			}
			items = append(items, proto.InboxItem{
				Origin: proto.InboxOriginTicket, Title: ticketTitle(tk), RefID: tk.ID,
			})
		}
	}

	// 源③ mention：@用户的未消费提及（collab.Mentions 已含未消费过滤，C5 锁住）。
	mentions, err := s.rooms.Mentions(s.roomUserActor(r), 0, 0)
	if err != nil {
		s.log.Warn("收件箱 mention 源读取失败", "member", s.roomUserActor(r), "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	for _, ev := range mentions {
		var msg proto.RoomMessage
		if err := json.Unmarshal(ev.Payload, &msg); err != nil {
			continue
		}
		items = append(items, proto.InboxItem{
			Origin: proto.InboxOriginMention, Title: "@你：" + msg.Body, CardID: ev.CardID,
			RefID: strconv.FormatInt(ev.Seq, 10),
		})
	}

	s.log.Info("收件箱已聚合", "member", s.roomUserActor(r), "items", len(items))
	writeJSON(w, http.StatusOK, map[string]any{"items": items})
}

// ticketDestructive 判定工单是否属「破坏性/不可逆/外部可见」类（既有硬纪律的机械
// 代理，D-destructive 台账 L10）：该工单 task 事件流存在 approver_decision 事件
// （ticket_id 匹配且 decision ∈ {escalate, error}）——审批者不敢放行或裁决失败即
// 系统标记的危险请求。窗口用 recentEventsLimit=100（server.go:65，与任务详情同款）。
func (s *Server) ticketDestructive(taskID, ticketID string) bool {
	events, err := s.st.EventsFrom(taskID, 0, recentEventsLimit)
	if err != nil {
		return false
	}
	for _, ev := range events {
		if ev.Type != proto.EventTypeApproverDecision {
			continue
		}
		var p approverDecisionPayload
		if err := json.Unmarshal(ev.Payload, &p); err != nil {
			continue
		}
		if p.TicketID == ticketID && (p.Decision == "escalate" || p.Decision == "error") {
			return true
		}
	}
	return false
}

// decisionTitle 收件箱 decision 条目标题 = 裁决正文首行（简报六段的第一段是「一句话」）。
func decisionTitle(d ledger.Decision) string {
	body := strings.TrimSpace(d.Body)
	if i := strings.IndexByte(body, '\n'); i >= 0 {
		body = body[:i]
	}
	if n := len([]rune(body)); n > 80 {
		return string([]rune(body)[:80]) + "…"
	}
	return body
}

// ticketTitle 收件箱 ticket 条目标题：gate=权限工单、ask=提问工单。
func ticketTitle(tk proto.Ticket) string {
	if tk.Kind == "gate" {
		return "权限工单待答复"
	}
	return "提问工单待答复"
}

// handleCardRebind POST /api/cards/{id}/rebind {to_session,carrier,expect} → 换绑。
// 走 rebindPort 端口（岔口二条件 2/4：换绑是本期新增账本能力、一律经门面，而
// gateway handler 不得引用门面——端口接口定义在此、具体绑定由组装点注入）。
// CAS 冲突由账本哨兵映射 409（ledgerErr）。
func (s *Server) handleCardRebind(w http.ResponseWriter, r *http.Request) {
	id := r.PathValue("id")
	var req struct {
		ToSession string `json:"to_session"`
		Carrier   string `json:"carrier"`
		Expect    string `json:"expect"`
	}
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		writeErr(w, http.StatusBadRequest, errors.New("bad json"))
		return
	}
	if s.rebind == nil {
		writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": "换绑端口未装配"})
		return
	}
	if err := s.rebind.Rebind(id, req.ToSession, req.Carrier, req.Expect); err != nil {
		s.log.Warn("换绑失败", "card", id, "to", req.ToSession, "cause", err)
		ledgerErr(w, err)
		return
	}
	s.log.Info("已换绑", "card", id, "to", req.ToSession, "carrier", req.Carrier)
	writeJSON(w, http.StatusOK, map[string]bool{"ok": true})
}

// rebindPort 是 gateway 房间 handler 对换绑能力的消费侧端口（岔口二条件 4 的
// 「另一个子系统消费侧端口」按此窄读：接口定义在 gateway 消费侧、实现适配器
// 只出现在组装点 server.go）。实现为 facadeBindAdapter（server.go）。
type rebindPort interface {
	Rebind(id, toSession, carrier, expect string) error
}
```

**完整代码块 B——`internal/agentd/ledgerapi.go` registerLedgerRoutes 末尾追加**（:48 之后、:51 之前）：

```go
	api.HandleFunc("GET /api/rooms", s.withRooms(s.handleRoomsList))
	api.HandleFunc("GET /api/rooms/{id}/messages", s.withRooms(s.handleRoomMessages))
	api.HandleFunc("POST /api/rooms/{id}/messages", s.withRooms(s.handleRoomSend))
	api.HandleFunc("POST /api/rooms/{id}/read", s.withRooms(s.handleRoomRead))
	api.HandleFunc("GET /api/inbox", s.withRooms(s.handleInbox))
	api.HandleFunc("POST /api/cards/{id}/rebind", s.withRooms(s.handleCardRebind))
```

**完整代码块 C——`internal/agentd/server.go` Server 结构体字段追加**（:156 `autoLedger` 之后）：

```go
	// B156.2 协作房间：入站门面实例与换绑端口，SetupAutomation 装配。
	rooms  *collab.Service
	rebind rebindPort
```

**完整代码块 D——`internal/agentd/server.go` 测试缝 + 守卫 + 适配器（追加在 SetupAutomation 附近）**：

```go
// SetRooms 注入协作房间服务（测试缝：整体替换 SetupAutomation 构造的实例）。
func (s *Server) SetRooms(svc *collab.Service) { s.rooms = svc }

// SetRebind 注入换绑端口（测试缝：同 SetRooms）。
func (s *Server) SetRebind(p rebindPort) { s.rebind = p }

// withRooms 守卫房间面端点：账本与会话服务未装配时 503（与 withLedger 同款降级）。
func (s *Server) withRooms(h http.HandlerFunc) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		if s.ledger == nil || s.rooms == nil {
			writeJSON(w, http.StatusServiceUnavailable, map[string]string{
				"error": "账本/会话服务未装配",
			})
			return
		}
		h(w, r)
	}
}

// facadeBindAdapter 把账本薄门面的换绑能力适配成 gateway 的 rebindPort（岔口二
// 条件 4 机械判据：单一 Facade 字段、方法体只做转调、由组装点注入消费端口）。
// 定义在组装点文件（server.go = target.json assembly 登记点），它 →Facade 的
// 调用边走组装点豁免（graph check），不构成 gateway 自身引用门面。
type facadeBindAdapter struct {
	f *ledgerapi.Facade
}

func (a facadeBindAdapter) Rebind(id, toSession, carrier, expect string) error {
	return a.f.BindDriver(id, toSession, carrier, expect)
}
```

**完整代码块 E——`internal/agentd/server.go` SetupAutomation 改写**（整函数替换；新增 `cursor` import）：

```go
func (s *Server) SetupAutomation(st *ledger.Store) {
	facade := ledgerapi.New(st)
	s.autoLedger = facade
	s.scheduling = scheduling.New(facadeAsRegistry{f: facade})
	s.rooms = collab.New(facade)
	s.rooms.SetCursorStore(cursor.New(filepath.Join(s.conf().DataDir, "room-cursors.json")))
	s.rebind = facadeBindAdapter{f: facade}
	runner := coordinatorRunner{h: hostapi.New()}
	s.keystone = keystone.New(runner, roomNarrator{c: s.rooms}, facade, attachLocator{})
	if s.pty != nil {
		s.ptyGate = ptyapi.New(s.pty)
	}
}
```

**完整代码块 F——`internal/agentd` 头 import 追加 `cursor`**：

```go
	"github.com/Xsxdot/handoff/internal/collab/cursor"
```

**完整代码块 G——`cmd/agentd.go` setupLedger 内补一行**（`srv.SetLedger(lst)` 之后、镜像挂载前）：

```go
	// B156.2 协作房间面装配：collab 入站门面 + 换绑端口 + 游标介质。设计上
	// SetupAutomation 是全仓唯一组装点（target.json assembly 登记），此处激活。
	srv.SetupAutomation(lst)
```

> SetupAutomation 同时构造 scheduling/keystone（零副作用构造，骨架期注释自证），
> 激活安全；B156.3 的实现卡可直接沿用已装配实例。

### T6.3 正控 (b)：Pointer 路由缺席断言「会红」的证明（强制项，交付必须报两个读数）

非红绿步骤（临时改动，验完还原）。判据 (a) 今天天然是绿的（唯一引用在白名单内），
不证明守卫有牙；本 task 的 (b) 是 (a) 唯一能证明自己会红的途径（C4 那轮 Service.Pointer
空壳能活到验收的同一形态）。

1. （~3min）临时注册：在 `registerLedgerRoutes` 追加一行
   `api.HandleFunc("POST /api/rooms/{id}/pointer", s.withRooms(s.handleRoomPointer))`，并临时写
   `func (s *Server) handleRoomPointer(w http.ResponseWriter, r *http.Request) { _, err := s.rooms.Pointer(r.PathValue("id"), proto.RoomMessage{Body: "x"}); if err != nil { writeErr(w, 500, err); return }; writeJSON(w, 200, map[string]bool{"ok": true}) }`
   （handler 真调 `Service.Pointer`——不是裸空壳）。
2. （~1min）跑 `go test ./internal/agentd/ -run TestPointerRouteAbsentFromSource -v`。
   预期 **FAIL**，红线原文形态：
   `白名单外 Pointer 引用: internal/agentd/<文件> Server.handleRoomPointer`。
   贴台账（plan 轮同形探针已实跑该形态：`--- FAIL` + `白名单外 Pointer 引用:
   .../server.go probeRedPointerCall`，台账 L13 有效读数）。
3. （~1min）**还原全部临时改动**（删除注册行与 handler）。
4. （~1min）复跑同命令，预期 **PASS**。贴台账。
5. 交付把两个读数写进收尾报文/台账：红线文 + 回绿文。

### T6.4 跑绿与回归（红绿收口）

非红绿步骤（回归动作）。

1. （~1min）核对测试文件无占位残留：`grep -n "strings.Repeat\|json.Number\|占位" internal/agentd/roomsapi_test.go` 零命中（代码块为最终形态）。
2. （~2min）跑绿：`go test ./internal/agentd/ -run 'TestRooms|TestRoomSend|TestRoomMessages|TestInbox|TestCardRebind|TestRoomsEndpoints|TestPointerRouteAbsent' -count=1 -v` 逐支核 PASS（plan 轮形态见 §1 基线绿 + T6.1 红文）。
3. （~2min）全量触及回归：`go build ./... && go vet ./internal/agentd/...`。
4. （~2min）**gofmt 闸**：`gofmt -l internal cmd` 必须零输出（charter-implement/review 纪律块正文都无 gofmt，自己收尾）。
5. （~2min）占位残留扫描：`grep -rn "占位\|strings.Repeat(\"\", 0)" internal/agentd/roomsapi_test.go internal/agentd/roomsapi.go` 零命中。
6. （~2min）提交（commit 1，message 见 §11）。

### T6.5 图增量、正控读数与收尾

非红绿步骤（图数据是声明 JSON，非行为断言）。

1. （~6min）图增量：`codegraph/target.json` 追加预定声明（代码块 A）；`codegraph/diffs/cards-B156.2-charter-4.json` 追加 nodesAdded/edgesAdded（代码块 B，`line`/`order` 按下方预填，**若实现过程行号漂移以最终提交文件的实际符号起始行为准**——机械修正，非占位）。
2. （~2min）图闸：`go run . graph check --repo . --view cards-B156.2-charter-4` 预期 fails=[] EXIT=0（**契约与消费边同提交，dead-contract 因有活边而消失**，§0.2）；`go run . graph validate --repo .` EXIT=0。warns 数不作判据（97 与基线一致）。
3. （~2min）**逐容器点数自检**（协调者实况：抽查符号名会命中别的容器同名符号，必须逐容器点数）：
   - k_agentd_Server 新增节点数 == 本卡新增 Server 方法数（六 handler + roomUserActor + ticketDestructive + withRooms = 10）；
   - k_agentd_fn 新增 == collabErr + decisionTitle + ticketTitle + facadeBindAdapter.Rebind = 4；
   - k_collab_fn 新增 == collab.New = 1；k_ledger_api_Facade 新增 == BindDriver = 1。
   脚本：对每个容器 `count(view 节点 container==C)` 比对源码导出符号数。
4. （~4min）**变异复验**（证明判据能变红，全部红文贴台账、逐一还原）：
   - 变异①（强制，Watchers 判据牙）：ticket 源去掉 `if watchers > 0 && ...` 的 watchers 分支（恒上浮）→ `TestInboxTicketExcludesDrivenTask` 红（「Watchers>0 应排除工单」）→ 还原；
   - 变异②（强制，破坏性不受限牙）：ticketDestructive 恒 false → `TestInboxDestructiveTicketFloatsWithWatchers` 红（「破坏性工单不受 Watchers 限制应上浮」）→ 还原；
   - 变异③（强制，错误映射牙）：collabErr 去掉 ErrReadOnly→409 分支 → `TestRoomSendErrMapping` 红（「终态房间应 409」）→ 还原；
   - 变异④（强制，rebind 牙）：facadeBindAdapter.Rebind 改 no-op → `TestCardRebindCASConflict` 红（「换绑未生效」）→ 还原；
   - 还原后 `go test ./internal/agentd/ -count=1` 复绿。
5. （~2min）提交（commit 2，message 见 §11）。

**完整代码块 A——`codegraph/target.json` contracts 段追加**（entries 用实际 label，理由 §0.1；逐字与 B156.3 的 d_keystone→d_collab 同文）：

```json
   {
    "from": "d_gateway",
    "to": "d_collab",
    "legacyBudget": 0,
    "entries": [
     "collab 入站门面"
    ],
    "legacyBudgetNote": "B156.2：预定声明落地（契约 §2.3，entries 用实际容器 label『collab 入站门面』，非 §2.3 字面——逐字照抄会 dead-entry，探针证据台账 L7）。控制面经入站 api 门面调协作房间域（六端点 handler）；预算 0 = 一律走门面，穿透内部即违法。与首个消费边同提交。"
   },
```

**完整代码块 B——`codegraph/diffs/cards-B156.2-charter-4.json` 追加**（在既有键之外新增/扩充；既有键一字不动。节点 `line` 按下方预填，实现后机械修正）：

```json
 "nodesAdded": {
  "n_agentd_Server_handleRoomsList": {"kind": "func", "container": "k_agentd_Server", "order": 2000, "name": "Server.handleRoomsList", "file": "internal/agentd/roomsapi.go", "line": 56, "signature": "func (s *Server) handleRoomsList(w http.ResponseWriter, r *http.Request)", "params": [], "returns": "", "summary": "GET /api/rooms 会话列表。"},
  "n_agentd_Server_handleRoomMessages": {"kind": "func", "container": "k_agentd_Server", "order": 2001, "name": "Server.handleRoomMessages", "file": "internal/agentd/roomsapi.go", "line": 67, "signature": "func (s *Server) handleRoomMessages(w http.ResponseWriter, r *http.Request)", "params": [], "returns": "", "summary": "GET /api/rooms/{id}/messages 房间历史（ErrNoRoom→404）。"},
  "n_agentd_Server_handleRoomSend": {"kind": "func", "container": "k_agentd_Server", "order": 2002, "name": "Server.handleRoomSend", "file": "internal/agentd/roomsapi.go", "line": 89, "signature": "func (s *Server) handleRoomSend(w http.ResponseWriter, r *http.Request)", "params": [], "returns": "", "summary": "POST /api/rooms/{id}/messages 用户发言（actor 服务端注入）。"},
  "n_agentd_Server_handleRoomRead": {"kind": "func", "container": "k_agentd_Server", "order": 2003, "name": "Server.handleRoomRead", "file": "internal/agentd/roomsapi.go", "line": 112, "signature": "func (s *Server) handleRoomRead(w http.ResponseWriter, r *http.Request)", "params": [], "returns": "", "summary": "POST /api/rooms/{id}/read 已读游标置位。"},
  "n_agentd_Server_handleInbox": {"kind": "func", "container": "k_agentd_Server", "order": 2004, "name": "Server.handleInbox", "file": "internal/agentd/roomsapi.go", "line": 127, "signature": "func (s *Server) handleInbox(w http.ResponseWriter, r *http.Request)", "params": [], "returns": "", "summary": "GET /api/inbox 收件箱三源聚合（decision/ticket/mention）。"},
  "n_agentd_Server_handleCardRebind": {"kind": "func", "container": "k_agentd_Server", "order": 2005, "name": "Server.handleCardRebind", "file": "internal/agentd/roomsapi.go", "line": 194, "signature": "func (s *Server) handleCardRebind(w http.ResponseWriter, r *http.Request)", "params": [], "returns": "", "summary": "POST /api/cards/{id}/rebind 换绑（走 rebindPort 端口）。"},
  "n_agentd_Server_roomUserActor": {"kind": "func", "container": "k_agentd_Server", "order": 2006, "name": "Server.roomUserActor", "file": "internal/agentd/roomsapi.go", "line": 51, "signature": "func (s *Server) roomUserActor(r *http.Request) string", "params": [], "returns": "string", "summary": "控制台成员/actor 标识（web:+hostOnly）。"},
  "n_agentd_Server_ticketDestructive": {"kind": "func", "container": "k_agentd_Server", "order": 2007, "name": "Server.ticketDestructive", "file": "internal/agentd/roomsapi.go", "line": 174, "signature": "func (s *Server) ticketDestructive(taskID, ticketID string) bool", "params": [], "returns": "bool", "summary": "破坏性工单判定：approver_decision escalate/error 事件链（D-destructive）。"},
  "n_agentd_Server_withRooms": {"kind": "func", "container": "k_agentd_Server", "order": 2008, "name": "Server.withRooms", "file": "internal/agentd/server.go", "line": 2256, "signature": "func (s *Server) withRooms(h http.HandlerFunc) http.HandlerFunc", "params": [], "returns": "http.HandlerFunc", "summary": "房间端点守卫：账本/会话未装配 503。"},
  "n_agentd_collabErr": {"kind": "func", "container": "k_agentd_fn", "order": 2000, "name": "collabErr", "file": "internal/agentd/roomsapi.go", "line": 36, "signature": "func collabErr(w http.ResponseWriter, err error)", "params": [], "returns": "", "summary": "collab 哨兵→HTTP 状态映射。"},
  "n_agentd_decisionTitle": {"kind": "func", "container": "k_agentd_fn", "order": 2001, "name": "decisionTitle", "file": "internal/agentd/roomsapi.go", "line": 183, "signature": "func decisionTitle(d ledger.Decision) string", "params": [], "returns": "string", "summary": "收件箱 decision 条目标题（Body 首行）。"},
  "n_agentd_ticketTitle": {"kind": "func", "container": "k_agentd_fn", "order": 2002, "name": "ticketTitle", "file": "internal/agentd/roomsapi.go", "line": 190, "signature": "func ticketTitle(tk proto.Ticket) string", "params": [], "returns": "string", "summary": "收件箱 ticket 条目标题（gate/ask）。"},
  "n_agentd_facadeBindAdapter_Rebind": {"kind": "func", "container": "k_agentd_fn", "order": 2003, "name": "facadeBindAdapter.Rebind", "file": "internal/agentd/server.go", "line": 2272, "signature": "func (a facadeBindAdapter) Rebind(id, toSession, carrier, expect string) error", "params": [], "returns": "error", "summary": "换绑端口适配器：转调 Facade.BindDriver。"},
  "n_collab_New": {"kind": "func", "container": "k_collab_fn", "order": 2000, "name": "New", "file": "internal/collab/service.go", "line": 60, "signature": "func New(lc client.LedgerClient) *Service", "params": [], "returns": "*Service", "summary": "包级构造器（契约冻结；视图补户口，台账 L6 选项①）。"},
  "n_ledger_api_Facade_BindDriver": {"kind": "func", "container": "k_ledger_api_Facade", "order": 2000, "name": "Facade.BindDriver", "file": "internal/ledger/api/api.go", "line": 93, "signature": "func (f *Facade) BindDriver(id, session, carrier, expect string) error", "params": [], "returns": "error", "summary": "换绑直通镜像（C1 覆盖债补录，台账 L12）。"}
 },
 "edgesAdded": [
  ["n_agentd_Server_handleRoomsList", "n_collab_Service_ListRooms"],
  ["n_agentd_Server_handleRoomMessages", "n_collab_Service_History"],
  ["n_agentd_Server_handleRoomSend", "n_collab_Service_Send"],
  ["n_agentd_Server_handleRoomRead", "n_collab_Service_MarkRead"],
  ["n_agentd_Server_handleInbox", "n_collab_Service_Mentions"],
  ["n_agentd_Server_handleInbox", "n_ledger_Store_ListDecisions"],
  ["n_agentd_Server_handleInbox", "n_store_Store_ListTasks"],
  ["n_agentd_Server_handleInbox", "n_store_Store_PendingTickets"],
  ["n_agentd_Server_handleInbox", "n_agentd_Hub_Watchers"],
  ["n_agentd_Server_handleCardRebind", "n_agentd_facadeBindAdapter_Rebind"],
  ["n_agentd_facadeBindAdapter_Rebind", "n_ledger_api_Facade_BindDriver"],
  ["n_agentd_Server_SetupAutomation", "n_collab_New"]
 ]
```

> 顺序纪律：edgesAdded 的「调用边门控」按当前源码校验调用文件是否真的 import 对方包。
> **图增量必须在本卡代码落地之后、与代码同批或次批提交**（§0.2 硬顺序：target.json 契约
> 与 edgesAdded 消费边必须同 commit）。
> 图覆盖债如实记录：helper 常量（webUserMember 测试常量不入图）、Server 未导出字段
> （rooms/rebind）不入图（m_collab_Service 同待遇）；`n_collab_New` 与 `n_ledger_api_Facade_BindDriver`
> 是本卡补录的既有覆盖债（构造器/门面方法的户口），不是本卡新造符号。

**测试范围声明（最小化合计）**：本卡只跑 `./internal/agentd/...`（主）；`go build ./...`（依赖面回归）；`go vet ./internal/agentd/...`；图闸 `graph check/validate`；变异复验限定 `-run 'TestInboxTicketExcludesDrivenTask|TestInboxDestructiveTicketFloatsWithWatchers|TestRoomSendErrMapping|TestCardRebindCASConflict'`。全仓测试不属于本卡。

## 4. 缺陷族对抗审查结论（逐族）

| 族 | 设问 | 结论 |
|---|---|---|
| 生命周期/状态机中断 | agentd 重启丢游标？收件箱 Watchers 归零窗口？ | 游标纯缓存（拍板 5.4），重启丢失无害；Watchers 进程内快照、重启短暂归零造成误上浮——拍板 5.4 附注已声明接受，真机清单 #3 观察项；s.ledger==nil/rooms==nil 时 withRooms 503，不 panic |
| 静默失败/误导报错 | 收件箱某源失败会不会被吞成空列表？ | decision/mention 源失败如实 500（承重）；ticket 源失败降级跳过并 warn（handleCardsList 同族先例），不吞主查询；ticketDestructive 事件读失败回 false（保守：当作非破坏性受 Watchers 过滤） |
| 跨平台假设 | RemoteAddr 取 host、时间、游标路径 | roomUserActor 走 hostOnly（IPv6 方括号剥离，hostguard.go:236 既有）；游标文件路径由组装点（持 DataDir）注入；无新平台假设 |
| 假红/假绿测试 | Watchers 判定是不是 mock 假绿？破坏性判据会不会恒假？ | **Watchers 用真实 hub 订阅翻转**（Subscribe/退订，非 mock 返回值）——内建正控；破坏性判据的牙由变异②保证（ticketDestructive 恒 false → 红）；(a) 白名单守卫今天天然绿，由正控 (b) 证明会红（这是本卡唯一的「今天绿」判据，必须报两个读数） |
| 门禁绕过 | Pointer 会不会被 HTTP 路由可达？ | (a) 源码级白名单守卫（读源码的 Go 测试，graph check 无牙的理由见 §1 #4）+ (b) 正控双读数；白名单留正当出口（C7 派发指针需显式加白名单），不许放宽断言 |

**序列化边界设问**：本卡新增的序列化/投影点：
① 收件箱 InboxItem 三源手搭投影——decision 条目 `json.Marshal(ledgerDecisionWire(d))`（复用既有 wire）、mention 条目原样透传 `ev.Payload`（RoomMessage 原始 JSON）——**贯穿真实序列化边界的回归**：TestInboxGoldenKeyShapes 断言三源条目键集与金样本一致（decision 带 card_id+payload、ticket/mention omitempty 省略 card_id），TestInboxRefIDShapes 断言 RefID 三种十进制串/ticket id 原文；「字段缺失 vs 值为零」由 CardID 的 omitempty 键缺席断言分辨（金样本 fixture 孪生，web/src/api/testdata/RoomsFixture.json 不可改）。② RoomSummary wire 由 C8 孪生夹具承接（集成冒烟覆盖一次真实序列化边界）。③ rebind body/响应是纯 JSON 转手（无手写投影）。无遗漏投影点。

## 5. 五项检查

1. **缺陷族对抗**：§4 逐族有结论 ✓
2. **序列化边界**：§4 末段，投影点清单 + 金样本键集断言（InboxItem 三源）+ RefID 形状断言 ✓
3. **上下文预算**：§2 八文件圈得出；新跨域边全部落在已声明（d_gateway→d_ledger/orchestration、entry 覆盖）或同提交声明（d_gateway→d_collab）的方向上 ✓
4. **类型标注**：边界型子系统（d_gateway），行为断言机内可闭环（真 SQLite + httptest 全链）；无新增真机清单项（既有真机清单 #1-#6 归协调者）✓
5. **接缝覆盖（双向）**：
   - 测试→缝：全部端点测试的入口 = HTTP handler → 调用链穿过 spec 测试接缝清单 #1（collab 入站 api 门面，调用方侧 = gateway HTTP 层）；收件箱 ticket 源经 s.st/s.hub（d_orchestration 既有面）；无入口偏离 ✓
   - 缝→测试：缝 #1 本卡负责的断言面（ListRooms/History/Send/MarkRead/Mentions 的 gateway 消费 + 错误映射）逐条有端点级测试（§3 T6.1 映射表）✓
   - 内部锁：`TestPointerRouteAbsentFromSource` 声明见 §7；无其他无入口测试 ✓
   - 退路同闸：本计划步骤正文无条件退路（无「若意外先绿就改直喂 X」条款）；「行号漂移按实际修正」是机械修正不是换入口 ✓

## 6. 与 C7/C8 的交界（下游照此写）

- **C7（CLI）**：① `d_cli→d_collab` 预定声明（契约 §2.3）与本卡同文——entries 用实际 label `["collab 入站门面"]`，**不能逐字照抄 §2.3**（dead-entry，§0.1）；② C7 在 `cmd/card_dispatch.go` 落派发指针行（`Service.Pointer`）时**必须同一提交把该引用加进 pointerWhitelist**（file=cmd/card_dispatch.go, fn=<函数名>，写明理由「C7 派发指针：派发成功落卡房间指针行，B156.2 欠账 #11」），否则 C6 的守卫翻红——这是「留正当出口」的既定设计；③ C7 rebind 走 CLI 侧组装（main.go 持 facade 可直调 `facade.BindDriver`，不在 agentd 包不受岔口二条件 4 约束）。
- **C8（web）**：消费 `GET /api/rooms`（`{"rooms":[...]}`）、`GET /api/rooms/{id}/messages`（`{"messages":[...]}`）、`POST /api/rooms/{id}/messages`（`{"seq":N}`）、`POST /api/rooms/{id}/read`（`{upto_seq}`）、`GET /api/inbox`（`{"items":[...]}`）、`POST /api/cards/{id}/rebind`（`{to_session,carrier,expect}`）——响应形状在本卡冻结；成员/actor 标识 = `web:<host>`，C8 的 @ 用户提及须用同一标识；金样本 twin 夹具（web/src/api/testdata/RoomsFixture.json）一字不动。

## 7. 内部锁声明（占位符扫描节的一部分，逐条自我声明）

| 测试 | 入口 | 为什么从声明缝构造不出 |
|---|---|---|
| TestPointerRouteAbsentFromSource | go/ast 源码解析遍历（无 HTTP/Service 入口） | 契约 §4「HTTP/CLI 路由表中不存在可达 Pointer 的入口」与 :404「Pointer 调用方在仓内只有控制面组件」冻的正是**源码级调用方白名单**这一族断言，spec 测试接缝清单（五条）里没有对应缝——它的观测对象是「全仓非测试源码里 Pointer 选择器的分布」，任何 Service 缝/HTTP 缝都观测不到「另一个文件里有没有调 Pointer」。且该断言必须随 go test 复现（graph check 不解析源码，§1 #4）——只能是一支读源码的 Go 测试。判定按接收者形状（go/ast，排除 unsafe/atomic 裸标识符），对第三方同名方法是过包含的（红线安全方向，出口=显式加白名单），机制与排除项理由写进代码头注释（plan 轮已实跑验证） |

退路同闸自查：§3 各 task 步骤正文无条件退路。

## 8. 占位符扫描自我声明

全文无 TBD／「加适当的错误处理」／「同 Task N」／描述做什么却不给代码。`roomsapi.go`/`pointer_gate_test.go` 全文件给出、`server.go`/`ledgerapi.go`/`cmd/agentd.go` 给出完整改动块、target.json/视图 diff 给出完整追加块。**正当出口（「断言逐条列全 + 指认既有 harness」例外）使用情况：未使用**——roomsapi_test.go 全部断言给出全文（复用 newLedgerEnv/seedCard/seedChildCard/ledgerGet/ledgerPost/getJSON/mustCreateTask 既有助手），无一占位。视图 diff 的 nodesAdded `line`/`order` 按本 plan 代码块排版预填，附机械修正说明（值在提交时确定，非 TBD）。占位残留扫描命令在 T6.4 步骤 5。

## 9. 跨卡审计输入（L3 重档：扇出前由协调者组织独立上下文审计；本节提供素材并自审待拍板）

**跨卡签名清单（本卡 Produces / 相关 Consumes，逐字符）**：

| 签名 | 产出方 | 消费方 | 一致性 |
|---|---|---|---|
| `ListRooms(project string) ([]proto.RoomSummary, error)` | C5（契约 §3.3 逐字） | 本卡 GET /api/rooms、C7 room list、C8 | 一字一致 |
| `History(roomID string, beforeSeq int64, limit int) ([]proto.LedgerEvent, error)` | C4 | 本卡 GET messages、C8 | 一字一致 |
| `Send(roomID string, msg proto.RoomMessage, actor string) (int64, error)` | C4 | 本卡 POST messages、C7 room send、C8 | 一字一致 |
| `MarkRead(member, roomID string, uptoSeq int64) error` | C5 | 本卡 POST read、C8 | 一字一致 |
| `Mentions(member string, afterSeq int64, limit int) ([]proto.LedgerEvent, error)` | C5 | 本卡收件箱 mention 源 | 一字一致 |
| `Consume(seq int64, consumer string) error` | C5 | C8（房间页交互）；本卡不直接消费 | 一字一致 |
| `ListDecisions(openOnly bool) ([]Decision, error)` | 既有 ledger | 本卡收件箱 decision 源（岔口二方案 A） | ledger/decisions.go:93 |
| `PendingTickets(taskID string) ([]proto.Ticket, error)` / `ListTasks() ([]proto.Task, error)` | 既有 store | 本卡 ticket 源 | store.go:1251/:409 |
| `Watchers(taskID string) int` | 既有 Hub | 本卡 ticket 源 | hub.go:118 |
| `BindDriver(id, session, carrier, expect string) error`（Facade） | C1 | 本卡 rebind 适配器转调 | api.go:93 |
| `RebindDriver(id, toSession, carrier, expect string) error`（Store） | C1 | facade.BindDriver 转调 | binding.go:79 |
| rebindPort.Rebind（本卡新） | 本卡 | C6 自身 handler | 与 Facade.BindDriver 参数逐字符一致 |
| target.json `d_gateway→d_collab` 预定声明 | 本卡 | 同提交 edgesAdded 消费边 | §0.2 硬顺序 |

**spec 故事归属（本卡）**：故事 1（深夜清账）的收件箱三源聚合与答复面——本卡实现 inbox 编排（decision/ticket/mention 三源判据逐条）、C8 承接渲染与答复直达；故事 2/4/5 的指针/简报书写面归 C4/C7，本卡只保证 Pointer 路由缺席（(a)(b)）；故事 6（跨卡 @）的 @ 定向消费归 C5，本卡经 Mentions 端进收件箱。

**图覆盖债**：`n_collab_New`、`n_ledger_api_Facade_BindDriver` 为本卡补录的既有覆盖债（§0.3/§0.5，视图 diff 补户口）；`ledgerapi.New` 构造器无节点（SetupAutomation→ledgerapi.New 边仍缺失，与 collab.New 同族但不在本卡 handler 调用面，记债不补）；收件箱 decision 源的 `s.ledger.ListDecisions` 直调是岔口二条件 3 指定的「将来整体迁门面时一并迁」的可观测点。

## 10. 自审三查

1. **spec 覆盖**：契约 §3.5 表六行逐行指到 T6.1 测试；§3.6 三源判据逐条指到测试（decision open 全量/项目级、ticket Watchers 排除+破坏性不受限、mention 未消费）；§4 收件箱 6 条 + 路由缺席断言指到 T6.1/§0.1；:404 白名单指到 TestPointerRouteAbsentFromSource；澄清三集成冒烟指到 TestInboxIntegrationSmoke；欠账 #5/#11 半/#12 半/#9 Go 半全归属 ✓
2. **占位符扫描**：§8 ✓
3. **跨 task 签名一致性**：§9 清单；collab 方法签名与契约 §3.3 逐字一致（service.go 亲读）；Facade.BindDriver/Store.RebindDriver 逐字一致 ✓

## 11. 提交切分

- commit 1（T6.1-T6.4）：`feat(agentd): 房间 HTTP 面六端点 + 收件箱三源编排 + Pointer 路由缺席源码守卫（B156.2.6 欠账#5+#11路由缺席半+#9Go半）`——含 roomsapi.go、ledgerapi.go（6 行注册）、server.go（字段/缝/SetupAutomation/适配器/withRooms）、cmd/agentd.go（激活行）、roomsapi_test.go、pointer_gate_test.go。**本提交不含 target.json 契约**（无消费边声明，view 无新增边，graph check 保持绿）。
- commit 2（T6.5）：`graph(b156.2.6): gateway→collab 预定声明落地 + 房间 handler 与适配器入图（target + charter-4 视图增量，含 collab.New/Facade.BindDriver 补录）`——含 codegraph/target.json、codegraph/diffs/cards-B156.2-charter-4.json。**target.json 契约与 edgesAdded 消费边同提交（§0.2 硬顺序）**。
- 台账 `docs/superpowers/ledgers/ledger-b156.2.6-plan.md` 随实现轮持续追加，与产出物同批提交。
- **两提交均须通过 `gofmt -l internal cmd` 零输出**（charter-implement/review 纪律块正文均无 gofmt，自己收尾）。

## 12. 明确不做（防越界）

- 不写 C7/C8 任何代码；`d_cli→d_collab` 预定声明归 C7（§0.1 同文提示）。
- 不改 proto（RoomSummary/InboxItem/RoomMessage 形状）、types.go 词表、ledger 生产代码（RebindDriver/RecordRoomMessage 等 C1/C2 已实现，本卡零改动）、金样本 fixture（rooms_fixture_test.go / rooms.test.ts / RoomsFixture.json 一字不动）。
- **不给 ledger.Store 或 collab 加导出测试钩子**：Watchers 翻转用真实 hub 订阅；破坏性判定用真实 store 事件；collab 测试经既有 Service 方法。
- 不改 `registerLedgerRoutes` 的既有行、不重构它造路由清单（判据 (a) 不需要）。
- 不 absorb 视图（finish 阶段动作）；不修裸 graph check 的既有 warn（97 与基线一致）。
- 不加真机清单项（本卡全行为机内可闭环；既有真机清单归协调者）。
- 收件箱 decision 源**不**为满足「新增能力走门面」去扩 Facade——岔口二方案 A 只覆盖既有能力（ListDecisions），条件 2 的边界写进 §0.4；本期新增账本能力（换绑）已按条件 2 走适配器（§0.5）。