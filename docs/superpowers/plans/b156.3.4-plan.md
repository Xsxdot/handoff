# B156.3.4 Plan：K4 协调者生命周期接线——拉起两端点 + 开卡即绑 + attach 互斥

> 状态：plan 节点产出物（本节点只产计划，不产实现代码；diff 里不得出现 `.go`/`.ts`）。
> 卡：B156.3.4 ｜ 有效基线分支：`claude/kai-156-3-ba-df7357` ｜ 合并目标以此为准。
> 上游：spec `docs/superpowers/specs/2026-08-26-b156.3-automation-keystone-design.md`、
> contract `docs/superpowers/specs/b156.3-contract.md`、breakdown
> `docs/superpowers/specs/b156.3-breakdown.md` §三-K4、§十五澄清 2 与「岔口拍板记录」。
> 台账：`docs/superpowers/plans/b156.3.4-plan-ledger.md`（本 plan 节点已分两次提交，
> 含协调者裁决原文与基线探针读数，见文末）。
>
> **有界文件集**（breakdown §三-K4 + 协调者裁决 A 扩容，越出先停下提问）：
> - `internal/keystone/keystone.go`（裁决 A：本卡纳入，改动钉死一处——launchRound 消费
>   入参 spec，公共签名不动）+ 同包测试文件（K2 先例：测试文件是验收义务的物理载体）
> - `internal/agentd/{server.go 接线一行, 新文件 coordapi.go, 新文件 coordapi_test.go}`
> - `internal/client/coordinator.go`（新，CLI 拨号方法——K3 先例 `client/squads.go` 同底座）
> - `cmd/{card.go 加 flag 与调用, 新文件 card_coordinate.go, 新文件 card_coordinate_test.go}`
>
> **K3 前置依赖（协调者裁决，2026-08-26）**：`scheduling.Service.SquadRows() ([]SquadRow, error)`
> （`internal/scheduling/registry_read.go`）、`proto.CoordinatorLaunchResp`、
> `client.CoordinatorLaunch`、`client.decodeWire` 的载体都在 **K3 分支
> `cards/B156.3.3-charter-3`**，尚未并入 HEAD。**implement 轮开工第一件事：确认 K3 已并入
> 功能线**（判据见 Task 0 步 3）；未并入则停下提问，不得自造替代。

---

## 0. 基线复核（判据先在基线跑——本节点已实跑的部分）

以下命令由 plan 作者于基线真实跑过，原始输出见台账：

| 判据 | 基线读数 | 含义 |
|---|---|---|
| `go build ./...` | exit 0 | 触及包可编译 |
| `go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v` | PASS (0.12s) | 竖切 harness 存活；Task A 照抄其 fake 形状 |
| `go test ./internal/keystone/ ./internal/agentd/ ./internal/scheduling/ -count=1` | 三包全 ok（agentd 116.9s） | 红/绿归属有对照 |
| grep HEAD `internal/proto|internal/client|internal/agentd` 的 `CoordinatorLaunch(Resp)` | 计数 0 | K3 交付物未入 HEAD——K4 全部 Task 的编译前提是 K3 已并入 |

**未验证（如实声明）**：Task A–D 的测试红绿态与各夹具在基线的具体输出，需实现票按
本 plan 的取证程序在基线/实现后落证；Task B/C 的编译前提是 K3 已并入（Task 0 步 3 判据）。

## 1. 契约对照表（冻结物逐条 → 本 plan 落点）

| 冻结条目 | 本 plan 对应 |
|---|---|
| contract §11 三端点（POST coordinator/launch、GET coordinator、POST attach；`card add --coordinate`） | Task B 的 coordapi.go 三 handler + 路由；Task D 的 CLI |
| contract §6.2 两入口合一（LaunchForCard source 只进审计） | Task B handleCoordLaunch 把 body.source 原样传 LaunchForCard；Task D 的 CLI 传 card_create |
| contract §15 澄清 2（组装点解析协调者小队 LaunchAdmit→Binding→Carrier 读 HomeDir；keystone launchRound 须消费入参） | Task A 修 launchRound；Task B 的 spec 组装链；T1 断言 CLI/HomeDir/Workdir 非空 |
| 协调者裁决①（SquadRows 过滤 role=coordinator；0→400 指路 / ≥2→409 歧义 / 恰1→用） | Task B resolveCoordinatorSquad + T2/T3 |
| 协调者裁决②（K4 扩容 keystone.go，公共签名不动） | Task A |
| contract §11 附加约束（岔口四 B：未登记返回含指路文案与最小参数示例） | T2 断言 `handoff squad create --role coordinator` |
| spec §4.4 attach 互斥（同一会话不许两个驾驶员；接管态只在进程内存） | Task B handleCoordAttach（SetAttach true/false）；T4 穿过 HTTP 的 Decide 拒醒断言 |
| spec §6.3/contract §6.3 铁律（拉起不产生 task） | T1 `TasksOf` 零断言（竖切断言延伸到 gateway 层） |
| contract §11（POST attach 跨机沿用 ?machine= 转发） | handleCoordAttach 首行 forwardIfRequested |
| 配对原语归属表（SetAttach true=K4 attach 端点；SetAttach false=K4 同端点交回路径） | handleCoordAttach 的 active=true/false 两分支 |
| spec §5.1 开卡即绑（CLI 开卡动作发生时拉起并绑定） | Task D：card add --coordinate → 建卡成功后 CoordinatorLaunchAs(card_create) |

## 2. 设计定案（拍板已定 + plan 级定案，后者进 §6 待拍板）

### D1 协调者小队识别（协调者裁决①，不重开）

`SquadRows()` 过滤 `Role==coordinator`：0 → 400 可行动错误（含 `handoff squad create
--role coordinator --member <载体名>` 最小参数示例，岔口四 B 附加约束）；≥2 → 409
歧义错误逐一点名候选；恰 1 → 用之。零新增签名、零新增配置。SquadRows 载体在 K3 分支，
**引用即声明**：本 plan 的 Consumes 直接写 `scheduling.Service.SquadRows()`，implement
开工前确认 K3 并入（Task 0 步 3）。

### D2 launch 端点的 SessionSpec 组装链（澄清 2 的落点）

```
body.source（缺省 manual）→ GetCard 存在性 → resolveCoordinatorSquad → LaunchAdmit(squad.Name)
  → Carrier(binding.Carrier) → SessionSpec{
       CLI: binding.Executor,        // 有效执行者（覆盖 > 载体缺省，bindingFor 已定）
       HomeDir: carrier.HomeDir,     // 载体显式携带（spec §4.3，不拼 ~）
       Model: binding.Model,
       Workdir: resolveCoordWorkdir(card),  // 项目位置根，best-effort（见 D5）
     }
  → keystone.LaunchForCard(ctx, card, source, spec)
```

`LaunchAdmit` 与 `Carrier` 签名现状冻结一字不改；keystone 侧 `launchRound` 消费入参
spec 由 Task A 补（澄清 2 的骨架期欠账，裁决 A）。

### D3 三端点的响应/错误契约（wire 形状以本 plan 为准，K6 镜像）

| 端点 | 成功 | 失败 |
|---|---|---|
| POST `.../coordinator/launch` | 200 `proto.CoordinatorLaunchResp`（K3 已冻结形状：woke/session_id/rebuilt/escalated/output） | 400 source 非法；404 卡不存在；400 无协调者小队（指路）；409 歧义/并发满；503 自动化层未装配；502 拉起承载失败 |
| GET `.../coordinator` | 200 `{"bound":bool,"attach_active":bool,"attach":{machine,dir,command}\|null}` | 404 卡不存在；503 未装配 |
| POST `.../attach` | 200 `keysclient.AttachInfo`（active=true，json 标签 machine/dir/command 已有）；200 `{"ok":true}`（active=false 交回） | 400 active 必填/未绑定会话（定位失败时回滚接管态）；404 卡不存在；503 未装配 |

launch 的 body 是 `{"source": string}`，空或缺省 = manual（K3 已交付的
`Client.CoordinatorLaunch` 发 `{}` 因此天然走 manual，供看板按钮）。

### D4 组装点接线行（唯一生产接线处仍是 SetupAutomation）

`internal/agentd/server.go` 只加一行路由注册（`registerCoordRoutes(api)`，在
`registerLedgerRoutes(api)` 之后）。**生产启动接线（谁调 SetupAutomation）归 K5**——
cmd/agentd.go:451 现状只 SetLedger，K4 端点以 `withCoordinator` 的 503 守卫未装配态，
与 withLedger 同形（能力未装配是显式状态，不是静默降级）。

### D5 已知边界与移交注记（如实声明，不伪装已解决）

1. **launch 满员不排队（409）**：LaunchAdmit 返回 ErrNoSlot 时端点回 409「并发已满」，
   不写 launch_queue。**launch_queue 的写入臂无人认领**——配对原语表把 Enqueue 归 K2，
   但 K2 只写 ignition_queue；协调者优先的清队顺序（QueueKinds）在无写入臂时退化为
   空转。进 §6 待拍板。
2. **Workdir best-effort**：`resolveCoordWorkdir` 经 `resolveProject("", card.Project,
   s.st.ListProjectLocations())` 尽力解析项目位置根；项目未登记/解析失败 → 空串 + Warn
   日志，不阻断拉起。澄清 2 原文只提 CLI/HOME/模型，Workdir 属 plan 级定案。
3. **重复拉起会覆盖会话引用**（无已绑定守卫）：再次 launch 会以新会话引用覆盖
   `sessions[card]`，旧会话进程由 hostapi Timeout 上界兜底——与「在飞不恢复、宁可误
   唤醒不可漏唤醒」同一口径。进 §6 待拍板（可选加 409 守卫）。
4. **K3 已交付 `Client.CoordinatorLaunch`（无 source，POST `{}`）**：K4 为 CLI 新增
   `CoordinatorLaunchAs(cardID, source)`——两条拨号方法并存，K3 版供看板/控制台
   （manual），K4 版供开卡即绑（card_create）。是否合并待协调者裁决（§6）。
5. 两笔图债（baseline 行号漂移、陈旧域声明 d_orchestration）按卡上下文不修不顺手修。

---

## 3. Task 分解

> 粒度说明：每步 2–5 分钟一个动作。红绿模板套在锁缝断言的步骤上（Task A 步 1-4、
> Task B 步 2-6、Task D 步 2-5）。每个 Task 收尾跑「只跑触及包」的最小测试面；全量
> `go test ./...` 属全卡收尾（Task E）。

### Interfaces 总表（跨任务/跨卡的唯一名字来源）

Consumes（现状冻结签名，一字不改；标注 K3 的为 K3 分支交付物）：
- `scheduling.Service.SquadRows() ([]scheduling.SquadRow, error)` **[K3 分支交付]**
- `scheduling.SquadRow{Version int; Squad Squad}`、`Squad.Role scheduling.SquadRole`
- `scheduling.Service.LaunchAdmit(squadName string) (Binding, error)`（scheduling.go:198）
- `scheduling.Service.Carrier(name string) (Carrier, error)`（scheduling.go:138）
- `scheduling.ErrNoSlot / ErrNoHealthy / ErrRoleMismatch / ErrNotFound`（errors.Is 可判）
- `keystone.Service.LaunchForCard(ctx, card, source string, spec keysclient.SessionSpec) (RoundResult, error)`（keystone.go:117，签名不动）
- `keystone.Service.SetAttach(card string, active bool)`（keystone.go:139）、
  `AttachState(card) bool`（keystone.go:126）、`Locate(card, workdir string) (keysclient.AttachInfo, error)`（keystone.go:146）
- `keystone.Service.Decide(ev WakeEvent) Decision`（attach 集成断言用，keystone.go:75）
- `keysclient.SessionSpec{CLI, HomeDir, Model, Workdir string; Env []string}`、
  `keysclient.AttachInfo{Machine, Dir, Command string}`（json 标签 machine/dir/command）
- `proto.CoordinatorLaunchResp{Woke bool; SessionID, Output string; Rebuilt, Escalated bool}` **[K3 分支交付]**
- `client.CoordinatorLaunch(ctx, cardID) (*proto.CoordinatorLaunchResp, error)` **[K3 分支交付，本卡不消费只并存]**
- `resolveProject(projectID, projectName string, entries []proto.ProjectLocation) (proto.ProjectLocation, error)`（agentd 既有，projectresolve.go:67）
- `proto.ProjectLocation{ProjectID, Name, Path, OriginURL string; CreatedAt time.Time}`

Produces：
- `internal/keystone`：`launchRound(card, extra string, spec keysclient.SessionSpec)`——私有签名加 spec 参数（公共签名不动，裁决 A）；LaunchForCard/Wake 两个调用点机械更新
- `internal/agentd`（新文件 coordapi.go）：`registerCoordRoutes(api *http.ServeMux)`、
  `withCoordinator(h http.HandlerFunc) http.HandlerFunc`、
  `handleCoordLaunch` / `handleCoordStatus` / `handleCoordAttach`、
  `resolveCoordinatorSquad() (scheduling.Squad, error)`、`resolveCoordWorkdir(cardID string) string`、
  `var errNoCoordinatorSquad, errAmbiguousCoordinatorSquad error`、`type coordinatorStatusResp`
- `internal/client`（新文件 coordinator.go）：`CoordinatorLaunchAs(ctx, cardID, source string) (*proto.CoordinatorLaunchResp, error)`
- `cmd`（新文件 card_coordinate.go + card.go 加 flag/调用）：`cardAddCoordinate bool`、`coordinateAfterCreate(cmd *cobra.Command, cardID string) error`
- 对 K6（跨卡，待其 plan 消费）：三端点 wire 形状见 §D3；attach 响应即 `keysclient.AttachInfo` 的 machine/dir/command

---

### Task 0：基线复核 + K3 前置确认（不改产品代码）

**文件**：台账追加。
**Interfaces**：无。

1. 复跑基线（与 plan 作者读数对照）：`go build ./...` → exit 0；
   `go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v` → PASS。
   原文贴台账。
2. 确认 K3 已并入功能线，三条缺一不可（合并后跑）：
   - `test -f internal/scheduling/registry_read.go`（SquadRows 载体）；
   - `grep -c "func (s \*Service) SquadRows" internal/scheduling/registry_read.go` ≥ 1；
   - `grep -c "CoordinatorLaunchResp" internal/proto/scheduling.go` ≥ 1。
   任一不满足 = K3 未并入 → **停下提问**，不得自造替代、不得提前开 Task B/C/D。
3. 确认生产接线现状：`grep -n "SetupAutomation" cmd/agentd.go` 零命中（SetLedger 之外
   无自动化装配）——记录在案：K4 端点生产态依赖 K5 的启动接线，503 守卫是预期形态。

**验收**：K3 三判据全过（或已停下提问）；台账含基线输出原文。

---

### Task A：internal/keystone launchRound 消费入参 spec（裁决 A）

**文件**：`internal/keystone/keystone.go`（仅改 launchRound 与两个调用点）、
`internal/keystone/keystone_spec_test.go`（新）。
**Interfaces**：见 §总表 Consumes 的 keystone/keysclient 项。
**测试范围**：只跑 `go test ./internal/keystone/ -count=1`。

1. 【红】建 `keystone_spec_test.go`（整文件，照抄）：

```go
package keystone

import (
	"context"
	"errors"
	"testing"

	"github.com/Xsxdot/handoff/internal/keysclient"
	"github.com/Xsxdot/handoff/internal/proto"
)

// specRecorder 记录 Launch 收到的 SessionSpec——「spec 回潮」的观测点（契约
// §15 澄清 2：launchRound 必须消费入参，防空 spec 回潮）。
type specRecorder struct {
	launches []keysclient.SessionSpec
}

func (r *specRecorder) Launch(spec keysclient.SessionSpec, prompt string) (keysclient.TurnResult, error) {
	r.launches = append(r.launches, spec)
	return keysclient.TurnResult{SessionID: "sess-new", Output: "ok"}, nil
}

func (r *specRecorder) Resume(ref keysclient.SessionRef, prompt string) (keysclient.TurnResult, error) {
	return keysclient.TurnResult{SessionID: ref.SessionID, Output: "ok"}, nil
}

// stubLedgerView 让 briefing 的读账路径返回「未找到」而非崩溃——launchRound 会
// 拼开场简报，nil 接口直接 panic。
type stubLedgerView struct{}

func (stubLedgerView) GetCard(string) (proto.Card, error)               { return proto.Card{}, errors.New("not found") }
func (stubLedgerView) EventsFromAsc([]string, int64, int) ([]proto.LedgerEvent, error) { return nil, nil }
func (stubLedgerView) EffectiveBaseBranch(string) (string, error)       { return "", errors.New("not found") }
func (stubLedgerView) MarkNeedsHuman(string, string, string) error      { return nil }

// TestLaunchForCardConsumesSessionSpec 锁澄清 2：LaunchForCard 的 spec 必须原样
// 传给承载缝（CLI/HomeDir/Model/Workdir 逐一相等）。
//
// 变异复验程序（必须执行，两次读数落台账）：
//  1. 把 launchRound 改回自造空 spec（忽略入参 spec）——本测试必翻红（CLI 变空）；
//  2. 恢复实现 → 复跑转绿。
func TestLaunchForCardConsumesSessionSpec(t *testing.T) {
	rec := &specRecorder{}
	svc := New(rec, nil, stubLedgerView{}, nil)
	want := keysclient.SessionSpec{CLI: "opencode", HomeDir: "/home/coord", Model: "fast", Workdir: "/w"}
	if _, err := svc.LaunchForCard(context.Background(), "B1", "card_create", want); err != nil {
		t.Fatalf("LaunchForCard: %v", err)
	}
	if len(rec.launches) != 1 {
		t.Fatalf("Launch 次数=%d，want 1", len(rec.launches))
	}
	if rec.launches[0] != want {
		t.Fatalf("spec 未消费（回潮）：\n got=%+v\nwant=%+v", rec.launches[0], want)
	}
}
```

   运行 `go test ./internal/keystone/ -run TestLaunchForCardConsumesSessionSpec -count=1`
   → **预期 FAIL**（launchRound 自造空 spec，gotSpec.CLI="" ≠ want）。失败原文贴台账。

2. 【最小实现】改 `internal/keystone/keystone.go` 三处（签名变化只发生在私有
   `launchRound`；`LaunchForCard` 等冻结公共签名一字不动——裁决 A 的「签名不动」指
   公共面，spec 参数的承载只能走私有方法签名）：

   a. `launchRound`（159 行起）签名加 `spec keysclient.SessionSpec`，删掉自造空 spec：

```go
// launchRound 用新载体承接同一协调者身份并绑定。spec 由组装点解析后传入
// （协调者小队 → LaunchAdmit → Binding → Carrier 读 HomeDir，契约 §15 澄清 2），
// 不再自造空 spec——骨架期忽略入参的欠账已补齐，防空 spec 回潮。重建四步的
// 输入由开场简报携带：读卡 → 会话史（账本事件流）→ timeline → 仓内文档指针。
func (s *Service) launchRound(card, extra string, spec keysclient.SessionSpec) (RoundResult, error) {
	if s.narrator != nil {
		_ = s.narrator.Say(card, "载体已更换：新载体承接同一协调者身份（重建四步已执行）")
	}
	result, err := s.runner.Launch(spec, s.briefing(card, nil)+extra)
	if err != nil {
		return RoundResult{}, err
	}
	ref := keysclient.SessionRef{CLI: spec.CLI, SessionID: result.SessionID}
	s.mu.Lock()
	s.sessions[card] = ref
	s.mu.Unlock()
	return RoundResult{Woke: true, SessionID: result.SessionID, Rebuilt: true, Output: result.Output}, nil
}
```

   b. `LaunchForCard`（118 行）转发入参：`result, err := s.launchRound(card, "", spec)`。

   c. `Wake` 的 resume 失败兜底（106 行）补最小 spec（保留原 CLI 连续性；HomeDir/Model
      在此处已不可得，K5 唤醒重建路径届时消费已修好的行为、不再动本函数）：

```go
	// resume 失败：换载体承接同一协调者身份，房间落「载体已更换」指针。
	// 重建 spec 保留原会话的 CLI（其余字段此刻不可得，由 K5 唤醒重建路径补齐）。
	rebuilt, launchErr := s.launchRound(card, prompt, keysclient.SessionSpec{CLI: ref.CLI})
```

3. 【绿】复跑步骤 1 的测试 → PASS。原文贴台账。
4. 【变异复验】按测试注释三步执行并落台账：launchRound 改回自造空 spec → 必红
   （gotSpec.CLI="" 原文）→ 恢复 → 复绿。**无此记录本 Task 不得收尾。**
5. 回归：`go test ./internal/keystone/ -count=1` → 全绿（竖切 TestIgnitionVerticalSlice
   不得翻红——其 LaunchForCard 传 spec{CLI:"opencode"}、fakeRunner 不校验 spec，
   本改动对其只增不减）。

**验收判据**：spec 四字段逐一相等；Launch 恰一次；变异复验两步读数在案；keystone 包全绿。

---

### Task B：internal/agentd 三端点 + 穿过 HTTP 的缝级测试

**文件**：`internal/agentd/coordapi.go`（新）、`internal/agentd/coordapi_test.go`（新）、
`internal/agentd/server.go`（`Handler()` 内 `s.registerLedgerRoutes(api)` 之后加一行
`	s.registerCoordRoutes(api)`）。
**Interfaces**：见 §总表。
**测试范围**：只跑 `go test ./internal/agentd/ -run 'TestCoord' -count=1`（红绿期），
收尾跑 `go test ./internal/agentd/ -count=1`（存量不许翻红）。

1. 【实现·helpers 先行】建 `coordapi.go`（整文件，含路由/守卫/解析/三 handler）：

```go
// coordapi.go 实现协调者生命周期接线（B156.3 K4）：三个端点——
//   POST /api/cards/{id}/coordinator/launch  一键拉起（source=manual，看板按钮）
//   GET  /api/cards/{id}/coordinator         绑定与接管态
//   POST /api/cards/{id}/attach              attach 接管/交回（人工接管互斥的牙齿）
//
// 职责：组装点在此解析协调者小队产出 SessionSpec（契约 §15 澄清 2——SquadRows
// 过滤 role=coordinator → LaunchAdmit → Binding → Carrier 读 HomeDir），这是编制域
// 与 keystone 域在控制面的唯一交汇点。
//
// 边界：
//   - 只走既有入站门面（scheduling.Service / keystone.Service），零新增业务规则；
//   - keystone/scheduling 未装配（SetupAutomation 未执行）时 withCoordinator 给 503，
//     不静默降级（与 withLedger 同形）；
//   - attach 跨机沿用 ?machine= 转发（forwardIfRequested，forward.go:54）；
//   - 拉起路径全程不产生 task（铁律，spec 测试接缝 3，T1 断言）。
package agentd

import (
	"encoding/json"
	"errors"
	"fmt"
	"net/http"
	"strings"

	"github.com/Xsxdot/handoff/internal/keysclient"
	"github.com/Xsxdot/handoff/internal/proto"
	"github.com/Xsxdot/handoff/internal/scheduling"
)

// errNoCoordinatorSquad 表示仓内没有 role=coordinator 的小队（岔口四 B：不做出厂
// 种子，未登记返回指路错误）。
var errNoCoordinatorSquad = errors.New("未登记协调者小队")

// errAmbiguousCoordinatorSquad 表示存在多个协调者小队，拉起目标不唯一。
var errAmbiguousCoordinatorSquad = errors.New("协调者小队不唯一")

func (s *Server) registerCoordRoutes(api *http.ServeMux) {
	api.HandleFunc("POST /api/cards/{id}/coordinator/launch", s.withLedger(s.withCoordinator(s.handleCoordLaunch)))
	api.HandleFunc("GET /api/cards/{id}/coordinator", s.withLedger(s.withCoordinator(s.handleCoordStatus)))
	api.HandleFunc("POST /api/cards/{id}/attach", s.withLedger(s.withCoordinator(s.handleCoordAttach)))
}

// withCoordinator 守卫自动化层装配：keystone 或 scheduling 未装配时 503。
func (s *Server) withCoordinator(h http.HandlerFunc) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		if s.keystone == nil || s.scheduling == nil {
			writeJSON(w, http.StatusServiceUnavailable, map[string]string{
				"error": "自动化层未装配（SetupAutomation 未执行）",
			})
			return
		}
		h(w, r)
	}
}

// resolveCoordinatorSquad 找出唯一的协调者小队（SquadRows 过滤 role=coordinator）。
// 0 → errNoCoordinatorSquad；≥2 → errAmbiguousCoordinatorSquad（错误带全部候选名）。
// SquadRows 由 K3 交付（internal/scheduling/registry_read.go），开工前已确认并入。
func (s *Server) resolveCoordinatorSquad() (scheduling.Squad, error) {
	rows, err := s.scheduling.SquadRows()
	if err != nil {
		return scheduling.Squad{}, err
	}
	var names []string
	var single *scheduling.Squad
	for i := range rows {
		if rows[i].Squad.Role != scheduling.RoleCoordinator {
			continue
		}
		names = append(names, rows[i].Squad.Name)
		if single == nil {
			q := rows[i].Squad
			single = &q
		}
	}
	switch len(names) {
	case 0:
		return scheduling.Squad{}, errNoCoordinatorSquad
	case 1:
		return *single, nil
	default:
		return scheduling.Squad{}, fmt.Errorf("%w（候选：%s）", errAmbiguousCoordinatorSquad, strings.Join(names, "、"))
	}
}

// handleCoordLaunch POST /api/cards/{id}/coordinator/launch：一键拉起协调者并绑定
// （spec §5.1 入口 2）。source 记录拉起来源（manual=看板按钮，card_create=开卡即绑），
// 只进审计；两入口共用 keystone.LaunchForCard 单一实现（§6.2）。
//
// 行为顺序：source 校验 → 卡存在 → 协调者小队识别（0→400 指路 / ≥2→409 歧义）→
// LaunchAdmit 两级准入（满→409）→ 载体读 HomeDir → 组装 SessionSpec → LaunchForCard。
func (s *Server) handleCoordLaunch(w http.ResponseWriter, r *http.Request) {
	id := r.PathValue("id")
	var body struct {
		Source string `json:"source"`
	}
	if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
		writeErr(w, http.StatusBadRequest, fmt.Errorf("解析请求体: %w", err))
		return
	}
	source := body.Source
	if source == "" {
		source = "manual"
	}
	if source != "manual" && source != "card_create" {
		writeErr(w, http.StatusBadRequest, fmt.Errorf("source 只能是 manual 或 card_create，收到 %q", source))
		return
	}
	if _, err := s.ledger.GetCard(id); err != nil {
		ledgerErr(w, err)
		return
	}
	squad, err := s.resolveCoordinatorSquad()
	if err != nil {
		switch {
		case errors.Is(err, errNoCoordinatorSquad):
			// 岔口四 B：不做出厂种子，未登记返回含指路文案与最小参数示例的可行动错误。
			writeErr(w, http.StatusBadRequest, fmt.Errorf(
				"未登记协调者小队：先登记载体，再登记协调者小队（示例：handoff squad create --name coord --role coordinator --member <载体名> --max-concurrency 1）"))
			return
		case errors.Is(err, errAmbiguousCoordinatorSquad):
			writeErr(w, http.StatusConflict, err)
			return
		default:
			s.log.Error("协调者小队识别失败", "card", id, "cause", err)
			writeErr(w, http.StatusInternalServerError, err)
			return
		}
	}
	binding, err := s.scheduling.LaunchAdmit(squad.Name)
	if err != nil {
		if errors.Is(err, scheduling.ErrNoSlot) {
			writeErr(w, http.StatusConflict, fmt.Errorf(
				"协调者并发已满（小队 %s）：等现役回合结束名额自动回收后重试，或先 attach 现有会话", squad.Name))
			return
		}
		s.log.Error("协调者准入被拒", "card", id, "squad", squad.Name, "cause", err)
		writeErr(w, http.StatusBadRequest, fmt.Errorf("协调者准入被拒: %w", err))
		return
	}
	carrier, err := s.scheduling.Carrier(binding.Carrier)
	if err != nil {
		s.log.Error("读协调者载体失败", "card", id, "carrier", binding.Carrier, "cause", err)
		writeErr(w, http.StatusInternalServerError, fmt.Errorf("读载体 %s: %w", binding.Carrier, err))
		return
	}
	spec := keysclient.SessionSpec{
		CLI:     binding.Executor,
		HomeDir: carrier.HomeDir,
		Model:   binding.Model,
		Workdir: s.resolveCoordWorkdir(id),
	}
	s.log.Info("拉起协调者", "card", id, "source", source, "squad", squad.Name,
		"carrier", binding.Carrier, "target", binding.Target, "cli", spec.CLI,
		"home_dir", spec.HomeDir, "workdir", spec.Workdir)
	result, err := s.keystone.LaunchForCard(r.Context(), id, source, spec)
	if err != nil {
		s.log.Error("拉起协调者失败", "card", id, "source", source, "cause", err)
		writeErr(w, http.StatusBadGateway, fmt.Errorf("拉起协调者失败: %w", err))
		return
	}
	writeJSON(w, http.StatusOK, proto.CoordinatorLaunchResp{
		Woke: result.Woke, SessionID: result.SessionID, Rebuilt: result.Rebuilt,
		Escalated: result.Escalated, Output: result.Output,
	})
}

// resolveCoordWorkdir 尽力解析卡所属项目在本机的位置根作为会话工作目录
// （SessionSpec.Workdir=项目位置根）。解析不到不阻断拉起（置空由承载层缺省），
// 只留日志——plan §D5.2 的 best-effort 定案。
func (s *Server) resolveCoordWorkdir(cardID string) string {
	card, err := s.ledger.GetCard(cardID)
	if err != nil {
		return ""
	}
	entries, err := s.st.ListProjectLocations()
	if err != nil {
		s.log.Warn("协调者工作目录解析失败：列项目位置出错", "card", cardID, "cause", err)
		return ""
	}
	loc, err := resolveProject("", card.Project, entries)
	if err != nil {
		s.log.Warn("协调者工作目录解析失败：项目未登记", "card", cardID, "project", card.Project, "cause", err)
		return ""
	}
	return loc.Path
}

// coordinatorStatusResp 是 GET /api/cards/{id}/coordinator 的响应（绑定与接管态）。
// attach 为 null 即未绑定协调者会话（bound=false）。K6 按本形状镜像。
type coordinatorStatusResp struct {
	Bound        bool                   `json:"bound"`
	AttachActive bool                   `json:"attach_active"`
	Attach       *keysclient.AttachInfo `json:"attach,omitempty"`
}

func (s *Server) handleCoordStatus(w http.ResponseWriter, r *http.Request) {
	id := r.PathValue("id")
	if _, err := s.ledger.GetCard(id); err != nil {
		ledgerErr(w, err)
		return
	}
	active := s.keystone.AttachState(id)
	var attach *keysclient.AttachInfo
	if info, err := s.keystone.Locate(id, ""); err == nil {
		attach = &info
	}
	s.log.Debug("协调者状态读出", "card", id, "bound", attach != nil, "attach_active", active)
	writeJSON(w, http.StatusOK, coordinatorStatusResp{
		Bound: attach != nil, AttachActive: active, Attach: attach,
	})
}

// handleCoordAttach POST /api/cards/{id}/attach：attach 深聊的接管/交回同一端点
// （配对原语表：SetAttach true 与 false 都在本端点）。
//
// active=true：先置接管再定位（缺口期宁可占用不可唤醒）；定位失败回滚接管态并 400。
// active=false：交回无头，自动唤醒恢复。
func (s *Server) handleCoordAttach(w http.ResponseWriter, r *http.Request) {
	if s.forwardIfRequested(w, r) { // 跨机 ?machine= 转发（契约 §11）
		return
	}
	id := r.PathValue("id")
	var body struct {
		Active  *bool  `json:"active"`
		Workdir string `json:"workdir"`
	}
	if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
		writeErr(w, http.StatusBadRequest, fmt.Errorf("解析请求体: %w", err))
		return
	}
	if body.Active == nil {
		writeErr(w, http.StatusBadRequest, fmt.Errorf("active 必填（true=接管，false=交回无头）"))
		return
	}
	if _, err := s.ledger.GetCard(id); err != nil {
		ledgerErr(w, err)
		return
	}
	if *body.Active {
		s.keystone.SetAttach(id, true)
		info, err := s.keystone.Locate(id, body.Workdir)
		if err != nil {
			s.keystone.SetAttach(id, false) // 回滚：没有终端可接管，不得留下静默占用
			s.log.Warn("attach 定位失败，接管态已回滚", "card", id, "cause", err)
			writeErr(w, http.StatusBadRequest, err)
			return
		}
		s.log.Info("attach 接管", "card", id, "machine", info.Machine, "dir", info.Dir, "command", info.Command)
		writeJSON(w, http.StatusOK, info)
		return
	}
	s.keystone.SetAttach(id, false)
	s.log.Info("attach 交回", "card", id)
	writeJSON(w, http.StatusOK, map[string]bool{"ok": true})
}
```

2. 【红】建 `coordapi_test.go`，先写夹具 + T1/T1b/T2/T3（T4/T5 可同文件同批写入）:

```go
package agentd

import (
	"encoding/json"
	"errors"
	"net/http"
	"strings"
	"testing"

	"github.com/Xsxdot/handoff/internal/keystone"
	"github.com/Xsxdot/handoff/internal/keysclient"
	"github.com/Xsxdot/handoff/internal/ledger"
	"github.com/Xsxdot/handoff/internal/proto"
	"github.com/Xsxdot/handoff/internal/scheduling"
)

// fakeCoordRunner 记录 Launch 收到的 SessionSpec 与次数（防「spec 回潮」的 agentd
// 侧观测点）；failLaunch 置真时 Launch 报错——source 只进审计与错误包装，承载失败
// 的错误原文是「source 进 LaunchForCard」的唯一可观测面。
type fakeCoordRunner struct {
	launches   []keysclient.SessionSpec
	resumes    []string
	failLaunch bool
}

func (r *fakeCoordRunner) Launch(spec keysclient.SessionSpec, prompt string) (keysclient.TurnResult, error) {
	r.launches = append(r.launches, spec)
	if r.failLaunch {
		return keysclient.TurnResult{}, errors.New("承载不可用")
	}
	return keysclient.TurnResult{SessionID: "sess-coord", Output: "ok"}, nil
}

func (r *fakeCoordRunner) Resume(ref keysclient.SessionRef, prompt string) (keysclient.TurnResult, error) {
	r.resumes = append(r.resumes, prompt)
	return keysclient.TurnResult{SessionID: ref.SessionID, Output: "ok"}, nil
}

// fakeCoordNarrator 记录叙事文本（keystone 出站缝③ 替身）。
type fakeCoordNarrator struct{ lines []string }

func (n *fakeCoordNarrator) Say(cardID, text string) error { n.lines = append(n.lines, text); return nil }

// newCoordEnv 装配真实编制域（SetupAutomation 全真接线：facadeAsRegistry +
// scheduling.Service）+ 注入 fake 端口的真实 keystone（fakeCoordRunner 记录 spec）。
// launch 的 SessionSpec 组装必须真实穿过编制域解析（SquadRows→LaunchAdmit→Carrier）。
func newCoordEnv(t *testing.T) (*ledgerEnv, *fakeCoordRunner) {
	t.Helper()
	env := newLedgerEnv(t)
	env.srv.SetupAutomation(env.ledger)
	runner := &fakeCoordRunner{}
	env.srv.SetKeystone(keystone.New(runner, &fakeCoordNarrator{}, env.srv.autoLedger, attachLocator{}))
	return env, runner
}

// createCoordCard 建一张协调者测试卡（project=handoff，attachment-gates 流）。
func createCoordCard(t *testing.T, env *ledgerEnv) string {
	t.Helper()
	id, err := env.ledger.CreateCard(ledger.NewCard{
		Title: "协调者生命周期卡", Project: "handoff", Workflow: "attachment-gates", Actor: "test"})
	if err != nil {
		t.Fatalf("建卡: %v", err)
	}
	return id
}

// seedCoordinatorSquad 登记协调者载体 + 小队（物理位/政策位各 1）。
func seedCoordinatorSquad(t *testing.T, env *ledgerEnv) {
	t.Helper()
	svc := env.srv.Scheduling()
	if err := svc.PutCarrier(scheduling.Carrier{Name: "c1", Machine: "linux-01",
		CLI: "opencode", HomeDir: "/home/coordinator",
		Credential: scheduling.CredentialStandalone}, 0); err != nil {
		t.Fatalf("登记载体: %v", err)
	}
	if err := svc.PutSquad(scheduling.Squad{Name: "coord", Role: scheduling.RoleCoordinator,
		Members: []string{"c1"}, MaxConcurrency: 1}, 0); err != nil {
		t.Fatalf("登记协调者小队: %v", err)
	}
}

// TestCoordLaunchEndpointSuccess 缝⑤×缝①×缝②：一键拉起（source=manual）走真实
// 编制域解析出非空 SessionSpec（CLI/HomeDir/Workdir 逐一断言——钉澄清 2，防空
// spec 回潮）；拉起恰一次 Launch；两级计数各 +1；全程不产生 task。
func TestCoordLaunchEndpointSuccess(t *testing.T) {
	env, runner := newCoordEnv(t)
	seedCoordinatorSquad(t, env)
	if err := env.st.CreateProjectLocation(&proto.ProjectLocation{
		Name: "handoff", Path: "/repo/handoff"}); err != nil {
		t.Fatalf("登记项目位置: %v", err)
	}
	cardID := createCoordCard(t, env)

	code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator/launch",
		`{"source":"manual"}`)
	if code != http.StatusOK {
		t.Fatalf("launch 状态=%d body=%s", code, body)
	}
	var resp struct {
		Woke      bool   `json:"woke"`
		SessionID string `json:"session_id"`
	}
	if err := json.Unmarshal([]byte(body), &resp); err != nil || !resp.Woke || resp.SessionID != "sess-coord" {
		t.Fatalf("launch 响应异常: %s err=%v", body, err)
	}
	if len(runner.launches) != 1 {
		t.Fatalf("Launch 次数=%d，want 恰 1", len(runner.launches))
	}
	got := runner.launches[0]
	if got.CLI != "opencode" || got.HomeDir != "/home/coordinator" {
		t.Fatalf("spec 回潮：CLI/HomeDir 必须由编制域解析出来，got=%+v", got)
	}
	if got.Workdir != "/repo/handoff" {
		t.Fatalf("工作目录应解析为项目位置根，got=%q", got.Workdir)
	}
	facade := env.srv.autoLedger
	for key, want := range map[string]int{"squad/coord": 1, "carrier/c1": 1} {
		if n := runningCountIn(t, facade, key); n != want {
			t.Fatalf("计数 %s=%d，want %d", key, n, want)
		}
	}
	// 铁律：拉起路径全程不产生 task（竖切断言延伸到 gateway 层）。
	links, err := env.ledger.TasksOf(cardID)
	if err != nil || len(links) != 0 {
		t.Fatalf("拉起不应产生 task：links=%v err=%v", links, err)
	}
}

// TestCoordLaunchSourceFlowsIntoKeystone 缝⑤×缝②：source 参数原样进 LaunchForCard
//（来源只进审计与错误包装——承载失败时错误原文含「来源 <source>」）。两值各验一遍，
// 每次拉起恰触发一次 Launch。
func TestCoordLaunchSourceFlowsIntoKeystone(t *testing.T) {
	env, runner := newCoordEnv(t)
	seedCoordinatorSquad(t, env)
	runner.failLaunch = true
	cardID := createCoordCard(t, env)

	for _, tc := range []struct{ src, want string }{
		{"manual", "来源 manual"},
		{"card_create", "来源 card_create"},
	} {
		code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator/launch",
			`{"source":"`+tc.src+`"}`)
		if code != http.StatusBadGateway {
			t.Fatalf("[%s] 承载失败应 502，状态=%d body=%s", tc.src, code, body)
		}
		if !strings.Contains(body, tc.want) {
			t.Fatalf("[%s] 来源未进 LaunchForCard：body=%s", tc.src, body)
		}
	}
	if len(runner.launches) != 2 {
		t.Fatalf("两次拉起应各触发恰一次 Launch，实际 %d", len(runner.launches))
	}
}

// TestCoordLaunchNoSquadActionableError 缝⑤×缝①：未登记协调者小队时 400 且报文
// 含指路文案与最小参数示例（岔口四 B 附加约束）。执行者小队在场证明过滤按 role 生效。
func TestCoordLaunchNoSquadActionableError(t *testing.T) {
	env, _ := newCoordEnv(t)
	svc := env.srv.Scheduling()
	if err := svc.PutCarrier(scheduling.Carrier{Name: "e1", Machine: "linux-01",
		CLI: "opencode", Credential: scheduling.CredentialStandalone}, 0); err != nil {
		t.Fatal(err)
	}
	if err := svc.PutSquad(scheduling.Squad{Name: "exec", Role: scheduling.RoleExecutor,
		Members: []string{"e1"}}, 0); err != nil {
		t.Fatal(err)
	}
	cardID := createCoordCard(t, env)
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator/launch", `{}`)
	if code != http.StatusBadRequest {
		t.Fatalf("无协调者小队应 400，状态=%d body=%s", code, body)
	}
	for _, want := range []string{"handoff squad create", "--role coordinator", "--member"} {
		if !strings.Contains(body, want) {
			t.Fatalf("指路文案缺 %q：%s", want, body)
		}
	}
}

// TestCoordLaunchAmbiguousSquadConflict 缝⑤×缝①：≥2 个协调者小队 → 409，歧义
// 错误逐一点名候选。
func TestCoordLaunchAmbiguousSquadConflict(t *testing.T) {
	env, _ := newCoordEnv(t)
	svc := env.srv.Scheduling()
	for _, c := range []scheduling.Carrier{
		{Name: "c1", Machine: "m1", CLI: "opencode", Credential: scheduling.CredentialStandalone},
		{Name: "c2", Machine: "m2", CLI: "opencode", Credential: scheduling.CredentialStandalone},
	} {
		if err := svc.PutCarrier(c, 0); err != nil {
			t.Fatal(err)
		}
	}
	for _, q := range []scheduling.Squad{
		{Name: "coord-a", Role: scheduling.RoleCoordinator, Members: []string{"c1"}},
		{Name: "coord-b", Role: scheduling.RoleCoordinator, Members: []string{"c2"}},
	} {
		if err := svc.PutSquad(q, 0); err != nil {
			t.Fatal(err)
		}
	}
	cardID := createCoordCard(t, env)
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator/launch", `{}`)
	if code != http.StatusConflict {
		t.Fatalf("多协调者小队应 409，状态=%d body=%s", code, body)
	}
	for _, want := range []string{"coord-a", "coord-b"} {
		if !strings.Contains(body, want) {
			t.Fatalf("歧义错误应点名候选，缺 %q：%s", want, body)
		}
	}
}

// TestCoordAttachTakeoverMutesWake 缝⑤×缝②：attach 端点置 SetAttach(card,true) 后
// Decide 对一切种类恒判不醒（竖切已有域内断言，这里穿过 HTTP 层的集成断言），
// 交回后恢复。定位三元组经真实 attachLocator 产出。
func TestCoordAttachTakeoverMutesWake(t *testing.T) {
	env, _ := newCoordEnv(t)
	seedCoordinatorSquad(t, env)
	cardID := createCoordCard(t, env)
	if code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator/launch", `{}`); code != http.StatusOK {
		t.Fatalf("预拉起失败：%d %s", code, body)
	}
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/attach",
		`{"active":true,"workdir":"/repo/handoff"}`)
	if code != http.StatusOK {
		t.Fatalf("attach 接管应 200，状态=%d body=%s", code, body)
	}
	var info struct {
		Machine string `json:"machine"`
		Dir     string `json:"dir"`
		Command string `json:"command"`
	}
	if err := json.Unmarshal([]byte(body), &info); err != nil {
		t.Fatalf("attach 响应解析失败: %v body=%s", err, body)
	}
	if info.Dir != "/repo/handoff" || !strings.Contains(info.Command, "sess-coord") {
		t.Fatalf("定位三元组异常: %+v", info)
	}
	if d := env.srv.keystone.Decide(keystone.WakeEvent{Kind: keystone.WakeTaskTerminal, Card: cardID}); d.Wake {
		t.Fatal("attach 接管中自动唤醒不应发生（穿过 HTTP 的互斥断言）")
	}
	if code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/attach", `{"active":false}`); code != http.StatusOK {
		t.Fatalf("attach 交回应 200，状态=%d body=%s", code, body)
	}
	if d := env.srv.keystone.Decide(keystone.WakeEvent{Kind: keystone.WakeTaskTerminal, Card: cardID}); !d.Wake {
		t.Fatal("交回后自动唤醒应恢复")
	}
}

// TestCoordStatusEndpoint 缝⑤×缝②：GET coordinator 的绑定/接管态三态推进
//（未拉 → bound=false；拉起 → bound=true；接管 → attach_active=true）。
func TestCoordStatusEndpoint(t *testing.T) {
	env, _ := newCoordEnv(t)
	seedCoordinatorSquad(t, env)
	cardID := createCoordCard(t, env)

	code, body := ledgerGet(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator")
	if code != http.StatusOK {
		t.Fatalf("GET coordinator 状态=%d body=%s", code, body)
	}
	for _, want := range []string{`"bound":false`, `"attach_active":false`} {
		if !strings.Contains(body, want) {
			t.Fatalf("未绑定态缺 %s：%s", want, body)
		}
	}
	if code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator/launch", `{}`); code != http.StatusOK {
		t.Fatalf("拉起失败：%d %s", code, body)
	}
	code, body = ledgerGet(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator")
	if code != http.StatusOK || !strings.Contains(body, `"bound":true`) || strings.Contains(body, `"attach_active":true`) {
		t.Fatalf("绑定态异常：%d %s", code, body)
	}
	if code, body := ledgerPost(t, env.testAgentdEnv, "/api/cards/"+cardID+"/attach", `{"active":true}`); code != http.StatusOK {
		t.Fatalf("attach 失败：%d %s", code, body)
	}
	code, body = ledgerGet(t, env.testAgentdEnv, "/api/cards/"+cardID+"/coordinator")
	if code != http.StatusOK || !strings.Contains(body, `"attach_active":true`) {
		t.Fatalf("接管态未反映：%d %s", code, body)
	}
}
```

   补齐 import（`errors`/`net/http`/`strings`/`encoding/json`/`testing` + 四个仓内包）。
   此时 coordapi.go 尚未接线（server.go 未注册路由）：`go test ./internal/agentd/
   -run 'TestCoord' -count=1` → **预期 FAIL/ERROR**（未注册路径 404 / `withCoordinator`
   对未装配 keystone 的 503 冲突——红态以「行为未点亮」为准，编译错说明夹具本身坏了，
   先修夹具再判红）。失败原文贴台账。

3. 【接线】`server.go` 的 `Handler()` 内、`s.registerLedgerRoutes(api)` 之后加一行：
   `	s.registerCoordRoutes(api)`。

4. 【绿】复跑 `go test ./internal/agentd/ -run 'TestCoord' -count=1` → 全绿。原文贴台账。
5. 【变异复验】对「防 spec 回潮」做变异（Task A 已做过同靶的域内变异；这里是穿过
   真实编制域+HTTP 的整链变异）：临时把 handleCoordLaunch 的 spec 组装改回
   `CLI:""`/`HomeDir:""` → T1 必红（spec.CLI 断言失败）→ 恢复 → 复绿。两步读数落台账。
6. 回归：`go test ./internal/agentd/ -count=1` → 全绿（存量 ledgerapi/cardstep/discipline
   测试不许翻红）。

**验收判据汇总**：
- T1：200 + Woke/SessionID 正确；Launch 恰 1；spec.CLI=="opencode"、HomeDir=="/home/coordinator"、
  Workdir=="/repo/handoff"；`squad/coord`==1、`carrier/c1`==1；TasksOf 零；
- T1b：两 source 各 502 且错误含「来源 <source>」；Launch 恰 2；
- T2：400 + 三处指路文案；
- T3：409 + 两候选点名；
- T4：接管 200 + 三元组（Dir 与 command 含 sess-coord）+ 接管中 Decide 拒醒 + 交回恢复；
- T5：三态推进各含对应 JSON 键；
- 变异复验两步读数在案；agentd 包全量绿。

---

### Task C：internal/client CoordinatorLaunchAs + cmd card add --coordinate

> 注：本 Task 命名沿用 breakdown 的「cmd/create 路径」语义——仓内创建命令是
> `card add`（cmd/card.go:29），契约/拆解写 `card create --coordinate` 的措辞映射到
> `card add --coordinate`（§6 待拍板 ②）。不新增 `card create` 命令。

**文件**：`internal/client/coordinator.go`（新）、`cmd/card_coordinate.go`（新）、
`cmd/card.go`（加 flag 与调用）、`cmd/card_coordinate_test.go`（新）。
**Interfaces**：见 §总表；`client.decodeWire` 由 K3 交付（client/squads.go）。
**测试范围**：只跑 `go test ./internal/client/ ./cmd/ -run 'TestCardAddCoordinate' -count=1`
（红绿期），收尾跑 `go test ./internal/client/ ./cmd/ -count=1`。

1. 【实现】建 `internal/client/coordinator.go`（整文件）：

```go
// coordinator.go —— 协调者拉起拨号方法（B156.3 K4）。
//
// 与 K3 的 CoordinatorLaunch 同底座（do/httpError/decodeWire）；差别只在带 source：
// K3 版 POST {}（服务端缺省 manual，供看板/控制台按钮），本版显式带 source，供 CLI
// 开卡即绑（card add --coordinate → source=card_create）。两条拨号并存的原因见 plan
// §D5.4 / §6 待拍板 ④。
package client

import (
	"context"
	"net/http"
	"net/url"

	"github.com/Xsxdot/handoff/internal/proto"
)

// CoordinatorLaunchAs 以指定 source 拉起并绑定协调者（POST /api/cards/{id}/
// coordinator/launch）。source 只进审计：manual=看板按钮、card_create=开卡即绑。
func (c *Client) CoordinatorLaunchAs(ctx context.Context, cardID, source string) (*proto.CoordinatorLaunchResp, error) {
	resp, err := c.do(ctx, http.MethodPost,
		"/api/cards/"+url.PathEscape(cardID)+"/coordinator/launch",
		map[string]string{"source": source})
	if err != nil {
		return nil, err
	}
	defer resp.Body.Close()
	if resp.StatusCode != http.StatusOK {
		return nil, c.httpError("coordinator launch", resp)
	}
	var out proto.CoordinatorLaunchResp
	if err := decodeWire(resp, &out); err != nil {
		return nil, err
	}
	return &out, nil
}
```

2. 【实现】建 `cmd/card_coordinate.go`（整文件）：

```go
// card_coordinate.go —— card add --coordinate 的开卡即绑接线（B156.3 K4，
// spec §5.1 入口 1）：卡创建成功后向本机 agentd 拉起协调者（source=card_create）。
//
// 失败语义：卡已创建，不因拉起失败回滚（否则用户重跑 add 会造重复卡）；错误以
// stderr 出声 + 结构化日志留痕，命令仍成功退出——建卡是本次命令的主产出，拉起是
// 可补动作（控制台一键拉起 / card coordinate）。plan §D5 / §6 待拍板 ③。
package cmd

import (
	"context"
	"fmt"
	"log/slog"

	"github.com/Xsxdot/handoff/internal/client"
	"github.com/spf13/cobra"
)

// coordinateAfterCreate 在卡创建成功后拉起并绑定协调者（source=card_create）。
func coordinateAfterCreate(cmd *cobra.Command, cardID string) error {
	ctx := cmd.Context()
	if ctx == nil {
		ctx = context.Background()
	}
	addr, token, err := LocalEndpoint()
	if err != nil {
		return fmt.Errorf("读取本机 agentd 端点失败: %w", err)
	}
	cl := client.New(addr, token)
	resp, err := cl.CoordinatorLaunchAs(ctx, cardID, "card_create")
	if err != nil {
		return err
	}
	slog.Default().Info("开卡即绑已拉起协调者", "card", cardID, "session", resp.SessionID, "agentd", addr)
	return nil
}
```

3. 【接线】`cmd/card.go`：
   a. flag 变量区加 `cardAddCoordinate bool`；
   b. `cardAddCmd.RunE` 的 `CreateCard` 成功之后、`printCardJSON` 之前插入：

```go
	// 开卡即绑（spec §5.1 入口 1）：拉起失败不阻断建卡（见 card_coordinate.go 文件头）。
	if cardAddCoordinate {
		if err := coordinateAfterCreate(cmd, card.ID); err != nil {
			slog.Default().Warn("开卡即绑拉起失败（卡已创建，可稍后控制台一键拉起）",
				"card", card.ID, "cause", err)
			fmt.Fprintf(cmd.ErrOrStderr(),
				"注意：协调者拉起失败（%v）——卡 %s 已创建，可稍后从控制台或 card coordinate 拉起\n",
				err, card.ID)
		}
	}
```

   c. `init()` 的 `cardAddCmd.Flags()` 区加：
   `cardAddCmd.Flags().BoolVar(&cardAddCoordinate, "coordinate", false, "开卡即绑：创建成功后拉起并绑定协调者")`。
   补 import `log/slog`。

4. 【红】建 `cmd/card_coordinate_test.go`：

```go
package cmd

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/http/httptest"
	"path/filepath"
	"strings"
	"testing"
	"time"

	"github.com/Xsxdot/handoff/internal/config"
)

// TestCardAddCoordinateTriggersCardCreateLaunch 开卡即绑：card add --coordinate
// 创建成功后恰触发一次 source=card_create 的拉起（fake agentd 记录请求）。
func TestCardAddCoordinateTriggersCardCreateLaunch(t *testing.T) {
	var launches int
	var gotBody string
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		if r.Method == http.MethodPost && strings.Contains(r.URL.Path, "/coordinator/launch") {
			launches++
			b, _ := io.ReadAll(r.Body)
			gotBody = string(b)
			w.Header().Set("Content-Type", "application/json")
			fmt.Fprint(w, `{"woke":true,"session_id":"s1","rebuilt":true}`)
			return
		}
		http.NotFound(w, r)
	}))
	t.Cleanup(ts.Close)

	dir := t.TempDir()
	cfgPath := filepath.Join(dir, "config.yaml")
	if err := config.Save(cfgPath, &config.Config{
		Listen: "127.0.0.1:0", Token: "t", DataDir: dir, StallTimeout: 2 * time.Hour,
		Ledger: config.LedgerConfig{Enabled: true},
	}); err != nil {
		t.Fatalf("写配置: %v", err)
	}
	seedCardCLIForTest(t, dir, []string{"card", "add", "标题", "--project", "demo", "--coordinate"})

	resetFlags(t)
	// 先 resetAllFlags 再设包级变量：resetAllFlags 会把 flag 绑定的包级变量（agentdURL/
	// configPath）复位到默认值，赋值得放在它之后（--config 参数在 Execute 里会再设一次
	// configPath，这里显式赋值是为了 LocalEndpoint 前的次序不依赖 cobra 内部）。
	resetAllFlags(rootCmd)
	configPath = cfgPath
	agentdURL = ts.URL
	rootCmd.PersistentFlags().Set("agentd", ts.URL)
	var out, errb bytes.Buffer
	rootCmd.SetOut(&out)
	rootCmd.SetErr(&errb)
	rootCmd.SetArgs([]string{"--config", cfgPath, "card", "add", "标题", "--project", "demo", "--coordinate"})
	t.Cleanup(func() { rootCmd.SetArgs(nil) })
	if err := Execute(); err != nil {
		t.Fatalf("card add --coordinate 应成功: %v（stderr=%q）", err, errb.String())
	}
	var created struct {
		ID string `json:"id"`
	}
	if err := json.Unmarshal([]byte(strings.TrimSpace(out.String())), &created); err != nil || created.ID == "" {
		t.Fatalf("stdout 应单行卡 JSON: %q err=%v", out.String(), err)
	}
	if launches != 1 {
		t.Fatalf("开卡即绑应恰触发一次拉起，实际 %d", launches)
	}
	if !strings.Contains(gotBody, "card_create") {
		t.Fatalf("source=card_create 未携带: %s", gotBody)
	}
}

// TestCardAddCoordinateLaunchFailureDegrades 拉起失败不阻断建卡：卡已创建，
// stderr 出声可行动提示，命令仍成功（建卡是主产出，拉起是可补动作）。
func TestCardAddCoordinateLaunchFailureDegrades(t *testing.T) {
	var launches int
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		if strings.Contains(r.URL.Path, "/coordinator/launch") {
			launches++
			http.Error(w, `{"error":"未登记协调者小队：handoff squad create ..."}`, http.StatusBadRequest)
			return
		}
		http.NotFound(w, r)
	}))
	t.Cleanup(ts.Close)

	dir := t.TempDir()
	cfgPath := filepath.Join(dir, "config.yaml")
	if err := config.Save(cfgPath, &config.Config{
		Listen: "127.0.0.1:0", Token: "t", DataDir: dir, StallTimeout: 2 * time.Hour,
		Ledger: config.LedgerConfig{Enabled: true},
	}); err != nil {
		t.Fatalf("写配置: %v", err)
	}
	seedCardCLIForTest(t, dir, []string{"card", "add", "标题", "--project", "demo", "--coordinate"})

	resetFlags(t)
	resetAllFlags(rootCmd)
	configPath = cfgPath
	agentdURL = ts.URL
	rootCmd.PersistentFlags().Set("agentd", ts.URL)
	var out, errb bytes.Buffer
	rootCmd.SetOut(&out)
	rootCmd.SetErr(&errb)
	rootCmd.SetArgs([]string{"--config", cfgPath, "card", "add", "标题", "--project", "demo", "--coordinate"})
	t.Cleanup(func() { rootCmd.SetArgs(nil) })
	if err := Execute(); err != nil {
		t.Fatalf("拉起失败不应阻断建卡: %v", err)
	}
	if launches != 1 {
		t.Fatalf("应尝试恰一次拉起，实际 %d", launches)
	}
	var created struct {
		ID string `json:"id"`
	}
	if err := json.Unmarshal([]byte(strings.TrimSpace(out.String())), &created); err != nil || created.ID == "" {
		t.Fatalf("卡应已创建: %q err=%v", out.String(), err)
	}
	if !strings.Contains(errb.String(), "协调者拉起失败") {
		t.Fatalf("stderr 应出声拉起失败提示: %q", errb.String())
	}
}
```

   运行 `go test ./cmd/ -run 'TestCardAddCoordinate' -count=1` → **预期 FAIL**（flag
   未定义 / 无协调者拉起调用，具体红态以编译错或断言失败原文为准）。失败原文贴台账。

5. 【绿】复跑 → 全绿。原文贴台账。
6. 回归：`go test ./internal/client/ ./cmd/ -count=1` → 全绿（card 族存量测试不许翻红）。

**验收判据**：TestCardAddCoordinateTriggersCardCreateLaunch——stdout 单行卡 JSON、
launches==1、gotBody 含 card_create；TestCardAddCoordinateLaunchFailureDegrades——
卡已创建、launches==1、stderr 含「协调者拉起失败」；client 与 cmd 包全量绿。

---

### Task D：反面断言 + 收尾

**文件**：台账收尾。
**测试范围**：全卡最小面 = 触及四包 + 竖切。

1. 【grep 判据】`grep -rn "scheduling\|keysclient\|schedclient" internal/ledgerstep/`
   → 零命中（exit 1）。K4 不触碰 ledgerstep，但「拉起路径不产生 task」的铁律反面
   断言已在 T1 落；本 grep 防装配侧回潮把编制域 import 漏进 ledgerstep。命中即停下。
2. 【门禁】`go build ./... && go vet ./internal/... ./cmd/...`（exit 0）+ `gofmt -l
   internal/keystone internal/agentd internal/client cmd`（无输出）。
3. 【收尾回归】`go test ./internal/keystone/ ./internal/agentd/ ./internal/scheduling/
   ./internal/client/ ./cmd/ -count=1` → 全绿。原文贴台账。
4. 【全量】`go test ./... -count=1` → 除既有环境抖动项（grok 包 TestGrokToolTimingPaired
   偶发超时，单独复跑与整包复跑均 ok，contract §13 已注明）外全绿；图闸
   `go run . graph check --repo .` fails=0（若 K4 新增容器边未声明，停下按 §11
   implements 补登口径办理，不放开闸门）。
5. 台账收尾：核对 §2 五条注记在案、§4 五项检查结论在案、§6 待拍板清单在案。

**验收判据**：grep 零命中；build/vet/gofmt 干净；触及五包全绿；全量测试无新增红；
图闸 fails=0（或已按补登口径办理并记录）。

---

## 4. 五项检查（出稿后自审，逐项结论）

1. **缺陷族对抗审查**：
   - 生命周期族：launch 满员不排队（409）——窗口「出队与真派之间」归 K5；重复拉起覆盖
     会话引用 → §D5.3 注记（hostapi Timeout 上界兜底，与「在飞不恢复」同口径）；attach
     定位失败回滚接管态（半程态不留）✓；agentd 重启接管态丢失 = 拍板记录③明文接受 ✓
   - 静默失败族：三端点每步有日志（拉起始末/准入被拒/定位失败回滚/交回）；T2 的指路
     错误是岔口四 B 的可行动形态 ✓；launch 502 带原始错误（`%w`）✓
   - 跨平台族：attach 命令形态由 attachLocator 按 CLI 拼装（既有，不在本卡拼）；HomeDir
     由载体显式携带（不拼 ~）✓；Windows 条件性真机项照 breakdown 真机清单第 6 条 ✓
   - 假红/假绿族：无 skip 型假绿（全部测试无条件跑）；T1 的 spec 断言带变异复验（§3 Task
     B 步 5）；T1b 的 source 断言是**可违反**的正面断言（source 不透传则 body 缺「来源
     manual」）✓
   - 门禁绕过族：三端点全在 withLedger+withCoordinator 内（未装配 503，无旁路）；CLI
     `card add --coordinate` 经 client.CoordinatorLaunchAs 走 HTTP 单一入口，不存在 CLI
     直连编制域的第二条写入口（K3 的 squad 族同判据）✓
2. **序列化边界设问**（手写投影/序列化逐处点名 + 断言）：
   ① launch 请求体 `{"source"}`（handler decode）——T1b/T2/T3 经真实 JSON 边界 ✓
   ② launch 响应 `proto.CoordinatorLaunchResp`（K3 冻结形状）——T1 断言 woke/session_id ✓
   ③ GET 响应 `coordinatorStatusResp`（本卡手搭 DTO）——T5 断言三个 JSON 键 ✓
   ④ attach 请求体 `{"active","workdir"}` + 响应 `keysclient.AttachInfo`——T4 断言
      dir/command 键 ✓
   ⑤ SessionSpec 组装链（SquadRows→LaunchAdmit→Binding→Carrier→spec 手写投影）——
      T1 断言 CLI/HomeDir/Workdir 逐值 ✓（「两端各自有测试」被整链断言替代）
   ⑥ CLI→agentd 的 source=card_create（client 序列化）——TestCardAddCoordinate
      asserts gotBody 含 card_create ✓
   ⑦ active 用 `*bool` 区分「字段缺失」与「false」（可空类型分辨缺失/零值）✓
3. **上下文预算检查**：四个实现文件 + 四个测试文件 + server.go 一行，全部圈得出；
   Task 0 的 K3 确认是前置门，不是新增文件 ✓
4. **类型标注**：d_keystone/d_gateway/d_cli 均为逻辑型，无边界型真机义务；本卡真机
   依赖（四跳/attach 端到端）全部在 breakdown 真机清单第 1/5/7 条，归协调者执行，
   本卡不新增真机条目 ✓
5. **接缝覆盖（双向，spec §7 接缝清单与本卡相关部分）**：
   - 缝② Keystone 入站 api 门面：T1/T1b/T4/T5 入口全走 HTTP，调用链穿过 LaunchForCard/
     SetAttach/Locate/AttachState/Decide ✓；Task A 单测入口直接是 LaunchForCard（缝②
     本体）✓
   - 缝① 编制域入站 api 门面：T1/T2/T3 调用链穿过 SquadRows/LaunchAdmit/Carrier ✓
   - 缝③ 出站 client 接口：fakeCoordRunner/fakeCoordNarrator 是 keystone 侧端口的
     test 替身（注释标注测试缝），T4 的 Decide 拒醒钉「attach 互斥」、T1 的 TasksOf
     零断言钉「不产生 task」铁律 ✓
   - 缝⑤ 控制台契约层缝：T1–T5 全是 HTTP 端点入口 + TestCardAddCoordinate 经 client
     拨号 ✓
   - 每条被本卡触及的缝至少一支缝级断言（①②③⑤各 ≥1）；「测试→缝」与「缝→测试」
     双向成立 ✓
   - 内部锁：**零**——无入口不在任何缝上的测试（resolveCoordinatorSquad/resolveCoordWorkdir
     只经 HTTP 链覆盖，不抽独立单测）✓
   - 退路闸：本 plan 无「若意外先绿就改成直喂 X」类退路条款 ✓

## 5. 占位符扫描（自我声明）

- TBD /「加适当的错误处理」/「同 Task N」：无。
- 正当出口声明零处——全部测试代码均为完整代码块。
- **K3 前置依赖不是占位符**：`SquadRows`/`CoordinatorLaunchResp`/`decodeWire` 是已交付
  于 K3 分支的冻结符号（本 plan 已核 `git show cards/B156.3.3-charter-3` 原文），
  有明确载体与并入判据（Task 0 步 3），非「待实现」占位。

## 6. 异议与待拍板

1. **launch_queue 写入臂无人认领**（§D5.1）：配对原语表把 Enqueue 归 K2 但 K2 只写
   ignition_queue；K4 满员 409 不写 launch_queue。协调者优先的清队顺序在无写入臂时
   退化为空转——建议补认领（K5 出队侧或 K4 满员入队），本期判据不受影响。
2. **`card create` vs `card add` 命名**：契约/拆解写 `card create --coordinate`，仓内
   创建命令实为 `card add`（无 create 别名）。本 plan 映射为 `card add --coordinate`，
   不新增 create 命令。若协调者要字面契约形态，加 create 别名即可，判据不变。
3. **拉起失败降级语义**（§D5/C 文件头）：建卡成功 + stderr 可行动提示 + exit 0——防
   「重跑 add 造重复卡」。若协调者要求失败即整体失败，改一行。
4. **K3/K4 两条拨号方法并存**（§D5.4）：K3 已交付 `Client.CoordinatorLaunch`（POST {}，
   供看板 manual）；K4 新增 `CoordinatorLaunchAs(cardID, source)`（供 CLI card_create）。
   若协调者偏好把 K3 方法改签名为带 source 的统一方法，须动 K3 交付物——默认本 plan
   零触碰 K3，新增并行方法。
5. **重复拉起覆盖会话引用**（§D5.3）：本 plan 不加「已绑定 409」守卫（Locate 判绑定
   是间接读，且与「宁可误唤醒」口径一致）。若协调者要守卫，是 handleCoordLaunch 前段
   的 ~5 行加一支断言。
6. 跨卡审计（L3 法定步骤）所需的对侧 plan 不在本回合可得范围内：本 plan 的 Consumes
   （SquadRows 签名、CoordinatorLaunchResp 字段、K3 拨号方法）已按 K3 分支原文逐字
   核对，**跨卡签名字段级比对结论：待拍板**（协调者扇出前审计时核对；K6 的 wire 镜像
   以 §D3 为准）。
7. 派发前自审：本 plan 无任何需要驱动 handoff 系统自身的验收步骤，全部可派发。

## 7. 交稿自检

- breakdown K4 验收逐条指到 Task：三端点 wire 往返 + source=manual 恰一次 + 非空
  SessionSpec → Task B T1/T1b；未登记指路（岔口四 B）→ T2；attach 互斥穿 HTTP →
  T4；`card add --coordinate` 恰一次 card_create → Task C 两支；不产生 task → T1；
  ✅ 无落空。
- 协调者裁决逐条落位：① SquadRows 过滤规则 → D1/T2/T3；② keystone.go 扩容 →
  Task A，公共签名不动 ✅。
- 判据形状自查：点数不抽查（Launch 次数、计数两键、spec 三值、launches 计数）；
  无行号判据；T1/Task A 单测各配变异复验；可违反正面断言（source 透传、spec 消费）；
  双写逐份点名（spec 组装链、wire DTO）✅。
- 平台不变量自查：未碰 handoff CLI 写路径、未起 executor、未派发；「没跑到结果的
  命令」均已标注（K3 并入判据、红绿取证归实现票），无一条写成结论 ✅。
- 台账：本 plan 节点已边干边落（docs/superpowers/plans/b156.3.4-plan-ledger.md，
  含裁决原文与基线读数），与本文件同批提交；本文件为 plan 节点最终交付物。