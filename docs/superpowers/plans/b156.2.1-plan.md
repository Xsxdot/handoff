# B156.2.1（C1）实现计划：d_ledger·绑定席位与活性租约（欠账 #4）

## 0. 目标与冻结边界

本卡把 Ticket 0 的绑定/租约空壳换成真实行为（contract §3.2 全部、§4「绑定与心跳」8 条、§7 欠账 #4），并补齐 §11.4 一④分派给 C1 的「在席出键」正形断言。产出物：一个实现提交，含

- `cards.driver_carrier` 加列迁移 + `driver_leases` 建表（SQLite/PG 两方言 DDL 各自路径）；
- 绑定二法 `ClaimCardAs` / `RebindDriver` 真实实现，既有 `ClaimCard` 转调 `ClaimCardAs(id,owner,"")` 且行为逐字节不变；
- 租约四法 `RenewDriverLease` / `DropDriverLease` / `DriverLeaseOf` / `AllDriverLeases` 真实实现；
- `internal/ledger/api/api.go` BindDriver 注释遗留分派销账（expect="" 分派已在账本侧落地）；
- 测试组 binding_test.go（新）＋ api 包缝级测试 api_test.go（新）；既有 move_test.go/tasks_test.go/runlock_test.go/store_pg_test.go 零改动且保持全绿。

**明确不做**（越线即返工）：collab 包零触碰（C4/C5）；ReleaseCard/TakeoverCard 零改动；`Card` 结构体与 `proto.Card` 不加 carrier 字段（澄清一「只存不解释」——proto/rooms.go:56-68 RoomSummary 无 carrier 字段，亲读，carrier 本期无任何 wire 投影）；金样本（§6）与 §5 拍板零触碰；target.json/best.json/视图文件零改动（零新跨域边，见 §2）；不 absorb 视图（finish 节点动作）；不写 CLI/Web（C7/C8）。分支纪律：只在执行时当前分支工作，不切分支、不改 git 配置；合并目标 feature/b156-workbench 是 finish 阶段的事。

## 1. 已核实的基线事实（全部亲跑或亲读；原始输出在台账 docs/superpowers/ledgers/2026-08-26-b156.2.1-plan-ledger.md）

| # | 事实 | 证据 |
| --- | --- | --- |
| F1 | 四个空壳说谎：RebindDriver 恒 nil 且零列写入；RenewDriverLease 恒 (true,nil)；DriverLeaseOf 恒 exists=false；driver_leases 表、cards.driver_carrier 列均不存在 | 台账§空壳探针（临时测试实跑输出）；binding.go:29-76 空壳体 |
| F2 | 基线 `go test ./internal/ledger/... -count=1` 绿（ledger ok；api 无测试文件） | 台账原始输出 |
| F3 | `go run . graph check --repo . --view cards-B156.2-charter-4` fails=0 warns=97；裸 check 报 4 条 dead（3 dead-contract+1 dead-interface）为 absorb 前固有形态 | 台账原始输出；与协调者本轮补充①实测一致 |
| F4 | SQLite v1.56.0 重复加列报错原文含子串 `duplicate column name:` | 最小程序实测（台账§库行为事实核验） |
| F5 | SQLite v1.56.0 支持 `INSERT … ON CONFLICT(session) DO UPDATE SET … = excluded.…`；最小程序实测两连发后仍单行 | 同上 |
| F6 | pgx v5.10.0 PgError.Error() = `Severity + ": " + Message + " (SQLSTATE " + Code + ")"`（pgconn/errors.go:53-55）→ PG 重复列错误串含 `already exists`、不含 `duplicate column`。服务器侧消息文本未真机验证 | 模块缓存源码亲读；PG 半归真机清单 |
| F7 | ensureSchema 迁移策略预留：「后续加列走 ALTER + 容忍 duplicate column」（store.go:195-197 注释原文）；仓内先例 internal/store/store.go:232-249 只容忍 SQLite 文案 | store.go:195-197、internal/store/store.go:226-278 亲读 |
| F8 | ClaimCard 语义全集：同事务 CAS、终态 ErrBadState、他主 ErrCASConflict、重入幂等、不落事件、空 owner 普通 error 拒绝 | move.go:143-175 + move_test.go:74-118 |
| F9 | takeover 先例：TakeoverCard 覆写绑定并落 EvDriverTakeover payload{from,to}（tasks.go:95-118）；运行锁过期抢占 actor=新持有者（runlock.go:83-93） | tasks.go:102-108、runlock.go:89 |
| F10 | 时间抽象：tval()/toTime() 两方言编解码（store.go:124-146）；注入时钟 s.timeNow() 生产回退 time.Now（store.go:35-47）；读面不过滤过期是 RunLockOf 明文约定（runlock.go:143-144） | 亲读 |
| F11 | 测试基座：newTestStore=Open(t.TempDir())（store_test.go:12-20）、seedStore=种子工作流（cards_test.go:12-19）；跨包造卡走公开 API PutWorkflow（workflows.go:154-155）+CreateCard（cards.go:19-21）；MergeCards(ids,into,actor) 公开（merge.go:15），Following 派生在 derived.go:96 | 亲读 |
| F12 | 符号锚全部可决议：n_ledger_Store_RebindDriver / ClaimCardAs / RenewDriverLease / ClaimCard =ok；m_collab_client_LedgerClient、m_ledger_api_Facade =ok（graph sym --view cards-B156.2-charter-4） | 台账§符号锚自检 |

## 2. 图事实与本卡的图义务

C1 零新跨域边：改动全部落在 d_ledger 域内四文件（binding/move/store/api）；client.LedgerClient 方法集不变；proto 不动。测试文件不入图（baseline.json 无任何 _test.go 节点，亲查）。计划约束实现**不引入新函数符号**（ALTER 直接内联进 ensureSchema，不抽 helper），视图 cards-B156.2-charter-4 因此零叠加、无图覆盖债。图闸照协调者补充①执行：

```bash
go run . graph check --repo . --view cards-B156.2-charter-4
```

预期 fails 空；warns 计数允许因行号漂移变动（warn 非闸）。裸 check 的 4 条 dead 不是本卡义务，不许去修。

## 3. Interfaces（跨卡逐字签名，供跨卡审计比对）

### Consumes（冻结物原文，一字不改）

```go
// contract §3.2（internal/ledger/binding.go 现有骨架，F1）
func (s *Store) ClaimCardAs(id, owner, carrier string) error
func (s *Store) RebindDriver(id, toSession, carrier, expect string) error
func (s *Store) RenewDriverLease(session string, ttl time.Duration) (bool, error)
func (s *Store) DropDriverLease(session string) error
func (s *Store) DriverLeaseOf(session string) (DriverLease, bool, error)
func (s *Store) AllDriverLeases() ([]DriverLease, error)

type DriverLease struct {
    Session   string
    ExpiresAt time.Time
}
const (
    DriverLeaseTTL           = 5 * time.Minute
    DriverLeaseRenewInterval = 2 * time.Minute
)
```

```go
// contract §3.4 出站接口（internal/collab/client/client.go:19-42；C1 只实现其账本半，不改接口）
BindDriver(id, session, carrier, expect string) error
DriverLease(session string) (time.Time, bool, error) // expiresAt + 是否存在
```

复用的仓内既有符号（不改）：`getCardTx(s,tx,id)`（cards.go:374-380，ErrNotFound 语义）、`s.appendEvent(tx,sink,cardID,typ,actor,payload)`（events.go:21-23）、`EvDriverTakeover`（types.go 词表）、`ErrNotFound/ErrCASConflict/ErrBadState`（ledger.go:17-26）、`log()`（ledger.go:33，slog 结构化 logger，禁 print）、`PutWorkflow(name string, def WorkflowDef) (int, error)` 与 `WorkflowDef{States []string; Gates map[string]Gate}`（workflows.go:154-155、types.go:252-256）、`CreateCard(NewCard{Title, Project, Priority, Parent, Workflow, BaseBranch, Actor string})`（cards.go:19-21）、`MergeCards(ids []string, into, actor string) error`（merge.go:15）。

### Produces（下游卡按此消费）

1. **对 C4（Send 执法）**：换绑成功后 `GetCard(id).DriverSession` 即新会话——旧会话写权判定读当前值自然剥权（§4 条「RebindDriver 成功后旧会话…」的账本前提）。测试符号：`TestRebindDriverSuccessGoldPayload`。
2. **对 C5（live 导出）**：`client.DriverLease(session)` 返回真实 (expiresAt, exists)，不过滤过期；活性判定由 C5 按 ExpiresAt 与其注入时钟做。测试符号：`TestFacadeBindDriverAndLeaseThroughClientSeam`。
3. **对 C6/C7（rebind 端点/命令）**：`facade.BindDriver` 完整 expect 语义——expect==当前值、或 expect=="" 且当前无绑定 → 成功；否则 wrap `ledger.ErrCASConflict`。api.go:92-96 注释销账。
4. **schema**：任意存量库经 `ledger.Open(dsn)` 幂等升级出 `cards.driver_carrier TEXT` 列与 `driver_leases(session TEXT PRIMARY KEY, expires_at)` 表。
5. **测试符号清单**（全名，供 review 点名）：package ledger 于 internal/ledger/binding_test.go——`TestOpenMigratesLegacyCardsForCarrierAndLeaseTable`、`TestRenewDriverLeaseUpsertsOwnRowOnly`、`TestDriverLeaseReadsReturnExpiredRowsAndMissing`、`TestAllDriverLeasesReturnsAllRowsSorted`、`TestClaimCardAsWritesCarrierColumns`、`TestLegacyClaimCardDelegatesWithEmptyCarrier`、`TestRebindDriverConflictKeepsEverything`、`TestRebindDriverSuccessGoldPayload`、`TestRebindDriverEmptyExpectRequiresUnbound`、`TestCarrierOpaqueRoundTrip`；package api 于 internal/ledger/api/api_test.go——`TestFacadeBindDriverAndLeaseThroughClientSeam`、`TestFollowingProjectionKeyPresence`。

## 4. 接缝清单与双向覆盖（对照 spec §8 测试接缝清单五条）

本卡触及的声明缝只有一条：**缝②出站 client 接口（实现侧）**。其余四缝不在本卡文件集内：①入站门面归 C4、③card_events 消费缝归 C6、④环节执行体缝归 C3、⑤控制台契约层缝归 C6/C8。

**缝 → 测试**：缝②由两支锁住——入口调用是接口类型变量 `var lc client.LedgerClient = api.New(st)` 之后的 `lc.BindDriver/lc.DriverLease/lc.GetCard/lc.ListAllCards`，穿过真实接口（api.go:27 `var _ client.LedgerClient = (*Facade)(nil)` 为编译期背书）：`TestFacadeBindDriverAndLeaseThroughClientSeam`、`TestFollowingProjectionKeyPresence`。

**测试 → 缝**（逐支声明入口）：

| 测试 | 入口符号 | 缝归属 |
| --- | --- | --- |
| TestFacadeBindDriverAndLeaseThroughClientSeam | client.LedgerClient 接口变量 | 缝②（缝级） |
| TestFollowingProjectionKeyPresence | client.LedgerClient.ListAllCards/GetCard ＋手写投影 cardWire 的 JSON 出键断言（§11.4 一④ C1 半，序列化边界） | 缝②（缝级） |
| 其余十支 | Store 公开方法（契约 §3.2 冻结签名的所在层）与 Store.Open | 内部锁，理由见下 |

**内部锁合法性声明**（纪律块要求的形状：「从声明缝构造不出这条断言」＋为什么）：

- ClaimCardAs / RenewDriverLease / DropDriverLease / AllDriverLeases 四法：**不在** client.LedgerClient 方法集里（client.go:19-42 冻结面只向 collab 暴露 BindDriver+DriverLease），入站门面 collab.Service 属 C4/C5 文件集不可触碰——契约 §3.2 把签名冻在 Store 层，唯一能构造断言的入口就是 Store 公开方法本身。
- DriverLeaseOf 的完整结构体（Session 字段）与 AllDriverLeases 的排序契约：client 缝只透出 `(time.Time,bool,error)`，Session 维度从缝上不可见。
- 迁移测试：缝上没有 schema 视角；Open 本身是公开缝，测试从它进。
- carrier 列断言（SQL 直查）：carrier 全链无 struct/wire 投影（澄清一），任何更高缝上都构造不出「列值原样存取」断言。

无未声明的内部锁；无以「更短/更纯/好写」为由的降格；步骤正文无会改变测试入口符号的条件退路。

## 5. Task 1：schema 迁移——driver_carrier 加列 + driver_leases 建表

### 有界文件集

改：`internal/ledger/store.go`、新建 `internal/ledger/binding_test.go`。只读参照：internal/store/store.go:226-278（先例）、codegraph/diffs/cards-B156.2-charter-4.json（确认零叠加）。

### Interfaces

Consumes：ensureSchema 两方言 ddl 切片（store.go:198-336）、q()/tval()（store.go:105-131）、log()。
Produces：Produces#4（Open 幂等迁移）；binding_test.go 的迁移测试。

### 步骤

测试范围：只跑 ./internal/ledger 定向测试；不跑全仓。

1. 【判据基线复核】动手前跑并记录原始输出（必须复现 F2/F3：绿＋fails=0 warns=97；这是判据先行复核，不是实现后验收）：

   ```bash
   go test ./internal/ledger -count=1
   go run . graph check --repo . --view cards-B156.2-charter-4
   ```

2. 【红】新建 internal/ledger/binding_test.go，先写入文件头注释＋imports＋迁移测试。文件头：

   ```go
   // 绑定席位与活性租约的行为锁（B156.2 契约 §3.2 / §4「绑定与心跳」八条）。
   // 本文件只测 Store 公开门面；缝级断言（client.LedgerClient 实现侧）在
   // internal/ledger/api/api_test.go。carrier 断言直查 SQL 列：该字段本期无
   // 任何 struct/wire 投影（澄清一：只存不解释），更高缝上构造不出该断言。
   package ledger
   ```

   本步骤 import 只需：database/sql、path/filepath、testing、time（Task 2/3 追加测试时再补 encoding/json/errors/fmt/strings，禁止提前留 unused import）。

   测试完整代码：

   ```go
   // 锁 Open 的迁移义务：旧库（无 driver_carrier 列）升级后可读可写、
   // 存量绑定保活、二次 Open 幂等。
   func TestOpenMigratesLegacyCardsForCarrierAndLeaseTable(t *testing.T) {
       dir := t.TempDir()
       dbPath := filepath.Join(dir, "legacy.db")
       legacy, err := sql.Open("sqlite", dbPath)
       if err != nil {
           t.Fatal(err)
       }
       // 旧形态 cards 表：照抄 store.go SQLite 分支但去掉 driver_carrier，
       // 再塞一行带绑定的存量卡——模拟升级前的真实部署。
       if _, err := legacy.Exec(`CREATE TABLE cards (
           id TEXT PRIMARY KEY, title TEXT NOT NULL, status TEXT NOT NULL,
           terminate_reason TEXT, priority TEXT NOT NULL DEFAULT '中',
           project TEXT NOT NULL, parent_id TEXT REFERENCES cards(id),
           workflow_name TEXT NOT NULL, workflow_version INTEGER NOT NULL,
           attachments TEXT NOT NULL DEFAULT '[]', acceptance_criteria TEXT,
           base_branch TEXT, driver_session TEXT, driver_heartbeat_at TEXT,
           created_at TEXT NOT NULL, updated_at TEXT NOT NULL)`); err != nil {
           t.Fatal(err)
       }
       now := time.Now().UTC().Format(time.RFC3339Nano)
       if _, err := legacy.Exec(`INSERT INTO cards (id, title, status, priority, project,
           workflow_name, workflow_version, attachments, driver_session, created_at, updated_at)
           VALUES ('B9001','存量卡','进行中','中','p','bug',1,'[]','cli:old@h',?,?)`, now, now); err != nil {
           t.Fatal(err)
       }
       legacy.Close()

       s, err := Open(dbPath)
       if err != nil {
           t.Fatalf("旧库升级 Open 失败: %v", err)
       }
       defer s.Close()
       var carrier sql.NullString
       if err := s.db.QueryRow(`SELECT driver_carrier FROM cards WHERE id = 'B9001'`).Scan(&carrier); err != nil {
           t.Fatalf("升列后应可读 driver_carrier（存量行取零值）: %v", err)
       }
       got, err := s.GetCard("B9001")
       if err != nil || got.DriverSession != "cli:old@h" {
           t.Fatalf("存量绑定不得丢失: %v %+v", err, got)
       }
       if _, err := s.db.Exec(`INSERT INTO driver_leases (session, expires_at) VALUES (?, ?)`,
           "sess-mig", s.tval(time.Now())); err != nil {
           t.Fatalf("driver_leases 应已建表: %v", err)
       }
       s.Close()
       // 幂等：第二次 Open 不得因重复加列报错（双文案容忍的回归点）。
       s2, err := Open(dbPath)
       if err != nil {
           t.Fatalf("二次 Open 应幂等: %v", err)
       }
       s2.Close()
   }
   ```

   跑红并记录原始输出：

   ```bash
   go test ./internal/ledger -run TestOpenMigratesLegacyCardsForCarrierAndLeaseTable -count=1
   ```

   预期红点：`升列后应可读 driver_carrier…: SQL logic error: no such column: driver_carrier`（F1 已证列不存在；Open 本身不失败）。若红在别处说明夹具写错——修夹具，不许动生产码来迁就夹具。

3. 【绿】store.go 两处改动。其一，PG 分支 ddl 切片内 `card_prefixes` INSERT 之前插入：

   ```go
   			`CREATE TABLE IF NOT EXISTS driver_leases (
   				session TEXT PRIMARY KEY,
   				expires_at TIMESTAMPTZ NOT NULL)`,
   ```

   SQLite 分支同位置插入：

   ```go
   			`CREATE TABLE IF NOT EXISTS driver_leases (
   				session TEXT PRIMARY KEY,
   				expires_at TEXT NOT NULL)`,
   ```

   其二，ensureSchema 的 DDL for 循环之后、`return nil` 之前插入（完整代码块）：

   ```go
   	// B156.2 绑定席位加列迁移：CREATE IF NOT EXISTS 不会给已存在的表补列，
   	// 加列走「ALTER + 容忍重复列」（store.go 文件头预留的第二版形态；
   	// internal/store 先例 store.go:232-249）。ADD COLUMN 两方言语法同形，
   	// 但重复列报错文案不同：SQLite 是 duplicate column name（modernc.org/sqlite
   	// v1.56.0 最小程序实测），PG 经 pgx 是 column … already exists (SQLSTATE
   	// 42701)（pgconn/errors.go:53-55 的 Error() 拼装格式）——两种都要容忍，
   	// 否则旧库第二次 Open 直接失败。本处只有这一条 ADD COLUMN，没有可被
   	// 「already exists」误吞的其他错误源。
   	if _, err := s.db.Exec(`ALTER TABLE cards ADD COLUMN driver_carrier TEXT`); err != nil {
   		msg := err.Error()
   		if !strings.Contains(msg, "duplicate column") && !strings.Contains(msg, "already exists") {
   			return fmt.Errorf("迁移 cards.driver_carrier: %w", err)
   		}
   		log().Info("cards.driver_carrier 列已存在，跳过加列")
   	} else {
   		log().Info("账本加列迁移完成", "table", "cards", "column", "driver_carrier")
   	}
   ```

   （store.go 已 import strings/fmt/log 可用；不加新 import。）然后：

   ```bash
   gofmt -w internal/ledger/store.go internal/ledger/binding_test.go
   go test ./internal/ledger -run TestOpenMigratesLegacyCardsForCarrierAndLeaseTable -count=1
   go test ./internal/ledger -count=1
   ```

   全部必须退出 0（迁移测试绿＋包回归绿——F2 的既有用例一个都不许掉）。

4. 【可观测性与注释】迁移成功/跳过各一条 slog Info（上面代码块已含）；ensureSchema 文件头注释不动（其预留句正是本次落地的依据）。禁 print/log.Printf。

5. 【变异复验】临时把容忍条件删掉一个分支（例如去掉 `already exists` 判定），重跑二次 Open 段——SQLite 下仍绿属预期（SQLite 文案是 duplicate column）；把整个容忍条件删成裸 return err，重跑迁移测试必须红（二次 Open 报重复列）。每次变异后还原并用 `git diff -- internal/ledger/store.go internal/ledger/binding_test.go` 确认只剩正确实现与测试。变异不留工作树。

### Task 1 验收与缺陷族结论

- 验收：步骤 3 三条命令全绿＋步骤 1 基线复核记录在台账。
- 生命周期/中断：迁移只在 Open 时执行、幂等；失败即 Open 失败（fail-fast 不静默降级），错误带表列名上下文。
- 静默失败：双文案容忍有注释与来源；误吞风险已被「仅此一条 ADD COLUMN」限定并言明。
- 跨平台假设：时间列沿用 tval/toTime；SQLite 机内实测，PG DDL 真机清单（breakdown 清单 #5 扩展项，见 §9）。
- 假红/假绿：红点钉在 no such column 上（非 Open 失败）；幂等断言防「只在新库测过」；变异复验两条。
- 门禁绕过：无新增边/路由/权限面；图闸 fails 空（§2）。
- 序列化边界：driver_leases 时间列经 tval 写/toTime 读，Task 2 读回断言构成 encode∘decode 过真实库的恒等链。

## 6. Task 2：租约四法填肉（RenewDriverLease / DropDriverLease / DriverLeaseOf / AllDriverLeases）

### 有界文件集

改：`internal/ledger/binding.go`、`internal/ledger/binding_test.go`。零触碰：runlock.go（两锁互不代写边界）、card_run_locks 表。

### Interfaces

Consumes：driver_leases 表（Task 1 产出）、s.mutate、s.tval/toTime、s.timeNow、log()、sql.ErrNoRows 判形（runlock.go:151 同法）。
Produces：Produces#2 的账本半；§4 条 6/7/8 全部行为。

### 步骤

测试范围：只跑 ./internal/ledger 定向＋包回归。

1. 【判据基线复核】F1 探针已证 RenewDriverLease 恒 (true,nil)、DriverLeaseOf 恒 false、表不存在——红态依据在台账，无需重复跑。
2. 【红】binding_test.go 追加三支测试（完整代码；import 增量：errors）：

   ```go
   // leaseExpires 直查租约行，供断言「库里的真实值」而非经被测方法的读回。
   func leaseExpires(t *testing.T, s *Store, session string) (time.Time, bool) {
       t.Helper()
       var expiresAt any
       err := s.db.QueryRow(s.q(`SELECT expires_at FROM driver_leases WHERE session = ?`), session).Scan(&expiresAt)
       if errors.Is(err, sql.ErrNoRows) {
           return time.Time{}, false
       }
       if err != nil {
           t.Fatalf("读租约行: %v", err)
       }
       return toTime(expiresAt), true
   }

   // §4 条7：upsert 自己那行；他人行不动；常量冻结值；时间走注入时钟；
   // 空 session 拒绝（照 ClaimCard 空 owner 先例 move.go:146-149）。
   func TestRenewDriverLeaseUpsertsOwnRowOnly(t *testing.T) {
       s := seedStore(t)
       if DriverLeaseTTL != 5*time.Minute || DriverLeaseRenewInterval != 2*time.Minute {
           t.Fatalf("租期常量被改: ttl=%v interval=%v", DriverLeaseTTL, DriverLeaseRenewInterval)
       }
       base := time.Date(2026, 8, 26, 10, 0, 0, 0, time.UTC)
       cur := base
       s.now = func() time.Time { return cur }

       if ok, err := s.RenewDriverLease("sess-a", time.Minute); err != nil || !ok {
           t.Fatalf("首建应生效: %v %v", ok, err)
       }
       expA, ok := leaseExpires(t, s, "sess-a")
       if !ok || !expA.Equal(base.Add(time.Minute)) {
           t.Fatalf("首建行 expires_at=%v want %v", expA, base.Add(time.Minute))
       }
       cur = base.Add(30 * time.Second)
       if ok, err := s.RenewDriverLease("sess-b", 3*time.Minute); err != nil || !ok {
           t.Fatalf("第二会话首建: %v %v", ok, err)
       }
       if expA2, _ := leaseExpires(t, s, "sess-a"); !expA2.Equal(expA) {
           t.Fatalf("续 sess-b 不得动 sess-a 行: %v → %v", expA, expA2)
       }
       cur = base.Add(time.Minute)
       if ok, err := s.RenewDriverLease("sess-a", 2*time.Minute); err != nil || !ok {
           t.Fatalf("续期: %v %v", ok, err)
       }
       if expA3, _ := leaseExpires(t, s, "sess-a"); !expA3.Equal(cur.Add(2 * time.Minute)) {
           t.Fatalf("续期应按注入时钟重算: got %v want %v", expA3, cur.Add(2*time.Minute))
       }
       if ok, err := s.RenewDriverLease("", time.Minute); err == nil || ok {
           t.Fatalf("空 session 应拒绝: ok=%v err=%v", ok, err)
       }
       if _, ok := leaseExpires(t, s, ""); ok {
           t.Fatal("空 session 不得落行")
       }
   }

   // §4 条6+8：不存在→exists=false；读面不过滤过期（RunLockOf 同一约定）；
   // Drop 幂等删。
   func TestDriverLeaseReadsReturnExpiredRowsAndMissing(t *testing.T) {
       s := seedStore(t)
       if _, ok, err := s.DriverLeaseOf("ghost"); err != nil || ok {
           t.Fatalf("不存在 session 应 false,nil: %v %v", ok, err)
       }
       all, err := s.AllDriverLeases()
       if err != nil || len(all) != 0 {
           t.Fatalf("空表应返回空切片: %v %v", all, err)
       }
       base := time.Date(2026, 8, 26, 10, 0, 0, 0, time.UTC)
       s.now = func() time.Time { return base }
       if ok, _ := s.RenewDriverLease("stale", -time.Minute); !ok {
           t.Fatal("负 TTL 造过期行应生效")
       }
       lease, ok, err := s.DriverLeaseOf("stale")
       if err != nil || !ok || !lease.ExpiresAt.Before(base) || lease.Session != "stale" {
           t.Fatalf("读面不过滤过期行且带 Session: %+v ok=%v err=%v", lease, ok, err)
       }
       if err := s.DropDriverLease("stale"); err != nil {
           t.Fatalf("删除: %v", err)
       }
       if err := s.DropDriverLease("stale"); err != nil { // 再删仍 nil＝幂等
           t.Fatalf("删除应幂等: %v", err)
       }
       if _, ok, _ := s.DriverLeaseOf("stale"); ok {
           t.Fatal("删除后行应消失")
       }
   }

   // 看板批量判活的输入契约：全量、按 session 排序、不过滤过期。
   func TestAllDriverLeasesReturnsAllRowsSorted(t *testing.T) {
       s := seedStore(t)
       base := time.Date(2026, 8, 26, 10, 0, 0, 0, time.UTC)
       s.now = func() time.Time { return base }
       for _, tc := range []struct {
           sess string
           off  time.Duration
       }{
           {"sess-c", time.Minute}, {"sess-a", -time.Minute}, {"sess-b", 3 * time.Minute},
       } {
           if ok, err := s.RenewDriverLease(tc.sess, tc.off); err != nil || !ok {
               t.Fatalf("造行 %s: %v %v", tc.sess, ok, err)
           }
       }
       all, err := s.AllDriverLeases()
       if err != nil || len(all) != 3 {
           t.Fatalf("应 3 行含过期行: %+v %v", all, err)
       }
       if !(all[0].Session < all[1].Session && all[1].Session < all[2].Session) {
           t.Fatalf("应按 session 升序: %+v", all)
       }
       for _, l := range all {
           if l.ExpiresAt.IsZero() {
               t.Fatalf("ExpiresAt 未解码: %+v", l)
           }
       }
   }
   ```

   跑红：

   ```bash
   go test ./internal/ledger -run 'TestRenewDriverLease|TestDriverLeaseReads|TestAllDriverLeases' -count=1 -v 2>&1 | tail -30
   ```

   预期红点逐条：TestRenewDriverLeaseUpsertsOwnRowOnly 死于 `no such table: driver_leases`（helper 首查）；TestDriverLeaseReadsReturnExpiredRowsAndMissing 第一断言绿（stub 恰好返回 false,nil）但造行后续查同表死；TestAllDriverLeasesReturnsAllRowsSorted 死于 `len(all)=0 != 3`。三条红因与 F1 一致才许进下一步。

3. 【绿】binding.go 替换四个空壳为真实实现（完整代码块；import 增量 database/sql/errors/fmt）：文件头注释的「Ticket 0 空壳」段同步删除（其历史职责已了结，头注释保留权威位置与互不代写声明）。逐方法：

   ```go
   // RenewDriverLease 续租/首建该 session 的租约行（upsert，session 全局一行：
   // 兼任 N 卡的协调者一条租约覆盖全部席位，「它活着吗」问的就是会话本身）。
   // 返回是否生效：true=行已写入（首建或续期）。负 ttl 合法——产已过期行，
   // 供消费侧与测试在注入时钟下演练活性翻转；活性判定不在本方法内做
   // （RunLockOf 同一约定：读面不过滤过期）。
   func (s *Store) RenewDriverLease(session string, ttl time.Duration) (bool, error) {
       if session == "" {
           log().Warn("租约续期被拒：session 为空")
           return false, fmt.Errorf("租约续期被拒：session 为空")
       }
       renewed := false
       var expiresAt time.Time
       err := s.mutate(func(tx *sql.Tx, _ *eventSink) error {
           expiresAt = s.timeNow().Add(ttl)
           // 两方言同形的 upsert：excluded.expires_at 引用插入候选行
           // （SQLite≥3.24 / PG≥9.5；modernc.org/sqlite v1.56.0 最小程序实测通过）。
           if _, err := tx.Exec(s.q(`INSERT INTO driver_leases (session, expires_at) VALUES (?, ?)
               ON CONFLICT(session) DO UPDATE SET expires_at = excluded.expires_at`),
               session, s.tval(expiresAt)); err != nil {
               return fmt.Errorf("续驱动租约: %w", err)
           }
           renewed = true
           return nil
       })
       if err != nil {
           log().Warn("驱动租约续期失败", "session", session, "cause", err)
           return false, err
       }
       log().Info("驱动租约已续", "session", session,
           "expires_at", expiresAt.Format(time.RFC3339))
       return renewed, nil
   }

   // DropDriverLease 删除该 session 的租约行（幂等：行不存在仍是成功 no-op）。
   func (s *Store) DropDriverLease(session string) error {
       dropped := false
       err := s.mutate(func(tx *sql.Tx, _ *eventSink) error {
           res, err := tx.Exec(s.q(`DELETE FROM driver_leases WHERE session = ?`), session)
           if err != nil {
               return fmt.Errorf("删驱动租约: %w", err)
           }
           n, err := res.RowsAffected()
           if err != nil {
               return fmt.Errorf("读删除影响行数: %w", err)
           }
           dropped = n > 0
           return nil
       })
       if err != nil {
           log().Warn("驱动租约删除失败", "session", session, "cause", err)
           return err
       }
       if dropped {
           log().Info("驱动租约已删除", "session", session)
       }
       return nil
   }

   // DriverLeaseOf 读单 session 租约；第二个返回值=行是否存在。
   // **不过滤过期**：「是否活着」由消费侧按 ExpiresAt 与同一注入时钟判定
   // （RunLockOf 同一约定，runlock.go:143-144）。
   func (s *Store) DriverLeaseOf(session string) (DriverLease, bool, error) {
       var lease DriverLease
       var expiresAt any
       err := s.db.QueryRow(s.q(`SELECT session, expires_at FROM driver_leases WHERE session = ?`),
           session).Scan(&lease.Session, &expiresAt)
       if errors.Is(err, sql.ErrNoRows) {
           return DriverLease{}, false, nil
       }
       if err != nil {
           return DriverLease{}, false, fmt.Errorf("读驱动租约 %s: %w", session, err)
       }
       lease.ExpiresAt = toTime(expiresAt)
       return lease, true, nil
   }

   // AllDriverLeases 全部租约行（看板批量判活用）。同样不过滤过期，
   // 按 session 排序保证读形确定。
   func (s *Store) AllDriverLeases() ([]DriverLease, error) {
       rows, err := s.db.Query(`SELECT session, expires_at FROM driver_leases ORDER BY session`)
       if err != nil {
           return nil, fmt.Errorf("读全部驱动租约: %w", err)
       }
       defer rows.Close()
       var out []DriverLease
       for rows.Next() {
           var lease DriverLease
           var expiresAt any
           if err := rows.Scan(&lease.Session, &expiresAt); err != nil {
               return nil, err
           }
           lease.ExpiresAt = toTime(expiresAt)
           out = append(out, lease)
       }
       return out, rows.Err()
   }
   ```

   然后：

   ```bash
   gofmt -w internal/ledger/binding.go internal/ledger/binding_test.go
   go test ./internal/ledger -run 'TestRenewDriverLease|TestDriverLeaseReads|TestAllDriverLeases' -count=1
   go test ./internal/ledger -count=1
   ```

   全部退出 0。

4. 【可观测性与注释】每方法入口/错误分支 slog 已内建（成功路径 Info 不静默、错误分支 Warn 带 cause）；导出注释已写参数语义与注意事项（负 ttl、不过滤过期、幂等）。禁 print。

5. 【变异复验】①把 upsert 的 `ON CONFLICT …` 子句临时删掉，重跑 TestRenewDriverLeaseUpsertsOwnRowOnly：二次续期必须撞 PK 变红，还原；②把 AllDriverLeases 的 `ORDER BY session` 临时删掉并重跑排序断言（可能偶绿——若如此改为乱序插桩验证后还原），结论记台账；③DropDriverLease 临时改成只 UPDATE expires_at（假删），幂等断言组红，还原。变异不留工作树。

### Task 2 验收与缺陷族结论

- 验收：步骤 3 两条命令全绿＋步骤 2 红点记录在台账。
- 生命周期/中断：租约行按 session 一行、upsert 覆写不累积（breakdown 缺陷族 1 的孤儿资源问题就此消解）；进程重启后行留库 TTL 自愈，绑定列不受影响。
- 静默失败：空 session 显式拒绝；失败 Warn 带 cause；恒真 stub 的欺骗面被 F1 探针+新测试双重关闭。
- 跨平台假设：时间全走 tval/toTime 与注入时钟，无 sleep；PG 方言 upsert 语法归真机清单（§9）。
- 假红/假绿：注入时钟拨到固定基准断言 ExpiresAt 精确相等；直查表断言防「用被测方法读回自证」；变异三条。
- 门禁绕过：无；写路径全走 mutate 串行域。
- 序列化边界：expires_at 经 tval 写/toTime 读，测试断言精确时刻相等即 encode∘decode 恒等过真实库。

## 7. Task 3：绑定二法填肉（ClaimCardAs / RebindDriver ＋ ClaimCard 转调）

### 有界文件集

改：`internal/ledger/binding.go`、`internal/ledger/move.go`（仅 ClaimCard 一函数）、`internal/ledger/binding_test.go`。零触碰：move.go 其余函数、tasks.go、runlock.go。

### Interfaces

Consumes：getCardTx、s.appendEvent、EvDriverTakeover、ErrNotFound/ErrCASConflict/ErrBadState、driver_carrier 列（Task 1）。
Produces：Produces#1/#3 的账本半；§4 条 1/2/3/5 全部行为＋expect="" 销账语义。

### 行为裁决（本计划的判断，理由已记台账§判断记录）

1. RebindDriver **不加**终态拒绝——冻结语义无此条，加了即越契约造行为。
2. RebindDriver 空 toSession 拒绝（普通 error 非哨兵）——照 ClaimCard 空 owner 先例 move.go:146-149；HTTP/CLI 面（C6/C7）只传非空。
3. RebindDriver 成功同时写 driver_heartbeat_at——TakeoverCard 先例 tasks.go:102 ＋ types.go:121 列注释「认领时刻」语义（换绑=新认领时刻）。
4. takeover 事件 actor=新绑定会话，payload 恰 {from,to} 两键——runlock 抢占先例 runlock.go:89；「缺键多键即红」（breakdown C1③）。
5. ClaimCardAs 体移植旧 ClaimCard 时唯一额外改动 time.Now()→s.timeNow()——生产等价（store.go:41-47），测试可拨钟。

### 步骤

测试范围：./internal/ledger 定向＋包回归（含既有 move_test/tasks_test 不许掉）。

1. 【判据基线复核】F1/F8 已证：RebindDriver 恒 nil 零写入、ClaimCard 现行语义全集有既有测试锁底。无需重跑。
2. 【红】binding_test.go 追加四支＋helper（完整代码；import 增量：encoding/json/fmt/strings）：

   ```go
   // carrierOf 直查列值。carrier 本期无 struct/wire 投影（澄清一），SQL 层是
   // 唯一能构造「原样存取」断言的入口（内部锁声明见计划 §4）。
   func carrierOf(t *testing.T, s *Store, id string) string {
       t.Helper()
       var carrier sql.NullString
       if err := s.db.QueryRow(s.q(`SELECT driver_carrier FROM cards WHERE id = ?`), id).Scan(&carrier); err != nil {
           t.Fatalf("读 carrier 列: %v", err)
       }
       return carrier.String
   }

   // countTakeoverEvents 取该卡的 EvDriverTakeover 事件切片。
   func countTakeoverEvents(t *testing.T, s *Store, id string) []Event {
       t.Helper()
       events, err := s.EventsFromAsc([]string{id}, 0, 1000)
       if err != nil {
           t.Fatal(err)
       }
       var out []Event
       for _, e := range events {
           if e.Type == EvDriverTakeover {
               out = append(out, e)
           }
       }
       return out
   }

   // §4 条1：ClaimCardAs 与 ClaimCard 同语义全集＋另写 driver_carrier 列。
   func TestClaimCardAsWritesCarrierColumns(t *testing.T) {
       s := seedStore(t)
       c, _ := s.CreateCard(NewCard{Title: "载体认领", Project: "p", Workflow: "bug", Actor: "t"})
       before, _ := s.GetCard(c.ID)

       if err := s.ClaimCardAs("B99999", "cli:a@h", "console:x"); !errors.Is(err, ErrNotFound) {
           t.Fatalf("不存在卡应 ErrNotFound: %v", err)
       }
       if err := s.ClaimCardAs(c.ID, "", "console:x"); err == nil {
           t.Fatal("空 owner 应被拒")
       }
       if err := s.ClaimCardAs(c.ID, "cli:a@h", "console:macbook"); err != nil {
           t.Fatalf("首次认领: %v", err)
       }
       got, _ := s.GetCard(c.ID)
       if got.Status != before.Status || got.DriverSession != "cli:a@h" || got.DriverHeartbeatAt.IsZero() {
           t.Fatalf("语义应与 ClaimCard 一致（不改状态、写认领时刻）: %+v", got)
       }
       if gotCarrier := carrierOf(t, s, c.ID); gotCarrier != "console:macbook" {
           t.Fatalf("carrier 应落列: %q", gotCarrier)
       }
       if evs := countTakeoverEvents(t, s, c.ID); len(evs) != 0 {
           t.Fatalf("认领不落事件: %+v", evs)
       }
       if err := s.ClaimCardAs(c.ID, "cli:b@h", "other"); !errors.Is(err, ErrCASConflict) {
           t.Fatalf("他主持有应 CAS 冲突: %v", err)
       }
       if gotCarrier := carrierOf(t, s, c.ID); gotCarrier != "console:macbook" {
           t.Fatalf("冲突路径不得动 carrier: %q", gotCarrier)
       }
       if err := s.ClaimCardAs(c.ID, "cli:a@h", "console:macbook"); err != nil {
           t.Fatalf("同 owner 重入应幂等: %v", err)
       }
       _ = s.MoveCard(c.ID, StatusDone, "", "t")
       if err := s.ClaimCardAs(c.ID, "cli:c@h", "x"); !errors.Is(err, ErrBadState) {
           t.Fatalf("终态卡认领应 ErrBadState: %v", err)
       }
   }

   // §4 条2：既有 ClaimCard 行为逐字节不变＋内部转调传空载体。
   // 变异靶：把 ClaimCard 恢复成旧体内联 UPDATE（不写 carrier）→ 归零断言红。
   func TestLegacyClaimCardDelegatesWithEmptyCarrier(t *testing.T) {
       s := seedStore(t)
       c, _ := s.CreateCard(NewCard{Title: "转调", Project: "p", Workflow: "bug", Actor: "t"})
       if err := s.ClaimCardAs(c.ID, "cli:a@h", "desktop:y"); err != nil {
           t.Fatalf("预置载体认领: %v", err)
       }
       if gotCarrier := carrierOf(t, s, c.ID); gotCarrier != "desktop:y" { // 前置非空，防断言空转
           t.Fatalf("前置载体应已落列: %q", gotCarrier)
       }
       // 老 API 重入（同 owner 幂等）：走转调路径，空载体覆盖历史值＝未登记载体。
       if err := s.ClaimCard(c.ID, "cli:a@h"); err != nil {
           t.Fatalf("既有 ClaimCard 重入: %v", err)
       }
       if gotCarrier := carrierOf(t, s, c.ID); gotCarrier != "" {
           t.Fatalf("ClaimCard 转调应传空载体: %q", gotCarrier)
       }
       if got, _ := s.GetCard(c.ID); got.DriverSession != "cli:a@h" || got.DriverHeartbeatAt.IsZero() {
           t.Fatalf("既有语义不得变: %+v", got)
       }
   }

   // §4 条3反面：expect≠当前 → ErrCASConflict 且零列变更、零事件追加；
   // 附不存在卡 ErrNotFound 与空目标会话拒绝。
   func TestRebindDriverConflictKeepsEverything(t *testing.T) {
       s := seedStore(t)
       c, _ := s.CreateCard(NewCard{Title: "换绑冲突", Project: "p", Workflow: "bug", Actor: "t"})
       if err := s.RebindDriver(c.ID, "sess-b", "car-b", ""); err != nil {
           t.Fatalf("无绑定卡 expect=\"\" 应成功: %v", err)
       }
       before, _ := s.GetCard(c.ID)
       beforeCarrier := carrierOf(t, s, c.ID)
       if err := s.RebindDriver(c.ID, "sess-c", "car-c", "wrong"); !errors.Is(err, ErrCASConflict) {
           t.Fatalf("expect 不符应 CAS 冲突: %v", err)
       }
       after, _ := s.GetCard(c.ID)
       if after.DriverSession != before.DriverSession || !after.DriverHeartbeatAt.Equal(before.DriverHeartbeatAt) {
           t.Fatalf("冲突路径不得改任何列: %+v → %+v", before, after)
       }
       if carrierOf(t, s, c.ID) != beforeCarrier {
           t.Fatalf("冲突路径不得改任何列: carrier %q → %q", beforeCarrier, carrierOf(t, s, c.ID))
       }
       if evs := countTakeoverEvents(t, s, c.ID); len(evs) != 1 {
           t.Fatalf("冲突不得追加事件: %d", len(evs))
       }
       if err := s.RebindDriver("B99999", "x", "y", ""); !errors.Is(err, ErrNotFound) {
           t.Fatalf("不存在卡应 ErrNotFound: %v", err)
       }
       if err := s.RebindDriver(c.ID, "", "car", "sess-b"); err == nil {
           t.Fatal("目标会话为空应被拒")
       }
       if after2, _ := s.GetCard(c.ID); after2.DriverSession != "sess-b" {
           t.Fatalf("空目标被拒后绑定不变: %q", after2.DriverSession)
       }
   }

   // §4 条5 金样本：成功覆写两列并落恰一条 EvDriverTakeover，
   // payload 恰 from/to 两键（缺键多键都红），actor=新会话。
   func TestRebindDriverSuccessGoldPayload(t *testing.T) {
       s := seedStore(t)
       c, _ := s.CreateCard(NewCard{Title: "换绑金样本", Project: "p", Workflow: "bug", Actor: "t"})
       if err := s.ClaimCardAs(c.ID, "sess-old", "car-old"); err != nil {
           t.Fatal(err)
       }
       if err := s.RebindDriver(c.ID, "sess-new", "car-new", "sess-old"); err != nil {
           t.Fatalf("正确前值应成功: %v", err)
       }
       got, _ := s.GetCard(c.ID)
       if got.DriverSession != "sess-new" {
           t.Fatalf("新绑定未覆写: %q", got.DriverSession)
       }
       if gotCarrier := carrierOf(t, s, c.ID); gotCarrier != "car-new" {
           t.Fatalf("新载体未覆写: %q", gotCarrier)
       }
       evs := countTakeoverEvents(t, s, c.ID)
       if len(evs) != 1 {
           t.Fatalf("应恰一条 takeover: %d", len(evs))
       }
       if evs[0].Actor != "sess-new" {
           t.Fatalf("actor 应为新会话: %q", evs[0].Actor)
       }
       var payload map[string]string
       if err := json.Unmarshal(evs[0].Payload, &payload); err != nil {
           t.Fatal(err)
       }
       if len(payload) != 2 || payload["from"] != "sess-old" || payload["to"] != "sess-new" {
           t.Fatalf("payload 应恰有 from/to 两键: %v", payload)
       }
   }

   // expect="" 分派销账（api.go Facade.BindDriver 注释遗留）：要求当前无绑定。
   func TestRebindDriverEmptyExpectRequiresUnbound(t *testing.T) {
       s := seedStore(t)
       c, _ := s.CreateCard(NewCard{Title: "空期望", Project: "p", Workflow: "bug", Actor: "t"})
       if err := s.RebindDriver(c.ID, "first", "car1", ""); err != nil {
           t.Fatalf("无绑定时空 expect 应成功: %v", err)
       }
       if err := s.RebindDriver(c.ID, "second", "car2", ""); !errors.Is(err, ErrCASConflict) {
           t.Fatalf("有绑定时空 expect 应 CAS 冲突: %v", err)
       }
   }

   // 澄清一反面断言：carrier 只存不解释——空格/斜杠/Unicode/超长串原样存取。
   func TestCarrierOpaqueRoundTrip(t *testing.T) {
       s := seedStore(t)
       carriers := []string{
           "",
           "with space",
           "slash/ed/path",
           "控制台·桌面🚀",
           strings.Repeat("x", 4096),
           "key:value; semi=eq&more",
       }
       for i, carrier := range carriers {
           c, _ := s.CreateCard(NewCard{Title: fmt.Sprintf("不透明%d", i), Project: "p", Workflow: "bug", Actor: "t"})
           sess := fmt.Sprintf("sess-%d", i)
           if err := s.ClaimCardAs(c.ID, sess, carrier); err != nil {
               t.Fatalf("carrier %q 认领: %v", carrier, err)
           }
           if got := carrierOf(t, s, c.ID); got != carrier {
               t.Fatalf("carrier 存取不改写: got %q(len %d) want %q(len %d)", got, len(got), carrier, len(carrier))
           }
           if err := s.RebindDriver(c.ID, sess+"-next", carrier+"|rebind", sess); err != nil {
               t.Fatalf("carrier %q 换绑: %v", carrier, err)
           }
           if got := carrierOf(t, s, c.ID); got != carrier+"|rebind" {
               t.Fatalf("换绑 carrier 不改写: got %q want %q", got, carrier+"|rebind")
           }
       }
   }
   ```

   跑红：

   ```bash
   go test ./internal/ledger -run 'TestClaimCardAs|TestLegacyClaimCard|TestRebindDriver|TestCarrierOpaque' -count=1 -v 2>&1 | tail -40
   ```

   预期红点逐条：TestClaimCardAsWritesCarrierColumns 死于 `carrier 应落列: ""`（stub 不写）；TestLegacyClaimCardDelegatesWithEmptyCarrier 死于前置 `前置载体应已落列`（防断言空转）；TestRebindDriverConflictKeepsEverything 死于 `expect 不符应 CAS 冲突`（stub 恒 nil）；TestRebindDriverSuccessGoldPayload 死于 `新绑定未覆写`；TestRebindDriverEmptyExpectRequiresUnbound 死于第二条 `应 CAS 冲突`；TestCarrierOpaqueRoundTrip 死于首例 `carrier 存取不改写`。红因与 F1 一致才许进下一步。

3. 【绿】其一，binding.go 替换 ClaimCardAs 空壳（完整代码块）：

   ```go
   // ClaimCardAs 认领归属并登记载体标识（B156.2 契约 §3.2）。语义与 ClaimCard
   // 完全一致：归属判定与归属写入同一事务（move.go 文件头警告的两写窗口）、
   // 不改状态列、不落事件、终态拒绝 ErrBadState、他主持有拒绝 ErrCASConflict、
   // 同 owner 重入幂等；另写 driver_carrier 列。
   //
   // carrier 是不透明载体标识（breakdown 澄清一，2026-08-26 定稿）：本期只存
   // 不解释——不解析、不校验、不假设格式；空串含义是「未登记载体」。既有
   // ClaimCard 保持签名不变、内部转调本方法传空串：老调用方零改动，显式认领
   // 同时把历史载体归零为未登记（权威在本次认领动作本身）。
   func (s *Store) ClaimCardAs(id, owner, carrier string) error {
       log().Info("开始认领归属", "card", id, "owner", owner)
       err := s.mutate(func(tx *sql.Tx, sink *eventSink) error {
           if owner == "" {
               log().Warn("认领被拒：owner 为空", "card", id)
               return fmt.Errorf("认领被拒：owner 为空")
           }
           card, err := getCardTx(s, tx, id)
           if err != nil {
               return fmt.Errorf("认领: 卡 %s: %w", id, err)
           }
           if card.Status == StatusDone || card.Status == StatusClosed {
               log().Warn("认领被拒：终态卡", "card", id, "status", card.Status)
               return fmt.Errorf("卡 %s 已处于终态 %s: %w", id, card.Status, ErrBadState)
           }
           if card.DriverSession != "" && card.DriverSession != owner {
               log().Warn("认领被拒：他主持有", "card", id,
                   "holder", card.DriverSession, "claimer", owner)
               return fmt.Errorf("卡 %s 已由 %s 认领: %w", id, card.DriverSession, ErrCASConflict)
           }
           if _, err := tx.Exec(s.q(`UPDATE cards SET driver_session = ?, driver_carrier = ?,
               driver_heartbeat_at = ? WHERE id = ?`),
               owner, carrier, s.tval(s.timeNow()), id); err != nil {
               return fmt.Errorf("认领写归属: %w", err)
           }
           return nil
       })
       if err != nil {
           log().Warn("认领归属失败", "card", id, "owner", owner, "cause", err)
           return err
       }
       log().Info("归属已认领", "card", id, "owner", owner)
       return nil
   }
   ```

   注意：sink 参数在认领路径不落事件，闭包签名保留 `sink *eventSink` 与 move.go 原形一致（不用 `_`，因为 appendEvent 未被调用时两者等价，但保持与被移植体逐字同形便于 review 比对——若 gofmt/vet 报 unused，改为 `_ *eventSink` 并在台账记录这一偏离）。

   其二，binding.go 替换 RebindDriver 空壳（完整代码块）：

   ```go
   // RebindDriver 换绑（契约 §3.2）：expect=当前绑定前值（CAS，不符返回
   // ErrCASConflict）；expect="" 与「前值为空」统一比较——即要求当前无绑定。
   // 成功=同事务覆写 driver_session+driver_carrier（并按 TakeoverCard 先例
   // 刷新 driver_heartbeat_at＝新认领时刻）且落恰一条 EvDriverTakeover
   // payload{from,to}、actor=新会话（运行锁抢占同位先例 runlock.go:89）。
   // 复用活跃事件类型，不新增绑定事件类型。写权检查读当前值：旧会话对该
   // 房间的一切后续判定随覆写自然剥权（C4 Send 执法依赖这一点）。
   // 终态卡不在拒绝之列：冻结语义无此条（房间面只读由 ReadOnly 执法）。
   func (s *Store) RebindDriver(id, toSession, carrier, expect string) error {
       log().Info("开始换绑驱动", "card", id, "to", toSession, "expect", expect)
       prev := ""
       err := s.mutate(func(tx *sql.Tx, sink *eventSink) error {
           if toSession == "" {
               log().Warn("换绑被拒：目标会话为空", "card", id)
               return fmt.Errorf("换绑被拒：目标会话为空")
           }
           card, err := getCardTx(s, tx, id)
           if err != nil {
               return fmt.Errorf("换绑: 卡 %s: %w", id, err)
           }
           if card.DriverSession != expect {
               log().Warn("换绑被拒：CAS 冲突", "card", id, "expect", expect, "actual", card.DriverSession)
               return fmt.Errorf("卡 %s 当前绑定 %q 非 %q: %w", id, card.DriverSession, expect, ErrCASConflict)
           }
           if _, err := tx.Exec(s.q(`UPDATE cards SET driver_session = ?, driver_carrier = ?,
               driver_heartbeat_at = ? WHERE id = ?`),
               toSession, carrier, s.tval(s.timeNow()), id); err != nil {
               return fmt.Errorf("换绑写归属: %w", err)
           }
           if _, err := s.appendEvent(tx, sink, id, EvDriverTakeover, toSession,
               map[string]string{"from": card.DriverSession, "to": toSession}); err != nil {
               return fmt.Errorf("换绑落事件: %w", err)
           }
           prev = card.DriverSession
           return nil
       })
       if err != nil {
           log().Warn("换绑驱动失败", "card", id, "to", toSession, "cause", err)
           return err
       }
       log().Info("驱动已换绑", "card", id, "from", prev, "to", toSession)
       return nil
   }
   ```

   其三，binding.go 头部 import 改为 database/sql、errors、fmt、time；文件头注释删去「Ticket 0 空壳：本文件全部方法无可观测行为…」句（职责了结），保留权威位置与互不代写声明段。

   其四，move.go 仅替换 ClaimCard 函数体为转调（注释保留原文语义段）：

   ```go
   // ClaimCard 认领卡的归属锁（人尺度）。归属判定与归属写入在同一事务完成，
   // 不改变状态列、不落认领事件；冲突返回 ErrCASConflict，归属不因时间流逝转移。
   // 同一 owner 重入幂等成功。
   //
   // B156.2 起：实现转调 ClaimCardAs(id, owner, "")（契约 §3.2 条2——行为
   // 逐字节不变；空载体含义=未登记载体，显式认领会把历史载体值归零）。
   func (s *Store) ClaimCard(id, owner string) error {
       return s.ClaimCardAs(id, owner, "")
   }
   ```

   然后：

   ```bash
   gofmt -w internal/ledger/binding.go internal/ledger/move.go internal/ledger/binding_test.go
   go test ./internal/ledger -count=1
   ```

   包回归必须全绿——特别是既有 TestClaimCardOwnershipSemantics / TestReleaseCardOwnershipSemantics / TestTakeoverCard* / ledgerstep 包不许掉（行为不变的权威证据）。补跑跨包回归：

   ```bash
   go test ./internal/ledgerstep/... -count=1
   ```

4. 【可观测性与注释】两个方法的入口 Info/失败 Warn/CAS Warn 已内建（错误分支带上下文、成功路径不静默）；导出注释写明参数语义（carrier 不透明、expect 双义）与注意事项（终态不拒、剥权机制）。禁 print。

5. 【变异复验】①把 ClaimCard 恢复成旧体内联 UPDATE（照抄 move.go git 历史版本函数体）→ TestLegacyClaimCardDelegatesWithEmptyCarrier 必须红，还原；②RebindDriver payload 临时加第三键 `"reason":"x"` → 金样本断言红（多键即红），还原；③payload 临时去掉 to 键 → 红（缺键即红），还原；④把 `card.DriverSession != expect` 临时改成 `expect != "" && card.DriverSession != expect` → TestRebindDriverEmptyExpectRequiresUnbound 第二断言红，还原。每次用 `git diff` 确认完全还原。

### Task 3 验收与缺陷族结论

- 验收：步骤 3 三条命令全绿＋步骤 2 红点记录在台账；既有归属测试组零改动全绿＝条 2「逐字节不变」的证据。
- 生命周期/中断：绑定/换绑全在 mutate 单事务（读-判-写无窗口，move.go:23-25 警告的窗口不复存在）；takeover 事件与列覆写同事务，崩溃不留半态。
- 静默失败：CAS 冲突文案点名当前持有者（既有文案风格）；空目标/空 owner 显式拒绝；恒真 stub 欺骗面关闭。
- 跨平台假设：carrier 不透明化正是消除格式假设的动作（澄清一）；时间走注入时钟。
- 假红/假绿：金样本恰两键断言（缺多都红）；冲突后双列回读断言；前置非空断言防转调测试空转；变异四条。
- 门禁绕过：事件只经 appendEvent（禁裸 INSERT 的仓规 events.go:20）；零新跨域边。
- 序列化边界：takeover payload JSON 两键金样本断言；heartbeat 经 tval 写。

## 8. Task 4：门面缝级断言（client.LedgerClient 实现侧）＋ Following 在席出键＋ BindDriver 注释销账

### 有界文件集

改：`internal/ledger/api/api.go`（仅 BindDriver 注释）、新建 `internal/ledger/api/api_test.go`。零触碰：client.go（接口冻结）、collab 包、agentd。

### Interfaces

Consumes：ledger.Open/PutWorkflow/CreateCard/MergeCards/RenewDriverLease（公开 API，F11）、client.LedgerClient、proto.Card 的 `Following string \`json:"following,omitempty"\``（proto/ledger.go:31）。
Produces：缝②实现侧的两支测试；api.go:92-96 注释销账。

### 步骤

测试范围：./internal/ledger/... 定向＋包回归。

1. 【红绿定位声明】TestFacadeBindDriverAndLeaseThroughClientSeam 在 Task 3 之前写会红（stub 恒 nil → GetCard 读回空绑定），本 task 排在 Task 3 之后故直接跑绿——其红灯证据由 F1 探针与 Task 3 红点记录背书。TestFollowingProjectionKeyPresence 属「写下去就绿」的防回归锁（cardWire 投影已随 charter-4 落地），红绿模板豁免（最薄路径条的免除分支）；它锁的是序列化边界回归，不是新行为。
2. 新建 internal/ledger/api/api_test.go（完整代码）：

   ```go
   // 缝级断言：client.LedgerClient（spec B156.2 测试接缝清单②）的实现侧行为。
   // 入口一律经接口类型变量，不直引 Facade 具体类型（编译期 implements 背书
   // 在 api.go:27）。卡与合并用 ledger 公开 API 造：PutWorkflow/CreateCard/
   // MergeCards 均为导出方法，无需包内夹具。
   package api

   import (
       "encoding/json"
       "errors"
       "path/filepath"
       "strings"
       "testing"
       "time"

       "github.com/Xsxdot/handoff/internal/collab/client"
       "github.com/Xsxdot/handoff/internal/ledger"
       "github.com/Xsxdot/handoff/internal/proto"
   )

   func newFacadeStore(t *testing.T) (*ledger.Store, client.LedgerClient) {
       t.Helper()
       st, err := ledger.Open(filepath.Join(t.TempDir(), "facade.db"))
       if err != nil {
           t.Fatal(err)
       }
       t.Cleanup(func() { st.Close() })
       return st, New(st)
   }

   func mustBugWorkflow(t *testing.T, st *ledger.Store) {
       t.Helper()
       if _, err := st.PutWorkflow("bug", ledger.WorkflowDef{States: []string{"待办", "进行中", "已完成"}}); err != nil {
           t.Fatal(err)
       }
   }

   // 换绑 CAS 与租约存在性从声明缝上可见的行为全集。
   func TestFacadeBindDriverAndLeaseThroughClientSeam(t *testing.T) {
       st, lc := newFacadeStore(t)
       mustBugWorkflow(t, st)
       card, err := st.CreateCard(ledger.NewCard{Title: "缝级", Project: "p", Workflow: "bug", Actor: "t"})
       if err != nil {
           t.Fatal(err)
       }

       if err := lc.BindDriver(card.ID, "sess-a", "car-a", ""); err != nil {
           t.Fatalf("无绑定卡 expect=\"\" 应成功: %v", err)
       }
       got, err := lc.GetCard(card.ID)
       if err != nil || got.DriverSession != "sess-a" {
           t.Fatalf("GetCard 应见新绑定: %v %+v", err, got)
       }
       if err := lc.BindDriver(card.ID, "sess-b", "car-b", "not-current"); !errors.Is(err, ledger.ErrCASConflict) {
           t.Fatalf("expect 不符应透传 ErrCASConflict: %v", err)
       }

       if _, ok, _ := lc.DriverLease("sess-a"); ok {
           t.Fatal("未续租前应不存在")
       }
       if ok, err := st.RenewDriverLease("sess-a", time.Minute); err != nil || !ok {
           t.Fatalf("续租（Store 层，非接口面）: %v %v", ok, err)
       }
       exp, ok, err := lc.DriverLease("sess-a")
       if err != nil || !ok || exp.IsZero() || !exp.After(time.Now().Add(30*time.Second)) {
           t.Fatalf("DriverLease 应返回真实过期时刻: %v ok=%v err=%v", exp, ok, err)
       }
   }

   // §11.4 一④ C1 半：「following」在席出键正形断言＋GetCard 恒缺席
   // （单卡读不派生跟随态）。这是手写投影 cardWire 的序列化边界回归锁。
   func TestFollowingProjectionKeyPresence(t *testing.T) {
       st, lc := newFacadeStore(t)
       mustBugWorkflow(t, st)
       carrier, err := st.CreateCard(ledger.NewCard{Title: "承载", Project: "p", Workflow: "bug", Actor: "t"})
       if err != nil {
           t.Fatal(err)
       }
       member, err := st.CreateCard(ledger.NewCard{Title: "成员", Project: "p", Workflow: "bug", Actor: "t"})
       if err != nil {
           t.Fatal(err)
       }
       if err := st.MergeCards([]string{member.ID}, carrier.ID, "t"); err != nil {
           t.Fatal(err)
       }

       all, err := lc.ListAllCards("")
       if err != nil {
           t.Fatal(err)
       }
       var memberWire *proto.Card
       for i := range all {
           if all[i].ID == member.ID {
               memberWire = &all[i]
           }
       }
       if memberWire == nil || memberWire.Following != carrier.ID {
           t.Fatalf("列表投影应带 Following=%q: %+v", carrier.ID, memberWire)
       }
       raw, err := json.Marshal(memberWire)
       if err != nil {
           t.Fatal(err)
       }
       if !strings.Contains(string(raw), `"following":"`+carrier.ID+`"`) {
           t.Fatalf("在席必须出键: %s", raw)
       }

       single, err := lc.GetCard(member.ID)
       if err != nil {
           t.Fatal(err)
       }
       if single.Following != "" {
           t.Fatalf("GetCard 不派生跟随态（§11.4）: %q", single.Following)
       }
       rawSingle, err := json.Marshal(single)
       if err != nil {
           t.Fatal(err)
       }
       if strings.Contains(string(rawSingle), `"following"`) {
           t.Fatalf("缺席不得出键（omitempty）: %s", rawSingle)
       }
   }
   ```

   注意 import 里 proto 仅用于指针类型标注——若 vet/gofmt 无意见则保留（memberWire *proto.Card 使断言语义显式）。

3. 销账：api.go BindDriver 注释替换为：

   ```go
   func (f *Facade) BindDriver(id, session, carrier, expect string) error {
       // expect 语义在账本侧 RebindDriver 落地（B156.2 欠账 #4）：expect=当前
       // 绑定前值 CAS；空 expect=要求当前无绑定。本方法保持直通镜像零业务判断。
       return f.st.RebindDriver(id, session, carrier, expect)
   }
   ```

4. 跑绿＋包回归＋跨包回归＋图闸：

   ```bash
   gofmt -w internal/ledger/api/api_test.go internal/ledger/api/api.go
   go test ./internal/ledger/... -count=1
   go build ./...
   go run . graph check --repo . --view cards-B156.2-charter-4
   ```

   图闸必须 fails 空；warns 计数允许漂移（记录进台账）。api 包从 [no test files] 变为有测试属预期。

5. 【变异复验】①临时把 api_test 的入口改成直引具体类型 `var lc *Facade = New(st)` 并删掉接口变量——这恰是接缝降格，禁止提交，仅用于自查确认当前版本确实走接口（对比 git diff 后还原）；②把 cardWire 的 `Following: v.Following` 行临时删掉 → TestFollowingProjectionKeyPresence 必须红（在席不出键），还原。变异不留工作树。

### Task 4 验收与缺陷族结论

- 验收：步骤 4 四条命令全绿＋fails 空。
- 生命周期/中断：纯读侧与注释销账，无状态机改动。
- 静默失败：CAS 冲突哨兵从缝上可判（errors.Is 对 wrap 链成立——RebindDriver 以 %w 包装）。
- 跨平台假设：无新增；时间断言用相对比较（After）不钉绝对值。
- 假红/假绿：接口变量入口防缝降格；出键/缺席双向断言区分「字段缺失」与「值为零」；变异②验证投影断言能红。
- 门禁绕过：无；图闸复核。
- 序列化边界：本 task 即 §11.4 一④ C1 半的落点（C6 落另一半 agentd 视图投影断言，不在本卡）。

## 9. 收口：全量回归、图闸与原子提交

前置：Task 1–4 全部完成且各自验收绿。按序执行并逐条记录原始输出到台账：

```bash
go test ./internal/ledger/... -count=1
go test ./internal/ledgerstep/... -count=1
go build ./...
gofmt -l internal/ledger internal/ledger/api internal/collab
go run . graph check --repo . --view cards-B156.2-charter-4
git diff --check
```

预期：两组测试 ok；build 无输出；gofmt -l 无文件列出（空输出即干净）；图闸 fails 空；git diff --check 无输出。全仓 `go test ./...` 不在本卡范围（implement 三段律：全量归总验收节点）。

真机清单（未验证，需真机，归协调者；扩展 breakdown 清单 #5/#6）：

| # | 项 | 为什么机内验不了 |
| --- | --- | --- |
| R1 | PG 部署上 driver_carrier 列迁移真跑（ALTER 双文案容忍的 PG 半）＋driver_leases 建表 | 本机无 PG 实例；pgx 错误格式已源码核对（F6），服务器消息文本未验证 |
| R2 | 心跳 TTL 在墙钟下的 live=false 翻转与续租节拍真实节奏（2min 续、5min 死） | 注入时钟验不了真实节奏 |

原子提交：确认工作树只含本轮允许文件后一次提交（不 push）：

```bash
git add docs/superpowers/plans/b156.2.1-plan.md docs/superpowers/ledgers/2026-08-26-b156.2.1-plan-ledger.md internal/ledger/store.go internal/ledger/binding.go internal/ledger/move.go internal/ledger/binding_test.go internal/ledger/api/api.go internal/ledger/api/api_test.go
git status --short
git commit -m "feat(ledger): binding seats and driver leases (B156.2.1)"
git show --format= --name-only HEAD
```

name-status 必须恰为上列八文件（binding_test.go/api_test.go 为新增 A，其余 M）；出现 collab/、web/、cmd/、codegraph/、specs 下任何文件即越界，必须回滚重查。台账随实现同批提交（边干边追加的最终形态一并入库）。

## 10. 计划自审

**五项检查**：
1. 缺陷族对抗：逐族结论在各 Task 验收栏＋§9 真机清单；五族（生命周期中断/静默失败/跨平台假设/假红假绿/门禁绕过）全部有答案或明确归真机。
2. 序列化边界：命中两处——cardWire Following 投影（Task 4 出键/缺席双向断言，§11.4 一④ C1 半）与 expires_at/driver_heartbeat_at 的 tval/toTime 往返（Task 2/3 精确时刻断言＝过真实库的 encode∘decode 恒等）。takeover payload 两键金样本为第三处 JSON 边界断言。
3. 上下文预算：四个 task 文件集均圈得出（store.go/binding.go/move.go/api.go ＋两个新测试文件），无竖切需求。
4. 类型标注：d_ledger 为逻辑型非边界型——显式真机清单不适用，PG 半以 R1 补位（breakdown 清单 #5 同源）。
5. 接缝覆盖：双向核过——缝→测试（缝②两支）；测试→缝（十二支入口逐支列于 §4 表）；内部锁四组全部持「从声明缝构造不出＋为什么」声明；无条件退路。

**占位符扫描**：无 TBD／无「同 Task N」／无「加适当错误处理」；全部测试给出完整代码块；唯一例外声明——无（本计划未使用「复用既有夹具而形态因包而异」例外；所有测试代码完整给出）。内部锁声明集中于 §4。

**spec 覆盖**：contract §4「绑定与心跳」8 条 → 条1 Task3/TestClaimCardAsWritesCarrierColumns；条2 Task3/TestLegacyClaimCardDelegatesWithEmptyCarrier＋既有测试组全绿；条3 Task3/TestRebindDriverConflictKeepsEverything；条4 归 C4（本卡交付其账本前提 Produces#1，剥权机制在 TestRebindDriverSuccessGoldPayload 锁定覆写事实）；条5 Task3/TestRebindDriverSuccessGoldPayload；条6 Task2/TestDriverLeaseReadsReturnExpiredRowsAndMissing；条7 Task2/TestRenewDriverLeaseUpsertsOwnRowOnly；条8 Task2 同支 Drop 段。§11.4 一④ C1 半 → Task4/TestFollowingProjectionKeyPresence。欠账 #4 全项 → Task1–3。carrier 不透明反面 → Task3/TestCarrierOpaqueRoundTrip。expect="" 销账 → Task3/TestRebindDriverEmptyExpectRequiresUnbound＋Task4 步骤 3。无静默带走。

**跨卡签名一致性**：Consumes 与 contract §3.2/§3.4 冻结签名逐字一致（对照亲读）；Produces 引用的下游消费符号（GetCard().DriverSession、client.DriverLease、ErrCASConflict wrap 链）均为冻结物既有名。

**派发前自审**：本计划无任何需要驱动派发系统自身的验收步骤（图闸是只读 graph 子命令）；无「本 task 由协调者执行」条目之外的冲突。

**收口前三问**：①本计划引用的全部命令输出均出自本工作树实跑（台账留档），未跑到结果的（PG 真跑、墙钟节奏）已标「未验证」入真机清单；②本节点未调用 handoff CLI 写命令、未起任何 executor（仅 graph check/graph sym 只读子命令）；③产出物落法定路径 docs/superpowers/plans/b156.2.1-plan.md。




