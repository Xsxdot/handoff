# B233.26 plan：反转 D1 + 十文件归域——刀序列与门槛

前置：spec `docs/superpowers/specs/b233.26.md`；实况 `docs/superpowers/notes/b233.26-reversal-survey.md`
（行号以此为准）。基线 cards/B233.1-charter-7 @ 4f66f42df，工作树
/Users/sycm/.handoff/worktrees/b233-residual（cards/B233-residual）。

刀序（每刀一个提交，提交点必须 `go build ./...` + 该刀触面包 `go test -p 1 -count=1` 绿）：

## 刀1：契约反转 + 反转面同批文件搬迁（一个编译单元）

反转面 29 符号中 ReconcileExecutorGone 的依赖闭包（*Hub、VoidTicketsWithAudit、
recoverTransit、NewFailedPayload→admissionFn）要求 Hub/ticketvoid/watchdog/admission
与契约族同批进 orchestration，否则方向反转不净。故刀1 = spec §范围1 全部 + 十文件中的
hub / ticketvoid / watchdog / admission 四文件。

顺序建议（实现者可在不破坏编译的前提下重排）：

1. hub.go → orchestration（NewHub/Hub 类型随迁；server.go 字段/构造/访问器、
   watchdog/b23313_retained 签名改前缀）。
2. ticketvoid.go → orchestration。
3. admission.go → orchestration（admissionFn 缝随迁）。
4. watchdog.go → orchestration（Server.RecoverOnStartup 包装留 gateway；最小导出）。
5. 契约/DTO/哨兵族迁 orchestration（b23313_retained.go 拆解 + orchestration_client.go）；
   别名哨兵退役改指 workspace 正身；ReconcileExecutorGone 实现体迁移。
6. 垫片消亡（RecoverTransit/CheckProcHeadroom 同包直调；Git 三兄弟改直引 workspace；
   TruncateRunes 按 spec 判据）+ 死代码退役（五垫片 + CanonPath，grep 取证）。
7. 9 个 orchestration 生产文件 import 翻转改净；agentd 侧消费点（server/handlers/
   roomsapi/status 等）改 `orchestration.` 前缀；cmd/agentd.go 按 survey §5 表改前缀；
   cmd/agentd_test.go:134 断言同步。
8. 方向守卫反转：orchestration 侧新守卫（生产不得 import agentd）；
   b23317_boundary_test.go 棘轮更新；b23313_managerfactory_test.go / _wire_test.go
   若此刀已可退役则退，否则留刀2。

门槛：build 绿；`go test -p 1 -count=1 ./internal/... ./cmd/...` 绿；
`go list -deps ./internal/orchestration` 不含 agentd。

## 刀2：余下文件搬迁 + 测试归置收尾

shutdown.go、pullstate.go（PullTracker 导出缝）、eventframes.go（hook 导出化）、
coordinator_home.go（首选整搬+窄注入，>2 新导出则降级拆分，判据入实现记录）、
logging.go（删 agentd/logging.go 并轨 orchestration/log.go）、b23313_retained.go 收尾消亡；
ManagerFactory 桥退役（若刀1 未退）；ptyreclaim_test.go 集成用例随迁改写；
agentd 留守测试的 NewHub 等引用改前缀。

门槛：build 绿；`go test -p 1 -count=1 ./...` 全绿（62 包）。

## 刀3：图对账（docs/test 提交）

best.json 容器重登记（k_agentd_Hub/pullTracker/fn/model 等 → k_orchestration_*，契约族
新容器）；target.json 棘轮按新实况改写（d_gateway→d_orchestration 语义反转 + 
Interfaces:["OrchestrationClient"] 归属更新）；按 docs/codegraph-scan-recipe.md 重扫
baseline.json 或最小视图对账；`codegraph check` 0 fails、`codegraph validate` 0 错。

## 刀4：docs 收口

实现记录（新导出清单、死代码证据、coordinator_home 判据）+ roadmap 残余条目核销 +
卡片落账。

## 验收命令

```
go build ./...
go test -p 1 -count=1 ./...
grep -rn 'internal/agentd' internal/orchestration --include='*.go'   # 生产零命中
go list -deps ./internal/orchestration | grep 'internal/agentd'      # 零输出
codegraph check && codegraph validate
```
