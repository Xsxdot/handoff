# B156.3.6 Plan：K6 d_web——载体/小队配置页、排队呈现、拉起与 attach

> 状态：plan 节点产出物。本节点只写计划，不写 .go/.ts 产品实现。
> 卡：B156.3.6。有效基线：claude/kai-156-3-ba-df7357；当前执行分支：
> cards/B156.3.6-charter-3。合并目标以此为准，不切分支、不 push。
>
> 冻结输入：
> docs/superpowers/specs/2026-08-26-b156.3-automation-keystone-design.md、
> docs/superpowers/specs/b156.3-contract.md、
> docs/superpowers/specs/b156.3-breakdown.md。
> K3 接缝对照：docs/superpowers/plans/b156.3.3-plan.md。
> K4 接缝对照：docs/superpowers/plans/b156.3.4-plan.md。
> 边干边落台账：docs/superpowers/plans/b156.3.6-plan-ledger.md。

## 0. 基线证据与计划判据

计划作者在当前基线已实际运行以下命令；输出摘要与未验证项逐行记录在台账。实现轮必须在开始对应 task 前重跑该 task 的命令，因为本计划只承诺产出文档，不把当前基线结果冒充实现后的结果。

| 范围 | 基线命令 | 实际结果 | 实现轮的绿判据 |
|---|---|---|---|
| Web 全套 | cd web && npm test | Test Files 108 passed (108)；Tests 1111 passed (1111)；Duration 15.49s；既有 HTMLCanvasElement.getContext() warning 未导致失败 | 改动后的全套 Vitest 通过；warning 不能被改写成失败 |
| Web 类型 | cd web && npm run typecheck | 输出 > web@0.0.0 typecheck、> tsc -b，退出 0 | tsc -b 退出 0 |
| K6 前端接缝 | cd web && npm test -- --run src/app/settings/SettingsPage.test.tsx src/app/cards/CardsPage.test.tsx src/app/cards/CardDrawer.test.tsx src/api/contract.test.ts | Test Files 4 passed (4)；Tests 88 passed (88)；Duration 1.91s | 加入三类新组件测试后全绿 |
| Go 编译 | go build ./... | 无输出，退出 0 | 改动后的 go build ./... 退出 0 |
| K4 HTTP 接缝 | go test ./internal/agentd/ -run 'TestCoord' -count=1 -v | 6 支既有 TestCoord* 均显式 SKIP；原始原因包含 PTY 测试根目录不可用、tmp read-only file system、repo-dot socket=112/113 超过 100；包最终 PASS/ok | 实现轮在可用 PTY 根目录执行新增 coordapi 断言；再次 SKIP 只能贴原文，不能写 pass |

本节点验收判据钉行为，不钉文件数：

1. GET /api/squads、两个 CAS PUT、GET /api/queue 和协调者三端点的 TS 请求函数与 Go wire DTO 字段逐字对应；K3 可选字段的缺席、协调者 attach 的 null 和 machine 空串都有真实 JSON 断言。
2. 设置页按行带 version 发 PUT；400/409 原文可见，成功后刷新，不把失败吞成空列表。
3. 队列面显示服务端 position；卡面看板与列表显示同卡最早队列位次；前端不重排、不把缺失位次变成 0。
4. 卡抽屉的拉起、状态、attach/交回都穿过 API client；忙时禁用；失败正文可见；POST attach 成功回执的 machine/dir/command 原样呈现。
5. roadmap 两项核销有锁：status 的 dir 取 resolveCoordWorkdir(id)，active 缺失 400、定位失败回滚、?machine= 转发各有 HTTP 断言。
6. jsdom 只锁 HTTP 回执与三元组展示；原生终端 tab 的真机行为列为未验证，不能伪装成前端测试通过。

## 1. 现状、边界与跨卡接口

### 1.1 有图检查与覆盖债

仓内有 codegraph/best.json，最优领域词表包含 d_web、d_web_contract、d_web_admin、d_web_command。按纪律运行 codegraph context d_web 的原始结果是：

~~~
/bin/bash: line 1: codegraph: command not found
~~~

因此不把 best.json 的容器归属当作调用链结论；符号/调用面以当前源码逐条核对，覆盖债写入台账。计划的有界文件集：

- Go wire/agentd：internal/proto/scheduling.go、internal/proto/contract_fixture_test.go、internal/agentd/coordapi.go、internal/agentd/coordapi_test.go。
- Web contract：web/src/api/scheduling.ts、web/src/api/contract.test.ts、web/src/api/testdata/CoordinatorStatus.json、web/src/api/testdata/CoordinatorAttachReleaseResp.json。
- Web 管理页：web/src/app/settings/SettingsPage.tsx、SettingsPage.test.tsx、新 SchedulingPage.tsx、SchedulingPage.test.tsx。
- Web 看板/卡：CardsPage.tsx、CardsPage.test.tsx、CardItem.tsx、ListView.tsx、新 QueuePanel.tsx/QueuePanel.test.tsx、新 CoordinatorPanel.tsx/CoordinatorPanel.test.tsx、CardDrawer.tsx/CardDrawer.test.tsx。

不在本卡：internal/scheduling 规则、internal/keystone 会话存储/唤醒策略、PTY host 终端本体、CLI squad 命令、第二套 TUI。

### 1.2 冻结 wire

K3 的 docs/superpowers/plans/b156.3.3-plan.md D3 与当前 web/src/api/scheduling.ts 定义以下既有类型，K6 不改字段名、optional 口径或路径：

~~~text
CarrierView = { name:string; machine:string; cli:string; home_dir:string;
  model?:string; credential:string; max_concurrency?:number; healthy:boolean; version:number }
SquadView = { name:string; role:string; members:string[]; max_concurrency?:number; version:number }
SquadsResp = { carriers:CarrierView[]; squads:SquadView[] }
CarrierInput = { name?:string; machine:string; cli:string; home_dir:string;
  model?:string; credential:string; max_concurrency?:number }
SquadInput = { name?:string; role:string; members:string[]; max_concurrency?:number }
SquadPutResp = { name:string; version:number }
QueueEntry = { kind:string; id:string; card:string; node?:string; squad:string;
  target?:string; executor?:string; model?:string; priority?:string; ready:boolean;
  actor:string; seq:number; position:number }
QueueResp = { queue:QueueEntry[] }
CoordinatorLaunchResp = { woke:boolean; session_id?:string; rebuilt:boolean;
  escalated:boolean; output?:string }
~~~

| 方法/路径 | 请求 | 200 响应 | K6 口径 |
|---|---|---|---|
| GET /api/squads | 无 | SquadsResp | 两空数组是合法空库 |
| PUT /api/squads/carriers/{name}?expect=N | CarrierInput | SquadPutResp | version 是下一次 CAS 来源 |
| PUT /api/squads/squads/{name}?expect=N | SquadInput | SquadPutResp | 409 后重取 |
| GET /api/queue | 无 | QueueResp | position 是后端全局 1 基位次 |
| POST /api/cards/{id}/coordinator/launch | {} | CoordinatorLaunchResp | 看板唯一拉起入口 |
| GET /api/cards/{id}/coordinator | 无 | CoordinatorStatus | 轮询状态，attach 是定位预览 |
| POST /api/cards/{id}/attach | active=true/workdir 或 active=false | active=true 为 AttachInfo，active=false 为 {ok:true} | POST 回执是本次三元组权威值 |

### 1.3 Interfaces

#### Consumes

~~~go
func (s *Server) resolveCoordWorkdir(cardID string) string
func (s *Server) handleCoordStatus(w http.ResponseWriter, r *http.Request)
func (s *Server) handleCoordAttach(w http.ResponseWriter, r *http.Request)
func (s *Server) forwardIfRequested(w http.ResponseWriter, r *http.Request) bool
func (s *keystone.Service) Locate(card, workdir string) (keysclient.AttachInfo, error)
func (s *keystone.Service) AttachState(card string) bool
func (s *keystone.Service) SetAttach(card string, active bool)
~~~

~~~ts
export function request<T>(path: string, init?: RequestInit): Promise<T>
export function postJSON<T>(path: string, body: unknown): Promise<T>
export function putJSON<T>(path: string, body: unknown): Promise<T>
export function usePoll<T>(fetcher: () => Promise<T>, intervalMs: number, opts?: { enabled?: boolean }): PollState<T>
export interface PollState<T> {
  data: T | null
  disconnected: boolean
  sessionExpired: boolean
  errorText: string
  refresh: () => void
}
~~~

#### Produces

Task A 在 internal/proto/scheduling.go 增加以下 DTO；Go struct tag 中的反引号按原样保留：

~~~go
// CoordinatorAttachInfo 是协调者会话的定位三元组；Machine 允许为空串表示本机。
type CoordinatorAttachInfo struct {
  Machine string `json:"machine"`
  Dir string `json:"dir"`
  Command string `json:"command"`
}

// CoordinatorStatus 是 GET coordinator 的状态；Attach=nil 序列化为 null 表示未绑定。
type CoordinatorStatus struct {
  Bound bool `json:"bound"`
  AttachActive bool `json:"attach_active"`
  Attach *CoordinatorAttachInfo `json:"attach"`
}

// CoordinatorAttachReleaseResp 是 active=false 交回无头后的成功回执。
type CoordinatorAttachReleaseResp struct {
  OK bool `json:"ok"`
}
~~~

Attach 不使用 omitempty，所以未绑定 wire 明确为 null，TS 不把它误写成缺席；Machine 不加 omitempty，本机空串是有值。机器值只镜像服务端 AttachInfo.Machine，不从浏览器 hostname 猜；dir 的 GET 来源固定为 resolveCoordWorkdir(id)。

~~~ts
/** POST attach=true 的服务端定位回执；machine 空串仍须保留。 */
export interface CoordinatorAttachInfo { machine: string; dir: string; command: string }
/** GET coordinator 的状态；未绑定时 attach 明确为 null。 */
export interface CoordinatorStatus { bound: boolean; attach_active: boolean; attach: CoordinatorAttachInfo | null }
/** POST attach=false 的服务端成功回执。 */
export interface CoordinatorAttachReleaseResp { ok: boolean }
/** 参数：完整 cardId；返回：GET coordinator wire；路径段必须 encodeURIComponent。 */
export const getCoordinatorStatus = (cardId: string) =>
  request<CoordinatorStatus>('/api/cards/' + encodeURIComponent(cardId) + '/coordinator')
/** 参数：完整 cardId 与 workdir；返回：AttachInfo；不改写服务端三元组。 */
export const attachCoordinator = (cardId: string, workdir: string) =>
  postJSON<CoordinatorAttachInfo>('/api/cards/' + encodeURIComponent(cardId) + '/attach', { active: true, workdir })
/** 参数：完整 cardId；返回：{ok:true}；路径段必须 encodeURIComponent。 */
export const releaseCoordinator = (cardId: string) =>
  postJSON<CoordinatorAttachReleaseResp>('/api/cards/' + encodeURIComponent(cardId) + '/attach', { active: false })
~~~

~~~ts
export interface SchedulingPageProps {}
export function SchedulingPage(props: SchedulingPageProps): JSX.Element
export interface QueuePanelProps {
  entries: QueueEntry[]; loading: boolean; disconnected: boolean
  sessionExpired: boolean; errorText: string; onOpenCard: (cardId: string) => void
}
export function QueuePanel(props: QueuePanelProps): JSX.Element
export function queuePositionByCard(entries: readonly QueueEntry[]): ReadonlyMap<string, number>
export interface CoordinatorPanelProps { cardId: string }
export function CoordinatorPanel(props: CoordinatorPanelProps): JSX.Element
~~~

已有组件只扩展 queuePosition?: number（CardItem）与 queuePositions?: ReadonlyMap<string, number>（ListView），不改既有必填参数。

## 2. Task A：协调者 wire、status 工作目录与 K4 错误锁

### 文件与基线

文件：internal/proto/scheduling.go、internal/proto/contract_fixture_test.go、internal/agentd/coordapi.go、internal/agentd/coordapi_test.go、web/src/api/scheduling.ts、web/src/api/contract.test.ts、新增两个 Coordinator fixture。

先跑：

~~~bash
go test ./internal/proto/ -run TestContractFixtures -count=1
go test ./internal/agentd/ -run 'TestCoord(StatusEndpoint|AttachTakeoverMutesWake)$' -count=1 -v
cd web && npm test -- --run src/api/contract.test.ts
~~~

预期 proto/web contract 通过；agentd 既有基线若因 PTY 显式 SKIP，记录原始原因，不写后端通过。新增纯 HTTP 错误锁必须改用下方无 PTY 装配，不能把 SKIP 当通过。

### 步骤

1. 追加上方三个 proto DTO。每个导出类型前写职责/边界注释，特别说明 attach 缺席与 machine 空串的区别。
2. 在 contract_fixture_test.go K3 cases 的 CoordinatorLaunchResp 后加入：

~~~go
{"CoordinatorStatus", CoordinatorStatus{
    Bound: true, AttachActive: false,
    Attach: &CoordinatorAttachInfo{
        Machine: "", Dir: "/repo/handoff", Command: "opencode --session sess-coord",
    },
}},
{"CoordinatorAttachReleaseResp", CoordinatorAttachReleaseResp{OK: true}},
~~~

运行 go test ./internal/proto/ -run TestContractFixtures -update 生成两个 fixture，检查 diff，再用不带 -update 的命令锁定字段缺席/零值。两个新增文件内容必须等于以下 Go 序列化产物：

web/src/api/testdata/CoordinatorStatus.json：

~~~json
{
  "bound": true,
  "attach_active": false,
  "attach": {
    "machine": "",
    "dir": "/repo/handoff",
    "command": "opencode --session sess-coord"
  }
}
~~~

web/src/api/testdata/CoordinatorAttachReleaseResp.json：

~~~json
{
  "ok": true
}
~~~
3. coordapi.go 删除私有 coordinatorStatusResp，加入仅做边界投影的函数：

~~~go
func coordinatorAttachInfo(info keysclient.AttachInfo) proto.CoordinatorAttachInfo {
    return proto.CoordinatorAttachInfo{
        Machine: info.Machine, Dir: info.Dir, Command: info.Command,
    }
}
~~~

函数注释说明不修改值、不拼命令；命令仍由 locator 产生。
4. handleCoordStatus 把 Locate(id, "") 改成 Locate(id, resolveCoordWorkdir(id))；成功映射到 proto.CoordinatorAttachInfo，失败用结构化 logger 带 card/cause 记录并保留现有 bound=false 口径。入口带 card，成功带 bound/attach_active/dir；不记录 session id/凭据。
5. active=true 成功返回 DTO，active=false 返回 proto.CoordinatorAttachReleaseResp{OK:true}。active 缺失先 Warn（card、reason=active_missing）再 400；定位失败保持 SetAttach(true)→Locate→SetAttach(false) 回滚，日志带 card/workdir/cause；不吞错误原文。forwardIfRequested 顺序不动。
6. scheduling.ts 追加上方 TS 类型与函数。每个导出函数注释写明参数、返回 wire、cardId 必须完整编码；复用 request/postJSON，不直接 fetch、不吞 ApiError。
7. contract.test.ts 导入两个 fixture 与新类型，逐条断言 bound=true、attach_active=false、machine key 存在且值为空、dir=/repo/handoff、command 含 sess-coord、release.ok=true；再构造 `attach:null` 的 CoordinatorStatus 未绑定态，断言 null 与字段缺席不混淆。该测试消费真实 fixture，属于序列化接缝。
8. 扩展 TestCoordStatusEndpoint，并把该测试的环境改为 `newNoPTYCoordEnv`：登记 ProjectLocation{Name:"handoff",Path:"/repo/handoff"}；未拉起时 HTTP 解码 proto.CoordinatorStatus，断言 Bound=false、Attach=nil 且原文含 `"attach":null`；拉起后断言 Bound、Attach 非 nil、Dir、Command、Machine 空串和 JSON 中存在 machine；接管后断言 AttachActive。这条真实 HTTP 测试核销 coordapi.go 原 Locate 空目录，不得被 PTY 探针跳过。
9. 先在 internal/agentd/coordapi_test.go 增加无 PTY 装配 helper，避免这三条纯 HTTP 错误锁被 `newLedgerEnv` 的 PTY 探针整支跳过。helper 必须照以下完整代码实现；它仍使用真实 ledger、真实 HTTP Handler、真实 keystone/scheduling，只把测试环境换成已有的 `newNoPTYLedgerEnv`：

~~~go
func newNoPTYCoordEnv(t *testing.T) (*ledgerEnv, *fakeCoordRunner) {
    t.Helper()
    env := newNoPTYLedgerEnv(t)
    env.srv.SetupAutomation(env.ledger)
    runner := &fakeCoordRunner{}
    env.srv.SetKeystone(keystone.New(runner, &fakeCoordNarrator{}, env.srv.autoLedger, attachLocator{}))
    return env, runner
}

func setCoordForwardTarget(t *testing.T, env *ledgerEnv, name, addr string) {
    t.Helper()
    env.srv.SetConfigPath(filepath.Join(t.TempDir(), "config.yaml"))
    if err := env.srv.swapConf(func(cfg *config.Config) error {
        cfg.Targets = map[string]config.Target{
            name: {Addr: addr, Token: testToken},
        }
        return nil
    }); err != nil {
        t.Fatalf("登记转发 target: %v", err)
    }
}
~~~

该 helper 需要在 coordapi_test.go 的 import 中加入 `path/filepath` 和 `github.com/Xsxdot/handoff/internal/config`；这是测试装配依赖，不是产品接口。新测试复用 `createCoordCard`、`seedCoordinatorSquad`、`ledgerPost`，转发测试复用 forward_test.go 的 httptest remote 记录方式，并按下列接缝断言：

~~~text
TestCoordAttachMissingActive：POST /api/cards/{id}/attach body={} → 400；body 含 active 必填；AttachActive(id)=false。
TestCoordAttachLocateFailureRollsBack：不先 launch，POST active=true/workdir=/repo/handoff → 400；body 含没有绑定；随后 AttachActive(id)=false。
TestCoordAttachForwardsMachineQuery：先将本地 keystone 的 AttachActive(cardID) 置 true，remote 记录 method/path/body/header；本地 POST /api/cards/{id}/attach?machine=devbox body active=false；本地返回 remote 状态/body，remote 收到 POST、去掉 machine 的 path、原 body、X-Handoff-Forwarded；请求后本地 AttachActive(cardID) 仍为 true，证明本地 release 未执行。
~~~

前三支使用 `newNoPTYCoordEnv`，不得因 PTY 探针 SKIP；每支逐条断言状态码、响应原文和副作用。status/既有生命周期测试可继续复用 `newCoordEnv`，若显式 SKIP 只记录原始 SKIP。转发测试先启动 remote httptest，再调用 `setCoordForwardTarget`，断言本地 release 没有被执行。
10. 锁缝红绿按三步执行：先不更新 fixture 运行 `go test ./internal/proto/ -run TestContractFixtures -count=1` 与新增 coordapi focused，确认原始失败；再按上面 DTO/handler/测试的最小改动实现；最后重跑 proto fixture、`go test ./internal/agentd/ -run 'TestCoord(StatusEndpoint|AttachTakeoverMutesWake|AttachMissingActive|AttachLocateFailureRollsBack|AttachForwardsMachineQuery)$' -count=1 -v` 与 web contract focused，只有实测全绿才记通过。随后运行 `go build ./...`；纯映射不另造周期，但必须被 HTTP/fixture 边界覆盖。

Produces：proto.CoordinatorStatus、proto.CoordinatorAttachInfo、proto.CoordinatorAttachReleaseResp，以及 getCoordinatorStatus(cardId:string): Promise<CoordinatorStatus>、attachCoordinator(cardId:string,workdir:string): Promise<CoordinatorAttachInfo>、releaseCoordinator(cardId:string): Promise<CoordinatorAttachReleaseResp>。下游 Task D 只消费这些精确签名。

## 3. Task B：设置页载体/小队配置

### 文件与基线

文件：web/src/app/settings/SettingsPage.tsx、SettingsPage.test.tsx、新 SchedulingPage.tsx、SchedulingPage.test.tsx。先跑：

~~~bash
cd web && npm test -- --run src/app/settings/SettingsPage.test.tsx
cd web && npm run typecheck
~~~

预期基线 SettingsPage 测试通过、tsc -b 退出 0。

### 新文件完整代码

SchedulingPage 只经 scheduling.ts，使用 usePoll(getSquads,10000)，每行保存取 row.version，不复制角色/CAS规则。以下为完整文件代码：

~~~tsx
// SchedulingPage.tsx —— 自动化编制配置分区。
// 职责：编辑载体/小队登记行并显示版本/健康快照。
// 边界：只经 api/scheduling.ts；不读 registry、不决定角色规则、不负责拉起。
import { useEffect, useState } from 'react'
import type { CarrierInput, CarrierView, SquadInput, SquadView } from '../../api/scheduling'
import { getSquads, putCarrier, putSquad } from '../../api/scheduling'
import { usePoll } from '../data/usePoll'
import { DisconnectedBanner, LoadFailed, SessionExpiredBanner } from '../lib/Banners'
import { errorMessage } from '../lib/format'
import { Button } from '@/components/ui/button'

type CarrierDraft = CarrierInput & { name: string }
type SquadDraft = SquadInput & { name: string }
const INPUT = 'h-8 rounded-md border bg-background px-2 text-xs'
export interface SchedulingPageProps {}

/** 空字符串表示 omitempty；0 不是“未填写”的替代值。 */
function optionalConcurrency(raw: string): number | undefined {
  if (raw.trim() === '') return undefined
  const value = Number(raw)
  return Number.isInteger(value) && value >= 0 ? value : undefined
}
function carrierDraft(row: CarrierView): CarrierDraft {
  return { name: row.name, machine: row.machine, cli: row.cli, home_dir: row.home_dir,
    model: row.model, credential: row.credential, max_concurrency: row.max_concurrency }
}
function squadDraft(row: SquadView): SquadDraft {
  return { name: row.name, role: row.role, members: [...row.members], max_concurrency: row.max_concurrency }
}

/**
 * 参数：空 props；返回：载体/小队编辑页；注意：保存必须使用服务端当前 version。
 */
export function SchedulingPage(_props: SchedulingPageProps) {
  const state = usePoll(getSquads, 10000)
  const [carriers, setCarriers] = useState<Record<string, CarrierDraft>>({})
  const [squads, setSquads] = useState<Record<string, SquadDraft>>({})
  const [errors, setErrors] = useState<Record<string, string>>({})
  const [busy, setBusy] = useState<string | null>(null)
  useEffect(() => {
    if (!state.data) return
    setCarriers(Object.fromEntries(state.data.carriers.map((r) => [r.name, carrierDraft(r)])))
    setSquads(Object.fromEntries(state.data.squads.map((r) => [r.name, squadDraft(r)])))
  }, [state.data])
  const saveCarrier = async (row: CarrierView) => {
    const draft = carriers[row.name]
    if (!draft) return
    const key = 'carrier:' + row.name
    setBusy(key); setErrors((e) => ({ ...e, [key]: '' }))
    try { await putCarrier(row.name, row.version, { ...draft, name: draft.name || undefined }); state.refresh() }
    catch (err) { setErrors((e) => ({ ...e, [key]: errorMessage(err) })) }
    finally { setBusy(null) }
  }
  const saveSquad = async (row: SquadView) => {
    const draft = squads[row.name]
    if (!draft) return
    const key = 'squad:' + row.name
    setBusy(key); setErrors((e) => ({ ...e, [key]: '' }))
    try { await putSquad(row.name, row.version, { ...draft, name: draft.name || undefined }); state.refresh() }
    catch (err) { setErrors((e) => ({ ...e, [key]: errorMessage(err) })) }
    finally { setBusy(null) }
  }
  if (state.sessionExpired) return <div className="p-3"><SessionExpiredBanner /></div>
  if (state.data === null) return <div className="p-3"><LoadFailed message={state.errorText || '正在读取载体/小队配置…'} onRetry={() => state.refresh()} /></div>
  return <div className="flex min-h-full flex-col gap-4 p-3">
    {state.disconnected && <DisconnectedBanner message={state.errorText} />}
    <div><h2 className="text-sm font-semibold">自动化编制</h2><p className="mt-1 text-xs text-muted-foreground">载体是物理承载，小队是角色与并发策略；保存使用当前版本的 CAS。</p></div>
    <section className="rounded-lg border p-3"><h3 className="mb-2 text-xs font-semibold">载体</h3>
      {state.data.carriers.map((row) => { const d = carriers[row.name] ?? carrierDraft(row); const key = 'carrier:' + row.name
        return <div key={row.name} className="mb-3 rounded-md border p-2">
          <div className="mb-2 flex items-center gap-2 text-xs"><span className="font-mono">{row.name}</span><span className="text-muted-foreground">v{row.version}</span><span>{row.healthy ? '健康' : '未探活'}</span></div>
          <div className="grid gap-2 md:grid-cols-3">
            <input aria-label={row.name + ' machine'} className={INPUT} value={d.machine} onChange={(e) => setCarriers((x) => ({ ...x, [row.name]: { ...d, machine: e.target.value } }))} />
            <input aria-label={row.name + ' cli'} className={INPUT} value={d.cli} onChange={(e) => setCarriers((x) => ({ ...x, [row.name]: { ...d, cli: e.target.value } }))} />
            <input aria-label={row.name + ' home_dir'} className={INPUT} value={d.home_dir} onChange={(e) => setCarriers((x) => ({ ...x, [row.name]: { ...d, home_dir: e.target.value } }))} />
            <input aria-label={row.name + ' model'} className={INPUT} value={d.model ?? ''} onChange={(e) => setCarriers((x) => ({ ...x, [row.name]: { ...d, model: e.target.value || undefined } }))} />
            <input aria-label={row.name + ' credential'} className={INPUT} value={d.credential} onChange={(e) => setCarriers((x) => ({ ...x, [row.name]: { ...d, credential: e.target.value } }))} />
            <input aria-label={row.name + ' max_concurrency'} className={INPUT} type="number" min="0" value={d.max_concurrency ?? ''} onChange={(e) => setCarriers((x) => ({ ...x, [row.name]: { ...d, max_concurrency: optionalConcurrency(e.target.value) } }))} />
          </div>
          <div className="mt-2"><Button size="sm" variant="outline" disabled={state.disconnected || busy !== null} onClick={() => void saveCarrier(row)}>保存载体</Button></div>
          {errors[key] && <p role="alert" className="mt-1 break-words text-xs text-destructive">{errors[key]}</p>}
        </div> })}
      {state.data.carriers.length === 0 && <p className="text-xs text-muted-foreground">尚未登记载体。</p>}
    </section>
    <section className="rounded-lg border p-3"><h3 className="mb-2 text-xs font-semibold">小队</h3>
      {state.data.squads.map((row) => { const d = squads[row.name] ?? squadDraft(row); const key = 'squad:' + row.name
        return <div key={row.name} className="mb-3 rounded-md border p-2">
          <div className="mb-2 flex items-center gap-2 text-xs"><span className="font-mono">{row.name}</span><span className="text-muted-foreground">v{row.version}</span></div>
          <div className="grid gap-2 md:grid-cols-3">
            <input aria-label={row.name + ' role'} className={INPUT} value={d.role} onChange={(e) => setSquads((x) => ({ ...x, [row.name]: { ...d, role: e.target.value } }))} />
            <input aria-label={row.name + ' members'} className={INPUT} value={d.members.join(',')} onChange={(e) => setSquads((x) => ({ ...x, [row.name]: { ...d, members: e.target.value.split(',').map((v) => v.trim()).filter(Boolean) } }))} />
            <input aria-label={row.name + ' max_concurrency'} className={INPUT} type="number" min="0" value={d.max_concurrency ?? ''} onChange={(e) => setSquads((x) => ({ ...x, [row.name]: { ...d, max_concurrency: optionalConcurrency(e.target.value) } }))} />
          </div>
          <div className="mt-2"><Button size="sm" variant="outline" disabled={state.disconnected || busy !== null} onClick={() => void saveSquad(row)}>保存小队</Button></div>
          {errors[key] && <p role="alert" className="mt-1 break-words text-xs text-destructive">{errors[key]}</p>}
        </div> })}
      {state.data.squads.length === 0 && <p className="text-xs text-muted-foreground">尚未登记小队。</p>}
    </section>
  </div>
}
~~~

### 步骤与测试

1. SettingsPage 的 SECTIONS 追加 scheduling/自动化编制，中央内容区挂 SchedulingPage；顶部“五个”改“六个”，其他分区与返回行为不动。
2. 先在 SchedulingPage.test.tsx 写红测并运行 focused：mock getSquads 返回 carrier version=3、max_concurrency 缺席和 coordinator squad version=2、members=[]；断言行/v3/v2。编辑并保存，逐字断言 putCarrier('c1',3,{name:'c1',machine:'m2',cli:'opencode',home_dir:'/h',credential:'standalone',model:undefined,max_concurrency:undefined}) 与 putSquad('coord',2,{name:'coord',role:'coordinator',members:[],max_concurrency:undefined})。reject ApiError(400,'载体不可达') 和 409 时 role=alert 显示原文；首拉失败显示 LoadFailed；断线时保存按钮 disabled。该红测入口是 SchedulingPage，属于 d_web_admin 接缝。
3. 最小实现后重跑该 focused，确认红测转绿，再跑 typecheck；保留新文件头、导出注释和 optionalConcurrency“空值缺席而非 0”的原因注释。入口输入（行名/version）只进入 API 调用，成功以刷新后的版本显示，失败以带行上下文的 alert 显示；基线 Web 无结构化 logger，因而不用 print/alert/新增 console。
4. SettingsPage.test.tsx 点击自动化编制断言 heading，同时保留旧分区断言。

Produces：SchedulingPage({}): JSX.Element。Consumes：K3 已有 `getSquads(): Promise<SquadsResp>`、`putCarrier(name:string,expect:number,input:CarrierInput): Promise<SquadPutResp>`、`putSquad(name:string,expect:number,input:SquadInput): Promise<SquadPutResp>`。

## 4. Task C：队列面与卡位次

### 文件与基线

文件：web/src/app/cards/CardsPage.tsx、web/src/app/cards/CardsPage.test.tsx、web/src/app/cards/CardItem.tsx、web/src/app/cards/ListView.tsx、新 web/src/app/cards/QueuePanel.tsx 与 QueuePanel.test.tsx。先跑：

~~~bash
cd web && npm test -- --run src/app/cards/CardsPage.test.tsx
cd web && npm run typecheck
~~~

### QueuePanel.tsx 完整代码

~~~tsx
// QueuePanel.tsx —— 看板的排队快照区。
// 职责：显示 GET /api/queue 的后端位次，并把卡号接回抽屉。
// 边界：不重排、不重算 ready、不调用队列规则；断线语义由 usePoll 提供。
import type { QueueEntry } from '../../api/scheduling'
import { DisconnectedBanner, SessionExpiredBanner } from '../lib/Banners'
export interface QueuePanelProps {
  entries: QueueEntry[]; loading: boolean; disconnected: boolean
  sessionExpired: boolean; errorText: string; onOpenCard: (cardId: string) => void
}
/** 返回每张卡的最早服务端位次；不排序、不把空卡号写入结果。 */
export function queuePositionByCard(entries: readonly QueueEntry[]): ReadonlyMap<string, number> {
  const positions = new Map<string, number>()
  for (const entry of entries) {
    if (entry.card === '') continue
    const current = positions.get(entry.card)
    if (current === undefined || entry.position < current) positions.set(entry.card, entry.position)
  }
  return positions
}
function entryLabel(entry: QueueEntry): string {
  return entry.kind === 'launch_queue' ? '拉起' : entry.kind === 'ignition_queue' ? '点火' : entry.kind
}
/** 参数：轮询快照与打开卡抽屉回调；返回：后端顺序的队列表；不触发规则计算。 */
export function QueuePanel({ entries, loading, disconnected, sessionExpired, errorText, onOpenCard }: QueuePanelProps) {
  return <section className="mx-4 mt-2 rounded-lg border bg-background p-3" aria-label="排队中">
    <div className="mb-2 flex items-center gap-2"><h2 className="text-xs font-semibold">排队中</h2><span className="text-[11px] text-muted-foreground">{entries.length} 条 · 后端顺序</span></div>
    {sessionExpired && <SessionExpiredBanner />}
    {disconnected && !sessionExpired && <DisconnectedBanner compact message={errorText} />}
    {loading && entries.length === 0 && <p className="text-xs text-muted-foreground">正在读取队列…</p>}
    {!loading && entries.length === 0 && !disconnected && !sessionExpired && <p className="text-xs text-muted-foreground">当前没有排队项。</p>}
    {entries.length > 0 && <ol className="space-y-1">{entries.map((entry) => <li key={entry.kind + '/' + entry.id} className="flex flex-wrap items-center gap-2 rounded border px-2 py-1.5 text-xs">
      <span className="font-mono font-semibold">#{entry.position}</span><button type="button" className="font-mono underline" onClick={() => onOpenCard(entry.card)}>{entry.card}</button>
      <span>{entryLabel(entry)}</span><span className="text-muted-foreground">{entry.node || entry.squad}</span>
      {entry.priority && <span className="text-muted-foreground">优先级 {entry.priority}</span>}
      <span className={entry.ready ? 'text-green-600' : 'text-amber-700'}>{entry.ready ? '可运行' : '等待条件'}</span>
    </li>)}</ol>}
    {entries.length === 0 && disconnected && <p role="alert" className="text-xs text-destructive">队列尚未成功读取：{errorText}</p>}
  </section>
}
~~~

### 步骤与测试

1. 先写 QueuePanel.test.tsx 红测并运行 focused：输入后端 position 2、1、3，断言 DOM 仍为 2、1、3且逐行显示 card/node-or-squad/priority/ready；同卡 4/2 得最小 2；空 card 不进 map；position=0 显示 #0，缺席不能转成 0；卡号点击调用 onOpenCard；断线保留 entries 且有“已断开”；首拉失败无 entries 时 role=alert 有 errorText；sessionExpired 显示会话失效且不显示“当前没有排队项”。该红测入口是 QueuePanel，属于 d_web_command queue 接缝。
2. CardsPage 导入 getQueue/QueuePanel，增加 `const queuePoll = usePoll(getQueue, POLL_MS)`，并以 `const queueLoading = queuePoll.data === null && !queuePoll.disconnected && !queuePoll.sessionExpired` 明确派生首拉态，再用 queuePositionByCard(queuePoll.data?.queue ?? []) 派生 map；在看板/列表前挂 QueuePanel，传入 queueLoading、disconnected、sessionExpired、errorText，点击走既有 openDrawer；不排序。
3. CardItem 加 queuePosition?: number，仅在 queuePosition !== undefined 时显示“排队 #N”；ListView 加 queuePositions?: ReadonlyMap<string,number> 与“排队”列；CardsPage 两处传同一 map。
4. CardsPage.test.tsx scheduling mock 的 getQueue 返回稳定队列，断言真实 CardsPage 看到排队中/#1/卡号，点击打开抽屉，切 list 后仍有排队列/#1；reject 时既有卡仍在、断线文案可见、其它写操作不误禁用。
5. 跑新增红测→最小实现→focused 绿测；新文件头、服务端 position 权威不重排的原因注释保留。首拉、成功快照、断线旧快照和 401 各有可见状态，禁止新增 console/print。

Produces：QueuePanelProps、queuePositionByCard(entries: readonly QueueEntry[]): ReadonlyMap<string,number>，并让 CardsPage 成为队列轮询组装点。Consumes：K3 已有 `getQueue(): Promise<QueueResp>`。

## 5. Task D：卡抽屉协调者、拉起与 attach

### 文件与基线

文件：新 CoordinatorPanel.tsx/CoordinatorPanel.test.tsx、CardDrawer.tsx/CardDrawer.test.tsx。先跑：

~~~bash
cd web && npm test -- --run src/app/cards/CardDrawer.test.tsx
cd web && npm run typecheck
~~~

预期基线 CardDrawer 测试通过、tsc -b 退出 0。

### CoordinatorPanel.tsx 完整代码

~~~tsx
// CoordinatorPanel.tsx —— 卡抽屉里的协调者状态、拉起、attach 入口。
// 职责：把生命周期 HTTP 回执转成人能行动的状态与定位三元组。
// 边界：不创建第二套 TUI；不解析目录；原生终端行为由真机验证。
import { useState } from 'react'
import { attachCoordinator, getCoordinatorStatus, launchCoordinator, releaseCoordinator } from '../../api/scheduling'
import type { CoordinatorAttachInfo } from '../../api/scheduling'
import { usePoll } from '../data/usePoll'
import { DisconnectedBanner, LoadFailed, SessionExpiredBanner } from '../lib/Banners'
import { errorMessage } from '../lib/format'
import { Button } from '@/components/ui/button'
/** cardId 是完整卡号；组件只显示服务端定位信息，不承诺浏览器已打开原生终端。 */
export interface CoordinatorPanelProps { cardId: string }
/** 参数：卡号；返回：协调者状态/拉起/attach/交回操作区；真机终端由另行清单验证。 */
export function CoordinatorPanel({ cardId }: CoordinatorPanelProps) {
  const state = usePoll(() => getCoordinatorStatus(cardId), 5000)
  const [busy, setBusy] = useState<'launch' | 'attach' | 'release' | null>(null)
  const [actionError, setActionError] = useState('')
  const [launchOutput, setLaunchOutput] = useState('')
  const [attachInfo, setAttachInfo] = useState<CoordinatorAttachInfo | null>(null)
  const launch = async () => {
    setBusy('launch'); setActionError(''); setLaunchOutput('')
    try { const result = await launchCoordinator(cardId); setLaunchOutput(result.output ?? (result.escalated ? '协调者已转人工处理' : '协调者已拉起')); state.refresh() }
    catch (err) { setActionError(errorMessage(err)) } finally { setBusy(null) }
  }
  const attach = async () => {
    setBusy('attach'); setActionError('')
    try { const result = await attachCoordinator(cardId, state.data?.attach?.dir ?? ''); setAttachInfo(result); state.refresh() }
    catch (err) { setActionError(errorMessage(err)) } finally { setBusy(null) }
  }
  const release = async () => {
    setBusy('release'); setActionError('')
    try { await releaseCoordinator(cardId); setAttachInfo(null); state.refresh() }
    catch (err) { setActionError(errorMessage(err)) } finally { setBusy(null) }
  }
  if (state.sessionExpired) return <section className="mb-5"><h3 className="mb-1.5 text-xs font-semibold">协调者</h3><SessionExpiredBanner /></section>
  if (state.data === null) return <section className="mb-5"><h3 className="mb-1.5 text-xs font-semibold">协调者</h3><LoadFailed message={state.errorText || '正在读取协调者状态…'} onRetry={() => state.refresh()} /></section>
  const info = attachInfo ?? state.data.attach
  const disabled = state.disconnected || busy !== null
  return <section className="mb-5 rounded-lg border p-3">
    <div className="flex items-center gap-2"><h3 className="text-xs font-semibold">协调者</h3><span className="text-xs">{state.data.bound ? '已绑定' : '未绑定'}</span>{state.data.attach_active && <span className="rounded-full border border-amber-300 bg-amber-50 px-1.5 text-[10px]">人工接管中</span>}</div>
    {state.disconnected && <DisconnectedBanner compact message={state.errorText} />}
    {!state.data.bound && <Button className="mt-2" size="sm" disabled={disabled} onClick={() => void launch()}>拉起协调者</Button>}
    {state.data.bound && !state.data.attach_active && <Button className="mt-2" size="sm" variant="outline" disabled={disabled} onClick={() => void attach()}>打开终端</Button>}
    {state.data.attach_active && <Button className="mt-2" size="sm" variant="outline" disabled={disabled} onClick={() => void release()}>交回无头</Button>}
    {launchOutput && <p className="mt-2 break-words text-xs text-muted-foreground">{launchOutput}</p>}
    {info && <dl className="mt-2 grid gap-1 text-xs"><div><dt className="inline text-muted-foreground">机器：</dt><dd className="inline">{info.machine || '本机'}</dd></div><div><dt className="inline text-muted-foreground">目录：</dt><dd className="inline break-all font-mono">{info.dir || '—'}</dd></div><div><dt className="inline text-muted-foreground">命令：</dt><dd className="inline break-all font-mono">{info.command || '—'}</dd></div></dl>}
    {actionError && <p role="alert" className="mt-2 break-words text-xs text-destructive">{actionError}</p>}
  </section>
}
~~~

按钮的本期含义是调用 attach 并显示服务端命令；不得在 jsdom 伪造 window.open 成真机 TUI 已打开，也不得以浏览器 hostname 替 machine。

### 步骤与测试

1. 先写 CoordinatorPanel.test.tsx 红测并运行 focused，mock scheduling API、使用受控 usePoll：未绑定点拉起，断言 launchCoordinator('B1')、output 与 refresh；400 reject 原文 role=alert；已绑定未接管点打开终端，断言 attachCoordinator('B1','/repo/handoff')，返回 machine/dir/command 逐项显示；接管态点交回，断言 releaseCoordinator('B1')、ok 后 refresh；断线保留状态且写按钮 disabled；401/首拉失败分别显示 SessionExpired/LoadFailed。该红测入口是 CoordinatorPanel，属于 d_web_command coordinator 接缝。
2. CardDrawer detail 内容区在 CardAttention 与“环节动作”之间插入 CoordinatorPanel cardId={id}；loading/error 分支不挂载它。
3. CardDrawer.test.tsx scheduling mock 提供 getCoordinatorStatus 默认未绑定、launch/attach/release 默认成功，确保旧测试不请求网络；CoordinatorPanel 专测全部动作，旧测试锁 CardDrawer→CoordinatorPanel 挂载 seam。
4. 红测→最小接入→跑 npm test -- --run src/app/cards/CoordinatorPanel.test.tsx src/app/cards/CardDrawer.test.tsx 与 npm run typecheck；保留文件头、导出参数/返回/注意事项注释与 API URL 编码。入口 cardId、外部调用成功回执、断线/失败正文和 busy 禁态必须可见；不用新增 console/print 或输出 session/credential。
5. 再跑 CardDrawer/CardsPage/contract focused，确认抽屉、队列、wire 同时绿。

Produces：CoordinatorPanel({cardId:string}): JSX.Element。Consumes：Task A 的 `getCoordinatorStatus(cardId:string): Promise<CoordinatorStatus>`、`attachCoordinator(cardId:string,workdir:string): Promise<CoordinatorAttachInfo>`、`releaseCoordinator(cardId:string): Promise<CoordinatorAttachReleaseResp>`，以及 K3 已有 `launchCoordinator(cardId:string): Promise<CoordinatorLaunchResp>`。

## 6. Task E：集成收口

### 文件与基线

只改 web/src/app/settings/SettingsPage.test.tsx、web/src/app/cards/CardsPage.test.tsx、web/src/app/cards/CardDrawer.test.tsx 的 mocks/断言和前述生产文件接缝。先跑：

~~~bash
cd web && npm test -- --run src/app/settings/SettingsPage.test.tsx src/app/cards/CardsPage.test.tsx src/app/cards/CardDrawer.test.tsx
~~~

基线实测 3 个文件通过；实现轮保留旧断言并扩大到 K6 focused 集合。

### 步骤

1. SettingsPage 测试与 SchedulingPage 测试的 scheduling mock 分开；CardsPage 提供稳定 queue/health/cards；CardDrawer 提供默认 coordinator status，避免旧 case 启动真实轮询。
2. 运行：

~~~bash
cd web && npm test -- --run src/api/contract.test.ts src/app/settings/SettingsPage.test.tsx src/app/settings/SchedulingPage.test.tsx src/app/cards/QueuePanel.test.tsx src/app/cards/CoordinatorPanel.test.tsx src/app/cards/CardsPage.test.tsx src/app/cards/CardDrawer.test.tsx
cd web && npm run typecheck
~~~

只有得到全绿输出才记 pass；失败时贴原始报错，不替它归因。

## 7. 缺陷族、序列化、类型和接缝自审

### 7.1 缺陷族对抗

- 空/零/缺席：fixture 锁 machine=""、attach_active=false、max_concurrency 缺席、members=[]、ready=false；前端不以 || 改写 0。
- 错误/断线：三条轮询分别显示首拉失败、旧快照断线、401 终止；PUT 400/409、launch 400、attach 400 原文可见；断线禁写。
- CAS/并发：保存用 GET version，成功 refresh，409 不假装成功、不覆盖服务端行。
- 排序/快照：QueuePanel 不 sort，卡位次取同卡最小服务端 position，不重读卡、不推导 ready/priority。
- 编码/路由：card/name 用 encodeURIComponent；?machine= 由 agentd 转发，Web 不拼 host。
- 生命周期：usePoll 清 timer；CoordinatorPanel 只在 detail 存在时挂载；busy 防双 POST；交回后 refresh。
- attach 安全：定位失败回滚 SetAttach(false)；status 不伪造 session；machine 不由浏览器补造；不建第二套终端。
- 可观测性：Go status/attach 外部调用前后及错误分支使用结构化 logger，带 card/workdir/cause；基线 Web 没有可复用的结构化 logger，故前端以成功回执、断线/失败正文和 busy 禁态作为用户可见观测，不新增 print/console，也不打印 credential/session。

### 7.2 序列化边界

| 边界 | 手写投影/消费点 | 断言 |
|---|---|---|
| keysclient.AttachInfo → proto DTO | internal/agentd/coordapi.go 的 coordinatorAttachInfo | TestCoordStatusEndpoint 与 attach 成功 |
| proto → JSON | internal/proto/contract_fixture_test.go | TestContractFixtures 逐字节 |
| JSON → TS | web/src/api/contract.test.ts | K3 缺席、CoordinatorStatus null、machine 空串、false/true/command |
| TS client → endpoint | web/src/api/scheduling.ts | CoordinatorPanel path/body，Go HTTP response |
| QueueResp → panel/card/list | CardsPage map 与 queuePositionByCard | QueuePanel、CardsPage board/list |
| SquadsResp → draft → PUT | SchedulingPage mapping | 精确 body，optional/空数组 |

新增字段必须新增边界行和真实穿边断言；两端各测不替代真实序列化回归。

### 7.3 类型标注与真机清单

1. 浏览器真实 GET/PUT squads，核对版本和 CAS。
2. 浏览器真实 GET queue，核对队列行与卡 chip 位次。
3. 浏览器真实点击拉起，核对 CoordinatorLaunchResp；无小队时核对 actionable 400 原文。
4. 浏览器真实 attach/交回，核对三元组与 ok=true。
5. 远端 ?machine= 转发并在目标机器验证命令打开原生 TUI/tab；未执行前写“未验证”，jsdom 不代验。

### 7.4 双向接缝矩阵

| 测试入口 | 接缝 | 锁定行为 |
|---|---|---|
| TestContractFixtures、contract.test.ts | d_web_contract | Go JSON 与 TS mirror、optional/zero |
| SchedulingPage.test.tsx、SettingsPage.test.tsx | d_web_admin | GET/PUT、版本、400/409、导航 |
| QueuePanel.test.tsx、CardsPage.test.tsx | d_web_command queue | 后端顺序、位次、点击抽屉、断线 |
| CoordinatorPanel.test.tsx、CardDrawer.test.tsx | d_web_command coordinator | launch/status/attach/release、错误原文 |
| TestCoordStatusEndpoint | gateway status JSON | resolveCoordWorkdir 与三元组 |
| 三支 TestCoordAttach* | K4 attach error seams | 400、回滚、转发/无副作用 |

反向覆盖：API mirror→fixture+contract；squads→SchedulingPage；queue→QueuePanel+CardsPage；launch→CoordinatorPanel 真实点击；status→CoordinatorPanel+Go HTTP；active→CoordinatorPanel+既有互斥测试；三条错误缝→三支独立 Go HTTP 测试。queuePositionByCard 纯测只是补充，不能顶替 CardsPage 调用链。

## 8. spec 覆盖、跨卡审计与计划自检

| 用户故事/核销项 | 具体归属 |
|---|---|
| 载体/小队配置与 400 呈现 | Task B |
| 队列呈现与卡位次 | Task C |
| 卡上拉起 | Task D |
| coordinator status/attach 三元组 | Task A + D |
| active 缺失、定位回滚、?machine= | Task A |
| 原生终端真机行为 | Task E 第 5 项清单，未验证前不写 pass |

K3 Produces 的 getSquads(): Promise<SquadsResp>、getQueue(): Promise<QueueResp>、launchCoordinator(cardId:string): Promise<CoordinatorLaunchResp> 与本计划 Consumes 逐字一致；K4 status/attach wire 由 Task A 收口后供 Task D 逐字消费；Task A→B/C/D 的签名在 §1.3 与各 Produces 重复列出。

这是单上下文自审，不能替代协调者要求的独立上下文跨卡审计；冻结物逐条、跨卡签名和 spec 归属最终状态为“待拍板”。本计划未调用 handoff CLI、未启动 executor。

计划自检声明：没有未命名的待办、未指定的错误分支、空测试骨架或未指定实现出口。Go 测试明确复用 `newNoPTYCoordEnv`、`newCoordEnv`、`createCoordCard`、`seedCoordinatorSquad`、`ledgerPost`（coordapi_test.go）及 remote forwarding harness（forward_test.go），Web 复用现有 Vitest/Testing Library setup 和 mocks；每处均列具体 pass/fail 断言。window.open/原生 TUI 没有作为退路。

## 9. 收尾

本节点只提交本计划与台账，不提交产品实现：

~~~bash
git diff --check
git diff --name-only -- docs/superpowers/plans/b156.3.6-plan.md docs/superpowers/plans/b156.3.6-plan-ledger.md
git add docs/superpowers/plans/b156.3.6-plan.md docs/superpowers/plans/b156.3.6-plan-ledger.md
git commit -m "docs(b156.3.6): web console implementation plan"
git status --short --branch
git log -1 --oneline
~~~

预期提交只含两份文档，不含 .go/.ts 产品代码；不 push。
