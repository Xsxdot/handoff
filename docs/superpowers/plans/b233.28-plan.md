# B233.28 实现计划：gateway 任务面与项目位置走编排查询门面

状态：**待 implement**。L3 轻档，单卡单缝，不建子卡（禁止 B233.28.x）。
卡号：B233.28（父卡 B233）　路由：contract → breakdown →（单轮）implement → review → acceptance → 图对账 → finish。
有效基线分支：`cards/B233.1-charter-7` @ `56c7a9c7`（本卡不切、不越过）。本计划落点分支：`cards/B233.28-charter-6`。
冻结物：`docs/superpowers/specs/b233.28-contract.md`（`cdbf6d598` 起含 C-1..C-4 回写）。源 spec：`docs/superpowers/specs/b233.28.md`。拆解：`docs/superpowers/specs/b233.28-breakdown.md`（P-1..P-4 均为 A）。
本计划不新增 HTTP 路径、CLI 命令、事件类型字面值、顶层领域、跨机协议、依赖方向；不改 `target.json` / `best.json` / `baseline.json` / `assembly`；不把 Session/Workbench/Auth 做成门面方法。

拍板输入（随本节点注入，逐条落实，不翻案）：

- **P-1=A（C-1）**：任务/项目**读路径**一律走 `s.mgr`；`s.mgr == nil` 按既有写路径同款 503 + Warn。受影响测试夹具注入**真 Manager**，不得回落 `s.st`。
- **P-2=A（C-2）**：`projectIndex()`（`workspace.LoadProjectIndex(s.st,…)`）与 `buildLocalTree()`（`workspace.BuildLocalTree(ctx, s.st,…)`）经 workspace 窄接口注入，**本卡不动**。
- **P-3=A（C-3）**：门面 `AnswerTicket` 只 `AppendEvent`、**不** `hub.Publish`。
- **P-4=A（C-4）**：验收 = `codegraph check --view cards/B233.28-charter` `fails=[]` 且 `d_gateway->d_orchestration` 命中 ≤ 76；`target.json` 零改动；命中下降属后续重扫卡（OOS）。

> 说明：本文件由 plan 节点对 `internal/orchestration/*_test.go`、`internal/agentd/*_test.go` 的**夹具/助手**改动属拟派实现节点要写的测试；plan 节点不落实现、不建脚手架。本文件就是本节点的产出物。

---

## §0 基线核验（plan 节点亲自跑到，原始输出）

本工作树 `cards/B233.28-charter-6` @ `cdbf6d5989dfa41aa8640933ff2854d227e171f6`。

```text
$ go build ./...
（空输出）BUILD_EXIT=0

$ go vet ./internal/agentd/ ./internal/orchestration/ ./cmd/...
（空输出）VET_EXIT=0

$ go test ./internal/orchestration/ -count=1
ok  	github.com/Xsxdot/handoff/internal/orchestration	59.354s
ORCH_EXIT=0

$ go test ./internal/agentd/ -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	159.038s
AGENTD_EXIT=0

$ go test ./cmd/... -count=1
ok  	github.com/Xsxdot/handoff/cmd	42.232s
CMD_EXIT=0

$ go test ./internal/agentd/ -run 'TestReplyAnswersTicketAndNotifies|TestReplyUnknownTicket404|TestReplySelfHealsWithoutWaiter|TestReplyRelayFailureReturns502|TestHandleReplyIdempotentDoesNotRelayTwice|TestAttachEmptyListsAreArrays|TestAuthRequired' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	0.697s

$ codegraph check --view cards/B233.28-charter --repo .
{"fails": [], "d_gateway->d_orchestration": 76, "warns_count": 36, "bestCoverage": {"assignedContainers":338,"viewContainers":338,"crossDomainEdges":1248,"misplacedSkipped":0}}
CHECK_EXIT=0

$ codegraph validate --view cards/B233.28-charter --repo .
（issues:null，0 issue）VALIDATE_EXIT=0
```

现状签名与调用面（plan 节点读源码核实，行号为**本轮读数**，`file#Symbol` 是引用锚，行号会漂）：

| 事实 | 出处 |
| --- | --- |
| `s.st` 生产直打方法与调用点已全量清点，与 contract §1.1/§1.3 逐条对齐 | contract §1.1/§1.3、breakdown §2.4；本计划 §3.W3 给逐文件清单 |
| `AnswerTicketApplied` 语义（`answer IS NULL`；`n==1`→true；同值→(false,nil)；异值/作废→`ErrTicketConflict`；不存在→`ErrNotFound`） | `internal/store/store.go#Store.AnswerTicketApplied` `:1275-1305` |
| `UpdateTaskState` CAS：`GetTask`→`CanTransit`→`UPDATE … WHERE id=? AND state=?`，0 行→`ErrBadTransit` | `internal/store/store.go#Store.UpdateTaskState` `:525-552` |
| `GetProjectLocationByName` 不存在回 `fmt.Errorf("项目 %s: %w", name, ErrNotFound)`，`errors.Is` 可判 | `internal/store/projects.go#Store.GetProjectLocationByName` `:82-90` |
| `ticket_answered` 事件字面值 `"ticket_answered"` + `TicketAnsweredPayload{TicketID,Answer}` JSON 键 `ticket_id`/`answer` | `internal/proto/proto.go:106`、`internal/orchestration/contracts.go:115` |
| `AnswerTicket` / `ResumeIfIdle` 骨架空壳与 `ErrTicketAnswerUnwired`（仅 facade.go 4 处自引用） | `internal/orchestration/facade.go:26,30,102,106` |
| `OrchestrationClient` 已含 15 个 B233.28 方法 | `internal/orchestration/orchestration_client.go:54-74` |
| `*Manager` 满足 `OrchestrationClient` | `internal/orchestration/manager.go:339` |
| manager 构造入口 `NewManager(st, hub, ads, cfg, envMapping, approver, gate, log)` | `internal/orchestration/manager.go:297` |
| `newManagerForServer(t, srv, ads)` 已存在（注入真 Manager 给白盒 env） | `internal/agentd/b23313_gateway_testhelpers_test.go:299-313` |
| `newTestEnv` / `newTestAgentdEnv` 默认**不注入** manager | `internal/agentd/server_test.go:66`、`internal/agentd/w3a_testhelpers_test.go:55` |
| 无 manager 时读路径今日仍返回 200（切门面后会变 503，故受测夹具必须注入） | 见 §0 基线 `TestAuthRequired` 绿、`TestAttachEmptyListsAreArrays` 绿 |

**未验证（需真机，不在本卡）**：真实跨机 reply/列任务/项目位置的五分支与重启自愈、并发双 reply 回迁唯一性、图重扫后的真实命中下降——沿用 breakdown §6 的五条，本计划机内只验契约形状与 HTTP 级行为。

---

## §1 有界文件集（本卡唯一实现范围）

### 1.1 生产改动

- `internal/orchestration/facade.go`：`AnswerTicket` / `ResumeIfIdle` 落地；删 `ErrTicketAnswerUnwired` 与随之无用的 `errors` import。
- `internal/agentd/handlers.go`、`tasksfanout.go`、`taskroute.go`、`taskplan.go`、`roomsapi.go`、`update.go`、`projectadmin.go`、`codegraph.go`、`coordapi.go`、`workspacefiles.go`：§3.W2/W3 的调用点切换与 `s.mgr` 未就绪守卫。

### 1.2 测试改动

- 新增：`internal/orchestration/facade_test.go`（W1 缝级断言）。
- 新增：`internal/agentd/b23328_source_guard_test.go`（W3 静态面断言）。
- 改助手/夹具与受影响用例：见 §3.W2/W3 的**精确清单**。

### 1.3 明令禁止（越界即退回 contract）

- 整包 `internal/agentd` 的 pty/hub/forward/render/frames/preview、`internal/executor`、登录/工作台/auth（`auth.go`/`authroutes.go`/`workbench_api.go`）生产代码；`internal/ledger`、`internal/collab`、`d_web`。
- `codegraph/target.json`、`best.json`、`baseline.json`、`assembly`。
- 给门面加 Session/Workbench/Auth 方法或 wire DTO/序列化边界。
- 改 `projectIndex()` / `buildLocalTree()` 的注入源（C-2）。

---

## §2 接缝、测试范围与内部锁声明

### 2.1 接缝清单（对照 spec §测试决定）

| 缝 | 入口符号 | 锁住的行为 | 承接 task |
| --- | --- | --- | --- |
| S1 编排门面任务查询/应答/回迁 | `Server.handleReply` → `OrchestrationClient.AnswerTicket`/`ResumeIfIdle`；`Manager.AnswerTicket`/`Manager.ResumeIfIdle` | gateway 只打门面（不持 CAS 循环）；无待办才 running；幂等/冲突/跨任务不泄露；CAS 并发唯一 | W1、W2 |
| S2 编排门面项目位置查询/更新 | `Server.handleProjectPatch`/`handleProjectBranches`/`handleProjectCodegraph` → `OrchestrationClient.GetProjectLocationByName`/`UpdateProjectLocation`/`ListProjectLocations` | 响应/状态码/错误分流逐字不变；不再直打 store | W3 |
| S3 图 | `codegraph check --view cards/B233.28-charter` | `fails=[]` 且 `d_gateway->d_orchestration` ≤ 76 | W4 |

### 2.2 测试范围声明（最小化）

- W1 只跑 `go test ./internal/orchestration/ -count=1`。
- W2 只跑 `go test ./internal/agentd/ -run 'Reply|HandleReply|B23313' -count=1`。
- W3 只跑 `go test ./internal/agentd/ -count=1`（读路径改动面宽，需整包；`integration_test.go`/`status_test.go` 等已注入 manager 的用例一并回归）。
- W4 跑三包全量 + `go build ./...` + `go vet ./internal/agentd/ ./internal/orchestration/ ./cmd/...` + `codegraph check/validate`。
- 全量测试不属于任何单个 task（W4 复跑不算「全量验收」，只算收口复核）。

### 2.3 内部锁声明（对照五项检查「接缝覆盖」）

- `TestB23328GatewayNoDirectStoreTaskProjectCalls`（§3.W3.4）是**内部锁**：入口是包级静态文件扫描，不在任何声明缝上。**合法理由**：从声明缝（HTTP/handler 入口）**构造不出**「gateway 生产代码不再直打 `s.st.<任务/项目方法>`」这条断言——它是对整包静态调用面的属性，任何运行时用例都可能因恰巧走别的分支而漏掉某处直打。故按「只能附加」声明，且每个实现类 task 另有缝级断言（W1 的 `Manager.AnswerTicket` 断言、W2 的 `TestHandleReplyDelegatesToFacade` 入口 `handleReply` 在 S1 上）。
- 步骤正文无「若意外先绿就改入口」类条件退路，退路同闸无触发。
- 假缝禁令：不把未导出纯函数当缝；不加纯映射测试。

---

## §3 Task DAG（W1 → W2 → W3 → W4）

依赖：W1 → W2 → W3 → W4。W1 门面行为可独立单测绿；W2/W3 共享 `internal/agentd` 编译面，同批提交保 `go build` 始终绿。

---

### W1：编排门面 `AnswerTicket` + `ResumeIfIdle` 落地（红→绿）

**文件与接口**

- 生产：`internal/orchestration/facade.go`。
- 测试：新增 `internal/orchestration/facade_test.go`（`package orchestration`）。
- Consumes：`store.GetTicket`、`store.AnswerTicketApplied`、`store.GetTask`、`store.PendingTickets`、`store.UpdateTaskState`、`store.ErrNotFound`/`ErrTicketConflict`/`ErrBadTransit`、`proto.EventTypeTicketAnswered`、`TicketAnsweredPayload`。
- Produces：`Manager.AnswerTicket(ctx, taskID, ticketID, answer) (applied bool, err error)`（行为）；`Manager.ResumeIfIdle(ctx, taskID)`（行为）；删除 `ErrTicketAnswerUnwired`。`OrchestrationClient` 签名不变（形状已冻）。

**步骤**

1. **写失败测试（红）。** 新建 `internal/orchestration/facade_test.go`：

```go
package orchestration

import (
	"context"
	"encoding/json"
	"errors"
	"sync"
	"testing"
	"time"

	"github.com/Xsxdot/handoff/internal/proto"
	"github.com/Xsxdot/handoff/internal/store"
)

// seedWaitingAnswerTask 落一条 waiting_answer 任务，可选挂一张未答工单。
func seedWaitingAnswerTask(t *testing.T, st *store.Store, taskID, ticketID string) {
	t.Helper()
	now := time.Now().UTC()
	if err := st.CreateTask(&proto.Task{ID: taskID, RepoPath: "/r", Executor: "fake",
		State: proto.TaskStateWaitingAnswer, CreatedAt: now, UpdatedAt: now}); err != nil {
		t.Fatalf("CreateTask(%s): %v", taskID, err)
	}
	if ticketID != "" {
		if _, err := st.CreateTicket(&proto.Ticket{ID: ticketID, TaskID: taskID, Kind: "ask",
			Request: json.RawMessage(`{}`), CreatedAt: now}); err != nil {
			t.Fatalf("CreateTicket(%s): %v", ticketID, err)
		}
	}
}

// TestFacadeAnswerTicketAnswersAndEmitsEvent 锁契约 §5.2 #8/#9/#10：
// applied=true 落库 + ticket_answered 事件（payload 线格式逐字）；同值幂等
// (false,nil)；异值 ErrTicketConflict；不存在/跨任务 ErrNotFound 且不泄露。
func TestFacadeAnswerTicketAnswersAndEmitsEvent(t *testing.T) {
	m, st, _, _ := newTestManager(t)
	seedWaitingAnswerTask(t, st, "t1", "tk1")
	ctx := context.Background()

	applied, err := m.AnswerTicket(ctx, "t1", "tk1", "答")
	if err != nil || !applied {
		t.Fatalf("AnswerTicket = (%v,%v)，want (true,nil)", applied, err)
	}
	tk, err := st.GetTicket("tk1")
	if err != nil {
		t.Fatalf("GetTicket: %v", err)
	}
	if tk.Answer == nil || *tk.Answer != "答" {
		t.Fatalf("应答未落库: %+v", tk.Answer)
	}
	evs, err := st.EventsFrom("t1", 0, 100)
	if err != nil {
		t.Fatalf("EventsFrom: %v", err)
	}
	var answered *proto.Event
	for i := range evs {
		if evs[i].Type == proto.EventTypeTicketAnswered {
			answered = &evs[i]
		}
	}
	if answered == nil {
		t.Fatal("applied=true 必须追加 ticket_answered 事件")
	}
	// 序列化边界：JSON 键与形状逐字不变（发出方从 gateway 搬到编排，同进程同库）
	want := `{"ticket_id":"tk1","answer":"答"}`
	if string(answered.Payload) != want {
		t.Fatalf("ticket_answered payload = %s，want %s", answered.Payload, want)
	}

	// 同值幂等
	applied, err = m.AnswerTicket(ctx, "t1", "tk1", "答")
	if err != nil || applied {
		t.Fatalf("同值重答 = (%v,%v)，want (false,nil)", applied, err)
	}
	// 异值冲突
	if _, err := m.AnswerTicket(ctx, "t1", "tk1", "别的"); !errors.Is(err, store.ErrTicketConflict) {
		t.Fatalf("异值重答 err=%v，want ErrTicketConflict", err)
	}
	// 不存在
	if _, err := m.AnswerTicket(ctx, "t1", "ghost", "x"); !errors.Is(err, store.ErrNotFound) {
		t.Fatalf("未知工单 err=%v，want ErrNotFound", err)
	}
	// 跨任务：按不存在处理，不泄露
	if _, err := st.CreateTicket(&proto.Ticket{ID: "tk-other", TaskID: "t-other", Kind: "ask",
		Request: json.RawMessage(`{}`), CreatedAt: time.Now().UTC()}); err != nil {
		t.Fatalf("CreateTicket(other): %v", err)
	}
	if _, err := m.AnswerTicket(ctx, "t1", "tk-other", "x"); !errors.Is(err, store.ErrNotFound) {
		t.Fatalf("跨任务工单 err=%v，want ErrNotFound", err)
	}
}

// TestFacadeResumeIfIdleOnlyWhenNoPending 锁契约 §5.2 #11/#12：
// 非 waiting_answer 不动；有待办不动；无待办才回迁 running。
func TestFacadeResumeIfIdleOnlyWhenNoPending(t *testing.T) {
	m, st, _, _ := newTestManager(t)
	ctx := context.Background()
	now := time.Now().UTC()
	if err := st.CreateTask(&proto.Task{ID: "t-run", RepoPath: "/r", Executor: "fake",
		State: proto.TaskStateRunning, CreatedAt: now, UpdatedAt: now}); err != nil {
		t.Fatalf("CreateTask(t-run): %v", err)
	}
	seedWaitingAnswerTask(t, st, "t-pending", "tk-p")
	seedWaitingAnswerTask(t, st, "t-idle", "")

	m.ResumeIfIdle(ctx, "t-run")
	if task, _ := st.GetTask("t-run"); task.State != proto.TaskStateRunning {
		t.Fatalf("running 任务被误改: %s", task.State)
	}
	m.ResumeIfIdle(ctx, "t-pending")
	if task, _ := st.GetTask("t-pending"); task.State != proto.TaskStateWaitingAnswer {
		t.Fatalf("仍有待办却回迁了: %s", task.State)
	}
	m.ResumeIfIdle(ctx, "t-idle")
	if task, _ := st.GetTask("t-idle"); task.State != proto.TaskStateRunning {
		t.Fatalf("无待办未回迁: %s", task.State)
	}
}

// TestFacadeResumeIfIdleConcurrentSingleTransition 锁契约 §5.2 #12 的 CAS 收敛：
// 两个并发回迁，最终恰到达 running，无 panic/死锁（唯一赢家由 store CAS 承担）。
func TestFacadeResumeIfIdleConcurrentSingleTransition(t *testing.T) {
	m, st, _, _ := newTestManager(t)
	seedWaitingAnswerTask(t, st, "t-race", "")
	var wg sync.WaitGroup
	for i := 0; i < 2; i++ {
		wg.Add(1)
		go func() {
			defer wg.Done()
			m.ResumeIfIdle(context.Background(), "t-race")
		}()
	}
	wg.Wait()
	if task, _ := st.GetTask("t-race"); task.State != proto.TaskStateRunning {
		t.Fatalf("并发回迁最终态 = %s，want running", task.State)
	}
}
```

2. **跑红。** `go test ./internal/orchestration/ -run 'TestFacadeAnswerTicket|TestFacadeResumeIfIdle' -count=1`。预期：`AnswerTicket` 返回 `ErrTicketAnswerUnwired`、`ResumeIfIdle` 空实现 → 断言失败。贴原始报错进台账。此红即本 task 的「红」。

3. **实现 `AnswerTicket`。** 把 `facade.go:96-107` 替换为：

```go
// AnswerTicket 在编排侧收口「应答一张工单」：工单归属校验、AnswerTicketApplied
// 落库（answer IS NULL 幂等守卫）、ticket_answered 事件。
//
// 参数：
//   - ctx: 预留上下文；本方法为同进程同库同步调用，不发起网络
//   - taskID: 工单必须归属的任务（跨任务按不存在处理，不泄露信息）
//   - ticketID / answer: 工单与应答原文
//
// 返回：
//   - applied=true：本次真正从 NULL 写入新答案（且已追加 ticket_answered 事件）
//   - applied=false, err=nil：同值重答（幂等），调用方按 200 + idempotent 应答
//   - store.ErrTicketConflict / store.ErrNotFound：原样透出（不许重包装，会断 errors.Is）
//
// 为什么只 AppendEvent 不 hub.Publish：与现状 gateway.handleReply 及编排内
// approvePermission 逐字一致；唤醒 executor 由 gateway 侧 hub.NotifyAnswer 承担
// （contract C-3）。事件只落库，attach/wait 从事件表读。
func (m *Manager) AnswerTicket(ctx context.Context, taskID, ticketID, answer string) (applied bool, err error) {
	m.log.Info("工单应答门面进入", "task", taskID, "ticket", ticketID)
	tk, err := m.st.GetTicket(ticketID)
	if err != nil {
		return false, err // store.ErrNotFound 原样透出
	}
	if tk.TaskID != taskID {
		// 跨任务工单按不存在处理，不泄露（与现状 handleReply tk.TaskID != taskID → 404 同款）
		return false, store.ErrNotFound
	}
	applied, err = m.st.AnswerTicketApplied(ticketID, answer)
	if err != nil {
		return false, err // ErrTicketConflict / ErrNotFound 原样透出
	}
	if !applied {
		m.log.Info("工单应答幂等（同值重答）", "task", taskID, "ticket", ticketID)
		return false, nil
	}
	if _, aerr := m.st.AppendEvent(taskID, proto.EventTypeTicketAnswered,
		TicketAnsweredPayload{TicketID: ticketID, Answer: answer}); aerr != nil {
		// 与现状一致：事件追加失败只 Warn，不阻断应答成功
		m.log.Warn("追加工单答复事件失败", "task", taskID, "ticket", ticketID, "cause", aerr)
	}
	m.log.Info("工单应答完成", "task", taskID, "ticket", ticketID)
	return true, nil
}
```

4. **实现 `ResumeIfIdle`。** 把 `facade.go:109-115` 替换为：

```go
// ResumeIfIdle 在编排侧收口「无待办则回迁 running」：任务处于 waiting_answer 且
// PendingTickets 为空时，经 store.UpdateTaskState 的 CAS 迁移到 running；CAS 撞
// ErrBadTransit（并发赢家已迁移）时有界重读重试（3 次），重试耗尽只 Warn。
//
// 为什么有界重试：两个工单被并发回答时，两个请求都可能读到「已无工单」，先执行者
// 成功、后执行者收到 ErrBadTransit；重试让意图在最新快照上重新评估（contract §4）。
// 无返回值：与现状 gateway.resumeIfIdle 一致——回迁失败只记日志，不影响 reply 成功。
func (m *Manager) ResumeIfIdle(ctx context.Context, taskID string) {
	for attempt := 0; attempt < 3; attempt++ {
		task, err := m.st.GetTask(taskID)
		if err != nil {
			m.log.Error("reply 后读取任务失败", "task", taskID, "cause", err)
			return
		}
		if task.State != proto.TaskStateWaitingAnswer {
			return // 任务已不在等待应答，无需回迁
		}
		pending, err := m.st.PendingTickets(taskID)
		if err != nil {
			m.log.Error("reply 后查询待办工单失败", "task", taskID, "cause", err)
			return
		}
		if len(pending) > 0 {
			return // 仍有未答工单，任务保持 waiting_answer
		}
		if err := m.st.UpdateTaskState(taskID, proto.TaskStateRunning); err == nil {
			m.log.Info("reply 后任务回迁 running", "task", taskID)
			return
		} else if !errors.Is(err, store.ErrBadTransit) {
			m.log.Error("reply 后恢复任务运行失败", "task", taskID, "cause", err)
			return
		}
		// ErrBadTransit：状态被并发变更（如另一 reply 已回迁），重读最新快照重试
	}
	m.log.Warn("reply 后恢复任务运行重试耗尽", "task", taskID)
}
```

5. **删哨兵。** 删除 `facade.go:26-30` 的 `ErrTicketAnswerUnwired` 声明及其注释。**不动 import**：`"errors"` 仍被 W1 第 4 步 `ResumeIfIdle` 的 `errors.Is` 使用（`"context"` 亦然）。以 `go build ./...` 报错为准，若报 unused import 才删对应项。

6. **跑绿。** `go test ./internal/orchestration/ -run 'TestFacadeAnswerTicket|TestFacadeResumeIfIdle' -count=1`，预期 `ok`。

7. **加日志与注释。** 每处错误分支带 `task`/`ticket`/`cause` 上下文（已在上文代码给出），入口带输入；成功路径不静默。导出函数注释已写参数/返回/注意事项与「为什么」。

**判据**

- `TestFacadeAnswerTicketAnswersAndEmitsEvent`、`TestFacadeResumeIfIdleOnlyWhenNoPending`、`TestFacadeResumeIfIdleConcurrentSingleTransition` 全绿。
- 变异自检（可变红）：把 `AnswerTicket` 的 `tk.TaskID != taskID` 分支删掉 → 跨任务断言必红；把 `ResumeIfIdle` 的 `len(pending) > 0` 分支删掉 → 有待办断言必红；把事件 payload 换成 `map[string]string` → 线格式断言必红。
- `grep -rn 'ErrTicketAnswerUnwired' --include='*.go' .` 零命中。
- `go build ./...` 退出码 0。

---

### W2：`handleReply` 收敛 + 删 `resumeIfIdle`（红→绿）

**文件与接口**

- 生产：`internal/agentd/handlers.go`。
- 测试：`internal/agentd/server_test.go`（`package agentd_test`）新增 `TestHandleReplyDelegatesToFacade`；`newTestEnvWithCfg` 默认注入真 Manager。
- Consumes：W1 的 `Manager.AnswerTicket`/`ResumeIfIdle`；`OrchestrationClient` 接口。
- Produces：`handleReply` 只经 `s.mgr`；`Server.resumeIfIdle` 删除；HTTP 响应逐字不变。

**步骤**

1. **写失败测试（红）。** 在 `internal/agentd/server_test.go` 追加（文件已 import `context`/`orchestration`/`workspace`/`config`/`fake`/`io`/`slog`）：

```go
// recordingOrchClient 借嵌入接口获得完整方法集，覆盖 reply 路径关心的方法与
// byTask 中间件会调用的查询方法（reply 路由被 byTask 包住）。
type recordingOrchClient struct {
	orchestration.OrchestrationClient
	answerCalls int
	resumeCalls int
	applied     bool
	answerErr   error
}

func (c *recordingOrchClient) GetTask(string) (*proto.Task, error) { return nil, store.ErrNotFound }
func (c *recordingOrchClient) MirrorTaskTarget(string) (string, bool, error) {
	return "", false, nil
}
func (c *recordingOrchClient) AnswerTicket(_ context.Context, _, _, _ string) (bool, error) {
	c.answerCalls++
	return c.applied, c.answerErr
}
func (c *recordingOrchClient) ResumeIfIdle(context.Context, string) { c.resumeCalls++ }
func (c *recordingOrchClient) RelayAnswer(string, string, string) error { return nil }
func (c *recordingOrchClient) NoteDeliveryFailed(string, string, error) {}

// TestHandleReplyDelegatesToFacade 锁 S1：reply 只打编排门面，不再自己
// GetTicket/AnswerTicketApplied/AppendEvent/CAS 回迁。fake client 未落库，
// 若 handler 回退直打 s.st，answerCalls 恒 0 且回报 404 → 本测红。
func TestHandleReplyDelegatesToFacade(t *testing.T) {
	env := newTestEnvWithCfg(t, &config.Config{Token: testToken,
		Executor: config.ExecutorConfig{Default: "fake"}},
		slog.New(slog.NewTextHandler(io.Discard, nil)))
	fakeMgr := &recordingOrchClient{applied: true}
	env.srv.SetManager(fakeMgr)

	resp := env.post(t, "/api/tasks/t1/reply", `{"ticket_id":"tk1","answer":"允许"}`)
	defer resp.Body.Close()
	if resp.StatusCode != http.StatusOK {
		t.Fatalf("reply 返回 %d, want 200", resp.StatusCode)
	}
	if fakeMgr.answerCalls != 1 {
		t.Fatalf("门面 AnswerTicket 调用次数=%d, want 1", fakeMgr.answerCalls)
	}
	if fakeMgr.resumeCalls != 1 {
		t.Fatalf("门面 ResumeIfIdle 调用次数=%d, want 1", fakeMgr.resumeCalls)
	}
}
```

> `TestHandleReplyDelegatesToFacade` 用 `recordingOrchClient`（记录型 fake）**只锁调用归属**，不锁响应形状——响应形状由既有真 Manager 用例（`TestReplyAnswersTicketAndNotifies` 等）锁。`store` 与 `proto` 已在 `server_test.go` import；`context`/`config`/`io`/`slog`/`orchestration` 已在。

2. **跑红。** `go test ./internal/agentd/ -run 'TestHandleReplyDelegatesToFacade' -count=1`。预期：`recordingOrchClient` 未定义或 `answerCalls=0`（handler 未走门面）。贴原始报错进台账。

3. **改 `newTestEnvWithCfg` 注入真 Manager。** `internal/agentd/server_test.go:66-75`，在 `srv := agentd.NewServer(...)` 之后、`ts := testhttp.NewServer(...)` 之前插入：

```go
	// B233.28 P-1-A：任务/项目读路径切 s.mgr 后，未注入 manager 的读接口会 503。
	// 生产恒在 Serve 前 SetManager（cmd/agentd.go），故测试夹具默认注入真 Manager；
	// 需要 manager 缺席语义的用例（登记 503 等）用 newTestAgentdEnv 或自行覆盖。
	srvMgr := orchestration.NewManager(st, srv.Hub(),
		map[string]executor.Adapter{"fake": fake.New(nil)},
		&config.Config{Token: cfg.Token, DataDir: t.TempDir(), Executor: config.ExecutorConfig{Default: "fake"}},
		nil, nil, newTestGate(t), slog.New(slog.NewTextHandler(io.Discard, nil)))
	srvMgr.SetWorkspace(workspace.NewCapability())
	srv.SetManager(srvMgr)
```

   `newTestGate` 在 `integration_test.go`（同 `agentd_test` 包）已存在。不改 `newTestEnvWithLogger`（它转调 `newTestEnvWithCfg`）。`status_test.go` / `TestDispatchEnvFailureReturns500WithCause` 会在此后自行 `SetManager` 覆盖，行为不变。

4. **改 `handleReply`（`internal/agentd/handlers.go:348-457`）。** 在函数体开头（`taskID := ...` 与日志之后）加未就绪守卫；把第 2 步「GetTicket + 归属校验」与第 3 步「AnswerTicketApplied + AppendEvent」替换为一次 `s.mgr.AnswerTicket` 调用；末尾 `s.resumeIfIdle(taskID)` 改 `s.mgr.ResumeIfIdle(r.Context(), taskID)`。响应分支逐字保留。改后函数体为：

```go
func (s *Server) handleReply(w http.ResponseWriter, r *http.Request) {
	taskID := r.PathValue("id")
	s.log.Info("reply 请求", "method", r.Method, "path", r.URL.Path, "task", taskID)

	if s.mgr == nil {
		s.log.Warn("reply 请求到达但 manager 未注入", "task", taskID)
		writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": "manager 未就绪"})
		return
	}

	var req replyRequest
	if err := json.NewDecoder(io.LimitReader(r.Body, 1<<20)).Decode(&req); err != nil {
		s.log.Warn("reply 请求体解析失败", "task", taskID, "err", err)
		writeJSON(w, http.StatusBadRequest, map[string]string{"error": "请求体必须是 JSON {ticket_id, answer}"})
		return
	}
	if req.TicketID == "" || req.Answer == "" {
		s.log.Warn("reply 缺少 ticket_id 或 answer", "task", taskID)
		writeJSON(w, http.StatusBadRequest, map[string]string{"error": "ticket_id 与 answer 不能为空"})
		return
	}

	// B233.28：归属校验 + 落库 + ticket_answered 事件收进编排门面；错误原样分流
	applied, err := s.mgr.AnswerTicket(r.Context(), taskID, req.TicketID, req.Answer)
	if err != nil {
		if errors.Is(err, store.ErrTicketConflict) {
			s.log.Warn("reply 工单冲突", "task", taskID, "ticket", req.TicketID, "cause", err)
			writeJSON(w, http.StatusConflict, map[string]string{"error": err.Error()})
			return
		}
		if errors.Is(err, store.ErrNotFound) {
			s.log.Warn("reply 目标工单不存在", "task", taskID, "ticket", req.TicketID)
			writeJSON(w, http.StatusNotFound, map[string]string{"error": "工单不存在"})
			return
		}
		s.log.Error("回答工单失败", "task", taskID, "ticket", req.TicketID, "cause", err)
		writeJSON(w, http.StatusInternalServerError, map[string]string{"error": "内部错误"})
		return
	}
	if !applied {
		s.log.Info("reply 幂等无需二次中继", "task", taskID, "ticket", req.TicketID, "idempotent", true)
		writeJSON(w, http.StatusOK, map[string]any{"ok": true, "relayed": true, "idempotent": true})
		return
	}

	// 唤醒阻塞在该 ticket 上的 WaitAnswer 调用者；无人等待时 RelayAnswer 自愈中继
	// （why 见函数头）。relayed=false 即「回答已落库但 executor 侧递送失败」
	relayed := true
	reason := ""
	if !s.hub.NotifyAnswer(req.TicketID, req.Answer) {
		if err := s.mgr.RelayAnswer(taskID, req.TicketID, req.Answer); err != nil {
			relayed = false
			reason = truncateRunes(err.Error(), 200)
			s.log.Error("reply 自愈中继失败", "task", taskID, "ticket", req.TicketID, "cause", err)
		}
	}
	s.log.Info("reply 完成", "task", taskID, "ticket", req.TicketID,
		"answer", truncateRunes(req.Answer, 80), "relayed", relayed)

	if !relayed {
		// 中继失败：回答已落库但 executor 未收到（why 与状态处理见函数头）
		s.mgr.NoteDeliveryFailed(taskID, req.TicketID, errors.New(reason))
		writeJSON(w, http.StatusBadGateway, replyResult{OK: true, Relayed: false, Reason: reason})
		return
	}

	// 应答已落库与唤醒完成，无其余未答工单时回迁 running；回迁规则（何时 running、
	// CAS 重试）归编排侧，gateway 不再持一份 CAS 循环（contract §5.2 #15）
	s.mgr.ResumeIfIdle(r.Context(), taskID)

	writeJSON(w, http.StatusOK, replyResult{OK: true, Relayed: true})
}
```

   > 顶部 `s.mgr == nil` 守卫使旧代码里「无等待者且 manager 未注入 → 502 + reason」分支不可达（旧分支存在只因应答走 `s.st`）；收敛后 mgr 缺席即 503 fail-closed。原函数头注释（流程/502 语义的 why）保留；`handleReply` 的 502 五分支语义在 manager 已注入的生产态逐字不变（contract §5.2 #14）。删掉函数内旧的三段 store 直打与旧 `resumeIfIdle` 调用。

5. **删 `Server.resumeIfIdle`（`internal/agentd/handlers.go:459-490`）。**
   `grep -rn 'resumeIfIdle' internal/agentd --include='*.go'` 复核：除本函数外无生产引用。

6. **跑绿。** `go test ./internal/agentd/ -run 'Reply|HandleReply|B23313' -count=1`，预期 `ok`。特别关注既有 `TestReplyAnswersTicketAndNotifies`、`TestReplyUnknownTicket404`、`TestReplySelfHealsWithoutWaiter`、`TestReplyRelayFailureReturns502`、`TestHandleReplyIdempotentDoesNotRelayTwice`、`TestB23313SetManagerRejectsTypedNil`。

7. **加日志与注释。** 守卫用 `Warn` + `task`；门面错误分流保留原 `Warn`/`Error` 级别与文案；`ResumeIfIdle` 的日志在 W1 已加。

**判据**

- `TestHandleReplyDelegatesToFacade` 绿。
- 既有 reply 六测全绿（响应体/状态码逐字：200 `{"ok":true,"relayed":true}`、幂等 200 `…"idempotent":true`、502 `{"ok":true,"relayed":false,"reason":…}` + `delivery_failed`）。
- 变异自检：把 `handleReply` 改回 `s.st.AnswerTicketApplied` → `TestHandleReplyDelegatesToFacade` 必红（`answerCalls=0` 或 404）。
- `go build ./...` 退出码 0。

---

### W3：任务/项目读路径切 `s.mgr` + 受影响夹具注入（红→绿）

**文件与接口**

- 生产：`handlers.go`、`tasksfanout.go`、`taskroute.go`、`taskplan.go`、`roomsapi.go`、`update.go`、`projectadmin.go`、`codegraph.go`、`coordapi.go`、`workspacefiles.go`。
- 测试：新增 `internal/agentd/b23328_source_guard_test.go`；改受影响夹具与用例（见 3.3 清单）。
- Consumes：`OrchestrationClient` 的 `ListTasks`/`GetTask`/`PendingTickets`/`EventsFrom`/`EventsFromAsc`/`LatestEvent`/`CountEvents`/`ListMirrorTasks`/`MirrorTaskTarget`/`MirrorEventsFrom`/`ListProjectLocations`/`GetProjectLocationByName`/`UpdateProjectLocation`。
- Produces：gateway 读路径只经 `s.mgr`；`s.mgr==nil` 503 + Warn。

#### 3.1 逐文件调用点切换（精确锚）

1. `internal/agentd/handlers.go`
   - `handleListTasks`（`s.st.ListTasks:203` → `s.mgr.ListTasks()`）：函数体开头加未就绪守卫（见下模板）。
   - `handleGetTask`（`:254` `GetTask`、`:265` `PendingTickets`、`:271` `EventsFrom`）：加守卫，三处改 `s.mgr.*`。
   - `taskOrErr`（`:1119`）：`s.st.GetTask` → `s.mgr.GetTask`；加守卫（返回 `nil,false` + 503）。
   - `handleEvents`（`:1536` `GetTask`、`:1540` `MirrorTaskTarget`、`:1639` `MirrorEventsFrom`、`:1641` `EventsFromAsc`、`:1658` `LatestEvent`、`:1740` `CountEvents`）：在 `from_seq` 解析之后、`websocket.Accept` 之前加守卫；六处改 `s.mgr.*`。
     - 注意：`mirrored` 分支用 `s.mgr.GetTask`/`s.mgr.MirrorTaskTarget`；重放/截断诊断用 `s.mgr.MirrorEventsFrom`/`s.mgr.EventsFromAsc`/`s.mgr.LatestEvent`/`s.mgr.CountEvents`。
   - `handleDone`（`:933`、`:939`、`:956`）：已有 mgr 守卫（`:907`）；三处 `s.st.GetTask` → `s.mgr.GetTask`（它们是 contract §1.1 列的 GetTask 直调点 `:933,:939,:956`，属收口面，必须切）。
   - `handleStop`（`:988`、`:1005`）：已有 mgr 守卫（`:983`）；两处 `s.st.GetTask` → `s.mgr.GetTask`（contract §1.1 `:988,:1005`）。
   - `handleResume`（`:1120`，即 `taskOrErr` 内）：见上 `<taskOrErr>` 条。
   - 逐条依据：contract §1.1 的 gateway 现状调用点列出 `handlers.go` 的 `s.st.GetTask` `:254,:933,:939,:956,:988,:1005,:1120,:1536`，§10 明文「§1.1 / §1.3 列的 `s.st.*` 全部改用 `s.mgr.*`」。故本卡收口面内 handlers.go 的**全部** `s.st.GetTask` 都要切，`handleDone`/`handleStop`/`handleResume` 不例外。
   - **不切**：§1.1/§1.3 未列的 `s.st.*`——`server.go` 的 `s.st.SetEventHook:323`（assembly 装配点）、登录/工作台/auth 的 `s.st.Session*`/`AuthTicket*`/`Workbench*`（contract §1.4）、以及 `s.st.UpdateTaskState`（`:477` 在 `resumeIfIdle` 内，随 W2 删函数一并消失）。

```go
// 未就绪守卫模板（各 handler 函数体开头，先于任何 s.mgr 调用）：
	if s.mgr == nil {
		s.log.Warn("任务列表请求到达但 manager 未注入", "remote_addr", r.RemoteAddr)
		writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": "manager 未就绪"})
		return
	}
```

2. `internal/agentd/tasksfanout.go`：`tasksAll` 的 `s.st.ListTasks:28` → `s.mgr.ListTasks()`、`s.st.ListMirrorTasks:40` → `s.mgr.ListMirrorTasks()`。`tasksAll` 仅由 `handleListTasks` 的 `scope=all` 分支调用，守卫已在 `handleListTasks` 顶部，故此处不再重复守卫。

3. `internal/agentd/taskroute.go`：`byTask` 中间件在 `id == "" || isForwarded(r)` 短路之后加守卫；`s.st.GetTask:34` → `s.mgr.GetTask(id)`、`s.st.MirrorTaskTarget:42` → `s.mgr.MirrorTaskTarget(id)`。

```go
		if s.mgr == nil {
			s.log.Warn("任务路由：manager 未注入", "task", id)
			writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": "manager 未就绪"})
			return
		}
```

4. `internal/agentd/taskplan.go`：`handleTaskPlan` 加守卫；`s.st.GetTask:43` → `s.mgr.GetTask(taskID)`。

5. `internal/agentd/roomsapi.go`
   - `enrichRoomAttachments`（`:109` `s.st.ListTasks`）：加 `if s.mgr == nil { return }`（房间列表是只读降级，不阻断主响应）；改 `s.mgr.ListTasks()`。
   - `handleInbox`（`:403` `ListTasks`、`:413` `PendingTickets`、`:459` `EventsFrom`）：函数体开头加守卫 → `writeErr(w, http.StatusServiceUnavailable, errors.New("manager 未就绪"))`；三处改 `s.mgr.*`。
   - `ticketDestructive`（`:459` 附近的 `s.st.EventsFrom`）：改 `s.mgr.EventsFrom`；`s.mgr==nil` 时返回 `false`（保守不标破坏性，与「读不出就保守」一致）。

6. `internal/agentd/update.go`：`activeCount` 的 `s.st.ListTasks:289` → `s.mgr.ListTasks()`；开头加：

```go
	if s.mgr == nil {
		return 0, fmt.Errorf("manager 未就绪")
	}
```

   （`update.go` 已 import `fmt`，不必新增 import。`handleUpdate` 在闸一分支把错误映射为 500——这是「未就绪不能假装没有活跃任务」的 fail-closed 处置。）

7. `internal/agentd/projectadmin.go`
   - `handleProjectPatch`（已有 mgr 守卫 `:65`）：`s.st.GetProjectLocationByName:87` → `s.mgr.GetProjectLocationByName(name)`、`s.st.UpdateProjectLocation:136` → `s.mgr.UpdateProjectLocation(name, newName, newPath)`。
   - `handleProjectBranches`（`:166`）：函数体开头加守卫；改 `s.mgr.GetProjectLocationByName(name)`。
   - `handleProjectWorktreeCreate`（`:246`）：把已有 mgr 守卫（当前在 `GetProjectLocationByName` 之后）上移到 `name := r.PathValue("name")` 之后；改 `s.mgr.GetProjectLocationByName(name)`。

8. `internal/agentd/codegraph.go`
   - `handleProjectCodegraph`（`:33`）与 `handleProjectCodegraphSource`（`:144`）：各加守卫；改 `s.mgr.GetProjectLocationByName(name)`。
   - 两处错误分流保留：`errors.Is(err, store.ErrNotFound)` → 404「项目 X 未登记」。

9. `internal/agentd/coordapi.go`：`resolveCoordWorkdir` 的 `s.st.ListProjectLocations:264` → `s.mgr.ListProjectLocations()`；开头加 `if s.mgr == nil { return "" }`（best-effort 解析，缺 manager 只留空）。

10. `internal/agentd/workspacefiles.go`：`resolveWorkspace` 的 `s.st.ListProjectLocations:101` → `s.mgr.ListProjectLocations()`；在读位置表处加：

```go
	if s.mgr == nil {
		// 闸门坏了要关上：读不出位置表时拒绝而不是放行
		s.log.Error("工作树白名单：manager 未注入，按拒绝处理")
		return "", false
	}
```

   > 草稿区短路径（`scratchRoot()`）仍在最前，不依赖 manager。

#### 3.2 新增静态面断言（内部锁，见 §2.3）

新建 `internal/agentd/b23328_source_guard_test.go`：

```go
// b23328_source_guard_test.go —— B233.28 的包级静态面断言（内部锁，附属于 S1/S2）。
//
// 职责：钉住「任务/项目收口面涉及的 gateway 生产文件不再出现 s.st.<方法> 选择器」。
// 边界：只扫 §3.1 列出的 10 个生产文件；不扫登录/工作台/auth（本卡不动）、不扫
// server.go（assembly 装配点，含 s.st.SetEventHook 等既有插线）。
//
// 为什么是内部锁而非缝级：从任何声明缝（HTTP/handler 入口）构造不出「整包不再直打
// store」这条静态属性——运行时用例可能恰巧走别的分支而漏掉某处直打。
package agentd

import (
	"go/ast"
	"go/parser"
	"go/token"
	"path/filepath"
	"runtime"
	"testing"
)

func TestB23328GatewayNoDirectStoreTaskProjectCalls(t *testing.T) {
	files := []string{
		"handlers.go", "tasksfanout.go", "taskroute.go", "taskplan.go",
		"roomsapi.go", "update.go", "projectadmin.go", "codegraph.go",
		"coordapi.go", "workspacefiles.go",
	}
	// 本卡收口面：这些方法在 gateway 生产文件里必须只经 s.mgr
	closure := map[string]bool{
		"ListTasks": true, "GetTask": true, "PendingTickets": true,
		"EventsFrom": true, "EventsFromAsc": true, "LatestEvent": true,
		"CountEvents": true, "ListMirrorTasks": true, "MirrorTaskTarget": true,
		"MirrorEventsFrom": true, "GetTicket": true, "AnswerTicketApplied": true,
		"AppendEvent": true, "UpdateTaskState": true,
		"GetProjectLocationByName": true, "UpdateProjectLocation": true,
		"ListProjectLocations": true,
	}
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	dir := filepath.Dir(thisFile)
	fset := token.NewFileSet()
	found := 0
	for _, f := range files {
		path := filepath.Join(dir, f)
		af, err := parser.ParseFile(fset, path, nil, 0)
		if err != nil {
			t.Fatalf("解析 %s: %v", f, err)
		}
		ast.Inspect(af, func(n ast.Node) bool {
			sel, ok := n.(*ast.SelectorExpr)
			if !ok {
				return true
			}
			// 只匹配形如 s.st.Method 的选择器：X 必须是 s.st
			inner, ok := sel.X.(*ast.SelectorExpr)
			if !ok {
				return true
			}
			id, ok := inner.X.(*ast.Ident)
			if !ok || id.Name != "s" || inner.Sel.Name != "st" {
				return true
			}
			if closure[sel.Sel.Name] {
				found++
				t.Errorf("%s:%d 生产路径仍直打 store: s.st.%s（应经 s.mgr）",
					f, fset.Position(sel.Pos()).Line, sel.Sel.Name)
			}
			return true
		})
	}
	if found != 0 {
		t.Fatalf("本卡收口面仍有 %d 处 s.st 直打", found)
	}
}
```

#### 3.3 受影响测试夹具/用例注入清单（P-1-A 精确清单）

**package `agentd`（白盒）**：在 `internal/agentd/b23313_gateway_testhelpers_test.go` 追加助手：

```go
// ensureTestManager 给默认未注入 manager 的白盒测试环境挂上真实编排实现。
//
// B233.28 P-1-A：任务/项目读路径与 byTask 中间件改经 s.mgr 后，未注入 manager 的
// 既有用例会 503。**写路径用例（登记/派发等）需要 manager 缺席的 503 语义，故不
// 改 newTestAgentdEnv 默认值**，受影响用例显式调用本助手。
//
// ads 传 nil：读路径不需要 adapter；写路径用例另有自己的 manager 装配。
func ensureTestManager(t *testing.T, env *testAgentdEnv) *testAgentdEnv {
	t.Helper()
	env.mgr = newManagerForServer(t, env.srv, nil)
	return env
}
```

> 本助手不新增 import：转调同文件已有的 `newManagerForServer(t, srv, nil)`。

逐文件逐用例（每处加一行 `ensureTestManager(t, env)`，或对自建 Server 用 `newManagerForServer(t, srv, …)`）：

| 文件 | 改动点 |
| --- | --- |
| `internal/agentd/bundle_handler_test.go` | `newBundleEnv`：`env = newTestAgentdEnvWithCfg(...)` 后加 `ensureTestManager(t, env)`（`handleTaskBundle` 经 `taskOrErr` 读任务） |
| `internal/agentd/b23319_retained_test.go` | `TestTaskListAnnotatesProjectID`：`env := newTestAgentdEnv(t)` 后加 `ensureTestManager(t, env)` |
| `internal/agentd/codegraph_test.go` | `TestCodegraphEndpoint`、`TestCodegraphDeclsWireConditions`（子测试内 env）、`TestCodegraphComparisonDataOptional`、`TestCodegraphComparisonDataTargetFailure`、`TestCodegraphSource`：各自 env 后加 `ensureTestManager(t, env)` |
| `internal/agentd/forward_relay_test.go` | `newRelayForwardEnv`：`newTestAgentdEnvWithCfg(...)` 后加 `ensureTestManager(t, env)` |
| `internal/agentd/frames_stream_test.go` | `newTestServerWithTask`：`env := newTestAgentdEnvWithCfg(...)` 后加 `ensureTestManager(t, env)`（惠及 `workspace_run_http_test.go` 的 `TestTaskRunMissingWorkdirReturns400`） |
| `internal/agentd/mirrorws_test.go` | `TestMirrorWSReplaysAndLive`：env 后加 `ensureTestManager(t, env)` |
| `internal/agentd/render_stream_test.go` | `newRenderServer`：`s := NewServer(...)` 后、`ts := testhttp.NewServer(...)` 前加 `newManagerForServer(t, s, nil)`（`handleTaskRender` 经 `taskRepoOrErr` 读任务；该助手直接建 Server，无 `testAgentdEnv`） |
| `internal/agentd/taskplan_test.go` | `seedPlanTask`：函数体首行加 `ensureTestManager(t, env)`（env 为参数） |
| `internal/agentd/taskroute_test.go` | `TestTaskRouteForwardsViaMirrorIndex`（remote、local 两个 env）、`TestTaskRoute404WhenNowhere`（local）、`TestTaskRouteNeverForwardsWhenForwarded`（remote、local） |
| `internal/agentd/tasksfanout_test.go` | `seedTasksEnv`：env 后加 `ensureTestManager(t, env)` |
| `internal/agentd/diffbase_test.go` | `TestBranchesEndpointReportsTaskBase`：`srv := NewServer(...)` 后加 `newManagerForServer(t, srv, map[string]executor.Adapter{"fake": fake.New(nil)})`（其余 diff 用例已 `SetManager`，不动） |
| `internal/agentd/update_test.go` | `newUpdateServer`：`srv.SetRestart(...)` 后加 `newManagerForServer(t, srv, nil)` |
| `internal/agentd/hostguard_test.go` | `newHostTestEnv`：`ts := testhttp.NewServer(...)` 后加 `newManagerForServer(t, srv, nil)`（惠及 `auth_test.go` 的 `/api/tasks` 与 `/ws/events` 用例） |
| `internal/agentd/reveal_test.go` | `newRevealEnv`：`env := newTestAgentdEnv(t)` 后加 `ensureTestManager(t, env)` |
| `internal/agentd/ws_regression_round2_test.go` | 夹具内 `env.srv = NewServer(...)` 后加 `newManagerForServer(t, env.srv, nil)`（在 `env.ts` 建立前） |

> 助手落点：`ensureTestManager` 定义在 `b23313_gateway_testhelpers_test.go`，转调同文件已有的 `newManagerForServer`，**不新增 import**。`newManagerForServer` 的 `ads` 传 `nil` 即可（读路径不需要 adapter）。

**不改**：`forward_test.go`（远程 env 必须 manager 缺席以验 503）、`env_test.go`/`executor_default_test.go`（未就绪 503 断言）、`projecttree_test.go`（C-2 注入面未动，`/api/projects/tree` handler 不经 `s.mgr`）、`workspacefiles_test.go` 的 `TestScratchRootWhitelisted`/`TestScratchEntryRoundTrip`（走草稿区短路或纯包函数，wsFilesFixture 已注入 manager）、`integration_test.go`/`status_test.go`/`receiver_*`/`wakeconsumer_test`/`coordapi_test`/`ledgerapi_test`（已注入 manager）、`preview_raw_ws_test.go`/`forward_ws_test.go`/pty 系列（不触任务读收口面）。

**package `agentd_test`（黑盒）**：`newTestEnvWithCfg` 已在 W2 步骤 3 注入真 Manager，`server_test.go` 全部读/WS 用例与 `status_test.go`、`auth_test.go` 等一并受益；无需逐用例改。

#### 3.4 步骤

1. **写失败测试（红）。** 先加 `b23328_source_guard_test.go`（3.2）。跑 `go test ./internal/agentd/ -run 'TestB23328GatewayNoDirectStoreTaskProjectCalls' -count=1` → 预期红（列出当前 `s.st.*` 直打行）。贴原始报错进台账。
2. **切调用点。** 按 3.1 逐文件替换 + 加守卫。
3. **补夹具注入。** 按 3.3 清单加助手与逐用例调用。
4. **跑红→绿。** `go test ./internal/agentd/ -run 'TestB23328GatewayNoDirectStoreTaskProjectCalls' -count=1` → 预期绿（收口面 0 命中）。
5. **跑受影响面。** `go test ./internal/agentd/ -count=1` → 预期 `ok`。
6. **加日志与注释。** 每个新增守卫带 `Warn` + 入口标识；`handleEvents` 守卫注释说明「Accept 前拒绝，避免升级后再关」；`resolveWorkspace` 守卫注释「闸门坏了要关上」；`activeCount` 注释「未就绪不假装无活跃任务」。

**判据**

- 静态面断言绿（收口面 0 处 `s.st.<closure>`）。
- `go test ./internal/agentd/ -count=1` 绿（含 `TestAuthRequired`、`TestAttachEmptyListsAreArrays`、`TestCodegraphEndpoint`、`TestTaskPlan*`、`TestTasks*`、`TestTaskRoute*`、`TestMirrorWSReplaysAndLive`、`TestInboxThreeSources`、`TestUpdate*`、`TestReveal*`）。
- `go vet ./internal/agentd/` 退出码 0。
- 变异自检：把任一读方法改回 `s.st.ListTasks` → 静态断言必红；把 fixtures 注入删掉 → 对应用例 503 必红。

---

### W4：行为复跑与图对账

**步骤**

1. `go build ./...` → 退出码 0。
2. `gofmt -l $(git ls-files '*.go')` → 无输出。
3. `go vet ./internal/agentd/ ./internal/orchestration/ ./cmd/...` → 退出码 0。
4. `go test ./internal/orchestration/ ./internal/agentd/ ./cmd/... -count=1` → 三个 `ok`。
5. `codegraph check --view cards/B233.28-charter --repo .` → `fails: []`，`d_gateway->d_orchestration` ≤ 76。贴原始输出。
6. `codegraph validate --view cards/B233.28-charter --repo .` → 0 issue。
7. `grep -rn 'ErrTicketAnswerUnwired\|resumeIfIdle' --include='*.go' .` → 生产零命中（`resumeIfIdle` 仅可能出现在注释；若注释残留一并清理）。
8. 台账追加每步命令与原始输出。

**判据**

- `fails=[]` 且命中 ≤ 76（P-4-A）；三包全绿；静态检查全过。
- 若 `codegraph check` 因本卡实现新命中 > 76：**停，交协调者**（§5.3 #17 预算零改动；不得抬预算抹绿）。命中下降属后续重扫卡，不在本卡验收。

---

## §4 序列化边界设问

- **无新增数据字段/DTO/手写投影**：`AnswerTicket` 的 `bool`、`ResumeIfIdle` 的 void 是进程内结果；`ticket_answered` payload 形状与 JSON 键逐字不变（发出方从 gateway 搬到编排，同进程同库）。无 roundtrip 属性测试需求（无新字段）。
- **仍须跑既有穿过真实边界的回归，且不得删弱**：`internal/store/store_test.go` 的 `AnswerTicketApplied` 幂等/冲突用例、`internal/client/execution_test.go`/`follow_test.go` 的 `ticket_answered` 字面值、`internal/agentd/server_test.go` reply 全链。W1 新增的 `TestFacadeAnswerTicketAnswersAndEmitsEvent` 断言 `string(ev.Payload) == {"ticket_id":"tk1","answer":"答"}`，是穿过真实序列化边界（store AppendEvent → SQLite → EventsFrom）的回归。
- **可空类型区分「缺失」与「零值」**：`Ticket.Answer *string` 的 nil（未答）与空串是两件事，由 store 的 `answer IS NULL` 守卫承载；门面不得改判据（contract §4）。本次不新增该分辨的字段。

---

## §5 缺陷族对抗审查（逐族正面回答）

- **生命周期 / 状态机中断**：**无新增，因为**迁移不引入后台 goroutine/临时目录；`AnswerTicket` 同步落库+append，`ResumeIfIdle` 同步 CAS。宿主中途重启时，应答已落库（`answer` 非空）与状态均在 SQLite，重启走既有 `RecoverOnStartup`（waiting_answer 在探测范围）。**未验证，需真机**：真实重启窗口下 reply 的 502/恢复行为（breakdown §6.2）。
- **静默失败 / 误导报错**：**无新增窗口，因为**每一错误路径原样透出（`ErrTicketConflict`→409、`ErrNotFound`→404、其余→500）；`AppendEvent` 失败仍只 Warn 不阻断（与现状同）；门面禁止重包装错误。守住一条：`AnswerTicket` 返回 `(false,nil)` 的幂等分支必须仍走「200 + idempotent」早返回、不继续中继（`TestHandleReplyIdempotentDoesNotRelayTwice` 锁）。
- **跨平台假设**：**无，因为**纯同进程 Go 方法搬迁，不解析路径/进程组/权限模型，不碰 webview。CI 有 `GOOS=windows go vet ./...` 门；本卡新增测试只用标准库与已有夹具。
- **假红 / 假绿测试**：三条风险——①最关键：读路径切 `s.mgr` 后，未注入 manager 的既有测试若被改成「跳过断言」会假绿；P-1-A 要求注入真 Manager（真 store + 真 hub + fake adapter），并用静态面断言补覆盖，不得用只回固定值的假 client 抹平响应形状断言（唯一例外：`TestHandleReplyDelegatesToFacade` 用记录型 fake **只为锁调用归属**，响应形状仍由既有真 Manager 用例锁）。②静态面断言若扫注释/字符串会假红——本计划按 AST 只匹配 `s.st.<Method>` 选择器表达式。③变异用例（改回 `s.st`、删待办分支、改 payload）必须真能红，已逐条写进 W1/W2/W3 判据。
- **门禁绕过**：**无新增写路径，因为**不新增 HTTP 路由/CLI 命令；`reply` 的鉴权（Bearer + host 白名单）与 `byTask` 透明路由中间件原样保留。一处须核：`byTask` 包住全部 `/api/tasks/{id}/*` 路由，改数据源后鉴权/路由顺序不变（中间件仍在鉴权之后、handler 之前，本卡只换数据源）；`byTask` 只读判定，无 TOCTOU 新增。
- **枚举新值过既有白名单**：**无，因为**不新增 `TaskState`/`EventType`/`kind`/HTTP 路径/CLI 子命令；`running`/`waiting_answer` 与 `EventTypeTicketAnswered` 均既有值。

**承重安全属性有测试锁住**：①「工单不可重复回答」由 `AnswerTicketApplied` 的 `answer IS NULL` 守卫承载（`store_test.go` 已有可变红用例），门面不得改判据；②「回迁赢家唯一」由 `store.UpdateTaskState` CAS 承载，W1 并发用例锁「双回迁最终收敛 running」；③「跨任务工单不泄露」由 `AnswerTicket` 归属校验承载，W1 补跨任务用例。

---

## §6 上下文预算检查

- W1 有界：`facade.go` + `facade_test.go`（+ 既有 `newTestManager` 助手）。
- W2 有界：`handlers.go`（`handleReply`/`resumeIfIdle` 段）+ `server_test.go`。
- W3 有界：§3.1 的 10 个 handler 文件 + §3.3 列出的既有 `*_test.go` + 新静态断言文件。
- 圈不出文件集的情况不存在；无新文件除两个测试文件。

## §7 类型标注（边界型真机清单）

`d_gateway` 是 boundary 子系统，行为验收以§0/§3 的显式命令 + 期望值为准（机内可闭环）；跨机/重启/并发真实场景列 breakdown §6 五条，标注「未验证，需真机」，本计划不宣称通过。

---

## §8 占位符扫描（自审）

- 无 TBD / 「加适当的错误处理」/「同 Task N」/「参考上文」；每个改动点给出精确文件、精确锚、完整代码或逐字指令。
- 测试代码完整可编译（复用已存在助手：`newTestManager`、`newManagerForServer`、`newTestGate`、`newTestEnv`、`ensureTestManager` 为本计划新增）。
- 唯一以「逐用例加一行」替代完整测试函数的正当出口：§3.3 的既有测试**夹具注入**——断言逐条保留（响应形状不变），只需在既有用例前注入 manager；逐条清单已列全，不存在「另有若干未列出的用例」。
- 内部锁自我声明见 §2.3；退路同闸无触发。

## §9 自审三查

- **spec 覆盖**：用户故事 1（reply 落库/事件/唤醒/回迁）→ W1+W2；故事 2（列/读/工单/事件/镜像）→ W3；故事 3（项目位置读改）→ W3；故事 4（登录/工作台不变、图不动）→ 不改动 + §1.3 禁止项；故事 5（预算 ≤76 且 fails=[]）→ W4（P-4-A 口径）。契约 §5.1 #1–#7（接口形状）Ticket 0 已落、本卡不改；§5.2 #8–#16 → W1/W2/W3；§5.3 #17–#20 → W4；§5.4 #21–#23 → 不改动。
- **占位符扫描**：见 §8。
- **跨 task 类型/签名一致性**：W1 Produces 的 `AnswerTicket(ctx, taskID, ticketID, answer) (bool, error)` / `ResumeIfIdle(ctx, taskID)` 与 W2 Consumes（`s.mgr.AnswerTicket(r.Context(), taskID, req.TicketID, req.Answer)`、`s.mgr.ResumeIfIdle(r.Context(), taskID)`）逐字一致；与冻结的 `OrchestrationClient`（`orchestration_client.go:65-66`）一致。W3 消费的 13 个读方法签名逐字取自 `orchestration_client.go:55-73`。
- **收口前两查**：① 本计划所有「现状事实」均来自本节点亲自读源码/跑命令；未亲自跑到结果的（真机五条）已标「未验证」。② 本节点未碰 handoff CLI、未起 executor、未派发。
