# B233.25 plan：六族存量红逐族根因修复

前置事实（2026-09-13）：

- 六族清单见 spec。执行顺序建议：先做与图无关的五族（2–6），最后复跑 TestRepoContractGate
  （其主根因随 B233.23 收口）。

步骤：

1. 逐族复跑拿失败原文（`go test -p 1 -run '<名>' <包>`），记录完整报错。
2. 族 2（unix socket 路径）：定位测试里 socket 目录来源（临时目录前缀过长的拼接），改短
   （如 `os.MkdirTemp("", "hp")` 级别），断言不动。
3. 族 3/4/5/6：先读测试意图（锁的是什么契约），再分「测试假设过时 / 生产真 bug / 环境依赖」
   三类处置；前两类根因修复，第三类给证据与结论。
4. TestRepoContractGate：B233.23 之后复跑；仍红则读断言与 check 实况对齐缺口。
5. 全量验证：`go test -p 1 ./...`；结果原文进收尾报告。
6. 提交：`test(B233.25): 存量测试红根因修复——六族归零`（可按族拆提交，均带 B233.25 前缀）。

执行中偏差如实记入收尾段。
