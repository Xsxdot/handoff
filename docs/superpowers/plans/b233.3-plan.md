# B233.3 实现计划：能力 client 复用直连与 relay 传输

状态：执行计划  
卡：B233.3  
定级：L3 轻档（单轮 implement，禁止扇出子卡）  
法定产出：`docs/superpowers/plans/b233.3-plan.md`  
事实台账：`docs/superpowers/ledgers/2026-09-07-b233.3-plan-ledger.md`

**已由 plan docs/superpowers/plans/b233.3-plan.md 吸收（2026-09-07）**（契约 §9 移交 plan 附区）

拍板结果（breakdown §0，提交 `1eef56c7`）必须按字面执行，不得把未选方案写成事实：

| 岔口 | 本计划落地 |
|---|---|
| **P1(a)** | 不扇出子卡。内部 ticket T1–T6 是单卡执行顺序。 |
| **P2(a)** | 只收 `cmd/upgrade.go` 的 `newAgentdClient`：生产唯一 `client.NewRelay` 旁路改走 `targetclient.New`。不改 desktop / 本机 `client.New`。 |
| **P3(a)** | 过滤留在 `waitOnce` / `FollowEvents` / `backlog`。不改 `ExecutionClient` 的 `all` 参数。不在 WS/`streamOnce` 按类型丢帧。 |
| **P4(a)** | `forwardWorktreeIfRequested` 零账本写入；`handleProjectWorktreeCreate` 在转发成功后挂卡再写响应。HTTP `CardResults` 保持。Web 零改。 |
| **P5(a)** | 不实现重试器。锁 `do` 一次 `hc.Do` + 禁标准幂等头。acceptance 标明未用真重试器验单任务。 |
| **P6(a)** | `Client.do` 在 relay 传输错误上包装 `ErrTunnelDisconnected`。ctx 取消仍不得变成 `ErrUnreachable` 或 `ErrTunnelDisconnected`。 |
| **P7(a)** | 本卡写 `CHANGELOG.md` `[Unreleased]`：跨机失败可区分不可达 / 隧道断开；forward 不再声称传输写了卡。 |

上游：已批准 spec `docs/superpowers/specs/b233.3.md`；已冻结契约 `docs/superpowers/specs/b233.3-contract.md`（提交 `328285ee`，55 条 + Ticket 0）；已拍板拆解 `docs/superpowers/specs/b233.3-breakdown.md`。

读者：零上下文、品味存疑。按步骤复制代码。不要发明第三条交互协议、不要把 `ApprovalClient` 做成跨机 HTTP、不要给 Dispatch 加 `Idempotency-Key` / `X-Idempotency-Key`、不要改 relay 握手字节、不要让传输写卡、不要在 WS 层丢 progress、不要改 Web、不要实现重试器。

真机本机 ↔ linux-01 直连归协调者；本环境无 relay 节点时不发明拓扑。acceptance 标明 relay 连通未验、未用真重试器验单任务。

---

## 0. 硬边界（写进步骤，不是散文）

- 生产执行入口保持 `handoff dispatch|wait|reply|continue|stop` → `ExecutionClient` 方法 → 既有 HTTP/WS 路径。路径与 JSON 键不变。
- 具名 target 选路只许经 `targetclient.New` / `Pool.For`。本机空 target 继续 `client.New` / `NewLocal`。
- `ExecutionClient` 不含 `HTTPClient`、工作区、卡、机器、PTY。`HTTPClient()` 只许出现在 `internal/agentd/forward.go` 与 `forward_ws.go`。
- `d_transport → d_ledger` 禁止。挂卡只许 `handleProjectWorktreeCreate` → `attachCardBaseBranches` → `ledger.SetCardBaseBranch`。
- 不新增 `EventType`，不放宽 B233.1 冻结 17–26 可交付词表，不改 `all bool` 语义。
- `Client.do` 保持一次 `hc.Do`。禁止把重试放进 `do`。禁止标准幂等头。
- 不改 `CONNECT`/`CONNECT_OK`/`REGISTER` 字面值、`e2eInfoPrefix`、`DerivePSK`、Noise、`relayYamuxConfig` 保活。
- 日志用 `slog` / `c.log()` / `s.log`，禁 `fmt.Print*`。token 与 PSK 不得进日志。
- 真机项标「未验证，需真机」，机内不得写成绿。驱动派发系统自身的项由协调者执行，不派发。
- `codegraph check --view cards-B233.3-charter` 的 ApprovalClient 2 条 fail 不得变成 3 条；本卡不修 B233.1 absorb。

---

## 1. 基线证据、图覆盖与最薄路径

### 1.1 本节点已实跑（HEAD `1eef56c7`，未改实现）

| 范围 | 命令 | 实跑结果 |
|---|---|---|
| 全仓编译 | `go build -o /dev/null .` | 退出 0，stdout 空 |
| 触及包 | `go test ./internal/targetclient ./internal/relay ./internal/client -count=1` | ok；6.147s / 0.563s / 9.385s |
| Ticket 0 能力/投递/禁头 | `go test ./internal/client -run 'TestTransportAndExecutionClientCompileOnAggregate\|TestProductionHTTPClientCallersAreGatewayOnly\|TestWSDialOptionsPinsClientTransport\|TestDispatchOmitsStandardIdempotencyHeaders\|TestWaitDeliveryPolicyMatchesIsDeliverableAlias\|TestIsDeliverableAliasesWaitDeliveryPolicy\|TestErrTunnelDisconnectedDistinctFromUnreachable\|TestFollowAllDeliversAuditEvents\|TestWaitEventSkipsProgress\|TestUnreachableWhenNoResponse\|TestNotUnreachableOnContextCancel' -count=1` | ok 0.575s |
| 选路/池 | `go test ./internal/targetclient -run 'TestNewDirect\|TestNewRelay\|TestNewNoEndpoint\|TestNewRelayLowEntropyToken\|TestPoolRebuildsOnTargetChange\|TestPoolReusesClient\|TestWarmOnlyTouchesRelayTargets' -count=1` | ok 0.410s |
| relay 金样本 | `go test ./internal/relay -run 'TestConnectFrameGoldenBytes\|TestConnectOKFrameGoldenBytes\|TestRegisterFrameTypeLiteral\|TestDerivePSKGoldenVector\|TestRelayYamuxConfigKeepsLibraryKeepalive' -count=1` | ok 0.213s |
| 建树转发（旧正向） | `go test ./internal/agentd -run 'TestForwardWorktreeStripsCardIDsAndAttachesLocally\|TestForwardWorktreeErrorAndCancel' -count=1` | ok 1.841s。今日绿是因为 **forward 成功路径仍挂卡** |
| 图闸 | `codegraph --repo . --view cards-B233.3-charter check` | fails=2（ApprovalClient dead-*）；warns=145。本卡不修 |
| T3 探针（已删文件） | `NewRelay` + `Dialer.Close` + `Dispatch` | `errors.Is(ErrUnreachable)=true`，`ErrTunnelDisconnected=false`。原文见台账 |

### 1.2 图与源码核对

查图命令：`codegraph --repo . --view cards-B233.3-charter <子命令>`。

- `sym ExecutionClient`：`internal/client/execution.go:35`，`d_transport_channel`。
- `sym Transport`：同文件；另有 `ledgerstep.Transport` 与 `proxycfg.Transport`，查本卡用 `internal/client` 路径。
- `sym WaitDeliveryPolicy`：`internal/client/delivery.go:23`。
- `sym ErrTunnelDisconnected` / `ErrUnreachable` / `ErrNoEndpoint`：图未覆盖，回落 grep。
- `flow n_client_Client_do` / `n_agentd_Server_forwardWorktreeIfRequested`：**degraded=true，steps=[]**。禁止用 chain 冒充。已读源码。
- `flow n_client_Client_Dispatch`：degraded=false，经 `do`。源码 `client.go:737`。
- `flow n_client_Client_WaitEvent`：degraded=false；`readCursor` → `waitOnce` 循环 → 成功则 `writeCursor`。
- `who-calls n_client_Client_HTTPClient` 空边（`unscannedEntries=5`）。生产调用：`forward.go`、`forward_ws.go`。
- `who-calls n_agentd_Server_attachCardBaseBranches` 漏了 `forward.go:225`。以源码为准。

符号锚（`resolve --doc` 认这种写法）：

`internal/client/execution.go#Transport`、`internal/client/execution.go#ExecutionClient`、`internal/client/execution.go#ErrTunnelDisconnected`、`internal/client/delivery.go#WaitDeliveryPolicy`、`internal/client/client.go#Client.do`、`internal/client/client.go#Client.Dispatch`、`internal/client/client.go#Client.Reply`、`internal/client/client.go#Client.WaitEvent`、`internal/client/client.go#Client.FollowEvents`、`internal/client/client.go#isDeliverable`、`internal/client/client.go#Client.waitOnce`、`internal/client/client.go#Client.wsDialOptions`、`internal/targetclient/targetclient.go#New`、`internal/targetclient/targetclient.go#newWithDialer`、`internal/targetclient/pool.go#Pool.For`、`internal/targetclient/pool.go#NewLocal`、`internal/targetclient/warm.go#Pool.Warm`、`internal/agentd/forward.go#Server.forwardWorktreeIfRequested`、`internal/agentd/projectadmin.go#Server.handleProjectWorktreeCreate`、`internal/agentd/projectadmin.go#Server.attachCardBaseBranches`、`cmd/upgrade.go`、`cmd/root.go#newTargetClientNamed`、`internal/config/config.go#Target.IsRelay`。

库行为（禁止凭记忆；本轮对照源码）：

1. POST 默认不可重放；带 `Idempotency-Key` 或 `X-Idempotency-Key` 则可重放：`/usr/local/go/src/net/http/request.go` `Request.isReplayable` 1534–1548 行。
2. 不可重放请求在 `shouldRetryRequest` 返回 false：同目录 `transport.go` 847–849 行。
3. `http.Client.Timeout` 零值 = 无客户端超时（本仓库用 context）。
4. `yamux.DefaultConfig`：`EnableKeepAlive=true`，`KeepAliveInterval=30s`，`ConnectionWriteTimeout=10s`，`AcceptBacklog=256`：`github.com/hashicorp/yamux@v0.1.2/mux.go` 62–72 行。
5. `coder/websocket.Dial` 未给 `HTTPClient` 时用 `http.DefaultClient`：`github.com/coder/websocket@v1.8.15/dial.go` `cloneWithDefaults` 75–77 行。

### 1.3 最薄路径

本卡要锁的行为今天从声明缝调用**还得不到**：

- 建树 `?machine=` 成功后，挂卡发生在 `forward.go`（传输路径写账本），不是 handler。
- relay 隧道失败被吞成 `ErrUnreachable`（探针已证实）。
- `cmd/upgrade.go` 具名 relay 仍 `client.NewRelay`。

Ticket 0 空壳、选路金样本、`WaitDeliveryPolicy` 别名、禁标准头、`all=true` 交付 progress **已经绿**，不构成「已有活路径」来免除 T5。

DAG 第一条 = T5（点亮「forward 零账本 + handler 挂卡」）。T1/T2/T4 文件集与 T5 基本不相交，可在其后按序落地。T3 要看见 relay 错误如何到达 `do`。T6 收旁路/CHANGELOG/回归。

```
Ticket 0（已冻结 328285ee）
   │
   ▼
  T5 建树转发停写卡（handler 挂卡）     ← 最薄路径
   │
   ├─→ T1 选路/池锁（NewLocal / Warm≠可达 / 池重建）
   ├─→ T2 能力面 + 裸 HTTP 白名单 + 能力方法日志
   └─→ T4 可交付策略（过滤留在能力方法；streamOnce 不丢帧）
            │
            ▼
           T3 ErrTunnelDisconnected 在 Client.do 接线
            │
            ▼
           T6 旁路收口 / 禁头与一次 Do / 金样本 / CHANGELOG / 真机清单（协调者）
```

---

## 2. 跨 task 冻结接口（逐字，邻居只看得见这个）

**Produces（T5，T6 Consumes 必须逐字符相同）：**

```go
// internal/agentd/forward.go
func (s *Server) forwardWorktreeIfRequested(w http.ResponseWriter, r *http.Request) (handled bool, ws *proto.Workspace, cardIDs []string)
```

语义：`handled=false` 本机继续；`handled=true && ws==nil` 已写完错误响应；`handled=true && ws!=nil` 目标建树成功、本函数不写账本、调用方挂卡后 `writeJSON`。

**Produces（T3，T6 Consumes）：**

```go
// internal/client/client.go
type Client struct {
    // …既有字段保持…
    relayBacked bool
}
func NewRelay(d *relay.Dialer, token string) *Client // 必须 relayBacked=true
func (c *Client) do(ctx context.Context, method, path string, body any) (*http.Response, error)
// relayBacked 且非 ctx 取消/超时时：errors.Is(err, ErrTunnelDisconnected)
// 直连非 ctx 失败：errors.Is(err, ErrUnreachable)
// ctx 取消/超时：原样返回，不得包装两条哨兵
```

**Consumes（各 task 对既有缝，不得改签名）：**

```go
type Transport interface {
    BaseURL() string
    HTTPClient() *http.Client
}
type ExecutionClient interface {
    Dispatch(ctx context.Context, opts DispatchOpts) (*proto.Task, error)
    Reply(ctx context.Context, taskID, ticketID, answer string) error
    Continue(ctx context.Context, taskID, instructions string) error
    Stop(ctx context.Context, taskID string) (worktreeRemoved bool, err error)
    WaitEvent(ctx context.Context, taskID string, all bool) (*proto.Event, error)
    FollowEvents(ctx context.Context, taskID string, all bool, idle time.Duration, onEvent func(*proto.Event) error, onBacklog func(*BacklogSummary) error) error
}
func New(name string, t config.Target, log *slog.Logger) (*client.Client, func(), error)
func (p *Pool) For(name string) (*client.Client, error)
func NewLocal(addr, token string) *client.Client
func WaitDeliveryPolicy(t proto.EventType) bool
func isDeliverable(t proto.EventType) bool
func (s *Server) attachCardBaseBranches(ws proto.Workspace, ids []string, actor string) proto.Workspace
func (s *Server) handleProjectWorktreeCreate(w http.ResponseWriter, r *http.Request)
```

---

## 3. Task 1（T5）：建树转发停写卡——传输搬运，应用挂卡

### 精确文件集

- `internal/agentd/forward.go`（`forwardWorktreeIfRequested`、`forwardJSON` 注释）
- `internal/agentd/projectadmin.go`（`handleProjectWorktreeCreate` 开头）
- `internal/agentd/forward_ledger_guard_test.go`（新建）
- `internal/agentd/forward_test.go`（保持既有 HTTP 缝测试；改日志相关的假绿点）

不得改 `CreateManualWorktree`、Web、`attachCardBaseBranches` 的挂卡规则。不得改 `forwardTo` 的裸 HTTP 搬运。

### Interfaces

Consumes：`stripWorktreeCardIDs`、`forwardJSON`、`attachCardBaseBranches`、`writeJSON`、`copyForwardHeaders`、`s.ledgerActor`。  
Produces：生产 `forward.go` 零 `attachCardBaseBranches` / `SetCardBaseBranch`；HTTP `POST /api/projects/{name}/worktrees?machine=` 成功仍带 `CardResults`。

### 基线判据先跑

```bash
go test ./internal/agentd -run 'TestForwardWorktreeStripsCardIDsAndAttachesLocally|TestForwardWorktreeErrorAndCancel' -count=1
```

今日预期：全绿（旧正向：forward 路径挂卡）。实现后这两支必须仍绿，且新增源码守卫绿。

测试范围：`go test ./internal/agentd -run 'TestForwardWorktree|TestForwardSourceDoesNotWriteLedger' -count=1`。收尾再 `go build -o /dev/null .`。

### 步骤

1. **写失败测试（缝：`POST /api/projects/{name}/worktrees?machine=` + 生产 `forward.go`）**

   新建 `internal/agentd/forward_ledger_guard_test.go`。夹具不必起 HTTP：源码守卫锁「传输不写卡」。HTTP 行为继续由既有 `TestForwardWorktreeStripsCardIDsAndAttachesLocally` 锁（照抄该文件的 `newTestAgentdEnvWithCfg` / `testhttp.NewServer` / `seedAgentdLedger` / `ledger.Open`）。

```go
package agentd

import (
	"os"
	"path/filepath"
	"runtime"
	"strings"
	"testing"
)

func TestForwardSourceDoesNotWriteLedger(t *testing.T) {
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	body, err := os.ReadFile(filepath.Join(filepath.Dir(thisFile), "forward.go"))
	if err != nil {
		t.Fatal(err)
	}
	text := string(body)
	for _, needle := range []string{"attachCardBaseBranches", "SetCardBaseBranch"} {
		if strings.Contains(text, needle) {
			t.Fatalf("forward.go 是传输路径，不得写账本（含 %q）", needle)
		}
	}
}
```

2. **跑红**

```bash
go test ./internal/agentd -run TestForwardSourceDoesNotWriteLedger -count=1
```

预期：断言红（`forward.go` 含 `attachCardBaseBranches`）。确认不是拼写错。

3. **最小实现**

把 `forwardWorktreeIfRequested` 整函数替换为（保留它上面的 `forwardJSON` 不动，只改成功收口与签名；错误路径仍自己写响应）：

```go
// forwardWorktreeIfRequested 是建树请求专用的 ?machine 转发路径。
//
// 返回：
//   - handled=false：不是跨机请求，调用方走本机建树
//   - handled=true 且 ws==nil：已向 w 写入拒绝或目标错误，调用方必须直接 return
//   - handled=true 且 ws!=nil：目标建树成功。本函数不写账本、不写成功响应；
//     copyForwardHeaders 已落到 w。调用方用 cardIDs 挂卡后再 writeJSON。
//
// 为什么建树不能走通用的 forwardIfRequested：card_ids 指的是协调者本机账本里的卡。
func (s *Server) forwardWorktreeIfRequested(w http.ResponseWriter, r *http.Request) (handled bool, ws *proto.Workspace, cardIDs []string) {
	name := r.URL.Query().Get("machine")
	if name == "" || isForwarded(r) {
		return false, nil, nil
	}
	s.log.Info("建树请求开始专用转发", "machine", name, "path", r.URL.Path)
	target, ok := s.conf().Targets[name]
	if !ok {
		s.log.Warn("建树专用转发被拒：机器未定义", "machine", name)
		writeJSON(w, http.StatusBadRequest, map[string]string{"error": "机器 " + name + " 未定义"})
		return true, nil, nil
	}
	c, err := s.pool.For(name)
	if err != nil {
		s.log.Error("建树专用转发失败：取目标客户端", "machine", name, "cause", err)
		writeJSON(w, http.StatusBadGateway, map[string]string{"error": "转发到 " + name + " 失败: " + err.Error()})
		return true, nil, nil
	}
	raw, err := io.ReadAll(io.LimitReader(r.Body, forwardBodyLimit+1))
	if err != nil {
		s.log.Error("建树专用转发失败：读取请求", "machine", name, "cause", err)
		writeJSON(w, http.StatusBadGateway, map[string]string{"error": "读取转发请求失败: " + err.Error()})
		return true, nil, nil
	}
	if len(raw) > forwardBodyLimit {
		s.log.Warn("建树专用转发被拒：请求体过大", "machine", name, "bytes", len(raw),
			"limit", forwardBodyLimit)
		writeJSON(w, http.StatusRequestEntityTooLarge, map[string]string{"error": "请求体超过 1 MiB 上限"})
		return true, nil, nil
	}
	var original proto.CreateWorktreeReq
	if err := json.Unmarshal(raw, &original); err != nil {
		s.log.Warn("建树专用转发失败：请求 JSON 无法解析", "machine", name, "cause", err)
		writeJSON(w, http.StatusBadRequest, map[string]string{"error": "请求体必须是 JSON {mode, branch, base}"})
		return true, nil, nil
	}
	body, stripErr := stripWorktreeCardIDs(raw)
	if stripErr != nil {
		s.log.Warn("建树专用转发失败：请求对象无法裁剪", "machine", name, "cause", stripErr)
		writeJSON(w, http.StatusBadRequest, map[string]string{"error": "请求体必须是 JSON object"})
		return true, nil, nil
	}
	status, headers, payload, err := s.forwardJSON(r, name, c, target.Token, body)
	if err != nil {
		s.log.Error("建树专用转发失败：目标请求", "machine", name, "cause", err)
		writeJSON(w, http.StatusBadGateway, map[string]string{"error": "转发到 " + name + " 失败: " + err.Error()})
		return true, nil, nil
	}
	if status < http.StatusOK || status >= http.StatusMultipleChoices {
		s.log.Warn("建树专用转发原样回送目标错误", "machine", name, "status", status)
		copyForwardHeaders(w, headers)
		w.WriteHeader(status)
		if _, err := w.Write(payload); err != nil {
			s.log.Warn("建树专用转发回送目标错误失败", "machine", name, "status", status, "cause", err)
		}
		return true, nil, nil
	}
	var out proto.Workspace
	if err := json.Unmarshal(payload, &out); err != nil {
		s.log.Error("建树专用转发失败：目标响应不是 Workspace", "machine", name, "status", status, "cause", err)
		writeJSON(w, http.StatusBadGateway, map[string]string{"error": "目标建树响应无法解析: " + err.Error()})
		return true, nil, nil
	}
	out.CardResults = nil
	copyForwardHeaders(w, headers)
	s.log.Info("建树专用转发成功，交应用挂卡", "machine", name, "branch", out.Branch,
		"card_count", len(original.CardIDs))
	return true, &out, original.CardIDs
}
```

`handleProjectWorktreeCreate` 开头（`projectadmin.go` 约 916–921 行）替换为：

```go
	handled, forwarded, cardIDs := s.forwardWorktreeIfRequested(w, r)
	if handled {
		if forwarded == nil {
			return
		}
		if len(cardIDs) > 0 && s.ledger != nil {
			*forwarded = s.attachCardBaseBranches(*forwarded, cardIDs, s.ledgerActor(r))
		}
		writeJSON(w, http.StatusOK, *forwarded)
		s.log.Info("跨机建树完成并由本机挂卡", "machine", r.URL.Query().Get("machine"),
			"branch", forwarded.Branch, "card_result_count", len(forwarded.CardResults))
		return
	}
	if s.forwardIfRequested(w, r) {
		return
	}
```

本机路径（`CreateManualWorktree` 之后的 `attachCardBaseBranches`）保持不动。

把 `forwardJSON` 注释里「成功之后还要在**本端**接着挂卡（响应要改写）」改成：「成功/失败分叉留给调用方：失败原样透传；成功由 handler 挂卡再写响应。本函数不写账本。」

4. **跑绿**

```bash
go test ./internal/agentd -run 'TestForwardSourceDoesNotWriteLedger|TestForwardWorktreeStripsCardIDsAndAttachesLocally|TestForwardWorktreeErrorAndCancel' -count=1
```

然后 `go build -o /dev/null .`。

5. **加日志**  
   已在上块：转发入口、取客户端失败、目标错误、成功交应用、handler 挂卡完成。禁止打 token。成功路径不得静默。

6. **加注释**  
   新签名的返回三态写在函数头（已写）。handler 上补一句 why：跨机挂卡是账本用例，不能留在 forward。

### 验收

- `TestForwardSourceDoesNotWriteLedger` 命中且 ok。
- `TestForwardWorktreeStripsCardIDsAndAttachesLocally` 仍 ok：目标请求无 `card_ids`、未知键保留、本机卡基线=返回 branch、响应 `CardResults[0].OK`。
- `TestForwardWorktreeErrorAndCancel` 仍 ok：目标 500 原样、取消 502、都不写卡。
- 缺陷族：假绿——旧正向若只测剥 `card_ids` 不够，源码守卫必须在。静默失败——目标失败不得本端报挂卡成功（既有错误路径）。

---

## 4. Task 2（T1）：选路工厂与池

### 精确文件集

- `internal/targetclient/pool_test.go`（补 NewLocal / For 复用）
- `internal/targetclient/warm_test.go`（补 Warm≠HTTP 探活）
- `internal/targetclient/pool.go` / `warm.go` / `targetclient.go`（预期零行为改动；只许补注释）

不得改握手字节、不得让 `New` 发网络。

### Interfaces

Consumes：`New`、`newWithDialer`、`Pool.For`、`NewLocal`、`Pool.Warm` / `warmOnce`、`Target.IsRelay`。  
Produces：无新签名。

### 基线判据先跑

```bash
go test ./internal/targetclient -run 'TestNewDirect|TestNewRelay|TestNewNoEndpoint|TestNewRelayLowEntropyToken|TestPoolRebuildsOnTargetChange|TestPoolReusesClient|TestWarmOnlyTouchesRelayTargets' -count=1
```

今日预期：全绿。本 task **新增测试今日写下去也会绿**（锁现状）。若意外红，贴原文停下，不要改工厂迁就错误判据。这不是「为了制造红而改坏代码」的周期。

测试范围：`go test ./internal/targetclient -count=1`。

### 步骤

1. **写锁现状的测试（缝：`targetclient.New` / `NewLocal` / `Pool.For` / `Warm`）**

在 `pool_test.go` 追加：

```go
func TestNewLocalDoesNotEnterPool(t *testing.T) {
	p := NewPool(confOf(map[string]config.Target{
		"mac-02": {Addr: "10.0.0.2:7777", Token: "tok"},
	}), slog.Default())
	defer p.Close()
	local := NewLocal("127.0.0.1:7777", "tok")
	if p.size() != 0 {
		t.Fatalf("NewLocal 不得写入 entries，size=%d", p.size())
	}
	got, err := p.For("mac-02")
	if err != nil {
		t.Fatalf("For: %v", err)
	}
	if p.size() != 1 {
		t.Fatalf("池内只能有 For 的条目，size=%d", p.size())
	}
	if local == got {
		t.Fatal("NewLocal 的 client 不得是池里那一个")
	}
}

func TestPoolForReturnedClientMustStayReusable(t *testing.T) {
	p := NewPool(confOf(map[string]config.Target{
		"mac-02": {Addr: "10.0.0.2:7777", Token: "tok"},
	}), slog.Default())
	defer p.Close()
	a, err := p.For("mac-02")
	if err != nil {
		t.Fatal(err)
	}
	a.HTTPClient().CloseIdleConnections()
	b, err := p.For("mac-02")
	if err != nil {
		t.Fatal(err)
	}
	if a != b {
		t.Fatal("调用方关空闲连接不得迫使池重建 client（隧道归池）")
	}
}
```

在 `warm_test.go` 追加（补 import：`net/http`、`net/http/httptest`、`strings`）：

```go
func TestWarmSuccessDoesNotProbeAgentdHTTP(t *testing.T) {
	hits := 0
	ts := httptest.NewServer(http.HandlerFunc(func(http.ResponseWriter, *http.Request) {
		hits++
	}))
	t.Cleanup(ts.Close)
	addr := strings.TrimPrefix(ts.URL, "http://")
	p := NewPool(confOf(map[string]config.Target{
		"direct": {Addr: addr, Token: "t"},
		"relayed": {Relay: "wss://r.example.com/relay", Credential: "c",
			Node: "n", Token: "0123456789abcdef0123456789abcdef"},
	}), slog.Default())
	defer p.Close()
	p.ensure = func(context.Context, string) error { return nil }
	p.warmOnce(context.Background(), map[string]*warmState{})
	if hits != 0 {
		t.Fatalf("Warm 成功不得去拨 agentd HTTP，hits=%d", hits)
	}
}
```

2. **跑这些测试**（预期绿）

```bash
go test ./internal/targetclient -run 'TestNewLocalDoesNotEnterPool|TestPoolForReturnedClientMustStayReusable|TestWarmSuccessDoesNotProbeAgentdHTTP|TestNewDirect|TestNewRelay' -count=1
```

3. **生产代码**  
   不改行为。若上一步红，先读失败原文，不要「顺手」让 Warm 去 GET `/api/status`。

4. **加日志**  
   `New` / `For` / `Warm` 已有入口与失败日志。本票不新加 Info 热路径。确认 `newWithDialer` 失败仍点名 target，且不打 token。

5. **加注释**  
   `NewLocal` 已有「不写入 Pool.entries」。在 `Warm` 文档补一句 why：「预热成功只说明隧道，不能当成 agentd 可达（冻结 9）。」

### 验收

- 直连 `Addr=10.0.0.2:7777` → `BaseURL=http://10.0.0.2:7777`；合法 relay → `http://localhost`；皆空 → `ErrNoEndpoint`；token <32 hex → 失败。返回值满足 `ExecutionClient` 与 `Transport`（既有编译断言）。
- `TestNewLocalDoesNotEnterPool`、`TestWarmSuccessDoesNotProbeAgentdHTTP`、`TestPoolRebuildsOnTargetChange`、`TestPoolReusesClient` 命中且 ok。
- 缺陷族：假绿——构造测试不得冒充连通性（本票断言的是 BaseURL / 池指针 / HTTP 次数=0）。

---

## 5. Task 3（T2）：能力面接线与裸 HTTP 白名单

### 精确文件集

- `internal/client/client.go`（`Dispatch` / `Reply` / `Continue` / `Stop` 日志）
- `internal/client/execution.go`（注释：能力面不含 HTTPClient）
- `cmd/execution_surface_test.go`（新建；生产命令不得手拼执行 HTTP）
- 既有 `internal/client/execution_test.go`（回归，不改断言）

不得改 HTTP 路径或 JSON 键。不得把 `ApprovalClient` 暴露成 REST。变量类型保持 `*client.Client`（契约 §8 欠账，不是本票冻结项）。

### Interfaces

Consumes：Ticket 0 的 `ExecutionClient` / `Transport`。  
Produces：无新方法。生产 `.HTTPClient()` 仍只在两个 forward 文件。

### 基线判据先跑

```bash
go test ./internal/client -run 'TestTransportAndExecutionClientCompileOnAggregate|TestProductionHTTPClientCallersAreGatewayOnly|TestWSDialOptionsPinsClientTransport|TestDispatchOmitsStandardIdempotencyHeaders' -count=1
```

今日预期：全绿。CLI 源码守卫写下去也应当绿（生产已走类型化方法）。若红，先看是不是扫描到了测试文件。

测试范围：`go test ./internal/client ./cmd -run 'TestTransportAndExecutionClientCompileOnAggregate|TestProductionHTTPClientCallersAreGatewayOnly|TestWSDialOptionsPinsClientTransport|TestDispatchOmitsStandardIdempotencyHeaders|TestCLIExecutionCommandsDoNotHandRollHTTP' -count=1`。

### 步骤

1. **写 CLI 源码守卫（缝：cmd 生产执行入口）**

新建 `cmd/execution_surface_test.go`：

```go
package cmd

import (
	"os"
	"path/filepath"
	"runtime"
	"strings"
	"testing"
)

func TestCLIExecutionCommandsDoNotHandRollHTTP(t *testing.T) {
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	dir := filepath.Dir(thisFile)
	files := []string{"dispatch.go", "wait.go", "reply.go", "continue.go", "stop.go"}
	needles := []string{".HTTPClient()", "http.NewRequest", "client.NewRelay("}
	for _, name := range files {
		body, err := os.ReadFile(filepath.Join(dir, name))
		if err != nil {
			t.Fatal(err)
		}
		text := string(body)
		for _, n := range needles {
			if strings.Contains(text, n) {
				t.Fatalf("%s 生产路径不得手拼 HTTP / 取裸客户端（含 %q）", name, n)
			}
		}
	}
}
```

2. **跑守卫 + 既有白名单**

```bash
go test ./cmd -run TestCLIExecutionCommandsDoNotHandRollHTTP -count=1
go test ./internal/client -run 'TestProductionHTTPClientCallersAreGatewayOnly|TestTransportAndExecutionClientCompileOnAggregate' -count=1
```

预期：绿。若 CLI 守卫红，先看命中的是不是注释里的 `http.NewRequest`——把注释改成不出现该字面量，不要删类型化调用。

3. **加日志（能力方法；`do` 热路径保持 Debug）**

`Dispatch` 在组 body 之后、`c.do` 之前：

```go
	c.log().Info("Dispatch 进入", "url", c.baseURL, "relay", c.relayBacked,
		"executor", opts.Executor, "prompt_runes", len([]rune(opts.Prompt)))
```

失败分支（`do` 返回 err 与非 200）各打 `c.log().Error("Dispatch 失败", "url", c.baseURL, "cause", err)`。成功在 `return &task, nil` 前：

```go
	c.log().Info("Dispatch 完成", "url", c.baseURL, "task", task.ID, "state", task.State)
```

`Reply`：

```go
	c.log().Info("Reply 进入", "url", c.baseURL, "relay", c.relayBacked,
		"task", taskID, "ticket", ticketID, "answer_runes", len([]rune(answer)))
```

失败 Error，成功 `c.log().Info("Reply 完成", "task", taskID, "ticket", ticketID)`。

`Continue` / `Stop` 同样：入口带 task，失败带 cause，成功带结果（Stop 带 `worktree_removed`）。禁止把 token、plan_b64、answer 全文打进 Info。

`relayBacked` 字段在 T3 才加。本票若先于 T3 落地：日志先写 `"relay", false` 的编译会失败。因此 **本票的日志字段 `relay` 必须在 T3 的 `relayBacked` 落地之后才能编译**——若执行顺序上 T2 在 T3 前，日志先打 `"url", c.baseURL` 不引用 `relayBacked`；T3 再补 `"relay", c.relayBacked`。推荐本卡按 DAG：T2 日志不引用尚未存在的字段。

T2 日志最小集（不依赖 T3 字段）：

```go
c.log().Info("Dispatch 进入", "url", c.baseURL, "executor", opts.Executor, "prompt_runes", len([]rune(opts.Prompt)))
```

T3 步骤里给这四条入口日志补 `"relay", c.relayBacked`。

4. **跑绿**  
   上列测试 + `go test ./internal/client -run 'TestDispatchOmitsStandardIdempotencyHeaders|TestReplyRoundTrip|TestWaitEventSkipsProgress' -count=1`。

5. **加注释**  
   `execution.go` 文件头已有职责+边界。确认仍写「HTTPClient 只在 Transport 上」。不要在 `ExecutionClient` 上加方法。

### 验收

- 白名单测试仍能因多一处生产 `.HTTPClient()` 变红。
- CLI 五文件不含手拼执行 HTTP。
- `wsDialOptions` 的 `HTTPClient` 是本 client 的 `hc` 且 `Timeout==0`。
- 缺陷族：门禁——网络认证 ≠ 任务操作授权；本票不新开审批 HTTP。静默失败——Dispatch 失败不得返回成功 Task（既有解码路径 + 新入口/失败日志）。

---

## 6. Task 4（T4）：可交付策略归属

### 精确文件集

- `internal/client/client.go`（`waitOnce` / `FollowEvents` 跳过分支的 Debug 日志）
- `internal/client/delivery.go`（注释已声明归属；保持词表）
- 不改 `cmd/wait.go`，不改 `streamOnce` 按类型过滤。

### Interfaces

Consumes：`WaitDeliveryPolicy`、`isDeliverable`、`WaitEvent`、`FollowEvents`。  
Produces：无新签名。`all=true` 仍交付 progress。

### 基线判据先跑

```bash
go test ./internal/client -run 'TestWaitDeliveryPolicyMatchesIsDeliverableAlias|TestIsDeliverableAliasesWaitDeliveryPolicy|TestFollowAllDeliversAuditEvents|TestWaitEventSkipsProgress' -count=1
```

今日预期：全绿。本票不改变过滤位置。写下去仍应绿。

测试范围：同上 + `go test ./internal/client -run 'TestFollow' -count=1`（触及 follow 包内既有用例）。

### 步骤

1. **不加会改变入口符号的新测试。** 缝级断言就是上列既有测试：入口 `WaitDeliveryPolicy` / `WaitEvent` / `FollowEvents`。

2. **加日志**  
   `waitOnce` 跳过分支：

```go
		if !all && !isDeliverable(ev.Type) {
			c.log().Debug("wait 跳过不可交付事件", "task", taskID, "seq", ev.Seq, "type", ev.Type)
			return nil
		}
```

   `FollowEvents` 的 `streamOnce` 回调里，在 `fromSeq = ev.Seq` 之后、过滤处：

```go
			if !all && !isDeliverable(ev.Type) {
				c.log().Debug("follow 跳过不可交付事件", "task", taskID, "seq", ev.Seq, "type", ev.Type)
				return nil
			}
```

   不要把这条日志升到 Info（progress 会刷屏）。不要为了「对称」去改 FollowEvents 的内存 `fromSeq` 推进时机（契约 §11 C-2）。

3. **加注释**  
   `waitOnce` 过滤处 why：策略在应用谓词，传输 `streamOnce` 必须仍见到该帧。`delivery.go` 已有归属说明，保持。

4. **跑绿**  
   基线那组测试。若 `TestFollowAllDeliversAuditEvents` 红，说明有人在 `streamOnce` 丢帧——退回，不要改 `all` 语义。

### 验收

- `WaitDeliveryPolicy(progress)` 及七类审计为 false；`question` / `permission_request` 为 true。
- `all=true` 仍交付 progress。WaitEvent 默认跳过 progress，磁盘 cursor 落到 question seq。
- 缺陷族：假绿——七类假值显式断言已在 Ticket 0。序列化——类型字符串走 proto，不复制第二份词表。

---

## 7. Task 5（T3）：失败诊断——不可达 ≠ 隧道断开 ≠ ctx 取消

### 精确文件集

- `internal/client/client.go`（`Client` 字段、`NewRelay`、`do`、`MarkForwarded`、`NoRedirect`）
- `internal/client/execution.go`（哨兵注释：生产已接线）
- `internal/client/execution_test.go`（接线测试）
- `internal/client/unreachable_test.go` 不改既有直连用例

不得改握手字节。不得把包装放进 `Dialer.Close`（P6-(a) 包装点是 `do`）。`doStream` 不是 ExecutionClient 路径，本票不给它加包装。

### Interfaces

Consumes：`NewRelay`、`ExecutionClient.Dispatch`、`ErrUnreachable`、`ErrTunnelDisconnected`。  
Produces：`Client.relayBacked`；`do` 的包装契约见 §2。

### 基线判据先跑

```bash
go test ./internal/client -run 'TestErrTunnelDisconnectedDistinctFromUnreachable|TestUnreachableWhenNoResponse|TestNotUnreachableOnContextCancel' -count=1
```

今日预期：全绿。接线测试写下去会红：本节点探针 `NewRelay`+`Close`+`Dispatch` 为 `ErrUnreachable=true`、`ErrTunnelDisconnected=false`。

测试范围：`go test ./internal/client -run 'TestErr|TestUnreachable|TestNotUnreachable|TestRelayTunnel' -count=1`。

### 步骤

1. **写失败测试（缝：`ExecutionClient.Dispatch`）**

在 `execution_test.go` 追加 import：`log/slog`、`github.com/Xsxdot/handoff/internal/relay`。

```go
func TestRelayClosedDispatchIsTunnelDisconnected(t *testing.T) {
	d := relay.NewDialer("ws://127.0.0.1:1/relay", "c", "n",
		"0123456789abcdef0123456789abcdef", "", slog.Default())
	cl := client.NewRelay(d, "tok")
	if err := d.Close(); err != nil {
		t.Fatal(err)
	}
	_, err := cl.Dispatch(context.Background(), client.DispatchOpts{Prompt: "x"})
	if err == nil {
		t.Fatal("关闭的 relay 隧道上 Dispatch 必须失败")
	}
	if !errors.Is(err, client.ErrTunnelDisconnected) {
		t.Fatalf("要 ErrTunnelDisconnected，实得 %v", err)
	}
	if errors.Is(err, client.ErrUnreachable) {
		t.Fatalf("隧道断开不得与 ErrUnreachable 互认：%v", err)
	}
}

func TestRelayCanceledDispatchIsNotTunnelOrUnreachable(t *testing.T) {
	d := relay.NewDialer("ws://127.0.0.1:1/relay", "c", "n",
		"0123456789abcdef0123456789abcdef", "", slog.Default())
	t.Cleanup(func() { _ = d.Close() })
	cl := client.NewRelay(d, "tok")
	ctx, cancel := context.WithCancel(context.Background())
	cancel()
	_, err := cl.Dispatch(ctx, client.DispatchOpts{Prompt: "x"})
	if !errors.Is(err, context.Canceled) {
		t.Fatalf("要 context.Canceled，实得 %v", err)
	}
	if errors.Is(err, client.ErrUnreachable) || errors.Is(err, client.ErrTunnelDisconnected) {
		t.Fatalf("ctx 取消不得包装成两条网络哨兵：%v", err)
	}
}

func TestMarkForwardedRelayCopyKeepsTunnelSentinel(t *testing.T) {
	d := relay.NewDialer("ws://127.0.0.1:1/relay", "c", "n",
		"0123456789abcdef0123456789abcdef", "", slog.Default())
	cl := client.NewRelay(d, "tok").MarkForwarded()
	if err := d.Close(); err != nil {
		t.Fatal(err)
	}
	_, err := cl.Dispatch(context.Background(), client.DispatchOpts{Prompt: "x"})
	if !errors.Is(err, client.ErrTunnelDisconnected) {
		t.Fatalf("MarkForwarded 副本必须仍识别隧道断开，实得 %v", err)
	}
	if errors.Is(err, client.ErrUnreachable) {
		t.Fatalf("副本不得退回 ErrUnreachable：%v", err)
	}
}
```

2. **跑红**

```bash
go test ./internal/client -run 'TestRelayClosedDispatchIsTunnelDisconnected|TestRelayCanceledDispatchIsNotTunnelOrUnreachable|TestMarkForwardedRelayCopyKeepsTunnelSentinel' -count=1
```

预期：`TestRelayClosedDispatchIsTunnelDisconnected` 断言红（今日是 Unreachable）。取消那支今日可能已绿（`do` 已排除 ctx）。副本那支与主路径同红。

3. **最小实现**

在 `Client` 结构体 `initErr` 旁增加：

```go
	// relayBacked 为 true 时 hc.Transport 来自 relay.Dialer。
	// do 把非 ctx 的传输失败包装成 ErrTunnelDisconnected，而不是 ErrUnreachable。
	relayBacked bool
```

`NewRelay` 构造字面量加 `relayBacked: true`。

`MarkForwarded` 与 `NoRedirect` 的 `cp := &Client{...}` 都抄 `relayBacked: c.relayBacked`。漏抄 = 副本测试红。

替换 `do` 里 `hc.Do` 失败块（保留 ctx 先行）：

```go
	resp, err := c.hc.Do(req)
	if err != nil {
		if errors.Is(err, context.Canceled) || errors.Is(err, context.DeadlineExceeded) {
			return nil, err
		}
		if c.relayBacked {
			c.log().Debug("relay 隧道请求未拿到响应",
				"method", method, "path", path, "url", c.baseURL, "cause", err)
			return nil, fmt.Errorf("%w: %s %s: %w", ErrTunnelDisconnected, method, path, err)
		}
		c.log().Debug("agentd 请求未拿到响应",
			"method", method, "path", path, "url", c.baseURL, "cause", err)
		return nil, fmt.Errorf("%w: %s %s: %w", ErrUnreachable, method, path, err)
	}
	c.log().Debug("agentd 请求拿到响应",
		"method", method, "path", path, "status", resp.StatusCode, "url", c.baseURL, "relay", c.relayBacked)
	return resp, nil
```

给 T2 已加的 Dispatch/Reply/Continue/Stop 入口日志补 `"relay", c.relayBacked`。

`execution.go` 哨兵注释把「生产路径的隧道失败仍包装为 ErrUnreachable」改成：「生产路径由 Client.do 在 relayBacked 时包装本哨兵；直连仍是 ErrUnreachable。两条不得 errors.Is 互认。」

4. **跑绿**

```bash
go test ./internal/client -run 'TestRelay|TestErrTunnel|TestUnreachable|TestNotUnreachable|TestDispatchOmits' -count=1
```

5. **加日志**  
   已在 `do`。CLI `cmd/project.go` 的 `errors.Is(err, client.ErrUnreachable)` 不会把新哨兵当够不着（互斥）。不要在 project.go 把隧道断开改成「跳过本机登记」——那是误导处置；本票不改该分支。

6. **加注释**  
   `relayBacked` 字段 why 已写。`do` 里 ctx 先行的 why 保持。

### 验收

- 关闭 Dialer 后 Dispatch：`ErrTunnelDisconnected` 真、`ErrUnreachable` 假。
- 已取消 ctx：`context.Canceled`，两条网络哨兵都假。
- 直连端口 1：仍 `ErrUnreachable`（既有测试）。
- 缺陷族：假绿——互斥测试只锁哨兵不相认；接线测试去掉包装必须红。跨平台——断言的是 `errors.Is`，不依赖 errno 字符串。

---

## 8. Task 6（T6）：旁路、一次 Do、金样本、CHANGELOG、真机清单

### 精确文件集

- `cmd/upgrade.go`（`newAgentdClient`）
- `cmd/upgrade_test.go`（源码守卫；`newAgentdClient` 签名保持 `func(Endpoint) agentdPeer`，既有替身赋值不改）
- `internal/client/execution_test.go`（`do` 一次 `hc.Do`）
- `CHANGELOG.md` `[Unreleased]`
- T1–T5 测试作为回归

本 task 含驱动派发系统自身的真机项 → **由协调者执行，不派发**（见 §11）。实现者负责机内测试、`go build`、CHANGELOG、upgrade 工厂。

### 基线

`cmd/upgrade.go` 今日含 `client.NewRelay`。源码守卫写下去会红。

```bash
go test ./internal/relay -run 'TestConnectFrameGoldenBytes|TestConnectOKFrameGoldenBytes|TestRegisterFrameTypeLiteral|TestDerivePSKGoldenVector|TestRelayYamuxConfigKeepsLibraryKeepalive' -count=1
```

今日绿；本票不改这些文件。

### 步骤

1. **写失败测试（缝：upgrade 生产选路 + Dispatch 头/次数）**

在 `upgrade_test.go` 追加 import：`os`、`path/filepath`。

```go
func TestUpgradeProductionDoesNotCallNewRelay(t *testing.T) {
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	body, err := os.ReadFile(filepath.Join(filepath.Dir(thisFile), "upgrade.go"))
	if err != nil {
		t.Fatal(err)
	}
	text := string(body)
	if strings.Contains(text, "client.NewRelay") {
		t.Fatal("cmd/upgrade.go 生产路径不得调用 client.NewRelay；具名 relay 走 targetclient.New")
	}
	if !strings.Contains(text, "targetclient.New") {
		t.Fatal("cmd/upgrade.go 必须经 targetclient.New 选路")
	}
}
```

在 `execution_test.go` 追加一次-Do 计数（缝：`ExecutionClient.Dispatch` 穿过真实 `http.Client`）：

```go
type countRoundTripper struct {
	n    int
	next http.RoundTripper
}

func (c *countRoundTripper) RoundTrip(req *http.Request) (*http.Response, error) {
	c.n++
	return c.next.RoundTrip(req)
}

func TestDispatchDoOncePerCall(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		_ = json.NewEncoder(w).Encode(proto.Task{ID: "T1"})
	}))
	t.Cleanup(ts.Close)
	cl := client.New(ts.URL, "tok")
	ct := &countRoundTripper{next: cl.HTTPClient().Transport}
	cl.HTTPClient().Transport = ct
	if _, err := cl.Dispatch(context.Background(), client.DispatchOpts{Prompt: "x"}); err != nil {
		t.Fatalf("Dispatch: %v", err)
	}
	if ct.n != 1 {
		t.Fatalf("do 单次调用 hc.Do 次数=%d，want 1（重试不得放进 do）", ct.n)
	}
}
```

`TestDispatchDoOncePerCall` 今日预期绿（`do` 已是一次）。它锁的是冻结 36，不是「加上重试器」。P5-(a) 禁止为了让「重试仍单任务」变绿而去实现重试。

2. **跑红（upgrade 守卫）**

```bash
go test ./cmd -run TestUpgradeProductionDoesNotCallNewRelay -count=1
```

预期：断言红。

3. **最小实现（upgrade 工厂）**

`cmd/upgrade.go` import 增加：

```go
	"github.com/Xsxdot/handoff/internal/targetclient"
```

保留 `internal/relay`（`probeMachine` 仍 `CheckTokenEntropy`）。删除 `newAgentdClient` 里的 `relay.NewDialer` + `client.NewRelay`。

在 `agentdPeer` 接口附近增加失败替身（构造失败不能 panic，也不能改钩子签名——既有测试按 `func(Endpoint) agentdPeer` 赋值）：

```go
type upgradeDialErr struct{ err error }

func (u upgradeDialErr) Status(context.Context) (*proto.StatusResp, error) { return nil, u.err }
func (u upgradeDialErr) PushUpdate(context.Context, string, string, []byte, bool) (*proto.UpdateResp, error) {
	return nil, u.err
}
func (u upgradeDialErr) PullUpdate(context.Context, string, string, bool) (*proto.UpdateResp, error) {
	return nil, u.err
}
func (u upgradeDialErr) RestartAgentd(context.Context, bool) (*proto.UpdateResp, error) {
	return nil, u.err
}
func (u upgradeDialErr) WaitVersion(context.Context, string, time.Duration, time.Duration, bool) error {
	return u.err
}
```

替换 `newAgentdClient` 闭包：

```go
	newAgentdClient = func(ep Endpoint) agentdPeer {
		if ep.RelayURL != "" {
			name := ep.Name
			if name == "" {
				name = ep.Node
				if name == "" {
					name = "upgrade-relay"
				}
			}
			c, _, err := targetclient.New(name, config.Target{
				Relay:      ep.RelayURL,
				Credential: ep.Credential,
				Node:       ep.Node,
				Token:      ep.Token,
			}, slog.Default())
			if err != nil {
				slog.Default().Error("upgrade 经工厂构造 relay client 失败", "target", name, "cause", err)
				return upgradeDialErr{err: err}
			}
			slog.Default().Info("upgrade 经选路工厂构造 relay client",
				"target", name, "base_url", c.BaseURL(), "relay", true)
			return c
		}
		return client.New(ep.Addr, ep.Token)
	}
```

禁止把 token 写入日志。`probeMachine` 里既有的熵检查保留（与工厂双重闸一致，不是旁路）。

4. **CHANGELOG** 在 `## [Unreleased]` 的 `### 变更` 下追加（已有 B233.1 条，本卡另起一条）：

```markdown
- **跨机失败可区分目标不可达与 relay 隧道断开（B233.3）。** `ExecutionClient` 在 relay 传输失败时返回可 `errors.Is` 的隧道断开哨兵，不再与「机器未登记 / 够不着」混用。跨机建树转发不再由传输层写卡账本；挂卡留在建树 handler，HTTP `CardResults` 不变。
```

5. **跑绿 / 回归**

```bash
go test ./cmd -run 'TestUpgradeProductionDoesNotCallNewRelay|TestUpgradeWaitTimeoutPushLeavesRoomForWindowsRepetition' -count=1
go test ./internal/client -run 'TestDispatchDoOncePerCall|TestDispatchOmitsStandardIdempotencyHeaders|TestRelayClosedDispatchIsTunnelDisconnected|TestProductionHTTPClientCallersAreGatewayOnly' -count=1
go test ./internal/targetclient ./internal/relay -count=1
go test ./internal/agentd -run 'TestForwardSourceDoesNotWriteLedger|TestForwardWorktree' -count=1
go build -o /dev/null .
```

然后（无沙箱限制环境）：

```bash
go test ./internal/client/ ./internal/targetclient/ ./internal/relay/ ./internal/agentd/ ./cmd/ -count=1
codegraph --repo . --view cards-B233.3-charter check
```

沙箱 `Operation not permitted`：记原文，不归因。全量 `go test ./...` 留给 acceptance。图闸 fail 条数不得多于 2。

6. **加注释**  
   `newAgentdClient` 上写 why：具名 relay 选路只许一份（工厂），upgrade 曾经是第二份。`upgradeDialErr` 写「测试缝保持 `func(Endpoint) agentdPeer`，构造失败不能改签名」。

### 验收

- 生产 `cmd/upgrade.go` 无 `client.NewRelay`，有 `targetclient.New`。
- Dispatch 无标准幂等头；单次 Dispatch `RoundTrip` 次数=1。
- relay 金样本仍绿。
- CHANGELOG 含本卡两条用户可见变化。
- **acceptance 必须标明：未用真重试器验「重试仍单任务」；本环境无 relay 节点则 relay 连通未验。**
- 真机项见 §11，本票不把未跑的真机写成绿。

---

## 9. 五项检查

### 9.1 缺陷族

项目清单：`docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md` §3（顶部没有 `基线版本：charter@<commit>`——对不上基线标注，按项目清单加严并并入承重安全属性）。

**生命周期 / 状态机中断**

- 选路：`New` 不发网络。池隧道由 `Pool.Close` / 配置变更 `cleanup` 收；调用方不得关 `For` 返回的隧道（T1 `TestPoolForReturnedClientMustStayReusable`）。
- wait：cursor 在协调者本机文件。agentd 重启不删 cursor。cursor 写失败不吞已返回事件（既有）。
- 挂卡：forward 成功、挂卡中途崩溃 → 树已建、卡可能未挂。T5 保持单张失败不删树。不得只靠内存。
- 隧道：`Dialer.Close` 置 `closed`，禁止再惰性重连当成功（T3 夹具就是这条）。
- **无新孤儿类型，因为**不新增进程/工单表/临时目录。upgrade 旁路收口不改变换版生命周期。

**静默失败 / 误导报错**

- Dispatch 失败不得返回成功 Task。
- 无端点必须 `ErrNoEndpoint` 且点名 target。
- Warm 成功当成 agentd 可达 = 误导；T1 反面断言。
- 隧道断开报成「机器未登记 / 不可达」= 误导；T3。
- ctx 取消报成网络哨兵会让降级分支在 Ctrl-C 后继续；T3 + 既有。
- 目标建树失败本端报挂卡成功 = 禁止（T5 既有错误路径）。
- 新路径取 `HTTPClient` 乱拼 = 白名单禁止。

**跨平台假设**

- 本卡不新引入路径分隔、进程组或 webview 假设。cursor 路径沿用既有。
- relay / 直连在 Windows 上的 localhost 占位 **未验证，需真机**（§11）。握手字节不改。
- **无其它新平台假设，因为**选路输入是字符串字段，测试锁 BaseURL 字面值。

**假红 / 假绿测试**

- Ticket 0 金样本不是「跨机故事已完成」。
- `TestForwardWorktreeStripsCardIDsAndAttachesLocally` 在 T5 后仍锁 HTTP 行为；另加源码守卫，避免「只把挂卡挪到 handler 却忘了删 forward」的假绿，以及「只删 forward 忘了 handler」的假红（HTTP 测试会红）。
- `who-calls HTTPClient` 空 ≠ 无调用方。白名单走读源码。
- Warm / New 构造测试不得冒充连通性。
- `all=true` 是「WS 层丢帧」的反面断言。
- 夹具 `Dialer.Close` 不是真中途断流；真机项 §11.8 标缺口。

**门禁绕过**

- 具名 relay 绕过工厂会得到错误 BaseURL。P2-(a) 收 upgrade。
- `HTTPClient` 是绕过能力面的写路径。门覆盖全部非测试 `.go`。
- 网络认证 ≠ 任务操作授权。不新开审批 HTTP。
- 挂卡走既有 `SetCardBaseBranch`。forward 停写后不得在传输里另开「成功就当挂上」。
- TOCTOU：`Pool.For` 比较 Target 后返回，随后配置再变——下一次 For 才重建。窗口是既有池语义。

**序列化边界**

- 本卡不新增 Ticket/EventType/Workspace JSON 字段。P4-(a) 继续用既有 `CardResults`。
- `Dispatch` 手搭 `map[string]any` 是已有投影；禁标准头穿过真实 HTTP（既有 + T6 回归）。P5-(a) 不加新键。
- CONNECT / PSK 金样本已穿过真实编码函数。
- `WaitDeliveryPolicy` 是类型→唤醒的投影；`all=true` 穿过 WS 交付。

**枚举新值过既有白名单**

- 不新增 EventType。`ErrTunnelDisconnected` 不是 EventType。`cmd/project.go` 的 `ErrUnreachable` 分支不会吞新哨兵（不相认）。
- `FrameType` 字面值不改。web `QUIET_EVENTS` 预期零改。

**承重安全属性（必须能变红）**

| 属性 | 锁在哪 |
|---|---|
| 具名 relay 不得绕过工厂 | T1 BaseURL + T6 upgrade 源码守卫 |
| relay token <32 hex 拒绝 | 既有 `TestNewRelayLowEntropyToken` |
| Dispatch 不带标准幂等头 | `TestDispatchOmitsStandardIdempotencyHeaders` |
| `do` 单次一次 `hc.Do` | `TestDispatchDoOncePerCall` |
| 传输路径不写卡 | `TestForwardSourceDoesNotWriteLedger` |
| WaitEvent 跳过的事件不推进磁盘 cursor | `TestWaitEventSkipsProgress` |
| 两条失败哨兵不相认 | T3 接线测试 |
| ctx 取消不是网络哨兵 | T3 + 既有 |
| token / PSK 不进日志 | 本卡新增日志字段清单不含它们 |

**webview**

- **无，因为** P4-(a) 不改 `NewWorktreeDialog`：同一 `card_ids`、同一 `CardResults`。

### 9.2 序列化边界

| 边界 | 文件 | 断言 |
|---|---|---|
| Dispatch JSON + 头 | `internal/client/client.go#Client.Dispatch` | `TestDispatchOmitsStandardIdempotencyHeaders` 穿过 httptest |
| 建树请求剥 `card_ids` | `internal/agentd/forward.go#stripWorktreeCardIDs` | `TestForwardWorktreeStripsCardIDsAndAttachesLocally` 读目标收到的 map；未知键 `future_key` 保留 |
| `CardResults` | `proto.Workspace` 既有字段 | 同一 HTTP 测试；P4-(a) 不新手搭键 |
| CONNECT / CONNECT_OK / PSK | `internal/relay` | 金样本 |
| 事件类型 → 唤醒 | `WaitDeliveryPolicy` | 别名单测 + `all=true` WS |

没有「两端各自绿、中间无穿过边界的测试」的新字段。

### 9.3 上下文预算

每 task 文件集已圈出。不把整包 `internal/agentd`（89 文件 / 28308 行，命中架构法第三条）当派发上下文。回答：本卡触点是 `forward.go` / `projectadmin.go` 的建树 handler，不插竖切还债卡。

### 9.4 类型标注

`d_transport` / `d_gateway` / `d_maintenance` 为边界型：机内锁契约形状，行为真机见 §11。`d_cli` / `d_ledger` / `d_protocol` 逻辑型，机内闭环。`d_web` 本卡零改。

### 9.5 接缝覆盖（双向）

spec 测试决定 5 条缝：

| 缝 | 锁它的测试（入口符号） |
|---|---|
| 1 选路工厂 `targetclient.New` / 池 | `TestNewDirect` / `TestNewRelay` / `TestNewNoEndpoint`；T6 upgrade 守卫入口是读 `upgrade.go` 生产源，对应冻结 1 的生产选路 |
| 2 执行能力跨机 `ExecutionClient`（调用方 dispatch/wait/reply） | `TestDispatchOmitsStandardIdempotencyHeaders` 入口 `Dispatch`；`TestReplyRoundTrip` 入口 `Reply`；`TestWaitEventSkipsProgress` 入口 `WaitEvent`；CLI `TestCLIExecutionCommandsDoNotHandRollHTTP`；跨机成功语义走 §11，机内不得写成已绿 |
| 3 写请求不重复派发 | `TestDispatchOmitsStandardIdempotencyHeaders` + `TestDispatchDoOncePerCall`。**不**用真重试器验单任务（P5-(a)） |
| 4 forward 不再挂卡 | HTTP 入口 `POST .../worktrees?machine=`（`TestForwardWorktreeStripsCardIDsAndAttachesLocally`）；源码守卫见内部锁声明 |
| 5 isDeliverable 不在传输 | `TestFollowAllDeliversAuditEvents` 入口 `FollowEvents(..., all=true)`；`TestWaitEventSkipsProgress` 入口 `WaitEvent` |

缝 → 测试：五条均有缝级入口。  
测试 → 缝：上表入口都在缝上或链上（Dispatch/Reply/WaitEvent/FollowEvents/New/建树 HTTP）。

**内部锁（已声明，不顶替缝级断言）：**

1. `TestForwardSourceDoesNotWriteLedger` —— 从建树 HTTP 响应的 `CardResults` **构造不出**「挂卡发生在 handler 而不是 forward」：两端看起来一样。必须读生产 `forward.go`。
2. `TestUpgradeProductionDoesNotCallNewRelay` —— 本环境无 relay 节点，从 `upgrade --check` 行为 **构造不出**「是否走工厂」；必须读生产源码。
3. `TestProductionHTTPClientCallersAreGatewayOnly`（既有）—— `who-calls` 空，图覆盖不足；必须读源码。

**无未声明退路。** T2 若 CLI 守卫因注释字面量红，只改注释，不改入口符号。

---

## 10. 自审三查

### spec 覆盖（用户故事 → task）

| 故事 | task |
|---|---|
| 1 本机经执行能力 client 在 linux-01 跑 AGY/Codex；Ask/审批引用同本机 | T2 机内形状 + T6 回归；成功语义 §11.1–3 |
| 2 wait 断线重连按游标补拉 | T4 既有 cursor 测试 + §11.4 |
| 3 建树 `card_ids`：账本挂卡在应用，传输不写卡 | T5 |
| 4 写请求网络重试不产生第二个任务 | T6 锁约束；**未用真重试器验** |
| 5 progress 不唤醒 wait；规则不在传输 | T4 |

冻结 1–55 归属与 breakdown §2.1 一致。P1–P7 全 (a)，未开未选方案。

### 占位符扫描

无 TBD。「照抄 harness」自我声明：

- 建树 HTTP：`newTestAgentdEnvWithCfg` / `testhttp.NewServer` / `seedAgentdLedger` / `ledger.CreateCard` ← `internal/agentd/forward_test.go`、`ledger_fixtures_test.go`
- 关闭 Dialer 的 relay client ← 本节点探针（与 `client_relay_test.go` 的 `NewRelay` 同形）
- CLI 假 agentd ← `cmd/dispatch_test.go` 的 `runDispatch`（本卡不改它；T6 回归既有）
- 计数 RoundTripper 是新夹具，完整代码已写在 T6，不是骨架

### 跨 task 签名

§2 与 T5/T3/T6 Interfaces 逐字相同：`forwardWorktreeIfRequested` 三返回值、`Client.relayBacked`、`do` 包装、`ExecutionClient` 六方法未改。upgrade 钩子签名保持 `func(Endpoint) agentdPeer`。

---

## 11. 真机清单（协调者执行，不派发）

机内不得把本表写成绿。免费/限额模型不可用则该项失败并贴原文，禁止改走付费通道。

1. 本机经执行能力 client 对 linux-01 直连派发一次 AGY（或限额恢复后的 Codex）：任务可订阅，Ask/审批引用与本机 OpenCode 相同语义。**未验证，需真机。**
2. 跨机出现业务 Ask：`wait` 收到 `question`；`reply --answer` 回到原 ticket。**未验证，需真机。**
3. 跨机待外部审批：`wait` 收到 `permission_request`；`reply --approve` / `--deny` 回程仍走 `Reply`。**未验证，需真机。**
4. wait 断线重连：按游标补拉未决 Ask/待审批，不丢、不把重放当新请求。**未验证，需真机。**
5. progress 不唤醒默认 wait；需要全量时应仍能见到 progress。**未验证，需真机。**
6. 控制台（或等价 HTTP）对远端机器建树并带 `card_ids`：目标请求无 `card_ids`；协调者账本挂上基线。**未验证，需真机。**
7. 直连目标进程停掉：Dispatch/wait 为 `ErrUnreachable`，不是成功。**未验证，需真机。**
8. 若有 relay 节点：隧道中途断开为 `ErrTunnelDisconnected`，不得被当成「机器未登记」。本环境无节点则本项记缺口，不发明拓扑。**未验证，需真机。**
9. `handoff upgrade --check` 对具名直连 target 仍能探活；若将来有 relay 目标，不得再走 `client.NewRelay` 旁路。**未验证，需真机。**
10. 写请求重试仍单任务：**未用真重试器验**（P5-(a)）。**未验证，需真机。**
11. Windows（若验收范围含）：localhost 占位的 relay client、cursor 路径、进程中断。本卡无新假设，抽查既有路径是否被分流破坏。**未验证，需真机。**
12. 免费/限额模型不可用：派发失败贴原文，禁止改走付费通道。**未验证，需真机。**

---

## 12. 图覆盖债（本卡不修图归属）

- `sym ErrUnreachable` / `ErrTunnelDisconnected` / `ErrNoEndpoint` 不在图中。
- `who-calls n_client_Client_HTTPClient` 空；`unscannedEntries=5`。
- `who-calls attachCardBaseBranches` 漏 `forward.go`；`who-calls forwardWorktreeIfRequested` 漏 handler。实现后 handler 仍应是唯一生产调用方，图可能继续漏。
- `flow Client.do` / `forwardWorktreeIfRequested` degraded。
- `k_client_model` 与 `k_client_Client` 分裂。新接口已在 Ticket 0 进 model。
- 基线把部分 executor 容器标成 `d_executor`，best 归 `d_execution_contract`。本卡不修。
- `forwardWorktreeIfRequested` 基线域字段 `d_coordination_api`；best 容器 `k_agentd_Server` 归 `d_gateway`。
- `codegraph check` 2 条 ApprovalClient dead-* 与 HEAD 相同，不修 absorb。

---

## 13. 实现者收尾命令（每个 task 后）

```bash
go build -o /dev/null .
# 再跑该 task 声明的 -run / 包测试
```

禁止在未跑到结果的命令上写通过。agentd / cmd 全包测试耗时长，task 内循环用 `-run`；T6 再跑触及面包。全量 `go test ./...` 留给 acceptance。
