# B156.3.5 Plan：K5 d_orchestration·自动化编排

> 状态：plan 节点产出物；本回合只写计划，不修改 Go/TypeScript 产品代码。
> 卡：B156.3.5；有效基线分支：claude/kai-156-3-ba-df7357；当前分支：cards/B156.3.5-charter-2。
> 冻结输入：docs/superpowers/specs/2026-08-26-b156.3-automation-keystone-design.md、
> docs/superpowers/specs/b156.3-contract.md、docs/superpowers/specs/b156.3-breakdown.md。
> 台账：docs/superpowers/plans/b156.3.5-plan-ledger.md，与本文件同批提交。

本卡把 K2/K4 原语接成可长期运行的宿主循环：队列持久化重放、出队先唤醒协调者再沿
原入口真派、账本事件消费与同卡合并、resume 失败重建链，以及正常/失败回合的两级
名额归还。scheduling.Service.Release 的生产调用方归本卡；契约 11 的启动对账只作
兜底，不替代回合结束释放。

---

## 0. 基线复核与图覆盖债

下列命令已经在动手前真实执行，原始结果和失败原文逐条追加到本卡台账：

| 判据 | 基线实测 | 计划意义 |
|---|---|---|
| go build ./... | exit 0，stdout/stderr 为空 | 修改前全仓可编译 |
| go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v | PASS；ok github.com/Xsxdot/handoff/internal/keystone 0.350s | 既有 resume/重建 harness 存活，但不能替代 K5 缝测试 |
| go test ./internal/agentd/ -run 'TestCoord|TestSquadNode|TestQueue|TestScheduling' -count=1 | ok github.com/Xsxdot/handoff/internal/agentd 2.186s | 现有相关回归为绿 |
| go run . graph context d_orchestration --repo . | view=baseline；best 领域 d_orchestration；actual.byCurrentDomain 为 d_coordination_task=10、d_coordination_api=1；fociTruncated.total=152 | 图应然/实然位置已取证 |
| go run . graph check --repo . | fails: []，但有既有 anchor-off-domain、best-dangling、container-misplaced、legacy、oversized-package warnings | 不把既有图债当本卡问题 |
| codegraph context d_orchestration | 失败原文：/bin/bash: line 1: codegraph: command not found | 原 binary 缺失；使用仓内 fallback，不声称原命令已验证 |
| rg -n 'SetupAutomation' cmd/agentd.go | 无输出 | 生产 bootstrap 尚未调用 SetupAutomation |
| coordapi.go | LaunchAdmit 成功后 LaunchForCard 失败直返 502，无 Release；409 文案只在 ErrNoSlot 分支 | 必须修复名额泄漏和 409 误导 |
| cardstep.go | 小队 Admit 后只有 releaseCardStep，runStepFn 返回不归还 scheduler 名额 | 必须补执行者结束释放 |

判据不钉文件数或新增行数；只钉真实队列消费、Wake 顺序/次数、事件内容、同卡合并、
两级计数回零、seq 幂等和错误投影。

图覆盖债：fallback context 能给出 d_orchestration 的 best/actual，但 `go run . graph sym`
对 `internal/agentd/server.go#SetupAutomation`、`internal/agentd/cardstep.go#startCardStep`、
`internal/scheduling/scheduling.go#Release` 均返回“不在图中”；原始失败已入台账。本计划以
源码行号和 rg 回落核实这些接缝，实施后不把未入图符号写成图已覆盖。

---

## 1. 冻结接口与 K2 字段比对

### 1.1 直接消费的签名

~~~go
func (s *Service) LaunchAdmit(squadName string) (Binding, error)
func (s *Service) Admit(req IgnitionRequest) (Binding, error)
func (s *Service) Release(squadName, carrierName string) error
func (s *Service) Enqueue(req IgnitionRequest, kind string) (position int, err error)
func (s *Service) PopReady(kind string) (IgnitionRequest, bool, error)
var QueueKinds = []string{KindLaunchQueue, KindIgnitionQueue}

type WakeEvent struct { Kind WakeKind; Card, Summary string }
type RoundResult struct { Woke bool; SessionID string; Rebuilt bool; Escalated bool; Output string }
func (s *Service) Decide(ev WakeEvent) Decision
func (s *Service) Wake(ctx context.Context, card string, evs []WakeEvent) (RoundResult, error)
func (s *Service) LaunchForCard(ctx context.Context, card, source string, spec keysclient.SessionSpec) (RoundResult, error)

func (f *Facade) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]proto.LedgerEvent, error)
func (s *Server) startCardStep(cardID string, req proto.CardStepReq) error
func (s *Server) resolveCoordinatorSquad() (scheduling.Squad, error)
func (s *Server) SetupAutomation(st *ledger.Store)
~~~

### 1.2 跨卡字段级结论

对照 docs/superpowers/plans/b156.3.2-plan.md 的 Interfaces 和 admitSquadStep 代码块，
K2 Produces 与本卡 Consumes 逐字段完全一致：

| 字段 | K2 写入 | K5 消费 | 结论 |
|---|---|---|---|
| Card string | cardID | startCardStep(req.Card, ...) | 一致 |
| Squad string | node.Override.Squad | 仅快照/诊断，再入口重新读节点 | 一致 |
| Node string | req.Step | CardStepReq.Step = req.Node | 一致 |
| Target string | effectiveCovers 后的 target | CardStepReq.Target = req.Target | 一致 |
| Executor string | effectiveCovers 后的 executor | CardStepReq.Executor = req.Executor | 一致 |
| Model string | effectiveCovers 后的 model | CardStepReq.Model = req.Model | 一致 |
| Priority string | card.Priority | 不重读、不重算 | 一致 |
| Ready bool | true | 不重算 | 一致 |
| Actor string | req.Actor | CardStepReq.Actor = req.Actor | 一致 |
| envelope Req | json:"req" | PopReady 解包 IgnitionRequest | 一致 |
| envelope Seq int64 | 首次入队序 | 仅用于 PopReady FIFO | 一致 |
| kind | ignition_queue | QueueKinds 先 launch 后 ignition | 一致 |
| key | Card 或 Card|Node | PopReady 删除该行 | 一致 |

出队真派再入口固定为：

~~~go
err := s.startCardStep(req.Card, proto.CardStepReq{
    Step: req.Node, Target: req.Target, Executor: req.Executor,
    Model: req.Model, Actor: req.Actor,
})
~~~

这不是直接调用 StepRunner，也不使用 req.Squad 绕过节点定义；入口再次读取当前卡和
Override.Squad。冻结 IgnitionRequest 没有 Extra，队列不自造字段；快照的 Target、
Executor、Model 仍作为请求覆盖传入。以上比对本轮完成，不留待拍板。

---

## 2. 文件集、任务接口与顺序

有界实现文件：

- internal/agentd/scheddrain.go：清队、协调者回合包装、Release kick
- internal/agentd/wakeconsumer.go：事件解包、过滤、批量消费
- internal/agentd/server.go：自动化字段、构造初始化、StartAutomation
- internal/agentd/cardstep.go：执行者回合结束 Release
- internal/agentd/coordapi.go：统一 launch wrapper、502/409 分类
- cmd/agentd.go：账本打开后的生产装配和启动

测试文件：

- internal/agentd/scheddrain_test.go
- internal/agentd/wakeconsumer_test.go
- internal/agentd/coordapi_test.go（追加）
- internal/agentd/cardstep_test.go（追加）

Task A Consumes：

~~~go
func (s *Service) PopReady(kind string) (IgnitionRequest, bool, error)
func (s *Service) Enqueue(req IgnitionRequest, kind string) (position int, err error)
func (s *Service) LaunchAdmit(squadName string) (Binding, error)
func (s *Service) Release(squadName, carrierName string) error
func (s *Service) QueueSnapshot() ([]QueuedRequest, error)
func (s *Server) resolveCoordinatorSquad() (scheduling.Squad, error)
func (s *Server) startCardStep(cardID string, req proto.CardStepReq) error
func (s *Service) Wake(ctx context.Context, card string, evs []keystone.WakeEvent) (keystone.RoundResult, error)
func (s *Service) LaunchForCard(ctx context.Context, card, source string, spec keysclient.SessionSpec) (keystone.RoundResult, error)
~~~

Task A Produces：

~~~go
func (s *Server) StartAutomation(ctx context.Context)
func (s *Server) drainQueuesOnce(ctx context.Context) (processed int, err error)
func (s *Server) wakeCoordinatorRound(ctx context.Context, card string, evs []keystone.WakeEvent) (keystone.RoundResult, error)
func (s *Server) launchCoordinatorRound(ctx context.Context, card, source string) (keystone.RoundResult, error)
func (s *Server) releaseSchedulingBinding(card string, binding scheduling.Binding)
~~~

Task B Consumes：

~~~go
func (f *Facade) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]proto.LedgerEvent, error)
func (s *Service) Decide(ev WakeEvent) Decision
func (s *Server) wakeCoordinatorRound(ctx context.Context, card string, evs []keystone.WakeEvent) (keystone.RoundResult, error)
func (s *Service) AttachActive(card string) bool
~~~

Task B Produces：

~~~go
func (s *Server) consumeAutomationEventsOnce(ctx context.Context) (processed int, escalated bool, err error)
func automationWakeEvent(ev proto.LedgerEvent) (keystone.WakeEvent, bool, error)
func mirroredTaskTypeAndPayload(ev proto.LedgerEvent) (string, json.RawMessage, error)
~~~

Task C Consumes：

~~~go
func (s *Server) SetupAutomation(st *ledger.Store)
func setupLedger(cfg *config.Config, srv *agentd.Server, ctx context.Context, logger *slog.Logger) (func(), error)
func (s *Server) startCardStep(cardID string, req proto.CardStepReq) error
func (s *Server) launchCoordinatorRound(ctx context.Context, card, source string) (keystone.RoundResult, error)
~~~

Task C Produces：

~~~go
func (s *Server) StartAutomation(ctx context.Context)
func (s *Server) handleCoordLaunch(w http.ResponseWriter, r *http.Request)
~~~

---

## 3. 实施任务

### Task 0：基线取证（不改产品代码）

**文件**：只追加本计划台账。

**测试范围**：Task 0 已执行 build、agentd/keystone 最小命令；实现者开始每个 task
前重跑该 task 的最小命令并追加台账；全量测试只在 Task D。

**步骤**：

1. 确认分支和工作树只含计划/台账，复核 server.go:2238、coordapi.go:93-167、
   cardstep.go:46-124、cmd/agentd.go:440-465。
2. 复核 K2 字段表，禁止 Extra 入队，禁止 req.Squad 旁路 startCardStep。
3. 复核 EventsFromAsc 的空 cardIDs=全流、fromSeq 开区间、升序、limit<=0 默认 1000；
   消费者固定每次读 nil,cursor,500。
4. 复核 setupSquadEnv、runningCountIn、waitFor、newCoordEnv 和 keystone fake runner；
   测试沿用这些夹具，不复制第二套 store/registry。

**日志/注释**：不加代码；每确立事实立即追加台账。

**验收**：台账有真实输出和 codegraph 二进制失败原文；无产品代码变更。

---

### Task A：清队循环、重启重放与协调者回合 Release

**文件**：新增 internal/agentd/scheddrain.go；修改 internal/agentd/server.go。生产
bootstrap 由 Task C 修改 cmd/agentd.go。

**测试范围**：基线先跑
go test ./internal/agentd/ -run 'TestCoord|TestSquadNode|TestQueue|TestScheduling' -count=1；
实现后只跑
go test ./internal/agentd/ -run 'TestAutomationQueue|TestSchedulingRelease' -count=1 -v。

**基线判据**：基线已实跑 ok github.com/Xsxdot/handoff/internal/agentd 2.186s；
开始 Task A 前必须重跑并把结果追加台账。

**scheddrain.go 完整代码块**：

~~~go
// scheddrain.go —— K5 自动化编排的清队与协调者回合边界。
//
// 职责：按 scheduling.QueueKinds 重放持久队列；执行队列先以
// keystone.Wake(queue_release) 确认现场，再调用 startCardStep；
// 每个 LaunchAdmit 占用的两级名额在 LaunchForCard/Wake 返回后归还。
// 边界：不实现 scheduling 排序/CAS、不实现 keystone 重建、不直调 StepRunner。
package agentd

import (
    "context"
    "errors"
    "fmt"
    "time"

    "github.com/Xsxdot/handoff/internal/keysclient"
    "github.com/Xsxdot/handoff/internal/keystone"
    "github.com/Xsxdot/handoff/internal/proto"
    "github.com/Xsxdot/handoff/internal/scheduling"
)

const automationPollInterval = 2 * time.Second
const automationBatchLimit = 100

type coordinatorAdmissionError struct {
    squad string
    err error
}

func (e *coordinatorAdmissionError) Error() string {
    return fmt.Sprintf("协调者小队 %s 准入失败: %v", e.squad, e.err)
}
func (e *coordinatorAdmissionError) Unwrap() error { return e.err }

type coordinatorLookupError struct{ err error }

func (e *coordinatorLookupError) Error() string { return fmt.Sprintf("识别协调者小队失败: %v", e.err) }
func (e *coordinatorLookupError) Unwrap() error { return e.err }

// StartAutomation 启动 agentd 生命周期内唯一的自动化循环；依赖未装配时只记录可行动告警。
func (s *Server) StartAutomation(ctx context.Context) {
    if s.scheduling == nil || s.keystone == nil || s.autoLedger == nil {
        s.log.Warn("自动化循环未启动：依赖尚未装配",
            "has_scheduling", s.scheduling != nil,
            "has_keystone", s.keystone != nil, "has_ledger", s.autoLedger != nil)
        return
    }
    s.automationStartOnce.Do(func() {
        s.log.Info("自动化清队与事件唤醒循环启动",
            "poll", automationPollInterval, "queue_kinds", scheduling.QueueKinds)
        go s.automationLoop(ctx)
    })
}

func (s *Server) automationLoop(ctx context.Context) {
    s.runAutomationPass(ctx)
    ticker := time.NewTicker(automationPollInterval)
    defer ticker.Stop()
    for {
        select {
        case <-ctx.Done():
            s.log.Info("自动化循环停止", "cause", ctx.Err())
            return
        case <-ticker.C:
            s.runAutomationPass(ctx)
        case <-s.automationKick:
            s.runAutomationPass(ctx)
        }
    }
}

func (s *Server) runAutomationPass(ctx context.Context) {
    if _, _, err := s.consumeAutomationEventsOnce(ctx); err != nil {
        s.log.Error("自动化事件消费轮失败", "cause", err)
    }
    if _, err := s.drainQueuesOnce(ctx); err != nil {
        s.log.Error("自动化队列清队轮失败", "cause", err)
    }
}

// drainQueuesOnce 按 QueueKinds 法定顺序最多处理 100 行；失败行回填并停止当前 kind。
func (s *Server) drainQueuesOnce(ctx context.Context) (processed int, err error) {
    for _, kind := range scheduling.QueueKinds {
        for processed < automationBatchLimit {
            req, ok, popErr := s.scheduling.PopReady(kind)
            if popErr != nil {
                return processed, fmt.Errorf("出队 %s 失败: %w", kind, popErr)
            }
            if !ok {
                break
            }
            processed++
            s.log.Info("自动化队列出队", "kind", kind, "card", req.Card,
                "node", req.Node, "squad", req.Squad, "priority", req.Priority)
            switch kind {
            case scheduling.KindLaunchQueue:
                if _, launchErr := s.launchCoordinatorRound(ctx, req.Card, "manual"); launchErr != nil {
                    s.requeueAutomation(req, kind, launchErr)
                    return processed, nil
                }
            case scheduling.KindIgnitionQueue:
                if err := s.drainIgnitionRequest(ctx, req); err != nil {
                    s.requeueAutomation(req, kind, err)
                    return processed, nil
                }
            default:
                return processed, fmt.Errorf("清队遇到未声明 kind %q", kind)
            }
        }
        if processed >= automationBatchLimit {
            break
        }
    }
    return processed, nil
}

func (s *Server) drainIgnitionRequest(ctx context.Context, req scheduling.IgnitionRequest) error {
    backlog := 0
    if rows, err := s.scheduling.QueueSnapshot(); err != nil {
        s.log.Warn("生成出队简报失败，仍继续唤醒",
            "card", req.Card, "node", req.Node, "cause", err)
    } else {
        backlog = len(rows)
    }
    summary := fmt.Sprintf("queue_release kind=%s card=%s node=%s backlog=%d",
        scheduling.KindIgnitionQueue, req.Card, req.Node, backlog)
    decision := s.keystone.Decide(keystone.WakeEvent{
        Kind: keystone.WakeQueueRelease, Card: req.Card, Summary: summary,
    })
    if !decision.Wake {
        s.log.Info("队列出队被人工接管暂缓", "card", req.Card,
            "node", req.Node, "reason", decision.Reason)
        return fmt.Errorf("队列出队暂缓：%s", decision.Reason)
    }
    result, err := s.wakeCoordinatorRound(ctx, req.Card, []keystone.WakeEvent{{
        Kind: keystone.WakeQueueRelease, Card: req.Card, Summary: summary,
    }})
    if err != nil {
        return fmt.Errorf("队列出队唤醒失败: %w", err)
    }
    if !result.Woke {
        return errors.New("队列出队唤醒未运行协调者回合")
    }
    // K2/K5 的唯一再入口：不得把 req 直接转成 runner。
    return s.startCardStep(req.Card, proto.CardStepReq{
        Step: req.Node, Target: req.Target, Executor: req.Executor,
        Model: req.Model, Actor: req.Actor,
    })
}

func (s *Server) requeueAutomation(req scheduling.IgnitionRequest, kind string, cause error) {
    position, err := s.scheduling.Enqueue(req, kind)
    if err != nil {
        s.log.Error("自动化队列回填失败", "kind", kind, "card", req.Card,
            "node", req.Node, "cause", cause, "requeue_error", err)
        return
    }
    s.log.Warn("自动化请求暂缓并回填队列", "kind", kind, "card", req.Card,
        "node", req.Node, "position", position, "cause", cause)
}

// launchCoordinatorRound 是 HTTP 手动拉起与 launch_queue 共用的入口。
// 只有 LaunchAdmit 错误使用 coordinatorAdmissionError，LaunchForCard 失败仍为 502。
func (s *Server) launchCoordinatorRound(ctx context.Context, card, source string) (keystone.RoundResult, error) {
    var zero keystone.RoundResult
    squad, err := s.resolveCoordinatorSquad()
    if err != nil {
        return zero, &coordinatorLookupError{err: err}
    }
    binding, err := s.scheduling.LaunchAdmit(squad.Name)
    if err != nil {
        return zero, &coordinatorAdmissionError{squad: squad.Name, err: err}
    }
    defer s.releaseSchedulingBinding(card, binding)
    carrier, err := s.scheduling.Carrier(binding.Carrier)
    if err != nil {
        s.log.Error("读协调者载体失败", "card", card,
            "squad", binding.Squad, "carrier", binding.Carrier, "cause", err)
        return zero, fmt.Errorf("读载体 %s: %w", binding.Carrier, err)
    }
    spec := keysclient.SessionSpec{
        CLI: binding.Executor, HomeDir: carrier.HomeDir, Model: binding.Model,
        Workdir: s.resolveCoordWorkdir(card),
    }
    s.log.Info("自动化拉起协调者回合", "card", card, "source", source,
        "squad", binding.Squad, "carrier", binding.Carrier,
        "cli", spec.CLI, "home_dir", spec.HomeDir, "workdir", spec.Workdir)
    result, err := s.keystone.LaunchForCard(ctx, card, source, spec)
    if err != nil {
        return result, fmt.Errorf("拉起协调者回合失败: %w", err)
    }
    s.log.Info("自动化拉起协调者回合结束", "card", card, "source", source,
        "session", result.SessionID, "rebuilt", result.Rebuilt, "escalated", result.Escalated)
    return result, nil
}

// wakeCoordinatorRound 为 Wake 临时占用协调者回合名额，Wake 返回后释放。
// Release 失败只留完整身份日志，不覆盖 Wake 原始结果；启动对账仅是兜底。
func (s *Server) wakeCoordinatorRound(ctx context.Context, card string,
    evs []keystone.WakeEvent) (keystone.RoundResult, error) {
    var zero keystone.RoundResult
    squad, err := s.resolveCoordinatorSquad()
    if err != nil {
        return zero, &coordinatorLookupError{err: err}
    }
    binding, err := s.scheduling.LaunchAdmit(squad.Name)
    if err != nil {
        return zero, &coordinatorAdmissionError{squad: squad.Name, err: err}
    }
    defer s.releaseSchedulingBinding(card, binding)
    s.log.Info("自动化唤醒协调者回合", "card", card,
        "event_count", len(evs), "squad", binding.Squad, "carrier", binding.Carrier)
    result, err := s.keystone.Wake(ctx, card, evs)
    if err != nil {
        return result, fmt.Errorf("唤醒协调者回合失败: %w", err)
    }
    s.log.Info("自动化唤醒协调者回合结束", "card", card,
        "session", result.SessionID, "rebuilt", result.Rebuilt, "escalated", result.Escalated)
    return result, nil
}

// releaseSchedulingBinding 释放一次准入产生的两级计数；空 binding 是存量直绑路径。
func (s *Server) releaseSchedulingBinding(card string, binding scheduling.Binding) {
    if binding.Squad == "" || binding.Carrier == "" || s.scheduling == nil {
        return
    }
    if err := s.scheduling.Release(binding.Squad, binding.Carrier); err != nil {
        s.log.Error("自动化名额归还失败", "card", card,
            "squad", binding.Squad, "carrier", binding.Carrier, "cause", err)
        return
    }
    s.log.Info("自动化名额已归还", "card", card,
        "squad", binding.Squad, "carrier", binding.Carrier)
    s.kickAutomation()
}

func (s *Server) kickAutomation() {
    select {
    case s.automationKick <- struct{}{}:
    default:
    }
}
~~~

**server.go 字段/构造块**：在自动化服务引用后加入：

~~~go
automationStartOnce sync.Once
automationMu        sync.Mutex
automationCursor    int64
automationSeen      map[int64]struct{}
automationKick      chan struct{}
// automationRoundHook 仅供同包测试读取 RoundResult；生产恒为 nil。
automationRoundHook func(card string, result keystone.RoundResult)
~~~

在 NewServer 字面量加入：

~~~go
automationSeen: make(map[int64]struct{}),
automationKick: make(chan struct{}, 1),
~~~

**测试及变异**：

1. TestAutomationQueueRestartReplay 使用 setupSquadEnv 和真实 ledger registry 写入一个
   launch_queue、两个 ignition_queue；新建 Server 后调用 drainQueuesOnce；断言按 QueueKinds
   先 launch 后 ignition，三行都从 QueueSnapshot 消失。
2. TestAutomationIgnitionDrainWakesBeforeTrueDispatch 预绑定协调者会话，队列有一行且另有
   一行积压；断言 Resume 恰一次、briefing 含 queue_release/backlog=1、执行者入口恰一次，
   且入口发生在 Resume 返回后。删除或后移 wakeCoordinatorRound 必须翻红。
3. TestAutomationRoundReleasesCoordinatorCounters 记录
   sched_running/squad/coord 和 sched_running/carrier/coord-carrier 起始值；分别执行
   launchCoordinatorRound 与 wakeCoordinatorRound，逐键断言回到起始值。删除两个
   Release defer 必须翻红。
4. TestAutomationReleaseKicksDrain 断言释放后 automationKick 收到信号；这是附加锁，不能
   代替计数断言。

测试复用声明：照抄 internal/agentd/scheddispatch_test.go 的 setupSquadEnv、runningCountIn、
waitFor 和 K4 coordapi_test.go fake runner；不复制 store/transport 生命周期。断言逐条列全，
入口为 drainQueuesOnce 或两个 wrapper，均穿过本 task 接缝。

**日志/注释**：新文件头、StartAutomation 参数/生命周期、出队/准入/Wake/真派/回填/Release
每个成功和错误分支都带结构化字段；注释解释 QueueKinds、Wake-before-dispatch、cursor
和 Release 边界；禁止 print。

**Task A 验收**：只跑新增队列/Release 测试及既有 agentd 最小回归；队列清空、Wake 先于
真派、同一回合计数归还、回填和 kick 均有真实结果。

---

### Task B：账本事件消费者、过滤、同卡合并与重建兜底

**文件**：新增 internal/agentd/wakeconsumer.go、internal/agentd/wakeconsumer_test.go；
修改 server.go 自动化状态初始化。

**测试范围**：基线先跑
go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v；
实现后只跑
go test ./internal/agentd/ -run 'TestAutomationEvent|TestAutomationFallback' -count=1 -v。

**事件映射**：

- task_mirrored 解包 task_type/payload；completed、failed、turn_failed → WakeTaskTerminal。
- permission_request、question → WakeTicket。
- room_message 仅 RoomMsgUser 且 BySystem=false → WakeMessage；pointer/BySystem=true 不 Wake。
- approver_disabled 只作审计，不能抑制后续 permission_request。
- progress、heartbeat、approver_decision、permission_reuse、permission_auto_allow、
  deny_guidance_*、approval_dropped、tickets_voided、archived 等只推进 cursor，不 Wake。

**wakeconsumer.go 完整核心代码块**：

~~~go
// wakeconsumer.go —— K5 账本事件到 keystone 唤醒的唯一消费者。
//
// 职责：读取 card_events 全流，解包 task_mirrored/room_message，过滤非唤醒事件，
// 按卡合并为一次 Wake；成功后推进 seq 并记录 seen，防游标回退重复唤醒。
// 边界：不写 ledger schema、不解释 task 状态、不决定 attach/rebuild。
package agentd

import (
    "context"
    "encoding/json"
    "fmt"
    "sort"

    "github.com/Xsxdot/handoff/internal/keystone"
    "github.com/Xsxdot/handoff/internal/ledger"
    "github.com/Xsxdot/handoff/internal/proto"
)

type mirroredTaskEnvelope struct {
    TaskType string          `json:"task_type"`
    Payload json.RawMessage `json:"payload"`
}

func mirroredTaskTypeAndPayload(ev proto.LedgerEvent) (string, json.RawMessage, error) {
    if ev.Type != ledger.EvTaskMirrored {
        return "", nil, fmt.Errorf("事件 %d 不是 task_mirrored: %s", ev.Seq, ev.Type)
    }
    var envelope mirroredTaskEnvelope
    if err := json.Unmarshal(ev.Payload, &envelope); err != nil {
        return "", nil, fmt.Errorf("事件 %d 的 task_mirrored envelope 解码失败: %w", ev.Seq, err)
    }
    if envelope.TaskType == "" || len(envelope.Payload) == 0 {
        return "", nil, fmt.Errorf("事件 %d 的 task_mirrored 缺 task_type/payload", ev.Seq)
    }
    return envelope.TaskType, envelope.Payload, nil
}

// automationWakeEvent 的 false 表示合法但不唤醒；摘要最多 400 rune，原事件仍在账本。
func automationWakeEvent(ev proto.LedgerEvent) (keystone.WakeEvent, bool, error) {
    if ev.CardID == "" {
        return keystone.WakeEvent{}, false, nil
    }
    switch ev.Type {
    case ledger.EvTaskMirrored:
        taskType, payload, err := mirroredTaskTypeAndPayload(ev)
        if err != nil {
            return keystone.WakeEvent{}, false, err
        }
        var kind keystone.WakeKind
        switch proto.EventType(taskType) {
        case proto.EventTypeCompleted, proto.EventTypeFailed, proto.EventTypeTurnFailed:
            kind = keystone.WakeTaskTerminal
        case proto.EventTypePermissionRequest, proto.EventTypeQuestion:
            kind = keystone.WakeTicket
        default:
            return keystone.WakeEvent{}, false, nil
        }
        return keystone.WakeEvent{
            Kind: kind, Card: ev.CardID,
            Summary: fmt.Sprintf("%s: %s", taskType, truncateRunes(string(payload), 400)),
        }, true, nil
    case ledger.EvRoomMessage:
        var msg proto.RoomMessage
        if err := json.Unmarshal(ev.Payload, &msg); err != nil {
            return keystone.WakeEvent{}, false,
                fmt.Errorf("事件 %d 的 room_message 解码失败: %w", ev.Seq, err)
        }
        if msg.Kind != proto.RoomMsgUser || msg.BySystem {
            return keystone.WakeEvent{}, false, nil
        }
        return keystone.WakeEvent{
            Kind: keystone.WakeMessage, Card: ev.CardID,
            Summary: truncateRunes(msg.Body, 400),
        }, true, nil
    case ledger.EvNeedsHuman, ledger.EvNeedsCleared:
        return keystone.WakeEvent{
            Kind: keystone.WakeTaskTerminal, Card: ev.CardID,
            Summary: fmt.Sprintf("%s: %s", ev.Type, truncateRunes(string(ev.Payload), 400)),
        }, true, nil
    default:
        return keystone.WakeEvent{}, false, nil
    }
}

func (s *Server) consumeAutomationEventsOnce(ctx context.Context) (processed int, escalated bool, err error) {
    s.automationMu.Lock()
    from := s.automationCursor
    s.automationMu.Unlock()
    events, err := s.autoLedger.EventsFromAsc(nil, from, 500)
    if err != nil {
        return 0, false, fmt.Errorf("读自动化账本事件失败 cursor=%d: %w", from, err)
    }
    type pending struct { seq int64; ev keystone.WakeEvent }
    pendingByCard := map[string][]pending{}
    maxProcessed := from
    for _, ev := range events {
        if ev.Seq <= from {
            continue
        }
        s.automationMu.Lock()
        _, duplicate := s.automationSeen[ev.Seq]
        s.automationMu.Unlock()
        if duplicate {
            if ev.Seq > maxProcessed { maxProcessed = ev.Seq }
            continue
        }
        wake, yes, mapErr := automationWakeEvent(ev)
        if mapErr != nil { return processed, escalated, mapErr }
        if ev.Seq > maxProcessed { maxProcessed = ev.Seq }
        if yes {
            pendingByCard[ev.CardID] = append(pendingByCard[ev.CardID],
                pending{seq: ev.Seq, ev: wake})
        } else {
            s.automationMu.Lock()
            s.automationSeen[ev.Seq] = struct{}{}
            s.automationMu.Unlock()
        }
    }

    cards := make([]string, 0, len(pendingByCard))
    for card := range pendingByCard { cards = append(cards, card) }
    sort.Strings(cards)
    for _, card := range cards {
        batch := pendingByCard[card]
        evs := make([]keystone.WakeEvent, 0, len(batch))
        for _, item := range batch { evs = append(evs, item.ev) }
        decision := s.keystone.Decide(evs[0])
        if !decision.Wake {
            s.log.Info("自动化事件因 attach 暂缓", "card", card,
                "event_count", len(evs), "reason", decision.Reason)
            return processed, escalated, nil
        }
        result, wakeErr := s.wakeCoordinatorRound(ctx, card, evs)
        if wakeErr != nil { return processed, escalated, wakeErr }
        if s.automationRoundHook != nil { s.automationRoundHook(card, result) }
        if result.Escalated { escalated = true }
        for _, item := range batch {
            s.automationMu.Lock()
            s.automationSeen[item.seq] = struct{}{}
            s.automationMu.Unlock()
            processed++
        }
        s.log.Info("自动化事件批次已唤醒", "card", card,
            "event_count", len(evs), "session", result.SessionID,
            "rebuilt", result.Rebuilt, "escalated", result.Escalated)
    }
    s.automationMu.Lock()
    if maxProcessed > s.automationCursor { s.automationCursor = maxProcessed }
    s.automationMu.Unlock()
    return processed, escalated, nil
}
~~~

游标要求：非唤醒合法行映射成功后立即标 seen；Wake 行在 Wake 成功后标 seen。坏
envelope 停在原 cursor，不能吞成过滤事件。attach 暂缓不标 seen、不推进该批之后的 cursor。
只有所有前序事件成功映射/处理后才写 maxProcessed，不能先移动 cursor 再 Wake。

**测试和变异**：

1. TestAutomationEventMappingThroughConsumer 入口必须是 consumeAutomationEventsOnce；真实写入
   completed、failed、turn_failed、permission_request、question、RoomMsgUser、
   RoomMsgPointer、BySystem=true、progress 和 coordinator pointer。前五类/用户消息映射
   精确，pointer、BySystem、progress 不增 Resume；同卡 terminal+ticket+user 一次 briefing
   含三个 summary，Resume 恰一次；跨卡各一次。
2. TestAutomationApproverDisabledFallsBackToWake 写入 approver_disabled 后 permission_request；
   断言 permission summary 在 briefing、Resume 恰一次、processed==1。disabled 不单独 Wake，
   也不能抑制 permission。
3. TestAutomationCursorRewindIsIdempotent 首次消费后 Resume==1，退 cursor=0 再消费；
   断言 Resume 仍为 1、第二次 processed==0；删除 seen 查询/写入必须翻红。
4. TestAutomationAttachDefersAndThenWakes 设置 attach=true，断言 Resume==0 且 cursor 不过
   事件；解除 attach 后再消费，断言恰一次 Wake。
5. TestAutomationFallbackResumeRebuildFailure 由真实消费者输入 terminal 镜像事件，fake
   runner 的 Resume/Launch 均失败；断言 escalated==true、各一次调用、ledger 新增
   needs_human 事件且 reason 含 resume 和重建。

测试复用声明：照抄 internal/keystone/slice_test.go fake runner、
internal/agentd/ledgerapi_test.go ledger fixture、internal/proto/rooms_fixture_test.go
消息构造；这些夹具含包私有生命周期，计划以逐条 pass/fail 断言替代重复初始化。新增测试
入口均为消费者，穿过 Facade.EventsFromAsc→decode→wakeCoordinatorRound→keystone.Wake。

**日志/注释**：新文件头和辅助函数写职责、边界、空 payload 原因；读流、解码、attach、
Wake、成功批次均带 card/seq/event_count/cause；坏 envelope 不静默。

**Task B 验收**：事件精确映射、同卡一回合、disabled 正面一次、游标回退幂等、attach
不丢事件、resume/rebuild 双失败 needs_human 均有真实结果。

---

### Task C：生产 bootstrap、执行者回合释放与 coordinator 502/409 修复

**文件**：cmd/agentd.go、internal/agentd/cardstep.go、internal/agentd/coordapi.go；
追加 internal/agentd/cardstep_test.go、internal/agentd/coordapi_test.go。

**测试范围**：基线先跑 go build ./... 和 agentd 最小回归；实现后只跑
go test ./internal/agentd/ -run 'TestAutomationRound|TestCoordLaunch|TestCardStep' -count=1 -v。

#### C1 bootstrap

setupLedger 是唯一持有 *ledger.Store 的 bootstrap 点。在 cmd/agentd.go:440-465 的
srv.SetLedger(lst) 之后插入：

~~~go
srv.SetLedger(lst)
// B156.3 K5：账本打开后才能装配 registry、keystone 和自动化事件流。
// ctx 随 agentd 停机取消；首轮先重放队列/事件，再进入轮询。
srv.SetupAutomation(lst)
srv.StartAutomation(ctx)
logger.Info("自动化编排已挂载", "dsn", ldsn, "poll", "2s")
~~~

StartAutomation 用 sync.Once 防止第二个 goroutine；SetupAutomation 不清空 cursor/seen。
依赖不齐时记录带三个布尔值的 WARN，不静默运行。

#### C2 执行者 Release

把 cardstep.go:120-123 的 goroutine 替换为：

~~~go
go func() {
    defer s.releaseCardStep(cardID)
    defer s.releaseSchedulingBinding(cardID, binding)
    s.log.Info("卡节点回合开始", "card", cardID, "node", req.Step,
        "squad", binding.Squad, "carrier", binding.Carrier)
    s.runStepFn(context.Background(), runner, cardID, req.Step)
    s.log.Info("卡节点回合返回", "card", cardID, "node", req.Step,
        "squad", binding.Squad, "carrier", binding.Carrier)
}()
~~~

binding 为空时 helper no-op，存量直绑不碰 scheduling；runStepFn 成功或失败返回都释放。
纪律解析失败的同步回滚仍在 goroutine 启动前发生，可改用同一 helper 统一日志。

#### C3 coordinator helper 与错误投影

coordapi.go 保留 source/卡存在性校验，改为调用
launchCoordinatorRound(r.Context(), id, source)。handler 错误分流完整块：

~~~go
result, err := s.launchCoordinatorRound(r.Context(), id, source)
if err != nil {
    var admissionErr *coordinatorAdmissionError
    var lookupErr *coordinatorLookupError
    switch {
    case errors.As(err, &lookupErr) && errors.Is(lookupErr.err, errNoCoordinatorSquad):
        writeErr(w, http.StatusBadRequest, fmt.Errorf(
            "未登记协调者小队：先登记载体，再登记协调者小队（示例：handoff squad create --name coord --role coordinator --member coord-carrier --max-concurrency 1）"))
    case errors.As(err, &lookupErr) && errors.Is(lookupErr.err, errAmbiguousCoordinatorSquad):
        writeErr(w, http.StatusConflict, lookupErr.err)
    case errors.As(err, &lookupErr):
        s.log.Error("协调者小队识别失败", "card", id, "cause", lookupErr.err)
        writeErr(w, http.StatusInternalServerError, lookupErr.err)
    case errors.As(err, &admissionErr) && errors.Is(admissionErr.err, scheduling.ErrNoSlot):
        writeErr(w, http.StatusConflict, fmt.Errorf(
            "协调者并发已满（小队 %s）：等现役回合结束名额自动回收后重试，或先 attach 现有会话",
            admissionErr.squad))
    case errors.As(err, &admissionErr):
        writeErr(w, http.StatusBadRequest,
            fmt.Errorf("协调者准入被拒: %w", admissionErr.err))
    default:
        s.log.Error("协调者回合失败", "card", id, "source", source, "cause", err)
        writeErr(w, http.StatusBadGateway,
            fmt.Errorf("拉起协调者失败: %w", err))
    }
    return
}
s.log.Info("协调者 HTTP 拉起成功", "card", id, "source", source,
    "session", result.SessionID, "rebuilt", result.Rebuilt, "escalated", result.Escalated)
writeJSON(w, http.StatusOK, proto.CoordinatorLaunchResp{
    Woke: result.Woke, SessionID: result.SessionID, Rebuilt: result.Rebuilt,
    Escalated: result.Escalated, Output: result.Output,
})
~~~

coordinatorAdmissionError 的唯一最终定义：

~~~go
type coordinatorAdmissionError struct {
    squad string
    err error
}

func (e *coordinatorAdmissionError) Error() string {
    return fmt.Sprintf("协调者小队 %s 准入失败: %v", e.squad, e.err)
}
func (e *coordinatorAdmissionError) Unwrap() error { return e.err }

type coordinatorLookupError struct{ err error }

func (e *coordinatorLookupError) Error() string { return fmt.Sprintf("识别协调者小队失败: %v", e.err) }
func (e *coordinatorLookupError) Unwrap() error { return e.err }
~~~

它只包装 LaunchAdmit 错误；LaunchForCard 失败不包它，即使原文含“满”也只能走 502。

#### C4 失败拉起和执行者测试

TestCoordLaunchFailureReleasesCapacityAndKeeps502 复用 K4 newCoordEnv/fake runner/真实
runningCountIn：

1. 登记 RoleCoordinator 小队和 MaxConcurrency=1 载体，记录
   sched_running/squad/coord、sched_running/carrier/coord-carrier 起始值。
2. fake runner 令 LaunchForCard 失败；真实 POST handler 断言 502、body 含 launch 失败上下文、
   不含“协调者并发已满”，两计数回到起始值。
3. runner 恢复成功，第二次同一 HTTP 入口必须到达 runner 而不是 409；返回后两计数仍回零。
4. 删除 launchCoordinatorRound 的 Release defer 后重跑必须翻红：第二次 409 或计数不回零。

TestCardStepAdmittedRoundReleasesCapacity 通过 startCardStep 缝，把 runStepFn 设置为立即
返回，断言小队/载体计数从起始值加一又回到起始值；再覆盖 runStepFn 失败返回；删除
defer s.releaseSchedulingBinding 必须翻红。

测试复用声明：newCoordEnv、fake runner、runningCountIn、HTTP harness 照抄既有测试文件；
断言已列全，入口分别为真实 HTTP handler 和 startCardStep，不只调用 wrapper。

**日志/注释**：bootstrap 记录装配；cardstep 记录回合开始/返回/Release；coordapi 记录
source/card/准入分类/runner 结果；StartAutomation 有完整参数、生命周期注释。

**Task C 验收**：只跑指定 agentd 测试和 build；HTTP 502/409、失败/成功计数归还和生产
装配均有真实结果。

---

### Task D：最小收尾（协调者执行，不派发）

**文件**：上述有界文件集，不扩张到 ledger schema、keystone 规则、web 或 CLI。

**测试范围**：Task A/B/C 各自绿后执行全卡收尾；全量不属于单个实现 task。

**步骤**：

1. gofmt 触及 Go 文件，输出追加台账。
2. git diff --check；扫描计划和新增代码中的 TODO、TBD、适当处理、同 Task；不得留占位符。
3. 复核入口、外部调用前后、每条错误分支、成功路径、Release 成败都有结构化日志；
   全文无 fmt.Print/log.Printf。
4. 复核新文件头、导出函数参数/返回/边界注释，以及 Wake-before-dispatch、seen、
   Release 不由启动对账替代的原因注释。

---

## 4. 五项计划自审

### 4.1 缺陷族对抗审查

| 缺陷族 | 反问 | 判据 |
|---|---|---|
| 生命周期/泄漏 | LaunchAdmit、Admit、Wake、runStep 各返回是否归还？ | A/C 分别锁 coordinator wrapper、执行者 defer；删 Release 变异必红；错误带 card/squad/carrier |
| 重启/持久化 | Server 内存清空后何者可重放？ | registry 队列 PopReady 重建；事件 cursor 明确是进程缓存；QueueSnapshot 真实清空 |
| 顺序/竞态 | 两队、同卡、cursor 回退会否漏/重？ | QueueKinds 先 launch；同卡一次 Wake；seen rewind；attach 暂缓不前移 |
| 序列化/字段漂移 | K2 写入和 K5 消费是否同形？ | 九字段、Req/Seq、kind/key 表；真实 ledger enqueue/pop/再入口断言；不加 Extra |
| 静默失败/伪装 | 失败是否当成功/容量满？ | 坏 envelope 停 cursor；LaunchForCard 固定 502；仅 admission ErrNoSlot 409 |
| 唤醒过滤 | progress/pointer/disabled 会否误唤醒或漏 permission？ | 全表测试；disabled 后 permission 正面一次 Wake |
| 兜底 | resume/rebuild 双失败可见吗？ | 消费者集成断言 Resume/Launch、needs_human、escalated |
| 假红/假绿 | 是否只抽查计数或纯 mapper？ | 两级计数逐键回零；入口为 HTTP/消费者/清队；删除点明确 |
| 外部边界 | 是否偷偷加 OS/网络假设？ | 只用 keysclient/ledger facade；bounded ticker；不直达 DB/host |

### 4.2 序列化边界清单

1. AppendMirroredEvent 的 task_type/payload → mirroredTaskEnvelope：真实账本写入并通过
   消费者；坏 payload 停 cursor。
2. RecordRoomMessage JSON → RoomMessage：user/body、pointer/BySystem、空 cardID 分流在
   消费者入口断言。
3. Enqueue queuedEntry{Req,Seq} → PopReady → CardStepReq：真实 registry 穿透，逐字段检查
   Step/Target/Executor/Model/Actor，Extra 缺席显式断言。
4. RoundResult → CoordinatorLaunchResp：真实 HTTP 断言 Woke/SessionID/Rebuilt/Escalated/
   Output，不只 status。
5. running counter JSON → runningCountIn：逐键读取 squad/carrier，不只看队列。

本卡无新增跨语言投影；RoundResult bool、BySystem、queuedEntry 缺省字段分别区分缺失/零值。

### 4.3 上下文预算

Task A 只触及清队/Server 状态，Task B 只触及消费者/状态，Task C 只触及 bootstrap/
两个入口；跨 task 只经第二节签名。不重新打开调度原语、ledger schema、keystone 规则。

### 4.4 类型标注与真机清单

本卡是 d_orchestration 逻辑编排，使用已有 d_scheduling/d_keystone 出站端口，不新增
执行机协议类型或 CLI runner。实现收尾逐项执行真实 smoke 并记录原始输出：

1. 真实 agentd 启动日志确认 setupLedger→SetupAutomation→StartAutomation 各一次。
2. 真实 SQLite 登记 coordinator carrier/squad，调用 launch；runner 返回后两条 running 计数回 0。
3. 真实镜像写 permission_request，等待一个 poll tick，确认 coordinator 收到一次 Wake。
4. 真实 attach active 写同类事件，确认 attach 期间不 Wake，释放后仍消费。
5. 真实 launch 失败后再次点火，确认第一次 502、第二次非 409、计数均归零。

### 4.5 接缝覆盖（双向）

接缝清单：

- S1：scheduling.PopReady → K5 清队。
- S2：keystone.Wake(queue_release) → startCardStep 再入口。
- S3：Facade.EventsFromAsc → decode → keystone.Wake。
- S4：LaunchAdmit/Admit → 回合返回 → scheduling.Release。
- S5：setupLedger → SetupAutomation → StartAutomation。
- S6：HTTP handleCoordLaunch → launchCoordinatorRound → 502/409 response。

测试→缝：

- QueueRestartReplay 入口 drainQueuesOnce，穿 S1/S4。
- IgnitionDrainWakesBeforeTrueDispatch 入口 drainQueuesOnce，穿 S1→S2→S4。
- EventMapping、ApproverDisabled、CursorRewind、Attach、Fallback 的入口均为
  consumeAutomationEventsOnce，穿 S3。
- RoundReleasesCoordinatorCounters 入口为两个 wrapper，穿 S4；CardStepAdmitted... 入口
  为 startCardStep，穿执行者 Admit→Release。
- CoordLaunchFailure... 入口为真实 HTTP handler，穿 S5/S6。

缝→测试：S1 有重启清队；S2 有 Wake-before-dispatch；S3 有事件全表/disabled/过滤；
S4 有 coordinator 与 executor 释放；S5 有真实 bootstrap smoke；S6 有 502/409 projection。
每条缝至少一条缝级断言。没有纯 mapper 顶替缝测试，没有改变入口符号的条件退路。

---

## 5. 占位符和跨卡审计声明

- TBD、TODO、同 Task N、适当处理：本计划无实现占位；每个失败分支有精确处置和日志字段。
- harness 例外：Task A/C 复用 setupSquadEnv、newCoordEnv、fake runner、runningCountIn、waitFor；
  这些夹具带包私有 store/transport 生命周期，计划用逐条 pass/fail 断言代替重复初始化。
  Task B 通过真实 ledger API 写入，不以 mapper 直测代替消费者缝。
- 跨卡签名审计已完成：K2 IgnitionRequest 九字段、queuedEntry Req/Seq、kind/key/raw
  covers 与本计划 1.2 一字对齐；startCardStep 再入口已给完整代码。
- spec 故事归属：队列重放和 queue_release 真派→Task A；过滤/合并/disabled/重建兜底→Task B；
  Release 生产接线和失败 502/409→Task C；bootstrap→Task C；全卡 smoke/build/test/graph→
  Task D（协调者执行）。
- 不包含驱动 handoff 系统自身的验收步骤；不调用 handoff CLI。

---

## 6. 交付顺序和最终判据

顺序固定为 Task 0 → Task A → Task B → Task C → Task D。

全卡收尾真实执行并记录：

~~~text
go test ./internal/agentd/ -run 'TestAutomationQueue|TestAutomationEvent|TestAutomationFallback|TestCoordLaunchFailure|TestCardStepAdmittedRoundReleasesCapacity' -count=1 -v
go build ./...
go test ./...
go run . graph check --repo .
~~~

最终行为判据：

- 重建 Server 后 registry 中 launch/ignition 队列按法定顺序真实 PopReady 消费；
- ignition 出队 Wake queue_release 恰一次、summary 含 backlog、Wake 返回后才进入 startCardStep；
- terminal/ticket/user 精确映射，同卡一次 Wake；progress/pointer 不唤醒；approver_disabled 后
  permission_request 正面一次唤醒；
- cursor 回退/重复 seq 不二次 Wake，attach 暂缓后不漏事件；resume+rebuild 双失败进入
  MarkNeedsHuman 且 RoundResult.Escalated=true；
- 完整点火→准入→回合结束后，sched_running/squad/coord 与
  sched_running/carrier/coord-carrier 均回到起始值；删除 Release 变异必翻红；
- LaunchAdmit 成功但 LaunchForCard 失败返回 502、不返回 409，名额归还后下一次请求到达 runner；
- 生产真实执行 SetupAutomation/StartAutomation，不出现第二个并发消费者。

收尾追加每条命令的原始结果，git add 两份计划台账/文档并 commit，不 push。
