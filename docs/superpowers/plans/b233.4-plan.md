# B233.4 实现计划：工作区与 Git 能力独立（基线、隔离、结果留存和清理）

状态：执行计划  
卡：B233.4  
定级：L3 轻档（单轮 implement，禁止扇出子卡）  
法定产出：`docs/superpowers/plans/b233.4-plan.md`  
事实台账：`docs/superpowers/ledgers/2026-09-07-b233.4-plan-ledger.md`

**已由 plan docs/superpowers/plans/b233.4-plan.md 吸收（2026-09-07）**（契约 §9 移交 plan 附区）

拍板结果（breakdown §0，提交 `65476f0b`）必须按字面执行，不得把未选方案写成事实：

| 岔口 | 本计划落地 |
|---|---|
| **P1(a)** | Capability 适配器落在 agentd（`internal/agentd/workspace_capability.go`），包装现有包级函数。git 实现留 `internal/agentd/workspace.go`。本卡不搬 2844 行。 |
| **P2(a)** | `Manager` 增字段 `ws workspace.Capability`；`NewManager` 签名不改。组装点绑定。生产路径 nil → Dispatch / CreateManual **失败**（`ErrWorkspaceUnavailable`），禁止降级包级函数。测试注入 fake/adapter。 |
| **P3(a)** | 改 CLI `cmd/stop.go` **和** Web `web/src/app/task/Composer.tsx`：成功且未删树 → 「现场已留存，显式 reclaim/gc 才清」；不再把 `worktree_removed=false` 说成「清理失败」。JSON/HTTP 字段不改。成功 Stop 后 `worktree_removed` 恒 false（契约 §12 C-6）。 |
| **P4(a)** | 审阅/diff 现场 `rev-parse`，`NewResultRef` 组装；不新增 `Task` 字段或 HTTP。 |
| **P5(a)** | 本卡写 `CHANGELOG.md` `[Unreleased]`：Stop/取消不再删除 managed 工作树；现场留到显式 reclaim/gc 或归档 Done；CLI/Web 提示改为留存。 |

上游：已批准 spec `docs/superpowers/specs/b233.4.md`；已冻结契约 `docs/superpowers/specs/b233.4-contract.md`（提交 `102a8d7a`，75 条 + §12 C-0…C-14）；已拍板拆解 `docs/superpowers/specs/b233.4-breakdown.md`。

读者：零上下文、品味存疑。按步骤复制代码。不要发明第二套 git 协议、不要给 `Capability` 加 `Push`/`RunCmd`/`ListDir`、不要给 `PrepareReq`/`ManualReq` 加 `CardIDs`、不要改 `forward.go`、不要改 push 归属、不要新增 HTTP/CLI 子命令、不要加 `retained` 字段、不要把 `Capability` 放进 `k_agentd_fn`、不要 nil 降级包级函数、不要一次搬走 `workspace.go`。

真机项标「未验证，需真机」，机内不得写成绿。本节点不派发。

---

## 0. 硬边界（写进步骤，不是散文）

- 生产派发入口保持 `handoff dispatch` → `client.Dispatch` → `POST /api/tasks` → `Manager.Dispatch`。不改 CLI dispatch 字面值、不改路由表。
- `NewManager` 签名保持：
  `func NewManager(st *store.Store, hub *Hub, ads map[string]executor.Adapter, cfg *config.Config, envMapping func() map[string]string, approver *Approver, gate *permgate.Gate, log *slog.Logger) *Manager`
- 具体适配器类型构造只许：`NewGitCapability` 定义处、`cmd/agentd.go` 组装、`Server.SetManager` 在 `ws==nil` 时的默认绑定、测试帮手。生产路径禁止第二处 `PrepareWorkspace` 权威入口。
- `Capability` 为 nil：`Dispatch` 与 `CreateManual` 返回 `ErrWorkspaceUnavailable`（HTTP 503）。**不得**改调包级函数。脏检查/基线不得被跳过——因为根本走不下去。
- Stop **零次** `RecycleManaged` / `RemoveManagedWorktree`。成功 Stop 后 `worktree_removed=false`（含 managed）。有 `Commit`/`BaseCommit` 字段不构成删树授权。
- Done / compensate / Reclaim / GC 在动手前问 `workspace.MayRecycle`。`Decision==keep` 不得回收。Reclaim 的 `--force`/prune 仍走自己的 `git worktree remove`（不是 `RecycleManaged`：后者不降级 `--force`）。
- Done 问 `MayRecycle` 时 **State 必须是刚迁成的 `completed`、`Terminal=true`**，不得把 transit 前读到的 `waiting_review` 喂进去——否则永远 keep，归档树永不回收。
- 用户树（`Managed==false`）与手工树（`worktrees/manual`、无任务行）永不当任务垃圾删。GC 不扫手工目录。
- `internal/workspace` 不得 import `internal/proto` / `internal/ledger`。挂卡只在 `Server.attachCardBaseBranches`，且必须在 `CreateManual` **成功之后**。
- `d_orchestration→d_workspace` 接线前对账：现状 18 / 预算 19。占用 call 边后若超预算，**先涨 `legacyBudget` 到实际边数**并在 `legacyBudgetNote`/`entries` 列出新增符号。禁止造零边节点凑数。禁止把 `Capability` 放进 `k_agentd_fn`。
- 日志用 `m.log` / `log()`（`slog`），禁 `fmt.Print*`。不默认 Info 完整 diff 正文。
- 超时字面值不改：`WorkspaceGitTimeout=2m`、`FetchTimeout=2m`、`RunCmdTimeout=10m`。
- 不改 `forward.go`。不改 reclaim `--force` 语义。不改状态机。push 路径 OOS。
- 有界文件集之外只许改本 plan 与本台账。

---

## 1. 基线证据、图覆盖与最薄路径

### 1.1 本节点已实跑（HEAD `65476f0b`，未改实现）

| 范围 | 命令 | 实跑结果 |
|---|---|---|
| Ticket 0 金样本 | `go test ./internal/workspace -count=1` | ok 0.558s |
| 编码对齐 | `go test ./internal/agentd -run 'TestID8MatchesWorkspaceCanonical\|TestBaseCommitReMatchesWorkspaceIsCommitSHA' -count=1` | ok 0.575s |
| Stop/Done/Prepare/diff/reclaim 现状 | `go test ./internal/agentd -run 'TestStopRemovesManagedWorktree\|TestStopReportsWorktreeRemoved\|TestDoneRemovesManagedWorktree\|TestDoneKeepsUserWorktree\|TestDispatchFailedAfterWorkspaceCleansManagedWorktree\|TestTaskDiffTargetKeepsWorktreeWhenPresent\|TestTaskDiffTargetFallsBackToRepoWhenWorktreeGone\|TestTaskDiffTargetKeepsWorktreeWhenBranchUnknown\|TestPrepareWorkspaceNewWorktreeAllowsDirtyMainRepo\|TestPrepareWorkspaceMutualExclusionAndInjection\|TestReclaimRefusesNonTerminal\|TestReclaimRefusesNotManaged' -count=1` | ok 2.073s。**今日绿是因为 Stop 仍删树。** T3 必须退役 `TestStopRemovesManagedWorktree` / `TestStopReportsWorktreeRemoved` 的旧正向，留下 = 假绿或假红 |
| client JSON | `go test ./internal/client -run 'TestStopParsesWorktreeRemoved' -count=1` | ok 0.527s；true/false/缺字段都走真实 JSON |
| 全仓编译 | `go build -o /dev/null .` | 退出 0，stdout 空 |
| 图闸 | `codegraph --repo . --view cards-B233.4-charter check` | `fails: []`；warns=145 存量，本卡不修 |
| cmd Stop 过滤 | `go test ./cmd -run 'Stop' -count=1` | ok 0.615s（无既有 stop CLI 用例） |

实现者每个 task 动手前重跑该 task 的最小命令，预期沿用上表（T3 起 Stop 旧正向会红，那是要退役的信号，不是回归失败）。

### 1.2 图与源码核对

查图命令：`codegraph --repo . --view cards-B233.4-charter <子命令>`。

- `sym PrepareWorkspace`：`n_agentd_PrepareWorkspace`，`internal/agentd/workspace.go:418`，domain 基线 `d_coordination_task`（best 把 `k_agentd_fn` 归 `d_orchestration`，本卡不修归属，契约 §12 C-14）。
- `sym Capability`：`m_workspace_Capability`，`internal/workspace/capability.go:119`，domain `d_workspace`，container `k_workspace_model`。
- `flow Manager.Dispatch`：degraded=false，steps=95；源码仍直调 `EnsureRepoUsable` → `ResolveBaseline` → `PrepareWorkspace`（`manager.go:848-930`）。
- `flow Manager.Stop`：degraded=false，steps=28；源码 `manager.go:1703-1717` 仍 `RemoveManagedWorktree`，成功则 `worktreeRemoved=true`。
- `flow PrepareWorkspace` / `Manager.compensateWorkspace` / `MayRecycle` / `Server.handleTaskDiff`：**degraded=true，steps=[]**。禁止用 chain 冒充。已读源码。记图覆盖债。
- `who-calls PrepareWorkspace`：`Manager.Dispatch`、`PrepareBranch`。
- `who-calls MayRecycle`：无生产边；`unscannedEntries=5`，空不证明无调用方。接线后用 `who-calls n_workspace_MayRecycle` 复核。
- `who-calls n_agentd_id8`：`PrepareWorkspace` / `taskBranch`。`n_workspace_ID8` 仅被 `TaskBranch` 调用（生产未收口）。
- `context d_workspace`：kind=boundary。接口列表仍以 Mirror/localsync 为主；Capability 已在视图但未进 context 主链——接线后不强制 context 形态，以 `sym`/`who-calls`/`check` 为准。

符号锚（`resolve --doc` 认这种写法）：

`internal/workspace/capability.go#Capability`、`internal/workspace/retain.go#MayRecycle`、`internal/workspace/identity.go#ID8`、`internal/workspace/identity.go#TaskBranch`、`internal/workspace/identity.go#IsCommitSHA`、`internal/workspace/identity.go#NewResultRef`、`internal/agentd/workspace.go#PrepareWorkspace`、`internal/agentd/workspace.go#ResolveBaseline`、`internal/agentd/workspace.go#RemoveManagedWorktree`、`internal/agentd/workspace.go#DiffRange`、`internal/agentd/workspace.go#ReadFile`、`internal/agentd/workspace.go#taskDiffTarget`、`internal/agentd/workspace.go#id8`、`internal/agentd/workspace.go#gitExec`、`internal/agentd/manualworktree.go#CreateManualWorktree`、`internal/agentd/manager.go#Manager.Dispatch`、`internal/agentd/manager.go#Manager.Done`、`internal/agentd/manager.go#Manager.Stop`、`internal/agentd/manager.go#Manager.compensateWorkspace`、`internal/agentd/reclaim.go#Manager.Reclaim`、`internal/agentd/gc.go#Manager.GC`、`internal/agentd/server.go#Server.handleStop`、`internal/agentd/server.go#Server.handleTaskDiff`、`internal/agentd/server.go#Server.handleTaskFile`、`internal/agentd/server.go#Server.SetManager`、`internal/agentd/projectadmin.go#Server.handleProjectWorktreeCreate`、`internal/agentd/projectadmin.go#Server.attachCardBaseBranches`、`cmd/agentd.go`、`cmd/stop.go`、`cmd/wait.go#id8`。

库行为（禁止凭记忆）：

1. `WorkspaceGitTimeout = 2 * time.Minute`：`internal/agentd/workspace.go:113`。覆盖 Prepare/Remove/CreateManual 整组 git。
2. `FetchTimeout = 2 * time.Minute`：同文件 :925。仅基线补拉，不并入 WorkspaceGitTimeout。
3. `removeWorktreeAttempts = 5`、`removeWorktreeBackoff = 400 * time.Millisecond`：:864-865。`RemoveManagedWorktree` 失败不降级 `--force`。
4. `gitExec` 每次 `MkdirTemp` 空 hooks 目录 + `-c core.hooksPath=`：:128-150。不拼接 shell。
5. `id8` / `workspace.ID8` 按**字节**截前 8，不是 rune。任务 ID 为 UUID ASCII。
6. `os.Stat` 跟随符号链接；`taskDiffTarget` 用它判树在不在（:2838）。Windows 占用 **未验证，需真机**。
7. `encoding/json` 缺布尔字段解码为 false：`TestStopParsesWorktreeRemoved` 已锁 `{"status":"stopped"}` → false。
8. `Capability.DiffRange` 冻结签名含 `context.Context`；包级 `DiffRange` 内部 `context.Background()`（契约 C-8）。适配器可先看 `ctx.Err()` 再落到包级函数。

### 1.3 最薄路径

本卡要锁的行为今天从声明缝调用**还得不到**：Dispatch 不经 `Capability`；Stop 仍删 managed 树；Done/Reclaim/GC 未问 `MayRecycle`；`NewResultRef` 无生产调用；工作区函数虽零写卡但 HTTP 仍直接调包级 `CreateManualWorktree`。Ticket 0 空壳不是活路径。

T1 今日对齐测试已绿（两边同值），它收的是「只留一套截断」——不是点亮缺失行为。点亮活路径的是 **T2**（Dispatch 经 Capability）。T3 必须看见 T2 的 `ws`，否则生产仍走包级删除。

```
Ticket 0（已冻结 102a8d7a）
   │
   ├──────────────┬──────────────┐
   ▼              ▼              ▼
  T1 身份收口    T2 Capability   T4 结果引用
  id8/taskBranch  适配器+Dispatch  + diff 右端
  /IsCommitSHA                    （可与 T2 并行）
                  │
                  ├──────────────┐
                  ▼              ▼
                 T3 留存接线    T5 不挂卡
                 Stop/Done/      CreateManual
                 Reclaim/GC      之后才挂卡
                  │              │
                  └──────────────┘
                           ▼
                          T6 接缝回归 + CHANGELOG
                             + 真机清单（协调者）
```

---

## 2. 跨 task 冻结接口（逐字，邻居只看得见这个）

**Produces（T1，T2/T4 Consumes 必须逐字符相同）：**

```go
// internal/agentd/workspace.go —— 薄包装，禁止复制切片规则
func id8(taskID string) string
func taskBranch(taskID string) string
// 生产路径的 40 位校验改调 workspace.IsCommitSHA；删除包内 baseCommitRe

// cmd/wait.go 通知短 id 收成 workspace.ID8
func id8(s string) string // 函数名可保留，函数体必须调用 workspace.ID8
```

**Produces（T2，T3/T4/T5 Consumes）：**

```go
// internal/agentd/workspace_capability.go
var ErrWorkspaceUnavailable = errors.New("工作区能力未注入")

func NewGitCapability() workspace.Capability
func (m *Manager) SetWorkspace(c workspace.Capability)
func (m *Manager) Workspace() workspace.Capability

type gitCapability struct{}
var _ workspace.Capability = gitCapability{}

func (gitCapability) EnsureRepoUsable(ctx context.Context, repo string) error
func (gitCapability) ResolveBaseline(ctx context.Context, repo, sha string) (workspace.Baseline, error)
func (gitCapability) Prepare(ctx context.Context, req workspace.PrepareReq) (workspace.Prepared, error)
func (gitCapability) CreateManual(ctx context.Context, repo, worktreesDir string, req workspace.ManualReq) (workspace.ManualTree, error)
func (gitCapability) DiffRange(ctx context.Context, repo, base, head string) (string, error)
func (gitCapability) ReadFile(ctx context.Context, repo, rel string) (workspace.FileContent, error)
func (gitCapability) RecycleManaged(ctx context.Context, repo, workdir string) error
```

**Consumes（T2 对既有缝，禁止改签名）：**

```go
func PrepareWorkspace(ctx context.Context, req WorkspaceReq) (Workspace, error)
func ResolveBaseline(ctx context.Context, repo, sha string) (Baseline, error)
func EnsureRepoUsable(ctx context.Context, repo string) error
func RemoveManagedWorktree(ctx context.Context, repo, workdir string) error
func DiffRange(repo, baseBranch, headRev string) (string, error)
func ReadFile(repo, rel string) (proto.FileRead, error)
func CreateManualWorktree(ctx context.Context, repo, worktreesDir string, req proto.CreateWorktreeReq) (proto.Workspace, error)
func (m *Manager) Dispatch(ctx context.Context, req DispatchReq) (task *proto.Task, err error)
func NewManager(st *store.Store, hub *Hub, ads map[string]executor.Adapter, cfg *config.Config, envMapping func() map[string]string, approver *Approver, gate *permgate.Gate, log *slog.Logger) *Manager
```

**Produces（T3）：**

```go
func (m *Manager) Stop(ctx context.Context, taskID string) (worktreeRemoved bool, err error)
// 成功路径 worktreeRemoved 恒为 false；函数体内零次 RecycleManaged/RemoveManagedWorktree

func stopOutcomeLine(taskID string, removed bool) string
func runStop(cmd *cobra.Command, c *client.Client, taskID string) error
```

**Produces（T4）：**

```go
func (m *Manager) assembleResultRef(ctx context.Context, task *proto.Task, repo, headRev string) (workspace.ResultRef, error)
func taskDiffTarget(task *proto.Task) (repo, headRev string) // 签名不改
```

**Consumes（各 task 对既有 HTTP/CLI，禁止改）：**

```go
func (c *Client) Dispatch(ctx context.Context, opts DispatchOpts) (*proto.Task, error)
func (c *Client) Stop(ctx context.Context, taskID string) (worktreeRemoved bool, err error)
func (c *Client) Diff(ctx context.Context, taskID, base string) (string, error)
func (s *Store) SetCardBaseBranch(id, branch, actor string) error
func (t TaskState) IsTerminal() bool // 仅 completed / failed
```

不得改：`proto.Task` 字段集、`worktree_removed` JSON 键、路由表、`CreateWorktreeReq.CardIDs`（应用请求可带；Capability 请求不可带）。

---

## 3. Task 1：身份收口——生产路径只留一套截断规则

### 精确文件集

- `internal/agentd/workspace.go`（`id8`、`taskBranch`、删除 `baseCommitRe`、`ResolveBaseline` 改调 `IsCommitSHA`）
- `cmd/wait.go`（通知 `id8` 收成 `workspace.ID8`）
- `internal/agentd/workspace_identity_contract_test.go`（对齐测试保持；补一条 ResolveBaseline 大写 sha 拒）
- `codegraph/target.json`（T1 首次生产 import `internal/workspace`：对账后涨 `d_orchestration→d_workspace` 与必要时 `d_cli→d_workspace`）

### 测试范围

`go test ./internal/agentd -run 'TestID8MatchesWorkspaceCanonical|TestBaseCommitReMatchesWorkspaceIsCommitSHA|TestResolveBaselineRejectsUppercaseSHA' -count=1`  
`go test ./internal/workspace -run 'TestID8AndTaskBranchGoldenVectors|TestIsCommitSHAGoldenVectors' -count=1`  
`go test ./cmd -run 'id8|ID8|Wait' -count=1`（若无命中用例，编译 `go build -o /dev/null .` 即可）  
全量编译：`go build -o /dev/null .`

本 task 不跑全仓测试。

### 接缝

- 缝级：`ResolveBaseline`（Dispatch 前置；冻结 17/20–22）。入口符号 `ResolveBaseline`。
- 内部锁（声明）：`TestID8MatchesWorkspaceCanonical` 同时调 `id8` 与 `workspace.ID8`。从声明缝只能看见分支名字符串，看不见实现是一份还是两份切片——「从声明缝构造不出这条断言」。Prepare 既有测试仍锁 `handoff/<id8>` 字符串（缝上的值）。

### 步骤

- [ ] **Step 1：判据在基线跑**（动手前）  
  `go test ./internal/agentd -run 'TestID8MatchesWorkspaceCanonical|TestBaseCommitReMatchesWorkspaceIsCommitSHA' -count=1`  
  预期 ok（本节点已跑 ok 0.575s）。

- [ ] **Step 2：写失败测试（缝级，ResolveBaseline 大写 sha）**  
  在 `internal/agentd/workspace_identity_contract_test.go` 追加。今日 `baseCommitRe` 已拒大写，此测试**可能先绿**——那不是本票要点亮的新行为。本票红绿套在「删掉第二套正则之后大写仍拒」：先改测试名钉 `workspace.IsCommitSHA` 口径，再删 `baseCommitRe`。若删正则后此测试红且原因是「未调用 IsCommitSHA」，才写调用。

```go
func TestResolveBaselineRejectsUppercaseSHA(t *testing.T) {
	repo := initTestRepo(t)
	_, err := ResolveBaseline(context.Background(), repo, "482AAB1F9E12A3B4C5D6E7F8A9B0C1D2E3F4A5B6")
	if !errors.Is(err, ErrBadWorkspaceReq) {
		t.Fatalf("大写 hex 必须失败且错误链含 ErrBadWorkspaceReq，实得 %v", err)
	}
}
```

照抄夹具：`initTestRepo`（`internal/agentd/workspace_test.go`）。

- [ ] **Step 3：最小实现**  
  `internal/agentd/workspace.go` 增加 import `"github.com/Xsxdot/handoff/internal/workspace"`。替换：

```go
// id8 截取任务 ID 前 8 字节，用于分支名与 worktree 目录名的稳定短标识。
// 唯一截断规则在 workspace.ID8：禁止在本函数复制切片，改字面值只改那一处。
func id8(taskID string) string {
	return workspace.ID8(taskID)
}

// taskBranch 由任务 ID 派生分支名 handoff/<id8>。
func taskBranch(taskID string) string {
	return workspace.TaskBranch(taskID)
}
```

删除 `var baseCommitRe = regexp.MustCompile(...)`。`ResolveBaseline` 里 `baseCommitRe.MatchString(sha)` 改为 `workspace.IsCommitSHA(sha)`。若 `regexp` 在本文件再无引用，删 import。

`cmd/wait.go` 的 `id8`：

```go
func id8(s string) string {
	return workspace.ID8(s)
}
```

增加 import `internal/workspace`。这是第三套截断的收口，不是新接缝。

- [ ] **Step 4：跑绿**  
  Step「测试范围」三条。对齐测试必须仍 ok。反面（不要提交）：把 `id8` 改回 `taskID[:7]` → `TestID8MatchesWorkspaceCanonical` 必须红。

- [ ] **Step 5：图预算对账**  
  `codegraph --repo . --view cards-B233.4-charter check`。若 `d_orchestration→d_workspace` 或 `d_cli→d_workspace` 超预算：把 `legacyBudget` 提到**实际边数**（或实际边数 + 后续 T2–T5 预留，预留必须在 `legacyBudgetNote` 写明「B233.4 接线预留，最终以 T6 check 实数为准」），`entries` 按 check 报错原文的 label 填 `ID8` / `TaskBranch` / `IsCommitSHA`。禁止造零边节点。

- [ ] **Step 6：日志**  
  本票是纯函数包装，无新 I/O。不新增日志。生产路径分支名已由 Dispatch「工作区就绪」Info 打出（`manager.go:1034`）。`ResolveBaseline` 非法 sha 已有 Warn（保持）。

- [ ] **Step 7：注释**  
  `id8`/`taskBranch` 文档注明「唯一规则在 workspace 包，禁止复制切片」。`wait.go` `id8` 注明「通知短 id 与任务分支/目录名同一截断」。

- [ ] **Step 8：提交**  
  `docs(B233.4): T1 身份编码收成 workspace.ID8/TaskBranch`

---

## 4. Task 2：Capability 适配器与 Dispatch 接线

### 精确文件集

- 新增 `internal/agentd/workspace_capability.go`
- 新增 `internal/agentd/workspace_capability_test.go`
- `internal/agentd/manager.go`（字段 `ws`；Dispatch 改走 `m.ws`；`writeDispatchError` 的 503 在 server.go）
- `internal/agentd/server.go`（`SetManager` 默认绑定；`writeDispatchError` 映射 `ErrWorkspaceUnavailable` → 503）
- `cmd/agentd.go`（`NewManager` 之后 `SetWorkspace(NewGitCapability())`）
- `internal/agentd/manager_test.go`（`newTestManagerWithApprover` 绑定；`TestDispatchFailedAfterWorkspaceCleansManagedWorktree` 的直接 `NewManager` 补绑定）
- `codegraph/target.json` 与必要时 `codegraph/diffs/cards-B233.4-charter.json`

不改 CLI dispatch。不改 `workspace.go` 包级函数签名。

### 测试范围

`go test ./internal/agentd -run 'TestDispatchCallsCapability|TestDispatchNilCapabilityFailsClosed|TestDispatchFailedAfterWorkspaceCleansManagedWorktree|TestPrepareWorkspaceNewWorktreeAllowsDirtyMainRepo|TestPrepareWorkspaceMutualExclusionAndInjection' -count=1`  
`go test ./internal/workspace -count=1`  
`go build -o /dev/null .`  
`codegraph --repo . --view cards-B233.4-charter check`

### 接缝

全部缝级。入口：`Manager.Dispatch`（spec 测试决定 1；冻结 25–35、65、8–9）。HTTP `POST /api/tasks` 经 `handleDispatch` 进入同一方法。禁止只测适配器内部函数名。

### 步骤

- [ ] **Step 1：判据在基线跑**  
  `go test ./internal/agentd -run 'TestPrepareWorkspaceNewWorktreeAllowsDirtyMainRepo|TestPrepareWorkspaceMutualExclusionAndInjection|TestDispatchFailedAfterWorkspaceCleansManagedWorktree' -count=1`  
  预期 ok（本节点已跑，含在 2.073s 那组）。今日 Dispatch **不**经 Capability——T2 新测试先红。

- [ ] **Step 2：写失败测试**  
  新建 `internal/agentd/workspace_capability_test.go`。照抄夹具：`newTestManagerWithApprover`、`registerTestProject`、`initTestRepo`、`fake.New`（`manager_test.go`）。**先写测试再写 `SetWorkspace`/`NewGitCapability`：首红允许是编译红（undefined）。空壳落地后必须再见一次断言红**（`prepare` 计数仍为 0，因为 Dispatch 还没接线）。

```go
package agentd

import (
	"context"
	"errors"
	"sync"
	"testing"

	"github.com/Xsxdot/handoff/internal/executor"
	"github.com/Xsxdot/handoff/internal/executor/fake"
	"github.com/Xsxdot/handoff/internal/workspace"
)

type countingCap struct {
	inner   workspace.Capability
	mu      sync.Mutex
	ensure  int
	resolve int
	prepare int
	recycle int
	create  int
	diff    int
	read    int
}

func (c *countingCap) inc(p *int) {
	c.mu.Lock()
	*p++
	c.mu.Unlock()
}

func (c *countingCap) EnsureRepoUsable(ctx context.Context, repo string) error {
	c.inc(&c.ensure)
	return c.inner.EnsureRepoUsable(ctx, repo)
}
func (c *countingCap) ResolveBaseline(ctx context.Context, repo, sha string) (workspace.Baseline, error) {
	c.inc(&c.resolve)
	return c.inner.ResolveBaseline(ctx, repo, sha)
}
func (c *countingCap) Prepare(ctx context.Context, req workspace.PrepareReq) (workspace.Prepared, error) {
	c.inc(&c.prepare)
	return c.inner.Prepare(ctx, req)
}
func (c *countingCap) CreateManual(ctx context.Context, repo, dir string, req workspace.ManualReq) (workspace.ManualTree, error) {
	c.inc(&c.create)
	return c.inner.CreateManual(ctx, repo, dir, req)
}
func (c *countingCap) DiffRange(ctx context.Context, repo, base, head string) (string, error) {
	c.inc(&c.diff)
	return c.inner.DiffRange(ctx, repo, base, head)
}
func (c *countingCap) ReadFile(ctx context.Context, repo, rel string) (workspace.FileContent, error) {
	c.inc(&c.read)
	return c.inner.ReadFile(ctx, repo, rel)
}
func (c *countingCap) RecycleManaged(ctx context.Context, repo, workdir string) error {
	c.inc(&c.recycle)
	return c.inner.RecycleManaged(ctx, repo, workdir)
}

func TestDispatchCallsCapability(t *testing.T) {
	repo := initTestRepo(t)
	fk := fake.New(nil)
	m, _, _ := newTestManagerWithApprover(t, map[string]executor.Adapter{"fake": fk}, "fake", nil)
	spy := &countingCap{inner: NewGitCapability()}
	m.SetWorkspace(spy)
	pid := registerTestProject(t, m, repo)
	task, err := m.Dispatch(context.Background(), DispatchReq{
		ProjectID: pid, Prompt: "x", Executor: "fake", NewWorktree: true,
	})
	if err != nil {
		t.Fatalf("Dispatch: %v", err)
	}
	if !task.WorktreeManaged || task.WorkDir == "" {
		t.Fatalf("new-worktree 元数据缺失: %+v", task)
	}
	if task.BaseCommit != "" && !workspace.IsCommitSHA(task.BaseCommit) {
		t.Fatalf("BaseCommit 必须是 40 位小写 hex 或空仓空串，实得 %q", task.BaseCommit)
	}
	if spy.prepare < 1 || spy.resolve < 1 || spy.ensure < 1 {
		t.Fatalf("Dispatch 必须经 Capability：ensure=%d resolve=%d prepare=%d", spy.ensure, spy.resolve, spy.prepare)
	}
}

func TestDispatchNilCapabilityFailsClosed(t *testing.T) {
	repo := initTestRepo(t)
	fk := fake.New(nil)
	m, _, _ := newTestManagerWithApprover(t, map[string]executor.Adapter{"fake": fk}, "fake", nil)
	m.SetWorkspace(nil)
	pid := registerTestProject(t, m, repo)
	_, err := m.Dispatch(context.Background(), DispatchReq{
		ProjectID: pid, Prompt: "x", Executor: "fake", NewWorktree: true,
	})
	if !errors.Is(err, ErrWorkspaceUnavailable) {
		t.Fatalf("nil Capability 必须失败且错误链含 ErrWorkspaceUnavailable，实得 %v", err)
	}
}
```

- [ ] **Step 3：跑红**  
  `go test ./internal/agentd -run 'TestDispatchCallsCapability|TestDispatchNilCapabilityFailsClosed' -count=1`  
  预期：先编译红（`SetWorkspace`/`NewGitCapability`/`ErrWorkspaceUnavailable` undefined）。实现空壳后再跑：`TestDispatchCallsCapability` 断言红（prepare=0），`TestDispatchNilCapabilityFailsClosed` 在帮手已默认绑定、测试再 `SetWorkspace(nil)` 之后，Dispatch 若仍走包级函数会成功派发——那就是要消灭的双路径，必须红。

- [ ] **Step 4：最小实现——适配器文件**  
  新建 `internal/agentd/workspace_capability.go`：

```go
// workspace_capability.go —— 工作区 Capability 的 agentd 适配器（B233.4）。
//
// 职责：把既有包级 git 函数暴露为 workspace.Capability；供 Manager 持有。
// 边界：不搬 workspace.go；不写账本；不授权 push；具体类型只在组装点构造。
package agentd

import (
	"context"
	"errors"

	"github.com/Xsxdot/handoff/internal/proto"
	"github.com/Xsxdot/handoff/internal/workspace"
)

// ErrWorkspaceUnavailable 表示 Manager 未注入工作区能力。
// nil 不得解释成跳过 EnsureRepoUsable 或脏检查——调用方必须失败。
var ErrWorkspaceUnavailable = errors.New("工作区能力未注入")

// gitCapability 把包级 git 函数适配成 Capability。P1(a)：实现仍留在本包。
type gitCapability struct{}

// NewGitCapability 返回生产用工作区能力。只在组装点调用。
func NewGitCapability() workspace.Capability { return gitCapability{} }

var _ workspace.Capability = gitCapability{}

// SetWorkspace 注入工作区能力。测试可换 fake；生产由组装点绑定。
func (m *Manager) SetWorkspace(c workspace.Capability) { m.ws = c }

// Workspace 返回已注入的能力，可能为 nil。
func (m *Manager) Workspace() workspace.Capability { return m.ws }

func (m *Manager) requireWorkspace() error {
	if m == nil || m.ws == nil {
		return ErrWorkspaceUnavailable
	}
	return nil
}

func (gitCapability) EnsureRepoUsable(ctx context.Context, repo string) error {
	return EnsureRepoUsable(ctx, repo)
}

func (gitCapability) ResolveBaseline(ctx context.Context, repo, sha string) (workspace.Baseline, error) {
	b, err := ResolveBaseline(ctx, repo, sha)
	if err != nil {
		return workspace.Baseline{}, err
	}
	return workspace.Baseline{Start: b.Start, Ahead: b.Ahead, Fetched: b.Fetched}, nil
}

func (gitCapability) Prepare(ctx context.Context, req workspace.PrepareReq) (workspace.Prepared, error) {
	ws, err := PrepareWorkspace(ctx, WorkspaceReq{
		Repo: req.Repo, TaskID: req.TaskID, Branch: req.Branch, NewBranch: req.NewBranch,
		Base: req.Base, Worktree: req.Worktree, NewWorktree: req.NewWorktree, WorktreesDir: req.WorktreesDir,
	})
	if err != nil {
		return workspace.Prepared{}, err
	}
	return workspace.Prepared{
		Branch: ws.Branch, WorkDir: ws.WorkDir, Managed: ws.Managed,
		NewBranchTip: ws.NewBranchTip, PrevRef: ws.PrevRef,
		RepoDirtyCount: ws.RepoDirtyCount, RepoDirtyFiles: ws.RepoDirtyFiles,
	}, nil
}

func (gitCapability) CreateManual(ctx context.Context, repo, worktreesDir string, req workspace.ManualReq) (workspace.ManualTree, error) {
	// 刻意不把 CardIDs 传入包级函数：挂卡是应用的事。
	ws, err := CreateManualWorktree(ctx, repo, worktreesDir, proto.CreateWorktreeReq{
		Mode: req.Mode, Branch: req.Branch, Base: req.Base,
	})
	if err != nil {
		return workspace.ManualTree{}, err
	}
	return workspace.ManualTree{Path: ws.Path, Branch: ws.Branch, Head: ws.Head, Managed: ws.Managed}, nil
}

func (gitCapability) DiffRange(ctx context.Context, repo, base, head string) (string, error) {
	if err := ctx.Err(); err != nil {
		return "", err
	}
	return DiffRange(repo, base, head)
}

func (gitCapability) ReadFile(ctx context.Context, repo, rel string) (workspace.FileContent, error) {
	if err := ctx.Err(); err != nil {
		return workspace.FileContent{}, err
	}
	f, err := ReadFile(repo, rel)
	if err != nil {
		return workspace.FileContent{}, err
	}
	return workspace.FileContent{Content: f.Content, Size: f.Size, Truncated: f.Truncated, Binary: f.Binary, SHA256: f.SHA256}, nil
}

func (gitCapability) RecycleManaged(ctx context.Context, repo, workdir string) error {
	return RemoveManagedWorktree(ctx, repo, workdir)
}
```

- [ ] **Step 5：Manager 字段 + 组装点 + Dispatch 接线**  
  `manager.go` 的 `Manager` 结构体在 `gate` 字段后增加：

```go
	// ws 是工作区能力。生产由组装点注入；nil 不得跳过基线/脏检查。
	ws workspace.Capability
```

增加 import `internal/workspace`。

`Dispatch` 在 `EnsureRepoUsable` 之前插入：

```go
	if err := m.requireWorkspace(); err != nil {
		m.log.Error("dispatch 失败：工作区能力未注入",
			"project_id", req.ProjectID, "project_name", req.ProjectName)
		return nil, err
	}
```

把三处包级调用换成（保持原顺序与原日志）：

```go
	if err := m.ws.EnsureRepoUsable(ctx, repoPath); err != nil {
		return nil, err
	}
	// … ResolveDefaultBase 段不变 …
	baselineWS, err := m.ws.ResolveBaseline(ctx, repoPath, req.BaseCommit)
	if err != nil {
		return nil, err
	}
	baseline := Baseline{Start: baselineWS.Start, Ahead: baselineWS.Ahead, Fetched: baselineWS.Fetched}
```

`PrepareWorkspace(...)` 换成：

```go
	prepared, err := m.ws.Prepare(ctx, workspace.PrepareReq{
		Repo: repoPath, TaskID: taskID,
		Branch: req.Branch, NewBranch: req.NewBranch, Base: start,
		Worktree: req.Worktree, NewWorktree: req.NewWorktree,
		WorktreesDir: filepath.Join(m.cfg.DataDir, "worktrees"),
	})
	if err != nil {
		return nil, fmt.Errorf("git 工作区准备: %w", err)
	}
	ws := Workspace{
		Branch: prepared.Branch, WorkDir: prepared.WorkDir, Managed: prepared.Managed,
		NewBranchTip: prepared.NewBranchTip, PrevRef: prepared.PrevRef,
		RepoDirtyCount: prepared.RepoDirtyCount, RepoDirtyFiles: prepared.RepoDirtyFiles,
	}
```

后面 `compensateWorkspace` 仍吃本地 `Workspace`。禁止留下「ws==nil 则调包级函数」分支。

`newTestManagerWithApprover` 在 `return NewManager(...)` 之后绑定：

```go
	m := NewManager(st, hub, ads, cfg, nil, approver, newTestGate(t), logger)
	m.SetWorkspace(NewGitCapability())
	return m, st, hub
```

`TestDispatchFailedAfterWorkspaceCleansManagedWorktree` 里直接 `NewManager` 之后加 `m.SetWorkspace(NewGitCapability())`。

`cmd/agentd.go` 在 `mgr := agentd.NewManager(...)` 之后：

```go
		mgr.SetWorkspace(agentd.NewGitCapability())
		srv.SetManager(mgr)
```

`Server.SetManager`：

```go
func (s *Server) SetManager(m *Manager) {
	s.mgr = m
	if m != nil {
		m.conf = s.conf
		if m.ws == nil {
			m.SetWorkspace(NewGitCapability())
		}
		s.log.Info("manager 已挂接，配置读取切到活快照", "default_executor", s.conf().Executor.Default)
	}
}
```

`writeDispatchError` 增加（在 Dirty/Busy 之前或其余 500 之前均可，但必须 `errors.Is`）：

```go
	case errors.Is(err, ErrWorkspaceUnavailable):
		s.log.Error("dispatch 失败：工作区能力未注入", "project", projectRef, "cause", err)
		writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": err.Error()})
```

- [ ] **Step 6：跑绿**  
  测试范围命令。既有 Prepare 测试经新入口仍绿（帮手已绑定真实适配器）。`grep -n 'NewGitCapability\|gitCapability{' -- *.go` 生产代码（排除 `_test.go`）只许 `workspace_capability.go`、`cmd/agentd.go`、`server.go`。

- [ ] **Step 7：图**  
  `codegraph --repo . --view cards-B233.4-charter check`。超预算则涨 `legacyBudget` 到实际边数，`entries` 按报错原文声明 `Capability` / 方法名。禁止把 `Capability` 放进 `k_agentd_fn`。`go list -f '{{.Imports}}' ./internal/workspace` 仍不含 `internal/proto`、`internal/ledger`。

- [ ] **Step 8：日志**  
  Dispatch 入口已有。补：`requireWorkspace` 失败 Error（已写）。`Prepare` 前后沿用「工作区就绪」Info（成功）与原失败 Error。适配器内包级 git 已有 `gitExec` Info，不重复打完整 diff。

- [ ] **Step 9：注释**  
  文件头职责+边界；`SetWorkspace`/`NewGitCapability`/`ErrWorkspaceUnavailable` 文档；Dispatch 接线处注明「禁止 nil 降级包级函数」。

- [ ] **Step 10：提交**  
  `feat(B233.4): Dispatch 经 workspace.Capability 准备基线与隔离树`

---

## 5. Task 3：留存接线——Stop 留现场，归档/显式/补偿才回收

### 精确文件集

- `internal/agentd/manager.go`（`Stop`、`Done`、`compensateWorkspace`）
- `internal/agentd/reclaim.go`（`Reclaim` 问 `MayRecycle`）
- `internal/agentd/gc.go`（执行路径 `keep` 则 skip）
- `internal/agentd/manager_test.go`（退役旧 Stop 删树正向）
- `internal/agentd/reclaim_test.go`（waiting_review 专测）
- `internal/agentd/server_test.go`（HTTP `worktree_removed` 对 managed 也是 false）
- `cmd/stop.go`、新增 `cmd/stop_test.go`
- `web/src/app/task/Composer.tsx`、`Composer.test.tsx`
- `web/src/api/types.ts`、`web/src/api/client.ts`（注释）

不改状态机、不改 reclaim `--force`、不加 `retained` 字段。

### 测试范围

`go test ./internal/agentd -run 'TestStop|TestDoneRemovesManagedWorktree|TestDoneKeepsUserWorktree|TestDispatchFailedAfterWorkspaceCleansManagedWorktree|TestReclaimRefusesNonTerminal|TestReclaimRefusesWaitingReview|TestReclaimRefusesNotManaged' -count=1`  
`go test ./cmd -run 'TestStopCLIRetainCopy' -count=1`  
`go test ./internal/workspace -run TestMayRecycle -count=1`  
Web：`cd web && npm test -- src/app/task/Composer.test.tsx`（`package.json` 的 `test` 脚本是 `vitest run`）。

### 接缝

缝级。入口：`Manager.Stop` / `Manager.Done` / `Manager.Reclaim` / `Manager.GC` / `Manager.compensateWorkspace`（冻结 50–64）；CLI `runStop`（`Client.Stop` 真实 JSON）；Web `stopTask`。`MayRecycle` 表测不得顶替 Stop 接线。

### 步骤

- [ ] **Step 1：判据在基线跑**  
  `go test ./internal/agentd -run 'TestStopRemovesManagedWorktree|TestStopReportsWorktreeRemoved|TestDoneRemovesManagedWorktree' -count=1`  
  预期 **ok 且旧正向要求删树**。T3 改实现后这两支必须先改测试再 impl，否则新契约假红。

- [ ] **Step 2：退役旧正向，写新失败测试**  
  将 `TestStopRemovesManagedWorktree` **整函数替换**为：

```go
func TestStopRetainsManagedWorktree(t *testing.T) {
	repo := initTestRepo(t)
	fk := fake.New(nil)
	m, st, _ := newTestManagerWithApprover(t, map[string]executor.Adapter{"fake": fk}, "fake", nil)
	spy := &countingCap{inner: NewGitCapability()}
	m.SetWorkspace(spy)
	pid := registerTestProject(t, m, repo)
	task, err := m.Dispatch(context.Background(), DispatchReq{
		ProjectID: pid, Prompt: "x", Executor: "fake", NewWorktree: true,
	})
	if err != nil {
		t.Fatal(err)
	}
	workDir := task.WorkDir
	recycleBefore := spy.recycle
	removed, err := m.Stop(context.Background(), task.ID)
	if err != nil {
		t.Fatalf("Stop: %v", err)
	}
	if removed {
		t.Fatal("成功 Stop 后 worktree_removed 必须为 false（契约 C-6）")
	}
	if spy.recycle != recycleBefore {
		t.Fatalf("Stop 零次 RecycleManaged，实得 %d→%d", recycleBefore, spy.recycle)
	}
	if _, err := os.Stat(workDir); err != nil {
		t.Fatalf("stop 后 managed worktree 必须仍在: %v", err)
	}
	cur, _ := st.GetTask(task.ID)
	if cur.State != proto.TaskStateFailed {
		t.Fatalf("stop 后 state=%s, want failed", cur.State)
	}
	if out := gitOut(t, repo, "branch", "--list", "handoff/"+id8(task.ID)); out == "" {
		t.Fatalf("stop 不得删除任务分支")
	}
}
```

`TestStopReportsWorktreeRemoved`：managed 分支改为 `if removed { t.Fatal("managed stop 应返回 false") }`；原地模式保持 false。注释改成 C-6 口径。

`TestReclaimRefusesWaitingReview` 追加到 `reclaim_test.go`（照抄 `TestReclaimRefusesNonTerminal`，状态改 `proto.TaskStateWaitingReview`）：

```go
func TestReclaimRefusesWaitingReview(t *testing.T) {
	m, repo := newReclaimManager(t)
	wt := newWorktree(t, repo, "wt-wr", "f-wr")
	id := seedTerminalTask(t, m, repo, wt, "f-wr", proto.TaskStateWaitingReview, true)
	_, err := m.Reclaim(context.Background(), id, false)
	if !errors.Is(err, ErrReclaimNotTerminal) {
		t.Fatalf("waiting_review 应拒绝，实得 %v", err)
	}
	if _, serr := os.Stat(wt); serr != nil {
		t.Fatalf("拒绝后工作树必须保留：%v", serr)
	}
}
```

`newReclaimManager` 不走 Dispatch，不必强绑 Capability（Reclaim 删除仍用自己的 git 路径）。若实现误要求 `m.ws`，测试会红——那时绑 `NewGitCapability()`，**不要**为过测试跳过 `MayRecycle`。

- [ ] **Step 3：跑红**  
  `go test ./internal/agentd -run 'TestStopRetainsManagedWorktree|TestStopReportsWorktreeRemoved' -count=1`  
  预期：Stop 仍删树 → Stat 失败或 `removed==true` 断言红。确认不是 typo。

- [ ] **Step 4：最小实现——Stop / Done / compensate / Reclaim / GC**

**Stop**（`manager.go`）：删除 `if cur.WorktreeManaged && cur.WorkDir != "" { RemoveManagedWorktree ... worktreeRemoved=true }` 整段。在 transit(failed) 成功之后、`purgeTaskCache` 之前插入：

```go
	dec := workspace.MayRecycle(workspace.RecycleInput{
		Managed:  cur.WorktreeManaged,
		Manual:   false,
		Terminal: true, // 刚迁 failed
		State:    string(proto.TaskStateFailed),
		Trigger:  workspace.TriggerStop,
	})
	m.log.Info("stop 留存判据", "task", taskID, "decision", dec,
		"managed", cur.WorktreeManaged, "workdir", cur.WorkDir, "trigger", workspace.TriggerStop)
	if dec != workspace.RetainKeep {
		m.log.Error("stop 判据不是 keep，仍不回收（冻结 62）", "task", taskID, "decision", dec)
	}
	worktreeRemoved = false
```

更新 `Stop` 文档注释：取消留存；`worktreeRemoved` 成功路径恒 false；显式 reclaim/gc 才清；有 commit 不构成授权。

**Done**：把 `if cur.WorktreeManaged && cur.WorkDir != "" { RemoveManagedWorktree }` 换成：

```go
	dec := workspace.MayRecycle(workspace.RecycleInput{
		Managed:  cur.WorktreeManaged,
		Manual:   false,
		Terminal: true,
		State:    string(proto.TaskStateCompleted), // 刚迁 completed，禁止喂 waiting_review
		Trigger:  workspace.TriggerDone,
	})
	m.log.Info("done 回收判据", "task", taskID, "decision", dec,
		"managed", cur.WorktreeManaged, "workdir", cur.WorkDir)
	if dec == workspace.RetainRecycle {
		if err := m.requireWorkspace(); err != nil {
			m.log.Error("done 需回收但工作区能力未注入", "task", taskID, "cause", err)
		} else if werr := m.ws.RecycleManaged(ctx, cur.RepoPath, cur.WorkDir); werr != nil {
			m.log.Error("清理 managed worktree 失败", "task", taskID, "workdir", cur.WorkDir, "cause", werr)
			// 既有：只降级 progress，不回滚 completed
			if evt, aerr := m.st.AppendEvent(taskID, proto.EventTypeProgress, progressPayload{
				Text: worktreeCleanupHint(taskID, werr),
			}); aerr != nil {
				m.log.Error("追加 worktree 清理失败事件失败", "task", taskID, "cause", aerr)
			} else {
				m.hub.Publish(evt)
			}
		} else {
			m.log.Info("managed worktree 已清理", "task", taskID, "workdir", cur.WorkDir)
		}
	} else {
		m.log.Info("done 按判据留存工作树", "task", taskID, "decision", dec, "workdir", cur.WorkDir)
	}
```

**compensateWorkspace**：`if ws.Managed { RemoveManagedWorktree }` 换成：

```go
	if ws.Managed {
		dec := workspace.MayRecycle(workspace.RecycleInput{
			Managed: true, Manual: false, Terminal: false, State: "",
			Trigger: workspace.TriggerCompensate,
		})
		m.log.Info("补偿回收判据", "task", taskID, "decision", dec, "workdir", ws.WorkDir)
		if dec != workspace.RetainRecycle {
			m.log.Info("补偿按判据不回收", "task", taskID, "decision", dec)
		} else if err := m.requireWorkspace(); err != nil {
			m.log.Error("补偿需回收但工作区能力未注入", "task", taskID, "cause", err)
			return
		} else if err := m.ws.RecycleManaged(ctx, repo, ws.WorkDir); err != nil {
			m.log.Error("补偿清理 managed worktree 失败，保留分支待查",
				"repo", repo, "workdir", ws.WorkDir, "branch", ws.Branch,
				"retry", "handoff reclaim "+shortTaskID(taskID), "cause", err)
			return
		}
	} else {
        // 原用户树切回 PrevRef 段保持不变（MayRecycle keep，不 RecycleManaged）
```

**Reclaim**：在 `IsTerminal` / `WorktreeManaged` 既有门之后、`repoWorktrees` 之前：

```go
	dec := workspace.MayRecycle(workspace.RecycleInput{
		Managed:  cur.WorktreeManaged,
		Manual:   false,
		Terminal: cur.State.IsTerminal(),
		State:    string(cur.State),
		Trigger:  workspace.TriggerExplicit,
	})
	m.log.Info("reclaim 回收判据", "task", taskID, "decision", dec, "state", cur.State)
	if dec != workspace.RetainRecycle {
		m.log.Warn("reclaim 被判据拒绝", "task", taskID, "decision", dec, "state", cur.State)
		if !cur.State.IsTerminal() {
			return nil, fmt.Errorf("任务 %s 状态 %s，%w", taskID, cur.State, ErrReclaimNotTerminal)
		}
		if !cur.WorktreeManaged || cur.WorkDir == "" {
			return nil, fmt.Errorf("任务 %s：%w", taskID, ErrReclaimNotManaged)
		}
		return nil, fmt.Errorf("任务 %s 回收判据为 %s，拒绝回收", taskID, dec)
	}
```

删除动作仍走现有 `gitRun worktree remove`（保留 `--force`/prune）。不要改成 `RecycleManaged`。

**GC** `appendGCWorktreesExecute`：在 `Reclaim` 之前，对终态 managed 行问 `MayRecycle`（TriggerExplicit）；`keep` 则 `status=skipped` 且目录不删。非 managed 既有 skip 保持。

- [ ] **Step 5：CLI / Web 文案（P3-a）**  
  `cmd/stop.go` 抽出：

```go
func stopOutcomeLine(taskID string, removed bool) string {
	if removed {
		return fmt.Sprintf("任务 %s 已中止（状态 failed，managed worktree 已删除，分支保留）\n", taskID)
	}
	return fmt.Sprintf("任务 %s 已中止（状态 failed，现场已留存，显式 reclaim/gc 才清，分支保留）\n", taskID)
}

func runStop(cmd *cobra.Command, c *client.Client, taskID string) error {
	removed, err := c.Stop(cmd.Context(), taskID)
	if err != nil {
		return err
	}
	fmt.Fprint(cmd.OutOrStdout(), stopOutcomeLine(taskID, removed))
	return nil
}
```

`RunE` 在 `newTargetClient` 成功后调 `runStop`。文件头边界改为「成功 Stop 不删树；文案不得把 false 说成清理失败」。

新建 `cmd/stop_test.go`（照抄 `cmd/gc_test.go` 的 httptest + cobra 夹具）：

```go
func TestStopCLIRetainCopy(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		if r.URL.Path != "/api/tasks/T1/stop" || r.Method != http.MethodPost {
			t.Fatalf("unexpected %s %s", r.Method, r.URL.Path)
		}
		w.WriteHeader(http.StatusOK)
		_, _ = w.Write([]byte(`{"status":"stopped","worktree_removed":false}`))
	}))
	t.Cleanup(ts.Close)
	var out bytes.Buffer
	cmd := &cobra.Command{}
	cmd.SetContext(context.Background())
	cmd.SetOut(&out)
	if err := runStop(cmd, client.New(ts.URL, "tok"), "T1"); err != nil {
		t.Fatalf("runStop: %v", err)
	}
	got := out.String()
	if !strings.Contains(got, "留存") {
		t.Fatalf("成功未删树应提示留存，实得 %q", got)
	}
	if strings.Contains(got, "已删除") || strings.Contains(got, "清理失败") {
		t.Fatalf("不得把 false 说成已删除或清理失败，实得 %q", got)
	}
}
```

这是穿过真实 JSON 边界的投影测试（client 解码 `worktree_removed` + CLI 拼串）。

`Composer.tsx` 把 false 文案改为 `'已停止；现场已留存，显式 reclaim/gc 才清。'`。true 文案可留（契约禁止成功 Stop 报 true；若出现即回归）。

`Composer.test.tsx`：mock `stopTask` 为 `{ worktree_removed: false }`，点停止并确认后：

```ts
await waitFor(() => {
  expect(screen.getByText(/现场已留存/)).toBeInTheDocument()
})
expect(screen.queryByText(/清理失败/)).not.toBeInTheDocument()
```

确认按钮交互照抄本文件「done 需二次确认」的 click 模式；若双按钮重名，用 `getAllByRole` 点对话框那一个。

`types.ts` / `client.ts` 注释改为：成功 Stop 后恒 false，表示现场留存，不是清理失败。

- [ ] **Step 6：跑绿**  
  测试范围。`TestDoneRemovesManagedWorktree` 必须仍绿（归档回收）。`TestDispatchFailedAfterWorkspaceCleansManagedWorktree` 仍绿（补偿回收）。`TestStopRetainsManagedWorktree` 绿。

- [ ] **Step 7：日志**  
  Stop/Done/Reclaim/compensate 每条问判据时 Info：task、trigger、decision、managed、workdir。keep 不静默。Recycle 失败 Error 带 cause。成功回收 Info。Stop 成功路径 Info 已有 `worktree_removed`。

- [ ] **Step 8：注释**  
  Stop 头注释重写「为什么不删」。Done 注明「State 必须用 completed 问 MayRecycle」。CLI/Web 注明 false≠失败。

- [ ] **Step 9：提交**  
  `feat(B233.4): Stop 留存 managed 工作树，回收改问 MayRecycle`

---

## 6. Task 4：结果引用与 diff 右端

### 精确文件集

- `internal/agentd/workspace_capability.go`（`assembleResultRef`）
- `internal/agentd/server.go`（`handleTaskDiff` / `handleTaskFile` 经 Capability）
- `internal/agentd/diffbase_test.go`（补 HEAD≠任务分支）
- `internal/agentd/manager_test.go` 或新建 `internal/agentd/resultref_test.go`（BaseCommit 不漂；assembleResultRef）

不新增 HTTP 路径。不把 `ResultRef` 写入 JSON。

### 测试范围

`go test ./internal/agentd -run 'Diff|ReadFile|TaskDiffTarget|BaseCommit|ResultRef|assembleResultRef' -count=1`

### 接缝

缝级。入口：`Server.handleTaskDiff`（`GET /api/tasks/{id}/diff`）；`taskDiffTarget` 被该 handler 调用。`NewResultRef` 由 `assembleResultRef` 在同一 handler 调用——测 `assembleResultRef` 若只直喂、不经 handler，算内部锁。本票用 handleTaskDiff 的既有行为测试锁右端，再用 `assembleResultRef` 锁 commit 空/非法（内部锁声明：HTTP 不回传 ResultRef，从声明缝构造不出字段级断言；C-5 禁止新序列化边界）。

### 步骤

- [ ] **Step 1：判据在基线跑**  
  `go test ./internal/agentd -run 'TestTaskDiffTarget' -count=1` 预期 ok。

- [ ] **Step 2：写测试**

`diffbase_test.go` 追加（既有 `TestTaskDiffTargetFallsBackToRepoWhenWorktreeGone` 已锁右端字符串；本条钉「不得是 HEAD」在主仓 HEAD 存在时）：

```go
func TestTaskDiffTargetFallbackHeadRevIsNotHEAD(t *testing.T) {
	gone := filepath.Join(t.TempDir(), "gone")
	task := &proto.Task{RepoPath: t.TempDir(), WorkDir: gone, Branch: "handoff/deadbeef"}
	_, head := taskDiffTarget(task)
	if head == "HEAD" {
		t.Fatal("树已回收时右端不得是主仓 HEAD")
	}
	if head != "handoff/deadbeef" {
		t.Fatalf("右端应是任务分支，实得 %q", head)
	}
}
```

`resultref_test.go`：

```go
func TestAssembleResultRefEmptyCommitAllowed(t *testing.T) {
	m, _, _ := newTestManagerWithApprover(t, map[string]executor.Adapter{"fake": fake.New(nil)}, "fake", nil)
	ref, err := m.assembleResultRef(context.Background(), &proto.Task{Branch: "feat/x"}, "/no/such/repo", "HEAD")
	if err != nil {
		t.Fatalf("commit 未知应允许空：%v", err)
	}
	if ref.Commit != "" {
		t.Fatalf("不得编造 commit：%+v", ref)
	}
}

func TestBaseCommitDoesNotDriftWithDefaultBranch(t *testing.T) {
	repo := initTestRepo(t)
	fk := fake.New(nil)
	m, st, _ := newTestManagerWithApprover(t, map[string]executor.Adapter{"fake": fk}, "fake", nil)
	pid := registerTestProject(t, m, repo)
	task, err := m.Dispatch(context.Background(), DispatchReq{
		ProjectID: pid, Prompt: "x", Executor: "fake", NewWorktree: true,
	})
	if err != nil {
		t.Fatal(err)
	}
	base := task.BaseCommit
	gitT(t, repo, "commit", "--allow-empty", "-m", "after-dispatch")
	cur, err := st.GetTask(task.ID)
	if err != nil {
		t.Fatal(err)
	}
	if cur.BaseCommit != base {
		t.Fatalf("BaseCommit 不得随默认分支漂：%q → %q", base, cur.BaseCommit)
	}
}
```

照抄 `initTestRepo` / `gitT` / `newTestManagerWithApprover`。

既有 `ReadFile` 逃逸测试保持。`handleTaskFile` 改走 Capability 后必须仍 400。

- [ ] **Step 3：实现 `assembleResultRef` + handler 接线**

```go
// assembleResultRef 在审阅现场组装结果引用。commit 取 rev-parse；失败则空串，不编造。
func (m *Manager) assembleResultRef(ctx context.Context, task *proto.Task, repo, headRev string) (workspace.ResultRef, error) {
	commit := ""
	if out, _, err := gitProbe(ctx, repo, "rev-parse", headRev); err == nil {
		got := strings.TrimSpace(out)
		if workspace.IsCommitSHA(got) {
			commit = got
		} else {
			m.log.Warn("rev-parse 不是 40 位小写 hex，commit 留空", "repo", repo, "head", headRev, "got", got)
		}
	}
	path, branch := "", ""
	if task != nil {
		branch = task.Branch
		path = task.Workdir()
		if _, err := os.Stat(path); err != nil {
			path = ""
		}
	}
	ref, err := workspace.NewResultRef(repo, branch, commit, path)
	if err != nil {
		m.log.Error("组装结果引用失败", "repo", repo, "branch", branch, "commit", commit, "cause", err)
		return workspace.ResultRef{}, err
	}
	m.log.Info("结果引用已组装", "repo", ref.Repo, "branch", ref.Branch, "commit", ref.Commit, "path", ref.Path)
	return ref, nil
}
```

`handleTaskDiff` 在 `DiffRange` 之前（`s.mgr` 未注入则沿用既有 503 模式）：

```go
	if s.mgr == nil {
		writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": "manager 未就绪"})
		return
	}
	if _, err := s.mgr.assembleResultRef(r.Context(), task, repo, headRev); err != nil {
		writeJSON(w, http.StatusInternalServerError, map[string]string{"error": truncateRunes(err.Error(), 200)})
		return
	}
	if err := s.mgr.requireWorkspace(); err != nil {
		writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": err.Error()})
		return
	}
	diff, err := s.mgr.Workspace().DiffRange(r.Context(), repo, base, headRev)
```

不要把 ResultRef 放进 `writeJSON`。`handleTaskFile` 同样经 `s.mgr.Workspace().ReadFile`，映射回 `proto.FileRead` 再写 JSON（键名保持 `content/size/truncated/binary/sha256`）。这是手写投影：既有 file 测试必须穿过它仍绿。

- [ ] **Step 4：跑绿 + 日志 + 注释**  
  DiffRange 空/- 前缀失败保持包级行为（适配器转发）。成功路径打「结果引用已组装」（不含 diff 正文）。

- [ ] **Step 5：提交**  
  `feat(B233.4): 审阅 diff 组装 ResultRef 且右端不用主仓 HEAD`

---

## 7. Task 5：不挂卡——工作区函数零写账本

### 精确文件集

- `internal/agentd/projectadmin.go`（`handleProjectWorktreeCreate` 改走 `Capability.CreateManual`，之后才 `attachCardBaseBranches`）
- `internal/agentd/projectadmin_test.go` 或新建测试
- **不含** `forward.go`（零 diff）

### 测试范围

`go test ./internal/agentd -run 'Worktree|CardBase|ForwardWorktree|CreateManual|PrepareReqHasNoCardIDs' -count=1`  
`go test ./internal/workspace -run 'TestPrepareReqHasNoCardIDs|TestCapabilityHasNoPush' -count=1`  
`git diff --name-only` 不得含 `internal/agentd/forward.go`

### 接缝

缝级。入口：`Server.handleProjectWorktreeCreate`（`POST /api/projects/{name}/worktrees`）。冻结 3–7、15。

### 步骤

- [ ] **Step 1：基线**  
  `go test ./internal/workspace -run TestPrepareReqHasNoCardIDs -count=1` 预期 ok。`grep SetCardBaseBranch internal/agentd/manualworktree.go` 预期无命中（本节点 grep 事实：CreateManualWorktree 零引用 CardIDs）。

- [ ] **Step 2：写失败测试**  
  HTTP 建树带 `card_ids`：间谍 Capability 的 `create`≥1，且 `ManualReq` 无卡号；假 ledger 的 `SetCardBaseBranch` 在 CreateManual **返回之后**才被调用。若现有 `projectadmin_test.go` 已有「建树后挂卡」测试，在其上加「CreateManual 调用时 ledger 计数仍为 0」。

用 `countingCap` + 可计数的 ledger stub。若项目已有 ledger 测试双，照抄 `projectadmin_test.go` 里 `SetManager`/`ledger` 注入方式（文件名：`internal/agentd/projectadmin_test.go`）。断言逐条：

1. `POST` 200，响应含树 path。  
2. 间谍 `create >= 1`。  
3. 间谍实现体内不可见 `SetCardBaseBranch`（countingCap 不调 ledger）。  
4. `attachCardBaseBranches` 之后假 ledger 调用次数 = `len(card_ids)`（应用侧）。  
5. 一张卡失败不删树（既有注释语义；缺则补）。

- [ ] **Step 3：实现**  
  `handleProjectWorktreeCreate` 在解出 `loc` 之后：

```go
	if s.mgr == nil {
		writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": "manager 未就绪"})
		return
	}
	if err := s.mgr.requireWorkspace(); err != nil {
		s.log.Error("建树失败：工作区能力未注入", "name", name, "cause", err)
		writeJSON(w, http.StatusServiceUnavailable, map[string]string{"error": err.Error()})
		return
	}
	tree, err := s.mgr.Workspace().CreateManual(r.Context(), loc.Path,
		filepath.Join(s.conf().DataDir, "worktrees"),
		workspace.ManualReq{Mode: req.Mode, Branch: req.Branch, Base: req.Base})
	if err != nil {
        // 原 ErrBadWorktreeReq → 400 映射保持
	}
	ws := proto.Workspace{Path: tree.Path, Branch: tree.Branch, Head: tree.Head, Managed: tree.Managed}
	if len(req.CardIDs) > 0 && s.ledger != nil {
		ws = s.attachCardBaseBranches(ws, req.CardIDs, s.ledgerActor(r))
	}
```

禁止把 `req.CardIDs` 传入 `ManualReq`。禁止在 `CreateManual` 之前写账本。

- [ ] **Step 4：日志**  
  入口已有 `card_count`。补：CreateManual 前 Info（name/repo/mode/branch，**不**打 card_ids 进能力调用）；成功后 Info path；挂卡段沿用 `attachCardBaseBranches` 已有日志。能力未注入 Error。

- [ ] **Step 5：跑绿 + 提交**  
  `feat(B233.4): 手工建树经 Capability，挂卡留在应用`

---

## 8. Task 6：接缝回归、CHANGELOG、真机清单

### 精确文件集

- T1–T5 已列测试入口（本票只加回归与文档）
- `CHANGELOG.md` `[Unreleased]`（P5-a）
- 本 plan 真机清单（给协调者，机内不写绿）

### 测试范围

`go build -o /dev/null .`  
`go test ./internal/workspace/ ./internal/agentd/ ./internal/client/ ./cmd/ -count=1`  
（沙箱 `Operation not permitted` 记原文，不归因。）  
`codegraph --repo . --view cards-B233.4-charter check` → `fails: []`  
`git diff --name-only` 相对本卡起点不得含 `internal/agentd/forward.go`  
Web：`Composer.test.tsx` 仍绿。

全量测试不属于本 task（三段律：集成节点才全量）。

### 接缝回归（每条可判 pass/fail）

1. **普通派发缝**：`Dispatch` + `NewWorktree` → 隔离树存在、`WorktreeManaged==true`、`BaseCommit` 匹配 `^[0-9a-f]{40}$` 或空仓空；间谍 `Prepare>=1`。互斥参数拒发（既有 `TestPrepareWorkspaceMutualExclusionAndInjection`）。
2. **复用用户树脏**：拒发，错误链含 `ErrDirtyWorktree`，用户树内容仍在。
3. **取消缝**：成功任务 `Stop` → 树仍在、`failed`、`worktree_removed=false`；随后 `Reclaim` 才删 managed。
4. **待验收缝**：`waiting_review` 时 `Reclaim`/`GC` 不删树（`TestReclaimRefusesWaitingReview`）。
5. **归档缝**：`Done` 后 managed 目录消失、分支仍在。
6. **挂卡缝**：HTTP 建树带 card_ids → 树在、账本有基线；工作区 CreateManual 路径 `SetCardBaseBranch` 次数=0。
7. **diff 缝**：树在用 HEAD；Done 后用任务分支，不是主仓 HEAD。
8. **变异**（acceptance 用，本票测试必须能红）：把 Stop 再调 `RecycleManaged` → 故事 2 / `TestStopRetainsManagedWorktree` 红。
9. CHANGELOG `[Unreleased]` 含 Stop 留存。
10. 真机项见下，本票不把未跑的真机写成绿。

若 1–7 已由 T2–T5 测试覆盖，本票不复制粘贴第二份；跑触及包确认仍绿。缺哪条补哪条，禁止用计数（「新增 N 个测试文件」）代替行为。

CHANGELOG `[Unreleased]` → `### 变更` 追加一条（与 B233.1 条并列，不删旧条）：

```
- **Stop/取消不再删除 managed 工作树（B233.4）。** `handoff stop` 与控制台「停止任务」只把任务落 failed，现场留到显式 `handoff reclaim` / `gc` 或归档 `handoff done`。CLI/Web 提示改为留存，不再把 `worktree_removed=false` 说成清理失败。
```

### 真机清单（归协调者执行；机内不得写成已绿）

1. `handoff dispatch --new-worktree`：独立 worktree + 40 位 `BaseCommit`；主仓脏仍成功。**未验证，需真机。**
2. 脏的用户 worktree / 原地派发：拒发，用户文件仍在。**未验证，需真机。**
3. `handoff stop`：failed；managed 目录仍在；CLI **不**打印「已删除」；人可进树看现场。**未验证，需真机。**
4. `waiting_review` 后 `reclaim`/`gc`：拒绝或跳过，目录仍在；`handoff diff` 仍能出相对 `BaseCommit` 的素材。**未验证，需真机。**
5. `handoff done` 之后：managed 目录消失，任务分支仍在；再 `diff` 走任务分支而非主仓 HEAD。**未验证，需真机。**
6. stop 之后再 `handoff reclaim`：managed 树被清。**未验证，需真机。**
7. 用户自带 worktree 任务 done/stop/gc：该树仍在。**未验证，需真机。**
8. 控制台建树带卡号：树在 `worktrees/manual/...`；卡基线写下；建树函数路径无账本错误。**未验证，需真机。**
9. Web 点「停止任务」：提示为留存而非「清理失败」。**未验证，需真机。**
10. executor 启动失败：刚建的 managed 树被补偿回收。**未验证，需真机。**
11. agentd 在 Stop 之后、reclaim 之前重启：树仍在，任务仍 failed，reclaim 仍可用。**未验证，需真机。**
12. Windows（若验收范围含）：文件占用下 `worktree remove` 失败是否仍只走 reclaim `--force`、Stop 是否仍留存。**未验证，需真机。**
13. 不验证 push（OOS）。抽查既有节点 pass 推 origin 不得无故失败。**未验证，需真机。**

### 步骤

- [ ] 跑测试范围；沙箱失败贴原文。  
- [ ] 写 CHANGELOG。  
- [ ] `grep -n 'RemoveManagedWorktree\|RecycleManaged' internal/agentd/manager.go`：Stop 函数体内零命中。  
- [ ] 日志/注释收口自检（implement 铁律）。  
- [ ] 提交：`docs(B233.4): CHANGELOG 记录 Stop 留存；接缝回归`

---

## 9. 五项检查

### 9.1 缺陷族对抗审查

项目清单：`docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md` §3。顶部没有 `基线版本：charter@<commit>`——按项目清单加严，并并入承重安全属性。清单债不在本卡改。

**生命周期 / 状态机中断**  
Prepare 内部失败仍由 `rollbackWorkspace` 自收。Dispatch 在 executor 接管前失败：`compensateWorkspace` + `TriggerCompensate` → 回收 managed。Stop 中途崩溃：本卡 Stop 本就不回收，崩溃与成功同为留存；收口靠 reclaim/gc 重入（幂等 `already_absent` 既有）。Done 回收失败：既有降级 progress，不回滚 completed。`gitExec` 的 `handoff-empty-hooks-*` 仍由 OS 临时目录回收。**无新孤儿种类，因为**不新增进程/工单表/工作树种类。agentd 重启后 Capability 由组装点再绑；未完成 managed 树不会因重启被 Stop 误删（Stop 要人触发）。

**静默失败 / 误导报错**  
Stop 报 `worktree_removed=true` 却没删 = 本卡消灭的窗口（C-6，成功恒 false）。CLI/Web 把 false 说成清理失败 = T3 改文案。nil Capability 当免检 = T2 禁止。Prepare 失败不得返回成功 Task（既有）。挂卡失败不得把建树报失败（既有单项 Error）。diff 回收后用主仓 HEAD = T4 锁右端。Done 回收失败不得假装树已无（日志 + progress）。

**跨平台假设**  
git worktree 路径、`os.Stat` 判存在、Windows 文件占用：沿用 5×400ms 重试，本卡不改超时。**未验证，需真机**（T6 清单 12）。`id8` 按字节截断，**无新路径分隔假设**。`OpenRoot` 逃逸 Windows 符号链接 **未验证，需真机**。P3 只改字符串，无 webview 新假设。

**假红 / 假绿测试**  
Ticket 0 金样本不是故事 2。`TestStopRemovesManagedWorktree` 旧正向必须退役。`MayRecycle` 表绿 ≠ Stop 接线绿——T3 穿过 `Manager.Stop`。测试锁调用方依赖的行为（树在/不在、右端字符串、Prepare 被调用），不锁适配器函数名。P1(a) 不搬家，测试不因改包路径无意义地红。沙箱失败记原文。

**门禁绕过**  
回收门是 `MayRecycle` + Reclaim 终态。Stop 不得直调 `RemoveManagedWorktree`。用户树所有触发源 keep。手工树无任务行进不了 Reclaim；GC 不扫 `worktrees/manual`。脏检查：new-worktree 免主仓脏，用户树/原地不免。读文件逃逸走 `ReadFile`。TOCTOU：Reclaim 动手前重读快照保持。push 不进 Capability。

**序列化边界**  
本卡不新增 Task/HTTP 字段。`worktree_removed` 既有布尔，client 真 JSON 测试保持。CLI `stopOutcomeLine` 与 Web `setMessage` 是两处手写投影，T3 各加断言；CLI 测试穿过 `Client.Stop` 解码。`ResultRef` 只进程内 + 日志，不进 show/HTTP（否则退回 contract）。`PrepareReq` 不经 JSON；`CreateWorktreeReq.CardIDs` 仍是应用字段。`handleTaskFile` 经 `FileContent` 映射回 `proto.FileRead`：既有 file JSON 测试必须穿过。

**枚举新值过既有白名单**  
不新增 TaskState / EventType。生产 switch 用 `TriggerStop` 等常量，不得手写 `"stop"` 拼错。不把 `RetainKeep` 写进 `Ticket.Kind`。

**承重安全（必须能变红）**

| 属性 | 锁 |
|---|---|
| Stop 不回收 managed | T3 Stat 存在 + recycle 计数不变 |
| 非 managed 永不按任务垃圾删 | T3 GC skip + Reclaim `ErrReclaimNotManaged` |
| 手工树不被 GC 扫到 | 既有 GC 只走任务表；T5 不给手工树任务行 |
| waiting_review 不能 reclaim | T3 `TestReclaimRefusesWaitingReview` |
| nil Capability 不是免检 | T2 `TestDispatchNilCapabilityFailsClosed` |
| 工作区函数零写卡 | T5 间谍计数 |
| 读文件逃逸失败 | T4 / 既有 |
| BaseCommit 不随默认分支漂 | T4 |
| diff 回收后右端不是主仓 HEAD | T4 |
| 无 Push | Ticket 0 `TestCapabilityHasNoPush` |

### 9.2 序列化边界设问

| 字段 | 产生 | 手写投影 | 穿过边界的测试 |
|---|---|---|---|
| `worktree_removed` | `Manager.Stop` 返回值 | HTTP JSON；`Client.Stop` 解码；CLI `stopOutcomeLine`；Web `setMessage` | `TestStopParsesWorktreeRemoved`（缺字段 vs false）；`TestStopCLIRetainCopy`；Composer 留存文案 |
| `ResultRef` | `assembleResultRef` | 仅日志，无 wire | 不进 HTTP；禁止当已发布字段 |
| `FileContent` → `FileRead` | `Capability.ReadFile` | `handleTaskFile` 映射 | 既有 file 路由测试 |
| `PrepareReq` | 进程内 | 无 JSON | 不适用 |
| `CreateWorktreeReq.CardIDs` | HTTP 应用 | 不进 `ManualReq` | T5 |

用可空/缺字段区分：`worktree_removed` 缺席 = false（已锁），不是「未知」。不新增 `*bool`。

### 9.3 上下文预算

每个 task 文件集已圈出。不把整包 `internal/agentd`（89 文件 / 28308 行）当派发上下文。架构法第三条命中已在 breakdown 回答：不插竖切还债卡。`work*` 家族其余文件不随本卡扩权。

### 9.4 类型标注

`d_workspace` boundary：机内锁 Capability/MayRecycle/编码；git 现实行为走 T6 真机清单。  
`d_orchestration` logic：Dispatch/Stop/Done 可闭环。  
`d_gateway` / `d_cli` / `d_web`：HTTP 形状保持；文案真机清单 3、9。

### 9.5 接缝覆盖（双向）

spec 测试决定：

| 缝 | 入口符号 | 锁它的测试 |
|---|---|---|
| PrepareWorkspace（Dispatch）new-worktree / 复用 / 互斥 | `Manager.Dispatch` | T2 `TestDispatchCallsCapability`；既有 Prepare 互斥/脏检查 |
| 结果引用对上准确 commit | `Server.handleTaskDiff` / `taskDiffTarget` / `assembleResultRef` | T4 右端 + BaseCommit 不漂 |
| 清理：未验收不删；取消留存 | `Manager.Stop` / `Reclaim` / `GC` / `Done` | T3 Stop 留存、waiting_review reclaim、Done 仍回收 |
| 不挂卡 | `Server.handleProjectWorktreeCreate` / `Capability.CreateManual` | T5 |

测试 → 缝：上表每支入口都在缝上。内部锁仅 T1 对齐测试、T4 `assembleResultRef` 空 commit（HTTP 不回传该结构）。  
缝 → 测试：四条缝均有缝级断言。

退路：无「若意外先绿就改成直喂 X」的入口改写。T1 ResolveBaseline 大写 sha 今日可能已绿，已声明那不是新行为的红绿，红绿对象是删第二套正则。

---

## 10. 自审三查

**spec 覆盖**

| 故事 / 契约组 | task |
|---|---|
| 故事 1 普通派发 new-worktree | T2+T4+T6 |
| 故事 2 取消后现场仍在 | T3+T6 |
| 故事 3 waiting_review 清理不拆树 | T3+T6 |
| 故事 4 带 card_ids 建树：树在工作区、卡在应用 | T5+T6 |
| 冻结 1–15 能力面 | T2/T5 + Ticket 0 |
| 16–24 基线身份 | T1+T4 |
| 25–40 隔离与准备 | T2 + 既有 Prepare |
| 41–49 结果引用与 diff | T4 |
| 50–64 留存与清理 | T3 |
| 65–75 生产入口与字面值 | T2 保持入口；超时不改；T1/Ticket 0 字面值 |

**占位符扫描**  
无 TBD。测试复用夹具已指认文件名：`manager_test.go` 的 `newTestManagerWithApprover`/`registerTestProject`/`initTestRepo`/`fake.New`；`workspace_test.go` 的 `initTestRepo`/`gitT`/`gitOut`；`reclaim_test.go` 的 `newReclaimManager`/`seedTerminalTask`/`newWorktree`；`cmd/gc_test.go` 的 httptest+cobra。T5 ledger 间谍「照抄 `projectadmin_test.go` 注入方式」已声明——断言逐条可判。内部锁已在 §3 接缝与 §9.5 声明。

**跨 task 签名**  
§2 Produces/Consumes 逐字。`Capability` 七方法与冻结 4.1 一致。`MayRecycle(RecycleInput) Decision` 与冻结 4.2 一致。`NewManager` 签名不改。`Stop` 仍 `(worktreeRemoved bool, err error)`。

**派发前自审**  
真机清单与「本 task 由协调者执行」仅 T6 真机项，标注机内不写绿。无步骤驱动 handoff CLI。

**收口自查**  
① 基线命令均亲自跑到退出码，结论未写未跑的测试。② 本轮未调 handoff CLI、未起新 executor。③ 未写实现代码。
