# B156.3.4 实现台账（implement 轮）

> 卡：B156.3.4 ｜ 分支：cards/B156.3.4-charter-2 ｜ plan：docs/superpowers/plans/b156.3.4-plan.md
> 边干边落：每确立一个事实即追加一行；未跑到结果的命令不写结论。

## Task 0：基线复核 + K3 前置确认

- 起点状态：分支 cards/B156.3.4-charter-2 @ 05876a28（plan 提交），工作树 clean。
- **K3 并入**：HEAD 原不含 2b5dd44ab6dec8f004e721f161bb6ba98989bffd
  （`git merge-base --is-ancestor` → no）。已执行
  `git merge --no-ff 2b5dd44ab6dec8f004e721f161bb6ba98989bffd -m "merge(B156.3.3): …"`，
  exit 0，无冲突。合并带入 21 文件 / +4565 行（含 internal/scheduling/registry_read.go、
  internal/proto/scheduling.go、internal/client/squads.go、internal/agentd/schedapi.go）。
- **K3 三判据（合并后实跑）**：
  - `test -f internal/scheduling/registry_read.go` → 存在 ✓
  - `grep -c "func (s \*Service) SquadRows" internal/scheduling/registry_read.go` → 1 ≥1 ✓
  - `grep -c "CoordinatorLaunchResp" internal/proto/scheduling.go` → 2 ≥1 ✓
- **基线复跑（合并后实跑原文）**：
  - `go build ./...` → exit 0。
  - `go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v` →
    `--- PASS: TestIgnitionVerticalSlice (0.08s)` / `ok github.com/Xsxdot/handoff/internal/keystone 0.086s`。
- **生产接线现状**：`grep -n "SetupAutomation" cmd/agentd.go` → 零命中（exit 1）。
  记录在案：K4 端点生产态依赖 K5 启动接线，withCoordinator 503 是预期形态（plan §D4）。
- **与 plan 的偏差记录（Task C 文件集）**：plan 写「cmd/card_coordinate_test.go（新）」，
  但 K3 合并带入同名文件（63 行，K3 Task E 的 card coordinate 命令级测试）。处置：
  K4 测试**追加**进该既有文件而非覆盖——有界文件集不变（同一路径），只改「新」为「追加」。
- 环境事实（供 Task A–D 引用）：Server.st 为 *store.Store（CreateProjectLocation/
  ListProjectLocations 在 internal/store/projects.go）；s.ledger 为 *ledger.Store
  （GetCard/CreateCard/TasksOf）；runningCountIn 在 scheddispatch_test.go:92；
  attachLocator.Locate 对空 SessionID 报错、非空产出 {Machine, Dir:workdir,
  Command:"<CLI> --session <SessionID>"}。

## Task A：keystone launchRound 消费入参 spec

- 【红】`go test ./internal/keystone/ -run TestLaunchForCardConsumesSessionSpec -count=1`
  第一发 → **编译红**：`invalid operation: rec.launches[0] != want (struct containing
  []string cannot be compared)`——plan 模板假设 SessionSpec 可 ==，实际含 `Env []string`。
  处置：改逐字段断言（CLI/HomeDir/Model/Workdir + Env 长度），判据本体（四字段逐一相等）
  不变；偏差记入本台账与产出物。
- 【红·断言】修夹具后复跑 → `spec 未消费（回潮）：got={CLI: HomeDir: Model: Workdir:
  Env:[]} want={CLI:opencode HomeDir:/home/coord Model:fast Workdir:/w Env:[]}`，
  exit 1——功能缺失型断言红，确认非 typo ✓。
- 【实现】keystone.go 三处：launchRound 签名加 spec 并删自造空 spec；
  LaunchForCard 转发入参；Wake resume 失败兜底传 `{CLI: ref.CLI}`。
  **plan 偏差**：Wake 的无会话引用分支（keystone.go:99，`!ok` 分支）也是 launchRound
  调用点，plan 只点名两个调用点——机械更新为传零值 spec（与原自造 `{Workdir:""}` 等价）。
- 【绿】复跑同命令 → PASS；`go test ./internal/keystone/ -count=1` → ok（0.130s），
  竖切未翻红 ✓。
- 【变异复验】锚点唯一性先证：`grep -c "result, err := s.runner.Launch(spec, s.briefing"`
  → 1。变异（可编译等价：函数体内 `spec = keysclient.SessionSpec{}` 忽略入参）→ 复跑
  → 红：`spec 未消费（回潮）：got={CLI: ...}`，exit 1。恢复原文 → 全绿。两读数在案 ✓。
- gofmt：首跑 `gofmt -l internal/keystone/` 报 keystone_spec_test.go（单行函数体对齐），
  `gofmt -w` 后复跑输出空 ✓。
- 提交：c332d1b6。

## Task B：agentd 三端点 + 穿 HTTP 缝级测试

- 【红】coordapi_test.go 落地后 `go test ./internal/agentd/ -run 'TestCoord' -count=1`
  → 六支全 FAIL，形态为「未注册路径 404 page not found」（路由未接线的行为红，
  非 typo）✓。第一发夹具编译错：`CreateCard` 返回 `(ledger.Card, error)` 而 plan
  模板按 string id 写——改取 `card.ID`（夹具偏差 1）。
- 【接线】server.go Handler()：`s.registerCoordRoutes(api)` 加在 registerSchedulingRoutes
  之后（plan 说 registerLedgerRoutes 之后；K3 已插入 scheduling 行，语义不变——
  注册顺序对互不重叠的路径模式无影响）。夹具偏差 2 见下。
- 【绿】复跑同命令 → 六支 PASS，ok 1.563s ✓。
- **夹具偏差 2（T1b 第二发 409）**：MaxConcurrency=1 下第一次拉起（承载失败 502）
  已占两级名额且无人释放（生产上名额归还挂回合终局 cardstep.go:86，失败拉起的
  归还窗口 plan §D5.1/§4 检查1 明文归 K5），第二次拉起撞 ErrNoSlot→409。处置：
  测试内两次拉起之间显式 `scheduling.Release("coord","c1")` 模拟回合结束归还，
  T1b 的被测语义（source 进 LaunchForCard 错误包装）不受影响。**产品侧不加释放臂**
  ——越出本卡有界文件集与 plan 范围。
- 【变异复验】锚点唯一性先证（两锚各 grep -c = 1）。第一发变异（CLI:""/HomeDir:""）
  编译不过（carrier 未使用）——按纪律①不算数，换可编译等价变异
  `binding.Executor+"x"` / `carrier.HomeDir+"x"` → T1 红：
  `spec 回潮：… got={CLI:opencodex HomeDir:/home/coordinatorx …}` exit 1。恢复 → 绿 ✓。
- 【回归】`go test ./internal/agentd/ -count=1` → ok（141.703s，超时上限提到 420s 后实跑）✓。
- gofmt：coordapi_test.go 首跑被点名，gofmt -w 后 internal/agentd/ 输出空 ✓。
- 提交：766f4d01。

## 断机恢复（2026-08-26，接管轮）

- 前任务 cd4c0266 因 agentd 重启执行器消失 turn_failed 并归档；代码在分支
  cards/B156.3.4-charter-2。
- 现场核对：恢复分支 cards/B156.3.4-charter-3 与 charter-2 同指 58d1f092
  （`git log charter-3..charter-2` 与 `charter-2..charter-3` 双向均空）——
  Task 0/A/B/C 六笔提交零丢失，**无需合入动作，未重写任何已提交内容**。
- 台账缺口：Task C 只落了代码提交、无台账段（断机截断）。以下 Task C 读数为
  **恢复轮亲跑**，非沿用旧会话结论。

## Task C：client CoordinatorLaunchAs + cmd card add --coordinate（恢复轮补验）

- 【红态在案（断机前）】：提交 58d1f092 提交信息记录「红态 unknown flag: --coordinate 在案」。
- 【绿·恢复轮亲跑】`go test ./cmd/ -run 'TestCardAddCoordinate' -count=1 -v` → exit 0，
  TestCardAddCoordinateTriggersCardCreateLaunch 与 TestCardAddCoordinateLaunchFailureDegrades
  全 PASS，`ok github.com/Xsxdot/handoff/cmd 0.290s` ✓。
- 【回归】并入 Task D 步 3 触及五包读数（见下），client/cmd 均 ok，card 族存量未翻红 ✓。

## Task D：反面断言 + 收尾门禁（恢复轮亲跑）

1. 【grep 反面断言】`grep -rn "scheduling\|keysclient\|schedclient" internal/ledgerstep/`
   → 零命中（exit 1）✓。
2. 【门禁】
   - `go build ./...` → exit 0；
   - `go vet ./...` → exit 0（全仓档，比 plan 的 `./internal/... ./cmd/...` 更严）；
   - `gofmt -l internal/keystone internal/agentd internal/client cmd` → 输出空（exit 0）；
   - 九个本轮改动 .go 文件逐一 `gofmt -l` → 输出空；
   - **gofmt 正控**：故意写坏格式的探针文件放入 cmd/ → `gofmt -l cmd` 打印该文件名
     （证明命令真的在看）→ 删除探针，工作树回到 clean。「零输出即通过」有正控背书 ✓。
3. 【触及五包回归】`go test ./internal/keystone/ ./internal/agentd/
   ./internal/scheduling/ ./internal/client/ ./cmd/ -count=1` → 五包全 ok
   （keystone 0.079s / agentd 111.988s / scheduling 0.556s / client 9.172s / cmd 8.224s）✓。
4. 【全量】`go test ./... -count=1` → **exit 0，ok 包 51、FAIL 0、"no test files" 6**
   （原始输出存仓外 /root/.handoff/tmp/a2268f9b/fulltest.log）；contract §13 注明的既有
   抖动项 grok TestGrokToolTimingPaired 本轮未出现，无分栏必要（本卡引入的红 0 / 基线
   既有的红 0）。
5. 【图闸】`go run . graph check --repo .` → `"fails": []`（fails=0）；warns 全部为既有
   legacy 直调预算提示、best.json 陈旧容器与 oversized-package/prefix-family 应答项，
   非本卡新增容器边，未放开任何闸门 ✓（第三条应答义务：cmd 51 文件、agentd 61 文件的
   有界文件集回答随 K4 plan §有界文件集与本台账在案，不立专项重构）。

## 收尾两问（配对原语）

### 问一「多了什么」（从代码进；范围＝本卡实现提交 c332d1b6 / 766f4d01 / 58d1f092 的
diff。K3 合入提交 577b6dcb 带入的 SquadRows / CoordinatorLaunchResp / decodeWire /
Client.CoordinatorLaunch 系上游交付物、在本卡开工前已冻结，不计入本轮新增）

- 本轮新增导出符号共 **1** 个：`client.Client.CoordinatorLaunchAs`
  - 生产命中 **3** 行（internal/client/coordinator.go 定义行＋文档注释 2 行；其中真实
    调用点 1 处：cmd/card_coordinate.go:31）／测试命中 **0** 行——生产消费方存在，
    不是「只造了没人用的原语」✓。
- 其余新增符号均为非导出：coordapi.go（registerCoordRoutes、withCoordinator、
  handleCoordLaunch、handleCoordStatus、handleCoordAttach、resolveCoordinatorSquad、
  resolveCoordWorkdir、coordinatorStatusResp、errNoCoordinatorSquad、
  errAmbiguousCoordinatorSquad）、keystone_spec_test.go（specRecorder、stubLedgerView）、
  cmd（cardAddCoordinate、coordinateAfterCreate）——不构成对外契约面，不入问一清单主体。
- 自检 A（模式坏→假阳性）：`git grep -E "New\(" -- '*.go' | grep -v '_test\.go' | wc -l`
  → **223**（三位数，POSIX ERE 模式有效，不是 `\b` 坏模式）✓。
- 自检 B（排除坏→假阴性）：`git grep -E "specRecorder" -- '*.go' | grep -v '_test\.go'
  | wc -l` → **0**（确定只在测试里的符号未被误计入生产命中）✓。

### 问二「少了什么」（从契约进；每条点名可 grep 载体，全部实证于 HEAD）

| 契约义务 | 载体符号/字段 | 实证 |
|---|---|---|
| POST coordinator/launch 一键拉起 | `handleCoordLaunch`（coordapi.go 定义×1） | grep 命中 |
| GET coordinator 绑定/接管态 | `handleCoordStatus` ＋ `coordinatorStatusResp`（bound/attach_active/attach 字段） | 各定义×1 |
| POST attach 接管/交回（配对原语同端点） | `handleCoordAttach` 内 `SetAttach(id, true)`（:242）/回滚 `SetAttach(id, false)`（:245）/交回（:254） | 三处 |
| §6.2 两入口合一，source 只进审计 | `LaunchForCard(r.Context(), id, source, spec)`（coordapi.go:157） | ×1 |
| 小队识别 0→400 指路 / ≥2→409 / 恰1→用 | `resolveCoordinatorSquad` ＋ `errNoCoordinatorSquad` ＋ `errAmbiguousCoordinatorSquad` | 各×1 |
| 澄清2：launchRound 消费入参 spec | `func (s *Service) launchRound(card, extra string, spec keysclient.SessionSpec)`（keystone.go:162） | ×1 |
| 未装配 503 不静默降级 | `withCoordinator`（coordapi.go 定义×1，三路由均包裹） | ×1 |
| 跨机 ？machine= 转发 | handleCoordAttach 首行 `forwardIfRequested(w, r)` | ×1 |
| Workdir best-effort | `resolveCoordWorkdir`（coordapi.go 定义×1） | ×1 |
| 开卡即绑 CLI 入口（spec §5.1 入口1） | flag `BoolVar(&cardAddCoordinate`（card.go）→ `coordinateAfterCreate(cmd, card.ID)`（card.go 接线×1）→ `CoordinatorLaunchAs(ctx, cardID, "card_create")`（card_coordinate.go:31） | 链上三点齐 |
| 路由接线唯一生产点 | `registerCoordRoutes(api)`（server.go:546，Handler() 内一行） | ×1 |
| 拉起路径不产生 task（铁律，「必须不发生」型反向义务） | 无生产载体属预期；承载物是测试断言：TestCoordLaunchEndpointSuccess 内 `env.ledger.TasksOf(cardID)` 零断言（coordapi_test.go） | 在案 |

两问互补核验：问一扫不出「缺字段」族（如 coordinatorStatusResp 少一个 json 键），
问二逐条点名后可证伪——本轮两问均无缺口。

## 与 plan 的偏差汇总（全卡五条，前四条断机前已在案）

1. SessionSpec 含 `Env []string` 不可 `==` → Task A 测试改逐字段断言（判据本体不变）。
2. Wake 无会话引用分支也是 launchRound 调用点（plan 只列两个）→ 机械更新传零值 spec。
3. T1b 夹具偏差：MaxConcurrency=1 下两次拉起之间显式 Release 模拟回合终局归还；
   产品侧不加释放臂（越界）。
4. server.go 接线行落在 registerSchedulingRoutes 之后（plan 写 registerLedgerRoutes
   之后；K3 已插入 scheduling 行，注册顺序对互不重叠的路径模式无影响）。
5. card_coordinate_test.go 由 plan 的「新文件」变为向 K3 已带入的同名文件追加
   （Task 0 台账在案）。

## 收尾自检与移交

- plan Task D 步 5：§2 五条注记（D1–D5）、§4 五项检查结论、§6 待拍板清单均在
  plan 文档（b156.3.4-plan.md，已提交）原文在案，本卡未推翻任何一条。
- plan §6 七条待拍板（launch_queue 写入臂、card create 别名、拉起失败降级语义、
  K3/K4 拨号并存、重复拉起守卫、跨卡签名字段级审计、派发自审）**全部维持待协调者
  拍板**，本卡未自行裁决；K4 判据不受影响。
- 未完成项：无（Task 0/A/B/C/D 全部收口）。
- 平台不变量自查：本轮未调用 handoff CLI 写路径、未起 executor/子任务、未 push；
  所有结论均有本轮亲跑输出对应。
