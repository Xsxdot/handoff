# B233.22 plan：internal/ 按 best.json 重排

前置事实（2026-09-13，功能线含 .18/.19；.20/.21 若已合入则以工作树实况为准）：

- best.json 容器→域映射为唯一应然判据（codegraph/best.json）。
- internal/agentd 现存文件按 k_agentd_* 应然域分家：d_gateway（Server/handlers/forward/
  auth/routes/hostAllowlist/orchestration_client/scheduling_client 等 HTTP/WS/转发面）
  vs d_orchestration（Manager 已在 internal/orchestration；Hub、编排辅助、无 Server 绑定
  的编排类型仍在 agentd）。
- 基线图（codegraph/baseline.json）滞后，不作盘点依据；盘点读真实代码。

步骤：

1. 盘点：遍历 internal/ 各包非测试源文件，按主容器 best.json 归属标 A/B/未登记三档，
   写 docs/superpowers/notes/b233.22-placement.md。
2. A 档逐个 `git mv`：实现 + 测试随迁；旧包引用改新包；import 整理；行为零改动。
   （预期热点：internal/agentd 的 Hub/编排辅助 → internal/orchestration；其余以盘点为准。）
3. B 档逐条在卡 note 落延期理由（涉及哪些缝、为什么不在本卡动）。
4. 验证：go build ./...；go test -p 1 ./...（存量红除外）；实况表与 diff 对账。
5. 提交：`refactor(B233.22): internal/ 按 best.json 重排——A 档错位文件归域`。

执行中偏差如实记入收尾段。
