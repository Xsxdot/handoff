# B156.3.3 Implement 台账（K3 人机登记面）

> 分支：cards/B156.3.3-charter-2 ｜ 起点：b8639cd7（协调者快进后的 merge，含 K1+K7+K2 实装）
> 白名单（协调者拍板 A）：internal/scheduling/{registry_read.go 新建, scheduling.go 仅 var 块追加哨兵}；
> 不碰符号：stepRunning/Release/PopReady/Enqueue/Admit/LaunchAdmit/admitInto/acquire/casCount/readCount/
> ErrRetryExhausted 各产生消费点；不碰 server.go SetupAutomation(2236-2246) 与 translateRegistryErr。

## Task 0 基线取证（2026-08-26）

- 树确认：`git log --oneline -1` → b8639cd7 merge「汇入 K1+K7+K2 已验收实装」；
  `grep -n ErrCASConflict internal/agentd/server.go` → 2306/2309 两行命中（K2 CAS 翻译在场）。
- `go build ./...` → exit 0（BUILD_OK）。
- `go test ./internal/scheduling/ ./internal/proto/ -count=1` → 两包 ok。
- `go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1` → ok 0.104s（竖切兜底存活）。
- grep `api/squads|api/queue|coordinator/launch`（internal/cmd/web）→ 0 命中；grep
  `QueueSnapshot|SquadRows|CarrierRows` 全仓 → 0；grep `coordinate` cmd/ 非测试 → 0。全绿地。
- `go run . graph check --repo .` → `"fails": []`（warns=既知图债，不新增）。
- web：`npm install --prefer-offline` 完成；`npx vitest run src/api/contract.test.ts` → 36 passed。
- 读数与 plan §0 一致；差异仅一处：scheduling 包现有 scheduling_test.go（plan 基线时无测试文件，
  K2 合并带入），红绿对照更完整。

## 拍板与依赖声明

- translateRegistryErr 的 CAS 翻译归 K2（已在树），本卡不改写；TC2 的 409 断言依赖它。
- ErrInvalid 落点 = scheduling 包（理由见产出物报文）；CAS 409 判定用 schedclient.ErrCASConflict
  （哨兵正主在 port 契约包，agentd 已 import，零新增边）。

## Task A：internal/scheduling 只读投影（白名单内）

- `git diff internal/scheduling/scheduling.go` → 仅 var 块一个 hunk（ErrInvalid 声明 + 注释），
  无其他 hunk；PutCarrier/PutSquad 函数体未动（校验行保持裸 fmt.Errorf，包装点归持函数的卡）。
- 【红】`go test ./internal/scheduling/ -run 'TestRowsCarryVersionsForCASLock|TestQueueSnapshotMatchesDrainOrder'`
  → FAIL：`载体行数 0 ≠ 1` / `快照行数 0 ≠ 4: []`（空壳断言红，非编译红）。
- 【绿】registry_read.go 真身后同命令 → ok。复用声明：解码 schema=queuedEntry/实体类型
  （json.Unmarshal 进既有类型）；位次权威=s.position()（Enqueue 返回值同一来源）；
  清队顺序=QueueKinds。不复用 listEntities：它只回 Body、丢 Version 元数据（CAS 编辑回路需要版本行），
  故调其同源 s.repo.List；orderRank/queueID 未用——投影不自行排秩、不自造主键。
- 回归：TestIgnitionVerticalSlice → ok 0.084s（PopReady 未动，等价性兜底）；scheduling 全包 ok。

## Task B：proto wire DTO + 契约 fixture

- internal/proto/scheduling.go 新建（CarrierView/SquadView/SquadsResp/CarrierInput/
  SquadInput/SquadPutResp/QueueEntry/QueueResp/CoordinatorLaunchResp）。
- contract_fixture_test.go cases 追加三样本；`go test ./internal/proto/ -run TestContractFixtures
  -update -count=1` → ok；去 -update 复跑 → ok；git status 目击三个新 json：
  SquadsResp.json / QueueResp.json / CoordinatorLaunchResp.json。
- fixture 边界目检：QueueResp `"ready": false` 显式在场、target/executor/model 缺席；
  SquadsResp 载体行 `"healthy": true` 与 `"max_concurrency": 2` 显式在场、小队行无
  max_concurrency 键、model 缺席。

## Task C：gateway schedapi.go 四端点

- 【红】schedapi_test.go 六支（TC1–TC6）先跑 → 全 404：
  `空库 GET 应 200，得 404：404 page not found` 等（缺端点红，非编译红）。
- 【绿】schedapi.go + server.go 注册行一行后同选择器 → ok 2.594s；
  `go test ./internal/agentd/ -count=1` 全包 → ok 139.778s（存量不翻红）。
- server.go 本卡 diff = 恰一行 `s.registerSchedulingRoutes(api)`（Handler() 内
  registerLedgerRoutes 之后，本卡自己的注册点）；SetupAutomation(2236-2246) 与
  translateRegistryErr 零改动（git diff 核验见 Task G）。
- 400 分类实现与 plan 的差异（如实声明）：域内 PutCarrier/PutSquad 校验行未包 ErrInvalid
  （白名单只许 var 块追加），网关按三层分类：①wire 边界预检（词表值引用 scheduling
  导出常量 CredentialStandalone/CredentialMainHomeSync/RoleExecutor/RoleCoordinator，
  零复制）；②PutSquad 的成员缺失经 errors.Is(err, scheduling.ErrNotFound) → 400
  （域内该错误以 %w 包装 NotFound 上浮）；③scheduling.ErrInvalid 臂就位待域侧通电。
  TC2/TC3 的 400 断言全部真实通过。

## Task D/E：client 拨号 + squad 命令族 + card coordinate

- 【红】cmd/squad_test.go 四支先跑 → `unknown command "squad" for "handoff"`（缺命令红）。
- 【绿】internal/client/squads.go + cmd/squad.go 落地后 `go test ./cmd/ -run TestSquad
  -count=1` → ok。
- 【红】cmd/card_coordinate_test.go 三支先跑 → 全 FAIL（TE-1 `请求形状不符:  ` 空拨零、
  TE-2 缺卡号未被拒、TE-3 err=nil stderr 空；命令缺席，cobra 打帮助即返 nil）。
- 【绿】card.go 追加 cardCoordinateCmd + AddCommand 注册行后同命令 → ok。
- cmd 包全量回归随 Task G。

## Task F：TS 镜像

- web/src/api/scheduling.ts 新建（九接口 + 五函数，字段=proto json tag 一一对应，
  omitempty ↔ 可选 ?）；contract.test.ts 追加三 fixture import + describe('scheduling wire')。
- `npm run typecheck`（tsc -b）→ 干净（首跑 TS1484 类型导入报错，改 type-only import 后清零——
  报错原文在回合记录）；
  `npx vitest run src/api/contract.test.ts` → 39 passed（36+3）。

## Task G：门禁读数

- 门禁四条：`go build ./...` OK；`go vet ./...` exit 0；`gofmt -l $(git diff --name-only
  b8639cd7 -- '*.go')` 首轮抓到 schedapi.go/schedapi_test.go（注释列表缩进 + import 排序），
  gofmt -w 修复后输出为空；六包测试两轮全绿（删除孤儿方法后复跑）：
  scheduling 0.548s / proto 0.005s / agentd 114.991s / client 9.427s / cmd 9.378s /
  keystone 0.148s 全 ok。
- grep 反面判据：cmd/ 内 internal/scheduling = 0、internal/schedclient = 0
  （首跑曾=1，是 squad.go 头注释里的路径字样，非 import，已改写措辞归零——如实记录）。
  护栏：ls cmd/*.go | wc -l = 106（>20）；正控 internal/agentd/ 非测试文件
  scheduling import 命中 = 5（>0，模式能命中真 import）。
- 图硬闸：`go run . graph check --repo .` → fails=[]（warns=101 既知图债）；
  `go test ./cmd/ -run TestRepoContractGate -count=1` → ok。

## 变异自验（三探针，先断言命中唯一再改语义）

1. schedPutErr 的 CAS 臂 StatusConflict→StatusBadRequest：TC2 翻红
   「过期 expect 应 409 带冲突文案，得 400」→ 击杀。已还原。
2. carrierView 的 Healthy: c.Healthy→true：TC2 仍绿——**等价变异**（wire 入口无
   healthy 字段、PutCarrier 恒把零值翻真，两写法对所有可达输入行为相同），不是测试
   无牙；投影保持读实体原值。已还原。
3. queueEntryView 的 Position→Position+1：TC4 翻红 → 击杀。已还原。
   还原核验：grep 计数归位 + gofmt 空 + 两支复跑 ok。

## 收尾自审·孤儿清理（配对判据执法）

- Client.Queue：生产命中 0 / 测试命中 0 → **已删**（本卡 CLI 面无 queue 命令，
  Go 侧消费方不存在；预造即违反配对判据；GET /api/queue 的 wire 契约由 TC4 与
  TS getQueue/fixture 锁住）。
- Client.PutCarrier：生产调用 0（schedapi.go:128 的 s.scheduling.PutCarrier 是编制域
  门面同名方法，非此符号）→ **已删**（契约 §11 CLI 面无 carrier 族，plan D5.1 引导缝；
  载体 PUT wire 契约由 TC2 + TS putCarrier 两侧锁住）。删除后 build/vet/gofmt/
  cmd 测试复跑全绿。
- 删除与 plan §3 Produces 的偏差如实申报：Produces 曾列 Queue/PutCarrier 两方法；
  plan 自身也要求「每个符号有消费者」，两方法在本卡与契约的 CLI 面内都无消费者，
  按「删掉或说明」取删。K4/K6 若需 Go 侧拨号随其卡自加。

## 问一「多了什么」（从代码进；逐符号生产/测试命中分开报）

自检两向：
- 模式自检：`git grep -E 'New\(' -- '*.go' | grep -v _test | wc -l` = **220**（三位数 ✓，
  排除模式没坏）；注意 grep 命中含注释行，下表数字同口径。
- 排除自检：rowsRegistry（仅 registry_read_test.go 存在）生产命中 = **0** ✓。

新增导出符号清单（本轮共 19 个 Go 导出符号 + TS 文件一个）：

| 符号 | 生产命中 | 测试命中 | 归属/消费方 |
|---|---|---|---|
| scheduling.ErrInvalid | 定义1+errors.Is臂1（schedapi.go:223） | 2 | 验收判据「400 分流」；**产生点=0 如实申报**（%w 包装点在 PutCarrier/PutSquad 函数体，白名单禁改；消费臂就位待域侧通电） |
| scheduling.SquadRow/CarrierRow/QueuedRequest | 类型定义+schedapi 投影链 | registry_read_test | GET 读面载体（白名单拍板A） |
| scheduling.SquadRows() | 3（定义+handleSquadsGet） | 2 | 同上 |
| scheduling.CarrierRows() | 3（定义+handleSquadsGet） | 2 | 同上 |
| scheduling.QueueSnapshot() | 3（定义+handleQueueGet） | 5 | 同上 |
| proto.CarrierView/SquadView/SquadsResp/CarrierInput/SquadInput/SquadPutResp/QueueEntry/QueueResp/CoordinatorLaunchResp | 依次 6/7/5/4/6/9/6/5/4 | 2/1/4/0/0/0/2/2/1 | 线格式权威（验收判据2；K4 消费 CoordinatorLaunchResp、K6 消费全部 View） |
| client.Squads() | cl.Squads( ×2（squad.go set/list） | stub 测试经 cobra | D1 CLI 走网关 |
| client.PutSquad() | ×2（create/set） | 同上+TD 直证 | 同上 |
| client.CoordinatorLaunch() | ×1（card.go:453） | TE 经 cobra | card coordinate（契约§11） |
| cmd.squadCmd 族 + cardCoordinateCmd | 非导出 var；rootCmd/cardCmd init 注册 | runLedgerCLI 四支+三支 | 契约§11 CLI 面 |
| web/src/api/scheduling.ts（九接口五函数） | prod/test=0（K6 未开工） | interfaces 被 contract.test.ts type-import 承接 | **验收判据2 明文要求的镜像产出物**，对 K6 待消费（plan Produces 末条明文） |

结论：除上表已点名的 ErrInvalid（声明性哨兵）与 TS 导出面（跨卡法定交付物）外，
其余新符号均有生产消费方；两个零消费 client 方法已删除。

## 问二「少了什么」（从契约进；义务→可 grep 载体）

| 契约义务 | 承载符号/字段（可 grep） |
|---|---|
| GET /api/squads 双集合带版本 | 路由 `"GET /api/squads"`（schedapi.go:44）、proto.SquadsResp.Carriers/.Squads、CarrierView.Version、SquadView.Version ← CarrierRow.Version/SquadRow.Version；handler handleSquadsGet |
| PUT carriers/{name}?expect=N | 路由 `"PUT /api/squads/carriers/{name}"`、expectVersion()、handleCarrierPut、SquadPutResp.Version=expect+1 |
| PUT squads/{name}?expect=N | 路由 `"PUT /api/squads/squads/{name}"`、handleSquadPut；词表=scheduling.RoleExecutor/RoleCoordinator/CredentialStandalone/CredentialMainHomeSync（schedapi.go:139-145/163-169 引用） |
| 成员引用不存在→400 点名 | errors.Is(err, scheduling.ErrNotFound)（handleSquadPut）+域内 %w 包装链（scheduling.go:172-175 既有）；TC3 断言含 "ghost" |
| GET /api/queue 清队序全局位次 | 路由 `"GET /api/queue"`、QueueSnapshot()（QueueKinds×position()）、proto.QueueEntry.Position、handler handleQueueGet；TC4 位次断言 B2→B3→B1 |
| 空集是合法态 | handleSquadsGet 的 make([]...,0,...)（非 nil）＋TestSquadsGetEmptyIsLegalState 断言 `"carriers":[]`/`"squads":[]` |
| 五端点过既有鉴权中间件 | 注册进 Handler() api mux（server.go:545 一行），auth 包整个 mux（server.go:576 root.Handle("/", s.auth(mux))）；TestSchedulingEndpointsRequireAuth 401/200 双读数 |
| handoff squad create/set/list | cmd/squad.go squadCreateCmd/squadSetCmd/squadListCmd；TD-1..TD-4 |
| handoff card coordinate <卡号> | cmd/card.go cardCoordinateCmd + client.CoordinatorLaunch；TE-1..TE-3（POST 空对象体/缺参拒/指路透传） |
| 非法 kind/域值 400 有断言 | handleCarrierPut 凭据词表预检（报文含 standalone 与 main_home_sync 字面，源自常量插值）、handleSquadPut 角色词表预检（executor/coordinator）；TC2/TC3 断言原文在台账 |
| CAS 冲突 409 | schedPutErr 的 errors.Is(err, schedclient.ErrCASConflict) 臂（schedapi.go:225）；依赖 server.go:2306-2309 translateRegistryErr（K2 已落地未改写）；TC2 expect=99→409 |
| 未装配降级 503 | withScheduling「编制域未装配」文案；TestSchedulingEndpointsDegradedWithoutSetup |
| TS 镜像字段逐一对照 | web/src/api/scheduling.ts 九接口 ↔ proto json tag；testdata/{SquadsResp,QueueResp,CoordinatorLaunchResp}.json 由 TestContractFixtures 逐字节钉住；contract.test.ts describe('scheduling wire') 三支 |

## 正交扫描（多了什么）

A. 新增跨子系统绑定点 = **0**。证据：`git diff b8639cd7 -- '*.go' | grep '^\+.*\.New('`
   仅 5 行且全部为 errors.New（错误构造×2）、测试夹具（ledgerapi.New/scheduling.New 在
   registry_read_test.go、slog.New 在 schedapi_test.go）；生产代码 scheduling.New(
   计数改前=1 改后=1（均在 SetupAutomation，本卡未触碰该函数）。registerSchedulingRoutes
   的调用点恰 1 处（Handler 内路由注册，非服务构造，不构成第二组装点）。
B. 新增文件 12 个（两 Go 生产+四 Go 测试+一 proto DTO+一 client 拨号+两 cmd+一 TS+
   一台账+三 fixture json 中属新增者）与导出符号归属见问一表；每项均可指到验收判据
   条目或 plan Produces。新增依赖方向 = 0：agentd→scheduling/schedclient、client→proto、
   cmd→proto 均为既有边；graph check fails=[]。
C. 计数式判据改前/改后双读数：scheduling.New( = 1→1；cmd 内 scheduling/schedclient
   引用 = 0→0；server.go diff = +1 行；scheduling.go diff = +8 行恰一个 hunk
   （`@@ -108,6 +108,14 @@ var (`，白名单内）；client 导出方法 0→3（计划 5，收尾删 2 孤儿）。
D. 同一语义两遍检查：排序语义单点=position()（快照按其返回值排位，registry_read.go
   无第二比较器——diff 自检通过）；队列解码 schema 单点=queuedEntry（json.Unmarshal 进
   既有类型）；词表值单点=scheduling 导出常量（网关引用不复制字面）；PUT 公共尾段单点=
   putRegistration（删孤儿后仍被 PutSquad 唯一使用，无多开资源句柄问题）。

---

## 修复轮（2026-08-26，审阅退回三条 major + 两条 minor 裁决）

> 分支：cards/B156.3.3-charter-3 ｜ 起点：eaf68fc1（第一轮 K3 实装）｜ 提交：分次，见下。
> 三条 major 全修、两条 minor 按协调者裁决执行；server.go 本轮**零改动**（裁决 2）。
> 只写亲跑读数（命令 + 退出码）；「预计」「应通过」一律不写。

### 基线取证（修复轮）

- `git log --oneline -1` → eaf68fc1（第一轮提交在场，工作树 clean）。
- `go test ./internal/agentd/ -run TestSquadPutValidatesMembersAndRole -count=1`
  → **FAIL**（TC3 红实证）：`schedapi_test.go:166: 幽灵成员应 400 点名成员，得 409：
  {"error":"小队 sq 成员 ghost: scheduling: 实体不存在"}`——Major-1 复现，退出码 1。
- 读码核对：schedapi.go handleSquadPut 的 ErrNotFound 臂写 `http.StatusConflict`
  （注释写「400 而非 500」、代码写 409，自相矛盾）；schedPutErr 的 ErrInvalid 臂写
  `http.StatusConflict`（doc 注释写「400 校验未过 / 409 CAS 冲突」，代码与注释矛盾）；
  scheduling.go PutCarrier/PutSquad 校验行四处裸 `fmt.Errorf`（Major-3 空白名 500）。

### 红（修复轮，先写测试）

1. `internal/scheduling/registry_read_test.go` 新增 `TestPutValidationWrapsErrInvalid`：
   Carrier 登记不完整/凭据词表外、Squad 名空白/角色词表外四处必须 `errors.Is(ErrInvalid)`，
   成员引用缺失必须 `errors.Is(ErrNotFound)`（Major-1 判据的域侧半臂）。
2. `internal/agentd/schedapi_test.go` 新增 TC7 两支 `TestCarrierPutBlankNameIs400` /
   `TestSquadPutBlankNameIs400`：空白名（路径 `%20` → 空格）PUT 应 400。
- 红读（亲跑）：`go test ./internal/scheduling/ -run TestPutValidationWrapsErrInvalid`
  → FAIL `载体登记不完整应包 ErrInvalid，得 载体登记不完整：name/machine/cli 必填`（退出码 1）；
  `go test ./internal/agentd/ -run 'TestCarrierPutBlankNameIs400|TestSquadPutBlankNameIs400|TestSquadPutValidatesMembersAndRole'`
  → 三支 FAIL：TC3 `得 409`、TC7 两支 `得 500`（退出码 1）。红因确认：缺 %w 包装（非 typo）。

### 绿（修复轮实现）

- **Major-3**（裁决 1 白名单扩容）：scheduling.go PutCarrier/PutSquad 四处校验 return
  改 `fmt.Errorf("%w: ...", ErrInvalid, ...)`；哨兵注释更新（不再自称「仅声明」）。
  `internal/scheduling/` diff 只落这两个函数体 + 哨兵注释，**不碰** stepRunning/Release/
  PopReady/Enqueue/Admit/LaunchAdmit/admitInto/acquire/casCount/readCount/
  ErrRetryExhausted 任何产生点消费点；registry_read.go、queueLess/queuedEntry/position/
  orderRank/listEntities 一字未动（只读投影复用判据原样成立）。
- **Major-2**：schedPutErr 的 ErrInvalid 臂 `StatusConflict → StatusBadRequest`；头注更新。
- **Major-1**：handleSquadPut 的 ErrNotFound 臂 `StatusConflict → StatusBadRequest`；注释更新。
- **minor-1**（裁决 3）：web/src/api/scheduling.ts CarrierInput/SquadInput 各补 `name?: string`
  （Go DTO 有 `Name json:"name,omitempty"`，TS↔Go 逐字段对照缺字段即违约；调用侧仍不传）。
- **minor-2**（裁决 4）：QueueSnapshot 损坏行取舍维持现状，只落账不改码——第一轮文件头注
  已如实申报「position 对损坏行静默跳过」，本轮不引入新排序比较或新解码逻辑。

### 变异自验（修复轮，三探针，先断言命中唯一再改语义）

1. PutCarrier 校验行 `%w` 去掉 → `TestPutValidationWrapsErrInvalid` FAIL
   `得 载体登记不完整：name/machine/cli 必填`（退出码 1）→ 击杀。已还原。
2. schedPutErr ErrInvalid 臂改回 `StatusConflict` → `TestCarrierPutBlankNameIs400` FAIL
   `得 409`（退出码 1）→ 击杀。已还原。
3. handleSquadPut ErrNotFound 臂改回 `StatusConflict` → `TestSquadPutValidatesMembersAndRole`
   FAIL `得 409`（退出码 1）→ 击杀。已还原。
- 还原核验：gofmt 空 + `go test ./internal/scheduling/ ./internal/agentd/ -run
  'TestPutValidationWrapsErrInvalid|TestCarrierPutBlankNameIs400|TestSquadPutBlankNameIs400|TestSquadPutValidatesMembersAndRole'`
  → ok。

### 收尾三档读数（修复轮，全部亲跑）

1. `go test ./... -count=1` → **退出码 0**，51 个包 ok（含 agentd 160s、cmd 16s；
   `internal/cmd` 不存在是笔误路径，非红）。
2. `go build ./...` → 退出码 0；`go vet ./...` → 退出码 0。
3. `gofmt -l <本轮改动 .go 五个>` → 输出为空，退出码 0；正控：对
   `/root/.handoff/tmp/49e3fc56/gofmtctl/bad.go`（故意坏格式）跑同命令 → **打印文件名**
   （正控生效，零输出即通过判据不是空跑）。
- web：`npm run typecheck`（tsc -b）→ 退出码 0；`npx vitest run src/api/contract.test.ts`
  → 1 passed（39 tests，退出码 0）。

### 问一「多了什么」（修复轮从代码进）

- 自检两向：模式 `git grep -E 'New\(' -- '*.go' | grep -v _test | wc -l` = **220**（三位数 ✓）；
  排除 `rowsRegistry(` 生产命中 = **0** ✓（仅测试）。
- **本轮新增导出符号 0 个**（`git diff eaf68fc1 -- '*.go'` 的 `^+func/^+type/^+var/^+const`
  只有三个 `Test*` 测试函数，非导出生产符号）。
- 新增跨子系统绑定点 = **0**：`git diff eaf68fc1 -- '*.go' | grep '^+.*\.New('`（排除 _test）
  = 无；`scheduling.New(` 计数改前 = 1 改后 = 1（均在 SetupAutomation，本轮未触碰）。

### 问二「少了什么」（修复轮从契约进）

| 契约义务（修复轮） | 承载符号/字段（可 grep） |
|---|---|
| Major-1：成员引用缺失 → 400 | `handleSquadPut` 的 `errors.Is(err, scheduling.ErrNotFound)` 臂 `writeErr(w, http.StatusBadRequest, err)`（schedapi.go:175） |
| Major-2：ErrInvalid 臂 → 400 | `schedPutErr` 的 `case errors.Is(err, scheduling.ErrInvalid):` + `writeErr(w, http.StatusBadRequest, err)`（schedapi.go:225） |
| Major-3：校验失败包 ErrInvalid | PutCarrier/PutSquad 四处 `fmt.Errorf("%w: ...", ErrInvalid, ...)`（scheduling.go:133/137/169/172） |
| minor-1：TS 镜像补 name | web/src/api/scheduling.ts `CarrierInput.name?: string`、`SquadInput.name?: string` |
| minor-2：QueueSnapshot 边界落账 | 台账本节（不改码，文件头注已申报） |

### 正交扫描（修复轮）

A. 新增跨子系统绑定点 = 0（见问一）。server.go **零改动**（裁决 2：`git diff eaf68fc1 -- internal/agentd/server.go` 为空）。
B. 新增导出符号 = 0；新增文件 = 0；新增依赖方向 = 0（只改既有函数体与既有文件）。
C. 计数式判据改前/改后：`scheduling.New(` 1→1；`grep -c 'writeErr(w, http.StatusBadRequest' internal/agentd/schedapi.go` 0→2（ErrInvalid+ErrNotFound 两臂各一，均合法）；TS 接口字段 `name?: string` 0→2。
D. 同一语义两遍检查：修复轮无新增排序/解码/词表副本；`%w` 包装只此一处产生 ErrInvalid（域），
   消费只在 schedPutErr 一处（网关），无第二入口。

---

## 修复轮 2（2026-08-26，协调者追认三 major 处置）

> 分支：cards/B156.3.3-charter-4 ｜ 起点：10db8338（修复轮 1 台账）｜ 提交：分次，见下。
> 三条 major 按协调者裁决执行；Major-1 = 零改动（裁决追认现状）；Major-2 的映射转型
> 经提问后协调者裁决允许保留（自检改写为「无校验用途转型」）；Major-3 补 set 命令级测试。

### 协调者裁决（本回合前问询）

- 自检「schedapi.go 不再出现 CredentialSource(/SquadRole( 转型调用」与冻结约束冲突
  （proto 不 import 域 → DTO 字段 string；域字段类型 CredentialSource/SquadRole）。
  协调者裁决：**允许保留映射转型**。自检改写为——schedapi.go 里每个转型命中逐条
  列出并标注角色（映射/校验）；校验用途命中数必须为 0（不得与导出词表常量比较、
  不得依转型结果分支报错）。

### Major-1：server.go 位置判据（零改动）

- `git diff 10db8338..HEAD -- internal/agentd/server.go` 行数 = **0**（零改动，自动满足
  追认判据「锚点上方净新增最小化」）。
- SetupAutomation 锚点行号：改前 2237 / 改后 2237（试合实测，偏移量 = **0**）。

### Major-2：收敛——删三块 wire 预检（真缺陷）

- 【红】schedapi_test.go TC2 凭据词表断言 / TC3 角色词表断言各加「登记校验未过」
  前缀（锁定拒因来自域 ErrInvalid 臂而非网关预检）。亲跑：
  `go test ./internal/agentd/ -run 'TestCarrierPutRoundtripThroughWire|TestSquadPutValidatesMembersAndRole'`
  → FAIL：`得 400：{"error":"载体 c2 的凭据来源只能是 standalone 或 main_home_sync"}` /
  `得 400：{"error":"小队 sq 角色只能是 executor 或 coordinator"}`（缺前缀 = 预检在拦截）。
- 【绿】删 schedapi.go 三块预检（machine/cli 必填、凭据词表、角色词表）+ 重写文件头
  分类策略为两层（wire 线格式 / 域校验）+ 移除 strings import + handleSquadPut 头注更新。
  同命令 → ok。schedapi.go 净 diff = +11/-34。
- 新增 wire 级收敛证明 `TestCarrierPutMissingRequiredFieldsIs400`（缺 machine/缺 cli 两支
  → 400 且带「登记校验未过」前缀；若预检复辟，报文缺前缀即翻红）。
- **自检（亲跑）**：
  - 词表常量只在域内：`CredentialStandalone/MainHomeSync` 生产命中仅 scheduling.go
    （声明 45-46 + 校验 135）；`RoleExecutor/Coordinator` 生产命中仅 scheduling.go
    （校验 171/197/210）——agentd 生产代码零引用（测试造夹具不算）。initflow 的
    RoleExecutor/Coordinator 是另一域常量，非编制域词表。
  - schedapi.go 转型命中恰好 2 处，全部标注 **映射**：`scheduling.CredentialSource(
    in.Credential)`（schedapi.go:107，构造 Carrier 纯映射）与 `scheduling.SquadRole(
    in.Role)`（schedapi.go:141，构造 Squad 纯映射）。校验用途命中数 = **0**。

### Major-3：squad set 命令级测试（补零覆盖）

- 新增四支（cmd/squad_test.go，TD-5..TD-8）：
  1. `TestSquadSetRejectsMissingNameBeforeDialing`——缺 --name 本地拒绝、不拨号；
  2. `TestSquadSetSurfacesStaleExpectConflict`——坏 expect（显式 --expect=5 原样带出，
     服务端 409）报文可行动且点名出路；
  3. `TestSquadSetSurfacesActionableServerReject`——服务端 400 报文透传且含小队名；
  4. `TestSquadSetEditLoopUsesReadVersion`——版本往返：GET version=3 → PUT ?expect=3，
     未给字段保持现状、给了字段覆盖、双侧渲染。
- 变异自验（先断言命中唯一再改语义，亲跑）：
  - `expect := found.Version → expect := 0`（squad.go:100，count=1）→ TD-8 FAIL
    `编辑回路应 GET 取版本后以 expect=3 写回，得 PUT ...?expect=0` → 击杀。已还原。
  - `expect = squadExpect → expect = found.Version`（count=1）→ TD-6 FAIL
    `用户显式 --expect 应原样带出，得 expect=1` → 击杀。已还原。
  - 还原核验：`git diff cmd/squad.go` 为空 + 复跑 ok。

### 问一「多了什么」（修复轮 2 从代码进）

- 自检两向：模式 `git grep -E 'New\(' -- '*.go' | grep -v _test | wc -l` = **220**（三位数 ✓）；
  排除 `rowsRegistry(` 生产命中 = **0** ✓（仅测试）。
- **本轮新增导出符号 0 个**（`git diff 10db8338..HEAD -- '*.go'` 生产文件无
  `^+func/^+type/^+var/^+const`；新增均为测试函数）。
- 新增跨子系统绑定点 = **0**：`git diff 10db8338..HEAD -- '*.go' | grep '^+.*\.New('`
  （排除 _test）= 无；新增文件 = 0；新增依赖方向 = 0。

### 问二「少了什么」（修复轮 2 从契约进）

| 契约义务（修复轮 2） | 承载符号/字段（可 grep） |
|---|---|
| Major-1：server.go 零改动 | `git diff 10db8338..HEAD -- internal/agentd/server.go` = 空；SetupAutomation 2237→2237 |
| Major-2：删三块 wire 预检 | schedapi.go `handleCarrierPut`/`handleSquadPut` 内无 `strings.TrimSpace(in.Machine)`/`scheduling.CredentialStandalone`/`scheduling.RoleExecutor` 命中；词表常量生产命中仅 scheduling.go:135/171 |
| Major-2：400/500 分类两层化 | schedapi.go 文件头「400/500 分类策略（...收敛后，两层）」；schedPutErr 的 `case errors.Is(err, scheduling.ErrInvalid)` → 400 |
| Major-3：set 缺参/坏 expect | `TestSquadSetRejectsMissingNameBeforeDialing` / `TestSquadSetSurfacesStaleExpectConflict` |
| Major-3：set 400 可行动含名 | `TestSquadSetSurfacesActionableServerReject`（断言 err 含 "sq"+"executor"） |
| Major-3：set 版本往返 | `TestSquadSetEditLoopUsesReadVersion`（断言 PUT query=="expect=3"） |

### 正交扫描（修复轮 2）

A. 新增跨子系统绑定点 = 0；scheduling.New( 计数改前=1 改后=1（SetupAutomation 未触碰）。
B. 新增导出符号 = 0；新增文件 = 0；新增依赖方向 = 0。
C. 计数式判据双读数：schedapi.go 中 `scheduling.CredentialStandalone/MainHomeSync/RoleExecutor/
   RoleCoordinator` 引用 改前 4 → 改后 0；`schedapi.go` 转型命中 改前 4（2 校验+2 映射）→
   改后 2（纯映射）；server.go 锚点 2237→2237。
D. 同一语义两遍检查：词表校验检查点改前两处（网关预检+域校验）→ 改后唯一（域）；
   `%w` 产生点仍只在 scheduling.go 四处校验行，消费只在 schedPutErr 一处。

### 收尾三档读数（修复轮 2，全部亲跑）

1. `go test ./... -count=1` → **退出码 0**，全包 ok（agentd 145.946s、cmd 14.312s、ledger 20.896s）。
2. `go build ./...` → 退出码 0；`go vet ./...` → 退出码 0。
3. `gofmt -l internal/agentd/schedapi.go internal/agentd/schedapi_test.go cmd/squad_test.go`
   → 输出为空，退出码 0；正控：对 `/root/.handoff/tmp/6999f29a/gofmtctl/badfmt.go`
   （故意坏格式）跑同命令 → **打印文件名**（正控生效，零输出判据非空跑）。
- web：`npx vitest run src/api/contract.test.ts` → 39 passed（本轮 TS 零改动，仅复核）。

### 图硬闸

- `go run . graph check --repo .` → `"fails": []`（warns=既知图债，未新增跨域边）。

### 分次提交

- ff7977e2 `fix(agentd): Major-2 收敛——删 schedapi.go 三块 wire 预检...`
- d4e4eca5 `test(cmd): Major-3 补 squad set 编辑回路命令级测试...`
- （台账随收尾提交）
