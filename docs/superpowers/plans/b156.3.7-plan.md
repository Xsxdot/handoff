# Plan：B156.3.7 — K7 registry 双方言 PG smoke 回归

> 状态：**plan 出稿（2026-08-26，plan 节点 handoff executor）**
> 卡：B156.3.7 ｜ 有效基线分支：`claude/kai-156-3-ba-df7357` ｜ 工作分支：`cards/B156.3.7-charter`
> 上游：契约 `docs/superpowers/specs/b156.3-contract.md`（§3.1、§9 测试义务④）、
> 拆解 `docs/superpowers/specs/b156.3-breakdown.md` §三-K7（四岔口拍板记录对本卡有约束力）
> 法定产出物：本文件 + 台账 `docs/superpowers/plans/b156.3.7-plan-ledger.md`
> 红线自守：本轮零实现代码落盘；判据代码以下列方式验证——完整测试文件作为**探针**
> 在基线实跑（SQLite 腿 PASS、PG 腿显式 SKIP、go vet/gofmt 干净、整包回归 ok）后删除，
> diff 中无 `.go` 文件。探针全文 = 本文 Task A1 的代码块，逐字符一致。

---

## 0. 岔口拍板遵从声明

四条裁决逐一对照，均不触及本卡范围，无异见：

| 岔口 | 裁决 | 对 K7 的关系 |
|---|---|---|
| 一（CLI 覆盖面） | 方案 A 仅 opencode | 无关（hostapi 归 K1） |
| 二（唤醒边） | 方案 B+兜底 | 无关（keystone 编排归 K4/K5） |
| 三（准入 CAS 化） | 方案 A | 无关（scheduling 原子化归 K2）；K7 只从存储层锁 CAS 版本语义，不触准入并发 |
| 四（出厂种子） | 方案 B | 无关（登记面归 K3/K4） |

无「认为裁决错了」的异议项；未发现拆解错误。

---

## 1. 四段式（照抄拆解 §三-K7 并补行号锚）

- **①契约引用**：§3.1（registry 四原语签名与新表进 ensureSchema 双方言 DDL）、
  §9 测试义务④「registry 双方言（PG smoke）回归」。拍板记录①（字节化端口）是
  本卡要压的序列化边界——K7 的 PG 腿把存储方言再压一层。
- **②意图与为什么**：registry 是编制域一切持久的地基。现状查证发现：**全部
  registry SQL 至今只在 SQLite 上真实执行过**（仓内唯一触及 registry 的测试是
  keystone 竖切，走临时 SQLite；PG 冒烟三支测试均不触 registry 表）。若某条
  DDL/DML 在 PG 方言漏登记或方言不兼容，闸门全绿而生产建表/写入当场失败——
  「两侧入口各自绿、中间白名单挡死」的通道分裂形状。仓内已有 PG smoke 先例
  （`mirror_pg_test.go` / `store_pg_test.go`，挂点 `newPGStore`），照其挂点补齐。
- **③验收**（判据级，全部可判 pass/fail）：
  1. 新增 `TestPGRegistryCASAndSeq`：设 `LEDGER_TEST_PG_DSN` 时真跑 Put/Get/
     List/Delete + CAS 冲突两方向 + Seq 全表单调 + ErrNotFound 缺失语义 +
     入参校验错误分支 + body 多形态语义恒等；未设 DSN 时**显式 t.Skip 且输出
     原因**（禁止静默绿）。
  2. SQLite 侧同判据的对偶测试 `TestSQLiteRegistryCASAndSeq` 无条件跑
     （机内默认绿的那半），与 PG 腿共用同一判据体函数。
  3. 真机：PG DSN 环境下非 skip 跑一次并落证（真机清单第 4 条，
     **本 task 由协调者执行，不派发**）。
- **④入口指针**：
  - `internal/ledger/registry.go`（四原语：RegistryPut:31、RegistryGet:80、
    RegistryList:95、RegistryDelete:116）
  - `internal/ledger/store.go`（Open 双方言分支 54–66；ensureSchema registry DDL：
    PG 264–269、SQLite 339–344）
  - `internal/ledger/store_pg_test.go`（newPGStore 先例 10–22，skip 文案在 14）
  - 同形先例：`internal/ledger/mirror_pg_test.go`（跨连接竞争测试的清理纪律）

- **有界文件集**（拆解圈定，plan 不越出）：
  `internal/ledger/{registry_pg_test.go 新, registry.go 仅当发现方言缺口才改}`。

---

## 2. 判据先在基线跑（本节命令全部由本节点亲自跑到，输出为原文要点）

| # | 命令 | 实测结果 |
|---|---|---|
| V1 | `go build ./...` | exit 0 |
| V2 | `go test ./internal/ledger/ -count=1` | `ok github.com/Xsxdot/handoff/internal/ledger 12.489s` |
| V3 | `go test ./internal/ledger/ -run 'TestPG' -v -count=1` | 三支既有 PG 测试全部 `--- SKIP`（0.00s），整包 `ok`——skip 显式可见、非静默 |
| V4 | 探针文件落地后 `-run 'TestSQLiteRegistryCASAndSeq\|TestPGRegistryCASAndSeq' -v -count=1` | SQLite 腿 `--- PASS (0.09s)`；PG 腿 `SKIP: 未设 LEDGER_TEST_PG_DSN，跳过 PG 冒烟`（newPGStore:14 原文文案） |
| V5 | 探针在场时 `go vet ./internal/ledger/` 与 `gofmt -l internal/ledger/` | 均干净 |
| V6 | 探针在场时整包 `go test ./internal/ledger/ -count=1` | ok（12.520s） |

V4–V6 即「判据写下去的状态」的实证：SQLite 半边写下即绿（最薄路径豁免成立，
见 §6）；PG 半边在本环境**不可执行**——本机无 docker、无本地 postgres 二进制、
无 PG 相关环境变量（已探测），故 PG 腿行为判据如实标「**未验证，需真机**」，
归真机清单第 4 条。

### 库行为事实出处（纪律要求的 file:line 或最小验证程序）

| 库行为事实 | 出处 |
|---|---|
| Go string 参数写入 JSONB 列按原样字节编码 | pgx v5.10.0（`go.mod` 钉版）`pgtype/json.go`:24–27（PlanEncode 对 `string` 返回专用 plan）、95–101（原样 append 字节） |
| JSONB 列可扫描进 Go `*string` | 同文件 152–154（`case *string` 分支）与 173–179（scanPlanAnyToString 原样拷贝）——`RegistryGet` 把 body 扫进 `var body string` 因此在 PG 成立 |
| PG JSONB **不保留**空白与键序（归一化存储） | PostgreSQL 文档 jsonb 类型章节的明文行为；本节点无真 PG 可复演，测试跑通即为复演——字节级恒等断言因此**不可用**，判据改用语义恒等（§5 断言③） |
| Store 层对 kind 只要求非空，白名单执法在编制域 | registry.go:34–36（仅空串校验）；scheduling.go:226–228（Enqueue 拒未知排队种类）——冒烟用冻结五 kind 之外的 kind 合法且不绕任何门 |

---

## 3. Interfaces

### Consumes（全部为仓内现存符号，零新增接缝）

```go
// internal/ledger
func Open(dsn string) (*Store, error)                                    // store.go:51
func (s *Store) RegistryPut(kind, id string, expectVersion int, body []byte, actor string) (int, error)
func (s *Store) RegistryGet(kind, id string) (RegistryEntry, error)      // 缺失 ErrNotFound
func (s *Store) RegistryList(kind string) ([]RegistryEntry, error)       // seq 升序
func (s *Store) RegistryDelete(kind, id string, expectVersion int, actor string) error
type RegistryEntry struct {
    Kind    string          `json:"kind"`
    ID      string          `json:"id"`
    Version int             `json:"version"`
    Seq     int64           `json:"seq"`
    Body    json.RawMessage `json:"body"`
}
var ErrNotFound    // ledger.go:22  errors.New("ledger: 记录不存在")
var ErrCASConflict // ledger.go:23  errors.New("ledger: 状态已被并发修改")
// 包内既有测试助手（store_pg_test.go:10）
func newPGStore(t *testing.T) *Store   // 未设 LEDGER_TEST_PG_DSN 时 t.Skip("未设 LEDGER_TEST_PG_DSN，跳过 PG 冒烟")
```

### Produces（仅包内非导出测试符号；生产 API 零变化）

```go
func TestPGRegistryCASAndSeq(t *testing.T)                                // PG 腿（条件执行）
func TestSQLiteRegistryCASAndSeq(t *testing.T)                            // SQLite 腿（无条件）
func assertRegistrySemantics(t *testing.T, s *Store)                      // 双方言共用判据体
func assertSameJSON(t *testing.T, label string, want, have []byte)        // 语义恒等断言
func cleanRegistrySmoke(t *testing.T, s *Store)                           // kind 边界清场
const registrySmokeKind = "registry-smoke-test"
```

邻卡交集核查：K2/K5 经 `schedclient.Registry` 端口与 `facadeAsRegistry` 适配器消费
本层，接口形状本卡一字不动——Produces 全部是 `package ledger` 内部测试符号，与
任何邻卡无签名冲突面。

### 条件分支（Task B2）的 Produces 预告

若真机 PG 腿翻红且根因落在 `registry.go` 内的方言无关 SQL 用法：允许对该文件做
**行为保持**的最小修复（修复后 PG/SQLite 两腿同绿）。若根因落在 `store.go` 的
registry DDL 或其它任何文件——**越出有界文件集，停下提问，不得自行修**。

---

## 4. Task 分解（动作粒度 2~5 分钟）

### Task A：新增双方言回归测试（有界文件集中的新文件）

**A1. 写文件 `internal/ledger/registry_pg_test.go`，内容为下述完整代码**（含文件头
职责/边界注释、每个 helper 的为什么注释——注释步骤与代码步骤同动作交付）：

```go
// registry 双方言冒烟回归（B156.3 契约 §3.1 四原语 + §9 测试义务④，卡 B156.3.7）。
//
// 职责：把 registry 四原语的冻结语义（CAS 版本、全表单调 Seq、ErrNotFound、
// 入参校验、body 字节搬运边界）在同一组判据下分别压过 PG 与 SQLite 两个存储
// 方言——某条语句只在一侧方言可用时，另一腿必翻红。
//
// 边界：只测 internal/ledger 存储层；编制域语义（准入、队列排序）归 K2 的
// scheduling 层测试，门面 DTO 映射已由竖切（internal/keystone/slice_test.go）
// 锁定，此处不再重复。
//
// 为什么两条腿共用一个文件：拆解为本卡圈定的有界文件集只允许新增一个测试
// 文件；两腿判据体是同一个函数，分文件只会复制粘贴。
//
// 已知方言不对称（刻意不锁进断言）：PG 的 JSONB 归一化空白与键序，SQLite
// TEXT 逐字节透传——序列化边界因此断言 JSON 语义恒等（assertSameJSON）而非
// 字节恒等；消费方（schedclient.Record → 编制域）按语义读 body，字节形态不是
// 任何契约的一部分。
package ledger

import (
	"encoding/json"
	"errors"
	"fmt"
	"path/filepath"
	"reflect"
	"testing"
)

// registrySmokeKind 是冒烟专用的 kind——刻意取冻结五 kind（carrier/squad/
// sched_running/ignition_queue/launch_queue）之外的值：Store 层对 kind 只要求
// 非空（契约拍板记录④「账本不理解 body」，白名单执法在编制域
// scheduling.go Enqueue）。用外来 kind 让清理段的作用域与生产数据零交集，
// LEDGER_TEST_PG_DSN 即使指向带真数据的开发库也不会误伤或撞行。
const registrySmokeKind = "registry-smoke-test"

// TestPGRegistryCASAndSeq 是真机半边：设 LEDGER_TEST_PG_DSN 后真跑判据体；
// 未设时 newPGStore 显式 skip 并输出原因（skip 是绿的，但必须让人看见为什么绿；
// 真机清单第 4 条要求至少一次非 skip 落证）。
func TestPGRegistryCASAndSeq(t *testing.T) {
	s := newPGStore(t)
	assertRegistrySemantics(t, s)
}

// TestSQLiteRegistryCASAndSeq 是机内默认绿的半边：与 PG 腿共用判据体，
// 无条件执行——它锁「SQLite 行为不被后续改动破坏」，并与 PG 腿构成同判据对偶。
func TestSQLiteRegistryCASAndSeq(t *testing.T) {
	s, err := Open(filepath.Join(t.TempDir(), "ledger.db"))
	if err != nil {
		t.Fatalf("打开临时 SQLite 账本: %v", err)
	}
	t.Cleanup(func() { s.Close() })
	assertRegistrySemantics(t, s)
}

// assertRegistrySemantics 双方言共用的判据体。断言编号①–⑨与 plan §5 的
// 验收映射表一一对应；seq 断言一律用相对比较（共享库上绝对前值不定）。
func assertRegistrySemantics(t *testing.T, s *Store) {
	t.Helper()
	cleanRegistrySmoke(t, s)
	t.Cleanup(func() { cleanRegistrySmoke(t, s) })

	// ① 新建：expectVersion=0 且行不存在 → 版本从 1 起
	v, err := s.RegistryPut(registrySmokeKind, "ent-1", 0, []byte(`{"n":1}`), "smoke")
	if err != nil {
		t.Fatalf("新建 ent-1: %v", err)
	}
	if v != 1 {
		t.Fatalf("新建后版本应为 1，得 %d", v)
	}

	// ② CAS 冲突两个方向：对已存在行重放 expect=0；用过期/未来版本号更新
	if _, err := s.RegistryPut(registrySmokeKind, "ent-1", 0, []byte(`{"n":9}`), "smoke"); !errors.Is(err, ErrCASConflict) {
		t.Fatalf("已存在行 expect=0 应 ErrCASConflict，得 %v", err)
	}
	cur, err := s.RegistryGet(registrySmokeKind, "ent-1")
	if err != nil {
		t.Fatalf("读回 ent-1: %v", err)
	}
	if _, err := s.RegistryPut(registrySmokeKind, "ent-1", cur.Version+1, []byte(`{"n":8}`), "smoke"); !errors.Is(err, ErrCASConflict) {
		t.Fatalf("未来版本应 ErrCASConflict，得 %v", err)
	}

	// ③ Get 读回元字段 + 序列化边界（JSON 语义恒等，理由见文件头不对称注记）
	if cur.Kind != registrySmokeKind || cur.ID != "ent-1" || cur.Version != 1 {
		t.Fatalf("元字段不符: %+v", cur)
	}
	assertSameJSON(t, "ent-1 body", []byte(`{"n":1}`), cur.Body)

	// ④ Seq 全表单调：更新后严格递增；跨实体新建也严格递增
	v2, err := s.RegistryPut(registrySmokeKind, "ent-1", cur.Version, []byte(`{"n":2}`), "smoke")
	if err != nil || v2 != cur.Version+1 {
		t.Fatalf("按当前版本更新: v=%d err=%v", v2, err)
	}
	after, err := s.RegistryGet(registrySmokeKind, "ent-1")
	if err != nil {
		t.Fatalf("更新后读回: %v", err)
	}
	if after.Seq <= cur.Seq {
		t.Fatalf("更新后 seq 应严格递增: %d -> %d", cur.Seq, after.Seq)
	}
	if _, err := s.RegistryPut(registrySmokeKind, "ent-2", 0, []byte(`{"n":3}`), "smoke"); err != nil {
		t.Fatalf("新建 ent-2: %v", err)
	}
	e2, err := s.RegistryGet(registrySmokeKind, "ent-2")
	if err != nil {
		t.Fatalf("读回 ent-2: %v", err)
	}
	if e2.Seq <= after.Seq {
		t.Fatalf("跨实体 seq 应严格递增: %d -> %d", after.Seq, e2.Seq)
	}

	// ⑤ List：只含本 kind、按 seq 升序、body 语义恒等
	rows, err := s.RegistryList(registrySmokeKind)
	if err != nil {
		t.Fatalf("List: %v", err)
	}
	if len(rows) != 2 {
		t.Fatalf("应恰 2 行，得 %d", len(rows))
	}
	if rows[0].ID != "ent-1" || rows[1].ID != "ent-2" || rows[0].Seq >= rows[1].Seq {
		t.Fatalf("List 应按 seq 升序: %+v", rows)
	}
	assertSameJSON(t, "list[0] body", []byte(`{"n":2}`), rows[0].Body)

	// ⑥ ErrNotFound 缺失语义（errors.Is 可判定——包装错误不得吞哨兵）
	if _, err := s.RegistryGet(registrySmokeKind, "absent"); !errors.Is(err, ErrNotFound) {
		t.Fatalf("缺失读应 ErrNotFound，得 %v", err)
	}

	// ⑦ body 多形态语义恒等：嵌套对象/数组/unicode/null/数字精度各取一样本
	samples := []string{
		`{"name":"载体甲","nested":{"k":[1,2,{"deep":true}]}}`,
		`[1,"二",3.5,null]`,
		`{"emoji":"🚀","empty":"","zero":0,"big":9007199254740993}`,
		`true`,
	}
	for i, sample := range samples {
		id := fmt.Sprintf("shape-%d", i)
		if _, err := s.RegistryPut(registrySmokeKind, id, 0, []byte(sample), "smoke"); err != nil {
			t.Fatalf("写入样本 %s: %v", sample, err)
		}
		back, err := s.RegistryGet(registrySmokeKind, id)
		if err != nil {
			t.Fatalf("读回样本 %s: %v", sample, err)
		}
		assertSameJSON(t, id, []byte(sample), back.Body)
	}

	// ⑧ 入参校验错误分支：空 kind / 空 id / 空 body 在写路径拒绝
	if _, err := s.RegistryPut("", "x", 0, []byte(`{}`), "smoke"); err == nil {
		t.Fatal("空 kind 应被拒")
	}
	if _, err := s.RegistryPut(registrySmokeKind, "", 0, []byte(`{}`), "smoke"); err == nil {
		t.Fatal("空 id 应被拒")
	}
	if _, err := s.RegistryPut(registrySmokeKind, "x", 0, nil, "smoke"); err == nil {
		t.Fatal("空 body 应被拒")
	}

	// ⑨ Delete 的 CAS 三分支：错版本冲突 → 对版本成功 → 重复删除冲突 → 删后读缺失
	if err := s.RegistryDelete(registrySmokeKind, "ent-2", e2.Version+7, "smoke"); !errors.Is(err, ErrCASConflict) {
		t.Fatalf("错版本删除应 ErrCASConflict，得 %v", err)
	}
	if err := s.RegistryDelete(registrySmokeKind, "ent-2", e2.Version, "smoke"); err != nil {
		t.Fatalf("对版本删除: %v", err)
	}
	if err := s.RegistryDelete(registrySmokeKind, "ent-2", e2.Version, "smoke"); !errors.Is(err, ErrCASConflict) {
		t.Fatalf("重复删除应 ErrCASConflict，得 %v", err)
	}
	if _, err := s.RegistryGet(registrySmokeKind, "ent-2"); !errors.Is(err, ErrNotFound) {
		t.Fatalf("删除后读应 ErrNotFound，得 %v", err)
	}
}

// assertSameJSON 断言两段字节是同一份 JSON 文档。语义比较而非字节比较：
// PG JSONB 归一化空白与键序（文件头注记），字节比较在 PG 腿必然假红；
// 数字统一 unmarshal 为 float64 后比较——JSON 数文本就没有整浮之分，语义层面无损。
func assertSameJSON(t *testing.T, label string, want, have []byte) {
	t.Helper()
	var w, h any
	if err := json.Unmarshal(want, &w); err != nil {
		t.Fatalf("%s: 判据自身不是合法 JSON: %v", label, err)
	}
	if err := json.Unmarshal(have, &h); err != nil {
		t.Fatalf("%s: 存储层返回的不是合法 JSON: %v", label, err)
	}
	if !reflect.DeepEqual(w, h) {
		t.Fatalf("%s: JSON 语义不等: want=%s have=%s", label, want, have)
	}
}

// cleanRegistrySmoke 清掉本冒烟自己 kind 的行——杀伤半径以 kind 为界，不碰
// 同库其它数据（store_pg_test.go 清理段的同款纪律）。测试体前后各清一次：
// 前清防上次异常残留让「expect=0 新建」假冲突，后清防残留扩散。
// 清场失败直接 Fatal 而非像 mirror_pg_test.go:19 那样忽略：我们的行有唯一的
// kind 边界，DELETE 失败意味着库本身不可用，继续跑只会产出不可信结果。
func cleanRegistrySmoke(t *testing.T, s *Store) {
	t.Helper()
	if _, err := s.db.Exec(s.q(`DELETE FROM registry WHERE kind = ?`), registrySmokeKind); err != nil {
		t.Fatalf("清场 registry kind=%s: %v", registrySmokeKind, err)
	}
}
```

**A2. 机内验证（预期输出以 V4–V6 实测为准）**：

```
go test ./internal/ledger/ -run 'TestSQLiteRegistryCASAndSeq|TestPGRegistryCASAndSeq' -v -count=1
# 预期：TestPGRegistryCASAndSeq --- SKIP（原因文案）；TestSQLiteRegistryCASAndSeq --- PASS
go test ./internal/ledger/ -count=1      # 预期 ok
go vet ./internal/ledger/ && gofmt -l internal/ledger/   # 预期均干净
```

**A3. 提交**：仅 `docs/`（本 plan + 台账）与本测试文件入 commit；确认 diff 无
`.go` 以外越轨项（本节点轮次无此项，实现轮次按此自查）。

### Task B：PG 腿真机执行与缺口处置（真机清单第 4 条）

**B1（本 task 由协调者执行，不派发）**：在指向专用测试库的
`LEDGER_TEST_PG_DSN` 下跑：

```
LEDGER_TEST_PG_DSN=postgres://… go test ./internal/ledger/ -run 'TestPGRegistryCASAndSeq' -v -count=1
```

原始输出落证台账；DSN 必须指向专用测试库（判据体会 DELETE 本 kind 的行，
store_pg_test.go:38 注释的同款前提）。

**B2 若翻红**：贴原始报错原文，不替它归因。根因处置分流：
- 根因在 `registry.go` 内（如占位符用法、扫描类型）：走红绿周期——先让该测试
  在 PG 上稳定红，再做行为保持的最小修复，两腿同绿后提交；修复的错误包装沿用
  既有 `fmt.Errorf("registry %s/%s …: %w", kind, id, …)` 模式保留上下文，
  不新引 logger（包内结构化日志面 `log()` 只属 store.go 打开路径，测试层以
  t.Fatalf/t.Logf 承担可观测性）。
- 根因在 `store.go` DDL 或其它任何文件：越出有界文件集，**停下提问**。

### 最薄路径条豁免声明（红绿模板为何不套在 Task A）

本卡锁的行为今天从声明缝调用已得到断言的预期结果（SQLite 半边：Ticket 0 落地后
经竖切真实跑过；判据体 V4 实测 PASS），写下即绿——豁免成立。PG 半边的「红」
无法在无机内 PG 的环境下构造，其红绿周期只能在 B1/B2 的真机现场发生，已在该处
写明。缝上存在旧覆盖（竖切经门面的间接覆盖）不构成免除，本卡的直接缝级测试
仍是新增交付物。

---

### Task C：双方言表集合对等检（协调者追加，2026-08-26）

> **这是协调者对本卡有界文件集的一次显式扩张**，不是执行者自行越界。扩张范围：
> `internal/ledger/store.go`（一次行为保持的提取重构）＋ 一个新测试文件。理由与
> 判据见下；实现票照此执行，不必再问。

**为什么加**：2026-08-26 对方卡线上真实发生过一次——`store.go` 的 registry 表与
另一张新表在 **SQLite 块**撞出合并冲突并被人工处置，而 **PG 块被 git 静默自动合上**。
只看报冲突那一处、按直觉二选一，就会留下「一个方言有这张表、另一个没有」，
**且全程不报错**。本卡 Task A 的 PG 腿在无 DSN 时 `t.Skip`，所以那种缺口在日常
`go test ./...` 里**永远是绿的**，要等到有人拿真 PG 跑（很可能是生产建表）才炸。

这条危害不限于 registry：`ensureSchema` 里每一张表都在同一个形状下。

**做什么**（两步，都很小）：

1. 把 `ensureSchema` 里构造 DDL 语句表的那段提取成 `func ddlStatements(pg bool) []string`，
   `ensureSchema` 改为调用它。**行为逐字保持**——只搬运，不改任何 SQL 文本、不改顺序。
2. 新增测试：取 `ddlStatements(true)` 与 `ddlStatements(false)`，各自解析出
   `CREATE TABLE IF NOT EXISTS <名>` 的表名集合，断言**两个集合相等**；不相等时
   报文要列出「只在 PG 有」与「只在 SQLite 有」两份差集。

**为什么是集合相等而不是「registry 在不在」**：判据写成「那张表在」只保护写判据那天
想到的那一张；写成集合相等，以后任何人往任一方言块加表而漏掉另一块，都会当场翻红，
判据不需要跟着改。（同理：**不要用行号当判据**——PG 块 264–269、SQLite 块 339–344
这组行号只用于定位，它们会漂。）

**验收**：

- `go test ./internal/ledger/ -run TestDDLDialectParity -count=1` 绿；
- **变异复验（必做）**：从 PG 那一支的语句表里临时删掉 `registry` 建表语句，该测试
  必须翻红且报文点出「只在 SQLite 有：registry」；复原后复绿。这一条是本 task 唯一
  能证明它有牙齿的证据——**无条件跑，不依赖 DSN**，正是 Task A 的 PG 腿覆盖不到的那半。
- 提取重构的等价性：`go build ./... && go vet ./... && go test ./internal/ledger/ -count=1`
  全绿，且 `ddlStatements` 两个分支的语句文本与重构前逐字节一致（用 `git diff` 自证：
  搬运行之外无字符改动）。

**扩张后的有界文件集**：`internal/ledger/{registry_pg_test.go 新, ddl_parity_test.go 新,
store.go 仅本 task 的提取重构, registry.go 仅当发现方言缺口才改}`。

---

## 5. 验收映射（breakdown K7 ③ 三条 ↔ 判据体断言 ↔ 测试范围）

| breakdown 验收 | 断言 | 所在腿 |
|---|---|---|
| 设 DSN 真跑 Put/Get/List/Delete | ①③④⑤⑨ | PG（条件）+ SQLite（无条件）双腿同判据 |
| CAS 冲突 | ②⑨ | 双腿 |
| Seq 单调 | ④⑤ | 双腿 |
| ErrNotFound 缺失语义 | ⑥⑨ | 双腿 |
| 显式 t.Skip 且输出原因 | newPGStore:14 既有实现，V3/V4 实测文案 | PG 腿 |
| 入参校验（补充锁定，防方言间校验漂移） | ⑧⑦ | 双腿 |
| 真机非 skip 落证 | B1 | 协调者 |

**测试范围声明（最小化）**：本卡只跑 `./internal/ledger/` 一个包的三条命令
（A2 全列）。全量 `go test ./...` 不属于本 task，归集成收口。

---

## 6. 五项检查

**1. 缺陷族对抗审查**（对照 breakdown §四五族）：

- **生命周期/状态机中断**：无状态机可中断；Store 连接经 t.Cleanup 关闭，无孤儿
  资源；共享 PG 库上的残留行被前清兜底。结论：无风险。
- **静默失败/误导报错**：本卡最大的静默形状恰是「PG skip 恒绿」——处置=skip 带
  原因（既有实现实测）+ 真机第 4 条非 skip 落证义务；断言失败全部携带
  期望 vs 实际与 kind/id 上下文。结论：有风险，两条防线如上。
- **跨平台假设**：判据体不含路径/OS/时区假设（TempDir 由框架供给、时间只用
  相对 seq 比较）；Windows 项无关。结论：无风险。
- **假红/假绿**：seq 用相对比较防共享库前值不定造成的假红；JSONB 字节差异已
  从断言中剔除（防 PG 腿假红）；变异复验举例（本卡虽无强制要求，列出三个
  变异靶证明判据能变红）：把 RegistryPut 的 COALESCE(MAX(seq),0)+1 改成常量
  1 → ④翻红；ErrNotFound 包装改成裸返回 sql.ErrNoRows → ⑥⑨翻红；Delete 去掉
  RowsAffected==0 检查 → ⑨翻红。结论：有防线。
- **门禁绕过**：测试写路径全部经 Store 公开方法，无旁路；冒烟外来 kind 不绕
  白名单（白名单执法在 scheduling 层，§2 出处表末行）。结论：无风险。

**2. 序列化边界设问**：body 从 Put 到 Get 的投影链＝Go string 参数 → 方言编码
（TEXT 透传 / JSONB 解析重排）→ 扫描回 string → RawMessage。断言⑦的多形态
表驱动样本穿过这条真实存储边界，一条判据覆盖嵌套/数组/unicode/null/数字精度
一族（roundtrip 思想的表驱动形态）；「字段缺失 vs 值为零」的可空分辨在此不适用
（body 是整块字节，无逐字段投影——逐字段分辨是 K2 对 IgnitionRequest 的义务，
归属不同卡）。「两端各自有测试不算数」的链路要求：本判据本身就是穿过边界的
那一支。已知不对称（JSONB 归一化）已文档化并刻意不锁。

**3. 上下文预算检查**：有界文件集 1+1 文件（新测试文件 + 条件性 registry.go），
判据代码 ~230 行，圈得住。

**4. 类型标注**：d_ledger 为逻辑型子系统，无边界型真机清单义务；但本卡自带一条
环境依赖验收（PG），已按真机清单第 4 条格式登记（执行法、归属、落证方式齐）。

**5. 接缝覆盖（双向）**：

- 缝清单（本卡 spec 测试决定的接缝）：S1 = Store.Registry 四原语 ↔ 双方言存储
  边界（契约 §3.1）。S2 = schedclient.Record 字节化端口两侧翻译——归属竖切与
  契约 §13 验证记录，K7 不重复锁、也不删其既有锁。
- 测试→缝：两支测试的入口调用符号均为 S1 上的公开方法（看入口不看标注）✓。
- 缝→测试：S1 被 SQLite 腿（无条件）与 PG 腿（条件）各锁一遍 ✓。
- 内部锁声明：**无内部锁**——所有断言入口都在 S1 缝上，无需援引例外理由。
- 退路声明：步骤正文无条件退路。

---

## 7. 占位符扫描自我声明

全文无 TBD/「适当的错误处理」/「同 Task N」；Task A 给出完整可编译代码并在基线
实跑验证（V4–V6）。例外通道（照抄既有 harness + 断言清单）未使用。harness 复用
仅限既有符号 `newPGStore`（指认了文件与行号），其余代码完整直给。

## 8. 跨卡审计（单卡稿的自审形态，结论标注依据）

1. **对照冻结物逐条**：Consumes 四原语签名与契约 §3.1 代码块及源码
   registry.go:31/80/95/116 逐字符一致；RegistryEntry 五字段一致；§9④ 原文
   「registry 双方言（PG smoke）回归」已落 Task A/B。
2. **跨卡签名比对**：Produces 全部为包内非导出测试符号（§3），与 K2/K5 消费的
   facade/端口签名零交集；条件分支 B2 的修改半径限于包内私有 SQL 用法，不改
   任何导出签名。
3. **spec 故事归属**：契约 §9 测试义务④ → Task A（机内双腿）+ Task B（真机）；
   breakdown K7 ③ 三条验收 → §5 映射表逐条有着落，无落空项。

## 9. 收口自查

① 有没有把没亲自跑到结果的命令写成结论？——PG 腿真跑未发生，全文凡涉 PG
行为处均标「未验证，需真机」（§2 V4 说明、§4 B1、§6 族 1/2）；SQLite 腿、
编译、vet、fmt、整包回归均为本节点亲跑，证据在 §2 与台账。
② 这一轮碰过 handoff CLI 或起过新 executor 吗？——没有；全程只动 go/git/grep
本地工具链，未派发、未调 handoff 写路径。
