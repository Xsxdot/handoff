# B156.2.2 实现计划：C2 d_ledger·事件写入点补肉（欠账 #2 账本半 + #7 账本半）

**状态**：plan 节点产出（charter 流），2026-08-26。
**有效基线分支**：`feature/b156-workbench`（合并目标以此为准）。本计划在 `cards/B156.2.2-charter` 上执行，HEAD=b4231fb5。
**上游输入**：spec `2026-08-25-b156.2-collaboration-rooms-design.md`（已批准）；契约 `b156.2-contract.md`（含 §11 微修订）；breakdown `b156.2-breakdown.md`（C2 卡四段式）。
**台账**：`docs/superpowers/ledgers/ledger-b156.2.2-plan.md`（基线复核与探针证据在案）。

本卡只做两件事，均为纯填肉、零签名改动、零 schema 改动：

1. `Store.RecordMessageConsumed`：消费标记的账本半——同 mutate 事务内查重后写，恰好一次的权威落点（拍板 5.4）；
2. `Store.EnsureComment`：幂等留痕评论的账本半——C3（补解析接入）的承重件。

门面半（collab.Consume/Pending/Mentions）归 C5；NodeStep 接入归 C3。**账本半验收断言不依赖门面行为，反之亦然**（拍板 5.4 的分工纪律）。

---

## 0. 冻结物核对（对照冻结物逐条）

| 冻结条目 | 本计划的对应 | 越界？ |
| --- | --- | --- |
| 契约 §3.3 Consume 注释「RecordMessageConsumed 在同一 mutate 事务内查重后写（ClearNeedsHumanFrom 同形）。幂等重复消费返回 nil」 | T2.1 全部行为 | 否 |
| 契约 §3.7 EnsureComment 签名与 dedupe_key 语义；「供 ledgerstep 直接经 St 使用，不经 collab 门面」 | T2.2；测试入口因此只能落在 Store 本体（内部锁声明见 §7） | 否 |
| 契约 §4 读面块后三条：「Consume 成功后同参重试返回 nil 且不产生第二条 message_consumed」「Consume 对他人已消费的同一条返回 nil 且不产生第二条标记」「message_consumed 账本 payload 含 message_seq 与 consumer 两键」 | T2.1 的 TestRecordMessageConsumedExactlyOnceThroughSeam 三段断言（账本面；门面路径由 C5 另锁） | 否 |
| 契约 §4 补解析块第 2 条的账本半：「同一张卡重复触发补解析不产生第二条说明评论（EnsureComment dedupe_key 生效）」 | T2.2 的 TestEnsureCommentWritesOncePerKey（账本半=二次调用 false 且 EvComment 计数不变；全条断言归 C3） | 否 |
| 契约 §3.1 词表：EvMessageConsumed 已在 types.go:74；EvComment 在 types.go:58 | 直接消费既有常量，零词表改动 | 否 |
| breakdown 岔口六方案甲（若已拍板）：Consume 对无效 seq 幂等 nil | RecordMessageConsumed 不校验消息存在性，反面测试锁死该选择并在注释言明 | 否 |
| 拍板 5.4：恰好一次权威在账本事件，会话侧游标纯缓存 | 查重+写入同 mutate 事务=单写者串行化免费获得恰好一次 | 否 |
| spec 测试接缝清单 #2：出站 client 接口缝 | T2.1 断言从 api.Facade（LedgerClient 的组装点绑定实现）进入 | 否 |

零触碰点名（照抄 breakdown）：d_protocol（本轮不改一字）、d_sessions、d_collab 生产代码、d_gateway、d_cli、d_web。**proto 不加 ConsumedMarker 结构**——载荷两键由 ledger 侧局部 struct 与 collab 侧局部镜像各自声明，等值由两侧字面量测试钉住（先例 TestRoomEventTypeLiteralMatchesLedger，service_test.go:222-226）；给 proto 加类型即动 d_protocol，违反零触碰。

## 1. 现状查证（全部亲读亲测，出处可复查）

| # | 事实 | 出处 |
| --- | --- | --- |
| 1 | RecordMessageConsumed 空壳：吞参返 nil，零行为 | `internal/ledger/rooms.go:33-38` |
| 2 | EnsureComment 空壳：吞参返 (false,nil)，零行为 | `internal/ledger/events.go:232-238` |
| 3 | Facade.RecordMessageConsumed 直通转调已接线，签名一致 | `internal/ledger/api/api.go:76-78` |
| 4 | ClearNeedsHumanFrom 同形先例：mutate 内 SELECT 后按条件 appendEvent | `internal/ledger/events.go:312-340` |
| 5 | mutate 写事务：PG advisory lock 串行化 / SQLite 单连接单写者 → fn 内读-判-写原子 | `internal/ledger/store.go:152-166`、store.go:61-65 |
| 6 | card_events.payload：SQLite TEXT / PG JSONB；EventsFromAsc 以 string 扫描两方言通吃 | store.go:225-229、293-297；events.go:88 |
| 7 | getCardTx 卡不存在返回 ErrNotFound | `internal/ledger/cards.go:374-380` |
| 8 | RecordRoomMessage：cardID 非空时 getCardTx 存在性校验 + appendEvent，返回 seq | `internal/ledger/rooms.go:15-28` |
| 9 | addComment 载荷形状 {kind,body,refs[,human_reset_node]}，kind∈{普通,更正} | `internal/ledger/events.go:210-213` |
| 10 | MarkNeedsHuman 空 reason 报错先例（守卫风格） | events.go:280-283 |
| 11 | slog 结构化 logger：包内 log() = slog.Default() | `internal/ledger/ledger.go:33` |
| 12 | LedgerClient 接口含 RecordMessageConsumed(cardID string, msgSeq int64, consumer string) error（8 方法之一，§11 微修订后） | `internal/collab/client/client.go`（契约 §3.4） |
| 13 | 库缝形态夹具先例：真 SQLite + PutWorkflow 种子 + CreateCard | `internal/collab/service_test.go:24-48` |
| 14 | 图闸现状：`--view cards-B156.2-charter-4` fails=0 warns=97（2026-08-26 实测）；裸 check 4 处 dead 为 absorb 前固有形态，不修 | 台账在案 |

**基线绿**：`go build ./...`=OK；`go test ./internal/ledger/...`=ok 12.707s、api 包 no test files（台账原文）。

## 2. 有界文件集（上下文预算检查）

| 文件 | 动作 | 归属 task |
| --- | --- | --- |
| `internal/ledger/rooms.go` | 补肉 RecordMessageConsumed + 新增 consumedMarker 类型 | T2.1 |
| `internal/ledger/api/api_test.go` | **新建**（package api）：T2.1 缝级断言 | T2.1 |
| `internal/ledger/events.go` | 补肉 EnsureComment | T2.2 |
| `internal/ledger/events_test.go` | 追加 2 个测试函数 | T2.2 |

圈得出，四个文件一个域。与 breakdown 试划的 `rooms_test.go(新)` 不同：RecordMessageConsumed 的断言全部可从缝（api.Facade）构造，落 `api/api_test.go` 更符合接缝覆盖闸；rooms_test.go 不再需要。偏差理由记录于此。

## 3. Task 分解

### T2.1 RecordMessageConsumed 补肉（欠账 #2 账本半）

**Interfaces**

- Produces：
  - `func (s *Store) RecordMessageConsumed(cardID string, msgSeq int64, consumer string) error`（签名与冻结 stub 一字不变）
  - 事件形状：type=`message_consumed`（types.go:74）、card_id=cardID 入参（""=项目级无卡事件）、actor=consumer、payload JSON 恰两键 `{"message_seq":<int64>,"consumer":"<string>"}`。
- Consumes（既有符号，精确签名）：
  - `func (s *Store) mutate(fn func(tx *sql.Tx, sink *eventSink) error) error`（store.go:156）
  - `func (s *Store) appendEvent(tx *sql.Tx, sink *eventSink, cardID, typ, actor string, payload any) (int64, error)`（events.go:19）
  - `func getCardTx(s *Store, tx *sql.Tx, id string) (Card, error)`（cards.go:374；不存在→ErrNotFound）
  - `const EvMessageConsumed = "message_consumed"`（types.go:74）
  - `func log() *slog.Logger`（ledger.go:33）

**行为语义（实现前读三遍）**

1. 查重键=`(msgSeq, consumer)`。seq 全局唯一自增，cardID 不参与查重键——调用方传错 cardID 时幂等性优先于报错。
2. marker 的 actor 列存 consumer：查重先按列粗筛（`WHERE type=? AND actor=?`），再解载荷精比 message_seq。载荷精确匹配无法用跨方言 SQL 表达（SQLite TEXT vs PG JSONB），Go 解析是唯一可移植路径。
3. 不校验 msgSeq 指向的消息是否存在、是否 room_message：岔口六方案甲「目标态『已消费』对不存在消息天然成立」，反面测试锁死；consumer 为空则报错——「谁消费了哪条」没有「谁」无意义（守卫风格照 MarkNeedsHuman 先例）。
4. cardID 非空时校验卡存在（与 RecordRoomMessage 同形）；marker 挂在与被消费消息相同的流上。

**步骤（红绿模板：锁缝断言步骤，法定适用）**

1. （~2min）基线复核：跑 `go test ./internal/ledger/api/` → 「no test files」（或台账记录的现状）；确认 rooms.go:33-38 仍是 stub。判据已在 plan 轮基线验证（台账探针三条 FAIL 原文）。
2. （~5min）写失败测试：新建 `internal/ledger/api/api_test.go`，内容=下方完整代码块。
3. （~1min）跑红：`go test ./internal/ledger/api/ -run TestRecordMessageConsumed -v`。预期恰三条 FAIL（ExactlyOnce「实得 0」/GroupMarker「恰一条: []」/Rejects「got <nil>」）、一条 PASS（UnknownSeq 幂等 nil 语义锁今天即绿，预期内）。贴原文到台账。
4. （~4min）最小实现：`internal/ledger/rooms.go` 按下方代码块改写 RecordMessageConsumed 并新增 consumedMarker 类型；文件头注释保持既有职责声明不动。
5. （~1min）跑绿：同步骤 3 命令全 PASS；再跑 `go test ./internal/ledger/...` 确认无回归。贴原文到台账。
6. （~1min）日志检查：成功路径、幂等跳过路径均有结构化日志（代码块内已含），grep 确认无 print/fmt.Println 进生产路径：`grep -n "Println\|fmt.Print" internal/ledger/rooms.go` 应零命中。
7. （~1min）提交：`git add internal/ledger/rooms.go internal/ledger/api/api_test.go && git commit`（message 见 §8）。

**完整代码块——`internal/ledger/api/api_test.go`（新建，package api）**

```go
// Package api 的缝级测试（spec 测试接缝清单 #2：client.LedgerClient 出站
// 接口缝的实现侧）。RecordMessageConsumed 的账本行为断言从 Facade 进入——
// 它是组装点绑定的真实实现，穿过它等于穿过声明缝；计数与载荷键集均可经
// Facade.EventsFromAsc 观测，无需绕进 Store 内部。
package api

import (
	"encoding/json"
	"errors"
	"path/filepath"
	"testing"

	"github.com/Xsxdot/handoff/internal/ledger"
	"github.com/Xsxdot/handoff/internal/proto"
)

// newFixture 起真 SQLite 账本并按组装点同形构造 Facade（service_test.go
// 夹具同形）。工作流种子只服务建卡；群级路径不需要任何卡。
func newFixture(t *testing.T) (*Facade, *ledger.Store) {
	t.Helper()
	st, err := ledger.Open(filepath.Join(t.TempDir(), "ledger.db"))
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	t.Cleanup(func() { st.Close() })
	if _, err := st.PutWorkflow("bug", ledger.WorkflowDef{Nodes: []ledger.NodeDef{
		{Name: ledger.StatusTodo, Next: ledger.StatusDoing},
		{Name: ledger.StatusDoing, Next: ledger.StatusDone},
		{Name: ledger.StatusDone},
	}}); err != nil {
		t.Fatalf("seed workflow: %v", err)
	}
	return New(st), st
}

// mustCardFor 建一张卡并返回。
func mustCardFor(t *testing.T, st *ledger.Store, title string) ledger.Card {
	t.Helper()
	card, err := st.CreateCard(ledger.NewCard{Title: title, Project: "handoff", Workflow: "bug", Actor: "test"})
	if err != nil {
		t.Fatalf("建卡: %v", err)
	}
	return card
}

// markersOf 数全流上的 message_consumed 事件并解出载荷键集。
func markersOf(t *testing.T, f *Facade) map[int64]struct {
	CardID  string
	Keys    map[string]json.RawMessage
	Payload map[string]any
} {
	t.Helper()
	events, err := f.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatalf("读事件流: %v", err)
	}
	out := map[int64]struct {
		CardID  string
		Keys    map[string]json.RawMessage
		Payload map[string]any
	}{}
	for _, ev := range events {
		if ev.Type != "message_consumed" {
			continue
		}
		var keys map[string]json.RawMessage
		if err := json.Unmarshal(ev.Payload, &keys); err != nil {
			t.Fatalf("解消费标记载荷键集: %v", err)
		}
		var p map[string]any
		if err := json.Unmarshal(ev.Payload, &p); err != nil {
			t.Fatalf("解消费标记载荷: %v", err)
		}
		out[ev.Seq] = struct {
			CardID  string
			Keys    map[string]json.RawMessage
			Payload map[string]any
		}{ev.CardID, keys, p}
	}
	return out
}

// TestRecordMessageConsumedExactlyOnceThroughSeam 恰好一次的三条账本面：
// 首次消费落恰一条标记（载荷金样键集=message_seq+consumer 两键、不多不少）、
// 同参重试 no-op、他人消费同一条各落一条互不顶替。
func TestRecordMessageConsumedExactlyOnceThroughSeam(t *testing.T) {
	f, st := newFixture(t)
	card := mustCardFor(t, st, "消费标记卡")
	seq, err := f.RecordRoomMessage(card.ID, proto.RoomMessage{Room: card.ID,
		Kind: proto.RoomMsgUser, Body: "留言"}, "user:sy")
	if err != nil {
		t.Fatalf("发消息: %v", err)
	}

	if err := f.RecordMessageConsumed(card.ID, seq, "user:sy"); err != nil {
		t.Fatalf("首次消费: %v", err)
	}
	got := markersOf(t, f)
	if len(got) != 1 {
		t.Fatalf("首次消费后应恰 1 条标记，实得 %d", len(got))
	}
	for _, m := range got {
		if m.CardID != card.ID {
			t.Fatalf("卡房间消费标记应挂卡流: card=%q", m.CardID)
		}
		if len(m.Keys) != 2 || m.Keys["message_seq"] == nil || m.Keys["consumer"] == nil {
			t.Fatalf("载荷键集漂移（冻结=message_seq+consumer 恰两键）: %v", m.Keys)
		}
		if ms, _ := m.Payload["message_seq"].(float64); int64(ms) != seq {
			t.Fatalf("message_seq 应为 %d，实得 %v", seq, m.Payload["message_seq"])
		}
		if c, _ := m.Payload["consumer"].(string); c != "user:sy" {
			t.Fatalf("consumer 应为 user:sy，实得 %v", m.Payload["consumer"])
		}
	}

	if err := f.RecordMessageConsumed(card.ID, seq, "user:sy"); err != nil {
		t.Fatalf("同参重试应幂等 nil: %v", err)
	}
	if n := len(markersOf(t, f)); n != 1 {
		t.Fatalf("同参重试后仍应恰 1 条标记，实得 %d", n)
	}

	if err := f.RecordMessageConsumed(card.ID, seq, "cli:b@h"); err != nil {
		t.Fatalf("他人消费同一消息应各落一条: %v", err)
	}
	if n := len(markersOf(t, f)); n != 2 {
		t.Fatalf("两个消费者共应 2 条标记，实得 %d", n)
	}
}

// TestRecordMessageConsumedGroupMarkerIsCardless 群级消息的消费标记同为
// 无卡事件（项目级），且同样恰好一次。
func TestRecordMessageConsumedGroupMarkerIsCardless(t *testing.T) {
	f, _ := newFixture(t)
	seq, err := f.RecordRoomMessage("", proto.RoomMessage{Room: "global",
		Kind: proto.RoomMsgUser, Body: "群消息"}, "user:sy")
	if err != nil {
		t.Fatalf("群级发送: %v", err)
	}
	if err := f.RecordMessageConsumed("", seq, "user:sy"); err != nil {
		t.Fatalf("群级消费: %v", err)
	}
	got := markersOf(t, f)
	if len(got) != 1 {
		t.Fatalf("群级消费标记应恰一条，实得 %d", len(got))
	}
	for _, m := range got {
		if m.CardID != "" {
			t.Fatalf("群级消费标记应为无卡事件: card=%q", m.CardID)
		}
	}
	if err := f.RecordMessageConsumed("", seq, "user:sy"); err != nil {
		t.Fatalf("群级重试: %v", err)
	}
	if n := len(markersOf(t, f)); n != 1 {
		t.Fatalf("群级重试后仍应恰一条，实得 %d", n)
	}
}

// TestRecordMessageConsumedUnknownSeqIsIdempotentNil 锁死岔口六方案甲的
// 选择：对不存在 seq 的消费是幂等 nil 而非报错。若未来契约加 ErrNoMessage，
// 本测试随该契约修订一起改。
func TestRecordMessageConsumedUnknownSeqIsIdempotentNil(t *testing.T) {
	f, _ := newFixture(t)
	if err := f.RecordMessageConsumed("", 987654321, "user:x"); err != nil {
		t.Fatalf("对不存在 seq 消费应幂等 nil（岔口六方案甲），got %v", err)
	}
}

// TestRecordMessageConsumedRejectsMissingCardAndEmptyConsumer 反面：卡房间
// 的标记要求卡存在（ErrNotFound）；空 consumer 是调用方 bug，直接报错。
func TestRecordMessageConsumedRejectsMissingCardAndEmptyConsumer(t *testing.T) {
	f, _ := newFixture(t)
	if err := f.RecordMessageConsumed("B99999", 1, "user:x"); !errors.Is(err, ledger.ErrNotFound) {
		t.Fatalf("不存在的卡必须报 ErrNotFound，got %v", err)
	}
	if err := f.RecordMessageConsumed("", 1, ""); err == nil {
		t.Fatal("空 consumer 必须报错：没有「谁」的消费标记无意义")
	}
}
```

**完整代码块——`internal/ledger/rooms.go` 的 RecordMessageConsumed（替换 :30-38 整段；文件头与其余部分不动；新增 import `"encoding/json"` 与 `"fmt"`，与既有 import 块合并）**

```go
// consumedMarker 是 message_consumed 事件的载荷 schema（契约 §4 金样键集：
// 恰 message_seq 与 consumer 两键）。actor 列另存 consumer 一份供查重与
// 读侧扫描先按列粗筛；载荷才是权威。collab 侧不得 import 本包，其本地
// 镜像 struct 的等值由两侧字面量测试钉住（TestRoomEventTypeLiteralMatchesLedger
// 同形）。刻意不放 proto：d_protocol 本轮零触碰。
type consumedMarker struct {
	MessageSeq int64  `json:"message_seq"`
	Consumer   string `json:"consumer"`
}

// RecordMessageConsumed 落消息消费标记：同一 mutate 事务内查重后写
// （ClearNeedsHumanFrom 同形，events.go），同 (msgSeq, consumer) 重复消费
// 是幂等 no-op。恰好一次由 mutate 的单写者串行化免费获得（store.go mutate
// 注释）——查重与写入之间的窗口被事务吃掉，这是拍板 5.4「权威在账本事件」
// 的机制兑现。
//
// 语义边界（都有测试钉着）：
//   - cardID 非空时必须指向存在的卡（ErrNotFound），标记挂同一张卡的流上；
//     cardID=""=群级消息的项目级标记。cardID 不参与查重键——seq 全局唯一，
//     传错 cardID 时幂等性优先于报错。
//   - 不校验 msgSeq 是否指向存在的 room_message：目标态「已消费」对不存在
//     消息天然成立（breakdown 岔口六方案甲），静默面由 Pending/Mentions 只列
//     未消费兜底。要改这个选择先回 contract。
//   - consumer 为空直接报错：「谁消费了哪条」没有「谁」无意义。
//
// 返回 nil 含三种情形：首次写入成功、本人重复消费跳过、他人已消费后再写
// 自己的标记（各消费者一条，互不顶替）——前两种全流恰一条本人标记，第三种
// 是新消费者的首次写入。
func (s *Store) RecordMessageConsumed(cardID string, msgSeq int64, consumer string) error {
	if consumer == "" {
		return fmt.Errorf("消费标记必须带 consumer")
	}
	return s.mutate(func(tx *sql.Tx, sink *eventSink) error {
		if cardID != "" {
			if _, err := getCardTx(s, tx, cardID); err != nil {
				return fmt.Errorf("消费标记: 卡 %s: %w", cardID, err)
			}
		}
		// 查重：先按 type+actor 列粗筛出本人的全部标记，再解载荷精确比对
		// message_seq。载荷匹配无法用跨 SQLite(TEXT)/PG(JSONB) 方言的 SQL
		// 表达，Go 解析是唯一可移植路径；单消费者的标记量以「他消费过的
		// 消息数」为界，全扫可承受。
		rows, err := tx.Query(s.q(`SELECT payload FROM card_events WHERE type = ? AND actor = ?`),
			EvMessageConsumed, consumer)
		if err != nil {
			return fmt.Errorf("查消费标记: %w", err)
		}
		defer rows.Close()
		for rows.Next() {
			var raw string
			if err := rows.Scan(&raw); err != nil {
				return fmt.Errorf("扫消费标记行: %w", err)
			}
			var marker consumedMarker
			if err := json.Unmarshal([]byte(raw), &marker); err != nil {
				continue // 非法载荷不该出现；跳过不中断幂等判定
			}
			if marker.MessageSeq == msgSeq && marker.Consumer == consumer {
				log().Info("消费标记幂等跳过：同参标记已存在",
					"msg_seq", msgSeq, "consumer", consumer, "card", cardID)
				return nil
			}
		}
		if err := rows.Err(); err != nil {
			return fmt.Errorf("遍历消费标记: %w", err)
		}
		seq, err := s.appendEvent(tx, sink, cardID, EvMessageConsumed, consumer,
			consumedMarker{MessageSeq: msgSeq, Consumer: consumer})
		if err != nil {
			return err
		}
		log().Info("消息消费标记已落账", "msg_seq", msgSeq, "consumer", consumer,
			"card", cardID, "event_seq", seq)
		return nil
	})
}
```

### T2.2 EnsureComment 补肉（欠账 #7 账本半）

**Interfaces**

- Produces：
  - `func (s *Store) EnsureComment(cardID, dedupeKey, body, actor string) (bool, error)`（签名与冻结 stub 一字不变；bool=true=本次真的写了）
  - 评论事件形状：type=`comment`（EvComment）、payload `{"kind":"普通","body":<body>,"refs":[],"dedupe_key":<dedupeKey>}`。
- Consumes：
  - `mutate` / `appendEvent` / `getCardTx` / `log()`（同 T2.1）
  - `const EvComment = "comment"`（types.go:58）

**行为语义**

1. 查重范围=**同卡内** EvComment 载荷 `dedupe_key` 字段相等；不同卡同键各写各的。
2. 空 dedupeKey（含纯空白）报错——没有键就没有幂等判据（守卫先例 MarkNeedsHuman reason）。body 不校验（评论是记录不是校验，AddComment 同立场）。
3. 不复用 addComment：它解析 `#卡号` 引用建 relates 边，系统生成的说明文本可能误建边；EnsureComment 直接 appendEvent，payload 形状与 addComment 输出兼容（多一个 dedupe_key 键，timeline 渲染忽略未知键）。
4. kind 固定 "普通"。C3 的 dedupeKey 建议拼法 `<reason前缀>:<卡号>:<节点名>`（如 `终态遗留裁决:B123:plan`）——建议非冻结，C3 自定。

**步骤（红绿模板：锁缝断言步骤，法定适用）**

1. （~2min）基线复核：`go test ./internal/ledger/ -run 'TestEnsureComment' -v` → no tests to run（stub 无测试）。
2. （~4min）写失败测试：`internal/ledger/events_test.go` 追加下方两个测试函数（import 块已有 encoding/json/errors/testing，无需增）。
3. （~1min）跑红：`go test ./internal/ledger/ -run 'TestEnsureComment' -v`。预期两条 FAIL（首调 wrote=false≠true / 空键未报错）。贴原文到台账。
4. （~4min）最小实现：按下方代码块替换 `internal/ledger/events.go` 的 EnsureComment（:228-238 整段，含 doc comment）。
5. （~1min）跑绿：同步骤 3 命令全 PASS；`go test ./internal/ledger/...` 无回归。贴原文。
6. （~1min）日志与注释检查：代码块内日志/注释已含；`grep -n "Println" internal/ledger/events.go` 零命中。
7. （~1min）提交：`git add internal/ledger/events.go internal/ledger/events_test.go && git commit`。

**完整代码块——追加到 `internal/ledger/events_test.go` 末尾**

```go
// TestEnsureCommentWritesOncePerKey 幂等留痕：同键二次调用不写、不同键各写
// 一条。入口是 Store.EnsureComment 本体——它不经任何声明缝（LedgerClient
// 方法集不含它、RunOnce 接线归 C3），内部锁理由见 b156.2.2-plan.md §7。
func TestEnsureCommentWritesOncePerKey(t *testing.T) {
	s := seedStore(t)
	card := mk(t, s, "遗留裁决卡")

	wrote, err := s.EnsureComment(card.ID, "终态遗留裁决:B123:plan", "存在 open 裁决，转等人", "step:plan")
	if err != nil || !wrote {
		t.Fatalf("首次写入应返回 true,nil，得到 (%v,%v)", wrote, err)
	}
	wrote, err = s.EnsureComment(card.ID, "终态遗留裁决:B123:plan", "存在 open 裁决，转等人", "step:plan")
	if err != nil || wrote {
		t.Fatalf("同键二次调用应返回 false,nil（不产生第二条），得到 (%v,%v)", wrote, err)
	}
	wrote, err = s.EnsureComment(card.ID, "终态遗留裁决:B456:review", "另一节点的说明", "step:review")
	if err != nil || !wrote {
		t.Fatalf("不同键应各写一条，得到 (%v,%v)", wrote, err)
	}

	events, err := s.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	var comments []Event
	for _, ev := range events {
		if ev.Type == EvComment {
			comments = append(comments, ev)
		}
	}
	if len(comments) != 2 {
		t.Fatalf("EvComment 应恰 2 条（同键去重生效），实得 %d", len(comments))
	}
	var first struct {
		Kind      string `json:"kind"`
		Body      string `json:"body"`
		DedupeKey string `json:"dedupe_key"`
		Actor     string
	}
	if err := json.Unmarshal(comments[0].Payload, &first); err != nil {
		t.Fatal(err)
	}
	first.Actor = comments[0].Actor
	if first.Kind != "普通" || first.Body != "存在 open 裁决，转等人" ||
		first.DedupeKey != "终态遗留裁决:B123:plan" || first.Actor != "step:plan" {
		t.Fatalf("说明评论载荷漂移: %+v", first)
	}
}

// TestEnsureCommentEmptyKeyRejected 空 dedupeKey 直接报错且零副作用——
// 没有键就没有幂等判据。
func TestEnsureCommentEmptyKeyRejected(t *testing.T) {
	s := seedStore(t)
	card := mk(t, s, "空键卡")
	if _, err := s.EnsureComment(card.ID, "  ", "正文", "step:plan"); err == nil {
		t.Fatal("空白 dedupeKey 必须报错")
	}
	events, err := s.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range events {
		if ev.Type == EvComment {
			t.Fatalf("拒绝后不应有任何评论事件: %+v", ev)
		}
	}
}
```

**完整代码块——`internal/ledger/events.go` 的 EnsureComment（替换 :228-238 整段；import 块已含 strings/json/fmt/sql，零增减）**

```go
// EnsureComment 幂等留痕：payload 带 dedupe_key，同卡内同键的说明评论已
// 存在则不写、返回 false；本次真的写了才返回 true。环节执行体「终态遗留
// 裁决」补解析（B156.2 契约 §3.7）用，保证重复驱动同一张卡不产生第二条
// 说明评论。查重与写入同 mutate 事务（ClearNeedsHumanFrom 同形）。
//
// 与 AddComment 的差异：不做 #卡号 引用建边——说明文本由系统生成，引用
// 解析反而可能误建 relates 边；payload 多 dedupe_key 一键，timeline 渲染
// 忽略未知键。dedupeKey 空（含纯空白）直接报错：没有键就没有幂等判据。
// 查重范围限同卡：不同卡的同键评论互不影响。
func (s *Store) EnsureComment(cardID, dedupeKey, body, actor string) (bool, error) {
	if strings.TrimSpace(dedupeKey) == "" {
		return false, fmt.Errorf("幂等评论必须带 dedupe_key")
	}
	wrote := false
	err := s.mutate(func(tx *sql.Tx, sink *eventSink) error {
		if _, err := getCardTx(s, tx, cardID); err != nil {
			return fmt.Errorf("幂等评论: 卡 %s: %w", cardID, err)
		}
		rows, err := tx.Query(s.q(`SELECT payload FROM card_events WHERE card_id = ? AND type = ?`),
			cardID, EvComment)
		if err != nil {
			return fmt.Errorf("查说明评论: %w", err)
		}
		defer rows.Close()
		for rows.Next() {
			var raw string
			if err := rows.Scan(&raw); err != nil {
				return fmt.Errorf("扫评论行: %w", err)
			}
			var payload struct {
				DedupeKey string `json:"dedupe_key"`
			}
			if err := json.Unmarshal([]byte(raw), &payload); err != nil {
				continue // 老评论无 dedupe_key 或载荷异常：不参与命中，继续
			}
			if payload.DedupeKey == dedupeKey {
				log().Info("说明评论幂等跳过：同键已存在", "card", cardID, "dedupe_key", dedupeKey)
				return nil
			}
		}
		if err := rows.Err(); err != nil {
			return fmt.Errorf("遍历评论: %w", err)
		}
		if _, err := s.appendEvent(tx, sink, cardID, EvComment, actor,
			map[string]any{"kind": "普通", "body": body, "refs": []string{}, "dedupe_key": dedupeKey}); err != nil {
			return err
		}
		wrote = true
		log().Info("说明评论已落账", "card", cardID, "dedupe_key", dedupeKey, "actor", actor)
		return nil
	})
	return wrote, err
}
```

### T2.3 回归、图闸与收尾

非红绿步骤（回归动作，无独立周期）。

1. （~3min）全量触及包回归：`go build ./... && go test ./internal/ledger/... ./internal/collab/... ./internal/ledgerstep/...`。预期全 ok（ledgerstep 未动但 EnsureComment 是其 C3 承重件，跑一次证明零破坏）。
2. （~2min）图闸：`go run . graph check --repo . --view cards-B156.2-charter-4`。预期 fails=0（warns 谱 ≈97 为基线噪音，允许浮动但不许新增 fail 类别）。
3. （~2min）占位残留扫描：`grep -rn "Ticket 0 空壳" internal/ledger/rooms.go internal/ledger/events.go` 零命中；`grep -n "_ = cardID\|_ = msgSeq\|_ = consumer\|_ = dedupeKey" internal/ledger/*.go` 零命中。
4. （~2min）台账收尾：把三步原始输出追加进 `docs/superpowers/ledgers/ledger-b156.2.2-plan.md`；`git add` 全部产出物（含台账）并 commit。
5. 收尾报文按 handoff 纪律输出 branch/commit/summary 与 handoff-verdict 块。

**测试范围声明（最小化合计）**：本卡只跑 `./internal/ledger/...`（主）、`./internal/ledger/api/`（T2.1 缝测）、`./internal/ledgerstep/... ./internal/collab/...`（T2.3 回归护栏）。全仓测试不属于本卡。

## 4. 缺陷族对抗审查结论（逐族）

| 族 | 设问 | 结论 |
| --- | --- | --- |
| 生命周期/状态机中断 | 查重读到的标记与写入之间有窗口吗？进程崩溃留半截吗？ | 无窗口：查重与写入同 mutate 事务（PG advisory lock / SQLite 单连接，store.go:152-166）；append-only 单条 INSERT 无半截态。孤儿资源：消费标记与普通评论一样只增，量级=消费次数，可承受（与 A.5 护栏同一立场，注释已言明） |
| 静默失败/误导报错 | 幂等 nil 会不会掩盖调用方 bug？ | 三处防线：空 consumer 报错；卡不存在报 ErrNotFound；幂等跳过有 Info 日志可观测。「无效 seq 静默 nil」是有意选择且有测试+注释锁死（岔口六方案甲） |
| 跨平台假设 | SQL 方言、时间、编码 | 查重不用方言 JSON 函数（SQL 粗筛+Go 解析）；不引入时间比较；载荷解析容忍 PG JSONB 的键序规范化（解析后比对值，不比对文本） |
| 假红/假绿 | 测试会不会恒绿？ | 探针实测三条断言今天对 stub 跑红（台账原文）；变异靶：去掉 RecordMessageConsumed 的查重循环→ExactlyOnce 重试段红；去掉 EnsureComment 查重→WritesOncePerKey 第二段红；去掉空键守卫→Rejected 红 |
| 门禁绕过 | 有没有绕过 appendEvent 的裸 INSERT？ | 零新 SQL 写 card_events——两条路径都走 appendEvent 唯一入口；查重是纯 SELECT |

序列化边界设问：本卡新增一处手写投影=consumedMarker 的 marshal/unmarshal（产生侧 appendEvent 内 json.Marshal，消费侧=查重自解+C5 将来解析）。断言覆盖：ExactlyOnce 用 `map[string]json.RawMessage` 断言键集恰二（区分「字段缺失」与「值为零」——message_seq 为 0 时键仍在、consumer 空串场景被空 consumer 守卫挡在生产外）；roundtrip 属性测试不另设——载荷仅两标量键、marshal 由 encoding/json 承担，金样键集断言已锁形状，属性测试在此无增量。C5 解析侧镜像的等值锁定移交 C5 计划（本计划 Interfaces 已给出精确键名）。

## 5. 五项检查

1. **缺陷族对抗**：§4 逐族有结论 ✓
2. **序列化边界**：§4 末段，投影点清单+断言形状 ✓
3. **上下文预算**：§2 四文件圈得出 ✓
4. **类型标注**：逻辑型子系统，无真机清单项；PG 方言的 JSONB 载荷读取路径由 EventsFromAsc 同款 string 扫描背书（机内 SQLite 全绿不外推 PG——PG 冒烟属 store_pg_test.go 门控范畴，本卡零 DDL/零新方言语法，风险面为零） ✓
5. **接缝覆盖（双向）**：
   - 测试→缝：api_test.go 四支测试入口全是 `(*Facade).RecordMessageConsumed` / `(*Facade).RecordRoomMessage` / `(*Facade).EventsFromAsc`——均属 spec 测试接缝清单 #2（LedgerClient 实现）✓
   - 缝→测试：清单八方法中本卡负责 RecordMessageConsumed 一条的账本行为锁定 ✓（其余七条的缝级断言分属 C1/C4/C5/C6 各卡，不在本卡）
   - 内部锁：events_test.go 两支，声明见 §7 ✓

## 6. 与 C1（B156.2.1 绑定席位与活性租约）的文件级交集

C1 正在并行跑 plan 轮。按 breakdown 划界：C1 动 binding.go、move.go、store.go、api/api.go＋binding_test.go(新)、move_test.go；本卡动 rooms.go、events.go、api/api_test.go(新)、events_test.go(追加)。

| 交界面 | 结论 | 处置 |
| --- | --- | --- |
| 生产 .go 文件 | **零交集**（四个文件互不相触；本卡零 schema 改动故不碰 store.go；本卡不改 Facade 故不碰 api/api.go——RecordMessageConsumed 直通已存在且编译通过） | 无需协调 |
| 测试 .go 文件 | 零交集（binding_test/move_test vs api_test/events_test） | 无需协调 |
| `codegraph/diffs/cards-B156.2-charter-4.json` | **唯一潜在交集**。两卡都是纯填肉零签名改动，默认**都不动视图**；仅当任一侧实现轮发现锚点漂移需要修正时才会碰 | 后落地者 rebase 到先落地者之上；视图 JSON 按 nodesModified/nodesAdded 数组取并集手工合（JSON 无行级语义冲突，union 即正确）；合后必跑 `graph check --view cards-B156.2-charter-4` fails=0 复核。**不许另起新视图文件**（协调者本轮补充第一条规定） |
| `go.mod/go.sum` | 零交集（两卡零新依赖） | — |
| rebase 顺序 | 两卡同为 DAG 根，无先后依赖。规则：后 merge 者负责 rebase 自己的两个 commit 到对方落地之后 | rebase 后必跑：`go build ./... && go test ./internal/ledger/... ./internal/ledger/api/` + 步骤 2 的图闸命令 |

## 7. 内部锁声明（占位符扫描节的一部分，逐条自我声明）

| 测试 | 入口 | 为什么从声明缝构造不出 |
| --- | --- | --- |
| TestEnsureCommentWritesOncePerKey | `Store.EnsureComment` | 缝#2 的 client.LedgerClient 方法集被契约 §3.4 冻结（扩方法先回 contract），其中**没有也不该有** EnsureComment；缝#4（环节执行体）的入口是 NodeStep.RunOnce，其补解析接线整条归 C3——本卡若从 RunOnce 构造断言就必须替 C3 把接入写了，越卡。契约 §3.7 明文「不经 collab 门面——它不是跨子系统缝」。故只能锁在 Store 本体 |
| TestEnsureCommentEmptyKeyRejected | 同上 | 同一理由；且错误守卫发生在进入 mutate 之前，任何更远的缝都观测不到它 |

退路同闸自查：T2.1/T2.2 步骤正文无条件退路（无「若意外先绿就改成直喂 X」条款）。

## 8. 占位符扫描自我声明

全文无 TBD／「适当的错误处理」／「同 Task N」；两处测试代码块均完整可编译且已探针实测对 stub 跑红（T2.1 经 internal/ledger/api/zz_probe_test.go、T2.2 经 internal/ledger/zz_probe2_test.go，原文在台账，两文件均已删除未入提交）。正当出口（「照抄既有 harness」例外）使用情况：**未使用**——两块测试代码均给出全文。C3 将来的 dedupeKey 拼法标注为「建议非冻结」，是移交说明不是占位符。

## 9. 跨卡审计输入（L3 重档：扇出前由协调者组织独立上下文审计；本节提供素材并自审待拍板）

**跨卡签名清单（本卡 Produces / 相关 Consumes，逐字符）**：

| 签名 | 产出方 | 消费方 | 一致性 |
| --- | --- | --- | --- |
| `RecordMessageConsumed(cardID string, msgSeq int64, consumer string) error` | 本卡（Store/Facade 双层同名，api.go:76-78 直通已存在） | C5 经 client.LedgerClient（接口方法集契约 §3.4 冻结） | 待审计：与 C5 plan 的调用实参序核对 |
| payload 键 `message_seq`(int64)/`consumer`(string)；actor=consumer；marker card_id=消息所属流 | 本卡 | C5 的 Pending/Mentions 扫描解析 | 待审计：C5 plan 必须含本地镜像 struct+字面量等值测试 |
| `EnsureComment(cardID, dedupeKey, body, actor string) (bool, error)` | 本卡 | C3 经 `St *ledger.Store` 直调 | 待审计：C3 plan 的调用与 bool 语义（true=写了）方向核对 |

**spec 故事归属（与本卡相关的故事链路）**：故事 6（跨卡沟通 @提及）与「深夜清账」故事的消费可靠性依赖本卡恰好一次语义（账本半）+C5（门面半）；「协调者死亡窗口」恢复路径依赖本卡 EnsureComment+C3（RunOnce 接入）。本卡不单独闭环任何用户故事，属承重件。

## 10. 自审三查

1. **spec 覆盖**：本卡承接的契约条目（§3.3 Consume 注释账本半、§3.7、§4 三+一条）逐条指到 T2.1/T2.2 验收，无遗漏；不属本卡的半边（门面半、RunOnce 半）已显式移交 C5/C3 ✓
2. **占位符扫描**：§8 ✓
3. **跨 task 签名一致性**：§9 清单；与冻结 stub 签名逐字符一致（rooms.go:33、events.go:232 亲读） ✓

## 11. 提交切分

- commit 1（T2.1）：`feat(ledger): RecordMessageConsumed 消费标记恰好一次补肉（B156.2.2 账本半）`
- commit 2（T2.2）：`feat(ledger): EnsureComment 幂等留痕补肉（B156.2.2 欠账#7 账本半）`
- commit 3（T2.3）：`docs(b156.2.2): plan 节点产出与台账收尾`（若 plan 文档与台账选择先行单独提交，则此条并入 commit 1 前置；顺序不冻结，三件产出齐全即可）

## 12. 明确不做（防越界）

- 不写 collab.Consume/Pending/Mentions（C5）；不碰 service.go 任何一个 stub。
- 不写 NodeStep 补解析（C3）；不给 ledgerstep 加任何调用。
- 不改 proto、types.go 词表、schema、target.json、best.json、金样本 fixture。
- 不 absorb 视图（finish 阶段动作）；不修裸 graph check 的 4 处 dead。
