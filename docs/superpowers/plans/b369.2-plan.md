# B369.2（S2 连接核兑换、切机清罐与离线补配）——实现计划

- **读者假设**：执行者对代码库零上下文、品味存疑。本计划自包含：每个任务给精确文件路径、完整代码、判据命令与预期；不猜、不占位。
- **卡**：B369.2（B369 的第 2 张子卡，对应 breakdown 的 S2；L3 重档）。
- **有效基线分支**：`cards/B369-charter-4`（本卡合并目标）。本计划在分支 `cards/B369.2-charter` 工作。
- **上游冻结物**（只消费，不重开）：
  - spec：`docs/superpowers/specs/2026-09-13-mobile-app-design.md`（头部状态 **已批准**，逐字核对通过）。
  - contract：`docs/superpowers/specs/b369-contract.md`（**已冻结** @ `6f10ba7c`）。本计划主要消费 §3.2/§3.3/§3.4、§4.2（条 12–21）、§4.5（条 30–32）、§6 命中 2、§8.2/§8.8、§9 附区条 2。
  - breakdown：`docs/superpowers/specs/b369-breakdown.md`（**已拍板 P1–P6 全案 A**）。本计划消费 §3.3（S2 四段式与验收）。
- **本节点（plan）台账**：`docs/superpowers/ledgers/2026-09-19-b369.2-plan-ledger.md`（已随本提交落盘）。
- **实现节点台账（执行者边干边落）**：`docs/superpowers/ledgers/2026-09-19-b369.2-impl-ledger.md`。

---

## 0. 基线事实（亲跑，节点出稿轮）

### 0.1 工具链与基线读数

| 命令 | 读数 |
| --- | --- |
| `go version` | `go version go1.26.1 linux/amd64` |
| `go build ./...` | 退出码 0 |
| `go test ./internal/mobilecore/... ./internal/proto/... -count=1` | `ok .../mobilecore 0.007s`；`ok .../proto 0.013s` |
| `go test ./internal/mobilecore/... -race -count=1` | `ok .../mobilecore 1.020s` |
| `go vet ./internal/mobilecore/...` | 退出码 0 |
| `go test ./internal/relay/... -count=1` | `ok .../relay 0.012s` |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | `ok .../cmd 0.071s` |
| `codegraph validate --view cards-B369-charter` | EXIT=0；`"issues": null`、`"edgeIssues": null` |
| `codegraph check --view cards-B369-charter` | EXIT=0；`"fails": []` |
| `go test ./... -count=1` | 退出码 1；失败集合**恰 1 支**（见 §0.2），其余包全 ok |

### 0.2 基线红与跨契约冲突（协调者已裁决）

`internal/client/execution_test.go#TestProductionHTTPClientCallersAreGatewayOnly` 在**本卡基线上已红**：

```text
--- FAIL: TestProductionHTTPClientCallersAreGatewayOnly (0.01s)
    execution_test.go:158: 生产代码不得再取 HTTPClient 拼请求（网关 forward 除外）: internal/mobilecore/core.go, internal/mobilecore/proxy.go
FAIL
FAIL	github.com/Xsxdot/handoff/internal/client	9.507s
FAIL
```

原因：B233.3 冻结条 25（`docs/superpowers/specs/b233.3-contract.md:170`）规定生产源码 `.HTTPClient()` 只许出现在 `internal/agentd/forward.go` 与 `forward_ws.go`；而 B369 Ticket 0 的 `internal/mobilecore/core.go:155`（`cl.HTTPClient().Do`）与 `internal/mobilecore/proxy.go:38`（`Transport: cl.HTTPClient().Transport`）取用了它，且 B369 contract §3.3 明文冻结 `proxy.go` 必须这么写（`b369-contract.md:189`）。两张冻结卡正面冲突，本卡无权自裁。

**协调者裁决（工单 `9f6bd37f-1ab6-4cf2-a329-5b6ce13cb9d4`，2026-09-19）**：

1. 本卡**不改**那条冻结守卫、**不停工**；把冲突如实写进本计划「基线事实」节（已记卡待用户裁决是否把 mobilecore 纳入允许清单）。
2. 本卡新增文件不得再出现**除 `.HTTPClient()` 之外**的新违规，也不得改那条守卫测试。
3. 若实现需要 transport，把 `Core` 已持有的 client 字段按现状 `core.go` 的写法透传，不新造取用点。

**据此的设计决定**：程序化兑换**不经 `client.HTTPClient()`**——`Core.Pair` 已为每台在线机起回环反代，兑换改为对**回环源**发 `GET /console?ticket=...`（普通 `http.Client` + `CheckRedirect=ErrUseLastResponse` + `Proxy:nil`），反代照旧用 `cl.HTTPClient().Transport` 选路。因此新文件 `session.go` **零 `.HTTPClient()` 调用**，守卫报告的违规文件集合保持 `{core.go, proxy.go}` 不变（不新增违规文件）。领票走既有类型化方法 `Client.IssueAuthTicket`，也不触 `HTTPClient`。

### 0.3 判据先在基线跑（本卡验收命令）

| 命令 | 基线预期 |
| --- | --- |
| `go build ./...` | 退出码 0 |
| `go test ./internal/mobilecore/... -count=1` | ok（Ticket 0 竖切与部分 bundle 已绿） |
| `go test ./internal/mobilecore/... -race -count=1` | ok |
| `go vet ./internal/mobilecore/...` | 退出码 0 |
| `go test ./internal/relay/... -count=1` | ok |
| `go test ./internal/client/... -count=1` | **FAIL，且唯一失败=§0.2 守卫**；本卡完成时违规文件集合必须仍恰为 `{core.go, proxy.go}`（不得出现 `session.go`） |
| `go test ./cmd/ -run TestRepoContractGate -count=1` | ok |
| `codegraph validate --view cards-B369-charter` | issues=null（本卡不改图，作回归） |
| `codegraph check --view cards-B369-charter` | fails=[] |

**关键基线确证（读码）**：`internal/mobilecore` 现有导出方法仅 `Pair`/`Origin`/`MachineNames`/`Close`（`grep -rn "func (c \*Core)" internal/mobilecore/core.go` 另有私有 `replaceMachine`/`startLoopback`），`Activate`/`Session`/`Retry` 均不存在（`codegraph sym 'Core.Activate' --view cards-B369-charter` 未命中）。因此本卡「程序化 ticket→cookie」「切机清罐」「离线补配」三条行为在基线**从声明缝调用得不到预期结果**，Task 1 必须是点亮兑换的最薄可跑路径（见 §3 的「最薄路径条」）。

### 0.4 计划代码的机械验证（出稿轮在 `$TMPDIR` 的仓库副本亲跑）

计划把「全部任务落地后的终局代码」与各任务测试都写全了，因此下列事实可**在出稿轮机械验证**（不是「读着像对」）：

| 验证 | 命令 | 实测结果 |
| --- | --- | --- |
| 终局可编译 | `go build ./...` | 退出码 0 |
| 终局测试全绿 | `go test ./internal/mobilecore/... -count=1` | `ok`（15 支：Ticket 0 两支 + 本卡 13 支） |
| 终局竞态/静态 | `go test ./internal/mobilecore/... -race -count=1` / `go vet` / `gofmt -l` | ok / 退出码 0 / 零输出 |
| Task 1 红态 | 基线 + 仅 `session_test.go`（T1 块） | `build failed`：`undefined: sessionCookieName`、`core.Activate undefined` 等 |
| Task 1 中间态绿 | 基线 + T1 实现 + T1 测试 | `ok` |
| Task 2 红态 | Task 1 态 + T2 测试 | `同机重复 Activate 必须命中缓存（不重复领票），tickets=2` FAIL |
| Task 2 的同机测试首绿 | Task 1 态 + T2 测试（`-run TestSessionJarSwitchesMachine`） | `ok`（见 §5 步骤 2 的「首绿免除」声明） |
| 变异 1（跟随 302） | T1 态改 `CheckRedirect` 返回 nil | FAIL：`Activate: 兑换 ticket 期望 302，得到 200` |
| 变异 2（缓存不看机器） | 终局改 `if c.cookieValidLocked()` | FAIL：`切机后必须重兑换…A="sess-…alpha-1" B="sess-…alpha-1"` |
| 变异 3（去熵闸） | 终局注释 `CheckTokenEntropy` | FAIL：`短 token 必须被熵闸拒绝` |
| 变异 4（反代注入 Authorization） | 终局 `proxy.go` Director 加注入行 | FAIL：`兑换请求不得带 Authorization，上游看到 "Bearer INJECTED"` |
| 裁决合规 | 终局 `grep -rn '\.HTTPClient()' internal/mobilecore` | 命中仍恰 `core.go` 与 `proxy.go`（`session.go` 零命中，违规集合不新增） |
| 冻结守卫不新增违规 | `go test ./internal/client/ -run TestProductionHTTPClientCallersAreGatewayOnly -count=1` | 仍红且原文恰 `internal/mobilecore/core.go, internal/mobilecore/proxy.go`（与基线同集合） |
| 存量隧道不破 | `go test ./internal/relay/... -count=1` | `ok` |

副本验证不替代实现节点的真实跑测（执行者须在自己的工作树复跑并把原文记实现台账）；它只证明计划给的代码与判据自洽、可跑。副本在 `$TMPDIR`，不入库、不提交。

---

## 1. 本卡干什么（一段话）

Ticket 0 的 `Core` 已能配对、探测、起每机独立回环反代（`internal/mobilecore/core.go`）。本卡补齐 spec 承诺的三条可观测行为：

1. **程序化 ticket→cookie 兑换**（contract §3.4 / breakdown §3.3 ②）：Go 核用 token 副本经类型化方法领一次性 ticket → 对**本机回环源**请求 `/console?ticket=...`（302 导航流，**不跟随**）→ 取 `Set-Cookie` 里的 `handoff_session`，供绑定面注入 webview cookie jar（P4=A：兑换在 Go 核，壳只做薄桥接）。
2. **每机独立端口的单槽会话罐**（spec 多机会话域 / contract 条 31）：切机即清罐并重兑换，任何时刻一罐只装一机的 cookie（cookie 不按端口隔离，RFC 6265）。
3. **离线补配**（contract §8.8 / spec「上线后补配」）：`Core.Pair` 对离线机在册标 offline，新增重试入口把可达的离线机翻回 online。

同时按契约条 11/13/14/19/20/21 把 token 熵闸、重复扫码幂等、资源不泄漏、`Close` 幂等与关闭后拒收锁死。**回环反代不注入凭据**是承重安全属性（contract §6 命中 2），本卡不改反代，且任何兑换实现都不得在反代层补 Authorization。

## 1.1 有界文件集（本卡只许碰这些文件）

| 文件 | 角色 |
| --- | --- |
| `internal/mobilecore/core.go` | 改：`machine` 结构（登记/在线态）、`Pair`→`registerLocked`、`Retry`、`Origin`/`MachineNames` 离线语义、`Close` 清罐、`Activate`/`Session`/`ActiveMachine` |
| `internal/mobilecore/session.go` | 新：`SessionCookie` DTO、`exchangeTicket`、`toSessionCookie`、cookie 名与设备名常量 |
| `internal/mobilecore/session_test.go` | 新：fake agentd 夹具 + 全部兑换/切机/补配/资源安全测试 |
| `docs/superpowers/ledgers/2026-09-19-b369.2-impl-ledger.md` | 新：实现台账（边干边落） |

**只读，不改**：`internal/mobilecore/proxy.go`（除非发现缺陷，缺陷另行提问）、`internal/proto/**`、`internal/client/**`、`internal/relay/**`、`internal/agentd/**`、`cmd/**`、`mobile/bind/**`（归 S4）、`codegraph/**`（见 §7 recon 说明，本卡不手改冻结 diff）、那条守卫测试 `internal/client/execution_test.go`。

**禁触**：任何 `.HTTPClient()` 新增取用点（协调者裁决 2）。

## 1.2 图覆盖债

本节点亲跑 `codegraph sym`：

- `Core.Pair`/`Core`/`EncodePairBundle` 在 `--view cards-B369-charter` 命中（Ticket 0 已入视图）；基线无 `--view` 未命中。
- `Core.Activate`、`Retry`、`Session`、`ActiveMachine`、`SessionCookie`、`exchangeTicket`、`sessionCookieName` **均未命中**（本卡首建）——记图覆盖债，实现节点收口时按下表把符号清单写入实现台账，**交 integrate/recon 补 `codegraph/diffs`**（沿用 B358.1 plan Task 4 步 4 的先例：不手改冻结 diff）。

新增符号清单（交 recon）：`mobilecore.SessionCookie`（model，`k_mobilecore_model`）、`mobilecore.Core.Activate` / `.Session` / `.ActiveMachine` / `.Retry`（func，`k_mobilecore_Core`）、`mobilecore.exchangeTicket` / `.toSessionCookie` / `.sameSiteName`（func，`k_mobilecore_fn`）；边：`Core.Activate`→`exchangeTicket`、`exchangeTicket`→`Client.IssueAuthTicket`、`exchangeTicket`→`toSessionCookie`。

---

## 2. 终局形状（全部任务落地后的样子）

```go
// internal/mobilecore/session.go（新）
type SessionCookie struct {
    Name, Value, Path string
    HttpOnly, Secure  bool
    SameSite          string
    MaxAge            int
}
const sessionCookieName = "handoff_session"
const mobileDeviceName  = "handoff-mobile"
func exchangeTicket(ctx context.Context, machine, origin string, cl *client.Client, log *slog.Logger) (SessionCookie, error)

// internal/mobilecore/core.go（改）
var errCoreClosed = errors.New("连接核已关闭")
type Core struct {
    mu              sync.Mutex
    exchMu          sync.Mutex // 串行化 Activate 的领票+兑换
    dial            DialFunc
    log             *slog.Logger
    machines        map[string]*machine
    closed          bool
    active          string        // 单槽会话罐：当前机器
    cookie          SessionCookie
    cookieExpiresAt time.Time
}
type machine struct {
    name    string
    reg     proto.PairMachine // 原始登记，补配重试用
    relay   *proto.PairRelay  // 补配重试用
    online  bool
    origin  string
    cl      *client.Client
    cleanup func()
    srv     *http.Server
    ln      net.Listener
}
func (c *Core) Pair(ctx, bundleJSON) (PairResult, error)          // 登记全部机器（含离线）
func (c *Core) registerLocked(ctx, b, m) PairedMachine            // 私有
func (c *Core) Retry(ctx, machine string) (PairedMachine, error)  // 离线补配
func (c *Core) Origin(machine string) (string, error)             // 离线返回错误
func (c *Core) MachineNames() []string                            // 含离线
func (c *Core) Close() error                                      // 幂等、清罐
func (c *Core) Activate(ctx, machine string) (SessionCookie, error) // 切机清罐 + 兑换/缓存
func (c *Core) Session() (SessionCookie, error)                   // 当前活动 cookie
func (c *Core) ActiveMachine() string
```

**执行顺序线性**：`Task 0 → 1 → 2 → 3 → 4 → 5`。全部任务动 `internal/mobilecore` 同一批文件，平行无益且冲突（同缝串行）。

**中间态声明**（同分支先后任务，不得中途合并/派发部分成果）：

- Task 1 后、Task 2 前：`Activate` 每次调用都重新兑换，无同机缓存。
- Task 1 后、Task 3 前：离线的机器**不登记**（`PairResult` 仍标 offline，但 `MachineNames` 不含离线机）。
- Task 3 后、Task 4 前：无并发保护测试；`Close` 已幂等但无「关闭后拒收」断言。

以上窗口内 `internal/mobilecore` 包测试全绿。

---

## 3. Task 0：基线复跑（不写码）

### 文件范围

无代码改动；台账追加。

### Interfaces

无（纯核验任务）。

### 步骤

1. 复跑 §0.3 全部命令，读数与表一致；不一致 → 停下报告协调者，不自行修。
2. 特别复核：`go test ./internal/client/... -count=1` **必红**，且原文恰为 `internal/mobilecore/core.go, internal/mobilecore/proxy.go`（§0.2）。这是**已知基线红**，Task 5 的验收不是「它变绿」，而是「违规集合不新增」。
3. 台账追加：基线读数原样 + §0.2 冲突行。

### Task 0 验收

- 命令读数与 §0.3 一致；`internal/client` 守卫红原文在台账。

---

## 4. Task 1：程序化 ticket→cookie 兑换（最薄路径）

> **最薄路径条**：本卡要锁的「兑换」行为在基线**不存在**（`Core.Activate` 未命中、`SessionCookie` 未命中）——测试写下去必红。本 task 是点亮该行为的最薄可跑路径：`session.go` 的兑换原语 + `Core` 的单机 `Activate`，测试一次真实调用穿 `Core.Pair`→relay E2E→回环反代→fake agentd→`Set-Cookie`→`Activate` 返回 cookie。

### 文件范围

`internal/mobilecore/session.go`（新建）、`internal/mobilecore/core.go`（改）、`internal/mobilecore/session_test.go`（新建）。

### Interfaces

Consumes（既有签名，逐字符保持）：

```go
// internal/client/client.go
func New(addr, token string) *Client
func NewRelay(d *relay.Dialer, token string) *Client
func (c *Client) BaseURL() string
func (c *Client) HTTPClient() *http.Client
func (c *Client) IssueAuthTicket(ctx context.Context, deviceName string) (*proto.AuthTicketResp, error) // :1267
// internal/relay/dialer.go
func NewDialer(relayURL, credential, node, token, account string, log *slog.Logger) *Dialer
func CheckTokenEntropy(token string) error // entropy.go:8
// internal/proto/pairing.go
func DecodePairBundle(raw string) (PairBundle, error)
func EncodePairBundle(b PairBundle) (string, error)
// 既有测试夹具（internal/mobilecore/core_test.go，同包可直接用）
func startFakeRelay(t *testing.T, token, account, node string, upstream http.Handler) (string, func())
```

Produces（本任务新增/改变）：

```go
// internal/mobilecore/session.go
type SessionCookie struct {
    Name     string `json:"name"`
    Value    string `json:"value"`
    Path     string `json:"path"`
    HttpOnly bool   `json:"http_only"`
    Secure   bool   `json:"secure"`
    SameSite string `json:"same_site"`
    MaxAge   int    `json:"max_age"`
}
const sessionCookieName = "handoff_session"
const mobileDeviceName  = "handoff-mobile"
func exchangeTicket(ctx context.Context, machine, origin string, cl *client.Client, log *slog.Logger) (SessionCookie, error)
func toSessionCookie(ck *http.Cookie) SessionCookie
func sameSiteName(m http.SameSite) string

// internal/mobilecore/core.go
var errCoreClosed = errors.New("连接核已关闭")
func (c *Core) Activate(ctx context.Context, machine string) (SessionCookie, error) // 本 task：无缓存，每次兑换
func (c *Core) Session() (SessionCookie, error)
func (c *Core) ActiveMachine() string
func (c *Core) registerLocked(ctx context.Context, b proto.PairBundle, m proto.PairMachine) PairedMachine
// Pair 改为循环调用 registerLocked；machine 结构加 reg/relay/online 字段
```

### 步骤 1（判据先在基线跑）

跑 `go test ./internal/mobilecore/... -count=1` 与 `-race`，确认基线 ok（§0.3）。跑 `codegraph sym 'Core.Activate' --view cards-B369-charter` 确认未命中（红的前提）。

### 步骤 2（红）

新建 `internal/mobilecore/session_test.go`，写入下面的夹具与四支测试。此刻它们**编译失败**（`SessionCookie`/`Activate`/`Session`/`ActiveMachine` 不存在），红即判据成立，原样输出记台账。

```go
// B369.2（S2）连接核兑换、切机清罐与离线补配的机内夹具与测试。
//
// 边界：fake agentd 复刻 agentd 的两条路由语义——POST /api/auth/tickets 由
// token 签发一次性 ticket；GET /console?ticket= 原子消费后 Set-Cookie + 302。
// /api/status 在无 handoff_session cookie 时回 401（复刻回环门禁：无 cookie
// 过不了闸）。所有上游经真 relay 线格式（startFakeRelay）到达。
package mobilecore

import (
	"context"
	"encoding/json"
	"io"
	"log/slog"
	"net/http"
	"net/url"
	"strconv"
	"strings"
	"sync"
	"testing"
	"time"

	"github.com/Xsxdot/handoff/internal/proto"
)

// pairToken 是 fake relay 夹具用的合法高熵 token（64 hex，过 CheckTokenEntropy）。
const pairToken = "00112233445566778899aabbccddeeff00112233445566778899aabbccddeeff"

// fakeAgentd 是行为等价的对端 agentd（只覆盖本卡所需路由）。
//
// 兑换链形状：POST /api/auth/tickets 由 Authorization 里的 token 签发 ticket，
// ticket 文本编码 device_name（= handoff-mobile-<机器名>），故不同机器换出的
// cookie 值不同——切机清罐断言据此区分机器。GET /console 回 Set-Cookie + 302。
type fakeAgentd struct {
	mu          sync.Mutex
	tickets     int
	afterLogin  int
	consoleAuth []string // /console 请求上的 Authorization（应为空）
	consoleCode int      // 0 表示 302
	omitCookie  bool
	extraCookie string
}

func (f *fakeAgentd) ServeHTTP(w http.ResponseWriter, r *http.Request) {
	switch r.URL.Path {
	case "/api/status":
		if _, err := r.Cookie(sessionCookieName); err != nil {
			w.WriteHeader(http.StatusUnauthorized)
			_, _ = io.WriteString(w, "no session")
			return
		}
		w.WriteHeader(http.StatusOK)
		_, _ = io.WriteString(w, "ok")
	case "/api/auth/tickets":
		var body struct {
			DeviceName string `json:"device_name"`
		}
		_ = json.NewDecoder(r.Body).Decode(&body)
		f.mu.Lock()
		f.tickets++
		n := f.tickets
		f.mu.Unlock()
		w.Header().Set("Content-Type", "application/json")
		_ = json.NewEncoder(w).Encode(proto.AuthTicketResp{
			URL:       "http://" + r.Host + "/console?ticket=" + url.QueryEscape(body.DeviceName) + "-" + strconv.Itoa(n),
			ExpiresAt: time.Now().Add(time.Minute),
		})
	case "/console":
		f.mu.Lock()
		f.consoleAuth = append(f.consoleAuth, r.Header.Get("Authorization"))
		code, omit, extra := f.consoleCode, f.omitCookie, f.extraCookie
		f.mu.Unlock()
		if code == 0 {
			code = http.StatusFound
		}
		if extra != "" {
			w.Header().Add("Set-Cookie", extra)
		}
		if !omit {
			http.SetCookie(w, &http.Cookie{
				Name:     sessionCookieName,
				Value:    "sess-" + r.URL.Query().Get("ticket"),
				Path:     "/",
				HttpOnly: true,
				SameSite: http.SameSiteLaxMode,
			})
		}
		w.Header().Set("Location", "/after-login")
		w.WriteHeader(code)
	case "/after-login":
		f.mu.Lock()
		f.afterLogin++
		f.mu.Unlock()
		w.WriteHeader(http.StatusOK)
	default:
		http.NotFound(w, r)
	}
}

// relayBundle 造一份单机 relay 形态 bundle（token 走 fake relay 的 E2E）。
func relayBundle(relayURL, name string) proto.PairBundle {
	return proto.PairBundle{
		Version: proto.PairVersion,
		Relay:   &proto.PairRelay{URL: relayURL, Credential: "pipecred"},
		Machines: []proto.PairMachine{
			{Name: name, Token: pairToken, Node: "devbox"},
		},
	}
}

// localClient 是不走任何代理的 HTTP 客户端（回环源与 fake 上游都用它）。
func localClient() *http.Client {
	return &http.Client{Transport: &http.Transport{Proxy: nil}}
}

// TestExchangeTicketVerticalSlice 是最薄路径竖切：一份 bundle 走完编码→配对
// （relay E2E + 探测 + 回环反代）→程序化兑换（领票 + /console Set-Cookie）→
// 拿到会话 cookie；并锁两条承重断言：兑换不跟随 302、兑换请求不带 Authorization、
// 回环门禁（无 cookie 被拒、带 cookie 可达）。
func TestExchangeTicketVerticalSlice(t *testing.T) {
	fa := &fakeAgentd{}
	relayURL, cleanup := startFakeRelay(t, pairToken, "acc1", "devbox", fa)
	defer cleanup()

	payload, err := proto.EncodePairBundle(relayBundle(relayURL, "devbox"))
	if err != nil {
		t.Fatal(err)
	}
	core := New(nil, slog.Default())
	defer core.Close()
	ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel()

	res, err := core.Pair(ctx, payload)
	if err != nil {
		t.Fatalf("配对失败: %v", err)
	}
	if len(res.Machines) != 1 || !res.Machines[0].Online || res.Machines[0].Origin == "" {
		t.Fatalf("应一台在线机带 origin: %+v", res.Machines)
	}
	origin := res.Machines[0].Origin

	ck, err := core.Activate(ctx, "devbox")
	if err != nil {
		t.Fatalf("Activate: %v", err)
	}
	if ck.Name != sessionCookieName || !strings.Contains(ck.Value, "devbox") {
		t.Fatalf("cookie 形状不对: %+v", ck)
	}
	if ck.Path != "/" || !ck.HttpOnly || ck.Secure {
		t.Fatalf("cookie 属性不对（明文 loopback 必须非 Secure）: %+v", ck)
	}
	if ck.SameSite != "Lax" {
		t.Fatalf("SameSite 应 Lax: %+v", ck)
	}
	if got := core.ActiveMachine(); got != "devbox" {
		t.Fatalf("ActiveMachine=%q，期望 devbox", got)
	}
	if fa.afterLogin != 0 {
		t.Fatalf("兑换不得跟随 302，/after-login 命中 %d 次", fa.afterLogin)
	}
	for _, a := range fa.consoleAuth {
		if a != "" {
			t.Fatalf("兑换请求不得带 Authorization，上游看到 %q", a)
		}
	}

	// 回环门禁：无 cookie 的同机请求被 agentd 拒（401）。
	noCookie, err := localClient().Get(origin + "/api/status")
	if err != nil {
		t.Fatalf("回环请求: %v", err)
	}
	noCookie.Body.Close()
	if noCookie.StatusCode != http.StatusUnauthorized {
		t.Fatalf("无 cookie 应被回环门禁拒，得到 %d", noCookie.StatusCode)
	}
	// 带上兑换来的 cookie 即可达（webview 的正常形态）。
	req, _ := http.NewRequest(http.MethodGet, origin+"/api/status", nil)
	req.AddCookie(&http.Cookie{Name: ck.Name, Value: ck.Value})
	withCookie, err := localClient().Do(req)
	if err != nil {
		t.Fatalf("带 cookie 请求: %v", err)
	}
	defer withCookie.Body.Close()
	if withCookie.StatusCode != http.StatusOK {
		t.Fatalf("带会话 cookie 应可达，得到 %d", withCookie.StatusCode)
	}
}

// TestExchangeTicketFailureModes 锁「失败不得静默成功」：非 302、缺 Set-Cookie、
// cookie 空值都必须返回错误，且不得留下活动会话（字段缺失 vs 值为零的区分）。
func TestExchangeTicketFailureModes(t *testing.T) {
	cases := []struct {
		name string
		mut  func(*fakeAgentd)
	}{
		{"200 无 Set-Cookie", func(f *fakeAgentd) { f.consoleCode = http.StatusOK; f.omitCookie = true }},
		{"302 无 Set-Cookie", func(f *fakeAgentd) { f.omitCookie = true }},
		{"200 带 cookie 非 302", func(f *fakeAgentd) { f.consoleCode = http.StatusOK }},
		{"cookie 空值", func(f *fakeAgentd) { f.extraCookie = "handoff_session=; Path=/"; f.omitCookie = true }},
	}
	for _, tc := range cases {
		t.Run(tc.name, func(t *testing.T) {
			fa := &fakeAgentd{}
			tc.mut(fa)
			relayURL, cleanup := startFakeRelay(t, pairToken, "acc1", "devbox", fa)
			defer cleanup()
			payload, err := proto.EncodePairBundle(relayBundle(relayURL, "devbox"))
			if err != nil {
				t.Fatal(err)
			}
			core := New(nil, slog.Default())
			defer core.Close()
			ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
			defer cancel()
			if _, err := core.Pair(ctx, payload); err != nil {
				t.Fatal(err)
			}
			if _, err := core.Activate(ctx, "devbox"); err == nil {
				t.Fatal("兑换失败必须返回错误，不得静默成功")
			}
			if core.ActiveMachine() != "" {
				t.Fatalf("兑换失败后不得留下活动会话: %q", core.ActiveMachine())
			}
			if _, err := core.Session(); err == nil {
				t.Fatal("无活动会话时 Session 必须报错")
			}
		})
	}
}

// TestExchangeTicketCookieSelection 锁 Set-Cookie 解析的手写投影边界：多个
// Set-Cookie 头、name 大小写（大写是诱饵）、属性逐项（Path/HttpOnly/Secure/SameSite）。
func TestExchangeTicketCookieSelection(t *testing.T) {
	fa := &fakeAgentd{extraCookie: "HANDOFF_SESSION=decoy; Path=/; Secure"}
	relayURL, cleanup := startFakeRelay(t, pairToken, "acc1", "devbox", fa)
	defer cleanup()
	payload, err := proto.EncodePairBundle(relayBundle(relayURL, "devbox"))
	if err != nil {
		t.Fatal(err)
	}
	core := New(nil, slog.Default())
	defer core.Close()
	ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel()
	if _, err := core.Pair(ctx, payload); err != nil {
		t.Fatal(err)
	}
	ck, err := core.Activate(ctx, "devbox")
	if err != nil {
		t.Fatalf("Activate: %v", err)
	}
	if !strings.Contains(ck.Value, "devbox") {
		t.Fatalf("必须选中 handoff_session（大写名是诱饵）: %+v", ck)
	}
	if ck.Path != "/" || !ck.HttpOnly || ck.Secure || ck.SameSite != "Lax" {
		t.Fatalf("cookie 属性投影失准: %+v", ck)
	}
	// 缺 name 也不行：只回大写名等价于没有会话。
	fa2 := &fakeAgentd{extraCookie: "HANDOFF_SESSION=decoy; Path=/", omitCookie: true}
	relayURL2, cleanup2 := startFakeRelay(t, pairToken, "acc1", "devbox", fa2)
	defer cleanup2()
	payload2, _ := proto.EncodePairBundle(relayBundle(relayURL2, "devbox"))
	core2 := New(nil, slog.Default())
	defer core2.Close()
	ctx2, cancel2 := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel2()
	if _, err := core2.Pair(ctx2, payload2); err != nil {
		t.Fatal(err)
	}
	if _, err := core2.Activate(ctx2, "devbox"); err == nil {
		t.Fatal("只有大写 cookie 名必须报错（name 精确匹配）")
	}
}

// TestPairDecodeFailureRegistersNothing 契约条 21：解码失败返回错误、不登记机器。
func TestPairDecodeFailureRegistersNothing(t *testing.T) {
	core := New(nil, slog.Default())
	defer core.Close()
	if _, err := core.Pair(context.Background(), "not-json"); err == nil {
		t.Fatal("解码失败必须报错")
	}
	if names := core.MachineNames(); len(names) != 0 {
		t.Fatalf("解码失败不得登记机器: %v", names)
	}
}
```

> 注：上面的 import 块是 Task 1 独有的精确集合（逐包都被用到，无占位）。Task 3 追加测试时需要新增 `errors`、`net/http/httptest` 与 `github.com/Xsxdot/handoff/internal/client`（见 Task 3 步骤 2）；Task 4 追加测试时需要新增 `os`（见 Task 4 步骤 2）——各任务步骤会显式说明。

### 步骤 3（跑红）

```bash
go test ./internal/mobilecore/... -count=1
```

预期：**编译失败**（`undefined: SessionCookie` / `Activate` 等）。红即成立，原样输出记台账。

### 步骤 4（最小实现）

新建 `internal/mobilecore/session.go`（完整）：

```go
// 本文件实现移动连接核的会话兑换与单槽会话罐：Go 核用 token 副本程序化领
// 一次性 ticket，经本机回环反代请求 /console 拿 Set-Cookie，供绑定面注入
// webview cookie jar（spec 实现决定：回环门禁 + 多机会话域）。
//
// 边界：
//   - 不注入任何凭据给反代：ticket 在 URL 查询里，兑换请求本身不带
//     Authorization；cookie 只在本包内解析、由绑定面取走
//   - cookie 不落日志：日志只带 cookie 名与机器名（token / cookie 等同主令牌集）
//   - 不 import internal/agentd：cookie 名以本包常量镜像，由会话测试的
//     「源对齐断言」与 agentd 源逐字锁定
package mobilecore

import (
	"context"
	"errors"
	"fmt"
	"log/slog"
	"net/http"
	"net/url"
	"strings"
	"time"

	"github.com/Xsxdot/handoff/internal/client"
)

const (
	// sessionCookieName 镜像 agentd 的会话 cookie 名
	// （internal/agentd/auth.go#sessionCookieName）。移动核不得 import agentd；
	// 该字面值由 TestSessionCookieNameMatchesAgentdSource 与 agentd 源逐字锁定。
	sessionCookieName = "handoff_session"
	// mobileDeviceName 是移动核代领 ticket 时登记的设备名前缀（纯展示）。
	// 后接机器名，便于 agentd 会话表区分是哪台机器换出的会话。
	mobileDeviceName = "handoff-mobile"
	// exchangeTimeout 是单次 ticket→cookie 兑换的时限。
	exchangeTimeout = 5 * time.Second
)

// SessionCookie 是一台机器程序化兑换得到的浏览器会话 cookie（注入 webview 用）。
//
// 字段形状保持 gomobile 可绑（仅 string/bool/int；不用 time.Time）——绑定面把
// 它交给壳的平台 cookie API（WKHTTPCookieStore / CookieManager）。
type SessionCookie struct {
	Name     string `json:"name"`
	Value    string `json:"value"`
	Path     string `json:"path"`
	HttpOnly bool   `json:"http_only"`
	Secure   bool   `json:"secure"`
	SameSite string `json:"same_site"`
	MaxAge   int    `json:"max_age"`
}

// exchangeTicket 用 cl 的 token 副本领一张一次性 ticket，再经本机回环反代请求
// 兑换地址拿 Set-Cookie，返回会话 cookie。
//
// 参数：
//   - machine: 机器名（登记进设备名，便于对端会话表区分）
//   - origin: 该机的回环源（http://127.0.0.1:<port>），兑换请求必过反代
//   - cl: 该机的已选路客户端（领 ticket 走它的 token 与 Transport）
//   - log: 结构化日志
//
// 返回：
//   - 兑换到的 handoff_session cookie
//   - 领票失败、兑换地址不可解析、非 302、无 Set-Cookie、cookie 空值均返回错误
//
// 注意：
//   - **不跟随 302**：agentd 的 /console 是导航流，跟随会把请求带到 / 并把
//     cookie 换成对端跳转；用 http.ErrUseLastResponse 只取响应头
//   - 兑换请求不带 Authorization，反代也不注入——回环门禁的承重断言
func exchangeTicket(ctx context.Context, machine, origin string, cl *client.Client, log *slog.Logger) (SessionCookie, error) {
	log.Info("程序化兑换 ticket→cookie 开始", "machine", machine, "origin", origin)
	ticket, err := cl.IssueAuthTicket(ctx, mobileDeviceName+"-"+machine)
	if err != nil {
		log.Warn("代领 ticket 失败", "machine", machine, "cause", err)
		return SessionCookie{}, fmt.Errorf("代领 ticket: %w", err)
	}
	u, err := url.Parse(ticket.URL)
	if err != nil {
		return SessionCookie{}, fmt.Errorf("ticket 兑换地址不可解析: %w", err)
	}
	exURL := strings.TrimRight(origin, "/") + u.RequestURI()
	ectx, cancel := context.WithTimeout(ctx, exchangeTimeout)
	defer cancel()
	req, err := http.NewRequestWithContext(ectx, http.MethodGet, exURL, nil)
	if err != nil {
		return SessionCookie{}, fmt.Errorf("构造兑换请求: %w", err)
	}
	hc := &http.Client{
		Transport: &http.Transport{Proxy: nil},
		CheckRedirect: func(*http.Request, []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := hc.Do(req)
	if err != nil {
		log.Warn("兑换请求失败", "machine", machine, "origin", origin, "cause", err)
		return SessionCookie{}, fmt.Errorf("请求兑换地址: %w", err)
	}
	defer resp.Body.Close()
	if resp.StatusCode != http.StatusFound {
		log.Warn("兑换响应非 302", "machine", machine, "status", resp.StatusCode)
		return SessionCookie{}, fmt.Errorf("兑换 ticket 期望 302，得到 %d", resp.StatusCode)
	}
	for _, ck := range resp.Cookies() {
		if ck.Name != sessionCookieName {
			continue
		}
		if ck.Value == "" {
			// 名字在、值为空：既不是「字段缺失」也不是可用会话，必须报错。
			log.Warn("兑换响应 cookie 值为空", "machine", machine, "cookie_name", ck.Name)
			return SessionCookie{}, errors.New("兑换响应的 " + sessionCookieName + " cookie 值为空")
		}
		out := toSessionCookie(ck)
		log.Info("程序化兑换 ticket→cookie 成功",
			"machine", machine, "origin", origin, "cookie_name", out.Name, "secure", out.Secure)
		return out, nil
	}
	log.Warn("兑换响应没有会话 cookie", "machine", machine, "cookie_name", sessionCookieName)
	return SessionCookie{}, errors.New("兑换响应没有 " + sessionCookieName + " cookie")
}

// toSessionCookie 把标准库 cookie 投影成本包 DTO（跨绑定面的手工投影点）。
//
// 注意：只搬运壳注入 webview 所需的字段；不复制 Raw/RawExpires（webview 由
// 壳按 Name/Value/Path/域自行构造 cookie），也不改 MaxAge 语义（<=0 即会话 cookie）。
func toSessionCookie(ck *http.Cookie) SessionCookie {
	return SessionCookie{
		Name:     ck.Name,
		Value:    ck.Value,
		Path:     ck.Path,
		HttpOnly: ck.HttpOnly,
		Secure:   ck.Secure,
		SameSite: sameSiteName(ck.SameSite),
		MaxAge:   ck.MaxAge,
	}
}

// sameSiteName 把标准库 SameSite 枚举投影成绑定面可携带的稳定字面值。
func sameSiteName(m http.SameSite) string {
	switch m {
	case http.SameSiteDefaultMode:
		return "Default"
	case http.SameSiteLaxMode:
		return "Lax"
	case http.SameSiteStrictMode:
		return "Strict"
	case http.SameSiteNoneMode:
		return "None"
	}
	return ""
}
```

改 `internal/mobilecore/core.go`：

1. 在 `PairResult` 之后加入哨兵与结构（替换文件里现有的 `Core`、`machine` 定义）：

```go
// errCoreClosed 是核关闭后的统一拒绝错误：Close 幂等，关闭后 Pair/Origin/
// Activate/Retry/Session 一律拒收。
var errCoreClosed = errors.New("连接核已关闭")

// Core 是移动连接核。零值不可用，必须经 New 构造。
//
// 会话罐是**单槽**：任何时刻至多装一机的 cookie（cookie 不按端口隔离，
// RFC 6265），切机即清空并由 Activate 重兑换。
type Core struct {
	mu     sync.Mutex
	exchMu sync.Mutex // 串行化 Activate 的领票+兑换，避免并发切机交错覆盖
	dial   DialFunc
	log    *slog.Logger

	machines map[string]*machine
	closed   bool

	active          string // 当前会话罐所属机器名；空 = 无活动会话
	cookie          SessionCookie
	cookieExpiresAt time.Time
}

type machine struct {
	name    string
	reg     proto.PairMachine // 原始登记，供离线补配重试
	relay   *proto.PairRelay  // 该机所属 bundle 的管道端点，供补配重试
	online  bool
	origin  string
	cl      *client.Client
	cleanup func()
	srv     *http.Server
	ln      net.Listener
}
```

2. 用下面的实现替换现有 `Pair`（其余 `probeReachable`/`replaceMachine`/`startLoopback` 不动），并新增 `registerLocked` 与三个会话方法：

```go
// Pair 解析一份配对 bundle 并登记其中的全部机器：可达的机器起 loopback 反代，
// 不可达的机器标 offline（部分 bundle，不整单失败）。重复扫码幂等：重配覆盖
// 旧客户端与反代，不泄漏旧端口与 goroutine。
func (c *Core) Pair(ctx context.Context, bundleJSON string) (PairResult, error) {
	b, err := proto.DecodePairBundle(bundleJSON)
	if err != nil {
		// 解码失败不登记任何机器（契约条 21）。
		return PairResult{}, err
	}
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.closed {
		return PairResult{}, errCoreClosed
	}
	out := PairResult{}
	for _, m := range b.Machines {
		out.Machines = append(out.Machines, c.registerLocked(ctx, b, m))
	}
	return out, nil
}

// registerLocked 登记一台机器（调用方须持有 c.mu）：可达则拨号→探测→起回环
// 反代并标在线，不可达则在册标离线（部分 bundle，不整单失败）。同名旧机器一律
// 先停再换（重复扫码幂等、不泄漏资源）。
func (c *Core) registerLocked(ctx context.Context, b proto.PairBundle, m proto.PairMachine) PairedMachine {
	cl, cleanup, derr := c.dial(ctx, b, m)
	if derr != nil {
		c.log.Warn("配对机器登记失败，标离线待补配", "machine", m.Name, "cause", derr)
		return PairedMachine{Name: m.Name, Online: false}
	}
	if !probeReachable(ctx, cl) {
		cleanup()
		c.log.Warn("配对机器不可达，标离线待补配", "machine", m.Name)
		return PairedMachine{Name: m.Name, Online: false}
	}
	origin, ln, srv, lerr := c.startLoopback(m.Name, cl)
	if lerr != nil {
		cleanup()
		c.log.Warn("机器回环反代起不来，标离线待补配", "machine", m.Name, "cause", lerr)
		return PairedMachine{Name: m.Name, Online: false}
	}
	c.replaceMachine(m.Name, &machine{
		name: m.Name, reg: m, relay: b.Relay, online: true,
		origin: origin, cl: cl, cleanup: cleanup, srv: srv, ln: ln,
	})
	c.log.Info("机器配对在线", "machine", m.Name, "origin", origin)
	return PairedMachine{Name: m.Name, Origin: origin, Online: true}
}

// Activate 把某台机器的会话罐置为当前：程序化兑换本机的 ticket→cookie，
// 返回应注入 webview cookie jar 的 cookie。反代不注入凭据，cookie 由绑定面
// 交给壳（回环门禁成立）。
//
// 参数：
//   - machine: 已配对且在线机器名
//
// 返回：
//   - 该机的 handoff_session cookie
//   - 机器未配对、核已关闭、兑换失败时返回错误（不返回空 cookie 冒充成功）
//
// 注意：本任务（Task 1）每次调用都重新兑换；同机缓存与切机清罐由 Task 2 接入。
func (c *Core) Activate(ctx context.Context, machine string) (SessionCookie, error) {
	c.exchMu.Lock()
	defer c.exchMu.Unlock()
	c.mu.Lock()
	if c.closed {
		c.mu.Unlock()
		return SessionCookie{}, errCoreClosed
	}
	m, ok := c.machines[machine]
	if !ok {
		c.mu.Unlock()
		return SessionCookie{}, fmt.Errorf("未配对的机器 %q", machine)
	}
	origin, cl := m.origin, m.cl
	c.mu.Unlock()

	ck, err := exchangeTicket(ctx, machine, origin, cl, c.log)
	if err != nil {
		return SessionCookie{}, err
	}
	c.mu.Lock()
	if c.closed {
		c.mu.Unlock()
		return SessionCookie{}, errCoreClosed
	}
	c.active = machine
	c.cookie = ck
	c.cookieExpiresAt = expiryFrom(ck)
	c.mu.Unlock()
	c.log.Info("会话罐已切换", "machine", machine, "origin", origin, "cookie_name", ck.Name)
	return ck, nil
}

// Session 返回当前活动机器的会话 cookie（无活动会话时返回错误）。
func (c *Core) Session() (SessionCookie, error) {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.closed {
		return SessionCookie{}, errCoreClosed
	}
	if !c.cookieValidLocked() {
		return SessionCookie{}, errors.New("当前没有活动会话")
	}
	return c.cookie, nil
}

// ActiveMachine 返回当前会话罐所属机器名；无活动会话时为空串。
func (c *Core) ActiveMachine() string {
	c.mu.Lock()
	defer c.mu.Unlock()
	return c.active
}

// cookieValidLocked 判定单槽罐里是否装着一个可用会话（调用方须持有 c.mu）。
func (c *Core) cookieValidLocked() bool {
	if c.active == "" || c.cookie.Name == "" || c.cookie.Value == "" {
		return false
	}
	if !c.cookieExpiresAt.IsZero() && !time.Now().Before(c.cookieExpiresAt) {
		return false
	}
	return true
}

// expiryFrom 把 cookie 的 MaxAge 翻成绝对过期时刻；<=0（会话 cookie）返回零值。
func expiryFrom(ck SessionCookie) time.Time {
	if ck.MaxAge <= 0 {
		return time.Time{}
	}
	return time.Now().Add(time.Duration(ck.MaxAge) * time.Second)
}
```

### 步骤 5（跑绿）

```bash
go build ./...
go test ./internal/mobilecore/... -count=1
go test ./internal/mobilecore/... -race -count=1
go vet ./internal/mobilecore/...
```

预期全部退出 0。若 `go test` 报 `pairProbeTimeout` 等未定义，说明 `core.go` 的既有区块被误删，按基线恢复。

### 步骤 6（变异验证，可红证明）

临时把 `exchangeTicket` 的 `CheckRedirect` 改成返回 `nil`（跟随 302）→ `go test ./internal/mobilecore/ -run TestExchangeTicketVerticalSlice -count=1` 必红。实测原文：`Activate: 兑换 ticket 期望 302，得到 200`（跟随 `/after-login` 后状态码已被换成 200，故未到「不跟随」断言就先红）。`git checkout -- internal/mobilecore/session.go` 还原必绿。结果记台账（原文）。

### Task 1 验收

- 四支测试（竖切、失败模式、cookie 选择、解码失败不登记）全绿；`-race`/`vet` 退出 0。
- 变异（跟随 302）先红后绿。
- 兑换请求带空 Authorization、回环门禁 401/200 两断言成立。
- `grep -n '\.HTTPClient()' internal/mobilecore/session.go` **零命中**。

---

## 5. Task 2：切机清罐与同机缓存

> 依赖 Task 1（`Activate` 已能兑换）。

### 文件范围

`internal/mobilecore/core.go`（改 `Activate`）、`internal/mobilecore/session_test.go`（加测试）。

### Interfaces

Produces（行为变化，签名不变）：`(*Core).Activate` 增「切机清罐 + 同机缓存」语义；`Session`/`ActiveMachine` 可观察单槽罐。

### 步骤 1（判据先在基线跑）

在 Task 1 完成后，`TestActivateSameMachineCached`（下面）在 Task 1 的「每次兑换」实现下**必红**（tickets 会涨到 2）——红的前提成立。

### 步骤 2（红）

在 `session_test.go` 追加：

```go
// TestSessionJarSwitchesMachine 锁多机会话域（契约条 31）：每机独立回环端口 +
// 单槽会话罐。切机后 cookie 必须是新机的、旧机 cookie 不再可得；切回旧机重兑换。
// 机器名进设备名、进而进 ticket 与 cookie 值，故可区分机器。
func TestSessionJarSwitchesMachine(t *testing.T) {
	fa := &fakeAgentd{}
	relayURL, cleanup := startFakeRelay(t, pairToken, "acc1", "devbox", fa)
	defer cleanup()
	bundle := proto.PairBundle{
		Version: proto.PairVersion,
		Relay:   &proto.PairRelay{URL: relayURL, Credential: "pipecred"},
		Machines: []proto.PairMachine{
			{Name: "alpha", Token: pairToken, Node: "devbox"},
			{Name: "beta", Token: pairToken, Node: "devbox"},
		},
	}
	payload, err := proto.EncodePairBundle(bundle)
	if err != nil {
		t.Fatal(err)
	}
	core := New(nil, slog.Default())
	defer core.Close()
	ctx, cancel := context.WithTimeout(context.Background(), 15*time.Second)
	defer cancel()
	res, err := core.Pair(ctx, payload)
	if err != nil {
		t.Fatalf("配对: %v", err)
	}
	origins := map[string]string{}
	for _, pm := range res.Machines {
		if !pm.Online {
			t.Fatalf("两机都应在线: %+v", pm)
		}
		origins[pm.Name] = pm.Origin
	}
	if origins["alpha"] == origins["beta"] {
		t.Fatalf("每机必须独立回环端口: %+v", origins)
	}

	ckA, err := core.Activate(ctx, "alpha")
	if err != nil {
		t.Fatalf("Activate alpha: %v", err)
	}
	ckB, err := core.Activate(ctx, "beta")
	if err != nil {
		t.Fatalf("Activate beta: %v", err)
	}
	if ckA.Value == ckB.Value {
		t.Fatalf("切机后必须重兑换，cookie 不得沿用旧机: A=%q B=%q", ckA.Value, ckB.Value)
	}
	if !strings.Contains(ckA.Value, "alpha") || !strings.Contains(ckB.Value, "beta") {
		t.Fatalf("cookie 未绑定机器: A=%q B=%q", ckA.Value, ckB.Value)
	}
	if core.ActiveMachine() != "beta" {
		t.Fatalf("切机后 ActiveMachine=%q，期望 beta", core.ActiveMachine())
	}
	sess, err := core.Session()
	if err != nil {
		t.Fatalf("Session: %v", err)
	}
	if sess.Value != ckB.Value {
		t.Fatalf("单槽会话罐必须只装当前机器: got=%q want=%q", sess.Value, ckB.Value)
	}
	// 切回 alpha：必须重新兑换出 alpha 的 cookie。
	ckA2, err := core.Activate(ctx, "alpha")
	if err != nil {
		t.Fatalf("切回 alpha: %v", err)
	}
	if !strings.Contains(ckA2.Value, "alpha") {
		t.Fatalf("切回 alpha 必须重兑换: %+v", ckA2)
	}
	if core.ActiveMachine() != "alpha" {
		t.Fatalf("切回后 ActiveMachine=%q", core.ActiveMachine())
	}
}

// TestActivateSameMachineCached 锁同机重复 Activate 命中缓存（不重复领票）；
// 缓存是切机重兑换语义的对偶：机器没变就不打扰对端。
func TestActivateSameMachineCached(t *testing.T) {
	fa := &fakeAgentd{}
	relayURL, cleanup := startFakeRelay(t, pairToken, "acc1", "devbox", fa)
	defer cleanup()
	payload, err := proto.EncodePairBundle(relayBundle(relayURL, "devbox"))
	if err != nil {
		t.Fatal(err)
	}
	core := New(nil, slog.Default())
	defer core.Close()
	ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel()
	if _, err := core.Pair(ctx, payload); err != nil {
		t.Fatal(err)
	}
	if _, err := core.Activate(ctx, "devbox"); err != nil {
		t.Fatalf("首次 Activate: %v", err)
	}
	fa.mu.Lock()
	afterFirst := fa.tickets
	fa.mu.Unlock()
	if afterFirst != 1 {
		t.Fatalf("首次 Activate 应恰领 1 张 ticket，得到 %d", afterFirst)
	}
	if _, err := core.Activate(ctx, "devbox"); err != nil {
		t.Fatalf("重复 Activate: %v", err)
	}
	fa.mu.Lock()
	afterSecond := fa.tickets
	fa.mu.Unlock()
	if afterSecond != 1 {
		t.Fatalf("同机重复 Activate 必须命中缓存（不重复领票），tickets=%d", afterSecond)
	}
}
```

跑红：`go test ./internal/mobilecore/ -run 'TestActivateSameMachineCached|TestSessionJarSwitchesMachine' -count=1`。`TestActivateSameMachineCached` 必红（无缓存）。`TestSessionJarSwitchesMachine` 在 Task 1 的「每次清/换」实现下多半已绿；若它此刻已绿，属「写下去就会绿的免除」，不要求变红，但它的**变异必须可红**（步骤 5）。

### 步骤 3（最小实现）

用下面的版本替换 `core.go` 的 `Activate`（新增缓存判定与显式清罐）：

```go
// Activate 把某台机器的会话罐置为当前：切机即清掉旧机器的 cookie，程序化
// 兑换（或命中同机缓存复用）本机的 ticket→cookie，返回应注入 webview cookie
// jar 的 cookie。
//
// 参数：
//   - machine: 已配对且在线机器名
//
// 返回：
//   - 该机的 handoff_session cookie
//   - 机器未配对、核已关闭、兑换失败时返回错误（不返回空 cookie 冒充成功）
//
// 注意：
//   - 每机独立回环端口 + 核内单槽罐：任何时刻一罐只装一机的 cookie
//     （cookie 不按端口隔离，RFC 6265）
//   - 同机重复调用命中缓存，不重复领票；切到别的机器再切回会重新兑换
//   - 兑换在 c.exchMu 下串行（网络调用不持 c.mu），清罐在兑换前显式执行
func (c *Core) Activate(ctx context.Context, machine string) (SessionCookie, error) {
	c.exchMu.Lock()
	defer c.exchMu.Unlock()
	c.mu.Lock()
	if c.closed {
		c.mu.Unlock()
		return SessionCookie{}, errCoreClosed
	}
	m, ok := c.machines[machine]
	if !ok {
		c.mu.Unlock()
		return SessionCookie{}, fmt.Errorf("未配对的机器 %q", machine)
	}
	if !m.online {
		c.mu.Unlock()
		return SessionCookie{}, fmt.Errorf("机器 %q 当前离线，无法建立会话", machine)
	}
	if c.active == machine && c.cookieValidLocked() {
		ck := c.cookie
		c.mu.Unlock()
		return ck, nil
	}
	// 清罐：任何时刻一罐只装一机的 cookie——切机先把旧机的 cookie 清掉。
	c.active = ""
	c.cookie = SessionCookie{}
	c.cookieExpiresAt = time.Time{}
	origin, cl := m.origin, m.cl
	c.mu.Unlock()

	ck, err := exchangeTicket(ctx, machine, origin, cl, c.log)
	if err != nil {
		return SessionCookie{}, err
	}
	c.mu.Lock()
	if c.closed {
		c.mu.Unlock()
		return SessionCookie{}, errCoreClosed
	}
	c.active = machine
	c.cookie = ck
	c.cookieExpiresAt = expiryFrom(ck)
	c.mu.Unlock()
	c.log.Info("会话罐已切换", "machine", machine, "origin", origin, "cookie_name", ck.Name)
	return ck, nil
}
```

### 步骤 4（跑绿）

```bash
go test ./internal/mobilecore/... -count=1
go test ./internal/mobilecore/... -race -count=1
```

预期 ok。

### 步骤 5（变异验证，切机清罐可红）

临时把缓存判定改成「只要罐非空就复用、不看机器」：

```go
if c.cookieValidLocked() { ck := c.cookie; c.mu.Unlock(); return ck, nil }
```

→ `go test ./internal/mobilecore/ -run TestSessionJarSwitchesMachine -count=1` 必红（`切机后必须重兑换` 或 `单槽会话罐`）。`git checkout -- internal/mobilecore/core.go` 还原必绿。原文记台账。

### Task 2 验收

- 两支测试绿；`-race` 退出 0。
- 变异（不清罐/不换机）先红后绿。
- `Session`/`ActiveMachine` 可观察单槽罐（断言已落）。

---

## 6. Task 3：离线补配与离线登记

> 依赖 Task 1/2。离线机要在册（`MachineNames` 可见、`Origin` 拒绝、可 `Retry`）。

### 文件范围

`internal/mobilecore/core.go`（改 `registerLocked`/`Origin`/`MachineNames`，新增 `Retry`）、`internal/mobilecore/session_test.go`（加测试）。

### Interfaces

Produces：

```go
func (c *Core) Retry(ctx context.Context, machine string) (PairedMachine, error)
func (c *Core) Origin(machine string) (string, error)  // 语义：未配对/离线均报错
func (c *Core) MachineNames() []string                 // 语义：含离线机
```

### 步骤 1（判据先在基线跑）

`Core.Retry` 不存在（`codegraph sym Retry --view cards-B369-charter` 未命中），新增测试必红。

### 步骤 2（红）

在 `session_test.go` 追加：

```go
// TestRetryOfflineThenOnline 锁离线补配（contract §8.8 / spec「上线后补配」）：
// 首次拨号失败的机器在册标 offline 且 Origin 空、出现在配对清单；重试入口在
// 可达后把机器翻回 online 并给出回环源。用注入 DialFunc 模拟「先不可达、后可达」。
func TestRetryOfflineThenOnline(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(http.StatusOK)
	}))
	defer ts.Close()

	var mu sync.Mutex
	calls := 0
	dial := func(ctx context.Context, b proto.PairBundle, m proto.PairMachine) (*client.Client, func(), error) {
		mu.Lock()
		calls++
		n := calls
		mu.Unlock()
		if n == 1 {
			return nil, nil, errors.New("模拟首次不可达")
		}
		return client.New(ts.URL, m.Token), func() {}, nil
	}
	core := New(dial, slog.Default())
	defer core.Close()
	payload, err := proto.EncodePairBundle(proto.PairBundle{
		Version: proto.PairVersion,
		Machines: []proto.PairMachine{
			{Name: "late", Token: pairToken, Addr: "http://127.0.0.1:1"},
		},
	})
	if err != nil {
		t.Fatal(err)
	}
	res, err := core.Pair(context.Background(), payload)
	if err != nil {
		t.Fatalf("部分 bundle 不应整单失败: %v", err)
	}
	if len(res.Machines) != 1 || res.Machines[0].Online || res.Machines[0].Origin != "" {
		t.Fatalf("首次应离线且 Origin 空: %+v", res.Machines)
	}
	if _, err := core.Origin("late"); err == nil {
		t.Fatal("离线机 Origin 必须报错")
	}
	names := core.MachineNames()
	if len(names) != 1 || names[0] != "late" {
		t.Fatalf("离线机必须在配对清单: %v", names)
	}
	pm, err := core.Retry(context.Background(), "late")
	if err != nil {
		t.Fatalf("Retry: %v", err)
	}
	if !pm.Online || pm.Origin == "" {
		t.Fatalf("补配后应在线: %+v", pm)
	}
	if got, err := core.Origin("late"); err != nil || got != pm.Origin {
		t.Fatalf("在线机 Origin 应可查: %q %v", got, err)
	}
	// 未登记机器的补配必须报错（不凭空造机器）。
	if _, err := core.Retry(context.Background(), "ghost"); err == nil {
		t.Fatal("未登记机器 Retry 必须报错")
	}
}

// TestTokenEntropyGateRejectsWeakToken 契约条 11：relay 形态 token 少于 32 hex
// 字符被 entropy 闸拒绝，Core.Pair 据此标离线不整单失败。
func TestTokenEntropyGateRejectsWeakToken(t *testing.T) {
	if _, _, err := DefaultDial(context.Background(),
		proto.PairBundle{Relay: &proto.PairRelay{URL: "wss://relay.invalid"}},
		proto.PairMachine{Name: "m", Token: "abcd", Node: "n"}); err == nil {
		t.Fatal("短 token 必须被熵闸拒绝")
	}
	payload, err := proto.EncodePairBundle(proto.PairBundle{
		Version: proto.PairVersion,
		Relay:   &proto.PairRelay{URL: "wss://relay.invalid", Credential: "c"},
		Machines: []proto.PairMachine{
			{Name: "weak", Token: "abcd", Node: "n"},
		},
	})
	if err != nil {
		t.Fatal(err)
	}
	core := New(nil, slog.Default())
	defer core.Close()
	res, err := core.Pair(context.Background(), payload)
	if err != nil {
		t.Fatalf("弱 token 不应整单失败: %v", err)
	}
	if len(res.Machines) != 1 || res.Machines[0].Online || res.Machines[0].Origin != "" {
		t.Fatalf("弱 token 机器应离线且 Origin 空: %+v", res.Machines)
	}
}
```

跑红：`go test ./internal/mobilecore/ -run 'TestRetryOfflineThenOnline|TestTokenEntropyGateRejectsWeakToken' -count=1`（编译失败即红，因为 `Core.Retry` 不存在）。本 task 的测试代码使用 `errors`、`net/http/httptest` 与 `github.com/Xsxdot/handoff/internal/client`，需在 `session_test.go` 的 import 块补进这三个包（`httptest` 与 `client` 在本 task 的两个测试里首次用到）。

### 步骤 3（最小实现）

**3a. `registerLocked`**：把三条离线分支从「直接返回、不登记」改为「登记离线记录后返回」。用下面替换 `registerLocked`：

```go
// registerLocked 登记一台机器（调用方须持有 c.mu）：可达则拨号→探测→起回环
// 反代并标在线，不可达则**在册**标离线（部分 bundle，不整单失败；离线机保留
// 原始登记供 Retry 补配）。同名旧机器一律先停再换（重复扫码幂等、不泄漏资源）。
func (c *Core) registerLocked(ctx context.Context, b proto.PairBundle, m proto.PairMachine) PairedMachine {
	offline := &machine{name: m.Name, reg: m, relay: b.Relay, online: false}

	cl, cleanup, derr := c.dial(ctx, b, m)
	if derr != nil {
		c.log.Warn("配对机器登记失败，标离线待补配", "machine", m.Name, "cause", derr)
		c.replaceMachine(m.Name, offline)
		return PairedMachine{Name: m.Name, Online: false}
	}
	if !probeReachable(ctx, cl) {
		cleanup()
		c.log.Warn("配对机器不可达，标离线待补配", "machine", m.Name)
		c.replaceMachine(m.Name, offline)
		return PairedMachine{Name: m.Name, Online: false}
	}
	origin, ln, srv, lerr := c.startLoopback(m.Name, cl)
	if lerr != nil {
		cleanup()
		c.log.Warn("机器回环反代起不来，标离线待补配", "machine", m.Name, "cause", lerr)
		c.replaceMachine(m.Name, offline)
		return PairedMachine{Name: m.Name, Online: false}
	}
	c.replaceMachine(m.Name, &machine{
		name: m.Name, reg: m, relay: b.Relay, online: true,
		origin: origin, cl: cl, cleanup: cleanup, srv: srv, ln: ln,
	})
	c.log.Info("机器配对在线", "machine", m.Name, "origin", origin)
	return PairedMachine{Name: m.Name, Origin: origin, Online: true}
}
```

**3b. 新增 `Retry`**（放在 `registerLocked` 之后）：

```go
// Retry 对一台此前标记离线的机器重试配对（spec「离线机标记、上线后补配」）：
// 可达则补起回环反代并翻 online，仍不可达则保持 offline（返回 Online=false 且
// 不报错——补配是幂等重试，不是一次必须成功的操作）。
//
// 参数：
//   - machine: 已登记机器名（在线/离线均可；在线机重试等价重配）
//
// 返回：
//   - 该机的最新配对摘要
//   - 未登记机器、核已关闭时返回错误
func (c *Core) Retry(ctx context.Context, machine string) (PairedMachine, error) {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.closed {
		return PairedMachine{}, errCoreClosed
	}
	old, ok := c.machines[machine]
	if !ok {
		return PairedMachine{}, fmt.Errorf("未配对的机器 %q", machine)
	}
	c.log.Info("离线补配重试开始", "machine", machine, "was_online", old.online)
	b := proto.PairBundle{Version: proto.PairVersion, Relay: old.relay}
	pm := c.registerLocked(ctx, b, old.reg)
	c.log.Info("离线补配重试结束", "machine", machine, "online", pm.Online)
	return pm, nil
}
```

**3c. `Origin`/`MachineNames`**：用下面替换两函数：

```go
// Origin 返回某台机器 webview 应加载的 loopback 源。
//
// 边界：未配对或离线的机器都返回错误——离线机没有可加载的源，前端据
// PairResult.Online=false 显示「离线」而非加载空源（contract 缺陷族 7）。
func (c *Core) Origin(machine string) (string, error) {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.closed {
		return "", errCoreClosed
	}
	m, ok := c.machines[machine]
	if !ok {
		return "", fmt.Errorf("未配对的机器 %q", machine)
	}
	if !m.online {
		return "", fmt.Errorf("机器 %q 当前离线", machine)
	}
	return m.origin, nil
}

// MachineNames 返回已登记机器名（含离线机，供设置页配对清单）。
func (c *Core) MachineNames() []string {
	c.mu.Lock()
	defer c.mu.Unlock()
	names := make([]string, 0, len(c.machines))
	for n := range c.machines {
		names = append(names, n)
	}
	return names
}
```

### 步骤 4（跑绿）

```bash
go test ./internal/mobilecore/... -count=1
go test ./internal/mobilecore/... -race -count=1
```

### 步骤 5（变异验证，token 熵闸可红）

临时注释 `DefaultDial` 里的 `relay.CheckTokenEntropy(m.Token)` 调用 → `go test ./internal/mobilecore/ -run TestTokenEntropyGateRejectsWeakToken -count=1` 必红。`git checkout -- internal/mobilecore/core.go` 还原必绿。原文记台账。

### Task 3 验收

- 两支测试绿；离线机在 `MachineNames`、`Origin` 报错、`Retry` 翻 online。
- 变异（去熵闸）先红后绿。

---

## 7. Task 4：资源安全、幂等与并发收口

> 依赖 Task 3（离线机在册）。

### 文件范围

`internal/mobilecore/core.go`（`Close` 清罐）、`internal/mobilecore/session_test.go`（加测试）。

### Interfaces

Produces：`(*Core).Close` 幂等语义扩展（清会话罐）；关闭后 `Pair`/`Origin`/`Activate`/`Retry`/`Session` 全部拒绝。

### 步骤 1（判据先在基线跑）

Task 3 后 `Close` 已幂等（基线即有），但「关闭后拒收」无断言；`Close` 清罐亦未实现。

### 步骤 2（红）

在 `session_test.go` 追加（本 task 的测试代码使用 `context`、`log/slog`、`net/http`、`net/http/httptest`、`os`、`strings`、`sync`、`testing`、`time` 与 `internal/client`；其中 `os` 是本 task 首次用到，需补进 import 块）：

```go
// TestPairIdempotentNoLeak 契约条 19：同名机器重配覆盖旧客户端与反代，旧资源
// 恰回收一次（不泄漏端口/goroutine）。用注入 DialFunc 的 cleanup 计数作确定性判据。
func TestPairIdempotentNoLeak(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(http.StatusOK)
	}))
	defer ts.Close()
	var mu sync.Mutex
	cleanups := 0
	dial := func(ctx context.Context, b proto.PairBundle, m proto.PairMachine) (*client.Client, func(), error) {
		return client.New(ts.URL, m.Token), func() {
			mu.Lock()
			cleanups++
			mu.Unlock()
		}, nil
	}
	core := New(dial, slog.Default())
	defer core.Close()
	payload, err := proto.EncodePairBundle(proto.PairBundle{
		Version: proto.PairVersion,
		Machines: []proto.PairMachine{
			{Name: "m", Token: pairToken, Addr: "http://127.0.0.1:1"},
		},
	})
	if err != nil {
		t.Fatal(err)
	}
	ctx := context.Background()
	r1, err := core.Pair(ctx, payload)
	if err != nil {
		t.Fatal(err)
	}
	r2, err := core.Pair(ctx, payload)
	if err != nil {
		t.Fatal(err)
	}
	if r1.Machines[0].Origin == r2.Machines[0].Origin {
		t.Fatalf("重配应换新回环端口: %q", r1.Machines[0].Origin)
	}
	mu.Lock()
	got := cleanups
	mu.Unlock()
	if got != 1 {
		t.Fatalf("旧客户端 cleanup 应恰回收一次，得到 %d", got)
	}
	if names := core.MachineNames(); len(names) != 1 {
		t.Fatalf("重配不得多出机器: %v", names)
	}
}

// TestCloseIdempotentAndRejectsAfter 契约条 20：Close 幂等；关闭后 Pair/Origin/
// Activate/Retry/Session 一律拒绝且不留会话罐。
func TestCloseIdempotentAndRejectsAfter(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(http.StatusOK)
	}))
	defer ts.Close()
	dial := func(ctx context.Context, b proto.PairBundle, m proto.PairMachine) (*client.Client, func(), error) {
		return client.New(ts.URL, m.Token), func() {}, nil
	}
	core := New(dial, slog.Default())
	payload, err := proto.EncodePairBundle(proto.PairBundle{
		Version: proto.PairVersion,
		Machines: []proto.PairMachine{
			{Name: "m", Token: pairToken, Addr: "http://127.0.0.1:1"},
		},
	})
	if err != nil {
		t.Fatal(err)
	}
	ctx := context.Background()
	if _, err := core.Pair(ctx, payload); err != nil {
		t.Fatal(err)
	}
	if core.ActiveMachine() != "" {
		// 未兑换过，罐应为空。
		t.Fatalf("未 Activate 前不应有活动会话: %q", core.ActiveMachine())
	}
	if err := core.Close(); err != nil {
		t.Fatalf("首次 Close: %v", err)
	}
	if err := core.Close(); err != nil {
		t.Fatalf("Close 必须幂等: %v", err)
	}
	if _, err := core.Pair(ctx, payload); err == nil {
		t.Fatal("Close 后 Pair 必须拒绝")
	}
	if _, err := core.Origin("m"); err == nil {
		t.Fatal("Close 后 Origin 必须拒绝")
	}
	if _, err := core.Activate(ctx, "m"); err == nil {
		t.Fatal("Close 后 Activate 必须拒绝")
	}
	if _, err := core.Retry(ctx, "m"); err == nil {
		t.Fatal("Close 后 Retry 必须拒绝")
	}
	if _, err := core.Session(); err == nil {
		t.Fatal("Close 后 Session 必须拒绝")
	}
}

// TestConcurrentPairActivateClose 并发压测（-race）：Pair/Activate/Close 并发下
// 无数据竞争、无 panic。Activate 未命中机器返回错误属预期。
func TestConcurrentPairActivateClose(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(http.StatusOK)
	}))
	defer ts.Close()
	dial := func(ctx context.Context, b proto.PairBundle, m proto.PairMachine) (*client.Client, func(), error) {
		return client.New(ts.URL, m.Token), func() {}, nil
	}
	core := New(dial, slog.Default())
	payload, err := proto.EncodePairBundle(proto.PairBundle{
		Version: proto.PairVersion,
		Machines: []proto.PairMachine{
			{Name: "m", Token: pairToken, Addr: "http://127.0.0.1:1"},
		},
	})
	if err != nil {
		t.Fatal(err)
	}
	ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel()
	var wg sync.WaitGroup
	for i := 0; i < 6; i++ {
		wg.Add(1)
		go func() { defer wg.Done(); _, _ = core.Pair(ctx, payload) }()
	}
	for i := 0; i < 6; i++ {
		wg.Add(1)
		go func() { defer wg.Done(); _, _ = core.Activate(ctx, "m") }()
	}
	wg.Add(1)
	go func() { defer wg.Done(); _ = core.Close() }()
	wg.Wait()
}
```

再追加两条**源码守卫**（内部锁，理由见 §9）：

```go
// TestSessionFileHasNoRawHTTPClient 锁协调者裁决（2026-09-19）：本卡新增文件
// 不得新增 client.HTTPClient() 取用点（B233.3 冻结条 25 与 B369 §3.3 的冲突
// 已记卡；既有违规只允许留在 core.go / proxy.go）。
func TestSessionFileHasNoRawHTTPClient(t *testing.T) {
	b, err := os.ReadFile("session.go")
	if err != nil {
		t.Fatal(err)
	}
	if strings.Contains(string(b), ".HTTPClient()") {
		t.Fatal("session.go 不得取 client.HTTPClient()（本卡裁决：不新增违规取用点）")
	}
}

// TestSessionCookieNameMatchesAgentdSource 锁 wire 常量对齐：mobilecore 不得
// import agentd，cookie 名是镜像字面值——直接比对 agentd 源，防漂移（序列化边界）。
func TestSessionCookieNameMatchesAgentdSource(t *testing.T) {
	b, err := os.ReadFile("../agentd/auth.go")
	if err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(string(b), `sessionCookieName = "handoff_session"`) {
		t.Fatal("mobilecore 镜像的 cookie 名与 agentd 源漂移，须同步 sessionCookieName")
	}
}
```

### 步骤 3（最小实现）

`Close` 用下面替换（补清罐）：

```go
// Close 收掉全部 loopback 反代与客户端并清空会话罐（幂等）。
func (c *Core) Close() error {
	c.mu.Lock()
	defer c.mu.Unlock()
	if c.closed {
		return nil
	}
	c.closed = true
	for _, m := range c.machines {
		stopMachine(m)
	}
	c.machines = map[string]*machine{}
	c.active = ""
	c.cookie = SessionCookie{}
	c.cookieExpiresAt = time.Time{}
	return nil
}
```

### 步骤 4（跑绿）

```bash
go test ./internal/mobilecore/... -count=1
go test ./internal/mobilecore/... -race -count=1
go vet ./internal/mobilecore/...
```

### 步骤 5（变异验证，反代不注入凭据）

把 `proxy.go` 的 Director 临时加一行 `r.Header.Set("Authorization", "Bearer INJECTED")` → `go test ./internal/mobilecore/ -run TestExchangeTicketVerticalSlice -count=1` 必红（兑换请求不得带 Authorization）。`git checkout -- internal/mobilecore/proxy.go` 还原必绿。原文记台账。

### 步骤 6（清理与收口）

- 跑 `gofmt -l internal/mobilecore` 必须零输出；`goimports` 整理后 import 只保留实际使用项（`session_test.go` 最终应含：`context`、`encoding/json`、`errors`、`io`、`log/slog`、`net/http`、`net/http/httptest`、`net/url`、`os`、`strconv`、`strings`、`sync`、`testing`、`time`、`internal/client`、`internal/proto`）。

### Task 4 验收

- 五支新测试（幂等不泄漏、Close 幂等与关闭后拒收、并发压测、两条源码守卫）绿；`-race`/`vet`/`gofmt` 干净。
- 变异（反代注入 Authorization）先红后绿。
- `grep -rn '\.HTTPClient()' internal/mobilecore` 只命中 `core.go` 与 `proxy.go`（无新文件）。

---

## 8. Task 5：收口核验与移交

### 文件范围

无生产码改动；台账追加。

### 步骤

1. 全量本卡判据复跑（原文记台账）：

```bash
go build ./...
go test ./internal/mobilecore/... -count=1
go test ./internal/mobilecore/... -race -count=1
go vet ./internal/mobilecore/...
go test ./internal/relay/... -count=1
go test ./cmd/ -run TestRepoContractGate -count=1
grep -rn '\.HTTPClient()' internal/mobilecore
codegraph validate --view cards-B369-charter
codegraph check --view cards-B369-charter
```

预期：

- 除 `./internal/client/...` 外全绿；`grep` 恰两条（`core.go`、`proxy.go`）。
- `codegraph validate` issues=null、`check` fails=[]（本卡不改图）。
- `go test ./internal/client/... -count=1` **仍红**且原文恰为 `internal/mobilecore/core.go, internal/mobilecore/proxy.go`——这是已知基线红（§0.2），本卡验收判据是「违规集合不新增」，**不是**「变绿」。把原文记台账。

2. 图覆盖债符号清单落实现台账（§1.2 的表），交 integrate/recon 补 `codegraph/diffs`。本卡**不手改** `codegraph/diffs/cards-B369-charter.json`（冻结物）。

3. 台账收口：§自审结论回填。

### Task 5 验收

- 上列命令读数符合预期；已知红原文与「违规集合不新增」结论在台账；符号清单在台账。

---

## 9. 序列化边界与接缝覆盖

### 序列化边界设问

本卡新增跨边界数据 `SessionCookie`：产生方 = `exchangeTicket` 解析 `Set-Cookie`（`*http.Cookie` → `SessionCookie` 手工投影 `toSessionCookie`），消费方 = 绑定面（S4）→ 壳的平台 cookie API。手写投影点只有 `toSessionCookie` 与 `sameSiteName`。穿过真实序列化边界的测试：

- `TestExchangeTicketVerticalSlice` / `TestExchangeTicketCookieSelection`：真实 HTTP 响应头（`Set-Cookie`）经反代回到 Core，逐属性断言（Path/HttpOnly/Secure/SameSite）。
- 多 `Set-Cookie` 头与 name 大小写：`TestExchangeTicketCookieSelection`（诱饵大写名 + 真名）。
- **可空/零值区分**：`TestExchangeTicketFailureModes` 的「cookie 空值」用例区分「`handoff_session` 缺失」与「名在值为零」——两者都拒绝，但错误路径不同（`errors.New("...没有 ... cookie")` vs `errors.New("...cookie 值为空")`）。
- 与 agentd 的 wire 常量对齐：`TestSessionCookieNameMatchesAgentdSource` 直接比对 agentd 源字面值（两端各自测试≠链路有测试，这条把常量链锁住）。

`PairBundle` 的反序列化归 S1（本卡不新增字段）。

### 接缝覆盖（双向）

spec §「测试接缝（草单）」缝 #2 = 回环反代 → relay Transport 适配（存量符号 `internal/client/client.go#NewRelay` 一带）；spec §「测试决定」最高缝 = 配对契约端到端（CLI 产出载体 → App 解析 → fake-relay + agentd → ticket→cookie → `/api` 可达）。

**缝 → 测试**（每条缝至少一支缝级断言）：

| 缝（spec） | 锁它的测试 | 断言 |
| --- | --- | --- |
| 缝 #2：回环反代→relay Transport 适配（`NewRelay` 一带） | `TestExchangeTicketVerticalSlice`、`TestSessionJarSwitchesMachine` | 经 relay 隧道的 `Core.Pair` 探测成功 + 每机独立 `127.0.0.1:<port>` Origin |
| 最高缝：ticket→cookie 会话 → `/api` 可达 | `TestExchangeTicketVerticalSlice` | 真实 `Core.Activate` 换出 cookie；无 cookie 401、带 cookie 200 |
| 回环门禁（不注入凭据） | `TestExchangeTicketVerticalSlice` | `/console` 上游 Authorization 为空；反代路径不补凭据 |
| 多机会话域（一罐一机） | `TestSessionJarSwitchesMachine`、`TestActivateSameMachineCached` | 切机换 cookie；同机不重领 |
| 离线补配 | `TestRetryOfflineThenOnline` | offline 在册→Retry 翻 online |
| 契约条 11/19/20/21 | `TestTokenEntropyGateRejectsWeakToken`、`TestPairIdempotentNoLeak`、`TestCloseIdempotentAndRejectsAfter`、`TestPairDecodeFailureRegistersNothing` | 逐条 |

**测试 → 缝**（入口符号是否在缝上）：

| 测试 | 入口符号 | 是否在缝上 |
| --- | --- | --- |
| TestExchangeTicketVerticalSlice | `Core.Pair`、`Core.Activate` | 是（App 配对/兑换入口） |
| TestExchangeTicketFailureModes | `Core.Pair`、`Core.Activate`、`Core.Session` | 是 |
| TestExchangeTicketCookieSelection | `Core.Pair`、`Core.Activate` | 是 |
| TestSessionJarSwitchesMachine | `Core.Pair`、`Core.Activate`、`Core.Session`、`Core.ActiveMachine`、`Core.Origin` | 是 |
| TestActivateSameMachineCached | `Core.Pair`、`Core.Activate` | 是 |
| TestRetryOfflineThenOnline | `Core.Pair`、`Core.Retry`、`Core.Origin`、`Core.MachineNames` | 是 |
| TestTokenEntropyGateRejectsWeakToken | `DefaultDial`、`Core.Pair` | `DefaultDial` 是 `DialFunc` 缝本体；`Core.Pair` 是缝 |
| TestPairIdempotentNoLeak | `Core.Pair`、`Core.MachineNames` | 是 |
| TestCloseIdempotentAndRejectsAfter | `Core.Close`、`Core.Pair`、`Core.Activate`、`Core.Retry`、`Core.Origin`、`Core.Session` | 是 |
| TestConcurrentPairActivateClose | `Core.Pair`、`Core.Activate`、`Core.Close` | 是 |
| TestPairDecodeFailureRegistersNothing | `Core.Pair`、`Core.MachineNames` | 是 |

**内部锁声明**（默认非法，逐条给「从声明缝构造不出」的理由）：

1. `TestSessionFileHasNoRawHTTPClient`——被锁对象是「新增源文件不含 `.HTTPClient()`」这一**源码性质**，从 `Core.Pair`/`Activate` 的任何调用都构造不出「某个文件里没有字符串」；它是协调者裁决（工单 `9f6bd37f`）的机械化，属源码守卫（先例：B233.3 `execution_test.go`、B233.17 边界守卫）。
2. `TestSessionCookieNameMatchesAgentdSource`——被锁对象是「本包镜像的 cookie 名与 agentd 源字面值一致」；mobilecore 不得 import agentd（contract 条 24），运行时无法从声明缝看见 agentd 的未导出常量，只有读源能锁。这是**序列化边界的常量链**，不是实现细节。
3. `TestConcurrentPairActivateClose`——并发安全性质只有并发入口能构造；入口即 `Pair`/`Activate`/`Close`（已在缝上），故它其实是**缝级**断言，非内部锁。
4. 其余测试入口全部是 `Core` 的导出壁面（`Pair`/`Activate`/`Session`/`ActiveMachine`/`Retry`/`Origin`/`MachineNames`/`Close`）或 `DefaultDial` 缝，无内部锁。

**退路同闸**：本计划步骤正文无「若意外先绿就改入口符号」类条件退路；唯一「先绿不要求变红」的豁免是 Task 2 的 `TestSessionJarSwitchesMachine`（已声明，且其变异必须可红）。

---

## 10. 缺陷族对抗（九族 → 验收栏映射）

| 族 | 设问 | 本卡答案与落点 |
| --- | --- | --- |
| 1 生命周期/状态机中断 | 兑换中途核关闭？端口/goroutine 泄漏？ | 兑换在 `exchMu` 下、网络不持 `c.mu`；兑换后复核 `closed`，关闭即丢弃结果。`replaceMachine`/`stopMachine` 收口 listener+srv+cleanup；`TestPairIdempotentNoLeak`（cleanup 恰一次）、`TestCloseIdempotentAndRejectsAfter`、`-race` + `TestConcurrentPairActivateClose`。真实进程重启/真机切后台归真机清单 #2/#6（breakdown §5）。 |
| 2 静默失败/误导报错 | 兑换失败会不会报成功？ | 非 302、无 `Set-Cookie`、cookie 空值全部返回错误且不留活动会话；`Activate` 不返回空 cookie 冒充成功（`TestExchangeTicketFailureModes`）。兑换设备名进 ticket 便于排障。 |
| 3 跨平台假设 | 回环/ cookie 域？ | `127.0.0.1` 回环 + 每机独立端口 + 单槽罐；cookie `Secure=false`（明文 loopback）。iOS `WKHTTPCookieStore`/Android `CookieManager` 的真实注入与清罐**未验证，需真机 #2**。 |
| 4 假红/假绿 | 测试能变红吗？ | 五处变异：跟随 302（Task 1）、不清罐/不换机（Task 2）、去熵闸（Task 3）、反代注入 Authorization（Task 4）。兑换测试穿真 relay 线格式与真 HTTP 头，非手抄 JSON。`gofmt`/`vet` 收口。 |
| 5 门禁绕过 | 回环门禁会被兑换破坏吗？ | 承重：反代不注入凭据（contract §6 命中 2）。兑换请求 `Authorization` 为空断言 + 无 cookie 401 断言 + Task 4 变异。兑换用 token 副本，但那是**核内部**领票，不是给反代补凭据。 |
| 6 序列化边界 | 新增字段手写投影？ | `SessionCookie`（`toSessionCookie`/`sameSiteName`）；`Set-Cookie` 解析测试覆盖多头部/大小写/属性/空值；与 agentd 常量的源对齐断言。见 §9。 |
| 7 枚举过白名单 | 两态契约？ | `PairedMachine.Online` true/false 与 `Origin` 空/非空：离线机 `Online=false` 且 `Origin` 空、`Core.Origin()` 报错（`TestRetryOfflineThenOnline`、`TestTokenEntropyGateRejectsWeakToken` 反例）。不新增枚举。 |
| 8 承重安全属性 | 可变红吗？ | (i) 反代不注入凭据→Task 4 变异；(ii) 每机独立端口→`TestSessionJarSwitchesMachine`（同端口即红）；(iii) token 熵闸→Task 3 变异；(iv) 单槽一罐→Task 2 变异。 |
| 9 webview 候选族 | 触 webview？ | 核不触 webview；cookie 注入 webview 是 S4/壳职责，真实行为**未验证，需真机 #2**。 |

---

## 11. 五项检查与自审三查

**五项检查**：

1. **缺陷族对抗**——§10 逐族有答案并映射验收栏。✓
2. **序列化边界**——§9 列出 `SessionCookie` 的产生/消费/投影点与穿过真实边界的断言，含缺失 vs 零值区分。✓
3. **上下文预算**——§1.1 有界文件集四条；每 task 文件范围见各节，全部落在集合内。✓
4. **类型标注**——S2 是边界型（接缝对面是 relay 服务端与对端 agentd 的外部现实，breakdown §1），机内验契约形状 + fake relay/agentd，真机项显式列 §10 族 1/3/9 与「未验证」行。✓
5. **接缝覆盖双向**——§9 两张表 + 内部锁逐条声明 + 退路同闸。✓

**自审三查**：

- **spec 覆盖**：breakdown §3.3 ① 契约引用逐条落 task——§3.2（T1/T2/T3）、§3.3（T1/T4）、§3.4（T1/T2）、§4.2 条 11（T3）/12–16（T1）/17–18（T1，既有竖切回归）/19（T4）/20（T4）/21（T1）/22–24（事实，不改）/25–27（不触发）/28–32（T1/T2）；§4.5 条 30（T1）/31（T2）/32（T1）；§8.2（T1）、§8.8（T3）；P4=A（T1 兑换在核）。✓
- **占位符扫描**：全篇无 TBD/「适当处理」/「同 Task N」；新测试与实现均给完整代码；无「复用夹具豁免」声明（各测试完整）。✓
- **跨 task 签名一致**：`SessionCookie` 字段、`exchangeTicket(ctx, machine, origin, cl, log)`、`Core.Activate(ctx, machine) (SessionCookie, error)`、`Core.Session`、`Core.ActiveMachine`、`Core.Retry`、`registerLocked(ctx,b,m)` 在各 task 步骤中逐字一致；Task 1 的 `Activate` 被 Task 2 整段替换（同一签名），Task 1 的 `registerLocked` 被 Task 3 整段替换（同一签名）；`machine` 结构字段在 Task 1 定义、Task 3 使用，无中途改形。✓

---

## 12. 明确不做的改动（越界即回退）

1. 不改 `internal/client/execution_test.go` 那条冻结守卫；不把 mobilecore 加进白名单（协调者裁决 1）。
2. 新增文件不出现任何 `.HTTPClient()` 取用点（协调者裁决 2）；`core.go:155` 与 `proxy.go:38` 的既有取用**保持原样**（不动冻结卡）。
3. 不改 `internal/proto/**`（wire 不动，归 S1）；不改 `internal/agentd/**`（兑换走既有路由，归 d_gateway 只读）。
4. 不改 `internal/client/**`（用既有 `IssueAuthTicket`/`NoRedirect` 语义，但不调用 `NoRedirect`——兑换自建带 `ErrUseLastResponse` 的普通 client 即可，因为目标是回环源不是选路 client）。
5. 不改 `mobile/bind/**`（绑定面桥接归 S4）。
6. 不手改 `codegraph/diffs/**`（图对账归 recon/integrate）。
7. 不做真实 relay 服务端/真实 agentd 端到端、真机 cookie jar——真机清单 #2/#5/#6。
8. 不实现「离线机自动轮询上线」——补配是显式重试入口（spec「上线后补配」的核侧载体），自动轮询归后续硬化。

---

## 13. 岔口（供协调者/评审）

1. **`Timing`/缓存过期策略**：本计划用 `MaxAge` 推绝对过期（`<=0` 视为会话 cookie 不过期）。真实 agentd 的 `sessionCookie` 下发 `MaxAge` 为正（30 天）；若对端未来改成不发送 `MaxAge`，本卡缓存会在核进程存活期内一直复用。可接受（切机必重兑换），但如需更强，可在后续把「同机缓存」也加时限。信息项，不阻塞。
2. **`IssueAuthTicket` 要求主令牌**：`handleIssueTicket` 对会话身份 403（`authroutes.go:131`）。本卡兑换用机器 token（主令牌副本）领票，符合 spec 凭据模型；若未来改用 cookie 会话领票会 403——不在本卡范围。
3. **`TestSessionJarSwitchesMachine` 的两机同 node**：为让单个 fake relay 的 E2E 同时服务两机，两机共用 `Node`=devbox 与同一 token，靠 `Name` 区分。这是夹具约束，不改变生产语义（生产 bundle 中不同机器的 node 通常不同，`DefaultDial` 按 `m.Node` 建 Dialer）。

---

## 14. 收口判据（B369.2 完成的唯一判定）

Task 4 步骤 4 的四条命令全绿（`go test ./internal/mobilecore/...`、`-race`、`go vet`、`gofmt -l` 零输出）+ Task 4 步骤 5 变异先红后绿 + Task 5 的 `grep`/`codegraph`/已知红原文全部符合预期 + 台账收口完整 + 图覆盖债符号清单在实现台账。在此之前，任何「应该差不多了」不构成完成；在此之后，本卡交回协调者（挂附件/移列归协调者）。

---

## 实现者纪律（派发注入段）

1. **TDD 铁律只套锁缝断言**：标「红」的步骤先写测试跑红（原样输出记台账）再最小实现跑绿；测试占位清理、注释、日志步骤不设独立红绿周期。
2. **测试三段律**：每任务只跑触及包（`go test ./internal/mobilecore/...` + 本任务 `-run` 单测）；`go build ./...` 每任务收尾跑；全量测试不属于任何单个任务，归 integrate。
3. **修复熔断**：撞到非预期红，先建红反馈回路查根因；禁止改判据迁就实现、禁止跳过失败。
4. **中间态不外泄**：Task 1→2→3 之间的中间窗口是同分支先后任务；不得中途合并、不得按部分成果通知协调者验收。
5. **红线**：不碰 §12 禁触文件；发现计划与冻结物矛盾 → 停下提问（远程执行发工单问协调者，本地问用户），不自造替代，**特别是不自行裁决 §0.2 的 HTTPClient 冲突**。
6. **台账边干边落**：`docs/superpowers/ledgers/2026-09-19-b369.2-impl-ledger.md` 每确立一个事实追加一行（命令+原始输出、放弃的尝试、判断）；提交事实记「提交当时命令与输出」，随后至多 amend 一次收进同批提交，不 chase hash。
7. **不派发**：执行者不调 handoff 派发 CLI、不做卡操作（挂附件/移列归协调者）；本计划 Task 0–5 无一需要驱动派发系统自身。
