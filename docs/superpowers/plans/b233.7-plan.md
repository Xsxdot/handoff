# B233.7 实现计划：核销 09-07 质量复审 R1–R9

## 0. 计划边界、已拍板选择与交付形态

本计划对应卡 B233.7，目标冻结基线为 `cards/B233.1-charter-7 @ 497a7712`。当前执行分支是 `cards/B233.7-charter-3`；执行者只在当前分支工作，不切分支、不改 Git 配置、不 push。

本文件是本节点唯一法定产物。P3-A 已拍板，本卡按一张 L3 plan 的 S0–S6 有界任务段交付，不创建 B233.7.x 子卡。P1-B、P2-A、P4-A 已拍板，下面直接写成实施约束，不留实现决策口：

| 选择 | 本计划的落点 |
| --- | --- |
| P1-B | `ApprovalClient` 的动态实例继续由 Manager 注入；在该既有注入对象上提供一个可选的、非 `ApprovalClient` 方法集的失败回调能力。OpenCode adapter 只作结构断言并报告 `Manager.NoteDeliveryFailed`，不写 store、不加 `AdapterEvent` 类型、不加用户可见事件名。 |
| P2-A | 普通执行启动在 Manager 现有 `StartReq` 装配处，从同一个 `StartReq.Discipline` 构造 `ProfileReq.TaskOverlay`，再调用既有 `executor.Profile`。五家 adapter 不各自获取或拼接纪律正文。 |
| P3-A | S0 → S1/S2；S3–S6 可在 S0 后并行实现；最终由协调者执行 S0 收口与跨卡审计。 |
| P4-A | 所有冻结文本与验收命令继续引用 `497a7712`；本机 ref 若另有过期哈希，不改冻结物。 |

本节点不修改运行时代码、不创建脚手架。下述代码块是给 implement 节点的精确接口/断言草案；执行时只允许修改各 task 的文件集合。

## 1. 基线证据与图覆盖债

### 1.1 已在动手前实际跑过的判据

以下命令已在本工作树、计划编写前实际执行。基线绿色只说明既有行为，不替代新增锁缝；B350 的红色是 S5 要修正的冻结断言。

```text
go build ./...
→ 退出码 0，标准输出为空

go test ./internal/agentd -run 'TestQuestionReissueAfterAnswerCreatesNewTicket|TestOpenCodeWaitQuestionUsesRespondAsk|TestTaskDiffTargetFallsBackToRepoWhenWorktreeGone|TestAssembleResultRefEmptyCommitAllowed' -count=1
→ ok github.com/Xsxdot/handoff/internal/agentd 0.376s

go test ./internal/executor/... ./internal/hostapi ./internal/scheduling ./internal/workspace ./internal/client -count=1
→ internal/executor、五家 executor 子包、internal/hostapi、internal/scheduling、internal/workspace、internal/client 均 ok；实际输出含：
  ok github.com/Xsxdot/handoff/internal/executor 0.218s
  ok github.com/Xsxdot/handoff/internal/executor/opencode 18.745s
  ok github.com/Xsxdot/handoff/internal/hostapi 0.647s
  ok github.com/Xsxdot/handoff/internal/scheduling 4.058s
  ok github.com/Xsxdot/handoff/internal/workspace 0.011s
  ok github.com/Xsxdot/handoff/internal/client 9.633s

go test ./internal/hostapi -run 'TestWakeHomeReadyRequiresTurnOutputNotCredFile|TestWakeHomeSuppliesMainCredentialBeforeTurn|TestWakeHomeOccupiedNeverOverwrites' -count=1 -v
→ 三测试均 PASS；ok github.com/Xsxdot/handoff/internal/hostapi 0.022s

go test ./internal/keystone/ -run '^TestIgnitionVerticalSlice$' -count=1 -v
→ FAIL；slice_test.go:157: 绑定解析错误: {Squad:exec-1 Carrier:opencode-1 Target:linux-01 Executor:opencode Model: HomeDir:/home/coordinator/.opencode-home}（期望载体 opencode-1 / 机 linux-01 / 执行者覆盖 claude）
  FAIL github.com/Xsxdot/handoff/internal/keystone 0.158s

go test ./cmd -run '^TestRepoContractGate$' -count=1
→ FAIL；graph_gate_test.go:38: 契约违规 [dead-contract] 契约 d_execution→d_orchestration 声明的方向没有活跃 call、implements 或组装点豁免边
  graph_gate_test.go:38: 契约违规 [dead-interface] 契约 d_execution→d_orchestration 声明的接口 "ApprovalClient" 在 d_execution 中不存在
  FAIL github.com/Xsxdot/handoff/cmd 0.043s
```

S0 收口时必须用相同的定向命令复跑；图门禁的两个既有失败不得被本卡计划伪装成通过，本卡也不修改 `codegraph` 契约声明来消除它们。

### 1.2 图查询与源码回落

项目存在 `codegraph/`，先使用了 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . <subcommand>`。最佳领域入口使用 `d_orchestration`、`d_execution`/`d_execution_adapters`、`d_gateway`、`d_workspace`、`d_execution_host`、`d_scheduling`、`d_keystone`、`d_transport`。

- `context d_coordination_task` 实际失败，原文是：`Error: 领域 "d_coordination_task" 不在最优树词表中；... "d_coordination_task" 是**现状视图**领域 id ...`。因此本计划不把现状 id 当最佳领域，也不替协调者重写领域声明。
- `context d_scheduling` 成功，但 warning 指向缺失 `codegraph/domains/d_scheduling.json`；这是图覆盖债。
- `sym/flow` 已命中 `n_agentd_Manager_Dispatch`、`n_agentd_Manager_RelayAnswer`、`n_hostapi_Host_WakeHome`、`n_scheduling_Service_Admit`。Dispatch/RelayAnswer/WakeHome 的 flow 有序步骤可用。
- `flow n_agentd_Server_handleTaskDiff` 与 `flow n_agentd_Manager_handleQuestion` 返回 `degraded=true`、空 `steps`，所以 S1/S3 对这两个入口以源码为准，不拿 `chain` 冒充流程图。
- `sym n_executor_Profile` 实际失败：`Error: 符号 "n_executor_Profile" 不在图中（图未覆盖或名字有误）；近似候选: []`。五家 Profile 方法记为图覆盖债，以源码和 profile contract 测试为准。

### 1.3 冻结签名与跨 task Interfaces

下面是所有 task 共享的精确 Consumes；实现节点不得把“形状相近”的别名当成同一接口。

```go
type ticketRequest struct {
    Kind       string `json:"kind"`
    Permission string `json:"permission,omitempty"`
    Question   string `json:"question,omitempty"`
    QuestionID string `json:"question_id,omitempty"`
}

type RespondAskReq struct {
    TaskID     string
    TicketID   string
    QuestionID string
    Answer     string
}

type AskResponder interface {
    RespondAsk(context.Context, RespondAskReq) error
}

type ApprovalClient interface {
    PolicySnapshot(context.Context) (PolicySnapshot, error)
    Request(context.Context, ApprovalRequest) (ApprovalResult, error)
    Await(context.Context, ApprovalRef) (ApprovalDecision, error)
    Acknowledge(context.Context, ApprovalAck) error
}

type StartReq struct {
    Task        proto.Task
    PlanContent string
    TaskDir     string
    Env         []string
    Discipline  string
    Approval    ApprovalClient
}

type Adapter interface {
    Start(context.Context, StartReq) error
    Events(taskID string) <-chan AdapterEvent
    Send(context.Context, taskID, text string) error
    RespondPermission(context.Context, taskID, permID, decision, reason string) error
    Stop(taskID string) error
}

type ProfileFile struct {
    Name    string
    Content string
}

type ProfileReq struct {
    HomeDir     string
    Isolated    bool
    Credential  CredentialPolicy
    Rules       []ProfileFile
    Skills      []ProfileFile
    TaskOverlay []ProfileFile
}

type Profile interface {
    Inspect(context.Context, ProfileReq) (ProfileReport, error)
    Prepare(context.Context, ProfileReq) (ProfileReport, error)
    Verify(context.Context, ProfileReq) (ProfileReport, error)
}

func (m *Manager) Dispatch(ctx context.Context, req DispatchReq) (task *proto.Task, err error)
func (m *Manager) waitQuestion(ctx context.Context, taskID, ticketID string)
func (m *Manager) RelayAnswer(taskID, ticketID, answer string) error
func (m *Manager) NoteDeliveryFailed(taskID, ticketID string, cause error)
func (m *Manager) handleQuestion(ctx context.Context, taskID string, ev AdapterEvent)
func (a *Adapter) authorizeNativePermission(r *runState, ev executor.AdapterEvent)
func (a *Adapter) RespondPermission(ctx context.Context, taskID, permID, decision, reason string) error
func (m *Manager) assembleResultRef(ctx context.Context, task *proto.Task, repo, headRev string) (workspace.ResultRef, error)
func (s *Server) handleTaskDiff(http.ResponseWriter, *http.Request)
func (h *Host) WakeHome(ctx context.Context, req WakeRequest) (WakeReply, error)
func bindingFor(q Squad, c Carrier, req IgnitionRequest) Binding
```

S2 使用的既有能力解析也必须原样消费：

```go
type Provider interface {
    Name() string
    Report() CapabilityReport
}

func ProfileFromProvider(p Provider) (Profile, bool)
```

S3 的工作区边界不扩面：

```go
type ResultRef struct {
    Repo   string
    Branch string
    Commit string
    Path   string
}

type Capability interface {
    EnsureRepoUsable(context.Context, string) error
    ResolveBaseline(context.Context, string, string) (Baseline, error)
    Prepare(context.Context, PrepareReq) (Prepared, error)
    CreateManual(context.Context, string, string, ManualReq) (ManualTree, error)
    DiffRange(context.Context, string, string, string) (string, error)
    ReadFile(context.Context, string, string) (FileContent, error)
    RecycleManaged(context.Context, string, string) error
}
```

### 1.4 允许修改的文件总集

| 段 | 生产文件 | 测试/文档文件 | 明确不碰 |
| --- | --- | --- | --- |
| S1 | `internal/agentd/manager.go`、`internal/agentd/approval_client.go`、`internal/executor/opencode/adapter.go` | `internal/agentd/manager_test.go`、`approver_test.go`、`integration_test.go`、`resume_test.go`、`internal/executor/opencode/question_test.go`、`adapter_test.go` | `internal/proto` 只读；不改 `ApprovalClient`、`AdapterEvent`、事件枚举、store 旁路 |
| S2 | `internal/agentd/manager.go`、`internal/executor/profile.go`、`internal/executor/opencode/profile.go`、`claudecode/profile.go`、`grok/profile.go`、`codex/profile.go`、`agy/profile.go` | `internal/executor/profile_contract_test.go`、五家既有 `profile_test.go` | `internal/agentd/coordinator_home.go` 只作负向边界核对，不把协调者路径改成普通任务入口；不改五家 adapter 启动文件 |
| S3 | `internal/agentd/server.go` | `internal/agentd/diffbase_test.go`、`resultref_test.go`、`workspace_capability_test.go`、`server_test.go` | `internal/agentd/workspace_capability.go`、`internal/workspace/capability.go` 只读契约；不加 HTTP response 字段 |
| S4 | 无生产改动；若实现节点发现 `internal/hostapi/probe.go` 的现有语义退化，先停在 S0 回报，不自行扩大范围 | `internal/hostapi/probe_test.go`、`internal/executor/opencode/adapter_test.go` | 不把恒真 `reloadProbe` 当生产证据，不改跨机/CLI 探针协议 |
| S5 | 无生产改动：`internal/scheduling/scheduling.go` 只读 | `internal/keystone/slice_test.go` | 不改 `bindingFor`、准入、队列或物理身份 |
| S6 | 无运行时代码 | `docs/superpowers/specs/b233.3.md`、`docs/roadmap.md`、`skills/handoff/SKILL.md` | 不改 CLI/HTTP 命令语义，不加重试器或幂等键 |

## 2. DAG、最薄路径与协调者守门

```text
S0（边界/冻结物/图债核验；协调者执行，不派发）
├── S1（Ask QuestionID + permission delivery_failed；P1-B）
├── S2（StartReq.Discipline → Profile TaskOverlay + 五家 Verify；P2-A）
├── S3（ResultRef.Commit → DiffRange）
├── S4（WakeHome 真实输出证据）
├── S5（B350 物理身份竖切）
└── S6（R8 文档真话对账）
```

最薄可跑路径约束：S1 先沿已有 `handleQuestion → CreateTicket → waitQuestion/RelayAnswer` 写穿 JSON 失败测试，再补字段和读取；R4 先沿已有 `authorizeNativePermission → RespondPermission` 写一个真实 adapter 失败事件测试，再接 P1-B 回调。S2 先在已有 `Dispatch → StartReq` 装配点增加 Profile spy 的失败测试，再补 common Profile Prepare；不能只给 Profile 写文件而不让普通启动调用它。S3 先把 `handleTaskDiff → Capability.DiffRange` 的 spy 接到 HTTP 竖切，再改消费变量。S4/S5/S6 已有最薄路径：S4 既有 WakeHome 测试已绿、只清理假证据；S5 竖切已实际跑红；S6 是冻结文档句子对账。

S0 的边界检查、最终全量测试、真机清单和本计划的跨卡审计均由协调者执行，不派发给其它执行者。

## 3. Task S1：Ask 原生身份与 permission 投递失败统一可见

### 3.1 文件、Consumes 与 Produces

**精确文件集：**

- 生产：`internal/agentd/manager.go`、`internal/agentd/approval_client.go`、`internal/executor/opencode/adapter.go`。
- 测试：`internal/agentd/manager_test.go`、`internal/agentd/approver_test.go`、`internal/agentd/integration_test.go`、`internal/agentd/resume_test.go`、`internal/executor/opencode/question_test.go`、`internal/executor/opencode/adapter_test.go`。

**Consumes：** `ticketRequest` 当前三字段形状；`RespondAskReq`、`AskResponder`、四方法 `ApprovalClient`、`StartReq.Approval`、`Adapter.RespondPermission`、`Manager.NoteDeliveryFailed` 与既有 `proto.EventTypeDeliveryFailed`。

**Produces：**

```go
type ticketRequest struct {
    Kind       string `json:"kind"`
    Permission string `json:"permission,omitempty"`
    Question   string `json:"question,omitempty"`
    QuestionID string `json:"question_id,omitempty"`
}

// 仅在 taskApprovalClient 动态对象上增加可选能力；不把它加入 ApprovalClient。
func (c *taskApprovalClient) NoteDeliveryFailed(taskID, ticketID string, cause error)
```

`opencode` 包内仅定义结构断言所需的私有形状，不导出新接口：

```go
type deliveryFailureReporter interface {
    NoteDeliveryFailed(taskID, ticketID string, cause error)
}
```

该断言的动态实现是 Manager 注入的 `*taskApprovalClient`；如果测试注入的 client 没有该能力，测试必须明确失败，不能把“只打日志”当成功。

### 3.2 步骤与红绿锁缝

1. **基线判据先复核（2–5 分钟）。** 先运行 §1.1 的 agentd 定向命令，再运行：

   ```bash
   go test ./internal/agentd ./internal/executor/opencode -run 'TestQuestionReissueAfterAnswerCreatesNewTicket|TestQuestionReissue|TestOpenCodeWaitQuestionUsesRespondAsk|Test.*Permission' -count=1
   ```

   预期：基线已有 Ask/permission 测试通过；新增的 `QuestionID`/`delivery_failed` 断言尚不存在，不得把“没有测试”写成覆盖。

2. **写 Ask 接缝失败测试并跑红（2–5 分钟）。** 复用 `internal/agentd/manager_test.go` 现有 `TestQuestionReissueAfterAnswerCreatesNewTicket` 的 Manager、临时 store、hub 和 adapter harness；不新造 store。测试入口必须仍是 `m.handleQuestion`，而不是直接测试 `NamespacedTicketID`。将同一个非空 `QuestionID` 两次送入：第一次创建并回答原命名 ticket，第二次创建新 uuid ticket。逐条断言：

   ```go
   // 断言清单；照抄既有 TestQuestionReissueAfterAnswerCreatesNewTicket 的夹具变量名。
   // 1. 第二 ticket.ID != 第一 ticket.ID。
   // 2. 两张 ticket 的 Kind 都等于 "ask"。
   // 3. 第二张 ticket.Request 是合法 JSON。
   // 4. 第二张 ticket.Request.question_id == "question-native-1"。
   // 5. waitQuestion 取第二张 ticket 后调用 RespondAsk 的 QuestionID == "question-native-1"。
   // 6. RelayAnswer 取第二张 ticket 后调用 RespondAsk 的 QuestionID == "question-native-1"。
   // 7. 第一次未回答的同一 taskID:QuestionID 重放，ticket 数量不增加。
   // 8. 该未回答重放的 question 事件数量不增加。
   // 9. QuestionID 为空时 ticket.Request 的 question_id 字段缺失或为空，回程 QuestionID 必须为空。
   ```

   先只加断言再跑同一命令，基线必须因第 4–6 条失败而红；若 harness 不能让入口到达 waiter，按本节声明的既有 harness 调整等待栅栏，不把测试改成直喂 `RespondAsk`。

3. **实现 Ask 持久化读取（2–5 分钟）。** 在 `handleQuestion` 的首次 ticket 和“旧单已答另开 ticket”两个构造点都填 `QuestionID: ev.QuestionID`；`omitempty` 只让无原生 id 的情况保持旧 wire 形状。`waitQuestion` 收到回答后读取并反序列化该 ticket 的 `Request`，优先使用持久 `req.QuestionID`；只有该字段为空且 ticket id 确实是 `NamespacedTicketID(taskID, nativeID)` 时才使用 `NativeIDFromTicket`。`RelayAnswer` 采用同一优先级。未知/uuid ticket 不得把 uuid 当原生 QuestionID。

   每条 JSON 读取、ticket 读取、回程失败分支带 `task`、`ticket`、`question_id`（非空时）和 `cause` 的结构化日志；成功的持久化与回程也带 `question_id`，但不得记录完整回答正文。

4. **跑 Ask 绿并锁安全反例（2–5 分钟）。** 复跑 S1 agentd 定向命令；再补错 task、错 ticket owner、重复回答的断言：命令返回错误，ticket 内容与 question 事件数不改变。只跑 `internal/agentd` 触及测试，不跑全仓。

5. **写 permission 失败接缝测试并跑红（2–5 分钟）。** 复用 `internal/executor/opencode/adapter_test.go` 的 `newFakeServer`、`startFakeRun` 与既有 `fakeApprovalClient` 形状，真实入口为 `authorizeNativePermission` 触发的 permission 事件。注入一个会让 `RespondPermission` 返回指定错误的 fake transport，并让动态 approval object 记录 Ack。逐条断言：

   ```go
   // 失败用例的入口必须是 Adapter 处理原生 permission 事件，不得直接调用 NoteDeliveryFailed。
   // 1. ApprovalClient.Request/Authorize 已消费当前 task 的审批 client。
   // 2. RespondPermission 收到 taskID、原生 permissionID、"once" 或 "reject" 及 reason。
   // 3. delivery_failed 事件恰有既有 proto.EventTypeDeliveryFailed 类型。
   // 4. payload.ticket_id == taskID + ":" + permissionID。
   // 5. payload.reason 包含实际回传错误；构造超过 500 rune 的错误时 len([]rune(reason)) <= 500。
   // 6. payload.hint == 现有 NoteDeliveryFailed resume 指引。
   // 7. AckFormed 可以存在，但 AckDelivered 与 AckExecuted 都不存在。
   // 8. WaitDeliveryPolicy/isDeliverable 对该事件返回可交付，而不是吞掉事件。
   // 9. RespondPermission 成功的对照用例保留 AckDelivered，并且只有成功后才有它。
   ```

   基线必须在第 3、4、7 条至少一条上跑红；这条测试不能只测 `Acknowledge` helper 或 Manager 事件构造。

6. **实现 P1-B 失败回调（2–5 分钟）。** 在 `taskApprovalClient` 上增加上面的同名可选能力，它校验传入 task 与绑定的 `c.taskID` 相同后调用 `c.m.NoteDeliveryFailed(taskID, ticketID, cause)`。在 `authorizeNativePermission` 的决定已形成但 `RespondPermission` 返回错误分支调用该能力；`Authorize` 本身失败后尝试发送 reject 也要覆盖“reject 回传失败”的可见失败分支。若可选能力缺失，记录带 task/perm/ref 的 Error 并返回失败，不新发事件、不写 store。

   `AckDelivered` 和 `AckExecuted` 必须继续位于 `RespondPermission` 成功之后；回调失败不得向成功路径跳转。每个外部调用前后、每条 return 错误分支和成功回传均用项目 `slog` 结构化日志；不使用 `fmt.Println`/`log.Printf`。

7. **跑 permission 绿与真实序列化边界（2–5 分钟）。** 复跑 `go test ./internal/agentd ./internal/executor/opencode -run 'Test(B2337|Question|Permission|Delivery)' -count=1`。测试必须从真实 manager/adapter 入口取得原始 store/event JSON，再 `json.Unmarshal` `ticketRequest` 和 delivery payload；不能用两个互不相连的 struct 单测替代。

8. **补导出/非显然逻辑注释（2–5 分钟）。** 为 `ticketRequest.QuestionID` 写注释说明它是原生 id、uuid ticket 不能反推；为 `taskApprovalClient.NoteDeliveryFailed` 写参数、绑定 task 校验和“不扩展 ApprovalClient”的注意事项；为 adapter 的可选断言写为什么沿既有注入对象回传。现有方法头注释不得被删。

### 3.3 S1 验收、缺陷族与接缝双向覆盖

测试范围仅为 `internal/agentd`、`internal/executor/opencode` 的上述定向测试；S1 不跑全仓。

| 缺陷族 | 验收反问与结论 |
| --- | --- |
| 生命周期/中断 | Manager 重启后未答原生问题重放是否仍复用 ticket、已答再问是否仍可答；机内 ticket 持久化锁定，重启前后 executor pending 回收 **未验证，需真机**。 |
| 静默失败/误导 | permission 回传失败不能只落日志；`delivery_failed` payload 要含 task/ticket/reason/hint，AckDelivered 不得出现；事件追加失败后的外部恢复 **未验证，需真机**。 |
| 跨平台边界 | JSON、Manager 和 adapter 形状机内可验；OpenCode 原生 HTTP/SSE 在不同 OS/CLI 的真实回传 **未验证，需真机**。 |
| 假红/假绿 | 测试入口穿过 `handleQuestion` 和 `authorizeNativePermission`，并读取真实 JSON；仅测 helper、仅看 Ack 数量均不合格。 |
| 门禁绕过 | 错 task/owner、错误 ticket、审批 client 绑定和 `RespondPermission` 仍走既有门；adapter 不碰 store。并发回答与审批竞态 **未验证，需真机**。 |
| 序列化 | `ticketRequest.Marshal → Ticket.Request → Unmarshal → RespondAskReq.QuestionID` 和 `deliveryFailedPayload → Event.Payload → WaitDeliveryPolicy` 各有穿边界断言；缺失/空/非空 QuestionID 三态分开。 |
| 枚举/安全 | 只复用 `ask`、`question`、`delivery_failed`、既有 AckStage；reason 继续 500 rune 截断，回答正文不进日志。 |

接缝双向清单：

| 声明接缝 | 锁它的测试入口与断言 |
| --- | --- |
| `handleQuestion → ticketRequest JSON` | `TestQuestionReissueAfterAnswerCreatesNewTicket`（入口 `m.handleQuestion`），断言 question_id 缺失/空/非空和新旧 ticket。 |
| `waitQuestion → AskResponder.RespondAsk` | 既有/扩展 `TestOpenCodeWaitQuestionUsesRespondAsk`（入口 Manager waiter），断言持久 QuestionID。 |
| `RelayAnswer → AskResponder.RespondAsk` | S1 新增 manager 回程分支（入口 `m.RelayAnswer`），断言同一持久 QuestionID。 |
| `authorizeNativePermission → RespondPermission → NoteDeliveryFailed` | OpenCode adapter permission failure 竖切（入口 `authorizeNativePermission`），断言真实 event JSON 与 Ack 缺失。 |
| `NoteDeliveryFailed → WaitDeliveryPolicy` | 同一 S1 event 测试继续调用既有 delivery policy，断言 delivery_failed 可交付。 |

反向核对：上表五条每条至少一支 seam-level test；S1 所有新增测试入口均穿过表内接缝。内部 helper 测试只能附加，不能替代上述入口；本计划没有以内部锁替代声明缝。

## 4. Task S2：普通任务纪律进入 Profile 任务层并验证内容

### 4.1 文件、边界与精确生产装配

**精确文件集：**

- 生产：`internal/agentd/manager.go`、`internal/executor/profile.go`、`internal/executor/opencode/profile.go`、`internal/executor/claudecode/profile.go`、`internal/executor/grok/profile.go`、`internal/executor/codex/profile.go`、`internal/executor/agy/profile.go`。
- 测试：`internal/agentd/manager_test.go`、`internal/executor/profile_contract_test.go`、`internal/executor/opencode/profile_test.go`；五家共用已有 `profile_contract_test.go` table，OpenCode 的专有回归继续在其已存在的 `profile_test.go` 扩展，不创建不存在的家族测试文件。

**Consumes：** `Manager.Dispatch` 已有 `discText`、`proto.Task.HomeDir`、`executor.Adapter`、`executor.Provider`、`executor.ProfileFromProvider`、`StartReq.Discipline` 和五家既有 `Profile` 实现。

**Produces：** 在 `internal/agentd/manager.go` 的现有 `ad.Start` 前收口一个私有装配函数：

```go
func (m *Manager) prepareTaskProfile(ctx context.Context, start executor.StartReq) error
```

函数行为必须精确为：

```go
start := executor.StartReq{
    Task:        *task,
    PlanContent: string(planContent),
    TaskDir:     taskDir,
    Env:         startEnv,
    Discipline:  discText,
    Approval:    approval,
}
if err := m.prepareTaskProfile(ctx, start); err != nil {
    // 记录 task failed，带 task/executor/home/discipline_present/cause；不调用 adapter.Start。
}
if err := ad.Start(ctx, start); err != nil {
    // 沿既有 adapter start 错误路径。
}
```

`prepareTaskProfile` 只接受 `StartReq.Discipline` 这一份权威正文，构造：

```go
executor.ProfileReq{
    HomeDir:    start.Task.HomeDir,
    Isolated:   start.Task.HomeDir != "",
    TaskOverlay: []executor.ProfileFile{
        {Name: filepath.Join(start.Task.ID, "discipline.md"), Content: start.Discipline},
    },
}
```

其中 `TaskOverlay` 仅在 `strings.TrimSpace(start.Discipline) != ""` 时出现；空纪律返回 nil，不伪造 prepared overlay。非空纪律但 `HomeDir == ""` 或 adapter 不是 `executor.Provider`/没有 Profile 能力时，返回带 task/executor/home 的明确错误并沿 Dispatch 的失败状态路径，不调用 `Start`，不能静默跳过。`start.Task.ID` 是任务命名空间；Profile 现有 `..` 文件名防线继续生效，两个任务即使正文或 basename 相同也落在不同子目录。`discipline.md` 只表示通用任务层载荷；“不覆盖生产规则名”的断言必须使用各 Profile 真实 `RulesRelFile` basename，不能用它代替生产规则 fixture。

五家 Profile 的 `Prepare` 保持现有原生规则/skills 分支，TaskOverlay 一律写 `filepath.Join(req.HomeDir, ".handoff", "task-overlay", filepath.Clean(o.Name))`，不得把 overlay 路径重定向到 `RulesRelFile`。五家 `Verify` 统一增加任务层内容验证：

- `req.TaskOverlay` 为空时保留既有“全局规则存在即可报告规则已验证”的兼容语义；若 `req.Rules` 有内容，则比较全局文件字节。
- `req.TaskOverlay` 非空时，逐个读取对应 `.handoff/task-overlay/` 文件并与 `ProfileFile.Content` 按字节比较；任一缺失、读错或内容不符都必须 `Verified=false` 并将具体相对路径放入 `Missing` 或 `Notes`，不能因全局规则存在而变 true。
- `req.TaskOverlay` 全部正确且全局规则存在/内容正确时才 `Verified=true`；`EngineOK` 仍由真实引擎决定，机内不得置 true。

五个真实规则相对路径必须按当前生产常量核对：

```text
opencode  .config/opencode/AGENTS.md          basename AGENTS.md
claude    .claude/CLAUDE.md                   basename CLAUDE.md
grok      .grok/AGENTS.md                     basename AGENTS.md
codex     .codex/AGENTS.md                    basename AGENTS.md
agy       .gemini/antigravity-cli/AGENTS.md   basename AGENTS.md
```

### 4.2 步骤与红绿锁缝

1. **基线判据先复核（2–5 分钟）。** 先跑 §1.1 executor/agentd 定向命令，再跑：

   ```bash
   go test ./internal/executor/... ./internal/agentd -run 'TestProfilesVerifyExistingRules|TestProfilePrepareTaskOverlayDoesNotOverwriteGlobalRules|TestDispatchCallsCapability' -count=1
   ```

   预期：既有 profile contract 通过；普通 Dispatch 没有 Profile TaskOverlay 生产调用，新的 Manager 接缝测试还不存在。

2. **写普通 Dispatch seam 失败测试并跑红（2–5 分钟）。** 在 `internal/agentd/manager_test.go` 复用现有 `newTestManagerWithApprover`、项目/工作区 fixture 和 fake adapter 结构，令 fake adapter 同时实现现有 `Provider` 与 `Profile()` accessor，并让 profile spy 记录每次 `Prepare(ProfileReq)`。测试入口为 `m.Dispatch`，不是直接调用 profile。逐条断言：

   ```go
   // 1. DispatchReq.DisciplineText 为非空正文，Manager 形成 StartReq.Discipline 同字节正文。
   // 2. Profile.Prepare 恰调用一次，ProfileReq.TaskOverlay 非空且只有任务正文载荷。
   // 3. ProfileReq.TaskOverlay[0].Content 与 StartReq.Discipline 按 []byte 完全相等。
   // 4. ProfileReq.HomeDir == 绑定 task.HomeDir，Name == filepath.Join(task.ID, "discipline.md")。
   // 5. fake adapter Start 收到的 Discipline 与 ProfileReq.TaskOverlay[0].Content 相等。
   // 6. Discipline/DisciplineText 都空时 Profile.Prepare 不被调用，不产生空 overlay。
   // 7. 非空纪律但无 HomeDir/Profile capability 时 Dispatch 返回错误、任务不调用 Start，且错误含 task/executor/home。
   ```

   基线必须因第 2–5 条失败而红；若当前 fake adapter 不具 Provider 形状，测试只在本文件新增一个记录 Profile capability 的同构 fake，不改 `executor.Adapter` 公共接口。

3. **实现 common Profile 装配（2–5 分钟）。** 在现有 `ad.Start` 之前先创建 `StartReq`，调用 `prepareTaskProfile(ctx, start)`；用 `executor.ProfileFromProvider` 解析 profile，不在五家 adapter 的 `Start` 中重复读取纪律。成功路径记录 task/executor/home/overlay_count/content_bytes；空纪律记录 `overlay_skipped=true`；Profile lookup、Prepare 和 start 前后的每条错误/成功分支用结构化 logger，禁止 print。

4. **跑普通 Dispatch 绿（2–5 分钟）。** 复跑 S2 Manager 定向测试；再跑既有 `TestDispatchUnknown`、`TestDispatchCallsCapability` 等只触及 agentd 的回归。确认 profile Prepare 失败时不会调用 adapter.Start，也不会把 ProfileReport.Prepared=false 当成功。

5. **写五家 Profile 内容失败测试并跑红（2–5 分钟）。** 在 `internal/executor/profile_contract_test.go` 将现有五家 table 扩成同一 harness。每家 `RulesRelFile` 由该包真实常量提供，任务 overlay 名使用 `filepath.Join("task-a", filepath.Base(RulesRelFile))` 和 `filepath.Join("task-b", filepath.Base(RulesRelFile))`，禁止 `task-1.md`/`task-2.md`。每家断言逐条如下：

   ```go
   // 1. Prepare 以 globalContent 写 RulesRelFile，以 taskA/taskBContent 写两个任务层文件。
   // 2. 读取磁盘字节：两份 overlay 内容分别精确等于 taskA/taskBContent。
   // 3. 读取磁盘字节：RulesRelFile 仍精确等于 globalContent。
   // 4. Verify(req 只有 Rules、没有 TaskOverlay) 保留既有全局规则存在语义。
   // 5. Verify(req 含 Rules + TaskOverlay，但删 taskA 文件) 返回 err=nil 且 Verified=false。
   // 6. Verify(req 含 Rules + TaskOverlay，但把 taskA 改成 wrong bytes) 返回 err=nil 且 Verified=false。
   // 7. Verify(req 含 Rules + TaskOverlay，只有全局文件而无 taskA) 仍 Verified=false。
   // 8. Verify(req 含 Rules + TaskOverlay，所有全局/任务文件内容正确) Verified=true、EngineOK=false。
   // 9. taskA/taskB 使用同一 basename 时，读取彼此路径不得得到对方正文。
   // 10. task overlay 的真实路径前缀必须是 HomeDir/.handoff/task-overlay/，不能等于任何 RulesRelFile 路径。
   ```

   先运行 `go test ./internal/executor -run '^TestProfilesVerify' -count=1` 和五家 profile 定向命令；基线必须让第 5–8 条至少一条失败，因为当前 Verify 只 Stat 全局规则。

6. **实现五家 Verify 内容检查、保留 Prepare 隔离（2–5 分钟）。** 在五家 `profile.go` 的 `Verify` 中沿各自已有 logger/错误风格读取预期 Rules 与 TaskOverlay。新增的路径错误、读取错误、字节不符和 success 都要含 harness/home/relative path/expected-vs-actual state；Prepare 的写失败保持 `Prepared=false`/error。每个导出 `Prepare`/`Verify` 方法补参数、返回值和 `EngineOK` 不代表真实引擎的注释；非显然的“任务层与全局规则同名仍走 `.handoff`”写 why 注释。

7. **跑五家绿并做 coordinator_home 负向核对（2–5 分钟）。** 运行：

   ```bash
   go test ./internal/executor/... -run 'TestProfiles|TestProfile|Test.*Overlay' -count=1
   go test ./internal/agentd -run 'TestCoordinatorHome|TestDispatch|TestB2337' -count=1
   ```

   检查 `internal/agentd/coordinator_home.go` 中 `coordinatorHomeSupplier.Prepare` 仍只传 `Rules`/`Skills`，没有被测试标成普通任务 TaskOverlay 的生产证据。五家 Profile 不调用 `WakeHome`/`ProbeHome`。

8. **补文件头/导出注释（2–5 分钟）。** `internal/executor/profile.go` 的职责边界补明 TaskOverlay 是任务目录输入；五家修改的 `Prepare`/`Verify` 注释分别说明 `HomeDir`、规则/任务层内容、返回报告与真实 EngineOK 限制；Manager 私有 helper 注释说明唯一正文来源是 `StartReq.Discipline`，不得从本地纪律目录回退。

### 4.3 S2 验收、缺陷族与接缝双向覆盖

测试范围仅为 `internal/agentd`、`internal/executor` 及五家 profile 子包的上述定向命令；不把全仓测试归给 S2。

| 缺陷族 | 验收反问与结论 |
| --- | --- |
| 生命周期/中断 | Profile Prepare 中断/重启后半写文件与任务目录回收 **未验证，需真机**；机内要保证失败不返回 Prepared/Verified。 |
| 静默失败/误导 | 非空纪律无 Profile/Home 必须显式失败；缺失/错误 overlay 必须非 Verified，日志指出路径；生产 CLI 用户提示 **未验证，需真机**。 |
| 跨平台边界 | 五家 Go 路径和字节判据机内可验；HOME 权限、大小写、Windows 分隔符与真实 CLI 读取 **未验证，需真机**。 |
| 假红/假绿 | Manager seam 必须捕获 ProfileReq；五家测试读真实磁盘字节、使用真实规则 basename、断言全局不变和同名任务隔离；只 Stat 文件不合格。 |
| 门禁绕过 | 普通任务只经既有 Profile capability 与任务 HOME，不把纪律写全局 RulesRelFile；Profile 不支持时不得启动。并发 Prepare 的 TOCTOU **未验证，需真机**。 |
| 序列化 | `StartReq.Discipline → ProfileReq.TaskOverlay.Content → os.WriteFile → Verify os.ReadFile` 为一条真实边界；空/缺失/非空三态与 Unicode/换行按 `[]byte` 断言。 |
| 枚举/安全 | 不新增 overlay/task/event 枚举；任务名子目录隔离、`..` 继续拒绝，纪律正文不写进全局规则与普通日志。 |

接缝双向清单：

| 声明接缝 | 锁它的测试入口与断言 |
| --- | --- |
| `Manager.Dispatch → StartReq.Discipline` | Manager Dispatch seam test，入口 `m.Dispatch`，断言 StartReq 字节。 |
| `StartReq.Discipline → Profile.Prepare(ProfileReq.TaskOverlay)` | 同一 Manager seam test，spy 入口为 Profile capability，断言非空/空两态及 Name/HomeDir。 |
| 五家 `Profile.Prepare → .handoff/task-overlay` | `TestProfiles...` table 通过五家具体 Profile，读盘断言真实路径和内容。 |
| 五家 `Profile.Verify → 任务层内容` | 同一 table 的缺失/错误/全局-only/正确四态。 |
| TaskOverlay 与 `RulesRelFile` 隔离 | OpenCode 既有 profile 测试改写 + 五家 table，入口仍 Profile.Prepare，断言生产 basename 同名全局内容不变。 |

Coordinator HOME 是负向边界，不作为任何正向 seam test；这不是内部锁替代声明缝。

## 5. Task S3：ResultRef.Commit 驱动任务 diff

### 5.1 文件与接口

**精确文件集：**

- 生产：`internal/agentd/server.go`。
- 只读生产契约：`internal/agentd/workspace_capability.go`、`internal/workspace/capability.go`。
- 测试：`internal/agentd/diffbase_test.go`、`resultref_test.go`、`workspace_capability_test.go`、`server_test.go`。

**Consumes：**

```go
func (m *Manager) assembleResultRef(ctx context.Context, task *proto.Task, repo, headRev string) (workspace.ResultRef, error)
func (s *Server) handleTaskDiff(w http.ResponseWriter, r *http.Request)
func (c *countingCap) DiffRange(ctx context.Context, repo, base, head string) (string, error)
```

**Produces：** `handleTaskDiff` 内部保留 `workspace.ResultRef` 局部值并选择 `head`：

```go
ref, err := s.mgr.assembleResultRef(r.Context(), task, repo, headRev)
if err != nil {
    // 既有 500 JSON，带 task/repo/headRev/cause。
}
head := headRev
if ref.Commit != "" {
    head = ref.Commit
} else {
    s.log.Warn("ResultRef 缺少准确 commit，按任务分支降级", "task", taskID,
        "repo", repo, "head_rev", headRev, "result_path", ref.Path)
}
diff, err := s.mgr.Workspace().DiffRange(r.Context(), repo, base, head)
```

如果 `ref.Path == ""`，不得因此阻断准确 commit；不得把 `ref.Commit` 加到 `map[string]string{"diff": diff}` response。

### 5.2 步骤与锁缝

1. **基线判据先复核（2–5 分钟）。** 运行 §1.1 agentd 定向命令，并运行：

   ```bash
   go test ./internal/agentd -run 'TestTaskDiff|TestAssembleResultRef|TestDispatchCallsCapability' -count=1
   ```

   预期既有 helper/fallback 通过，但当前 `handleTaskDiff` 丢弃 ref；新增 HTTP/Capability seam 先不存在。

2. **写 HTTP seam 失败测试并跑红（2–5 分钟）。** 复用 `internal/agentd/server_test.go` 的 Server/Manager/HTTP fixture 与 `countingCap` 结构；将 workspace capability 包装为记录 `repo/base/head` 的 spy，并让 `assembleResultRef` 取得一个真实合法 40 位 commit C。入口必须是 HTTP `GET /api/tasks/{id}/diff?base=...` 或现有 server handler 的等价请求，不可直接测 helper。逐条断言：

   ```go
   // 1. HTTP 状态仍为 200（fixture 的 base/repo/branch 合法）。
   // 2. spy 的唯一 DiffRange 调用收到 head == commitC，逐字节相等。
   // 3. spy 收到的 repo/base 未被改写。
   // 4. response JSON 只有既有 diff 字段；不含 commit、path、result_ref。
   // 5. ref.Path 为空而 ref.Commit == commitC 时仍调用 DiffRange(commitC)。
   // 6. rev-parse/assemble 只得到空 commit 时，DiffRange head == 原 headRev。
   // 7. 空 commit fallback 日志含 task、repo、head_rev 与降级原因字段。
   ```

   基线必须在第 2 或第 5 条跑红；测试不得只调用 `assembleResultRef`。

3. **实现 ResultRef 消费（2–5 分钟）。** 在现有 manager 非空检查之后组装 ref；准确 commit 用作 `DiffRange` head，空 commit 才使用原 `headRev`。保留既有 repo/worktree/branch 校验与 HTTP status 分支；如为获取 ref 调整判断顺序，必须保持非法 base 仍 400、workspace 不可用仍 503、DiffRange 普通错误仍 500。每次 assemble、fallback、DiffRange 前后及错误分支加结构化日志；不要把 commit 写入 HTTP 响应。

4. **跑 S3 绿并锁 fallback（2–5 分钟）。** 运行 `go test ./internal/agentd -run 'Test(TaskDiff|AssembleResultRef|ResultRef)' -count=1`。Spy 必须只看到一次 `DiffRange`；非空 commit 与空 commit 两个分支分别锁定，Path 空不成为伪造错误。

5. **补注释（2–5 分钟）。** 在 `handleTaskDiff` 的 ref/head 选择处写 why：`ResultRef.Commit` 是同一次结果组装的权威 head；空 commit 才按原分支降级；Path 允许在 managed worktree 回收后为空。不得修改 workspace contract 注释。

### 5.3 S3 验收、缺陷族与接缝双向覆盖

测试范围仅 `internal/agentd` 指定 diff/resultref/server 测试；不跑全仓。

| 缺陷族 | 验收反问与结论 |
| --- | --- |
| 生命周期/中断 | worktree 回收、commit 可读而 Path 为空由机内 fixture 锁；真实回收/重启后 commit 仍可读 **未验证，需真机**。 |
| 静默失败/误导 | 非空 commit 被丢弃必须被 spy 抓出；空 commit fallback 有日志；错误 HTTP status 保持既有分支。 |
| 跨平台边界 | Go 参数与 JSON 机内可验；真实 git、权限、Windows worktree **未验证，需真机**。 |
| 假红/假绿 | 入口必须穿 HTTP/Server/Capability；不接受只测 `assembleResultRef` 的内部锁。 |
| 门禁绕过 | 沿既有 Server/Workspace 路径，不开读仓旁路；worktree check 与 diff action 的竞态 **未验证，需真机**。 |
| 序列化 | HTTP request/response 真实编码断言 response 不新增 ResultRef；`Commit` 非空、空串与 Path 空分别进入 spy。 |
| 枚举/安全 | 不新增 diff/event/state 枚举；commit 只作参数，不拼 shell 字符串或外发字段。 |

接缝双向清单：

| 声明接缝 | 锁它的测试入口与断言 |
| --- | --- |
| `handleTaskDiff → assembleResultRef` | HTTP diff seam test，入口 server handler，断言返回 ref 被保留。 |
| `ResultRef.Commit → Capability.DiffRange.head` | 同一 HTTP test 的 spy，断言 head 精确为 C。 |
| 空 commit → 原 headRev fallback | 同一 test table 的第二 case，断言 headRev 与降级日志。 |
| `Path == ""` 允许 | 同一 test 的准确 C/空 Path case。 |
| HTTP response shape | 同一 test 解码 JSON，断言只有 diff。 |

## 6. Task S4：WakeHome 真实探测证据与假证据清理

### 6.1 文件、接口与步骤

**精确文件集：** `internal/hostapi/probe_test.go`、`internal/executor/opencode/adapter_test.go`；`internal/hostapi/probe.go` 只读核对，现有生产语义不改。

**Consumes：**

```go
func (h *Host) WakeHome(ctx context.Context, req WakeRequest) (WakeReply, error)
func (h *Host) runWake(ctx context.Context, req WakeRequest, targetHome string) (WakeReply, error)
```

1. **基线判据先复核（2–5 分钟）。** 运行已实际通过的三条 hostapi 定向测试；并运行 `go test ./internal/executor/opencode -run 'TestApplySnapshot|TestProbe' -count=1`。预期 WakeHome 四态已有真实输出/凭据反例通过。

2. **改写假 probe 测试并跑红/绿（2–5 分钟）。** 将 `TestApplySnapshotInjectedProbeSuccess` 中的恒真闭包：

   ```go
   r.reloadProbe = func(ctx context.Context) (bool, error) {
       return true, nil
   }
   ```

   改成读取刚写入 `taskDir/configFileName` 的 JSON，只有 `ExternalDirectory == "ask"` 且 `Bash["*"] == "ask"` 时返回 true；读文件、解析 JSON、状态不符均返回 false/error。测试注释说明这只是配置写入的 observable probe，不是跨机真实引擎 ready 证据。这样测试不会用 `return true` 代替观察结果。

3. **保留/扩展 WakeHome 反面断言（2–5 分钟）。** `TestWakeHomeReadyRequiresTurnOutputNotCredFile` 继续用真实 fake CLI 的错误/空 output + 已存在 credential 文件，断言不得 ready；非空 RunTurn output 才 ready；错误和空输出分别保持既有 `NeedLogin`/`Unreachable` 结果。日志断言包含 CLI/home/turn output state，测试只使用结构化 logger，禁止 print。

4. **生产 basename 静态核对（2–5 分钟）。** 在 S2 五家 profile contract 的实际规则 basename 测试通过后，运行 `rg -n 'return true|reloadProbe|task-1\.md|task-2\.md' internal/hostapi internal/executor/opencode internal/executor/profile_contract_test.go`；命中恒真 probe 或无关假名都判计划失败，配置读取闭包与生产 `RulesRelFile` basename 不判为恒真证据。

5. **补测试注释（2–5 分钟）。** 为四态 WakeHome fixture 和 observable config probe 写职责/边界注释：凭据文件是输入条件，不是 ready 证据；`EngineOK`/跨机 CLI 仍列真机。运行 `go test ./internal/hostapi ./internal/executor/opencode -run 'Test(WakeHome|ApplySnapshot|Probe)' -count=1`。

### 6.2 S4 验收与接缝覆盖

测试范围只跑 `internal/hostapi`、`internal/executor/opencode` 的上述测试；不改生产探针。

缺陷族结论：生命周期中止/残留 ready、不同 OS/真实 webview、跨机 CLI 回收均 **未验证，需真机**；静默失败由空/错误 output 反例锁；假绿由禁止恒真闭包和真实文件/回合输出锁；门禁由 WakeHome 既有 credential/occupied 分支保留；序列化只涉及 observable config JSON，必须读取真实文件；不新增枚举或事件。

接缝双向清单：`Host.WakeHome → runWake/RunTurn output` 由 `TestWakeHomeReadyRequiresTurnOutputNotCredFile` 锁；`ApplySnapshot → probeServeReloadsConfig` 由改写后的 observable config test 锁。S4 不把 `reloadProbe` 注入 helper 当生产 WakeHome 证据。

## 7. Task S5：B350 竖切与物理身份

### 7.1 文件、接口与步骤

**精确文件集：** `internal/keystone/slice_test.go`；只读 `internal/scheduling/scheduling.go#bindingFor`。

**Consumes：**

```go
func bindingFor(q Squad, c Carrier, req IgnitionRequest) Binding
func (s *Service) Admit(req IgnitionRequest) (Binding, error)
```

1. **基线判据先复核（2–5 分钟）。** 运行已实际跑红的：

   ```bash
   go test ./internal/keystone/ -run '^TestIgnitionVerticalSlice$' -count=1 -v
   ```

   原始失败必须仍是请求 `Executor: claude` 被错误期望成绑定 executor 的旧断言；不要改生产 `bindingFor`。

2. **写最小断言修复并跑绿（2–5 分钟）。** 在 `TestIgnitionVerticalSlice` 把期望改为：carrier `opencode-1`、physical target `linux-01`、`Binding.Executor == "opencode"`、`Binding.HomeDir == "/home/coordinator/.opencode-home"`；请求 `Model` 覆盖仍按既有测试规则单独断言。删除/改写“执行者覆盖 claude”的旧注释，使它明确请求 Executor 不得覆盖物理 CLI。入口仍是完整 Keystone → scheduling → `Admit` 竖切，不增加直接 `bindingFor` 内部锁。

3. **补注释与日志核对（2–5 分钟）。** 只在测试注释说明物理身份来自 carrier、模型可覆盖；不改生产 logger/调度。运行 `go test ./internal/keystone/ -run '^TestIgnitionVerticalSlice$' -count=1 -v`，预期 PASS。

### 7.2 S5 验收与接缝覆盖

测试范围仅 `internal/keystone` 该竖切及必要的 `internal/scheduling` 定向测试；不跑全仓。

缺陷族结论：准入生命周期/槽位释放和跨机 physical machine **未验证，需真机**；误导由反面断言请求 executor 不生效锁；假绿由完整竖切入口锁；门禁由 `Admit` 真实计数路径锁；序列化不新增字段但必须保留既有 carrier identity；不改 enum/安全边界。

接缝双向清单：唯一声明缝 `Admit → bindingFor/IdentityOf(carrier)` 由 `TestIgnitionVerticalSlice` 的完整入口锁；测试入口不直接调用内部 `bindingFor`。反向无未锁缝。

## 8. Task S6：R8 文档真话对账，不增加运行时重试

### 8.1 文件、步骤与精确句子结果

**精确文件集：** `docs/superpowers/specs/b233.3.md`、`docs/roadmap.md`、`skills/handoff/SKILL.md`。没有运行时代码或新增接口。

**Consumes：** `internal/executor/opencode/api.go#API.do` 的“一次 `httpClient.Do(req)`”、既有 `reply`/`resume` 说明，以及 `docs/superpowers/specs/b233.3.md` 当前“写请求重试必须幂等（重复派发仍是一次任务）”和“模拟超时重试仍单任务”原文。

1. **基线判据先复核（2–5 分钟）。** 运行：

   ```bash
   rg -n -C 3 '写请求重试|模拟超时重试|未用真重试器验单任务|reply 报 502|resume' docs/superpowers/specs/b233.3.md docs/roadmap.md skills/handoff/SKILL.md
   ```

   预期实际命中旧承诺：`b233.3.md:52` 的“写请求重试必须幂等（重复派发仍是一次任务）”、`:87` 的“模拟超时重试仍单任务”、`roadmap.md:85` 的“未用真重试器验单任务”和 skill 的 reply 502→resume 行。

2. **改三处文档并做句子级绿（2–5 分钟）。**

   - `docs/superpowers/specs/b233.3.md:52` 改成：一元写请求由调用方决定是否再次发起；网络失败后先查任务状态；本说明不保证重新派发仍映射到同一个远端任务。保留“先落请求再通知/游标补拉”的既有语义。
   - `docs/superpowers/specs/b233.3.md:87` 改成“写请求失败处置（派发入口）：先查任务状态，再由协调者决定是否重新派发；不承诺重派同任务”，删除“模拟超时重试仍单任务”的承诺，不添加幂等键/重试器测试。
   - `docs/roadmap.md:85` 把“未用真重试器验单任务”改为“未实现自动重试器；本卡只保留一次 HTTP 调用与失败后 status-first 处置说明”，不把未验证写成已保证。
   - `skills/handoff/SKILL.md` 在现有排障表附近新增派发失败条目：网络/502 后先用 `handoff tasks/show` 核对本地与远端任务，再决定 `dispatch`；不要先按“同一任务”假设重复派发。保留既有“reply 报 502 → resume”行，不把 reply delivery_failed 改成 dispatch 重试。

3. **跑文档对账与注释核对（2–5 分钟）。** 运行：

   ```bash
   rg -n '写请求重试必须幂等|模拟超时重试仍单任务|重试仍是一次任务|未用真重试器验单任务' docs/superpowers/specs/b233.3.md docs/roadmap.md skills/handoff/SKILL.md
   rg -n -C 2 '先查任务状态|重新派发|同一个远端任务|reply 报 502|resume' docs/superpowers/specs/b233.3.md docs/roadmap.md skills/handoff/SKILL.md
   ```

   第一条命令不得命中旧承诺；第二条命令必须命中 status-first、无同任务保证和既有 resume 处置。输出只作文档证据，不写成运行时重试已验证。

4. **补文档边界注释（2–5 分钟）。** 在三处修改附近明确：R8 不修改命令语义、不新增 transport retry、不承诺远端 idempotency；对 `delivery_failed` 的用户动作仍是 `resume <task>`。不写“已通过真机”或其它未执行结论。

### 8.2 S6 验收与接缝覆盖

测试范围只跑上述 `rg` 文档对账和 `git diff --check`；S6 不跑 Go 全量测试。

缺陷族结论：生命周期/远端孤儿、跨机网络、真实 502 后本地/远端状态均 **未验证，需真机**；静默误导由旧保证删除与 status-first 句子锁；假绿不能用文档命中证明运行时幂等；门禁保留 `resume`/`tasks` 先查路径；无新增 JSON/enum/安全载荷。

接缝双向清单：文档入口 `b233.3` retry semantics、`roadmap` deferred fact、`skills/handoff` troubleshooting action 各由第二条 `rg` 命中；没有测试标注为内部 runtime seam。不得把 `API.do` 单测写成 S6 的实现证据。

## 9. S0 收口：协调者执行，不派发

S0 不是实现 task，下面的命令由协调者在所有 S1–S6 完成后执行；本计划明确标注“本 task 由协调者执行，不派发”。

1. **范围守门：** `git diff --name-only 497a7712..HEAD` 的新增/修改生产、测试、文档文件必须全部属于 §1.4；出现集合外路径则判 fail，不以同目录放宽。
2. **静态占位符扫描：**

   ```bash
   rg -n 'TBD|TODO|占位|同 Task|加适当的错误|待拍板' docs/superpowers/plans/b233.7-plan.md
   ```

   预期无实现占位符。例外声明：本计划仅在“跨卡审计”一节使用“待拍板”一词表示单上下文自审按纪律必须交协调者复核；它不是实现缺口。测试 harness 代码块引用既有夹具的地方也在本计划 §10 明确逐条列出断言，因此不属于骨架占位。

3. **格式/路径/图核验：** `git diff --check`；对本计划中能被当前图命中的入口运行 `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/plans/b233.7-plan.md`。若 resolve 失败，保留原始错误并只修正坏锚，不把失败写成通过；`n_executor_Profile` 等已知图覆盖债继续以源码核对。
4. **定向验收集合：** S1–S5 按各节命令跑触及包；S6 跑文档命令；全量 `go test ./...`、`go build ./...` 和 `go test ./cmd -run '^TestRepoContractGate$'` 只作为协调者最终收口，不归属单个 task。图门禁若仍输出基线 dead-contract/dead-interface，贴原文并标为既有欠账。
5. **真机收尾清单：** 下面项目没有机内结果就不能写 pass：

   - OpenCode 跨真实执行机/CLI 的同一 native QuestionID：已答再问可答、wait 与 resume 都回到原问题；重启前后未答重放不重复。
   - OpenCode native permission 决定成功/回传失败各一次：真实 `delivery_failed` 可在 `wait`/事件流看到，失败无 delivered ack，`resume <task>` 可采取行动。
   - 五家真实 executor 启动读取任务层纪律：同一 carrier 两个并行任务使用相同生产 basename/不同 task namespace，正文互不污染且全局 RulesRelFile 不变；跨 OS/权限至少抽查一次。
   - managed worktree 回收或重启后，任务 diff 使用准确 ResultRef.Commit；空 Path 不误阻断，空 commit fallback 有日志。
   - 每个支持 CLI 的 WakeHome 真探测：凭据文件 alone 不 ready，真实非空回合输出才 ready；进程杀死/重启不遗留假 ready。
   - B350 真实载体物理机器/CLI/HOME 与请求覆盖冲突时仍取 carrier；模型覆盖保持既有允许语义。
   - 真实网络失败/502 后先查本地与远端任务状态，再决定是否重派；不把重派解释成同远端任务；reply `delivery_failed` 仍走 resume。

## 10. 五项计划自审

### 10.1 Spec 故事逐条归属

| 用户故事/冻结范围 | 具体归属 |
| --- | --- |
| 提问对得上原题 | S1 步骤 2–4：`handleQuestion`、真实 ticket JSON、waitQuestion、RelayAnswer。 |
| 批了但没送到可见 | S1 步骤 5–7：`authorizeNativePermission`、P1-B callback、delivery_failed 与 Ack 反断言。 |
| 这一票纪律在这一票上 | S2 步骤 2–7：Manager StartReq、五家 Profile Prepare/Verify、全局规则/任务层隔离。 |
| diff 对那次结果 | S3 步骤 2–4：HTTP → assembleResultRef → Capability spy。 |
| 准备好必须是真探测 | S4 步骤 2–4：WakeHome 真实 output、observable config probe、恒真证据排除。 |
| 载体身份不可被请求覆盖 | S5 步骤 2：完整 Keystone 竖切。 |
| 派发失败不自动再来一次 | S6 步骤 2–3：三处文档句子与既有 resume。 |
| R7 跨机行为 | 只在 §9.5 真机清单；contract #52 明确不计本卡机内 acceptance。 |

### 10.2 序列化边界逐条对照

| 数据 | 每处手写投影/序列化 | 必须穿过的断言 |
| --- | --- | --- |
| QuestionID | `ticketRequest` JSON marshal、Ticket.Request 持久化、wait/Relay unmarshal、`RespondAskReq.QuestionID` | S1 同一入口区分字段缺失、空串、非空原生 id；已答新 uuid 仍回原 id。 |
| delivery_failed | `deliveryFailedPayload` JSON、`AppendEvent` event payload、WaitDeliveryPolicy 消费 | S1 读取真实 event JSON，检查 ticket/reason/hint 和可交付；错误原因按 rune 截断。 |
| Discipline | `DispatchReq/DisciplineText` → Manager `StartReq.Discipline` → `ProfileReq.TaskOverlay.Content` → 文件字节 → Verify 读回 | S2 Manager seam + 五家真实 Prepare/Verify，空/缺失/非空与换行 Unicode 逐字节相等。 |
| ResultRef | `assembleResultRef` 返回结构 → Server head 投影 → `Capability.DiffRange` 参数；HTTP response map | S3 spy 断言 C 精确传递、空 commit fallback、response 没有新字段、Path 空不阻断。 |
| R8 | 无运行时序列化边界 | 只做文档 literal 对账，不虚构 wire 行为。 |

### 10.3 缺陷族总审

S1–S6 已在各自验收栏逐族回答 lifecycle、silent/misleading、cross-platform、false-green、gate-bypass、serialization、enum/security。所有真实 CLI、跨机、worktree 回收和重启窗口均显式写 **未验证，需真机**；没有将机内 fake 结果扩大为真机 pass。

为避免“枚举/安全”合写掩盖遗漏，按八个缺陷族逐列复核如下；每格都是本计划相应 task 验收栏中的结论摘要：

| 段 | 生命周期/状态机中断 | 静默失败/误导报错 | 跨平台假设 | 假红/假绿测试 | 门禁绕过 | 序列化边界 | 枚举新值过既有白名单 | 承重安全属性 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S1 | 重启重放机内锁；旧 goroutine/pending request 回收未验证，需真机 | delivery_failed 不得只打日志；payload/action 可见 | OpenCode HTTP/SSE 跨 OS 未验证，需真机 | 入口穿 Manager/adapter/真实 JSON，不接受 helper-only | owner/审批绑定/RespondPermission 仍过既有门 | ticketRequest、event payload、RespondAskReq 三态/截断穿边界 | 只复用 ask/question/delivery_failed/AckStage | 新 ticket 可答、未答重放去重、失败无 Delivered 各能使错误实现变红 |
| S2 | 半写 overlay、重启残留回收未验证，需真机 | Profile 缺失/错误必须非 Verified 且有路径 | 五家真实 CLI、权限、大小写、OS 未验证，需真机 | 读真实磁盘字节、生产 basename、跨任务反污染 | 不写全局 RulesRelFile；无 Profile 不启动 | Discipline→StartReq→ProfileReq→文件→Verify roundtrip | 不新增 overlay/task/event 枚举 | 同名任务隔离、全局规则不变、错误内容不 Verified |
| S3 | 回收后准确 commit/空 Path 机内锁，真实重启未验证 | 丢 commit、错误 fallback、错误 status 由 spy/HTTP 锁 | 真实 git/worktree/权限未验证，需真机 | 必须穿 HTTP→Server→Capability | 不开绕过 Server 权限的读仓旁路 | ref→head 参数与 response shape 穿边界 | 不新增 diff/event/state 枚举 | C 精确入 head、空 commit fallback、空 Path 允许 |
| S4 | host 重启/残留 ready 未验证，需真机 | 空/错误 output 与凭据 alone 不能 ready | CLI/webview/OS 未验证，需真机 | 禁恒真 probe，读取真实 config/output | 保留 WakeHome credential/occupied 既有门 | ApplySnapshot config JSON 实读 | 不新增 probe/event 枚举 | ready 必须由真实非空检测输出支撑 |
| S5 | 真实槽位释放/机器回收未验证，需真机 | 请求 executor 不能伪装成物理绑定 | 物理机跨平台未验证，需真机 | 完整 Keystone 竖切，不直测 bindingFor | 真实 Admit/occupancy 路径保留 | 不新增 wire 字段，载体身份不丢 | 不新增 state/identity 枚举 | Target/Executor/HomeDir 物理一致，模型覆盖规则不变 |
| S6 | 远端孤儿/重派状态未验证，需真机 | 文档不再承诺同任务，保留 resume 指引 | 真实网络/502 未验证，需真机 | 文档命中不等于 runtime idempotency | status-first 后才由协调者决定 dispatch/resume | 无新增 runtime serialization | 不新增 retry/event enum | 不自动重试、不虚构远端同任务身份 |

R1–R9 归属也逐项固定：R1→S4；R2→S1；R3→S1（保留 agentd 审批实现，不迁包）；R4→S1；R5 已由上游范围排除，不在本卡新增任务；R6→S2；R7→§9.5 真机统一收尾而非机内 acceptance；R8→S6；R9→S3。没有把 R5/R7 的延后状态误记成实现完成。

### 10.4 接缝双向总表

测试 → 接缝：S1 入口是 `handleQuestion`/`waitQuestion`/`RelayAnswer`/`authorizeNativePermission`，S2 是 `Manager.Dispatch`/Profile capability，S3 是 HTTP `handleTaskDiff`，S4 是 `WakeHome`/observable probe，S5 是完整 `TestIgnitionVerticalSlice`，S6 是三处文档对账；每支均在对应小节列出。

接缝 → 测试：Ask、permission、Profile 装配、五家 Prepare/Verify、ResultRef commit/fallback/Path、WakeHome output、B350 physical identity、R8 三文档均至少有一支入口级断言；未锁定的跨机项转 §9.5 真机清单，不冒充机内通过。

### 10.5 跨卡审计状态

本回合不能调用独立 executor/subagent，故跨卡审计由单上下文自审完成，结论按纪律记为**待拍板**，必须由协调者在派发前独立复核。已完成的对照内容如下：

1. 冻结物逐条：S1 覆盖 #1–#24，S2 覆盖 #25–#34，S3 覆盖 #35–#40，S4 覆盖 #41–#44，S5 覆盖 #45–#48，S6 覆盖 #49–#52；P1–P4 采用 breakdown 已拍板文字。
2. 跨卡签名逐字：S1 消费 `ApprovalClient` 四方法、`StartReq`、`RespondAskReq`；S2 生产/消费 `StartReq.Discipline` 与 `ProfileReq.TaskOverlay`；S3 消费 `ResultRef`/`Capability.DiffRange`；S5 消费 `bindingFor`，均与 contract 当前签名一致；P1-B 不改 `ApprovalClient`。
3. spec 故事归属：逐条见 §10.1，没有只写“各 task 覆盖”而不指到具体入口。

协调者复核重点是：S2 的 task namespace 文件名是否与现有五家 harness 的真实读取方式一致，以及 P1-B 的动态可选能力是否确实只存在于既有注入对象；如实现需新增公共接口、事件、持久字段或 adapter 级纪律投影，必须退回 contract，不在实现中顺手扩面。

## 11. 占位符扫描例外声明

本计划无 `TBD`、空任务、`同 Task N`、未给代码的“适当错误处理”或未归属故事。唯一允许的 harness 例外是：S1/S2/S3 测试复用仓内既有夹具，因其构造形状属于对应 Go 包的私有 harness，本计划没有重新抄一份会漂移的 setup；但每支测试都指名了要照抄的文件/测试入口，并逐条列全可判 pass/fail 断言。该例外不允许把入口改成 helper 单测，也不允许省略真实 JSON/HTTP/磁盘边界。
