# B156.2.4 实现计划：C4 d_collab·Send 白名单执法全矩阵 + Pointer 实现（欠账 #1 + #11 实现半）

**状态**：plan 节点产出（charter 流），2026-08-26。
**有效基线分支**：`feature/b156-workbench`（合并目标以此为准）。本计划在 `cards/B156.2.4-charter` 上执行，HEAD=c2e91ca0。
**上游输入**：spec `2026-08-25-b156.2-collaboration-rooms-design.md`（已批准）；契约 `b156.2-contract.md`（含 §11 微修订）；breakdown `b156.2-breakdown.md`（C4 卡四段式 + Pointer 归属补记）。
**台账**：`docs/superpowers/ledgers/ledger-b156.2.4-plan.md`（本计划所有探针/图闸/变异证据在案）。

本卡做三件事（欠账 #1 全量 + #11 实现半 + C4/C5 共用沉放结构落位）：

1. **Send 白名单执法全矩阵**：把竖切阶段的占位（未接线分支返回 ErrKindNotAllowed）连根拔掉，换真执法——协调者类绑定比对、relay 一级父、user 对抗、并入只读、换绑剥权合取；
2. **Pointer 函数体**（归属补记裁决归 C4）：置 Kind/BySystem、复用 resolveRoom、只读拒写；
3. **根包瘦身 + 子包沉放**：`internal/collab/room` 执法内核子包（命名与沉放清单由本计划定稿，C5 依此写）。

---

## 0. 冻结物核对（对照冻结物逐条）

| 冻结条目 | 本计划的对应 | 越界？ |
| --- | --- | --- |
| 契约 §3.3 Send 执法规则逐行（kind 词表 / 房间解析 / 只读 / 协调者类绑定 / relay 一级父 / user 对抗 / pointer 拒收） | T4.1 矩阵测试 + T4.2/T4.3 实现逐条 | 否 |
| 契约 §3.3 Pointer「系统组件写指针行的专用入口；HTTP/CLI 面不得路由它」 | T4.2/T4.3 Pointer 实现；路由缺席断言归 C6（不越卡） | 否 |
| 契约 §4 发送执法 10 条（条 4-10 + merged 只读 + Rebind 剥权） | T4.1 各测试逐条对应（映射表见 §3 T4.1） | 否 |
| 契约 §4 门面块「internal/collab 全包（_test.go 除外）无任何 ledger 包 import，graph check --view fails 空」 | T4.5 图闸 + grep 扫描 | 否 |
| 契约 §11.4「Card.Following 仅在列表方法上有值；GetCard 恒空；实现节点不得假设 GetCard 能判并入态」 | room.Resolve 取数走 ListAllCards 全量投影（D3） | 否 |
| breakdown C4 ②「service.go 只剩 Service/New/哨兵；resolveRoom/writer 判定/房间解析沉入子包（命名归实现），_test.go 豁免照旧」 | T4.2/T4.3 沉放清单（§6） | 否 |
| breakdown C4 ③「Pointer 写入读回 Kind==pointer && BySystem==true（置位在内部）；merged/终态→ErrReadOnly；grep return 0,nil 为 0；变异靶改回空壳两支断言翻红」 | T4.1 判据一/二测试 + T4.5 变异复验 | 否 |
| breakdown C4 ③「graph check --view cards-B156.2-charter-4 fails 空；go test ./internal/collab/... 全绿」 | T4.5 图闸 + T4.4 绿 | 否 |
| breakdown 岔口十「user 最窄读法：卡房间拒该卡绑定值；relay 额外拒直接父绑定值；群房间仅非空」 | D4：user 相关卡={该卡, 直接父}（本计划按此实现并写测试，协调者否了就放宽） | 否 |
| spec 测试接缝清单 #1（入站 api 门面） | 全部矩阵/Pointer 测试从 Service.Send/Service.Pointer 进入 | 否 |
| 拍板 5.5（relay 仅一级父） | VerifyWriter relay 分支只查直接父 | 否 |
| 拍板 5.2（白名单单点执法在 Send） | pointer 经 Send 一律拒收保持；白名单唯一写入口 | 否 |

零触碰点名（照抄 breakdown + 契约）：d_protocol（金样本不改一字）、d_sessions、d_ledger 生产代码、d_gateway、d_cli、d_web、target.json（C6/C7 的预定声明不归本卡）。

## 1. 现状查证（全部亲读亲测，出处可复查）

| # | 事实 | 出处 |
| --- | --- | --- |
| 1 | Send 占位：协调者类/relay 分支落 ErrKindNotAllowed（"竖切阶段不伪造放行"），user 分支只查 actor 非空、**无绑定比对**（user actor==绑定值可发的洞今天存在） | `internal/collab/service.go:66-75`（亲读） |
| 2 | Pointer 空壳 `return 0, nil`，无可观测行为 | `internal/collab/service.go:82-86` |
| 3 | resolveRoom 用 GetCard：存在性 + Status 终态只读；**无并入（Following）判定** | `internal/collab/service.go:174-184` |
| 4 | GetCard 不派生 Following（返回裸 Card）；并入态唯一枚举源=ListActiveCards/ListAllCards | `internal/ledger/cards.go:410-416`、`internal/ledger/api/api.go#cardWire`（契约 §11.4） |
| 5 | ListAllCards=ListCards{IncludeTerminal:true} 直通，含终态与 Following 非空的并入成员 | `internal/ledger/api/api.go:58-71` |
| 6 | C1 已落地：ClaimCardAs/RebindDriver（EvDriverTakeover、CAS）真实实现；换绑剥权合取现在可真跑 | `internal/ledger/binding.go:36-113` |
| 7 | MergeCards 只许非终态非重复成员并入承载卡；并入卡保持非终态（Following 非空即冻结） | `internal/ledger/merge.go:15-90` |
| 8 | 终态字面量=ledger.StatusDone("已完成")/StatusClosed("终止")；collab 侧 resolveRoom 已有同字面量（门面禁令不 import ledger） | `internal/ledger/types.go:16-17`、`service.go:182` |
| 9 | 既有测试钩子：竖切、群级无卡事件、未知 kind、pointer 经 Send、未知房间、空 actor、终态只读、History 过滤、Mentions 过滤、事件类型字面量 pin | `internal/collab/service_test.go`（全量亲读） |
| 10 | 既有占位反伪造测试 TestCoordinatorKindsNotForgedBySkeleton 断言「err==nil 即失败」——真执法后该断言恒过（非绑定→ErrNotWriter 非 nil），**须由真矩阵测试替换** | `service_test.go:164-173` |
| 11 | 图闸现状：`go run . graph check --repo . --view cards-B156.2-charter-4` → `"fails": []` EXIT=0（2026-08-26 实测）；warns 数不作判据 | 台账 L3 |
| 12 | 代码图是手工声明 JSON，不扫描源码；**新建子包必须在本轮视图 diff 里声明 containersAdded/nodesAdded，否则符号永远进不了 baseline**（协调者实况：internal/ledger/api 14 方法只入图 6 个的反面案例在 B228） | 台账 L7-P8 |
| 13 | 视图容器命名约定：包级函数容器 `<pkg>_fn`、实体容器 `<pkg>_model`（`2026-08-19-codegraph-design.md:38-41`）；节点 id `n_<pkg>_<Name>` / `m_<pkg>_<Name>` | baseline.json 亲读 |
| 14 | 既有测试缝形态：service_test.go（package collab）真 SQLite + Facade，测试文件不计图边 | `service_test.go:24-48` 头注释 |

**基线绿（plan 轮实测）**：`go test ./internal/collab/...`=ok（0.799s）；`graph check --view cards-B156.2-charter-4` fails=[] EXIT=0（台账 L2/L3）。

## 2. 有界文件集（上下文预算检查）

| 文件 | 动作 | 归属 task |
| --- | --- | --- |
| `internal/collab/room/errors.go` | **新建**：错误哨兵单点定义 | T4.2 |
| `internal/collab/room/room.go` | **新建**：Resolve/VerifyWriter/KindAllowed/IsTerminalStatus/读侧助手/常量 | T4.2 |
| `internal/collab/service.go` | 改写：哨兵再导出 + Send 全矩阵 + Pointer 填肉 + History/Mentions 改走子包 | T4.3 |
| `internal/collab/service_test.go` | 改写：替换占位测试 + 追加矩阵/Pointer 测试 + 字面量 pin 改引用 | T4.1（矩阵/Pointer）+ T4.3（pin 改引用） |
| `codegraph/best.json` | containers 加 k_collab_room_fn / k_collab_room_model → d_collab | T4.5 |
| `codegraph/diffs/cards-B156.2-charter-4.json` | containersAdded + nodesAdded + edgesAdded（累计视图，沿用既有文件，**不许另起新视图**） | T4.5 |

圈得出：四个代码/测试文件 + 两个图数据文件，全在 d_collab 域内。测试范围最小化：只跑 `./internal/collab/...`；全量回归 `go build ./...`（service.go 依赖面）与图闸；**全仓 go test 不属于本卡**。

## 3. Task 分解

### T4.1 立红：执法矩阵与 Pointer 测试（锁缝断言步骤，法定红绿）

**Interfaces**

- Produces（本卡锁定的行为，全部从入站门面进入）：
  - `Service.Send(roomID string, msg proto.RoomMessage, actor string) (int64, error)`（签名不变）
  - `Service.Pointer(roomID string, msg proto.RoomMessage) (int64, error)`（签名不变）
  - 哨兵集 `ErrNoRoom/ErrKindNotAllowed/ErrReadOnly/ErrNotWriter/ErrNotBound`（符号不变）
- Consumes（既有，精确签名，见 §1）：
  - `func (s *Store) ClaimCardAs(id, owner, carrier string) error`（binding.go:36）
  - `func (s *Store) RebindDriver(id, toSession, carrier, expect string) error`（binding.go:79）
  - `func (s *Store) MergeCards(ids []string, into, actor string) error`（merge.go:15）
  - `func (s *Store) CloseCard(id, reason, actor string) error`（cards.go:658）
  - `func (s *Store) CreateCard(nc NewCard) (Card, error)`（cards.go:244，NewCard.Parent 字段）
  - `func (s *Store) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]Event, error)`

**判据→测试映射表（契约 §4 条目号）**：

| 判据 | 测试 |
| --- | --- |
| 条 4：escalation/deviation/closing/reply 非当前绑定者→ErrNotWriter | TestSendCoordinatorKindsRequireBinding |
| 条 5：relay 直接父绑定者可发 | TestSendRelayAllowsDirectParentWriter |
| 条 6：relay 祖父/无关会话→ErrNotWriter | TestSendRelayRejectsGrandparentAndUnrelated |
| 条 7：user actor==房间卡绑定值→ErrNotWriter（含直接父——岔口十最窄读法） | TestSendUserRejectsCardBinding / TestSendUserRejectsParentBinding |
| 条 8：pointer 经 Send 一律拒收 | TestSendRejectsPointerViaSend（既有保留） |
| 条 9/10：合法 user 落一条 room_message 逐字段一致、seq=库行 seq | TestSendUserMessageVerticalSlice（既有保留） |
| merged_into 非空 Send→ErrReadOnly | TestSendRejectsMergedCardRoom |
| RebindDriver 成功后旧会话协调者类 Send→ErrNotWriter | TestSendRebindRevokesOldSession |
| 群房间 user 仅要求非空 | TestSendUserGroupRoomRequiresNonEmpty |
| **判据一**：Pointer 写入读回 Kind==pointer && BySystem==true（置位在内部） | TestPointerWritesPointerMessage + TestPointerOverridesCallerKindAndBySystem |
| **判据二**：merged/终态房间 Pointer→ErrReadOnly | TestPointerRejectsReadOnlyRoom |
| Pointer 未知房间→ErrNoRoom | TestPointerRejectsUnknownRoom |

**步骤**

1. （~2min）基线复核：`go test ./internal/collab/...` 全绿（台账 L2）；`sed -n '164,173p' internal/collab/service_test.go` 确认占位反伪造测试仍在。
2. （~6min）写失败测试：用下方完整代码块**替换整个 `internal/collab/service_test.go`**——矩阵/Pointer 测试（本次）+ 既有绿测试保留。**注意：下方代码块的字面量 pin 暂保持旧形（`protoRoomEventType`，T4.3 才改 `room.RoomEventType`）**——因为 room 包在 T4.2 才建，T4.1 若引用 room 会编译失败，红run就成了编译错而不是断言失败。
   > **顺序依赖**：本 task 与 T4.2/T4.3 的提交同批：先写测试（红）→ 建子包 → 改写 service.go + pin 改引用 → 一起跑绿 → 一起提交。红绿是 T4.1 红 → T4.4 绿的一个周期，中间 T4.2/T4.3 为内部实现步骤不配独立周期。
3. （~1min）跑红：`go test ./internal/collab/ -run 'TestSend|TestPointer' -v`。预期（plan 轮探针实测原文，台账 L-P1）：TestSendCoordinatorKindsRequireBinding / RelayAllowsDirectParentWriter / RelayRejectsGrandparentAndUnrelated / UserRejectsCardBinding / **UserRejectsParentBinding** / RejectsMergedCardRoom / RebindRevokesOldSession / PointerWritesPointerMessage / PointerOverridesCallerKindAndBySystem / PointerRejectsReadOnlyRoom / PointerRejectsUnknownRoom **恰 11 支 FAIL**；TestSendUserGroupRoomRequiresNonEmpty 与全部既有绿测试（竖切/群级/未知kind/pointer拒收/未知房间/空actor/终态只读）**保持绿**（回归钉，预期内）。红文贴台账。
4. （~1min）台账记红文。
5. 本 task 不单独提交（与 T4.4 绿同批，见步骤 2 顺序依赖）。

**完整代码块——`internal/collab/service_test.go`（整文件替换；plan 轮已验证该文件全文装回后全绿，证据台账 L-P3）**

```go
// 协作房间域入站门面的直通竖切与执法矩阵测试（B156.2 契约 §3.3/§4）。
//
// 竖切（重档法定步骤）：一次真实调用 Send(user, 卡房间) 穿过 Service →
// client.LedgerClient 接口 → internal/ledger/api.Facade → 真 SQLite
// ledger.Store，落 card_events 后由 History 读回。测试钉在主缝上（库缝
// 形态 = 夹具直调），这是 Ticket 0「越过空壳的可观测行为须有能变红的测试」
// 的正当出口。
//
// 执法矩阵（欠账 #1）：Send 的协调者类/relay/user 书写者校验、并入只读、
// 换绑剥权，与 Pointer 实现，全部从入站门面（Service.Send/Service.Pointer）
// 断言——缝#1 是唯一法定入口，规则实现（room 子包）不设独立单测。
//
// 本文件的 import internal/ledger(/api) 仅存在于 _test.go：图边采集排除
// 测试文件（charter/graph edgegate.go:190），不构成 d_collab→d_ledger 生产边。
package collab

import (
	"encoding/json"
	"path/filepath"
	"testing"

	"github.com/Xsxdot/handoff/internal/ledger"
	ledgerapi "github.com/Xsxdot/handoff/internal/ledger/api"
	"github.com/Xsxdot/handoff/internal/proto"
)

// newFixture 起真 SQLite 账本并按组装点同形绑定 Facade。
func newFixture(t *testing.T) (*Service, *ledger.Store) {
	t.Helper()
	st, err := ledger.Open(filepath.Join(t.TempDir(), "ledger.db"))
	if err != nil {
		t.Fatalf("open: %v", err)
	}
	t.Cleanup(func() { st.Close() })
	if _, err := st.PutWorkflow("bug", ledger.WorkflowDef{Nodes: []ledger.NodeDef{
		{Name: ledger.StatusTodo, Next: ledger.StatusDoing},
		{Name: ledger.StatusDoing, Next: ledger.StatusDone},
		{Name: ledger.StatusDone},
	}}); err != nil {
		t.Fatalf("seed workflow: %v", err)
	}
	return New(ledgerapi.New(st)), st
}

func mustCard(t *testing.T, s *Service, st *ledger.Store, title string) ledger.Card {
	t.Helper()
	card, err := st.CreateCard(ledger.NewCard{Title: title, Project: "handoff", Workflow: "bug", Actor: "test"})
	if err != nil {
		t.Fatalf("建卡: %v", err)
	}
	return card
}

// mustCardWithParent 建一张直接父卡为 parentID 的卡。
func mustCardWithParent(t *testing.T, s *Service, st *ledger.Store, title, parentID string) ledger.Card {
	t.Helper()
	card, err := st.CreateCard(ledger.NewCard{Title: title, Project: "handoff", Workflow: "bug", Parent: parentID, Actor: "test"})
	if err != nil {
		t.Fatalf("建子卡: %v", err)
	}
	return card
}

// mustBind 用 ClaimCardAs 给卡绑一个协调者会话。
func mustBind(t *testing.T, st *ledger.Store, id, owner string) {
	t.Helper()
	if err := st.ClaimCardAs(id, owner, ""); err != nil {
		t.Fatalf("绑定 %s→%s: %v", id, owner, err)
	}
}

// mustAnyCard 建一张默认标题的卡。
func mustAnyCard(t *testing.T, s *Service, st *ledger.Store) ledger.Card {
	return mustCard(t, s, st, "竖切夹具卡")
}

// TestSendUserMessageVerticalSlice 直通竖切：一次真实调用穿全链。
func TestSendUserMessageVerticalSlice(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)

	msg := proto.RoomMessage{Room: card.ID, Kind: proto.RoomMsgUser,
		Body: "先停一下，验收判据我想改", Refs: []string{"docs/x.md"}, Mentions: []string{"B156"}}
	seq, err := svc.Send(card.ID, msg, "user:sy")
	if err != nil {
		t.Fatalf("竖切发送失败: %v", err)
	}
	if seq <= 0 {
		t.Fatalf("seq 必须为正: %d", seq)
	}

	events, err := st.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	var found *ledger.Event
	for i := range events {
		if events[i].Type == "room_message" {
			found = &events[i]
		}
	}
	if found == nil {
		t.Fatalf("card_events 里没有 room_message 行: %+v", events)
	}
	if found.CardID != card.ID || found.Actor != "user:sy" {
		t.Fatalf("落账行字段漂移: card=%q actor=%q", found.CardID, found.Actor)
	}
	var back proto.RoomMessage
	if err := json.Unmarshal(found.Payload, &back); err != nil {
		t.Fatal(err)
	}
	if back.Body != msg.Body || back.Kind != proto.RoomMsgUser || len(back.Refs) != 1 {
		t.Fatalf("载荷往返不一致: %+v", back)
	}

	history, err := svc.History(card.ID, 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(history) != 1 || history[0].Seq != seq {
		t.Fatalf("History 读回与发送不符: %+v", history)
	}
}

// TestSendGroupMessageLandsAsCardlessEvent 群级消息走无卡事件。
func TestSendGroupMessageLandsAsCardlessEvent(t *testing.T) {
	svc, st := newFixture(t)
	const room = "project:handoff"
	seq, err := svc.Send(room, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "全局通知"}, "user:sy")
	if err != nil {
		t.Fatalf("群房间发送失败: %v", err)
	}
	events, err := st.EventsFromAsc([]string{}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range events {
		if ev.Seq == seq && ev.CardID != "" {
			t.Fatalf("群级消息必须是无卡事件: %+v", ev)
		}
	}
}

func TestSendRejectsUnknownKind(t *testing.T) {
	svc, _ := newFixture(t)
	if _, err := svc.Send("project:x", proto.RoomMessage{Kind: "heartbeat", Body: "心跳"}, "user:sy"); err != ErrKindNotAllowed {
		t.Fatalf("白名单外 kind 必须拒收，got %v", err)
	}
}

func TestSendRejectsPointerViaSend(t *testing.T) {
	svc, _ := newFixture(t)
	if _, err := svc.Send("global", proto.RoomMessage{Kind: proto.RoomMsgPointer, Body: "指针"}, "system:pointer"); err != ErrKindNotAllowed {
		t.Fatalf("pointer 经 Send 必须拒收，got %v", err)
	}
}

func TestSendRejectsUnknownRoom(t *testing.T) {
	svc, _ := newFixture(t)
	if _, err := svc.Send("B99999", proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy"); err != ErrNoRoom {
		t.Fatalf("不存在房间必须返回 ErrNoRoom，got %v", err)
	}
}

func TestSendRejectsEmptyActorForUserKind(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, ""); err != ErrNotWriter {
		t.Fatalf("空 actor 必须返回 ErrNotWriter，got %v", err)
	}
}

func TestSendRejectsTerminalCardRoom(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	if err := st.CloseCard(card.ID, ledger.CloseCancelled, "test"); err != nil {
		t.Fatalf("置终态: %v", err)
	}
	if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy"); err != ErrReadOnly {
		t.Fatalf("终态卡房间必须只读，got %v", err)
	}
}

// TestSendCoordinatorKindsRequireBinding 契约 §4 条 4：escalation/deviation/
// closing/reply 由非当前绑定者发送 → ErrNotWriter；当前绑定者可发。
func TestSendCoordinatorKindsRequireBinding(t *testing.T) {
	svc, st := newFixture(t)
	for _, kind := range []string{proto.RoomMsgEscalation, proto.RoomMsgDeviation,
		proto.RoomMsgClosing, proto.RoomMsgReply} {
		card := mustCard(t, svc, st, "协调者执法卡")
		if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: kind, Body: "x"}, "cli:a@h"); err != ErrNotWriter {
			t.Fatalf("kind %s 非绑定者必须 ErrNotWriter，got %v", kind, err)
		}
		mustBind(t, st, card.ID, "cli:a@h")
		seq, err := svc.Send(card.ID, proto.RoomMessage{Kind: kind, Body: "x"}, "cli:a@h")
		if err != nil || seq <= 0 {
			t.Fatalf("kind %s 当前绑定者可发，got err=%v seq=%d", kind, err, seq)
		}
	}
}

// TestSendRelayAllowsDirectParentWriter 契约 §4 条 5：relay 可由直接父卡当前
// 绑定者发送；条 6 反例在 TestSendRelayRejectsGrandparentAndUnrelated。
func TestSendRelayAllowsDirectParentWriter(t *testing.T) {
	svc, st := newFixture(t)
	parent := mustCard(t, svc, st, "父卡")
	child := mustCardWithParent(t, svc, st, "子卡", parent.ID)
	mustBind(t, st, parent.ID, "cli:p@h")
	if _, err := svc.Send(child.ID, proto.RoomMessage{Kind: proto.RoomMsgRelay, Body: "衔接"}, "cli:p@h"); err != nil {
		t.Fatalf("直接父绑定者 relay 必须可发，got %v", err)
	}
	mustBind(t, st, child.ID, "cli:c@h")
	if _, err := svc.Send(child.ID, proto.RoomMessage{Kind: proto.RoomMsgRelay, Body: "衔接"}, "cli:c@h"); err != nil {
		t.Fatalf("本卡绑定者 relay 必须可发，got %v", err)
	}
}

// TestSendRelayRejectsGrandparentAndUnrelated 契约 §4 条 6：祖父卡（只查一级
// 父，拍板 5.5）与无关联会话 → ErrNotWriter。
func TestSendRelayRejectsGrandparentAndUnrelated(t *testing.T) {
	svc, st := newFixture(t)
	gp := mustCard(t, svc, st, "祖父卡")
	parent := mustCardWithParent(t, svc, st, "父卡", gp.ID)
	child := mustCardWithParent(t, svc, st, "子卡", parent.ID)
	mustBind(t, st, gp.ID, "cli:g@h")
	mustBind(t, st, parent.ID, "cli:p@h")
	for _, actor := range []string{"cli:g@h", "cli:unrelated@h"} {
		if _, err := svc.Send(child.ID, proto.RoomMessage{Kind: proto.RoomMsgRelay, Body: "x"}, actor); err != ErrNotWriter {
			t.Fatalf("relay actor=%s 必须 ErrNotWriter，got %v", actor, err)
		}
	}
}

// TestSendUserRejectsCardBinding 契约 §4 条 7：user 类 actor 等于房间卡当前
// 绑定值 → ErrNotWriter（用户不是协调者）。
func TestSendUserRejectsCardBinding(t *testing.T) {
	svc, st := newFixture(t)
	card := mustCard(t, svc, st, "绑定卡")
	mustBind(t, st, card.ID, "cli:a@h")
	if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "cli:a@h"); err != ErrNotWriter {
		t.Fatalf("user actor==绑定值必须 ErrNotWriter，got %v", err)
	}
	if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy"); err != nil {
		t.Fatalf("user 非绑定者应可发，got %v", err)
	}
}

// TestSendUserRejectsParentBinding 岔口十最窄读法：子卡房间的 user 类还拒
// 直接父卡绑定者（相关卡={该卡, 直接父}，与拍板 5.5 一级父同构）。
func TestSendUserRejectsParentBinding(t *testing.T) {
	svc, st := newFixture(t)
	parent := mustCard(t, svc, st, "父卡")
	child := mustCardWithParent(t, svc, st, "子卡", parent.ID)
	mustBind(t, st, parent.ID, "cli:p@h")
	if _, err := svc.Send(child.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "cli:p@h"); err != ErrNotWriter {
		t.Fatalf("user actor==直接父绑定值必须 ErrNotWriter，got %v", err)
	}
	if _, err := svc.Send(child.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy"); err != nil {
		t.Fatalf("user 非任何绑定者应可发，got %v", err)
	}
}

// TestSendRejectsMergedCardRoom 并入承载卡的房间（merged_into 非空）Send →
// ErrReadOnly。
func TestSendRejectsMergedCardRoom(t *testing.T) {
	svc, st := newFixture(t)
	carrier := mustCard(t, svc, st, "承载卡")
	member := mustCard(t, svc, st, "并入卡")
	if err := st.MergeCards([]string{member.ID}, carrier.ID, "test"); err != nil {
		t.Fatalf("合并: %v", err)
	}
	if _, err := svc.Send(member.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy"); err != ErrReadOnly {
		t.Fatalf("并入房间必须 ErrReadOnly，got %v", err)
	}
}

// TestSendRebindRevokesOldSession 换绑剥权合取（依赖 C1）：RebindDriver 成功
// 后旧会话对该房间的协调者类 Send → ErrNotWriter，新会话可发。
func TestSendRebindRevokesOldSession(t *testing.T) {
	svc, st := newFixture(t)
	card := mustCard(t, svc, st, "换绑卡")
	mustBind(t, st, card.ID, "cli:old@h")
	if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgEscalation, Body: "x"}, "cli:old@h"); err != nil {
		t.Fatalf("换绑前旧会话可发，got %v", err)
	}
	if err := st.RebindDriver(card.ID, "cli:new@h", "", "cli:old@h"); err != nil {
		t.Fatalf("换绑: %v", err)
	}
	if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgEscalation, Body: "x"}, "cli:old@h"); err != ErrNotWriter {
		t.Fatalf("换绑后旧会话必须 ErrNotWriter，got %v", err)
	}
	if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgEscalation, Body: "x"}, "cli:new@h"); err != nil {
		t.Fatalf("换绑后新会话可发，got %v", err)
	}
}

// TestSendUserGroupRoomRequiresNonEmpty 群房间无卡可比：user 仅要求 actor
// 非空（岔口十最窄读法）。
func TestSendUserGroupRoomRequiresNonEmpty(t *testing.T) {
	svc, _ := newFixture(t)
	if _, err := svc.Send("project:handoff", proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, ""); err != ErrNotWriter {
		t.Fatalf("群房间空 actor 必须 ErrNotWriter，got %v", err)
	}
	if _, err := svc.Send("global", proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy"); err != nil {
		t.Fatalf("群房间非空 actor 可发，got %v", err)
	}
}

// TestPointerWritesPointerMessage Pointer 写入后读回该消息，断言
// Kind==pointer && BySystem==true、正文与 seq 落账一致（本卡新增判据一）。
func TestPointerWritesPointerMessage(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	seq, err := svc.Pointer(card.ID, proto.RoomMessage{Body: "spec 已定稿"})
	if err != nil {
		t.Fatalf("Pointer: %v", err)
	}
	if seq <= 0 {
		t.Fatalf("Pointer seq 必须为正，got %d", seq)
	}
	events, err := st.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	var found *ledger.Event
	for i := range events {
		if events[i].Seq == seq {
			found = &events[i]
		}
	}
	if found == nil {
		t.Fatalf("没找到 pointer 落账行")
	}
	if found.CardID != card.ID || found.Actor != "system:pointer" {
		t.Fatalf("pointer 行身份漂移: card=%q actor=%q", found.CardID, found.Actor)
	}
	var back proto.RoomMessage
	if err := json.Unmarshal(found.Payload, &back); err != nil {
		t.Fatal(err)
	}
	if back.Kind != proto.RoomMsgPointer || !back.BySystem {
		t.Fatalf("Pointer 置位失效: kind=%q by_system=%v", back.Kind, back.BySystem)
	}
	if back.Body != "spec 已定稿" || back.Room != card.ID {
		t.Fatalf("pointer 载荷漂移: body=%q room=%q", back.Body, back.Room)
	}
	hist, err := svc.History(card.ID, 0, 0)
	if err != nil || len(hist) != 1 || hist[0].Seq != seq {
		t.Fatalf("History 读回 pointer 失败: %v %d", err, len(hist))
	}
}

// TestPointerOverridesCallerKindAndBySystem 置位在 Pointer 内部：调用方传入
// 的 Kind/BySystem 被覆盖为 pointer/true（本卡新增判据一的反面形）。
func TestPointerOverridesCallerKindAndBySystem(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	seq, err := svc.Pointer(card.ID, proto.RoomMessage{Kind: proto.RoomMsgEscalation, Body: "x", BySystem: false})
	if err != nil || seq <= 0 {
		t.Fatalf("Pointer: %v seq=%d", err, seq)
	}
	events, err := st.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	var back proto.RoomMessage
	for i := range events {
		if events[i].Seq == seq {
			if err := json.Unmarshal(events[i].Payload, &back); err != nil {
				t.Fatal(err)
			}
		}
	}
	if back.Kind != proto.RoomMsgPointer || !back.BySystem {
		t.Fatalf("Pointer 必须自置 kind/by_system: kind=%q by_system=%v", back.Kind, back.BySystem)
	}
}

// TestPointerRejectsReadOnlyRoom 终态或并入房间调 Pointer → ErrReadOnly
// （本卡新增判据二）。
func TestPointerRejectsReadOnlyRoom(t *testing.T) {
	svc, st := newFixture(t)
	term := mustAnyCard(t, svc, st)
	if err := st.CloseCard(term.ID, ledger.CloseCancelled, "test"); err != nil {
		t.Fatalf("置终态: %v", err)
	}
	if _, err := svc.Pointer(term.ID, proto.RoomMessage{Body: "x"}); err != ErrReadOnly {
		t.Fatalf("终态房间 Pointer 必须 ErrReadOnly，got %v", err)
	}
	carrier := mustCard(t, svc, st, "承载卡")
	member := mustCard(t, svc, st, "并入卡")
	if err := st.MergeCards([]string{member.ID}, carrier.ID, "test"); err != nil {
		t.Fatalf("合并: %v", err)
	}
	if _, err := svc.Pointer(member.ID, proto.RoomMessage{Body: "x"}); err != ErrReadOnly {
		t.Fatalf("并入房间 Pointer 必须 ErrReadOnly，got %v", err)
	}
}

// TestPointerRejectsUnknownRoom Pointer 解析不到房间 → ErrNoRoom。
func TestPointerRejectsUnknownRoom(t *testing.T) {
	svc, _ := newFixture(t)
	if _, err := svc.Pointer("B99999", proto.RoomMessage{Body: "x"}); err != ErrNoRoom {
		t.Fatalf("未知房间 Pointer 必须 ErrNoRoom，got %v", err)
	}
}

func TestHistoryFiltersNonRoomEvents(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	if _, err := st.AddComment(card.ID, "普通评论", "普通", "tester"); err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "房内发言"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	history, err := svc.History(card.ID, 0, 10)
	if err != nil {
		t.Fatal(err)
	}
	if len(history) != 1 {
		t.Fatalf("History 必须滤掉非 room_message 事件: %d 条", len(history))
	}
}

func TestMentionsFiltersByMember(t *testing.T) {
	svc, st := newFixture(t)
	mustAnyCard(t, svc, st)
	if _, err := svc.Send("project:handoff",
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "@B145 看", Mentions: []string{"B145"}}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Send("project:handoff",
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "无提及"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	hit, err := svc.Mentions("B145", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(hit) != 1 {
		t.Fatalf("@提及过滤失准: %d 条", len(hit))
	}
	other, err := svc.Mentions("B999", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(other) != 0 {
		t.Fatalf("未提及成员不得命中: %d 条", len(other))
	}
}

// TestRoomEventTypeLiteralMatchesLedger 钉住 collab 侧字面量与账本词表的
// 等式；测试文件不计图边，可同时看见两侧（门面禁令只约束生产代码）。
// T4.3 会把 protoRoomEventType 改引用为 room.RoomEventType（常量随沉放迁移）。
func TestRoomEventTypeLiteralMatchesLedger(t *testing.T) {
	if protoRoomEventType != ledger.EvRoomMessage {
		t.Fatalf("房间事件类型字面量漂移: %q != %q", protoRoomEventType, ledger.EvRoomMessage)
	}
}
```

### T4.2 实现：room 执法内核子包（内部实现步骤，不配独立红绿）

**Interfaces**

- Produces：
  - `func Resolve(lc client.LedgerClient, roomID string) (*Room, error)`
  - `func VerifyWriter(r *Room, kind, actor string) error`
  - `func KindAllowed(kind string) bool`
  - `func IsTerminalStatus(status string) bool`
  - `func SameRoom(ev proto.LedgerEvent, roomID string) bool`
  - `func MentionsMember(ev proto.LedgerEvent, member string) bool`
  - `func UnmarshalMessage(raw json.RawMessage, msg *proto.RoomMessage) error`
  - `type Room struct { ID, CardID, Kind, Project string; ReadOnly bool; Card *proto.Card; ParentSession string }`
  - `const RoomEventType = "room_message"`；`KindCard/KindProject/KindGlobal`
  - 哨兵 `ErrNoRoom/ErrKindNotAllowed/ErrReadOnly/ErrNotWriter/ErrNotBound`（在 errors.go）
- Consumes：`client.LedgerClient.ListAllCards(project string) ([]proto.Card, error)`（api.go:58）、`proto.Card`/`proto.RoomMessage`/`proto.LedgerEvent`。

**行为语义（实现前读三遍）**

1. `Resolve` 卡房间取数=**一次 `ListAllCards("")`**：GetCard 无 Following（契约 §11.4），并入只读判定只有列表方法送得来，且终态卡也在列表内（IncludeTerminal:true）。入参 `"B99999"` 不在列表 → `ErrNoRoom`（直接返回不包裹，既有测试用 `==` 比较）。`ListAllCards` 自身报错按原样向上传播，不伪装成 ErrNoRoom。
2. `Room.ParentSession` 在 Resolve 内从同一张 byID 表顺带解析（直接父卡当前绑定者；父不存在/无父恒空串）——relay/user 的一级父判定不再二次 GetCard，且与 Resolve 同一快照更一致。
3. `VerifyWriter` 空 actor 一律 ErrNotWriter（任何 kind 都需要书写者身份）；群房间（r.Card==nil）仅 user 可写且要求非空，协调者类/relay 一律拒（群房间无绑定席位可比）。
4. 哨兵返回**不包裹**（`return ErrNotWriter`），与既有测试的 `==` 比较兼容。

**步骤**

1. （~2min）`mkdir -p internal/collab/room`；写 `errors.go`（下方完整代码块）。
2. （~3min）写 `room.go`（下方完整代码块，含文件头职责+边界、导出函数注释、结构化日志、非显然逻辑 why）。
3. （~1min）编译门：`go build ./internal/collab/room/`（plan 轮已实测 EXIT=0）。**测试仍红**（service.go 未改），预期内。

**完整代码块——`internal/collab/room/errors.go`（新建）**

```go
// 协作房间域的执法错误哨兵（契约 §3.3 哨兵集冻结，映射表不变）。
// 定义在执法内核子包、由根包 collab 再导出：gateway/CLI 只认 collab.ErrXxx
// 符号，根包与子包不可互 import（根包依赖子包），哨兵只能单点定义在子包。
package room

import "errors"

var (
	// ErrNoRoom 400：房间不存在
	ErrNoRoom = errors.New("collab: 房间不存在")
	// ErrKindNotAllowed 400：kind 不在白名单 / pointer 走错门
	ErrKindNotAllowed = errors.New("collab: 消息形态不在白名单")
	// ErrReadOnly 409：房间并入冻结或终态归档
	ErrReadOnly = errors.New("collab: 房间已只读（并入冻结或终态归档）")
	// ErrNotWriter 403：书写者与房间身份不符
	ErrNotWriter = errors.New("collab: 书写者与房间身份不符")
	// ErrNotBound 409：需要绑定席位的动作遇到无绑定卡
	ErrNotBound = errors.New("collab: 卡无绑定席位")
)
```

**完整代码块——`internal/collab/room/room.go`（新建；plan 轮已验证编译+全链路绿，行号供 T4.5 视图 diff 用）**

```go
// Package room 是协作房间域的执法内核（B156.2 C4/C5 沉放结构）：房间解析、
// 只读判定与书写者执法的唯一实现处。根包 collab 只保留门面 Service 与错误
// 哨兵的再导出，规则一律沉在本子包（协调者护栏：根包只留门面）。
//
// 门面禁令与根包同款：本包生产代码只经 client.LedgerClient 与 proto 与外界
// 交互，零 import internal/ledger——执法所需的「已并入」状态（Following）与
// 终态判定全部走列表投影（ListAllCards），不与账本内部类型耦合。
//
// C5（消费与注意力读模型）与本包共用沉放结构：Pending/Consume/ListRooms/
// MarkRead/Unread 所需的房间判定与读侧过滤助手（SameRoom/MentionsMember/
// UnmarshalMessage/RoomEventType）都在本包。
package room

import (
	"encoding/json"
	"log/slog"

	"github.com/Xsxdot/handoff/internal/collab/client"
	"github.com/Xsxdot/handoff/internal/proto"
)

// log 结构化 logger（同 ledger 包 log() 先例，slog.Default()）。
func log() *slog.Logger { return slog.Default() }

// 房间三类（proto.RoomSummary.Kind 词表；C5 的列表投影共用，等值由
// service_test.go 的字面量测试钉住）。
const (
	KindCard    = "card"
	KindProject = "project"
	KindGlobal  = "global"
)

// Room 是房间解析的完整结果：识别房间形态并携带执法所需全部上下文。
// Card 为 nil 表示群房间（无卡可比）；ParentSession 只在卡房间且卡有直接父
// 时可能非空（直接父卡当前绑定者，供 relay/user 的一级父判定，拍板 5.5）。
type Room struct {
	ID            string
	CardID        string
	Kind          string
	Project       string
	ReadOnly      bool
	Card          *proto.Card
	ParentSession string
}

// Resolve 把房间标识解析为执法上下文。
//
// 卡房间（卡号）：存在性、终态、并入态与绑定来自一次 ListAllCards 全量投影
// ——GetCard 不派生 Following（契约 §11.4），并入只读判定只有列表方法送得来，
// 且终态卡也在列表内（IncludeTerminal:true）。群房间恒可写（本期群房间无
// 终态、无绑定可比），不查卡。
//
// 返回哨兵：房间不存在 → ErrNoRoom（直接返回不包裹，调用方 == / errors.Is
// 皆可）。账本读失败（ListAllCards 报错）按原样向上传播，不伪装成 ErrNoRoom。
func Resolve(lc client.LedgerClient, roomID string) (*Room, error) {
	if isGroupRoom(roomID) {
		kind, name := groupRoomKind(roomID)
		log().Info("房间解析为群房间", "room", roomID, "kind", kind, "project", name)
		return &Room{ID: roomID, Kind: kind, Project: name}, nil
	}
	cards, err := lc.ListAllCards("")
	if err != nil {
		return nil, err
	}
	byID := make(map[string]proto.Card, len(cards))
	for _, c := range cards {
		byID[c.ID] = c
	}
	c, ok := byID[roomID]
	if !ok {
		log().Warn("房间解析失败：卡不存在", "room", roomID)
		return nil, ErrNoRoom
	}
	r := &Room{
		ID:       roomID,
		CardID:   c.ID,
		Kind:     KindCard,
		Project:  c.Project,
		ReadOnly: IsTerminalStatus(c.Status) || c.Following != "",
		Card:     &c,
	}
	if c.ParentID != "" {
		if p, ok := byID[c.ParentID]; ok {
			r.ParentSession = p.DriverSession
		}
	}
	log().Info("房间解析完成", "room", roomID, "card", c.ID, "read_only", r.ReadOnly,
		"bound", c.DriverSession, "parent_session", r.ParentSession)
	return r, nil
}

// VerifyWriter 按 kind 执法书写者身份（契约 §3.3 执法规则 + §4 清单）。失败
// 统一返回 ErrNotWriter（含空 actor）。
//
// 矩阵：
//   - 协调者类（escalation/deviation/closing/reply）：actor==该卡当前绑定值；
//   - relay：actor==该卡当前绑定值，或 actor==直接父卡当前绑定值（只查一级，
//     拍板 5.5）；
//   - user：actor 非空且不等于该卡当前绑定值；卡有直接父时亦不等于直接父卡
//     绑定值（岔口十最窄读法：相关卡={该卡, 直接父}，与拍板 5.5 一级父同构）；
//   - 群房间无卡可比：仅 user 可写且要求 actor 非空。
func VerifyWriter(r *Room, kind, actor string) error {
	if actor == "" {
		log().Warn("书写者被拒：actor 为空", "room", r.ID, "kind", kind)
		return ErrNotWriter
	}
	switch kind {
	case proto.RoomMsgUser:
		if r.Card == nil {
			return nil
		}
		if actor == r.Card.DriverSession {
			log().Warn("user 被拒：actor 是房间卡绑定者", "room", r.ID,
				"card", r.CardID, "actor", actor)
			return ErrNotWriter
		}
		if r.ParentSession != "" && actor == r.ParentSession {
			log().Warn("user 被拒：actor 是直接父卡绑定者", "room", r.ID,
				"card", r.CardID, "parent_actor", actor)
			return ErrNotWriter
		}
	case proto.RoomMsgRelay:
		if r.Card == nil {
			log().Warn("relay 被拒：群房间无卡可比", "room", r.ID)
			return ErrNotWriter
		}
		if actor != r.Card.DriverSession && actor != r.ParentSession {
			log().Warn("relay 被拒：非本卡或直接父绑定者", "room", r.ID,
				"card", r.CardID, "actor", actor, "bound", r.Card.DriverSession,
				"parent_session", r.ParentSession)
			return ErrNotWriter
		}
	default:
		if r.Card == nil {
			log().Warn("协调者类被拒：群房间无绑定席位可比", "room", r.ID, "kind", kind)
			return ErrNotWriter
		}
		if actor != r.Card.DriverSession {
			log().Warn("协调者类被拒：非当前绑定者", "room", r.ID,
				"card", r.CardID, "kind", kind, "actor", actor,
				"bound", r.Card.DriverSession)
			return ErrNotWriter
		}
	}
	return nil
}

// KindAllowed kind 是否在受控词表内。
func KindAllowed(kind string) bool {
	switch kind {
	case proto.RoomMsgEscalation, proto.RoomMsgDeviation, proto.RoomMsgClosing,
		proto.RoomMsgRelay, proto.RoomMsgReply, proto.RoomMsgUser, proto.RoomMsgPointer:
		return true
	}
	return false
}

// IsTerminalStatus 卡终态判据。取值必须等于 ledger.StatusDone/StatusClosed
// （"已完成"/"终止"）；门面禁令禁止 import ledger，等式由 service_test.go
// 的字面量测试钉住。C5 的 ListRooms 只读投影同用它。
func IsTerminalStatus(status string) bool {
	return status == "已完成" || status == "终止"
}

// isGroupRoom 群房间标识形如 project:<name> 或 global。
func isGroupRoom(roomID string) bool {
	const prefix = "project:"
	return roomID == "global" || len(roomID) > len(prefix) && roomID[:len(prefix)] == prefix
}

// groupRoomKind 解析群房间形态：global 返回 (global,"")；project 群返回
// (project, 名字)。
func groupRoomKind(roomID string) (kind, name string) {
	if roomID == "global" {
		return KindGlobal, ""
	}
	const prefix = "project:"
	return KindProject, roomID[len(prefix):]
}

// RoomEventType 是账本流上房间消息的事件类型字面量。取值必须等于
// ledger.EvRoomMessage；门面禁令禁止 import ledger，等式由金样本测试
// （internal/proto/rooms_fixture_test.go / service_test.go）钉住。
const RoomEventType = "room_message"

// SameRoom 判断事件是否属于该房间：卡房间的 card_id 即房间号；群级事件看
// 载荷 Room 字段。
func SameRoom(ev proto.LedgerEvent, roomID string) bool {
	if isGroupRoom(roomID) {
		var msg proto.RoomMessage
		if err := UnmarshalMessage(ev.Payload, &msg); err != nil {
			return false
		}
		return msg.Room == roomID
	}
	return ev.CardID == roomID
}

// MentionsMember 判断事件的 mentions 是否含该成员。
func MentionsMember(ev proto.LedgerEvent, member string) bool {
	var msg proto.RoomMessage
	if err := UnmarshalMessage(ev.Payload, &msg); err != nil {
		return false
	}
	for _, m := range msg.Mentions {
		if m == member {
			return true
		}
	}
	return false
}

// UnmarshalMessage 解码房间消息载荷；未知键不报错（encoding/json 默认）。
func UnmarshalMessage(raw json.RawMessage, msg *proto.RoomMessage) error {
	return json.Unmarshal(raw, msg)
}
```

### T4.3 实现：service.go 改写（哨兵再导出 + Send 全矩阵 + Pointer 填肉 + 读面改走子包）

**Interfaces**

- Produces（对外契约符号一字不变）：
  - `New(lc client.LedgerClient) *Service`（冻结签名不动）
  - `(s *Service) Send(roomID string, msg proto.RoomMessage, actor string) (int64, error)`
  - `(s *Service) Pointer(roomID string, msg proto.RoomMessage) (int64, error)`
  - 哨兵再导出 `ErrNoRoom = room.ErrNoRoom` 等五个
- Consumes：`room.Resolve/VerifyWriter/KindAllowed/RoomEventType/SameRoom/MentionsMember`、`client.LedgerClient.RecordRoomMessage(cardID string, msg proto.RoomMessage, actor string) (int64, error)`。

**步骤**

1. （~2min）用下方完整代码块**替换整个 `internal/collab/service.go`**。
2. （~1min）编译：`go build ./internal/collab/...` 应过（plan 轮已实测）。
3. （~2min）**pin 改引用（T4.1 保留旧形，本步迁移）**：`service.go` 删除 `protoRoomEventType` 后，`TestRoomEventTypeLiteralMatchesLedger` 必须改指 `room.RoomEventType`（否则编译失败），并追加 `TestRoomStatusLiteralMatchesLedger`。用下方「完整代码块——service_test.go 的两处 pin 替换」替换 T4.1 测试文件末尾的两段（`TestRoomEventTypeLiteralMatchesLedger` 整段 + import 块加 `room` 一行）：
   - import 块：在 `"github.com/Xsxdot/handoff/internal/ledger"` 之前加一行 `"github.com/Xsxdot/handoff/internal/collab/room"`；
   - 下方代码块替换测试文件末尾的 `TestRoomEventTypeLiteralMatchesLedger` 整段。
4. （~1min）日志检查：`grep -n "Println\|fmt.Print" internal/collab/service.go internal/collab/room/*.go` 零命中；成功路径与每条错误分支均有 slog 日志（代码块内已含）。

**完整代码块——service_test.go 的两处 pin 替换（替换末尾的 TestRoomEventTypeLiteralMatchesLedger 整段；plan 轮已验证全文装回后全绿）**

```go
// TestRoomEventTypeLiteralMatchesLedger 钉住 room 侧字面量与账本词表的
// 等式；测试文件不计图边，可同时看见两侧（门面禁令只约束生产代码）。
func TestRoomEventTypeLiteralMatchesLedger(t *testing.T) {
	if room.RoomEventType != ledger.EvRoomMessage {
		t.Fatalf("房间事件类型字面量漂移: %q != %q", room.RoomEventType, ledger.EvRoomMessage)
	}
}

// TestRoomStatusLiteralMatchesLedger 钉住 room.IsTerminalStatus 的终态字面量
// 与账本 StatusDone/StatusClosed 的等式（内部锁，理由见 §7）。
func TestRoomStatusLiteralMatchesLedger(t *testing.T) {
	if !room.IsTerminalStatus(ledger.StatusDone) || !room.IsTerminalStatus(ledger.StatusClosed) {
		t.Fatalf("终态字面量漂移: IsTerminalStatus(%q)=%v, IsTerminalStatus(%q)=%v",
			ledger.StatusDone, room.IsTerminalStatus(ledger.StatusDone),
			ledger.StatusClosed, room.IsTerminalStatus(ledger.StatusClosed))
	}
	if room.IsTerminalStatus(ledger.StatusDoing) {
		t.Fatalf("进行中不应判终态")
	}
}
```

**完整代码块——`internal/collab/service.go`（整文件替换；plan 轮已验证全文装回后全绿）**

```go
// Package collab 是协作房间域（B156.2）的入站门面：房间消息播发、白名单
// 与书写者执法、@定向消费队列与注意力面读模型的唯一入口。外界（gateway、
// CLI）只 import 本包与 proto DTO，不得深入内部；对外部账本的能力需求经
// client.LedgerClient 接口由组装点绑定（双向门面，架构法第四条第 3 档）。
//
// 根包只保留门面：执法规则（房间解析、只读判定、书写者校验）全部沉在
// internal/collab/room 子包；错误哨兵定义在 room、由本包再导出。
//
// 与 d_sessions（终端 PTY 回放域）同名不同物：本包管协作房间，不管 PTY。
package collab

import (
	"log/slog"

	"github.com/Xsxdot/handoff/internal/collab/client"
	"github.com/Xsxdot/handoff/internal/collab/room"
	"github.com/Xsxdot/handoff/internal/proto"
)

// log 结构化 logger（同 ledger 包 log() 先例，slog.Default()）。
func log() *slog.Logger { return slog.Default() }

// 错误哨兵。调用方（CLI/agentd）按哨兵翻译为退出码或 HTTP 状态：
// ErrNoRoom→400/404、ErrKindNotAllowed→400、ErrNotWriter→403、
// ErrReadOnly/ErrNotBound→409。定义在 room 子包（执法内核产生它们），
// 本包再导出以保持 collab.ErrXxx 契约符号不变。
var (
	ErrNoRoom         = room.ErrNoRoom
	ErrKindNotAllowed = room.ErrKindNotAllowed
	ErrReadOnly       = room.ErrReadOnly
	ErrNotWriter      = room.ErrNotWriter
	ErrNotBound       = room.ErrNotBound
)

// historyDefaultLimit History 未给 limit 时的取数上限。
const historyDefaultLimit = 200

// pointerActor Pointer 写指针行时落账的 actor 标识（契约 §3.3 Pointer 无
// actor 参数、调用方身份不可得，固定系统组件标识）。
const pointerActor = "system:pointer"

// Service 协作房间域入站门面。依赖只收 client 接口。
type Service struct {
	lc client.LedgerClient
}

// New 组装点调用；lc 的具体实现是 internal/ledger/api.Facade。
func New(lc client.LedgerClient) *Service {
	return &Service{lc: lc}
}

// Send 发消息：白名单 + 书写者执法的唯一写入口（pointer 除外——它只能走
// Pointer，经本方法一律拒收）。返回落账 seq。
//
// 执法矩阵（契约 §3.3 逐条 + §4 冻结清单）：kind 词表校验（pointer 拒收）
// → 房间解析（room.Resolve：存在性 + 只读判定）→ 只读拒写 → 按 kind 的
// 书写者校验（room.VerifyWriter）→ 经 client 发布。全部规则实现在 room
// 子包，本方法只做编排。
func (s *Service) Send(roomID string, msg proto.RoomMessage, actor string) (int64, error) {
	if !room.KindAllowed(msg.Kind) || msg.Kind == proto.RoomMsgPointer {
		return 0, room.ErrKindNotAllowed
	}
	r, err := room.Resolve(s.lc, roomID)
	if err != nil {
		return 0, err
	}
	if r.ReadOnly {
		return 0, room.ErrReadOnly
	}
	if err := room.VerifyWriter(r, msg.Kind, actor); err != nil {
		return 0, err
	}
	msg.Room = roomID
	seq, err := s.lc.RecordRoomMessage(r.CardID, msg, actor)
	if err != nil {
		return 0, err
	}
	log().Info("房间消息已落账", "room", roomID, "kind", msg.Kind, "actor", actor, "seq", seq)
	return seq, nil
}

// Pointer 系统组件写指针行的专用入口；HTTP/CLI 面不得路由它（冻结清单有
// 对应断言）。kind=pointer 与 BySystem=true 由本方法自置（rooms.go 注释
// 「本键只应由 Service.Pointer 置真」），调用方只递 roomID 与正文；房间
// 解析复用 room.Resolve，并入冻结/终态归档房间拒写（ErrReadOnly）。
func (s *Service) Pointer(roomID string, msg proto.RoomMessage) (int64, error) {
	r, err := room.Resolve(s.lc, roomID)
	if err != nil {
		return 0, err
	}
	if r.ReadOnly {
		return 0, room.ErrReadOnly
	}
	msg.Kind = proto.RoomMsgPointer
	msg.BySystem = true
	msg.Room = roomID
	seq, err := s.lc.RecordRoomMessage(r.CardID, msg, pointerActor)
	if err != nil {
		return 0, err
	}
	log().Info("指针行已落账", "room", roomID, "card", r.CardID, "seq", seq)
	return seq, nil
}

// History 读房间历史：seq 游标分页（beforeSeq 排他、升序截尾），只返回
// type=room_message 的事件。limit<=0 取 historyDefaultLimit。
func (s *Service) History(roomID string, beforeSeq int64, limit int) ([]proto.LedgerEvent, error) {
	if limit <= 0 {
		limit = historyDefaultLimit
	}
	events, err := s.lc.EventsFromAsc([]string{}, beforeSeq, 0)
	if err != nil {
		return nil, err
	}
	out := make([]proto.LedgerEvent, 0, len(events))
	for _, ev := range events {
		if ev.Type != room.RoomEventType || !room.SameRoom(ev, roomID) {
			continue
		}
		out = append(out, ev)
		if len(out) >= limit {
			break
		}
	}
	return out, nil
}

// Pending @定向消费队列（读侧）：mentions∋consumer 且尚无本人消费标记的
// 群级消息 + 其绑定卡房间的未消费用户留言。实现随欠账 #3 补齐游标维度；
// 当前返回空切片占位，调用方不得依赖其完整性。
func (s *Service) Pending(consumer string) ([]proto.LedgerEvent, error) {
	_ = consumer
	return []proto.LedgerEvent{}, nil
}

// Consume 消费落账（恰好一次的权威在账本 message_consumed 事件）。
// Ticket 0 空壳：无可观测行为。
func (s *Service) Consume(seq int64, consumer string) error {
	_ = seq
	_ = consumer
	return nil
}

// Mentions 收件箱源③：@member 的未消费提及。
func (s *Service) Mentions(member string, afterSeq int64, limit int) ([]proto.LedgerEvent, error) {
	if limit <= 0 {
		limit = historyDefaultLimit
	}
	events, err := s.lc.EventsFromAsc([]string{}, afterSeq, 0)
	if err != nil {
		return nil, err
	}
	out := make([]proto.LedgerEvent, 0, len(events))
	for _, ev := range events {
		if ev.Type != room.RoomEventType || !room.MentionsMember(ev, member) {
			continue
		}
		out = append(out, ev)
		if len(out) >= limit {
			break
		}
	}
	return out, nil
}

// ListRooms 会话列表（扁平活动排序 + 项目筛选）。派生成员规则随欠账 #3
// 落地；当前返回空切片占位，调用方不得依赖其完整性。
func (s *Service) ListRooms(project string) ([]proto.RoomSummary, error) {
	_ = project
	return []proto.RoomSummary{}, nil
}

// MarkRead 未读游标置位（打开房间即已读）。Ticket 0 空壳。
func (s *Service) MarkRead(member, roomID string, uptoSeq int64) error {
	_ = member
	_ = roomID
	_ = uptoSeq
	return nil
}

// Unread 未读计数。Ticket 0 空壳：恒返回 0。
func (s *Service) Unread(member, roomID string) (int, error) {
	_ = member
	_ = roomID
	return 0, nil
}
```

### T4.4 跑绿与回归（红绿收口）

非红绿步骤（回归动作）。

1. （~2min）跑绿：`go test ./internal/collab/... -v -run 'TestSend|TestPointer'` 全 PASS；再 `go test ./internal/collab/...` 全包 ok。贴原文到台账。
2. （~2min）全量触及回归：`go build ./... && go vet ./internal/collab/...`（plan 轮已实测两命令 EXIT=0）。
3. （~1min）占位残留扫描：`grep -rn "竖切阶段不伪造放行\|Ticket 0 空壳" internal/collab/` 零命中（Pending/Consume/ListRooms/MarkRead/Unread 的占位注释是 C5 的半成品，属本卡允许保留——扫描只认「竖切阶段不伪造放行」这句旧占位文案与 Send/Pointer 的空壳注释）。
4. （~2min）提交（commit 1，message 见 §11）。

### T4.5 图增量、变异复验与收尾

非红绿步骤（图数据是声明 JSON，非行为断言）。

1. （~5min）图增量：`codegraph/best.json` containers 增加两行（下方代码块 A）；`codegraph/diffs/cards-B156.2-charter-4.json` 增加 containersAdded/nodesAdded/edgesAdded（下方代码块 B，nodesAdded 的 `line` 按下方给的行号，**若实现过程行号漂移以最终提交文件的实际符号起始行为准**——机械修正，非占位）。
2. （~2min）图闸：`go run . graph check --repo . --view cards-B156.2-charter-4` 预期 fails=[] EXIT=0（plan 轮已用同形临时视图+临时 best.json 改动实测 fails=[]，台账 P7/P8）。warns 数不作判据。
3. （~2min）变异复验（证明判据能变红）：
   - 变异①：把 `Pointer` 函数体改回 `_ = roomID; _ = msg; return 0, nil` → `go test ./internal/collab/ -run 'TestPointer'` 四支全红（plan 轮实测：Writes「seq 必须为正，got 0」/Overrides「<nil> seq=0」/ReadOnly「got <nil>」/Unknown「got <nil>」）→ **还原**；
   - 变异②：把 `VerifyWriter` default 分支的绑定比对 `actor != r.Card.DriverSession` 改成恒假 → `TestSendCoordinatorKindsRequireBinding` 与 `TestSendRebindRevokesOldSession` 红（plan 轮实测原文：条 4「当前绑定者可发，got err=collab: 书写者与房间身份不符」）→ **还原**。
   - 红文贴台账；两变异均还原后 `go test ./internal/collab/...` 复绿。
4. （~1min）Pointer 函数体 grep 判据：`sed -n '/func (s \*Service) Pointer/,/^}/p' internal/collab/service.go | grep -c "return 0, nil"` 应为 0。
5. （~2min）提交（commit 2，message 见 §11）。

**完整代码块 A——`codegraph/best.json` 的 containers 段追加两行**（在既有 256 行容器映射后追加，id 无冲突）：

```json
  "k_collab_room_fn": "d_collab",
  "k_collab_room_model": "d_collab",
```

**完整代码块 B——`codegraph/diffs/cards-B156.2-charter-4.json` 追加**（在既有 view/base/summary/nodesModified 之外新增四个键；既有键一字不动；行号来自本 plan 的 room.go 排版——Room:36 / Resolve:55 / VerifyWriter:102 / KindAllowed:149 / IsTerminalStatus:161 / SameRoom:188 / MentionsMember:200 / UnmarshalMessage:214）：

```json
 "containersAdded": {
  "k_collab_room_fn": {"label": "collab.room 包级函数", "kind": "函数组", "domain": "d_collab"},
  "k_collab_room_model": {"label": "collab.room 数据模型", "kind": "实体", "domain": "d_collab"}
 },
 "nodesAdded": {
  "n_collab_room_Resolve": {"kind": "func", "container": "k_collab_room_fn", "order": 1, "name": "Resolve", "file": "internal/collab/room/room.go", "line": 55, "signature": "func Resolve(lc client.LedgerClient, roomID string) (*Room, error)", "params": [["lc", "client.LedgerClient", ""], ["roomID", "string", ""]], "returns": "(*Room, error)", "summary": "Resolve 把房间标识解析为执法上下文。"},
  "n_collab_room_VerifyWriter": {"kind": "func", "container": "k_collab_room_fn", "order": 2, "name": "VerifyWriter", "file": "internal/collab/room/room.go", "line": 102, "signature": "func VerifyWriter(r *Room, kind, actor string) error", "params": [["r", "*Room", ""], ["kind", "string", ""], ["actor", "string", ""]], "returns": "error", "summary": "VerifyWriter 按 kind 执法书写者身份。"},
  "n_collab_room_KindAllowed": {"kind": "func", "container": "k_collab_room_fn", "order": 3, "name": "KindAllowed", "file": "internal/collab/room/room.go", "line": 149, "signature": "func KindAllowed(kind string) bool", "params": [["kind", "string", ""]], "returns": "bool", "summary": "KindAllowed kind 是否在受控词表内。"},
  "n_collab_room_IsTerminalStatus": {"kind": "func", "container": "k_collab_room_fn", "order": 4, "name": "IsTerminalStatus", "file": "internal/collab/room/room.go", "line": 161, "signature": "func IsTerminalStatus(status string) bool", "params": [["status", "string", ""]], "returns": "bool", "summary": "IsTerminalStatus 卡终态判据。"},
  "n_collab_room_SameRoom": {"kind": "func", "container": "k_collab_room_fn", "order": 5, "name": "SameRoom", "file": "internal/collab/room/room.go", "line": 188, "signature": "func SameRoom(ev proto.LedgerEvent, roomID string) bool", "params": [["ev", "proto.LedgerEvent", ""], ["roomID", "string", ""]], "returns": "bool", "summary": "SameRoom 判断事件是否属于该房间。"},
  "n_collab_room_MentionsMember": {"kind": "func", "container": "k_collab_room_fn", "order": 6, "name": "MentionsMember", "file": "internal/collab/room/room.go", "line": 200, "signature": "func MentionsMember(ev proto.LedgerEvent, member string) bool", "params": [["ev", "proto.LedgerEvent", ""], ["member", "string", ""]], "returns": "bool", "summary": "MentionsMember 判断事件的 mentions 是否含该成员。"},
  "n_collab_room_UnmarshalMessage": {"kind": "func", "container": "k_collab_room_fn", "order": 7, "name": "UnmarshalMessage", "file": "internal/collab/room/room.go", "line": 214, "signature": "func UnmarshalMessage(raw json.RawMessage, msg *proto.RoomMessage) error", "params": [["raw", "json.RawMessage", ""], ["msg", "*proto.RoomMessage", ""]], "returns": "error", "summary": "UnmarshalMessage 解码房间消息载荷。"},
  "m_collab_room_Room": {"kind": "model", "container": "k_collab_room_model", "order": 8, "name": "Room", "file": "internal/collab/room/room.go", "line": 36, "summary": "Room 是房间解析的完整结果：识别房间形态并携带执法所需全部上下文。", "fields": [["ID", "string", ""], ["CardID", "string", ""], ["Kind", "string", ""], ["Project", "string", ""], ["ReadOnly", "bool", ""], ["Card", "*proto.Card", ""], ["ParentSession", "string", ""]]}
 },
 "edgesAdded": [
  ["n_collab_Service_Send", "n_collab_room_KindAllowed"],
  ["n_collab_Service_Send", "n_collab_room_Resolve"],
  ["n_collab_Service_Send", "n_collab_room_VerifyWriter"],
  ["n_collab_Service_Pointer", "n_collab_room_Resolve"],
  ["n_collab_Service_History", "n_collab_room_SameRoom"],
  ["n_collab_Service_Mentions", "n_collab_room_MentionsMember"],
  ["n_collab_room_Resolve", "m_proto_Card"],
  ["n_collab_room_VerifyWriter", "m_proto_Card"],
  ["n_collab_room_SameRoom", "m_proto_RoomMessage"],
  ["n_collab_room_MentionsMember", "m_proto_RoomMessage"],
  ["n_collab_room_UnmarshalMessage", "m_proto_RoomMessage"]
 ]
```

> 顺序纪律：edgesAdded 的「调用边门控」会按当前源码校验调用文件是否真的 import 对方包（plan 轮实测：代码落地前 service.go 未 import room 时报 6 条完整性问题；代码落地后 validate EXIT=0、check fails=[]）。**因此图增量必须在本卡的代码落地之后、与代码同批或次批提交**，绝不能先在无代码的基线上写边。
> 图覆盖债如实记录：nodesAdded 只覆盖 room 子包**导出**符号；未导出函数（isGroupRoom/groupRoomKind/log）与哨兵 var 不入图（与既有 resolveRoom 等未导出符号同待遇）。best.json 只加容器归属，不改既有 256 行映射、不改 domain 结构。

**测试范围声明（最小化合计）**：本卡只跑 `./internal/collab/...`（主）；`go build ./...`（依赖面回归）；图闸 `go run . graph check --repo . --view cards-B156.2-charter-4`；变异复验限定 `-run 'TestPointer'` 与两个绑定测试。全仓测试不属于本卡。

## 4. 缺陷族对抗审查结论（逐族）

| 族 | 设问 | 结论 |
| --- | --- | --- |
| 生命周期/状态机中断 | 换绑与写入之间有无窗口？并入/拆回会不会让只读判定漂移？ | 无窗口：写权检查读 Resolve 同一快照（ListAllCards 一次取全），RebindDriver 覆写后任何后续 Send 都读新绑定值（C1 的 mutate 事务保证读-判-写原子）；并入判定=Following 非空，拆回后 ListAllCards 不再派生 Following→只读自动解除（契约「拆回解冻」行为面归 C5 列表投影，本卡执法面已就绪）。进程崩溃无半截态：本卡零新写路径 |
| 静默失败/误导报错 | 占位 ErrKindNotAllowed 冒充执法通过的假绿是否根除？ListAllCards 失败会不会被吞成 ErrNoRoom？ | 占位已连根拔（T4.4 扫描零命中）；Resolve 把 ListAllCards 真实错误原样上抛、只有「卡不在列表」才返回 ErrNoRoom，不把基础设施故障伪装成房间不存在 |
| 跨平台假设 | SQL 方言、时间、绑定判据 | 本卡零新 SQL、零时间比较（只读判定用状态与 Following 投影，不依赖时钟）；绑定比对是纯字符串等值 |
| 假红/假绿 | 测试会不会恒绿？空壳放行会不会测不出来？ | plan 轮实测：基线探针恰 10 支红（含 user==绑定值放行的洞、并入不拒的洞、Pointer 空壳），实现后全绿；变异①Pointer 改回空壳→四支 Pointer 全红、变异②去绑定比对→条 4/换绑剥权红——判据有真实变红靶子（台账 P5/P6） |
| 门禁绕过 | 有没有绕过 Send/Pointer 的写路径？哨兵下沉会不会破坏 gateway 映射？ | 无：白名单唯一写入口仍是 Service.Send（pointer 例外走 Service.Pointer），本卡不改 client 接口、不加新出口；哨兵仍是同一变量值（root 再导出同一 *errors.errorString），`==`/`errors.Is` 与既有一致；mux 缺席断言归 C6 |

**序列化边界设问**：本卡不新增手写投影——pointer 消息仍是 proto.RoomMessage 经 `RecordRoomMessage`（appendEvent 内 json.Marshal）落账，读回侧测试 `json.Unmarshal` 断言 `kind==pointer && by_system==true && body/room` 逐字段，**这条穿过真实序列化边界**（产生侧 ledger marshal → 消费侧 test unmarshal）；「字段缺失 vs 值为零」由金样本测试（rooms_fixture_test.go，by_system omitempty 缺席不出键）既有覆盖，本卡不改 DTO 无新投影点。视图 diff JSON 是图元数据不是数据路径。

## 5. 五项检查

1. **缺陷族对抗**：§4 逐族有结论 ✓
2. **序列化边界**：§4 末段，投影点清单+贯穿序列化边界的断言 ✓
3. **上下文预算**：§2 六文件圈得出（四代码/测试 + 两图数据），不插竖切卡 ✓
4. **类型标注**：逻辑型子系统，行为验收全机内可闭环（真 SQLite 夹具）；无真机清单项 ✓
5. **接缝覆盖（双向）**：
   - 测试→缝：service_test.go 全部矩阵/Pointer 测试入口=Service.Send/Service.Pointer（缝#1 入站门面），既有绿测试同入口；无入口偏离 ✓
   - 缝→测试：缝#1 本卡负责的断言面（发送执法 10 条 + merged 只读 + Rebind 剥权 + Pointer 判据一二 + 未知房间）逐条有缝级测试（§3 T4.1 映射表）；群房间 user 非空也锁了 ✓
   - 内部锁：两支字面量 pin（TestRoomEventTypeLiteralMatchesLedger 既有改引用 + TestRoomStatusLiteralMatchesLedger 新增），声明见 §7 ✓
   - 退路同闸：本计划步骤正文无条件退路（无「若意外先绿就改直喂 X」条款）；「若行号漂移按实际修正」是机械修正不是换入口 ✓

## 6. 与 C5 的交界与沉放清单（下游照此写）

协调者押后 C5 等本计划定子包命名——**本计划定稿**：

- **执法内核子包**：`internal/collab/room`（本卡建）。
- **游标子包预留**：`internal/collab/cursor`（未读游标 MarkRead/Unread 的持久化介质，**C5 建包**，本卡不建不占名）。
- **沉放清单（本卡已完成）**：
  - 自 service.go 沉入 room：`resolveRoom→Resolve`、`kindAllowed→KindAllowed`、`isGroupRoom`（未导出）、`sameRoom→SameRoom`、`mentionsMember→MentionsMember`、`unmarshalRoomMessage→UnmarshalMessage`、`protoRoomEventType→RoomEventType`、`roomKind 常量→KindCard/KindProject/KindGlobal`、五个错误哨兵→`room.ErrXxx`（根包再导出）。
  - 根包保留：`Service`、`New`、哨兵再导出、`historyDefaultLimit`、`pointerActor`、各 Service 方法薄编排。
- **C5 可复用符号（签名逐字符）**：`Resolve(lc client.LedgerClient, roomID string) (*Room, error)`、`VerifyWriter(r *Room, kind, actor string) error`、`IsTerminalStatus(status string) bool`、`SameRoom(ev proto.LedgerEvent, roomID string) bool`、`MentionsMember(ev proto.LedgerEvent, member string) bool`、`UnmarshalMessage(raw json.RawMessage, msg *proto.RoomMessage) error`、`const RoomEventType`、`ErrNotBound`。
- **文件级交集**：C5 动 service.go（Pending/Consume/ListRooms/MarkRead/Unread 补肉）+ `internal/collab/cursor/` 新包 + service_test.go 追加——与已落地的本卡代码同文件追加，**rebase 后追加**，不与本卡冲突；若 C5 需要给 room 子包加新符号，追加到既有文件并同步视图 diff（沿用 cards-B156.2-charter-4 视图，不许另起）。

## 7. 内部锁声明（占位符扫描节的一部分，逐条自我声明）

| 测试 | 入口 | 为什么从声明缝构造不出 |
| --- | --- | --- |
| TestRoomEventTypeLiteralMatchesLedger（既有，改引用 room.RoomEventType） | 无缝 | 跨包字符串字面量等式（room 侧 "room_message" == ledger 侧 EvRoomMessage）只能在看见两侧的测试文件里断言；任何 Service 缝都观测不到「两个包的字面量相等」——这与既有同形 pin 测试先例一致（service_test.go:222-226） |
| TestRoomStatusLiteralMatchesLedger（新增） | 无缝 | 同上：room.IsTerminalStatus 的字面量（"已完成"/"终止"）与 ledger.StatusDone/StatusClosed 的等式。终态判定本身已由缝级测试锁行为（TestSendRejectsTerminalCardRoom/TestPointerRejectsReadOnlyRoom），pin 是防两包词表漂移的编译期护栏 |

退路同闸自查：§3 各 task 步骤正文无条件退路。

## 8. 占位符扫描自我声明

全文无 TBD／「加适当的错误处理」／「同 Task N」／描述不做却无代码。代码块均完整可编译，且**全部在本 plan 轮临时装回实测**：红侧（探针 10 支红）、绿侧（`go test ./internal/collab/...` ok）、变异侧（两靶红）证据在台账 L-P1/P3/P5/P6。service_test.go 分两段给出（T4.1 全量 + T4.3 pin 替换段），两段拼接=plan 轮装回验证过的最终形态（旧形 pin 只在 T4.1→T4.3 的窗口内存在，T4.3 后与验证形态一致）。正当出口（「照抄既有 harness」例外）使用情况：**未使用**——测试夹具（newFixture/mustCard/mustBind）既有形态直接沿用，全部代码给出全文。

视图 diff 的 nodesAdded `line`/`order` 字段按本 plan 代码块排版预填，附机械修正说明（实现过程行号漂移按最终文件实际符号行改）——值在提交时确定，非 TBD。

## 9. 跨卡审计输入（L3 重档：扇出前由协调者组织独立上下文审计；本节提供素材并自审待拍板）

**跨卡签名清单（本卡 Produces / 相关 Consumes，逐字符）**：

| 签名 | 产出方 | 消费方 | 一致性 |
| --- | --- | --- | --- |
| `Send(roomID string, msg proto.RoomMessage, actor string) (int64, error)` | 本卡（签名不变） | C6 HTTP handler、C7 room 命令族 | 与契约 §3.3 一字一致；哨兵返回值不变 |
| `Pointer(roomID string, msg proto.RoomMessage) (int64, error)` | 本卡（签名不变，身体填肉） | C7 派发指针（card_dispatch.go）、B156.3 narrator | 置位在内部：C7 断言 kind==pointer ∧ by_system==true ∧ 正文含卡号与节点名（不查 actor——actor 固定 "system:pointer"） |
| 哨兵 `ErrNoRoom/ErrKindNotAllowed/ErrReadOnly/ErrNotWriter/ErrNotBound` | 本卡（root 再导出同值） | C6 错误映射 400/403/409/404 表 | 变量同一性保持，`==`/`errors.Is` 不破坏 |
| `room.Resolve` 等七符号 + `RoomEventType`（§6 清单） | 本卡 | C5 | C5 消费侧签名照 §6 抄写，勿加别名类型 |
| 并入只读判定来源=ListAllCards.Following | 本卡（Resolve） | C5（ListRooms 沉底/只读投影共用同一投影） | 与契约 §11.4 一致：GetCard 恒空，不得在 C5 里假设 GetCard 判并入 |

**spec 故事归属（本卡）**：故事 2（开卡→房间落指针）的指针落点本卡实现（Pointer 身体），C7 接派发触发；故事 5（换绑剥权）的写权剥失本卡合取断言（C1 覆写 + Send 读当前值）；故事 4/6 的书写者矩阵（父子 relay、@）本卡执法。故事 1/3 的读面（列表/收件箱/收口）归 C5/C6/C7/C8。

**图覆盖债**：本卡产出物（room 子包）在基线图里无节点——T4.5 视图增量是本卡的图登记动作；基线 collab 未导出助手本就缺图（既有 debt），本卡不扩大。已如实记录。

## 10. 自审三查

1. **spec 覆盖**：契约 §4 发送执法 10 条 + merged 只读 + Rebind 剥权 + Pointer 判据一二 + 门面禁令逐条指到 T4.1 测试/T4.5 图闸，无遗漏；不属本卡的半边（Pending/Consume 等 C5、mux 缺席 C6、派发触发 C7）显式移交 ✓
2. **占位符扫描**：§8 ✓
3. **跨 task 签名一致性**：§9 清单；Send/Pointer/哨兵与冻结签名逐字符一致（service.go:55-86 亲读），room 子包新符号在 §6 全列 ✓

## 11. 提交切分

- commit 1（T4.1-T4.4）：`feat(collab): Send 白名单执法全矩阵 + Pointer 实现 + 根包瘦身（B156.2.4 欠账#1+#11实现半）`——含 service_test.go、room/errors.go、room/room.go、service.go。
- commit 2（T4.5）：`graph(b156.2.4): room 子包容器与符号入图（best+charter-4 视图增量）`——含 codegraph/best.json、codegraph/diffs/cards-B156.2-charter-4.json。
- 台账 `docs/superpowers/ledgers/ledger-b156.2.4-plan.md` 随实现轮持续追加，与产出物同批提交。

## 12. 明确不做（防越界）

- 不写 collab.Pending/Consume/ListRooms/MarkRead/Unread（C5）；不建 cursor 子包。
- 不改 proto、types.go 词表、schema、target.json、金样本 fixture（rooms_fixture_test.go / rooms.test.ts 一字不动）。
- 不 absorb 视图（finish 阶段动作）；不修裸 graph check 的既有 dead（absorb 前固有形态）。
- 不新增 target.json 预定声明（d_gateway/d_cli→d_collab 归 C6/C7 同提交写）。
- 不加真机清单项（本卡全行为机内可闭环）。