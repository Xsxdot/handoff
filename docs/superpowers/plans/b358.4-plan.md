# B358.4（d_gateway）会话 HTTP 端点接线——实现计划

- **卡**：B358.4（B358 拆解 S4，L3 重档子系统执行者依契约自写、协调者只审）
- **依据**：contract `docs/superpowers/specs/b358-contract.md`（§3.3/§3.5 门面签名、§3.7 会话房间形态、§4.1 条 1–13、§4.6 条 39–50 已由 B358.3 落地、§8.3 欠账 3、§9 附区）＋ breakdown `docs/superpowers/specs/b358-breakdown.md` §3.5（S4 四段式）＋ spec `docs/superpowers/specs/b358.md` §4.1/§4.4、§6 接缝 #1/#5 gateway 侧
- **前置已合入**：B358.1（d92f0b92，S1 会话语义收口）、B358.2（77affbb5，S2 投影精化）、B358.3（75adb4eb，S3 唤醒改接 + session wait 通道）——本计划全部判据对当前工作树（`cards/B233.1-charter-7` @ `75adb4eb`）实测，非对冻结时点的旧读数
- **图覆盖债**：本卡触及面整体不在 `codegraph/best.json`（探针原文见台账）——一律 `file#Symbol` 源码锚；新增/修改符号清单在 Task 4 落台账，交 integrate/recon 补视图 diff
- **台账**：`docs/superpowers/ledgers/2026-09-12-b358.4-plan-ledger.md`

## 0. 本卡干什么（一段话）

把 Ticket 0 已有的 collab.Service 会话门面接成控制台可消费的 HTTP 面：**会话建/列/详情/归档/拉卡/移出**六个端点注册进 `registerLedgerRoutes` 同一 mux（新建 `sessionsapi.go`）；**发言、已读、历史复用既有 `/api/rooms` 端点**（会话房间经 B358.1 已可读写，零新增）；错误映射沿 collab 哨兵族，本计划定稿映射表与路由命名（§2，随后写进验收作字面断言）。双重职责：① 按 B358.1 拍板②，把划归本卡的 **agentd 红窗 10 支**（`roomsapi_test.go` 旧房间发言成功断言）按新会话语义改写收口为绿；② 吸收 **B358.1 review 移交两支**（collab 直测补齐）。B362 golden `TestLegacyNodeEventSequenceUnchanged` 与 cmd 全部红窗**禁触**。

## 0.1 有界文件集（本卡只许碰这些文件）

**生产（2 个）**：
- 新建 `internal/agentd/sessionsapi.go`——六个 handler + `sessionErr` 错误映射
- `internal/agentd/ledgerapi.go`——仅 `registerLedgerRoutes` 内 rooms 注册块后追加 6 行（§2.1 路由表逐字）

**测试（3 个）**：
- 新建 `internal/agentd/sessionsapi_test.go`——新端点测试族
- `internal/agentd/roomsapi_test.go`——**仅** §0.4 红窗 10 支就地改写（文件头注释同步一句改写说明）；其余测试一字不动（含存活测试 TestRoomSendErrMapping / TestRoomsEndpoints503WithoutLedger 及全部 attach/inbox 非红窗测试）
- `internal/collab/sessions_test.go`——末尾追加移交两支测试 + 一个 stub 类型；既有内容不动

**禁触（越界即回退）**：
- `internal/agentd/wakeconsumer*.go`（S3 已收口；B358.3 红窗改写 5 支已是绿）
- `cmd/**` 全部（含 `cmd/session.go`、`cmd/room_test.go` 的 6 支 cmd 红窗——归 S5）
- `TestLegacyNodeEventSequenceUnchanged`（B362 golden，`scheddispatch_test.go`）——禁触禁改
- `internal/collab` 生产文件、`internal/ledger/**`、`internal/proto/**`（S1/Ticket 0 已收口）
- `web/**`（S6）、`internal/agentd/auth_test.go`（4 支 `TestSession*` 是 auth 域，与协作会话同名不同物）
- 工作树无关脏文件（`web/src/app/task/Composer.test.tsx`、`.commandcode/`、`web/design-qa*` 等）不卷入提交

## 0.2 现状依赖行为（必须按出处实现；行号为 2026-09-12 读数，以符号锚为准）

| # | 事实 | 出处 |
|---|---|---|
| 1 | `collabErr` 现有映射：ErrNoRoom/ErrKindNotAllowed→400、ErrNotWriter→403、ErrReadOnly/ErrNotBound→409、默认 500；**无 ErrBadState 格** | `internal/agentd/roomsapi.go#collabErr`（:32-43） |
| 2 | `mapSessionError` 把 client.ErrNotFound 归一为 collab.ErrNoRoom；SessionDetail/ArchiveSession/JoinCard/LeaveCard 四个门面方法均经它出栈 | `internal/collab/sessions.go#mapSessionError`（:452-462） |
| 3 | `translateNotFound` 只翻 ErrNotFound→client.ErrNotFound，**ErrBadState 原样透传**——gateway 用 `errors.Is(err, ledger.ErrBadState)` 判定（agentd 本就 import ledger），无需新哨兵 | `internal/ledger/api/api.go#translateNotFound`（:147-155） |
| 4 | JoinCardToSession：已属他会话→ErrBadState（文案「卡 %s 已属会话 %s」）、会话已归档→ErrBadState（「会话 %s 已归档」）、卡不存在→裸 ErrNotFound（经 Facade 翻译）、会话不存在→ErrNotFound（文案含会话 id）；同会话重复 join 幂等 | `internal/ledger/sessions.go#JoinCardToSession`（:168-200）、`#getSessionTx`（:292-298）、`getCardTx`（cards.go:374-380） |
| 5 | ArchiveSession 幂等（已归档短路返回 nil，B358.1 拍板①）；LeaveCardToSession 幂等（不在会话内返回 nil）；GetSession 不存在→ErrNotFound | `internal/ledger/sessions.go#ArchiveSession`（:140-162）、`#LeaveCardToSession`（:204-231） |
| 6 | `Send` 会话路径：Resolve 会话分支只解析形态不查会话表 → `sendToSession` 查会话本体：GetSession 失败→ErrNoRoom、已归档→ErrReadOnly、成员执法→ErrNotWriter；会话消息落**无卡事件**（CardID=""），读它必须全流读（`EventsFromAsc(nil, …)`） | `internal/collab/service.go#Send`（:86-101）、`#sendToSession`（:135-167）、`internal/collab/room/room.go#Resolve`（:59-70） |
| 7 | `sessionWriters`＝显式成员 ∪ 会话内卡当前席位；ListAllCards 读失败向上传播、注释明言「不伪装成 ErrNotWriter」——移交①的源码事实 | `internal/collab/service.go#sessionWriters`（:103-129） |
| 8 | owner 写入 Members 初值（CreateSession 落 owner 进显式成员）→ 控制台成员作 owner 即天然在书写者集内 | `internal/ledger/sessions.go#CreateSession`（:39-75，`Members: []string{owner}`） |
| 9 | 会话 DTO wire 键集已冻结；`nodes/timeline/preview/members/cards/last_active/seat` 均 omitempty（缺失≠零值的分辨依据） | `internal/proto/sessions.go`（:75-138）；金样本 `internal/proto/sessions_fixture_test.go`（TestSessionSummaryGoldenProjection / TestSessionDetailGoldenProjection 等 6 支） |
| 10 | rooms 注册块五行在 `registerLedgerRoutes`；`withRooms` 守卫（ledger/rooms 缺一→503） | `internal/agentd/ledgerapi.go:49-53`、`internal/agentd/server.go:2767-2771` |
| 11 | HTTP 测试夹具：`newRoomsEnv`＝真 SQLite 账本 + seed workflow "bug" + SetupAutomation；`webUserMember="web:127.0.0.1"`（httptest 固定 127.0.0.1 → roomUserActor 恒此值）；`ledgerGet/ledgerPost/env.getJSON` 全部穿真实 httptest server | `internal/agentd/roomsapi_test.go:23-36`、`ledgerapi_test.go:25-52/134` |
| 12 | B358.3 的 agentd 测试形态参照：真 Facade + 真会话夹具 + 断言穿 `env.srv.rooms`/`env.ledger`；P4 统一记法 `user:tester` 已在 collab 级夹具使用（owner 记法先例） | `internal/agentd/wakeconsumer_b358_test.go:26-59` |

## 0.3 图覆盖债

本节点亲跑 `codegraph sym`：`collab.Service.CreateSession/ListSessions/SessionDetail/ArchiveSession/JoinCard/LeaveCard`、`collab.sessionWriters`、`collab.mapSessionError`、`agentd.collabErr`、`agentd.handleRoomSend`、`agentd.registerLedgerRoutes`、`proto.SessionDetail` 共 12 符号全部「不在图中」（原文见台账）。与 breakdown §6 同欠。本计划一律 `file#Symbol` 源码锚；本卡新增符号（sessionsapi.go 六 handler + sessionErr、sessionsapi_test.go 测试族、roomsapi_test.go 改写符号、sessions_test.go 两支新测试）在 Task 4 落清单，交 integrate/recon 补 `codegraph/diffs` 视图。

## 0.4 红窗基线与核算方式

**基线红集合（2026-09-12 @ `75adb4eb` 工作树实测，恰 19 支；原文与逐支名单见台账与 §1）**：

`internal/agentd`（11 支）：
1. `TestRoomsListEndpoint` 2. `TestRoomsListUnreadAndAttachProjection` 3. `TestRoomMessagesEndpoint` 4. `TestRoomSendEndpoint` 5. `TestRoomReadEndpoint` 6. `TestInboxThreeSources` 7. `TestInboxMentionSource` 8. `TestInboxRefIDShapes` 9. `TestInboxGoldenKeyShapes` 10. `TestInboxIntegrationSmoke` ——前 10 支 = B358.1 plan §0.4 红窗（旧房间发言成功断言），**改写责任归本卡**；
11. `TestLegacyNodeEventSequenceUnchanged` ——B362 golden，**禁触禁改**。

`cmd`（8 支，全部禁触归 S5/既有）：12. `TestRoomSendLandsRoomMessageWithUserKind` 13. `TestRoomSendCoordinatorAcceptsExplicitSeatFlags` 14. `TestRoomSendCoordinatorKindUsesGrokHostSession` 15. `TestRoomSendCarriesRefAndMention` 16. `TestRoomReadStdoutSeqPrefix` 17. `TestRoomListSortedByActivity` 18. `TestRepoContractGate` 19. `TestServePermissionHookDenyWithReasonAndStep0`（macOS 环境性失败）。

**存活确认**（不得误改）：`TestRoomSendErrMapping`、`TestRoomsEndpoints503WithoutLedger`（agentd）；`TestRoomSendUserRejectsSeatFlagsWithoutSideEffects`、`TestRoomInboxWalksAgentdHTTP`（cmd，禁触面）。

**本卡核算规则（与 B358.3 §3 同款）**：

1. **改写的 10 支不算既有红**：Task 2 就地改写后必须**收口为绿**（名字全部保留，逐支改写说明写进测试注释；红因是「旧房间发言成功断言随 S1 失效」，不是回归）。
2. **收口判据**：`go test ./internal/agentd -count=1` 失败集合 = **恰** `{TestLegacyNodeEventSequenceUnchanged}`，逐支同名无增无减；`go test ./cmd -count=1` 失败集合 = **恰** 基线 8 支逐支同名。多出一支 = 本卡波及面清点漏项 → 停手上报协调者（修复熔断），不得顺手改越界文件；少一支（意外修复）同样上报对账。
3. **TestRepoContractGate 违规清单只同不增**：收口时跑该测试并逐行比对违规清单与基线 6 条（1 dead-interface + 5 over-budget）——**不得新增**任何一条（本卡零新增跨域 import：agentd 已 import collab/ledger/proto/room）；基线条目不得少（少了 = 意外修复，同样上报对账）。
4. **B362 golden 禁触**：`TestLegacyNodeEventSequenceUnchanged` 在收口时必须仍以原红因在失败集合中（不是本卡职责，修复归 B362 charter:debug）。
5. **collab/ledger/proto 三包保持绿**：`go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` 退出 0。

## 1. 已亲跑的基线与判据

以下命令在基线（`75adb4eb` 工作树）亲跑，结果记入台账。执行者在 Task 0 复跑同组命令，读数不同先停下报告，不得带着意外继续：

| 命令 | 基线结果（2026-09-12 实测） |
| --- | --- |
| `go build ./...` | 退出 0 |
| `go test ./internal/agentd -count=1` | FAIL，失败恰 §0.4 的 11 支（10 红窗 + B362 golden） |
| `go test ./cmd -count=1` | FAIL，失败恰 §0.4 的 8 支 |
| `go test ./cmd -run TestRepoContractGate -count=1 -v` | FAIL，违规恰 6 条（清单见 §0.4 规则 3 与台账） |
| `go test ./internal/agentd -run 'TestSession' -count=1` | ok——恰 auth 域 4 支（TestSessionFailureReasons / TestSessionRoutesListRevokeLogout / TestSessionCannotIssueTicket / TestSessionCookieAttrsStayInSync）；本卡新端点测试同入此 filter，它们保持绿 |
| `go test ./internal/collab/... -count=1` | ok |
| `grep -rn "api/sessions" internal/ cmd/ --include="*.go" \| grep -v _test` | 零命中——路由命名无冲突 |

判据先行的代码级确证（读码得出，出处 0.2#2/3/6）：基线上「会话房间不存在时经 Send 发言」**返回 ErrNoRoom 但无直测**（移交②）、「sessionWriters 读失败向上传播」**仅源码成立**（移交①）、「POST /api/sessions」**404**（路由不存在）——Task 1/3 的测试在基线必红或缺位，红/缺位是有意义的。

## 2. 终局形状（plan 定稿，随后写进验收作字面断言）

### 2.1 路由表（字面断言一：mux 注册行）

全部经 `withRooms` 守卫（未装配 503，与 rooms 五端点同款），注册进 `registerLedgerRoutes` rooms 块后：

```go
api.HandleFunc("POST /api/sessions", s.withRooms(s.handleSessionCreate))
api.HandleFunc("GET /api/sessions", s.withRooms(s.handleSessionsList))
api.HandleFunc("GET /api/sessions/{id}", s.withRooms(s.handleSessionDetail))
api.HandleFunc("POST /api/sessions/{id}/archive", s.withRooms(s.handleSessionArchive))
api.HandleFunc("POST /api/sessions/{id}/cards", s.withRooms(s.handleSessionJoinCard))
api.HandleFunc("DELETE /api/sessions/{id}/cards/{cardID}", s.withRooms(s.handleSessionLeaveCard))
```

**发言、已读、历史零新增**：`POST /api/rooms/{id}/messages`、`POST /api/rooms/{id}/read`、`GET /api/rooms/{id}/messages` 直接以 `session:<n>` 作 `{id}` 使用（会话房间经 B358.1 已可读写——`collab.Service.Send` 会话路径 + `room.SameRoom` 会话分支既有）。

### 2.2 错误映射表（字面断言二：哨兵 → HTTP 状态）

**新映射 `sessionErr`（只覆盖会话写面六端点可达的哨兵，不与其他哨兵混流——缺陷族 7）**：

| 出口哨兵（经 collab.Service） | HTTP | 可达路径 | 断言支 |
|---|---|---|---|
| `collab.ErrNoRoom`（含 `mapSessionError` 归一的 client.ErrNotFound） | **404** | 详情不存在会话；JoinCard 不存在卡/会话；Archive 不存在会话；Leave 不存在会话 | TestSessionDetailEndpointKeyset / TestSessionJoinCardEndpoint / TestSessionLeaveCardEndpoint |
| `ledger.ErrBadState`（已属他会话 / 会话已归档，`errors.Is` 直判——0.2#3 透传事实） | **409** | JoinCard 对已属他会话的卡；JoinCard 对已归档会话 | TestSessionJoinCardEndpoint / TestSessionArchiveEndpoint |
| 其他（账本写失败等） | 500 | — | 不写声称验证它的测试（与 roomsapi.go:296-300 既有裁决同款纪律） |

**发言/已读路径沿用既有 `collabErr`（不改）**：ErrNoRoom→400（TestRoomSendErrMapping 存活锁 + Task 2 补会话房间同断言）、ErrNotWriter→403、ErrReadOnly→409。breakdown 缺陷族 2 的「ErrNoRoom 对会话列表/详情必须 404 且文案含会话 id」由 `getSessionTx` 文案（「会话 %s: …」）+ `writeErr` 原样透出满足；「History 404 分支今天不可达、不写假测试」既有裁决沿用。

### 2.3 身份注入（P4 的 gateway 半边，本计划定稿）

- **群主（owner）＝ `roomUserActor(r)`（`web:<host>`，服务端注入）**。请求体只收 `title`；body 里出现的 `owner`/`actor` 字段一律忽略（结构上不解码——反例断言见 Task 1）。
- 依据：P4 拍板 A 字面「gateway/CLI 既有 actor 注入面不动，仅在会话成员匹配与 mention 解析层使用统一记法」——gateway 的成员维度（未读游标、mention 源、已读）今天就是 `roomUserActor`（既有注入面）；owner 进 `Members`（0.2#8）后控制台成员天然在书写者集内，S6 控制台建会话即可发言/收 @/置已读，全链零新机制。CreateSession 本就把 owner 落进显式成员，P4「开会话时落显式成员」由此满足，无需 AddSessionMember 调用。
- 统一记法（`user:<name>`/`agent:<name>`）的注入点归 CLI 半边（S5 定稿，先例 `internal/agentd/wakeconsumer_b358_test.go#mustWakeSessionFixture` 的 `user:tester`）与 mention 解析层（S3 已落）。
- **备选（被否）**：body 收 `owner`（统一记法）。被否理由：违反 breakdown 缺陷族 5 字面「actor 一律服务端注入，请求体不带身份字段」的会话版，且控制台无统一身份来源（S6 范围）。若协调者翻转：`handleSessionCreate` 解码体 +owner 校验、TestSessionsCreateEndpoint 两处断言翻转，其余不动。

### 2.4 响应形状

- `POST /api/sessions` → 200 + `proto.Session` 全文（键含 `id`（形如 `session:<n>`）/`title`/`owner`/`archived`/`created_at`…）。
- `GET /api/sessions` → 200 + `{"sessions": [SessionSummary…]}`（member 维度未读投影在行上）。
- `GET /api/sessions/{id}` → 200 + `proto.SessionDetail` 全文（键集字面断言见 Task 1 验收）。
- `POST …/archive`、`POST …/cards`、`DELETE …/cards/{cardID}` → 200 + `{"ok": true}`。

## 3. Task 0：基线复跑与红窗清点（不写码）

### 文件范围

无代码改动；读数落台账。

### 步骤

1. 复跑 §1 表格七条命令，逐条与基线读数比对。**任何一条不符 → 停手上报**（判据错了的代价大于实现错）。
2. verbose 枚举两处红窗（`go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1 -v`、`go test ./cmd -run TestRoom -count=1 -v`），失败名单与 §0.4 逐支比对，确认存活测试全绿。
3. 台账记 Task 0 读数（含 TestRepoContractGate 违规 6 条原文，收口比对要用）。

### Task 0 验收

- §1 七条读数与基线一致；红窗名单逐支同名；存活测试绿；台账已记。

## 4. Task 1：会话六端点（sessionsapi.go + 注册 + sessionsapi_test.go，红→绿）

### 文件范围

新建 `internal/agentd/sessionsapi_test.go`、新建 `internal/agentd/sessionsapi.go`、`internal/agentd/ledgerapi.go`（仅注册 6 行）。

### Interfaces

**Consumes（既有，逐字）**：

```go
// internal/collab/sessions.go / service.go —— 入站门面（spec §6 缝 #1）
func (s *Service) CreateSession(title, owner, actor string) (proto.Session, error)
func (s *Service) ListSessions(member string) ([]proto.SessionSummary, error)
func (s *Service) SessionDetail(id string) (proto.SessionDetail, error)
func (s *Service) ArchiveSession(id, actor string) error
func (s *Service) JoinCard(sessionID, cardID, actor string) error
func (s *Service) LeaveCard(sessionID, cardID, actor string) error
var ErrNoRoom / ErrReadOnly / ErrNotWriter / ErrKindNotAllowed / ErrNotBound  // collab 哨兵
// internal/ledger/ledger.go
var ErrBadState = errors.New("ledger: 当前状态不允许该操作")
// internal/agentd 既有
func (s *Server) roomUserActor(r *http.Request) string                        // "web:"+hostOnly
func writeJSON(w http.ResponseWriter, status int, v any)
func writeErr(w http.ResponseWriter, code int, err error)                     // {"error": "..."}
func (s *Server) withRooms(h http.HandlerFunc) http.HandlerFunc               // 503 守卫
```

**Produces（本卡新增，交 S6 消费与 recon 入图）**：

```go
// internal/agentd/sessionsapi.go
func (s *Server) handleSessionCreate(w http.ResponseWriter, r *http.Request)   // POST /api/sessions
func (s *Server) handleSessionsList(w http.ResponseWriter, r *http.Request)    // GET /api/sessions
func (s *Server) handleSessionDetail(w http.ResponseWriter, r *http.Request)   // GET /api/sessions/{id}
func (s *Server) handleSessionArchive(w http.ResponseWriter, r *http.Request)  // POST /api/sessions/{id}/archive
func (s *Server) handleSessionJoinCard(w http.ResponseWriter, r *http.Request) // POST /api/sessions/{id}/cards
func (s *Server) handleSessionLeaveCard(w http.ResponseWriter, r *http.Request)// DELETE /api/sessions/{id}/cards/{cardID}
func sessionErr(w http.ResponseWriter, err error)                              // §2.2 映射表
// internal/agentd/ledgerapi.go —— registerLedgerRoutes 追加 §2.1 六行
```

### 步骤（红绿只套在锁缝断言上；本任务一条红绿周期贯穿）

**步骤 1（红）**：新建 `internal/agentd/sessionsapi_test.go`，写入下列全部测试与夹具（完整代码，直接落）：

```go
// sessionsapi_test.go —— B358.4 会话 HTTP 端点缝级测试。
//
// 职责：六端点全部从真实 mux 注册进 httptest（ledgerGet/ledgerPost 穿
// httptest.NewServer——「空壳冒充接线」的形状禁令，breakdown S4 缺陷族 4），
// 调用链穿过 collab 入站门面（spec §6 缝 #1 的调用方侧）。断言：§2.1 路由
// 形状、§2.2 错误映射表逐格、proto.SessionDetail 键集金样本、缺陷族 5 两反例
// （请求体塞 actor 被忽略；伪造 member 查询参数不影响本人未读）。
// 边界：不测 collab 门面内部语义（S1 已收口）；不测 TS 半边（S6 孪生金样本）。
package agentd

import (
	"encoding/json"
	"fmt"
	"strings"
	"testing"

	"github.com/Xsxdot/handoff/internal/ledger"
	"github.com/Xsxdot/handoff/internal/proto"
)

// mustConsoleSession 经 HTTP 建一场会话（owner=roomUserActor=webUserMember），
// 返回会话投影。owner 即控制台成员——web actor 因此天然在书写者集内
// （collab.Service.sessionWriters 显式成员半边），控制台发言/已读/收 @ 全链可用。
func mustConsoleSession(t *testing.T, env *ledgerEnv, title string) proto.Session {
	t.Helper()
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/sessions", fmt.Sprintf(`{"title":%q}`, title))
	if code != 200 {
		t.Fatalf("POST /api/sessions: %d %s", code, body)
	}
	var session proto.Session
	if err := json.Unmarshal([]byte(body), &session); err != nil {
		t.Fatalf("解码建会话响应: %v", err)
	}
	if !strings.HasPrefix(session.ID, "session:") {
		t.Fatalf("会话 id 应形如 session:<n>: %q", session.ID)
	}
	return session
}

// TestSessionsCreateEndpoint 锁：建会话 200 + session:<n> id + 群主服务端注入。
// 缺陷族 5 反例（同支内）：body 塞 owner/actor 字段一律忽略；空标题 400。
func TestSessionsCreateEndpoint(t *testing.T) {
	env := newRoomsEnv(t)
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/sessions",
		`{"title":"需求对齐","owner":"user:mallory","actor":"user:mallory"}`)
	if code != 200 {
		t.Fatalf("POST /api/sessions: %d %s", code, body)
	}
	var session proto.Session
	if err := json.Unmarshal([]byte(body), &session); err != nil {
		t.Fatal(err)
	}
	if !strings.HasPrefix(session.ID, "session:") {
		t.Fatalf("响应应含 session:<n> 形 id: %q", session.ID)
	}
	if session.Owner != webUserMember {
		t.Fatalf("群主必须服务端注入（body 的 owner 字段被忽略）: %q", session.Owner)
	}
	// 反例落账核对：EvSessionCreated 的 actor 也是服务端注入值，不是 body 塞的串。
	events, err := env.ledger.EventsFromAsc(nil, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	created := false
	for _, ev := range events {
		if ev.Type != ledger.EvSessionCreated {
			continue
		}
		created = true
		if ev.Actor != webUserMember {
			t.Fatalf("建会话 actor 应服务端注入 %q，实得 %q", webUserMember, ev.Actor)
		}
	}
	if !created {
		t.Fatal("EvSessionCreated 应已落账")
	}
	// 空标题拒绝（handler 卫生检查，与 handleCardCreate 同款）。
	if code, _ = ledgerPost(t, env.testAgentdEnv, "/api/sessions", `{"title":"  "}`); code != 400 {
		t.Fatalf("空标题应 400: %d", code)
	}
}

// TestSessionsListUnreadAndMemberForgery 锁：列表含新行、Kind=session、
// 两条无 @ 消息 → Unread=2（breakdown S4 ③ 判据）；缺陷族 5 反例：伪造
// ?member= 查询参数不影响本人未读投影（member 维度只认服务端注入）。
func TestSessionsListUnreadAndMemberForgery(t *testing.T) {
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "未读对账场")
	for _, body := range []string{"第一条", "第二条"} {
		code, resp := ledgerPost(t, env.testAgentdEnv,
			"/api/rooms/"+session.ID+"/messages", fmt.Sprintf(`{"body":%q}`, body))
		if code != 200 {
			t.Fatalf("会话发言 %q: %d %s", body, code, resp)
		}
	}
	var out struct {
		Sessions []proto.SessionSummary `json:"sessions"`
	}
	// member 查询参数是伪造面：handler 只认 roomUserActor。
	code, body := ledgerGet(t, env.testAgentdEnv, "/api/sessions?member=user:mallory")
	if code != 200 {
		t.Fatalf("GET /api/sessions: %d %s", code, body)
	}
	if err := json.Unmarshal([]byte(body), &out); err != nil {
		t.Fatal(err)
	}
	var found *proto.SessionSummary
	for i := range out.Sessions {
		if out.Sessions[i].ID == session.ID {
			found = &out.Sessions[i]
		}
	}
	if found == nil {
		t.Fatalf("列表应含新会话: %+v", out.Sessions)
	}
	if found.Kind != proto.SessionKind {
		t.Fatalf("行 Kind 应为 session: %q", found.Kind)
	}
	if found.Title != "未读对账场" || found.Owner != webUserMember {
		t.Fatalf("标题/群主投影错误: %+v", *found)
	}
	if found.Unread != 2 {
		t.Fatalf("两条无 @ 消息 → Unread=2（伪造 member 参数不得改写本人未读）: %d", found.Unread)
	}
}

// TestSessionDetailEndpointKeyset 锁：详情响应键集与 proto.SessionDetail 金样本
// 逐键一致（handler 不手抖改键——序列化边界断言穿真实 HTTP，不是本地 marshal）；
// omitempty 三态语义（nodes 无 task_mirrored 时**缺键**而非 null）；不存在会话
// → 404 且文案含会话 id（breakdown 缺陷族 2）。
func TestSessionDetailEndpointKeyset(t *testing.T) {
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "详情键集场")
	card := seedCard(t, env, "进群卡")
	if code, body := ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards",
		fmt.Sprintf(`{"card":%q}`, card.ID)); code != 200 {
		t.Fatalf("拉卡进群: %d %s", code, body)
	}
	if code, body := ledgerPost(t, env.testAgentdEnv, "/api/rooms/"+session.ID+"/messages",
		`{"body":"键集场的第一条"}`); code != 200 {
		t.Fatalf("会话发言: %d %s", code, body)
	}
	code, body := ledgerGet(t, env.testAgentdEnv, "/api/sessions/"+session.ID)
	if code != 200 {
		t.Fatalf("GET /api/sessions/{id}: %d %s", code, body)
	}
	var raw map[string]json.RawMessage
	if err := json.Unmarshal([]byte(body), &raw); err != nil {
		t.Fatal(err)
	}
	// 顶层键集：summary 恒在；timeline 有结构事件在场；nodes 无 task_mirrored
	// 事件 → omitempty 缺键（缺失≠null 的字面分辨）。
	assertKeyset(t, "SessionDetail 顶层", raw, "summary", "timeline")
	if _, ok := raw["nodes"]; ok {
		t.Fatalf("nodes 无镜像事件应缺键（omitempty），实得: %v", raw["nodes"])
	}
	var summaryRaw map[string]json.RawMessage
	if err := json.Unmarshal(raw["summary"], &summaryRaw); err != nil {
		t.Fatal(err)
	}
	assertKeyset(t, "SessionSummary", summaryRaw,
		"id", "kind", "title", "owner", "archived", "unread", "needs_human",
		"last_activity", "preview", "members", "cards")
	var members []map[string]json.RawMessage
	if err := json.Unmarshal(summaryRaw["members"], &members); err != nil {
		t.Fatal(err)
	}
	if len(members) == 0 {
		t.Fatal("显式成员（群主）应在 members 投影")
	}
	assertKeyset(t, "SessionMember[0]", members[0], "identity", "kind", "status")
	var cards []map[string]json.RawMessage
	if err := json.Unmarshal(summaryRaw["cards"], &cards); err != nil {
		t.Fatal(err)
	}
	if len(cards) != 1 {
		t.Fatalf("详情 cards 应含进群卡恰一张: %v", cards)
	}
	assertKeyset(t, "SessionCard[0]", cards[0], "card_id", "title", "status")
	var cardID string
	if err := json.Unmarshal(cards[0]["card_id"], &cardID); err != nil {
		t.Fatal(err)
	}
	if cardID != card.ID {
		t.Fatalf("详情 cards 应含该卡: %q != %q", cardID, card.ID)
	}
	var seat string
	if err := json.Unmarshal(cards[0]["seat"], &seat); err == nil && seat != "" {
		t.Fatalf("进群≠配人：seat 应缺键或空（空座虚线），实得 %q", seat)
	}
	// breakdown 缺陷族 2：ErrNoRoom 对详情必须 404 且文案含会话 id。
	code, body = ledgerGet(t, env.testAgentdEnv, "/api/sessions/session:999")
	if code != 404 {
		t.Fatalf("不存在会话详情应 404: %d %s", code, body)
	}
	if !strings.Contains(body, "session:999") {
		t.Fatalf("404 文案应含会话 id（可行动错误）: %s", body)
	}
}

// assertKeyset 断言 decoded JSON 对象的键集与期望逐键一致（多键少键都红）。
func assertKeyset(t *testing.T, label string, obj map[string]json.RawMessage, want ...string) {
	t.Helper()
	if len(obj) != len(want) {
		t.Fatalf("%s 键数应 %d 实得 %d: %v", label, len(want), len(obj), obj)
	}
	for _, k := range want {
		if _, ok := obj[k]; !ok {
			t.Fatalf("%s 缺键 %q: %v", label, k, obj)
		}
	}
}

// TestSessionJoinCardEndpoint 锁：拉卡成功 + 详情含卡 + 幂等（条 10）+
// 已属他会话 409（ErrBadState，文案可行动）+ 不存在卡/会话 404（§2.2 映射表）。
func TestSessionJoinCardEndpoint(t *testing.T) {
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "拉卡场")
	card := seedCard(t, env, "进群卡")
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards",
		fmt.Sprintf(`{"card":%q}`, card.ID))
	if code != 200 {
		t.Fatalf("拉卡进群: %d %s", code, body)
	}
	// 同会话重复 join 幂等（契约条 10）。
	if code, _ = ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards",
		fmt.Sprintf(`{"card":%q}`, card.ID)); code != 200 {
		t.Fatalf("同会话重复拉卡应幂等 200: %d", code)
	}
	// 已属其它会话 → 409 且文案含「已属会话」（可行动错误文案）。
	other := mustConsoleSession(t, env, "另一场")
	code, body = ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+other.ID+"/cards",
		fmt.Sprintf(`{"card":%q}`, card.ID))
	if code != 409 {
		t.Fatalf("已属他会话应 409: %d %s", code, body)
	}
	if !strings.Contains(body, "已属会话") {
		t.Fatalf("409 文案应可行动（含「已属会话」）: %s", body)
	}
	// 不存在卡 → 404（mapSessionError 归一后走 §2.2 表）。
	code, _ = ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards", `{"card":"B99999"}`)
	if code != 404 {
		t.Fatalf("不存在卡应 404: %d", code)
	}
	// 不存在会话 → 404。
	code, _ = ledgerPost(t, env.testAgentdEnv, "/api/sessions/session:999/cards",
		fmt.Sprintf(`{"card":%q}`, card.ID))
	if code != 404 {
		t.Fatalf("不存在会话应 404: %d", code)
	}
}

// TestSessionLeaveCardEndpoint 锁：移出成功 + 详情不再含卡 + 幂等（条 11）+
// 不存在会话 404。
func TestSessionLeaveCardEndpoint(t *testing.T) {
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "移出场")
	card := seedCard(t, env, "移出卡")
	if code, body := ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards",
		fmt.Sprintf(`{"card":%q}`, card.ID)); code != 200 {
		t.Fatalf("拉卡进群: %d %s", code, body)
	}
	code, body := ledgerDelete(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards/"+card.ID, "")
	if code != 200 {
		t.Fatalf("移卡出群: %d %s", code, body)
	}
	// 幂等：不在会话内再移 → 200（契约条 11）。
	if code, _ = ledgerDelete(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards/"+card.ID, ""); code != 200 {
		t.Fatalf("重复移卡应幂等 200: %d", code)
	}
	// 不存在会话 → 404。
	if code, _ = ledgerDelete(t, env.testAgentdEnv, "/api/sessions/session:999/cards/"+card.ID, ""); code != 404 {
		t.Fatalf("不存在会话移卡应 404: %d", code)
	}
}

// TestSessionArchiveEndpoint 锁：归档 200 + 幂等（条 6）+ 详情 archived=true +
// 归档后发言 409（S1 语义经既有发言端点呈现）+ 归档后拉卡 409 且文案含「已归档」。
func TestSessionArchiveEndpoint(t *testing.T) {
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "归档场")
	card := seedCard(t, env, "归档卡")
	if code, body := ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards",
		fmt.Sprintf(`{"card":%q}`, card.ID)); code != 200 {
		t.Fatalf("拉卡进群: %d %s", code, body)
	}
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/archive", `{}`)
	if code != 200 {
		t.Fatalf("归档: %d %s", code, body)
	}
	// 幂等：重复归档 200（Store 级短路，B358.1 拍板①）。
	if code, _ = ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/archive", `{}`); code != 200 {
		t.Fatalf("重复归档应幂等 200: %d", code)
	}
	// 详情 archived=true。
	var out struct {
		Summary proto.SessionSummary `json:"summary"`
	}
	code, body = ledgerGet(t, env.testAgentdEnv, "/api/sessions/"+session.ID)
	if code != 200 {
		t.Fatalf("归档后详情: %d %s", code, body)
	}
	if err := json.Unmarshal([]byte(body), &out); err != nil {
		t.Fatal(err)
	}
	if !out.Summary.Archived {
		t.Fatalf("归档后 summary.archived 应 true: %+v", out.Summary)
	}
	// 归档后发言 → 409（ErrReadOnly 经既有 collabErr 映射）。
	if code, _ = ledgerPost(t, env.testAgentdEnv, "/api/rooms/"+session.ID+"/messages", `{"body":"x"}`); code != 409 {
		t.Fatalf("归档会话发言应 409: %d", code)
	}
	// 归档后拉卡 → 409 且文案含「已归档」（可行动错误文案，ErrBadState）。
	code, body = ledgerPost(t, env.testAgentdEnv, "/api/sessions/"+session.ID+"/cards",
		fmt.Sprintf(`{"card":%q}`, card.ID))
	if code != 409 {
		t.Fatalf("归档会话拉卡应 409: %d %s", code, body)
	}
	if !strings.Contains(body, "已归档") {
		t.Fatalf("409 文案应可行动（含「已归档」）: %s", body)
	}
}

// TestSessionsEndpoints503WithoutLedger 锁 withRooms 守卫对会话端点同款降级
// （与 TestRoomsEndpoints503WithoutLedger 同族）。
func TestSessionsEndpoints503WithoutLedger(t *testing.T) {
	env := newTestAgentdEnv(t)
	if code, _ := ledgerGet(t, env, "/api/sessions"); code != 503 {
		t.Fatalf("未挂账本应 503: %d", code)
	}
}
```

其中 `ledgerDelete` **是既有 helper**（`internal/agentd/ledgerapi_test.go:114`，签名 `func ledgerDelete(t *testing.T, env *testAgentdEnv, path, body string) (int, string)`）——直接使用、不重复定义、不改动它；DELETE 无请求体处传 `""`。

跑红：

```
$ go test ./internal/agentd -run 'TestSession' -count=1 -v
--- FAIL: TestSessionsCreateEndpoint（404 路由不存在）
--- FAIL: TestSessionsListUnreadAndMemberForgery / TestSessionDetailEndpointKeyset /
         TestSessionJoinCardEndpoint / TestSessionLeaveCardEndpoint / TestSessionArchiveEndpoint
（红因 = 路由未接：POST /api/sessions 404——功能缺失非 typo；4 支 auth TestSession* 保持 PASS）
```

**步骤 2（绿：最小实现）**：新建 `internal/agentd/sessionsapi.go`（完整代码，直接落）：

```go
// 会话（群）HTTP 面（B358.4）：会话建/列/详情/归档/拉卡/移出六端点，注册进
// registerLedgerRoutes 同一 mux；发言、已读、历史复用既有 /api/rooms 端点
// （roomsapi.go——会话房间经 B358.1 会话语义已可读写）。
//
// 边界：本文件是 gateway 对 d_collab 入站 api 门面会话半边的消费侧（spec 测试
// 接缝清单 #1 的调用方）；不得 import internal/collab 之外的门面，账本能力经
// s.ledger 既有直调面触达。actor/成员标识服务端注入（roomUserActor），请求体
// 不带身份字段——body 里的 owner/actor 字段一律不解码（会话群主=控制台成员
// 身份，P4 裁决「gateway 既有 actor 注入面不动」的 gateway 半边落点，见
// docs/superpowers/plans/b358.4-plan.md §2.3）。错误映射：sessionErr（写面）
// + 既有 collabErr（发言路径）。
package agentd

import (
	"encoding/json"
	"errors"
	"net/http"
	"strings"

	"github.com/Xsxdot/handoff/internal/collab"
	"github.com/Xsxdot/handoff/internal/ledger"
)

// sessionErr 把 collab.Service 会话写面哨兵翻译为 HTTP 状态（b358.4-plan §2.2
// 定稿映射表）：ErrNoRoom（含 mapSessionError 归一的 client.ErrNotFound）→404、
// ledger.ErrBadState（已属他会话/会话已归档）→409、其余 500。只覆盖会话写面
// 六端点可达的哨兵，不与 collabErr 的发言路径映射混流（缺陷族 7）——若未来
// 会话写面冒出 ErrNotWriter/ErrReadOnly，按 500 暴露，不静默归并。
func sessionErr(w http.ResponseWriter, err error) {
	code := http.StatusInternalServerError
	switch {
	case errors.Is(err, collab.ErrNoRoom):
		code = http.StatusNotFound
	case errors.Is(err, ledger.ErrBadState):
		code = http.StatusConflict
	}
	writeErr(w, code, err)
}

// handleSessionCreate POST /api/sessions {title} → CreateSession。群主（owner）
// 与 actor 同为控制台成员身份（roomUserActor，服务端注入）；请求体只解码
// title——塞 owner/actor 字段一律忽略（TestSessionsCreateEndpoint 反例锁）。
func (s *Server) handleSessionCreate(w http.ResponseWriter, r *http.Request) {
	var req struct {
		Title string `json:"title"`
	}
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		writeErr(w, http.StatusBadRequest, errors.New("bad json"))
		return
	}
	title := strings.TrimSpace(req.Title)
	if title == "" {
		writeErr(w, http.StatusBadRequest, errors.New("会话标题不能为空"))
		return
	}
	actor := s.roomUserActor(r)
	session, err := s.rooms.CreateSession(title, actor, actor)
	if err != nil {
		// 可达失败只有账本写失败（owner 恒非空、title 已验）——服务端错误。
		s.log.Warn("建会话失败", "title", title, "actor", actor, "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	s.log.Info("会话已创建", "session", session.ID, "title", title, "actor", actor)
	writeJSON(w, http.StatusOK, session)
}

// handleSessionsList GET /api/sessions → ListSessions（谁需要我：未读/标签/预览）。
// member 维度 = 控制台成员身份（服务端注入）；?member= 查询参数不参与
// （TestSessionsListUnreadAndMemberForgery 反例锁）。
func (s *Server) handleSessionsList(w http.ResponseWriter, r *http.Request) {
	member := s.roomUserActor(r)
	summaries, err := s.rooms.ListSessions(member)
	if err != nil {
		s.log.Warn("会话列表读取失败", "member", member, "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	s.log.Info("会话列表响应成功", "member", member, "sessions", len(summaries))
	writeJSON(w, http.StatusOK, map[string]any{"sessions": summaries})
}

// handleSessionDetail GET /api/sessions/{id} → SessionDetail（成员/节点/timeline
// 三块）。不存在 → 404 且文案含会话 id（mapSessionError 已归一
// client.ErrNotFound → ErrNoRoom，getSessionTx 文案带 id）。
func (s *Server) handleSessionDetail(w http.ResponseWriter, r *http.Request) {
	id := r.PathValue("id")
	detail, err := s.rooms.SessionDetail(id)
	if err != nil {
		s.log.Warn("会话详情读取失败", "session", id, "cause", err)
		sessionErr(w, err)
		return
	}
	s.log.Info("会话详情响应成功", "session", id)
	writeJSON(w, http.StatusOK, detail)
}

// handleSessionArchive POST /api/sessions/{id}/archive → ArchiveSession（幂等，
// Store 级已归档短路）。归档后只读由 collab.Send / collab.JoinCard 在会话本体
// 上执法（B358.1），本 handler 不重复判定。
func (s *Server) handleSessionArchive(w http.ResponseWriter, r *http.Request) {
	id := r.PathValue("id")
	actor := s.roomUserActor(r)
	if err := s.rooms.ArchiveSession(id, actor); err != nil {
		s.log.Warn("归档会话失败", "session", id, "actor", actor, "cause", err)
		sessionErr(w, err)
		return
	}
	s.log.Info("会话已归档", "session", id, "actor", actor)
	writeJSON(w, http.StatusOK, map[string]bool{"ok": true})
}

// handleSessionJoinCard POST /api/sessions/{id}/cards {card} → JoinCard
// （进群 ≠ 配人，配人仍走 B307 三按钮）。已属他会话/会话已归档 → 409
// （ErrBadState）；卡或会话不存在 → 404（ErrNoRoom）。
func (s *Server) handleSessionJoinCard(w http.ResponseWriter, r *http.Request) {
	id := r.PathValue("id")
	var req struct {
		Card string `json:"card"`
	}
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		writeErr(w, http.StatusBadRequest, errors.New("bad json"))
		return
	}
	cardID := strings.TrimSpace(req.Card)
	if cardID == "" {
		writeErr(w, http.StatusBadRequest, errors.New("卡号不能为空"))
		return
	}
	actor := s.roomUserActor(r)
	if err := s.rooms.JoinCard(id, cardID, actor); err != nil {
		s.log.Warn("拉卡进群失败", "session", id, "card", cardID, "actor", actor, "cause", err)
		sessionErr(w, err)
		return
	}
	s.log.Info("卡已进群", "session", id, "card", cardID, "actor", actor)
	writeJSON(w, http.StatusOK, map[string]bool{"ok": true})
}

// handleSessionLeaveCard DELETE /api/sessions/{id}/cards/{cardID} → LeaveCard
// （幂等：不在会话内返回 nil）。
func (s *Server) handleSessionLeaveCard(w http.ResponseWriter, r *http.Request) {
	id := r.PathValue("id")
	cardID := r.PathValue("cardID")
	actor := s.roomUserActor(r)
	if err := s.rooms.LeaveCard(id, cardID, actor); err != nil {
		s.log.Warn("移卡出群失败", "session", id, "card", cardID, "actor", actor, "cause", err)
		sessionErr(w, err)
		return
	}
	s.log.Info("卡已移出会话", "session", id, "card", cardID, "actor", actor)
	writeJSON(w, http.StatusOK, map[string]bool{"ok": true})
}
```

**步骤 3**：`internal/agentd/ledgerapi.go#registerLedgerRoutes` 的 rooms 五行之后追加 §2.1 六行（逐字）。

**步骤 4（绿）**：

```
$ go build ./...   → 退出 0
$ go test ./internal/agentd -run 'TestSession' -count=1 → ok（新 7 支 + auth 4 支全绿）
```

### Task 1 验收（字面断言）

1. **路由命名断言**：`grep -c "api/sessions" internal/agentd/ledgerapi.go` = 6，且六行与 §2.1 逐字一致；`grep -rn "api/sessions" internal/ cmd/ --include="*.go" | grep -v _test | grep -v "agentd/ledgerapi.go" | grep -v "agentd/sessionsapi"` 零命中（路由只在一处注册）。
2. **映射表字面断言**：`sessionErr` 恰两格——`collab.ErrNoRoom→404`、`ledger.ErrBadState→409`；测试支 TestSessionDetailEndpointKeyset（404+文案含 id）/ TestSessionJoinCardEndpoint（409 含「已属会话」、404×2）/ TestSessionArchiveEndpoint（409 含「已归档」）/ TestSessionLeaveCardEndpoint（404）逐格过。
3. **反例两支**：TestSessionsCreateEndpoint（body 塞 owner/actor 被忽略，落账 actor=webUserMember）；TestSessionsListUnreadAndMemberForgery（`?member=` 伪造不影响 Unread=2）。
4. **键集金样断言**：TestSessionDetailEndpointKeyset 顶层 {summary,timeline} + nodes 缺键 + Summary 11 键 + Member/Card 键集逐键过（穿过真实 HTTP JSON，非本地 marshal）。
5. `go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1`：红窗仍是基线 10 支（Task 1 不动 roomsapi_test.go）。

## 5. Task 2：红窗 10 支改写收口（roomsapi_test.go 就地改写）

### 文件范围

仅 `internal/agentd/roomsapi_test.go`；只改 §0.4 清单 10 支的函数体与文件头注释，**其余测试一字不动**。

### Interfaces

Consumes：Task 1 的全部路由（`/api/sessions` 族 + 既有 `/api/rooms` 发言/已读）；夹具 `mustConsoleSession`（sessionsapi_test.go，同包可见）；既有 `newRoomsEnv/seedCard/webUserMember/env.srv.rooms/env.ledger`。

Produces：10 支同名测试（改写后绿），红窗核算对账面。

### 步骤

**步骤 1**：文件头注释追加一行改写说明：

```go
// B358.4 红窗改写：下方标注「B358.4 红窗改写」的 10 支原以旧房间 user 发言成功
// 为断言/夹具，随 B358.1 S1 只读归档失效；按新会话语义就地改写（名字保留供
// 红窗核算逐支比对），断言意图逐支对应迁移，见 docs/superpowers/plans/b358.4-plan.md §5。
```

**步骤 2**：按对照表逐支改写（全代码；发送者身份一律用会话成员——`mustConsoleSession` 的 owner=webUserMember，控制台 actor 天然在书写者集内）：

| # | 测试（名字保留） | 改写内容 |
|---|---|---|
| 1 | `TestRoomsListEndpoint` | preview 正面锚迁会话列表（写入面只在会话房间）+ 旧卡房间读面对质保留断言 |
| 2 | `TestRoomsListUnreadAndAttachProjection` | 未读/已读半边迁会话（2 条发送 → Unread=2 → /read → 0）；attach 半边原样（无发言依赖，卡房间挂账） |
| 3 | `TestRoomMessagesEndpoint` | 夹具卡房间 → 会话房间；断言逐条照抄 |
| 4 | `TestRoomSendEndpoint` | POST 目标卡房间 → 会话房间；全流读改 `EventsFromAsc(nil,…)`（会话消息无卡）；补「不存在会话房间 → 400」断言 |
| 5 | `TestRoomReadEndpoint` | 夹具卡房间 → 会话房间；断言照抄 |
| 6–10 | `TestInboxThreeSources / TestInboxMentionSource / TestInboxRefIDShapes / TestInboxGoldenKeyShapes / TestInboxIntegrationSmoke` | 仅 mention 源夹具：`Send("project:p",…)` → 会话房间发送；decision/ticket 两源与全部键集断言原样 |

逐支代码（改写后全文，直接落）：

```go
func TestRoomsListEndpoint(t *testing.T) {
	// B358.4 红窗改写：原夹具「向卡房间 user 发言」随 S1 只读归档失效。
	// 改锁会话语义下的等价行为——(a) 列表投影 preview 的正面锚迁到会话列表
	// （写入面现在只在会话房间）；(b) /api/rooms 旧读面对质保留（S1：读史不断）。
	env := newRoomsEnv(t)
	card := seedCard(t, env, "卡A")
	session := mustConsoleSession(t, env, "列表预览场")
	if _, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "hi"}, webUserMember); err != nil {
		t.Fatal(err)
	}
	var out struct {
		Sessions []proto.SessionSummary `json:"sessions"`
	}
	code, body := ledgerGet(t, env.testAgentdEnv, "/api/sessions")
	if code != 200 {
		t.Fatalf("GET /api/sessions: %d %s", code, body)
	}
	if err := json.Unmarshal([]byte(body), &out); err != nil {
		t.Fatal(err)
	}
	var found *proto.SessionSummary
	for i := range out.Sessions {
		if out.Sessions[i].ID == session.ID {
			found = &out.Sessions[i]
		}
	}
	if found == nil {
		t.Fatalf("会话应出现在列表: %+v", out.Sessions)
	}
	if found.Preview == nil || found.Preview.Body != "hi" || found.Preview.Seq <= 0 || found.Preview.CreatedAt.IsZero() {
		t.Fatalf("真实 HTTP /api/sessions 应保留 preview 正文、seq、created_at: %+v", found.Preview)
	}
	// 旧房间读面对质保留：卡房间行仍在 /api/rooms（无需发言即可读）。
	var rooms struct {
		Rooms []proto.RoomSummary `json:"rooms"`
	}
	if code := env.getJSON(t, "/api/rooms?project=p", &rooms); code != 200 {
		t.Fatalf("GET /api/rooms: %d", code)
	}
	roomFound := false
	for _, room := range rooms.Rooms {
		if room.ID == card.ID {
			roomFound = true
		}
	}
	if !roomFound {
		t.Fatalf("旧卡房间读面应保留: %+v", rooms.Rooms)
	}
}

func TestRoomsListUnreadAndAttachProjection(t *testing.T) {
	// B358.4 红窗改写：未读/已读半边迁会话语义（写入面只在会话房间）；
	// attach 投影半边不依赖发言，卡房间挂账断言原样保留。
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "未读迁移场")
	if _, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "一"}, webUserMember); err != nil {
		t.Fatal(err)
	}
	second, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "二"}, webUserMember)
	if err != nil {
		t.Fatal(err)
	}
	// attach 半边（原样）：本机挂账卡 → /api/rooms 行带 attach。
	card := seedCard(t, env, "可挂账卡")
	if err := env.st.CreateTask(&proto.Task{ID: "T1", RepoPath: "/repo", WorkDir: "/work/B1"}); err != nil {
		t.Fatal(err)
	}
	if err := env.ledger.LinkTask(card.ID, "", "T1", ledger.PurposeImplement, "test"); err != nil {
		t.Fatal(err)
	}
	var out struct {
		Sessions []proto.SessionSummary `json:"sessions"`
		Rooms    []proto.RoomSummary    `json:"rooms"`
	}
	code, body := ledgerGet(t, env.testAgentdEnv, "/api/sessions")
	if code != 200 {
		t.Fatalf("GET /api/sessions: %d %s", code, body)
	}
	if err := json.Unmarshal([]byte(body), &out); err != nil {
		t.Fatal(err)
	}
	for _, s := range out.Sessions {
		if s.ID == session.ID && s.Unread != 2 {
			t.Fatalf("两条未读消息应投影 unread=2: %+v", s)
		}
	}
	code, body = ledgerGet(t, env.testAgentdEnv, "/api/rooms")
	if code != 200 {
		t.Fatalf("GET /api/rooms: %d %s", code, body)
	}
	if err := json.Unmarshal([]byte(body), &out); err != nil {
		t.Fatal(err)
	}
	var roomFound *proto.RoomSummary
	globalFound := false
	for i := range out.Rooms {
		if out.Rooms[i].ID == card.ID {
			roomFound = &out.Rooms[i]
		}
		if out.Rooms[i].Kind == "global" {
			globalFound = true
			if out.Rooms[i].Unread < 0 {
				t.Fatalf("global unread 必须在线: %+v", out.Rooms[i])
			}
		}
	}
	if roomFound == nil {
		t.Fatalf("卡房间应出现在列表: %+v", out.Rooms)
	}
	if !globalFound {
		t.Fatal("global 房间应出现在列表")
	}
	if roomFound.Attach == nil || roomFound.Attach.Target != "" || roomFound.Attach.TaskID != "T1" ||
		roomFound.Attach.WorkDir != "/work/B1" || roomFound.Attach.Command != "handoff attach T1" {
		t.Fatalf("本机挂账 attach 投影错误: %+v", roomFound.Attach)
	}
	// 已读：POST /api/rooms/{session}/read 到最新 seq → 会话行 unread=0。
	if code, body = ledgerPost(t, env.testAgentdEnv, "/api/rooms/"+session.ID+"/read",
		fmt.Sprintf(`{"upto_seq":%d}`, second)); code != 200 {
		t.Fatalf("POST /read: %d %s", code, body)
	}
	code, body = ledgerGet(t, env.testAgentdEnv, "/api/sessions")
	if code != 200 {
		t.Fatalf("GET /api/sessions after read: %d %s", code, body)
	}
	if err := json.Unmarshal([]byte(body), &out); err != nil {
		t.Fatal(err)
	}
	for _, s := range out.Sessions {
		if s.ID == session.ID && s.Unread != 0 {
			t.Fatalf("已读后 unread 应为 0: %+v", s)
		}
	}
}

func TestRoomMessagesEndpoint(t *testing.T) {
	// B358.4 红窗改写：夹具卡房间 → 会话房间；「写侧受守、读侧宽容」正面锚
	// 与否定锚断言逐条照抄（条数与内容都断言，不只断言非空）。
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "历史场")
	first, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "第一条"}, webUserMember)
	if err != nil {
		t.Fatal(err)
	}
	second, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "第二条"}, webUserMember)
	if err != nil {
		t.Fatal(err)
	}
	var out struct {
		Messages []proto.LedgerEvent `json:"messages"`
	}
	code := env.getJSON(t, "/api/rooms/"+session.ID+"/messages?limit=10", &out)
	if code != 200 {
		t.Fatalf("GET messages: %d", code)
	}
	if len(out.Messages) != 2 {
		t.Fatalf("历史应恰好两条: %+v", out.Messages)
	}
	seqs := map[int64]bool{}
	bodies := map[string]bool{}
	for _, ev := range out.Messages {
		seqs[ev.Seq] = true
		var msg proto.RoomMessage
		if err := json.Unmarshal(ev.Payload, &msg); err != nil {
			t.Fatal(err)
		}
		bodies[msg.Body] = true
	}
	if !seqs[first] || !seqs[second] {
		t.Fatalf("两条消息的 seq 都应返回: %d %d (%v)", first, second, seqs)
	}
	if !bodies["第一条"] || !bodies["第二条"] {
		t.Fatalf("两条消息正文都应返回: %v", bodies)
	}
	// 读侧宽容的否定锚（原样）：不存在的房间 → 200 且零条。
	var empty struct {
		Messages []proto.LedgerEvent `json:"messages"`
	}
	code = env.getJSON(t, "/api/rooms/NO-SUCH/messages", &empty)
	if code != 200 {
		t.Fatalf("GET 不存在房间应 200: %d", code)
	}
	if len(empty.Messages) != 0 {
		t.Fatalf("不存在房间历史应零条: %+v", empty.Messages)
	}
}

func TestRoomSendEndpoint(t *testing.T) {
	// B358.4 红窗改写：POST 发言成功断言从卡房间（S1 起只读）迁到会话房间。
	// 会话消息是无卡事件 → 读账本改全流读（0.2#6）；actor 服务端注入断言原样。
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "发送场")
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/rooms/"+session.ID+"/messages", `{"body":"你好"}`)
	if code != 200 {
		t.Fatalf("POST messages: %d %s", code, body)
	}
	events, err := env.ledger.EventsFromAsc(nil, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	found := false
	for _, ev := range events {
		if ev.Type != ledger.EvRoomMessage {
			continue
		}
		var msg proto.RoomMessage
		if err := json.Unmarshal(ev.Payload, &msg); err != nil {
			t.Fatal(err)
		}
		if msg.Body == "你好" {
			found = true
			if !strings.HasPrefix(ev.Actor, "web:") {
				t.Fatalf("actor 应服务端注入（不经请求体）: %q", ev.Actor)
			}
		}
	}
	if !found {
		t.Fatalf("消息未落账: %+v", events)
	}
	// 写侧受守补断言：不存在的会话房间 → 400（ErrNoRoom→400 既有映射对
	// 会话房间同样成立；与存活的 TestRoomSendErrMapping 同族）。
	if code, _ := ledgerPost(t, env.testAgentdEnv, "/api/rooms/session:999/messages", `{"body":"x"}`); code != 400 {
		t.Fatalf("不存在会话房间发言应 400: %d", code)
	}
}

func TestRoomReadEndpoint(t *testing.T) {
	// B358.4 红窗改写：夹具卡房间 → 会话房间；MarkRead 水位清零断言照抄。
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "已读场")
	seq, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, webUserMember)
	if err != nil {
		t.Fatal(err)
	}
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/rooms/"+session.ID+"/read",
		fmt.Sprintf(`{"upto_seq":%d}`, seq))
	if code != 200 {
		t.Fatalf("POST read: %d %s", code, body)
	}
	// 读尽后未读应 0（MarkRead 到当前最大 seq → 水位语义清空）。
	if n, err := env.srv.rooms.Unread(webUserMember, session.ID); err != nil || n != 0 {
		t.Fatalf("读尽后未读应 0: %v %d", err, n)
	}
}
```

`TestInbox*` 五支：只改 mention 源夹具（原行 `env.srv.rooms.Send("project:p", …)`，project 房间 S1 起只读），在 `env := newRoomsEnv(t)` 之后插一行会话夹具，再把发送目标换成 `session.ID`。**赋值形态逐支照抄原文件**——捕获 seq 的两支（`TestInboxMentionSource`/`TestInboxRefIDShapes`）保持 `seq, err := env.srv.rooms.Send(…)`，不捕获 seq 的三支（`TestInboxThreeSources`/`TestInboxGoldenKeyShapes`/`TestInboxIntegrationSmoke`）保持 `if _, err := env.srv.rooms.Send(…); err != nil {` 形；消息正文与 `Mentions: []string{webUserMember}` 逐字不变：

```go
	// B358.4 红窗改写：mention 源夹具从 project 群房间迁会话房间（旧房间只读）。
	session := mustConsoleSession(t, env, "<该测试的场名，见下表>")
	seq, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "改动影响 B145", Mentions: []string{webUserMember}}, webUserMember)
```

五支场名依次：`TestInboxThreeSources`→`收件箱三源场`、`TestInboxMentionSource`→`收件箱提及场`、`TestInboxRefIDShapes`→`收件箱引用形状场`、`TestInboxGoldenKeyShapes`→`收件箱键集场`、`TestInboxIntegrationSmoke`→`收件箱冒烟场`。五支的其余断言（decision/ticket 源、RefID 形状、金样本键集、消费后消失、集成冒烟）与 decision/ticket/task 夹具（含 `TestInboxRefIDShapes` 的 `card := seedCard(...)` 与 `dec.ID` 断言）**逐字保留**。

**步骤 3（绿 + 红窗半边收口复核）**：

```
$ go build ./...   → 退出 0
$ go test ./internal/agentd -run 'TestRooms|TestRoom|TestInbox' -count=1 -v
  → 10 支全部 PASS（红窗本卡半边收口绿）；TestRoomSendErrMapping /
    TestRoomsEndpoints503WithoutLedger / 非红窗 attach 与 inbox 测试全部 PASS（存活无波及）
$ go test ./internal/agentd -count=1 → 失败恰 1 支：TestLegacyNodeEventSequenceUnchanged（B362 golden，禁触）
```

### Task 2 验收

1. §0.4 红窗 10 支全绿，**名字逐支保留**；每支函数体首行有「B358.4 红窗改写」注释（指认原断言与迁移去向）。
2. 存活测试（TestRoomSendErrMapping / TestRoomsEndpoints503WithoutLedger）与全部非红窗测试逐字未动（`git diff internal/agentd/roomsapi_test.go` 只含 10 支函数体 + 文件头注释行）。
3. agentd 失败集合恰 {TestLegacyNodeEventSequenceUnchanged}。

## 6. Task 3：B358.1 review 移交两支（internal/collab 直测）

### 文件范围

仅 `internal/collab/sessions_test.go` 末尾追加（既有内容不动；imports 已含 errors/client/proto，零新增 import）。

### Interfaces

Consumes：`collab.New(lc client.LedgerClient)`、既有夹具 `newSessionFixture(t) (*Service, *ledger.Store, client.LedgerClient)`、哨兵 `ErrNoRoom/ErrNotWriter`、`client.LedgerClient.ListAllCards(project string) ([]proto.Card, error)`。

Produces（测试符号，交 recon 入图）：

```go
type listAllCardsFailingClient struct{ client.LedgerClient }                       // fake 注入桩
func TestSessionWritersReadFailureNotWriterGuard(t *testing.T)                     // 移交①
func TestSessionSendToMissingSessionRoomIsErrNoRoom(t *testing.T)                  // 移交②
```

### 步骤（两支都是既有正确行为的回归锁——写下去即绿，按最薄路径条免除红绿；声明见 §9 内部锁）

**步骤 1**：文件末尾追加（完整代码，直接落）：

```go
// —— B358.4 移交两支（B358.1 review：仅源码成立的两个事实补直测）——

// listAllCardsFailingClient 只把 ListAllCards 换成注入失败，其余能力走真实
// Facade（内嵌接口值）——构造「读账本失败」这个真实账本给不出的形状。
type listAllCardsFailingClient struct{ client.LedgerClient }

func (c listAllCardsFailingClient) ListAllCards(project string) ([]proto.Card, error) {
	return nil, errInjectedLedgerRead
}

var errInjectedLedgerRead = errors.New("注入：读卡列表失败")

// TestSessionWritersReadFailureNotWriterGuard 锁（B358.1 移交①）：sessionWriters
// 组装书写者集时读卡列表失败必须向上传播原错误，不得伪装成 ErrNotWriter——
// 一次账本读错被说成「你不是成员」（403）是缺陷族 2 的静默失败。fake 注入是
// 唯一构造点：gateway 侧 s.rooms 是真实 Facade 组装，从 HTTP 缝造不出读失败。
func TestSessionWritersReadFailureNotWriterGuard(t *testing.T) {
	svc, _, lc := newSessionFixture(t)
	session, err := svc.CreateSession("读失败反例", "user:sy", "user:sy")
	if err != nil {
		t.Fatalf("建会话: %v", err)
	}
	guarded := New(listAllCardsFailingClient{lc})
	_, err = guarded.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy")
	if err == nil {
		t.Fatal("读卡列表失败后发言应报错")
	}
	if errors.Is(err, ErrNotWriter) {
		t.Fatalf("读账本失败不得伪装 403（ErrNotWriter）: %v", err)
	}
	if !errors.Is(err, errInjectedLedgerRead) {
		t.Fatalf("读失败应原样向上传播，实得: %v", err)
	}
}

// TestSessionSendToMissingSessionRoomIsErrNoRoom 锁（B358.1 移交②）：会话房间
// 不存在（session:999 无本体）时经 Send 发言 → ErrNoRoom。路径：Resolve 会话
// 分支只解析形态（room.go，不查会话表）→ sendToSession 的 GetSession 失败 →
// mapSessionError（client.ErrNotFound → ErrNoRoom）。此前仅源码成立，无直测。
func TestSessionSendToMissingSessionRoomIsErrNoRoom(t *testing.T) {
	svc, _, _ := newSessionFixture(t)
	_, err := svc.Send("session:999", proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy")
	if !errors.Is(err, ErrNoRoom) {
		t.Fatalf("不存在的会话房间发言必须 ErrNoRoom，实得: %v", err)
	}
	if errors.Is(err, ErrReadOnly) {
		t.Fatalf("不得误报只读（房间不是在而是无）: %v", err)
	}
}
```

**步骤 2（绿）**：

```
$ go build ./...   → 退出 0
$ go test ./internal/collab -run 'TestSessionWritersReadFailureNotWriterGuard|TestSessionSendToMissingSessionRoomIsErrNoRoom' -count=1 → ok
```

若意外红：先跑 `go test ./internal/collab -count=1` 全量核对是否 S1 语义与此处理解冲突——**停下上报协调者**，不得改 collab 生产代码（越界）。

### Task 3 验收

两支绿；`go test ./internal/collab/... -count=1` 全量绿；collab 生产文件零改动（`git diff --stat internal/collab` 只含 sessions_test.go）。

## 7. Task 4：收口核算与移交

### 文件范围

无代码改动（读数与清单落台账）。

### 步骤

1. **三段律全量**：`go build ./...` 退出 0；`go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` 退出 0；`go test ./internal/agentd -count=1`、`go test ./cmd -count=1` 失败集合落台账。
2. **红窗核算（§0.4 规则 2）**：agentd 失败集合逐支比对 = 恰 {TestLegacyNodeEventSequenceUnchanged}；cmd 失败集合逐支比对 = 恰基线 8 支同名。多/少都停手上报。
3. **契约闸比对（§0.4 规则 3）**：`go test ./cmd -run TestRepoContractGate -count=1 -v`，违规清单与 Task 0 记录的基线 6 条逐行比对——无新增、无减少，原文落台账。
4. **图符号清单**：新增（sessionsapi.go 六 handler + sessionErr、sessionsapi_test.go 测试族 + mustConsoleSession/ledgerDelete/assertKeyset、sessions_test.go 两支 + stub）与修改（roomsapi_test.go 10 支改写、ledgerapi.go 注册块）逐符号落台账，交 integrate/recon 补视图 diff；收尾跑 `codegraph resolve --doc docs/superpowers/plans/b358.4-plan.md` 修坏锚。
5. **文件集核对**：`git diff --stat` 受控 = 5 个文件（0.1 有界文件集）+ 本计划 + 台账；无关脏文件不卷入。

### Task 4 验收

§13 收口判据全部成立；台账含红窗核算原文与图符号清单。

## 8. 明确不做的改动（越界即回退）

1. **不动 `internal/agentd/wakeconsumer*.go`**——S3 已收口（契约 §4.6 条 39–50 已落地）；本卡所有端点都不触唤醒路径。
2. **不动 `cmd/**`**——cmd 红窗 6 支归 S5；`cmd/session.go`（S3 的 session wait）禁触。
3. **不动 B362 golden** `TestLegacyNodeEventSequenceUnchanged`。
4. **不动 collab/ledger/proto 生产文件**——门面八方法与 Store 语义 Ticket 0/S1 已收口；本卡只消费。
5. **不加 `AddSessionMember` 端点**——breakdown S4 端点清单只有六个；成员扩张的载体（谁经控制台入群）归 S6/后续卡，facade 方法已备好。
6. **不改发言/已读/历史端点行为**——复用既有 `/api/rooms`（含 collabErr 映射与空正文 400）；会话房间 404 分支不可达的既有裁决（roomsapi.go:296-300）沿用，不写假测试。
7. **不新增哨兵、不改 wire DTO**——ErrBadState 用既有 `ledger.ErrBadState`（0.2#3）；proto 键集已冻结（改形状先回 contract）。
8. **不删旧房间读面**——`/api/rooms` 列表与历史对质保留（S1 P5 裁决）。

## 9. 序列化边界与接缝覆盖

### 序列化边界设问

新增数据从产生到消费之间**无手写序列化**：handler 直接 `writeJSON`（encoding/json）投出 proto DTO，无手搭 map 投影（除 `{"sessions":…}`/`{"ok":true}` 两层信封，无字段投影）。跨语言链（Go DTO → HTTP JSON → S6 的 TS 镜像）的 Go 半边由 TestSessionDetailEndpointKeyset 锁——断言**穿过真实 HTTP 响应**（httptest 返回体解码回 map 再逐键比对），不是本地 marshal 自查；`nodes` 缺键断言分辨「字段缺失」与「值为零」（omitempty 语义）。roundtrip 属性测试不另设：编码由 encoding/json 承担、proto 金样本（`sessions_fixture_test.go`）已锁形状，键集断言在此无增量；TS 侧孪生金样本逐键比对是 S6 验收（契约 §7.3）。

### 接缝覆盖（spec §6 缝清单，双向）

**缝 #1（会话子系统入站门面，调用方 = 控制面 HTTP 端点）**——本卡全部缝级测试：

- **测试 → 缝**：Task 1/2 每支测试入口 = HTTP 路由（真实 mux）→ handler → `Service.CreateSession/ListSessions/SessionDetail/ArchiveSession/JoinCard/LeaveCard/Send/MarkRead/Unread`——入口符号全部在缝上（调用链穿过缝，看入口不看标注）。
- **缝 → 测试**：本卡触及的门面方法每支至少一条缝级断言——CreateSession（TestSessionsCreateEndpoint）、ListSessions（TestSessionsListUnreadAndMemberForgery）、SessionDetail（TestSessionDetailEndpointKeyset）、ArchiveSession（TestSessionArchiveEndpoint）、JoinCard（TestSessionJoinCardEndpoint）、LeaveCard（TestSessionLeaveCardEndpoint）、Send/MarkRead/Unread/Mentions/History/Consume（红窗改写 10 支 + 存活的 TestRoomSendErrMapping）。WakeTargets/MessageWakeTargets/AddressesCard 不属本卡（S3 已收口）。

**内部锁声明（两支，附加不顶替）**：`TestSessionWritersReadFailureNotWriterGuard` 与 `TestSessionSendToMissingSessionRoomIsErrNoRoom` 的入口是 collab.Service 方法直调，不在任何缝的 HTTP 入口上。合法理由：**从声明缝构造不出这条断言**——「读账本失败不伪装 403」要求把 fake LedgerClient 注入 `collab.New`，而 gateway 侧 `s.rooms` 是组装点用真实 Facade 建好的（`server.go:2691`），HTTP 缝上无法注入读失败；「会话房间不存在 → ErrNoRoom」同理在 collab 层才是判定本体（HTTP 呈现 400 由存活的 TestRoomSendErrMapping 同族断言承担）。这是移交两支的指定形态（协调者派工报文原文）。

**缝 #2/#3/#4/#5**：分属 S3（唤醒判定）、S3（席位身份）、S6（控制台契约层）、S2（详情投影缝的 collab 半边已收口）——本卡不新增断言、不触碰其文件。

### 退路同闸

无条件退路。步骤正文里的每个动作都是确定动作（`ledgerDelete` 为既有 helper 的事实已查证并写入正文，非条件分支）；没有任何会改变测试入口符号的备选写法。

## 10. 缺陷族对抗（九族 → 验收栏映射）

| 族 | 设问结论 | 验收落点 |
|---|---|---|
| 1 生命周期/状态机中断 | 归档竞态：账本单写者串行化（Store.mutate），JoinCard 的归档判定在卡进群同事务；handler 无状态无后台资源 | TestSessionArchiveEndpoint（归档后 join 409、archive 幂等）；PG 真库并发归真机（breakdown §5 #4） |
| 2 静默失败/误导报错 | 404 文案含会话 id（getSessionTx）；409 文案「已属会话/已归档」可行动；读失败不伪装 403（移交①直测）；无「报成功没落账」——写路径全走账本事务，join/archive/leave 成功以 200+账本事件为证 | TestSessionDetailEndpointKeyset / TestSessionJoinCardEndpoint / TestSessionArchiveEndpoint / Task 3 |
| 3 跨平台假设 | httptest 形状与 OS 无关；hostOnly/鉴权沿用 agentd 既有 middleware，无新路径依赖 | 机内全绿即形状收口；真实对端归真机清单 |
| 4 假红/假绿 | 全部测试穿真实 mux 注册（ledgerGet/ledgerPost/ledgerDelete → httptest.NewServer）——直调 handler 函数 = 空壳冒充接线，禁令落测试纪律；路由不存在时测试红在 404 | Task 1 步骤 1 红读数 + §10 本行 |
| 5 门禁绕过 | actor 一律服务端注入（roomUserActor）；请求体塞 owner/actor 被忽略（反例）；伪造 `?member=` 不影响他人未读（反例）；鉴权沿用既有 middleware 无旁路 | TestSessionsCreateEndpoint / TestSessionsListUnreadAndMemberForgery |
| 6 序列化边界 | 见 §9：键集断言穿真实 HTTP；omitempty 缺失≠零值分辨；TS 半边归 S6 | TestSessionDetailEndpointKeyset |
| 7 枚举白名单 | sessionErr 恰两格不混流（ErrNotWriter/ErrReadOnly 不进会话写面映射，混入即 500 暴露）；状态码映射逐哨兵用例（表 §2.2） | Task 1 验收 2 |
| 8 承重安全属性 | 「member 维度未读以服务端注入 actor 为准」+「body 身份字段被忽略」各一支可变红测试（变异：handler 改读 query member / 解码 body owner → 红）；错误映射变异（删 ErrNoRoom 格）→ 404 断言红 | Task 1 验收 3 + Task 4 变异抽查 |
| 9 webview 候选族 | 本卡是 HTTP 形状层，不触浏览器；真实浏览器建会话/发言/归档走查随 S6 真机 #7 | 机内形状验收 + 真机清单归属注记 |

**Task 4 变异抽查（承重安全属性两发，先红后还原绿，不提交变异）**：① `handleSessionsList` 的 `member := s.roomUserActor(r)` 改为 `r.URL.Query().Get("member")` → TestSessionsListUnreadAndMemberForgery 必须红；② `sessionErr` 注释掉 ErrNoRoom 格 → TestSessionDetailEndpointKeyset 的 404 断言必须红。

## 11. 五项检查与自审三查

1. **缺陷族对抗**：§10 逐族有结论，落验收栏。
2. **序列化边界**：§9，键集断言穿真实序列化边界。
3. **上下文预算**：每 task 文件集有界（Task 1 = 3 文件、Task 2 = 1 文件、Task 3 = 1 文件、Task 0/4 = 0 文件）。
4. **类型标注（边界型）**：行为验收 = 显式真机清单——**机内已验**：路由形状/错误映射/键集/幂等/身份注入（httptest 全链）；**归真机**（不属本卡执行）：真实浏览器三态走查（S6 真机 #7，会话端点为其取数面）、跨机 web:<host> 漂移的成员身份观察（P4 已知边界，多人时代 OOS）、PG 真库（breakdown §5 #4）。
5. **接缝覆盖**：§9 双向 + 内部锁声明两支。

**自审三查**：

- **spec 覆盖**：breakdown §3.5 ③ 逐条指到验收——建会话 200+id（Task 1.1）/列表行+Unread=2（Task 1.2/Task 2.2）/拉卡含卡+已属 4xx+不存在 404（Task 1.4）/归档发言 409+拉卡 4xx+文案（Task 1.5）/详情键集金样（Task 1.3）/路由命名字面断言（Task 1 验收 1）/agentd 全量（Task 4）；缺陷族 2/4/5 的三处点名断言各有着落；红窗 10 支（Task 2）与移交两支（Task 3）全吸收。
- **占位符扫描**：全文无 TBD/「适当的错误处理」/「同 Task N」；测试代码全部给全文；复用的既有 harness helper（newRoomsEnv/seedCard/ledgerGet/ledgerPost/ledgerDelete/mustCreateTask，指认文件名与行为——`ledgerDelete` 在 `ledgerapi_test.go:114` 已查证存在）不构成占位符。**正当出口声明（1 处）**：Task 2 的 TestInbox 五支为对既有文件的机械改写（一行夹具 + 一个发送目标参数），计划给出改写前后形状、逐支场名与「其余断言逐字保留」的照抄指认（照抄对象 = `internal/agentd/roomsapi_test.go` 现文件本体），不重复誊抄未变的既有断言——符合「断言逐条列全 + 指认要照抄的既有 harness（文件名）」例外；除这五支外全部测试给出全文。
- **跨 task 签名一致**：Task 1 Produces 与 Task 2 Consumes（mustConsoleSession/路由）逐字同源；Consumes 的门面签名与 contract §3.5 / sessions.go 现状逐字核对（0.2#2-8）。

## 12. 岔口（协调者审查重点）

1. **【本计划定稿，审查重点】owner 注入点（P4 的 gateway 半边）**：`owner = roomUserActor`（§2.3）。依据、被否备选与翻转成本见该节。若协调者拍板翻转（body.owner 统一记法）：改 `handleSessionCreate` 解码体 + TestSessionsCreateEndpoint 两处断言，其余 task 不动；需同步在 S5 plan 对齐 CLI 半边口径。
2. **【待拍板】无**——本卡不需要新哨兵（ErrBadState 走既有 `ledger.ErrBadState` 透传，0.2#3）、不动 wakeconsumer/cmd、不改 wire 形状，无「先回 contract」项。
3. **【边界声明，非岔口】**控制台成员发言权依赖 owner=roomUserActor 进 Members（§2.3）——若 S6 引入统一登录身份，会话成员迁移属后续卡（多人时代 OOS 已登记）。
4. **【移交确认】**B358.4 卡 note 的移交两支以协调者派工报文为准（仓内未检索到卡原文），已逐字吸收为 Task 3；若卡 note 原文与报文有出入，以卡 note 为准并回报。

## 13. 收口判据（B358.4 完成的唯一判定）

1. `go build ./...` 退出 0。
2. `go test ./internal/agentd -count=1` 失败集合 = 恰 `{TestLegacyNodeEventSequenceUnchanged}`（逐支比对，无增无减）——红窗 10 支收口绿。
3. `go test ./cmd -count=1` 失败集合 = 恰基线 8 支同名（逐支比对）。
4. `go test ./cmd -run TestRepoContractGate -count=1 -v` 违规清单与基线 6 条逐行一致（无新增、无减少）。
5. `go test ./internal/agentd -run 'TestSession' -count=1` 退出 0；`go test ./internal/collab/... ./internal/ledger/... ./internal/proto/... -count=1` 退出 0。
6. `git diff --stat` 受控 = 0.1 有界文件集 + 本计划 + 台账。
7. 台账含：Task 0 基线读数、红窗核算原文、TestRepoContractGate 两轮清单、图符号清单、变异抽查两发记录。

## 实现者纪律（派发注入段）

- TDD 三段律：编译全量、测试局部、集成全量——每个 task 的测试命令只跑触及包；全量只在 Task 4。
- 日志：handler 每条错误分支带 session/id/card/actor/cause 上下文（s.log.Warn），成功路径 Info 不静默（§4 步骤 2 代码已含）；禁 print。
- 注释：sessionsapi.go 文件头职责+边界已给；每个 handler 导出注释已给；roomsapi_test.go 改写支首行注释已给。
- 修复熔断：任何测试意外红且无法归因到本计划明说的行为变化 → 停手上报，不顺手修越界文件。
- 本计划无派发系统自身的验收步骤（全部本地 go test/go build/grep），无「由协调者执行，不派发」条目。

---

## 协调者拍板（补记，2026-09-12）

§12.1 定稿点审查后**修正一处**，其余照准：

1. **CreateSession 的 owner 来源修正：owner 取请求体（统一记法，`user:<name>` / `agent:<name>` 前缀校验），不取 roomUserActor。**理由：owner 是会话初始成员（契约条 3：Members 初值含 owner），P4 裁定成员身份用统一记法、`web:<host>` 是机器位会漂；「gateway 既有 actor 注入面不动」指的是**审计 actor**（谁调的 HTTP，照旧 roomUserActor 服务端注入）——成员身份与审计 actor 是两个字段，不得混用。实现面：请求体增 owner 字段（校验前缀），Task 1.1 相应调整；CLI 侧（S5）同原则。
2. **AddSessionMember 无端点（显式不做项）：照准，附核对义务。**spec §2.2 投递寻址化——@寻址与成员集合无关，主 agent 无需成员身份即可被 @（通道已由 B358.3 落地）；成员身份只执法写面与列表投影。真机走查若发现补员路径缺失成实际阻塞，落新卡。记 B358 父卡 integrate 核对项。
3. **红窗核算、ErrBadState 沿既有哨兵、禁触清单：照准。**
