# B156.3.5 计划台账

- 2026-08-27：工作树为 `/root/.handoff/worktrees/4951ea96`，当前分支为 `cards/B156.3.5-charter-2`；初始 `git status --short --branch` 输出为 `## cards/B156.3.5-charter-2`，无工作树改动。
- 2026-08-27：仓内存在 `codegraph/best.json`、`codegraph/target.json`、`codegraph/domains/d_orchestration.json`；执行 `codegraph context d_orchestration` 失败，原文为 `/bin/bash: line 1: codegraph: command not found`。后续以仓内图 JSON 与 `rg`/源码窗口替代，并在计划中记录图覆盖债。
- 2026-08-27：`go run . graph --help` 成功，确认仓内仅有弃用的 graph fallback；随后 `go run . graph context d_orchestration --repo .` 成功，输出 `view=baseline`、best 领域 `d_orchestration`，包为 `internal/agentd`/`internal/store`，`fociTruncated.total=152`，`actual.byCurrentDomain` 为 `d_coordination_task=10`、`d_coordination_api=1`，并报告 stale=5；因此计划同时记录“命令行二进制缺失”和“fallback 图覆盖/陈旧声明债”。
- 2026-08-27：基线 `go build ./...` 成功，退出码 0，stdout/stderr 为空。
- 2026-08-27：基线 `go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v` 成功，原始关键输出为 `--- PASS: TestIgnitionVerticalSlice (0.34s)`、`PASS`、`ok github.com/Xsxdot/handoff/internal/keystone 0.350s`；竖切唤醒/重建 harness 可复用但不能替代 K5 消费者缝测试。
- 2026-08-27：基线 `go test ./internal/agentd/ -run 'TestCoord|TestSquadNode|TestQueue|TestScheduling' -count=1` 成功，原始输出为 `ok github.com/Xsxdot/handoff/internal/agentd 2.186s`；当前相关 agentd 回归为绿。
- 2026-08-27：基线 `go run . graph check --repo .` 成功（`fails: []`），但 stdout 报告 `anchor-off-domain`、`best-dangling`、`container-misplaced`、`legacy`、`oversized-package` 等既有 warnings；本卡只圈定 `internal/agentd/{scheddrain.go,wakeconsumer.go,server.go,coordapi.go,cardstep.go}` 与对应测试，不修图债。
- 2026-08-27：源码核实 `internal/scheduling/scheduling.go:216-222` 的 `Release(squadName, carrierName string) error` 是两级递减且计数归零幂等；`Enqueue` 在 `:224-254` 产生冻结 `queuedEntry{Req,Seq}`，`PopReady` 在 `:256-297` CAS 删除并按 Ready→Priority→Seq 排序；`internal/ledger/events.go:60-77` 的 `EventsFromAsc` 是 `seq > fromSeq` 升序、limit<=0 默认 1000。
- 2026-08-27：源码核实 `internal/agentd/coordapi.go:93-167` 当前路径在 `LaunchAdmit` 成功后调用 `LaunchForCard` 失败只返回 502、不 Release；同时 409 文案只应属于 `LaunchAdmit` 的 `ErrNoSlot` 分支。该遗漏纳入本卡计划及变异复验。
- 2026-08-27：源码核实 `cmd/agentd.go:440-465` 的 `setupLedger` 已完成 `SetLedger` 与镜像启动，但 `rg -n 'SetupAutomation' cmd/agentd.go` 无输出；生产启动尚未调用 `SetupAutomation`，本卡计划补齐 `setupLedger` 后的自动化启动接线。
- 2026-08-27：源码核实 `internal/agentd/cardstep.go:46-124` 在小队 `Admit` 后只 `defer releaseCardStep`，正常/失败的 `runStepFn` 返回没有 `s.scheduling.Release`；K5 计划补上同一已准入 binding 的结束归还，避免执行者槽位单调增长。
- 2026-08-27：源码核实 `internal/ledger/mirror.go:29-78` 把原 task 类型和 payload 包成 `task_mirrored`，`internal/proto/rooms.go:24-40` 定义 `RoomMessage{Room,Kind,Body,Refs,Mentions,DecisionID,BySystem}`；因此事件消费者必须解包 task_mirrored，并只对 `RoomMsgUser && !BySystem` 产出 `WakeMessage`。
- 2026-08-27：`go run . graph sym --help` 成功确认符号查询语法；查询 `internal/agentd/server.go#SetupAutomation`、`internal/agentd/cardstep.go#startCardStep`、`internal/scheduling/scheduling.go#Release` 均失败，原文分别为 `Error: 符号 "..." 不在图中（图未覆盖或名字有误）；近似候选: []。确认图未覆盖时回落 grep，并把该符号记入本节点产出物的「图覆盖债」小节`（随后各有 `exit status 1`）。这些符号已由源码行号和 rg 回落核实，计划不声称图已覆盖。
- 2026-08-27：计划文档已写入 `docs/superpowers/plans/b156.3.5-plan.md`；实现代码未修改。首次 `git diff --check -- docs/superpowers/plans/b156.3.5-plan.md docs/superpowers/plans/b156.3.5-plan-ledger.md` 无输出且退出 0；`git status --short --branch` 输出 `## cards/B156.3.5-charter-2` 加两份未跟踪计划文件。
- 2026-08-27：计划自检 `git diff --check -- docs/superpowers/plans/b156.3.5-plan.md docs/superpowers/plans/b156.3.5-plan-ledger.md` 无输出且退出 0；围栏检查原始输出为 `fenced blocks: balanced`；尖括号检查原始输出为 `angle placeholders: none`；实现占位符检查原始输出为 `implementation placeholders: none`。
- 2026-08-27：计划关键行为检索命中 queue restart、Wake-before-dispatch、approver_disabled 正面兜底、cursor rewind、fallback、502/409、cardstep Release、两级 `coord` 计数、`automationSeen` 与 `StartAutomation`；入口和缝双向清单已覆盖。
- 2026-08-27：最终暂存核验 `git diff --cached --check` 无输出且退出 0；`git diff --cached --name-status` 原始输出为 `A docs/superpowers/plans/b156.3.5-plan-ledger.md`、`A docs/superpowers/plans/b156.3.5-plan.md`；`git status --short --branch` 原始输出为 `## cards/B156.3.5-charter-2` 与两文件均 staged。
- 2026-08-27：首个收口提交命令成功，原始输出为 `[cards/B156.3.5-charter-2 b30900ca] docs: add B156.3.5 automation orchestration plan`、`2 files changed, 1003 insertions(+)`；随后将该提交事实补入台账并 amend，最终提交 hash 以最后一次 `git log` 为准。
- 2026-08-27：实现节点重跑基线 `go build ./...`，原始结果为空输出、退出码 0。
- 2026-08-27：实现节点重跑基线 `go test ./internal/keystone/ -run TestIgnitionVerticalSlice -count=1 -v`，原始结果含 `--- PASS: TestIgnitionVerticalSlice (0.15s)`、`PASS`、`ok github.com/Xsxdot/handoff/internal/keystone 0.160s`，退出码 0。
- 2026-08-27：实现节点重跑基线 `go test ./internal/agentd/ -run 'TestCoord|TestSquadNode|TestQueue|TestScheduling' -count=1`，原始结果为 `ok github.com/Xsxdot/handoff/internal/agentd 2.228s`，退出码 0。
- 2026-08-27：实现节点重跑 `go run . graph check --repo .`，退出码 0，原始 JSON `fails: []`；stdout 同时列出既有 `anchor-off-domain`、`best-dangling`、`container-misplaced`、`legacy`、`oversized-package` warnings，未将其归因于本卡。
- 2026-08-27：Task A 首红 `go test ./internal/agentd/ -run 'TestAutomationQueue|TestSchedulingRelease' -count=1 -v` 在壳实现下退出码 1，原始断言为 `处理行数=0，want 3`；首个编译红中的 `undefined: ledgerstep` 已先修正为测试导入遗漏，当前红确认为功能壳缺失而非 typo。
- 2026-08-27：Task A 夹具在受限环境中不能使用 `setupSquadEnv`：原始失败为 `PTY 测试根目录不可用；... mkdir /tmp/handoff-pty-1127568964: read-only file system` 与仓内 socket 超过上限；新增测试改用现有 `newNoPTYLedgerEnv` 并仅为配置写入准备临时 config，未改变产品路径。
- 2026-08-27：Task A 实现后 `go test ./internal/agentd/ -run 'TestAutomation(QueueRestartReplay|IgnitionDrainWakesBeforeTrueDispatch|RoundReleasesCoordinatorCounters|ReleaseKicksDrain)$' -count=1 -v` 退出码 0，原始结果 4 个主测试 PASS（Round 下 launch/wake 两个子测试 PASS），`ok github.com/Xsxdot/handoff/internal/agentd 0.604s`。
- 2026-08-27：Task A 实现后 `go build ./...` 退出码 0，stdout/stderr 为空。
- 2026-08-27：Task A Release 变异前通过 `rg -n -F 'defer s.releaseSchedulingBinding(card, binding)' internal/agentd/scheddrain.go` 唯一核对得到两处：182、220；移除 launch wrapper 的第一处后 `go build ./...` 退出码 0；行为正控 `go test ./internal/agentd/ -run 'TestAutomationRoundReleasesCoordinatorCounters/launch$' -count=1 -v` 退出码 1，原始断言为 `launch 后计数 squad/coord=1，want 0`，确认测试钉住 Release；变异已恢复。
- 2026-08-27：Task B 首红在补齐测试导入 typo 后，空消费者壳的 `go test ./internal/agentd/ -run 'TestAutomation(Event|Fallback|Approver|Cursor|Attach)' -count=1 -v` 退出码 1；原始断言依次为 `处理唤醒事件数=0，want 6`、`permission_request 应处理 1 条，实得 0`、`首次消费 processed=0 err=<nil>，want 1/nil`、`解除 attach 后 processed=0 err=<nil>，want 1/nil`、`resume/rebuild 双失败 processed=0 escalated=false err=<nil>`，确认当前红来自功能缺失而非编译/拼写错误。
- 2026-08-27：Task B 事件消费者实现后首轮只有兜底回执失败：`go test ... -run 'TestAutomation(Event|Fallback|Approver|Cursor|Attach)' ...` 原始红为 `resume/rebuild 双失败 processed=0 escalated=false err=唤醒协调者回合失败: resume: resume failed; 重建: launch failed`；这是既有 keystone 返回零值 RoundResult 的真实缺口，未改测试期望。
- 2026-08-27：为修复上述缺口仅把 `internal/keystone/keystone.go` 双失败返回改为 `RoundResult{Escalated:true}`；该路径同时已有 `MarkNeedsHuman`，是让 K5 消费者能如实投影升级状态所必需的越界文件，已在本台账显式登记。
- 2026-08-27：修复后 `go test ./internal/agentd/ -run 'TestAutomation(EventMappingThroughConsumer|ApproverDisabledFallsBackToWake|CursorRewindIsIdempotent|AttachDefersAndThenWakes|FallbackResumeRebuildFailure)$' -count=1 -v` 退出码 0，5 个主测试均 PASS，`ok github.com/Xsxdot/handoff/internal/agentd 0.713s`；`go test ./internal/keystone/ -run 'TestIgnitionVerticalSlice|TestLaunchForCardConsumesSessionSpec' -count=1 -v` 退出码 0，2 个测试 PASS，`ok github.com/Xsxdot/handoff/internal/keystone 0.072s`；`go build ./...` 退出码 0，stdout/stderr 为空。
- 2026-08-27：Task B seen 变异前 `rg -n -F 'if duplicate {' internal/agentd/wakeconsumer.go` 唯一命中第 116 行；替换为 `if false && duplicate` 后 `go build ./...` 退出码 0，行为正控 `go test ./internal/agentd/ -run 'TestAutomationCursorRewindIsIdempotent$' -count=1 -v` 退出码 1，原始断言 `游标回退后 processed=1 err=<nil>，want 0/nil`；变异已恢复。
- 2026-08-27：Task C 基线 `go build ./...` 退出码 0、stdout/stderr 为空；`go test ./internal/agentd/ -run 'TestCoord|TestSquadNode|TestQueue|TestScheduling' -count=1` 退出码 0，原始结果 `ok github.com/Xsxdot/handoff/internal/agentd 1.932s`。
- 2026-08-27：Task C 首红 `go test ./internal/agentd/ -run 'TestCoordLaunchFailureReleasesCapacityAndKeeps502|TestCardStepAdmittedRoundReleasesCapacity' -count=1 -v` 退出码 1；原始断言为 `回合结束后计数 squad/sq1=1，want 0` 与 `失败回合后计数 squad/coord=1，want 0`，分别确认执行者和协调者回合缺 Release。
- 2026-08-27：Task C 实现后 no-PTY 缝测试 `go test ./internal/agentd/ -run 'TestAutomationRoundReleasesCoordinatorCounters|TestCoordLaunchFailureReleasesCapacityAndKeeps502|TestCardStepAdmittedRoundReleasesCapacity' -count=1 -v` 退出码 0，3 个主测试 PASS（Round 下 launch/wake 两子测试 PASS），`ok github.com/Xsxdot/handoff/internal/agentd 0.652s`；`go build ./...` 退出码 0，stdout/stderr 为空。
- 2026-08-27：Task C bootstrap 回归 `go test ./cmd/ -run TestSetupLedgerMountsWithRetiredEnabledFlag -count=1 -v` 退出码 0，主测试 PASS，原始结果 `ok github.com/Xsxdot/handoff/cmd 0.166s`；日志实见账本打开、自动化相关启动期间镜像运行并在 context canceled 后退出。
- 2026-08-27：Task C 收窄命令 `go test ./internal/agentd/ -run 'TestAutomationRound|TestCoordLaunch|TestCardStep' -count=1 -v` 退出码 0；本轮新增 no-PTY 测试均 PASS，既有依赖 PTY 测试因原始错误 `PTY 测试根目录不可用；... mkdir /tmp/...: read-only file system` 被 SKIP，未将 skip 写成通过。
- 2026-08-27：Task C cardstep Release 变异前 `rg -n -F 'defer s.releaseSchedulingBinding(cardID, binding)' internal/agentd/cardstep.go` 唯一命中 122 行；移除后 `go build ./...` 退出码 0，`go test ./internal/agentd/ -run 'TestCardStepAdmittedRoundReleasesCapacity$' -count=1 -v` 退出码 1，原始断言 `回合结束后计数 squad/sq1=1，want 0`；变异已恢复。
- 2026-08-27：Task C coordinator wrapper Release 变异前 `rg -n -F 'defer s.releaseSchedulingBinding(card, binding)' internal/agentd/scheddrain.go` 唯一核对到 2 处（185、223）；移除 launch wrapper 的第一处后 `go build ./...` 退出码 0，`go test ./internal/agentd/ -run 'TestCoordLaunchFailureReleasesCapacityAndKeeps502$' -count=1 -v` 退出码 1，原始断言 `失败回合后计数 squad/coord=1，want 0`；变异已恢复。
- 2026-08-27：收尾全量测试 `go test ./...` 退出码 0；原始输出共 50 个 `ok`、0 个 `FAIL`，6 个 `? ... [no test files]`（`internal/collab/client`、`internal/collab/cursor`、`internal/collab/room`、`keysclient`、`ptyapi`、`schedclient`），可作为唯一跨包污染/初始化顺序验收读数。
- 2026-08-27：收尾编译 `go build ./...` 退出码 0，stdout/stderr 为空；收尾静态检查 `go vet ./...` 退出码 0，stdout/stderr 为空。
- 2026-08-27：本轮改动 Go 文件清单为 `cmd/agentd.go`、`internal/agentd/cardstep.go`、`internal/agentd/cardstep_test.go`、`internal/agentd/coordapi.go`、`internal/agentd/coordapi_test.go`、`internal/agentd/scheddrain.go`、`internal/agentd/scheddrain_test.go`、`internal/agentd/server.go`、`internal/agentd/wakeconsumer.go`、`internal/agentd/wakeconsumer_test.go`、`internal/keystone/keystone.go`；命令 `gofmt -l cmd/agentd.go internal/agentd/cardstep.go internal/agentd/cardstep_test.go internal/agentd/coordapi.go internal/agentd/coordapi_test.go internal/agentd/scheddrain.go internal/agentd/scheddrain_test.go internal/agentd/server.go internal/agentd/wakeconsumer.go internal/agentd/wakeconsumer_test.go internal/keystone/keystone.go` 退出码 0、无输出。
- 2026-08-27：gofmt 正控先误用可被 formatter 保持的单行函数，命令退出码 0 且无输出；随后将临时文件改为 `func PositiveControl( ){println("gofmt")}`，同一命令 `gofmt -l /root/.handoff/tmp/e4c7735a/gofmt-positive-control.go` 退出码 0 且原始输出为 `/root/.handoff/tmp/e4c7735a/gofmt-positive-control.go`；正控文件随后删除，未进入工作树。
- 2026-08-27：`git diff --check 17bf7885..HEAD` 退出码 0、无输出；新增代码日志/占位符扫描均无匹配（两条 `git diff -U0 17bf7885..HEAD -- '*.go' | rg ...` 命令退出码 1、stdout 为空，表示没有匹配而非命令绿），未将 grep 的无匹配退出码冒充业务测试通过。
- 2026-08-27：收尾 `go run . graph check --repo .` 退出码 0，原始 JSON 顶层 `fails: []`；仍报告基线已有 `anchor-off-domain`、`best-dangling`、`container-misplaced`、`legacy`、`oversized-package` warnings，本卡未改图文件也未把 warnings 计为本卡失败。
- 2026-08-27：配对原语问一“多了什么”——本轮新增导出符号共 12 个，逐项按 `git grep -n -E '${name}\\(' -- '*.go' | grep -v '_test\\.go'`（生产）与 `git grep -n -E '${name}\\(' -- '*_test.go'`（测试）计数：`StartAutomation` 生产命中 2、测试命中 0；`TestCardStepAdmittedRoundReleasesCapacity` 0、1；`TestCoordLaunchFailureReleasesCapacityAndKeeps502` 0、1；`TestAutomationQueueRestartReplay` 0、1；`TestAutomationIgnitionDrainWakesBeforeTrueDispatch` 0、1；`TestAutomationRoundReleasesCoordinatorCounters` 0、1；`TestAutomationReleaseKicksDrain` 0、1；`TestAutomationEventMappingThroughConsumer` 0、1；`TestAutomationApproverDisabledFallsBackToWake` 0、1；`TestAutomationCursorRewindIsIdempotent` 0、1；`TestAutomationAttachDefersAndThenWakes` 0、1；`TestAutomationFallbackResumeRebuildFailure` 0、1。生产新增导出仅 `StartAutomation`，且已有 `cmd/agentd.go` 消费；其余 11 个是测试导出函数，生产命中为 0 不构成生产哨兵。
- 2026-08-27：问一模式坏了自检：`git grep -n -E 'New\\(' -- '*.go' | grep -v '_test\\.go' | wc -l` 原始输出 `225`，达到三位数，确认 POSIX ERE 模式有效；排除坏了自检：`git grep -n -E 'TestAutomationQueueRestartReplay\\(' -- '*.go' | grep -v '_test\\.go' | wc -l` 原始输出 `0`，同符号未过滤版本原始输出 `1`，确认 `_test.go` 排除生效。
- 2026-08-27：配对原语问二“少了什么”逐条载体清单：队列法定顺序由 `scheduling.QueueKinds` 承载；持久出队/CAS 由 `scheduling.PopReady` 承载；队列请求回填由 `scheduling.Enqueue`、`requeueAutomation` 承载；清队入口由 `drainQueuesOnce` 承载；Ignition Wake-before-dispatch 顺序由 `drainIgnitionRequest`、`wakeCoordinatorRound`、`startCardStep` 承载；协调者 launch 统一入口由 `launchCoordinatorRound` 承载；两级名额归还由 `releaseSchedulingBinding`、`scheduling.Release` 承载；释放后的即时唤醒由 `automationKick`、`kickAutomation` 承载。
- 2026-08-27：配对原语问二继续列出事件契约载体：账本读取由 `autoLedger.EventsFromAsc` 承载；task envelope 解包由 `mirroredTaskEnvelope`、`mirroredTaskTypeAndPayload` 承载；事件映射/过滤由 `automationWakeEvent`、`keystone.WakeKind` 承载；同卡批处理由 `pendingByCard` 承载；游标回退幂等由 `automationCursor`、`automationSeen` 承载；单轮消费由 `consumeAutomationEventsOnce` 承载；成功后投影由 `wakeCoordinatorRound` 承载。
- 2026-08-27：配对原语问二继续列出生命周期与错误投影载体：生产装配由 `setupLedger`、`SetupAutomation`、`StartAutomation` 承载；循环生命周期由 `automationStartOnce`、`automationLoop`、`runAutomationPass` 承载；执行者结束释放由 `startCardStep` 内 `defer s.releaseSchedulingBinding(cardID, binding)` 承载；HTTP launch 分类由 `handleCoordLaunch`、`coordinatorAdmissionError`、`coordinatorLookupError` 承载；resume/rebuild 双失败升级由 `keystone.RoundResult.Escalated`、`MarkNeedsHuman` 承载；遗留载体身份断言由 `carrier/` 断言承载。
- 2026-08-27：分次提交核验：`git log --oneline 17bf7885..HEAD` 已包含 `51a257b0`（Task A）、`b3bb2557`（Task B）、`b73ae2d4`（Task C）三次可独立编译提交；本行收尾台账待作为 Task D 独立提交。
