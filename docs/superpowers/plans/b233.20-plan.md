# B233.20 plan：九个非执行面收窄聚合 client

前置事实（2026-09-13，功能线 59dfb16c5 + .18/.19 之后）：

- B233.16 先例：internal/client/execution.go 定义 `ExecutionClient` + `var _` 编译期断言；
  消费方（StepRunner.Clients）持接口。
- 聚合 client 生产持有者盘点（grep `\*client\.Client`）：internal/ledgermirror/mirror.go
  （Source:35、Machines.For:70、clients:82/333、subscribe:461）、internal/agentd
  （clientForTarget:432、forward.go:112/244 提供方 + 各 handle* 消费点）、cmd
  （runGC:58、runFramesClient:61、syncViaBundleClient:148、runReclaimList:67、runReclaimOne:91、
  runAttach:97、pickAttachTask:127、newTargetClient 族:237/241 等组装点）。

步骤：

1. 在 internal/client 按面定义能力接口（命名沿现有风格，如 `EventStream`、`PtyDial`、
   `ReclaimOps`、`ProjectAdmin`、`QueryTasks`…；一个面一个接口，方法集只含该面实际调用的
   能力），聚合 `Client` 逐个 `var _` 断言。缺方法的能力先补到聚合 client（转发既有通道）。
2. 逐面替换消费方类型引用（字段/签名/局部变量）为能力接口；`Machines.For` 返回收窄；
   `Source` 签名改接口；`Mirror.clients` 改接口 map。
3. agentd 侧：`clientForTarget` 保持返回聚合（提供方），各消费 handler 改持能力接口
   （按面拆分 `clientFor*` 收窄入口，沿 B233.16 具名入口先例）。
4. cmd 侧：run* 函数签名改收各自能力接口；组装点（root.go/Each 命令入口）保持聚合构造注入。
5. 验证与证据：spec §验收 1–4（grep 命令：
   `grep -rn "\*client\.Client" --include="*.go" internal/ cmd/ | grep -v _test`，
   逐一标注白名单身份）。
6. 提交：`refactor(B233.20): 非执行面消费收窄——九面不再持聚合 client`。

执行中偏差如实记入收尾段。
