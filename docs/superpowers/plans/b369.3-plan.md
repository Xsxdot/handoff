# B369.3（S3）S3 工具链 gate 与轮换文档 —— 实现计划

- 卡号：B369.3（父卡 B369；拆解序号 S3）
- 有效基线分支：`cards/B369-charter-4`（本卡合并目标）
- 冻结 contract：`docs/superpowers/specs/b369-contract.md` @ `6f10ba7c`（§0 工具链 gate 回执、§4.4 条 28–29、§8.1/§8.6/§8.9、§9 附区条 4）
- 已拍板拆解：`docs/superpowers/specs/b369-breakdown.md` @ `4ab6415a`（P5=A 壳工程不进本仓、P6=A 轮换文档并入 `mobile/README.md`）
- 上游 spec：`docs/superpowers/specs/2026-09-13-mobile-app-design.md`（状态：已批准；用户故事 8 轮换）
- 图视图：`codegraph/diffs/cards-B369-charter.json`（view=`cards-B369-charter`，base `977720e2`）
- 本节点产出物：`docs/superpowers/plans/b369.3-plan.md`（计划文档；不写实现代码）
- 本节点台账：`docs/superpowers/ledgers/2026-09-19-b369.3-plan-ledger.md`

---

## 0. 本卡干什么（一段话）

S3 把 contract §0 的**两条工程约束**落进 `mobile/` 模块，并给出一份壳工程能照着做的构建说明，再把 spec 用户故事 8 的 **token 轮换唯一处置**写成文档。产物三件：

1. **`mobile/go.mod` 补 `tool golang.org/x/mobile/cmd/gobind` 指令**（+ `go.sum` 同步）——没有它，新版 gomobile 在 bind 时报
   `golang.org/x/mobile` 不在本模块依赖图内。
2. **`mobile/build.sh`**——一条命令产出 `handoff-mobile.aar` / `handoff-mobile.xcframework`；Android 分支**显式 `-androidapi 21`**（NDK 30 移除了 API<21，gomobile 缺省 minsdk=16），iOS 分支 `-target=ios`，脚本 `set -euo pipefail` 且注释记 gomobile / NDK 版本。
3. **`mobile/README.md`**——模块职责、绑定面、构建步骤、两条硬约束，以及 **token 泄露的唯一处置 = 双端轮换节点 token 并重启**（明确「`handoff sessions revoke` 只杀 cookie 会话，救不了 token 泄露」）。

**为什么必须在这里做**：工具链 gate 已在 darwin 双端 PASS（contract §0 台账），但两条约束只存在于台账文字里，模块自身没有承载——换一台机器从头 bind 会重新踩 `unsupported API version 16` 和 `missing golang.org/x/mobile dependency`；轮换文档则是 spec 用户故事 8 的唯一无归属承诺（拆解 §0 待拍板 6，已裁 P6=A 并入本卡）。本卡把它们固化进模块，不靠记忆。

**边界**：不碰 `internal/mobilecore/**`（S2 的面）、不改 `mobile/bind/bind.go`（S4 的面）、不产出壳工程（P5=A）、不改 `codegraph/` 冻结物、不跑全量测试。

---

## 1. 基线事实（本节点实测，动手前已复核）

| 事实 | 原始读数 |
|---|---|
| Go 版本 | `go version go1.26.1 linux/amd64` |
| 起点 HEAD | `4ab6415af5e03b623795243040be48d222f1d3a4`（branch `cards/B369.3-charter-2`，工作树干净） |
| 根模块 build | `go build ./...` → EXIT=0 |
| mobile 模块 build | `cd mobile && go build ./...` → EXIT=0 |
| mobile 模块测试 | `cd mobile && go test ./... -count=1` → `? github.com/Xsxdot/handoff/mobile/bind [no test files]`，EXIT=0 |
| mobile 模块 vet/fmt | `go vet ./...` 空；`gofmt -l .` 空 |
| 根含 mobile？ | `go list ./... \| grep -c handoff/mobile` → `0`（嵌套 module 出根构建图，contract 条 22 复核） |
| 契约闸基线 | `go test ./cmd/ -run TestRepoContractGate -count=1` → `ok github.com/Xsxdot/handoff/cmd 0.066s` |
| 图闸基线 | `codegraph --repo . check --view cards-B369-charter` → `fails=[]`；`codegraph --repo . validate --view cards-B369-charter` → `issues=null`、`edgeIssues=null`；`codegraph --repo . check`（基线）→ `fails=[]` |
| `mobile/go.mod` 的 tool 指令 | **不存在**（`grep -n "tool golang.org/x/mobile" mobile/go.mod` 无命中） |
| `mobile/build.sh` / `mobile/README.md` | **均不存在**（`ls mobile/` 仅 `bind/ go.mod go.sum`） |
| 现状 `go tool` 解析 | `cd mobile && go tool -n golang.org/x/mobile/cmd/gobind` → `go: no such tool "golang.org/x/mobile/cmd/gobind"`，EXIT=2 |
| 工具链 gate 台账 | contract §0：go 1.26.1 + gomobile@`v0.0.0-20260908204917-8b95e45f8d3e` + Xcode 26.6 + NDK 30.0.16248370，darwin 双端 PASS（spike） |
| 轮换命令事实 | `handoff sessions revoke` 只吊销 cookie 会话（`cmd/sessions.go#sessionsRevokeCmd`，注释「主令牌不可吊销」）；`handoff init` **不**改 token（`cmd/init.go:186` 只打印 `cfg.Token`）；无 token 轮转子命令 |
| token 事实 | `cfg.Token = randToken()` = 16 字节 / 32 hex（`internal/config/config.go:641`）；relay 形态 token 同时是 E2E PSK 源（contract §1.1） |

### 1.1 库行为出处（两条例外事实，均读到源码原文）

- **`tool` 指令是 gomobile 的硬前置**：`golang.org/x/mobile/cmd/gomobile/bind.go:87` 调 `mobileModuleAvailable()`；失败时报
  `gomobile bind requires golang.org/x/mobile in the current module, but it is not in the module dependency graph.`（`:92` 提示 `go get -tool golang.org/x/mobile/cmd/gobind`）；`mobileModuleAvailable`（`:391`）以 `packages.Load("golang.org/x/mobile/bind")` 的成功与否判定。模块缓存路径：`$GOMODCACHE/golang.org/x/mobile@v0.0.0-20260908204917-8b95e45f8d3e/cmd/gomobile/bind.go`。
- **`go get -tool` 的产出**（本节点在 `$TMPDIR` 的 `mobile/` 副本上实测，非生产工作树）：见 §1.2。

### 1.2 tool 指令落地读数（$TMPDIR 探针实测）

在 `$TMPDIR/mobile-probe3`（`cp -r mobile/.`，把 `replace ... => ../` 改为绝对路径指向真实工作树）执行：

```
$ printf '\ntool golang.org/x/mobile/cmd/gobind\n' >> go.mod
$ go get -tool golang.org/x/mobile/cmd/gobind@v0.0.0-20260908204917-8b95e45f8d3e
$ go mod tidy
$ go build ./...            → EXIT=0
$ go mod tidy -diff         → EXIT=0（干净）
$ go tool golang.org/x/mobile/cmd/gobind   → 正常启动（"no exported names in the package \".\""）
```

`$TMPDIR/probe-off` 另证 `GOPROXY=off go get -tool ...@v0.0.0-20260908204917-8b95e45f8d3e` → EXIT=0（模块已在本地 module cache，沙箱无网也可解析）。
`$TMPDIR/probe-get`（`go get` + `go mod tidy`）与 `probe3` 产出的 `go.mod` / `go.sum` **逐字节相同**，故 git 提交结果是确定的。

**期望 `mobile/go.mod` 终态**（相对现状的差异仅两处：间接依赖块 + 末尾 tool 行）：

```go
// 移动端 App 的连接核绑定模块（B369）。独立嵌套 Go module，经 replace 指回主模块；
// 与 desktop/ 同构：内部协议逻辑零重实现，只把 internal/mobilecore 经 gomobile
// 编成 Android AAR / iOS XCFramework。反向依赖禁止（根模块不得 import 本模块）。
module github.com/Xsxdot/handoff/mobile

go 1.26.1

require github.com/Xsxdot/handoff v0.0.0-00010101000000-000000000000

require (
	github.com/coder/websocket v1.8.15 // indirect
	github.com/flynn/noise v1.1.0 // indirect
	github.com/hashicorp/yamux v0.1.2 // indirect
	golang.org/x/crypto v0.55.0 // indirect
	golang.org/x/mobile v0.0.0-20260908204917-8b95e45f8d3e // indirect
	golang.org/x/mod v0.41.0 // indirect
	golang.org/x/sync v0.23.0 // indirect
	golang.org/x/sys v0.48.0 // indirect
	golang.org/x/tools v0.50.0 // indirect
)

replace github.com/Xsxdot/handoff => ../

tool golang.org/x/mobile/cmd/gobind
```

**`mobile/go.sum` 的确定变化**（相对现状）：删除 `x/sync v0.22.0`、`x/sys v0.47.0` 两行（各 h1 + /go.mod），新增以下 12 行（`go-cmp` 及 mobile 的传递依赖）：

```
github.com/google/go-cmp v0.6.0 h1:ofyhxvXcZhMsU5ulbFiLKl/XBFqE1GSq7atu8tAmTRI=
github.com/google/go-cmp v0.6.0/go.mod h1:17dUlkBOakJ0+DkrSSNjCkIjxS6bF9zb3elmeNGIjoY=
golang.org/x/mobile v0.0.0-20260908204917-8b95e45f8d3e h1:zYnHsDmxfxKH/x9DjYNAdoIdMsP8enB1Ncb10b3pdHk=
golang.org/x/mobile v0.0.0-20260908204917-8b95e45f8d3e/go.mod h1:+g6JKqLjAprgkdZKuuBhkhIN9zvSktraeSABDN2M7p8=
golang.org/x/mod v0.41.0 h1:qJmnOUb4YB+FsEuM3HcWucdZASCPGhsX6uljO6pog0c=
golang.org/x/mod v0.41.0/go.mod h1:Ek9pY8RKWXwsWvd3rQiHYtMqkjSUV+s1Rj7j4H5Ur6o=
golang.org/x/tools v0.50.0 h1:c2ifzfcuY7L90lZ2aKd8S4K2NpASF08SZx9ZuJkHmSU=
golang.org/x/tools v0.50.0/go.mod h1:7ulVMw3831Mwi5EZD6RomGyffr4VFjuNYXf2BbCEAV0=
golang.org/x/tools/go/expect v0.1.1-deprecated h1:jpBZDwmgPhXsKZC6WhL20P4b/wmnpsEAGHaNy0n/rJM=
golang.org/x/tools/go/expect v0.1.1-deprecated/go.mod h1:eihoPOH+FgIqa3FpoTwguz/bVUSGBlGQU67vpBeOrBY=
golang.org/x/tools/go/packages/packagestest v0.1.1-deprecated h1:1h2MnaIAIXISqTFKdENegdpAgUXz6NrPEsbIeWaBRvM=
golang.org/x/tools/go/packages/packagestest v0.1.1-deprecated/go.mod h1:RVAQXBGNv1ib0J382/DPCRS/BPnsGebyM1Gj5VSDpG8=
```

（`go mod tidy` 产出即上述形状；hash 由模块版本唯一决定，本节点已实测。`golang.org/x/sync` / `golang.org/x/sys` 的 0.23.0 / 0.48.0 两行随升级替换原 0.22.0 / 0.47.0 两行。）

---

## 2. 图覆盖债

本节点亲测 `codegraph --repo . sym <名>`：

- **命中（基线图）**：`NewDialer` → `n_relay_NewDialer`（`d_transport_tunnel`）。
- **命中（卡视图）**：`EncodePairBundle --view cards-B369-charter` → `n_proto_EncodePairBundle`（`d_protocol`）；`Core.Pair --view cards-B369-charter` → `n_mobilecore_Core_Pair`（`d_transport_channel`）。
- **未命中（图覆盖债）**：本卡三件产物 `mobile/go.mod`、`mobile/build.sh`、`mobile/README.md` 全部在 `mobile/` 嵌套独立 module，**图外**——扫描配方 `scripts/codegraph-rescan/main.go` 排除 `mobile` 前缀（contract §1.3、breakdown §1.2）。本卡不新增任何图内符号，**不补图、不改 `codegraph/` 冻结物**；该债记于此，供后续图对齐节点处置。

---

## 3. Task DAG

```text
T1 静态契约锁测试（先写，预期红）
   │   新增 mobile/toolchain_test.go：读 go.mod / build.sh / README.md 断言
   ▼
T2 mobile/go.mod 加 tool 指令（+ go.sum 同步）
   │   跑 T1 → go.mod 断言转绿，build.sh/README 两条仍红
   ▼
T3 mobile/build.sh（新）
   │   跑 T1 → build.sh 断言转绿
   ▼
T4 mobile/README.md（新，含 token 轮换）
   │   跑 T1 → 全绿
   ▼
T5 收口复核（mobile build/vet/fmt/test + 根反依赖 + 契约闸 + 图三闸；不改图）
```

**最薄路径条**：本卡要锁的行为「`mobile/` 模块自带 gobind tool 指令、构建脚本带两条硬约束、README 写明轮换唯一处置」**今天从任何缝都得不到预期结果**（`go.mod` 无 tool、`build.sh`/`README.md` 不存在）。故 DAG 第一条就是 T1——把断言写下来**必然红**（`os.ReadFile` 失败 / `strings.Contains` 为假），T2–T4 落盘后转绿。这是本卡唯一的红绿周期。

**已知假绿风险（写进验收，不掩盖）**：linux 工作树**无法**跑真实 `gomobile bind`（无 Xcode/NDK）。本卡的机内判据只到「Go 源码可编译 + 工具链开关与文档内容正确」；**`go build ./...` 全绿 ≠ 能产出 AAR/XCFramework**。真实 bind 归 darwin / 移动 CI（§7）。

---

## 4. Task T1：静态契约锁测试（先写，预期红）

### 4.1 精确文件与改动

- **新**：`mobile/toolchain_test.go`（`package main`；`mobile/` 根目录 test-only 包，与 S4 的 `mobile/moduleisolation_test.go` 同款形态——嵌套 module 图外，不进根构建图）。
- 测试范围（最小化）：只跑 `cd mobile && go test ./... -count=1`；不跑全量测试。
- 关键节点日志：本 task 交付物是测试，无生产日志面；可观测性以 `t.Fatalf` 报文体承载（会打印被查文件全文）。不向任何产品文件加 `print`/`log`。
- 注释：文件头写职责+边界；三个测试各写「为什么必须锁」。

### 4.2 Interfaces

- **Consumes**（只读文本文件，不改）：
  - `mobile/go.mod`（期望含 `tool golang.org/x/mobile/cmd/gobind`）
  - `mobile/build.sh`（期望含 `set -euo pipefail`、`-androidapi 21`、`-target=ios`、`NDK`）
  - `mobile/README.md`（期望含 `轮换`、`重启`、`revoke`）
- **Produces**（本卡测试内符号，不跨卡消费）：`mobileDir(t)`、`readMobile(t, p)`、`TestGoModHasGobindToolDirective`、`TestBuildScriptCarriesAndroidAPI21`、`TestReadmeDocumentsTokenRotation`。
- 先例形态：`mobile/moduleisolation_test.go#mobileRoot`（用 `runtime.Caller(0)` 定位，不依赖 cwd；S4 卡，图外）。

### 4.3 步骤

**步骤 1（写测试文件全文）**：

```go
// 本文件锁 mobile/ 模块的工具链与构建契约：go.mod 的 gobind tool 指令、
// build.sh 的两条硬约束（Android -androidapi 21 / iOS -target=ios）、
// README 的 token 轮换唯一处置。三者都是壳工程能编起来的前提，而本仓
// linux 开发机跑不了真正的 gomobile bind——这些静态契约是机内唯一能锁的。
//
// 边界：不调用 gomobile / NDK / Xcode，不发网络请求；只读本目录下的文本文件。
package main

import (
	"os"
	"path/filepath"
	"runtime"
	"strings"
	"testing"
)

// mobileDir 返回本测试文件所在目录（mobile/ 模块根），不依赖 go test 的 cwd。
func mobileDir(t *testing.T) string {
	t.Helper()
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	return filepath.Dir(thisFile)
}

// readMobile 读 mobile/ 下相对路径 p 的文本；读不到即 Fail。
func readMobile(t *testing.T, p string) string {
	t.Helper()
	b, err := os.ReadFile(filepath.Join(mobileDir(t), p))
	if err != nil {
		t.Fatalf("读 mobile/%s 失败: %v", p, err)
	}
	return string(b)
}

// TestGoModHasGobindToolDirective 锁 go.mod 的 tool 指令。
// 没有它，新版 gomobile 在 bind 时报 golang.org/x/mobile 不在依赖图内
// （见 golang.org/x/mobile/cmd/gomobile/bind.go#mobileModuleAvailable）。
func TestGoModHasGobindToolDirective(t *testing.T) {
	gomod := readMobile(t, "go.mod")
	if !strings.Contains(gomod, "tool golang.org/x/mobile/cmd/gobind") {
		t.Fatalf("mobile/go.mod 缺 tool golang.org/x/mobile/cmd/gobind 指令"+
			"（gomobile bind 会报 golang.org/x/mobile 不在依赖图内）:\n%s", gomod)
	}
}

// TestBuildScriptCarriesAndroidAPI21：Android 分支必须显式 -androidapi 21
// （NDK 30 移除 API<21，gomobile 缺省 minsdk=16），脚本必须 fail-closed，
// iOS 分支必须 -target=ios；注释须记 NDK 版本（版本不写死就会随环境漂）。
func TestBuildScriptCarriesAndroidAPI21(t *testing.T) {
	sh := readMobile(t, "build.sh")
	for _, want := range []string{"set -euo pipefail", "-androidapi 21", "-target=ios", "NDK"} {
		if !strings.Contains(sh, want) {
			t.Fatalf("mobile/build.sh 缺 %q:\n%s", want, sh)
		}
	}
}

// TestReadmeDocumentsTokenRotation：README 必须写明 token 泄露的唯一处置是
// 轮换节点 token 并重启，且点出 revoke（吊销 cookie 会话）不足以处置 token 泄露。
func TestReadmeDocumentsTokenRotation(t *testing.T) {
	readme := readMobile(t, "README.md")
	for _, want := range []string{"轮换", "重启", "revoke"} {
		if !strings.Contains(readme, want) {
			t.Fatalf("mobile/README.md 缺 %q——轮换文档不完整:\n%s", want, readme)
		}
	}
}
```

**步骤 2（跑红）**：

```bash
cd mobile && go test ./... -count=1
```

预期（三支全红，因为三个文件此刻都不含所需内容；原文形状）：

```
--- FAIL: TestGoModHasGobindToolDirective (...)
    ... mobile/go.mod 缺 tool golang.org/x/mobile/cmd/gobind 指令 ...
--- FAIL: TestBuildScriptCarriesAndroidAPI21 (...)
    ... 读 mobile/build.sh 失败: open .../mobile/build.sh: no such file or directory
--- FAIL: TestReadmeDocumentsTokenRotation (...)
    ... 读 mobile/README.md 失败: open .../mobile/README.md: no such file or directory
FAIL
```

**步骤 3（变异验证——判据有牙）**：T2–T4 落盘、T1 转绿后，做一次变异自验并**立即还原**：
把 `mobile/build.sh` 里的 `-androidapi 21` 临时改成 `-androidapi 16` → `TestBuildScriptCarriesAndroidAPI21` 必红；
`git checkout -- mobile/build.sh` 还原后绿。（本卡无产品代码，变异只碰新增产物，不进提交。）
`mobile/README.md` 的轮换段若把「轮换节点 token 并重启」删成「吊销会话即可」，`TestReadmeDocumentsTokenRotation` 会因缺 `轮换`/`重启` 变红——这是「不得把轮换写成吊销 cookie 即够」的最低机内防线；语义级审阅仍归 review。

### 4.4 测试清单与缝覆盖

| 测试 | 入口（缝） | 锁什么 |
|---|---|---|
| `TestGoModHasGobindToolDirective` | `mobile/go.mod` 文本 | contract §4.4 条 29 / §8.1：gobind tool 指令在模块内 |
| `TestBuildScriptCarriesAndroidAPI21` | `mobile/build.sh` 文本 | contract §0 约束 2 / §8.6：Android `-androidapi 21`、iOS `-target=ios`、fail-closed、版本注释 |
| `TestReadmeDocumentsTokenRotation` | `mobile/README.md` 文本 | contract §8.9 / spec 用户故事 8：轮换唯一处置落地 |

**缝 → 测试**：本卡锁的缝 =「`mobile/` 模块的可复现构建契约」（go.mod tool 指令 + build.sh 两条硬约束）与「token 轮换文档」。三支测试逐条覆盖 contract §0/§4.4/§8.1/§8.6/§8.9，无遗漏的验收条目。
**测试 → 缝**：三支测试的入口都是本卡直接交付的产物文件（go.mod / build.sh / README.md），**全部是缝级断言，无内部锁**。
**变异验证**：`-androidapi 21 → 16` 使第二支红（§4.3 步骤 3）；README 删轮换段使第三支红；删 go.mod tool 行使第一支红。

---

## 5. Task T2：`mobile/go.mod` 加 tool 指令（+ `go.sum` 同步）

### 5.1 精确文件与改动

- **改**：`mobile/go.mod`、`mobile/go.sum`。
- 不改 `mobile/bind/**`。
- 测试范围：`cd mobile && go test ./... -count=1`（T1 第一支转绿）；`cd mobile && go build ./...`。

### 5.2 步骤

**步骤 1（加指令并整理依赖）**：

```bash
cd mobile
go get -tool golang.org/x/mobile/cmd/gobind@v0.0.0-20260908204917-8b95e45f8d3e
go mod tidy
```

预期（本节点在 `$TMPDIR` 副本实测原文；沙箱无网时模块已在本地 module cache，可加 `GOPROXY=off`，本节点实测该形式 `go get` EXIT=0）：

```
go: added golang.org/x/mobile v0.0.0-20260908204917-8b95e45f8d3e
go: added golang.org/x/mod v0.41.0
go: upgraded golang.org/x/sync v0.22.0 => v0.23.0
go: upgraded golang.org/x/sys v0.47.0 => v0.48.0
go: upgraded golang.org/x/tools v0.0.0-20180917221912-90fa682c2a6e => v0.50.0
```

**步骤 2（核对终态与 §1.2 一致）**：

```bash
cd mobile
go mod tidy -diff        # 预期无输出、EXIT=0
go build ./...           # 预期 EXIT=0
go test ./... -count=1   # T1 第一支转绿，第二、三支仍红
go tool -n golang.org/x/mobile/cmd/gobind   # 预期打印缓存内 gobind 路径（不再是 "no such tool"）
```

**步骤 3（版本 pin 说明）**：`go get -tool` 会把 gomobile 版本钉在 go.mod 的间接依赖里（`golang.org/x/mobile v0.0.0-20260908204917-8b95e45f8d3e`）。`build.sh` 与 README 引用同一版本字符串（§6/§7），三处必须一致。

### 5.3 验收

- `cd mobile && go build ./...` 退出 0；`go mod tidy -diff` 干净。
- `mobile/go.mod` 末行含 `tool golang.org/x/mobile/cmd/gobind`；间接依赖含 `golang.org/x/mobile v0.0.0-20260908204917-8b95e45f8d3e`。
- `go tool -n golang.org/x/mobile/cmd/gobind` 退出 0。
- 根模块 `go list ./...` 仍不含 `mobile/`（本卡不改反向依赖，复核不回归）。

---

## 6. Task T3：`mobile/build.sh`（新）

### 6.1 精确文件与改动

- **新**：`mobile/build.sh`（可执行，`chmod +x`）。
- 不改其它文件。
- 测试范围：`cd mobile && go test ./... -count=1`（T1 第二支转绿）；脚本语法 `bash -n mobile/build.sh`。

### 6.2 文件全文

```bash
#!/usr/bin/env bash
# mobile/build.sh —— 用 gomobile 把 mobile/bind 绑定面编成 Android AAR / iOS XCFramework。
#
# 职责：给壳工程一条可复现的绑定产物构建命令，把工具链版本与两条硬约束写死在这里。
#
# 边界：
#   - 只在装了 Xcode / Android NDK 的机器上跑（darwin）；本仓 linux 开发机不执行它
#   - 不修改任何被跟踪文件；产物落 $ROOT/dist/mobile/（已被 .gitignore 的 /dist/ 覆盖）
#   - 不含协议逻辑：绑定面在 mobile/bind，核在 internal/mobilecore
#
# 工具链（gate 台账：docs/superpowers/specs/b369-contract.md §0，darwin 双端 PASS）：
#   go 1.26.1 + gomobile@v0.0.0-20260908204917-8b95e45f8d3e + Xcode 26.6 + NDK 30.0.16248370
#
# 两条硬约束（写死在下面的命令里）：
#   1. Android 显式 -androidapi 21：NDK 30 移除了 API<21 的 platform，而 gomobile
#      缺省 minsdk=16，不加会在 bind 时报 "unsupported API version 16"。
#   2. go.mod 带 tool golang.org/x/mobile/cmd/gobind：新版 gomobile 要求
#      golang.org/x/mobile 在本模块依赖图内，否则报 "missing golang.org/x/mobile dependency"。
set -euo pipefail

GOMOBILE_VERSION="v0.0.0-20260908204917-8b95e45f8d3e"
NDK_VERSION="30.0.16248370"

MODULE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT="$(cd "$MODULE_DIR/.." && pwd)"
OUT_DIR="$ROOT/dist/mobile"

usage() {
  printf '用法：%s android|ios|all\n' "$0" >&2
}

target="${1:-}"
case "$target" in
  android|ios|all) ;;
  -h|--help|"")
    usage
    exit 0
    ;;
  *)
    printf '未知目标：%s\n' "$target" >&2
    usage
    exit 2
    ;;
esac

command -v gomobile >/dev/null 2>&1 || {
  printf '找不到 gomobile；安装：go install golang.org/x/mobile/cmd/gomobile@%s\n' "$GOMOBILE_VERSION" >&2
  exit 1
}
command -v gobind >/dev/null 2>&1 || {
  printf '找不到 gobind；安装：go install golang.org/x/mobile/cmd/gobind@%s（或先跑 gomobile init）\n' "$GOMOBILE_VERSION" >&2
  exit 1
}
mkdir -p "$OUT_DIR"

build_android() {
  if [ -z "${ANDROID_NDK_HOME:-}" ]; then
    printf 'Android 构建需要 ANDROID_NDK_HOME（例：<sdk>/ndk/%s）\n' "$NDK_VERSION" >&2
    exit 1
  fi
  printf '==> Android AAR（-androidapi 21）\n' >&2
  ( cd "$MODULE_DIR" && gomobile bind -target=android -androidapi 21 \
      -o "$OUT_DIR/handoff-mobile.aar" ./bind )
}

build_ios() {
  printf '==> iOS XCFramework\n' >&2
  ( cd "$MODULE_DIR" && gomobile bind -target=ios \
      -o "$OUT_DIR/handoff-mobile.xcframework" ./bind )
}

case "$target" in
  android) build_android ;;
  ios) build_ios ;;
  all)
    build_android
    build_ios
    ;;
esac

printf '==> 产物已写入 %s\n' "$OUT_DIR" >&2
```

### 6.3 步骤

**步骤 1**：写文件、`chmod +x mobile/build.sh`。
**步骤 2（机内可跑部分）**：

```bash
bash -n mobile/build.sh                          # 预期 EXIT=0（语法）
cd mobile && go test ./... -count=1              # T1 第二支转绿，第三支仍红
```

**步骤 3（不做了什么，写进验收）**：**不在本工作树跑** `bash mobile/build.sh android|ios|all`——linux 无 Xcode/NDK/gomobile，跑它只会得到「找不到 gomobile」的假红，不构成对脚本正确性的验证。真实 bind 归 darwin（§7 #1）。脚本正确性机内判据 = 语法通过 + T1 内容断言 + contract §0 darwin 台账。
**步骤 4（变异验证）**：见 §4.3 步骤 3。

### 6.4 验收

- `bash -n mobile/build.sh` 退出 0。
- 文件含 `set -euo pipefail`、字面量 `-androidapi 21`、`-target=ios`、NDK 版本注释（T1 第二支锁定）。
- 失败路径非零退出：`gomobile`/`gobind` 缺失、`ANDROID_NDK_HOME` 未设、未知目标 → `exit 1`/`exit 2`，无静默继续。

---

## 7. Task T4：`mobile/README.md`（新，含 token 轮换唯一处置）

### 7.1 精确文件与改动

- **新**：`mobile/README.md`。
- 不改其它文件。
- 测试范围：`cd mobile && go test ./... -count=1`（T1 第三支转绿，全绿）。

### 7.2 文件全文

```markdown
# mobile/ —— 移动端 App 的 Go 连接核绑定模块

本模块是 B369 移动端 App 的 **Go 连接核**：把 `internal/mobilecore`（拨 relay/直连
隧道 + 回环反代 + 配对载体解析）经 gomobile 编成 Android AAR / iOS XCFramework，
供 Kotlin/Swift 薄壳调用。壳侧只做 webview 容器、扫码配对、凭据入安全存储
（Keychain/Keystore），**一切协议逻辑在 Go 核**，壳内零重实现。

## 结构

- `bind/` —— gomobile 绑定面（壳与核的唯一交接面）。导出面见下节，别在别处另加
  导出符号（绑定面积最小 = 攻击面最小）。
- 本模块是**独立嵌套 Go module**（自带 `go.mod` + `replace ../`），与 `desktop/`
  同构：根模块的 `go list ./...` 不含它；反向依赖禁止（根模块不得 import 本模块）。

## 绑定面（壳的契约面）

    func Pair(bundleJSON string) (mobilecore.PairResult, error)
    func Origin(machine string) (string, error)
    func MachineNames() []string
    func Close() error

导出面**不含** `Token` / `Dial` / `Credential` 等可绕过回环门禁的入口（回环反代
不注入凭据是承重安全属性，见 contract §4.2/§6）。

## 构建

前置：go 1.26.1、gomobile、Xcode（iOS）、Android SDK + NDK 30.0.16248370。

    go install golang.org/x/mobile/cmd/gomobile@v0.0.0-20260908204917-8b95e45f8d3e
    go install golang.org/x/mobile/cmd/gobind@v0.0.0-20260908204917-8b95e45f8d3e

    ./build.sh android      # → ../dist/mobile/handoff-mobile.aar
    ./build.sh ios          # → ../dist/mobile/handoff-mobile.xcframework
    ./build.sh all

**两条硬约束**（写死在 `build.sh` 里，改动前先读它的注释）：

1. **Android 必须显式 `-androidapi 21`**：NDK 30 移除了 API<21 的 platform，而
   gomobile 缺省 minsdk=16，不加会在 bind 时报 `unsupported API version 16`。
2. **`mobile/go.mod` 必须带 `tool golang.org/x/mobile/cmd/gobind` 指令**：新版
   gomobile 要求 `golang.org/x/mobile` 在本模块依赖图内，否则报
   `missing golang.org/x/mobile dependency`。

**`go build ./...` ≠ `gomobile bind`**：前者只证明 Go 源码可编译，不证明能产出
AAR/XCFramework。真正的 bind 只能在装了 Xcode/NDK 的机器上跑；本仓 linux 开发机
跑不了，别把 `go build` 全绿当成绑定产物已验证。

## 凭据卫生与 token 轮换

配对 bundle / QR 的保护等级**等同主令牌集**：不落日志、即扫即弃。手机丢失先吊销
浏览器会话；需要更狠时按下述流程轮换。

- **先吊销浏览器会话**：`handoff sessions revoke <session-id>` 只杀 cookie 会话
  （`DELETE /api/auth/sessions/{id}`），**不能吊销节点 token**——它救不了 token 泄露。
- **token 泄露的唯一处置：双端轮换节点 token 并重启。**

  1. 在泄露的节点机上把 `~/.handoff/config.yaml` 的 `token:` 改成新的随机值
     （32 位十六进制；可用 `openssl rand -hex 16` 生成）。`handoff init` 重跑**不会**
     换 token（默认取当前配置），必须手改。
  2. 重启该机 agentd：`handoff service restart`（未托管时手工重启进程）。
  3. 在协调机 `~/.handoff/config.yaml` 对应 target 里把 `token:` 改成同一个新值。
     relay 形态下 token 同时是 E2E PSK 源，两端必须一致；改完无需重启 CLI（每次
     调用现读配置）。
  4. 手机上的旧配对 bundle 立即失效，需重新扫码配对。

> 为什么不是「吊销 cookie 即够」：cookie 会话是 ticket 兑换出来的短命凭证，吊销它
> 不影响 token 本体；token 是 agentd 的主 Bearer 令牌，持它 = 持该机全量 API 权限
> （contract §3.2 凭据模型）。只有把 token 换成新值，泄露的那份副本才作废。
```

### 7.3 步骤

**步骤 1**：写文件。
**步骤 2**：

```bash
cd mobile && go test ./... -count=1    # 三支全绿
```

**步骤 3（内容自检，非机内逻辑）**：逐条对照 contract §8.9——「token 泄露唯一处置 = 双端轮换节点 token 并重启」；文档不得把轮换写成「吊销 cookie 会话即够」（README 已显式写反例一句）。轮换步骤里不发明不存在的子命令（现状无 token 轮转子命令，故步骤是编辑 `config.yaml` + `handoff service restart`）。

### 7.4 验收

- `mobile/README.md` 存在，含轮换唯一处置（双端轮换 token + 重启），并显式区分 `handoff sessions revoke` 只杀 cookie 会话（T1 第三支锁定 `轮换`/`重启`/`revoke`）。
- 文档写明 `go build` ≠ `gomobile bind`。
- 文档中的 gomobile 版本与 `build.sh`、`go.mod` 三处一致（`v0.0.0-20260908204917-8b95e45f8d3e`）。

---

## 8. Task T5：收口复核（不改图、不改产品代码）

测试范围：仅下述命令；不跑全量测试。

```bash
go build ./...                                        # 根：预期 EXIT=0
cd mobile && go build ./... && go vet ./... && gofmt -l . && go test ./... -count=1   # 预期全绿
git diff --stat                                       # 交付物仅：mobile/go.mod, mobile/go.sum, mobile/build.sh, mobile/README.md, mobile/toolchain_test.go, docs/**
go list ./... | grep -c 'handoff/mobile'              # 预期 0
go test ./cmd/ -run TestRepoContractGate -count=1     # 预期 ok
codegraph --repo . check --view cards-B369-charter    # 预期 fails=[]
codegraph --repo . validate --view cards-B369-charter # 预期 issues=null edgeIssues=null
codegraph --repo . check                              # 预期 fails=[]
```

若任一图闸意外变红，**停下提问**，不自行改 `codegraph/` 冻结物。本卡不产生图 diff（产物全在 `mobile/` 图外 + 文档）。

---

## 9. 整卡验收判据（行为化）

1. `mobile/go.mod` 含 `tool golang.org/x/mobile/cmd/gobind` 指令与 `golang.org/x/mobile v0.0.0-20260908204917-8b95e45f8d3e`；`cd mobile && go mod tidy -diff` 干净。
2. `cd mobile && go build ./...` 退出 0；`go build ./...`（根）退出 0。
3. `mobile/build.sh` 存在且可执行，含 `set -euo pipefail`、字面量 `-androidapi 21`、`-target=ios`、NDK 版本注释；`bash -n mobile/build.sh` 退出 0。
4. `mobile/README.md` 含 token 泄露唯一处置（双端轮换 token + 重启），显式区分 `revoke` 不足；含 `go build` ≠ `gomobile bind`。
5. `cd mobile && go test ./... -count=1` 三支测试（`TestGoModHasGobindToolDirective`、`TestBuildScriptCarriesAndroidAPI21`、`TestReadmeDocumentsTokenRotation`）全绿；变异（`-androidapi 21 → 16`）可红，还原后绿。
6. `go list ./...`（根）不含 `mobile/`；`go test ./cmd/ -run TestRepoContractGate` PASS；`codegraph check/validate --view cards-B369-charter` 与基线 `check` 保持 0 fail / 0 issue。
7. **未验证（不得写成结论）**：真实 `gomobile bind` 产出 AAR/XCFramework、macOS shell 兼容性、真机壳加载——归 §10。

---

## 10. 未验证，需真机 / darwin（不属本卡机内闭环）

- **真实工具链 bind**：`mobile/build.sh android|ios` 在本工作树（linux，无 Xcode/NDK）**跑不了**，AAR/XCFramework 产出未验证；以 contract §0 的 darwin 双端 PASS 台账为据（spike 曾产出 `spike.xcframework` 两 slice + `spike.aar` 四 ABI）。**不得把 linux 上没跑成的 bind 写成结论。**
- **macOS shell 兼容性**：脚本按 bash 写；macOS 默认 bash 3.2 / zsh 下的行为未实测（`set -euo pipefail`、`BASH_SOURCE`、`command -v` 均属通用写法，但仍未验证）。
- **壳侧接线**：`mobile/bind` 导出面在真机（Kotlin/Swift）加载与调用未验证（P5=A 壳工程不进本仓；归 S4/真机清单）。
- **token 轮换流程的端到端**：文档给出的步骤基于现状命令（`handoff service restart` + 编辑 `config.yaml`）静态核对，未在真机跑一遍完整轮换。

---

## 11. 五项检查（自审）

### 11.1 缺陷族对抗审查

1. **生命周期/状态机中断**：无——构建是幂等的一次性命令；轮换是文档。
2. **静默失败/误导报错**：`build.sh` `set -euo pipefail`；工具缺失/`ANDROID_NDK_HOME` 缺失/未知目标各自非零退出并给可行动报错；`go build` 全绿被明确声明为「≠ 能 bind」，防假绿。
3. **跨平台假设**：本卡全在移动工具链（darwin + NDK/Xcode），linux 无法验证——已在 §10 记「未验证」；脚本注释写明 NDK 30 / androidapi 21 的来由。
4. **假红/假绿**：无自动化测试可跑 bind；本卡明确区分「go build 全绿」与「产物已出」。T1 的变异验证保证内容断言有牙。
5. **门禁绕过**：不适用（无权限面）。
6. **序列化边界**：不适用（无序列化）。
7. **枚举新值过既有白名单**：不适用。
8. **承重安全属性**：轮换文档是「token 泄露唯一处置」的承载——文档把它写成 revoke 即够会让泄露副本永久有效。T1 锁关键词，语义审阅归 review。
9. **webview 候选族**：不适用（构建卡）。

### 11.2 序列化边界设问

不适用：本卡不产生任何跨进程数据字段，无手写序列化/投影。`go.mod`/`go.sum` 是 Go 工具链的确定性产出（`go mod tidy` 校验），非手搭。

### 11.3 上下文预算检查

有界文件集 = `mobile/go.mod`、`mobile/go.sum`、`mobile/build.sh`、`mobile/README.md`、`mobile/toolchain_test.go`（5 个文件，全在一个嵌套 module 内）+ 本计划与台账。不触 `internal/**`、`cmd/**`、`web/**`、`codegraph/**`。圈得出。

### 11.4 类型标注

不适用（无边界型子系统接口，仅构建与文档）。真机清单见 §10。

### 11.5 接缝覆盖（双向）

- **测试 → 缝**：三支测试的入口分别是 `mobile/go.mod`、`mobile/build.sh`、`mobile/README.md`——即本卡全部交付产物本身，无入口落在缝外的内部锁。
- **缝 → 测试**：contract §4.4 条 29（go.mod tool）、§0 约束 2 / §8.6（`-androidapi 21` + 版本注释）、§8.9/spec 故事 8（轮换文档）逐条被对应测试锁住；§4.4 条 28（darwin 双端 bind PASS）由 contract §0 台账承载，机内无对应测试（已在 §10 标未验证）。
- **内部锁**：无。本卡无「从声明缝构造不出该断言」的例外，故无需退路声明。
- **退路同闸**：无步骤条件退路改变测试入口符号。

---

## 12. 自审三查

1. **spec/契约覆盖逐条能指到 task**：
   - contract §0 约束 1（go.mod tool 指令）→ T2；约束 2（Android `-androidapi 21`）→ T3；
   - contract §4.4 条 29 → T2；条 28（darwin PASS）→ §10 未验证 + contract §0 台账；
   - contract §8.1（go.mod tool + bind）→ T2；§8.6（构建脚本/文档）→ T3；§8.9（轮换文档）→ T4；
   - spec 用户故事 8（双端轮换）→ T4；spec 测试决定（工具链 gate）→ contract §0 台账（本卡不重跑，§10 记）；
   - 拆解 S3 ③ 四条验收 → T2/T3/T4/T5；P6=A → T4 的落点 `mobile/README.md`。
2. **占位符扫描**：无 TBD /「加适当的错误处理」/「同 Task N」/描述性占位。三件产物给出全文与逐字节终态；命令与预期输出均本节点实测。**内部锁声明**：无内部锁，见 §11.5。
3. **跨 task 类型/签名一致性**：本卡无跨卡 Go 签名（产物是构建配置与文档；`mobile/bind` 导出面归 S4）。三处 gomobile 版本字符串（`go.mod` / `build.sh` / `README.md`）在计划中已钉同一字面量 `v0.0.0-20260908204917-8b95e45f8d3e`。

---

## 13. 红线与不做

- **不改 `internal/mobilecore/**`**（S2 的面）；**不改 `mobile/bind/**`**（S4 的面）；**不产出壳工程**（P5=A）。
- **不改图**（`codegraph/` 冻结物）；产物全在 `mobile/` 图外 + 文档，无图 diff。
- **不跑全量测试**；**不在本工作树跑真实 `gomobile bind`**（linux 无工具链，跑了也只是假红）。
- **不发明命令**：轮换文档只引用现状存在的 `handoff sessions revoke` / `handoff service restart` / 编辑 `config.yaml`；不写不存在的 `token rotate` 子命令。
- **不重跑工具链实验**：以 contract §0 darwin 台账为据，如实标未验证。
