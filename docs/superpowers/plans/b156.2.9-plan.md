# B156.2.9 Plan：C9 纪律层文本修订（欠账 #10，非代码）

> 状态：charter 流 plan 节点产出稿（2026-08-26；同日补轮：增第四节「叙事文体」，上轮三节与锚一字未动）。
> 基线分支：`feature/b156-workbench`（尖端 b4231fb5，实测=本卡工作分支 cards/B156.2.9-charter 的起点；本地仅有远程跟踪引用 `origin/feature/b156-workbench`）。合并目标以此为准。
> 上游：spec `docs/superpowers/specs/2026-08-25-b156.2-collaboration-rooms-design.md`（§3.2/§3.4/§5/§6.3/§8.1）；契约 `docs/superpowers/specs/b156.2-contract.md`（§3.8 冻结面、§7 欠账 #10）；breakdown `docs/superpowers/specs/b156.2-breakdown.md`（C9 卡面、岔口七）。
> 范围裁决（协调者，岔口七方案 A）：交付物 = 仓内 `skills/handoff/SKILL.md` 三节文本修订 + 仓内落 discipline 具名资源新版本的种子文本与写入步骤文档。**账本运行期资源的实际版本写入不属于本卡**，归协调者合入后单独执行（向用户报备）。验收止于仓内一致性。补轮（2026-08-26）：协调者补第五来源 spec §5 文体段，SKILL.md 增第四节，其余裁决不变。
> 台账：`docs/superpowers/ledgers/ledger-b156.2.9-charter.md`（边干边追加，同批提交）。

---

## 0. 决策记录与范围边界

**D1 四来源→三节的映射**（补轮扩为五来源→四节，见 D6）。协调者给定四个内容来源（spec §3.2 六段、§3.4 收口四段、§6.3 重建四步、§8.1 executor 义务），SKILL.md 落三节：

| SKILL.md 新节 | 来源 | 理由 |
|---|---|---|
| 升级简报：六段契约（缺段拒收） | §3.2 | 独立主题 |
| 收口摘要：四段与偏差上报义务 | §3.4 + §8.1 | 同一安全网的两半：executor 收尾段如实上报（§8.1）↔ 协调者收口清单列全（§3.4「自主消化的不许消失」）——拆开则两侧都失去执法对象 |
| 换绑与重建四步 | §6.3 | 独立主题 |
| 叙事文体：同事协作叙事（三问齐全，正文不贴代码） | §5 :151-159 | 独立主题（补轮新增，来源由协调者补齐） |

**D2 具名资源只点 charter-implement，不扩围**。contract §3.8 与 §7#10 均为单数「executor 纪律块」；b229 P3 先例明示「只点名 charter-implement，不扩围」（`docs/superpowers/specs/b229-breakdown.md:15-16`）；填补级偏差的三个来源（实现选择/顺手小修/测试微调，spec §3.1 判据表）全部产生于实现轮。向其余六个角色块扩围属运行期行为变更，归协调者另裁，不在本卡。

**D3 种子形态 = 逐字片段 + 锚点 + 合并步骤，不是整块正文**。charter-implement 的权威正文在协调者侧账本具名资源里（B229 后形态），仓内无副本（实测：全仓 grep 无正文；本机 discipline 表为空表先例见 b229.7 台账 ：37）。自造一份「完整新正文」等于虚构输入（纪律红线：依赖输入缺失时提问/如实标注，不自造）。因此种子给出两个待插入片段的逐字正文、各自的插入锚、合并后的机械校验与 cmp 回读——每一个将进入运行期的字符都先在仓内受审。此为「无占位符」铁律下「缺失输入不自造」的正当出口，在此自我声明。

**D4 补充四（台账边界待办）折入本卡，不独立成卡**。评估：该待办要改的文本面（执行者纪律块红线「边干边落台账」条目）与本卡种子改的是同一具名资源、同一批次交付；独立成卡反而多一次运行期版本写入、多一张卡的协调成本。措辞按协调者给定边界照写：纪律块那条约束执行者的取证链（审阅者逐条复核，必须随分支走），用户长期指示约束协调者的过程记录（留 timeline），两层各管各的、互不替代。

**D5 版本号不写死**。运行期新版本号 = put 返回值（当前最大版本+1，`internal/ledger/disciplines.go:55-59`），仓内文档一律写「v(N+1)」，杜绝臆造版本号腐烂。

**D6 补轮：叙事文体入围（2026-08-26）**。上轮 Out-of-Scope 显式缺口由协调者补齐来源后纳入：第五来源 = spec §5 文体段（:158-159 原句照抄）+ 书写者规则语境（:151-154）。落点与理由：

- **落点**：SKILL.md 第四节 `### 叙事文体：同事协作叙事（三问齐全，正文不贴代码）`，仍挂在既有顶层节内、插在「换绑与重建四步」之后。
- **顶层 `##` 标题冻结不改**：AC-1 与 §2 基线证据均钉住原串，改标题等于作废上轮已校准判据；且叙事文体是横切规则——简报、收口、衔接消息都按它说话，不是与前三节并列的第四个主题。
- **执法齿为本卡补写**：spec 只给方向不给判据形态（同 T2 片段 A「漏报视同虚报」先例），齿 = 三问齐全／证据给链接正文不贴代码／叙事不是心跳，各配可数拒收话术（§3 第四节），审阅者能对任一条具体消息说出「不合规因为 X」。
- **不进 charter-implement 纪律块（D2 不变）**：叙事类消息书写者=房间成员中的协调者实体，「executor 与非成员不可写」（spec :151-152 实测原文）——executor 不写房间消息，无义务可下。
- **前置条件引语「本节三款」冻结**：上轮引语按「一字不动」保留原样，不改为四款；读法=前置条件约束整个顶层节（含第四节），字面差异由本条留痕，实现轮不得顺手改写。

### Out of Scope（显式列出，防静默带走/防扩大）

- **运行期 `handoff discipline put` 的实际执行**——归协调者（岔口七方案 A），由 Task 2 的写入步骤文档承接。
- 其余六个角色块（charter-plan/contract/review/breakdown/recon/integrate）的同类义务补充（D2）。
- ~~叙事文体的系统化条款~~（2026-08-26 补轮已纳入：协调者补 spec §5 来源，见 D6 与 T1 第四节；此条留痕，非静默带走。）
- SKILL.md 头部 frontmatter description 的触发词更新（非来源内容）。
- 任何 Go/web 代码面改动、任何图结构改动（非代码卡）。

---

## 1. Interfaces（Consumes / Produces）

执行者只看得到本 plan，邻居用什么名字全靠这张表。本卡无代码签名，接口即**精确字符串与路径**。

**Consumes**：

| 输入 | 精确出处 |
|---|---|
| 六段契约原文 | spec :81-91（§3.2 全部，含末段「对 spec 的 diff」语言规则） |
| 收口四段原文 | spec :103-105（§3.4 全部） |
| 重建四步原文 | spec :191-196（§6.3 全部） |
| executor 义务原文 | spec :262-263（§8.1：「执行中偏差如实记入 ledger 收尾段」「填补级偏差账的完备性来源」） |
| 文体段原文 | spec :158-159（§5 文体句全句，T1 第四节引语逐字照抄） |
| 书写者规则语境 | spec :151-154（§5：叙事类消息书写者=房间成员中的协调者实体；无协调者在场退化为机械信号；不伪造叙事） |
| 纪律层冻结面 | contract §3.8（:333-335）、欠账 #10（:464） |
| 台账边界两句 | 本 prompt 补充四（协调者给定边界原文） |
| 运行期 CLI 行为 | `cmd/discipline.go:65`（get 首行 `<name> v<N>`）、:79（put 读文件全文）、:88/:92（PutDiscipline、返回 `{"name":...,"version":N}`）；`internal/ledger/disciplines.go:43-67`（版本=max+1 只插不改）、:20（64KiB 上限）、:1-5（派发自动取最新版记 DispatchSnapshot） |

**Produces**（精确字符串，跨 task 一致性以此为准）：

| 产物 | 精确形态 |
|---|---|
| SKILL.md 新顶层节标题 | `## 协作房间纪律：升级简报、收口摘要与重建` |
| SKILL.md 三小节标题 | `### 升级简报：六段契约（缺段拒收）` / `### 收口摘要：四段与偏差上报义务` / `### 换绑与重建四步` |
| SKILL.md 第四小节标题（补轮） | `### 叙事文体：同事协作叙事（三问齐全，正文不贴代码）` |
| 执法齿标记字符串 | `三问齐全` / `叙事不是心跳` / `拒收话术` —— 在 SKILL.md 各 ≥1（AC-9） |
| 义务条款字符串 S1 | `执行中偏差如实记入 ledger 收尾段` —— 必须在 SKILL.md 与种子文件片段 A 中**逐字节一致地各出现 ≥1 次** |
| 种子文件 | `docs/superpowers/disciplines/b156.2-charter-implement-seed.md`（片段 A/B 逐字正文+锚点） |
| 写入步骤文档 | `docs/superpowers/disciplines/b156.2-discipline-write-steps.md`（八步操作序列） |
| 台账边界句 S2 | `两层各管各的，互不替代` —— 种子片段 B 含它，write-steps 校验命令引用它 |

---

## 2. 判据先在基线跑（已跑，证据嵌入）

plan 节点已在基线 b4231fb5 实跑以下判据，输出原文：

```
$ grep -c "^### 升级简报六段契约" skills/handoff/SKILL.md   → 0
$ grep -c "^### 收口摘要与偏差上报义务" skills/handoff/SKILL.md → 0
$ grep -c "^### 换绑与重建四步" skills/handoff/SKILL.md      → 0
$ for w in 升级简报 收口摘要 重建四步 执行中偏差如实记入 六段; do grep -c "$w" …; done
  → 0 0 0 0 0        （三节现状不存在，判据能红）
$ grep -n "^## 会话恢复：从零接管" skills/handoff/SKILL.md   → 530（唯一命中，插入锚）
$ grep -c "^## " skills/handoff/SKILL.md → 17 ;  "^### " → 17
$ ls docs/superpowers/disciplines… → No such file or directory（目标目录不存在，随 T2 新建）
```

保真锚（引文不改写的机械判据）：21 条单行安全锚逐一 `grep -cF <锚> docs/superpowers/specs/2026-08-25-b156.2-collaboration-rooms-design.md` 实测全部 =1：
`什么被推翻/要动什么，挂卡号`｜`证据用链接锚进 timeline`｜`为什么当时没预见`｜`动哪条判据/范围/契约`｜`只要可行必含「维持原商定」`｜`不答复卡停在什么状态、多久后再提醒`｜`简报的语言是「对 spec 的 diff」`｜`做了什么（一段话）`｜`这是安全网执法点`｜`验收证据指针`｜`残余去向`｜`父卡收口另带子卡汇总`｜`用户手动换绑`｜`当前绑定 CAS 校验`｜`（字段/附件/验收判据）`｜`读卡会话史`｜`会话史天生是交接简报`｜`读仓内文档`｜`凡承重的必须落账`｜`限额检测/自动拉起/自动换绑归三期`｜`执行中偏差如实记入 ledger 收尾段`

**判据更正记录**（判据先在基线跑的价值实证）：初选三条锚「自主消化的不许消失，这是安全网执法点」「换绑写入按当前绑定 CAS 校验」「填补级偏差账的完备性来源」grep 均=0——它们在 spec 里被粗体标记+手动换行劈开（:103-104/:191-192/:262-263），属跨行串，恒红假缺陷；已换成上表单行安全锚。**教训进判据：保真锚必须单行验证后才准入计划。**

补轮（2026-08-26）第四节判据，基线 b4231fb5 实跑：

```
$ grep -c "^### 叙事文体" skills/handoff/SKILL.md   → 0
$ for w in 叙事文体 同事协作叙事 正文不贴代码 三问齐全 拒收话术 叙事不是心跳; do grep -c "$w" …; done
  → 0 0 0 0 0 0        （第四节现状不存在，判据能红）
```

补轮新增保真锚 11 条（对 spec 单行 `grep -cF` 实测全部 =1，且对 plan §3 草文双测各 ≥1）：
`技术细节的家是卡 timeline`｜`语义现状不变`｜`timeline 是账，房间是播`｜`讲发生了什么/为什么/意味着什么`｜`凡引证据给链接，正文不贴代码`｜`executor 与非成员不可写`｜`退化为机械信号`｜`不伪造叙事`｜`没消息 = 按 spec 在走`｜`白名单的敌人是心跳与日志`｜`「开始了/进行中/跑测试了」`

合计 **32 条锚**（上轮 21 + 本轮 11）。锚更正又一次：「同事协作叙事」grep -F spec=0——它在 :158→159 被换行劈开，跨行串弃用、只进小节标题不当锚；「没消息 = 按 spec 在走」按 spec :156 原空格逐字照抄。

---

## 3. Task 1：SKILL.md 四节（最薄路径；上轮三节 + 补轮第四节）

**文件**：`skills/handoff/SKILL.md`（唯一触及文件）。
**最薄路径声明**：今天从声明面（SKILL.md）调判据得不到预期结果（§2 已证红，含补轮第四节红证），Task 1 即点亮行为的最薄可跑路径——直接落文本，无竖切可言（非代码卡）。

**插入位置（精确锚）**：「账本模式的红旗」表末行 `| 「卡的事件流里没有，那就是没发生」 | 镜像可能滞后。…… |` 之后、`## 会话恢复：从零接管`（:530）之前，空一行接新节。

**新增全文（逐字落下；粗体与编号为排版层，句子本体照抄 spec，连接句已标〈胶〉）**：

```markdown
## 协作房间纪律：升级简报、收口摘要与重建

> 前置条件：本节三款走 B156.2 协作房间层（房间、简报、收口摘要都是账本事件流，
> 契约见 `docs/superpowers/specs/b156.2-contract.md`）。该层未部署的机器上按原样
> 回退：请示用 `decision open`、过程记录用 `card note`——「回合末四分法落账」不变。
> 〈胶：回退形态取自本 skill 既有「回合末四分法落账」表，未引入新机制。〉

### 升级简报：六段契约（缺段拒收）

推翻级偏差停卡后发升级简报等答复。〈胶：触发面一句，详见 spec §3.1 判据表。〉
简报是「对 spec 的 diff」的载体契约，**缺段拒收，格式是契约不是模板建议**。六段：

1. **一句话**：什么被推翻/要动什么，挂卡号；
2. **商定 vs 实测**：spec 第几条写的什么 ↔ 实际发现什么，证据用链接锚进 timeline（测试原文、文件清单、轮次），正文不贴代码；
3. **根因**：为什么当时没预见（世界变了/当时信息不足/理解偏差）；
4. **影响半径**：动哪条判据/范围/契约，波及哪些卡；
5. **选项 + 后果 + 推荐**：2~3 个，只要可行必含「维持原商定」（防简报变成既成事实追认书）；
6. **停在哪**：不答复卡停在什么状态、多久后再提醒。

拒收判据（发出前自检，收到时执法）：六段任缺一段即退回补写，不得代答、不得跳过；
第 5 段没有「维持原商定」选项视同缺段；证据没有锚进 timeline（正文贴代码或口说
无凭）视同第 2 段缺失。〈胶：把 spec 标题里的「缺段拒收」写成可执行的拒收动作，
协调者指令要求措辞是判据不是建议。〉

简报的语言是「对 spec 的 diff」，不是对代码的 diff——spec 是用户注意力驻留过的
地方，这是把用户语境接回来的最短路径。

### 收口摘要：四段与偏差上报义务

卡到终态时，协调者发的最后一条消息是收口摘要，四段：

1. 做了什么（一段话）；
2. **全部偏差清单**：推翻级裁决结果 + 填补级逐条——自主消化的不许消失，这是安全网执法点；
3. 验收证据指针（timeline 锚）；
4. 残余去向（落 roadmap/新卡的逐条链接）。

父卡收口另带子卡汇总（几张顺利、几张有值得读的偏差、链接）。

执法点两条：

- **协调者侧**：第 2 段不是选填，缺「全部偏差清单」不得发收口；「没有偏差」也是
  一行明写的事实，不许留白。
- **执行者侧**：「执行中偏差如实记入 ledger 收尾段」——填补级偏差账的完备性来源
  （该义务经 charter-implement 纪律块下发，种子见
  `docs/superpowers/disciplines/b156.2-charter-implement-seed.md`）。执行者回合
  收尾必须如实列出本回合自主消化的偏差，协调者汇总进上面的清单；任何一侧漏列，
  安全网即破。

### 换绑与重建四步

限额、载体更换、接管：用户手动换绑（新载体可为另一 agent 的 CLI）。换绑写入按
当前绑定 CAS 校验，写入即撤销旧会话的房间写权与推进权（防旧会话醒来继续发消息
推卡）。新任协调者开局先做**重建四步**，一步不跳：

1. **读卡**（字段/附件/验收判据）：`handoff card show <id>`；
2. **读卡会话史**：房间页或 `handoff room read`——会话史天生是交接简报，第一读者
   是用户，第二读者是你；
3. **读 timeline**：`handoff card show <id>` 的事件流；
4. **读仓内文档**：spec / 契约 / plan 等附件与分支上的相关文档。

配套义务：**凡承重的必须落账**——跨 agent 换载体时旧 transcript 不可携带，账外无
不可再生之物。限额检测/自动拉起/自动换绑归三期。

### 叙事文体：同事协作叙事（三问齐全，正文不贴代码）

技术细节的家是卡 timeline，语义现状不变（timeline 是账，房间是播）。〈胶：spec §5 :158 原句上半〉
文体：同事协作叙事——讲发生了什么/为什么/意味着什么；凡引证据给链接，正文不贴代码。〈胶：spec :159 原句下半；「同事协作叙事」在 spec 里被换行劈开不能当保真锚，教训同六段那节〉

书写者规则：叙事类消息的书写者是房间成员中的协调者实体，executor 与非成员不可写；
无协调者在场时房间面退化为机械信号（工单上浮、needs-human、指针），不伪造叙事，
接管者唤醒后补写。〈胶：语境取自 spec §5 :151-154；书写权执法面在房间层代码（C 卡），本节落的是协调者纪律面。〉

文体三条，发出前自检、收到时执法：

1. **三问齐全**：每条叙事消息要答得了三个问题——发生了什么／为什么／意味着什么。
   只报状态不说原因＝缺「为什么」；说了变化不说影响＝缺「意味着什么」；缺任何一问
   即不合规，退回补写。〈胶：把「讲发生了什么/为什么/意味着什么」写成可数的三问，
   缺一问即可指认，形态对齐六段的缺段拒收。〉
2. **证据给链接，正文不贴代码**：证据一律给引用锚（git 路径 / timeline 锚 / 卡号 /
   附件），正文出现成块代码或日志原文即不合规——技术细节回卡 timeline 去，那里才是
   它的家。
3. **叙事不是心跳**：「开始了/进行中/跑测试了」这类进度播报不进房间——没消息 = 按 spec 在走；
   白名单的敌人是心跳与日志，不是对话。

拒收话术（审阅者拿它对任一条具体消息说出 X 才算执法）：「这条缺『为什么』」/
「这条正文贴了代码，证据没给链接」/「这是心跳消息，不在白名单」。
```

（落地时删掉全部 `〈胶：…〉` 标注——它们只是本 plan 的出处审计标记，不进 SKILL.md。注意形态：上轮三节的标注独占整行；补轮第四节的标注多为句尾内联——只删 `〈胶：…〉` 标注本体及其紧邻的空格，标注前的正文一律保留。）

**步骤（红绿套在文本存在性断言上，本卡的「缝断言」等价物就是这些 grep）**：

1. 【≤2min】复跑 §2 红证四条 `grep -c "^### …"`（含补轮的 `^### 叙事文体`；应仍全 0；不为 0 说明有人动过，停下对账）。
2. 【≤5min】按精确锚插入上方全文（删全部〈胶：…〉标注本体，含第四节内联形态；标注前正文保留）。
3. 【≤2min】绿证：AC-1～AC-4、AC-9 全绿（命令见 §5）。
4. 【≤3min】保真比对：32 条锚 + S1 在 SKILL.md `grep -cF` 各 ≥1（AC-4）。
5. 【≤2min】结构自查：`grep -c "^## "` 由 17 变 18；`^### ` 由 17 变 21（新增四小节）；新节行号 < 530+偏移且其后紧邻仍是「会话恢复」节；markdown 表格/列表无断裂（肉眼过一遍渲染源）。
6. 【≤1min】提交：`git add skills/handoff/SKILL.md && git commit -m "docs(b156.2.9): SKILL.md 增协作房间纪律四节——简报六段/收口与偏差上报/重建四步/叙事文体"`。

**测试范围声明（最小化）**：非代码卡——不跑 go test / vitest / tsc；全部机械验证 = 本节与 §5 列出的 grep/diff/wc/awk 命令。全量测试不属于本卡。
**日志/注释步骤声明**：无代码面故无日志点；注释义务由新节的前置条件引语与两份新文档的头注释履行（见 T2）。

---

## 4. Task 2：discipline 种子文本 + 写入步骤文档

**文件**（均为新建；目录 `docs/superpowers/disciplines/` 随本 task 创建）：

### 4.1 种子文件 `docs/superpowers/disciplines/b156.2-charter-implement-seed.md` 全文

````markdown
# charter-implement 纪律块·新版本种子（B156.2.9 / 欠账 #10）

> 职责：charter-implement 权威正文的**下一版增量**在这里逐字定稿；权威正文本身在
> 协调者侧账本具名资源（B229 后形态），仓内无副本，所以本文件不是可直接
> `discipline put` 的整块正文，而是「两个待插入片段 + 各自插入锚」。合并步骤、
> 机械校验、cmp 回读与回滚见同目录 `b156.2-discipline-write-steps.md`。
> 边界：片段正文一经合入运行期不得就地改写——要改措辞先改本文件并重走审核，
> 再发更新版本（不可变版本化）。

## 片段 A：收尾段偏差上报义务

来源：spec §8.1（`docs/superpowers/specs/2026-08-25-b156.2-collaboration-rooms-design.md`
:262-263 原句「executor 纪律块补『执行中偏差如实记入 ledger 收尾段』——填补级偏差
账的完备性来源」）；执法齿（漏报视同虚报）为本卡补写，与既有纪律文体（如「已验必须
带证据」）同强度。

插入锚：charter-implement 正文中含「收尾纪律」的那一条目的末尾（该条目结束、下一
空行或下一编号项之前）。

正文（逐字，含首尾）：

```text
此外，「执行中偏差如实记入 ledger 收尾段」：本回合自主消化的全部偏差——含填补级
（spec 未覆盖的实现选择、顺手小修、测试微调）——逐条如实列进最终报文，供协调者汇入
收口摘要的「全部偏差清单」；这是填补级偏差账的完备性来源，漏报视同虚报。
```

## 片段 B：台账边界澄清

来源：执行者纪律块红线「边干边落台账」（约束执行者取证链：仓内文件、随分支走、
与本节点产出物同批提交）与用户对协调者会话的长期指示（过程记录留 timeline，不写
一个又一个文件）两条规则分层不同、不冲突但边界未写明；同族先例：review 节点
「平台层要写台账 vs 角色层禁 commit」的跨层矛盾。

插入锚：正文中含「边干边落台账」的红线条目末尾。

正文（逐字，含首尾）：

```text
（边界澄清：本条约束的是执行者的取证链——台账是仓内文件，随分支走、与本节点产出物
同批提交，供审阅者逐条复核。协调者的过程记录落在卡 timeline 而非台账文件，那是另一
层的规矩；两层各管各的，互不替代。）
```
````

### 4.2 写入步骤文档 `docs/superpowers/disciplines/b156.2-discipline-write-steps.md` 全文

````markdown
# charter-implement 纪律块新版本写入步骤（B156.2.9 / 欠账 #10 运行期半边）

> **本步骤归协调者本人执行，不派发给任何 executor。**地点：协调机（权威账本在本机
> `~/.handoff`）。时机：B156.2.9 分支合入 feature/b156-workbench 之后、下一次派发
> implement 节点之前。这是对在跑系统的配置变更，动手前向用户报备一句（理由：代码与
> 数据分开回滚，岔口七方案 A）。
> 命令行为依据（源码出处，非记忆）：`cmd/discipline.go:65` get 打印 `<name> v<N>`
> 首行；`:79` put 读文件全文为正文；`:88/:92` PutDiscipline 返回
> `{"name":"...","version":N}`。`internal/ledger/disciplines.go:43-67` 版本=当前
> 最大+1、只插不改；`:20` 正文上限 64KiB；`:1-5` 派发自动取最新版并记进
> DispatchSnapshot。以上 CLI 行为本卡未真机实跑（执行器禁用 handoff CLI），以第 5 步
> cmp 回读兜底真伪。

## 步骤

1. 取当前正文并记版本：
       handoff discipline get charter-implement > /tmp/ci-current.txt
   `head -1` 应为 `charter-implement v<N>`，记下 N；剥离首行得纯正文：
       tail -n +2 /tmp/ci-current.txt > /tmp/ci-body.md

2. 合入两个片段（正文从仓内种子文件逐字复制，不得改写）：
   片段 A 追加到含「收尾纪律」条目的末尾；片段 B 追加到含「边干边落台账」的红线条目
   末尾。结果存 /tmp/ci-new.md。**任一锚点找不到（正文漂移）就停**：把
   `head -20 /tmp/ci-body.md` 贴给协调者裁决换锚，禁止即兴近似。

3. 机械校验（四条全过才准 put）：
       diff /tmp/ci-body.md /tmp/ci-new.md                          # 只许两处新增块，零删除零改写
       grep -c "执行中偏差如实记入 ledger 收尾段" /tmp/ci-new.md     # 期望 ≥1
       grep -c "两层各管各的，互不替代" /tmp/ci-new.md               # 期望 ≥1
       wc -c /tmp/ci-new.md                                         # 必须 < 65536（64KiB 上限）

4. 写入新版本（不可变版本化，旧版永不改写）：
       handoff discipline put charter-implement /tmp/ci-new.md
   记下返回 JSON 里的版本号（应为 N+1）。

5. 回读比对（roundtrip 逐字节，b229 integrate 同款）：
       handoff discipline get charter-implement | tail -n +2 | cmp - /tmp/ci-new.md && echo BYTE_IDENTICAL
   且首行版本为 v(N+1)。不一致即停，排查后再试。

6. 生效确认：无需重启——下一次 implement 节点派发自动取最新版并把版本号写进
   DispatchSnapshot（disciplines.go:1-5）；派发事件里 discipline_version 应为 N+1。

7. 回滚：真正的回滚是把旧正文再 put 一遍（cp /tmp/ci-body.md 为草稿后走同一校验
   流程），产生 v(N+2) 内容=旧文；应急单次派发可用 `--discipline-override <角色名>`
   临时换块（语义是换名字不是回版本）。旧版本行永远在库可审计。

8. 向用户报备一句话模板：「B156.2.9 的纪律文本已写入运行期：charter-implement
   v(N+1)（新增收尾偏差上报义务与台账边界澄清），代码在 feature/b156-workbench，
   数据变更可单独回滚。」
````

### 4.3 步骤

1. 【≤2min】建目录与两文件，全文照抄 §4.1/§4.2（去掉转义说明行）。
2. 【≤2min】一致性绿证：S1 在两文件各 ≥1（AC-5）；S2 在种子与 write-steps 各 ≥1（AC-6）。
3. 【≤2min】台账追加本 task 条目；提交：`git add docs/superpowers/disciplines docs/superpowers/ledgers/ledger-b156.2.9-charter.md && git commit -m "docs(b156.2.9): discipline 种子与运行期写入步骤——charter-implement 偏差上报义务+台账边界澄清"`。

---

## 5. 验收看板（每条可判 pass/fail）

| # | 判据（命令 → 期望） | 基线读数 |
|---|---|---|
| AC-1 | `grep -c "^## 协作房间纪律：升级简报、收口摘要与重建" skills/handoff/SKILL.md` → 1 | 0 |
| AC-2 | 四条 `^### ` 小节标题 grep -c 各 → 1 | 0,0,0,0 |
| AC-3 | `awk '/^## 协作房间纪律/{a=NR}/^## 会话恢复：从零接管/{b=NR}END{exit!(a&&b&&a<b)}' skills/handoff/SKILL.md` → exit 0；且 `grep -c "^## "` =18、`grep -c "^### "` =21（基线 17/17） | 17,17 |
| AC-4 | §2 的 32 条保真锚对 SKILL.md `grep -cF` 各 ≥1 | —（新面） |
| AC-5 | S1=`执行中偏差如实记入 ledger 收尾段` 在 SKILL.md 与种子文件 `grep -cF` 各 ≥1 | 0 |
| AC-6 | S2=`两层各管各的，互不替代` 在种子与 write-steps `grep -cF` 各 ≥1；write-steps 含 `BYTE_IDENTICAL`、`--discipline-override`、`65536` 字样各 ≥1 | —（新文件） |
| AC-7 | 改动面封闭：`git diff --name-only origin/feature/b156-workbench...HEAD` ⊆ {skills/handoff/SKILL.md, docs/superpowers/disciplines/*, docs/superpowers/ledgers/ledger-b156.2.9-charter.md} | — |
| AC-8 | 图检查（应急，仅在 AC-7 发现代码面被碰时跑）：`go run . graph check --repo . --view cards-B156.2-charter-4` → fails=0 warns=97；裸 check 报的 4 条 dead-contract/dead-interface 是 absorb 前固有形态，**不是本卡要修的** | 协调者实测口径 |
| AC-9 | 执法齿标记（补轮第四节）：`三问齐全` / `叙事不是心跳` / `拒收话术` 在 SKILL.md `grep -cF` 各 ≥1 | 0,0,0 |

---

## 6. 缺陷族对抗审查（逐族结论）

1. **生命周期/状态机中断**：无运行态可中断。变体风险＝skill sync 把超前四节推到功能未部署机器——防线＝前置条件引语明示回退形态（decision open/card note 不变）。孤儿资源：无（纯文档）。
2. **静默失败/误导报错**：最大口子＝运行期合并时锚点漂移导致协调者静默放弃——防线＝write-steps 第 2 步「找不到锚就停＋上报」；put 后 cmp 回读防「写了但没写对」；diff 只增不减防顺手改写历史条文。
3. **跨平台假设**：纯中文 markdown，无平台敏感物；正文远小于 64KiB 上限（wc -c 校验在案）。
4. **假红/假绿**：全部判据基线预跑存证（§2）；跨行串假锚事故已发生并修正（三条，记录在案）——最终 21 锚全部单行实测=1；补轮同型事故再抓一条（「同事协作叙事」被 :158→159 换行劈开，弃用），32 锚全部单行双测后才准入；变异建议：从 SKILL.md 删除任一小节 → 对应 AC-2 红；删第四节任一执法齿 → AC-9 红；改动片段 A 一字 → AC-5 的逐字节比对（write-steps 第 3 步 diff/grep）红。
5. **门禁绕过**：运行期写入若混进实现轮提交即绕开岔口七裁决——防线＝AC-7 改动面封闭＋write-steps 头部「归协调者，不派发」；graph check 应急闸兜底意外代码面（AC-8）。

## 7. 五项检查

1. **缺陷族对抗**：§6，逐族有结论。
2. **序列化边界**：唯一真实序列化链＝种子片段 → 协调者手工合并 → `put` 文件读入 → 库行 → `get` 回读。手写环节两处（合并编辑、`tail -n +2` 剥头），各配断言：diff 只增不减（第 3 步）、get 回读与源文件 `cmp` BYTE_IDENTICAL（第 5 步）——一条穿过真实变换链的回归闭环，满足「两端各自有检查≠链路有检查」。roundtrip 属性测试不适用（一次性数据操作，b229 P2 先例：不值得留长期命令面）。
3. **上下文预算**：T1 圈定 SKILL.md 单文件；T2 两份新文档＋台账。圈得出，无需竖切卡。
4. **类型标注**：本卡无边界型子系统 task。真机清单＝write-steps 八步（真机账本操作本就无法机内代跑），执行者/时机/机器已在文档头部钉死。
5. **接缝覆盖（双向）**：spec 测试接缝清单五缝全是代码缝；本卡零代码、零测试入口——**测试→缝**方向无测试入口可查（ vacuous ）；**缝→测试**方向五缝均不由本卡锁（各自归 C1–C8）。内部锁声明：本卡无任何测试函数，故「每个实现类 task 至少一条缝级断言」的正当出口在此声明——**从声明缝构造不出文本存在性与引文保真断言，因为 C9 不触任何缝**；等价锁定物＝§5 grep 判据组（文本面的缝即「节标题存在＋引文保真＋改动面封闭」）。退路检查：步骤正文无条件退路。

## 8. 自审三查

- **spec 覆盖**：故事1（深夜清账读简报/收口）→T1§1§2；故事5（换绑重建）→T1§3；§8.1 executor 义务→T1§2 引述＋T2 片段A；contract §3.8 五项→六段✓(T1)、叙事文体✓(T1§4，补轮落点)、收口✓(T1)、重建✓(T1)、executor 义务✓(T2)；欠账#10→T1+T2，运行期半→write-steps 归协调者；补充四台账边界→T2 片段B（D4）。
- **占位符扫描**：全文无 TBD／「适当的错误处理」／「同 Task N」／有描述无代码。T2 种子的「片段+锚点」形态系 D3 自我声明的正当出口（缺失输入是协调者侧运行期数据，纪律禁止自造；每个将入运行期的字符均已逐字落盘）。
- **跨 task 字符串一致性**：S1/S2 与节标题字符串在 T1/T2/验收命令三处逐字一致（§1 Produces 表为唯一权威源）。

## 9. 派发前自审

本 plan 的全部验收步骤可在工作树机内执行。唯一需要驱动运行期数据的动作（`handoff discipline put`）已整体移出实现范围、落在 write-steps 文档并标注「**归协调者本人执行，不派发**」——与执行者纪律「不派发、不调派发 CLI」无冲突。

## 10. 交棒声明

- 实现节点按 T1→T2 序贯执行；两 task 各自独立成 commit；台账同批提交。
- review 节点点名项：AC-4 保真逐条复跑（防引文漂移，含补轮 11 锚）；AC-9 执法齿在场（第四节可执法性）；AC-7 改动面封闭（防运行期写入混入）；write-steps 第 2/5 步的停机条件在场（防锚点漂移即兴与假绿回读）。
- finish 节点提醒：合入 feature/b156-workbench 后，write-steps 移交协调者执行并向用户报备（岔口七方案 A 尾巴）。
