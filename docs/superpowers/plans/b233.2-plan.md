# B233.2 实现计划：harness 能力归拢（执行、协调、一次性调用与隔离 HOME）

状态：执行计划
卡：B233.2
定级：L3 轻档（单轮 implement，禁止 `card split`）
法定产出：`docs/superpowers/plans/b233.2-plan.md`
事实台账：`docs/superpowers/ledgers/2026-09-07-b233.2-plan-ledger.md`

拍板结果（breakdown §0，提交 `0f1441d4`）必须按字面执行，不得把未选方案写成事实：

| 岔口 | 本计划落地 |
|---|---|
| **P1(a)** | 单卡内部 ticket T1–T7，不扇出子卡。 |
| **P2(a)** | 一个 Bundle：生产 `Adapter` 同时是 `Provider`，并按报告实现所支持的 `OneShot`/`Profile`/`Skills`/`Coordinator`。禁止两张表。fake 只保证 `execution=true`，其余能力 `ReportHas=false` 且不实现对应接口。 |
| **P3(a)** | `skill.Install` 仍写 `.handoff/skill` 基准副本，再对传入的每家 `Skills` 调 `Install`。路径表删除。部分成功语义留在 `Install`。不退役基准副本。CLI 保持薄。 |
| **P4(a)** | `Coordinator` 实现放 `internal/executor/opencode/coordinator.go`（包 `hostapi.Host`）。**不**放 `hostapi` 包——`driver.go` 包注释冻结「只 import 标准库」。`coordinatorRunner` 只做 `keysclient.Runner`→`Coordinator` 包装，并把 `context.Background()` 换成可取消 ctx。 |
| **P5(a)** | 第二家免审证明用 Claude：`WriteTaskEnv` 增加 `PolicySnapshot`；原生 allow 是可精确表达子集，禁止裸 `"Bash"`。AGY 仍走执行真机，不顶替本证明。 |
| **P6(a)** | 本卡写 `CHANGELOG.md` `[Unreleased]`。 |

上游：已批准 spec `docs/superpowers/specs/b233.2.md`；已冻结契约 `docs/superpowers/specs/b233.2-contract.md`（提交 `f571e323`，134 条 + §12 C-0…C-8）；已拍板拆解 `docs/superpowers/specs/b233.2-breakdown.md`。

读者：零上下文、品味存疑。按步骤复制代码。不要新立 `d_harness`、不要平行 `Executor` 接口、不要扩 `supportedCLIs` 冒充协调、不要把 grok `--effort low` 写回提供方默认、不要复制 session db / 整棵 HOME、不要 OpenCode 真机走付费模型、不要给 Claude/Grok/Codex/AGY 新造协调产品、不要 import `internal/permgate` 进各家 adapter、不要让 `keysclient` / `Manager` 再写 `.config/opencode` 或 `switch executor` 拼 argv。

真机 OpenCode 必须用免费模型（现状读数 `opencode-go/deepseek-v4-flash`）。Codex 限额期失败不得写成「不支持执行会话」。真机清单归协调者，机内不得写成绿。

---

## 0. 硬边界（写进步骤，不是散文）

- 生产入口保持：`Manager.Dispatch`、`keysclient.Runner`、`Approver.Decide`、`handoff skill install`。不新造用户命令、不新造 EventType / Ticket.Kind。
- 执行会话能力面 = 既有 `Adapter` 五动作。恢复 = 可选 `Recoverer`（类型断言）。Ask = `AskResponder`（本卡不重开）。
- Bundle：`map[string]executor.Adapter` 仍是组装表。生产 Adapter 值同时满足 `Provider` 以及报告为 true 的能力接口。`NewManager` 从 ads 推导 `*executor.Registry`，禁止第二张名单。
- `hostapi.Host.RunTurn` 不是消费方面。`keysclient.Runner` 零签名增量；包装在 `coordinatorRunner`。
- `d_execution → d_policy` 禁止。adapter 不得引用 `agentd.Approver` 具体类型。
- `d_keystone → d_execution` 不得在 T1 写入（dead-contract）。T3 包装落地**同提交**声明 `interfaces: ["Coordinator"]`、`legacyBudget: 0`。
- `d_orchestration → d_execution` 预算 27、基线实数 **26**。本卡消费既有边（Dispatch/Decide 已 import executor）。若 `codegraph check` 因新 call 超预算：把 `legacyBudget` 提到实际边数并在 `legacyBudgetNote` 列出新增符号。禁止造零边节点凑数。
- 日志用 `slog`（`*slog.Logger` 或 `slog.Default()`），禁 `fmt.Print*`。harness 名、能力名、HOME 是否隔离、校验结果可 Info；凭据和规则全文不得默认 Info。
- 真机项标「未验证，需真机」，机内不得写成绿。
- OpenCode 真机模型：`opencode-go/deepseek-v4-flash`。不可用则该项失败并贴原文，禁止改走会员通道。

---

## 1. 基线证据、图覆盖与最薄路径

### 1.1 本节点已实跑（HEAD `0f1441d4`，未改实现）

| 范围 | 命令 | 实跑结果 |
|---|---|---|
| Ticket 0 金样本 | `go test ./internal/executor ./internal/executor/grok ./internal/skill -run 'TestBaselineReportMatrix\|TestHarnessNameLiterals\|TestUnsupportedError\|TestOneShotArgv\|TestOneShotArgsStillEncodesGrokLowUntilMigrated\|TestAgentDirsContract\|TestRecovererIsNotAdapterMethodSet\|TestGuardRemove\|TestZeroProfileReport\|TestCoordTypesMirror\|TestRequireLaunchPrompt' -count=1` | 三包 ok |
| executor 包 | `go test ./internal/executor -count=1` | ok 0.209s |
| agentd 触及 | `go test ./internal/agentd -run 'TestCoordinatorHomeSupplyOnLaunchAndResume\|TestOneShot\|TestApproverDecide' -count=1` | ok 0.969s |
| 组装表 | `go test ./cmd -run 'TestAdapterRegistryHasAlwaysAvailableExecutors\|TestAdaptersForRegistersClaudeOnAllPlatforms' -count=1` | ok 0.571s |
| 全仓编译 | `go build -o /dev/null .` | 退出 0，stdout 空 |
| 图闸 | `codegraph --repo . --view cards-B233.2-charter check` | 2 fail：`ApprovalClient` dead-contract / dead-interface（只在 233.1 视图）。warns=145。本卡不修这两条。`d_orchestration→d_execution` 26/27 |

今日绿、实现后必须改锁的正向：

- `TestOneShotArgs` / `TestOneShotArgsStillEncodesGrokLowUntilMigrated`：grok 默认 `--effort low`。T4 退役 `OneShotArgs` 时删这两支或改锁 Invoke。
- `TestAgentDirsContract`：锁 `agentDirs` 变量。T6 删表后必须迁到各家 `SkillsRelDir` 金样本。
- `coordinatorRunner.Launch` 走 `context.Background()`：今日协调取消测试若只断言 hostapi 拒绝 claude，会假绿。T3 必须锁 `errors.Is(ErrCapabilityUnsupported)` 且零次 `RunTurn`。

### 1.2 图与源码核对

查图：`codegraph --repo . --view cards-B233.2-charter <子命令>`。

符号锚（`resolve --doc` 认这种写法）：

`internal/executor/capability.go#Provider`、`internal/executor/capability.go#BaselineReport`、`internal/executor/capability.go#NewRegistry`、`internal/executor/executor.go#Adapter`、`internal/executor/recoverer.go#Recoverer`、`internal/executor/coordinator.go#Coordinator`、`internal/executor/oneshot_cap.go#OneShot`、`internal/executor/oneshot.go#OneShotArgs`、`internal/executor/profile.go#Profile`、`internal/executor/skills_cap.go#Skills`、`internal/executor/grok/oneshot_argv.go#OneShotArgv`、`internal/executor/opencode/taskenv.go#WriteTaskEnv`、`internal/executor/claudecode/taskenv.go#WriteTaskEnv`、`internal/agentd/manager.go#Manager.Dispatch`、`internal/agentd/manager.go#Manager.adapterFor`、`internal/agentd/manager.go#restorer`、`internal/agentd/approver.go#Approver.Decide`、`internal/agentd/approver.go#Approver.defaultRunCmd`、`internal/agentd/server.go#coordinatorRunner.Launch`、`internal/agentd/coordinator_home.go`（`copyCoordinatorRules` 短名不在图中）、`internal/hostapi/hostapi.go#Host.RunTurn`、`internal/hostapi/driver.go#supportedCLIs`、`internal/keysclient/keysclient.go#Runner`、`internal/skill/install.go#Install`、`cmd/agentd.go#adaptersForWithProbe`、`cmd/skill.go`（`skillInstallCmd.RunE`）。

库行为（禁止凭记忆，出处已在契约 §3；本节点复核源码注释与 `driveTurn`）：

1. `exec.CommandContext` ctx done → 默认 `Process.Kill`（契约 §3）。OneShot.Invoke 走这条。不保证杀进程组。
2. 协调回合超时走 `watchDeadline` → `killGroup`（unix 进程组）。与 OneShot **不是**同一条杀树。本卡不统一。
3. `os.Remove` unix = unlink，不跟随符号链接。skill 迁移老软链依赖此语义。
4. `json.Unmarshal` 默认忽略未知字段。`hostapi.consumeEvents` 依赖它。本卡不改。
5. `CommandContext(nil, …)` panic。Invoke/Launch 的 ctx 不得传 nil。

### 1.3 最薄路径

本卡要锁的行为今天从声明缝调用**还得不到**：`BaselineReport` 无生产调用方；`Dispatch` 不 `Require(execution)`；`Decide` 仍 `OneShotArgs`；`Launch` 仍 `Background` + 错误形状不是 `ErrCapabilityUnsupported`；`copyCoordinatorRules` 仍写死 OpenCode 路径；`skill.Install` 仍持 `agentDirs`。Ticket 0 空壳不是活路径。

DAG 第一条 = T1。T2–T6 文件集按包大致不相交，必须在 T1 的 `Provider.Report` 之后落地（否则各票自造名单）。T7 必须看到实现而不是空壳。

```
Ticket 0（已冻结 f571e323）
   │
   ▼
  T1 五家 Provider.Report + Registry 从 ads 推导（Bundle）
   │
   ├─→ T2 执行：Require(execution) + restorer→Recoverer + Claude 免审 ⊆ 快照
   ├─→ T3 协调：OpenCode Coordinator + Runner 包装 + 其余不支持
   ├─→ T4 OneShot.Invoke + Approver 迁入 + grok effort 在调用方
   ├─→ T5 Profile HOME + 退役 copyCoordinatorRules
   └─→ T6 Skills 路径表迁出 + Install 循环 Registry
            │
            ▼
           T7 退役 grep、接缝回归、CHANGELOG、变异、真机清单（真机=协调者）
```

---

## 2. 跨 task 冻结接口（逐字，邻居只看得见这个）

**Produces（T1，T2–T7 Consumes 必须逐字符相同）：**

```go
// 各生产 Adapter（opencode/claudecode/grok/codex/agy）
func (a *Adapter) Name() string
func (a *Adapter) Report() executor.CapabilityReport
var _ executor.Provider = (*Adapter)(nil)

// fake
func (f *Fake) Name() string // "fake"
func (f *Fake) Report() executor.CapabilityReport // 仅 execution=true

// internal/agentd/manager.go —— NewManager 签名一字不改，内部推导 Registry
func registryFromAds(ads map[string]executor.Adapter) *executor.Registry
```

**Produces（T2）：**

```go
// Manager 生产代码不再声明私有 restorer
r, ok := ad.(executor.Recoverer)

// internal/executor/claudecode/taskenv.go
func WriteTaskEnv(taskDir, taskID, planContent, sockPath, handoffBin, disciplineBlock string, snap executor.PolicySnapshot) (settingsPath, mcpPath, promptText string, err error)
```

**Produces（T3）：**

```go
// internal/executor/opencode/coordinator.go
type Coordinator struct { Host *hostapi.Host; log *slog.Logger }
func NewCoordinator(h *hostapi.Host, log *slog.Logger) *Coordinator
func (c *Coordinator) Launch(ctx context.Context, spec executor.CoordSessionSpec, prompt string) (executor.CoordTurnResult, error)
func (c *Coordinator) Resume(ctx context.Context, ref executor.CoordSessionRef, prompt string) (executor.CoordTurnResult, error)
func (c *Coordinator) CancelTurn(ctx context.Context, ref executor.CoordSessionRef) error
var _ executor.Coordinator = (*Coordinator)(nil)

// internal/agentd/server.go
type coordinatorRunner struct {
    coord       executor.Coordinator
    reg         *executor.Registry
    prepareHome func(keysclient.SessionSpec) (string, error)
}
func (r coordinatorRunner) Launch(spec keysclient.SessionSpec, prompt string) (keysclient.TurnResult, error)
func (r coordinatorRunner) Resume(ref keysclient.SessionRef, prompt string) (keysclient.TurnResult, error)
```

**Produces（T4）：**

```go
// 各家
func OneShotArgv(model, prompt string, limits executor.OneShotLimits) ([]string, error) // grok 已有；其余家本票新增
func NewOneShot(log *slog.Logger) *OneShot
func (s *OneShot) Invoke(ctx context.Context, req executor.OneShotReq) (executor.OneShotReply, error)
func (a *Adapter) Invoke(ctx context.Context, req executor.OneShotReq) (executor.OneShotReply, error) // 委托 NewOneShot(a.log)

// internal/executor/oneshot_run.go
func RunOneShotProcess(ctx context.Context, argv []string, env []string, workdir, homeDir string) (string, error)

// internal/agentd/approver.go
func (a *Approver) BindOneShot(shot executor.OneShot)
func (a *Approver) Decide(ctx context.Context, permission, taskSummary string) ApproverDecision
// Decide 零次调用 OneShotArgs
```

**Produces（T5）：**

```go
func (a *Adapter) Inspect(ctx context.Context, req executor.ProfileReq) (executor.ProfileReport, error)
func (a *Adapter) Prepare(ctx context.Context, req executor.ProfileReq) (executor.ProfileReport, error)
func (a *Adapter) Verify(ctx context.Context, req executor.ProfileReq) (executor.ProfileReport, error)
```

**Produces（T6）：**

```go
const SkillsRelDir = "<各家相对路径金样本>" // 见冻结 124
func (a *Adapter) Inspect(ctx context.Context, home string) ([]executor.SkillSite, error)
func (a *Adapter) Install(ctx context.Context, req executor.SkillInstallReq) ([]executor.SkillSite, error)
func (a *Adapter) Remove(ctx context.Context, req executor.SkillRemoveReq) error

// internal/skill/install.go
func Install(content, home string, providers []executor.Skills) ([]Site, error)
func Status(content, home string, providers []executor.Skills) ([]Site, error)
```

**Consumes（各 task 对既有缝）** 不得改：

```go
type Adapter interface {
    Start(ctx context.Context, req StartReq) error
    Events(taskID string) <-chan AdapterEvent
    Send(ctx context.Context, taskID, text string) error
    RespondPermission(ctx context.Context, taskID, permID, decision, reason string) error
    Stop(taskID string) error
}
type Runner interface {
    Launch(spec SessionSpec, prompt string) (TurnResult, error)
    Resume(ref SessionRef, prompt string) (TurnResult, error)
}
func (h *Host) RunTurn(ctx context.Context, req TurnRequest) (TurnReply, error)
func (c *Client) Dispatch(ctx context.Context, opts DispatchOpts) (*proto.Task, error)
```

---

## 3. Task 1：五家 Provider.Report 接线 / Registry 从 ads 推导

### 精确文件集

- `internal/executor/opencode/report.go`（新建）
- `internal/executor/claudecode/report.go`（新建）
- `internal/executor/grok/report.go`（新建）
- `internal/executor/codex/report.go`（新建）
- `internal/executor/agy/report.go`（新建）
- `internal/executor/fake/report.go`（新建）
- `internal/agentd/manager.go`（`NewManager` 内推导 `reg`；新增 `registryFromAds` / `RegistryFromAds`）
- `internal/agentd/server.go`（`providers *executor.Registry` + `SetProviders`）
- `cmd/agentd.go`（`ads` 之后 `srv.SetProviders(agentd.RegistryFromAds(ads))`）
- `cmd/agentd_test.go`（生产 Report 对账）
- `internal/executor/capability_test.go`（PATH ≠ 五类全支持）

不得改 `hostapi.ProbeHome`。不得改 `BaselineReport` 矩阵。

### Interfaces

Consumes：`executor.Provider`、`BaselineReport`、`NewRegistry`、`adaptersForWithProbe`。
Produces：五家生产 `Report()` 与 `BaselineReport` 逐字段一致；fake 仅 execution；`Manager.reg` 来自 ads，不是第二张表。

### 基线判据先跑

```bash
go test ./internal/executor -run 'TestBaselineReportMatrix|TestHarnessNameLiterals|TestUnsupportedError|TestRegistryRequireCoordination|TestGrokBaselineOneShotHasNoDefaultEffort' -count=1
go test ./cmd -run 'TestAdapterRegistryHasAlwaysAvailableExecutors|TestAdaptersFor' -count=1
```

今日预期：全绿。实现后这些金样本必须仍绿。

测试范围：`go test ./internal/executor ./internal/executor/opencode ./internal/executor/claudecode ./internal/executor/grok ./internal/executor/codex ./internal/executor/agy ./internal/executor/fake ./cmd -count=1`（本 task 触及包）。

### 步骤

1. **写失败测试（缝：`Provider.Report` / `adaptersForWithProbe`）**

在 `cmd/agentd_test.go` 追加（包 `cmd`，照抄同文件已有 `adaptersForWithProbe` 调用）。import 增加 `"errors"` 与 `"github.com/Xsxdot/handoff/internal/executor"`（该文件已有 `strings`/`io`/`slog`/`testing`）：

```go
func TestProductionAdapterReportsMatchBaseline(t *testing.T) {
	log := slog.New(slog.NewTextHandler(io.Discard, nil))
	ads := adaptersForWithProbe("darwin", log, t.TempDir())
	for _, name := range []string{"opencode", "claude", "codex", "agy"} {
		ad, ok := ads[name]
		if !ok {
			t.Fatalf("缺少 %s", name)
		}
		p, ok := ad.(executor.Provider)
		if !ok {
			t.Fatalf("%s 未实现 Provider", name)
		}
		got := p.Report()
		want, err := executor.BaselineReport(name)
		if err != nil {
			t.Fatal(err)
		}
		if got.Harness != want.Harness {
			t.Fatalf("%s harness = %q", name, got.Harness)
		}
		if len(got.Caps) != 5 {
			t.Fatalf("%s caps = %d", name, len(got.Caps))
		}
		for _, cap := range executor.AllCapabilityNames() {
			if executor.ReportHas(got, cap) != executor.ReportHas(want, cap) {
				t.Fatalf("%s %s = %v want %v", name, cap, executor.ReportHas(got, cap), executor.ReportHas(want, cap))
			}
		}
		if name == executor.HarnessCodex {
			var oneshot executor.CapabilityDecl
			for _, c := range got.Caps {
				if c.Name == executor.CapOneShot {
					oneshot = c
				}
			}
			if !containsStr(oneshot.NativeLimits, executor.NativeLimitSandboxReadOnly) ||
				!containsStr(oneshot.NativeLimits, executor.NativeLimitInvokeExec) {
				t.Fatalf("codex oneshot limits = %#v", oneshot.NativeLimits)
			}
		}
		if name == executor.HarnessGrok {
			for _, c := range got.Caps {
				if c.Name != executor.CapOneShot {
					continue
				}
				for _, lim := range c.NativeLimits {
					if lim == "effort=low" || lim == executor.EffortLow {
						t.Fatalf("grok oneshot 不得把 effort=low 编进 NativeLimits: %#v", c.NativeLimits)
					}
				}
			}
		}
	}
	if grokAd, ok := ads["grok"]; ok {
		p := grokAd.(executor.Provider)
		if executor.ReportHas(p.Report(), executor.CapCoordination) {
			t.Fatal("grok 不得声明 coordination")
		}
	}
}

func TestFakeReportIsExecutionOnly(t *testing.T) {
	log := slog.New(slog.NewTextHandler(io.Discard, nil))
	ads := adaptersForWithProbe("darwin", log, t.TempDir())
	p, ok := ads["fake"].(executor.Provider)
	if !ok {
		t.Fatal("fake 必须实现 Provider，否则 Registry 漏登记")
	}
	rep := p.Report()
	if rep.Harness != "fake" {
		t.Fatalf("fake harness = %q", rep.Harness)
	}
	if !executor.ReportHas(rep, executor.CapExecution) {
		t.Fatal("fake 必须声明 execution")
	}
	for _, cap := range []executor.CapabilityName{
		executor.CapCoordination, executor.CapOneShot, executor.CapProfile, executor.CapSkills,
	} {
		if executor.ReportHas(rep, cap) {
			t.Fatalf("fake 不得冒充 %s", cap)
		}
	}
}

func TestRegistryGetUnknownListsSupportedHarnesses(t *testing.T) {
	log := slog.New(slog.NewTextHandler(io.Discard, nil))
	ads := adaptersForWithProbe("darwin", log, t.TempDir())
	reg := executor.NewRegistry(providersFromAds(ads)...)
	_, err := reg.Get("gemini")
	if !errors.Is(err, executor.ErrUnknownHarness) {
		t.Fatalf("err = %v", err)
	}
	msg := err.Error()
	for _, name := range []string{"opencode", "claude", "grok", "agy", "codex"} {
		if !strings.Contains(msg, name) {
			t.Fatalf("未知 harness 错误必须列出 %s: %q", name, msg)
		}
	}
}

func TestRequireClaudeCoordinationIsUnsupported(t *testing.T) {
	log := slog.New(slog.NewTextHandler(io.Discard, nil))
	ads := adaptersForWithProbe("darwin", log, t.TempDir())
	reg := executor.NewRegistry(providersFromAds(ads)...)
	err := reg.Require(executor.HarnessClaude, executor.CapCoordination)
	if !errors.Is(err, executor.ErrCapabilityUnsupported) {
		t.Fatalf("err = %v", err)
	}
	want := "harness 不支持该能力: claude 不支持 coordination（禁止静默兜底到另一家）"
	if err.Error() != want {
		t.Fatalf("error = %q want %q", err.Error(), want)
	}
}

func providersFromAds(ads map[string]executor.Adapter) []executor.Provider {
	out := make([]executor.Provider, 0, len(ads))
	for _, ad := range ads {
		if p, ok := ad.(executor.Provider); ok {
			out = append(out, p)
		}
	}
	return out
}

func containsStr(ss []string, w string) bool {
	for _, s := range ss {
		if s == w {
			return true
		}
	}
	return false
}
```

在 `internal/executor/capability_test.go` 追加（反面：PATH 里有二进制 ≠ 五类全支持）：

```go
func TestCLIOnPathIsNotFiveCapabilities(t *testing.T) {
	// 缝：RequireCapability / ReportHas。构造「名字叫 claude、PATH 可忽略、报告仍不支持 coordination」。
	rep, err := executor.BaselineReport(executor.HarnessClaude)
	if err != nil {
		t.Fatal(err)
	}
	if executor.ReportHas(rep, executor.CapCoordination) {
		t.Fatal("冻结 46：CLI 在 PATH 不得被解释成 coordination=true")
	}
	if err := executor.RequireCapability(rep, executor.CapCoordination); !errors.Is(err, executor.ErrCapabilityUnsupported) {
		t.Fatalf("err = %v", err)
	}
}
```

2. **跑红**

```bash
go test ./cmd -run 'TestProductionAdapterReportsMatchBaseline|TestFakeReportIsExecutionOnly|TestRegistryGetUnknownListsSupportedHarnesses|TestRequireClaudeCoordinationIsUnsupported' -count=1
```

预期：`Adapter` 未实现 `Provider`（类型断言失败）或编译红。确认不是拼写错。

3. **最小实现**

每家生产包新建 `report.go`（以 OpenCode 为例，其余四家只改常量名）：

```go
package opencode

import "github.com/Xsxdot/handoff/internal/executor"

var _ executor.Provider = (*Adapter)(nil)

// Name 返回登记名。必须与 cmd/agentd.go 组装表键相同。
func (a *Adapter) Name() string { return executor.HarnessOpenCode }

// Report 返回本家能力矩阵。查询面与 BaselineReport 对齐；「支持」不等于已接线。
func (a *Adapter) Report() executor.CapabilityReport {
	rep, err := executor.BaselineReport(executor.HarnessOpenCode)
	if err != nil {
		return executor.CapabilityReport{Harness: executor.HarnessOpenCode}
	}
	return rep
}
```

claude / grok / codex / agy 把 `HarnessOpenCode` 换成对应常量。

`internal/executor/fake/report.go`：

```go
package fake

import "github.com/Xsxdot/handoff/internal/executor"

var _ executor.Provider = (*Fake)(nil)

func (f *Fake) Name() string { return "fake" }

func (f *Fake) Report() executor.CapabilityReport {
	return executor.CapabilityReport{
		Harness: "fake",
		Caps: []executor.CapabilityDecl{
			{Name: executor.CapExecution, Supported: true},
			{Name: executor.CapCoordination, Supported: false},
			{Name: executor.CapOneShot, Supported: false},
			{Name: executor.CapProfile, Supported: false},
			{Name: executor.CapSkills, Supported: false},
		},
	}
}
```

`internal/agentd/manager.go`：在 `Manager` 结构体 ads 旁增加：

```go
	reg *executor.Registry
```

把 `NewManager` 里现有的 `return &Manager{ ... }`（`manager.go:312-326`）整块替换为：

```go
	m := &Manager{
		st: st, hub: hub, ads: ads, cfg: cfg,
		appendEvent:  st.AppendEvent,
		conf:         func() *config.Config { return cfg },
		approver:     approver, gate: gate, log: log,
		env:          env,
		apInflight:   map[string]bool{},
		apFails:      map[string]int{},
		apDisabled:   map[string]bool{},
		denyGuidance: map[string]string{},
		aaCount:      map[string]int{},
		stopping:     map[string]struct{}{},
		sweepOwned:   map[string]struct{}{},
		reg:          registryFromAds(ads),
	}
	if m.log != nil {
		names := make([]string, 0, len(ads))
		for n := range ads {
			names = append(names, n)
		}
		m.log.Info("能力 Registry 已从 ads 推导", "n", len(ads), "registered", names)
	}
	return m
}

func RegistryFromAds(ads map[string]executor.Adapter) *executor.Registry {
	return registryFromAds(ads)
}

func registryFromAds(ads map[string]executor.Adapter) *executor.Registry {
	ps := make([]executor.Provider, 0, len(ads))
	for _, ad := range ads {
		if p, ok := ad.(executor.Provider); ok {
			ps = append(ps, p)
		}
	}
	return executor.NewRegistry(ps...)
}
```

**不要**改 `NewManager` 参数表。`sweepOwned` 若源码已有更多字段，保留那些字段，只追加 `reg`。

`internal/agentd/server.go` 的 `Server` 结构体增加 `providers *executor.Registry`，并：

```go
func (s *Server) SetProviders(r *executor.Registry) { s.providers = r }
```

`cmd/agentd.go` 在 `ads := defaultAdapters(logger)` 成功、`NewManager` 之前：

```go
		srv.SetProviders(agentd.RegistryFromAds(ads))
```

4. **跑绿**

```bash
go test ./cmd -run 'TestProductionAdapterReportsMatchBaseline|TestFakeReportIsExecutionOnly|TestRegistryGetUnknownListsSupportedHarnesses|TestRequireClaudeCoordinationIsUnsupported' -count=1
go test ./internal/executor -run 'TestBaselineReportMatrix|TestCLIOnPathIsNotFiveCapabilities' -count=1
go test ./internal/agentd ./cmd -count=1
```

5. **加关键节点日志**

- `NewManager`：登记完成（已写）。
- 各家 `Report()` 是纯数据，不打 Info（避免热路径刷屏）。错误分支：`BaselineReport` 对已知名不会错；fallback 空报告时 Adapter 构造处不静默——`Report` 若走到 fallback，打 `log.Error("BaselineReport 失败，返回空能力报告", "harness", name)`。生产 `BaselineReport` 对五家不会错，这条是防御。

OpenCode `Report` 改成（其余家同）：

```go
func (a *Adapter) Report() executor.CapabilityReport {
	rep, err := executor.BaselineReport(executor.HarnessOpenCode)
	if err != nil {
		if a != nil && a.log != nil {
			a.log.Error("BaselineReport 失败，返回空能力报告", "harness", executor.HarnessOpenCode, "cause", err)
		}
		return executor.CapabilityReport{Harness: executor.HarnessOpenCode}
	}
	return rep
}
```

6. **加注释**

每个新建 `report.go` 文件头：

```go
// report.go —— 本家 Provider 能力报告（B233.2 Bundle）。
//
// 职责：让生产 Adapter 可查询五类能力；矩阵与 BaselineReport 对齐。
// 边界：无 I/O；报告「支持」不是生产入口已挂上。
```

`registryFromAds` 文档注释：参数是 ads 全表；只收实现了 `Provider` 的值；后写入覆盖同名（`NewRegistry` 已有此语义）。

### 验收

- 上列测试绿。
- `grep -n "type restorer"` 仍在（T2 才删）。
- 缺陷族：报告纯数据无进程；未声明视为不支持；不得只断言 BaselineReport。

---

## 4. Task 2：执行会话 Require + Recoverer + Claude 免审 ⊆ 快照

### 精确文件集

- `internal/agentd/manager.go`（`Dispatch`/`resolveExecutor` 后 `Require(execution)`；两处 `ad.(restorer)` 改 `executor.Recoverer`；删除私有 `restorer`；Claude 也注入 Approval）
- `internal/executor/claudecode/taskenv.go`（`WriteTaskEnv` 增 `snap`）
- `internal/executor/claudecode/adapter.go`（Start 把 snapshot 传进 WriteTaskEnv）
- `internal/executor/claudecode/taskenv_test.go`
- `internal/agentd` 里所有 `WriteTaskEnv(` 调用方（仅 claude 包内）

不得改 `ApprovalClient` 签名。不得重开 OpenCode Ask/审批。P5 未选 AGY 本票只保证不无故变坏。

### Interfaces

Consumes：T1 `Manager.reg`、`executor.Recoverer`、`executor.PolicySnapshot`、`StartReq.Approval`。
Produces：见 §2 T2。

### 基线判据先跑

```bash
go test ./internal/executor/claudecode -run 'TestWriteTaskEnv' -count=1
go test ./internal/agentd -run 'TestDispatchUnknown|TestResume' -count=1
```

今日：`WriteTaskEnv` 6 参数，无 snap。`manager.go` 两处 `ad.(restorer)`（约 1413、3698 行）。

测试范围：`go test ./internal/agentd ./internal/executor/claudecode ./internal/executor -count=1`。

### 步骤

1. **写失败测试（缝：`Manager.Dispatch` / `claudecode.WriteTaskEnv` / `Manager.ResumeTask`）**

`internal/executor/claudecode/taskenv_test.go` 追加：

```go
func TestWriteTaskEnvEmptyWorkdirWithSnapshotFails(t *testing.T) {
	dir := t.TempDir()
	snap := executor.PolicySnapshot{Version: "v1", Scope: executor.ApprovalScope{Workdir: ""}}
	_, _, _, err := WriteTaskEnv(dir, "T-1", "计划", "/tmp/p.sock", "/bin/handoff", "", snap)
	if err == nil {
		t.Fatal("空 Workdir 的快照必须失败，禁止写盘")
	}
}

func TestWriteTaskEnvSnapshotAllowIsSubsetAndNotNakedBash(t *testing.T) {
	dir := t.TempDir()
	snap := executor.PolicySnapshot{
		Version: "v1",
		Scope:   executor.ApprovalScope{Workdir: dir},
	}
	settingsPath, _, _, err := WriteTaskEnv(dir, "T-1", "计划", "/tmp/p.sock", "/bin/handoff", "", snap)
	if err != nil {
		t.Fatal(err)
	}
	var settings struct {
		Permissions struct {
			Allow []string `json:"allow"`
		} `json:"permissions"`
	}
	readJSON(t, settingsPath, &settings)
	if contains(settings.Permissions.Allow, "Bash") {
		t.Fatalf("快照下 allow 不得含裸 Bash（无法精确表达无限制 bash ⊆ 快照）: %v", settings.Permissions.Allow)
	}
	allowed := map[string]struct{}{}
	for _, r := range allowRules {
		allowed[r] = struct{}{}
	}
	for _, got := range settings.Permissions.Allow {
		if _, ok := allowed[got]; !ok {
			t.Fatalf("allow 出现快照不可表达项 %q，完整 %v", got, settings.Permissions.Allow)
		}
	}
}
```

把既有 `WriteTaskEnv(...)` 调用补上最后参数：

```go
executor.PolicySnapshot{Version: "test", Scope: executor.ApprovalScope{Workdir: dir}}
```

`dir` 取该测试的 `t.TempDir()` 或已有 `dir`。这是编译红（签名变了）+ 空 Workdir 断言红。

在 `internal/agentd/resume_test.go` 追加（夹具用同文件已有的 `newTestManagerWithAds` / `mustCreateTask`）：

```go
func TestFakeAdapterDoesNotSatisfyRecoverer(t *testing.T) {
	fk := fake.New(nil)
	if _, ok := any(fk).(executor.Recoverer); ok {
		t.Fatal("fake 不得实现 Recoverer（冻结 50）")
	}
	m, st, _ := newTestManagerWithAds(t, map[string]executor.Adapter{"fake": fk}, "fake")
	mustCreateTask(t, st, &proto.Task{
		ID: "t-fake-norecover", RepoPath: "/r", Executor: "fake",
		State: proto.TaskStateRunning,
	})
	if alive := m.ResumeTask("t-fake-norecover"); alive {
		t.Fatal("未实现 Recoverer 的 adapter 必须走不存活失败路径，禁止 ResumeTask 返回 true")
	}
}
```

本支今日基线对「fake 无 Resume」已经为真，可能先绿。本票要锁的新行为是生产代码删除私有 `restorer`、改断言 `executor.Recoverer`：`rg 'type restorer interface' internal/agentd/manager.go` 必须从有到无。既有 `TestResumeTaskAssemblesRequest`（`recordingRestorer`）在改断言后必须仍绿。

2. **跑红**

```bash
go test ./internal/executor/claudecode -run 'TestWriteTaskEnvEmptyWorkdirWithSnapshotFails|TestWriteTaskEnvSnapshotAllowIsSubsetAndNotNakedBash|TestWriteTaskEnvGeneratesSettingsAndMCP' -count=1
```

预期：编译红 `WriteTaskEnv` 参数个数不对，或空 Workdir 仍成功。

3. **最小实现**

`claudecode/taskenv.go` 签名改为 §2。函数体在写 settings 之前：

```go
	if strings.TrimSpace(snap.Version) != "" && strings.TrimSpace(snap.Scope.Workdir) == "" {
		return "", "", "", fmt.Errorf("%w: 空 Workdir", executor.ErrUnrepresentablePolicy)
	}
```

写 allow 时**继续用 `allowRules` 前缀白名单**（它已是可精确表达子集）。额外守卫：

```go
	allow := append([]string(nil), allowRules...)
	filtered := allow[:0]
	for _, r := range allow {
		if r == "Bash" {
			continue // 无限制 bash 无法 ⊆ 快照
		}
		filtered = append(filtered, r)
	}
	allow = filtered
```

`adapter.go` Start 在调用 `WriteTaskEnv` 前取快照：

```go
	var snap executor.PolicySnapshot
	if req.Approval != nil {
		s, serr := req.Approval.PolicySnapshot(ctx)
		if serr != nil {
			rollback()
			return fmt.Errorf("读取政策快照: %w", serr)
		}
		snap = s
		a.log.Info("claude 使用政策快照", "task", req.Task.ID, "version", snap.Version)
	}
	settingsPath, mcpPath, promptText, err := WriteTaskEnv(req.TaskDir, req.Task.ID, req.PlanContent, sockPath, bin, req.Discipline, snap)
```

`manager.go`：

- 删除 `type restorer interface { ... }` 整块（约 3594–3605）。
- 两处 `ad.(restorer)` 改为 `ad.(executor.Recoverer)`。
- `Dispatch` 在 `resolveExecutor` 成功后、`ad.Start` 前：

```go
	if m.reg != nil {
		if rerr := m.reg.Require(execName, executor.CapExecution); rerr != nil {
			m.log.Error("dispatch 执行能力不足", "executor", execName, "cause", rerr)
			return nil, fmt.Errorf("%w: %v", errBadDispatchRequest, rerr)
		}
	}
```

注意：fake 声明了 execution，测试派发 fake 仍过。未知名仍走 `resolveExecutor` 的「未注册」。

- Approval 注入从「仅 OpenCode」扩到 Claude：

```go
	if isOpenCodeExecutor(execName, m.conf().Executor.Default) || execName == executor.HarnessClaude {
```

`Dispatch` 成功路径已有 adapter 启动日志；补一条：

```go
	m.log.Info("dispatch 经执行能力面", "task", taskID, "executor", execName)
```

`ResumeTask` 在 `ok` 为 false 时已有 Warn「不支持执行恢复」——保持。成功路径已有后续日志则不静默。

4. **跑绿**

```bash
go test ./internal/executor/claudecode ./internal/agentd ./internal/executor -count=1
```

5. **加关键节点日志 / 6. 加注释**

- `WriteTaskEnv`：入口 Info（task、version、task_dir）；写盘失败 Error；成功 Info。不要把 allow 全表打 Info。
- 文件头补一句：PolicySnapshot 入参后，裸 Bash 禁止进入 allow。
- `ErrUnrepresentablePolicy` 定义在 opencode 包。claude 不要 import opencode。改用 `fmt.Errorf("快照含无法在原生配置实施的明确禁止: 空 Workdir")` 或把哨兵升到 `executor` 包。

**升哨兵（最小）**：`internal/executor/approval.go` 已有契约类型。在 `internal/executor/policy.go` 新建：

```go
package executor

import "errors"

// ErrUnrepresentablePolicy 表示快照含无法在原生配置精确实施的条件。
var ErrUnrepresentablePolicy = errors.New("快照含无法在原生配置实施的明确禁止")
```

然后改 `opencode/taskenv.go` 的 `var ErrUnrepresentablePolicy = ...` 为：

```go
var ErrUnrepresentablePolicy = executor.ErrUnrepresentablePolicy
```

（别名保持现有测试 `opencode.ErrUnrepresentablePolicy` 仍可 `errors.Is`。）claude 用 `executor.ErrUnrepresentablePolicy`。

既有 opencode 测试若比较指针相等，改用 `errors.Is`。先跑：

```bash
go test ./internal/executor/opencode -run 'Unrepresentable|WriteTaskEnv|WritePermission' -count=1
```

今日绿；改动后必须仍绿。

### 验收

- fake `ResumeTask` → false。
- `grep -n "type restorer interface" internal/agentd/manager.go` 无命中。
- Claude 写盘 allow 无裸 `Bash`；空 Workdir 失败。
- 缺陷族：未实现 Recoverer 假装恢复 = 本票消灭的窗口；原生 allow 是第 0 道门。

---

## 5. Task 3：协调会话 OpenCode 实装，其余不支持，Runner 退役为包装

### 精确文件集

- `internal/executor/opencode/coordinator.go`（新建）
- `internal/executor/opencode/coordinator_test.go`（新建）
- `internal/agentd/server.go`（`coordinatorRunner` 改持 `Coordinator`+`Registry`）
- `internal/agentd/coordinator_home_test.go`（构造 runner 时注入 coord+reg）
- `codegraph/target.json`（`d_keystone→d_execution` interfaces Coordinator）
- `internal/hostapi/driver.go`：**不得**扩 `supportedCLIs`

### Interfaces

Consumes：`Coordinator`、`RequireLaunchPrompt`、`RequireResumeRef`、`Host.RunTurn`、T1 Registry。
Produces：见 §2 T3。`keysclient.Runner` 签名不变。

### 基线判据先跑

```bash
go test ./internal/hostapi -run 'RunTurn' -count=1
go test ./internal/agentd -run 'TestCoordinatorHomeSupplyOnLaunchAndResume' -count=1
```

今日：Launch 用 `context.Background()`；claude CLI 走 hostapi「未实装」，**不是** `ErrCapabilityUnsupported`。

测试范围：`go test ./internal/executor/opencode ./internal/hostapi ./internal/agentd ./internal/keysclient ./internal/executor -count=1`。

### 步骤

1. **写失败测试（缝：`Coordinator.Launch` / `coordinatorRunner.Launch`）**

`internal/executor/opencode/coordinator_test.go`：假 CLI 桩**照抄** `internal/hostapi/runturn_test.go` 的 `installFakeCLI` / `withArgvCapture` / `FAKECLI_SLEEP`（断言逐条列全，不发明第二种桩）。

```go
func TestCoordinatorLaunchEmptyPrompt(t *testing.T) {
	c := NewCoordinator(hostapi.New(), slog.Default())
	_, err := c.Launch(context.Background(), executor.CoordSessionSpec{CLI: "opencode", HomeDir: t.TempDir()}, "")
	if !errors.Is(err, executor.ErrEmptyLaunchPrompt) {
		t.Fatalf("err = %v", err)
	}
	_, err = c.Launch(context.Background(), executor.CoordSessionSpec{CLI: "opencode", HomeDir: t.TempDir()}, "  \n")
	if !errors.Is(err, executor.ErrEmptyLaunchPrompt) {
		t.Fatalf("blank err = %v", err)
	}
}

func TestCoordinatorResumeEmptySession(t *testing.T) {
	c := NewCoordinator(hostapi.New(), slog.Default())
	_, err := c.Resume(context.Background(), executor.CoordSessionRef{CLI: "opencode"}, "hi")
	if !errors.Is(err, executor.ErrEmptyResumeSession) {
		t.Fatalf("err = %v", err)
	}
}

func TestCoordinatorLaunchPassesCtxAndGetsSession(t *testing.T) {
	installFakeCLI(t) // 把 hostapi/runturn_test.go 的 installFakeCLI 整函数复制进本文件（占位符例外，见 §11）。不要抽共享测试包。
	c := NewCoordinator(hostapi.New(), slog.Default())
	got, err := c.Launch(context.Background(), executor.CoordSessionSpec{
		CLI: "opencode", HomeDir: t.TempDir(), Workdir: t.TempDir(),
	}, "hello")
	if err != nil {
		t.Fatal(err)
	}
	if got.SessionID == "" || got.Output == "" {
		t.Fatalf("got %+v", got)
	}
}

func TestCoordinatorLaunchHonorsCancel(t *testing.T) {
	installFakeCLI(t)
	t.Setenv("FAKECLI_SLEEP", "30")
	c := NewCoordinator(hostapi.New(), slog.Default())
	ctx, cancel := context.WithTimeout(context.Background(), 200*time.Millisecond)
	defer cancel()
	start := time.Now()
	_, err := c.Launch(ctx, executor.CoordSessionSpec{
		CLI: "opencode", HomeDir: t.TempDir(), Workdir: t.TempDir(),
	}, "hello")
	if err == nil {
		t.Fatal("取消后必须失败")
	}
	if time.Since(start) > 5*time.Second {
		t.Fatalf("未在 ctx 取消后打断，耗时 %s", time.Since(start))
	}
}
```

`internal/agentd` 新建 `coordinator_runner_test.go`（入口 `coordinatorRunner.Launch`）：

```go
func TestCoordinatorRunnerUnsupportedDoesNotCallOpenCode(t *testing.T) {
	var runs int
	h := hostapi.New()
	_ = h
	reg := executor.NewRegistry(executor.StaticProvider{
		HarnessName: executor.HarnessClaude,
		Rep: mustReport(t, executor.HarnessClaude),
	})
	r := coordinatorRunner{
		coord: countingCoord{fn: func() { runs++ }},
		reg:   reg,
	}
	_, err := r.Launch(keysclient.SessionSpec{CLI: "claude", HomeDir: t.TempDir()}, "hi")
	if !errors.Is(err, executor.ErrCapabilityUnsupported) {
		t.Fatalf("err = %v", err)
	}
	if runs != 0 {
		t.Fatalf("禁止兜底 OpenCode，coord 调用次数=%d", runs)
	}
}

func TestCoordinatorRunnerUnsupportedGrokCodexAGY(t *testing.T) {
	for _, name := range []string{"grok", "codex", "agy"} {
		reg := executor.NewRegistry(executor.StaticProvider{
			HarnessName: name,
			Rep:         mustReport(t, name),
		})
		r := coordinatorRunner{coord: countingCoord{}, reg: reg}
		_, err := r.Launch(keysclient.SessionSpec{CLI: name, HomeDir: t.TempDir()}, "hi")
		if !errors.Is(err, executor.ErrCapabilityUnsupported) {
			t.Fatalf("%s err = %v", name, err)
		}
		if !strings.Contains(err.Error(), "禁止静默兜底到另一家") {
			t.Fatalf("%s 错误文本: %v", name, err)
		}
	}
}

func mustReport(t *testing.T, name string) executor.CapabilityReport {
	t.Helper()
	rep, err := executor.BaselineReport(name)
	if err != nil {
		t.Fatal(err)
	}
	return rep
}

type countingCoord struct{ fn func() }

func (c countingCoord) Launch(context.Context, executor.CoordSessionSpec, string) (executor.CoordTurnResult, error) {
	if c.fn != nil {
		c.fn()
	}
	return executor.CoordTurnResult{}, nil
}
func (c countingCoord) Resume(context.Context, executor.CoordSessionRef, string) (executor.CoordTurnResult, error) {
	return executor.CoordTurnResult{}, nil
}
func (c countingCoord) CancelTurn(context.Context, executor.CoordSessionRef) error { return nil }
```

2. **跑红** — `Coordinator` 未定义或 runner 仍走 hostapi 未实装文本。

3. **最小实现**

`internal/executor/opencode/coordinator.go`：

```go
// coordinator.go —— OpenCode 协调会话能力（B233.2）。
//
// 职责：把 Coordinator 三方法映射到 hostapi.Host.RunTurn。
// 边界：不进派发、不 CreateTask；hostapi 仍不是消费方面。
package opencode

import (
	"context"
	"log/slog"
	"sync"

	"github.com/Xsxdot/handoff/internal/executor"
	"github.com/Xsxdot/handoff/internal/hostapi"
)

type Coordinator struct {
	Host *hostapi.Host
	log  *slog.Logger
	mu   sync.Mutex
	canc map[string]context.CancelFunc
}

func NewCoordinator(h *hostapi.Host, log *slog.Logger) *Coordinator {
	if log == nil {
		log = slog.Default()
	}
	return &Coordinator{Host: h, log: log, canc: map[string]context.CancelFunc{}}
}

func (c *Coordinator) Launch(ctx context.Context, spec executor.CoordSessionSpec, prompt string) (executor.CoordTurnResult, error) {
	if err := executor.RequireLaunchPrompt(prompt); err != nil {
		return executor.CoordTurnResult{}, err
	}
	c.log.Info("Coordinator.Launch 进入", "cli", spec.CLI, "home_isolated", spec.HomeDir != "", "prompt_bytes", len(prompt))
	return c.turn(ctx, hostapi.TurnRequest{
		CLI: spec.CLI, HomeDir: spec.HomeDir, Workdir: spec.Workdir,
		Model: spec.Model, Prompt: prompt, Env: spec.Env,
	}, spec.HomeDir)
}

func (c *Coordinator) Resume(ctx context.Context, ref executor.CoordSessionRef, prompt string) (executor.CoordTurnResult, error) {
	if err := executor.RequireResumeRef(ref); err != nil {
		return executor.CoordTurnResult{}, err
	}
	c.log.Info("Coordinator.Resume 进入", "cli", ref.CLI, "session_set", ref.SessionID != "")
	return c.turn(ctx, hostapi.TurnRequest{
		CLI: ref.CLI, HomeDir: ref.HomeDir, Workdir: ref.Workdir,
		Model: ref.Model, Prompt: prompt, SessionID: ref.SessionID, Env: nil,
	}, ref.HomeDir)
}

func (c *Coordinator) CancelTurn(_ context.Context, ref executor.CoordSessionRef) error {
	key := ref.HomeDir + "\x00" + ref.CLI
	c.mu.Lock()
	cancel, ok := c.canc[key]
	c.mu.Unlock()
	if !ok {
		c.log.Info("CancelTurn 无在途回合", "cli", ref.CLI)
		return nil
	}
	c.log.Info("CancelTurn 取消在途回合", "cli", ref.CLI)
	cancel()
	return nil
}

func (c *Coordinator) turn(ctx context.Context, req hostapi.TurnRequest, keyHome string) (res executor.CoordTurnResult, err error) {
	if ctx == nil {
		ctx = context.Background()
	}
	ctx, cancel := context.WithCancel(ctx)
	key := keyHome + "\x00" + req.CLI
	c.mu.Lock()
	c.canc[key] = cancel
	c.mu.Unlock()
	defer func() {
		cancel()
		c.mu.Lock()
		delete(c.canc, key)
		c.mu.Unlock()
		if err != nil {
			c.log.Error("协调回合失败", "cli", req.CLI, "cause", err)
		} else {
			c.log.Info("协调回合完成", "cli", req.CLI, "session_set", res.SessionID != "", "output_bytes", len(res.Output))
		}
	}()
	reply, err := c.Host.RunTurn(ctx, req)
	if err != nil {
		return executor.CoordTurnResult{}, err
	}
	return executor.CoordTurnResult{SessionID: reply.SessionID, Output: reply.Output}, nil
}
```

`server.go` `coordinatorRunner` 改为 §2 字段。`Launch`：

```go
func (r coordinatorRunner) Launch(spec keysclient.SessionSpec, prompt string) (keysclient.TurnResult, error) {
	name := filepath.Base(spec.CLI)
	if r.reg != nil {
		if err := r.reg.Require(name, executor.CapCoordination); err != nil {
			slog.Default().Error("协调能力不支持，拒绝 Launch", "cli", spec.CLI, "cause", err)
			return keysclient.TurnResult{}, err
		}
	}
	if r.prepareHome != nil {
		prepared, err := r.prepareHome(spec)
		if err != nil {
			slog.Default().Error("协调者 Launch 准备 HOME 失败", "cli", spec.CLI,
				"home_dir", spec.HomeDir, "workdir", spec.Workdir, "cause", err)
			return keysclient.TurnResult{}, err
		}
		spec.HomeDir = prepared
	}
	if r.coord == nil {
		return keysclient.TurnResult{}, executor.UnsupportedError(name, executor.CapCoordination)
	}
	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()
	got, err := r.coord.Launch(ctx, executor.CoordSessionSpec{
		CLI: spec.CLI, HomeDir: spec.HomeDir, Model: spec.Model, Workdir: spec.Workdir, Env: spec.Env,
	}, prompt)
	if err != nil {
		return keysclient.TurnResult{}, err
	}
	return keysclient.TurnResult{SessionID: got.SessionID, Output: got.Output}, nil
}
```

`Resume` 同形：先 `Require(CapCoordination)`，再 `prepareHome`，再 `coord.Resume`。**禁止**再直接 `r.h.RunTurn`。

`SetupAutomation`：在已有 `s.hostAPI = ...` 之后：

```go
	coord := opencode.NewCoordinator(s.hostAPI, slog.Default())
	runner := coordinatorRunner{coord: coord, reg: s.providers, prepareHome: supplier.Prepare}
```

Server 增加 `providers *executor.Registry` 与 `SetProviders`。`cmd/agentd.go` 在 `ads := defaultAdapters` 之后、`setupLedger` 之前：

```go
		srv.SetProviders(agentd.RegistryFromAds(ads))
```

把 `registryFromAds` 导出为 `RegistryFromAds`（或同包测试不需要导出；cmd 在另一个包，必须导出）。

`coordinator_home_test.go` 里所有 `coordinatorRunner{h: h, prepareHome: ...}` 改为注入 `coord: opencode.NewCoordinator(h, slog.Default())` 与一个含 opencode BaselineReport 的 Registry。

**target.json** 在 contracts 数组增加（与 `d_keystone→d_ledger` 同形）：

```json
  {
   "from": "d_keystone",
   "to": "d_execution",
   "legacyBudget": 0,
   "interfaces": ["Coordinator"],
   "legacyBudgetNote": "B233.2：coordinatorRunner 包装 Coordinator；keysclient.Runner 签名不变。零调用预算——keystone 生产代码不得 import 各家 adapter。"
  }
```

`supportedCLIs` 保持只含 opencode。Coordinator 在 Require 之后才进 RunTurn，claude 不会碰到「未实装」这条老错误。

4. **跑绿** + 日志/注释（文件头已写）。`CancelTurn` 无在途 Info；有则 Info。Launch 入口/出口已写。

5. Coordinator 实现**禁止**调用 `Manager.Dispatch` / `CreateTask`。测试：`grep -n "Dispatch\\|CreateTask" internal/executor/opencode/coordinator.go` 无命中。

### 验收

- claude/grok/codex/agy Launch → `errors.Is(ErrCapabilityUnsupported)`，countingCoord 零次。
- OpenCode 空 prompt / 空 SessionID 哨兵。
- ctx 取消打断（机内假 CLI sleep）。
- `codegraph --repo . --view cards-B233.2-charter check` 不得**新**增 fail。ApprovalClient 两条存量 fail 保持。

---

## 6. Task 4：OneShot.Invoke 迁入 Approver，grok effort 是调用方 Limits

### 精确文件集

- `internal/executor/oneshot_run.go`（新建）
- `internal/executor/oneshot_run_test.go`（新建，ctx 取消打断进程）
- `internal/executor/opencode/oneshot.go`、`oneshot_argv.go`（新建）
- `internal/executor/claudecode/oneshot.go`、`oneshot_argv.go`（新建）
- `internal/executor/agy/oneshot.go`、`oneshot_argv.go`（新建）
- `internal/executor/codex/oneshot.go`、`oneshot_argv.go`（新建）
- `internal/executor/grok/oneshot.go`（新建；argv 已有）
- 各家 `Adapter.Invoke` 委托（可写在 oneshot.go）
- `internal/agentd/approver.go`（Decide 改 Invoke；删对 OneShotArgs 的调用；`BindOneShot`）
- `internal/agentd/approver_test.go`、`export_test.go`、`approval_client_test.go`（runCmd 缝换 OneShot 缝）
- `cmd/agentd.go`（组装点 BindOneShot）
- 最后一步：删除 `internal/executor/oneshot.go` 的 `OneShotArgs`，改 `oneshot_test.go` / `oneshot_cap_test.go`

### 基线

```bash
go test ./internal/executor -run 'TestOneShotArgs|TestOneShotArgsStillEncodesGrokLowUntilMigrated' -count=1
go test ./internal/executor/grok -run 'OneShotArgv' -count=1
go test ./internal/agentd -run 'Approver|Decide' -count=1
```

今日：Decide 调 OneShotArgs；grok 默认 low。

测试范围：`go test ./internal/executor ./internal/executor/grok ./internal/executor/opencode ./internal/executor/claudecode ./internal/executor/agy ./internal/executor/codex ./internal/agentd ./cmd -count=1`。

### 步骤

1. **写失败测试（缝：`OneShot.Invoke` / `Approver.Decide`）**

`internal/executor/oneshot_run_test.go`：

```go
func TestRunOneShotProcessHonorsCancel(t *testing.T) {
	ctx, cancel := context.WithTimeout(context.Background(), 200*time.Millisecond)
	defer cancel()
	start := time.Now()
	_, err := executor.RunOneShotProcess(ctx, []string{"sleep", "30"}, nil, "", "")
	if err == nil {
		t.Fatal("取消后必须失败")
	}
	if time.Since(start) > 5*time.Second {
		t.Fatalf("进程未被打断，耗时 %s", time.Since(start))
	}
}
```

各家 `oneshot_argv_test.go` 金样本（入口编码函数；序列化边界，见 §11）。以 claude 为例：

```go
func TestOneShotArgv(t *testing.T) {
	got, err := OneShotArgv("haiku", "p", executor.OneShotLimits{})
	if err != nil {
		t.Fatal(err)
	}
	want := []string{"claude", "-p", "--model", "haiku", "p"}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("got %v want %v", got, want)
	}
	got, err = OneShotArgv("", "p", executor.OneShotLimits{})
	if err != nil {
		t.Fatal(err)
	}
	if !reflect.DeepEqual(got, []string{"claude", "-p", "p"}) {
		t.Fatalf("got %v", got)
	}
}
```

opencode：`{"opencode","run","-m","m1","p"}` / `{"opencode","run","p"}`。
agy：`{"agy","--model","m","-p","p"}` / `{"agy","-p","p"}`。
codex：与今日 `TestOneShotArgs` 的两条完全相同（含 sandbox/exec 旗）；Limits 不得出现 sandbox 字段——`OneShotLimits` 结构体没有该字段，编译器已锁。

`internal/agentd/approver_test.go` 把 `newTestApprover` 换成：

```go
type stubShot struct {
	out, lastPrompt string
	last            executor.OneShotReq
	err             error
}

func (s *stubShot) Invoke(_ context.Context, req executor.OneShotReq) (executor.OneShotReply, error) {
	s.last = req
	s.lastPrompt = req.Prompt
	nonce := extractNonceForTest(req.Prompt)
	return executor.OneShotReply{Text: injectNonceForTest(s.out, nonce), Status: executor.OneShotOK}, s.err
}

func newTestApprover(t *testing.T, out string, err error) *Approver {
	a, aerr := NewApprover(config.ApproverConfig{Executor: "opencode", Timeout: time.Second}, nil, slog.Default())
	if aerr != nil {
		t.Fatal(aerr)
	}
	a.BindOneShot(&stubShot{out: out, err: err})
	return a
}
```

新增（缝 `Approver.Decide`）：

```go
func TestDecideInvokesOneShotWithGrokEffortLow(t *testing.T) {
	a, err := NewApprover(config.ApproverConfig{Executor: "grok", Timeout: time.Second}, nil, slog.Default())
	if err != nil {
		t.Fatal(err)
	}
	stub := &stubShot{out: `{"decision":"escalate","reason":"x"}`}
	a.BindOneShot(stub)
	d := a.Decide(context.Background(), "Bash: ls", "摘要")
	if d.Err != nil && d.Approve {
		t.Fatalf("unexpected approve: %+v", d)
	}
	if stub.last.Limits.Effort != executor.EffortLow {
		t.Fatalf("grok 审批必须由调用方传入 EffortLow，got %q", stub.last.Limits.Effort)
	}
	if stub.last.Limits.Timeout != 0 {
		t.Fatalf("Timeout 不得编进 Limits 当 argv；取消走 ctx。got %s", stub.last.Limits.Timeout)
	}
}

func TestDecideDoesNotCallOneShotArgs(t *testing.T) {
	a := newTestApprover(t, `{"decision":"escalate","reason":"x"}`, nil)
	_ = a.Decide(context.Background(), "Bash: ls", "摘要")
	// 本测试的存在意义：Decide 源码不得再出现 OneShotArgs。由 T4 收尾 grep 锁。
}
```

`approval_client_test.go` 里「argv 来自 OneShotArgs、不得出现 /session/」改为：stub 记录 `OneShotReq`；`Prompt` 不含 `/session/`；`Workdir`/`HomeDir` 不是被审批任务目录。既有负例语义保持。

2. **跑红** — `BindOneShot` / `RunOneShotProcess` / `Invoke` undefined。

3. **最小实现**

`internal/executor/oneshot_run.go`：

```go
package executor

import (
	"bytes"
	"context"
	"fmt"
	"os/exec"
)

const maxOneShotOutput = 64 << 10

func RunOneShotProcess(ctx context.Context, argv []string, env []string, workdir, homeDir string) (string, error) {
	if ctx == nil {
		return "", fmt.Errorf("OneShot.Invoke 禁止 nil ctx")
	}
	if len(argv) == 0 {
		return "", fmt.Errorf("OneShot argv 为空")
	}
	cmd := exec.CommandContext(ctx, argv[0], argv[1:]...)
	if len(env) > 0 {
		cmd.Env = env
	}
	if workdir != "" {
		cmd.Dir = workdir
	}
	if homeDir != "" {
		cmd.Env = append(cmd.Environ(), "HOME="+homeDir)
	}
	var out bytes.Buffer
	cmd.Stdout = &out
	cmd.Stderr = &out
	err := cmd.Run()
	b := out.Bytes()
	if len(b) > maxOneShotOutput {
		b = b[:maxOneShotOutput]
	}
	return string(b), err
}
```

各家 `OneShotArgv`（`Limits.Timeout` 一律不编进 argv）。grok 已有，不要改默认。其余四家完整函数：

```go
// internal/executor/opencode/oneshot_argv.go
func OneShotArgv(model, prompt string, _ executor.OneShotLimits) ([]string, error) {
	if model != "" {
		return []string{"opencode", "run", "-m", model, prompt}, nil
	}
	return []string{"opencode", "run", prompt}, nil
}

// internal/executor/claudecode/oneshot_argv.go
func OneShotArgv(model, prompt string, _ executor.OneShotLimits) ([]string, error) {
	if model != "" {
		return []string{"claude", "-p", "--model", model, prompt}, nil
	}
	return []string{"claude", "-p", prompt}, nil
}

// internal/executor/agy/oneshot_argv.go
func OneShotArgv(model, prompt string, _ executor.OneShotLimits) ([]string, error) {
	if model != "" {
		return []string{"agy", "--model", model, "-p", prompt}, nil
	}
	return []string{"agy", "-p", prompt}, nil
}

// internal/executor/codex/oneshot_argv.go
func OneShotArgv(model, prompt string, _ executor.OneShotLimits) ([]string, error) {
	argv := []string{"codex", "exec", "--skip-git-repo-check", "--ephemeral",
		"--color", "never", "--sandbox", "read-only", "--ignore-user-config"}
	if model != "" {
		argv = append(argv, "-m", model)
	}
	return append(argv, prompt), nil
}
```

各家 `oneshot.go`：

```go
type OneShot struct{ log *slog.Logger }

func NewOneShot(log *slog.Logger) *OneShot {
	if log == nil {
		log = slog.Default()
	}
	return &OneShot{log: log}
}

func (s *OneShot) Invoke(ctx context.Context, req executor.OneShotReq) (executor.OneShotReply, error) {
	s.log.Info("OneShot.Invoke 进入", "harness", executor.HarnessClaude, "model", req.Model, "prompt_bytes", len(req.Prompt), "effort", req.Limits.Effort)
	argv, err := OneShotArgv(req.Model, req.Prompt, req.Limits)
	if err != nil {
		s.log.Error("OneShot argv 编码失败", "cause", err)
		return executor.OneShotReply{Status: executor.OneShotFailed}, err
	}
	out, err := executor.RunOneShotProcess(ctx, argv, req.Env, req.Workdir, req.HomeDir)
	if ctx.Err() != nil {
		s.log.Info("OneShot 已取消", "cause", ctx.Err())
		return executor.OneShotReply{Text: out, Status: executor.OneShotCanceled}, ctx.Err()
	}
	if err != nil {
		s.log.Error("OneShot 进程失败", "cause", err)
		return executor.OneShotReply{Text: out, Status: executor.OneShotFailed}, err
	}
	s.log.Info("OneShot.Invoke 完成", "status", executor.OneShotOK, "output_bytes", len(out))
	return executor.OneShotReply{Text: out, Status: executor.OneShotOK}, nil
}

func (a *Adapter) Invoke(ctx context.Context, req executor.OneShotReq) (executor.OneShotReply, error) {
	return NewOneShot(a.log).Invoke(ctx, req)
}
```

`var _ executor.OneShot = (*OneShot)(nil)` 与 `var _ executor.OneShot = (*Adapter)(nil)`。

`Approver` 增加字段 `shot executor.OneShot` 与：

```go
func (a *Approver) BindOneShot(shot executor.OneShot) { a.shot = shot }
```

`Decide` 替换 `OneShotArgs` + `runCmd` 段：

```go
	if a.shot == nil {
		d := ApproverDecision{Approve: false, ElapsedMS: time.Since(start).Milliseconds(), Err: fmt.Errorf("审批者未绑定 OneShot")}
		a.log.Error("审批者裁决缺少 OneShot", "executor", a.executorName)
		return d
	}
	req := executor.OneShotReq{Prompt: prompt, Model: a.model, Limits: executor.OneShotLimits{}}
	if a.executorName == executor.HarnessGrok {
		req.Limits.Effort = executor.EffortLow
	}
	ctx, cancel := context.WithTimeout(ctx, a.timeout)
	defer cancel()
	reply, err := a.shot.Invoke(ctx, req)
	elapsed := time.Since(start).Milliseconds()
	if err != nil {
		d := ApproverDecision{Approve: false, ElapsedMS: elapsed, Err: err}
		a.log.Error("审批者裁决失败", "permission", truncateRunes(permission, 80),
			"cause", err, "output", truncateRunes(reply.Text, 200), "elapsed_ms", elapsed)
		return d
	}
	d := parseDecision(reply.Text, nonce, elapsed)
```

**禁止**把 `reply` 当 allow/deny。解析仍是 `parseDecision`。

`cmd/agentd.go` 在 `ads := defaultAdapters(logger)` 之后：

```go
		if ap != nil {
			bindApproverOneShot(ap, ads, cfg.Approver.Executor, logger)
		}
```

同文件：

```go
func bindApproverOneShot(ap *agentd.Approver, ads map[string]executor.Adapter, name string, log *slog.Logger) {
	if ap == nil || name == "" {
		return
	}
	if ad, ok := ads[name]; ok {
		if shot, ok := ad.(executor.OneShot); ok {
			ap.BindOneShot(shot)
			log.Info("审批者已绑定 OneShot", "executor", name)
			return
		}
	}
	switch name {
	case executor.HarnessOpenCode:
		ap.BindOneShot(opencode.NewOneShot(log))
	case executor.HarnessClaude:
		ap.BindOneShot(claudecode.NewOneShot(log))
	case executor.HarnessGrok:
		ap.BindOneShot(grok.NewOneShot(log))
	case executor.HarnessCodex:
		ap.BindOneShot(codex.NewOneShot(log))
	case executor.HarnessAGY:
		ap.BindOneShot(agy.NewOneShot(log))
	default:
		log.Error("审批者执行者无 OneShot 可绑定", "executor", name)
	}
}
```

`switch` 只允许出现在 `cmd/agentd.go` 组装点。

把所有 `ap.runCmd =` 换成 `BindOneShot(&stubShot{...})`。`consultApproverForTest` 同样。`runCmd` 与 `defaultRunCmd` 在本票末删除。

4. **退役 OneShotArgs**（同一 task 最后）：删除 `oneshot.go` 中 `OneShotArgs`。`TestOneShotArgs` 改为各家 `OneShotArgv` 已覆盖则删除。`TestOneShotArgsStillEncodesGrokLowUntilMigrated` **删除**（职责被 `TestDecideInvokesOneShotWithGrokEffortLow` + `TestOneShotArgvGoldenVectors` 的 no-effort 支接走）。

5. **跑绿**

```bash
go test ./internal/executor ./internal/executor/grok ./internal/executor/opencode ./internal/executor/claudecode ./internal/executor/agy ./internal/executor/codex ./internal/agentd ./cmd -count=1
grep -n OneShotArgs --include='*.go' internal/agentd internal/executor || true
```

生产代码零命中 `OneShotArgs`。

6. **日志/注释**：Invoke 入口/编码失败/进程失败/取消/成功；Decide 已有入口出口。新文件头职责+边界。`bindApproverOneShot` 注释写清：组装点允许 switch；审批者包禁止 switch。

### 验收

- grok Decide → `Limits.Effort == EffortLow`。
- 空 Effort 的 grok argv 无 `--effort`。
- ctx 取消打断 `sleep 30`。
- Decide 失败 fail-closed（Approve=false + Err）。
- 缺陷族：序列化边界 argv 金样本 + Approver→Invoke 穿过真实边界。

---

## 7. Task 5：Profile / 隔离 HOME

### 精确文件集

- 各家 `profile.go`（新建）：`Inspect`/`Prepare`/`Verify` + `RulesRelFile`
- `internal/agentd/coordinator_home.go`：删 `copyCoordinatorRules`；Prepare 改调 Profile；symlink 白名单可保留作防御，但写入走 Profile
- `internal/agentd/coordinator_home_test.go`
- `internal/agentd/server.go`：`SetRuleLoader`
- `cmd/agentd.go`：`loadCarrierRules` / `readSkillTree` / `SetRuleLoader` 接线
- `internal/executor/opencode/profile_test.go`、`internal/executor/claudecode/profile_test.go`

### 基线

```bash
go test ./internal/agentd -run 'TestCoordinatorHome' -count=1
go test ./internal/executor -run 'TestZeroProfileReportIsNotSuccess' -count=1
```

今日：`copyCoordinatorRules` 源/目标都是 `.config/opencode`。occupied 子测已锁 sessions.db 不被破坏。

测试范围：`go test ./internal/executor/... ./internal/agentd -count=1`。

### 步骤

1. **写失败测试（缝：`Profile.Prepare` / `coordinatorHomeSupplier.Prepare`）**

`internal/executor/opencode/profile_test.go` 断言逐条：

- 零值 Report 三布尔 false（可调 `executor` 已有测试，本票保持）。
- `Prepare` 写入 `req.Rules` 到 `{HomeDir}/.config/opencode/{Name}`，读盘内容相等。
- 目标 HOME 预先放 `{HomeDir}/.local/share/opencode/sessions.db`；Prepare 后该文件仍在且内容不变；Prepare **不得新拷**一份从「源 HOME」来的 sessions.db（构造源侧 sessions.db，目标侧不得出现源内容）。
- `TaskOverlay` 写入 `{HomeDir}/.handoff/task-overlay/{Name}`，**不**覆盖 `{HomeDir}/.config/opencode/AGENTS.md`。两次 Prepare 不同 Overlay：全局 AGENTS.md 保持第一次 Rules 内容。
- 第一次 Prepare 把目标规则目录改成无写权限（或注入失败钩：本票用 `os.Chmod(parent, 0500)` unix）；返回 err 或 `Prepared=false`；chmod 恢复后再 Prepare 成功（可重试）。Windows 无 POSIX 位则 `t.Skip` 权限用例，读盘用例仍跑。
- 测试不得调用 `ProbeHome`/`WakeHome`。

claude `profile_test.go`：

- `Prepare` 后 `Notes` 或 `Missing` 含凭据不足含义（「Keychain」或「无可靠文件凭据」）；不得声称已拷贝登录态。

`coordinator_home.go` 生产路径：`Prepare` 不再调用 `copyCoordinatorRules` 作为权威。测试 `TestCoordinatorHomeSupplyOnLaunchAndResume` 必须仍绿：AGENTS.md / skills 仍出现在目标 OpenCode 目录——改由 Profile 写入。sessions.db sentinel 子测仍绿。

2. **跑红**

3. **最小实现**

各家 `Profile` 方法挂在 Adapter 上（Bundle）。OpenCode：

- `Inspect`：stat 原生规则/skills 路径，填 `Missing`。
- `Prepare`：
  - `HomeDir` 空 → error。
  - `MkdirAll` 原生根 `filepath.Join(req.HomeDir, ".config", "opencode")` 0700。
  - 写 `req.Rules` 到该根（`Name` 禁止 `..`）。
  - 写 `req.Skills` 到 `skills/` 子树。
  - 写 `req.TaskOverlay` 到 `filepath.Join(req.HomeDir, ".handoff", "task-overlay")`。
  - **不**遍历、不拷 `.local/share/opencode` 除调用方已存在文件之外的任何东西。
  - 失败返回 err 且 `Prepared=false`。成功 `Prepared=true`，`Verified`/`EngineOK` 仍 false。
- `Verify`：规则文件存在则 `Verified=true`。`EngineOK` 保持 false（真机才验引擎；机内禁止假装）。

claude：原生根 `.claude`。凭据：`Notes` 追加 `claude 凭据在 Keychain，无可靠文件判据，未拷贝登录态`。agy：`.gemini/antigravity-cli`。grok：`.grok`。codex：`.codex`。

`coordinatorHomeSupplier` 增加：

```go
	profileFor func(cli string) (executor.Profile, error)
```

`Prepare` 在投影 handoff `config.yaml` 与 `copyMissingCoordinatorCredential` 之后，替换 `copyCoordinatorRules`：

```go
	if p.profileFor == nil {
		return "", errors.New("协调者供给缺少 Profile")
	}
	prof, err := p.profileFor(spec.CLI)
	if err != nil {
		return "", err
	}
	if p.loadRules == nil {
		return "", errors.New("协调者供给缺少规则装载函数")
	}
	rules, skills, err := p.loadRules(mainHome, spec.CLI)
	if err != nil {
		return "", err
	}
	rep, err := prof.Prepare(context.Background(), executor.ProfileReq{
		HomeDir:    targetHome,
		Isolated:   true,
		Credential: executor.CredentialMainHomeSync,
		Rules:      rules,
		Skills:     skills,
	})
	if err != nil {
		return "", err
	}
	if !rep.Prepared {
		return "", fmt.Errorf("Profile.Prepare 未成功 prepared=false notes=%v", rep.Notes)
	}
```

各家导出规则文件相对路径（与 `SkillsRelDir` 同文件）：

```go
// opencode
const RulesRelFile = ".config/opencode/AGENTS.md"
// claude
const RulesRelFile = ".claude/CLAUDE.md"
// grok
const RulesRelFile = ".grok/AGENTS.md"
// codex
const RulesRelFile = ".codex/AGENTS.md"
// agy
const RulesRelFile = ".gemini/antigravity-cli/AGENTS.md"
```

`coordinator_home.go` **禁止**再 switch 路径。增加注入：

```go
	loadRules func(mainHome, cli string) (rules, skills []executor.ProfileFile, err error)
```

`Prepare` 里 `rules, skills, err := p.loadRules(mainHome, spec.CLI)`。`loadRules` 由组装点 `cmd/agentd.go` 注入（组装点允许 switch）。完整注入函数：

```go
func loadCarrierRules(mainHome, cli string) ([]executor.ProfileFile, []executor.ProfileFile, error) {
	base := filepath.Base(cli)
	var ruleRel, skillsRel string
	switch base {
	case executor.HarnessOpenCode:
		ruleRel, skillsRel = opencode.RulesRelFile, opencode.SkillsRelDir
	case executor.HarnessClaude:
		ruleRel, skillsRel = claudecode.RulesRelFile, claudecode.SkillsRelDir
	case executor.HarnessGrok:
		ruleRel, skillsRel = grok.RulesRelFile, grok.SkillsRelDir
	case executor.HarnessCodex:
		ruleRel, skillsRel = codex.RulesRelFile, codex.SkillsRelDir
	case executor.HarnessAGY:
		ruleRel, skillsRel = agy.RulesRelFile, agy.SkillsRelDir
	default:
		return nil, nil, executor.UnsupportedError(base, executor.CapProfile)
	}
	var rules []executor.ProfileFile
	if b, err := os.ReadFile(filepath.Join(mainHome, ruleRel)); err == nil {
		rules = append(rules, executor.ProfileFile{Name: filepath.Base(ruleRel), Content: string(b)})
	} else if !errors.Is(err, os.ErrNotExist) {
		return nil, nil, err
	}
	skills, err := readSkillTree(filepath.Join(mainHome, skillsRel), "")
	return rules, skills, err
}
```

`loadCarrierRules` 与 `readSkillTree` 放 **`cmd/agentd.go`**（组装点已 import 五家包）。**禁止** `internal/agentd` import `internal/executor/opencode` 等各家包——那会把路径知识从组装点漏进编排。

`Server` 增加 `SetRuleLoader(fn func(mainHome, cli string) ([]executor.ProfileFile, []executor.ProfileFile, error))`。`cmd` 在 `ads := defaultAdapters` 之后 `srv.SetRuleLoader(loadCarrierRules)`。`SetupAutomation`：`supplier.loadRules = s.ruleLoader`。

`readSkillTree` 完整函数（放 cmd 同文件）：

```go
func readSkillTree(root, rel string) ([]executor.ProfileFile, error) {
	dir := root
	if rel != "" {
		dir = filepath.Join(root, rel)
	}
	entries, err := os.ReadDir(dir)
	if errors.Is(err, os.ErrNotExist) {
		return nil, nil
	}
	if err != nil {
		return nil, err
	}
	var out []executor.ProfileFile
	for _, e := range entries {
		child := e.Name()
		if rel != "" {
			child = filepath.Join(rel, e.Name())
		}
		p := filepath.Join(root, child)
		info, err := os.Lstat(p)
		if err != nil {
			return nil, err
		}
		if info.Mode()&os.ModeSymlink != 0 {
			return nil, fmt.Errorf("规则源含 symlink %q", p)
		}
		if e.IsDir() {
			sub, err := readSkillTree(root, child)
			if err != nil {
				return nil, err
			}
			out = append(out, sub...)
			continue
		}
		b, err := os.ReadFile(p)
		if err != nil {
			return nil, err
		}
		out = append(out, executor.ProfileFile{Name: child, Content: string(b)})
	}
	return out, nil
}
```

`coordinator_home_test.go` 注入一个测试用 `loadRules`：直接读 `mainHome/.config/opencode/AGENTS.md` 与 skills（测试夹具本来就造这两处），不要让测试包 import 五家。原生**写入**位置只存在于各家 `profile.go`。

删除 `copyCoordinatorRules` 函数。symlink 拒绝表可保留 opencode 路径作为防御，但不得再是唯一写入权威。

`SetupAutomation` 注入 `profileFor`：从 `s.providers.Get(filepath.Base(cli))` 断言 `executor.Profile`。

4. **日志**：Prepare 入口（cli、isolated、home 路径）；写每类文件前后 Debug/Info（只记路径与字节数）；失败 Error；成功 Info（Prepared/Verified/EngineOK 三布尔）。

5. **注释**：文件头；「为什么 Overlay 分目录」；「为什么 claude 报不足」。

### 验收

- occupied sessions.db 不被拷/不破坏。
- 两 Overlay 不互相覆盖全局规则。
- `grep copyCoordinatorRules --include='*.go'` 生产代码零命中。
- 测试文件不得出现 `ProbeHome`/`WakeHome` 调用冒充 Prepare。

---

## 8. Task 6：Skills 路径表迁出

### 精确文件集

- 各家 `skills.go`（新建）+ `SkillsRelDir` 常量
- `internal/skill/install.go`、`state.go`（删 `agentDirs`；`Install`/`Status` 收 `[]executor.Skills`）
- `internal/skill/install_test.go`
- `cmd/skill.go`
- `internal/executor/skills_paths_test.go`（新建，金样本）

### 基线

```bash
go test ./internal/skill -count=1
go test ./cmd -run 'Skill' -count=1
```

今日：`agentDirs` 五条路径；一家失败继续；父目录不存在 skipped。

### 步骤

1. **写失败测试（缝：`skill.Install` / `Skills.Install` / `cmd skillInstallCmd`）**

断言（照抄 `install_test.go` 既有夹具：临时 home、只建 `.claude`）：

- `Install(content, home, providers)` 仍写基准副本。
- 只对 providers 调 Install；相对路径来自各家 `SkillsRelDir`，调用方**没有** `[]string{".claude/skills",...}`。
- 一家失败（把该家父目录建成文件而不是目录，或 chmod 0500）：返回的 sites 含该家 skipped+Note，其它已装仍 installed；`err==nil`（基准成功）。
- 父目录不存在 → skipped，不创建 `~/.codex`。
- `GuardRemove` 已有测试保持。
- `TestAgentDirsContract` 改为读五家常量：

```go
func TestNativeSkillRelDirs(t *testing.T) {
	got := []string{
		claudecode.SkillsRelDir,
		codex.SkillsRelDir,
		opencode.SkillsRelDir,
		grok.SkillsRelDir,
		agy.SkillsRelDir,
	}
	want := []string{
		".claude/skills",
		".codex/skills",
		".config/opencode/skills",
		".grok/skills",
		".gemini/antigravity-cli/skills",
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("got %#v want %#v", got, want)
	}
}
```

放 `internal/executor/skills_paths_test.go` 包 `executor_test`。

2. **跑红** — `SkillsRelDir` undefined；`Install` 参数个数不对。

3. **最小实现**

各家：

```go
const SkillsRelDir = ".claude/skills" // 按金样本
```

`Inspect`/`Install`/`Remove` 挂在 Adapter：把今日 `skill.Install` 循环体**一家**的逻辑搬过来（父目录不存在 skipped；RemoveAll 软链；写 `SKILL.md`）。`Remove` 先 `executor.GuardRemove`。`Name` 空则 `SkillDirName("")`。

`skill.Install`：

```go
func Install(content, home string, providers []executor.Skills) ([]Site, error) {
	// 基准副本：保持现有 MkdirAll+WriteFile，失败才 return err
	sites := []Site{{Path: target, State: StateInstalled}}
	ctx := context.Background()
	for _, p := range providers {
		if p == nil {
			continue
		}
		got, err := p.Install(ctx, executor.SkillInstallReq{Home: home, Content: content})
		if err != nil {
			// 一家失败继续：把 err 记进 Note，不 return
			sites = append(sites, Site{Path: "", State: StateSkipped, Note: err.Error()})
			continue
		}
		for _, s := range got {
			sites = append(sites, Site{Path: s.Path, State: s.State, Note: s.Note})
		}
	}
	return sites, nil
}
```

`Status` 同样：先 check 基准，再每家 `Inspect`。

删 `agentDirs`。`cmd/skill.go`：

```go
		providers := []executor.Skills{
			claudecode.New(slog.Default()),
			codex.New(slog.Default()),
			opencode.New(slog.Default()),
			grok.New(slog.Default()),
			agy.New(slog.Default()),
		}
		sites, err := skill.Install(skillContent, home, providers)
```

`Status` 同。这是 CLI 组装点，允许 New 五家。**禁止**在 cmd 再写相对路径表。

既有 `install_test.go` 全部改为传入五家 `New(slog.Default())`（或每家一个 stub Skills）。`TestInstallSkipsMissingAgentDirs` 期望 installed=2（基准+claude）、skipped=4 保持。

4. **日志**：Install 入口（home、provider 数）；每家成功 Info（path、state）；跳过 Warn（path、note）；基准失败 Error。

5. **注释**：为什么不代建 HOME；为什么 Remove 要 Authorized。

### 验收

- `grep agentDirs --include='*.go'` 零命中。
- `grep -n '.claude/skills' cmd internal/skill` 生产代码零平行表（测试金样本除外）。
- 一家失败不全盘失败。

---

## 9. Task 7：退役、接缝回归、CHANGELOG、变异、真机清单

### 精确文件集

- T1–T6 已列测试的回归
- `CHANGELOG.md`
- 本票可新增 `internal/agentd/capability_seam_test.go`（穿过生产入口的缝级断言）

**真机步骤标注：本段真机清单由协调者执行，不派发。** 执行者只写机能在本机闭环的测试与 grep。

### 基线

```bash
go build -o /dev/null .
```

今日退出 0。

测试范围：`go test ./internal/executor/... ./internal/agentd/ ./internal/hostapi/ ./internal/skill/ ./internal/keysclient/ ./cmd/ -count=1`。全量测试不在本 task（三段律）。

### 步骤

1. **缝级回归测试**（每条缝至少一支，入口必须在缝上）

| 缝 | 入口符号 | 断言 |
|---|---|---|
| 执行能力面 | `Manager.Dispatch` | 未知执行者失败；fake 经 `Require(execution)` 能 Start；`reg.Require` 先于 Start（可用间谍 Adapter 计数：Require 失败时 Start 零次——用未声明 execution 的 stub Provider 注册进 ads） |
| 协调能力面 | `coordinatorRunner.Launch` | OpenCode 经 Coordinator；claude 得 `ErrCapabilityUnsupported` 且 countingCoord 零次 |
| OneShot | `Approver.Decide` | 零次 `OneShotArgs`；grok `Limits.Effort==EffortLow` |
| Profile | `coordinatorHomeSupplier.Prepare` | 不再 `copyCoordinatorRules`；sessions.db 不拷 |
| Skills | `skill.Install` | 调用方无路径表；一家失败继续 |
| 免审 | `claudecode.WriteTaskEnv` | 裸 Bash 不进 allow |

把缺的那支补进对应包测试。不要用内部帮手顶替。

2. **退役 grep**（必须零命中，测试名/注释除外）

```bash
rg -n 'copyCoordinatorRules' --glob '*.go'
rg -n 'OneShotArgs' --glob '*.go'
rg -n 'type restorer interface' internal/agentd
rg -n 'var agentDirs' --glob '*.go'
rg -n '\.config/opencode' internal/keysclient internal/agentd/manager.go
```

`coordinator_home.go` 若 symlink 拒绝表仍含 `.config/opencode` 路径：允许作为防御名单，但函数 `copyCoordinatorRules` 必须不存在。在 grep 清单里写明允许命中的文件：仅 `rejectCoordinatorHomeSymlinks` 字符串表。

3. **CHANGELOG.md `[Unreleased]` → `### 变更` 追加：**

```
- **harness 能力归拢（B233.2）。** 审批一次性改走 OneShot 能力面；grok `--effort low` 改为调用方 Limits，不再是 grok 一次性默认。协调 Launch 对未实装协调能力的家返回明确不支持错误（禁止静默兜底到 OpenCode）。隔离 HOME 规则写入改走 Profile，不再写死 `.config/opencode`。`handoff skill install` 不再由调用方持各家路径表。
```

4. **变异（acceptance 用；本票先把测试写到能红）**

- 若 `keysclient` 或 `coordinator_home` 再写死 `.config/opencode` 作为规则权威写入 → 故事 6 的 Profile 测试红（目标路径由 Profile 决定，断言读盘路径是各家 `SkillsRelDir`/`AGENTS.md` 原生位置，不是编排包拼的）。
- 若 OneShot 把 low effort 写回 grok 默认 → `TestOneShotArgvGoldenVectors` 的 no-effort 支 + `TestDecideInvokesOneShotWithGrokEffortLow` 一对红。
- Codex 限额期失败归因模板（写进本计划，真机用）：stderr/日志含限额、quota、usage 时记「限额，不是能力缺失」；**禁止**把它写进 `Report().Caps` 的 coordination/execution=false。

5. **加日志/注释**：本票以回归为主。`capability_seam_test.go` 文件头写职责=接缝回归。

6. **跑绿**

```bash
go build -o /dev/null .
go test ./internal/executor/... ./internal/agentd/ ./internal/hostapi/ ./internal/skill/ ./internal/keysclient/ ./cmd/ -count=1
```

沙箱 `Operation not permitted`：记原文，不归因；无沙箱环境复跑。

### 真机清单（协调者执行，不派发；机内不得写成绿）

与 breakdown §5 相同，抄录为执行者交付物。OpenCode 免费模型；Codex 限额失败不得写成不支持 execution。

1. 本机 OpenCode 免费模型派执行任务。
2. 本机 Grok 派执行任务。
3. 本机 Claude 派执行任务；不可用则记缺口，不改成不迁 Claude。
4. linux-01 AGY 派执行任务。
5. linux-01 Codex（限额恢复后）派执行任务。
6. 对 Claude 调协调 Launch：不支持，不兜底 OpenCode。
7. 本机 OpenCode 拉起协调者 Launch/Resume；HOME 由 Profile 准备。
8. 审批一次性：OpenCode 免费模型 + Grok 各一次经 Invoke。
9. 同一载体并发两任务：全局规则不被 Overlay 覆盖。
10. 故意失败的 HOME 准备：不报成功；修复后重试成功。
11. `handoff skill install`：五家落点；弄坏一家后其它仍装上。
12. OpenCode 免费模型不可用：失败原文；不改会员模型。
13. claude 隔离 HOME：报凭据不足。
14. OpenCode Prepare 后 EngineOK 与文件已写分开观察。
15. ctx 取消进行中的 OpenCode 协调回合。
16. Windows 抽查（若范围内）：OneShot Kill、协调杀树、skill 非软链。

---

## 10. 五项检查

### 10.1 缺陷族对抗审查

项目清单：`docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md` §3。顶部无 `基线版本：charter@…`——按项目清单加严，并并入承重安全属性。

**生命周期**：执行恢复未实现 → 失败路径（T2）。协调取消走 Host `watchDeadline`/`killGroup`（T3），与 OneShot `Process.Kill` 不统一。Profile 半残由下次 Prepare 收口（T5）。Skills 一家失败继续（T6）。无新孤儿类型。

**静默失败**：不支持必须 `errors.Is(ErrCapabilityUnsupported)`（T1/T3）。Prepare 文件写成 ≠ EngineOK（T5）。Decide Invoke 失败 fail-closed（T4）。Codex 限额 ≠ 能力缺失（T7 模板）。BaselineReport ≠ 已接线（C-8）。

**跨平台**：路径 `filepath`。claude Keychain 报不足。skill 不回退目录软链。Windows 权限位测试 Skip。协调杀树 Windows **未验证，需真机**。

**假红/假绿**：不得只断言空壳。claude Launch 必须锁哨兵+零次兜底。EngineOK 机内保持 false。测试锁调用方行为（Invoke 被 Decide 调用），不锁 Bundle 字段顺序。

**门禁**：Claude allow ⊆ 可表达快照；Remove 无 Authorized 不得删；凭据 standalone 不得整树拷。TOCTOU：全局规则与 Overlay 分文件，本卡不新分布式锁。

**序列化边界**：argv 五家金样本 + Approver→Invoke。`Coord*` 与 keysclient 同形已由 Ticket 0 反射锁；包装用手填同名字段，禁止手搭 map。CLI 打印 `SkillSite`：T6 用真实 Install 返回值断言 Path/State/Note。`CapabilityName` 不进 Ticket.Kind。缺字段 vs 零值：`HomeDir` 空串非法（Prepare 已拒）。

**枚举白名单**：`Cap*`/`Harness*`/`EffortLow`/`Credential*`/`OneShotOK|Failed|Canceled`/`Skill*` 流经 Registry、OneShotArgv、Profile、CLI 状态打印、`supportedCLIs`（不得把 Harness* 全写进去）。

**承重安全**（必须能变红）：见 breakdown §4.2 表，全部指到 T1–T6 测试。

**webview**：无，因为无新事件类型、无前端安装入口。

### 10.2 序列化边界文件清单

| 投影 | 文件 | 断言 |
|---|---|---|
| grok argv | `internal/executor/grok/oneshot_argv.go` | Ticket 0 + T4 no-effort |
| 其余家 argv | 各家 `oneshot_argv.go` | T4 金样本 |
| Approver→Invoke | `approver.go` | T4 `TestDecideInvokesOneShotWithGrokEffortLow` |
| SkillSite CLI 打印 | `cmd/skill.go` | 既有打印循环 + T6 sites 字段 |
| Coord 字段 | `coordinator.go` 包装赋值 | Ticket 0 反射；包装逐字段复制 |
| settings.json allow | `claudecode/taskenv.go` | T2 读盘 |

### 10.3 上下文预算

每个 task 文件集已圈出。不插竖切。agentd 整包债已在 breakdown 回答。

### 10.4 类型标注

`d_execution` / `d_maintenance` / `d_gateway` hostapi = 边界型：行为真机清单在 T7。`d_orchestration` / `d_keystone` / `d_cli` = 逻辑型：机内可闭环。

### 10.5 接缝覆盖（双向）

spec 测试决定 6 条缝：

| 缝 | 锁它的测试入口 |
|---|---|
| 1 执行能力面 | T2 `Dispatch`/`ResumeTask`；T7 间谍 Start 计数 |
| 2 协调能力面 | T3 `coordinatorRunner.Launch` / `Coordinator.Launch` |
| 3 OneShot | T4 `Approver.Decide` |
| 4 Profile | T5 `Profile.Prepare` / `coordinatorHomeSupplier.Prepare` |
| 5 Skills | T6 `skill.Install`（生产签名，cmd 调用它） |
| 6 免审不放宽 | T2 `WriteTaskEnv` 读盘 |

测试→缝：上表入口均在缝上或穿过（Dispatch→Require→Start；Decide→Invoke；Install→Skills.Install）。
缝→测试：六条均有。
内部锁（附加，不能顶替）：

| 内部锁 | 理由（从声明缝构造不出） |
|---|---|
| 各家 `var _ Recoverer` / `var _ Provider` | 编译期满足性，无法从 Dispatch 构造 |
| `grok.OneShotArgv` 金样本 | 序列化边界编码函数；缝级由 Decide→Invoke 另锁 |
| `TestCLIOnPathIsNotFiveCapabilities` | 锁查询面矩阵，PATH 是外部现实 |

无「若意外先绿就改入口」的退路。

---

## 11. 自审三查

### spec 覆盖

| 故事 | task |
|---|---|
| 1 OpenCode 执行 | T2+T7 |
| 2 Grok 执行；cheap 在调用方 | T2+T4 |
| 3 AGY 执行 | T2+T7 真机 |
| 4 Codex 执行 + NativeLimits | T1+T2+T7 |
| 5 Claude 执行；协调不支持 | T2+T3 |
| 6 拉起协调者；Profile HOME | T3+T5 |
| 7 审批一次性五家 | T4 |
| 8 并发两任务 Overlay | T5+T7 真机 |
| 9 skill 安装 | T6 |

### 占位符扫描

无 TBD /「加适当错误处理」/「同 Task N」。

**声明的夹具例外**：T3 假 CLI 照抄 `internal/hostapi/runturn_test.go` 的 `installFakeCLI`；T4 stub 照抄 `newTestApprover`+`injectNonceForTest`/`extractNonceForTest`（`internal/agentd/approver_test.go`）；T5/T3 runner 测试照抄 `coordinator_home_test.go` 的 `installCoordinatorFakeCLI`。断言已逐条列全。

### 跨 task 签名

§2 逐字。`WriteTaskEnv` claude 新增 snap 与 OpenCode 已有 snap 同类型 `executor.PolicySnapshot`。`Install(content, home, providers []executor.Skills)` 是 T6 Produces，cmd Consumes。`BindOneShot(executor.OneShot)` T4 Produces，cmd Consumes。`RegistryFromAds` T1/T3 共用。

### 跨卡审计

L3 轻档无扇出子卡。冻结 1–134 与本计划引用一致：不另立 Executor；Recoverer 可选；Coordinator 在 executor；grok effort 在 Limits；hostapi 不搬；P1–P6 全 a。无兄弟卡 Produces/Consumes 要对齐。结论：单上下文自审，不代替重档跨卡拍板（本卡无扇出）。

---

## 12. 图覆盖债（本节点新记 + 继承）

- `copyCoordinatorRules` / `coordinatorHomeSupplier.Prepare` 短名不在图中。
- `who-calls n_hostapi_Host_RunTurn` 漏 `coordinatorRunner.Launch`。
- `flow n_hostapi_Host_RunTurn` 摘要「骨架期恒返回 ErrUnavailable」，与 `runTurn` 源码不符；steps=1 return 是过期流。
- `ApprovalClient` 基线/233.2 视图 check 2 fail；233.1 视图 fails=[]。本卡不把节点复制进 233.2 视图。
- `n_keysclient_Runner` 短名不命中（模型在 keysclient 包）。
- 基线容器 `k_executor_model` domain 仍 `d_executor`；best 归 `d_execution_contract`。本卡不修归属。
- `d_execution_adapters` 职责文案仍写四家、缺 agy。本卡不改结构树。
- `flow Approver.Decide` degraded。

---

本节点该干什么一律以注入的纪律块为准。
