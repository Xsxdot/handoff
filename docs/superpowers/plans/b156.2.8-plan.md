# B156.2.8 实现计划：C8 d_web·控制台房间面（欠账 #8 + A.6）

**状态**：plan 节点产出（charter 流），2026-08-26。
**有效基线分支**：`feature/b156-workbench`（合并目标以此为准）。本计划在 `cards/B156.2.8-charter` 上执行，HEAD=82b49cb2（C6+C7 已合入功能线，本地==远程已核，BASE_OK）。
**上游输入**：spec `2026-08-25-b156.2-collaboration-rooms-design.md`（已批准）；契约 `b156.2-contract.md`（含 §11 微修订）；breakdown `b156.2-breakdown.md`（C8 卡四段式 + 岔口九裁决 + 澄清一/三 + A.6）。
**台账**：`docs/superpowers/ledgers/ledger-b156.2.8-plan.md`（本计划所有探针/图闸证据在案）。

本卡做四件事（欠账 #8 全量 + A.6 + 澄清一展示面 + 岔口九图登记）：

1. **rooms.ts fetch 函数接线**：五个端点逐端点类型化（GET rooms / GET messages / POST messages / POST read / GET inbox），响应信封形状与 C6 冻结的 handler 逐字一致；
2. **三个房间面页面**：会话列表（扁平活动排序 + 项目筛选 + live/read_only 徽标 + 错误态非空列表）、房间页（历史分页 + 发送 POST + 只读禁写反面断言 + 打开即已读）、收件箱（三源分区 + 决策选项按钮直调 answerDecision + 答复后明示文案）；carrier/bound_session 不透明展示（澄清一）；轮询间隔默认值落常量（A.6）；
3. **Shell 路由 + 左栏 dock 入口**（spec §8.1 扩展点②，Shell path 先例）；
4. **图登记**：新页面容器归属 `d_web_command`（岔口九，视图 diff 级，零图结构改动）。

---

## 0. 冻结物核对（对照冻结物逐条）

| 冻结条目 | 本计划的对应 | 越界？ |
| --- | --- | --- |
| 契约 §3.5 HTTP 表五行消费侧（rooms/messages/send/read/inbox）+ 响应形状（C6 交接 §6：`{"rooms":[...]}`/`{"messages":[...]}`/`{"seq":N}`/`{"ok":true}`/`{"items":[...]}`） | T8.1 fetch 测试逐端点 + T8.1 实现 | 否 |
| 契约 §6 TS 孪生金样本（`web/src/api/testdata/RoomsFixture.json` 一字不动、`web/src/api/rooms.ts` 类型形状冻结） | T8.1 仅补 fetch 函数体，不动类型形状与 fixture；fetch 测试对同一夹具断言（澄清三） | 否 |
| 契约 §3.3「用户点击直接走既有 AnswerDecision 落 decision_answered，不新造第二答复通道」 | T8.2 InboxPage 复用 `answerDecision`（ledger.ts:304-305） | 否 |
| 契约 §3.3「答复已落账，等待协调者唤醒」明示文案（spec §3.3） | T8.2 InboxPage 答复后渲染 | 否 |
| 拍板 5.3「REST 轮询，本期不新增 WS」 | 三页面全走 `usePoll`（既有轮询原语，拍板 5.3 引用） | 否 |
| A.6「收件箱轮询间隔默认值落常量」 | T8.1/T8.2 `COLLAB_POLL_MS` 落 `web/src/app/rooms/constants.ts`，三页面共用；断言经 mock usePoll 锁 interval 参数 | 否（A.6 授权本卡） |
| 澄清一「carrier 不透明标识，展示层不做格式假设，空串=未登记」 | 展示面落在 `RoomSummary.bound_session` 原样展示（wire 无 carrier 字段，见 §0.1 D1） | 否（对账留痕） |
| 澄清三「C8 fetch 函数类型对同一夹具断言，链条经孪生夹具闭合，不做 vitest 直连 Go」 | T8.1 `rooms.fetch.test.ts` 复用 `testdata/RoomsFixture.json` 断言 fetchInbox/fetchRoomMessages 解码 | 否 |
| 已知陷阱一「jsdom 看不见布局」 | §7 真机清单分栏；组件断言只锁数据流与交互语义 | 否 |
| 已知陷阱二「只读房禁写是反面断言：真触发提交再断言无 POST fetch，且同表跑正控」 | T8.2 RoomDetailPage 只读禁写测试（fetch 边界断言 + 同表正控） | 否 |
| 已知陷阱四「错误态不是空列表」 | T8.2 RoomsListPage 断线横幅 ≠（空） | 否 |
| 岔口九「新页面容器归属 d_web_command（k_web_app_task/board/overlay 先例）」 | T8.4 best.json + 视图 diff containersAdded 登记 `k_web_app_rooms(_model) → d_web_command` | 否 |
| breakdown C8 ③「dock/路由入口可达（Shell path 先例）」 | T8.3 Shell 路由 + ProjectTree dock 按钮 | 否 |
| 已知事实「不存在的房间 → 200 空列表，不指望 404」 | T8.2 房间页对未知 id 渲染空历史不报错 | 否 |
| 契约 §8.0「存量直调不扩大」「Web 方向是 HTTP 边界不入图」 | C8 零 Go 改动、零新增直调；页面经既有 `request/postJSON` 底座 | 否 |

**上游状态位**：spec「已批准（用户，2026-08-25）」、contract 冻结声明、breakdown C8 卡四段式，均亲读核对一致；本卡无待拍板岔口（岔口九已裁出稿倾向、澄清一/三已生效）。

## 0.1 关键事实与裁决（亲读亲测，出处可复查）

| # | 事实 / 裁决 | 出处 |
|---|---|---|
| D1 | **wire 无 carrier 字段**：`driver_carrier` 只活在 ledger Store SQL 与 CLI rebind（grep 全仓非测试），proto.Card/RoomSummary/InboxItem 均无。澄清一「房间面原样展示」的展示面 = `RoomSummary.bound_session`（不透明标识，原样展示零解析）；carrier 本体本期无展示数据源，加字段要回 contract，不在本卡 | 台账 L14 |
| D2 | **RoomSummary 不在金样本**：RoomsFixture.json 只有 escalation/user 两 message case + 三 inbox case，无 RoomSummary case（Go `rooms_fixture_test.go` 同）。T8.1 fetch 测试用**内联** RoomSummary 样本（含 last_activity RFC3339 串直通断言），不动冻结 fixture | 台账 L16 |
| D3 | **项目筛选用客户端过滤**（CardsPage 同构）：单流全量轮询，select 过滤 `room.project===p \|\| room.id==="project:"+p`；`fetchRooms(project)` 参数仍在 fetch 层测试（端点契约保留） | 台账 D3 |
| D4 | **只读房禁写守卫在 handler 内**（`if (summary?.read_only) return`，且先于空正文判定）：发送按钮不因只读 disabled——已知陷阱二要求「真触发提交/点击再断言无 POST fetch」，disabled 会让点击到不了 handler、断言空转。输入框 disabled 仅作 UX | 台账 D4 |
| D5 | **房间页历史**：`usePoll` 只轮询最新一页（limit=200，升序），「加载更早」按 `before=最老seq-1` 手动叠加；「打开房间即已读」= 到新 maxSeq 置一次 `markRoomRead(id, maxSeq)` | 台账 D5 |
| D6 | **A.6 常量**：`COLLAB_POLL_MS = 5000` 落 `web/src/app/rooms/constants.ts`，三页面共用；断言经 mock `usePoll` 检查 interval 参数 == 常量 | 台账 D6 |
| D7 | **图登记**：best.json containers 加 `k_web_app_rooms`/`k_web_app_rooms_model → d_web_command`；视图 diff `containersAdded` 加两枚。web 容器图节点数为 0（前端不扫描成节点，L9），本卡零节点登记 | 台账 D7 |
| D8 | **web 工具链**：本工作树 `web/node_modules` 不存在（.gitignore 忽略），实现轮基线先 `npm ci`（web/），再 `npx tsc -b` / vitest | 台账 L24 |
| D9 | **图标**：`lucide-react ^1.31.0`；只挑仓库已证明存在的图标（grep 实采集合）——会话=MessageSquareText、收件箱=AlertTriangle | 台账 L23 |

**基线绿（plan 轮实测）**：`go run . graph check --repo . --view cards-B156.2-charter-4` → fails=[] EXIT=0（warns=96，不作判据）；`go run . graph validate --repo .` EXIT=0；`git status` 干净。web 侧 `npx tsc -b`/vitest 因无 node_modules 未实跑（D8），由实现轮 T8.1 步骤 1 建立基线。

---

## 1. 现状查证（全部亲读亲测，出处可复查）

| # | 事实 | 出处 |
|---|---|---|
| 1 | C6 六端点已注册进 registerLedgerRoutes 同一 mux；响应信封 `{"rooms":[...]}`/`{"messages":[...]}`/`{"seq":N}`/`{"ok":true}`/`{"items":[...]}`；POST messages 只收 body/refs/mentions，kind 服务端固定 user、actor 服务端注入 | `internal/agentd/ledgerapi.go:49-54`、`internal/agentd/roomsapi.go:47-203` |
| 2 | History 的 ErrNoRoom→404 特例今天不可达：C4 合规 History 对不存在房间返回 200 空列表 | `roomsapi.go:57-61`（注释自证 + 台账） |
| 3 | `rooms.ts` 类型镜像全量冻结（RoomMsgKind/RoomMessage/InboxItem/RoomSummary/RoomHistoryItem=LedgerEvent），**无 fetch 函数**（文件尾注释「fetch 函数随实现节点接线」） | `web/src/api/rooms.ts:63` |
| 4 | `answerDecision(id:number, answer:string)` → POST /api/decisions/{id}/answer；既有答复通道，收件箱复用不新造 | `web/src/api/ledger.ts:304-305` |
| 5 | `usePoll<T>(fetcher, intervalMs, opts?)` 既有轮询原语（document.hidden 停表/断线留数据/401 终止）；`usePoll.test.ts` fake timers 先例 | `web/src/app/data/usePoll.ts:27-31` |
| 6 | 页面先例：CardsPage 页内 `const POLL_MS = 2500` + `usePoll(() => fetchCards(...), POLL_MS)` + 项目 `<select>` 客户端筛选；测试用 `vi.mock('../../api/ledger')` + MemoryRouter | `web/src/app/cards/CardsPage.tsx:15,78`、`CardsPage.test.tsx` |
| 7 | 测试 fetch 桩先例：`vi.stubGlobal('fetch', vi.fn().mockResolvedValue(new Response(...)))` | `web/src/api/client.test.ts:21-41` |
| 8 | Shell：ledgerEnabled 门控 `/cards`、`/flows`（:481-486）；`fullPageRoute` 数组 :391；ProjectTree dock props → navigate | `web/src/app/shell/Shell.tsx` |
| 9 | ProjectTree 底部 dock 段（图标按钮）：工作项/流程 ledgerEnabled 门控、看板/工单/设置常驻；lucide 图标 import 行 :35-37 | `web/src/app/tree/ProjectTree.tsx:757-836` |
| 10 | 图：best.json containers 260 个，`k_web_app_board/overlay/task → d_web_command`（叶子域存在）；baseline.json web 容器节点数为 0；charter/graph@v0.8.0 `merge.go:84` ContainersAdded 合并、`best.go:127-133` 只校验容器→叶子域引用不校验图内存在 | 台账 L8/L9/L10 |
| 11 | `formatRelative(iso, now)` RFC3339 → 相对时间（房间页/列表页时间展示） | `web/src/app/lib/format.ts:33` |
| 12 | 测试环境：jsdom + `@testing-library/react` + `vitest ^4`（`expect.poll` 可用）；`web/src/test/setup.ts` 接 jest-dom + 自动 cleanup | `web/vite.config.ts:39-40`、`web/src/test/setup.ts` |

---

## 2. 有界文件集（上下文预算检查）

| 文件 | 动作 | 归属 task |
|---|---|---|
| `web/src/api/rooms.ts` | 追加 fetch 五函数 + import `request`/`postJSON`（类型形状一字不动） | T8.1 |
| `web/src/api/rooms.fetch.test.ts` | **新建**：五端点 URL/方法/请求体断言 + 对 RoomsFixture.json 的解码断言（澄清三） | T8.1 |
| `web/src/app/rooms/constants.ts` | **新建**：`COLLAB_POLL_MS = 5000`（A.6） | T8.1 |
| `web/src/app/rooms/RoomsListPage.tsx` | **新建**：会话列表页 | T8.2 |
| `web/src/app/rooms/RoomsListPage.test.tsx` | **新建**：排序/徽标/筛选/错误态/空态/导航/bound_session 不透明 | T8.2 |
| `web/src/app/rooms/RoomDetailPage.tsx` | **新建**：房间页（分页/发送/只读禁写/已读） | T8.2 |
| `web/src/app/rooms/RoomDetailPage.test.tsx` | **新建**：fetch 边界断言（含只读禁写反面 + 正控） | T8.2 |
| `web/src/app/rooms/InboxPage.tsx` | **新建**：收件箱页（三源 + answerDecision） | T8.2 |
| `web/src/app/rooms/InboxPage.test.tsx` | **新建**：三源分区/选项按钮/答复文案 | T8.2 |
| `web/src/app/rooms/pollInterval.test.tsx` | **新建**：A.6 三页面轮询间隔 == 常量 | T8.2 |
| `web/src/app/shell/Shell.tsx` | 加 `/rooms`、`/rooms/:id`、`/inbox` 路由（ledgerEnabled 门控）+ fullPageRoute 两路径 + ProjectTree props 两行 | T8.3 |
| `web/src/app/tree/ProjectTree.tsx` | 加 `onOpenRooms`/`onOpenInbox` props + dock 两按钮 + lucide import 两图标 | T8.3 |
| `web/src/app/shell/Shell.test.tsx` | 追加路由挂载 + dock 入口 seam 断言（mock `../../api/rooms`） | T8.3 |
| `web/src/app/tree/ProjectTree.test.tsx` | 追加 dock 按钮回调断言（内部锁，§7 声明） | T8.3 |
| `codegraph/best.json` | containers 加 `k_web_app_rooms`/`k_web_app_rooms_model → d_web_command` | T8.4 |
| `codegraph/diffs/cards-B156.2-charter-4.json` | containersAdded 加两枚（label/kind/domain 按 web 容器惯例）；**沿用既有视图文件，不另起新视图** | T8.4 |

圈得出：5 个新代码文件 + 6 个新测试文件 + 2 个既有文件改动 + 2 个图数据文件，全在 d_web 域内（k_web_api_rooms/k_web_api_rooms_model/d_web_command/d_web_contract），**零 Go 改动、零跨域边、零节点登记**（web 侧不扫描成图节点，L9）。测试范围最小化：只跑新增测试文件 + `npx tsc -b` + 既有 `rooms.test.ts`/`Shell.test.tsx`/`ProjectTree.test.tsx` 回归（vitest 按文件过滤）。全量 vitest 不属于本卡（T8.5 收尾全量一次）。

---

## 3. Task 分解

**最薄路径条**：接缝 #5（k_web_api_rooms）今天只有类型镜像、无函数体——从缝上调用 `fetchRooms` 等拿不到任何行为，测试写下去必红。因此 DAG 第一条 T8.1 点亮最薄可跑路径（fetch 五函数 + 对金样本的解码断言），T8.2 页面、T8.3 路由、T8.4 图登记依次依赖它，无「写下去就会绿」的前置。

### T8.1 立红立绿：rooms.ts fetch 五端点 + COLLAB_POLL_MS 常量（锁缝断言步骤，法定红绿）

**Interfaces**

- Produces（本卡锁定，全部消费 C6 冻结的 HTTP 端点；调用链穿过 spec 测试接缝清单 #5——Web 侧 api client `k_web_api_rooms`）：
  - `fetchRooms(project?: string): Promise<RoomSummary[]>`
  - `fetchRoomMessages(id: string, opts?: { before?: number; limit?: number }): Promise<RoomHistoryItem[]>`
  - `sendRoomMessage(id: string, body: string, opts?: { refs?: string[]; mentions?: string[] }): Promise<{ seq: number }>`
  - `markRoomRead(id: string, uptoSeq: number): Promise<{ ok: boolean }>`
  - `fetchInbox(): Promise<InboxItem[]>`
  - `COLLAB_POLL_MS: number`（`web/src/app/rooms/constants.ts`）
- Consumes（既有，精确 wire 形状，见 §1 #1）：GET `/api/rooms?project=` → `{rooms: RoomSummary[]}`；GET `/api/rooms/{id}/messages?before=&limit=` → `{messages: LedgerEvent[]}`；POST `/api/rooms/{id}/messages` body `{body,refs?,mentions?}` → `{seq: number}`；POST `/api/rooms/{id}/read` body `{upto_seq}` → `{ok: boolean}`；GET `/api/inbox` → `{items: InboxItem[]}`。

**步骤**

1. **判据先在基线跑**（动手前，一次性）：
   - `cd web && npm ci`（D8：本工作树无 node_modules）。
   - `npx tsc -b` 预期 EXIT=0（基线绿）；`npx vitest run src/api/rooms.test.ts` 预期全绿（金样本既有测试）。
   - 若基线不绿，停下如实记录原始输出，不进入下一步。
2. **写失败测试** `web/src/api/rooms.fetch.test.ts`（全文见代码块 A）——`fetchRooms` 等导出尚不存在 → `tsc`/vitest 红。断言逐条：五端点 URL/方法/请求体；`fetchInbox`/`fetchRoomMessages` 对 `testdata/RoomsFixture.json` 的解码（澄清三）；RoomSummary 内联样本（D2：last_activity 串直通、bound_session 缺席不出键、live/read_only 布尔）。跑红：`npx vitest run src/api/rooms.fetch.test.ts` 报「不存在导出」类错误（原文留存）。
3. **最小实现**：
   - `web/src/api/rooms.ts` 追加 fetch 五函数（代码块 B）；**类型形状一字不动**。
   - `web/src/app/rooms/constants.ts` 新建（代码块 C，A.6）。
4. **跑绿**：`npx vitest run src/api/rooms.fetch.test.ts` 全绿；`npx tsc -b` EXIT=0。
5. **加关键节点日志**（web 侧 console.debug，先例 CardsPage:156）：`sendRoomMessage`/`markRoomRead` 失败路径在调用方（T8.2 页面）console.debug 带上下文；本 task 的 fetch 函数不加日志（纯传输层，失败由 usePoll/调用方呈现）。
6. **加注释**：rooms.ts 新增函数段头注释（端点 + 响应形状出处）；constants.ts 文件头注释（A.6 出处 + 为什么是三页面共用常量）。
7. **提交** commit 1（见 §11）。

**代码块 A——`web/src/api/rooms.fetch.test.ts`（全文）**：

```ts
// rooms.ts fetch 函数的端点契约测试（B156.2 C8）。
//
// 逐端点断言 URL/方法/请求体形状；并复用 testdata/RoomsFixture.json（契约 §6
// 金样本）做解码断言——澄清三：C6 httptest 全链一发 + 本文件对同一夹具断言，
// 链条经孪生夹具闭合。RoomSummary 不在金样本（台账 D2），用内联样本断言。
import { afterEach, describe, expect, it, vi } from 'vitest'
import {
  fetchInbox,
  fetchRoomMessages,
  fetchRooms,
  markRoomRead,
  sendRoomMessage,
} from './rooms'
import fixture from './testdata/RoomsFixture.json'

const cases = fixture as {
  case: string
  message?: { room: string; kind: string; body: string; refs?: string[]; mentions?: string[]; decision_id?: number }
  item?: { origin: string; title: string; card_id?: string; ref_id: string; payload?: unknown }
}[]

function jsonResp(body: unknown): Response {
  return new Response(JSON.stringify(body), {
    status: 200,
    headers: { 'Content-Type': 'application/json' },
  })
}

const roomsFixture: unknown = [
  {
    id: 'B1',
    kind: 'card',
    project: 'handoff',
    title: 'B1 卡会话',
    bound_session: 'console:alice@box',
    live: true,
    read_only: false,
    last_activity: '2026-08-26T08:00:00.123456789+08:00',
  },
  {
    id: 'project:handoff',
    kind: 'project',
    title: 'handoff 项目群',
    live: false,
    read_only: false,
    last_activity: '2026-08-26T07:00:00+08:00',
  },
]

afterEach(() => vi.unstubAllGlobals())

describe('fetchRooms', () => {
  it('GET /api/rooms，project 参数走查询串，解包 rooms 数组', async () => {
    const fetchMock = vi.fn().mockResolvedValue(jsonResp({ rooms: roomsFixture }))
    vi.stubGlobal('fetch', fetchMock)
    const rooms = await fetchRooms()
    expect(fetchMock.mock.calls[0][0]).toBe('/api/rooms')
    expect(rooms).toHaveLength(2)
    // last_activity 原样直通（time.Time RFC3339Nano → TS string），不做格式转换
    expect(rooms[0].last_activity).toBe('2026-08-26T08:00:00.123456789+08:00')
    expect(rooms[0].live).toBe(true)
    expect(rooms[0].read_only).toBe(false)
    expect(rooms[0].bound_session).toBe('console:alice@box')

    await fetchRooms('handoff')
    expect(fetchMock.mock.calls[1][0]).toBe('/api/rooms?project=handoff')
  })

  it('bound_session 缺席不出键：解码为 undefined 而非空串（可空 vs 零值）', async () => {
    const fetchMock = vi.fn().mockResolvedValue(
      jsonResp({ rooms: [{ id: 'global', kind: 'global', title: '全员群', live: false, read_only: false, last_activity: 'x' }] }),
    )
    vi.stubGlobal('fetch', fetchMock)
    const [room] = await fetchRooms()
    expect(room.bound_session).toBeUndefined()
  })
})

describe('fetchRoomMessages', () => {
  it('GET /api/rooms/{id}/messages，before/limit 走查询串', async () => {
    const fetchMock = vi.fn().mockResolvedValue(jsonResp({ messages: [] }))
    vi.stubGlobal('fetch', fetchMock)
    await fetchRoomMessages('B1', { before: 10, limit: 200 })
    expect(fetchMock.mock.calls[0][0]).toBe('/api/rooms/B1/messages?before=10&limit=200')
  })

  it('解码金样本 message 为 LedgerEvent 载荷（澄清三：对同一夹具断言）', async () => {
    const escalation = cases.find((c) => c.case === 'escalation-full')!.message!
    const events = [
      { seq: 7, card_id: 'B156', type: 'room_message', actor: 'cli:me@box', payload: escalation, created_at: '2026-08-25T10:00:00+08:00' },
    ]
    const fetchMock = vi.fn().mockResolvedValue(jsonResp({ messages: events }))
    vi.stubGlobal('fetch', fetchMock)
    const messages = await fetchRoomMessages('B156')
    const payload = messages[0].payload as { kind: string; refs?: string[]; decision_id?: number }
    expect(messages[0].seq).toBe(7)
    expect(payload.kind).toBe('escalation')
    expect(payload.refs).toHaveLength(2)
    expect(payload.decision_id).toBe(7)
  })
})

describe('sendRoomMessage', () => {
  it('POST /api/rooms/{id}/messages，body 形状 body/mentions（refs 空不出现）', async () => {
    const fetchMock = vi.fn().mockResolvedValue(jsonResp({ seq: 99 }))
    vi.stubGlobal('fetch', fetchMock)
    await sendRoomMessage('B1', '先停一下', { mentions: ['user:sy'] })
    const [url, init] = fetchMock.mock.calls[0] as [string, RequestInit]
    expect(url).toBe('/api/rooms/B1/messages')
    expect(init.method).toBe('POST')
    expect(JSON.parse(init.body as string)).toEqual({ body: '先停一下', mentions: ['user:sy'] })
  })
})

describe('markRoomRead', () => {
  it('POST /api/rooms/{id}/read，body {upto_seq}', async () => {
    const fetchMock = vi.fn().mockResolvedValue(jsonResp({ ok: true }))
    vi.stubGlobal('fetch', fetchMock)
    await markRoomRead('B1', 42)
    const [url, init] = fetchMock.mock.calls[0] as [string, RequestInit]
    expect(url).toBe('/api/rooms/B1/read')
    expect(init.method).toBe('POST')
    expect(JSON.parse(init.body as string)).toEqual({ upto_seq: 42 })
  })
})

describe('fetchInbox', () => {
  it('GET /api/inbox，解码金样本三源（澄清三）', async () => {
    const items = cases.filter((c) => c.item !== undefined).map((c) => c.item!)
    const fetchMock = vi.fn().mockResolvedValue(jsonResp({ items }))
    vi.stubGlobal('fetch', fetchMock)
    const inbox = await fetchInbox()
    expect(inbox.map((i) => i.origin)).toEqual(['decision', 'ticket', 'mention'])
    const decision = inbox.find((i) => i.origin === 'decision')!
    expect(decision.card_id).toBe('B156')
    expect(decision.ref_id).toBe('7')
    expect(decision.payload).toBeDefined()
    const ticket = inbox.find((i) => i.origin === 'ticket')!
    expect(ticket.card_id).toBeUndefined()
  })
})
```

**代码块 B——`web/src/api/rooms.ts` 追加段（全文，替换文件尾注释行）**：

```ts
// fetch 函数随实现节点接线（契约交棒欠账 #9）；本提交只冻结类型镜像。
// ---- C8 接线（契约 §3.5 端点 + §3.6 收件箱；响应信封形状与 C6 handler 逐字一致）----
import { postJSON, request } from './client'

// fetchRooms 会话列表（GET /api/rooms?project=）。project 省略取全部。
export const fetchRooms = (project = ''): Promise<RoomSummary[]> =>
  request<{ rooms: RoomSummary[] }>(
    `/api/rooms${project ? `?project=${encodeURIComponent(project)}` : ''}`,
  ).then((response) => response.rooms ?? [])

// fetchRoomMessages 房间历史（GET /api/rooms/{id}/messages）。before 排他游标、
// limit<=0 由服务端取 200；返回升序 room_message 事件。
export const fetchRoomMessages = (
  id: string,
  opts: { before?: number; limit?: number } = {},
): Promise<RoomHistoryItem[]> => {
  const q = new URLSearchParams()
  if (opts.before !== undefined) q.set('before', String(opts.before))
  if (opts.limit !== undefined) q.set('limit', String(opts.limit))
  const qs = q.toString()
  return request<{ messages: RoomHistoryItem[] }>(
    `/api/rooms/${encodeURIComponent(id)}/messages${qs ? `?${qs}` : ''}`,
  ).then((response) => response.messages ?? [])
}

// sendRoomMessage 用户发言（POST /api/rooms/{id}/messages）。kind 服务端固定 user、
// actor 服务端注入；refs/mentions 为空时不出键（与 Go 侧 omitempty 一致）。
export const sendRoomMessage = (
  id: string,
  body: string,
  opts: { refs?: string[]; mentions?: string[] } = {},
): Promise<{ seq: number }> => {
  const payload: { body: string; refs?: string[]; mentions?: string[] } = { body }
  if (opts.refs !== undefined && opts.refs.length > 0) payload.refs = opts.refs
  if (opts.mentions !== undefined && opts.mentions.length > 0) payload.mentions = opts.mentions
  return postJSON<{ seq: number }>(`/api/rooms/${encodeURIComponent(id)}/messages`, payload)
}

// markRoomRead 置已读游标（POST /api/rooms/{id}/read）：打开房间即已读（spec §7）。
export const markRoomRead = (id: string, uptoSeq: number): Promise<{ ok: boolean }> =>
  postJSON<{ ok: boolean }>(`/api/rooms/${encodeURIComponent(id)}/read`, { upto_seq: uptoSeq })

// fetchInbox 待回复收件箱（GET /api/inbox）：三源聚合在 gateway 编排（契约 §3.6）。
export const fetchInbox = (): Promise<InboxItem[]> =>
  request<{ items: InboxItem[] }>('/api/inbox').then((response) => response.items ?? [])
```

**代码块 C——`web/src/app/rooms/constants.ts`（全文）**：

```ts
// 房间读面的轮询间隔默认值（拍板 5.3：REST 轮询，本期不新增 WS）。
// A.6 要求默认值落成具名常量：会话列表/房间页/收件箱三个页面共用同一常量，
// 不在组件里散写魔数。单人使用下秒级延迟无感（spec 拍板 5.3 附注）。
export const COLLAB_POLL_MS = 5000
```

**测试范围声明（最小化）**：本 task 只跑 `npx vitest run src/api/rooms.fetch.test.ts` + `src/api/rooms.test.ts`（金样本回归）+ `npx tsc -b`。不跑全量。

---

### T8.2 立红立绿：三个页面 + 页面测试（锁缝断言步骤，法定红绿）

**Interfaces**

- Produces（组件，调用链穿过接缝 #5）：
  - `RoomsListPage`：会话列表页（/rooms 路由挂载）
  - `RoomDetailPage`：房间页（/rooms/:id，`useParams<{id}>`）
  - `InboxPage`：收件箱页（/inbox）
  - 内部数据流：三页面 `usePoll(fetchRooms | fetchRoomMessages | fetchInbox, COLLAB_POLL_MS)`；房间页 `markRoomRead`/`sendRoomMessage`；收件箱 `answerDecision`
- Consumes：T8.1 的五个 fetch 函数 + `COLLAB_POLL_MS` + `answerDecision`（ledger.ts:304）+ `formatRelative`（lib/format.ts:33）。

**步骤**

1. **判据先在基线跑**：`npx vitest run src/api/rooms.fetch.test.ts`（T8.1 绿）确认上游绿；`npx tsc -b` EXIT=0。
2. **写失败测试**（四个测试文件全文见代码块 D–G）：页面组件尚不存在 → import 报错/红。断言逐条：
   - RoomsListPage：渲染排序（服务端序直出）、live/read_only 徽标、bound_session 原样展示（澄清一）、项目筛选、错误态非（空）、空列表、点击行导航到 /rooms/:id；
   - RoomDetailPage：最新页渲染 + 「加载更早」按 `before=最老seq-1` 请求并叠加、打开即已读 `markRoomRead(id,maxSeq)`、可写房发送 POST messages 且清空输入、**只读房禁写（反面：真触发发送点击后无 POST messages 的 fetch）+ 同表正控（read_only=false 同一交互必打 POST）**；
   - InboxPage：三源分区渲染、决策选项按钮点击调 `answerDecision(id, option)`、答复后「答复已落账，等待协调者唤醒」文案出现；
   - pollInterval：三页面 `usePoll` 的 interval 参数 == `COLLAB_POLL_MS`（A.6，mock usePoll）。
   跑红：`npx vitest run src/app/rooms` 报「模块不存在」类错误（原文留存）。
3. **最小实现**：`constants.ts` 已建（T8.1）；新建 `RoomsListPage.tsx`、`RoomDetailPage.tsx`、`InboxPage.tsx`（全文见代码块 H–J）。页面间不共享除常量外状态。
4. **跑绿**：`npx vitest run src/app/rooms` 全绿；`npx tsc -b` EXIT=0。
5. **加关键节点日志**（console.debug，先例 CardsPage:156）：房间页「加载更早」失败、发送失败；收件箱答复失败。成功路径不静默加日志（不制造心跳日志——spec §5 卡会话默认静默）。
6. **加注释**：每个新文件头写职责+边界（见代码块 H–J 头部）；`send` 守卫顺序（只读先于空正文）写「为什么」（D4）。
7. **提交** commit 2（见 §11）。

**代码块 D——`web/src/app/rooms/RoomsListPage.test.tsx`（全文）**：

```tsx
// 会话列表页的数据流与交互语义断言（B156.2 C8）。
// jsdom 看不见布局（已知陷阱一）：断言只锁数据流/交互/文案，布局进真机清单。
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { fireEvent, render, screen } from '@testing-library/react'
import { MemoryRouter, Route, Routes } from 'react-router-dom'
import { fetchRooms } from '../../api/rooms'
import type { RoomSummary } from '../../api/rooms'
import { RoomsListPage } from './RoomsListPage'

vi.mock('../../api/rooms', async (importOriginal) => ({
  ...(await importOriginal<typeof import('../../api/rooms')>()),
  fetchRooms: vi.fn(),
}))

const room = (over: Partial<RoomSummary> = {}): RoomSummary => ({
  id: 'B1', kind: 'card', title: 'B1 卡会话', live: false, read_only: false,
  last_activity: '2026-08-26T08:00:00+08:00', ...over,
})

const renderPage = () =>
  render(
    <MemoryRouter initialEntries={['/rooms']}>
      <Routes>
        <Route path="/rooms" element={<RoomsListPage />} />
        <Route path="/rooms/:id" element={<p>room-hit</p>} />
      </Routes>
    </MemoryRouter>,
  )

describe('会话列表页', () => {
  beforeEach(() => vi.mocked(fetchRooms).mockResolvedValue([]))

  it('渲染服务端给出的顺序（LastActivity 降序由服务端保证，页面不二次排序）', async () => {
    vi.mocked(fetchRooms).mockResolvedValue([
      room({ id: 'B2', title: '较新房间' }),
      room({ id: 'B1', title: '较旧房间' }),
    ])
    renderPage()
    await screen.findByText('较新房间')
    const list = screen.getByTestId('room-list')
    expect(list.textContent!.indexOf('较新房间')).toBeLessThan(list.textContent!.indexOf('较旧房间'))
  })

  it('live/read_only 徽标', async () => {
    vi.mocked(fetchRooms).mockResolvedValue([room({ live: true, read_only: true })])
    renderPage()
    await screen.findByText('B1 卡会话')
    expect(screen.getByText('在线')).toBeInTheDocument()
    expect(screen.getByText('只读')).toBeInTheDocument()
  })

  it('bound_session 原样展示，不做格式假设（澄清一）', async () => {
    const opaque = 'console:alice@box/with spaces::weird'
    vi.mocked(fetchRooms).mockResolvedValue([room({ bound_session: opaque })])
    renderPage()
    expect(await screen.findByText(opaque)).toBeInTheDocument()
  })

  it('项目筛选：卡房间按 project、项目群按 project:<name>，其余隐藏', async () => {
    vi.mocked(fetchRooms).mockResolvedValue([
      room({ id: 'B1', project: 'handoff', title: 'handoff 卡' }),
      room({ id: 'B2', project: 'benchmarking', title: 'benchmarking 卡' }),
      { id: 'project:handoff', kind: 'project', title: 'handoff 项目群', live: false, read_only: false, last_activity: 'x' },
    ])
    renderPage()
    await screen.findByText('handoff 卡')
    fireEvent.change(screen.getByLabelText('项目'), { target: { value: 'handoff' } })
    expect(screen.getByText('handoff 项目群')).toBeInTheDocument()
    expect(screen.queryByText('benchmarking 卡')).not.toBeInTheDocument()
  })

  it('错误态不是空列表：数据源失败显示错误横幅，不渲染（空）', async () => {
    vi.mocked(fetchRooms).mockRejectedValue(new Error('账本库未配置'))
    renderPage()
    expect(await screen.findByText(/会话列表加载失败/)).toBeInTheDocument()
    expect(screen.queryByText('（空）')).not.toBeInTheDocument()
  })

  it('空列表渲染（空），不显示错误', async () => {
    renderPage()
    expect(await screen.findByText('（空）')).toBeInTheDocument()
    expect(screen.queryByText(/会话列表加载失败/)).not.toBeInTheDocument()
  })

  it('点击行导航到 /rooms/:id', async () => {
    vi.mocked(fetchRooms).mockResolvedValue([room({ id: 'B1' })])
    renderPage()
    fireEvent.click(await screen.findByText('B1 卡会话'))
    expect(await screen.findByText('room-hit')).toBeInTheDocument()
  })
})
```

**代码块 E——`web/src/app/rooms/RoomDetailPage.test.tsx`（全文）**：

```tsx
// 房间页：历史分页（before 游标）+ 发送 POST + 只读禁写（已知陷阱二：disabled 不算数，
// 必须真触发提交/点击再断言没有发出 POST /api/rooms/{id}/messages 的 fetch）+ 同表正控。
// 本文件在 fetch 边界断言（stubGlobal fetch），不 mock rooms 模块。
import { describe, expect, it, vi } from 'vitest'
import { fireEvent, render, screen, waitFor } from '@testing-library/react'
import { MemoryRouter, Route, Routes } from 'react-router-dom'
import type { LedgerEvent } from '../../api/ledger'
import type { RoomHistoryItem, RoomSummary } from '../../api/rooms'
import { RoomDetailPage } from './RoomDetailPage'

function jsonResp(body: unknown): Response {
  return new Response(JSON.stringify(body), { status: 200, headers: { 'Content-Type': 'application/json' } })
}

const roomB1 = (over: Partial<RoomSummary> = {}): RoomSummary => ({
  id: 'B1', kind: 'card', title: 'B1 卡会话', live: false, read_only: false,
  last_activity: '2026-08-26T08:00:00+08:00', ...over,
})

const ev = (seq: number, body: string, kind = 'user'): RoomHistoryItem =>
  ({ seq, card_id: 'B1', type: 'room_message', actor: 'cli:me@box', payload: { room: 'B1', kind, body }, created_at: '2026-08-26T08:00:00+08:00' }) as unknown as LedgerEvent as RoomHistoryItem

// fetchStub 挡下房间页全部请求；older 在 before 查询时返回。
function fetchStub(rooms: RoomSummary[], messages: RoomHistoryItem[], older: RoomHistoryItem[] = []) {
  const fn = vi.fn((input: RequestInfo | URL, init?: RequestInit) => {
    const url = String(input)
    const method = init?.method ?? 'GET'
    if (url === '/api/rooms' && method === 'GET') return Promise.resolve(jsonResp({ rooms }))
    if (url.startsWith('/api/rooms/B1/messages') && method === 'GET') {
      return Promise.resolve(jsonResp({ messages: url.includes('before=') ? older : messages }))
    }
    if (url === '/api/rooms/B1/messages' && method === 'POST') return Promise.resolve(jsonResp({ seq: 99 }))
    if (url === '/api/rooms/B1/read' && method === 'POST') return Promise.resolve(jsonResp({ ok: true }))
    return Promise.resolve(jsonResp({}))
  })
  vi.stubGlobal('fetch', fn)
  return fn
}

const postMessages = (fn: ReturnType<typeof vi.fn>) =>
  fn.mock.calls.filter(([input, init]) => {
    const method = (init as RequestInit | undefined)?.method ?? 'GET'
    return String(input) === '/api/rooms/B1/messages' && method === 'POST'
  })

const readPosts = (fn: ReturnType<typeof vi.fn>) =>
  fn.mock.calls.filter(([input, init]) => {
    const method = (init as RequestInit | undefined)?.method ?? 'GET'
    return String(input) === '/api/rooms/B1/read' && method === 'POST'
  })

const renderPage = () =>
  render(
    <MemoryRouter initialEntries={['/rooms/B1']}>
      <Routes>
        <Route path="/rooms/:id" element={<RoomDetailPage />} />
      </Routes>
    </MemoryRouter>,
  )

describe('房间页历史', () => {
  it('渲染最新一页，「加载更早」按 before=最老seq-1 请求并叠加', async () => {
    const fn = fetchStub([roomB1()], [ev(1, '第一条'), ev(2, '第二条')], [ev(0, '更早一条')])
    renderPage()
    expect(await screen.findByText('第一条')).toBeInTheDocument()
    expect(screen.getByText('第二条')).toBeInTheDocument()
    fireEvent.click(screen.getByRole('button', { name: '加载更早' }))
    await waitFor(() => expect(screen.getByText('更早一条')).toBeInTheDocument())
    const older = fn.mock.calls.find(([input]) => String(input).includes('before=0')) as [string, RequestInit] | undefined
    expect(older).toBeDefined()
  })

  it('打开房间即已读：到新 maxSeq 置一次 markRoomRead(id, maxSeq)', async () => {
    const fn = fetchStub([roomB1()], [ev(1, 'a'), ev(2, 'b')])
    renderPage()
    await screen.findByText('a')
    await waitFor(() => expect(readPosts(fn)).toHaveLength(1))
    expect(JSON.parse(String((readPosts(fn)[0][1] as RequestInit).body))).toEqual({ upto_seq: 2 })
  })
})

describe('房间页发送', () => {
  it('可写房（read_only=false）：发送框 POST messages 且清空输入', async () => {
    const fn = fetchStub([roomB1()], [ev(1, 'hi')])
    renderPage()
    await screen.findByText('hi')
    fireEvent.change(screen.getByLabelText('消息正文'), { target: { value: '新的留言' } })
    fireEvent.click(screen.getByRole('button', { name: '发送' }))
    await waitFor(() => expect(postMessages(fn)).toHaveLength(1))
    expect(JSON.parse(String((postMessages(fn)[0][1] as RequestInit).body))).toEqual({ body: '新的留言' })
    await waitFor(() => expect((screen.getByLabelText('消息正文') as HTMLTextAreaElement).value).toBe(''))
  })

  it('只读房禁写（反面断言）：真触发发送点击，没有 POST messages 的 fetch', async () => {
    const fn = fetchStub([roomB1({ read_only: true })], [ev(1, '已归档')])
    renderPage()
    await screen.findByText('已归档')
    fireEvent.click(screen.getByRole('button', { name: '发送' }))
    // 轮询 500ms 断言仍是 0：若写路径被接上 fetch，这里必然翻红
    await expect.poll(() => postMessages(fn).length).toBe(0)
  })

  it('同表正控：同一交互在 read_only=false 房间必须打出 POST messages', async () => {
    const fn = fetchStub([roomB1({ read_only: false })], [ev(1, '正常')])
    renderPage()
    await screen.findByText('正常')
    fireEvent.change(screen.getByLabelText('消息正文'), { target: { value: 'x' } })
    fireEvent.click(screen.getByRole('button', { name: '发送' }))
    await waitFor(() => expect(postMessages(fn)).toHaveLength(1))
  })
})
```

**代码块 F——`web/src/app/rooms/InboxPage.test.tsx`（全文）**：

```tsx
// 收件箱页：三源分区 + 决策选项按钮直调既有 answerDecision + 答复后明示文案。
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { fireEvent, render, screen, waitFor } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { answerDecision } from '../../api/ledger'
import { fetchInbox } from '../../api/rooms'
import type { InboxItem } from '../../api/rooms'
import { InboxPage } from './InboxPage'

vi.mock('../../api/rooms', async (importOriginal) => ({
  ...(await importOriginal<typeof import('../../api/rooms')>()),
  fetchInbox: vi.fn(),
}))
vi.mock('../../api/ledger', async (importOriginal) => ({
  ...(await importOriginal<typeof import('../../api/ledger')>()),
  answerDecision: vi.fn().mockResolvedValue({ ok: true }),
}))

const items: InboxItem[] = [
  {
    origin: 'decision', title: '推翻级简报：契约语义冲突', card_id: 'B156', ref_id: '7',
    payload: { id: 7, card_id: 'B156', body: '要不要先退化？', options: ['重试三次', '立即退化'], status: 'open', created_by: 'cli:me@box' },
  },
  { origin: 'ticket', title: '权限工单待答复', ref_id: 'tkt-42' },
  { origin: 'mention', title: '@你：改动影响 B145', card_id: 'B145', ref_id: '918' },
]

describe('收件箱页', () => {
  beforeEach(() => vi.mocked(fetchInbox).mockResolvedValue(items))

  it('三源分区渲染', async () => {
    render(<MemoryRouter><InboxPage /></MemoryRouter>)
    expect(await screen.findByText('要不要先退化？')).toBeInTheDocument()
    expect(screen.getByText('权限工单待答复')).toBeInTheDocument()
    expect(screen.getByText('@你：改动影响 B145')).toBeInTheDocument()
    expect(screen.getByText('需要你 · 简报')).toBeInTheDocument()
    expect(screen.getByText('兜底上浮工单')).toBeInTheDocument()
  })

  it('决策选项按钮点击直调 answerDecision（既有通道，不新造）', async () => {
    render(<MemoryRouter><InboxPage /></MemoryRouter>)
    fireEvent.click(await screen.findByRole('button', { name: '立即退化' }))
    await waitFor(() => expect(vi.mocked(answerDecision)).toHaveBeenCalledWith(7, '立即退化'))
  })

  it('答复后显示「答复已落账，等待协调者唤醒」', async () => {
    render(<MemoryRouter><InboxPage /></MemoryRouter>)
    fireEvent.click(await screen.findByRole('button', { name: '重试三次' }))
    expect(await screen.findByText('答复已落账，等待协调者唤醒。')).toBeInTheDocument()
  })
})
```

**代码块 G——`web/src/app/rooms/pollInterval.test.tsx`（全文）**：

```tsx
// A.6 轮询间隔默认值落常量：三个房间面页面把 COLLAB_POLL_MS 传给 usePoll，
// 不散写魔数。mock usePoll 后只断言 interval 参数 == 常量。
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { render } from '@testing-library/react'
import { MemoryRouter, Route, Routes } from 'react-router-dom'
import { usePoll } from '../data/usePoll'
import { COLLAB_POLL_MS } from './constants'
import { InboxPage } from './InboxPage'
import { RoomDetailPage } from './RoomDetailPage'
import { RoomsListPage } from './RoomsListPage'

const pollState = { data: null, disconnected: false, sessionExpired: false, errorText: '', refresh: vi.fn() }

vi.mock('../data/usePoll', () => ({ usePoll: vi.fn(() => pollState) }))
vi.mock('../../api/rooms', () => ({
  fetchRooms: vi.fn(),
  fetchRoomMessages: vi.fn(),
  sendRoomMessage: vi.fn(),
  markRoomRead: vi.fn(),
  fetchInbox: vi.fn(),
}))
vi.mock('../../api/ledger', async (importOriginal) => ({
  ...(await importOriginal<typeof import('../../api/ledger')>()),
  answerDecision: vi.fn(),
}))

describe('A.6 轮询间隔常量', () => {
  beforeEach(() => vi.mocked(usePoll).mockClear())

  it('会话列表页以 COLLAB_POLL_MS 轮询', () => {
    render(<MemoryRouter><RoomsListPage /></MemoryRouter>)
    expect(vi.mocked(usePoll)).toHaveBeenCalledWith(expect.any(Function), COLLAB_POLL_MS)
  })

  it('房间页以 COLLAB_POLL_MS 轮询（摘要与历史两条）', () => {
    render(
      <MemoryRouter initialEntries={['/rooms/B1']}>
        <Routes><Route path="/rooms/:id" element={<RoomDetailPage />} /></Routes>
      </MemoryRouter>,
    )
    const calls = vi.mocked(usePoll).mock.calls
    expect(calls.filter(([, interval]) => interval === COLLAB_POLL_MS)).toHaveLength(2)
  })

  it('收件箱页以 COLLAB_POLL_MS 轮询', () => {
    render(<MemoryRouter><InboxPage /></MemoryRouter>)
    expect(vi.mocked(usePoll)).toHaveBeenCalledWith(expect.any(Function), COLLAB_POLL_MS)
  })
})
```

**代码块 H——`web/src/app/rooms/RoomsListPage.tsx`（全文）**：

```tsx
// 会话列表页（spec §7 注意力面第一件：扁平活动排序列表）。
//
// 职责：
//   - 轮询 GET /api/rooms 全量，渲染 RoomSummary 扁平列表
//   - 项目筛选（客户端过滤，CardsPage 同构；端点仍支持 ?project=，见 rooms.ts fetchRooms）
//   - live / read_only / kind 徽标；bound_session 原样展示（澄清一：不透明载体标识，
//     不做任何格式假设）
//
// 边界：
//   - 排序是服务端契约（ListRooms LastActivity 降序），本组件不二次排序
//   - 错误态与空列表是两个东西：usePoll 断线时展示错误横幅，不渲染成空列表（已知陷阱四）
//   - 布局/滚动/长列表性能进真机清单，jsdom 断言只锁数据流与交互语义
import { useMemo, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { fetchRooms } from '../../api/rooms'
import type { RoomSummary } from '../../api/rooms'
import { usePoll } from '../data/usePoll'
import { formatRelative } from '../lib/format'
import { COLLAB_POLL_MS } from './constants'

// KIND_LABEL 房间类型 → 中文标签；未知 kind 原样显示（词表演进时降级不炸）。
const KIND_LABEL: Record<string, string> = {
  card: '卡会话',
  project: '项目群',
  global: '全员群',
}

function RoomRow({ room }: { room: RoomSummary }) {
  const navigate = useNavigate()
  return (
    <button
      type="button"
      onClick={() => navigate(`/rooms/${encodeURIComponent(room.id)}`)}
      className="w-full rounded-md border px-3 py-2 text-left hover:bg-accent/60"
    >
      <div className="flex flex-wrap items-center gap-2 text-sm">
        <span className="font-medium">{room.title}</span>
        <span className="rounded-full border px-1.5 py-0.5 text-[10px] text-muted-foreground">
          {KIND_LABEL[room.kind] ?? room.kind}
        </span>
        {room.live && <span className="text-[10px] text-green-600">在线</span>}
        {room.read_only && <span className="text-[10px] text-amber-700">只读</span>}
      </div>
      {/* bound_session 是绑定者会话标识（driver_session 投影），不透明展示，不做格式假设（澄清一） */}
      {room.bound_session !== undefined && room.bound_session !== '' && (
        <div className="mt-0.5 font-mono text-[11px] text-muted-foreground">{room.bound_session}</div>
      )}
      <div className="mt-0.5 text-[11px] text-muted-foreground">{formatRelative(room.last_activity)}</div>
    </button>
  )
}

export function RoomsListPage() {
  const [project, setProject] = useState('')
  const poll = usePoll(fetchRooms, COLLAB_POLL_MS)
  const rooms = poll.data ?? []
  const projectOptions = useMemo(
    () => [...new Set(rooms.map((r) => r.project).filter((p): p is string => !!p))].sort(),
    [rooms],
  )
  // 项目筛选：卡房间按 project 匹配，项目群按 project:<name> 匹配（global 恒在）。
  const filtered = useMemo(
    () =>
      project === ''
        ? rooms
        : rooms.filter((r) => r.project === project || r.id === `project:${project}`),
    [rooms, project],
  )

  return (
    <main className="flex h-full min-h-0 w-full flex-col bg-background">
      <header className="flex flex-wrap items-center gap-2 border-b px-4 py-2.5">
        <span className="text-sm font-semibold">会话</span>
        <select
          aria-label="项目"
          value={project}
          onChange={(event) => setProject(event.target.value)}
          className="rounded-md border bg-background px-2 py-1 text-xs"
        >
          <option value="">全部项目</option>
          {projectOptions.map((item) => (
            <option key={item} value={item}>{item}</option>
          ))}
        </select>
      </header>
      {poll.disconnected && (
        <p role="alert" className="border-b bg-amber-50 px-4 py-1.5 text-xs text-amber-800">
          会话列表加载失败：{poll.errorText}
        </p>
      )}
      <div data-testid="room-list" className="min-h-0 flex-1 space-y-1.5 overflow-y-auto p-3">
        {poll.data === null && !poll.disconnected ? (
          <p className="text-sm text-muted-foreground">正在读取…</p>
        ) : filtered.length === 0 ? (
          <p className="text-sm text-muted-foreground">（空）</p>
        ) : (
          filtered.map((room) => <RoomRow key={room.id} room={room} />)
        )}
      </div>
    </main>
  )
}
```

**代码块 I——`web/src/app/rooms/RoomDetailPage.tsx`（全文）**：

```tsx
// 房间页（spec §7 注意力面「打开房间即已读」的消费面）。
//
// 职责：
//   - 轮询最新一页历史（limit=200，升序），「加载更早」按 before=最老seq-1 手动叠加
//   - 打开房间即已读：到新 maxSeq 置一次 markRoomRead(id, maxSeq)
//   - 发送框：POST /api/rooms/{id}/messages；只读房由 handler 内守卫拦截（D4——
//     disabled 断言不算数，必须真触发提交再断言没有 POST fetch）
//
// 边界：
//   - 不存在的房间 = 200 空列表（C4 合规 History），照常渲染空历史，不报错
//   - 自身摘要（read_only/bound_session/live）来自列表端点（无 /rooms/{id} 单查端点）；
//     不在列表中的房间按只读未知处理，发送合法性由服务端执法
//   - 布局/滚动/长列表性能进真机清单，jsdom 断言只锁数据流与交互语义
import { useEffect, useMemo, useState } from 'react'
import { useParams } from 'react-router-dom'
import { fetchRoomMessages, fetchRooms, markRoomRead, sendRoomMessage } from '../../api/rooms'
import type { RoomHistoryItem, RoomMessage, RoomSummary } from '../../api/rooms'
import { usePoll } from '../data/usePoll'
import { errorMessage, formatRelative } from '../lib/format'
import { COLLAB_POLL_MS } from './constants'

// HISTORY_LIMIT 每页条数：与契约 History limit<=0 取 200 的默认一致（方案 A）。
const HISTORY_LIMIT = 200

// KIND_LABEL 消息 kind → 中文标签；未知 kind 原样显示。
const KIND_LABEL: Record<string, string> = {
  escalation: '推翻级简报',
  deviation: '偏差叙事',
  closing: '收口摘要',
  relay: '父卡衔接',
  reply: '协调者应答',
  user: '用户',
  pointer: '里程碑',
}

function MessageRow({ event }: { event: RoomHistoryItem }) {
  const msg = (event.payload ?? {}) as Partial<RoomMessage>
  return (
    <div className="rounded-md border px-3 py-2 text-sm">
      <div className="flex flex-wrap items-center gap-2 text-[11px] text-muted-foreground">
        <span className="rounded-full border px-1.5 py-0.5">
          {KIND_LABEL[msg.kind ?? event.type] ?? (msg.kind ?? event.type)}
        </span>
        <span>#{event.seq}</span>
        <span>{formatRelative(event.created_at)}</span>
      </div>
      <p className="mt-1 whitespace-pre-wrap">{msg.body}</p>
    </div>
  )
}

export function RoomDetailPage() {
  const { id = '' } = useParams<{ id: string }>()
  const [older, setOlder] = useState<RoomHistoryItem[]>([])
  const [loadingOlder, setLoadingOlder] = useState(false)
  const [draft, setDraft] = useState('')
  const [sending, setSending] = useState(false)
  const [sendError, setSendError] = useState('')
  const [markedSeq, setMarkedSeq] = useState<number | null>(null)

  const summaryPoll = usePoll(fetchRooms, COLLAB_POLL_MS)
  const summary: RoomSummary | undefined = useMemo(
    () => summaryPoll.data?.find((room) => room.id === id),
    [summaryPoll.data, id],
  )

  // 历史：只轮询最新一页，更早页手动叠加（older 前缀 + latest 按 seq 去重）。
  const messagesPoll = usePoll(
    () => fetchRoomMessages(id, { limit: HISTORY_LIMIT }),
    COLLAB_POLL_MS,
    { enabled: id !== '' },
  )
  const latest = messagesPoll.data ?? []
  const all = useMemo(() => {
    const seen = new Set<number>()
    const out: RoomHistoryItem[] = []
    for (const event of [...older, ...latest]) {
      if (seen.has(event.seq)) continue
      seen.add(event.seq)
      out.push(event)
    }
    return out
  }, [older, latest])
  const oldestSeq = all.length > 0 ? all[0].seq : 0
  const maxSeq = all.length > 0 ? all[all.length - 1].seq : 0

  // 打开房间即已读（spec §7）：到新消息水位置一次已读，不逐条打。
  useEffect(() => {
    if (maxSeq > 0 && maxSeq !== markedSeq) {
      setMarkedSeq(maxSeq)
      void markRoomRead(id, maxSeq).catch(() => {})
    }
  }, [maxSeq, markedSeq, id])

  const loadOlder = async () => {
    if (loadingOlder || oldestSeq === 0) return
    setLoadingOlder(true)
    try {
      const page = await fetchRoomMessages(id, { before: oldestSeq - 1, limit: HISTORY_LIMIT })
      setOlder((prev) => [...page, ...prev])
    } catch (err) {
      console.debug('[rooms] 加载更早消息失败', id, errorMessage(err))
    } finally {
      setLoadingOlder(false)
    }
  }

  // 发送守卫顺序关键：只读判定在空正文判定**之前**——只读房点击发送必须命中
  // 这条守卫并直接返回，否则「空正文先 return」会让只读断言空转（已知陷阱二，D4）。
  const send = async () => {
    if (summary?.read_only) return
    const body = draft.trim()
    if (body === '' || sending) return
    setSending(true)
    setSendError('')
    try {
      await sendRoomMessage(id, body)
      setDraft('')
      messagesPoll.refresh()
    } catch (err) {
      console.debug('[rooms] 发送消息失败', id, errorMessage(err))
      setSendError(errorMessage(err))
    } finally {
      setSending(false)
    }
  }

  return (
    <main className="flex h-full min-h-0 w-full flex-col bg-background">
      <header className="flex flex-wrap items-center gap-2 border-b px-4 py-2.5">
        <span className="text-sm font-semibold">{summary?.title ?? id}</span>
        {summary?.read_only && <span className="text-[10px] text-amber-700">只读（并入/已归档）</span>}
        {summary?.bound_session !== undefined && summary.bound_session !== '' && (
          <span className="font-mono text-[11px] text-muted-foreground">{summary.bound_session}</span>
        )}
        {messagesPoll.disconnected && (
          <span className="text-[11px] text-amber-700">消息流断线：{messagesPoll.errorText}</span>
        )}
      </header>
      <div className="min-h-0 flex-1 space-y-1.5 overflow-y-auto p-3">
        {messagesPoll.data === null && !messagesPoll.disconnected ? (
          <p className="text-sm text-muted-foreground">正在读取…</p>
        ) : (
          <>
            {all.length > 0 && (
              <button
                type="button"
                onClick={() => void loadOlder()}
                disabled={loadingOlder}
                className="w-full rounded-md border px-3 py-1.5 text-xs"
              >
                {loadingOlder ? '加载中…' : '加载更早'}
              </button>
            )}
            {all.length === 0 ? (
              <p className="text-sm text-muted-foreground">（还没有消息）</p>
            ) : (
              all.map((event) => <MessageRow key={event.seq} event={event} />)
            )}
          </>
        )}
      </div>
      <footer className="flex flex-col gap-1.5 border-t px-4 py-2.5">
        {summary?.read_only && (
          <p className="text-[11px] text-amber-700">房间只读，不能发送消息。</p>
        )}
        <div className="flex items-center gap-2">
          <textarea
            aria-label="消息正文"
            value={draft}
            onChange={(event) => setDraft(event.target.value)}
            placeholder={summary?.read_only ? '' : '发消息…'}
            disabled={summary?.read_only}
            rows={2}
            className="min-w-0 flex-1 rounded-md border bg-background px-2 py-1.5 text-sm"
          />
          <button
            type="button"
            onClick={() => void send()}
            className="rounded-md border px-3 py-1.5 text-sm"
          >
            发送
          </button>
        </div>
        {sendError !== '' && <p role="alert" className="text-xs text-destructive">{sendError}</p>}
      </footer>
    </main>
  )
}
```

**代码块 J——`web/src/app/rooms/InboxPage.tsx`（全文）**：

```tsx
// 收件箱页（spec §3.3 答复直达 + §7 注意力面第二件：待回复收件箱）。
//
// 职责：
//   - 轮询 GET /api/inbox，三源分区渲染（decision / ticket / mention）
//   - decision 条目渲染候选项按钮，点击直调既有 answerDecision（不新造第二答复通道，
//     契约 §3.3），答复后显示「答复已落账，等待协调者唤醒」并轮询刷新（open 裁决消失）
//
// 边界：
//   - 协调者不在场检测本期无数据源（wire 无 live 字段），答复后一律明示等待文案
//   - 决策 payload 是 Decision 对象（C6 投影），解析失败渲染降级行不炸
import { useState } from 'react'
import { answerDecision } from '../../api/ledger'
import type { Decision } from '../../api/ledger'
import { fetchInbox } from '../../api/rooms'
import type { InboxItem } from '../../api/rooms'
import { usePoll } from '../data/usePoll'
import { errorMessage } from '../lib/format'
import { COLLAB_POLL_MS } from './constants'

function DecisionCard({ item, onAnswered }: { item: InboxItem; onAnswered: (id: number) => void }) {
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState('')
  const decision = item.payload as Decision | undefined
  const decisionId = decision?.id ?? Number(item.ref_id)
  if (!decision || Number.isNaN(decisionId)) {
    return (
      <div className="rounded-md border px-3 py-2 text-sm">
        <p className="font-medium">{item.title}</p>
        <p className="text-xs text-muted-foreground">（裁决详情缺失）</p>
      </div>
    )
  }
  const answer = async (option: string) => {
    if (busy) return
    setBusy(true)
    setError('')
    try {
      await answerDecision(decisionId, option)
      onAnswered(decisionId)
    } catch (err) {
      console.debug('[inbox] 答复失败', decisionId, errorMessage(err))
      setError(errorMessage(err))
    } finally {
      setBusy(false)
    }
  }
  return (
    <div className="rounded-md border border-amber-200 bg-amber-50 px-3 py-2 text-sm">
      <p className="font-medium">{decision.body}</p>
      <div className="mt-1.5 flex flex-wrap gap-1.5">
        {(decision.options ?? []).map((option) => (
          <button
            key={option}
            type="button"
            onClick={() => void answer(option)}
            disabled={busy}
            className="rounded-md border bg-background px-2.5 py-1 text-xs"
          >
            {option}
          </button>
        ))}
      </div>
      {error !== '' && <p role="alert" className="mt-1 text-xs text-destructive">{error}</p>}
    </div>
  )
}

export function InboxPage() {
  const poll = usePoll(fetchInbox, COLLAB_POLL_MS)
  const items = poll.data ?? []
  const [answered, setAnswered] = useState<number | null>(null)

  const byOrigin = (origin: string) => items.filter((item) => item.origin === origin)

  return (
    <main className="flex h-full min-h-0 w-full flex-col bg-background">
      <header className="flex items-center gap-2 border-b px-4 py-2.5">
        <span className="text-sm font-semibold">待回复收件箱</span>
        {poll.disconnected && <span className="text-[11px] text-amber-700">断线：{poll.errorText}</span>}
      </header>
      {answered !== null && (
        <p role="status" className="border-b bg-green-50 px-4 py-1.5 text-xs text-green-800">
          答复已落账，等待协调者唤醒。
        </p>
      )}
      <div className="min-h-0 flex-1 space-y-3 overflow-y-auto p-3">
        <section aria-label="简报与裁决">
          <h2 className="mb-1.5 text-xs font-semibold text-muted-foreground">需要你 · 简报</h2>
          {byOrigin('decision').map((item) => (
            <DecisionCard key={item.ref_id} item={item} onAnswered={setAnswered} />
          ))}
          {byOrigin('decision').length === 0 && (
            <p className="text-sm text-muted-foreground">（无待答复简报）</p>
          )}
        </section>
        <section aria-label="工单">
          <h2 className="mb-1.5 text-xs font-semibold text-muted-foreground">兜底上浮工单</h2>
          {byOrigin('ticket').map((item) => (
            <div key={item.ref_id} className="rounded-md border px-3 py-2 text-sm">
              <p className="font-medium">{item.title}</p>
              <p className="font-mono text-[11px] text-muted-foreground">{item.ref_id}</p>
            </div>
          ))}
          {byOrigin('ticket').length === 0 && (
            <p className="text-sm text-muted-foreground">（无上浮工单）</p>
          )}
        </section>
        <section aria-label="提及">
          <h2 className="mb-1.5 text-xs font-semibold text-muted-foreground">@你</h2>
          {byOrigin('mention').map((item) => (
            <div key={item.ref_id} className="rounded-md border px-3 py-2 text-sm">
              <p className="font-medium">{item.title}</p>
              {item.card_id !== undefined && item.card_id !== '' && (
                <p className="font-mono text-[11px] text-muted-foreground">{item.card_id}</p>
              )}
            </div>
          ))}
          {byOrigin('mention').length === 0 && (
            <p className="text-sm text-muted-foreground">（无提及）</p>
          )}
        </section>
      </div>
    </main>
  )
}
```

**测试范围声明（最小化）**：本 task 只跑 `npx vitest run src/app/rooms` + `npx tsc -b`。不跑全量。

---

### T8.3 立红立绿：Shell 路由 + 左栏 dock 入口（锁缝断言步骤，法定红绿）

**Interfaces**

- Produces（接线，调用链穿过接缝 #5——dock 点击/路由挂载最终抵达页面 → rooms API）：
  - Shell 新增路由 `/rooms`（RoomsListPage）、`/rooms/:id`（RoomDetailPage）、`/inbox`（InboxPage），全部 ledgerEnabled 门控（与 `/cards` 同块）
  - ProjectTree 新增 props `onOpenRooms?`/`onOpenInbox?` + dock 两按钮
- Consumes：T8.2 三个页面组件；`navigate`（react-router）；ProjectTree 既有 dock 布局。

**步骤**

1. **判据先在基线跑**：`npx vitest run src/app/shell/Shell.test.tsx src/app/tree/ProjectTree.test.tsx`（T8.2 之前基线全绿）。
2. **写失败测试**（代码块 K–L）：
   - `Shell.test.tsx` 追加：`vi.mock('../../api/rooms', ...)`；路由挂载 `/rooms`/`/inbox` 直达断言（fetchRooms/fetchInbox 被调=页面经接缝挂载）；dock 按钮「会话」「收件箱」点击 → 对应 fetch 被调（seam 贯穿）。红：路由不存在 → fetch 不被调。
   - `ProjectTree.test.tsx` 追加：dock 按钮点击调 `onOpenRooms`/`onOpenInbox`（内部锁，§7 声明）。红：props/按钮不存在。
   跑红：`npx vitest run src/app/shell/Shell.test.tsx src/app/tree/ProjectTree.test.tsx` 新增用例红（原文留存）。
3. **最小实现**：
   - `Shell.tsx`：import 三页面；`fullPageRoute` 加 `/rooms`、`/inbox`；ledgerEnabled 块加三路由；ProjectTree 传 `onOpenRooms={() => navigate('/rooms')}`、`onOpenInbox={() => navigate('/inbox')}`（代码块 M）。
   - `ProjectTree.tsx`：props 接口加两可选；lucide import 加 `AlertTriangle, MessageSquareText`；dock 段 ledgerEnabled 块内加两按钮（代码块 N）。
4. **跑绿**：`npx vitest run src/app/shell/Shell.test.tsx src/app/tree/ProjectTree.test.tsx` 全绿；`npx tsc -b` EXIT=0。
5. **加关键节点日志**：无新增日志（路由/入口是纯分发，不产生业务事件；不制造心跳日志）。
6. **加注释**：Shell 路由块注明「房间面三路由，spec §8.1 扩展点②」；ProjectTree dock 按钮注明「会话/收件箱是房间面的全局入口（岔口九归 d_web_command）」。
7. **提交** commit 3（见 §11）。

**代码块 K——`web/src/app/shell/Shell.test.tsx` 追加段**：

```tsx
// —— B156.2 C8 追加：房间面路由与 dock 入口（seam 贯穿——点击/挂载最终到达 rooms API）——
vi.mock('../../api/rooms', async () => {
  const actual = await vi.importActual<typeof import('../../api/rooms')>('../../api/rooms')
  return {
    ...actual,
    fetchRooms: vi.fn().mockResolvedValue([]),
    fetchInbox: vi.fn().mockResolvedValue([]),
    fetchRoomMessages: vi.fn().mockResolvedValue([]),
    markRoomRead: vi.fn().mockResolvedValue({ ok: true }),
    sendRoomMessage: vi.fn().mockResolvedValue({ seq: 1 }),
  }
})

describe('房间面路由与 dock 入口', () => {
  it('/rooms 挂载会话列表页：fetchRooms 被调（经接缝 #5）', async () => {
    const rooms = await import('../../api/rooms')
    render(
      <MemoryRouter initialEntries={['/rooms']}>
        <AppRoutes />
      </MemoryRouter>,
    )
    await waitFor(() => expect(vi.mocked(rooms.fetchRooms)).toHaveBeenCalled())
  })

  it('/inbox 挂载收件箱页：fetchInbox 被调', async () => {
    const rooms = await import('../../api/rooms')
    render(
      <MemoryRouter initialEntries={['/inbox']}>
        <AppRoutes />
      </MemoryRouter>,
    )
    await waitFor(() => expect(vi.mocked(rooms.fetchInbox)).toHaveBeenCalled())
  })

  it('左栏 dock 点「会话」→ 路由切到 /rooms → fetchRooms 被调', async () => {
    const rooms = await import('../../api/rooms')
    render(
      <MemoryRouter initialEntries={['/']}>
        <AppRoutes />
      </MemoryRouter>,
    )
    fireEvent.click(await screen.findByRole('button', { name: '会话' }))
    await waitFor(() => expect(vi.mocked(rooms.fetchRooms)).toHaveBeenCalled())
  })

  it('左栏 dock 点「收件箱」→ 路由切到 /inbox → fetchInbox 被调', async () => {
    const rooms = await import('../../api/rooms')
    render(
      <MemoryRouter initialEntries={['/']}>
        <AppRoutes />
      </MemoryRouter>,
    )
    fireEvent.click(await screen.findByRole('button', { name: '收件箱' }))
    await waitFor(() => expect(vi.mocked(rooms.fetchInbox)).toHaveBeenCalled())
  })
})
```

**代码块 L——`web/src/app/tree/ProjectTree.test.tsx` 追加段**：

```tsx
// —— B156.2 C8 追加：dock 按钮回调接线（内部锁，声明见计划 §7）——
// props() 助手的 over 类型加两可选字段，返回对象加两项：
//   onOpenRooms?: () => void
//   onOpenInbox?: () => void
//   onOpenRooms: over.onOpenRooms ?? vi.fn(),
//   onOpenInbox: over.onOpenInbox ?? vi.fn(),
describe('dock 会话/收件箱入口', () => {
  it('点「会话」触发 onOpenRooms', () => {
    const onOpenRooms = vi.fn()
    render(<ProjectTree {...props({ onOpenRooms })} />)
    fireEvent.click(screen.getByRole('button', { name: '会话' }))
    expect(onOpenRooms).toHaveBeenCalled()
  })

  it('点「收件箱」触发 onOpenInbox', () => {
    const onOpenInbox = vi.fn()
    render(<ProjectTree {...props({ onOpenInbox })} />)
    fireEvent.click(screen.getByRole('button', { name: '收件箱' }))
    expect(onOpenInbox).toHaveBeenCalled()
  })
})
```

**代码块 M——`web/src/app/shell/Shell.tsx` 改动块**：

```tsx
// 1) import（与 CardsPage 等并列）
import { InboxPage } from '../rooms/InboxPage'
import { RoomDetailPage } from '../rooms/RoomDetailPage'
import { RoomsListPage } from '../rooms/RoomsListPage'

// 2) fullPageRoute（:391 原数组加两项）
const fullPageRoute = ['/cards', '/flows', '/rooms', '/inbox', '/settings', '/machines', '/codegraph']
  .some((path) => location.pathname.startsWith(path))

// 3) Routes ledgerEnabled 块内（:481-486，/cards /flows 之后加）
// 房间面三路由：spec §8.1 扩展点②。路由挂载即页面经接缝 #5 轮询。
<Route path="/rooms" element={<RoomsListPage />} />
<Route path="/rooms/:id" element={<RoomDetailPage />} />
<Route path="/inbox" element={<InboxPage />} />

// 4) ProjectTree props（:454 onOpenFlows 旁加两行）
onOpenRooms={() => navigate('/rooms')}
onOpenInbox={() => navigate('/inbox')}
```

**代码块 N——`web/src/app/tree/ProjectTree.tsx` 改动块**：

```tsx
// 1) lucide import（:35-37 行内加两图标）
import {
  AlertTriangle, Archive, ChevronRight, FolderGit2, GitBranch, HardDrive, Home, LayoutGrid,
  MessageSquareText, Plus, Search, Settings, SquareKanban, Ticket, WifiOff, Workflow,
} from 'lucide-react'

// 2) props 接口（:73 onOpenFlows 附近加两可选）
// 会话/收件箱：房间面（d_web_command）的全局入口，spec §8.1 扩展点②。
onOpenRooms?: () => void
onOpenInbox?: () => void

// 3) 解构签名（:232 加两项）
onOpenRooms, onOpenInbox,

// 4) dock 段（:789-799 流程按钮之后、工单按钮之前，ledgerEnabled 门控）
{ledgerEnabled && onOpenRooms && (
  <button
    type="button"
    aria-label="会话"
    title="会话（房间面）"
    onClick={onOpenRooms}
    className="rounded-md p-1.5 text-muted-foreground hover:bg-accent/60 hover:text-foreground"
  >
    <MessageSquareText className="size-4" />
  </button>
)}
{ledgerEnabled && onOpenInbox && (
  <button
    type="button"
    aria-label="收件箱"
    title="收件箱（待回复）"
    onClick={onOpenInbox}
    className="rounded-md p-1.5 text-muted-foreground hover:bg-accent/60 hover:text-foreground"
  >
    <AlertTriangle className="size-4" />
  </button>
)}
```

**测试范围声明（最小化）**：本 task 只跑 `npx vitest run src/app/shell/Shell.test.tsx src/app/tree/ProjectTree.test.tsx` + `npx tsc -b`。不跑全量。

---

### T8.4 图登记 + 图闸（非红绿，纯机械 + 验证）

**背景（D7/L10）**：web 容器在 baseline 图节点数为 0；`merge.go:84` 把视图 diff 的 containersAdded 并入视图容器集；`best.go:127-133` 校验容器→叶子域引用。C8 唯一图动作 = 容器归属登记（岔口九），零节点、零边。

**步骤**

1. **判据先在基线跑**：`go run . graph check --repo . --view cards-B156.2-charter-4` → fails=[] EXIT=0（plan 轮已实测：fails=[] warns=96）；`go run . graph validate --repo .` EXIT=0。
2. **best.json**：`containers` 加两键（代码块 O）。
3. **视图 diff**：`codegraph/diffs/cards-B156.2-charter-4.json` 的 `containersAdded` 加两枚（代码块 P；label/kind 按 baseline 既有 web 容器惯例——`k_web_app_board` label=`/app/board组件与函数` kind=`React 组件/函数`，`_model` label=`/app/board 类型` kind=`TypeScript 模型`）。
4. **图闸**：`go run . graph check --repo . --view cards-B156.2-charter-4` → fails=[] EXIT=0；`go run . graph validate --repo .` EXIT=0。
5. **计数自检**：
   - `python3 -c "import json;d=json.load(open('codegraph/best.json'));print(d['containers']['k_web_app_rooms'], d['containers']['k_web_app_rooms_model'])"` == `d_web_command d_web_command`；
   - `python3 -c "import json;d=json.load(open('codegraph/diffs/cards-B156.2-charter-4.json'));print(sorted(d['containersAdded']))"` 含 `k_web_app_rooms`、`k_web_app_rooms_model`。
6. **提交** commit 4（见 §11）。

**代码块 O——`codegraph/best.json` containers 段追加**：

```json
    "k_web_app_rooms": "d_web_command",
    "k_web_app_rooms_model": "d_web_command"
```

（追加进 `"containers": { ... }` 对象，键序无所谓——JSON 对象无序。）

**代码块 P——`codegraph/diffs/cards-B156.2-charter-4.json` containersAdded 追加**：

```json
  "k_web_app_rooms": {
    "label": "/app/rooms组件与函数",
    "kind": "React 组件/函数",
    "domain": "d_web_command"
  },
  "k_web_app_rooms_model": {
    "label": "/app/rooms 类型",
    "kind": "TypeScript 模型",
    "domain": "d_web_command"
  }
```

**测试范围声明（最小化）**：只跑图闸 `graph check --view` + `graph validate`，不跑任何代码测试。

---

### T8.5 收尾全量 + 变异复验（红绿收口）

**步骤**

1. **判据先在基线跑**：`npx tsc -b` EXIT=0；`npx vitest run`（web 全量）预期全绿。
2. **变异复验（强制，交付报两个读数）**：
   - **变异①（只读禁写守卫的牙）**：临时把 `RoomDetailPage.tsx` 的 `if (summary?.read_only) return` 守卫删除 → `npx vitest run src/app/rooms/RoomDetailPage.test.tsx` 的只读禁写反面用例**必红**（POST 泄漏）→ 还原 → 复跑全绿。报「变异前绿 / 变异后红 / 还原后绿」三读数。
   - **变异②（A.6 常量）**：临时把 `RoomsListPage.tsx` 的 `COLLAB_POLL_MS` 换成魔数 `2500` → `pollInterval.test.tsx` 会话列表用例红 → 还原。
   - **变异③（错误态）**：临时把列表页断线横幅改回渲染空列表 → 错误态用例红 → 还原。
3. **图闸复验**：`graph check --view cards-B156.2-charter-4` fails=[]、`graph validate` EXIT=0（T8.4 后无图改动，防御性复跑）。
4. **收尾清单**：`git status` 只剩本卡文件；`npx eslint .`（web）若基线支持则跑，不新增 lint 错误（web 侧先例：`npm run lint`）。
5. **提交**：无新提交（变异还原后工作树干净；如需修小问题则并入前序 commit——见 §11）。

**测试范围声明（最小化）**：收尾全量 `npx vitest run`（web 唯一一次全量）+ `npx tsc -b` + 图闸。Go 侧零触碰，不跑 `go test`。

---

## 4. 缺陷族对抗审查结论（逐族）

| 族 | 设问 | 结论 |
|---|---|---|
| 生命周期/状态机中断 | 轮询断线/重连、页面卸载竞态、答复后状态残留 | usePoll 既有语义兜底（断线留数据/401 终止/visibilitychange 停表）；页面无跨帧状态机；答复后 `answered` 本地态 + 轮询刷新，卸载即丢（无害）。`markRoomRead` 失败 catch 静默（已读是弱承诺，下次轮询补），不吞发送错误（sendError 展示）。 |
| 静默失败/误导报错 | 列表/历史/收件箱失败是否被呈现为空列表？发送失败是否吞错？ | 列表页断线横幅 ≠（空）（T8.2 错误态用例）；房间页消息流断线 header 提示；发送失败 sendError 原文展示；收件箱断线 header 提示。无静默空列表路径。 |
| 跨平台假设 | 时间格式、URL 编码、图标存在性 | last_activity 原样直通（D2）；`encodeURIComponent(room.id)`（id 含 `project:` 冒号）；图标只用仓库已证明存在集合（D9）。无平台假设。 |
| 假红/假绿 | disabled 断言空转、mock 掩盖写路径、常量为魔数绕过 | 只读禁写走 fetch 边界 + `expect.poll`（T8.2 E 块）+ 变异①证明能红；A.6 断言经 mock usePoll 锁 interval 参数 == 常量 + 变异②；列表错误态断言空态缺失 + 变异③。三个变异都有确定性靶。 |
| 门禁绕过 | 只读房写路径是否被接上但「恰好没触发」 | 守卫在 handler 内（D4），正控同表必打 POST（T8.2 E 块）；变异①删除守卫即红——守卫有牙。 |

## 5. 五项检查

1. **缺陷族对抗审查**：§4 逐族有结论，无风险处写明理由。
2. **序列化边界设问**：本卡新增数据字段 = rooms.ts 五个 fetch 函数的响应解码（RoomSummary/InboxItem/LedgerEvent.payload）。每处手写投影/边界：
   - `rooms.fetch.test.ts`：fetchRooms 内联 RoomSummary 样本解码（last_activity 串直通 + bound_session 缺席=undefined，可空 vs 零值）、fetchInbox 对金样本三源解码（card_id 缺席 vs 有值）、fetchRoomMessages 对金样本 escalation payload 解码——一条 fetch 边界断言族；
   - 澄清三闭合：C6 httptest 全链一发（上游已完成）+ 本文件对同一夹具断言——链条经孪生夹具闭合，两侧各自测试 ≠ 链路测试，此处为链路的 C8 半；
   - RoomSummary 不在金样本（D2），内联样本在计划内给出，不属于「改形状回 contract」。
   无手搭 map/DTO 转换/CLI 拼输出（无 CLI 侧）；跨语言另一侧（Go）由 C6 锁。结论：有一条穿过真实序列化边界的回归（fetch 解码），合格。
3. **上下文预算检查**：§2 有界文件集 16 个文件全在 d_web 域，圈得出；无竖切需求（web 平铺目录既有结构，新页面目录按既有 `k_web_app_*` 惯例归 d_web_command，不插竖切卡）。
4. **类型标注**：d_web 为 logic 型；布局类验收以显式真机清单（§7）交付，组件断言明确不覆盖布局维度。
5. **接缝覆盖（双向，对照 spec 测试接缝清单 #5）**：
   - **测试 → 缝**：rooms.fetch.test.ts 入口=fetch 五函数（k_web_api_rooms，#5 主体）；RoomsListPage/RoomDetailPage/InboxPage 测试入口=页面组件，调用链经 `request/postJSON` → HTTP 端点（穿过 #5）；InboxPage 经 `answerDecision`（k_web_api_ledger，#5 同构）；Shell 测试入口=路由/dock 点击 → 页面 → rooms API（穿过 #5）。
   - **缝 → 测试**：缝 #5 的五端点 + answerDecision 每条至少一支缝级断言（T8.1 fetch 五支 + T8.2 页面交互各支），无未锁缝。
   - **内部锁**：仅 ProjectTree dock 按钮回调测试（T8.3 代码块 L）入口在 ProjectTree 按钮、不在缝 #5——§7 声明理由（按钮→路由→页面→API 的完整路径已在 Shell.test 经缝断言，此处只锁按钮级接线）。

## 6. 与上下游的交界

- **上游 C6（HTTP 面）**：五个端点的响应信封、POST body 形状、错误映射在本卡冻结消费（§1 #1）；`ErrNoRoom→404` 特例不可达、前端按 200 空列表设计（§1 #2）；actor/成员标识 `web:<host>` 服务端注入，前端 @ 提及只需把 `user:sy` 这类用户标识放进 mentions（本期页面不含 @ 输入控件，发送框仅 body——@ 输入控件不在 C8 验收内，记真机清单观察项）。
- **上游 C7（CLI）**：无交界（CLI 直调 collab，web 经 HTTP，两侧独立）。
- **上游 C5（读模型）**：ListRooms 排序/过滤语义、History 分页语义由 C5 保证，web 只消费不重算（列表页不二次排序、房间页 before 游标按升序 seq-1）。
- **下游（本卡后）**：无下游卡消费房间面；三期 B156.3 keystone 叙事换绑轮可能在 dock/房间面增加入口，届时走「显式加白名单/加路由」的既有出口，不重开本卡。

## 7. 内部锁声明（占位符扫描节的一部分，逐条自我声明）

| 测试 | 入口 | 为什么从声明缝构造不出 |
|---|---|---|
| ProjectTree.test.tsx「dock 会话/收件箱入口」（T8.3 代码块 L） | ProjectTree dock 按钮 | 观测对象是「左栏按钮→onOpenRooms 回调」这一层接线；按钮→路由→页面→API 的完整路径已在 Shell.test 经缝断言（seam 贯穿）。按钮级回调本身不经任何缝——它只是一个 props 传递。按「每个实现类 task 至少一条缝级断言」计：T8.3 的缝级断言在 Shell.test（/rooms、/inbox 挂载 + dock 点击 seam 贯穿），此内部锁只作附加。 |

退路同闸自查：§3 各 task 步骤正文无条件退路。

## 8. 占位符扫描自我声明

全文无 TBD／「加适当的错误处理」／「同 Task N」／描述做什么却不给代码。`rooms.fetch.test.ts`、`RoomsListPage.tsx`、`RoomDetailPage.tsx`、`InboxPage.tsx`、`RoomsListPage.test.tsx`、`RoomDetailPage.test.tsx`、`InboxPage.test.tsx`、`pollInterval.test.tsx`、`constants.ts` 全文件给出；`rooms.ts`/`Shell.tsx`/`ProjectTree.tsx` 给出完整改动块；best.json/视图 diff 给出完整追加块。**正当出口（「断言逐条列全 + 指认既有 harness」例外）使用情况**：未使用——页面测试全部断言给出全文（复用 vi.mock 模块打桩 + stubGlobal fetch 既有 harness，client.test.ts:21-41 先例）。占位残留扫描命令在 T8.5 步骤 4（`git status` 只列本卡文件）。

## 9. 跨卡审计输入（L3 重档：扇出前由协调者组织独立上下文审计；本节提供素材并自审待拍板）

**跨卡签名清单（本卡 Produces / 相关 Consumes，逐字符）**：

| 签名 | 产出方 | 消费方 | 一致性 |
|---|---|---|---|
| `ListRooms(project string) ([]proto.RoomSummary, error)` | C5（契约 §3.3 逐字） | C6 GET /api/rooms → C8 `fetchRooms` | wire 一致（§6 金样本 + 内联样本） |
| `History(roomID string, beforeSeq int64, limit int) ([]proto.LedgerEvent, error)` | C4 | C6 GET messages → C8 `fetchRoomMessages` | wire 一致（LedgerEvent 镜像已冻） |
| `Send(roomID string, msg proto.RoomMessage, actor string) (int64, error)` | C4 | C6 POST messages → C8 `sendRoomMessage` | body 形状一致（body/refs/mentions；kind/actor 服务端固定） |
| `MarkRead(member, roomID string, uptoSeq int64) error` | C5 | C6 POST read → C8 `markRoomRead` | `{upto_seq}` 一致 |
| `Mentions(member string, afterSeq int64, limit int) ([]proto.LedgerEvent, error)` | C5 | C6 收件箱 mention 源 → C8 `fetchInbox` | wire 一致 |
| `AnswerDecision(id int64, answer string, actor string) error` | 既有 ledger | C6 POST /decisions/{id}/answer → C8 `answerDecision` | ledger.ts:304-305 已冻 |
| `InboxItem`/`RoomSummary`/`RoomMessage` wire 形状 | 契约 §3.3/§6 金样本 | C8 rooms.ts 类型镜像（**一字不动**） | 双侧金样本锁定 |
| `d_web_command` 容器归属 | 岔口九 | C8 图登记（T8.4） | 与 k_web_app_board/overlay/task 同域 |

**spec 故事归属（本卡）**：故事 1（深夜清账）——会话列表 + 收件箱三源 + 答复直达 decision_answered + 收口摘要阅读，全部落在本卡三页面；故事 2/3/4/5 的叙事书写面归 C4/C5/C7，本卡只消费其结果（房间页渲染叙事消息、收件箱呈现简报）。spec §8.1 扩展点②「会话列表页、房间页、收件箱」逐项归属本卡。无跨卡落空。

**图覆盖债**：web 容器节点数为 0（前端不扫描成图节点，L9）——本卡零节点登记，无新债；`RoomSummary` 无金样本（D2）记入本卡测试（内联样本），如需补金样本属 contract 节点范畴，记债不擅动。

## 10. 自审三查

1. **spec 覆盖**：spec §7 注意力面三件套（会话列表/收件箱/未读游标=打开即已读）逐项指到 T8.2 页面；spec §3.3 答复直达与明示文案指到 T8.2 InboxPage；spec §8.1 扩展点②指到 T8.3；契约 §3.5 五行消费侧指到 T8.1；A.6 指到 T8.1/T8.2；澄清一/三指到 T8.2/T8.1；欠账 #8 全量 ✓。
2. **占位符扫描**：§8 ✓。
3. **跨 task 类型/签名一致性**：T8.1 五个 fetch 函数签名与 T8.2 页面消费、T8.1 测试断言逐字符一致；`COLLAB_POLL_MS` 三页面引用一致；wire 类型形状零改动。

## 11. 提交切分

- commit 1（T8.1）：`feat(web): rooms.ts fetch 五端点接线 + 轮询间隔常量（B156.2.8 欠账#8 half + A.6）`——rooms.ts、constants.ts、rooms.fetch.test.ts。
- commit 2（T8.2）：`feat(web): 会话列表/房间页/收件箱三页面 + 组件测试（B156.2.8 欠账#8 页面）`——rooms/ 目录下 3 页面 + 4 测试文件。
- commit 3（T8.3）：`feat(web): 房间面 Shell 路由 + 左栏 dock 入口（B156.2.8 spec §8.1 扩展点②）`——Shell.tsx、ProjectTree.tsx、Shell.test.tsx、ProjectTree.test.tsx。
- commit 4（T8.4）：`graph(b156.2.8): web 房间页容器归属登记（k_web_app_rooms(_model) → d_web_command，岔口九）`——best.json、cards-B156.2-charter-4.json。
- 每个 commit 前跑对应 task 的测试范围声明（§3 各 task）+ `npx tsc -b`；变异复验读数在 T8.5 交付报文里。
- 台账 `docs/superpowers/ledgers/ledger-b156.2.8-plan.md` 随实现轮持续追加，与产出物同批提交。

## 12. 明确不做（防越界）

- **不动 Go 侧任何文件**（零 `internal/` 改动，零 `cmd/` 改动）。
- **不改 rooms.ts 类型形状、不动 `testdata/RoomsFixture.json`、不动 Go 金样本**（改形状回 contract 节点）。
- **不做 @ 输入控件 / 富文本 / 消息预览**（方案 A 半条作废：无 LastMessage 字段）；房间页不渲染 decision 选项按钮（答复面在收件箱，spec §3.3）。
- **不加 WebSocket、不加轮询外推送**（拍板 5.3）。
- **不把 A.2 列表分页做成服务端分页**（列表全量）。
- **不做 carrier wire 字段**（wire 无此字段，展示面=bound_session 不透明展示，D1）。
- **不重排 RoomsFixture.json / 不加 RoomSummary 金样本**（属 contract 节点，本卡用内联样本）。
- **不碰 timeline/看板语义、不做 markdown 导出、不做移动端**（spec §10 Out of Scope）。