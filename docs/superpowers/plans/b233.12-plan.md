# B233.12 实现计划：审批入站收口（Hooks 不再投递，送达只在原生回传成功后）

状态：已拍板的单卡实现计划（L3 轻档，单缝，不拆子卡）。
卡号：B233.12（父卡 B233）　路由：contract → breakdown →（单轮）implement → review → acceptance → finish。
有效基线分支：`cards/B233.1-charter-7`（本卡不切换、不越过）。
本实现计划落点分支：`cards/B233.12-charter-2` @ `2577493c`（拍板已回写）。
冻结物：`docs/superpowers/specs/b233.12-contract.md`（冻结 #1–#16）；源 spec：`docs/superpowers/specs/b233.12.md`；拆解：`docs/superpowers/specs/b233.12-breakdown.md`。
本计划不新增 HTTP 路径、CLI 命令、事件类型、顶层领域或导出符号；不改 `internal/executor/approval.go`、`internal/executor/authorize.go`、`internal/proto`、`internal/store` 的生产代码。

拍板输入（P1–P5，来自 breakdown §0 回写）：
- **P1=A**：#11 按**调用级**执行，闸在 adapter。`authorizeNativePermission` 仅在 `Status==Deny` 或 `Rule ∈ {"approver","reuse"}` 时 `reportDeliveryFailure`；免审 Allow 跳过。
- **P2=A**：#16 由两条测试传递覆盖——agentd 经生产 `bindApproval` 断言钩子零回传；opencode 经 fake client 断言 adapter 恰一次。不复制 `fakeServer` 到 agentd，不新增导出测试缝。
- **P3**：拆法 (i)。新增包内 `auditAutoAllowOnly`（只审计 + `noteAutoAllowed`），`autoAllowPermission` = `auditAutoAllowOnly` + 既有回传；`bindApproval` 只绑 `auditAutoAllowOnly`；`noteAutoAllowed` 跟审计走。
- **P4**：`internal/agentd/approval_client_test.go#TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses` 纳入文件集，改成 `Request` 后 `DeliveredAt==nil`、`Acknowledge(AckDelivered)` 后才复用。
- **P5**：`respondAutoAllowReplay` 保持既有回传语义，不改 agy 双 hook 重放；本卡只断 OpenCode 的二次投递与提前送达。

## 1. 目标、冻结行为与接缝

一次 OpenCode 原生权限请求只有一条原生投递：审批 client 拥有决定（allow/deny/pending、工单 answer、审计），harness adapter 拥有原生投递，`DeliveredAt` 只表示原生回传已成功。

| 接缝 | 入口符号 | 本卡必须锁住的行为 |
|---|---|---|
| S1 原生投递 | `func (a *Adapter) authorizeNativePermission(r *runState, ev executor.AdapterEvent)`（`internal/executor/opencode/adapter.go`） | 一次权限请求 `RespondPermission` 恰好一次；allow 终态失败不产 `delivery_failed`（#11）；`AckDelivered` 只在该次成功之后。 |
| S2 审批决定与送达 | `func (c *Client) Request(ctx, req) (executor.ApprovalResult, error)` 与 `func (c *Client) consult(ctx, ev) (executor.ApprovalResult, error)`（`internal/approval/client.go`） | AutoAllow 与审批者 allow 返回时 `DeliveredAt==nil`；`Client.Request`/`consult` 成功路径都不调 `MarkTicketDelivered`；`Acknowledge(AckDelivered)` 是唯一写入点。 |
| S3 生产组装 | `func (m *Manager) bindApproval(taskID string, snap executor.PolicySnapshot) executor.ApprovalClient`（`internal/agentd/approval_client.go`） | 绑给 `Hooks.AutoAllow` 的生产实现执行期间不得 `RespondPermission`；#16 的「一条链只回传一次」由此保证。 |
| R1 非 OpenCode 兼容 | `func (m *Manager) handlePermission(ctx, taskID string, ev executor.AdapterEvent)`（`internal/agentd/manager.go`） | AutoAllow 分支仍由 `autoAllowPermission` 回传一次（#14），本卡不回退。 |

假缝禁令：`permissionAutoAllowPayload` 纯审计编码、`deliveryFailedPayload` 纯事件编码、fingerprint 函数不占接缝名额。

### 1.1 冻结接口（实现者只以此表跨文件对接）

审批契约（`internal/executor/approval.go`，本卡不改）：

```go
type ApprovalDecision struct {
	Status ApprovalStatus // "allow" / "deny" / "pending"
	Reason string
	Rule   string
}

type ApprovalResult struct {
	Ref      ApprovalRef
	Decision ApprovalDecision
}

type ApprovalAck struct {
	Ref      ApprovalRef
	NativeID string
	Stage    AckStage // "formed" / "delivered" / "executed"
}

type ApprovalClient interface {
	PolicySnapshot(ctx context.Context) (PolicySnapshot, error)
	Request(ctx context.Context, req ApprovalRequest) (ApprovalResult, error)
	Await(ctx context.Context, ref ApprovalRef) (ApprovalDecision, error)
	Acknowledge(ctx context.Context, ack ApprovalAck) error
}
```

审批实现与组装：

```go
func (c *Client) Request(ctx context.Context, req executor.ApprovalRequest) (executor.ApprovalResult, error)
func (c *Client) consult(ctx context.Context, ev executor.AdapterEvent) (executor.ApprovalResult, error)
func (c *Client) Acknowledge(ctx context.Context, ack executor.ApprovalAck) error

// 新增：包内，仅审计 + noteAutoAllowed，不回传原生应答。
func (m *Manager) auditAutoAllowOnly(taskID string, ev executor.AdapterEvent, verdict permgate.Verdict)
// 保持：审计（转调 auditAutoAllowOnly）+ 一次 RespondPermission；只由 handlePermission 调用。
func (m *Manager) autoAllowPermission(taskID string, ev executor.AdapterEvent, verdict permgate.Verdict)
func (m *Manager) bindApproval(taskID string, snap executor.PolicySnapshot) executor.ApprovalClient
func (m *Manager) takeAutoAllowed(taskID string) int
```

原生投递：

```go
func (a *Adapter) authorizeNativePermission(r *runState, ev executor.AdapterEvent)
func (a *Adapter) reportDeliveryFailure(ctx context.Context, r *runState, permID string, cause error)
// 新增：包内，闸门判据。deny 或有工单的 allow 才上报。
func deliveryFailureHasTicket(dec executor.ApprovalDecision) bool
```

持久权威（不改）：

```go
func (s *Store) MarkTicketDelivered(id string) error
func (s *Store) FindReusableGrant(taskID, fingerprint string) (*proto.Ticket, error)
func (s *Store) UndeliveredAnswers(taskID string) ([]proto.Ticket, error)
```

## 2. 有界文件集与明确不改范围

| 子系统 | 生产文件 | 测试文件 |
|---|---|---|
| 审批决定/送达（S2） | `internal/approval/client.go` | `internal/approval/client_test.go`、`internal/agentd/approval_client_test.go`（P4 一处） |
| 编排组装/非 OpenCode（S3/R1） | `internal/agentd/manager.go`、`internal/agentd/approval_client.go` | `internal/agentd/approval_client_test.go`、`internal/agentd/permgate_wire_test.go` |
| OpenCode 原生投递（S1） | `internal/executor/opencode/adapter.go` | `internal/executor/opencode/adapter_test.go` |

不改对象：`internal/executor/approval.go`、`internal/executor/authorize.go`、`internal/proto/proto.go`、`internal/store/store.go`、`internal/permgate/*`、`internal/agentd/approver.go`、`internal/agentd/server.go`。禁止把整个 `internal/agentd` 或 `internal/executor` 带入。若实现发现需要集合外文件，先停回协调者重画，不自行扩边。

测试范围声明：每个 task 只跑本 task 声明的包测试（只跑触及包）。全量/卡级汇总见 §11，不归任何单个 task。

## 3. Task 1：审批 client 送达确认收口（S2）

### 文件与接口

- 生产：`internal/approval/client.go`。
- 测试：`internal/approval/client_test.go`、`internal/agentd/approval_client_test.go`（仅改 P4 那一处）。
- Consumes：`executor.AdapterEvent`、`executor.ApprovalRequest`、`executor.PermFingerprint`、`executor.ReuseFingerprint`、`store.Store.MarkTicketDelivered`、`store.Store.FindReusableGrant`。
- Produces：`Client.consult` 返回 `ApprovalResult{Ref, Decision{Status: ApprovalAllow, Rule: "approver"}}`，工单 `answer=="allow"` 且 `DeliveredAt==nil`；唯一送达写入仍是 `Client.Acknowledge(AckDelivered)`。

### 基线判据（已在当前基线亲自运行）

```text
go test ./internal/approval -run 'TestClient(ConsultAllowPersistsGrant|AutoAllowHasNoTicket|NoteDeliveryFailedTaskIsolation|ReuseRequiresDeliveredGrant|AcknowledgeStages)' -count=1
ok  	github.com/Xsxdot/handoff/internal/approval	0.363s

go test ./internal/agentd -run 'Test(SafeCommandPermissionAuditsOnceWithoutTicket|AnsweredTicketReplayStillResponds|ApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses|ApproverApprovesPermissionWithoutWaking|ApprovePermissionAdapterForFailureNotesDeliveryFailed)' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	0.559s
```

上述基线只说明现状（含错误时序）可运行；实现后本 task 只跑：

```text
go test ./internal/approval -run 'TestClient(ConsultAllowPersistsGrant|AutoAllowHasNoTicket|NoteDeliveryFailedTaskIsolation|ReuseRequiresDeliveredGrant|AcknowledgeStages)' -count=1
go test ./internal/agentd -run 'TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses' -count=1
```

### 实现步骤（每步 2–5 分钟）

1. **先改测试为锁新时序（红）。** 把 `internal/approval/client_test.go#TestClientConsultAllowPersistsGrant`（当前 `:411`，`:424` 断言 `DeliveredAt == nil` 反向）整体替换为：

   ```go
   func TestClientConsultAllowPersistsGrant(t *testing.T) {
   	st, _, client, _ := newClientFixture(t, permgate.Verdict{Action: permgate.Consult}, true,
   		func(context.Context, string, string) approval.ConsultDecision {
   			return approval.ConsultDecision{Approve: true, Reason: "approved by fixture", ElapsedMS: 7}
   		})
   	req := permissionRequest("native-approver", "python3 script.py")
   	first, err := client.Request(context.Background(), req)
   	if err != nil {
   		t.Fatalf("Request: %v", err)
   	}
   	if first.Decision.Status != executor.ApprovalAllow || first.Decision.Rule != "approver" {
   		t.Fatalf("approver allow result=%+v", first.Decision)
   	}
   	fp := executor.ReuseFingerprint("v1", executor.PermFingerprint(executor.AdapterEvent{
   		PermissionID: req.NativeID, Text: req.Text, Perm: req.Perm,
   	}))
   	tk, err := st.GetTicket(first.Ref.ID)
   	if err != nil || tk.Answer == nil || *tk.Answer != "allow" {
   		t.Fatalf("审批者批准必须落 answer=allow 的工单: %+v err=%v", tk, err)
   	}
   	if tk.DeliveredAt != nil {
   		t.Fatalf("Request 返回 allow 时不得已送达（DeliveredAt 必须为空）: %+v", tk)
   	}
   	if prior, err := st.FindReusableGrant("task-client-fixture", fp); err != nil || prior != nil {
   		t.Fatalf("未送达 allow 不得被复用: prior=%+v err=%v", prior, err)
   	}
   	events, err := st.EventsFromAsc("task-client-fixture", 0, 100)
   	if err != nil {
   		t.Fatalf("EventsFromAsc: %v", err)
   	}
   	if eventCount(events, proto.EventTypeApproverDecision) != 1 ||
   		eventCount(events, proto.EventTypeTicketAnswered) != 1 ||
   		eventCount(events, proto.EventTypePermissionRequest) != 0 {
   		t.Fatalf("approver events=%+v", events)
   	}
   	decision := payloadMap(t, events[0].Payload)
   	if decision["decision"] != "approve" || decision["elapsed_ms"] != float64(7) {
   		t.Fatalf("approver_decision payload=%v", decision)
   	}
   	// 送达确认只在 AckDelivered。
   	if err := client.Acknowledge(context.Background(), executor.ApprovalAck{
   		Ref: first.Ref, NativeID: req.NativeID, Stage: executor.AckDelivered,
   	}); err != nil {
   		t.Fatalf("AckDelivered: %v", err)
   	}
   	tk, err = st.GetTicket(first.Ref.ID)
   	if err != nil || tk.DeliveredAt == nil {
   		t.Fatalf("AckDelivered 之后必须送达: %+v err=%v", tk, err)
   	}
   	second, err := client.Request(context.Background(), permissionRequest("native-approver-2", "python3 script.py"))
   	if err != nil {
   		t.Fatalf("reuse Request: %v", err)
   	}
   	if second.Decision.Status != executor.ApprovalAllow || second.Decision.Rule != "reuse" {
   		t.Fatalf("已送达同指纹应复用: %+v", second.Decision)
   	}
   }
   ```

   再改 `internal/agentd/approval_client_test.go#TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses`（当前 `:482`）：把 `:559-561` 的「`DeliveredAt` 必须非空」改为下述反向断言，并在该断言后增加 `FindReusableGrant` 未命中：

   ```go
   	if tk.DeliveredAt != nil {
   		t.Fatalf("Request 返回 allow 时不得已送达（DeliveredAt 必须为空）")
   	}
   	if prior, err := st.FindReusableGrant("T-app-reuse", expectedFP); err != nil || prior != nil {
   		t.Fatalf("未送达 allow 不得被复用: prior=%+v err=%v", prior, err)
   	}
   ```

   并在第二次请求 `req2`（当前 `:582`）之前插入显式送达确认：

   ```go
   	// P4：复用前必须显式送达；Request 本身不写 DeliveredAt。
   	if err := c.Acknowledge(context.Background(), executor.ApprovalAck{
   		Ref: res1.Ref, NativeID: req1.NativeID, Stage: executor.AckDelivered,
   	}); err != nil {
   		t.Fatal(err)
   	}
   	delivered, err := st.GetTicket(expectedTicketID)
   	if err != nil || delivered.DeliveredAt == nil {
   		t.Fatalf("AckDelivered 之后必须送达: %+v err=%v", delivered, err)
   	}
   ```

2. **跑红。** 运行：

   ```text
   go test ./internal/approval -run 'TestClientConsultAllowPersistsGrant' -count=1
   go test ./internal/agentd -run 'TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses' -count=1
   ```

   预期两条均红，失败信息命中 `Request 返回 allow 时不得已送达` 或 `未送达 allow 不得被复用`。把原始输出追加进台账；不得把该红改写成环境问题。

3. **最小实现（绿）。** 在 `internal/approval/client.go#Client.consult` 删除提前送达块（当前 `:373-376`）：

   ```go
   	if err := c.hooks.Store.MarkTicketDelivered(ticketID); err != nil {
   		c.logger().Error("审批者批准：标记送达失败", "task", c.taskID, "ticket", ticketID, "cause", err)
   		return c.escalate(ctx, ev, "审批者标记送达失败")
   	}
   ```

   并把紧随其后的日志（当前 `:379`）改成与事实一致：

   ```go
   	c.logger().Info("审批者批准决定已落库，尚未送达", "task", c.taskID, "ticket", ticketID)
   ```

   不改 `Client.Acknowledge`（`AckDelivered` 仍调 `MarkTicketDelivered`，`AckFormed`/`AckExecuted` 不写）。给 `consult` 补一行注释说明边界：

   ```go
   	// 送达时间戳归 Acknowledge(AckDelivered)（B233.7 第 58 条 / 冻结 #5、#7）；
   	// 本函数只负责形成并持久化决定，绝不在此写 delivered_at。
   ```

   给 `Client.Request` 的 AutoAllow 分支补边界注释（不改行为）：

   ```go
   	// 钩子只写审计与计数，不得做原生投递；OpenCode 的 RespondPermission
   	// 只由 adapter 的 authorizeNativePermission 一次完成（冻结 #3/#16）。
   ```

4. **跑绿。** 重跑第 2 步两条命令，预期 `ok`；再重跑基线命令，确认既有 `TestClientAutoAllowHasNoTicket`、`TestClientReuseRequiresDeliveredGrant`、`TestClientAcknowledgeStages`、`TestClientNoteDeliveryFailedTaskIsolation` 仍 `ok`。

### Task 1 接缝断言与反向变异

入口为声明缝 S2 的 `Client.Request`/`Client.consult`，断言读回真实 Store 的 `DeliveredAt`（可空指针区分「缺失」与「零值」）。反向变异：把 `MarkTicketDelivered` 加回 `consult`，`TestClientConsultAllowPersistsGrant` 与 `TestApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses` 必须红。仅断言 `Request` 返回 allow 不读 Store 不算通过。

## 4. Task 2：agentd 拆分 AutoAllow 审计与回传、组装只绑审计（S3/R1，覆盖 #16 生产侧）

### 文件与接口

- 生产：`internal/agentd/manager.go`、`internal/agentd/approval_client.go`。
- 测试：`internal/agentd/approval_client_test.go`、`internal/agentd/permgate_wire_test.go`。
- Consumes：Task 1 的 `Client.Request` 行为、`permgate.Verdict{Rule, Action}`、`permgate.RuleSafeCommand`、`m.appendEvent`/`m.st.AppendEvent`、`m.adapterFor`、`m.noteAutoAllowed`。
- Produces：`m.auditAutoAllowOnly(taskID, ev, verdict)`（只审计 + 计数）；`bindApproval` 的 `Hooks.AutoAllow` 指向它；`m.autoAllowPermission` 保持「审计 + 一次回传」，仅 `handlePermission` 的 AutoAllow 分支消费。

### 基线判据（已在当前基线亲自运行）

```text
go test ./internal/agentd -run 'Test(SafeCommandPermissionAuditsOnceWithoutTicket|AnsweredTicketReplayStillResponds|ApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses|ApproverApprovesPermissionWithoutWaking|ApprovePermissionAdapterForFailureNotesDeliveryFailed)' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	0.559s
```

实现后本 task 只跑：

```text
go test ./internal/agentd -run 'Test(ApprovalClientAutoAllowProductionHookDoesNotRespond|SafeCommandPermissionAuditsOnceWithoutTicket|SafeCommandAuditFailureStillResponds|AnsweredTicketReplayStillResponds|ApproverApprovesPermissionWithoutWaking|ApprovePermissionAdapterForFailureNotesDeliveryFailed|ApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses)' -count=1
```

### 实现步骤（每步 2–5 分钟）

1. **先写生产侧接缝测试（红）。** 在 `internal/agentd/approval_client_test.go` 追加：

   ```go
   // 缝 #16（生产组装侧）：经生产 bindApproval 的 AutoAllow 钩子不得回传原生应答。
   // 生产钩子是 auditAutoAllowOnly；RespondPermission 只能由 adapter 负责。
   // 反向变异：bindApproval 改回 m.autoAllowPermission → recordedPerms 非空，测试红。
   func TestApprovalClientAutoAllowProductionHookDoesNotRespond(t *testing.T) {
   	m, st, _, adapter := newTestManager(t)
   	taskID := "T-auto-prod"
   	work := t.TempDir()
   	now := time.Now().UTC()
   	mustCreateTask(t, st, &proto.Task{ID: taskID, RepoPath: work, WorkDir: work, Executor: "fake",
   		State: proto.TaskStateRunning, CreatedAt: now, UpdatedAt: now})
   	tmp := executor.TaskTmpDir(m.cfg.DataDir, taskID)
   	command := "go test ./... > " + filepath.Join(tmp, "out")
   	scope := executor.ApprovalScope{
   		Workdir:    work,
   		TaskDir:    taskDirOf(m, taskID),
   		TaskTmpDir: tmp,
   	}
   	c := m.bindApproval(taskID, executor.PolicySnapshot{Version: "v1", TaskID: taskID, Scope: scope})
   	res, err := c.Request(context.Background(), executor.ApprovalRequest{
   		NativeID: "native-auto-prod",
   		Text:     "Bash: " + command,
   		Perm:     &executor.PermRequest{Tool: executor.PermToolBash, Command: command},
   	})
   	if err != nil {
   		t.Fatalf("Request: %v", err)
   	}
   	if res.Decision.Status != executor.ApprovalAllow {
   		t.Fatalf("白名单命令应自动放行，实得 %+v", res.Decision)
   	}
   	if got := adapter.recordedPerms(); len(got) != 0 {
   		t.Fatalf("生产 AutoAllow 钩子不得回传原生应答，实得 %v", got)
   	}
   	if got := countEvents(mustEvents(t, st, taskID), proto.EventTypePermissionAutoAllow); got != 1 {
   		t.Fatalf("自动放行审计事件 = %d，want 1", got)
   	}
   	if _, err := st.GetTicket(taskID + ":native-auto-prod"); !errors.Is(err, store.ErrNotFound) {
   		t.Fatalf("免审不得建工单，err=%v", err)
   	}
   	if got := m.takeAutoAllowed(taskID); got != 1 {
   		t.Fatalf("P3：计数应跟审计走，实得 %d", got)
   	}
   }
   ```

2. **跑红。**

   ```text
   go test ./internal/agentd -run 'TestApprovalClientAutoAllowProductionHookDoesNotRespond' -count=1
   ```

   预期红，失败信息命中 `生产 AutoAllow 钩子不得回传原生应答`。记录原始输出。

3. **拆分生产函数。** 把 `internal/agentd/manager.go#Manager.autoAllowPermission`（当前 `:2302-2350`）改为下述两个函数，函数体除「计数位置」和「回传」外与现状逐字一致（保留 `appendEvent` 兜底与全部日志字段）：

   ```go
   // auditAutoAllowOnly 只写自动放行审计并累计计数，不碰原生连接。
   //
   // bindApproval 把本函数绑给 approval.Hooks.AutoAllow，供 OpenCode 入站使用：
   // OpenCode 的原生投递只由 adapter 的 authorizeNativePermission 完成一次。
   // 免审无工单（B249），因此这里不产 delivery_failed。
   //
   // 注意：noteAutoAllowed 跟审计走、不跟原生投递走（P3）——OpenCode 不再回传，
   // 计数若留在回传后就会从本段汇总里消失。
   func (m *Manager) auditAutoAllowOnly(taskID string, ev executor.AdapterEvent, verdict permgate.Verdict) {
   	m.log.Info("权限请求自动放行", "task", taskID, "perm", ev.PermissionID,
   		"action", verdict.Action.String(), "rule", verdict.Rule, "reason", verdict.Reason)
   	if verdict.Rule == permgate.RuleSafeCommand {
   		if ev.Perm == nil {
   			m.log.Error("白名单自动放行缺结构化权限载荷", "task", taskID,
   				"perm", ev.PermissionID, "rule", verdict.Rule)
   		} else {
   			payload := permissionAutoAllowPayload{
   				PermissionID: ev.PermissionID,
   				Permission:   permEventText(ev.Text),
   				Tool:         ev.Perm.Tool,
   				Command:      ev.Perm.Command,
   				Paths:        append([]string(nil), ev.Perm.Paths...),
   				Rule:         verdict.Rule,
   				Reason:       verdict.Reason,
   			}
   			appendEvent := m.appendEvent
   			if appendEvent == nil && m.st != nil {
   				appendEvent = m.st.AppendEvent
   			}
   			if appendEvent == nil {
   				m.log.Error("白名单自动放行审计无落库入口", "task", taskID,
   					"perm", ev.PermissionID, "rule", verdict.Rule)
   			} else if _, err := appendEvent(taskID, proto.EventTypePermissionAutoAllow, payload); err != nil {
   				m.log.Error("白名单自动放行审计落库失败", "task", taskID,
   					"perm", ev.PermissionID, "rule", verdict.Rule, "cause", err)
   			} else {
   				m.log.Info("白名单自动放行审计已落库", "task", taskID,
   					"perm", ev.PermissionID, "rule", verdict.Rule)
   			}
   		}
   	}
   	m.noteAutoAllowed(taskID)
   }

   // autoAllowPermission 是非 OpenCode 权威：审计 + 一次原生回传。
   // 只有 handlePermission 的 AutoAllow 分支调用它（冻结 #14）。
   //
   // 注意：没有工单可失败，因此回传失败不产 delivery_failed 事件；最常见的
   // 失败成因是订阅重放（同一权限请求被再次投递，而 executor 侧那次请求早已
   // 应答完毕），按 Warn 记录即可。adapterFor 失败意味着任务运行态已没，executor
   // 侧那次请求将无人应答——Error 级，同样无工单可失败。
   func (m *Manager) autoAllowPermission(taskID string, ev executor.AdapterEvent, verdict permgate.Verdict) {
   	m.auditAutoAllowOnly(taskID, ev, verdict)
   	ad, err := m.adapterFor(taskID)
   	if err != nil {
   		m.log.Error("自动放行：解析执行者失败，该权限请求将无人应答",
   			"task", taskID, "perm", ev.PermissionID, "cause", err)
   		return
   	}
   	actx, acancel := unaryCtx(context.Background())
   	defer acancel()
   	if err := ad.RespondPermission(actx, taskID, ev.PermissionID, "once", ""); err != nil {
   		m.log.Error("自动放行回传 executor 失败（多为订阅重放，请求已失效）",
   			"task", taskID, "perm", ev.PermissionID, "cause", err)
   		return
   	}
   	m.log.Info("自动放行已回传 executor", "task", taskID, "perm", ev.PermissionID)
   }
   ```

   `respondAutoAllowReplay`（当前 `:2685`）**不改**：它只回传、不重复审计，是重放路径（P5 保持）；不得在其中调用 `auditAutoAllowOnly`，否则会追加第二条审计事件并破坏 `hasPermissionAutoAllow` 的幂等标记。

4. **改组装点。** 在 `internal/agentd/approval_client.go#bindApproval`（当前 `:50`）把：

   ```go
   		AutoAllow:          m.autoAllowPermission,
   ```

   改为：

   ```go
   		AutoAllow:          m.auditAutoAllowOnly,
   ```

   并在 `bindApproval` 注释补一句：

   ```go
   	// AutoAllow 只审计、不回传：OpenCode 的原生投递归 adapter（冻结 #3/#16）。
   	// 非 OpenCode 的回传在 handlePermission 的 autoAllowPermission 路径。
   ```

5. **跑绿。**

   ```text
   go test ./internal/agentd -run 'Test(ApprovalClientAutoAllowProductionHookDoesNotRespond|SafeCommandPermissionAuditsOnceWithoutTicket|SafeCommandAuditFailureStillResponds|AnsweredTicketReplayStillResponds|ApproverApprovesPermissionWithoutWaking|ApprovePermissionAdapterForFailureNotesDeliveryFailed|ApprovalClientApproverAllowCreatesReusableGrantAndSecondRequestReuses)' -count=1
   ```

   预期 `ok`：生产钩子零回传；`handlePermission` AutoAllow 仍恰回传一次、重放后为两次；非 OpenCode `approvePermission` 仍 `markDelivered`。

### Task 2 接缝断言与反向变异

入口为声明缝 S3 的 `bindApproval` 产物 `Client.Request` 与 R1 的 `handlePermission`。`TestApprovalClientAutoAllowProductionHookDoesNotRespond` 用 `adapter.recordedPerms()` 锁「生产钩子回传次数为 0」；`TestSafeCommandPermissionAuditsOnceWithoutTicket` 锁非 OpenCode 恰一次/重放两次。反向变异：`bindApproval` 改回 `m.autoAllowPermission` → 前者红；`handlePermission` AutoAllow 分支去掉回传 → 后者红。内部计数断言（`takeAutoAllowed`）挂在 `Client.Request` 入口上，是附加断言，不顶替缝断言。

## 5. Task 3：OpenCode adapter 免审失败不上报、按决定闸门（S1，覆盖 #10/#11）

### 文件与接口

- 生产：`internal/executor/opencode/adapter.go`。
- 测试：`internal/executor/opencode/adapter_test.go`。
- Consumes：`executor.ApprovalDecision{Status, Rule}`、`executor.ApprovalAllow`、`executor.ApprovalDeny`、`deliveryFailureReporter`、`a.reportDeliveryFailure`、`a.RespondPermission`、`executor.AckDelivered`。
- Produces：`deliveryFailureHasTicket(dec) bool`；`authorizeNativePermission` 只在 `Status==Deny` 或 `Rule ∈ {"approver","reuse"}` 时上报失败；免审 Allow 失败走 Warn 分支、不上报、不产 `delivery_failed`。

### 基线判据（已在当前基线亲自运行）

```text
go test ./internal/executor/opencode -run 'Test(AuthorizeNativePermissionReportsDeliveryFailure|StartToPermissionFlow)' -count=1
ok  	github.com/Xsxdot/handoff/internal/executor/opencode	0.020s
```

实现后本 task 只跑：

```text
go test ./internal/executor/opencode -run 'Test(AuthorizeNativePermissionReportsDeliveryFailure|AuthorizeNativePermissionAllowDeliversOnce|AuthorizeNativePermissionAutoAllowFailureDoesNotReport|StartToPermissionFlow)' -count=1
```

### 实现步骤（每步 2–5 分钟）

1. **让 fake client 的 `Request` 决定可配置，并新增两支测试（红）。** 修改 `internal/executor/opencode/adapter_test.go#fakeApprovalClient`（当前 `:1775-1829`）：加字段 `decision executor.ApprovalDecision`，`Request` 按配置返回，零值缺省为 `{Status: ApprovalAllow, Rule: "approver"}`（即既有 `TestAuthorizeNativePermissionReportsDeliveryFailure` 代表一张有工单的批准）：

   ```go
   type fakeApprovalClient struct {
   	mu       sync.Mutex
   	reqs     []executor.ApprovalRequest
   	acks     []executor.ApprovalAck
   	failures []deliveryFailure
   	decision executor.ApprovalDecision
   }
   ```

   ```go
   func (f *fakeApprovalClient) setDecision(dec executor.ApprovalDecision) {
   	f.mu.Lock()
   	defer f.mu.Unlock()
   	f.decision = dec
   }

   func (f *fakeApprovalClient) Request(_ context.Context, req executor.ApprovalRequest) (executor.ApprovalResult, error) {
   	f.mu.Lock()
   	defer f.mu.Unlock()
   	f.reqs = append(f.reqs, req)
   	dec := f.decision
   	if dec.Status == "" {
   		dec = executor.ApprovalDecision{Status: executor.ApprovalAllow, Rule: "approver"}
   	}
   	return executor.ApprovalResult{
   		Ref:      executor.ApprovalRef{ID: "ref-1"},
   		Decision: dec,
   	}, nil
   }
   ```

   加 helper：

   ```go
   func ackStagePresent(acks []executor.ApprovalAck, stage executor.AckStage) bool {
   	for _, ack := range acks {
   		if ack.Stage == stage {
   			return true
   		}
   	}
   	return false
   }
   ```

   追加 Test A（allow 成功恰一次 + AckDelivered 在成功之后）：

   ```go
   func TestAuthorizeNativePermissionAllowDeliversOnce(t *testing.T) {
   	quietLog(t)
   	fs := newFakeServer(t)
   	taskID := "task-permission-allow-once"
   	ad, _ := startFakeRun(t, fs, taskID, t.TempDir(), t.TempDir())
   	r := ad.lookup(taskID)
   	approval := &fakeApprovalClient{}
   	approval.setDecision(executor.ApprovalDecision{Status: executor.ApprovalAllow, Rule: "approver"})
   	r.approval = approval

   	fs.push(permissionAskedEvent("perm-allow-once", "bash", "echo once"))
   	deadline := time.After(2 * time.Second)
   	for {
   		_, acks, _ := approval.snapshot()
   		if ackStagePresent(acks, executor.AckDelivered) {
   			break
   		}
   		select {
   		case <-deadline:
   			t.Fatal("等待 AckDelivered 超时")
   		case <-time.After(10 * time.Millisecond):
   		}
   	}
   	calls := fs.perms()
   	if len(calls) != 1 {
   		t.Fatalf("一次权限请求应恰发 1 次原生应答，实得 %d", len(calls))
   	}
   	if !strings.Contains(calls[0].body, `"response":"once"`) {
   		t.Fatalf("应答体=%q，应含 once", calls[0].body)
   	}
   	_, _, failures := approval.snapshot()
   	if len(failures) != 0 {
   		t.Fatalf("成功路径不得上报投递失败: %+v", failures)
   	}
   }
   ```

   追加 Test B（免审失败不上报，#11）：

   ```go
   // 缝 #11：免审 AutoAllow（无工单）回传失败不得上报 NoteDeliveryFailed。
   // 反向变异：去掉 decide 闸门无条件上报 → failures 非空，测试红。
   func TestAuthorizeNativePermissionAutoAllowFailureDoesNotReport(t *testing.T) {
   	quietLog(t)
   	fs := newFakeServer(t)
   	fs.failPermissionResponses(http.StatusBadGateway)
   	taskID := "task-permission-autoallow-fail"
   	ad, _ := startFakeRun(t, fs, taskID, t.TempDir(), t.TempDir())
   	r := ad.lookup(taskID)
   	approval := &fakeApprovalClient{}
   	approval.setDecision(executor.ApprovalDecision{Status: executor.ApprovalAllow, Rule: "safe-command"})
   	r.approval = approval

   	fs.push(permissionAskedEvent("perm-autoallow-fail", "bash", "go test ./..."))
   	deadline := time.After(2 * time.Second)
   	for len(fs.perms()) == 0 {
   		select {
   		case <-deadline:
   			t.Fatal("等待原生应答尝试超时")
   		case <-time.After(10 * time.Millisecond):
   		}
   	}
   	time.Sleep(100 * time.Millisecond)
   	_, acks, failures := approval.snapshot()
   	if len(failures) != 0 {
   		t.Fatalf("免审 AutoAllow 无工单，失败不得上报 delivery_failed: %+v", failures)
   	}
   	if ackStagePresent(acks, executor.AckDelivered) || ackStagePresent(acks, executor.AckExecuted) {
   		t.Fatalf("投递失败后不得 AckDelivered/AckExecuted: %+v", acks)
   	}
   }
   ```

2. **跑红。**

   ```text
   go test ./internal/executor/opencode -run 'Test(AuthorizeNativePermissionAutoAllowFailureDoesNotReport|AuthorizeNativePermissionAllowDeliversOnce)' -count=1
   ```

   预期 `TestAuthorizeNativePermissionAutoAllowFailureDoesNotReport` 红（现状无条件上报 `failures==1`）；`AllowDeliversOnce` 预期绿（既有成功路径已恰一次），它是 #16 adapter 侧的行为锁，不属于红绿周期。记录原始输出。

3. **加闸门并松绑上报点。** 在 `internal/executor/opencode/adapter.go` 增加包内 helper（放在 `reportDeliveryFailure` 附近，不 import `permgate`）：

   ```go
   // deliveryFailureHasTicket 判定一次终态决定背后是否有可恢复的持久工单。
   // 免审 AutoAllow 由审批 client 直接返回且不建工单（B249），失败不产
   // delivery_failed（冻结 #11）。adapter 不得 import permgate，只按既有
   // ApprovalDecision.Rule/Status 判：
   //   - deny 一定来自有工单的裁决（人工/审批者/升级）
   //   - allow 只有 approver / reuse 有工单；safe-command / self-command 以及
   //     范围内的自动放行（Rule 为空）都无工单
   func deliveryFailureHasTicket(dec executor.ApprovalDecision) bool {
   	if dec.Status == executor.ApprovalDeny {
   		return true
   	}
   	return dec.Rule == "approver" || dec.Rule == "reuse"
   }
   ```

   把 `authorizeNativePermission` 的终态回传失败分支（当前 `:851-855`）改为：

   ```go
   	if err := a.RespondPermission(ctx, r.taskID, ev.PermissionID, decision, reason); err != nil {
   		a.log.Error("RespondPermission 失败", "task", r.taskID, "perm", ev.PermissionID, "cause", err)
   		if deliveryFailureHasTicket(dec) {
   			a.reportDeliveryFailure(ctx, r, ev.PermissionID, err)
   		} else {
   			a.log.Warn("免审 AutoAllow 回传失败，无工单可恢复，不产 delivery_failed",
   				"task", r.taskID, "perm", ev.PermissionID, "rule", dec.Rule, "cause", err)
   		}
   		return
   	}
   ```

   **不改** `Authorize` 失败后的 reject 分支（当前 `:834-843`）：那是审批未形成时的拒绝回传，不是 AutoAllow 终态，保持既有 `reportDeliveryFailure`。给该分支补一行注释，说明它不属于 #11 闸门。

4. **跑绿。**

   ```text
   go test ./internal/executor/opencode -run 'Test(AuthorizeNativePermissionReportsDeliveryFailure|AuthorizeNativePermissionAllowDeliversOnce|AuthorizeNativePermissionAutoAllowFailureDoesNotReport|StartToPermissionFlow)' -count=1
   ```

   预期 `ok`：有工单决定失败仍上报 1 次；免审失败 0 次；allow 成功恰一次且 `AckDelivered` 在成功之后。

### Task 3 接缝断言与反向变异

入口为声明缝 S1 的 `authorizeNativePermission`（经真实 `permission.asked` → `Authorize` 驱动）。`AllowDeliversOnce` 锁「一次权限请求恰一次原生应答」，`AutoAllowFailureDoesNotReport` 锁「无工单失败不产事件」。反向变异：`deliveryFailureHasTicket` 改成恒真 → `AutoAllowFailureDoesNotReport` 红；让 adapter 在 allow 成功前后各回传一次 → `AllowDeliversOnce`/`TestStartToPermissionFlow` 红。仅断言 `RespondPermission` 被调用、不看 fake server 收到的 path/body 不算通过。

## 6. Task 4：非 OpenCode 兼容回归与整卡接缝审计（无生产改动）

### 文件与接口

- 生产：无。
- 测试：复用 `internal/agentd/permgate_wire_test.go`、`internal/agentd/approver_test.go`、`internal/approval/client_test.go`。
- Consumes：Task 1–3 的全部改动。
- Produces：无新符号；确认冻结 #14（`handlePermission` AutoAllow 仍回传一次）与既有 `approvePermission`/`waitPermission`/`markDelivered` 行为。

### 步骤

1. 跑兼容回归：

   ```text
   go test ./internal/agentd -run 'Test(SafeCommandPermissionAuditsOnceWithoutTicket|SafeCommandAuditFailureStillResponds|AnsweredTicketReplayStillResponds|ApproverApprovesPermissionWithoutWaking|ApproverEscalateFallsThroughToReviewer|ApprovePermissionAdapterForFailureNotesDeliveryFailed|NilApproverKeepsCurrentBehavior)' -count=1
   go test ./internal/approval -run 'TestClient(AutoAllowHasNoTicket|ReuseRequiresDeliveredGrant|AcknowledgeStages|NoteDeliveryFailedTaskIsolation|ConsultFailClosed)' -count=1
   ```

   预期均 `ok`。`ApproverApprovesPermissionWithoutWaking` 走 `approvePermission` → `markDelivered`，证明非 OpenCode 权威未被迁移。

2. 跑编译面：

   ```text
   go build ./internal/approval ./internal/executor ./internal/executor/opencode ./internal/agentd ./internal/store
   go vet ./internal/approval ./internal/executor/opencode ./internal/agentd
   ```

3. 对照 §8.3 双向接缝表逐条核：S1 由 Task 3 两支测试锁；S2 由 Task 1 两支测试锁；S3 由 Task 2 的 `TestApprovalClientAutoAllowProductionHookDoesNotRespond` 锁；R1 由 `TestSafeCommandPermissionAuditsOnceWithoutTicket` 锁。任何一条缝缺测试即停回协调者。

## 7. 序列化/投影边界清单

本卡不新增数据字段，手写投影均不变；只需断言「事件存在/缺失」这一行为事实，不适用「字段缺失 vs 零值」的 roundtrip（无新字段，故不新增 roundtrip 属性测试）。

| # | 边界 | 文件/符号 | 本卡必须断言 |
|---|---|---|---|
| 1 | AutoAllow 审计 → Store 事件 | `internal/agentd/manager.go#permissionAutoAllowPayload` / `auditAutoAllowOnly` | `permission_auto_allow` 事件恰 1 条、payload 字段与现状一致；**无** `delivery_failed`。 |
| 2 | 投递失败 → `delivery_failed` 事件 | `internal/agentd/manager.go#Manager.NoteDeliveryFailed` / `deliveryFailedPayload` | 免审 AutoAllow 失败**不存在** `delivery_failed` 事件；有工单决定失败仍存在该事件（store 事件类型计数）。 |
| 3 | 审批决定 → 工单 answer | `internal/approval/client.go#ticketAnsweredPayload` | `answer == "allow"` 严格相等；`DeliveredAt` 由 `Acknowledge(AckDelivered)` 单独写。 |
| 4 | 复用查询 | `internal/store/store.go#FindReusableGrant` | 未送达 allow 查不到；`Acknowledge(AckDelivered)` 后才查到（读取真实 Store 行的 `delivered_at IS NOT NULL`）。 |

## 8. 缺陷族对抗审查

| 缺陷族 | 对抗问题 | 结论与测试归属 |
|---|---|---|
| 生命周期/状态机中断 | 提前标记送达是否会在重启前留下「已送达」假事实？ | 是。Task 1 删除 `consult` 提前写入；`Request` 返回与 `RespondPermission` 之间崩溃由 `UndeliveredAnswers` + resume 补投。该崩溃窗口真实行为**未验证，需真机**。 |
| 静默失败/误导报错 | `consult` 日志是否仍报「已送达」？免审失败是否被读成错误？ | Task 1 改日志为「决定已落库，尚未送达」；Task 3 免审失败按 Warn，不产事件；`MarkTicketDelivered` 对无工单 ref 是 0 行 no-op（contract §8.2 C-4）。 |
| 跨平台假设 | 是否解析路径/进程组/权限模型？ | 无。本卡只改审批/投递时序；OpenCode 原生 HTTP 跨平台归真机清单。 |
| 假红/假绿测试 | 是否用替身钩子冒充生产钩子？是否只断言返回不读 Store？ | Task 2 的 #16 断言强制经 `bindApproval`；Task 1 读回真实 Store `DeliveredAt`（可空指针）。三处变异各对应一条可变红测试。 |
| 门禁绕过 | `DeliveredAt` 是否还有其它提前写入口？ | 生产写入只剩 `Client.Acknowledge(AckDelivered)` 与 `Manager.markDelivered`（均在成功 `RespondPermission` 之后）；本卡只删 `consult` 这一处违约写入。 |
| 枚举新值过白名单 | 是否新增 EventType/TaskState/AckStage/ApprovalStatus/kind？ | 无，复用既有 `delivery_failed`/`permission_auto_allow`/`allow`/`delivered`。 |
| 承重安全属性 | 「送达只在原生回传成功后」「一次请求一次投递」「免审不产假事件」是否各有能变红的测试？ | 有：Task 1 `TestClientConsultAllowPersistsGrant`；Task 2 `TestApprovalClientAutoAllowProductionHookDoesNotRespond`；Task 3 `TestAuthorizeNativePermissionAutoAllowFailureDoesNotReport`。 |
| 项目第六族 webview/平台 | 是否改 d_web/浏览器 API？ | 无。 |

**P1 拍板附带影响（记录，不改裁决）**：闸门按 `Deny` 或 `Rule ∈ {"approver","reuse"}` 上报。人工协调者在 OpenCode 上的放行经 `Await` 返回 `Rule==""` 的 Allow，与范围内的自动放行同形，故其回传失败本卡不再上报 `delivery_failed`。这是 P1 为排除范围内自动放行（最普遍的免审）而接受的取舍；冻结 #11/#12 只覆盖 AutoAllow 与 approver allow，故不违反冻结条目。若后续要求覆盖人工放行失败，需新卡（要一个「有无工单」的稳定信号，P1-D 被否）。

## 9. 类型标注与真机清单

按子系统类型验收，不以编译通过代替：

- 逻辑型 `d_orchestration`：`Request`/`consult` 不写送达、`bindApproval` 生产钩子零回传、`handlePermission` AutoAllow 仍回传一次。
- 边界型 `d_execution`：一次权限请求一次 `RespondPermission`；免审失败不产 `delivery_failed`；`AckDelivered` 只在成功后。
- 契约型 `d_execution_contract`/`d_protocol`/`d_policy`：不改签名/字段/常量。

以下真机/跨机项目在本节点**未验证**，实施者在最终验收记录逐项结果，不得写成单元测试 pass：

| 项目 | 状态 |
|---|---|
| OpenCode 免审真实任务只收到一次原生 allow | 未验证 |
| OpenCode 审批者放行真实 Store 送达时序 | 未验证 |
| OpenCode 原生应答 4xx/断连后 `UndeliveredAnswers` + `handoff resume` 补投 | 未验证 |
| `consult` 返回到 `RespondPermission` 成功之间重启 agentd 的崩溃窗口 | 未验证 |
| agy（HOME+workspace 双 hook）免审与已答重放行为不回退 | 未验证 |
| Linux/macOS/Windows 执行机 OpenCode HTTP 与 `RespondPermission` 路由 | 未验证 |

## 10. Spec/contract 覆盖矩阵

| 冻结条目 | 归属 task |
|---|---|
| #1 四方法签名不变；#2 Hooks 字段不变 | 全体（不改契约面） |
| #3 `bindApproval` 生产 AutoAllow 不 `RespondPermission` | Task 2 |
| #4 `Request` AutoAllow 不建工单 | Task 1（保持）+ Task 2 断言 |
| #5 `consult` 返回前不 `MarkTicketDelivered` | Task 1 |
| #6 `Request` 返回 approver allow 时 `DeliveredAt==nil` | Task 1 |
| #7 `Request`/`consult` 成功路径不写送达 | Task 1 |
| #8/#9 `Acknowledge` 仅 `AckDelivered` 写送达 | Task 1（保持 + 断言） |
| #10 一次 PermissionID 恰一次 `RespondPermission` | Task 3 |
| #11 免审终态失败不 `NoteDeliveryFailed`/不产事件 | Task 3 |
| #12 approver allow 失败进 `UndeliveredAnswers` | Task 1（删提前送达即为前提） |
| #13 `FindReusableGrant` 仍严格 SQL | Task 1 断言 |
| #14 `handlePermission` AutoAllow 仍回传一次 | Task 2 + Task 4 |
| #15 不新增 HTTP/CLI/事件/领域 | 全体 |
| #16 生产组装链全程恰一次、生产钩子 0 次 | Task 2（agentd 侧）+ Task 3（adapter 侧） |

spec 用户故事：故事 1 → Task 2+3；故事 2 → Task 1；故事 3 → Task 2+4。

## 11. 收口自检（实现者完成后执行）

1. 每个 task 只跑 §3–§6 声明的包测试；全量不归任何单 task。
2. 卡级汇总：`go test ./internal/approval ./internal/executor/opencode ./internal/agentd`，以及 §6 的 `go build`/`go vet`。
3. 对照 §8.3 做双向接缝审计：每支测试入口属于至少一条声明缝（或 R1）；每条缝至少有一支真实入口断言。内部计数断言（`takeAutoAllowed`）只附加、不顶替。
4. 日志一律走项目 `slog` 结构化字段，禁 `fmt.Print*`；入口、外部调用前后、错误分支、成功路径均带上下文。
5. 新函数补职责/参数/返回/注意事项注释；非显然逻辑补「为什么」。
6. 运行 `git diff --check`；对本计划做仓内占位语句扫描（`TBD`/`TODO`/`加适当的错误处理`/`同 Task`），命中即处理后再提交。
7. 把命令原始输出、失败尝试、判断与提交命令追加进台账 `docs/superpowers/ledgers/2026-09-11-b233.12-plan-ledger.md`；提交时台账与本计划同批纳入。

## 12. 派发前自审

本计划没有任何需要驱动派发系统自身的验收步骤：全部验收为本地 `go test`/`go build`/`go vet` 与读取真实 Store 行。无「由协调者执行、不派发」的步骤。
