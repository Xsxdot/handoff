# B156.2.3 实现计划：C3 d_ledger(ledgerstep)·终态遗留裁决补解析接入（欠账 #7 其余半）

**状态**：plan 节点产出（charter 流），2026-08-26。
**有效基线分支**：`feature/b156-workbench`（合并目标以此为准）。本计划在 `cards/B156.2.3-charter` 上执行，HEAD=c2e91ca0（含 C1 绑定租约、C2 事件写入点补肉、breakdown Pointer 归属修订的合入）。
**上游输入**：spec `2026-08-25-b156.2-collaboration-rooms-design.md`（已批准）；契约 `b156.2-contract.md`（含 §11 微修订）；breakdown `b156.2-breakdown.md`（C3 卡四段式）。
**台账**：`docs/superpowers/ledgers/ledger-b156.2.3-plan.md`（探针与判据证据在案）。

本卡只做一件事：给 `NodeStep.RunOnce` 的入口加「终态遗留裁决」中间态检测与处置。中间态的账本半承重件 `Store.EnsureComment` 已由 C2 填肉合入功能线（commit e917933e），本卡只接线。**签名零改动、schema 零改动、零新导出符号。**

本波并行：与 C4（B156.2.4，重构 service.go 沉放结构）同波、与 C4 零文件交集；C5 押后。

---

## 0. 冻结物核对（对照冻结物逐条）

| 冻结条目 | 本计划的对应 | 越界？ |
| --- | --- | --- |
| 契约 §3.7 中间态定义：卡 Status∈{已完成,终止} 且存在 status=open 的 Decision | T3.1 检测条件（`card.Status == StatusDone || card.Status == StatusClosed` ∧ 按卡过滤后的 open 裁决非空，实现块内联） | 否 |
| 契约 §3.7 处置：RunOnce 入口检测到中间态→转等人，reason 固定前缀「终态遗留裁决」，timeline 留说明评论 | T3.1 `haltForHumanEnsure`（reason 前缀 + EnsureComment 幂等评论 + MarkNeedsHuman） | 否 |
| 契约 §3.7 绝不伪造 decision_answered（答案是用户的事实，系统不代答） | T3.1 负断言：全路径（卡事件流）无 `EvDecisionAnswered`。**本条是稳定假绿的温床，配正断言（转等人真的发生 + reason 前缀）两条一起才有牙（协调者本轮补充第 1 条）** | 否 |
| 契约 §3.7 幂等判据：重复驱动同一张卡不产生第二条说明评论，落法=EnsureComment 同事务查后写 | T3.1 幂等测试（两次驱动恰 1 条 EvComment）+ 变异靶（EnsureComment 换 AddComment 应翻红——**C2 已填肉，本条变异现在可真跑，协调者本轮补充第 2 条**） | 否 |
| 契约 §3.7「供 ledgerstep 直接经 St 使用，不经 collab 门面」 | 测试入口落在 `NodeStep.RunOnce`（spec 测试接缝清单 #4 环节执行体缝），不经 collab | 否 |
| 契约 §4 补解析块 3 条（条 1 转等人 reason 前缀 / 条 2 幂等 / 条 3 无 decision_answered） | T3.1 三支测试逐条对应 | 否 |
| breakdown C3 ④「open 判定复用 ListDecisions(openOnly) 按卡过滤」 | T3.1 实现块：`St.ListDecisions(true)` 后按 `d.CardID == cardID` 过滤 | 否 |
| breakdown C3 有界文件集：node.go ＋ node_test.go | §2 一致 | 否 |
| spec 测试接缝清单 #4（环节执行体缝：调用方 handoff card dispatch --step） | 三支测试入口全部是 `NodeStep.RunOnce`（缝内） | 否 |

零触碰点名（照抄 breakdown）：d_protocol（本轮不改一字）、d_sessions、d_collab 生产代码、d_gateway、d_cli、d_web、`internal/ledger` 除 events.go 已由 C2 填肉外零触碰。**只动 `internal/ledgerstep/node.go` 及其测试。**

## 1. 现状查证（全部亲读亲测，出处可复查）

| # | 事实 | 出处 |
| --- | --- | --- |
| 1 | RunOnce 入口结构：读卡(:136-140) → `!n.Node.Dispatch` 报错(:143-150) → 基线人工清单 → 回合封顶 → 派发 | `internal/ledgerstep/node.go` |
| 2 | haltForHuman 落卡三件套先例：gatedWrite → AddComment(body 非空) → MarkNeedsHuman → 返回 Outcome{ActionNeedsHuman} | `node.go:81-98` |
| 3 | 当前 node.go 无任何补解析/ListDecisions/EnsureComment/decision_answered 引用（grep 全包零命中） | 台账实测 |
| 4 | EnsureComment 已填肉：同卡内 dedupe_key 查后写，二次调用返回 false | `internal/ledger/events.go:228-280`（C2 合入 e917933e） |
| 5 | ListDecisions(openOnly=true) 全表 open 裁决；Decision 含 ID/CardID/Status 字段 | `internal/ledger/decisions.go:93-104`、`types.go:271-282` |
| 6 | 终态常量：StatusDone=已完成 / StatusClosed=终止 | `types.go:16-17` |
| 7 | 终态入口：MoveCard 到已完成（无 gate 拦截，move_test.go 先例）/ CloseCard 到终止（reason 受控词表 {取消,废弃,搁置}） | `move.go:17`、`cards.go:658-679` |
| 8 | 事件常量：EvComment/EvDecisionAnswered | `types.go:58,62` |
| 9 | ledgerstep 测试夹具：`nodeLedger(t)`（开真 SQLite+种子 bug 工作流+建卡）、`newNodeStep(t,st,node,msg,err)`（注入假 Dispatch/Await）；node_test.go 既有 import 已含 context/fmt/strings/testing/ledger | `node_test.go:12-22,234-249` |
| 10 | runner 对纯人工列直接调 RunOnce 且不装配 WriteGate/Session/runlock——终态叶子列被再驱动走这条 | `runner.go:91-95` |
| 11 | 基线绿：`go build ./...`=BUILD_OK；`go test ./internal/ledgerstep/...`=ok 1.198s | 台账原文 |
| 12 | 图闸现状：`graph check --view cards-B156.2-charter-4` EXIT=0 fails=0 | 台账原文 |
| 13 | 探针实测：中间态（终态+open 裁决）可构造；今天 RunOnce 不拦、走派发→「裁决解析失败」，reason 前缀断言翻红 | 台账探针原文 |

**判据已在基线验过**（plan 轮探针，台账原文）：三支测试的失败断言今天全部对现状跑红（reason 前缀 / 不得派发 / 评论恰一条）；「中间态可构造」探针 PASS 证明夹具路径（OpenDecision+MoveCard+CloseCard+ListDecisions 过滤）在测试里真实可走。

## 2. 有界文件集（上下文预算检查）

| 文件 | 动作 | 归属 task |
| --- | --- | --- |
| `internal/ledgerstep/node.go` | 入口加检测 + 新私有 helper haltForHumanEnsure + import strings | T3.1 |
| `internal/ledgerstep/node_test.go` | 追加 3 个测试函数 | T3.1 |

圈得出，两文件同包。与 C4 零交集（§6）。**测试文件不用另起**——node_test.go 是既有缝内夹具的家（与 breakdown 划界一致）。

## 3. Task 分解

### T3.1 终态遗留裁决补解析接入（契约 §3.7 + §4 补解析块 3 条）

**Interfaces**

- Produces（全部为行为，零新导出符号）：
  - `RunOnce(ctx, cardID)` 对中间态返回 `Outcome{Action: ActionNeedsHuman, Reason: "终态遗留裁决：..."}`（前缀固定）；卡事件流新增恰一条 EvComment（含「终态遗留裁决」正文）+ 一条 EvNeedsHuman；**不产生任何 EvDecisionAnswered**；不派发、不落裁决、不改状态。
  - 新私有 helper：`func (n *NodeStep) haltForHumanEnsure(cardID, reason, dedupeKey, body string) (Outcome, error)`（与 haltForHuman 同构，说明评论走 EnsureComment 幂等写）。
- Consumes（既有符号，精确签名）：
  - `func (s *Store) GetCard(id string) (Card, error)`（cards.go:410）
  - `func (s *Store) ListDecisions(openOnly bool) ([]Decision, error)`（decisions.go:93）
  - `func (s *Store) EnsureComment(cardID, dedupeKey, body, actor string) (bool, error)`（events.go:237；C2 已填肉）
  - `func (s *Store) MarkNeedsHuman(cardID, reason, actor string) error`（events.go:322）
  - `const StatusDone = "已完成"` / `const StatusClosed = "终止"`（types.go:16-17）
  - `type Decision struct { ID int64; CardID string; ...; Status string }`（types.go:271-282）
  - `func (n *NodeStep) gatedWrite(action string) error`（node.go:74）/ `func (n *NodeStep) actor() string`（node.go:70）
  - 包内 `log/slog` logger（RunOnce 已建 logger）

**行为语义（实现前读三遍）**

1. 检测点=RunOnce 读卡之后、`!n.Node.Dispatch` 检查之前（「入口检测」字面 + 真实恢复场景是终态叶子列被再驱动，而 runner.go:91 对纯人工列直接调 RunOnce 不装配 WriteGate——检测放 Dispatch 检查之后会被「纯人工列不可执行」的配置错误吞掉）。
2. 条件=卡 Status∈{已完成,终止} ∧ 该卡存在 status=open 的 Decision。open 判定按契约走 `ListDecisions(openOnly=true)` 全表取 open 再按 `CardID==cardID` 过滤（不做 DecisionsOf——那含已答复的，与「open 判定」字面不符）。
3. 处置=转等人（reason 固定前缀「终态遗留裁决」）+ timeline 说明评论（走 EnsureComment 幂等留痕，**绝不走 AddComment**——否则重复驱动刷屏）+ MarkNeedsHuman。
4. dedupeKey=「终态遗留裁决:」+cardID（**卡级**，跨节点驱动同一张卡也只一条说明；不带时间戳、不带节点名，保证同卡重复驱动命中同一键）。C2 plan 建议的 `<前缀>:<卡号>:<节点名>` 含节点名会随驱动节点换名而产生第二条，与契约「重复驱动同一张卡不产生第二条」字面不符，故取卡级（C2 注释明示「建议非冻结，C3 自定」）。
5. 绝不伪造 decision_answered：本路径一个 Decision 写操作都不做。
6. 检测失败（ListDecisions 报错）直接返回 error（与 RunOnce 其它读错误同形，不吞）。

**步骤（红绿模板：锁缝断言步骤，法定适用）**

1. （~2min）基线复核：跑 `go test ./internal/ledgerstep/...` → ok（台账原文）；grep 确认 node.go 仍无补解析引用：`grep -n "终态遗留\|ListDecisions\|EnsureComment" internal/ledgerstep/node.go` 应零命中。
2. （~5min）写失败测试：`internal/ledgerstep/node_test.go` 末尾追加下方三个测试函数（import 块已含 context/fmt/strings/testing/ledger，零新增）。
3. （~1min）跑红：`go test ./internal/ledgerstep/ -run 'TestNodeStepLeftover' -v`。预期三支 FAIL（今天全走派发→Await("")→「裁决解析失败」转等人路径，Action 恰为 needs_human 故动作断言假绿、但行为断言翻红）：HaltsForHuman 三条断言红——「reason 应以『终态遗留裁决』开头，实得 裁决解析失败」+「绝不允许继续派发」+「应恰一条含『终态遗留裁决』的说明评论，实得 1 条 body=裁决解析失败…」（正文不含前缀）；HaltIsIdempotent 两条驱动各落一条「裁决解析失败」评论→「重复驱动两次后应仍恰 1 条说明评论…实得 2」；OnTerminatedCard「reason 前缀」红。贴原文到台账。
4. （~4min）最小实现：按下方代码块改 `internal/ledgerstep/node.go`（新增 import `"strings"`；新增 haltForHumanEnsure；RunOnce 读卡后插检测块）。
5. （~1min）跑绿：同步骤 3 命令全 PASS；再跑 `go test ./internal/ledgerstep/...` 确认无回归（既有 15 支测试不受影响——它们的卡全在 StatusTodo 且无裁决）。贴原文到台账。
6. （~3min）变异自验（两发，每发先 grep 断言命中唯一，改语义不改「有没有用」，编译过后再跑行为断言，完事 revert 回绿灯）：
   - 变异甲（正断言有牙）：把检测守卫整段删除（或条件取反为 `card.Status != StatusDone && card.Status != StatusClosed`）→ TestNodeStepLeftoverDecisionHaltsForHuman 必须红（正断言：应转等人/不得派发/评论含前缀）。**只留负断言（无 decision_answered）的版本必须是绿的——这正是「反面断言假绿」实锤，正断言把它锁住。**
   - 变异乙（幂等有牙）：haltForHumanEnsure 内 `n.St.EnsureComment` 改成 `n.St.AddComment`（签名不适配就 `.AddComment(cardID, body, "普通", n.actor())`）→ TestNodeStepLeftoverDecisionHaltIsIdempotent 必须红（两次驱动后评论应恰 1 条，实得 2 条）。C2 填肉后本条变异才可真跑。
   - 两发均记录输出原文到台账并 revert，`git status --short` 干净后再走下一步。
7. （~1min）日志与注释检查：新代码块内已含结构化日志（检测命中 logger.Warn）与注释；`grep -n "Println\|fmt.Print" internal/ledgerstep/node.go` 应零命中。
8. （~1min）提交：`git add internal/ledgerstep/node.go internal/ledgerstep/node_test.go && git commit`（message 见 §11）。

**完整代码块——追加到 `internal/ledgerstep/node_test.go` 末尾**

```go
// TestNodeStepLeftoverDecisionHaltsForHuman 终态遗留裁决补解析（契约 §3.7
// 正断言）：卡已到终态但挂着 open 裁决（协调者死亡窗口产物）时，RunOnce 入口
// 必须转等人、reason 以「终态遗留裁决」开头，绝不允许继续派发，绝不伪造
// decision_answered（答案是用户的事实，系统不代答）。负断言（全路径事件流无
// decision_answered）单独成立时是稳定假绿——它不能证明补解析跑过；正断言
// 才是牙，两条一起锁。
func TestNodeStepLeftoverDecisionHaltsForHuman(t *testing.T) {
	s, c := nodeLedger(t)
	cardID := c.ID
	if _, err := s.OpenDecision(cardID, "终态遗留请示", []string{"选项A", "选项B"}, "step:review"); err != nil {
		t.Fatalf("开裁决: %v", err)
	}
	if err := s.MoveCard(cardID, ledger.StatusDone, "", "test"); err != nil {
		t.Fatalf("移到已完成: %v", err)
	}

	dispatched := false
	step := &NodeStep{
		St:   s,
		Node: ledger.NodeDef{Name: "待审阅", Dispatch: true, Verdict: true, Template: "review-generic"},
		Dispatch: func(context.Context, ledger.Card, ledger.NodeDef) (string, string, error) {
			dispatched = true
			return "linux-01", "task-1", nil
		},
		Await: func(context.Context, string, string) (string, error) { return "", nil },
	}
	out, err := step.RunOnce(context.Background(), cardID)
	if err != nil {
		t.Fatalf("RunOnce: %v", err)
	}
	if out.Action != ActionNeedsHuman {
		t.Fatalf("正断言：应转等人，实得 %q", out.Action)
	}
	if !strings.HasPrefix(out.Reason, "终态遗留裁决") {
		t.Fatalf("正断言：reason 应以「终态遗留裁决」开头，实得 %q", out.Reason)
	}
	if dispatched {
		t.Fatal("终态遗留裁决必须在派发之前拦下：绝不允许继续派发")
	}

	events, err := s.EventsFromAsc([]string{cardID}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	commentCount, answeredCount := 0, 0
	var commentBody string
	for _, ev := range events {
		switch ev.Type {
		case ledger.EvComment:
			commentCount++
			commentBody = string(ev.Payload)
		case ledger.EvDecisionAnswered:
			answeredCount++
		}
	}
	if commentCount != 1 || !strings.Contains(commentBody, "终态遗留裁决") {
		t.Fatalf("应恰一条含「终态遗留裁决」的说明评论，实得 %d 条 body=%q", commentCount, commentBody)
	}
	if answeredCount != 0 {
		t.Fatalf("负断言：全路径事件流不得出现 decision_answered，实得 %d", answeredCount)
	}
}

// TestNodeStepLeftoverDecisionHaltIsIdempotent 幂等判据（契约 §4 补解析第 2
// 条）：重复驱动同一张卡不产生第二条说明评论（EnsureComment dedupe_key 生效）。
// 变异靶：实现把 EnsureComment 换回 AddComment → 本测试「恰 1 条评论」翻红。
// dedupeKey 必须是卡级且不带时间戳/节点名，否则同卡重驱动会换键而漏掉第二条。
func TestNodeStepLeftoverDecisionHaltIsIdempotent(t *testing.T) {
	s, c := nodeLedger(t)
	cardID := c.ID
	if _, err := s.OpenDecision(cardID, "终态遗留请示", []string{"选项A"}, "step:review"); err != nil {
		t.Fatalf("开裁决: %v", err)
	}
	if err := s.MoveCard(cardID, ledger.StatusDone, "", "test"); err != nil {
		t.Fatalf("移到已完成: %v", err)
	}
	step := newNodeStep(t, s, ledger.NodeDef{Name: "待审阅", Dispatch: true, Verdict: true, Template: "review-generic"}, "", nil)
	for i := 0; i < 2; i++ {
		out, err := step.RunOnce(context.Background(), cardID)
		if err != nil || out.Action != ActionNeedsHuman {
			t.Fatalf("第 %d 次驱动: %v %+v", i+1, err, out)
		}
	}
	events, err := s.EventsFromAsc([]string{cardID}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	commentCount, answeredCount := 0, 0
	for _, ev := range events {
		switch ev.Type {
		case ledger.EvComment:
			commentCount++
		case ledger.EvDecisionAnswered:
			answeredCount++
		}
	}
	if commentCount != 1 {
		t.Fatalf("重复驱动两次后应仍恰 1 条说明评论（EnsureComment dedupe_key 生效），实得 %d", commentCount)
	}
	if answeredCount != 0 {
		t.Fatalf("负断言：重复驱动路径也不得出现 decision_answered，实得 %d", answeredCount)
	}
}

// TestNodeStepLeftoverDecisionOnTerminatedCard 中间态的另一半：Status=终止
// （CloseCard 进入）同样触发补解析转等人，reason 前缀一致。
func TestNodeStepLeftoverDecisionOnTerminatedCard(t *testing.T) {
	s, c := nodeLedger(t)
	cardID := c.ID
	if _, err := s.OpenDecision(cardID, "终止前遗留", []string{"选项A"}, "step:review"); err != nil {
		t.Fatalf("开裁决: %v", err)
	}
	if err := s.CloseCard(cardID, ledger.CloseAbandoned, "test"); err != nil {
		t.Fatalf("终止: %v", err)
	}
	step := newNodeStep(t, s, ledger.NodeDef{Name: "待审阅", Dispatch: true, Verdict: true, Template: "review-generic"}, "", nil)
	out, err := step.RunOnce(context.Background(), cardID)
	if err != nil || out.Action != ActionNeedsHuman {
		t.Fatalf("终止卡: %v %+v", err, out)
	}
	if !strings.HasPrefix(out.Reason, "终态遗留裁决") {
		t.Fatalf("reason 前缀: %q", out.Reason)
	}
}
```

**完整代码块——`internal/ledgerstep/node.go` 的改动（三处：import、新 helper、RunOnce 检测块）**

改动一（import 块加 `"strings"`，与既有 import 合并，保持 gofmt 分组）：

```go
import (
	"context"
	"errors"
	"fmt"
	"log/slog"
	"strings"

	"github.com/Xsxdot/handoff/internal/ledger"
)
```

改动二（在 haltForHuman 之后、routeTo 之前插入）：

```go
// haltForHumanEnsure 与 haltForHuman 同构，但说明评论走 EnsureComment 幂等
// 留痕：同 dedupeKey 不产生第二条评论。供「终态遗留裁决」补解析（契约 §3.7）
// 用——重复驱动同一张卡只留一条说明，避免看板被刷屏；也绝不伪造
// decision_answered（答案是用户的事实，系统不代答）。
func (n *NodeStep) haltForHumanEnsure(cardID, reason, dedupeKey, body string) (Outcome, error) {
	if err := n.gatedWrite("等人留痕"); err != nil {
		return Outcome{Action: ActionNeedsHuman, Reason: reason}, err
	}
	if body != "" {
		if _, err := n.St.EnsureComment(cardID, dedupeKey, body, n.actor()); err != nil {
			return Outcome{}, err
		}
	}
	if err := n.St.MarkNeedsHuman(cardID, reason, n.actor()); err != nil {
		return Outcome{}, err
	}
	return Outcome{Action: ActionNeedsHuman, Reason: reason}, nil
}
```

改动三（RunOnce 读卡块之后、`if !n.Node.Dispatch {` 之前插入）：

```go
	// 终态遗留裁决补解析（B156.2 契约 §3.7）：卡已到终态但仍挂着 open 裁决，
	// 是协调者死亡窗口的产物。继续推进没有意义（终态没有下一列），而答案是
	// 用户的事实，系统绝不代答（绝不伪造 decision_answered）。转等人，并走
	// EnsureComment 幂等留痕——重复驱动同一张卡不产生第二条说明评论。
	// 检测放 !n.Node.Dispatch 检查之前：真实恢复场景是终态叶子列（无 Dispatch）
	// 被再驱动，runner.go 对纯人工列直接调 RunOnce 不装配 WriteGate，放后面
	// 会被「纯人工列不可执行」的配置错误吞掉。
	if card.Status == StatusDone || card.Status == StatusClosed {
		open, openErr := n.St.ListDecisions(true)
		if openErr != nil {
			logger.Warn("读取 open 裁决失败", "cause", openErr)
			return Outcome{}, openErr
		}
		var leftover []ledger.Decision
		for _, d := range open {
			if d.CardID == cardID {
				leftover = append(leftover, d)
			}
		}
		if len(leftover) > 0 {
			ids := make([]string, 0, len(leftover))
			for _, d := range leftover {
				ids = append(ids, fmt.Sprint(d.ID))
			}
			reason := fmt.Sprintf("终态遗留裁决：卡已到终态（%s）但仍有 %d 条未答复裁决", card.Status, len(leftover))
			body := "本卡已到终态但仍有 open 裁决未答复（协调者死亡窗口的产物）。答案属于用户，系统不代答（绝不伪造 decision_answered）——请恢复推进的协调者把裁决转达用户。open 裁决 id：" + strings.Join(ids, ", ")
			logger.Warn("检测到终态遗留裁决，转等人", "status", card.Status, "open_decisions", len(leftover))
			return n.haltForHumanEnsure(cardID, reason, "终态遗留裁决:"+cardID, body)
		}
	}
```

### T3.2 回归、图闸与收尾

非红绿步骤（回归动作，无独立周期）。

1. （~3min）全量触及包回归：`go test ./internal/ledgerstep/... ./internal/ledger/...`。预期全 ok（ledger 一并跑因为 EnsureComment 是本卡承重件，虽然它已被 C2 测过，跑一次证明集成零破坏）。
2. （~2min）图闸：`go run . graph check --repo . --view cards-B156.2-charter-4`。预期 EXIT=0 且解析 JSON 得 fails=0（node.go 在 d_ledger 域内，零跨域边，不应引入新 fail；warns 谱不单列为判据）。
3. （~2min）占位残留扫描：`grep -n "return 0, nil\|return false, nil" internal/ledgerstep/node.go` 应只命中既有签名行或零命中；`grep -n "终态遗留裁决" internal/ledgerstep/node.go` 应命中检测块（正文+reason+dedupeKey 至少三处）；确认无「Ticket 0 空壳」残留。
4. （~2min）台账收尾：把三步原始输出追加进 `docs/superpowers/ledgers/ledger-b156.2.3-plan.md`；`git add` 全部产出物并 commit。
5. 收尾报文按 handoff 纪律输出 branch/commit/summary 与 handoff-verdict 块。

**取证纪律（协调者本轮补充）**：`go test ... | ...` 接了管道后 `$?` 是管道末段退出码恒 0。一律用 `go test ... > out.log 2>&1; echo EXIT=$?`，或 `${PIPESTATUS[0]}`，或 `set -o pipefail`。本卡所有红绿步骤均按此取证。

**测试范围声明（最小化合计）**：本卡只跑 `./internal/ledgerstep/...`（主）+ `./internal/ledger/...`（T3.2 回归护栏）。全仓测试不属于本卡。

## 4. 缺陷族对抗审查结论（逐族）

| 族 | 设问 | 结论 |
| --- | --- | --- |
| 生命周期/状态机中断 | 中间态检测与落卡有窗口吗？重复驱动会刷屏吗？ | 检测读卡与 ListDecisions 是两个读（无事务包裹），但读后动作（EnsureComment/MarkNeedsHuman）各自同事务幂等；两读之间的并发窗口只影响「多驱动同时进来是否各打一条」——EnsureComment 幂等保证评论恰一条、MarkNeedsHuman 追加事件（既有语义，多次转人等本来就多条 needs_human，看板以最后一条为准）。进程崩溃半截态：EnsureComment 与 MarkNeedsHuman 各自独立事务，缺一即「有评论无标记」或反之——与既有 haltForHuman 三件套同一暴露面，不新增风险 |
| 静默失败/误导报错 | 检测失败会静默吗？ | ListDecisions 报错直接返回 error（不吞）；检测命中路径 logger.Warn 结构化留痕；reason 前缀让看板可 grep。绝不伪造 decision_answered 由负断言锁死 |
| 跨平台假设 | SQL 方言、时间、编码 | 零新 SQL（复用 ListDecisions/EnsureComment/MarkNeedsHuman 既有实现）；零时间比较；reason/body 纯文本 UTF-8 |
| 假红/假绿 | 测试会不会恒绿？ | 探针实测正断言今天翻红（台账原文）；变异甲（删守卫）→正断言红、负断言仍绿——证明负断言单独是假绿温床、正断言有牙；变异乙（EnsureComment→AddComment）→幂等测试红。两个变异靶都是「改语义不改有没有用」，编译过即行为断言 |
| 门禁绕过 | 有没有绕过既有落卡入口？ | EnsureComment/MarkNeedsHuman 都是账本既有方法；评论经 EnsureComment（C2 同事务查后写）；无裸 INSERT、无绕过 appendEvent |
| 序列化边界 | 新增投影点？ | 零：不新增 payload 字段、不改事件形状；reason/body/dedupeKey 均为纯字符串入参，被既有方法原样序列化。EnsureComment 的 dedupe_key 键已在 C2 金样键集测试锁过，本卡不再重复 |

## 5. 五项检查

1. **缺陷族对抗**：§4 逐族有结论 ✓
2. **序列化边界**：零新增投影点（§4 末段）；dedupe_key 键集等值已由 C2 锁定，本卡不重复 ✓
3. **上下文预算**：§2 两文件圈得出 ✓
4. **类型标注**：逻辑型子系统，机内可闭环，无真机清单项 ✓
5. **接缝覆盖（双向）**：
   - 测试→缝：三支测试入口全是 `NodeStep.RunOnce`（spec 测试接缝清单 #4 环节执行体缝的入口符号）——看入口不靠标注 ✓
   - 缝→测试：接缝 #4 本卡负责的「终态遗留裁决补解析」三行为（转等人前缀 / 幂等 / 无 decision_answered）逐条有缝级断言 ✓
   - 内部锁：零。三支测试全部从缝入口构造，无需内部锁声明 ✓

## 6. 与 C4（B156.2.4）的文件级交集

C4 本轮重构 `internal/collab/service.go` 沉放结构（Send 执法矩阵 + Pointer 实现），动 collab 域文件；本卡只动 `internal/ledgerstep/node.go` 及其测试。

| 交界面 | 结论 |
| --- | --- |
| 生产 .go 文件 | 零交集（node.go vs service.go/子包） |
| 测试 .go 文件 | 零交集（node_test.go vs service_test.go/子包测试） |
| `codegraph/diffs/cards-B156.2-charter-4.json` | 两卡默认都不动视图；仅当任一侧实现轮需修正锚点才碰，后落地者 rebase 上去并按 nodesAdded/nodesModified 数组取并集手工合，不许另起新视图 |
| go.mod/go.sum | 零新依赖 |
| rebase 顺序 | 两卡独立根；后 merge 者 rebase 自己的 commit 到对方之上，rebase 后必跑 `go build ./... && go test ./internal/ledgerstep/...` + T3.2 图闸命令 |

## 7. 内部锁声明（占位符扫描节的一部分，逐条自我声明）

无。三支测试入口全部是 `NodeStep.RunOnce`——spec 测试接缝清单 #4 的入口符号，缝内直接构造，不需要任何内部锁。

退路同闸自查：T3.1/T3.2 步骤正文无条件退路（无「若意外先绿就改成直喂 X」条款）。

## 8. 占位符扫描自我声明

全文无 TBD／「适当的错误处理」／「同 Task N」；两处实现代码块与一处测试代码块均完整可编译。测试代码的每条断言已逐条列全且可判 pass/fail，断言构造自接缝 #4 的入口 `NodeStep.RunOnce`，**未使用「照抄既有 harness」例外**——测试全文给出，只复用既有夹具 helper（nodeLedger/newNodeStep，node_test.go 既有，指认在案）。dedupeKey 拼法「终态遗留裁决:」+cardID 是本卡的定案（卡级理由见 §3.1 行为语义 4），不是占位符。

## 9. 跨卡审计输入（L3 重档：扇出前由协调者组织独立上下文审计；本节提供素材并自审待拍板）

**跨卡签名清单（本卡 Produces / 相关 Consumes，逐字符）**：

| 签名 | 产出方 | 消费方 | 一致性 |
| --- | --- | --- | --- |
| `EnsureComment(cardID, dedupeKey, body, actor string) (bool, error)` | C2（events.go:237） | 本卡 `haltForHumanEnsure`（`n.St.EnsureComment(cardID, dedupeKey, body, n.actor())`） | 逐字符一致；bool 语义（true=写了）本卡不使用，无所谓 |
| `ListDecisions(openOnly bool) ([]Decision, error)` | 既有（decisions.go:93） | 本卡 `n.St.ListDecisions(true)` | 逐字符一致 |
| `MarkNeedsHuman(cardID, reason, actor string) error` | 既有（events.go:322） | 本卡 haltForHumanEnsure | 逐字符一致 |
| `NodeStep.RunOnce(ctx context.Context, cardID string) (Outcome, error)` | 既有 | 本卡行为扩展 | 签名零改动 |

**spec 故事归属（与本卡相关的故事链路）**：故事 1（深夜清账）的「协调者死亡窗口恢复」依赖本卡把遗留裁决上浮给用户（正断言）+不伪造答复（负断言）；「换绑后新任协调者重建」场景的账外不留事实依赖 C2 的 EnsureComment（本卡承重件已就位）。本卡不单独闭环任何用户故事，属承重件。

## 10. 自审三查

1. **spec 覆盖**：契约 §3.7 逐句 + §4 补解析块 3 条 + breakdown C3 全承接，逐条指到 T3.1 的三支测试；spec §8.1 末条（环节执行体幂等补解析归 contract）已由契约兑现 ✓
2. **占位符扫描**：§8 ✓
3. **跨 task 签名一致性**：§9 清单；本卡零新导出符号、零签名改动 ✓

## 11. 提交切分

- commit 1（T3.1）：`feat(ledgerstep): 终态遗留裁决补解析接入（B156.2.3 欠账#7 其余半）`
- commit 2（T3.2）：`docs(b156.2.3): 实现轮台账收尾`（含台账追加；plan 文档与 plan 轮台账由本 plan 节点先行提交）

## 12. 明确不做（防越界）

- 不写 collab.Consume/Pending/Mentions（C5）；不碰 service.go 任何一个 stub（C4）。
- 不写 EnsureComment/RecordMessageConsumed（C2 已办）；不给 ledger 包加任何代码。
- 不改 proto、types.go 词表、schema、target.json、best.json、金样本 fixture、`codegraph/diffs/cards-B156.2-charter-4.json`。
- 不动 `EvDecisionAnswered` 的任何生产写路径（AnswerDecision 是既有事实源，本卡只在测试里断言它不出现）。
- 不 absorb 视图（finish 阶段动作）；不修裸 graph check 的固有 dead（absorb 前形态）。