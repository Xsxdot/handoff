# B358.2 实现计划：详情页投影精化（跨会话 timeline 污染修复 + needs_human + seat_bound/卡收口 + 成员状态诚实判据）

**卡**：B358.2（B358 的 S2 子卡，d_collab）
**级别**：L3 重档子卡；plan 由子系统执行者依契约自写，协调者只审
**基线**：分支 `cards/B233.1-charter-7` @ `0be4a2c6`（B358.1 已合入：`Service.Send`/`VerifyWriter` 已是成员执法制）
**上游冻结物**：
- 契约 `docs/superpowers/specs/b358-contract.md`：§3.2（wire DTO 与词表）、§3.9（R2/R3 载体）、§4.2 条 14–18（成员派生与状态）、§4.3 条 31、§4.7 条 51–54、§4.8 条 55–56、§7.5（补签轮已落码）、§8.5/§8.10（本卡欠账）、§9 附区（driver_takeover 归属判据）
- 拆解 `docs/superpowers/specs/b358-breakdown.md` §3.3（本计划的任务来源）、§2.4 澄清 3 + 发现 A、§5 真机清单 #5（归协调者）
- spec `docs/superpowers/specs/b358.md` §4.3 末条、§4.4

**读者假设**：执行者对代码库零上下文。本文给出每个任务的完整代码块与逐条可判 pass/fail 的验收；不许猜。

## 0. 有界文件集与边界

| 文件 | 角色 |
|---|---|
| `internal/collab/sessions.go` | 唯一生产代码改动点（timeline 归属修复、两个新分支、needs_human 分支、sessionNodes 日志） |
| `internal/collab/sessions_test.go` | 唯一测试改动点（全部新测试落此文件） |

- **不越界**：本计划零改动 ledger/proto/agentd/cmd。ledger 与 proto 的 R2/R3 载体已由补签轮落码（契约 §7.5），本卡只做**消费**。
- **硬约束**：`internal/collab` 非测试零 import `internal/ledger`（契约条 36）。因此生产代码里 ledger 事件类型与状态值只能**字面量 + 注释指认唯一定义点**；测试文件**已** import ledger，用 `ledger.StatusDone` 等常量生产事件充当漂移哨兵（词表若变，测试先红）。
- 唯一预告的临时越界：Task 6 的变异验证需**临时**触碰 `internal/collab/service.go`（当场还原、零最终 diff）——见该任务步骤 5 的声明。

## 1. 现状符号速查（2026-09-12 读数，行号仅供定位，以 `file#Symbol` 为准）

| 符号 | 现状 | 位置 |
|---|---|---|
| `collab.Service.SessionDetail` | 详情页投影入口（本卡全部断言的主缝） | `internal/collab/sessions.go:68` |
| `sessionTimeline` | **本卡主 bug**：driver_takeover 用全量卡表 `byCard`（`ListAllCards("")` 全表）判归属——任何卡的换绑落进每场会话；`session_created` 查载荷键 `session` 而账本写的键是 `id`（过滤恒空）；`session_card_joined/left` 无归属判定 | `internal/collab/sessions.go:307`（唯一调用方 `SessionDetail` :89） |
| `sessionMembers` / `memberStatus` | 成员派生与状态：`lc.DriverLease` + `nowFn()`（注入钟）；无租约→last_active+零值，未过期→working+到期时刻，过期→last_active+到期时刻 | `internal/collab/sessions.go:219` / `:256` |
| `sessionNodes` | task_mirrored envelope `{node,task_type}` 聚合，按 `session.Cards` 过滤；解码失败静默 continue、**无日志**（缺陷族 2 欠账） | `internal/collab/sessions.go:284` |
| `needsHumanByCard` | needs_human/needs_cleared 推导 `Summary.NeedsHuman`（已在，无需改）；timeline 无 needs_human 分支 | `internal/collab/sessions.go:346` |
| `collab.nowFn` | 包内注入时钟（测试拨针点） | `internal/collab/service.go:31` |
| `collab.log()` | 包级 slog（`sessions.go` 同包可直接用） | `internal/collab/service.go:25` |
| `payloadString` | 载荷取串键（Task 1 重写后仅 status_moved 分支与既有测试使用） | `internal/collab/sessions.go:362` |
| `Store.BindSeat` | 同事务落 `EvDriverSeatBound`，payload 恰 `{to}`，actor=席位自称（R2 已落码） | `internal/ledger/binding.go:32` |
| `Store.RebindSeat` | 落 `EvDriverTakeover`，payload `{from,to}` | `internal/ledger/binding.go:74` |
| `Store.MarkNeedsHuman / ClearNeedsHuman` | needs_human 载荷 `{reason}` / needs_cleared 载荷 `{}` | `internal/ledger/events.go:384` / `:398` |
| `Store.MoveCard(id,to,expect,actor)` | 落 `EvStatusMoved{from,to}`；`to` 必须在工作流 States；`expect=""` 免 CAS | `internal/ledger/move.go:17` |
| `Store.CloseCard(id,reason,actor)` | 落 `EvStatusMoved{from,to:"终止",reason}` | `internal/ledger/cards.go:692` |
| `Store.AppendMirroredEvent` | 落 `task_mirrored`，载荷 `{"node","attempt","task_type","payload"}`（无 round/target 字段） | `internal/ledger/mirror.go:56` |
| `Store.RenewDriverLease(session,ttl)` | 租约 upsert（**测试专属生产者**——生产零调用方，breakdown 澄清 3） | `internal/ledger/binding.go:145` |
| `ledger.StatusDone / StatusClosed` | `"已完成"` / `"终止"`（timeline card_closed 判定值域的唯一定义点） | `internal/ledger/types.go:15-20` |
| proto timeline kind 词表 | 8 值逐值冻结（含 `SessionEventSeatBound`/`SessionEventNeedsHuman`/`SessionEventCardClosed`；**无 needs_cleared kind**） | `internal/proto/sessions.go:111-120`、金样本 `internal/proto/sessions_fixture_test.go#TestSessionTimelineKindVocabulary` |
| 测试夹具 | `newSessionFixture(t) (*Service, *ledger.Store, client.LedgerClient)`（真 SQLite + workflow `bug`：待办→进行中→已完成）、`sessionCard(t,st,title)` | `internal/collab/sessions_test.go:25` / `:43` |
| 时钟夹具先例 | 同一可拨源 `cur` 驱动 `nowFn`（同源拨钟范式） | `internal/collab/readmodel_test.go:557` |

## 2. 图覆盖债

`codegraph sym` 本轮探针全部未命中（原文见台账 §三）：`sessionTimeline`、`sessionMembers`、`memberStatus`、`sessionNodes`、`needsHumanByCard`、`collab.Service.SessionDetail`。Ticket 0 新符号仅存在于 `codegraph/diffs/cards-B358-charter.json` 视图 diff。本文一律 `file#Symbol` 源码锚；**实现节点收尾时把本卡新增/修改符号（含全部新测试函数）补进视图 diff（recon 纪律）**。

## 3. 任务 DAG 与测试范围

```text
Task 1 timeline 归属修复（发现 A 全族，先红后绿）
  ├──> Task 2 seat_bound / card_closed 分支（R2/R3 消费，先红后绿）
  │      └──> Task 3 needs_human 分支 + 标签翻转（先红后绿）──┐
  ├──> Task 3（同上；2 与 3 都只依赖 1，按序执行避免同函数编辑冲突）
Task 4 成员状态诚实判据（同源注入时钟 + 变异）────────────────┤
Task 5 sessionNodes 锁缝 + 解码失败留日志 ────────────────────┼──> Task 7 收口
Task 6 B287 双会话反例（审查移交）────────────────────────────┘
```

- 每个 task 的测试范围（implement 三段律的最小化声明）：只跑 `go test ./internal/collab -run <该任务正则> -count=1`；全量归 Task 7。
- Task 1–3 写下去会红（行为缺失，最薄路径=点亮分支）；Task 4–7 的测试写下去应当绿（行为已在、缺锁）。**后者若实测红：停手，按 charter:debug 建红回路查根因，不许改断言凑绿（假红的处理同假绿）。**
- 所有新测试的入口调用都是导出门面方法（`SessionDetail`/`ListSessions`/`Send`/`Mentions`）——落在 spec §6 接缝 #1（会话入站门面）与 #5（详情页投影缝）上；**禁止直调未导出投影函数（`sessionTimeline` 等）当测试入口**（内部锁默认非法）。

## 4. Task 1：sessionTimeline 归属修复（跨会话 timeline 污染，发现 A 全族）

**意图与为什么**：timeline 只装本会话的结构事实。Ticket 0 的 `sessionTimeline` 三处归属偏差是同族根因（逐分支归属判定缺失/错源）：① driver_takeover 用**全量卡表**判归属（breakdown 发现 A，本卡主 bug）——任何卡的换绑落进每场会话；② `session_created` 查载荷键 `session`，而账本写面键是 `id`（`internal/ledger/sessions.go:64-66`）——过滤恒空，他场会话的建群行串场；③ `session_card_joined/left` 无归属判定——他场进群行串场。按根因修复一次修全族（拒绝补丁式只修字面点名的那一处），每处偏差配独立反例。

**Interfaces**：
- Consumes：`proto.SessionEventCreated/Archived/CardJoined/CardLeft/SeatRebound`（`internal/proto/sessions.go:111-120`）；`log()`（包内既有）；`proto.LedgerEvent`。
- Produces（包内未导出，供 Task 2/3 的代码块对齐）：签名变更 `sessionTimeline(events []proto.LedgerEvent, session proto.Session) []proto.SessionTimelineEvent`（原第三参 `byCard map[string]proto.Card` 废弃，改收 `session proto.Session`——归属判据从全量卡表换成 `session.Cards` 成员集）；新助手 `structEventFields(ev proto.LedgerEvent) map[string]any`（解析失败留日志、返回 nil）。`payloadString` 保留（status_moved 分支与既有测试仍用）。

**步骤**：

1. 判据基线复核（动手前）：`go test ./internal/collab -run 'TestSession' -count=1` 退出 0（本计划出稿日已亲跑绿，见台账 §二；执行日复跑）。
2. 把下列测试追加进 `internal/collab/sessions_test.go`（import 增 `"strings"`——本测试的 seat_rebound Detail 透传断言用到）：

```go
// sessionStructSeqs 扫全流建「事件类型/归属键值 → seq」索引（测试辅助）。
// created 的归属键是 id、joined/left 是 card——与 ledger 写面载荷同形。
func sessionStructSeqs(t *testing.T, st *ledger.Store) map[string]int64 {
	t.Helper()
	events, err := st.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	out := map[string]int64{}
	for _, ev := range events {
		var fields map[string]any
		if err := json.Unmarshal(ev.Payload, &fields); err != nil {
			continue
		}
		switch ev.Type {
		case ledger.EvSessionCreated:
			if id, _ := fields["id"].(string); id != "" {
				out["created/"+id] = ev.Seq
			}
		case ledger.EvSessionCardJoined:
			if card, _ := fields["card"].(string); card != "" {
				out["joined/"+card] = ev.Seq
			}
		}
	}
	return out
}

// cardSet 把卡号切片转集合（测试辅助）。
func cardSet(ids []string) map[string]bool {
	out := make(map[string]bool, len(ids))
	for _, id := range ids {
		out[id] = true
	}
	return out
}

// TestSessionTimelineBelongsToOneSession 跨会话污染反例（breakdown §2.4
// 发现 A）+ 同族反例：timeline 只装本会话的结构事实。卡 A ∈ 会话一、
// 卡 B 不属任何会话、卡 C ∈ 会话二：B 的席位事件不得进任何会话 timeline；
// 会话二的建群/进群事件不得进会话一（Ticket 0 三处归属偏差：takeover 全量
// 卡表、created 查错载荷键、joined/left 无判定）。本会话自己的行必须保留
// （防修复过删）。先红后绿：在修复前的代码上本测试必红。
func TestSessionTimelineBelongsToOneSession(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	cardA := sessionCard(t, st, "会话一成员卡")
	cardB := sessionCard(t, st, "无会话卡")
	cardC := sessionCard(t, st, "会话二成员卡")

	session1, err := svc.CreateSession("会话一", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session1.ID, cardA.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	session2, err := svc.CreateSession("会话二", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session2.ID, cardC.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	// 无会话卡 B：坐下 + 换绑，落 seat_bound 与 takeover 两个卡事件。
	if err := st.BindSeat(cardB.ID, "cli:opencode#b-1", proto.SeatSourceBind); err != nil {
		t.Fatal(err)
	}
	if err := st.RebindSeat(cardB.ID, "cli:codex#b-2", proto.SeatSourceCoordinate, "cli:opencode#b-1"); err != nil {
		t.Fatal(err)
	}
	// 会话一成员卡 A 坐下再换绑：seat_rebound 行必须留在会话一（防过删）。
	if err := st.BindSeat(cardA.ID, "cli:opencode#a-1", proto.SeatSourceBind); err != nil {
		t.Fatal(err)
	}
	if err := st.RebindSeat(cardA.ID, "cli:codex#a-2", proto.SeatSourceCoordinate, "cli:opencode#a-1"); err != nil {
		t.Fatal(err)
	}

	seqs := sessionStructSeqs(t, st)
	d1, err := svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	d2, err := svc.SessionDetail(session2.ID)
	if err != nil {
		t.Fatal(err)
	}
	s1full, err := st.GetSession(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	s2full, err := st.GetSession(session2.ID)
	if err != nil {
		t.Fatal(err)
	}

	// 反例主体（发现 A）：每场会话的 timeline 里，带卡号的行只许属于本会话的卡。
	for name, tc := range map[string]struct {
		detail proto.SessionDetail
		own    map[string]bool
	}{
		"会话一": {d1, cardSet(s1full.Cards)},
		"会话二": {d2, cardSet(s2full.Cards)},
	} {
		for _, row := range tc.detail.Timeline {
			if row.CardID != "" && !tc.own[row.CardID] {
				t.Fatalf("%s timeline 串进非本会话卡的事件行（发现 A）: %+v", name, row)
			}
		}
	}
	// 卡 B 不属任何会话：它的任何行不得出现在任何 timeline。
	for _, d := range []proto.SessionDetail{d1, d2} {
		for _, row := range d.Timeline {
			if row.CardID == cardB.ID {
				t.Fatalf("无会话卡 B 的事件串进 timeline: %+v", row)
			}
		}
	}
	// 同族反例：他场会话的结构事件（created/joined）不串场。
	for _, bad := range []struct {
		detail proto.SessionDetail
		seq    int64
		what   string
	}{
		{d1, seqs["created/"+session2.ID], "会话二的 created"},
		{d1, seqs["joined/"+cardC.ID], "卡 C 进会话二的 joined"},
		{d2, seqs["created/"+session1.ID], "会话一的 created"},
		{d2, seqs["joined/"+cardA.ID], "卡 A 进会话一的 joined"},
	} {
		for _, row := range bad.detail.Timeline {
			if row.Seq == bad.seq {
				t.Fatalf("他场结构事件串场（%s）: %+v", bad.what, row)
			}
		}
	}
	// 防过删：会话一自己的 created、卡 A 的 joined 必须在；卡 A 的
	// seat_rebound 行必须在且 Detail 原样透传 {from,to}（缺陷族 6）。
	for _, want := range []struct {
		seq  int64
		kind string
	}{
		{seqs["created/" + session1.ID], proto.SessionEventCreated},
		{seqs["joined/" + cardA.ID], proto.SessionEventCardJoined},
	} {
		found := false
		for _, row := range d1.Timeline {
			if row.Seq == want.seq && row.Kind == want.kind {
				found = true
			}
		}
		if !found {
			t.Fatalf("会话一自己的行被过删: want seq=%d kind=%s", want.seq, want.kind)
		}
	}
	reboundFound := false
	for _, row := range d1.Timeline {
		if row.Kind == proto.SessionEventSeatRebound && row.CardID == cardA.ID {
			reboundFound = true
			if !strings.Contains(row.Detail, `"from":"cli:opencode#a-1"`) ||
				!strings.Contains(row.Detail, `"to":"cli:codex#a-2"`) {
				t.Fatalf("seat_rebound Detail 应原样透传载荷（可对质）: %+v", row)
			}
		}
	}
	if !reboundFound {
		t.Fatalf("会话一成员卡 A 的换绑行被过删: %+v", d1.Timeline)
	}
}
```

3. **跑红**：`go test ./internal/collab -run TestSessionTimelineBelongsToOneSession -count=1` —— 在未修复代码上必须红，且三组断言各自红（① B 的行串场；② 会话二的 created/joined 串进会话一；其中 ③ 防过删断言保持绿）。把红的原始输出记台账。若任何一组**写下去就绿**，停手上报（判据或对现状的理解有错）。
4. 最小实现：在 `internal/collab/sessions.go` 替换 `sessionTimeline` 整函数并在 `payloadString` 旁新增 `structEventFields`，把 `SessionDetail` 的调用行 `Timeline: sessionTimeline(events, session.ID, byCard)` 改为 `Timeline: sessionTimeline(events, session)`：

```go
// structEventFields 解析结构事件载荷为键值表；解析失败留日志并返回 nil，
// 调用方跳过该行（缺陷族 2：静默跳过只许丢一行，不吞整段 timeline）。
func structEventFields(ev proto.LedgerEvent) map[string]any {
	var fields map[string]any
	if err := json.Unmarshal(ev.Payload, &fields); err != nil {
		log().Warn("会话结构事件载荷不可解析，timeline 跳过该行",
			"seq", ev.Seq, "type", ev.Type, "cause", err)
		return nil
	}
	return fields
}

// sessionTimeline 投影会话 timeline：结构事实（建群/归档/进群/移出）+
// 席位变更（换绑）。归属判据（契约 §9「仅当事件所属卡在该会话内时归入」的
// 全族应用，B358.2 修复 breakdown 发现 A）：
//   - 无卡结构事件：载荷归属键（created 是 id——与 ledger 写面
//     map{"id",...} 同形；其余是 session）等于本会话 id 才归入；
//   - 卡事件：仅当 ev.CardID ∈ 本会话当前 Cards 才归入。判定按当前成员——
//     卡移出后其历史行不再出现在本会话 timeline（契约 §9 字面）。
// Ticket 0 偏差：driver_takeover 曾用全量卡表判归属（任何卡的换绑落进每场
// 会话）、created 曾查错载荷键（session vs id，过滤恒空）、joined/left 曾无
// 判定——三处同族一并修复，反例见 TestSessionTimelineBelongsToOneSession。
func sessionTimeline(events []proto.LedgerEvent, session proto.Session) []proto.SessionTimelineEvent {
	inSession := make(map[string]bool, len(session.Cards))
	for _, cardID := range session.Cards {
		inSession[cardID] = true
	}
	out := []proto.SessionTimelineEvent{}
	for _, ev := range events {
		kind := ""
		cardID := ""
		detail := ""
		switch ev.Type {
		case "session_created":
			fields := structEventFields(ev)
			if fields == nil || fields["id"] != session.ID {
				continue
			}
			kind = proto.SessionEventCreated
		case "session_archived":
			fields := structEventFields(ev)
			if fields == nil || fields["session"] != session.ID {
				continue
			}
			kind = proto.SessionEventArchived
		case "session_card_joined":
			fields := structEventFields(ev)
			if fields == nil || fields["session"] != session.ID {
				continue
			}
			card, _ := fields["card"].(string)
			kind, cardID = proto.SessionEventCardJoined, card
		case "session_card_left":
			fields := structEventFields(ev)
			if fields == nil || fields["session"] != session.ID {
				continue
			}
			card, _ := fields["card"].(string)
			kind, cardID = proto.SessionEventCardLeft, card
		case "driver_takeover":
			// 席位变更：仅当事件所属卡在该会话内时归入本 timeline（契约 §9）。
			// detail 原样透传 {from,to} 载荷——可对质，不做二次解释（缺陷族 6）。
			if !inSession[ev.CardID] {
				continue
			}
			kind, cardID, detail = proto.SessionEventSeatRebound, ev.CardID, string(ev.Payload)
		default:
			// 未知 ledger 事件类型不进 timeline（缺陷族 7：不得被默认分支收编）。
			// seat_bound / card_closed / needs_human 分支由 Task 2/3 增补。
			continue
		}
		out = append(out, proto.SessionTimelineEvent{
			Seq: ev.Seq, Kind: kind, CardID: cardID, Detail: detail,
			Actor: ev.Actor, CreatedAt: ev.CreatedAt,
		})
	}
	return out
}
```

5. **跑绿**：`go test ./internal/collab -run TestSessionTimelineBelongsToOneSession -count=1` 退出 0；随即局部回归 `go test ./internal/collab -count=1` 退出 0（既有 `TestSessionVerticalSlice` 的「timeline 非空」断言等不得破）。
6. 提交（一次提交含测试与实现；提交前 `gofmt -l internal/collab` 为空）。

**验收栏**（逐条独立可判）：
- `go test ./internal/collab -run TestSessionTimelineBelongsToOneSession -count=1` 退出 0，且步骤 3 有红记录（先红后绿——缺陷族 4 的「必须能红」由此满足）。
- `go test ./internal/collab -count=1` 退出 0。
- 反例覆盖 breakdown §3.3 ③ 第 1 条字面：卡 A ∈ 会话 1、卡 B ∉ 任何会话，B 落 `driver_takeover` 后会话 1 与会话 2 的 timeline 都不含该行。
- 缺陷族映射：族 4（假红/假绿）→ 步骤 3 红记录 + 防过删正例；族 6（序列化边界）→ seat_rebound Detail 透传断言；族 7（枚举）→ default continue 保留（未知事件不收编）；族 2（静默失败）→ `structEventFields` 解析失败留日志。日志/注释步骤已内嵌步骤 4（结构性跳过留日志；函数职责与归属判据注释成文）。无新增日志缺失：成员过滤类跳过是正常路径非错误分支，不加日志（防高频刷屏——声明）。

## 5. Task 2：timeline 增 seat_bound / card_closed 分支（R2/R3 消费，契约欠账 §8.10）

**意图与为什么**：R2/R3 已由补签轮落码（`EvDriverSeatBound` 已随 `BindSeat` 落账、`SessionEventCardClosed` 常量已在），但 timeline 没有消费分支——「谁在何时接手现场」「卡何时收口」（spec 用户故事 9）在详情页不可见。seat_bound 归属判据沿用契约 §9（仅当事件所属卡在该会话内）；card_closed 复用既有 `EvStatusMoved` 载体，当且仅当 `to ∈ {已完成, 终止}` 记行，终止 `reason` 随 payload 透传。

**Interfaces**：
- Consumes：Task 1 的 `sessionTimeline(events, session)` 新签名与 `inSession` 成员集；`proto.SessionEventSeatBound`、`proto.SessionEventCardClosed`；`payloadString`。
- Produces：无新符号（两个 switch case 块）。

**步骤**：

1. 判据基线：Task 1 已绿后进行。把下列测试追加进 `internal/collab/sessions_test.go`（本文件此时应已含 `"strings"` import，缺则补）：

```go
// TestSessionTimelineSeatBoundAndCardClosed R2/R3 的 timeline 消费（契约欠账
// §8.10）：seat_bound 以 EvDriverSeatBound 为唯一载体、归属判据同 §9； 
// card_closed 以既有 EvStatusMoved 为载体、当且仅当 to ∈ {已完成,终止} 记行，
// 终止 reason 透传 Detail；普通列间转移与他会话卡不产生行。测试用
// ledger.StatusDone/StatusClosed 常量生产事件——账本词表若变，此处先红
// （字面量漂移哨兵，契约条 36「collab 非测试零 import ledger」的测试侧镜像）。
func TestSessionTimelineSeatBoundAndCardClosed(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	cardA := sessionCard(t, st, "收口观察卡")
	cardE := sessionCard(t, st, "终止观察卡")
	cardB := sessionCard(t, st, "无会话卡")
	session1, err := svc.CreateSession("收口会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	session2, err := svc.CreateSession("旁观会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session1.ID, cardA.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session1.ID, cardE.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}

	// R2：成员卡坐下 → seat_bound 行；无会话卡坐下 → 不进任何 timeline。
	if err := st.BindSeat(cardA.ID, "cli:opencode#a-1", proto.SeatSourceBind); err != nil {
		t.Fatal(err)
	}
	if err := st.BindSeat(cardB.ID, "cli:opencode#b-1", proto.SeatSourceBind); err != nil {
		t.Fatal(err)
	}
	d1, err := svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	var boundRow *proto.SessionTimelineEvent
	for i := range d1.Timeline {
		row := d1.Timeline[i]
		if row.Kind == proto.SessionEventSeatBound && row.CardID == cardB.ID {
			t.Fatalf("无会话卡 B 的 seat_bound 串场: %+v", row)
		}
		if row.Kind == proto.SessionEventSeatBound && row.CardID == cardA.ID {
			boundRow = &d1.Timeline[i]
		}
	}
	if boundRow == nil {
		t.Fatalf("坐下应产生 seat_bound 行: %+v", d1.Timeline)
	}
	if boundRow.Actor != "cli:opencode#a-1" {
		t.Fatalf("seat_bound actor 应为席位自称（账本写面即如此）: %+v", boundRow)
	}
	if !strings.Contains(boundRow.Detail, `"to":"cli:opencode#a-1"`) {
		t.Fatalf("seat_bound Detail 应原样透传载荷 {to}: %+v", boundRow)
	}

	// R3：普通列间转移不记行；to=已完成 记 card_closed；终止（含 reason）透传。
	if err := st.MoveCard(cardA.ID, ledger.StatusDoing, ledger.StatusTodo, "t"); err != nil {
		t.Fatal(err)
	}
	d1, err = svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	for _, row := range d1.Timeline {
		if row.Kind == proto.SessionEventCardClosed && row.CardID == cardA.ID {
			t.Fatalf("普通列间转移不得记 card_closed（契约条 56）: %+v", row)
		}
	}
	if err := st.MoveCard(cardA.ID, ledger.StatusDone, ledger.StatusDoing, "t"); err != nil {
		t.Fatal(err)
	}
	if err := st.CloseCard(cardE.ID, ledger.CloseAbandoned, "t"); err != nil {
		t.Fatal(err)
	}
	// 无会话卡 B 终止 → 不进任何会话 timeline。
	if err := st.CloseCard(cardB.ID, ledger.CloseCancelled, "t"); err != nil {
		t.Fatal(err)
	}
	d1, err = svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	d2, err := svc.SessionDetail(session2.ID)
	if err != nil {
		t.Fatal(err)
	}
	for _, d := range []proto.SessionDetail{d1, d2} {
		for _, row := range d.Timeline {
			if row.Kind == proto.SessionEventCardClosed && row.CardID == cardB.ID {
				t.Fatalf("无会话卡 B 的 card_closed 串场: %+v", row)
			}
		}
	}
	closedA, closedE := false, false
	for _, row := range d1.Timeline {
		if row.Kind == proto.SessionEventCardClosed && row.CardID == cardA.ID {
			closedA = true
			if !strings.Contains(row.Detail, `"to":"已完成"`) {
				t.Fatalf("card_closed Detail 应透传 to=已完成: %+v", row)
			}
		}
		if row.Kind == proto.SessionEventCardClosed && row.CardID == cardE.ID {
			closedE = true
			if !strings.Contains(row.Detail, `"to":"终止"`) || !strings.Contains(row.Detail, `"reason":"废弃"`) {
				t.Fatalf("终止 card_closed 应透传 to 与 reason（不做二次解释）: %+v", row)
			}
		}
	}
	if !closedA || !closedE {
		t.Fatalf("to∈{已完成,终止} 应各记一行 card_closed: closedA=%v closedE=%v", closedA, closedE)
	}
	// 词表对齐（缺陷族 7）：timeline 所有行的 kind ∈ proto 冻结词表 8 值。
	allowed := map[string]bool{
		proto.SessionEventCardJoined: true, proto.SessionEventCardLeft: true,
		proto.SessionEventSeatBound: true, proto.SessionEventSeatRebound: true,
		proto.SessionEventNeedsHuman: true, proto.SessionEventCardClosed: true,
		proto.SessionEventArchived: true, proto.SessionEventCreated: true,
	}
	for _, d := range []proto.SessionDetail{d1, d2} {
		for _, row := range d.Timeline {
			if !allowed[row.Kind] {
				t.Fatalf("timeline 出现词表外 kind: %+v", row)
			}
		}
	}
}
```

2. **跑红**：`go test ./internal/collab -run TestSessionTimelineSeatBoundAndCardClosed -count=1` —— 必须红在 `boundRow == nil`（seat_bound 分支缺失）与 `closedA/closedE == false`（card_closed 分支缺失）两处；词表扫与串场反例此时绿（没有行就没有串场）。红输出记台账。
3. 最小实现：在 `sessionTimeline` 的 `case "driver_takeover":` 块**之后**、`default:` 之前插入两个 case（逐字）：

```go
		case "driver_seat_bound":
			// R2（契约 §4.7 条 54）：EvDriverSeatBound 是 seat_bound 行的唯一
			// 载体（唯一定义点 internal/ledger/types.go——collab 非测试零 import
			// ledger，契约条 36，故字面量 + 注释指认）；归属判据沿用 §9「仅当
			// 事件所属卡在该会话内」。载荷 {to}、actor=席位自称，detail 原样
			// 透传（可对质，缺陷族 6）。
			if !inSession[ev.CardID] {
				continue
			}
			kind, cardID, detail = proto.SessionEventSeatBound, ev.CardID, string(ev.Payload)
		case "status_moved":
			// R3（契约 §4.8 条 56）：载体是既有 EvStatusMoved。当且仅当
			// to ∈ {已完成, 终止} 记 card_closed（字面值即 ledger.StatusDone /
			// ledger.StatusClosed；漂移由测试用 ledger 常量生产事件钉住）；
			// 普通列间转移不是结构事实。终止的 reason 随 payload 透传 Detail。
			if !inSession[ev.CardID] {
				continue
			}
			switch payloadString(ev.Payload, "to") {
			case "已完成", "终止":
				kind, cardID, detail = proto.SessionEventCardClosed, ev.CardID, string(ev.Payload)
			default:
				continue
			}
```

4. **跑绿**：`go test ./internal/collab -run TestSessionTimelineSeatBoundAndCardClosed -count=1` 退出 0。
5. **变异验证**（缺陷族 8：判定必须可证咬合）：临时把 `case "status_moved":` 里 `switch payloadString(ev.Payload, "to")` 的两值判定改成无条件记行（删掉 switch、直接置 kind）→ 复跑该测试必须红在「普通列间转移不得记 card_closed」→ 还原 → 复跑绿。变异前后输出记台账。
6. 局部回归 `go test ./internal/collab -count=1` 退出 0；提交。

**验收栏**：
- 红记录（步骤 2）+ 绿 + 变异红/还原绿（步骤 5）四段原始输出在台账。
- `go test ./internal/collab -count=1` 退出 0。
- 缺陷族映射：族 7（枚举白名单）→ 新分支 kind 全部取 proto 常量 + 词表 8 值扫断言 + 未知事件仍走 default continue；族 6（序列化边界）→ `{to}` 与 `{from,to,reason}` 原样透传断言；族 8 → 步骤 5 变异；族 5（门禁绕过）不适用——纯读投影无新写路径（breakdown §3.3 族 5 字面）。日志：本任务两分支的跳过（非成员卡、普通转移）都是正常归属过滤非错误分支，不加日志（声明，同 Task 1）；解析失败日志已由 Task 1 `structEventFields` 覆盖 structural 分支，status_moved 载荷解析失败走 `payloadString` 静默返回空串落入 default continue——与既有 `payloadString` 契约一致，属正常过滤（声明）。

## 6. Task 3：timeline 增 needs_human 分支 + NeedsHuman 标签翻转锁

**意图与为什么**：冻结常量 `proto.SessionEventNeedsHuman` 已在（契约 §3.2），投影未消费——「需要你」只亮在列表标签，详情页 timeline 缺「谁在何时喊人」这行结构事实（spec §4.3 末条：needs_human 亮起归详情页 timeline）。`needs_cleared` 无 timeline kind（词表 8 值逐值冻结，无它）——清除只翻标签，不造行。

**Interfaces**：
- Consumes：Task 1 的 `sessionTimeline(events, session)` 签名与 `inSession`；`proto.SessionEventNeedsHuman`。
- Produces：无新符号（一个 switch case 块）。

**步骤**：

1. 把下列测试追加进 `internal/collab/sessions_test.go`：

```go
// TestSessionTimelineNeedsHuman needs_human 进 timeline（契约 §3.2 词表 +
// breakdown §3.3 ③）：卡 ∈ 会话的 needs_human 事件出恰一条 kind=needs_human
// 行且 Summary.NeedsHuman 亮起；卡 ∉ 会话的不出现、不点亮他会话标签；
// needs_cleared 不产生 timeline 行（词表 8 值无它——补签轮逐值冻结），
// 标签由 needsHumanByCard 翻灭。
func TestSessionTimelineNeedsHuman(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	cardA := sessionCard(t, st, "等人观察卡")
	cardB := sessionCard(t, st, "无会话卡")
	session1, err := svc.CreateSession("等人会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	session2, err := svc.CreateSession("隔壁会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session1.ID, cardA.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := st.MarkNeedsHuman(cardA.ID, "验收不过", "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := st.MarkNeedsHuman(cardB.ID, "与本会话无关", "user:sy"); err != nil {
		t.Fatal(err)
	}
	d1, err := svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	d2, err := svc.SessionDetail(session2.ID)
	if err != nil {
		t.Fatal(err)
	}
	if !d1.Summary.NeedsHuman {
		t.Fatal("会话内卡 needs_human 后列表标签应亮起")
	}
	if d2.Summary.NeedsHuman {
		t.Fatal("他会话卡的 needs_human 不得点亮本会话标签")
	}
	rows := 0
	for _, row := range d1.Timeline {
		if row.Kind == proto.SessionEventNeedsHuman {
			rows++
			if row.CardID != cardA.ID {
				t.Fatalf("needs_human 行归属错误: %+v", row)
			}
		}
	}
	if rows != 1 {
		t.Fatalf("应恰一条 needs_human 行, got %d: %+v", rows, d1.Timeline)
	}
	for _, row := range d2.Timeline {
		if row.Kind == proto.SessionEventNeedsHuman {
			t.Fatalf("无会话卡 B 的 needs_human 串进会话二: %+v", row)
		}
	}
	before := len(d1.Timeline)
	if err := st.ClearNeedsHuman(cardA.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	d1, err = svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	if d1.Summary.NeedsHuman {
		t.Fatal("needs_cleared 后标签应熄灭")
	}
	if len(d1.Timeline) != before {
		t.Fatalf("needs_cleared 不得产生 timeline 行（词表无此 kind）: before=%d after=%d",
			before, len(d1.Timeline))
	}
}
```

2. **跑红**：`go test ./internal/collab -run TestSessionTimelineNeedsHuman -count=1` —— 必须红在 `rows != 1`（分支缺失）；标签断言此时绿（`needsHumanByCard` 已在）。红输出记台账。
3. 最小实现：在 `sessionTimeline` 的 `case "status_moved":` 块**之后**、`default:` 之前插入（逐字）：

```go
		case "needs_human":
			// 等人标记进 timeline（proto.SessionEventNeedsHuman；spec §4.3 末条
			// 「needs_human 亮起归详情页 timeline」）。归属判据同 §9；载荷
			// {reason} 原样透传 Detail。needs_cleared 不进 timeline——词表 8 值
			// 无它（补签轮逐值冻结），标签熄灭由 needsHumanByCard 承担。
			if !inSession[ev.CardID] {
				continue
			}
			kind, cardID, detail = proto.SessionEventNeedsHuman, ev.CardID, string(ev.Payload)
```

4. **跑绿**：`go test ./internal/collab -run TestSessionTimelineNeedsHuman -count=1` 退出 0。
5. **变异验证**：临时删掉该分支的 `if !inSession[ev.CardID] { continue }` → 复跑必须红在「无会话卡 B 的 needs_human 串进会话二」→ 还原 → 复跑绿。输出记台账。
6. 局部回归 `go test ./internal/collab -count=1` 退出 0；提交。

**验收栏**：
- breakdown §3.3 ③ 第 2 条字面：`needs_human` 事件（卡 ∈ 会话）后 `SessionDetail.Timeline` 出现 `kind=needs_human` 行；`needs_cleared` 后 `Summary.NeedsHuman` 翻 false（label 消失有测试）。
- 红记录 + 绿 + 变异红/还原绿在台账；`go test ./internal/collab -count=1` 退出 0。
- 缺陷族映射：族 7 → kind 取常量、needs_cleared 不进 timeline（词表逐值对齐）；族 8 → 步骤 5 变异；族 2 → 归属跳过是正常过滤不加日志（声明）；族 5/9 不适用（只读投影/不触 web）。

## 7. Task 4：成员状态诚实判据（working/last_active/empty + 同源注入时钟 + 词表扫）

**意图与为什么**：「看板不说谎」（spec §4.4）——`working` 只能由未过期租约推出；无租约只报 `last_active`+零值；空座报 `empty`；`listening` 生产零载体（breakdown 澄清 3：租约只有到期时刻、无状态语义），生产绝不报它，更不报「在线」。行为已在 `memberStatus`（Ticket 0），缺的是**可证咬合**的锁：时钟必须同源（契约条 18：不得「判据读真实钟、夹具拨别的钟」）、拨钟即翻（缺陷族 4）、删过期判断必须变红（缺陷族 8）。

**Interfaces**：
- Consumes：`collab.nowFn`（`internal/collab/service.go:31` 包内注入点）；`client.LedgerClient#DriverLease(session string) (time.Time, bool, error)`；测试专属生产者 `Store.RenewDriverLease(session string, ttl time.Duration) (bool, error)`。
- Produces：无生产代码改动；测试助手 `memberByCard` / `memberByIdentity`。

**步骤**：

1. import 增 `"time"`（`internal/collab/sessions_test.go`）。追加测试：

```go
// memberByCard 按卡号取席位成员（测试辅助）。
func memberByCard(t *testing.T, d proto.SessionDetail, cardID string) proto.SessionMember {
	t.Helper()
	for _, m := range d.Summary.Members {
		if m.CardID == cardID {
			return m
		}
	}
	t.Fatalf("找不到卡 %s 的席位成员: %+v", cardID, d.Summary.Members)
	return proto.SessionMember{}
}

// memberByIdentity 按身份取成员（测试辅助）。
func memberByIdentity(t *testing.T, d proto.SessionDetail, identity string) proto.SessionMember {
	t.Helper()
	for _, m := range d.Summary.Members {
		if m.Identity == identity {
			return m
		}
	}
	t.Fatalf("找不到成员 %s: %+v", identity, d.Summary.Members)
	return proto.SessionMember{}
}

// TestSessionMemberStatusHonest 成员状态诚实判据（契约条 17/18 + breakdown
// §2.4 澄清 3）：working 只能由未过期租约推出，判据时钟与租约判定同一注入
// 时钟（拨钟即翻）；无租约报 last_active+零值；空座报 empty；观察到的状态串
// ⊆ {working,last_active,empty}——四值词表减 listening（生产零载体，澄清 3），
// 「online」等词表外串自然被拒。本测试写下去应绿（行为已在）——红即停手上报。
func TestSessionMemberStatusHonest(t *testing.T) {
	svc, st, lc := newSessionFixture(t)
	cardA := sessionCard(t, st, "租约卡")
	cardC := sessionCard(t, st, "空座卡")
	session1, err := svc.CreateSession("状态会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session1.ID, cardA.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session1.ID, cardC.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := st.BindSeat(cardA.ID, "cli:opencode#a-1", proto.SeatSourceBind); err != nil {
		t.Fatal(err)
	}
	// 注入时钟与租约判定同源（条 18）：nowFn 拨到可拨源 cur，working →
	// last_active 的翻转只由 cur 前拨触发（同源范式先例
	// readmodel_test.go#TestListRoomsLiveFlip）。
	base := time.Date(2026, 1, 1, 0, 0, 0, 0, time.UTC)
	cur := base
	oldNow := nowFn
	nowFn = func() time.Time { return cur }
	t.Cleanup(func() { nowFn = oldNow })

	// 未过期租约：Store.RenewDriverLease 是本测试专属生产者（生产零调用方，
	// 澄清 3）；expiresAt = 墙钟 + 1h，必在 cur（2026-01-01）之后 → working。
	if _, err := st.RenewDriverLease("cli:opencode#a-1", time.Hour); err != nil {
		t.Fatal(err)
	}
	exp, exists, err := lc.DriverLease("cli:opencode#a-1")
	if err != nil || !exists {
		t.Fatalf("租约应可读: exp=%v exists=%v err=%v", exp, exists, err)
	}
	d1, err := svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	seat := memberByCard(t, d1, cardA.ID)
	if seat.Status != proto.SessionMemberWorking {
		t.Fatalf("未过期租约应报 working: %+v", seat)
	}
	if !seat.LastActive.Equal(exp) {
		t.Fatalf("working 的 LastActive 应为租约到期时刻 %v（可证实的「活到几点」）: %+v", exp, seat)
	}

	// 拨钟过期（同一 cur 前拨过 expiresAt）→ last_active；LastActive 仍是
	// 到期时刻。拨钟即翻 = 判据用的是注入钟的证明（缺陷族 4）。
	cur = time.Now().Add(2 * time.Hour)
	d1, err = svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	seat = memberByCard(t, d1, cardA.ID)
	if seat.Status != proto.SessionMemberLastActive {
		t.Fatalf("拨钟过期后应翻 last_active: %+v", seat)
	}
	if !seat.LastActive.Equal(exp) {
		t.Fatalf("过期租约的 LastActive 应为到期时刻 %v: %+v", exp, seat)
	}

	// 无租约的显式成员：last_active + 零值 LastActive（无「最后活跃」可证实读数）。
	owner := memberByIdentity(t, d1, "user:sy")
	if owner.Status != proto.SessionMemberLastActive || !owner.LastActive.IsZero() {
		t.Fatalf("无租约成员应报 last_active+零值: %+v", owner)
	}
	// 空座卡：empty + 空身份 + 零值。
	empty := memberByCard(t, d1, cardC.ID)
	if empty.Status != proto.SessionMemberEmpty || empty.Identity != "" || !empty.LastActive.IsZero() {
		t.Fatalf("空座卡应报 empty: %+v", empty)
	}
	// 词表扫（缺陷族 8 + 澄清 3）：全部观察值 ∈ {working,last_active,empty}。
	for _, m := range d1.Summary.Members {
		switch m.Status {
		case proto.SessionMemberWorking, proto.SessionMemberLastActive, proto.SessionMemberEmpty:
		default:
			t.Fatalf("成员状态词表外取值（listening 无生产载体；online 不可证实）: %+v", m)
		}
	}
}
```

2. **跑绿确认**：`go test ./internal/collab -run TestSessionMemberStatusHonest -count=1` 退出 0。**若红：停手**——要么行为已偏离本判据（升级协调者），要么判据写错（修正判据而非实现——判据错的代价大于实现错）。
3. **变异①**（契约条 17 / 缺陷族 8）：临时把 `memberStatus` 的过期分支改成恒 working（`if expiresAt.After(nowFn())` 一行替换为 `if true`）→ 复跑必须红在「拨钟过期后应翻 last_active」→ 还原 → 复跑绿。
4. **变异②**：临时把 `memberStatus` 的 `return proto.SessionMemberLastActive, time.Time{}` 改成 `return "online", time.Time{}` → 复跑必须红在词表扫 → 还原 → 复跑绿。
5. 局部回归 `go test ./internal/collab -count=1` 退出 0；提交（本任务零生产代码改动，提交只含测试）。

**验收栏**：
- breakdown §3.3 ③ 第 3 条字面：`Store.RenewDriverLease` 造未过期租约 + 注入时钟 → `working`；拨钟过期 → `last_active`（**同一注入时钟**，契约条 18——测试内 working/last_active 的翻转只由 `cur` 前拨触发）；空座卡 → `empty`；断言词表外状态串不出现（取三值集，严于四值——`listening` 无生产载体，澄清 3）。
- 变异①②的红/绿输出在台账（「working 只能由未过期租约推出」的可咬合证明）。
- `go test ./internal/collab -count=1` 退出 0。
- 缺陷族映射：族 4 → 同源拨钟即翻；族 8 → 变异①②；族 2 → 无（投影无吞错路径——`DriverLease` 读失败按无租约处理是既有取舍，`memberStatus` 注释已声明，本卡不改）；族 3/5/9 不适用。日志/注释：零生产改动，N/A（声明）。真机：成员状态生产诚实性的真实账本读数归协调者真机清单 #5（breakdown §5），本计划不冒充。

## 8. Task 5：sessionNodes 锁缝 + 解码失败留日志 + 留白定稿

**意图与为什么**：任务节点投影 `sessionNodes` 行为已在（按 `session.Cards` 过滤、envelope `{node,task_type}` 聚合）但零测试；且解码失败静默吞行违反缺陷族 2（不得静默吞掉整卡节点——逐事件 continue 保留其余、解码失败要留日志）。留白定稿（breakdown 授权 plan 定填法）：envelope 无 round/target/title 字段 → `Round=0`、`Target=""`、`Title=""`，**不为填空造新账本读**。

**Interfaces**：
- Consumes：`Store.AppendMirroredEvent(cardID string, ev ledger.MirroredEvent) (bool, error)`（测试生产者）；`proto.SessionNode` 字段集。
- Produces：无新符号（`sessionNodes` 内两处日志 + 测试助手无）。

**步骤**：

1. import 确认 `"time"`（Task 4 已加）。追加测试：

```go
// TestSessionNodesAggregation 任务节点投影（breakdown §3.3 ③ + 缺陷族 2）：
// 卡 ∈ 会话的 task_mirrored（含 node 字段）聚合进 Nodes；卡 ∉ 会话的不出现；
// envelope 无 round/target/title 字段 → Round/Target/Title 留空（plan 定稿：
// 不为填空造新账本读）；单条载荷解码失败只跳该行、同卡其余节点保留。
// 本测试写下去应绿（行为已在）——红即停手上报。
func TestSessionNodesAggregation(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	cardA := sessionCard(t, st, "节点观察卡")
	cardB := sessionCard(t, st, "无会话卡")
	session1, err := svc.CreateSession("节点会话", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.JoinCard(session1.ID, cardA.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	now := time.Now()
	if _, err := st.AppendMirroredEvent(cardA.ID, ledger.MirroredEvent{
		Target: "tgt-1", Task: "task-1", Node: "implement", SourceSeq: 1,
		Type: "task_started", Payload: []byte(`{"step":1}`), CreatedAt: now,
	}); err != nil {
		t.Fatal(err)
	}
	// 解码失败样本：非法 JSON 内层 → 组合载荷整体不可解析 → 跳该行。
	if _, err := st.AppendMirroredEvent(cardA.ID, ledger.MirroredEvent{
		Target: "tgt-1", Task: "task-1", Node: "review", SourceSeq: 2,
		Type: "task_broken", Payload: []byte(`{"broken`), CreatedAt: now,
	}); err != nil {
		t.Fatal(err)
	}
	// 无会话卡 B 的镜像 → 不进 Nodes。
	if _, err := st.AppendMirroredEvent(cardB.ID, ledger.MirroredEvent{
		Target: "tgt-2", Task: "task-2", Node: "plan", SourceSeq: 3,
		Type: "task_started", Payload: []byte(`{}`), CreatedAt: now,
	}); err != nil {
		t.Fatal(err)
	}
	d1, err := svc.SessionDetail(session1.ID)
	if err != nil {
		t.Fatal(err)
	}
	if len(d1.Nodes) != 1 {
		t.Fatalf("应恰一条节点（解码失败跳过、他会话卡不进）: %+v", d1.Nodes)
	}
	node := d1.Nodes[0]
	if node.CardID != cardA.ID || node.Node != "implement" || node.State != "task_started" {
		t.Fatalf("节点投影字段错误: %+v", node)
	}
	if node.Round != 0 || node.Target != "" || node.Title != "" {
		t.Fatalf("Round/Target/Title 应留空（envelope 无对应字段，不为填空造新账本读）: %+v", node)
	}
}
```

2. **跑绿确认**：`go test ./internal/collab -run TestSessionNodesAggregation -count=1` 退出 0；红即停手上报（同 Task 4 纪律）。
3. 加日志（缺陷族 2 欠账）：`sessionNodes` 的解码守卫两分支替换为（逐字）：

```go
			if err := json.Unmarshal(ev.Payload, &envelope); err != nil {
				log().Warn("task_mirrored 载荷不可解析，节点投影跳过该行",
					"seq", ev.Seq, "card", ev.CardID, "cause", err)
				continue
			}
			if envelope.Node == nil {
				log().Warn("task_mirrored 载荷缺 node 字段，节点投影跳过该行",
					"seq", ev.Seq, "card", ev.CardID)
				continue
			}
```

4. **变异验证**（缺陷族 2「不得静默吞掉整卡节点」）：临时把步骤 3 首个分支的 `continue` 改成 `break`（解码失败即中止整卡扫描）→ 复跑必须红在「应恰一条节点」（有效节点被吞）→ 还原 → 复跑绿。输出记台账。
5. 局部回归 `go test ./internal/collab -count=1` 退出 0；提交。

**验收栏**：
- breakdown §3.3 ③ 第 4 条字面：卡 ∈ 会话的 `task_mirrored`（含 node 字段）聚合进 `Nodes`，卡 ∉ 会话的不出现；`Round`/`Target` 词表位为空（留白定稿含 `Title`，同判据）。
- 变异红/还原绿在台账；`go test ./internal/collab -count=1` 退出 0。
- 缺陷族映射：族 2 → 日志 + break 变异 + 逐事件 continue 保留其余（测试隐含：cardA 的有效节点在坏样本之后仍被投影）；族 6 → `SessionNode` 键集由 proto 金样本锁（`TestSessionDetailGoldenProjection`，Task 7 复跑）；族 3/5/9 不适用。

## 9. Task 6：B287 双会话反例——只清本房间不牵连他房间（审查移交）

**意图与为什么**：B358.1 审查移交本卡的反例欠账：会话房间的「回复即清提及」（B287 语义，B358.1 已延伸到会话）必须**按房间清**——用户在会话一回复，只清会话一的未消费提及；他在会话二被 @ 的提及必须原样留在收件箱 mention 源。现状 `consumeRoomMentions` 以 `room.SameRoom(ev, roomID)` 过滤（`internal/collab/service.go:185`），预期绿——补的是防回归的锁。**若实测红：停手**——修复面在 `service.go#consumeRoomMentions`，越出本卡有界文件集（sessions.go/sessions_test.go），升级协调者待拍板，不得自行越界修。

**Interfaces**：
- Consumes：`Service.Send` / `Service.Mentions`（既有缝）；`Store.AddSessionMember`（测试直用真 Store）；`room.ConsumedSeqs(events, consumer)`（测试侧读消费标记）。
- Produces：无生产代码改动；零新助手。

**步骤**：

1. 追加测试（import 已齐：`room` 包既有 import）：

```go
// TestMentionClearScopedToSessionRoom B287 审查移交反例：会话房间「回复即清
// 提及」只清本房间——user:alice 在两场会话各被 @ 一次，仅回复会话一时，会话
// 一的提及被消费（EvMessageConsumed 在），会话二的提及必须仍留在收件箱
// mention 源（不牵连他房间）。预期绿（consumeRoomMentions 已按 SameRoom
// 过滤）；实测红即越界修复面，停手上报。
func TestMentionClearScopedToSessionRoom(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	session1, err := svc.CreateSession("清提及-会话一", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	session2, err := svc.CreateSession("清提及-会话二", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	for _, sid := range []string{session1.ID, session2.ID} {
		if err := st.AddSessionMember(sid, "user:alice", "user:sy"); err != nil {
			t.Fatal(err)
		}
	}
	seq1, err := svc.Send(session1.ID, proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "@alice 看会话一", Mentions: []string{"user:alice"},
	}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	seq2, err := svc.Send(session2.ID, proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "@alice 看会话二", Mentions: []string{"user:alice"},
	}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	mentions, err := svc.Mentions("user:alice", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(mentions) != 2 {
		t.Fatalf("两条提及都应在收件箱: %+v", mentions)
	}
	// alice 只回复会话一。
	if _, err := svc.Send(session1.ID, proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: "收到，会话一处理中",
	}, "user:alice"); err != nil {
		t.Fatal(err)
	}
	mentions, err = svc.Mentions("user:alice", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(mentions) != 1 || mentions[0].Seq != seq2 {
		t.Fatalf("会话二的提及必须保留、会话一的应被清: %+v", mentions)
	}
	// 权威在账本消费标记：seq1 已消费、seq2 未消费。
	events, err := st.EventsFromAsc([]string{}, 0, 1000)
	if err != nil {
		t.Fatal(err)
	}
	consumed := room.ConsumedSeqs(events, "user:alice")
	if !consumed[seq1] {
		t.Fatalf("会话一提及应落 EvMessageConsumed: seq=%d", seq1)
	}
	if consumed[seq2] {
		t.Fatalf("会话二提及不得被牵连消费: seq=%d", seq2)
	}
}
```

2. **跑绿确认**：`go test ./internal/collab -run TestMentionClearScopedToSessionRoom -count=1` 退出 0。红 → **停手上报岔口**（见任务头）。
3. **变异验证**（证明反例咬合）：**临时**把 `internal/collab/service.go#consumeRoomMentions` 循环条件里的 `!room.SameRoom(ev, roomID) ||` 摘除 → 复跑必须红在「会话二提及不得被牵连消费」→ **当场还原** → 复跑绿。声明：这是当场还原的临时探针（验证测试能红），最终 diff 必须零触碰 service.go——提交前 `git diff --stat 0be4a2c6 -- internal/collab` 复核只有 sessions.go/sessions_test.go 两文件（入场前既有脏文件清单见 Task 7 步骤 3，不纳入核对）。
4. 局部回归 `go test ./internal/collab -count=1` 退出 0；提交（零生产代码改动）。

**验收栏**：
- `go test ./internal/collab -run TestMentionClearScopedToSessionRoom -count=1` 退出 0；变异红/还原绿输出在台账。
- 缺陷族映射：族 4（假红/假绿）→ 变异步骤；族 1（生命周期）→ 无（一次性进程、无孤儿资源）；族 6 → 无新序列化边界。日志/注释：零生产改动，N/A（声明）。

## 10. Task 7：收口——全量绿 + 金样本复跑 + 源码级对齐审查

**意图与为什么**：S2 各任务只跑局部；收口跑全量与契约金样本，并以源码审查清单钉住「词表对齐 / 零 import 越界」这类 grep 可判的事实（缺陷族 7 的镜像应用）。归档后详情可读（缺陷族 1）的断言落在此处。

**Interfaces**：Consumes 全部既有。Produces：无。

**步骤**：

1. 追加测试：

```go
// TestSessionDetailReadableAfterArchive 缺陷族 1：会话归档后详情投影仍可读
// （归档只读执法在写路径 Send/JoinCard，不封读面），timeline 含自己的
// created 与 archived 两行。
func TestSessionDetailReadableAfterArchive(t *testing.T) {
	svc, _, _ := newSessionFixture(t)
	session, err := svc.CreateSession("归档后读", "user:sy", "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := svc.ArchiveSession(session.ID, "user:sy"); err != nil {
		t.Fatal(err)
	}
	detail, err := svc.SessionDetail(session.ID)
	if err != nil {
		t.Fatalf("归档后详情必须可读: %v", err)
	}
	kinds := map[string]bool{}
	for _, row := range detail.Timeline {
		kinds[row.Kind] = true
	}
	if !kinds[proto.SessionEventCreated] || !kinds[proto.SessionEventArchived] {
		t.Fatalf("归档后 timeline 应含 created 与 archived: %+v", detail.Timeline)
	}
}
```

2. 全量与金样本（逐条记录退出码）：
   - `go build ./...` → 退出 0。
   - `go test ./internal/collab/... -count=1` → ok（含 room 子包守卫，S1 成果不回退）。
   - `go test ./internal/proto -run 'TestSession' -count=1` → ok（金样本 4 支：Summary / Detail / 成员状态词表 / timeline kind 词表——投影不新增 wire 键的证据。**判据字面调整**：breakdown §3.3 ③ 写的 `-run TestSessionsFixture` 实测零匹配（proto 无该名测试），以 `-run 'TestSession'` 等价落地，见待拍板 1）。
3. 源码级审查清单（逐条 pass/fail 记台账；fail 即修，修完复跑第 2 步）：
   - `grep -rn '"github.com/Xsxdot/handoff/internal/ledger"' internal/collab --include='*.go' | grep -v _test` → 零命中（契约条 36）。
   - `sessionTimeline` 的 kind 赋值全部取 `proto.SessionEvent*` 常量，无裸字面量 kind（读码核对）。
   - `default: continue` 保留——未知 ledger 事件类型不进 timeline（缺陷族 7）。
   - `sessionNodes` 解码失败分支保留逐事件 `continue`（缺陷族 2）。
   - `git diff --stat 0be4a2c6 -- internal/collab` → 只含 `sessions.go` 与 `sessions_test.go` 两个文件（有界文件集复核）；`docs/superpowers/` 下本卡新增只许 `plans/b358.2-plan.md` 与 `ledgers/2026-09-12-b358.2-plan-ledger.md` 两个文件。工作树里与本卡无关的**入场前既有**脏文件——`web/src/app/task/Composer.test.tsx`、`.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、`web/design-qa*`——不在核对范围、**不得触碰**（2026-09-12 status 读数，台账 §二有原文）。
4. 图覆盖债移交：把本卡新测试函数名与 `sessionTimeline` 新签名列进台账 §三，供 recon 补视图 diff。
5. 提交。

**验收栏**：第 2 步三条命令全部通过且读数在台账；第 3 步清单逐条 pass；新增测试绿（写下去应绿，红即停手上报）。缺陷族映射：族 1 → 归档后详情可读断言；族 7 → 词表对齐清单；其余族已在各任务栏。

## 11. 五项检查（plan 出稿自审）

1. **缺陷族对抗审查**（逐族结论已映射进对应任务验收栏，汇总）：族 1 生命周期→Task 7（归档可读；纯投影无写、无孤儿资源）；族 2 静默失败→Task 1/5（解析失败留日志 + break 变异）+ 各任务「正常过滤不加日志」声明；族 3 跨平台→无，纯 Go 投影不触路径/进程/webview；族 4 假红/假绿→Task 1/2/3 先红后绿 + Task 4 同源拨钟即翻 + Task 6 变异；族 5 门禁绕过→不适用（只读投影无新写路径）；族 6 序列化边界→透传断言（Task 1/2/3）+ 键集金样本（Task 7）；族 7 枚举白名单→kind 常量化 + 词表 8 值扫 + default continue（Task 2/7）；族 8 承重安全属性→「不报不可证实的」（Task 4 变异）与「归属正确」（Task 1 红绿）；族 9 webview→无，渲染归 S6。
2. **序列化边界设问**：本卡零新增 wire 字段、零新增手写序列化——投影输出走既有 `json.Marshal` + proto 金样本锁键集；对既有透传型边界（事件 payload → `Detail` 字符串）加了原样保留断言（Task 1/2/3）。roundtrip 属性测试不命中（无 encode 新形状），此为声明非缺席。
3. **上下文预算**：每个任务圈定 `internal/collab/sessions.go` + `internal/collab/sessions_test.go` 两个文件（Task 6 的临时探针例外已声明、零最终 diff）。
4. **类型标注**：逻辑型——全部验收机内闭环；不设真机步骤。真机清单 #5（成员状态生产诚实性的真实账本读数）归协调者（breakdown §5），本计划只冒充过它的机内半边。
5. **接缝覆盖（双向，对照 spec §6 接缝清单中本卡相关的 #1/#5）**：
   - 测试→缝：全部新测试入口符号 = `SessionDetail`（Task 1/2/3/4/5/7）、`Mentions`+`Send`（Task 6）、`ArchiveSession`→`SessionDetail`（Task 7）——全在接缝 #1（入站门面）/#5（投影缝装配点）上；无直调未导出函数的入口。
   - 缝→测试：接缝 #5 的三块投影各有缝级断言——timeline（Task 1/2/3）、成员状态（Task 4）、任务节点（Task 5）；接缝 #1 由 Task 6（Send/Mentions 语义）与 Task 7（归档读面）锁住。两个方向齐，无内部锁需声明（变异探针非测试）。

## 12. 待拍板岔口（不阻塞派发）

1. **判据字面修正**：breakdown §3.3 ③ 第 5 条 `-run TestSessionsFixture` 在 `internal/proto` 零匹配（该包无该名测试；会话侧金样本实为 `TestSessionSummaryGoldenProjection` 等 4 支；`-run` 是正则，零匹配=空跑假绿）。计划以 `-run 'TestSession'` 等价落地（命中上述 4 支；`TestRoomMessageReplyToGolden` 不被命中、且与投影键集无关）。请协调者追认（如需，可在 breakdown 补一行修订记录）。
2. **发现 A 同族扩修**：发现 A 字面只点名 driver_takeover 全量卡表偏差；计划按「timeline 只装本会话的结构事实」的意图把 `session_created`（载荷键错位）与 `session_card_joined/left`（无判定）两处同族偏差一并修（Task 1，各有反例）。拒绝补丁式修复原则的直接应用；请协调者追认。
3. **条件岔口**：Task 6 若实测红，修复面在 `service.go#consumeRoomMentions`（越出本卡有界文件集）——届时停手上报，由协调者裁决（扩界或另立条目）。现状读数判断预期绿（`service.go:185` 已按 `SameRoom` 过滤）。

## 13. 出稿自检三查

- **spec 覆盖**（breakdown §3.3 ③ 逐条 → task）：①跨会话污染反例→Task 1；②needs_human 行 + label 翻转→Task 3；③成员状态诚实判据（含条 18 时钟与变异）→Task 4；④SessionNode 聚合与留白→Task 5；⑤proto 金样本保持绿→Task 7。R2/R3 增补条目（seat_bound/card_closed，契约欠账 §8.10）→Task 2。审查移交（B287 双会话反例）→Task 6。归档可读（缺陷族 1）→Task 7。**无遗漏。**
- **占位符扫描**：全部测试与实现代码成块给出、可逐字落盘；无 TBD/「同 Task N」/纯描述骨架；未使用「复用既有 harness 省略代码」的例外出口（台账 §六已声明）。
- **跨 task 类型/签名一致**：Task 1 产出的 `sessionTimeline(events, session)` 新签名与 `structEventFields` 被 Task 2/3 的代码块逐字消费；测试助手 `sessionStructSeqs`/`cardSet`（Task 1）、`memberByCard`/`memberByIdentity`（Task 4）一次定义、无重名（已 grep 全包核过）；import 增量（`strings` Task 2 前、`time` Task 4 前）已逐任务注明。

---

## 协调者拍板（补记，2026-09-12 复工）

§岔口三条裁决：

1. **判据字面修正：准。**基线亲证 `-run TestSessionsFixture` 零匹配（no tests to run，假绿），`-run 'TestSession'` 命中 4 支金样本。breakdown §3.3 ③条5 回写修订记录（同批提交）。
2. **发现 A 同族扩修：准。**`session_created` 载荷键错位与 `card_joined/left` 无判定与 takeover 归属偏差同违「timeline 只装本会话的结构事实」一条契约，并入 Task 1、各配独立反例是对的——同族不拆三张卡。
3. **条件岔口（T6 红）：维持停手上报。**`consumeRoomMentions` 在 service.go，越出 S2 有界集；实测红则停，由协调者裁扩集或另立卡（先例：B358.1 的 Store 短路扩集）。现状读数预期绿，探针变异当场还原零最终 diff，已声明，准。
