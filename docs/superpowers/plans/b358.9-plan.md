# B358.9 实现计划：会话身份本质重做——一名多端、人名收口、端只落款

读者：零上下文实现执行者。工作目录：本工作树（当前分支 `cards/B358.9-charter-6`）。
上游 spec：`docs/superpowers/specs/b358.9.md`（头部「**状态**：**已批准（2026-09-17 用户批准）**」）。
冻结契约：`docs/superpowers/specs/b358.9-contract.md`（头部「**上游状态：已批准**」「**冻结状态：本提交随 … 同批冻结**」）。
拆解稿：`docs/superpowers/specs/b358.9-breakdown.md`（状态「**已拍板（2026-09-17，用户拍板；P-1..P-5 全按甲）**」）。
本计划台账：`docs/superpowers/ledgers/2026-09-18-b358.9-plan-ledger.md`（本节点全部事实、命令原文）。
有效基线：`cards/B233.1-charter-7` @ `94246fc8`（`git merge-base HEAD cards/B233.1-charter-7` 实读）。

**边界（红线）**：只在当前分支工作；不切分支、不改 git 配置、不 push。只改下文各 task「有界文件集」内的文件；
集合外生产改动退回协调者。本节点只出计划，不写实现、不建脚手架。

**编号权威**：本计划的任务号沿用 breakdown 的 S1–S4，但**计划内一律称 T1–T4**；冻结清单条目号沿用 contract §5 的 A–J。

---

## 0. 拍板约束（不变量，违反即失败）

来自 breakdown §0 的裁决（P-1..P-5 全甲）与 contract 冻结语义，实现必须逐条照做，不得改乙：

- **P-1 甲**：会话/房间写面未配名一律 **403 + 文案含 `console_user`**；不新增专用状态码。
- **P-2 甲**：读面 `GET /api/sessions`、`GET /api/rooms` 与写面同门（未配名 403 + 同文案）；**不**降级放行。
- **P-3 甲**：`sessionMembers` kind 修正只改生产分支；`TestSessionExplicitMembers` 的 `cli:` 形状夹具**只改夹具**为 `user:`；
  `AddSessionMember` **不加**统一记法白名单门（B366 自助加入与 CLI 夹具仍走它）。
- **P-4 甲**：控制台 `isSelfActor` 取服务端 `GET /api/identity` 的 `member` **精确等值**；拉取失败回落 `actor.startsWith('user:')`；
  取消 `startsWith('web:')`。
- **P-5 甲**：**T1 → T4 硬顺序**（T4 合并/端到端验收在 T1 之后；T4 的 vitest 可先绿）。T2/T3 与 T1 无文件交叠、可与 T1 并行。
- **C-1**：会话发言复用 `handleRoomSend`（`POST /api/rooms/{id}/messages`），**不存在 `handleSessionSend`**。
- **C-2**：`internal/agentd/ledgerapi.go` 的 `web:` 卡面审计 actor **不在本卡范围**，原样保留。
- **C-3**：测试侧零处构造 `identity{...}` 字面量；夹具换脸的实际对象是 `webUserMember` 常量与 `web:` 断言。
- **决定 8 fail-closed**：认不出人名 → 拒绝 + 可行动报错；**禁止旧脸回落**；端戳缺失不属于失败。
- **决定 1**：端戳只做落款，**不参与**写权限/@ 路由/已读/成员名单。
- **不新增领域/容器**（contract §4）；图视图 `cards-B358.9-charter-4` 不变。

---

## 1. 基线事实（动手前先复核，原始输出见台账 §4）

| 事实 | 命令 | 基线结果（本节点实跑） |
| --- | --- | --- |
| 全仓编译 | `go build ./...` | 退出 0，无输出 |
| proto/config 绿 | `go test ./internal/proto/ ./internal/config/ -count=1` | 两包 `ok` |
| collab 绿 | `go test ./internal/collab/... -count=1` | `collab 7.953s`、`collab/room 0.002s` 绿；client/cursor 无测试文件 |
| agentd 全量 | `go test ./internal/agentd/ -count=1 -timeout 15m` | 退出 0（首跑包级 FAIL 无具体 FAIL 行，复跑全绿；按环境抖动记） |
| cmd 全量 | `go test ./cmd/ -count=1` | **仅 `TestRepoContractGate` 红**（B370 遗留 `d_transport→d_ledger` dead-contract），其余绿 |
| CLI session 面 | `go test ./cmd/ -run 'TestSession' -count=1` | `ok .../cmd 4.611s` |
| web 依赖 | `npm ci --prefix web --ignore-scripts --no-audit --no-fund` | `added 290 packages in 2s` |
| web 类型 | `cd web && npx tsc -b` | 退出 0 |
| web 全量 | `cd web && npx vitest run` | `Test Files 131 passed (131)` / `Tests 1426 passed (1426)` |
| 图校验 | `codegraph validate --repo . --view cards-B358.9-charter-4` | 唯一 issue `[cards-B374-charter] … k_collab_model …`（既有、非本卡） |
| 图检查 | `codegraph check --repo . --view cards-B358.9-charter-4` | `fails` 仅 `dead-contract d_transport→d_ledger`（B370 遗留） |

**web 依赖说明**：`web/node_modules` 由本节点 `npm ci` 装入且被 `.gitignore` 忽略。实现节点若工作树没有它，先 `npm ci`。
**未实际跑过的 web 命令不许写成绿**；跑不了就标「未验证」。

### 1.1 依赖库/平台既成行为（契约 §1.3，逐条已核，出处如下）

- 配置严格解码：未知键让 agentd 启动失败，已知键清单是错误文案的手写串——`internal/config/config.go#decodeStrict`（`:562-574`）。
  加 `console_user` 必须同步改清单（Ticket 0 已改并在 `internal/config/config_test.go#TestConsoleUserRoundTrip` 锁定）。
- `omitempty` 是配置硬要求：空值不写盘——`internal/config/config.go#Config` PathDirs 注释（`:70`）、`#save`（`:650`）。
- 浏览器登录设备名由 ticket 登记名 + UA 浏览器名拼成，且入库前净化（`sanitizeDeviceName`）——
  `internal/agentd/authroutes.go#handleConsole`（`:167`）、`#sanitizeDeviceName`（`:67`）；`cmd/console.go:47-53` CLI 缺省主机名。
  读侧**不再二次加工**（设备名入库已净化）。
- `identity` 零值与主令牌身份不可区分是有意的——`internal/agentd/auth.go#identityFrom` 注释（`:64-71`）。
  端戳装填到 `identity.device`：主令牌恒空串，不引入新的「未鉴权」歧义。
- `sessionFromRequest` 在鉴权时已查会话行——`internal/agentd/auth.go#sessionFromRequest`（`:82`）。端戳随鉴权一次读出，
  **不二次查库**（不新增 `d_gateway→d_orchestration` 调用边）。
- 事件订阅/唤醒只读寻址，与成员集合无关——`internal/collab/room/delivery.go#ResolveDelivery`（`:30`）＋源码守卫
  `internal/collab/room/delivery_gate_test.go#TestResolveDeliveryShapeHasNoMemberSet`。端戳**不得**进入寻址判定。
- `cursor.Store` 的 `member` 是 `string`，与人名键兼容；文件 `room-cursors.json` 纯缓存非权威——
  `internal/collab/cursor/cursor.go` 包注释（`:1-5`）、`#Store.MarkRead`（`:35`）。

### 1.2 图事实与覆盖债

- 图命中（`--view cards-B358.9-charter-4`）：`Server.resolveConsoleIdentity`（`identity.go:38`、d_gateway/added）、
  `n_proto_MemberIdentity`（`identity.go:45`、d_protocol/added）、`Server.roomUserActor`（`roomsapi.go:49`、d_gateway/ok）、
  `Service.sessionMembers`（`sessions.go:219`、d_collab/ok）。
- `codegraph flow n_agentd_Server_handleRoomSend` → `degraded=true steps=0`（无控制流数据，回落读源码；**未拿 chain 冒充**）。
- **图覆盖债（未命中，出稿用 `file#Symbol` 源码锚，收尾跑 `codegraph resolve --doc` 校锚）**：`sessionWriters`、`sendToSession`、
  `validSessionOwner`、`validSessionMemberIdentity`、`handleSessionCreate`、`handleSessionsList`、`handleSessionMemberAdd`、
  `handleSessionJoinCard`、`handleSessionArchive`、`handleSessionLeaveCard`、`cmd.sessionSendActor`。

---

## 2. 有界文件集与 DAG

### 2.1 文件集（集合外生产改动退回协调者）

| Task | 子系统 | 文件集 |
| --- | --- | --- |
| **T1** | `d_gateway` | `internal/agentd/roomsapi.go`、`internal/agentd/sessionsapi.go`、`internal/agentd/roomsapi_test.go`、`internal/agentd/sessionsapi_test.go`、`internal/agentd/identity_face_test.go`（新建） |
| **T2** | `d_collab` | `internal/collab/sessions.go`、`internal/collab/sessions_test.go`、`internal/collab/service_test.go` |
| **T3** | `d_cli` | `cmd/session.go`、`cmd/session_test.go` |
| **T4** | `d_web` | `web/src/api/rooms.ts`、`web/src/api/rooms.test.ts`、`web/src/api/rooms.fetch.test.ts`、`web/src/api/testdata/RoomsFixture.json`、`web/src/app/rooms/sessionModel.ts`、`web/src/app/rooms/sessionModel.test.ts`、`web/src/app/rooms/SessionChat.tsx`、`web/src/app/rooms/SessionChat.test.tsx`、`web/src/app/rooms/NewSessionDialog.tsx`、`web/src/app/rooms/NewSessionDialog.test.tsx`、`web/src/app/rooms/sessionOwnerPrefs.ts`（删除）、`web/src/app/shell/Shell.tsx`、`web/src/app/shell/Shell.test.tsx` |

**零改动面（冻结，防扩面）**：`internal/proto/*`、`internal/config/*`、`internal/collab/cursor/cursor.go`、`internal/collab/room/*`、
`internal/ledger/*`、`internal/agentd/ledgerapi.go`（C-2）、`internal/agentd/identity.go`、`internal/agentd/auth.go`、`cmd/console.go`。
一个 task 若需触碰集合外**生产**文件，停下并回协调者重核边界（测试文件随实现同步可接受）。

### 2.2 DAG

```text
T1 gateway 端点换脸 + fail-closed + 端戳 + owner 缺省（d_gateway）──[P-5 硬顺序]──> T4 web 内容面（d_web）
T2 collab kind 修正 + 协调者/游标回归（d_collab）───────────┐
T3 CLI 协议校验收敛 + 主 agent 出示端到端（d_cli）──────────┴──> 集成（全量 + 真机，协调者）
```

- T1 必须先于 T4。T2/T3 与 T1 无文件交叠、无行为依赖，可并行写码与合并。
- **最薄路径**（架构法「最薄路径条」）：T1 要锁的「控制台 actor = `user:<console_user>` + 端戳进 payload」今天从声明缝
  （`POST /api/rooms/{id}/messages`）调用**得不到**（现产 `web:127.0.0.1`，测试写下去必红）——T1 是本卡第一条点亮该行为的路径，
  必须最先落。T2 的 `agent:` kind 行为今天也红（恒判 human），故其 kind 测试先写。T3 的 `--agent` 行为 Ticket 0 已落（写下去会绿），
  仅收敛与回归。T4 的 `fetchIdentity` 今天不存在（写下去必红），依赖 T1 的端点已存在（P-5）。

---

## 3. 接缝覆盖（双向，对照 spec §6 测试接缝清单）

spec §6 的 5 条接缝与 plan 的映射：

| spec 接缝 | 锁它的 task/测试（入口调用符号） |
| --- | --- |
| #1 agentd 身份解析（transport→人名+端，调用方 sessions/rooms 各端点） | T1：`handleRoomSend`/`handleRoomSend`（HTTP）、`requireConsoleIdentity`（经各 handler） |
| #2 collab 写者判定（`VerifyWriter`/`sessionWriters`，调用方 room send） | T2：`Service.Send`（穿写者集）、`Service.ListSessions`/`SessionDetail`（kind） |
| #3 游标换键（`cursor.Store`，调用方 readmodel/cursors） | T2：`Service.MarkRead`/`Service.Unread`/`cursor.Store.Snapshot` |
| #4 控制台身份读缝（`GET /api/identity`，调用方控制台会话页/新建表单） | T1：`handleIdentity`（HTTP）；T4：`fetchIdentity`（TS） |
| #5 CLI 出示主 agent 身份（调用方外部 harness 的 session create/wait/send） | T3：`sessionSendActor`/`sessionCreateCmd`/`runSessionWait`（CLI 命令） |

**缝 → 测试**：每条缝至少一支缝级断言（见各 task 验收表），无落空缝。
**测试 → 缝**：上述测试的入口调用符号均在清单某条缝上或穿过该缝。
**内部锁声明（默认非法、只能附加）**：
- T2 的 `TestSessionWritersExcludesEmptySeat` 直调私有 `svc.sessionWriters`（入口不在任何声明缝上）。合法理由唯一形状——
  「从声明缝 `Service.Send` 构造不出这条断言 + 为什么」：`Send` 对空 actor 恒 `ErrNotWriter`，与「书写者集是否含空串」无区分力；
  要观测书写者集内容只能直调私有方法。**故声明为内部锁，不顶替任何缝级断言**（T2 另有 `Send`/`ListSessions` 缝级断言）。
- 无其它内部锁；**无退路步骤**（本计划正文不含「若意外先绿就改成直喂 X」式条件退路）。

---

## 4. T1 —— gateway 端点换脸 + fail-closed + 端戳 + owner 缺省（d_gateway）

**契约引用**：§3.3（解析）、§3.4（读缝）、§3.5（端戳 wire）、§3.8（owner 缺省）；§5 C 组条 13–22、D 组条 23–24、H 组条 42（HTTP 半边）、
I 组条 43–45；§8.1/§8.3（HTTP 半边）/§8.4；spec 决定 1/3/6/8；用户故事 U1/U2/U3/U6/U7；breakdown P-1/P-2/P-5。

**意图与为什么**：把 Ticket 0 已落、但只有测试单点保护的 `resolveConsoleIdentity` 接进**所有**会话/房间端点，退役
`roomUserActor()="web:"+hostOnly` 旧脸；发话把登录会话设备名装进 `RoomMessage.Device`（只落款）；缺名 fail-closed（绝不回落旧脸）；
建会话 owner 缺省为解析人名。**这是 spec 决定 1/3/6/8 的核心行为，空壳不具备。**

**Consumes**（已存在，不改签名）：
```go
// internal/agentd/identity.go
type consoleIdentity struct { Member, Device string; Configured bool }
func (s *Server) resolveConsoleIdentity(r *http.Request) consoleIdentity
// internal/proto/identity.go
func MemberIdentity(kind, name string) (string, error)
func ValidateMemberIdentity(raw string) bool
const IdentityKindUser = "user"; const IdentityKindAgent = "agent"
// internal/agentd/roomsapi.go（已存在）
func writeJSON(w http.ResponseWriter, status int, v any)
// internal/agentd/ledgerapi.go（已存在）
func writeErr(w http.ResponseWriter, code int, err error)
func collabErr(w http.ResponseWriter, err error)
func sessionErr(w http.ResponseWriter, err error)
// internal/agentd/server.go（已存在）
func (s *Server) conf() *config.Config
```
**Produces**（本 task 新建/改写）：
```go
// internal/agentd/roomsapi.go
func (s *Server) requireConsoleIdentity(w http.ResponseWriter, r *http.Request) (consoleIdentity, bool) // 新建
// internal/agentd/sessionsapi.go
func validSessionOwner(owner string) bool // 改正文为 proto.ValidateMemberIdentity
// 删除 func (s *Server) roomUserActor(r *http.Request) string
// internal/agentd/sessionsapi.go::handleSessionCreate 的 owner 缺省语义
```

### 4.1 先写失败测试

新建 `internal/agentd/identity_face_test.go`（`package agentd`）。需要 import：`encoding/json`、`fmt`、`net/http`、`net/http/httptest`、
`os`、`strings`、`testing`、`time`、`github.com/Xsxdot/handoff/internal/ledger`、`github.com/Xsxdot/handoff/internal/proto`、
`github.com/Xsxdot/handoff/internal/store`。

测试辅助（同文件）：
```go
// setConsoleUser 原地换活配置里的 console_user（白盒夹具）：newRoomsEnv 默认配
// "sycm"，fail-closed 探针需要空串/非法值。Store 一份副本，避免改共享指针。
func setConsoleUser(t *testing.T, env *ledgerEnv, value string) {
	t.Helper()
	next := *env.srv.conf()
	next.ConsoleUser = value
	env.srv.cfg.Store(&next)
}

// mustNamedSession 造一个带指定设备名的登录会话，返回 cookie 明文。
func mustNamedSession(t *testing.T, st *store.Store, id, device string) string {
	t.Helper()
	plain := "cookie-" + id
	now := time.Now()
	if err := st.CreateSession(&store.Session{
		ID: id, TokenHash: store.HashCredential(plain), DeviceName: device,
		CreatedAt: now, ExpiresAt: now.Add(time.Hour), LastSeenAt: now,
	}); err != nil {
		t.Fatalf("CreateSession %s: %v", id, err)
	}
	return plain
}

// ledgerPostCookie 带 cookie 发 POST（cookie 路径才有登录会话设备名端戳）。
func ledgerPostCookie(t *testing.T, ts *httptest.Server, path, body, cookie string) (int, string) {
	t.Helper()
	req, err := http.NewRequest(http.MethodPost, ts.URL+path, strings.NewReader(body))
	if err != nil { t.Fatal(err) }
	req.Header.Set("Content-Type", "application/json")
	req.AddCookie(&http.Cookie{Name: sessionCookieName, Value: cookie})
	resp, err := ts.Client().Do(req)
	if err != nil { t.Fatal(err) }
	defer resp.Body.Close()
	data, _ := io.ReadAll(resp.Body)
	return resp.StatusCode, string(data)
}

// ledgerGetCookie 带 cookie 发 GET。
func ledgerGetCookie(t *testing.T, ts *httptest.Server, path, cookie string) (int, string) {
	t.Helper()
	req, err := http.NewRequest(http.MethodGet, ts.URL+path, nil)
	if err != nil { t.Fatal(err) }
	req.AddCookie(&http.Cookie{Name: sessionCookieName, Value: cookie})
	resp, err := ts.Client().Do(req)
	if err != nil { t.Fatal(err) }
	defer resp.Body.Close()
	data, _ := io.ReadAll(resp.Body)
	return resp.StatusCode, string(data)
}

// roomMessageEvents 返回全量账本事件（调用方自行按 type/body 过滤）。
func roomMessageEvents(t *testing.T, env *ledgerEnv) []ledger.Event {
	t.Helper()
	events, err := env.ledger.EventsFromAsc(nil, 0, 10000)
	if err != nil { t.Fatal(err) }
	return events
}
```
（补 import `io`。）

**（a）换脸 + 端戳（缝级，#1/#4；条 13/16/17/23/24）**
```go
func TestConsoleFaceActorAndDeviceStamp(t *testing.T) {
	env := newRoomsEnv(t) // console_user = "sycm"
	session := mustConsoleSession(t, env, "换脸场")
	cookie := mustNamedSession(t, env.st, "face-device", "mbp / Safari")
	code, body := ledgerPostCookie(t, env.testAgentdEnv, "/api/rooms/"+session.ID+"/messages", `{"body":"你好"}`, cookie)
	if code != 200 {
		t.Fatalf("cookie 发言应 200: %d %s", code, body)
	}
	found := false
	for _, ev := range roomMessageEvents(t, env) {
		if ev.Type != ledger.EvRoomMessage { continue }
		var msg proto.RoomMessage
		if err := json.Unmarshal(ev.Payload, &msg); err != nil { t.Fatal(err) }
		if msg.Body != "你好" { continue }
		found = true
		if ev.Actor != consoleMember { // consoleMember == "user:sycm"
			t.Fatalf("actor 应换脸为解析人名 %q，实得 %q", consoleMember, ev.Actor)
		}
		if msg.Device != "mbp / Safari" {
			t.Fatalf("端戳应为登录会话设备名，实得 %q", msg.Device)
		}
	}
	if !found { t.Fatal("消息未落账") }
}
```
> 变异：`handleRoomSend` 回 `"web:"+hostOnly` → actor 断言红；不装填 Device → 端戳断言红。

**（b）未配名 fail-closed（缝级，#8.1；条 14/16/43–44 + P-1/P-2 + U3）**
```go
func TestConsoleFaceFailClosedWithoutName(t *testing.T) {
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "缺名场")
	before := len(roomMessageEvents(t, env))
	setConsoleUser(t, env, "")
	probes := []struct{ method, path, body string }{
		{"POST", "/api/rooms/" + session.ID + "/messages", `{"body":"x"}`},
		{"POST", "/api/rooms/" + session.ID + "/read", `{"upto_seq":1}`},
		{"POST", "/api/sessions", `{"title":"x"}`},
		{"POST", "/api/sessions/" + session.ID + "/members", `{}`},
		{"POST", "/api/sessions/" + session.ID + "/archive", `{}`},
		{"GET", "/api/inbox", ""},
		{"GET", "/api/sessions", ""},
		{"GET", "/api/rooms?limit=50", ""},
	}
	for _, p := range probes {
		var code int
		var body string
		if p.method == http.MethodGet {
			code, body = ledgerGet(t, env.testAgentdEnv, p.path)
		} else {
			code, body = ledgerPost(t, env.testAgentdEnv, p.path, p.body)
		}
		if code != http.StatusForbidden {
			t.Fatalf("%s %s 未配名必须 403，实得 %d %s", p.method, p.path, code, body)
		}
		if !strings.Contains(body, "console_user") {
			t.Fatalf("%s %s 拒收文案应含 console_user（可行动）: %s", p.method, p.path, body)
		}
	}
	after := len(roomMessageEvents(t, env))
	if after != before {
		t.Fatalf("未配名写面必须零落账: before=%d after=%d", before, after)
	}
}

func TestConsoleFaceRejectsIllegalName(t *testing.T) {
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "非法名场")
	setConsoleUser(t, env, "bad name")
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/rooms/"+session.ID+"/messages", `{"body":"x"}`)
	if code != http.StatusForbidden || !strings.Contains(body, "console_user") {
		t.Fatalf("非法 console_user 必须 fail-closed 403+文案: %d %s", code, body)
	}
}
```
> 变异：缺名回落到 `web:<host>` → 403 断言红（得到 200/400）；漏判某写面 → 该 probe 红。

**（c）建会话 owner 缺省（缝级，#4 条 43–45）**
```go
func TestSessionCreateOwnerDefaultsToConsoleIdentity(t *testing.T) {
	env := newRoomsEnv(t)
	code, body := ledgerPost(t, env.testAgentdEnv, "/api/sessions", `{"title":"默认群主"}`)
	if code != 200 {
		t.Fatalf("缺 owner 已配名应 200 且缺省为人名: %d %s", code, body)
	}
	var session proto.Session
	if err := json.Unmarshal([]byte(body), &session); err != nil { t.Fatal(err) }
	if session.Owner != consoleMember {
		t.Fatalf("缺省 owner 应为解析人名 %q，实得 %q", consoleMember, session.Owner)
	}
	// 显式 owner 仍按统一记法校验并保留。
	code, body = ledgerPost(t, env.testAgentdEnv, "/api/sessions", `{"title":"显式","owner":"agent:opencode"}`)
	if code != 200 {
		t.Fatalf("显式合法 owner: %d %s", code, body)
	}
	if err := json.Unmarshal([]byte(body), &session); err != nil { t.Fatal(err) }
	if session.Owner != "agent:opencode" {
		t.Fatalf("显式 owner 应保留: %q", session.Owner)
	}
	if code, _ = ledgerPost(t, env.testAgentdEnv, "/api/sessions", `{"title":"x","owner":"web:1"}`); code != 400 {
		t.Fatalf("显式机器位 owner 应 400: %d", code)
	}
	if code, _ = ledgerPost(t, env.testAgentdEnv, "/api/sessions", `{"title":"x","owner":"mallory"}`); code != 400 {
		t.Fatalf("显式裸名 owner 应 400: %d", code)
	}
}
```

**（d）多端已读按人合并（缝级，#3/#1 调用方；U2 机内可行版）**
```go
func TestConsoleReadMergesAcrossDevices(t *testing.T) {
	env := newRoomsEnv(t)
	session := mustConsoleSession(t, env, "多端已读场")
	if _, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "一"}, consoleMember); err != nil {
		t.Fatal(err)
	}
	seq2, err := env.srv.rooms.Send(session.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "二"}, consoleMember)
	if err != nil { t.Fatal(err) }
	cookieA := mustNamedSession(t, env.st, "dev-a", "设备A")
	cookieB := mustNamedSession(t, env.st, "dev-b", "设备B")
	// A 端全量已读。
	if code, body := ledgerPostCookie(t, env.testAgentdEnv, "/api/rooms/"+session.ID+"/read",
		fmt.Sprintf(`{"upto_seq":%d}`, seq2), cookieA); code != 200 {
		t.Fatalf("A 端已读: %d %s", code, body)
	}
	// B 端拉列表：同一个人的游标键 => 未读 0（端不拆红点）。
	code, body := ledgerGetCookie(t, env.testAgentdEnv, "/api/sessions", cookieB)
	if code != 200 {
		t.Fatalf("B 端列会话: %d %s", code, body)
	}
	var out struct{ Sessions []proto.SessionSummary `json:"sessions"` }
	if err := json.Unmarshal([]byte(body), &out); err != nil { t.Fatal(err) }
	for _, s := range out.Sessions {
		if s.ID == session.ID && s.Unread != 0 {
			t.Fatalf("一端已读应各端都清（按人合并），实得 unread=%d", s.Unread)
		}
	}
}
```

**（e）源码守卫（禁止旧脸回落）**
```go
func TestConsoleFaceNoWebFallbackSourceGuard(t *testing.T) {
	for _, path := range []string{"roomsapi.go", "sessionsapi.go"} {
		raw, err := os.ReadFile(path)
		if err != nil { t.Fatalf("读 %s: %v", path, err) }
		if strings.Contains(string(raw), `"web:"`) {
			t.Errorf("%s 仍有旧传输层脸字面量 \"web:\"（应已换脸为 resolveConsoleIdentity）", path)
		}
	}
}
```

**（f）既有夹具换脸（P-3 上半）**
1. `internal/agentd/roomsapi_test.go`：把常量 `webUserMember = "web:127.0.0.1"` 改为
   `const consoleMember = "user:sycm"`，并在两个测试文件内全文替换标识符 `webUserMember` → `consoleMember`（`roomsapi_test.go` 17 处、
   `sessionsapi_test.go` 10 处）。改注释说明 `newRoomsEnv` 配 `console_user="sycm"`。
2. `roomsapi_test.go` 的 `newRoomsEnv` 末尾追加配名（`roomsapi_test.go:70-75`）：
   ```go
   func newRoomsEnv(t *testing.T) *ledgerEnv {
       t.Helper()
       env := newNoPTYLedgerEnv(t)
       seedAgentdLedger(t, env.ledger, "bug")
       SetupAutomationForTest(t, env.srv, env.ledger)
       // B358.9：房间/会话面换脸后必须有 console_user，否则全部端点 fail-closed。
       next := *env.srv.conf()
       next.ConsoleUser = "sycm"
       env.srv.cfg.Store(&next)
       return env
   }
   ```
3. `roomsapi_test.go` 的 `TestRoomSendEndpoint` 断言 `strings.HasPrefix(ev.Actor, "web:")` 改为 `ev.Actor != consoleMember`。
4. `sessionsapi_test.go` 的 `TestSessionsCreateEndpoint` 中「owner 缺失 → 400」断言改为「已配名 → 200 且 owner == consoleMember」
   （契约 §3.8：缺省 = 解析人名）。保留 `web:`/裸名 400 两条反例与 `actor` 落账断言。

**跑红**（只跑触及包）：
```bash
go test ./internal/agentd/ -count=1 -run 'TestConsoleFace|TestSessionCreateOwnerDefaultsToConsoleIdentity|TestConsoleReadMergesAcrossDevices' -v
```
基线必然失败（actor 仍 `web:`、未配名不拒收、owner 必填）——把真实失败原文追加台账。

### 4.2 最小实现

**（1）`internal/agentd/roomsapi.go`：新增统一身份门，退役 `roomUserActor`。**

```go
// requireConsoleIdentity 解析本请求的控制台身份；未配名/非法名时写 403 可行动
// 错误并返回 ok=false（调用方立即 return）。这是会话/房间「需要谁是我」端点的
// 统一身份门（B358.9 决定 8 fail-closed）：未配名 ≠ 非成员——文案写清 console_user，
// 前端据 GET /api/identity 的 configured 渲染横幅并把原文透传（breakdown P-1 甲）。
func (s *Server) requireConsoleIdentity(w http.ResponseWriter, r *http.Request) (consoleIdentity, bool) {
	id := s.resolveConsoleIdentity(r)
	if !id.Configured {
		s.log.Warn("控制台身份未配置，请求拒收（fail-closed）", "method", r.Method, "path", r.URL.Path)
		writeErr(w, http.StatusForbidden, errors.New(
			"本机未配置控制台人名 console_user，无法确定身份；请在 agentd 配置文件中设置 console_user 后重试"))
		return consoleIdentity{}, false
	}
	return id, true
}
```
删除 `roomUserActor`（`:46-51`）整段。改 `handleRoomsList`：
```go
func (s *Server) handleRoomsList(w http.ResponseWriter, r *http.Request) {
	project := r.URL.Query().Get("project")
	id, ok := s.requireConsoleIdentity(w, r)
	if !ok {
		return
	}
	member := id.Member
	params, err := parseRoomsListParams(r)
	// …其余不变（member 变量替换 roomUserActor 调用）…
```
改 `handleRoomSend`（换脸 + 端戳）——身份门置于最前（fail-closed 最早）：
```go
func (s *Server) handleRoomSend(w http.ResponseWriter, r *http.Request) {
	roomID := r.PathValue("id")
	id, ok := s.requireConsoleIdentity(w, r)
	if !ok {
		return
	}
	var req struct {
		Body     string   `json:"body"`
		Refs     []string `json:"refs,omitempty"`
		Mentions []string `json:"mentions,omitempty"`
		ReplyTo  int64    `json:"reply_to,omitempty"`
	}
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		writeErr(w, http.StatusBadRequest, errors.New("bad json"))
		return
	}
	if req.ReplyTo < 0 {
		writeErr(w, http.StatusBadRequest, errors.New("reply_to 必须为非负 seq；0 = 无回复锚"))
		return
	}
	if strings.TrimSpace(req.Body) == "" {
		writeErr(w, http.StatusBadRequest, errors.New("消息正文不能为空"))
		return
	}
	// 端戳只做落款：登录会话设备名随 payload 走（决定 1/3），不参与任何判定。
	seq, err := s.rooms.Send(roomID, proto.RoomMessage{
		Kind: proto.RoomMsgUser, Body: req.Body, Refs: req.Refs, Mentions: req.Mentions,
		ReplyTo: req.ReplyTo, Device: id.Device,
	}, id.Member)
	if err != nil {
		s.log.Warn("房间消息发送失败", "room", roomID, "actor", id.Member, "cause", err)
		collabErr(w, err)
		return
	}
	s.log.Info("房间消息已发送", "room", roomID, "seq", seq, "actor", id.Member)
	writeJSON(w, http.StatusOK, map[string]int64{"seq": seq})
}
```
改 `handleRoomRead`、`handleInbox`：身份门置于最前，`member := id.Member` 替换 `s.roomUserActor(r)`（含日志字段）。

**身份门覆盖清单（显式声明，防扩面/防漏）**：
- 需身份（加 `requireConsoleIdentity`）：写面 `handleRoomSend`、`handleRoomRead`、`handleSessionCreate`、`handleSessionMemberAdd`、
  `handleSessionArchive`、`handleSessionJoinCard`、`handleSessionLeaveCard`；读面 `handleRoomsList`、`handleSessionsList`、`handleInbox`。
- **不加身份门（显式豁免，理由）**：`handleRoomMessages`（房间历史）与 `handleSessionDetail`（会话详情）——二者都无 member 维度、
  无未读投影，既未被 contract §8.1 点名，也不在 P-2 点名的两个**列表**读面内。实现不得以「一致性」为由扩大 fail-closed 面。

**（2）`internal/agentd/sessionsapi.go`：校验收敛 + 各 handler 过身份门。**
```go
// validSessionOwner 校验统一记法（B358.9 §8.3 HTTP 半边）：收敛到 proto 唯一
// 定义处，退役第二份重复实现（契约 §5 H 组条 42）。
func validSessionOwner(owner string) bool {
	return proto.ValidateMemberIdentity(owner)
}
```
import 增加 `"github.com/Xsxdot/handoff/internal/proto"`；`strings` 仍用于 TrimSpace/Trim。
`handleSessionCreate`：
```go
func (s *Server) handleSessionCreate(w http.ResponseWriter, r *http.Request) {
	id, ok := s.requireConsoleIdentity(w, r)
	if !ok {
		return
	}
	var req struct {
		Title string `json:"title"`
		Owner string `json:"owner"`
	}
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		writeErr(w, http.StatusBadRequest, errors.New("bad json"))
		return
	}
	title := strings.TrimSpace(req.Title)
	if title == "" {
		writeErr(w, http.StatusBadRequest, errors.New("会话标题不能为空"))
		return
	}
	// owner 缺省 = 解析出的人名（契约 §3.8、决定 6：owner=创建者）；显式 owner
	// 仍按统一记法校验。审计 actor 恒为同一解析人名（两字段在此不再是两个来源）。
	owner := strings.TrimSpace(req.Owner)
	if owner == "" {
		owner = id.Member
	} else if !validSessionOwner(owner) {
		writeErr(w, http.StatusBadRequest,
			errors.New(`owner 必须是统一记法 user:<name> 或 agent:<name>`))
		return
	}
	session, err := s.rooms.CreateSession(title, owner, id.Member)
	if err != nil {
		s.log.Warn("建会话失败", "title", title, "owner", owner, "actor", id.Member, "cause", err)
		writeErr(w, http.StatusInternalServerError, err)
		return
	}
	s.log.Info("会话已创建", "session", session.ID, "title", title, "owner", owner, "actor", id.Member)
	writeJSON(w, http.StatusOK, session)
}
```
`handleSessionMemberAdd`、`handleSessionsList`、`handleSessionArchive`、`handleSessionJoinCard`、`handleSessionLeaveCard`：各在函数首插
```go
	id, ok := s.requireConsoleIdentity(w, r)
	if !ok {
		return
	}
```
并把 `actor := s.roomUserActor(r)` / `member := s.roomUserActor(r)` 改为 `actor := id.Member` / `member := id.Member`（含日志字段）。
`handleSessionMemberAdd` 的 body identity 校验改用 `validSessionOwner`（已收敛）。

### 4.3 关键节点日志与注释

- **日志**：`requireConsoleIdentity` 缺名拒收带 `method`/`path` 的 Warn（不静默）；成功路径沿用各 handler 既有 Info；
  失败分支带 `actor`/`member`/`cause` 上下文（已在上面代码块体现）。
- **注释**：`requireConsoleIdentity` 写职责+边界（未配名≠非成员、文案可行动）；`handleRoomSend` 端戳处写「只做落款，不参与判定」；
  `handleSessionCreate` owner 缺省处写来源与依据（§3.8/决定 6）；删除的 `roomUserActor` 不留注释残句。

### 4.4 验收

| 判据 | 断言 |
| --- | --- |
| 配名 actor=人名 + 端戳进 payload（条 17/23/24、U1） | `TestConsoleFaceActorAndDeviceStamp` |
| 未配名全写面/读面 403+文案+零落账（条 14/16、U3、P-1/P-2） | `TestConsoleFaceFailClosedWithoutName` |
| 非法 console_user 等同未配（条 15） | `TestConsoleFaceRejectsIllegalName` |
| owner 缺省=人名；显式保留；显式非法 400（条 43–45） | `TestSessionCreateOwnerDefaultsToConsoleIdentity` |
| 多端已读按人合并（U2） | `TestConsoleReadMergesAcrossDevices` |
| 源码零 `"web:"` 回落 | `TestConsoleFaceNoWebFallbackSourceGuard` |
| 两处校验收敛到 proto（条 42 HTTP 半边） | 既有 `TestSessionMemberAddEndpoint` ④ + `TestSessionsCreateEndpoint` 反例保绿 |
| 既有换脸后 agentd 全绿 | `go test ./internal/agentd/ -count=1 -timeout 15m` |

**测试范围声明**：本 task 只跑 `go test ./internal/agentd/ -count=1` 与 `go build ./...`；不跑全量。
**五项检查**：族 2（未配名≠非成员，逐写面显式判 `Configured`，文案可行动）；族 4（缺名回落是假绿温床，
用 (b) 的反例堵）；族 5（全需身份端点同门，逐端点枚举防漏）；追加设问（端戳序列化链路见 §9；游标键换名归 T2）。
**上下文预算**：文件集 5 个，有界。**类型标注**：边界型 task，验收含真机清单（§10.1/10.3）。

---

## 5. T2 —— collab kind 修正 + 协调者跟进/游标值语义回归（d_collab）

**契约引用**：§3.6（成员投影）、§3.7（游标/写者值语义）；§5 F 组条 31–35、G 组条 36–38、H 组条 39–41；§8.2/§8.8；
spec 决定 2/5；U6；breakdown P-3。

**意图与为什么**：修 `sessionMembers` 的 kind 漂移（`agent:<名>` 成员恒被判 `human`——契约 §1.2 冻结的待修断言），改为按统一记法
前缀判定；为决定 5「协调者按派生自动在成员里/可 @/能回话」与决定 2「已读按人」补**能变红**的回归断言（现状有正例、缺反例）。

**Consumes**（已存在，不改签名）：
```go
// internal/proto/identity.go
func ParseMemberIdentity(raw string) (kind, name string, err error)
const IdentityKindUser = "user"; const IdentityKindAgent = "agent"
// internal/proto/sessions.go
const SessionMemberHuman = "human"; const SessionMemberAgent = "agent"; const SessionMemberSeat = "seat"
// internal/collab/service.go（已存在）
func (s *Service) sessionWriters(session proto.Session) ([]string, error)
func (s *Service) MarkRead(member, roomID string, uptoSeq int64) error
func (s *Service) Unread(member, roomID string) (int, error)
// internal/collab/cursor/cursor.go（已存在）
func (s *Store) Snapshot(member string) (map[string]int64, error)
```
**Produces**（只改正文，签名冻结）：`func (s *Service) sessionMembers(session proto.Session, byCard map[string]proto.Card) []proto.SessionMember`。

### 5.1 先写失败测试

**（a）kind 前缀修正（缝级，#2 调用方；条 39/40，U4）**——`internal/collab/sessions_test.go`：
```go
// TestSessionMemberKindByIdentityPrefix 锁 B358.9 契约 §5 H 组条 39/40：显式成员
// kind 按统一记法前缀判定——agent:<名> 不得因不是 cli: 形状而被误报 human。
func TestSessionMemberKindByIdentityPrefix(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	session, err := svc.CreateSession("kind 场", "user:sy", "user:sy")
	if err != nil { t.Fatal(err) }
	if err := st.AddSessionMember(session.ID, "agent:opencode", "user:sy"); err != nil {
		t.Fatalf("加 agent 成员: %v", err)
	}
	detail, err := svc.SessionDetail(session.ID)
	if err != nil { t.Fatal(err) }
	if m := memberByIdentity(t, detail, "agent:opencode"); m.Kind != proto.SessionMemberAgent {
		t.Fatalf("agent:<名> 成员 kind 应为 agent，实得 %q", m.Kind)
	}
	if m := memberByIdentity(t, detail, "user:sy"); m.Kind != proto.SessionMemberHuman {
		t.Fatalf("user:<名> 成员 kind 应为 human，实得 %q", m.Kind)
	}
}
```
> 变异：分支改回 `strings.HasPrefix(identity,"cli:")` → `agent:opencode` 被判 human → 红。

**（b）席位 kind（缝级；条 41）**——`internal/collab/sessions_test.go`：
```go
func TestSessionSeatMemberKindDerivedFromCard(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	card := sessionCard(t, st, "席位 kind 卡")
	session, err := svc.CreateSession("席位 kind 场", "user:sy", "user:sy")
	if err != nil { t.Fatal(err) }
	if err := svc.JoinCard(session.ID, card.ID, "user:sy"); err != nil { t.Fatal(err) }
	if err := st.BindSeat(card.ID, "cli:opencode#seat-k", proto.SeatSourceCoordinate); err != nil { t.Fatal(err) }
	detail, err := svc.SessionDetail(session.ID)
	if err != nil { t.Fatal(err) }
	if m := memberByCard(t, detail, card.ID); m.Kind != proto.SessionMemberSeat {
		t.Fatalf("卡派生席位 kind 应为 seat，实得 %q", m.Kind)
	}
	// 席位不落成员表：session.Members 不含 cli: 席位串（条 35）。
	if got, err := svc.lc.GetSession(session.ID); err != nil {
		t.Fatal(err)
	} else {
		for _, m := range got.Members {
			if strings.HasPrefix(m, "cli:") {
				t.Fatalf("席位身份不得写进 session.Members: %v", got.Members)
			}
		}
	}
}
```
（`svc.lc` 是包内可访问的 `client.LedgerClient`；`sessions_test.go` 已 import `strings`。）

**（c）空座书写者（内部锁，声明见 §3）**——`internal/collab/service_test.go`：
```go
// TestSessionWritersExcludesEmptySeat 锁条 32：空座卡不得产空串书写者。
// 内部锁：从声明缝 Service.Send 构造不出这条断言（空 actor 恒 ErrNotWriter，
// 与「书写者集是否含空串」无区分力），故直调私有 sessionWriters——理由见 plan §3。
func TestSessionWritersExcludesEmptySeat(t *testing.T) {
	svc, st, _ := newSessionFixture(t)
	card := sessionCard(t, st, "空座书写者卡")
	session, err := svc.CreateSession("空座书写者场", "user:sy", "user:sy")
	if err != nil { t.Fatal(err) }
	if err := svc.JoinCard(session.ID, card.ID, "user:sy"); err != nil { t.Fatal(err) }
	// 不 BindSeat => driver_session 空座。
	got, err := svc.lc.GetSession(session.ID)
	if err != nil { t.Fatal(err) }
	writers, err := svc.sessionWriters(got)
	if err != nil { t.Fatal(err) }
	for _, w := range writers {
		if w == "" {
			t.Fatalf("空座不得产空串书写者: %v", writers)
		}
	}
}
```

**（d）游标值语义（缝级，#3；条 36–38）**——`internal/collab/service_test.go`：
```go
// TestCursorKeyedByMemberIdentity 锁条 36–38：游标形参仍是 member string（编译期，
// 下面直接传串），传入 user:<name> 以该串为键往返；旧脸键不得回落到人名游标
// （禁半新半旧并行判定，契约 §3.7）。
func TestCursorKeyedByMemberIdentity(t *testing.T) {
	svc, st := newFixture(t)
	sid := mustSession(t, svc, st, "游标人名场")
	seq, err := svc.Send(sid, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy")
	if err != nil { t.Fatal(err) }
	if err := svc.MarkRead("user:sy", sid, seq); err != nil { t.Fatal(err) }
	if n, err := svc.Unread("user:sy", sid); err != nil || n != 0 {
		t.Fatalf("人名键游标应生效: %v %d", err, n)
	}
	snap, err := svc.cursor.Snapshot("web:127.0.0.1")
	if err != nil { t.Fatal(err) }
	if len(snap) != 0 {
		t.Fatalf("旧脸键查询不得返回人名游标（禁半新半旧）: %v", snap)
	}
}
```

**（e）协调者跟进回归（缝级，条 31–34，U6）**：不写新实现，**保持**既有测试全绿并逐条对账：
`TestSessionMembershipEnforcement`（席位∈书写者集、非成员被拒）、`TestSessionRebindRevokesOldSeatWriter`（换绑剥权）、
`TestWakeTargetsAddressing`（@卡号命中当前席位）、`TestSessionWritersReadFailureNotWriterGuard`。运行命令：
```bash
go test ./internal/collab/ -count=1 -run 'TestSessionMemberKindByIdentityPrefix|TestSessionSeatMemberKindDerivedFromCard|TestSessionWritersExcludesEmptySeat|TestCursorKeyedByMemberIdentity|TestSessionMembershipEnforcement|TestSessionRebindRevokesOldSeatWriter|TestWakeTargetsAddressing|TestSessionWritersReadFailure' -v
```
**（f）P-3 夹具修正**：`internal/collab/sessions_test.go` 的 `TestSessionExplicitMembers`（`:302/305`）与
`TestLedgerClientSessionPassthrough`（`:333/336`）把 `"cli:claude#desktop"` 改为 `"user:claude"`（`cli:` 形状串不配当显式成员），
断言意图（幂等、恰两条）不变。

基线 `TestSessionMemberKindByIdentityPrefix` 必红（agent 被误报 human）；`TestCursorKeyedByMemberIdentity` 必绿（游标现状已按串）
——把红/绿原文追加台账。

### 5.2 最小实现（`internal/collab/sessions.go` 的 `sessionMembers`）

```go
// sessionMembers 派生会话成员：显式成员（人/主 agent）+ 会话内各卡的当前席位。
// 显式成员 kind 按统一记法前缀判定（B358.9 契约 §5 H 组条 39/40）：user:→human、
// agent:→agent。历史脏行（旧 web:/cli: 脸）不崩：kind 按最保守的 human 兜底并留痕。
// 状态只报可证实的——空座报 empty；不报「在线」。
func (s *Service) sessionMembers(session proto.Session, byCard map[string]proto.Card) []proto.SessionMember {
	members := make([]proto.SessionMember, 0, len(session.Members)+len(session.Cards))
	for _, identity := range session.Members {
		kind := proto.SessionMemberHuman
		parsedKind, _, err := proto.ParseMemberIdentity(identity)
		switch {
		case err != nil:
			log().Warn("会话成员身份非统一记法，kind 按 human 兜底",
				"session", session.ID, "identity", identity, "cause", err)
		case parsedKind == proto.IdentityKindAgent:
			kind = proto.SessionMemberAgent
		}
		status, lastActive := s.memberStatus(identity)
		members = append(members, proto.SessionMember{
			Identity: identity, Kind: kind, Status: status, LastActive: lastActive,
		})
	}
	for _, cardID := range session.Cards {
		card, ok := byCard[cardID]
		if !ok {
			continue
		}
		member := proto.SessionMember{
			Kind: proto.SessionMemberSeat, CardID: cardID, CardTitle: card.Title,
		}
		if card.DriverSession == "" {
			member.Status = proto.SessionMemberEmpty
		} else {
			member.Identity = card.DriverSession
			member.Status, member.LastActive = s.memberStatus(card.DriverSession)
		}
		members = append(members, member)
	}
	return members
}
```
席位分支与 `sessionWriters`/`cursor.Store` **零改动**（签名与值语义已符合契约 §3.7）。删旧注释「kind 由身份前缀近似（cli: 视为 agent，其余人）；精确种类归 plan」。

### 5.3 日志与注释

- **日志**：非法显式成员身份 Warn 带 `session`/`identity`/`cause`（不静默降级）。
- **注释**：`sessionMembers` 头写 kind 判据与前缀来源；脏行兜底处写「为什么」；`sessionWriters`/游标不改注释。

### 5.4 验收

| 判据 | 断言 |
| --- | --- |
| agent 成员 kind=agent、user 成员 kind=human（条 39/40） | `TestSessionMemberKindByIdentityPrefix` |
| 卡派生席位 kind=seat；席位不落成员表（条 41/35） | `TestSessionSeatMemberKindDerivedFromCard` |
| 空座不产空串书写者（条 32） | `TestSessionWritersExcludesEmptySeat`（内部锁，理由见 §3） |
| 游标以人名为键、旧键不回落（条 36–38） | `TestCursorKeyedByMemberIdentity` |
| 协调者席位∈书写者集、可 @、换绑剥权（条 31–34） | 既有 `TestSessionMembershipEnforcement`/`TestSessionRebindRevokesOldSeatWriter`/`TestWakeTargetsAddressing` 保绿 |
| collab 全量绿 | `go test ./internal/collab/... -count=1` |

**测试范围声明**：只跑 `go test ./internal/collab/... -count=1`（不跑全量）。
**缺陷族**：族 1（纯投影，无后台态、无孤儿）；族 2（`sessionWriters` 读卡失败向上传播，既有测试保持）；族 4（kind 反例、空座反例、
旧键反例各自能红）；族 7/追加（新分支过 `sessionMembers` 既有 switch；无第二白名单）。
**上下文预算/类型标注**：文件集 3 个；logic 型机内闭环。

---

## 6. T3 —— CLI 协议校验收敛 + 主 agent 出示端到端（d_cli）

**契约引用**：§3.1（统一记法）、§3.6（`sessionSendActor` 三形态）；§5 E 组条 26–30、H 组条 42（CLI 半边）；§8.3（CLI 半边）/§8.9；
spec 决定 4；U4/U5。

**意图与为什么**：把 CLI 侧 `validSessionMemberIdentity` 收敛到 `proto.ValidateMemberIdentity`（退役第二份重复实现），
并回归 spec 决定 4 的「主 agent 以启动身份自理」链路：`session create --owner agent:<名>`、`session send --agent <名>`、
`session wait <member>`。**不新增「拉人」命令**。

**Consumes**（已存在，不改签名）：
```go
// internal/proto/identity.go
func ValidateMemberIdentity(raw string) bool
func MemberIdentity(kind, name string) (string, error)
// cmd/session.go（已存在）
func sessionSendActor(agentFlag, cliFlag, sessionFlag string, cliChanged bool) (string, error)
func ledgerActor() string
func currentSeatIdentity(cli, session string) (string, error)
```
**Produces**：`validSessionMemberIdentity` 改正文为 `return proto.ValidateMemberIdentity(identity)`（或删除、调用点直用 proto）。
`sessionSendActor`/`sessionCreateCmd`/`runSessionWait` 签名与行为不变。

### 6.1 先写失败测试

`cmd/session_test.go` 追加：

**（a）统一记法收敛源码守卫（条 42 CLI 半边）**
```go
// TestSessionMemberNotationSingleDefinition 锁 B358.9 契约 §5 H 组条 42（CLI 半边）：
// 统一记法规则只在 proto 一处定义，cmd 侧不得再内联 CutPrefix 前缀判定。
func TestSessionMemberNotationSingleDefinition(t *testing.T) {
	raw, err := os.ReadFile("session.go")
	if err != nil { t.Fatalf("读 session.go: %v", err) }
	src := string(raw)
	for _, banned := range []string{`CutPrefix(owner, "user:")`, `CutPrefix(identity, "user:")`,
		`CutPrefix(owner, "agent:")`, `CutPrefix(identity, "agent:")`} {
		if strings.Contains(src, banned) {
			t.Fatalf("cmd/session.go 仍内联统一记法规则 %q——应收敛到 proto.ValidateMemberIdentity", banned)
		}
	}
}
```
（`session_test.go` 已 import `os`/`strings`；若缺则补。）

**（b）`--agent` 空串等价无 flag（条 28）**
```go
// TestSessionSendAgentEmptyFallsThrough 锁条 28：--agent 名为空串等价无 flag，
// 走 ledgerActor 默认形态（不冒充主 agent、不报错）。
func TestSessionSendAgentEmptyFallsThrough(t *testing.T) {
	dir := t.TempDir()
	_, _, _, sessionID, actor := mustSendFixture(t, dir, "空 agent 场")
	if _, _, err := runLedgerCLI(t, dir, "session", "send", sessionID, "人话", "--agent", ""); err != nil {
		t.Fatalf("空 --agent 应等价无 flag: %v", err)
	}
	if actor != ledgerActor() {
		t.Fatalf("夹具 actor 漂移: %q", actor)
	}
}
```

**（c）既有端到端保持**：`TestSessionSendAgentIdentity`（条 26/27/29）、`TestSessionSendActorResolution`（条 30）、
`TestSessionCreateCommand`（`--owner agent:<名>` 语义）逐条对账。

跑红：
```bash
go test ./cmd/ -count=1 -run 'TestSessionMemberNotationSingleDefinition|TestSessionSendAgentEmptyFallsThrough|TestSessionSendAgentIdentity|TestSessionSendActorResolution' -v
```
基线 `TestSessionMemberNotationSingleDefinition` 必红（现有内联 CutPrefix）；其余应绿（Ticket 0 已落）。原文追加台账。

### 6.2 最小实现（`cmd/session.go`）

```go
// validSessionMemberIdentity 校验会话成员统一记法（B358.9 §8.3 CLI 半边）：
// 收敛到 proto 唯一定义处，退役与 gateway 侧同规则的重复实现（契约条 42）。
func validSessionMemberIdentity(identity string) bool {
	return proto.ValidateMemberIdentity(identity)
}
```
（删除函数体内两段 `strings.CutPrefix`；`strings` 仍用于 `TrimSpace`/`Join`，import 保留。`proto` 已 import。）
`sessionSendActor`、`sessionCreateCmd`、`runSessionWait` **零改动**（契约 §3.6 冻结）。

### 6.3 日志与注释

- **日志**：沿用既有入口/失败 Warn（不改）；无新增静默路径。
- **注释**：`validSessionMemberIdentity` 注释改为「收敛到 proto 唯一处」；删「统一收口归后续重构卡」旧注。

### 6.4 验收

| 判据 | 断言 |
| --- | --- |
| 统一记法单一定义（条 42 CLI 半边） | `TestSessionMemberNotationSingleDefinition` |
| `--agent` 落账 actor=agent:<名>；互斥；非法 fail-closed 零落账（条 26/27/29） | 既有 `TestSessionSendAgentIdentity` 保绿 |
| 三形态优先级（条 30） | 既有 `TestSessionSendActorResolution` 保绿 |
| 空 `--agent` 等价无 flag（条 28） | `TestSessionSendAgentEmptyFallsThrough` |
| `create --owner agent:` / `wait` 回归（U4/U5） | 既有 `TestSessionCreateCommand`/`TestSessionWait*` 保绿 |
| CLI 全量（除 B370 遗留 `TestRepoContractGate`） | `go test ./cmd/ -count=1` |

**测试范围声明**：只跑 `go test ./cmd/ -run 'TestSession' -count=1` 与 `go build ./...`；`TestRepoContractGate` 的
`d_transport→d_ledger` dead-contract 是基线既有红（B370 遗留），**不计入本 task**。
**缺陷族**：族 1（一次性进程，无孤儿资源）；族 2（用法错先于开账本、fail-closed 零落账，既有）；族 4（三形态优先级与互斥反例，既有）；
族 5（不可自报他人身份：身份只能来自 `ledgerActor` 或成对 flag；`--agent` 是自报启动身份，语义即如此）。
**上下文预算/类型标注**：文件集 2 个；logic 型机内闭环。

---

## 7. T4 —— web 控制台身份内容面 + TS 孪生金样本（d_web）

**契约引用**：§3.1（`IdentityResp` 三键恒出）、§3.4（`GET /api/identity`）、§3.5（TS 镜像 `RoomMessage.device?`）；
§5 D 组 TS 侧、§8.5/§8.6；U1/U2/U3/U7；breakdown P-4/P-5。**依赖 T1 已落**（P-5 硬顺序；vitest 可先绿，合并/端到端验收在 T1 后）。

**意图与为什么**：控制台消费身份读缝与端戳——落款显示人名+端名（不再是 `web:<host>`）、新建会话表单删 owner 手输
（服务端按解析人名落）、未配名给可行动提示；`isSelfActor` 判据按 P-4；补齐 `IdentityResp`/`device` 的 TS 强类型与孪生金样本。

**Consumes**（Go 侧契约，T1 后生效）：
```go
// GET /api/identity → 200 proto.IdentityResp{member,device,configured}（三键恒出）
// POST /api/sessions {title} → owner 缺省为解析人名
// RoomMessage.device（omitempty，只落款）
```
**Produces**（TS，签名冻结）：
```ts
export interface IdentityResp { member: string; device: string; configured: boolean }
export const fetchIdentity: () => Promise<IdentityResp>
export const createSession: (title: string) => Promise<Session>
export function isSelfActor(actor: string, selfMember: string | null): boolean
export function signatureText(actor: string, device?: string): string
```

### 7.1 先写失败测试

**（a）`web/src/api/rooms.test.ts`**——import 增 `IdentityResp` 类型与 `fetchIdentity`；追加：
```ts
describe('身份读缝孪生样本（B358.9）', () => {
  it('identity-configured 三键恒出且与 Go 金样本逐键一致', () => {
    const id = cases.find((c) => c.case === 'identity-configured')!.identity as IdentityResp
    expect(Object.keys(id).sort()).toEqual(['configured', 'device', 'member'])
    expect(id.member).toBe('user:sycm')
    expect(id.device).toBe('mbp / Safari')
    expect(id.configured).toBe(true)
  })
  it('identity-unconfigured：值空但键仍在（缺失≠零值）', () => {
    const id = cases.find((c) => c.case === 'identity-unconfigured')!.identity as IdentityResp
    expect(Object.keys(id).sort()).toEqual(['configured', 'device', 'member'])
    expect(id.configured).toBe(false)
  })
  it('user-stamped：device 键在线；user-minimal 缺 device 键（omitempty）', () => {
    const stamped = cases.find((c) => c.case === 'user-stamped')!.message!
    expect(stamped.device).toBe('mbp / Safari')
    const raw = cases.find((c) => c.case === 'user-minimal')!.message! as unknown as Record<string, unknown>
    for (const banned of ['refs', 'mentions', 'decision_id', 'by_system', 'device']) {
      expect(raw, `可选键 ${banned} 不得出现`).not.toHaveProperty(banned)
    }
  })
})
```
（`cases` 的类型断言需扩 `identity?: IdentityResp`。）

**（b）`web/src/api/rooms.fetch.test.ts`**——import 增 `fetchIdentity`；替换 createSession 用例并新增：
```ts
it('createSession POST {title}——不再带 owner（服务端按解析人名缺省）', async () => {
  const fetchMock = vi.fn().mockResolvedValue(jsonResp({ id: 'session:1', title: 'T', owner: 'user:sycm', archived: false, created_at: '', updated_at: '' }))
  vi.stubGlobal('fetch', fetchMock)
  await createSession('需求对齐')
  const [url, init] = fetchMock.mock.calls[0] as [string, RequestInit]
  expect(url).toBe('/api/sessions')
  expect(init.method).toBe('POST')
  expect(JSON.parse(init.body as string)).toEqual({ title: '需求对齐' })
})

it('fetchIdentity GET /api/identity 三键解码', async () => {
  const fetchMock = vi.fn().mockResolvedValue(jsonResp({ member: 'user:sycm', device: 'mbp / Safari', configured: true }))
  vi.stubGlobal('fetch', fetchMock)
  const id = await fetchIdentity()
  expect(fetchMock.mock.calls[0][0]).toBe('/api/identity')
  expect(id).toEqual({ member: 'user:sycm', device: 'mbp / Safari', configured: true })
})
```

**（c）`web/src/app/rooms/sessionModel.test.ts`**——import 增 `isSelfActor`/`signatureText`；追加：
```ts
describe('自方判定与落款（B358.9 P-4）', () => {
  it('isSelfActor 与人名精确等值：他人 user:/agent:/web: 都不给自方', () => {
    expect(isSelfActor('user:sycm', 'user:sycm')).toBe(true)
    expect(isSelfActor('user:other', 'user:sycm')).toBe(false)
    expect(isSelfActor('agent:opencode', 'user:sycm')).toBe(false)
    expect(isSelfActor('web:127.0.0.1', 'user:sycm')).toBe(false)
  })
  it('identity 拉取失败（null）回落 user: 前缀；web: 永不判自方（反例）', () => {
    expect(isSelfActor('user:sycm', null)).toBe(true)
    expect(isSelfActor('web:127.0.0.1', null)).toBe(false)
  })
  it('signatureText 有端戳出人名·端名，缺端戳只出人名', () => {
    expect(signatureText('user:sycm', 'mbp / Safari')).toBe('user:sycm · mbp / Safari')
    expect(signatureText('user:sycm', undefined)).toBe('user:sycm')
  })
})
```
> 反例（P-4 温床 3）：旧实现 `actor.startsWith('web:')` 下 `isSelfActor('web:127.0.0.1', ...)` 无法接收第二参而编译红；
> 新实现下 `web:127.0.0.1` 必须 false——改而不改测试数据也能红。

**（d）`web/src/app/rooms/NewSessionDialog.test.tsx`**——整体重写：表单只有标题、`onCreate(title)` 不带 owner、
`configured=false` 渲染含 `console_user` 的可行动提示、`configured=false` 时创建禁用。
```tsx
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { NewSessionDialog } from './NewSessionDialog'

beforeEach(() => window.localStorage.clear())

const open = (over: { configured?: boolean } = {}) =>
  render(<NewSessionDialog open busy={false} error="" configured={over.configured ?? true}
    onCancel={() => {}} onCreate={() => {}} />)

describe('NewSessionDialog（B358.9：仅标题，owner 服务端缺省）', () => {
  it('表单只有标题：无群主输入框（B358.9 反例）', () => {
    open()
    expect(screen.getByRole('textbox', { name: '会话标题' })).toBeInTheDocument()
    expect(screen.queryByRole('combobox', { name: '群主身份' })).toBeNull()
    expect(screen.queryByLabelText('群主身份')).toBeNull()
  })
  it('提交只带标题：onCreate(title)', async () => {
    const onCreate = vi.fn()
    const user = userEvent.setup()
    render(<NewSessionDialog open busy={false} error="" configured onCancel={() => {}} onCreate={onCreate} />)
    await user.type(screen.getByRole('textbox', { name: '会话标题' }), '新场')
    await user.click(screen.getByRole('button', { name: '创建' }))
    expect(onCreate).toHaveBeenCalledWith('新场')
  })
  it('未配名：渲染含 console_user 的可行动提示且创建禁用', () => {
    open({ configured: false })
    expect(screen.getByRole('alert')).toHaveTextContent('console_user')
    expect(screen.getByRole('button', { name: '创建' })).toBeDisabled()
  })
  it('空标题禁用创建', () => {
    open()
    expect(screen.getByRole('button', { name: '创建' })).toBeDisabled()
  })
})
```

**（e）`web/src/app/rooms/SessionChat.test.tsx`**——mock 增 `fetchIdentity`；追加落款测试：
```tsx
// vi.mock('../../api/rooms', ...) 的返回对象加：
//   fetchIdentity: vi.fn(),
// beforeEach 里：vi.mocked(fetchIdentity).mockResolvedValue({ member: 'user:sycm', device: '', configured: true })

it('落款渲染人名+端名；device 缺失只出人名（B358.9 决定 1）', async () => {
  render(<SessionChat sessionId="session:1" summary={summary()}
    events={[
      event(51, '甲', { actor: 'user:sycm', payload: { room: 'session:1', kind: 'user', body: '甲', device: 'mbp / Safari' } }),
      event(52, '乙', { actor: 'user:sycm' }),
    ]}
    historyError="" onSent={() => {}} />)
  expect(await screen.findByText(/user:sycm · mbp \/ Safari/)).toBeInTheDocument()
  expect(screen.getByTestId('msg-52')).toHaveTextContent('user:sycm · #52')
})
it('自方判定取服务端人名：本人消息靠右（items-end）', async () => {
  vi.mocked(fetchIdentity).mockResolvedValue({ member: 'user:sycm', device: '', configured: true })
  render(<SessionChat sessionId="session:1" summary={summary()}
    events={[event(53, '我', { actor: 'user:sycm' })]} historyError="" onSent={() => {}} />)
  expect(await screen.findByTestId('msg-53')).toHaveClass('items-end')
})
```

**（f）`web/src/app/shell/Shell.test.tsx`**——mock 的 `../../api/rooms` 增 `fetchIdentity: vi.fn().mockResolvedValue({ member: 'user:sycm', device: '', configured: true })`；
重写「新建会话」两条用例：不再有群主输入框；`createSession` 只收标题；移除 owner 记忆与自动补员断言（改为
「建会话后不调用 addSessionMember」）。

**（g）`web/src/api/testdata/RoomsFixture.json`** 追加三个 case（在数组末尾）：
```json
{
  "case": "identity-configured",
  "identity": { "member": "user:sycm", "device": "mbp / Safari", "configured": true }
},
{
  "case": "identity-unconfigured",
  "identity": { "member": "", "device": "", "configured": false }
},
{
  "case": "user-stamped",
  "message": { "room": "session:1", "kind": "user", "body": "带端戳", "device": "mbp / Safari" }
}
```
（与 Go 金样本 `internal/proto/sessions_fixture_test.go#TestRoomMessageDeviceStampGolden`、`#TestIdentityRespWireShape` 逐键一致；
**同一 JSON 为唯一来源，TS 不手抄**。）

跑红（T1 已落后写下的 web 测试对 T4 改动仍是红——组件尚未改）：
```bash
cd web && npx vitest run src/api/rooms.test.ts src/api/rooms.fetch.test.ts src/app/rooms/sessionModel.test.ts src/app/rooms/NewSessionDialog.test.tsx src/app/rooms/SessionChat.test.tsx
```

### 7.2 最小实现

**（1）`web/src/api/rooms.ts`**
```ts
// IdentityResp 是 GET /api/identity 的响应（B358.9 契约 §3.1）。三键恒出（无
// omitempty）：member 空可能是 console_user 未配置，也可能是后端太老没发键。
export interface IdentityResp {
  member: string
  device: string
  configured: boolean
}

// fetchIdentity 控制台身份读缝（GET /api/identity）：本文请求的人名+端名+是否已配名。
// 落款显示、isSelfActor 判据、缺名可行动提示的数据源。
export const fetchIdentity = (): Promise<IdentityResp> => request<IdentityResp>('/api/identity')
```
替换 `createSession`：
```ts
// createSession 建会话（POST /api/sessions {title}）：owner 由服务端按解析人名
// 缺省（B358.9 契约 §3.8；决定 6 owner=创建者），前端不传 owner。
export const createSession = (title: string): Promise<Session> =>
  postJSON<Session>('/api/sessions', { title })
```

**（2）`web/src/app/rooms/sessionModel.ts`** 替换 `isSelfActor`：
```ts
// isSelfActor 自方消息判定：与本次请求的控制台人名精确等值（B358.9 P-4 甲）。
// selfMember 取服务端 GET /api/identity 的 member；拉取失败（null）回落
// user: 前缀——但绝不把 web:<host> 旧脸判为自方（本卡要退役的机器位）。
export function isSelfActor(actor: string, selfMember: string | null): boolean {
  if (selfMember !== null) return actor === selfMember
  return actor.startsWith('user:')
}

// signatureText 落款：人名 + 端名（有端戳时）；端戳缺失只出人名（决定 1）。
export function signatureText(actor: string, device?: string): string {
  return device ? `${actor} · ${device}` : actor
}
```

**（3）`web/src/app/rooms/SessionChat.tsx`**
- import 改：`import { addSessionMember, fetchIdentity, sendRoomMessage } from '../../api/rooms'`；
  `import type { IdentityResp, RoomHistoryItem, SessionSummary } from '../../api/rooms'`；`import { applyMention, isSelfActor, memberKindLabel, mentionCandidates, segmentBody, signatureText } from './sessionModel'`。
- `MessageRow` props 增 `selfMember: string | null`；`const self = isSelfActor(event.actor, selfMember)`；
  落款行 `<div ...>{signatureText(event.actor, payload.device)} · #{event.seq}</div>`（`payload` 类型断言增 `device?: string`）。
- `SessionChat` 增身份态（一次拉取，失败回落 null 并 logRoom）：
```tsx
  const [identity, setIdentity] = useState<IdentityResp | null>(null)
  useEffect(() => {
    let cancelled = false
    fetchIdentity()
      .then((got) => { if (!cancelled) setIdentity(got) })
      .catch((error: unknown) => {
        if (!cancelled) setIdentity(null)
        logRoom('warn', 'session_identity_fetch_failed', { error: errorMessage(error) })
      })
    return () => { cancelled = true }
  }, [])
  const selfMember = identity?.member ?? null
```
- 渲染处传 `selfMember={selfMember}`（`map` 中的 `MessageRow`）。

**（4）`web/src/app/rooms/NewSessionDialog.tsx`**：替换为标题-only 版本（见 §7.1(d) 测试对应的实现）：
```tsx
// NewSessionDialog —— 新建会话对话框（B358.9：只有标题；owner 由服务端按解析
// 人名缺省，前端不传）。未配 console_user 时给可行动提示并禁用创建。
import { useEffect, useRef, useState } from 'react'

export function NewSessionDialog({ open, busy, error, configured, onCancel, onCreate }: {
  open: boolean; busy: boolean; error: string; configured: boolean
  onCancel: () => void
  onCreate: (title: string) => void
}) {
  const [title, setTitle] = useState('')
  const wasOpen = useRef(false)
  useEffect(() => {
    if (open && !wasOpen.current) setTitle('')
    wasOpen.current = open
  }, [open])
  if (!open) return null
  return (
    <div role="dialog" aria-label="新建会话" className="fixed inset-0 z-50 flex items-center justify-center bg-black/40">
      <div className="w-80 rounded-xl border bg-background p-4 shadow-xl">
        <h3 className="text-sm font-semibold">新建会话</h3>
        <label className="mt-3 block text-xs text-muted-foreground" htmlFor="new-session-title">标题</label>
        <input id="new-session-title" aria-label="会话标题" value={title} onChange={(event) => setTitle(event.target.value)}
          className="mt-1 w-full rounded-md border px-2 py-1 text-sm" />
        {!configured && (
          <p role="alert" className="mt-2 text-xs text-amber-700">
            本机未配置控制台人名 console_user，无法确定身份；请在 agentd 配置文件中设置 console_user 后重试。
          </p>
        )}
        {error !== '' && <p role="alert" className="mt-2 text-xs text-destructive">{error}</p>}
        <div className="mt-4 flex justify-end gap-2">
          <button type="button" onClick={onCancel} className="rounded-md border px-2.5 py-1 text-xs">取消</button>
          <button type="button" disabled={title.trim() === '' || !configured || busy}
            onClick={() => onCreate(title.trim())}
            className="rounded-md bg-slate-900 px-2.5 py-1 text-xs text-white disabled:opacity-50">创建</button>
        </div>
      </div>
    </div>
  )
}
```

**（5）`web/src/app/shell/Shell.tsx`**
- import：删 `saveLastSessionOwner`；`import { fetchSessions, addSessionMember, createSession, fetchIdentity } from '../../api/rooms'`。
- 删 `sessionMemberIdentities` memo（`:187-197`）。
- 增身份态（一处拉取；缺名提示数据源）。**拉取失败不得误判「未配名」**：只有服务端明确回 `configured=false` 才提示/禁用；
  失败（网络/401）保持放行，让创建请求把服务端 403 可行动原文显示在对话框（fail-visible 而非误报）：
```tsx
  const [consoleConfigured, setConsoleConfigured] = useState(true)
  useEffect(() => {
    let cancelled = false
    fetchIdentity()
      .then((id) => { if (!cancelled) setConsoleConfigured(id.configured) })
      .catch((error: unknown) => {
        console.warn('shell.identity_fetch_failed', { error: errorMessage(error) })
      })
    return () => { cancelled = true }
  }, [])
```
- `confirmCreateSession` 重写（去 owner/记忆/自助补员；owner 缺省=控制台人名，补员冗余）：
```tsx
  // confirmCreateSession 建会话（B358.9）：owner 由服务端按解析人名缺省，前端只交
  // 标题；建后刷新会话流。403「以当前身份加入会话」一键仍在 SessionChat（U7 老屋）。
  const confirmCreateSession = async (title: string) => {
    setCreateBusy(true)
    setCreateError('')
    console.debug('shell.session.create_started', { title })
    try {
      const session = await createSession(title)
      setCreateOpen(false)
      sessionsState.refresh()
      console.debug('shell.session.created', { sessionId: session.id, title })
    } catch (error: unknown) {
      setCreateError(errorMessage(error))
      console.warn('shell.session.create_failed', { title, error: errorMessage(error) })
    } finally {
      setCreateBusy(false)
    }
  }
```
- 渲染处（`:1049-1050`）改为：
```tsx
      <NewSessionDialog open={createOpen} busy={createBusy} error={createError} configured={consoleConfigured}
        onCancel={() => setCreateOpen(false)} onCreate={(title) => void confirmCreateSession(title)} />
```
**（6）删除 `web/src/app/rooms/sessionOwnerPrefs.ts`**（S4 后无消费者）。

### 7.3 日志与注释

- **日志**：`fetchIdentity` 失败 `logRoom('warn','session_identity_fetch_failed')`（不静默）；建会话继续既有
  `console.debug` 两处（started/created）。
- **注释**：`IdentityResp`/`fetchIdentity`/`createSession`/`isSelfActor`/`signatureText` 各写职责与依据；`SessionChat` 身份态写
  「一次拉取、失败回落、绝不把 web: 判自方」。

### 7.4 验收

| 判据 | 断言 |
| --- | --- |
| `IdentityResp` 三键恒出与 Go 金样本逐键一致（§3.1、条 21） | `rooms.test.ts` 身份读缝组 |
| `RoomMessage.device` 非零在线、零值缺键（§3.5、条 23/24 TS 侧） | `rooms.test.ts` user-stamped/user-minimal |
| `fetchIdentity` 端点与解码 | `rooms.fetch.test.ts` |
| `createSession` 只发 `{title}` | `rooms.fetch.test.ts` |
| `isSelfActor` 精确等值 + `web:` 反例（P-4） | `sessionModel.test.ts` |
| 落款人名+端名 / 缺端戳只人名（U1） | `sessionModel.test.ts` + `SessionChat.test.tsx` |
| 表单只有标题、未配名可行动提示（U3） | `NewSessionDialog.test.tsx` |
| Shell 建会话只交标题、无 owner 记忆/补员 | `Shell.test.tsx` |
| web 全量类型 + 测试 | `cd web && npx tsc -b` && `npx vitest run` |

**测试范围声明**：只跑 `cd web && npx tsc -b` 与 `npx vitest run`（含全量 131 文件；web 全量本身即本 task 的机内闭环，不跨包）。
**缺陷族**：族 2（fetch 失败渲染可行动错误态，`configured=false` 是提示不是崩溃；fixture 缺 `device` 键 ≠ `device:""`）；
族 3（不引入新浏览器 API；真实 webview 归真机）；族 4（反例见 `isSelfActor`/表单；孪生同源 JSON）；族 5（前端不自报身份，请求不带
identity——与 gateway 纪律镜像）；追加（跨语言整链一条穿 fixture JSON；`SessionMemberKind` 三值联合与 Go 词表逐值一致）。
**上下文预算/类型标注**：文件集 13 个，有界；logic 型 vitest/tsc 机内闭环，真实渲染归真机（§10.1）。

---

## 8. 缺陷族对抗审查（逐族结论，入各 task 验收栏）

- **族 1 生命周期/状态机中断**：T1/T4 请求内解析与纯渲染；T2 纯投影；T3 一次性进程。无新增后台 goroutine/临时目录/锁；
  端戳随消息载荷走，不建注册表（契约 §6 拍板）。游标旧键孤儿是缓存非权威（无回收责任）。
- **族 2 静默失败/误导报错**：最高危=未配名若漏判 `Configured` 会以 `ErrNotWriter` 误报「非成员」。堵法：`requireConsoleIdentity`
  统一门 + 逐写面 probe（T1 (b)），文案含 `console_user`；T2 读卡失败向上传播；T4 fetch 失败渲染错误态。
- **族 3 跨平台假设**：无新路径/进程组/webview API。端戳入库前已净化，读侧不再加工。真实 Wails/Chromium 渲染归真机。
- **族 4 假红/假绿**：温床 1=留 `web:` 回落（既有测试仍绿）→ T1 源码守卫 + 未配名反例；温床 2=kind 无 `agent:` 断言→T2 (a)；
  温床 3=`isSelfActor` 测试数据仍用 `web:`→T4 (c) 反例；温床 4=device 只测 fixture 不经 handler→T1 (a) + T4 同源 fixture。
- **族 5 门禁绕过**：会话/房间面全经 `withRooms`、actor 服务端注入；fail-closed 是新增的门，共享 `requireConsoleIdentity` 一道门，
  T1 逐端点枚举；无 TOCTOU（请求内一次解析）。
- **追加设问一 序列化边界**：见 §9。
- **追加设问二 枚举新值过白名单**：无新枚举。逐处核：`sessionMembers` kind 分支（废弃 `cli:`、启用 `user:`/`agent:`）；
  前端 `SessionMemberKind` 三值联合不变；`isSelfActor` 的 `web:` 判据退役；`validSessionOwner`/`validSessionMemberIdentity`
  收敛到 proto（接受 `user:`/`agent:`）。逐处登记，无通道分裂。
- **追加设问三 承重安全属性**：「未配名不可写」T1 (b) 能红；「非成员不能写」既有 `TestSessionMembershipEnforcement`；
  「已读按人合并」T1 (d)；「端戳不参与判定」§9 源码守卫 + 既有 `ResolveDelivery` 守卫。
- **追加设问四 webview/平台**：T4 真机（§10.1）；无新浏览器 API。

---

## 9. 序列化边界与穿边界回归

**新增/改动字段逐处列出：**

| 字段/链路 | 手写序列化/投影点 | 断言 |
| --- | --- | --- |
| `RoomMessage.Device` | Go struct（`rooms.go:37-42`，omitempty）→ `json.Marshal`（ledger `RawMessage` 落库）→ `History` 读回 → HTTP body → TS `RoomMessage.device?` → `SessionChat` 落款 | T1 (a) 穿真实 handler→账本 payload；Go 金样本 `TestRoomMessageDeviceStampGolden`；T4 user-stamped fixture + `rooms.test.ts` |
| `IdentityResp` 三键 | Go struct（无 omitempty）→ `writeJSON` → HTTP body → TS `IdentityResp` → `fetchIdentity` | Go 金样本 `TestIdentityRespWireShape`；T1 `handleIdentity` 既有测试；T4 fixture + `rooms.test.ts`/`rooms.fetch.test.ts` |
| 游标 member 键 | `member` string 进 `room-cursors.json`（`map[string]map[string]int64` 手写 JSON） | T2 `TestCursorKeyedByMemberIdentity`；既有 `TestCursorFilePersistsAcrossInstances` |
| 会话 create owner | `POST /api/sessions` body（Go handler 解析）→ `Session.Owner` 落库 → `ListSessions` 投影 → TS `SessionSummary.owner` | T1 (c) 穿真实 HTTP；T4 `createSession` 只发 title |
| `sessionMembers.kind` | Go 投影 → `SessionMember.Kind` → TS `SessionMemberKind` | T2 (a)(b)；既有 `sessions_fixture_test.go` 键集 |

**必须有一条穿过真实序列化边界的回归**：T1 (a)（handler→账本 payload，断言 `actor` 与 `device`）；T4 跨语言整链穿同一
`RoomsFixture.json`（identity-* 与 user-stamped 与 Go 金样本逐键一致）。
**可空类型区分「字段缺失」与「值为零」**：`device` 零值省键 vs 非零在线（Go/T4 双侧）；`IdentityResp` 恒出键（缺失≠空串）。

---

## 10. 未验证，需真机（归协调者执行）

1. **真实桌面壳/浏览器渲染**（T4）：缺名横幅、新建表单只有标题、落款人名+端名、`device` 缺失回退仅人名——jsdom 只证组件契约。
2. **真实多机同名**（T1/U2）：两台机器配同一 `console_user`、不同设备名，各自控制台/CLI 的发言与已读合并——机内只证同进程双登录会话。
3. **真实端戳来源**（T1）：`handoff console --device`／桌面壳登记 + 浏览器名经 `sanitizeDeviceName` 后的实际值——机内用固定 DeviceName。
4. **外部 harness 真机链**（T3/U4/U5）：真实 `session create --owner agent:` + `session wait <member>` + `session send --agent`。
5. **迁移后真实 datadir**（T1/T2）：旧 `web:` 游标孤儿在真实 `room-cursors.json` 上的无脏读——机内可近似。
6. **spec Out of Scope 登记**（不派卡）：老 `web:` 孤儿成员行展示治理、会话级主 agent 拉起+@ 唤醒绑定、拉权对称（新拉人 op）。

---

## 11. 跨 task 一致性自审（供协调者独立上下文跨卡审计）

**签名字符串逐字比对**：
- T1 Produces `requireConsoleIdentity → (consoleIdentity, bool)`；T1 内部 Consumers 是 `roomsapi.go`/`sessionsapi.go` 同包函数——一致。
- T1 删除 `roomUserActor`：全仓生产调用点仅 `roomsapi.go`/`sessionsapi.go`（grep 11+9 处），均在 T1 文件集内；测试文件无直调——**无悬空调用**。
- T1 依赖 contract 冻结签名 `resolveConsoleIdentity`/`MemberIdentity`/`ValidateMemberIdentity`/`IdentityResp`/`RoomMessage.Device`——已在 Ticket 0 落盘，逐字一致。
- T2 Consumers `ParseMemberIdentity`/`SessionMember*` 常量与 T1 无交集；T2 不 import agentd（域方向）。
- T3 Consumers `proto.ValidateMemberIdentity`/`MemberIdentity`，与 T1 收敛点同符号。
- T4 Consumes 的 TS 形状与 T1 的 wire（`{title}`、`IdentityResp` 三键、`device`）逐字对齐。

**spec 故事逐条归属**：U1→T1(行为)+T4(渲染)；U2→T1(d)+T2(d)；U3→T1(b)+T4(表单提示)；U4→T3+（owner/成员注入 T1）；
U5→T3；U6→T2(e)；U7→T1(成员端点)+T4(SessionChat 403 一键保留)。**无落空故事。**

**冻结物比对**：本计划引用 contract §3.1–§3.8/§5 A–J 条目与原文核对一致；三处边界澄清 C-1/C-2/C-3 遵守（无 `handleSessionSend`、
卡面 `web:` 不换脸、无 `identity{}` 测试构造）。**跨卡审计本身由协调者在扇出前以独立上下文执行**（本计划不自批）。

---

## 12. 占位符扫描（自我声明）

- 无 TBD /「加适当的错误处理」/「同 Task N」/「同前」式占位；每个实现步骤给出完整代码块。
- **正当出口声明**：T4 的组件测试复用既有 vitest + `@testing-library/react` harness 与 `vi.mock('../../api/rooms')` 形态，
  断言逐条列全（每条可判 pass/fail）并指认照抄文件（`SessionChat.test.tsx`/`Shell.test.tsx` 既有 mock 形态）；Go 侧全部给完整测试代码。
- 内部锁：仅 T2 `TestSessionWritersExcludesEmptySeat`，理由已在 §3 声明（唯一合法形状：从声明缝构造不出该断言）。
- 无未声明的条件退路（「若意外先绿」类）。

---

## 13. 图覆盖债与收尾

- 未命中符号（`file#Symbol` 源码锚）：`sessionWriters`、`sendToSession`、`validSessionOwner`、`validSessionMemberIdentity`、
  `handleSessionCreate`、`handleSessionsList`、`handleSessionMemberAdd`、`handleSessionJoinCard`、`handleSessionArchive`、
  `handleSessionLeaveCard`、`cmd.sessionSendActor`。
- `codegraph validate --view cards-B358.9-charter-4`：唯一 issue `[cards-B374-charter] … k_collab_model …`（既有）。
- `codegraph check --view cards-B358.9-charter-4`：`fails` 仅 `dead-contract d_transport→d_ledger`（B370 遗留）。
- **本卡不新增领域/容器**（`best.json` 不变）；契约 §4 的跨域边（`d_gateway→d_protocol`、`d_cli→d_protocol`）已随 target.json 注记。
- 收尾：`codegraph resolve --repo . --view cards-B358.9-charter-4 --doc docs/superpowers/plans/b358.9-plan.md` 校锚。

## 14. 台账

`docs/superpowers/ledgers/2026-09-18-b358.9-plan-ledger.md`（本节点事实、命令原文、图覆盖债、判断与设计决议）。
