# B233.19 plan：项目登记/事件镜像/预览仓主迁出 agentd

前置事实（2026-09-13，基点 59dfb16c5 + B233.18 ae1687279）：

- projectfanout.go(111)/projectjoin.go(59)/projectresolve.go(99)：非测试 import 仅 proto。
  k_agentd_projectIndex（best.json 归 d_workspace）在 projectjoin.go。
- projectadmin.go(444)：3 个 `handleProject*` handler，import projectid/proto/store/workspace。
- projecttree.go(103)：1 个 handler，import proto/workspace。
- mirror.go(431)：import proto/store/targetclient；Mirror 结构体被 agentd 消费（WS 面）。
- preview_owner.go(554)：import proto/store/workspace；PreviewOwner/PreviewHub 由 cmd
  构造注入（cmd/agentd.go:183-189 SetPreviewOwner/SetPreviewMirror/SetPreviewOpener）。
- internal/workspace 非测试 import 全景：proto、gitproc、prochost（不 import store/targetclient）。
- 回收编排已在 internal/orchestration/reclaim.go；bundle 已在 internal/workspace/bundle.go。

步骤：

1. 直迁三件（零新边）：`git mv` projectfanout/projectjoin/projectresolve 的实现与测试进
   internal/workspace，包名与导出性按 workspace 惯例调整；agentd 引用点改指 workspace。
2. projectadmin/projecttree：handler 体留 agentd；把 handler 里调用的登记/索引/树构造逻辑
   提为 workspace 函数（store 触点经 workspace 本地接口，如 `ProjectStore`，由 agentd 或
   cmd 提供适配器）；projectadmin 现有 workspace import 消化。
3. mirror.go：Mirror 循环迁 workspace；定义本地接口（建议 `RemoteTaskSource`=
   ListTasks/StreamEvents 能力、`MirrorStore`=mirror_events/cursor 持久化），适配器留
   agentd（包 targetclient/store 细节）；Server 持注入实例，WS 推送面不动。
4. preview_owner.go：owner-truth 逻辑迁 workspace（本地接口 `PreviewStore`），适配器留
   agentd；Proxy/Static/Launcher/OpenService 的 HTTP/OS 面留 agentd。不可行则整卡降级
   （见 spec §范围 4），note 落账。
5. 测试随迁；agentd 集成测试（bundle_e2e/preview_regression/mirror_test 等）保绿，断言不改。
6. 验证与证据：
   - `go build ./...`
   - `go test -p 1 ./internal/agentd/ ./internal/workspace/ ./cmd/`（存量红除外）
   - `ls internal/agentd/ | grep -E "projectfanout|projectjoin|projectresolve|mirror|preview_owner"`
   - `grep -rn "handoff/internal/store\|handoff/internal/targetclient\|handoff/internal/agentd" internal/workspace/*.go | grep -v _test`
7. 提交：`refactor(B233.19): agentd 前台包收尾——项目登记/事件镜像/预览仓主迁出`。

执行中偏差如实记入收尾段。
