# B234 plan 节点台账

| 时间 | 类型 | 事实 |
|---|---|---|
| 2026-08-28 | 基线/分支 | `git status --short --branch` 原始输出：`## cards/B234-charter`；工作区干净。`git log -5 --oneline --decorate` 原始输出首行：`a36ffb71 (HEAD -> cards/B234-charter, origin/fix/b234-macos-test-ports) docs: B234 spec r1（并入 B193）`。当前基线就是 `origin/fix/b234-macos-test-ports`，未切分支。 |
| 2026-08-28 | 规范输入 | 已读取 `docs/superpowers/specs/b234.md` 全文（177 行）、`docs/superpowers/specs/b234-ledger.md` 全文（18 行）与 `docs/superpowers/reviews/b234-spec-review.md` 全文（239 行）。spec 状态为已批准 r1、定级 L2，法定产出为 `docs/superpowers/plans/b234-plan.md`。 |
| 2026-08-28 | 图摘要 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . summary` 原始输出：`3636 节点 / 4735 边 / 20 领域`；仓内有 `codegraph/best.json`，因此后续使用 best 领域 id。 |
| 2026-08-28 | 图查询 | 已运行 `context d_gateway` 与 `context d_sessions`；输出含 `actual`、`truncated`/`fociTruncated` 机械字段，d_sessions 实际容器均在 `d_sessions`，无 `misplaced`。查询期间未把截断结果当完整调用面。 |
| 2026-08-28 | 图查询 | `go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . sym n_ptyhost_Host_Close` 原始命中：`internal/ptyhost/client.go:284`，签名 `func (h *Host) Close(id string) error`；`sym n_engine_Engine_Close` 命中 `internal/ptyhost/engine/engine.go:299`，签名 `func (h *Engine) Close(id string) error`；`sym n_hostproc_Run` 命中 `internal/ptyhost/hostproc/hostproc.go:93`，签名 `func Run(specPath string) (runErr error)`。 |
| 2026-08-28 | 图覆盖债 | `who-calls n_ptyhost_Host_Close` 原始输出含 `unscannedEntries: 6` 与 warning `基线仍有 6 个未扫描入口：查询结果为空不等于没有调用方`；结果混入 `Host.Attach`/`Host.Write` 等 `conn.Close()` 假边。测试夹具不在图中，`hostproc.Run` defer 内的 `eng.Close` 仍须源码核对。 |
| 2026-08-28 | 基线测试 | `go test ./internal/ptyhost ./internal/ptyhost/engine ./internal/ptyhost/hostproc -run 'Test(ClientCloseRemovesSession|CloseAllKillsLiveSessions|CloseRemovesSession|KillEndsProcessAndCleansDir)' -count=1` 原始输出：`ok github.com/Xsxdot/handoff/internal/ptyhost 0.025s`、`ok github.com/Xsxdot/handoff/internal/ptyhost/engine 0.006s`、`ok github.com/Xsxdot/handoff/internal/ptyhost/hostproc 0.008s`；退出码 0。 |
| 2026-08-28 | 基线测试 | `go test ./internal/agentd -run 'TestPtyWS(EchoRoundTrip|Resize|ResumeSince)|TestPtySessionListAndDelete' -count=1` 原始输出：`ok github.com/Xsxdot/handoff/internal/agentd 0.576s`；退出码 0。 |
| 2026-08-28 | 基线测试 | `go test ./internal/agentd -count=1` 实际退出码 0，原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd 174.681s`。当前 Linux 基线没有复现 Darwin `EADDRNOTAVAIL`，不能据此替代 macOS 合 main 门。 |
| 2026-08-28 | 现状核对 | `rg -n 'httptest\\.New(Server|UnstartedServer)' internal/agentd --glob '*_test.go' | wc -l` 原始输出 `21`；调用分布覆盖 13 个测试文件，并含 `ws_regression_round2_test.go` 的 `NewUnstartedServer`。 |
| 2026-08-28 | 现状核对 | `rg -n 'client\\.New\\(' internal/agentd --glob '*_test.go'` 实际代码调用为 `integration_test.go:92`、`:425`、`:714` 三处；其余命中均为注释/扫描守卫。`rg -n 't\\.Parallel\\(' internal/agentd --glob '*_test.go'` 只命中 `ws_regression_round2_test.go:427` 的注释。 |
| 2026-08-28 | 依赖核对 | `go version && go env GOROOT GOPATH GOMODCACHE` 原始输出：`go version go1.26.1 linux/amd64`、`/usr/local/go`、`/root/go`、`/root/go/pkg/mod`。Go `httptest` 源码 `/usr/local/go/src/net/http/httptest/server.go:104-109` 显示 `NewServer` 委托 `NewUnstartedServer` 后 `Start`；`:118-122` 显示未启动服务先持有 `net.Listen` 的 listener；`:131-146` 显示 `Start` 为 `Server.Client()` 单独建立 `*http.Transport` 与 `DialContext`；`:327-334` 说明 `Client()` 返回该客户端且 `Server.Close` 关闭其 idle connections。 |
| 2026-08-28 | 依赖核对 | `go list -m -f '{{.Dir}} {{.Version}}' github.com/coder/websocket` 原始输出：`/root/go/pkg/mod/github.com/coder/websocket@v1.8.15 v1.8.15`；源码 `dial.go:23-27` 声明 `DialOptions.HTTPClient *http.Client`，`dial.go:120-128` 显示 `websocket.Dial` 经 `opts.cloneWithDefaults` 使用该选项。 |
| 2026-08-28 | 源码核对 | `internal/client/client.go:247-268` 实际为 `NewWithWSTiming` 创建独立 `*http.Transport`，`DialContext` 是 `(&net.Dialer{Timeout: dialTimeout}).DialContext`，`Proxy: nil`；`HTTPClient()` 在 `:294-306` 返回共享 `*http.Client`，所以测试可在构造后仅替换该测试 Transport 的 DialContext，不改生产默认。 |
| 2026-08-28 | 源码核对 | `internal/ptyhost/client.go:64-67` 的 `clientSession` 目前只有 `meta`；Open 在 `:165-166` 启动唯一 `cmd.Wait` goroutine，成功后 `:194` 调 `remember(meta)` 丢掉通道；Close 在 `:300-306` 读控制帧后把 EOF/超时视为成功并 `forget`；`cleanupDetached` 在 `:622-635` 复用同一 wait 通道。 |
| 2026-08-28 | 源码核对 | `internal/ptyhost/engine/engine.go:72-86` 的 session 没有退出完成通道；`reap` 在 `:195-206` 唯一调用 `waitExitCode`，而 `platform_unix.go:111-127` 的 `waitExitCode` 唯一调用 `cmd.Wait`。当前 `Engine.Close` `:299-326` 删除列表后 SIGTERM，并在 goroutine 中 `time.Sleep(termGrace)` 再异步 `killPty`。 |
| 2026-08-28 | 源码核对 | `internal/ptyhost/hostproc/hostproc.go:125-151` 的 `Run` defer 先 `srv.stopNow()`，再调 `eng.Close`，随后删 socket、释放锁、`sessdir.Remove`；`:237-247` 的 `stopNow` 关闭 listener/连接但不杀 PTY。因此让 `Engine.Close` 等现有 reap 会使 hostproc 在清目录前等待 shell 完成。 |
| 2026-08-28 | 源码核对 | `internal/ptyhost/closeall.go:40-98` 的 `CloseAll` 先 Scan、Adopt，再并发调用 `h.Close`，由 `context.WithTimeout` 施加总 budget；超时分支只记录 Warn 后返回。`cmd/service.go:240-269` 的 `ptyCloseBudget` 为 `2*time.Second`，调用方 `closePtySessionsForStop` 不阻断服务停止。 |
| 2026-08-28 | 源码核对 | `internal/ptyhost/client_test.go:204-246` 的 `startClientHost` 通过独立 `hostproc.Run`、`sessdir.Scan` 后 `Adopt`，现有 `TestClientCloseRemovesSession` `:142-158` 只检查 `Run` 在 1 秒内结束；`survive_test.go:20-79` 的 `TestSurviveAgentdClientRestart` 是重启后的 Adopt 路径，现只等目录消失。 |
| 2026-08-28 | 源码核对 | `internal/agentd/ptyreclaim.go:90-121` 的 `shutdownPtySessions(context.Context)` 并发调用 `s.pty.Close`，固定 `ptyShutdownWait = 2s`，超时只记 Warn；`cmd/service.go:240-244` 的 `ptyCloseBudget = 2s` 也固定，`CloseAll` 超时只记 Warn。计划必须保留两处预算及同步 `Close` 语义。 |
| 2026-08-28 | 源码核对 | `internal/agentd/pty_api.go:110-162,244-263` 的 POST→`Host.Open` 与 DELETE→`Host.Close` 是真实 HTTP 接缝；DELETE 成功仍写 200，`ErrNoSession` 仍写 404，Close 其它错误写 500。 |
| 2026-08-28 | 源码核对 | `internal/agentd/ptyreclaim_test.go:116-176` 的 graceful Trigger 明确不调用 `shutdownPtySessions`；新增回归不能把 Trigger/升级路径误接到 PTY kill。 |
| 2026-08-28 | 依赖核对 | `/usr/local/go/src/net/http/httptest/server.go:104-146,327-334` 实测：`NewServer` 委托 `NewUnstartedServer` 后 `Start`，`Start` 创建独立 `*http.Transport`，`Client` 返回它；因此 helper 必须同时包装服务端 Accept 和 `ts.Client`/DefaultClient/custom client 的拨号。 |
| 2026-08-28 | 依赖核对 | `/usr/local/go/src/net/http/transport.go:893-902` 实测 `Transport.CloseIdleConnections` 只关闭 idle 连接，不中断 in-use 连接；helper 清理只能在 `ts.Close` 后调用，不能把它当请求取消。 |
| 2026-08-28 | 依赖核对 | `/usr/local/go/src/net/tcpsock.go:214-229` 实测 `TCPConn.SetLinger(0)` 丢弃未发送数据并调用底层 `setLinger`；验收要锁 Accept 侧与 Dial 侧均调用，不能只锁 helper 函数被调用。 |
| 2026-08-28 | 依赖核对 | `/root/go/pkg/mod/github.com/coder/websocket@v1.8.15/dial.go:23-27,120-128` 实测 `DialOptions.HTTPClient` 可指定拨号客户端，缺省回落 `http.DefaultClient`；因此 WS 回归应从 `websocket.Dial` 入口走默认 client 的受控 transport。 |
| 2026-08-28 | 基线测试 | `go test ./cmd -run 'TestService' -count=1` 原始输出：`ok github.com/Xsxdot/handoff/cmd 0.009s`；退出码 0。cmd 服务停止接缝的基线判据可执行。 |
| 2026-08-28 | 计划产出 | 已写入法定产物 `docs/superpowers/plans/b234-plan.md`（1670 行）与本台账；计划含 Task A/B/C 的有界文件集、精确接口、完整关键代码块、基线命令、日志/注释步骤、序列化/类型/缺陷族/接缝双向自审。 |
| 2026-08-28 | 计划自审 | `rg -n -i 'TBD|TODO|__BT__|同 Task|适当的错误处理' docs/superpowers/plans/b234-plan.md docs/superpowers/plans/b234-plan-ledger.md` 无输出；`git diff --check` 无输出；未执行实现测试，未把计划预期写成实现 pass。 |
| 2026-08-28 | Task A 基线 | `go test ./internal/agentd -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd 372.516s`；退出码 0；当前运行环境为 Linux，不能替代 macOS 门。 |
| 2026-08-28 | Task A 先红 | 新增 `internal/testhttp/server_test.go` 后运行 `go test ./internal/testhttp -run 'Test(NewServer|RetryDialContext)' -count=1`；退出码 1，原始输出含 `undefined: setLinger`、`undefined: NewServer`、`undefined: RetryDialContext`、`undefined: MaxDialAttempts` 与 `FAIL ... [build failed]`；符号缺失而非拼写错误，进入空壳阶段。 |
| 2026-08-28 | Task A 空壳红 | 加入仅满足编译的 `internal/testhttp/server.go` 空壳后再次运行同一命令；退出码 1，原始断言红为 `SetLinger 调用次数=0` 与 `dial 次数=1，期望上限 4`；测试接缝确认有效，开始最小实现。 |
| 2026-08-28 | Task A helper 绿 | 执行 `gofmt -w internal/testhttp/server.go internal/testhttp/server_test.go && go test ./internal/testhttp -run 'Test(NewServer|RetryDialContext)' -count=1`；退出码 0，原始输出：`ok  github.com/Xsxdot/handoff/internal/testhttp 0.035s`。 |
| 2026-08-28 | Task A 迁移编译红 | 迁移 21 个构造点并运行 `go test ./internal/agentd -run 'TestHTTPFixtureDialRoutesReachRetryLimit|TestNoDirectHttptestServers' -count=1`；首次退出码 1，原始报错：`internal/agentd/diffbase_test.go:11:2: "net/http/httptest" imported and not used`、`internal/agentd/ledgerapi_test.go:10:2: "net/http/httptest" imported and not used`、`FAIL ... [build failed]`；移除两个迁移遗留 import。 |
| 2026-08-28 | Task A 接缝绿 | 清理 import 后运行 `gofmt -w ... && go test ./internal/agentd -run 'TestHTTPFixtureDialRoutesReachRetryLimit|TestNoDirectHttptestServers' -count=1`；退出码 0，原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd 0.107s`；扫描与 DefaultClient/WS/client.New 三路上限测试均通过。 |
| 2026-08-28 | Task A 局部绿 | 并行运行 `go test ./internal/testhttp -count=1` 与计划范围的 agentd 回归 `go test ./internal/agentd -run 'TestHTTPFixtureDialRoutesReachRetryLimit|TestNoDirectHttptestServers|TestPtySessionListAndDelete|TestPtyWS(EchoRoundTrip|Resize|ResumeSince)' -count=1`；两者退出码均 0，原始输出分别为 `ok  github.com/Xsxdot/handoff/internal/testhttp 0.035s`、`ok  github.com/Xsxdot/handoff/internal/agentd 0.264s`。 |
| 2026-08-28 | Task A 编译绿 | `go build ./...` 退出码 0，原始输出为空。 |
| 2026-08-28 | Task A 全包绿 | `go test ./internal/agentd -count=1` 退出码 0，原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd 136.412s`；当前 Linux 结果仍不替代 macOS 验收门。 |
| 2026-08-28 | Task A 变异锚点 | `rg -n -F 'attempt+1 == MaxDialAttempts' internal/testhttp/server.go` 唯一命中 `server.go:136`；允许对该唯一比较做可编译边界变异。 |
| 2026-08-28 | Task A 变异结果 | 将唯一比较改为 `attempt+1 == MaxDialAttempts-1`；先运行 `go build ./...` 退出 0、无输出，确认变异可编译；再运行相关测试，原始断言红为 `dial 次数=3，期望上限 4`；随后运行 `go test ./internal/testhttp -count=1` 退出 1，同一 `--- FAIL`；测试拦住了边界变异。 |
| 2026-08-28 | Task A 变异恢复 | 已恢复 `attempt+1 == MaxDialAttempts`，未保留变异。 |
| 2026-08-28 | Task B 基线 | `go test ./internal/ptyhost/engine ./internal/ptyhost/hostproc -run 'Test(Close|Kill|CloseRemovesSession|KillEndsProcessAndCleansDir)' -count=1` 退出码 0；原始输出：`ok  github.com/Xsxdot/handoff/internal/ptyhost/engine 0.003s`、`ok  github.com/Xsxdot/handoff/internal/ptyhost/hostproc 0.006s`。 |
| 2026-08-28 | Task B 先红 | 新增 FIFO/late seam `TestCloseWaitsForReapAndLateTrap` 后运行 `go test ./internal/ptyhost/engine -run TestCloseWaitsForReapAndLateTrap -count=1`；退出码 1，原始输出：`initcmd_test.go:153: shell trap 未打开 release FIFO`；现有 `Engine.Close` 已提前触发进程组终止，测试前提在当前 Linux shell 上未建立，需调整测试锁使其可观察而不改变目标。 |
| 2026-08-28 | Task B 测试裁决 | 当前 `/bin/sh` 对未捕获 SIGTERM 不执行 EXIT trap，故在同一 InitCommand 增加 `trap 'exit 0' TERM;`；EXIT trap 内容、FIFO 门与 late 断言不变，保证 TERM 后仍由 EXIT trap 阻塞等待释放。 |
| 2026-08-28 | Task B 真实锁缝红 | 为避免 shell 阻塞在测试 fake shell 的 `read` 上，在 InitCommand 末尾加入 `while :; do :; done`；运行 `go test ./internal/ptyhost/engine -run TestCloseWaitsForReapAndLateTrap -count=1`，退出码 1，原始断言：`Engine.Close 在 EXIT trap 写入 late marker 前返回`；旧实现提前返回，功能红成立。 |
| 2026-08-28 | Task B hostproc 先绿 | 升级 `TestKillEndsProcessAndCleansDir` 加入 HOME/ready/late/目录断言后运行 `go test ./internal/ptyhost/hostproc -run TestKillEndsProcessAndCleansDir -count=1`，退出码 0，原始输出：`ok  github.com/Xsxdot/handoff/internal/ptyhost/hostproc 0.006s`；该接缝基线先绿，未将其伪写成红。 |
| 2026-08-28 | Task B Engine 绿 | 加入 `session.exitedDone`，由已有唯一 `reap` 在 exit code/订阅/PTY 文件收口后关闭；`Engine.Close` 仅等待该通道并保留 SIGTERM→termGrace→SIGKILL。运行 `go test ./internal/ptyhost/engine -run TestCloseWaitsForReapAndLateTrap -count=1` 退出码 0，原始输出：`ok  github.com/Xsxdot/handoff/internal/ptyhost/engine 0.268s`。 |
| 2026-08-28 | Task B hostproc 绿 | 修改 Engine 后运行 `go test ./internal/ptyhost/hostproc -run TestKillEndsProcessAndCleansDir -count=1` 退出码 0，原始输出：`ok  github.com/Xsxdot/handoff/internal/ptyhost/hostproc 0.005s`。 |
| 2026-08-28 | Task B 局部绿 | `gofmt -w internal/ptyhost/hostproc/hostproc.go && go test ./internal/ptyhost/engine ./internal/ptyhost/hostproc -run 'Test(CloseWaitsForReapAndLateTrap|CloseRemovesSession|KillEndsProcessAndCleansDir)' -count=1` 退出码 0；原始输出：`ok  .../engine 0.266s`、`ok  .../hostproc 0.006s`。 |
| 2026-08-28 | Task B Wait 审计 | `rg -n 'cmd\\.Wait\\(' internal/ptyhost/engine` 退出码 0，唯一原始命中：`internal/ptyhost/engine/platform_unix.go:116: err := cmd.Wait()`；Engine 未新增第二次 Wait。 |
| 2026-08-28 | Task B 包级绿 | 并行运行 `go test ./internal/ptyhost/engine ./internal/ptyhost/hostproc -count=1` 与 `go build ./...`；测试退出码 0，原始输出：`ok  .../engine 22.223s`、`ok  .../hostproc 0.031s`；编译退出码 0、无输出。 |
| 2026-08-28 | Task B 变异锚点 | `rg -n -F 'case <-s.exitedDone:' internal/ptyhost/engine/engine.go` 唯一命中 `engine.go:319`；等待分支具备唯一可编译变异锚点。 |
| 2026-08-28 | Task B 变异结果 | 将唯一等待分支改为 `case <-time.After(0):`；`go build ./...` 退出 0、无输出；相关测试原始断言红：`Engine.Close 在 EXIT trap 写入 late marker 前返回`；随后 `go test ./internal/ptyhost/engine ./internal/ptyhost/hostproc -count=1` 退出 1，engine 同一 `--- FAIL`、hostproc `ok`；测试拦住变异。 |
| 2026-08-28 | Task B 变异恢复 | 已恢复 `case <-s.exitedDone:`，未保留变异。 |
| 2026-08-28 | Task C 基线 | 并行运行 `go test ./internal/ptyhost -run 'Test(ClientOpenList|ClientCloseRemovesSession|SurviveAgentdClientRestart)' -count=1`、`go test ./internal/agentd -run 'TestPtySessionListAndDelete|TestGracefulShutdownKeepsPtySession' -count=1`、`go test ./cmd -run 'TestService' -count=1`；三者退出码均 0，原始输出分别为 `ok  github.com.Xsxdot/handoff/internal/ptyhost 0.011s`、`ok  github.com.Xsxdot/handoff/internal/agentd 0.128s`、`ok  github.com.Xsxdot/handoff/cmd 0.009s`。 |
| 2026-08-28 | Task C 环境事实 | 默认 PTY 根测试 `TestClientOpenCloseWaitsForPtyhostAndShell` 仅得到 skip：原始输出 `PTY 测试根不可用；source=tmp ... read-only file system；source=repo-dot ... socket=113 字节，上限=100`；用任务可写覆盖根复跑后进入真实进程。 |
| 2026-08-28 | Task C 先红 | 为使真实 Linux shell 的 EXIT trap 可观测，Open 锁缝使用 FIFO、TERM→exit、busy loop；`HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/57592256 go test ./internal/ptyhost -run TestClientOpenCloseWaitsForPtyhostAndShell -count=1 -v` 退出码 1，原始断言：`Host.Close 在 EXIT trap 写入 late marker 前返回`；旧 Host.Close 提前返回，功能红成立。 |
| 2026-08-28 | Task C Adopt 红 | 新增真实 `reclaimPtySessions`→Adopt→DELETE/shutdown 入口后运行 `HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/57592256 go test ./internal/agentd -run 'Test(ReclaimedPtyDeleteWaitsForShell|ShutdownPtySessionsWaitsForReclaimedShell)' -count=1 -v`；退出码 1，原始断言分别为 `DELETE 返回后 late marker 不存在`、`shutdownPtySessions 返回后 late marker 不存在`；末尾另有 `go: failed to trim cache: open .../gocache/trim.txt: no such file or directory`。 |
| 2026-08-28 | Task C Host 绿 | `HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/57592256/open-02 go test ./internal/ptyhost -run TestClientOpenCloseWaitsForPtyhostAndShell -count=1 -v` 退出码 0，原始输出：`--- PASS ... (11.19s)`、`ok  .../internal/ptyhost 0.027s`；Open 的 waitDone 路径在真实 ptyhost 上等待 late。 |
| 2026-08-28 | Task C EOF 绿 | `HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/57592256/eof-02 go test ./internal/ptyhost -run TestCloseDoesNotTreatControlEOFAsSuccess -count=1 -v` 退出码 0，原始输出：`--- PASS ... (3.01s)`、`ok  .../internal/ptyhost 3.014s`；EOF 且目录保留时返回错误并清登记。 |
| 2026-08-28 | Task C Adopt 绿 | `HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/57592256/adopt-01 go test ./internal/agentd -run 'Test(ReclaimedPtyDeleteWaitsForShell|ShutdownPtySessionsWaitsForReclaimedShell)' -count=1 -v` 退出码 0，原始输出：`--- PASS TestReclaimedPtyDeleteWaitsForShell (2.00s)`、`--- PASS TestShutdownPtySessionsWaitsForReclaimedShell (0.10s)`、`ok  .../internal/agentd 2.102s`。 |
| 2026-08-28 | Task C Adopt client 绿 | 分别用独立覆盖根运行 `TestClientCloseRemovesSession`、`TestSurviveAgentdClientRestart`、`TestCloseAllKillsLiveSessions`；三次退出码均 0，原始 `ok` 分别为 `.../internal/ptyhost 0.027s`、`0.029s`、`0.028s`，均包含 late/目录/登记断言。 |
| 2026-08-28 | Task C service 夹具 | service 测试首次因 105 字节 socket 超限失败，原始错误为 `检查会话 socket 路径: 会话 socket 路径过长（105 字节，上限 100）`；随后修正为使用 `HANDOFF_PTY_TEST_ROOT` 作为短数据父目录并用 `config.Defaults()`，真实运行 `HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/57592256 go test ./cmd -run TestClosePtySessionsForStopWaitsForLateTrap -count=1 -v` 退出码 0，原始输出包含 `control_error=EOF`、`closed=1`、`ok .../cmd 0.026s`。 |
| 2026-08-28 | Task C 变异锚点 | `rg -n -F 'if errors.Is(err, os.ErrNotExist)' internal/ptyhost/client.go` 唯一命中 `client.go:521`；Adopt 的 session_dir 消失守卫可做语义变异。 |
| 2026-08-28 | Task C 变异结果 | 将唯一 session_dir 守卫改为 `if err == nil`；`go build ./...` 退出 0、无输出；EOF 负面真实测试原始断言红：`control EOF 且会话目录仍在时 Close 不得返回成功`；变异被拦住。 |
| 2026-08-28 | Task C 变异恢复 | 已恢复 `errors.Is(err, os.ErrNotExist)`，未保留变异。 |
| 2026-08-28 | Task C 变异恢复校正 | 初次恢复用短文本锚点误改了 `getFresh`；diff 审阅发现后已按函数上下文纠正：`getFresh` 恢复 `err == nil`，`waitPtyhostExit` 恢复 `errors.Is(err, os.ErrNotExist)`。 |
| 2026-08-28 | Task C 收尾绿 | 并行运行 `go test ./internal/ptyhost ./internal/agentd ./cmd -count=1` 与 `go build ./...`；编译退出码 0、无输出；测试退出码 0，原始输出：`ok  .../internal/ptyhost 0.082s`、`ok  .../internal/agentd 135.473s`、`ok  .../cmd 10.894s`。默认环境下部分 PTY 用例依赖根目录选择，真实关键入口已用短覆盖根单独验证。 |
| 2026-08-28 | Task C 校正编译 | 变异恢复后的函数上下文复核确认 `getFresh` 使用 `err == nil`、`waitPtyhostExit` 使用 `errors.Is(err, os.ErrNotExist)`；执行 `gofmt -w ...` 退出码 0，执行 `go build ./...` 退出码 0、无输出。 |
| 2026-08-28 | Task C 校正后关键绿 | 隔离根分别运行 `TestClientOpenCloseWaitsForPtyhostAndShell`、`TestCloseDoesNotTreatControlEOFAsSuccess`、`TestReclaimedPtyDeleteWaitsForShell`、`TestShutdownPtySessionsWaitsForReclaimedShell`；四次退出码均 0，原始输出分别为 `PASS / ok .../internal/ptyhost 0.979s`、`PASS / ok .../internal/ptyhost 3.007s`、`PASS / ok .../internal/agentd 0.825s`、`PASS / ok .../internal/agentd 0.850s`。 |
| 2026-08-28 | 局部 service 首次失败 | `HANDOFF_PTY_TEST_ROOT=/root/.handoff/tmp/57592256/service-02 go test ./cmd -run TestClosePtySessionsForStopWaitsForLateTrap -count=1 -v` 退出码 1；原始错误：`service_pty_test.go:28: stat /root/.handoff/tmp/57592256/service-02: no such file or directory`。该覆盖父目录未预先存在，结果不作测试结论。 |
| 2026-08-28 | 局部收尾绿 | `go test ./internal/testhttp -count=1` 退出 0：`ok .../internal/testhttp 0.034s`；HTTP 夹具/负扫描退出 0：`ok .../internal/agentd 0.113s`；Engine/hostproc seam 退出 0：`ok .../engine 0.275s`、`ok .../hostproc 0.005s`；service stop 重跑退出 0，原始输出含 `control_error=EOF`、`closed=1`、`--- PASS ... (0.02s)`、`ok .../cmd 0.026s`。 |
| 2026-08-28 | 集成全量绿 | `go test ./...` 退出码 0；原始输出包含 `ok github.com/Xsxdot/handoff 0.750s`、`ok github.com/Xsxdot/handoff/cmd 14.756s`、`ok github.com/Xsxdot/handoff/internal/agentd 166.644s`、`ok github.com/Xsxdot/handoff/internal/ptyhost 0.102s`、`ok github.com/Xsxdot/handoff/internal/ptyhost/engine 22.270s`、`ok github.com/Xsxdot/handoff/internal/ptyhost/hostproc 0.034s`、`ok github.com/Xsxdot/handoff/internal/testhttp 0.052s`，其余列出的包亦均为 `ok`。 |
| 2026-08-28 | 收尾编译与静态门 | 全量 `go build ./...` 退出码 0、无输出；`git diff --check` 无输出；`internal/agentd` 中无直接 `httptest.NewServer/NewUnstartedServer` 命中；`rg -n 'cmd\\.Wait\\(' internal/ptyhost/engine` 唯一命中 `internal/ptyhost/engine/platform_unix.go:116: err := cmd.Wait()`。 |
| 2026-08-28 | Task A 清理修正 | 删除 4 处迁移后遗留的重复 HTTP server cleanup（cardstep discipline/local、mirror 两 target、WS regression）；`gofmt` 退出 0，agentd helper/负扫描/Forward/Mirror/CardStep/WS 选择性测试退出 0：`ok .../internal/agentd 4.407s`，cleanup 静态扫描无输出。 |
| 2026-08-28 | 最终集成全量绿 | 最后一处代码清理后再次执行 `go test ./...`，退出码 0；原始输出包含 `ok github.com/Xsxdot/handoff  (cached)`、`ok github.com/Xsxdot/handoff/cmd (cached)`、`ok github.com/Xsxdot/handoff/internal/agentd 182.977s`、`ok github.com/Xsxdot/handoff/internal/ptyhost 0.119s`、`ok github.com/Xsxdot/handoff/internal/ptyhost/hostproc 0.108s`、`ok github.com/Xsxdot/handoff/internal/testhttp (cached)`，其余包均为 `ok` 或 `(cached)`。 |
| 2026-08-28 | 最终编译绿 | 全量 `go build ./...` 退出码 0、无输出。 |
| 2026-08-28 | 首轮提交 | `git add -A` 与 `git commit -m "fix: stabilize test ports and pty close"` 均退出码 0，首轮提交为 `07af4ed5`；随后以 amend 收录本条台账。 |
