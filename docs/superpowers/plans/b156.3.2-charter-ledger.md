# B156.3.2 charter（实现）节点台账（边干边落）

约定：每确立一个事实追加一行；命令与原始输出随行。分支 `cards/B156.3.2-charter-2`，
基线 d157e8b2。plan：`b156.3.2-plan.md`；plan 节点台账：`b156.3.2-plan-ledger.md`。

## Task 0：基线取证

- 2026-08-26 基线自证：当前分支 `cards/B156.3.2-charter-2`，工作树干净，HEAD=d157e8b2；`go build ./...` exit 0（BUILD_OK）。
- 2026-08-26 plan §Task0 代码与现行夹具的两处出入（照抄会编译失败/走不通，按现状适配并在此留痕）：
  1) `newFakeTargetMachine(t)` 现行签名是 `(t *testing.T, capSupported *bool)`——必须传 `&yes`；
     能力位 nil 时 discipline.ResolveDispatch 返回 ErrUnsupportedTarget（dispatch.go:66），派发链在 resolveStepDiscipline 同步段被拦，金样取证不出 dispatched。
  2) plan 预期「恰好 2 行」，实测稳定 **3 行**：`waitFor(TasksOf==1)` 只证明 LinkTask 落库，
     RecordDispatch（dispatched 事件）在其后；且首行是建卡时的 `card_created`（CreateCard appendEvent），
     plan 的预期漏了它。取证程序改为等事件流出现 `dispatched` 再读（临时文件内注释已说明）。
- 2026-08-26 GOLDEN 输出原文（连续两次运行逐字节一致）：
  ```
  GOLDEN|1|card_created|test|[{"title":"环节测试卡"},{"workflow":"bug"},{"workflow_version":1}]
  GOLDEN|2|comment|web:test|[{"body":"挂账 task T-fake-01@ftm（implement）"},{"kind":"普通"}]
  GOLDEN|3|dispatched|web:test|[{"branch":"cards/B1-implement"},{"discipline_name":"implement"},{"discipline_version":1},{"executor":"opencode"},{"model":""},{"purpose":"implement"},{"target":"ftm"},{"task_id":"T-fake-01"},{"template":"feature-impl"},{"template_version":2}]
  ```
- 2026-08-26 临时文件 internal/agentd/golden_dump_test.go 已删除；`git status` 确认无 .go 残留后进入 Task A。

## Task A：准入原子化（含两处对 plan 的偏离，均有实证）

- 2026-08-26 【红·编译】建 scheduling_test.go（plan §TaskA 步1 原文 + 补 encoding/json；strings 移到 Task B 再加）：
  首跑 `undefined: scheduling.ErrRetryExhausted`——新缝符号首红（编译红），核实为 Produces 清单符号非拼写错。
- 2026-08-26 只补 ErrRetryExhausted 哨兵声明让测试可编译后，**行为红取证**：
  `--- FAIL: TestConcurrentAdmitRespectsTwoLevelCaps … 成功数=5，期望恰=4` ——基线 TOCTOU 超发形状复现（plan 探针同形状 6>4）。
- 2026-08-26 【偏离一·有实证】按 plan §D1 代码块逐字落地 acquire(q,c)（上界判断折进 CAS、满员返回终局 ErrNoSlot、resolve/hasSlot 保持预滤分工）后，
  并发测试连续翻红且方向是**少进不是超发**：成功数=2 / ok / 3,3 / FAIL ——plan 自认的「良性偏保守」（D1 注记：输家拿过时成员预滤读数撞终局
  ErrNoSlot）在 N=16 打容量 4 下不是边缘现象而是主路径：resolve 在 c1 满员前给出的成员选择占绝大多数，输家全数死于 c1 终局而不看 c2。
  这同时违反冻结准入判据「小队有位 且 载体有位」——另一成员有位也报满员。**plan 的实现形状与它自己的验收判据（成功数恰=M）不可同时成立**。
- 2026-08-26 偏离一的修法（全部冻结条款保持不变：Admit/LaunchAdmit 公开签名与流程不变、无锁、修法全在包内、计数键仍只有两类）：
  resolve/hasSlot 删除，新增私有 admitInto(q,req)（按成员声明序逐个尝试原子准入，某成员满员换下一个，健康成员全满才 ErrNoSlot，
  无健康成员才 ErrNoHealthy）；acquire 改签名为 (q Squad, c Carrier, req IgnitionRequest) (Binding, error)，满员返回**内部哨兵**
  errMemberFull（不外露）；Binding 三元组投影抽成 bindingFor（与旧 resolve 同式：一次性覆盖>载体缺省）。变异靶唯一性反而更干净：
  全仓两级上界判断只剩 acquire 内一份。
- 2026-08-26 【偏离二·根因诊断】修完仍有 2~3/4 波动。临时插桩分流失败原因，抓到真凶：
  `admit-N 其他错误: registry sched_running/squad/s1 版本冲突: 期望 0 实际 1: ledger: 状态已被并发修改`
  ——这是 **ledger.ErrCASConflict**（ledger.go:23），而 scheduling 全部重试判断的是 **schedclient.ErrCASConflict**
  （schedclient.go:15）。组装点适配器 facadeAsRegistry.Put/Delete 原样透传门面错误，translateRegistryErr 只翻译了 NotFound——
  schedclient.ErrCASConflict 的文档自己写着「组装点的适配器负责翻译底层同义错误」（schedclient.go:13），冲突翻译从未被做过。
  CAS 重试路径经真实适配器**一直是死代码**，K2 是第一个真正依赖它的消费者。插桩读数（修复前）：success=3 noSlot=12 budget=0 other=4（全是未翻译冲突）等形态。
- 2026-08-26 偏离二修法：translateRegistryErr 增加 `ledger.ErrCASConflict → schedclient.ErrCASConflict` 分支并把 Put/Delete 过它
  （server.go 组装点接线行，有界文件集内；该函数的法定职责就是哨兵翻译）。测试侧同构 harness（facadeRegistry）同步补翻译。
  修复后连续 6 次运行逐字节一致：`诊断: success=4 noSlot=12 budget=0 other=0 squad=4 c1=2 c2=2` → 全绿。
- 2026-08-26 【绿】去掉插桩恢复 plan 判据原文后：`go test ./internal/scheduling/ -run TestConcurrentAdmitRespectsTwoLevelCaps -count=1`
  → ok；`go vet ./internal/scheduling/` 干净。
- 2026-08-26 【变异复验·三步读数】施加位置与执行位置同一棵树（worktree ce0588ac，分支 cards/B156.3.2-charter-2）：
  - 施加前锚点唯一性断言：squad_guard=1 carrier_guard=1（grep -c）；
  - 变异=整块删除 acquire 内两个 `if ... >= ...MaxConcurrency { return Binding{}, errMemberFull }`（改语义的最小删除，编译过 MUT_COMPILE_OK）；
  - 变异后行为断言+全量：`--- FAIL: TestConcurrentAdmitRespectsTwoLevelCaps ... 成功数=16，期望恰=4`（16 路全进，超发 300%，红得响亮）；
  - 恢复：从备份字节级回写（git diff 仅剩本 Task 有意改动），复跑 → ok 绿。
- 2026-08-26 回归：`go test ./internal/scheduling/ ./internal/keystone/ -count=1` → 双 ok（竖切覆盖 Enqueue/PopReady/QueueKinds 存量行为）。

## Task B：roundtrip 序列化边界锁

- 2026-08-26 免红绿声明（plan §TaskB：现有 Enqueue→PopReady 已正确 roundtrip，此测试锁回归不点亮新行为）。落地即绿：
  `--- PASS: TestIgnitionRequestRoundtripThroughRegistry`（零值分辨/全字段/卡级排队无节点 三子用例 + 空队列 (false,nil)）；
  `go test ./internal/scheduling/ -count=1` 全绿。

## Task C：Squad 解析层 + 排队分支接线

- 2026-08-26 【红】建 scheddispatch_test.go（夹具+T1/T2/T3；plan 原文两处适配：
  ①CreateCard 返回 Card 非 id 字符串，取 .ID；②包内已有 countEvents(approver_test.go:258)，改名 countCardEvents 避免 redeclare）。
  接线前运行：T1 `等待条件超时`、T2 首卡同因超时、T3 两子用例 `实得 <nil>` ——失败全部来自「Squad 分支不存在」的行为缺失，非编译错。
  （plan 预想 T1 是 lastDispatch 无 target 键——实测老路径连派发都发不出去：无 Binding 时 target 为空，resolveStepDiscipline 走
  「目标机未定」分支返回空产物，异步 ViaTemplate 失败，task 永远不产生。红的形状比 plan 预想更早，但同样证明行为缺席。）
- 2026-08-26 【偏离三·plan 与契约冲突，按契约改判】接线后 T2 队列行快照断言翻红：`队列快照 Target=，want ftm`。
  根因：plan 的 oracle 把载体投影（ftm）写进入队快照，但契约 §4.1 明确 IgnitionRequest 的 Target/Executor/Model 是
  **「一次性覆盖」**，载体字段解析只发生在准入时刻（§4.2 条 8）；满员时没有命中载体可投影，且 §5 规定出队后「再次走同一步入口」
  重新准入——队列行存原始覆盖才是正确语义（出队时哪台机器有空位当时才知道）。按契约把 oracle 改为 Target=""、Executor="grok"、
  Model=""、Priority="中"、Ready=true、Actor="web:test"（9 字段点数不变）。改后 T1/T2/T3 全绿。
- 2026-08-26 【绿】`go test ./internal/agentd/ -run 'TestSquadNode|TestEffectiveCovers' -count=1` → 4 支全 PASS
  （parity 矩阵 5 行 + T1 + T2 + T3 两子用例）。
- 2026-08-26 【回归】`go test ./internal/agentd/ -count=1` → ok(98.7s)，存量 cardstep/discipline/ledgerapi 测试零翻红。

## Task D：反面断言 + 收尾

- 2026-08-26 【金样】legacyGolden 字面量 = Task 0 取证原文逐行粘贴（3 行，含 card_created——plan 预期 2 行系漏计，取证程序已证）。
  `TestLegacyNodeEventSequenceUnchanged`：SetupAutomation 刻意在场的非小队派发链，事件序列逐行全等 PASS(0.89s)；
  调度侧零痕迹点数：sched_running=0 行、ignition_queue=0 行。等待条件与金样取证同款（等 dispatched 出现再读，防时序漂移）。
  注：plan 的 T4 形状里 waitFor(TasksOf==1) 后直接读事件流有竞态（Task 0 已实证），正式版改用同款稳定等待。
- 2026-08-26 【零 import 反面判据 + 强制正控】
  - 护栏（证明 grep 确实读到了内容）：`wc -c internal/ledgerstep/*.go | tail -1` → `170950 total`；
  - 反面判据：`grep -rn "scheduling\|keysclient\|schedclient" internal/ledgerstep/` → 零输出，exit=1；
  - 正控：临时向 wire.go import 块注入 `_ "github.com/Xsxdot/handoff/internal/scheduling"`（锚点唯一性断言 count==1 通过），
    同一条 grep 当场命中 `internal/ledgerstep/wire.go:7` exit=0 ——判据有牙；
  - 还原：从备份字节级回写，`git diff --stat internal/ledgerstep/` 为空，复跑 grep → 零输出 exit=1。
- 2026-08-26 【收尾门禁四条】
  1. `go build ./...` → exit 0；
  2. `go vet ./...` → exit 0（VET_EXIT=0）；
  3. `gofmt -l internal/agentd internal/scheduling` → 空输出（曾抓出 scheddispatch.go 注释块缺空行、scheduling_test.go 格式，已 -w 修复后复查为空）；
  4. `go test ./internal/scheduling/ ./internal/agentd/ ./internal/keystone/ -count=1` → 三包 ok
     （scheduling 0.186s / agentd 94.1s / keystone 0.119s）；另加相邻包保险：ledgerstep ok(6.1s)、ledger/api ok、schedclient 无测试文件。
- 2026-08-26 【D5 四条注记核对】①Release 生产接线无人认领→归 K5（配对原语归属表在案），本卡只在装配失败回滚路径调用 Release；②Extra 不随队列存活（契约 §4.1 九字段无槽位）；③CAS 重试预算 8 次与 ErrRetryExhausted 分流（实测 N=16 下 budget=0，未触发预算耗尽）；④两笔图债不修不顺手修。

## 修复轮（charter-3，2026-08-26）：范围=【一】translateRegistryErr 注释 + 【二】ErrRetryExhausted 生产消费者

- 2026-08-26 基线自证：分支 `cards/B156.3.2-charter-3`，HEAD=40bd8a4b（上轮实现提交），工作树干净；
  `go build ./...` exit 0（BUILD_OK）；`go vet ./...` exit 0（VET_OK）；`gofmt -l internal/agentd internal/scheduling` 空输出。
- 2026-08-26 基线测试：`go test ./internal/scheduling/ ./internal/agentd/ ./internal/keystone/ -count=1`
  → 三包 ok（scheduling 0.169s / agentd 134.653s / keystone 0.119s）。
- 2026-08-26 协调者缺陷亲核复现（改前读数）：全仓 `ErrRetryExhausted` 计数=8，其中**非 scheduling 包命中=0**
  ——哨兵无生产消费者属实；`grep -c errors.Is internal/agentd/scheddispatch.go`=2；生产代码 `scheduling.New(` =1 处
  （仅 server.go SetupAutomation 组装点）；ledgerstep 对 scheduling/keysclient/schedclient 零 import（0 命中）。
- 2026-08-26 设计定案（照协调者裁决，不重开）：准入路径 ErrRetryExhausted 与 ErrNoSlot **同处置转排队**，
  但前置一条 WARN 级落痕「准入重试预算耗尽，按满员转排队」；两条哨兵收敛到**同一个 Enqueue 调用点**
  （不写第二份入队语义）。递减路径（scheduling.go stepRunning 的 ErrRetryExhausted）不碰，归 K5。

- 2026-08-26 【二·红】先写测试 TestSquadAdmitBudgetExhaustedQueuesWithWarn（替身注册表 casConflictOnRunning：
  sched_running 的 Put 恒冲突、其余透传生产同款 facadeAsRegistry；经既有测试缝 SetScheduling 注入），接线前首跑：
  `--- FAIL: ... 预算耗尽应与满员同处置静默排队（返回 nil），实得 小队 "sq1" 准入被拒: scheduling: 运行计数连续冲突: squad/sq1 与 carrier/c1`
  ——失败来自「分流分支不存在」的行为缺失（缺陷原形：预算耗尽被当受理拒绝推去等人），非编译错。

- 2026-08-26 【二·实现】admitSquadStep 分流条件改为 `ErrNoSlot || ErrRetryExhausted`，后者先落
  `s.log.Warn("准入重试预算耗尽，按满员转排队", card/node/squad/actor/cause)` 再走**同一个 Enqueue 调用点**
  （不写第二份入队语义）。复跑 → `--- PASS: TestSquadAdmitBudgetExhaustedQueuesWithWarn`。
- 2026-08-26 【二·强制正控】锚点唯一性断言 count==1（grep -c）；变异=去掉 `|| ErrRetryExhausted` 半句
  （改语义非删除行，`go build ./internal/agentd/` 过 MUT_COMPILE_OK）；变异后当场翻红：
  `--- FAIL ... 实得 小队 "sq1" 准入被拒: scheduling: 运行计数连续冲突: squad/sq1 与 carrier/c1`——判据有牙；
  从备份字节级回写还原（git diff 仅剩本修复轮有意改动），复跑转绿 ok。施加位置与执行位置同一棵树（本 worktree）。
- 2026-08-26 【一】server.go translateRegistryErr 补拍板注释：「哨兵替换会丢底层报文，诊断信息由调用方日志补」，
  维持双裸哨兵现状不改包裹式。纯注释改动。
- 2026-08-26 【公开面证明】基线树（git archive HEAD=40bd8a4b 展开）与当前树各跑 `go doc -all ./internal/scheduling`
  再 diff：逐字节一致；`./internal/agentd` 的 doc diff 仅 admitSquadStep（非导出函数）头注释一行文字——
  两包导出符号零增减，「本轮新增导出符号为零」成立。
- 2026-08-26 【收尾门禁四条】`go build ./...` exit 0；`go vet ./...` exit 0；
  `gofmt -l internal/agentd internal/scheduling` 空输出；
  `go test ./internal/scheduling/ ./internal/agentd/ ./internal/keystone/ -count=1` 三包 ok
  （scheduling 0.154s / agentd 124.987s / keystone 0.103s）；相邻保险：ledgerstep ok(7.363s)。
- 2026-08-26 【反面判据回归】ledgerstep 对 scheduling/keysclient/schedclient 零 import：改后 grep 命中 0（与改前同）；
  金样 TestLegacyNodeEventSequenceUnchanged 随 agentd 包全量绿——存量直绑行为未动。
- 2026-08-26 【多了什么·正交扫描】
  A 跨子系统绑定点：本轮生产代码新增 **0** 个——`scheduling.New(` 生产计数改前 1 改后 1（恒为 server.go SetupAutomation
  组装点唯一一处）；SetScheduling 注入只发生在新测试内（既有测试缝，非生产绑定点）。
  B 新增导出符号/新文件/新依赖方向：全部 **0/无**（go doc 双包 diff 为证）。测试文件新增 import `log/slog`
  （断言 WARN 落痕所需）与 `schedclient`（替身须实现既有 Registry 端口），均为测试侧、不引入新生产依赖方向。
  C 计数式判据改前/改后：scheddispatch.go `errors.Is` 2→3；全仓 `ErrRetryExhausted` 8→14 且生产消费者 0→1
  （scheddispatch.go）；ledgerstep 零 import 0→0。
  D 同语义两遍检查：无——两条哨兵收敛在唯一 Enqueue 调用点，WARN 只加在分流入口，未复制入队逻辑。
- 2026-08-26 【对裁决示例的一处如实偏差】WARN 字段带 card/node/squad/actor/cause；**离散 carrier 字段给不出**：
  失败时 Binding 为零值，scheduling 公开面本轮冻结（逐字不变已证明）拿不到载体名，载体身份在 cause 文本的计数键里
  （`carrier/<名>`）。已在代码注释声明此代价，不在生产侧解析错误散文（跨子系统耦合更劣）。递减路径
  （scheduling.go stepRunning:489）按裁决未碰，归 K5。

## 偏离汇总（供审阅快速定位）

1. **准入结构**（Task A）：resolve/hasSlot 删除，admitInto 按成员原子准入循环 + acquire 返回内部哨兵 errMemberFull。
   动因：plan 字面实现被其自己的并发验收判据证伪（少进 2~3/4，违反冻结准入判据）。公开 API/无锁/包内修法/计数键全部不变。
2. **冲突哨兵翻译**（server.go translateRegistryErr + 测试同构 harness）：补 `ledger.ErrCASConflict → schedclient.ErrCASConflict`。
   动因：schedclient 文档规定的适配器职责从未实现，CAS 重试经真实适配器是死代码（插桩实证）。
3. **T2 oracle**（scheddispatch_test.go）：队列行 Target 按 plan 应为 "ftm"，按契约 §4.1「一次性覆盖」+§5「再次走同一步入口」改判 ""。
   plan 与冻结契约冲突时以契约为准。
4. 小适配（不改行为）：newFakeTargetMachine 补 &yes 实参；CreateCard 返回值取 .ID；countEvents 改名 countCardEvents；
   金样 3 行（含 card_created）与稳定等待条件；ErrRetryExhausted 提前到红取证阶段声明（新缝符号首红规则允许）。
