# B233.16 实现计划：跨机执行消费面（生产持有 ExecutionClient）的测试收口

状态：待拍板（L3 轻档，单卡，单缝=跨机执行消费面）。本节点为 plan；产出物是本文件 + 台账，不含实现代码、不建卡、不派发、不调用 handoff CLI。
卡号：B233.16（父卡 B233；阻塞 B233.17）。
有效基线分支：`cards/B233.1-charter-7`（本卡合并目标；当前工作分支 `cards/B233.16-charter-5`，本计划不切分支、不越过基线）。
冻结物：`docs/superpowers/specs/b233.16-contract.md`（冻结清单 #1–#57；轮 2 加冻 `runWaitOnce`）。
拆解稿：`docs/superpowers/specs/b233.16-breakdown.md`（§0 拍板 P1=A / P2=A / P3=A / P4=A；§4 单一实现范围与有界文件集）。
源 spec：`docs/superpowers/specs/b233.16.md`。

**本轮范围**：只覆盖契约 §7 移交的四件欠账 **T2/T3/T4/T5**。Ticket 0（生产侧收窄 + 8 组合接口 + 9 具名入口 + `runWaitOnce` + `StepRunner.Clients` 收窄 + 装配直通 + 三份 T1 签名锁 + target.json 注记 + 视图 diff 17 节点 + 契约 C-0/C-2/C-3 回写）**已完成，本计划不重做、不重开**。

**本计划不加/不改任何生产代码**：T1 已把消费点参数类型钉死在能力接口上，Ticket 0 已落全部具名入口。T2/T3 全是测试代码；T4/T5 是回归与对账。

---

## 0. 基线读数（本计划所有判据的出处；本节点亲自运行）

```text
$ git branch --show-current
cards/B233.16-charter-5
$ git log --oneline -3
83311cf1 breakdown(B233.16): 复核轮闭环——P1 独立复核、节点计数 16→17、拍板回写确认
f335c2b5 contract(B233.16): freeze wait-once named entry (P1=A) + boundary clarifications
9e03cb35 docs(B233.16): 拆解稿拍板归档——P1=A 退回 contract 加冻，P2–P4=A
$ git status --short
（空，工作树干净）

$ go build ./...
（无输出）
$ echo $?            # 合并 stdout 后退出码
0
$ go vet ./cmd/ ./internal/agentd/ ./internal/ledgerstep/
（无输出）
$ echo $?
0

$ go test ./cmd -run 'TestExecutionSignatureLocks|TestStopCLIRetainCopy|TestDispatch|TestWait|TestReply|TestContinue|TestCLIExecutionCommandsDoNotHandRollHTTP' -count=1
ok  	github.com/Xsxdot/handoff/cmd	0.624s
$ go test ./internal/ledgerstep -run 'TestRunner|TestB2336|TestClientFinalMessage|TestWire|TestB23316' -count=1
ok  	github.com/Xsxdot/handoff/internal/ledgerstep	2.495s
$ go test ./internal/agentd -run 'TestCardStep|TestStartCardStep|TestDisciplineTargetCap|TestB351|TestB23310|TestCardStepProbe' -count=1
ok  	github.com/Xsxdot/handoff/internal/agentd	9.553s

# 基线存量红（与 B233.16 无关，见 §7.4；本节点亲自跑到的原文）
$ go test ./cmd -run TestRepoContractGate -count=1
--- FAIL: TestRepoContractGate (0.03s)
    graph_gate_test.go:38: 契约违规 [over-budget] d_cli->d_workspace 直调 11 条超出预算 9
    graph_gate_test.go:38: 契约违规 [over-budget] d_gateway->d_workspace 直调 17 条超出预算 1
    graph_gate_test.go:38: 契约违规 [over-budget] d_orchestration->d_workspace 直调 49 条超出预算 19
    graph_gate_test.go:38: 契约违规 [over-budget] d_workspace->d_orchestration 直调 3 条超出预算 2
    graph_gate_test.go:38: 契约违规 [over-budget] d_workspace->d_protocol 直调 23 条超出预算 3
FAIL
$ go test ./internal/agentd -run TestLegacy -count=1
--- FAIL: TestLegacyNodeEventSequenceUnchanged (0.15s)
    scheddispatch_test.go:531: 第 1 条漂移：
        got=GOLDEN|2|dispatched|web:test|[...]
        want=GOLDEN|2|comment|web:test|[{"body":"挂账 task T-fake-01@ftm（implement）"},{"kind":"普通"}]
FAIL

# 图（使用 PATH 上的 codegraph，非 go run）
$ codegraph --repo . check
fails = 5（全部 [over-budget]，d_cli/d_gateway/d_orchestration→d_workspace 与 d_workspace→d_orchestration/d_protocol）
warns = 139
Error: 契约对照发现 5 处违规
$ codegraph --repo . check --view cards-B233.16-charter
fails = 5（与不带视图逐条一致），warns = 139（144 个 "kind" 行 = 5 + 139）
$ codegraph --repo . validate
（无 over-budget/非法输出；仅打印 flag 帮助尾部）
$ codegraph --repo . sym ExecutionClient
Error: 符号 "ExecutionClient" 不在图中（图未覆盖或名字有误）……回落 grep，记入图覆盖债
```

**图覆盖债（本轮实测，不得把空 `who-calls` 写成无调用方）**：

- `codegraph sym` 未命中：`ExecutionClient`、`runStop`、`runReply`、`runWaitOnce`、`dispatchStep`、`probeStatus`、`StepClient`（仅在 `codegraph/diffs/cards-B233.16-charter.json` 视图 diff，基线 best 图未吸收）。
- 命中：`runUntilDone`（baseline 返回节点）。
- 因此本计划一律用 `文件#符号` + 源码行锚，不用空图查询结论。

**基线判据复核结论（动手前已跑）**：`go build` / `go vet` 两命令 exit=0；三包定向用例 exit=0；两处存量红原文如上。T2/T3 的测试全部建立在「Ticket 0 生产已满足」之上——写下去即绿（见 §14「最薄路径条」豁免）。

---

## 1. 目标与接缝映射

一个事实：跨机执行消费点的**生产**依赖声明已经是能力接口（Ticket 0），本轮把「有牙、可机内复验」补上——替身能注入、扫描有守卫、行为无回归、图不劣化。

| 欠账 | 它锁的行为 | 接缝（spec §接缝清单 / contract §5） | 测试落点 |
|---|---|---|---|
| T2-CLI | CLI 执行消费点的替身注入：替身只实现所需接口方法，经具名入口被真实调用 | `cmd#runStop`/`runReply`/`runContinue`/`dispatchTask`/`resolveBareDiscipline`/`resolveCardDispatchDiscipline`/`compensateCardDispatch`/`runWaitOnce`/`runUntilDone`/`runFollow`/`warnIfTimeoutBelowStall`/`autoSyncAfterWait`（contract #13–20、#27–35） | `cmd/execution_signature_lock_test.go`（追加 T2 段） |
| T2-agentd | agentd 四个执行消费点的替身注入 + `probeStatus` 10s 时限 | `agentd#dispatchStep`/`probeStatus`/`(*Server).compensateStep`/`(*Server).pushWorkBranchVia`（contract #21–24、#31–34） | `internal/agentd/execution_signature_lock_test.go`（追加 T2 段） |
| T2-ledgerstep | `StepRunner.Clients` 字段替身注入，`diffNode`/`awaitNode`/`finishTask` 各经 `StepClient` 一次 | `ledgerstep#StepRunner.Clients` / `#clientFinalMessage`（contract #25–26、#38） | `internal/ledgerstep/execution_signature_lock_test.go`（追加 T2 段） |
| T3 | 「执行面文件不得持聚合依赖」源码守卫（符号级白名单 + 剔除注释/字符串 + 可变红） | `cmd/execution_surface_test.go` 既有守卫（contract §1 C-2、§7 P3/P4） | `cmd/execution_surface_test.go`（扩展） |
| T4 | 收窄零行为变化：定向用例逐条复跑 + 存量红不新增 | #45–51 | §7 命令清单（不改文件） |
| T5 | 契约图不新增 fail/warn；entries 与真实符号一致 | #52–57 | §8 命令清单（不改文件） |

**T2 替身注入形态（P2=A，不得推翻）**：替身直接注入已冻具名入口；**不为测试给生产加包级可变全局**（不改 `newTargetClient`/`targetClient` 为 var）。命令入口的 RunE 路径继续由既有 httptest 假 agentd 用例覆盖，两类证据不互替。

**T3 范围与粒度（P3=A / P4=A）**：扫描范围**仅 `cmd`**、且只扫执行面文件；白名单按符号级只放行 `newTargetClient`/`newTargetClientNamed`/`targetClient`；扫描前剔除注释与字符串；配可变红守卫。

**假缝禁令**：T2 的每个替身入口都落在 spec §接缝清单的符号上；T3 守卫守卫的是真实生产文件。无「只为走满分支」的假测试。

---

## 2. 有界文件集与测试范围

本计划触及文件（全部在 breakdown §4④ 有界文件集内，**不新增任何文件**）：

| 文件 | 动作 | 归属 task |
|---|---|---|
| `cmd/execution_signature_lock_test.go` | 追加 T2 CLI 替身 + 测试 | T2-CLI |
| `internal/agentd/execution_signature_lock_test.go` | 追加 T2 agentd 替身 + 测试 | T2-agentd |
| `internal/ledgerstep/execution_signature_lock_test.go` | 追加 T2 ledgerstep 替身 + 测试 | T2-ledgerstep |
| `cmd/execution_surface_test.go` | 扩展 T3 源码守卫 | T3 |
| `docs/superpowers/plans/b233.16-plan.md` | 本产出物 | plan |
| `docs/superpowers/ledgers/2026-09-12-b233.16-plan-ledger.md` | 台账 | plan |

**不改**：`cmd/*.go` 生产文件、`internal/agentd/*.go` 生产文件、`internal/ledgerstep/*.go` 生产文件、`internal/client/*`、`internal/proto/*`、`codegraph/*`、既有 httptest 链路用例（`internal/ledgerstep/runner_test.go` 的 WS/attach/done/diff 链、`cmd/*_test.go` 的假 agentd 用例）。
禁止把整个 `cmd` / `internal/agentd` / `internal/ledgerstep` / `internal/executor` 带入。

**测试范围声明（每 task 只跑触及包）**：

- T2-CLI：`go test ./cmd -run 'TestCLIExecutionStubsDoNotImplementTransport|TestRunStopUsesInjected|TestRunReplyUsesInjected|TestRunContinueUsesInjected|TestDispatchTaskUsesInjected|TestResolveBareDisciplineUsesInjected|TestResolveCardDispatchDisciplineUsesInjected|TestCompensateCardDispatchUsesInjected|TestRunWaitOnceUsesInjected|TestRunUntilDoneUsesInjected|TestRunFollowUsesInjected|TestWarnIfTimeoutBelowStallUsesInjected|TestAutoSyncAfterWaitUsesInjected|TestExecutionSignatureLocks' -count=1`
- T2-agentd：`go test ./internal/agentd -run 'TestAgentdExecutionStubsDoNotImplementTransport|TestDispatchStepUsesInjected|TestProbeStatusUsesInjected|TestCompensateStepUsesInjected|TestPushWorkBranchViaUsesInjected|TestExecutionSignatureLocks' -count=1`
- T2-ledgerstep：`go test ./internal/ledgerstep -run 'TestStepRunnerClientsInjectsStepClientOnly|TestStepClientStubDoesNotImplementTransport|TestExecutionSignatureLocks' -count=1`
- T3：`go test ./cmd -run 'TestCLIExecutionCommandsDoNotHandRollHTTP|TestCLIExecutionSurfaceHoldsNarrowDependencies|TestCLIExecutionSurfaceGuardHasTeeth' -count=1`
- T4/T5：见 §7/§8（整包/图级，不属于任何单个 task 的实现步骤）。

**全量测试不属任何单个 task**（见 §7 T4）。

---

## 3. Task T2-CLI：CLI 最小替身注入

**文件**：`cmd/execution_signature_lock_test.go`（只追加，不改 T1 既有 var 块与 `TestExecutionSignatureLocks`）。

**Consumes**（生产已存在，逐字，本轮只引用不改）：
- `func runStop(cmd *cobra.Command, c client.ExecutionClient, taskID string) error`
- `func runReply(cmd *cobra.Command, c client.ExecutionClient, taskID, answer string) error`
- `func runContinue(cmd *cobra.Command, c client.ExecutionClient, taskID, instructions string) error`
- `func dispatchTask(ctx context.Context, cli client.ExecutionClient, opts client.DispatchOpts) (*proto.Task, error)`
- `type dispatchClient interface { client.ExecutionClient; Status(ctx) (*proto.StatusResp, error) }`
- `func resolveBareDiscipline(ctx context.Context, cli dispatchClient, rawFile string) (discipline.ResolvedDiscipline, error)`
- `func resolveCardDispatchDiscipline(ctx context.Context, cl dispatchClient, st *ledger.Store, name, target string) (discipline.ResolvedDiscipline, error)`
- `type compensateClient interface { client.ExecutionClient; StopAndReclaim(ctx, taskID string) error }`
- `func compensateCardDispatch(ctx context.Context, cl compensateClient, cardID, target, taskID string) error`
- `type waitClient interface { client.ExecutionClient; WaitArchived(ctx, taskID); Attach(ctx, taskID); Status(ctx) }`
- `func runWaitOnce(cmd *cobra.Command, cli waitClient, taskID, addr string) error`
- `func runUntilDone(cmd *cobra.Command, taskID, addr string, cli waitClient) error`
- `func runFollow(cmd *cobra.Command, taskID, addr string, cli waitClient) error`
- `func warnIfTimeoutBelowStall(ctx context.Context, cli waitClient, idle time.Duration)`
- `func autoSyncAfterWait(cmd *cobra.Command, cli waitClient, addr string, ev *proto.Event)`

**Produces**（仅测试包内符号，不导出、不进生产）：
- `type execStub struct`（只实现 `client.ExecutionClient` 六方法）
- `type dispatchStub struct{ *execStub; statusCalls atomic.Int32; status *proto.StatusResp }`（`dispatchClient`）
- `type waitStub struct{ *execStub; archivedCalls/attachCalls/statusCalls atomic.Int32; archived *proto.Event; attachInfo *client.AttachInfo; attachErr error; status *proto.StatusResp }`（`waitClient`）
- `type compensateStub struct{ *execStub; reclaimCalls atomic.Int32; reclaimErr error }`（`compensateClient`）

### 3.1 基线判据（动手前，已在本节点跑过）

- `go test ./cmd -run 'TestExecutionSignatureLocks' -count=1` → `ok github.com/Xsxdot/handoff/cmd`（原文见 §0）。
- 新增用例应当**写下去即绿**：Ticket 0 已把所有入口收窄（`go build ./...` exit=0）。若任一新用例红，说明注入面在 Ticket 0 后又被改动，**停下来回协调者**，不要改生产来迁就测试。

### 3.2 完整代码（追加到 `cmd/execution_signature_lock_test.go` 末尾）

```go
// ---- B233.16 T2：CLI 最小替身注入 ----
//
// 替身只实现各消费点真正需要的接口方法，由已冻具名入口直接接收——证明
//「生产持有 ExecutionClient」的注入面成立（P2=A：不为测试给生产加包级可变全局）。
// 调用计数是「替身确实被注入」的哨兵：若生产仍内联 client.New，计数恒为 0，
// 断言变红。命令入口的 RunE 路径继续由既有 httptest 假 agentd 用例覆盖，
// 两类证据不互替。

type execStub struct {
	dispatchCalls atomic.Int32
	replyCalls    atomic.Int32
	continueCalls atomic.Int32
	stopCalls     atomic.Int32
	waitCalls     atomic.Int32
	followCalls   atomic.Int32

	stopRemoved  bool
	waitEvent    *proto.Event
	followEvent  *proto.Event
	lastDispatch client.DispatchOpts
}

// var _ client.ExecutionClient = (*execStub)(nil) —— 六方法齐备的编译期证据。
var _ client.ExecutionClient = (*execStub)(nil)

func (s *execStub) Dispatch(_ context.Context, opts client.DispatchOpts) (*proto.Task, error) {
	s.dispatchCalls.Add(1)
	s.lastDispatch = opts
	return &proto.Task{ID: "T-exec-stub"}, nil
}

func (s *execStub) Reply(context.Context, string, string, string) error {
	s.replyCalls.Add(1)
	return nil
}

func (s *execStub) Continue(context.Context, string, string) error {
	s.continueCalls.Add(1)
	return nil
}

func (s *execStub) Stop(context.Context, string) (bool, error) {
	s.stopCalls.Add(1)
	return s.stopRemoved, nil
}

func (s *execStub) WaitEvent(context.Context, string, bool) (*proto.Event, error) {
	s.waitCalls.Add(1)
	return s.waitEvent, nil
}

func (s *execStub) FollowEvents(_ context.Context, _ string, _ bool, _ time.Duration,
	onEvent func(*proto.Event) error, _ func(*client.BacklogSummary) error) error {
	s.followCalls.Add(1)
	if s.followEvent != nil {
		return onEvent(s.followEvent)
	}
	return nil
}

// dispatchStub 只比 execStub 多一个 Status——组合面的额外方法恰为消费点今日
// 真实调用（B233.16 契约 #4）。
type dispatchStub struct {
	*execStub
	statusCalls atomic.Int32
	status      *proto.StatusResp
}

func (s *dispatchStub) Status(context.Context) (*proto.StatusResp, error) {
	s.statusCalls.Add(1)
	return s.status, nil
}

var _ dispatchClient = (*dispatchStub)(nil)

// waitStub 是 wait 路径的组合面：额外方法恰为 WaitArchived/Attach/Status（#5）。
type waitStub struct {
	*execStub
	archivedCalls atomic.Int32
	attachCalls   atomic.Int32
	statusCalls   atomic.Int32
	archived      *proto.Event
	attachInfo    *client.AttachInfo
	attachErr     error
	status        *proto.StatusResp
}

func (s *waitStub) WaitArchived(context.Context, string) (*proto.Event, error) {
	s.archivedCalls.Add(1)
	return s.archived, nil
}

func (s *waitStub) Attach(context.Context, string) (*client.AttachInfo, error) {
	s.attachCalls.Add(1)
	return s.attachInfo, s.attachErr
}

func (s *waitStub) Status(context.Context) (*proto.StatusResp, error) {
	s.statusCalls.Add(1)
	return s.status, nil
}

var _ waitClient = (*waitStub)(nil)

// compensateStub 只多一个 StopAndReclaim（#6）。
type compensateStub struct {
	*execStub
	reclaimCalls atomic.Int32
	reclaimErr   error
}

func (s *compensateStub) StopAndReclaim(context.Context, string) error {
	s.reclaimCalls.Add(1)
	return s.reclaimErr
}

var _ compensateClient = (*compensateStub)(nil)

// TestCLIExecutionStubsDoNotImplementTransport 是可观察的接口隔离判据：四个替身
// 都不满足 client.Transport（即都没有 HTTPClient/BaseURL），也没有任何工作区/卡/
// 机器/PTY 方法——若组合面划分被破坏（被迫多实现方法），这里先红。
func TestCLIExecutionStubsDoNotImplementTransport(t *testing.T) {
	stubs := map[string]any{
		"execStub":       &execStub{},
		"dispatchStub":   &dispatchStub{execStub: &execStub{}},
		"waitStub":       &waitStub{execStub: &execStub{}},
		"compensateStub": &compensateStub{execStub: &execStub{}},
	}
	for name, s := range stubs {
		if _, ok := s.(client.Transport); ok {
			t.Fatalf("%s 实现了 client.Transport（含 HTTPClient/BaseURL），接口隔离破裂", name)
		}
	}
}

func TestRunStopUsesInjectedExecutionClient(t *testing.T) {
	stub := &execStub{stopRemoved: false}
	cmd := &cobra.Command{}
	cmd.SetContext(context.Background())
	var out bytes.Buffer
	cmd.SetOut(&out)
	if err := runStop(cmd, stub, "T1"); err != nil {
		t.Fatalf("runStop: %v", err)
	}
	if got := stub.stopCalls.Load(); got != 1 {
		t.Fatalf("Stop 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if !strings.Contains(out.String(), "留存") {
		t.Fatalf("runStop 输出应含留存提示，实得 %q", out.String())
	}
}

func TestRunReplyUsesInjectedExecutionClient(t *testing.T) {
	stub := &execStub{}
	cmd := &cobra.Command{}
	cmd.SetContext(context.Background())
	var out bytes.Buffer
	cmd.SetOut(&out)
	if err := runReply(cmd, stub, "T1", "allow"); err != nil {
		t.Fatalf("runReply: %v", err)
	}
	if got := stub.replyCalls.Load(); got != 1 {
		t.Fatalf("Reply 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if got := strings.TrimSpace(out.String()); got != `{"ok":true}` {
		t.Fatalf("reply stdout = %q，want 单行 {\"ok\":true}", got)
	}
}

func TestRunContinueUsesInjectedExecutionClient(t *testing.T) {
	stub := &execStub{}
	cmd := &cobra.Command{}
	cmd.SetContext(context.Background())
	var out bytes.Buffer
	cmd.SetOut(&out)
	if err := runContinue(cmd, stub, "T1", "改一下"); err != nil {
		t.Fatalf("runContinue: %v", err)
	}
	if got := stub.continueCalls.Load(); got != 1 {
		t.Fatalf("Continue 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if got := strings.TrimSpace(out.String()); got != `{"ok":true}` {
		t.Fatalf("continue stdout = %q，want 单行 {\"ok\":true}", got)
	}
}

func TestDispatchTaskUsesInjectedExecutionClient(t *testing.T) {
	stub := &execStub{}
	task, err := dispatchTask(context.Background(), stub,
		client.DispatchOpts{Prompt: "plan", ProjectName: "proj1"})
	if err != nil {
		t.Fatalf("dispatchTask: %v", err)
	}
	if got := stub.dispatchCalls.Load(); got != 1 {
		t.Fatalf("Dispatch 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if task == nil || task.ID != "T-exec-stub" {
		t.Fatalf("dispatchTask 返回 %+v，want 替身任务", task)
	}
	if stub.lastDispatch.Prompt != "plan" || stub.lastDispatch.ProjectName != "proj1" {
		t.Fatalf("DispatchOpts 透传失真: %+v", stub.lastDispatch)
	}
}

func TestResolveBareDisciplineUsesInjectedDispatchClient(t *testing.T) {
	resetFlags(t)
	configPath = writeTestConfig(t, "listen: \"127.0.0.1:1\"\ntoken: \"tok\"\n")
	stub := &dispatchStub{
		execStub: &execStub{},
		status:   &proto.StatusResp{DisciplinesSupported: boolPtr(true)},
	}
	if _, err := resolveBareDiscipline(context.Background(), stub, ""); err != nil {
		t.Fatalf("resolveBareDiscipline: %v", err)
	}
	if got := stub.statusCalls.Load(); got != 1 {
		t.Fatalf("Status 调用次数 = %d，want 1（探活必须经注入的组合面）", got)
	}
}

func TestResolveCardDispatchDisciplineUsesInjectedDispatchClient(t *testing.T) {
	resetFlags(t)
	configPath = writeTestConfig(t, "listen: \"127.0.0.1:1\"\ntoken: \"tok\"\n")
	st, err := ledger.Open(t.TempDir() + "/ledger.db")
	if err != nil {
		t.Fatal(err)
	}
	t.Cleanup(func() { _ = st.Close() })
	stub := &dispatchStub{
		execStub: &execStub{},
		status:   &proto.StatusResp{DisciplinesSupported: boolPtr(true)},
	}
	if _, err := resolveCardDispatchDiscipline(context.Background(), stub, st, "", ""); err != nil {
		t.Fatalf("resolveCardDispatchDiscipline: %v", err)
	}
	if got := stub.statusCalls.Load(); got != 1 {
		t.Fatalf("Status 调用次数 = %d，want 1（探活必须经注入的组合面）", got)
	}
}

func TestCompensateCardDispatchUsesInjectedCompensateClient(t *testing.T) {
	stub := &compensateStub{execStub: &execStub{}}
	if err := compensateCardDispatch(context.Background(), stub, "B1", "mac-02", "T-1"); err != nil {
		t.Fatalf("compensateCardDispatch: %v", err)
	}
	if got := stub.reclaimCalls.Load(); got != 1 {
		t.Fatalf("StopAndReclaim 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
}

func TestRunWaitOnceUsesInjectedWaitClient(t *testing.T) {
	t.Cleanup(func() { waitTimeout, waitNoSync, notifyFlag = 0, false, false })
	waitTimeout, waitNoSync, notifyFlag = 0, false, false
	stub := &waitStub{
		execStub: &execStub{},
		waitEvent: &proto.Event{
			Seq: 7, TaskID: "t1", Type: proto.EventTypeProgress,
			Payload: json.RawMessage(`{}`),
		},
	}
	cmd := &cobra.Command{}
	cmd.SetContext(context.Background())
	var out bytes.Buffer
	cmd.SetOut(&out)
	if err := runWaitOnce(cmd, stub, "t1", "127.0.0.1:9999"); err != nil {
		t.Fatalf("runWaitOnce: %v", err)
	}
	if got := stub.waitCalls.Load(); got != 1 {
		t.Fatalf("WaitEvent 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if got := strings.Count(strings.TrimSpace(out.String()), "\n"); got != 0 {
		t.Fatalf("一次性 wait 的 stdout 必须严格一行: %q", out.String())
	}
	if !strings.Contains(out.String(), `"seq":7`) {
		t.Fatalf("stdout 应是事件 JSON，实得 %q", out.String())
	}
}

func TestRunUntilDoneUsesInjectedWaitClient(t *testing.T) {
	t.Cleanup(func() { waitTimeout, waitNoSync, notifyFlag = 0, false, false })
	waitTimeout, waitNoSync, notifyFlag = 0, false, false
	stub := &waitStub{
		execStub: &execStub{},
		archived: &proto.Event{Seq: 21, TaskID: "t1", Type: proto.EventTypeArchived},
	}
	cmd := &cobra.Command{}
	cmd.SetContext(context.Background())
	var out bytes.Buffer
	cmd.SetOut(&out)
	if err := runUntilDone(cmd, "t1", "127.0.0.1:9999", stub); err != nil {
		t.Fatalf("runUntilDone: %v", err)
	}
	if got := stub.archivedCalls.Load(); got != 1 {
		t.Fatalf("WaitArchived 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if got := strings.Count(strings.TrimSpace(out.String()), "\n"); got != 0 {
		t.Fatalf("--until-done 成功 stdout 必须严格一行: %q", out.String())
	}
}

func TestRunFollowUsesInjectedWaitClient(t *testing.T) {
	t.Cleanup(func() { waitTimeout, waitNoSync, notifyFlag = 0, false, false })
	waitTimeout, waitNoSync, notifyFlag = 0, false, false
	stub := &waitStub{
		execStub: &execStub{},
		followEvent: &proto.Event{
			Seq: 3, TaskID: "t1", Type: proto.EventTypeProgress,
			Payload: json.RawMessage(`{}`),
		},
	}
	cmd := &cobra.Command{}
	cmd.SetContext(context.Background())
	var out bytes.Buffer
	cmd.SetOut(&out)
	if err := runFollow(cmd, "t1", "127.0.0.1:9999", stub); err != nil {
		t.Fatalf("runFollow: %v", err)
	}
	if got := stub.followCalls.Load(); got != 1 {
		t.Fatalf("FollowEvents 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if !strings.Contains(out.String(), `"seq":3`) {
		t.Fatalf("follow 应把事件逐行写出，实得 %q", out.String())
	}
}

func TestWarnIfTimeoutBelowStallUsesInjectedWaitClient(t *testing.T) {
	stub := &waitStub{
		execStub: &execStub{},
		status:   &proto.StatusResp{StallTimeout: "2h"},
	}
	// idle<=0 是「不设 --timeout」：不得探活。
	warnIfTimeoutBelowStall(context.Background(), stub, 0)
	if got := stub.statusCalls.Load(); got != 0 {
		t.Fatalf("idle=0 不应探活，Status 调用次数 = %d", got)
	}
	warnIfTimeoutBelowStall(context.Background(), stub, time.Hour)
	if got := stub.statusCalls.Load(); got != 1 {
		t.Fatalf("idle=1h<stall=2h 应探活一次，Status 调用次数 = %d", got)
	}
}

func TestAutoSyncAfterWaitUsesInjectedWaitClient(t *testing.T) {
	resetFlags(t)
	configPath = writeTestConfig(t, "listen: \"127.0.0.1:1\"\ntoken: \"tok\"\n")
	t.Cleanup(func() { waitNoSync = false })
	waitNoSync = false
	// Attach 返回错误即停在「自动同步跳过」分支，不触发任何 git 操作，
	// 但足以证明跨机 Attach 经注入的 waitClient 走。
	stub := &waitStub{execStub: &execStub{}, attachErr: errors.New("stub attach fail")}
	cmd := &cobra.Command{}
	cmd.SetContext(context.Background())
	var errBuf bytes.Buffer
	cmd.SetErr(&errBuf)
	autoSyncAfterWait(cmd, stub, "127.0.0.1:9999",
		&proto.Event{TaskID: "t1", Type: proto.EventTypeCompleted})
	if got := stub.attachCalls.Load(); got != 1 {
		t.Fatalf("Attach 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if !strings.Contains(errBuf.String(), "自动同步跳过") {
		t.Fatalf("Attach 失败应只打跳过提示，实得 %q", errBuf.String())
	}
}
```

**需在 `cmd/execution_signature_lock_test.go` 的 import 块追加**：`bytes`、`encoding/json`、`errors`、`strings`、`sync/atomic`（`context`/`time`/`client`/`ledger`/`proto`/`cobra` 已有）。`boolPtr` 复用同包 `cmd/upgrade_helpers_test.go` 的既有定义；`resetFlags`/`writeTestConfig` 复用 `cmd/root_test.go`。

### 3.3 可观测性（T2-CLI）

- 新段首写职责/边界注释（见上）；每个替身与被测入口写「它证明什么、若注入缺失会怎样」。
- 本 task **不新增生产执行路径**，无关键节点日志可加；Ticket 0 已落的消费点日志（`runStop`/`runReply`/`runContinue`/`dispatchTask`/`compensateCardDispatch`/`runWaitOnce`）逐字保留，由 §7 T4 回归覆盖。测试失败信息即本 task 的可观测面（每条 `t.Fatalf` 带期望值与实得值）。

### 3.4 步骤（2–5 分钟一动作）

1. 在 `cmd/execution_signature_lock_test.go` import 块追加 §3.2 列出的 5 个包。
2. 把 §3.2 的 T2 段整体追加到文件末尾（T1 的 var 块与空 `TestExecutionSignatureLocks` 不动）。
3. 跑 `go test ./cmd -run 'TestCLIExecutionStubsDoNotImplementTransport|TestExecutionSignatureLocks' -count=1` → 期望 `ok`（先验替身接口满足与 T1 不被干扰）。
4. 跑 §2 的 T2-CLI 全量 run 表达式 → 期望 `ok`。
5. 若任一步红：不改生产，记录原文并回协调者（见 §3.1）。

### 3.5 红绿 / 变异（锁缝断言）

T2 是「注入面」锁：Ticket 0 已完成生产，故**写下去即绿**（§14 豁免）。它**不是只断言今日绿**——调用计数（`stopCalls==1` 等）就是牙：若生产把 `runStop` 改成内联 `newTargetClient()` 而忽略入参，计数恒 0，用例变红。变异复核（可选、配对还原）：临时把 `runStop` 的 `c client.ExecutionClient` 改回 `*client.Client`，`go vet ./cmd/` 编译失败（T1 §4 已证），随后还原、`git diff` 干净；不改本 plan 的判定。

---

## 4. Task T2-agentd：agentd 最小替身注入

**文件**：`internal/agentd/execution_signature_lock_test.go`（只追加）。

**Consumes**（生产已存在，逐字）：
- `func dispatchStep(ctx context.Context, cl client.ExecutionClient, opts ledgerstep.DispatchOpts, target string) (*proto.Task, error)`
- `type statusClient interface { client.ExecutionClient; Status(ctx) (*proto.StatusResp, error) }`
- `func probeStatus(ctx context.Context, cl statusClient) (*proto.StatusResp, error)`
- `type stopReclaimClient interface { client.ExecutionClient; StopAndReclaim(ctx, taskID string) error }`
- `func (s *Server) compensateStep(ctx context.Context, cl stopReclaimClient, cardID, target, taskID string) error`
- `type runClient interface { client.ExecutionClient; Run(ctx, taskID, cmd string) (string,int,error) }`
- `func (s *Server) pushWorkBranchVia(ctx context.Context, cl runClient, target, branch, taskID string) error`

**Produces**（测试包内）：
- `type executionStub struct`（六方法 + 计数 + `lastDispatch`）
- `type statusStub struct{ *executionStub; statusCalls atomic.Int32; statusDeadline atomic.Int64; status *proto.StatusResp }`
- `type compensateStepStub struct{ *executionStub; reclaimCalls atomic.Int32 }`
- `type runStub struct{ *executionStub; runCalls atomic.Int32; lastRunCmd string }`

### 4.1 基线判据（动手前已跑）

- `go test ./internal/agentd -run 'TestExecutionSignatureLocks' -count=1` → 本节点随 §0 的 agentd 定向 run 一起 exit=0（`TestCardStep...` 组）。新增用例写下去即绿。

### 4.2 完整代码（追加到 `internal/agentd/execution_signature_lock_test.go` 末尾）

```go
// ---- B233.16 T2：agentd 最小替身注入 ----
//
// 四个执行消费点各注入只实现所需方法的替身：dispatchStep（ExecutionClient）、
// probeStatus（+Status）、compensateStep（+StopAndReclaim）、pushWorkBranchVia（+Run）。
// 调用计数是「替身确实被注入」的哨兵；替身均不满足 client.Transport。

type executionStub struct {
	dispatchCalls atomic.Int32
	replyCalls    atomic.Int32
	continueCalls atomic.Int32
	stopCalls     atomic.Int32
	waitCalls     atomic.Int32
	followCalls   atomic.Int32

	lastDispatch client.DispatchOpts
}

var _ client.ExecutionClient = (*executionStub)(nil)

func (s *executionStub) Dispatch(_ context.Context, opts client.DispatchOpts) (*proto.Task, error) {
	s.dispatchCalls.Add(1)
	s.lastDispatch = opts
	return &proto.Task{ID: "T-agentd-stub"}, nil
}

func (s *executionStub) Reply(context.Context, string, string, string) error {
	s.replyCalls.Add(1)
	return nil
}

func (s *executionStub) Continue(context.Context, string, string) error {
	s.continueCalls.Add(1)
	return nil
}

func (s *executionStub) Stop(context.Context, string) (bool, error) {
	s.stopCalls.Add(1)
	return true, nil
}

func (s *executionStub) WaitEvent(context.Context, string, bool) (*proto.Event, error) {
	s.waitCalls.Add(1)
	return nil, nil
}

func (s *executionStub) FollowEvents(context.Context, string, bool, time.Duration,
	func(*proto.Event) error, func(*client.BacklogSummary) error) error {
	s.followCalls.Add(1)
	return nil
}

type statusStub struct {
	*executionStub
	statusCalls    atomic.Int32
	statusDeadline atomic.Int64
	status         *proto.StatusResp
}

func (s *statusStub) Status(ctx context.Context) (*proto.StatusResp, error) {
	s.statusCalls.Add(1)
	if dl, ok := ctx.Deadline(); ok {
		s.statusDeadline.Store(int64(time.Until(dl)))
	}
	return s.status, nil
}

var _ statusClient = (*statusStub)(nil)

type compensateStepStub struct {
	*executionStub
	reclaimCalls atomic.Int32
}

func (s *compensateStepStub) StopAndReclaim(context.Context, string) error {
	s.reclaimCalls.Add(1)
	return nil
}

var _ stopReclaimClient = (*compensateStepStub)(nil)

type runStub struct {
	*executionStub
	runCalls   atomic.Int32
	lastRunCmd string
}

func (s *runStub) Run(_ context.Context, _, cmd string) (string, int, error) {
	s.runCalls.Add(1)
	s.lastRunCmd = cmd
	return "", 0, nil
}

var _ runClient = (*runStub)(nil)

// TestAgentdExecutionStubsDoNotImplementTransport 同 CLI 侧：接口隔离的可观察判据。
func TestAgentdExecutionStubsDoNotImplementTransport(t *testing.T) {
	stubs := map[string]any{
		"executionStub":     &executionStub{},
		"statusStub":        &statusStub{executionStub: &executionStub{}},
		"compensateStepStub": &compensateStepStub{executionStub: &executionStub{}},
		"runStub":           &runStub{executionStub: &executionStub{}},
	}
	for name, s := range stubs {
		if _, ok := s.(client.Transport); ok {
			t.Fatalf("%s 实现了 client.Transport（含 HTTPClient/BaseURL），接口隔离破裂", name)
		}
	}
}

func TestDispatchStepUsesInjectedExecutionClient(t *testing.T) {
	stub := &executionStub{}
	task, err := dispatchStep(context.Background(), stub,
		ledgerstep.DispatchOpts{Prompt: "p", Project: "proj"}, "mac-02")
	if err != nil {
		t.Fatalf("dispatchStep: %v", err)
	}
	if got := stub.dispatchCalls.Load(); got != 1 {
		t.Fatalf("Dispatch 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if task == nil || task.ID != "T-agentd-stub" {
		t.Fatalf("dispatchStep 返回 %+v，want 替身任务", task)
	}
	if stub.lastDispatch.Target != "mac-02" || stub.lastDispatch.Prompt != "p" {
		t.Fatalf("DispatchOpts 映射失真（Target 必须取具名 dispatchStep 的 target 参数）: %+v",
			stub.lastDispatch)
	}
}

func TestProbeStatusUsesInjectedStatusClientAndTenSecondDeadline(t *testing.T) {
	yes := true
	stub := &statusStub{
		executionStub: &executionStub{},
		status:        &proto.StatusResp{DisciplinesSupported: &yes},
	}
	resp, err := probeStatus(context.Background(), stub)
	if err != nil {
		t.Fatalf("probeStatus: %v", err)
	}
	if got := stub.statusCalls.Load(); got != 1 {
		t.Fatalf("Status 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if resp == nil || resp.DisciplinesSupported == nil || !*resp.DisciplinesSupported {
		t.Fatalf("probeStatus 返回 %+v，want 能力位 true", resp)
	}
	got := time.Duration(stub.statusDeadline.Load())
	if got < 8*time.Second || got > 10*time.Second {
		t.Fatalf("probeStatus 施加的探活时限 = %v，want ≈10s（契约 #49）", got)
	}
}

func TestCompensateStepUsesInjectedStopReclaimClient(t *testing.T) {
	s := &Server{log: discardLogger()}
	stub := &compensateStepStub{executionStub: &executionStub{}}
	if err := s.compensateStep(context.Background(), stub, "B1", "mac-02", "T-1"); err != nil {
		t.Fatalf("compensateStep: %v", err)
	}
	if got := stub.reclaimCalls.Load(); got != 1 {
		t.Fatalf("StopAndReclaim 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
}

func TestPushWorkBranchViaUsesInjectedRunClient(t *testing.T) {
	s := &Server{log: discardLogger()}
	stub := &runStub{executionStub: &executionStub{}}
	if err := s.pushWorkBranchVia(context.Background(), stub, "mac-02", "cards/B1", "T-1"); err != nil {
		t.Fatalf("pushWorkBranchVia: %v", err)
	}
	if got := stub.runCalls.Load(); got != 1 {
		t.Fatalf("Run 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if want := `git push origin "cards/B1"`; stub.lastRunCmd != want {
		t.Fatalf("Run 命令 = %q，want %q", stub.lastRunCmd, want)
	}
}
```

**需在 import 块追加**：`sync/atomic`、`time`、`strings`（`strings` 本段未用可省；`context`/`testing`/`client`/`ledgerstep`/`proto` 已有）。`discardLogger()` 复用 `internal/agentd/watchdog_test.go` 的既有定义。

### 4.3 可观测性

- 段首与每个替身/用例写职责与「红时说明什么」；`probeStatus` 的 10s 用 `statusDeadline` 实测（不是静态断言）。
- 无生产新增路径，不加生产日志；`compensateStep`/`pushWorkBranchVia` 的既有 `s.log` 文案由 §7 T4 复跑确认逐字不变。

### 4.4 步骤

1. import 块追加 `sync/atomic`、`time`。
2. 追加 §4.2 全段。
3. `go test ./internal/agentd -run 'TestAgentdExecutionStubsDoNotImplementTransport|TestExecutionSignatureLocks' -count=1` → `ok`。
4. `go test ./internal/agentd -run 'TestDispatchStepUsesInjected|TestProbeStatusUsesInjected|TestCompensateStepUsesInjected|TestPushWorkBranchViaUsesInjected' -count=1` → `ok`。

### 4.5 红绿 / 变异

同 §3.5：写下去即绿；计数即牙。`probeStatus` 若被改成不加 10s 时限，`statusDeadline` 为 0（无 deadline）→ 断言红——这条用例同时锁住契约 #49。

---

## 5. Task T2-ledgerstep：StepRunner.Clients 最小替身注入

**文件**：`internal/ledgerstep/execution_signature_lock_test.go`（只追加）。

**Consumes**（生产已存在）：
- `type StepClient interface { client.ExecutionClient; Diff(ctx, taskID, base string) (string, error); Attach(ctx, taskID string) (*client.AttachInfo, error); Done(ctx, taskID, note string) (bool, error) }`
- `type StepRunner struct{ ... Clients func(target string) (StepClient, error) ... }`
- `func (r *StepRunner) diffNode() func(context.Context, string, string) ([]string, error)`
- `func (r *StepRunner) awaitNode() func(context.Context, string, string) (string, error)`
- `func (r *StepRunner) finishTask() func(context.Context, string, string) error`
- `func clientFinalMessage(ctx context.Context, cl finalMessageClient, taskID string) (string, error)`（经 `awaitNode` 消费）

**Produces**（测试包内）：
- `type stepClientStub struct{ diffCalls/attachCalls/doneCalls/waitCalls atomic.Int32 }`

### 5.1 基线判据（动手前已跑）

- `go test ./internal/ledgerstep -run 'TestRunner|TestB2336|TestClientFinalMessage|TestWire' -count=1` → `ok`（§0 原文）。新增用例写下去即绿。
- 既有真实 HTTP 链路用例 `TestRunnerLocalClientUsesWaitAndDiffWire`（`internal/ledgerstep/runner_test.go`）**不得改动/删弱**——替身测隔离、链路测真实，两条证据并存。

### 5.2 完整代码（追加到 `internal/ledgerstep/execution_signature_lock_test.go` 末尾）

```go
// ---- B233.16 T2：ledgerstep 最小替身注入 ----
//
// 经 StepRunner.Clients 字段注入只实现 StepClient 的替身，分别跑 diffNode /
// awaitNode / finishTask 三条真实消费路径。调用计数是「替身确实被注入」的哨兵；
// 既有真实 HTTP 链路测试（runner_test.go 的 WS/attach/done/diff）保持不动。

type stepClientStub struct {
	diffCalls   atomic.Int32
	attachCalls atomic.Int32
	doneCalls   atomic.Int32
	waitCalls   atomic.Int32
}

var _ StepClient = (*stepClientStub)(nil)

func (s *stepClientStub) Dispatch(context.Context, client.DispatchOpts) (*proto.Task, error) {
	return &proto.Task{ID: "T-step-stub"}, nil
}

func (s *stepClientStub) Reply(context.Context, string, string, string) error { return nil }
func (s *stepClientStub) Continue(context.Context, string, string) error      { return nil }
func (s *stepClientStub) Stop(context.Context, string) (bool, error)          { return true, nil }

func (s *stepClientStub) WaitEvent(context.Context, string, bool) (*proto.Event, error) {
	s.waitCalls.Add(1)
	return &proto.Event{
		Type:    proto.EventTypeCompleted,
		Payload: json.RawMessage(`{"final_text":"final"}`),
	}, nil
}

func (s *stepClientStub) FollowEvents(context.Context, string, bool, time.Duration,
	func(*proto.Event) error, func(*client.BacklogSummary) error) error {
	return nil
}

func (s *stepClientStub) Diff(context.Context, string, string) (string, error) {
	s.diffCalls.Add(1)
	return "diff --git a/docs/out.md b/docs/out.md\n--- a/docs/out.md\n+++ b/docs/out.md\n", nil
}

func (s *stepClientStub) Attach(context.Context, string) (*client.AttachInfo, error) {
	s.attachCalls.Add(1)
	return &client.AttachInfo{
		RecentEvents: []proto.Event{{
			Type:    proto.EventTypeCompleted,
			Payload: json.RawMessage(`{"final_text":"final"}`),
		}},
	}, nil
}

func (s *stepClientStub) Done(context.Context, string, string) (bool, error) {
	s.doneCalls.Add(1)
	return true, nil
}

// TestStepClientStubDoesNotImplementTransport：替身不满足 client.Transport。
func TestStepClientStubDoesNotImplementTransport(t *testing.T) {
	if _, ok := any(&stepClientStub{}).(client.Transport); ok {
		t.Fatal("stepClientStub 实现了 client.Transport（含 HTTPClient/BaseURL），接口隔离破裂")
	}
}

func TestStepRunnerClientsInjectsStepClientOnly(t *testing.T) {
	stub := &stepClientStub{}
	runner := &StepRunner{Clients: func(target string) (StepClient, error) {
		if target != "mac-02" {
			t.Fatalf("Clients 收到 target=%q，want mac-02", target)
		}
		return stub, nil
	}}

	paths, err := runner.diffNode()(context.Background(), "mac-02", "T-1")
	if err != nil {
		t.Fatalf("diffNode: %v", err)
	}
	if got := stub.diffCalls.Load(); got != 1 {
		t.Fatalf("Diff 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if len(paths) != 1 || paths[0] != "docs/out.md" {
		t.Fatalf("diff 投影 = %v，want [docs/out.md]", paths)
	}

	msg, err := runner.awaitNode()(context.Background(), "mac-02", "T-1")
	if err != nil {
		t.Fatalf("awaitNode: %v", err)
	}
	if got := stub.waitCalls.Load(); got != 1 {
		t.Fatalf("WaitEvent 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
	if got := stub.attachCalls.Load(); got != 1 {
		t.Fatalf("Attach 调用次数 = %d，want 1（最终报文取用经同一替身）", got)
	}
	if msg != "final" {
		t.Fatalf("最终报文 = %q，want final", msg)
	}

	if err := runner.finishTask()(context.Background(), "mac-02", "T-1"); err != nil {
		t.Fatalf("finishTask: %v", err)
	}
	if got := stub.doneCalls.Load(); got != 1 {
		t.Fatalf("Done 调用次数 = %d，want 1（替身必须真的被注入）", got)
	}
}
```

**需在 import 块追加**：`encoding/json`、`sync/atomic`、`time`、`client`、`proto`（`context`/`testing` 已有）。

### 5.3 可观测性

- 段首职责/边界注释；每个替身方法注明它服务哪条消费路径。
- 无生产新增路径，不加生产日志；`diffNode`/`awaitNode`/`finishTask` 的既有 `slog` 日志逐字保留，由 §7 T4 复跑覆盖。

### 5.4 步骤

1. import 块追加 `encoding/json`、`sync/atomic`、`time`、`client`、`proto`。
2. 追加 §5.2 全段。
3. `go test ./internal/ledgerstep -run 'TestStepClientStubDoesNotImplementTransport|TestStepRunnerClientsInjectsStepClientOnly|TestExecutionSignatureLocks' -count=1` → `ok`。
4. 确认既有 `TestRunnerLocalClientUsesWaitAndDiffWire` 仍绿：`go test ./internal/ledgerstep -run 'TestRunnerLocalClientUsesWaitAndDiffWire|TestB2336TerminalRun' -count=1` → `ok`。

### 5.5 红绿 / 变异

写下去即绿；计数即牙。若 `StepRunner.Clients` 被改回 `func(string)(*client.Client,error)`，T1 编译失败（contract §4 已证）。

---

## 6. Task T3：执行面源码守卫扩展（`cmd`，符号级白名单 + 可变红）

**文件**：`cmd/execution_surface_test.go`（扩展；既有 `TestCLIExecutionCommandsDoNotHandRollHTTP` 不动）。

**Consumes**（生产事实）：执行面生产文件 = `dispatch.go`/`wait.go`/`reply.go`/`continue.go`/`stop.go`/`card_dispatch.go`；工厂符号 `newTargetClient`/`newTargetClientNamed`/`targetClient` 是合法聚合持有者（contract #40/#41）。
**Produces**（测试包内）：`executionSurfaceFiles`、`executionFactorySymbols`、`stripCommentsAndStrings`、`executionSurfaceViolations` 与两支测试。

### 6.1 基线实测（动手前已跑）

```text
$ grep -rn '\*client\.Client\|client\.New(' cmd/*.go | grep -v '_test.go'
cmd/card_dispatch.go:194:func targetClient(target string) (*client.Client, func(), error) {   ← 白名单
cmd/card_dispatch.go:205:		return client.New(addr, token), func() {}, nil                ← 白名单（同函数内）
cmd/root.go:237/241/248 ...（工厂文件，不在扫描集）
cmd/attach.go / gc.go / pull.go / reclaim.go / frames.go / project.go / card_driver.go / card_node.go / upgrade.go（非执行面，不在扫描集）
```

结论：六个执行面文件里，聚合出现**仅**在 `cmd/card_dispatch.go#targetClient`（白名单）内；其余五个执行面文件今日零聚合。判据正确，可落扫描。

**§6.2 扫描算法已在本节点独立验证**（把 `stripCommentsAndStrings`+`executionSurfaceViolations` 的逐字逻辑抽到仓外临时程序跑，不改工作树）：

```text
REAL dispatch.go -> []
REAL wait.go -> []
REAL reply.go -> []
REAL continue.go -> []
REAL stop.go -> []
REAL card_dispatch.go -> []
nonFactory -> [2:sneaky]      # 非白名单聚合用法判红（有牙）
factory -> []                 # 工厂符号放行
decoys -> []                  # 注释/字符串里的同形文本不误报
rawstring -> []               # 多行原始字符串不误报、行结构不被破坏
```

即：T3 的算法在真实执行面文件上零误报，且 synthetic 用例确实变红。

### 6.2 完整代码（追加到 `cmd/execution_surface_test.go` 末尾）

```go
// ---- B233.16 T3：执行面不得持聚合依赖（符号级白名单 + 剔除注释/字符串） ----

// executionSurfaceFiles 是「跨机执行消费点」所在的生产文件。工厂所在文件
// （root.go）不在其列：工厂保留聚合是 contract #40 明许的组装形态。
var executionSurfaceFiles = []string{
	"dispatch.go", "wait.go", "reply.go", "continue.go", "stop.go", "card_dispatch.go",
}

// executionFactorySymbols 是唯一放行的符号：聚合只允许出现在工厂里（P4=A）。
var executionFactorySymbols = map[string]bool{
	"newTargetClient":      true,
	"newTargetClientNamed": true,
	"targetClient":         true,
}

// stripCommentsAndStrings 用 go/scanner 重建源码：注释与字符串/字符字面量的内容
// 替换为等长空白（保留换行），其余 token 原样回填到原偏移——这样后续按行做符号级
// 扫描时不会把注释里的示例文本或字符串内容误判成聚合依赖。
func stripCommentsAndStrings(src []byte) []byte {
	out := make([]byte, len(src))
	for i := range out {
		out[i] = ' '
	}
	fset := token.NewFileSet()
	file := fset.AddFile("", fset.Base(), len(src))
	var s scanner.Scanner
	s.Init(file, src, nil, scanner.ScanComments)
	for {
		pos, tok, lit := s.Scan()
		if tok == token.EOF {
			break
		}
		if tok == token.COMMENT {
			continue
		}
		text := lit
		if tok == token.STRING || tok == token.CHAR {
			text = blankPreservingNewlines(lit)
		} else if text == "" {
			text = tok.String()
		}
		copy(out[file.Offset(pos):], text)
	}
	return out
}

// blankPreservingNewlines 把字面量内容替换为等长空白但保留换行，避免把多行
// 原始字符串压成一行而错乱行号。
func blankPreservingNewlines(s string) string {
	b := []byte(s)
	for i := range b {
		if b[i] != '\n' && b[i] != '\r' {
			b[i] = ' '
		}
	}
	return string(b)
}

// executionSurfaceViolations 返回非白名单符号里的聚合用法（行号:符号）。
// cleaned 必须已经过 stripCommentsAndStrings。
func executionSurfaceViolations(cleaned []byte) []string {
	funcRe := regexp.MustCompile(`^func\s+(?:\([^)]*\)\s+)?([A-Za-z_]\w*)`)
	var out []string
	current := ""
	for i, line := range strings.Split(string(cleaned), "\n") {
		if m := funcRe.FindStringSubmatch(strings.TrimLeft(line, " \t")); m != nil {
			current = m[1]
		}
		if !strings.Contains(line, "*client.Client") && !strings.Contains(line, "client.New(") {
			continue
		}
		if executionFactorySymbols[current] {
			continue
		}
		sym := current
		if sym == "" {
			sym = "<package>"
		}
		out = append(out, fmt.Sprintf("%d:%s", i+1, sym))
	}
	return out
}

// TestCLIExecutionSurfaceHoldsNarrowDependencies：执行面生产文件不得出现
// 非白名单的 *client.Client / client.New。
func TestCLIExecutionSurfaceHoldsNarrowDependencies(t *testing.T) {
	_, thisFile, _, ok := runtime.Caller(0)
	if !ok {
		t.Fatal("runtime.Caller 失败")
	}
	dir := filepath.Dir(thisFile)
	var violations []string
	for _, name := range executionSurfaceFiles {
		body, err := os.ReadFile(filepath.Join(dir, name))
		if err != nil {
			t.Fatal(err)
		}
		for _, v := range executionSurfaceViolations(stripCommentsAndStrings(body)) {
			violations = append(violations, name+":"+v)
		}
	}
	if len(violations) != 0 {
		t.Fatalf("执行面文件不得持聚合依赖（白名单仅工厂符号 %v）：%v",
			executionFactorySymbols, violations)
	}
}

// TestCLIExecutionSurfaceGuardHasTeeth 是可变红守卫：人为构造的非白名单聚合用法
// 必须被判红，工厂符号与注释/字符串里的同形文本必须放行。只断言「今日绿」不足以
// 证明守卫有牙（P4=A）。
func TestCLIExecutionSurfaceGuardHasTeeth(t *testing.T) {
	nonFactory := []byte("package cmd\n\nfunc sneaky() *client.Client {\n\treturn client.New(\"a\", \"b\")\n}\n")
	if got := executionSurfaceViolations(stripCommentsAndStrings(nonFactory)); len(got) == 0 {
		t.Fatal("非白名单聚合用法必须被判红（守卫无牙）")
	}

	factory := []byte("package cmd\n\nfunc targetClient(t string) (*client.Client, func(), error) {\n\treturn client.New(\"a\", \"b\"), func() {}, nil\n}\n")
	if got := executionSurfaceViolations(stripCommentsAndStrings(factory)); len(got) != 0 {
		t.Fatalf("工厂符号应被白名单放行，实得 %v", got)
	}

	decoys := []byte("package cmd\n\n// 说明：*client.Client 与 client.New( 只出现在注释里\nvar note = \"*client.Client client.New(\"\n")
	if got := executionSurfaceViolations(stripCommentsAndStrings(decoys)); len(got) != 0 {
		t.Fatalf("注释/字符串里的同形文本不得误报，实得 %v", got)
	}
}
```

**需在 import 块追加**：`fmt`、`go/scanner`、`go/token`、`regexp`（`os`/`path/filepath`/`runtime`/`strings`/`testing` 已有）。

### 6.3 可观测性

- 函数级注释写明「为什么白名单按符号而非整文件、为什么剔除注释/字符串」（见上）。
- 无生产日志改动；守卫失败信息附带 `文件:行号:符号`，定位到具体越界点。

### 6.4 步骤

1. import 块追加 `fmt`、`go/scanner`、`go/token`、`regexp`。
2. 追加 §6.2 全段。
3. `go test ./cmd -run 'TestCLIExecutionCommandsDoNotHandRollHTTP|TestCLIExecutionSurfaceHoldsNarrowDependencies|TestCLIExecutionSurfaceGuardHasTeeth' -count=1` → 期望 `ok`。
4. 若第 3 步在「现有文件」上红：说明执行面文件里存在未预见聚合用法，**不改生产**，把 `文件:行:符号` 原文记下回协调者（可能是漏收窄或需扩白名单的契约变更）。

### 6.5 红绿 / 变异

- 守卫有牙由 `TestCLIExecutionSurfaceGuardHasTeeth` 的 synthetic 非白名单用例承担（红→设计目的）。
- 配对变异（人工、可选）：临时在 `cmd/stop.go` 加 `var _ *client.Client` → `TestCLIExecutionSurfaceHoldsNarrowDependencies` 必红；随后移除、`git diff` 干净。本计划不把该人工变异写成自动步骤。

---

## 7. Task T4：行为不变回归

**文件**：不改文件（只跑命令、记原文到台账）。

**测试范围声明**：最后一轮整包/全量汇总，不属于任何单个实现 task。

### 7.1 命令与判据

```bash
gofmt -l cmd internal/agentd internal/ledgerstep                # 期望空（仅新增测试文件，应已被 gofmt 规整）
go vet ./cmd/ ./internal/agentd/ ./internal/ledgerstep/
go build ./...
go test ./cmd -count=1
go test ./internal/agentd -count=1
go test ./internal/ledgerstep -count=1
go test ./... -count=1
```

### 7.2 定向复跑（B229 / B233.10 / B233.12 / B233.13 关联）

```bash
go test ./cmd -run 'TestStopCLIRetainCopy|TestDispatch|TestWait|TestWaitUntilDone|TestReply|TestContinue|TestCardDispatch|TestCard|TestExecutionSignatureLocks' -count=1
go test ./internal/ledgerstep -run 'TestRunner|TestB2336|TestClientFinalMessage|TestWire' -count=1
go test ./internal/agentd -run 'TestCardStep|TestStartCardStep|TestDisciplineTargetCap|TestB351|TestB23310|TestProbe' -count=1
```

### 7.3 判据（钉行为，不钉计数）

- `go build ./...` / `go vet` 三包 exit=0。
- 上述定向 run 全绿；`internal/ledgerstep` 的真实 HTTP 链路用例（`TestRunnerLocalClientUsesWaitAndDiffWire`、`TestB2336TerminalRunDoesNotBecomeBusinessVerdict`）仍绿——**替身测试没有替代链路测试**。
- `go test ./...` 的**唯一**红是 §7.4 两条基线存量红；不得出现第三条。

### 7.4 基线存量红（本卡不改，逐条原文见 §0）

1. `cmd.TestRepoContractGate`：5 条 `d_*→d_workspace` over-budget（与 `codegraph check` 不带视图的 fail 集合逐一致），属 B233.15 迁出后的存量 workspace 预算问题，非本卡引入。
2. `internal/agentd.TestLegacyNodeEventSequenceUnchanged`：事件序列第 2 条漂移（`dispatched` vs 金样 `comment`），属 B233.10 存量时序问题，非本卡类型收窄引入。

**归因判据**（不得把存量红算成本卡失败或本卡通过）：本卡只加测试文件、不改生产，故「stash 后原样复现」不适用（没有生产 diff）——改判据为「红的内容与 §0 基线逐字一致，且与新增测试无调用关系」。`TestLegacyNodeEventSequenceUnchanged` 由 `internal/agentd` 包内既有测试触发，`TestRepoContractGate` 读 `codegraph/target.json`，均不经本卡新增测试的入口。

### 7.5 步骤

1. 依次跑 §7.1 + §7.2，把原始输出（含退出码）追加进台账。
2. 若出现第三条红：停下，把原文与最小复现记台账并回协调者；**不改生产、不放宽既有测试**。

---

## 8. Task T5：契约图对账

**文件**：不改文件（只跑命令、记原文到台账）。

### 8.1 命令与判据

```bash
codegraph --repo . check
codegraph --repo . check --view cards-B233.16-charter
codegraph --repo . validate
codegraph --repo . sym runUntilDone
```

- `check`（带/不带视图）的 `fails` 与 §0 基线一致（5 条，全部 `d_*→d_workspace` 存量 over-budget），`warns` 与 §0 一致（139）。
- `validate` 不新增 `issues`。
- `d_cli→d_transport` entries 含 `client 实体` 与 `client.Client`、预算 10；`d_gateway→d_transport` entries 含 `targetclient.Pool`/`client.Client`/`client 实体`、预算 9（Ticket 0 已冻结，本节点只复核不改）；不新建 `d_ledger→d_transport`（contract #52–#56）。
- `codegraph sym ExecutionClient`/`runStop`/`runWaitOnce`/`dispatchStep`/`probeStatus`/`StepClient` 仍不命中——记入图覆盖债（§0）；`runUntilDone` 命中作为对照。

### 8.2 步骤

1. 跑 §8.1 四条命令，原文（尤其 fails/warns 摘要与 exit）追加进台账。
2. 与 §0 基线逐条比对；不一致即停下回协调者（图被本卡测试改动影响的可能性极小，此处是「不劣化」门）。

**派发前自审**：T4/T5 不需要驱动派发系统，不涉及「本 task 由协调者执行」的冲突步骤。

---

## 9. 五项检查（实现类 plan 自审）

1. **缺陷族对抗审查**（按 `defect-families` 逐族，结论进 §3.5/§4.5/§5.5/§6.5）：
   - 假红/假绿测试：T2 的牙是**调用计数/哨兵**（替身未注入则计数 0 → 红），不是「只断言今日绿」；T3 的牙是 synthetic 非白名单用例 + 注释/字符串反例；替身不满足 `client.Transport` 是接口隔离的可观察判据。
   - 生命周期/状态机中断：本卡不改运行时代码；`runWaitOnce`/`awaitNode`/`probeStatus` 的行为由 T4 既有用例复跑覆盖。
   - 静默失败/误导报错：T4 复跑错误文案与退出码（`TestStopCLIRetainCopy` 文案、`TestWaitUntilDone*` 的 `ExitTimeout`/`ErrDependencyFailed`）。
   - 跨平台假设：T3 的 `go/scanner` 纯文本处理与 OS 无关；T2 无 OS 分支。
   - 门禁绕过：本卡不新增写/执行路径；T3 白名单允许的工厂非门禁。
   - 序列化边界：无新增字段/DTO/投影（见 #2）。
   - 枚举新值过白名单：无新增 EventType/State/命令/路径。
2. **序列化边界设问**：本卡不产生任何新数据字段；`client.DispatchOpts`/`AttachInfo`/`proto.Task`/`StatusResp`/`Event` 的 JSON 键不动。T2 的替身只做 Go 内方法调用，不跨序列化边界——无需 roundtrip。
3. **上下文预算检查**：每个 task 圈定单一有界文件（§2）；圈得出。
4. **类型标注**：非边界型（同进程 Go 类型/测试），无真机清单；breakdown §6 的真机项均为「确认非新增」。
5. **接缝覆盖（双向）**：
   - **测试→缝**：T2-CLI 每支用例的入口符号都在 contract #13–20/#27–35 的具名入口上；T2-agentd 对应 #21–24/#31–34；T2-ledgerstep 对应 #25–26；T3 入口是执行面生产文件本身（源码守卫），不属于消费点缝，属接缝清单之外的**守卫**（spec §测试决定 T3 明列）。
   - **缝→测试**：§1 表中每条缝至少被一支 T2 用例锁住（CLI 12 入口、agentd 4 入口、ledgerstep 2 入口）。
   - **内部锁**：t2 中的 `TestCLIExecutionStubsDoNotImplementTransport`/`TestAgentdExecutionStubsDoNotImplementTransport`/`TestStepClientStubDoesNotImplementTransport` 入口是替身类型本身，不在消费点缝上——声明为**附加内部锁**，不顶替任何缝级断言；理由：它们断言的是「替身方法集恰好最小」，从消费点缝构造不出（缝级调用的入参类型只保证能力接口，不保证替身没多实现 Transport）。T3 的 `TestCLIExecutionSurfaceGuardHasTeeth` 同理：入口是扫描辅助函数，不在接缝清单缝上，声明为**附加内部锁**，不顶替 T2 缝级断言（它顶替的是 T3 守卫自身的「有牙」要求）。无其他内部锁、无未声明退路。
6. **退路同闸**：本计划无「若意外先绿就改成直喂 X」类条件退路；所有入口符号一次性冻结。

---

## 10. 跨卡审计

本卡为单卡 L3 轻档、不扇出子卡（breakdown 明令禁止 B233.16.x），plan 集合只有本文件。按「单上下文兜底=自审可以但结论一律待拍板」：

- **对照冻结物**：本计划引用 contract #13–26（依赖声明）、#27–35（具名入口）、#36–39（编译期锁，不在本轮改动）、#40–44（工厂保留）、#45–51（行为不变）、#52–57（图）。T2 的替身方法集与 #4–#11 逐条一致（`dispatchStub`=+Status、`waitStub`=+WaitArchived/Attach/Status、`compensateStub`=+StopAndReclaim、agentd `statusStub`=+Status、`compensateStepStub`=+StopAndReclaim、`runStub`=+Run、`stepClientStub`=+Diff/Attach/Done）。无与冻结物矛盾处。
- **跨卡签名**：无子卡，无跨卡 Produces/Consumes 对齐问题；本卡消费的 `client.ExecutionClient` 六方法签名与 B233.3 冻结逐字一致（`internal/client/execution.go:35-42`）。
- **spec 故事逐条归属**：故事 1/4/5 → T4/T5；故事 2/3 → T1（Ticket 0）+ T2。无落空故事。
- **结论：待拍板**。

---

## 11. 派发前自审

本计划所有验收命令均本地运行；T4/T5 不驱动派发系统；无「本 task 由协调者执行」类冲突步骤。派发 implement 时，本计划即完整工单。

---

## 12. 占位符扫描与内部锁声明

**占位符扫描**：全文无 TBD /「同 Task N」/「加适当的错误处理」/「描述做什么却不给代码」。每个 task 给出精确文件、完整可编译代码块、import 追加清单、精确 run 表达式。

**允许的例外（测试骨架）**：无。T2 的替身代码全量给出，不复用异包夹具形态。

**内部锁声明**：见 §9.5 末条——三条 `*DoesNotImplementTransport` 与 T3 的 `TestCLIExecutionSurfaceGuardHasTeeth` 为附加内部锁，不顶替缝级断言，理由已逐条给。

**图覆盖债**：`ExecutionClient`/`runStop`/`runReply`/`runWaitOnce`/`dispatchStep`/`probeStatus`/`StepClient` 不在图（§0 实测），本计划用 `文件#符号`+行锚，不写空 `who-calls` 结论。

---

## 13. 自审三查

- **spec 覆盖**：spec §测试决定 T1（Ticket 0 已落）→ 本计划不重做；T2 → §3/§4/§5；T3 → §6；T4 → §7；T5 → §8。spec §接缝清单 18 行逐条在 §1 指向 T2 用例。
- **占位符扫描**：见 §12。
- **跨 task 类型/签名一致性**：T2 三个 task 使用的消费点签名全部在各自 Consumes 段逐字给出，且与 `cmd`/`agentd`/`ledgerstep` 生产定义（本节点已读）一致；替身方法签名与接口逐字对齐，无别名差异。

---

## 14. DAG 与最薄路径条

```
Ticket 0（已完成，生产收窄 + T1 签名锁）
        │
        ├─► Task T2-CLI（§3）──────┐
        ├─► Task T2-agentd（§4）───┤
        ├─► Task T2-ledgerstep（§5）┤
        ├─► Task T3（§6）──────────┤
        │                          ▼
        │                  Task T4 行为回归（§7，依赖上面全部）
        │                  Task T5 图对账（§8，依赖上面全部）
```

四支 T2/T3 之间无依赖（不同文件），可顺序或并行；T4/T5 仅验证，排在最后。

**最薄路径条判定（本卡豁免）**：本卡要锁的行为是「生产持有 ExecutionClient 且替身可注入」。Ticket 0 已让声明缝可达且生产正确，故 T2 测试**写下去就会绿**——按纪律「写下去就会绿的才免除」，不需要额外竖切卡点亮。T3 的守卫今天在现有执行面文件上也是绿的（§6.1 实测），其红由 synthetic 非白名单用例与人工配对变异证明。

---

## 15. 台账

与产出物同批提交：`docs/superpowers/ledgers/2026-09-12-b233.16-plan-ledger.md`（记录本节点跑过的命令与原始输出、图覆盖债、拍板引用）。
