# Plan：B156.3.1 — K1 d_execution_host·hostapi.RunTurn 承载实装（opencode 四跳；岔口一裁决 A）

> 状态：plan 节点产出（2026-08-26，handoff executor）。
> 卡：B156.3.1 ｜ 有效基线分支：`claude/kai-156-3-ba-df7357` ｜ 本卡合并目标以此为准。
> 上游附件：spec `docs/superpowers/specs/2026-08-26-b156.3-automation-keystone-design.md`、
> 契约 `docs/superpowers/specs/b156.3-contract.md`、拆解 `docs/superpowers/specs/b156.3-breakdown.md`（§三 K1 小节为本卡四段式与有界文件集的法定出处）。
> 台账：`docs/superpowers/plans/b156.3.1-plan.ledger.md`（与本稿同批提交，边干边落的既定事实全部在册）。

**拍板遵从声明**：本卡受拆解文档「岔口拍板记录」四条裁决约束。对本卡生效的是
**岔口一 → 方案 A**（仅实装 opencode；其余 CLI 值返回**含 CLI 名**的「未实装」
可行动错误，不静默兜底）。其余三条岔口的落点在 K2/K4/K5，本卡不触及。
**异议：无**——本稿对四条裁决没有异议，未发现裁决与本仓现状矛盾之处。

---

## 一、四段式（自拆解 §三-K1 抄录并具体化）

### ① 契约引用

- 契约 §7 hostapi 面：`TurnRequest` / `TurnReply` / `ErrUnavailable` / `New()` /
  `(*Host).RunTurn(ctx, TurnRequest) (TurnReply, error)` —— **签名一字不改**
  （冻结物逐字对照见 §十一-1）。
- 契约 §9 测试义务①：hostapi.RunTurn 对 opencode 载体的真实四跳——机内桩测之外，
  实现票必须在协调机实跑钉死（本 plan 把执行法写成脚本，见 §十三）。
- 契约 §6.3 Runner 缝铁律：只 Launch/Resume 两动作，不进派发状态机、不产生 task；
  竖切断言已存在（`internal/keystone/slice_test.go:271-276`），本票**不得删除**。
- spec §4.3 隔离 HOME（HOME=全套由载体登记显式携带）、§4.5 载体前提。
- 拆解「岔口拍板记录」岔口一附加约束 + 「边界澄清」澄清 2 的反面注记：
  组装侧直通层 `coordinatorRunner` 现状漏传 Workdir/Timeout 属 **K4 接线范围**，
  本卡有界文件集不含 `internal/agentd/server.go`，不得顺手改。

### ② 意图与为什么

coordinatorRunner 已直通 RunTurn（`internal/agentd/server.go:2313-2327`），hostapi
实装后整条拉起/唤醒链自动通电——它是 keystone 一切行为的物理前提。本卡是七张子卡里
唯一接缝对面是外部现实的子卡（边界型）：机内只能验契约形状与桩测行为，真实 opencode
行为靠本节点已就地实证的机制事实（台账 F1–F7）+ 协调者真机复演兜底。

### ③ 验收（判据先在基线复核过，逐条可判 pass/fail）

机内（逻辑半边，全部从声明缝 `hostapi.Host.RunTurn` 入口进入）：

| # | 判据 | 红绿 |
|---|---|---|
| A1 | 假 CLI 桩下新建会话：RunTurn 返回非空 SessionID 且回合文本在场 | 写下即红（骨架恒 ErrUnavailable）→ 实装后绿 |
| A2 | 带 SessionID 续接走 resume 分支：argv 出现 `-s <id>`，会话 id 原样透传 | 同上 |
| A3 | Timeout 到点判失败不挂死：错误标明超时、进程树被杀、总耗时远小于桩睡眠时长 | 同上 |
| A4 | 反面断言（岔口一裁决 A）：CLI="claude"/"grok" 的错误信息**含该 CLI 名且含「未实装」标记词**——只断言含名字会被 LookPath 失败假绿骗过（§九-族4 变异分析） | 同上 |
| A5 | 隔离 HOME 注入：req.HomeDir 非空时子进程 HOME=该值（经桩回读 env 证据断言）；HomeDir 字段赢过 req.Env 里的同名行 | 同上 |
| A6 | 包门禁：`go build ./... && go test ./internal/hostapi/... -count=1` 绿；`gofmt -l internal/hostapi` 无输出；`go vet ./internal/hostapi/...` 干净 | 收尾 |

真机（行为半边）：四跳实跑落证 = 真机清单第 1 条，**归协调者执行**（§十三）。

### ④ 入口指针

- 实装对象：`internal/hostapi/hostapi.go`（ErrUnavailable:19、RunTurn:47）+
  新增实现文件（见文件集）。
- 复用参考：`internal/prochost/`（日志先例 prochost.go:142；本票**不用**其 shim，
  理由 §五-2）、`internal/executor/opencode/`（serve 协议知识所在包；同属顶层域
  d_execution 不跨界，contract §7 明文——但 run 形态连 import 都不需要，见 §五-1）。
- 组装侧直通层（只读参考，不改）：`internal/agentd/server.go#coordinatorRunner`
  （2307–2327）。
- 假 CLI 桩仓内先例：`internal/executor/claudecode/timing_test.go:249-266`。

### 有界文件集（越出即停下提问）

```
internal/hostapi/hostapi.go        （RunTurn 体改薄委托 + 陈旧注释修正；导出面零改动）
internal/hostapi/driver.go         （新增：run 形态驱动本体）
internal/hostapi/group_unix.go     （新增：//go:build unix 进程组终止）
internal/hostapi/group_other.go    （新增：//go:build !unix 退化终止）
internal/hostapi/runturn_test.go   （新增：四支缝级测试 + 桩夹具）
docs/superpowers/plans/b156.3.1-plan.ledger.md （台账追加）
```

一个包，圈得出。**不含** keysclient/keystone/agentd/opencode/prochost 的任何文件；
两笔图债（baseline 行号漂移、陈旧 d_orchestration 声明）不修不顺手修（卡上下文红线）。

---

## 二、Interfaces（跨 task 与跨卡的唯一名字来源）

Consumes（既有，零改动，签名已与源码逐字核过）：

```go
// internal/hostapi/hostapi.go（本包，冻结面）
type TurnRequest struct{ CLI, HomeDir, Workdir, Model, Prompt, SessionID string; Env []string; Timeout time.Duration }
type TurnReply struct{ SessionID, Output string }

// internal/executor/claudecode/timing_test.go:249（形态先例，非 import）
t.TempDir() 落脚本 + t.Setenv("PATH", binDir+string(os.PathListSeparator)+os.Getenv("PATH"))
```

Produces（全部包内未导出；task 间按此逐字对齐）：

```go
const DefaultTurnTimeout = 30 * time.Minute                       // Task 2 产，Task 3 消
var supportedCLIs = map[string]bool{"opencode": true}             // Task 2 产，Task 3 消
func log() *slog.Logger                                           // Task 2 产，Task 3 消
func runTurn(ctx context.Context, req TurnRequest) (TurnReply, error) // Task 2 起、Task 3 补全
func resumeOrNew(req TurnRequest) string                          // Task 3 产，Task 3 消
func watchDeadline(ctx context.Context, p *os.Process, done <-chan struct{}) // Task 3 产/消
func buildArgv(bin string, req TurnRequest) []string              // Task 3
func buildEnv(req TurnRequest) []string                           // Task 3
func consumeEvents(r io.Reader) (TurnReply, error)                // Task 3
func tailBytes(b *bytes.Buffer, n int) string                     // Task 3
func configureProcess(cmd *exec.Cmd)                              // Task 3（双平台各一份）
func killGroup(p *os.Process) error                               // Task 3（双平台各一份）
type ocEvent struct { … }                                         // Task 3（JSONL 解析形状）
```

跨卡 Produces：**零新增导出符号**。组装点 coordinatorRunner（K4 范围）今天消费的
`hostapi.New()` / `RunTurn` 签名不变，K1 合入即自动通电，无需对方改一行。

---

## 三、基线判据复核记录（本节点亲自跑到的事实，非引用记忆）

实现票动手前，以下事实已在**本节点**就地实证（隔离 HOME、opencode 1.18.21，
完整命令与输出摘录见台账 F1–F7）。实现票若在这些事实上开工受阻，先复跑对应探针
再怀疑代码：

- F1 新建回合：`opencode run --format json "<msg>"` → exit 0，stdout 为 JSONL
  事件流，每行顶层带 `"sessionID":"ses_…"`，文本在 `part.text`。
- F2 续接：`… -s <id> "<问>"` → 全程同一 sessionID，上一回合内容被记得
  ——resume 分支的机制就是 `-s`。
- F3 会话不存在：exit 1 + stderr `Error: Session not found` + stdout 空
  ——resume 失败有干净信号，keystone 兜底链（重建→转等人）依赖它。
- F4 flag 面：`--format json`、`-m provider/model`、`-s`（help 原文在台账）。
- F5 空消息：exit 1 + `Error: You must provide a message or a command`
  ——run 形态无「无消息建会话」，故空 prompt 守卫响报（§五-3）。
- F6 `--` 分隔符有效：横杠开头的消息原样进 prompt。
- F7 TUI 第三/四跳：**未验证**（无头环境驱不了 TUI），维持真机清单第 1 条。

仓内现状（读码直核）：hostapi.go:47-48 骨架恒返回 ErrUnavailable；ErrUnavailable
全仓消费方 grep = **0**（实装后哨兵转纯兼容保留）；基线 `go build ./...` exit 0、
hostapi 包 `[no test files]`。四支测试写下去必红的静态依据即 hostapi.go:48
（返回的错误既不含 CLI 名也无「未实装」字样）。

---

## 四、Task DAG（串行单线；每步 2–5 分钟一动作）

```
T1 桩夹具+四支缝级测试（跑红留证） → T2 门禁段（A4 绿）
  → T3 驱动段+A1/A2/A3/A5 绿 → T4 冻结面微修+收尾门禁 → T5 台账+commit
```

最薄路径条自查：本卡要锁的行为（A1–A5）今天从 RunTurn 缝调用得不到（恒
ErrUnavailable），故 T2/T3 就是点亮行为的最薄可跑路径——门禁先行、驱动随后，
没有更薄的中间态可再拆。

---

### Task 1：假 CLI 桩夹具 + 四支缝级测试（红）

**目标**：把验收 A1–A5 写成测试落盘，对现状骨架跑出**红**并留存原文。
**文件**：`internal/hostapi/runturn_test.go`（新）。
**测试范围声明（最小化）**：只跑 `go test ./internal/hostapi/... -count=1`；
全量测试不属于任何单个 task。

写入完整内容：

```go
// runturn_test.go —— RunTurn 缝级测试。缝 = target.json 已声明的
// d_gateway→d_execution 方向上的 hostapi.Host.RunTurn 门面（消费方：组装点
// coordinatorRunner）。四支测试入口全部落在该缝上，无内部锁。
//
// 假 CLI 桩沿 PATH 注入（仓内先例 internal/executor/claudecode/timing_test.go
// #installPersistentFakeClaude）；桩回放的事件形状对齐上游实测抓取
// （plan §三 F1/F2），不是凭记忆编的。
package hostapi

import (
	"context"
	"os"
	"path/filepath"
	"strings"
	"testing"
	"time"
)

// installFakeCLI 把受控 shell 脚本伪装成载体 CLI 放进 PATH 最前。桩行为：
// argv 逐行记入 $FAKECLI_ARGV_FILE（断言证据），随后追加一行 env:HOME=…
// （隔离 HOME 断言证据）；$FAKECLI_SLEEP 非空时先睡够秒数（超时用例）；
// 最后按 run --format json 形态回放三行 JSONL（step_start/text/step_finish），
// 会话 id 取 `-s` 的值，缺省 ses_fake_new。
func installFakeCLI(t *testing.T) {
	t.Helper()
	binDir := t.TempDir()
	script := `#!/bin/sh
: "${FAKECLI_ARGV_FILE:?FAKECLI_ARGV_FILE 必须设置}"
for a in "$@"; do printf '%s\n' "$a" >>"$FAKECLI_ARGV_FILE"; done
printf 'env:HOME=%s\n' "$HOME" >>"$FAKECLI_ARGV_FILE"
if [ -n "$FAKECLI_SLEEP" ]; then sleep "$FAKECLI_SLEEP"; fi
SID=""; prev=""
for a in "$@"; do
  if [ "$prev" = "-s" ]; then SID="$a"; fi
  prev="$a"
done
if [ -z "$SID" ]; then SID="ses_fake_new"; fi
printf '%s\n' "{\"type\":\"step_start\",\"sessionID\":\"$SID\",\"part\":{\"type\":\"step-start\"}}"
printf '%s\n' "{\"type\":\"text\",\"sessionID\":\"$SID\",\"part\":{\"type\":\"text\",\"text\":\"ok-$SID\"}}"
printf '%s\n' "{\"type\":\"step_finish\",\"sessionID\":\"$SID\",\"part\":{\"type\":\"step-finish\",\"reason\":\"stop\"}}"
`
	fake := filepath.Join(binDir, "fakecli")
	if err := os.WriteFile(fake, []byte(script), 0o755); err != nil {
		t.Fatal(err)
	}
	t.Setenv("PATH", binDir+string(os.PathListSeparator)+os.Getenv("PATH"))
}

// withArgvCapture 准备桩的证据文件并把路径交给桩环境。
func withArgvCapture(t *testing.T) string {
	t.Helper()
	p := filepath.Join(t.TempDir(), "argv.txt")
	t.Setenv("FAKECLI_ARGV_FILE", p)
	return p
}

// readLines 读证据文件为非空行切片。
func readLines(t *testing.T, p string) []string {
	t.Helper()
	raw, err := os.ReadFile(p)
	if err != nil {
		t.Fatalf("读桩证据 %s: %v", p, err)
	}
	var out []string
	for _, l := range strings.Split(strings.TrimSpace(string(raw)), "\n") {
		if l != "" {
			out = append(out, l)
		}
	}
	return out
}

// A1 缝级红绿：新建会话必须返回非空 SessionID 与回合文本，且隔离 HOME
// 以 req.HomeDir 为准注入子进程环境。
func TestRunTurnNewSessionReturnsSessionID(t *testing.T) {
	installFakeCLI(t)
	capture := withArgvCapture(t)
	home := t.TempDir()
	h := New()
	reply, err := h.RunTurn(context.Background(), TurnRequest{
		CLI: "fakecli", HomeDir: home, Workdir: t.TempDir(), Prompt: "开场简报",
	})
	if err != nil {
		t.Fatalf("新建回合失败: %v", err)
	}
	if reply.SessionID == "" {
		t.Fatalf("新建会话 SessionID 为空")
	}
	if reply.SessionID != "ses_fake_new" {
		t.Fatalf("SessionID=%q, want ses_fake_new", reply.SessionID)
	}
	if !strings.Contains(reply.Output, "ok-ses_fake_new") {
		t.Fatalf("Output=%q 缺回合文本", reply.Output)
	}
	wantHome := "env:HOME=" + home
	lines := readLines(t, capture)
	found := false
	for _, l := range lines {
		if l == wantHome {
			found = true
		}
	}
	if !found {
		t.Fatalf("子进程 HOME 未注入为 %s，证据: %v", wantHome, lines)
	}
}

// A2 缝级红绿：带 SessionID 续接必须走 resume 分支——argv 出现 `-s <id>`
// 且回显会话 id 原样透传。
func TestRunTurnResumePassesSessionID(t *testing.T) {
	installFakeCLI(t)
	capture := withArgvCapture(t)
	h := New()
	reply, err := h.RunTurn(context.Background(), TurnRequest{
		CLI: "fakecli", SessionID: "ses_known", Prompt: "唤醒简报",
	})
	if err != nil {
		t.Fatalf("续接回合失败: %v", err)
	}
	if reply.SessionID != "ses_known" {
		t.Fatalf("SessionID=%q, want ses_known", reply.SessionID)
	}
	lines := readLines(t, capture)
	found := false
	for i, l := range lines {
		if l == "-s" && i+1 < len(lines) && lines[i+1] == "ses_known" {
			found = true
		}
	}
	if !found {
		t.Fatalf("argv 未出现 -s ses_known（resume 分支缺失）: %v", lines)
	}
}

// A3 缝级红绿：Timeout 到点判失败且不挂死——错误标明超时，总耗时远小于
// 桩睡眠时长（真进程真超时，不做环境探针式 skip）。
func TestRunTurnTimeoutFailsPromptly(t *testing.T) {
	installFakeCLI(t)
	withArgvCapture(t)
	h := New()
	begin := time.Now()
	_, err := h.RunTurn(context.Background(), TurnRequest{
		CLI: "fakecli", Prompt: "慢回合",
		Env:     []string{"FAKECLI_SLEEP=30"},
		Timeout: 300 * time.Millisecond,
	})
	if err == nil {
		t.Fatalf("超时回合应失败")
	}
	if !strings.Contains(err.Error(), "超时") {
		t.Fatalf("错误应标明超时: %v", err)
	}
	if el := time.Since(begin); el > 10*time.Second {
		t.Fatalf("超时未及时终止进程树，耗时 %v", el)
	}
}

// A4 反面断言（岔口一裁决 A）：未实装 CLI 的错误必须含该 CLI 名**且含
// 「未实装」标记词**。后者防一种变异假绿：删掉名单门禁后 LookPath 对
// "claude" 的失败信息同样含名字，只查名字的门禁形同虚设。
func TestRunTurnUnsupportedCLINamesTheCLI(t *testing.T) {
	installFakeCLI(t)
	h := New()
	for _, cli := range []string{"claude", "grok"} {
		_, err := h.RunTurn(context.Background(), TurnRequest{CLI: cli, Prompt: "x"})
		if err == nil {
			t.Fatalf("CLI %q 应报未实装错误", cli)
		}
		if !strings.Contains(err.Error(), cli) {
			t.Fatalf("错误应含 CLI 名 %q: %v", cli, err)
		}
		if !strings.Contains(err.Error(), "未实装") {
			t.Fatalf("错误应含「未实装」标记词（防 LookPath 假绿）: %v", err)
		}
	}
}
```

步骤：
1. 写入上述文件（≤2 分钟，全文照抄）。
2. `go vet ./internal/hostapi/...` 干净（编译期确认）。
3. `go test ./internal/hostapi/... -count=1` → **期望 4 支全红**，失败原因均为
   `hostapi: 协调者会话承载尚未接线`（A4 的报错形态为「错误应含 CLI 名」）。
   **把原始输出原文贴进台账**——这就是「判据先在基线跑」的执法记录：判据错的
   代价大于实现错，红必须发生在动手前。

**Interfaces**：Consumes 见 §二；Produces 无（纯测试）。
**日志/注释步骤**：测试文件的桩行为注释与缝归属注释随上文一次到位（核对点：
文件头注释指认了缝与先例出处）。

---

### Task 2：driver.go 门禁段（A4 绿）

**目标**：CLI 名单门禁 + 空 prompt 守卫 + 日志入口 + 缺省常量落地，A4 翻绿。
**文件**：`internal/hostapi/driver.go`（新）。

写入完整内容：

```go
// driver.go —— RunTurn 的 run 形态驱动：把一回合描述翻译成一次
// `<CLI> run --format json [--] <prompt>` 子进程，流式解析 JSONL 事件行，
// 汇出会话 id 与回合文本。
//
// 职责：
//   - CLI 名单门禁（岔口一裁决 A：仅 opencode；名单外报含名+含「未实装」的
//     可行动错误，绝不静默兜底到 opencode）
//   - argv/env 组装：model 透传、`-s` 即 resume、`--` 防横杠开头消息被当
//     flag（plan §三 F6）、隔离 HOME 确定性注入（HomeDir 字段是 HOME 的
//     语义所有者，赢过 req.Env 里的同名行）
//   - 超时执法：到点杀整棵进程树（unix 进程组 / 其余平台直接子进程），
//     回合判失败不挂死
//   - JSONL 解析：sessionID 取首个非空值；text part 按到达序拼接为 Output
//
// 边界：
//   - 不认识派发状态机的任何概念（task/waiting_review/verdict），铁律同包注释
//   - 零持久化：会话身份由 CLI 自己的存储承担（隔离 HOME 内），resume 连续性
//     靠 `run -s`（plan §三 F2），不靠本层记忆
//   - 只 import 标准库：不经 prochost shim（shim 的 detached 语义与 TaskID/
//     MarkRoot 归属判定是执行任务形状，协调者回合借用会把伪 task 凭据泄进
//     归属子系统）；孤儿风险由 Timeout 上界兜底（breakdown 缺陷族 1 明文
//     接受该形态）。也因此不需要 import opencode 包——run 形态只用它的
//     命令行契约，不用它任何代码
package hostapi

import (
	"bytes"
	"context"
	"fmt"
	"log/slog"
	"os/exec"
	"path/filepath"
	"time"
)

// DefaultTurnTimeout 是 TurnRequest.Timeout 为 0 时的缺省回合上界。数字不是
// 本票定的：hostapi.go 的 TurnRequest 注释（契约 §7 冻结面的一部分）已写死
// 「0 = 用包内缺省（30 分钟）」，实现照办不另立。
const DefaultTurnTimeout = 30 * time.Minute

// supportedCLIs 是本门面实装的载体 CLI 名单（岔口一裁决 A）。键按
// filepath.Base(req.CLI) 匹配：绝对路径形式的载体同样命中。名单扩张属于
// 契约变更，不在实现票权限内。
var supportedCLIs = map[string]bool{"opencode": true}

// log 返回包日志入口（跟随 agentd 的 slog 配置，先例 prochost.go:142）。
func log() *slog.Logger { return slog.Default().With("mod", "hostapi") }

// runTurn 是 RunTurn 的本体；hostapi.go 只留薄委托（冻结面文件零逻辑）。
// 本 task 先落门禁段，驱动段在下一 task 补全。
func runTurn(ctx context.Context, req TurnRequest) (TurnReply, error) {
	if !supportedCLIs[filepath.Base(req.CLI)] {
		log().Warn("拒绝未实装的载体 CLI", "cli", req.CLI)
		return TurnReply{}, fmt.Errorf(
			"hostapi: 载体 CLI %q 未实装（本门面当前仅支持 opencode，B156.3 岔口一裁决 A）；不静默兜底",
			req.CLI)
	}
	if req.Prompt == "" && req.SessionID == "" {
		// keysclient.Runner.Launch 的注释承诺「prompt 为空表示只建立会话」，
		// 但 run 形态没有无消息建会话的形态（plan §三 F5：空消息 exit 1）。
		// 今天全部调用方（keystone.launchRound 简报）恒非空，出现空值即接线
		// bug——宁可响报也不造一条没人要的模型回合（张力已挂跨卡审计待拍板，
		// 见 plan §十一-4）。
		log().Warn("拒绝空 prompt 的新建回合", "cli", req.CLI)
		return TurnReply{}, fmt.Errorf(
			"hostapi: 新建会话需要非空 prompt（载体 CLI %q 的 run 形态必须有消息）；出现此错误说明调用方接线有误",
			req.CLI)
	}
	return TurnReply{}, fmt.Errorf("hostapi: 驱动段未落地") // Task 3 替换
}
```

步骤：
1. 写入上述文件。
2. `go test ./internal/hostapi/... -count=1 -run TestRunTurnUnsupportedCLINamesTheCLI`
   → **绿**（贴输出进台账）。
3. 同命令跑其余三支 → **仍红**（失败原因变为 `驱动段未落地`）——确认门禁没有
   顺带把别的判据碰绿（判据独立性检查）。
4. 核对点：错误分支各带上下文字段（cli）的结构化日志已就位；成功路径日志在
   Task 3 落（现在还没有成功路径，不许预放恒真日志）。

**Interfaces**：Produces = `DefaultTurnTimeout` / `supportedCLIs` / `log()` /
`runTurn`（§二逐字一致）。

---

### Task 3：driver.go 驱动段 + 双平台终止（A1/A2/A3/A5 绿）

**文件**：`internal/hostapi/driver.go`（补全）、`group_unix.go`（新）、
`group_other.go`（新）。

driver.go 追加（接在 Task 2 内容之后，替换占位返回）：

```go
import ( // 与 Task 2 的 import 块合并为：
	"bytes"
	"bufio"
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"io"
	"log/slog"
	"os"
	"os/exec"
	"path/filepath"
	"strings"
	"time"
)
```

（上面整块只是合并说明；实际操作是把 driver.go 顶部 import 块换成这一块，
再追加以下全部代码，并把 runTurn 尾部的占位返回删掉、改为调用 `driveTurn`：）

```go
// ocEvent 是 `run --format json` 事件行的最小解析形状。字段名对齐上游实测
// 抓取（plan §三 F1/F2），不是猜的；未知字段由 json.Unmarshal 默认忽略，
// 上游加字段不炸解析。
type ocEvent struct {
	Type      string `json:"type"`
	SessionID string `json:"sessionID"`
	Part      struct {
		Type string `json:"type"`
		Text string `json:"text"`
	} `json:"part"`
}

// driveTurn 是一回合的进程编排主体（门禁通过后由此接力）。
func driveTurn(ctx context.Context, req TurnRequest) (TurnReply, error) {
	timeout := req.Timeout
	if timeout <= 0 {
		timeout = DefaultTurnTimeout
	}
	bin, err := exec.LookPath(req.CLI)
	if err != nil {
		log().Error("找不到载体 CLI", "cli", req.CLI, "cause", err)
		return TurnReply{}, fmt.Errorf("hostapi: 找不到载体 CLI %q: %w", req.CLI, err)
	}

	ctx, cancel := context.WithTimeout(ctx, timeout)
	defer cancel()

	cmd := exec.Command(bin, buildArgv(bin, req)...)
	cmd.Dir = req.Workdir // 空 = 继承 agentd cwd；生产调用方的 Workdir 非空语义由组装点（K4，澄清 2）保证
	cmd.Env = buildEnv(req)
	configureProcess(cmd)

	stdout, err := cmd.StdoutPipe()
	if err != nil {
		return TurnReply{}, fmt.Errorf("hostapi: 建 stdout 管道: %w", err)
	}
	var stderr bytes.Buffer
	cmd.Stderr = &stderr

	log().Info("协调者回合开始", "cli", req.CLI, "mode", resumeOrNew(req),
		"workdir", req.Workdir, "timeout", timeout.String(),
		"prompt_bytes", len(req.Prompt)) // 提示词与环境变量值永不进日志，只记长度
	started := time.Now()
	if err := cmd.Start(); err != nil {
		log().Error("拉起载体 CLI 失败", "cli", req.CLI, "cause", err)
		return TurnReply{}, fmt.Errorf("hostapi: 拉起 %q: %w", req.CLI, err)
	}
	watchDone := make(chan struct{})
	go watchDeadline(ctx, cmd.Process, watchDone)

	reply, parseErr := consumeEvents(stdout)
	waitErr := cmd.Wait()
	close(watchDone)
	dur := time.Since(started)

	if ctx.Err() != nil {
		log().Warn("协调者回合超时终止", "cli", req.CLI,
			"timeout", timeout.String(), "duration", dur.String())
		return TurnReply{}, fmt.Errorf("hostapi: 回合超时（上界 %v），已终止进程树: %w",
			timeout, ctx.Err())
	}
	if waitErr != nil {
		tail := tailBytes(&stderr, 4096)
		log().Warn("载体 CLI 回合失败", "cli", req.CLI, "duration", dur.String(),
			"stderr_tail_bytes", len(tail))
		// stderr 尾部进错误消息：resume 打错 id 时 keystone 兜底链需要看到
		// 「Session not found」（plan §三 F3）才能正确降级重建。
		return TurnReply{}, fmt.Errorf("hostapi: 回合失败（%v）stderr 尾部: %s",
			waitErr, tail)
	}
	if parseErr != nil {
		log().Warn("回合输出解析异常", "cli", req.CLI, "cause", parseErr)
		return TurnReply{}, fmt.Errorf("hostapi: 回合输出解析失败: %w", parseErr)
	}
	if reply.SessionID == "" {
		// exit 0 却没拿到会话 id：上游输出形态可能升级，如实响报，
		// 不用空 id 假装成功（静默失败族）。
		log().Warn("回合成功但事件流未携带 sessionID", "cli", req.CLI)
		return TurnReply{}, fmt.Errorf(
			"hostapi: 回合成功但事件流未携带 sessionID（载体 CLI %q 输出形态可能升级，需核对 plan §三 F1 抓取形状）",
			req.CLI)
	}
	log().Info("协调者回合完成", "cli", req.CLI, "session_id", reply.SessionID,
		"output_bytes", len(reply.Output), "duration", dur.String())
	return reply, nil
}

// resumeOrNew 返回回合模式的日志用词。
func resumeOrNew(req TurnRequest) string {
	if req.SessionID == "" {
		return "new"
	}
	return "resume"
}

// watchDeadline 在 ctx 到点时杀掉整棵进程树；done 关闭后安静退出。杀失败只
// 记日志：Wait 的返回错误已足够让回合判失败，这里不再叠加二次错误。
func watchDeadline(ctx context.Context, p *os.Process, done <-chan struct{}) {
	select {
	case <-ctx.Done():
		if err := killGroup(p); err != nil {
			log().Warn("超时终止进程组失败", "pid", p.Pid, "cause", err)
		}
	case <-done:
	}
}

// buildArgv 组装 run 形态命令行。`--` 终止 flag 解析（plan §三 F6）：提示词
// 内容不可信，横杠开头的消息不能被当成未知 flag 吞掉。
func buildArgv(bin string, req TurnRequest) []string {
	args := []string{bin, "run", "--format", "json"}
	if req.Model != "" {
		args = append(args, "-m", req.Model)
	}
	if req.SessionID != "" {
		args = append(args, "-s", req.SessionID)
	}
	return append(args, "--", req.Prompt)
}

// buildEnv 合成子进程环境：基础环境剔除被覆盖键 → 按 req.Env 原序追加 →
// HomeDir 非空时覆写 HOME（spec §4.3：隔离 HOME 是物种边界的环境执法；
// HomeDir 字段赢过 env 清单里的同名行——env 文件不该有能力偷换物种边界）。
// 值永不进日志。
func buildEnv(req TurnRequest) []string {
	override := map[string]bool{}
	for _, kv := range req.Env {
		if k, _, ok := strings.Cut(kv, "="); ok && k != "" {
			override[k] = true
		}
	}
	homeSet := req.HomeDir != ""
	if homeSet {
		override["HOME"] = true
	}
	out := make([]string, 0, len(os.Environ())+len(req.Env)+1)
	for _, kv := range os.Environ() {
		k, _, _ := strings.Cut(kv, "=")
		if override[k] {
			continue
		}
		out = append(out, kv)
	}
	for _, kv := range req.Env {
		if homeSet {
			if k, _, _ := strings.Cut(kv, "="); k == "HOME" {
				continue
			}
		}
		out = append(out, kv)
	}
	if homeSet {
		out = append(out, "HOME="+req.HomeDir)
	}
	return out
}

// consumeEvents 流式消费 JSONL 事件行直到 EOF：sessionID 取首个非空值，
// type=text 且 part.type=text 的文本按到达序以换行拼接为回合输出。Output
// 允许为空（工具型回合可能没有 text part），sessionID 不允许（driveTurn 执法）。
// 非 JSON 行（杂散告警等）跳过并记 Debug，不让脏行炸掉整条流。
func consumeEvents(r io.Reader) (TurnReply, error) {
	var reply TurnReply
	var texts []string
	sc := bufio.NewScanner(r)
	sc.Buffer(make([]byte, 0, 64*1024), 8*1024*1024) // 单行上限 8MB：长回合的 text part 可以很大
	for sc.Scan() {
		line := sc.Bytes()
		if len(bytes.TrimSpace(line)) == 0 {
			continue
		}
		var ev ocEvent
		if err := json.Unmarshal(line, &ev); err != nil {
			log().Debug("跳过非 JSON 事件行", "bytes", len(line))
			continue
		}
		if reply.SessionID == "" && ev.SessionID != "" {
			reply.SessionID = ev.SessionID
		}
		if ev.Type == "text" && ev.Part.Type == "text" {
			texts = append(texts, ev.Part.Text)
		}
	}
	if err := sc.Err(); err != nil {
		return TurnReply{}, fmt.Errorf("读事件流: %w", err)
	}
	reply.Output = strings.Join(texts, "\n")
	return reply, nil
}

// tailBytes 返回缓冲区末尾至多 n 字节；按字节切可能切开 UTF-8，只进错误消息
// 供人读，可接受。
func tailBytes(b *bytes.Buffer, n int) string {
	if b.Len() <= n {
		return b.String()
	}
	return string(b.Bytes()[b.Len()-n:])
}
```

   > **批注（2026-08-26 实现轮回填）：上面代码块的进程组装形态已实证错误，禁止照抄。**
   > `exec.Command(bin, buildArgv(bin, req)...)` 且 `buildArgv` 首元素为 bin，会把
   > 子进程 argv 组成 `[bin, bin, run, …]`：真 opencode 把第二个 binpath 当未知
   > 位置参数吞掉、落进根命令而非 `run` 子命令（台账探针 P0 用真二进制
   > `run --help` 两形态对照实证）；而 shell 桩的 `"$@"` 不含 `$0`，机内桩测对此
   > 恒绿——照抄本块会在真机上炸、测试却全绿。
   > **正确形态**：`cmd := exec.Command(bin)` 然后 `cmd.Args = buildArgv(bin, req)`
   > 整体赋值（argv[0]=bin 恰一次），见 `internal/hostapi/driver.go` driveTurn。
   > 两形态真机读数与修法全文见 `b156.3.1-plan.ledger.md`「探针 P0」。

并把 Task 2 版 runTurn 的最后一行占位返回替换为：

```go
	return driveTurn(ctx, req)
```

同时给 runTurn 的 doc 注释把「驱动段在下一 task 补全」一句删去（注释与实况一致纪律）。

`group_unix.go` 完整内容：

```go
//go:build unix

// group_unix.go —— unix 平台的进程组终止。run 子进程 Setpgid 成独立组，
// 超时按负 pid 对整组 SIGKILL：opencode 可能自起内嵌 server（`--port` flag
// 所示），只杀直接子进程会留下持有管道/端口的孤儿组。
package hostapi

import (
	"os"
	"os/exec"
	"syscall"
)

// configureProcess 让子进程自成进程组（必须在 Start 前设置）。
func configureProcess(cmd *exec.Cmd) {
	if cmd.SysProcAttr == nil {
		cmd.SysProcAttr = &syscall.SysProcAttr{}
	}
	cmd.SysProcAttr.Setpgid = true
}

// killGroup 对整个进程组 SIGKILL（负 pid 语义）。
func killGroup(p *os.Process) error {
	return syscall.Kill(-p.Pid, syscall.SIGKILL)
}
```

`group_other.go` 完整内容：

```go
//go:build !unix

// group_other.go —— 无进程组概念的平台退化路径：只杀直接子进程。opencode
// 自起 server 若成树残留，由真机清单第 6 条（Windows 条件项）承接；现役
// 协调机为 mac/linux（breakdown 缺陷族 3 原文），本文件不是本卡的验收面。
package hostapi

import (
	"os"
	"os/exec"
)

// configureProcess 无平台特化配置。
func configureProcess(cmd *exec.Cmd) {}

// killGroup 退化为单进程终止。
func killGroup(p *os.Process) error { return p.Kill() }
```

步骤：
1. 写入三个文件、替换占位返回（动作拆两次提交内完成即可，无需各自 commit）。
2. `go vet ./internal/hostapi/...` 干净。
3. `go test ./internal/hostapi/... -count=1` → **四支全绿**（原文贴台账）。
   若 A3 翻红：**先查重试/时序预算再查语义**——本票无 CAS，重点查桩 sleep 与
   Timeout 的相对量级（30s vs 300ms 余量大；CI 慢机不至于翻红）。
4. 核对点（日志）：入口带输入（mode/workdir/timeout/prompt_bytes）、外部调用
   前后（Start/Wait 各一条 Info/Warn）、每条错误分支带上下文、成功路径不静默
   ——逐条指认 driveTurn 内对应行。
5. 核对点（注释）：driver.go 文件头职责+边界已写；每个导出外函数（包内函数）
   的「为什么」注释在位；`errors`/`os` 等 import 若未被最终版本使用必须删除
   （gofmt+vet 双闸会拦，此处显式提醒避免来回）。

**Interfaces**：Produces = §二所列全部未导出符号（逐字一致）；Consumes =
TurnRequest/TurnReply + 标准库。
**测试范围**：仍只跑 `./internal/hostapi/...`。

---

### Task 4：hostapi.go 冻结面微修 + 收尾门禁

**文件**：`internal/hostapi/hostapi.go`（仅注释与 RunTurn 体；导出面一字不改）。

改动一（RunTurn 体改薄委托，doc 更新为实况）：

```go
// RunTurn 执行一回合（实装本体见 driver.go：run 形态驱动 + JSONL 解析 +
// 超时执法）。签名是契约 §7 冻结面，一字不改。
func (h *Host) RunTurn(ctx context.Context, req TurnRequest) (TurnReply, error) {
	return runTurn(ctx, req)
}
```

改动二（ErrUnavailable 注释更新为实况；变量保留——全仓消费方为零，删除属
越契约改动，契约 §7 导出面含它）：

```go
// ErrUnavailable 是骨架期的兼容哨兵（契约 §7 冻结导出面，保留不删）。
// RunTurn 实装后正常路径不再返回它：名单外 CLI 得到含 CLI 名的「未实装」
// 错误（岔口一裁决 A），名单内失败得到具体原因。
var ErrUnavailable = errors.New("hostapi: 协调者会话承载尚未接线")
```

改动三（包注释第 7–8 行的「实现归实现票」陈旧句改为实况）：

```go
// 实现走 `opencode run` 无头形态（见 driver.go），同属 d_execution 顶层域，
// 不跨子系统边界；会话身份由 CLI 自家存储承担，本门面零状态。
```

步骤：
1. 三处改动落地（`git diff -- internal/hostapi/hostapi.go` 确认：无导出符号
   行变化）。
2. 收尾门禁（全部原文贴台账）：
   ```bash
   gofmt -l internal/hostapi          # 期望：无输出
   go vet ./internal/hostapi/...      # 期望：干净
   go build ./...                     # 期望：exit 0
   go test ./internal/hostapi/... -count=1 -v   # 期望：ok，4 支 PASS
   go run . graph check --repo .      # 期望：fails=0（硬闸自证：本票零新增跨域边）
   ```
3. 铁律断言在位确认（本票**不得删除**竖切断言）：
   ```bash
   grep -c "Launch(keysclient.SessionSpec" ../../internal/keystone/slice_test.go
   ```
   期望 ≥1（slice_test.go:273 的接口形状断言仍在）。
4. 图数据：本票零新增跨域边、零 target.json 改动；新符号按仓库惯例随本轮
   触及做机械补登（contract §11 移交区口径），不做手工重扫。

**测试范围声明**：`./internal/hostapi/...` 触及包；`go build ./...` 全仓编译门禁；
全量 `go test ./...` 不属于本 task（implement 三段律，归收口轮）。

---

### Task 5：台账收尾 + commit

1. 台账补齐：四支测试的红/绿原始输出、门禁五条输出、grep 计数、commit hash。
2. `git add docs/superpowers/plans/b156.3.1-plan*.md internal/hostapi/` 后
   commit（不 push，不切分支，不改 git 配置）。commit message 建议：
   `impl(b156.3.1): hostapi.RunTurn 承载实装（run 形态；岔口一裁决 A）`。

---

## 五、设计决定与被否方案（对抗审查前置）

1. **run 形态而非 serve 协议形态**。依据：四跳探针本身就是围绕 `run -s` 展开
   （contract §1 台账⑫行原词「`run -s` resume / TUI 吃 `-s` / 接管往返」）；
   breakdown K1 验收指定「shell 脚本桩」，serve 形态要求桩模拟 HTTP 服务，
   shell 做不到——验收形状反向锁死了实现形态；会话身份由 opencode 自家存储
   （隔离 HOME 内）承担，TUI 接管因此免费成立。被否：serve 协议驱动
   （复用 internal/executor/opencode/api.go 全套）——能力过剩、与桩测验收形状
   冲突、还要自管 serve 生命周期/端口/密码三件套。
2. **裸 exec + 进程组超时杀，不经 prochost shim**（理由全文见 driver.go 边界
   注释）：shim 的 detached 语义与 TaskID/MarkRoot 归属判定是执行任务形状；
   孤儿上界 = Timeout（breakdown 缺陷族 1 明文接受）。
3. **空 prompt 响报守卫**（Task 2 内注释全文）：F5 实证 run 形态无原生支撑；
   今天全部调用方恒非空，出现即接线 bug。与 keysclient.Launch 注释承诺的张力
   → §十一-4 待拍板项。
4. **T4 加「未实装」标记词断言**：变异审查发现只查 CLI 名会假绿（删门禁后
   LookPath 报错同样含名）——§九-族4。

## 六、缺陷族对抗审查（逐族正面回答）

**1. 生命周期/状态机中断**：agentd 在回合中途崩溃 → runner 成孤儿，Timeout
是唯一上界（breakdown 族 1 明文接受；本票把上界做成真的：进程组 SIGKILL）。
hostapi 零状态零持久化，重启无可丢之物；会话身份在 opencode 自家存储里，
resume 天然承接。结论：窗口存在且有界，无无人认领的资源。

**2. 静默失败/误导报错**：三条防线——非零退出把 stderr 尾部 4KB 带进错误
（resume 打错 id 时 keystone 兜底链看得到 `Session not found`，F3）；exit 0 但
无 sessionID 响报不假装成功（driveTurn 内守卫）；exit 0 但解析器出错同样响报。
结论：无静默通道。

**3. 跨平台假设**：unix 进程组杀 / 非 unix 退化杀，分文件隔离且注释写明退化
后果；Windows 树残留归真机清单第 6 条（条件性，现役 mac/linux）。HOME 由
登记显式携带，本票只注入不推导、不拼 `~`。结论：无未经声明的平台假设。

**4. 假红/假绿测试**：A3 用真进程真超时（sleep 30 vs Timeout 300ms），无探针
式 skip；A4 双断言防 LookPath 假绿（§五-4）；桩事件形状对齐 F1 实抓而非想象
（上游改形状时空 sessionID 守卫会响报指路 F1）；变异复验盘点——删 resume 分支
→ A2 红（argv 无 `-s`）；删 HOME 注入 → A1 红（env 证据缺失）；删门禁 → A4 红
（无「未实装」）；删超时杀 → A3 红（挂死或耗时不达标）。结论：每支判据都有
能变红的变异靶。

**5. 门禁绕过**：物种门——driver.go 只 import 标准库（收尾 grep 判据：
`go list -deps` 或目测 import 块），执行域不新增对账本/会话/编制域的认识，
card_id 之外的账本概念零接触；派发门——本票不产生 task、不触碰 ledgerstep，
Runner 缝形状断言（slice_test.go:271-276）在位并由 Task 4 步骤 3 复核。结论：
无新增绕过面。

## 七、序列化边界设问

本票**零新增数据字段**：TurnRequest/TurnReply 冻结不动，registry/wire DTO 不
涉及。唯一序列化接触面是**消费方向**的外部 JSONL（ocEvent）：手写投影只有
consumeEvents 一处，其健壮性已有断言网（未知字段忽略 = 上游加字段安全；缺
sessionID 有响报守卫；非 JSON 行跳过有日志）。roundtrip 属性测试不适用——
本票没有 encode 半边，「两端各自有测试」的白名单问题不存在。结论：无未设防
投影点。

## 八、枚举新值过既有白名单

唯一名单 = supportedCLIs（本票自己就是门的执法者）；名单外值有 A4 锁。不新增
WakeKind/kind/角色等任何既有白名单的值。Priority 词表、QueueKinds 等本票不触及。

## 九、五项检查（出稿自审，结论在列）

1. **缺陷族对抗审查**：§六，五族逐族有答案。
2. **序列化边界**：§七，零新增投影点，消费向有守卫。
3. **上下文预算**：五个文件一个包（§〇文件集），圈得出；无竖切插卡需求。
4. **类型标注**：本卡为边界型子系统——行为验收的显式真机清单 = 真机清单第 1 条
   四跳（§十三给出逐条命令与期望），机内桩测半边由 A1–A6 承载，两层不互替。
5. **接缝覆盖（双向）**：
   - 测试→缝：A1–A5 入口符号全部是 `(*Host).RunTurn` ∈ target.json 已声明的
     `d_gateway→d_execution` entries（hostapi.Host/hostapi 包级函数）——看入口
    不看标注，全部合格。
   - 缝→测试：该缝被 A1–A5 至少一支缝级断言锁住（实际四支）✓。keysclient.Runner
     缝由既有竖切断言锁定（slice_test.go:271-276），本票只确认不删 ✓。
   - **内部锁声明：无**。四支测试入口全在缝上；正文无「意外先绿就改直喂 X」类
     条件退路。
   - 退路同闸：无退路安排。

## 十、占位符扫描自我声明

全文无 TBD /「适当错误处理」/「同 Task N」/只述不码。Task 1–4 的全部代码块
完整可抄；harness 例外的引用式写法**未使用**（桩夹具全文在列，无需指认照抄）。
唯一的「待替换」是 Task 2 内一行显式占位返回（`驱动段未落地`）——它是 Task 3
的法定替换点，红绿序列的组成部分，不是计划缺口，在此自我声明。

## 十一、跨卡审计（L3 重档；单上下文自审，结论一律**待拍板**）

1. **对照冻结物逐条**：契约 §7 hostapi 面代码块与 hostapi.go 现状源码逐字比对
   ——`TurnRequest` 七字段名/序/注释、`TurnReply` 两字段、`New()`、`RunTurn`
   签名、`ErrUnavailable` 导出名全部一致（本节点已核，台账在册）。本 plan 的
   改动清单不触碰其中任何导出行。**待拍板**。
2. **跨卡签名逐字比对**：本卡 Produces = 零新导出符号；Consumes 面
   （keysclient.SessionSpec/SessionRef/TurnResult、TurnRequest/TurnReply）与
   消费方 coordinatorRunner（server.go:2313-2317/2322-2324）的字段传递逐字核过
   ——coordinatorRunner.Launch 现状漏传 Workdir/Timeout 属 **K4 范围**（澄清 2），
   本卡不改 server.go，两卡在文件集上无交集。spec 故事归属：故事 1（开卡即绑）/
   故事 2（批准后放手）/故事 8（深夜唤醒链）的物理前提 = 本卡 RunTurn；接线归
   K4/K5 对应 task，无落空故事。**待拍板**。
3. **spec 测试接缝清单归属**：五条缝中本卡触两条——缝 3（出站 client 接口，
   竖切断言维持）与 hostapi 门面缝（d_gateway→d_execution，A1–A5 锁定）；其余
   三缝不触及。**待拍板**。
4. **审计发现（minor，提请拍板裁夺）**：keysclient.go Runner.Launch doc 注释
   承诺「prompt 为空表示只建立会话」，而 run 形态无原生支撑（F5）——本票以
   响报守卫处置（§五-3）。两个可选收口：(a) 该句注释由下次触及 keysclient 的
   卡修订；(b) 维持注释、由组装点保证 Launch 永收非空 prompt。本票选 (b) 的
   事实前提（今日调用方恒非空）已核实。**待拍板**。

   > **协调者拍板（2026-08-26）：两案都不采，改第三案——当场删掉那句注释。**
   >
   > 查证：`grep "prompt 为空\|只建立会话"` 扫 spec / 契约 / 拆解三份文档 + keystone
   > 与 hostapi 源码，**除 keysclient.go:39 那一句外零命中**。这个承诺从未进过任何
   > 治理文档，是契约轮出码时注释单方面发明的契约，也没有任何调用方依赖它
   > （`LaunchForCard` 恒送开场简报）。
   >
   > 不采 (a)：把修订推给「下次触及这个文件的卡」，正是今晚刚踩过的 `Service.Pointer`
   > 无主那条路——「下一个人会做」在跨卡场景里等于没人做。
   >
   > 不采 (b)：维持一句**没有任何代码兑现**的承诺，靠组装点的不成文约定保证它不被
   > 触发。这是今晚反复清理的那一族（假注释 + 空壳法定入口）：约定不是执法，
   > K4 接线时完全可能传空 prompt（「先建会话、prompt 稍后送」正是它字面允许的用法），
   > 换来一个 exit 1 的费解错误。
   >
   > **已落地**（协调者提交，不占本票文件集）：`keysclient.go` 的注释改成陈述实况
   > ——prompt 必须非空、原承诺为何删除、实现方对空 prompt 必须响报且不得静默兜底成
   > 「只建会话」。本票 §五-3 的响报守卫**保留且升格为法定**：它现在是这条注释的
   > 执行半边，不再是「防御一个理论上不该发生的输入」。
   >
   > 教训归档：**一个从未进过任何治理文档的承诺，混在冻结面的注释里活了下来。**
   > 契约文档与代码注释是两份会各自漂移的东西，冻结了前者不等于冻结了后者。

## 十二、派发前自审

本 plan 的收尾门禁与测试命令全部可由执行者在本仓直接跑；唯一需要驱动派发系统
自身的步骤是真机清单第 1 条四跳实跑——已标注「**本 task 由协调者执行，不派发**」
（§十三）。执行纪律的「不派发、不调 handoff CLI」与本 plan 无冲突点。

## 十三、真机清单第 1 条执行法（本 task 由协调者执行，不派发）

```bash
# 目标协调机上执行；CO_HOME = 登记载体的全套隔离 HOME（显式携带值，不拼 ~）
export CO_HOME=<carrier-home>; mkdir -p /tmp/fourhop && cd /tmp/fourhop

# 第 1 跳：无头新建，断言 stdout JSONL 首行携带 sessionID、text 含 hop1-ok
HOME=$CO_HOME opencode run --format json "reply with exactly: hop1-ok" | tee hop1.jsonl
SID=$(python3 -c "import json,sys;print(json.loads(sys.stdin.readline())['sessionID'])" <hop1.jsonl)

# 第 2 跳：resume 连续，断言全程同一 SID 且答得出上一跳内容
HOME=$CO_HOME opencode run --format json -s "$SID" "what did I ask? one word" | tee hop2.jsonl

# 第 3 跳：TUI 接管同一会话（人工确认上下文在场后退出交回）
HOME=$CO_HOME opencode -s "$SID"

# 第 4 跳：交回无头仍连续
HOME=$CO_HOME opencode run --format json -s "$SID" "say hop4-ok" | tee hop4.jsonl
```

原始输出四份 tee 文件 + 会话 id 一并落证台账；第 3 跳附人工确认一句（上下文
是否在场）。本节点 F7 未跑第 3/4 跳，此为其法定清偿处。环境前提：spec §4.5
载体前提（选定 CLI 登录态）与真机清单第 2/3 条联动，非本卡单独闭环。

## 十四、收尾自检

1. spec 覆盖：本卡四段式逐条落到 task（①契约引用→Task 2/4 门禁与冻结面微修；
   ②意图→Task 3 驱动段；③验收 A1–A6→Task 1–4 判据行；④入口指针→文件集与
   只读参考清单）；真机义务→§十三；铁律维持→Task 4 步骤 3。
2. 占位符扫描：§十，一处显式声明。
3. 跨 task 类型/签名一致性：§二 Produces 清单与 T2/T3/T4 代码块逐字一致
   （DefaultTurnTimeout/supportedCLIs/log/runTurn/driveTurn 及 helpers）。
4. ① 没跑到结果的命令不写结论：F7 明标未验证；四跳真机归协调者；② 本节点
   未碰 handoff 写路径、未起 executor、graph 仅只读 context/check 类查询。

红线自守：本轮 diff 仅含本 plan 与台账两个 .md 文件，零 `.go`/`.ts` 落盘。
