# B233.17 实现计划：组装点收窄与编译期封界

状态：可执行实现计划（2026-09-12）
卡：B233.17（父卡 B233）
基线：功能线 `cards/B233.1-charter-7 @ 686d87abf`；本工作分支起点 `bad7fdb5`（拍板回写）
输入：`docs/superpowers/specs/b233.17.md`、`docs/superpowers/specs/b233.17-contract.md`、`docs/superpowers/specs/b233.17-breakdown.md`（§0.1 拍板 P1–P5 全 A）
冻结物：contract @ `bd9c086c`；Ticket 0 守卫 `internal/agentd/b23317_boundary_test.go`；`codegraph/target.json`（assembly + 六方向配额 + entries）；本分支视图 `codegraph/diffs/cards-B233.17-charter-2.json`

本计划只做三件事：**handler 退出 assembly 文件**（缝 1）、**四包编译期封界**（缝 2）、**B233.16 守卫漏判收口**（缝 3 的锁）。目标图（`target.json`）与视图已在 contract 冻结，本卡实现**不改 `best.json`/`baseline.json`、不抬配额**。

---

## 1. 目标、冻结边界与执行顺序

三句逐条：

1. **处理请求的代码不在免检文件里。** `internal/agentd/server.go` 里 21 个 `Server.handle*`（`:889`–`:2333`）连同同段私有 helper/DTO 迁到不在 assembly 名单的新文件；`server.go` 保留 `NewServer`/`Set*`/`Handler()` 路由表/`SetupAutomation`（插线）并**仍留在** assembly 名单。`internal/agentd/codegraph.go` 已在 contract 冻结时退出名单（整文件 handler），本卡不再动它。
2. **四包加编译期门锁。** 每个包建 `internal/<包>/internal/<子包>/`，把**无跨域消费者、且不产生 import 环**的实现细节物理迁入；公开包只留构造、DTO、错误哨兵、消费方接口。gateway(`internal/agentd`) 与 `cmd` 生产不得 import 嵌套路径（守卫 1 已在）。
3. **配额棘轮到门面。** 六条有门面方向 `legacyBudget=0` 已随 contract 冻结；实现**不得抬任何本卡方向配额**，不得删 provider 容器 entries 造假绿（守卫 5 已锁 `legacyBudget ≤ 冻结值` 与 `interfaces`）。

**P1-A 验收口径（拍板）**：本卡**不承诺 `codegraph check` 归零**（`best.json`/`baseline.json` 禁改，`dead-entry`/`over-budget` 是 B233.13 未 absorb 的基线滞后）。验收 = 守卫五条绿 + `codegraph validate` 相对基线**零新增问题** + `codegraph check` 相对基线**不新增** fail/warn + `go build ./...` 绿 + 定向测试绿。完整图重扫/absorb 落 roadmap。

**P2-A**：`cmd/execution_surface_test.go#executionSurfaceViolations` 的函数名跨函数不重置纳入本卡（缝 3 的锁，不新开缝）。
**P3-A**：nested 只收无跨域消费者、无 import 环的细节；被 gateway/orchestration 直调的公开能力函数留公开包。
**P4-A**：`*scheduling.Service` 留公开包，`internal/agentd/scheduling_client.go:58` 断言原样。
**P5-A**：contract 头部有效基线 hash 由 `94246fc8` 更正为 `686d87abf`（纯事实更正）。

**执行顺序**（同一分支，同批提交以保 `go build` 始终绿）：

```text
W0 文档更正（contract 头部 + C-1..C-5 澄清）
  → W1 缝 1：handler 迁出 assembly 文件 + 启用 assembly 无 handle* 断言
  → W2 缝 2：四包 nested（orchestration / workspace / approval / scheduling）
  → W3 缝 3 的锁：B233.16 executionSurfaceViolations 函数名重置 + 可变红用例
  → W4 全接缝回归、图对账、文件集合门禁
```

W1 与 W3 物理独立；W2 依赖 W1 后的 gateway 编译面稳定。任何实现若需要新开顶层域、改 `best.json`/`baseline.json`、抬某方向配额、搬 `SetupAutomation` 里 `collab`/`keystone`/`hostapi` 的构造——**先停在本 task 退回协调者**，不得自行扩大范围。

---

## 2. 基线判据与本节点亲跑读数

以下命令在本轮工作树 HEAD `bad7fdb5` 亲跑，原始读数入本节（本节点未跑全量 `go test ./...`，全量归 implement W4）：

```text
go build ./...                                          → 退出码 0，无输出
go test ./internal/agentd/ -run TestB23317 -count=1     → ok github.com/Xsxdot/handoff/internal/agentd 0.006s
go test ./cmd -run TestCLIExecutionSurface -count=1     → ok github.com/Xsxdot/handoff/cmd 0.004s
go test ./internal/approval/ ./internal/scheduling/ ./internal/workspace/ -count=1
                                                         → ok approval 0.728s / ok scheduling 4.143s / ok workspace 5.766s
codegraph check --repo .                                 → fails=7：3 dead-entry（gateway→orchestration 的
    orchestration 实体 / orchestration.Manager / orchestration（包级函数））、1 dead-interface（gateway→orchestration
    OrchestrationClient）、3 over-budget（d_gateway->d_orchestration 直调 207/0、d_orchestration->d_workspace 9/0、
    d_workspace->d_orchestration 3/0）；退出码 1
codegraph check --view cards-B233.17-charter-2 --repo .  → fails=6（dead-interface 被视图补 OrchestrationClient 消除；
    3 dead-entry + 3 over-budget 仍在）；退出码 1
codegraph validate --repo .                              → 发现 122 个完整性问题、0 个失鲜节点；退出码 1（基线存量）
```

**已查证、实现必须依赖的事实（出处 file:line）**：

- 组装现状：`codegraph/target.json#assembly = ["main.go","internal/agentd/server.go","cmd/agentd.go"]`；`internal/agentd/codegraph.go` 已不在名单。
- `server.go` 定义 21 个 `Server.handle*`（`:889` `handleStatus` … `:2333` `handleEvents`），末条 `handleEvents` 的闭括号紧邻 `:2623 func (s *Server) noteTruncationDiagnosed`。
- `Handler()` 路由表在 `server.go:637`；`NewServer` `:249`；`SetManager` `:373`；`SetScheduling` `:2753`；`SetupAutomation` `:2673`——均留 assembly。
- 嵌套 `internal/<包>/internal/` 当前**不存在**（`find internal -type d -name internal` 只有顶层）。
- 公开构造：`orchestration.NewManager` `internal/orchestration/manager.go:291`；`scheduling.New` `internal/scheduling/scheduling.go:173`；`workspace.NewCapability` `internal/workspace/capability_impl.go:17`；`approval.NewClient` `internal/approval/client.go:72`。
- `internal/orchestration` 9 个生产文件反 import `internal/agentd`（`agentd.Hub`/`TerminalOutcome`），是既有方向，封界后嵌套包 import 公开 `agentd` 合法。
- `approval` 在 `best.json` 归 `d_orchestration`；生产消费方是 `internal/orchestration/approval_client.go:28` 的 `approval.NewClient`。

**Go 语言事实（已在 `/root/.handoff/tmp/456774b5/` 迷你模块亲跑，非记忆）**：

- 父包可 import 自己的 `internal/<pkg>/internal/<sub>`；子包可 import 父包；**两者同时成立即 import cycle**。实测：`internal/workspace` 的 `NewCapability()` 返回类型来自嵌套包、嵌套包又 import 公开 `workspace` 取 `Capability`/`PrepareReq` → `import cycle not allowed`（失败）。反过来，**嵌套包不 import 父包**时，父包 import 嵌套包编译通过（成功）。
- 因此封界取「**嵌套包只依赖 stdlib / 与父包无回边**、父包 import 嵌套包」的形态；凡需要父包公开类型、且被父包生产调用的细节（会形成回边）一律留公开包。
- `internal/` 可见性编译期执法：`internal/workspace/internal/...` 不被 `internal/agentd`、`cmd`、其他兄弟包 import（实测 `use of internal package ... not allowed`）。

---

## 3. 文件范围与跨 task Interfaces

### 3.1 有界文件集

生产文件（只允许）：

- 缝 1：`internal/agentd/server.go`（瘦身）、**新** `internal/agentd/handlers.go`（handler 与同段 helper/DTO 迁入）。
- 缝 2：**新** `internal/orchestration/internal/cacheplan/cacheplan.go`、**新** `internal/workspace/internal/gitproc/{runshell.go,procgroup_unix.go,procgroup_other.go}`、**新** `internal/approval/internal/decision/decision.go`、**新** `internal/scheduling/internal/logging/logging.go`；以及各自公开包的引用点 `internal/orchestration/{cachegc.go,gc.go}`、`internal/workspace/gitworkspace.go`、`internal/approval/client.go`、`internal/scheduling/status.go`。
- 缝 3：`cmd/execution_surface_test.go`、**新** `internal/agentd/b23317_assembly_test.go`（或并入现有 `b23317_boundary_test.go`）。
- 文档：`docs/superpowers/specs/b233.17-contract.md`（头部 hash + 澄清 C-1..C-5 回写）。

测试文件（只允许）：

- 缝 1：`internal/agentd/b23317_boundary_test.go`（新增 assembly 断言）。
- 缝 2：`internal/workspace/internal/gitproc/runshell_test.go`（由 `internal/workspace/runshell_test.go` 迁入，改包名）、`internal/workspace/workspace_procgroup_unix_test.go`（改 import nested）、`internal/orchestration/{cachegc_test.go,gc_test.go}`（改符号限定）、**新** `internal/approval/internal/decision/decision_test.go`、**新** `internal/scheduling/internal/logging/logging_test.go`。
- 缝 3：`cmd/execution_surface_test.go`（新增可变红用例）。

**禁止**：改 `codegraph/target.json`（已冻结）、`codegraph/best.json`、`codegraph/baseline.json`、本分支视图 diff；把整包 `internal/agentd`（pty/hub/forward/render/frames/web）、`internal/executor`、`internal/ledger`、`internal/collab`、`internal/sessions`、`d_web` 生产代码带入；`SetupAutomation` 里 `collab`/`keystone`/`hostapi` 的构造搬迁；新 HTTP/CLI/事件类型。出现集合外生产改动即退回协调者。

### 3.2 Consumes / Produces 精确签名

| task | Consumes | Produces / 约束 |
| --- | --- | --- |
| W1 | `func (s *Server) handleStatus…handleEvents`（`server.go` 889–2621 连续段，含私有 helper/DTO） | 新文件 `internal/agentd/handlers.go`，`package agentd`，函数签名逐字不变；`server.go` 只剩插线 + 非 handler helper（`noteTruncationDiagnosed`/`writeEvent`/`writeJSON`/`truncateRunes`/`Handler`/`NewServer`/`Set*`/`SetupAutomation`）。 |
| W2a | `internal/orchestration/cachegc.go` 纯函数 | `internal/orchestration/internal/cacheplan`：`func ActiveLeaf(dataDir, taskID string) string`、`func LegacyLeaf(dataDir, taskID string) string`、`func TmpRoot(dataDir string) string`、`func PathEqual(a, b string) bool`、`func ID8(taskID string) string`、`func IsTmpRoot(dataDir, path string) bool`、`func ActiveLeafOccupied(tasks []proto.Task, selfID string) bool`、`type LeafPlan struct{ TaskID, Path, Kind string; Skip bool; Note string }`、`func PlanTaskCacheLeaves(dataDir, taskID string, tasks []proto.Task) []LeafPlan`、`func SumRegularFileBytes(root string) (int64, error)`。只 import stdlib + `proto` + `executor`，**不 import orchestration**。 |
| W2b | `internal/workspace/runshell.go`、`workspace_procgroup_{unix,other}.go` | `internal/workspace/internal/gitproc`：`func RunShell() (string, error)`、`func ResolveRunShell(goos string, lookPath func(string) (string, error), stat func(string) error) (string, error)`、`func RunShellCandidates(goos string) []string`、`func SetProcGroup(cmd *exec.Cmd)`、`var KillProcGroup = func(pid int){}`（unix 实现按组 `SIGKILL`，other 空操作）。只 import stdlib。 |
| W2c | `internal/approval/client.go` 纯裁决映射 | `internal/approval/internal/decision`：`func GateDecision(answer string) (decision, reason string)`、`func MapGateAnswer(ans string) executor.ApprovalDecision`、`func PermEventText(s string) string`。只 import stdlib + `executor`，**不 import approval**。 |
| W2d | `internal/scheduling/status.go` 日志入口 | `internal/scheduling/internal/logging`：`func StatusLog() *slog.Logger`（等价 `slog.Default().With("mod","scheduling")`）。只 import stdlib。 |
| W3 | `cmd/execution_surface_test.go#executionSurfaceViolations` | 修复后：包级 `var/const/type` 声明行重置函数名上下文，白名单工厂函数之后的包级 `*client.Client`/`client.New(` 必须判红。 |

---

## 4. W0：contract 文档更正（P1-A/P5-A/C-1..C-5）

2–5 分钟，零代码。

1. **基线判据**：`grep -n "94246fc8" docs/superpowers/specs/b233.17-contract.md` → 命中头部 `:8`。期望存在。
2. **改头部 hash**：把 `docs/superpowers/specs/b233.17-contract.md:8` 的 `@ 94246fc8` 改为 `@ 686d87abf`（P5-A；`cmd/agentd.go` 于 `686d87ab` 才进 assembly，`94246fc8` 的 target 不含它）。
3. **追加边界澄清**：在 contract §2/§6 附近追加一节「B233.17 边界澄清（拍板回写）」，逐条落：
   - C-1：`codegraph check` 收敛判据改为「相对基线不新增 fail/warn」；`over-budget`/`dead-entry` 归零主证据是完整图重扫或 absorb B233.13，落 `docs/roadmap.md`（若 roadmap 已记则不重复）。
   - C-2：缝 3 的守卫锁包含修 `cmd/execution_surface_test.go#executionSurfaceViolations` 的函数名重置，并配「白名单函数后包级聚合必须红」的可变红用例。
   - C-3：nested 收**无跨域消费者且无 import 环**的实现细节；被 gateway/orchestration 直接消费的公开能力函数与公开契约类型留公开包；门锁是「gateway 生产不得 import 嵌套路径」，不是「不得调用 workspace 公开函数」。
   - C-4：头部 hash 更正为 `686d87abf`（同第 2 步）。
   - C-5：`scheduling.Service` 留公开包，`internal/agentd/scheduling_client.go:58` 断言原样；nested 只收无跨域消费者的细节。
4. **验证**：`go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . resolve --doc docs/superpowers/specs/b233.17-contract.md` → 退出码 0。

---

## 5. W1：缝 1 —— handler 迁出 assembly 文件

### 5.1 目标文件与边界

- 改：`internal/agentd/server.go`；新建 `internal/agentd/handlers.go`；测试 `internal/agentd/b23317_boundary_test.go`。
- 不改 `Handler()` 的 URL→函数名登记（留在 `server.go` 是插线）、`NewServer`/`SetManager`/`SetScheduling`/`SetupAutomation`、`codegraph.go`、`target.json`。

### 5.2 红绿步骤（锁定断言：assembly 文件无 `Server.handle*`）

1. **基线判据（2 分钟）**：`go test ./internal/agentd/ -run TestB23317 -count=1` → `ok`；现有 `TestB23317HandlerGuardHasTeeth` 只证明工具哨兵有牙，**未**对 assembly 文件做实质断言——本步新增的断言在基线必红，见第 2 步。
2. **先写失败断言（2–5 分钟）**：在 `internal/agentd/b23317_boundary_test.go` 末尾追加：

   ```go
   // TestB23317AssemblyFilesHaveNoHandlerDefs 是缝 1 的实质断言：assembly 名单里的
   // 任何 .go 文件都不得定义 Server.handle*（免检只留给开机插线）。handler 搬迁前
   // server.go 含 21 个 handle*，本测试必红。
   func TestB23317AssemblyFilesHaveNoHandlerDefs(t *testing.T) {
   	repo, assembly := b23317AssemblyFiles(t)
   	for _, rel := range assembly {
   		body, err := os.ReadFile(filepath.Join(repo, rel))
   		if err != nil {
   			t.Fatalf("读 assembly 文件 %s: %v", rel, err)
   		}
   		if got := b23317HandlerDefsIn(body); len(got) != 0 {
   			t.Fatalf("assembly 文件 %s 不得定义 Server.handle*（缝 1）：%v", rel, got)
   		}
   	}
   }
   ```

3. **跑红（2–5 分钟）**：`go test ./internal/agentd/ -run '^TestB23317AssemblyFilesHaveNoHandlerDefs$' -count=1` → 预期 `FAIL`，报文含 `internal/agentd/server.go 不得定义 Server.handle*` 且列出 21 个 `Server.handle*`。不得出现 `no tests to run`。
4. **搬迁（3–5 分钟）**：新建 `internal/agentd/handlers.go`，`package agentd`，把 `server.go` 中从 `func (s *Server) handleStatus` 起、到 `handleEvents` 闭括号为止的连续段整体剪到新文件。该段内的顶层声明（顺序不动）为：
   `handleStatus`、`handleFootprint`、`handleReclaimList`、`handleReclaim`、`writeReclaimError`、`handleListTasks`、`handleGetTask`、`type taskDetail`、`type replyRequest`、`type replyResult`、`handleReply`、`resumeIfIdle`、`type dispatchRequest`、`handleDispatch`、`writeDispatchError`、`type projectAddRequest`、`handleProjectAdd`、`handleProjectList`、`handleProjectRemove`、`writeProjectError`、`type continueRequest`、`handleContinue`、`type doneRequest`、`type doneResult`、`handleDone`、`handleStop`、`releaseTaskCarrierOccupancy`、`parseForce`、`handleResume`、`taskOrErr`、`taskRepoOrErr`、`handleTaskDiff`、`handleTaskBundle`、`handleTaskBranches`、`handleTaskFile`、`type runRequest`、`type runResponse`、`handleTaskRun`、`writeManagerError`、`handleEvents`。
   **不搬**：`noteTruncationDiagnosed`、`writeEvent`、`writeJSON`、`truncateRunes`（留在 `server.go`，同包互引）。
   函数体、注释、字面值逐字不动（结构搬迁，零行为变化）。
5. **修 import（2–5 分钟）**：`handlers.go` 起始 import 集（按需增删，`gofmt` 后以 `go build` 为准）：

   ```go
   import (
   	"context"
   	"encoding/json"
   	"errors"
   	"fmt"
   	"io"
   	"net/http"
   	"os"
   	"sort"
   	"strconv"
   	"strings"
   	"time"

   	"github.com/Xsxdot/handoff/internal/client"
   	"github.com/Xsxdot/handoff/internal/executor"
   	"github.com/Xsxdot/handoff/internal/ledger"
   	"github.com/Xsxdot/handoff/internal/proto"
   	"github.com/Xsxdot/handoff/internal/scheduling"
   	"github.com/Xsxdot/handoff/internal/store"
   	"github.com/Xsxdot/handoff/internal/workspace"
   	"github.com/coder/websocket"
   )
   ```

   运行 `go build ./internal/agentd`；按编译器报错删除 `handlers.go` 未用 import、给 `server.go` 删除已随迁的未用 import（`go build` 会逐条点名）。`gofmt -w internal/agentd/handlers.go internal/agentd/server.go`。
6. **加注释（2 分钟）**：`handlers.go` 文件头写职责与边界：

   ```go
   // handlers.go —— agentd HTTP/WS 处理请求的实现（B233.17 从 server.go 迁出）。
   //
   // 职责：实现 /api 与 /ws 的处理函数与其私有 DTO/helper。
   // 边界：**不在 codegraph assembly 免检名单**——请求处理不享受开机插线豁免，
   // 对后厨的跨域直调会被 codegraph check 看见。URL→函数名登记（Handler()）、
   // 构造与 Set* 注入仍在 server.go（插线，名单内）。
   package agentd
   ```

7. **跑绿（2–5 分钟）**：`go test ./internal/agentd/ -run TestB23317 -count=1` → `ok`，且 `TestB23317AssemblyFilesHaveNoHandlerDefs` 从红转绿。再跑 `go build ./...` → 退出码 0。
8. **变异证据（2 分钟，可选但建议）**：临时在 `internal/agentd/server.go` 末尾加 `func (s *Server) handleZZProbe(w http.ResponseWriter, r *http.Request) {}`，跑 `go test ./internal/agentd/ -run '^TestB23317AssemblyFilesHaveNoHandlerDefs$' -count=1` 必须红；随即删除该函数，`git diff` 不得残留。**此 probe 只在 implement 变异验证时存在，验证后立即删除。**

### 5.3 测试范围声明

只跑 `./internal/agentd` 与本 task 断言；不跑全量。

### 5.4 可判定验收

- `b23317HandlerDefsIn` 对 `assembly` 三个文件均返回 0。
- `Handler()` 仍注册全部路由；`go build ./...`=0；既有 `internal/agentd` 测试不因搬迁变红。

---

## 6. W2：缝 2 —— 四包编译期封界

### 6.1 设计原则（先读，决定每个文件能不能搬）

- 嵌套包**只依赖 stdlib 或与父包无回边的外部包**；一旦嵌套包 import 父公开包、父包又 import 嵌套包，就是 import cycle（`go build` 立即失败）。
- 被 gateway/orchestration **直接消费**的公开符号（如 `workspace.PrepareWorkspace`/`MarkIgnored`/`ID8`、`scheduling.OccupancyCarrierKey`/`SquadRow`、`approval.NewClient`/`Hooks`）**留公开包**（P3-A/P4-A）。
- 公开构造（`NewCapability`/`New`/`NewClient`/`NewManager`）留公开包；嵌套不得成为 `cmd`/agentd 的 import 目标（守卫 1 + Go internal 规则双重执法）。

### 6.2 W2a：orchestration → `internal/orchestration/internal/cacheplan`

1. **基线判据（2 分钟）**：`go test ./internal/orchestration/ -run 'TestCache|TestPlanTaskCache|TestSumRegular|TestActiveLeaf|TestGC' -count=1` 记录现状（若未命中则跑 `go test ./internal/orchestration/ -count=1`）。
2. **新建嵌套包**：`internal/orchestration/internal/cacheplan/cacheplan.go`，`package cacheplan`，import `errors`、`io/fs`、`path/filepath`、`internal/executor`、`internal/proto`。把 `internal/orchestration/cachegc.go` 的以下函数**整体迁移并把首字母大写**（体逐字不动）：`cacheActiveLeaf`→`ActiveLeaf`、`cacheLegacyLeaf`→`LegacyLeaf`、`cacheTmpRoot`→`TmpRoot`、`cachePathEqual`→`PathEqual`、`cacheID8`→`ID8`、`isCacheTmpRoot`→`IsTmpRoot`、`activeLeafOccupied`→`ActiveLeafOccupied`、`type cacheLeafPlan`→`type LeafPlan`（字段导出化：`TaskID/Path/Kind string`、`Skip bool`、`Note string`）、`planTaskCacheLeaves`→`PlanTaskCacheLeaves`、`sumRegularFileBytes`→`SumRegularFileBytes`。文件头注释写职责（任务私有缓存叶子路径/短号占用/tmp 根保护/收口删除的纯逻辑）与边界（无 Manager、不写状态）。
3. **改公开调用点**：
   - `internal/orchestration/cachegc.go`：保留 `removeCacheLeaf`、`purgeTaskCache` 两个 Manager 方法；`purgeTaskCache` 中 `planTaskCacheLeaves(...)`→`cacheplan.PlanTaskCacheLeaves(...)`、`isCacheTmpRoot(...)`→`cacheplan.IsTmpRoot(...)`；删除已迁的纯函数；新增 import `.../internal/orchestration/internal/cacheplan`。
   - `internal/orchestration/gc.go:78/85/97`：`planTaskCacheLeaves`→`cacheplan.PlanTaskCacheLeaves`、`isCacheTmpRoot`→`cacheplan.IsTmpRoot`、`sumRegularFileBytes`→`cacheplan.SumRegularFileBytes`。
4. **改测试**：`internal/orchestration/cachegc_test.go`（`package orchestration`）把对 `cacheID8`/`cacheActiveLeaf`/`cacheLegacyLeaf`/`cacheTmpRoot`/`isCacheTmpRoot`/`planTaskCacheLeaves`/`activeLeafOccupied`/`sumRegularFileBytes` 的调用加 `cacheplan.` 限定（`cacheLeafPlan` 若出现改 `cacheplan.LeafPlan`），并 import nested；`gc_test.go:152` 同改 `cacheplan.SumRegularFileBytes`。
5. **编译与跑绿（2–5 分钟）**：`go build ./...`=0；`go test ./internal/orchestration/ -count=1`=ok。

### 6.3 W2b：workspace → `internal/workspace/internal/gitproc`

1. **基线判据（2 分钟）**：`go test ./internal/workspace/ -run 'TestResolveRunShell|TestRunShellCandidates|TestRunCmd' -count=1` → ok。
2. **新建三个文件**：
   - `internal/workspace/internal/gitproc/runshell.go`：`package gitproc`；从 `internal/workspace/runshell.go` 迁入 `runShellCandidates`→`RunShellCandidates`、`dedupStrings`（保持小写）、`resolveRunShell`→`ResolveRunShell`、`statFile`（小写）、`runShell`→`RunShell`。import `fmt`/`log/slog`/`os`/`os/exec`/`path/filepath`/`runtime`。**注意**：原 `resolveRunShell` 内的 `log().Info`/`log().Error` 在嵌套包里没有 `log()`，逐处改为 `slog.Default()`（语义与 workspace 的 `log()` 完全一致）。文件头写明这是 `RunCmd` 的 shell 解析细节、无跨域消费者。
   - `internal/workspace/internal/gitproc/procgroup_unix.go`：`//go:build unix`，`package gitproc`；从 `workspace_procgroup_unix.go` 迁入 `setProcGroup`→`SetProcGroup`、`var killProcGroup`→`var KillProcGroup`（体不变，按组 `SIGKILL`）。
   - `internal/workspace/internal/gitproc/procgroup_other.go`：`//go:build !unix`，`package gitproc`；`SetProcGroup` 空操作、`var KillProcGroup = func(pid int) {}`。
3. **改公开调用点** `internal/workspace/gitworkspace.go`：`runShell()`（RunCmd 内）→`gitproc.RunShell()`；`setProcGroup(cmd)`→`gitproc.SetProcGroup(cmd)`；`killProcGroup(cmd.Process.Pid)`→`gitproc.KillProcGroup(cmd.Process.Pid)`；新增 import `.../internal/workspace/internal/gitproc`。删除 `internal/workspace/runshell.go` 与两个 `workspace_procgroup_*.go`。`openNonBlock` 两文件**不动**（保留公开包）。
4. **迁测试**：把 `internal/workspace/runshell_test.go` 移到 `internal/workspace/internal/gitproc/runshell_test.go`，改 `package gitproc`，调用名改 `ResolveRunShell`/`RunShellCandidates`。
5. **改测试** `internal/workspace/workspace_procgroup_unix_test.go`：保持 `package workspace`（仍要 `RunCmd`/`initGitRepo`），把 `origKill := killProcGroup` / `killProcGroup = ...` 改为 `origKill := gitproc.KillProcGroup` / `gitproc.KillProcGroup = ...`，import nested。
6. **编译与跑绿**：`go build ./...`=0；`go test ./internal/workspace/ -count=1`=ok；另跑 `GOOS=windows go build ./internal/workspace/...`=0 与 `GOOS=darwin go build ./internal/workspace/...`=0（验 build tag 文件齐）。

### 6.4 W2c：approval → `internal/approval/internal/decision`

1. **基线判据（2 分钟）**：`go test ./internal/approval/ -count=1` → ok。
2. **新建** `internal/approval/internal/decision/decision.go`，`package decision`，import `strings` + `internal/executor`。从 `internal/approval/client.go` 迁入并导出：
   - `gateDecision`→`func GateDecision(answer string) (decision, reason string)`（体逐字，保留「只有精确 `allow` 放行一次」的注释）。
   - `mapGateAnswer`→`func MapGateAnswer(ans string) executor.ApprovalDecision`（体逐字）。
   - `permEventText`→`func PermEventText(s string) string`（体逐字，用 `executor.TruncationMarker`；常量 `200` 随迁）。
   文件头写职责（approval 的裁决映射/文案截断纯逻辑）与边界（不 import approval、不碰 Store/Hub）。
3. **改公开调用点** `internal/approval/client.go`：`gateDecision(`→`decision.GateDecision(`、`mapGateAnswer(`→`decision.MapGateAnswer(`、`permEventText(`→`decision.PermEventText(`；删除已迁函数；新增 import nested。`ConsultDecision`/`Hooks`/`AnswerHub`/`Client`/`NewClient` 与 payload 结构体**留公开包**。
4. **加测试**：新建 `internal/approval/internal/decision/decision_test.go`（`package decision`），至少断言：`GateDecision("allow")=="once"`、`GateDecision("deny: 原因")` 原因透传、其余 answer 拒绝；`MapGateAnswer("allow").Status==executor.ApprovalAllow`、非 allow 为 Deny；`PermEventText` 超 200 字符截断且带 `executor.TruncationMarker`。
5. **编译与跑绿**：`go build ./...`=0；`go test ./internal/approval/ -count=1`=ok（外部 `approval_test` 只用公开面，不受影响）。

### 6.5 W2d：scheduling → `internal/scheduling/internal/logging`

1. **基线判据（2 分钟）**：`go test ./internal/scheduling/ -count=1` → ok。
2. **新建** `internal/scheduling/internal/logging/logging.go`，`package logging`，import `log/slog`：
   ```go
   // StatusLog 返回编制域结构化 logger（mod=scheduling）。包级日志入口的实现细节，
   // 无跨域消费者；公开 scheduling 包经 statusLog() 转发，保持调用点不变。
   func StatusLog() *slog.Logger { return slog.Default().With("mod", "scheduling") }
   ```
   配 `logging_test.go` 断言：`StatusLog()` 非 nil，且对同一次调用返回的 logger 调 `Info("probe")` 不 panic；再用一个自定义 `slog.Handler` 经 `slog.New` 无法读取 `slog.Default()` 的属性，故属性断言改为**源码级**：断言 `internal/scheduling/internal/logging/logging.go` 文本含 `"mod", "scheduling"`（保持与既有 `statusLog` 逐字一致）；行为断言只要求非 nil + 可 Info。
3. **改公开 `internal/scheduling/status.go`**：删除原 `func statusLog()` 体，改为转发并 import nested：
   ```go
   func statusLog() *slog.Logger { return logging.StatusLog() }
   ```
   其余调用点（`scheduling.go` 60+ 处 `statusLog()`）**一字不改**。
4. **编译与跑绿**：`go build ./...`=0；`go test ./internal/scheduling/ -count=1`=ok。
5. **说明（写进本 task 提交信息/注释）**：`scheduling` 的公开读模型（`SquadRow`/`CarrierRow`/`QueuedRequest`）、纯函数（`Occupancy*`、`ResolveLookup`/`BindPhysical`）、`*Service` 均被 gateway 直接消费或会产生 import 回边，按 P3-A/P4-A 留公开包；nested 只收日志入口这一无跨域消费者的细节。**不得为了凑目录而搬公开能力函数。**

### 6.6 W2 变异证据（缝 2 有牙，不新建仓内 probe 文件）

守卫 1/2 已在 contract Ticket 0 落码：`TestB23317GatewayProductionDoesNotImportNestedInternals`（扫 agentd+cmd 生产 `.go`）+ `TestB23317NestedInternalGuardHasTeeth`（人为嵌套 import 判红）。W2 完成后跑：

```text
go test ./internal/agentd/ -run 'TestB23317(GatewayProductionDoesNotImportNestedInternals|NestedInternalGuardHasTeeth)' -count=1  → ok
go build ./...  → 退出码 0
find internal -type d -name internal  → 含 orchestration/workspace/approval/scheduling 四个 nested 目录
```

Go 的 `internal/` 可见性本身是编译期锁（已亲跑验证），无需在仓内插 probe。

---

## 7. W3：缝 3 的锁 —— B233.16 `executionSurfaceViolations` 函数名重置

### 7.1 目标文件与边界

- 改：`cmd/execution_surface_test.go`。只修测试守卫的解析 bug，不改 `executionSurfaceFiles`/`executionFactorySymbols`/`stripCommentsAndStrings`。

### 7.2 红绿步骤（锁定断言：白名单函数后包级聚合必须红）

1. **基线判据（2 分钟）**：`go test ./cmd -run TestCLIExecutionSurface -count=1` → ok（bug 是「应红却放行」，故基线绿）。
2. **先写可变红用例（2–5 分钟）**：在 `TestCLIExecutionSurfaceGuardHasTeeth` 末尾追加：

   ```go
   // 包级聚合写在白名单工厂函数之后：函数名上下文不得沿用到包级声明（B233.16 漏判）。
   leakAfterFactory := []byte("package cmd\n\nfunc targetClient(t string) (*client.Client, func(), error) {\n" +
   	"\treturn client.New(\"a\", \"b\"), func() {}, nil\n}\n\nvar leak *client.Client\n")
   if got := executionSurfaceViolations(stripCommentsAndStrings(leakAfterFactory)); len(got) == 0 {
   	t.Fatal("白名单工厂函数之后的包级 *client.Client 必须判红（函数名上下文不得沿用）")
   }
   ```

3. **跑红（2–5 分钟）**：`go test ./cmd -run '^TestCLIExecutionSurfaceGuardHasTeeth$' -count=1` → 预期 `FAIL`，报文含「白名单工厂函数之后的包级 *client.Client 必须判红」。不得 `no tests to run`。
4. **修 `executionSurfaceViolations`（2–5 分钟）**：把函数体替换为：

   ```go
   // executionSurfaceViolations 返回非白名单符号里的聚合用法（行号:符号）。
   // cleaned 必须已经过 stripCommentsAndStrings。
   //
   // 函数名上下文只对「包裹该行的方法/函数」有效：遇到包级 var/const/type 声明
   // 必须重置，否则白名单工厂函数之后紧跟的包级聚合会沿用它而被误放行（B233.16 漏判）。
   func executionSurfaceViolations(cleaned []byte) []string {
   	funcRe := regexp.MustCompile(`^func\s+(?:\([^)]*\)\s+)?([A-Za-z_]\w*)`)
   	declRe := regexp.MustCompile(`^(var|const|type)\b`)
   	var out []string
   	current := ""
   	for i, line := range strings.Split(string(cleaned), "\n") {
   		trimmed := strings.TrimLeft(line, " \t")
   		if m := funcRe.FindStringSubmatch(trimmed); m != nil {
   			current = m[1]
   		} else if declRe.MatchString(line) {
   			// 包级声明（列 0）开始：此后的聚合不属于上一个函数。
   			current = ""
   		}
   		if !strings.Contains(line, "*client.Client") && !strings.Contains(line, "client.New(") {
   			continue
   		}
   		if executionFactorySymbols[current] {
   			continue
   		}
   		sym := current
   		if sym == "" {
   			sym = "<package>"
   		}
   		out = append(out, fmt.Sprintf("%d:%s", i+1, sym))
   	}
   	return out
   }
   ```

   说明：`stripCommentsAndStrings` 保留包级声明行的列 0 形态，故 `declRe.MatchString(line)` 只命中包级 `var/const/type`（函数体内的同名声明有缩进，不误伤）。工厂函数内的 `client.New(` 仍由 `current` 命中白名单放行。
5. **跑绿（2–5 分钟）**：`go test ./cmd -run 'TestCLIExecutionSurface' -count=1` → `ok`，且新用例从红转绿；既有 `TestCLIExecutionSurfaceHoldsNarrowDependencies` 仍绿。
6. **加注释**：上面的函数头注释即第 4 步的一部分。

### 7.3 测试范围声明

只跑 `./cmd` 与本 task 断言。

---

## 8. W4：全接缝回归与交接门禁

W4 只在 W0–W3 全绿后执行，不新增生产路径。

### 8.1 回归命令与判据

```text
go build ./...                                                    → 退出码 0
go test ./internal/agentd/ ./internal/orchestration/ ./internal/approval/ ./internal/scheduling/ ./internal/workspace/ ./cmd -count=1  → 全部 ok
go test ./internal/agentd/ -run TestB23317 -count=1               → ok（含新增 assembly 断言）
go test ./cmd -run TestCLIExecutionSurface -count=1               → ok（含新增可变红用例）
go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate
                                                                  → issues == 122（基线值），无新增
go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . check
                                                                  → fails ≤ 7，且不新增 fail 种类/条目（C-1）
go run github.com/Xsxdot/charter/graph/cmd/codegraph --repo . validate --view cards-B233.17-charter-2
                                                                  → issues == 122，无新增
git diff --check                                                  → 退出码 0
git status --short --branch                                       → 仅第 3.1 节列出的文件
git diff --name-only                                              → 不含 codegraph/best.json、codegraph/baseline.json、target.json、视图 diff
```

判定规则：

- 定向测试出现任何新红 → fail。
- `validate` 相对基线**新增任何问题** → fail；`check` 新增任何 fail 条目（种类或计数）→ fail。若 `check` 因嵌套包新增图节点产生新条目，原文记录并停回协调者裁决（不得改 `best.json`/`baseline.json`/`target.json` 抹平）。
- `go test ./...` 全量跑（implement 执行）时若只出现 roadmap 已记的基线存量红（如 `TestRepoContractGate` 的 `dead-contract d_execution→d_orchestration`/`dead-interface ApprovalClient`），原文记录为基线遗留；出现其他失败即 fail。

### 8.2 接缝覆盖双向核对（对照 spec §契约语义与接缝）

| 声明缝 | 至少一支入口测试 | 断言 |
| --- | --- | --- |
| 缝 1 免检名单不再覆盖 handler 定义 | W1 新增 `TestB23317AssemblyFilesHaveNoHandlerDefs`（入口：读 `target.json#assembly` + 源码） | assembly 三文件 `b23317HandlerDefsIn==0`；变异把 `handle*` 搬回 assembly 文件 → 红（现有 `TestB23317HandlerGuardHasTeeth` 锁工具，`TestB23317AssemblyFreezeExcludesHandlerFile` 锁 codegraph.go 不在名单）。 |
| 缝 2 四包里间对 gateway 生产不可见 | W2 后的 `TestB23317GatewayProductionDoesNotImportNestedInternals` + `TestB23317NestedInternalGuardHasTeeth`（入口：扫 agentd/cmd 生产 `.go`） | 生产不 import 四包嵌套；人为嵌套 import 判红；`go build ./...`=0。 |
| 缝 3 有门面方向直闯配额为 0（含 B233.16 守卫漏判） | `TestB23317FacadeBudgetsStayRatchet`（入口：读 `target.json#contracts`）+ W3 `TestCLIExecutionSurfaceGuardHasTeeth`（入口：`executionSurfaceViolations`） | 六方向 `legacyBudget ≤ 冻结值` 且 `interfaces` 含 `OrchestrationClient`；白名单函数后包级聚合判红（变异）。 |

**内部锁声明**：W2c `decision_test.go`、W2d `logging_test.go`、W2b 的 `runshell_test.go` 是包内白盒测试，**属内部锁、只补充、不顶替缝级断言**。理由：这些嵌套包无对外契约（正是「无跨域消费者」的细节），从声明缝（agentd/cmd 生产 import 扫描）构造不出对「纯裁决映射逐分支」「平台 shell 候选顺序」的断言——缝级守卫只断言 import 面与预算，无法进入纯函数分支。故内部锁附加、缝级断言由主表三支承担。

**退路同闸**：本节无会改变测试入口符号的条件退路；W1–W3 均从声明缝入口（`target.json` 读取、源码扫描、`executionSurfaceViolations`）断言。

### 8.3 序列化边界

本卡**无新增数据字段/DTO/手写投影**：`proto`/`client.DispatchOpts`/HTTP DTO 形状与 JSON 键一律未动；`approval` 的 payload 结构体留公开包且字段不变。故无 roundtrip 属性测试需求。实现不得删除或改弱既有 `internal/client/frozen_dispatch_test.go`、`internal/ledgerstep` 真实 HTTP 链路用例。

### 8.4 缺陷族对抗审查（逐族结论）

- **生命周期/状态机中断**：无，因为只搬文件与目录，不改启动时序、任务状态机、watchdog/reconcile；`Handler()` 留名单，运行中请求分派不变。
- **静默失败/误导报错**：无新增，因为不改错误传播；唯一新增可观察失败是编译期（import 嵌套 → `go build` 失败），信号明确。需防的是守卫静默放行：W1 assembly 断言 + W3 漏判修复各自有可变红用例。
- **跨平台假设**：W2b 保留 `//go:build unix/!unix` 构建约束随文件搬迁；以 `GOOS=windows/darwin go build ./internal/workspace/...` 机内验证，真机运行归真机清单。
- **假红/假绿测试**：①嵌套目录「建了但没搬进实现」是假绿——W2 每个 nested 包都有实际迁移的函数 + 编译引用；②W1 若只留哨兵不启实质断言是假绿——W1 新增 assembly 实质断言；③B233.16 漏判是已证假绿——W3 修复 + 可变红用例。
- **门禁绕过**：W1 若把 handler 误搬回 assembly 文件，守卫必红；`target.json` 是仓内静态文件，无 TOCTOU。
- **枚举新值过既有白名单**：无，因为不新增 `TaskState`/`EventType`/`kind`/CLI 子命令/HTTP 路径。
- **webview/平台表现差异**：无，因为不改 `d_web`/浏览器 API/webview 渲染。

---

## 9. 用户故事与冻结条目归属

| 用户故事 / 冻结条目 | task |
| --- | --- |
| 故事 1：handler 不在免检文件，开机插线仍在免检文件，agentd 仍能启动注入 | W1（`TestB23317AssemblyFilesHaveNoHandlerDefs` + `go build`） |
| 故事 2：gateway 生产 import 里间路径 `go build` 失败；`cmd` 仍从公开包 `New` 再注入 | W2（守卫 1/2 + `go build`；`cmd/agentd.go:226` `NewManager`、`:490` `New` 不变） |
| 故事 3：`d_gateway→d_orchestration` 配额 0；变异加直调 check 红 | W0（contract 口径）+ W4（`TestB23317FacadeBudgetsStayRatchet`）；真实图收敛归 roadmap（P1-A） |
| 故事 4：CLI/Web 行为文案不变 | W4 定向回归；真机清单标未验证 |
| 故事 5：父卡四句收后三句 | W1/W2/W3 覆盖；父卡本身不标完成 |
| 冻结 #1/#2（assembly 名单） | W1（名单已冻，不动） |
| 冻结 #7/#8（生产不 import 嵌套、哨兵有牙） | W2 |
| 冻结 #9/#10/#11（四包 nested、`cmd` 只公开 `New`、断言可编译） | W2a–W2d |
| 冻结 #12（不新增域/协议/路径/命令/事件） | W1–W4 |
| 冻结 #13–#18（六方向 budget/entries/interfaces） | 已随 contract 冻结，W4 守卫 5 复核，实现不动 |
| 冻结 #19/#20/#21/#22（只降不抬、复核 note、`best.json` 不改） | W4 门禁 |
| 冻结 #23/#24（check 口径，P1-A） | W0 C-1 + W4 |
| 冻结 #25/#26（HTTP 路径/字面值不变） | W1（函数体逐字搬）+ W4 |
| 冻结 #27（行为回归） | W4 |
| 冻结 #28/#29/#30（Ticket 0 守卫、视图、build） | W4 复核 |
| spec §实现决定「B233.16 守卫漏判随守卫收口修」 | W3 |

---

## 10. 真机清单（未验证，需协调者/实现者在真实宿主执行）

本卡为结构重排，不新增跨机/executor 行为；以下机内只验契约形状：

1. 真实 `handoff dispatch/reply/continue/done/stop/wait`、工作区文件、审批免审回传各跑一次，stdout、退出码、HTTP 路径逐字不变。
2. 平台矩阵：Linux/macOS/Windows 真机运行命令行为；跨平台**编译**在 W4 机内验，真机运行归此。
3. 四包 nested 后 agentd 启动：编排/编制/工作区能力可注入，卡步生命周期与今日一致。
4. 真实 executor 会话（B233.12 残余）行为不变。
5. 完整图真重扫 / absorb B233.13（本卡 OOS，P1-A 的收敛正主）：合线后重扫或重写本卡视图再 absorb，使 `codegraph check` 的 `over-budget`/`dead-entry` 真实归零；**不造假基线**。

---

## 11. 计划自审（三查）

- **spec 覆盖**：spec 三句逐条指到 W1（handler 退出免检）、W2（四包门锁）、W3（守卫漏判锁）；配额棘轮已随 contract 冻结、由 W4 守卫 5 复核；用户故事 §9 逐条落 task。无落空故事。
- **占位符扫描**：本计划不含 TBD/「同 Task N」/「加适当处理」；每个 task 给出精确文件路径、可复制代码块或逐条迁移清单与断言。W2 测试复用既有 harness 处（`bin`? 无）已逐条列迁移或新写；无骨架测试。**无自我声明的例外需要**（所有测试代码/断言均给出或逐条列出）。
- **跨 task 类型/签名一致性**：W1 只改文件位置，签名不变；W2a–W2d 的 Produces 签名与公开调用点逐字对齐（`cacheplan.*`、`gitproc.*`、`decision.*`、`logging.*`）；W3 只改测试私有函数。无跨 task 参数别名差异。
- **上下文预算**：W1 圈 `internal/agentd/{server.go,handlers.go,b23317_boundary_test.go}`；W2a 圈 orchestration 3 文件 +1 新包；W2b 圈 workspace 3 文件 +1 新包；W2c 圈 approval 2 文件；W2d 圈 scheduling 2 文件；W3 圈 cmd 1 文件。均有界，无竖切卡。
- **类型标注**：`d_gateway`=boundary（`go build`+源码守卫+行为回归机内闭环）；`d_orchestration`/`d_scheduling`/`d_cli`=logic（`go build`+编译断言）；`d_workspace`=boundary（`go build`+既有 HTTP 级测试）；平台运行/真实宿主列 §10「未验证，需真机」。
- **接缝双向**：§8.2 主表每条缝都有入口测试；内部锁逐条声明理由且不顶替缝级断言。
- **派发自审**：本计划无驱动 handoff/派发系统的验收步骤；不派发、不调 handoff CLI、不起新 executor。
- **本节点边界**：本文件只写计划，不搬 handler、不建 nested、不改 `target.json`/`best.json`/`baseline.json`、不抬配额。
