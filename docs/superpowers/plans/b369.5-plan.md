# B369.5（S5）CLI `console --qr` 配对载体 —— 实现计划

- **卡号**：B369.5（父卡 B369；拆解序号 S5）
- **有效基线分支**：`cards/B369-charter-4`（本卡合并目标）
- **冻结 contract**：`docs/superpowers/specs/b369-contract.md` @ `6f10ba7c`
- **已拍板拆解**：`docs/superpowers/specs/b369-breakdown.md` @ `4ab6415a`（P1–P6 全案 A）
- **上游 spec**：`docs/superpowers/specs/2026-09-13-mobile-app-design.md`（状态：已批准）
- **图视图**：`codegraph/diffs/cards-B369-charter.json`（view=`cards/B369-charter`，base `977720e2`）
- **本节点产出物**：`docs/superpowers/plans/b369.5-plan.md`（计划文档；不写实现代码）
- **本节点台账**：`docs/superpowers/ledgers/2026-09-19-b369.5-plan-ledger.md`（边干边追加，随本批提交）

---

## 0. 本卡干什么（一段话）

给 CLI 加一条生成移动端配对载体的命令：`handoff console --qr`（终端二维码）/ `--bundle`（一行粘贴串）。
它读本机配置里的各 target 登记，逐机用该机主令牌 `IssueAuthTicket` 代领一张一次性 ticket，
组装 `proto.PairBundle`，调 `proto.EncodePairBundle` 编码，再把载荷渲染成二维码或直接打印。
**不在 CLI 重实现 bundle 编解码**（contract §8.3 原文），一切走 `proto.EncodePairBundle`；
某机 ticket 代领失败只标为无 ticket（部分 bundle，contract §3.1「离线机可缺」），不整单失败。

**为什么必须在这里做**：spec 用户故事 1「用户在 CLI 执行配对命令生成配对载体（QR），手机 App 扫码完成配对」
是本期唯一没有载体的承诺；breakdown §4 行为闭环表第一行把它指到 S5。现状 `--qr` 仅存在于
`docs/superpowers/plans/2026-08-11-agentd-browser-auth.md:56`（设计未实现），仓内零 QR 实现、零 QR 依赖、
`cmd/console.go` 无 `--qr`（flag 仅 print-url/device/no-open，`cmd/console.go:75-80`）。

**边界**：

- 不改 `internal/proto/pairing.go`（契约冻结的 wire 形状与签名）。若实现中发现 wire 缺陷，**停下提问**，不自行改冻结物。
- 不改 `internal/client`、`internal/agentd`、`mobile/`、`web/`（本卡不碰协议与运行时）。
- 管 stdout 契约：`--print-url` 仍恰好一行 URL（桌面壳接线，`cmd/console_test.go:14`）；`--qr`/`--bundle` 是新增配对面。
- 凭据卫生：token/ticket 只进 stdout 的载荷，**绝不落日志**（contract §8 凭据卫生）。

---

## 1. 基线事实（本轮实测，动手前已复核）

| 事实 | 原始读数 |
|---|---|
| Go 版本 | `go version go1.26.1 linux/amd64` |
| 基线 `cmd` 测试 | `go test ./cmd/ -run TestConsole -count=1` → `ok github.com/Xsxdot/handoff/cmd 0.008s`（EXIT=0） |
| 根模块 build | `go build ./...` → EXIT=0 |
| Windows 交叉编译 | `GOOS=windows GOARCH=amd64 go build ./...` → EXIT=0 |
| `mobile/` 模块 build | `cd mobile && go build ./...` → EXIT=0 |
| 图闸（基线视图） | `codegraph check --view cards-B369-charter` → `fails=0`（EXIT=0）；`codegraph validate --view cards-B369-charter` → `issues=null`（EXIT=0） |
| 图闸（纯基线） | `codegraph check` → `fails=0`（EXIT=0）；`codegraph validate` → `issues=null` |
| 契约闸 | `go test ./cmd/ -run TestRepoContractGate -count=1` → `ok`（EXIT=0） |
| `--qr`/`--bundle` 现状 | 不存在；`console --bundle` 实测报 `unknown flag: --bundle`（临时探针，已删） |
| 图预算危险点 1 | `d_cli→d_policy` **恰好 35/35**（`codegraph check` 实测 `legacyHits.d_cli->d_policy=35`，`target.json` `legacyBudget:35`）——本卡**不得新增任何 `config.*` 直调**，否则 over-budget |
| 图预算危险点 2 | `d_cli→d_protocol` 基线 1/1（唯一额度被 `n_cmd_currentSeatIdentity→n_proto_EncodeSeatIdentity` 占用）；`entries` 仅 `"proto 实体"`，**不含** `"proto（包级函数）"`。加 `console.go→proto.EncodePairBundle` 边后 `check` 报 `over-budget 2/1`（临时探针实测） |
| 图预算危险点 3 | `d_cli→d_transport` 12/12（`client.Client` 经 `newTargetClientNamed` 既有边，本卡复用，不新增） |
| 既有组装点 | `newTargetClientNamed(name)`（`cmd/root.go:241`）按 target 名选路，内部走 `targetclient.New`；`Endpoints("")`（`cmd/root.go:144`）返回本机 + 各 target 的 `Endpoint`（`cmd/root.go:102`） |
| 领 ticket 现状 | `client.Client.IssueAuthTicket(ctx, deviceName)`（`internal/client/client.go:1267`）→ `POST /api/auth/tickets`，返回 `*proto.AuthTicketResp{URL, ExpiresAt}` |
| ticket 寿命 | `ticketLifetime = 60 * time.Second`（`internal/agentd/auth.go:34`） |
| QR 库现状 | `go.mod` 无 QR 依赖；实测可拉 `rsc.io/qr v0.2.0`（`go get` EXIT=0；`go.sum` 加 2 行） |
| QR 单码容量（rsc.io/qr v0.2.0 实测） | byte 模式二分：L=2953、M=2331、Q=1663、H=1273 字节；超界 `qr.Encode` 返回 `"text too long to encode as QR"` |
| QR 渲染参考（skip2 实测对照） | 半块字符渲染（`█/▀/▄`）在 `rsc.io/qr` 上需自写；`Code.Black(x,y) bool`、`Code.Size int`（`rsc.io/qr` v0.2.0 `qr.go:81/74`） |
| 真实 8 机直连 bundle 实测字节 | 1903–1957（ticket URL 64 hex；avg ≈ 245/机）；12 机 ≈ 2779；16 机 > 2953（超 L 级） |
| `desktop/go.mod` 漂移 | 加 `rsc.io/qr` 后 `cd desktop && go mod tidy` 实测**无残留**（qr 不进 desktop 依赖闭包）——CI 的 desktop 依赖对齐门不破 |

**库行为出处**：`rsc.io/qr v0.2.0`（`module rsc.io/qr`，版本实测可拉）。关键行为——`Encode(text string, level Level) (*Code, error)`、`Level` 常量 L/M/Q/H、`Code.Black(x,y)`、`Code.Size`、超容量错误文本——全部以本轮探针亲测为准（§1 表内读数），不依赖记忆。
`encoding/json` omitempty 语义（nil 指针省键、空串省键）由 `internal/proto/pairing_fixture_test.go` 既有金样本锁定，本卡不改。

---

## 2. 图覆盖债

本轮亲测 `codegraph sym` 与 `check --view cards-B369-charter`：

- **命中（view 内）**：`EncodePairBundle`（`n_proto_EncodePairBundle`）、`PairBundle`/`PairMachine`/`PairTicket`/`PairRelay`（`m_proto_*`）、`newTargetClient`（`n_cmd_newTargetClient`）、`IssueAuthTicket`（`n_client_Client_IssueAuthTicket`）、`Endpoints`（`n_cmd_Endpoints`）、`loadCLIConfig`、`effectiveConfigPath`（均 `k_cmd_fn`）。
- **未命中（图覆盖债）**：基线 `codegraph sym consoleCmd` 返回「不在图中」，近似候选 `n_cmd_consoleCmd_RunE`（图只收 RunE 节点，不收命令变量）。回落 grep 确认命令变量定义于 `cmd/console.go:34`。本卡新增符号随视图 diff 落盘（§5），债记于此。
- 本卡**新增全部在 `cmd/console.go` / `cmd/console_test.go`**（`_test.go` 不计图边），且不动 `internal/proto`（其符号已在 base 视图 diff 内）。

---

## 3. 判据先在基线跑（本轮已复核，逐条可判）

| 判据 | 基线行为（动手前复核） | 本卡预期 |
|---|---|---|
| `console --bundle` 未知 flag | `unknown flag: --bundle`（实测） | 实现后输出一行载荷 |
| `--qr`/`--bundle` 与 `--print-url`/`--no-open` 互斥 | 无此校验 | 互斥报错 |
| `--print-url` 恰好一行 URL | `ok`（既有 3 支测试） | 保持 `ok` |
| `d_cli→d_protocol` 预算 | 1/1；加边即 over-budget（实测） | 补 entry「proto（包级函数）」后 0 直调 |
| `d_cli→d_policy` 预算 | 35/35 | 复用 `Endpoints`，不新增直调，保持 35 |
| 二维码容量 | 12 机 2779B（L 级可容）、16 机 >2953B（L 级拒） | `--qr` 对 16 机报错指向 `--bundle` |
| `d_cli→d_policy` 不破 | 实测 `legacyHits.d_cli->d_policy=35`（预算 35） | 实现后仍 35 |

**判据要钉行为不钉计数**：「`--bundle` 输出能被 `proto.DecodePairBundle` 解回 + 含全部可达机器」钉的是跨语言 wire 可用这一目标；「实测字节数写入断言」钉的是「单码能容纳现实 N 机」这一容量目标——不是「加了 N 行」。

---

## 4. Task DAG

```text
T1 依赖与许可：go.mod 加 rsc.io/qr v0.2.0（唯一新增依赖）
      │
T2 图契约修订：codegraph/target.json 补 d_cli→d_protocol entry「proto（包级函数）」
      │  （授权见 §6；补 entry 后 T3 的 proto 包级边才不 over-budget）
      ▼
T3 cmd/console.go 实现（flags + 组装 + 渲染 + 互斥）
      │
T4 cmd/console_test.go 测试族（主缝 roundtrip + 部分 bundle + 互斥 + 容量 + 守卫）
      │
T5 图视图 diff：codegraph/diffs/cards-B369-charter.json 补本卡新符号与边
      │
T6 收口复核：全量 cmd 测试 + 图三闸 + 契约闸 + Windows/desktop
```

**最薄路径条**：本卡要锁的行为（CLI 产出可被 `proto.DecodePairBundle` 解回的 bundle）今天从声明缝调不到——`console --bundle` 是未知 flag，`--qr` 无渲染实现。T3 就是点亮该行为的最薄可跑路径；T1/T2 是它的两条前置（依赖 + 图许可）。T4 先红后绿：主缝 roundtrip 测试在 T3 前必红（无 flag）。

**红绿周期范围声明**：T3 里**唯一**的锁缝断言是「`--bundle` 输出可解回」这一条缝级测试（T4 的 `TestConsoleQRBundleRoundTrip`），它在 T3 前红、T3 后绿。其余测试（互斥、容量、守卫、部分 bundle）是同一实现上的附加锁，不配独立红绿周期（它们随 T3 落地即绿或有自己的反面示例，见 T4）。T1/T2 是配置改动，无测试面，不配红绿。

---

## 5. 精确改动与完整代码

### T1：依赖 `go.mod` / `go.sum`

**文件**：`go.mod`、`go.sum`。

执行：

```bash
go get rsc.io/qr@v0.2.0
go mod tidy
```

实测产物（本轮亲跑，`go mod tidy` 后）：

- `go.mod` 的**主 require 块**末尾加一行（第 22 行）：`rsc.io/qr v0.2.0`（**不是** indirect——`cmd/console.go` 直接 import）。
- `go.sum` 末尾加两行：

```
rsc.io/qr v0.2.0 h1:6vBLea5/NRMVTz8V66gipeLycZMl/+UlFmk8DvqQ6WY=
rsc.io/qr v0.2.0/go.mod h1:IF+uZjkb9fqyeF/4tlBoynqmQxUoPfWEKh921coOuXs=
```

**验收**：`go build ./...` 退 0；`cd desktop && go mod tidy` 后 `git diff --exit-code desktop/go.mod desktop/go.sum` 为空（实测 qr 不进 desktop 闭包）。

### T2：图契约修订（`codegraph/target.json`）

**文件**：`codegraph/target.json`（唯一改动：`d_cli→d_protocol` 的 `entries` 加一项）。

**授权**：协调者裁决（工单 `74f09134`）选 A——授权 plan 将 `d_cli→d_protocol` 补 entry「proto（包级函数）」，与 B369 contract 对 `d_transport→d_protocol` 的同款机械修订一致；entries 命中不计 legacy，预算保持 1。理由：contract §8.3 明令 CLI 不得重实现 bundle 编解码、必须调 `proto.EncodePairBundle`，故该边是契约要求；contract 节点当时只给 `d_transport→d_protocol` 补了 entry，`d_cli→d_protocol` 属漏项。**除此之外不改 target/baseline 其它条目。**

JSON 无法用行内 diff 表达，故给该 contract 对象的完整前后块（用编辑器替换该对象）：

改动前（`codegraph/target.json`，`grep -n '"from": "d_cli"'` 后第 4 个对象，行 81–88）：

```json
  {
   "from": "d_cli",
   "to": "d_protocol",
   "legacyBudget": 1,
   "entries": [
    "proto 实体"
   ],
   "legacyBudgetNote": "B294：CLI preview open 只经 client.Client 发送 PreviewOpenReq；不穿透 agentd。B312：CLI 只经 proto 的席位身份编码 helper 生成 actor，不复制编码规则。"
  },
```

改动后：

```json
  {
   "from": "d_cli",
   "to": "d_protocol",
   "legacyBudget": 1,
   "entries": [
    "proto 实体",
    "proto（包级函数）"
   ],
   "legacyBudgetNote": "B294：CLI preview open 只经 client.Client 发送 PreviewOpenReq；不穿透 agentd。B312：CLI 只经 proto 的席位身份编码 helper 生成 actor，不复制编码规则。 B369.5：console --qr/--bundle 复用配对载体包级编解码（proto.EncodePairBundle），补 entry「proto（包级函数）」（contract §8.3 明令 CLI 不得重实现）。entries 命中不计 legacy，预算保持 1 不变。"
  },
```

**为什么不用别的方案**：CLI 侧组装 bundle 需要 `proto.PairBundle`/`proto.PairMachine`/`proto.PairTicket` 类型与 `proto.EncodePairBundle` 函数（都在 `d_protocol`）。contract §8.3 禁止在 CLI 重实现，故这些引用无法避免；`proto 实体` entry 只覆盖类型（`proto.Task` 等 model 容器），不覆盖包级函数，所以必须补 `proto（包级函数）`。

**验收**：`codegraph check --view cards-B369-charter` fails=0（补 entry + T5 视图后）；`codegraph check`（纯基线）仍 fails=0。

### T3：`cmd/console.go`（整体替换）

**文件**：`cmd/console.go`（全量替换为下方代码；原 3 个既有测试保持可编译）。

**Interfaces**

Consumes（既有，逐字符保持）：

```go
// cmd/root.go
type Endpoint struct { Name, Addr, Token string; Local bool; RelayURL, Credential, Node string } // :102
func Endpoints(only string) ([]Endpoint, error)                       // :144，本机 Local=true + 各 target
func newTargetClientNamed(name string) (*client.Client, func(), error)// :241，按 target 名选路
func effectiveConfigPath() string                                     // cmd/service.go:88
// internal/client
func (c *Client) IssueAuthTicket(ctx context.Context, deviceName string) (*proto.AuthTicketResp, error) // :1267
// internal/proto（B369 冻结）
const PairVersion = 1                                                 // :24
type PairBundle struct { Version int; Relay *PairRelay; Machines []PairMachine; IssuedAt, ExpiresAt time.Time }
type PairRelay struct { URL, Credential string }
type PairMachine struct { Name, Token, Credential, Node, Addr string; Ticket *PairTicket }
type PairTicket struct { URL string; ExpiresAt time.Time }
func EncodePairBundle(b PairBundle) (string, error)                   // :111
// rsc.io/qr v0.2.0
func Encode(text string, level Level) (*Code, error)
func (c *Code) Black(x, y int) bool
```

Produces（本卡新增的进程内符号）：

```go
var consoleQR, consoleBundle bool
const pairBundleLifetime = 60 * time.Second
func runConsolePair(cmd *cobra.Command) error
func buildPairBundle(ctx context.Context, targets []Endpoint, device string, log *slog.Logger) (proto.PairBundle, error)
func pairMachineForTarget(ctx context.Context, ep Endpoint, device, relayCred string, log *slog.Logger) proto.PairMachine
func issuePairTicketForTarget(ctx context.Context, name, device string) (*proto.PairTicket, error)
func renderBundleQR(payload string) (string, error)
```

**完整代码**（本轮已编译、已跑绿，直接照抄）：

```go
// 本文件实现 handoff console 子命令：用主令牌换一张一次性 ticket，
// 并把兑换 URL 交给系统浏览器（或打印出来给桌面壳用）；B369.5 起新增
// 配对载体输出（--qr/--bundle），供移动 App 扫码配对。
//
// 职责：
//   - 调 client.IssueAuthTicket 取兑换 URL
//   - 默认调系统浏览器打开；--print-url 只打印（这是桌面壳的接线点）
//   - --bundle/--qr：读本机配置的 targets，逐机代领 ticket，组装
//     proto.PairBundle 并编码为 QR 载荷/终端二维码（不重实现 bundle 编解码）
//   - 设备名缺省取本机主机名（CLI 没有 User-Agent 可推断）
//
// 边界：
//   - 不实现任何鉴权逻辑：凭据的签发与校验全在 agentd 侧
//   - 不管前端是否存在：本命令的成功判据是「拿到兑换 URL」，兑换后落地页上有什么
//     由 agentd 决定（W5a 之后是真实控制台，不带 embedweb 标签构建时是 stub 说明页）。
//     不要因为落地页的形态变化而改这里的成功判据
//   - --target 可用，但那是**诊断入口**不是产品路径（产品路径是「只连本机
//     agentd，由它向远端转发」），不要因为它好用就当成跨机方案
//   - 配对载体编解码唯一定义在 internal/proto（契约 §8.3：CLI 不得重实现）；
//     本文件只做「读配置 → 逐机代领 ticket → 调 proto.EncodePairBundle → 渲染」
//   - 凭据卫生：token/ticket 只进 stdout 的载荷，绝不落日志；错误分支只带
//     target 名与 cause，不带 token
package cmd

import (
	"context"
	"errors"
	"fmt"
	"log/slog"
	"os"
	"os/exec"
	"runtime"
	"sort"
	"strings"
	"time"

	"github.com/Xsxdot/handoff/internal/proto"
	"github.com/spf13/cobra"
	"rsc.io/qr"
)

var (
	consolePrintURL bool
	consoleDevice   string
	consoleNoOpen   bool
	consoleQR       bool
	consoleBundle   bool
)

// pairBundleLifetime 是配对载体载荷的展示有效期。
//
// 每台 agentd 的一次性 ticket 寿命是 60s（internal/agentd/auth.go#ticketLifetime，
// 见 contract §9 附区条 5：寿命沿用 60s，N 张同时起算），载体不应承诺比 ticket
// 更长的窗口；这里刻意不 import internal/agentd（那会新增 d_cli→d_gateway 跨域边），
// 只复述同一常量并注明出处。
const pairBundleLifetime = 60 * time.Second

// consoleCmd 打开浏览器控制台或输出配对载体。
var consoleCmd = &cobra.Command{
	Use:   "console",
	Short: "在浏览器中打开 agentd 控制台，或输出移动端配对载体（--qr/--bundle）",
	// 位置参数没有意义：console 的输入全走 flag（--print-url/--device/--no-open），
	// 多余的参数说明用法错误，静默忽略会让拼错的命令「看似成功」——尤其桌面壳
	// 依赖 stdout 恰好一行的契约，多喂一个参数被吞掉会直接破坏那条契约
	Args: cobra.NoArgs,
	RunE: func(cmd *cobra.Command, _ []string) error {
		if consoleQR || consoleBundle {
			// 配对载体模式（--qr/--bundle）与单 URL 模式（--print-url/--no-open）
			// 语义不同（前者是 App 扫码的整包，后者是桌面壳消费的一行 URL），
			// 同给会让 stdout 契约歧义——在触达任何网络前明确拒绝。
			// 注意 --print-url 与 --no-open 仍是同义（历史行为），不互斥。
			if consolePrintURL || consoleNoOpen {
				return errors.New("--qr/--bundle（配对载体）与 --print-url/--no-open（单 URL）不能同时使用")
			}
			return runConsolePair(cmd)
		}
		c, cleanup, err := newTargetClient()
		if err != nil {
			return err
		}
		defer cleanup()
		device := consoleDevice
		if device == "" {
			// CLI 没有 User-Agent 可推断，用主机名作缺省展示名；
			// 取不到主机名时留空，由服务端补浏览器名
			device, _ = os.Hostname()
		}
		tk, err := c.IssueAuthTicket(cmd.Context(), device)
		if err != nil {
			return err
		}
		if consolePrintURL || consoleNoOpen {
			// 只打 URL，一行，无任何前后缀：桌面壳直接把这一行交给 loadURL
			fmt.Fprintln(cmd.OutOrStdout(), tk.URL)
			return nil
		}
		if oerr := openBrowser(tk.URL); oerr != nil {
			// 打不开浏览器不是失败：把 URL 打出来，用户自己粘贴即可，
			// 而 ticket 只有 60 秒，静默失败会让人完全摸不着头脑
			fmt.Fprintf(cmd.ErrOrStderr(), "打开浏览器失败（%v），请手动打开下面的地址（60 秒内有效）：\n", oerr)
			fmt.Fprintln(cmd.OutOrStdout(), tk.URL)
		} else {
			// 成功打开也要让人知道发生了什么，否则静默无感
			fmt.Fprintln(cmd.ErrOrStderr(), "已在浏览器中打开控制台（链接 60 秒内有效）")
		}
		return nil
	},
}

// runConsolePair 组装并输出移动端配对载体（--bundle 出粘贴串，--qr 出终端二维码）。
//
// 与 --print-url 的分工：--print-url 只出本机一张 ticket 的兑换 URL（桌面壳接线，
// stdout 恰好一行）；--qr/--bundle 出的是完整 bundle（relay 端点 + 各 target 登记
// + 每机 ticket），消费者是移动 App 的扫码解码方。
//
// 参数：
//   - cmd: cobra 命令，提供 ctx 与 stdout/stderr
//
// 返回：
//   - 配置加载失败、无 target、bundle 非法、载荷超二维码容量时返回错误（非 0 退出）
func runConsolePair(cmd *cobra.Command) error {
	// 复用既有的 Endpoints 取机器清单：它封装了「读配置 → 本机 + 各 target 换算」
	// 这条既有路径（cmd/root.go），避免在 console.go 里新增对 config 包的直调
	// （d_cli→d_policy 预算已满 35/35，多一条直调即 over-budget）。
	eps, err := Endpoints("")
	if err != nil {
		return err
	}
	// 本机（Local）不是待配对的执行机，不进 bundle；只剩本机 = 无 target。
	var targets []Endpoint
	for _, ep := range eps {
		if !ep.Local {
			targets = append(targets, ep)
		}
	}
	if len(targets) == 0 {
		// 空 bundle 会撞 proto 的「无机器登记」拒收；这里提前给出可行动错误
		return fmt.Errorf("配置 %s 里没有任何 target，无法生成配对载体（先 handoff init 并登记执行机）", effectiveConfigPath())
	}
	device := consoleDevice
	if device == "" {
		device, _ = os.Hostname()
	}
	b, err := buildPairBundle(cmd.Context(), targets, device, slog.Default())
	if err != nil {
		return err
	}
	payload, err := proto.EncodePairBundle(b)
	if err != nil {
		// 不重实现编解码：非法 bundle 由 proto 拒收，这里只带上下文
		return fmt.Errorf("组装配对载体: %w", err)
	}
	if consoleBundle {
		// 恰好一行载荷：粘贴串形态，不受二维码容量限制
		fmt.Fprintln(cmd.OutOrStdout(), payload)
		return nil
	}
	art, err := renderBundleQR(payload)
	if err != nil {
		return err
	}
	fmt.Fprint(cmd.OutOrStdout(), art)
	return nil
}

// buildPairBundle 以各 target 的 Endpoint 登记逐机代领 ticket，组装一份 PairBundle。
//
// 语义（contract §3.1）：一份 bundle = 一个 relay 端点 + 管道 credential + 各机器
// 登记；relay 形态机器与直连形态机器按各自 Endpoint 形态登记。某机 ticket 代领失败
// （离线/连不上）只 Warn 并让该机无 ticket（部分 bundle，contract §3.1「离线机可缺」），
// 不整单失败。
//
// 参数：
//   - ctx: 请求上下文（逐机 IssueAuthTicket 用）
//   - targets: 待配对机器清单（Endpoints 换算结果，已剔除本机）；Endpoint 是
//     cmd 包内类型，经它取形态/令牌不新增 d_cli→d_policy 直调
//   - device: 设备展示名（传给 agentd 的 ticket 展示名，可空）
//   - log: 结构化日志器；失败逐机 Warn
//
// 返回：
//   - 组装完成的 PairBundle；relay 形态 target 使用多个不同 relay 端点时返回错误
//     （wire 只有单一 relay 端点，见 contract §3.1，无法表达多端点）
//
// 为什么先做完 relay 端点一致性检查再逐机领 ticket：多端点配置是纯粹的本地
// 配置冲突，应在触达任何网络之前失败（否则错误取决于哪台先拨号，且测试不可判）。
func buildPairBundle(ctx context.Context, targets []Endpoint, device string, log *slog.Logger) (proto.PairBundle, error) {
	if log == nil {
		log = slog.Default()
	}
	// 输出可复现（golden 字节稳定）：按机器名排序。
	sorted := make([]Endpoint, len(targets))
	copy(sorted, targets)
	sort.Slice(sorted, func(i, j int) bool { return sorted[i].Name < sorted[j].Name })
	// 阶段一：统一 relay 端点（wire 只承载一个）。
	relayURL := ""
	relayCred := ""
	for _, ep := range sorted {
		if ep.RelayURL == "" {
			continue
		}
		if relayURL == "" {
			relayURL, relayCred = ep.RelayURL, ep.Credential
			continue
		}
		if relayURL != ep.RelayURL {
			return proto.PairBundle{}, fmt.Errorf("target %q 的 relay 端点 %q 与其它 target 的 %q 不同——配对载体只承载单一 relay 端点（contract §3.1），请分码配对", ep.Name, ep.RelayURL, relayURL)
		}
	}
	// 阶段二：逐机登记并代领 ticket（此时不再可能因配置冲突中止）。
	// IssuedAt/ExpiresAt 取同一次 now：两次 time.Now() 可能跨秒，让 ExpiresAt-IssuedAt
	// 漂移出恰好 60s（契约 §9 附区条 5：N 张 ticket 同时起算）。
	now := time.Now().UTC()
	b := proto.PairBundle{
		Version:   proto.PairVersion,
		IssuedAt:  now,
		ExpiresAt: now.Add(pairBundleLifetime),
	}
	for _, ep := range sorted {
		b.Machines = append(b.Machines, pairMachineForTarget(ctx, ep, device, relayCred, log))
	}
	if relayURL != "" {
		b.Relay = &proto.PairRelay{URL: relayURL, Credential: relayCred}
	}
	return b, nil
}

// pairMachineForTarget 组装单台机器的登记；ticket 代领失败只标为无 ticket（部分 bundle）。
// 不返回 error：单机失败是「部分 bundle」语义（contract §3.1），不整单失败。
func pairMachineForTarget(ctx context.Context, ep Endpoint, device, relayCred string, log *slog.Logger) proto.PairMachine {
	m := proto.PairMachine{Name: ep.Name, Token: ep.Token}
	if ep.RelayURL != "" {
		m.Node = ep.Node
		if ep.Credential != relayCred {
			// 机器级凭证覆盖：通常空，沿用 bundle.Relay（contract §3.1）
			m.Credential = ep.Credential
		}
	} else {
		m.Addr = ep.Addr
	}
	tk, err := issuePairTicketForTarget(ctx, ep.Name, device)
	if err != nil {
		// 部分 bundle：离线机标记、上线后补配（contract §3.1；不整单失败）
		log.Warn("配对载体：该机 ticket 代领失败，标为无 ticket", "target", ep.Name, "cause", err)
		return m
	}
	m.Ticket = tk
	return m
}

// issuePairTicketForTarget 用该 target 的主令牌代领一张一次性 ticket。
//
// 参数：
//   - ctx: 请求上下文
//   - name: config.Targets 里的登记名（经 newTargetClientNamed 选路：直连或 relay）
//   - device: 设备展示名（可空）
//
// 返回：
//   - ticket（URL + 过期时刻）；选路失败或 agentd 连不上时返回错误，由调用方标离线
func issuePairTicketForTarget(ctx context.Context, name, device string) (*proto.PairTicket, error) {
	c, cleanup, err := newTargetClientNamed(name)
	if err != nil {
		return nil, err
	}
	defer cleanup()
	resp, err := c.IssueAuthTicket(ctx, device)
	if err != nil {
		return nil, err
	}
	return &proto.PairTicket{URL: resp.URL, ExpiresAt: resp.ExpiresAt}, nil
}

// renderBundleQR 把配对载荷渲染成终端二维码（半块字符 + 4 模块静默区）。
//
// 参数：
//   - payload: proto.EncodePairBundle 的紧凑 JSON 串
//
// 返回：
//   - 多行二维码文本（换行结尾）；载荷超过单码容量时返回带 --bundle 退路的错误
func renderBundleQR(payload string) (string, error) {
	code, err := qr.Encode(payload, qr.L)
	if err != nil {
		// L 级单码容量约 2953 字节（rsc.io/qr v0.2.0 实测）；超界退路 = 粘贴串
		return "", fmt.Errorf("生成二维码失败（载荷 %d 字节，可能超过单码容量）: %w；改用 handoff console --bundle 输出粘贴串", len(payload), err)
	}
	const quiet = 4 // 静默区：扫码器需要
	size := code.Size
	black := func(x, y int) bool { return code.Black(x, y) }
	var sb strings.Builder
	for y := -quiet; y < size+quiet; y += 2 {
		for x := -quiet; x < size+quiet; x++ {
			top, bot := black(x, y), black(x, y+1)
			switch {
			case top && bot:
				sb.WriteRune('█')
			case top && !bot:
				sb.WriteRune('▀')
			case !top && bot:
				sb.WriteRune('▄')
			default:
				sb.WriteByte(' ')
			}
		}
		sb.WriteByte('\n')
	}
	return sb.String(), nil
}

func init() {
	consoleCmd.Flags().BoolVar(&consolePrintURL, "print-url", false, "只打印兑换 URL，不打开浏览器（桌面壳用）")
	consoleCmd.Flags().StringVar(&consoleDevice, "device", "", "设备展示名（缺省取本机主机名）")
	consoleCmd.Flags().BoolVar(&consoleNoOpen, "no-open", false, "不打开浏览器（等价于 --print-url）")
	consoleCmd.Flags().BoolVar(&consoleQR, "qr", false, "输出配对载体终端二维码（供移动 App 扫码配对）")
	consoleCmd.Flags().BoolVar(&consoleBundle, "bundle", false, "输出配对载体粘贴串（一行 JSON，二维码超容量时的退路）")
	// --qr 与 --bundle 是同一份载荷的两种编码（终端二维码 / 一行粘贴串），
	// 同给输出语义歧义；--print-url 与 --no-open 是历史同义词，保持不互斥。
	consoleCmd.MarkFlagsMutuallyExclusive("qr", "bundle")
	rootCmd.AddCommand(consoleCmd)
}

// openBrowser 用系统默认方式打开一个 URL。
//
// 注意：各平台命令不同；不支持的平台返回错误，由调用方降级为打印 URL
func openBrowser(rawURL string) error {
	var cmd *exec.Cmd
	switch runtime.GOOS {
	case "darwin":
		cmd = exec.Command("open", rawURL)
	case "windows":
		cmd = exec.Command("rundll32", "url.dll,FileProtocolHandler", rawURL)
	default:
		cmd = exec.Command("xdg-open", rawURL)
	}
	if out, err := cmd.CombinedOutput(); err != nil {
		return fmt.Errorf("%v: %s", err, truncateBytes(string(out), 200))
	}
	return nil
}
```

**关键节点日志与注释（纪律 3/4）**：`pairMachineForTarget` 失败路径 `log.Warn("配对载体：该机 ticket 代领失败，标为无 ticket", "target", ep.Name, "cause", err)`（入口带 target、错误分支带 cause，不含 token）；成功路径不静默（`--qr`/`--bundle` 的输出本身就是成功信号，且不额外打 stderr 以免污染载荷）。注释：文件头职责+边界；`buildPairBundle`/`pairMachineForTarget`/`issuePairTicketForTarget`/`renderBundleQR` 均写参数/返回/为什么。用项目结构化 logger（`log/slog`），无 `fmt.Printf` 到 stderr 的调试输出。

### T4：`cmd/console_test.go`（追加测试；既有 3 支不动）

**文件**：`cmd/console_test.go`（改 import 块 + 文件末尾追加）。

**步骤 1（红）**：先落 T4 的 `TestConsoleQRBundleRoundTrip`（下方代码），在 T3 之前跑：

```bash
go test ./cmd/ -run TestConsoleQRBundleRoundTrip -count=1
```

预期 FAIL（`unknown flag: --bundle`，本轮基线实测原文见台账 §三）。红即判据成立。

**步骤 2（改 import 块）**：把文件头 import 替换为：

```go
import (
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
	"net/http/httptest"
	"os"
	"strings"
	"testing"

	"github.com/Xsxdot/handoff/internal/proto"
	"rsc.io/qr"
)
```

**步骤 3（追加测试体）**：在文件末尾追加（完整代码，本轮已跑绿）：

```go
// --- B369.5 配对载体测试 ---

// pairBundleFakeAgentd 起一个假 agentd：POST /api/auth/tickets 回一张指向自身的 ticket。
func pairBundleFakeAgentd(t *testing.T, token string) string {
	t.Helper()
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		if r.URL.Path != "/api/auth/tickets" {
			t.Errorf("非预期路径: %s", r.URL.Path)
		}
		if got := r.Header.Get("Authorization"); got != "Bearer "+token {
			t.Errorf("Authorization = %q", got)
		}
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(map[string]any{
			"url":        "http://" + r.Host + "/console?ticket=abc",
			"expires_at": "2026-09-14T00:01:00Z",
		})
	}))
	t.Cleanup(ts.Close)
	return ts.URL
}

// runConsoleForTest 以 args 执行 console，捕获 stdout/stderr（只经 rootCmd.SetOut，
// 绝不调 consoleCmd.SetOut——cobra 的 outWriter 一旦设置会跨用例残留）。
func runConsoleForTest(t *testing.T, cfgPath string, args ...string) (string, string, error) {
	t.Helper()
	resetFlags(t)
	targetName = ""
	configPath = cfgPath
	rootCmd.PersistentFlags().Lookup("agentd").Changed = false
	consoleQR, consoleBundle, consolePrintURL, consoleNoOpen = false, false, false, false
	rootCmd.SetArgs(args)
	var stdout, stderr bytes.Buffer
	rootCmd.SetOut(&stdout)
	rootCmd.SetErr(&stderr)
	t.Cleanup(func() {
		rootCmd.SetArgs(nil)
		rootCmd.SetOut(nil)
		rootCmd.SetErr(nil)
	})
	err := Execute()
	return stdout.String(), stderr.String(), err
}

// TestConsoleQRBundleRoundTrip 钉死 S5 主缝：console --bundle 对多 target 配置产出的
// stdout（恰好一行 JSON）必须能被 proto.DecodePairBundle 解回，含全部可达机器（每机
// token/形态/ticket）。
func TestConsoleQRBundleRoundTrip(t *testing.T) {
	ts1 := pairBundleFakeAgentd(t, "token-aaa")
	ts2 := pairBundleFakeAgentd(t, "token-bbb")
	cfgPath := writeTestConfig(t, "listen: \"127.0.0.1:1\"\ntoken: \"local-tok\"\n"+
		"targets:\n  devbox:\n    addr: \""+strings.TrimPrefix(ts1, "http://")+"\"\n    token: \"token-aaa\"\n"+
		"  nas:\n    addr: \""+strings.TrimPrefix(ts2, "http://")+"\"\n    token: \"token-bbb\"\n")
	stdout, _, err := runConsoleForTest(t, cfgPath, "console", "--bundle")
	if err != nil {
		t.Fatalf("console --bundle: %v", err)
	}
	lines := strings.Split(strings.TrimRight(stdout, "\n"), "\n")
	if len(lines) != 1 {
		t.Fatalf("--bundle stdout 应恰好一行载荷，实得 %d：%q", len(lines), stdout)
	}
	b, err := proto.DecodePairBundle(lines[0])
	if err != nil {
		t.Fatalf("CLI 产出的载荷必须能被 proto.DecodePairBundle 解回: %v\nraw=%s", err, lines[0])
	}
	if len(b.Machines) != 2 {
		t.Fatalf("应含全部可达机器 2 台: %+v", b.Machines)
	}
	byName := map[string]proto.PairMachine{}
	for _, m := range b.Machines {
		byName[m.Name] = m
		if m.Ticket == nil {
			t.Fatalf("可达机 %q 必须带 ticket", m.Name)
		}
	}
	if byName["devbox"].Token != "token-aaa" || byName["devbox"].Addr == "" {
		t.Fatalf("devbox 登记不对: %+v", byName["devbox"])
	}
	if byName["nas"].Token != "token-bbb" || byName["nas"].Addr == "" {
		t.Fatalf("nas 登记不对: %+v", byName["nas"])
	}
}

// TestConsoleBundlePartialOnOfflineTarget 离线机（连不上）只标为无 ticket，不整单失败。
func TestConsoleBundlePartialOnOfflineTarget(t *testing.T) {
	dead := httptest.NewServer(http.NotFoundHandler())
	deadAddr := strings.TrimPrefix(dead.URL, "http://")
	dead.Close()
	live := pairBundleFakeAgentd(t, "token-live")
	cfgPath := writeTestConfig(t, "listen: \"127.0.0.1:1\"\ntoken: \"local-tok\"\n"+
		"targets:\n  live:\n    addr: \""+strings.TrimPrefix(live, "http://")+"\"\n    token: \"token-live\"\n"+
		"  offline:\n    addr: \""+deadAddr+"\"\n    token: \"token-off\"\n")
	stdout, _, err := runConsoleForTest(t, cfgPath, "console", "--bundle")
	if err != nil {
		t.Fatalf("离线机不整单失败: %v", err)
	}
	b, err := proto.DecodePairBundle(strings.TrimSpace(stdout))
	if err != nil {
		t.Fatalf("部分 bundle 应可解回: %v (stdout=%q)", err, stdout)
	}
	byName := map[string]proto.PairMachine{}
	for _, m := range b.Machines {
		byName[m.Name] = m
	}
	if byName["live"].Ticket == nil {
		t.Fatal("在线机必须带 ticket")
	}
	if byName["offline"].Ticket != nil {
		t.Fatal("离线机必须无 ticket（部分 bundle）")
	}
}

// TestConsoleBundleNoTargetsErrors 无任何 target 时必须明确报错，不产出空 bundle。
func TestConsoleBundleNoTargetsErrors(t *testing.T) {
	cfgPath := writeTestConfig(t, "listen: \"127.0.0.1:1\"\ntoken: \"local-tok\"\n")
	stdout, _, err := runConsoleForTest(t, cfgPath, "console", "--bundle")
	if err == nil {
		t.Fatal("无 target 应报错")
	}
	if !strings.Contains(err.Error(), "没有任何 target") {
		t.Fatalf("报错应点明无 target: %v", err)
	}
	if stdout != "" {
		t.Errorf("失败时 stdout 应为空: %q", stdout)
	}
}

// TestConsoleOutputModesMutuallyExclusive 输出模式互斥：--qr 与 --bundle 是同一载荷
// 的两种编码（cobra flag 互斥）；--qr/--bundle 与 --print-url/--no-open 是配对面与
// 桌面壳单 URL 面（RunE 前置拒绝）。三种组合都必须报错且不触达网络。
func TestConsoleOutputModesMutuallyExclusive(t *testing.T) {
	// 每例一个子测试：flag 的 Changed 标记跨 Execute 累积，只有子测试的
	// t.Cleanup（resetFlags 注册）跑完才会清，故不能在同一测试体里循环 Execute。
	for _, tc := range []struct {
		name string
		args []string
	}{
		{"qr 与 bundle 互斥", []string{"console", "--qr", "--bundle"}},
		{"qr 与 print-url 互斥", []string{"console", "--qr", "--print-url"}},
		{"bundle 与 no-open 互斥", []string{"console", "--bundle", "--no-open"}},
	} {
		t.Run(tc.name, func(t *testing.T) {
			cfgPath := writeTestConfig(t, "listen: \"127.0.0.1:1\"\ntoken: \"local-tok\"\n")
			_, _, err := runConsoleForTest(t, cfgPath, tc.args...)
			if err == nil {
				t.Fatalf("%v 同给应报错", tc.args)
			}
		})
	}
	// --print-url 与 --no-open 是历史同义词，不得互斥（既有调用方两种都用）。
	t.Run("print-url 与 no-open 同义不互斥", func(t *testing.T) {
		ts := pairBundleFakeAgentd(t, "测试令牌")
		cfgPath := writeTestConfig(t, "listen: \""+strings.TrimPrefix(ts, "http://")+"\"\ntoken: \"测试令牌\"\n")
		stdout, _, err := runConsoleForTest(t, cfgPath, "console", "--print-url", "--no-open")
		if err != nil {
			t.Fatalf("--print-url 与 --no-open 同义，不得报错: %v", err)
		}
		if strings.Count(strings.TrimRight(stdout, "\n"), "\n") != 0 {
			t.Fatalf("同义词组合仍须恰好一行 URL: %q", stdout)
		}
	})
}

// TestConsoleQRArtifactIsTerminalQR --qr 的 stdout 必须是可渲染的二维码文本。
func TestConsoleQRArtifactIsTerminalQR(t *testing.T) {
	live := pairBundleFakeAgentd(t, "token-qr")
	cfgPath := writeTestConfig(t, "listen: \"127.0.0.1:1\"\ntoken: \"local-tok\"\n"+
		"targets:\n  devbox:\n    addr: \""+strings.TrimPrefix(live, "http://")+"\"\n    token: \"token-qr\"\n")
	stdout, _, err := runConsoleForTest(t, cfgPath, "console", "--qr")
	if err != nil {
		t.Fatalf("console --qr: %v", err)
	}
	if !strings.ContainsAny(stdout, "█▀▄") {
		t.Fatalf("--qr 输出应是二维码半块字符画:\n%s", stdout)
	}
	if strings.Count(stdout, "\n") < 10 {
		t.Fatalf("二维码应有多行: %q", stdout)
	}
}

// TestConsolePrintURLStillOneLine 不破坏既有契约：--print-url 仍恰好一行 URL。
func TestConsolePrintURLStillOneLine(t *testing.T) {
	ts := pairBundleFakeAgentd(t, "测试令牌")
	var stdout bytes.Buffer
	runSubcommandForTest(t, &stdout, ts, "测试令牌", []string{"console", "--print-url"})
	lines := strings.Split(strings.TrimRight(stdout.String(), "\n"), "\n")
	if len(lines) != 1 || !strings.HasPrefix(lines[0], "http") {
		t.Fatalf("--print-url 仍须恰好一行 URL: %q", stdout.String())
	}
}

// TestConsoleProductionDoesNotLogToken 源码级守卫：console.go 不得把 token 写进日志。
func TestConsoleProductionDoesNotLogToken(t *testing.T) {
	body, err := os.ReadFile("console.go")
	if err != nil {
		t.Fatal(err)
	}
	text := string(body)
	for _, bad := range []string{"\"token\", m.Token", "\"token\", t.Token", "\"token\", cfg.Token"} {
		if strings.Contains(text, bad) {
			t.Fatalf("console.go 不得记录 token: %q", bad)
		}
	}
}

// TestConsoleBundleCapacityBudget 实测 N 机 bundle 字节数（拆解 §3.6 ③ 容量预算；
// contract §9 附区条 3：PairMachineBudgetBytes=200 是声明值，实测上界写入断言）。
//
// 用真实 ticket 长度（64 hex）而非短桩：否则测出的字节数偏小，预算失真。
// 单码容量按 rsc.io/qr v0.2.0 实测：L 级 2953 字节、M 级 2331 字节、Q 级 1663、
// H 级 1273（byte 模式二分实测）。渲染固定用 L 级（renderBundleQR），故上界以 L 为准。
func TestConsoleBundleCapacityBudget(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(map[string]any{
			"url":        "http://" + r.Host + "/console?ticket=" + strings.Repeat("c", 64),
			"expires_at": "2026-09-14T00:01:00Z",
		})
	}))
	t.Cleanup(ts.Close)
	addr := strings.TrimPrefix(ts.URL, "http://")
	var b strings.Builder
	b.WriteString("listen: \"127.0.0.1:1\"\ntoken: \"local-tok\"\ntargets:\n")
	for i := 0; i < 8; i++ {
		fmt.Fprintf(&b, "  machine-%02d:\n    addr: \"%s\"\n    token: \"token-%02d\"\n", i, fmt.Sprintf("%s", addr), i)
	}
	cfgPath := writeTestConfig(t, b.String())
	stdout, _, err := runConsoleForTest(t, cfgPath, "console", "--bundle")
	if err != nil {
		t.Fatalf("console --bundle: %v", err)
	}
	payload := strings.TrimSpace(stdout)
	// 实测字节数写入断言（改 wire 形状/字段名会当场红，提醒重估 N 上界）。
	t.Logf("实测 8 机直连 bundle 字节数=%d（avg=%.1f/机）", len(payload), float64(len(payload))/8)
	// 声明值 PairMachineBudgetBytes=200 是 contract 的声明预算（不含 ticket URL 的
	// 保险上限），实测每机 ~238B 已超声明值——本条只把**实测**上界钉住：8 机 <= 2600
	// 字节，仍在 L 级单码容量（2953）内；超界退路 = --bundle 粘贴串（contract §9 附区条 3）。
	if len(payload) > 2600 {
		t.Fatalf("8 机 bundle 超实测上界 2600：%d", len(payload))
	}
	// 单码可编码（L 级）：容量上界的机器判据
	if _, err := qr.Encode(payload, qr.L); err != nil {
		t.Fatalf("8 机 bundle 应能在 L 级单码内编码: %v (len=%d)", err, len(payload))
	}
}

// TestConsoleQROverflowSuggestsBundle 超过单码容量的多机 bundle 经 --qr 必须
// 给出可行动错误（指向 --bundle 粘贴串退路），不是静默产出残缺二维码。
// 用 16 台共享同一个 fake agentd 的 target（名字不同即不同机器登记），
// bundle >3.5KB > L 级单码 2953 字节（rsc.io/qr v0.2.0 实测）。
func TestConsoleQROverflowSuggestsBundle(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(map[string]any{
			"url":        "http://" + r.Host + "/console?ticket=" + strings.Repeat("c", 64),
			"expires_at": "2026-09-14T00:01:00Z",
		})
	}))
	t.Cleanup(ts.Close)
	addr := strings.TrimPrefix(ts.URL, "http://")
	var b strings.Builder
	b.WriteString("listen: \"127.0.0.1:1\"\ntoken: \"local-tok\"\ntargets:\n")
	for i := 0; i < 16; i++ {
		fmt.Fprintf(&b, "  machine-%02d:\n    addr: \"%s\"\n    token: \"token-%02d\"\n", i, addr, i)
	}
	cfgPath := writeTestConfig(t, b.String())
	_, _, err := runConsoleForTest(t, cfgPath, "console", "--qr")
	if err == nil {
		t.Fatal("16 机 bundle 超单码容量，--qr 应报错")
	}
	if !strings.Contains(err.Error(), "--bundle") {
		t.Fatalf("错误应指向 --bundle 粘贴串退路: %v", err)
	}
}
```

**步骤 4（跑绿）**：`go test ./cmd/ -run TestConsole -count=1` → `ok`（本轮实测 15 支全绿）。

**测试范围声明（最小化）**：本卡只跑 `go test ./cmd/ -count=1`（触及包）与 `go test ./cmd/ -run TestRepoContractGate -count=1`；**不跑全量测试**（全量归 integrate）。

**内部锁声明（接缝覆盖）**：本 task 的 `TestConsoleProductionDoesNotLogToken` 是**源码级守卫**（读 `console.go` 文本），入口不在声明缝上——但这是本卡刻意的内部锁：它锁的源码性质（源码不含 `"token", …` 日志键）**无法从声明缝（cobra 命令/`--bundle`/`--qr`）构造出断言**——从任何命令调用都看不到「生产源码某处是否 log 了 token」，只能读源码。此为本计划唯一内部锁，理由为「从声明缝构造不出」，符合纪律豁免形状；其余全部测试入口都落在缝上（见 §7 缝覆盖表）。

### T5：图视图 diff（`codegraph/diffs/cards-B369-charter.json`）

**文件**：`codegraph/diffs/cards-B369-charter.json`（改：在既有 `nodesAdded`/`edgesAdded` 上追加本卡符号；**沿用既有文件，不另起新视图**——先例 `docs/superpowers/plans/b156.2.4-plan.md:1102`）。

在 `nodesAdded` 对象内追加 5 个节点（`line` 按 T3 落地后的实际 `func` 声明起始行填；下方数值是本轮实测实现的行号，若实现漂移以实际为准——机械修正，非占位）：

```json
  "n_cmd_runConsolePair": {
   "kind": "func",
   "container": "k_cmd_fn",
   "order": 9100,
   "name": "runConsolePair",
   "file": "cmd/console.go",
   "line": 121,
   "signature": "func runConsolePair(cmd *cobra.Command) error"
  },
  "n_cmd_buildPairBundle": {
   "kind": "func",
   "container": "k_cmd_fn",
   "order": 9101,
   "name": "buildPairBundle",
   "file": "cmd/console.go",
   "line": 186,
   "signature": "func buildPairBundle(ctx context.Context, targets []Endpoint, device string, log *slog.Logger) (proto.PairBundle, error)"
  },
  "n_cmd_pairMachineForTarget": {
   "kind": "func",
   "container": "k_cmd_fn",
   "order": 9102,
   "name": "pairMachineForTarget",
   "file": "cmd/console.go",
   "line": 229,
   "signature": "func pairMachineForTarget(ctx context.Context, ep Endpoint, device, relayCred string, log *slog.Logger) proto.PairMachine"
  },
  "n_cmd_issuePairTicketForTarget": {
   "kind": "func",
   "container": "k_cmd_fn",
   "order": 9103,
   "name": "issuePairTicketForTarget",
   "file": "cmd/console.go",
   "line": 259,
   "signature": "func issuePairTicketForTarget(ctx context.Context, name, device string) (*proto.PairTicket, error)"
  },
  "n_cmd_renderBundleQR": {
   "kind": "func",
   "container": "k_cmd_fn",
   "order": 9104,
   "name": "renderBundleQR",
   "file": "cmd/console.go",
   "line": 279,
   "signature": "func renderBundleQR(payload string) (string, error)"
  },
```

在 `edgesAdded` 数组内追加（端点均已在图：`n_cmd_consoleCmd_RunE`/`n_cmd_Endpoints`/`n_cmd_effectiveConfigPath`/`n_cmd_newTargetClientNamed` 在 baseline；`n_proto_EncodePairBundle`/`m_proto_PairBundle`/`m_proto_PairMachine`/`n_client_Client_IssueAuthTicket` 在 base 视图 diff）：

```json
  ["n_cmd_consoleCmd_RunE", "n_cmd_runConsolePair"],
  ["n_cmd_runConsolePair", "n_cmd_Endpoints"],
  ["n_cmd_runConsolePair", "n_cmd_buildPairBundle"],
  ["n_cmd_runConsolePair", "n_cmd_renderBundleQR"],
  ["n_cmd_runConsolePair", "n_cmd_effectiveConfigPath"],
  ["n_cmd_runConsolePair", "n_proto_EncodePairBundle"],
  ["n_cmd_buildPairBundle", "n_cmd_pairMachineForTarget"],
  ["n_cmd_buildPairBundle", "n_proto_EncodePairBundle"],
  ["n_cmd_buildPairBundle", "m_proto_PairBundle"],
  ["n_cmd_pairMachineForTarget", "m_proto_PairMachine"],
  ["n_cmd_pairMachineForTarget", "n_cmd_issuePairTicketForTarget"],
  ["n_cmd_issuePairTicketForTarget", "n_cmd_newTargetClientNamed"],
  ["n_cmd_issuePairTicketForTarget", "n_client_Client_IssueAuthTicket"],
```

**门控说明（实测）**：`n_cmd_buildPairBundle→n_proto_EncodePairBundle` 是包级函数调用，`codegraph validate` 的 edgegate 要求 `cmd/console.go` 文件 import `internal/proto`——T3 的 import 块已含之（否则 validate 报 `no-import`，本轮实测过该红）。若实现时把 proto 调用挪进别的不 import proto 的文件，validate 会红——这是判据不是障碍。

**验收**：`codegraph validate --view cards-B369-charter` → `issues=null`；`codegraph check --view cards-B369-charter` → `fails=0`（本轮实测）。

### T6：收口复核

**改**：无（纯验证）。**测试范围**：仅下述命令。

```bash
go build ./...                                          # 预期 EXIT=0
GOOS=windows GOARCH=amd64 go build ./...                # 预期 EXIT=0（QR 库纯 Go，跨平台）
go vet ./cmd/                                           # 预期 EXIT=0
gofmt -l cmd/console.go cmd/console_test.go             # 预期无输出
go test ./cmd/ -run TestConsole -count=1                # 预期 ok（15 支）
go test ./cmd/ -count=1                                 # 预期 ok（全 cmd 包）
go test ./cmd/ -run TestRepoContractGate -count=1       # 预期 ok（warns 33）
codegraph --repo . check --view cards-B369-charter      # 预期 fails=[]
codegraph --repo . validate --view cards-B369-charter   # 预期 issues=null
codegraph --repo . check                                # 预期 fails=[]（纯基线仍绿）
codegraph --repo . validate                             # 预期 issues=null
cd desktop && go mod tidy && git diff --exit-code -- go.mod go.sum   # 预期无输出（qr 不进 desktop 闭包）
```

**Windows 终端渲染说明**：`renderBundleQR` 只用 U+2588/U+2580/U+2584 与空格（半块字符），不用 ANSI 颜色转义——Windows Terminal / conhost（开启 VT）均可渲染；`--bundle`（纯 ASCII 一行 JSON）是所有平台的兜底。跨平台真机终端走查归拆解 §5 #7（本卡不夸大）。

---

## 6. 契约增量与图修订节（授权留痕）

- **本卡不新增跨子系统方向**。全部跨域边落在既有方向：`d_cli→d_protocol`（proto 包级函数，补 entry 后 entries 命中）、`d_cli→d_transport`（`client.Client` 既有边，复用 `newTargetClientNamed`）、`d_cli→d_policy`（**零新增直调**：复用 `Endpoints` 做配置读取）。
- **唯一图修订**：`codegraph/target.json` 的 `d_cli→d_protocol` 补 entry「proto（包级函数）」，预算保持 1（entries 命中不计 legacy）。**理由**：contract §8.3 明令 CLI 不得重实现 bundle 编解码、必须调 `proto.EncodePairBundle`，故该边是契约要求；B369 contract 节点当时只给 `d_transport→d_protocol` 补了 entry，`d_cli→d_protocol` 属漏项（协调者工单 `74f09134` 裁决选 A 授权）。
- **实测佐证**：补 entry 前，把 `console.go→EncodePairBundle` 边补进视图后 `check` 报 `over-budget d_cli->d_protocol 2/1`；补 entry 后 `fails=0`、`d_cli->d_protocol` 退出 legacyHits（0 直调）。
- **不改** `best.json`/`baseline.json`/其它 contract 条目。

---

## 7. 整卡验收判据（行为化，逻辑型机内闭环）

1. `go test ./cmd/ -run TestConsole -count=1` 退 0，且以下测试存在并通过：`TestConsoleQRBundleRoundTrip`、`TestConsoleBundlePartialOnOfflineTarget`、`TestConsoleBundleNoTargetsErrors`、`TestConsoleOutputModesMutuallyExclusive`、`TestConsoleQRArtifactIsTerminalQR`、`TestConsolePrintURLStillOneLine`、`TestConsoleProductionDoesNotLogToken`、`TestConsoleBundleCapacityBudget`、`TestConsoleQROverflowSuggestsBundle`（外加既有 3 支不破）。
2. **主缝可红**：`TestConsoleQRBundleRoundTrip` 在 T3 之前红（`unknown flag: --bundle`，实测），T3 后绿。
3. `--print-url` 仍恰好一行 URL（`TestConsolePrintURLOutputContract` 与 `TestConsolePrintURLStillOneLine` 双锁）；`--print-url`/`--no-open` 同义不互斥（历史行为）。
4. `console --qr`/`--bundle` 对多 target 配置产出可被 `proto.DecodePairBundle` 解回的 bundle，含全部可达机器（每机 token/形态/ticket），离线机无 ticket（部分 bundle）。
5. 无 target 明确报错（不产空 bundle）；`--qr`/`--bundle` 与 `--print-url`/`--no-open` 互斥；`--qr` 超单码容量报错指向 `--bundle`。
6. 容量：8 机 bundle 实测字节数写入断言（本轮 1903–1957，断言上界 2600）且能在 L 级单码内编码。
7. 跨语言 wire：CLI 产出走 `proto.EncodePairBundle`（源码 import 与 edgegate 双证），未重实现编解码；与 S1 共享同一 wire 键集（由 `proto.DecodePairBundle` 解回证明）。
8. 图三闸：`check/validate --view cards-B369-charter` 与纯基线 `check/validate` 全 0 fail / 0 issue；`TestRepoContractGate` PASS；`d_cli→d_policy` 保持 35/35（不新增直调）。
9. 交付物：`cmd/console.go`（改）、`cmd/console_test.go`（改）、`go.mod`、`go.sum`、`codegraph/target.json`（改）、`codegraph/diffs/cards-B369-charter.json`（改）。`internal/proto/pairing.go` 零改动（`git diff --stat` 可判）。
10. `go build ./...`、`GOOS=windows GOARCH=amd64 go build ./...`、`go vet ./cmd/`、`gofmt -l` 全过；`cd desktop && go mod tidy` 无残留。

---

## 8. 缺陷族对抗审查（逐族设问，验收栏）

基于卡族：CLI/配置/跨语言序列化/凭据卫生。

1. **生命周期/状态机中断**：CLI 一次性进程，无孤儿；ticket 60s 过期——命令在拿到全部 ticket 后**立即**输出（无 sleep/等待）；`newTargetClientNamed` 的 cleanup 由 `defer` 释放（relay 隧道）。部分 bundle 不整单失败。**验收**：`TestConsoleBundlePartialOnOfflineTarget` + `TestConsoleQRBundleRoundTrip`。
2. **静默失败/误导报错**：某机 `IssueAuthTicket` 失败 → Warn（带 target/cause）+ 该机无 ticket，**不**静默吞也不整单失败；无 target 明确报错；`--qr` 超容量报错指向 `--bundle`；`proto.EncodePairBundle` 失败带上下文。反面断言：`TestConsoleBundleNoTargetsErrors`（stdout 为空）、`TestConsoleQROverflowSuggestsBundle`。
3. **跨平台假设**：`rsc.io/qr` 纯 Go、无 CGO；渲染只用 Unicode 半块字符（无 ANSI）；`--bundle` 纯 ASCII 兜底。**真机终端扫码可扫性未验证，需真机**（拆解 §5 #7）——本卡不得写成「真机可扫」。
4. **假红/假绿**：测试穿真 cobra 命令（`Execute`）与真 `proto.EncodePairBundle`/`DecodePairBundle`（不测手抄 JSON）；`--bundle` 的一行/解回双断言防「输出看似 JSON 实则不可解」；二维码断言用半块字符 + 行数，非「非空即过」。`TestConsoleProductionDoesNotLogToken` 为源码守卫（见 §T4 内部锁声明）。
5. **门禁绕过**：`--qr`/`--bundle` 与 `console` 现状同信任基线（本机主令牌经 `newTargetClientNamed`）；`handleIssueTicket` 对会话身份 403 的既有执法不因新 flag 放松（未改动 agentd）；CLI 不重实现编解码（contract §8.3，源码 import + edgegate 双证）。移动端回环门禁属 S2/S4，本卡不碰。
6. **序列化边界**：**本卡主战场**——CLI 产出的 bundle 是跨语言序列化边界（Go 编 → QR → Kotlin/Swift 解）。本卡有 `--bundle` 输出 → `proto.DecodePairBundle` 解回的测试（穿真实 JSON 编解码）。可空类型区分字段缺失/零值：`Ticket *PairTicket`——`TestConsoleBundlePartialOnOfflineTarget` 断言离线机 `Ticket == nil`（字段缺失）而非零值。跨语言整链的另一半（Kotlin/Swift 解码）归真机（P5=A）。
7. **枚举新值过既有白名单**：不新增枚举。`PairVersion` 未改；`--qr`/`--bundle`/`--print-url`/`--no-open` 的互斥关系在 flag 校验层明确（`MarkFlagsMutuallyExclusive("qr","bundle")` + RunE 前置拒绝配对面 vs 单 URL 面），并有反例（`TestConsoleOutputModesMutuallyExclusive` 三个子例 + 同义不互斥）。
8. **承重安全属性**：token 副本是主 Bearer 令牌——命令不得把 token 写进日志/历史/临时文件。**验收**：`TestConsoleProductionDoesNotLogToken`（源码守卫无 `"token", …` 日志键）+ `pairMachineForTarget` 的 Warn 只带 target/cause。QR 展示等级等同主令牌，文档卫生说明归 S3 `mobile/README.md`（本卡不越界写文档）。
9. **webview 候选族**：无，因为 CLI 不触 webview。

**序列化边界设问（跨每个手写投影）**：本卡从产生到消费的投影链 = `Endpoint`（cmd 包内）→ `proto.PairMachine`/`PairBundle`（内存）→ `proto.EncodePairBundle`（JSON）→ stdout → `proto.DecodePairBundle`（测试消费）。**无手搭 map/DTO 转换/CLI 拼输出**——`proto.EncodePairBundle` 是唯一编码点；`PairTicket` 由 `AuthTicketResp` 字段逐个赋值（`URL`/`ExpiresAt`），有一个显式投影点，已由主缝 roundtrip 测试穿过（断言 ticket URL 与 expires 在解回后非空/可判）。

---

## 9. 五项检查自审

1. **缺陷族对抗**：§8 逐族有结论（含「无，因为……」）。✔
2. **序列化边界设问**：§8 末段列出手写投影点（`PairTicket` 赋值）并加断言；主缝 roundtrip 穿真实 JSON 编解码；可空 `Ticket` 区分缺失/零值。✔
3. **上下文预算检查**：有界文件集 = `cmd/console.go`、`cmd/console_test.go`、`go.mod`、`go.sum`、`codegraph/target.json`、`codegraph/diffs/cards-B369-charter.json`（6 个，圈得出）。✔
4. **类型标注**：本卡是 CLI/逻辑型（非边界型子系统接缝），真机清单明确标注为未验证（§7 条 3、§10）。✔
5. **接缝覆盖（双向）**：

   | 缝（声明面） | 锁它的测试（入口符号） |
   |---|---|
   | `console --bundle` 命令 stdout | `TestConsoleQRBundleRoundTrip`、`TestConsoleBundlePartialOnOfflineTarget`、`TestConsoleBundleNoTargetsErrors` |
   | `console --qr` 命令 stdout | `TestConsoleQRArtifactIsTerminalQR`、`TestConsoleQROverflowSuggestsBundle` |
   | `console --print-url` 命令 stdout | `TestConsolePrintURLOutputContract`、`TestConsoleAgentdNotRunning`、`TestConsoleRejectsPositionalArgs`、`TestConsolePrintURLStillOneLine` |
   | flag 互斥校验层 | `TestConsoleOutputModesMutuallyExclusive` |
   | `proto.EncodePairBundle`（跨语言编码缝） | `TestConsoleQRBundleRoundTrip`（产出→Decode 解回）、`TestConsoleBundleCapacityBudget`（编码成功） |

   **测试 → 缝**：上表每支测试的入口都在 `console` 命令/`proto.EncodePairBundle` 缝上；唯一例外 `TestConsoleProductionDoesNotLogToken` 为源码守卫（内部锁，理由见 §T4）。**缝 → 测试**：列出的每条缝至少一支缝级断言锁住。**退路同闸**：本计划无「条件退路」（无「若意外先绿就改成直喂 X」类步骤），故无未声明内部锁。

---

## 10. 未验证，需真机（不属本卡机内闭环）

- **真机终端二维码扫码**：`rsc.io/qr` 生成的矩阵经半块字符在真实手机扫码器（iOS 相机 / Android 相机）的可扫性、终端宽高比/等宽字体下的观感——机内只能验「有半块字符 + 多行」与「可编码」，**真机可扫性未验证**（拆解 §5 #7、contract §9 附区条 3）。
- **壳侧 Kotlin/Swift 解码**：CLI 产出的载荷被真实 App 解码——归 S4/真机清单 #1（P5=A 壳工程不进本仓）。
- **Windows 终端实际渲染**：半块字符在 conhost/PowerShell 下的显示——本卡只保证纯 Go 可编译与无 ANSI 转义，真机观感未验证。

---

## 11. 红线与不做

- **不改** `internal/proto/pairing.go`（契约冻结）、`internal/client`、`internal/agentd`、`mobile/`、`web/`。
- **不新增** `d_cli→d_policy` 直调（预算 35/35 已满）；配置读取一律走 `Endpoints`。
- **不重实现** bundle 编解码（contract §8.3）。
- **不做** S1/S2/S3/S4/S6 的活：不碰 wire 测试、连接核兑换、构建脚本、壳工程、web。
- **不跑全量测试**（归 integrate）。
- **CHANGELOG 残留**：本卡是用户可见 CLI 增量（新增 `--qr`/`--bundle`），按 CONTRIBUTING「用户可见改动记 CHANGELOG」应加一条；但 CHANGELOG 不在本卡有界文件集（拆解 §3.6 ④只列 cmd 两文件），故**交由 integrate/finish 节点补 Unreleased 条目**（先例：B353 `f2129a25 docs(B353): CHANGELOG Unreleased` 由文档节点补）。本卡不越界。
- 临时文件一律用任务临时目录（`/root/.handoff/tmp/a28e38d6/opencode`），不触碰任务范围外路径。
