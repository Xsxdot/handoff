# B358.7 实现计划：升级三档纪律文本与 OOS 登记（文档 / 纪律，本仓半边）

- **卡**：B358.7（S7，`docs/superpowers/specs/b358-breakdown.md` §3.8）
- **节点**：plan（L3 重档，子系统执行者依契约自写、协调者只审）
- **执行分支**：`cards/B233.1-charter-7`（B358.1–.5 已合入工作树：S1 语义收口 / S2 投影 / S3 唤醒 + session wait / S4 HTTP 端点 / S5 CLI session 命令族——本计划引用的新模型事实全部以树上已落代码为准，出处见 §3）
- **上游锚**：breakdown §3.8（四段式）+ §0 P6 裁决（含 2026-09-12 B363 纠正：三档升级纪律归 handoff 平台按角色注入，不属 charter 仓——本卡只动本仓两文档，不碰 charter 仓）；契约 §8.6（repo 半边）、§8.8（roadmap 登记）；spec §2.5（三档边界）、§7（OOS 五项）、实现决定 8（对外文案去官僚化）
- **角色边界**：本文全部是计划。执行者不写实现代码、不碰卡、不派发；git 操作仅限本卡两文档 + 台账的 add/commit。

---

## 0. 任务概览与 Interfaces

| Task | 内容 | 文件 |
|---|---|---|
| T1 | 基线读数固化（判据先在基线跑） | 台账 |
| T2 | SKILL.md 协作房间纪律节整体改写（逐字替换文本见 §3.2） | `skills/handoff/SKILL.md` |
| T3 | roadmap.md 追加 B358 OOS 登记节（逐字追加文本见 §4.2） | `docs/roadmap.md` |
| T4 | 验收复跑 + 提交（implement 侧） | 台账 + 两文档 |

**Consumes**（执行者只看得见本计划，这是他知道邻居约定的唯一途径）：
- breakdown §3.8 ③ 三条验收 grep 断言（逐字继承，见 §5）；
- spec §2.5 三档判据原文（填补级 / 推翻级 / 人三道门 + 「推只在升级那一刻，拉随时可行」）；
- spec 实现决定 8：对外文案不再出现 kind 名（escalation / deviation / closing / relay）；
- 树上已落命令面（**已查证**，本计划 §3 出处）：`handoff session wait <member> [--since <seq>] [--timeout <dur>]`、`session create <title> --owner <身份>`、`session list [--member <身份>] [--json]`、`session detail <会话> [--json]`、`session archive <会话>`、`session join/leave <会话> <卡号>`、`session send <会话> <正文...> [--ref 锚] [--mention 目标] [--cli 物种 --session id]`；寻址来源是 `--mention` flag（不从正文解析）；`room read` 保留只读对质、`room send` 对旧房间失败。

**Produces**：SKILL.md 新节「协作会话纪律」全文（§3.2 逐字冻结）、roadmap.md「来自 B358 spec」节全文（§4.2 逐字冻结）。无跨卡签名交接（纯文档卡）；spec §4 行为闭环表归属本卡的一行是「主 agent 定时巡场（拉）……S7（纪律）」，由 §3.2 小节「定时巡场（拉）」覆盖。

---

## 1. 判据先在基线跑（T1，已于 plan 节点实测，读数落台账）

执行者开工前**复跑**下表四条，把读数追加进台账（plan 节点 2026-09-12 首测读数在括号里）：

| # | 命令 | 基线预期（plan 节点实测） |
|---|---|---|
| 1 | `awk '/^## 协作房间纪律/{f=1;next} /^## /{f=0} f' skills/handoff/SKILL.md \| grep -c "escalation\|deviation\|closing\|relay"` | 0（实测 0）——改写后以新节名 `/^## 协作会话纪律/` 复跑，仍须 0 |
| 2 | `grep -c "@主 agent\|推翻级\|填补级" skills/handoff/SKILL.md` | 3（实测 3：旧 :586/:608/:618 三行）——改写后 ≥3 |
| 3 | `grep -n "B358" docs/roadmap.md` | 无命中（实测 exit 1）——追加后命中一节 |
| 4 | `git diff --stat` | 仅 `web/src/app/task/Composer.test.tsx`（**与本卡无关的既有脏文件**，实测 1 file）；另有未跟踪 `.commandcode/`、`docs/superpowers/plans/b353-probe-follow-harness.md`、`web/design-qa-sidebar-after.png`、`web/design-qa.md`。**一律不碰**；改写后 diff 相对基线的增量必须是且仅是两文档 + 台账 |

基线判据 1 说明：旧节（:578–666）内本来就没有这四个 kind 名（它们在节外的占座节 ：406-407 与排障表 ：749）——断言 1 锁的是「改写不得把 kind 名带进新节」，不是「清除节内旧词」。

---

## 2. T2：SKILL.md 协作房间纪律节改写

### 2.1 范围与边界

- **只动一节**：从 `## 协作房间纪律：升级简报、收口摘要与重建`（现状 :578）到 `## 会话恢复：从零接管`（现状 :668）之前的空行为止，即现状 :578–666。节的**其余部分（占座节、排障表等）一字不动**——节外残留见 §7 岔口 1。
- **保留的机制**（换载体词，正文骨架不动）：升级简报六段契约（缺段拒收）、收口摘要四段与偏差上报义务、重建四步、叙事文体三问。
- **替换的话术**：卡房间→会话（群）；kind 白名单/书写者矩阵→发言自由 + 身份执法；「升级简报等答复」的载体从 kind 消息→会话 `--mention` 寻址；`handoff room read`→`session list/detail`；B156.2 契约引用→B358 契约引用；新增「会话即工作单元：在场与寻址」「升级三档」两小节与协调者侧新规。
- **禁入词**：新节内不得出现 `escalation` / `deviation` / `closing` / `relay`（breakdown §3.8 ③ 断言 1）。取代史的叙述允许提「kind 白名单已废止」（不含四个禁词，且是 spec §0 取代行的原话级别事实）。

### 2.2 操作（一个 Edit，old_string 从工作树复制）

1. Read `skills/handoff/SKILL.md`，定位 :578 起的旧节，**old_string 取旧节全文**（:578 首行到 :666「……『这是心跳消息，不在白名单』。」行，含其间所有行；从工作树逐字复制，勿凭记忆重构——:667 空行与 :668 下节标题不在替换范围内）。
2. new_string 逐字使用下文（本块即法定产出文本，执行者不得改写措辞；四反引号外栏是为容纳节内 ```bash 围栏，反引号本身不是产出文本的一部分）：

````markdown
## 协作会话纪律：会话、寻址与三档升级

> 载体：本节走 B358 会话层——会话（群）即工作单元，契约见
> `docs/superpowers/specs/b358-contract.md`。B156.2 的房间制模型（卡:房间 1:1、
> 消息 kind 白名单、书写者矩阵）已由 B358 显式废止：旧卡房间只读归档，
> `handoff room read` 保留作历史对质面，`handoff room send` 不再是发言入口。
> 旧 agentd 上没有 `session` 命令族——那时请示与落账退回「回合末四分法落账」
> （`decision open` / `card note` / `card move` / `card needs`），那四条从来不依赖会话层。

### 会话即工作单元：在场与寻址

- **会话（群）= 工作单元**：一场会话由人 / 主 agent 开（`handoff session create --owner <user:名字|agent:名字> <标题>`），卡是会话里的工作项；**进群 ≠ 配人**——`handoff session join <会话> <卡号>` 只建讨论面，配不配协调者仍在卡上按三颗按钮（bind / coordinate / rebind）。一张卡同时只挂一个会话。协调者不拉卡进群——它只管自己那张卡。
- **成员**：人与主 agent 是显式成员（统一记法 `user:<名字>` / `agent:<名字>`）；群里各卡的**当前席位**是它的协调者成员（派生不落表）。执行者不入群不写账——永不做项。
- **发言自由，身份执法**：群里不分内容类别、各自以自己名义说话（`handoff session send <会话> <正文>`；协调者用 `--cli <物种> --session <id>` 成对出示席位身份）。谁说的落账可查，执法只回答两件事：「非成员不能写」（成员 = 显式成员 ∪ 会话内各卡当前席位）与「归档只读」。
- **@寻址投递，无寻址不唤醒**：一条消息的接收人由**发送者写下的寻址**决定，与「群里有谁」无关——`handoff session send` 用 `--mention <目标>`（可重复）寻址：`--mention <卡号>` 唤醒那张卡的**当前席位**（换绑后依然找得到）、`--mention agent:<名字>` / `--mention user:<名字>` 唤醒那位成员。**无寻址的发言落账、进未读，不唤醒任何人**；系统永不按成员集合投递；要叫几个人就写几个 `--mention`，没有「@所有人」。回复寻址（`reply_to` 隐式寻址原作者）在模型里保留，但今天的发言命令还没有设置入口——要唤醒谁，显式 `--mention` 谁，别用「我回了他的消息」代替寻址。
- **知会类结构事件进详情页，不进聊天流**：卡进群/移出、协调者入群、换绑、卡收口或终止、`needs_human` 亮起——这些是结构事实，归 `handoff session detail <会话>` 的 timeline 与 `session list` 的「需要你」标签，**不发进聊天流**。聊天流是人话与升级；结构轨迹在详情页可查。
- **外部会话的订阅入口（推）**：`handoff session wait <成员身份> [--timeout <时长>]` 阻塞订阅「寻址命中我」——首个命中输出一行唤醒 JSON（命中条 + 引用条 + 未读数）后退出 0，到点退出 124。外部会话（人 / 主 agent 自己开着的对话）用它被 @ 醒来：醒—处置—再挂。订阅只读：不落账、不推进未读。
- **定时巡场（拉）**：主 agent 主动巡场用 `handoff session list --member <自己>`（谁需要我）与 `handoff session detail <会话>`（成员状态、派发节点、timeline）。**推只在升级那一刻，拉随时可行**——不被每个动作叫醒。

### 升级三档：填补级自决 / 推翻级 @主 agent / 人掌三道门

| 档 | 判据 | 动作 |
|---|---|---|
| **填补级** | spec 未覆盖的实现选择、顺手小修、新问题落新卡 | 自决：落账、继续，**一个 `--mention` 都不发** |
| **推翻级** | 实测证伪 spec 商定；要动范围 / 验收判据 / 契约语义；超轮僵局；不可逆动作的提案 | 停卡，在会话里 `--mention agent:<主 agent 名字>` 发**升级简报**（六段，见下），等答复 |
| **人** | spec 批准、验收结论、合 main 三道人工门 | 主 agent 判不了就往上一级 `--mention user:<人>` |

判据不由频率兜底：不是「@ 多了就少 @」，是每一条 `--mention` 都必须落在上表判据内。无内容可发就是无 @——群里安静是常态，说明一切按 spec 在走。

**协调者侧的模型级 / 跨仓裁决，先进会话叙事再动手**：改纪律归属、跨仓路由、流程层纠正这类超出单卡范围的决定，先在会话里发一条叙事（三问齐全）把裁决与理由落进群史，再执行动作——裁决只活在协调者自己的会话记忆里，接班者与主 agent 都看不见。本条先例已行（2026-09-12 三档升级纪律归属的纠正，即 B364 前身教训）。

### 升级简报：六段契约（缺段拒收）

推翻级偏差停卡后，在会话里 @主 agent 发升级简报等答复。
简报是「对 spec 的 diff」的载体契约，**缺段拒收，格式是契约不是模板建议**。六段：

1. **一句话**：什么被推翻/要动什么，挂卡号；
2. **商定 vs 实测**：spec 第几条写的什么 ↔ 实际发现什么，证据用链接锚进 timeline（测试原文、文件清单、轮次），正文不贴代码；
3. **根因**：为什么当时没预见（世界变了/当时信息不足/理解偏差）；
4. **影响半径**：动哪条判据/范围/契约，波及哪些卡；
5. **选项 + 后果 + 推荐**：2~3 个，只要可行必含「维持原商定」（防简报变成既成事实追认书）；
6. **停在哪**：不答复卡停在什么状态、多久后再提醒。

拒收判据（发出前自检，收到时执法）：六段任缺一段即退回补写，不得代答、不得跳过；
第 5 段没有「维持原商定」选项视同缺段；证据没有锚进 timeline（正文贴代码或口说
无凭）视同第 2 段缺失。

简报的语言是「对 spec 的 diff」，不是对代码的 diff——spec 是用户注意力驻留过的
地方，这是把用户语境接回来的最短路径。

### 收口摘要：四段与偏差上报义务

卡到终态时，协调者在会话里发的最后一条消息是收口摘要，四段：

1. 做了什么（一段话）；
2. **全部偏差清单**：推翻级裁决结果 + 填补级逐条——自主消化的不许消失，这是安全网执法点；
3. 验收证据指针（timeline 锚）；
4. 残余去向（落 roadmap/新卡的逐条链接）。

父卡收口另带子卡汇总（几张顺利、几张有值得读的偏差、链接）。

执法点两条：

- **协调者侧**：第 2 段不是选填，缺「全部偏差清单」不得发收口；「没有偏差」也是
  一行明写的事实，不许留白。
- **执行者侧**：「执行中偏差如实记入 ledger 收尾段」——填补级偏差账的完备性来源
  （该义务经 charter-implement 纪律块下发，种子见
  `docs/superpowers/disciplines/b156.2-charter-implement-seed.md`）。执行者回合
  收尾必须如实列出本回合自主消化的偏差，协调者汇总进上面的清单；任何一侧漏列，
  安全网即破。

### 换绑与重建四步

限额、载体更换、接管：按「占座」节换绑，接班者只能是当前对话或新叫的机器人：

```bash
handoff card rebind <id> --self      # 这场对话接班
handoff card rebind <id> --launch    # 新叫机器人接班
```

没有 `--to` / `--carrier` / `--expect`。换绑写入即撤销旧席位的发言权与推进权
（防旧会话醒来继续发消息推卡；旧席位身份再发言会被拒、不再被唤醒）。新任协调者
开局先做**重建四步**，一步不跳：

1. **读卡**（字段/附件/验收判据）：`handoff card show <id>`；
2. **读会话史**：`handoff session list --json` 找到该卡所在会话（每行摘要含该会话的卡清单），`handoff session detail <会话>` 读群史与 timeline——会话史天生是交接简报，第一读者是用户，第二读者是你；
3. **读 timeline**：`handoff card show <id>` 的事件流；
4. **读仓内文档**：spec / 契约 / plan 等附件与分支上的相关文档。

配套义务：**凡承重的必须落账**——跨 agent 换载体时旧 transcript 不可携带，账外无
不可再生之物。限额检测/自动拉起/自动换绑归三期。

### 叙事文体：同事协作叙事（三问齐全，正文不贴代码）

技术细节的家是卡 timeline，语义现状不变（timeline 是账，会话是播）。
文体：同事协作叙事——讲发生了什么/为什么/意味着什么；凡引证据给链接，正文不贴代码。

书写者规则：会话里人人以自己名义发言，不再有按内容分类的书写门槛——执法只在
身份：发言者必须身份可验证（人 = 会话身份、协调者 = 席位身份），非成员不能发言。
执行者不入群不写账；无协调者在场时，会话面只有人与主 agent 的对话，机械事实归
详情页 timeline（工单上浮、needs-human 标签），不伪造叙事，接管者唤醒后补写。

文体三条，发出前自检、收到时执法：

1. **三问齐全**：每条叙事消息要答得了三个问题——发生了什么／为什么／意味着什么。
   只报状态不说原因＝缺「为什么」；说了变化不说影响＝缺「意味着什么」；缺任何一问
   即不合规，退回补写。
2. **证据给链接，正文不贴代码**：证据一律给引用锚（git 路径 / timeline 锚 / 卡号 /
   附件），正文出现成块代码或日志原文即不合规——技术细节回卡 timeline 去，那里才是
   它的家。
3. **叙事不是心跳**：「开始了/进行中/跑测试了」这类进度播报不进会话——没消息 = 按
   spec 在走；这条的敌人是心跳与日志，不是对话。

拒收话术（审阅者拿它对任一条具体消息说出 X 才算执法）：「这条缺『为什么』」/
「这条正文贴了代码，证据没给链接」/「这是心跳消息」。
````

3. 替换后自查：`grep -c "^## 协作会话纪律：会话、寻址与三档升级" skills/handoff/SKILL.md` → 1；`grep -c "^## 协作房间纪律" skills/handoff/SKILL.md` → 0；下节 `## 会话恢复：从零接管` 仍在且在新节之后。

### 2.3 新文本的事实出处（每条断言对得上树上事实，已查证）

| 文本断言 | 树上事实 | 出处 |
|---|---|---|
| `session wait <member>` 首个命中输出一行唤醒 JSON 后退出 0、超时 124、订阅只读 | `cmd/session.go#runSessionWait`（条 39–46 注释与实现） | `cmd/session.go:49-70` |
| `--mention` 是寻址来源（不从正文解析） | `sessionSendCmd.Flags().StringArrayVar(&sessionSendMention, "mention", …)` | `cmd/session.go` init 块 |
| 协调者 `--cli/--session` 成对出示席位身份 | `sessionSendCmd` RunE 成对校验 | `cmd/session.go` |
| `room read` 保留只读对质、`room send` 不再是发言入口 | S5 验收：旧房间 `room read` 可读、`room send` 非 0 退出 | breakdown §3.6 ③；`cmd/room.go`（`--kind` flag 面仍在，写路径被 S1 收死） |
| 无 `--reply-to` 设置入口 | `session send` flag 全集 = `--ref/--mention/--cli/--session`；`roomsapi.go` 无 reply_to 处理 | 本计划出稿时 grep 实测 |
| `session list --json` 每行含卡清单 | `SessionSummary.Cards []SessionCard` | 契约 §3.2；`internal/proto/sessions.go` |
| 席位换绑后旧席位被拒、不再被唤醒 | S1/S3 验收反例 | breakdown §3.2/§3.3 ③ |
| 种子文档 `b156.2-charter-implement-seed.md` 在仓 | 文件存在 | `ls` 实测（plan 节点） |

---

## 3. T3：roadmap.md 追加 B358 OOS 登记节

### 3.1 落位

文件末尾追加（roadmap 既有各节无时序纪律，追加末尾最确定；验收只认「有一节、五项逐条在文」）。操作：Read `docs/roadmap.md` 取**末尾两行原文**作 old_string，new_string = 该两行 + 空行 + 下文整节。

### 3.2 逐字追加文本

```markdown

## 来自 B358 spec（2026-09-12，OOS 登记）

- **主 agent 的内部对话面（handoff 自带载体）**：B358 只支持外部会话形态（人 / 主 agent 在自己的对话工具里开会话，经 `session wait` 收寻址命中）；handoff 自己跑的对话面要单独定性「聊天面承载 agent 会话」与 B156.2 原判（聊天面不跑执行者会话）的关系；内部 agent 的 print/continue 唤醒属「参战形态 v2」。来源：`docs/superpowers/specs/b358.md` §7；breakdown §0 P1 裁决。
- **成员状态的心跳写入路径**：B358 只要求「不报不可证实的」——生产只报 `working`（有未过期租约时）/`last_active`/`empty`，`listening` 保留作词表位、随本项启用；精确态若要覆盖外部会话，需单独设计续租与超时。来源：同上 spec §7；breakdown §2.4 澄清 3。
- **多人时代**：多人类成员、登录与权限体系、线程、已读回执、在场状态、消息搜索。B156.2 已记（触发条件：第二个人类成员共用账本），B358 锚点翻转后仍成立；数据形状已预留（消息 author、会话成员集、按成员未读游标），届时是加数据不是改模型。来源：同上 spec §7；`docs/superpowers/specs/2026-08-25-b156.2-collaboration-rooms-design.md` Out of Scope。
- **会话模板与 routines、goal 驱动自主推进**：B156.4 四期输入，与 B156.3 的 roadmap 段合流。来源：同上 spec §7；`docs/superpowers/specs/2026-08-26-b156.3-automation-keystone-design.md` Out of Scope。
- **历史翻页与富文本渲染**：历史翻页是 B289 残余（服务端 `before` 游标已备，缺 UI 与调用）、富文本是 B275 残余；B358 会话面沿用同一消息载体，两残余原样顺延。来源：同上 spec §7；本文件「来自 B289 残余」段。
```

（注意：块首的空行是内容的一部分——新节与文件既有末行之间隔一个空行。）

spec §7 的「永不做」清单（`@all`/`@here`、按成员集合投递、执行者入群写账、聊天面跑执行者会话）**不落 roadmap**——roadmap 头部明文「『永不做』不落这里」。

---

## 4. T4：验收复跑与提交（implement 侧）

1. 复跑 §1 四条判据，读数追加台账（预期：1→0、2→≥3、3→命中一节、4→基线脏文件不变 + 新增两文档与台账）。
2. breakdown §3.8 ③ 断言逐字复跑（见 §5 验收清单），每条命令与输出原文落台账。
3. 行为对账自检（breakdown §3.8 缺陷族 2 的对账义务）：§2.3 表逐行复核——文档说的命令与树上命令面一致，不改命令名、不发明 flag。
4. 提交一次（含台账）：
   ```bash
   git add skills/handoff/SKILL.md docs/roadmap.md docs/superpowers/ledgers/2026-09-12-b358.7-plan-ledger.md docs/superpowers/plans/b358.7-plan.md
   git commit -m "docs(B358.7): SKILL.md 协作会话纪律节改写（会话+寻址+三档升级）+ roadmap B358 OOS 登记"
   ```
   不碰基线脏文件（`Composer.test.tsx` 等 §1 表 #4 所列）；不 amend 跨批。

---

## 5. 验收清单（逐字继承 breakdown §3.8 ③，逐条可判 pass/fail）

| # | 断言 | 判定 |
|---|---|---|
| AC-1 | `awk '/^## 协作会话纪律/{f=1;next} /^## /{f=0} f' skills/handoff/SKILL.md \| grep -n "escalation\|deviation\|closing\|relay"` | 无输出（grep 退出码 1）＝pass（breakdown 原文为「协作房间纪律节范围内零命中」；节名随改写更替，边界判据同形） |
| AC-2 | `grep -c "@主 agent\|推翻级\|填补级" skills/handoff/SKILL.md` | ≥3（基线 3，改写后预期显著高于 3） |
| AC-3 | `grep -n "B358" docs/roadmap.md` 命中一节，且节内五项逐条可 grep：`内部对话面`/`心跳写入路径`/`多人时代`/`会话模板`/`goal 驱动自主推进`/`历史翻页`/`富文本` 各 ≥1 | 全命中＝pass |
| AC-4 | `git diff --stat`（相对基线读数）增量仅 `skills/handoff/SKILL.md`、`docs/roadmap.md`、台账、本计划四个文件 | 增量恰为此集＝pass（breakdown 原文「两文档 + 台账」；本计划自身是 plan 节点法定产出，随同批提交） |

---

## 6. 缺陷族对抗（继承 breakdown §3.8，落验收栏）

1. **生命周期/状态机**：无（纯文档）。
2. **静默失败/误导报错**：无运行时行为，但「文档说的与代码做的必须一致」——由 §2.3 事实出处表 + T4 第 3 步对账承担；integrate 的对账核对（breakdown §4 行为闭环表）是第二道。
3. **跨平台假设**：无。
4. **假红/假绿**：AC-1 锁「新节零 kind 名」（基线已 0，锁的是不引入）；AC-2 基线恰好 =3 处在达标线上——它的判据力弱（改写后数值上涨但断言不因改写变红），真正的红绿来自 AC-3（基线 0 命中）与 AC-4（基线无本卡文件）。台账记下这层强弱关系，防「断言绿了就当改动被锁住」的错觉。
5. **门禁绕过**：无——纪律文本是门禁本身。charter 半边不属本卡（B363 纠正：三档升级纪律归 handoff 平台按角色注入，本节即载体）；「charter 半边归协调者」的 P6 残余表述已被 breakdown §0 P6 裁决行标注作废。
6. **序列化边界**：不适用（无数据字段、无手写投影）。
7. **枚举新值**：不适用（无枚举）。
8. **承重安全属性**：「判据不由频率兜底」（spec 实现决定 6）已写进三档小节正文——这是文本里最承重的一句，改写时不得软化。
9. **webview**：无。

---

## 7. 岔口与缺口（待拍板，不阻塞 T2/T3 执行）

1. **节外残留机制词（待拍板）**：`skills/handoff/SKILL.md` 节外仍引用已死的 room 发言机制——:394 与 :406-407（占座节：`kind != user` 的 `room send`（`escalation` / `closing` 等）出示席位）、:745 与 :749（排障表：`room send --kind escalation` 书写者报错行）。S1 落地后旧房间只读归档、发言入口已是 `session send`，这些行与现实不符；但 breakdown §3.8 ④ 把本卡有界范围钉在「仅协作房间纪律节范围」，AC-1 也只扫节内。选项 a：另立后续文档小卡收口；选项 b：协调者批准本卡越出节界一并改掉（同一文件、AC-4 集合不变，但越出 breakdown 字面）。**默认按 a 执行（只动节内），拍板可翻。**
2. **B364 引用出处（记缺口）**：新规落款的「B364 前身教训」来自协调者派发指令（2026-09-12），本仓文档查无 B364 卡（只有 breakdown P6 内的 B363 纠正注）。若该编号后续变化，改一行落款即可，不动规则正文。
3. **重建四步第 2 步无直查命令（低风险记缺口）**：树上没有「查卡属于哪场会话」的直查命令，按可用面写成 `session list --json` 找卡；后续若 `card show` 补会话归属展示，同步更新该步。
4. **roadmap 落位**：追加文件末尾（既有各节无时序纪律，验收不认位置只认内容）。默认决定，无需拍板。

---

## 8. 五项检查自审（plan 节点）

1. **缺陷族对抗**：§6 逐族有结论（含「无，因为……」）。
2. **序列化边界**：不适用，已声明。
3. **上下文预算**：有界文件集 = `skills/handoff/SKILL.md`（一节）+ `docs/roadmap.md`（追加一节）+ 台账 + 本计划；每 task 圈得出。
4. **类型标注**：不适用（无边界型代码面）；无真机清单项——本卡判据全部本地 grep/awk 可判。
5. **接缝覆盖**：spec §6 接缝清单是代码缝，本卡无代码测试缝。**内部锁声明**：无新增测试文件；文档卡的缝级断言 = AC-1～AC-4 的 grep/awk 断言（锁「旧词清除/新词在文/节边界」，breakdown §3.8 缺陷族 4 明文「不锁文风」）；构造不出更强断言的理由：纪律文本没有声明缝可调用，其行为对账由 §2.3 事实出处表与 integrate 核对承担。

**占位符扫描声明**：本计划无 TBD /「同 Task N」/「加适当……」；两处大段产出文本（§2.2、§3.2）逐字冻结，执行者照抄。测试例外条款不适用（无测试代码）。

**自审三查**：spec 覆盖——spec §7 五项 OOS → T3 逐条；spec §2.5 三档 + 实现决定 8 → T2；spec §4 行为闭环「主 agent 巡场（拉）」纪律半边 → T2「定时巡场（拉）」小节；spec 用户故事 3/4/5/6 的纪律半边全部落在 T2 新文本，机制半边归 S1–S5（已落地）。占位符——已扫。跨 task 签名一致性——无跨卡签名；T2 新文本引用的命令面与 T4 对账表同源（§2.3）。

**图覆盖债**：无。纯文档卡，未触碰任何代码符号，无需 codegraph 查询（breakdown §6 的图债清单不因本卡新增）。

---

## 协调者拍板（补记，2026-09-12）

1. **岔口1（节外残留机制词）：准越界同批改掉。**:394/:406-407（占座节）与 :745/:749（排障表）引用的 `room send --kind escalation` 是本卡家族亲手废除的死机制，留在操作手册里是主动误导；文件集不变（同文件）、AC-1 判定范围不受影响，改写按同款载体词替换纪律执行，逐处记台账。
2. **缺口2：新规落款直接引「B364（账本卡，协调者自主裁决的告知机制）」**——账本可查即档案，不要求仓内另有文档。
3. **缺口3（无「查卡属哪场会话」直查命令）：记父卡 integrate 备档**，`card show` 补会话归属投影时同步。
4. **缺口4（roadmap 落位文件末尾）：准。**
