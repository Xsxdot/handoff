# B233.18 plan：SetupAutomation 房间/keystone/hostapi 构造上移 cmd

前置事实（2026-09-13，基点 59dfb16c5）：

- `SetupAutomation`：internal/agentd/server.go:931。构造序：facade=`ledgerapi.New(st)` →
  automationCursorStore/Load → `s.rooms=collab.New(facade)` + `SetCursorStore(cursor.New(...))` →
  `s.hostAPI=hostapi.NewWithCredentialPathFor(toolchain.CredRelPathFor)` →
  supplier=`coordinatorHomeSupplier{...}`（闭包持 s.conf/s.providers/s.ruleLoader）→
  `coord=opencode.NewCoordinator(s.hostAPI, slog.Default())` → `runner=coordinatorRunner{...}` →
  `resolver=coordinatorSessionRefResolver{...}` → `ks=keystone.New(runner, roomNarrator{c: s.rooms}, facade, attachLocator{...})`
  → `ks.SetSessionRefResolver(resolver)` → `s.pty != nil` 时 `s.ptyGate=ptyapi.New(s.pty)`。
- 调用点：cmd/agentd.go:481 `srv.SetupAutomation(lst)`（注释自认「全仓唯一组装点」）。
- 注入缝先例：`SetConfigPath` / `SetPreviewOwner` / `SetPreviewMirror` / `SetPreviewOpener` /
  `SetRooms`（server.go:1044，现测试缝）。
- cmd 侧在 :481 处已持有 lst（ledger.Store）与 logger。

步骤：

1. cmd/agentd.go 在 `srv.SetupAutomation(lst)` 位置改为「先构造、后注入」：
   - facade、automationCursor 装配允许留在 Server（若收缩后 SetupAutomation 只剩这两件 +
     ptyGate，可整体保留方法名，仅删除三域服务的构造段）。
   - 三域构造段移入 cmd：rooms（collab.New + cursor）、hostAPI、keystone 全家（supplier/
     coord/runner/resolver/narrator/locator）。装配辅助类型定义尽量不搬家：在 agentd 包导出
     构造函数（如 `NewCoordinatorRunner(...)`），cmd 调用；确需搬家的逐个记录理由。
   - Server 暴露构造所需的最小 getter（providers、ruleLoader、conf、pty 等已有导出则复用），
     新增 setter 沿 `SetPreview*` 命名。
2. `SetRooms` 测试缝升格为生产注入缝；keystone/hostAPI 同款新增。字段注释同步改
   （server.go:170/178 的「SetupAutomation 装配」改为「cmd 装配注入」）。
3. `SetupAutomationForTest`（b23314_scheduling_testhelpers_test.go:39）语义保持：装配完成后
   字段状态等价；测试侧可改走新注入缝，但断言不改。
4. grep 证据：`grep -rn "collab.New(\|keystone.New(\|hostapi.New" --include="*.go" .`——
   生产调用点只在 cmd；internal 包内的命中逐个列出并标测试/豁免。
5. 验证：`go build ./...`；`go test -p 1 ./internal/agentd/ ./cmd/`（存量红除外）；结果原文进收尾报告。
6. 提交：`refactor(B233.18): SetupAutomation 房间/keystone/hostapi 构造上移 cmd`。

执行中偏差如实记入收尾段。
