# B156.2.5 实现计划：C5 d_collab·消费恰好一次与注意力读模型（欠账 #2 collab 半 + #3 + A.1/A.5）

**状态**：plan 节点产出（charter 流），2026-08-26。
**有效基线分支**：`feature/b156-workbench`（合并目标以此为准）。本计划在 `cards/B156.2.5-charter` 上执行，HEAD=1a150604（C4 已合入功能线）。
**上游输入**：spec `2026-08-25-b156.2-collaboration-rooms-design.md`（已批准）；契约 `b156.2-contract.md`（含 §11 微修订）；breakdown `b156.2-breakdown.md`（C5 卡四段式 + 岔口一/六裁决 + A.1/A.5）。
**台账**：`docs/superpowers/ledgers/ledger-b156.2.5-plan.md`（本计划所有探针/图闸/变异证据在案）。

本卡做四件事（欠账 #2 collab 半 + #3 全量 + A.1 + A.5 注记）：

1. **Consume 恰好一次的门面路径**：`Service.Consume` 定位消息所在卡 → `client.RecordMessageConsumed`（查重与写入原子性在账本事务内，C2 已锁）——本卡锁「门面路径 + 幂等语义 + 两键 payload」；
2. **注意力读模型**：`Pending`（@定向群级 ∪ 所绑卡房未消费 user 留言）、`Mentions` 补**未消费过滤**（契约 §3.3「未消费提及」缺口）、`ListRooms`（活动降序 + 项目筛选 + 终态沉底 + live）、`MarkRead`/`Unread`（seq 水位游标）；
3. **游标介质**：新子包 `internal/collab/cursor`（A.1 岔口四方案甲：datadir JSON 文件 + tmp+rename 原子写 + 纯内存默认），`Service.SetCursorStore` 注入；
4. **活性翻转的判据加固**：collab 包内注入时钟 `nowFn`，假 client 与注入时钟同一可拨源（协调者 C1 同族教训，变异靶①确定性翻红）。

---

## 0. 冻结物核对（对照冻结物逐条）

| 冻结条目 | 本计划的对应 | 越界？ |
| --- | --- | --- |
| 契约 §3.3 Pending/Consume/Mentions/ListRooms/MarkRead/Unread 注释逐条（Pending 群级@∪所绑卡房未消费、Consume 幂等、Mentions 未消费、ListRooms 条目与排序、MarkRead/Unread 按成员按房间） | T5.1 测试逐条 + T5.2/T5.3 实现 | 否 |
| 契约 §4 读面 7 条（History 两条为 C4 已锁、Pending 两条、Mentions 一条、Consume 两条、payload 两键一条）+ 房间生命周期 5 条（merged 只读/终态只读/ListRooms 排序/live/拆回解冻） | T5.1 测试逐条对应（映射表见 §3 T5.1） | 否 |
| breakdown C5 ③「live=true 当且仅当租约未过期（同一时钟判定——夹具把两侧时钟拨到同一可拨源）」 | T5.1 TestListRoomsLiveFlip：假 client 的 expiresAt 与 collab 注入时钟（nowFn）取同一可拨源 cur，变异靶①确定性翻红；真实集成用确定性真实时钟（+5m 缓冲 / 负 TTL）。**夹具机制裁决见 §4 假红假绿族**（零 d_ledger 表面） | 否（机制是 breakdown 指令的合规替代，理由记台账 L5） |
| breakdown C5 ③「MarkRead 并发原子（tmp+rename 或等效，并发 goroutine 压测无交错损坏）」 | T5.3 TestCursorConcurrentMarkRead（50 goroutine + 新实例读回） | 否 |
| breakdown C5 ③「Unread 计数=last_activity>cursor 未消费口径与 Pending 一致」 | 本计划定语义：**未读 = 该房间 room_message 中 seq > 游标的条数**（水位语义；「打开房间即置已读」MarkRead 到当前最大 seq 后即 0）——A.1 是移交 plan 附区，口径归本卡决策（D3） | 否（A.1 授权） |
| breakdown C5 ③「A.5 护栏注记落代码注释（触发条件照抄，不加过滤参数）」 | room.ReadAllEvents 注释照抄「单流 >10^5 行实测变慢再议」 | 否 |
| breakdown C5 ③「拆回解冻 ReadOnly 回 false 历史不丢」 | T5.1 TestListRoomsUnmergeRestoresAndKeepsHistory（真实 UnmergeCard） | 否 |
| 契约 §3.3「Mentions 收件箱源③：@member 的**未消费**提及」 | T5.1 TestMentionsExcludesConsumed（C4 只锁了成员过滤，本卡补消费维度） | 否（契约原文就要求未消费，C4 未做） |
| 岔口六方案甲「Consume 无效 seq 幂等 nil」 | T5.1 TestConsumeInvalidSeq（回归锁：基线绿、变异红） | 否 |
| 契约 §2.2「根包护栏：读侧判定与过滤助手沉 room 子包」 | room 子包追加 ConsumedSeqs/RoomIDOf/MessageKind/ReadAllEvents/ConsumedEventType | 否 |
| 门面禁令「internal/collab 全包生产零 ledger import」 | 全部实现代码零 ledger import（测试文件豁免照旧） | 否 |
| 金样本 §6（rooms_fixture_test.go / rooms.test.ts 一字不动）、proto DTO 不改 | 本卡零触碰 d_protocol | 否 |

**零触碰点名**（照抄 breakdown + 契约）：d_protocol（金样本/DTO 一字不改）、d_sessions、d_ledger 生产代码（本卡零改动——live 测试不新增 ledger 导出钩子）、d_gateway、d_cli、d_web、target.json（C6/C7 的预定声明不归本卡）。

## 1. 现状查证（全部亲读亲测，出处可复查）

| # | 事实 | 出处 |
| --- | --- | --- |
| 1 | 读侧五法全是空壳：Pending 返回空切片、Consume no-op nil、ListRooms 空切片、MarkRead no-op、Unread 恒 0 | `internal/collab/service.go:128-186`（亲读） |
| 2 | **Mentions 无未消费过滤**：C4 实现只滤成员（契约 §3.3「未消费提及」缺口） | `service.go:145-164`；契约 §3.3 Mentions 注释 |
| 3 | 门面禁令：collab 生产零 ledger import；room 子包经 client 接口取数 | `service.go`/`room/room.go` 全文件亲读 |
| 4 | `ListAllCards` = ListCards{IncludeTerminal:true}，含终态与 Following 非空的并入成员；`GetCard` 不派生 Following | `internal/ledger/api/api.go:58-71`、契约 §11.4 |
| 5 | `EventsFromAsc` 升序、fromSeq 排他、limit<=0 取 1000——分页读全流的基座 | `internal/ledger/events.go:63-103` |
| 6 | `RecordMessageConsumed` 已真实实现：同 (msgSeq,consumer) 幂等、payload 恰两键（message_seq/consumer）、cardID 不参与查重键 | `internal/ledger/rooms.go:37-106`（C2 已锁） |
| 7 | `DriverLeaseOf` 返回 exists+expiresAt、不过滤过期；`RenewDriverLease` 支持负 TTL 造过期行 | `internal/ledger/binding.go:120-189` |
| 8 | **ledger.Store.now 未导出**（store.go:38），collab 测试（package collab）无法设置 Store 注入时钟——跨包同步两侧时钟需新增 ledger 导出测试钩子，超出 d_collab 卡边界。**裁决**：活性翻转走假 client（接缝#2，expiresAt 与 collab 注入时钟同一可拨源），真实集成用确定性真实时钟 | `store.go:38`、`binding_test.go:96`（同包测试直接写 s.now） |
| 9 | `UnmergeCard` 真实实现（拆回解冻可真跑） | `internal/ledger/merge.go:94-110` |
| 10 | 既有测试钩子：newFixture/mustCard/mustBind/mustAnyCard/mustCardWithParent（真 SQLite + Facade，package collab） | `internal/collab/service_test.go`（全量亲读） |
| 11 | proto.RoomSummary wire 形状冻结（ID/Kind/Project/Title/BoundSession/Live/ReadOnly/LastActivity）；金样本测试不含 RoomSummary | `internal/proto/rooms.go:55-68`、`rooms_fixture_test.go` |
| 12 | 图现状：k_collab_Service 持 m_collab_Service + 9 方法（order 10-19）；k_collab_room_fn 7 函数 + k_collab_room_model 1；客户端接口是单一 model 节点 m_collab_client_LedgerClient（无 per-方法节点）；常量不入图（RoomEventType/Kind* 先例） | `codegraph/baseline.json` 亲读（台账 L9） |
| 13 | 图闸基线：`graph check --repo . --view cards-B156.2-charter-4` fails=[] EXIT=0（warn 97 不作判据）；`graph validate` EXIT=0 | 台账 L3 |
| 14 | gofmt 是独立闸：`go build/test/vet` 全绿时 `gofmt -l` 照样能列脏文件（协调者实况 + 本计划 P8 复现） | 台账 P8 |

**基线绿（plan 轮实测）**：`go test ./internal/collab/...`=ok；`graph check --view cards-B156.2-charter-4` fails=[]（台账 L2/L3）。

## 2. 有界文件集（上下文预算检查）

| 文件 | 动作 | 归属 task |
| --- | --- | --- |
| `internal/collab/room/room.go` | 追加 ConsumedEventType/ReadAllEvents/ConsumedSeqs/RoomIDOf/MessageKind | T5.2 |
| `internal/collab/cursor/cursor.go` | **新建**：游标介质 Store（内存/文件双形态） | T5.2 |
| `internal/collab/service.go` | 改写：nowFn + cursor 字段 + SetCursorStore + Pending/Consume/Mentions/ListRooms/MarkRead/Unread 填肉 | T5.2 |
| `internal/collab/readmodel_test.go` | **新建**：C5 读模型测试（接缝#1 入口 + 接缝#2 假 client） | T5.1（红）+ T5.4（绿） |
| `internal/collab/cursorfile_test.go` | **新建**：游标文件介质测试（持久化 + 并发原子 + 字面量 pin） | T5.3（含 SetCursorStore 依赖） |
| `codegraph/best.json` | containers 加 k_collab_cursor_fn/k_collab_cursor_Store → d_collab | T5.5 |
| `codegraph/diffs/cards-B156.2-charter-4.json` | containersAdded + nodesAdded + edgesAdded（累计视图，沿用既有文件，**不许另起新视图**） | T5.5 |

圈得出：五个代码/测试文件 + 两个图数据文件，全在 d_collab 域内（本卡零 d_ledger 改动）。测试范围最小化：只跑 `./internal/collab/...`（主）；`go build ./...`（依赖面回归）；图闸 `graph check --view cards-B156.2-charter-4`；变异复验限定 `-run` 单测。全仓 go test 不属于本卡。

## 3. Task 分解

### T5.1 立红：读模型测试（锁缝断言步骤，法定红绿）

**Interfaces**

- Produces（本卡锁定的行为，全部从入站门面进入）：
  - `(s *Service) Pending(consumer string) ([]proto.LedgerEvent, error)`（签名不变）
  - `(s *Service) Consume(seq int64, consumer string) error`（签名不变）
  - `(s *Service) Mentions(member string, afterSeq int64, limit int) ([]proto.LedgerEvent, error)`（签名不变）
  - `(s *Service) ListRooms(project string) ([]proto.RoomSummary, error)`（签名不变）
  - `(s *Service) MarkRead(member, roomID string, uptoSeq int64) error`（签名不变）
  - `(s *Service) Unread(member, roomID string) (int, error)`（签名不变）
- Consumes（既有，精确签名，见 §1）：
  - `func (s *Store) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]Event, error)`（events.go:63）
  - `func (s *Store) RecordMessageConsumed(cardID string, msgSeq int64, consumer string) error`（rooms.go:60）
  - `func (s *Store) RenewDriverLease(session string, ttl time.Duration) (bool, error)`（binding.go:120）
  - `func (s *Store) MergeCards(ids []string, into, actor string) error` / `UnmergeCard(id, actor string) error`（merge.go:15/94）
  - `func (s *Store) AddComment(cardID, body, kind, actor string) (Event, error)`（events.go:167）
  - client 接口 `ListAllCards/DriverLease/RecordMessageConsumed`（契约 §3.4 逐字）

**判据→测试映射表（契约 §4 条目号）**：

| 判据 | 测试 |
| --- | --- |
| Pending 返回「mentions∋consumer 群级 ∪ 所绑卡房未消费留言」且每条不带本人标记 | TestPendingGroupMentionAndConsume / TestPendingCardRoomUserMessages |
| Mentions 未消费提及（含 C4 已锁的成员过滤） | TestMentionsExcludesConsumed（既有 TestMentionsFiltersByMember 保持绿） |
| Consume 同参重试 nil 无第二条 | TestConsumeIdempotentSameArgs |
| Consume 他人已消费同条 nil、各消费者一条标记 | TestConsumeSecondConsumerOwnMarker |
| Consume 无效 seq 幂等 nil 不落标记（岔口六方案甲） | TestConsumeInvalidSeq |
| message_consumed payload 恰两键 | TestConsumePayloadTwoKeys |
| ListRooms LastActivity 降序 + project 过滤 + global 恒在 | TestListRoomsSortsByActivityDescAndProjectFilter |
| 终态卡沉底 | TestListRoomsTerminalSinks |
| merged_into 非空 / 终态 → ReadOnly | TestListRoomsReadOnlyFlags |
| live=true 当且仅当租约未过期（同一可拨源） | TestListRoomsLiveFlip（假 client）+ TestListRoomsLiveRealStore（真实集成） |
| 拆回解冻 ReadOnly 回 false、历史不丢 | TestListRoomsUnmergeRestoresAndKeepsHistory |
| MarkRead/Unread 水位语义、按成员按房间独立 | TestMarkReadUnreadWatermark / TestMarkReadPerRoomAndMember |

**步骤**

1. （~2min）基线复核：`go test ./internal/collab/...` 全绿（台账 L2）；确认 Pending/Consume/ListRooms/MarkRead/Unread 仍是空壳（§1 表 #1）。
2. （~6min）写失败测试：创建 `internal/collab/readmodel_test.go`（下方完整代码块），替换测试文件里的 `newFixture` 等既有助手照旧复用。**本文件只用 Service 方法 + 既有符号（room/ledger/proto），不引用尚不存在的 SetCursorStore / cursor 包——基线可编译**；`TestListRoomsLiveFlip` 引用的 `nowFn` 在 T5.2 才进 service.go，T5.1 的红色步骤以「编译失败=红」不成立，**顺序依赖见下**。
   > **顺序依赖**：本 task 与 T5.2/T5.3 的提交同批：先写测试（红）→ 沉放助手 + 建游标包 + 改写 service.go → 一起跑绿 → 一起提交。红绿是 T5.1 红 → T5.4 绿的一个周期，中间 T5.2/T5.3 为内部实现步骤不配独立周期。`nowFn` 依赖：T5.1 红跑前，在测试文件临时声明 `var nowFn = time.Now`（探针同款，P1 已验证 14 支红）；T5.2 落地后删除该临时声明（service.go 已提供），避免重复声明——计划轮实测路径见台账 P1/P3。
3. （~1min）跑红：`go test ./internal/collab/ -run 'TestPending|TestConsume|TestMentions|TestListRooms|TestMarkRead' -v`。预期（plan 轮探针实测原文，台账 P1）：**14 支 FAIL**（PendingGroupMention「Pending 应只含…: []」/PendingCardRoom「Pending 应只含…: []」/ConsumeIdempotent「不得产生第二条标记: 0」/ConsumeSecondConsumer「A 应恰一条: 0」/ConsumePayloadTwoKeys「没有消费标记事件」/MentionsExcludes「消费后 Mentions 应清空:[…]」/ListRoomsSorts「列表条目数不符: []」/TerminalSinks「条目数: []」/ReadOnlyFlags「并入卡应只读」/LiveFlip（stub 空列表 panic 或断言）/LiveRealStore/Unmerge「并入卡房间应只读」/MarkReadUnread「未读应 3: 0」/MarkReadPerRoom「卡B 未读应 1: 0」）+ **1 支 PASS（回归锁）**：ConsumeInvalidSeq（stub 本就无副作用，红只能在变异上，见 T5.5 变异④）。红文贴台账。
4. （~1min）台账记红文。
5. 本 task 不单独提交（与 T5.4 绿同批，见步骤 2 顺序依赖）。

**完整代码块——`internal/collab/readmodel_test.go`（新建；plan 轮已全文装回验证：红侧 14 支红 + 绿侧全绿，证据台账 P1/P3）**

> 该文件内容与 `internal/collab/service_test.go`（既有，不改）同包；T5.1 红跑时在文件底部临时加一行 `var nowFn = time.Now`（供 TestListRoomsLiveFlip 编译），T5.2 删除。下方代码块为**不含临时 nowFn** 的最终形态。

```go
// 读模型测试（B156.2 C5）：Pending/Consume/Mentions 未消费、ListRooms、
// MarkRead/Unread、live 活性翻转。全部从入站门面（Service 方法）进入
// （spec 测试接缝清单 #1）；排序/筛选/翻转等需精确控制时钟与事件时刻的
// 逻辑测试用假 client（接缝 #2 测试替身），真实集成用真 SQLite。
//
// 本文件 import internal/ledger(/api) 仅存在于 _test.go：图边采集排除测试
// 文件，不构成 d_collab→d_ledger 生产边。
package collab

import (
	"encoding/json"
	"testing"
	"time"

	"github.com/Xsxdot/handoff/internal/collab/room"
	"github.com/Xsxdot/handoff/internal/ledger"
	"github.com/Xsxdot/handoff/internal/proto"
)

// fakeLC 是 client.LedgerClient 的测试替身（接缝 #2）：可控卡片、事件与租约，
// 供 ListRooms 排序/筛选/活性翻转等需要精确时刻的读模型测试使用。全部方法
// 保持与接口一一对应；未用到的写方法返回 nil（本文件不测它们）。
type fakeLC struct {
	cards  []proto.Card
	events []proto.LedgerEvent
	leases map[string]time.Time // session -> expiresAt
}

func (f *fakeLC) GetCard(id string) (proto.Card, error) {
	for _, c := range f.cards {
		if c.ID == id {
			return c, nil
		}
	}
	return proto.Card{}, ledger.ErrNotFound
}
func (f *fakeLC) ListActiveCards(project string) ([]proto.Card, error) {
	var out []proto.Card
	for _, c := range f.cards {
		if project != "" && c.Project != project {
			continue
		}
		if room.IsTerminalStatus(c.Status) {
			continue
		}
		out = append(out, c)
	}
	return out, nil
}
func (f *fakeLC) ListAllCards(project string) ([]proto.Card, error) {
	var out []proto.Card
	for _, c := range f.cards {
		if project != "" && c.Project != project {
			continue
		}
		out = append(out, c)
	}
	return out, nil
}
func (f *fakeLC) RecordRoomMessage(cardID string, msg proto.RoomMessage, actor string) (int64, error) {
	return 0, nil
}
func (f *fakeLC) RecordMessageConsumed(cardID string, msgSeq int64, consumer string) error {
	return nil
}
func (f *fakeLC) EventsFromAsc(cardIDs []string, fromSeq int64, limit int) ([]proto.LedgerEvent, error) {
	if limit <= 0 {
		limit = 1000
	}
	var out []proto.LedgerEvent
	for _, ev := range f.events {
		if ev.Seq <= fromSeq {
			continue
		}
		if len(cardIDs) > 0 {
			hit := false
			for _, id := range cardIDs {
				if ev.CardID == id {
					hit = true
					break
				}
			}
			if !hit {
				continue
			}
		}
		out = append(out, ev)
		if len(out) >= limit {
			break
		}
	}
	return out, nil
}
func (f *fakeLC) BindDriver(id, session, carrier, expect string) error {
	return nil
}
func (f *fakeLC) DriverLease(session string) (time.Time, bool, error) {
	exp, ok := f.leases[session]
	return exp, ok, nil
}

// fakeRoomMsg 造一条房间消息事件（群房间 roomID 带 project:/global 前缀时
// 为无卡事件，同 ledger.RecordRoomMessage 语义）。
func fakeRoomMsg(seq int64, roomID string, at time.Time) proto.LedgerEvent {
	payload, _ := json.Marshal(proto.RoomMessage{Room: roomID, Kind: proto.RoomMsgUser, Body: "x"})
	cardID := roomID
	if roomID == "global" || len(roomID) > 8 && roomID[:8] == "project:" {
		cardID = ""
	}
	return proto.LedgerEvent{Seq: seq, CardID: cardID, Type: room.RoomEventType, Actor: "user:sy", Payload: payload, CreatedAt: at}
}

// countConsumedFor 数该消费者全流的 message_consumed 标记数。
func countConsumedFor(t *testing.T, st *ledger.Store, consumer string) int {
	t.Helper()
	events, err := st.EventsFromAsc([]string{}, 0, 10000)
	if err != nil {
		t.Fatal(err)
	}
	n := 0
	for _, ev := range events {
		if ev.Type == ledger.EvMessageConsumed && ev.Actor == consumer {
			n++
		}
	}
	return n
}

// TestPendingGroupMentionAndConsume 契约 §4「Pending 返回群级@」：@ 到
// consumer 的未消费群消息进 Pending；消费后消失；未提及的不进。
func TestPendingGroupMentionAndConsume(t *testing.T) {
	svc, _ := newFixture(t)
	consumer := "cli:a@h"
	seq, err := svc.Send("project:handoff",
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "@cli:a@h 看", Mentions: []string{consumer}}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Send("project:handoff",
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "无提及"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	pending, err := svc.Pending(consumer)
	if err != nil {
		t.Fatal(err)
	}
	if len(pending) != 1 || pending[0].Seq != seq {
		t.Fatalf("Pending 应只含 @ 到 consumer 的未消费群消息: %+v", pending)
	}
	if err := svc.Consume(seq, consumer); err != nil {
		t.Fatal(err)
	}
	after, err := svc.Pending(consumer)
	if err != nil {
		t.Fatal(err)
	}
	if len(after) != 0 {
		t.Fatalf("消费后 Pending 应清空: %+v", after)
	}
}

// TestPendingCardRoomUserMessages 契约 §4「其绑定卡房间的未消费留言」：
// 未绑定卡房间的留言不进；协调者类消息（非 user 留言）不进。
func TestPendingCardRoomUserMessages(t *testing.T) {
	svc, st := newFixture(t)
	consumer := "cli:a@h"
	bound := mustCard(t, svc, st, "绑定卡")
	other := mustCard(t, svc, st, "非绑定卡")
	mustBind(t, st, bound.ID, consumer)
	mustBind(t, st, other.ID, "cli:b@h")

	msgSeq, err := svc.Send(bound.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "给我留言"}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Send(other.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "别人的卡留言"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if _, err := svc.Send(bound.ID, proto.RoomMessage{Kind: proto.RoomMsgEscalation, Body: "简报"}, consumer); err != nil {
		t.Fatal(err)
	}
	pending, err := svc.Pending(consumer)
	if err != nil {
		t.Fatal(err)
	}
	if len(pending) != 1 || pending[0].Seq != msgSeq {
		t.Fatalf("Pending 应只含绑定卡房间的未消费 user 留言: %+v", pending)
	}
}

// TestConsumeIdempotentSameArgs 契约 §4「同参重试返回 nil 且不产生第二条」。
func TestConsumeIdempotentSameArgs(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	seq, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "留言"}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	consumer := "cli:a@h"
	if err := svc.Consume(seq, consumer); err != nil {
		t.Fatal(err)
	}
	if err := svc.Consume(seq, consumer); err != nil {
		t.Fatalf("同参重试应幂等 nil，got %v", err)
	}
	if n := countConsumedFor(t, st, consumer); n != 1 {
		t.Fatalf("同参重试不得产生第二条标记: %d", n)
	}
}

// TestConsumeSecondConsumerOwnMarker 契约 §4「他人已消费的同一条返回 nil」：
// 各消费者一条自己的标记，互不顶替（账本侧 C2 已锁，此处锁门面路径）。
func TestConsumeSecondConsumerOwnMarker(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	seq, _ := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "留言"}, "user:sy")
	if err := svc.Consume(seq, "cli:a@h"); err != nil {
		t.Fatal(err)
	}
	if err := svc.Consume(seq, "cli:b@h"); err != nil {
		t.Fatalf("他人已消费的同一条对新消费者是首次写入，got %v", err)
	}
	if n := countConsumedFor(t, st, "cli:a@h"); n != 1 {
		t.Fatalf("A 应恰一条: %d", n)
	}
	if n := countConsumedFor(t, st, "cli:b@h"); n != 1 {
		t.Fatalf("B 应恰一条: %d", n)
	}
}

// TestConsumeInvalidSeq 岔口六方案甲：seq 不存在或非 room_message 一律幂等
// nil 且不落标记。基线探针绿（stub 本就无副作用），本测试是回归锁——
// 变异靶：实现写标记或返回错误即红。
func TestConsumeInvalidSeq(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	if _, err := st.AddComment(card.ID, "普通评论", "普通", "t"); err != nil {
		t.Fatal(err)
	}
	if err := svc.Consume(999999, "cli:a@h"); err != nil {
		t.Fatalf("不存在的 seq 应幂等 nil，got %v", err)
	}
	events, err := st.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	var commentSeq int64
	for _, ev := range events {
		if ev.Type == ledger.EvComment {
			commentSeq = ev.Seq
		}
	}
	if err := svc.Consume(commentSeq, "cli:a@h"); err != nil {
		t.Fatalf("非 room_message seq 应幂等 nil，got %v", err)
	}
	if n := countConsumedFor(t, st, "cli:a@h"); n != 0 {
		t.Fatalf("无效 seq 不得落标记: %d", n)
	}
}

// TestConsumePayloadTwoKeys 契约 §4「payload 含 message_seq 与 consumer 两键」。
func TestConsumePayloadTwoKeys(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	seq, _ := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "留言"}, "user:sy")
	if err := svc.Consume(seq, "cli:a@h"); err != nil {
		t.Fatal(err)
	}
	events, err := st.EventsFromAsc([]string{card.ID}, 0, 100)
	if err != nil {
		t.Fatal(err)
	}
	for _, ev := range events {
		if ev.Type != ledger.EvMessageConsumed {
			continue
		}
		var m map[string]any
		if err := json.Unmarshal(ev.Payload, &m); err != nil {
			t.Fatal(err)
		}
		if len(m) != 2 || m["message_seq"] != float64(seq) || m["consumer"] != "cli:a@h" {
			t.Fatalf("消费标记 payload 应恰 message_seq/consumer 两键: %v", m)
		}
		return
	}
	t.Fatal("没有消费标记事件")
}

// TestMentionsExcludesConsumed 契约 §3.3「未消费提及」：消费后从 Mentions 消失。
func TestMentionsExcludesConsumed(t *testing.T) {
	svc, st := newFixture(t)
	mustAnyCard(t, svc, st)
	seq, err := svc.Send("project:handoff",
		proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "@B145 看", Mentions: []string{"B145"}}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	hit, err := svc.Mentions("B145", 0, 0)
	if err != nil || len(hit) != 1 {
		t.Fatalf("未消费提及应命中: %v %d", err, len(hit))
	}
	if err := svc.Consume(seq, "B145"); err != nil {
		t.Fatal(err)
	}
	after, err := svc.Mentions("B145", 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(after) != 0 {
		t.Fatalf("消费后 Mentions 应清空: %+v", after)
	}
}

// TestListRoomsSortsByActivityDescAndProjectFilter 契约 §4「LastActivity 降序、
// project 过滤、global 恒在」。
func TestListRoomsSortsByActivityDescAndProjectFilter(t *testing.T) {
	base := time.Date(2026, 8, 26, 10, 0, 0, 0, time.UTC)
	fake := &fakeLC{
		cards: []proto.Card{
			{ID: "B1", Title: "卡一", Status: "进行中", Project: "p1", CreatedAt: base, UpdatedAt: base},
			{ID: "B2", Title: "卡二", Status: "进行中", Project: "p1", CreatedAt: base, UpdatedAt: base},
			{ID: "B3", Title: "卡三", Status: "进行中", Project: "p2", CreatedAt: base, UpdatedAt: base},
		},
		events: []proto.LedgerEvent{
			fakeRoomMsg(1, "B1", base.Add(1*time.Hour)),
			fakeRoomMsg(2, "project:p1", base.Add(2*time.Hour)),
			fakeRoomMsg(3, "B2", base.Add(3*time.Hour)),
		},
	}
	svc := New(fake)
	all, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	wantAll := []string{"B2", "project:p1", "B1", "B3", "project:p2", "global"}
	if len(all) != len(wantAll) {
		t.Fatalf("列表条目数不符: %+v", all)
	}
	for i, id := range wantAll {
		if all[i].ID != id {
			t.Fatalf("第 %d 项应为 %s: %+v", i, id, all)
		}
	}
	p1, err := svc.ListRooms("p1")
	if err != nil {
		t.Fatal(err)
	}
	wantP1 := []string{"B2", "project:p1", "B1", "global"}
	if len(p1) != len(wantP1) {
		t.Fatalf("p1 筛选条目数不符: %+v", p1)
	}
	for i, id := range wantP1 {
		if p1[i].ID != id {
			t.Fatalf("p1 第 %d 项应为 %s: %+v", i, id, p1)
		}
	}
}

// TestListRoomsTerminalSinks 契约 §4「终态卡沉底」：有活动的终态卡房间沉到
// 列表尾部且只读。
func TestListRoomsTerminalSinks(t *testing.T) {
	base := time.Date(2026, 8, 26, 10, 0, 0, 0, time.UTC)
	fake := &fakeLC{
		cards: []proto.Card{
			{ID: "BA", Title: "活跃卡", Status: "进行中", Project: "p1", CreatedAt: base, UpdatedAt: base},
			{ID: "BT", Title: "终态卡", Status: ledger.StatusDone, Project: "p1", CreatedAt: base, UpdatedAt: base.Add(2 * time.Hour)},
		},
		events: []proto.LedgerEvent{
			fakeRoomMsg(1, "BT", base.Add(3*time.Hour)),
			fakeRoomMsg(2, "BA", base.Add(1*time.Hour)),
		},
	}
	svc := New(fake)
	rooms, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	if len(rooms) != 4 {
		t.Fatalf("条目数: %+v", rooms)
	}
	if rooms[0].ID != "BA" {
		t.Fatalf("活跃卡应排最前: %+v", rooms)
	}
	last := rooms[len(rooms)-1]
	if last.ID != "BT" || !last.ReadOnly {
		t.Fatalf("终态卡应沉底且只读: %+v", rooms)
	}
}

// TestListRoomsReadOnlyFlags 契约 §4「merged_into 非空 / 终态 → ReadOnly」。
func TestListRoomsReadOnlyFlags(t *testing.T) {
	base := time.Date(2026, 8, 26, 10, 0, 0, 0, time.UTC)
	fake := &fakeLC{
		cards: []proto.Card{
			{ID: "B1", Title: "普通卡", Status: "进行中", Project: "p1", CreatedAt: base, UpdatedAt: base},
			{ID: "B2", Title: "并入卡", Status: "进行中", Project: "p1", Following: "B9", CreatedAt: base, UpdatedAt: base},
			{ID: "B3", Title: "终态卡", Status: ledger.StatusClosed, Project: "p1", CreatedAt: base, UpdatedAt: base},
		},
	}
	svc := New(fake)
	rooms, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID := map[string]proto.RoomSummary{}
	for _, r := range rooms {
		byID[r.ID] = r
	}
	if byID["B1"].ReadOnly {
		t.Fatal("普通卡不应只读")
	}
	if !byID["B2"].ReadOnly {
		t.Fatal("并入卡应只读")
	}
	if !byID["B3"].ReadOnly {
		t.Fatal("终态卡应只读")
	}
}

// TestListRoomsLiveFlip 活性翻转：租约未过期 live=true、过期后 live=false。
// 假 client 的租约 expiresAt 与 collab 注入时钟（nowFn）取自同一可拨源 cur，
// 基线选在过去：变异把判据改读 time.Now() 时真实时钟恒在 expiresAt 之后，
// live=true 断言必然翻红——两侧时钟不同源的假绿被结构上排除（协调者 C1
// 同族教训）。变异靶见 plan 变异①。
func TestListRoomsLiveFlip(t *testing.T) {
	base := time.Date(2026, 1, 1, 0, 0, 0, 0, time.UTC)
	cur := base
	oldNow := nowFn
	nowFn = func() time.Time { return cur }
	t.Cleanup(func() { nowFn = oldNow })

	fake := &fakeLC{
		cards: []proto.Card{
			{ID: "B1", Title: "绑定卡", Status: "进行中", Project: "p1", DriverSession: "cli:a@h", CreatedAt: base, UpdatedAt: base},
		},
		leases: map[string]time.Time{"cli:a@h": base.Add(5 * time.Minute)},
	}
	svc := New(fake)

	rooms, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID := map[string]proto.RoomSummary{}
	for _, r := range rooms {
		byID[r.ID] = r
	}
	if !byID["B1"].Live || byID["B1"].BoundSession != "cli:a@h" {
		t.Fatalf("租约未过期应 live=true: %+v", byID["B1"])
	}
	cur = base.Add(10 * time.Minute) // 同一可拨源前拨
	rooms2, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID2 := map[string]proto.RoomSummary{}
	for _, r := range rooms2 {
		byID2[r.ID] = r
	}
	if byID2["B1"].Live {
		t.Fatalf("租约过期后应 live=false: %+v", byID2["B1"])
	}
}

// TestListRoomsLiveRealStore 真实集成：未续租 live=false；有效租约 live=true；
// 过期租约 live=false（真实时钟下确定性：+5m 缓冲、负 TTL 已过期）。
func TestListRoomsLiveRealStore(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	mustBind(t, st, card.ID, "cli:a@h")

	rooms, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID := map[string]proto.RoomSummary{}
	for _, r := range rooms {
		byID[r.ID] = r
	}
	if byID[card.ID].Live {
		t.Fatal("未续租应 live=false")
	}
	if ok, err := st.RenewDriverLease("cli:a@h", 5*time.Minute); err != nil || !ok {
		t.Fatalf("续租: %v %v", ok, err)
	}
	rooms2, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID2 := map[string]proto.RoomSummary{}
	for _, r := range rooms2 {
		byID2[r.ID] = r
	}
	if !byID2[card.ID].Live {
		t.Fatal("有效租约应 live=true")
	}
	if ok, err := st.RenewDriverLease("cli:a@h", -time.Minute); err != nil || !ok {
		t.Fatalf("负 TTL 造过期行: %v %v", ok, err)
	}
	rooms3, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID3 := map[string]proto.RoomSummary{}
	for _, r := range rooms3 {
		byID3[r.ID] = r
	}
	if byID3[card.ID].Live {
		t.Fatal("过期租约应 live=false")
	}
}

// TestListRoomsUnmergeRestoresAndKeepsHistory 契约 §4「拆回解冻」：并入时
// 房间 ReadOnly=true；UnmergeCard 后回 false；历史消息不丢。
func TestListRoomsUnmergeRestoresAndKeepsHistory(t *testing.T) {
	svc, st := newFixture(t)
	carrier := mustCard(t, svc, st, "承载卡")
	member := mustCard(t, svc, st, "并入卡")
	seq, err := svc.Send(member.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "并入前留言"}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	if err := st.MergeCards([]string{member.ID}, carrier.ID, "test"); err != nil {
		t.Fatal(err)
	}
	rooms, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID := map[string]proto.RoomSummary{}
	for _, r := range rooms {
		byID[r.ID] = r
	}
	if !byID[member.ID].ReadOnly {
		t.Fatal("并入卡房间应只读")
	}
	if _, err := svc.Send(member.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy"); err != ErrReadOnly {
		t.Fatalf("并入房间发送应 ErrReadOnly: %v", err)
	}
	if err := st.UnmergeCard(member.ID, "test"); err != nil {
		t.Fatal(err)
	}
	rooms2, err := svc.ListRooms("")
	if err != nil {
		t.Fatal(err)
	}
	byID2 := map[string]proto.RoomSummary{}
	for _, r := range rooms2 {
		byID2[r.ID] = r
	}
	if byID2[member.ID].ReadOnly {
		t.Fatal("拆回后房间应解除只读")
	}
	history, err := svc.History(member.ID, 0, 0)
	if err != nil {
		t.Fatal(err)
	}
	if len(history) != 1 || history[0].Seq != seq {
		t.Fatalf("拆回后历史消息不丢: %+v", history)
	}
}

// TestMarkReadUnreadWatermark 未读水位：未读=该房间 seq>游标的 room_message
// 条数；MarkRead 到某 seq 后只数之后的；读尽即 0。
func TestMarkReadUnreadWatermark(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	seqs := []int64{}
	for _, body := range []string{"一", "二", "三"} {
		seq, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: body}, "user:sy")
		if err != nil {
			t.Fatal(err)
		}
		seqs = append(seqs, seq)
	}
	if n, err := svc.Unread("user:sy", card.ID); err != nil || n != 3 {
		t.Fatalf("未读应 3: %v %d", err, n)
	}
	if err := svc.MarkRead("user:sy", card.ID, seqs[1]); err != nil {
		t.Fatal(err)
	}
	if n, err := svc.Unread("user:sy", card.ID); err != nil || n != 1 {
		t.Fatalf("读到第二条后未读应 1: %v %d", err, n)
	}
	if err := svc.MarkRead("user:sy", card.ID, seqs[2]); err != nil {
		t.Fatal(err)
	}
	if n, err := svc.Unread("user:sy", card.ID); err != nil || n != 0 {
		t.Fatalf("读尽后未读应 0: %v %d", err, n)
	}
}

// TestMarkReadPerRoomAndMember 游标按成员按房间独立。
func TestMarkReadPerRoomAndMember(t *testing.T) {
	svc, st := newFixture(t)
	cardA := mustCard(t, svc, st, "卡A")
	cardB := mustCard(t, svc, st, "卡B")
	seqA, _ := svc.Send(cardA.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "a1"}, "user:sy")
	if _, err := svc.Send(cardB.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "b1"}, "user:sy"); err != nil {
		t.Fatal(err)
	}
	if err := svc.MarkRead("user:sy", cardA.ID, seqA); err != nil {
		t.Fatal(err)
	}
	if n, _ := svc.Unread("user:sy", cardA.ID); n != 0 {
		t.Fatalf("卡A 已读应 0: %d", n)
	}
	if n, _ := svc.Unread("user:sy", cardB.ID); n != 1 {
		t.Fatalf("卡B 未读应 1: %d", n)
	}
	if n, _ := svc.Unread("cli:a@h", cardA.ID); n != 1 {
		t.Fatalf("另一成员游标独立，应 1: %d", n)
	}
}
```

### T5.2 实现：room 助手 + cursor 子包 + service.go 填肉（内部实现步骤，不配独立红绿）

**Interfaces**

- Produces（本卡新导出符号，均入图，见 T5.5）：
  - room：`const ConsumedEventType = "message_consumed"`、`func ReadAllEvents(lc client.LedgerClient, fromSeq int64) ([]proto.LedgerEvent, error)`、`func ConsumedSeqs(events []proto.LedgerEvent, consumer string) map[int64]bool`、`func RoomIDOf(ev proto.LedgerEvent) string`、`func MessageKind(ev proto.LedgerEvent) string`
  - cursor：`type Store`、`func New(path string) *Store`、`(s *Store) MarkRead(member, roomID string, uptoSeq int64) error`、`(s *Store) Cursor(member, roomID string) (int64, error)`
  - collab：`(s *Service) SetCursorStore(st *cursor.Store)`（A.1 注入机制，契约 Service 方法签名一字不变）
- Consumes：`client.LedgerClient.ListAllCards/EventsFromAsc/DriverLease/RecordMessageConsumed`（契约 §3.4 逐字）。

**行为语义（实现前读三遍）**

1. `ReadAllEvents` 分页读全流：每页 1000（EventsFromAsc limit 语义，events.go:63-103），以页尾 seq 为下页游标，页短于 1000 即流尽。A.5 护栏注记照抄「单流 >10^5 行实测变慢再议」。
2. `ConsumedSeqs` 按事件 actor 列（=consumer）粗筛 + 载荷 message_seq/consumer 精确匹配（载荷才是权威，rooms.go:37-40 注释同款）。
3. `RoomIDOf` 卡级看 card_id、群级看载荷 Room；`MessageKind` 解载荷 kind，失败空串。
4. `cursor.Store` 全部读写经互斥锁；文件介质 tmp+rename 原子写；`loaded` 标志保证首次访问才读盘。
5. `Service.Pending` 所绑卡房间 = ListAllCards 里 DriverSession==consumer 的**全部**卡（含终态/并入——只读冻结防新写，不抹旧留言，杜绝无人消费黑洞）；卡级只收 kind==user，群级只收 @ 定向。
6. `Service.Consume` 全流按 seq 定位消息所在卡，传 `ev.CardID`（群级为空串=项目级标记）；seq 不存在或非 room_message → 幂等 nil 不落标记（岔口六方案甲）。
7. `Service.Mentions` 补未消费过滤 + 全流分页（C4 只滤成员）。
8. `Service.ListRooms` 条目=全部卡房间（终态沉底）+ 各项目 project:<name> + global；LastActivity=最新 room_message 时刻，卡房间无消息回退 UpdatedAt；Live 按不同绑定会话去重读租约；DriverLease 错误向上传播。
9. `Service.Unread` = 该房间 room_message 中 seq>游标的条数；`MarkRead` 单调只进不退。

**步骤**

1. （~2min）`mkdir -p internal/collab/cursor`；写 `cursor/cursor.go`（下方完整代码块）。
2. （~3min）`internal/collab/room/room.go` 追加五个符号（下方完整代码块，接文件末尾）。
3. （~3min）用下方完整代码块**替换整个 `internal/collab/service.go`**。
4. （~1min）删除 T5.1 在测试文件里的临时 `var nowFn = time.Now`（service.go 已提供）。
5. （~1min）编译门：`go build ./internal/collab/...`（plan 轮已实测 EXIT=0）。测试仍红或已绿取决于是否已写 T5.1——顺序依赖见 T5.1 步骤 2。
6. （~1min）日志与注释检查：`grep -n "Println\|fmt.Print" internal/collab/` 零命中；成功路径与每条错误分支均有 slog 日志（代码块内已含）；新文件头写职责+边界、导出函数写参数/返回/注意事项、非显然逻辑带「为什么」（代码块内已含，P8c 已按此补 Pending/Consume/Mentions/ListRooms/MarkRead/Unread 六方法的入口/错误分支/成功路径日志）。

**完整代码块——`internal/collab/cursor/cursor.go`（新建；plan 轮已验证 gofmt 干净 + 全链路绿）**

```go
// Package cursor 是未读游标的持久化介质（B156.2 C5，移交区 A.1 岔口四方案甲）：
// 按成员按房间记 seq 水位，datadir 下 JSON 文件缓存，tmp+rename 原子写。
// 纯缓存非权威（拍板 5.4）：重启丢失无害、打开房间即自愈；免引入第二个
// 数据库句柄。path 为空的 Store 是纯内存形态（Service.New 的默认），组装点
// 持 DataDir，用 cursor.New(path) + Service.SetCursorStore 换成文件介质。
package cursor

import (
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"sync"
)

// Store 未读游标存储：data 是 member -> roomID -> uptoSeq 的 map。全部读写
// 经互斥锁串行化（HTTP handler 并发到达的原子性在此）；文件介质每次写入
// tmp+rename 原子替换。
type Store struct {
	mu     sync.Mutex
	path   string
	loaded bool
	data   map[string]map[string]int64
}

// New 构造游标存储。path 空 = 纯内存；非空 = 文件介质，首次访问时从 path
// 加载，之后每次 MarkRead 后原子落盘。
func New(path string) *Store {
	return &Store{path: path, data: map[string]map[string]int64{}}
}

// MarkRead 置该成员在该房间的已读水位。单调：只进不退（并发到达时取大值），
// 防止旧请求把新水位拉回。member/roomID 为空直接报错（没有「谁/哪」的游标
// 无意义）。
func (s *Store) MarkRead(member, roomID string, uptoSeq int64) error {
	if member == "" || roomID == "" {
		return fmt.Errorf("游标必须带 member 与 roomID")
	}
	s.mu.Lock()
	defer s.mu.Unlock()
	if err := s.ensureLoaded(); err != nil {
		return err
	}
	perRoom := s.data[member]
	if perRoom == nil {
		perRoom = map[string]int64{}
		s.data[member] = perRoom
	}
	if uptoSeq > perRoom[roomID] {
		perRoom[roomID] = uptoSeq
	}
	return s.persist()
}

// Cursor 读该成员在该房间的已读水位；未记过返回 0。
func (s *Store) Cursor(member, roomID string) (int64, error) {
	s.mu.Lock()
	defer s.mu.Unlock()
	if err := s.ensureLoaded(); err != nil {
		return 0, err
	}
	return s.data[member][roomID], nil
}

// ensureLoaded 首次访问时从磁盘加载（重启/换实例后读回持久化水位）。
func (s *Store) ensureLoaded() error {
	if s.path == "" || s.loaded {
		return nil
	}
	s.loaded = true
	raw, err := os.ReadFile(s.path)
	if err != nil {
		if os.IsNotExist(err) {
			return nil // 首次使用：无文件即空游标
		}
		return fmt.Errorf("读游标文件 %s: %w", s.path, err)
	}
	if len(raw) == 0 {
		return nil
	}
	if err := json.Unmarshal(raw, &s.data); err != nil {
		return fmt.Errorf("解析游标文件 %s: %w", s.path, err)
	}
	return nil
}

// persist tmp+rename 原子写：主文件不被半截写损坏；崩溃残留的 tmp 残屑无害
// （拍板 5.4「重启丢失无害」同族，只是更保守——数据已过 rename 才可见）。
func (s *Store) persist() error {
	if s.path == "" {
		return nil
	}
	raw, err := json.Marshal(s.data)
	if err != nil {
		return fmt.Errorf("编码游标: %w", err)
	}
	if err := os.MkdirAll(filepath.Dir(s.path), 0o700); err != nil {
		return fmt.Errorf("建游标目录: %w", err)
	}
	tmp := s.path + ".tmp"
	if err := os.WriteFile(tmp, raw, 0o600); err != nil {
		return fmt.Errorf("写游标 tmp: %w", err)
	}
	if err := os.Rename(tmp, s.path); err != nil {
		return fmt.Errorf("游标原子替换: %w", err)
	}
	return nil
}
```

**完整代码块——`internal/collab/room/room.go` 末尾追加（plan 轮已验证 gofmt 干净 + 全链路绿；行号供 T5.5 视图 diff）**

```go

// ConsumedEventType 是账本流上消息消费标记的事件类型字面量。取值必须等于
// ledger.EvMessageConsumed；门面禁令禁止 import ledger，等式由
// readmodel_test.go 的字面量测试钉住。
const ConsumedEventType = "message_consumed"

// ReadAllEvents 分页读完整事件流（升序游标、每页 1000、EventsFromAsc 语义）。
// A.5 护栏注记（契约 §8 A.5，照抄触发条件）：单流 >10^5 行实测变慢再议，
// 不预先优化、不加类型过滤参数——Pending/Mentions/ListRooms/Unread 的全流
// 扫描都走这里。
func ReadAllEvents(lc client.LedgerClient, fromSeq int64) ([]proto.LedgerEvent, error) {
	var out []proto.LedgerEvent
	seq := fromSeq
	for {
		page, err := lc.EventsFromAsc([]string{}, seq, 1000)
		if err != nil {
			return nil, err
		}
		out = append(out, page...)
		if len(page) < 1000 {
			return out, nil
		}
		seq = page[len(page)-1].Seq
	}
}

// ConsumedSeqs 收集该消费者已消费的消息 seq 集：按事件 actor 列（=consumer）
// 粗筛 message_consumed 事件 + 载荷 message_seq/consumer 精确匹配（载荷才是
// 权威，ledger/rooms.go 注释同款）。Pending/Mentions 的「未消费」过滤共用。
func ConsumedSeqs(events []proto.LedgerEvent, consumer string) map[int64]bool {
	consumed := make(map[int64]bool)
	for _, ev := range events {
		if ev.Type != ConsumedEventType || ev.Actor != consumer {
			continue
		}
		var marker struct {
			MessageSeq int64  `json:"message_seq"`
			Consumer   string `json:"consumer"`
		}
		if err := json.Unmarshal(ev.Payload, &marker); err != nil {
			continue // 非法载荷不该出现；跳过不破坏消费集
		}
		if marker.Consumer != consumer {
			continue
		}
		consumed[marker.MessageSeq] = true
	}
	return consumed
}

// RoomIDOf 事件所属房间 id：卡级事件 card_id 即房间号；群级无卡事件看载荷
// Room 字段（ListRooms 建活动索引用）。
func RoomIDOf(ev proto.LedgerEvent) string {
	if ev.CardID != "" {
		return ev.CardID
	}
	var msg proto.RoomMessage
	if err := UnmarshalMessage(ev.Payload, &msg); err != nil {
		return ""
	}
	return msg.Room
}

// MessageKind 事件载荷的消息 kind；非 room_message 或解码失败返回空串。
func MessageKind(ev proto.LedgerEvent) string {
	var msg proto.RoomMessage
	if err := UnmarshalMessage(ev.Payload, &msg); err != nil {
		return ""
	}
	return msg.Kind
}
```

**完整代码块——`internal/collab/service.go`（整文件替换；plan 轮已验证 gofmt 干净 + 全链路绿）**

```go
// Package collab 是协作房间域（B156.2）的入站门面：房间消息播发、白名单
// 与书写者执法、@定向消费队列与注意力面读模型的唯一入口。外界（gateway、
// CLI）只 import 本包与 proto DTO，不得深入内部；对外部账本的能力需求经
// client.LedgerClient 接口由组装点绑定（双向门面，架构法第四条第 3 档）。
//
// 根包只保留门面：执法规则（房间解析、只读判定、书写者校验）与读侧过滤
// 助手全部沉在 internal/collab/room 子包；未读游标介质在 internal/collab/cursor
// 子包；错误哨兵定义在 room、由本包再导出。
//
// 与 d_sessions（终端 PTY 回放域）同名不同物：本包管协作房间，不管 PTY。
package collab

import (
	"log/slog"
	"sort"
	"time"

	"github.com/Xsxdot/handoff/internal/collab/client"
	"github.com/Xsxdot/handoff/internal/collab/cursor"
	"github.com/Xsxdot/handoff/internal/collab/room"
	"github.com/Xsxdot/handoff/internal/proto"
)

// log 结构化 logger（同 ledger 包 log() 先例，slog.Default()）。
func log() *slog.Logger { return slog.Default() }

// nowFn 可注入时钟，仅供测试替换（breakdown：活性判定所需时钟用 collab 包内
// 变量注入，不改构造器）。生产不设，回退到 time.Now。ListRooms 的 live 字段
// 判定读它；测试把它与假 client 的租约 expiresAt 拨到同一可拨源，防「判据
// 读真实时钟、夹具拨别的钟」的假绿。
var nowFn = time.Now

// 错误哨兵。调用方（CLI/agentd）按哨兵翻译为退出码或 HTTP 状态：
// ErrNoRoom→400/404、ErrKindNotAllowed→400、ErrNotWriter→403、
// ErrReadOnly/ErrNotBound→409。定义在 room 子包（执法内核产生它们），
// 本包再导出以保持 collab.ErrXxx 契约符号不变。
var (
	ErrNoRoom         = room.ErrNoRoom
	ErrKindNotAllowed = room.ErrKindNotAllowed
	ErrReadOnly       = room.ErrReadOnly
	ErrNotWriter      = room.ErrNotWriter
	ErrNotBound       = room.ErrNotBound
)

// historyDefaultLimit History/Mentions 未给 limit 时的取数上限。
const historyDefaultLimit = 200

// pointerActor Pointer 写指针行时落账的 actor 标识（契约 §3.3 Pointer 无
// actor 参数、调用方身份不可得，固定系统组件标识）。
const pointerActor = "system:pointer"

// Service 协作房间域入站门面。依赖只收 client 接口。
type Service struct {
	lc     client.LedgerClient
	cursor *cursor.Store
}

// New 组装点调用；lc 的具体实现是 internal/ledger/api.Facade。未读游标默认
// 纯内存（拍板 5.4 降为缓存）；组装点持 DataDir 时用 SetCursorStore 换文件介质。
func New(lc client.LedgerClient) *Service {
	return &Service{lc: lc, cursor: cursor.New("")}
}

// SetCursorStore 把未读游标换成指定介质（移交区 A.1 岔口四方案甲：datadir 下
// JSON 文件缓存，tmp+rename 原子写）。组装点（agentd server / CLI main）持
// DataDir，用 cursor.New(filepath.Join(cfg.DataDir, "room-cursors.json")) 接线；
// 不调用则保持 New 的纯内存默认——游标非权威，重启丢失无害、打开房间即自愈。
func (s *Service) SetCursorStore(st *cursor.Store) { s.cursor = st }

// Send 发消息：白名单 + 书写者执法的唯一写入口（pointer 除外——它只能走
// Pointer，经本方法一律拒收）。返回落账 seq。
//
// 执法矩阵（契约 §3.3 逐条 + §4 冻结清单）：kind 词表校验（pointer 拒收）
// → 房间解析（room.Resolve：存在性 + 只读判定）→ 只读拒写 → 按 kind 的
// 书写者校验（room.VerifyWriter）→ 经 client 发布。全部规则实现在 room
// 子包，本方法只做编排。
func (s *Service) Send(roomID string, msg proto.RoomMessage, actor string) (int64, error) {
	if !room.KindAllowed(msg.Kind) || msg.Kind == proto.RoomMsgPointer {
		return 0, room.ErrKindNotAllowed
	}
	r, err := room.Resolve(s.lc, roomID)
	if err != nil {
		return 0, err
	}
	if r.ReadOnly {
		return 0, room.ErrReadOnly
	}
	if err := room.VerifyWriter(r, msg.Kind, actor); err != nil {
		return 0, err
	}
	msg.Room = roomID
	seq, err := s.lc.RecordRoomMessage(r.CardID, msg, actor)
	if err != nil {
		return 0, err
	}
	log().Info("房间消息已落账", "room", roomID, "kind", msg.Kind, "actor", actor, "seq", seq)
	return seq, nil
}

// Pointer 系统组件写指针行的专用入口；HTTP/CLI 面不得路由它（冻结清单有
// 对应断言）。kind=pointer 与 BySystem=true 由本方法自置（rooms.go 注释
// 「本键只应由 Service.Pointer 置真」），调用方只递 roomID 与正文；房间
// 解析复用 room.Resolve，并入冻结/终态归档房间拒写（ErrReadOnly）。
func (s *Service) Pointer(roomID string, msg proto.RoomMessage) (int64, error) {
	r, err := room.Resolve(s.lc, roomID)
	if err != nil {
		return 0, err
	}
	if r.ReadOnly {
		return 0, room.ErrReadOnly
	}
	msg.Kind = proto.RoomMsgPointer
	msg.BySystem = true
	msg.Room = roomID
	seq, err := s.lc.RecordRoomMessage(r.CardID, msg, pointerActor)
	if err != nil {
		return 0, err
	}
	log().Info("指针行已落账", "room", roomID, "card", r.CardID, "seq", seq)
	return seq, nil
}

// History 读房间历史：seq 游标分页（beforeSeq 排他、升序截尾），只返回
// type=room_message 的事件。limit<=0 取 historyDefaultLimit。
func (s *Service) History(roomID string, beforeSeq int64, limit int) ([]proto.LedgerEvent, error) {
	if limit <= 0 {
		limit = historyDefaultLimit
	}
	events, err := s.lc.EventsFromAsc([]string{}, beforeSeq, 0)
	if err != nil {
		return nil, err
	}
	out := make([]proto.LedgerEvent, 0, len(events))
	for _, ev := range events {
		if ev.Type != room.RoomEventType || !room.SameRoom(ev, roomID) {
			continue
		}
		out = append(out, ev)
		if len(out) >= limit {
			break
		}
	}
	return out, nil
}

// Pending @定向消费队列（读侧）：全流扫描「mentions∋consumer 且尚无本人
// message_consumed 标记的群级消息」∪「consumer 所绑卡房间内未消费的用户
// 留言（kind==user）」。所绑卡房间 = ListAllCards 里 DriverSession==consumer
// 的全部卡（含终态/并入卡——只读冻结防新写，不抹旧留言，杜绝无人消费的黑洞）。
func (s *Service) Pending(consumer string) ([]proto.LedgerEvent, error) {
	events, err := room.ReadAllEvents(s.lc, 0)
	if err != nil {
		log().Warn("待消费队列组装失败：读事件流", "consumer", consumer, "cause", err)
		return nil, err
	}
	consumed := room.ConsumedSeqs(events, consumer)
	bound := map[string]bool{}
	cards, err := s.lc.ListAllCards("")
	if err != nil {
		log().Warn("待消费队列组装失败：读卡列表", "consumer", consumer, "cause", err)
		return nil, err
	}
	for _, c := range cards {
		if c.DriverSession == consumer {
			bound[c.ID] = true
		}
	}
	out := []proto.LedgerEvent{}
	for _, ev := range events {
		if ev.Type != room.RoomEventType || consumed[ev.Seq] {
			continue
		}
		if ev.CardID == "" {
			if room.MentionsMember(ev, consumer) {
				out = append(out, ev)
			}
			continue
		}
		if bound[ev.CardID] && room.MessageKind(ev) == proto.RoomMsgUser {
			out = append(out, ev)
		}
	}
	log().Info("待消费队列已组装", "consumer", consumer, "count", len(out))
	return out, nil
}

// Consume 消费落账（恰好一次的权威在账本 message_consumed 事件，拍板 5.4）。
// 幂等：同参重复消费返回 nil；seq 不存在或非 room_message 同样幂等 nil
// （岔口六方案甲——「已消费」目标态对不存在消息天然成立，静默面由
// Pending/Mentions 只列未消费兜底）。查重与写入的原子性在账本同 mutate
// 事务内（C2 已锁），本方法只负责定位消息所在卡并把 cardID 传给 client。
func (s *Service) Consume(seq int64, consumer string) error {
	events, err := room.ReadAllEvents(s.lc, 0)
	if err != nil {
		log().Warn("消费失败：读事件流", "seq", seq, "consumer", consumer, "cause", err)
		return err
	}
	for _, ev := range events {
		if ev.Seq != seq {
			continue
		}
		if ev.Type != room.RoomEventType {
			return nil // 非 room_message：幂等 nil，不落标记
		}
		if err := s.lc.RecordMessageConsumed(ev.CardID, seq, consumer); err != nil {
			log().Warn("消费落账失败", "seq", seq, "consumer", consumer, "card", ev.CardID, "cause", err)
			return err
		}
		log().Info("消息已消费", "seq", seq, "consumer", consumer, "card", ev.CardID)
		return nil
	}
	return nil // 不存在：幂等 nil
}

// Mentions 收件箱源③：@member 的未消费提及。全流扫描 afterSeq 之后、
// mentions∋member 且尚无本人 message_consumed 标记的 room_message。
// limit<=0 取 historyDefaultLimit。
func (s *Service) Mentions(member string, afterSeq int64, limit int) ([]proto.LedgerEvent, error) {
	if limit <= 0 {
		limit = historyDefaultLimit
	}
	events, err := room.ReadAllEvents(s.lc, afterSeq)
	if err != nil {
		log().Warn("提及读取失败：读事件流", "member", member, "cause", err)
		return nil, err
	}
	consumed := room.ConsumedSeqs(events, member)
	out := []proto.LedgerEvent{}
	for _, ev := range events {
		if ev.Type != room.RoomEventType || !room.MentionsMember(ev, member) {
			continue
		}
		if consumed[ev.Seq] {
			continue
		}
		out = append(out, ev)
		if len(out) >= limit {
			break
		}
	}
	log().Info("未消费提及已组装", "member", member, "count", len(out))
	return out, nil
}

// ListRooms 会话列表：扁平活动排序 + 项目筛选（project=="" 表示全部）。
// 条目=全部卡房间（终态卡沉底）+ 各项目的 project:<name> 群 + global 恒在。
// LastActivity=该房间最新一条 room_message 时刻；卡房间无消息时回退卡的
// UpdatedAt。Live=绑定者租约未过期（同一注入时钟 nowFn 判定），无绑定时
// false；DriverLease 读失败向上传播（C6 handler 退化 503，不吞错）。
func (s *Service) ListRooms(project string) ([]proto.RoomSummary, error) {
	cards, err := s.lc.ListAllCards(project)
	if err != nil {
		log().Warn("会话列表组装失败：读卡列表", "project", project, "cause", err)
		return nil, err
	}
	events, err := room.ReadAllEvents(s.lc, 0)
	if err != nil {
		log().Warn("会话列表组装失败：读事件流", "project", project, "cause", err)
		return nil, err
	}
	rooms := []proto.RoomSummary{}
	byID := map[string]int{}
	terminal := map[string]bool{}
	projects := map[string]bool{}
	for _, c := range cards {
		isTerm := room.IsTerminalStatus(c.Status)
		terminal[c.ID] = isTerm
		byID[c.ID] = len(rooms)
		rooms = append(rooms, proto.RoomSummary{
			ID: c.ID, Kind: room.KindCard, Project: c.Project, Title: c.Title,
			BoundSession: c.DriverSession,
			ReadOnly:     isTerm || c.Following != "",
			LastActivity: c.UpdatedAt,
		})
		if c.Project != "" {
			projects[c.Project] = true
		}
	}
	for p := range projects {
		byID["project:"+p] = len(rooms)
		rooms = append(rooms, proto.RoomSummary{ID: "project:" + p, Kind: room.KindProject, Project: p, Title: p})
	}
	byID["global"] = len(rooms)
	rooms = append(rooms, proto.RoomSummary{ID: "global", Kind: room.KindGlobal, Title: "全员"})
	// 活动：扫全流，逐房间取最新 room_message 时刻（升序遍历，后写覆盖）。
	for _, ev := range events {
		if ev.Type != room.RoomEventType {
			continue
		}
		if idx, ok := byID[room.RoomIDOf(ev)]; ok && ev.CreatedAt.After(rooms[idx].LastActivity) {
			rooms[idx].LastActivity = ev.CreatedAt
		}
	}
	// 活性：按不同绑定会话去重读租约（兼任多席只问一次「会话活着吗」）。
	liveOf := map[string]bool{}
	sessionSeen := map[string]bool{}
	for _, r := range rooms {
		if r.BoundSession == "" || sessionSeen[r.BoundSession] {
			continue
		}
		sessionSeen[r.BoundSession] = true
		expiresAt, exists, err := s.lc.DriverLease(r.BoundSession)
		if err != nil {
			log().Warn("会话列表组装失败：读租约", "session", r.BoundSession, "cause", err)
			return nil, err
		}
		liveOf[r.BoundSession] = exists && expiresAt.After(nowFn())
	}
	for i := range rooms {
		if bs := rooms[i].BoundSession; bs != "" {
			rooms[i].Live = liveOf[bs]
		}
	}
	// 排序：非终态（含群房间）按活动降序在前，终态卡房间沉底（各自内部按
	// 活动降序；Stable 保证同活动时保持插入序，输出形状确定）。
	active := []proto.RoomSummary{}
	sunk := []proto.RoomSummary{}
	for _, r := range rooms {
		if terminal[r.ID] {
			sunk = append(sunk, r)
		} else {
			active = append(active, r)
		}
	}
	sort.SliceStable(active, func(i, j int) bool { return active[i].LastActivity.After(active[j].LastActivity) })
	sort.SliceStable(sunk, func(i, j int) bool { return sunk[i].LastActivity.After(sunk[j].LastActivity) })
	log().Info("会话列表已组装", "project", project, "count", len(rooms))
	return append(active, sunk...), nil
}

// MarkRead 未读游标置位（打开房间即已读）。按成员按房间记 seq 水位，单调
// 只进不退；并发到达由 cursor.Store 互斥锁 + tmp+rename 原子写兜底。
func (s *Service) MarkRead(member, roomID string, uptoSeq int64) error {
	if err := s.cursor.MarkRead(member, roomID, uptoSeq); err != nil {
		log().Warn("未读游标置位失败", "member", member, "room", roomID, "upto_seq", uptoSeq, "cause", err)
		return err
	}
	log().Info("未读游标已置位", "member", member, "room", roomID, "upto_seq", uptoSeq)
	return nil
}

// Unread 未读计数：该房间内 room_message 事件中 seq 大于该成员游标的条数
// （水位语义：打开房间 MarkRead 到当前最大 seq 后即 0；未读过 = 全量）。
func (s *Service) Unread(member, roomID string) (int, error) {
	cursorSeq, err := s.cursor.Cursor(member, roomID)
	if err != nil {
		log().Warn("未读计数失败：读游标", "member", member, "room", roomID, "cause", err)
		return 0, err
	}
	events, err := room.ReadAllEvents(s.lc, cursorSeq)
	if err != nil {
		log().Warn("未读计数失败：读事件流", "member", member, "room", roomID, "cause", err)
		return 0, err
	}
	n := 0
	for _, ev := range events {
		if ev.Type != room.RoomEventType || !room.SameRoom(ev, roomID) {
			continue
		}
		n++
	}
	log().Info("未读计数完成", "member", member, "room", roomID, "unread", n)
	return n, nil
}
```

### T5.3 追加：游标文件介质测试（锁缝断言步骤；依赖 T5.2 的 SetCursorStore/cursor 包，随 T5.4 绿同批）

**Interfaces**

- Consumes：`(s *Service) SetCursorStore(st *cursor.Store)`（T5.2 产出）、`cursor.New(path string) *Store`。

**步骤**

1. （~2min）写 `internal/collab/cursorfile_test.go`（下方完整代码块）。
2. （~1min）编译门：`go build ./internal/collab/...`。
3. 本文件两支测试的牙齿证明不走独立红跑（T5.2 落地后它们写下去即绿），改由 T5.5 变异④⑤⑥ 提供：SetCursorStore 换成**不持久化的内存存储** → 持久化断言红；MarkRead 改 no-op → 水位断言红；cursor 去互斥锁 → 并发压测在 -race 下竞态（变异⑤⑥为可选红靶，变异④为强制）。

**完整代码块——`internal/collab/cursorfile_test.go`（新建；plan 轮已验证 gofmt 干净 + 全链路绿）**

```go
// 游标文件介质测试（B156.2 C5，移交区 A.1 岔口四方案甲）：tmp+rename 原子
// 写、并发压测无交错损坏、跨实例持久化。入口一律经 Service.MarkRead/Unread
// （接缝 #1），文件介质的注入经 SetCursorStore（组装点同形接线）。
package collab

import (
	"path/filepath"
	"sync"
	"testing"

	"github.com/Xsxdot/handoff/internal/collab/cursor"
	"github.com/Xsxdot/handoff/internal/collab/room"
	"github.com/Xsxdot/handoff/internal/ledger"
	ledgerapi "github.com/Xsxdot/handoff/internal/ledger/api"
	"github.com/Xsxdot/handoff/internal/proto"
)

// TestCursorFilePersistsAcrossInstances 文件介质跨实例持久化：MarkRead 后新
// Service + 新 Store 同路径重开，水位仍在（重启/换实例不丢已读）。
func TestCursorFilePersistsAcrossInstances(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	seq, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy")
	if err != nil {
		t.Fatal(err)
	}
	path := filepath.Join(t.TempDir(), "cursors.json")
	svc.SetCursorStore(cursor.New(path))
	if err := svc.MarkRead("user:sy", card.ID, seq); err != nil {
		t.Fatal(err)
	}
	svc2 := New(ledgerapi.New(st))
	svc2.SetCursorStore(cursor.New(path))
	if n, err := svc2.Unread("user:sy", card.ID); err != nil || n != 0 {
		t.Fatalf("持久化游标应生效: %v %d", err, n)
	}
}

// TestCursorConcurrentMarkRead 并发 MarkRead 无交错损坏 + 单调水位：50 个
// goroutine 各写一个消息 seq，最终水位取最大（只进不退）、文件仍可被新实例
// 完整解析（tmp+rename 原子性证明）。
func TestCursorConcurrentMarkRead(t *testing.T) {
	svc, st := newFixture(t)
	card := mustAnyCard(t, svc, st)
	seqs := make([]int64, 0, 50)
	for i := 0; i < 50; i++ {
		seq, err := svc.Send(card.ID, proto.RoomMessage{Kind: proto.RoomMsgUser, Body: "x"}, "user:sy")
		if err != nil {
			t.Fatal(err)
		}
		seqs = append(seqs, seq)
	}
	path := filepath.Join(t.TempDir(), "cursors.json")
	svc.SetCursorStore(cursor.New(path))

	var wg sync.WaitGroup
	for _, s := range seqs {
		wg.Add(1)
		go func(seq int64) {
			defer wg.Done()
			if err := svc.MarkRead("user:sy", card.ID, seq); err != nil {
				t.Errorf("MarkRead(%d): %v", seq, err)
			}
		}(s)
	}
	wg.Wait()

	if n, err := svc.Unread("user:sy", card.ID); err != nil || n != 0 {
		t.Fatalf("单调水位应取最大 seq，未读应 0: %v %d", err, n)
	}
	svc2 := New(ledgerapi.New(st))
	svc2.SetCursorStore(cursor.New(path))
	if n, err := svc2.Unread("user:sy", card.ID); err != nil || n != 0 {
		t.Fatalf("并发写后文件应完好且水位=最大 seq: %v %d", err, n)
	}
}

// TestConsumedEventTypeLiteralMatchesLedger 钉住 room.ConsumedEventType 与
// 账本 EvMessageConsumed 的等式（内部锁，理由见 §7）。
func TestConsumedEventTypeLiteralMatchesLedger(t *testing.T) {
	if room.ConsumedEventType != ledger.EvMessageConsumed {
		t.Fatalf("消费事件类型字面量漂移: %q != %q", room.ConsumedEventType, ledger.EvMessageConsumed)
	}
}
```

### T5.4 跑绿与回归（红绿收口）

非红绿步骤（回归动作）。

1. （~2min）跑绿：`go test ./internal/collab/... -count=1` 全 ok；再 `-v -run 'TestPending|TestConsume|TestMentionsExcludes|TestListRooms|TestMarkRead|TestCursor|TestConsumedEvent'` 逐支核 PASS（plan 轮 18 支全 PASS，台账 P3）。贴原文到台账。
2. （~2min）全量触及回归：`go build ./... && go vet ./internal/collab/...`（plan 轮已实测两命令 EXIT=0，台账 P4）。
3. （~2min）**gofmt 闸**：`gofmt -l internal/collab/` 必须零输出——协调者实况：build/test/vet 与 gofmt 正交，四样全绿时格式照样可脏（本卡 P8 已复现初稿被 gofmt 列出 4 文件的教训）。若被列出，`gofmt -w` 后重跑。
4. （~2min）占位残留扫描：`grep -rn "Ticket 0 空壳\|当前返回空切片占位\|当前返回空切片\|_ = consumer\|_ = seq\|return \[\]proto.LedgerEvent{}, nil\|return \[\]proto.RoomSummary{}, nil\|return 0, nil" internal/collab/service.go` 零命中（Pending/Consume/ListRooms/MarkRead/Unread 的占位已连根拔掉）。
5. （~2min）提交（commit 1，message 见 §11）。

### T5.5 图增量、变异复验与收尾

非红绿步骤（图数据是声明 JSON，非行为断言）。

1. （~5min）图增量：`codegraph/best.json` containers 追加两行（下方代码块 A）；`codegraph/diffs/cards-B156.2-charter-4.json` 追加 containersAdded/nodesAdded/edgesAdded（下方代码块 B，nodesAdded 的 `line` 按下方给的行号，**若实现过程行号漂移以最终提交文件的实际符号起始行为准**——机械修正，非占位）。
2. （~2min）图闸：`go run . graph check --repo . --view cards-B156.2-charter-4` 预期 fails=[] EXIT=0（plan 轮已用同形临时改动实测 fails=[]，台账 P9）；`go run . graph validate --repo .` EXIT=0。warns 数不作判据（97 与基线一致）。
3. （~2min）**逐容器点数自检**（协调者实况：抽查符号名会命中别的容器同名符号，必须逐容器点数）：k_collab_room_fn=11、k_collab_room_model=1、k_collab_cursor_fn=1、k_collab_cursor_Store=3、k_collab_Service=11——与源码导出符号数逐一相等（plan 轮已验，台账 P10）。脚本：对每个容器 `count(view 节点 container==C)` 比对 `grep -c 导出符号`。
4. （~4min）变异复验（证明判据能变红，全部红文贴台账、逐一还原）：
   - 变异①（**强制**，活性判据牙）：ListRooms 判据 `expiresAt.After(nowFn())` → `expiresAt.After(time.Now())` → `go test ./internal/collab/ -run TestListRoomsLiveFlip` 红（plan 轮实测「租约未过期应 live=true … Live:false」）→ 还原；
   - 变异②（强制，Pending 消费过滤牙）：Pending 循环去掉 `consumed[ev.Seq]` → TestPendingGroupMentionAndConsume 红（「消费后 Pending 应清空:[…]」）→ 还原；Mentions 循环去掉 `if consumed[ev.Seq]` → TestMentionsExcludesConsumed 红 → 还原；
   - 变异③（强制，Consume 写路径牙）：Consume 定位到消息后**不调** `RecordMessageConsumed` 直接 return nil → TestConsumeIdempotentSameArgs 红（「不得产生第二条标记: 0」）+ TestConsumePayloadTwoKeys 红（「没有消费标记事件」）→ 还原；
   - 变异④（强制，回归锁牙）：Consume 对无效 seq 也写标记（去掉 type 检查与不存在分支的幂等 nil）→ TestConsumeInvalidSeq 红（「无效 seq 不得落标记」）→ 还原；
   - 变异⑤（可选）：SetCursorStore 换成 `cursor.New("")`（纯内存，不落盘）→ TestCursorFilePersistsAcrossInstances 红 → 还原。
   - 变异⑥（可选）：cursor.Store.MarkRead 去掉单调 max（直接覆写）→ 并发压测在 `-race` 下可能出现竞态/水位错——若验证不稳，以变异⑤ + 变异③的牙齿为已满足，⑥ 不强制。
   - 还原后 `go test ./internal/collab/... -count=1` 复绿。
5. （~2min）提交（commit 2，message 见 §11）。

**完整代码块 A——`codegraph/best.json` 的 containers 段追加两行**（在既有容器映射后追加，id 无冲突）：

```json
  "k_collab_cursor_fn": "d_collab",
  "k_collab_cursor_Store": "d_collab",
```

**完整代码块 B——`codegraph/diffs/cards-B156.2-charter-4.json` 追加**（在既有键之外新增/扩充四个键；既有键一字不动；行号来自本 plan 代码块排版——room.go: ConsumedEventType 221 / ReadAllEvents 227 / ConsumedSeqs 246 / RoomIDOf 269 / MessageKind 281；cursor.go: Store 19 / New 28 / MarkRead 35 / Cursor 56；service.go: SetCursorStore 68）：

```json
 "containersAdded": {
  "k_collab_cursor_fn": {"label": "collab.cursor 包级函数", "kind": "函数组", "domain": "d_collab"},
  "k_collab_cursor_Store": {"label": "collab.cursor Store 类型", "kind": "类型", "domain": "d_collab"}
 },
 "nodesAdded": {
  "n_collab_room_ReadAllEvents": {"kind": "func", "container": "k_collab_room_fn", "order": 9, "name": "ReadAllEvents", "file": "internal/collab/room/room.go", "line": 227, "signature": "func ReadAllEvents(lc client.LedgerClient, fromSeq int64) ([]proto.LedgerEvent, error)", "params": [["lc", "client.LedgerClient", ""], ["fromSeq", "int64", ""]], "returns": "([]proto.LedgerEvent, error)", "summary": "ReadAllEvents 分页读完整事件流（A.5 护栏注记：单流 >10^5 行实测变慢再议，不预先优化）。"},
  "n_collab_room_ConsumedSeqs": {"kind": "func", "container": "k_collab_room_fn", "order": 10, "name": "ConsumedSeqs", "file": "internal/collab/room/room.go", "line": 246, "signature": "func ConsumedSeqs(events []proto.LedgerEvent, consumer string) map[int64]bool", "params": [["events", "[]proto.LedgerEvent", ""], ["consumer", "string", ""]], "returns": "map[int64]bool", "summary": "ConsumedSeqs 收集该消费者已消费的消息 seq 集（Pending/Mentions 未消费过滤共用）。"},
  "n_collab_room_RoomIDOf": {"kind": "func", "container": "k_collab_room_fn", "order": 11, "name": "RoomIDOf", "file": "internal/collab/room/room.go", "line": 269, "signature": "func RoomIDOf(ev proto.LedgerEvent) string", "params": [["ev", "proto.LedgerEvent", ""]], "returns": "string", "summary": "RoomIDOf 事件所属房间 id：卡级看 card_id，群级看载荷 Room。"},
  "n_collab_room_MessageKind": {"kind": "func", "container": "k_collab_room_fn", "order": 12, "name": "MessageKind", "file": "internal/collab/room/room.go", "line": 281, "signature": "func MessageKind(ev proto.LedgerEvent) string", "params": [["ev", "proto.LedgerEvent", ""]], "returns": "string", "summary": "MessageKind 事件载荷的消息 kind；解码失败返回空串。"},
  "m_collab_cursor_Store": {"kind": "model", "container": "k_collab_cursor_Store", "order": 1, "name": "Store", "file": "internal/collab/cursor/cursor.go", "line": 19, "signature": "type Store struct {\n\tmu sync.Mutex\n\tpath string\n\tloaded bool\n\tdata map[string]map[string]int64\n}", "summary": "Store 未读游标存储：member -> roomID -> uptoSeq，互斥锁串行化 + tmp+rename 原子写。"},
  "n_collab_cursor_New": {"kind": "func", "container": "k_collab_cursor_fn", "order": 1, "name": "New", "file": "internal/collab/cursor/cursor.go", "line": 28, "signature": "func New(path string) *Store", "params": [["path", "string", ""]], "returns": "*Store", "summary": "New 构造游标存储；path 空=纯内存，非空=JSON 文件介质。"},
  "n_collab_cursor_Store_MarkRead": {"kind": "func", "container": "k_collab_cursor_Store", "order": 2, "name": "Store.MarkRead", "file": "internal/collab/cursor/cursor.go", "line": 35, "signature": "func (s *Store) MarkRead(member, roomID string, uptoSeq int64) error", "params": [["member", "string", ""], ["roomID", "string", ""], ["uptoSeq", "int64", ""]], "returns": "error", "summary": "MarkRead 置已读水位（单调只进不退，tmp+rename 原子落盘）。"},
  "n_collab_cursor_Store_Cursor": {"kind": "func", "container": "k_collab_cursor_Store", "order": 3, "name": "Store.Cursor", "file": "internal/collab/cursor/cursor.go", "line": 56, "signature": "func (s *Store) Cursor(member, roomID string) (int64, error)", "params": [["member", "string", ""], ["roomID", "string", ""]], "returns": "(int64, error)", "summary": "Cursor 读该成员在该房间的已读水位；未记过返回 0。"},
  "n_collab_Service_SetCursorStore": {"kind": "func", "container": "k_collab_Service", "order": 20, "name": "Service.SetCursorStore", "file": "internal/collab/service.go", "line": 68, "signature": "func (s *Service) SetCursorStore(st *cursor.Store)", "params": [["st", "*cursor.Store", ""]], "returns": "", "summary": "SetCursorStore 把未读游标换成指定介质（组装点接线；不调用保持 New 的纯内存默认）。"}
 },
 "edgesAdded": [
  ["n_collab_Service_Pending", "n_collab_room_ConsumedSeqs"],
  ["n_collab_Service_Pending", "n_collab_room_MentionsMember"],
  ["n_collab_Service_Pending", "n_collab_room_MessageKind"],
  ["n_collab_Service_Pending", "n_collab_room_ReadAllEvents"],
  ["n_collab_Service_Consume", "n_collab_room_ReadAllEvents"],
  ["n_collab_Service_Mentions", "n_collab_room_ConsumedSeqs"],
  ["n_collab_Service_Mentions", "n_collab_room_ReadAllEvents"],
  ["n_collab_Service_ListRooms", "n_collab_room_ReadAllEvents"],
  ["n_collab_Service_ListRooms", "n_collab_room_RoomIDOf"],
  ["n_collab_Service_ListRooms", "n_collab_room_IsTerminalStatus"],
  ["n_collab_Service_Unread", "n_collab_room_ReadAllEvents"],
  ["n_collab_Service_Unread", "n_collab_room_SameRoom"],
  ["n_collab_Service_MarkRead", "n_collab_cursor_Store_MarkRead"],
  ["n_collab_Service_Unread", "n_collab_cursor_Store_Cursor"],
  ["n_collab_room_ReadAllEvents", "m_proto_LedgerEvent"],
  ["n_collab_room_ConsumedSeqs", "m_proto_LedgerEvent"],
  ["n_collab_room_RoomIDOf", "m_proto_RoomMessage"],
  ["n_collab_room_MessageKind", "m_proto_RoomMessage"]
 ]
```

> 顺序纪律：edgesAdded 的「调用边门控」会按当前源码校验调用文件是否真的 import 对方包（C4 已实测：代码落地前声明会报完整性问题）。**图增量必须在本卡的代码落地之后、与代码同批或次批提交**。
> 图覆盖债如实记录：nodesAdded 只覆盖新导出符号；room 的常量（ConsumedEventType/Kind*）与 Service 的 nowFn 未导出不入图（既有 RoomEventType 同待遇）；cursor.Store 未导出字段（mu/path/loaded/data）只进 signature 不进 fields 清单（m_collab_Service 同待遇）。best.json 只加容器归属，不改既有映射、不改 domain 结构。

**测试范围声明（最小化合计）**：本卡只跑 `./internal/collab/...`（主）；`go build ./...`（依赖面回归）；图闸 `graph check/validate`；变异复验限定 `-run 'TestListRoomsLiveFlip|TestPendingGroupMentionAndConsume|TestMentionsExcludesConsumed|TestConsumeIdempotentSameArgs|TestConsumePayloadTwoKeys|TestConsumeInvalidSeq|TestCursorFilePersistsAcrossInstances'`。全仓测试不属于本卡。

## 4. 缺陷族对抗审查结论（逐族）

| 族 | 设问 | 结论 |
| --- | --- | --- |
| 生命周期/状态机中断 | 消费标记与 Pending 读之间有无窗口？进程重启丢游标？ | 恰好一次由账本同 mutate 事务的查重+写单写者串行化（C2 已锁）；Consume 读到的是已提交事件，无「读到未提交」窗口。游标是纯缓存（拍板 5.4），重启丢失无害、打开房间即自愈；文件介质崩溃残留 tmp 残屑无害（rename 原子性）。driver_leases 过期行 upsert 无累积（C1 已注释言明） |
| 静默失败/误导报错 | Consume 无效 seq 的静默 nil 会不会吞真错？ListRooms 数据源失败会不会被吞成空列表？ | 无效 seq 幂等 nil 是有意选择（岔口六方案甲，注释言明 + 变异④锁「不落标记」）；真错路径（ReadAllEvents/ListAllCards/DriverLease 报错）全部原样向上传播，不伪装成空列表——C6 handler 据此退化 503 不吞错 |
| 跨平台假设 | 时间、路径、文件写 | 时间走时间戳比较不涉方言（DriverLeaseOf 已解码）；游标文件路径由组装点（持 DataDir 侧）注入，collab 不做路径假设；tmp+rename 是 POSIX 通用原子语义，无平台专属 API |
| 假红/假绿测试 | 活性翻转会不会「判据读真实时钟、夹具拨别的钟」而假存活？占位会不会测不出来？ | **核心防线**：TestListRoomsLiveFlip 用假 client（接缝#2），租约 expiresAt 与 collab 注入时钟（nowFn）取**同一可拨源 cur**，基线选在过去——变异①（判据改读 time.Now()）时真实时钟恒在 expiresAt 之后、live=true 断言确定性翻红（plan 轮已实测）。真实集成测试用确定性场景（+5m 缓冲 live=true、负 TTL/无租约 live=false），不依赖跨钟同步。C1 协调者打废过的「读 time.Now() + 拨时钟」变异族在此被结构排除。占位消灭由 T5.4 步骤 4 扫描 + T5.1 红 + 变异②③④ 共同锁定 |
| 门禁绕过 | 有没有绕过 Send/Pointer 的写路径？游标写会不会穿门面？ | 白名单唯一写入口仍是 Service.Send/Pointer（本卡零新增写出口）；消费写经 Service.Consume → client.RecordMessageConsumed（缝#1→缝#2 都过）；游标写经 Service.MarkRead → cursor.Store（同门面）。collab 生产零 ledger import 由 graph check 硬闸执法 |

**序列化边界设问**：本卡新增的序列化/投影点：
① Consume 定位消息后取 `ev.CardID` 传给 RecordMessageConsumed（无手写序列化，卡 ID 直接传递）；② `ConsumedSeqs` 解码 message_consumed payload（message_seq/consumer 两键）——与 ledger 侧 `consumedMarker`（rooms.go:37-40）是**跨包镜像**，无共享类型（门面禁令），两键等值由 TestConsumePayloadTwoKeys（锁 payload 恰两键）+ TestConsumedEventTypeLiteralMatchesLedger（锁事件类型字面量）跨真实序列化边界锁住；③ RoomSummary 输出 wire 由 C8 web 侧孪生夹具承接（C6/C8 的集成冒烟，本卡不重复）；④ 游标 JSON 文件是**自产自消**（cursor 包内 encode/decode 同一 struct），且经 TestCursorFilePersistsAcrossInstances 跨实例 roundtrip——「字段缺失 vs 值为零」对 int64 seq 无歧义（缺键=0 即未读过，语义正确）。无遗漏投影点。

## 5. 五项检查

1. **缺陷族对抗**：§4 逐族有结论 ✓
2. **序列化边界**：§4 末段，投影点清单 + 贯穿真实序列化边界的断言（consumedMarker 镜像两键、游标文件 roundtrip）✓
3. **上下文预算**：§2 七文件圈得出（五代码/测试 + 两图数据），全在 d_collab 域内，本卡零 d_ledger 改动 ✓
4. **类型标注**：逻辑型子系统，行为验收全机内可闭环（真 SQLite 夹具 + 假 client）；无真机清单项 ✓
5. **接缝覆盖（双向）**：
   - 测试→缝：全部测试入口=Service 六方法（Pending/Consume/Mentions/ListRooms/MarkRead/Unread，缝#1 入站门面）；假 client 测试经接缝#2（测试替身，spec 测试接缝清单 #2 的正当用途）；无入口偏离 ✓
   - 缝→测试：缝#1 本卡负责的断言面（Pending 两条、Mentions 未消费、Consume 四条、ListRooms 五条、MarkRead/Unread 两条）逐条有缝级测试（§3 T5.1 映射表）；缝#2 的假替身用于需要精确时刻的测试 ✓
   - 内部锁：三支字面量 pin（TestRoomEventTypeLiteralMatchesLedger 既有、TestRoomStatusLiteralMatchesLedger 既有、TestConsumedEventTypeLiteralMatchesLedger 新增），声明见 §7 ✓
   - 退路同闸：本计划步骤正文无条件退路（无「若意外先绿就改直喂 X」条款）；「若行号漂移按实际修正」是机械修正不是换入口；变异⑤⑥ 标「可选」不改变任何测试入口 ✓

## 6. 与 C6 的交界（下游照此写）

- **SetCursorStore 组装接线**：C6（gateway HTTP）在 `internal/agentd/server.go` 组装点 `collab.New(ledgerapi.New(s.ledger))` 后补一行 `SetCursorStore(cursor.New(filepath.Join(cfg.DataDir, "room-cursors.json")))`——DataDir 只有组装点侧持有（collab.New 冻结签名不带路径）。C6 若未接线，游标回落纯内存（拍板 5.4 语义内，重启丢失无害），不阻塞功能。C7（CLI）如需文件介质同形接线。
- **C6 消费的签名**（本卡产出，逐字符）：`ListRooms(project string) ([]proto.RoomSummary, error)`、`History(roomID string, beforeSeq int64, limit int) ([]proto.LedgerEvent, error)`、`Mentions(member string, afterSeq int64, limit int) ([]proto.LedgerEvent, error)`、`MarkRead(member, roomID string, uptoSeq int64) error`、`Unread(member, roomID string) (int, error)`、`Consume(seq int64, consumer string) error`——全部与契约 §3.3 一字一致。
- **收件箱 mention 源**：C6 的 inbox mention 源直接消费本卡 `Mentions`（已含未消费过滤），三源判据中的「mention 源=未消费提及，不含已消费项」在本卡锁住。
- **金样本**：RoomSummary 不进本卡验收面（C6/C8 的集成冒烟按澄清三走孪生夹具）。

## 7. 内部锁声明（占位符扫描节的一部分，逐条自我声明）

| 测试 | 入口 | 为什么从声明缝构造不出 |
| --- | --- | --- |
| TestRoomEventTypeLiteralMatchesLedger（既有） | 无缝 | 跨包字符串字面量等式（room.RoomEventType "room_message" == ledger.EvMessageConsumed 侧词表）只能在看见两侧的测试文件里断言；任何 Service 缝都观测不到「两个包的字面量相等」 |
| TestRoomStatusLiteralMatchesLedger（既有） | 无缝 | 同上：room.IsTerminalStatus 字面量（"已完成"/"终止"）与 ledger.StatusDone/StatusClosed 的等式；终态判定本身已由缝级测试（TestSendRejectsTerminalCardRoom 等）锁行为 |
| TestConsumedEventTypeLiteralMatchesLedger（新增） | 无缝 | 同上：room.ConsumedEventType "message_consumed" 与 ledger.EvMessageConsumed 的等式；消费行为本身已由缝级测试（TestConsume*）锁住，pin 是防两包词表漂移的编译期护栏 |

退路同闸自查：§3 各 task 步骤正文无条件退路。

## 8. 占位符扫描自我声明

全文无 TBD／「加适当的错误处理」／「同 Task N」／描述做什么却不给代码。代码块均完整可编译，且**全部在本 plan 轮临时装回实测**：红侧（探针 14 支红）、绿侧（`go test ./internal/collab/...` 18 支全 PASS）、变异侧（①②③④ 全红，⑤⑥ 可选）证据在台账 P1/P3/P5-P7。`service.go` 整文件给出替换块、`room.go` 给出追加块、`cursor/cursor.go`/`readmodel_test.go`/`cursorfile_test.go` 全文件给出。正当出口（「照抄既有 harness」例外）使用情况：**未使用**——测试夹具（newFixture/mustCard/mustBind）既有形态直接沿用，全部代码给出全文。

`nowFn` 依赖的两阶段（T5.1 红跑前测试文件临时声明 → T5.2 落地后删除）是**顺序依赖的机械动作**，非占位——探针已验证该形态（P1 声明后 14 支红、P3 删除后全绿）。视图 diff 的 nodesAdded `line`/`order` 字段按本 plan 代码块排版预填，附机械修正说明（实现过程行号漂移按最终文件实际符号行改）——值在提交时确定，非 TBD。

## 9. 跨卡审计输入（L3 重档：扇出前由协调者组织独立上下文审计；本节提供素材并自审待拍板）

**跨卡签名清单（本卡 Produces / 相关 Consumes，逐字符）**：

| 签名 | 产出方 | 消费方 | 一致性 |
| --- | --- | --- | --- |
| `Pending(consumer string) ([]proto.LedgerEvent, error)` | 本卡（签名不变） | C6 inbox、C7 room inbox | 与契约 §3.3 一字一致 |
| `Consume(seq int64, consumer string) error` | 本卡（签名不变） | C6（若有直接消费）、C8（房间页未读交互后可触发） | 与契约 §3.3 一字一致 |
| `Mentions(member string, afterSeq int64, limit int) ([]proto.LedgerEvent, error)` | 本卡（签名不变，补未消费过滤） | C6 inbox mention 源 | 与契约 §3.3「未消费提及」一致 |
| `ListRooms(project string) ([]proto.RoomSummary, error)` | 本卡（签名不变） | C6 GET /api/rooms、C7 room list、C8 会话列表 | 与契约 §3.3 一字一致 |
| `MarkRead(member, roomID string, uptoSeq int64) error` / `Unread(member, roomID string) (int, error)` | 本卡（签名不变） | C6 POST read、C8 房间页 | 与契约 §3.3 一字一致 |
| `SetCursorStore(st *cursor.Store)` | 本卡（A.1 机制新增） | C6/C7 组装点接线（§6） | 契约 New(lc) 冻结签名不动；游标介质归 A.1 决策 |
| room 新符号 `ReadAllEvents/ConsumedSeqs/RoomIDOf/MessageKind/ConsumedEventType` | 本卡 | 仅本卡 Service 方法 | C6/C7 不消费（不越卡） |
| Consumes：`client.LedgerClient.ListAllCards/EventsFromAsc/DriverLease/RecordMessageConsumed` | 契约 §3.4 | 本卡 | 逐字对照契约，零扩方法（扩方法先回 contract） |

**spec 故事归属（本卡）**：故事 1（深夜清账）的收件箱未消费提及源（Mentions）与列表/未读读模型（ListRooms/MarkRead/Unread）本卡实现，C6/C8 承接端点与页面；故事 3（批量 bug 并入）的 merged 只读投影本卡列表面锁定（TestListRoomsReadOnlyFlags），C4 已锁 Send 执法面；故事 6（跨卡 @）的 @定向消费（Pending/Mentions）本卡实现。故事 2/4/5 的指针/换绑/简报书写面归 C4/C7。

**图覆盖债**：本卡产出物（cursor 子包 + room 新助手 + SetCursorStore）在基线图里无节点——T5.5 视图增量是本卡的图登记动作；常量不入图（RoomEventType 先例）；未导出符号（nowFn、cursor 字段）不入图。已如实记录。

## 10. 自审三查

1. **spec 覆盖**：契约 §4 读面 7 条 + 房间生命周期 5 条逐条指到 T5.1 测试；Mentions 未消费（§3.3 注释）指到 TestMentionsExcludesConsumed；A.1 游标介质 + A.5 护栏注记指到 T5.2/T5.3；不属本卡的半边（History 已锁、inbox 三源编排 C6、金样本双侧 C6/C8）显式移交 ✓
2. **占位符扫描**：§8 ✓
3. **跨 task 签名一致性**：§9 清单；六方法签名与契约 §3.3 逐字一致（service.go:107-186 亲读），room/cursor 新符号全列 ✓

## 11. 提交切分

- commit 1（T5.1-T5.4）：`feat(collab): 消费恰好一次门面路径 + 注意力读模型 + 游标介质（B156.2.5 欠账#2collab半+#3+A.1+A.5）`——含 room.go 追加、cursor/cursor.go、service.go、readmodel_test.go、cursorfile_test.go。
- commit 2（T5.5）：`graph(b156.2.5): cursor 子包与 room 读助手入图（best+charter-4 视图增量）`——含 codegraph/best.json、codegraph/diffs/cards-B156.2-charter-4.json。
- 台账 `docs/superpowers/ledgers/ledger-b156.2.5-plan.md` 随实现轮持续追加，与产出物同批提交。
- **两提交均须通过 `gofmt -l internal/collab/` 零输出**（C4 因格式被验收拦回的教训，与 build/test/vet/graph 全部正交）。

## 12. 明确不做（防越界）

- 不写 C6/C7/C8（gateway/CLI/web）任何代码；target.json 预定声明（d_gateway/d_cli→d_collab）不归本卡。
- 不改 proto（含 RoomSummary 形状）、types.go 词表、ledger 生产代码、金样本 fixture（rooms_fixture_test.go / rooms.test.ts 一字不动）、契约 Service 方法签名。
- **不给 ledger.Store 加导出时钟钩子**：活性翻转的时钟同步用假 client + 同一可拨源实现（§4 假红假绿族），跨 d_ledger 表面是越界。
- 不改 History 数据面（C4 已锁行为；A.5 覆盖其 1000 窗性能，本卡不重写）。
- 不 absorb 视图（finish 阶段动作）；不修裸 graph check 的既有 dead（absorb 前固有形态）。
- 不加真机清单项（本卡全行为机内可闭环）。