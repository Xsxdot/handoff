# B156.3.4 plan 节点台账（边干边落）

约定：每确立一个事实追加一行；命令与原始输出随行。本文件与 b156.3.4-plan.md 同批提交。

- 2026-08-26 输入齐读：spec（384 行，头部已批准）、contract（593 行，冻结+§13.5 归属更正+§14 叙事换绑轮+§15 澄清四条）、breakdown（620 行，岔口拍板记录在 §41-94）。
- 2026-08-26 当前分支 `cards/B156.3.4-charter`，工作树含本卡 charter 前的既有提交。
- 2026-08-26 组装点核实（server.go）：SetupAutomation(2236) 构造 ledgerapi.Facade→s.autoLedger、scheduling.New(facadeAsRegistry{facade})、collab.New(facade)、coordinatorRunner{h:hostapi.New()}、keystone.New(runner, roomNarrator{c:rooms}, facade, attachLocator{})；SetKeystone(2255)/SetScheduling(2252)/Scheduling(2258)/Keystone(2261)/PtyAPI(2249)；facadeAsRegistry(2266-2293)+translateRegistryErr(2302-2313)；coordinatorRunner(2317-2334)；roomNarrator(2352-2359，Pointer 已由 C4 填肉，正文「已由 C4」在场)；attachLocator(2363-2373，SessionID 空→报错，Command=CLI+" --session "+SessionID)。
- 2026-08-26 keystone 核实（keystone.go 194 行）：LaunchForCard(117-123) 调 launchRound(card,"") **忽略入参 spec**；launchRound(159-173) 自造 `spec := keysclient.SessionSpec{Workdir:""}`——澄清 2 的骨架期欠账在案；Decide(75-85) 四类唤醒+attach 拒醒；AttachState(126)/AttachActive(129)/SetAttach(139)/Locate(146-155，无会话→"该卡没有绑定的协调者会话")；briefing(177-193)。
- 2026-08-26 keysclient 核实（keysclient.go 78 行）：SessionSpec{CLI,HomeDir,Model,Workdir,Env}，HomeDir 注释「协调者载体禁空，由配置审核保证」；Runner.Launch/Resume 铁律「prompt 必须非空」注释在案（2026-08-26 删空 prompt 承诺）；Narrator.Say；LedgerView 四方法。
- 2026-08-26 scheduling 核实（scheduling.go 567 行）：LaunchAdmit(198-207) 校验 Role==coordinator；Release(210-215) 幂等；Carrier(138)/Carriers(147)/Squad(176) 读面；**无 Squads()/Launches() 枚举方法**——协调者小队识别无公开枚举入口；admitInto/acquire/bindingFor 消费覆盖>载体缺省。
- 2026-08-26 路由核实：registerLedgerRoutes(ledgerapi.go:30-52) 内注册 POST step 等，withLedger(54-64) 包全部；Handler()(server.go:451-585) 中 api mux 由 s.auth(mux) 包鉴权，/api/ 前缀不落 SPA 兜底；forwardIfRequested(forward.go:54-74) 支持 ?machine= 转发，isForwarded(43) 防环；attach 端点契约 §11 注明「跨机沿用 ?machine= 转发」。
- 2026-08-26 CLI 核实：card 创建命令是 `card add`（cmd/card.go:29-49，直接 openLedger 写库），**仓内无 `card create` 命令**——契约/拆解写的 `card create --coordinate` 与现状命名不符；card_node.go 展示 CLI→本机 agentd 的既有模式（LocalEndpoint→client.New→cl.CardStep）；TargetEndpoint(190-237) 支持 --agentd 覆盖（测试可指 httptest）。
- 2026-08-26 竖切 harness 核实（slice_test.go 277 行）：fakeRunner(只记 prompts，不记 spec)/fakeNarrator/recordingLedger/registryViaFacade 四夹具可照抄；TestIgnitionVerticalSlice 当前把 spec{CLI:"opencode"} 传给 LaunchForCard 但不验证 spec 被消费——launchRound 消费 spec 的改动不会翻红既有测试。
- 2026-08-26 测试基座核实：newLedgerEnv(ledgerapi_test.go:138-149)/newNoPTYLedgerEnv(153-172)/ledgerPost/ledgerGet/ledgerPut 等 HTTP helper；SetupAutomation 在 scheddispatch_test.go:70 先例（真实 facadeAsRegistry + scheduling.Service）；CLI 测试 runLedgerCLI(ledgercli_test.go:24-45) 写 Token:"t" 配置，dispatch_autoregister_test.go:204-207 先例（configPath/targetName/agentdURL 包级变量指向 httptest）。
- 2026-08-26 待拍板问题一（协调者小队识别）：契约 §6.2/§11 未定义 launch 端点如何确定协调者小队（无卡→小队映射，scheduling 无 Squads() 枚举，contract §4.3 只有 Squad(name)/Carriers()）；澄清 2 的 LaunchAdmit→Binding→Carrier 链缺一个 squad 名来源。
- 2026-08-26 待拍板问题二（keystone.go 越界）：澄清 2 明说 launchRound 须改为消费入参 spec（keystone.go:159-160），而 K4 有界文件集（breakdown §三-K4）只含 internal/agentd+cmd，不含 internal/keystone；K4 验收「携带非空 SessionSpec 防空 spec 回潮」经真实 keystone 穿过 HTTP 断言时恒红，除非放行 keystone.go。