# C1.7 实现计划：handoff 宿主挂载 charter 二期 viewer

> 本计划对应 `docs/superpowers/specs/2026-08-24-codegraph-host-mount-spec.md`、冻结契约
> `docs/superpowers/specs/c1.7-contract.md` 和拆解稿 `docs/superpowers/specs/c1.7-breakdown.md`。
> C1.7 是 L3 轻档：不建立 Gateway/Web/Desktop 子卡，三个工作项 T1/T2/T3 在同一轮
> implement 中完成，并在同一个实现提交中收口。

## 0. 冻结前提、基线与范围

### 0.1 本轮已经复核的基线

开工时当前树不含 `dd95a6e2d`/`b29fc0f45` 对象；按补充裁定执行了 `git fetch origin && git merge origin/main`，`origin/main` 从 `d4053c0a` 更新到 `b29fc0f4`，合并提交为 `0bd11bca`，无冲突。`b29fc0f45` 是当前 HEAD 的祖先。

合并后实读值：

- 根 `go.mod`：`github.com/Xsxdot/charter/graph v0.6.0`。
- `desktop/go.sum`：`github.com/Xsxdot/charter/graph v0.6.0` 的 module/hash 两条记录。
- 前端 `web/package.json` 的脚本：`typecheck = tsc -b`、`build = tsc -b && vite build`、`test = vitest run`。
- `internal/agentd/webroute_test.go#consoleTestEnv` 已提供真实 `Server`、真实 SQLite store、`httptest.Server` 和有效 session cookie。

以下命令在未改实现的基线亲自运行过，计划中的期望值以此为准：

```text
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go build ./...
exit code: 0

GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go test ./cmd -run TestRepoContractGate -count=1
ok  github.com/Xsxdot/handoff/cmd 0.037s

cd desktop && GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go list -m -f '{{.Path}} {{.Version}}' github.com/Xsxdot/charter/graph
github.com/Xsxdot/charter/graph v0.6.0

GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go list -m -f '{{.Path}} {{.Version}}' github.com/Xsxdot/charter/graph
github.com/Xsxdot/charter/graph v0.6.0

cd desktop && GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go mod tidy && git diff --exit-code -- go.mod go.sum
exit code: 0

npm --prefix web run typecheck
exit code: 127
sh: 1: tsc: not found

npm --prefix web run build
exit code: 127
sh: 1: tsc: not found

npm --prefix web test -- Shell.test.tsx
exit code: 127
sh: 1: vitest: not found

git diff --exit-code -- .github/workflows/ci.yml
exit code: 0
```

前端基线的三个失败只说明当前工作树没有 `web/node_modules`，不把它们写成业务回归；实现者在 T2 验证前先执行 `npm --prefix web ci`，该命令只安装 `web/package-lock.json` 已锁定的依赖。

### 0.2 外部前置与单提交规则

协调者必须先完成 charter 仓的 `graph/v0.7.0` tag 发布和可消费门；实现者消费精确 tag，不写 `replace` 冒充 tag，不改 `c1.7-contract.md`、spec、breakdown 或 `codegraph/target.json`。本轮不发 tag、不重打包、不重启 agentd；这些属于协调者真机收口。

T1 必须先把根模块从 `v0.6.0` 升到 `v0.7.0`，T3 才能运行 desktop tidy。若先 tidy，desktop 会按根模块的旧 `v0.6.0` 解回去；因此依赖顺序是：

```text
协调者完成 graph/v0.7.0 tag
             │
             ▼
T1 Gateway：根 go.mod/go.sum + /codegraph/app/ mux
             │
             ├──────────────► T3 DesktopAlign：desktop tidy 到 v0.7.0
             │
             └──────────────► T2 Web：iframe 薄壳 + Shell + 删除旧副本
                                      │
                                      ▼
                         定向验证、范围审计、同一提交
```

T2 必须使用 T1 冻结的 `/codegraph/app/` 字符串；T2 与 T3 在 T1 后均可做，但三者不拆提交。最终提交必须一次性包含 T1/T2/T3 的全部实现文件与本台账，禁止先交 desktop 独立提交或先交 Web 独立提交。

### 0.3 不在本轮的文件与行为

- 不改 `internal/agentd/codegraph.go` 的两条 GET API、响应体、`?machine=` 转发或错误语义。
- 不改 `internal/agentd/forward.go`、`web/src/api/client.ts`；后者的既有 `fetchCodegraph` / `fetchCodegraphSource` 是 API wire helper，不随旧 viewer 副本删除。
- 不改 `.github/workflows/ci.yml`，不删除或放宽 Windows 交叉编译、Windows vet、install.sh 和 desktop tidy 门。
- 不改 `codegraph/target.json`，尤其不删除或迁移已恢复的 `d_cli → d_ledger` `ledger.Store` 条目。
- 不建立第二个 viewer 源码副本，不新增 CORS、`postMessage`、第二端口、独立 listener、token 旁路或 handoff 侧 graph state。

## 1. 工作项接口与有界文件集

### T1 Gateway：主模块升版与 auth 内同源静态挂载

**允许变更文件：**

- `go.mod`
- `go.sum`
- `internal/agentd/server.go`
- `internal/agentd/webroute_test.go`

**只读参照：** `internal/agentd/webhandler.go#newSPAHandler`、`internal/agentd/codegraph.go#Server.handleProjectCodegraph`、`internal/agentd/codegraph.go#Server.handleProjectCodegraphSource`、`internal/agentd/forward.go#Server.forwardIfRequested`、`internal/webui.FS`。

**Consumes：**

```go
func FS() fs.FS
func newSPAHandler(fsys fs.FS, log *slog.Logger) http.Handler
func (s *Server) auth(next http.Handler) http.Handler
func (s *Server) Handler() http.Handler
```

其中 `FS()` 是依赖 `github.com/Xsxdot/charter/graph/webui` 的 `charterwebui.FS`，其 FS 根直接包含 `index.html`；`newSPAHandler` 已负责 GET/HEAD 文件命中、深路径回落 index、hash 资源缓存和非读方法 405，T1 不复制这套逻辑。

**Produces：**

```go
charterwebui.FS() fs.FS // 仅作为 /codegraph/app/ 下 newSPAHandler 的输入
func (s *Server) Handler() http.Handler // 保持既有 /api、/ws、/console 和根 SPA 行为，并新增 /codegraph/app/
func TestCodegraphRoute(t *testing.T) // 通过 httptest.Server + auth + mux 锁住静态挂载接缝
```

### T2 Web：iframe 薄壳、Shell 接线、旧副本删除与 fullPageRoute

**允许变更文件：**

- `web/src/app/codegraph/CodegraphFrame.tsx`（新建）
- `web/src/app/shell/Shell.tsx`
- `web/src/app/shell/Shell.test.tsx`
- 删除 `web/src/app/codegraph/CallTree.test.tsx`
- 删除 `web/src/app/codegraph/CallTree.tsx`
- 删除 `web/src/app/codegraph/CodegraphPage.test.tsx`
- 删除 `web/src/app/codegraph/CodegraphPage.tsx`
- 删除 `web/src/app/codegraph/DetailPanel.test.tsx`
- 删除 `web/src/app/codegraph/DetailPanel.tsx`
- 删除 `web/src/app/codegraph/DomainDetail.test.tsx`
- 删除 `web/src/app/codegraph/DomainDetail.tsx`
- 删除 `web/src/app/codegraph/DomainPanorama.test.tsx`
- 删除 `web/src/app/codegraph/DomainPanorama.tsx`
- 删除 `web/src/app/codegraph/FocusGraph.test.tsx`
- 删除 `web/src/app/codegraph/FocusGraph.tsx`
- 删除 `web/src/app/codegraph/domainlayout.test.ts`
- 删除 `web/src/app/codegraph/domainlayout.ts`
- 删除 `web/src/app/codegraph/domains.test.ts`
- 删除 `web/src/app/codegraph/domains.ts`
- 删除 `web/src/app/codegraph/graphmath.test.ts`
- 删除 `web/src/app/codegraph/graphmath.ts`
- 删除 `web/src/app/codegraph/useCodegraph.ts`

**只读参照：** `web/src/app/workbench/useWorkbench.ts#BaseDir`、`web/src/app/workbench/useWorkbench.ts#useWorkbench`、`web/src/app/tree/ProjectTree.tsx#workspaceBase`、`web/src/api/client.ts`、`web/src/App.tsx#AppRoutes`、`web/src/app/shell/Shell.test.tsx#renderShell`。

**Consumes：**

```ts
export interface BaseDir {
  projectName: string
  // 其它字段由既有工作台使用，薄壳不读取
}

export interface WorkbenchApi {
  base: BaseDir | null
}

export function useWorkbench(): WorkbenchApi
export function workspaceBase(project: ProjectNode, machine: string, ws: Workspace): BaseDir
```

Shell 仍从 `ProjectTree` 的 `onOpenCodegraph={() => navigate('/codegraph')}` 进入；T2 只从 `wb.base?.projectName` 产生 iframe query，不从项目树复制选择器或直接调用 API client。

**Produces：**

```tsx
export interface CodegraphFrameProps {
  project: string
}

export function CodegraphFrame({ project }: CodegraphFrameProps): JSX.Element
```

路由消费的完整调用形状为：

```tsx
<Route
  path="/codegraph"
  element={<CodegraphFrame project={wb.base?.projectName ?? ''} />}
/>
```

### T3 DesktopAlign：嵌套模块依赖账对齐

**允许变更文件：** `desktop/go.sum`；如 `go mod tidy` 按工具实际需要重写 `desktop/go.mod`，只允许其与既有 `replace github.com/Xsxdot/handoff => ../` 相关的无业务变更，禁止添加 charter 业务 import 或改 CI。按当前 `v0.6.0` tidy-clean 基线，预期只变更 `desktop/go.sum`。

**只读参照：** 根 `go.mod`/`go.sum`、`desktop/go.mod`、`.github/workflows/ci.yml`、`release_workflow_test.go`。

**Consumes：** T1 产出的根模块 `github.com/Xsxdot/charter/graph v0.7.0` 与 `desktop/go.mod` 的既有 handoff replace。

**Produces：** `desktop/go.sum` 中 `github.com/Xsxdot/charter/graph v0.7.0` 的 module/hash 记录；在根模块和 `desktop/` 运行下列命令均得到精确 resolved 版本：

```text
github.com/Xsxdot/charter/graph v0.7.0
```

T3 不新增运行时导出函数、HTTP API、wire 字段或日志点；这是 module resolver/CI manifest 边界，不能用应用日志代替 clean diff。

## 2. 单轮实现步骤

### T1 步骤：先锁完整路由缝，再最小实现

#### T1.1 基线判据与最小测试范围

动源码前重新运行本计划 §0.1 的根 `go build ./...`、`go test ./cmd -run TestRepoContractGate -count=1`；期望分别是退出码 0 与 `ok github.com/Xsxdot/handoff/cmd ...`。T1 只跑 `./internal/agentd` 的 route-level 用例和现有 codegraph API 用例，不跑全仓测试；测试范围为 `internal/agentd/webroute_test.go`、`internal/agentd/codegraph_test.go` 所在包。

#### T1.2 写失败测试并跑红

在 `internal/agentd/webroute_test.go` 追加下面的完整测试。它复用现有 `consoleTestEnv`、`getWithCookie`，入口必须是 `ts.URL` 的真实 HTTP 请求，不能改成直接调用 `newSPAHandler`；动态从 charter index 找一个 hashed asset，避免把依赖版本的 hash 文件名写死。

```go
func TestCodegraphRoute(t *testing.T) {
	ts, cookie := consoleTestEnv(t)

	read := func(resp *http.Response) []byte {
		t.Helper()
		defer resp.Body.Close()
		body, err := io.ReadAll(resp.Body)
		if err != nil {
			t.Fatalf("读取 %s: %v", resp.Request.URL.Path, err)
		}
		return body
	}

	rootResp := getWithCookie(t, ts, "/", cookie)
	if rootResp.StatusCode != http.StatusOK {
		t.Fatalf("GET / 状态码 = %d，want 200", rootResp.StatusCode)
	}
	rootBody := read(rootResp)

	charterResp := getWithCookie(t, ts, "/codegraph/app/", cookie)
	if charterResp.StatusCode != http.StatusOK {
		t.Fatalf("GET /codegraph/app/ 状态码 = %d，want 200", charterResp.StatusCode)
	}
	charterBody := read(charterResp)
	if bytes.Equal(charterBody, rootBody) {
		t.Fatal("/codegraph/app/ 返回了 handoff 根 index，而不是 charter index")
	}
	if !bytes.Contains(charterBody, []byte("<html")) {
		t.Fatalf("charter index 不是 HTML，body 前缀 = %q", charterBody[:min(len(charterBody), 120)])
	}

	deepResp := getWithCookie(t, ts, "/codegraph/app/graph/domains/core", cookie)
	if deepResp.StatusCode != http.StatusOK {
		t.Fatalf("GET charter 深路径状态码 = %d，want 200", deepResp.StatusCode)
	}
	if deepBody := read(deepResp); !bytes.Equal(deepBody, charterBody) {
		t.Fatal("charter 深路径没有回落到同一份 charter index")
	}

	assetMatch := regexp.MustCompile(`(?:src|href)="(\./assets/[^"?#]+)"`).FindSubmatch(charterBody)
	if len(assetMatch) != 2 {
		t.Fatalf("charter index 未找到 assets 资源引用，body 前缀 = %q", charterBody[:min(len(charterBody), 240)])
	}
	assetPath := strings.TrimPrefix(string(assetMatch[1]), ".")
	assetResp := getWithCookie(t, ts, "/codegraph/app"+assetPath, cookie)
	if assetResp.StatusCode != http.StatusOK {
		t.Fatalf("GET charter asset 状态码 = %d，want 200", assetResp.StatusCode)
	}
	if got := assetResp.Header.Get("Cache-Control"); got != "public, max-age=31536000, immutable" {
		t.Fatalf("charter hashed asset Cache-Control = %q，want immutable", got)
	}
	_ = read(assetResp)

	postReq, err := http.NewRequest(http.MethodPost, ts.URL+"/codegraph/app/", nil)
	if err != nil {
		t.Fatalf("构造 POST: %v", err)
	}
	postReq.AddCookie(&http.Cookie{Name: sessionCookieName, Value: cookie})
	postResp, err := ts.Client().Do(postReq)
	if err != nil {
		t.Fatalf("POST /codegraph/app/: %v", err)
	}
	defer postResp.Body.Close()
	if postResp.StatusCode != http.StatusMethodNotAllowed {
		t.Fatalf("POST /codegraph/app/ 状态码 = %d，want 405", postResp.StatusCode)
	}

	unauthResp, err := ts.Client().Get(ts.URL + "/codegraph/app/")
	if err != nil {
		t.Fatalf("未登录 GET /codegraph/app/: %v", err)
	}
	defer unauthResp.Body.Close()
	unauthBody, err := io.ReadAll(unauthResp.Body)
	if err != nil {
		t.Fatalf("读取未登录响应: %v", err)
	}
	if unauthResp.StatusCode != http.StatusUnauthorized {
		t.Fatalf("未登录 GET /codegraph/app/ 状态码 = %d，want 401", unauthResp.StatusCode)
	}
	if strings.Contains(strings.ToLower(string(unauthBody)), "<html") {
		t.Fatalf("未登录响应被任一 SPA index 吞掉：%q", unauthBody)
	}
}
```

测试新增 import 必须是标准库 `bytes`、`regexp`；`io`、`net/http`、`strings` 已在 `webroute_test.go` 使用。若当前 Go 版本没有预声明的 `min`，同文件必须使用显式边界表达式而不是新增 helper；本仓 `go.mod` 已声明 Go 1.26.1，所以上述 `min` 直接可编译。

运行：

```text
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go test ./internal/agentd -run '^TestCodegraphRoute$' -count=1
```

基线预期为红：在未注册专属前缀时，`/codegraph/app/` 会落到 handoff 根 SPA，charter index 与根 index 的反向断言失败；不得因为 POST 的单项已经是 405 就跳过整支测试。

#### T1.3 最小实现：升版、别名和前缀注册

在 `go.mod` 把直接 require 精确改为下面一行，保留其它依赖原样；执行 `GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go mod tidy` 生成根 `go.sum`，不得添加 `replace`：

```text
github.com/Xsxdot/charter/graph v0.7.0
```

在 `internal/agentd/server.go` 增加别名 import：

```go
charterwebui "github.com/Xsxdot/charter/graph/webui"
```

将 `Server.Handler` 的静态 mux 装配改成下面完整代码；API、WS、根 handoff FS 和 auth 包裹关系必须保持原顺序。专属路径注册在根 fallback 之前，`StripPrefix` 的参数精确为无尾斜杠的 `"/codegraph/app"`。成功路径用现有结构化 `s.log.Info` 留下挂载来源，错误/非读请求继续由 `newSPAHandler` 的结构化日志负责；不使用 `print`。

```go
	mux := http.NewServeMux()
	mux.Handle("/api/", api)
	mux.Handle("/ws/", api)
	mux.Handle(
		"/codegraph/app/",
		http.StripPrefix("/codegraph/app", newSPAHandler(charterwebui.FS(), s.log)),
	)
	mux.Handle("/", newSPAHandler(webui.FS(), s.log))

	// charter viewer 与 handoff 自有 console 是两棵 FS；专属前缀必须先注册，
	// 且仍在 auth 内，避免深路径错误回落到另一棵 index 或绕过会话鉴权。
	s.log.Info("代码图 viewer 静态资源已挂载",
		"path", "/codegraph/app/", "source", "charter/graph/webui")

	// /console 是唯一不经主令牌/cookie 的路由——ticket 本身就是它的凭据，
	// 因此它挂在 auth 之外、hostGuard 之内。Go 1.22 的 mux 按精确度选择，
	// "GET /console" 胜过 "/"。
	root := http.NewServeMux()
	root.Handle("/", s.auth(mux))
	root.HandleFunc("GET /console", s.handleConsole)
```

实现注意事项：

- 不对 `charterwebui.FS()` 再拼 `dist/`；v0.7.0 的 FS 根已经是 dist 子树。
- 不把 charter handler 注册到 `root` 的 auth 外层；未登录资源必须先收到现有鉴权响应。
- 不改两条 API 的注册行 `GET /api/projects/{name}/codegraph` 与 `GET /api/projects/{name}/codegraph/source`。
- 文件头/导出点注释保持职责、边界、`FS` 根和 auth 前置关系可读；不复制标准库或 `newSPAHandler` 的实现。

#### T1.4 跑绿与 T1 最小范围验收

依次运行以下命令；每条命令只覆盖触及的 Go 包或负向范围：

```text
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go test ./internal/agentd -run '^TestCodegraphRoute$' -count=1
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go test ./internal/agentd -run '^TestCodegraph(Endpoint|ComparisonDataOptional|ComparisonDataTargetFailure|Source)$' -count=1
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go build ./...
rg -n 'github.com/Xsxdot/charter/graph v0\.7\.0|charterwebui|StripPrefix\("/codegraph/app"' go.mod go.sum internal/agentd/server.go
rg -n 'replace .*charter/graph' go.mod
git diff --exit-code -- internal/agentd/codegraph.go internal/agentd/forward.go
```

期望：两支 Go 测试和构建退出码均为 0；第一条 `rg` 命中 v0.7.0、别名和无尾斜杠前缀；第二条 `rg` 无输出且退出码为 1（把“无 replace”作为明确负向断言记录）；API/forward 负向 diff 退出码为 0。这里不把 `rg` 的退出码 1 当业务失败。

### T2 步骤：先锁声明缝，再落薄壳和删除旧副本

#### T2.1 安装前端依赖并声明最小测试范围

在 T2 开始先执行：

```text
npm --prefix web ci
```

只跑 `web/src/app/shell/Shell.test.tsx` 这一个既有 Shell harness、`npm --prefix web run typecheck` 和 `npm --prefix web run build`；不跑全量 Vitest。Shell 测试已有真实 `MemoryRouter`/`AppRoutes`、`ProjectTree` fixture、工作区选择、Breadcrumb 和 FileTree 断言；不另建 `CodegraphFrame.test.tsx`，因为薄壳没有业务状态，iframe src 与两个外壳消费者必须从声明入口 `AppRoutes → Shell` 锁。

#### T2.2 写失败测试并跑红

在 `web/src/app/shell/Shell.test.tsx` 保留既有 `renderShell` 和 `tree` fixture，追加以下两支测试。第一支从真实项目树选择工作区、点击真实“代码图”入口，穿过 `AppRoutes`、`Shell` route 和新薄壳，逐个验证 query 序列化；第二支同时锁中央 iframe、Breadcrumb 和右栏 FileTree，并验证离开 `/codegraph` 后两者恢复。

```tsx
it.each([
  ['空格', 'project name'],
  ['斜杠', 'project/name'],
  ['中文', '项目/中文'],
])('代码图 iframe 对项目名（%s）只做 query 编码', async (_kind, projectName) => {
  const specialTree: ProjectTreeResp = {
    ...tree,
    projects: tree.projects.map((project) => ({ ...project, name: projectName })),
  }
  vi.mocked(fetchProjectTree).mockResolvedValue(specialTree)

  renderShell()
  fireEvent.click(await screen.findByText('integration/b2-b3'))
  await waitFor(() => expect(screen.getByText('文件')).toBeInTheDocument())
  fireEvent.click(screen.getByRole('button', { name: '代码图' }))

  await waitFor(() => expect(document.querySelector('iframe[title="代码图"]')).not.toBeNull())
  const frame = document.querySelector('iframe[title="代码图"]')
  expect(frame?.getAttribute('src')).toBe(
    `/codegraph/app/?project=${encodeURIComponent(projectName)}`,
  )
})

it('/codegraph 同时隐藏 Breadcrumb/FileTree，回到工作台后恢复', async () => {
  renderShell()
  fireEvent.click(await screen.findByText('integration/b2-b3'))
  await waitFor(() => expect(screen.getByText('文件')).toBeInTheDocument())
  expect(screen.getByLabelText('当前位置')).toBeInTheDocument()

  fireEvent.click(screen.getByRole('button', { name: '代码图' }))
  await waitFor(() => expect(document.querySelector('iframe[title="代码图"]')).not.toBeNull())
  expect(screen.queryByText('文件')).not.toBeInTheDocument()
  expect(screen.queryByLabelText('当前位置')).not.toBeInTheDocument()

  fireEvent.click(screen.getByText('integration/b2-b3'))
  await waitFor(() => expect(screen.getByText('文件')).toBeInTheDocument())
  expect(screen.getByLabelText('当前位置')).toBeInTheDocument()
})
```

在旧 `CodegraphPage` 和旧 `fullPageRoute` 下运行：

```text
npm --prefix web test -- Shell.test.tsx
```

预期新增断言先红：旧路由不会渲染目标 iframe，且 `/codegraph` 未纳入 `fullPageRoute` 时外壳反向断言不能同时成立。测试入口不是直接渲染 `CodegraphPage`，因此不能把删除旧组件当成测试通过。

#### T2.3 最小实现：新文件、Shell route 和整页白名单

创建 `web/src/app/codegraph/CodegraphFrame.tsx`，完整内容如下。文件头必须写职责与边界；导出函数注释必须写 `project` 参数、返回 iframe、空串合法和不会读取 token/cookie/API 的注意事项。组件不写 `fetch`、`document.cookie`、`Authorization`、`localStorage`、`postMessage`、项目选择器或 `console`。

```tsx
// CodegraphFrame —— /codegraph 的宿主薄壳。
//
// 职责：把当前工作台项目名编码进同源 charter viewer iframe 的 URL。
// 边界：不取 API、不读取 token/cookie、不复制项目选择器或 viewer 状态；
// charter viewer 自己负责同源请求与二期代码图渲染。
export interface CodegraphFrameProps {
  project: string
}

// 参数：project 是当前 BaseDir.projectName；空串是合法输入，viewer 会跳过取数。
// 返回：指向宿主 /codegraph/app/ 的同源 iframe；project 只进入 query 且经过编码。
// 注意：不要把 machine、token、cookie、host 或第二端口写进 src。
export function CodegraphFrame({ project }: CodegraphFrameProps): JSX.Element {
  const src = `/codegraph/app/?project=${encodeURIComponent(project)}`
  return (
    <iframe
      title="代码图"
      src={src}
      className="h-full w-full border-0"
    />
  )
}
```

在 `web/src/app/shell/Shell.tsx`：

1. 删除 `import { CodegraphPage } from '../codegraph/CodegraphPage'`。
2. 保留 `onOpenCodegraph={() => navigate('/codegraph')}` 原样。
3. 把现有 `/codegraph` route 替换为冻结调用：

```tsx
<Route
  path="/codegraph"
  element={<CodegraphFrame project={wb.base?.projectName ?? ''} />}
/>
```

4. 把 `fullPageRoute` 的完整表达式改为：

```tsx
const fullPageRoute = ['/cards', '/flows', '/settings', '/machines', '/codegraph']
  .some((path) => location.pathname.startsWith(path))
```

保留下面两个消费者的同一个 `fullPageRoute`，不要分别写第二套条件：

```tsx
{wb.base && !desktop && !fullPageRoute && <Breadcrumb base={wb.base} />}
```

```tsx
{wb.base && wb.base.kind === 'workspace' && !fullPageRoute && (
  <div className="w-[280px] shrink-0">
    <FileTree
      base={wb.base}
      refreshKey={fileTreeNonce}
      taskId={currentTaskId}
      onOpenFile={(rel) => wb.open({ kind: 'file', rel })}
      onOpenTerminal={(rel) => wb.openTerminal(undefined, undefined, rel)}
      revealSupported={caps.reveal('')}
    />
  </div>
)}
```

注释要解释两个边界：iframe 是唯一 viewer 来源，`fullPageRoute` 同时约束 Routes 外的 Breadcrumb/FileTree；不在 Shell 中加入 cookie/token 逻辑，不改 ProjectTree 入口。

删除 T2 文件清单中的 19 个旧副本文件，使 `web/src/app/codegraph/` 最终只含 `CodegraphFrame.tsx`。删除后不得留下从目录内到 `fetchCodegraph` / `fetchCodegraphSource` 的引用；目录外的 `web/src/api/client.ts` 两个 helper 保留不动。

#### T2.4 跑绿与 T2 最小范围验收

先跑：

```text
npm --prefix web test -- Shell.test.tsx
npm --prefix web run typecheck
npm --prefix web run build
```

期望三条退出码均为 0；Shell 测试必须具体证明：选中工作区后 iframe src 的三个特殊项目名均是 `/codegraph/app/?project=${encodeURIComponent(project)}`；`/codegraph` 下 Breadcrumb 和 FileTree 都不存在；回到 `/` 后二者都恢复。

再跑结构与负向检查：

```text
test "$(find web/src/app/codegraph -maxdepth 1 -type f | wc -l)" = 1
test -f web/src/app/codegraph/CodegraphFrame.tsx
! rg -n 'CodegraphPage|fetchCodegraph|fetchCodegraphSource' web/src/app/codegraph
! rg -n 'CodegraphPage' web/src/app/shell/Shell.tsx
git diff --exit-code -- web/src/api/client.ts
```

每条命令都必须实际退出码 0；最后一条证明删除旧副本没有顺手改既有 API wire helper。`! rg` 是 shell 负向断言，匹配到文本时整条命令应失败。

### T3 步骤：T1 之后做一次 v0.6.0 → v0.7.0 tidy

#### T3.1 版本输入复核

T1 绿后，在根与 desktop 分别运行：

```text
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go list -m -f '{{.Path}} {{.Version}}' github.com/Xsxdot/charter/graph
cd desktop && GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go list -m -f '{{.Path}} {{.Version}}' github.com/Xsxdot/charter/graph
```

此时根命令的期望是 `github.com/Xsxdot/charter/graph v0.7.0`；desktop 在 tidy 前可能仍解析旧 sum，不能把它写成通过。T1 已完成后才允许进入下一步。

#### T3.2 单步 tidy

只在 `desktop/` 执行：

```text
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go mod tidy
```

预期从 `desktop/go.sum` 删除 v0.6.0 两条记录并写入 v0.7.0 module/hash 两条记录；当前基线显示 `desktop/go.mod` 不直接 require charter，因此不要为“让 grep 命中”擅自添加 direct require。若工具实际改写 `desktop/go.mod`，只能接受与根模块依赖解析直接相关的 tidy 结果，并在范围审计中解释每一行。

#### T3.3 T3 验收

```text
cd desktop && GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go list -m -f '{{.Path}} {{.Version}}' github.com/Xsxdot/charter/graph
cd desktop && git diff --exit-code -- go.mod go.sum
rg -n 'github.com/Xsxdot/charter/graph v0\.[^7]' desktop/go.mod desktop/go.sum
rg -n 'github.com/Xsxdot/charter/graph v0\.7\.0' desktop/go.sum
git diff --exit-code -- .github/workflows/ci.yml
```

期望：desktop resolved 输出精确为 `v0.7.0`；tidy 后 manifest clean 命令退出 0；旧版本 `rg` 无输出且退出码 1；v0.7.0 `rg` 命中 module/hash 记录；CI 文件负向 diff 退出 0。不要把 `rg` 无旧版本的退出码 1 当成失败。

T3 无生产代码日志；这是一次性 module 工具流程，禁止用 shell `echo` 代替证据，保留 go 命令原始 stdout/stderr 到台账。T1/T2 的成功路径分别由 `s.log.Info` 和 iframe DOM/定向测试可观测，不新增 `print` 或浏览器 `console`。

## 3. 同轮收口与机内验收

### 3.1 最小定向验证矩阵

完成 T1/T2/T3 后，只跑触及包和文件的门：

```text
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go test ./internal/agentd -run '^TestCodegraphRoute$' -count=1
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go test ./internal/agentd -run '^TestCodegraph(Endpoint|ComparisonDataOptional|ComparisonDataTargetFailure|Source)$' -count=1
GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go build ./...
npm --prefix web test -- Shell.test.tsx
npm --prefix web run typecheck
npm --prefix web run build
cd desktop && GOSUMDB=off GOMODCACHE=/root/.handoff/tmp/af3a1b69/gomodcache go list -m -f '{{.Path}} {{.Version}}' github.com/Xsxdot/charter/graph
cd desktop && git diff --exit-code -- go.mod go.sum
```

所有 Go/前端命令退出码必须为 0；desktop list 必须输出 `github.com/Xsxdot/charter/graph v0.7.0`。全仓 Go test、全量 Vitest 和 GitHub CI 不属于本轮单个实现步骤；协调者在集成/真机阶段统一执行。

### 3.2 逐条原子检查

实现者在提交前逐条检查并把结果写台账：

1. `go.mod` direct require 精确是 `github.com/Xsxdot/charter/graph v0.7.0`，无针对它的 `replace`。
2. `internal/agentd/server.go` 使用 `charterwebui.FS()`，只挂 `/codegraph/app/`；handoff `webui.FS()` 仍只挂 `/`；专属 handler 在根 fallback 之前且在 `s.auth(mux)` 之内。
3. `TestCodegraphRoute` 经过完整 `httptest.Server`，逐条锁登录根 index、深路径同一 charter index、真实 hashed asset、POST 405、未登录 401 非 HTML、根 index 与 charter index 不同。
4. 两条 `/api/projects/{name}/codegraph[(/source)]` 的方法/路径和 `internal/agentd/codegraph.go`、`forward.go` 无 diff。
5. `CodegraphFrame` 导出 props 只有必填 `project: string`，src 精确为 `/codegraph/app/?project=${encodeURIComponent(project)}`，没有 fetch/cookie/token/Authorization/CORS/postMessage/项目选择器。
6. Shell route 传 `wb.base?.projectName ?? ''`；ProjectTree 的代码图入口未改；`fullPageRoute` 同时控制 Breadcrumb 与 FileTree；离开 `/codegraph` 后两者恢复。
7. 旧目录只剩 `CodegraphFrame.tsx`，旧副本引用消失，`web/src/api/client.ts` 无 diff。
8. desktop 根/嵌套模块 resolved 都是 v0.7.0，tidy 后 `desktop/go.mod`/`go.sum` clean，CI 文件无 diff；无独立 desktop 提交。
9. `codegraph/target.json`、contract/spec/breakdown 没有本轮修改。

### 3.3 同一提交

完成定向命令并确认只剩本计划、台账和 T1/T2/T3 允许文件后，执行一次：

```text
git add go.mod go.sum internal/agentd/server.go internal/agentd/webroute_test.go desktop/go.sum desktop/go.mod web/src/app/codegraph web/src/app/shell/Shell.tsx web/src/app/shell/Shell.test.tsx docs/superpowers/plans/c1.7-plan.md docs/superpowers/ledgers/2026-08-25-c1.7-contract-ledger.md
git diff --cached --check
git diff --cached --name-status
git commit -m "feat(codegraph): mount charter v0.7 viewer in handoff"
```

`desktop/go.mod` 只有在 T3 实际改写时才进入 staged；不因命令模板把未变文件伪造成改动。提交前 `git diff --cached --name-status` 只能出现 T1/T2/T3、计划和台账允许路径；不 push、不改分支、不改 git 配置。

## 4. 协调者真机验收（本 task 由协调者执行，不派发）

以下浏览器/换装项目不能用 Go mock、Vitest 或 iframe stub 抵账；当前计划节点不宣称已验证。必须严格按这个顺序：

### 4.1 先验宿主路径与鉴权

在无在飞派发任务的窗口，用已换装的 handoff/agentd 和登录浏览器依次确认：

1. 登录态 `GET /codegraph/app/` 返回 charter index，不是 handoff 根 index。
2. 登录态访问 `/codegraph/app/<深路径>` 回退到与第 1 步字节相同的 charter index；再取一个真实 hashed asset，确认命中并有 hash 缓存头。
3. 登录态 `POST /codegraph/app/` 得 405，不被 SPA 回退吞成 200。
4. 未登录 `GET /codegraph/app/` 得现有鉴权响应而不是 handoff/charter HTML。

### 4.2 再验 iframe 同源传输边界

确认第 4.1 节全通过后，再从控制台项目树进入代码图，观察浏览器 Network：

- iframe 静态资源和 `/api/projects/{name}/codegraph`、`/source` 都走同源 handoff origin，并自动带会话 cookie。
- Network 中没有 `Authorization` 头、没有 CORS 响应/预检、没有第二端口、没有第二 listener。
- 项目名为空、含空格、斜杠、中文时，iframe `?project=` 与 API 请求还原到同一项目；`?machine=` 仍只由宿主既有 API handler 处理。

### 4.3 最后验控制台布局与二期形态

确认前两步全通过后，再检查 `/codegraph` 页面：

- 无顶部 Breadcrumb、无右侧 FileTree；离开 `/codegraph` 回到工作台后二者恢复。
- 控制台 iframe 渲染的是 graph/v0.7.0 所带的**二期形态**：理想树全景、下钻、迁移清单、执法/欠账读数；详情面板、源码窗口、焦点历史、视图（分支 diff）选择器仍可用。
- 读数与 `codegraph check` CLI 同口径。
- 这里挂的是 `graph/v0.7.0`，它指向 charter master tip，**不含 C1.5 上下文装配器**；因此期望是二期形态而不是三期形态。
- 在桌面 Wails/webview、Windows/macOS/Linux 真实环境下无白屏、无布局挤压；换装 handoff 并重启 agentd 后重复第 4.1—4.3 节，确认无孤儿进程、临时目录或半挂载路由。

## 5. 缺陷族对抗审查与验收结论

依据 `docs/superpowers/specs/2026-08-21-handoff-instantiation-checklist.md §3`，本计划逐族给出锁法；实现后的命令/真机原始结果必须回填台账，计划不把“未验证”写成 pass。

| 缺陷族 | T1/T2/T3 设问与结论 |
|---|---|
| 生命周期/状态机中断 | 静态 GET/HEAD 和 iframe 不创建 task、ticket、executor 或临时目录；agentd 重启后必须重新装配 mux，换装期间无在飞任务、无孤儿进程属于 §4 真机项。T3 是一次性 tidy，无运行资源。 |
| 静默失败/误导报错 | route 测试不仅断言 200，还区分 charter/root index、深路径同一 index、真实 asset、405、未登录非 HTML；薄壳只产生 iframe URL，不显示“请求成功”假状态；桌面换装后的错误文案留真机。 |
| 跨平台假设 | URL 路径只由 `StripPrefix` + `fs.FS` 处理，不拼 OS 文件分隔符；`encodeURIComponent` 只编码 query。Linux Go/TypeScript 绿不能推出 Wails、Windows、macOS 行为，逐平台项留在 §4.3。 |
| 假红/假绿测试 | T1 测完整 `httptest.Server`，不直测 handler；T2 测 `AppRoutes → Shell → ProjectTree → CodegraphFrame`，不测被删除旧组件内部纯函数；T2 同时断言 Breadcrumb 和 FileTree，避免只修中央 route。测试新增断言必须先在旧实现预期红，再以最小实现变绿。 |
| 门禁绕过 | charter handler 必须在 `s.auth(mux)` 内，未登录 route-level 测试必须能红；不加 replace/CORS/第二端口/token 旁路；CI、Windows、install.sh 文本不改，desktop tidy clean 后才允许下游门继续。 |
| 序列化边界 | 本轮没有 JSON/DTO/wire 字段新增；唯一新手写投影是 `BaseDir.projectName → encodeURIComponent(project) → iframe src`，由 T2 的空格/斜杠/中文三例穿过真实 Shell route 断言。charter viewer 自身 query→API path 的编码是 v0.7.0 依赖边界，由 §4.2 真机验证，不用 handoff mock 冒充。 |
| 新枚举值/白名单 | 没有新状态、事件类型、kind 或枚举值；`/codegraph` 是既有 pathname 白名单扩展，必须同时命中 fullPageRoute、Breadcrumb、FileTree 三个消费者。无其它白名单登记项。 |
| webview/平台表现差异 | iframe 同源 cookie、浏览器 Network、Wails/webview 尺寸和桌面重启都不能由 jsdom 推断，严格归 §4.2—4.3 的协调者真机清单。 |
| 承重安全属性 | 未登录不得读取 charter 静态资源；route-level 未登录断言与真机未登录访问双锁。薄壳不保存或转发凭据，不产生新的 token/租约/共享持久资源，因此不新增 token 唯一性测试。 |

## 6. 接缝覆盖、序列化边界与自审

### 6.1 声明缝 ↔ 测试双向矩阵

| 声明缝 | 生产入口/调用链 | 锁它的测试或验收 | 入口是否穿缝 |
|---|---|---|---|
| 1. `Server.Handler` mux 装配、`newSPAHandler`、`s.auth` | `TestCodegraphRoute` → `consoleTestEnv` → `httptest.Server` → `Server.Handler()` → auth → mux → `StripPrefix` → charter `newSPAHandler` | `TestCodegraphRoute` 逐条断言 index 来源、深回退、asset、POST、未登录；根 index 反向比较 | 是，入口是完整 HTTP 路由栈，不是内部 helper |
| 2. `CodegraphFrame` → Shell `/codegraph` route | `Shell.test.tsx` → `renderShell` → `AppRoutes` → `Shell` → ProjectTree 代码图按钮 → `/codegraph` Route → `CodegraphFrame` | 三个特殊项目名的 iframe src 断言、`CodegraphFrame` 结构 grep、typecheck/build | 是，测试入口穿 `AppRoutes` 和 Shell route |
| 3. `fullPageRoute` → Breadcrumb/FileTree 两个消费者 | 同一 Shell 渲染中，`/codegraph` 改变 `fullPageRoute`，分别影响 `Shell.tsx:478` 和 `:567` | `/codegraph` 同时无 Breadcrumb/FileTree；点击目录回 `/` 后两者同时恢复 | 是，测试入口是 Shell 的声明路由和两个实际 DOM 消费者 |

每条测试都从上表某个声明入口进入；没有仅测内部纯函数的附加锁。每条声明缝都至少有一支缝级断言，不用内部测试替代。T2 不创建 `CodegraphFrame.test.tsx` 的理由是：从该独立符号构造不出“Shell 传入当前 BaseDir 项目名、fullPageRoute 同时卸载两处外壳”的断言；Shell 测试已覆盖完整调用链，单测会成为不能证明接缝的内部锁。

### 6.2 手写序列化/投影清单

1. `web/src/app/shell/Shell.tsx`：从 `wb.base?.projectName ?? ''` 传入 `CodegraphFrame`，由 Shell 测试的三个项目名断言。
2. `web/src/app/codegraph/CodegraphFrame.tsx`：对 `project` 调用 `encodeURIComponent` 并拼入 iframe query，由同一支 Shell 测试断言完整 src。
3. `web/src/api/client.ts`、`internal/agentd/codegraph.go`、`internal/agentd/forward.go`：本轮不改，不新增字段；v0.7.0 viewer 的 query/API 投影只在真实浏览器 §4.2 验证。

没有新增 JSON/DTO/CLI 字段，所以不添加伪 roundtrip 测试；“字段缺失”和“值为零”的区分不适用于本轮新增面。

### 6.3 上下文预算、类型与负向范围

- T1 只圈 4 个文件，T2 只圈 3 个新增/修改文件 + 19 个机械删除文件，T3 只圈 1 个预期变化文件；均为有界文件集。
- 边界型 T1/T3 的验收都有精确 HTTP/module 真机清单；逻辑型 T2 的验收有精确 DOM/src/结构清单。
- 不修改契约文档；不修改 `ledger.Store` 目标图条目；不把前端旧副本目录外的 API helper 当删除对象。

## 7. 规格故事归属与占位符扫描

### 7.1 用户故事归属

- 故事 1（代码图显示二期理想树/欠账读数）：T1 提供 v0.7.0 viewer 静态资源，§4.3 协调者真机验证二期形态和 CLI 同口径。
- 故事 2（代码图不被右栏/面包屑挤压）：T2 的 `fullPageRoute` 与 Shell 双消费者测试，§4.3 再做浏览器复验。
- 故事 3（只有 charter 一份 viewer 源码）：T2 删除 19 个旧副本并做目录/引用/`client.ts` 负向检查。
- 故事 4（同源 cookie、无 Authorization/CORS/第二端口）：T1 auth 内挂载 + T2 iframe src，按 §4.1 → §4.2 顺序真机验证。
- 故事 5（`?machine=` 转发语义不变）：T1 保持 API/forward 文件零 diff，§4.2 真实浏览器请求验证既有宿主转发。

### 7.2 计划自检

落盘后运行：

```text
git diff --check
占位符扫描命令（针对本计划文件）
```

第一条必须退出 0 且无输出；第二条必须无匹配。另运行 `git diff --name-only`，确认当前实现节点只包含本计划、台账和 T1/T2/T3 允许文件；本轮不改 spec、contract、breakdown、target 或 API helper。

本节点产出是计划文档，不在此节点实现 Go/TS、不创建脚手架、不伪造实现测试绿；实现完成标准是后续单轮 implement 按本计划拿到真实编译/测试/真机结果并以同一提交收口。
