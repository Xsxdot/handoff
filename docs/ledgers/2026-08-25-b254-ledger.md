# B254 计划节点执行台账

- 2026-08-25：当前工作树为 `/root/.handoff/worktrees/e9d42fef`，当前分支为 `cards/B254-charter`；初始 `git status --short` 无输出，未发现用户已有改动。
- 2026-08-25：初始 HEAD 为 `deb3d461 docs(spec): B252/B253/B254 三卡 spec 定稿`；有效基线分支按任务注入为 `claude/archive-247-close-252-254-4fe606`，本轮未切分支。
- 2026-08-25：已读取 `docs/superpowers/specs/2026-08-25-b254-workbranch-skip-empty-rounds.md`；冻结方案为 `Store.WorkBranch` 跳过已终态且无本轮工作分支提交的非审阅派发轮；审阅跳过、正常轮覆盖取最后一条、`card dispatch --base` 与 origin 自动放行均为 out of scope。
- 2026-08-25：项目存在 `codegraph/`，且 `codegraph/best.json` 含最优领域 `d_ledger`；按纪律执行 `go run . graph context d_ledger`，实际退出码 0，输出确认 `view=baseline`、`domain.id=d_ledger`、包 `internal/ledger`/`internal/ledgermirror`/`internal/ledgerstep`、`truncated=false`、`fociTruncated` 未出现。
- 2026-08-25：对 `internal/ledger/events.go#Store.WorkBranch` 执行 `go run . graph chain ... --with-source`、`go run . graph sym ...`、`go run . graph who-calls ...`，三个命令均实际失败；原始错误分别为 `Error: 节点 "internal/ledger/events.go#Store.WorkBranch" 不在图中；近似候选: `、`Error: 符号 "internal/ledger/events.go#Store.WorkBranch" 不在图中（图未覆盖或名字有误）；近似候选: []`、`Error: 节点 "internal/ledger/events.go#Store.WorkBranch" 不在图中；近似候选: `。本计划记录该符号图覆盖债，现状签名改用源码 grep/阅读核对。
- 2026-08-25：源码核对事实：`internal/ledger/events.go:405` 的现状签名为 `func (s *Store) WorkBranch(cardID string) (WorkBranchInfo, error)`；当前按 `EventsFromAsc(..., 10000)` 升序读取卡事件、`TasksOf` 建 task→purpose 回落表，跳过 review 后对非空 `snapshot.Branch` 覆盖赋值，未读 task 终态或产出。
- 2026-08-25：源码核对事实：`internal/ledger/events.go:115-131` 的 `DispatchSnapshot` 含 `Target`、`TaskID`、`Branch`、`Purpose`；`internal/ledger/tasks.go:13-20` 的 `TaskLink` 含 `Target`、`TaskID`、`Purpose`；`internal/ledger/mirror.go:14-21` 的 `MirroredEvent` 把远端 task 的原始 `Type`/`Payload`/`SourceSeq` 写入协调者账本。
- 2026-08-25：源码核对事实：`internal/ledger/taskstate.go:138-145` 已将镜像 `archived`/`failed` 视为收口、`completed`/`turn_failed` 仍视为 waiting_review；`internal/ledger/taskstate.go:65-76` 的 `mirroredTaskPayload` 形态为 `task_type` + 原始 `payload`，可复用于 `WorkBranch` 的账本内回放。
- 2026-08-25：依赖侧源码核对事实：`internal/agentd/manager.go:569-578` 的失败 payload 为 `branch,omitempty` 与 `commit,omitempty`；`internal/agentd/manager.go:3184-3210` 的 completed/turn_failed 结果 payload 均带 `branch` 与 `commit` 字段；`internal/proto/proto.go:31-35` 的 task 终态是 `completed`/`failed`，但协调者镜像中真正收口事件为 `archived`/`failed`，不能把 `completed` 或 `turn_failed` 当终态。
- 2026-08-25：基线判据命令 `go test ./internal/ledger ./internal/ledgerstep -run 'TestWorkBranch|TestViaTemplateSecondRoundGetsNumberedBranch|TestViaTemplateContinuationUsesLocalWorkBranch|TestViaTemplateRejectsCrossTargetBeforeTransport|TestViaTemplateNodePurposeTakesReviewPath' -count=1 -v` 实际退出码 0；原始结果末尾为 `--- PASS: TestWorkBranchSkipsReviewRounds`、`--- PASS: TestViaTemplateSecondRoundGetsNumberedBranch`、`--- PASS: TestViaTemplateContinuationUsesLocalWorkBranch`、`--- PASS: TestViaTemplateRejectsCrossTargetBeforeTransport`、`--- PASS: TestViaTemplateNodePurposeTakesReviewPath`、`ok github.com/Xsxdot/handoff/internal/ledger 0.142s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 0.326s`。
- 2026-08-25：基线测试的真实日志还确认 `ViaTemplate` 首轮派发到 `mac-02`，第二轮同机使用 `cards/D1-implement` 本地工作分支并生成 `cards/D1-implement-2`；跨机测试真实打印 `WARN 工作分支跨目标机，拒绝接续`。这些是实现节点必须保留的回归行为。
- 2026-08-25：计划层判断：零产出资格只在账本内判定。对同一非审阅 dispatched 快照，按其 `TaskID` 回放 `EvTaskMirrored` 的 `task_type` 与原始 payload：最新 task_type 必须为 `failed` 或 `archived`；且至少一条该 task payload 的 `branch` 与快照 `Branch` 精确相等、非空 `commit` 非空，才证明该轮产出并保留指针。无镜像、非终态、缺 task_id、缺 branch/commit、分支不相等均 fail-closed 保留，不访问执行机 git。
- 2026-08-26：已按法定路径创建 `docs/superpowers/plans/b254-plan.md`；计划覆盖 Task 1 账本回放、Task 2 `ViaTemplate` 调用方与文案、Task 3 协调者门禁，包含精确文件范围、Consumes/Produces 签名、完整测试/实现代码块、基线/红绿/日志/注释步骤、序列化边界、缺陷族、接缝双向覆盖、用户故事归属、变异表和提交命令。
- 2026-08-26：计划自检命令实际输出 `758 2652 docs/superpowers/plans/b254-plan.md`、`14 234 docs/ledgers/2026-08-25-b254-ledger.md`；围栏扫描输出 `fence_count=30 balanced=1`；占位符扫描退出码 1 且无输出；末尾空白扫描退出码 1 且无输出；`git diff --check` 退出码 0 且无输出。计划与台账当时仍为未跟踪文件。
- 2026-08-26：计划复核修正：Task 2 明确替换既有 `TestViaTemplateRejectsCrossTargetBeforeTransport`，避免旧的 `git push`/`--base` 正向断言与新文案冲突；跨机错误代码块对历史 `Target==""` 单独给出“创建该分支的原执行机”文案，避免把空目标机伪装成可行动地址。
- 2026-08-26：最终文档门禁实际输出 `fence_count=30 balanced=1`；占位符扫描和末尾空白扫描均退出码 1 且无输出（表示无命中）；暂存 `git diff --cached --check` 退出码 0 且无输出；`git diff --cached --name-only` 仅列出 `docs/ledgers/2026-08-25-b254-ledger.md` 与 `docs/superpowers/plans/b254-plan.md`，未写实现代码。
- 2026-08-26：执行 `git add docs/superpowers/plans/b254-plan.md docs/ledgers/2026-08-25-b254-ledger.md && git diff --cached --check && git commit -m "docs(b254): add implementation plan"` 实际退出码 0；原始提交输出为 `[cards/B254-charter a87c4dfd] docs(b254): add implementation plan`、`2 files changed, 783 insertions(+)`，未 push。
- 2026-08-26：最终复核实际输出：`git status --short --branch` 为 `## cards/B254-charter`（无未提交改动）；`git log -2 --oneline` 为 `de9fbde7 docs(b254): record plan validation`、`a87c4dfd docs(b254): add implementation plan`；`git rev-parse --short HEAD` 为 `de9fbde7`；`git diff --check` 无输出；法定计划与台账均 `required_files=present`。
- 2026-08-26：提交 `e37d1839 docs(b254): finalize plan ledger` 实际成功；其后复核输出为 `## cards/B254-charter`、`e37d1839 docs(b254): finalize plan ledger`、`de9fbde7 docs(b254): record plan validation`、`a87c4dfd docs(b254): add implementation plan`、`e37d1839`、`fence_count=30 balanced=1`，占位符扫描无输出，未 push。
- 2026-08-26：执行计划 §0.1 基线命令 `go test ./internal/ledger ./internal/ledgerstep -run 'TestWorkBranch|TestViaTemplateSecondRoundGetsNumberedBranch|TestViaTemplateContinuationUsesLocalWorkBranch|TestViaTemplateRejectsCrossTargetBeforeTransport|TestViaTemplateNodePurposeTakesReviewPath' -count=1 -v` 实际退出码 0；原始输出含 `--- PASS: TestWorkBranchSkipsReviewRounds`、四个既有 `ViaTemplate` 用例 `PASS`、`ok github.com/Xsxdot/handoff/internal/ledger 0.282s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 0.588s`。
- 2026-08-26：先新增 Task 1 接缝测试与真实 `AppendMirroredEvent` 夹具，执行 `go test ./internal/ledger -run 'TestWorkBranch(SkipsZeroOutputTerminalRounds|KeepsProducedFailedRound|KeepsProducedArchivedRound|SkipsZeroOutputAndUsesPreviousProducedRound|KeepsLiveTurnFailedRound)$' -count=1 -v` 实际退出码 1；原始输出为零产出四个子用例断言 `零产出终态轮应被跳过并返回 ErrNotFound，实得 <nil>`，跳过后回退用例断言 `实得 {Branch:cards/P1-implement-2 Target:linux-01}`，有产出失败/归档与 `turn_failed` 用例 `PASS`；确认旧 `WorkBranch` 行为被接缝测试拦住后才进入实现。
- 2026-08-26：实现账本回放后执行 `gofmt -w internal/ledger/events.go internal/ledger/events_test.go && go test ./internal/ledger -run 'TestWorkBranch(SkipsZeroOutputTerminalRounds|KeepsProducedFailedRound|KeepsProducedArchivedRound|SkipsZeroOutputAndUsesPreviousProducedRound|KeepsLiveTurnFailedRound|SkipsReviewRounds)$' -count=1 -v` 实际退出码 0；原始输出五组新增行为与 `TestWorkBranchSkipsReviewRounds` 均 `PASS`，包结果 `ok github.com/Xsxdot/handoff/internal/ledger 1.181s`，日志真实出现零产出跳过、上一条有产出分支选择与无分支 `ErrNotFound`。
- 2026-08-26：Task 1 收尾命令 `go build ./... && go test ./internal/ledger -count=1 && git diff --check && git diff --name-only` 实际退出码 0；原始输出为 `ok github.com/Xsxdot/handoff/internal/ledger 13.785s`、改动文件 `docs/ledgers/2026-08-25-b254-ledger.md`、`internal/ledger/events.go`、`internal/ledger/events_test.go`，`go build ./...`、`git diff --check` 无输出。
- 2026-08-26：Task 2 新测试首次格式化/运行未形成行为红，命令 `gofmt -w internal/ledgerstep/dispatch_test.go && go test ./internal/ledgerstep -run 'TestViaTemplate(AllowsCrossTargetAfterZeroOutputFailure|RejectsCrossTargetBeforeTransport)$' -count=1 -v` 实际退出码 2；原始错误：`internal/ledgerstep/dispatch_test.go:459:42: missing ',' before newline in composite literal`、`internal/ledgerstep/dispatch_test.go:471:50: missing ',' before newline in composite literal`、`internal/ledgerstep/dispatch_test.go:472:4: expected ';', found 'else'`、`internal/ledgerstep/dispatch_test.go:485:2: expected declaration, found 'if'`、`internal/ledgerstep/dispatch_test.go:499:2: expected declaration, found d`、`internal/ledgerstep/dispatch_test.go:548:2: expected declaration, found d`、`internal/ledgerstep/dispatch_test.go:569:2: expected declaration, found 'for'`、`internal/ledgerstep/dispatch_test.go:588:4: expected declaration, found d`；先修正测试语法，再重新验证功能缺失红。
- 2026-08-26：修正测试语法后重跑 `gofmt -w internal/ledgerstep/dispatch_test.go && go test ./internal/ledgerstep -run 'TestViaTemplate(AllowsCrossTargetAfterZeroOutputFailure|RejectsCrossTargetBeforeTransport)$' -count=1 -v` 实际退出码 1；原始输出中 `TestViaTemplateAllowsCrossTargetAfterZeroOutputFailure` 为 `PASS`，`TestViaTemplateRejectsCrossTargetBeforeTransport` 失败断言：`跨机拒绝缺少真实可走路径 "工作分支归属于另一台执行机"`，实得 `工作分支只存在于创建它的那台机器：上次目标机 "mac-02"，本次目标机 "linux-01"；请先 push 到 origin（git push origin cards/D1-implement），再用显式 --base 指定`；确认旧文案接缝红后进入 Task 2 文案实现。
- 2026-08-26：实现 Task 2 文案后执行 `gofmt -w internal/ledgerstep/dispatch.go internal/ledgerstep/dispatch_test.go && go test ./internal/ledgerstep -run 'TestViaTemplate(AllowsCrossTargetAfterZeroOutputFailure|RejectsCrossTargetBeforeTransport|SecondRoundGetsNumberedBranch|ContinuationUsesLocalWorkBranch|NodePurposeTakesReviewPath)$' -count=1 -v` 实际退出码 0；原始输出五个入口均 `PASS`，包结果 `ok github.com/Xsxdot/handoff/internal/ledgerstep 0.517s`，并真实打印零产出后 `linux-01` 从空基线派发与有产出跨机拒绝。
- 2026-08-26：Task 2 收尾并补跑计划基线：并行执行基线命令、`go build ./...`、`go test ./internal/ledgerstep -count=1`、`git diff --check && git diff --name-only` 均实际退出码 0；原始输出含 `ok github.com/Xsxdot/handoff/internal/ledger 2.412s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 0.958s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 10.256s`，改动文件为台账及四个计划实现/测试文件，`go build ./...` 与 `git diff --check` 无输出。
- 2026-08-26：最终定向门禁首次并行执行：`go test ./internal/ledger ./internal/ledgerstep -count=1` 实际退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/ledger 14.978s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 8.933s`；`go vet ./internal/ledger ./internal/ledgerstep`、四文件 `gofmt -l`、`git diff --check` 均实际退出码 0 且无输出。race 进程首轮超过工具等待窗，未据此下结论。
- 2026-08-26：单独重跑 `go test ./internal/ledger ./internal/ledgerstep -race -count=1` 实际退出码 0；原始输出 `ok github.com/Xsxdot/handoff/internal/ledger 20.220s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 10.087s`。
- 2026-08-26：变异 1（终态无提交时将 `keepsWorkBranchSnapshot` 的 `return false` 改为 `return true`）先执行 `go build ./...`，实际退出码 0；行为断言 `go test ./internal/ledger -run 'TestWorkBranch(SkipsZeroOutputTerminalRounds|SkipsZeroOutputAndUsesPreviousProducedRound)$' -count=1 -v` 实际退出码 1，原始输出四个零产出子用例均 `零产出终态轮应被跳过并返回 ErrNotFound，实得 <nil>`，回退用例 `实得 {Branch:cards/P1-implement-2 Target:linux-01}`；随后全量触及包 `go test ./internal/ledger ./internal/ledgerstep -count=1 -v 2>&1 | grep -c '^--- FAIL'` 输出 `3`。已用 apply_patch 恢复。
- 2026-08-26：变异 2（将 terminal 集合删去 `archived`）先执行 `go build ./...`，实际退出码 0；行为断言 `go test ./internal/ledger -run 'TestWorkBranch(SkipsZeroOutputTerminalRounds|KeepsProducedArchivedRound)$' -count=1 -v` 实际退出码 1，原始输出 `archived_without_prior_result` 失败而其余零产出/有产出归档断言 `PASS`；全量失败计数命令 `go test ./internal/ledger ./internal/ledgerstep -count=1 -v 2>&1 | grep -c '^--- FAIL'` 输出 `1`。已用 apply_patch 恢复。
- 2026-08-26：变异 3（只按 commit 非空而不比较 payload branch，用 `len(state.producedBranches)>0` 代替精确分支匹配）先执行 `go build ./...`，实际退出码 0；行为断言 `go test ./internal/ledger -run 'TestWorkBranchSkipsZeroOutputTerminalRounds/failed_commit_on_another_branch$' -count=1 -v` 实际退出码 1，原始输出 `零产出终态轮应被跳过并返回 ErrNotFound，实得 <nil>`；全量失败计数输出 `1`。已用 apply_patch 恢复。
- 2026-08-26：变异 4（把 `turn_failed` 加入 terminal 集合）先执行 `go build ./...`，实际退出码 0；行为断言 `go test ./internal/ledger -run 'TestWorkBranchKeepsLiveTurnFailedRound$' -count=1 -v` 实际退出码 1，原始输出 `WorkBranch: 卡 P1 没有有资格的非审阅 dispatched 快照（尚未派出实现轮，或最近轮零产出）: ledger: 记录不存在`；全量失败计数输出 `1`。已用 apply_patch 恢复。
- 2026-08-26：变异 5（把无镜像/坏镜像的 fail-closed `return true` 改为跳过 `return false`）先执行 `go build ./...`，实际退出码 0；行为断言 `go test ./internal/ledgerstep -run 'TestViaTemplateSecondRoundGetsNumberedBranch$' -count=1 -v` 实际退出码 1，原始输出 `WorkBranch: 卡 D1 没有有资格的非审阅 dispatched 快照（尚未派出实现轮，或最近轮零产出）: ledger: 记录不存在`；全量失败计数输出 `5`。已用 apply_patch 恢复。
- 2026-08-26：变异 6（恢复旧跨机错误文案）先执行 `go build ./...`，实际退出码 0；行为断言 `go test ./internal/ledgerstep -run 'TestViaTemplateRejectsCrossTargetBeforeTransport$' -count=1 -v` 实际退出码 1，原始输出缺少 `工作分支归属于另一台执行机`，实得旧文案 `工作分支只存在于创建它的那台机器：上次目标机 "mac-02"，本次目标机 "linux-01"；请先 push 到 origin（git push origin cards/D1-implement），再用显式 --base 指定`；重新执行全量失败计数命令实际输出 `1`。已用 apply_patch 恢复。
- 2026-08-26：变异 7（把跨机判断从 Transport 前移到 Transport 后）先执行 `gofmt -w internal/ledgerstep/dispatch.go && go build ./...`，实际退出码 0；行为断言 `go test ./internal/ledgerstep -run 'TestViaTemplateRejectsCrossTargetBeforeTransport$' -count=1 -v` 实际退出码 1，原始输出真实调用第二次派发后断言 `跨机拒绝不得调用 Transport，调用次数=2`；全量失败计数输出 `1`。已用 apply_patch 恢复。
- 2026-08-26：所有临时变异恢复后最终门禁实际通过：`go test ./internal/ledger ./internal/ledgerstep -count=1` 输出 `ok github.com/Xsxdot/handoff/internal/ledger 24.629s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 13.390s`，退出码 0；`go test ./internal/ledger ./internal/ledgerstep -race -count=1` 退出码 0，原始输出 `ok github.com/Xsxdot/handoff/internal/ledger 37.671s`、`ok github.com/Xsxdot/handoff/internal/ledgerstep 21.819s`；`go build ./...`、`go vet ./internal/ledger ./internal/ledgerstep`、四文件 `gofmt -l`、`git diff --check` 均退出码 0 且无输出。
- 2026-08-26：执行 `git add docs/superpowers/plans/b254-plan.md docs/ledgers/2026-08-25-b254-ledger.md internal/ledger/events.go internal/ledger/events_test.go internal/ledgerstep/dispatch.go internal/ledgerstep/dispatch_test.go && git diff --cached --check && git diff --cached --name-only && git commit -m "fix(ledger): skip zero-output work branches"` 实际退出码 0；原始提交输出 `[cards/B254-charter-2 2a4ea4fd] fix(ledger): skip zero-output work branches`、`5 files changed, 322 insertions(+), 23 deletions(-)`，未 push。
