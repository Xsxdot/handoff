# B249 执行台账

- 2026-08-25：当前目录为 `/root/.handoff/worktrees/a1b0469b`；`git status --short --branch` 原始输出为 `## cards/B249-charter-3`，工作区初始无未提交改动。
- 2026-08-25：初始 HEAD 为 `f2ee6b9d chore(ledger): record B249 breakdown close`。
- 2026-08-25：按补充要求执行 `git fetch origin`；原始失败输出：`error: cannot open '/root/.handoff/repos/handoff/.git/worktrees/a1b0469b/FETCH_HEAD': Read-only file system`。
- 2026-08-25：B248 提供的短对象 `a95178954` 不在本地对象库；`git cat-file -t a95178954` 原始输出：`fatal: Not a valid object name a95178954`；`git show ... a95178954` 原始输出：`fatal: ambiguous argument 'a95178954': unknown revision or path not in the working tree.`；祖先检查原始输出：`fatal: Not a valid object name a95178954` 后 `B248 is not ancestor of HEAD`。B248 是否已在当前基线中未验证。
- 2026-08-25：`command -v codegraph` 无输出，当前 PATH 未找到 codegraph；现状签名核验改用 grep，并记录覆盖债。
- 2026-08-25：基线命令 `go test ./internal/executor/... -count=1` 通过，原始输出为 executor、claudecode、codex、fake、grok、opencode、rawtap、turn 各包 `ok`；基线命令 `go test ./internal/permgate -count=1` 通过，原始输出为 `ok github.com/Xsxdot/handoff/internal/permgate 0.006s`。
- 2026-08-25：组合基线首次执行 `go build ./...; go vet ...; go test ...; git diff --check` 在工具 30 秒等待窗口只返回 `ok github.com/Xsxdot/handoff/internal/permgate 0.006s`，未据此下结论，拆分重跑。
- 2026-08-25：基线 `go build ./...` 实际退出码 0，原始输出为空。
- 2026-08-25：基线 `go vet ./internal/permgate ./internal/agentd ./internal/client ./internal/proto ./internal/store ./internal/ledgermirror ./internal/executor/...` 实际退出码 0，原始输出为空。
- 2026-08-25：基线组合测试 `go test ./internal/agentd ./internal/client ./internal/proto ./internal/store ./internal/ledgermirror ./internal/executor/... -count=1` 实际退出码 0；原始输出：`ok github.com/Xsxdot/handoff/internal/agentd 159.754s`、`ok github.com/Xsxdot/handoff/internal/client 29.015s`、`ok github.com/Xsxdot/handoff/internal/proto 0.007s`、`ok github.com/Xsxdot/handoff/internal/store 24.717s`、`ok github.com/Xsxdot/handoff/internal/ledgermirror 21.998s`、`ok github.com/Xsxdot/handoff/internal/executor 0.002s`、`ok github.com/Xsxdot/handoff/internal/executor/claudecode 15.499s`、`ok github.com/Xsxdot/handoff/internal/executor/codex 16.172s`、`ok github.com/Xsxdot/handoff/internal/executor/fake 0.006s`、`ok github.com/Xsxdot/handoff/internal/executor/grok 18.050s`、`ok github.com/Xsxdot/handoff/internal/executor/opencode 32.106s`、`ok github.com/Xsxdot/handoff/internal/executor/rawtap 0.004s`、`ok github.com/Xsxdot/handoff/internal/executor/turn 0.842s`。
- 2026-08-25：基线 `git diff --check` 实际退出码 0，原始输出为空。
- 2026-08-25：契约核对事实：`executor.TaskTmpDir(dataDir, taskID string) string` 位于 `internal/executor/tempdir.go:11-16`；当前金样本在 `internal/executor/tempdir_test.go:9-41` 锁定 UUID、短 ID、空 ID 的 `filepath.Join` 语义。
- 2026-08-25：契约核对事实：`permgate.Scope` 当前只有 `Workdir`/`TaskDir`（`internal/permgate/permgate.go:66-73`），`InScope` 当前仅遍历两根（`internal/permgate/path.go:39-68`）；`Manager.judgePermission` 当前仅组装两根（`internal/agentd/manager.go:1845-1848`）。
- 2026-08-25：契约核对事实：`Gate.judgeBash` 当前在 `internal/permgate/permgate.go:216-252` 合并 `req.Paths`、`RedirectTargets`、`WriteArgTargets`，逐个范围判定后调用 `judgeCommand`；`judgeCommand` 当前在 `internal/permgate/blacklist.go:133-165` 且最终只返回 `Consult`/`Escalate`。
- 2026-08-25：契约核对事实：`WriteArgTargets` 当前写命令表在 `internal/permgate/writeargs.go:35-47`，未包含 `gofmt`、`git add`、`git diff --output`；`selfCmdReadOnly` 当前在 `internal/permgate/selfcmd.go:23-31`，未包含 `graph`。
- 2026-08-25：契约核对事实：`Manager.handlePermission` 当前在 `internal/agentd/manager.go:1743-1749` 丢弃 Verdict 后调用旧签名 `autoAllowPermission(taskID, ev)`；`autoAllowPermission` 当前在 `internal/agentd/manager.go:1889-1913` 只回传 `once` 并累计，不写审计事件。
- 2026-08-25：契约核对事实：`proto.EventType` 当前常量区在 `internal/proto/proto.go:38-124` 无 `permission_auto_allow`；`client.isDeliverable` 当前在 `internal/client/client.go:123-149` 不可交付集合无该类型。
- 2026-08-25：契约核对事实：claude Start 将 `req.Env` 原样传给 `StartProc`（`internal/executor/claudecode/adapter.go:249-260`），grok Start 将 `req.Env` 传给 `startServe`（`internal/executor/grok/adapter.go:192-195`），opencode Start 将 `req.Env` 传给 `startServe`（`internal/executor/opencode/adapter.go:355-357`）；Codex 已在 Start/Cold Resume 追加 `tmpEnvKVs`（`internal/executor/codex/adapter.go:269-275`、`internal/executor/codex/resume.go:112-114`）。
- 2026-08-25：依赖源码核对：`/usr/local/go/src/path/filepath/path.go:123-131` 规定 `filepath.Join` 忽略空元素并 Clean；`:156-163` 规定 `filepath.Abs` 对相对路径接当前工作目录并 Clean；`:176-206` 规定 `filepath.Rel` 对无法相对化/跨卷返回错误。
- 2026-08-25：当前台账写入后的 `git status --short --branch` 原始输出为 `## cards/B249-charter-3` 与 `?? docs/ledgers/2026-08-25-b249-ledger.md`；`git diff --stat` 无输出（未跟踪文件不计入 diff stat）。
- 2026-08-25：B248 前置实查（在已失败 `git fetch origin` 后只读检查）：当前 HEAD 与缓存远程跟踪分支 `refs/remotes/origin/claude/codex-sandbox-handoff-permissions-558d49` 的 `execWrapperRx` 均含 `\s-c\b`、未含 `\s-[a-z]*c[a-z]*\b`；缓存远程最新提交为 `dd445a56 docs(b248,b249): 权限判据两卡 spec 定稿`。因 fetch 失败且目标 `a95178954` 不在本地，此项 B248 是否已合入仍未验证。
- 2026-08-25：命令 `rg -n "type StartReq|type ResumeReq|func \\(.*\\) Start\\(|func \\(.*\\) Resume\\(|type Adapter interface|type EventType|type Manager struct|type Scope struct|func \\(g \\*Gate\\) Judge|func \\(m \\*Manager\\) autoAllowPermission" internal docs/superpowers/specs` 实跑退出 0；输出确认契约列出的 `Start`/`Resume`/`Gate.Judge`/`Manager.autoAllowPermission` 现状签名与文件位置，并确认 `b249-contract.md` 是当前契约来源。
- 2026-08-25：命令 `sed -n '1,240p' docs/superpowers/specs/b249-contract.md && sed -n '1,280p' docs/superpowers/specs/2026-08-25-b249-breakdown.md && sed -n '1,280p' docs/superpowers/specs/2026-08-25-b249-permission-noise-reduction.md` 退出 0，但工具输出被截断（原始输出标记 `Total output lines: 526`、`truncated`）；已读取到契约中的精确签名、顺序、事件与测试冻结条目，未把截断部分当作额外事实。
- 2026-08-25：命令 `sed -n '53,92p' internal/executor/executor.go; sed -n '1,60p' internal/executor/resume.go; sed -n '180,240p' internal/executor/executor.go; sed -n '1,90p' internal/executor/claudecode/resume.go; sed -n '1,90p' internal/executor/grok/resume.go; sed -n '1,90p' internal/executor/opencode/resume.go` 退出 0；输出确认 `StartReq.TaskDir/Env`、`ResumeReq.TaskID/TaskDir/Env/Cold` 和适配器 `Start`/`Resume` 的真实边界，且输出被工具截断但所需字段均在已见片段。
- 2026-08-25：命令 `rg -n "type PermRequest|type Request struct|type Verdict|RuleSafe|permEventText|PermissionID|Permission string|Tool string" internal/executor internal/permgate internal/agentd | head -100; sed -n '1,120p' internal/permgate/permgate.go; sed -n '1,130p' internal/agentd/manager.go` 退出 0；输出确认 `executor.PermRequest`/`permgate.Request`/`permgate.Verdict` 以及 Manager 的结构化权限字段位置，未发现现有 `RuleSafeCommand`。
- 2026-08-25：已按法定精确路径创建计划文档 `docs/superpowers/plans/b249-plan.md`；内容覆盖 Task 0 前置、四个实现 task、Task 5 协调者门禁、接口签名、测试范围、日志/注释、五项自审与收口命令。当前尚未运行计划文档门禁，也尚未提交。
- 2026-08-25：命令 `rg -n "permissionResponseTimeout|perm.*Timeout|context.WithTimeout\\(context.Background\\(\\)" internal/agentd/manager.go; sed -n '1880,1920p' internal/agentd/manager.go` 退出 0；输出显示当前自动放行使用 `unaryCtx(context.Background())`，不存在 `permissionResponseTimeout`。
- 2026-08-25：命令 `sed -n '235,315p' internal/agentd/manager.go` 退出 0；输出显示 `NewManager` 使用 `return &Manager{...}` 字面量，当前未设置 `appendEvent` 字段；另有 `internal/agentd/permgate_wire_test.go:112` 的最小 `Manager{gate:nil,log:slog.Default()}` literal 需要在实现时避免触发权限审计路径。
- 2026-08-25：命令 `sed -n '120,160p' internal/executor/executor.go; rg -n "func \\(m \\*Manager\\) handlePermission|judgePermission\\(|type permissionPayload|unaryCtx" internal/agentd/manager.go | head -30; sed -n '1718,1768p' internal/agentd/manager.go; sed -n '1818,1878p' internal/agentd/manager.go` 退出 0；输出确认 `executor.PermRequest` 精确字段为 `Tool/Command/Paths`，当前 `handlePermission` 丢弃 Verdict 且 `judgePermission` 只组装 Workdir/TaskDir，当前 response context helper 名为 `unaryCtx`。
- 2026-08-25：计划门禁命令 `awk '/^```/{n++} END{print "fence_count=" n, "balanced=" (n%2==0)}' docs/superpowers/plans/b249-plan.md` 实跑输出 `fence_count=38 balanced=1`；占位符扫描 `rg -n "permissionResponseTimeout|TBD|TODO|同 Task [0-9]|加适当错误处理|适当的错误处理" docs/superpowers/plans/b249-plan.md` 无输出；计划 `git diff --check` 无输出。计划与台账仍为未跟踪文件，尚未提交。
- 2026-08-25：为复核 Task 3 精确基线命令，运行 `go test ./internal/agentd ./internal/proto ./internal/client -count=1`，退出码 0；原始输出：`ok  github.com/Xsxdot/handoff/internal/agentd 134.197s`、`ok  github.com/Xsxdot/handoff/internal/proto 0.003s`、`ok  github.com/Xsxdot/handoff/internal/client 9.319s`。
- 2026-08-25：为复核 Task 4 精确基线命令，运行 `go test ./internal/store ./internal/proto ./internal/client ./internal/ledgermirror -count=1`，退出码 0；原始输出：`ok  github.com/Xsxdot/handoff/internal/store 5.089s`、`ok  github.com/Xsxdot/handoff/internal/proto 0.003s`、`ok  github.com/Xsxdot/handoff/internal/client 9.357s`、`ok  github.com/Xsxdot/handoff/internal/ledgermirror 2.252s`。
- 2026-08-25：计划修订：为 Task 1–5 补逐 task Interfaces（Consumes/Produces），将 Manager 测试代码改为现有 `newTestManager`/`chanAdapter`/`mustEvents` harness，响应上下文改为已核实的 `unaryCtx`，并校正 Task 3/4 文件计数。未写实现代码。
- 2026-08-25：尝试执行 `git add docs/superpowers/plans/b249-plan.md docs/ledgers/2026-08-25-b249-ledger.md`；原始失败输出：`fatal: Unable to create '/root/.handoff/repos/handoff/.git/worktrees/a1b0469b/index.lock': Read-only file system`。后续 `git diff --cached --check`、`git diff --cached --stat`、`git diff --cached --name-only` 未产生输出；工作树仍显示两个未跟踪文件。
- 2026-08-25：使用提升权限重试同一 `git add` 命令，退出码 0，原始输出为空；计划与台账现已进入 Git index。随后追加本行，因此台账需再次 `git add`。
- 2026-08-25：暂存内容的 `git diff --cached --check` 暴露计划文档 6 行 Markdown 末尾空格；`rg -n '[[:blank:]]$' docs/superpowers/plans/b249-plan.md docs/ledgers/2026-08-25-b249-ledger.md` 原始输出列出计划第 3、4、5、6、617、700 行。已用 apply_patch 删除这些空格，未修改语义；需重新暂存。
- 2026-08-25：重新 `git add` 后运行 `git diff --cached --check`，退出码 0、原始输出为空；暂存统计为台账 38 行、计划 725 行，共 763 行新增；当前分支仍为 `cards/B249-charter-3`，仅两份本卡文件 staged。
- 2026-08-25：执行 `git commit -m "docs(b249): add implementation plan"`，退出码 0；原始输出：`[cards/B249-charter-3 de2a4105] docs(b249): add implementation plan`、`2 files changed, 764 insertions(+)`，并创建两份法定产出文件。
- 2026-08-25：提交后复核命令 `git diff --check` 无输出；占位符扫描无输出；`awk '/^```/{n++} END{print "fence_count=" n, "balanced=" (n%2==0)}' docs/superpowers/plans/b249-plan.md` 输出 `fence_count=38 balanced=1`；`git status --short --branch` 输出当前分支和台账单文件修改，`git rev-parse --short HEAD` 输出 `de2a4105`。
- 2026-08-25：执行 `git add docs/ledgers/2026-08-25-b249-ledger.md && git diff --cached --check && git commit -m "docs(b249): record plan validation"`；各命令退出码 0，commit 原始输出：`[cards/B249-charter-3 e7e4dc9a] docs(b249): record plan validation`、`1 file changed, 2 insertions(+)`。下一步只记录本行并完成最终收口提交。
- 2026-08-25：执行最终收口提交前复核：`git status --short --branch` 原始输出为 `## cards/B249-charter-3`；`git rev-parse --short HEAD` 输出 `3aba7635`；`git log -3 --oneline` 输出 `3aba7635 docs(b249): finalize ledger`、`e7e4dc9a docs(b249): record plan validation`、`de2a4105 docs(b249): add implementation plan`；`git diff --check` 无输出；HEAD stat 为 `3aba7635 docs(b249): finalize ledger`、台账 1 行变更。已确认工作树干净，以下仅将本事实写入台账并提交，不再改计划。
