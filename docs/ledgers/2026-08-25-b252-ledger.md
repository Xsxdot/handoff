# B252 实现节点台账

职责：记录本卡实现节点对 codex `requestApproval` 档位字段的取证、命令原文与裁决依据。
边界：不把 codex 二进制内部的 `sandbox_permissions` 枚举臆造为 app-server 线协议字段；若
真实/录制报文没有该字段，按 spec 退回，不改生产解析逻辑。

- 2026-08-25：`git status --short --branch` 原始输出为 `## cards/B252-charter`，工作树干净。
- 2026-08-25：读取 `internal/executor/codex/perm.go`，`commandApproval` 当前字段为 `ItemID/ThreadID/TurnID/Command/Cwd/CommandActions`，`parseCommandApproval` 只做 JSON 反序列化与 `itemId` 非空校验。
- 2026-08-25：读取 `docs/superpowers/specs/2026-08-09-handoff-codex-adapter-design.md` §1.2，仓内真实录制的 `item/commandExecution/requestApproval` params 含 `threadId/turnId/itemId/startedAtMs/environmentId/command/cwd/commandActions/proposedExecpolicyAmendment/availableDecisions`，未含 `sandbox_permissions`、`sandboxPermissions`、`sandbox` 或 `require_escalated`。
- 2026-08-25：读取 `internal/executor/codex/appserver_test.go:307` 与 `:378`，仓内 requestApproval 测试夹具只含 `itemId`，没有档位字段。
- 2026-08-25：上游 Codex app-server README 的 command approval 协议说明将 `additionalPermissions` 标为实验性“每命令沙箱追加权限”载荷；该字段表示 `with_additional_permissions` 方向，不能据此推出 `require_escalated` 脱沙箱字段存在。
- 2026-08-25：上游 Codex `ExecCommandApprovalParams` schema（浏览核对）列出 `approvalId/callId/command/conversationId/cwd/parsedCmd/reason`，未列 `sandbox_permissions` 或 `require_escalated`。
- 2026-08-25：上游 Codex `SandboxPermissions` 枚举取证确认 `use_default/with_additional_permissions/require_escalated` 是执行器内部/工具请求概念；不能替代 app-server `requestApproval` 线协议夹具。
- 2026-08-25：因此没有合法的“带 `require_escalated` 档位的 requestApproval 夹具”，不能按 spec 要求编造字段或写一个无真实协议依据的红测；本卡最小档在当前可核对的线协议上不可实施，退回 spec。
- 2026-08-25：命令 `GOMODCACHE=/root/.handoff/tmp/dd7819a7/gomodcache go test ./internal/executor/codex/... -count=1` 原始输出：`ok  github.com/Xsxdot/handoff/internal/executor/codex 5.982s`。
- 2026-08-25：命令 `GOMODCACHE=/root/.handoff/tmp/dd7819a7/gomodcache GOCACHE=/root/.handoff/tmp/dd7819a7/gocache go test ./... -run '^$' -count=1` 原始输出以 `ok ... [no tests to run]` 覆盖根包、cmd 与全部 internal 包，命令退出码 `0`。
