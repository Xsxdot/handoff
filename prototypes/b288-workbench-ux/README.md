# B288 工作台 UX 原型

Fork 自 `prototypes/b285-workbench-polish`。对照五条验收：

1. 打开任务后 tab / 左栏已打开行仍显示任务原名，不再变成 `TUI · xxxx`
2. 拖到黑底终端窗格时，左/右/上/下/中半区落点阴影足够可见
3. 三列均分可用宽度，不出现横向滚动
4. 任务组只留未终态 + 已打开项；completed/failed 进「已结束」（默认收起）
5. 折叠箭头在标题右侧；项目 / 任务·目录 / 机器 / 行用字重、缩进、分组标题分开
