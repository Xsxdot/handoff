# B292 编制并发形态（fork）

从 `prototypes/base/` 复制。只改设置页「自动化」分区：小队不再设总并发，每个成员载体一行政策位；两个 opencode 载体示范「模型列空 = CLI 默认」和「模型列填 flash」。

直开：

```text
prototypes/b292-squad-concurrency/pages/settings.html
```

浏览器会落到「自动化」。点「建小队 / 编辑」看成员行。
