# AI 开发控制台上下文

这个上下文定义桌面端对项目、工作树和 handoff 任务的统一语言。桌面端是一个常驻的观察与控制入口，核心对象必须能精确指向本地或远程开发机上的具体工作空间。桌面端展示运行时真实存在的任务，不负责限制同一工作树上的并发数量。

## 工作空间

**项目**：用户管理的一个逻辑代码仓库，至少有一个项目位置。一个项目可以仅位于本机、仅位于一台远程开发机，或同时位于本机和一台远程开发机；不能绑定多台远程开发机。
_避免_：把“至少一个位置”误解为“只能一个位置”，或让一个项目绑定多台远程开发机。

**项目位置**：一个项目在某个代码宿主上的根目录及其工作树集合。项目位置是本机位置或远程位置；同一项目最多各有一个。本机位置与远程位置彼此独立，不能假设目录内容或运行时状态天然同步。
_避免_：副本、实例（这些词不能表达它位于哪台机器、是否可操作）。

**代码宿主**：承载项目位置及其文件、终端和任务运行时的机器，可以是本机或一台已配对的远程开发机。一个 handoff 任务必须绑定一个具体项目位置，因此执行目标始终唯一。
_避免_：把项目本身当作任务调度目标；当项目有两个位置时，必须继续指明项目位置或工作树。

**项目根目录**：一个项目位置在其代码宿主上的入口目录。每个被选择的位置都必须有自己的项目根目录。
_避免_：只记录项目级目录而不区分本机与远程位置。

**开发机**：可以承载项目远程位置的机器，也承载 handoff 执行器、Env 文件和自动审批者配置。一个项目最多选择一台开发机。本机拥有同样的文件与运行时能力，但不作为“远程开发机”展示。
_避免_：服务器、节点（除非讨论基础设施）。

**工作树**：项目在某个项目位置中的一个具体可操作代码目录，包含主目录和由分支创建的独立目录。
_避免_：只用分支代替工作树；分支是版本关系，工作树是文件与运行时边界。

## 执行对象

**开发机代理**：运行在开发机上的 `agentd`，负责让桌面端或其他客户端连接该机器，并管理该机上的任务与执行器生命周期。
_避免_：把开发机代理称为执行器或任务智能体。

**执行器**：实际执行 handoff 任务的引擎，例如 opencode、Claude Code 或 grok。执行器属于任务运行时，不等同于承载它的开发机代理。
_避免_：把执行器和 agentd 统称为智能体。

**Env 文件**：存放在一台代码宿主的 handoff 目录中的一份 `.env` 文件。一台机器可以有多份 Env 文件，每份文件包含多个环境变量；执行器和自动审批者可以各自选择其中一份，也可以不选择。
_避免_：把 Env 文件建模成全局、机器、项目和任务之间逐级覆盖的一张变量表。

**执行器配置**：某台机器上每个执行器的启用状态，以及该执行器启动任务时可选加载的 Env 文件。配置属于机器，不属于桌面端全局设置。
_避免_：在全局设置中启用一个执行器后假设所有机器都具备相同能力与凭据。

**自动审批者**：某台机器为 handoff 分级审批配置的一次轻量执行器调用。handoff 先把审批请求交给自动审批者；只有自动审批者无法确定时才向上升级到用户。自动审批者可选择该机器的一份 Env 文件，也可以不配置。
_避免_：把审批者理解为某个人、项目维护者或桌面端当前用户。

桌面端管理已配对的开发机代理及其状态、能力、连接和重启；远程安装或升级代理不属于桌面端的直接高权限操作。

**handoff 任务**：在一个具体工作树中运行的、可被继续、审批、回答、停止和审核的执行会话。
_避免_：把一次提问、一次权限请求或一个 executor 进程单独称为任务。

**任务卡片**：任务在桌面端中的控制单元，展示任务状态、工作树上下文、执行器进度，以及等待审批或回答等需要用户介入的提示。
卡片负责提醒和进入任务现场；完整审批与回答在任务现场完成，停止可以快捷触发但必须二次确认。
_避免_：把任务卡片等同于纯看板条目；它必须能进入任务现场并执行动作。

**任务现场**：用户打开任务后看到的实时执行画面、上下文、待处理工单和相关文件入口。
任务现场默认内嵌在桌面端主区域；支持原生 TUI 的 executor 使用原生画面，其他 executor 使用桌面端统一 TUI，外部终端 attach 只是辅助入口。
_避免_：把任务现场只理解为日志查看器，或把外部终端作为唯一入口。

**全局任务面板**：跨项目汇总 handoff 任务的看板视图，卡片携带项目、项目位置、工作树和执行器上下文。项目筛选支持下拉多选，并可从项目树进入带过滤条件的同一视图。
_避免_：为每个项目复制一套互不关联的任务看板。

**工作台**：桌面端的主要操作区域，以终端标签页为基本单位。用户从项目或工作树打开终端，在标签页中输入命令、进入 TUI，并可将一个标签页拆分成左右视图；全局任务面板是独立页面，不占用工作台的终端语义。
_避免_：把工作台做成任务看板的详情页，或把每个任务强制限制成一个标签页。

**终端标签页**：工作台中的持久会话，绑定一个项目位置和工作树，并保留自己的当前目录、TUI 会话和拆分布局。再次从导航打开同一工作树时，优先聚焦已有标签页。
_避免_：把标签页只当成一次性终端窗口，或用当前选中的项目覆盖它原本的工作树身份。

终端标签页的拆分面板是独立会话，但共享该标签页绑定的项目位置和工作树上下文；一个面板可以运行 handoff TUI，另一个面板可以运行命令或查看日志。
同一桌面端窗口中，一个 handoff 任务复用一个主要控制标签页；多个桌面端实例可以分别观察或控制同一任务。

## 产品边界

**桌面端控制台**：跨本地与远程项目位置汇总项目、工作树和 handoff 任务的观察与控制入口。它可以进入任务现场并执行用户动作，但不拥有任务调度、工作树加锁或并发裁决责任。
_避免_：把桌面端当成执行器调度器或代码工作树的唯一所有者。

**项目位置不可用**：远程项目位置所在的开发机断开或无法连接时，该位置的文件树、文件内容、Env 文件、任务现场和操作入口均不可用；同一项目的本机位置仍可独立使用。除非另行引入并明确标记的缓存快照，否则不把断线状态降级成只读。
项目在左侧导航中仍保留已知的远程位置、工作树和任务，并标记连接状态与最后在线时间；重连后才恢复该位置的实时数据和操作能力。
_避免_：离线只读、缓存内容（除非产品明确区分实时内容与历史快照）。
